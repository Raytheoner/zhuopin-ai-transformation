---
status: 待审（`OP-0907-X`【CC】2026-09-07 起草；队列 §一 #358 明写「design 审通过前不得实现」）
---

# design · 平台 audit 留存与归档策略

> 队列 §一 `#358`｜起草 `OP-0907-X`【CC】2026-09-07｜派出线 ＝ Cowork 环境总线 `OP-0906-W`（批 B-0907_R）
> 🔴 **本件是草案，不是已审件。** Decisions D1–D9 是**建议**，Open Questions O1–O3 **必须由 Shao Peishen 本人拍**（O1 属合规红线，孙涛不可代——见 `.claude/rules/场景建造与合规.md` §二 决策代理边界）。
> 审材料 ＝ 本件 ＋ `proposal.md`（十条实测事实 ＋ 十二条 `git check-ignore` 输出）。

---

## 🔴 审前置：两条必须先有真实输出，缺则本件不进审

| # | 项 | 为什么它是**审的**前置而不是**实现的**前置 |
|---|---|---|
| **P1** | `.51` 上 `baoguan_access_trace.jsonl`（及同目录其余审计类文件）的**实际大小与行数**，`Get-ChildItem` 真实输出 | 队列行转述的 309 MB 若属实且确为 `*_access_trace.jsonl`，则它是 **`TRACE` 类**，D5 的阈值默认值应就着它定；若那 309 MB 其实落在 `baoguan_audit.jsonl`（`COMPLIANCE` 类）上，**D5 的整个默认值和 D7 的迁移优先级都要重排**。⇒ 这不是实现细节，是定 Decision 的输入 |
| **P2** | `.51` 该盘剩余磁盘空间 | 决定「先封存后压缩」还是「封存即压缩」（D6）。若余量充裕，段文件保持未压缩最利于取证与 grep；若吃紧，须封存即 gzip，而 gzip 会让"逐字节比对"多一层解压步骤 |

**执行方式**：两条均为 `.51` **只读**取证（`ssh` + `Get-ChildItem` / `Get-PSDrive`），⏭️ **属收口批**（`zhuopin-lan-closeout`），本 session 不自跑、不代跑。
🔴 **在 P1/P2 有真实输出之前，D5／D6 的数值一律视为占位**，审时不得当已定值照准。

---

## Context（本件之前已经确定的事）

三件事已成文，本件不重新论证，只列出以免被当作开放项：

1. **红线 2 的措辞是「append-only，3 年留存」**——五处载体一字不差（`.claude/rules/场景建造与合规.md` §三.2、全景规划 §4.2、ISO26262 规范草案、QD-B／O1／O2／SC11 场景 CLAUDE.md）。
2. **访问痕迹与合规审计物理分离**是既有决定（`connector_audit.py` 首部 D2，「Paul 拍板」）。本件的三分类建立在它之上。
3. **ClickHouse 是 9 月的汇聚 sink，JSONL 是写入路径**（C3 冲突消除方案，`Phase1-基础设施与智能体架构设计.md:37`）。`ClickHouseSink` 至今 `NotImplementedError`。

---

## Decisions（九条 · 建议待审）

| # | Decision | 理由 / 出处 |
|---|---|---|
| **D1** | **留存类别三分：`COMPLIANCE`／`TRACE`／`OPS`，由写方在构造 sink 时显式声明；工具 MUST NOT 从文件名、路径或内容推断类别，MUST NOT 对未声明类别的文件执行任何删除或封存（fail-closed）** | 实测：`fi2_audit.jsonl`／`fi2_access_trace.jsonl`／`fi2_http_requests.jsonl`／`pending_queue_appends.jsonl` 同在一个 `reports/` 下，**命名上没有任何机器可判的差别**。任何"按名字猜"的实现，第一次猜错就是删证据。⇒ **fail-closed 是本包最重要的一条**：认不出来就什么都不做，宁可文件涨大 |
| **D2** | **归档 ＝ 滚动封存（roll-and-seal）：热文件整份改名为不可变段，另起新热文件。MUST NOT 从任何文件中移动、改写或删除任何一行** | 「换本子，不撕页」。这一条把"归档"和"删证据"在**机制上**分开了——因为归档动作根本不具备写旧文件的能力 |
| **D3** | 🔴 **封存 MUST NOT 重算任何一行的 `prev_hash`。段文件是原热文件的逐字节副本（rename，非 rewrite）** | **本项目 2026-07-28 已经重算过一次，且 `verify_chain()` 通过**（CHANGELOG 第 43 行原文）。⇒ **重算过的链，验证结果永远是 ✅** —— 哈希链对"由持有写权限者执行的整理动作"零防御力。这是本包所有约束里唯一一条**已经出过事**的 |
| **D4** | **段封条 `<段名>.seal.json`：`{segment, line_count, first_line_sha256, last_line_sha256, first_ts, last_ts, sealed_at, sealed_by, prev_segment_seal_sha256}`；封条自身按 `prev_segment_seal_sha256` 成链。新热文件首行 MUST 是一条 `action="audit_segment_continuation"` 的记录，其 `prev_hash` ＝ 上一段末行的真实 sha256** | 封条解决"段被整个删掉"（无封条即断链）；续接记录解决"段边界处链断成两截"。**两者缺一都留缺口**：只有续接记录时，删掉整段仍可让剩下的链自洽 |
| **D5** | **封存触发阈值按类别取默认值：`COMPLIANCE` ＝ 行数 ≥ 50,000 **或** 跨自然月；`TRACE` ＝ 行数 ≥ 200,000 或跨自然月；`OPS` ＝ 跨自然月。三者均可按 sink 覆盖** | ⚠️ **数值待 P1 回来后定，现为占位。** 取"或跨自然月"是因为**月是可举证的最小自然单位**——审计员问"2026 年 7 月的记录在哪"，答案应当是一个文件名，而不是"在第 3 和第 4 段里各一半" |
| **D6** | **段文件默认不压缩；是否封存即 gzip 由 P2（磁盘余量）定** | 未压缩段可直接 `grep`／逐字节比对，取证成本最低。⚠️ 待 P2 |
| **D7** | **封存 MUST 在写方进程内、`JsonlSink` 的写锁内完成，MUST NOT 由外部定时任务／sweep 对运行中的审计文件执行** | 2026-06-13 审计报告 P1 已实测：`JsonlSink` 的锁与 `prev_hash` 缓存**仅进程内有效**，两个进程写同一 JSONL 必产生断链误报，**且无法与真篡改区分**。⇒ 若让 sweep 或 cron 去 rename 一个服务正在追加的文件，制造出的正是"无法与真篡改区分"的断链。**在锁内做，这个竞态根本不存在** |
| **D8** | **封存段是未来 ClickHouse 迁移的天然导入单元**：每段一次批量导入，封条的 `line_count` ＋ `last_line_sha256` 即导入校验凭据。本包 MUST NOT 依赖 `ClickHouseSink`（它仍是 `NotImplementedError`） | 全景规划 §4.2 的 ClickHouse 是**汇聚**层，不是留存层。⇒ 迁移与否都不影响本包成立；反过来本包让迁移变得可校验 |
| **D9** | 🔴 **封存 MUST NOT 跨文件合并。**每个热文件封存为自己独立的段序列；MUST NOT 把多个场景／多个 OEM 上下文的审计并成一份 | **2026-07-28 那次做的恰恰是"把两份归并成一份"**，若实现者照先例走，就会在 OEM 隔离（红线 3）上开一个洞。须落断言测试，不得只写在文档里 |

---

## Open Questions（三条 · 🔴 均须 Shao Peishen 本人答，均不设默认项）

### O1 · 满 3 年之后怎么办？「3 年」是下限还是有效期？

**现状**：全项目五处载体一律止于「3 年留存」四个字，**没有任何一处说过满 3 年后能不能删**。

| 选项 | 选它会发生什么 | 代价 |
|---|---|---|
| **(a) 3 年是下限，满 3 年后仍不删**（🔎 本件推荐） | 实现最简：`COMPLIANCE` 类**永不删除**，代码里根本没有那个删除函数（同口径点台账 `LedgerStore` 无 `delete()` 的做法）。磁盘按当前速率线性增长 | 磁盘长期只涨不落。按本机实测最大热文件 2.58 MB／5,170 行的量级，3 年后仍属 GB 以内，**代价可接受** |
| (b) 3 年是有效期，满 3 年可删，删除须留痕并经他签认 | 磁盘可回收 | 🔴 需要新造一条"合规审计的删除授权"路径，**而这条路径一旦存在，就是本包想堵的那个洞**。且 IATF 审核时"我们删过审计"这句话本身要解释 |
| (c) 本包不决，`COMPLIANCE` 先按永不删除实现，满 3 年前另立行再议 | 与 (a) 的代码完全相同，区别只在**留不留一个待办** | 多一条 3 年后到期的待办 |

🔴 **不设默认项**，理由（按根 CLAUDE.md §5「默认项只在两个前提下成立」自问两条）：① 他不答，谁去执行？——**没有人**，本包卡在这里；② 「不答」的代价是"停在原地"还是"错误继续发生"？——**是前者**（现状是不删，继续不删不产生新错误）。⇒ 停得起，但**这是合规红线，孙涛不可代**，须他本人答。

### O2 · 链尾外部锚定：封条摘要要不要进 git？

**背景**：2026-06-13 全盘审计 P0 第二条整改项原文——「链尾哈希定期外部锚定（另一主机／ClickHouse／每日企微播报链尾指纹），否则**持有写权限者可整链重算，IATF 3 年可信留存不成立**」。至今无实现。外部评审（`supplychain收割与平台底座评审清单.md:55`）点的是同一处。

| 选项 | 选它会发生什么 | 代价 |
|---|---|---|
| **(a) 封条摘要进 git**，落 `3-治理与合规/审计链锚点/` 下的 `.md` 或 `.json`（实测该两种形态入库、`.jsonl` 与 `reports/` 下一律被忽略）（🔎 本件推荐） | git 提交本身是链式且已推 GitHub ⇒ 审计文件的写方**改不动**它。零新增基础设施 | 审计指纹（哈希／行数／时刻）出现在一个更多人可见的地方。⚠️ **无任何业务数据**——封条里没有 `decision`／`evaluator`／`payload` |
| (b) 每日企微播报链尾指纹 | 锚在群消息里，人能看见 | 依赖机器人链路存活；群消息可被撤回；无结构化校验 |
| (c) 等 ClickHouse 迁移时一并做 | 少做一件事 | 🔴 `ClickHouseSink` 至今 `NotImplementedError`、迁移无排期 ⇒ **等于继续不做**，而 P0 整改项已挂了近三个月 |
| (d) 本包不做锚定，只做封存 | 本包范围最小 | 🔴 **封存本身会变得可伪造**：持有写权限者可以重造一整套段 ＋ 封条链，跨段校验照样 ✅。⇒ **选 (d) 等于本包最核心的防护力打对折** |

🔴 **不设默认项**：①「不答谁执行」——无人；②「不答的代价」——**是后者，错误在继续发生**（P0 整改项自 2026-06-13 挂账至今 ≈ 86 天，期间任何持有写权限者都可整链重算而不留痕）。⇒ 按根 CLAUDE.md §5，此项**须标「本项无默认，须你明确答复」并登记 §四**。

### O3 · WORM／数字签名要不要进本包？

外部评审要的是"3 年留存、可归责、**不可抵赖**"。本包做到的是**可检测**（任何删改必然暴露），不是**不可能**（仍无法阻止有 root 的人重造一切 —— O2(a) 的 git 锚点把这个门槛抬到了"还得同时改 GitHub 历史"，但仍非密码学不可抵赖）。

| 选项 | 选它会发生什么 | 代价 |
|---|---|---|
| **(a) 不进本包，另立行**（🔎 本件推荐） | 本包按期交付；不可抵赖另开题（需签名密钥管理／独立日志主机，是一个独立工程） | 外部评审那条挂账继续挂，但**从"完全没防护"变成"有可检测性 ＋ git 锚"** |
| (b) 并入本包 | 一次做全 | 🔴 密钥管理牵出「凭据形态」整条线（参见 `openspec/specs/secrets-provider`），本包范围翻倍、design 审拖长；且与 O2 高度耦合 |

🔴 **不设默认项**：本项若不答，本包**仍可交付**（按 (a) 走），但**范围边界未经他确认**，属"建议未获答复须再次提醒"一类。

---

## 为什么本包三个问题都不设默认项（方法留痕）

根 CLAUDE.md §5 的判据是两问：**① 他不答，谁去执行？② 「不答」的代价是"停在原地"还是"错误继续发生"？**

- O1：无执行者、代价是停在原地，**但属合规红线**（孙涛不可代）⇒ 不设默认。
- O2：无执行者、**代价是错误在继续发生**（第二问答"后者"）⇒ 按纪律**必须**不设默认并登记 §四。
- O3：无执行者、代价是范围边界未确认 ⇒ 不设默认。

🔑 三条都答"无执行者"不是巧合：**留存口径的执行者只能是定口径的人**。这也是为什么队列 #358 把题目从"`.51` 日志轮转"抬到"平台留存策略"是对的——前者有执行者（运维），后者没有。

---

## 三条容易被顺手做错的地方（写给 apply 的 CC）

**⑴ 不要让 `verify_chain()` 的 genesis 豁免"每段首行都放行"。**
引入续接记录后，第 2 段起的热文件首行**看起来很像 genesis**。若实现者顺手把 `idx==1` 的豁免改成"每个段的第 1 行都豁免"，**2026-08-04 `fix-a-security-compliance-p0` 堵上的绕过就原样回来了**（那个绕过是：删光全文件 `prev_hash` 字段重写，整链即通过）。
⇒ 正确形态：**只有第 1 段的第 1 行可以是 genesis；第 2 段起首行 MUST 是续接记录且 `prev_hash` 非空**，缺则判断链。须落成正反两组断言。

**⑵ 不要用「重命名 ＋ 新建」的顺序做封存。**
若先 `rename` 再 `open` 新文件，两步之间服务若被杀，**热文件不存在但续接记录还没写** ⇒ 下次启动会当 genesis 重新开链，**段间链就此断掉且事后无法区分是崩溃还是篡改**。
⇒ 正确顺序：**锁内**先算出上一段末行 sha256 → 写好续接记录到临时新文件 → 原子 rename 旧文件为段 → 原子 rename 临时文件为热文件 → 写封条。任何一步失败即整体回退，**MUST NOT 留下"半封存"状态**。

**⑶ 不要在真实审计文件上做首次验证。**
tasks §1–§3 全部在 `tmp` 构造文件上跑，**四条反向对照组必须先全绿**；§4 才碰本机真实文件，**且只做只读比对，不执行封存**。
🔑 成因：这是本项目反复栽的同一处——2026-08-28 环境体检自己先读了 `wecom-service-home` worktree 里那份**停在 08-04 的同名诱饵**，已写下「审计可能已断」的结论，靠另一条对不上的旁证才揪回来。**审计文件在本仓库有已知的同名多份问题**（主工作区 vs worktree），动它之前先确认动的是哪一份。

---

## 与 9 月 ClickHouse 迁移的衔接

| 问题 | 结论 |
|---|---|
| 本包是否阻塞迁移？ | **否**。封存段是 rename 出来的原始 JSONL，格式不变 |
| 迁移是否让本包作废？ | **否**。ClickHouse 是汇聚查询层；**写入路径仍是 JSONL**（C3 决定），热文件仍会涨 |
| 迁移时怎么用上本包？ | 每个封存段 ＝ 一次批量导入；封条的 `line_count` ＋ `last_line_sha256` ＝ 导入完整性校验凭据。**导完不删段文件**（D2） |
| ClickHouse 能不能当 O2 的锚？ | 能，但它自己也在 `.51` 上、也由同一批人管 ⇒ **不构成独立锚**。git／GitHub 才是异主机异管理域的锚 |

---

## 审记录

**待 Shao Peishen 审。** 审时须先看 P1／P2 的真实输出（否则 D5／D6 是占位值），再逐条裁 D1–D9 与 O1–O3。
