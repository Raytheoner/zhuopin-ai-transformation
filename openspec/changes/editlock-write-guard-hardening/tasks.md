# editlock-write-guard-hardening Tasks

> 🔴 **本文件在 design 审通过前不得开工。** 0.x 是前置闸，逐条过完才进 1.x。
> 执行环境：**CC**（写生产码、跑测试、自行 commit+push，一任务一 worktree）。

## 0. 前置闸（design 审后、动手前）

- [ ] 0.1 确认 design 审结论已回填队列 §一 `#455` 行（决策点 ②③④⑤ 的拍板结果必须是白纸黑字，不凭记忆）
- [ ] 0.2 用 `工具-队列查询.py --digest` 核对 `0-学习与工具/工具-共享文档编辑锁.py` 是否有其它在途 openspec 变更包同时改动 `cmd_edit_row`/`cmd_append_row`（design「触碰区核对」列的前置检查），若有且未合入 master，停手回报
- [ ] 0.3 建 worktree，确认 `pip show zhuopin_platform` 的 `Editable project location` 指向何处（#98 已记的静默漂移陷阱），并在收工报告里写明

## 1. 取证与口径（🔴 必须先于实现）

- [ ] 1.1 白盒读 `queue_table.py::_mask_backtick_spans`／`split_row_cells`／`has_bare_pipe` 与 `工具-共享文档编辑锁.py::cmd_edit_row`／`cmd_append_row`（约 L1940-2110），确认新增校验的挂载点
- [ ] 1.2 🔴 **现网全量实测**：对两份队列文件（`跨桌任务队列-机制环境.md`／`跨桌任务队列-业务场景.md`）全文提取所有反引号跨度，逐一核实是否均能被"游程配对"算法正确判定为闭合；如实记录是否发现除 `#337`/`#422` 外的其它已塌列历史行（design 决策点⑤遗留问题），发现即登记队列行
- [ ] 1.3 🔴 对 1.2 的取证结果，专门核实"是否存在故意写孤立反引号（不构成 code span）"这种用法（design 决策点②的前提假设），零命中也要如实记录"零命中"而非"不存在"
- [ ] 1.4 核实 `工具-队列结构lint.py`／`工具-落库sweep.py`／`工具-队列查询.py` 是否消费 `edit-row`/`append-row` 的返回码或 stdout 格式（proposal Impact 里标为"未核"的那项，在此销账）

## 2. 实现

- [ ] 2.1 `queue_table.py` 新增反引号奇偶判据函数（命名与 docstring 按 design 决策点①，须与 `_mask_backtick_spans` 的"未闭合视为普通文本"语义显式对比说明，避免混用）
- [ ] 2.2 `cmd_edit_row`／`cmd_append_row` 各入口归一后的新值，先过 2.1 的奇偶校验，不通过即拒绝并指出字段名（design 决策点②：拍板结论为拒绝，若他要求加逃生阀在此一并实现）
- [ ] 2.3 `cmd_edit_row` 拼装出最终整行后、写盘前，新增回读校验：`queue_table.split_row_cells(new_line)` 切列数与 `SECTION_COLUMN_COUNTS[section]` 比对，不符拒绝写入
- [ ] 2.4 `cmd_append_row` 现有写入前列数校验（约 L2065-2071）的裸 `str.split("|")` 替换为 `queue_table.split_row_cells`，与 2.3 口径统一
- [ ] 2.5 `cmd_edit_row` 新增 `--repair` 参数：仅跳过对**旧行**（`_locate_row` 返回的 `cells`）列数合法性的前置检查，2.2／2.3 两项校验对本次改动照常执行
- [ ] 2.6 移除 `cmd_edit_row`／`cmd_append_row` 内对 `queue_table.has_bare_pipe(value)` 的前置检查调用（proposal「本次替代哪一项既有校验」——功能性被 2.2/2.3 取代，`has_bare_pipe` 函数本身保留，其它调用方不受影响）
- [ ] 2.7 `append-row` 明确拒绝 `--repair` 参数（spec Scenario「append-row 不接受 --repair 参数」）
- [ ] 2.8 更新文件头说明段与相关函数 docstring，新增本三项校验的条目说明

## 3. 测试

- [ ] 3.1 反引号未闭合的新值 → 拒绝写入，不落盘
- [ ] 3.2 反引号闭合、跨度内含竖线的新值 → 放行（对应 #324 历史场景的回归夹具）
- [ ] 3.3 双反引号游程包裹单反引号内容（反引号总数为奇数但闭合合法）→ 放行（非恒真自证：证明未采用"总数奇偶"简化算法）
- [ ] 3.4 拼装结果回读列数不符 → 拒绝写入，不落盘（对应 #454 历史场景的回归夹具：还原当时截断出的具体字符串，确认新校验会拦下它）
- [ ] 3.5 `append-row` 含合法反引号跨度内竖线的新增行 → 回读列数吻合、放行（证明已替换为反引号感知切列，不再误判）
- [ ] 3.6 未传 `--repair`，已塌列行任何操作（含 `--append`）→ 拒绝
- [ ] 3.7 传 `--repair`，已塌列行改动后合法 → 写入成功，行列数恢复
- [ ] 3.8 传 `--repair`，但新值未过奇偶校验或回读列数仍不符 → 仍拒绝（证明 `--repair` 不放宽结果校验）
- [ ] 3.9 `append-row` 传 `--repair` → 拒绝（参数不适用）
- [ ] 3.10 🔴 **反向用例（非恒真自证）**：临时还原到本包实现前的旧逻辑（或用 mock 关闭新校验），同一批 3.1/3.4 用例必须由"放行"变为本包要修的失效状态——证明拒绝确实来自新增判据，不是判据恒真
- [ ] 3.11 `0-学习与工具` 全量回归绿、零漂移

## 4. 现网验证（不落盘）

- [ ] 4.1 用本包判据对现网两份队列做一次**只读**全量跑，确认命中集与 1.2/1.3 的取证结果一致（不一致即实现与取证脱节，回 2.x）
- [ ] 4.2 若 1.2 发现 `#337`/`#422` 外的其它已塌列历史行，评估是否在本次收口一并用 `--repair` 修复，或另立队列行处置（如实记录选择理由）

## 5. 收口

- [ ] 5.1 队列 §一 `#455` 行回填：实现结论、1.x 取证要点、3.x 覆盖情况、是否已用 `--repair` 修复历史遗留行
- [ ] 5.2 `#324`／`#454` 行内各追加一段指针，说明本包已处置对应缺陷（不改历史正文，只追加新段落）
- [ ] 5.3 §二 批次登记，触发一次 sweep，并看一眼 `reports/sweep-commit.log` 末几行确认真落库（协议〇.8："触发不等于一定会落库"）
- [ ] 5.4 `/opsx:archive editlock-write-guard-hardening -y`（完工即归档；若本次不归档，理由须写进机器认得的三处入口之一）
