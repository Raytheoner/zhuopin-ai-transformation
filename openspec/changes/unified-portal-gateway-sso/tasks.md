## 1. 前置确认（apply 开工前）

- [ ] 1.1 向 Shao Peishen/IT 确认企微自建应用（OAuth 网页授权）凭据（CorpID/AgentID/Secret）是否已在企微管理后台注册；未注册则先提交申请（参考 CLAUDE.md 记录的 U9C MCP 申请先例），并记录预计到位时间
- [ ] 1.2 核对 `.51` 上端口占用表，确认 8090 未被占用；若冲突另选端口并同步更新 design.md 决策 2
- [ ] 1.3 新建独立 worktree/分支（一任务一分支一 worktree，队列协议〇.5），把本变更包收尾时的实现代码限定在该 worktree

## 2. 网关骨架与反向代理路由（对应 spec `portal-gateway-routing`）

- [ ] 2.1 新建网关服务目录（Flask app + `requests` 反向代理），路由表用显式白名单配置（域/场景 → 后端 base URL），初始只含 `/`（占位，尚未接入 8092）
- [ ] 2.2 实现流式转发：请求方法/头/body（含 multipart 文件上传）原样转发到目标后端，响应流式返回，覆盖 QD-B/FI2 已有的 xlsx 上传路径不受影响的验证用例
- [ ] 2.3 实现 `/api/ping` 健康检查端点，豁免鉴权
- [ ] 2.4 实现应急直连说明（仅内部文档/注释记录，不在员工可见页面暴露 8091-8094 直连地址）
- [ ] 2.5 编写单测：新增路由映射后端零改动即生效、单个后端异常不影响其余路由、健康检查豁免鉴权

## 3. 企微 OAuth SSO（对应 spec `portal-wecom-sso`）

- [ ] 3.1 实现 OAuth2 网页授权跳转与回调，用 `code` 换取 userid（依赖任务 1.1 的凭据到位）
- [ ] 3.2 实现 `zp_portal_sso` Cookie 签发与校验（HMAC-SHA256，含 userid + 过期时间，无服务端 session 存储，风格对齐 `simple_gate.py`）
- [ ] 3.3 实现会话过期重新引导登录、主动登出立即失效
- [ ] 3.4 实现应急本地口令通道（仅 Shao Peishen 与运维知悉，独立于 `zp_gate`/`zp_portal_sso`），配置来源与部署方式待定（沿用环境变量惯例）
- [ ] 3.5 编写单测：OAuth 成功/失败路径、userid 与既有 `whitelist.py` 命名空间一致、会话过期与登出、应急通道独立生效（可 mock 企微接口）

## 4. 三层权限判定（对应 spec `portal-permission-model`）

- [ ] 4.1 新建部门映射表文件（userid → 域 → 层级），初始录入决策件 §五决策3 已列出的人员（姚祖怡/唐燕萍/陈忱 + 解植雅/汤易水/朱映桦/李姣龙 等）
- [ ] 4.2 实现三层权限判定中间件：公开只读/域成员/域管理员，映射表未命中 fail-closed 为公开只读
- [ ] 4.3 实现敏感字段判据的应用方式（判定辅助函数/装饰器，供各路由标注哪些字段/操作需要域管理员及以上）
- [ ] 4.4 编写单测：三层各自可见范围、非域成员跨域访问被拒绝、映射表未命中 fail-closed、敏感字段对域成员隐藏对域管理员可见

## 5. 访问日志（对应 spec `portal-access-log`）

- [ ] 5.1 设计访问日志字段（userid/域场景路径/时间戳/鉴权结果），与队列 #112 使用率埋点需求对齐，避免字段返工
- [ ] 5.2 实现日志落盘（含鉴权通过与拒绝两种情形均留痕），确认不记录敏感字段明文
- [ ] 5.3 实现"开发期"标记逻辑，对齐《价值度量指标口径表》§〇bis 起算日规则
- [ ] 5.4 编写单测：通过/拒绝请求均留痕、敏感字段不进日志正文、开发期标记与起算日判定

## 6. 首个真实收编试点：门户首页（8092）（对应 design 决策 3）

- [ ] 6.1 把根路径 `/` 接入网关路由表，代理至命令中心（8092）
- [ ] 6.2 端到端验证：真实企微账号登录 → 换取 userid → 公开只读权限可访问首页 → 访问日志留痕
- [ ] 6.3 程序化访问逐项验证不中断：命令中心自身的 `sync_sales_data.py`、`deploy-server.ps1` 健康检查、任何既有只读取证脚本（对照决策件 §八风险4"最易翻车处"）
- [ ] 6.4 验证网关不可用时，8092 仍可通过原直连端口访问（应急通道生效）

## 7. 部署与收口

- [ ] 7.1 网关注册 Windows 计划任务（`SYSTEM` + `AtStartup`，同 SC8/QD-B/命令中心既有惯例），配失败重启
- [ ] 7.2 真实部署 `.51`，冒烟三件套（`/api/ping`、门户首页 200、一次真实登录+权限判定验证）+ 回滚 SOP（`schtasks /End` + 员工回退旧端口直连）
- [ ] 7.3 更新平台底座 CLAUDE.md / AI 运营指挥中心相关文档，补充网关章节，说明其与 `simple_gate.py` 的共存关系与后续收编计划指针
- [ ] 7.4 队列 #162 回写产出路径 + 状态改"待验收/完成"；在 §四 或队列行内追加线③存量收编（财务→质量→采购）后续任务为"待领"行
- [ ] 7.5 提交 Shao Peishen 审阅本次真实部署与试点验证结果（非仅 design.md 文本审），确认后再评估是否需要正式把 8091/8093/8094 排入线③
- [ ] 7.6 `/opsx:archive` 归档本变更包；commit + push + 收工重跑文档台账
