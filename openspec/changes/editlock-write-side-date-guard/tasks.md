# editlock-write-side-date-guard Tasks

> 🔴 **本文件在 design 审通过前不得开工。** 0.x 是前置闸，逐条过完才进 1.x。
> 执行环境：**CC**（写生产码、跑测试、自行 commit+push，一任务一 worktree）。

## 0. 前置闸（design 审后、动手前）

- [ ] 0.1 确认 `openspec/changes/editlock-hold-scope-and-wip-block` 的状态——若仍在途且未合入 master，**停手并回报**（两包改同一函数 `_validate_release_structure`，见 design「触碰区核对」）
- [ ] 0.2 确认 design 审结论已回填队列 §一 #381 行（决策点 5、6 的拍板结果必须是白纸黑字，不凭记忆）
- [ ] 0.3 建 worktree，确认 `pip show zhuopin_platform` 的 `Editable project location` 指向何处（#98 已记的静默漂移陷阱），并在收工报告里写明

## 1. 取证与口径（🔴 必须先于实现）

- [ ] 1.1 白盒读 `_validate_release_structure` 与 `_diff_touched_rows`，确认新增 ⑪ 的挂载点与 ①～⑩ 口径一致（只对本次改动行生效）
- [ ] 1.2 🔴 **现网全量实测定白名单**：对两份队列文件（`跨桌任务队列-机制环境.md`／`跨桌任务队列-业务场景.md`）**全文**提取所有 `YYYY-MM-DD` 且 > 本机当日的日期，**逐条判定性质**（定时触发／硬期／截止／观察窗口／历史遗留／真错误），产出一张对照表附进本包
  - [ ] 1.2.1 据该表定白名单关键词，**每个关键词旁注明它对应实测表里的哪几条**——不得出现"觉得可能会用到"的关键词
  - [ ] 1.2.2 **如实记录表里有没有"真错误"**（即当年写错、至今没人发现的未来日期）。**有 ⇒ 这本身是一条独立发现，须登记队列行**；没有也要写明"零命中"，并说清这只代表队列这两份文件
- [ ] 1.3 核实 `工具-队列结构lint.py`／`工具-落库sweep.py`／`工具-队列查询.py` 是否消费 `release` 的返回码或 stdout 格式（proposal Impact 里标为"未核"的那项，**在此销账**）

## 2. 实现

- [ ] 2.1 新增私有函数：从行文本提取 `YYYY-MM-DD` 日期串及其上下文窗口
- [ ] 2.2 新增 ⑪ 判定：未来日期 ＋ 不在白名单上下文 ＋ 无 `日期豁免：` ⇒ 计入 `violations`
- [ ] 2.3 比较基准用 `datetime.now().date()`（**MUST NOT** 用 `utcnow()`，见 design「时区与时钟」第 1 条）
- [ ] 2.4 `cmd_release` 新增 `--allow-future-date`（**不携带理由文本**）；仅当该行状态列含 `日期豁免：<理由>` 时生效
- [ ] 2.5 拒绝提示须含：命中的日期、所在行编号、本机当日、以及三条出路各自的确切走法（改日期／落进白名单上下文／写 `日期豁免：` 并加开关）
- [ ] 2.6 更新文件头说明段与 `_validate_release_structure` docstring，新增 ⑪ 条目（现有编号实测止于 ⑩）

## 3. 测试

- [ ] 3.1 正向：新增行含裸未来日期 → release 被拒
- [ ] 3.2 白名单：同一日期落在 `[S:timed=...]` 等白名单上下文内 → 放行
- [ ] 3.3 逃生阀：写 `日期豁免：<理由>` ＋ 传开关 → 放行；**只传开关不写理由 → 仍拒**
- [ ] 3.4 只对本次改动生效：历史行含未来日期、本次未触碰 → 不触发
- [ ] 3.5 🔴 **反向用例（非恒真自证）**：清空白名单后，同一夹具立刻被拒 —— 证明放行不是因为判据恒真
- [ ] 3.6 🔴 **跨日界用例**：冻结时钟到本地 2026-08-24 07:00（UTC 为 08-23）→ 写 `2026-08-24` **必须放行**（它是本地当日，不是未来）
- [ ] 3.7 过去日期一律放行（决策点 2）
- [ ] 3.8 `0-学习与工具` 全量回归绿、零漂移

## 4. 现网验证（不落盘）

- [ ] 4.1 用本包判据对现网两份队列做一次**只读**全量跑，确认命中集与 1.2 的对照表一致（不一致即实现与取证脱节，回 2.x）

## 5. 收口

- [ ] 5.1 队列 §一 #381 子项⑵ 回填：实现结论、1.2 对照表要点、3.x 覆盖情况
- [ ] 5.2 §二 批次登记 ＋ 触发一次 sweep，并**看一眼 `reports/sweep-commit.log` 末几行**确认真落库（协议〇.8 已记："触发不等于一定会落库"）
- [ ] 5.3 `/opsx:archive editlock-write-side-date-guard -y`（完工即归档；若本次不归档，理由须写进机器认得的三处入口之一）

## 6. 🔴 降指针（**独立一步，带前置条件，不得与 5.x 同批做完**）

- [ ] 6.1 **前置**：⑪ 已在生产**真实拒绝过至少一次**并留痕（不是单测）。**未满足即停在此处**——#381 硬约束「先机制在生产真实验活，后降指针」
- [ ] 6.2 满足后：根 `CLAUDE.md` §5 时间戳条**写侧段**降为一句判据 ＋ 指针（拟稿见 proposal「本次退休哪一个既有守卫」）；原文续存 `进度编年-CHANGELOG.md` 附录 F
- [ ] 6.3 🔴 降指针那句**必须写明"机器守的只是队列这一条路径"**——`Last Updated`／接力节标题两类不走编辑锁，仍是人守（proposal「已知残余风险」第 2 条）
- [ ] 6.4 **读侧半条不动**（"引用任何时刻前先答一句 UTC 还是本地"）
- [ ] 6.5 §二 批次登记，并在批次行写明「**本批新入法**」与「**本批变更参数**」（协议〇.8）
