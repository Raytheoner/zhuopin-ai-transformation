# plan-backpressure-scanner Tasks

> ✅ **design 审已过（Shao Peishen 2026-09-07 合审 §4 (a)＝全部决策点按起草方推荐）**，apply 由 `OP-0907-AH`【CC】执行（分支 `claude/op0907ah-plan-scanner-462`，起点 ＝ `claude/op0907j-plan-scanner-462` rebase 到 master）。**D5 ＝ (b)**（出队列行草案、由人落库）｜**D4 ＝ (a) 先行**｜**D7 ＝ (a′)**。
> ⬇️ **下面这段是起草期原文，写于审过之前，原样保留不追改。**
>
> 🛑 **design 未审，全部任务不得开工。** 本次范围（队列 §一 `#462`，2026-09-07 `OP-0907-J`）＝ **propose ＋ design 起草**，明确不建脚本、不改 `工具-落库sweep.py`。
> 🔴 **三条须拍板项**：**D5 无默认项**（"产出队列行"的自动化档位）、D4（前置闸两步走）、D7（"共用判定函数"的落地形式）。**D5 未答之前，第 5 节整节不得开工**；第 1-4 节只读侧不受其阻塞。
> **机制/环境类变更包**｜来源：`#462`（2026-09-02 立，Shao Peishen 当日答 (a)）｜核心逻辑取证件＝`1-转型规划/0-全景路线图/取证件-2026-09-02-规划与执行覆盖差集.md` §三

## 0. apply 前置（不得跳过）

- [x] 0.1 🔴 **design 审已过**，且 D4／D5／D7 三项各有明确答复（D5 无默认项，必须有他的字母答复才算过）【Shao Peishen／design 审】
  - ✅ 2026-09-07 合审材料 §4 答 (a)＝全部按起草方推荐；design.md 顶部已回填审过行。
- [x] 0.2 🔴 **复跑一次本 propose 的四条实测**，确认结论未随仓库变动而失效【CC】
  - ✅ 2026-09-07 `OP-0907-AH` 逐字复跑取证件 §三 原脚本，实测输出：`§一 数据行总数 = 157`（propose 期 151）／`🔴 三处皆无 = 2: ['工程研发 R2', '销售 S4 技术文档']`／`🟡 仅队列有行 = 15`；右边界四条 `'O1' in 'O10 设备' -> True`、`'FI1' in 'FI10-…' -> True`、`'SC1' in 'SC10-…' -> True`、`'S1' in 'SC1 供应商风险' -> False`，**与 propose 逐字一致**。⇒ 行数变了、结论没变，四个判据缺陷全部复现。
- [x] 0.3 确认 `第 13 类` 仍无争号【CC】
  - ✅ `grep -c "第 13 类" 0-学习与工具/工具-落库sweep.py` ＝ 0（退出码 1），当前最高为第 12 类。

## 1. registry 建表（D1）

- [x] 1.1 建 registry JSONL 正本 ＝ `1-转型规划/0-全景路线图/场景registry-规划倒逼扫描器.jsonl`，7 字段，35 行 ＋ 5 行 `retired`（Q1/Q3/Q5/Q7/Q8）【CC】
  - 别名逐条复核过：销售域改为「场景码 ＋ 中文名」并存（取证件那版只写中文名，正是 `S4 技术文档` 与 `技术文档生成` 判成两个结果的根因）；质量域保留 `QD-A`／`QD-B`／`8D`／`立项门禁`／`PPAP`。
  - 🔴 `excludes` 改为对象 `{"phrase", "source"}`，`source` 缺 `#N` 即被 `load_registry` 拒绝——tasks 3.1 的机器守。
- [x] 1.2 🔴 `git check-ignore` 实测 registry 路径**未被忽略**（退出码 1）【CC】
  - 🔴 **首测是被忽略的**：`.gitignore:32 **/*.jsonl` 会整条吞掉它（propose ⑤ 只测了 `reports/` 状态文件，没测 registry 自身）。照 `#439` 口径点台账既例补一条**单文件**否定规则后复测：`git check-ignore <registry>` 退出码 ＝ **1**，`git status -uall` 中可见（`?? 1-转型规划/0-全景路线图/场景registry-规划倒逼扫描器.jsonl`）。详见 design「apply 期实测补注 ⒞」。
- [x] 1.3 一致性校验（registry ↔ 权威排期表，双向差集，CI 硬失败）【CC】
  - `python 0-学习与工具/工具-规划倒逼扫描器.py --lint-registry` ⇒ `✓ 通过（双向差集为空）`，差集非空即退出码 1。
  - 🔴 两处 apply 期修正（design「补注 ⒜」）：按域列取码 ＋ 剥括注 ＋ registry 别名兜底（质量域三场景在表里根本没写码）。
  - [x] 1.3.1 断言：排期表新增一个码而 registry 未跟 ⇒ 校验失败并点名该码（`test_排期表新增码而registry未跟则校验失败并点名`）
  - [x] 1.3.2 断言：单元格内中文/脚注/删除线被改而码集合未变 ⇒ 校验通过（`test_中文标题改写而码集合未变则校验通过`）
  - [x] 1.3.3 断言：`retired` 行不制造永久差集（`test_退休编号不制造永久差集`）

## 2. 三处查判定（D2）

- [x] 2.1 队列侧：只扫 §一 **任务列的标的段**（`target_segment`：首个粗体段，无粗体取前 60 字）【CC】
  - [x] 2.1.1 🔴 断言：`#463` MUST NOT 使销售域五场景被判已承接（`test_销售域五场景不因463被判已承接`），并配两条反向对照：`test_463仍在队列里_否则本条断言是空的`（证明干草堆里确有那根针）＋ `test_反向对照_扫全行则销售域立刻被判已承接`（证明消掉假阴性的是判据、不是运气）
  - [x] 2.1.2 🔴 断言：标的段点名多个场景的行不得因数量达阈值被整体排除——`test_标的段点名多个场景时全部计入` 实测 `#339` 仍是 FI3 的承接证据、`#467` 仍是 SC4 的
  - [x] 2.1.3 断言：标的段之外的沿革/背景引用不构成承接（`test_标的段之外的引用不构成承接`）
- [x] 2.2 `4-数字员工` 侧：只扫 `<部门>/` 下一层目录名，不递归（`test_工程目录只扫一层不递归`）【CC】
- [x] 2.3 `openspec/changes`（含 archive）侧：包名首段、大小写不敏感【CC】
  - 🔴 apply 期修正：归档包带 `YYYY-MM-DD-` 前缀，照字面取首段会得到 `2026` ⇒ **archive 这一处查恒不命中**。已加剥日期前缀（`package_head_segment`）＋ 单测，详见 design「补注 ⒝」。
- [x] 2.4 🔴 **右边界断言**【CC】
  - [x] 2.4.1 `FI1` ∉ `FI10-存货跌价智能分析`；`SC1` ∉ `SC10-BOM评审与物料库管控`；`O1` ∉ `O10`
  - [x] 2.4.2 正向对照 `SC4` ∈ `SC4-合同条款自动提取与审核`
  - [x] 2.4.3 🔴 **反向对照组**：`_alias_regex(..., right_boundary=False)` 后三条全部失败（`test_反向对照_去掉右边界后三条必须全部失败`）
- [x] 2.5 判定记名：每条「已承接」附命中证据（`Evidence` 带 where/ref/alias/excerpt；`test_已承接结论必须记名`）【CC】

## 3. 命名空间对撞（D3）

- [x] 3.1 `excludes` 表填充，**每条附真实来源行号**【CC】
  - 入表 7 条，全部经队列真身核实：R1←`#202`（Champion 门禁）／`#439`（口径点台账）；R3←`#416`／`#447`（followup_readme_bridge）；R4←`#440`（纪律 eval 套件）；R5←`#337`（场景级 R5）／`#391`（R5 门禁）／`#399`,`#373`（R5 分母）；S1←`#259`（收口外部对抗性评审）。
  - 🔴 **两条已知但今日队列零命中的对撞不入表、写进 registry 头注**（守「宁可多报」）：`Q4`←`2027-Q4` 季度写法（`--digest --grep 2027-Q4` ＝ 0/157）、S1/S2/S3←阶段编号「筑基期」（＝ 0/157）。补进去须带行号。
  - [x] 3.1b 🔴 断言：`R1` 不得因 `#202` 被判已承接；`S1` 不得因 `#259` 被判已承接【CC】
- [x] 3.2 🔴 断言：`excludes` 命中**只降级不剔除**（`test_被排除的场景仍留在清单里_只降级不剔除`），另配反向对照 `test_反向对照_没有excludes时R1会被判已承接`【CC】
- [x] 3.3 输出「疑似已承接」时打印是哪条 `excludes` 命中的**及其来源行号**（`test_降级时打印是哪条excludes命中的`）【CC】

## 4. 前置依赖处置（D4，按 (a) 先行）

- [x] 4.1 清单中每个场景附前置总表对应行原文；无行标「前置总表无此场景行」【CC】
- [x] 4.2 🔴 断言：判定路径中不存在任何按 emoji 推断前置就绪的分支【CC】
  - AST 判据落在**比较/`in`/`startswith`** 上（不是"正文里出现 emoji"——渲染层当然要用 🔴/🟡 打三档标）：`test_判定路径里不存在按emoji推断前置的分支`。
- [ ] 4.3 立一条独立队列行：前置总表增设机器可读 `[P:]` 字段并回填约 13 行（(b) 的落点，不在本包内做）【Cowork 总线】
  - ⏳ **本 apply 泳道不写主仓队列**（派单件红线③），已在收工报告里交看护者派发。
- [x] 4.4 spec 内「缺字段非静默降级」条款先落地（`_prereq_machine_field`，缺字段恒 `unknown`、绝不回退关键词/emoji 推断；`test_机器字段缺失时非静默降级为未知`）【CC】

## 5. 输出与队列行产出（D5 ＝ (b)，已拍板）

- [x] 5.1 三档清单输出 ＋ 范围声明文案；`--write-report` 落 `reports/规划倒逼-未承接清单.md`【CC】
- [x] 5.2 接 sweep **第 13 类**，子进程调用（`_run_plan_backpressure_json`／`_check_plan_backpressure`）【CC】
- [x] 5.3 🔴 「本轮是否真跑到」的留痕从第一版就带【CC】
  - `_mark_plan_backpressure_scan` 三态（`scanned`／`unavailable`／`skipped`）＋ `_note_plan_backpressure_skipped` 挂在 `main()` 两个 except 分支上；断言 `test_本轮未巡检与已巡检且无告警在文件里长得不一样`。
- [x] 5.4 指纹抑制（集合不变即静默）＋ 陈化催办（N=7）【CC】
  - 指纹＝未承接场景码集合，复用 `_track_and_alert_standing_state`（24h 再提醒窗）；陈化用 `_plan_backpressure_first_seen`＋`_plan_backpressure_stale_codes`，含「消失后再出现按新问题重新计时」断言。
- [x] 5.5 `suspended` 场景移出常规推送、保留在清单；另按 30 天周期发暂缓复核提醒【CC】
- [x] 5.6 误报与负例**分开计数**；负例 MUST NOT 触发任何自动的判据收紧【CC】
  - `reports/sweep-plan-backpressure-counts.json` 两栏分列；断言 `test_负例不触发任何自动的判据收紧`（sweep 全文不含 registry 路径 ⇒ 没有任何自动改判据的路径）。
- [x] 5.7 🔴 **写侧按拍板档位 (b) 实现 ＝ 出草案、不写队列**（`queue_row_drafts`，随 `--json` 送出，sweep 告警里给取全文命令 ＋ 并入审核提醒）【CC】
  - [x] 5.7.1 🔴 断言：执行扫描器后两份队列**逐字节不变**（sha256 前后比对，`test_跑完扫描后两份队列逐字节不变`）
  - [x] 5.7.2 🔴 断言：`suspended` 场景无论哪一档都不出草案（`test_暂缓场景无论哪一档都不出队列行草案`）
  - [x] 5.7.3 写侧未放开 ⇒ **连"能写"的路径都不存在**：AST 断言模块不 import `subprocess`、无 `subprocess.run/Popen/os.system`、写盘只落 `reports/`（`test_模块内既不调子进程也不import编辑锁`）。日后若放开，须走编辑锁既有命令 ＋ append-only 审计——条款留在 spec，本次不实现。
  - [x] 5.7.4 `--dry-run` 跑满一周的约定对 (b) 档**不适用**（扫描器根本没有写盘到队列的路径），如实登记不套用；替代物 ＝ 8.3 的 7 天零误报观察窗。

## 6. 退休动作（D8，与上线同批）

- [x] 6.1 🔴 取证件 §三 代码块 → 指向本包实现的指针【CC】
- [x] 6.2 该件 frontmatter `status` 改为 `已落成常驻扫描器（plan-backpressure-scanner），本件转历史件`【CC】
- [x] 6.3 🔴 正文其余部分一字不改；diff 复核确认只落在两处【CC】
  - `git diff` ＝ `5 insertions(+), 44 deletions(-)`，逐行核对：改动仅 frontmatter `status` 一行 ＋ §三 代码块（第 50–92 行）整块换指针，无第三处。
- [x] 6.4 🔴 本节与 5.2 同批合入（同一分支、同一批 commit，不先上线再退）【CC】

## 7. 知识资产台账登记

- [ ] 7.1 三条默会判断进《跨场景前置数据与知识库任务总表》§一.2【Cowork】
  - ⏳ 本 apply 泳道不代 Cowork 写台账，交看护者派发。
- [ ] 7.2 负例累积口径写入 `#462` 行内【Cowork】
  - ⏳ 同上（本泳道不写主仓队列）。

## 8. 验收（晋档条件，缺一不晋）

- [x] 8.1 四个已知误判形态各有一条**在真实数据上跑的**断言测试，且**每条都有反向对照组**【CC】
  - ⑴ 自指行：`RealSelfReferentialRow`（3 条，含两条反向对照）｜⑵ 命名空间对撞：`RealNamespaceCollision`（6 条，含 `test_反向对照_没有excludes时R1会被判已承接`）｜⑶ 右边界：`BoundaryAssertions`（4 条，含 `test_反向对照_去掉右边界后三条必须全部失败`）｜⑷ 宽紧两档：`TargetSegment`（5 条，含 AST 守"阈值 3 不得复活"）。
- [ ] 8.2 全 35 个场景的判定结果与一次**人工逐个复核**逐格一致【CC ＋ Cowork 复核】
  - CC 侧已做一次逐格核对：**「三处皆无」13 个 ＝ O3／O4／Q4／FI4／FI7／R2／R3／R4／S2／S3／S4／S5／S6，与 design D2 实测表逐个相同**；「疑似」3 个（R1←#202／R5←#337,#391／S1←#259）＝ design 预测的 4 个灰区去掉 FI3（FI3 经 `#339` 正确判为已承接，design 原文亦称其为"一个真行"）。⏳ **待 Cowork 独立复核**方可勾。
- [ ] 8.3 接 sweep 后**连续 7 天零误报**【观察窗】—— 2026-09-07 起算
- [x] 8.4 陈化抑制实测：同一批未承接场景不得在 24 小时内推送两次【CC】
  - 骨架 ＝ `_track_and_alert_standing_state`（per-key `last_alerted` ＋ 24h 窗），断言 `test_同一批24小时内不重复推送`／`test_陈化催办阈值为7天且新出现的码不算陈化`／`test_场景消失后再出现按新问题重新计时`。
  - **端到端实测（2026-09-07，真实仓库连跑两次 `_check_plan_backpressure`）**：第一轮出「三处皆无 13（域级暂缓 8、进推送 5）」＋两条待推送告警（无 webhook 故只留痕）；**第二轮日志里两条告警行全部消失、只剩盘点行** ⇒ 集合不变即静默、24h 内不重复推送，实测成立。
- [ ] 8.5 价值指标基线由 Shao Peishen 确认：主指标目标恒 0，**基线 ＝ 2026-09-07 修好判据后的 13**【Shao Peishen】
- [x] 8.6 🔴 断言：实现中**不存在**任何"按标的段所点名的场景数量做整体排除"的分支（AST 扫全模块，`test_实现中不存在按场景数量做整体排除的分支`）【CC】

## 9. 收口

- [x] 9.1 `.claude/rules/队列与落库.md` 增一行指针（判定正本／registry／第 13 类／"只出草案不写队列"／取证件已退休）【CC】
- [x] 9.2 全量测试绿 ＋ 零回归【CC】
  - `test_工具-规划倒逼扫描器.py` ＝ 57 passed / 59 subtests；`test_工具-落库sweep.py` 全量回归见收工报告。
- [ ] 9.3 `#462` 队列行回写 ＋ §二 批次登记【CC】
  - ⏳ **本 apply 泳道按派单件红线③不碰主仓工作区、不写队列**，交看护者收口。
- [ ] 9.4 tasks 全 [x] 后当场 `/opsx:archive plan-backpressure-scanner -y`【CC】
  - ⏳ 8.2／8.3／8.5 与 4.3／7.x／9.3 尚未闭合，**不得提前归档**。
