# plan-backpressure-scanner Tasks

> 🛑 **design 未审，全部任务不得开工。** 本次范围（队列 §一 `#462`，2026-09-07 `OP-0907-J`）＝ **propose ＋ design 起草**，明确不建脚本、不改 `工具-落库sweep.py`。
> 🔴 **三条须拍板项**：**D5 无默认项**（"产出队列行"的自动化档位）、D4（前置闸两步走）、D7（"共用判定函数"的落地形式）。**D5 未答之前，第 5 节整节不得开工**；第 1-4 节只读侧不受其阻塞。
> **机制/环境类变更包**｜来源：`#462`（2026-09-02 立，Shao Peishen 当日答 (a)）｜核心逻辑取证件＝`1-转型规划/0-全景路线图/取证件-2026-09-02-规划与执行覆盖差集.md` §三

## 0. apply 前置（不得跳过）

- [ ] 0.1 🔴 **design 审已过**，且 D4／D5／D7 三项各有明确答复（D5 无默认项，必须有他的字母答复才算过）【Shao Peishen／design 审】
- [ ] 0.2 🔴 **复跑一次本 propose 的四条实测**，确认结论未随仓库变动而失效（`#463` 是否仍在 §一、`#439`/`#440` 的 R1/R4 是否仍在、右边界三条是否仍成立、当前三处皆无数是否仍为 2）——**距 propose 已过 N 天的话，数会变；判据缺陷不会变，但要重新贴一次输出**【CC】
- [ ] 0.3 确认 `第 13 类` 仍无争号（`grep "第 13 类" 0-学习与工具/工具-落库sweep.py` 零命中）；若已被占，顺延并在本包内改齐（同第 6 类当年顺延的既例）【CC】

## 1. registry 建表（D1）

- [ ] 1.1 建 registry JSONL 正本，落 `1-转型规划/0-全景路线图/`，7 字段（`code`/`domain`/`title`/`planned_month`/`aliases`/`excludes`/`suspended`），35 行 ＋ 5 行 `retired`（Q1/Q3/Q5/Q7/Q8）【CC】
  - 🔴 别名以取证件 §三 的既有别名表为起点，**逐条复核**——那份表的销售域六条用的是中文场景名，而 `#463` 里写的是另一种写法（`S4 技术文档` vs `技术文档生成`），**同一批场景因写法差异被判成不同结果，这正是本包要修的**
- [ ] 1.2 🔴 `git check-ignore` 实测 registry 路径**未被忽略**（退出码 1），并贴出输出——不是"应该没被忽略"这类推断【CC】
- [ ] 1.3 一致性校验（registry ↔ 权威排期表，双向差集，CI 硬失败）；**只比场景码，不解析中文标题与排期月**【CC】
  - [ ] 1.3.1 断言：排期表新增一个码而 registry 未跟 ⇒ 校验失败并点名该码
  - [ ] 1.3.2 断言：排期表单元格内的中文/脚注/删除线被改而码集合未变 ⇒ 校验通过（防校验器自身易碎）
  - [ ] 1.3.3 断言：`retired` 行不制造永久差集

## 2. 三处查判定（D2）

- [ ] 2.1 队列侧：只扫 §一 **任务列的标的段**（首个粗体段，无粗体取前 60 字）【CC】
  - [ ] 2.1.1 🔴 断言：`#463`（状态列点名 14 个、任务列只点名 `SC4`）MUST NOT 使销售域五场景被判已承接 —— **这是本包的招牌用例**：宣布问题存在的那一行，此前被判据当成了问题已解决的证据
  - [ ] 2.1.2 🔴 断言：标的段点名多个场景的行 **不得** 因数量达到任何阈值而被整体排除 —— 反例组必须含 `#339`（Q2/FI3/FI4/FI7，是 FI3 唯一真承接行）与 `#467`（SC4/SC7/FI2/FI3，SC4 场景立行行）；**这两条是 design D2 起草期被撤回的"阈值 3"判据会误杀的行**
  - [ ] 2.1.3 断言：标的段之外的沿革/背景引用不构成承接（如某行标的是 A、后段提到 B ⇒ 只计 A）
- [ ] 2.2 `4-数字员工` 侧：只扫 `<部门>/` 下一层目录名，不递归【CC】
- [ ] 2.3 `openspec/changes`（含 archive）侧：包名首个 `-` 之前的段，大小写不敏感【CC】
- [ ] 2.4 🔴 **右边界断言**（钉死取证件那处"注释声称有守卫、实际没有"的缺陷）【CC】
  - [ ] 2.4.1 断言：`FI1` ∉ `FI10-存货跌价智能分析`；`SC1` ∉ `SC10-BOM评审与物料库管控`；`O1` ∉ `O10`
  - [ ] 2.4.2 断言：`SC4` ∈ `SC4-合同条款自动提取与审核`（正向对照）
  - [ ] 2.4.3 🔴 **反向对照组**：移除右侧 lookahead 后 2.4.1 三条必须全部失败——否则说明测试没测到守卫
- [ ] 2.5 判定记名：每条"已承接"结论附命中证据（行号／路径／包名）；断言输出中不存在只有布尔值的结论【CC】

## 3. 命名空间对撞（D3）

- [ ] 3.1 registry 的 `excludes` 表填充，**每条附真实来源行号**（同 `TRIAGE_NEGATION_PHRASES` 的既有做法）【CC】
  - 已实测的三组命名空间：⑴ **需求侧改进项 R1-R5** ← `#439`「R1 · 口径点台账」／`#440`「R4 · 纪律 eval 套件」／R3 ＝ followup_readme_bridge；⑵ **R5 另撞三处** ← 跟进信稿次「R5 分母」(`#417`)＋ FI2「R5 门禁」(`#391`)＋ memory 审核「场景级 R5」(`#337`)；⑶ 🔴 **Phase 1 阶段编号 S1/S2/S3** ← `#259`「S1 收口外部对抗性评审」——根 CLAUDE.md 第一句就在用这套阶段编号，它比销售域场景码更高频，且误判方向正好是"把该报的判成不用报"
  - 另有 `Q4` ← 2027-Q4 季度写法（前置总表与全景规划多处混用）
- [ ] 3.1b 🔴 断言：`R1` 不得因 `#202`（Champion 门禁）被判已承接；`S1` 不得因 `#259`（筑基期收口）被判已承接【CC】
- [ ] 3.2 🔴 断言：`excludes` 命中 **只降级不剔除**——场景 SHALL 出现在「疑似已承接·待人确认」一档，MUST NOT 从输出消失【CC】
- [ ] 3.3 输出「疑似已承接」一档时，同时打印是哪一条 `excludes` 词命中的（否则这一档没法被人判）【CC】

## 4. 前置依赖处置（D4，按 (a) 先行）

- [ ] 4.1 为清单中每个场景附《跨场景前置数据与知识库任务总表》对应行原文；无行则标「前置总表无此场景行」【CC】
- [ ] 4.2 🔴 断言：判定路径中不存在任何按 emoji 推断前置就绪的分支（AST 或等价手段扫全模块）【CC】
  - 成因：该表图例里 🔴 ＝「关键前置（卡场景上线）」而非「未就绪」，按 emoji 判会得到与语义相反的结果
- [ ] 4.3 立一条独立队列行：前置总表增设机器可读前置状态字段并回填约 13 行（(b) 的落点，不在本包内做）【Cowork 总线】
- [ ] 4.4 spec 内的"缺字段非静默降级"条款先落地，为 (b) 切换预留【CC】

## 5. 🛑 输出与队列行产出（D5 拍板前整节不得开工）

- [ ] 5.1 三档清单输出 ＋ 范围声明文案；落 `reports/`【CC】 —— **本项只读，D5 未答亦可做**
- [ ] 5.2 接 sweep **第 13 类**，子进程调用【CC】 —— **本项只读推送，D5 未答亦可做**
- [ ] 5.3 🔴 **"本轮是否真跑到"的留痕从第一版就带**（第 8 类当年的事故形态：整轮早退时的痕迹与"没有告警"完全相同）【CC】
- [ ] 5.4 指纹抑制（集合不变即静默）＋ 陈化催办（N=7，同 `#312` 缺口二取值）【CC】
- [ ] 5.5 `suspended` 场景移出常规推送、保留在清单；另按 30 天周期发"该暂缓已持续 N 天，是否仍成立"复核提醒【CC】
- [ ] 5.6 误报与负例**分开计数**；断言：负例 MUST NOT 触发任何自动的判据收紧【CC】
- [ ] 5.7 🛑 **写侧：按 D5 拍板的档位实现**（起草方推荐 (b)＝出队列行草案落 `reports/`，由人一条命令落库）【CC】
  - [ ] 5.7.1 🔴 断言：D5 拍板前执行扫描器，两份队列文件**逐字节不变**
  - [ ] 5.7.2 🔴 断言：`suspended` 场景**无论哪一档都不自动写行**
  - [ ] 5.7.3 写入若放开，必须经编辑锁既有命令（`acquire`/`append-row`/`release`），不得裸改；动作写 append-only 审计
  - [ ] 5.7.4 首轮以 `--dry-run` 跑满一周再放开（同 `decision_reminder_check.py` 既有约定）

## 6. 退休动作（D8，与上线同批，不得延后）

- [ ] 6.1 🔴 取证件 `取证件-2026-09-02-规划与执行覆盖差集.md` §三 代码块 → 换成指向本包实现的一行指针【CC】
- [ ] 6.2 该件 frontmatter `status` 由 `取证已完成，待落成常驻扫描器` 改为 `已落成常驻扫描器（plan-backpressure-scanner），本件转历史件`【CC】
- [ ] 6.3 🔴 **正文其余部分一字不改**（守"历史记录不追改"）；改完做一次 diff 复核，确认改动只落在 §三 代码块与 frontmatter 两处【CC】
- [ ] 6.4 🔴 **本节与 5.2（接 sweep）同批合入**——不得"先上线、回头再退"。理由：本包第一天就是最危险的一天，那时两份判据同时存在、结果不同，而旧的那份写在一份标题为「取证已完成」的权威件里，读者没有理由怀疑它【CC】

## 7. 知识资产台账登记

- [ ] 7.1 三条默会判断进《跨场景前置数据与知识库任务总表》§一.2（同 `#454` 已开的机制类条目先例）【Cowork】
  - ⒜「这个场景算不算已经有人接了」的尺度（含"标的段"的边界这条尺度型判据，以及 design D2 起草期撤回"阈值 3"那次判例）—— 持有人 Shao Peishen／backup 孙涛
  - ⒝「前置依赖算不算已解除」的尺度 —— 持有人 Shao Peishen／backup 值周巡检当值 session
  - ⒞「哪些域现在根本不该被倒逼」—— 持有人 Shao Peishen／backup 孙涛；来源 `#463`（2026-09-02 明令）
- [ ] 7.2 负例累积口径写入 `#462` 行内：满 5 条负例才回头收紧判据、才占用他一次时间（同 `#454`）【Cowork】

## 8. 验收（晋档条件，缺一不晋）

- [ ] 8.1 四个已知误判形态各有一条**在真实数据上跑的**断言测试，且**每条都有反向对照组**【CC】
- [ ] 8.2 全 35 个场景的判定结果与一次**人工逐个复核**逐格一致；差异逐条写明是判据错还是人错【CC ＋ Cowork 复核】
- [ ] 8.3 接 sweep 后**连续 7 天零误报**（误报＝报了未承接而实际三处之一有真实承接；"人决定不立行"不算误报）【观察窗】
- [ ] 8.4 陈化抑制实测：同一批未承接场景不得在 24 小时内推送两次【CC】
- [ ] 8.5 价值指标基线由 Shao Peishen 确认：风险型主指标「规划已排期、但三处皆无」的场景数，目标恒 0；**基线＝2026-09-07 修好判据后的 13**（2026-09-02 的 14 与 09-07 旧判据的 2 均为旧口径，只作沿革，不作基线）【Shao Peishen】
- [ ] 8.6 🔴 断言：判定实现中**不存在**任何"按标的段所点名的场景数量做整体排除"的分支（AST 或等价手段扫全模块）—— design D2 起草期撤回的那条判据不得以任何形式复活【CC】

## 9. 收口

- [ ] 9.1 场景/模块 CLAUDE.md 或对应 rules 更新（本包属机制类，落点＝`.claude/rules/队列与落库.md` 一行指针）【CC】
- [ ] 9.2 全量测试绿 ＋ 零回归【CC】
- [ ] 9.3 `#462` 队列行回写（状态列开头 ✅、产出路径、下游追行）＋ §二 批次登记【CC】
- [ ] 9.4 tasks 全 [x] 后当场 `/opsx:archive plan-backpressure-scanner -y`【CC】
