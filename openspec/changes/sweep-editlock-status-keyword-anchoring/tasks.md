## 1. Design 审核（阻塞后续所有任务）

- [ ] 1.1 提交 design.md 四个决策点给 Shao Peishen 审核（可直接回复 (a)/(b) 或指定其它方案；未回应按 design.md 已标注的默认项执行）
- [ ] 1.2 记录审核结果到 design.md「审批记录」小节，队列 §一 #248 行同步回填

## 2. sweep 判据锚定（决策点 1/4）

- [ ] 2.1 `工具-落库sweep.py::_classify_section_two_rows` 改为依据状态列去除前导 `*`/空白（半角空格、制表符、全角空格）后的开头片段判定："✅" 开头→已完成（跳过）；"待" 开头→待处理；其余→模糊状态（记日志、不纳入本轮）
- [ ] 2.2 新增/更新单测：① 开头 `✅`、说明文字含"待"字样的行判已完成（复现 #248 真实事故场景）；② 开头"待处理"的行判待处理（真实反向场景，防判据收得过紧）；③ 开头既非 `✅` 也非"待"的行判模糊状态且记日志
- [ ] 2.3 用现存生产队列文件 §二 全部 12 条行跑历史兼容核对，固化为参数化单测，确认新旧判据结果一致（本 propose 阶段已完成一次分析性核对，见 design.md「历史兼容核对」，此处需转为可重复运行的自动化测试）

## 3. 编辑锁断言门槛引号排除（决策点 2/3）

- [ ] 3.1 `工具-共享文档编辑锁.py::_validate_release_structure` ④ 新增引号剔除预处理（正则 `「[^」]*」|『[^』]*』`），对剔除后的文本做原有 P0/P1 token + "未核"/"未做的核实"共现检测
- [ ] 3.2 新增/更新单测：① 引号内含"未做的核实"字样、引号外无共现的行不被拦截（复现 #221 潜在场景）；② 引号外真实共现的行仍被拦截（真实反向场景，构造合成用例，防判据收得过松）；③ 部分引号保护场景（未核字样在引号内、P0/P1 在引号外且引号外另有裸写未核字样）仍正确拦截
- [ ] 3.3 用现存生产队列文件 §一 全部 61 条行跑历史兼容核对，固化为参数化单测，确认唯一分歧行 #221 在新判据下不再命中，且与人工判断一致；其余 60 条行无分歧

## 4. 真实验证

- [ ] 4.1 登记一个测试批次，跑一次真实（非 `--dry-run`）sweep，确认取活→落库→回写全链路正常，且状态列含判据关键词但不在开头的测试用例不被误取活
- [ ] 4.2 构造一个引号保护测试内容的 §一 行，走一次真实 `release`，确认不再被误拦截；另构造一个引号外真实共现的测试行，确认仍被正确拦截

## 5. 验收与收尾

- [ ] 5.1 全量回归（sweep + 编辑锁工具对应测试文件）零漂移
- [ ] 5.2 两份工具文件头部说明段补充本次判据锚定的背景（沿用既有"文件头部记录设计决策历史"惯例）
- [ ] 5.3 队列 §一 #248 行回填：状态、产出路径、单测清单、真实验证记录、历史兼容核对结果
- [ ] 5.4 `/opsx:archive` 归档本变更包
- [ ] 5.5 收工重跑文档台账
