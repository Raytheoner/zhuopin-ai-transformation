# editable-pth-blindspot-guard Tasks

> 🔴 **本文件在 design 审通过前不得开工。** 0.x 是前置闸，逐条过完才进 1.x。
> 执行环境：**CC**（写生产码、跑测试、自行 commit+push，一任务一 worktree）。
> 🔴 **本文件只列计划，不写测试代码**——3.x 是计划中的单测覆盖点清单，实现时才落成代码（OP-0904-L 范围只到 design 草案，实现另开任务）。

## 0. 前置闸（design 审后、动手前）

- [ ] 0.1 🔴 重新核实 `#454` 当前状态（是否已合入 `master`／仍在途／具体触碰内容），按 design 决策点 5 的拍板结果决定先后顺序——若结论是"等 `#454` 先合入"，本任务在此停手回报，不继续
- [ ] 0.2 确认 design 审结论已回填队列 §一 `#459` 行（决策点 4／5 的拍板结果必须是白纸黑字，不凭记忆）
- [ ] 0.3 确认分支 `claude/queue-410-editable-probe` 仍可 `git log`／`git show 330218c` 读到（本地引用足够，不依赖 origin 是否已清理该分支）
- [ ] 0.4 建 worktree

## 1. 取证与口径（🔴 必须先于实现，design 决策点 1 的前置）

- [ ] 1.1 `git diff 330218c^ 330218c -- 0-学习与工具/工具-落库sweep.py` 取出完整 patch；与当前 `master`（apply 时的 HEAD）同一文件做一次 `git apply --check` 或人工 diff，确认无冲突或列出冲突点
- [ ] 1.2 🔴 **现网只读全量重测**（proposal 已知残余风险第 3 条要求）：对本机 site-packages 全量重新跑一遍"editable 分发形态普查"（finder 个数、`.pth` 个数、setuptools/pip 版本），与 `330218c` 提交信息里记录的"9 个分发全 finder、0 个 compat"做比对；如有变化（新增/减少 editable 安装、版本升级），如实记录变化点，不得假设"一周内不可能变"
- [ ] 1.3 白盒读 `330218c` 新增的三个函数（`_parse_editable_pth`／`_editable_pth_key`／`_merge_editable_detail`）与其调用点改动，确认与当前 `_check_editable_install_targets`（行号可能已漂移）的挂载方式一致

## 2. 实现

- [ ] 2.1 cherry-pick 或手动移植 `330218c` 对 `0-学习与工具/工具-落库sweep.py` 的改动（不含队列文件那 3 行）
- [ ] 2.2 按 design 决策点 4，确认回显文案改为三个独立显式计数器（`unreadable`／`module_anomalies`／`pth_anomalies`），而非沿用现基线的减法
- [ ] 2.3 按 design 决策点 2，确认"hook 形态 `.pth` 引用的 finder 不存在"判定归入 `EDITABLE_FORM_UNREADABLE`
- [ ] 2.4 文件头 `#410` 说明段补一段"第二种形态（compat 模式）"注释，参照 `330218c` 原文，注明本次立行来源是 `#459`（不是把 `#410` 的历史记录追改）

## 3. 测试（计划中的单测覆盖点清单——本阶段只列清单，不写代码）

沿用并核对 `330218c` 已有的 14 条用例，逐条确认在当前基线上仍然成立、命名与断言口径不需要调整：

- [ ] 3.1 `test_纯路径pth指向worktree副本判为幽灵import`——阳性主路径
- [ ] 3.2 `test_纯路径pth目标不存在判为断链`——阳性主路径的另一形态
- [ ] 3.3 `test_纯路径pth指向主工作树时零告警但仍逐项回显`——阴性但不省略回显（`OP-0819-F` 纪律）
- [ ] 3.4 `test_pth的key不含版本号`——key 纪律
- [ ] 3.5 `test_hook形态pth不重复计数也不误报`——finder 已覆盖的模块，`.pth` 那一路不重复算
- [ ] 3.6 `test_hook形态引用的finder不存在必须报判据不可用`——决策点 2 的衍生判定
- [ ] 3.7 `test_pth读得懂但空白按判据不可用处理`——空文件边界
- [ ] 3.8 `test_零安装与零扫到必须外观可分`——🔴 本包最核心的反向断言，直接对应 proposal 的"Why"（零告警 ≠ 零风险）
- [ ] 3.9 `test_同key的finder异常与pth异常合并而非互相覆盖`——`_merge_editable_detail` 的存在理由
- [ ] 3.10 `test_零执行_pth一行都不跑`——`.pth` 解析同样遵守"判据不得与被检对象共享失败模式"
- [ ] 3.11 `test_真实安装_import解析到worktree时判据抓得到`——真跑 pip，非 mock
- [ ] 3.12 `test_真实安装_compat模式无finder时判据同样抓得到`——真跑 pip，本包要修的那个洞的正向证明
- [ ] 3.13 `test_真实安装_指向正常目录时不误报`——真跑 pip 的阴性对照
- [ ] 3.14 `test_真实finder能被纯文本解析`——上游模板漂移哨兵（setuptools 产物格式一旦变化，这条会先炸）

新增（若 1.2 现网重测发现基线漂移，或 apply 时与 `#454` 的合并产生新的交互面，据实际情况追加）：

- [ ] 3.15 若 1.2 发现本机 setuptools/pip 版本已变化，补一条对应新版本产物格式的真实用例（不假设旧版本产物格式永久有效）
- [ ] 3.16 全量回归：`0-学习与工具` 目录全量单测绿、零新增漂移（`330218c` 记录的"2 条预存失败"须在当前基线上重新判定是否仍存在、是否与本包触碰区无关）

## 4. 现网验证（不落盘）

- [ ] 4.1 用改后判据对本机现网 site-packages 做一次**只读**常驻轮次跑法，确认日志逐项回显（`.pth` 直挂条数、异常条数、判据不可用项数三个数字都出现，即便为 0）
- [ ] 4.2 确认 `reports/sweep-editable-install-state.json` 状态文件的 key 集合按预期包含/不包含新扫描路径产生的 key（视 1.2 的现网现状而定）

## 5. 收口

- [ ] 5.1 队列 §一 `#459` 行回填：实现结论、1.2 现网重测结果、3.x 覆盖情况、与 `#454` 的最终串行顺序
- [ ] 5.2 §二 批次登记 ＋ 触发一次 sweep，并看一眼 `reports/sweep-commit.log` 末几行确认真落库
- [ ] 5.3 `/opsx:archive editable-pth-blindspot-guard -y`（完工即归档；若本次不归档，理由须写进机器认得的三处入口之一）
- [ ] 5.4 分支 `claude/queue-410-editable-probe` 在本包内容确认已合入 `master` 后可删除（登记但不在本任务内执行，防止 design 审未过时被误删）
