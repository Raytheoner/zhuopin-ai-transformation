# editable-pth-blindspot-guard Tasks

> 🔴 **本文件在 design 审通过前不得开工。** 0.x 是前置闸，逐条过完才进 1.x。
> 执行环境：**CC**（写生产码、跑测试、自行 commit+push，一任务一 worktree）。
> 🔴 **本文件只列计划，不写测试代码**——3.x 是计划中的单测覆盖点清单，实现时才落成代码（OP-0904-L 范围只到 design 草案，实现另开任务）。

## 0. 前置闸（design 审后、动手前）

- [ ] 0.1 🔴 重新核实 `#454` 当前状态（是否已合入 `master`／仍在途／具体触碰内容），按 design 决策点 5 的拍板结果决定先后顺序——若结论是"等 `#454` 先合入"，本任务在此停手回报，不继续
  - 🔴 **本条未按字面执行，如实留痕（2026-09-08 `OP-0908-O` 补记）**：本包已于 `2635cd5`（2026-09-05）先合入 master，`#454` 当时未合、至今仍是 `[S:blocked]`（2026-09-08 实测，手段＝`工具-队列查询.py --row 454 --field status`）。即 **design 决策点 5 推荐的 ①「等 `#454` 先合入」若照办，本包会被一条阻塞行无限期扣住**。Shao Peishen 2026-09-08 答 `(b)`：D4／D5 判为已随 3a 一并通过，本次只补留痕、不重开裁决（原文在队列 §一 `#459` 行末段）。⚠️ 该 `(b)` 是「补一句留痕」而非「重新裁一次」——若日后发现 D4/D5 的实质与 3a 的射程不一致，须回来重裁。**残余合并成本仍在 `#454` 执行方那一侧**；已核 `#454` 尚未开始改 `工具-落库sweep.py`（其状态列自陈仍在等人），故成本目前是「读一次 diff」，不是「解一堆冲突」。
- [x] 0.2 确认 design 审结论已回填队列 §一 `#459` 行（决策点 4／5 的拍板结果必须是白纸黑字，不凭记忆）
- [x] 0.3 确认分支 `claude/queue-410-editable-probe` 仍可 `git log`／`git show 330218c` 读到（本地引用足够，不依赖 origin 是否已清理该分支）
- [x] 0.4 建 worktree

## 1. 取证与口径（🔴 必须先于实现，design 决策点 1 的前置）

- [x] 1.1 `git diff 330218c^ 330218c -- 0-学习与工具/工具-落库sweep.py` 取出完整 patch；与当前 `master`（apply 时的 HEAD）同一文件做一次 `git apply --check` 或人工 diff，确认无冲突或列出冲突点
- [x] 1.2 🔴 **现网只读全量重测**（proposal 已知残余风险第 3 条要求）：对本机 site-packages 全量重新跑一遍"editable 分发形态普查"（finder 个数、`.pth` 个数、setuptools/pip 版本），与 `330218c` 提交信息里记录的"9 个分发全 finder、0 个 compat"做比对；如有变化（新增/减少 editable 安装、版本升级），如实记录变化点，不得假设"一周内不可能变"
- [x] 1.3 白盒读 `330218c` 新增的三个函数（`_parse_editable_pth`／`_editable_pth_key`／`_merge_editable_detail`）与其调用点改动，确认与当前 `_check_editable_install_targets`（行号可能已漂移）的挂载方式一致

## 2. 实现

- [x] 2.1 cherry-pick 或手动移植 `330218c` 对 `0-学习与工具/工具-落库sweep.py` 的改动（不含队列文件那 3 行）
- [x] 2.2 按 design 决策点 4，确认回显文案改为三个独立显式计数器（`unreadable`／`module_anomalies`／`pth_anomalies`），而非沿用现基线的减法
- [x] 2.3 按 design 决策点 2，确认"hook 形态 `.pth` 引用的 finder 不存在"判定归入 `EDITABLE_FORM_UNREADABLE`
- [x] 2.4 文件头 `#410` 说明段补一段"第二种形态（compat 模式）"注释，参照 `330218c` 原文，注明本次立行来源是 `#459`（不是把 `#410` 的历史记录追改）

## 3. 测试（计划中的单测覆盖点清单——本阶段只列清单，不写代码）

沿用并核对 `330218c` 已有的 14 条用例，逐条确认在当前基线上仍然成立、命名与断言口径不需要调整：

- [x] 3.1 `test_纯路径pth指向worktree副本判为幽灵import`——阳性主路径
- [x] 3.2 `test_纯路径pth目标不存在判为断链`——阳性主路径的另一形态
- [x] 3.3 `test_纯路径pth指向主工作树时零告警但仍逐项回显`——阴性但不省略回显（`OP-0819-F` 纪律）
- [x] 3.4 `test_pth的key不含版本号`——key 纪律
- [x] 3.5 `test_hook形态pth不重复计数也不误报`——finder 已覆盖的模块，`.pth` 那一路不重复算
- [x] 3.6 `test_hook形态引用的finder不存在必须报判据不可用`——决策点 2 的衍生判定
- [x] 3.7 `test_pth读得懂但空白按判据不可用处理`——空文件边界
- [x] 3.8 `test_零安装与零扫到必须外观可分`——🔴 本包最核心的反向断言，直接对应 proposal 的"Why"（零告警 ≠ 零风险）
- [x] 3.9 `test_同key的finder异常与pth异常合并而非互相覆盖`——`_merge_editable_detail` 的存在理由
- [x] 3.10 `test_零执行_pth一行都不跑`——`.pth` 解析同样遵守"判据不得与被检对象共享失败模式"
- [x] 3.11 `test_真实安装_import解析到worktree时判据抓得到`——真跑 pip，非 mock
- [x] 3.12 `test_真实安装_compat模式无finder时判据同样抓得到`——真跑 pip，本包要修的那个洞的正向证明
- [x] 3.13 `test_真实安装_指向正常目录时不误报`——真跑 pip 的阴性对照
- [x] 3.14 `test_真实finder能被纯文本解析`——上游模板漂移哨兵（setuptools 产物格式一旦变化，这条会先炸）

新增（若 1.2 现网重测发现基线漂移，或 apply 时与 `#454` 的合并产生新的交互面，据实际情况追加）：

- [ ] 3.15 若 1.2 发现本机 setuptools/pip 版本已变化，补一条对应新版本产物格式的真实用例（不假设旧版本产物格式永久有效）
  - ⏭️ **不触发（2026-09-08 实测）**：1.2 复测本机仍是 setuptools 81.0.0 / pip 26.1.2，与 `330218c` 记录同版本，产物格式未变 ⇒ 本条的前置条件不成立，**不是漏做**。
- [x] 3.16 全量回归：`0-学习与工具` 目录全量单测绿、零新增漂移（`330218c` 记录的"2 条预存失败"须在当前基线上重新判定是否仍存在、是否与本包触碰区无关）

## 4. 现网验证（不落盘）

- [x] 4.1 用改后判据对本机现网 site-packages 做一次**只读**常驻轮次跑法，确认日志逐项回显（`.pth` 直挂条数、异常条数、判据不可用项数三个数字都出现，即便为 0）
- [x] 4.2 确认 `reports/sweep-editable-install-state.json` 状态文件的 key 集合按预期包含/不包含新扫描路径产生的 key（视 1.2 的现网现状而定）

## 5. 收口

- [ ] 5.1 队列 §一 `#459` 行回填：实现结论、1.2 现网重测结果、3.x 覆盖情况、与 `#454` 的最终串行顺序
- [ ] 5.2 §二 批次登记 ＋ 触发一次 sweep，并看一眼 `reports/sweep-commit.log` 末几行确认真落库
- [ ] 5.3 `/opsx:archive editable-pth-blindspot-guard -y`（完工即归档；若本次不归档，理由须写进机器认得的三处入口之一）
- [ ] 5.4 分支 `claude/queue-410-editable-probe` 在本包内容确认已合入 `master` 后可删除（登记但不在本任务内执行，防止 design 审未过时被误删）

---

## 附录 · 收口留痕（2026-09-08，CC `OP-0908-O`，泳道 `500-sweep-carrier`，批 `B-0908_四泳道机制根治`）

> 本附录只登记「谁在什么时候用什么手段核的」。队列 §一 `#459` 行内四处收口缺口 ②③④⑤ 的处置逐条如下。

**② tasks 2.4（文件头注释未注明本次立行来源）—— 已补。** `0-学习与工具/工具-落库sweep.py` 第二种 editable 形态注释段末尾补一段：立行来源＝队列 §一 `#459`，实现随 `2635cd5` 合入 master、openspec 包随 merge `fc4e4ca` 合入。🔴 **上面那行 `OP-0831-N-A1` ／ `#410` 的历史署名一字未改**（守「历史记录不追改」：已发生事实的原文不改，当下生效的指针才改）。

**③ design 决策点 4／5 无拍板留痕 —— 已由总线闭合，本包侧同步。** Shao Peishen 2026-09-08 答 `(b)`，留痕正本在队列 §一 `#459` 行末段；本文件侧的对应动作＝勾上 0.2、并在 0.1 下方如实写明 D5 推荐 ① 未被遵循及其现状（见上）。**D4 的落实是可验的**：本轮只读实跑的回显里三个计数器各自独立出现（`模块映射 12 条：正常 12 条、指向异常 0 条`／`` `.pth` 直挂 0 条：正常 0 条、指向异常 0 条``／`判据不可用 0 项`），**没有任何一个数字是由另一个减出来的** ⇒ 决策点 4 ⒜ 已在码上生效，不只是文档里写着。

**④ `2635cd5` 合入 master 无门禁答复留痕 —— 仍无从判定，如实报出，本次不追认。** `reports/lane-watch-state.json` 中没有 `459-382b-impl` 这条泳道的任何记录（同批 `456-lint-impl`／`341-apply`／`455-apply` 都有），故「口头批了未落库」与「压根没批」两种可能**外观相同**。🔴 **本包不替它补一条事后记录**——补了就把「查不到」变成「查到了」，那正是这类留痕机制唯一要防的事。

**⑤ tasks 4.1／4.2（现网只读验证）—— 已执行，证据如下。** 手段＝直接 import 本文件同目录的 `工具-落库sweep.py`，用**临时目录**充当 `repo_root` 调 `_check_editable_install_targets`，因此状态文件写在临时目录、`.env` 不存在故 webhook 取不到 ⇒ **未写真 `reports/`、未推企微**，全程只读本机 site-packages。
- **4.1（逐项回显，三个数字都出现、即便为 0）**：`site-packages 3 处，editable 分发 9 个（finder 形态）＋ .pth 9 份`；`模块映射 12 条：正常 12 条、指向异常 0 条`；`` `.pth` 直挂 0 条：正常 0 条、指向异常 0 条``；`判据不可用 0 项`。9 份 `.pth` **全部落在「由 finder 承载」那一支**（hook 形态），故直挂计数为 0 —— 这正是 1.2 所说的「本机 9 个分发全 finder、0 个 compat」在改后判据下的表现，**「0 条」在这里是被判出来的，不是没扫**。
- **4.2（状态文件 key 集合）**：本轮产生的 key 集合＝空；现网 `reports/sweep-editable-install-state.json` 原文＝`{}`。**两者一致 ⇒ 新增的 `.pth` 扫描路径没有引入任何新 key**（它只在异常时写 key，而本机零异常）。
- **1.2 基线复测**：setuptools 81.0.0 / pip 26.1.2，与 `330218c` 记录同版本 ⇒ 产物格式未漂移，3.15 不触发。

**仍开着的 5.x**：5.1（队列 `#459` 行回填）与 5.2（§二 批次登记）由看护者／总线在收工时按队列纪律做，本泳道只出回写件、不自行写队列真身；5.3 归档、5.4 删分支同理，均待总线派发。

**勾选依据（谁核的、用什么核的）**——🔑 本节存在的理由＝「只有动作没有手段的验证声明 ＝ 没有验证」：
- **本次（`OP-0908-O`）亲自核过的**：0.3（`git show -s --oneline 330218c` ⇒ 可读，标题 `feat(sweep): #410 editable 判据补 .pth 纯路径形态盲区 ＋ 真实产物真机验活`）；3.1–3.14（`grep -c "def test_<名>"` 逐条实测，**14 条各命中 1 次、零缺项零重名**）；3.16（全量 `pytest test_工具-落库sweep.py` 本轮实跑，结果见收工报告）；4.1／4.2／1.2（上面的只读实跑）；2.4（本次亲手补的）。
- **沿用 `OP-0906-H` 只读核验 ＋ `OP-0906-G` 看护者复核的结论、本次未重复取证的**：1.1／1.3／2.1／2.2／2.3。其手段已写在队列 §一 `#459` 行内（`git show 2635cd5`／`git merge-base --is-ancestor`／`git grep -c`／`git cat-file -t`／`git merge-tree --write-tree` 等），本次不复述、也不冒充是自己核的。
- 0.4（建 worktree）＝流程动作，由当时的实现泳道完成，无独立取证物。
