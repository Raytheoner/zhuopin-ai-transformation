# Tasks — 串行闸归一化（`OP-0907-K`，2026-09-07）

> ✅ **apply 已完成（`OP-0907-AJ`，2026-09-07，分支 `claude/op0907aj-serial-gate-482-apply`）** —— design 审 2026-09-07 已过（Shao Peishen 合审 §5 (a)＝全部决策点按起草方推荐）：**D3 拍 (a)／D5 拍 (a)／D6 拍「删」**。
> 上一轮的 propose+design 范围约束（`OP-0907-K`「MUST NOT 改动两个工具任何一行」）**已随 design 审通过而解除**，本轮即是被授权的实现轮。
> 🟡 **未做、已交看护者的五类**：ff 合入 master／据实改 `质量部#13`／队列回写（7.3–7.5）／`质量部#7` 那处人写豁免的人裁／「回填覆盖闭环态」另立行。逐条见文末「🟡 停点」。

## 0. 🔴 开工前置（未决，须 Shao Peishen 或 design 审拍板）

- [x] 0.1 **design 审拍板 D3**（✅ 拍 (a) 日期排序键，2026-09-07 合审）：「最近一封」用哪把尺子。推荐 (a) 日期排序键。🔴 **拍 (a) 会立刻把陈忱的闸从 ✅ 开变成 🔒 锁**（依据 `质量部#13` 2026-09-04 `✅ 已推送`）。若拍 (b) 维持现状，则 `latest_dispatched_letter` 那句「与跟进闸同一把尺子」的 docstring MUST 当批改掉。
- [x] 0.2 **design 审拍板 D5**（✅ 拍 (a) 编号列主键，2026-09-07 合审）：④ 的根治走 (a) 行身份改用编号列主键，还是 (b) 新增专用标记 `行长外置：`。推荐 (a)。🔴 (a) 会削弱两态语义检查的一个面（改已有编号行的状态为 `🆕 待发` 将不再被拦），缓解方案见 D5(a) 代价段，须被明确看见。
- [x] 0.3 **design 审拍板 D6**（✅ 拍「删」，2026-09-07 合审）：存量 5 行机器豁免删还是留。推荐删（辨析见 D6：它们是控制指令不是历史叙述）。
- 🟡 **0.4 未做·已停手**（🟡 档，派单件明标「据实改 `质量部#13` 状态」须他一个字母）。🔴 **本包一旦 ff，`陈忱` 的闸立刻由 ✅ 开变 🔒 锁**——实测已复现（`python 0-学习与工具/工具-跟进闸查询.py --to 陈忱` ⇒ `闸：🔒 锁 ／ 依据：质量部#13 · ✅ 已推送 2026-09-04 06:24 UTC`）。原文：**配套前置（若 0.1 拍 (a)）**：上线前先把 `质量部#13` 据实改为 `✅ 无需回复`（一次合法转态，不需要新机制），并把「发送脚本回填覆盖闭环态判定」另立队列行——该漏洞 `质量部#7` 行内已记 20 天，至今无承接方。
- [x] 0.5（`npx openspec validate --all --strict` ＝ **174 passed / 0 failed**；本包 delta 全部 `ADDED`，不依赖未 sync 的主 spec）**本包不得先于依赖包归档**：核对 `followup-closure-evidence-gate` 与 `followup-readme-phase2` 的 delta 是否已 sync 进主 specs；若未 sync，本包只能用 `ADDED`（当前即如此）。

## 1. 权威判据（`5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/followup_gate.py`）

- [x] 1.1 新增 `RecipientIdentity` 类型与 `recipient_identity(cell) -> RecipientIdentity | None`（D1／D2 规则）
- [x] 1.2 新增 `same_recipient(a, b) -> bool`，🔴 任一侧 `None` 一律 `False`
- [x] 1.3 `recipient_department` 改为 `recipient_identity(...)[0]` 的派生，**对外签名与语义不变**
- [x] 1.4 若 0.1 拍 (a)：新增 `latest_letter_for_recipient(rows, identity)`，与 `_letter_sort_key` 共用同一排序键
- [x] 1.5 隔离环境回落实现（无平台包时）MUST 与权威实现由**同一份单测双跑**，否则回落分支会静默漂移
- [x] 1.6 `0-学习与工具/工具-队列结构lint.py` 的符号断言名单加入 1.1／1.2／1.4 新符号；既有符号一个都不改名

## 2. 🔴 回归用例先写先跑先红（D7，次序不得与第 3 节对调）

- [x] 2.1 T1 误放（`质量部#7` 实况：多写注记 ⇒ 现状放行）——**改造前 MUST 红**
- [x] 2.2 T3 误放镜像（`采购部#21` 实况：少写注记）——**改造前 MUST 红**
- [x] 2.3 T6 两端同判（append 前置 vs release 后置）——**改造前 MUST 红**
- [x] 2.4 T2 误拦（`质量部#10` 实况）／T4 解析失败不匹配／T5 跨部门同名不合并
- [x] 2.5 🔴 **「改造前跑一次、三条红」的实测输出**（`OP-0907-AJ` 2026-09-07；用例先写、消费方一行未改时跑
  `python -m pytest "0-学习与工具/test_工具-共享文档编辑锁.py" -k FollowupSerialGateIdentityTests -q`）：

  ```
  FAILED …::test_T1_误放_新增行多写后括号注记不得绕过串行闸
  FAILED …::test_T3_误放镜像_新增行少写后括号注记同样不得绕过
  FAILED …::test_T6_append前置与release后置对同一形态给出同一结论
  FAILED …::test_T4_收信人解析失败不判为同一人且出声
  FAILED …::test_T7_已发出的信改作废且理由为开闸措辞时被拒并指名判据
  FAILED …::test_T8_已发出的信改作废且理由过短无凭据时被拒
  FAILED …::test_T9b_作废豁免逃生阀放行_但空理由拒绝
  FAILED …::test_串行豁免收窄_有标记没理由不再放行
  FAILED …::test_D5a_编号行状态被改成终态仍被两态语义拦住
  FAILED …::test_D5a_无编号行回落到全单元格身份并出声
  10 failed, 4 passed, 414 deselected in 12.51s
  ```

  🔴 **T1 的失败模式正是本包要修的那件事**：改造前 `_validate_followup_readme_release(prior+new, prior)`
  返回 `[]`——「多写一个后括号注记 ⇒ 串行原则被静默绕过、零告警」。
  改造后同一批用例 **28 passed, 1 skipped, 21 subtests passed**（含无平台包回落分支的双跑）。

## 3. 四处消费方改调（每处改完全量回归）

- [x] 3.1 `工具-共享文档编辑锁.py::_validate_followup_readme_release` 收信人比对改 `same_recipient`；解析失败记 violation 并指名该行
- [x] 3.2 `工具-跟进闸查询.py::build_report`／`all_recipients` 改 `recipient_identity`（`_collect_intake_rows` 的部门粒度本包不改，其路标另有承接）
- [x] 3.3 `工具-跟进信README登记.py::_assert_gate_open` 改调 ＋ 两处静默分支出声（解析失败、`GateQueryError`）
- [x] 3.4 `readme_table.split_department_and_name` 改为权威函数的薄封装（保留符号名——`dispatch.py`／`draft_gap_detection.py`／`工具-跟进信README查询.py` 三个现役消费方按名取值）
- [x] 3.5 若 0.1 拍 (a)：三处「最近一封」改调 1.4；`latest_dispatched_letter` docstring 与事实对齐

## 4. `❌ 已作废` 防滥用判据（D4）

- [x] 4.1 `followup_gate` 新增 `classify_void_risk(prior_status, is_new_row) -> "low" | "high"`
- [x] 4.2 新增 `validate_void_reason(status_cell, risk) -> list[str]`，实现 R1（≥8 全角字）／R2（黑名单）／R3（可核凭据形态）
- [x] 4.3 `_validate_followup_readme_release` 接入，判据源取 acquire 快照，**不读 git**
- [x] 4.4 `作废豁免：` 逃生阀（放行 ＋ 打印留痕；空理由拒绝）
- [x] 4.5 T7／T8 反例单测 ＋ T9 正例单测（🔴 T9 取归档件里那行真实作废的理由文本，防止判据把合法作废拦死）
- [x] 4.6 拒绝文案 MUST 指名命中的是 R1／R2／R3 的哪一条、以及命中的黑名单词是哪一个

## 5. 停止机器制造豁免（D5，方案待 0.2）

- [x] 5.1 退休 `工具-跟进信README行长外置.py::_find_serial_gate_conflicts`（复刻实现，逐字继承了 3.1 的缺陷），改调权威判据
- [x] 5.2 删除 `SERIAL_WAIVER_REASON` 常量与自动写入路径
- [x] 5.3 若 0.2 拍 (a)：`_followup_row_identity` 改编号列主键 ＋ 无编号行回落并出声 ＋ 两态语义检查改为同时看身份与状态迁移
- [~] 5.4 **不适用**（0.2 拍 (a)，本条是 (b) 分支）——若 0.2 拍 (b)：新增 `行长外置：` 专用标记及其「除主要事项列外逐字相等」的机器验证
- [x] 5.5 `串行豁免：` 判据收窄为「标记后 MUST 跟非空理由」（不做语义判断）
- [x] 5.6 T10：对含长行的 README 副本跑一次行长外置，产物中 `串行豁免：` 计数 MUST 为 0 且 release 通过

## 6. 清存量（D6，🔴 MUST 在第 5 节之后）

- [x] 6.1 删除 README 里**机器写入**的 `串行豁免：D3 行长外置压缩摘要，非新起草跟进信`
  —— 🔴 **实测是 4 行、不是 5 行**（`采购部#12`／`IT部#8`／`质量部#10`／`质量部#12`，全在「交期要点」列）。
  design D6 点名的第 5 处 `质量部#7` **不是机器写的**：它在**发送状态**列，是人写的历史叙述
  「⚠️ **机器判据仍只认 `📥 已回件并回灌` 前缀**……**须走 `串行豁免：前信质量部#7 为 ✅ 无需回复 形态、发出即闭环` 逃生阀**」
  ——反引号里那句是被**引用**的口令，不是本行自己的豁免声明。⇒ **属「需要人裁」，未动，已交看护者**（🟡 停点 ⑷）。
  实测：全表 `串行豁免：` 命中数 **5 → 1**；
  `_validate_followup_readme_release(当前, origin/master 快照)` ＝ ✅ 无违规。
- [x] 6.2 🔴 `跟进信行日志/质量部#10.md` 里**人写**的那段「本行不是真的豁免」**一个字不动**（历史记录不追改；且它已迁出 README 行内、不构成开闸口令）
- [x] 6.3 commit message 写明依据（`#482` ④ ＋ design D6），并说明为何这五处不属「历史记录不追改」的保护范围

## 7. 口径正本与人守退休

- [x] 7.1 README 串行原则段：写入 D2 的「变更收信人 MUST 改 `·` 之后的姓名，MUST NOT 用括注表达」
- [x] 7.2 README 串行原则段：写入 `❌ 已作废` 的分档要求与 `作废豁免：` 逃生阀
- 🟡 **7.3 未做·已交看护者**（派单件硬口径 4：本件「不写队列文件」）。原文：🔴 **删除 §四 `#124` 截止段那条人守**（「凡新增跟进信行务必让收信人列与该收信人前一封逐字一致」）——本包让它成为多余。**净减一条人守，MUST 在收工时确认它真的被删掉了**，否则本包的 one-in-one-out 账不平。
- 🟡 **7.4 未做·已交看护者**（同 7.3）。原文：回写队列 §四 `#124`／`#90`：本包交付后标已承接，指向本变更包
- 🟡 **7.5 未做·已交看护者**（同 7.3）。原文：回写队列 §一 `#482` 状态与产出路径

## 8. 验收（proposal §验收与晋档「真实生效」五条）

- [x] 8.1 `followup_gate` 新函数全量单测绿，含 🔴 误放方向反例
- [x] 8.2 四处实现全部改调同一函数，`工具-队列结构lint.py` 可证明无第五处分叉
- [x] 8.3 在**生产 README 真身**上复算，四位收信人的「最近一封」四把尺子给出同一答案
- [x] 8.4 存量 5 行清完，且再跑一次行长外置不产生新豁免
- [x] 8.5 🔴 **一次真实双向验证**：构造「同一收信人、注记不同、前一封在途」⇒ append 与 release 两端都拒；把前一封改闭环 ⇒ 两端都放。**这一条不发生，前四条全绿也不算生效。**
- [x] 8.6 价值指标复测并登记：误判行数 5→0；机器写豁免行数 5→0 且增长率归零；独立实现份数 4→1

---

## 实测记录（`OP-0907-AJ` 收工，2026-09-07）

- **单测**：`0-学习与工具/test_工具-共享文档编辑锁.py -k FollowupSerialGate` ＝ **28 passed / 1 skipped / 21 subtests**；
  四个跟进信工具套 ＋ `test_followup_gate.py` 合跑 ＝ **171 passed**；
  `5-平台底座/wecom-aibot-service/tests` ＋ `zhuopin_platform/tests` ＝ **1323 passed / 2 skipped**。
- **8.2 无第五处分叉**：`工具-队列结构lint.py` 的符号断言名单已扩到 9 个新符号
  （`recipient_identity`／`same_recipient`／`recipient_department_raw`／`latest_letter_for_recipient`／
  `letter_row_identity`／`classify_void_risk`／`validate_void_reason`／`void_waiver_reason`／`serial_waiver_reason`），
  实测 lint 通过；第 4 处分叉 `工具-跟进信README行长外置.py::_find_serial_gate_conflicts` **已删除**，单测钉死它不得复活。
- **8.3 四把尺子同一答案**（对生产 README 真身复算）：

  | 收信人 | ①闸查询 | ②编辑锁 | ③配对侧 `latest_letter_for_recipient` | ④`latest_dispatched_letter` |
  |---|---|---|---|---|
  | 陈忱 | 质量部#13 | 质量部#13 | 质量部#13 | 质量部#13 |
  | 姚祖怡 | 采购部#21 | 采购部#21 | 采购部#21 | 采购部#21 |
  | 唐燕萍 | 财务部#17 | 财务部#17 | 财务部#17 | 财务部#17 |
  | 陈承 | IT部#11 | IT部#11 | IT部#11 | IT部#11 |

- **8.4**：`python 0-学习与工具/工具-跟进信README行长外置.py --dry-run` ⇒ 无候选行；自动写豁免的写盘路径已随
  `SERIAL_WAIVER_REASON` 一并删除，**不可能再产生新豁免**。
- **8.5 真实双向验证**（拿生产 README 真身做底本，只读、不落盘）：

  ```
  前一封在途(质量部#13 ✅已推送): append前置=拒 ｜ release后置=拒 ｜ ✅ 同判
     文案：跟进信串行原则：新增行收信人「质量 · 陈忱」的最近一封「质量部#13」
           （2026-09-04，✅ 已推送 2026-09-04 06:24 UTC）尚未闭环……
  前一封改闭环(质量部#13 ✅无需回复): append前置=放 ｜ release后置=放 ｜ ✅ 同判
  ```

  新增行收信人写 `质量部 · 陈忱（可分担朱映桦）`、前一封写 `质量部 · 陈忱（质量域 AI 专员）`
  ——**注记不同、判为同一人**，这正是改造前会静默放行的那一种。
- **8.6 价值指标**：误判行数 **5 → 0**（4 行删除 ＋ 1 行交人裁，判据侧已不再产生）；
  机器写豁免行数 **4 → 0 且增长率归零**（写盘路径删除）；独立实现份数 **4 → 1**（lint 机器守）。
- `openspec validate --all --strict` ＝ **174 passed / 0 failed**。

## 🟡 停点（逐条交看护者）

| # | 停点 | 为什么停 | 不做的代价 |
|---|---|---|---|
| ⑴ | **ff 合入 master** | 派单件明标 🟡 | 全部改动停在 `claude/op0907aj-serial-gate-482-apply`，不生效 |
| ⑵ | **据实改 `质量部#13` 为 `✅ 无需回复`**（0.4） | 派单件明标 🟡 | 🔴 **ff 后陈忱的闸立刻由 ✅ 开变 🔒 锁**。这是判据的**正确输出**（该行状态列确实是 `✅ 已推送`），但它本应是 `✅ 无需回复`（交期要点写「知会性质，不用回复」），被发送脚本回填覆盖。改这一格属既有合法转态，不需要新机制 |
| ⑶ | **队列回写 7.3／7.4／7.5** | 派单件硬口径 4：本件不写队列文件 | §四 `#124` 那条人守仍挂着——本包已让它成为多余，**不删则 one-in-one-out 账不平** |
| ⑷ | **`质量部#7` 那处人写的 `串行豁免：` 叙述** | 需要人裁「这行该不该豁免」，派单件规则 6 | 它在机器眼里**仍是一个开闸口令**（`serial_waiver_reason` 读到非空理由 ⇒ 放行）。风险已被 D5(a) 大幅压低（改编号主键后该行只有**改编号**才会被判成新增行），但不为零 |
| ⑸ | **「发送脚本回填覆盖闭环态」另立队列行**（0.4 后半） | 同 ⑶ | 该漏洞在 `质量部#7` 行内已记 20 天、无承接方；不立行则 ⑵ 改完还会被再覆盖一次 |
