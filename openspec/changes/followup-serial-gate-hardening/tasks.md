# Tasks — 串行闸归一化（`OP-0907-K`，2026-09-07）

> 🔴 **本包当前只完成 propose ＋ design 阶段**（队列 §一 `#482` 派单明写「本次范围＝只到 propose＋design 起草，不 apply」；`OP-0907-K` 另加硬约束：**MUST NOT 改动 `工具-跟进闸查询.py`／`工具-共享文档编辑锁.py` 任何一行**）。
> 下方第 1–7 节为**实现期任务清单，全部未开工**；实现须另派单件（属 CC 建造，触碰生产代码与生产 README）。

## 0. 🔴 开工前置（未决，须 Shao Peishen 或 design 审拍板）

- [ ] 0.1 **design 审拍板 D3**：「最近一封」用哪把尺子。推荐 (a) 日期排序键。🔴 **拍 (a) 会立刻把陈忱的闸从 ✅ 开变成 🔒 锁**（依据 `质量部#13` 2026-09-04 `✅ 已推送`）。若拍 (b) 维持现状，则 `latest_dispatched_letter` 那句「与跟进闸同一把尺子」的 docstring MUST 当批改掉。
- [ ] 0.2 **design 审拍板 D5**：④ 的根治走 (a) 行身份改用编号列主键，还是 (b) 新增专用标记 `行长外置：`。推荐 (a)。🔴 (a) 会削弱两态语义检查的一个面（改已有编号行的状态为 `🆕 待发` 将不再被拦），缓解方案见 D5(a) 代价段，须被明确看见。
- [ ] 0.3 **design 审拍板 D6**：存量 5 行机器豁免删还是留。推荐删（辨析见 D6：它们是控制指令不是历史叙述）。
- [ ] 0.4 **配套前置（若 0.1 拍 (a)）**：上线前先把 `质量部#13` 据实改为 `✅ 无需回复`（一次合法转态，不需要新机制），并把「发送脚本回填覆盖闭环态判定」另立队列行——该漏洞 `质量部#7` 行内已记 20 天，至今无承接方。
- [ ] 0.5 **本包不得先于依赖包归档**：核对 `followup-closure-evidence-gate` 与 `followup-readme-phase2` 的 delta 是否已 sync 进主 specs；若未 sync，本包只能用 `ADDED`（当前即如此）。

## 1. 权威判据（`5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/followup_gate.py`）

- [ ] 1.1 新增 `RecipientIdentity` 类型与 `recipient_identity(cell) -> RecipientIdentity | None`（D1／D2 规则）
- [ ] 1.2 新增 `same_recipient(a, b) -> bool`，🔴 任一侧 `None` 一律 `False`
- [ ] 1.3 `recipient_department` 改为 `recipient_identity(...)[0]` 的派生，**对外签名与语义不变**
- [ ] 1.4 若 0.1 拍 (a)：新增 `latest_letter_for_recipient(rows, identity)`，与 `_letter_sort_key` 共用同一排序键
- [ ] 1.5 隔离环境回落实现（无平台包时）MUST 与权威实现由**同一份单测双跑**，否则回落分支会静默漂移
- [ ] 1.6 `0-学习与工具/工具-队列结构lint.py` 的符号断言名单加入 1.1／1.2／1.4 新符号；既有符号一个都不改名

## 2. 🔴 回归用例先写先跑先红（D7，次序不得与第 3 节对调）

- [ ] 2.1 T1 误放（`质量部#7` 实况：多写注记 ⇒ 现状放行）——**改造前 MUST 红**
- [ ] 2.2 T3 误放镜像（`采购部#21` 实况：少写注记）——**改造前 MUST 红**
- [ ] 2.3 T6 两端同判（append 前置 vs release 后置）——**改造前 MUST 红**
- [ ] 2.4 T2 误拦（`质量部#10` 实况）／T4 解析失败不匹配／T5 跨部门同名不合并
- [ ] 2.5 🔴 **把「改造前跑一次、2.1–2.3 三条红」的实测输出原样贴进本行**——否则无从证明这三条测试真的在守什么

## 3. 四处消费方改调（每处改完全量回归）

- [ ] 3.1 `工具-共享文档编辑锁.py::_validate_followup_readme_release` 收信人比对改 `same_recipient`；解析失败记 violation 并指名该行
- [ ] 3.2 `工具-跟进闸查询.py::build_report`／`all_recipients` 改 `recipient_identity`（`_collect_intake_rows` 的部门粒度本包不改，其路标另有承接）
- [ ] 3.3 `工具-跟进信README登记.py::_assert_gate_open` 改调 ＋ 两处静默分支出声（解析失败、`GateQueryError`）
- [ ] 3.4 `readme_table.split_department_and_name` 改为权威函数的薄封装（保留符号名——`dispatch.py`／`draft_gap_detection.py`／`工具-跟进信README查询.py` 三个现役消费方按名取值）
- [ ] 3.5 若 0.1 拍 (a)：三处「最近一封」改调 1.4；`latest_dispatched_letter` docstring 与事实对齐

## 4. `❌ 已作废` 防滥用判据（D4）

- [ ] 4.1 `followup_gate` 新增 `classify_void_risk(prior_status, is_new_row) -> "low" | "high"`
- [ ] 4.2 新增 `validate_void_reason(status_cell, risk) -> list[str]`，实现 R1（≥8 全角字）／R2（黑名单）／R3（可核凭据形态）
- [ ] 4.3 `_validate_followup_readme_release` 接入，判据源取 acquire 快照，**不读 git**
- [ ] 4.4 `作废豁免：` 逃生阀（放行 ＋ 打印留痕；空理由拒绝）
- [ ] 4.5 T7／T8 反例单测 ＋ T9 正例单测（🔴 T9 取归档件里那行真实作废的理由文本，防止判据把合法作废拦死）
- [ ] 4.6 拒绝文案 MUST 指名命中的是 R1／R2／R3 的哪一条、以及命中的黑名单词是哪一个

## 5. 停止机器制造豁免（D5，方案待 0.2）

- [ ] 5.1 退休 `工具-跟进信README行长外置.py::_find_serial_gate_conflicts`（复刻实现，逐字继承了 3.1 的缺陷），改调权威判据
- [ ] 5.2 删除 `SERIAL_WAIVER_REASON` 常量与自动写入路径
- [ ] 5.3 若 0.2 拍 (a)：`_followup_row_identity` 改编号列主键 ＋ 无编号行回落并出声 ＋ 两态语义检查改为同时看身份与状态迁移
- [ ] 5.4 若 0.2 拍 (b)：新增 `行长外置：` 专用标记及其「除主要事项列外逐字相等」的机器验证
- [ ] 5.5 `串行豁免：` 判据收窄为「标记后 MUST 跟非空理由」（不做语义判断）
- [ ] 5.6 T10：对含长行的 README 副本跑一次行长外置，产物中 `串行豁免：` 计数 MUST 为 0 且 release 通过

## 6. 清存量（D6，🔴 MUST 在第 5 节之后）

- [ ] 6.1 删除 5 行 README 单元格里机器写入的 `串行豁免：D3 行长外置压缩摘要，非新起草跟进信`（`采购部#12`／`IT部#8`／`质量部#7`／`质量部#10`／`质量部#12`）
- [ ] 6.2 🔴 `跟进信行日志/质量部#10.md` 里**人写**的那段「本行不是真的豁免」**一个字不动**（历史记录不追改；且它已迁出 README 行内、不构成开闸口令）
- [ ] 6.3 commit message 写明依据（`#482` ④ ＋ design D6），并说明为何这五处不属「历史记录不追改」的保护范围

## 7. 口径正本与人守退休

- [ ] 7.1 README 串行原则段：写入 D2 的「变更收信人 MUST 改 `·` 之后的姓名，MUST NOT 用括注表达」
- [ ] 7.2 README 串行原则段：写入 `❌ 已作废` 的分档要求与 `作废豁免：` 逃生阀
- [ ] 7.3 🔴 **删除 §四 `#124` 截止段那条人守**（「凡新增跟进信行务必让收信人列与该收信人前一封逐字一致」）——本包让它成为多余。**净减一条人守，MUST 在收工时确认它真的被删掉了**，否则本包的 one-in-one-out 账不平。
- [ ] 7.4 回写队列 §四 `#124`／`#90`：本包交付后标已承接，指向本变更包
- [ ] 7.5 回写队列 §一 `#482` 状态与产出路径

## 8. 验收（proposal §验收与晋档「真实生效」五条）

- [ ] 8.1 `followup_gate` 新函数全量单测绿，含 🔴 误放方向反例
- [ ] 8.2 四处实现全部改调同一函数，`工具-队列结构lint.py` 可证明无第五处分叉
- [ ] 8.3 在**生产 README 真身**上复算，四位收信人的「最近一封」四把尺子给出同一答案
- [ ] 8.4 存量 5 行清完，且再跑一次行长外置不产生新豁免
- [ ] 8.5 🔴 **一次真实双向验证**：构造「同一收信人、注记不同、前一封在途」⇒ append 与 release 两端都拒；把前一封改闭环 ⇒ 两端都放。**这一条不发生，前四条全绿也不算生效。**
- [ ] 8.6 价值指标复测并登记：误判行数 5→0；机器写豁免行数 5→0 且增长率归零；独立实现份数 4→1
