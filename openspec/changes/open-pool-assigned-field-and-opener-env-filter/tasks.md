# tasks — open-pool-assigned-field-and-opener-env-filter

> 🚨 **紧急件。** 🛑 1.x 起须 design 审过后才开工。执行环境：**CC**（写生产码、自行 commit+push、一任务一 worktree）。
> 🔴 **触碰区与在办的 `#435` 零重叠**（本包碰 `open_pool_reminder.py` ＋ 看板 JS ＋ 协议〇；`#435` 碰 `工具-落库sweep.py` ＋ `工具-共享文档编辑锁.py`）⇒ **可并行，不必等 `#435`**。

## 0. 前置

- [ ] 0.1 design D1-D4 审通过（记录于队列 §一 `#312`）

## 1. 实现

- [ ] 1.1 `open_pool_reminder.py`：判据加「行含 `[A:` 即排除」（不解析取值）
- [ ] 1.2 看板 artifact `zhuopin-project-status` JS 同判据（🔴 **两处必须同一判据**——`#312` 本就是两半一行，判据分叉即再次失真）
- [ ] 1.3 `find_opener_path`：候选块 `【设置】` 执行环境 != 行领取方环境 ⇒ 不给路径，改提示「opener 未定位，见队列行」
- [ ] 1.4 顺序守卫：检出 `[A:…]` 出现在 `[D:…]` 之前即告警（🔴 **这是本包最容易被跳过的一项**——写反不会报错，只会让域字段静默变 `None`）
- [ ] 1.5 存量回填候选器：列「`[S:open]` 且正文以 🔄 起首」的行；🔴 只出清单、不自动写
- [ ] 1.6 协议〇 状态字段说明补一段：字段顺序 `S → D → A`，A 可选

## 2. 测试

- [ ] 2.1 `[S:open][D:机][A:已派出]` → 既有解析器仍得 `('open','机')`，池子排除
- [ ] 2.2 `[S:open][A:已派出][D:机]` → **告警**（回归钉子：该写法会让 D 变 `None`）
- [ ] 2.3 删掉 `[A:…]` → 该行重新进池
- [ ] 2.4 opener 环境过滤：Cowork 块不得充当 CC 行的 opener（**用 `#435` 与 `开场prompt-【Cowork】构建环境接力-2026-08-29晚.md` 这对真实数据做回归**）
- [ ] 2.5 全量回归绿、零回归

## 3. 真实验活

- [ ] 3.1 对真实队列跑一次 dry-run：`#435`（打 A 后）与 `#381` 应不在池中；`#436` 等真待领行仍在
- [ ] 3.2 看板卡与推送两处输出集合**逐条一致**（判据同一的唯一验收方式）
- [ ] 3.3 回填候选器对真实队列跑，至少列出 `#381`；清单交人过目

## 4. 收口

- [ ] 4.1 给 `#435`／`#381` 等已确认在办行打 `[A:已派出]`（**人过目后写，不自动**）
- [ ] 4.2 §二 批次登记 ＋ commit+push（ff-only）
- [ ] 4.3 `#312` 回写：状态列开头写实测结果；与 `#435` 子项 E 互写指针（两包是近亲，防各造一套判据）
- [ ] 4.4 全部 [x] 后**当场** `/opsx:archive open-pool-assigned-field-and-opener-env-filter -y`
