# tasks — open-pool-assigned-field-and-opener-env-filter

> 🚨 **紧急件。** 🛑 1.x 起须 design 审过后才开工。执行环境：**CC**（写生产码、自行 commit+push、一任务一 worktree）。
> 🔴 **触碰区与在办的 `#435` 零重叠**（本包碰 `open_pool_reminder.py` ＋ 看板 JS ＋ 协议〇；`#435` 碰 `工具-落库sweep.py` ＋ `工具-共享文档编辑锁.py`）⇒ **可并行，不必等 `#435`**。

## 0. 前置

- [x] 0.1 design D1-D4 审通过（Shao Peishen 2026-08-30 答「审过」，整体通过、无点名修改；记录于队列 §一 `#312`）

## 1. 实现

- [x] 1.1 `open_pool_reminder.py`：判据加「行含 `[A:` 即排除」（不解析取值）
- [x] 1.2 ✅ **Cowork 已完成（2026-08-30 `OP-0829-W`）**：改的是本机源文件 `C:\Users\Paul Shao\Claude\Artifacts\zhuopin-project-status\index.html`（**仓库外载体，不进 git**；动前 `Copy-Item` 备份为 `index.html.bak-20260830-OP0830D` 并留原文 SHA16）。**三处改动**：⑴ 入池判据加 `-and $scq -notmatch '\[A:'`（与 1.1 的 `_ASSIGNED_MARKER in status_cell` **逐字同一判据**：作用对象同为整个状态格、同为字面量匹配、同不解析取值）；⑵ 🔴 **同批必改的二阶项——桶归类的剥前缀正则 `^[\s\*]*\[S:[^\]]*\](\[D:.\])?\s*` 加上 `(\[A:[^\]]*\])?`**：不加则带 `[A:]` 的行前缀剥不干净，`^🔄`／`^✅` 等桶判据全部失配、整行掉进「其他」桶——**这正是该文件 L210-214 注释里记过的那次「122 行全落其他桶、看起来最健康的时候恰是坏掉的时候」的复发条件**，CC 侧无此结构故派单件未提示，由 Cowork 侧取证补上；⑶ 页脚判据说明同步。**回读核验七项全过**：U+FFFD＝0／三处改动各命中 1／`<script>` 开闭 3:3／中文抽验正常／65,380→65,594 B。
      🔴 **一处未能核实、如实登记**：本 session 的 `Artifact list`（scope=all）返回空，**够不到已发布的 artifact 本体**，故只能确认源文件已改，**无法验证发布态是否随之生效**——留待他打开看板时一眼可见。
- [x] 1.3 `find_opener_path`：候选块 `【设置】` 执行环境 != 行领取方环境 ⇒ 不给路径，改提示「opener 未定位，见队列行」
- [x] 1.4 顺序守卫：检出 `[A:…]` 出现在 `[D:…]` 之前即告警（🔴 **这是本包最容易被跳过的一项**——写反不会报错，只会让域字段静默变 `None`）
- [x] 1.5 存量回填候选器：列「`[S:open]` 且正文以 🔄 起首」的行；🔴 只出清单、不自动写
- [x] 1.6 协议〇 状态字段说明补一段：字段顺序 `S → D → A`，A 可选

## 2. 测试

- [x] 2.1 `[S:open][D:机][A:已派出]` → 既有解析器仍得 `('open','机')`，池子排除
- [x] 2.2 `[S:open][A:已派出][D:机]` → **告警**（回归钉子：该写法会让 D 变 `None`）
- [x] 2.3 删掉 `[A:…]` → 该行重新进池
- [x] 2.4 opener 环境过滤：Cowork 块不得充当 CC 行的 opener（**用 `#435` 与 `开场prompt-【Cowork】构建环境接力-2026-08-29晚.md` 这对真实数据做回归**）——单测用真实文件内容做集成测试（`test_build_pool_items_real_cowork_file_not_used_as_cc_opener`），另配 5 条手工构造边界用例（确认冲突排除／匹配放行／跳到下一候选／行 env 未知不过滤／候选块未声明环境不过滤）
- [x] 2.5 全量回归绿、零回归——服务测试 658 passed／1 skipped（含新增 22 项），零失败零回归

## 3. 真实验活

- [x] 3.1 对真实队列跑一次 dry-run，🔴 **前提已变、如实更正**：`#435` 已于 `OP-0830-C`（本包开工前）转 `[S:done]`（六件同车已交付），**不再需要靠 `[A:...]` 排除**——它现在不在池中是因为 `done`，不是因为打了 A；本次真正验证 A 字段效果的是 `#381`（打 A 前 22 行在池、含 `381`；打 A 后 21 行、`381` 已不在池）。`#436` 等真待领行仍在（22/21 行逐一核对，仅 `381` 一行差异）。
- [ ] 3.2 🟡 **判据已逐字比对一致，但「两处真跑比对」仍未完成——如实不勾（2026-08-30 Cowork）**。
      **已做**：推送侧真跑 `parse_open_pool_rows` 对两份生产队列 ⇒ **22 条**（`240/341/358/373/374/379/382/390/391/394/403/410/416/418/423/424/425/431/432/433/434/436`）；判据文本逐字比对——CC `if _ASSIGNED_MARKER in status_cell` ／ 看板 `-and $scq -notmatch '\[A:'`，作用对象、匹配方式、不解析取值三点一致。
      **未做的那一半，以及为什么不硬做**：看板判据嵌在 artifact 的 PowerShell 数据层字符串里，本线**手写复刻跑了一次，得 0 条**——那不是「看板排除得更严」，是复刻有 bug。🔑 **正是「太干净的结果，先问它是不是根本没读到我以为的那个对象」那一族**（根 `CLAUDE.md` §5 工具静默回退）。**用一个自己写的复刻去验收另一份实现，等于把待验对象换成了自己**，故**不以复刻充当验收**。
      **收口路径（只差一次真跑）**：他打开看板卡，读「可 Open 池」的 N 与行号清单，与上面 22 条比对；一致即勾。
- [x] 3.3 回填候选器对真实队列跑，列出 `381`（且仅此一条——已逐行人工复核其余 21 条 open 行的状态列自陈文本，`382`/`410`/`416`/`418` 等语义各异但均非"在办"自陈，如实登记见 §四 回写，未纳入回填）；清单交人过目

## 4. 收口

- [x] 4.1 给 `#381` 打 `[A:已派出]`（人过目后写，不自动）。🔴 **`#435` 不适用**（已转 `done`，打 A 无意义）；其余 21 条 open 行逐一读过状态列自陈文本，均无"已派出/在办"自陈，未打 A（含疑似接近但证据不足的 `#416`/`#418`——`#418` 更像 `#435` 子项 E 的 `blocked` 候选而非本包的 `A` 候选，见 §四 回写）。
- [x] 4.2 §二 批次登记 ＋ commit+push（ff-only）
- [x] 4.3 `#312` 回写：状态列开头写实测结果；与 `#435` 子项 E 互写指针（两包是近亲，防各造一套判据）
- [ ] 4.4 🔴 **暂不归档**（2026-08-30 更新阻碍项）——1.2 已由 Cowork 完成，**唯一未闭合的是 3.2 的「两处真跑比对」**，它需要他打开看板卡读一次数（见 3.2 收口路径）。**预期观察窗口：3 天**（至 2026-09-02）；届时仍未比对，由值周巡检决定是带 3.2 未验的说明归档、还是继续挂。🔴 **本行即「这次不归档的理由」，写在机器认得的地方**（根 `CLAUDE.md`「完工即归档」纪律三条入口之一：`暂不归档` ＋ `预期观察窗口：N 天`）。
