# opener-id-claim-semantics Tasks

> ~~🔴 **design 审通过前一条都不得动手。**~~ ✅ **闸已合上（2026-09-10 `OP-0910-R` 回填）**：Shao Peishen 2026-09-08 答 `1(a)`（design 放行、按推荐取值 ①a／②a／③a），答案已回填 `design.md` 文首与各决策点；2026-09-10 追加授权机器占位（`#549` ⑶，见 design 文首追加段）。本清单据此 apply，勾选＝本泳道实做。

## 0. 前置闸（design 审后、动手前）

- [x] 0.1 决策点 ①②③ 均已拍板，答案逐条回填 `design.md`（写明拍板日期与手段）。✅ 2026-09-10 `OP-0910-R`，手段＝队列 `#531` 行现读。
- [x] 0.2（③ 选 (a)，不适用）决策点 ③ 若选 (b)（照派工原文改 `_reserve_ids`）⇒ **先核 `#480` 先例的具体做法**，并与协议〇.8「编号永不复用」当面对质，冲突未解不得动手。
- [x] 0.3 核同泳道 A3（`single-guarded-copy`）是否已 apply——两包同触 `工具-opener生成.py`，须定先后。✅ 实查未 apply ⇒ 本包先行，该包后 rebase；已写进两包（design「触碰区核对」＋ 该包 tasks 0.5）。

## 1. 取证与口径（🔴 必须先于实现）

- [x] 1.1 ✅ 2026-09-10 实测：`session接力-Phase1收口.md` 在跑表 1 行（`OP-0910-H`，09-10 10:1x 起、未划、是真在跑）＋ 一句并发备注（`OP-0910-A`／`OP-0910-G`）；无遗忘残留。**实测各线接力卡「⏳ 本线在跑会话」表当前的残留行数**（未划掉的在跑行），逐条判断是真在跑还是忘了划——这是决策点 ①(a)／②(a) 下"仍会挡号"的实际规模。**propose 期未核。**
- [x] 1.2 ✅ 两份 ps1 均不取号、不含 `--op-id`／`OP-` 字样（`grep -c` 均为 0），只解析 plan 正文 ⇒ 无连带面。核 `工具-opener批处理执行.ps1`／`v2.ps1` 是否自行取号或复用 `--op-id`（决定文案与行为改动的连带面）。**propose 期未核。**
- [x] 1.3 ✅ 基线文案（改前）：「编号 OP-MMDD-X 当日（MMDD）已被使用（撞号，队列 #461／#487 P7① 查重，命中全称或 `[Win]MMDDX-` 短形）；下一个空号：OP-MMDD-Y」。取基线：跑一次 `python 0-学习与工具/工具-opener生成.py`（任意合法参数 ＋ 一个当日已用号）确认现行撞号报错文案原文，抄进本清单。

## 2. 实现

- [x] 2.1 ✅（`_scan_used_suffixes` docstring） `_scan_used_suffixes` 文档字符串补射程自陈：只认已落盘痕迹；取号与落盘之间存在真空；据 `#487` 裁定 ⑵，该真空不由本工具消除。
- [x] 2.2 ✅（报错文案末尾「射程见 `_scan_used_suffixes` 文档字符串」） `_check_op_id_not_reused` 报错文案**指向** 2.1 的自陈（不重述）。
- [x] 2.3 ✅（骨架「取号」节） `opener骨架.md`「取号」节写入纪律正文：「未落盘不算占；取到号但 opener 从未派出 ⇒ 当场作废、不预留、不登记；不得为『等下可能会用』先写进任何文件占位」。
- [x] 2.4 ✅（①(a)：改写为「只为可见性，不构成占号」＋ 写明仍会被扫到的例外） `opener骨架.md` 文末「在跑清单」条按决策点 ① 的答案改写／删除／升格。
- [x] 2.5 ✅（②(a)：零代码改动，只写例外说明） 决策点 ② 若选 (b)：`_scan_used_suffixes` 排除在跑清单表格区（须先造结构判据）；选 (a)：只写例外说明，零代码改动。
- [x] 2.6 ✅（③(a)：`openspec/specs/editlock-queue-number-reservation/spec.md` Purpose 补交叉说明） 决策点 ③ 按答案落地：(a) 只在 `editlock-queue-number-reservation` spec Purpose 补交叉说明；(b)/(c) 另按其内容实现。
- [x] 2.7 ✅（模板库 §〇 补一条判据） `专线opener模板库.md` §〇 补一条判据（**只写判据、不写正文**）。

## 3. 测试

- [x] 3.1 ✅ `test_工具-opener生成.py`：撞号报错文案含指向射程自陈的那一句（正例）。
- [x] 3.2（②(b) 未中选，不适用） 决策点 ②(b) 若中选：在跑清单内的号**不**被判为已用（正例）／落在正文其它位置的同一个号**仍**被判为已用（**非恒真自证**，证明排除的是清单区而不是整份文件）。
- [x] 3.3 ✅（改为 2026-09-10 追加授权的机器占位：`test_工具-opener生成.py::取号即声明`，含 fail-closed 用例与变异靶子） 决策点 ①(c) 若中选：占位落盘成功 ⇒ 出件；落盘失败 ⇒ 非零码退出且不出件（fail-closed）。
- [x] 3.4 ✅ 实测数见队列 `#549` 收工回写 全量跑 `test_工具-opener生成.py` ＋ `test_工具-opener块lint.py`，**实测数抄进收工报告**（不得只写"通过"）。

## 4. 现网验证（不落盘）

- [x] 4.1 ✅ 见 `#549` 收工回写（当日已用号 `OP-0910-A` 试取） 用一个当日已用号试取 ⇒ 确认报错文案与射程自陈一致。
- [x] 4.2 ✅ 见 `#549` 收工回写（在跑清单里的 `OP-0910-H` 试取 ⇒ 按 ②(a) 被拦） 用一个仅出现在接力卡在跑清单里的号试取 ⇒ 按决策点 ② 的答案确认行为符合预期（这是 ①／② 是否真的生效的唯一验证动作）。

## 5. 收口

- [x] 5.1 ✅（随 `#549` 收工回写 `#531`／`#487`） 队列 §一 `#487` 回填：design 审结论、三个决策点答案、**「派工描述经实测收窄」的如实说明**（原描述指向 `_reserve_ids`，实撞与可改点在 OP 号空间）。
- [ ] 5.2 ⏳ 本泳道未动（两条定夺项仍在同目录 `#487主项复核-例外句源头不放.md`，去向由总线定，不随本包销号） `#487` 主项复核结论（同目录 `#487主项复核-例外句源头不放.md`）中的**两条定夺项**登记去向——不得随本包销号而默认已解决。
- [x] 5.3 ✅（§二 批次 `B-0910_起棒基建`） 收工重跑文档台账；登记 §二 待 commit 批次，由落库 sweep 取活。
- [ ] 5.4 🔴 **合入 master 是 🟡 档，泳道自己不合**（分支已 push，等 sweep／看护者串行 ff）。 🔴 **合入 master 是 🟡 档，泳道自己不合。**
