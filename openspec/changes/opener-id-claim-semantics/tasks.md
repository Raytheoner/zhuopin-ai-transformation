# opener-id-claim-semantics Tasks

> 🔴 **design 审通过前一条都不得动手。** 三个决策点全部待 Shao Peishen 拍板；决策点 ③ 尤其重要——它决定本包要不要碰队列编号侧。

## 0. 前置闸（design 审后、动手前）

- [ ] 0.1 决策点 ①②③ 均已拍板，答案逐条回填 `design.md`（写明拍板日期与手段）。
- [ ] 0.2 决策点 ③ 若选 (b)（照派工原文改 `_reserve_ids`）⇒ **先核 `#480` 先例的具体做法**，并与协议〇.8「编号永不复用」当面对质，冲突未解不得动手。
- [ ] 0.3 核同泳道 A3（`single-guarded-copy`）是否已 apply——两包同触 `工具-opener生成.py`，须定先后。

## 1. 取证与口径（🔴 必须先于实现）

- [ ] 1.1 **实测各线接力卡「⏳ 本线在跑会话」表当前的残留行数**（未划掉的在跑行），逐条判断是真在跑还是忘了划——这是决策点 ①(a)／②(a) 下"仍会挡号"的实际规模。**propose 期未核。**
- [ ] 1.2 核 `工具-opener批处理执行.ps1`／`v2.ps1` 是否自行取号或复用 `--op-id`（决定文案与行为改动的连带面）。**propose 期未核。**
- [ ] 1.3 取基线：跑一次 `python 0-学习与工具/工具-opener生成.py`（任意合法参数 ＋ 一个当日已用号）确认现行撞号报错文案原文，抄进本清单。

## 2. 实现

- [ ] 2.1 `_scan_used_suffixes` 文档字符串补射程自陈：只认已落盘痕迹；取号与落盘之间存在真空；据 `#487` 裁定 ⑵，该真空不由本工具消除。
- [ ] 2.2 `_check_op_id_not_reused` 报错文案**指向** 2.1 的自陈（不重述）。
- [ ] 2.3 `opener骨架.md`「取号」节写入纪律正文：「未落盘不算占；取到号但 opener 从未派出 ⇒ 当场作废、不预留、不登记；不得为『等下可能会用』先写进任何文件占位」。
- [ ] 2.4 `opener骨架.md` 文末「在跑清单」条按决策点 ① 的答案改写／删除／升格。
- [ ] 2.5 决策点 ② 若选 (b)：`_scan_used_suffixes` 排除在跑清单表格区（须先造结构判据）；选 (a)：只写例外说明，零代码改动。
- [ ] 2.6 决策点 ③ 按答案落地：(a) 只在 `editlock-queue-number-reservation` spec Purpose 补交叉说明；(b)/(c) 另按其内容实现。
- [ ] 2.7 `专线opener模板库.md` §〇 补一条判据（**只写判据、不写正文**）。

## 3. 测试

- [ ] 3.1 `test_工具-opener生成.py`：撞号报错文案含指向射程自陈的那一句（正例）。
- [ ] 3.2 决策点 ②(b) 若中选：在跑清单内的号**不**被判为已用（正例）／落在正文其它位置的同一个号**仍**被判为已用（**非恒真自证**，证明排除的是清单区而不是整份文件）。
- [ ] 3.3 决策点 ①(c) 若中选：占位落盘成功 ⇒ 出件；落盘失败 ⇒ 非零码退出且不出件（fail-closed）。
- [ ] 3.4 全量跑 `test_工具-opener生成.py` ＋ `test_工具-opener块lint.py`，**实测数抄进收工报告**（不得只写"通过"）。

## 4. 现网验证（不落盘）

- [ ] 4.1 用一个当日已用号试取 ⇒ 确认报错文案与射程自陈一致。
- [ ] 4.2 用一个仅出现在接力卡在跑清单里的号试取 ⇒ 按决策点 ② 的答案确认行为符合预期（这是 ①／② 是否真的生效的唯一验证动作）。

## 5. 收口

- [ ] 5.1 队列 §一 `#487` 回填：design 审结论、三个决策点答案、**「派工描述经实测收窄」的如实说明**（原描述指向 `_reserve_ids`，实撞与可改点在 OP 号空间）。
- [ ] 5.2 `#487` 主项复核结论（同目录 `#487主项复核-例外句源头不放.md`）中的**两条定夺项**登记去向——不得随本包销号而默认已解决。
- [ ] 5.3 收工重跑文档台账；登记 §二 待 commit 批次，由落库 sweep 取活。
- [ ] 5.4 🔴 **合入 master 是 🟡 档，泳道自己不合。**
