# opener-id-claim-semantics Design

> ✅ **design 已审、决策点已回填（本段 2026-09-10 由 CC 泳道 `549-550-opener-infra`／`OP-0910-R` 回填，承接队列 §一 `#531` ⑴、`#549`）。**
> **回填依据（手段＝`python 0-学习与工具/工具-队列查询.py --row 531 --field all` 现读）**：Shao Peishen **2026-09-08 答 `1(a)`＝design 放行、三个决策点按起草方推荐取值** ⇒ **① (a)／② (a)／③ (a)**（逐条见各决策点下的「✅ 已答」行）。该答复此前只落在队列行、未回填本包，`tasks.md` 首行「design 审通过前一条都不得动手」因此把本包自己锁死（`#531` 主项原话「他答过」与「包内可见他答过」是两件事）——本段即解锁动作。
> ➕ **2026-09-10 追加授权（Shao Peishen 当日答 `1a，2a`，载体＝`1-转型规划/0-全景路线图/派单件-【CC】549起棒基建与550哨兵-2026-09-10.md` §三 ＋ 队列 §一 `#549` ⑶）**：在 ①(a) 之上**加一层机器占位**（`reports/op-id-claims.jsonl`，取号即写、落档后自动清理、按时效自动失效）。它**不推翻 ①(a)**：「未落盘不算占」仍是**永久占用**的唯一语义；占位只是给「取号→落档」那段真空一个**有时效的机器可见窗口**，到期即作废＝「未派出即作废」的机器实现，不是人手写进 `.md` 的占位登记（本 spec「不得为未派出的号做占位登记」禁的是后者）。与 proposal「`reports/` 变体不可见」的顾虑对照：本次台账按 `git rev-parse --git-common-dir` 落**主工作区** `reports/`，各 worktree 共享一份（同 `#504` 心跳件修法），可见性问题已解；跨机器仍不可见，两桌同机故可接受。
> ✅ **阈值一项已答**：占位时效 `CLAIM_TTL_MINUTES = 120`——Shao Peishen 2026-09-10 答 `1a` 定为 120 分钟（追认建造方初值，依据见 `工具-opener生成.py` 常量注释）；此前按根 `CLAUDE.md` §5「阈值类不设默认生效」登记为定夺项、未答期间不停建。
> ~~🔴 **本件是起草稿，未经审。**~~ 起草：泳道 `503-opener-single`（`OP-0908-Q`，批 `B-0908_四泳道机制根治`）做到 design 起草完即停——**起草是 🟢、审是 🟡**，起草方不自审自过、不合入 master。
> **决策人**＝Shao Peishen。~~**三个决策点全部待拍板。**~~ **三个决策点已于 2026-09-08 拍板（`1(a)`），2026-09-10 回填。**
> 🔴 **本包与派工描述有出入，见 proposal「Why §二」**：`#487` 状态列指向 `_reserve_ids`／`editlock-queue-number-reservation`，而实撞与可改点都在 `工具-opener生成.py` 的 OP 号空间。**起草方未按原描述动手，也未擅自把它删掉**——原样列为决策点 ③ 交审（依据＝该行自己写过的「已获授权 ≠ 该照原方案执行」）。

## 两个编号空间的分界（本包全部推理的前提，先钉死）

| | OP 号（`OP-MMDD-X`） | 队列行编号（`#503`） |
|---|---|---|
| 取号入口 | `工具-opener生成.py::_check_op_id_not_reused` | `工具-共享文档编辑锁.py::_reserve_ids`（`acquire --reserve`） |
| 判据来源 | 扫 `1-转型规划/` 全树 `.md` 的**已落盘痕迹** | **高水位线标注行**（只信它，fail-loud 不回落扫表） |
| 未用号怎么办 | **扫不到 ⇒ 可被再取**（＝ ⑵ 的语义，现行行为） | **留空洞、永不复用**（协议〇.8；`_reserve_ids` 文档字符串明写「本函数不做任何释放未用编号的操作，调用方也不需要」） |
| 2026-09-06 实撞在哪 | ✅ 这里（`OP-0906-D` 被两条线各取一次） | ❌ 不在这里 |

🔑 **两者对「预留后从未落盘」给出的答案不同，而且都是有意的**：OP 号是**流水号**（每天从 A 重排、只需当日不撞），队列行编号是**永久身份**（`#503` 这个号将被无数处引用，复用即毁掉可追溯性）。⇒ **把 ⑵ 搬到队列编号上会与协议〇.8 正面冲突**，不是"顺手一起改"。

## 决策点 ① 🔴 **须拍板**：⑴ 与 ⑵ 今天并存，留哪一条

**问题不是"⑵ 对不对"（他已裁两次），是"⑵ 与现存的 ⑴ 载体怎么办"。**

`opener骨架.md` 文末现有一条人守：「开新会话先登本线接力卡的『⏳ 本线在跑会话』清单……仓库 grep 只看得见已落档的编号，看不见此刻正在跑、还没写回任何文件的那一条。」——**接力卡在 `1-转型规划/` 树内，`_scan_used_suffixes` 会扫到它** ⇒ **登了表的号就变成"已落盘"、就算占**。于是今天的实际规则是：

> **算不算占，取决于取号人有没有记得去登一张表。**

| 案 | 做法 | 代价 |
|---|---|---|
| **(a)〔荐〕** | **⑵ 为唯一语义**：明写「未落盘不算占、未派出即作废不预留」；接力卡在跑清单**改写为「只为可见性，不构成占号」**（保留表，去掉占号含义） | 语义唯一、无需维护占位台账。代价 ＝ **真空期撞号这一残余被正式接受**（proposal 残余风险 1），且接力卡里的在跑行仍会被 `_scan_used_suffixes` 扫到 ⇒ **事实上仍然挡号**，与"不构成占号"的措辞矛盾 ⇒ 若选 (a)，须一并决定：**要不要让 `_scan_used_suffixes` 跳过接力卡的在跑清单**（这才是让 (a) 真正成立的那一刀） |
| (b) | 维持现状（两条并存，靠人判） | 🔴 **起草方明确不推荐**：本包的全部论证就是"同一机制两条相反语义、由人守决定走哪条"；维持现状 ＝ 本包不退休任何守卫、也不解决任何问题，且协议〇.9 措施 B 要求写明"为何不能退"，(b) 写不出这个理由 |
| (c) | **⑴ 为唯一语义**：取号成功即强制落一处占位（接力卡在跑表，或 `reports/op-id-ledger.jsonl`），落盘失败即不出件 | 撞号真正消除；代价 ＝ 占位台账要人维护（残留行持续挡号）；若走 `reports/` 版本，实测该目录被 `.gitignore:50:**/reports/` 整目录忽略 ⇒ 台账跨 worktree 不可见、不进 git，**不可见的占号台账等于没占**（同 `#504` 心跳件的坑） |

〔荐〕**(a)**，并配套「`_scan_used_suffixes` 是否跳过在跑清单」这一刀 —— 见决策点 ②。
✅ **已答 (a)**（Shao Peishen 2026-09-08 答 `1(a)`，按起草方推荐；回填 2026-09-10 `OP-0910-R`，手段＝队列 `#531` 行现读）。⑵ 为唯一永久占用语义；接力卡在跑清单改写为「只为可见性，不构成占号」（已落 `opener骨架.md` 文末）。➕ 2026-09-10 追加机器占位（见文首追加授权段）。
🔴 **本项无默认项，须明确答复。** 判据：不答 ⇒ 两条相反语义继续并存 ⇒ **错误继续发生**（撞号已发生两次，两次都是事后由人发现），不是"停在原地"。

## 决策点 ② 🔴 **须拍板**：(a) 中选时，`_scan_used_suffixes` 要不要跳过接力卡在跑清单

只有决策点 ① 选 (a) 时本项才成立（选 (c) 时恰恰相反，要**加强**读它）。

| 案 | 做法 | 代价 |
|---|---|---|
| **(a)〔荐〕** | **不跳过**，但把在跑清单的语义写清：「登进来的号**会**被查重扫到、**会**挡号——这是可见性的副作用，接受它」 | 措辞与实现一致，零代码改动；代价 ＝ ⑵ 的"未落盘不算占"在**登过表的那部分号**上并不成立，语义仍有一个例外，须在纪律正文里写明这个例外 |
| (b) | **跳过**：`_scan_used_suffixes` 识别接力卡在跑清单的表格区并排除 | ⑵ 语义纯净；代价 ＝ ⑴ 那条人守彻底失去作用（登了也不挡号），且要给"哪一段算在跑清单"造一条结构判据——**新判据 ＝ 新的漂移面**（同族教训：`工具-泳道纪律复制lint.py` 宁可运行时从正本提取也不写死词表） |
| (c) | 折中：跳过**已划掉**的行、保留未划掉的行 | 需要"划掉"的机器判据（删除线？状态列？），而该表是人手维护的自由格式 ⇒ **判据必然脆**，起草方不推荐 |

〔荐〕**(a)**（不跳过 ＋ 写明例外）。理由：本项目已反复吃亏于"为了让措辞干净而新造一条结构判据"；语义带一个写明白的例外，好过多一条会漂的判据。
✅ **已答 (a)**（同上）。`_scan_used_suffixes` **不跳过**接力卡在跑清单；例外已写进 `opener骨架.md`「取号」节：登进在跑清单的号**会**被查重扫到、**会**挡号，这是可见性的副作用。零代码改动。

## 决策点 ③ 🔴 **须拍板**：派工描述指向的队列编号侧，动还是不动

`#487` 状态列的派工原文要求改 `_reserve_ids`／`reserved_map` ＋ `editlock-queue-number-reservation` spec delta。**实测（本包起草时读码）该处对"预留后从未落盘"已有明确、相反且有理由的立场**（协议〇.8 编号永不复用、预留即烧号）。

| 案 | 做法 | 代价 |
|---|---|---|
| **(a)〔荐〕** | **不动**。只在 `editlock-queue-number-reservation` spec 的 Purpose 补一句交叉说明：队列行编号按协议〇.8 永不复用，与 OP 号的 ⑵ 刻意不同，两者不得互相援引 | 零行为改动、消除下一个人再次把两者混为一谈的可能；代价 ＝ 与派工原文不符，须在 `#487` 行内如实写明"派工描述经实测收窄"（同该行 2026-09-06 改判承接载体的先例） |
| (b) | 照派工原文改 `_reserve_ids`：预留后未落盘的号**回退高水位线**、允许复用 | 🔴 **与协议〇.8「编号永不复用」正面冲突**；且高水位线回退在并发下极危险（另一条线可能已基于新水位线取号）⇒ 起草方明确不推荐 |
| (c) | 折中：不回退高水位线，但在 release 时**报告**本次预留了 N 个、实际落盘 M 个、烧掉 N−M 个 | 纯增可观测性、不改行为；代价 ＝ 又一段"打印出来但没有承接载体"的自陈（`#503` 刚刚点名的形态） |

〔荐〕**(a)**。
✅ **已答 (a)**（同上）。**不动** `_reserve_ids`／`reserved_map`；交叉说明补进 `openspec/specs/editlock-queue-number-reservation/spec.md` Purpose（本次 apply 一并落）。派工描述经实测收窄一事，随 `#549` 收工回写 `#487`／`#531` 行。

## 与其它在途变更的触碰区核对

- **`#503`（同泳道 A3，变更包 `single-guarded-copy`）**：同触 `工具-opener生成.py`。A3 加 `--out`／`--anchor`／指纹，本包只改**报错文案与文档字符串**（决策点 ①(c) 若中选才会加读取逻辑）⇒ **可并存**，但两包若同期 apply 须同一分支或明确先后（本泳道已串行执行，A3 在前）。
  ✅ **先后已定（2026-09-10 `OP-0910-R` 实查，tasks 0.3）**：`single-guarded-copy` **尚未 apply**（其 `tasks.md` 首行仍写「design 审通过前一条都不得动手」，design 三个决策点未回填；手段＝`ls openspec/changes/` ＋ 读该包 tasks 首节）⇒ **本包先行**（随 `#549` 泳道 apply，分支 `claude/op0910r-549-550-opener-infra`），`single-guarded-copy` 后 apply 时须 rebase 到本包之后，其 0.2 记的「形态编号 ⑧」已被占（`#487` 追记⑴），**⑨ 亦已被 `#550` 占**，它须另取。同一结论已写进该包 `tasks.md` 0.x。
- `orchestration-guards`（P7① 撞号查重的来源包）——本包不改其行为，只补射程自陈 ⇒ 可并存。**其当前状态本包未核**（proposal Impact 已如实标注）。
- `#480` 先例（派工描述援引）——**本包未核其具体做法**，决策点 ③ 若选 (b) 须先核。

## 实现细节（非决策点，但实现必须照做）

1. **射程自陈只写一份**：写在 `_scan_used_suffixes` 的文档字符串里，`_check_op_id_not_reused` 的报错文案**指向它**而不是重述（指向而非复制，同 `工具-泳道纪律复制lint.py` 口径）。
2. **纪律正文落 `opener骨架.md`「取号」节**（它已是取号规则的所在地），模板库 §〇 只留判据一句、不复制正文。
3. 若决策点 ①(c) 中选：落盘失败即不出件（fail-closed，与生成器现有「缺字段直接报错退出、不出半成品」一致）。

## 本包不解决、也不假装解决的

- **真空期撞号**：⑵ 明确接受它（proposal 残余风险 1）。
- **「未派出即作废」无机器守**：没有任何机制能发现一个会话把号留着不用（残余风险 2）。
- **`#487` 主项**（`set_session_title` 例外句）：复核结论另见同目录 `#487主项复核-例外句源头不放.md`。
