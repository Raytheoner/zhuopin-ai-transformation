# 任务 · 无答交起算点按规则 1 取值（队列 #401）

> 业务口径无决策点（规则 1 全文 ＋「20 日 ＝ 自然月 20 号」已由姚祖怡书面签认，
> 排期由 Shao Peishen 2026-08-25 拍板 (a)）。design **D1 一项无默认**（「三个月内」的边界
> 开闭），已按保守侧落定并登记待他以判例确认，**不阻塞本包**——它只影响边界当天那批行，
> 两种读法下都不会低估。

## 0. 前置

- [x] 0.1 判据签认状态复核：规则 1 全文（采购部#15 回件）＋「20 日 ＝ 自然月 20 号」2026-08-18 确认
- [x] 0.2 §四 #111 已拍板 (a) 并销号，建造承接 ＝ §一 #401
- [x] 0.3 独立 worktree ＋ 新分支，基于 `master`

## 1. 引擎层（TDD：先测后码）

- [x] 1.1 `forecast._shift_months`：自然月加减，目标月无该日时收敛月末；单测覆盖 8-31+3 ⇒ 11-30、11-30+3 ⇒ 2-28、闰年 2-29
- [x] 1.2 `forecast.ship_within_horizon`：三个月窗口判据（含边界当天，design D1）
- [x] 1.3 `forecast.no_feedback_start_date`：规则 1／规则 2 分支唯一决策点
- [x] 1.4 单测 · 他的原例逐字复现：出货 12-05 ⇒ 起算 09-20
- [x] 1.5 单测 · 反例「20 日是自然月 20 号、不是相对偏移」：同月内出货日 1/5/19/20/21/31 起算点必须**完全相同**
- [x] 1.6 单测 · 跨年回绕：出货 1/2/3 月 ⇒ 起算落到**上一年** 10/11/12 月（`month-3` 直算会得到 -2/-1/0 月）
- [x] 1.7 单测 · 月末：锚月的选取与出货日的「日」无关（3-31 与 3-01 同锚月）
- [x] 1.8 单测 · 起算点可早于今天且**不钳制**（design D3；加 `max(start, today)` 即红）
- [x] 1.9 `estimate_material_arrivals` 新增 `heuristic_base_date` 可选入参；缺省 ⇒ 逐字节沿用 `demand_date`

## 2. 分支互斥（规则 1 / 2 / 3）

- [x] 2.1 单测 · 边界当天走规则 2、次日走规则 1（互斥锚点，差一天翻面）
- [x] 2.2 单测 · 出货日已过期／等于今天 ⇒ 恒判「在三个月内」，起算＝今天（那 38 行不动）
- [x] 2.3 单测 · 未来但在三个月内 ⇒ **仍取出货日**（规则 2 部分覆盖原样保留，design D2 的显式契约）
- [x] 2.4 单测 · 规则 3 仍成立：无答交腿前移后，齐料日仍取「有答交 / 无答交」中更晚的那个，瓶颈物料随之易主
- [x] 2.5 单测 · 方向性：规则 1 只前移不后移，差值 ＝ 出货日与起算点之差

## 3. 开关与审计

- [x] 3.1 `config.kit_date_rule1_enabled()`，默认 OFF（同 `net_inventory_enabled` 纪律）
- [x] 3.2 单测 · 环境变量缺省时必须 OFF（用 `delenv`，不读机器当下环境）
- [x] 3.3 `config.active_param_version()`：ON 时版本串自带 `+rule1`；单测两侧
- [x] 3.4 `baoguan.assess_supply_risk` 唯一调用点：开关 OFF 传 `None`、ON 传 `no_feedback_start_date(...)`
- [x] 3.5 单测 · `assess_supply_risk` 两侧结论（OFF ＝ 改造前口径；ON ＝ 规则 1 结论）

## 4. 回归

- [x] 4.1 SC8 全量：**502 passed / 3 skipped**（master 基线 479 + 新增 23，零回归）
- [x] 4.2 **把开关强制 `on` 再跑一遍全量：502 passed，与 OFF 完全相同** ⇒ mock 语料对规则 1 无端到端覆盖力（design D4 已如实写明，不得读作「已验证」）
- [x] 4.3 mock 黄金基准 `tests/test_golden.py` 零漂移
- [x] 4.4 真实黄金基准 `tests/test_golden_real.py`（`data/golden/real_frozen` 冻结件，gitignore 件，从主工作区取用）零漂移
- [x] 4.5 平台底座全量：380 passed / 1 skipped，零回归

## 5. 前后对照表

- [x] 5.1 `scripts/compare_kit_date_rule1.py`：同一份输入跑两遍（开关 off/on），出四色计数 ＋ 逐行变动 ＋ 分支适用面 ＋ **三条不变式自检**
- [x] 5.2 `--cache` 冻结格式与 `compare_kit_date_cumulative.py` **完全一致**，两脚本可互换复用同一份冻结输入
- [x] 5.3 离线冒烟：合成 4 行（已过期／三月内／三月外／跨年）跑通全流程，三条不变式自检全绿
- [ ] 5.4 🔴 **LAN 留步** —— 用生产载荷（`.51` 的 `C:\baoguan\.env` 口径，`SC8_NET_INVENTORY=on`）跑真实对照表并落 tracked `docs/`
- [ ] 5.5 🔴 **LAN 留步** —— 姚祖怡原始 43 行逐行核验（现行晚 89~92 天这个实测值在新代码下应归零）

## 6. 部署（🔴 全部 LAN 留步）

- [ ] 6.1 🔴 **LAN 留步** —— 部署 `.51:8091` ＋ 冒烟（`/api/ping`、关键页 200、一次全量重算）＋ 验进程 `CreationDate` 真刷新
- [ ] 6.2 🔴 **LAN 留步** —— 翻开关 `SC8_KIT_DATE_RULE1=on` 写入 `C:\baoguan\.env`。**硬前置 ＝ 5.4 对照表出件 ＋ 姚祖怡复核签认**（随采购部#19，§一 #402）；**未签认前不得翻**

## 7. 收口

- [x] 7.1 场景 CLAUDE.md 更新（§6 那条「规则 1 实测未覆盖」改写为已建造待验证）
- [x] 7.2 队列 §一 #401 回写（明写哪几步 LAN 留步）＋ §二 批次登记
- [ ] 7.3 openspec 归档 —— **待 6.2 完成后**（判据真正生效才算交付，非「代码合入即完」）
