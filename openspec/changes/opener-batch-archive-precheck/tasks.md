# opener-batch-archive-precheck Tasks

> 🔴 **本包状态：propose ＋ design 已出件，待 Shao Peishen 审。下列 0.x 之后的任务一律不得开工。**
> 出件 2026-08-25（队列 #397，#395 已并入）。**暂不归档**——本包尚未 apply，不适用完工即归档纪律。

## 0. design 审前置（🔴 阻塞全部后续任务）

- [ ] 0.1 Shao Peishen 审 **决策点 1**：四态判定表（C 态"归档命中即完成、不读状态列"；D 态 fail-open）。**无默认项。**
- [ ] 0.2 Shao Peishen 审 **决策点 2**：锚点沿用 opener 标题、`§四 #N` 排除、多行号取合取。**无默认项。**
- [ ] 0.3 Shao Peishen 审 **决策点 3**：挂点位置与共用 helper；跳过不得流入哨兵判定。**无默认项。**
- [ ] 0.4 Shao Peishen 审 **决策点 6**：派单件 status 义务 (a)+(b)，(b) 另立一行紧跟 #396。**无默认项。**
- [ ] 0.5 决策点 4／5 按建议 (a) 执行（不答即按建议——二者均不改变对外语义，符合默认项两个前提）。

## 1. 单测先行（真实行号回放，不用构造数据）

- [ ] 1.1 A 态：`#397`（live 机制环境 · `[S:open]`）⇒ 派出。
- [ ] 1.2 B 态：`#395`（live 机制环境 · `[S:done]`，已并入 #397 但从未进归档）⇒ 跳过，理由 `live-done`。
- [ ] 1.3 C 态：`#368`（`归档-202608.md` 第 583 行）⇒ 跳过，理由 `archived`。
- [ ] 1.4 🔴 **C 态防回归（本包最关键的一条）**：断言 `#368` 判定**不依赖 `cells[5]`**——该格实际内容为 `[S:open][D:业] 🆕 未开工`；若实现改为读状态字段，本用例**必须失败**。
- [ ] 1.5 D 态：不存在的行号（如 `#9999`）⇒ **派出** ＋ 告警 `unresolved`（断言不是跳过）。
- [ ] 1.6 D 态：无行号标题（A2 的 `协议〇.8／§四 #44`）⇒ 派出 ＋ 告警，且文案与 1.5 **可区分**。
- [ ] 1.7 锚点排除：A22 标题 `#353／§四 #108(a)` ⇒ 只抽出 `353`，`108` 不得进入查询。
- [ ] 1.8 合取语义：`#397／#396` 中仅一行 done ⇒ **派出**；两行皆 done ⇒ 跳过。
- [ ] 1.9 假阳性防护：断言 `#368` 命中的是第 583 行（首格 `368`），而非 533/535/902/904/906/912/1028 那 7 处正文提及。
- [ ] 1.10 章节归属：archive 中 `§四 #88` 存在 ⇒ 查 §一 `#88` 不得命中它。
- [ ] 1.11 多 §一 表：`归档-202608.md` 内 4 个 §一 表全部被扫到（逐份解析后合并，**不拼接文本**——队列 #312 反例锁死）。
- [ ] 1.12 🔴 哨兵短路：被跳过的 opener **不得**被判为 `NO-SENTINEL`；v1 不 `break`、v2 不停泳道。
- [ ] 1.13 `-Force` 绕过校验，且日志留痕 `[Force]`。

## 2. 实现：`工具-队列查询.py`（决策点 5）

- [ ] 2.1 新增 `--include-archive`（默认关）与 `--format json`（默认 text）。
- [ ] 2.2 归档件发现：glob `1-转型规划/0-全景路线图/跨桌任务队列-归档-*.md`（契约写进 spec）。
- [ ] 2.3 归档侧章节匹配 `^#{2,3}\s*§?\s*一[、\s·]`（兼容 live 与 archive 两种写法、H2/H3、单文件多表）。
- [ ] 2.4 JSON 契约按 design 决策点 5：`carrier=archive ⇒ done=true`，**不看 `status_field`**。
- [ ] 2.5 回归：不传新开关时，输出与退出码与现状**逐字一致**（含"未找到"仍 exit 0）。

## 3. 实现：`工具-opener派出前校验.ps1`（新增，决策点 3）

- [ ] 3.1 `Set-OpenerDispatchDecision -Openers -RepoRoot [-Force]`，补 `Skip`/`SkipReason` 两属性。
- [ ] 3.2 锚点抽取：先剥 `§四 #N(x)` 与 `§二 …`，再取 `(?:队列|§一)\s*#(\d+)`。
- [ ] 3.3 调 Python 只读 JSON，**不解析中文文案、不依赖退出码**（取证四：未找到也返回 0）。
- [ ] 3.4 Python 调用失败／JSON 解析失败 ⇒ **fail-open**（派出 ＋ 告警），绝不 fail-closed。

## 4. 挂接 v1（`工具-opener批处理执行.ps1`）

- [ ] 4.1 dot-source helper；在 **L76 之后**（`-Only` 过滤后）调用一次。
- [ ] 4.2 `foreach`（L103）内 `Skip` ⇒ `continue`，记 `SKIPPED`，**在状态计算之前短路**。
- [ ] 4.3 汇总表与收尾提示加 `SKIPPED` 行（含行号与理由）。
- [ ] 4.4 `-DryRun`（L81）显示跳过标记与理由。
- [ ] 4.5 **#395 并入项**：第 130 行提示改为 `-Command` 形态的完整可粘贴命令；改完用一次空过滤触发该提示、确认打印出的命令**直接可粘贴可跑**。

## 5. 挂接 v2（`工具-opener批处理执行v2.ps1`）

- [ ] 5.1 dot-source helper；在 **L60 之后、L62 泳道分组之前**调用一次。
- [ ] 5.2 分组时滤除 `Skip` 成员；**整泳道被跳空 ⇒ 不 `Start-Job`**，不占并发额、不等 `-StaggerSec`。
- [ ] 5.3 泳道清单打印（L66–69）反映真实将执行的成员。
- [ ] 5.4 `SKIPPED` 进汇总表与 `summary.txt`；退出码不受跳过影响。

## 6. 端到端回放验收

- [ ] 6.1 用 `本周计划-2026-08-24.md` 原文回放：**必须复现 A4 被跳过**，其余 13 项一个不少照常派出。
- [ ] 6.2 用 `建造波次-2026-08-25-泳道版.md` 回放：五泳道成员与跳过标记正确。
- [ ] 6.3 v1 与 v2 各跑一次 `-DryRun`，跳过标记与原因**两处一致**。
- [ ] 6.4 价值指标基线填真值：从 run `20260824-165657` 的 A4 日志读出实际耗时（**不估算**）。

## 7. 收口

- [ ] 7.1 全量测试绿、零回归。
- [ ] 7.2 队列 #397／#395 回写；#396 不动（另一行的活）。
- [ ] 7.3 `/opsx:archive opener-batch-archive-precheck -y`（完工即归档，不拖到下次 session）。

## 8. 不在本包范围（如实登记，非遗漏）

- [ ] 8.1 **#396 治标**（9 份派单件逐份核实改对）——#396 自己的活。
- [ ] 8.2 **sweep 派单件 status 常驻告警**（决策点 6 的 (b)）——建议另立一行紧跟 #396，依赖其 9 份样本标定口径。
- [ ] 8.3 **归档 `#368` 状态列与任务列自相矛盾**（design 取证二附带发现）——建议登记 §四 提请值周清扫在迁移时同步状态列。本包判据刻意不依赖它，修不修不影响正确性。
