## Why

### 先纠正派单件 §一 的一处立论（本变更包开工取证推翻，判据不变、理由变）

派单件 §一 断言「**这不是人忘了归档，是判据死锁 ⇒ 做不了**」。**「做不了」这半句实测不成立。**

`openspec/changes/archive/` 下 **50 个已归档包，其中 39 个在归档时是 N/N**（另 11 个带未勾项归档，最早 2026-06-06）。做法是**先把那条 archive task 自己勾上，再跑 archive** —— 最近三例（`sweep-resident-service-judge` 2026-08-22、`editlock-chokepoint-six-fixes` 2026-08-23、`sc2-weekly-report-mvp` 2026-08-21）全是这么做的。⇒ **归档一直在发生，人守规则被满足过至少 39 次；`sweep-ops-webhook-cutover` 也不是「结构上永远无法归档」，它就是还没做完。**

### 那么真正的缺陷是什么

**死锁是真的，但它锁死的不是归档动作，而是那个机器读得到的完工信号。**

`openspec list` 的 `N/M` 对两类包给出**完全相同的外观**：

| 包 | 外观 | 真相 |
|---|---|---|
| `sweep-ops-webhook-cutover` | 30/31 | 差的那 1 条**就是它自己的 archive 步骤** ⇒ 实质完工 |
| `queue-status-machine-field` | 47/48 | 差的那 1 条是 `8.3 真实主工作区验证` ⇒ 真没做完 |

**两者在 `N/M` 上不可区分，而它们该被怎么处理是相反的。** 后果已经现实发生，且记在队列 §四 #87 里：sweep 的「疑似遗忘归档」告警**分不出这两类**，于是 2026-08-23 那轮对三个守纪律的包误报；而误报的代价不是噪音，是**它会训练人忽略这条告警** —— 那条告警存在的全部意义正是在真的漏了归档时把人拦住。

同一天的另一面：`sc2-weekly-report-mvp` 的 delta spec 曾长期进不了 `openspec/specs/`（`sc2-*` 由 0 命中变 5 命中才修好），而它已部署 `.51:8096` 并让业务部门在用 —— **一个已上线的场景 spec 层零落地，是 IATF 可追溯性会直接挂掉的形态。** 这一例已修，但**产生它的那个盲区没修**。

### 🔴 第二次取证：判据在生产真身上首跑，3 命中 3 个都不该动（2026-08-23 14:xx 本地）

判定器对 17 个在途包跑 `--dry-run`，判出 3 个「实质完工」。**逐个打开看那条未勾的 archive 行写了什么：**

| 包 | 进度 | 那条 archive 行上作者写了什么 |
|---|---|---|
| `editlock-hold-scope-and-wip-block` | 33/34 | 「🔴 **本次不做，前置条件未满足，如实登记不假装完工**……**而不是直接归档**」 |
| `sweep-startup-nonblocking` | 25/26 | 「**本次不执行，前置条件未满足，如实留置**。未闭合项：……」 |
| `sweep-ops-webhook-cutover` | 30/31 | 子项：「🔴 **本轮不做，前置条件确实不满足**：3.3 与 5.2 的人眼半边均未闭合」 |

⇒ **判据每一次命中，命中的都是一个作者明确写了「不要归档」的包。** 连派单件称为「死锁纯样本」的 `sweep-ops-webhook-cutover` 也不例外（其 5.2 已于 08-23 闭合，但 3.3 的解除条件至今没有任何一半；且 3.3 那条虽打 `[x]`，正文写的是「本项未经真实验证……不得写成 ✅，不得计入已验收项」）。

**判据为什么看不见这些字**：它问的是「这行里有没有 `archive` 关键词」，而「本次不做」写在同一行的后半截或下一行的子项里。队列 §四 #87 早已说穿这个形态（「都把理由写了……只是没写在机器认得的地方」），并**明确否掉了靠认自然语言补救**（「模糊匹配会让降噪变成默认」）。

**⇒ 已据此加一道形态判别 `carries_human_note()`：不判断那些字是什么意思，只判断「这里有没有人留了话」** —— 光秃的 archive 行（最近三次真实归档全是这个形态）可放行；带说明文字或带缩进子项的一律不放行、交人看一眼。按今天的真身跑，**3 个命中全部落到「有人留了话」这一边 ⇒ 自动归档集合为空**，与实际正确处置一致。

### ✅ 范围已定：(b) 只修告警判据（Shao Peishen 2026-08-23 拍板）

**不做自动归档。** 同一个判定器只用来给 sweep 的「疑似遗忘归档」告警**分类**，归档动作仍由人做。
理由（权衡件全文＝`1-转型规划/0-全景路线图/定夺件-【CC】自动归档范围-a自动归档vs-b只修告警-2026-08-23.md`）：

- 收益侧缩水（从「解锁做不了的事」→「省 50 次人已经做对的往返」），代价侧没变（不可逆目录移动）；
- 真实痛点在告警不在归档 —— #87 记的是 **4 报 3 误**，而「误报的代价不是噪音，是它会训练人忽略这条告警」；
- 判据实测 **0/3**，人 **39/50 且没出过错**。**「机制比人靠谱」的前提是机制判得比人准，这条在本例上暂不成立。**

⇒ **本变更的内核是**：把「未勾的那几条到底是不是只剩 archive 这个动作本身」这个判断从人脑里搬到代码里 —— 它此前被执行过 50 次，每次都靠人打开 `tasks.md` 逐行读。判据不变（派单件 §三.1 给的判据是对的），变的只是为什么值得做：**不是「破一个解不开的死锁」，是「把一个做了 50 次、且机器分不出真假的手工判断机制化」。**

### 本次退休哪一个既有守卫（协议〇.9 措施 B，one-in-one-out）

**退休：`STALE_CHANGE_COMPLETION_THRESHOLD = 0.80` 作为「该归档了」的判据这一用法。**

那个 0.80 是个**代理判据** —— 用「完成率够高」猜「大概是忘了归档」。它猜错的方式已被实测坐实：
08-23 那轮 4 报 3 误，因为「完成率 97%」既可能是「只差归档」也可能是「还差一条真活」。
本变更用精确判据（未勾项是否全部为 archive 动作本身）取代它做这个判断；0.80 **降级为纯噪声闸**
（只管「这包是不是很久没动了」），不再承担归档判断。

⚠️ **不退休「完工即归档纪律」的人守条款** —— 实测它被遵守了 39 次，是有效的，且 (b) 方案下归档
仍然由人做。**上一版 proposal 曾提议退休它，那是建立在「归档做不了」这个已被推翻的前提上的，撤回。**

**守卫净增 = 0**：告警仍是一条，只是它说的话从「疑似遗忘归档」（对 3/4 的对象不成立）变为三类
各自准确的措辞。**不新增第二套主工作区判定** —— 判定器的 `assert_main_workspace` 复用
`工具-落库sweep.py` 已有的 `MAIN_WORKSPACE` 与 `_assert_not_a_linked_worktree`。

### 伴生文件的 .gitignore 覆盖（队列 #328②）

**本变更不新增任何此前不存在的文件名形态。** 判定与归档过程的日志**并入落库 sweep 已有的日志流**（`log: list[str]` 收集 → sweep 既有落盘路径），不新建独立日志/状态/快照文件；`--dry-run` 只打印到 stdout。

核实方式（不是推断）：`git check-ignore -v reports/<任意文件>` 实测命中 `.gitignore:28:**/reports/`，即便日后确需在 `reports/` 下落状态文件也已被覆盖。归档本身产生的目录移动（`openspec/changes/<name>/` → `openspec/changes/archive/<date>-<name>/`）是 openspec CLI 的既有行为，**是被跟踪的正常产物、不属于自动生成的忽略对象**。

## What Changes

- **新增** `0-学习与工具/工具-变更包自动归档.py`：把 `openspec/changes/*/tasks.md` 判为四态之一
  —— `substantively-complete`（未勾项全部是 archive 动作本身）／`incomplete`（尚有真未完项）／
  `complete`／`no-tasks`；并对每个未勾项做形态判别 `carries_human_note()`（**这行或其缩进子项上
  有没有人留了话**）。自带 `--dry-run` 清单。
- **🔴 不新增归档动作。** 本工具**不会**调用 `openspec archive`、不会勾任何 task、不会移动任何目录。
- **修改** `0-学习与工具/工具-落库sweep.py` 的 `_announce_stale_in_flight_changes`：命中滞留的包
  按上述四态**分三类各自措辞**，不再一律称「疑似遗忘归档」：
  1. **实质完工 ＋ 无人留话** ⇒ 「只差归档这一步」——**这才是真的遗忘归档**，照旧升级告警；
  2. **实质完工 ＋ 有人留话** ⇒ 「作者已写明理由，但未用机器认得的入口」，**并在告警正文里逐条
     列出那三个入口**（`暂不归档` ／ `预期观察窗口：N 天` ／ `--ack-stale-change`）；
  3. **尚有真未完项** ⇒ 「已 X 天无改动，尚有 N 条真未完项」——**不叫遗忘归档**（它没完工，
     谈不上忘了归档）。
- **修改** `1-转型规划/0-全景路线图/专线opener模板库.md` §〇 收工段：写入那三条降噪入口
  （队列 §四 #87 ⑶；实测三个词目前在任何一份收工纪律里 **0 命中**），＋ 根 `CLAUDE.md` §5 一行指针。
- **🔴 不做**（原派单件 §四）：**不禁止把 archive 写进 tasks**。实测那条 archive 行正是作者写
  「为什么还不能归档」的载体（三个包都写在那儿），(b) 方案下判定器读得到它 —— **禁掉它等于拆掉
  一个正在起作用的载体**。派单件 §四 的前提（「把它写进 tasks 正是死锁的来源」）已被推翻。
- **不碰** `sc2-weekly-report-mvp`（`No tasks`）：判为 `no-tasks` 并**显式记名**，不静默略过。

## Capabilities

### New Capabilities

- `change-completion-classification`: 变更包完工形态的精确判定与告警分类 —— 四态判据、
  「有人留了话」的形态判别、三类告警各自的措辞与可执行出口、以及执行环境前置（只在主工作区判定）。

### Modified Capabilities

（无。`openspec/specs/` 下现无任何 capability 覆盖「疑似遗忘归档」告警——实测 `STALE_CHANGE`／
「在途变更包」／「疑似遗忘」在 specs 全库 0 命中，故本变更整体落为新 capability，不产生 delta。）

## 知识资产三问（强制，全景规划 §1.4 第 2 条）

1. **本流程哪些判断是人脑默会经验？**
   - **「这条未勾项是流程动作，还是真的没做完」** —— 目前完全靠读的人当场判断。本变更把它显性化为一条可执行判据：行内含 `archive`（大小写不敏感）**且**含 `/opsx:archive`／`openspec archive`／「归档」三者之一，**两条须同时满足，宁严勿宽**。
   - **阈值/尺度**：刻意**不**放宽到「未勾数 ≤1」。实测反例 `queue-status-machine-field` 47/48，那 1 条（`8.3 真实主工作区验证`）是真未完项 —— 按数量判会把它错归档。
   - **例外**：`No tasks` 的包（`sc2-weekly-report-mvp`）判不了完工，**显式跳过并记名**。
   - **「这条 archive 行上有没有人留了话」** —— 显性化为形态判别（`carries_human_note`），**只认「有没有字」，不认「那些字什么意思」**。这是刻意的：#87 已论证「模糊匹配会让降噪变成默认」。
   - **未显性化、且本次不打算显性化的**：「这个包现在到底该不该归档」—— (b) 方案下这个判断仍然留给人，机器只负责把三类分开、把可执行出口摆在他面前。

2. **由谁显性化？**
   - 持有人：**Shao Peishen**（判据口径拍板人；2026-08-23 拍板范围取 (b)）
   - backup：**孙涛**（决策代理，可代 design 审与 L2 日常审批；不可代合规红线变更）

3. **用什么方法提取？**
   - **历史案例反推**（主）：以 2026-08-23 实测的 17 个在途包为全量样本，逐包人工判定「实质完工 / 真未完 / 判不了」，再从判定结果反推判据文本，并用同一批样本做正反例回归（`sweep-ops-webhook-cutover` 必须命中；`queue-status-machine-field`、`open-pool-reminder-dual-file-and-staleness` 必须不命中）。
   - 判据条文回发 Shao Peishen 确认一行 —— 与判例批改法同构：**先给真实案例的判定对照，再要口径签认**，不做抽象设问。

## 验收与晋档条件（强制，四档口径）

- **本变更包交付后所处档位**：**档 3 内部服务** —— 由本机常驻的落库 sweep 每轮调用，作用于真实生产 `openspec/changes/`，非 mock、非对客。
- **晋下一档的条件**（档 3 → 档 4 对客交付）：**不适用，本变更包永不晋档 4** —— 仓库内部流程工具，无对客面。显式登记，免得下一轮误以为漏了。
- **验收判据（逐条，全部可机器复核）**：
  1. **判据正反例**：`sweep-ops-webhook-cutover`／`editlock-hold-scope-and-wip-block`／`sweep-startup-nonblocking` 三个包判为 `substantively-complete` **且** `unchecked_carry_notes=True`；`queue-status-machine-field`（47/48）与 `open-pool-reminder-dual-file-and-staleness`（21/23）判为 `incomplete`；`sc2-weekly-report-mvp` 判为 `no-tasks` 且**出现在清单里、不静默略过**。
  2. **08-23 那轮误报可复现且已消除**：用当日 4 个被报的包做端到端夹具，断言旧措辞「疑似遗忘归档」**对其中 0 个成立**，三类新措辞各自命中正确对象。
  3. **三条降噪入口出现在告警正文里** —— 断言「作者已写理由但未用机器入口」这一类的告警文案中，`暂不归档`／`预期观察窗口`／`--ack-stale-change` 三个词全部出现（**告警必须给出可执行出口，不能只说「你错了」**）。
  4. **非主工作区拒绝判定**：`assert_main_workspace` 对 linked worktree 抛 `RefuseToRun`，异常文案说明原因。
  5. **回归零漂移**：`0-学习与工具` 全量绿；`openspec validate --all --strict` 相对 master 基线不新增失败。
- **🔴 本变更包不含、也不得被理解为已含的**（(a) 方案的验收项，随范围定为 (b) 一并作废，显式登记免得下轮误以为漏做）：**首个真实归档**、**脏工作区跳过**、**归档失败路径告警**、**真建 linked worktree 的执行路径单测** —— 这四项全部依附于「自动执行归档」，(b) 不做归档，故不适用。
- **价值指标**（取风险型 + 工时型）：
  - **风险型（主）**：**「疑似遗忘归档」这个措辞的误用率**。基线 = 2026-08-23 那轮 **4 报 3 误**（#87）；目标 = 该措辞只落在「实质完工 ＋ 无人留话」这一类上，其余两类各有准确措辞 ⇒ 误用归零。
  - **工时型（次）**：每次告警省去的「人打开 tasks.md 逐行读、判断这条到底是不是只差归档」。基线 = 08-23 那轮 4 个包各一次，且**当轮人工判读的结论与本判据一致**（#87 的四路处置就是那次人工判读的产物）。
  - ⚠️ **不宣称的指标**：不宣称「省下归档工时」（(b) 不做归档），也不宣称「让本来归不了档的包能归档」（实测归档一直在发生）。**两条都不写进指标。**

## Impact

- **新增文件**：`0-学习与工具/工具-变更包自动归档.py` ＋ `0-学习与工具/test_工具-变更包自动归档.py`。
- **修改文件**：
  - `0-学习与工具/工具-落库sweep.py` —— 只改 `_announce_stale_in_flight_changes` 的**分类与措辞**；`_find_stale_in_flight_changes` 的命中集合不变（0.80／3 天两个闸原样保留，只是不再承担「该归档了」这个判断）。**不新增任何写操作、不新增调用点、收尾段不动。**
  - `1-转型规划/0-全景路线图/专线opener模板库.md` §〇 收工段 —— 写入三条降噪入口。
  - 根 `CLAUDE.md` §5 —— **一行指针**（该文件今日已由 71.7K 瘦到 56.0K，**不得展开**）。
- **不修改**：`openspec/templates/`（实测无 tasks 模板）；变更包 tasks 的写法约定（见 What Changes 末条，派单件 §四 撤销）。
- **受影响系统**：本机常驻落库 sweep 的告警正文。**`openspec/changes/` 目录结构不受影响** —— 本变更不移动任何目录。
- **红线核对**：
  - **mock 先行** —— 单测用真实原文做夹具、不触碰真实仓库；对生产真身只跑只读 `--dry-run`。
  - **audit 留痕** —— 告警正文逐条回显命中的包、判定结论与判定理由（#87 教训：只印一句写死的话、不说命中了什么，判据就无法被现场证伪）。
  - **OEM 隔离 / L2 门禁 / ISO 26262** —— 均不适用：只读本仓库规格文档并改一条告警的措辞，不触碰 OEM 技术数据、不做业务决策、不生成安全相关代码。
  - **不可逆性** —— **本变更不含任何不可逆操作**。这正是范围定为 (b) 的理由。
