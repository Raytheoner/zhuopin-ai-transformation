# queue-lint-restate-package-progress Tasks

> 队列 §一 `#456`。本包由 Cowork `OP-0902-A` 泳道 A（无人值守无头 CC）起草。
> 🔴 **本 session 只做起草（D1 🟢 `openspec_draft`），未改任何生产代码，
> `工具-队列结构lint.py` 一行未动。** §2 起全部未开工。

## 0. 待定夺（本包不得归档，也不得 apply）

**动手的都不是本 session（无人在场的无头批处理，收工即结束）⇒ 按根 `CLAUDE.md` §5
「答完之后由谁动手」判据，一律不给默认值、只登记待总线派发。**

- [ ] 0.1 🔴 **裁定 proposal「开放点 1」的四选项**——本包已实测证明**紧凑形态判不出来**
      （窗口 0/8/20/40/80/160 字 ⇒ 命中 0/1/5/9/17/28 行，无拐点；两条真实致错行
      `#452`＝28 字、`#439`＝664 字，同一窗口不可能兼顾）。四选项与各自代价见 proposal，
      本包倾向**选项 1（形态 C ＋ baseline 棘轮，存量 25 行）**，但不替 Shao Peishen 选。
- [ ] 0.2 **追认「一期只告警、不计退出码」**（与 `#366`／S2 一期同构）。若要求一期即计退出码，
      则 0.1 必须先选出一个高精度形态——而本包实测认为它不存在。
- [ ] 0.3 **追认扫描面 ＝ §一 非 `[S:done]` 行 ＋ §四**，§二 批次行不入。
      与既有 `FOLLOWUP_RESTATE_SCOPE_SECTIONS` 同一口径，不为本判据另开解析。
- [ ] 0.4 **追认逃生阀写法 `包进度豁免：〈理由〉`**（沿用 `称呼豁免：`／`WIP豁免：` 已验证范式，
      理由只认行内标记、不认命令行开关）。第一个真实用例＝队列 `#456` 行自己（自指）。
- [ ] 0.5 **追认覆盖率缺口如实登记**：本判据覆盖不了 `#439` 那一类「整段散文陈述过期状态」，
      交付后该形态覆盖率仍为 0%。🔴 **本项是五项里唯一承认交付后仍有漏洞的**——不得以
      「已上判据」冒充已解决。
- [ ] 0.6 **裁定选项 4（写侧提示）的去向**：它触碰 `工具-共享文档编辑锁.py`，
      看护件 `OP-0902-A` §二 已明写**锁工具族恒最后一波、本批全部不入**
      （`#455`／`#341`／`#454`／`#448` 均触碰同一文件）⇒ 本包不实现，仅登记待那一波。

> ⚠️ **本包尚无 `design.md`。** 队列 `#456` 行内明写「命中 openspec 触发门槛 ①改变全项目口径
> ⇒ **须走 openspec 含 design 审，不得直接改码**」。design 审在泳道看护 D1 判据里是 🟡 档
> （`openspec_design_review`），本 session 为无人值守 ⇒ **如实留步、不代办**。

## 1. 前置取证（已完成，本 session 实跑）

- [x] 1.1 全量扫描两份生产队列：**§一 活行 ＋ §四 共 53 行提到 openspec 包名，共 154 处包名**；
      §一 `[S:done]` 历史行另有 5 行（含 §二 时为 107 行）。
- [x] 1.2 **窗口标定**（「包名 ±W 字内出现进度词」，进度词表撒到 25 个候选）：
      ±0/±8/±20/±40/±80/±160 ⇒ 命中 **0/1/5/9/17/28** 行（0%/2%/9%/17%/32%/53%）。
      **无拐点，是平滑斜坡** —— 与信状态判据 8/8 字「恰好命中且仅命中 5 处」形成对照。
- [x] 1.3 🔴 **两条真实致错行的最小距离实测**：`#452` ＝ **28 字**；`#439` ＝ **664 字**。
      队列行长中位 **5187 字**、最大 **38342 字** ⇒ 664 字窗口等同于「同一行内共现」＝无约束。
      **⇒ 兼顾二者且保精度的窗口不存在**，这是「不硬上」结论的实测依据。
- [x] 1.4 **退路形态噪音量实测**：形态 A（含归档包）**53 行**／形态 B（仅活跃包）**50 行**／
      形态 C（活跃包且本行未写 `tasks.md` 指针）**25 行**。53 行里已有 **26 行**自写指针。
- [x] 1.5 **对照基准实跑**：现有 `_followup_restate_scan` 一期实测 **5 处活行**
      （`python 0-学习与工具/工具-队列结构lint.py` 当日输出）⇒ 形态 A 是它的 **10 倍**噪音。
- [x] 1.6 **反向核实「有没有可退休的既有守卫」**：现有五项判据（列数／§二 状态列／权威模块可
      import／称呼判据／信状态复述）**无一能被本判据覆盖**；最近的信状态复述那条是同原则、
      不同对象，合并会把一条精度 100% 的判据拖下水。结论＝本包退休的是**人守规则**，非守卫代码。
- [x] 1.7 `git check-ignore -v 0-学习与工具/队列包进度判据-baseline.json` 实跑：
      **无输出、退出码 1（未被任何规则忽略）**，符合预期（若走 baseline 路，它必须入库）。
- [x] 1.8 并入审核（协议〇.10）已于取号前完成，见 `#456` 行内：`#366`（`[S:blocked]`，并入会
      把本项冻住）／`#455`（同为写侧校验但改编辑锁、触碰区不重叠可并行）／`#454`／`#352`
      （已 `[S:done]`）——**无一可并，故新建**。

## 2. design（未开工，🟡 档，须先过 0.1-0.6）

- [ ] 2.1 起草 `design.md`，把 0.1 的裁定结果、四选项对照表、以及取证 1.2/1.3/1.4 的实测数
      落成决策点。
- [ ] 2.2 design 审通过后方可进入 §3。**未过 design 审不得 apply**（`#456` 行内硬约束）。

## 3. 判据实现（未开工）

- [ ] 3.1 `_package_progress_scan`：包名词表实时枚举（长→短匹配）＋ `tasks.md` 指针排除
      ＋ 两类豁免分类计数。
- [ ] 3.2 词表不可达时 fail-loud（不回退成空词表）。
- [ ] 3.3 baseline 棘轮 ＋ `--emit-baseline`（只打 stdout，无一键重刷开关）——**仅当 0.1 选中
      带 baseline 的选项时才做**。
- [ ] 3.4 接入 `main()` 打印区，**不接入 `lint()` 返回值与退出码**（一期）。
- [ ] 3.5 打印覆盖率缺口声明（spec 最后一条 Requirement）。

## 4. 单测（未开工）

- [ ] 4.1 两向用例：点了包名无指针即报／已写指针不报。
- [ ] 4.2 扫描面用例：§二 不入、§一 `[S:done]` 豁免且计数。
- [ ] 4.3 词表用例：长→短匹配不被子串窃取（`fi2-recon-mvp` vs `fi2-recon-report`）；
      新建目录当天即覆盖；词表不可达 fail-loud。
- [ ] 4.4 baseline 用例：存量不报／再点一个新包即报／纯搬家不报／emit 不写盘／缺失 fail-loud。
- [ ] 4.5 豁免用例：行内标记豁免且计数、豁免行不入 baseline。
- [ ] 4.6 真实队列回归护栏：在真实队列上实跑，输出行数须与 tasks 1.4 的实测值一致
      （**不一致即追因，不得就地改判据去凑**）。
- [ ] 4.7 自指回归：本判据 MUST NOT 命中它自己写在源码注释里的说明段
      （同 `#355` 已踩过一次的坑）。

## 5. 收口（未开工）

- [ ] 5.1 `openspec validate queue-lint-restate-package-progress --strict` 保持 valid。
- [ ] 5.2 ff 合入 master（🟡 档 `merge_to_master`，须停下问）。
- [ ] 5.3 队列 `#456` 回写（由 Cowork 侧统一做，泳道 A 不写队列）。
- [ ] 5.4 **暂不归档**：§0 六项待定夺全未完成、§2-§5 全未开工 ⇒ 不得 `/opsx:archive`。
