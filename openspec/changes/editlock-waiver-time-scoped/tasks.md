# Tasks · editlock-waiver-time-scoped

## 0. 状态：出件即停（派单件 `OP-0907-AM` 步骤 1）

- [x] 0.1 命中 CLAUDE.md §5 openspec 门槛第③条判定完成——同一接口、同一输入，`release` 结果由**放行**变**拒绝**（方向与 `editlock-aibot-registration-completeness-exemption` 相反）
- [x] 0.2 现状取证（只读）：判据裸子串、取材面含触碰行整行、`#382` 行内真实残留、2026-09-07 16:21 sweep 21 个孤儿——见 design.md §一
- [x] 0.3 `.gitignore` 覆盖核实（`git check-ignore -v` 实测，非推断）——见 proposal.md
- [x] 0.4 propose ＋ design 出件，`openspec validate --strict` 绿
- [ ] 0.5 🟡 **待 Shao Peishen 一字母**（design.md §三 三个决策点）；未获答复前不动任何一行代码

## 1. 实现 · 编辑锁侧（D1–D3，`0-学习与工具/工具-共享文档编辑锁.py`）

- [ ] 1.1 **apply 前置**：`git log -1 -- 0-学习与工具/工具-共享文档编辑锁.py` 核 `#455`（Y-A5 编辑锁写侧）是否已 ff；未 ff 则 rebase 到其分支之上做
- [ ] 1.2 D1：`_registration_completeness_violations` 改为只接收本次 `acquire` note 作为豁免来源；`_auto_sync_followup_reply_state` 与 `_opener_guard_violations` 的 `waiver_sources` **原样不动**
- [ ] 1.3 D2：新增结构化解析（路径分隔符全角/半角/顿号，反引号可选，作者与到期可选）；有效路径＝工作树内存在 **或** 出现在本次脏文件集合中
- [ ] 1.4 D2：只放行点名件；拒绝文案写出「已点名放行 M 个、仍有 N 个未点名」
- [ ] 1.5 D2：到期已过即失效（年份按「早于今日 180 天以上则解为次年」判，跨年不误判）
- [ ] 1.6 D3：泛豁免拒绝，文案与「未写豁免」可区分，只给两条出路
- [ ] 1.7 fail-closed 分支：豁免仍适用，有效性只按「工作树内存在」判
- [ ] 1.8 删掉拒绝文案里「或本次触碰的队列行内写…」——留着一条已不生效的出路比没有出路更坏
- [ ] 1.9 在 `REGISTRATION_WAIVER_MARKER` 旁写清判据来源（时间维 vs 空间维）与 `#382` 那条实证，供下一个改这里的人看见

## 2. 单测 · 编辑锁侧（`0-学习与工具/test_工具-共享文档编辑锁.py::RegistrationCompletenessTests`）

- [ ] 2.1 泛豁免拒（`登记豁免：他线脏文件由其作者线自登` ＋ 未覆盖脏文件 ⇒ 仍拒绝）
- [ ] 2.2 点名豁免只放点名件（两个脏文件、点名一个 ⇒ 仍拒绝，且文案含「仍有 1 个未点名」）
- [ ] 2.3 点名全部 ⇒ 放行
- [ ] 2.4 🔴 **行内旧豁免不再生效**——用 `#382` 任务列那句**真实残留**原文做 fixture：把它放进一条本次触碰过的队列行，note 里不写豁免 ⇒ 仍拒绝
- [ ] 2.5 到期已过的豁免失效；到期未过的生效（含跨年用例：`12-31` 与 `01-02` 不互相误判）
- [ ] 2.6 反例：机器人身份 ＋ 未覆盖脏文件 ＋ **无任何豁免文本** ⇒ 仍放行（身份通路未被误伤）
- [ ] 2.7 反例：机器人身份 ＋ 取数失败 ＋ 无有效豁免 ⇒ 仍 fail-closed 拒绝
- [ ] 2.8 反例：`转态豁免：`／`opener豁免：` 写在本次触碰的队列行内 ⇒ 照旧放行（本次收窄未溢出到那两项）
- [ ] 2.9 🔴 **反向对照**：把脚本换回 master 版跑这一批，2.1／2.2／2.4／2.5 必须变红，2.6／2.7／2.8 必须保持绿——否则证明不了新增用例咬住的是本次改动

## 3. 实现 · sweep 侧（D4，`0-学习与工具/工具-落库sweep.py`）

- [ ] 3.1 **apply 前置**：核 `#479`（Y-A7 sweep-manifest）是否已 ff，同 1.1 处置
- [ ] 3.2 新增常量 `ORPHAN_SECTION_FOUR_HOURS = 6`（独立于 `ORPHAN_ALERT_THRESHOLD_HOURS = 3`）
- [ ] 3.3 新增**一个独立函数** `_escalate_long_lived_orphans_to_section_four`，自读 `reports/sweep-orphan-state.json`，**不改** `_track_and_alert_orphan_paths` 的签名与返回值
- [ ] 3.4 **一处调用**，放在两份队列文件的循环**之后**（循环内调用会同轮登两次）
- [ ] 3.5 当日去重：orphan-state 条目加字段 `section_four_logged_on`；跨天仍孤儿则再登
- [ ] 3.6 走既有 CLI：`acquire --reserve 1 --section 四` → `append-row --section 四` → `release`
- [ ] 3.7 §四 行内容：文件清单（≤10 条，超出写「…等 N 个」）＋ 首见时间与存续小时数 ＋ (a) 代登 / (b) 丢弃；等谁＝Shao Peishen；**不标默认项**
- [ ] 3.8 升格失败不改本轮退出码、只留一行日志；`--dry-run` 下只打印不写

## 4. 单测 · sweep 侧（`0-学习与工具/test_工具-落库sweep.py`）

- [ ] 4.1 跨阈值孤儿 ⇒ 产生 §四 一行，行内含文件、首见时间、两个选项
- [ ] 4.2 未跨阈值 ⇒ 不登
- [ ] 4.3 同日多轮 ⇒ 只一行；跨天仍孤儿 ⇒ 再一行
- [ ] 4.4 反例：升格不对孤儿文件做任何 `git add`／`commit`，git 状态不变
- [ ] 4.5 反例：升格失败（锁被占／取号失败）⇒ 本轮退出码不变
- [ ] 4.6 `--dry-run` 不写队列

## 5. 验收与收尾

- [ ] 5.1 全量 `python -m pytest 0-学习与工具/test_工具-共享文档编辑锁.py 0-学习与工具/test_工具-落库sweep.py`，贴 passed 数，零失败
- [ ] 5.2 **真 CLI 真 git 仓库受控复现**（不只跑单测）：实验组（点名豁免）放行、对照组（泛豁免）被拒，两组文案原样记进 tasks
- [ ] 5.3 push 分支，**不 ff master**（🟡，ff 由总线一字母）
- [ ] 5.4 `#416` 状态列追加收工段（K2 ≤4 KB，超即外置到 `1-转型规划/0-全景路线图/队列行日志/#416.md`）
- [ ] 5.5 §二 登批次（commit 由 `ZhuopinCommitSweep` 自动取活）
- [ ] 5.6 落地后回填**下一轮 sweep 的孤儿数**到 `#416`——这是唯一能证明「止血真的止住了」的生产信号
- [ ] 5.7 通知 Cowork：机器守已落地，可把 `.claude/rules/队列与落库.md` 的止血句降为一行指针
