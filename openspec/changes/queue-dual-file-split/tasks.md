> **本次交付范围＝ 0（design 审前置）。1-6 全部为 `/opsx:apply` 阶段任务，本轮未做，如实登记为待执行，不假装已完工。**
>
> **门槛（决策点 0）**：第 1 节「观察期门槛」全部勾选之前，不得开始第 2 节及以后的任何 apply 任务——即便 design.md 已获批，也不代表可以跳过观察期直接执行（proposal.md「Why」第 2 点已如实登记这一区分）。

## 0. design 审前置

- [ ] 0.1 Shao Peishen 审核 design.md 决策点 0（本设计产出时点与 apply 执行时点是否分离；是否接受"design 现在做、apply 等观察期结束"这一默认项，或明示跳过观察期的理由）。
- [ ] 0.2 Shao Peishen 审核决策点 1（两份文件命名与章节归属）。
- [ ] 0.3 Shao Peishen 审核决策点 2（维持单一编号空间——继承方案件既有结论，预期无异议）。
- [ ] 0.4 Shao Peishen 审核决策点 3（§二 域判定＝物理文件位置，不新增字段）。
- [ ] 0.5 Shao Peishen 审核决策点 4（权威路径解析收拢方案，含 sweep 遗留第 4 处一并处理）。
- [ ] 0.6 Shao Peishen 审核决策点 5（幽灵副本检测——绝对路径打印 + 影子副本漂移警告）。
- [ ] 0.7 Shao Peishen 审核决策点 6（一次性机械切分迁移策略与回滚点）。
- [ ] 0.8 Shao Peishen 审核决策点 7（四类消费者验证方案；共用锁/独立锁留 Open Questions，apply 阶段定）。

## 1. 观察期门槛（apply 前必须满足，非本轮范围）

- [ ] 1.1 确认已达 2026-08-23（域字段 2026-08-09 落地 + 14 天）或 Shao Peishen 明示跳过理由。
- [ ] 1.2 复核观察期内真实数据：WIP 计数准确性是否仍有争议、看板/值周巡检按域分列后的实际使用反馈、机制类行是否仍在淹没业务线的队列阅读体验。
- [ ] 1.3 基于 1.2 数据做出"执行阶段 3"或"阶段 3 可以不做、本变更包标记设计完成后归档"的判断，写入队列 #315 状态列。

## 2. 单测先行（决策点 4/5：路径解析与幽灵副本检测）

- [ ] 2.1 `queue_table.resolve_queue_path`/`iter_queue_paths` 单测（合法域、非法域 fail-loud、双路径遍历）。
- [ ] 2.2 编辑锁 `--domain` 路由单测（未声明拒绝、声明后正确路由到对应文件）。
- [ ] 2.3 影子副本漂移检测单测（内容不一致时告警、内容一致不误报、主工作区内不误报、本地无副本不误报）。
- [ ] 2.4 §二 域路由单测（`append-row --section 二` 未声明目标文件时拒绝）。

## 3. 实现：权威路径解析收拢（决策点 4）

- [ ] 3.1 `queue_table.py` 新增 `QUEUE_MECHANISM_PATH_REL`/`QUEUE_BUSINESS_PATH_REL`/`resolve_queue_path`/`iter_queue_paths`。
- [ ] 3.2 `工具-落库sweep.py::QUEUE_REL` 切换（收尾 #313 遗留第 4 处）。
- [ ] 3.3 `工具-共享文档编辑锁.py::DEFAULT_TARGET` 切换为按域解析。
- [ ] 3.4 `工具-队列查询.py::DEFAULT_TARGET` 切换，`--row` 查询跨两份文件查找 + 命中文件标注。
- [ ] 3.5 `工具-文档台账生成.py::QUEUE_PATH` 切换为遍历 `iter_queue_paths()`。
- [ ] 3.6 `工具-队列结构lint.py`（经既有 importlib 复用编辑锁常量，随 3.3 自动带出，核实无需独立改动）。
- [ ] 3.7 `wecom-aibot-service/aibot_service/repo_paths.py::DEFAULT_QUEUE_RELATIVE_PATH` 切换为双常量 + `queue_appender.py` 域路由（机器人来件默认写业务场景文件）。
- [ ] 3.8 给 Cowork 侧提供 `zhuopin-project-status` JS 读双文件的正则/伪代码片段（登记，不代为改该 artifact）。

## 4. 实现：幽灵副本检测（决策点 5）

- [ ] 4.1 `acquire`/`status` 输出新增绝对路径打印。
- [ ] 4.2 本地影子副本漂移检测（`os.path.samefile` + 内容比对，只读警告不自动处理）。

## 5. 一次性机械切分（决策点 6，观察期门槛通过后执行）

- [ ] 5.1 按 `[D:]` 字段一次性把 §一 现存数据行拆入两份新文件；协议〇/§三/§四/编号高水位线整体移入机制环境文件。
- [ ] 5.2 §二 现存数据行按当前所在批次的关联任务域拆入对应文件（人工复核，同 #308 决策点 3"一次性迁移用启发式+人工复核兜底"范式）。
- [ ] 5.3 旧文件 `跨桌任务队列.md` 改为指针文件，不删除。
- [ ] 5.4 业务场景文件顶部新增指向机制环境文件的协议〇/§三/§四/高水位线指针。
- [ ] 5.5 `git commit`，commit message 显著标注"此 commit 之后新增的行，回滚需人工核对"（决策点 6 已声明的不完全可逆点）。

## 6. 四类消费者真实场景验证（决策点 7，#315 期望产出③）

- [ ] 6.1 编辑锁 `acquire`/`release` 在两份文件上各真实跑一次完整流程。
- [ ] 6.2 `工具-落库sweep.py` 在两份文件各造一条真实 §二 批次并触发落库。
- [ ] 6.3 值周巡检生成一期真实《本周计划》，核实正确合并两份文件统计。
- [ ] 6.4 拆件巡逻触发一次真实企微来件升格，核实正确路由到业务场景文件。
- [ ] 6.5 基于 6.1-6.4 的真实观察，拍板"共用锁 vs 独立锁"（Open Questions 遗留项）。
- [ ] 6.6 `python 0-学习与工具/工具-仓库外载体扫描.py "跨桌任务队列"` 核实四类仓库外载体（值周巡检/拆件巡逻定时任务真身、Cowork artifacts、已安装 skill、`.51` 四服务页面）均已同步，无遗漏引用旧文件名的位置。
