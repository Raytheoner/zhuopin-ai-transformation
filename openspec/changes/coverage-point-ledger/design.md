---
status: 生效（Shao Peishen 2026-09-01 当场审定：新增 D7／D9，D1–D5 整体照准；本件 2026-09-02 补落包）
---

# design · 口径点台账

> 队列 §一 `#439`｜审材料正本 ＝ `1-转型规划/0-全景路线图/design审前置-口径点台账三开放点收敛-2026-09-01.md`（`OP-0901-D`，§9 审记录／§5 全套包预写／§6 十九条 `git check-ignore` 实测）
> **本件由 `OP-0902-A` 于 2026-09-02 补建**——design 审在 09-01 就通过了，包在 09-02 才落，中间丢了一次。**下节必须留在最前面。**

---

## 🔴 落包勘误（2026-09-02）

**2026-09-02 的落包只读了 08-30 草案，没读 09-01 的收敛件。** 后果三条，逐条实证：

1. **三个开放点被重新打开** —— `tasks.md` §1 把「拆点粒度判据／台账是否按域分文件／双路并跑期」列为「未收敛、design 审前全部不得开工」，而这三点 09-01 已由 Shao Peishen 全部拍完（分别 ⇒ D1、D2、D4）。
2. **台账格式被反向改判** —— proposal 把「保 `.jsonl` ＋ 加 `!` 例外」判为 ❌「**在一条写着「绝不入库」的规则上凿洞**」，改判 `.yaml`。**而「凿洞」正是他 09-01 拍的 (b)（D7），且已在 `/tmp` 独立 repo 实跑 11 条验证、含 4 条反向对照组证明未误放行任何运行时审计 JSONL。**
3. **`design.md` 未建** —— 实测方式：`for d in openspec/changes/*/; do ls "$d"; done`，34 个在途包里 `coverage-point-ledger` 是**唯一**没有 `design.md` 的一个；D1–D9 九条 Decisions 无一进包。

**根因（grep 实证，非推断）**：草案 `1-转型规划/0-全景路线图/openspec提案草案-口径点台账-2026-08-30.md` 对 `design审前置|OP-0901-D|2026-09-01` 三个模式**全库 0 命中** —— 它没有任何前向指针指向 09-01 的收敛件。唯一把两者连起来的载体是队列 `#439` 的状态列。**落包线读了草案、没读队列行。**

⇒ 同 `CLAUDE.md` 顶部 OP-0819-A ⑴「**一份拆成两份，下游只跟了一份**」，区别是本次下游跟的是**过期的那一份**。

🔑 **由此得到一条可复用判据（建议进 §5 规则退休制计数，暂不入 CLAUDE.md 正文）**：

> **凡「落档不落包」「先出前置件、后落包」的前置件，必须在被它更新的上游件里留一条前向指针。**
> 否则那份上游件就是一枚哑雷：**内容自洽、格式完整、任何人读它都不会报错，只是结论已经过期** —— 又一次「错误不产生任何信号」。

⚠️ **另记一处，性质更隐蔽**：proposal 里那段 `.yaml` 改判**自带「我已实测」的语气**（它确实跑了 `git check-ignore -v`，输出也真实），**但它推翻的那 11 条实测从未被它读到**。⇒ 同 OP-0819-F ⑶「**推翻一条既有实测记录前，须与原记录同口径复测一次**」——更正比原始错误更难被发现，正因为它带着复核的口吻。

---

## Decisions（九条）

| # | Decision | 理由 / 出处 |
|---|---|---|
| **D1** | **拆点判据采用 P1–P4**：P1 为主判别式（「专员只答这一条、完全不答其他条，这一条能否成立」）、P2 为比例校验（判例行是点的证据不是点本身，实测约 3:1）、P3 为回溯起点（`决策点:` 字段括号内以 `/` 分隔的每一项）、P4 为类型分流（判定规则/口径/阈值 ⇒ 永不默认生效；试用反馈、材料索取 ⇒ 可标默认；通报 ⇒ 不产生点） | 审材料 §2.3。**判据不必发明**——`决策点:` 字段已被实践 **18 次**，覆盖率 7 月 37% → 8/01-10 **70%** → 8/11-31 **0%**，最后一封＝`采购部-姚祖怡-跟进-2026-08-10-…`。⇒ **R1 的性质是「给一个已被验证可行、曾达 70%、因无载体而消亡的实践建载体」**，不是新建能力 |
| **D2** | **台账按域分文件**（`采购域`／`财务域`／`质量域`／`销售域`／`IT域` 五份平铺于 `6-人才与组织/部门AI专员跟进/口径点台账/`）；**ID 前缀隐含域，不设独立 `域` 字段** | 审材料 §3（Shao Peishen 2026-09-01 拍 (a)）。OEM 隔离红线的直接后果；**少一个字段就少一处可能与文件名不一致的副本** |
| **D3** | **跨域度量只读聚合，禁止任何合并落盘的产物（含缓存）** | 审材料 §3.2⑵。🔴 须落断言测试 |
| **D4** | **新旧双路并跑 ≥ 28 天（4 周）** | 审材料 §4.3。实测 35 封闭环信往返周期 P90 ＝ 26 天上取整；**草案「≥1 个完整往返周期」有歧义**（按中位 7 天理解严重不足），必须改写成绝对天数。并跑要覆盖的不是典型往返，是最慢的那一类 |
| **D5** | **`已签认` 状态只能由真实回件驱动**，不存在任何超期自动签认路径 | IATF 显式签认红线（判据/口径/阈值类永不默认生效）。🔴 须落断言测试，不得只写在文档里 |
| **D6** | **信是视图、点是真身** —— 跟进信从台账投影生成，回件解析后回写台账；不在台账之外维护第二份口径点清单 | 草案设计核心。实测队列引 `部门#N` 已 **576 处**、两天增 70 处（约 35 处/天）⇒ 再建一份平行表只会成为第 577 处会漂移的引用 |
| **D7** | **台账格式 ＝ JSONL，并在 `.gitignore` 加一条范围严格限定的 `!` 例外**：`!6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl` | Shao Peishen 2026-09-01 拍 (b)。审材料 §6.3 已实测 11 条全过，**含 4 条反向对照组**（`reports/wecom_aibot_audit.jsonl`／`4-数字员工/…/reports/audit.jsonl`／`5-平台底座/logs/runtime.jsonl`／台账子目录内 `.jsonl`）证明例外未误放行任何运行时审计 JSONL。🔑 **判断显式留痕在 `.gitignore` 里，而非靠换扩展名绕开**——规则的实质保护力不变，只是让它表达本意 |
| **D8** | **台账目录下不得建子目录**，五份域文件平铺 | 审材料 §6.3 第 4 条对照组实测：`!` 例外**不递归**，子目录内 `.jsonl` 仍被静默忽略。🔴 须落成 lint 断言——又一个「错误不产生任何信号」 |
| **D9** | **质量域台账入库，与其他域同等对待** | Shao Peishen 2026-09-01 拍 (a)。依据一条现状事实：质量部跟进信（含 8D 判例原文）**本就已在 git 里**，台账入库不比现状更差；按域分文件（D2）已满足隔离要求。⚠️ **代价须写明：这是维持现状风险，不是消除它** |

---

## Open Questions

**已清空。**

原唯一开放项「JSONL vs YAML」由 **D7** 定死。
⚠️ 该项曾于 2026-09-02 被落包件**重新打开一次并给出相反结论**，已由本件《落包勘误》节关回。**它不是一个仍在争论的问题。**

---

## 🔴 apply 前置：CC 必须先做的两件事（不得跳过）

**⑴ 那 11 条是在 `/tmp` 独立 repo 上验的规则逻辑，不是真仓库状态。**
CC 在真 `.gitignore` 落这条例外后，**必须在真仓库原样复跑那 11 条并贴出输出**。
🔑 **为什么这条必须写出来**：09-01 那次会话已两次栽在「拿到一个看起来很正常的结果」上（PowerShell 逐行比对给出的假「相同」、自己构造的伴生件路径给出的假缺口）；**2026-09-02 本次又栽了两次**（手写 `find("## 一、")` 截 §一 拿到 47 行/WIP=6 的假值、`grep | head` 截断得出「分诊器零调用」的假结论）。**在临时 repo 上验过 ≠ 在真仓库里成立**，两者之间隔着一份可能已被别人改过的 `.gitignore`。

**⑵ 例外规则必须加在 `.gitignore` 第 32 行 `**/*.jsonl` 之后**（git 后序规则优先）。
**加在之前完全无效，且不报错。**

---

## 审记录（Shao Peishen 2026-09-01 当场审定）

| # | 事项 | 裁决 | 落点 |
|---|---|---|---|
| ⑴ | D6「台账入库」与 `.gitignore:32 **/*.jsonl`（原注释「所有运行时审计 JSONL — 含业务数据，绝不入库」，仓库 `.jsonl` 入库数实测 ＝ 0）冲突 | **(b) 保留 JSONL ＋ 加 `!` 精确例外** | 新增 **D7**（连带 **D8**） |
| ⑵ | 质量域台账含 OEM 技术数据，是否入库 | **(a) 入库，与其他域同等** | 新增 **D9** |
| ⑶ | 其余五条 Decisions（D1–D5） | **(a) 整体照准** | 不变 |

**判「审已过」的三条依据**：① 前置已满足——草案要求的 `git check-ignore -v` 真实输出共 **19 条**（8 条首轮 ＋ 11 条例外验证），全部符合预期；② Decisions 九条全部定死，无悬置项；③ Open Questions 已清空。

🔑 **附一条方法留痕**：草案定的「三行 `check-ignore` 输出须落包后才能补」是**误以为存在的阻塞** —— 实测该命令**不要求文件存在**（8 条被测路径当时 `Test-Path` 全为 False，仍正常返回规则行号）。该前置在草案阶段即可满足。
