# 口径点台账 Proposal

> **落包完成 2026-09-02（Cowork `OP-0902-A`）。来源两份，缺一即错：**
> **① 草案** `1-转型规划/0-全景路线图/openspec提案草案-口径点台账-2026-08-30.md`（`OP-0830-A-附`）；
> **② 🔴 收敛件（权威，覆盖草案）** `1-转型规划/0-全景路线图/design审前置-口径点台账三开放点收敛-2026-09-01.md`（`OP-0901-D`）——三个开放点已于 2026-09-01 由 Shao Peishen 全部拍完，Decisions D1–D9 见本包 `design.md`。
> 类别判定：**机制/环境类变更包**（守护本仓库流程自身），故 §「退休哪一个既有守卫」为强制节。
> 依据：`AI原生SDLC手册对照评估-构建流程与需求管理-2026-08-30.md` §三.一、§四 R1。
>
> 🔴 **2026-09-02 勘误：本件初版曾把台账格式由 `.jsonl` 改判为 `.yaml`，该改判已撤回。**
> 初版落包时只读了草案、没读收敛件，于是把「保 `.jsonl` ＋ 加 `!` 例外」判为「在写着绝不入库的规则上凿洞」而否掉 —— **而那正是 Shao Peishen 2026-09-01 拍的 (b)（D7），且已实测 11 条通过、含 4 条反向对照组**。成因与判据见 `design.md`《落包勘误》节。**格式以 D7 为准 ＝ JSONL ＋ 精确例外。**
---

## Why（为什么做）

**需求管理的单位是「信」，而我们要度量、要串行、要销号的对象是「口径点」——单位错位导致三件事同时失真。**

三条实测证据：

1. **度量拿不到真值。** `6-人才与组织/部门AI专员跟进/度量复盘-判例批改法回退闸到期-2026-08-23.md` §二 自陈：闸原文要的是"开放口径点提出到销点中位天数"，实际算的是"信发出到回件闭环天数"，**"没有逐点台账，就没有精确值"**。中位 2 天这个达标结论建立在代理指标上。
2. **销点动作散在四处**——队列行／README 状态列／design Decisions／代码 commit，没有任何一处能回答"这个口径点现在在哪"。跨引用靠人写的文字维系：README ↔ 队列双向交叉引用 **576 处**（2026-09-01 实测；08-30 为 506 处 ⇒ **两天增 70 处、约 35 处/天**），"编号滞后"一族已复发 **6 次**。
   🔑 **这一笔债不是静态的，是每天在涨的** —— 该论证比结构论证更硬：它给「先做 R1」提供了时间维度的理由。
3. **一封信 ≤10 条判例，各点不同时销**——信闭环 ≠ 点闭环，而串行闸只看信。

**手册对位**：Anthropic《AI-Native SDLC Playbook》第 1 阶段 intent.md——"每个阶段结束时往版本控制里写一个产物，下一个阶段从读取这个产物开始"。本项目从 `proposal.md` 往后这条链是完整的，**唯独最上游那一节没有产物**。

---

## What Changes（改什么）

**新增能力：**

- **`口径点台账`**：一点一条，机器可读，**格式＝ `.jsonl` ＋ `.gitignore` 精确例外**（D7）；**按域分五份平铺于 `6-人才与组织/部门AI专员跟进/口径点台账/`**（D2），**目录下不得建子目录**（D8，例外不递归）。随代码进版本控制。字段：
  | 字段 | 说明 |
  |---|---|
  | `id` | 稳定标识，形如 `FI2-D19-03`（场景-判例批次-序号）。🔴 **不随队列行增删漂移**，这正是它优于 `§一 #N` 的地方 |
  | `场景` | 归属场景码（FI2／SC8／QD-B…） |
  | `提出人` / `提出日` | 提出人取自人员名录正本，🔴 不推断 |
  | `判例原文` / `拟改判定` | 判例批改法的两栏原样 |
  | `状态` | `待问` → `在途` → `已签认` → `已回灌` → `已落码` ／ `已作废`（单向，回退须留因） |
  | `承接载体` | openspec change 名 ／ 队列行号 ／ commit sha，至少一项 |
  | `载体信` | 该点由哪封信投影出去（`部门#N`），可多封 |
  | `到期` | 可空；非空则进超期扫描 |

- **跟进信改为台账的投影**：起草时从台账取"该收信人状态＝`待问`"的点生成判例包；回件解析后**回写台账**。🔴 **信是视图，点是真身**——这是本变更的设计核心，不是在信之外再加一张表（那只会变成第 507 处会漂移的引用）。

- **度量派生**：`口径点提出 → 销点` 中位天数由台账直接算出，取代现行代理指标。

**不改：**

- 判例批改法本身（格式、≤10 条、判据类不默认生效、不加并行专员）——**只换承载单位，不动方法**。
- 串行闸口径（改按点计属 R2，另立变更包，硬依赖本包）。
- 已闭环的 **38** 封历史信不追改（同"历史记录不追改"惯例）；**回溯拆点只做未闭环的 21 封**。
  ⚠️ **数字已按 2026-09-01 实测刷新**（草案的 17／15 系 08-30 值，已全线过期）：README 主表全量 60 封 ＝ 已闭环 38（`📥 已回件并回灌` 34／`✅ 无需回复` 3／`📨 已确认闭环` 1）＋ 未闭环 21（`✅ 已推送` 15／`✅ 已发` 6）＋ 已作废 1。**一律采用 README 主表全量口径。**

---

## 知识资产三问（强制，全景规划 §1.4 第 2 条）

**1. 本流程哪些判断是人脑默会经验？**

| 默会判断 | 现在只在谁脑子里 |
|---|---|
| **"这段回件里有几个口径点"**——一封回件的自由文本要拆成几条，边界在哪 | 🔴 **已成文 ＝ P1–P4（D1）**。判据不必发明：跟进信 frontmatter 的 `决策点:` 字段**已实践 18 次**，覆盖率 7 月 37% → 8/01-10 **70%** → 8/11-31 **0%**（最后一封＝`采购部-姚祖怡-跟进-2026-08-10-…`）。⇒ **R1 的性质是「给一个曾达 70%、因无载体而消亡的实践建载体」** |
| **"这个点算不算已签认"**——专员回了一句"可以"，是签认了整条还是只认了前半句 | 同上；现行只有信级四态，无点级判据 |
| **"这个点的承接载体是谁"**——落 openspec 还是落队列行还是就地改代码 | 🔴 **有现成来源 ＝ frontmatter `配套:` 字段**，实测 **36/67 封（54%）**已在用，取值形态正是所需：`队列 #114`／`openspec fi2-recon-mvp`／`design D19`／`§一 #343`／`§四 #59`。**台账两个核心字段都不是从零设计，是把已有实践固化** |
| **"哪些点必须专员签认、哪些可默认生效"** | 🔴 **这一条已成文**（判据/口径/阈值类永不默认生效，IATF 显式签认红线），是四条里唯一不默会的 |

**2. 由谁显性化？**

- **持有人**：Shao Peishen（OPVP，判定权威单点）
- **backup**：孙涛（决策代理范围内：design 审、批间 Triage、L2 日常审批）
- 登记进《跨场景前置数据与知识库任务总表》§一.2 知识资产台账。
- 🔴 **不含任何部门专员**——拆点判据是我方内部口径，走 grill 不走判例批改法（`zhuopin-requirement-grill` 边界：受访者只有场景发起人）。

**3. 用什么方法提取？**

**历史案例反推为主 ＋ AI 起草·专家批改为辅**：

- 取已闭环的 **38** 封信作为**训练集**——它们的回件与后续处置都已发生完毕，逐封反推"当时实际拆成了几个点、每个点最终落在哪"，由此归纳拆点边界与承接载体判据；
  🔴 **其中 18 封带 `决策点:` 字段者是已标注样本**：边界由当时的起草者判定，属**专家标注、非事后反推**，先吃这 18 封并直接作黄金集（P3）。
- 取未闭环的 **21** 封作为**验证集**——用归纳出的判据实拆一遍，交 Shao Peishen 批改；
- 🔴 **不设并行提取人**（同判例批改法"不加并行专员"）。

---

## 验收与晋档条件（强制，四档口径）

**本变更包交付后场景所处档位**：**档 2（真实数据跑通）**。
理由：台账建成并回溯拆点后即在真实数据上运行；但"跟进信从台账投影生成"这条链要到 R2/R3 落地后才算内部服务化。

**晋下一档（档 3 内部服务）的条件**（逐条）：

1. 未闭环 15 封的回溯拆点完成，且 Shao Peishen 签认拆点结果；
2. `工具-跟进闸查询.py` 改为从台账取数（不再直读 README 表格），且**新旧双路并跑 ≥28 天（4 周）**后输出对齐（**D4**：实测 35 封闭环信往返周期 P90 ＝ 26 天上取整。🔴 草案原文「≥1 个完整往返周期」有歧义——按中位 7 天理解则严重不足，慢的那一半信从未被双路同时验证过，切换后才暴露就是生产事故）；
3. 至少一封新信**完整走通**"台账 → 投影生成判例包 → 发出 → 回件解析 → 回写台账"全链；
4. 度量脚本能直接产出"口径点提出 → 销点"中位数，与现行信级代理指标同期并跑一轮做对照。

**价值指标**（三类取二）：

- **质量型**：口径点"承接载体缺失"数量 —— 基线由本包交付时的首次全量扫描确定，目标归零并保持；
- **风险型**：跟进信编号／状态两份副本互相印证着一起错的复发次数 —— 基线 ＝ **6 次**（README 原文自陈第二至第六次复发，加上 2026-08-10 未编号的首次；日期＝08-10／08-12／08-18／08-21／08-25 两处），目标交付后 **0**。
- 基线由业务 Champion（Shao Peishen）在启动前确认存档。

**LLM 判据黄金集**：
本包**含 LLM 运行时判断**——"回件自由文本 → 拆成几个点 / 每点状态如何"是模型判断。
黄金集计划：以**那 18 封带 `决策点:` 字段的信**的**冻结回件原文 ＋ 字段内已有的拆点结果**为首批黄金集（目标 **≥18 组**）。🔴 **它们是当时起草者的专家标注，不是事后反推**——这比草案原设想的"从 17 封闭环信反推"强一档。🔴 **无黄金集不晋档 3。**

---

## 🔴 退休哪一个既有守卫（机制/环境类变更包强制，协议〇.9 措施 B）

**退休对象：README 表格「编号」列作为取号依据的地位，以及本变更覆盖到的"人写的自由文本进了机器要读的位置"那一处。**

具体：

- **可退**：README「下一个可用号」段与下表「编号」列这**两份自由文本副本**——本包落地后取号一律从台账派生（台账 `id` 是稳定标识、由机器分配）。这两处已实测**六次互相印证着一起错**，是本仓库同族复发次数最高的一处。退休方式 ＝ **降为派生视图**（继续显示、但不再是判据来源），不删除。
- 🔴 **不可退，且必须写明为何不能**：
  - **README「发送状态」列**——它是 2026-08-21 你答 §四 #85 选 (b) 确立的"一封信状态的唯一权威源"。本包管的是**点**，不是**信**；两者是不同粒度的两件事，**不得借本包之机把信级权威源也一并改掉**（那会同时动两个口径，出问题时无法定位是哪一个引起的）。信级权威源的调整属 R2，另包处理。
  - **判例批改法本身**——它解决的是"怎么问"，本包解决的是"问出来的东西存在哪"，不同层，不构成替代关系。

---

## 🔴 伴生文件的 .gitignore 覆盖（强制，队列 #328 子项②）—— **已实测，共 19 条；结论 ＝ JSONL ＋ 精确例外（D7）**

**实测分两轮，全部为 `git check-ignore -v` 真实输出、非推断。**

🔑 **先记一条方法留痕**：草案定的「三行 `check-ignore` 输出须落包后才能补」是**误以为存在的阻塞** —— 实测该命令**不要求文件存在**（8 条被测路径当时 `Test-Path` 全为 False，仍正常返回规则行号）。⇒ 该前置在草案阶段即可满足，不必等落包。

### 第一轮（2026-09-01，未加例外前，8 条）—— 推翻了草案「直接入库」的假设

| 新增形态 | 应否被忽略 | **实测** | 命中规则 |
|---|---|---|---|
| `…/口径点台账/采购域.jsonl` | **否，必须进版本控制** | 🚫 **被忽略** | `.gitignore:32 **/*.jsonl` |
| `…/口径点台账/质量域.jsonl` | 同上 | 🚫 **被忽略** | 同上 |
| `…采购域.jsonl.editlock`（及 `.editlock.lastknown`／`.snapshot`／`.mutex.stale`） | **是** | 🚫 被忽略（符合预期） | `.gitignore:69 *.editlock*` |
| `reports/口径点度量-YYYY-MM.json` | **是**（可随时重算） | 🚫 被忽略 | `.gitignore:35 **/reports/` |

**`.gitignore:31` 注释原文 ＝「所有运行时审计 JSONL — 含业务数据，绝不入库」；仓库中已入库的 `.jsonl` 实测 ＝ 0（一个例外都没有）。**

> ⚠️ **本轮第一次差点报一个假警，如实留痕**：按草案措辞构造了 `xxx.jsonl.lastknown` 这类路径，实测「不被忽略」，一度判为缺口。**去查现网真实伴生件命名后才发现是构造的样本不对**——真实命名一律带 `.editlock` 前缀，被 `*.editlock*` 正确覆盖。🔑 **凡结论建立在自己构造的样本上，先去看一眼真身。**

### 第二轮（2026-09-01，Shao Peishen 拍 (b) 后，11 条）—— 例外方案已验证有效

**验证方式**：在 `/tmp` 新建独立 git repo、复制真实 `.gitignore`、追加例外后实跑，**全程不碰真仓库**。追加内容：

```gitignore
# 队列 #439 / OP-0901-D design 审 2026-09-01（Shao Peishen 拍 (b)）：口径点台账是人工整理的口径记录，
# 不属第 31 行所指的「运行时审计 JSONL」。仓库首个 .jsonl 入库例外，范围严格限定到该目录。
!6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl
```

| 组 | 路径 | 期望 | 实测 |
|---|---|---|---|
| 台账主文件 | `…/口径点台账/{采购,财务,质量,销售,IT}域.jsonl`（5 条） | 不被忽略 | ✅ 全部不被忽略 |
| **对照组** | `reports/wecom_aibot_audit.jsonl` | 仍被忽略 | ✅ |
| **对照组** | `4-数字员工/财务部/FI2-…/reports/audit.jsonl` | 仍被忽略 | ✅ |
| **对照组** | `5-平台底座/logs/runtime.jsonl` | 仍被忽略 | ✅ |
| **对照组** | `…/口径点台账/子目录/深层.jsonl` | 仍被忽略 | ✅ |
| 伴生件 | `…/采购域.jsonl.editlock`、`….editlock.lastknown` | 仍被忽略 | ✅ |

🔴 **对照组是本节的重点、不是陪衬**：它证明这个例外**没有误放行任何真正的运行时审计 JSONL** —— 红线的实质保护力未被削弱，只是让规则表达了它的本意。

🔴 **由第 4 条对照组带出 D8**：例外**不递归**，`口径点台账/子目录/*.jsonl` 仍被忽略 ⇒ **台账目录下不得建子目录**，否则其中的 `.jsonl` 会被静默忽略 —— 又一个「错误不产生任何信号」。**须落成 lint 断言，不能只写在文档里。**

### 🔴 为什么是「加例外」而不是「换扩展名」（Shao Peishen 2026-09-01 拍 (b) ＝ D7）

那条规则的注释原文是：

```
# 所有运行时审计 JSONL — 含业务数据，绝不入库
**/*.jsonl
```

⇒ **它挡的是「含业务数据的运行时审计」**（现存命中项：`fi2_audit.jsonl`／`connector_access.jsonl`／`sc8_real_validation.jsonl` 等）。**口径点台账是受版本控制的设计产物，性质与之相反——它撞规则纯粹是因为共用了扩展名。**

**三条出路对比**（`.yaml`／`.md`／`.json`／`.ndjson` 均实测会入库，故三条在技术上都可行；判定分歧不在能否入库，在**留不留痕**）：

| 方案 | 判定 |
|---|---|
| **保 `.jsonl` ＋ 加范围严格限定的 `!` 例外（✅ 采用 ＝ D7）** | ✅ **判断显式留痕在 `.gitignore` 里**，后人读规则就读到了这次判断本身及其理由（例外行上方两行注释写明队列行号、审期与依据）。**规则的实质保护力不变**——4 条反向对照组实测已证明未误放行任何运行时审计 JSONL。例外规则本身作为新形态，已按 #328 走完核实（第二轮 11 条） |
| ~~`.yaml`~~ | ❌ **换扩展名等于绕开规则而不留痕**：`.gitignore` 里看不到这次判断发生过，后人只会看到台账莫名其妙不是 JSONL，且那条「绝不入库」的规则仍在字面上覆盖着一个它本不该覆盖的对象。⚠️ **本包初版曾错误采用此方案，2026-09-02 已撤回**，成因见 `design.md`《落包勘误》节 |
| `.md` 表格 | ❌ **正是要根治的病**——README 表格 576 处交叉引用、编号滞后复发 6 次；台账要机器读，不该退回 md 表格 |

🔑 **这个问题只有实跑 `git check-ignore` 才会暴露。** 草案里那句「落包时必须补上真实输出，缺则 proposal 不完整」当时只是照规矩办事，**没料到真会撞上**。⇒ **#328 那条 MANDATORY 不是形式，它真能拦住东西。**

⚠️ **遗留约束（三条）**：
1. **台账目录下不得建子目录**（D8，例外不递归）；
2. **例外规则必须写在 `.gitignore` 第 32 行 `**/*.jsonl` 之后**（git 后序优先），**写在之前完全无效且不报错**；
3. 运行时派生物一律落 `reports/`。实现期若出现本节未列举的第四种文件形态，**回到本节重新实测**，不得沿用本结论。

---

## Impact（影响面）

**受影响 specs / 代码：**

- 新建：台账数据文件 ＋ 读写模块 ＋ 回溯拆点一次性脚本
- 改动：`0-学习与工具/工具-跟进闸查询.py`（取数源由 README 改为台账，晋档 3 时）
- 改动：`6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md`（编号段降为派生视图）
- **不改**：`工具-共享文档编辑锁.py::_validate_followup_readme_release`（属 R2）

**红线核对：**

| 红线 | 核对结论 |
|---|---|
| **mock 先行** | ✅ 回溯拆点先在 17 封已闭环信上跑（结果已知，可对答案），再动未闭环的 15 封 |
| **audit 留痕** | ✅ 台账每次状态转换写 append-only 记录（谁改的、何时、依据哪封回件） |
| **OEM 隔离** | ✅ **已定：按域分文件**（**D2**，Shao Peishen 2026-09-01 拍 (a)）——质量域物理独立成文件；**跨域度量只读聚合、禁止任何合并落盘的产物（含缓存）**（**D3**，须落断言测试）。**质量域入库与其他域同等**（**D9**，依据＝质量部跟进信含 8D 判例原文本就已在 git 里，台账入库不比现状更差；⚠️ 属维持现状风险、非消除） |
| **L2 人工门禁** | ✅ 不涉——台账只记录口径，不触发任何业务执行 |
| **ISO 26262** | ✅ 不涉——非安全相关代码 |
| **判据类永不默认生效** | ✅ 台账 `已签认` 状态**只能由真实回件驱动**，不得有任何"超期自动签认"路径。🔴 这条须在 tasks 里落成断言测试，不能只写在文档里 |

---

## ✅ 三个开放点已收敛（Shao Peishen 2026-09-01 全部拍完）

| 开放点 | 收敛结论 | 落点 |
|---|---|---|
| ① 拆点粒度判据 | **P1–P4**；且判据不必发明——`决策点:` 字段已实践 18 次、曾达 70% 覆盖后归零 | **D1** |
| ② 台账是否按域分文件 | **按域分五份平铺**，质量域物理独立；跨域只读聚合、禁止合并落盘；质量域入库与其他域同等 | **D2／D3／D9** |
| ③ 双路并跑期多长 | **≥28 天（4 周）**，取实测 P90 ＝ 26 天上取整 | **D4** |

🔴 **本节保留为收敛记录，不得再当作开放项。** 九条 Decisions 与审记录见本包 `design.md`；审材料正本见 `1-转型规划/0-全景路线图/design审前置-口径点台账三开放点收敛-2026-09-01.md`（`OP-0901-D`）。
