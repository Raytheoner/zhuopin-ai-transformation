# coverage-point-ledger Tasks

> 🛑 **design 审通过前全部不得开工。** 执行环境标注见各条。
> 来源草案 `OP-0830-A-附`（2026-08-30）｜落包 `OP-0902-A`（2026-09-02）｜**机制/环境类变更包**

## 1. design 审（先行，三个开放点必须先收敛）

- [ ] 1.1 **拆点粒度判据** —— 一条判例回件拆成几个点、边界在哪。🔴 **这是本包最核心的默会经验**（知识资产三问 §1 第一条），靠历史案例反推产出，**不是拍脑袋定**【Cowork ＋ 他】
- [ ] 1.2 **台账是否按域分文件** —— 🔴 **直接关系 OEM 隔离红线**：质量域口径点可能含 OEM 技术数据（PPAP／FMEA／含 OEM 信息的 8D），跨域统一存放**会破隔离**。建议按域分文件，须 design 审定【他】
- [ ] 1.3 **`工具-跟进闸查询.py` 双路并跑期多长** —— 新旧口径对照几轮才算对齐（建议 ≥1 个完整信往返周期）【他】
- [ ] 1.4 审过后 `design.md` 转 `status: 生效`，本节勾完方可开工下节【Cowork】

## 2. 台账本体

- [ ] 2.1 台账 schema 定稿（8 字段，见 proposal「What Changes」表）＋ 空台账落盘【Cowork】
  - 🔴 **格式必须是 `.yaml`，不得用 `.jsonl`** —— 后者会被 `.gitignore:32:**/*.jsonl` 全局吞掉（proposal「伴生文件」节有实测输出）。**本包所有新增受控文件同此约束**；运行时派生物一律落 `reports/`。
  - 🔴 `id` 形如 `FI2-D19-03`（场景-判例批次-序号），**不随队列行增删漂移** —— 这正是它优于 `§一 #N` 的地方。
- [ ] 2.2 台账读写模块（含 append-only 审计：谁改的／何时／依据哪封回件）【CC】
- [ ] 2.3 🔴 **断言测试：`已签认` 状态只能由真实回件驱动，不得存在任何「超期自动签认」路径**【CC】
  - 成因：判据/口径/阈值类**永不默认生效**是 IATF 显式签认红线。proposal 已写明「这条须在 tasks 里落成断言测试，不能只写在文档里」——**本项就是那条落点**。

## 3. 回溯拆点（先 mock 后真实，守红线）

- [ ] 3.1 **训练集：已闭环 17 封信**逐封反推「当时实际拆成几个点、每点最终落在哪」，归纳拆点边界与承接载体判据【Cowork】
  - ✅ **合规**：这 17 封结果已知、可对答案，符合「先 mock 跑通逻辑再切真实」红线。
- [ ] 3.2 **验证集：未闭环 15 封**用归纳出的判据实拆一遍，交 Shao Peishen 批改【Cowork ＋ 他】
- [ ] 3.3 🔴 **已闭环的 17 封不追改**（同「历史记录不追改」惯例），回溯拆点只做未闭环的 15 封【Cowork】
- [ ] 3.4 **黄金集建立**：以 17 封的冻结回件原文 ＋ 专家认可的拆点结果为首批（目标 ≥17 组）。🔴 **无黄金集不晋档 3**【Cowork】
  - 成因：本包含 LLM 运行时判断——「回件自由文本 → 拆成几个点／每点状态如何」是模型判断，**必须有黄金集才能验证它没漂**。

## 4. 接入（晋档 3 的前提，本期只做到可切换）

- [ ] 4.1 `工具-跟进闸查询.py` 增加「从台账取数」通路，**与现行直读 README 双路并跑**，不切换默认【CC】
- [ ] 4.2 度量脚本：由台账直接算「口径点提出 → 销点」中位天数，与现行信级代理指标同期并跑做对照【CC】
- [ ] 4.3 README「编号」段降为**派生视图**（继续显示、不再是判据来源），🔴 **不删除**【Cowork】
  - ⚠️ **只降编号段，`发送状态` 列一字不动** —— 那是 2026-08-21 §四 #85 答 (b) 确立的「一封信状态的唯一权威源」，属**信级**；本包管**点级**。**不得借本包之机同时动两个粒度的口径**，否则出问题无法定位是哪一个引起的。信级调整属 R2。

## 5. 验收与晋档

- [ ] 5.1 **本包交付＝档 2（真实数据跑通）**：台账建成并回溯拆点后即在真实数据上运行【Cowork】
- [ ] 5.2 晋档 3 四条件（逐条核，缺一不晋）：① 未闭环 15 封拆点完成且他签认；② 闸查询新旧两路输出对齐一轮；③ **至少一封新信完整走通「台账 → 投影生成判例包 → 发出 → 回件解析 → 回写台账」全链**；④ 度量脚本与代理指标同期并跑一轮做对照【他 ＋ Cowork】
  - 🔴 **③ 是本包的真验收点** —— 只建台账、不走通投影链，等同 `#382` 那次「只验消费端不验产生端」。**台账建成 ≠ 信是它的投影。**
- [ ] 5.3 价值指标基线由 Shao Peishen 在启动前确认存档：质量型＝「承接载体缺失」数量（首次全量扫描定基线，目标归零）；风险型＝「编号/状态两份副本互相印证着一起错」复发次数，**基线 6 次，目标 0**【他】

## 6. 收口

- [ ] 6.1 队列登记与批次回写【Cowork】
- [ ] 6.2 本包归档 `/opsx:archive coverage-point-ledger -y`（完工即归档纪律）【Cowork/CC】

## 7. 已知未闭合（如实登记，非本期范围）

- [ ] 7.1 **R2（串行闸改按点计）不在本包** —— 它硬依赖本包，另立变更包。**本包不动串行闸口径。**
- [ ] 7.2 **OEM 隔离的最终形态待 1.2 定** —— 若审定为按域分文件，2.1 的 schema 与 2.2 的读写模块须相应分片；**在 1.2 未定之前不得先建单一大文件**（建了再拆会留一份跨域的历史快照在 git 里，那本身就破隔离）。
