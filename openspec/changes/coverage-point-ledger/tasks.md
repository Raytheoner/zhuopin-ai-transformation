# coverage-point-ledger Tasks

> ✅ **design 已审过（Shao Peishen 2026-09-01 当场审定，`OP-0901-D`）**，九条 Decisions 见本包 `design.md`。
> 🛑 **但 apply 的前置仍未解除**：队列 `#439` 去 🛑 的条件 ＝ 可动 WIP 回落。**审过 ≠ 可开工。**
> 来源：草案 `OP-0830-A-附`（2026-08-30）＋ **收敛件 `OP-0901-D`（2026-09-01，权威，覆盖草案）**｜落包 `OP-0902-A`（2026-09-02）｜**机制/环境类变更包**
> 🔴 **2026-09-02 勘误**：本件初版按草案把三个开放点重列为「未收敛、审前不得开工」，并把台账格式改判为 `.yaml` —— **两者均已撤回**，成因见 `design.md`《落包勘误》节。

## 0. apply 前置（CC 必做，不得跳过）

- [x] 0.1 🔴 在真 `.gitignore` 落 D7 例外后，**在真仓库原样复跑第二轮那 11 条 `git check-ignore -v` 并贴出输出** —— `/tmp` 独立 repo 上验的是**规则逻辑**、不是真仓库状态，两者之间隔着一份可能已被别人改过的 `.gitignore`【CC】
- [x] 0.2 🔴 例外规则**必须加在 `.gitignore` 第 32 行 `**/*.jsonl` 之后**（git 后序优先）；**加在之前完全无效且不报错**【CC】

## 1. 台账建表

- [x] 1.1 定 schema（8 字段，见 proposal「What Changes」表），**格式 ＝ JSONL**（D7）【Cowork】 —— ✅ 2026-09-06 `OP-0906-W`：`6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`（每行＝一次事件 append-only、`fact_date`／`recorded_on` 并存落 spec 第四条；`id` 两形态，与 `criteria_signoff` 同名一条留 §四 待两包合审）
  - 🔴 `id` 形如 `FI2-D19-03`（场景-判例批次-序号），**不随队列行增删漂移** —— 这正是它优于 `§一 #N` 的地方。
  - 🔴 **ID 前缀隐含域，不另设 `域` 字段**（D2）——少一个字段就少一处可能与文件名不一致的副本。
- [x] 1.2 建**五份域文件骨架**（`采购域`／`财务域`／`质量域`／`销售域`／`IT域`）平铺于 `6-人才与组织/部门AI专员跟进/口径点台账/`，空文件入版本控制【Cowork】 —— ✅ 2026-09-06 `OP-0906-W`：五个 0 字节 `.jsonl` 平铺；`git check-ignore`（无 `-v`）五条退出码 1＝全部不被忽略，`git status -uall` 可见；对照组 `口径点台账/子目录/深层.jsonl` 仍命中 `.gitignore:32`
  - 🔴 **目录下不得建子目录**（D8）——`!` 例外不递归，子目录内 `.jsonl` 会被**静默忽略**。
- [x] 1.3 台账读写模块（含 append-only 审计：谁改的／何时／依据哪封回件）【CC】
  - ✅ `OP-0906-Z`（2026-09-06）：`5-平台底座/zhuopin_platform/zhuopin_platform/coverage_point_ledger/`（`models`／`store`／`aggregate`／`overdue`／`lint`／`errors`）。审计三问落 `by`／`recorded_on`＋`fact_date` 并存／`evidence`＋`letters`，**不另建审计文件**（台账自己就是审计轨）。`LedgerStore` **没有** `update()`／`delete()`／`set_status()` —— append-only 不是纪律，是没有那个函数。测试 `tests/test_coverage_point_ledger_store.py`。
  - ✅ **`carrier` 口径已定死（Shao Peishen 2026-09-06 经 `OP-0906-X` 拍板 (a)）**：`carrier` ≥1 **仅对 `建点` 行**生效，`转态` 行可空、写入期不强制。SCHEMA §二 字段表已同步改齐、`models.py` 首部留痕已由「待合审」改为「已定死」。理由 ⑴ 强制会让 tasks §6.3 的质量型指标「承接载体缺失数量」**结构性恒为 0**、⑵ 强制会逼 §2 回溯拆点**编造**载体（与「历史记录不追改」相抵）。⇒ 载体缺失走度量（`Snapshot.missing_carrier`）。**本条不再进两包合审**；合审剩余项只有 SCHEMA §四「`id` 与 `criteria_signoff` 同名」那条。
- [x] 1.4 🔴 **断言测试：跨域聚合只读、不产生任何合并落盘产物（含缓存）**（D3）【CC】
  - ✅ `tests/test_coverage_point_ledger_aggregate.py::test_D3_跨域聚合只读不落盘_含缓存`：五个域各有点的 tmp 台账上跑 `aggregate()`，期间堵死 `builtins.open`／`io.open`（Path.open 走的是后者，不是前者）／`write_text`／`write_bytes`／`touch`／`mkdir`，并对台账目录树与一个**空的当前工作目录**各做调用前后 sha256 指纹比对；同时断言确实覆盖了 5/5 个域（否则「什么都没干」也能通过）。另有反向对照组 `test_守卫本身有效_写盘会被抓到` 与 `test_D3_Snapshot无任何落盘方法`。
- [x] 1.5 🔴 **断言测试：`已签认` 状态只能由真实回件驱动，不存在任何「超期自动签认」路径**（D5）【CC】
  - 成因：判据/口径/阈值类**永不默认生效**是 IATF 显式签认红线。proposal 已写明「这条须在 tasks 里落成断言测试，不能只写在文档里」——**本项就是那条落点**。
  - ✅ `tests/test_coverage_point_ledger_signoff.py`，一条「路径不存在」从三面同时打：**构造面** `test_D5_已签认无回件则连对象都造不出来`（无 `evidence` 的 `已签认` 在 `LedgerEvent` 构造期即抛，连对象都造不出来）；**行为面** `test_D5_超期扫描只出催办草稿_不改状态不加行`（超期 1000 天的点扫完仍是 `在途`，台账指纹逐字节不变）；**入口面** `test_D5_全包只有一处写台账内容的代码`（AST 扫全包，写内容的代码有且只有 `store.py::_append_line`）。另有正向对照组（带落档回件的 `已签认` 必须写得进去）、`判据类` 不得配 `default_after_h`、`overdue.py` 不得 import `LedgerStore`。
- [x] 1.6 🔴 **lint 断言：台账目录下不得出现子目录**（D8）【CC】
  - ✅ `tests/test_coverage_point_ledger_lint.py::test_D8_台账目录出现子目录即lint失败`：干净目录先过（防「本来就红」）→ 建子目录后 fatal 且报告点出「`!` 例外不递归、其中 `.jsonl` 并未入库」→ `assert_clean()` 抛。空子目录同样违规（违规发生在建目录那一刻）。CLI 闸 `python -m zhuopin_platform.coverage_point_ledger.lint <目录>` 违规退出码 1。**真台账目录实跑通过**（只读）：`✅ …/口径点台账：目录布局合规（无子目录、五份域文件平铺）`，exit=0。

## 2. 回溯拆点（先已标注样本、后补拆，守红线）

- [ ] 2.1 **先吃 18 封带 `决策点:` 字段的信** —— 边界已由当时的起草者判定，属**专家标注、非事后反推**，直接入库并作首批黄金集（目标 ≥18 组）【Cowork】
  - ✅ **合规**：这批结果已知、可对答案，符合「先 mock 跑通逻辑再切真实」红线。
  - 🔴 **无黄金集不晋档 3**（本包含 LLM 运行时判断：「回件自由文本 → 拆几个点／每点状态」是模型判断，必须有黄金集才能验证它没漂）。
- [ ] 2.2 用 **P1 判别式**补拆其余已闭环信（训练集共 **38** 封）【Cowork】
- [ ] 2.3 用归纳出的判据实拆**未闭环 21 封**（验证集），交 Shao Peishen 批改【Cowork ＋ 他】
- [ ] 2.4 🔴 **已闭环的 38 封不追改**（同「历史记录不追改」惯例）【Cowork】
- [ ] 2.5 首次全量扫描「承接载体缺失」数量，确定价值指标基线【Cowork】

## 2bis. 回件 → 台账回写接线（2026-09-07 缺口补登，派单 `OP-0907-AK`）

> **缺口实证**：2026-09-07 `采购部#21` 回件 14:03 到、事件驱动拆件 30 分钟自动跑完（`#499` 新立、README 转 📥、§四 `#167`/`#168`），**台账零回写**。成因不是故障：本包 §6.2③ 要求「回件解析 → 回写台账」走通，但 §1–§7 **没有任何一条建造它**，拆件 skill `huijian-chaijian-patrol` §三 只回灌队列行与接力卡；写盘也只有 Python 入口、无 CLI，无头拆件会话无法调用。`采购部#21` 本身非台账投影信（采购域零建点）属正常形态，但同样的回件若是 `财务部#17`（`FI10-G-07` 已 `在途`）今天也接不上。

- [x] 2b.1 台账写入 CLI：新增 `coverage_point_ledger/cli.py`（`python -m zhuopin_platform.coverage_point_ledger.cli transition --id <ID> --status <在途|已签认|已回灌> --fact-date <YYYY-MM-DD> --by <OP> [--letters 部门#N …] [--evidence <仓库根相对路径>] [--note …]`），唯一写盘仍经 `LedgerStore.append_many`；`--status 已签认` 缺 `--evidence` ⇒ exit≠0（D5 不开口子）；`recorded_on` 由 CLI 当场取本机时刻带 `+08:00`【CC】
  - ✅ `OP-0907-AK`（2026-09-07）：`5-平台底座/zhuopin_platform/zhuopin_platform/coverage_point_ledger/cli.py` ＋ 同目录 `letters.py`（信↔点判据），两个子命令 `transition`（写）／`points-by-letter`（只读，4bis 第一步）。**验证手段**：`pytest tests/test_coverage_point_ledger_cli.py` **18 passed**；CLI 实跑四条 —— 真台账 `points-by-letter --letter 财务部#17` ⇒ `[POINT] FI10-G-07 在途 财务域`、**exit=0**；`--letter 采购部#21` ⇒ `[NONE] 本信非台账投影，零回写`、**exit=0**；tmp 台账 `transition --status 已签认` 缺 `--evidence` ⇒ `[REJECTED] …D5…`、**exit=3**；补 `--evidence` 后同一条 ⇒ `[OK] FI10-G-07 → 已签认（fact_date=2026-09-12，recorded_on=2026-09-07T16:38:24+08:00）`、**exit=0**（事实日 ≠ 写入日，两者并存）。
  - 🔑 **多做了一个只读子命令 `points-by-letter`**（2b.1 原文只写 `transition`）：2b.2 要求 4bis「查五份域文件 `letters` 含该编号的点」，而拆件是**无头非交互班次**——没有这条命令，那一步只能靠会话自己 grep 五份 JSONL 再自行判断，判据就退回到模型判断。判据落 `letters.py::points_for_letter`，回件侧与发送侧共用**同一份**（否则又是「两份副本互相印证着一起错」，tasks 6.3 风险型指标那条）。
  - 🔴 退出码是判据、勿改语义：`0` 成功（**查到 0 个点也是 0** —— 非投影是正常形态，非零会让巡逻把正常信当故障重试）／`2` 用法错／`3` 台账拒绝（含 D5）／`4` 读取失败／`5` 定位不到台账目录。
- [x] 2b.2 拆件 skill §三 加 **4bis 台账回写**：对每封拆件的信 `部门#N`，查五份域文件 `letters` 含该编号的点；回件对该点已答 ⇒ `transition --status 已签认 --fact-date <回件落档日> --evidence <7-外部文档/<部门>/… 落档件>`；未答 ⇒ 保持 `在途`、报告点名；**无任何点引用该信 ⇒ 报告固定一句「本信非台账投影，零回写」（`采购部#21` 即此形态）**。源码件 `0-学习与工具/skills源码/huijian-chaijian-patrol/SKILL.md` 与副本 `0-学习与工具/定时任务源码/huijian-chaijian-patrol.SKILL.md` 同步改，安装件位置由 `0-学习与工具/工具-定时任务源码备份.py` 取、不猜路径【CC】
  - ✅ `OP-0907-AK`（2026-09-07）：**两份同步改**（`git diff --numstat` 各 `17 0`，插入式改动、无重写）——源码件 `0-学习与工具/skills源码/huijian-chaijian-patrol/SKILL.md` ＋ 副本 `0-学习与工具/定时任务源码/huijian-chaijian-patrol.SKILL.md`，两份内容逐字节相同（`diff --strip-trailing-cr` 无输出）。4bis 插在 §三.4「回灌与销行」与 §三.5「需 Shao Peishen 项」之间，五小步：⑴ `points-by-letter` 只读查点 ⑵ `[NONE]` ⇒ 报固定一句、**本步结束不写台账**（并明写「不得因为查不到点就去建点」）⑶ 有点则逐点判已答／未答／新口径三分支 ⑷ 非 0 退出码照抄进报告、**不重试不改参数绕过** ⑸ 报告加一行「台账回写：已签认 N 点／仍在途 M 点」。
  - 🔴 **安装件未改，本条只改了仓库内两份**：安装件位置由 `0-学习与工具/工具-定时任务源码备份.py::DEFAULT_SOURCE_DIR` 取得 ＝ `~/Claude/Scheduled/huijian-chaijian-patrol/SKILL.md`（不猜路径），改动前已核**它与镜像逐字节一致**。派单件「不做什么」明令**不注册／改定时任务**，而回镜方向是单向的（真身→镜像，「改镜像不生效」）⇒ **4bis 要真跑起来，须把新版拷进安装件**，这一步留给 Shao Peishen／总线，见收工报告待办第 1 项。
- [x] 2b.3 发送侧接线：跟进信发出成功后自动 `transition --status 在途 --letters 部门#N`（今日 `财务部#17` 的 `在途` 两行是 `OP-0907-L` 手写脚本补的，不是机制）；挂在发送脚本发出成功之后，触碰文件由 CC 定位，🔴 避开在途泳道文件 `0-学习与工具/工具-跟进闸查询.py`（`#482`）与 `0-学习与工具/工具-跟进信README登记.py`（`#447`）【CC】
  - ✅ `OP-0907-AK`（2026-09-07）：新增 `5-平台底座/wecom-aibot-service/aibot_service/ledger_writeback.py`，挂在 `aibot_service/delivery.py::push_followup` **返回前**（主送→回填→两处抄送全部尘埃落定之后）。`push_followup` 是 `scripts/push_followup_letter.py`（手动单封）与 `aibot_service/dispatch.py::dispatch_followup_letters`（批量）**共同**的发送核心，挂一处两条路都覆盖。**验证手段**：`pytest tests/test_ledger_writeback.py` **17 passed**（全程 mock `fakes.fake_client_factory`，不连企微、不发任何真实消息）；回归 `pytest tests/`（wecom-aibot-service）**812 passed, 1 skipped**、`pytest tests/`（zhuopin_platform）**553 passed, 1 skipped**，零回归。
  - **触碰文件**（🔴 已避开在途泳道两文件，`工具-跟进闸查询.py`／`工具-跟进信README登记.py` **一字未动**）：`aibot_service/ledger_writeback.py`（新）、`aibot_service/delivery.py`（＋import ＋返回前一段 try/except ＋ `timedelta` 补进 datetime import）、`tests/test_ledger_writeback.py`（新）。信编号解析复用 `zhuopin_platform/shared_tools/followup_gate.py::parse_letter_number`（**只读 import，该文件未改**）——全项目唯一一份「信编号长什么样」的实现，不另写正则。
  - 🔑 **「这封信投影哪些点」的两个来源（2b.3 原文未指定，此处如实登记本方裁法）**：⑴ 台账侧已连线 ＝ 点的 `letters` 里已出现同一个 `部门#N`（起草期连的线，真身实例 `FI10-G-07` 的 `财务部#17（待你审，暂不占号）`，**括注一律归一化容忍**——字面量比较会把它与干净的 `财务部#17` 判成两封信，且判错后的外观与「不投影」一模一样）；⑵ 信侧自陈 ＝ 信 md frontmatter `口径点:`／`配套:` 里反引号引起来的 id（真身实例 `财务部#17` 的 `配套:` 就写着 `` `FI10-G-07` ``）。🔴 **两个来源都与 `Snapshot.points` 求交**，故「`配套:` 自由文本里解析出噪声」最坏只是没命中、**不会凭空建出一个点**（有断言：`test_2b3_信侧自陈了一个台账里不存在的点则不写`）。
  - 🔴 **只推一步 `待问`→`在途`**：已 `在途` 跳过（重推不产生第二行 `在途`）；`已签认` 及以后跳过（拖回 `在途` 是无因倒退，凑一个 `note` 出来的理由是假的）。`fact_date` ＝ 发出日、`recorded_on` ＝ 写入时刻，两者分开取。
  - 🔴 **失败一律不上抛**：回写异常由 `push_followup` 捕获记审计 `followup_ledger_writeback_failed`，与 `cc_to_paul`／`cc_group` 同一隔离模式——台账没写上可事后补（`cli transition` 就是那条路），把一次成功的发送报成失败、让下一班当「待发」重发才是不可挽回的（断言 `test_2b3_台账回写失败不影响发送已成功这个事实`）。**「不投影任何点」也写审计**（`non_projection: true`）：否则它与「这一步压根没跑」在审计里长得一样，正是本次要补的那个洞的形状。
  - ⚠️ **如实登记一条限制（不是缺陷、是输入侧还差一步）**：来源⑴ 要求起草期就把 `部门#N` 写进点的 `letters`，来源⑵ 要求信 frontmatter 写 `口径点:`／`配套:` 带点 id。今日真身 `财务部#17` **走得通来源⑵**（已实跑核对 `配套:` 字段确含 `` `FI10-G-07` ``）；但**起草侧尚无机器守强制这两者之一**（近 27 封 `决策点:` 覆盖率为 0 是同源问题，见 §5.3）。⇒ 今天新起草的信若两者都不写，本接线会安静地判为「非投影」（有审计留痕、可查）。补法属起草侧，**归 §5.3 与 2b.5**，本件不越界处理。
- [x] 2b.4 🔴 断言测试两组：⑴ 负样本＝非投影信跑 4bis 逻辑 ⇒ 五份域文件字节数前后相等；⑵ 正样本＝`FI10-G-07` 模拟回件 ⇒ 恰一行 `已签认` 且带 `evidence`，`fact_date`＝回件日而非写入日；两组各配反向对照【CC】
  - ✅ `OP-0907-AK`（2026-09-07）：`5-平台底座/zhuopin_platform/tests/test_coverage_point_ledger_writeback.py`（回件侧 4bis 两组）＋ `5-平台底座/wecom-aibot-service/tests/test_ledger_writeback.py`（发送侧）。**验证手段**：`pytest tests/test_coverage_point_ledger_writeback.py` **7 passed**、`pytest tests/test_ledger_writeback.py` **17 passed**、`pytest tests/test_coverage_point_ledger_cli.py` **18 passed**。
  - ⑴ **负样本**（`test_2b4_负样本_非投影信跑4bis_五份域文件字节数不变`）：`采购部#21` 走完 4bis ⇒ 五份域文件**字节数逐份相等**、退出码 0、输出含固定一句。**反向对照**＝ `test_2b4_负样本反向对照_投影信跑同一段逻辑字节数必须变`：换成 `财务部#17` 跑同一段逻辑，财务域**必须**变大、且**其余四份必须一字节不变**（写串域即 OEM 隔离越界）——没有这条，「字节数不变」可能只是因为这段逻辑什么都不做。
  - ⑵ **正样本**（`test_2b4_正样本_模拟回件恰一行已签认且带evidence_事实日是回件日`）：`FI10-G-07` 模拟回件 ⇒ 全库 `已签认` **恰一行**（不是「至少一行」）、带 `evidence` 且值就是传入的落档件、`fact_date` ＝ 回件落档日、`recorded_on` ＝ 今天，并断言**两者不相等**。回件日取 `2023-05-18` 这个绝不可能等于运行日的过去日期，使「事实日 ≠ 写入日」**无条件成立**，而不是「今天恰好不是那一天」才成立。**反向对照**＝ `test_2b4_正样本反向对照_同一回件缺evidence则拒且全库无已签认`：唯一变量是 `--evidence` 有无 ⇒ 退出码 3、字节数不变、全库找不出 `已签认`、点仍 `在途`。
  - 🔑 **两组都逐字调 SKILL.md §三 4bis 里写的那两条命令**（经 `cli.main`，与无头拆件会话跑的是同一段代码），不另写一份「测试专用的 4bis 逻辑」——那样测的是测试自己，不是那个会自动跑起来的东西。
  - 📌 **幂等边界如实登记**（`test_2b4_正样本_已签认后重复跑一次不产生第二行`，断言的是**会**产生第二行）：台账是事件账，同一条命令跑两次就是记下两次事件。写成断言是为了让这件事**被看见** —— 4bis 的调用方须按信只跑一次，重跑先看 `points-by-letter` 的当前状态。
- [ ] 2b.5 §6.2③ 全链验收改由真实 `财务部#17` 回件触发：回件到 → 拆件 4bis 自动回写 → 报告见「已签认 N 点」；未走通不勾 §6.2③【Cowork ＋ 他】

## 3. 度量派生

- [ ] 3.1 「口径点提出 → 销点」中位天数脚本：**逐份读、内存聚合**，不落盘（D3）【CC】
- [ ] 3.2 与现行信级代理指标同期并跑做对照【CC】
- [ ] 3.3 🔴 **断言测试：`提出日`／各状态转换日一旦写入，不得被后续补记覆盖**【CC】
  - 成因（本包最硬的一条实证）：2026-08-23 有 12 封信被批量「补转态」，**其真实回件日在补记那一刻被覆盖式销毁、不可复原**。度量中位数因此由 2 天变成 7 天，而**底层事实一天没变**。⇒ 没有台账，历史事实会被**不可逆销毁**，而不只是算不准；且**销毁不产生任何信号**。
  - 补记 SHALL 另立字段与事实日并存；事实日不可考则标 `事实日未知`，**MUST NOT 用补记日顶替**，度量时单独列出、不混入中位数。

## 4. 闸取数源切换（晋档 3）

- [ ] 4.1 `0-学习与工具/工具-跟进闸查询.py` 增加「从台账取数」通路，**与现行直读 README 双路并跑**，不切换默认【CC】
- [ ] 4.2 🔴 **双路并跑 ≥28 天（4 周）**（D4），逐轮记录差异【CC】
  - 成因：实测 35 封闭环信往返周期 **P90 ＝ 26 天**，上取整 4 周。草案「≥1 个完整往返周期」有歧义——按中位 7 天理解则**慢的那一半信从未被双路同时验证过**，切换后才暴露就是生产事故。
- [ ] 4.3 差异归零后切换，旧路降为对照【CC】

## 5. README 降为派生视图

- [ ] 5.1 「编号」段与「编号」列改由台账 `id` 派生（继续显示、不再是判据来源），🔴 **不删除**【Cowork】
- [ ] 5.2 ⚠️ **只降编号段，`发送状态` 列一字不动** —— 那是 2026-08-21 §四 #85 答 (b) 确立的「一封信状态的唯一权威源」，属**信级**；本包管**点级**。**不得借本包之机同时动两个粒度的口径**，否则出问题无法定位是哪一个引起的。信级调整属 R2【Cowork】
- [ ] 5.3 恢复 frontmatter `决策点:` 字段的写入纪律【Cowork ＋ 总线派发】
  - 🔑 **该动作不依赖本包落地**：下一封跟进信起就写，成本接近零，且**立刻停止继续失血**（覆盖率 8/11 起已连续 27 封为 0%）。

## 6. 验收与晋档

- [ ] 6.1 **本包交付 ＝ 档 2（真实数据跑通）**：台账建成并回溯拆点后即在真实数据上运行【Cowork】
- [ ] 6.2 晋档 3 四条件（逐条核，缺一不晋）：① 未闭环 21 封拆点完成且他签认；② 闸查询新旧两路**并跑满 28 天**后输出对齐；③ **至少一封新信完整走通「台账 → 投影生成判例包 → 发出 → 回件解析 → 回写台账」全链**；④ 度量脚本与代理指标同期并跑做对照【他 ＋ Cowork】
  - 🔴 **③ 是本包的真验收点** —— 只建台账、不走通投影链，等同 `#382` 那次「只验消费端不验产生端」。**台账建成 ≠ 信是它的投影。**
- [ ] 6.3 价值指标基线由 Shao Peishen 在启动前确认存档：质量型＝「承接载体缺失」数量（首次全量扫描定基线，目标归零）；风险型＝「编号/状态两份副本互相印证着一起错」复发次数，**基线 6 次，目标 0**【他】

## 7. 收口

- [ ] 7.1 队列 `#439` 登记与 §二 批次回写【Cowork】
- [ ] 7.2 本包归档 `/opsx:archive coverage-point-ledger -y`（完工即归档纪律）【Cowork/CC】

## 8. 已知未闭合（如实登记，非本期范围）

- [ ] 8.1 **R2（串行闸改按点计）不在本包** —— 它硬依赖本包，另立变更包。**本包不动串行闸口径。**
- [ ] 8.2 ⚠️ **不据本包结论重开信级回退闸** —— 2026-08-24 那次闸评估判「达标有余（中位 2 天）」，用同一套方法今日重算是 **7 天**，恰卡在目标 ≤7 天边缘；但**变的只是账补了一部分**，属信级度量的账（R2 范围），本包不处置。
