## 1. Design 审核（阻塞后续所有任务）

- [x] 1.1 提交 design.md 四个决策点给 Shao Peishen 审核（可直接回复 (a)/(b) 或指定其它方案；未回应按 design.md 已标注的默认项执行）—— 2026-08-05 经业务总线 session 批准，四点全部 (a)，附验收硬要求（见 design.md「审批记录」）
- [x] 1.2 记录审核结果（批准的方案组合）到本变更包 —— design.md 已追加「审批记录」小节；队列 §一 #236(1) 行待下一 apply session 一并回填完成状态（本次仅记录批准结果，未启动 apply 实施）

## 2. 协议文本改写

- [ ] 2.1 改写协议〇.1（"开工"）：认领任务时须在 §二 登记预登记批次行，先于任何实质改动
- [ ] 2.2 改写协议〇.3（"收工"）：收工时回到预登记批次行做精确化（文件清单/commit message/状态），不新增第二行
- [ ] 2.3 明确预登记批次的状态列文本约定（如"在办（预登记，收工时精确化）"），确保不含"待"字样、不被 sweep 现有判据误处理
- [ ] 2.4 `专线opener模板库.md` 补一句"认领即预登记"的口令指针
- [ ] 2.5 若决策点 4 批准为 (a)（默认）：在协议〇.7 编辑锁段落明确"预登记内容"适用其既有降级路径（锁占用时记入域接力文件⏳节）

## 3. 工具化能力（仅当决策点 3 批准为 (b) 时执行；若批准为 (a) 默认，跳过本节，转 4）

- [ ] 3.1 `工具-共享文档编辑锁.py` 的 `acquire` 新增可选预登记参数（如 `--preregister-batch <batch_id> <文件集合>`），在获取锁的同一原子操作内写入预登记批次行
- [ ] 3.2 新增/更新 `test_工具-共享文档编辑锁.py` 单测覆盖：正常预登记写入、锁占用时降级、与既有 `acquire` 无参数调用路径不冲突
- [ ] 3.3 CLI 端到端验证一次真实预登记调用

## 4. 陈旧预登记批次检测（仅当决策点 2 批准为 (a) 默认时执行；若批准为 (b)，改为新建独立检测脚本，任务改写）

- [ ] 4.1 `zhuopin-queue-audit` skill 源码新增检测项：状态为"在办（预登记，收工时精确化）"且登记时长超过阈值（默认 24 小时，随批准结果可调）的行。**🔴 验收硬要求（Shao Peishen 2026-08-05 批准时补充）**：skill 源码与本变更包须明确写清该检测在值周巡检整体流程中的具体挂载位置（如"排在对账审计之后、生成《本周计划》之前"）与精确触发判据（状态文案匹配逻辑，含未来状态文案措辞变化时如何仍能识别），不得只写"新增一项检查"——同 #219／#204 教训，规则成文但无具体挂载点/触发条件即等同无载体
- [ ] 4.2 检测结果纳入值周巡检《本周计划》生成逻辑的提示段，并在本变更包内注明该提示段在《本周计划》文档结构中的具体位置
- [ ] 4.3 （若决策点 2 批准为 (b)）新建独立检测脚本+状态持久化+webhook 通道，复用 `_load_webhook_url`/`_send_wecom_markdown` 现成基础设施；对应单测覆盖首次检测/阈值内不重复/阈值外再提醒

## 5. 验收与收尾

- [ ] 5.1 全量回归（若第 3 节有代码改动：`test_工具-共享文档编辑锁.py`）零漂移
- [ ] 5.2 至少一次真实认领场景验证"预登记 → 收工精确化"全流程无阻塞
- [ ] 5.3 队列 §一 #236 行回填 (1) 完成状态与产出路径
- [ ] 5.4 `/opsx:archive` 归档本变更包
- [ ] 5.5 收工重跑文档台账
