# Tasks — aibot-inbound-whitelist-li-jiaolong

> **状态：propose 已成稿，🔴 design 审未过，全部 apply 项未开工。**
> 本包命中根 `CLAUDE.md` §5 门槛 ②（鉴权与数据可见性）与 ③（改变既有模块对外语义），按「不跳步」停在 propose，**未预写任何代码**。

## 0. 前置：Shao Peishen design 审（🔴 唯一阻塞项）

- [ ] 0.1 **决策点 1**：入站准入判据取 (甲) 逐人拍板 ／ (乙) 出站即入站 ／ (丙) 按名录部门整段开通 —— 见 `design.md`。**注意：三者对「本次实际放行谁」结果相同（只有李姣龙一人），差别在今后默认**
- [ ] 0.2 **决策点 2**：解植雅（`2025621`，采购部，2026-07-20 已被静默挡回一次）取 (a) 不动 ／ (b) 一并补入 ／ (c) 只补可见性不放行
- [ ] 0.3 **决策点 3**：`whitelist_rejected` 补独立通道告警 —— 是否批准（与 0.1／0.2 解耦，可单独批）

## 1. 入站放行（design 审通过后）

- [ ] 1.1 `whitelist.py::WHITELISTED_SENDER_USERIDS` 增 `"2025672"`（李姣龙，财务部）
- [ ] 1.2 `whitelist.py` 顶部注释补一段成因（同既有陈承段体例）：出站已通而入站不通、`#379` 年度提醒收件人即她、`2025672` 系纯数字工号不可推断
- [ ] 1.3 `department_mapping.yaml` 增 `"2025672": 财务部`（按 `"2023458": IT` 既有写法加引号）
- [ ] 1.4 **`intake.py` 不改**——`DEPARTMENT_TO_QUEUE_OWNER` 已含 `"财务部": "财务专线"`（核验，非改动）

## 2. 单测

- [ ] 2.1 `is_whitelisted("2025672")` 为真；既有 6 项一字不动
- [ ] 2.2 `resolve_department("2025672", mapping)` == `财务部`（覆盖 YAML 数字键经 `str()` 归一后仍可查中）
- [ ] 2.3 **不变式测**：白名单与部门映射两表求差——不得存在「在白名单、不在部门映射」的 userid（`PAUL_USERID` 按既有口径豁免，须在测里显式标注豁免而非默默跳过）
- [ ] 2.4 若 0.3 获批：`whitelist_rejected` 触发告警、告警不含正文、告警失败不吞审计
- [ ] 2.5 服务全量回归（基线：487 passed）＋ 平台全量（基线：380 passed），零漂移

## 3. 归档链路验证（本机可做，不需真人）

- [ ] 3.1 以 `2025672` 构造入站帧，验证归档落 `7-外部文档/财务部/` 而非 `待分拣/`
- [ ] 3.2 验证追加的队列行 owner ＝ `财务专线`，且**不含**「发送人身份待确认」标注

## 4. 🔴 真实发送冒烟（对外动作 —— 本 session 留步，未做）

- [ ] 4.1 **向李姣龙本人真实发送一条企微消息**
- [ ] 4.2 **验收判据（原样承自队列 `#380`，不得放宽）**：**只看命令返回码不算数，须在 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl` 见到 `errcode=0` 才算通**
- [ ] 4.3 若 1.x 已 apply，一并验回件方向：她回一句话后不再触发 `whitelist_rejected`，而是正常归档

> **留步原因**：向真人发企微消息属对外动作，本 session 为无人值守批处理，按纪律不发。**A20 重启后出站配置已确认生效于运行码**（见 `proposal.md` 复核表），故本项一旦有人在场即可直接执行，无技术前置。

## 5. 收口

- [ ] 5.1 回写队列 `#380`（本 session 已完成 propose 段回写；apply 完成后需再回写一次）
- [ ] 5.2 `openspec archive` 本变更
- [ ] 5.3 `#379` 年度提醒 09-01 首触发前确认 4.x 已闭合
