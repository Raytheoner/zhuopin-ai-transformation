# lane-watch-mode Tasks

> ✅ **design 已于 2026-09-02 审定并转 `status: 生效`（D1–D7），§2 起可开工。** 执行环境标注见各条。
> 队列 §一 `#452`｜起草 `OP-0902-A`（2026-09-02）

## 1. design 审

- [x] 1.1 ✅ **2026-09-02 Shao Peishen 审定三条未决**：① 触发词用原词「开启泳道看护」，**并加备用触发＝每工作日 15:00 定时启动**（他原话「如果不能事件触发或不合适，每天下午三点启动」）⇒ 落 D5；② 收工汇总**加**「本批停了几次、各停在哪」列 ⇒ 落 D6；③ 首跑灰度**直接到 6**、不走灰度 2 ⇒ 落 D7（保留「他在场」一条）。【他】
- [x] 1.2 ✅ `design.md` frontmatter 已转 `status: 生效`（2026-09-02，`OP-0902-A`）。⚠️ 如实记：他未另说「审核通过」四字，系据其对全部未决项的逐条明确答复转生效，件内已写明「若与本意不符请当场退回」。【Cowork】
- [x] 1.4 🆕 **补：`openspec validate --strict` 缺口修复（2026-09-02 `OP-0902-C` 发现并补齐，本项不在原任务书编号内，如实登记）** —— 本包自立行起从未有 `specs/` 目录（`proposal.md`/`design.md`/`tasks.md` 三件齐，唯独缺 delta spec），`openspec validate lane-watch-mode --strict` 实测报错「Change must have at least one delta」。已补 `specs/lane-watch/spec.md`（含 `## Purpose` ＋ 7 条 `## ADDED Requirements`，覆盖 D1 四档／⏭️ 转出语义／LAN 探针 fail-safe／D5 超时收回／5.6 看门狗／D6 汇总完整性／🔴 永不自动执行对外发送与生产部署），复核 `validate --strict` 转绿。**理由**：本包 §6/§7（首跑验收/归档）不在本次范围，但若不补这道缺口，包本身处于"将来无法合法归档"的隐藏坏状态，且本次动手改动了本包大量行为（D1 四档、3.5、5.6），理应一并把 spec 契约补齐，不留给下一棒发现。【CC】

## 2. skill 本体

- [x] 2.1 ✅ 定稿 `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（2026-09-02 `OP-0902-C`，v2.0 重写：`OP-0902-B` 出件的 v1.0「三模式并存」框架已作废；v2.0 改为「两个 workflow 怎么选」二行对照表，opener 正文四句约定新增 ⏭️ 转出处置、5.6 看门狗轮询指令，D6 收工汇总新增「本批转出 N 项」一行。沿革详见同目录 `CHANGELOG.md`）【Cowork】
- [x] 2.2 ✅ **已安装（2026-09-02，Cowork `OP-0902-A`）** —— `save_skill` 装引用式指针 `zhuopin-lane-watch`（`skill_01GsmGMFZdgiXknSA3FKeqch`），骨架沿 `zhuopin-lane-clearpool` 既有引用式装载器：**不承载规则正文**，只指向 `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（执行编排正本）与 `openspec/changes/lane-watch-mode/design.md`（上位判据正本），并留四条「不读正本也必须记住的红线」＋「读不到就停下、不要凭印象跑一整批」。**理由同 clearpool/kickoff-prompt v2.0：save_skill 要求整份 passthrough，长文复述必然静默漂移；此后规则只改仓库文件，本指针永不需要同步。**【Cowork】
- [x] 2.3 ✅ **指针回读验证通过（2026-09-02）** —— 安装后 `zhuopin-lane-watch` 即出现在会话可用 skill 清单；description 触发词逐字核对：主触发「开启泳道看护」✅、别名「offlan清池」「off-LAN 清池」「周末清池」✅（5.2 承接清池触发词）、四档语义（🟢/🟡/⏭️/🔴）与 D1 一致 ✅、与 `zhuopin-lan-closeout` 的分工句 ✅。【Cowork】

## 3. 停/续状态机（本包唯一的新建代码）

- [x] 3.1 ✅ 续跑状态文件设计与实现：`reports/lane-watch-state.json`（批次／波次／泳道／在等什么／选项／他答了什么）【CC】
  - 🔴 **必须落在 `reports/` 下** —— proposal「伴生文件」节已实测 `**/reports/` 覆盖（`git check-ignore -v` 结果见彼），2026-09-02 复核仍命中，结论未变。
- [x] 3.2 ✅ 决策点拦截：泳道 opener 内置 D1 **四档**判据（2026-09-02 `OP-0902-C` 按新 design 改写——`工具-泳道看护状态机.py` 新增 `TIER_TRANSFER`/`TRANSFER_ACTIONS`，`YELLOW_ACTIONS` 移除 `deploy_51` 并重新编号 ①-④ 与 design.md D1 表逐字对齐），命中 🟡/🔴 即停并落状态、命中 ⏭️ 即转出不进问答【CC】
- [x] 3.3 ✅ 续跑入口：Cowork 读状态文件 → 起下一段泳道（SKILL.md v2.0 步骤 5 已写明具体命令与流程）【Cowork/CC】
- [x] 3.4 ✅ 🔴 **判据未覆盖时按 🟡 停**（fail-safe），且续跑请求里须问「本动作该进哪一档」，答复回灌 D1 表——`classify()` 未命中任何已知表时恒 fail-safe 到 🟡，新增的 ⏭️ 档不参与 fail-safe（避免未知动作被误判为"可转出不用问"）【CC】
- [x] 3.5 ✅ 🆕 **开跑先跑 LAN 探针，据结果决定候选范围**（2026-09-02 收敛后新增，`OP-0902-C` 实现）：新增 `lan_status()`/`lan-status` CLI 子命令，只读引用 `工具-未闭合产出扫描.py::probe_lan`（不复制 ping/HTTP 判据代码）。on-LAN ⇒ 全部可领活（`.51` 项除外，走 D1 第四档转出）；off-LAN 或探针不过 ⇒ 按 off-LAN 处理（fail-safe），排除依赖内网的活、其余照跑、被排除项列入收工汇总「待回内网」清单（沿清池三清单既有惯例，SKILL.md 已写明）。【CC】

## 4. 接线（复用既有件，不新建）

- [x] 4.1 ✅ 只读核对 `工具-opener批处理执行v2.ps1` 调用签名与无头 CC 参数（**不改它**）——2026-09-02 复核：该脚本自 `OP-0902-B` 核对后未再变动（master 后续提交均为队列文档类），`OPENER_PARTIAL` 不触发中断逻辑的结论仍成立【Cowork/CC 只读】
- [x] 4.2 ✅ 企微"等人"事件接入既有四类事件推送通道——`pause`/`transfer-out`（新增"转出"通知）/`check-heartbeat`（新增"看门狗"通知）均已复用 `_load_wecom_sender`/`_notify_best_effort`，推送失败降级仅落状态【CC】
- [x] 4.3 ✅ 触碰区排波复用清池步骤 2 实现，只读引用不复制代码——SKILL.md v2.0 步骤 1 已注明「`zhuopin-lane-clearpool/SKILL.md` 步骤 1-2 虽已退休为独立触发入口，判据文本仍是本步骤的参照」【CC】
- [~] 4.4 ❌ **本项已否掉，不做**（2026-09-02 同日 Shao Peishen 二次审定：「定时任务不合适，事件驱动以后再说，现在还是用『开启泳道看护』手动触发」）。原内容＝备用触发接线每工作日 15:00 定时启动。**⇒ 本包本期只有手动触发一种入口。** 🔑 他的改判与 D5 里那个前提冲突分析一致：定时触发的根本问题不是技术实现，而是它与「他在场」这个设计前提相矛盾——定时班次触发时他大概率不在，跑出来只有 🟢 档、🟡 档全堆成待答清单，**那样它就退化成一个"会自动做杂活的清池"，不是他要的模式**。
- [x] 4.5 ✅ 🔴 **待答超时兜底（D5 解法 3）**：任一泳道停在决策点超 **4 小时**未获答复 ⇒ 降回原状态、注明"因超时未获答复而收回"、并入当日收工汇总——`check_timeouts` 对 D1 四档与看门狗（🐕）触发的 paused 态一视同仁（同一 `status=="paused"` 判据），无需为新增的 ⏭️/🐕 分别改写【CC】
  - 成因（2026-09-02 定时触发否掉后改写）：**他触发后完全可能中途离开工位**；无限挂着的泳道会持续占用 worktree 与触碰区，让别的 session 抢不到它的活。降回原状态是无损的——他回来仍可重新触发。**⇒ 本项不随 4.4 一同否掉，手动触发下依然需要。**

## 5. 🔴 清池退休与职能吸收（2026-09-02 架构收敛后整节重写；原「边界收窄」三条已作废）

- [x] 5.1 ✅ **把 clearpool 的职能整体吸收进本包**：D1 白名单、触碰区排波、看护件落档、心跳看护逐项对照迁入，**不是删掉是承接**（SKILL.md v2.0「与 `zhuopin-lan-closeout` 如何分工」表 ＋ 步骤 1-6 已逐项写明承接方式）；唯一不迁的是「遇决策绕开不入批」——它正是错误前提（「清池＝人不在」）的产物【Cowork】
- [x] 5.2 ✅ **触发词「offlan清池」保留为本包别名**（他习惯了，不强制改口），与「开启泳道看护」指向同一 skill（SKILL.md v2.0「触发与前置」已写明）【Cowork】
- [x] 5.3 ✅ `zhuopin-lane-clearpool` 退休：`SKILL.md` frontmatter `status`/`description` 已改为指向本包，正文顶部加退休说明横幅，正文其余部分按「历史记录不追改」原样保留（作本包步骤 1-2 判据引用来源）【Cowork】
  - 🔴 **退休前已确认 clearpool 的 openspec 变更包 `lane-clearpool-skill` 的处置** —— 其 4 项未完成（2.1/2.2/2.3/3.3）已于 5bis 逐项对照承接（2.1→本包 4.1／2.2→本包 4.2／2.3→本包 5.6／3.3→本包 5.7），该包 `tasks.md` 已同步标注 🔁 转承接，`proposal.md` 已补退休说明尾注，**未让一个未归档的包连同其 skill 静默消失**。
- [x] 5.4 ✅ LAN 收口 `description` 补边界指向：本 skill 专管 `.51` 部署与 LAN 留步，其余走 `zhuopin-lane-watch`（`zhuopin-lan-closeout/SKILL.md` frontmatter 已补一句；同目录 `CHANGELOG.md` 已记录）【Cowork】
- [x] 5.5 ✅ 🔴 **LAN 收口正本的红线段一字不动** —— 只改 description 的适用场景描述，不动规则（D1/D2/D3 三节正文原样保留，逐字核对未变）【Cowork】

### 5bis · 承接 `lane-clearpool-skill` 变更包的 4 项未完成（2026-09-02 Shao Peishen 答 (a) 定）

🔴 **不能让一个未归档的变更包连同它的 skill 一起静默消失** —— 那正是本项目反复吃亏的那类事。逐项对照后**实际只需新增两项**，另两项已被本包既有 task 覆盖：

| clearpool 原 task | 处置 |
|---|---|
| 2.1 核对 `工具-opener批处理执行v2.ps1` 调用签名与无头 CC 参数（只读） | ✅ **已被本包 4.1 覆盖**，同一件事，不重复列 |
| 2.2 心跳推企微段接线 | ✅ **已被本包 4.2 覆盖**（企微"等人"事件接入四类推送通道） |
| 2.3 波间自动推进与 30 分钟无心跳暂停的看护逻辑 | ⬇️ **新增 5.6 承接** |
| 3.3 队列 `#312` 子项回写 ＋ 本包归档 | ⬇️ **新增 5.7 承接** |

- [x] 5.6 ✅ **波间自动推进与看门狗**（承接 clearpool 2.3）：**任一泳道 30 分钟无心跳 → 暂停后续波 ＋ 告警 ＋ 等人**（2026-09-02 `OP-0902-C`，`工具-泳道看护状态机.py::check_heartbeat`/`check-heartbeat` CLI 子命令实现，`tier` 标 🐕 与 D1 四档区分；已在 `paused` 态的泳道不重复触发）【CC】
  - ⚠️ **与本包 D3 的关系**：D3 管「单项失败只停该泳道、其余继续」；本项管「**无心跳**」——那是失联不是失败，**分不清它是在跑长回归还是真挂了**，故一律暂停等人。两条不冲突，分别覆盖「跑失败」与「不吭声」。
  - 📌 clearpool `#382` 行内已留一条相关判据：**预计等待超过 10 分钟的，等待期内要再写一行「仍在等 X，预计还要 N 分钟」**，否则 10 分钟判读线在长回归面前会常态性失效。**已落**（SKILL.md v2.0 步骤 4 心跳约定已写明此提示行的写法与成因）。
  - 「波次跑完自动推进下一波」是 Cowork 侧编排语义（沿用批处理脚本既有的串行/并行调度，不新增代码），未新建独立实现，与清池既有做法一致。
- [x] 5.7 ✅ **两项均已完成，2026-09-02 `OP-0902-A` 逐项核实后勾（非采信自述）**：⑴ **包归档**——`openspec/changes/archive/2026-09-02-lane-clearpool-skill/` 实测在，且 `tasks.md` 顶部已按「不得只标归档不写去向」写明退休去向（「本包 skill 已由 `zhuopin-lane-watch` 吸收退休（架构收敛，见 `lane-watch-mode/design.md`「架构收敛：三个 workflow → 两个」节）」）；⑵ **队列 `#312` 子项回写**——`OP-0902-C` 已做，证据＝§二 `B-0902_13_OP0902C_452架构收敛重派完成` 文件清单明列「§一 `#312` offlan清池子项回写退休说明」。⚠️ **另如实记一处（非本包范围）**：`#312` 现为 `[S:blocked]`，卡在「他打开看板读『可 Open 池』的 N 与行号、与 22 条比对」，其**预期观察窗口 3 天至 2026-09-02 今日到期**——剩余唯一动作在 Shao Peishen 手上，本包不处置。【Cowork/CC】
  - 🔴 归档时须在包内写明「**本包 skill 已由 `lane-watch-mode` 吸收退休，剩余项已于 2026-09-02 转承接，见该包 tasks 5bis**」，**不得只标归档不写去向**。
  - **进度（2026-09-02 `OP-0902-C`）**：`lane-clearpool-skill` 的 `tasks.md`（4 项标 🔁 转承接）与 `proposal.md`（退休说明尾注，写明成因与去向）**已完成**；队列 `#312`「offlan清池」子项回写**因编辑锁被 Cowork-业务总线-OP0902 占用（§四 #150／B-0902_3 批次）未能即时完成**，将在锁释放后本会话内补做，或如仍未完成则在收工回写 `#452` 时如实登记待补，不冒充已完成；`openspec archive` 归档命令视 Cowork/CC 分工与本会话工具可用性执行。

## 6. 首跑验收

- [ ] 6.1 首跑 **上限 6**（D7：2026-09-02 Shao Peishen 审定直接到 6、不走灰度 2）＋ **Shao Peishen 在场**【他＋CC】
  - 🔴 **与 clearpool／lan-closeout 两包既例不同（那两包首跑均强制 2），可跳过的三条理由见 design D7**：本包 🟢 档全在 worktree 内隔离／不碰 `.51`·真实库·对外发送（前两包强制 2 的成因正是它们碰生产面）／一切不可逆动作都在 🟡🔴 档会停下来问他。
  - ⚠️ **「他在场」这一条保留，且不是为了限条数**——是为了让 D1 判据表在首次真实碰撞时有人当场改判。
- [ ] 6.2 🔴 **至少命中一次 🟡 档决策点并成功续跑** —— **只跑通 🟢 档不算验收通过**。
  - 成因（写死在此，防重蹈）：`#382` 事件驱动那次，只验了消费端（`check_patrol_signal.py` 返回 `[NO-SIGNAL]`）就判"空巡验活通过"，**而那条路径根本不经过桥一**；产生端一次都没验，结果链路建成后从未真实触发过一次。**停/续机制之于本包，等同产生端之于 `#382`——它才是新增的那部分。**
- [ ] 6.3 三判据全过方摘灰度：**误停 0**（不该停的没停）／**漏停 0**（该停的都停了）／**每次停都有可读的"在等什么"落档**【Cowork】
- [ ] 6.4 触碰区防撞零越界（沿清池首跑判据）【Cowork】

## 7. 收口

- [ ] 7.1 队列 `#452` 回写（判据→证据→行内状态处置）＋ 登记 §二 批次【Cowork】
- [ ] 7.2 本包归档 `/opsx:archive lane-watch-mode -y`（完工即归档纪律，不拖到下次 session）【Cowork/CC】

## 8. 已知未闭合（如实登记，非本期范围）

- [ ] 8.1 **企微侧回复→续跑桥未做**。Shao Peishen 2026-09-02 答 ④ 选 (b)，但当场明示「记下以后做」⇒ 本期实现 (a)：企微只推通知，答复须回到 Cowork。
  ⚠️ **风险预登记**：那是又一条要建的链路，**它自己也会有"死链"风险**——同 `#382`（建成、接线、开关全开，却因常驻进程未重启而从未真实触发）。**做它之前先看 `#382` 的验活结论**。
- [ ] 8.2 **D1 表的完备性无法在首跑证明**。三档判据覆盖的是当前已知动作类型；真实运行中必然出现未覆盖的新动作，靠 3.4 的 fail-safe（按 🟡 停）兜住并逐条回灌。**⇒ 本表是活的，不是一次定稿。**
