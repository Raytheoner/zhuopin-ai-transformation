# lane-watch-mode Tasks

> ✅ **design 已于 2026-09-02 审定并转 `status: 生效`（D1–D7），§2 起可开工。** 执行环境标注见各条。
> 队列 §一 `#452`｜起草 `OP-0902-A`（2026-09-02）

## 1. design 审

- [x] 1.1 ✅ **2026-09-02 Shao Peishen 审定三条未决**：① 触发词用原词「开启泳道看护」，**并加备用触发＝每工作日 15:00 定时启动**（他原话「如果不能事件触发或不合适，每天下午三点启动」）⇒ 落 D5；② 收工汇总**加**「本批停了几次、各停在哪」列 ⇒ 落 D6；③ 首跑灰度**直接到 6**、不走灰度 2 ⇒ 落 D7（保留「他在场」一条）。【他】
- [x] 1.2 ✅ `design.md` frontmatter 已转 `status: 生效`（2026-09-02，`OP-0902-A`）。⚠️ 如实记：他未另说「审核通过」四字，系据其对全部未决项的逐条明确答复转生效，件内已写明「若与本意不符请当场退回」。【Cowork】

## 2. skill 本体

- [ ] 2.1 定稿 `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（引用式装载器 ＋ 规则正本两件，沿 clearpool/lan-closeout 既有形态）【Cowork】
- [ ] 2.2 `save_skill` 安装引用式指针，触发词按 1.1 裁定【Cowork】
- [ ] 2.3 指针回读验证（安装后即出现在会话可用 skill 清单，名称与 description 触发词逐字核对）【Cowork】

## 3. 停/续状态机（本包唯一的新建代码）

- [ ] 3.1 续跑状态文件设计与实现：`reports/lane-watch-state.json`（批次／波次／泳道／在等什么／选项／他答了什么）【CC】
  - 🔴 **必须落在 `reports/` 下** —— proposal「伴生文件」节已实测 `**/reports/` 覆盖（`git check-ignore -v` 结果见彼）；若实现期发现须落在 `reports/` 之外，**回到 proposal 该节重新实测并补忽略规则，不得默认沿用该结论**。
- [ ] 3.2 决策点拦截：泳道 opener 内置 D1 三档判据，命中 🟡/🔴 即停并落状态【CC】
- [ ] 3.3 续跑入口：Cowork 读状态文件 → 起下一段泳道【Cowork/CC】
- [ ] 3.4 🔴 **判据未覆盖时按 🟡 停**（fail-safe），且续跑请求里须问「本动作该进哪一档」，答复回灌 D1 表【CC】
- [ ] 3.5 🆕 **开跑先跑 LAN 探针，据结果决定候选范围**（2026-09-02 收敛后新增）：on-LAN ⇒ 全部可领活（`.51` 项除外，走 D1 第四档转出）；off-LAN ⇒ 自动排除依赖内网的活、其余照跑、被排除项列入收工汇总「待回内网」清单。🔴 **探针不过（状态判不准）⇒ 按 off-LAN 处理**（fail-safe）。**复用 `zhuopin-lan-closeout` 既有探针实现，只读引用不复制代码。**【CC】

## 4. 接线（复用既有件，不新建）

- [ ] 4.1 只读核对 `工具-opener批处理执行v2.ps1` 调用签名与无头 CC 参数（**不改它**）【Cowork/CC 只读】
- [ ] 4.2 企微"等人"事件接入既有四类事件推送通道【CC】
- [ ] 4.3 触碰区排波复用清池步骤 2 实现，只读引用不复制代码【CC】
- [~] 4.4 ❌ **本项已否掉，不做**（2026-09-02 同日 Shao Peishen 二次审定：「定时任务不合适，事件驱动以后再说，现在还是用『开启泳道看护』手动触发」）。原内容＝备用触发接线每工作日 15:00 定时启动。**⇒ 本包本期只有手动触发一种入口。** 🔑 他的改判与 D5 里那个前提冲突分析一致：定时触发的根本问题不是技术实现，而是它与「他在场」这个设计前提相矛盾——定时班次触发时他大概率不在，跑出来只有 🟢 档、🟡 档全堆成待答清单，**那样它就退化成一个"会自动做杂活的清池"，不是他要的模式**。
- [ ] 4.5 🔴 **待答超时兜底（D5 解法 3）**：任一泳道停在决策点超 **4 小时**未获答复 ⇒ 降回原状态、注明"因超时未获答复而收回"、并入当日收工汇总【CC】
  - 成因（2026-09-02 定时触发否掉后改写）：**他触发后完全可能中途离开工位**；无限挂着的泳道会持续占用 worktree 与触碰区，让别的 session 抢不到它的活。降回原状态是无损的——他回来仍可重新触发。**⇒ 本项不随 4.4 一同否掉，手动触发下依然需要。**

## 5. 🔴 清池退休与职能吸收（2026-09-02 架构收敛后整节重写；原「边界收窄」三条已作废）

- [ ] 5.1 **把 clearpool 的职能整体吸收进本包**：D1 白名单、触碰区排波、看护件落档、心跳看护逐项对照迁入，**不是删掉是承接**；唯一不迁的是「遇决策绕开不入批」——它正是错误前提（「清池＝人不在」）的产物【Cowork】
- [ ] 5.2 **触发词「offlan清池」保留为本包别名**（他习惯了，不强制改口），与「开启泳道看护」指向同一 skill【Cowork】
- [ ] 5.3 `zhuopin-lane-clearpool` 退休：description 改为指向本包，或按 save_skill 的退休惯例处置【Cowork】
  - 🔴 **退休前须确认 clearpool 的 openspec 变更包 `lane-clearpool-skill` 的处置** —— 它现有 4 项未完成（2.1/2.2/2.3/3.3），**不得让一个未归档的包连同其 skill 一起静默消失**；要么先收口再退休，要么在本包 tasks 里明确承接那 4 项。
- [ ] 5.4 LAN 收口 `description` 补边界指向：本 skill 专管 `.51` 部署与 LAN 留步，其余走 `zhuopin-lane-watch`【Cowork】
- [ ] 5.5 🔴 **LAN 收口正本的红线段一字不动** —— 只改 description 的适用场景描述，不动规则【Cowork】

### 5bis · 承接 `lane-clearpool-skill` 变更包的 4 项未完成（2026-09-02 Shao Peishen 答 (a) 定）

🔴 **不能让一个未归档的变更包连同它的 skill 一起静默消失** —— 那正是本项目反复吃亏的那类事。逐项对照后**实际只需新增两项**，另两项已被本包既有 task 覆盖：

| clearpool 原 task | 处置 |
|---|---|
| 2.1 核对 `工具-opener批处理执行v2.ps1` 调用签名与无头 CC 参数（只读） | ✅ **已被本包 4.1 覆盖**，同一件事，不重复列 |
| 2.2 心跳推企微段接线 | ✅ **已被本包 4.2 覆盖**（企微"等人"事件接入四类推送通道） |
| 2.3 波间自动推进与 30 分钟无心跳暂停的看护逻辑 | ⬇️ **新增 5.6 承接** |
| 3.3 队列 `#312` 子项回写 ＋ 本包归档 | ⬇️ **新增 5.7 承接** |

- [ ] 5.6 **波间自动推进与看门狗**（承接 clearpool 2.3）：波次跑完自动推进下一波；**任一泳道 30 分钟无心跳 → 暂停后续波 ＋ 告警 ＋ 等人**【CC】
  - ⚠️ **与本包 D3 的关系**：D3 管「单项失败只停该泳道、其余继续」；本项管「**无心跳**」——那是失联不是失败，**分不清它是在跑长回归还是真挂了**，故一律暂停等人。两条不冲突，分别覆盖「跑失败」与「不吭声」。
  - 📌 clearpool `#382` 行内已留一条相关判据：**预计等待超过 10 分钟的，等待期内要再写一行「仍在等 X，预计还要 N 分钟」**，否则 10 分钟判读线在长回归面前会常态性失效。实现时一并落。
- [ ] 5.7 **队列 `#312` 子项回写 ＋ `lane-clearpool-skill` 包归档**（承接 clearpool 3.3）【Cowork/CC】
  - 🔴 归档时须在包内写明「**本包 skill 已由 `lane-watch-mode` 吸收退休，剩余项已于 2026-09-02 转承接，见该包 tasks 5bis**」，**不得只标归档不写去向**。

## 6. 首跑验收

- [ ] 6.1 首跑 **上限 6**（D7：2026-09-02 Shao Peishen 审定直接到 6、不走灰度 2）＋ **Shao Peishen 在场**【他＋CC】
  - 🔴 **与 clearpool／lan-closeout 两包既例不同（那两包首跑均强制 2），可跳过的三条理由见 design D7**：本包 🟢 档全在 worktree 内隔离／不碰 `.51`·真实库·对外发送（前两包强制 2 的成因正是它们碰生产面）／一切不可逆动作都在 🟡🔴 档会停下来问他。
  - ⚠️ **「他在场」这一条保留，且不是为了限条数**——是为了让 D1 判据表在首次真实碰撞时有人当场改判。
- [ ] 6.2 🔴 **至少命中一次 🟡 档决策点并成功续跑** —— **只跑通 🟢 档不算验收通过**。
  - 成因（写死在此，防重蹈）：`#382` 事件驱动那次，只验了消费端（`check_patrol_signal.py` 返回 `[NO-SIGNAL]`）就判"空巡验活通过"，**而那条路径根本不经过桥一**；产生端一次都没验，结果链路建成后从未真实触发过一次。**停/续机制之于本包，等同产生端之于 `#382`——它才是新增的那部分。**
- [ ] 6.3 三判据全过方摘灰度：**误停 0**（不该停的没停）／**漏停 0**（该停的都停了）／**每次停都有可读的"在等什么"落档**【Cowork】
- [ ] 6.4 触碰区防撞零越界（沿清池首跑判据）【Cowork】

## 7. 收口

- [ ] 7.1 队列 `#452` 回写（判据→证据→行内状态处置）＋ 登记 §二 批次【Cowork】
- [ ] 7.2 本包归档 `/opsx:archive lane-watch-mode -y`（完工即归档纪律，不拖到下次 session）【Cowork/CC】

## 8. 已知未闭合（如实登记，非本期范围）

- [ ] 8.1 **企微侧回复→续跑桥未做**。Shao Peishen 2026-09-02 答 ④ 选 (b)，但当场明示「记下以后做」⇒ 本期实现 (a)：企微只推通知，答复须回到 Cowork。
  ⚠️ **风险预登记**：那是又一条要建的链路，**它自己也会有"死链"风险**——同 `#382`（建成、接线、开关全开，却因常驻进程未重启而从未真实触发）。**做它之前先看 `#382` 的验活结论**。
- [ ] 8.2 **D1 表的完备性无法在首跑证明**。三档判据覆盖的是当前已知动作类型；真实运行中必然出现未覆盖的新动作，靠 3.4 的 fail-safe（按 🟡 停）兜住并逐条回灌。**⇒ 本表是活的，不是一次定稿。**
