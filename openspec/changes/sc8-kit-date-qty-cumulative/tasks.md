# 任务 · 齐料日期按答交数量累计取值（队列 #344）

> 业务口径无决策点（四条已由姚祖怡逐条签认）。design 的 D3／D7 两项**无默认、须 Shao Peishen 决定**，
> 但两者都**不阻塞本变更包开工**：D3 在他答复前按保守项在线上生效并已登记队列；D7 本包只实测不改。

## 1. 前置

- [x] 1.1 `git fsck --connectivity-only` 通过；工作分支基于最新 `origin/master`
- [x] 1.2 判例签认状态复核：3 条判例 ✅×3（由归档 docx 的 `word/document.xml` sdt 计数与我方原件对照坐实，非 python-docx 文本提取）＋ 口径 ⑷ 文本回件「对」

## 2. 引擎层（TDD：先测后码）

- [x] 2.1 `_cumulative_confirmed_batches` 由 `sc8/baoguan.py` 下沉到 `sc8/forecast.py`，`baoguan.py` 再导出；反例单测锁死「`from .baoguan import` 与 `from .forecast import` 拿到的是同一个函数对象」
- [x] 2.2 `estimate_material_arrivals` 新增 `material_commitments` / `required_qty` 两入参；单测：两参齐备才走新分支，任一缺省 ⇒ 与改造前逐字节一致（零漂移）
- [x] 2.3 单测 · 判例 1：`R01I.0622` 真实答交序列（含 7 笔 qty=0 ＋ 2027-05-20 的 10000）⇒ 齐料日 2027-05-20，**不是** 2026-08-20
- [x] 2.4 单测 · 判例 2：一笔即够 ⇒ 取该笔日期，不再往后累
- [x] 2.5 单测 · 判例 3：8000＋9000 覆盖 15000 ⇒ 取 2027-01-20（第二笔），不是第一笔也不是第三笔
- [x] 2.6 单测 · 口径 ⑷：答交记录全为 qty=0 ⇒ 等同无答交，进 `no_feedback_materials`，到货日＝启发式估算日，**不取那个 0 数量的日期**
- [x] 2.7 单测 · D3：答了但累计不够 ⇒ 进 `no_feedback_materials`，到货日＝`max(启发式估算日, 最晚正数答交日)`；并覆盖"最晚正数答交日早于估算日"的反向分支
- [x] 2.8 单测 · 瓶颈物料与 kit_date 随新到货日联动改变（不是只改了 arrivals 而漏了下游）

## 3. 调用层（TDD：先测后码）

- [x] 3.1 `assess_supply_risk` 计算 `required_qty`：净额 ON 且有 inventory ⇒ `gross − 现货`；否则 ⇒ `gross`；单测两分支
- [x] 3.2 单测：同一份输入下，上方齐料日采用的那一笔 **与** 下方 BOM 缺口清单 `confirmed_batches` 的最后一笔**是同一天**（口径一致性锁死——这条单测就是本变更的存在理由）
- [x] 3.3 单测：`material_commitments=None`（数据源整体失败降级）⇒ 四色与齐料日与改造前完全一致
- [x] 3.4 单测：`gap_qty <= 0`（现货已覆盖）的物料不因新口径产生异常到货日

## 4. 展示层

- [x] 4.1 `sc8/webapp.py::_materials_page`「取数说明」第 6 条过期文案撤换（队列 #334 行内登记的尾巴）；单测断言页面不再出现「待回件」「正在与采购部确认中」字样

## 5. 真实数据验证（本变更包的重头，非走过场）

- [x] 5.1 `scripts/compare_kit_date_cumulative.py`：同一份真实输入跑「修复前/修复后」两遍，输出四色计数 ＋ 逐行差异
- [x] 5.2 用**生产载荷**跑（`SC8_NET_INVENTORY=on` 等以 `.51` 的 `C:\baoguan\.env` 为准，**不用仓库根默认值**——本机默认会静默走另一套口径）
- [x] 5.3 姚祖怡原始举证案例 `F02N.0224` / `R01I.0622` 端到端复现，逐笔答交与新齐料日逐位核对
- [x] 5.4 `scripts/find_cumulative_evidence.py`：为判例 2／判例 3（需求量为假设数）补真实多笔累计佐证
- [x] 5.5 实测规则 1／规则 3 是否已被现行代码覆盖（**只测不改**，结论进底稿 §五）
- [x] 5.6 对照底稿落 tracked `docs/`（**不落 gitignored 的 `reports/`**——#267 事故）

## 6. 回归

- [x] 6.1 SC8 全量 ＋ 平台底座 ＋ 同仓其余场景全绿，零回归
- [x] 6.2 mock 黄金基准精确不漂移（基线用 `git archive origin/master` 抽纯净副本实测重定，**不照抄场景 CLAUDE.md 上一行的数字**）
- [ ] 6.3 `openspec validate --all --strict`

## 7. 发布收口（四关）

- [ ] 7.1 ff 合入 `master`（`git rev-list --count master..<分支>` ＝ 0）
- [ ] 7.2 `sync-to-server.ps1` 部署 `.51`；**进程 `CreationDate` 独立核对真刷新**（不只信脚本打印"服务已重启"，SC8 2026-07-14 教训）
- [ ] 7.3 服务器文件 SHA256 与 master 工作区逐字节一致
- [ ] 7.4 冒烟：`/api/ping` ／ 关键页 200 ／ 一次全量重算；**从笔记本外部**实测（只在 `.51` 本机冒烟 200 是假象）
- [ ] 7.5 回滚 SOP 在位并实测
- [ ] 7.6 场景 `CLAUDE.md` 更新（含把「齐料日期只按最早答交日期取值」这条已解除的阻塞项摘掉）
- [ ] 7.7 `/opsx:archive`

## 8. 第 8 步（发布即起草跟进信）

- [ ] 8.1 🔴 **不起草、不发送**——查串行闸：姚祖怡最近一封 `采购部#18`（2026-08-24 起草）状态为 `⏳ 待你审`，**未到闭环形态**，闸锁着 ⇒ 按根 `CLAUDE.md` 第 8 步分支 ⑵，登记队列「待前信闭环后发」并写明拟发要点，本步到此为止
