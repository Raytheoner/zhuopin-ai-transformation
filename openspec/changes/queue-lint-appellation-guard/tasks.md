# queue-lint-appellation-guard Tasks

## 0. 待定夺（五项，全部未完成，本包不得归档）

**五项动手的都不是本 session（本 session 为无人在场的无头批处理，收工即结束）⇒ 按根
`CLAUDE.md` §5「答完之后由谁动手」判据，一律不给选项、只登记待总线派发。**

- [ ] 0.1 **追认 design 决策点 1**：baseline 用「计数棘轮」而非「快照哈希」。已知盲区＝同一行内
      「删一处、加一处」净变化为 0 不会被拦。
- [ ] 0.2 **追认 design 决策点 2**：行键跨文件全局唯一、刻意不含文件名。已知边界＝§二 批次号
      不由高水位线发号，两份队列理论上可撞号（方向是变松、会漏报不会误报）。
- [ ] 0.3 **追认 design 决策点 3**：刻意不提供 `--update-baseline` 一键重刷，`--emit-baseline`
      只打 stdout。代价＝真需要重刷时多两步操作。
- [ ] 0.4 **追认 design 决策点 4 的已知边界 1（假阴性）**：`Paul Shao` 一律放行，真有人写
      「Paul Shao 拍板」时不会报。这是刻意选的方向（误报会把「绝不可改」的路径推给下一个人去改）。
      🔴 **本项是五项里唯一放宽了判据的**，其余四项都是收紧或结构选择。
- [ ] 0.5 **裁定 `UNMATCHED_QUEUE_OWNER = "Paul"` 的处置**（`wecom-aibot-service/aibot_service/
      intake.py:42`）——目前唯一仍在往队列里写 `Paul` 的机器写入方。本包**刻意未改**：它不在队列
      `#352` 声明的触碰区内、归属机器人链路另一条派发线。现状实测已接近死码（入件白名单 6 人在
      `#387`⑷ 补入 `IT` 后全部有部门映射）。已登记队列 `#352` 回写行，待总线派发。

> ⚠️ 本包已实现并跑通、CI 判据已可生效，但按「完工即归档纪律」的机器可读要求，**tasks 未全
> `[x]` ⇒ 不得 `/opsx:archive`**。不归档理由已写在机器认得的地方（下方 §5 `暂不归档` 行）。

## 1. 前置取证（已完成）

- [x] 1.1 全量扫描两份生产队列，把 88 处 `Paul` 逐处归类：**74 处称呼用法（21 个活行）／
      7 处路径与账户名／7 处 `[S:done]` 历史行**。判据的每一条豁免都从这批真实语料反推。
- [x] 1.2 逐处看过 7 处 `Paul Shao`：**全部**落在路径（`C:\Users\Paul Shao\...`）或计划任务
      运行身份（`Paul Shao / S4U`）里，**无一是称呼用法** ⇒ 支持「紧跟 ` Shao` 一律放行」。
- [x] 1.3 `git check-ignore -v 0-学习与工具/队列称呼判据-baseline.json` 实跑：**无输出、退出码 1
      （未被忽略）**，符合预期（baseline 必须入库）。
- [x] 1.4 核实 CI 矩阵含 `0-学习与工具` ⇒ 本包单测会被 CI 真实执行（`工具-CI矩阵发现.py` 实跑）。
- [x] 1.5 反向核实「有没有可退休的既有守卫」：现有四项判据无一能被称呼判据覆盖，结论＝
      本包退休的是**人守规则**（根 `CLAUDE.md` §1 的执行部分），非守卫代码。

## 2. 判据实现（已完成）

- [x] 2.1 `_APPELLATION_RE`：大小写敏感 + 前后不许紧邻拉丁字母 + `(?![ \t]+Shao)` 路径否定前瞻
- [x] 2.2 `_appellation_row_key`：`§区#行号`，跨文件全局唯一、不含文件名
- [x] 2.3 `_appellation_scan`：扫 §一／§二／§四 表格数据行，返回命中数 + 片段 + 豁免原因
- [x] 2.4 `load_appellation_baseline`：读不到／解析不了 → **fail-loud**，不回退成空字典
- [x] 2.5 `appellation_check`：计数棘轮比较；豁免分类计数；漂移单独计数（不算违规）
- [x] 2.6 `emit_appellation_baseline`：只返回 JSON 文本、不写盘；豁免行不入 baseline
- [x] 2.7 违规进 `lint()` 返回值 ⇒ 计入退出码（与 #366/S2 只 warn 的策略分野见 design）
- [x] 2.8 `main()` 加 `--emit-baseline` 分支；豁免与 baseline 统计分类打印

## 3. baseline 冻结（已完成）

- [x] 3.1 `--emit-baseline` 产出并入库 `0-学习与工具/队列称呼判据-baseline.json`
- [x] 3.2 实测冻结值：扫描得 **21 个键 / 74 处命中**（2026-08-24），与 §1.1 归类结果逐数对上；
      收工回写给 `#352` 行加 `称呼豁免：` 标记后重跑，**入库值 20 键 / 72 处**（该行转由行内标记
      豁免、按设计不入 baseline）。🔴 重跑后逐键 diff 过一次：**唯一变化就是 `一#352` 掉出，
      无任何其它行被顺手吸进 baseline**——这正是「baseline 单调收紧」那条观察指标的第一次实测。
- [x] 3.3 baseline 文件内写明「重刷方式」与「历史记录不追改」两段说明，避免被当成可随手刷的缓存

## 4. 测试与验证（已完成）

- [x] 4.1 两向锁死：`test_baseline内命中不报` / `test_baseline外新增行即报`
- [x] 4.2 棘轮：`test_baseline内行再加一处即报` / `test_行内命中变少只算漂移不算违规`
- [x] 4.3 搬家反例：`test_行从一份队列挪到另一份不产生假违规`
- [x] 4.4 三类豁免：路径与账户名 / `[S:done]` / 行内 `称呼豁免：`，后两类均断言「必须可计数」
- [x] 4.5 误伤反例：`test_代码标识符不被误伤`（`PAUL_USERID` / `cc_to_paul` / `paulista`）
- [x] 4.6 fail-loud：baseline 缺失 / baseline 损坏，各一条且措辞可区分
- [x] 4.7 退出码：`test_违规计入lint退出码`
- [x] 4.8 `--emit-baseline`：不写盘 / 豁免行不入 baseline
- [x] 4.9 真实队列回归护栏：`AppellationRealQueueTests`——真实队列 + 入库 baseline 零违规，
      且断言 `live_rows > 0`（受管活行为 0 说明扫描面塌了，那时「零违规」是假的）
- [x] 4.10 **变异验证（判据不是空转）**：空 baseline 喂真实队列 → **21 条违规 / 21 活行 /
      74 命中**，与 `--emit-baseline` 逐数对上；换回入库 baseline → **0 条违规、退出码 0**
- [x] 4.11 单测全绿：`0-学习与工具` **705 passed／63 subtests**（新增 14 用例，零回归）
- [x] 4.12 🔴 **按 CI 的真实条件复验（本地绿 ≠ CI 绿，首轮已实际栽过一次）**：用 `git show master:<队列>` 取出**仓库里那一份**队列再跑判据 ⇒ 受管活行 21／命中 74／
      豁免 2／**违规 0**。成因与修法见 design 末节「首轮 CI 红的成因」。

## 5. 归档

- [ ] 5.1 `/opsx:archive queue-lint-appellation-guard -y`
      —— **暂不归档**：§0 五项待定夺（design 决策点 1-4 追认 + `UNMATCHED_QUEUE_OWNER` 处置）
      均须 Shao Peishen 签认，本 session 为无人在场的无头批处理，一律登记后停在该点。
