# Tasks — followup-supplement-channel

> ✅ **design 审已过：Shao Peishen 2026-08-25 答「全按建议」，六点全批**（§四 #119 已销号）：
> ①(a) 独立第二张表 ②(b) 不传参强制匹配主表标题、匹配不到即报错·编辑锁第二实现同批改
> ③(b) `push` 加 `--table` 人工通道化 ④(b) 复用闭环四态＋需回复列 ⑤(a)+(b) 双挂·sweep 告警不做
> ⑥(b) 并回 `followup_*` 加 `decision.kind`·历史三条不追改。
> **apply 执行会话**：2026-08-25 CC 无头批处理 A29，worktree `a29-supplement-apply`，分支 `claude/a29-supplement-channel-apply`。
>
> 🔴 **暂不归档** —— 6.4（端到端真实发送复现）**留步未做**：本次为无人在场的无头批处理会话，
> 对外真实消息一律留步登记不发（那封合并补件已于 2026-08-25 06:56 发出，重跑等于给唐燕萍再发一遍）。
> **与 LAN／`.51` 无关**（补件通道走公网 aibot，且本服务已由 Shao Peishen 2026-08-13 拍板永不部署 `.51`）；
> 留步的原因只有一个：**它会真的发一条消息给一个真人**。待人在场时补跑 6.4，再当场 `/opsx:archive`。

## 0. 前置闸（不满足则本文件其余各节一律不得开始）

- [x] 0.1 design 决策点 1（补件载体形态）已签认 —— (a) 案 C
- [x] 0.2 design 决策点 2（显式选表的改法）已签认 —— (b) 不传参强制匹配主表标题、匹配不到即 raise
- [x] 0.3 design 决策点 4（班次纳入 or 人工通道化）已签认 —— (b) 人工通道化
- [x] 0.4 design 决策点 5（补件终态语义）已签认 —— (b) 复用闭环四态 ＋ 显式「需回复」列
- [x] 0.5 design 决策点 3、6 已签认 —— 3＝(a)+(b) 双挂、(c) 不做；6＝(b) 并回正式前缀
- [x] 0.6 §五 未闭合项 1（签认型补件的回件配对）已决定 —— **另立**：机制修复并入 §一 `#366` 作 M6，不在本包；
      临时人工护栏已写进 README 补件⑶（唐燕萍下封含「甲／乙／分母／签认」字样的回件归补件行、勿配 `财务部#15`）

## 1. 显式选表（两份独立实现同批）

- [x] 1.1 `aibot_service/readme_table.py`：`iter_rows` / `locate_row` 增 `section` 入参；按签认的 2(b) —— 不传即**强制匹配主表章节标题**，匹配不到 `raise ReadmeTableError`，MUST NOT 回退到「第一个表」
- [x] 1.2 🔴 `0-学习与工具/工具-共享文档编辑锁.py::_followup_readme_rows` 同批改 —— 新增 `FOLLOWUP_MAIN_TABLE_SECTION`／`FOLLOWUP_SUPPLEMENT_TABLE_SECTION`／`FollowupReadmeSectionError`／`_followup_section_span`；原「找不到返回空列表」改为抛错（空列表与「真的没有数据」在调用方眼里逐字相同）
- [x] 1.3 `_followup_header_col_index` 同步（第三处依赖同一隐式判据的函数）
- [x] 1.4 逐一核对 8 个消费者：`approval`（加 `section`）／`delivery`（加 `section`）／`dispatch`（主表默认、补件结构性读不到）／`draft_gap_detection`（零改动，默认即主表）／`followup_readme_bridge`（零改动）／编辑锁 `release` 校验（加两表标题断言）／编辑锁自动转态（改走出声降级包装）／`工具-跟进闸查询.py`（零改动，已捕获 `ReadmeTableError` 转 `GateQueryError`，本就 fail-loud）
      - ⚠️ 两处「读 README 只为交叉参考、读不到不该阻断队列 release」的调用方改走 `_followup_readme_rows_advisory`：**降级可以不阻断，但绝不可以不出声**（会打印告警），与原实现的静默空列表**不是同一件事**

## 2. 反例测试与守卫（决策点 3 ＝ ⑤a+b）

- [x] 2.1 反例测试：补件表在前 ⇒ 主表消费者输出**逐字不变**（两份实现各一条）
      —— `tests/test_readme_table_section_selection.py`、`0-学习与工具/test_工具-共享文档编辑锁-补件选表.py`
- [x] 2.2 🔴 **变异验证**：把改判前的原实现逐字照搬为「变异体」，喂给**同一条**反例判据，**必须变红**
      —— 两份实现各有一条 `test_反例对改判前的原实现变红`，均以 `pytest.raises(AssertionError)`／`assertRaises` 机械断言，**永久留在用例里、不会随时间腐烂**；另各配一条钉住「它具体错成了补件表且全程无异常」
- [x] 2.3 编辑锁 `release` 断言两表章节标题均在 —— 缺任一即拒绝 release 并指明缺的是哪个标题；
      快照侧无法定位主表时亦拒绝（章节结构在持锁窗口内被改动 ⇒ 比对基准失效，MUST NOT 拿空的旧身份集合硬判）
- [x] 2.4 ⚠️ 知悉：CI 整体 run 长期 failure（队列 §一 #398 ⑶）⇒ 2.1 的红绿信号可能不可见，**已本地实跑取证**（见 §6.1 数字）

## 3. 补件表建立与批准通道

- [x] 3.1 README《补件登记》节：列表项 → 表（**内容原样重组，零实质改写**；现存 3 条全部迁入）
      —— 六格逐字取自原列表项；每条的完整成因／内容三件／硬前置核对／代词自检**全部原文保留在下方逐封小节**；
      ⑴⑵ 的 `🔄 已并入…` 与 ⑶ 的 `✅ 已推送 2026-08-25 06:56 本地（人工通道）` **迁入时原样保留**（历史记录不追改）
      —— 同批把本节那句「本节改用列表而非表格，从形态上免疫」标为已被本包取代，并写明**绕开一个缺陷时要问它顺带绕开了什么**
- [x] 3.2 表头：`承接编号` / `日期` / `收信人` / `主要事项` / `需回复` / `发送状态`
- [x] 3.3 `approve_followup_letter.py` 加 `--table 主表|补件`；补件行**复用全部既有判据**（草稿态断言／依据必填／冷却窗口／审计），一条未放宽
- [x] 3.4 「需回复」列为空 ⇒ 拒绝批准（`SupplementReplyRequiredMissingError`），且**不启动冷却计时器**、不修改文件
- [x] 3.5 【#400】转终态时**同一次写入**剥除编号列 `UNNUMBERED_MARKERS` 括注（`write_cells` 一次写出两格）；不改编号数值；无 `<部门>#<数字>` 的单元格一律不动（`销售部（未发，不编号）` 的括注是该格仅有的信息）
- [x] 3.6 🔴 【#400】**变异验证**：`采购部#18` 真实两格喂新判据必须产生一次修改；再喂一次必须幂等无修改
      —— 单测一条 ＋ **对生产 README 实跑一次**：`采购部#18（待你审，暂不占号）` → `采购部#18`，复喂无修改；存量实例已清零

## 4. 发送通道（按 0.3 签认结果 ＝ 4(b)）

- [x] 4.1 4(b) 人工通道 ＋ `push_followup_letter.py --table`；补件**不进**自动班次（结构性——班次默认只读主表，不靠过滤）
- [x] 4.2 🔴 **三案共同强制项**：班次每轮显式打印补件表待发行数，**含 `--dry-run`、含 0 行的情形**
      —— `summarize_supplement_table()` 在 `dispatch_followup_letters()` **任何提前 return 之前**算好并挂 `DispatchOutcome.supplement_note`（非可选字段），脚本两条路径各打印一次；补件表读不到时同样出声，不装作「没有」
- [x] 4.3 **不适用·条件已被签认消解** —— 仅当签认为 4(a) 或 4(c) 时需要（`has_unmarked_imminent_deadline` 对补件的行为）。签认为 4(b)，补件不进班次，该判据不作用于补件
- [x] 4.4 **不适用·条件已被签认消解** —— 仅当签认为 4(c) 时需要（「发送成功→回填」时间窗内的重复发送）。签认为 4(b)，只有一条发送路径，该时间窗不存在

## 5. 审计（决策点 6 ＝ ⑥b）

- [x] 5.1 统一 action 名 ＝ 既有 `followup_*`；补件靠 `decision.kind="supplement"` 区分（`delivery` 十处审计事件与 `approval` 全部带 `kind`）
- [x] 5.2 既有 3 条 `supplement_*` 历史事件**不追改**（本次一字未动）
- [x] 5.3 回填终态按决策点 5：通知型 → `✅ 无需回复`；签认型 → `✅ 已推送 <日期>`；读不到「需回复」列时按签认型（保守方向）

## 6. 回归与验收

- [x] 6.1 服务全量 `pytest` ＋ 平台全量 `pytest`，零漂移；既有失败用例逐条判定
      —— 服务 **534 passed / 1 skipped**（基线 498＋新增 36）；平台 **380 passed / 1 skipped**（与既有基线一致）；
      工具基线两份 **262 passed**（改判前后同数）＋ 新增一份 **11 passed**。
      **改动后失败的 8 例**全在 `FollowupReadmeStructuralValidationTests`，成因＝其 fixture 只写主表章节；
      按新契约那本就是一份不合法的 README ⇒ **判为「改判」而非「放宽」**，已补齐补件表章节，现 13 passed
- [x] 6.2 `dispatch_followup_letters.py --dry-run` 输出含补件表状态行（**沉默不算通过**）
      —— 对生产 README 实跑：`[dry-run] 主表无「🆕 待发」行……` ＋ `[dry-run] [补件表] 共 3 行，其中「🆕 待发」0 行……本通道不自动发送补件`
- [x] 6.3 闸查询 `--all` 前后逐字对照，确认补件表建立后无一收信人闸状态改变
      —— 五位收信人全部核对：唐燕萍仍 ✅ 开、依据仍是 `财务部#15`（对照 design 实测：补件若进主表，她的闸在 `⏳ 待你审` 与 `✅ 已推送` **两态下都会被锁死**）；
      另对生产 README 实测：去掉补件表整节主表仍 54 行且逐字相等；**把补件表挪到主表之前主表读数仍逐字不变**
- [ ] 6.4 🔴 **留步未做（如实登记，非遗漏）** —— 端到端复现须**真的发一条消息给唐燕萍**，本次为无人在场的无头批处理会话，
      对外真实消息一律留步登记不发。**与 LAN／`.51` 无关**（走公网 aibot；本服务已拍板永不部署 `.51`）。
      待人在场时补跑：走 `--table 补件` 发那封已发过的合并补件，**须见 `errcode=0` 与审计事件**，不得以「命令没报错」代替（同 §一 #82 教训）
- [x] 6.5 价值指标复测：「有发送状态、却不在任何机器认得的载体上」的对外件数量，基线 3 → **0**（三条补件均已在机器可读的补件表内，可被 `iter_rows(text, "补件登记")` 读到）
- [x] 6.6 更新 `5-平台底座/wecom-aibot-service/CLAUDE.md` §5 状态时间线
- [x] 6.7 队列 §一 #399（含并入的 #400）回写；§二 登记 commit 批次
- [ ] 6.8 `/opsx:archive followup-supplement-channel -y` —— **暂不归档**（判据见本文件抬头；6.4 未闭合，tasks 未全 [x]）
