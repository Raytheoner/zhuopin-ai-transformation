# Tasks — followup-supplement-channel

> 🔴 **本变更包停在 design 审。以下任务一项都不得开始。**
> 出件会话（2026-08-25 CC 无头批处理 A24）只做 propose＋design，未 apply、未改一行生产源码。
> **决策点 1／2／3／4／5／6 六项全部无默认项**（提出方是无人在场的无头批处理会话，收工即结束 ⇒ 按根 CLAUDE.md §5 判据答不出执行者），已登记队列 §四 待 Shao Peishen 签认。
> **暂不归档**（本包尚未 apply，不适用归档判定器）。

## 0. 前置闸（不满足则本文件其余各节一律不得开始）

- [ ] 0.1 design 决策点 1（补件载体形态）已签认
- [ ] 0.2 design 决策点 2（显式选表的改法）已签认 —— 🔴 它是本包「改变对外语义」之所在
- [ ] 0.3 design 决策点 4（班次纳入 or 人工通道化）已签认 —— 签认结果决定 §4 整节写法
- [ ] 0.4 design 决策点 5（补件终态语义）已签认
- [ ] 0.5 design 决策点 3、6 已签认（已给建议，但同样无默认项）
- [ ] 0.6 §五 未闭合项 1（签认型补件的回件配对）已决定：本包内解决 / 另立队列行 / 暂不处理

## 1. 显式选表（两份独立实现同批）

- [ ] 1.1 `aibot_service/readme_table.py`：`iter_rows` / `locate_row` 增章节选表；按签认的 2(a)/(b)/(c) 决定默认行为
- [ ] 1.2 🔴 `0-学习与工具/工具-共享文档编辑锁.py::_followup_readme_rows` 同批改 —— **不改它 ＝ 只修了一半**
- [ ] 1.3 `_followup_header_col_index` 同步（它是第三处依赖同一隐式判据的函数）
- [ ] 1.4 逐一核对 8 个消费者：`approval` / `delivery` / `dispatch` / `draft_gap_detection` / `followup_readme_bridge` / 编辑锁 release 校验 / 编辑锁自动转态 / `工具-跟进闸查询.py`

## 2. 反例测试与守卫（决策点 3）

- [ ] 2.1 反例测试：补件表在前 ⇒ 主表消费者输出不变或 fail-loud
- [ ] 2.2 🔴 **变异验证**：把改判前的原实现喂给 2.1，**必须变红**；不红即空转，须重写判据
- [ ] 2.3 编辑锁 `release` 断言两表章节标题均在
- [ ] 2.4 ⚠️ 知悉：CI 整体 run 长期 failure（队列 §一 #398 ⑶）⇒ 2.1 的红绿信号可能不可见，须本地实跑取证

## 3. 补件表建立与批准通道

- [ ] 3.1 README《补件登记》节：列表项 → 表（**内容原样重组，零实质改写**；现存 3 条）
- [ ] 3.2 表头：`承接编号` / `日期` / `收信人` / `主要事项` / `需回复` / `发送状态`（按决策点 5 定）
- [ ] 3.3 `approve_followup_letter.py` 加 `--table`；补件行复用全部既有判据（草稿断言／依据必填／冷却窗口／审计）
- [ ] 3.4 「需回复」列为空 ⇒ 拒绝批准
- [ ] 3.5 【#400】转终态时**同一次写入**剥除编号列 `UNNUMBERED_MARKERS` 括注；不改编号数值
- [ ] 3.6 🔴 【#400】**变异验证**：`采购部#18` 真实两格喂新判据必须产生一次修改；再喂一次必须幂等无修改

## 4. 发送通道（写法取决于 0.3 的签认结果）

- [ ] 4.1 按签认结果实现：4(a) 班次纳入 ／ 4(b) 人工通道 ＋ `push_followup_letter.py --table` ／ 4(c) 两条路
- [ ] 4.2 🔴 **三案共同强制项**：班次每轮显式打印补件表待发行数，**含 `--dry-run`、含 0 行的情形**
- [ ] 4.3 若签认 4(a) 或 4(c)：补测 `has_unmarked_imminent_deadline` 对补件的行为（design §五 未闭合项 2）
- [ ] 4.4 若签认 4(c)：补测「发送成功→回填」时间窗内的重复发送（design §五 未闭合项 3）

## 5. 审计（决策点 6）

- [ ] 5.1 按签认结果统一 action 名；建议案为并回 `followup_*` ＋ `decision.kind="supplement"`
- [ ] 5.2 既有 3 条 `supplement_*` 历史事件**不追改**（历史记录不追改）
- [ ] 5.3 回填终态按决策点 5：通知型 → `✅ 无需回复`；签认型 → `✅ 已推送 <日期>`

## 6. 回归与验收

- [ ] 6.1 服务全量 `pytest` ＋ 平台全量 `pytest`，零漂移；既有失败用例逐条判定「改判 vs 放宽」
- [ ] 6.2 `dispatch_followup_letters.py --dry-run` 输出含补件表状态行（**沉默不算通过**）
- [ ] 6.3 闸查询 `--all` 前后逐字对照，确认补件表建立后无一收信人闸状态改变
- [ ] 6.4 🔴 端到端复现：用 2026-08-25 那封真实合并补件走新通道，**须见 `errcode=0` 与审计事件**；不得以「命令没报错」代替（同 §一 #82 教训）。走公网 aibot 通道，**与 LAN 无关**
- [ ] 6.5 价值指标复测：「有发送状态、却不在任何机器认得的载体上」的对外件数量，基线 3 → 目标 0
- [ ] 6.6 更新 `5-平台底座/wecom-aibot-service/CLAUDE.md` §5 状态时间线
- [ ] 6.7 队列 §一 #399（含并入的 #400）回写；§二 登记 commit 批次
- [ ] 6.8 `/opsx:archive followup-supplement-channel -y`（tasks 全 [x] 后当场跑，不拖到下次 session）
