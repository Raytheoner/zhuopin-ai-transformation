> 当前检查点（2026-09-25）：用户1/2/3授权已执行主线ff、Aibot单模块切换和轮询包装原位替换；下文历史执行包并非仍待授权。新hooks正常信任、发布工作树正负例、主仓普通身份sandbox正负例及现场安装模块隔离消费均已实测通过。S4U调度阻点已通过用户批准的InteractiveToken模式解决，并完成实际工具/随机凭据/5hooks验证；仅账户已登录时运行。末轮注册器源码待自动落库；任务Disabled、patrol.enabled=false，业务闸关闭。最新证据以acceptance.md末节为准。

# 发布准备件：#648（未获发布、未通过总闸）

候选代码在 codex/mechanism-migration-648；当前检查点及后续修复见 git log。业务开工闸关闭。此文件是后续执行清单，不是执行证据。

## 必须先完成的原生验收

1. 已完成正常信任核验：linked worktree实际加载主仓项目hooks；没有代写trust。
2. 原生 apply_patch 合法写入及无锁队列写入拒绝，必须关联同一 thread 的 PreToolUse/PostToolUse 与目标文件实际状态；使用隔离夹具，不触碰真实队列。
3. 三条消费者依次实跑：轮询脚本到 provider；合法生成/lint 的 opener 到新工作树写入、测试、review、交接；patrol 隔离信号到模型执行及消费确认。每条核对静默、成功、失败、超时、重复、恢复；既有纯状态机证据沿用，只补原生接缝。产物、工具、hook、进程证据缺一则不通过。
4. 新工作树中的配置 blob 继承已证实；正常信任和当前桥接的原生正负例已证实；批处理全新工作树事件链仍在验收。新修复提交后采用新候选 HEAD，不把旧6c2检查点的代码测试外推成新版本全验收。

## 逐项现场变更包

| 项 | 发布件 | 切换前证据 | 失败恢复 |
|---|---|---|---|
| 主线资产 | 本分支 Git diff、acceptance.md、证据摘要 | 确认候选HEAD、主线现时HEAD；核对主线未跟踪 .agents/.codex/handoff 与候选的逐文件差异；保存原件后才申请ff，不盲覆盖 | 保留原件及候选分支；不清旧工作树，不force-push |
| 轮询守 | 工具-轮询守.ps1、工具-注册轮询守计划任务.ps1、统一provider | 旧任务已Disabled；保留已导出的XML、账户、触发器；注册器生成包装与被调用版本一致；正常计划触发必须实证 | 停用该任务，保留sticky和运行证据；禁止恢复旧模型Action |
| Aibot事件拆件 | patrol_dispatch.py、provider及共享runtime解析 | 服务仍在旧工作树；确认真实Python进程和模块来源；准备逐文件备份及最小受控重启窗口；另获该项部署授权 | 关闭自动拆件消费、保留信号和失败状态；不回退到旧模型，不批量停止业务服务 |
| CommitSweep退出传播 | 已验非零传播的VBS修复 | 对照现场包装路径/内容、先备份；具体变更授权后替换；计划运行需核下游与包装退出码 | 暂停该项再修复，保留日志；不能用任务返回0代替下游结果 |
| 其余自动化 | 五项既有Codex任务保持PAUSED | 选择单一调度责任，核对原周期/时区/账户、与Windows消费者无双跑后逐项启用 | 暂停模型消费者，信号留存；不新建重复任务 |

ff与现场部署仍需逐项授权；原生隔离链与现场验收分开记录；进入现场动作前取得对应逐项授权。不执行 .51 动作；如后续涉及，仅按 zhuopin-lan-closeout 正本及其原授权流程执行。

## 能力边界

34技能和9插件各自状态见asset-disposition.md。本次用到的规划/TDD/隔离/审查/验证路径有实证；业务跟进、外发、在线研究、旧guardian等未验证能力不得进入自动消费。英文和中文last30days属于行业研报项目，保留现状，不再列作本线在线验收缺口；其他无关项目技能和配置同样按最新intent排除。历史覆盖缺口、早期测试隔离事故和复制历史资料被自动审批拒绝的记录保持可见。

## 暂停恢复验收依据

provider默认暂停不启动；poll/batch须显式开关；patrol配置缺失默认暂停且保留信号；watcher失败退避/三次停链及重启落盘已有隔离回归。现场暂停恢复还须在上述受控切换时关联真实进程、信号、计划状态取证。任何恢复路径均不得启动Claude。


## 2026-09-25 原生验收后的具体执行包（尚未执行）

实现提交 c2e8c9d5；候选已在自身分支吸收主线 a707dca2 的两份治理文档，具备 ff 祖先关系。该同步只改队列/自动台账，不改变已测实现。精确候选 HEAD 见本机 main-asset-overlap-final.json；每次授权后执行前仍核对现时HEAD，不覆盖新增变化。

1. **主线 ff 与版本化资产**：现有108个未跟踪文件与候选同名，8个内容不同。只对清单中同名文件逐个核旧hash、保存原件与manifest后移出阻挡位置，再进行ff；失败保留分支并恢复原件，禁止清理其他文件/工作树。原历史memory、settings备份、runtime和凭据不进入候选。主仓既有业务脏文件和本轮队列状态保持原样，不stash全仓、不混提交。
2. **新工作树真实继承**：从合入后的主线创建专用验证工作树，检查版本化配置blob和共享runtime解析。候选portable hooks.json与当前已信任的主仓固定路径版本不同，配置hash变化后须走正常/hooks审阅信任；不能沿用旧hash声称新配置已信任。完成正常信任后再做必要原生正负例，不能启用无守卫消费者。
3. **Aibot局部切换**：对象为ZhuopinAibotDevListener及其wecom-service-home工作树中的patrol_dispatch.py；新模块SHA与备份计划见aibot-cutover-preparation.json。仅更新该模块及已合入的共享provider依赖，自动拆件先保持禁用，精确识别服务PID后受控重启；不使用广泛杀同名Python的方法。以隔离信号验证实际服务加载版本/消费/去重，随后才按授权启用业务模型消费；不借验收对外发送。
4. **轮询计划任务**：沿用已导出的ZhuopinPollGuard原周期/账户/XML，核对Action→VBS→PowerShell→provider版本，替换旧模型入口；五项Codex自动化继续PAUSED。先用隔离探针/信号完成真实任务触发及失败返回证据，再开放原探针。真实探针有副作用，不得为了冒烟无授权运行。
5. **CommitSweep包装**：候选包含VBS退出码透传修复，ff若更新实际调用文件即构成现场变化，需相应授权。保存旧包装与hash，验证计划Action实际指向，再在隔离非零子进程验证包装返回；不以计划任务返回0推断落库成功。

旧guardian模式仍明确拒绝启动；已批准设计第6项不把它冒充迁移完成。可用替代为明确授权的headless批处理，不能把用户泛称“开启看护”自动改成headless。此限制在业务开工决定时必须明示。业务、L2、ASIL、.51及真实发送闸保持原规则。
