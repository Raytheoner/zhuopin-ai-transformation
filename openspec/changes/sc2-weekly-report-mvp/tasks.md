# sc2-weekly-report-mvp Tasks

> 🔴 **本清单按 design.md「需 design 审拍板」四项的默认项编写**（①(a) 不指派 backup／②(a) W-4 同期周／③(a) 不新增底座方法／④(a) 端口 8095）。**若审查改判**：改 ② 影响 3.1–3.3；改 ③ 影响 2.x 并须补 `platform-data-connectors` delta spec 与独立 design 审；改 ④ 影响 8.2；改 ① 影响 9.3。
>
> ⚠️ **apply 前须先经 Shao Peishen 审 design**（CLAUDE.md §5 第 3 步）。本清单产出即停，未获审核不得开工 §2 起各项。

## 1. 场景骨架

- [x] 1.1 建 `4-数字员工/采购部/SC2-采购周报自动生成/{sc2/,tests/}` 与 `pyproject.toml`（照 SC7 同款结构）
- [x] 1.2 `tests/conftest.py` 照抄 SC7/O2 同款 worktree 路径引导（队列 #300），**放在任何 import 之前**；`tests/__init__.py` 一并建
- [x] 1.3 真实验证 `python -m pytest` 在**未安装** `zhuopin_platform` editable 的情况下可正常 collection（复现队列 #313② 的检验方式，不只看 CLI 入口）

## 2. 取数层 `sources.py`（spec: sc2-feed-source）

- [x] 2.1 定义冻结数据集模型与 `mock`/`real` 双实现的统一接口
- [x] 2.2 `mock` 实现：固定样例数据，覆盖正常/空窗口/关闭行/限流四类形态
- [x] 2.3 `real` 实现：ERP 侧 `get_purchase_orders` + `get_purchase_line_status`（D17 剔除 LineStatus 3/4/5）
- [x] 2.4 ~~SRM 侧~~ → **D15-R 改为 ERP `GR/Query` 收货侧**（SRM 实测 300234 不允许查 7 天前数据，历史窗口取不到）
- [x] 2.5 real fail-loud：任一源失败即中止，**不得静默降级为 mock/缓存/部分数据**；单测覆盖「SRM 挂而 ERP 正常 → 整体失败」
- [x] 2.6 SRM 跨度 ≤60 天与 30 秒限流的本地前置处理；单测覆盖「限流 ≠ 无数据」的可区分性
- [x] 2.7 ⚠️ 底座**纯新增**（③(a) 默认项明示允许「纯新增且零行为变更」）：`ReceiptLine` ＋ `get_receipt_lines()` ＋ `PurchaseOrder` 4 个带缺省值字段；**未改任何既有方法行为**，平台回归 295 passed 零失败为证

## 3. 窗口层 `windows.py`（spec: sc2-feed-source）

- [x] 3.1 基准日期 → (W, W-1, W-4) 三窗口纯函数；**不读当前时刻**，同基准日期恒得同划分
- [x] 3.2 ISO 周口径，跨年周边界连续不重叠；单测覆盖跨年
- [x] 3.3 单测：同基准日期重复调用结果一致（可复算）

## 4. 指标层 `metrics.py`（spec: sc2-metric-engine）

- [x] 4.1 纯函数骨架：冻结数据集 + 窗口 → 指标集；**不触网、不读时钟、不写盘**（单测在无网络下跑通即证）
- [x] 4.2 实现「算得出的全部指标」（D5：**不预先砍**），下单/在途/收货/交付及时/供应商维度等
- [x] 4.3 口径未定的指标带**具体假设标注**（如「以供应商承诺交期为基准（口径待确认）」），非「口径待定」四字
- [x] 4.4 空窗口与除零：输出「无数据」态而非 0%；环比涉无数据窗口输出「无可比基准」而非 100%/∞
- [x] 4.5 异常识别：阈值**外部配置、不硬编码**；默认阈值带「未经确认」标注，且**不得因超时自动转确认**
- [x] 4.6 单测覆盖 4.1–4.5 各条，含同输入同输出

## 5. 组装层 `report.py`（spec: sc2-weekly-report）

- [x] 5.1 三窗口并列组装：本周值 + 周环比 + 月同比，**同一份周报内**呈现
- [x] 5.2 历史窗口缺失时明示「无可比基准」，**不省略该列**
- [x] 5.3 周报可追溯标注：基准日期、三窗口起止、数据源与取数时刻
- [x] 5.4 快照读写：含指标值/口径假设/异常阈值/基准日期，足以不重新取数即复现该期
- [x] 5.5 单测：依快照重渲染与原期一致；口径变更后历史期**不被追溯改写**
- [x] 5.6 🔴 所有生成物落 `reports/` 下；单测/自检确认运行后 `git status` **无未被 gitignore 覆盖的新增未跟踪文件**

## 6. 确认层 `review.py`（spec: sc2-anomaly-review）

- [x] 6.1 状态机：生成后默认「待确认」，**未确认不得进入推送流程**
- [x] 6.2 超时**不得**自动确认、不得自动推送；单测覆盖
- [x] 6.3 带异常仍可确认（异常不阻断），但该判断须被记录
- [x] 6.4 确认写平台 `audit`：确认人/时刻/期次/快照标识/异常列表**五项齐全**，追加写不覆盖
- [x] 6.5 确认状态持久化（落 `reports/`），服务重启后不丢失、不重复推送；单测覆盖

## 7. 交付层 `webapp.py` + `notify.py`（spec: sc2-report-delivery）

- [x] 7.1 Flask 蓝图，**所有路由位于 `/procurement/sc2` 前缀下**；页内链接与静态资源**不用根路径绝对引用**
- [x] 7.2 `install_flask_gate` + `install_flask_access_log` 接入；单测/冒烟确认无口令请求被拒且不返回业务数据
- [x] 7.3 预留网关鉴权接入点（过渡期可空壳，**但必须存在**），使网关落地只换实现、不动业务路由
- [x] 7.4 「确认发布」按钮，交互与 SC8 保供看板同款
- [x] 7.5 `notify.py`：企微推送**仅**采购部群 + 姚祖怡；扩大范围须显式变更，不由配置默认或运行时推断
- [x] 7.6 `run_sc2.py` 服务入口顶部加 #300 同款路径引导

## 8. mock→real 切换与真实数据验证（D4/D19/D20）

- [x] 8.1 mock 全链路跑通 + 全量单测绿（先测后实现，SuperPowers 顺序）
- [x] 8.2 🔴 **F14 参数名对照测试**：对 `ZpViewPurOrder/Query` 及本场景首碰的每个端点各做一次「故意传错参数名」对照，**记录实际表现**；命中「静默返回全表」则按 spec 加业务字段二次过滤
- [x] 8.3 切 real 跑一次真实数据周报，固化黄金基准到 `reports/`（不入库）
- [x] 8.4 O-5 已结：三端点字段全查**无工时字段** ⇒ 「完工日/工时」类首版不做；**同批新发现 O-6：无交期字段 ⇒ 准时率亦撤下**
- [x] 8.5 自检：本包是否改动了任何底座既有方法在相同输入下的行为——**若是，停下补 `platform-data-connectors` delta spec 并回报**（③ 的兜底）

## 9. 收口（CLAUDE.md §5 第 5–8 步）

- [x] 9.1 `openspec validate sc2-weekly-report-mvp --strict` 与 `--all --strict` 全绿
- [ ] 9.2 `/opsx:archive sc2-weekly-report-mvp -y`（**tasks 全 [x] 后当场归档，不拖到下次 session**）
- [ ] 9.3 场景 `CLAUDE.md` 六段式（定位/决策/底座/红线/时间线/依赖）；🔴 **含 D9 过渡期端口豁免留痕**——理由、批准来源、注销条件三项齐全；backup 缺口按 ①(a) 登记
- [ ] 9.4 知识资产台账登记：O-1/O-2/O-3/O-4 四项入《跨场景前置数据与知识库任务总表》§一.2
- [ ] 9.5 发布收口四关：部署 `.51`（端口 8095）+ **补防火墙入站规则**（只在 `.51` 本机冒烟 200 是假象）+ 冒烟（关键页 200／一次全量重算／门禁生效）+ 可常驻（守护/自愈，非临时 worktree 跑一次）+ 回滚 SOP 在位
- [ ] 9.6 起草跟进信（README 登记行发送状态**只写 `⏳ 待你审`**）
- [ ] 9.7 🔴 **发送前三条硬前置全过方可发**：① 已 ff 合入 master（`git rev-list --count master..<分支>` = 0）②`.51` 已部署且冒烟通过 ③ 用真实案例端到端复现。**且须先过串行闸**——姚祖怡手上采购部#14 于 2026-08-13 已推送、未回件，**其回件并回灌前不得发下一封**
- [ ] 9.8 commit + push + 收工重跑台账；队列行回写（✅ 在状态列开头）
