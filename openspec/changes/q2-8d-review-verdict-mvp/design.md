# design · q2-8d-review-verdict-mvp

> **design 审：🟡 待 Shao Peishen**（无头泳道 `OP-0917-Q` 2026-09-17 建到档 1；业务判据已由陈忱签认，本 design 只审工程决策 D1–D8）。业务口径出处以场景 `intent.md` §二 为准，不重抄。未连真实解析、未部署、未发信、未改 V3.2 正本与判例台账。

## Context
档 1（12 passed）；承接点＝D 段全文；`CriteriaRegistry("Q2")` 24 条覆盖层全签认，`PENDING` 两条未签认；L2。

## Decisions（✅ **2026-09-18 Shao Peishen 审过，答 `1a` ＝ D1–D8 全数通过，无驳回**）

> ✅ **审过留痕**：裁决人 Shao Peishen，2026-09-18，答复原文 `1a 2a 3a` 第 1 项；整表通过，未驳回任何一条 D。⇒ tasks §3 收口闸解除。
> 🔴 **其中两条是泳道明确提请、本次一并拍过的**：**D3**（M10 六组重复项取 (a)「通用条只判场景无关判据」——陈忱答的是「逐组二选一」、**未逐字指 a/b**，故属我方推断，现经拍板成立）；**D4**（D3-03 的 1 分并入新增 `D3-06` 作完整度基础分，总分仍 100）。**两条此后按已签认口径引用，不得再标「推断」。**
> 🔴 **审过≠口径已签认**：`Open Questions` 一条未动（`Q2-G-01` 措施可验证性判定标准随下一封质量部信携带、不单起；`Q2-G-04` 持有人/backup 属人事），`PENDING` 注册表仍读即抛；语义层 25 条规则＋红线①⑤ 档 1 无实现，只留 `SemanticSource` 接口。
| # | 决策 | 备选／代价 |
|---|---|---|
| **D1** | 规则表＝V3.2 JSON 搬运件（原表不改，sha256 用例守）＋签认覆盖层（每条改动从 `CRITERIA` 取值、`overlay_note` 留痕） | 手抄 V3.3——两份真身无签认链，否 |
| **D2** | 语义层只留接口：`PendingSemantic` 一切待人工，等级只出区间；`HumanVerdictSource` 为确认态，未来 LLM 为疑似态（P1） | 关键词假装判语义——违 V1–V5，否 |
| **D3** | 🟡 M10 六组重复取 (a)：通用条只判场景无关判据（词表并集、≥2 领域），加严留场景特有条；她答「逐组二选一」未逐字指 a/b | (b) 删场景特有条并回分——改 V3.2 条目集合须她再签 |
| **D4** | 🟡 M4 落法：新增 `D3-06`（1 分完整度基础分＝并入的 D3-03，总分仍 100）＋段内递增扣分 1 分/缺项、封顶 8、不扣负；`D3-03` 退休 | D3-06 纯扣分＋保留 D3-03——负责人/完成日计两次 |
| **D5** | 红线④⑥可自动判 D，①②③⑤永不：④ 仅 HIGH＋确认为空 ≥2 项，抽取未命中转人工核（P2）；⑥ 关键词＋更新语义＋填 N（判例 7）；①⑤ 关键词预筛只出疑似；②③ 恒本批不验收（③规则引擎预判只作证据） | ③ 按 M18 自动判——判例 11 晚于 M18，否 |
| **D6** | 结构闸先于一切：D3–D7 任一空 ⇒ 结构性退回，不跑红线不评分；客户模板缺 D6 例外（D6 规则 NA、剔出满分） | 先跑红线——多三次 AI 调用，否 |
| **D7** | 安全分流在评分后、退回建议前：`safety_related` True／None／非 HIGH ⇒ 转人工，评分照出；ASIL C/D `AsilExcludedError` | 不评分——工程师要看分，否 |
| **D8** | 审计：每份一条 `AuditEvent(Q2, 8d_report_verdict, L2)`，`decision` 带版本／等级区间／处置／语义来源，`data_sources.rules`＝sha256；`evaluator` 待指定至 `Q2-G-04` | — |

### 实施约束（档 2 起）
1. 引擎数字只来自 `CRITERIA.value_of`；2. 语义层上线唯一前提＝`PENDING.SEMANTIC_LAYER_ACCEPTANCE` 按 V1–V5 校准签认（排除红线②③、样本 3 单列）；3. 处置永远是「建议」。

## Open Questions（不阻断档 1）
`Q2-G-01` 措施可验证性标准／`Q2-G-02` 语义层签认／`Q2-G-03` 样本 3 ※／`Q2-G-04` 持有人 backup；乙档人名识别按「姓名／角色配对」实装，真实校准误差大再回她补判据。
