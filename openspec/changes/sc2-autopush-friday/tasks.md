# sc2-autopush-friday Tasks

## 1. 取消确认发布前置（§四 #89 (a)）

- [x] 1.1 删除 `review.ensure_publishable()` 与 `UnconfirmedError`（不留不调用的闸）
- [x] 1.2 `notify.push()` 去掉确认校验；模块头写明拍板来源、两条代价与「装回去的唯一落点」
- [x] 1.3 `review.py` 模块头改写：由「推送闸门」降为「事后签认记录」
- [x] 1.4 页面文案改写（周五 20:00 自动推送；按钮语义改为事后复核）
- [x] 1.5 单测：以「符号不存在」反证闸确已拆掉，而非留了旁路
- [x] 1.6 单测：**未确认也推得出去**（原「未确认时推送被拒」的反向断言）
- [x] 1.7 幂等与推送范围两条保护保留并配单测（取消前置后幂等比过去更要紧）

## 2. 推送通道改走 aibot（队列 #282）

- [x] 2.1 新增 `sc2/outbox.py`：落盘 JSONL，只写部门名不写 chatid
- [x] 2.2 部门键校验，拼错**当场上抛**（中继侧是 fail-closed 静默跳过）
- [x] 2.3 一期写两条记录（群 ＋ 私信），使半成功状态可表达（D36）
- [x] 2.4 删除 `config.wecom_webhook()` 与 `SC2_WECOM_WEBHOOK_URL`
- [x] 2.5 `outbox.pending()` 积压计数 ＋ 三处可见（CLI／页面／API）
- [x] 2.6 单测：源码层反证推送层不再出现 webhook；chatid 未被抄进场景代码
- [x] 2.7 `git check-ignore -v` 实测 `reports/sc2_group_outbox.jsonl` 命中 `.gitignore:28`

## 3. 周五 20:00 自动生成并推送

- [x] 3.1 新增 CLI `run_sc2.py autopush`（生成 → 落快照 → 写 outbox）
- [x] 3.2 缺省 `--mode real`（与 serve/report 相反，理由见 D32）＋ 单测钉住
- [x] 3.3 缺省不截断行级状态取数，与 serve 同口径 ＋ 单测钉住
- [x] 3.4 `--no-push` 演练开关
- [x] 3.5 单测：端到端生成并入 outbox／重复跑不重复推送／`--no-push` 不写 outbox
- [x] 3.6 单测：**周五基准日必带 O-7「本周窗口尚未走完」声明**（D33，防日后被删）

## 4. 部署

- [x] 4.1 `ZhuopinDeploy.psm1` 新增可选 `-Trigger` 与 `-ExecutionTimeLimitHours`（缺省值未变）
- [x] 4.2 `deploy-server.ps1` 注册 `Sc2WeeklyAutoPush`（周五 20:00 ＋ 2 小时时限 ＋ IgnoreNew）
- [x] 4.3 两个 `.ps1` 实测：UTF-8 **BOM 保留** ＋ `Parser::ParseFile` 零语法错
- [x] 4.4 部署 `.51:8096` ＋ 发布四关冒烟
- [x] 4.5 回滚 SOP 更新（含新任务的删除命令）

## 5. 回归

- [x] 5.1 SC2 全量测试零失败
- [x] 5.2 平台底座全量回归零失败（`-Trigger` 为可选参数，五个既有调用方不受影响）
- [x] 5.3 `openspec validate --all --strict` 通过

## 6. 收口

- [x] 6.1 场景 `CLAUDE.md` 更新（推送形态、部署状态、L3→交付自动化的定性变更）
- [x] 6.2 回写队列：§一 `#361` 收口销号（`[S:done]`）＋ 新立 §一 `#394`（中继）与 §四 `#113`（design 审追认）
      ＋ §二 批次 `B-0825_B`。🔴 **编号按协议〇.7 走 `acquire --reserve-multi` 预留取得**——
      首次按「高水位线 +1」自行续排被 release 校验拒绝，已摘行重来（校验器另拦下两处：
      §二 状态列**整格内不得出现 ✅**、文件清单不得写裸文件名）
- [ ] 6.3 **design 审追认**：D30-D38 逐条，其中 **D33（保留每期的窗口不完整声明）**与
      **D36／未送达（本包上线后群里仍收不到）**两条须重点看；O-10／O-11 两个开放点待明示。
      🔴 **核心决策（推送形态与 IATF 定性）已由 §四 #89 拍板，本项只追认实现级推导**
- [ ] 6.4 🔴 **中继建造（不在本包范围）** —— `fi2-source-inversion` 已批同形态但 0/58 未开工。
      **中继上线前，周五 20:00 只会生成并积压，群里收不到任何东西。** 已登记队列待派；
      建议合建一份通用中继按 `scenario` 分流，而非 SC2 与 FI2 各建一份（O-10）
- [ ] 6.5 跟进信 —— 🔴 **本轮不起草，两条各自独立成立，任一条都足以拦住**：
      **⑴ 串行闸 🔒 锁**（2026-08-25 实测 `工具-跟进闸查询.py --to 姚祖怡`：
      「依据：采购部#18（待你审，暂不占号）· 🆕 待发 · 在途」，下一个可用号 `采购部#19`）
      ⇒ 按串行原则，前一封未闭环即不得起草下一封；
      **⑵ 发送硬前置未满足** —— §5 第 8 步要求「用专员原始举证的真实案例做端到端复现」，
      而**推送这一环在中继上线前根本无法端到端复现**。
      ⚠️ 即便闸日后开了，⑵ 仍然拦着：**此刻发一封「新版已上线请试用」正是 `#228` 那族形态**
      （通知已送达而那条链路其实还没通）。已登记队列，待中继上线后重评
