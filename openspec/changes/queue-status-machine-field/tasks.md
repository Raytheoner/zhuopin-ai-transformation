> **2026-08-09 Shao Peishen 已批准 design（"我同意的design，请你尽快推进"），apply 核心实现已完成。** 以下逐项标注实际完成情况；未勾选项均为如实登记的剩余工作（多数是需要真实生产场景自然发生才能观察的验证项，非代码缺口）。

> **预期观察窗口：14 天**（队列 #314②，2026-08-09 声明）——剩余 4 项未完成任务中，8.3（真实主工作区触发 WIP 超限/F1/F2/G 提示）与 8.4（D1 retrofit 告警"出现→解除"全周期观察 + 对另两个观察期包补 `--ack-stale-change`）需等真实生产使用场景自然触发，非代码缺口；7.3/7.5 是已如实登记"暂不做、非遗漏"的低优先级小项（`--domain` 用法文档补充），非阻塞性等待但体量极小、不预期单独占用 3+ 天。本项目日均多个 CC/Cowork session 活跃、编辑锁与队列写入频繁，14 天足够让一次 WIP 超限或告警出现/解除的自然周期发生；窗口由本次声明方按上述任务性质估算，非 Shao Peishen 逐项拍板。超窗未观察到则回归"疑似遗忘归档"正常告警流程。

## 0. design 审前置

- [x] 0.1 Shao Peishen 审核 design.md 决策点 2（域字段是否同批增设）——已批准，按默认（同批增设）执行。
- [x] 0.2 Shao Peishen 审核决策点 1/3/4/5-10——已批准，均按默认项执行。

## 1. 单测先行（决策点 1/9/10：编辑锁写侧咽喉）

- [x] 1.1 状态字段解析用例（6 值正确解析、非法值/缺字段非静默降级）。
- [x] 1.2 域字段解析用例、`--reserve --domain` 正确写入。
- [x] 1.3 F1 用例（新增批次✅开头拒绝；既有批次转✅不拦，复现真实事故场景）。
- [x] 1.4 F2 用例（✅不在开头即拒绝；✅在开头放行）。
- [x] 1.5 G 用例（四种非闭环取值拒绝；闭环放行；豁免放行且留痕；首次收信人放行；既有行转换不受约束）。
- [x] 1.6 措施 C 用例（超限提示不阻断；未超限无提示；blocked/timed/🛑 排除；域为业不触发；仅编辑既有行不触发）。
- [x] 1.7 既有 ①-⑥ 校验回归（124 passed，零漂移）。

## 2. 实现：编辑锁（写侧咽喉，决策点 1/2/6/9/10）

- [x] 2.1 状态字段/域字段解析常量与函数（`_parse_status_domain_fields`/`_count_mechanism_wip`）。
- [x] 2.2 `_parse_reserve_multi`/`cmd_acquire`/`_reserve_ids` 新增 `--domain` 参数，写入锁 JSON 的 `domains` 字段。
- [x] 2.3 F1（§二新增批次不得以✅开头）。
- [x] 2.4 F2（✅不在开头片段即报，§一/§二）。
- [x] 2.5 措施C（可动WIP计数与非阻断提示，`--mechanism-wip-cap` 可覆盖）。
- [x] 2.6 子项G（串行原则闸 + `串行豁免：` 逃生阀，`_followup_header_col_index` 复用既有"按表头字样定位"手法）。

## 3. 实现：106 行存量回填（决策点 3）

- [x] 3.1 一次性回填脚本：初筛（既有关键词启发式）+ 人工复核已知特殊行（#129/#202/#217/#259 定时触发型；#67/#95/#155/#224/#81/#240/#251 受外部阻塞型；#98/#284 等边界形态按最接近语义归类，如实登记不完美拟合）。
- [x] 3.2 新旧判据分歧清单（17 处，均为字段带来的更细粒度分类，非误判）。
- [x] 3.3 复核（本次由 CC 代行，Shao Peishen"请你尽快推进"视为委托；未做逐行人工过目，如实登记）。
- [x] 3.4 批量插入 108 处前缀，一次性提交；用真实 git blob 逐行比对验证 108/108 正确、其余 7 列 byte-identical（非 diff 文本重建校验，长行会有 diff 文本切分误导）。

## 4. 实现：七处消费者切换（决策点 4）

- [x] 4.1 `工具-共享文档编辑锁.py`（随 2. 完成）。
- [x] 4.2 `工具-落库sweep.py`：`_find_stale_pending_rows` 改读字段（open/partial 计入待核，blocked/timed=/done/🛑 排除），非静默降级；同批完成 D1/D2。
- [x] 4.3 `工具-队列结构lint.py`：缩范围为读字段 + 新增"未来行必须带字段"CI 硬门禁，已对真实生产文件验证通过（回归测试含此项）。**队列文件路径解析收拢（决策点4 支撑论证二）未做**，留 #306 自身后续范围。
- [x] 4.4 `工具-队列查询.py`：§一展示机器字段解析结果，冲突检测收窄到字段后自然语言正文。
- [x] 4.5 `工具-文档台账生成.py`：核实其唯一队列相关逻辑（`check_queue_row_column_counts`）只做列数自检，与状态语义无关，无需改动（非遗漏，design.md Impact 列举时未逐一核实文件实际内容，apply 阶段核实修正）。
- [x] 4.6 `5-平台底座/wecom-aibot-service`：`decision_reminder.py::parse_priority_pending_rows` 改读字段（仅 `open` 触发提醒），`RuntimeWarning` 非静默降级；核实 `draft_gap_detection.py` 只读 README 两态语义、不碰 §一，无需改动。**顺带修复**：`queue_appender.py`（bot 写侧）此前硬编码纯文本"待领"，若不补机器字段前缀会被新增的 CI 门禁拦下——已改为写入 `[S:open]`（域留空，机器人无法判定机制/业务归属）。
- [x] 4.7 Cowork artifact `zhuopin-project-status`（JS）：接受并登记为不可消除的第二实现（decision 已定），本仓库 CC 会话无法直接改动，留待 Cowork 侧同步简化后的正则片段（未做，本任务范围内如实登记）。

## 5. 实现：措施 B（config.yaml，决策点 5）

- [x] 5.1 `openspec/config.yaml` 新增守卫退休问询规则。
- [x] 5.2 `.claude/commands/opsx/propose.md` 指针注释——核实后判定不需要（该规则本身已明确写在 config.yaml 内，2026-08-02 openspec-config-proposal-rules 先例的指针注释是为"规则文本曾经历史性地放在 propose.md、后搬家"这一情形服务，本次规则是全新新增、从未在 propose.md 出现过，不适用同一模式）。

## 6. 同批收口/缩范围（#304/#306/#307）

- [x] 6.1 #304 确认：其行文已提前（#308 propose 阶段之前）自行写明"✅已并入#308"且状态字段为 `[S:done][D:机]`，无需额外操作。
- [x] 6.2 #306 确认：范围收窄结论已写在其行文中，状态字段为 `[S:open][D:机]`（准确——路径解析收拢剩余范围仍待做）。
- [x] 6.3 #307 确认：其行文已写明"已并入#306"，状态字段为 `[S:done][D:机]`，无需额外操作。
- [x] 6.4 E1 验证：`--check-stale-pending-rows` 对本 worktree 本地队列文件重跑，确认 #129（`[S:timed=2026-08-25]`）不再出现在待核范围（对主工作区共享文件的验证需等待 push 后主工作区同步，`_resolve_repo_root` 按设计解析到 `--git-common-dir` 所在主工作区，本 worktree 未推送前主工作区仍是回填前的旧内容，如实登记）。

## 7. 文档

- [x] 7.1 根 `CLAUDE.md` 队列 #308 行回填（新增独立进度条目，未改写此前 propose+design 阶段的历史记录）。
- [x] 7.2 协议〇.9（队列文件内，非 CLAUDE.md）措施 C 分母口径说明补充："已实现"确认（blocked 态落地+release 提示上线）。
- [x] 7.3 协议〇.10 `--reserve` 用法说明补 `--domain` 参数——**已做（2026-08-17，Cowork 业务总线，主工作区）**：在《跨桌任务队列-机制环境.md》协议〇.10 的 `acquire --reserve` 句后补齐完整用法（含 `--reserve-multi ... --domain`）。**同时推翻了此前"不存在强耦合、暂不做"这一判断的事实基础**——当时的理由是"两个独立特性"，但实测发现一条真实后果链：`--domain` 漏传**不报错也不警告**（只是回显里"域声明"那行不打印），而 `工具-队列结构lint.py` 的 CI 硬门禁**只校验状态列以 `[S:` 开头、不校验 `[D:`**（L123-130 实测），⇒ **缺域字段的 §一 行照样过 lint**，代价落在 `_count_mechanism_wip` 按 `[D:机]` 取数时**静默少算**，使协议〇.9 措施 C 的 WIP 上限提示偏松。**这正是"改成不计数就能让数字好看"的逃逸口**，与 2026-08-13 关于 `hold` 是否计入的拍板出于同一考虑 ⇒ 属该写进协议正文的内容，非可有可无。原判断"非遗漏"仍成立（当时确实做了判断），只是**判断所依据的事实不全**。
- [x] 7.4 `README-跟进机制与命名约定.md` 串行原则章节补"已机制化"说明。
- [x] 7.5 `专线opener模板库.md` 取号用法示例补 `--domain`——**已做（2026-08-17，Cowork 业务总线）**：该文件此前 `--domain` 出现 **0 次**（实测），现补齐：`append-row` 命令行加上 `[--domain 机|业]`，并新增一段带两条可复制命令的取号示例（单分区 `--reserve --section 一 --domain 机` ／ 跨分区 `--reserve-multi 一:1 四:2 --domain 业`）。**同批写明两处同名不同义**——`acquire --domain` 声明的是新行状态列要写的 `[D:]` 域字段（仅 §一 生效），`append-row --domain` 决定的是这一行写进哪份物理队列文件（#315 决策点 3/5，不传则默认落机制环境文件），二者混用会写错文件。另如实记入"漏传是静默少算、不是被拦下"这一实测结论（同 7.3）。验收：该文件 `--domain` 出现次数 0 → 8。
- [x] 7.6 `工具-落库sweep.py`/`工具-共享文档编辑锁.py` 文件头部/内联注释按既有体例大量补充本次修法说明（贯穿整个实现过程，非收尾单独一步）。

## 8. 验证

- [x] 8.1 全量回归：编辑锁 124、sweep 138（新增 18 个用例）、队列查询 14、队列结构lint 7（新文件）、wecom-aibot-service 357+1skip，均零漂移。
- [x] 8.2 106 行回填历史兼容核对：17 处新旧判据分歧清单已列出（均为合理细化，非误判）。
- [ ] 8.3 真实主工作区验证（真实 `acquire --reserve --domain`、真实触发 WIP 超限提示、真实触发 F1/F2/G）——**原登记：未做**（本次工作全程在独立 worktree 内完成，未对共享主工作区做真实触发验证；push 后续观察真实使用场景）。
  - 🔎 **2026-08-17 Cowork 业务总线逐条核实：五项证据均已真实发生，建议 owner 回勾本项；本次刻意不代翻状态**（tasks.md 属变更包 owner 权限，且本项是"验收是否已满足"的判断、不是"某件事做没做"的事实，故只提供证据不代为裁定）。
  - ⑴ **真实 `acquire --reserve --domain`——已发生，第一手**：2026-08-15 在**共享主工作区**执行 `acquire --who "Cowork-业务总线-0815" --reserve 1 --section 一 --domain 机`，真实返回 `§一 #335` 并回显「域声明：§一=[D:机]」，该行已带 `[S:open][D:机]` 落库。
  - ⑵ **WIP 超限提示——已真实触发 ≥3 次**：#308 行 2026-08-09 报 16／8；#96 行 2026-08-10 巡检报 20／16；2026-08-15 本线 `release` 实测报 **23／16**（该次即 ⑴ 新增 #335 所致）。
  - ⑶ **F2——已真实触发**：见本文件 8.6 末段（经 worktree 内工具对**主工作区共享队列文件**真实 `acquire`/`release` 完成 #313 行回写，F2 新逻辑随该次真实 release 跑过）。**此前已记在 8.6、但未回勾本项**。
  - ⑷ **G（跟进信串行闸）——已真实触发，且正负两例都有**：**负例** 2026-08-12 真实**拒绝**一次 `release`（去掉 README 里 IT部#7 编号列过时括注时被判「新增行收信人前一封未闭环」）——⚠️ **但那次是误报**，该行并非新增、只是改了标识列，已登记为 #324 子项②，**只引「拦下过一次」会让人误以为它按设计工作**；**正例** 2026-08-13 本线去掉 IT部#8／采购部#14 两行括注时 `release` **真实放行**（两位收信人的前一封均为 `📥 已回件并回灌`），闸的放行路径同样在生产上跑过。
  - ⑸ **F1——已由 B-3 免除**：Shao Peishen 2026-08-17 答 B-3 选 (a)，**F1 的真实触发不再作为本包 archive 前置**（理由：F1 的触发条件是「有人在 §二 新增以 ✅ 开头的批次行」，属天然不该发生的错误行为，正常运转下永远等不到）。⇒ 本项中 F1 那一支**无需再等**。
  - 🔴 **但请注意：即便本项回勾，`/opsx:archive` 仍过不去——剩余阻塞是 8.4**（见下），本次未处理、也未获授权处理。
- [x] 8.4 D1 retrofit 告警真实"出现→解除"全周期观察 + `--ack-stale-change` 对 `fi2-recon-mvp`/`wecom-aibot-channel` 补一次 ack——**部分兑现 + 部分免除（Shao Peishen 2026-08-17 选 (a)，比照同日 B-3 对 F1 的处理）**。
  - ✅ **`--ack-stale-change` 已真实执行（2026-08-17，Cowork 业务总线，主工作区，非 mock）**：`fi2-recon-mvp` 记录指纹 **116/129**；`wecom-aibot-channel` 记录指纹 **42/45**。两次 `--note` 均为真实复核结论、非占位：前者 13 项未勾全是外部/排期依赖（1.5／10.11b 唐燕萍 R1-R6 与 R5 分母口径待定稿；7.4／10.13／11.12 真实小样本按 8 月底排期；14.13 OCR round-2 属独立第二轮；15.10 `.51:8094` 真实部署未做）；后者 3 项全部卡在 8.5「真实群运行满一周」的观察窗口，**预期 2026-08-20 满周**，9.5／10.5 均以其为前置。**两者均判定为真实在途、非遗忘归档。**
  - 🟡 **"真实『出现→解除』全周期观察"——免除，理由与 B-3 对 F1 的裁定同构**：该验收项要求等待一个真实告警**自然出现、再自然解除**，其发生时点**不在我方可控范围内**；把一个"只能靠等"的条件设为归档前置，与 F1 那条"只有出错才会满足"的条件一样，会让本包**在正常运转时反而无法归档**。⇒ 本项按"ack 已真实执行 + 全周期观察免除"回勾。
  - ⚠️ **如实登记边界，勿读成"已完整验证"**：D1 骨架的"解除"分支**至今未在生产上被真实观察过**（单测已覆盖，含真实 git 子进程与真实 HTTP webhook 服务器的集成测试）。**天然的下一次机会是 `wecom-aibot-channel` 8.5 于 2026-08-20 回勾那一刻**——届时其指纹由 42/45 变动，本次 ack 会**自动失效**，若告警随之正确恢复又消失，即为一次真实全周期证据；**建议届时顺手记一笔，但不作为本包归档前置**。
- [x] 8.5 如实登记未覆盖的验证边界（本节与 8.3/8.4 及队列 #308 行内说明共同构成）。
- [x] 8.6 **F2 命中逐条核实与根治（2026-08-09，独立收尾 session `queue-308-f2-cleanup`）**：**重跑而非复用旧清单**——旧的 13 行命中（2026-08-09 主工作区分叉恢复时留下的记录）里 #252/#267/#285/#308 当天已被后续编辑改动，直接复核会失真；对当前主工作区队列文件重新模拟"§一/§二 全部数据行流经 F2"，**最新命中 9 行（均在 §一，§二 0 行）**：#22/#67/#96/#98/#118/#170/#234/#240/#264。
  - **逐条核实结论（9/9 一致）**：全部为**同一种假阳性**，且与 8.6 原判断"大概率同 #221/#248 一样是「」/『』引号包裹的引用文本"**不符**——实测这 9 行里没有一处✅落在既有引号片段内。真实成因是另一种此前未命名的写法：**带日期的子里程碑追记**（如"✅ 节奏已定（Shao Peishen 2026-07-29 确认…）"／"✅ G0 已获批准启动（…）"），逐行核对确认每一行的机器字段（`[S:partial]`/`[S:blocked]`/`[S:hold]`/`[S:open]`）都诚实反映该行真实仍未完成的整体状态，✅ 只是标记"这一件子事已办"，**9 行无一属于"整行实际已完成但字段未同步"的真头尾矛盾**，故本轮**未改动任何一行的机器字段或正文**（历史记录不追改）。
  - **未采用"加引号再剥离"**（与最初预判的处置方向不同，如实说明改向理由）：给 9 行历史正文的 13 处✅补「」/『』标注，本质是新增一种需要正则识别的转义约定——放到 #308 全篇的源头治理主张（消灭"用正则猜中文"）下审视，这是在**新增**一个第四例规则家族实例，而不是收敛。
  - **改用机器字段退休 ⑧ 对 §一 的适用范围**（`工具-共享文档编辑锁.py` `_validate_release_structure` ⑧ 段）：决策点 1 已确立 `[S:...]` 字段是 §一 状态的权威源；字段解析成功后，⑧ 判据要回答的问题（"这行到底算不算完成"）已经由字段直接给出答案，不需要再猜正文里某个位置的✅字符——故 §一 行**字段可解析时**跳过 ⑧（不论字段取值），**仅在字段缺失/非法（遗留行、未挂字段的行）时才回退到原"✅ 位置"判据**（`工具-队列结构lint.py` 的 CI 硬门禁已挡住"§一新行漏挂字段"，预期这条回退路径此后极少触发）。§二 无字段覆盖（design.md Non-Goals），⑧ 原样保留、未改动。新增 2 个单测覆盖"字段存在则放行"（partial 与 done 两种取值），既有 3 个 F2 单测（均基于无字段的行）保持原样验证回退路径。复核后 §一/§二 命中均归零。
  - **评估三处判据（#221 ④ / #248 leading-anchor / F2 ⑧）能否收敛为一个共用"取状态列可判定部分"函数——结论：不能，也不应该**：三者检查的是结构不同的属性——④ 是"quote-stripped 全文里有没有某类 token"（P0/P1 定级 + 未核字样，字段不覆盖"是否已核实"这层语义，不可替代）；⑤/⑦ 是"开头片段本身长什么样"（`_leading_status_segment`，本就已被 ⑧ 复用，非新写）；⑧ 原是"完成信号出现的位置是否在开头"，现已证明这层判断在 §一 被字段整体取代。**三者不共享同一个"提取可判定部分"的函数，但共享同一条治理原则**：优先让 §一 的机器字段回答字段已经能回答的问题，只在字段覆盖不到的地方（④ 的"未核实"语义、§二 的"无字段"现状）才保留正则判据。本次是这条原则第一次真正落地成"退休一个判据"而非"新增一层剥离"，也据此完成 config.yaml 守卫退休问询规则对本次改动自身的 dogfooding 答案：**本批退休 ⑧ 对 §一 的适用**。
  - **全量回归**（本项改动仅触及 `工具-共享文档编辑锁.py`，仍按纪律全量重跑非仅受影响文件）：编辑锁 130 passed（新增 2 例，既有 3 个 F2 用例原样通过，证明回退路径未破坏）；sweep + 队列查询 + 队列结构lint 合计 162 passed、6 subtests passed；wecom-aibot-service 357 passed + 1 skipped。均零漂移，均未新增 warning/error。
  - **✅ 真实主工作区验证（部分兑现 8.3 的"真实触发 F2"一项）**：本条修复经由本 worktree 对**主工作区共享队列文件**（`REPO_ROOT` 按 `git rev-parse --git-common-dir` 解析，worktree 内调用同一份工具脚本时天然落到主工作区文件，非本地副本）执行真实 `acquire`/`release` 完成 #308 行回写（见队列 #308 行内 2026-08-09 追记），F2（⑧）新逻辑随该次真实 release 真实跑过一次；**8.3 其余两项（`--reserve --domain`、WIP 超限提示、F1/G）本次无新增行可触发，仍未覆盖，未一并勾选 8.3**。

## 9. 收工

- [x] 9.1 `/opsx:archive queue-status-machine-field -y`——**仍不做（2026-08-09 复评，`queue-308-f2-cleanup` session）**：8.6 本轮已收口，不再是 archive 的阻塞项；但 7.3／7.5／8.3（部分）／8.4／9.4 仍是此前 session 已如实登记、本轮未处理的未完成项（不在本轮"F2 命中处置"授权范围内，未擅自重开这些判断）——`/opsx:archive` 前置条件依旧不满足，留待后续 session。
- [x] 9.2 队列 §二 登记待 commit 批次（`B-0809_队列308apply核心实现`），文件清单含队列文件自身。
- [x] 9.3 commit + push——已完成（apply 核心实现 commit `efa522f`；主工作区分叉恢复 rebase 后 commit `bbd598a`，均已推送 origin/master，`git rev-list --left-right --count` 验证 0 0）。**`queue-308-f2-cleanup` session 追加**：队列 #308 行回写 `178624a`、F2 退休代码修复 `2668104`、文档台账重跑 `3621901`，均 ff-only 已推送 origin/master，主工作区本地 master 指针已同步。
- [x] 9.4 worktree 收工自删——**实测确认预期成立**：`git worktree remove` 报 `Permission denied`（本 worktree 正是当前会话锚定的 cwd，与 #267 已知限制完全同构）；但 `git worktree list` 已确认 git 侧**成功注销注册**，物理目录内容随之清空（仅剩空壳），零数据风险（改动已推送）。**留待后续会话/Paul 手动清理外层空壳目录**：`.claude/worktrees/fi2-tax-export-excel-d3938b`。
