> **本变更 propose 阶段产出，design 审通过前不得 apply**（本行由 CC 派单指令明确要求"propose＋design 写完停下等 Shao Peishen 审，不得直接 apply"）。以下任务清单待 design.md 审核通过后再执行，当前全部保持未勾选。

## 0. design 审前置（不属于实现任务，仅供审核对照）

- [ ] 0.1 Shao Peishen 审核 design.md 决策点 2（域字段是否同批增设）——已给默认项，若不答按默认执行。
- [ ] 0.2 Shao Peishen 审核决策点 1（状态字段取值集合/语法）、决策点 3（回填复核方式）、决策点 4（消费者切换顺序/JS 第二实现处理）、决策点 5-10（措施 B/C、子项 D/F/G 的具体实现方案）——均已给默认项。

## 1. 单测先行（决策点 1/9/10：编辑锁写侧咽喉）

- [ ] 1.1 `test_工具-共享文档编辑锁.py` 新增状态字段解析/校验用例：`[S:done]`/`[S:open]`/`[S:partial]`/`[S:hold]`/`[S:blocked]`/`[S:timed=YYYY-MM-DD]` 六值正确解析；非法值（如 `[S:foo]`）与缺字段行触发非静默降级日志（不是静默回退）。
- [ ] 1.2 域字段解析用例：`[D:机]`/`[D:业]` 与状态字段并列（`[S:open][D:机]`）正确解析；`--reserve --domain` 参数正确写入。
- [ ] 1.3 F1 用例：§二 新增批次行状态列以 `✅` 开头 → release 拒绝；既有批次行从"待"改为"✅" → 不拦（复现 `B-0728财务专线核实`/`B-0728队列#125回填` 两次真实事故场景）。
- [ ] 1.4 F2 用例：§一/§二 状态列含 `✅` 但不在开头片段 → release 拒绝（复现 2026-08-03 六行头尾不一致真实事故场景之一）；`✅` 在开头片段 → 正常放行。
- [ ] 1.5 G 用例：新增跟进信登记行，其收信人前一封状态为非闭环态（分别覆盖 `⏳ 待你审`/`🆕 待发`/`⏸ 暂缓`/`✅ 已推送 …` 四种取值）→ release 拒绝；前一封为 `📥 已回件并回灌 <日期>` → 放行；新增行含"串行豁免：<理由>" → 放行且提示留痕；该收信人历史上首次出现（无前一封）→ 放行，不受本检查约束。
- [ ] 1.6 措施 C 用例（决策点 6）：新增 `[D:机]` 行使可动 WIP 计数超过 8 → release 仍放行但打印提示；计数未超限 → 无提示；`blocked`/`timed=`/`🛑` 行不计入计数（正确排除）的边界用例。
- [ ] 1.7 回归既有 ①-⑥ 校验（列数/批次清单自声明/编号预留归属/④断言门槛/⑤模糊状态/⑥跟进信暂缓一致性）在新增校验加入后行为不变。

## 2. 实现：编辑锁（写侧咽喉，决策点 1/2/6/9/10）

- [ ] 2.1 新增状态字段/域字段的解析常量与函数（取值集合、正则、`_leading_status_segment` 之前的字段剥离逻辑，剥离后交给既有关键词判据作为过渡期兼容路径）。
- [ ] 2.2 `_parse_reserve_multi`/`cmd_acquire`/`_reserve_ids` 新增 `--domain` 参数解析与写入（随取号一并声明域，决策点 2）。
- [ ] 2.3 `_validate_release_structure` 新增 F1（§二 新增批次行状态列不得以 `✅` 开头，决策点 9）。
- [ ] 2.4 `_validate_release_structure` 新增 F2（§一/§二 `✅` 不在开头片段即报，决策点 9）。
- [ ] 2.5 `_validate_release_structure` 新增措施 C 的可动 WIP 计数与非阻断提示（决策点 6），计数口径读决策点 1/2 新增字段。
- [ ] 2.6 `_validate_followup_readme_release` 新增串行原则闸（决策点 10）：收信人列索引探测、"前一封"定位、闭环态判定、`串行豁免：` 逃生阀检测。

## 3. 实现：106 行存量回填（决策点 3）

- [ ] 3.1 编写一次性回填脚本：初筛状态维度 + 域维度，产出分组预览表（按 (S,D) 组合聚类）。
- [ ] 3.2 产出"新旧判据分歧清单"（相对现行关键词判据的分歧行，逐条列出供人工过目）。
- [ ] 3.3 复核人（Shao Peishen 或显式委托）过目分组预览表与分歧清单，确认或改判。
- [ ] 3.4 按最终确认结果批量插入 106 处前缀，一次性提交（走 §二 批次流程，协议〇.7/〇.8，文件清单含队列文件自身）。

## 4. 实现：七处消费者切换（决策点 4，按顺序、影子模式、逐一回归）

- [ ] 4.1 `工具-共享文档编辑锁.py`（已随 2. 完成，本项确认其为第一个切换点，无需重复实现）。
- [ ] 4.2 `工具-落库sweep.py`：`_leading_status_segment`/`_classify_section_two_rows`/`_find_stale_pending_rows`/#302 判据 改读机器字段（§一部分；§二维持既有逻辑，Non-Goals 已述）；影子模式跑一个真实触发周期，记录分歧，分歧清零后转正；同批完成决策点 7（D1 通用骨架抽取 + retrofit 分叉/spec缺口/在途包滞留三类告警的解除通知）与决策点 7（D2 `--ack-stale-change` CLI 与指纹抑制）。
- [ ] 4.3 `工具-队列结构lint.py`（#306）：待 4.1/4.2 完成且 3. 回填验证通过后，缩范围为"读定长字段" + 队列文件路径解析收拢（决策点 4 支撑论证二），新增"未来行必须带 `[S:` 前缀"硬门禁。
- [ ] 4.4 `工具-队列查询.py`（#268）：改读字段替代头尾关键词冲突检测。
- [ ] 4.5 `工具-文档台账生成.py`：消费者切换。
- [ ] 4.6 `5-平台底座/wecom-aibot-service/aibot_service/{decision_reminder.py,draft_gap_detection.py}`：消费者切换（经 `repo_paths.py` 集中）。
- [ ] 4.7 Cowork artifact `zhuopin-project-status`（JS）：登记简化后的解析正则片段，交 Cowork 侧同步（本仓库 CC 会话无法直接改动，见决策点 4）。

## 5. 实现：措施 B（config.yaml，决策点 5）

- [ ] 5.1 `openspec/config.yaml` 的 `rules.proposal` 新增守卫退休问询规则（决策点 5 已给出草案文本）。
- [ ] 5.2 `.claude/commands/opsx/propose.md`（若适用）按 2026-08-02 `openspec-config-proposal-rules` 既有惯例补一行指针注释，说明规则已迁至 config.yaml。

## 6. 同批收口/缩范围（#304/#306/#307）

- [ ] 6.1 确认 #304（首标记冒领）随决策点 1/9 落地自动消解，队列 #304 行回填"已随 #308 消解，不再单独处置"。
- [ ] 6.2 确认 #306（队列表格七份解析实现）范围收窄为决策点 4 的"读字段 + 路径解析收拢"，队列 #306 行回填缩范围结论与降级为 P3。
- [ ] 6.3 确认 #307 并入 #306，队列 #307 行回填"并入 #306，不单独处置"。
- [ ] 6.4 E1（决策点 8）：用 #302 的 `--check-stale-pending-rows` 对回填后的队列重跑一次，确认此前误报（如 #129）不再出现，作为验证项而非独立实现。

## 7. 文档

- [ ] 7.1 根 `CLAUDE.md` 队列 #308（本行）／#304／#306／#307／#302／#248 行回填，措辞遵循"净效果是减行不是加行"的既定结论。
- [ ] 7.2 协议〇.9 措施 C 分母口径说明由"人工核定"改为"读域字段"，补 `blocked` 态兑现说明。
- [ ] 7.3 协议〇.10 `--reserve` 用法说明补 `--domain` 参数示例。
- [ ] 7.4 `README-跟进机制与命名约定.md` 串行原则章节补"编辑锁 release 时机制守，带 `串行豁免：` 逃生阀"说明，与人守时期的旧描述区分。
- [ ] 7.5 `1-转型规划/0-全景路线图/专线opener模板库.md`（如涉及取号用法示例）同步补 `--domain` 参数。
- [ ] 7.6 `工具-落库sweep.py`/`工具-共享文档编辑锁.py` 文件头部按既有体例（"背景+根因+修法+日期"）追加本次修法说明。

## 8. 验证

- [ ] 8.1 全量回归：`工具-落库sweep.py`、`工具-共享文档编辑锁.py`、`工具-队列查询.py`、`工具-文档台账生成.py`、`工具-队列结构lint.py` 各自既有测试套件零漂移。
- [ ] 8.2 106 行回填后的历史兼容核对：新旧判据分歧清单逐条有人工结论（决策点 3）。
- [ ] 8.3 真实主工作区验证：至少一次真实 `acquire --reserve --domain` 声明、至少一次真实触发措施 C 的 WIP 超限提示、至少一次真实触发 F1/F2 拒绝 release、至少一次真实触发 G 的串行拦截或豁免。
- [ ] 8.4 至少一类 D1 retrofit 的告警（分叉/spec缺口/在途包滞留）真实经历一次"出现→解除"全周期并收到解除通知；`fi2-recon-mvp`/`wecom-aibot-channel` 补一次 `--ack-stale-change` 后确认不再重复告警（D2）。
- [ ] 8.5 如实登记未覆盖的验证边界（沿用本项目"真实验证范围说明"惯例，不假装全部覆盖）。

## 9. 收工

- [ ] 9.1 `/opsx:archive queue-status-machine-field -y`（tasks 全部勾选后当场归档，不拖延）。
- [ ] 9.2 队列 §二 登记待 commit 批次，文件清单含队列文件自身。
- [ ] 9.3 commit + push；收工重跑一次文档台账。
- [ ] 9.4 worktree 收工自删；删不掉则 `git worktree prune` 并如实登记待清。
