# oem-chroma-ownership-rejudge Tasks

> ✅ **design 审已通过（Shao Peishen，2026-09-02）**：D0=(b)／D1=(b)／D2=(a)／D3=(a)／D5=(a)，D4 随 D0 成立而维持。第 2 节已全部答定。
> ✅ **2026-09-08 更新（Cowork 业务总线 `OP-0908-V`）：第 3 节载体改字已全部落地** —— 3.1／3.2／3.3（全景规划四处）／3.8（前置总表三处）／3.9（质量域重审件 §2.1 追注）／3.6 后半（隔离规范新增 **§7 运维面**）／3.10（两份 docx 重转并回读核串）／3.11（零残差自检）本次一次做完，全景规划 §0.2 已登 2026-09-08 行。**第 3 节现只剩 3.13 未做**（本次新立）。
> 🔴 **仍暂不归档，理由变了、且只剩两条**：⑴ **3.13 未做** —— 本包 spec 的运维面 Requirement 缺 D4 反向触发条件那一句（`design.md` L18 声称已写、实测无此字面），**归档即把这处不对称固化进主 spec**；⑵ **第 4 节实施整节未做**（命中 openspec 门槛③，已另立 `#465`／`#466` 承接）。
> ⚠️ **归档次序硬约束不变**：**须先 archive 本包、后 `oem-audit-fail-closed`**，反序会用过渡版盖掉定稿版（两包各持同一 Requirement 的 MODIFIED，后者为超集）。
> 〔**历史成因，保留**：本节此前长期未做，不是遗忘而是范围——落字泳道 `oem-chroma-2`（批次 `B-0902_60`，OP-0902-B1）的 opener「不做什么」明写**不改全景规划／实施计划／权威排期表**，而 3.1/3.2/3.3/3.8/3.10 正落在那三份件上，故留给队列 §一 `#374` 承接，由本次总线会话落。〕
> ⚠️ **隔离规范之所以做了（超出该泳道 opener 的「做什么」逐条清单）**：D2 裁决明令「须同步修正 spec 与 §3.2 的冲突表述」⇒ 该件本次已被打开并盖 V1.1；而其 §4 Q4 行原写「**重判出结论前 2027-01 硬截止不得放宽**」，在重判已出结论后**与裁决直接矛盾**，同件内留着这句比不改更坏。故一并收口 §4／§5，**并逐处登记未做的 §7**。该判断由落字泳道作出，如判为越界，回滚面仅限本件。
> **承接载体（三处，均已建立，非空挂）**：队列 §一 **#374**（`[S:partial]`，承载剩余载体改字）／§一 **#465**（待判：L3 本体这一轮还建不建）／§一 **#466**（待做：D2 收紧的 `router.py` 实现）。
> **归档条件**：第 3 节与第 4 节全部 [x] 后，当场 `/opsx:archive oem-chroma-ownership-rejudge -y`。

## 1. 取证与出件（本泳道已完成）
- [x] 1.1 读毕 §一 #374 全文 ＋ §四 #88 全文（后者已归档 `跨桌任务队列-归档-202608.md` L1028）【CC】
- [x] 1.2 读毕 `5-平台底座/CLAUDE.md`（隔离边界正本）、根 `CLAUDE.md` §7 红线 3 与 §5 openspec 门槛【CC】
- [x] 1.3 读毕 `3-治理与合规/OEM数据隔离规范.md` 全文 ＋ `router.py` 全文 ＋ 现行 `platform-oem-isolation` spec【CC】
- [x] 1.4 读毕四处载体现状（全景规划 L973/L1356/L1115/L1151、前置总表 Q2/Q4/§三.4、差异清单 §二.1、QD-B 场景 CLAUDE.md）【CC】
- [x] 1.5 出结论件 `1-转型规划/0-全景路线图/OEM隔离层Chroma归属重判-结论件-2026-09-02.md`【CC】
- [x] 1.6 出本变更包 proposal ＋ design 草案 ＋ tasks ＋ spec delta【CC】
- [x] 1.7 openspec 门槛自评（三条全中）并写入 proposal【CC】

## 2. design 审（✅ 已通过 —— 裁决人 **Shao Peishen**，裁决日 **2026-09-02**）
- [x] 2.1 **D0 =(b)** 已知客户 NDA **无**「物理隔离／独立实例／独立存储介质」字样 ⇒ **不列前置**，单实例多 collection 方案成立；结论件 §九.2 的 NDA 取证缺口由裁决人以既有认知消解【Shao Peishen 2026-09-02】
- [x] 2.2 **D1 =(b)** 新 Q2 采**纯规则库逐条判定**（原话「先选简单 case 的实现」），历史 8D 仅作**离线校准语料、不进生产检索路径**；三条连带见 design D1【Shao Peishen 2026-09-02】
- [x] 2.3 **D2 =(a) 收紧** 不注入 audit 即内建默认 `AuditLogger`；连默认都不可用则 `guard()`/`resolve()` 直接拒绝（fail-closed）【🔴 `compliance_redline_change`，**Shao Peishen 本人** 2026-09-02，孙涛不可代之条已满足】
- [x] 2.4 **D3 =(a)** 到期日改为可机械重算的规则，重算并入 `zhuopin-rebaseline` 重组循环固定步骤；L1 改述为「不得下线、无到期日」；🔴 **`2027-01` 作废，按规则重算为 2027-05**【Shao Peishen 2026-09-02】
- [x] 2.5 **D5 =(a)** 采纳「校验入口未建成前通用库只读」，已有内容不受影响、仅禁新写入【Shao Peishen 2026-09-02】
- [x] 2.6 审结果已回写：本 tasks ＋ `design.md`（改已审定稿）＋ 同包 spec（定稿）＋ 队列 §一 #374（`[S:blocked]`→`[S:partial]`）【CC `oem-chroma-2`】
- [x] 2.7 🔴 **D1 连带项③／D2 实现，已按裁决人明示各立独立队列行**：§一 **#465**（待判：L3 本体这一轮还建不建）／§一 **#466**（待做：D2 收紧的 `router.py` 实现，命中门槛③ 须独立变更包）【CC `oem-chroma-2`，走编辑锁 `acquire --reserve 2 --section 一 --domain 业`，一次占用】

## 3. 载体改字（2.x 已全部裁决 ⇒ 已解封；🔴 本泳道只落 3.0，其余属另批，逐条拟改文本见结论件 §八）

> 🛑 **3.1／3.3／3.8／3.10 落在全景规划与前置总表上，`oem-chroma-2` 的 opener「不做什么」明写不改那三份件 ⇒ 本泳道一律不动，非遗漏。** 承接载体＝队列 §一 #374（`[S:partial]`）。

- [x] 3.0 🔴 **（本次新增，D2 裁决直接要求）`3-治理与合规/OEM数据隔离规范.md` §3.2 补明「未注入 audit 时怎么办」** ＝ 默认 `AuditLogger` ＋ fail-closed 兜底 ＋ 过渡纪律「本行闭环前新增调用点必须显式注入 audit」；**两份权威件的直接冲突在文本侧已消解**【CC `oem-chroma-2` 2026-09-02】
- [x] 3.1 全景规划 §加速启动总览「Chroma 归属」📌 条追加重判段（结论件 §八.1⑴）✅ **2026-09-08 已做**（`OP-0908-V`）—— v6 原文一字未删，以引用块追加六段现行口径。🔴 **未照抄结论件 §八.1⑴ 的拟改文本**：那份写于 2026-09-02 design 审**之前**，其中「L2/L3 的真正主人是新 Q2 8D」与「D1 答定前 2027-01 既不得放宽也不得收紧」两句**在 D1=(b) 裁决后均已不成立**（同 3.5 已记的同类教训）；实际落字按裁决口径写＝消费方 R1／到期日 2027-05／2027-01 作废
- [x] 3.2 全景规划 §8 战略决策清单 OEM 隔离条追注（§八.1⑵）✅ **2026-09-08 已做**（`OP-0908-V`）—— 前置身份由「Q4 PPAP 前置 ＋ QD-B 现行红线」改为**按三层各自绑定**：L1 常设无到期日不得下线／L2·L3 到期日 2027-05 消费方 R1；同批点明 v6「停摆则红线落空」**错在层次**，并挂 L3 待判项 `#465`
- [x] 3.3 全景规划 L1115／L1151 两处日期复述改为指针（§八.1⑶）——**消除同一日期三处独立复述** ✅ **2026-09-08 已做**（`OP-0908-V`）—— 两处「v6 硬截止 2027-01」均改为指向 §加速启动总览 Chroma 归属条并注明 `2027-01` 已作废。🔑 **同批多清两处结论件未列的活字**（v8 §2.1.3 Q2/Q4 未闭合提醒、Q4 场景块连带段的「重判出结论前 2027-01 不得放宽」）——加删除线并追注结论；**这两处属 3.11 零残差的范围，提前在同批清掉**
- [x] 3.4 `3-治理与合规/OEM数据隔离规范.md` §4 Q4 行改写（§八.2⑴）—— 已记入重判结论 ＋ 🔴 `2027-01` 作废、按 D3 规则重算为 **2027-05**【CC `oem-chroma-2`】
- [x] 3.5 同上 §4 Q2 行追加 —— ⚠️ **§八.2⑵ 的拟改文本是按分支 A 写的（「本行现为 L2/L3 唯一有日期的消费方」），D1=(b) 后该句不成立，已据裁决改写**：Q2 **不计入** L2/L3 到期日推算，但**分库门禁照常适用、约束点仍在 2026-09 内**；同批**更正 §4 R1 行日期 2026-10 → 2027-07**（即 3.12）【CC `oem-chroma-2`】
- [x] 3.6 同上 §5 映射表按现状更新 ✅ **已做**（按 L1/L2/L3 三层重核 ＋ 逐行补承接载体 ＋ 列头日期改 2026-09-02）／新增 §7 运维面隔离要求 ✅ **2026-09-08 已做**（Cowork 业务总线 `OP-0908-V`）—— 五条：⑴ 四条通道同受 §2/§3 约束、一次操作不得无控制地同时导出多家 OEM；⑵ 导出与备份必写 audit 且必须含操作人/范围/去向，审计不可用时 fail-closed；⑶ 多 OEM 快照的存放介质按最高密级同等管控，不因「是备份」降级；⑷ 调试日志须脱敏或按 OEM 分域落盘、不得汇入共享日志流，**临时性不构成豁免**；⑸ 本节在单实例形态下不得缺省，并承接 D4 反向触发条件。同批把 §5 表末「本表未覆盖运维面…挂账 tasks 3.6」改为指向 §7 并写明不进本表的理由；§6 追 V1.5 行。 🔑 **落地时查出一处 design 与 spec 的不对称、已如实写进 §7 落实状态段**：`design.md` L18 称 D4 反向触发条件「已写入 spec 的运维面/部署形态 Requirement 语境」，但本包 `specs/platform-oem-isolation/spec.md` L65-66 该 Requirement 正文**并无此字面** ⇒ 本次在规范 §7.5 补成条文，**spec 侧同一句仍缺、须在本包归档前补齐**（见 3.13）
- [x] 3.7 同上 §6 修订记录追一行 —— **已追 V1.1 行（2026-09-02）**，批准列记「Shao Peishen 本人裁决 D2=(a)」；⚠️ 该行**已声明本次只动 §3.2**，§4/§5/§7 未动，故本项虽 [x] 但**不代表 §6 已覆盖全部改字**，其余改字落地时须再追一行【CC `oem-chroma-2`】
- [x] 3.8 前置总表 Q4 行 ／ §三 行动项 4 ／ Q2 行三处改字（§八.3）✅ **2026-09-08 已做**（`OP-0908-V`）—— ⑴ **Q4 行**：「重判前 2027-01 不得放宽」加删除线并追结论（Q4 仅存储侧身份、不再是到期日来源；`2027-01` 作废因其算法输入正是本行前置 2027-02 的余量；硬隔离要求一点没变）；⑵ **§三 行动项 4**：追注重判结论四点，🔑 并把本条判词的**第三次适用记成它的反面**——跨域基础设施不止会「失去主人」，也会**背着一个没人回头重算的日期继续跑**（`2027-01` 历经 v7／v8 两次改 Q4 都无人回头算）；⑶ **Q2 行**：状态列追「隔离前置」段——归集时即分库、**约束点在 2026-09 内**，并写明**不因 D1=(b) 豁免**的判据（检索路径 vs 数据落盘是两件事）
- [x] 3.12 🔴 **（本次顺带查出并已改）隔离规范 §4 R1 行原写「R1（2026-10）」**，系 v6 整域顺延（R1→2027-07，前置约 2027-05）前的旧值 —— 已更正为 2027-07 并注明顺延成因是公司级优先级调整、非 9/30 条件未达；本行现为 L2/L3 唯一有日期的消费方【CC `oem-chroma-2`】
- [x] 3.9 `质量域场景重审-…-2026-08-21.md` §2.1 末句追注指向结论件（§八.5）✅ **2026-09-08 已做**（`OP-0908-V`）—— 以引用块追在「已登记 §五 收尾动作②」之后，**原文一字未改**；四条结论逐条对位本节：⑴ 本节「Q4 挂靠点已被拆掉」的推断成立（Q4 降为存储侧身份）；⑵ 🔴 **本节末句猜的「新 Q2 反而可能成为新挂靠点」被 D1=(b) 否掉**，但 Q2 分库门禁照常适用、约束点在归集期 2026-09 内；⑶ `2027-01` 作废、按 D3=(a) 重算为 **2027-05**（消费方退回 R1）；⑷ L1 无到期日不得下线，L3 建不建升为 `#465`
- [x] 3.10 🔴 触碰全景规划正文 ⇒ 用 `0-学习与工具/md转Word工具/md2word.py` **重转两份 docx**，入同批 commit（根 `CLAUDE.md` §5 排期同步纪律）✅ **2026-09-08 已做**（`OP-0908-V`）—— 两份均 `saved` 退出码 0：`卓品智能AI转型全景规划.docx`（143,437 B，892 段/17 表）＋ `卓品智能AI转型实施计划（最新版）.docx`（77,934 B，229 段/11 表）。🔑 **不是「跑完就算」——用 `python-docx` 回读正文＋全部表格单元格逐串核**：全景规划 5/5 命中（`L2/L3 到期日 ＝ 2027-05`／`2027-01` 已作废／`错在层次`／`§7 运维面`／`OP-0908-V`），实施计划 2/5 命中且**缺的三串正是它没改的内容**（符合预期，非漏转）
- [x] 3.11 grep 全库零残差自检（`2027-01`／`Chroma 归属`／`隔离层就绪`／`ISO 26262` 与 OEM 隔离共现处）✅ **2026-09-08 已做**（`OP-0908-V`）。🔑 **先定口径再动手，否则会误伤**：`2027-01` 全库 **301 处 / 95 件**（不是队列旧记的 213/51），但**绝大多数与本行无关**——SC10/FI10 排期、单测夹具日期、销售域 v5 旧值等都长这样；**只有「OEM 隔离层硬截止」这一语义的才算残差**，故按语义模式扫（`重判出结论前.*2027-01`／`v6 硬截止 2027-01`／`重判前 2027-01`／`隔离层就绪`）而非按裸日期扫。 **⇒ 活字面已清零，四处已改**：`卓品智能AI转型实施计划（最新版）.md` L71 Q4 行（权威件）＋ L163 甘特改指针；`5-平台底座/平台底座架构设计（AIOps讨论稿）.md` L166（并写明 per-OEM Collection 设计动作在 `#465` 判定前不排期）；`6-人才与组织/工程研发AI-Champion-首月任务单（模板）.md` L44（「隔离层就绪（9 月）」是 v6 前旧值，改为 R1 2027-07／前置 2027-05／L2 到期日 2027-05）；另全景规划 Q4 场景块标题「OEM 隔离层就绪」**精确到层**改为「L1 就绪」。 **⇒ 余下命中一律属「历史记录不追改」，逐类点名以免下一个读者再扫一遍**：结论件 §八 拟改文本（历史产出件）／`session接力-…-归档-202608.md`／`跨桌任务队列-归档-202608.md`／`进度编年-CHANGELOG.md`／全景规划 §0.2 与 `全景路线图重组机制与变更日志.md` 的变更日志行／`质量域场景重审-…-2026-08-21.md`（其 §2.1 已按 3.9 追注）／实施计划 L453 v8 增补行；**加了删除线的原句仍会被 grep 命中，属正常、不再计残差**。 ⚠️ **`ISO 26262` 一项未逐处核完**：全库 20+ 件命中，多为 ASIL C/D 红线的常规引用、与本次重判无交集；**已核两份权威件（全景规划／实施计划）无「把功能安全与数据隔离混为一谈」的表述**，其余按低风险留待下次巡逻，**如实标注、不谎报全扫**
- [ ] 3.13 🔴 **（2026-09-08 `OP-0908-V` 落 3.6 时新增）本包 `specs/platform-oem-isolation/spec.md` 的「运维面 SHALL 同受隔离约束」Requirement 补 D4 反向触发条件一句** —— `design.md` L18 声称该条件「已写入 spec 的运维面/部署形态 Requirement 语境」，实测正文（L65-66）无此字面；规范侧已于 §7.5 补成条文。**须在本包归档前补齐**，否则归档后主 spec 与规范会出现「一边有、一边无」的不对称，重演 §3.2 那类两份权威件冲突。⚠️ **本项改 spec 文本、须走 design 审**（改的是 Requirement 正文，非注释）

## 4. 实施（🔴 另派泳道，本包不做；`#374` 明写"不改隔离层实现代码"）
- [ ] 4.1 按 D2 裁决改 `router.py` 审计注入语义 ＋ 同步 spec ＋ 单测回归 —— ✅ **承接载体已建：队列 §一 #466**（2026-09-02 立）
- [ ] 4.2 按 D5 裁决实现通用库写入校验入口（或先置只读闸）—— ⚠️ **可由 #466 同包承接，由该行领取方判；两条不得都无人承接**（已写进 #466 行内）
- [ ] 4.3 `rag.retrieve()` 唯一入口建造（L2）——**排期由 D1/D3 的到期日规则推出 ＝ 2027-05**（D1=(b) 后最早消费方＝R1 2027-07／前置约 2027-05）
- [ ] 4.4 L3 Chroma 本体部署（IT 侧；⏭️ 触碰生产面，本包永不代办）—— 🔴 **建不建本身已成待判项，承接载体＝队列 §一 #465**

## 5. 收口
- [x] 5.1 队列 §一 #374 已回写（`[S:blocked]`→`[S:partial]`，原状态原文保留不删）；**未销号** —— `期望产出` 列明写「四处载体同批改字」而本次仅落其一【CC `oem-chroma-2` 2026-09-02】
- [x] 5.2 `3-治理与合规/OEM数据隔离规范.md` §6 修订记录已追 V1.1 行（同 3.7；该行已声明本次只动 §3.2）
- [ ] 5.3 tasks 全 [x] 后当场 `/opsx:archive oem-chroma-ownership-rejudge -y`（根 `CLAUDE.md` §5 完工即归档纪律）
      🔴 **本次不归档，理由见本件顶部 `暂不归档` 段**（第 3 节余 9 项 ＋ 第 4 节 4 项未做，均属另批/另行，承接载体 ＝ §一 #374／#465／#466）。判定器＝`0-学习与工具/工具-变更包自动归档.py`。
