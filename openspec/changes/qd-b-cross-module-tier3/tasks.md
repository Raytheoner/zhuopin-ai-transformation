# tasks —— QD-B 档三（C05/C06/C07）

## 1. 取数侧（模块四 ＋ 风险里程碑列）

- [x] 1.1 `parser.py::_parse_objectives_table` → `doc.tables["项目目标"]`（逐行取，跳「…」占位行）
- [x] 1.2 风险表补 `所属里程碑` 与 `行号` 两列
- [x] 1.3 单测：空白模板 6 固定标签在册且「…」不入表；华丰 9 行（含 3 行自定义、内容不丢）；风险里程碑列

## 2. 判定词表（单一可信源）

- [x] 2.1 新建 `rules/vocab.py`：`ASIL_NA_SYNONYMS`（Q2 封闭六项，含「无」）＋ `is_na_synonym`／`is_as_not_filled`
- [x] 2.2 `AS_NOT_FILLED_SYNONYMS` 取别名而非副本（Q3 选 (b)：复用 Q2 那组）
- [x] 2.3 空值一律不视同 NA（Q2 后半句），由调用方各自处置

## 3. C05（Q1：(c) ＋ 两条护栏）

- [x] 3.1 `data/rules/cross_module_c05.json`：豁免词表 ＋ 里程碑-阶段映射表 ＋ `_确认状态`／`_逐条取证`
- [x] 3.2 `_c05` 判定次序：漏填 → 豁免 → 相等 → 包含 → 映射表白名单 → 错误
- [x] 3.3 相似度只写进证据、永不作通过依据（含「不予采信」措辞）
- [x] 3.4 每条 C05 结论自带「本表待陈忱确认」
- [x] 3.5 单测：10 条判例逐条锁死（1–6 通过／7·10 错误／**8 不得通过**／**9 须豁免**）
- [x] 3.6 单测：映射表不得含第 8 条那条错映射（防后人"顺手补全"反转他的 ❌）

## 4. C06／C07（Q2/Q3/Q4）

- [x] 4.1 `_c06`：ASIL 未解析→转人工；空→转人工（写明扣分归规则 12）；NA 同义词→不适用；否则查模块四
- [x] 4.2 `_c07`：ISO21434 选项**不在表里**→转人工（Q4 (b)）；在表未勾→不适用；已勾→查模块四
- [x] 4.3 `_goal_verdict`：模块四未解析／无此行→转人工；空或「视为没填」词→错误；否则通过
- [x] 4.4 单测：六种 NA 写法、空判漏填、Q3 五种「视为没填」、华丰 C07 转人工且证据说清"不是未勾选"

## 5. 规则 12 并入「无」

- [x] 5.1 `r12_asil` 改走 `vocab.is_na_synonym`，不再字面枚举
- [x] 5.2 空值文案更正为「漏填」（行为不变）
- [x] 5.3 before/after 实测三样本对照（EQ17 98.80→100.00；华丰 96.44、邦奇 94.89 不变）

## 6. 黄金基准签认闸

- [x] 6.1 产出《基准变更说明》`docs/基准变更说明-规则12并入无-2026-08-25.md`
- [x] 6.2 `manifest.md` 与 `GOLDEN` **均不改**；EQ17 得分断言 `xfail(strict=True)`
- [x] 6.3 `PENDING_BASELINE` ＋ 断言"新值＝引擎真实重跑值"
- [x] 6.4 ✅ **签认已获并落地**（Shao Peishen 2026-08-25，§四 #112 选 (a)）：manifest 记入 100.00 ＋ `GOLDEN` 改 100.00 ＋ 摘 xfail 标记 ＋ 删 `PENDING_BASELINE`；变更依据与签认记录已写进 manifest《基准变更记录》节与《说明》frontmatter

## 7. 落库与呈现

- [x] 7.1 `.gitignore` 加 `!data/rules/*.json`（`*.json` 会静默吞掉判定表 → 静默判错）
- [x] 7.2 单测：表文件存在且非空
- [x] 7.3 三载体（文本／网页／Excel）跟随更新并锁死一致
- [x] 7.4 `summarize()` 计入 MANUAL；`status_label()` 不再有"未实现"分类

## 8. 收口

- [x] 8.1 全量回归：**181 passed / 1 xfailed / 0 skipped**（原 135 passed / 6 skipped）
      ➕ 签认落地后复跑：**181 passed / 0 xfailed / 0 skipped**（那 1 条 xfail 即本闸，标记已摘）
- [x] 8.2 场景 CLAUDE.md §5 状态时间线 ＋ 组 4 完成度更新
- [ ] 8.3 ⏸ 🔴 **「LAN 留步」**：`.51:8093` 部署 ＋ 三样本冒烟（发布四关）——签认阻塞已解除，
      改由**本机 off-LAN**（2026-08-25 波次前提）阻塞；代码侧已就绪，回 LAN 一个小批即可补。
      补法：`sync-to-server.ps1` → `Restart-ZhuopinTask` → `/api/ping` 200 ／首页 200 ／
      三份真实样本重传比对 `data/golden/manifest.md` 真值（EQ17 须为 **100.00·合格**）。
- [ ] 8.4 ⏸ **待前信闭环＋签认后**：向陈忱回发两份 C05 表请确认（质量部#9，串行闸见 README）

> **暂不归档**：6.4 已于 2026-08-25 签认落地；**8.3／8.4 两项仍未完成**——8.3 卡 off-LAN、
> 8.4 卡串行闸与陈忱确认两份 C05 表。本变更包**在 8.3 完成前不归档**
> （`/opsx:archive` 待 8.3 完成后执行）。⚠️ 另有一项独立于本清单的未裁定口径：
> 「档三三条是否应具备阻断效力／进扣分」本包判定为「不进」（design 决策 4），**须陈忱裁定**，
> 归队列 §一 #340 承接。
