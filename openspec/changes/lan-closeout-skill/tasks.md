# lan-closeout-skill Tasks

> 🛑 design 审通过前全部不得开工。

## 1. skill 本体
- [x] 1.1 定稿 `0-学习与工具/skills源码/zhuopin-lan-closeout/SKILL.md`（2026-08-29 审过即定稿转生效）【Cowork】
- [x] 1.2 save_skill 安装引用式指针（2026-08-29，触发词「我已回Lan」）【Cowork】
- [x] 1.3 指针回读验证（安装即入会话技能清单）【Cowork】

## 2. 接线
- [ ] 2.1 三源留步清单汇集逻辑核对（扫描器形态 1 输出格式／队列「LAN 留步：」grep 口径／看护件节名）【Cowork/CC 只读】
  - 🔴 **缺口并入（2026-08-31，`OP-0831-L` 第二批 ⑴ 核实中实证，Shao Peishen 当日答 ㉔ (a)）**：现行汇集只按「LAN 留步」字样 grep，扫出来后又因行状态 `[S:blocked]` 被排除 ⇒ **「回 LAN 这个动作本身会解除一整类 `[S:blocked]`」这件事，汇集逻辑从未表达**——`#419`／`#354` 这类"回 LAN 就能重新可动"的项永远轮不到。**拟修法**：汇集逻辑须增加一条——阻塞理由含 `off-LAN`／`不在网`／`无法取证` 的 `[S:blocked]` 行，在 on-LAN 时视为可动候选（不是自动改判，是"值得人读一遍判断"）。⚠️ **`OP-0831-L` 第二批 ⑴ 实测复核** 时发现：`#419`/`#354` 的**当前**阻塞理由实际都已不是"off-LAN 无法取证"字面（`#419` 现卡在待他拍板修法方案；`#354` 的 LAN 缺口 08-27 已补完、行内明写"不要再当 LAN 留步捞出来"）——**说明本缺口不只是"扫描器排除了本该可动的行"，还有"人转述阻塞理由时容易滞后于行内最新记载"这一半，两者都要汇集逻辑之外的读行判断兜底，不能指望关键词匹配单独解决**。详见队列 §一 #419／#354 本轮回写。
- [ ] 2.2 sweep「回 LAN 翻转推送」文案补一句：提醒他说「我已回Lan」即可收口（行动入口指路）【CC，随下次 sweep 触碰】

## 3. 首跑验收（灰度）
- [x] 3.1 首跑限 2 项、他在场；`.51` 项走完"快照→部署→冒烟→回写"全链一次【他＋CC】——2026-08-31 `OP-0831-L`：入选 `#403`（只读）+ `#394`（写），全程 Shao Peishen 在场，`#394` 完整走完快照（`.env` 双备份 SHA256 一致）→部署（写入 `WECOM_AIBOT_OUTBOX_PATHS`，重启服务）→冒烟（`outbox_relay_started` + 2 条真实送达 `errcode:0`）→回写（队列 `#403`/`#394` 状态列已追记）全链一次。
- [x] 3.2 复盘三判据：误收 0／不合格登记全部点名／每项证据链齐全——全过放开【Cowork】——2026-08-31 三判据自评全过（详见看护件 §五 收工汇总），**灰度摘除，第二批起用默认上限 6**（Shao Peishen 2026-08-31 当场确认）。
  - [ ] 3.2.1 **续批缺口（2026-08-31 `OP-0830-A` 并入，Shao Peishen 当日答 (a)；🔴 首跑前只登记、不改执行编排）**：步骤 6「收工」之后**没有第 7 步**，链条到汇总即止，**"清单还剩什么"与"起下一批"两件都是人守**；且步骤 6 只要求汇报「本批每项一行」，**未要求汇报剩余项数** ⇒ 第一批跑得漂亮、汇总也推了企微时，**剩余项不产生任何信号**（同族＝「写下需要动作、却没有承接方」）。**拟修法（复盘时定夺）**：收工汇总末尾加一行「本批 N 项／清单剩余 M 项；M>0 时提示可再说一次『我已回Lan 上限X』续批」。**为何不当场改**：改的是 `0-学习与工具/skills源码/zhuopin-lan-closeout/SKILL.md` 执行编排正本，而首跑必须跑 design 审过的那一版，否则首跑三判据失去参照。**佐证＝「常规三查」已含「上一收口批汇总已出」，该检查的存在本身就说明设计意图是每批由人重新触发**——本项补的不是触发方式，是**剩余量的可见性**。【Cowork，随 3.1 首跑复盘一并处置】🔴 **首跑已实证复现（2026-08-31）**：本批 opener 步骤 4 显式写了「skill 步骤 6 不含这一项…本批手动补上」——首跑真实印证本条诊断成立，skill 自身收工步骤确实不产生「清单剩余 M 项」这个数，全靠 opener 撰写方手动补一条指令才没漏；若下次 opener 忘记补这句，缺口会原样复发。修法仍留复盘处置，本条只登记「诊断已被真实首跑验证，非推测」。
