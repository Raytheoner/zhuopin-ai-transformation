# lan-closeout-skill Tasks

> 🛑 design 审通过前全部不得开工。

## 1. skill 本体
- [x] 1.1 定稿 `0-学习与工具/skills源码/zhuopin-lan-closeout/SKILL.md`（2026-08-29 审过即定稿转生效）【Cowork】
- [x] 1.2 save_skill 安装引用式指针（2026-08-29，触发词「我已回Lan」）【Cowork】
- [x] 1.3 指针回读验证（安装即入会话技能清单）【Cowork】

## 2. 接线
- [ ] 2.1 三源留步清单汇集逻辑核对（扫描器形态 1 输出格式／队列「LAN 留步：」grep 口径／看护件节名）【Cowork/CC 只读】
  - 🔴 **缺口并入（2026-08-31，`OP-0831-L` 第二批 ⑴ 核实中实证，Shao Peishen 当日答 ㉔ (a)）**：现行汇集只按「LAN 留步」字样 grep，扫出来后又因行状态 `[S:blocked]` 被排除 ⇒ **「回 LAN 这个动作本身会解除一整类 `[S:blocked]`」这件事，汇集逻辑从未表达**——`#419`／`#354` 这类"回 LAN 就能重新可动"的项永远轮不到。**拟修法**：汇集逻辑须增加一条——阻塞理由含 `off-LAN`／`不在网`／`无法取证` 的 `[S:blocked]` 行，在 on-LAN 时视为可动候选（不是自动改判，是"值得人读一遍判断"）。⚠️ **`OP-0831-L` 第二批 ⑴ 实测复核** 时发现：`#419`/`#354` 的**当前**阻塞理由实际都已不是"off-LAN 无法取证"字面（`#419` 现卡在待他拍板修法方案；`#354` 的 LAN 缺口 08-27 已补完、行内明写"不要再当 LAN 留步捞出来"）——**说明本缺口不只是"扫描器排除了本该可动的行"，还有"人转述阻塞理由时容易滞后于行内最新记载"这一半，两者都要汇集逻辑之外的读行判断兜底，不能指望关键词匹配单独解决**。详见队列 §一 #419／#354 本轮回写。
  - 🔴 **缺口的第三面：假阳性，且比前两面更容易被信（2026-09-01 08:16 实证，`OP-0901-A` 逐行核）**。sweep 在探到 off→on 时向运维群推送「下列 **8** 条 LAN 留步现在可以补做了」，列出 `#312`／`#334`／`#340`／`#354`／`#394`／`#401`／`#418`／`#422`。**逐行核对当前状态字段，8 条里零条有效**：`#401` 已是 `[S:done]`（前一日 `OP-0831-L` 销号）；`#394` 前一日已执行完毕（outbox 中继首条真实送达）；`#354` 前一日已改判 `[S:timed=2026-09-03]`、行内明写"不要再当 LAN 留步捞出来"；`#340` 前一日读行复核确认 LAN 缺口 08-27 已自解；`#334` 子项⑺前一日已完成；`#312`／`#418`／`#422` 三条在前一日看护件 §〇 落选表里**逐条点名过**（元讨论／人工门禁永不代办／判据方向错误）。
    🔴 **其中 `#422` 是最能说明问题的一条**：它的判据是「**要先真的离开内网**」⇒ **on-LAN 恰恰做不了**，而 sweep 偏偏是在「刚回到内网」这个时刻把它推出来的——**推送时机与该行自身判据的方向正好相反**。
    🔑 **⇒ 修法范围须从「补捞漏掉的」扩大到「先排掉已经不该在里面的」**：汇集逻辑当前只按「LAN 留步」字样 grep，**既不读行的当前状态字段（`[S:done]` 照推）、也不读行内后续的改判记载（"已自解""不要再当 LAN 留步"照推）**。最低限度须加两道过滤：⑴ 状态字段已 `[S:done]` 的行直接排除；⑵ 行内含"已自解／已解除／不要再当 LAN 留步捞出来"类否认表述的，降为「需人读一遍」而非直接列入可补做清单。
    ⚠️ **本条的危害形态＝「错误不产生任何信号」**：那条推送有条数、有格式、有队列链接、有 UTC/本地双时刻，**看起来完全正常**；只有把 8 行逐条拿去比对状态字段才会发现一条有效的都没有。**⇒ 而一份"看起来正常"的清单，正常反应是照着做，不是去核它。** 与 `#82`（告警建成 9 天每天在跑却一条没真发出）同族——**都表现为拿到一个看起来很正常的结果**。
    🟢 **缺口的第三面已修复并验收（2026-09-01，`OP-0901-B`，CC，worktree `lan-collect-filter-0901-f973af`，分支 `claude/lan-collect-filter-0901-f973af`）——判据→证据→处置**：
    **判据**：最低限度加两道过滤，且只做**降级**不做**沉默排除**（除 ⑴ 有全项目共用机器字段撑腰的那一种）：⑴ 状态字段已 `[S:done]` 的行直接排除（落 `done_excluded`）；⑵ 命中格内含否认表述（`不要再当 LAN 留步捞出来`／`已自行解除`／`早已闭合`／`本轮属扫描器形态 1 误报`／`不再成立`／`已收口`／`已解除`／`无需任何补做`）的行降级为 `needs_review`（需人读一遍），机器不据此判定已闭合（同本行判据 ⑶ 既有教训：识别"已闭合"结论段本质是猜中文——`#340`「已补做完成」与 `#334`「仍成立」两句字面结构几乎相同、语义相反）。🔴 判据 ⑵ 的范围刻意取「同一格」，不是「同一段」也不是「整行」——段级会漏 `#394`（LAN 声明与"已收口"结论分属状态格里的不同段，新证据分批追加、顺序不可控）、行级会误伤 `#312`（LAN marker 落在与否认表述完全无关的另一格）。
    **证据**（对 08:16 那 8 条真实回归，逐条去留理由，`工具-未闭合产出扫描.py --skip-lan-probe` 实跑核验）：
      - `#401` → 状态字段 `[S:done]`，过滤 ⑴ 排除。
      - `#354` → 命中格含「不要再把它当 LAN 留步捞出来派单」＋「已自行解除」，过滤 ⑵ 降级。
      - `#340` → 命中格含「早已闭合」「本轮属扫描器形态 1 误报」，降级。
      - `#334` → 命中格含「不再成立」，降级。
      - `#394` → 命中格（同一状态格、不同段）含「已收口」，降级——**验证了判据 ⑵ 的下限**：若范围收窄到"同一段"会漏掉这条。
      - `#418` → 命中格含「已解除」（原句「硬阻塞已解除，现在缺的是一次明确批准」——技术阻塞已消但人工门禁未过），降级。
      - `#422` → 命中格含「已解除」「已收口」，降级——**如实登记：命中多为该行讨论扫描器自身缺陷时引用 `#340`/`#47` 等示例连带触发，非精确针对本行**；结果方向仍正确（该行真判据是"要先真的离开内网"，与推送时机"刚回内网"相反，本就不该判可补做），但机制精度属巧合、非本次两道过滤的设计目标。
      - `#312` → LAN marker 落在「任务」格（描述 lan-closeout-skill 自身设计范围的元讨论，非真实留步登记），命中格内无否认表述 ⇒ **两道过滤均未捕获，仍留在 `items`**。**如实登记为已知残留**：这是本次两道过滤能力范围之外的第三种假阳性形态（"元讨论/描述性提及"，既不是"已完成"也不是"已否认"），不是漏做，是本次任务书界定的"至少两道过滤"没有覆盖到的第三类。
      - **过滤前 8 条 → 过滤后 `items` 仅剩 1 条（`#312`）**；对当前完整 13 条 form1 命中的全量结果：`items` 2 条（`#312`／§四`#131`，后者是本次收工后新出现、非当日 8 条之一，同属"未捕获"残留）、`needs_review` 9 条、`done_excluded` 2 条（`#401`／`#402`）。
    **处置**：代码改动集中在 `0-学习与工具/工具-未闭合产出扫描.py`（新增 `DENIAL_MARKER_RE`／`_hit_cell_indices`／`_row_has_denial_signal`，`scan_form1` 返回新增 `needs_review`／`done_excluded` 两键，`scan()`/`write_state()` 同步把 `needs_review` 计入"当前"避免被误判"✅已解除"，`cmd_ack_form1` 同步认 `needs_review` 为可确认对象，`format_report` 新增两小节）；`0-学习与工具/工具-落库sweep.py` 同步（`_check_unclosed_outputs` 日志同行回显两桶计数；`_announce_lan_flip` 的"现在可以补做了"推送**只计入 `items`**，`needs_review` 绝不进清单、仅附一句 FYI；`items` 为空但 `needs_review` 非空时日志明确区分于"真零条"）。单测：`test_工具-未闭合产出扫描.py` 新增 `Form1FalsePositiveFilterTests` 10 项（含"同一格"范围上下限两个反例、否定前缀反例、与既有 `--ack-form1` 机制的正交性、今日 8 条代表样本回归）；`test_工具-落库sweep.py` 新增 6 项。全量回归：`test_工具-未闭合产出扫描.py` 69 passed；`test_工具-落库sweep.py` 全量与 `UnclosedOutputGuardTests`（30 项含新增 6 项）均绿。`SKILL.md` 步骤 1 与新增同目录 `CHANGELOG.md` 已同步说明两道过滤的存在与用法。
    **未解决（如实登记，非本次范围）**：⑴ 2026-08-31 并入的假阴性半边（阻塞理由含 off-LAN 的 `[S:blocked]` 行在 on-LAN 时视为候选）**本轮未做**——任务书明确"先做排除、后做补捞"，当前假阳性已从 100%（8/8）降至可控（1/8 残留），补捞留后续任务；⑵ `#312`／`#422`（及后续可能出现的同族行）建议由人读一遍后用 `--ack-form1` 记录确认，机制侧暂不新增第三道"元讨论识别"过滤（避免重蹈"猜中文"覆辙，成因见判据段）。
- [ ] 2.2 sweep「回 LAN 翻转推送」文案补一句：提醒他说「我已回Lan」即可收口（行动入口指路）【CC，随下次 sweep 触碰】

## 3. 首跑验收（灰度）
- [x] 3.1 首跑限 2 项、他在场；`.51` 项走完"快照→部署→冒烟→回写"全链一次【他＋CC】——2026-08-31 `OP-0831-L`：入选 `#403`（只读）+ `#394`（写），全程 Shao Peishen 在场，`#394` 完整走完快照（`.env` 双备份 SHA256 一致）→部署（写入 `WECOM_AIBOT_OUTBOX_PATHS`，重启服务）→冒烟（`outbox_relay_started` + 2 条真实送达 `errcode:0`）→回写（队列 `#403`/`#394` 状态列已追记）全链一次。
- [x] 3.2 复盘三判据：误收 0／不合格登记全部点名／每项证据链齐全——全过放开【Cowork】——2026-08-31 三判据自评全过（详见看护件 §五 收工汇总），**灰度摘除，第二批起用默认上限 6**（Shao Peishen 2026-08-31 当场确认）。
  - [ ] 3.2.1 **续批缺口（2026-08-31 `OP-0830-A` 并入，Shao Peishen 当日答 (a)；🔴 首跑前只登记、不改执行编排）**：步骤 6「收工」之后**没有第 7 步**，链条到汇总即止，**"清单还剩什么"与"起下一批"两件都是人守**；且步骤 6 只要求汇报「本批每项一行」，**未要求汇报剩余项数** ⇒ 第一批跑得漂亮、汇总也推了企微时，**剩余项不产生任何信号**（同族＝「写下需要动作、却没有承接方」）。**拟修法（复盘时定夺）**：收工汇总末尾加一行「本批 N 项／清单剩余 M 项；M>0 时提示可再说一次『我已回Lan 上限X』续批」。**为何不当场改**：改的是 `0-学习与工具/skills源码/zhuopin-lan-closeout/SKILL.md` 执行编排正本，而首跑必须跑 design 审过的那一版，否则首跑三判据失去参照。**佐证＝「常规三查」已含「上一收口批汇总已出」，该检查的存在本身就说明设计意图是每批由人重新触发**——本项补的不是触发方式，是**剩余量的可见性**。【Cowork，随 3.1 首跑复盘一并处置】🔴 **首跑已实证复现（2026-08-31）**：本批 opener 步骤 4 显式写了「skill 步骤 6 不含这一项…本批手动补上」——首跑真实印证本条诊断成立，skill 自身收工步骤确实不产生「清单剩余 M 项」这个数，全靠 opener 撰写方手动补一条指令才没漏；若下次 opener 忘记补这句，缺口会原样复发。修法仍留复盘处置，本条只登记「诊断已被真实首跑验证，非推测」。
