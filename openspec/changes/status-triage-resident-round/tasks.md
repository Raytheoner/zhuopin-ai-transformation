# status-triage-resident-round Tasks

> **🔴 全表未勾、未开工。** 本包止步于 design 审（`OP-0906-K` 第 3 步）。**§0 全部通过前，§1 起的任何一项都不得开工。**

## 0. design 审前置（阻断全部后续项）

- [ ] 0.1 **前置登记**：Shao Peishen（或孙涛代理）答 design D4 —— (a) 只检测＋告警＋给可粘贴命令 ／ (b) 自动追加 §四 行 ／ (c) 不做 ⑵。**本项无默认值**；答 (b) 则 spec 须整体重写（见 spec 顶部说明）后再回 design 审一次。
- [ ] 0.2 **前置登记**：答 D3 —— 是否同意退休 release 校验 ⑨ 的候选接线（one-in-one-out 的退休对象）。
- [ ] 0.3 **前置登记**：定 `#454` 与 `#462` 的先后次序（两包触碰同一处 sweep 常驻轮次接线，`#454` 行内 2026-09-02 已记「交值周定」，至今未定）。
- [ ] 0.4 `openspec validate --strict status-triage-resident-round` 通过。

## 1. 否定词表与回测（前置 0.1/0.4）

- [ ] 1.1 定稿否定词表，每条附一个真实来源行号（起点四条：`已闭合`／`已解除`·`依赖解除`／`误报`·`假阳性`·`误命中`／`已由 Shao Peishen`·`已答`·`已批准`）。
- [ ] 1.2 **回测阻断项**：对 2026-09-06 实测 8 条逐条回测，须达「`#340`／`#470`／`#471`／`#472` 全降弱档，`#455`／`#394`／`#418` 全保强档」。**不达标即停，不得继续 §2。**
- [ ] 1.3 回测结果原文写进本文件（命中/降档逐行列出），不只写「通过」。

## 2. 只读候选出口（编辑锁侧，前置 1.2）

- [ ] 2.1 `工具-共享文档编辑锁.py` 新增只读子命令 `triage-candidates --json`：扫指定队列文件 §一，输出候选 JSON。**不 acquire 任何锁、不写任何文件。**
- [ ] 2.2 该子命令复用既有 `_suggest_status_reclassification()`／`_table_data_rows()`，**不新写解析**。
- [ ] 2.3 单测：① 不持锁即可运行；② 运行后 `git status --porcelain` 为空；③ 两份队列各自可指定。

## 3. sweep 第 12 类接线（前置 2.3、0.3）

- [ ] 3.1 新增 `_check_status_triage_candidates(repo_root, log)`：子进程调 2.1，分强弱两档，强档走 `_track_and_alert_standing_state`（key ＝ 队列|行号|措辞，节流 24h），弱档只回显。
- [ ] 3.2 子进程不可用时按第 10 类同形告警「判据不可用」，**不得判为零候选**。
- [ ] 3.3 接进 `main()` 非 dry-run 分支，排在第 11 类之后；异常捕获后继续主流程、不影响退出码。
- [ ] 3.4 单测（**派单件第 2 步点名的场景**）：构造「候选存在但当前 session 未被 WIP 拦」的夹具（`wip_count <= cap`），断言候选仍被产出并进入回显/推送渲染。
- [ ] 3.5 单测：反引号奇偶——命中片段含奇数反引号时输出已成对或已去除。
- [ ] 3.6 单测：零候选时回显行仍出现。

## 4. 决策台账缺口检测（前置 0.1 答 (a)；答 (b) 本节作废重写）

- [ ] 4.1 新增 §四 结构化解析（sweep 当前无）：取 §四 全表（含已结案行）正文里的全部 `#N`。
- [ ] 4.2 扫描面独立于分诊器：覆盖 `blocked`，不受 open/partial 限制。
- [ ] 4.3 两份队列行号空间重叠时 **报错而非静默匹配**（design D7 已知边界）。
- [ ] 4.4 渲染可粘贴的 `append-row --section 四` 草稿命令；业务域来源须在正文点明「跨文件登记，请确认归属」。
- [ ] 4.5 单测（**派单件第 2 步点名的场景**）：构造「§一 行内含『待 Shao Peishen』自陈、§四 无对应 `#N`」的夹具，断言该行被汇总进缺口清单并渲染出草稿命令。
- [ ] 4.6 单测：§四 已结案行提及即算已覆盖，不进缺口清单。
- [ ] 4.7 单测：运行后两份队列逐字节不变、零锁 acquire。

## 5. 退休 release ⑨ 接线（前置 0.2）

- [ ] 5.1 移除 release 校验 ⑨ 内 `_suggest_status_reclassification()` 调用与 `_mechanism_wip_over_cap_violations()` 的 `reclass_candidates` 形参。
- [ ] 5.2 `_suggest_status_reclassification()`／`_render_reclassification_candidates()`／`STALE_STATUS_PHRASES` 原样保留；既有单测保留（只改断言中不再出现候选清单的那一条）。
- [ ] 5.3 单测：超限拒绝文案不再含候选清单，但仍含 WIP 计数与「两条出路」。

## 6. 真实数据验证（前置 3.6、4.7、5.3）

- [ ] 6.1 对两份队列**真身**跑一次 `--dry-run`，输出与本包 design §1 的实测数（8 候选／3 强档／5 弱档／≤4 台账缺口）逐条比对，差异须解释。
- [ ] 6.2 `0-学习与工具` 单测全量绿、零回归。
- [ ] 6.3 企微推送**首次真实触发**留痕（运维群，非业务群）。

## 7. 收口（前置 6.3）

- [ ] 7.1 `工具-CI覆盖率护栏.py` 通过。
- [ ] 7.2 知识资产台账登记（proposal §知识资产三问第 3 条，apply 时执行）。
- [ ] 7.3 回写队列 `#454` `[S:done]` ＋ 登记 §二 批次。
- [ ] 7.4 `/opsx:archive status-triage-resident-round -y`。
