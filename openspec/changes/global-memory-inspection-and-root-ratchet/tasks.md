# tasks — global-memory-inspection-and-root-ratchet

> **✅ design 审已过（Shao Peishen 2026-08-29，答「审过」，D1-D6 六个决策点整体通过、无点名修改）。** 执行环境：**CC**（写生产码、自行 commit+push、一任务一 worktree）。
> ✅ **可开工**：2026-08-30 分诊改判 8 行后机制类可动 WIP 27 → 19，`#435` 已摘 🛑 转待领（现 20／22）。

## 0. 前置

- [x] 0.1 design 审通过（Shao Peishen 2026-08-29 答「审过」，记录于队列 §一 `#435`）
- [x] 0.2 WIP 回落至上限内，`#435` 摘 🛑 转待领（2026-08-30 分诊改判 8 行，27→19，实测用闸自身 `_count_mechanism_wip`）

## 1. 实现

- [ ] 1.1 `工具-落库sweep.py` 新增常量：`GLOBAL_MEMORY_TARGETS` / `GLOBAL_MEMORY_BYTE_CAP` / `ROOT_RATCHET_SLACK_BYTES` / `ROOT_RATCHET_MARGIN_BYTES`
- [ ] 1.2 `CLAUDE_MD_ROOT_BYTE_CAP`：`90 * 1024` → `48 * 1024`，常量处注释写明棘轮语义与「只降不升」
- [ ] 1.3 新增 `_check_global_memory_files()`：A1 路径存在性 / A2 尺寸 / A3 版本快照，三判据各自可独立关停
- [ ] 1.4 路径抽取器 `_extract_local_paths()`：认 `C:\…` 与 `~/…`；`~/` 按 `USERPROFILE` 展开；含空格片段整段取（本机用户目录含空格）
- [ ] 1.5 版本快照匹配器：排除路径内版本段（`Python314`／`nodejs`）与队列编号（`#NNN`）
- [ ] 1.6 受检对象缺失 ⇒ 告警且不中止整轮（D4）
- [ ] 1.7 棘轮提示行接入既有回显；告警去重复用 `CLAUDE_MD_SIZE_STATE_REL`，新增独立键

## 2. 测试（零回归为硬门槛）

- [ ] 2.1 A1 单测：真路径存在 / 真路径已迁走 / 散文误认 / `~` 展开 / 含空格路径
- [ ] 2.2 A3 单测：命中 `v2.1.x`；**不**命中 `Python314`、`nodejs`、`#422`
- [ ] 2.3 D4 单测：受检文件不存在时告警且后续检查照跑
- [ ] 2.4 棘轮单测：低于阈值 1,024 B 以上出提示；超阈值出告警
- [ ] 2.5 全量回归绿、零回归（对照当前基线数）

## 3. 真实验活（不是 mock）

- [ ] 3.1 对**真实** `~/.claude/CLAUDE.md`（2026-08-29 已订正版）跑一次：应零路径红、零版本快照红
- [ ] 3.2 造一次真阳性复验：临时在副本里写一条已失效路径，确认能报出行号与原文，**核完删副本**
- [ ] 3.3 root 棘轮回显核对：当前 47,863 B / 阈值 49,152 B，确认差额与提示行数值正确
- [ ] 3.4 连跑两轮确认 24h 去重生效（第二轮不重复刷同一条）

## 5. 子项 E — 状态字段与自陈一致性检查（2026-08-30 并入，`工具-共享文档编辑锁.py`）

- [ ] 5.1 措辞集常量 `STALE_STATUS_PHRASES`：硬阻塞／已押后／待 Shao Peishen／待拍板／需人在场／留步／常驻不销 等，**可增不可删**（每条附一个真实来源行号做注释）
- [ ] 5.2 `_suggest_status_reclassification()`：扫 `open`/`partial` 行，命中即产出 (行号, 现字段, 建议字段, 命中原话片段)
- [ ] 5.3 接入 release 的 WIP 阻断消息：现有两条出路之外附带候选清单
- [ ] 5.4 🔴 **只建议不自动改**；🔴 **不改 `_count_mechanism_wip` 计入口径**（D7：partial 里确有真可动者）
- [ ] 5.5 单测：命中 `#380` 那类原话；**不**命中「Cowork 半完成、CC 半待领」那类（`#96` 反例，防误伤）
- [ ] 5.6 真实回归：对 2026-08-30 改判前的队列快照跑，应能列出当天人工分诊出的那 8 行（**这是本子项唯一的验收判据——列不全就是漏报**）

## 4. 收口

- [ ] 4.1 值周巡检清单加一行「全局记忆巡检有红即当周处理，不跨周」（D6）
- [ ] 4.2 §二 批次登记 ＋ commit+push（ff-only）
- [ ] 4.3 队列行回写：状态列开头 ✅ ＋ 实测数字
- [ ] 4.4 全部 [x] 后**当场** `/opsx:archive global-memory-inspection-and-root-ratchet -y`
