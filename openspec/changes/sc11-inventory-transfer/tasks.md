# 任务 · SC11 库存智能调拨系统（队列 §一 #469）

> 🔴 **1.0 是硬闸**：本包 `design.md` 尚未撰写、design 审未过（🟡 档）。
> 第 2 组之后各项在 design 审通过前不得开工。
> 🔴 **另有两处须 design 审先裁决**：proposal「本包的两条边界判断」⑴ 四条原则的转录/自拟分界；
> ⑵「物流距离矩阵」是否补进前置总表（属规划文档改动，须走移交单）。

> 🔴 **本包 `暂不归档`（文本标记，机器认得的三条入口之一——`工具-落库sweep.py::STALE_CHANGE_DEFER_MARKER`）** —— 2026-09-03 `OP-0903-C1` 留。
> **理由**：本包已跑 `/opsx:sync`（增量 spec 已并入 `openspec/specs/sc11-transfer-planning/spec.md`），但 tasks **13/34 项已勾**，第 2 组前置到位闸整组未满足：`design.md` 未写且 design 审未过（🟡 档）；业务前置窗口在 **2027-02～03**，其中「3 委外仓库存实时可见」为内网通路、off-LAN 期间无法验证。
> ⚠️ **不是遗忘归档，是它还没完工，且距最早窗口尚有约 5 个月** —— 属「作者明知长期不会归档」，故用永久文本标记而非观察窗口。

## 1. 骨架期（不依赖前置，`OP-0903-B2` 已完成）

- [x] 1.1 **PMC 确认门禁**（先于算法落地）：`gate.approve()` 实名确认；`commit_to_erp()`／`notify_outsourced_warehouse()` 未确认即抛
- [x] 1.2 门禁不接受旁路参数：测试用 `inspect.signature` 断言执行侧只有 `draft` 一个入参
- [x] 1.3 已确认后仍抛 `NotWiredYet`（通道未接）：**不静默 no-op** —— 否则会产出『落库成功』的 audit 而 ERP 里什么都没有
- [x] 1.4 外发授权与调拨确认分离：错误文案写明「两件事、两道授权」，测试断言
- [x] 1.5 前置逐项实读并落进代码：`pending.py` 四项（含据实拆出的物流距离矩阵）
- [x] 1.6 结构模型：`Warehouse`／`TransferDemand`／`TransferCandidate`（源=目标即拒）／`TransferPlanDraft`（默认未确认，带假设顺序）
- [x] 1.7 四条原则可度量建模：正文逐字转录；缺数据返回「无从判断」而非 0（拦"缺数据冒充最优"）
- [x] 1.8 `rank_candidates()` 要求四条齐全的优先级顺序显式传入，少一条即抛
- [x] 1.9 需求分解走底座 `explode_bom`，逐计划展开保住上线日；计划未给上线仓即抛，不默认落到任一委外仓
- [x] 1.10 候选枚举：跳过目标仓自身、只收够货的仓；一条都满足不了落 `unmet` 不静默丢弃
- [x] 1.11 入口与留痕：`run_draft()`，`evaluator` 非空强校验，audit 带假设顺序 ＋ 四项 `blocked_by`
- [x] 1.12 测试：22 tests 全绿（门禁一组写在最前）
- [x] 1.13 场景 `CLAUDE.md` 六段式 ＋ `README.md`

## 2. 前置到位闸（🔴 未满足即不开工第 3 组之后）

- [ ] 2.1 本包 `design.md` 撰写并过 design 审（🟡 档，须 Shao Peishen 拍板）
- [ ] 2.2 「物流距离矩阵及其口径」补进前置总表 —— ✅ **裁决已下**：Shao Peishen 2026-09-03 `EE-5=(a)` 补进；落法已并入移交单 §3.3bis（含取证 §2.4、类型建议＝混合、最迟开工日 2027-02-04，与既有 2027-02 窗口同窗）。🔴 **本项仍未勾**：实际补行由全景路线图线执行、至今未落地；打标与 Owner 待业务侧签认。📌 **顺带**：移交单已一并要求补 §一.2 台账里缺失的 `SC11 调拨原则口径` 条目（该项在 §一 已标知识型，台账却无条目）。
- [ ] 2.3 生产计划数据通路接通（姚祖怡 + PMC + IT，窗口 2027-02～03）
- [ ] 2.4 3 委外仓库存实时可见（IT；🔴 内网通路，off-LAN 期间无法验证）
- [ ] 2.5 调拨原则口径经姚祖怡批改会签认、backup 实名点名
      > 🔴 起草面向姚祖怡的跟进信前必须先查串行闸：`python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`；
      > 信状态唯一权威＝跟进信 README「发送状态」列，tasks 只留指针、不复述闸状态。
- [ ] 2.6 物流距离/时长矩阵及其口径确定，持有人实名点名

## 3. 口径落地（依赖 2.5/2.6，不得自拟）

- [ ] 3.1 把签认后的优先级顺序落成一份**可版本化的配置**（不硬编码；旧拟稿须能被认出是旧口径产的，同 SC4 `lexicon_id` 做法）
- [ ] 3.2 距离矩阵载入，含口径标注（公里/车程/班次）
- [ ] 3.3 🔴 单测须断言：口径配置缺失/版本不匹配时 fail-loud，**不得回退到任何内置默认**
- [ ] 3.4 🔴 单测须断言：`rank_candidates` 仍不接受不全的优先级顺序（防实现期顺手加默认）

## 4. 真实数据接入（依赖 2.3/2.4）

- [ ] 4.1 生产计划源接入，替换 `plan_warehouse` 手工映射
- [ ] 4.2 委外仓实时库存源接入
- [ ] 4.3 替代料处理（规划原文「解析生产计划 BOM 明细（含替代料）」）—— 复用底座 BOM 替代料字段 `sequence`/`is_substitute`，不另立一套
- [ ] 4.4 部分调拨拆分规则（口径来自 2.5，本骨架期只产整条满足的候选）

## 5. 执行通道（🔴 全程受 PMC 门禁约束）

- [ ] 5.1 ERP 库存模块写入通道接入；`commit_to_erp` 移除 `NotWiredYet`，**保留确认闸**
- [ ] 5.2 🔴 单测须断言：接入后未确认仍抛 `PmcApprovalRequired`（门禁不得随通道接入而失效）
- [ ] 5.3 邮件外发通道接入；🔴 **须另取发信授权**，不因 PMC 已确认调拨内容而自动获得
- [ ] 5.4 月中临时任务实时决策（规划点名，本包未起）—— 须先判定是否引入 LLM 判断，若引入则同批建黄金集

## 6. 收口

- [ ] 6.1 真实数据验证（档2）→ `/opsx:archive` → commit + push
- [ ] 6.2 场景 `CLAUDE.md` 更新（含部署状态段）
- [ ] 6.3 发布收口：统一门户路由 `/procurement/sc11`（🔴 不新起端口）＋ 部署段基本测试 ＋ 回滚 SOP ＋ 灰度反馈入口
