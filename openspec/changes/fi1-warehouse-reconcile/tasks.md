> ⏸ **本变更包整体处于「条件复启暂缓」状态（见 proposal.md 顶部说明），非在办**。以下未勾选项非当前待办，复启前不主动推进。

## 1. design 收口与前置（apply 前确认；🔴 项阻断真实结案，不阻断 mock 开发）

- [ ] 1.1 收口-1：财务对接人交付差异分类规则 + L2 阈值（7/31）——未交付前用 Paul 临时口径占位跑 mock（临时口径已落 config + 规则注册表）
- [ ] 1.2 收口-2：确认标准损耗基准归属（工艺 BOM `m_scrap` vs 财务另立标准损耗表）
- [ ] 1.3 收口-3：确认产出/完工数量口径（`MO.FinishedQty` 是否=对账期产出 + 批次/在制/跨期归属）
- [ ] 1.4 收口-4：确认实际投料权威源 entity（SMT 线体投料过账 vs 工单领料 `MOPickList`）
- [ ] 1.5 收口-5：与 Paul 约定 MO/领料 CommonEntity 读提升进 ZpConnector 的时机（端点开放后另起变更）
- [ ] 1.6 前置登记：IT 开放 U9C MO/领料/出入库 webapi 端点（或 LAN/VPN）= 最终 U9C 直读前置

## 2. feed-source 数据接入层（mock 先行，先测后实现）

- [x] 2.1 定义投料/产出/BOM 统一加载接口与 `data_source` 三源开关（mock/csv/u9c，依赖注入，仿 SC8 sources）
- [x] 2.2 写测试：mock 加载产出/投料、Pydantic 边界校验（缺料号/数量/类型非法显式报错）
- [x] 2.3 写测试：`csv` 应急桥接加载（贴 U9C 语义字段，边界校验）
- [x] 2.4 写测试：`u9c` 端点不可达 fail-loud 抛 `RealEndpointNotReadyError`，不回退 mock/csv
- [x] 2.5 实现 mock 加载器 + 贴 U9C 实体 schema（`MO`/`MOPickList`）的 mock 夹具
- [x] 2.6 实现 `csv` 应急桥接 loader（ERP 导出投料/产出 CSV → 统一记录）
- [x] 2.7 实现 BOM 加载：调 `ZpConnector.get_bom_for_products`，消费 `(rows, failed_ids)`，残缺标"待人工核"
- [x] 2.8 实现 `u9c` MO/领料 `CommonEntity/Query` 读接口（端点占位 fail-loud；保持可提升进 ZpConnector 的接口形状）

## 3. reconcile-engine 纯算法对账引擎（先测后实现）

- [x] 3.1 写测试：理论净用量（单成品、多成品共用子件累加、产出为零、BOM 残缺）
- [x] 3.2 写测试：标准损耗基线 = 理论净用量 × `m_scrap`
- [x] 3.3 写测试：总差异（正/负）+ 差异率（含理论为 0 → NA·无理论基准）
- [x] 3.4 实现毛理论用量 / 标准损耗 / 总差异 / 差异率纯函数（无副作用）
- [x] 3.5 全部引擎单测绿

## 4. variance-classify 差异分类（规则注册表，先测后实现）

- [x] 4.1 设计规则注册表结构 `{规则ID, 条件, 分类档, 严重度, 是否触发 L2}` + 版本号
- [x] 4.2 写测试：标准损耗内→损耗溢短 / 超损 / 来料短缺/盘点疑点 / 临时口径占位标记
- [x] 4.3 用 Paul 临时口径填注册表基线
- [x] 4.4 实现分类引擎（按注册表判定，分类标准不写死代码）+ 规则版本登记
- [x] 4.5 分类单测绿

## 5. recon-report 聚合 + L2 门禁 + audit（先测后实现）

- [x] 5.1 写测试：逐料号报告契约（含理论/标准损耗/实际/差异/差异率/分类/需人工/版本/数据源）
- [x] 5.2 写测试：L2 超阈值→`需人工确认`不自动结案；阈值内→`AI 建议通过`仍待复核
- [x] 5.3 写测试：审计写入（数量为主、金额脱敏/聚合、原始单价不落 AI 侧）
- [x] 5.4 实现报告聚合 + "AI 对账建议非终局"标注 + BOM 残缺/无基准料号单列
- [x] 5.5 实现 L2 门禁（阈值 configurable）+ 写平台 `AuditLogger`（scenario=FI1）
- [x] 5.6 聚合/门禁/审计单测绿

## 6. 黄金基准回归（合成）

- [x] 6.1 构造合成对账 golden 样本（已知差异定位结论，覆盖各分类档）存 `data/golden/`（合成可入库）
- [x] 6.2 写黄金回归测试：引擎确定性零偏差 + 分类档与预期一致
- [x] 6.3 全场景回归绿（含 real fail-loud 行为冒烟）+ `python -m fi1.run` 端到端跑通

## 7. 真实数据验证（过渡期 = CSV 应急桥接；前置 1.1）

- [ ] 7.1 ERP 导出投料/产出 CSV → feed-source `csv` 切真实拉投料/产出（BOM 已真实）
- [ ] 7.2 小样本真实对账试跑，对接人核对账规则口径
- [ ] 7.3 对接人交付历史人工对账后：替换合成 golden 为真实 golden，回归零漂移
- [ ] 7.4 （最终切换，前置 1.6）IT 开放 MO/领料端点后切 `u9c` 直读，零改对账逻辑；评估读是否提升进 ZpConnector（收口-5）

## 8. 收口归档

- [x] 8.1 更新场景 `CLAUDE.md` 进度 + 状态
- [ ] 8.2 `/opsx:archive` → git commit + push
- [ ] 8.3 若实现时间较规划有变，记入场景进度并提示 Paul 通知 Cowork 回填路线图（CC 不自行改规划）
