# Tasks — project-hooks-write-time-sentinels

> 🛑 **design 审过前不得勾任何一项。** 审阅人＝Shao Peishen（队列 §一 #433 棒 A1 收工报告已注明）。
> 顺序有硬依赖：§1 → §2/§3（可并行）→ §4 → §5 → §6 → §7。**§7 降指针必须最后做，且有前置条件。**
>
> ✅ **apply 已完成到「可安装状态」（2026-08-29，`CC-OP0829A`，队列 §一 #433 建造棒）**：§1／§2／§3／§4.1-4.3／§5／§6.5-6.6／§8.1 共 36 项已勾，端到端单测 **36 passed**，`openspec validate --strict` 绿。
> ⏳ **未勾的 12 项全部卡在同一个前置上：安装单尚未由 Shao Peishen 执行**（§4.4 → §6.1-6.4 生产验活 → §7 降指针；§7.1 明写「apply 完成 ≠ 可以降指针」）。安装单＝`0-学习与工具/hooks/README-安装步骤.md`。
> 🔭 **预期观察窗口：21 天**（＝等安装 ＋ warn 两周实测 2026-08-29→2026-09-12；Shao Peishen 2026-08-29 答第 3 问选 (a)）。**故本包按「完工即归档纪律」的第二条出路显式声明观察窗口，不是忘了归档**——窗口内 sweep 应报「🔭 观察中」而非「疑似遗忘归档」。

## 1. 公共框架

- [x] 1.1 建 `0-学习与工具/hooks/`（🔴 **不是** `.claude/hooks/`——后者命中 protected-paths 且实测连只读都拦，见 design 已否决 C）
- [x] 1.2 写 `hooks-common.ps1`：stdin JSON 解析出写入目标路径与本次新增内容
- [x] 1.3 全逻辑包 `try{...}catch{ exit 0 }`（fail-open）；异常摘要进心跳
- [x] 1.4 心跳 `reports/hooks-heartbeat.json`：单文件、定名、覆盖写；**每次运行都写，含放行**
- [x] 1.5 时刻一律显式标基准（本机时区，形如 `2026-08-29T09:04:31+08:00`）；日期一律 `Get-Date` 当场重取
- [x] 1.6 拦截留痕复用 `~/.claude/audit-blocks-<date>.log`，不新造日志形态
- [x] 1.7 行内留痕式逃生阀通用解析（`<哨兵名>豁免：<理由>`；无理由不生效）
- [x] 1.8 单测：框架五条 Requirement 的全部 Scenario
- [x] 1.9 🔴 反例单测：**正文提到 `~/.claude/hooks/` 不得被误判**（本班亲身踩中，见 design 决策点 2）
- [x] 1.10 🔴 反例单测：**脚本抛异常必须放行，且心跳仍落盘**

## 2. H3 乱码哨兵

- [x] 2.1 写 `0-学习与工具/hooks/sentinel-mojibake.ps1`
- [x] 2.2 判 U+FFFD 与 BEL 等控制字符；白名单放行 `\t` `\n` `\r`
- [x] 2.3 目标路径本身也判（2026-07-04 事故形态＝路径与内容同现）
- [x] 2.4 二进制目标放行且不读内容
- [x] 2.5 代码块／块引用／`乱码豁免：` 三类不判
- [x] 2.6 反馈含行号 ＋ 行内偏移 ＋ 码点
- [x] 2.7 单测：spec 全部 Scenario，**含"同一坏字节在代码块内放行、在正文内拦截"这对对照**

## 3. H4 代词哨兵

- [x] 3.1 写 `0-学习与工具/hooks/sentinel-pronoun.ps1`
- [x] 3.2 🔴 名录**从 `6-人才与组织/人员名录-称谓与性别-正本.md` 读取，脚本内零硬编码**
  - 🔴 前置：该正本由并行棒 `OP-0828-Q` 产出。**若届时尚未落库，本节停手不做替代方案**（临时硬编码一份＝造第二份名录）
- [x] 3.3 判定限「同段落内名录人名 ＋ 其后最近第三人称代词」；多人同段且性别不同 ⇒ 放行
- [x] 3.4 名录外人物不触发
- [x] 3.5 块引用／代码块／`代词豁免：` 不判（历史记录不追改）
- [x] 3.6 🔴 名录缺失或解析出 0 人 ⇒ 输出「本类无法核验：<原因>」并放行，**禁止输出「未发现违规」**
- [x] 3.7 反馈逐条含行号 ＋ 人名 ＋ 写成的代词 ＋ 正确代词
- [x] 3.8 单测：黄金测试集 7 个名字（`祖怡`／`燕萍`／`映桦`／`植雅`／`易水`／`姣龙`／`国庆`）各一条写反用例，须全部判违规
- [x] 3.9 反例单测：块引用内旧信原文放行；同句在正文内拦截
- [x] 3.10 反例单测：名录缺失时输出**不得**含「未发现违规」

## 4. 挂接（含一次人工，不绕）

- [x] 4.1 写好项目 `.claude/settings.json` 的 `PostToolUse` 挂接**片段**（两条 matcher，各 `timeout: 10`），放进安装单，**不由 CC 写入该文件**
- [x] 4.2 🔴 出一份 `0-学习与工具/hooks/README-安装步骤.md`，**与 #398 那份至今未安装的 SessionStart 心跳钩子合并成一次人工动作**（沿用 `0-学习与工具/定时任务源码/health-check-signal/README-安装步骤.md` 的体例）
- [x] 4.3 安装单须写明"为什么这一步只能你手动做"（protected-paths `*/.claude/settings.json`，`mode: block`；绕过一次这条守卫此后就不再是守卫）
- [ ] 4.4 ⏳ **等 Shao Peishen 执行安装**

## 5. 自证在岗（准入条件，不可省）

- [x] 5.1 「最近 N 天零心跳即告警」的承接方——**优先并入落库 sweep 的常驻告警框架，不新建后台任务**（理由见 design 决策点 1）
- [x] 5.2 🔴 告警判据必须能被「心跳恢复」自动关掉（不得造出永远红着的告警）
- [x] 5.3 反例单测：心跳文件是坏 JSON 时，检查本身必须出声，不得静默

## 6. 生产验活（🔴「测试通过」不算）

- [ ] 6.1 H3 在生产工作区**真实拦下一次**，`audit-blocks` 有记录（贴原文进收工报告）
- [ ] 6.2 H4 在生产工作区**真实拦下一次**，`audit-blocks` 有记录
- [ ] 6.3 手工把挂接改坏一次 ⇒ 零心跳告警在约定窗口内响；改回 ⇒ 自动解除
- [ ] 6.4 🔴 **补答 P0-1 未答那半问**：请 Shao Peishen 在 Cowork 做一次受控 Edit，查 `audit-blocks` 与心跳有无该次记录；结论回写 design §五（10 分钟）
- [x] 6.5 `git check-ignore -v reports/hooks-heartbeat.json` **重跑一次**并把输出贴进收工报告（本棒是在 2026-08-28 的 `.gitignore` 上实测的，apply 时可能已变）
- [x] 6.6 `openspec validate --strict` 绿

## 7. 降指针（最后一步，有前置条件）

- [ ] 7.1 🔴 **前置**：§6.1 已完成（H3 在生产真实拦下过一次）。**apply 完成 ≠ 可以降指针**
- [ ] 7.2 把根 `CLAUDE.md` §5「乱码文件夹哨兵」条中**「开工与收工各查一次上级目录」那半条动作**降为一行指针
- [ ] 7.3 🔴 **只降那半条**：同条「出现乱码兄弟目录即停手、按既例隔离、不直删」的处置流程**保留原样**（H3 不覆盖事后处置）
- [ ] 7.4 在队列 §一 #284「规则退休制阈值计数台账」登记本次「人守 −1、机制守 +2」

## 8. 收口

- [x] 8.1 场景/工具侧文档更新（`0-学习与工具/hooks/README`）
- [ ] 8.2 队列 §一 #433 回写；§二 批次登记；commit + push
- [ ] 8.3 重跑文档台账
- [ ] 8.4 🔴 `/opsx:archive project-hooks-write-time-sentinels -y`（完工即归档，不拖到下次 session）
