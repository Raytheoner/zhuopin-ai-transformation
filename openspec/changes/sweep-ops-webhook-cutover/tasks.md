# sweep-ops-webhook-cutover Tasks

> 🔴 **本包尚未获审，以下全部为 apply 阶段任务，一项都不得提前动手。** 派单件 OP-0819-G 与 `#282` 行内均明写「propose ＋ design 后停下等 Shao Peishen 审」。
> 各任务后括注对应 `design.md` 决策点；**若审核改选了某个非默认项，对应任务须按新选项重写后再执行**。

## 0. 开工前置（apply session 必读，勿跳）

- [ ] 0.1 **在主工作区执行**，不在本 worktree 跑真实告警（决策点 7a）——本 worktree 的 `.env` 持有 `_OPS` **第一版无效值**（key 指纹 `ad11552c`，探针 `93000`），在此处验收会得到一个看起来像代码缺陷、实为配置陈旧的假故障
- [ ] 0.2 重新核一次 `工具-落库sweep.py` 的行号（文件可能已变动）：常量 1 处、`_load_webhook_url` 调用点 10 处、键名字面量 13 处；与 `design.md` §零 的实测清单比对，有差异即以实测为准并记录
- [ ] 0.3 复核 `_count_mechanism_wip`：本包不取号，WIP 应仍为 24／24 不变

## 1. 主体改动（`0-学习与工具/工具-落库sweep.py`）

- [ ] 1.1 L437 常量 `WECOM_WEBHOOK_ENV_KEY` 由 `"WECOM_WEBHOOK_URL"` 改为 `"WECOM_WEBHOOK_URL_OPS"`（决策点 1a）
- [ ] 1.2 13 处键名字面量改为从常量派生（决策点 3a）：L994／L1012／L1042(docstring)／L1114／L1155／L1181／L1774／L1798／L1844／L2062／L2073／L2709
- [ ] 1.3 **10 处调用点一处不碰**——改完 `git diff` 自检：改动应只落在 L437 与上述 13 行附近，不应出现在任何 `webhook_url = _load_webhook_url(repo_root)` 行
- [ ] 1.4 L1049 `prefix = WECOM_WEBHOOK_ENV_KEY + "="` 不改（引用常量，自动跟随）；确认前缀匹配仍带 `=`，否则两个互为前缀的键会互相误命中
- [ ] 1.5 改完全库 grep 自检：`工具-落库sweep.py` 内不应再出现 `WECOM_WEBHOOK_URL` 字面量（`_OPS` 那个常量值除外）

## 2. 单测（比照 FI2 `resolve_alert_webhook` 三用例，决策点 2a）

- [ ] 2.1 只有 `WECOM_WEBHOOK_URL_OPS` 时正常取到
- [ ] 2.2 **只有裸 `WECOM_WEBHOOK_URL` 时返回 None**（绝不回退，本包最硬的一条）
- [ ] 2.3 两者并存时只取 `_OPS`
- [ ] 2.4 两键互为前缀不误命中（`.env` 同时含两行时各取各的）
- [ ] 2.5 去向键缺失时某类告警被跳过、`main()` 退出码不变
- [ ] 2.6 推送抛异常时退出码不变（若既有用例已覆盖则复用、不重复造）

## 3. 决策点 4 若选 (a)：`alert_webhook.py` 同批改

> 若审核选 (b)，本节整节删除并在收口时登记为 `#282` ⑴ 包后续动作。

- [ ] 3.1 `5-平台底座/wecom-aibot-service/scripts/alert_webhook.py` 凭据来源改为**仓库根 `.env`** 的 `WECOM_WEBHOOK_URL_OPS`（`5-平台底座/.env` 里既无 `_OPS`、裸键值又长 0，只改键名等于没改）
- [ ] 3.2 配单测覆盖「读根 `.env` 的 `_OPS`」与「缺失时不回退裸键」
- [ ] 3.3 🔴 **收口时明写：本项未经真实验证，且在 §四 `#68` 生产载体（worktree `ops/wecom-service-home`，落后 246 提交）同步前不生效** —— 不得写成 ✅，不得计入已验收项

## 4. 文档

- [ ] 4.1 `3-治理与合规/通知通道架构决策件-webhook退役与aibot单一出口-2026-08-06.md` §5.1 就地标注：「替换 `WECOM_WEBHOOK_URL` 现值」这条约定**已作废**，取代方案为新增 `_OPS` 键分流，理由＝裸键同时承载 `发企微.py` 的业务内容通道（决策点 1）。**原文不删**（历史记录不追改）
- [ ] 4.2 §四 通道矩阵「运维逃生通道」一行补注实际键名为 `WECOM_WEBHOOK_URL_OPS`

## 5. 验收（档 2，真实投递；mock 证明不了本包要证明的事）

- [ ] 5.1 **正判据**：真实触发一条 sweep 机制告警，确认它**进的是运维群**
- [ ] 5.2 🔴 **负判据（止血是否成功的真正判据）**：确认**采购内部工作群不再收到**同一条告警。⚠️ 只验 5.1 不足以收口——它只证明"多了一个收件人"，不证明"少了一个"
- [ ] 5.3 `reports/sweep-commit.log` 按**宽口径**（行首 ✓ 且含「已推送」）统计切换日之后的增量，业务群新增应为 0。⚠️ 口径写死为宽口径——窄口径会漏掉「常驻服务部署提示已推送」这一类，同一份日志两种数法差一条
- [ ] 5.4 sweep 全量回归零漂移
- [ ] 5.5 跨子项目回归零漂移（至少：编辑锁、队列查询、队列结构 lint、台账；若动了 3.x 则加 `wecom-aibot-service`）
- [ ] 5.6 `openspec validate --all --strict` 全绿
- [ ] 5.7 rebase 到 `origin/master` 后**重跑一遍**再 push（不只在 rebase 前测过）

## 6. 收口

- [ ] 6.1 回写 §一 `#282`：⑴ 包第一个动作已完成、押后理由①经复核不成立、状态字段由 `hold` 转 `open`／`partial`
- [ ] 6.2 回写 §四 `#73`：止血已落地并经真实投递验证（含 5.1／5.2 两侧证据）
- [ ] 6.3 §二 登记批次，文件清单每条为仓库根相对完整路径且反引号包裹
- [ ] 6.4 🔴 **URL 值一律不落任何文件、报告、commit message**，只用 key 参数的 SHA256 前 8 位指纹
- [ ] 6.5 `/opsx:archive` —— **仅当上述全部 `[x]` 时才做**；3.3 那类"已改未验证"项未闭合即不得 archive，不假装完工
