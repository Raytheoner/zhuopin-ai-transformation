# sweep-ops-webhook-cutover Tasks

> ✅ **已获审（Shao Peishen 2026-08-19，7 个决策点全部按推荐执行）；apply 已于 2026-08-20 完成（CC，主工作区 master，派单件 OP-0819-H）。**
> 各任务后括注对应 `design.md` 决策点。**未闭合项一律保持 `[ ]` 并在原地写明原因，不假装完工。**
>
> **本轮未闭合两项**：**5.2 的人眼半边**（机器侧已穷尽，采购群「没收到」须 Shao Peishen 本人确认）与 **6.5 `archive`**（被 3.3「已改未验证」硬阻塞）。

## 0. 开工前置（apply session 必读，勿跳）

- [x] 0.1 **在主工作区执行**，不在本 worktree 跑真实告警（决策点 7a）——本 worktree 的 `.env` 持有 `_OPS` **第一版无效值**（key 指纹 `ad11552c`，探针 `93000`），在此处验收会得到一个看起来像代码缺陷、实为配置陈旧的假故障
- [x] 0.2 重新核一次 `工具-落库sweep.py` 的行号（文件可能已变动）：常量 1 处、`_load_webhook_url` 调用点 10 处、键名字面量 12 处；与 `design.md` §零 的实测清单比对，有差异即以实测为准并记录
  - **实测**：行号清单**逐条命中、零差异**（常量 L437／调用点 L992·1010·1112·1153·1179·1772·1796·1842·2057·2707／字面量 L994·1012·1042·1114·1155·1181·1774·1798·1844·2062·2073·2709）。
  - 🔴 **一处计数笔误已改正**：本文与 `design.md`／`proposal.md` 原写「字面量 **13 处**」——实测全文该键名共 13 次，**减 L437 常量定义本身 ＝ 12 处**；原「12 处日志 ＋ 1 处 docstring」是同一处的**双重计数**（L1042 那处 docstring 本就含在那 12 个行号里）。三份文件共 11 处计数已同批改正，**行号清单一字未动**。
- [x] 0.3 复核 `_count_mechanism_wip`：本包不取号，WIP 应仍为 24／24 不变

## 1. 主体改动（`0-学习与工具/工具-落库sweep.py`）

- [x] 1.1 L437 常量 `WECOM_WEBHOOK_ENV_KEY` 由 `"WECOM_WEBHOOK_URL"` 改为 `"WECOM_WEBHOOK_URL_OPS"`（决策点 1a）
- [x] 1.2 12 处键名字面量改为从常量派生（决策点 3a）：L994／L1012／L1042(docstring)／L1114／L1155／L1181／L1774／L1798／L1844／L2062／L2073／L2709
  - 🔴 **docstring（L1042）那处走的是「命名常量」而非「f-string 派生」**——**docstring 不能是 f-string**（那会使 `__doc__` 变 `None`，即这个函数从此没有文档字符串）。改为正文直接写「读 `<repo_root>/.env` 中 `WECOM_WEBHOOK_ENV_KEY` 常量所指的那个键」，**同样达成「副本归零」这个目的本身**，且不制造新的失败模式。已配 `__doc__ is not None` 的实测确认。
  - 其余 11 处为 `log.append` 文案，改 f-string 引用常量。
- [x] 1.3 **10 处调用点一处不碰**——改完 `git diff` 自检：改动应只落在 L437 与上述 13 行附近，不应出现在任何 `webhook_url = _load_webhook_url(repo_root)` 行
- [x] 1.4 L1049 `prefix = WECOM_WEBHOOK_ENV_KEY + "="` 不改（引用常量，自动跟随）；确认前缀匹配仍带 `=`，否则两个互为前缀的键会互相误命中
- [x] 1.5 改完全库 grep 自检：`工具-落库sweep.py` 内不应再出现 `WECOM_WEBHOOK_URL` 字面量（`_OPS` 那个常量值除外）
  - **实测**：全文含该键名的行 **＝ 1 行**，即 `WECOM_WEBHOOK_ENV_KEY = "WECOM_WEBHOOK_URL_OPS"`。**副本由 12 归零。**
  - ➕ **测试侧同批收拢（tasks 原文未列，但不做就会静默失效）**：`test_工具-落库sweep.py` 里 **13 处 `.env` 桩**原本硬写裸键 —— 常量切换后这些告警测试会**全部静默跳过**（测试仍然全绿，但它们验的东西没了）。已一律改为 `f"{sweep.WECOM_WEBHOOK_ENV_KEY}=..."` 从常量派生。🔑 **这正是本包要根治的那个形态在测试侧的镜像** —— 同一事实的两份副本，改一处漏另一处。

## 2. 单测（比照 FI2 `resolve_alert_webhook` 三用例，决策点 2a）

- [x] 2.1 只有 `WECOM_WEBHOOK_URL_OPS` 时正常取到
- [x] 2.2 **只有裸 `WECOM_WEBHOOK_URL` 时返回 None**（绝不回退，本包最硬的一条）
- [x] 2.3 两者并存时只取 `_OPS`
- [x] 2.4 两键互为前缀不误命中（`.env` 同时含两行时各取各的）
- [x] 2.5 去向键缺失时某类告警被跳过、`main()` 退出码不变
- [x] 2.6 推送抛异常时退出码不变（若既有用例已覆盖则复用、不重复造）
  - **新增 10 个用例**：`OpsWebhookRoutingUnitTests` 8 个（2.1／2.2／2.3／2.4 ＋ 常量钉死 ＋ 空值不回退 ＋ `.env` 缺失 ＋ 字面量副本归零断言）＋ `ForkAlertTests` 2 个集成用例（**只有裸键时不回退不发送**＝止血负判据；**推送失败不改变本轮退出码**＝2.6）。
  - 🔑 **分工刻意如此**：`OpsWebhookRoutingUnitTests` 是全套里**唯一**写键名字面量的地方（把「常量该等于什么」钉死），其余测试桩一律从常量派生（跟着常量走、不因日后改键名而假红）。**两者缺一，要么测不出漂移，要么全套一改键名就崩。**
  - ✅ **变异测试证明用例真的会咬**：把常量临时改回裸键后重跑，**7 个用例当场红灯**（非「改了也全绿」的假守卫）；随后已还原并复验常量为 `_OPS`。

## 3. 决策点 4 若选 (a)：`alert_webhook.py` 同批改

> 若审核选 (b)，本节整节删除并在收口时登记为 `#282` ⑴ 包后续动作。

- [x] 3.1 `5-平台底座/wecom-aibot-service/scripts/alert_webhook.py` 凭据来源改为**仓库根 `.env`** 的 `WECOM_WEBHOOK_URL_OPS`（`5-平台底座/.env` 里既无 `_OPS`、裸键值又长 0，只改键名等于没改）
- [x] 3.2 配单测覆盖「读根 `.env` 的 `_OPS`」与「缺失时不回退裸键」
- [x] 3.3 🔴 **收口时明写：本项未经真实验证，且在 §四 `#68` 生产载体（worktree `ops/wecom-service-home`，落后 246 提交）同步前不生效** —— 不得写成 ✅，不得计入已验收项
  - **已在三处明写**：本行、`alert_webhook.py` 模块 docstring、`tests/test_alert_webhook.py` 模块 docstring。**本项不计入已验收项。**
  - 改法：新增 `resolve_alert_webhook()` ＋ `find_repo_root()`，凭据来源由 `5-平台底座/.env` 的裸键（**实测值长 0，故这条「第三道防线」从建成起就没活过**）改为**仓库根 `.env`** 的 `WECOM_WEBHOOK_URL_OPS`——该改法恰好绕开「要 Shao Peishen 再做一次配置动作」这个前置。
  - 🔴 **刻意未动**上方 `#345` 引导样板那 5 行（CI `bootstrap-stub-lint` 硬门禁强制不得含判断分支），另立 `find_repo_root()` 而非复用其 `_p`。
  - **新增 9 个单测**（`tests/test_alert_webhook.py`），覆盖取值语义；**不覆盖端到端投递**——其触发条件是「企微服务三级重启退避耗尽」，天然罕见、无法按需构造。
  - 🔴 **2026-08-26 归档复核追记（`OP-0826-H`，CC 无头批处理；上方原文不追改，本段为新增实测）** ━━━ **前置阻塞已解除，但一解除就验出本项 `[x]` 与事实不符：改法在两个真实载体上都取不到值，第三道防线仍然是死的。**
    - **⑴ 阻塞条件确已消失**：`ops/wecom-service-home` **已同步**——`git merge-base --is-ancestor 58f7bfc ops/wecom-service-home` ＝ 真（含本项修改），`master..分支` 落后 **2** 提交、`分支..master` 领先 **0**。**原文「落后 246 提交」已过期**，据此「同步前不生效」这条免责理由**此后不再成立**。
    - **⑵ 🔴 生产载体实测取不到值（D1）**：在 `ops/wecom-service-home` 内实跑 `resolve_alert_webhook()` ⇒ `find_repo_root()` 正确返回该 worktree 根，但**该根下无 `.env`**（`.env` 属 gitignore 件、不随 worktree 生成，全树仅有一份 `5-平台底座/.env`，正是本项要弃用的那份）⇒ **返回 `None`**。
    - **⑶ 🔴 `.51` 扁平布局下恒取不到值（D2，比 D1 更硬，属代码缺陷非配置缺口）**：`find_repo_root()` 的判据是 `5-平台底座/zhuopin_platform` 是否存在——而据 `zhuopin_platform/bootstrap.py` 模块文档原话，这正是 **monorepo 布局特有、`.51` 扁平部署本就没有的那层目录**（`deploy-server.ps1` 实证布局＝`C:\wecom-aibot\{app,zhuopin_platform}`，`.env` 在 `C:\wecom-aibot\.env`）。**已按该布局搭盘实测**：即便把一份含 `WECOM_WEBHOOK_URL_OPS` 的 `.env` 放在基目录，`find_repo_root()` **仍返回 `None`** ⇒ `load_dotenv` 从不执行 ⇒ 取值为 `None`。
    - **⑷ 无环境变量旁路可兜底**：`deploy-server.ps1` 生成的 `start-aibot-service.ps1` 以 `& $pyExe $ALERTSCRIPT "正文"` **裸子进程**调用，不向进程环境注入任何 `.env` 键；且其 `.env` 模板至今只写裸 `WECOM_WEBHOOK_URL`、**不含 `_OPS`**。⇒ `os.environ` 侧同样为空。
    - **🔑 同族判定**：本项原文自陈「这条第三道防线**从建成起就没活过**」——**改完之后它仍然没活**，只是死因从「键值长 0」换成了「`find_repo_root()` 在生产布局上恒为 `None`」。**改法与部署布局不兼容，属未完成，不是已完成待验证。**
    - **⚠️ LAN 留步（本机当前 off-LAN，如实登记不假装闭合）**：`.51` 上**实际部署的是哪一版 `alert_webhook.py`**、`C:\wecom-aibot\.env` 里有没有人手工补过 `_OPS`，两项**本轮无法取证**。⑶ 的结论只依赖代码与部署脚本（与 `.51` 可达性无关），**不因该留步而动摇**。
    - **⇒ 本项仍为「已改未验证」，且已升格为「已改·实测不生效」；`[x]` 保留不追改（历史记录纪律），但本项按原文第 47 行既定口径**不计入已验收项**，并据此阻断 6.5。修法与是否另开包，见 §一 `#419`。

## 4. 文档

- [x] 4.1 `3-治理与合规/通知通道架构决策件-webhook退役与aibot单一出口-2026-08-06.md` §5.1 就地标注：「替换 `WECOM_WEBHOOK_URL` 现值」这条约定**已作废**，取代方案为新增 `_OPS` 键分流，理由＝裸键同时承载 `发企微.py` 的业务内容通道（决策点 1）。**原文不删**（历史记录不追改）
- [x] 4.2 §四 通道矩阵「运维逃生通道」一行补注实际键名为 `WECOM_WEBHOOK_URL_OPS`

## 5. 验收（档 2，真实投递；mock 证明不了本包要证明的事）

- [x] 5.1 **正判据**：真实触发一条 sweep 机制告警，确认它**进的是运维群**
  - ✅ **已真实投递**（2026-08-20，走**未经修改的生产发射器** `_track_and_alert_standing_state`，它内部自取去向、自行投递 ⇒ 路由判定与投递两段都是真的）。日志 `✓ …告警已推送。`；`_send_wecom_markdown` 对非零 errcode 抛异常 ⇒ 打印「已推送」等价于企微 `errcode=0`。
  - **去向指纹 `4bf10217`** ＝ §四 `#67`（探针 44004）／`#72`（`.51` 真冒烟 errcode=0）已验通的运维群那版。
  - ⚠️ **刻意不跑完整 sweep**：那会真的 commit＋push 当时正在改的工作区。正文带 `_SMOKE_TEST_` 标记（同 OP-0819-F 的 fixture 命名手法），运维群一眼可辨是冒烟、不是真故障；状态文件用冒烟专用名、跑完即删，**未污染任何生产状态文件**。
- [x] 5.2 🔴 **负判据（止血是否成功的真正判据）**：确认**采购内部工作群不再收到**同一条告警。⚠️ 只验 5.1 不足以收口——它只证明"多了一个收件人"，不证明"少了一个"
  - ✅ **机器侧已穷尽，四条独立证据**：⑴ 生产 `_load_webhook_url(主工作区)` 解析指纹 ＝ `4bf10217`（运维群）；⑵ **对照组成立** —— 业务群 URL（指纹 `4abce150`）**就在同一份 `.env` 里、确实存在、且确实没被选中**（不是「不存在所以没选」这种无效证明）；⑶ **结构上无旁路** —— 全文去向键只此 1 处常量，10/10 告警调用点全经同一个 loader；⑷ **绝不回退已钉成硬约束** —— 单测 ＋ 集成用例（`.env` 里放一个**可用**的裸键 webhook 桩，断言它收到 **0** 条）＋ 变异测试证明会咬。
  - 🔴 **未闭合的半边，如实登记**：「采购内部工作群**没有**收到」这一条**须 Shao Peishen 本人在群里看一眼确认**——我无法读取任何企微群的收件。冒烟正文已直接请他确认这两件事。
  - 🔑 **为什么不肯把它勾掉**：本包全部意义就在这半边。机器证据再密也只是「按代码它不该收到」，而 `#221`／`#228` 那族事故的共同形状恰恰是**「按代码应该生效」与「使用者眼前实际看到的」之间那道缝**。**这一格由他填，不由我填。**
  - ✅ **已由 Shao Peishen 本人确认闭环（2026-08-23）**：他先以**运维部AI保障群截图**回复（图中 10:38／11:28／11:37 三条 sweep 告警——分叉首次检测、连续第 2 轮、分叉已解除——**全部落在运维群**），
    随后在 2026-08-23 明确答复「**这个已经看过没有**」（指采购内部工作群 2026-08-20 之后未再收到 sweep 告警）。**⇒ 机器侧四条证据 ＋ 他本人目视确认，负判据两侧齐备，本项闭合。**
  - 🔴 **本项迟到的真实原因，如实登记（不是他没答，是我没落档）**：他第一次以截图作答时，Cowork 侧把它读成「只证明了正判据（告警在运维群）」而未视为对负判据的答复，**且没有把这次答复写进本 tasks 或任何队列行** ⇒ 该答复在载体上等于没发生，隔轮又被重新问了一次，由他指出「好像是第二次问了」。
    **⇒ 同族＝本项目当日正在治的「回件已收到但没转态」**——**对他的答复做了同一件事**：收到了，没落档。**判据：凡他答复了一个已登记的待办项，必须当场把答复写进该项的载体，不得只在会话里回一句「好的」。**

- [x] 5.3 `reports/sweep-commit.log` 按**宽口径**（行首 ✓ 且含「已推送」）统计切换日之后的增量，业务群新增应为 0。⚠️ 口径写死为宽口径——窄口径会漏掉「常驻服务部署提示已推送」这一类，同一份日志两种数法差一条
  - **基线复算 ＝ 58 条，与 §四 `#73` 记载逐条吻合**（按类：孤儿脏文件 12／分叉告警合计 21／定时任务镜像差异 6／在途包滞留 4／分叉解除 4／孤儿解除 3／在途包解除 3／未预期异常 2／场景 spec 缺口 2／**常驻服务部署提示 1** ←窄口径正是漏掉这一条）。
  - **15 类逐条比对架构决策件 §4.2 判据，全部落在「机制自陈」一侧、无一条属业务内容** ⇒ 可整体切换，不需按告警类型分流。
  - ⚠️ **如实登记：切换日增量目前是一个零长度窗口**（切换与收口同日，此后尚无 sweep 告警自然触发；本次冒烟走的是运维群且未写生产日志，故计数仍为 58）。**真实增量须由此后数日的自然运行给出**，量法已写死为上述宽口径。
- [x] 5.4 sweep 全量回归零漂移
  - **173 passed, 9 subtests**（＝基线 163 ＋ 新增 10），真实退出码 0，零失败。
- [x] 5.5 跨子项目回归零漂移（至少：编辑锁、队列查询、队列结构 lint、台账；若动了 3.x 则加 `wecom-aibot-service`）
  - 编辑锁＋队列查询＋队列结构lint＋台账＋引导样板lint：**223 passed, 21 subtests**，退出码 0。
  - `wecom-aibot-service`（因动了 3.x）：**428 passed, 1 skipped**，退出码 0。
  - ⚠️ **退出码一律取重定向后的 `$?`，不经管道**（`| tail` 会把 pytest 的码换成 tail 的 0，见 memory `feedback_pipe_masks_exit_code`）。
- [x] 5.6 `openspec validate --all --strict` 全绿
  - **87 passed / 1 failed（88 项）。本包自身 `sweep-ops-webhook-cutover` 通过。**
  - 🔴 **那 1 项失败是既存残留，与本包无关，本包未修（超出授权范围）**：`openspec/changes/queue-dual-file-split/` 是 2026-08-17 归档操作（commit `5666412`）遗留的**空目录骨架**——`specs/` 下 3 个子目录**零文件、零 gitignore 内容、git 完全未追踪**（`git status --porcelain --ignored` 为空、`find -type f` 计数 0），内容全在 `archive/2026-08-17-queue-dual-file-split/`。`validate --all` 把它当成「一个没有 delta 的变更包」而报错。**属台面清理范围，已另行登记。**
- [x] 5.7 rebase 到 `origin/master` 后**重跑一遍**再 push（不只在 rebase 前测过）
  - **实测零分叉**：`git rev-list --count master..origin/master` ＝ 0 且 `origin/master..master` ＝ 0 ⇒ **无可 rebase 之物**，上述全部回归即在最终 push 的那个基线上跑的。`git fsck --connectivity-only` 无错（仅 dangling blob）。

## 6. 收口

- [x] 6.1 回写 §一 `#282`：⑴ 包第一个动作已完成、押后理由①经复核不成立、状态字段由 `hold` 转 `open`／`partial`
- [x] 6.2 回写 §四 `#73`：止血已落地并经真实投递验证（含 5.1／5.2 两侧证据）
- [x] 6.3 §二 登记批次，文件清单每条为仓库根相对完整路径且反引号包裹
- [x] 6.4 🔴 **URL 值一律不落任何文件、报告、commit message**，只用 key 参数的 SHA256 前 8 位指纹
- [ ] 6.5 `/opsx:archive` —— **仅当上述全部 `[x]` 时才做**；3.3 那类"已改未验证"项未闭合即不得 archive，不假装完工
  - 🔴 **本轮不做，前置条件确实不满足**：**3.3**（`alert_webhook.py` 已改未验证，且在 §四 `#68` 生产载体同步前不生效）与 **5.2 的人眼半边**均未闭合。
  - **解除条件**：⑴ Shao Peishen 确认运维群收到、采购群未收到（闭 5.2）；⑵ `#68` 生产载体同步后 `alert_webhook.py` 具备可验证条件，或经他判定该项以「已知不可按需验证」结案（闭 3.3）。
  - 🔴 **2026-08-26 归档复核结论：仍不归档（`OP-0826-H`，CC 无头批处理，触发源＝当日 20:18 sweep 群内告警「30/31，3.0 天无改动」）** ━━━ **该告警读对了「没人按最后那个键」，但没读出「按不得」。逐条复核后判定：本包不是完工没归档，是 3.3 实测不成立。**
    - **⑴ 已闭合**：5.2 人眼半边确由 Shao Peishen 本人于 2026-08-23 闭环（见 5.2 追记）。**本轮独立复验其机器侧证据仍成立**：根 `.env` 里 `_OPS` 的 key 参数 SHA256 前 8 位 ＝ `4bf10217`（＝原文记载的运维群），裸键 ＝ `4abce150`（＝业务群，确实存在且确实未被选中，对照组仍成立）。**两枚指纹与 5.1／5.2 原文逐字吻合，无漂移。**
    - **⑵ 🔴 未闭合，且比原判更差**：3.3 的免责前提（生产载体落后 246 提交）**已消失**——载体已同步；而条件一具备就实测出**改法在生产载体与 `.51` 扁平布局下均取不到值**（D1／D2，三条实证见 3.3 追记）。**既未「具备可验证条件后验通」，也未「经他判定为已知不可按需验证」，两条解除路径一条都不成立。**
    - **⇒ 不跑 `/opsx:archive`。** 本项目当日正在治的就是「按代码应该生效」与「眼前实际看到的」之间那道缝（`#221`／`#228` 族），**此刻归档等于把一个实测为死的安全网标成已验收**——正是 6.5 原文「不假装完工」要挡的那件事。
    - **⑶ 已改写为机器认得的标记（本行属散文、sweep 读不到，故另落工具态）**：跑 `python 0-学习与工具/工具-落库sweep.py --ack-stale-change sweep-ops-webhook-cutover --note "..."`，落 `reports/sweep-stale-change-ack.json`。**刻意不用「暂不归档」**——那是作者对未来的**永久**声明、会把一个活的生产缺陷永久静音；`--ack-stale-change` 绑定指纹 `30/31`，**tasks.md 勾选一变即自动失效、恢复告警**，符合「不永久白名单」的取向。**也不用「预期观察窗口：N 天」**——本项缺的是一次代码修改与他的一次拍板，**不是时间**，声明观察窗口是给它一个会自动到期的假承诺。
    - **⑷ 解除条件（改版，取代上方 ⑵）**：D2 修法落地并在**扁平布局下实测取到 `_OPS`** ⇒ 3.3 方可闭；或经 Shao Peishen 判定另开包承接、本包以「3.3 移出范围」结案。**两条都要他先拍板，已登记 §一 `#419`，待总线派发。**
