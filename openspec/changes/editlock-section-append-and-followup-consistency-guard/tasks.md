## 1. Design 审核（阻塞后续所有任务）

- [ ] 1.1 提交 design.md 五个决策点给 Shao Peishen 审核
- [ ] 1.2 记录审核结果到 design.md「审批记录」小节

## 2. `append-row` 子命令（决策点 1/2，需求见 `specs/editlock-section-append-guard`）

- [ ] 2.1 新增 `cmd_append_row`：接受 `--section`、`--number`（§一/§四）或首个 `--cell` 即为批次号（§二）、若干 `--cell` 重复参数；按分区列序拼装
- [ ] 2.2 新增插入位置定位函数，复用 `_split_live_sections`/`_table_data_rows`，定位目标分区表格真实末行并插入（覆盖空分区场景）
- [ ] 2.3 写入前校验：字段数量与分区预期列数一致；拼装后按 `|` 切分列数与预期一致
- [ ] 2.4 裸竖线检测：字段值中未被反引号包裹的 `|` 触发拒绝，错误信息含字段序号与内容预览
- [ ] 2.5 新增/更新单测：结构化字段拼装成功、字段数不符拒绝、非末尾分区插入位置正确、空分区插入、列数不符拒绝、裸竖线拒绝、反引号内竖线不触发拒绝、编号字段原样透传

## 3. release 一致性校验（决策点 3/4/5，需求见 `specs/editlock-followup-hold-consistency-guard`）

- [ ] 3.1 独立实现 README 目标文件标注提取正则（不 import `aibot_service`，同既有惯例），复用 `_followup_readme_rows`/`FOLLOWUP_FINALIZED_STATUS`
- [ ] 3.2 `_validate_release_structure` 新增第⑥项：对本次持锁期间新增/修改的 §一/§四 行检测"暂缓关键词+反引号 `.md` 文件名引用"共现（§一：状态列cells[5]查关键词、cells[3]+cells[6]查文件名；§四：事项列cells[1]查两者）
- [ ] 3.3 正向校验：匹配到的 README 行「发送状态」为 `🆕 待发` 时拒绝 release
- [ ] 3.4 反向校验：匹配到的 README 行「发送状态」已是终态"已推送"类值而队列行仍含暂缓字样时,告警不阻断
- [ ] 3.5 新增/更新单测：正向拒绝、README已同步非待发时放行、未匹配到README行时不拒绝、反向告警放行、仅关键词无文件名不触发、仅文件名无关键词不触发

## 4. 历史兼容核对固化

- [ ] 4.1 用现存生产队列文件 §一/§四 全部行 + README 现有跟进信清单跑历史兼容核对（design.md 已给出分析性核对结论：#150 行触发反向告警、不触发正向拒绝），固化为可重复运行的单测

## 5. 真实验证

- [ ] 5.1 用 `append-row` 对一份测试队列文件真实追加一行到 §一/§四/§二 任一分区（非 mock，真实文件读写），确认插入位置正确、列数正确
- [ ] 5.2 构造测试队列行 + 测试 README，走真实 `cmd_acquire`/`cmd_release`（生产代码路径），确认正向场景真实被拒绝、反向场景真实告警放行

## 6. 验收与收尾

- [ ] 6.1 全量回归（`test_工具-共享文档编辑锁.py`）零漂移
- [ ] 6.2 `工具-共享文档编辑锁.py` 文件头部说明段补充本次两项加固的背景
- [ ] 6.3 `专线opener模板库.md` 补充 `append-row` 用法说明（若决策点 1 采用结构化字段调用形式，即默认项）
- [ ] 6.4 队列 §一 #258 行回填：状态、产出路径、单测清单、真实验证记录、历史兼容核对结果；同批核对 #294 完工状态（若已完工，评估其新增状态是否需要补进本变更"终态"判定集合）
- [ ] 6.5 `/opsx:archive` 归档本变更包
- [ ] 6.6 收工重跑文档台账
