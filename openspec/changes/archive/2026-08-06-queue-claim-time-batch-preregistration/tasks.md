## 1. Design 审核（阻塞后续所有任务）

- [x] 1.1 提交 design.md 四个决策点给 Shao Peishen 审核（可直接回复 (a)/(b) 或指定其它方案；未回应按 design.md 已标注的默认项执行）—— 2026-08-05 经业务总线 session 批准，四点全部 (a)，附验收硬要求（见 design.md「审批记录」）
- [x] 1.2 记录审核结果（批准的方案组合）到本变更包 —— design.md 已追加「审批记录」小节；队列 §一 #236(1) 行待下一 apply session 一并回填完成状态（本次仅记录批准结果，未启动 apply 实施）

## 2. 协议文本改写

- [x] 2.1 改写协议〇.1（"开工"）：认领任务时须在 §二 登记预登记批次行，先于任何实质改动 —— 2026-08-06 已落地
- [x] 2.2 改写协议〇.3（"收工"）：收工时回到预登记批次行做精确化（文件清单/commit message/状态），不新增第二行 —— 2026-08-06 已落地
- [x] 2.3 明确预登记批次的状态列文本约定（"在办（预登记，收工时精确化）"），确保不含"待"字样、不被 sweep 现有判据误处理 —— 已写入协议〇.1；额外确认该文案不含"✅"也不含"待"，会落进 sweep 的"模糊状态"分支但不阻塞其它批次（#238 批次隔离），且编辑锁⑤新校验（随 #247② 同车）已显式放行不误拦
- [x] 2.4 `专线opener模板库.md` 补一句"认领即预登记"的口令指针 —— **评估后判定不需要**：模板库 §〇 系列是"opener 元信息怎么写"的专项约定（执行环境标注/触碰区/工作区字段），与"认领时如何登记批次"主题不同，协议〇本身已是权威源，勉强插入反而制造重复表述，见队列 #236 行回填说明
- [x] 2.5 若决策点 4 批准为 (a)（默认）：在协议〇.7 编辑锁段落明确"预登记内容"适用其既有降级路径（锁占用时记入域接力文件⏳节）—— 2026-08-06 已落地

## 3. 工具化能力（仅当决策点 3 批准为 (b) 时执行；若批准为 (a) 默认，跳过本节，转 4）

**决策点 3 批准为 (a)（纯协议约定），本节按设计整体跳过——不实现 `--preregister-batch` 参数。**

- [x] 3.1～3.3 跳过（(a) 分支，见上）

## 4. 陈旧预登记批次检测（仅当决策点 2 批准为 (a) 默认时执行；若批准为 (b)，改为新建独立检测脚本，任务改写）

- [x] 4.1 `zhuopin-queue-audit` skill 源码新增检测项：状态为"在办（预登记，收工时精确化）"且登记时长超过阈值（24 小时）的行 —— 2026-08-06 已落地为**类型⑧**，挂载位置＝§二"逐行核对"末尾（其余七类之后、§三"卫生检查"之前），触发判据见 SKILL.md 类型⑧行原文（`git log -1 --follow` 定位行改动时间，无法定位则退化为距上次巡检间隔，取较严格者）
- [x] 4.2 检测结果纳入值周巡检《本周计划》生成逻辑的提示段 —— **本变更范围内的落点＝审计简报③修正清单**（见 SKILL.md §五"输出简报"既有结构，类型⑧与其余七类共用同一简报③清单，未新增《本周计划》专属提示段——值周巡检本就消费对账审计的修正后队列+简报，不需要为⑧单独在《本周计划》文档结构里再开一个位置，否则与既有"先对账后巡检"分工重复）
- [x] 4.3 跳过（决策点 2 批准为 (a)，非 (b)）

## 5. 验收与收尾

- [x] 5.1 全量回归——本节无第 3 节代码改动；编辑锁⑤校验（#247② 同车）单测已覆盖"预登记"文案放行，全量回归编辑锁 76 passed 零漂移
- [x] 5.2 真实认领场景验证 —— **改走单测覆盖**（未在生产队列文件写入/撤除临时验证行，理由见队列 #236 行回填的"验证方式"说明）：①"在办（预登记…）"落进 sweep 既有"模糊状态"分支且不阻塞其它批次，由既有回归测试覆盖（本变更未改动该逻辑，属 Non-Goal）；②该文案被编辑锁⑤放行，由新增单测 `test_new_batch_preregistered_status_passes` 直接验证
- [x] 5.3 队列 §一 #236 行回填 (1) 完成状态与产出路径 —— 2026-08-06 已回填
- [x] 5.4 `/opsx:archive` 归档本变更包 —— 本次执行
- [x] 5.5 收工重跑文档台账 —— 随本批收尾统一执行
