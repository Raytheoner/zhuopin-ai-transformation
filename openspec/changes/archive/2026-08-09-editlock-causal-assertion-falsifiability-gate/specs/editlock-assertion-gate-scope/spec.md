## ADDED Requirements

### Requirement: P0/P1 定级行须含证伪命令片段
编辑锁 `_validate_release_structure` SHALL 在剔除 §一 行状态列（cells[5]）中被「」/『』引号包裹的片段后，若剩余文本含 P0/P1 定级 token，MUST 要求同一状态列单元格内存在至少一处反引号 `` `...` `` 包裹的非空片段（视为"如果该断言错了，哪一条命令会证明它错"的证伪命令）；缺失时 SHALL 拒绝 release 并在提示中说明。本项检测**只判存在性，不判该片段内容是否真的具备证伪能力**。

#### Scenario: 缺证伪命令片段，拒绝 release
- **WHEN** 一个 §一 行状态列（剔除引号包裹片段后）含 P0/P1 定级 token，且状态列内不存在任何反引号包裹的片段
- **THEN** 编辑锁 SHALL 拒绝 release，提示状态列缺少证伪命令

#### Scenario: 含证伪命令片段，正常放行
- **WHEN** 一个 §一 行状态列含 P0/P1 定级 token，且状态列内存在至少一处反引号包裹的非空片段
- **THEN** 编辑锁 SHALL NOT 因本项而拒绝 release

#### Scenario: 引号包裹的 P0/P1 定级引用文本不触发本项检测
- **WHEN** 一个 §一 行状态列中的 P0/P1 定级 token 完整出现在 `「...」`/`『...』` 引号包裹的片段内（引用/复述判据本身），且引号外剩余文本不含 P0/P1 定级 token
- **THEN** 编辑锁 SHALL NOT 因本项而拒绝 release

#### Scenario: 与既有未核实字样校验相互独立
- **WHEN** 一个 §一 行状态列同时缺证伪命令片段、且同时含 P0/P1 定级与"未核"/"未做的核实"字样的共现
- **THEN** 编辑锁 SHALL 在 violations 中给出两条独立提示，本项判定不因既有校验已命中而跳过，既有校验也不因本项已命中而跳过

### Requirement: 检测边界与免责声明
本项检测 SHALL 仅判断证伪命令片段"是否存在"，不 SHALL 判断该片段内容是否确实构成对该断言的有效证伪；实现说明与文档 MUST 原样保留"只判有没有、判不了对不对"这一边界描述，不 SHALL 将本项表述为对断言正确性的质量保证。

#### Scenario: 无效命令片段仍视为通过
- **WHEN** 一个 §一 行状态列含 P0/P1 定级 token，且反引号内的片段与该断言并无实际证伪关系（如一条无关命令）
- **THEN** 编辑锁 SHALL 仍判定通过本项检测，不 SHALL 尝试判断命令片段与断言的语义相关性
