# Tasks — QD-B C01–C10 跨模块校验（档一 ＋ 档二）

## 1. 档二 · 跨模块校验实现

- [x] 1.1 新建 `qd_b_gate/rules/cross_module.py`：`CROSS_CHECKS` 元数据表（C01–C10 编号/名称/§三 原文规则/偏差处理）
- [x] 1.2 实现 C01（人员安排合计 ↔ 各阶段人月之和）、C08（预算总工时 ↔ 各阶段人月之和），10% 容差 → 警告
- [x] 1.3 实现 C02（成本 E ↔ 小计⑩之和）、C03（收入依据合计 ↔ 收入⑪之和），过 `_cb_income_scale` 换算
- [x] 1.4 实现 C04（模块一起止日期 ↔ 首阶段开始/末阶段结束），不一致 → 警告，不阻断
- [x] 1.5 C09/C10 复用规则 59/60 判定结果（查表，不重算）；查不到则 PENDING 并写明
- [x] 1.6 C05/C06/C07 判 PENDING，各写具体阻塞原因（非"扩容期"）
- [x] 1.7 等价规则标注：C01/C04/C08/C09/C10 条目内标注等价规则编号 ＋ 该规则本次判定结果（Q1 冲突可见）
- [x] 1.8 任一侧缺数 → PENDING 并写明缺哪一侧，不按 0 计算

## 2. 档一 · 三处呈现层止损

- [x] 2.1 `report.py`：删除 `CROSS_MODULE_NOTE` 恒定文案；`build_report` 填充 `cross_module_items`；`to_text()` ④段逐条渲染
- [x] 2.2 `evaluate.py`：贯通 `cross_module_items`（全链审计仍只记 82 条规则判定，跨模块段另记）
- [x] 2.3 `webapp.py`：跨模块段由一行提示改为表格呈现（状态色沿用既有）
- [x] 2.4 `xlsx_report.py`：新增「跨模块校验」sheet

## 3. 测试

- [x] 3.1 `tests/test_cross_module.py`：C01/C08 一致与偏差、C02/C03 量纲换算、C04 起/止、C09/C10 复用、C05/C06/C07 PENDING 文案含具体原因、缺数 PENDING 不按 0
- [x] 3.2 报告层测试：④段逐条渲染、无"尚未实现"整段文案、三载体一致
- [x] 3.3 **零分数漂移回归**：EQ17/邦奇 的档位·总分·一票否决驱动规则与 `data/golden/manifest.md` 真值
      逐一比对不变（合格 98.80 / 不合格 94.89 veto=74），并断言 C01–C10 条目不出现在 `all_results`／
      阻断项／警告项／转人工任一桶内（13 模块得分率同源于 `results`，不另断言）

## 4. 真实数据验证

- [x] 4.1 三份真实黄金样本（华丰/EQ17/邦奇）实跑，核对 C01–C10 逐条结果与 #340 评估件 §二 实测表一致
- [x] 4.2 确认两条真实命中复现：邦奇 C02 **差 46.662 万**（成本E 131.96 万 vs 小计⑩和 85.298 万）、
      EQ17 C04 起始差 3 天（2026-06-01 vs 2026-05-29）
      ⚠️ **一处与 #340 评估件的数字差异，如实登记**：评估件那支一次性探针报"偏差 54.7%"（分母取模块九侧
      85.298），本实现报 **35.4%**（分母取模块八侧 131.96，即被核对的声明值）。**绝对差、判定、结论均相同**；
      根因是 §三 原文只写"偏差 > 10%"、**未规定分母**——已随 Q1 一并列为待陈忱裁定项，代码内写明取哪一侧。
- [x] 4.3 全量回归零漂移（基线 102 passed / 6 skipped）

## 5. 发布收口（§5 发布即收口纪律四关）

- [ ] 5.1 `/opsx:archive qd-b-cross-module-check`
- [ ] 5.2 场景 `CLAUDE.md` 更新（六段式：状态时间线 ＋ 部署状态）
- [ ] 5.3 部署 `.51:8093` ＋ 冒烟（`/api/ping`、首页 200、真实样本全量重跑、下载 xlsx 校验）
- [ ] 5.4 队列 #340 回写（②实现已完成；③回话仍待业务总线成信）
