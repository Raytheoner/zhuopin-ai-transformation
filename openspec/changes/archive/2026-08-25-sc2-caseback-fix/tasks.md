# sc2-caseback-fix Tasks

## 1. 取证（先证伪，再动手）

- [x] 1.1 复现我方判例包上那五个环比数，锁定其取数路径（`po_cache.json` 2026-07-08 快照，2810 行，五数逐字相等）
- [x] 1.2 周起始日平移扫描（−4…+4 天）证明周边界两边一致，偏移不在边界上
- [x] 1.3 周号整体 −1 后与他的自算数比对，证明偏移量为整 1 周且方向一致
- [x] 1.4 `erpTypeCode` 全子集穷举，定位行集边界（剔除全程委外 ＋ 按订单行去重 → 8 周中 6 周逐字相等）
- [x] 1.5 `ZpViewPurOrder/Query` 全字段清单实测，确认其**没有**确认数量字段
- [x] 1.6 `Purchase/Query` 全字段清单实测，定位 `ConfirmQty`，三条判例逐条对上
- [x] 1.7 用 `GR/Query` 交叉验证 `rcvQtyTU` 的身份（＝累计入库量，非确认数量），排除那个"看上去也对得上"的错误候选

## 2. 底座（纯新增，零行为变更）

- [x] 2.1 `PurchaseOrder` 增 `doc_type` 字段（带缺省值，旧缓存可反序列化）
- [x] 2.2 `_ZpPurOrderRow` 增 `erpTypeCode`，`get_purchase_orders` 回填 `doc_type`
- [x] 2.3 新增 `get_purchase_line_details(item_codes)`，返回含 `LineStatus` ＋ `ConfirmQty` 的行级明细
- [x] 2.4 `get_purchase_line_status()` 改为 2.3 的薄封装，**返回值逐字不变**（单测守住）
- [x] 2.5 PO 磁盘缓存加 schema 版本号，版本不符即视为失效重取（D29：封死"旧缓存缺新字段 ⇒ 过滤静默失效"这个已踩过一次的坑）
- [x] 2.6 平台全量回归零失败

## 3. 窗口层（周序）

- [x] 3.1 `windows.py` 增采购口径周序（本年首个周一所在周为 W1；归属年取周一所在年）
- [x] 3.2 `Window.label()` 给出采购口径期次，`iso_label()` 保留为对照
- [x] 3.3 单测：2026 全年采购口径周号 ＝ ISO − 1；跨年连续不重叠不留缝

## 4. 取数层（字段与行集）

- [x] 4.1 `OrderLine` 增 `doc_type` 与 `qty_confirmed_known`
- [x] 4.2 `RealFeed` 改按 `ConfirmQty` 取订单量；取不到即标未知、**不回退 `qty`**
- [x] 4.3 剔除全程委外单据类型、按 `(单号, 行号)` 去重
- [x] 4.4 未知确认数量的行数写进 `source_notes`（No silent caps）
- [x] 4.5 单测覆盖 4.2–4.4 三条

## 5. 指标与呈现

- [x] 5.1 阈值默认值 ±50% → ±400%，`thresholds_confirmed` 由签认来源驱动
- [x] 5.2 数量/金额类指标只对确认数量已知的行求和，行数类照计
- [x] 5.3 措辞由「异常」改为「工作量波动参考」；确认推送路径一行未动
- [x] 5.4 周报与页面同时呈现采购口径周号、ISO 对照、起止日期、阈值签认来源
- [x] 5.5 单测覆盖 5.1–5.4

## 6. 真实数据验证与回归

- [x] 6.1 用他给的五周真实数据端到端跑一遍，逐周并排列出我方与他的行数
- [x] 6.2 A-1／A-2／A-3 三个真实单号端到端复现，打印实际取到的确认数量（A-3 须显示 200）
- [x] 6.3 SC2 全量回归零失败
- [x] 6.4 `git check-ignore -v` 实测核实快照与缓存文件名形态均被忽略
- [x] 6.5 `openspec validate --all --strict` 通过

## 7. 部署与收口

- [x] 7.1 ✅ **已部署 `.51:8096` ＋ 冒烟七项全过（2026-08-25 00:49-00:53 本地，CC `sc2-361-closeout-a7f31c`）**：
  `sync-to-server.ps1` 推送成功、服务重启并确认新进程存活（`NEW_PID=9440`）；`deploy-server.ps1` 在 `.51` 跑通。
  **冒烟**：`/api/ping` 200（`{"mode":"real","ok":true}`）／匿名访问被门禁 302／登录后关键页 200（5450 B）／
  `POST /api/refresh` **真实全量重算 200，耗时 78 秒，`period=2026-W34`**／重算后页面走快照 0 秒／
  **重算全程 PID 9284 未变**（非只信脚本打印「已重启」）。回滚 SOP 已随 `deploy-server.ps1` 更新（含新任务删除命令）。
  🔴 **原「被网络物理阻断」的判断已被实测推翻，成因值得记**：上一轮据 `Test-NetConnection` 的
  `PingSucceeded=False` **与** `TcpTestSucceeded=False` 判为物理不可达；本轮实测
  **`PingSucceeded=False` 但 `TcpTestSucceeded=True`** —— **ICMP 被禁而端口是通的**。
  ⇒ **`ping` 不通不等于主机不可达**；判可达性只认目标端口的 TCP 结果，不认 ICMP。
  ━━━ **以下为原文，保留** ━━━ 🔴 未做，被网络物理阻断：笔记本当前在 `192.168.3.35`（WLAN），`192.168.100.51` **ping 不通、8096 TCP 不通**：笔记本当前在 `192.168.3.35`（WLAN），`192.168.100.51` **ping 不通、8096 TCP 不通**（`Test-NetConnection` 实测 `PingSucceeded=False`／`TcpTestSucceeded=False`）。**⇒ `.51` 生产此刻仍跑旧码，周号与订单量两处仍是错的。** 待接入 `192.168.100.0/24` 后跑 `sync-to-server.ps1` ＋ `smoke-server.ps1`
- [x] 7.2 场景 `CLAUDE.md` 更新（口径修正段 ＋ 残差登记）
- [x] 7.3 查串行闸 —— **闸 ✅ 开**（`工具-跟进闸查询.py --to 姚祖怡`，依据 `采购部#17` 已 `📥`，下一个可用号 `采购部#18`）；🔴 **但仍未起草**：§5 第 8 步的触发条件是「**发布收口完成后**」，而 7.1 被网络阻断 ⇒ 触发条件未成立。**此时起草一封「新版已上线请试用」正是 #228 那族事故的形态**（通知已送达而生产未更新）。拟发要点已写进 §一 #361
- [x] 7.4 **design 审已通过**（2026-08-24，Shao Peishen 答《本周计划-2026-08-24》**B-12 选 (a)**：D22-D29 修法审核通过、放行部署）。⚠️ **O-8（跨年周序）／O-9（行集边界）两个开放点仍待姚祖怡签认**——它们要的是**业务方**的口径确认，不是 design 审能替代的，已随 §一 #361 挂账，**不阻断本包归档**
- [x] 7.5 回写队列 §一 #361／#362，收工重跑台账
