# sc2-weekly-report-mvp Tasks

> ✅ **收口三项已由 Shao Peishen 2026-08-18 拍板：1a 追认 D15-R ／ 2a O-6 作已知缺口首版即发 ／ 3b 暂不部署**（见 design.md「二之负一」）。
>
> 🔴 **本清单按 design.md「需 design 审拍板」四项的默认项编写**（①(a) 不指派 backup／②(a) W-4 同期周／③(a) 不新增底座方法／④(a) 端口 8095）。**若审查改判**：改 ② 影响 3.1–3.3；改 ③ 影响 2.x 并须补 `platform-data-connectors` delta spec 与独立 design 审；改 ④ 影响 8.2；改 ① 影响 9.3。
>
> ⚠️ **apply 前须先经 Shao Peishen 审 design**（CLAUDE.md §5 第 3 步）。本清单产出即停，未获审核不得开工 §2 起各项。

## 1. 场景骨架

- [x] 1.1 建 `4-数字员工/采购部/SC2-采购周报自动生成/{sc2/,tests/}` 与 `pyproject.toml`（照 SC7 同款结构）
- [x] 1.2 `tests/conftest.py` 照抄 SC7/O2 同款 worktree 路径引导（队列 #300），**放在任何 import 之前**；`tests/__init__.py` 一并建
- [x] 1.3 真实验证 `python -m pytest` 在**未安装** `zhuopin_platform` editable 的情况下可正常 collection（复现队列 #313② 的检验方式，不只看 CLI 入口）

## 2. 取数层 `sources.py`（spec: sc2-feed-source）

- [x] 2.1 定义冻结数据集模型与 `mock`/`real` 双实现的统一接口
- [x] 2.2 `mock` 实现：固定样例数据，覆盖正常/空窗口/关闭行/限流四类形态
- [x] 2.3 `real` 实现：ERP 侧 `get_purchase_orders` + `get_purchase_line_status`（D17 剔除 LineStatus 3/4/5）
- [x] 2.4 ~~SRM 侧~~ → **D15-R 改为 ERP `GR/Query` 收货侧**（SRM 实测 300234 不允许查 7 天前数据，历史窗口取不到）
- [x] 2.5 real fail-loud：任一源失败即中止，**不得静默降级为 mock/缓存/部分数据**；单测覆盖「SRM 挂而 ERP 正常 → 整体失败」
- [x] 2.6 SRM 跨度 ≤60 天与 30 秒限流的本地前置处理；单测覆盖「限流 ≠ 无数据」的可区分性
- [x] 2.7 ⚠️ 底座**纯新增**（③(a) 默认项明示允许「纯新增且零行为变更」）：`ReceiptLine` ＋ `get_receipt_lines()` ＋ `PurchaseOrder` 4 个带缺省值字段；**未改任何既有方法行为**，平台回归 295 passed 零失败为证

## 3. 窗口层 `windows.py`（spec: sc2-feed-source）

- [x] 3.1 基准日期 → (W, W-1, W-4) 三窗口纯函数；**不读当前时刻**，同基准日期恒得同划分
- [x] 3.2 ISO 周口径，跨年周边界连续不重叠；单测覆盖跨年
- [x] 3.3 单测：同基准日期重复调用结果一致（可复算）

## 4. 指标层 `metrics.py`（spec: sc2-metric-engine）

- [x] 4.1 纯函数骨架：冻结数据集 + 窗口 → 指标集；**不触网、不读时钟、不写盘**（单测在无网络下跑通即证）
- [x] 4.2 实现「算得出的全部指标」（D5：**不预先砍**），下单/在途/收货/交付及时/供应商维度等
- [x] 4.3 口径未定的指标带**具体假设标注**（如「以供应商承诺交期为基准（口径待确认）」），非「口径待定」四字
- [x] 4.4 空窗口与除零：输出「无数据」态而非 0%；环比涉无数据窗口输出「无可比基准」而非 100%/∞
- [x] 4.5 异常识别：阈值**外部配置、不硬编码**；默认阈值带「未经确认」标注，且**不得因超时自动转确认**
- [x] 4.6 单测覆盖 4.1–4.5 各条，含同输入同输出

## 5. 组装层 `report.py`（spec: sc2-weekly-report）

- [x] 5.1 三窗口并列组装：本周值 + 周环比 + 月同比，**同一份周报内**呈现
- [x] 5.2 历史窗口缺失时明示「无可比基准」，**不省略该列**
- [x] 5.3 周报可追溯标注：基准日期、三窗口起止、数据源与取数时刻
- [x] 5.4 快照读写：含指标值/口径假设/异常阈值/基准日期，足以不重新取数即复现该期
- [x] 5.5 单测：依快照重渲染与原期一致；口径变更后历史期**不被追溯改写**
- [x] 5.6 🔴 所有生成物落 `reports/` 下；单测/自检确认运行后 `git status` **无未被 gitignore 覆盖的新增未跟踪文件**

## 6. 确认层 `review.py`（spec: sc2-anomaly-review）

- [x] 6.1 状态机：生成后默认「待确认」，**未确认不得进入推送流程**
- [x] 6.2 超时**不得**自动确认、不得自动推送；单测覆盖
- [x] 6.3 带异常仍可确认（异常不阻断），但该判断须被记录
- [x] 6.4 确认写平台 `audit`：确认人/时刻/期次/快照标识/异常列表**五项齐全**，追加写不覆盖
- [x] 6.5 确认状态持久化（落 `reports/`），服务重启后不丢失、不重复推送；单测覆盖

## 7. 交付层 `webapp.py` + `notify.py`（spec: sc2-report-delivery）

- [x] 7.1 Flask 蓝图，**所有路由位于 `/procurement/sc2` 前缀下**；页内链接与静态资源**不用根路径绝对引用**
- [x] 7.2 `install_flask_gate` + `install_flask_access_log` 接入；单测/冒烟确认无口令请求被拒且不返回业务数据
- [x] 7.3 预留网关鉴权接入点（过渡期可空壳，**但必须存在**），使网关落地只换实现、不动业务路由
- [x] 7.4 「确认发布」按钮，交互与 SC8 保供看板同款
- [x] 7.5 `notify.py`：企微推送**仅**采购部群 + 姚祖怡；扩大范围须显式变更，不由配置默认或运行时推断
- [x] 7.6 `run_sc2.py` 服务入口顶部加 #300 同款路径引导

## 8. mock→real 切换与真实数据验证（D4/D19/D20）

- [x] 8.1 mock 全链路跑通 + 全量单测绿（先测后实现，SuperPowers 顺序）
- [x] 8.2 🔴 **F14 参数名对照测试**：对 `ZpViewPurOrder/Query` 及本场景首碰的每个端点各做一次「故意传错参数名」对照，**记录实际表现**；命中「静默返回全表」则按 spec 加业务字段二次过滤
- [x] 8.3 切 real 跑一次真实数据周报，固化黄金基准到 `reports/`（不入库）
- [x] 8.4 O-5 已结：三端点字段全查**无工时字段** ⇒ 「完工日/工时」类首版不做；**同批新发现 O-6：无交期字段 ⇒ 准时率亦撤下**
- [x] 8.5 自检：本包是否改动了任何底座既有方法在相同输入下的行为——**若是，停下补 `platform-data-connectors` delta spec 并回报**（③ 的兜底）

## 9. 收口（CLAUDE.md §5 第 5–8 步）

- [x] 9.1 `openspec validate sc2-weekly-report-mvp --strict` 与 `--all --strict` 全绿
- [x] 9.2 ✅ **已 archive（2026-08-21，OP-0821-G）** —— 9.6/9.7 阻塞解除后执行 `openspec archive sc2-weekly-report-mvp --yes`，5 份 delta spec（`sc2-anomaly-review`／`sc2-feed-source`／`sc2-metric-engine`／`sc2-report-delivery`／`sc2-weekly-report`）已合入 `openspec/specs/`。🔴 **验收走的就是那条 sweep 告警的原判据**：`openspec/specs/` 下 `sc2-*` 由 **0 命中变为 5 命中**，可追溯链断至此修复（详见收工报告与队列 `§一 #361`）。
- [x] 9.3 场景 `CLAUDE.md` 六段式（定位/决策/底座/红线/时间线/依赖）；🔴 **含 D9 过渡期端口豁免留痕**——理由、批准来源、注销条件三项齐全；backup 缺口按 ①(a) 登记
- [x] 9.4 知识资产台账登记：O-1/O-2/O-3/O-4 四项入《跨场景前置数据与知识库任务总表》§一.2
- [x] 9.5 ✅ **已部署并冒烟通过（2026-08-18，Shao Peishen 同日答 (a) 解除 3b）**。🔴 **端口 8096，不是原定的 8095**——部署前实测 `.51` 上 8095 已被 `ZhuopinRecruitAgent`（uvicorn，计划任务 Running）占用、8090 已被 `UnifiedPortalGateway` 占用，design 审 ④(a) 的依据「顺延现网 8091-8094」是错的（那次端口普查漏了两个），Shao Peishen 当日改判 8096。发布收口四关逐项：**部署** `C:\sc2\{app,zhuopin_platform,.venv}` 扁平布局 + `deploy-server.ps1`／`sync-to-server.ps1`（均带 UTF-8 BOM）；**防火墙** `Sc2-WebServer-8096` 入站规则已建，**并从笔记本外部实测** `/api/ping` 200（非只在 `.51` 本机）；**冒烟** ping 200／匿名访问被门禁 302／登录后关键页 200／`POST /api/refresh` 真实全量重算 200 耗时 102 秒／重算后页面走快照 0 秒／重算全程进程未重启（PID 恒为 6256）；**可常驻** 计划任务 `Sc2WebServer`（SYSTEM + AtStartup + 失败重启 3 次）State=Running，**进程 CreationDate 每次 sync 真实刷新已逐次核对**（4584→4464→6256），非只信脚本打印「已重启」；**回滚 SOP** 写在 `deploy-server.ps1` 收尾输出（End/Delete 计划任务，防火墙规则可留）
- [x] 9.6 ✅ **已完成（2026-08-21 由 OP-0821-G 核实并解除，两条阻塞理由均已不成立）**：① **归属**——判例包确由 Cowork 起草，已作为 **采购部#17「SC2 采购周报口径判例批改」** 于 **2026-08-20 12:20 UTC** 推送（本 CC session 未自行起草，原派单件「不要自行起草跟进信」仍被遵守）；四项基线口径逐一对上——判例表 A ＝ O-1 指标口径（收货量／在途金额，3 条真实采购订单行）、判例表 B ＝ O-4 异常波动阈值（5 个真实周，±50% 回测 11 周中 7 周触发）、问题 3 ＝ O-3 给谁看与频率、问题 4 ＝ O-2「3 人天/月」真实基线核实，**基线数 4 项一项不缺**；② **串行闸**——当时锁闸的 采购部#16 已于 **2026-08-20** 转 `📥 已回件并回灌`，闸开后 #17 随即发出。
  - **2026-08-21 解除依据（写明用什么核的）**：⑴ 权威载体 `6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` 该两行的「发送状态」列——`采购部#16` ＝ `📥 已回件并回灌（2026-08-20，Cowork 全景路线图线 OP-0820-D 转态）`、`采购部#17` ＝ `✅ 已推送 2026-08-20 12:20 UTC`；⑵ 两个 commit——`e4afa70`（信件建档）与 `30469bf`（发送状态回填），`git show 30469bf` 作者时间 `Thu Aug 20 20:20:28 2026 +0800` ＝ **12:20:28 UTC**，与状态列逐字对上，且 commit 前缀是 `bot(跟进信)`——**是发送机制自动回填的，不是人手写上去的**；⑶ 信件真身与同名 docx 均在库（`6-人才与组织/部门AI专员跟进/采购部-姚祖怡-跟进-2026-08-20-SC2采购周报口径判例批改.md`）。
  - ✅ **`采购部#17` 已于 2026-08-21 转 `📥 已回件并回灌`（Cowork 拆件巡逻第二班），本项依据比勾选当时更强——不只是「已发出」，是「已发出且已闭环」**：姚祖怡四件事全部作答无一留白（判例表 A 3 条与判例表 B 5 条全勾 ✏️ 改判并写了理由，问题 1 答「周五晚 8 点自动给出本周的，挂页面 ＋ 同步推群」，问题 2 答「约 1 人天/月」）；勾选真实性经直读回件 `word/document.xml` 的 `w14:checkbox` 复核（我方原件 24 个控件全 `val=0`，回件 8 个 `val=1`），**不是文本提取——`python-docx` 对该控件返回空串，外观与「一条都没勾」完全一致且不报错**。
  - ⚠️ **但判据本身没变，这一点比上面那条结论要紧**：本项要求的是「判例包已起草并发出」这个**动作**，不是「专员已回件」——**即便回件当时尚未到达也应勾**。把「专员是否回件」当成新的阻塞理由，会让 SC2 的可追溯链断无限期挂着。⇒ **本次是依据变强，不是判据变松。**
  - 📌 **回件带来的 8 条改判属 SC2 下一轮口径回灌**（已回灌 §一 `#361`），**与本次 archive 是两件事**：archive 修的是「已上线场景 spec 层零落地」这个可追溯链断，口径改判走各自的建造/变更包流程。
- [x] 9.7 ✅ **已完成（2026-08-21 由 OP-0821-G 逐条核实）**：三条硬前置 ＋ 串行闸共四条全过，**信已实际发出**，本项不再是待办而是已发生的事实。
  - **2026-08-21 解除依据（写明用什么核的）**：① **已 ff 合入 master**——`git merge-base --is-ancestor` 实测 `c39e33d`（代码）与 `b823860`（根 CLAUDE.md）**均已是 `origin/master` 的祖先**（非只看分支名）；② **`.51` 已部署且冒烟通过**——见 9.5（端口 8096，**笔记本外部**实测 `/api/ping` 200、匿名 302、`POST /api/refresh` 真实全量重算 200／102 秒）；③ **真实案例端到端复现**——2026-W34 期真实周报已渲染在 `.51` 页面，判例表 A 三条单号（`ZPCG20260413005`／`ZPCG20260415013`／`ZPCG20260409002`）均可按单号在 U9C 直接查到；④ **串行闸已开**——采购部#16 于 2026-08-20 转 `📥`（依据同 9.6 ⑴）。
  - **实际发送结果**：采购部#17 于 **2026-08-20 12:20 UTC** 真实推送（commit `30469bf` 自动回填发送状态为证）。
- [x] 9.8 ✅ commit + push（`c39e33d` 代码＋`b823860` 根 CLAUDE.md，均已 ff 入 master，服务器五文件 SHA256 与 master 逐字节一致）；**台账已重跑**——结果为空操作（仅时间戳变化），因本批改动全在 `4-数字员工/` 与 `openspec/` 下，两者均在台账扫描范围之外，故未提交该时间戳变更；**队列行回写：无行可写**——按 design 审 5(a) 本任务自始未立队列 §一 行（WIP 超限），跨会话载体是根 `CLAUDE.md`「当前进度」段与本文件
