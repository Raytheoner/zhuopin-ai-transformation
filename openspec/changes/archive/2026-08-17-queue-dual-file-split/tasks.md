> **2026-08-11 更新（apply 已执行，如实回填）**：Shao Peishen 回复"b；"，明示选择跳过 1-14 天观察期、按 (b) 立即执行 apply（其判断：现时有紧急理由，见队列 #315 行内回写）。**本节以下勾选反映 apply 阶段真实完成情况，未完成项如实留空、不假装打勾。**

## 0. design 审前置

- [x] 0.1-0.8 Shao Peishen 以"b；"一次性整体授权跳过观察期、批准进入 apply（未逐决策点单独复核书面记录；八个决策点均按 design.md 已列"默认项"执行，无一处偏离默认项）。

## 1. 观察期门槛（apply 前必须满足，非本轮范围）

- [x] 1.1 Shao Peishen 明示跳过理由（"b；"回复 + 后续"可以动手了，按 (b) 重新取快照后立即写"）。
- [x] 1.2/1.3 **N/A（Shao Peishen 2026-08-17 拍板 (a)）**——观察期本身系其 2026-08-11 以「b；」授权跳过，**条件永久不可满足**，不标 N/A 本包将永远无法出清单。原登记（保留）：未做（观察期本身被跳过，无观察数据可复核）——**如实登记为跳步，非本变更包缺陷**。⚠️ **N/A ≠ 已完成**：这两项从未执行过，勾选仅表示"不再等待"，不表示"已达成"。

## 2. 单测先行（决策点 4/5：路径解析与幽灵副本检测）

- [x] 2.1 `queue_table` 双路径单测（29 passed，含合法/非法域）。
- [x] 2.2 编辑锁 `--domain` 路由单测（`DualFileRoutingTests`）。
- [x] 2.3 影子副本漂移检测单测（`ShadowCopyCrossWorktreeTests`，真实跨 worktree 复现）。
- [x] 2.4 §二 域路由单测（未声明目标文件时默认落机制环境文件，非拒绝——见 design 决策点3"迁移期妥协"，与本条原文字面表述不同，已按妥协方案实现并测试覆盖）。

## 3. 实现：权威路径解析收拢（决策点 4）

- [x] 3.1 `queue_table.py` 新增双常量/`resolve_queue_path`/`iter_queue_paths`——已部署 commit `1ec886e` 起，`5-平台底座/zhuopin_platform` 29 passed。
- [x] 3.2 `工具-落库sweep.py::QUEUE_REL` 切换——2026-08-11 补部署（此前 commit 声明已部署但实际未同步到主工作区，本次核实后补齐），182 passed+6 subtests。
- [x] 3.3 `工具-共享文档编辑锁.py::DEFAULT_TARGET` 切换为按域解析，另于 apply 中途追加绝对路径归一化止血（commit `559a691`，锁域分裂止血，157 passed+5 subtests）。
- [x] 3.4 `工具-队列查询.py` 切换——2026-08-11 补部署，双文件联合查询真实验证通过（`--row 327` 命中标注文件名）。
- [x] 3.5 `工具-文档台账生成.py::QUEUE_PATH` 切换为遍历——2026-08-11 补部署，真实跑通（324 份文档，20 待补状态头，零异常）。
- [x] 3.6 `工具-队列结构lint.py`——**与原文预期不同：需要独立改动**（未随 3.3 自动带出，因其 `QUEUE_PATHS_REL` 是模块级独立常量），2026-08-11 补部署，真实跑通两份生产文件均通过。
- [x] 3.7 aibot 消费者：**最小止血已完成（2026-08-11 同日追加，commit `d1dd691`）**——`SubprocessQueueEditLock` 的锁域分裂已随 3.3 的绝对路径归一化止血；`repo_paths.py::DEFAULT_QUEUE_RELATIVE_PATH` 与 `run_aibot_service.py`（此前独立硬编码同一旧路径、不受 repo_paths 修复影响）均已改指机制环境文件，消除 `queue_appender._section_bounds` 因旧指针文件无表格标题而抛 `LookupError` 导致队列行静默丢失（同 #286 形态）的现时风险；`register-decision-reminder-task.ps1`/`register-followup-dispatch-task.ps1` 同步改点；`ops/wecom-service-home` worktree 已 ff-merge 至 `d1dd691` 并真实重启 `ZhuopinAibotDevListener`，审计日志确认新连接。**仍未做、如实登记为独立后续**：按 `[D:机/业]` 内容精确路由（当前统一落机制环境文件，与编辑锁未声明域时的既有"迁移期妥协"口径一致，非本次新发明）、跨文件高水位线同步的完整语义。
- [x] 3.8 Cowork JS 片段——**2026-08-11 Cowork 环境总线完成，且范围比原文预期多一个载体**。
  - **这不是待办、是拆分当天即已发生的现网故障**：两个 Cowork artifact 都在读 11:37 起只剩 1237 字节的指针文件 `跨桌任务队列.md`（正文只有两行去向指引，`## 一、` 标题与表格一个都不在）。修前实测 `zhuopin-project-status` 取到 §一 **0 行**、`zhuopin-todo-center` 状态机 `$sec` 恒为 0 ⇒ 待领与 §四 全空。
  - **载体一 `zhuopin-project-status`**（派单件点名范围）：数据层 `Read-Doc` 取数（原 L135）改为 `$QF` 双文件循环、行数组按序拼接；新鲜度检查清单（原 L271）由一份改列两份；三处兜底文案（原 L414/581/622）措辞同步。**§一/§二/§四 解析口径一字未改**——机器字段 `[S:…][D:…]` 在两份文件里都保留，下游桶归属/WIP/可 Open 池判据不受影响。
  - 🔑 **一处必须改的真实差异（实测发现，非照搬旧正则）**：§二 的 lookahead 必须由 `(?=\r?\n## )` 加为 `(?=\r?\n## |\z)`——**业务场景那份的 §二 是末节、其后没有下一个 `## `**，沿用原正则会整节失配，实测该文件 6 行批次一行都读不到**且不报错**（本项目记的「工具静默回退」形态）。§一 一并加上，防其日后也变成末节。另：§三 冻结标／§四／编号高水位线只在机制环境那份，对业务场景那份不去找，HWM 另用 `if($hwm -eq '')` 守住，不让后一份的空结果覆盖已取到的值。
  - **载体二 `zhuopin-todo-center`**（**超出派单件范围，核 3.8 时扫出，经 Shao Peishen 2026-08-11 会话内当场授权「本次一并修掉」后才动**）：同一根因，L77 硬编码同一份指针文件。改为按序扫两份、**每份文件开头 `$sec` 归零**（否则前一份的末节会污染后一份的首节归属）；opener 正文的 grep 指引与加载提示语同步为两份文件名。⚠️ **一处自身踩坑并已回补**：首次 `update_artifact` 未传 `description`，工具保留了旧摘要，卡片上仍写「只读直读《跨桌任务队列.md》」——已第二次调用显式传 `description` 补正，两次均已本机 PowerShell 反查落盘。
  - **真实渲染验证（A4 上一轮的缺口，本次不重复）**：不只核源码文本。① 用 node 把 `PS_FULL` / `PS` 两个 JS 字符串数组 `eval` 出来（顺带证明 JS 侧无语法错误）写成 `.ps1`，在本机**真实执行**，取回真实 JSON/HEX；② 把该真实产出注入同一份 HTML 作 stub，**在真实 Chrome 里真实打开并截图**。实测：`zhuopin-project-status` §一 **127** 行／§四 **25**／HWM `§一 #328 ｜ §四 #59`／可 Open 池 **18**／全局收工态 wkN **10**（桶：已完成 86・在办 18・待领 10・定时触发型 3・挂起 4・搁置 2・其他 4，**未出现 #312 那次「122 行全落其他桶」的形态**）；`zhuopin-todo-center` 待领 **20** 项／§四 **25** 项。三张受影响的卡（收件箱 21 项｜18 项急、可 Open 池、全局收工态）与待办中心均已截图确认真实出数。
  - **如实登记的验证边界**：真实渲染跑在 Chrome + stub 上，**未在 Cowork 应用内截图**——Claude 桌面应用不在 computer-use 可授权应用清单里，无法驱动。未被本次演练覆盖的只剩 `window.cowork.callMcpTool` 这一段传输，而它**不在本次改动面内**（数据层 PS 已真实执行、渲染层已真实渲染，两端都验过）。
  - **§二 待处理批次实测为 0**（124 行状态列开头全是 ✅）——已单独核对确认是真实数据，非解析失败；本次收工登记的批次行将成为它的第一条自然验证样本。

## 4. 实现：幽灵副本检测（决策点 5）

- [x] 4.1 `acquire`/`status` 输出新增绝对路径打印——已实现并部署。
- [x] 4.2 本地影子副本漂移检测——已实现并部署，含真实跨 worktree 复现测试。

## 5. 一次性机械切分（决策点 6，观察期门槛通过后执行）

- [x] 5.1-5.4 已完成（commit `d953009`），零信息丢失（byte-safe 切分算法 + 自动化校验），含两处存量数据异常（#325后孤儿内容/§二重复表头）原样保留、未擅自修正。
- [x] 5.5 已完成，commit message 已标注不完全可逆点。

## 6. 四类消费者真实场景验证（决策点 7，#315 期望产出③）

- [x] 6.1 编辑锁 `acquire`/`release` 真实跑通（部署验证 + #327 登记全流程，含中途发现并修复 HWM 正则误撞 frontmatter 的真实 bug）。
- [x] 6.2 sweep 真实批次落库验证——**2026-08-17 收口时业务场景侧亦已真实达成，本项闭合**。原登记（保留）：机制环境侧已真实达成，业务场景侧仍缺；整体保持未勾（值周巡检 2026-08-11 实测更正，原登记「未做」已不准确）。
  - ✅ **2026-08-17 CC 独立复核（未照抄 #315 行内清单，逐条比对文件真身与 git 历史）**：业务场景件 §二 现有 **15 条批次，其中 9 条落在拆分（`d953009`，2026-08-11 11:37）之后**——`B-0812_块B回补拆件巡逻三行回件与物料看板立行`／`B-0813_业务总线两封回信起草登记待审`／`B-0813_docx拍板a落档`／`B-0813_followup-letter-skill-v3.2同步`／`B-0813_两封回信真实送达与四行收口`／`B-0813_335派单件`／`B-0815_CC复命回灌与328子项4增补`／`B-0815_328子项6首次实用验证与一次误更正撤回`／`B-0815_328子项4丢失风险取证与a拍板`，**15/15 状态列均为「✅ 已完成（sweep 自动落库 <UTC 时刻>）」，无一条人工落库**。
  - **不止采信文件自述，另做 git 侧抽验**：`B-0815_CC复命回灌与328子项4增补` 自述「sweep 自动落库 2026-08-15 11:30 UTC」，对应 commit `5f2bf5e`（作者时间 2026-08-15 19:30:19 +0800 ＝ **11:30:19 UTC，逐分吻合**），其 diff 确实同时写入 `跨桌任务队列-业务场景.md` §二 该行与 `跨桌任务队列-机制环境.md`——**坐实 sweep 真实在业务场景件上取过活，非仅机制环境件**。
  - ⇒ 原文所记「业务场景侧仍缺、触发条件＝下一条业务类任务收工登记批次时自然发生」**已于 2026-08-12 起自然发生 9 次**，全部为自然轮次，符合本项原定「不得人为构造」的验收措辞。
  - ✅ **机制环境文件侧：已真实达成，非单测**。拆分（`d953009`，11:37）之后 sweep **真实处理过至少两条**该文件的 §二 待处理批次并推送——`B-0811_立行328与325开头校准`（commit `c1770a2`，06:39 UTC 轮）与 `B-0811_队列315-3.8看板JS双文件迁移`（3.8 收工登记的那条，在其"交 CC commit"口令被执行**之前**即被某轮 sweep 自然取走）。两条均为**自然轮次、非人为构造**，符合本项原定的验证方式。
  - ❌ **业务场景文件侧：仍缺，且不得人为构造**。实测该文件 §二 **至今零批次**（`跨桌任务队列-业务场景.md` 的 §二 自拆分起从未有过任何行），故 sweep 从未在该文件上取过活。**触发条件＝下一条业务类任务收工登记批次时自然发生**，届时观察一轮即可闭合本项。
  - 🔑 **本次更正暴露的一处判据缺陷（值得记）**：3.8 收工时把这条批次称为"6.2 的天然样本"并请 CC 手动 commit——**若真按其口令执行，样本会被销毁**：CC 手动提交后文件转 clean，而 sweep 只对**脏文件**取活，该批次会变成"永远取不到活的陈旧待处理行"（同族形态今日已第三次出现，见 §二 `B-0811_325分叉手动rebase收尾`）。**⇒ 通则：凡以「让 sweep 自然处理」为验证目的的批次，一律不得由人手先行 commit。** 本次是 sweep 抢先跑赢了那句口令，属侥幸，不是设计。
- [x] 6.3 值周巡检真实周报——**2026-08-17 周一那轮即是**（原登记「未做」，2026-08-17 收口时闭合）。
  - ✅ **CC 独立复核**：`1-转型规划/0-全景路线图/本周计划-2026-08-17.md` 真实存在（28,393 字节，frontmatter `生成方: Cowork 值周巡检（定时代办）`／`前置: 已先跑 zhuopin-queue-audit 跨会话对账审计并修正队列`，符合协议「开跑前先跑对账审计」的要求，非徒有其名的周报）。
  - **决策点 7 原验收措辞是「报告正确合并展示两份文件的机制类/业务类统计，不遗漏任一文件、不重复计数」**——该报告 L337 自述「队列两份真身本轮均直读（机制环境 1.34 MB／业务场景 254 KB），指针文件 `跨桌任务队列.md` 未被误读」，且其正文以合并口径给出机制类可动 WIP＝24 的统计。**「未误读已成纯指针的旧文件」这一点正是 2026-08-13 修死指针时点名要在本班验证的那条**，本轮真实通过。
- [x] 6.4 拆件巡逻真实来件路由——**2026-08-17 早班即是，且是双文件域路由的首次真实跑通**（原登记「未做」，2026-08-17 收口时闭合）。
  - ✅ **CC 独立复核 commit `5be32a4`（2026-08-17 09:36）的真实 diff——同一次提交内两份队列文件各按域落位**：`跨桌任务队列-业务场景.md` **+1 行** ＝ 新增 §一 #340（`[S:open][D:业]`，QD-B 跨模块校验评估与回话）；`跨桌任务队列-机制环境.md` **+6/-1 行** ＝ §一 #336 就地转 `[S:done][D:业]` ＋ 新增 §四 #63／#64 ＋ §二 批次行 `B-0817_拆件巡逻336陈忱质量部6回件`。**§四 按决策点 1 本就只存在于机制环境件，落位正确。**
  - ⚠️ **一处如实更正，未照抄 #315 行内表述**：#315 行内写「销行 §一 #336 落**业务场景件**并转 `[S:done][D:业]`」——**与文件真身不符：#336 实际位于机制环境件第 128 行**（`grep "^| 336 |"` 于业务场景件零命中）。**成因非路由失败**：#336 系 2026-08-15 企微反馈自动归档由机器人 `queue_appender` 追加，而 3.7 登记的「迁移期妥协」正是**机器人当前统一落机制环境文件、尚未按 `[D:]` 精确路由**；拆件巡逻是**就地销行**、未搬家，符合「不擅自搬动历史行」纪律。**⇒ 本项验收依据改以 #340（新增行按 `[D:业]` 正确落业务场景件）＋ §四 #63／#64（正确落机制环境件）为准，结论不变；但 #336 这一条不能作为「机器人来件按域路由已通」的证据——那部分仍是 3.7 明列的独立后续。**
- [x] 6.5 "共用锁 vs 独立锁"——**已拍板：独立锁（每份队列文件各自一把）**。其阻塞前提「6.1-6.4 未全部完成」已随上述三项闭合而解除（Shao Peishen 2026-08-17 授权本包 owner 复核后采纳或另判）。
  - 🔴 **CC 复核结论：另判，不采用「维持 design 推荐项＝共用锁」这一表述——它把 design 的推荐说反了**。`design.md` L230 原文：「**推荐倾向独立锁**（否则拆分就拿不到方案件 §4.1 表"文件锁"项列出的真实解耦收益）」。**design 推荐的是独立锁，不是共用锁。**
  - ✅ **且独立锁已是既成实现，无需任何改造**：`0-学习与工具/工具-共享文档编辑锁.py::_lock_path()` ＝ `target_path.with_name(target_path.name + ".editlock")`，**锁文件名由目标文件名派生 ⇒ 两份队列天然各持一把**；快照（`_snapshot_path`）与互斥体（`_mutex_path`）均在该锁路径之上再派生，同样逐文件独立。现场佐证：`1-转型规划/0-全景路线图/` 下只存在 `跨桌任务队列-机制环境.md.editlock.mutex.stale`，命名即逐文件。
  - **design 对独立锁提的唯一顾虑——「改高水位线时业务场景文件的写入者感知不到」——本轮未观察到**：0817 一天内 4 次不同身份 acquire／release 交替（拆件巡逻→sweep→值周巡检→sweep）全部正常串行、无一次误判占用。⚠️ **但如实登记该观察的证明力边界**：这 4 次的串行表现在共用锁下同样成立，**该观察并不能区分两种方案**；真正支撑本拍板的是上一条「独立锁已部署且四类消费者在其下无异常」这一既成事实。
  - ⚠️ **跨文件高水位线同步的完整语义仍未做**，属 **3.7 已明列的独立后续**，不因本项拍板而闭合。
- [ ] 6.6 仓库外载体扫描——🔴 **2026-08-17 由 `[x]` 退回 `[ ]`（Shao Peishen 当日拍板 (b) 的实质部分，非「打个勾」）；欠账已挪出本包、并入 §一 #98（首期体检 2026-08-28），本包带着这条明示欠账归档**。
  **退回理由**：下方 `[x]` 所依据的「②③④ 其余三类载体零命中」已于 2026-08-13 被证伪（见本条内嵌的环境总线追记——两份 `Claude\Scheduled\*\SKILL.md` 在 08-11 实跑日之前就各含 1 处旧指针并带病运行两天），且**剩余 63／67 处命中从未按「载体」分组逐类核定**。**⇒ 不得带着一个已知为假的勾归档。**
  **欠账去向**：§一 #98「月度环境体检例行」已并入一项（2026-08-17 值周巡检并入，协议〇.10 并入优先、未取号）——重跑 `python 0-学习与工具/工具-仓库外载体扫描.py "跨桌任务队列.md" --skip-http`，命中按「载体」分组逐类核定，**每一类的「零命中」都要独立证伪**。
  ⚠️ **本条以下原文原样保留，不删不改**（历史记录不追改）：其立论、其自证有效的论证、其两处盲区登记，以及 2026-08-13 的追记，都是这条欠账为何存在的依据本身；**保留它们才使日后回看的人不必再考古**。
  ━━━ 以下为 2026-08-11 原登记 ━━━
  ~~[x]~~ 仓库外载体扫描——**值周巡检 2026-08-11 实跑完成**。命令：`python 0-学习与工具/工具-仓库外载体扫描.py "跨桌任务队列.md" --skip-http`。结果：① **Cowork artifacts 命中 63 处**，其中当前版 `index.html` 的 L135（数据层 `Read-Doc` 取数）与 L271（新鲜度检查清单）为**真实取数点**、L414/581/622 为兜底文案，`versions/` 下数十处为历史快照不计；②③④ 其余三类载体零命中（`.51` 四服务页面已 `--skip-http` 跳过、已安装版 skill、`Claude\Scheduled` 定时任务真身）。
  🔑 **本条的价值不在「跑过了」，在于它当场自证有效**：该扫描的产出**正是本变更包 3.8 要修的那件事本身**——它扫出的是一个**真实现网故障**（两个 Cowork artifact 因拆分而集体读空），而不是一次零命中的形式化通过。**一个只会返回零命中的检查，无法证明自己还活着**；这一次它给出了非零、可复现、并直接驱动了修复的结果，故 6.6 可以判为真实通过，而非「执行过流程」。
  ⚠️ **一处如实登记的扫描盲区（3.8 执行中发现，不影响本条勾选）**：扫描以 `跨桌任务队列.md` 为关键词、按命中处计数，**但只逐一核定了 `zhuopin-project-status` 的命中行**；同一批 63 处命中里还含**第二个 artifact `zhuopin-todo-center`**（L77 硬编码同一份指针文件、同样已读空），系 3.8 执行时逐个 artifact 复核才发现。⇒ 教训：**命中处按「载体」分组核定，不要只核最显眼的那一个**——63 这个总数当时看着像"都是同一个文件的多处"，实际跨了两个载体。

  🔴🔴 **2026-08-13 环境总线追记：本条上方记的「④ `Claude\Scheduled` 定时任务真身 零命中」与事实不符，且该误记已造成两个定时任务带病运行两天** ━━━
  **事实**：`huijian-chaijian-patrol`（mtime 2026-08-08）与 `weekly-status-update`（mtime 2026-08-09）**两份 SKILL.md 自拆分起就各含 1 处 `跨桌任务队列.md` 旧指针**，均早于本条实跑日（08-11）。**照该指针读会解析到零行、静默输出「空巡」**——2026-08-13 第二班拆件巡逻实测撞上并当场绕开（当日恰为真空巡，故未造成漏拆，**下一个有待拆件的班次即会漏**）。
  **扫描工具本身没问题**：2026-08-13 用**同一命令、同一关键词**复跑，工具准确报出「【④ 定时任务真身】命中 3 处」并逐行给出路径与行号，与直接 `Select-String` 结果逐条一致。
  ⇒ **08-11 那次「零命中」是误记，非工具缺陷**。**成因未定、两个候选**：⒜ 只读了输出里最显眼的「① 命中 63 处」，②③④ 三类未逐类核对即写作零命中；⒝ 当时确有一个此后被修掉的 bug。**决定性证据是 08-11 那次的原始输出，而它没有留存** ⇒ 无法判定，**如实登记为未决**。
  🔑 **本条自己写下的立论「一个只会返回零命中的检查，无法证明自己还活着」在这里反噬了**：那个非零来自**类别 ①**，而 ②③④ 的零里**至少有一个是假零**。**「某一类返回了非零」不能证明「其余各类的零是真的」——每一类的零都要独立证伪。** 这是「枚举法会反复给出『已完备』的错觉」的**第五个实例**（前四：#300 入口清单／G6 CLI 穷举维度／#314③ 消费者清单／#321 README 全表比对）。
  ⇒ **两处旧指针已由环境总线于 2026-08-13 当场修复**（各改为并列读两份真身，并在文内写明"照旧指针读会静默空巡"的成因）；备份留在 `C:\Users\Paul Shao\Claude\Scheduled\_backup-20260813\`。
  ⚠️ **本条勾选状态未改动**——**是否因本次追记而退回 `[ ]`，属本变更包 owner 的判断，环境总线不代翻**（同「不擅自翻状态」纪律）。**但本条现有的完成依据已被削弱：它声称的四类载体全覆盖，实测有一类漏了两个真实载体。**

**⚠️ 收工如实登记（2026-08-11，同日三次更新）**：本变更包 apply 阶段完成核心迁移（物理切分 + 6/6 消费者路径解析，含 aibot 最小止血）与两处关键止血（锁域分裂、aibot 队列行静默丢失）。**第三次更新（Cowork 环境总线）**：3.8 已完成（两个 Cowork artifact 的现网读空故障已修并真实渲染验证）、6.6 已完成（仓库外载体扫描实跑，且其产出正是 3.8 本身）。

**仍未完成，不假装勾完**：
- **3.7** 的完整域路由（当前统一落机制环境文件，是迁移期妥协）与跨文件高水位线同步的完整语义——只做了最小止血。
- **6.2 / 6.3 / 6.4** —— sweep 真实批次落库、值周巡检真实周报、拆件巡逻真实来件路由三类真实场景验证。**6.2 本次已具备自然验证条件**：3.8 收工登记的批次行是双文件拆分后 §二 的第一条待处理批次（此前 124 行状态列开头全为 ✅、待处理为 0），下一轮 sweep 自然运行时即可观察，**建议不人为构造**。
- **6.5**「共用锁 vs 独立锁」—— 维持 design 阶段推荐项（共用锁），因 6.2-6.4 未全部完成，仍无法基于真实观察拍板。

**⇒ 仍不满足「完工即归档纪律」的归档前提，本次不 `/opsx:archive`**，留待 6.2-6.4 自然发生、3.7 完整语义补齐后再评估。

---

## 🟢 2026-08-17 收口归档（CC，独立复核后执行 Shao Peishen 当日拍板 (b)＋(a)）

**上方 2026-08-11 那段收工登记的结论已被此后的真实进展推翻，故在此收口，原文不删。**

**⚠️ 本次是「带着一条明示欠账归档」，不是「补完了才归档」** —— 这是拍板 (b) 的字面含义，写在这里使日后回看的人不必考古。

| 项 | 2026-08-11 状态 | 2026-08-17 处置 | 依据 |
|---|---|---|---|
| 1.2／1.3 | `[ ]` 未做 | **`[x]` N/A** | 观察期系 2026-08-11「b；」授权跳过，**条件永久不可满足**；不标 N/A 本包永远无法出清单。**N/A ≠ 已完成** |
| 6.2 | `[ ]` 业务场景侧缺 | **`[x]`** | 业务场景件 §二 拆分后已落 9 条批次，全部 sweep 自然轮次；git 侧抽验 `5f2bf5e` 时刻逐分吻合 |
| 6.3 | `[ ]` 未做 | **`[x]`** | `本周计划-2026-08-17.md`（值周巡检生成，前置已跑对账审计，两份真身均直读、未误读指针文件） |
| 6.4 | `[ ]` 未做 | **`[x]`（附一处更正）** | commit `5be32a4` 同次提交内两份文件各按域落位；**但 #315 行内「#336 落业务场景件」与真身不符，已更正，该条不作为证据** |
| 6.5 | `[ ]` 阻塞于 6.1-6.4 | **`[x]` 拍板独立锁** | 阻塞前提已解除；**另判**——design 推荐的本就是独立锁（#315 行内说反了），且 `_lock_path()` 逐文件派生，独立锁已是既成实现 |
| **6.6** | `[x]` | **🔴 退回 `[ ]`** | 其完成依据（②③④ 零命中）已于 2026-08-13 被证伪；**不得带着一个已知为假的勾归档** |

**归档后仍然开放、已挪出本包的欠账（逐条有主，非悬空）**：
1. **6.6 的仓库外载体逐类核定** → 已并入 **§一 #98**（月度环境体检，首期 2026-08-28）。
2. **3.7 的完整域路由**（机器人来件当前统一落机制环境文件，是迁移期妥协——#336 即其实例）**与跨文件高水位线同步的完整语义** → 仍为 3.7 行内明列的独立后续，**归档不使其闭合**。
3. **旧文件 `跨桌任务队列.md` 指针状态的保留时长** → design.md Open Questions 原有项，未定，归档不使其闭合。

**为何这仍然构成一次合规的归档**：本包的交付主体（物理切分、6/6 消费者路径解析、幽灵副本检测、四类消费者真实场景验证）已全部达成并经真实场景验证；剩余三项**均非本包主体的未完成部分，而是三个独立的后续任务**，且**每一项都已落到一个有主的载体上**。压着整包不归档既不能让这三项更快被做掉，还会让 sweep 的「疑似遗忘归档」告警持续空响——**那正是本轮 B-1 记的「WIP 提示连响 6 次无人理会」的同构失效方式**。
