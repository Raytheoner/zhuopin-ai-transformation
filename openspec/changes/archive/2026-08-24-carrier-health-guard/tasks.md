# carrier-health-guard Tasks

## 0. 待定夺

**本包三个超出五项预授权的决策点已于开工时提问并全部获答复（Shao Peishen 2026-08-23：Q1=(c)、Q2=(a)、Q3=(b)），记录在 design.md 决策点 1／2／7。本节无未闭合定夺项。**

- [x] 0.1 子项 B 信号出口档位 —— ✅ (c)：加列 ＋ sweep 第 5 类告警 ＋ 人工触发对齐脚本
- [x] 0.2 常驻执行体名单来源 —— ✅ (a)：从计划任务反查
- [x] 0.3 `wecom-service-home` 落后 305 本次治不治 —— ✅ (b)：同批 ff 并重启验活
- [x] 0.4 🔴 **0.1 的答案已被 Shao Peishen 同日推翻并改定**（补进队列 §一 #338 行首，明写优先级高于派单说明）：(c) 档仍是一次性对齐、只换了触发人；改为**分两级持续同步**——① ff 每轮由 sweep 自动做；② 重启按需，判据复用 #87 ⑶⑷ 白名单，须可一键关闭、首月只 ff。**本包按改版实现，0.1 的 (c) 作废。**

## 1. 前置取证（全部实跑，结果贴回 proposal/design）

- [x] 1.1 量根与 13 份场景/底座 `CLAUDE.md` 字节数，确认上线当天是否零告警
- [x] 1.2 实测 `wecom-service-home` 落后数（总数 ＋ 触碰 `CLAUDE.md` 数 ＋ 触碰服务目录数 ＋ 触碰队列文件数）
- [x] 1.3 实测四个 Zhuopin 计划任务的 Action 路径，确认哪几个指向 worktree
- [x] 1.4 `git grep` ＋ 全盘遍历双向确认 `工具-孤儿worktree扫描.py` 无调度器调用
- [x] 1.5 `git check-ignore -v` 实跑两个新状态文件，贴回 proposal
- [x] 1.6 确认 `run-*.ps1` wrapper 的 git 跟踪状态（影响 `#355` 能否被 ff 修复）
- [x] 1.7 读心跳文件当前值并标注 UTC/本地双基准

## 2. 子项 A：`CLAUDE.md` 尺寸/批次守卫（sweep 第 4 类）

- [x] 2.1 新增常量：两个阈值、状态文件路径、再提醒间隔
- [x] 2.2 尺寸量取——覆盖根 ＋ 两组 glob，排除 `.claude/worktrees/**`
- [x] 2.3 顶部段批次日期跨度判据——动态导入 `工具-CLAUDE进度段lint.py`，导入/解析失败显式报「判据不可用」
- [x] 2.4 接入 `_track_and_alert_standing_state`，key ＝ 文件相对路径（不含数值）
- [x] 2.5 **每轮回显「当前值/阈值/差额」**，零超限时也不省略
- [x] 2.6 挂进 `main()` 流程

## 3. 子项 B-1：执行体**持续同步**（sweep，改版后）

- [x] 3.1 计划任务反查执行体名单，三态显式区分（成功有/成功无/查询失败）
- [x] 3.2 落后计数（总数 ＋ `CLAUDE.md` 触碰数），用 git 提交计数
- [x] ~~3.3 阈值分档~~ —— 🔴 **随改版整条作废**：自动 ff 后落后恒为 0，`RESIDENT_CARRIER_LAG_ALERT_THRESHOLD` 已删除，配反例单测锁死它不再存在
- [x] 3.4 接入骨架，key ＝ worktree 名；告警语义改为**「有例外」**，正常对齐路径零推送
- [x] 3.7 **`_ff_carrier`**：三道停手闸（ahead>0／已跟踪脏／ff 失败），绝不 revert/rebase/reset
- [x] 3.8 **重启判据复用 `_touches_resident_service`**（#87 ⑶⑷ 白名单），不另写一套
- [x] 3.9 **开关 `CARRIER_AUTO_RESTART_ENABLED`**，缺省 OFF（键缺失/读失败/值不认识一律 OFF）
- [x] 3.10 开关 ON 时**委托** `工具-执行体对齐重启.ps1 -RestartOnly`，退出码直接取
- [x] 3.5 挂进 `main()` 流程
- [x] 3.6 确认 `_announce_resident_service_deployment_hint` 零改动

## 4. 子项 B-2：孤儿 worktree 扫描新增列

- [x] 4.1 新增落后列（两个数值）
- [x] 4.2 取不到时显示「未取到」，不显示 0
- [x] 4.3 确认三个桶的归属逻辑零改动

## 5. 子项 B-3：`工具-执行体对齐重启.ps1`

- [x] 5.1 身份校验（`.git` 条目 ＋ 注册项，不看 `git -C` 输出）
- [x] 5.2 可 ff 校验，不满足即停
- [x] 5.3 固化备份到仓库外 ＋ 回显清单件数
- [x] 5.4 停服：`Stop-ScheduledTask` ＋ 整条进程链先父后子 ＋ 复查零残留
- [x] 5.5 ff-only 合并并校验落后归零
- [x] 5.6 启动 ＋ 验重启（`CreationTime` 比对）
- [x] 5.7 验活（心跳 `alive_at` 晚于启动时刻，UTC/本地双标注）
- [x] 5.8 结构化摘要 ＋ 自身 `exit` 退出码
- [x] 5.9 `-DryRun` 干跑模式（先验流程再动真格）
- [x] 5.10 **`-RestartOnly` 模式**（跳过 ff 三关；落后 0 时不提前退出——那正是改版后的常态）

## 6. 单测

- [x] 6.1 子项 A：超限/未超限/worktree 副本排除/lint 不可用不静默/回显不省略
- [x] 6.2 子项 B-1（改版）：名单三态/正常路径零推送/开关三档语义/委托重启/key 不含数值
- [x] 6.5 **真 git 仓库的 ff 用例**（`ResidentCarrierFfTests`）：真 ff／已对齐／ahead>0／脏／unknown
- [x] 6.6 🔴 反例断言：**ahead>0 时执行体 HEAD 一个字节都没动**（「返回了正确结论」≠「没有动手」）
- [x] 6.7 反例断言：**名单未取到时对任何 worktree 零动手**
- [x] 6.3 子项 B-2：新增列/未取到不显示 0/归桶零改动（反例锁死）
- [x] 6.4 反例单测：告警 key **不得**包含数值（防「每涨一字节即新问题」回归）

## 7. ⑤ R5 尺寸半条降指针

- [x] 7.1 根 `CLAUDE.md` §5 文档治理 R5 —— 尺寸半条降为一行指针
- [x] 7.2 确认只降尺寸半条，条目数/单条长度（J2/J3/J5，已在 CI）与接力件定长（J6）不动

## 8. 验收

- [x] 8.1 `openspec validate --all --strict` 全绿
- [x] 8.2 ✅ 全量回归 **316 passed / 0 failed**（rebase 后复跑；`PYTEST_EXIT=0` 单独取，不经管道）
- [x] 8.3 ✅ sweep 实跑三轮，回显与告警均符合预期；子项A 逐项回显 14 份 `CLAUDE.md` ＋ 顶部段批次日期
- [x] 8.4 ⚠️ `openspec validate --all --strict` ＝ **100 passed / 1 failed**，唯一失败 `sc2-weekly-report-mvp` 属 §四 #99 已登记存量（空目录、零 delta），本包未触碰

## 9. Q3(b) 同批治：`wecom-service-home` 实跑对齐

- [x] 9.1 `-DryRun` 干跑，两种模式各一次（对齐＋重启 / 只重启）
- [x] 9.2 **由真 sweep 自动 ff** —— ✅ 实测 sweep 自动 ff **326 个提交**，落后 309→0；未跟踪件与 28 个 `reports/` 文件全部原样保留
- [x] 9.3 ✅ 实测告警发出：「触碰常驻服务代码路径 **7 处**，需人工重启（自动重启开关 OFF）」，逐条回显命中路径与命中来源（4 处「部署清单」＋3 处「平台底座」）
- [x] 9.4 ✅ 人工 `-RestartOnly` 三关取证：**坑⑴** `Stop-ScheduledTask` 后遗留 powershell+python 两个子进程（wscript 已被杀），先父后子终止、复查零残留；**坑⑵** 进程链 `CreationTime` 由 08-21 12:50 变为 08-24 00:18；**坑⑶** 心跳刷新至 `16:18:08Z`（00:18:08 本地）晚于启动发起时刻。退出码 0，直接取自脚本自身 `exit`
- [x] 9.5 ✅ 复跑 sweep：「已 ff 3 个提交，未触碰常驻服务代码路径 ⇒ 无需重启」，**例外自动解除并推送解除通知** —— 出现→告警→消失→解除的完整常驻状态语义在生产上跑通
- [x] 9.6 ✅ 如实记录：`#355` 孤立 CR **未被本次 ff 修复**（wrapper 不在版本控制内），仍挂 §一 #355
- [x] 9.7 🔴 **实跑当场抓出两个新缺陷并当场修掉＋补反例单测**：⑷ 脏判据漏了 `--untracked-files=no`，未跟踪件把 ff 整个挡住；⑸ 「重启」把两个一次性日任务也计划外拉起（启动后 9 个进程），改为只重启 `Running` 的（修后 3 个）
