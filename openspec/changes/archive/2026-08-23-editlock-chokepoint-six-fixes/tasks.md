# tasks — 编辑锁咽喉六修

## 0. 前置取证（已完成，结论见 design.md）

- [x] 0.1 跑 #351 行内自带的证伪命令，确认 ⑵ 的根因分析成立（结果 `True`）
- [x] 0.2 实测 §二 路径格式违规率（220／1371）与「加存在性校验的误报量」（98）
- [x] 0.3 实测 §二 批次号撞号率（27／174）
- [x] 0.4 实测 ⑷ 判据收窄曲线（整行 65 → 40字 19 → 25字 18 → 15字 13）
- [x] 0.5 从 `reports/sweep-commit.log` 取证 `OP-0822-E` 六个孤儿的出现时刻（12:20 UTC）与其占锁时刻（12:26:16 UTC）的先后 —— **推翻 #351 ⑹ 的差集判据**
- [x] 0.6 核实 `*.editlock*` 的 `.gitignore` 覆盖（`git check-ignore -v` 实测命中 `.gitignore:62`）
- [x] 0.7 核实 ② 是否存在于任何 spec（零命中 ⇒ 无 REMOVED delta 可写）
- [x] 0.8 核实 `append-row` 的现有调用方（企微机器人走独立路径不受影响；两份 SKILL.md 及其已安装副本受影响）

## 1. ⑴ append-row 锁归属校验

- [x] 1.1 `append-row` 新增 `--who` 参数
- [x] 1.2 写入前读锁：有效锁存在且 `--who` 缺失或不符 → 拒绝，提示持锁人＋重试走法
- [x] 1.3 无锁／已释放／已陈旧 → 正常写入，打印「本次为无锁写入」提示
- [x] 1.4 单测：他人持锁+无 `--who` 拒绝 ／ 他人持锁+`--who` 不符拒绝 ／ 本人持锁放行 ／ 陈旧锁放行 ／ 无锁放行

## 2. ⑵-a 裸竖线诊断合并 + 修正命令行

- [x] 2.1 arity 失败时扫全部 cell，含裸竖线者合并诊断
- [x] 2.2 按裸竖线拆分恢复字段；恰好等于预期列数时打印完整可复制命令行
- [x] 2.3 恢复后仍不符 → 只给合并诊断，不猜命令行
- [x] 2.4 单测：复现 2026-08-19 那次真实调用 ／ 无裸竖线时不添噪音 ／ 恢复失败时不打印命令行
- [x] 2.5 **⑵-b 不实现**（Shao Peishen 2026-08-23 拍板选 (b)）——在模块 docstring L116 附近加一行注释，写明该处已被复核并维持

## 3. ⑶ §二 文件清单路径格式校验

- [x] 3.1 实现路径形态判定（扩展名／目录尾斜杠）与四类违规判定（绝对路径／反斜杠／相对前缀／根下不存在的裸文件名）
- [x] 3.2 挂 `append-row` 写入前
- [x] 3.3 挂 release 结构校验（仅本次触碰的 §二 行）
- [x] 3.4 预登记批次行豁免
- [x] 3.5 单测：五条 Scenario 逐条（含根 `CLAUDE.md` 放行、通配路径放行、非路径片段不触及）

## 4. ⑷ 人的属性（性别代词）lint

- [x] 4.1 落 `PERSON_GENDER_ROSTER` 常量（21 人，注释写明唯一来源＝根 `CLAUDE.md` §1）
- [x] 4.2 实现邻近窗口判定（25 字 ＋ 异性名中断 ＋ 「他」非代词用法排除）
- [x] 4.3 挂 release 结构校验（§一／§二／§四 本次触碰行）
- [x] 4.4 `性别豁免：` 行内逃生阀
- [x] 4.5 单测：`其他` 不误报 ／ 同行多人属合法 ／ 超窗口不触发 ／ 豁免只对本行生效
- [x] 4.6 **名录同步单测**：从根 `CLAUDE.md` §1 抽带性别标注姓名，按「§1 ⊆ 常量」方向比对

## 5. ⑸ §二 批次号前缀查重

- [x] 5.1 提取 `B-<四位日期>_<第二段>` 前缀，跨两份队列文件比对
- [x] 5.2 挂 `append-row` 写入前，拒绝时给冲突批次全称 ＋ 建议序号（明写只是建议）
- [x] 5.3 挂 release 结构校验（仅本次新增的 §二 行）
- [x] 5.4 单测：同前缀拒绝 ／ 跨物理文件同样抓住 ／ 不符前缀形态不触发

## 6. ⑹ release 登记完整性校验

- [x] 6.1 `_local_git_status_paths()`：本机 git ＋ `-c core.quotepath=false` ＋ `--untracked-files=all` ＋ 重命名取新路径
- [x] 6.2 `acquire` 把结果存入锁 JSON `dirty_at_acquire`；`--reserve` 路径二次写锁时须保留该字段
- [x] 6.3 `release` 重新取数，按 sweep 后缀匹配口径判覆盖，待处理批次口径复用 `_leading_status_segment`
- [x] 6.4 排除 `*.editlock*` 伴生文件
- [x] 6.5 未覆盖路径按 acquire 快照分两组归因；无快照时按「无法判定」呈现
- [x] 6.6 取数失败 fail-closed
- [x] 6.7 `登记豁免：` 逃生阀（取材面＝note ＋ 本次触碰行，**不扫全文**）
- [x] 6.8 单测：未覆盖拒绝 ／ 全覆盖放行 ／ 已完成批次不算覆盖 ／ 伴生文件不计入 ／ **并发 session 场景** ／ 取数失败 fail-closed ／ 豁免只在取材面内生效

## 6bis. ⑹ 的落库 sweep 身份豁免（apply 期新增，非 propose 时预见）

- [x] 6b.1 `SWEEP_LOCK_WHO` 常量 ＋ 豁免分支（长注释写明「判据对它本就不成立」而非开后门）
- [x] 6b.2 单测锁死：换个身份该拦、sweep 身份放行
- [x] 6b.3 spec 补 Requirement「落库 sweep 身份豁免本项校验」

> 🔑 **本节是 apply 期撞出来的，值得记**：本变更包自己的 239 条单测**全绿**，是跑**邻居工具的测试套**（`test_工具-落库sweep.py`）才发现 ⑹ 会让 sweep 每轮 release 被拒、锁常驻占用、下一轮起跑探锁直接跳过 ⇒ **全项目唯一的落库机制停摆**。**一道守卫的影响面不止于它自己那个文件。**

## 7. 退休校验 ②

- [x] 7.1 删除 `_validate_release_structure` 中 `declares_self` 段与其 violation
- [x] 7.2 既有相关单测**改判而非删除**，用例 docstring 写明退休依据

## 8. 配套文档与部署（不做完就是「修好守卫、打断两个每日在跑的机制」）

- [x] 8.1 `1-转型规划/0-全景路线图/专线opener模板库.md` §〇.7 补 `--who` 与四条新校验
- [x] 8.2 `0-学习与工具/定时任务源码/huijian-chaijian-patrol.SKILL.md` 的 `append-row` 示例补 `--who`
- [x] 8.3 `0-学习与工具/定时任务源码/weekly-status-update.SKILL.md` 同上
- [x] 8.4 同步已安装副本 `C:\Users\Paul Shao\Claude\Scheduled\huijian-chaijian-patrol\SKILL.md`
- [x] 8.5 同步已安装副本 `C:\Users\Paul Shao\Claude\Scheduled\weekly-status-update\SKILL.md`
- [x] 8.6 `diff` 核验源码副本与已安装副本一致

## 9. 收口

- [x] 9.1 全量回归绿：`python -m pytest "0-学习与工具/test_工具-共享文档编辑锁.py" -q`
- [x] 9.2 队列结构 lint 与落库 sweep 在改动后仍可正常跑通（不被新校验误伤）
- [x] 9.3 **⑹ 真实复现**（派单件 §六 硬要求）：造脏文件 → 不登记批次 → 跑 `release` → 原样贴出被拒绝的输出
- [x] 9.4 `/opsx:archive editlock-chokepoint-six-fixes -y`
- [ ] 9.5 ff 入 master 并 push
- [ ] 9.6 §一 #351 逐项回填（⑴～⑹ 各自结论 ＋ ⑵-b 否决记录 ＋ ⑹ 判据改判记录）
- [ ] 9.7 §二 批次登记
