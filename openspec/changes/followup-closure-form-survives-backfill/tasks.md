# followup-closure-form-survives-backfill Tasks

> 🔴 **本包停在 design 审。§1 取证已完成，§2 起全部未开工。**
> 队列 `#353` opener 明写：「本次只做 propose＋design 出件并登记 §四 待审，
> 到 design 审即停、**不得 apply**」。
> 🟩 **2026-09-12 已签认并 apply**（Shao Peishen 回 `P3：1a，2a，3a，4a，5c，6a`，经 Cowork 环境总线
> `OP-0912-E` 转达；apply 泳道 `OP-0912-AB`，分支 `claude/op0912ab-p3-closure-form-apply`）。
> §2–§6 已落地；§7 中 7.2／7.3 两条「首次真实生效／首次真实开闸」须一封**新起草**的真信走完全程，
> 本泳道为无人在场的无头批处理会话、对外真实发送一律留步 ⇒ **暂不归档**（理由见 §0 末）。

## 0. 待签认（六项，全部未完成，本包不得 apply、不得归档）

**六项动手的都不是本 session**（本 session 为**无人在场的无头批处理**，收工即结束）
⇒ 按根 `CLAUDE.md` §5「答完之后由谁动手」判据，**一律不给选项、只登记待总线派发**；
且按「默认项只在两个前提下成立」——第一问答不出执行者 ⇒ **六项一律无默认项**。

- [x] 0.1 **签认 design 决策点 1**（🟩 已签认（Shao Peishen 2026-09-12 回 `P3：1a，2a，3a，4a，5c，6a`，见 design「决策点」节首））：闭环形态标注放「主要事项」列（(a)，同 `#241` 手法）
      而非状态列首段化（(b)，须放宽含 D8 红线门禁②在内的七处等值比较）或新增列（(c)）。
      🔴 **选 (b) 则本包 spec 整段作废须重写**，且必须由 Shao Peishen 亲自签认放宽门禁②。
- [x] 0.2 **签认 design 决策点 2**（🟩 已签认（Shao Peishen 2026-09-12 回 `P3：1a，2a，3a，4a，5c，6a`，见 design「决策点」节首））：回填时是否按闭环形态**直接写闭环态**（＝让机器自动开串行闸）。
      🔴 **本包刻意不推荐任何一边**——(a) 判错则同一收信人出现两封在途信（串行原则是明令
      优先于一切触发点规则的那条）；(b) 判对则起草人仍需写 `串行豁免：` 绕闸（而"拿逃生阀去绕
      它本来要拦的那件事"本项目已明记为坏味道）。**两边都难看，须其本人权衡。**
- [x] 0.3 **签认 design 决策点 3**（🟩 已签认（Shao Peishen 2026-09-12 回 `P3：1a，2a，3a，4a，5c，6a`，见 design「决策点」节首））：回填改「保留式」＋ 回填时把标注**快照**进状态格。
      🔴 它是决策点 5(c) 成立的**前提**：不做快照，防「事后追认」就只能回去加门禁。
      顺带治好的还有 `⏸ 暂缓` 理由被抹掉（proposal「已知未闭合 2」）。
- [x] 0.4 **签认 design 决策点 4**（🟩 已签认（Shao Peishen 2026-09-12 回 `P3：1a，2a，3a，4a，5c，6a`，见 design「决策点」节首））：标注取值限定为闭环四态枚举 ＋ 强制依据文本，越界 fail-loud。
      已知边界＝枚举实际退化为一个取值（`✅ 无需回复`），仍刻意写成四态枚举以免在消费者侧
      复制第二份口径。
- [x] 0.5 **签认 design 决策点 5**（🟩 已签认（Shao Peishen 2026-09-12 回 `P3：1a，2a，3a，4a，5c，6a`，见 design「决策点」节首））：防「事后追认」靠**结构**（快照）而非新增门禁。
      🔴 **配套缓解必须一并批**：闸查询在「标注 ≠ 快照」时须明确报出不一致——不做这条，
      (c) 就是用一个静默失效换掉了一个静默滥用。
- [x] 0.6 **签认 design 决策点 6**（🟩 已签认（Shao Peishen 2026-09-12 回 `P3：1a，2a，3a，4a，5c，6a`，见 design「决策点」节首））：只回改 `质量部#7` 一行，其余 53 行不追改，
      历史覆盖率如实登记为**永久 0%**（不表述为"逐步补齐"）。

> ⚠️ **本包不归档理由（写在机器认得的地方）**：**暂不归档** —— ~~本包按 opener 授权停在
> design 审，未 apply、未落任何生产码~~（2026-09-12 已 apply）；现仍不归档的唯一原因＝
> 7.2／7.3 两条「首次真实生效／首次真实开闸」尚未发生（须一封真信、一次真起草，
> 单测绿不算数），另 4.4 编辑锁两态语义一项如实登记为不一致（须另签放宽等值比较）。

## 1. 前置取证（已完成，本 session）

- [x] 1.1 **坐实覆盖点**：`readme_table.write_status()` 对状态格做整格替换
      （`cells[loc.status_col_index] = new_status`），`delivery.py:390-395` 与
      `approval.py:160` 两处调用它 ⇒ 起草时写在状态格里的内容**在批准那一步就已被抹掉一次**，
      根本轮不到发送（proposal「已知未闭合 3」）。
- [x] 1.2 **推翻队列 `#353` 行给的第一个建议**：全量扫出**七处**对状态格的等值比较
      （`gates.assert_finalized`／`readme_table.assert_draft_pending_review`／`dispatch.py:203,213`／
      `dispatch_followup_letters.py:71`／`工具-共享文档编辑锁.py:1717,1891`），其中一处是
      design.md D8 明文「不得放宽」的**门禁②红线** ⇒ 闭环形态**从来就没能待在状态列里**，
      问题形状是「无处安放」而非「被覆盖」。
- [x] 1.3 **找到它其实早有落脚点**：`质量部#7` 的**「主要事项」列**末尾已写着
      「🔑 **闭环形态＝起草时即判定为 `✅ 无需回复`**（…），**批准发出后直接记 `✅ 无需回复`、
      不占串行闸**」——**起草人早就写了，只是写成散文、机器读不到**。
      实测 `grep -c 闭环形态 README…md` ＝ **1**（全表 54 行仅此 1 处）。
- [x] 1.4 **实测语料标定**：README 表格数据行 **54 行**（第 201–254 行）；
      `✅ 无需回复` 形态 **3 行**（`IT部#8`／`采购部#14`／`质量部#7`，均为 2026-08-13～18 按
      「新增闭环三态」补标）；其余各行闭环依据一律写在状态格 `　━━━　` 之后。
- [x] 1.5 **找到可逐字复刻的先例**：队列 `#241` 的 `目标文件：\`xxx.md\`` 标注
      （`build_target_file_annotation`／`_TARGET_FILE_RE`），同列、同手法、docstring 明写
      「不改变表格结构…因此不受影响，无需同步修改」，2026-08-04 起在生产上跑着。
- [x] 1.6 **找到可逐字复刻的保留式回填范式**：S4 桥一
      `followup_readme_bridge.build_reply_arrived_status()`，其 docstring 的理由逐字适用于本包
      （「覆盖式写入会把它弄丢，而这一格没有任何别处的副本」）。
- [x] 1.7 **核实快照不与既有判据冲突**：`✅ 已推送` 之后的状态格**不参与任何等值比较**
      （七处等值全部发生在 `⏳ 待你审`／`🆕 待发`／`⏸ 暂缓` 三态）；
      `followup_gate` 走前缀匹配 + 归一化；桥一会把整个原状态原样接在其后
      ⇒ 快照能一路活到第九态与闭环态。**此为决策点 3／5(c) 成立的技术前提。**
- [x] 1.8 **`.gitignore` 覆盖核实（强制回答）**：`git check-ignore -v` 对本变更包文件
      **无输出、退出码 1**（未被任何规则忽略）⇒ 变更包正常入库，符合预期。
- [x] 1.9 **one-in-one-out 反向核实（协议〇.9 措施 B）**：逐条看过 followup 链路现有五道守卫
      （门禁①／门禁②／两态语义拦截／串行闸／S4 桥一桥二），**无一能被本判据覆盖**
      ⇒ 不硬凑退休；退休的是一条**人守约定**（`质量部#7` 行内「两态并列勿删任一」的人工留痕）。

## 2. 判据权威源（待签认后开工）

- [x] 2.1 `followup_gate` 新增 `CLOSURE_FORM_MARKER` 与 `parse_closure_form()`，取值从既有
      `CLOSED_STATUS_PREFIXES` 取，**不新造第二份口径**。（2026-09-12 `OP-0912-AB`：`followup_gate.py` §八，
      另含 `ClosureForm`／`build_closure_snapshot_segment`／`extract_closure_snapshot`／`closure_form_mismatch_warning`）
- [x] 2.2 越界取值／空依据 ⇒ fail-loud 并按「无标注」处理（保守方向＝闸仍锁）。（`ClosureForm.problem` 非空且 `value=None`；
      `delivery.py` 审计 `followup_closure_form_rejected`；闸查询 warnings 出声）
- [x] 2.3 单测：合法／越界／缺依据／无标注四类，含「无标注行行为与今天逐字相同」的回归护栏。
      （`zhuopin_platform/tests/test_followup_gate_closure_form.py` 23 例 ＋ `test_closure_form_backfill.py::test_无标注回填结果与本变更前逐字相同`）

## 3. 标注的写入与提取（待签认后开工）

- [x] 3.1 `readme_table.build_closure_form_annotation()`＋提取正则，逐字复刻 `#241` 手法。（正则在 `followup_gate._CLOSURE_FORM_RE`，
      与 `_TARGET_FILE_RE` 同构；`readme_table.extract_closure_form` 为薄封装、消费者侧不复制口径）
- [x] 3.2 反例单测：追加标注后**列数不变**、编辑锁结构校验不报违规。（`test_closure_form_backfill.py::test_追加标注后列数不变_与目标文件标注同列共存`；
      真身 6.1 回改经编辑锁 acquire→release 两轮校验通过）

## 4. 回填改保留式 ＋ 快照（待签认后开工，依赖 0.3）

- [x] 4.1 `delivery.py` 回填改保留式，沿用桥一范式，**不新造第二种分段**。（`resolve_backfill` 返回 `BackfillDecision`；
      保留只对「未发出」家族的附加内容生效——⚠️ 门禁②等值断言下经 `push_followup` 实际不可达，只在直接调用时成立，
      已由 `test_门禁二仍拒绝带附加内容的状态格` 如实钉住）
- [x] 4.2 回填时把标注快照进状态格，标识为「发出时快照」。（`followup_gate.CLOSURE_SNAPSHOT_LABEL`）
- [x] 4.3 无标注时回填结果与本变更前**逐字相同**（含不多出空分隔符）——配单测。（`test_无标注回填结果与本变更前逐字相同`／
      `test_端到端_无标注_行为与今天逐字相同`；既有 `test_supplement_channel.py::test_主表回填语义未变` 一字未改仍绿）
- [ ] 4.4 🔴 **兼容性反例单测**：把「写了快照的状态格」喂给 `classify_status`／`is_closed_status`／
      `is_reply_arrived_status`／桥一转态／编辑锁两处校验，断言结论与「未写快照的同一状态」一致。
      🟡 **部分完成、如实不勾**：`classify_status`／`normalize_status`／`is_closed_status`／`is_reply_arrived_status`／
      `is_not_yet_sent`／`is_dispatched`／桥一 `build_reply_arrived_status`／编辑锁**串行闸**一处——结论一致，
      均配用例（`test_closure_form_backfill.py` 兼容节 ＋ `test_工具-共享文档编辑锁.py::test_闭环形态快照态_串行闸放行且不需串行豁免`）。
      🔴 编辑锁**两态语义**一处结论**不一致**：它按整格等值判（七处等值比较之一，派单件明令不改），带快照段的
      `🆕 待发　━━━　…` 新增行不被拦（风险＝零：门禁②同为等值断言，该形态发不出去）。已由
      `test_新增行终态加快照段_两态语义等值拦截不命中_如实钉住` 钉住现状、spec 场景已如实更正；闭合须 Shao Peishen 另签放宽。
- [x] 4.5 决策点 2 的落点——**仅在 0.2 签认后实现**，未签认前此项 MUST 留空。（🟩 0.2 已签 `2a`＋三护栏 ⇒ 落字：
      spec `followup-status-backfill-preservation` 末节改写为 SHALL ＋ 三条护栏；实现＝`resolve_backfill` 主表分支，
      `form.value == NO_REPLY_NEEDED_STATUS` ⇒ 首段 `✅ 无需回复 <UTC>`、快照段、`✅ 已推送 <UTC>` 后段；
      护栏②反例＝`test_护栏二_越界取值_仍写已推送_闸仍锁_且问题被带回`／`test_端到端_越界标注_审计fail_loud_回填仍已推送`）

## 5. 闸判据与读侧（待签认后开工，依赖 0.5）

- [x] 5.1 串行闸对已发出的信只读状态格快照，不回读「主要事项」列。（结构性成立：编辑锁 `_followup_status_is_closed` 与
      闸查询 `classify_status` 均只读状态格前缀，本包未让任何闸判据读「主要事项」列；反例＝5.3 两条用例）
- [x] 5.2 🔴 `工具-跟进闸查询.py` 在「标注 ≠ 快照」时**明确报出不一致并声明以快照为准**
      （决策点 5(c) 的必配缓解，不做则 (c) 不成立）。（`followup_gate.closure_form_mismatch_warning` 三种形态＋
      `GateReport.closure_snapshot／closure_annotation`，「依据：」行同行输出「当前状态 · 怎样算闭环」；
      `test_工具-跟进闸查询.py` 5 例）
- [x] 5.3 单测：发出后补写标注 ⇒ 闸零效果；起草时即有标注 ⇒ 闸放行且**不需** `串行豁免：`。
      （编辑锁：`test_发出后补写标注_无快照_串行闸仍锁`／`test_闭环形态快照态_串行闸放行且不需串行豁免`（断言输出不含「检测到串行豁免声明」）；
      闸查询：`test_发出后补写标注_无快照_闸仍锁且点破事后追认`）

## 6. 迁移与文档（待签认后开工，依赖 0.6）

- [x] 6.1 只回改 `质量部#7` 一行：散文标注 → 机器可读标注；撤「发送状态」列的人工两态并列留痕。（2026-09-12：主要事项列
      492 B → 569 B（追加约 77 B 标注，< 600 B）；发送状态列 1876 B／sha256:20a8429deff9037f → 545 B／sha256:9488fc921cbafd06，
      走 `工具-跟进信README登记.py set-status`＋`工具-共享文档编辑锁.py acquire/release`（README 改动落主 checkout 工作区，同 `#501` 先例）；
      改写前状态格全文追加进 `跟进信行日志/质量部#7.md`（只追加一节，既有原句一字未动）；
      「发出时快照」段为按起草时判定回改、非机器回填，已在格内留痕）
- [x] 6.2 README 起草规范增补「闭环形态标注」一条（写法 ＋ 何时写 ＋ 事后补写无效）。（README「表格列定义与发送状态枚举」节
      串行原则一句之后新增一条，经编辑锁 acquire/release）
- [x] 6.3 断言全表被改数据行**有且仅有 1 行**（配脚本核验，不靠人数）。（脚本按 `readme_table.iter_rows` 逐行比对 `git show HEAD:README`
      与工作区：主表 49→49 行、changed=['质量部#7']（列 3／5）、added=[]、removed=[]；补件表 3→3 行零改动）

## 7. 验收（待签认后开工）

- [x] 7.1 `wecom-aibot-service` 与 `zhuopin_platform` 全量回归，零漂移。（判据＝差集：分支失败集 ⊆ 纯 master 同命令失败集；实测数见 §一 `#561` 追段）
- [ ] 7.2 🔴 **首次真实生效**：一封**新起草**的信走完「起草 → 批准 → 投递 → 回填」全程，
      回填后标注仍读得出。**单测绿不算数**——同族反面教材＝队列 `#82` 告警建成 9 天、
      每天在跑、一次都没真响过。（🔴 留步：须真发一封信给真人，本泳道无头无人在场、对外发送一律不做；
      端到端用 fake connector 已走通＝`test_端到端_起草时有标注_回填后标注仍读得出且闸开`，**不算数**）
- [ ] 7.3 🔴 **首次真实开闸**：一次「前信为 `✅ 无需回复` ⇒ 起草下一封 `release` 直接放行」，
      且输出中**不含** `检测到串行豁免声明`。（🔴 留步：须一次真实起草；`质量部#7` 回改后本可如此，但它已不是陈忱最近一封
      （现为 `质量部#14`）；单测形态＝`test_闭环形态快照态_串行闸放行且不需串行豁免`，**不算数**）
- [x] 7.4 逃生阀监测：`串行豁免：` 在 **README「现有跟进信清单」表的数据行内**出现次数不升。
      🔴 取数位置写死在数据行，**不是全文 `grep`**（队列 §四 `#58` 已实测踩过同族陷阱：
      整文件 grep 得 10、按状态列取数得 1、真实 0）。（2026-09-12 按 `iter_rows` 主表数据行逐格计数：HEAD＝0、回改后＝0）
- [x] 7.5 本包**不部署、不发信、不触碰 `.51`**（本服务按 2026-08-13 拍板永不部署 `.51`）
      ⇒ 无部署段、无冒烟段，验收只能在开发环境 ＋ 真实 README 上做，「档 3」含义弱一档，如实登记。
      （2026-09-12 如实登记：本次验收＝单测 ＋ 对真实 README 的只读闸查询 ＋ `质量部#7` 真身回改；无生产冒烟）
