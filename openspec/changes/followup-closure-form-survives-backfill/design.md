# Design — 跟进信「闭环形态判定」在回填后仍然存在

> 🔴 **本文件是 design 审的对象，六个决策点全部「须签认」、全部无默认项。**
> 依据根 `CLAUDE.md` §5「默认项只在两个前提下成立」：提出本包的 session 是**无人在场的
> 无头批处理会话，收工即结束** ⇒ 第一问「他若不答，谁去执行」答不出执行者 ⇒ 不得设默认项。
> 另：决策点 1／2／4／5 属**判据/口径类**，按专员跟进纪律**永不默认生效**（IATF 显式签认红线）。
> 🟩 2026-09-12 `OP-0912-J`：六个决策点已按样板补齐五件（问题标题／选项后果与代价／推荐与无默认／手段／关系），决策点 2 补推荐项（仍无默认），并附 6 条「现取更正」（见「决策点」节首）。**仍待审、仍未 apply。**

---

## 前置：问题的形状不是队列行写的那个形状

队列 `#353` 行的建议是「回填只写入状态列中约定的那一段」。取证之后这条**做不成**——
不是难做，是前提不成立：**闭环形态判定从来就没能待在状态列里**。

一封信从起草到发出，状态格被**七处等值比较**钉成「只能是那一个字串」
（清单见 proposal「前置取证」表）。其中 `gates.assert_finalized` 是 design.md D8 记着
「Paul 拍板不得放宽」的**门禁②红线**。

⇒ 真实形状是：**判定无处安放，而不是判定被覆盖。**
`质量部#7` 那格「两态并列」是**发出之后**由人手工补的，那时机器已经抹过一次了。

同时取证也发现，**起草人早就把判定写在「主要事项」列了**，写成了自由散文
（原文见 proposal）。全表 54 行里只此 1 处（实测 `grep -c 闭环形态` ＝ 1）。

⇒ 本包的任务是**把那句已经在写的话，改写到机器认得的形态上**，不是新造一个位置。

---

## 决策点

> 🟩 **design 审已签认（Shao Peishen 2026-09-12 回 `P3：1a，2a，3a，4a，5c，6a`，经 Cowork 环境总线 `OP-0912-E` 转达；审读件＝`1-转型规划/0-全景路线图/审读件-openspec-P3-闭环形态穿越回填6点-2026-09-12.md`）**：**六点全部无默认，六点全部已获明确字母。**
> **决策点 1 ＝ (a)**：闭环形态标注放「主要事项」列内，与队列 `#241` 的 `目标文件：` 同构同列同手法；零门禁改动、零表结构改动。🔴 该列有 `README_TOPIC_CAP_BYTES = 600` 行长判据，`质量部#7` 该格现 492 B ⇒ **标注须压在约 100 B 内，依据长文进 `跟进信行日志/<编号>.md`**，否则 6.1 回改时自己撞判据。
> **决策点 2 ＝ (a)＋三条护栏**（🔴 本项 IATF 显式签认红线、永不默认生效——现已获明确签认，tasks 4.5 可以落字）：回填时若标注为 `✅ 无需回复` 则**直接写闭环态 `✅ 无需回复 <UTC>`，串行闸当场开**。**三条护栏与 (a) 同时生效，缺一即退回 (b)**：① 只认**批准那一刻已存在**的标注（靠决策点 3 的快照结构性成立）；② 取值只认 `followup_gate.CLOSED_STATUS_PREFIXES` 四态枚举，越界按无标注处理 ⇒ 仍写 `✅ 已推送`、闸仍锁（保守方向）；③ 回填落字时**保留 `✅ 已推送 <UTC>` 作为快照后段**，使「何时推送」不丢（与补件表 `resolve_backfill_status` 直接写 `✅ 无需回复` 不带时刻的差别，因为主表是串行闸的判据来源、补件表不是）。
> **决策点 3 ＝ (a)**：回填改保留式 ＋ 把标注**快照**进状态格，落字形态沿用 `followup_readme_bridge.build_reply_arrived_status` 已在生产上跑着的范式。顺带保住 `⏸ 暂缓` 的暂缓理由与起草人写在状态格里的临时备注。
> **决策点 4 ＝ (a)**：限定枚举 ＋ 强制依据，越界 **fail-loud**（报出来、按「无标注」处理、闸仍锁）。🔴 **刻意仍写成四态枚举而不是布尔开关**——写成布尔就等于在这里悄悄复制了一份口径，而 `followup_gate` 模块文档明令「判据只此一份」。
> **决策点 5 ＝ (c)＋必配缓解**：**不新增任何门禁**（one-in-one-out 账目不动），靠决策点 3 的快照结构性防止事后追认。🔴 **必配缓解不是可选项**：`工具-跟进闸查询.py` 在「主要事项标注 ≠ 状态格快照」时**必须明确报出「以快照为准」**（tasks 5.2）——不做这条，(c) 就是用一个静默失效换掉了一个静默滥用。
> **决策点 6 ＝ (a)**：只回改 `质量部#7` 一行，其余 48 行一字不动；**历史覆盖率永久 0%**，不表述为「逐步补齐」。该行「主要事项」列现已不含那句散文（已迁入行日志）⇒ 实际动作是新写一条约 70 B 的标注 ＋ 撤掉发送状态列的两态并列人工留痕；**行日志里的原句不动**。
> 🔑 **四点咬合关系已成立**：3(a) 是 2(a) 护栏①的前提、4(a) 是护栏②的前提、5(c) 以 3(a) 为前提——他六点全取推荐，咬合链完整，无一处悬空。
> ⚠️ **本包如实登记的三条边界不因签认而消失**：① 标注只对**新起草**的信生效，存量在途信走完原路径；② 闸判据扫描面不变——README 正文（串行原则段、「下一个可用号」段）**仍然是自由文本、仍然没有机器判据覆盖**；③ 本服务永不部署 `.51`，验收只能在开发环境 ＋ 真实 README 上做，**「档 3」的含义因此比别的包弱一档**。
> 🔴 **本段只记签认，不改任何判据正文；apply 由后续 CC 泳道领，须逐条对照本段。**

> 🟩 **五件补齐（2026-09-12，`OP-0912-J`，批 `B-0912_夜批四泳道`，派单件 `派单件-【CC】openspec三包17决策点-2026-09-12.md`）**：六个决策点按 `openspec/changes/lane-watch-heartbeat-wiring/design.md` 样板补齐「问题标题／(a)(b) 各写后果与代价／推荐与本项无默认／推荐理由附手段／与 Non-Goals·它包关系」五件。**六项仍全部无默认**（理由见文件头）；原有选项与论证一字未删，只补缺的件；决策点 2 原文「不代为选择」处**补一个推荐项**（根 `CLAUDE.md` §5 输出三条：选择题也必须给推荐项），推荐不等于默认，签认义务不变。下列「现取更正」是重跑手段发现的与 2026-08-25 原文不符的事实，原文保留、只附更正。
>
> **现取更正（2026-09-12，worktree `openspec-17-decisions`，HEAD＝master `cd0cbca`）**：
> 1. README 主表数据行从 54 变 **49**（`python 0-学习与工具/工具-跟进信README查询.py --digest --json` 计数；`followup-readme-phase2` 已落地、部分行迁入 `README-归档-202609.md`）；`✅ 无需回复` 形态从 3 行变 **4 行**（digest `status_digest` 前缀分组：`✅ 无需回复（202…` ×2、`（起草时…` ×1、`（知会性…` ×1）。
> 2. 🔴 **`质量部#7` 的「主要事项」列已不含那句闭环形态散文**：该格现 239 字／492 B，末尾写「详见 `跟进信行日志/质量部#7.md`」（`--row 质量部#7 --field 主要` 现取）；`闭环形态` 字样在该行只剩**发送状态列**的两态并列留痕（`--row 质量部#7 | awk` 定位到 `[5] 发送状态`）与行日志文件（`grep -c 闭环形态 跟进信行日志/质量部#7.md` → 1）。⇒ 决策点 1 (a) 的理由 3「它就是起草人已经在做的事」**出处变了**：起草人写过、但已被行长治理迁走——这反而坐实「散文会被搬家、机器读不到」。
> 3. 🔴 新约束：`工具-共享文档编辑锁.py` L4681 `README_TOPIC_CAP_BYTES = 600`——「主要事项」列对**本次触碰行**有 600 B 行长判据（L4705–4725 `_readme_row_length_warnings_and_violations`，超限走 `跟进信行日志/` 外置）。`质量部#7` 该格现 492 B ⇒ 决策点 1 (a) 的标注**必须控制在约 100 B 内**（形如 `→ 闭环形态：\`✅ 无需回复\`（依据：三要素明写不用回）` 约 70 B），否则 6.1 回改时自己撞判据。已写进决策点 1 (a) 代价。
> 4. 七处等值比较**仍是七处**、行号已漂：`readme_table.py:74–80 assert_draft_pending_review`／`gates.py:15–16 assert_finalized`／`dispatch.py:72`（候选筛选）／`dispatch.py:240`（`PAUSED_STATUS`）／`dispatch.py:250`／`scripts/dispatch_followup_letters.py:75`／`工具-共享文档编辑锁.py:3989`、`:4241`（`grep -rn "==\|!="` 现取）。决策点 1 (b) 的代价不变。
> 5. 覆盖点现状：`delivery.py:442 write_status(text, loc, new_status)`，`new_status` 由 **L43–66 `resolve_backfill_status()`** 算出（2026-08-25 原文未有此函数）；`approval.py:225` 改为 `updates: dict[int, str] = {loc.status_col_index: FINALIZED_STATUS_MARKER}` 仍整格覆盖状态列；`followup_readme_bridge.py:478` 第三处调用 `write_status`。
> 6. 🔑 **决策点 2 出现了一个已拍板的先例**：`resolve_backfill_status` docstring 明写「队列 #399 决策点 5 答 (b)：补件表『需回复＝否』（通知型）回填**直接置 `✅ 无需回复`**——发出即了结」，主表一律 `✅ 已推送`；出处＝`openspec/changes/followup-supplement-channel/design.md` 决策点 5 (b)「通知型发出即转 `✅ 无需回复`……把『需回复』做成补件表的一个显式列、通知型起草时即写预期终态」，`#399` 行状态「apply 已完成」（`工具-队列查询.py --row 399 --section 一` 现取）。⇒ **「机器在回填时写闭环态」Shao Peishen 已对补件表放行过一次，放行条件＝取值来自起草时就定下、批准时已冻结的结构化字段。** 这改变了决策点 2 的推荐（见该节）。

### 决策点 1 · 闭环形态标注放在哪一列？（**须签认，无默认**）

- **(a) 「主要事项」列内标注〔推荐〕**——形如 `→ 闭环形态：\`✅ 无需回复\`（依据：…）`，与队列 `#241` 的 `目标文件：\`xxx.md\`` **同构、同列、同手法**。
  **选它会发生什么**：零门禁改动、零表结构改动；`extract_target_filename`／`_TARGET_FILE_RE`（`readme_table.py:289`）的正则与回落逻辑可逐字复刻；无标注的 48 行与今天逐字同行为。
  **代价（如实登记）**：① 「当前状态」与「怎样算闭环」落在两列，人读要合看两格——缓解＝`工具-跟进闸查询.py` 把两者拼在同一行输出，决策点 3 快照后发出之后两者同现于状态格；② 🔴 **「主要事项」列有 600 B 行长判据**（2026-09-12 现取，节首更正 3），`质量部#7` 该格现 492 B，标注须压在约 100 B 内，依据文本不能写成一段——**依据长文进 `跟进信行日志/<编号>.md`，标注只留枚举值＋一句话**。
- **(b) 「发送状态」列首段化**——状态格改为 `<状态段>　━━━　<闭环形态段>`，七处等值比较一律改为「取首段后再等值」。
  **选它会发生什么**：状态与闭环形态同格，人读一格即得。
  **代价**：🔴 要动七处等值比较（2026-09-12 现取仍是七处，行号见节首更正 4），其中 `gates.assert_finalized` 是 design.md D8 明文「不得放宽」的**门禁②红线**——把等值改前缀，无论技术上多么等价，都需要 Shao Peishen 亲自签认放宽门禁②；且 `approval.py:225` 的整格覆盖也必须一并改（proposal「已知未闭合 3」）。**选 (b) 则本包 spec 整段作废须重写。**
- **(c) 新增一列「闭环形态」**——表格 6 列变 7 列。
  **选它会发生什么**：结构化最彻底，机器零歧义（补件表的「需回复」列就是这条路，见节首更正 6）。
  **代价**：要改 `_validate_release_structure` 的列数校验、`_followup_readme_rows`／`readme_table.iter_rows` 两处解析，并**迁移 49 行 ＋ `README-归档-202609.md` 归档件同构表**（归档件「章节标题/表头与主表一致，按同一套解析逻辑读取」——`工具-跟进信README查询.py --help` 原文）⇒ 改一处表头要连带改归档件，违反「历史记录不追改」。

**推荐 (a)。🔴 本项无默认**——提出本包的 session 无人在场、收工即结束，第一问「他不答谁去执行」答不出执行者；且本项决定串行闸判据的载体位置，属口径类。
**推荐理由的手段**：① 2026-09-12 `grep -rn "def write_status\|def build_target_file_annotation\|_TARGET_FILE_RE =\|def assert_draft_pending_review" --include=*.py .` 现取四个锚点行号（`readme_table.py:203/329/289/74`）；② `grep -rn "FINALIZED_STATUS_MARKER\|FOLLOWUP_FINALIZED_STATUS\|PAUSED_STATUS\|assert_finalized" … | grep "==\|!="` 现取七处；③ `python 0-学习与工具/工具-跟进信README查询.py --row 质量部#7 --field 主要` 读出该格 239 字／492 B 与「详见行日志」句；④ `sed -n 4683,4725p 工具-共享文档编辑锁.py` 读到 `README_TOPIC_CAP_BYTES = 600` 与判据函数；⑤ `#241` 先例 docstring＝`readme_table.py:329 build_target_file_annotation`（2026-08-25 读，2026-09-12 行号复核）。
**与 Non-Goals／它包关系**：对应「不做的事」前三条（不改门禁①②、不改 `approval.py` 两态语义、不动列结构）；与 `followup-readme-phase2`（归档件同构表）的关系＝(a) 对归档件零影响、(c) 会牵连它；与 `followup-supplement-channel`（补件表「需回复」显式列）的关系＝同一需求在补件表走了 (c) 路线，**因为补件表是新表、无历史行与归档件可迁**，主表不具备这个条件。

---

### 决策点 2 · 回填时要不要按闭环形态直接写闭环态（＝让机器开串行闸）？（**须签认，无默认；判据/口径类，永不默认生效**）

即：若标注为 `✅ 无需回复`，`delivery.py` 回填时应写成——

- **(a) 直接写闭环态 `✅ 无需回复 <UTC>`：发出即闭环，串行闸当场开〔推荐，附三条护栏〕**
  **选它会发生什么**：`质量部#7 → #8` 那次不再需要 `串行豁免：`，本包价值一步兑现；与补件表已落地的行为（`resolve_backfill_status` L64–65：需回复＝否 ⇒ `NO_REPLY_NEEDED_STATUS`）**同一语义、同一函数内分支**。
  **代价**：🔴 **判错 ⇒ 闸被错误打开 ⇒ 同一收信人手上两封在途信**——串行原则是 Shao Peishen 2026-08-03 定的、明令优先于一切触发点规则的那一条。**护栏（须与 (a) 一并签认、缺一即退回 (b)）**：① 只认**批准那一刻已存在**的标注（决策点 5 (c) 的快照使其结构性成立——回填读的是批准时冻结的格，不是回填时的格）；② 取值只认 `followup_gate.CLOSED_STATUS_PREFIXES` 枚举（决策点 4 (a)），越界按无标注处理 ⇒ 仍写 `✅ 已推送`、闸仍锁（保守方向）；③ 回填落字时**保留 `✅ 已推送 <UTC>` 作为快照后段**（决策点 3），使「何时推送」这个事实不丢——与 `resolve_backfill_status` 对补件表**直接写 `✅ 无需回复` 不带时刻**相比，主表多留一段，因为主表是串行闸的判据来源、补件表不是。
- **(b) 仍写 `✅ 已推送 <UTC>`，闭环形态只作为标注被保留；闸判据另行读它才放行**
  **选它会发生什么**：机器永不改闸的状态字面；闸判据改成「读状态格 ＋ 读快照」两段合判。
  **代价**：判对 ⇒ 闸该开没开 ⇒ 起草人仍写一条 `串行豁免：` 绕过去——**「拿逃生阀去绕它本来要拦的那件事」是本项目已明记的坏味道**（`followup_gate.py` 模块文档 L14「两者不一致的代价是实测过的：质量部#7……」）；且闸判据要同时认两种形态，**判据分叉**——`followup_gate` 模块文档明令「判据只此一份」。

**两个方向的代价不对称，且 2026-08-25 出件时不对称的方向不明显——2026-09-12 现取的先例把方向指出来了。推荐 (a)＋三条护栏。🔴 本项无默认、永不默认生效**（IATF 显式签认红线；串行闸是人设的闸，开闸规则只能由 Shao Peishen 签）。**推荐不是默认：未获明确答复前 tasks 4.5 MUST 留空。**
**推荐理由的手段**：① 2026-09-12 `grep -n "def resolve_backfill_status" -A 40 delivery.py` 读到 L43–66 全文与 docstring「队列 #399 决策点 5 答 (b)」；② `grep -n "决策点 5\|需回复" openspec/changes/followup-supplement-channel/design.md` 读到 L124–131 原文「通知型发出即转 `✅ 无需回复`……把『需回复』做成显式列……起草时即写预期终态」；③ `python 0-学习与工具/工具-队列查询.py --row 399 --section 一` → `[S:blocked]` 状态列首句「apply 已完成、单测已本地实跑取证；仅剩 6.4 端到端真实发送留步」；④ `sed -n 52,57p followup_gate.py` 现取 `CLOSED_STATUS_PREFIXES` 四值；⑤ 不对称论证原文＝本节 2026-08-25 版（保留于上两条选项的代价行）。
**与 Non-Goals／它包关系**：与 `followup-supplement-channel` 决策点 5 (b) **同一口径的主表延伸**——该包放行的条件（起草时定、批准时冻、结构化取值）正是本节三条护栏；与 `followup-serial-gate-hardening`／`followup-letter-state-single-source` 两包共享 `followup_gate` 判据权威源，本包不在消费者侧另写口径（tasks 2.1）。

---

### 决策点 3 · 回填要不要从「整格覆盖」改成「保留式」并快照闭环形态？（**须签认，无默认**）

- **(a) 改保留式 ＋ 回填时把「主要事项」列标注**快照**进状态格〔推荐〕**
  落字形态沿用 S4 桥一**已在生产上跑着**的范式（`followup_readme_bridge.build_reply_arrived_status`，L134–150）：
  ```
  ✅ 已推送 <UTC>　━━━　闭环形态（发出时快照）━━━　✅ 无需回复（依据：…）
  ```
  其 docstring 逐字适用：「旧状态不删，是因为……**覆盖式写入会把它弄丢，而这一格没有任何别处的副本**。」
  **选它会发生什么**：① 同一个 `write_status` 抹掉的另两样东西顺带保住——`⏸ 暂缓` 的暂缓理由（`#294` 修法⑴ 新增的第三态，理由须人手写在格里）与起草人写在状态格里的任何临时备注（proposal「已知未闭合 2」）；② 🔴 **决策点 5 变成结构性的**：回填那一刻机器把标注复制进状态格，此后闸判据只认快照、不回读「主要事项」⇒ 发出后再补写标注对闸零效果 ⇒ 「起草时判定、非事后追认」不需要任何门禁去守。
  **代价**：状态格变长（现 `质量部#7` 该格 865 字／1876 B，`ROW_LENGTH_CAP_BYTES = 4096` 判据下余量充足，但每多一段就少一段余量）；`resolve_backfill_status` 要从「返回一个前缀字串」扩成「拼接前缀＋快照」，补件表分支**不得被连带改动**（它的 `✅ 无需回复` 不带后段，是 `#399` 已拍板的形态）。
- **(b) 不改，维持整格覆盖；只做决策点 1 的标注**
  **选它会发生什么**：改动面最小，只碰 `readme_table` 与 `followup_gate`。
  **代价**：🔴 本包只治了闭环形态一种，**`⏸ 暂缓` 的理由仍会被静默抹掉**；决策点 5 失去 (c) 选项，防「事后追认」只能回去加门禁（(a) 阻断或 (b) 告警，两者都已被实证为坏路，见决策点 5）。

**推荐 (a)。🔴 本项无默认**——它改变 `delivery.py` 回填的对外语义（§5 门槛③），且是决策点 5 (c) 的前提，须与决策点 5 同批签。
**推荐理由的手段**：① 2026-09-12 `sed -n 134,160p followup_readme_bridge.py` 读到范式与 docstring 原文；② `sed -n 430,445p delivery.py` 读到回填四步注释与 `write_status` 调用；③ `grep -n "ROW_LENGTH_CAP_BYTES\s*=" 工具-共享文档编辑锁.py` → L4597 `4096`；④ `--row 质量部#7 --field 发送状态` 读出该格 865 字／1876 B；⑤ 已核实不与既有机制冲突＝2026-08-25 tasks 1.7（七处等值全在 `⏳ 待你审`／`🆕 待发`／`⏸ 暂缓` 三态；`followup_gate.classify_status`／`normalize_status` 走前缀＋归一化——2026-09-12 `grep -n` 复核仍在 L131／L175）。
**与 Non-Goals／它包关系**：与 `followup-readme-phase2`（S4 桥一桥二）**复用其范式、不改其函数**；与 `followup-supplement-channel` 的关系＝补件表分支保持原形态不动；对应「不做的事」「不改 `approval.py` 的两态语义」——批准那一步仍整格覆盖，因为标注不在状态格里。

---

### 决策点 4 · 标注的合法取值——限定枚举还是自由文本？（**须签认，无默认；判据/口径类**）

- **(a) 限定枚举 ＋ 强制依据〔推荐〕**：取值 MUST 是 `followup_gate.CLOSED_STATUS_PREFIXES` 四者之一（`📥 已回件并回灌`／`✅ 无需回复`／`📨 已确认闭环`／`❌ 已作废`，2026-09-12 现取 L52–57），且 MUST 附一句依据；越界 ⇒ **fail-loud**（报出来，按「无标注」处理，闸仍锁）。
  **选它会发生什么**：机器零歧义；判据从权威源取、消费者侧不另写一份（模块文档明令）。
  **代价（已知边界）**：实测语料里唯一真实用过的是 `✅ 无需回复`（另三态都是事后转态，起草时不可能成立）⇒ 枚举**实际退化为一个取值**。**刻意仍写成四态枚举而不是布尔开关**——写成布尔就等于在这里悄悄复制了一份口径。
- **(b) 自由文本**：怎么写都行，闸判据靠模糊识别。
  **选它会发生什么**：起草人零学习成本。
  **代价**：🔴 本项目已为 (b) 付过学费——README 顶部「下一个可用号」段落**连续失真四次**，诊断原文「本段是自由文本、没有任何机器判据覆盖它」，2026-08-21 校准再补一刀「失真的不是一处而是两处，且两处会互相印证着一起错」。一个机器要读的位置上放自由文本，结局已验证过四遍。**且决策点 2 (a) 的护栏②完全依赖枚举——选 (b) 则决策点 2 只能退回 (b)。**

**推荐 (a)。🔴 本项无默认**——取值集合＝口径判据类，永不默认生效。
**推荐理由的手段**：① 2026-09-12 `sed -n 52,57p 5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/followup_gate.py` 现取四值；② `grep -n "CLOSED_STATUS_PREFIXES" followup_gate.py` L144 `is_closed_status` 与 L394 release 提示均从同一元组取值（判据只此一份的实证）；③ 「下一个可用号」失真四次＝README 顶部该段历史与 2026-08-21 校准记录（2026-08-25 读）；④ 语料标定＝`--digest --json` 分组：`✅ 无需回复` 4 行、`📥 已回件并回灌` 24 行、`📨 已确认闭环` 1 行、`❌ 已作废` 0 行（2026-09-12 现取）——起草时能判定的仍只有 `✅ 无需回复` 一种。
**与 Non-Goals／它包关系**：与 `followup-letter-state-single-source`（状态词汇单一来源）**同源**——本包不新造词汇，只复用其四态；与决策点 2 (a) 护栏②互为前提。

---

### 决策点 5 · 防「事后追认」的判据挂在哪？（**须签认，无默认**）

`质量部#7` 行内自己写着约束：「符合防滥用约束——**起草时判定、非事后追认**」（2026-09-12 `--row 质量部#7 --field 发送状态` 现取，该句仍在）。**今天没有任何机器在执行这句话。**

- **(a) 编辑锁 `release` 阻断**：比对 acquire 快照，若既有行新增了标注而其状态已过 `⏳ 待你审`，拒绝 release。
  **选它会发生什么**：事后追认写不进去。
  **代价**：🔴 「再加一道网」——本包按 one-in-one-out 已声明不新增拒绝写入的门禁；更要紧的是协议〇.9 ⑵ 的教训「一条阻断规则可能把自己的解法一起锁死」（「连来关行降 WIP 的那个 session 也会被挡在门外」）——这里的等价风险：一个来**修正**误写标注的 session 会被同一条规则挡住。
- **(b) 只告警不阻断。**
  **选它会发生什么**：零阻断。
  **代价**：已被实证为无效——协议〇.9 ⑵ 记着 2026-08-10→08-16 观察周「6 次每次都响了、每次都被越过，一个非阻断提示在连续 6 次被无视后信息量已经是零」。
- **(c) 结构性防止〔推荐〕**：**不新增任何门禁**，靠决策点 3 的快照——发出后再补写的标注对闸零效果，因为闸只读快照。
  **选它会发生什么**：数据流本身保证约束，零新增门禁、零新增告警。
  **代价（已知边界）**：(c) 只让事后追认**对闸无效**，**不阻止它被写下** ⇒ README 上仍可能出现一条「看起来像判定、实际不起作用」的标注，读的人会被误导。**必配缓解**＝`工具-跟进闸查询.py` 在「主要事项标注 ≠ 状态格快照」时**明确报出**「以快照为准」——**不做这条缓解，(c) 就是用一个静默失效换掉了一个静默滥用**（tasks 5.2）。

**推荐 (c)＋必配缓解。🔴 本项无默认**——它与决策点 3 互为前提，且决定是否新增一道写入门禁（one-in-one-out 账目）。
**推荐理由的手段**：① 协议〇.9 ⑵ 原文＝`跨桌任务队列-机制环境.md` §〇（2026-08-25 读；2026-09-12 未重读——读侧门禁下队列真身只能经 `工具-队列查询.py`，协议段不在其 `--row` 范围，**此条手段停留在 2026-08-25**，如实登记）；② `ls 0-学习与工具/工具-跟进闸查询.py` 现取存在，缓解有落点；③ `--row 质量部#7 --field 发送状态` 现取「起草时判定、非事后追认」原句仍在。
**与 Non-Goals／它包关系**：对应 proposal「本次退休哪一个既有守卫」节「刻意不新增任何一道会拒绝写入的门禁」；与 `editlock-write-guard-hardening`／`editlock-hold-scope-and-wip-block` 等编辑锁包的关系＝(c) **不碰编辑锁一行**，(a) 才会与它们抢同一处 release 校验链。

---

### 决策点 6 · 历史行的迁移范围——只改 `质量部#7` 一行，还是全表反推？（**须签认，无默认**）

- **(a) 只回改 `质量部#7` 一行〔推荐〕**：它是唯一写过闭环形态判定的行；把散文归一化为标注、撤掉两态并列的人工留痕。**其余 48 行一字不动。**
  **选它会发生什么**：历史覆盖率**永久 0%**（proposal 价值指标已写死，不表述为「逐步补齐」）。
  **代价**：🟩 2026-09-12 现取更正——`质量部#7` 的「主要事项」列已**不含**那句散文（迁入 `跟进信行日志/质量部#7.md`，该格余量 108 B），6.1 的「散文 → 标注」实际是**新写一条约 70 B 的标注**＋撤发送状态列两态并列留痕，不是改写既有句子；行日志里的原句**不动**（历史记录不追改）。
- **(b) 全表按现有状态格反推补标注**
  **选它会发生什么**：覆盖率账面 100%。
  **代价**：🔴 **考古式反推**——48 行里绝大多数当年根本没做过闭环形态判定，补出来的标注是我们今天替他们编的，**它会长得和真判定一模一样**（同族＝「一条『更正』把一条本来正确的记录改错了」，OP-0819-F 教训⑶）；且 `README-归档-202609.md` 里的已归档行按同一逻辑也得补，等于追改归档件。

**推荐 (a)。🔴 本项无默认**——迁移范围决定哪些历史行被触碰，属「历史记录不追改」判别的边界案例，须他裁。
**推荐理由的手段**：① 2026-09-12 `--digest --json` 现取主表 49 行；② `--row 质量部#7 --field 主要` 现取该格 239 字／492 B、末句「详见 `跟进信行日志/质量部#7.md`」；③ `grep -c 闭环形态 "6-人才与组织/部门AI专员跟进/跟进信行日志/质量部#7.md"` → 1；④ `ls 6-人才与组织/部门AI专员跟进/ | grep README-归档` → `README-归档-202609.md` 存在；⑤ 「历史记录不追改」判别＝根 `CLAUDE.md` §1。
**与 Non-Goals／它包关系**：对应「不做的事」「不追改历史行」；与 `followup-readme-phase2`（归档件）的关系＝(a) 不触碰归档件、(b) 必然触碰；tasks 6.3「被改数据行有且仅有 1 行」配脚本核验的取数位置＝主表数据行（同 7.4 的「不是全文 grep」纪律）。

---

## 已知边界（三条，与决策点无关，落地后仍然存在）

1. **标注只对「新起草」的信生效**。存量在途信（起草时无标注）走完原路径，与今天逐字同行为。
   这是刻意的兼容方向，写进 spec 的 MUST。
2. **闸判据的扫描面不变**。本包不扩大 `followup_gate` 的覆盖面——README 正文（串行原则段、
   「下一个可用号」段）**仍然是自由文本、仍然没有机器判据覆盖**。
   这是队列 `#352` 与 README lint 那条线的范围，不是本包的。
3. **本服务永不部署 `.51`**（Shao Peishen 2026-08-13 拍板）⇒ 本包无部署段、无冒烟段，
   验收只能在开发环境 ＋ 真实 README 上做。**「档 3」的含义因此比别的包弱一档**，如实登记。

---

## 不做的事（明确划界）

- **不改门禁①、门禁②**（决策点 1 选 (a) 时）。
- **不改 `approval.py` 的两态语义**——批准那一步仍是整格覆盖，因为标注不在状态格里。
  （决策点 1 若改选 (b)，此项必须一并改，见 proposal「已知未闭合 3」。）
- **不动 README 表格的列结构**，不新增列。
- **不追改 53 行历史行**。
- **不发任何跟进信、不起草任何跟进信**——本包只改机制，不触发第 8 步。
