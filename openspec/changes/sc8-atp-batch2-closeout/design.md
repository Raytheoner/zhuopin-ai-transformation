# sc8-atp-batch2-closeout Design（✅ 2026-09-03 已由 Shao Peishen 放行）

> ✅ **状态更新（`OP-0903-B1`，2026-09-03）：design 审已放行，tasks `0.1` 已勾。**
> 下面这句原写「本文件是草案…未通过前不得 `/opsx:apply`」，**该前提已不成立**，按现状改写；
> 🔴 **正文一字不追改**（历史记录不追改），只改这一处状态陈述。
> 🔴 **放行 ≠ 开工**：他同一句里写明「**实现属另立行、本轮不做**」，且 `1.5.4`／`3.1` 两项姚祖怡签认仍是硬前置
> （串行闸 🔒 锁，`采购部#20` 在途）⇒ **不得据此翻开关、不得部署、不得触碰 `.51`**。
> 📌 两份 spec delta 已于同日走 `/opsx:sync` 合入 `openspec/specs/`，**本包维持在办、不归档**（见 `tasks.md` §4）。
> 全部结论均由 2026-09-02 的只读实测得出（探针自行逐字实现三条规则、不复用被测代码以免自证）；
> 凡引用生产数字均标注取数日期与来源，未标注者一律是本机离线算术。
>
> ✅ **2026-09-02 更新（`OP-0902-B3`，泳道 B-0902_60）**：**D3 已由 Shao Peishen 拍板 ＝ (a) 逐字化**，
> 本文件据此从「D3 待定」改写为「D3 已定、三条缓释各有落点」。**design 审（tasks 0.1）本身仍未过，本次不改其状态。**
>
> ✅ **2026-09-03 更新（`OP-0903-A1`，泳道 `sc8-kitdate-3`，批 `B-0903_10`）—— 重审三项收口**：
> B3 的落字实质改写了 design，因而新开的 **D3-bis／D4／D5** 三项已由 **Shao Peishen 2026-09-03 全部裁决为 (a)**，
> 本次即按该裁决逐条补齐。三项落点：
>
> | 项 | 裁决 | 本次落点 |
> |---|---|---|
> | **D3-bis** | (a) 采纳「不重判」；🔴 **但 24→11 的更正须写进 design 与对照表**，并在下一封致姚祖怡的信中如实说明「我方原估 24、实测 11」 | 本文件 **D3**（三处 24 行表述就地更正）＋ **D3-bis**（结案）＋ 对照表 §〇-bis ＋ tasks 3.1c |
> | **D4** | (a) 补一条边界口径进判例包，**随下一封请姚祖怡签认**（不是记录风险接受） | 本文件 **D4** ＋ 判例包素材 附录 A ＋ tasks 3.1 |
> | **D5** | (a) 补一条验证用例覆盖不变式③（对照表上空过、未获验证），**补完再放行** | 本文件 **D5** ＋ spec「退化守卫」新增 Scenario ＋ tasks **1.2.13** ＋ 对照表 §三-bis（用例参数已算定） |
>
> 🔴 **本次仍不进实现、不部署、不触碰 `.51`、不起草或发送任何信**（姚祖怡串行闸 🔒 锁，`采购部#20` 在途）。
> 🔴 **design 审（tasks 0.1）本次仍不自行改状态** —— 裁决说的是「补完再放行」，补齐动作在本泳道，放行仍归 🟡 档。

## D0 · 🔴 三条缓释的落点索引（Shao Peishen 2026-09-02 随 D3 裁决一并要求）

他接受 (a) 的代价时附了三条缓释，**逐条须可指到具体落点，不得只写「已注意」**：

| # | 缓释要求（他的原话） | 落点 | 状态 |
|---|---|---|---|
| ① | 反例单测锁死逐字化前后的差异（**关掉修复即失败的退化守卫**，不是只测 happy path） | `tasks.md` §1.2.9~1.2.12 ＋ `specs/delivery-date-forecast/spec.md` 的「退化守卫」Requirement | 已写进 tasks 与 spec（本泳道**不进实现**，故未勾） |
| ② | 出一份「改判前后 24 行逐行对照表」落档供事后追溯 | `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_118_规则2逐字化改判前后24行对照-2026-09-02.md` | ✅ **已出件**（B3 产出）；🔴 结论与裁决书表述有出入 ⇒ **D3-bis 已于 2026-09-03 由 Shao Peishen 裁决闭合（不重判，但须更正数字并在下一封信中说明）** |
| ③ | design 中逐字引用那三条规则原文并标注来源与日期 | 本文件 **D1** | ✅ 已在（本次补标为缓释③的落点，原文一字未改） |

## D1 · 判据正本：姚祖怡 2026-08-18 三条规则逐字引用（＝**缓释③的落点**）

🔴 **§四 #65 答 (a) 的代价是「证据是企微文本而非签认件」，唯一的缓解手段就是把原文逐字钉在这里。**
以下三条逐字转录自 `采购部#14` 回件（姚祖怡，采购部 AI 专员，**男**；日期 2026-08-18；
转录载体＝队列 §一 `#118` 2026-08-18 拆件巡逻回灌段，标注为「逐字转录」）：

> **规则 1（出货日不在三个月内）** —— 无答交按照出货日往前推算 3 个月的 20 日起、再往后推算 90 天，预估齐套日期；
> **规则 2（出货日在三个月内）** —— 无答交按照此时此刻起往后推算 90 天，计算齐套日期；
> **规则 3（同一项目既有答交又有无答交）** —— 对比无答交的预估齐套日期与有答交的答交日期，**更晚**的那一个是齐套日期。

补充签认两条，同样逐字标注来源：

- **「20 日 ＝ 那个自然月的 20 号」** —— 姚祖怡 2026-08-18 于 `采购部#15` 回件确认（见 `#401` 行）。
- **规则 1「开起来」** —— 姚祖怡 2026-08-28 于 `采购部#19` 第 6 件勾选，原话「**☒ 认可 —— 按规则 1 改后的算法开起来。**」（🔴 该勾在段落里、不在表格里，Cowork 初版提取脚本曾整段漏掉，08-28 已更正）。

⚠️ **仍未被任何签认覆盖的一处**：三条规则**没有一条说过「三个月内」的边界当天算哪一边**。见 D4。

## D2 · 🔴 三条规则 vs 现有实现：逐条实测对位（opener 指定用实测、不用推断）

**取证件（tracked，可复跑）**：`4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_118_三条规则实测对位-2026-09-02.md`
——含方法、全部参数、逐条结果与复跑路径。🔴 刻意落 tracked 的 `docs/`、不落 gitignored `reports/`（`#267` 的教训是审计件随 worktree 删除而永久丢失）。
**探针**：一次性网格探针，只读、零网络、不改任何代码，落仓库外并已随收工删除；其方法与参数已全部写进上述取证件，可据以重建。
**参数**：`today = 2026-09-02`（`Get-Date -Format 'yyyy-MM-dd'` 现取）；`NO_FEEDBACK_LEAD_DAYS = 90`；
`rule1_horizon_months=3 / rule1_months_back=3 / rule1_start_day=20`；三个月边界（含边界）＝ `2026-12-02`。
**方法**：出货日网格 `today−60 … today+400` 逐日，把探针自行实现的逐字规则与 `forecast.no_feedback_start_date()` 逐点比对。

| 规则 | 现有实现落点 | 实测结果 | 结论 |
|---|---|---|---|
| **规则 1** | `forecast.no_feedback_start_date()` 规则 1 支 | 网格内适用出货日 **309 个，不一致 0 个**。抽样：出货 2026-12-05 ⇒ 起算 2026-09-20（＝逐字）；2027-02-20 ⇒ 2026-11-20；2027-04-20 ⇒ 2027-01-20 | ✅ **完全覆盖** |
| **规则 2** | 同函数的 `max(出货日, 今天)` 支 | 已过期支 61 个网格日、**不一致 0 个**；**未来但在窗口内支 91 个网格日、不一致 91 个**（实现恒晚 1~91 天，差值＝出货日−今天） | 🔴 **仅部分覆盖** —— 唯一仍与其书面判据不符的一支 |
| **规则 3** | `baoguan.py:800` / `forecast.py:350` 的 `kit_date = max(mat.arrivals.values())` | 有答交更晚 / 无答交更晚两个方向各测一次，**均与逐字规则一致** | ✅ **结构性覆盖**（不是碰巧对：有答交件与无答交件放进同一个 `arrivals` 字典后取 `max`，取更晚是这个数据结构本身的性质） |

**规则 1 的状态比 opener 假设的更靠前，必须先说清**：opener 与 §四 `#65` 都写「规则 1 是全新分支、此前任何载体均无」——那是 **2026-08-18 的快照**。实际上 §一 `#401` 已于 **2026-08-31（`OP-0831-L`）销号**：姚祖怡 08-28 单独签认 ＋ `.51` 部署并翻 `SC8_KIT_DATE_RULE1=on` ＋ `POST /api/refresh` ＋ 核数（四色 `🔴68/🟠27/🟡7/🟢1`，103 行）。⇒ **规则 1 不在本包范围内，本包一字不动。**

### 🔴 D2-bis · opener 点名要求「显式写清怎么解」的方向冲突：实测其前提已不成立

opener 与 §四 `#65` 记述的冲突是：

> 「`#344` 记的现状是『齐料日期只取最早答交日期』，而规则 3 要求对远期订单取**更晚**的日期 —— 两者落在同一取值函数上、方向相反。」

**这个冲突不需要调和，因为它的前提在 2026-08-24 就被消除了，而 §四 `#65` 是 08-18 写的。**

`#344`（`sc8-kit-date-qty-cumulative`）已上线「按答交数量累计到覆盖需求为止」的取值口径，
`_cumulative_confirmed_batches` 并由 `baoguan.py` **下沉到 `forecast.py`**，齐料日／BOM 缺口清单／物料看板三处共用同一个函数对象。

**实测（不是读代码推断）**：构造逐笔答交 `[(2026-10-20, 0.0), (2027-05-20, 10000.0)]`、需求 8000 —

- 「只取最早答交日期」会得到 **2026-10-20**；
- **现行实现返回 2027-05-20**（累计纳入 2 笔）。

⇒ **「只取最早答交日期」在当前代码里已不成立。** 现行在两处都在取更晚：

1. `_arrival_by_cumulative_qty` 中「答了但累计仍不够」走 `max(启发式估算日, 最晚一笔正数答交日)`——`#344` design D3 的原文即写明「取更晚与他自己写的规则 3 同向」；
2. 齐料日聚合 `max(全部子件到货日)`——即规则 3 本身。

**⇒ 本包对该冲突的处置：判定为「已消解」，不在本包内做任何调和动作；同时把上面这段实测写进本文件，作为「查过了、不是漏了」的留痕。**

🔑 **这一条留下的通用判据（比结论本身更值得记）**：**§四 的定夺行记录的是提出那一刻的现状，不是当前现状。** 本次若照 opener 的字面去「调和冲突」，会在一个已经不存在的问题上改一遍取值函数——而那个函数正是 `#344` 花了一整轮判例签认才定下来的。⇒ **凡定夺行里的「现状是……」，动手前必须现测一次。**

## D3 · ✅ 已拍板 (a) 逐字化 —— 规则 2 逐字化会让结论**更乐观**，方向与前两轮相反

> ✅ **Shao Peishen 2026-09-02 裁决：D3 ＝ (a) 逐字化。**
> 原话：「严格按姚祖怡（男）2026-08-18 书面文本实现规则 2。」
> 🔴 **他已明示知悉并接受代价**：~~24 行~~ 齐料日提前 18~79 天、结论整体变乐观，**方向与 `#344`／`#401` 两轮的「变保守」相反**。
> 附加三条缓释（落点见 **D0**），**缓释是裁决的一部分，不是建议**。
>
> 🔴 **2026-09-03 更正（Shao Peishen 裁决 D3-bis ＝ (a) 采纳「不重判」，同时命更正数字）**：
> 裁决书里这句「24 行」是**下达时点的估算**；缓释② 的对照表**逐行核完后实测为 11 行**（另 13 行不动）。
> **更正来源 ＝ 对 08-24 生产载荷底稿的逐行核对（24 行全列、无省略、无抽样），不是重新估算**，见 D3-bis 与
> `docs/queue_118_规则2逐字化改判前后24行对照-2026-09-02.md` §二。
> ⇒ 本文件此后凡出现「24 行」，一律指**适用面 24 行**（窗口内未来支的全部行数），
> **不再指「会前移的行数」——那是 11 行**。

**以下为拍板前的权衡论证，原样保留供追溯。**

`#344` 与 `#401` 两轮改动的共同论证是「改完更保守、不会低估，所以风险可控」。**P1 反过来**：

| | 现行 | 规则 2 逐字 |
|---|---|---|
| 起算点 | `max(出货日, 今天)` | 今天 |
| 无答交件估算到货日 | 出货日 + 90 | 今天 + 90 |
| 相对关系 | **更晚、更保守** | **更早、更乐观** |

**实测影响面（算术，假定该行瓶颈件为无答交件；`today=2026-09-02`）**：

- 窗口内未来出货日共 91 个：现行缺口恒 `+90` 天 ⇒ 四色 `🔴91`；逐字化后 ⇒ `🔴86 / 🟡3 / 🟢2`。
- **但真实语料的出货日集中在每月 20 日**，本窗口内只有三个：

| 出货日 | 齐料日 现行 → 逐字 | 提前 | 缺口天数 | 四色 |
|---|---|---:|---|---|
| 2026-09-20 | 2026-12-19 → **2026-12-01** | 18 天 | +90 → +72 | 🔴 → 🔴 |
| 2026-10-20 | 2027-01-18 → **2026-12-01** | 48 天 | +90 → +42 | 🔴 → 🔴 |
| 2026-11-20 | 2027-02-18 → **2026-12-01** | 79 天 | +90 → +11 | 🔴 → 🔴 |

⇒ **结论：齐料日会明显前移（18~79 天），而四色按当前语料一格不动。**
（🔴 上表三行是**按出货日分组的上界**，不是「三行会动」；逐行核后各组内真正会动的行数见 **D3-bis**。）

⚠️ **这个结论有三条边界，不得当成定论端给专员**：
1. 它是**算术**，不是生产实测 —— 本机 `.51` 实测不可达（`Test-NetConnection 192.168.1.51:8091` ⇒ `TcpTestSucceeded=False`／`PingSucceeded=False`），真实对照表属 LAN 留步。
2. 它**假定该行瓶颈件是无答交件**。若某行瓶颈是已答交件，规则 3 的 `max` 会让该行完全不动 ⇒ **真实影响面 ≤ 上表**。
3. 出货日「集中在每月 20 日」取自 2026-08-26 那份对照表的观察，**下个月的语料未必如此**（`#150` 已实证预测订单会月度整批滚动、单号全变）。

**适用行数**：08-26 生产基线实测 **24 行**（分支适用面：规则 1 43 行／规则 2 未来 24 行／规则 2 已过期 38 行，合计 105）。
🔴 **「适用 24 行」≠「会前移 24 行」** —— 逐行核后**会前移的是其中 11 行**，另 13 行的瓶颈件是已答交件、规则 3 的 `max` 让其一天不动（见 D3-bis）。

### 定夺项 D3 —— ✅ **已闭合：Shao Peishen 2026-09-02 答 (a)**

**规则 2 到底改不改？**（三选项原样保留，供审核时看清他是在什么之间选的）

- **✅ (a) 逐字化（本包 P1 所设计的方案 · 已采纳）** —— 按 §四 `#65` 答 (a) 的逻辑一以贯之：他的书面文本即签认，就该按他写的执行。代价 ＝ ~~24 行~~（**实测 11 行**，见 D3-bis）的齐料日提前 18~79 天，**结论变乐观**；专员打开看板会看到数字明显变化。收益 ＝ 我方口径与其书面判据完全一致，不再有「他说的和我们做的不一样」这一悬置。
- **(b) 维持现行保守读法，并把「我方刻意比你写的更保守」这件事明确告诉他** —— 代价 ＝ 存在一处**我方明知与其签认判据不符却不改**的口径，IATF 审核问起来比 (a) 更难解释；且 `#401` ⑺ 当初不改的理由是**排期**（一次上线一个自变量），不是**方向**，用排期理由长期不改是偷换。收益 ＝ 不引入乐观化。
- **(c) 逐字化但同时提高无答交件的兜底天数** —— 用参数抵消方向变化。🔴 **不推荐**：这会把一个已签认的判据和一个未签认的参数调整绑在一起，正是本项目反复吃亏的「一次上线两个自变量」。

**当时的建议亦为 (a)**，理由是：§四 `#65` 答 (a) 已经确立「他写下的即是签认」，那么**只挑其中让结论更保守的两条执行、把让结论更乐观的那一条压着不动，是我方在替他做业务判断**。真正的风险控制手段不是替他保守，而是 P1 的开关形态 ＋ 对照表 ＋ 他复核（见 D5）。

⇒ **tasks 0.3（「D3 若取 (b) 则 P1 全部取消」）随之作废**，本包 P1 范围确立。

### ✅ D3-bis · 缓释② 出件后的更正：**会前移的不是 24 行，是 11 行** —— 已闭合

> ✅ **Shao Peishen 2026-09-03 裁决：D3-bis ＝ (a) 采纳本班「不重判」判定。**
> 原话：「实测前移 11 行（**非裁决书所写 24 行**，另 13 行为已答交件、规则3 的 max 令其一天不动、四色一格不动），
> 实际代价小于他已接受的代价且方向一致 ⇒ 不重判。🔴 **但该数字更正须写进 design 与对照表**，
> 并在下一封致姚祖怡（男）的信中如实说明『我方原估 24、实测 11』。」
>
> ⇒ **三条落地义务**（缺一则本项不算闭合）：
> 1. ✅ **design 内更正** —— 本节 ＋ D3 三处「24 行」表述已就地更正（`OP-0903-A1`，2026-09-03）；
> 2. ✅ **对照表内更正** —— `docs/queue_118_规则2逐字化改判前后24行对照-2026-09-02.md` 新增 §〇-bis「更正记录」；
> 3. ⏳ **下一封信中如实说明「我方原估 24、实测 11」** —— 🔴 **本泳道未做，也不得做**：姚祖怡串行闸 🔒 锁
>    （`采购部#20` 在途），本包不起草、不发送任何信 ⇒ 登记为 **tasks 3.1c「待闸开后并进下一封」**。

🔴 **更正来源须写明：是逐行核对，不是重新估算。**
具体即对 `docs/queue_344_kit_date修复前后对照-2026-08-24.md`（生产载荷、tracked）中出货日
∈ {2026-09-20, 2026-10-20, 2026-11-20} 的行**全列逐行核**（24 行全列、无省略、无抽样），
再逐行判「现行齐料日是否**恰等于** 出货日+90」。**两处独立交叉验证已做**：
⑴ 筛出行数**恰为 24**，与 `#401` 对照表「分支适用面：规则 2（未来）24」逐个对上；
⑵ `#401` 变动行的出货日集合最早为 `2026-12-20`，**与本 24 行零交集** ⇒ 这 24 行自 08-24 起未被任何改动搬动过。
⇒ **「11」不是把「24」重估了一遍得出的另一个估计值，而是把「24」拆成 11 动 ＋ 13 不动的核对结果。**

缓释② 的对照表（`docs/queue_118_规则2逐字化改判前后24行对照-2026-09-02.md`）逐行算完，
结果与裁决书里那句「24 行齐料日提前 18~79 天」**对不上，必须在此更正**：

| | 裁决书表述 | 逐行推演 |
|---|---|---|
| 会前移 | 24 行 | **11 行** |
| 完全不动 | （未提） | **13 行** —— 其瓶颈件是已答交件，规则 3 的 `max` 让它们一天不动 |
| 提前量 | 18~79 天 | **≤18 / ≤48 / ≤79 天**（按出货日三组，且均为**上界**，非确定值） |
| 四色 | 整体变乐观 | **一格不动**（24 行全 🔴 → 全 🔴） |

**这就是本节上方「影响面」表的边界 ② 被逐行量化后的样子** —— 上方那张三行表假定「该行瓶颈件为无答交件」，
逐行核下来该假定只在 24 行中的 11 行成立。

🔴 **本项不构成需要请他重判的新事实**：实际代价**小于**他已接受的代价，方向一致（都是变乐观），量级更小。
✅ **该判定已于 2026-09-03 由 Shao Peishen 本人确认采纳**（见本节开头引文）—— 即「不重判」这件事本身**不再是本班的自判，而是他的裁决**。
但**必须写在这里**，否则下一个引用「24 行提前 18~79 天」的人会把一个上界当成实测值——
这与本包 D2-bis 记下的那条判据同族：**引用别处写下的「现状是……」之前，先自己算一遍。**

⚠️ **11 行这个数同样不是生产实测**：它由 tracked 的 08-24 生产载荷底稿 ＋ 本机离线算术推出，
「瓶颈件性质」一列是由「齐料日是否恰等于出货日+90」**反推**的。真实值仍属 **LAN 留步**（tasks 1.4.3）。

## D4 · 「三个月内」边界开闭：仍无签认，且承接载体已丢失

三条规则**没有一条说过边界当天算哪一边**。`#401` ⑻ 按偏保守侧落定（**含边界当天 ⇒ 走规则 2 ⇒ 起算更晚**），并登记「随对照表请他以判例确认」。

🔴 **实测该确认没有发生，且它的承接行已经没了**：`#402`／`采购部#19` 第六节送出的是规则 1 的整表对照，**未见边界开闭单独成决策点**；而 `#401` 已于 2026-08-31 销号 ⇒ **这一项现在不挂在任何一行上**。

**本包的处置**：重新承接（tasks 内挂出），**维持现行取值（含边界）不动**，随 P1 的对照表一并请他确认。

🔴 **D3 已取 (a)，本节原来的论证现已失效 —— 2026-09-02 更新**：

原文写的是「若 D3 采纳 (a)，支撑现行选法的『更保守』论证随之失效」。**D3 已于 2026-09-02 采纳 (a)，故该失效现已发生，不再是假设。**

- **失效前**：边界当天 ⇒ 走规则 2 ⇒ 起算 ＝ 出货日（更晚）⇒ 更保守 ⇒ 「取含边界」有论据。
- **失效后（P1 开启时）**：边界当天 ⇒ 走规则 2 逐字 ⇒ 起算 ＝ **今天**（更早）；而走规则 1 则起算 ＝ 出货日前推 3 个月的 20 日。
  两支孰早孰晚**不再有恒定方向**（取决于出货日落在该自然月的哪一天），⇒ **「含边界更保守」这句话在 P1 开启后连成立与否都不确定**。

⇒ **现行「含边界」从此只是一个「沿用旧值、无人签认、也不再有保守性背书」的取值**。
**本包不改它**（改它就是第三个自变量），但 **MUST NOT 再以「反正更保守」为由把它当成安全默认**——
这条论证已经死了，继续引用它就是拿一个失效的理由充当签认。

⚠️ **D4 现在比 D3 未定时更要紧，且它是本包唯一一处「明知论据已失效仍沿用」的取值** ⇒ **MUST NOT 被 P1 顺手带过**，须随 P1 对照表以判例形式单独问姚祖怡（tasks 3.1）。

### ✅ D4 定夺 —— Shao Peishen 2026-09-03 裁决 ＝ **(a) 补一条边界口径进判例包，随下一封请姚祖怡签认**

> **原话**：「补一条边界口径进判例包，随下一封请姚祖怡签认 —— 理由：D3 取 (a) 后
> 『含边界更保守』的论证由条件句变成既成事实、已失效，现行取值从此既无签认也无保守性背书，
> **必须补签，而不是记录风险接受**。」

⇒ **明确否掉的另一条路是「记录风险接受」**：本项 **MUST NOT** 以「已登记、已知悉、风险可接受」的形式收口。
**它要的是一个签认件，不是一条风险登记。**

**落点**：`4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_344_判例包素材-齐料日期不看答交数量-2026-08-18.md`
**附录 A**（本次 `OP-0903-A1` 新增，1 条判例、2 行批改表 ＋ 反推规则填空）。
🔴 **刻意并进已有判例包、不另起一个包**：姚祖怡处**串行闸 🔒 锁**（`采购部#20` 在途），
另起一个包等于多占一封信、且会与该包问的同一个取值函数分家 —— 正是判例批改法要消灭的「同一函数两周内签两套口径」。

🔴 **状态：`⏳ 待姚祖怡签认`。判据／口径类永不默认生效（IATF 显式签认红线）** ——
附录 A 内 **MUST NOT** 出现「超时默认生效」「48h 默认生效」或任何等价表述；未签认前，
现行「含边界」取值**只是事实上的沿用，不得在任何文件里被记作『已确认』**。

**本次已把该口径量化到可签的程度**（不签也看得懂代价，见 D5 §验证用例的同一组参数）：
以 `today = 2026-09-02`、边界日 `2026-12-02` 为例 ——

| 边界当天走哪一支 | 起算点 | 估算到货日 | 缺口 | 四色 |
|---|---|---|---:|---|
| **含边界 ⇒ 走规则 2 逐字（现行取值）** | 今天 2026-09-02 | 2026-12-01 | **−1 天** | **🟢** |
| **不含边界 ⇒ 走规则 1** | 2026-09-20（前推 3 个自然月的 20 日） | 2026-12-19 | **+17 天** | **🔴** |

⇒ **边界当天两支相差 18 天，且四色直接从 🟢 翻到 🔴。**
这就是「含边界更保守」失效的**可核算证据**：P1 开启后，含边界不但不更保守，在边界当天反而是**最乐观的那一支**。

## D5 · 上线形态：两个自变量、两个开关、两次上线

**P1 与 P2 各自一个独立开关、默认关闭，且 MUST NOT 同批开启。**

理由不是谨慎，是**归因**：`#401` 已实证「同一次上线带两个自变量，对照表上哪一行被哪一条搬动谁也说不清」。本包两项都会改看板上的数字，合并开启会让两份对照表互相污染。

**每一项的开启七步（`#401` 2026-08-28 那条教训逐字沿用，缺一则「已开」这句话不成立）**：

1. 反例单测覆盖全部分支；
2. SC8 全量回归零漂移（基线 **509 passed / 4 skipped**，本 worktree 2026-09-02 实测）；
3. mock 与真实黄金基准双绿；
4. 基于生产载荷出**真实前后对照表**（格式对齐 `queue_401_kit_date规则1修复前后对照-2026-08-26.md`，自带不变式自检）；
5. 姚祖怡复核该对照表；
6. `sync-to-server.ps1` 部署 `.51` 并**核进程 `CreationDate` 真刷新**；
7. 翻开关 → `POST /api/refresh` → **核数字确实变了**。

🔴 **第 6 步为什么不能省**：`#401` 2026-08-28 实证——若 `.51` 上跑的仍是旧代码，写入开关键**不报任何错、也不产生任何效果**，看板一个数字不变而**没有任何信号告诉你为什么**。这属本项目记过的「工具静默回退」族。P1 的 spec 已把「可由参数版本串识别出这种情形」写成硬要求，即对该教训的机制化。

🔴 **连带必查 `#445`**：`baoguan_service.py` 直接引用 `config.PARAM_VERSION` 常量而非 `active_param_version()`，`#401` 翻开关时已实证展示层的 `param_version` 不带分支后缀。本包新增开关会踩同一个坑 ⇒ **实现阶段须先确认 `#445` 已修，否则在本包内一并修**（属同一处缺陷的第二次触发，不是新范围）。

**P1 的不变式自检（对照表脚本必须自带，任一条不成立即判实现错误）**：
1. 只有「未来但在三个月内」那一支的行会变；
2. 齐料日只会**前移**、不会后移；
3. 🔴 **本包新增第三条：四色只会变绿、不会变红** —— 与 `#401` 的「红只减不增」形式相同但含义相反（那次是修低估，这次是采纳更乐观的判据），**必须单独写、不得照抄**。

🔴 **第 3 条在缓释② 的对照表上是「空过」的（2026-09-02 实测）**：24 行前后**全部维持 🔴**，无一行变色
⇒ 该不变式**没有被真正测到**，只是没出现反例。**MUST NOT 把「本轮没变色」记作「已验证不会变红」**——
这与 `#401` D4 那条负面结论（mock 全量零漂移 ≠ 已验证）是同一个错。
⇒ 该条须在 **LAN 真实对照表上再核一次**，且 tasks 1.2 须有一条**构造性单测**让它在人造语料上真正被触发一次（见 tasks 1.2.12）。

#### ✅ D5 定夺 —— Shao Peishen 2026-09-03 裁决 ＝ **(a) 补一条验证用例覆盖不变式③，补完再放行**

> **原话**：「补一条验证用例覆盖不变式③（它在对照表上空过、未获验证），补完再放行。」

🔴 **B3 留下的是一条「要造一份跨阈值输入」的占位要求（tasks 1.2.12），而没有人验证过这样的输入到底存不存在。**
占位要求本身不构成覆盖 —— 若阈值与窗口的算术恰好使得任何输入都无法跨档，那条不变式就是**永远无法被触发的**，
而 1.2.12 会在实现阶段变成一条写不出来的用例。**故本次的「补一条验证用例」＝ 把参数算定、把预期值写死。**

**四色判据**（沿用对照表 §二，本包不改）：缺口 ＝ 齐料日 − 出货日；`🔴 >3 天 / 🟡 1~3 天 / 🟢 ≤0 天`。

**参数**：`today = 2026-09-02`；`NO_FEEDBACK_LEAD_DAYS = 90`；瓶颈件为**无答交件**（其余子件到货日均早于它，故不干扰 `max`）。
逐字化后估算到货日恒为 `today + 90 = 2026-12-01`；现行为 `出货日 + 90`。
⇒ 后侧缺口 ＝ `90 − (出货日 − today)`，**随出货日单调递减** ⇒ 跨档输入必然存在，且可解析定位：

| 用例 | 出货日 | 距今 | 现行齐料日 | 现行缺口／四色 | 逐字化后齐料日 | 后缺口／四色 | 断言 |
|---|---|---:|---|---|---|---|---|
| **③-a 跨到 🟡** | 2026-11-30 | 89 天 | 2027-02-28 | +90 ／ 🔴 | 2026-12-01 | **+1 ／ 🟡** | 🔴→🟡 成立，方向为「变绿」 |
| **③-b 跨到 🟢** | 2026-12-01 | 90 天 | 2027-03-01 | +90 ／ 🔴 | 2026-12-01 | **0 ／ 🟢** | 🔴→🟢 成立，方向为「变绿」 |
| **③-c 反向哨兵** | 2026-11-27 | 86 天 | 2027-02-25 | +90 ／ 🔴 | 2026-12-01 | +4 ／ 🔴 | 紧邻 🟡 档却**仍 🔴**，钉住「变绿」不是无条件发生 |
| **③-d 反例（须判失败）** | 任一行 | — | — | — | 人为令其后移一天 | 更红一档 | 出现 🟢→🟡／🟡→🔴／🔴 更深 ⇒ 测试 **SHALL 失败** |

**窗口内全域自检（可手算复核，独立于上表）**：窗口内未来出货日共 91 个（`2026-09-03 … 2026-12-02`），
逐字化后 ⇒ **🔴 86 ／ 🟡 3（11-28、11-29、11-30）／ 🟢 2（12-01、12-02）**。
🔑 **这与本文件 D3「影响面」段独立写下的 `🔴86 / 🟡3 / 🟢2` 逐个吻合** —— 两处由不同路径算出、结果一致，
是一处交叉验证，不是互相抄。⇒ **跨档输入不但存在，还恰好是 5 个，全部落在窗口末端。**

🔴 **为什么生产语料对它零覆盖力，一句话讲清**：真实出货日集中在每月 20 日，
而能跨档的 5 个日子全在 11-28~12-02 之间 —— **每月 20 日这个习惯本身就把语料挡在跨档区之外**。
⇒ 这不是「碰巧没测到」，是**结构性测不到**；**LAN 上重跑一次也仍然测不到**，除非语料的出货日习惯改变。
⇒ **构造性用例是唯一出路**，`tasks 1.4.2` 的 LAN 自检**不能替代** `tasks 1.2.13`。

**落点**：spec `delivery-date-forecast` 的「规则 2 逐字化的退化守卫」Requirement 新增
Scenario「四色跨档由构造语料真正触发」；tasks 新增 **1.2.13**（参数与预期值即上表，实现阶段照抄不再另算）。
🔴 **本泳道不进实现，故 1.2.13 未勾** —— 本次交付的是**可直接落测的用例定义**，不是测试代码。

## D6 · P2 的落点与数据边界

- **导入件落点**：`4-数字员工/采购部/SC8-客户订单交期智能承诺/data/priority/`（原始 `.xlsx`）＋ 解析结果落 `data/`（`.json`）。
- 🔴 **`.gitignore` 实测缺口（`git check-ignore -v`，非推断）**：`data/priority/*.xlsx` **未被覆盖**；`data/*.json` 已被 `.gitignore:4:*.json` 覆盖。⇒ **实现阶段必须先补忽略规则再落第一个 `.xlsx`**。PMC 优先级表含真实客户订单优先级，属业务数据，本就不应入库。
- **resolver 粒度**：`priority_resolver` 接口粒度只到 `so_id`；真实数据里同一 FO 文档下多行项共享同一 `so_id`，resolver 对其无从区分 ⇒ **三级判据继续作为同一 `so_id` 内的二级排序**，这是既有 spec 已定的口径，本包不改。
- 🔴 **一处必须问、不得推断的空白**：`#118` 行内明写——姚祖怡 2026-08-12 给出三级判据时，**未说明这三级是否即 PMC 优先级表的最终形态、还是仍需表格覆盖**，「巡逻不推断」。本包同样不推断，列为 tasks 内的待确认项。

## D7 · 本包**不做**什么（逐条对应一个曾经被顺手做掉的先例）

- **不改规则 1** —— 已上线、已签认、已生效。
- **不改规则 3** —— 结构性已覆盖。
- **不重开「上月未齐套跨月占用」** —— 归档 §一 `#150`：姚祖怡 2026-08-06 三行判例**全批「按照现状判定」**，口径点已关闭且无需改代码。
- **不改「缺口 ≤0 不进缺料清单」** —— `baoguan.py:556`（`#151`）已按其原话在**展示层**过滤。🔴 `#118` 行内明写「**不得顺手把它改成计算层剔除**，否则会影响齐套数量本身」。
- **不改 SRM/PO 取数口径、不换取数源** —— `#344` design D1 已明确：换源与否是另一条独立判据、须另行签认。
- **不改四色分级阈值** —— 姚祖怡 2026-08-28 另有新指示（🔴 晚 2 周以上／🟡 晚 2 周以内，§四 `#110`），那是**显示层分级**、与本包的**起算点与占用次序**是两件事，混在一起会让对照表再多一个自变量。
- **不起草、不发送任何跟进信** —— 姚祖怡串行闸锁着（`采购部#20` 在途）；判闸唯一入口＝`python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`，起草前现跑。
- **不代归档 `sc8-kit-date-rule1-start`** —— 该包对应的 `#401` 已销号却未归档（违「完工即归档纪律」），但归档动作会改 `openspec/specs/` 主 spec、与本包的 spec delta 撞车。**只登记，交值周巡检或原线收口。**

## Open Questions

### ✅ 已闭合

1. **【Shao Peishen】D3：规则 2 改不改** —— ✅ **2026-09-02 答 (a) 逐字化**，附三条缓释（落点见 D0）。
   ⇒ tasks 0.2 可勾、0.3 作废；本包 P1 范围确立。
2. **【Shao Peishen】D3-bis：11 行 vs 24 行要不要重判** —— ✅ **2026-09-03 答 (a) 不重判**，
   但命「更正须写进 design 与对照表」＋「下一封致姚祖怡的信中如实说明『我方原估 24、实测 11』」。
   ⇒ 前两项本次已落（见 D3-bis）；第三项属对外发送，登记 **tasks 3.1c「待闸开后并进下一封」**。
3. **【Shao Peishen】D4：边界口径怎么收口** —— ✅ **2026-09-03 答 (a) 补一条边界口径进判例包、随下一封请姚祖怡签认**
   （🔴 明确否掉「记录风险接受」）。⇒ 判例包附录 A 已成稿，状态 `⏳ 待姚祖怡签认`（见下「仍未答」第 2 条）。
4. **【Shao Peishen】D5：不变式③ 空过怎么办** —— ✅ **2026-09-03 答 (a) 补一条验证用例，补完再放行**。
   ⇒ 用例参数与预期值已算定并落 spec ＋ tasks 1.2.13（见 D5 §验证用例）。**本项已补完**，放行仍归 tasks 0.1。

### 🔴 仍未答（两项均须**姚祖怡本人**答，无默认）

> 🔴 **两项当前都不得起草新信** —— 姚祖怡处**串行闸锁着**（`采购部#20` 在途）。
> ⇒ **登记为「待闸开后并进下一封」**，即闸一开就随下一封采购信并进去，**不为它们单独起信、不插队**。
> 判闸唯一入口＝`python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`，起草前现跑，**不得引用本文件里的闸态快照**。

2. **【须姚祖怡答】D4：「三个月内」的边界当天算哪一边** —— 现行取「含边界」，无人签认。
   🔴 **D3 已取 (a)，支撑现行选法的「更保守」论证已经失效**（不再是条件句，见 D4）。**无默认**；随 P1 对照表以判例形式问。
   ✅ **2026-09-03 已按 Shao Peishen 裁决成稿**：判例包素材 **附录 A**（1 条判例、2 行批改表 ＋ 反推规则填空、边界当天两支相差 18 天且四色 🟢/🔴 对立的算术已端出）。
   ⇒ 状态：**`⏳ 待姚祖怡签认` ＋ 待闸开后并进下一封**。🔴 **判据类永不默认生效，附录 A 内不得写任何「超时／默认生效」表述。**
3. **【须姚祖怡答】D6：三级判据是否即 PMC 优先级表的最终形态** —— 他 2026-08-12 未提，`#118` 明写巡逻不推断。**无默认**；随 P2 的对照表问。
   ⇒ 状态：**待闸开后并进下一封**。
4. **【须向姚祖怡说明，非提问】D3-bis 的数字更正** —— 下一封信中如实说明「**我方原估 24、实测 11**」
   （Shao Peishen 2026-09-03 裁决明令）。**不是决策点、不占决策点名额**，但**不得省略**：
   他手上那份 24 行的说法来自我方，我方核出 11 行却不告知，等于让他按一个已知不准的数继续判断。
   ⇒ 状态：**待闸开后并进下一封**（tasks 3.1c）。

### 🟡 尚待走审

- **design 审本身（tasks 0.1）仍未过** —— 🟡 档，未经 Shao Peishen 通过前不得 `/opsx:apply`、不得动手实现。
  本次 `OP-0902-B3` 只落 design/spec/tasks 与缓释② 出件，**未进实现、未部署、未触碰 `.51`**。
- **2026-09-03（`OP-0903-A1`）补充**：D3-bis／D4／D5 三项重审已按裁决**全部补齐**，
  ⇒ **「补完再放行」的『补完』这一半已完成**；🔴 **『放行』这一半仍是 🟡 档、仍归 tasks 0.1，本泳道不自行勾。**
  本次同样**未进实现、未部署、未触碰 `.51`、未起草或发送任何信**。
  ⚠️ **放行后仍带一项未闭合的对外依赖**：D4 的附录 A **须姚祖怡签认**，而串行闸 🔒 锁 ⇒
  即便 design 审过，**tasks 1.5.4／3.1 仍是硬前置，MUST NOT 因 design 已放行而视作可翻开关**。
