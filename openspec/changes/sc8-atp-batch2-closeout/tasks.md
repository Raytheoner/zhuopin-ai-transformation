# sc8-atp-batch2-closeout Tasks

> 🔴 **design 审属 🟡 档，未经 Shao Peishen 通过前一条都不得动手。**
> ✅ **D3 已于 2026-09-02 拍板 ＝ (a) 逐字化**（`OP-0902-B3`），原「D3 未答复前 §1 不许开工」的硬闸**已解除**；
> **但 0.1 design 审仍未过，它才是 §1 的现行闸**。
> 🔴 **本泳道（`OP-0902-B3`）只到 design/spec，未进实现** —— §1 起全部未勾属实，非遗漏。
>
> ✅ **2026-09-03 更新（`OP-0903-A1`，泳道 `sc8-kitdate-3`，批 `B-0903_10`）**：B3 落字后新开的
> **D3-bis／D4／D5** 三项已由 Shao Peishen **全部裁决为 (a)** 并按裁决补齐（见 design 头部更新表）。
> 新增／改动的 tasks 条目：**1.2.13**（D5 验证用例）／**3.1**（D4 判例包落点）／**3.1c**（D3-bis 数字更正随信说明）。
> 🔴 **0.1 design 审仍未勾** —— 裁决说的是「补完再放行」，本泳道只做「补完」，放行仍属 🟡 档。
>
> ✅ **2026-09-03 再更新（`OP-0903-B1`，泳道 `sc8-kitdate-4`，批 `B-0903_20`）—— design 已放行、spec 已落主 specs**：
> **`0.1` 与 `0.6.4` 本次回勾**（Shao Peishen 放行原文见 0.1；其选项 (a) 原文即含「勾 tasks 0.1」）。
> 两份 delta 已走 **`/opsx:sync`** 合入 `openspec/specs/`，**变更包维持在办、不归档**（详见新增 **§4**）。
> 🔴 **上面那句「未经通过前一条都不得动手」现已换闸、不是解除** —— 现行硬闸是他放行时同一句里写的
> 「**实现属另立行、本轮不做**」，外加 `1.5.4`／`3.1` 两项姚祖怡签认（串行闸 🔒，`采购部#20` 在途）。
> 🔴 **§1／§2 全部未勾仍属实、非遗漏**；`openspec/specs/` 里写着的开关**此刻并不存在**，见 **§4.3**。

## 0. design 审与前置定夺

- [x] 0.1 ✅ **Shao Peishen 2026-09-03 放行 design** —— 原话「予以放行」，依据＝D3-bis／D4／D5 三项补齐已完成 ＋ `openspec validate --all --strict` 127 passed / 0 failed；其选项 (a) 原文含「**勾 tasks 0.1**」⇒ 本次回勾属**落他的裁决**，非自审。
  🔴 **放行 ≠ 开工**：他同时明写 ⑴ 可写 spec、⑵ **实现属另立行、本轮不做**、⑶ D4 附录 A 须姚祖怡签认 ⇒ **tasks 1.5.4／3.1 仍是硬前置，不得据此翻开关、不得部署、不得触碰 `.51`**。⇒ §1 的硬闸由「design 审」换成「实现另立行的排期」，**不是解除**
- [x] 0.2 **D3 定夺：规则 2 改不改** —— ✅ **Shao Peishen 2026-09-02 答 (a) 逐字化**，原话「严格按姚祖怡（男）2026-08-18 书面文本实现规则 2」；已明示知悉并接受「24 行提前 18~79 天、结论变乐观、方向与前两轮相反」的代价
- [x] 0.3 ~~D3 若取 (b)：本包 P1 全部取消~~ —— **作废**：D3 已取 (a)，本条前提不成立
- [ ] 0.4 起草任何给姚祖怡的材料前，现跑 `python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`（判闸唯一入口）。🔴 实测当前闸态＝🔒 锁（`采购部#20` 在途）

### 0.5 🔴 D3 裁决附带的三条缓释（**缓释是裁决的一部分，不是建议**；索引见 design D0）

- [x] 0.5.1 **缓释③** —— design 中逐字引用三条规则原文并标注来源与日期：✅ 在 design **D1**（原文一字未改，本次补标其为缓释③落点）
- [x] 0.5.2 **缓释②** —— 出「改判前后 24 行逐行对照表」：✅ `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_118_规则2逐字化改判前后24行对照-2026-09-02.md`
  - 🔴 **落 tracked 的 `docs/` 而非 opener 字面写的 `reports/`** —— `reports/` 被 `.gitignore:35:**/reports/` 覆盖（`git check-ignore -v` 实测），审计件落进去会随 worktree 删除而丢（`#267`）。已在仓库根 `reports/` 另放同名副本满足字面要求，**正本在 `docs/`**
  - 🔴 **结论与裁决书表述有出入**：逐行核下来**会前移的是 11 行、不是 24 行**（另 13 行瓶颈件为已答交件，规则 3 的 `max` 让其不动）；四色**一格不动**。实际代价小于他已接受的代价、方向一致 ⇒ **不重判**，但已写进 design **D3-bis** 留痕
  - ✅ **2026-09-03 闭合**：Shao Peishen 裁决 **D3-bis ＝ (a)**，采纳「不重判」；「不重判」自此**不再是本班自判、而是他的裁决**。三项落地义务：design 内更正 ✅／对照表内更正 ✅（§〇-bis）／随下一封信如实说明 ⏳（tasks **3.1c**）

### 0.6 ✅ B3 落字后新开的三项重审 —— Shao Peishen 2026-09-03 **全部裁决 (a)**，本批（`OP-0903-A1`）按裁决补齐

- [x] 0.6.1 **D3-bis 11 vs 24** ＝ (a) 不重判 ＋ 命更正数字 ⇒ design D3／D3-bis 已更正、对照表 §〇-bis 已加；**随信说明留 3.1c**
- [x] 0.6.2 **D4 边界口径** ＝ (a) 补一条边界口径进判例包、**随下一封请姚祖怡签认**（🔴 明确否掉「记录风险接受」）⇒ 判例包**附录 A** 已成稿，状态 `⏳ 待姚祖怡签认`（详见 3.1）
- [x] 0.6.3 **D5 不变式③** ＝ (a) 补一条验证用例、**补完再放行** ⇒ 参数与预期值已算定，落 spec 新 Scenario ＋ tasks **1.2.13**
- [x] 0.6.4 ✅ **「放行」已于 2026-09-03 由 Shao Peishen 做出** —— 0.6.1~0.6.3 是裁决里的「补完」那一半（`OP-0903-A1` 完成），「放行」这一半由他本人在 `B-0903_10` 的 `resume` 里答 (a) 补齐；见 0.1
- [ ] 0.5.3 **缓释①** —— 反例单测（**关掉修复即失败的退化守卫**，不是只测 happy path）：spec 已加「规则 2 逐字化的退化守卫」Requirement（4 个 Scenario），tasks 已加 **1.2.9~1.2.12**。🔴 **本泳道不进实现，故本条未勾**；实现泳道须逐条落地并回勾

## 1. P1 · 无答交起算点规则 2 逐字化（🔴 design 审过后才开工）

### 1.1 实现
- [ ] 1.1.1 `sc8/config.py` 新增独立开关键（**MUST NOT** 与 `SC8_KIT_DATE_RULE1` 合并），默认 OFF
- [ ] 1.1.2 `sc8/config.py::active_param_version()` 在本开关 ON 时追加可识别分支标记
- [ ] 1.1.3 `forecast.no_feedback_start_date()` 增加规则 2 逐字支；🔴 **起算点仍是唯一决策点，`baoguan` 只传值不判分支**
- [ ] 1.1.4 🔴 核 `#445`：`baoguan_service.py` 是否仍直接引用 `config.PARAM_VERSION` 常量。已修则记一句证据；未修则在本包内一并修（同一缺陷第二次触发，非新范围）

### 1.2 单测（TDD，先写后改）
- [ ] 1.2.1 未来但在三个月内 ⇒ 起算点 ＝ 业务日期（新行为）
- [ ] 1.2.2 已过期 ⇒ 起算点 ＝ 业务日期（取值不变，钉住「本项没动这一支」）
- [ ] 1.2.3 三个月之外 ⇒ 仍走规则 1，逐值不变
- [ ] 1.2.4 规则 3 不受影响：既有答交又有无答交时齐料日仍取更晚
- [ ] 1.2.5 开关 OFF 时逐字节回到现行口径
- [ ] 1.2.6 两个开关四种组合各自独立成立（OFF/OFF、ON/OFF、OFF/ON、ON/ON）
- [ ] 1.2.7 🔴 不变式单测：同一输入下 ON 侧齐料日 **MUST NOT** 晚于 OFF 侧
- [ ] 1.2.8 🔴 参数版本串在展示层与审计两处取自同一次 `active_param_version()` 求值

#### 🔴 1.2.9~1.2.12 ＝ 缓释①「退化守卫」（Shao Peishen 2026-09-02 裁决附带，**不是可选加固**）

> 判据：**把修复关掉／退回旧写法，这四条必须立刻红。** 只测 happy path 的用例不满足本组要求 ——
> 它在「开关没真正接通」和「调用方漏传起算点」两种情形下同样会绿。

- [ ] 1.2.9 🔴 **退回旧起算点即红**：断言 ON 时「未来但在三个月内」支返回的**不是** `max(出货日, 业务日期)`；
  失败信息须打印实际起算点与业务日期之差。**注释里写死：若有人把这一支改回 `max(...)`，本条即为拦截点**
- [ ] 1.2.10 🔴 **差值逐日锁死、不只测单点**：对窗口内每个可能的未来出货日，断言 `OFF起算点 − ON起算点 ≡ 出货日 − 业务日期`。
  单点用例 **MUST NOT** 替代本条（`#401` 的教训：规则 1 的正确性一度只由抽样点承担）
- [ ] 1.2.11 🔴 **开关接通性本身被覆盖**：构造「实现存在但调用方漏传起算点」的情形，断言测试**判失败**而非因取值合法而通过
- [ ] 1.2.12 🔴 **四色「只变绿不变红」须有构造性用例真正触发一次**：
  缓释② 的对照表上该不变式**空过**（24 行前后全 🔴、无一行变色）⇒ 生产语料对它零覆盖力。
  须造一份跨阈值输入让它真被触发；**MUST NOT 因「没出现反例」记作已验证**（同 `#401` D4 的负面结论）。
  ⇒ **具体参数与预期值见 1.2.13**（2026-09-03 已算定，实现阶段照抄、不再另算）
- [ ] 1.2.13 🔴 **不变式③ 的验证用例（Shao Peishen 2026-09-03 裁决 D5 ＝ (a)「补完再放行」的落点）**：
  参数 `业务日期 = 2026-09-02`／`NO_FEEDBACK_LEAD_DAYS = 90`／瓶颈子件为无答交件；
  四色判据沿用 `缺口 = 齐料日 − 出货日`，`🔴 >3 / 🟡 1~3 / 🟢 ≤0`。四行断言：

  | 用例 | 出货日 | OFF 侧齐料日／缺口／色 | ON 侧齐料日／缺口／色 | 断言 |
  |---|---|---|---|---|
  | ③-a | 2026-11-30 | 2027-02-28 ／ +90 ／ 🔴 | 2026-12-01 ／ **+1** ／ **🟡** | 🔴→🟡 真跨档 |
  | ③-b | 2026-12-01 | 2027-03-01 ／ +90 ／ 🔴 | 2026-12-01 ／ **0** ／ **🟢** | 🔴→🟢 真跨档 |
  | ③-c | 2026-11-27 | 2027-02-25 ／ +90 ／ 🔴 | 2026-12-01 ／ +4 ／ 🔴 | 反向哨兵：紧邻 🟡 档仍 🔴 |
  | ③-d | 任一行 | — | 人为令其后移一天 | 出现更红一档 ⇒ 测试 **须失败** |

  🔴 **本条 MUST NOT 由 1.4.2 的 LAN 自检代替**：可跨档的出货日只有 5 个（`2026-11-28`~`2026-12-02`），
  而真实语料的出货日集中在**每月 20 日** ⇒ 该不变式在生产语料下属**结构性测不到**，LAN 复跑同样测不到。
  🔴 **本泳道不进实现，故未勾** —— 本次交付的是用例定义（参数＋预期值），不是测试代码

### 1.3 回归
- [ ] 1.3.1 SC8 全量 ≥ **509 passed / 4 skipped** 且零回归（基线 2026-09-02 本 worktree 实测）
- [ ] 1.3.2 开关强制 ON 再跑一遍全量，与 OFF 对比并**如实记录差异**。🔴 **若两侧再次完全相同，必须像 `#401` ⑷ 那样明写「这说明 mock 语料对本项没有端到端覆盖力」，不得读作「已验证」**
- [ ] 1.3.3 mock 黄金基准零漂移
- [ ] 1.3.4 真实黄金基准（`data/golden/real_frozen`）零漂移；🔴 冻结件属 gitignore 件，本 worktree 内不存在则先取用或重建，**不得因 skip 而当作通过**
- [ ] 1.3.5 平台底座回归零回归
- [ ] 1.3.6 `openspec validate --all --strict` 全绿

### 1.4 真实对照表（🔴 LAN／生产载荷，本机当前不可达 ⇒ 留步项）
- [ ] 1.4.1 扩写对照表脚本（复用 `scripts/compare_kit_date_rule1.py` 的 `--cache` 冻结格式，两脚本须可互换复用同一份冻结输入）
- [ ] 1.4.2 三条不变式自检写进脚本：⑴ 只有「未来但在三个月内」支的行会变；⑵ 齐料日只前移不后移；⑶ 🔴 **四色只变绿不变红**（与 `#401` 的「红只减不增」形式同、含义反，**单独写不照抄**）
- [ ] 1.4.3 出真实前后对照表，格式对齐 `docs/queue_401_kit_date规则1修复前后对照-2026-08-26.md`。
  🔴 **出表后须与缓释② 的离线推演件逐行比对**（`docs/queue_118_规则2逐字化改判前后24行对照-2026-09-02.md`）：
  该件推得「24 行中 11 行前移、13 行不动」，其中「瓶颈件性质」是由「齐料日是否恰等于出货日+90」**反推**的。
  **真实结果与推演不符时以真实为准，并回头改写该件与 design D3-bis**，不得让一份已被推翻的推演继续留作口径
- [ ] 1.4.4 🔴 对照表须同时端出「**齐料日提前 ≠ 交期变好**」这句反读防护——本项是采纳更乐观的判据，不是修好了一个低估

### 1.5 上线（🔴 七步缺一，「已开」这句话都不成立）
- [ ] 1.5.1 代码 ff 合入 master
- [ ] 1.5.2 `sync-to-server.ps1` 部署 `.51`，**核进程 `CreationDate` 真刷新**（不只看脚本打印「服务已重启」）
- [ ] 1.5.3 部署后先核基线零漂移（开关未翻时四色与部署前一致、参数版本串不带本项标记）
- [ ] 1.5.4 🔴 姚祖怡复核对照表并签认（硬前置，**未签认不得翻开关**）
- [ ] 1.5.5 翻开关（幂等追加，先备份 `.51` 上 `.env` 原文并记 SHA256）
- [ ] 1.5.6 `POST /api/refresh` 触发全量重算
- [ ] 1.5.7 **核四色确实变了** ＋ 核参数版本串确实带上了本项标记

## 2. P2 · PMC 优先级排序表导入并接入 `priority_resolver`（#15）

🔴 **P2 MUST NOT 与 P1 同批开启**（两个自变量，对照表会互相污染，见 design D5）。

### 2.1 前置
- [ ] 2.1.1 🔴 先补 `.gitignore` 覆盖 `data/priority/*.xlsx`，用 `git check-ignore -v` 验证命中后，才允许落第一个上传件（实测当前**未被覆盖**）
- [ ] 2.1.2 取回姚祖怡早前给过的导入表结构原件，确认列名与语义（数字越小优先级越高）
- [ ] 2.1.3 🔴 **待确认项（无默认）**：三级判据是否即优先级表的最终形态、还是仍需表格覆盖 —— 他 2026-08-12 未提，`#118` 明写不推断

### 2.2 实现
- [ ] 2.2.1 解析器：`.xlsx` → `{so_id: 优先级数字}`，解析结果落 `data/`（`.json`，已被 gitignore 覆盖）
- [ ] 2.2.2 🔴 fail-loud：无法解析／缺列／重号／非数值／订单标识对不上 ⇒ **拒绝导入并说明原因**，保留上一份有效件；**MUST NOT 静默回退到三级判据**
- [ ] 2.2.3 构造真实 `priority_resolver` 并接入 `_resolve_priority_order`；三级判据降为同一 `so_id` 内的二级排序（既有 spec 口径，不改）
- [ ] 2.2.4 导入留痕：上传人 + 上传时间 + 件标识写 audit，使任一次重算可回溯到具体某一份导入件（IATF）
- [ ] 2.2.5 `sc8/webapp.py` 导入入口；🔴 **不新起端口**，挂统一门户路由（根 `CLAUDE.md` §5 硬约束）
- [ ] 2.2.6 界面可判别当前占用次序由真实表决定还是三级判据兜底；未被表覆盖的订单数须显式呈现
- [ ] 2.2.7 独立开关，默认 OFF

### 2.3 单测与回归
- [ ] 2.3.1 resolver 生效时次序不被数量判据反超
- [ ] 2.3.2 同一 `so_id` 多行项按三级判据二级排序
- [ ] 2.3.3 表未覆盖的订单退回三级判据，且该情形被显式记录
- [ ] 2.3.4 五类导入失败各一条反例单测，均须拒绝而非回退
- [ ] 2.3.5 开关 OFF 时零漂移（即使系统内已存在一份已导入的表）
- [ ] 2.3.6 SC8 全量零回归 ＋ 黄金基准双绿

### 2.4 真实核验与上线（LAN／生产载荷 ⇒ 留步项）
- [ ] 2.4.1 真实数据下 resolver 生效前后的**占用次序对照表**出件
- [ ] 2.4.2 姚祖怡复核（硬前置）
- [ ] 2.4.3 部署 → 核 `CreationDate` → 翻开关 → `POST /api/refresh` → 核数字（同 1.5 七步）

## 3. 遗留承接与收口

### 3.0 🔴 两项须姚祖怡本人答的，统一登记为「**待闸开后并进下一封**」

> **串行闸当前 🔒 锁**（`采购部#20` 在途）⇒ **不得起草新信、不得为它们单独起信、不得插队**。
> 闸一开即**并进下一封采购信**一并问。判闸唯一入口＝`python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`，
> 起草前**现跑**，MUST NOT 引用本文件或 design 里的闸态快照（`#65` 行内已因写快照被 lint 判过一次）。

- [ ] 3.1 🔴 **D4「三个月内」边界开闭确认**（→ **待闸开后并进下一封**）—— 现行取「含边界」，无人签认；原承接行 `#401` 已销号 ⇒ 本包重新承接。**无默认**，随 P1 对照表以判例形式请姚祖怡确认。
  ⚠️ **D3 已取 (a)，支撑现行选法的「更保守」论证已经失效**（见 design D4，已由条件句改为既成事实）⇒ 本项重要性上升，且是本包唯一一处「明知论据已失效仍沿用」的取值，**MUST NOT 被 P1 顺手带过**
  ✅ **2026-09-03 已按 Shao Peishen 裁决 D4 ＝ (a) 成稿**（🔴 他明确否掉了「记录风险接受」这条路，要的是**签认件**）：
  落点 ＝ `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_344_判例包素材-齐料日期不看答交数量-2026-08-18.md` **附录 A**
  （1 条判例、2 行批改表 ＋ 反推规则填空 ＋ 边界当天两支相差 18 天且四色 🟢/🔴 对立的算术）。
  🔴 状态 `⏳ 待姚祖怡签认`；**判据／口径类永不默认生效，附录 A 内不得出现任何「超时／默认生效」表述**（已核，无该表述）。
  🔴 **成稿 ≠ 已发**：串行闸仍 🔒 锁（`采购部#20` 在途），本泳道未起草信、未发送任何件
- [ ] 3.1b 🔴 **D6 三级判据是否即 PMC 优先级表的最终形态**（→ **待闸开后并进下一封**）—— 同 2.1.3；他 2026-08-12 未提，`#118` 明写不推断。**无默认**，随 P2 对照表问
- [ ] 3.1c 🔴 **向姚祖怡如实说明「我方原估 24、实测 11」**（→ **待闸开后并进下一封**）——
  Shao Peishen 2026-09-03 裁决 D3-bis ＝ (a) 时**明令**的第三项落地义务（前两项「写进 design」「写进对照表」已于本日完成）。
  ⚠️ **这不是决策点、不占一信 ≤3 决策点的名额**，是一条**更正告知**：他手上那个「24 行」的说法来自我方，
  我方逐行核出 11 行却不告知，等于让他按一个已知不准的数继续判断。
  措辞要点：⑴ 24 是**适用面**、11 是**实际会前移的行数**，另 13 行因瓶颈是已答交件、规则 3 的 `max` 令其不动；
  ⑵ 提前量 ≤18／≤48／≤79 天均为**上界**；⑶ **四色一格不动**；⑷ 该 11 行仍是**离线算术、非生产实测**（LAN 留步）。
  🔴 本泳道**未起草任何信**（串行闸 🔒 锁）
- [x] 3.2 队列 §四 `#65` 回写 —— ✅ 该行 2026-09-02 已由 Cowork `OP-0902-X3` 回写「答 (a)」及其两条代价。**本泳道不重复回写**，只在 §二 登记本批次
- [x] 3.3 ✅ **队列 §一 `#118` 已回写**（`OP-0903-B1`，2026-09-03 逐条复核在位）：本条要求的四条实测结论（规则 1 已上线生效／规则 3 结构性已覆盖／规则 2 为唯一残项／方向冲突前提已不成立）＋ D3 拍板 (a) ＋ 缓释② 出件且**实际影响面 11 行而非 24 行**，**已由 `OP-0902-B3` 与 `OP-0903-A1` 两批分别写进 `#118` 行**；本批复核在位后不重复回写，只追加本批（spec 落主 specs／`暂不归档` 判定／`spec ≠ 已实现` 说明／闸态现取）
- [ ] 3.4 🔴 **只登记不代办**：`openspec/changes/sc8-kit-date-rule1-start/` 对应的 `#401` 已于 2026-08-31 销号却未归档（违「完工即归档纪律」）。**本包不代它归档**——归档会改 `openspec/specs/` 主 spec、与本包 spec delta 撞车。交值周巡检或原线收口
- [ ] 3.5 tasks 全 [x] 后当场 `/opsx:archive sc8-atp-batch2-closeout -y`（完工即归档纪律）
- [ ] 3.6 SC8 场景 `CLAUDE.md` 更新（🔴 其第 93~96 行关于「规则 1 待真实核验与签认」「三步 LAN 留步均未做」的记载已于 2026-08-27／08-31 全部过时，实测须改）

## 4. 落地记录 · `OP-0903-B1`（泳道 `sc8-kitdate-4`，批 `B-0903_20`，2026-09-03）

### 4.1 🔴 **暂不归档**（机器可认的声明，落点＝本 tasks.md；`工具-落库sweep.py:688 STALE_CHANGE_DEFER_MARKER`）

本包 **`暂不归档`**，理由不是遗忘、也不是无人留话，而是**真未完项仍在**：

- **`1.5.4` 姚祖怡复核对照表并签认** —— 硬前置，未签认不得翻开关；
- **`3.1` D4「三个月内」边界口径签认** —— 附录 A 已成稿，状态 `⏳ 待姚祖怡签认`；
- 二者共用同一条**串行闸**，实测 **🔒 锁**（`采购部#20` 在途）⇒ 闸不开则无从推进，**本包不为它们单独起信、不插队**。

🔴 **刻意不选另两条入口**，理由逐条写明（不静默择一）：

| 入口 | 为何不用 |
|---|---|
| `预期观察窗口：N 天` | 要填一个 `N`。本包的等待对象是**姚祖怡签认 ＋ 串行闸开闸**，其时点**无人给过**；由本泳道现编一个天数即等于**自造阈值**，违「判据／口径／阈值类绝不默认生效」。**不填 ＞ 乱填** |
| `工具-落库sweep.py --ack-stale-change` | 语义是「**复核者**对过去某一刻的确认」，且适用于「实质完工、只差归档」的包。本包 **72 项里 61 项真未完**（收工读数；`0.1`／`0.6.4`／`3.3` 为本批新勾的三项），不是那个形态；且本泳道无人可充当复核者 |

📌 现状下 `工具-变更包自动归档.py::alert_class` 对本包判 **`ALERT_UNFINISHED`**（「尚有 N 条真未完项 —— 它没完工，不属遗忘归档」），
与本节声明同向；本节是**显式化**那条判定，使「为什么不归档」不必靠读 72 个复选框反推。

### 4.2 spec 已落主 specs（🔴 **走 `/opsx:sync`，不是 `/opsx:apply`、更不是 `/opsx:archive`**）

- **两份 delta 已合入 `openspec/specs/`**：`delivery-date-forecast` ＋3 条 Requirement（规则 2 逐字化／其独立开关／退化守卫）；
  `pmc-priority-allocation` 改写 1 条（「PMC 优先级占用框架（桩实现）」，桩形态降为**降级路径**）＋新增 3 条（导入／fail-loud／独立开关）。
- **变更包本身维持在办、不归档**，spec delta 原文保留在包内（archive 时可再次合入，同名 Requirement 走「已存在则更新」）。
- 📌 **方法与队列 `#299`（2026-08-07）同款**：该行实测 FI2／FI1／`wecom-aibot-channel` 三包均有真未完项、**不得假装勾完**，
  故用 `/opsx:sync` 把 delta 合入主 specs 而**不归档**。本包情形相同，沿用同一判法。
- 🔴 **`/opsx:apply` 在本仓库的实际语义是「实现 tasks 里的待办」**（`.claude/commands/opsx/apply.md`；`openspec` CLI **无 apply 子命令**，
  能写 `openspec/specs/` 的只有 `archive` 与 agent 驱动的 `sync`）。而本轮 **🔴 明禁进实现**、**🔴 明禁归档**
  ⇒ 若按字面跑 `/opsx:apply`，交付物只能落空。取「把 spec 落进 `openspec/specs/`」这一**目的句**为准，用 `sync` 达成。

### 4.3 🔴 一条必须端在前面的如实说明：**spec 进主 specs ≠ 已实现**

`openspec/specs/` 现在写着「规则 2 逐字化由独立开关控制且默认关闭」——但**那个开关此刻并不存在**（`sc8/config.py` 未加键、
`forecast.no_feedback_start_date()` 未加分支）。**「默认关闭」与「压根没有」在行为上同为零漂移，在文字上却长得像已建成。**

⇒ 读 `openspec/specs/delivery-date-forecast` 与 `pmc-priority-allocation` 的人，**MUST NOT** 据此认为 P1／P2 已可用；
本包 §1／§2 全部未勾即其唯一权威状态。该口径与 `#299` 那条「spec 存在只代表 spec 层可追溯，不代表与当前代码逐字一致」同族，
但本包更进一步：**这里连"曾经一致过"都不成立** —— 是先写 spec、后建造。

### 4.4 本泳道未做（如实登记，非遗漏）

- **4.4.1** 🔴 **未进实现** —— §1／§2 一条未动（Shao Peishen 放行原文：「实现属另立行、本轮不做」）
- **4.4.2** 🔴 **未合入 master** —— `merge_to_master` 属 🟡 档，本泳道不自行拍；分支 `claude/op0903b1-atp-apply` 已 push，commit 不随 worktree 删除而丢
- **4.4.3** 🔴 **未起草、未发送任何信** —— 串行闸 🔒 锁；`3.1`／`3.1b`／`3.1c` 三项维持「待闸开后并进下一封」
- **4.4.4** 🔴 **未部署、未触碰 `.51`** —— 且本机当前 off-LAN，`1.4`／`2.4` 整节仍属 LAN 留步
- **4.4.5** 📌 **`3.6`（SC8 场景 CLAUDE.md 第 93~96 行过时）本轮未动** —— 不在本 opener 的「做什么」四项内，不自行扩范围；留原线或实现行收口
- **4.4.6** 📌 **`pmc-priority-allocation` 主 spec 的 `## Purpose` 仍是 `TBD - created by archiving change shortage-baoguan-criteria-v3`** ——
  本轮**刻意不改**：它不属本包 delta，改它等于在 🟡 档的口径正本上做范围外改动。登记交后续包顺手补

### 4.5 🔴 闸态**现取**结果：锁着，但**卡它的信不是 `#20`**

`python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`（2026-09-03 现跑，判闸唯一入口）：

```
闸：🔒 锁
依据：采购部#21 · ✅ 已推送 2026-08-31 13:50 UTC · 在途
最近入信：无未拆件的入信行
下一个可用号：采购部#22
```

⇒ **结论不变（仍 🔒 锁，`3.1`／`3.1b`／`3.1c` 仍不得起信）**，但本包与 opener 里多处写的
「`采购部#20` 在途」是**过时快照** —— 在途的是 **`#21`**，下一个可用号是 **`#22`**。

🔴 **此处刻意不去逐处改写正文里的 `#20`**：那些是各泳道当时的**如实记录**（历史记录不追改），
且 `3.0` 已明写「MUST NOT 引用本文件或 design 里的闸态快照，起草前**现跑**」——
**真正的修法是永远现跑，而不是把快照改新一点**（快照改新之后照样会再次变旧，`#65` 行已因写快照被 lint 判过一次）。
本节存在的意义只是：**留一条「快照确实已经旧了」的实证**，供下一班起信时不必再自己撞一次。
