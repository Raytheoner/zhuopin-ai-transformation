# claude-progress-section-lint Tasks

## 0. 待定夺（三项，全部未完成，本包不得归档）

**三项动手的都不是本 session ⇒ 按根 CLAUDE.md §5「答完之后由谁动手」判据，一律不给选项、只登记待总线派发。三项写法必须一致。**

- [ ] 0.1 **追认 design.md 决策点 1**：条目区由「形状判据」收窄为「区间判据」——规格 §六 J2 的裸判据在真身上数出 4 条而真值 2 条，实现按 (a) 收窄。 🔴 **二期后本项的分量变了，必须重说一遍**：一期这条判据只影响一行告警文案；二期已切 `--enforce`（§10），**它现在决定 CI 拦不拦人**——同一份根 CLAUDE.md，按形状判据是 4 条、按区间判据是 2 条，J2 上限 6 之下两者暂时都不违规，但差值 2 会一直挂在那里。**仍未获追认 ⇒ 本包仍不得归档**（2026-08-22 复核）。已登记队列 §四 #80，按根 CLAUDE.md §5「答完之后由谁动手」判据不给选项、待总线派发。
- [x] 0.2 **定夺 design.md「已知边界 2」** —— ✅ **Shao Peishen 2026-08-22 拍板「复用 J4 词表判已闭合」**。落地＝`migration_verdict()` 三态（§11.1）：条目正文不含 `CLAUDE_PROGRESS_OPEN_ITEM_WORDS` 任一词 ⇒ 判「整条已闭合」，可迁而不要求队列行。🔴 **本项开的口子与 J4 方向相反，边界已写进三处载体**（lint 模块注释／各 CHANGELOG 本批注解／反例单测 `test_含未结的条目绝不被判可迁`）：J4 那份词表自己写着「只当筛子、不当判官——未命中不代表安全」，两者能并存**只因代价不对称**——J4 在写入侧、误放行即丢失；本判据在迁出侧、误判只是「搬去了 CHANGELOG 而不是队列行」，原文仍在、可 grep。**实测该词表不是空转**：它当场保住了 SC8 自己那条2026-08-21 注解点名的两行（`2026-06-24` 未结／`2026-07-06` 未接线），另拦下 3 行（待办／阻塞／暂不）。⚠️ **待迁基线由 31 改为 26**（`wecom-aibot-service` 2026-08-22 已瘦身，条目由 24 变 19），派单件明写「先重跑一次拿真数」，已照做。
- [x] 0.3 **裁定二期那条 §一 行并入 `#351` 还是另立** —— ✅ **两问都答掉了，结论是「都不」**。⑴ **二期本体不占 §一 行**：与一期同样走本变更包完成，`--enforce` 已切、存量已清零，无剩余工作需要一条 §一 行承接。⑵ **`#351` 是错的并入位**（实测）：它是 `append-row` 咽喉三修，触碰区是队列写入侧，与本包零交集——把二期挂上去只会让日后按 `#351` 去找 lint 的人找到一个不含它的行（同 §四 #87 那条「我用『同一个文件』这个没核过的理由说服他」的教训）。⑶ **真正剩下的活是 J6 存量（三份接力件超限 5-9 倍），已并入 §一 `#337`**——同一命题（R5 在根 CLAUDE.md 之外无覆盖）、同一做法（原文原样迁 CHANGELOG／归档件）、同一执行时机（顺带执行不单独排期）。**并入而非另立还有一个硬理由：实测机制类可动 WIP ＝ 22／上限 22，已满格，另立会被 `release` 当场拒绝**——协议〇.10「并入优先」在这里不是风格偏好，是唯一可行路径。

> ✅ **2026-08-22 复核：三项中 0.2／0.3 已闭合，0.1 仍未获追认 ⇒ 本包仍不得归档。**⚠️ 不得因「其余全绿」把 0.1 顺手划掉——它现在是一道硬门禁的判据基础，比一期时更要紧。
>
> ⚠️ 本包按派单件 `派单件-【CC】CLAUDE进度段lint实现-2026-08-21.md` 的规格实现并已提交，规格来源＝ Shao Peishen 委托产出的 OP-0821-B 报告 §六。0.1／0.2 是**实现过程中规格未定死或被实测推翻**的部分，须追认，不是「实现前该问而没问」的部分；0.3 是派单前就已挂在 §四 #80 上的存量待裁定项。
>
> 🔴 **本节曾把这三项写成「两项 ＋ 一段游离的『原…裁定』文字」，收工报告抬头也据此写了「本报告 2 项定夺」**，2026-08-21 由 Shao Peishen 当场指出（原话：「有两个『定夺项』加一个『另』好像就是三个」）后更正。**教训：同一批待定夺项一旦分成两种写法，条数就不再可数**——「需你定夺」清单的编号必须覆盖全部需他答的事，任何挂在清单之外的「另：…」都等于把计数交回给他去做，与该格式规则（根 CLAUDE.md §5「会话末显式罗列决策项」格式四条）的设立目的正相反。

## 1. 前置核实

- [x] 1.1 实测规格给的裸正则在当前 master 根 CLAUDE.md 上的命中数，与规格自称的真值比对（结果：裸正则 4 条 vs 真值 2 条，差异来源已逐行定位，见 design 决策点 1）
- [x] 1.2 实测确认 `工具-落库sweep.py` 全文「lint」命中 0 —— 本项目 lint 一律在 CI，不挂 sweep
- [x] 1.3 `git check-ignore -v CLAUDE.md.editlock CLAUDE.md.editlock.snapshot` 实跑，贴回 proposal（命中 `.gitignore:62:*.editlock*`，退出码 0）
- [x] 1.4 实测各场景 CLAUDE.md 的顶部段结构，确认「不要假设统一结构」这条规格判断成立（结果：SC8 型另捞到 QD-B 与 wecom-aibot-service 两份；FI1/FI2/QD-A/SC1/SC2 五份结构不同）

## 2. 条目切分器（唯一权威实现）

- [x] 2.1 `工具-共享文档编辑锁.py` 新增 `_claude_progress_entries(text)`：顶部段界定 → 区间界定 → 两种前缀 ＋ 日期模式判定，返回 `[(行号, 正文)]`
- [x] 2.2 新增四个锚点常量（`CLAUDE_PROGRESS_HEADER_RE` / `_POINTER_RE` / `_ENTRY_RE` / `_DATE_RE`），并在模块 docstring 记录 J4 的成因与边界
- [x] 2.3 单测：🔴 前缀必须被数到／📦 之后的元说明不算条目／无头行返回空表不猜

## 3. J1 · 承接载体安全阀

- [x] 3.1 `工具-CLAUDE进度段lint.py` 新增 `load_queue_row_ids(repo_root)`：按 `queue_table.iter_queue_paths()` **逐份解析后合并**，缺失文件告警不静默降级
- [x] 3.2 新增 `carrier_of(body, repo_root, queue_ids)`：⑴ 行号存在性检查；⑵ 真实路径 ＋ 60 字符窗口内的章节号
- [x] 3.3 J2 超限输出中，无载体的条目**只说「请先立队列行再迁」、绝不说「请迁移」**
- [x] 3.4 反例单测：**拼接两份队列文本再解析会丢掉第一份的 §一**（锁死「逐份解析后合并」这个实现选择，此坑 2026-08-19 `#312` 已真实发生过一次）
- [x] 3.5 单测：业务场景文件里的行号同样有效／不存在的行号不算载体／路径不存在不算载体／章节号离路径太远不算载体

## 4. J2 / J3

- [x] 4.1 `check_entry_count`（上限默认 6，`--count-cap` 可覆盖），超限时对最早 N-cap 条逐条跑 J1
- [x] 4.2 `check_entry_length`（上限默认 1,200 字符，`--length-cap` 可覆盖）
- [x] 4.3 单测：未超限不报／超限点名待迁条数／单条超长报 J3／**按字符数不按字节数**

## 5. J4 · 写入侧拦截（编辑锁 release）

- [x] 5.1 新增 `_validate_claude_progress_open_item(current, snapshot)`，返回 `(违规列表, 逃生阀留痕列表)`；新增判定按**正文**比对而非行号
- [x] 5.2 未闭合词表（20 个说法，含规格实测补入的「未结」「未接线」）
- [x] 5.3 逃生阀 `进度豁免：<理由>`：有理由放行并打印留痕；**空豁免仍拒绝**
- [x] 5.4 新增 `_is_claude_progress_target`：绝对/相对路径归一化后比较，不只比字面量（`_is_queue_system_target` 的既有教训）
- [x] 5.5 `cmd_release` 新增分支；放行时把逃生阀理由追加进写回的 `history`
- [x] 5.6 单测 14 条（含派单件点名的反例：**含「未结」的条目必须触发 J4**；以及「在上方插入新条不误判既有条目」「非 CLAUDE 目标不跑本判据」「绝对路径同样被识别」）

## 6. J5 · 场景级

- [x] 6.1 结构 A / 结构 B 两种白名单签名识别；两者都不命中即报「未支持的结构」并列名
- [x] 6.2 结构 B 的条目正文取「状态」列（不含日期列）
- [x] 6.3 单测：表格型被识别并套用 J2/J3／条目正文不含日期列／未支持结构被列名

## 7. CI 接线（一期）

- [x] 7.1 `.github/workflows/ci.yml` 新增 job `claude-progress-lint`，与 `bootstrap-stub-lint` 同构
- [x] 7.2 **一期 `continue-on-error: true` ＋ 脚本默认告警模式**，并在 job 注释里写明「二期切 `--enforce` 时须同时删掉这行」
- [x] 7.3 YAML 解析实测通过，job 清单确认为 8 个

## 8. 验收

- [x] 8.1 对当前 master 跑 `--stats`：根 CLAUDE.md 条目数 **＝ 2**（与规格声明的当日真值一致），两条各 3,131／3,066 字符
- [x] 8.2 三条反例单测齐备（🔴 前缀被数到／拼接解析会丢行／「未结」触发 J4）
- [x] 8.3 `0-学习与工具` 全量回归零回归
- [x] 8.4 `openspec validate --all --strict` 通过
- [x] 8.5 输出二期工作量基线：**3 个文件违反 J2（合计需迁出 31 条）／15 条违反 J3／5 个文件结构未支持**

## 9. 收工（一期）

- [x] 9.1 commit + push
- [x] 9.2 回写队列 §四 `#80`
- [x] 9.3 收工重跑文档台账

---

# 二期（2026-08-22，OP-0822-D）

> **续用本包，不新建**（派单件明写）。二期四件 ＝ ⑴ 切 `--enforce` ／ ⑵ 存量迁出
> ／ ⑶ 接力件进 lint 范围（J6，新增）／ ⑷ 开场预算检查（J7，新增）。
>
> 🔴 **二期最该带走的一条认识（派单件原话）**：J1-J5 只管顶部进度段，而根 `CLAUDE.md`
> 的大头根本不在那里——2026-08-22 分段实测 §5 工作流占 59%、顶部进度段只占 16%。
> **前三次瘦身动的全是那 16%，所以「瘦了又涨」是必然的，不是执行力问题。** §5 那一块
> 由 OP-0822-B（Cowork）处理，不在本包范围；本包新增的 J7 是唯一一条直接度量
> 「开场要读多少字节」的判据，正是为了让这类「治错部位」下次能当场被看见。

## 10. 二期 ⑴ · 切 `--enforce`

- [x] 10.1 CI job `claude-progress-lint` 的命令加 `--enforce`
- [x] 10.2 **同批删掉 `continue-on-error: true`**（一期已把这条写进 job 注释）
      —— 🔴 只改一半的两种形态都表现为「门禁看起来装上了」：只加 `--enforce` 而留着
      `continue-on-error`，job 照常显示成功；只删 `continue-on-error` 而不加
      `--enforce`，脚本默认告警模式退出码恒 0
- [x] 10.3 **用 YAML 解析器复核，不用字符串匹配**（首次反查用 `"continue-on-error"
      not in job` 误报——命中的是新注释里提到这个词的那句话，YAML 键其实已删干净；
      **过严的反查与过松的反查一样会给出错误结论**）
- [x] 10.4 实跑 `--enforce` 退出码 ＝ 0；CI job 清单仍为 8 个

## 11. 二期 ⑵ · 存量迁出与 J3 收口

- [x] 11.1 J1 三态判定 `migration_verdict()`：有载体 ／ 无载体但已闭合 ／ 无载体且含
      未闭合措辞。**第三档绝不输出「可迁」**
- [x] 11.2 `open_item_hits()` 复用 `editlock.CLAUDE_PROGRESS_OPEN_ITEM_WORDS`，
      本文件不另立一份词表
- [x] 11.3 **迁前重测基线**（派单件硬要求）：31 → **26**（QD-B 14→6、SC8 11→6、
      wecom-aibot-service **19**→6）；J3 由 15 → **8**
- [x] 11.4 26 条原文原样迁出：QD-B 8 条／SC8 5 条／wecom 13 条，**一个字未改写**
- [x] 11.5 新建 QD-B ／ wecom-aibot-service 两份同目录 `CHANGELOG.md`
      （frontmatter 照抄 SC8 那份既有件）
- [x] 11.6 **迁哪几条由 J1 判定决定，不由「最早 N 条」机械决定**：SC8 五条 blocked
      全部原地留下（其中三条正是 SC8 自己 2026-08-21 注解点名「迁走即丢」的）,
      故改迁 `2026-08-18`／`2026-08-20` 两条有载体的新行
- [x] 11.7 8 条 J3 超长收口：5 条压缩（根 CLAUDE.md 2／QD-B 2／SC8 `**当前**` 1）、
      3 条随迁出消失；**压缩前原文全文存档，可 grep**
- [x] 11.8 三份场景文件的迁移注解补／更新 —— SC8 那份 2026-08-21 的注解已作废
      （它写「8 行不得迁走」，实测其中 3 行零未闭合措辞、本次已迁）
- [x] 11.9 迁出全程守编辑锁：根 `CLAUDE.md` 两次 acquire/release，J4
      `_validate_claude_progress_open_item` 对改写后的两条实跑 **零违规**
- [x] 11.10 落盘后扫控制字符与 U+FFFD：8 份改动件全部 0／0

## 12. 二期 ⑶ · J6 接力件定长交接卡

- [x] 12.1 `check_handoff_cards()`：字节 ≤ `--handoff-byte-cap`（默认 8,192）
- [x] 12.2 零 `## 【20xx-xx-xx` 形态日期节；**判据刻意只认全角方括号那一种形态**
      ——合规卡本身就有 `## 二、当前状态快照（2026-08-22）`，放宽即误伤
- [x] 12.3 **按字节不按字符**（R5 写的是 8 KB；这些文件几乎全是中文，
      按字符数判会得出一个宽约 3 倍的假上限）
- [x] 12.4 **用 glob 不用硬编码四个路径**，归档件（文件名含 `-归档-`）排除
- [x] 12.5 J6 结果进 warnings 不进 violations（存量三份超限 5-9 倍，
      一并硬拦等于把 `--enforce` 本身再推迟一轮）
- [x] 12.6 单测 7 条
- [x] 12.7 **实测发现：R5 改版实际只改了四份中的一份** —— `session接力-Phase1收口.md`
      8,113 B 合规，另三份 72,482／62,257／46,529 B。**存量清理已并入 §一 `#337`**

## 13. 二期 ⑷ · J7 开场预算

- [x] 13.1 `opening_budget()` ／ `check_opening_budget()`，`--budget-cap` 默认 120,000
- [x] 13.2 **无论超没超都回显** —— 只在超限时才打印，等于把「现在离上限还有多远」
      这个唯一能让人在越界前收手的信号藏起来
- [x] 13.3 缺件以 `-1` 显式标注、不按 0 混入合计（「工具静默回退」同族）
- [x] 13.4 **队列刻意不计入预算**：它 2026-08-06 起改由只读 CLI 按行号查（§一 #268），
      开场读入量 ≈ 0；按文件字节计会得出一个吓人却毫无意义的数
- [x] 13.5 单测 4 条

## 14. 二期收工

- [x] 14.1 `0-学习与工具` 全量回归零回归
- [x] 14.2 `openspec validate --all --strict` 通过
- [x] 14.3 commit + push，ff 入 master
- [x] 14.4 回写队列 §四 `#80`、§一 `#337`（并入 ＋ SC8 半边回填）、§二 批次行
- [x] 14.5 收工重跑文档台账
- [ ] 14.6 `/opsx:archive` —— 🔴 **卡在 §0.1 未获追认，本次不归档**
