# followup-letter-state-single-source Tasks

## 0. 待定夺（两项，本包不得归档）

**两项动手的都不是本 session ⇒ 按根 CLAUDE.md §5「答完之后由谁动手」判据，一律不给选项、只登记待总线派发。**

- [ ] 0.1 **追认 design 决策点 2**：编辑锁串行闸由「只认 `📥`」改为「闭环四态」。**这是本包唯一一处改变既有对外语义的改动**，也是唯一一处**放宽**（三种状态由拦变放）。派单件没写这一层，因为写它的人当时不知道机器判据与 README 正文已分裂 3 天。**已登记 §四**，待总线派发追认。
- [ ] 0.2 **裁定 `❌ 已作废` 判为闭环的防滥用问题**：它在语义上正确（作废的信不再等回件），但同时是一条可被滥用的捷径——不想等回件就把前信标作废。本包**不加防滥用判据**（那需要「作废须带理由 ＋ 谁批准」一整套，属 S5 范围），只记录风险。

## 1. 前置取证（全部实测，非推断）

- [x] 1.1 实测 README 49 行的状态写法分布，确认闭环四态判据对**真实装饰**（markdown 粗体／全角空格分段／括注）全部成立，未知类命中 0
- [x] 1.2 实测编号列与状态列矛盾的存量：**2 处**（`采购部（未发，不编号）` ＋ `IT部#7（待发，暂不占号）`），与派单件 §一 预期一致
- [x] 1.3 实测 S2 判据窗口标定：0 字窗口漏掉全部 5 处；40 字窗口误卷 18 处事后陈述；**8/8 恰好命中且仅命中那 5 处**
- [x] 1.4 实测 5 处的分区归属 —— **§二 一处都没有**，派单件 §三 的归属说明被推翻（见 design「被实测推翻的规格假设 ⑴」）
- [x] 1.5 实测入信文件名 ↔ README 目标文件的配对率，标定「专员常附 `-回复` 尾缀」这条归一化规则
- [x] 1.6 `git check-ignore -v` 实跑三个 README 锁文件形态，结果贴回 proposal

## 2. 判据权威化（`zhuopin_platform.shared_tools.followup_gate`）

- [x] 2.1 闭环四态 ＋ 在途五态 ＋ 第九态 ＋ `classify_status` 四分类（含刻意保留的 `unknown`）
- [x] 2.2 `normalize_status`：剥 markdown 星号与首尾空白/全角空格，整串前缀比对，**不按 emoji 判**（`📨 已确认闭环` 与 `📨 回件已到，待拆件` 同以 📨 开头）
- [x] 2.3 编号列：`parse_letter_number`（容忍括注）／`number_status_mismatch`
- [x] 2.4 入信文件名 ↔ 原信配对：`extract_reply_source_stem`／`normalize_letter_stem`／`reply_matches_letter`（逐字相等，**不做包含/编辑距离/最相似**）
- [x] 2.5 `find_unsynced_letters` 纯配对函数（**不解析文件**，两个消费者各自喂已解析好的行）
- [x] 2.6 单测 27 条，状态串与文件名**全部取自真实数据**

## 3. S3 · 只读闸查询 CLI

- [x] 3.1 `0-学习与工具/工具-跟进闸查询.py`：`--to` / `--all` / `--json`
- [x] 3.2 README 解析复用 `aibot_service.readme_table.iter_rows`（派单件明写不得自写正则）
- [x] 3.3 `REPO_ROOT` 取编辑锁算好的主工作区值，**不用本 worktree 的旧快照**（队列 #314①）
- [x] 3.4 「最近一封」按表格顺序、「下一个可用号」只看下表——不读 README 顶部那段已连续失真四次的自由文本
- [x] 3.5 退出码：闸开/闸锁均 0；解析失败/收信人不存在 2（**闸锁不是错误**）
- [x] 3.6 入信行逐份解析后合并；队列文件缺失 fail-loud；`IT部`→`IT` 目录别名
- [x] 3.7 人读与 `--json` 由同一个 `GateReport` 渲染
- [x] 3.8 单测 15 条

## 4. S2 · 队列禁止复述信状态（一期 `--warn`）

- [x] 4.1 `工具-队列结构lint.py` 新增 `followup_restate_warnings()`，窗口 8/8
- [x] 4.2 §二 完全不扫；§一 `[S:done]` 豁免但计数；两类都打印
- [x] 4.3 不计入 `lint()`、不改退出码；打印「二期基线」两个数
- [x] 4.4 `check_queue_table_importable` 扩至断言 `followup_gate` 可 import 及其关键符号
- [x] 4.5 单测 9 条（含反例单测⑵：§二 批次行不得误报；事后陈述不得误报）

## 5. S4 桥二 · 回灌完成强制转态

- [x] 5.1 `_validate_followup_reply_state_sync` ＋ `_followup_intake_records` ＋ `_followup_letter_records`
- [x] 5.2 挂**队列系统** `release`（不挂 README release，理由见 design 决策点 3）
- [x] 5.3 逃生阀 `转态豁免：` **只认本次持锁 note ＋ 本次触碰行**，不认队列全文（design 决策点 4；已配反例单测锁死）
- [x] 5.4 权威模块不可用时 fail-loud 打印后跳过
- [x] 5.5 串行闸闭环判据改四态，`FOLLOWUP_SERIAL_CLOSED_PREFIXES` 从权威模块取
- [x] 5.6 单测 12 条（含双文件合并、第九态不算闭环、文本反馈不误拦）

## 6. S4 桥一 · 回件到达即打第九态

- [x] 6.1 `aibot_service/followup_readme_bridge.py`
- [x] 6.2 接线 `connection.py::on_message`（**不放 `intake.py`**，理由见 design 决策点 5）
- [x] 6.3 走既有 `SubprocessQueueEditLock`；锁目标用仓库相对正斜杠路径（否则 README 专属 release 校验静默不跑）
- [x] 6.4 指数退避重试 3 次；用尽即审计 ＋ WARN ＋ 告警，**不新建第三个暂存文件**（design 决策点 6）
- [x] 6.5 匹配不上 ⇒ 不动 README、不抢锁、打 WARN、记审计
- [x] 6.6 幂等；持锁后**重读重定位**再写
- [x] 6.7 原状态原样保留（design 决策点 7）
- [x] 6.8 单测 17 条（含反例单测⑶）

## 7. 验收

- [x] 7.1 `--all` 覆盖全部 5 位收信人，无遗漏无重复
- [x] 7.2 对生产真身跑通，**当场照出一条真实漂移**：唐燕萍 §一 #369/#370 入信已到、README 未转态
- [x] 7.3 桥二全量扫描存量：**2 封不同步**（`财务部#11` 自 2026-08-07 起漂了 14 天；`采购部（未发，不编号）` 同日）
- [x] 7.4 S2 一期基线：**活行 3 处**（§四 #65／§一 #264／§一 #361），另 2 处 §一 `[S:done]` 历史豁免
- [x] 7.5 全量回归零回归
- [x] 7.6 `openspec validate --all --strict` 通过

## 8. 未做，写明为何（不假装完工）

- [ ] 8.1 **S5 未做**（状态列机器专有 ＋ `set_letter_state.py`）——拍板 (b) 的既定范围到 S4 为止，非遗漏。⇒ **README 状态列此刻仍可被人手改**。
- [x] 8.2 ✅ **已证伪并转勾（2026-09-01，`OP-0901-A` 审计实证）——桥一在生产上已成功打标 8 次**：`followup_readme_bridge_marked` 逐条为 `财务部#15`(08-24 10:26)／`IT部#9`(08-24 13:06)／`IT部#10`(08-24 18:00)／`质量部#9`(08-25 11:13)／`采购部#18`(08-26 08:16)／`质量部#10`(08-28 09:22)／`采购部#19`(08-28 12:09)／`采购部#20`(08-31 15:34)，两种配对通道（`latest`／`stem`）均实际走通。08-31 那次的完整生产流水线为 `archived`→`queue_appended`→`followup_readme_bridge_marked`→`queue_sync_pushed`→`group_notified`→`inbound_forwarded_to_paul`，且 README `采购部#20` 行的自动标记时刻 `2026-08-31T07:34:14Z` 与审计 `15:34:14` 本地**逐秒吻合**。**下方原文所依据的「落后 246 个提交」是 2026-08-19 的快照，其后该 worktree 已被同步（2026-09-01 实测只落后 39 提交，且这 39 个里没有一个碰过 `followup_readme_bridge.py`）**，桥一因此早已生效。
  🔴 **本条留一处方法教训，因为它险些导致相反的运维决策**：`OP-0901-A` 曾连续两轮以 `reply_arrived` 全历史 0 条为据，断言「桥一从未触发过」，并据此建议保留拆件巡逻、宣称「投档驱动的地基不成立」。**实际 `reply_arrived` 根本不是桥一的 action 名**——真名是 `followup_readme_bridge_marked`。⇒ **拿一个自己假设的键名去查、查不到就当作「那件事没发生」，是「某载体里没有 X ≠ X 不存在」的又一实例**；正确做法是**先列出该载体里实际存在的 action 名集合，再从中找**，而不是先设想一个名字去命中。**同一轮里 README 那行「📨 …（企微机器人自动标记）」已被读到并引用过，却因先入为主的结论而未被认出是反证。**
  ⚠️ **仍未闭合的半步（不随本条转勾）**：桥一只负责**打标**（`✅ 已推送`→`📨 回件已到，待拆件`），**拆件与回灌本身仍是人工派活**（`采购部#20` 那次由 `OP-0831-M` 执行）。⇒ 若下线拆件巡逻，须另有承接方去发现「README 上有 `📨` 行在等着」，否则回件会静默停在打标态。见 §一 待立行。
  ━━━ **原文（2026-08-19 记，已被上方实证证伪，按「历史记录不追改」保留）** ━━━ **桥一未在生产常驻服务上真实触发过**。它已按单测跑通全部分支，但生产服务跑在 worktree `ops/wecom-service-home`，且该载体 2026-08-19 实测**落后 master 246 个提交**（§四 #68）。⇒ **本包合入 master ≠ 桥一在跑**。这正是 OP-0819-F 那条教训的形状：`#82` 建成 9 天、每天在跑，却从未真实发出过一条消息。**不得据本包宣称桥一已生效。**
- [ ] 8.3 **桥二存量 2 封未清**——`release` 硬拦已上线，这 2 封会拦住下一个 release 队列的人。清理须改 README 状态列（有明确闭环依据，见 §一 #291/#293 状态列全文），**动作在主工作区、归拆件/巡逻线**，本 session 未擅自代办。
- [ ] 8.4 **S2 未切 `--enforce`**——一期只 warn，二期须先清 3 处活行。
