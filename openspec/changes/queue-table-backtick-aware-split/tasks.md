## 1. `queue_table` 权威模块新增能力

- [ ] 1.1 在 `zhuopin_platform.shared_tools.queue_table` 新增 `split_row_cells(line)`，复用 `BACKTICK_SPAN_RE`（`` `[^`]*` ``）同款正则识别反引号跨度，反引号内的 `|` 不作为列分隔符；行不以 `|` 开头返回 `None`；行不以 `|` 结尾仍正常返回切出的单元格列表（不静默丢弃，见队列 #314① 教训）。
- [ ] 1.2 新增 `parse_section_rows(section_text, section)`，逐行调用 `split_row_cells`，跳过表头/分隔行，复用 `column_count_ok` 完成列数判定，返回 `(原始行文本, 单元格列表, 列数是否符合布尔值)` 三元组列表。
- [ ] 1.3 单测覆盖：反引号内单个/多个竖线、反引号未闭合、整行被反引号包裹、行首无竖线、行尾被截断（对照队列 #313 真实破损文本）、`parse_section_rows` 对混合正常/异常行的分类结果。

## 2. 消费者迁移（保留函数名与签名，只换内部实现）

- [ ] 2.1 `工具-共享文档编辑锁.py::_table_data_rows` 改为委托 `queue_table.split_row_cells`（编辑锁一贯"原样返回交调用方判"的既有策略不变）。
- [ ] 2.2 `工具-队列查询.py::_table_data_rows` 同款迁移。
- [ ] 2.3 `工具-落库sweep.py::_parse_section_one`/`_parse_section_two` 迁移；显式验证"列数校验为 False 时仍按其既有'静默跳过'策略处理"这一行为未被新入口改变（design.md Risks 已标注这一落差点，不能假设自动成立）。
- [ ] 2.4 `工具-队列结构lint.py`（经由复用编辑锁的 `_table_data_rows`）验证其两项既有检查（列数／§二状态列格式）结果不受影响，不需要改动源码。

## 3. 全量回归与真实数据验证

- [ ] 3.1 4 处消费者各自既有测试套件全量跑通，零漂移（含队列 #314① 已新增的、基于 `git show 298c152` 真实历史行的回归用例——确认其断言在新实现下仍成立）。
- [ ] 3.2 对当前生产队列文件（`跨桌任务队列.md`）跑一次新旧切列结果 diff：逐行比对"迁移前 vs 迁移后"的单元格数量，任何变化的行需人工过目确认是修复（反引号内竖线不再误判）还是异常（不应出现，出现即说明实现有误）。
- [ ] 3.3 确认队列 #313／#267（若彼时仍处于队列 #314 修复前状态）两行的真实历史破损文本，经新实现处理后列数校验结果符合预期（真正的结构损坏——如结尾被截断——仍应被正确捕获，不能因为"反引号感知"而连真实破损也一并放过）。

## 4. 收尾

- [ ] 4.1 `openspec validate queue-table-backtick-aware-split --strict` 与 `openspec validate --all --strict` 均通过。
- [ ] 4.2 提交前确认本变更未在同一批次里静默改变 `has_bare_pipe`/`escape_bare_pipe` 的既有行为（design.md Non-Goals 已声明不改，需实测复核而非只读代码确认）。
- [ ] 4.3 完工回写队列对应行；变更包按"完工即归档纪律"跑 `/opsx:archive`。
