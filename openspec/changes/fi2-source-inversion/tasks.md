# fi2-source-inversion Tasks

> 🔴 **本包目前只到 propose ＋ design，全部任务未勾。** apply 须等 Shao Peishen 审过 design，且 §0 的硬前置解除后方可开工（派单件 `OP-0821-K` §五）。
> 📌 每一段的排序不是随意的：**0 段不过，后面全部不开工。**

## 0. 硬前置（未解除不得开工，逐条实测确认，不得凭书面答复销项）

- [ ] 0.1 **F1 · 探测项 P1**：实跑 `get_ap_lines(<真实AP单号>)` 并打印首行**全部键名**，确认返回的 JSON 行里存在 `IsAP`（或语义等价的立账标志字段）。🔴 **不成立则本包整体不可实现**，转为向陈承提 IT 缺口（`IT部#N`），本包停在 design。
- [ ] 0.2 **探测项 P2/P3**：同一次打印中确认「单据类型」与「AP 单制单人」两个字段是否存在。缺 P3 ⇒ 表2 一个业务方指定死的字段拿不到，须回问她替代口径。
- [ ] 0.3 **探测项 P4**：用**非法哨兵值**试 `AP/Query` 的 `isAP` 参数，判断是「报错（参数存在）」还是「静默返回全表（参数不存在且不报错）」。⚠️ 该端点已有 `POChange/Query` 式静默全表的先例，**不得以「没报错」推断参数生效**。
- [ ] 0.4 **探测项 P5**：取一张已知未立账 AP，看其 `AccrueDate` 是否为空。🔴 若为空，则 `dateFrom`/`dateTo`（按立账日期过滤）**会把要找的单全部排除且返回 0 行不报错** ⇒ 期间过滤须改用别的日期字段。
- [ ] 0.5 **F2 · 决策点 4 的范围矛盾已由唐燕萍裁定**（`财务部#15` Q2：跨月未立账 AP 走 (a)/(b)/(c) 哪一种）。未裁定前 MUST NOT 按我方推荐直接实现——它是账务口径，不是技术选择。
- [ ] 0.6 **F3 · 队列 `§一 #371`（P1 发票行级去重）已修复并重跑验证通过**。🔴 未修即 apply，等于每天把「你的单子对不上账」这个错误结论自动推给一批采购部同事。
- [ ] 0.7 **探测项 P7**：按 0.5 裁定的读法实跑一次入选查询（**只计数、不产出、不推送**），与 design 决策点 3.3 的估算（约 100 张 AP 单 / 500–600 明细行，上界 180 张）比对。偏差超 1 个数量级 ⇒ **回头改 design，不得硬跑**。
- [ ] 0.8 触碰区复查：开工当刻复查 `§一 #371` 与 `§一 #282` 状态，确认无他人在办 FI2 摄取通道与通知通道；有则先协调。

## 1. 取数层（平台连接器）

- [ ] 1.1 `ZpConnector` 新增未立账 AP 取数方法（复用既有 `_fi_query_paginated`，不新建请求原语）。过滤位置（服务端 or 客户端）按 0.3/0.4 的实测结果定，**并在方法 docstring 里写明是哪一种以及为什么**。
- [ ] 1.2 连接器层单测：分页耗尽、零结果、传输失败重试、HTTP 错误一次即抛 —— 四条既有语义的回归。
- [ ] 1.3 **零结果 MUST 与失败同等对待**的判据落在场景层而非连接器层（连接器返回空列表是合法的），在 1.2 里只确认连接器如实返回空。

## 2. 入选层（新建 `fi2/ap_intake.py`）

- [ ] 2.1 入选集合计算：未立账 ＋ 非预付款 ＋ 会计期间（按 0.5 裁定口径）＋ 按 AP 单号去重。
- [ ] 2.2 `ap_first_seen.json` 台账：只增不改；同日重复运行幂等（配单测，断言两次运行后文件**逐字节一致**）。
- [ ] 2.3 立账后条目移除 ＋ 写审计 `ap_first_seen_evicted`。**不实现按龄自动清理**（会让滞留最久的单静默变新）。
- [ ] 2.4 台账缺失/损坏时显式报告并要求重建；**MUST NOT 用当日日期静默填充**（配单测）。
- [ ] 2.5 日期取值：本机本地日期，**每次写入时重取**（不沿用进程启动时的缓存值，根 `CLAUDE.md` §5 硬规则第二次补齐）。
- [ ] 2.6 **零结果防线**：入选 0 张 ⇒ 不产出 Excel、不发通知、经 `WECOM_WEBHOOK_URL_OPS` 告警、写审计 `ap_intake_empty`（配单测）。
- [ ] 2.7 部分失败（PO/GR 逐单查询有失败）⇒ 整批中止、告警、**保留前一日产物不覆盖**（配单测）。
- [ ] 2.8 审计事件 `ap_intake_pulled`（入选张数/期间/成功与否）。

## 3. 判定层（`models.py` / `result_classify.py` / `recon_report.py`）

- [ ] 3.1 `models.py`：`ItemMatch` 增 `first_seen_date`；新增分类常量与状态常量 `l0_pending_invoice`。
- [ ] 3.2 `result_classify.py`：`无发票支撑` 细分为 `待票` / `长期待票`；阈值 N 从 `config.py` 读取（**新增 `FI2_PENDING_INVOICE_WORKDAYS`，strawman ＝ 5**）。
- [ ] 3.3 `recon_report.py`：`待票` → `l0_pending_invoice`，不进 `needs_review`、不进未通过清单、不触发点名；`长期待票` → `needs_review`。
- [ ] 3.4 R5 门禁范围补一句「`长期待票`/`待票` 不适用」，**门禁算法与阈值一字不改**。
- [ ] 3.5 🔴 **判据零漂移验证**：`match_engine.py` / `price_check.py` / `config.py` 既有容差常量 **`git diff` 必须为空**（除 3.2 新增的一个阈值常量外）。
- [ ] 3.6 🔴 **回归判据**：同一批 AP 单号分别走旧路径（`--ap-doc-nos`）与新路径（未立账驱动后过滤到同一批），`classification` 字段**逐单逐料品完全一致**（沿用 design D19-d 零漂移验证方式）。
- [ ] 3.7 mock 夹具补四象限样本：未立账×有票 / 未立账×无票未超期 / 未立账×无票已超期 / 已立账（应被排除）。
- [ ] 3.8 黄金基准不漂移（`tests/test_golden.py` 全绿，合成 golden 一个字节不改）。

## 4. 产出层（新建 `fi2/daily_workbook.py`）

- [ ] 4.1 《三单待核对明细表YYYYMMDD.xlsx》：字段按 design 决策点 7 的最小集（**待 Q3 回件后可能调整**）。
- [ ] 4.2 《三单核对结果汇总表YYYYMMDD.xlsx》：两张 sheet，名称与字段**逐字**按业务方指定（`核对通过明细表` / `核对未通过明细表`）。
- [ ] 4.3 未通过原因映射按 design §三 表；多重原因**同行并列，不取其一、不拆行**。
- [ ] 4.4 字段不可得（如制单人缺失）⇒ **保留该列并如实标注不可得**，MUST NOT 删列、MUST NOT 以其他字段顶替。
- [ ] 4.5 落点 `reports/`；**MUST NOT 新建 `data/` 下日产物目录**（design/proposal 已给出 `git check-ignore` 实测反例）。
- [ ] 4.6 落盘后 `git status --porcelain` 复验：两份产物**不出现在未跟踪清单**（防止 sweep 孤儿脏文件告警每日再生）。
- [ ] 4.7 审计事件 `workbook_generated`（两份表行数）。

## 5. 编排与定时（新建两个入口 ＋ 两个注册脚本）

- [ ] 5.1 `scripts/run_ap_intake_9am.py`（步骤一）。引导代码用 `zhuopin_platform.bootstrap.ensure_paths` **唯一样板**，服务/CLI 入口**不带 `strict=True`**（队列 #345）。
- [ ] 5.2 `scripts/run_ap_recon_10am.py`（步骤二＋三）。🔴 **核对前内联跑一次 `tax_export_scan.scan_once()`**，不依赖独立定时任务先跑完（design What Changes ⑥ 的时序竞态）。
- [ ] 5.3 步骤二 **复用步骤一落下的入选清单，不重新拉 AP**（重拉会让两步看到不同的世界）。
- [ ] 5.4 两个 `register-*-task.ps1`，`.51` 计划任务、工作日触发。🔴 **必须存 UTF-8 BOM**（根 `CLAUDE.md` 坑 5；FI2 自己 2026-08-10 已踩过一次，commit `75d7116` 补的 BOM）。
- [ ] 5.5 既有 `Fi2TaxExportDailyScan`（10:30）**保留不动**（它另行承担源头断供检测）。
- [ ] 5.6 🔴 `sync-to-server.ps1` **排除 `data/ap_first_seen.json`**，并**实跑一次部署前后台账逐字节比对**核验（不靠读脚本推断）。
- [ ] 5.7 回滚 SOP：两个任务的 `schtasks /End` + `/Delete` 命令写进场景 `CLAUDE.md` 部署段。

## 6. 面板（仅在 design 审确认纳入本包时才做）

- [ ] 6.1 面板如实展示 `待票` 计数与所属 AP 单（不因中性态而省略）。
- [ ] 6.2 ⏸ 「按 AP 单号查询」入口 —— **待 design 审的 Open Question 3 定**；若采纳「删面板链接、靠 Excel 附件承载」（推荐 (b)），本项不做。

## 7. 推送（前置未解除则整段不做，且不得降级）

- [ ] 7.1 ⏸ 前置 ①：企微群「AP单三单核对群」已由 Shao Peishen 建成。
- [ ] 7.2 ⏸ 前置 ②：该群 chatid 已按既有方式采集（群内发一条带标签的测试消息，经 `intake.py` 审计事件取 chatid），登记进 `department_group_chatid_mapping.yaml`。
- [ ] 7.3 ⏸ 前置 ③：跨机通路（design 决策点 8.2 的 ⑵ outbox 方案）已定并验通；含探测项 P6（`.51` 写、笔记本读）。
- [ ] 7.4 实现 outbox 落盘（`reports/fi2_group_outbox.jsonl`，已被 `reports/` 与 `**/*.jsonl` 双重覆盖）＋ 常驻服务侧轮询代发。
- [ ] 7.5 点名采用**纯文本列姓名**降级方案；**MUST NOT 由姓名推断 userid**（本组织已知存在纯数字 userid）。同人只列一次。
- [ ] 7.6 跳过推送必写审计 `group_notify_skipped`；推送失败降级记录、不改退出码。
- [ ] 7.7 🔴 **第一次真实运行 MUST 关闭推送**——先把两份表给唐燕萍看一眼确认格式与内容，再开推送。新受众（采购部）的第一条消息质量决定这个工具在他们眼里是什么。
- [ ] 7.8 🔴 **MUST NOT 为本包新起任何业务群 webhook**——前置不通就不发，不降级（`#282` 拍板）。

## 8. 收口

- [ ] 8.1 FI2 全量测试绿 + 零回归 + 黄金基准不漂移。
- [ ] 8.2 `openspec validate --all --strict` 全绿。
- [ ] 8.3 ⚠️ 与 `fi2-recon-mvp` 的 spec 交接核对：该包归档时会对 `fi2-feed-source`/`fi2-result-classify`/`fi2-recon-report` 二次 sync，**须人工核一次本包 delta 未被覆盖回去**（design 决策点 1 的遗留风险）。
- [ ] 8.4 场景 `CLAUDE.md` 更新（新驱动模式、两份产物、两个定时任务、回滚 SOP、部署段台账排除）。
- [ ] 8.5 部署 `.51` ＋ 冒烟（`/api/ping` 200、关键页 200、一次全量重算），且**在笔记本外部验一次**（根 `CLAUDE.md` 坑 5：只在 `.51` 本机冒烟 200 是假象）。
- [ ] 8.6 发布收口后**先查串行闸**（`python 0-学习与工具/工具-跟进闸查询.py --to 唐燕萍`）再决定起不起草跟进信；闸锁则只在队列行登记拟发要点，**不起草**（根 `CLAUDE.md` §5 场景固定流程第 8 步 2026-08-20 改定口径）。
- [ ] 8.7 `/opsx:archive` ＋ commit + push ＋ 收工重跑台账。
