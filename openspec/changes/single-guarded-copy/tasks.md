# single-guarded-copy Tasks

> 🔴 **本清单在 design 审通过前一条都不得动手。** design 三个决策点全部待 Shao Peishen 拍板。

## 0. 前置闸（design 审后、动手前）

- [ ] 0.1 确认 design 决策点 ①②③（含 ③-1／③-2）**均已拍板**，把答案逐条回填 `design.md` 对应节（写明拍板日期与手段）。
- [ ] 0.2 核 `cc-hooks-p3` 当前状态（是否已 apply、形态编号是否已占用 ⑧）——**propose 期未核，此处必须实查**（手段：`ls openspec/changes/`／该包 `tasks.md` 勾选状态／`grep 形态⑧ 0-学习与工具/工具-opener块lint.py`）。
- [ ] 0.3 核 `#504`（心跳件可见性）是否已改变 `reports/` 的处置口径——若已变，重算决策点 ②(c)／③-2(b) 的代价。
- [ ] 0.4 确认同泳道 A4（`#487`）已收工、无未提交改动占用 `opener骨架.md`／`工具-opener块lint.py`。
- [ ] 0.5 🔴 **先后已定（2026-09-10 `OP-0910-R`，队列 `#531` ⑵）**：`opener-id-claim-semantics` 已随 `#549` 泳道先 apply（分支 `claude/op0910r-549-550-opener-infra`，同触 `工具-opener生成.py`：取号查重段新增占位台账 `_claim_op_id`／`CLAIMS_FILE`）⇒ 本包 apply 前须 rebase 到其之后；0.2 记的形态编号 ⑧ 已被 `#487` 占、**⑨ 已被 `#550`（收工哨兵）占**，本包须另取 ⑩ 起。

## 1. 取证与口径（🔴 必须先于实现）

- [ ] 1.1 **全库基线**：跑 `python 0-学习与工具/工具-opener块lint.py`，记录当前「历史件／当前在用件」两类计数（形态①–⑦ 现状）。**原文抄进本清单**，不得只写「通过」。
- [ ] 1.2 **存量指纹缺口实测**：统计当前在用件中 opener 块总数（全部无指纹）＝ 形态⑧ 若无豁免时的新增阻断上限。
- [ ] 1.3 复核决策点 ②(c)／③-2(b) 的 `.gitignore` 结论仍成立：`git check-ignore -v reports/opener-fingerprints.jsonl`（propose 期实测命中 `.gitignore:50:**/reports/`）。
- [ ] 1.4 核 `工具-opener批处理执行.ps1`／`v2.ps1` 是否消费生成器 stdout（决定 ① 选 (a) 时的连带改动面）。

## 2. 实现

- [ ] 2.1 指纹函数**只写一份**：在 `工具-opener块lint.py` 定义 `block_fingerprint(block_text) -> str`（SHA-256 前 8 位十六进制，计算前归一化 `\r\n` → `\n`，且**排除指纹行本身**）；`工具-opener生成.py` 动态加载复用，**不另写第二份**（沿用编辑锁加载 lint 判据正本的既有惯例，含「取同目录兄弟、不取 `REPO_ROOT`」那条 worktree 教训）。
- [ ] 2.2 `工具-opener生成.py` 新增 `--out <仓库根相对路径>` 与 `--anchor <块所属章节标题>`；执行序 ＝ 拼装 → `check_block` 自检 → 写入落点 → **从落点文件回读** → 把回读到的字节打 stdout。**顺序不可颠倒**（理由见 design 实现细节 2）。
- [ ] 2.3 `工具-opener生成.py` 未传 `--out` 时的行为**按决策点 ① 的答案落地**（(a) fail-loud 退出码 1／(b) stderr 警告／(c) 默认落点）。
- [ ] 2.4 指纹行位置**按决策点 ② 的答案落地**；若选 (a)，同步在 `opener骨架.md` 三种骨架各加该行占位并说明「由生成器盖，勿手填」。
- [ ] 2.5 `工具-opener块lint.py` 新增**形态⑧**：块内指纹行缺失或与块正文算出的指纹不符 ⇒ 违规。存量豁免**按决策点 ③-1 的答案落地**。
- [ ] 2.6 射程自陈承接**按决策点 ③-2 的答案落地**；若选 (a)，`工具-共享文档编辑锁.py` 的 release 回显改为指向 lint 文件头「射程边界」节，**不重述**（指向而非复制）。
- [ ] 2.7 `工具-opener批处理执行.ps1`／`v2.ps1` 按 1.4 结论适配。

## 3. 规则与出件段同步（内容改动，与 2 同批）

- [ ] 3.1 根 `CLAUDE.md` §3 第三条改为**结果性禁令**：「会话不得手打、手改、削短任何 opener 块正文；每个字节只能来自 `工具-opener生成.py` 的同一次调用；要改内容 ⇒ 改参数重跑」。
- [ ] 3.2 `zhuopin-kickoff-prompt/SKILL.md`：步骤 9 改为「块由生成器 `--out` 出，聊天里贴的是**回读到的那份**」；**删除**步骤 10 末条「写入临时 `.md` 跑 lint」（守在没人消费的那份上，proposal「本次退休」第 2 条）。
- [ ] 3.3 `zhuopin-lane-watch/SKILL.md`：补 What Changes ④——看护者给泳道追加的执行环境约束**必须先回落库块**再交 Task/Agent，Task prompt 正文只能是落库块的逐字副本。
- [ ] 3.4 `1-转型规划/session接力-业务总线.md` §五 第一条**人守降为一行指针**（`#503` 期望产出 ③，规则退休制）。
- [ ] 3.5 `专线opener模板库.md` §〇 补一条判据说明（**只写判据、不写格式**，格式正本仍是 `opener骨架.md`）。
- [ ] 3.6 登记 skill **两向同步欠账**（模板库 §〇.10）——源码已改、已安装版未改，这本身是本包判据下的命中项（普查清单 3）。

## 4. 测试

- [ ] 4.1 `test_工具-opener生成.py`：`--out` 落盘成功、stdout 与文件内容**逐字节相等**；`--anchor` 命中/未命中；写入失败时**不打 stdout**（fail-closed）；未传 `--out` 的行为按 ① 的答案各配一例。
- [ ] 4.2 `test_工具-opener块lint.py` 形态⑧：⑴ 无指纹 ⇒ 违规；⑵ 指纹正确 ⇒ 放行；⑶ **非恒真自证**——块正文改一个字符、指纹保持原值 ⇒ 必须报违规（证明不是只检查「有没有那一行」）；⑷ `\r\n` 与 `\n` 两种行尾算出**同一个指纹**；⑸ 历史件按 ③-1 豁免路径不报。
- [ ] 4.3 `test_工具-共享文档编辑锁.py`：release 守卫逐字复用 `check_block`，形态⑧ 上岗后**会自动变严**——补一例证明历史件不被误拦，并跑全量确认零回归（同 `OP-0906-M` 的 `371 passed` 基线，**实测数抄进收工报告**）。
- [ ] 4.4 全库回归：再跑一次 1.1 的命令，与基线逐数对比，**在用件新增阻断数须等于预期**（预期数在 1.2 已算出）。

## 5. 现网验证（不落盘）

- [ ] 5.1 用真实参数出一份 opener（走 `--out`），核对：落库块与 stdout 块指纹一致、`--enforce` 零违规、六字段齐全。
- [ ] 5.2 **反向验证**：手工改落库块一个字符，确认形态⑧ 报出（这一步是本包唯一能证明它真的在守的动作）。
- [ ] 5.3 对本批看护件 `看护件-泳道看护批B-0908_四泳道机制根治-2026-09-08.md` 做一次**只读**核对：若本包已落地，A3 块那句 `worktree：☑ 新建` 与实际执行的冲突会不会被发现？**如实记录结论（很可能仍不会——形态⑧ 守的是来源不是真伪）**，写进收工报告，不得粉饰。

## 6. 收口

- [ ] 6.1 队列 §一 `#503` 回填：design 审结论、三个决策点答案、apply 落地 commit、1.1/4.4 实测数。
- [ ] 6.2 `#503` 期望产出 ② 的普查清单（本包 `同族普查-两份内容清单.md`）中**尚未处置的命中项**（清单 3／5／6／7／8）逐条登记去向——**不得在本包销号时默认它们已解决**。
- [ ] 6.3 收工重跑文档台账；登记 §二 待 commit 批次，由落库 sweep 取活。
- [ ] 6.4 🔴 **合入 master 是 🟡 档，泳道自己不合**——由他一字母后，看护者或 sweep 串行做。
