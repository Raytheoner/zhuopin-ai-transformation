# sweep-manifest-coverage-guard Specification

## Purpose
TBD - created by archiving change sweep-manifest-coverage-guard. Update Purpose after archive.
## Requirements
### Requirement: 清单只被部分覆盖时整批跳过，不得产出空壳提交
落库 sweep 在提交一个 §二 待处理批次之前，SHALL 校验该批次「文件清单」列声明的路径是否都能装进本次提交。

判定口径：`_status_paths` 走 `git status --porcelain=v1`，工作区改动与已 `git add` 进索引的改动都会列出，故「已解析出脏路径」的集合即「这次提交装得进去」的全集。清单中形状合格（见「形状判据」Requirement）却不在该全集内的片段，即判为**缺项**。

存在缺项时，sweep MUST **整批跳过**该批次：MUST NOT 提交该批次的任何文件，MUST NOT 改动该批次行的状态列（保持待处理），MUST NOT 将其误判为「遗留尾巴补销」。

跳过 MUST 是无损的——批次状态不动，其声明的内容合入 master 后，下一轮 sweep MUST 自动照常落库。sweep MUST NOT 因本校验把这类批次永久拦死。

sweep MUST NOT 只提交清单中能对上的那一部分文件、却套用清单登记的整批提交标题。该形态即本能力所治的事故本体（2026-08-29 提交 `6557047`：清单声明 8 条，实际只 `git add` 了队列文件一个，却挂着「哨兵建造 apply」的标题并把批次销成已完成）。

#### Scenario: 跨 worktree 批次的代码尚未到主仓，整批跳过
- **WHEN** 一个 §二 待处理批次的文件清单声明了队列文件与若干代码件，而此刻主仓工作区只有队列文件是脏的（代码件仍在别的 worktree 的分支上）
- **THEN** sweep 不产出任何提交，该批次行的状态列保持待处理，且历史中不出现该清单登记的提交标题

#### Scenario: 声明的文件真的到齐后，同一批次照常落库
- **WHEN** 同一条批次行，其清单声明的全部文件此刻在主仓工作区确有对应脏改动
- **THEN** sweep 照常提交该批次并将其状态列销为已完成

#### Scenario: 跳过不得被误当成遗留尾巴补销
- **WHEN** 某批次因本校验被跳过
- **THEN** 该行 MUST NOT 被标记为「补销遗留尾巴」或任何等价的已处理表述

### Requirement: 本校验只拦「部分覆盖」，不拦「零覆盖」
本校验 MUST 仅在已解析出至少一条脏路径（即该批次确实会产生一个内容提交）时才判定缺项。当一条脏路径都未解析出时，本校验 MUST 一律放行，不插手。

理由：「零解析、清单非空」是既有的「遗留尾巴补销」路径（队列 `#328`），其形态是「内容早已落库、只差销行」，本来就不产生内容提交，不存在空壳提交问题。本校验的作用域 MUST 刻意收窄到「能对上一部分、就拿这一部分冒充整批」这一条唯一会造出空壳提交的路径，MUST NOT 改变 sweep 其它任何既有行为。

#### Scenario: 零解析时不判缺项
- **WHEN** 某批次一条脏路径都未解析出，而清单中存在形状合格的路径片段
- **THEN** 本校验不判缺项，该批次按既有的遗留尾巴路径处理

#### Scenario: 全覆盖时不判缺项
- **WHEN** 某批次清单中的全部片段都已解析为脏路径
- **THEN** 本校验不判缺项，该批次照常落库

### Requirement: 形状判据须先筛掉正文强调片段
§二「文件清单」列 MUST NOT 被当作纯路径列表处理。参与覆盖校验的片段 MUST 先通过一条「看起来是不是一条仓库内路径」的形状判据；未通过者 MUST NOT 计入缺项。

**本条是本能力可用的前提，不是优化**：2026-08-29 现网实测，182 行共 1544 个反引号片段中 757 个（近一半）不是路径，而是分支名、行号、版本号、命令片段、gitignore 规则等正文强调。若直接以「未解析出脏路径的片段非空」作为缺项判据，几乎每一条登记详细的批次行都会被永久跳过——那不是收紧，而是关闭 sweep，等于重新造出队列 `#238` 已修掉的「批次跨天积压」。

形状判据 MUST 拒绝：以 `~` 开头的片段（仓库外引用）、以 `/` 结尾的片段（目录而非单个可提交文件）、含任何空白字符的片段（命令行）、含通配或花括号展开字符的片段（无法对到唯一路径）。形状判据 MUST 要求片段末尾形如「非点非斜杠字符 ＋ 点 ＋ 扩展名」，以排除正文里提到的裸扩展名与点开头短词，同时保留点开头目录下的真实路径。

该判据 MUST 被如实表述为**形状启发式而非真值**，且其两个方向的失手 MUST 在代码与文档中一并登记：漏认（真路径被当成散文）退化为校验前的旧行为、不新增破坏也不提供保证；误认（散文被当成路径）导致该批被点名跳过，人工修正登记写法后下一轮自动落库。

#### Scenario: 真实仓库内路径被认出
- **WHEN** 片段形如仓库内某个真实文件的路径，含省略前缀的通行写法、点开头目录下的路径、以及文件名含全角字符的路径
- **THEN** 形状判据认定其为声明路径，纳入覆盖校验

#### Scenario: 正文强调片段不被误认为路径
- **WHEN** 片段是分支名、提交短哈希、队列行号、命令片段、gitignore 规则、目录写法、通配写法、或仓库外的用户目录引用
- **THEN** 形状判据拒绝该片段，其不参与覆盖校验、不计入缺项

### Requirement: 校验自身失败必须 fail-open
本校验 MUST 以 fail-open 方式挂载：校验自身抛出任何异常时，sweep MUST 只记录一条日志痕迹，并按校验前的旧判据继续处理本批次与本轮其余批次。这道检查 MUST NOT 成为让整轮 sweep 停摆的新单点故障。

该契约无法由黑盒用例触发（校验体是纯函数，正常输入不抛），故 MUST 由源码结构断言钉死：`main()` 内对覆盖校验的调用 MUST 就地包在一个带 `except Exception` 的 `try` 中。该断言 MUST 只认「调用写在 try 正文这一层」的包裹，MUST NOT 接受函数体外层的兜底 `except Exception` 充数——外层兜底会让异常一路冒到轮末终止整轮，而本条要求的是就地接住、接住后继续跑完其余批次。

🔴 fail-open 的代价 MUST 被如实登记而非淡化：校验失效时空壳提交重新变为可能，且此时账面照样正常，唯一信号是那一行日志。这是一个有意接受的取舍——sweep 停摆的代价大于本检查静默失效的代价。

#### Scenario: 校验体抛异常，本轮继续
- **WHEN** 覆盖校验在处理某批次时抛出异常
- **THEN** sweep 记录一条说明「本轮按校验前的旧判据继续」的日志，并继续处理该批次与其余批次，不终止本轮

#### Scenario: 就地 try 被摘除时源码断言转红
- **WHEN** `main()` 内对覆盖校验的调用不再就地包在带 `except Exception` 的 try 中
- **THEN** 源码结构断言 MUST 失败——外层兜底 try 的存在 MUST NOT 使该断言通过

### Requirement: 跳过必须出声，且出声形态限于日志
批次被本校验跳过时，sweep MUST 在其运行日志中点名，且该条日志 MUST 同时含：批次号、所在队列文件、判为缺项的路径清单、本次实际能对上的路径清单、以及处置办法（代码若还在别的分支上则合入 master 后下一轮自动落库；若被点名的片段其实不是待落库路径，则修正登记写法）。sweep MUST NOT 静默跳过而不留痕。

🔴 本能力的出声形态 MUST 被如实限定为**日志留痕**，MUST NOT 被表述为「告警」或「通知」。本能力**不接推送通道**——跳过发生时没有人被主动通知。任何据本能力所作的表述，MUST NOT 使读者以为跳过会主动送达任何人。

若此后为本能力接入推送通道，MUST 连同阈值与去重状态机一并设计（队列 `#147` 实证：无去重的告警连响 17.1 小时后信息量归零），MUST NOT 只把日志行原样转发。

#### Scenario: 跳过日志的必备信息项
- **WHEN** 某批次被本校验跳过
- **THEN** 日志中该条记录含批次号、队列文件、缺项路径清单、本次能对上的路径清单、以及处置办法

#### Scenario: 出声不等于送达
- **WHEN** 描述本能力的出声形态
- **THEN** 该描述 MUST 写明其限于运行日志，MUST NOT 表述为会主动通知任何人

### Requirement: 覆盖范围须被如实声明
本能力的观察面 MUST 被如实限定为**主仓工作区的脏改动与索引内容**。本能力 MUST NOT 被表述为「知道文件在哪」——它不知道声明的路径是否存在于别的 worktree、也不知道其内容是否已合入 master，它只知道「这次提交装不装得进去」。

由此产生的登记侧口径 MUST 被一并写明：文件清单列中凡用反引号包起的路径形状片段，都会被当作「本批要落库的文件」；已由建造方自行 commit 合入 master 的代码件 MUST NOT 再用反引号写进文件清单，否则该批会被本校验正确地拦住。

本能力 MUST NOT 被表述为已校验「提交标题与提交内容相符」这件事本身——它只堵住了造出不符的一条已知路径；同类不符若从别的路径产生，本能力不覆盖。

#### Scenario: 已合入 master 的代码件写进清单会被拦
- **WHEN** 某批次的文件清单中，以反引号写出一条代码件路径，而该代码件已由建造方自行 commit 并合入 master、此刻主仓工作区对它没有脏改动
- **THEN** 本校验判该片段为缺项并整批跳过——这是本判据的正确行为，处置办法是登记方不将该路径写入清单

#### Scenario: 描述本能力时不得夸大观察面
- **WHEN** 任何文档描述本能力的判定依据
- **THEN** 该描述 MUST 限定为「主仓工作区脏改动与索引内容」，MUST NOT 表述为知晓文件在其它 worktree 或 master 上的存在状态

