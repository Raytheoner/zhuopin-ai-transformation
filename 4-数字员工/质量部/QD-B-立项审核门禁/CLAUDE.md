# CLAUDE.md — QD-B 项目立项审核门禁助手（场景级记忆）

> 本文件是 QD-B 场景的本地记忆/进度笔记，隔离于其他场景。
> 项目级上下文见仓库根 `CLAUDE.md`；QD-B 规划权威见全景规划 §2.1.3 质量部 Q6 行、
> `1-转型规划/质量域AI数字员工路线图.md`、`1-转型规划/质量旗舰开工就绪包-数据归集与治理共识.md`。
> 本场景 = CC 建造车间产物；**不改规划文档**（那是 Cowork 的活）。
>
> ✅ **极简版已发布上线（2026-07-23，队列 #79）**：变更包 `qd-b-project-gate-review` 已按
> 《发布即收口纪律》四关标准归档（`/opsx:archive`），**最小 Web 服务已部署 `.51`**——
> http://192.168.100.51:8093/ ，已接入 AI 运营指挥中心门户质量域卡，陈忱+朱映桦灰度试用中。
> 详见 §7「部署状态」。C01-C10 跨模块校验（任务4）+ B 类真实 LLM 判定（任务5.2）+ OEM 附件
> 隔离例外（任务6.6）等扩容期事项已在归档的 tasks.md 中显式标注延后，不阻塞本次发布。

## 1. 场景定位

- **场景**：QD-B = 质量旗舰（全景 Q6，Paul 第二管辖域），项目立项审核门禁助手。AI 出预审意见，L2 决策权在评审委员会/PMO，AI 结论永远是"预审建议"非终局。
- **全景编号**：全景规划 §2.1.3 质量部 Q6（S1 筑基期，2026-10 入轨，与 QD-A 8D 同月并行；2026-07-01 从 11 月提前）。
- **自动化等级**：L2 — 预审输出报告，评审委员会人工复核后决策；AI 不做终局判定。
- **MVP 范围**：仅开发类 `EQQR8082` A2.1；68 条 A 类确定性规则 + 10 条 B 类语义判定（依赖收口-2） + C01–C10 跨模块校验 + 聚合门禁报告；两档起步（合格/不合格，评分制收口-1 后可插拔）。研发类 `EQQR8106` A3 为二期。
- **当前状态**：极简版**已发布上线**（2026-07-23）——工程脚手架 + 规则注册表 + 解析探针（13 章节 100% 定位）+ A 类确定性规则（模块一~十，55/68 已实现，63/64 暂缓）+ 评分层 + C 类转人工 4 条（42/80/81/82）+ B 类语义占位（10条）+ 报告聚合（六段式）+ 全链审计，均已实现并通过 60 tests（含黄金基准 EQ17/邦奇/华丰三样本零偏差回归 + Web 服务 6 个测试）；最小 Web 服务已部署 `.51`（http://192.168.100.51:8093/ ），陈忱+朱映桦灰度试用中，详见 §7。C01-C10 跨模块校验（任务4）与 B 类真实语义判定（任务5.2）等扩容期事项不阻塞本次发布。

## 2. 关键决策记录（Paul 拍板）

| 决策 | 结论 | 依据 |
|------|------|------|
| 0.1：四能力分层与 MVP 两档起步 | **✅ 全部同意**（2026-06-27 Paul 审 design.md） | QD-A/doc_parser 现状纠正已纳入 design（QD-B 是唯一真实消费方） |
| D1：四能力分层 | 解析（intake-parser）/ 确定性规则（rule-engine）/ 语义判定（semantic-judge）/ 聚合门禁（review-gate）四层 | 确定性 68 条可纯单测；语义 10 条 LLM 独立；互不污染 |
| D2：规则单一可信源 | 工作汇总.xlsx「立项门禁」页 = 单一可信源（A1:K83）；代码维护规则注册表（registry.json），函数挂元数据 | IATF 单一可信源；规则版本 2026-06-27（82 条 = 68A/10B/4C） |
| D3：取数锚 | 章节标题文本定位，非绝对单元格坐标；非 A2.1 版本告警不静默 | 模板一月四版（A0→A2.1），坐标锚必然脆裂 |
| D4：解析探针先行 | 字段抽取准确率作规则判定前置门槛；字段区分"真为空（不合格）"vs"解析未抽到（待人工核）" | 解析可靠性是最大工程未知数，先证伪再建造 |
| D5：doc_parser 场景本地 | doc_parser 留 QD-B 场景本地（rule-of-three 未触发；QD-A 尚未建）；注释标注"QD-A 落地且真复用时再提升 shared_tools" | 无第 2 真实消费方，避免过早抽象 |
| D6：置信度 | 二级（高/低），对齐 SC8；低置信默认转人工；判据未定前全部转人工兜底 | 统一口径可审计；LLM 业务判据未定前不擅断 |
| D7：MVP 两档起步 | 错误项一票否决（有错误→不合格/无错误→合格）；评分制可插拔（收口-1 后接入，三档生效） | 评分细则未定不阻塞首版 |
| D8：严重度映射 | J 列（阻断/重要/一般/提示）→三级（错误/警告/提示）显式化；阻断→错误；默认重要→错误，一般→警告（收口-1 可调） | 两套词表须有确定映射，"一票否决"才可落地 |
| D9：审计与隔离 | 审核结论写 `zhuopin_platform.audit`（scenario=QD-B）；含 OEM 技术方案附件走 `data_isolation_layer` | 见红线 §4 OEM 隔离扩展条款 |
| D10：黄金基准 | 历史已结案立项书对比（技术服务类华丰 1 份 + 产品类 ≥1 份待补）；A 类 68 条零偏差；B 类 ≥80% 人工一致率（MVP） | 仿 SC8 门禁，脱敏后入 data/golden/ |

## 3. 复用底座资产

- **审计**：`zhuopin_platform.audit.AuditLogger`（scenario="QD-B"，关联立项文档版本ID，append-only hash-chain，IATF 3 年留存）。
- **数据隔离层**：`zhuopin_platform.data_isolation_layer.OEMRouter`（附件含特定 OEM 技术方案/报价时启用，见红线）。
- **不使用**：连接器（QD-B 读本地 Excel，不连 SRM/ERP）、Notifier（预审报告输出，不对外推送）。
- **场景本地**：`qd_b_gate/parser.py`（Excel 解析探针）；`qd_b_gate/rules/registry.py`（规则注册表加载）；`qd_b_gate/probe.py`（解析探针主入口）；`scripts/build_rule_registry.py`（从 xlsx 导出 82 条规则到 registry.json）。

## 4. 红线（建造时守住）

- **OEM 隔离扩展（质量域）**：立项书附件含特定 OEM 技术方案、OEM 专用报价、OEM 研发合作细节时，必须走 `data_isolation_layer.OEMRouter` 按客户路由，禁止跨 OEM 访问（根 CLAUDE.md §4 质量域扩展：含 OEM 信息的质量数据按客户隔离；物理分库，非逻辑过滤）。
- **立项书本身（无 OEM 附件）**：内部数字员工场景，不涉 OEM 技术数据，不强加 OEM 路由。
- **AI 不做终局决策**：AI 结论永远是"预审建议"，终局决策在评审委员会/PMO（L2 人工确认）。
- 每次审核判定全链写平台 `audit`（含规则版本号、抽取状态、判定理由，IATF 8.3 可追溯）。
- 先 mock/脱敏跑通逻辑；含真实立项书数据的黄金基准脱敏后入 `data/golden/`，不入库（.gitignore）。
- 解析字段区分"真为空（业务不合格）"vs"解析未命中（待人工核）"——不可将解析失败当业务不合格处理。
- ISO 26262 门禁：立项书若涉 ASIL C/D 功能安全项（ASIL 字段=是），规则 C06 强制触发，结论转功能安全工程师额外审核，AI 禁止自动判定安全相关结论（ASIL C/D = AI 绝对禁区）。
- **停审点（apply 前必须收口）**：0.2（质量部/PMO 给评分扣分细则或确认两档起步）/ 0.3（陈忱给 10 条 B 类规则业务判据）/ 0.4（陈忱补 ≥1 份产品类立项书脱敏样本）—— 未收口前不启动任务 4/7/对应实现。

## 5. 状态时间线

| 日期 | 状态 |
|------|------|
| 2026-06-11 | 质量域规划启动，QD-B 旗舰选定（与 QD-A 并列，全景 Q6；off-LAN 友好、风险可控）。 |
| 2026-06-11 | 《质量旗舰PRD-项目立项审核门禁》完成；D1–D6 全采纳推荐（选 A）；财务"可机器核规则"边界待财务共识（D2）。 |
| 2026-06-27 | Paul 审 design.md 全部同意（含 D1–D10 + QD-A 现状纠正）。工程脚手架 + 规则注册表 + 解析探针模块一（68/82 条规则注册，锚点 100% 定位，华丰试评对账 14/14 一致（100%））已落地。 |
| 2026-06-27 | 数据归集与治理共识就绪包更新（陈忱团队实操成果：7 份脱敏 8D 样本，脱敏方案/录入表字段/黄金基准对齐）。 |
| 2026-07-01 | QD-B 排期从 11 月提前至 2026-10，与 QD-A 同月并行（CC 建造进度超前，模块一 68/82 条规则 + 试评对账 100%）。 |
| 2026-07-14 | **注册表刷新到 20260710 权威权重表**（陈忱定稿版，errata 3 处已修）：一票否决 **17→15 阻断**——收益指标 61/62 阻断→重要（不再否决）、累计现金流 74 & 最后节点 66 →阻断；目标分类 26/指标量化 27 亦 阻断→重要（**Paul 2026-07-14 确认有意**）。八（二）weight_sum 重算 6.1→5.1、模块四 3.5→2.5。**评分口径裁定（Paul 2026-07-14）：QD-B 用 82 条权重表「模块基础分×系数/模块权重和」加权，不复现陈忱评审报告的 v5.0 每项标准分** → scoring 层保持不重写。scoring/registry 测试同步更新。**7-外部文档/质量部 被质量专线重组**：工作汇总.xlsx + 空白模板移入「产品类立项申请书及评审报告」子夹（已修 build/conftest 路径）。**收口-3 产品类样本已就位**（EQ17 合格 + 邦奇 不合格 + 各自评审报告 v5.0 答案键）：解析均 产品类 13/13 可用；**邦奇不合格的 driving rule=74 累计现金流阻断 + 69 现金流合计**（正是新升阻断项），现引擎因财务表勾稽规则未实现暂无法复现，已锁缺口测试。34 tests。 |
| 2026-07-03 | 陈忱交付收口-1（扣分权重表）+ 收口-2（半自动判据 v4）。**评分层落地**（scoring.py，仅阻断一票否决、100分制三档、14 模块权重和与权威表对齐）；**一票否决修正为仅阻断**（此前 MVP 占位「重要也否决」纠正）；规则版本升 2026-07-03，权重表覆盖 2 处严重度（规则34 重要→提示、66 重要→一般）。**模块一 A 类规则全实现**（+14/18/19/22，人员安排表&文本区域解析），试评对账 **18/18 一致**。25 tests 全绿。 |
| 2026-07-14（轨B） | **模块六~十财务表计算勾稽 A 类规则落地**（35-41风险系数/等级/高风险应对措施、43-46资源、47-51成本效益F=D-E&P=F+M&合计校验、52-62收入依据一致性/毛利率/收益指标达标、65-69预算表折旧⑦=②×15%/人月一致性/现金流合计、70-74现金流表月净现金流/累计现金流末期）；A 类 55/68 已实现（63委外占比/64付款方式因口径歧义暂缓，见 §6）。解析器新增风险/资源/成本效益/收入依据/预算表/现金流表 6 张结构化表格抽取，含成本效益表元/万元量纲不统一的归一化（收入依据表恒定万元作锚点判定量纲、除59/60/61外的跨表比对统一换算）。**用 EQ17（评审报告=合格99.2）+ 邦奇（评审报告=不合格，driving rule=74累计现金流阻断+69现金流合计，相差46.66万）验证「引擎档位==评审报告档位」，均一致**（`test_golden_product_class.py` 由"缺口锁定"改写为严格回归）；驱动同时发现并修正风险等级判定盲点（严重度=非常严重/灾难时系数分档法会漏判，须叠加严重度兜底恒判高风险，经 EQ17 风险#1 实证）。华丰试评对账可比条目 14→55、一致率仍 100%。34 tests 不变，28 passed/6 skipped（6 为既有 real_a21_path 空白模板 fixture 缺失，非本批引入）。 |
| 2026-07-20 | **C 类转人工 4 条 + B 类语义占位 + 报告聚合落地（M5/M6/M7，任务 6.1/6.2/6.3/6.7 + 5.1/5.3 部分）**。续建前先核实真实进度（跨桌任务队列 #45 rule ID 核对），发现**权威表并未随 20260717（80条）重编号**——`registry.json` 仍是 2026-07-10 82条版（63/64 PENDING 仍在），`81d997c`（07-14）早于 20260717 定版（07-16/17），此前"大概率已被覆盖"的假设不成立，已如实回写 #45（详见 §6 依赖行）。本批新增：① 解析器 `_parse_summary_signatures`（模块十二签字栏，标签在上、日期在下的纵向布局，且需按"含数字"而非"非空"判定——空白模板日期行是"年 月 日"填写骨架而非真空单元格，按非空判会把未签字误判为已签）；② `deterministic.py` 新增规则 42（应对措施只验在否，缺项→警告不否决）/80/81（签字只验在否，缺→阻断 FAIL，有→转人工核真伪）/82（立项决议恒 NA，申请阶段不判）；③ 新建 `rules/semantic.py`（B 类 10 条 MVP 占位，统一转人工，真实 v4 判据留扩容期）；④ `rules/engine.py` 新增 `run_manual_class_rules`/`run_semantic_rules`/`run_all`（A+B+C 全量，`run_rules` A-only 原样保留供既有黄金测试引用不动）；⑤ 新建 `report.py::GateReport/build_report`（六段式《立项审核报告》，④跨模块段如实标注"C01-C10 任务4未实现"而非空着不说明）；⑥ `evaluate.py` 改跑 `run_all()`（全链审计 68→82 条，C/B 类判定纳入 IATF 8.3 可追溯范围）。**黄金基准验证**：本地缺真实样本文件（gitignore、worktree 不共享），已从主工作区拷贝 EQ17/邦奇/华丰三份真实文件到本 worktree 验证——三者 tier/一票否决驱动规则均不因新增 C 类判定漂移（EQ17 合格、邦奇/华丰 不合格且驱动规则仍是 74/11，rule42/80/81 在三份真实文件上均返回"转人工"而非否决，因这些已批复项目本就签字/风险应对措施齐全）。34→54 tests 全绿零回归（含 20 个新测试）。 |
| 2026-07-23 | **极简版发布收口（队列 #79，见 §7）**：`data/golden/manifest.md` 结构化清单登记三样本；tasks.md 组4/5.2/5.4/3.2/3.6/3.7/6.6 逐项标注🧭扩容期 future-work；openspec 归档；新建 `qd_b_gate/webapp.py`+`scripts/run_qd_b_web.py`（最小 Web 服务，端口8093，上传→六段式报告，如实标注B类转人工/C01-C10未实现）+ 6 个新测试（54→60 tests 全绿）；`.51` 真实部署+防火墙+计划任务（`QdBWebServer`，SYSTEM+AtStartup）+ 真实样本全量跑冒烟通过；接入 AI 运营指挥中心门户质量域卡；灰度开闸（陈忱+朱映桦）。**顺带发现并修复一个部署层生产事故**：为推送门户页更新触发了命令中心 `CommandCenterWeb` 计划任务重启，暴露其原注册方式（AtLogOn 交互式登录+裸命令"python"）在 Administrator 会话处于"已断开"状态下无法启动新进程（0x80070002）——服务短暂中断，已改注册为 SYSTEM+AtStartup+解析绝对路径（同 SC8/QD-B 惯例）并真实验证 End+Run 重启可靠，详见 `1-转型规划/AI运营指挥中心/deploy-server.ps1` 内 2026-07-23 修复说明。 |
| 2026-07-27 | **陈忱灰度反馈三项改进落地（队列 #116，#104/#105 合并）**：新增 `qd_b_gate/report_items.py`（网页/Excel 共用的逐项打分展示层——标准分/实得分/扣分复用 scoring.py 既有公式`模块基础分×规则系数/模块权重和`，不改判据；13 模块得分率展示层把八（一）/（二）两个计分池合并成一行，凑齐陈忱原文"13个评审模块"，规则判定各自归属不变）+ `qd_b_gate/xlsx_report.py`（③ Excel 评分表导出：评审汇总/评审明细/扣分明细 3 sheet，格式基准=陈忱回件模板`质量部-ChenChen-回复-2026-07-24-...评审报告模板-0e48651...xlsx`，状态颜色通过绿/待改进黄/不合格红/不适用灰+转人工蓝/未实现灰两态如实标注、冻结表头、模块+状态 autofilter、文件名`项目名称_评审报告_YYYYMMDD.xlsx`）。`webapp.py` 报告页重排（①②）：顶部总分+不合格/待改进扣分小计、②13模块得分率一览（进度条+✅/⚠️）、③扣分明细表（按扣分降序，仅待改进/不合格）、④全量82项明细表（模块+状态双下拉筛选+"仅看问题项"复选框，纯前端 JS，无第三方库）、下载按钮置顶；⑤~⑧沿用既有跨模块/转人工/审计段不变。**开工前已核对 #110**（SC8 保供看板判例包网页表单化）：不同场景不同服务，代码不共享，无 UI 返工风险。60→91 tests 全绿零回归（新增 28 单测：report_items 18 + xlsx_report 10 + webapp 4）；三份黄金样本（华丰不合格/EQ17合格98.8/邦奇不合格）逐一验证 82 项/13 模块/财务摘要（毛利率展示层格式化为百分比，取数不变）均正确、Excel 可正常打开。`.51:8093` 真实部署+重启（`sync-to-server.ps1`）+ 冒烟：`/api/ping`/首页 200，真实上传华丰样本全链验证网页新板块+下载链接+下载的 xlsx 内容（3 sheet/82 行/正确状态集合）均与本地一致。均为展示层，未改 80 条权重表/扣分判据。详见队列 #116。 |

**Tasks 完成度（2026-07-23 极简版发布收口后，openspec 已归档）**：组 0（0.1-0.5 ✅ 全部已收口）；组 1（1.1-1.4 ✅）；组 2（2.1/2.3/2.4 ✅，2.2 ⬜🧭扩容期，2.5 ~）；组 3（3.1/3.3/3.4 ✅，3.2/3.6/3.7 ⬜🧭扩容期，3.5 ~）；组 4（C01-C10 跨模块）⬜🧭**扩容期**（报告④段如实标注未实现，不阻塞发布）；组 5（B 类语义）5.1/5.3 ~ 占位版已实现且满足"不擅断"红线，5.2/5.4 ⬜🧭扩容期；组 6（review-gate）6.1/6.2/6.3/6.4/6.5/6.7 ✅，6.6（OEM附件隔离）⬜🧭扩容期（立项书本身不涉OEM技术数据，不阻塞）；组 7（黄金基准正式入库 `data/golden/manifest.md`）✅；组 8（收口归档）✅；**组 9（极简版发布收口：Web服务/`.51`部署/门户接入/灰度开闸）✅**。归档件见 `openspec/changes/archive/2026-07-23-qd-b-project-gate-review/`。

## 6. 关键依赖/前置（解锁条件）

- ✅ 收口-1（陈忱 7-03）：扣分权重标准_82条规则.xlsx — 已落地评分层（scoring.py），三档判定就绪。
- ✅ 收口-2（陈忱 7-03）：半自动及人工判断标准_合并版v4.xlsx — B 类判据就绪，**语义实现（任务 5）待接入**（未接入前 B 类默认转人工）。
- ✅ 收口-3（陈忱，样本已就位 2026-07-14）：产品类立项书脱敏样本 EQ17（合格）+ 邦奇（不合格）— 产品类黄金基准回归通过（`test_golden_product_class.py`，引擎档位==评审报告档位）。
- 🟡 规则 63（委外占比）/64（付款方式）口径待陈忱澄清：63 用「委外费④/成本效益成本E」反推不出华丰试评"24.5%"，64 所需字段在当前模板未见专属单元格——两条暂标 PENDING，不影响已实现的 55 条 A 类规则判定。**跨桌任务队列 #45 核对结论（2026-07-20）：`registry.json`（`rule_version`）仍是 2026-07-10 版（82条口径，含 63/64 PENDING），并未随 20260717 定版（80条，删 63/64+整体重排+旧66→64/69→67/74→72）重编号**——`81d997c`（模块六~十财务表勾稽落地）提交于 07-14，早于 20260717 权威表定版（07-16/17），此前"大概率已被 81d997c 覆盖"的假设不成立。因队列 #45 原文本已注明"内部 ID 可不改，行为一致（F+M→F 已按实测口径，无需改）"，本轮判断**暂不做registry重编号**（重编号是纯文档对齐、非行为修复，做与不做都不影响判定结果，属"CC 定、可不改"授权范围内的选择）；但如未来需要对外（如给陈忱/PMO）核对规则编号，须知悉代码内部仍用旧编号，需按此条对照表换算。
- ✅ D8 定案（收口-1 权威）：仅「阻断」一票否决；重要/一般/提示按 0.5/0.3/0.1 系数扣分。
- 🟡 AIOps 第 2 人到位 — 质量场景开发吞吐的前置（全景 #1 风险，QD-A/QD-B 排期与此强绑定）。
- 🟡 QD-A 8D 场景落地 — doc_parser 真复用时提升 shared_tools 的前置（rule-of-three 第 2 消费方）。
- 运行：`python qd_b_gate/probe.py <立项书.xlsx>`（解析探针，空白模板或脱敏已填样本）。

## 7. 部署状态（2026-07-23 极简版发布收口，队列 #79）

- **地址**：http://192.168.100.51:8093/ （仅 LAN，无登录鉴权，同 SC8/命令中心惯例）
- **服务**：Flask+waitress，`qd_b_gate/webapp.py`（上传 xlsx → `evaluate()` 全链 → 六段式报告页）+
  `scripts/run_qd_b_web.py` 启动入口；计划任务 `QdBWebServer`（SYSTEM + AtStartup，失败重启3次，
  同 BaoguanWebServer/QdBWebServer 惯例，不依赖交互式登录会话）；防火墙 `QdB-WebServer-8093`（LAN 全网段）。
- **冒烟结果**：`/api/ping` 200；首页 200；真实黄金样本（EQ17/邦奇/华丰）全量上传验证，
  三份判定与 `data/golden/manifest.md` 记录的真值完全一致（EQ17 合格 得分98.80、邦奇/华丰
  不合格 一票否决，driving rule 分别为74/11）；audit hash-chain 校验通过。
- **门户接入**：AI 运营指挥中心门户（`1-转型规划/AI运营指挥中心/AI运营指挥中心-框架原型-v0.1.html`）
  质量域卡已从样例数据表替换为真实入口卡片（链接 + 试用版声明 + 反馈渠道），随命令中心
  `sync-to-server.ps1` 同步上线。
- **灰度范围**：陈忱（质量部，企微白名单内，反馈自动归档）+ 朱映桦（backup，暂不在白名单，
  经陈忱转）；一页 SOP 见 `1-转型规划/QD-B立项门禁试用版灰度SOP-2026-07-23.md`。
- **红线保持**：AI 预审建议非终局（disclaimer 显著标注）；上传件不入库（`reports/` gitignore）；
  B 类/C01-C10 报告页如实标注未实现，不伪装已判定；对客/对 ERP 均无自动外发/回写。
- **回滚**：`schtasks /End /TN QdBWebServer`（停）；`schtasks /Delete /TN QdBWebServer /F`（注销）。
- **部署副产品（顺带修复）**：命令中心 `CommandCenterWeb` 计划任务重启可靠性 bug（AtLogOn 交互式
  登录在 Administrator 会话"已断开"时无法启动新进程 + 裸命令"python"在 SYSTEM 账户下不在
  PATH）已一并修复为 SYSTEM+AtStartup+绝对路径，真实验证 End+Run 重启可靠，详见
  `1-转型规划/AI运营指挥中心/deploy-server.ps1`。
- **2026-07-27 重部署（队列 #116，报告页重排+Excel下载）**：`sync-to-server.ps1` 推送代码
  （`report_items.py`/`xlsx_report.py`/`webapp.py`/测试）+ 规则注册表，`Restart-ZhuopinTask`
  确认新进程存活（PID 480）；`/api/ping`/首页 200；真实上传华丰样本全链冒烟——报告页新增
  的②13模块得分率/③扣分明细/④全量82项双筛选/下载按钮均正确渲染，下载链接返回的 xlsx
  经 openpyxl 校验 3 sheet 齐全、评审明细 82 行、扣分明细仅含"待改进/不合格"两态。
  接口/端口/计划任务/防火墙/回滚 SOP 均沿用不变，无需重新注册。
- **2026-07-30 共享口令门禁上线（临时止血，队列 #160，Paul 直接派单，独立 worktree
  `four-services-temp-auth`）**：`.51` 四服务（本场景8093/保供看板8091/命令中心8092/FI2 8094）
  此前均零鉴权、绑 0.0.0.0，LAN 内任何人可见真实立项书财务数据——真实暴露。`webapp.py::create_app`
  接入 `zhuopin_platform.shared_tools.simple_gate.install_flask_gate`（HMAC签名Cookie 30天+
  X-Auth-Token程序化访问+登录页，密码环境变量 `ZP_GATE_PASSWORD` 未配置时自动 no-op）；`/api/ping`
  健康检查豁免。全量回归零漂移：QD-B 96 passed+25 skip（原91+5）。真实部署+curl验证：`/api/ping`
  放行、未登录302、Token/Cookie 均放行，且用保供看板(8091)登录的 Cookie 直接请求本服务成功（跨
  端口共享验证）。**非正式鉴权**——仅挡"随便谁都能打开"，正式身份走企微OAuth另待架构决策件；
  回滚：清空 `.env` 里 `ZP_GATE_PASSWORD=` 值+重启计划任务即恢复原状。详见队列 #160。
