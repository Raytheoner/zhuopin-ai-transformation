# QD-B 黄金基准 · 结构化清单

> 本文件是 `data/golden/` 的元数据索引（不含财务数字/OEM 报价，可安全入库）。
> 真实样本文件本身**不入库**（`.gitignore` 排除，含 OEM 报价附件/真实财务数据），
> 只存在于本机 LAN 目录，worktree 间不共享——续建 session 需从主工作区
> `4-数字员工/质量部/QD-B-立项审核门禁/data/golden/`（华丰）与
> `7-外部文档/质量部/产品类立项申请书及评审报告/`（EQ17/邦奇）拷入本地验证（同 2026-07-20 既例）。
> 引擎档位 vs 评审报告档位的回归验证见 `tests/test_golden_huafeng.py` / `tests/test_golden_product_class.py`。

| 样本 | 项目类型 | 模板版本 | 评审报告档位（真值） | 引擎档位（回归结果） | **基准得分** | 驱动规则 | 收口来源 |
|------|----------|----------|----------------------|----------------------|-------------|----------|----------|
| 华丰天然气发动机 EPA 认证服务咨询 | 技术服务类 | A2.1 | 不合格 | 不合格 | 96.44 | rule 11 项目结束日期未填写（一票否决），试评对账 55/55 条一致 | 收口-3 前既有样本，2026-06-27 起用 |
| EQ17 匹配 VM 发动机开发项目 | 产品类 | A2.1 | 合格（v5.0 评审报告） | 合格 | **100.00**（2026-08-25 起；此前 98.80，见下方变更记录） | 无阻断项 | 陈忱收口-3，2026-07-14 就位 |
| 邦奇新一代 TCU 合作开发项目 | 产品类 | A2.1 | 不合格（v5.0 评审报告） | 不合格 | 94.89 | rule 74 累计现金流末期阻断（相差 46.66 万，与 rule 69 现金流合计并发） | 陈忱收口-3，2026-07-14 就位 |

> 「基准得分」是引擎按 82 条规则重跑的总分，与 `tests/test_golden_product_class.py::TestCrossModuleZeroScoreDrift.GOLDEN`
> 是**同一份真值的两个副本**——改一处而不改另一处，等于黄金基准无声地移动了一半。
> 一票否决样本（华丰／邦奇）的档位由驱动规则决定，得分只作对账用。

## 基准变更记录

> 🔴 **黄金基准不得由实现方在变更包内直接改**（队列 §四 #83，Shao Peishen 2026-08-24 拍板 (b)）：
> 每次移动须先出《基准变更说明》（旧值／新值／依据／受影响样本穷举），经 Shao Peishen 签认后才更新本文件。

| 日期 | 样本 | 旧值 → 新值 | 档位 | 依据 | 签认 |
|------|------|-------------|------|------|------|
| 2026-08-25 | EQ17 | **98.80 → 100.00** | 合格 → 合格（**不变**） | 《基准变更说明——规则 12 ASIL 同义词集并入「无」》＝`docs/基准变更说明-规则12并入无-2026-08-25.md`；判据源＝陈忱 2026-08-21 回件 Q2（「/、NA、N/A、**无**、不涉及、不适用 均视同 NA」，并宣告词表封闭）；实现＝commit `4970e19`（`qd_b_gate/rules/vocab.py`，规则 12 与 C06 共用一份表） | **Shao Peishen 2026-08-25 签认通过**（队列 §四 **#112** 选 (a)，该行已销号；执行承接＝§一 #340） |

**这一条不是口径放宽，是修掉一条一直在报警的误判**——EQ17 的 `功能安全目标ASIL` 填的是 `无`，
旧同义词集里没有这个字，规则 12 判它「待改进」扣 1.20 分，而 EQ17 是**评审委员会判合格**的那一份；
那 1.20 分本来就不该扣。

**受影响面已穷举（全库只有 3 份已填立项书），before/after 双 checkout 实测对照**
（before ＝ `ac38bec`＝`4970e19^`，after ＝ `734ee18`，两侧跑同一支取证脚本）：

| 样本 | ASIL 原值 | 规则 12（before → after） | 总分（before → after） | 档位 | 一票否决驱动规则 |
|---|---|---|---|---|---|
| **EQ17** | `无` | 待改进(WARN) → **通过(PASS)** | **98.80 → 100.00** | 合格 → 合格（不变） | 无 → 无（不变） |
| 邦奇 | `A` | 通过 → 通过 | 94.89 → 94.89（**不变**） | 不合格 → 不合格 | rule 74 → rule 74（不变） |
| 华丰 | `/` | 通过 → 通过 | 96.44 → 96.44（**不变**） | 不合格 → 不合格 | rule 11 → rule 11（不变） |

**⇒ 三份样本里只有 EQ17 一份的分数移动，档位与一票否决驱动规则全部不变。** 空白模板（空单元格）
一路行为不变：旧新均判不合格，只更正了理由文案（「未勾选 ASIL 等级或填写不涉及」→「漏填」）。

## 回归标准（收口-3 + 极简版发布收口）

- **A 类确定性规则**：三份样本零偏差（引擎档位 == 评审报告档位）。
- **B 类语义 10 条 / C 类转人工 4 条**：MVP 占位版统一返回"转人工"，不参与一票否决——三份样本经验证均不因新增 C 类判定而漂移（rule 42/80/81 在三份已批复项目上均返回"转人工"而非否决，因签字/风险应对措施本就齐全）。
- **报告聚合**：`build_report()` 六段式报告如实标注"C01-C10 任务4未实现"，不空着不说明。

## 极简版发布收口新增验证（2026-07-23）

- 三份样本已通过本 worktree 内本地 Web 服务（`qd_b_gate/webapp.py`，端口 8093）上传-评估-出报告全链路验证，报告页判定与上表一致，详见场景 CLAUDE.md「部署状态」段。
