---
status: 在办
title: "SC8 进度编年 · CHANGELOG（场景级 CLAUDE.md 状态时间线迁移件）"
created: 2026-08-21
用途: 场景级 R5（OP-0821-B 判据 J5）——SC8 场景 CLAUDE.md 的「5. 状态时间线」只留最近一批，更早且已确认有承接载体的行迁入本件
关联: 4-数字员工/采购部/SC8-客户订单交期智能承诺/CLAUDE.md
---

# SC8 进度编年 · CHANGELOG

> 本文件是历史记录，内容不回溯改写。查最新进度请看同目录 `CLAUDE.md` 的「5. 状态时间线」。

## 2026-06-18 ～ 2026-08-07（首批迁移，2026-08-21，OP-0821-B）

> **迁移依据**：OP-0821-B 判据 J1 —— **迁出前须确认该行已有承接载体（队列行号，或 openspec 变更包／具名文件）；无承接载体者不得迁。** 本批 24 行全部通过 J1（每行至少点名一个队列 `#N` 或 `openspec/changes/` 变更包）。
>
> 🔴 **同期有 8 行未通过 J1，已刻意留在场景 CLAUDE.md 中，不得随后顺手迁走**：2026-06-10／06-24／07-02／07-06／07-14(B1+B3 部署)／07-15(A2 L/T 数据源)／07-15(C-1/C-2 apply)／07-16。其中 **06-24 行挂着「未结：7.2 LAN 真实联调验收」**、**07-06 行挂着「本次只搬代码、未接线到现有置信度流水线」**、**07-15(A2) 行挂着一个到 2027-03 才到期的长期跟踪项**——三者都没有任何队列行承接，迁走即丢。

| 日期 | 状态 |
|------|------|
| 2026-06-18 | 安全修复 A/B/C 并入 master（PR#10/#11/#12）：A2 submit_commitment 首道入队 / A1 TLS 校验 / B1 BOM fail-loud / B2 SRM 区分失败/未答 / C1 偏差监控 deviation.py / C2 真实黄金回归落地（3 张真实订单全低置信🔴，确定性偏差=0，real_frozen 不入库）。 |
| 2026-06-18 | SC8/SC1 真实口径收口（PR#13）：分层口径（/purchase/answer 主源）子件覆盖 9/90→58/90；S02Y.0188 瓶颈子件延期+61→+184（待 PMC 核实）。 |
| 2026-07-10 | **缺料/保供引擎口径改造**（跨桌任务队列 #17，`openspec/changes/shortage-baoguan-criteria-v3`，姚祖怡 07-10 缺料批改会圈选+现场会审 design 定稿）：新增 `sc8/period_match.py`（B2 周期累计供需匹配，会议现场重新定义——按"上次期望交付日次日→本次期望交付日"周期窗口累加 SRM 承诺，不满足时输出逐日可满足曲线，跨周期结转 carry_forward）；`sc8/sources.py` 新增 `load_material_commitments`（真实提取逐笔 SRM 承诺数量，替代原硬编码 `qty_committed=0`）；`BaoguanRow` 新增 `period_match` 字段（纯附加，`material_commitments` 缺省 None 时恒空、零漂移）；`build_dashboard` 新增 `priority_resolver` 框架桩参数（B4，PMC 优先级占用，仅接口未实现真排序）。**同批平台侧改动**（`zhuopin_platform`）：`get_purchase_orders` 新增真实 SRM 确认日期查询（A1，替换 `supplier_confirmed_date=expected_date` 占位）；`kit_engine.py` 新增 `filter_transit_by_arrival`/`bucket_shortage_by_lead_time` 纯函数（A1/A2，不改 `calc_shortage`/`explode_bom` 签名，O2/SC7 零影响）；`get_bom_for_products` **顺带修复生产活 bug**——按 BOM 主记录生效日期区间过滤当前版本，此前无条件取第一条，真实抽样 15 母件中 4 个/27%（S02Y.0035/S02Y.0162/S04Y.0112/S07Y.0137）因此取到过期 BOM 版本算齐套。**历史影响已排查（2026-07-14）**：姚祖怡核实这 4 个母件在受影响窗口（2026-06-24 上线真实 BOM 数据 ~ 2026-07-10 修复）内均无生产，实际风险=0，不发正式通知给 PMC/采购；若后续发现该窗口内确有实质订单量再走正式复核（队列 §四 #14）。全量回归：SC8 161+2skip / 平台167+1skip / SC1 53 / O2 20 / SC7 41（黄金基准 35850/640000/675850 精确不漂移），新增 39 tests 零回归。B1（多层递归）排期未定不做，C-1（主料替代料）随 openspec 批2 另案。L/T 数据源缺口登记跨桌任务队列 `#19`。真实数据 LAN 回归为独立后续任务。 |
| 2026-07-13/14 | **B1 多层 BOM 递归展开**（`openspec/changes/archive/2026-07-14-shortage-multilevel-bom-b1`）：姚祖怡批改 SC8净额开关底稿发现"半成品子件未二次分解"（S02Y.0035 瓶颈子件 R02A.0019 藏在未展开的半成品下，按期误判），Paul 现场确认"所有F开头需求的共性问题"、指示排期提前。`sc8/baoguan.py::_gross_need`/`sc8/forecast.py::estimate_material_arrivals` 改为复用 `kit_engine.explode_bom` 无条件递归展开半成品至叶子件（**方案迭代**：最初实现了"新开关`SC8_MULTILEVEL_BOM`+逐层现货抵扣"，开发中发现工作区已有预写测试规格描述更简单方案，经 Paul 确认改用无条件展开、不做净额、无新开关——`explode_bom_with_netting` 保留在平台底座作未来可选增强，本次未接入）。单层 BOM 场景结果与改造前完全一致（向后兼容）；半成品不再被误当作待答交物料查 SRM。全量回归：SC8 170+2skip / 平台175+1skip / SC1 53 / O2 20 / SC7 41（黄金基准精确不漂移），零回归。真实 LAN 环境多层取数性能/限流验证为独立后续任务。同批也排查确认第19-21行"按期误判"与本问题同根因，无需单独修复。 |
| 2026-07-14 | **`SC8_NET_INVENTORY` 翻 ON（净额开关黄金重核签字执行）**：姚祖怡企微回传《7/17 会材》预填件净额 5 项条件全勾+签字已填（`7-外部文档/采购部/`），Paul 当日与其线下二次确认将"D段"由"7/17同场签"改为"现在算数"——CC 执行：51 服务器 `.env` 追加 `SC8_NET_INVENTORY=on` + 严格重启验证（`schtasks /End` 这次真正杀掉了旧进程、无需手动 taskkill；新 PID 4044 CreationDate 与重启时间吻合）+ `/api/refresh` 全量重算。**核验结果与姚祖怡底稿预期吻合**：🔴真延期 32→17、🟢按期 0→3（命中 S02Y.0035，与底稿"3张转🟢"一致）；R01A.0061/R01A.0012（Paul 07-13 截图指出的"有货却判短缺"案例）翻 ON 后全量 116 行中不再作为任何成品瓶颈子件出现。IATF 翻 ON 治理留痕见 `7-外部文档/采购部/采购部-SC8净额开关翻ON执行记录-2026-07-14.md`。同批 Paul 二次确认另两点：B1 排期"分批"改"全量"（与已上线实现一致，无需回退单层）；A2 采购提前期 L/T 走"IT/SRM 自动取数"路线（姚曾提议的"人工可编辑字段"未采纳），跨桌任务队列 `#19` 路线定案、`#30` 归并关闭。**未做**：完整 A1-B4+B1 系统性真实 LAN 回归报告（本次只做 #3 收口所需的定向抽验，跨桌任务队列 `#24` 仍待领）；21 张成品保供黄金基准据新判级更新登记——**07-14 当日姚祖怡已对照线上看板抽验确认结果正确，此项视为完成**（详见执行记录 §四）。 |
| 2026-07-15 | **C-1/C-2 openspec 批2起草 + sync-to-server.ps1 重启可靠性修复（代码）+ #24 等效验收销行**：① 主料/替代料等价合并判缺料（C-1）+ 部分齐套显示（C-2）按 `保供看板v2-口径定稿.md` §2 生成 openspec 提案+design+3个spec delta+tasks 四件套（`openspec/changes/sc8-baoguan-substitute-partial-kit/`，`openspec validate` 通过），design.md 留 3 处技术决策（BomRow 新字段设计/替代料合并逻辑放场景层不下沉平台/C-2 与净额开关耦合）+ 3 个 Open Question（替代料 DTO 字段结构未经真实验证，已排为 tasks.md 第一步）待 Paul 审——**按项目流程本次只做到 design，未 apply、未写业务代码**。② `sync-to-server.ps1` 重启逻辑改为远程 PowerShell 轮询确认（端口真正释放才 `/Run`，新进程真正起来才算成功），修复 2026-07-14 观察到的 taskkill 有时不生效问题；⚠️ 本次施工环境无 LAN 访问 192.168.100.51，代码已过本地语法校验，真实部署验证留待下次有 LAN 访问的 session。③ 跨桌任务队列 `#24`（完整 A1-B4+B1 系统性 LAN 回归报告）Paul 拍板不再单独出，以既有定向抽验+姚祖怡看板整体抽验作等效验收，销行。 |
| 2026-07-15 | **①真实数据字段验证已补做，发现并修正一处真实错误**（Paul 回公司确认在 LAN 后，CC 用有凭证的环境对 7 母件/20 组替代料做只读实测）：替代料 DTO 确实自带独立 `m_usageQty`，但**恒为 1.0，与主件行真实用量（1/2/3/4/9/10/16 等值）无关，是 ERP 占位值**——design.md D2 原假设"替代料自带用量则优先用自己的"被证伪；已修正 `get_bom_for_products`（`5-平台底座/zhuopin_platform/.../erp_connector/connector.py`）：替代料恒继承主件行 `qty_per_unit`/`loss_rate`，不再采信替代料自身 `m_usageQty`/`m_scrap`。修复前的实现在主件行真实用量>1 的场景会严重低估替代料应有的展开需求量，进而可能得出"现货够、判齐"的错误结论——**是一个会导致真实误判的 bug，未上线前发现修复**。同步更新 `tests/test_bom_substitute_extraction.py`（1 个测试断言方向反转）+ `openspec/specs/platform-data-connectors/spec.md`（新增 Scenario）+ 归档 design.md（补充段记录验证过程与结论）。全量回归零漂移（平台193+1skip/SC8 188+3skip/O2 20/SC7 41黄金基准精确不漂移/SC1 53）。②③ 仍需姚祖怡本人参与判断，未做——20 组真实替代料样本已可直接作她的抽验素材，登记跨桌任务队列 `#33`。 |
| 2026-07-15 | **SRM 900401 阻塞解除 + `sc8-real-data-cutover` 归档**：真实只读实测确认携客云 `get_receive_board`（488 条真实记录）+ `get_confirmed_dates`（`/purchase/answer` 权威源，真实拿到确认交期）均可用，此前长期记录的"SRM 降级 mock（900401未开通）"已过时，已清理 `config.py::srm_source_mode`/`sc8/run.py`/`tests/test_real_integration.py` 里的过时说法，新增 `test_real_srm_schema` 真实集成测试（`SC8_RUN_REAL=1` 门禁）并跑通。**Paul 07-15 当场纠正一处过度保守的判断**：`config.srm_source_mode()` 默认值最初改成"仍 mock、生产显式opt-in"，机械照搬了 `SC8_NET_INVENTORY` 的"默认关"惯例——但复查后发现这个类比不成立：① 该开关唯一消费方是 `sc8/run.py`（小样本验证 runner），其中 FO/BOM 早就无条件真实、不受任何开关控制，SRM 单独挂 mock 默认只是 900401 阻塞期打的技术补丁，不是像净额开关那样需要专员签字复核的业务口径判断；② 部署运行的保供看板服务 `baoguan_service.py::compute_snapshot` 从来没有用过这个开关——SRM 调用一直硬编码字面量 `"real"`，900401 解除只是让这个早就是"real"的调用拿到更完整数据（`/purchase/answer` 权威源从此前部分受阻退化为看板辅助日期，恢复为权威源直接可用），并非从 mock 切到 real。**已改正**：`srm_source_mode()` 默认值改为 real（仍支持显式 `SC8_SRM_SOURCE=mock` 临时脱敏），`run.py` 注释、`test_sources_and_audit.py`（`test_srm_source_mode_defaults_real` 替换原 `_defaults_mock`，新增 `test_srm_source_mode_explicit_mock_override`）同步更新，SC8 全量 189 passed+4 skip，零回归。Paul 拍板"已并入主线的功能保留、未实现且过时的关闭"——`openspec/changes/sc8-real-data-cutover/`（6月建立、长期搁置、FO/BOM真实等有效成果已被后续变更包承接合入master）已归档 `archive/2026-07-15-sc8-real-data-cutover/`，委外真实路线识别（`is_outsourced_by_routing`）与凭证 Vault 化降级为独立 backlog（未做，非阻塞，现状维护清单兜底运行良好）。全量端到端 `run_small_sample`（FO+BOM+库存+SRM 全真实）烟测因 SRM `/purchase/answer` 30s/请求限流在单张真实订单的完整 BOM 上耗时过长（>15分钟未完成，非代码 bug，是真实限流叠加），已终止——**判定不必等它跑完**：真实数据管线的正确性已由更细粒度的 `test_real_fo_orders_schema`/`test_real_bom_schema`/`test_real_srm_schema` 三个独立真实测试分别验证通过，足以确认端到端真实数据链路可用。平台/SC1/SC7/O2 零回归（本行末次全量数字以 07-15 当天最后一次 SC8 CLAUDE.md 更新为准，见"当前"行）。 |
| 2026-07-20 | **`kittable_shortfall`（还差 N 件）口径改造**（跨桌任务队列 #63，源头 #33/#40 姚祖怡 07-16 企微回复）：姚祖怡确认口径＝"凑够客户下单总量（`so.qty`）还差多少"，推翻 design.md 07-15 原推荐的"凑够下一整套（`kittable_qty+1`）还差多少"（两口径数值差异大，总单量口径通常大得多）。`_kittable_qty` 瓶颈子件 shortfall 改按 `so.qty * qty_per_unit − avail`（下限 0，`best_qty`/`best_material` 判定逻辑不变）；字段注释/docstring 同步；`row_to_dict`/`_HTML_JS` tooltip 未硬编码旧口径措辞，核对后无需改动。TDD：`test_baoguan_partial_kit.py` 重写口径断言 + 新增"瓶颈子件已够撑满整单→shortfall=0"边界用例；`test_baoguan.py` 同步硬编码期望值。全量回归零漂移：SC8 190 passed+4 skip（+1）、平台 200+1skip、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。业务口径回灌 `1-转型规划/保供看板v2-口径定稿.md` §2 C-2·②+§5 裁决 5；design.md 归档件补"已实现"回填。 |
| 2026-07-22 | **姚祖怡灰度试用 11 条问题取证与 BOM 响应自证码防御修复**（跨桌任务队列 #66）：姚祖怡真实数据灰度试用报 11 条问题，其中 6 条疑似取数/映射 bug 用真实 API 重新查证均无法复现（并发批量拉取压测+刷新审计日志核查排除代码级串号/半坏快照）；发现 U9C `BOM/Query` 响应体自证码（`m_itemMaster.m_code`）此前从不核对，加校验兜底（不符即视为拉取失败、不静默采用，写审计留痕），5 新增单测，全量回归零漂移（`5-平台底座/zhuopin_platform`，commit `dcb287d`）。产出《保供看板取数取证结论与现状取值对照表》+《状态图例与判据说明》两份文档交姚祖怡确认，`.51` 复验通过。 |
| 2026-07-23 | **姚祖怡 V2 回灌落地**（跨桌任务队列 #87，源 `1-转型规划/缺料保供取值口径回灌-2026-07-23.md`）：①三处取值口径姚全确认现状即正确，无需改造；②判据 `NO_FEEDBACK_LEAD_DAYS` 30→90 天（Paul 07-23 拍板，姚反馈"30 天不符实际周期"）+ 新增无答复子件明细展示（`BaoguanRow.no_feedback_detail`：料号+毛需求量+估算到货日，`row_to_dict` JSON 键 `nfd`+HTML 卡片新增 `.nfd` 区块），`PARAM_VERSION` 同步 v0→v1（config.py 硬性要求：改启发式阈值必须 bump 版本号）；③命令中心 `:8092` 采购保供卡核实——确认是 2026-06 建的**静态样例原型**（硬编码占位数据/客户名，从未接 SC8 真实 API），"需备料"文案是原型自造标签，与 `:8091` 真实看板"待催"不同源，是姚困惑的根因，已按 QD-B 同款"真实入口链接"模式替换（详见部署段）；④#12-#15 新功能工作量评估见队列 #87 回写。TDD 全程：`test_baoguan.py` 新增/更新 6 个用例（30→90 日期断言更新 4 处 + no_feedback_detail 新增 2 处 + render_html 新增 1 处）。全量回归零漂移：SC8 193 passed+4 skip（+3）、平台 218+1skip、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。 |
| 2026-07-23 | **保供看板功能批1 建造+部署完成**（跨桌任务队列 #87④/§四#31，Paul 07-23 拍板批1=功能4项+#12+#14，交接见 `1-转型规划/开场prompt-保供看板功能批1-CC建造交接-2026-07-23.md`）：全部纯展示/派生列，**不改四色判定、净额、缺料计算口径**（红线守住）。① **PO 在途数据接入共享基础**——`sc8/sources.py::load_purchase_orders_by_material`（复用 `ZpConnector.get_purchase_orders`，按料号汇总 in_transit/partial 状态未清量）+ `config.po_transit_enabled()`（`SC8_PO_TRANSIT` 开关，**默认 ON**——与净额开关不同，本开关只影响纯展示列不改判据，无需专员重核签字；PO 端点异常时 `baoguan_service.compute_snapshot` **降级为无数据而非阻断整体重算**，与 FO/BOM/SRM 强 fail-loud 语义有意区分，已在代码注释明确记录该设计取舍）。② **#12 子件供给状态全展示**——新增 `ComponentSupplyStatus`（无在途/有在途未答复/既有在途又有答交/边界情况"有答交无在途"四态，交叉 PO 在途量 + 现有 SRM 答交判定），`BaoguanRow.component_status`，纯附加字段。③ **#14 需求日可齐套数量**——`_demand_kittable_qty`，与既有 C-2 `_kittable_qty` 同口径（全部直接子件+替代料等价合并），可用量在现货基础上叠加"到货日 ≤ 计划出货日"的在途 PO 量；**已知保守边界**：替代料本身不参与 `estimate_material_arrivals`（避免幻影组件误查 SRM，沿用既有设计），故替代料自身的在途量不计入本字段，只会低估不会高估。④ **料品名称（③）**——`BaoguanRow.component_names`，跨全部层级（含 B1 多层展开的深层叶子件）扫描整批组合 BOM 取 `component_name`，`no_feedback_detail`/子件供给状态/瓶颈子件标注均已接入。⑤ **功能4项 UI**——看板分页（10/50/100/200 行/页，默认10，`state.page`/`renderPager`）、导出 Excel（零依赖 HTML-table `.xls` 技巧，与既有导出 CSV 并列，导出范围=当前筛选全集）、图例入界面（`render_legend()` 动态嵌入当前 `no_feedback_lead_days` 等参数，看板内 📖 图例按钮展开/收起，内容与既有《保供看板状态图例与判据说明》一致）——均加入静态 `render_html` 与服务壳页 `webapp._shell_page`（共享同一份 `_HTML_JS`/`_HTML_STYLE`）。TDD 全程：新增 `tests/test_baoguan_po_transit.py`（20，component_names/component_status 四态/demand_kittable含替代料边界/row_to_dict 序列化/UI 元素静态冒烟）+ `tests/test_purchase_orders_by_material.py`（10，PO 聚合口径 + 开关默认值）+ `test_baoguan_service.py` 新增 3 例（真实数据流入/PO 源异常降级/开关关闭跳过调用）+ `test_baoguan_webapp.py` 补壳页新元素断言；全量回归零漂移：**SC8 226 passed+4 skip（+33）**、平台 218+1skip（零改动）、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**真实部署+真实数据验证**：`sync-to-server.ps1` 推送成功（`NEW_PID=5848`），`/api/ping`+首页+`/cases` 均 200，`POST /api/refresh` 全量重算成功（116 行：🔴24/🟠73/🟡0/🟢19）；抓取线上 `/api/baoguan` 快照核验新字段：116 行全部有 `dkq`（需求日可齐套已计算）、105/116 行有 `cst`（子件供给状态非空），真实样本含料号+真实品名（如 `KSDELPS1.22-TIVH-68-H3Q4`）+真实在途量+真实答交日期，字段口径与设计一致。**未做（按 Paul 拍板本批范围外）**：#13 需求关闭列（批2，结构独立，待姚祖怡定 3 口径：关闭状态存哪/谁有权关/部分关闭数量如何审计）；姚祖怡对线上看板 #12/#14/分页/导出/品名列/图例的抽验反馈仍待其本人确认（发布=部署供试用，非等同验收完成）。 |
| 2026-07-28 | **保供看板取数与展示修复批2（姚祖怡 07-28 判例回件，队列 #139，含 #128 并案）**：全部纯展示/派生列改造，**不改四色判定/净额/kittable_qty 计算口径核心**（红线守住，唯一例外见下方 #18-c 说明）。**① #17/#128 导出 Excel 去箭头**——子件明细行前的"↳"符号已彻底移除，层级区分改靠"前 11 列留空"这一独立列分组（本就存在，箭头只是冗余视觉提示，去掉不丢层级信息）。**② #18-a/b 答交数量/答交日期改为累计口径**——新增 `ComponentSupplyStatus.confirmed_batches`（`_cumulative_confirmed_batches`：按 SRM 供应计划确认日期升序累加确认数量，直至覆盖本行毛需求为止，可能产出多条 (日期,数量) 记录）；数据源改接 `sources.load_material_commitments`（供应计划看板逐期明细，姚祖怡截图所指的真实数据源），**不再单一依赖 `/purchase/answer` 的整单口径**（后者是本 bug 根因——PO 级单一 `vExpectedDate` 与分期排程的真实确认日期常不一致，如实测 R01D.0006 案例 `/purchase/answer` 给 2026-08-20，供应计划看板给 2026-07-20/3000）。旧 `confirmed_date` 字段保留供内部参考，前端渲染优先展示 `confirmed_batches`（`answerBatchesText()`），无数据时回退旧单一日期，零破坏性变更。`compute_snapshot` 新增 ⑦ 段接线 `load_material_commitments`，与 ⑥ PO 在途同一"纯展示派生列，失败降级不阻断"语义（各自独立请求、独立失败域，未做"共享同一次看板请求"式耦合，保持既有 monkeypatch 测试架构不变）。**③ #18-c 子件"未交订单"判定误判修复**——根因是 `load_purchase_orders_by_material` 的 60 天回溯窗口按 PO **创建日**（`makeDate`）过滤，误伤了创建久远但至今仍未清（未关闭/未结案）的真实在途 PO；真实案例 `ZPCG20251105008`（2025-11-05 下单，约 266 天前）R01D.0006 行 3,000pcs 已审核未清量因此被漏计，误判"无未交订单"。新增 `config.po_transit_lookback_days()`（`SC8_PO_TRANSIT_DAYS`，**默认 365**），`load_purchase_orders_by_material` 的 `days` 参数缺省改为读此配置（显式传参仍优先，向后兼容）。**⚠️ 已在代码注释与本行如实记录：这不是精确解**——真实探测证实 `ZpViewPurOrder/Query` 无客户端可读的"关闭/结案"状态字段（`status` 字段是审核工作流状态，全库 27,572 行中 99.5% 恒为 2，与完结与否无关），服务端也不做日期过滤（全量拉取，客户端二次过滤），故只能靠回溯窗口折衷；抽样统计显示全库跨各年龄段（60天/180天/365天/730天/1095天/1095天以上）均有大量"未清"状态的呆滞历史行（>1095天的仍有 2000+ 条），365 天覆盖了本次真实案例但未根治——彻底修复需 IT 补 PO 行级关闭状态字段，已登记为独立跟催项（性质与下方 #19 同类）。**④ #19 只取"核准"状态 FO——真实探测证实现有接口无法实现，本项未完成**：姚祖怡诉求"只取 ERP 预测订单状态=核准的项目，剔除已关闭的"，现有代码早已默认传 `status=2` 过滤（`webapp.py::fo_status` 默认值），但真实探测（两个独立真实案例交叉验证：`FO2026070001` 行60 `S02Y.0120`、行230 `S02Y.0166`，U9C 界面均清楚显示行级"状态"＝"关闭"，且行级状态与同单据其余"核准"行并存，证明 U9C 内部确有行级区分能力）证实 `/zp/api/ForecastOrder/Query` 返回的 `Status` 字段是**单据头**审核状态（`FO2026070001` 全部 90 行恒为 2，无一例外，与行级 UI 显示的"关闭"矛盾），接口**没有可读的行级关闭字段**——`status=2` 过滤对本诉求**完全无效**，非本次代码修改范围能解决。已更正 `sources.py`/`baoguan_service.py`/`run_baoguan_dashboard.py` 三处此前"传 status=2 即可剔除关闭"的过时/错误假设性注释（原假设从未经真实验证），标注为已证伪，避免误导后续开发；与队列 #80 发现的 `POChange/Query` 接口缺口同一类问题（IT 自建接口字段不完整），需 IT 补行级状态字段或等价查询能力，建议随该类接口缺口一并跟催。TDD 全程：`tests/test_baoguan_po_transit.py` 新增 9 例（箭头移除+cb 空/单批命中/多批累加/仅 confirmed 态计算+row_to_dict 序列化+HTML 渲染冒烟，含用姚祖怡截图真实数值复现的回归用例）、`tests/test_purchase_orders_by_material.py` 新增 6 例（`po_transit_lookback_days` 默认/覆盖/非法回退/下限 + 默认窗口生效 + 真实案例复现）、`tests/test_baoguan_service.py` 新增 2 例（material_commitments 接线成功/降级）+ `_patch_sources` 助手函数扩展默认 mock 该加载器（消除既有测试对真实网络的隐式依赖）。全量回归零漂移：**SC8 255 passed+4 skip（+16）**、平台 218+1skip（零改动）、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**真实部署+真实数据验证**：`sync-to-server.ps1` 推送成功（`NEW_PID=4616`），`/api/ping`+首页+`/cases` 均 200，`POST /api/refresh` 全量重算成功（117 行）；抓取线上 `/api/baoguan` 快照核验：R01D.0006（S02Y.0120 瓶颈子件）`tq`（未交订单量）从改造前的 0 变为真实 **6000**、`st` 正确判为 `transit_confirmed`（有未交订单已答交），③ 修复真实生效；`cb`（答交批次明细）当前为空——诊断证实并非代码缺陷，而是携客云 SRM 供应计划看板接口有"不允许查询当前时间 7 天之前数据"的官方限制（错误码 300234，与 `project_sc8_real_cutover` 记忆里 SC1 SRM 历史准时率同一约束）：姚祖怡截图的确认批次（2026-07-20/3000）如今已超过 7 天、无法再被接口重新读取以证明本次修复对该具体历史案例生效，但算法正确性已用她截图的真实数值在单元测试中完整复现（`test_component_status_confirmed_batches_single_match`），且对当前/未来任何确认日期落在未来 60 天窗口内的缺口物料，功能会在下次自然出现类似场景时正确展示。**未做**：#13 需求关闭列已由姚祖怡本人 07-28 判例回件明确**否决**（8/8 全否，见队列 #141，业务方案已定为"看板不建关闭功能，全部交由 PMC 在 ERP 侧完成，看板侧只需依赖④解决即可自动反映"，非本次范围内待办，而是④阻塞项解除后自动生效）；优先级占用扣减/多月逐期齐套仍属批2 ATP 引擎（队列 #118），本次未触碰。 |
| 2026-07-29 | **保供看板真实缺陷修复 + Paul 拍板改四色判定口径（姚祖怡 07-29 二次举证，队列 #147）**：姚祖怡坚持"两行出货日一样、料号形近"的旧解释（07-27 回信里给的初判）不成立，用真实截图证明**同一张卡片自己的数字互相矛盾**（S02Y.0135：徽标"确定齐料晚 +41天"，同卡片"出货→齐料"却是另一个日期对，算出 100+ 天）——**07-27 那次"看串行了"的判断是错的**，姚祖怡的坚持是对的。**根因**：卡片头部同时展示 `confirmed_gap_days`(仅确定承诺子件) 驱动的徽标 与 `gap_days`/`bottleneck_material`(全量口径，含无答复估算) 驱动的日期/瓶颈标签，无答复子件估算到货比确定承诺更晚时两套指标各自计算正确却指向不同日期/料号。**第一版修复**（仅展示层对齐、未改四色判定）已实现并部署，**但 Paul 复核后追问"头部徽标是否该取两者较大值"，进而拍板改四色判定核心口径**（此为业务决策，非 CC 自行判断——分级/严重度**改用 `gap_days`（全量，含无答复子件按 90 天保守估算），不再只看 `confirmed_gap_days`**：Paul 原话"看板就是现有信息的保守预测，任何没有答交就是要留90天才保险，并且在看板醒目预警"）。**最终实现**：`_classify()` 重写为以 `gap_days` 为主driver（>3→🔴，1-3→🟡，其余→🟢），措辞按瓶颈是否已答复区分"确定"（真实供应商承诺延期，建议协调到货/改期）vs"保守预警"（无答复子件估算延期，建议催供应商确认答交），颜色一致但不把估算包装成事实；🟠"待催"分支**结构性保留但当前生产参数下不可达**（no_feedback_n>0 时 gap_days 恒≥90>3，Paul 拍板"保留这个概念/UI，后续再优化"，不删除筛选按钮/KPI 统计）。卡片头部日期/瓶颈标签**改回统一用全量口径**`kit`/`bn`（与新徽标同源，`ckit`/`cbn`（确定口径）字段保留供 action 文案措辞判断用，不再是头部展示来源）。**顺带发现并修复两个真实缺陷**：① `_classify` 的"保守预警"措辞初版有多余逗号格式错误（"：，瓶颈子件"）；② **真实生产事故**——口径切换后首轮重算约 65 行同时从"待催"变"真延期"，`alert_dispatch.dispatch_new_reds` 循环内单次企微推送失败（真实触发 errcode=45009 API 频率限制）会让异常直接冒泡，**导致整个循环中断+`/api/refresh` 报错**，已建案的行不再补推、后续应处理的行完全跳过——改为单行推送失败只记 audit（新增 `baoguan_red_alert_push_failed` 事件）、不中断循环，案例正常建、其余行正常处理；`_alert_markdown`/建案 `confirmed_gap_days` 字段一并改用 `gap`（全量口径，与建案原因同源），措辞同样区分确定/保守预警。**已知后续项（未在本次做）**：`case_store.confirmed_gap_days` 字段名与 `case_draft.py` 客户草稿模板"确定延期约X天"措辞在瓶颈为无答复子件时不够准确，因对客闸全程关闭无实际外发风险，留独立后续任务。TDD 全程：`tests/test_baoguan.py` 新增/重写 7 例（含 `_classify` GAP 分支结构性保留的直接单测）、`tests/test_baoguan_netting.py`/`test_baoguan_partial_kit.py`/`test_baoguan_po_transit.py`/`test_baoguan_substitute_merge.py` 共 5 处既有断言从 RISK_GAP 更新为 RISK_RED（口径切换的直接后果，非回归）、`tests/test_baoguan_alert.py` 新增 5 例（gap 措辞区分/建案字段/推送失败韧性/audit 留痕）。全量回归零漂移：**SC8 266 passed+4 skip**、平台 218+1skip、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**真实部署+真实数据验证**：`sync-to-server.ps1` 两次推送成功（`NEW_PID=5600`→`8932`，第二次含韧性修复），全量重算后线上快照核对 S02Y.0135：`risk=red`、`gap=109`、`bn="R01E.0009"`（未答复瓶颈）、`action`="保供高风险（保守预警）：瓶颈子件 R01E.0009 尚未答复，按无答复估算晚出货 109 天，需紧急催供应商确认答交日期"——头部/徽标/措辞三者一致；`counts` 从 `{red:35,gap:65}` 变为 `{red:100,gap:0}`（65 行"待催"按拍板全部归入"真延期"，符合预期）；第一次重算真实触发企微限流报错、验证了韧性修复前的真实故障模式，修复后重算不再报错。**待办**：回复姚祖怡说明最终口径（两次徽标数字之争都源于同一展示缺陷，现已从根上改为"保守预测优先"）+ 请他抽验。 |
| 2026-07-29 | **保供看板批2 · 优先级分配引擎首版：跨订单共享现货池按优先级依次扣减（姚祖怡 07-29 二次举证，队列 #118/#147④）**：姚祖怡指出真实案例 S02Y.0210 被 3 张需求单（600/400/500 套，出货 07-20/08-20/09-20）共同争用现货 1,153 件，此前 `_kittable_qty`/`_covered_by_stock` 各自独立读取现货**全量**（每行各自算"库存现在够不够我这一单"，互相看不见对方），3 行都判"齐套"，但 600+400+500=1,500 > 1,153，实际第三行不该判齐——"必须逐级扣料"。**这不是纯展示层小补丁，是计算引擎架构级改动**：需要从"每行独立评估"改成"跨行共享同一个库存池、按优先级顺序依次分配"，属队列 #118「批2·优先级分配引擎」范畴，动工前与 Paul 确认了两点：① 是否现在就做（不必等 8 月上旬判例包）——**Paul 拍板现在做**；② 排序依据什么优先级——真实 PMC 优先级表（#15）尚未上线，**Paul 拍板临时用出货日期升序代替**（谁出货早谁先占，等 #15 上线后替换）。**实现**：新增 `_resolve_priority_order()`（有真实 `priority_resolver` 时用它决定顺序，否则按出货日期升序兜底）+ `_allocate_sequential_inventory()`（按优先级顺序从共享现货池依次扣减各料号毛需求，占用仅按需求数量不含富余——Paul 07-28 既有拍板口径——排在后面的订单只能拿到前面消耗后的剩余部分；只对"同一料号被2个及以上订单同时需要"的场景生效，未被竞争触碰的物料键原样保留调用方传入的库存全量值，不影响替代料/单一订单独占物料等既有行为）；`build_dashboard` 新增分配步骤，仅当 `SC8_NET_INVENTORY=on` 且传入 inventory 时生效（与既有净额开关同一入口，默认关时零漂移）。**`priority_resolver` 参数正式接线生效**——此前是 shortage-baoguan-criteria-v3（2026-07-10 会议）定义的 B4 框架桩（"本次只接受该参数、不调用/不实现真实排序逻辑"），本次是其**首次真正被消费**，为将来 #15 真实 PMC 优先级表上线预留了现成的接入点（届时传入真实 resolver 即自动切换，出货日期兜底自动失效，调用方无需改动）。**已知边界（未做）**：替代料等价合并（C-1）不参与本次跨订单分配（`_gross_need` 本就不含替代料行），已在代码注释记录为独立后续项，需先有真实替代料+多单竞争场景再评估。TDD 全程：新增 `tests/test_baoguan_sequential_allocation.py`（8 例：无竞争不受影响/双单按出货日期分先后/占用不含富余/`priority_resolver` 覆盖出货日期默认/物料无库存数据不参与分配/S02Y.0210 真实场景三行同构复现第三行不再误判齐套/净额开关关闭时完全不分配）。全量回归零漂移：**SC8 274 passed+4 skip（+8）**、平台 218+1skip、SC1 53、SC7 41（黄金基准精确不漂移，本次改动未触碰 SC7 复用的 `kit_engine` 底座）、O2 20。**首次真实部署验证即发现自身引入的真实生产 bug并当场根治**：`sync-to-server.ps1` 推送成功（`NEW_PID=1296`）、`/api/ping` 200、`POST /api/refresh` 全量重算成功，但抓取线上 S02Y.0210 真实快照核对发现**8 行 `kq`（可齐套套数）全部恒为 1153、`risk` 全部恒为 🟢，逐级扣料完全没生效**——与单测通过的结果矛盾。**根因**：`fo_to_sales_orders`（真实数据装配层）按 `fo.fo_id`（预测订单**文档级**单号）赋值 `so_id`，S02Y.0210 真实 8 行需求**全部共享同一个 so_id**="FO2026070001"（同一张 FO 文档下的不同行项）；首版 `_allocate_sequential_inventory`/`_resolve_priority_order` 用 `so.so_id` 当字典键/去重键，8 个订单对象被静默折叠成 1 条，跨行竞争检测形同虚设（此前所有单测用的都是各行 so_id 互不相同的简化场景，未覆盖这一真实拓扑，故未能提前拦截）。**修复**：两函数改用**下标**（在 `orders` 列表中的位置）定位具体行而非 so_id——`_allocate_sequential_inventory` 返回值从 `{so_id: 库存快照}` 改为按下标对齐的 `list[dict]`；`_resolve_priority_order` 同样改吃/吐下标，调用 `priority_resolver`（接口既有约定仍是 so_id 粒度）后按其返回顺序映射回下标，多个下标共享同一 so_id 时按原始相对顺序二级排序；`build_dashboard` 相应改用 `enumerate(orders)` 按下标取有效库存。新增 2 个单测直接复现"同一 FO 文档多行共享 so_id"的真实拓扑（`test_priority_resolver_same_so_id_multiple_lines_falls_back_to_original_order` + `test_real_case_same_fo_document_shares_one_so_id_across_lines`），**SC8 回归增至 276 passed+4 skip**，平台/SC1/SC7 重新确认零漂移。**修复后重新部署+真实数据复核**（`NEW_PID=3384`）：用独立诊断脚本直接复现 `build_dashboard` 内部计算（绕开 API 只看中间值）确认分配机制真实生效——① 隔离到 S02Y.0210 自身 8 行时，其内部共享料号 R01B.0009（单机用量 24，原始现货 255,834）按出货日期正确依次递减（255834→241434→231834→…→147834，与 8 行各自毛需求精确对应）；② 纳入全量 117 张真实订单一起竞争时，S02Y.0210 的瓶颈子件 R01D.0418（原始现货仅 5,767）在其排到之前就已被**出货更早的另一张单**（`S02Y.0120`，07-10 出货，单笔毛需求 9,920 > 全部现货）合法占满，致其 8 行 `kq` 现在**正确显示为 0**（不再是 bug 前的虚高 1153）——验证证实"现货是跨订单共享池"的口径在全量真实数据尺度下同样正确生效，且比 Paul 举例的"同一成品内部 3 行"场景更全面（覆盖了全公司层面的真实抢料）。**核心确认**：四色风险徽标由 `gap_days`（SRM 承诺口径，见 #147）驱动，与本次 `kittable_qty`/`kq` 的现货扣减是两条独立管线，本次改动不影响徽标判定，只影响"可齐套 N/M"这一现货侧辅助展示——已核对 Paul 报告的具体卡片元素正是该展示字段（`baoguan.py` 前端渲染第 923 行），确认改到了对的地方。**未做**：#15 真实 PMC 优先级表本身（独立任务，`priority_resolver` 接口已就绪等待接入）；替代料参与跨订单分配（已知边界，见上）。回复姚祖怡信（与 #147 合并一封）已起草待发。详见队列 #118。 |
| 2026-07-29 | **BOM 缺口物料清单八字段整体呈现 + 答交日期取错源头根治（队列 #152，姚祖怡三次重申：07-26 V6 #18-a → 07-28 判例回件 → 07-29 第三次，CC）**：根因不是哪条任务做错了，是拆法与验收单位错位——八字段散在 #118①（可用现货数量）/#151（缺口数量）/#139②（答交数量·日期）三处，从未有一条任务的验收标准是"八字段整体齐"，每次交付她都只看到补了一部分。本行补上这个缺失的整体验收单位。**④⑥ 可用现货数量/缺口数量**：`ComponentSupplyStatus` 新增 `available_qty`/`gap_qty` 字段，`_component_supply_status` 新增 `inventory` 入参（仅净额开关生效时由 `assess_supply_risk` 传入，取值＝该行订单在优先级分配后看到的每料号净额，与姚祖怡定义"现货净额−优先级排在本项目前已占用量"逐字一致）；`gap_qty≤0` 的子件在构造前即被过滤，不进入返回列表（#151，展示层过滤，不改 `mat`/`gross`/`kittable_qty` 等计算输入——已用直接单测锁住"过滤只影响展示、不影响计算"这条边界）。**⑦⑧ 答交数量/日期取数源头根治**：核实证实存在两个数据源（`confirmed_date`←`/purchase/answer` PO 整单口径，`confirmed_batches`←供应计划看板逐期明细，07-28 已改为主展示源）——但前端 `answerBatchesText()` 此前在 `confirmed_batches` 为空时会静默回退到 `confirmed_date`，而 SRM 供应计划看板对"7天前数据"有查询限制导致这个"回退"在生产中高频触发，等于"看起来像修好了却还在用旧口径"，正是三次重申的根因（抓取线上全量快照核验：7056 条子件展示记录里 4911 条/70% 当前 `cb` 为空、此前均在静默展示已知取错的旧值）。改为 `cb` 为空时如实显示"无"，不再回退（`answerQtyText`/`answerDateText` 取代 `answerBatchesText`），`confirmed_date` 字段仍序列化但只作内部排障参考。**呈现细节**：BOM 缺口物料清单从 flex 单行展示改为真 `<table>` 渲染（`.cst-table`），表格对齐+子件上下行对齐+内容过长自动换行（CSS `word-break`）均由真表格结构自然满足，"件·答交"分隔符随之消失（各字段独立成列）；导出 Excel 同步补齐可用现货数量/缺口数量两列。TDD 全程新增 9 测试（`tests/test_baoguan_bom_gap_eight_fields.py`：available_qty/gap_qty 计算+#151 过滤+展示计算分离+净额开关关闭零漂移+row_to_dict 序列化+八表头齐全+答交源头修复）+ 修复 3 处既有断言（新增 `aq`/`gq` 键/函数改名）。全量回归零漂移：**SC8 285 passed+4 skip（+9）**、平台 218+1skip、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**真实部署+真实数据验证**：`sync-to-server.ps1` 推送成功（`NEW_PID=8208`），`/api/ping`+首页+`/cases` 均 200，`POST /api/refresh` 全量重算成功（117 行）；抓取线上 `/api/baoguan` 快照核验 S02Y.0135/R01B.0115（姚祖怡原始举证案例）：`aq=161.0`/`gq=739.0`（900−161，正确）/`cd="2026-08-20"`（旧口径仍序列化）/`cb=[]`（真实复现"7天限制导致取不到"场景）——按新逻辑该行现在会显示"答交数量:无/答交日期:无"，不再是错误的 2026-08-20；全量 7056 条子件记录中 `gq≤0` 出现 0 次（#151 过滤在当前真实数据尺度下未被触发——数学上已证明这是既有 `_covered_by_stock`/`_drop_covered` 上游净额过滤的必然结果，本次新增过滤是面向未来变更的防御性保证，非本次可观测到的行为变化，白盒单测已直接覆盖其生效路径）。**待办**：请姚祖怡打开看板验收八字段是否齐全对齐、答交日期是否与 SRM 供应计划排产一致。 |
| 2026-07-30 | **四服务共享口令门禁上线（临时止血，队列 #160，Paul 直接派单，独立 worktree `four-services-temp-auth`）**：`.51` 上四个服务（8091本场景/8092命令中心/8093QD-B/8094FI2）此前零鉴权、绑 0.0.0.0，LAN 内任何人可见真实客户名/订单量/交期——真实暴露，本行即上方"待办#10"的止血收口。新增 `zhuopin_platform/shared_tools/simple_gate.py`（HMAC签名Cookie 30天+X-Auth-Token程序化访问+Flask `before_request`，密码环境变量 `ZP_GATE_PASSWORD` 未配置时自动 no-op），本场景 `webapp.py::create_app` 接入，`/api/ping` 健康检查豁免（deploy-server.ps1 本机检查/SC8 每小时后台重算线程本就不走HTTP，均不受影响）。全量回归零漂移：SC8 290 passed+4 skip（原285+5）。真实部署+curl验证：`/api/ping`放行、未登录302、`X-Auth-Token`放行、登录Cookie放行，且**用本服务(8091)登录的Cookie直接请求命令中心(8092)成功**（验证跨端口共享、iframe场景一次登录全通）。回滚SOP/口令交接详见队列 #160。**非正式鉴权**——只挡"随便谁都能打开"，不留访问痕迹、口令LAN内明文传输；正式身份走企微OAuth，另待架构决策件。 |
| 2026-08-03 | **姚祖怡 07-31 三问题 + 队列 #173 IT 前置解锁根治批（队列 #211/#212/#213/#173，Shao Peishen 08-03 拍板 §四#42 选(b)四件同批，CC，独立 worktree `sc8-quote-qty-date-batch`，openspec `sc8-quote-qty-date-batch`）**：本轮全程用真实 SRM/ERP 凭据做只读实测取证后再编码，不凭假设改字段。**① #211 答交数量/日期口径 v2**——真实探测 SRM 供应计划看板每条 record 自带 `receiveType`（此前从未读取）：用 R01B.0754 真实案例（姚祖怡截图错误值 500/2026-08-07）交叉验证，命中的正是 `receiveType=1`（按订单交货，挂 poErpNo）记录；同料号同时存在 `receiveType=2`（按排程交货，有 scheduleBatch）记录才是姚祖怡指定的唯一权威来源。`_extract_board_commitments` 新增 `receiveType==2` 筛选（范围仅限本函数驱动的⑦⑧答交展示，不改 `_extract_board_po_map`/四色风险判定既有口径，红线不动）。**顺带揪出第二个 bug**：`_cumulative_confirmed_batches` 累计目标此前用的是"本项目需求数量"（gross），姚祖怡原话明确应为"缺口数量"（gap_qty）——已改为优先用 gap_qty，净额开关关闭时退回 gross（零漂移）。**② #212 显示修复**——根因定位：`.cst-tag{white-space:nowrap}` 在 `table-layout:fixed` 窄列里，`confirmed_no_transit` 状态文案（含内嵌"（异常，如实展示）"注释，16字符）远长于其余三态（8字符），溢出盖住右侧"可用现货数量"列——正是姚祖怡红圈标出的两个字段"看不清楚"。改为 `.cst-tag` 允许换行 + 注释文字拆到独立 `.cst-tag-note` 元素，全站排查确认仅此一处组合有此风险。**③ #173 IT 前置解锁根治**——真实探测确认 IT 陈承 07-30 回件属实：`ForecastOrder/Query` 已补 `LineStatus`（当前 120 行中 106=核准/14=关闭，真实单号 `FO2026070001` 行60/行230 均为关闭，与 ERP 界面一致）；`loaders.py` 新增行级过滤。**PO 部分发现一个关键事实**：`get_purchase_orders` 现用的 `ZpViewPurOrder/Query` 端点**不带**行级状态字段（27,641行实测 0 命中），行级 `LineStatus`（0开立/1审核中/2未交清/3自然关闭/4短缺关闭/5超额关闭）实际暴露在**另一个端点** `Purchase/Query`（与既有 `get_purchase_lines` 同端点，此前只用于 FI2 三单匹配）——新增 `PurchaseOrder.line_no` 字段 + `ZpConnector.get_purchase_line_status()`（按料号批量查询，复用 `get_ap_lines_by_supplier` 已验证的批量分页模式）+ `load_purchase_orders_by_material` 按 `(po_id,line_no)` JOIN 剔除已关闭行（fail-soft，查询失败不连累既有数量口径）；**365天回溯窗口未退役**——两者互补非互斥。**真实改造前后对照**（当前真实 33 个料号抽样）：FO 120→106 行（14行关闭正确剔除）；PO 在途 20→19 个料号命中、合计 38070→37353 件，6 个料号数值变化，其中 `S01Y.0367` 从 50 件（数量口径误判"partial"）→ 0 件（真实已短缺/超额关闭）整个消失。**④ #213 替代料假缺口只读取证（未改任何判定代码）**——真实验证 F02N.0233 案例：其直接子件（14行，均包材结构件）无替代关系，替代料 R01A.0707↔R01A.0012 实际定义在其半成品子件 S02Y.0207 自己的 BOM 里（`get_bom_for_products(max_depth=2)` 逐字段核对与 ERP 截图一致，seq=120，qty_per_unit=19.0 两侧相同）；真实库存 R01A.0707=15,538/R01A.0012=2,945,049。根因＝`_substitute_groups(bom, so.item_code)` 只扫描 `product_id==so.item_code` 直属行，永远看不到嵌套一层的替代关系——该函数自身 2026-07-15 落地时的 docstring 已预留"未见真实需求前是独立后续任务"，本次即该真实需求。**与 #94 判定不同源、不合并**：#94 是 `pipeline.py::compute_forecasts`（SC8 交付承诺主流程）未过滤 `is_substitute` 行导致幻影组件误查 SRM，本次是 `baoguan.py::_substitute_groups`（保供看板）单层扫描漏判，文件不同、机制不同——#94 维持独立 P2，复核 `pipeline.py` 第55行确认过滤仍未加。影响面估算（真实 FO 数据 41 个不同料号）：17个F前缀/24个S前缀，S04Y.0112 等 S 前缀成品自身即 PCBA 层不受影响，F 前缀 17 个为上界估算非确诊数。修复本身触碰缺口/齐套判定红线，需专员重核黄金基准，留独立立项。TDD 全程新增 30 测试（`test_material_commitments.py`+2/`test_baoguan_bom_gap_eight_fields.py`+2/`test_fo_loader.py`+4/`test_purchase_orders_by_material.py`+6/`test_fi_connector.py`+4，另有既有 fixture 修复不计入新增），全量回归零漂移：**SC8 303 passed+4 skip（原290+13）**、平台 243 passed+1 skip（`test_po_srm_confirmed_date.py` 5 个失败经 `git stash` 验证为改动前既有失败，与本批无关——硬编码 fixture 日期 `2026-06-01` 随日历推移跌出 `get_purchase_orders` 默认60天窗口，纯粹是时间流逝导致，已 spawn_task 登记交独立 session 修复，不计入本批漂移）、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**真实部署+真实数据验证**：`sync-to-server.ps1` 推送成功（`NEW_PID=8964`），`/api/ping`+首页+`/cases` 均 200，`POST /api/refresh` 全量重算成功（106行，与真实 FO 过滤后行数精确吻合）；抓取线上 `/api/baoguan` 快照核验 S04Y.0112/R01B.0754：`cb=[]`（正确——该料真实确认批次日期 2026-12-25 超出 SRM 60天查询窗口，如实显示"无"而非错误的500/2026-08-07）、`cd` 仍序列化旧值供内部参考；`confirmed_no_transit` 状态在当前 106 行数据中出现 141 次（该分支被真实数据充分覆盖，非仅理论场景）；`.cst-tag` CSS 确认已改为 `white-space:normal`，`cst-tag-note` 元素在页面源码中存在。**收尾已完成**：采购部#10 跟进信（发布收口第8步）经 Shao Peishen 审核通过，已用企微机器人私信姚祖怡（含 docx 附件）+ 采购部群 webhook 通报，README 已回填 `✅ 已推送 2026-08-03 13:24 UTC`；队列 #211/#212/#213/#173/#94 五行结论已用协议〇.7 编辑锁写回（前次尝试因锁被 Cowork 质量专线占用而先落 `1-转型规划/session接力-采购域场景落地.md` 待补，锁释放后重新获取并完成回写，接力文件已标记处理完毕）。**后续独立待办**：#94 独立排期（一行 `is_substitute` 过滤修复，低风险，见队列 #94 行）；`_substitute_groups` 多层穿透改造留独立立项（含 17 个 F 前缀成品精确影响面核实，需专员重核黄金基准，见队列 #213 行）。 |
| 2026-08-03 | **A5 收口第②关补齐：分支 ff 入 master + 重新部署 + 端到端复现验证（队列 #228，值周巡检 P1 发现，CC 同一 session 续做）**：值周巡检发现上一行交付的分支未并 master（2 ahead/2 behind）、且从 master 视角看不到部署留痕，遂登记 P1。**① 实测判定（非采信推断）**：SSH 对 `.51` 远程 `sc8/{sources.py,loaders.py,baoguan.py}` 做 SHA256 逐文件哈希比对，与本机 worktree **逐字节完全一致**——证明巡检"未见留痕"的观察属实，但"可能未部署"的推断不成立：上一行结尾的部署（`NEW_PID=8964`）真实生效且持续在跑，姚祖怡任何时候打开看板看到的都已是修复后代码，不存在"生产仍是旧码"风险。**② rebase+ff 合入**：`git rebase master`（master 侧 2 个提交＝质量部#6发信+台账重跑，纯文档，零冲突）后 `git push origin <branch>:master` 快进远程（未 checkout 本机 master 工作区，沿用"远程直接快进"既有惯例）。**rebase 后全量回归一度失败**（`test_purchase_orders_by_material.py` 11 个失败）——**非 rebase 引入的真实回归**，根因是"全局 Python 共享环境 `pip install -e` 坑"（已知问题）：并发 session 把 `zhuopin_platform` 的 editable install 重新指向了主工作区，导致本场景 import 到缺 `line_no` 字段的旧版平台包；重新 `pip install -e .` 指回本 worktree 后全绿：SC8 303+4skip/平台243+1skip/SC1 53/SC7 41/O2 20。**③ 重新部署+冒烟**：`sync-to-server.ps1` 推送（`NEW_PID=380`，对应合并后 commit `777d4e0`），`/api/ping`+首页+`/cases` 均200，`POST /api/refresh` 成功（106行）；文件哈希再次核对一致。**④ 端到端复现验证——重要技术边界澄清（非失败）**：真实抓取显示 S04Y.0112/R01B.0754 当前 `cb=[]`（"无"），**既非**错误旧值 500/2026-08-07，**也非**姚祖怡给的正确值 14000/2026-12-25——根因是 `load_material_commitments` 查询窗口固定 `[今天,今天+60天]`（SRM 硬性单次跨度≤60天），而该确认记录 `boardDate=2026-12-25` 距今约144天，**结构上无法被"以今天为起点的60天窗口"覆盖，与代码对错无关**（同"7天前不可查"是同一类约束的反方向）。**已用独立探针证明修复逻辑本身完全正确**：手工查询 `[2026-10-27,2026-12-25]`窗口（跨度58天，合规）命中且仅命中一条记录——`receiveType=2`（按排程交货，权威口径）/`answerQty=14000.0`/`boardDate`转换后精确为`2026-12-25`——与姚祖怡数字逐位吻合，证明 receiveType 筛选与累计提取逻辑一旦记录进入查询窗口即会正确工作；**该窗口将于约2026-10-27自然打开，届时看板下次小时级重算即会自动正确显示，无需任何代码改动**。**⑤ 本行即部署留痕**。**⑥ 未起草时间差说明信**——①已证明部署从未落后，不存在"她在旧码期间看过"的时间差，无需该信。 |
| 2026-08-05 | **#223 客户草稿"确定延期"措辞修复（队列 #101①/#223/#208 同批 B 批，CC，独立 worktree `zealous-meitner-6a3325`）**：`case_draft.py` 生成客户改期草稿时，瓶颈子件若"无任何供应商答复"仍写死"确定延期约X天/预计调整至X前后"——把未知说成已知，一旦对客闸开闸会造成对客承诺口径失真。**新增 `SupplyCase.bottleneck_unanswered` 字段**（`case_store.py`，含旧库 `ALTER TABLE` 幂等迁移，兼容已有 `reports/*.db`）：`alert_dispatch.py::dispatch_new_reds` 建案时按既有 `_alert_markdown` 同款判据（瓶颈子件是否在 `nfd`/无答复明细里）自动写入；`webapp.py` 手动建案表单新增勾选项覆盖。`case_draft.py::_template`／`_build_prompt` customer 分支据此字段二选一措辞（"预计调整至…"vs"交期未确认…存在延期风险"），**只改对客口径，内部催货/协调模板不受影响**（红线守住，红线依据见 opener）。**风险等级低但不为零**：`CUSTOMER_OUTBOUND_ENABLED` 全程关闭无实际外发，本次是对客开闸评估前的提前量，**本批未部署 `.51`**（无客户可见影响、无发布收口触发条件，留待需要时随其它改动一并部署）。TDD 全程新增 10 测试（`case_draft`/`case_store`/`alert_dispatch`/`webapp` 各 2-3 个，含旧库迁移用例）。全量回归零漂移：**SC8 313 passed+4 skip（原303+13）**、平台248+1skip（同批 #208 修复后数字）、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。 |
| 2026-08-05 | **#262 答交数量/日期查询窗口固定60天根治（v3，姚祖怡 08-05 三度举证 `R01A.1028`，CC，独立 worktree `unified-portal-design-8a2ce3`）**：08-03 批次（#211/#212/#213/#173）留下的判断——"60天窗口外的确认记录窗口会自然打开、无需改动"——本次被证伪。姚祖怡明确"查不到就如实显示无"这个兜底业务侧从未认可过，要求"整体复核一遍"（不止修单例）。**根因真实坐实**：`load_material_commitments`（驱动⑦⑧答交展示）查询窗口固定 `[今天,今天+60]`，receiveType 筛选/累计算法本身正确，问题是窗口结构性排除更远期确认批次——真实探测 `R01A.1028`：旧窗口内仅未答交排程+一条按订单交货记录（按既有口径排除），延伸窗口 `[+60,+120)` 命中 `answerQty=10000/boardDate=2026-11-25`，与姚祖怡截图逐位吻合。**窗口分布探测**（决定 lookahead 取多大）：`[+120,+180)` 仍有 71 条已答交记录/65个料号，`[+180,+240)` 仅 1 条 record 0 已答交——180 天覆盖绝大多数真实答交、240天边际收益趋零。**修复**：新增 `config.material_commitment_lookahead_days()`（默认180天，`SC8_COMMITMENT_LOOKAHEAD_DAYS` 可覆盖）+ `sources._chunk_date_windows`（拆分≤60天不重叠窗口，SRM硬性单次跨度限制）+ `load_material_commitments` 分段查询后合并；范围仅限本函数，不影响 `load_srm_deliveries`/`_extract_board_po_map` 驱动的既有四色判定口径（红线不动，同 #211 v2 先例）。**限流权衡**：默认180天=3段查询，每次调用增加约60秒排队等待（1 req/30s 令牌桶），与同一 compute_snapshot 里 BOM 多层递归等步骤同数量级，可接受。TDD 全程新增10测试（`_chunk_date_windows` 边界/分段合并/远期复现/边界不重叠不重复计数 + config 4项）。全量回归零漂移：**SC8 323 passed+4 skip（原313+10）**、平台248+1skip、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**全量真实数据端到端复核**（范围含"整体复核一遍"）：1421个真实料号（FO+BOM真实拉取），264个（18.6%）在新窗口命中承诺记录；抽样31个料号逐条对照旧口径（60天）vs新口径（180天）——11个近期料号数值完全一致（零重复计数/零丢失），20个此前"无"的料号正确补齐远期记录；详见 `docs/queue_262_material_commitment_window_audit.md`（已入库，含完整对照表；原落 `reports/` 因 worktree 清理丢失、已改落此路径重建，见队列 #267）。**真实部署+端到端复现**（发信硬前置三条）：ff 合入 master（commit `654dc5c`）→ `sync-to-server.ps1` 推送成功（`NEW_PID=7540`）→ `/api/ping`+首页+`/cases` 均200 → `POST /api/refresh` 全量重算成功（103行）→ 抓取线上 `/api/baoguan` 快照核验 `F02N.0224`/`R01A.1028`：`cb=[{"d":"2026-11-25","q":10000.0}]`，**与姚祖怡截图正确值 10000/2026-11-25 完全一致**。 |
| 2026-08-05 | **#263 替代料关系多层穿透建造（队列 #263，随 #262 同批 D 批，Shao Peishen 08-05 拍板 (a) 先建后签，CC，独立 worktree `unified-portal-design-8a2ce3`）**：根因由 #213（2026-08-03）真实举证坐实——`_substitute_groups` 只扫描成品直属行，替代关系若定义在半成品子件自己的 BOM 里（真实案例 `F02N.0233`：`R01A.0707`↔`R01A.0012` 定义在半成品 `S02Y.0207` 自己的 BOM 里）则永远看不到，误判为缺口。**修复**：新增 `_bom_subtree_product_ids`（沿主料路径递归收集 product_id 及全部半成品子件闭包，与 `_gross_need` 的 `main_bom` 过滤同一口径——不沿替代料路径下钻）；`_substitute_groups` 改为跨闭包全部层级扫描，分组键从纯 `sequence` 改为 `(product_id, sequence)` 防止不同产品序号碰撞误并组；单层 BOM 场景（无半成品）退化为与改造前完全等价，零漂移。TDD 全程新增10测试（子树闭包/跨层级分组/序号碰撞/端到端复现 F02N.0233/单层零漂移）。全量回归零漂移：**SC8 333 passed+4 skip（原323+10）**、平台248+1skip、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**授权链**：她 08-05 回件"这个问题如果没有需要我们的决策项，请你按照现有的理解，直接建造完成修改"，但我方 08-03 信里明确告知修复会改变缺口/齐套计算结果、需她重核黄金基准签字，属授权前提不成立——Shao Peishen 08-05 拍板 (a)：开建，**签字改到改完之后做**，故本次交付强制含两份材料供她重核签字。**① 全量真实数据核验**：30个真实成品中11个（均F02N前缀，本次为实测确诊数，非#213当时的"17个F前缀"上界估算）替代分组因本次修复变化。**② 缺口/齐套数字对照**：如实记录 `_kittable_qty`（C-2）因既有架构限制（只扫描第一层直接子件，这11个成品第一层恰是半成品、不进真实库存）数字不变，非本次修复范围；真正受益的是 `_covered_by_stock`（驱动 BOM 缺口物料清单展示）——11个成品共数十个叶子件从"误判缺料"正确改判为"组合现货覆盖"。详见 `docs/queue_263_substitute_multilevel_audit.md`（已入库，含完整对照表；原落 `reports/` 因 worktree 清理丢失、已改落此路径重建，见队列 #267）。**真实部署+黄金基准重跑**：ff 合入 master（commit `19f1e39`）→ `sync-to-server.ps1` 推送成功（`NEW_PID=2480`）→ `/api/ping`+首页+`/cases` 均200 → `POST /api/refresh` 全量重算成功（103行），`counts={"red":98,"grn":5,"gap":0,"yel":0}`——**与修复前基准完全一致**（如实说明：四色由 `gap_days` 驱动，这11个成品此前就已因其它真实无答复料号判🔴，本次修复消除的是缺料清单里的假阳性，不是这些成品唯一的缺料问题，符合预期）。**抽样核验**：197项"料号×命中订单行"组合中195项（99%）已正确从 `nfd`/`cst` 缺料清单移除；**2项例外**（`F02N.0242`的`R01B.1126`两条qty=1000订单行）经诊断坐实为正确行为——4条订单行竞争同一料号（合计毛需求11600），组合现货（1846+3000=4846）不足以覆盖全部竞争订单，按既有跨订单优先级分配机制（`_allocate_sequential_inventory`，队列#118，2026-07-29落地）正确判定：出货更早的两条小额订单被满足，两条大额订单轮到时现货已耗尽仍正确判缺料——证明本次修复与既有跨订单库存分配机制正确协同，非遗漏。**待办**：向姚祖怡提交两份对照材料请其重核黄金基准并签字（跟进信合并 #262/#263 一并发送，见队列 §四#52 已拍板(b)）。 |
| 2026-08-06 | **队列 #110/#112 同批：看板反馈按钮+判例包网页表单化 + 四服务访问日志采集框架（Shao Peishen 08-06 拍板 (a) 先建采集、通道留白，CC，本 worktree `followup-dispatch-apply-25679f`）**：**#110 Feature A**——看板每行加 ✅正确/❌误判+原因 反馈按钮（`baoguan.py::card()` 新增 `feedbackHtml`/`bindFeedback`/`submitFeedback`），`POST /api/baoguan/feedback` 落 `reports/baoguan_feedback.jsonl`（新增 `feedback_store.py::JsonlAppendStore`，仿 `pending_queue.py` 骨架去掉幂等/状态翻转），只采集标注不改任何判据（红线守住）。**#110 Feature B**——判例包网页表单化，新增 `case_review.py`（`CaseReviewPackage`/`CaseItem`/`list_packages`/`load_package`/`render_review_page`）+ `webapp.py` 新增 `GET /cases/review`（列表）与 `GET/POST /cases/review/<package_id>`（表单/提交），07-28 硬约束全部满足（✏️自由文本与✅/❌独立记录、末尾不受约束自由补充区、支持追加"新增问题"条目）；判例包定义改为 Cowork 手写 JSON，落 `sc8/case_reviews/`（`.gitignore` 已开 `!sc8/case_reviews/*.json` 例外使其可入库，否则会被场景既有 `*.json` 通配规则静默吞掉）。**「每周五16:00机器人汇总报送」明确留白**——通道口径已被 Shao Peishen 同日拍板的 webhook 退役决策推翻（架构决策件 `3-治理与合规/通知通道架构决策件-webhook退役与aibot单一出口-2026-08-06.md`），新口径落点 #282 尚未开工，代码里不含任何发送触发，等 #282 收口后再接。**#112**——新增平台底座 `zhuopin_platform.shared_tools.access_log`（`AccessLogger`+`install_flask_access_log`），本场景 `webapp.py::create_app` 新增 `access_log_path` 可选参数接入，记时间/来源IP/method+path/status，排除 `/api/ping`，不采集个人身份；`run_baoguan_web.py` 生产端接线 `reports/baoguan_http_requests.jsonl`（与既有 `baoguan_access_trace.jsonl` 连接器痕迹语义不同，命名刻意区分）。**关键发现（本次建造前拦下的一次错误派单）**：派单原文写"发采购部群 webhook、勿重新设计"，读全行后发现该参数已被同日拍板推翻，且新口径落点 #282 未开工、代码侧无本地代发端点可接——按 webhook 建＝确定返工，越权代建 #282＝违规代做须走 design 审的架构变更，故本次范围收窄为"只建采集，通道留白"，与 Shao Peishen 会话内确认一致。全量回归零漂移：**SC8 360 passed+4 skip（原333+27）**、平台259+1skip（原248+11）、QD-B 83+25skip（+3）、FI2 110+7skip（+3）、命令中心14passed（+5）、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**真实部署+真实验证**：ff合入master（commit `774cdee`）→四服务`sync-to-server.ps1`推送成功（SC8`NEW_PID=6220`/QD-B`NEW_PID=2040`/FI2`NEW_PID=7756`/命令中心`NEW_PID=6440`，均2026-08-06 20:44-20:46创建）；真实`POST /api/baoguan/feedback`（200，`{"ok":true}`），SSH核验`baoguan_feedback.jsonl`真实落盘字段完整；`GET /cases/review`真实200（当前目录仅README无真实判例包，空列表页正确渲染）；`GET /cases/review/<不存在id>`真实404；四服务访问日志文件（`baoguan_http_requests.jsonl`/`qd_b_http_requests.jsonl`/`fi2_http_requests.jsonl`/`command_center_http_requests.jsonl`）SSH核验均真实落盘、字段完整、`/api/ping`确认零记录（豁免生效）。**顺带修复**：`1-转型规划/AI运营指挥中心/serve.py`一处潜在崩溃bug——`PORT`此前模块级读`sys.argv[1]`，import该模块时若`sys.argv`携带其它参数会崩溃（本次因需pytest下import才发现），已移到`__main__`块内。**如实登记边界**：判例包机制本身已用真实生产环境验证过路由行为（200/404），但尚无真实判例包内容跑过完整提交流程——Cowork至今未用新JSON schema起草过一次真实判例包，第一次真实使用时若字段理解有偏差需回来微调，非阻塞但如实留白。详见队列 #110/#112、`sc8/{feedback_store.py,case_review.py,case_reviews/README.md}`、`5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/access_log.py`。 |
| 2026-08-07 | **队列 #296/#297 同车：答交口径 v4 三态判据 + 替代料并列展示（姚祖怡 08-06 第四次举证，CC，独立 worktree `sc8-supply-board-delivery-a6ae5b`，openspec `sc8-baoguan-answer-tristate-substitute-display`，Shao Peishen 已审 design 通过）**：她首次书面给出完整 SRM 单据状态三态判据（差异已确认/无差异/待答交）——此前从未定义过，此前四轮修复都在猜。**#296 根因真实坐实（2026-08-07 生产凭据只读实测，穷举763条record/1302条item全字段）**：SRM 供应计划看板原始 API **没有**独立"单据状态"字段，三态由 `answerQty` 是否为 `None` 与 `planQty` 的关系派生——`None`＝待答交，非`None`（含`0`）＝已答交。**核心 bug**：`sources._extract_board_commitments` 此前 `int(answerQty or 0); if <=0: continue` 把"待答交"（None）与"已答交=0"混为一谈一并跳过（真实统计：365天窗口内223个item是`None`、669个是`0`）——已修复为只在`is None`时跳过。**状态列判定改挂同一数据源**：`baoguan._component_supply_status` 的"已答交/未答交"此前来自 `mat.no_feedback_materials`（旧`/purchase/answer`管线），与驱动⑦⑧展示的 `material_commitments`（`receiveType==2`管线）是两条独立管线，产生"答交数量显示无、状态却显示已答交"的矛盾（真实案例`R02A.0019`）——改为`confirmed`直接由`material_commitments.get(m)`是否非空派生，仅当该参数整体加载失败（`None`）才降级回退旧口径。**365天窗口**：`material_commitment_lookahead_days()`默认180→365（她明确要求），真实测算6段查询（非估算"约7段"），比180天多约90秒排队，判定可接受（同180天口径落地时的论证同量级）。**🔴 真实端到端验证过程中额外发现并修复同族第二层缺陷**：修完上述两处后，第一轮真实全量快照显示`R01D.0015`状态正确变"已答交"但答交数量仍显示"无"——根因是`_cumulative_confirmed_batches`自身也有`if q<=0:continue`，此前从未被触发（上游从不产生q=0记录），2.1的修复第一次让它变成活跳的真实缺陷；已修复为只过滤负数。真实规模：113个不同料号的`cb`此前**全部**由q=0记录构成、会被误显示"无"，`R01D.0015`只是其中之一。**#297（替代料并列展示，不改判定）**：`ComponentSupplyStatus`新增`role`字段，`_component_supply_status`对已在缺口清单中的主料行追加独立的替代料展示行（沿用主料需求量，独立算状态/现货/缺口/答交），前端标注"（主料）"/"（替代料）"。TDD全程新增24测试，全量回归零漂移：**SC8 377 passed+4 skip（原360+17）**、平台259+1skip、SC1 53、SC7 41（黄金基准精确不漂移）、O2 20。**两轮真实全量快照验证**（真实凭据，`SC8_NET_INVENTORY=on`，耗时938.7s/973.4s）：四色判定`counts`逐字段完全一致`{"red":102,"gap":0,"yel":0,"grn":5}`，n_rows=107，证明两项改动均未触碰判定红线。**指定案例真实复现**：`R01D.0015`从错误的"无"变为正确的"0、0"；`R02A.0019`从错误的"已答交"变为正确的"未答交"；她原始举证订单`F02N.0242`当天已不在活跃FO快照（订单逐日变化，非缺陷），但同一组真实物料`R01A.1459`/`R01A.1545`在今日活跃订单`F02N.0254`下正确并列展示，替代料真实现货**254**与她截图完全一致。全量真实数据规模统计与30组抽样对照表详见`docs/queue_296_material_commitment_tristate_audit.md`与`docs/queue_297_substitute_display_audit.md`。**真实部署+生产级复现**：ff合入master（commit`c1bc065`）→`sync-to-server.ps1`推送成功（`NEW_PID=8484 CREATED=2026-08-07 10:27:20`）→`/api/ping`+首页+`/cases`均200（带`X-Auth-Token`）→`POST /api/refresh`全量重算成功，`counts`与本机验证完全一致→抓取线上`/api/baoguan`快照核验`R01D.0015`/`R02A.0019`/`R01A.1459`/`R01A.1545`四个真实值均与本机验证一致。**待办**：起草跟进信（含§四#55索签决策点，见队列§四#52）提交Shao Peishen审。 |
