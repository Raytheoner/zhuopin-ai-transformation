# 姚祖怡 2026-08-28 回件落地底稿（`OP-0828-U`）

> **来源**：`1-转型规划/0-全景路线图/拆件-质量部10与采购部19回件-2026-08-28.md` §一（`采购部#19` 回件逐条提取，**已含 2026-08-28 对 §一⑹ 的更正**：第 5、6 件他都签认了，不是「未答复」）。
> **本件范围**：① 判例 4 真实案例取数 ② 判例表 A 三条读数口径的实现核实与差异清单 ③ 保供看板新阈值两种落法的行数分布 ④ 责任人列口径落档 ⑤ 规则 1「开起来」的可行性与前后对照。
> **本件不做**：不改任何生产代码、不对外发送任何消息。差异只列不改，等 `采购部#20` 一起走。
> **复跑**：`SC8_NET_INVENTORY=on python scripts/find_insufficient_cumulative_cases.py --cache <仓库外.json> --out <仓库外.md>`；本次的真实输入已冻结，**换算法/换开关重跑零额外 API 请求**。

---

## 〇、🔴 先说数据是从哪来的 —— 本次没有用 `.51` 的快照，理由与取证

**2026-08-28 15:40 本地实测，本机 off-LAN**（判据取 `LAN留步项捞出清单-2026-08-26.md` §〇 定下的那三项，缺一不算通）：

| 探针 | 结果 | 判读 |
|---|---|---|
| `ping 192.168.100.51` | **PingSucceeded=False** | ✗ |
| `.51:8091/api/ping` | **HTTP 502**；`-NoProxy` 直连则**超时** | ✗ **502 是本机代理给的**，不是服务给的 |
| 路由 `Find-NetRoute 192.168.100.51` | 走 **`Mihomo`** 接口（`198.18.0.1`，clash-verge TUN） | 本机 WLAN 段是 `10.11.41.157/22`，与 `192.168.100.0/24` 不通 |
| SMB `192.168.100.51\c$` | 超时不可达 | 连门禁口令 `ZP_GATE_PASSWORD` 都取不到 |

⇒ **`.51:8091` 的 `/api/materials`（`find_cumulative_evidence.py` 走的那条路）本次用不了。**

**但源系统可达，且是真的可达**（同样按「不能只看状态码」的判准核过）：

| 端点 | 结果 |
|---|---|
| `https://erp.equalitytec.com:4443/` | **HTTP 200**，`Server: Microsoft-IIS/10.0`、`X-Powered-By: ASP.NET`，**无 `Proxy-Connection`** ⇒ 真服务器答的 |
| `https://openapi.xiekeyun.com/` | HTTP 404（端点级路由）⇒ 可达 |
| 凭据实跑 | FO 预测订单 **103 行**（3.3s）／SRM `get_receive_board` **720 行**（5.8s） |

⇒ **改走「从源系统直接重算」**，新脚本 `scripts/find_insufficient_cumulative_cases.py`，与 `compute_snapshot` 同源同顺序。

### 🔴 本次口径缺口（每次引用下面的数字都要连这句一起引）

**未跑 `/purchase/answer`，`srm=[]`。** 该端点是进程级令牌桶 **1 req/30s**，几百个 `(PO, vendor)` 对 ＝ 数小时，且会把携客云配额抢光、连带拖慢 `.51` 上每小时重算的生产服务（`compare_kit_date_cumulative.py` 的注释里明确警告过这件事）。

- ✅ **不受影响（是真数）**：判例 4 的**料号／需求量／逐笔答交明细／累计差额／该子件三种算法下的到货日**。
  **依据不是「差别应该不大」，而是调用图**：`forecast._arrival_by_cumulative_qty` 在「有逐笔记录但累计不覆盖」这一支上**根本不读 `legacy_date`**（`legacy_date` 只在 `target_qty <= 0 or not commitments` 时用得上），而 `srm` 的唯一作用就是喂 `legacy_date`。物料看板的 `total`／`cb` 同理——它们只吃 `gap_qty` 与 `commitments`。
- ⚠️ **属变体口径（不得作为对姚祖怡的承诺数字）**：**行级齐料日、四色计数、阈值分布**。等价于 `compare_kit_date_cumulative.py` 里量过的「变体 W」。
  **本次给出一条同向旁证、但它不是证明**：变体口径下规则 1 ON 的四色 ＝ **66 / 29 / 7 / 1**，而 2026-08-26 `.51` 生产实测是 **71 / 27 / 7 / 0**（🟡 那一格逐格相同）。**接近，但不等于**。

### 另两处如实登记的取数残缺

1. **BOM 拉取 35 个料号失败**（连接器自带告警 `齐套可能残缺，对客承诺前须人工核对`），含成品 `F02N.0220`／`F02N.0225`／`F02N.0233` 等。
2. **规模比 08-26 小一圈**：本次 **103** 预测订单行／**1413** 叶子件／**423** 物料看板行，08-26 生产是 **105**／**1606**／（08-20 时 596）。⇒ 上面那句「66 vs 71 接近」里，**有一部分差异来自样本本身就小了一圈，不能全算到 `srm=[]` 头上**。

---

## 一、🔴 判例 4 真实案例 —— 他点名要的那个例子，取到了

### ⑴ 物料看板粒度（＝他 08-26 信里「某个料三个月要 15,000」那种说法）

**命中 108 / 423 行**满足「有逐笔答交、累计仍不足三个月合计缺口」。取正数答交笔数最多的前 5 条：

| # | 料号 | 品名 | 三月合计缺口 | 逐笔答交（全部） | 累计答交 | **还差** |
|---|---|---|---:|---|---:|---:|
| 1 | `R01D.1317` | SI2310AHE3-TP | 15,429 | 09-20 给 0 ＋ 10-20 给 0 ＋ **11-27 给 3,000** ＋ **2027-01-29 给 3,000** ＋ **2027-03-08 给 9,000** | 15,000 | **429** |
| 2 | `R01D.1255` | MMBZ33VAQ-TP | 188,129 | 09-20 给 0 ＋ 10-20 给 0 ＋ **10-26 给 84,000** ＋ **11-20 给 39,000** | 123,000 | **65,129** |
| 3 | `R01I.0007` | AUIR3241STR | 47,661 | **09-20 给 15,000** ＋ **10-20 给 10,000** | 25,000 | **22,661** |
| 4 | `R02D.0001` | 密封胶 7091 白色 | 127,252 | **09-20 给 102,920** ＋ **10-20 给 4,340** | 107,260 | **19,992** |
| 5 | `R01D.1237` | BAV99-QR | 69,334 | **09-20 给 33,000** ＋ **10-20 给 30,000** | 63,000 | **6,334** |

📌 **第 1 条 `R01D.1317` 是最好用的一条**：答了整整 **3 笔正数**（3,000＋3,000＋9,000＝15,000），却仍差 **429** —— 差得极少，正好说明「判为无答交」这个处理**不是因为供应商没答，而是因为答的量还差一点点**。他一看就能判这样合不合理。

### ⑵ 成品行子件粒度（齐料日**真正**的计算粒度）

**命中 95 条（成品行 × 子件），涉及 19 个料号、23 张成品卡片。**

📌 **一处必须一并向他说明的现象**：⑴ 与 ⑵ 命中的料号**基本不重合**。原因是累计目标不同——物料看板按**三个月合计缺口**累计，成品卡片按**这一张单的缺口数量**累计。**同一个料在物料看板上「答了但不够」、在某张成品卡片上却可能「够了」，这是对的**，与页面「取数说明」第 3 条讲的是同一件事。

**案例 A · `R01A.0103`（AC0603JR-070RL）—— 本行瓶颈，最有说服力的一条**

- 预测单 `FO2026080004` ／ 成品 `S02Y.0166` ／ 计划出货日 **2026-11-20**
- 本行毛需求 **50,000**，可用现货 **0** ⇒ 缺口数量（累计目标）**50,000**
- 逐笔答交：`2026-09-20` 给 **0**　`2026-10-20` 给 **0**　`2026-12-20` 给 **30,000**
- **累计 30,000，仍差 20,000** ⇒ 判例 4 那种情形；**该子件就是本行的瓶颈**

| 算法 | 规则1 OFF（生产现状）子件到货日 | 该行齐料日 | 规则1 ON 子件到货日 | 该行齐料日 |
|---|---|---|---|---|
| **现行**：max(估算日, 最晚一笔正数答交日) | **2027-02-18** | 2027-02-18 | **2027-02-18** | 2027-02-18 |
| 算法 B：取最早那笔答交日 | **2026-12-20** | 2027-02-18 | **2026-12-20** | 2027-02-18 |
| 算法 C：只取估算日 | 2027-02-18 | 2027-02-18 | 2027-02-18 | 2027-02-18 |

**案例 B · `R01A.0359`（AC1206FR-076K2L）—— 唯一一条三算法真的分成三个日期的**

- `FO2026080004` ／ `S02Y.0166` ／ 出货日 **2026-12-20**；毛需求 24,000、现货 0 ⇒ 目标 24,000
- 逐笔答交：`09-20` 给 0　`10-20` 给 0　`12-20` 给 **10,000** ⇒ **累计 10,000，仍差 14,000**

| 算法 | 规则1 OFF 到货日 | 该行齐料日 | 规则1 ON 到货日 | 该行齐料日 |
|---|---|---|---|---|
| **现行** | **2027-03-20** | 2027-03-20 | **2026-12-20** | 2026-12-21 |
| 算法 B | 2026-12-20 | 2027-03-20 | 2026-12-20 | 2026-12-21 |
| 算法 C | 2027-03-20 | 2027-03-20 | **2026-12-19** | 2026-12-21 |

其余三条（`R01D.0041`／`R01B.0014`／`R01A.0108`）形态相同，全文见 `scripts/find_insufficient_cumulative_cases.py` 的输出。

### ⑶ 🔴 取数之后才看得出来的一件事 —— 信里那句话需要微调

`采购部#19` 写的是：**「另外两种可选的算法（取最早那笔的日期／只取估算日）都会把齐料日算得比实际早」**。

**实测（5 条真实案例逐条核）**：

- **算法 C（只取估算日）在这批案例里几乎等于现行** —— 因为「最晚一笔正数答交日」**全部早于**估算日，`max()` 取的就是估算日。5 条里 4 条**完全相同**，第 5 条（案例 B 规则1 ON）只差 **1 天**。
  （数学上 `现行 = max(估算日, 最晚正数答交日) ≥ 算法C` 恒成立，所以那句话方向没错；**但「早」的幅度实测是 0～1 天，不是一个会改变判断的量**。）
- **真正会把齐料日算早的只有算法 B**：它把到货日拉到 `2026-12-20`——**而那一天累计才到 30,000／50,000**，缺口还差 20,000。**算法 B 等于用一个「其实还不够的量」对应的日期去当齐料日**，这正是信里担心的「错得危险」。

⇒ **给他看的时候，重点应该是「算法 B 会早 2–6 个月且早得没道理」，而不是「另两种都会早」。** 这一处措辞在 `采购部#20` 里应如实更正一句。

---

## 二、判例表 A 三条口径：实现核实与差异清单（🔴 只列不改）

他 2026-08-28 对判例表 A 三条**全部勾 ✅**。逐条核实现有实现（代码为准，非目测页面）：

### 判例 1 · 跨卡片缺口按出货月合计；窗口外缺口只在**页脚**单独提示

**引擎层 ✅ 一致**：`sc8/material_board.py::build_material_board` —— 按 `BaoguanRow.ship_date` 所在自然月归集；窗口外缺口进 `slot["out"]`、**不计入任何月度列与 `total`**；缺口全落窗口外的物料**不列行**，改记 `out_of_window_materials` / `out_of_window_qty`。单测已守（`tests/test_material_board.py` 两条）。

**展示层三处与他签认的那句话对不上**：

| # | 差异 | 位置 | 严重度 | 拟修法 |
|---|---|---|---|---|
| **D1** | 🔴 **Excel 导出里窗口外缺口完全没有提示** —— 既无逐行「窗口外 N」标签，也无「另有 N 个物料…」那句，更没有窗口外列 ⇒ **拿到导出件的人会把「总缺口」读成该物料的全部缺口** | `sc8/webapp.py::exportExcel`（`cells` 数组不含 `r.out`） | **高**（三条里唯一有实质误导风险的） | 导出表追加一列「窗口外缺口（不计入总缺口）」＋表尾一行汇总说明 |
| **D2** | 「另有 N 个物料的缺口全部落在窗口之外」这句**只写个数、不写数量** —— `meta.out_of_window_qty` 已算出并随载荷下发，页面从未显示。而判例 1 里他关心的正是那个 **1,516,800** | `sc8/webapp.py` 第 503-504 行 | 中 | 那句后面补「（合计 X）」 |
| **D3** | 位置是**表头上方的统计行**（`<div id="cnt">` 在 `#tblBox` 之前），不是页脚；逐行窗口外量则是**料号列的行内标签**。他签认的措辞是「**只在页脚单独提示**」 | `sc8/webapp.py` 第 608 行 | 低 | 两选一：挪到表下方，或在信里说明「我们放表头上方且逐行也标了，比页脚更显眼，是否保留」请他确认 |

🔑 **D3 属于「做多了」而非「做少了」**，D1／D2 才是真缺口。**建议 `采购部#20` 只报 D1／D2 的修复，D3 用一句话请他确认保留**——他签的是口径，不是像素位置。

### 判例 2 · 多供应商全部并列 —— ✅ **完全一致，无差异**

页面 `supList(r)` 返回 `r.sup` 全量、逐值一行；Excel `supText(r)` 用 `、` 连接**全部**、不截断；引擎 `"sup": list(supply.get("suppliers") or [])` 取自 `load_purchase_supply_by_material`（去重＋字典序）。

### 判例 3 · 无未交订单时显示「无未交订单」 —— ✅ **完全一致，无差异**

页面与 Excel 共用 `supText`／`supList`，空数组一律渲染成 **「无未交订单」**，与品牌／责任人的 `—（取数缺口）` **刻意分开措辞**；单测已守（`tests/test_baoguan_webapp_materials.py:112`）；页面「取数说明」第 6 条已写明两者区别。

---

## 三、🔴 保供看板分级阈值 —— 两种落法的行数分布，以及一条他一定要知道的结论

他 2026-08-28 定的新阈值：**🔴 晚 2 周以上／🟡 晚 2 周以内**。**他只给了两档，🟠 与 🟢 怎么落他没说，下面不替他补。**

### ⑴ 规则 1 **OFF**（＝他 08-28 打开看板时看到的那块板子）

| 桶 | 判据 | 行数 |
|---|---|---:|
| A | 晚 **>14 天** ⇒ 他定的 🔴 | **84** |
| B | 晚 **1–14 天** ⇒ 他定的 🟡 | **0** |
| C | 不晚（≤0）且有未答复子件（现行 🟠） | 0 |
| D | 不晚（≤0）且全部已答复（现行 🟢） | 0 |
| E | **无 BOM**（现行一律 🔴，与晚不晚无关） | 18 |
| F | 有 BOM 但无待到货子件（齐料日为空，现行落 🟢） | 1 |

| 落法 | 🔴 | 🟡 | 🟠 | 🟢 |
|---|---:|---:|---:|---:|
| ① 只保留红黄两色 | 102 | 1 | — | — |
| ② 红黄按他定，橙绿维持现行 | **102** | **0** | 0 | 1 |

### ⑵ 规则 1 **ON**（他已签认「开起来」）

| 桶 | 判据 | 行数 |
|---|---|---:|
| A | 晚 **>14 天** ⇒ 🔴 | **48** |
| B | 晚 **1–14 天** ⇒ 🟡 | **7** |
| C | 不晚且有未答复子件（现行 🟠） | 29 |
| D | 不晚且全部已答复（现行 🟢） | 0 |
| E | 无 BOM | 18 |
| F | 有 BOM 但无待到货子件 | 1 |

| 落法 | 🔴 | 🟡 | 🟠 | 🟢 |
|---|---:|---:|---:|---:|
| ① 只保留红黄两色 | 66 | 37 | — | — |
| ② 红黄按他定，橙绿维持现行 | **66** | **7** | **29** | **1** |

### ⑶ 🔴 这份分布最要紧的一句：**改阈值本身，一行都没有变色**

把两组数与「现行阈值（🔴>3 天／🟡1–3 天）」逐格比：

| | 规则1 OFF 现行阈值 | 规则1 OFF 新阈值(落法②) | 规则1 ON 现行阈值 | 规则1 ON 新阈值(落法②) |
|---|---|---|---|---|
| 🔴 / 🟠 / 🟡 / 🟢 | 102 / 0 / 0 / 1 | **102 / 0 / 0 / 1** | 66 / 29 / 7 / 1 | **66 / 29 / 7 / 1** |

**完全相同。** 原因是**「晚 4–14 天」这个区间在真实数据里是空的**——缺口要么 ≤0（不晚），要么以月计。把门槛从 3 天抬到 14 天，中间没有任何一行。

⇒ **两条结论，`采购部#20` 里都要如实告诉他**：
1. **真正让看板恢复分级的是规则 1，不是阈值**（红 102→66、橙 0→29、黄 0→7）。
2. **他定的阈值不会白定**——它把「多晚才算红」这条线从 3 天挪到 2 周，**今天恰好没有行落在中间**，但缺口一旦缩小到周级，这条线就开始起作用。**不要读成「他这个决定没用」。**

---

## 四、责任人列 —— 口径落定，此后不再追问

他 2026-08-28 原话：**「那就等 IT，按（a）。」**

⇒ **该列维持 `—（取数缺口）` 不变**（`sc8/material_board.py::FIELD_GAP`，D5-a）。

- **不做**：ERP「制单人」对照表；**不再向他追问**这一列。
- **解锁条件**：陈承（IT）核实携客云 OpenAPI 是否提供「请购需求池-采购订单协同-负责采购」对应端点。该动作现在**有了明确的业务授权与等待方**。
- **不得**用 `buyers`（ERP 制单人）顶替 —— 已被 `R01B.0115` 真实数据证伪，页面与导出两处均有单测守着不使用它。

---

## 五、🔴 规则 1「开起来」—— 他已签认，但本次开不了，原因不是没做

他 2026-08-28 原话：**`☒ 认可 —— 按规则 1 改后的算法开起来。`**（`采购部#19` 第 6 件，段落内勾选控件）
⇒ **§一 #401 行内那条硬前置「姚祖怡已签认」至此满足。**

### ⑴ 它确实只是一个配置开关，不需要改代码

`sc8/config.py::kit_date_rule1_enabled()` 每次调用现读环境变量 `SC8_KIT_DATE_RULE1`（默认 `off`）。生产开法 ＝ 在 `.51` 的 `C:\baoguan\.env` 里加一行 `SC8_KIT_DATE_RULE1=on` ＋ 重启服务。**无代码改动。**

### ⑵ 但本次**开不了**，且这不是「忘了做」

- **`.51` off-LAN，实测不可达**（见 §〇）⇒ 既改不了它的 `.env`，也重启不了它的服务。
- 🔴 **回内网后不能只翻开关，必须先核一件事**：`.51` 上跑的代码**是否已经含规则 1**（`forecast.no_feedback_start_date` / `heuristic_base_date` 入参）。**#401 的代码 08-25 才 ff 入 master，而 `.51:8091` 的上一次部署是 08-24（#344）**——若 `.51` 仍是旧版，`SC8_KIT_DATE_RULE1=on` **不会报任何错、也不会有任何效果**（旧代码根本不读这个键）。**这正是本项目记过的「工具静默回退」那一族：配置写上去了、看板一个数字没变、而没有任何信号告诉你为什么。**
  ⇒ **正确次序＝先 `sync-to-server.ps1` 部署并核 `CreationDate` 真刷新 → 再翻开关 → 再 `POST /api/refresh` → 再核四色确实变了。**

### ⑶ 开之前／开之后的两组数（按要求已跑，**同一份冻结输入，零额外 API 请求**）

| 风险 | 规则1 OFF | 规则1 ON | 差 | 信里给他的（08-26 `.51` 生产实测） |
|---|---:|---:|---:|---:|
| 🔴 真延期 | 102 | **66** | −36 | 71 |
| 🟠 待催 | 0 | **29** | +29 | 27 |
| 🟡 偏紧 | 0 | **7** | +7 | 7 |
| 🟢 按期 | 1 | **1** | ±0 | 0 |
| **合计** | **103** | **103** | — | **105** |

**逐行变动 36 / 103 行；其中齐料日变晚的 0 行** ✅ —— 规则 1 只应让齐料日前移，这条不变式过了。

🔴 **与信里那张表的核对结论（按要求「对不上就如实报，不要照抄」）**：
- **🟡 偏紧 7 逐格相同**；🟠 29 vs 27、🔴 66 vs 71、合计 103 vs 105 —— **接近但不相同**。
- **三条可解释的原因，且都不是实现问题**：① 本次 `srm=[]`（变体口径）；② 样本本身小一圈（103 行／1413 叶子件 vs 105 行／1606 叶子件）；③ BOM 35 个料号拉取失败。
- ⇒ **`采购部#20` 里若要引用四色数，应引用 08-26 那份 `.51` 生产实测的 71/27/7/0，不要用本次这组**；本次这组的用途是「量出规则 1 的方向与量级、并核那张表没有算错」，不是替换它。
