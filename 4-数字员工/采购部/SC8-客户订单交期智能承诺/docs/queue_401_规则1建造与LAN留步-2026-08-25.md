# 队列 #401 · 规则 1 起算点建造记录与 LAN 留步清单（2026-08-25）

> 变更包 `openspec/changes/sc8-kit-date-rule1-start/`　｜　分支 `claude/sc8-kit-date-rule1-9c4d1a`
> 判据 ＝ 姚祖怡 2026-08-18 书面签认的规则 1；排期 ＝ Shao Peishen 2026-08-25 拍板 (a)（§四 #111）

## 一、本次真正做完了什么

| 项 | 结论 |
|---|---|
| 规则 1 实现 | `forecast.no_feedback_start_date()` —— 起算点唯一决策点；`baoguan` 只传值不判分支 |
| 单测 | 新增 `tests/test_kit_date_rule1_start.py` **23 条**，全绿 |
| SC8 全量回归 | **502 passed / 3 skipped**（master 基线 479 ＋ 新增 23 ⇒ 零回归） |
| 开关强制 ON 再跑全量 | **502 passed，与 OFF 完全相同** |
| mock 黄金基准 | `test_golden.py` 零漂移 |
| 真实黄金基准（冻结件） | `test_golden_real.py` 零漂移（`data/golden/real_frozen`，从主工作区取用） |
| 平台底座回归 | **380 passed / 1 skipped**，零回归 |
| 对照表脚本 | `scripts/compare_kit_date_rule1.py`，离线合成数据冒烟跑通，三条不变式自检全绿 |
| openspec | `validate --all --strict` **112 passed / 0 failed** |
| 生产影响 | **零** —— 开关 `SC8_KIT_DATE_RULE1` 默认 OFF，`.51` 不设该键即维持现状 |

## 二、🔴 一条必须如实说出来的负面结论

**「开关强制 ON 跑全量，结果与 OFF 完全相同」既是好消息也是坏消息。**

好消息：零漂移是结构性的，`pipeline.py` 与黄金基准走不进新分支。

🔴 坏消息：**mock 语料里没有任何一行的出货日落在其 `today` 夹具的三个月之外** ⇒ **mock 全量回归对规则 1 没有端到端覆盖力**。规则 1 的正确性目前**只由新增的那 23 条单测承担**，真实覆盖要等回 LAN 跑对照表。

⇒ **「全量绿」在本变更上不得被读作「已验证」。** 写下这一句，是因为它很容易在三周后被人当成后者。

## 三、🔴 LAN 留步清单（本机当前 off-LAN，三步全部未做）

| # | 留步项 | 回 LAN 后的一键动作 | 前置 |
|---|---|---|---|
| L1 | SRM 真实数据核验 ＋ 真实前后对照表 | `SC8_NET_INVENTORY=on python scripts/compare_kit_date_rule1.py --cache "%TEMP%/sc8_rule1_inputs.json" --srm-cache <srm_answer_cache.json 本地副本> --out docs/queue_401_kit_date规则1修复前后对照-<日期>.md` | 在网段；`.env` 凭据（主工作区已有） |
| L2 | 黄金基准真实重跑 | 真实黄金夹具（`data/golden/real_frozen`）本次已用**冻结件**跑过且零漂移；回 LAN 后按 `scripts/build_golden_real.py` 重新生成一次再跑，确认新鲜取数下同样零漂移 | L1 完成（同一次取数即可） |
| L3 | 部署 `.51:8091` | `sync-to-server.ps1` ＋ 冒烟（`/api/ping`、关键页 200、一次全量重算）＋ **验进程 `CreationDate` 真刷新**（2026-07-14 那次「脚本说重启了其实没有」的教训） | L1／L2 |
| L4 | 翻开关 `SC8_KIT_DATE_RULE1=on` 写入 `C:\baoguan\.env` | 一行配置 | 🔴 **L1 对照表出件 ＋ 姚祖怡复核签认**（随采购部#19，§一 #402）。**未签认前不得翻** |

**为什么 L4 单列而不并进 L3**：部署与「让新判据生效」是两件事。L3 做完，`.51` 上跑的仍是现行口径（开关默认 OFF），**看板上一个数字都不会变**；这正是开关形态的用处——部署风险与判据风险不必同时承担。

## 四、本次没做、也不该由本 session 做的

- **复核信不起草、不发送**（无头批处理规则①）。对照表随**采购部#19** 走，聚合行 §一 #402，硬前置是采购部#18 回件闭环。本次连草稿都没有。
- **规则 2 的「部分覆盖」没改**（design D2）。未来但在三个月内的 24 行，现行起算点是出货日、他的规则 2 逐字是此时此刻，现行更晚更保守。已在 `no_feedback_start_date` docstring 与 `test_future_within_horizon_keeps_current_conservative_reading` 各钉一次。
- **「三个月内」边界开闭没有默认生效**（design D1）。按偏保守侧落定（含边界当天 ⇒ 走规则 2 ⇒ 起算更晚），已登记待他以判例确认。

## 五、复跑路径（任何人、零网络）

```bash
cd 4-数字员工/采购部/SC8-客户订单交期智能承诺
python -m pytest tests/test_kit_date_rule1_start.py -q     # 23 passed
python -m pytest -q                                        # 502 passed / 3 skipped
SC8_KIT_DATE_RULE1=on python -m pytest -q                  # 502 passed，与上一行相同
```
