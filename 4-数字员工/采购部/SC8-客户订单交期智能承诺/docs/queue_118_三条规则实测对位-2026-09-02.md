# 队列 #118 · 姚祖怡三条规则 vs 现有实现 · 逐条实测对位（2026-09-02）

> 变更包 `openspec/changes/sc8-atp-batch2-closeout/`（design D2 引用本件）｜ 分支 `claude/op0902a5-atp-propose`
> 前置 ＝ §四 `#65` Shao Peishen 2026-09-02 答 (a)：认姚祖怡（男）2026-08-18 的书面文本即显式签认
>
> 🔴 **本件刻意落 tracked 的 `docs/`、不落 gitignored `reports/`** —— `#267` 的教训是审计报告随 worktree 删除而永久丢失。
> 🔴 **只读取证，未改任何判定代码。**

## 一、判据正本（逐字转录，姚祖怡 2026-08-18 `采购部#14` 回件）

> **规则 1（出货日不在三个月内）** —— 无答交按照出货日往前推算 3 个月的 20 日起、再往后推算 90 天，预估齐套日期；
> **规则 2（出货日在三个月内）** —— 无答交按照此时此刻起往后推算 90 天，计算齐套日期；
> **规则 3（同一项目既有答交又有无答交）** —— 对比无答交的预估齐套日期与有答交的答交日期，**更晚**的那一个是齐套日期。

补充签认：**「20 日 ＝ 那个自然月的 20 号」**（2026-08-18 `采购部#15` 回件）；**规则 1「开起来」**（2026-08-28 `采购部#19` 第 6 件，原话「☒ 认可 —— 按规则 1 改后的算法开起来。」）。

## 二、方法

探针**自行逐字实现三条规则，不复用被测代码**（避免自证）；出货日网格 `today−60 … today+400` 逐日，与 `forecast.no_feedback_start_date()` 逐点比对。规则 3 与「方向冲突」两项走真实调用链 `estimate_material_arrivals`。

**参数（本机实测，非假设）**：`today = 2026-09-02`（`Get-Date -Format 'yyyy-MM-dd'` 现取）｜`NO_FEEDBACK_LEAD_DAYS = 90`｜`rule1_horizon_months=3 / rule1_months_back=3 / rule1_start_day=20`｜三个月边界（含边界）＝ `2026-12-02`｜本进程 `SC8_KIT_DATE_RULE1 = False`。

## 三、结果

| 规则 | 实现落点 | 实测 | 结论 |
|---|---|---|---|
| 规则 1 | `forecast.no_feedback_start_date()` 规则 1 支 | 适用出货日 **309 个，不一致 0 个** | ✅ 完全覆盖 |
| 规则 2 | 同函数 `max(出货日, 今天)` 支 | 已过期支 61 个、**不一致 0**；未来在窗口内支 91 个、**不一致 91 个**（实现恒晚 1~91 天） | 🔴 仅部分覆盖 |
| 规则 3 | `baoguan.py:800` / `forecast.py:350` 的 `max(mat.arrivals.values())` | 两个方向各一次，**均一致** | ✅ 结构性覆盖 |

**规则 1 抽样**：出货 2026-12-05 ⇒ 起算 2026-09-20（＝逐字）；2027-02-20 ⇒ 2026-11-20；2027-04-20 ⇒ 2027-01-20。

**规则 3 实证**（子件 `M_ANS` 有答交、`M_NOANS` 无答交，需求日 2026-12-20）：
- 有答交更晚：到货 2027-08-01 / 2026-12-19 ⇒ 齐料日 **2027-08-01** ＝ 逐字规则 3 ✅
- 无答交更晚：到货 2026-10-01 / 2026-12-19 ⇒ 齐料日 **2026-12-19** ＝ 逐字规则 3 ✅

⇒ 取更晚是 `arrivals` 字典 + `max` 这个数据结构本身的性质，**不是碰巧对**。

## 四、🔴 §四 #65 所述「方向冲突」：实测其前提已不成立

§四 `#65`（2026-08-18 写）记的是「`#344` 现状取最早答交日期，与规则 3 取更晚方向相反」。**`#344` 已于 2026-08-24 上线按答交数量累计的取值口径。**

**实证**：逐笔答交 `[(2026-10-20, 0.0), (2027-05-20, 10000.0)]`、需求 8000 —

| | 日期 |
|---|---|
| 「只取最早」会得到 | 2026-10-20 |
| **现行实现返回** | **2027-05-20**（累计纳入 2 笔） |

⇒ **「只取最早答交日期」在当前代码里已不成立。** 现行两处都在取更晚（`_arrival_by_cumulative_qty` 的 `max(fallback, 最晚正数答交日)` ＋ 齐料日聚合 `max`），与规则 3 同向、无冲突。

🔑 **通用判据**：**§四 定夺行记录的是提出那一刻的现状，不是当前现状。** 照字面去「调和冲突」，会在一个已不存在的问题上改一遍 `#344` 花整轮判例签认才定下来的取值函数。⇒ **凡定夺行里的「现状是……」，动手前必须现测一次。**

## 五、规则 2 逐字化的算术影响（🔴 是算术，不是生产实测）

假定该行瓶颈件为无答交件；缺口天数 ＝ 齐料日 − 出货日；四色 🔴>3 / 🟡1~3 / 🟢≤0。

- 窗口内未来出货日 91 个（2026-09-03 ~ 2026-12-02）：现行缺口恒 `+90` ⇒ `🔴91`；逐字后 ⇒ `🔴86 / 🟡3 / 🟢2`。转 🟡 门槛 ＝ 出货日 ≥ 2026-11-28，转 🟢 ≥ 2026-12-01。
- **真实语料出货日集中在每月 20 日**，本窗口内只有三个：

| 出货日 | 齐料日 现行 → 逐字 | 提前 | 缺口 | 四色 |
|---|---|---:|---|---|
| 2026-09-20 | 2026-12-19 → **2026-12-01** | 18 天 | +90 → +72 | 🔴 → 🔴 |
| 2026-10-20 | 2027-01-18 → **2026-12-01** | 48 天 | +90 → +42 | 🔴 → 🔴 |
| 2026-11-20 | 2027-02-18 → **2026-12-01** | 79 天 | +90 → +11 | 🔴 → 🔴 |

⇒ **齐料日明显前移 18~79 天，四色按当前语料一格不动。**

⚠️ **三条边界，不得当定论端给专员**：
1. 是**算术**不是生产实测 —— `.51` 本机实测不可达（`Test-NetConnection 192.168.1.51:8091` ⇒ `TcpTestSucceeded=False` / `PingSucceeded=False`），真实对照表属 **LAN 留步**；
2. **假定瓶颈件是无答交件**；若瓶颈是已答交件，规则 3 的 `max` 会让该行完全不动 ⇒ **真实影响面 ≤ 上表**；
3.「出货日集中在 20 日」取自 2026-08-26 那份对照表的观察，`#150` 已实证预测订单会月度整批滚动、单号全变。

**适用行数**：08-26 生产基线 **24 行**（分支适用面：规则1 43 / 规则2 未来 24 / 规则2 已过期 38，合计 105）。

## 六、其余核实（同为实测）

| 项 | 结论 |
|---|---|
| ②「缺口 ≤0 不进缺料清单」 | ✅ 已建，`baoguan.py:556`（`#151`），**展示层**过滤，未被顺手改成计算层 |
| 「上月未齐套跨月继续占用」 | ✅ 口径已关闭 —— 归档 §一 `#150`：姚祖怡 2026-08-06 三行判例**全批「按照现状判定」**，且无需改任何代码 |
| `#15` 真实 PMC 优先级表导入 | 🔴 **仍未接** —— `priority_resolver` 恒 `None`，跑的是三级兜底判据（出货日 → 数量降序 → 下标） |
| 规则 1 的生产状态 | ✅ **已生效** —— §一 `#401` 于 2026-08-31（`OP-0831-L`）销号：`.51` 已翻 `SC8_KIT_DATE_RULE1=on`，四色 `🔴68/🟠27/🟡7/🟢1`（103 行） |
| SC8 全量回归基线 | **509 passed / 4 skipped**（零改动状态下先取） |
| `.gitignore` 覆盖（`git check-ignore -v`） | `data/priority/*.xlsx` 🔴 **未被覆盖**；`data/*.json` ✅ 命中 `.gitignore:4`；`reports/` ✅ 命中 `.gitignore:2` |

## 七、复跑路径

本件的规则 1/2/3 对位可由既有单测直接复跑（零网络）：

```bash
cd 4-数字员工/采购部/SC8-客户订单交期智能承诺
python -m pytest tests/test_kit_date_rule1_start.py tests/test_kit_date_qty_cumulative.py -q   # 44 passed
python -m pytest -q                                                                            # 509 passed / 4 skipped
```

真实前后对照表（LAN 留步）：`scripts/compare_kit_date_rule1.py`，`--cache` 冻结格式与 `compare_kit_date_cumulative.py` 一致、可互换复用同一份冻结输入。

⚠️ **本件所用的一次性网格探针是仓库外临时件、已随收工删除**；其方法与全部参数已逐条写在 §二/§三，可据以重建。**本件的数字来源于该次运行，不是从代码推断的。**
