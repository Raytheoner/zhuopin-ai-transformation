"""FI10 存货跌价智能分析 —— **工程骨架（档 0，未过 design 审，无引擎实现）**。

标的（队列 §一 #474）：自动采集 U9C 库存账龄、在途采购与 BOM 用量，**对接芯片价格 API**
取核心器件价格趋势；结合 OEM 项目生命周期（APQP/EOP）识别呆滞；按准则执行 NRV vs Cost
自动跌价测试；库龄超期／芯片降价超阈值／项目终止未耗物料自动预警；生成跌价准备计提建议表
并支持 what-if 模拟。自动化等级 **L2**（AI 测算＋预警，财务/供应链经理确认）。

🔗 **链 D 联动点 L9**（`SC7` 呆滞 ↔ FI10 跌价**同口径**）。🔴 见下方「L9 当前无源可取」。

🔴 **本包当前只有骨架**：跌价测试引擎与三类预警均未实现——design 审（🟡）未过不进 apply。

━━━ **本场景有三处性质各不相同的缺口，勿混为一谈** ━━━

**⑴ 芯片价格 API ＝ 前置未满足，已独立立行（队列 §一 `#475`）。**
  ⇒ **依赖该 API 的部分停下登记、不做**：即"芯片降价超阈值"这一类预警。
  ⇒ **不依赖它的部分照做**：库存账龄、在途采购、BOM 用量、NRV vs Cost 准则规则引擎。
  ⚠️ **一处待判、本包不代下结论**（`#475` 明写）：前置总表 `O2` 行写「芯片**供货** API」，
     实施计划写「芯片**市场价格** API」，**命名／服务对象／时点三项均不同，是否同一项尚未定**。
     若是两项，则「供货」那项尚未逾期（11 月底截止）、「价格」那项已逾期，**紧迫度完全不同**。
     本包**如实沿用「未定」**，不替它下结论。

**⑵ L9「同口径」当前无源可取 ＝ 本泳道实测出的新情况。**
  `#474` 写「`SC7` 已有工程实体，呆滞口径应从那里取、不得另立一套」——但本泳道 2026-09-03
  实测：**`SC7` 工程实体里没有任何呆滞口径**（`sc7_inventory/business_rules.py` 只有 R1 金额
  阈值 / R2 未认证供应商两条），且 `SC7/CLAUDE.md` 三处明写「呆滞库存处置」属 **②期深化
  （2027-01）尚未落地**、其「业务口径待姚祖怡确认」。
  ⇒ **L9 要对齐的那个口径当前不存在**。FI10 若自行定义呆滞口径，即等于 `#474` 明令禁止的
     「另立一套」，且属 🟡 `change_criteria`。**本包不代判**，登记为 design 停审点。

**⑶ OEM 项目计划（PLM）＝ 涉 OEM 专属数据。**
  按根 `CLAUDE.md` §7-3 须按客户路由、禁跨库，实现时不得混库。本场景是五个财务场景里
  **唯一**明确触及 OEM 隔离的一个。
"""
