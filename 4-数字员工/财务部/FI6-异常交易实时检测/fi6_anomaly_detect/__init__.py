"""FI6 异常交易实时检测 —— **工程骨架（档 0，未过 design 审，无引擎实现）**。

标的（队列 §一 #471）：实时监控 U9C 每笔应付/应收交易并与历史模式对比，跑异常检测
（金额突增、频率异常、关联方交易识别），高风险交易触发审批并推送财务主管，维护可疑交易
案例库持续学习，出月度异常交易分析报告识别系统性内控漏洞。自动化等级 **L2**（AI 标记
并推送，财务主管确认处理）。

📌 **范围边界（#471 明写，勿越界）**：**付款环节**的重复/超额/账户风险**已独立为 `FI3`**
（排 2026-11 提前落地），**不在本场景内**。本场景管的是应付/应收交易层面的模式异常。

🔴 **本包当前只有骨架**：三类检测器与案例库均未实现——design 审（🟡）未过之前不进 apply。

🔴 **两处「不得默认生效」**（见 `config.py`）：三类异常的判定阈值、以及"关联方"的识别口径。

⚠️ **一处本泳道实测出的空缺，承接方须知**：`#471` 提到本场景与 `FI3` 有联动（复用其付款
校验结果），但 **`FI3` 目前没有工程实体**——`4-数字员工/财务部/` 下只有 `FI1` 与 `FI2`。
FI3 排 2026-11、早于本场景的 2027-04，**顺序上不倒置**，但"联动"在建造时无对象可接。
另：「历史异常案例库」在本项目内**无既有载体**，属须从零建的知识型资产。
"""
