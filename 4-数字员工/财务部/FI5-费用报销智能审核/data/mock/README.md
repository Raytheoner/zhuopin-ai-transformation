# FI5 mock 夹具

🔴 **全部为合成数据，不含任何真实报销/发票信息**。字段名贴 U9C 报销模块语义，但
**该模块端点是否可取数至今未经核实**（见 `fi5_expense_audit/config.py` 的
`U9C_EXPENSE_NOT_READY`）——夹具字段是按场景描述拟的，端点开放后须以真实 schema 校准。

`invoice_no` 一列**刻意写成 14 位带前导零**，用来提醒：本项目已实测证伪过「发票号字面
join」假设一次（FI2 `fi2-tax-export-ingest`，2026-08-07 归档）。位数／前导零／空格／
全半角／代码前缀是那次踩过的五个点，本场景启用发票对碰前须重做一次实测。
