"""FI5 费用报销智能审核 —— **工程骨架（档 0，未过 design 审，无引擎实现）**。

标的（队列 §一 #470）：在 U9C 报销流程基础上 OCR 识别发票、校验真伪与合规性；对照公司
报销政策（差旅标准／招待限额）自动审核；实时查预算余额、超预算拦截并通知上级；异常报销
（超标／频繁／关联交易）标风险等级；出部门级费用分析报告。自动化等级 **L2**（AI 初审 ＋
风险标记，财务经理抽审）。

🔴 **本包当前只有骨架**：`config` / `models` 已定形，四个引擎（policy_rules /
budget_guard / risk_grading / expense_report）**未实现**——按根 `CLAUDE.md` §5 第 3 步，
design 审（🟡 `openspec_design_review`）未过之前不进 `/opsx:apply`，不写实现。

🔴 **本场景的三处「不得默认生效」**（见 `config.py`，全部落成 `None` 且由 smoke 测试守）：
  · 差旅标准 / 招待限额 —— 属**知识型资产**，须财务侧显式产出并签认；
  · 超预算拦截阈值 —— 判据类；
  · 异常报销风险分级边界 —— 判据类。

⚠️ **OCR 一处必须写明的辨析（防误引，源 #470 状态列 ＋ 全景规划 §0.2 2026-08-09 行）**：
FI2 于 2026-08-04 作废 OCR 方案（8 张真实发票比对，「货物名称及规格」精确匹配仅 65.8%）
**不自动适用于本场景**——FI2 要的是逐字段结构化取数，FI5 要的是发票真伪与合规校验，两者
对 OCR 的精度要求不同。**但也不等于已验证可用**：承接方须自行做一次针对**报销发票**的
精度实测，不得直接沿用任一侧结论。该实测属真实数据动作，本骨架不做。
"""
