"""FI9 研发费用归集与高新认定助手 —— **工程骨架（档 0，未过 design 审，无引擎实现）**。

标的（队列 §一 #473）：自动从 U9C 项目成本模块采集各研发项目材料/人工/制造费用；基于
**会计准则 ＋ 企业政策规则引擎**判资本化/费用化；按高新要求生成研发费用辅助账；算研发
费用占比等核心指标、生成加计扣除备查资料包、做政府专项核算与研发项目投入产出分析。
自动化等级 **L2**（AI 归集＋分类建议，财务/研发总监审核确认）。

🔴 **本场景与其余四个财务场景最大的不同：产出是对外材料。**
本场景结论用于**政府申报与高新认定**。按根 `CLAUDE.md` §7-4 口径，其结论一律须人工确认
后方可使用，**AI 不得自动出具对外文件**。资本化/费用化判据直接影响对外申报 ⇒ 判据类
永不默认生效，此处的代价比其余场景高一个量级：判错不是报表难看，是申报材料出错。

🔴 **本包当前只有骨架**：规则引擎与辅助账生成均未实现——design 审（🟡）未过不进 apply。

⚠️ **两项数据依赖在本项目内从未出现过**（`#473`，本泳道 2026-09-03 复核确认）：
  · **工时系统** —— 「工时」二字在 4 份规划文档里只作为**依赖名**出现；代码侧仅
    `zhuopin_platform/shared_tools/models.py:126` 一句注释「用于 SMT 工时查询」，
    那是 `product_id` 字段的用途说明，**不是工时系统的连接器**。⇒ **无任何既有取数指针**。
    承接方开工第一件事应是**核实它是否存在**，而不是假设它存在。
  · **高新认定政策库** —— 无载体，属知识型资产，须财务侧产出并签认。
"""
