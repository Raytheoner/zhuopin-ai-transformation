---
status: 生效中
---

# 回滚 SOP —— `#423` 判例 3（发票装载范围）＋ `#424` 判例 5/6/7/8（摄取层认领与拆行）

> **形态**：代码级回退。本次改动落在**三个互相独立的模块级常量**上，可整体退、也可只退其中一个。
> **签认依据**：唐燕萍 2026-09-04 `财务部#16` 回件判例表逐行勾选（判例 3 ✅／判例 5·6·7 ✅／判例 8 ✅；判例 1·2·4 ❌）。
> 🔴 **回滚 ＝ 推翻她已签认的口径**，不是纯技术动作 —— 除「服务起不来」这类硬故障外，**执行前须由 Shao Peishen 拍板**，并当场记入队列 `#423`／`#424`。
> 前一份 SOP（`回滚SOP-423-KPI孤立发票口径-2026-08-28.md`，管 `_KPI_ORPHAN_MODE`）仍有效且**与本份互不覆盖**；那份里「`_INVOICE_SCOPE` 那条断言守的是 `#424`」一句有误——它守的是 `#423` ⒞，以本份为准。

## 一、三个开关（唯一开关，都在模块级常量上）

| # | 常量 | 文件 | 现值（2026-09-07 起） | 退回值 | 对应判例 |
|---|---|---|---|---|---|
| ⑴ | `_INVOICE_SCOPE` | `fi2/webapp.py` | `_INVOICE_SCOPE_AP_RANGE` | `_INVOICE_SCOPE_ALL` | #423 判例 3 |
| ⑵ | `_ITEM_MATCH_STRATEGY` | `fi2/tax_export_ingest.py` | `"qty_price_then_amount_then_single_item_then_subset_sum"` | `"qty_price"` | #424 判例 5/6/7 |
| ⑶ | `_ALLOW_INVOICE_ROW_SPLIT` | `fi2/tax_export_ingest.py` | `True` | `False` | #424 判例 8 |

⚠️ **⑶ 依赖 ⑵**：`_ITEM_MATCH_STRATEGY` 退回 `"qty_price"` 后拆行路径本就不会触发（`resolve_item_code_split` 第 ⑵ 条边界），⑶ 可不必同时改；反之只退 ⑶ 是合法的独立操作（保留判例 5/6/7、只撤判例 8）。

🔴 **判例 4 的落地（孤立发票不再进三单核对明细表、改进未匹配发票池）不由常量控制，是 `_render_table`／`_render_invoice_pool` 的代码结构。**
它**与 `_INVOICE_SCOPE` 档位无关**——她否的是「孤立发票出现在明细表里」这件事本身。要撤销它须 `git revert` 本次提交的对应 hunk，**不在本 SOP 的「改一个常量」范围内**；且撤销它 ＝ 让被排除的发票行重新无处可查，**风险高于其余三项**（她原话：「有可能只是目前业务尚未立账，待立账时才需要用到」）。

## 二、怎么退

1. 按上表改回常量值（这是唯一开关；`.51` 是整包同步部署，「合入 master 但留步不生效」只能靠默认值本身守住）。
2. **同步改回退化守卫单测**，否则测试会红：
   - `tests/test_webapp.py::test_kpi_default_mode_is_the_signed_off_one_and_is_the_only_switch` → `_INVOICE_SCOPE` 断言改回 `_INVOICE_SCOPE_ALL`；
   - `tests/test_tax_export_ingest.py::test_default_item_match_strategy_is_the_signed_off_one_and_is_the_only_switch` → 改回 `"qty_price"`、`_ALLOW_INVOICE_ROW_SPLIT is False`；
   - 退 ⑴ 还须改 `tests/test_webapp.py` 里 `test_scope_invoice_rows_default_is_now_ap_range_per_signoff`、`test_ap_range_scope_keeps_excluded_rows_retrievable_in_the_pool`、`test_kpi_orphan_mode_is_observationally_inert_under_ap_range_scope`，以及 `TestRunMockModeV8Panel` 两条（`另有 <b>1</b> 行孤立发票` 会重新出现）；
   - 退 ⑵/⑶ 还须改 `tests/test_tax_export_ingest.py` 里带 `signed_off`／`split` 字样的用例。
   🔑 **这些用例本来就是「口径不被无声改走」的守卫**——退口径时它们跟着改是正常的，红了不许绕过。
3. **跑测试**（必须全绿再推）：`python -m pytest -q`，本次基线 **249 passed, 9 skipped**。
4. **重新部署并重启**：`powershell -ExecutionPolicy Bypass -File ./sync-to-server.ps1`，打印 `NEW_PID`／`CREATED` 才算成功。

## 三、怎么验证退回成功

1. `curl http://192.168.100.51:8094/api/ping` → HTTP **200**。
2. **部署件与本地逐字节一致**（CRLF 差异要先归一，见下）：
   ```
   ssh supplychain-server "certutil -hashfile C:\fi2\app\fi2\webapp.py SHA256"
   ```
   与本地文件把 `\n` 换成 `\r\n` 后的 SHA256 相同（`scp` 到 Windows 侧会换行归一，直接比 `git hash-object` 会假报不一致）。
3. **行为验证**（比看代码更硬）：面板筛 `AP-2026080041` 一张单 —— 退回 ⑴ 后「未匹配成功的发票池」区块消失、KPI 重新出现「另有 N 行孤立发票」提示条。

## 四、🔴 本 SOP 管不到的部分

1. **它退不回数据。** ⑵/⑶ 改的是**摄取**口径；已经写进 `data/tax_export/invoice.csv` 的行不会因为退口径而消失，尤其判例 8 拆出来的多行。退口径后须另行决定是否重建 `invoice.csv`（`scripts/rebuild_invoice_csv.py`），那是 Shao Peishen 的另一个决定。
2. **它退不回 ledger。** 判例 5/6/7 解开的行已从 `unresolved` 出队；退回 `qty_price` 后它们不会自动回到待重试队列。
3. **它不恢复计划任务** `Fi2TaxExportDailyScan`（2026-08-28 停用至今，恢复权在 Shao Peishen，见队列 `#418`）。
