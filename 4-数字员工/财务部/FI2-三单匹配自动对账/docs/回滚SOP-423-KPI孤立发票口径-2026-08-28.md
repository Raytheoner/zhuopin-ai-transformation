---
status: 生效中
---

# 回滚 SOP —— `#423` ⒜「孤立发票移出 KPI 三档」

> **形态**：代码级回退（Shao Peishen 2026-08-28 选定）。**不做服务器快照级回退** —— 本次改动只有一个常量，快照的钝度换不来任何额外保障。
> **适用范围**：撤销 `#423` ⒜，把面板 KPI 退回 `COUNT_IN`（2026-07-31 v8 口径）。**不适用于**撤销同批部署的其余 20 个提交（见文末「本 SOP 管不到的部分」）。

## 一、什么时候该跑这个 SOP

命中任一即跑：

1. 唐燕萍（财务总监兼财务域 AI 专员）看到面板后明确要求退回旧口径；
2. `#423` 判例批改（`财务部#16`）回件选了 ⒝ 或 ⒞ 而非 ⒜；
3. 面板 KPI 出现与 ⒜ 预期不符的显示（例如孤立发票那条提示条不出现，或三档数值仍含孤立发票）。

## 二、怎么退（四步，约 5 分钟）

```bash
# 1. 改回一行常量（这是唯一开关）
#    文件：4-数字员工/财务部/FI2-三单匹配自动对账/fi2/webapp.py
#    _KPI_ORPHAN_MODE = _KPI_ORPHAN_SEPARATE   →   _KPI_ORPHAN_MODE = _KPI_ORPHAN_COUNT_IN
```

2. **同步改回退化守卫单测**（否则测试会红，且那条守卫本来就是用来防「口径被无声改走」的）：
   `tests/test_webapp.py::test_kpi_default_mode_is_the_signed_off_one_and_is_the_only_switch`
   断言改回 `_KPI_ORPHAN_COUNT_IN`；同文件三处 `本次共 11 项料品` 改回 `12`、`8 项BLOCK退回` 改回 `9`，并删掉 `另有 <b>1</b> 行孤立发票` 两条断言。
   🔴 **`_INVOICE_SCOPE` 那条断言不要动** —— 它守的是 `#424`，与本次无关。

3. **跑测试**（必须全绿再推）：
   ```bash
   python -m pytest -q
   ```
   预期 `230 passed, 9 skipped`。

4. **重新部署并重启**：
   ```bash
   powershell -ExecutionPolicy Bypass -File ./sync-to-server.ps1
   ```
   脚本自带重启轮询，打印 `NEW_PID`／`CREATED` 才算成功。

## 三、怎么验证退回成功（三条，缺一不可）

1. `curl http://192.168.100.51:8094/api/ping` → HTTP **200**，body `{"service":"FI2 三单匹配自动对账","status":"ok"}`。
2. **部署件与本地逐字节一致**：
   ```bash
   scp supplychain-server:C:/fi2/app/fi2/webapp.py /tmp/w.py && git hash-object /tmp/w.py
   ```
   与 `git rev-parse "HEAD:4-数字员工/财务部/FI2-三单匹配自动对账/fi2/webapp.py"` 相同。
3. **面板数值回到旧口径** —— 在 `.51` 上跑（只读，不写任何生产文件）：
   ```
   set "PYTHONIOENCODING=utf-8" && set "PYTHONUTF8=1" && ^
   C:\fi2\.venv\Scripts\python.exe C:\fi2\app\scripts\probe_131_ap_query_docno_filter.py ^
       --repro-kpi C:\fi2\app\data\tax_export
   ```
   「面板 KPI（现行默认口径）」那一行应重新显示 **本次共 3392 项料品／BLOCK 3392**（⒜ 生效时是 **共 1 项／BLOCK 1 ＋ 单列孤立发票 3391 行**）。
   ⚠️ **3392／3391 是 2026-08-28 13:5x 的实测值，会随发票摄取每天变化**；判据看的是**结构**（三档是否重新含入孤立发票），不是这两个具体数字。

## 四、🔴 本 SOP 管不到的部分（必须读）

1. **它退不回数据。** 若 `Fi2TaxExportDailyScan` 已被恢复并跑过重摄取，本 SOP 只退代码，`data/tax_export/invoice.csv` 已被改写的内容不会回来。数据侧的兜底是同目录 `invoice.csv.bak-*` 备份，属另一件事。
2. **它退不回同批部署的其余 20 个提交。** 2026-08-28 那次部署把 `.51` 从 2026-08-10（`427ba09`）一次追平到 `61cc418`，共 21 个提交。要整体退回需另做，本 SOP 不覆盖。
3. **它不恢复计划任务。** `Fi2TaxExportDailyScan` 于 2026-08-28 13:51:19 本地（＝05:51:19Z）停用，**恢复权在 Shao Peishen**（恢复＝批准 `#418` ⑸② 那次重摄取）。见队列 §一 `#418`。
