# FI8 mock 夹具

🔴 **全部为合成数据**。`opening_balance.csv` 的 `source` 列写死 `synthetic` 并非笔误——
**真实银行账户余额的取数授权尚未取得**：该数据面不在既有 U9C 财务侧 10 端点清单内，又涉
资金安全（同 FI3 的 L4 晋级须 CFO 会签口径），须财务侧与 CFO 办公室明确，**不得默认可得**
（队列 #472 状态列）。引擎读到未授权即 fail-loud，**不得以 0 余额或任何推算值代替**。

⚠️ 夹具**不含任何"预测结果"基准**。资金缺口窗口的高亮门限尚未签认，先造一组"标准答案"
等于先替 CFO 定了什么叫缺口。基准须待判据签认后再补进 `data/golden/`。
