# CLAUDE.md — O1 生产排程智能优化（场景级记忆）

> 本文件是 O1 场景的本地记忆/进度笔记，隔离于其他场景。
> 项目级上下文见仓库根 `CLAUDE.md`；O1 规划权威见全景规划 §2.1.3 运营部 O1 行、
> 实施计划 §一运营表、`1-转型规划/0-全景路线图/session接力-Phase1收口.md`。
> 本场景 = CC 建造车间产物；**不改规划文档**（那是 Cowork 的活）。

## 1. 场景定位

- **场景**：O1 = 运营部第 1 个数字员工，SMT 生产排程。
- **全景编号**：全景规划 §2.1.3 运营部 O1（S1 筑基期场景，排期 2026-09）。
- **自动化等级**：**L1**（Shao Peishen 2026-08-18 拍 design D3）—— 首版产出是纯计算返回的日期，无任何下发/推送/写回动作，无 L2 门禁。
  🔴 **口径分歧未消**：全景规划 §2.1.3 O1 行现写 `L2（AI 生成排程方案，计划员审核调整）`，与本场景实际的 L1 不一致。**回写全景规划或 opener 之一属 Cowork 归口**，已挂账队列 #343，本场景不自行改规划件。全景规划的 L2 定位在场景具备「排程方案下发车间」能力时生效，届时须补人工确认门禁。
- **MVP 范围（档 1）**：完工日推算引擎 + 场景入口 + 审计留痕；mock/CSV 夹具跑通，`real` 模式显式 fail-loud。
- 🔴 **首版是「完工日估算器」，不是「排程优化器」**——收割来的算法是 `max(所有物料到货日) + 工时天数`，无容量约束、无产线/设备分配、无优先级排序、无换线损耗。**与场景名「智能优化」的落差是真实的**，此处如实记录而不修饰；全景规划所载「设备利用率提升 15-20%」属首版不可达指标。
- **底座贡献**：无。O1 是排程引擎的**消费方 #1**，rule-of-three 未触发，引擎留在场景本地（同 O2 当初）。
- **OEM 隔离**：不适用（O1 读内部生产计划/物料到货，不涉 OEM 技术数据），**不强加 OEM 路由**（同 O2）。

## 2. 关键决策记录（Shao Peishen 2026-08-18 拍板，答「全部默认」）

| 决策 | 结论 | 依据 |
|------|------|------|
| ⓪ 收割还是从零重写（Q12(c)） | **收割** | 通读四个文件后确认其确实覆盖「到货+工时→完工日」；虽仅 72 行两函数，但三样非代码资产会被重写丢掉：业务方已确认的自然日口径、46 个真实料号、SRM 无排产端点的探查否定结论 |
| 目标档位（Q11(a)） | **档 1**，`real` fail-loud | 生产计划端点不可达；**未选 CSV 桥接**——那会新增「人工定期投放」依赖，2026-08-10~08-17 刚被同一形态咬过 |
| D1：引擎放哪 | **场景本地** `o1_smt_scheduling/schedule_engine.py` | 消费方 #1，rule-of-three 未触发；无第二消费方可校准接口形状，过早提底座会把 72 行函数的签名冻死在错误抽象上 |
| D2：收割方式 | **复制+改 import，不包装、不改签名** | 保签名 ⇒ 被收割实现的原测试可原样迁入当回归网，收割可信度不依赖新写的测试 |
| **D3：自动化等级** | **首版 L1** | 无下发动作即无可确认的动作对象，标 L2 会造出空门禁（同 2026-08-18 OEM 隔离层「写了但底层没有」的形态） |
| **D4：占位工时表处置** | **收割整表 + CSV 头声明块 + 返回值带 `lead_time_is_placeholder`** | O2 B6「不静默、不假装」红线的同构应用。**刻意不与 O2 同构**：O2 用 inline 夹具，本场景若改 inline 会丢掉 46 个真实料号——那是该表唯一的真实资产 |
| D5：数据源档位闸 | **复用底座 `_fallback_or_failloud`，不自建** | 同一语义两处实现必然漂移（队列 #308 所根治的那类问题） |
| D6：夹具形态 | 工时表用 CSV（保真实料号）+ 到货日用 inline dict | 两个输入资产性质不同：前者价值在真实料号必须留文件，后者本就由调用方传入 |
| D7：测试策略 | 原 10 个用例原样迁入 + 22 个新增边界用例 | 原测试与原实现同源、可能同源错误，故新增用例**从 spec 反推而非从实现反推** |
| plans / connector 互斥 | 同时提供即 `ValueError` | 二者同时给会让「这批工单哪来的」在审计里说不清，拒绝而非静默取舍 |

## 3. 复用底座资产

- **审计**：`zhuopin_platform.audit` — `AuditEvent(scenario="O1", action="smt_schedule", automation_level="L1")`，`audit_logger` 可选（None ＝ 静默）。
- **模型**：`zhuopin_platform.shared_tools.models.ProductionPlan`。
- **档位闸**：`zhuopin_platform.shared_tools.connector_errors.RealEndpointNotReadyError` —— 由底座 `ZpConnector._fallback_or_failloud` 抛出，场景侧**不捕获、不回退、不自建等价判断**。
- **不使用**：`data_isolation_layer`（不涉 OEM）、`Notifier`（L1 无推送）、`kit_engine`。
- **未改底座任何文件**（已实测：底座回归 295 passed + 1 skipped，零漂移）。

## 4. 红线（建造时守住）

- 🔴 **随附工时表 46 行工时值全为占位常数 7，非真实工时**——`fixtures/smt_lead_time.csv` 头部有显式声明块 + 机器可读标记 `@placeholder: true`；返回值与审计均带 `lead_time_is_placeholder`。**禁止用于任何对外承诺、交期答复或产能测算。** 无标记的工时表按占位处理（缺省偏保守）。
- 🔴 **首版是估算器不是优化器**，见 §1。对外描述本场景能力时不得暗示其含产能优化。
- 先 mock/脱敏跑通，再切真实数据；`real` 模式取生产计划**显式 fail-loud，绝不静默回退 mock**。
- 工时列非法的行**跳过并告警**，不得静默视为 0 天——否则会算出一个假完工日（同 O2 B6 精神）。
- 排产决策写平台 `audit`（automation_level=L1，IATF 3 年留存）。
- L1 场景无 L2 人工确认门禁；**若未来升级到「排程方案下发车间」，必须先过门禁评审**并同步改回自动化等级。
- **不新起端口**：首版无 Web 呈现；若后续要呈现，注册到统一门户 `/operations/o1` 路由并预留网关 auth 接入点（根 CLAUDE.md §5 硬约束）。

## 5. 状态时间线

| 日期 | 状态 |
|------|------|
| 2026-08-18 | ⓪ 步硬前置执行：通读 supplychain `smt_scheduling` 四个文件，判定收割。**发现工时表 46 行全为常数 7**（opener 与前置总表均未记载），据此把「前置总表无 O1 行 ＝ 无外部知识前置」更正为「该前置从未被登记」。 |
| 2026-08-18 | propose 产出（proposal/design/tasks + specs×2），`--strict` 通过；停在 §5 第 3 步等审。commit `a9251ec`。 |
| 2026-08-18 | Shao Peishen 答「全部默认」，七个决策点落定并写入 design §拍板记录。commit `2e05a83`。 |
| 2026-08-18 | Shao Peishen 答「放行」→ apply 执行完毕。**32 tests 全绿**；O2 回归 20 passed、底座回归 295 passed+1skip 均零漂移；`openspec validate --all --strict` 82/82。 |
| **当前** | **✅ O1 档 1 完成**，等待晋档 2 的四条解锁条件（见 §6）。**未部署 `.51`、未起草跟进信**——档 1 无真实数据、无对外呈现，§5「发布即收口纪律」的部署段与第 8 步前置均未成立。 |

## 6. 关键依赖/前置（解锁条件）

**晋档 2（真实数据跑通）须四条全部满足**：

1. 🔴 **U9C `CommonEntity/Query` 外网开放或 LAN/VPN 可达**，`UFIDA.U9.MO.MO.MO`（MO 工单实体）实测可取数 —— **卡 IT，非本项目可控**。现状：外网 404。见底座 `erp_connector/connector.py:927` 的 TODO。
2. 🔴 **SMT 工时表由运营部 PMC 实名确认并签字存档**，替换当前占位常数表，同时把 `@placeholder` 标记改 false —— **无 owner**（见第 4 条）。
3. 🟡 **「按自然日、不跳周末节假日」由 PMC 书面复核**仍可接受，或给出正式版口径 —— 该简化来自 supplychain MVP 阶段一次口头确认，**未记录确认人与日期**。
4. 🔴 **§一.2 知识资产台账的 O1 行持有人与 backup 点名** —— 运营部 PMC 接口人**至今未点名**（Shao Peishen 2026-08-18 按默认项 (b) 暂不点名，已挂账队列 #343）。

**5 项默会知识现状**（proposal 已列，4 项完全默会且无人认领）：各产品实际 SMT 工时 🔴 ／ 是否跳周末节假日 🟡 ／ 换线切换损耗 🔴 ／ 紧急插单优先级尺度 🔴 ／ 设备与人员产能约束 🔴。

⚠️ **一个须知悉的排期事实**：§二纪律要求「知识型前置启动前 8 周开首轮工作坊」；若 O1 正式版仍锚 2026-09，该窗口早已过。**且提取方法（从历史工单反推真实工时）本身依赖当前不可达的工单端点，工作坊现在也排不了**——即第 1 条不解，第 2、3 条也无从推进。

**运行**：`python -m pytest -v`（场景目录内，无需网络）；引擎与入口均为库形态，无 CLI 入口。
