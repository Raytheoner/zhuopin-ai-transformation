---
title: "财务部#16 回件勾选误判 · 取证与更正（唐燕萍 8 行判例全部作答，我方判成「全空」）"
created: 2026-09-07
status: 生效
会话: Cowork OP-0907-L
触发: Shao Peishen 2026-09-07 转述唐燕萍原话「每次 100% 都勾了，是你没有检测出来」＋ 三张回件截图
取证对象: `7-外部文档/财务部/财务部-tangyanping-回复-财务部#16-2026-09-04-财务部-唐燕萍-跟进-2026-08-28-面板KPI与发票认领两处口径判例批改-回复-4be75fd63dfb636bcc9328af0a096165.docx`
取证方法: zipfile 直读 `word/document.xml`，lxml 逐 `w:tbl`/`w:tr`/`w:tc` 遍历 `w:sdt` 内 `w14:checkbox/w14:checked/@w14:val`，并与字符层 `☒`/`☐` 计数交叉验证
结论: 🔴 我方 2026-09-04 拆件结论「全部 9 行勾选格〔w14:checkbox〕实测均为空」**错误**；她 8 行判例逐行勾选、无一遗漏
---

# 财务部#16 回件勾选误判 · 取证与更正

> 🔴 **一句话**：她**全勾了**，我方**数了容器、没读状态**，于是把她的作答判成了没作答——而这个方向的错误**不产生任何信号**。

## 一、实测数据（机器计数，非肉眼）

| 形态 | 计数 |
|---|---|
| `w14:checkbox` 控件元素 | 48（24 个 `w:sdt`，每个含 checked/unchecked 两处定义） |
| **`w14:checked w14:val="1"`（＝已勾）** | **8** |
| `w14:checked w14:val="0"`（＝未勾） | 16 |
| **字符层 `☒` U+2612** | **8** |
| 字符层 `☐` U+2610 | 16 |
| `w:highlight`（她插入的文字段） | 21 |
| `w:ins` / `w:del` / `word/comments*` | 0 / 0 / 无 |

**控件层与字符层两路完全一致：24 个勾选格、8 行判例、每行恰好勾一个，8 行全部作答。**

## 二、她的真实答复（逐格恢复）

> 列位：`[4]` ＝「✅ 这样判对」／`[5]` ＝「❌ 这样判不对」／`[6]` ＝「✏️ 应改判为（写一句）」。

### 判例表 A · 面板 KPI 口径

| 判例 | 她勾的 | 含义 |
|---|---|---|
| 1 | **❌ 这样判不对** | 🔴 **而我方止血已按判例 1 那一档上线** ⇒ 线上此刻跑的这一档，她判是错的 |
| 2 | ❌ 这样判不对 | |
| 3 | **✅ 这样判对** | 装载前先按本次 AP 范围过滤 ⇒ 装载 0 行；面板显示「本次共 1 项料品／BLOCK 1／孤立发票 0」 |
| 4 | ❌ 这样判不对 | 拟改写的是「真·孤立发票必须在面板上继续看得见」，**而她要的是不展现在面板、留在未匹配发票池** ⇒ 判 ❌ 并用文字给出正确做法 |

🔑 **她的勾选与她写的字完全自洽**：勾 3 ✅、1／2／4 ❌，文字里写「希望是判例 3 的展现形式，而又保留判例 4 的功能但不用展现孤立发票在面板上」——**不是「没按四选一勾、只给了自定义方案」，而是「勾了，并且把勾不出来的那一层用文字补上」**。

### 判例表 B · 摄取层认领口径

| 判例 | 她勾的 |
|---|---|
| 5（计量单位／包装换算，`AP-2026080041` 密封胶） | **✅ 这样判对** |
| 6（该 AP 单通篇只有一个料号） | **✅ 这样判对** |
| 7（同一料号多行、按合计开票，`AP-2026080137` 气泡袋） | **✅ 这样判对** |

🔴 **我方 09-04 判为「未获裁定」——实际三条全部已裁定。** 被丢弃的 **1,813 行发票／421 张发票／421 张 AP 单（其中 221 张今天零行）** 的认领口径，**她 09-04 就答完了**，我方却以为还在等她。

### 判例表 C · 一张发票行拆两条

| 判例 | 她勾的 |
|---|---|
| 8（跨两个料号合计开票，`AP-2026080137`） | **✅ 这样判对** |

## 三、她插入的三段文字（原文照录）

1. 「上表中的最后一列"应改判为（写一句）"我无法输入文字，请帮检查格式设定。」
   ⇒ **格式缺陷属实，但它只挡住第三列写字，不挡勾选**（已登记 §四 `#156`）。**我方由此推出「她不用复选框作答」是错误推论。**
2. 「另外：目前在面板上可以在 AP 单处输入单张或多张 AP 单号进行筛选，我们希望是判例 3 的展现形式，而又保留判例 4 的功能但不用展现孤立发票在面板上，不要把孤立发票过滤掉，而是留在未匹配成功的发票池，即如下图红框的孤立发票不需要体现在三单核对明细表中，但这些孤立发票有可能只是目前业务尚未立账，待立账时才需要用到。」
   「其实我们希望面板上在不输入 AP 单号也不选择期间时（即不是强制必输入段），就是默认展示所有未立账的 AP 单，去进行三单核对，而查询具体 AP 单号的功能最好是放在三单匹配结果面板关键汇总面板的下方（见下图）。另外人员和期间可以以下拉框选择的形式较为方便。」
   ⇒ 两条界面需求，**均为判例 3／4 勾选的展开说明，不是替代勾选的「自定义方案」**。
3. 「补充一点，拆行后两条的未税金额与税额分别相加，必须和发票行的未税金额与税额一致，如果因分摊产生的尾差则并入后一条记录（不是单独做一条记录，而是并入已有的最后一条记录）。」
   ⇒ 判例表 C 的 ✅ 之上再加一条算法约束。

## 四、我方错在哪（四层，逐层更根本）

1. **表层**：09-04 拆件结论写「全部 9 行勾选格〔`w14:checkbox`〕实测均为空」。**形式合规**（写出了用什么核的，满足根 `CLAUDE.md` 那条自陈纪律），**结论错误** ⇒ 🔑 **「写出用什么核的」只保证可追溯，不保证正确**；它让错误可被事后发现，但没能阻止错误发生。
2. **方法层**：查的是 `w14:checkbox` 这个**元素名**，数出容器 24 个，**没有读决定状态的子元素 `<w14:checked w14:val="1"/>`**。字符层 `☒`/`☐` 承载同一信息且与控件层一致——**两条路任一条走到底都能查出来，我方两条都停在半路。**
3. **根本层**：**错误方向是「把作答判成没作答」，而它不产生任何信号。** 我方安静地等，她那边是「我明明每次都勾了」——双方各自以为对方没动作，系统里没有一处会报错。同根 `CLAUDE.md`「能猜、猜错不报错、下游无信号」判词的又一形态。
4. **同族第五次**：`采购部#16`／`#17`「去找批注或勾选」→ `财务部#14`「形态是高亮不是批注」→ `财务部#15`「6 个真复选框一个没勾」→ 本次。前几次的教训写成了一句话「**先看形态分布，再决定去哪读**」，**而这次形态分布就摆在眼前（8 个 `checked=1`），照样读错** ⇒ 🔑 **教训写成句子不管用，得写成一个必然会被执行的检查步骤**（同 `zhuopin-followup-letter` v3.7「规则约束知道、步骤约束做」的裁决）。
5. 🔴 **最扎心的一条**：**三次都是她先开口我方才发现**——`财务部#13` 只发 md（她开口）、`财务部#16` 最后一列输不了字（她开口）、本次「我每次都勾了」（她开口）。**我方全部自检里没有一条是「回件到达后，用独立方法验一遍拆件结论」。**

## 五、待办（本件只登记，不在本会话执行）

| # | 事项 | 归属 | 紧急度 |
|---|---|---|---|
| 1 | **判例表 A 止血档改判**：线上跑的是判例 1（她判 ❌），须改成判例 3 那一档 | FI2／业务总线，队列 `#423` | 🔴 最急，触及生产口径 |
| 2 | **判例表 B 三条 ✅ 回灌**：1,813 行认领口径已获裁定，`#424` 可开工 | FI2／业务总线，队列 `#424` | 🔴 高（此前误判为阻塞） |
| 3 | **判例表 C 的尾差算法**回灌（尾差并入最后一条记录，非单起一行） | FI2／业务总线 | 中（我方此前已按此实现，与她一致） |
| 4 | **两条界面需求**：默认展示所有未立账 AP 单（AP 单号与期间非必输）＋ 查询框移到关键汇总面板下方＋人员/期间改下拉框 | FI2 面板 | 中 |
| 5 | 🔴 **机制修复：回件勾选状态解析器** —— 控件层（`w14:checked/@w14:val`）与字符层（`☒/☑/☐`）**两路必读且必须一致**，不一致即 fail-loud；拆件流程加硬步骤「凡结论为『未作答／未获裁定／全空』，必须附机器计数『N 个格、其中 checked=M』，M=0 时强制二次核字符层」 | 环境线，须走 openspec | 🔴 高（同族第五次） |
| 6 | **README `财务部#16` 行与 `跟进信行日志/财务部#16.md` 的拆件结论须更正** | 随 1／2 回灌一并做 | 高（错误结论仍是当前台账） |
| 7 | 🔴 **对她的更正补件**（内容见下节）——**对外发送，须 Shao Peishen 定夺并读回后发，本会话不代办** | 待定夺 | 🔴 高 |

## 六、`财务部#17` 需要更正的那一句（补件底稿要点）

`财务部#17`（2026-09-07 07:37+08:00 已发出、四条链路齐全、撤不回）正文写了：

> 「判例表 A 你没有按四选一勾，而是给了一个自定义方案……」

🔴 **这句是错的**，且**恰好错在她刚刚提出异议的那一点上**。更正补件应当包含：

1. **认错**：她 8 行判例逐行勾选、无一遗漏；是我方解析只数控件、没读勾选状态，把她的作答判成了没作答。**不是她没勾。**
2. **说清原因**（她已两次遇到"我做了但系统没看见"，须让她知道这次是查明了、不是又一次搪塞）。
3. **告知连带后果并致歉**：判例表 B 三条她 09-04 就裁定了，我方误判为"未获裁定"，**1,813 行那件事因此白等了三天**。
4. 🔴 **判例表 A 的口径纠正**：她判例 1 勾 ❌、判例 3 勾 ✅，而我方止血上线的正是判例 1 那一档 ⇒ **明确告诉她线上会按她勾的判例 3 改**，并给时点。
5. **不再向她要任何东西**——`财务部#17` 的底稿索取仍然有效，补件不加新决策点，避免一次给她两件事。

⚠️ **形态**：走《补件登记》表（`工具-跟进信README登记.py` 的 `--table 补件`／`approve_followup_letter.py --table 补件`），**不占跟进信编号、不违反串行原则**（历史先例：`财务部-唐燕萍-补件-2026-08-20-第13封改发Word版`、`补件-2026-08-24-R5分母全量实测与请你签认一句` 等四份）。
