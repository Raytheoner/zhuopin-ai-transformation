---
title: "需求确认方式升级——判例批改法 + 15 分钟微会（2026-07-27 周一起全域生效）"
created: 2026-07-25
status: 生效
用途: 替代"抽象规则问卷 + 判断预案稿回灌"作为各域 AI 专员需求确认的**唯一提问格式**。Paul 2026-07-25 拍板直接机制升级、不做试点、全域统一。本文件=专线（Cowork 各域）执行规范；专员侧一页说明见同目录《专员协作说明-新版需求确认怎么配合-2026-07-25.md》。
配套: 跟进纪律（CLAUDE.md §5 专员跟进纪律）｜ zhuopin-followup-letter skill（待升 v2）｜ 队列 #109
---

# 需求确认方式升级 · 判例批改法 + 微会机制

## 〇、为什么改（结论来自我们自己的记录，不是理论）

复盘 2026-07 采购域收敛路径，一个规律清晰可见：

- **慢**：口径问卷 → 圈选表 → 对照表 V1 → V2 → 判据说明 V2……每轮 2-4 天，6 轮以上仍有开放点（批 2 三口径至今未收）。
- **快**：① 63 条真实误判案例对照表（姚祖怡批得又快又准）；② 2026-07-14 Paul 与姚线下二次确认（一次谈话解掉 D 段/B1/L·T 三悬案，抵三封信）；③ 2026-07-21 姚拿真实看板截图报 11 条（信息密度超过任何圈选表）。

**诊断**：AI 专员是业务实操者、不是需求分析师。让他们评"抽象规则条文"是用错媒介；让他们评**具体判例**和**真实界面**，又快又准。瓶颈不在专员能力或投入，在我们递过去的东西的形态。

> 一句话原则：**不问规则，只问判例；不靠信件谈判，靠 15 分钟看着真东西定。**

## 一、判例批改法（此后唯一的口径提问格式）

### 1.1 硬规则

1. **禁止抽象设问**。"提前期应该设几天？""关闭状态该怎么定义？"——这类问题一律**不得**直接发给专员。
2. **一律转换为真实判例对照**：从真实数据（ERP/看板/历史单据）取 **≤10 条**真实案例，做成三列对照——`真实场景数据` ｜ `现状判定` ｜ `拟改判定`，专员只做三选一：**✅ 对 / ❌ 错 / ✏️ 改判 + 一句话理由**。

   > 🔴 **2b（2026-08-19 新增，Shao Peishen 当日拍板；成因＝这条缺口第一次真实阻塞了工作）——三个符号必须写明主语，且主语恒定为「拟改判定」那一列。**
   >
   > **它缺了两周多没人发现，直到姚祖怡当场卡住**：2026-08-19 他对 采购部#16 回件——「**中请我批改的选项 √ 和 × 分别代表什么含义，请你说明清楚，否则我无法改判**」。
   >
   > **这不是他的理解问题，是本条的结构性歧义**：上文只写「✅ 对 / ❌ 错」，**没有主语——对什么、错什么**。而两种常见表格布局下它的含义正好相反：
   > - **布局甲**（`现状判定` 有值、`拟改判定` 留白）：❌ ＝「**现状判定**是错的、要改」——采购部#13 即此，见队列 #266 对其 6 个勾选控件的逐格核对记录；
   > - **布局乙**（`拟改判定` 已填好我方建议的新判法）：✅ 既可读作「同意拟改」，也可读作「现状本来就对」——**方向相反**。
   >
   > **⇒ 统一定死：三个符号评的恒定是「拟改判定」那一列，不是「现状判定」。** 「现状判定」列只作对照、不需专员评价。
   >
   > **⇒ 每张判例表的表格正上方必须原样附下面这段图例（不得省略、不得改写）**：
   >
   > ```
   > 🔑 勾选栏怎么读（三个符号的确切含义，请先看这三行再动手）：
   > ✅ ＝「拟改判定这一格填的结果是对的」—— 引擎以后就该这么判。
   > ❌ ＝「拟改判定这一格填错了」—— 引擎不该这么判，但你不必写正确答案。
   > ✏️ ＝「填错了，而且我知道该怎么判」—— 勾它并在后面写一句你认为的正确判法。
   > ⇒ 三个符号评的都是最右边那列「拟改判定」，不是评「现状判定」。每行只勾一个。
   > ```
   >
   > **⇒ 表头三列的措辞同步带上主语**：`✅ 这样判对` ／ `❌ 这样判不对` ／ `✏️ 应改判为（写一句）`，**不再写无主语的「✅ 对／❌ 错／✏️ 改判」**。
   >
   > 🔴 **历史信如何解读（一次性声明，避免回头看批改记录时产生二义）**：**2026-08-19 之前发出的所有判例表，一律按「布局甲」口径解读——即 ✅／❌ 评的是「现状判定」**（已发信按"历史记录不追改"不修改信件本身）。**唯一例外＝采购部#16**：它是布局乙且已发出，其符号语义已由 2026-08-19 澄清答复单独说明（`采购部-姚祖怡-澄清-2026-08-19-判例批改符号语义.md`），按本条新口径（评「拟改判定」）解读。
3. **规则条文由专线反推**：拿到判例批改结果后，由专线归纳成规则条文，回发**一行**请专员确认"我这样理解对吗"（是/否），而不是让专员自己写规则。
4. **案例必须真实**：取自真实 ERP/看板数据（脱敏但不虚构）。虚构样例会让专员脱离直觉、退回抽象思考——命令中心 `:8092` 静态原型的虚构占位数据曾直接导致 11 条误判反馈（队列 #87），教训在此。
5. ~~**一信 ≤3 个决策点**。超过就拆信或转微会。~~ 🔴 **本条已于 2026-08-18 由 Shao Peishen 正式拍板取消**（原话：「**《不超过 3 个决策项》的规则可以突破，我正式拍板**」「**已完成跟进信内容都一次发完，不要被三个决策项数量限制**」）。

   > **触发场景（他当场举的例子）**：质量部#7 是一封「不用做、不用回」的纯通报信，而 C05/C06/C07 的判例表还得再发一封——**两封本可以是一封**。⇒ **把通报类信与有决策点的内容拆开发，是在浪费一次触达，也让专员多收一封信。**
   >
   > **⇒ 新口径（本条替代原第 5 条）**：
   > - **不设决策点数量上限**；**已完成、可交付的内容一次发完**，不为凑数量而拆信。
   > - **通报／告知／认错类内容不计入决策点**——它们不要求对方做任何事。
   > - **保留一项写法要求**：信首仍须**显式列出本信要他做几件事**（开放点计数照旧，见 §三），使他一眼看到工作量；**取消的是上限，不是计数**。
   >
   > 🔴 **三条底线不随本条放宽，逐条声明以免被连带误读**：
   > ⑴ **禁止抽象设问**（本节硬规则 1）——口径类一律仍须转真实判例三选一；
   > ⑵ **判据/口径/阈值类永不默认生效**（本节硬规则 6）——IATF 显式签认红线，与决策点数量无关；
   > ⑶ **跟进信串行原则**（同一收信人同时只能一封在途）与**发送硬前置三条**（改动已合入 master／`.51` 已部署且冒烟通过／用对方原始举证的真实案例端到端复现，2026-08-03 拍板）**均照旧**——**「一次发完」指的是内容组织方式，不是可以发还没上线的东西**（#228 事故即绕过该前置所致）。
   >
   > ⚠️ **一个值得盯住的观察点（可证伪）**：原第 5 条的成因是 §〇 的诊断——「**AI 专员是业务实操者、不是需求分析师**」，≤3 是为了不压垮他们。**放开上限后，若出现回件质量下降（答得潦草、只答前几项、或回件周期变长），说明上限确有其功能，应重议。** 观察指标沿用 §三 既有的「开放口径点从提出到销点的中位天数」，**外加一项新观察：单封信内被跳过未答的决策点数**。
6. **默认预案分级**（重要，勿放宽）：
   - **纯展示/交互类**（列宽、分页、导出、图例、排序）：可标注"超时 48h 按拟改默认生效"。
   - **判据/口径/阈值/计算规则类**：**永不默认生效**——IATF 16949 要求显式签认，超时一律升级 Paul 线下推动，不得以"未回复=同意"落地。

### 1.2 判例包标准结构（跟进信附件）

```
【判例批改表】<场景>-<议题>-<日期>
├─ 一、背景 3 行（为什么问、影响什么、什么时候要）
├─ 二、判例对照表（≤10 行，真复选框 docx）
│    ★ 表格正上方必须先放硬规则 2b 那段「勾选栏怎么读」图例，不得省略
│    | # | 真实场景（料号/单号/客户已脱敏） | 现状判定 | 拟改判定 | ✅这样判对 | ❌这样判不对 | ✏️应改判为 |
│    ★ 三个符号评的恒定是「拟改判定」列（硬规则 2b）；每行只勾一个
│    ★ 每一行都必须是真实案例（硬规则 4）——不得混入「假设某物料需要 15000…」这类构造题；
│      2026-08-19 实证：采购部#16 判例 3 用了虚构数据，与符号歧义叠加，是专员卡住的第二重原因
├─ 三、规则反推初稿（专线写，1-3 句，供专员一眼看"我批完的判例=这条规则"）
└─ 四、开放点计数（本场景还剩 N 项待收敛，本次收 M 项 → 剩 N-M）
```

### 1.2b 材料格式路线（Paul 2026-07-26 定案）

- **短期（现在起）＝Word 真复选框控件**：企微内点开即填、离线可用、我方已有成熟解析能力（`w14:checkbox` 与 `w:sym` 两种勾选状态均可 XML 精确读出，#29/#44 实战验证）。**实现方式（Paul 2026-07-26 定）＝把能力做进 md2word 唯一转换器**：转换时识别 md 里的 `☐`/`- [ ]` 勾选标记，直出 `w14:checkbox` 真控件，**不另建并存工具**（队列 #76 已按此改写，P1）。因此**专线照常写 markdown、照常跑 md2word 即可**，产出的 docx 天生可勾选——不需要记任何"该用哪个工具"的规则，原先"死字符 ☐ 禁用"的禁令在 #76 落地后自然作废。#76 落地前的过渡期，勾选类材料仍注明"可回文字 1/2/3 作答"。
- **中期＝网页表单**：判例包做成 `.51` 上的简单页面，专员从企微点链接打开→真按钮点选→提交即入库；零文件往返、零解析成本、天然留痕、可实时显示"还剩 N 项"。与看板行内反馈按钮同属一套技术，**建造时一并做**（队列 #110）。落地后 Word 真控件降为离线/无网备选通道，不废弃。
- **不采纳的替代**：PDF 表单（Acrobat 依赖+企微内填写体验差）、Excel（挤压案例描述空间）、飞书/钉钉多维表（引入新平台，违背企微单通道原则）。
- **纯文字问答信不附 docx**：如仅需回一两句话的跟进信（例：#91 陈承安全信），md 正文即可；只有**含勾选项**的判例包才必须走真控件附件。

### 1.3 什么仍然用文字信

- 进度告知、材料转交、日期确认、时限提醒——照旧。
- 变的只是**口径/需求确认**这一类，它此后必须以判例包形式出现。

## 二、15 分钟微会（收敛期场景的固定节拍）

1. **触发条件**：任一场景进入"口径收敛期"（有 ≥2 个开放口径点），即为该场景开一条每周固定微会。
2. **形式**：15-30 分钟，屏幕共享**真实看板/真实报告**，专员 + 域专线接口人（Paul 视需要出席；跨域裁决项才必须出席）。
3. **议程固定三段**：① 上周判例批改结果确认（3 分钟）② 本周判例包当场过（10 分钟，能当场定的当场定）③ 下周开放点与谁给什么（2 分钟）。
4. **跟进信的新定位**：**会前材料 + 会后纪要**，不再承担谈判功能。会上定的当场落纪要，机器人当天归档、当天进队列。
5. **允许"当场口头定"**：会中裁决由专线当场记入纪要并在 24h 内以纪要形式回发专员确认（一句"确认无误"即可），不再要求专员另写文档。

## 三、开放点收敛度看板（让专员看见终点线）

- 每个在建场景维护一个**开放口径点计数**：`本场景开放点 N 项 / 本次收 M 项 / 剩 N-M 项`，写在每封判例包的第四节和《部门AI专员协同一页纸》对应域节。
- 目的：专员当前的体感是"信没完没了"；显式计数把它变成"还剩 3 项就收工"，这是最便宜的推动力。
- 度量指标（月度记分牌收）：**开放口径点从提出到销点的中位天数**，目标从现状 ≈2 周/轮压到 **≤1 周**。

## 四、人力配置：加标注带宽，不加决策链

Paul 2026-07-25 明确可加人。**建议加法**：

| 层 | 角色 | 谁 | 做什么 |
|---|------|----|--------|
| 一线标注层（**建议新增**） | 判例初标员 | 各域实操者（采购=解植雅/汤易水，质量=朱映桦已在位，财务待唐燕萍指定） | 判例包**批量初标**（对/错/存疑），把 10 条压成 2-3 条分歧项 |
| 专员决策层（**不加人**） | AI 专员 | 姚祖怡/唐燕萍/陈忱/王泓钦 | 只裁一线标注的**分歧项** + 规则反推确认 |
| 跨域裁决层 | Paul | Paul | 只裁跨域/红线/资源项 |

**不建议加并行专员**：判定权威是单点（如采购口径归姚祖怡），增加并列评审人只会增加协调成本与口径分歧。每线另配一名 **backup**（质量域朱映桦模式已验证）以防休假断档。

## 五、中期方向（本次不建造，登记在案）

**把确认界面搬进工具本身**：专员每天都在用保供看板，在看板行内嵌 `✅正确 / ❌误判+原因` 反馈按钮 → 写 JSONL → 每周机器人汇总报送。需求确认从"回信"变成"使用中顺手标注"，零信件、天然留痕、样本量大。这同时就是队列 #67「错例每周机器人报送节奏固化」的实现形态。SC8 首发，稳定后复制 QD-B/FI2。→ 队列 #110。

## 六、生效与回退

- **生效**：2026-07-27（周一）起，各域专线所有口径类跟进一律按本文执行。
- **回退**：若 4 周内（至 2026-08-24）中位收敛天数未改善且专员反馈更累，回退到原方式，无损失（本次升级不改任何代码、不动已定口径）。
- **skill 同步**：`zhuopin-followup-letter` 升 v2 内置判例包结构与 ≤3 问约束（队列 #109）。
