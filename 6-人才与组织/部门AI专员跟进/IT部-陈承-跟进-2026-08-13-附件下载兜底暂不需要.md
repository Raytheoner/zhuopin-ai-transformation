---
status: 待发
title: "IT 部 · 陈承 跟进信 —— 附件下载兜底暂不需要，这件就此收口（IT 部跟进信第 8 封）"
created: 2026-08-13
收信人: IT部 · 陈承
---

# 陈承，回你一句：那个兜底暂时不用做

（IT 部跟进信第 8 封）

## 一、先谢你的定性 —— 你排除掉的正是我们原来的猜测

`AP-2025120181` 那个 302，你给的结论是**单例、非系统性，而且不是「存储路径写死 localhost」**。

这一句把我们原来那个提问解掉了：我们信里写的猜测就是「某个时间段入库的附件都存了 localhost 路径」，**你排除的恰恰是这条**。既然是单例，我们这边取附件的逻辑就不用加那层容错了。

## 二、答你的问题：暂不需要那个兜底

你问的那句我们看到了 —— 「之前回信附件下载是为了 OCR，现在 OCR 的方案已经否了，不需要附件下载了，如果还确认要的话我们按建议增加登录 cookie」。

**你这句对 OCR 的判断和我们的记录一致，不是误会。** FI2 的 round-2 已在 8 月 5 日改道为「税务系统导出 Excel → 定时取数」，附件下载这条取件路径确实随 OCR 方案一并作废了。

**所以：那个「服务端带登录态调 U9C 下载端点」的兜底，暂时不需要做，不占你的排期。**

方案本身没问题、也谢谢你主动给 —— **如果后续有场景又需要按 AP 单号取附件原件，我再来找你**，到时候直接照你这个思路走就行。

**这件到此收口，你不用再回了。**

---

## 三、顺带同步一件：企微凭据已收到，正在用

上一封信里请你办的自建应用，四项都齐了：**CorpID／AgentID／回调可信域名／可见范围**。

两点想说一下：

1. **你直接给了一个真实域名，把我们预判最容易卡住的那个点绕开了。** 我们原来最担心的就是「内网 IP 能不能登记成可信域名」，还专门问了替代方案 —— 你这样一给，这个问题自动消失了。
2. **Secret 你按信里的红线放在 `.51` 的 `.env` 里、没出现在聊天中，这个做法对。** 口令类的东西就该这么走，后面涉及密钥的协作也照此办理。

**下一步**是拿这套凭据去做统一门户「真人企微身份登录」那一环的真实验证。**排期还没定，定了再同步你**，届时如果需要你在后台配合调一下（比如可见范围微调），我提前说。

---

## 四、三要素

| | 内容 |
|---|---|
| **做什么** | **不用做任何事** —— 附件下载兜底不做，凭据也已到位 |
| **怎么做** | — |
| **什么时候交** | **不用回**；后续 OAuth 验证真需要你配合时，我另外找你 |

---

有问题随时找我。

—— OPVP Shao Peishen
2026-08-13
