---
status: 待发
title: "财务部 · 唐燕萍 补件 —— R5 分母：全量实测结果 ＋ 请你签认一句"
created: 2026-08-24
收信人: 财务部 · 唐燕萍（可分担李姣龙）
性质: 🔴 **对在途承诺的补件，不是新起跟进信** —— 不占部门编号、不进清单新行、不占串行闸
触发: 队列 §一 #375。`财务部#14` 正文已向她承诺「我们这周内拿几张真实的应付单做成一张对照表给你」
需回复: ✅ 是 —— **一个签认**（读法甲 ／ 读法乙 二选一）。🔴 **判据类，永不设超时默认生效**（IATF 显式签认红线）
形态说明: 🔴 **刻意没做对照表，并在信里写明为什么** —— 全量实测 M＝0，硬凑表会得到一张每行结论都相同的表，她看完不知道该勾什么（同「判据恒真、零信息量」一族）。派单件 OP-0823-E §三.3 已预先写死「M＝0 时不得硬凑表」
答复形态: ⚠️ 她连续两封信（#14／#15）都**没有勾真复选框**，而是在正文里插高亮段作答 ⇒ 本件不指望勾选控件，改为留出可直接写字的答复区
代词自检: ✅ 已跑 —— 全文第三人称仅指唐燕萍与李姣龙，二人均为女性（根 CLAUDE.md §1 名录），全部写作「她」；「他」0 处
---

# 唐工，R5 分母这件事，实测结果和我们预想的不一样

（**补件 · 不是新的跟进信**。只有**一件事**要你回：**最后那个签认**。）

> 我们答应你这周拿几张真实的应付单做成对照表给你勾。
> **表我们没做，但不是没干活——是先算了一遍，算完发现那张表做出来会误导你。**下面把实测结果原样给你。

---

## 一、先复述一下要定的到底是哪一句

R5 门禁判一张单是 **L2 自行消化**还是 **L3 转人工**，公式是：

> 这张 AP 单上「金额微差」料品的**差异总额** ÷ **分母** ≤ 0.5%（或差异总额 ≤ 1 元），就 L2 自行消化。

你两次给的都是「**整单（PO 行）合计未税金额**」。这句话我们读出两种意思，这才是一直没定下来的地方：

- **读法甲**（我们现在代码里就是这么算的）：这张 AP 单**实际引用到的那几行 PO** 的未税金额合计。
- **读法乙**：这些 PO **整张单子全部行**的未税金额合计（包括这张 AP 没用到的行）。

乙的分母一定比甲大，所以同样的差异额，乙算出来的比例更小、更容易被判成「L2 自行消化」。

---

## 二、我们把你们的真实数据全量算了一遍，两种读法算出来一次都没打架

范围：**640 张真实 AP 单 · 3,585 项料品**（就是现在面板上跑的那批真实数据）。

| 实测项 | 结果 |
|---|---|
| 参与计算的 AP 单（N） | **640 张**（其中 639 张能完整解析） |
| 料品项合计 | **3,585 项** |
| 其中落进「**金额微差**」这一档的 | 🔴 **只有 1 项** |
| **甲、乙两种分母算出的分流结果不同的单（M）** | 🔴 **0 张** |

**唯一那一张「金额微差」单**（`AP-2026070027`，康卓电子（无锡）有限公司，料品 `F02N.0191`）：

| | 分母 | 差异额 ÷ 分母 | 判定结果 |
|---|---|---|---|
| **读法甲** | 7,074,439.24（引用到的 22 行 PO） | 约 0.00000014% | L2 自行消化 |
| **读法乙** | 18,711,177.14（13 张 PO 全部 51 行） | 约 0.00000005% | L2 自行消化 |

**这张单的差异额是 1 分钱（0.01 元）。**

而且还有一层：**差异额只要不超过 1 元，门禁的「绝对额」那一半就已经先放行了，分母根本没参与判定。** 也就是说这一张单，两种读法不但结论一样，连算都不用算。

---

## 三、所以我们为什么没给你那张对照表

如果按原计划抽 10 张单做成对照表，**表上每一行的「甲判定」和「乙判定」都会写着同一个结果**。你看完只会得出「两种好像都行」，然后不知道该勾哪个——**那张表除了占你时间，什么也说明不了。**

**⇒ 与其给你一张看不出差别的表，不如直接把「实测没有差别」这句话告诉你。**

---

## 四、🔴 但这句话仍然需要你签认一次，不能我们自己定

虽然实测没差别，**但「没差别」是今天这批数据的事实，不是规则本身**。数据一变（比如某张 AP 只引用了一张大 PO 上的一两行），两种读法就会分道扬镳。规则得先定死，不能等撞上了再说。

按 IATF 的要求，**判据类的口径必须由你明确签认，我们不设「不回复就默认执行」**。

### 请你选一个（在下面直接写字或标记都行，不用勾选框）

| 选哪个 | 含义 | 你的选择（在这一列写「√」或任何标记） |
|---|---|---|
| **读法甲**（我们建议） | 分母 ＝ 这张 AP **实际引用到的那几行 PO** 的未税金额合计 | |
| **读法乙** | 分母 ＝ 这些 PO **整张单全部行**的未税金额合计 | |

**我们建议读法甲，理由一句话**：R5 要衡量的是「这张应付单本身有多大误差」，拿它没用到的 PO 行去撑大分母，会让误差看起来比实际小。乙的分母越大，越容易把该转人工的单放行。

> 如果你觉得两个都不对、或者你说的其实是第三种意思，请直接写下来，不用迁就我们给的选项。

---

## 五、顺带告诉你一个我们算的时候才发现的事（不用你回，但你可能想知道）

3,585 项料品里，落进各档的分布是这样的：

| 判定档 | 项数 |
|---|---|
| 完全匹配 | 2,497 |
| 无发票支撑（发票还没到） | 883 |
| **数量金额不符** | **204** |
| **金额微差** | **1** |

**「金额微差」这一档几乎是空的，而「数量金额不符」有 204 项。**

R5 门禁只对「金额微差」生效——照现在这个分布，**这道门禁实际上几乎从来不会被触发**。这可能说明「微差」和「不符」之间的那条线（现在是每料品未税金额尾差 ±0.5 元）划得偏紧，真实数据里的差异要么是 0、要么一下就跳过 0.5 元了。

**这件事不用你现在答**，等你哪天有空看一眼那 204 项里有多少其实是你眼里的「小差异」，我们再一起调那条线。先记在这儿，免得以后发现了又要重新解释一遍。

---

**要你回的只有第四节那一个签认。其余都是告知。**

—— OPVP Shao Peishen

2026-08-24
