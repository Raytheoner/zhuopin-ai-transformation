---
title: "跟进信行日志 采购部#20"
created: 2026-09-06
status: 生效
用途: 跟进信 README 主表编号 采购部#20 的列历史外置件（followup-readme-phase2 D3 口径：单格超阈值即外置，原文原样、可 grep）。行内只留首段＋末段＋指针。
---

---

# 采购部#20 · 主要事项列外置前原文（2026-09-06 外置，4976 B）

**上封那句话是我们错了；判例 4 的真实例子在这儿**（源＝他 `采购部#19` 回件的四项实质答复 ＋ 落地底稿 `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_334_402_姚祖怡0828回件四项落地-2026-08-28.md`〔`OP-0828-U`〕；串行闸起草前现跑 `python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡` ＝ ✅ 开、下一可用号 `采购部#20`）。 ━━━ 🔴 **信首第一节就是致歉，其余往后放**：他在 `采购部#19` 回件里反问「我确实已经勾选了……你是没有看到吗？」——**是我方没看到**。两种读法各跑一遍他 8-26 那封回件（工具内 `read_checkboxes()` ／ 直接拆 Word 读原始标记），**结果逐字一致：9 个复选框、3 个勾上的** ⇒ `采购部#19` 开头那句「我们把 Word 文件逐格读过，9 个格子确实都没有内容」**是错的**，害他多回一封本不必回的信。**成因如实告知：不是工具读不出来，是我方流程那一步压根没去调这个函数** —— 比「工具有 bug」更不该发生。📌 **并主动交代同日第二次同类错**：我方拆他 8-28 回件时用了只扫表格的读法，把他写在**段落里**的两个勾（第 5、6 件）整个漏掉、一度以为他没答，发现后已更正；由此立成硬规矩——**「对方没答」这个结论，永远只能是「我这个读法没读到」的下位**。 ━━━ **请他办两件半（判据类，不设超时默认生效）**：① **判例 4 终局确认** —— 他上封只写了两个字「要例子」，本次真实案例已取到：满足「有逐笔答交、累计仍不足」的物料共 **108／423 行**，信里给前 5 条逐笔明细，🔴 主推 `R01D.1317`（答了 3 笔正数合计 15,000、**却仍差 429**，差得极少，最能说明「判为无答交」是不是合理）；**并如实更正上封一句写宽了的话** —— 原写「另两种算法都会把齐料日算早」，实测「只取估算日」那种 5 条里 4 条完全相同、1 条只差 1 天，**真正算早的只有「取最早那笔」那一种（早 2–6 个月且早得没道理）**。② **保供看板橙与绿两档怎么落** —— 他上封只给了 🔴>2 周／🟡≤2 周两档，(甲) 只留红黄 ／ (乙) 红黄按他定、橙绿另议；🔴 **并必须先告诉他那个反直觉结论：改阈值本身一行都没变色**（新旧阈值四色逐格相同，因「晚 4–14 天」区间在真实数据里是空的）⇒ **真正让看板恢复分级的是规则 1（红 102→66、橙 0→29、黄 0→7），但他定的线不是白定**（缺口缩到周级即开始起作用），**不得让他读成「这个决定没用」**。半件 ③ **窗口外缺口提示的位置**（判例 1 展示层 D3：我方放表头上方＋逐行标注，他签的措辞是「只在页脚」）—— **属做多了不是做少了**，请他一句确认保留还是挪回。 ━━━ **信中一并如实交代两件我方未做成的**：⑴ **规则 1 他已签认「开起来」，今天没开成** —— 我方机器不在厂内网，`.51` 改不了配置也重启不了；🔴 且回内网后**不能只翻开关**，须先核 `.51` 上的代码是否已含规则 1（其代码 8-25 才入主线、`.51` 上次部署是 8-24，**若仍是旧版则开关打开不报错、也无任何效果**，属「工具静默回退」族），正确次序＝先部署并确认真刷新 → 再翻开关 → 再触发重算 → 再核四色确实变了。⑵ **本次四色计数属「变体口径」** —— 取数时 `.51` 不可达、改从源系统重算，且为不抢占携客云配额、不拖慢生产每小时重算而**未跑答交明细的一个补充源**；信中明写「方向可信、绝对值不作数」（本次 66/29/7/1 vs 同期生产实测 71/27/7/0，黄色格逐格相同），**并声明判例 4 那张表里的料号/需求量/逐笔答交/还差多少是真数、不受此影响**。 ━━━ **知会两件不用回**：责任人列按他答的「等 IT，按 (a)」维持「—（取数缺口）」、不做对照表、不再追问；替代料黄金基准按他签认的「按修复后的算法继续跑」执行。**另**：判例 2（多供应商并列）与判例 3（无未交订单显示）实现核实 ✅ **完全一致无差异**；判例 1 **引擎层一致**，差异只在展示层 D1（Excel 导出件完全无窗口外提示，**有实质误导风险，我方修**）／D2（那句提示只写个数不写数量，**我方修**）／D3（位置，请他确认）。 → 目标文件：`采购部-姚祖怡-跟进-2026-08-28-读取缺陷致歉与判例4真实案例.md` ＋ 同名 docx。 ✅ **起草期代词自检已跑**：全文「他」0 处、「她」0 处（通篇第二人称「你」；初稿有一处引述内部规矩时写了「他没答」，已改为「对方没答」以免他误读为指第三人）。
