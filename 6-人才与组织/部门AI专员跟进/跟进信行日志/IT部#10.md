---
title: "跟进信行日志 IT部#10"
created: 2026-09-06
status: 生效
用途: 跟进信 README 主表编号 IT部#10 的列历史外置件（followup-readme-phase2 D3 口径：单格超阈值即外置，原文原样、可 grep）。行内只留首段＋末段＋指针。
---

---

# IT部#10 · 主要事项列外置前原文（2026-09-06 外置，4304 B）

⚠️ **本行收信人列曾误写为「IT工具部 · 陈承」，2026-08-24 当日发出后更正为「IT部」，原因值得记**：`工具-跟进闸查询.py` 的「下一个可用号」是**从本列的部门名推算**的，写成组织架构上的真名（IT 全员名录里陈承确属 `IT工具部`）会让它算出 `IT工具部#1`，**把连续了 10 封的 `IT部#N` 系列劈成两支**。🔑 **⇒ 本列的部门名是「编号系列的键」，不是「这个人名片上的部门」** —— 同 `--department` 参数那条（映射表的键 ≠ 名片上的部门，PMC 一律填采购部）**是同一族第二个入口**：两处都是「看起来该填真名、实际该填键」。 ━━━ **授权不买了，改请他加两个 `/zp/api` 接口**（源＝§四 #105 Shao Peishen 2026-08-24 拍板「不买授权，我们自己实现接口」；需求规格正本 ＝ `1-转型规划/IT接口需求-U9C自建端点-生产工单与采购价目表-2026-08-24.md`）。**信首显式计数「只有 1 件事要你办」** ━━━ **⑴ 先谢一句并点出他这次最有用的那条** —— 他 08-24 回件把根因定在**用友补丁层面而非调用方式**，**这一条直接终结了我方 6 月底以来所有「换个调法再试试」的方向**。 **⑵ 结论：不买 ¥72,000 授权，并 🔴 明确请他停掉采购线**（不用再跟用友谈补丁／不用报预算／若已开流程请回一句以便同步撤）——**理由写成「不是嫌贵，是这笔支出的形状不对」**：按他说的后续每新增 ERP 账号还要再买，等于把 ERP 账号变成按人付费。 **⑶ 改走他已交付过六次的老路**（`ForecastOrder`／`Stock`／`Purchase`／`GR`／`AP`／`ZpViewPurOrder`）：**A `GET /zp/api/PurPriceList/Query`**（采购价目表，先做）＋ **B `GET /zp/api/MO/Query`**（生产工单，可排后）；`MOPickList` 明说可等（FI1 封存中）。 🔴 **⑷ A 排第一的理由写成了一张「现在有三个数是我们编的」表**——实测 `connector.py::get_suppliers()` 的四个硬编码常量：起订量／包装倍数 ＝ 1 ⇒ **建议采购量恒等于缺口本身、这一步等于没算**；提前期 ＝ 30 天 ⇒ **2 天的通用件与 90 天的车规芯片同一个「最迟下单日」，而那正是采购同事排优先级的那一列**；认证状态恒为真 ⇒ **「已认证供应商中选」那道门从未生效、可能推出未认证供应商，IATF 下要出问题**。 **⑸ 两份字段清单**（A：6 必需 ＋ 5 建议 ＋ 4 参数；B：8 必需 ＋ 4 建议 ＋ 7 参数），**含三处硬要求**：`SupplierCode`／`ItemCode` 须与 `ZpViewPurOrder` 同源同值、`UnitPrice` 须注明含未税、`itemCode` 须支持多值。 🔴 **⑹ 第七节四条通用要求，每条挂一个我方真踩过的坑，写成「不是提要求，是把我们摔过的地方告诉你」**：① **行级状态一次给到位**（`ForecastOrder` 头状态恒为 2、跑了一个多月才被两个真实案例证伪；`Purchase` 同族第二次）；② **日期过滤须写明过滤哪个字段**（`AP/Query` 的立账日期坑；类推「**没开工的工单实际开工日必然为空，挂在它上面会把待开工工单全部静默过滤掉——而那恰恰是排程最需要的一批**」）；③ **非法参数必须报错、不得静默返回全表**（`POChange/Query` 坑）；④ 形态沿用既有六接口、我方不改架构。 **⑺ 验收我方承担**：先测试库 → 真实料号逐字段对 U9C 界面核 → 核完再上正式库；**明写「不接受接口通了就算完」**。 **⑻ 两句回执**：附件下载他选 A（§四 #60）已按他说的定了、不再推进；企微自建应用四项早已到位、与本信无关不用再动。 → 目标文件：`IT部-陈承-跟进-2026-08-24-不买授权改自建两个接口.md` ＋ 同名 docx（**已转，frontmatter 未渗入正文、无勾选项故 `w14:checkbox` ＝ 0，均已实测**）。 ✅ **代词自检已跑**：**全文「他」「她」各 0 处**（通篇第二人称「你」，仅第三节表格提到接口名与实体名，无第三人称指代）。 **串行闸**：`IT部#9` 已于 2026-08-24 由 S4 桥二自动转 `📥`，闸开，**无需 `串行豁免：`**。
