---
title: "IT部#11 · 端点 A 详细字段清单 ＋ 一句澄清 ＋ 撤回端点 B"
收信人: 陈承（IT部 · IT 域 AI 专员）
编号: IT部#11
起草日期: 2026-09-02
起草方: Cowork 业务总线 OP-0902
源: 队列 §一 #403 ⑴（§四 #125 拍板 D-1(a) ＋ D-3(a) 合一封）；陈承 2026-08-24 IT部#10 回件
status: 待你审
---

# 陈承，你要的详细字段清单在这儿；另有一句请你先回，还有一个端点我方撤回

先说三件事，第一件只要你回一个字。

| # | 事项 | 你要做什么 | 预计耗时 |
|:--:|---|---|---|
| 1 | 🔴 **一句澄清**：「料号与供应商无强相关」是哪种「无」 | **回 ⑴ 或 ⑵ 即可**，不用展开 | 1 分钟 |
| 2 | **端点 A 的详细字段清单**（你 08-24 要的那份） | 过一遍，看能不能做、要不要改 | 15 分钟 |
| 3 | **端点 B（`/zp/api/MO/Query`）我方撤回** | **不用回，只是别再做了** | 0 |

🔴 **第 3 件请优先看一眼** —— 如果你已经开始做端点 B，**现在停下来还来得及**。原因在第三节。

---

## 一、🔴 先请你回一句：那个「无强相关」是哪种无？

你 08-24 回件第 3 条写了「料号与供应商无强相关」。**这句话我方读不准，而它直接决定端点 A 在技术上成不成立**——所以放在字段清单前面问。

| 可能含义 | 若是这个，会怎样 |
|---|---|
| **⑴ ERP 里根本不维护「某供应商对某料号」这层关系** | 🔴 **端点 A 做不出来**——`UnitPrice`／`IsApproved` 没有承载它们的行。这种情况下我方要改口径，**并把「已认证供应商筛选」这条业务规则显式作废**（而不是让它继续恒真地挂着） |
| **⑵ 关系存在，只是一个料号常有多个供应商、没有唯一主供** | ✅ **端点 A 照常成立**——我方本来就按「已认证供应商中选单价最低」处理多供情形，多行正是我方要的 |

**回一句「是 ⑴」或「是 ⑵」就够了。**

---

## 二、端点 A 的详细字段清单 —— 这就是你 08-24 要的那份

按「料品主档能不能给」切成两块，**这正是判断走 `ItemMaster/Query` 还是自建 `PurPriceList/Query` 时要看到的切分**。

### 2.1 🅰 料品级字段（一行 ＝ 一个料号，你提的 `/webapi/ItemMaster/Query` 能给）

| # | 字段（U9C 侧名） | 我方字段名 | 必需？ | 我方怎么用 |
|---|---|---|---|---|
| 1 | `ItemCode` | `ItemCode` | 🔴 必需 | 主键；须与既有 `ZpViewPurOrder.itemCode` **同源同值**，否则两端点 join 不上 |
| 2 | `purchaseBatchQty` | `MPQ` | 🔴 必需 | 采购建议量按包装倍数向上取整 |
| 3 | `purchaseBatchQty`（同一字段第二用途） | `MOQ` | 🔴 必需 | **按你 08-24 口径，MOQ 与 MPQ 同取此字段**；`MinRcvQty` 我方不取 |
| 4 | `ItemName` | `ItemName` | 建议 | 报表与给采购看的界面，只有料号还要再查一次 |

**⇒ 料品级实际只要 2 个不同字段**（`ItemCode` ＋ `purchaseBatchQty`），加一个建议项 `ItemName`。

### 2.2 🅱 供应商级字段（一行 ＝ 供应商 × 料号，🔴 料品主档结构上给不了）

| # | 字段 | 必需？ | 我方怎么用 | 你 08-24 是否已答 |
|---|---|---|---|---|
| 5 | `SupplierCode` | 🔴 必需 | 与既有 `ZpViewPurOrder.supplyCode` 对齐 | ❌ 未答 |
| 6 | `SupplierName` | 建议 | 同 `ItemName` 理由 | ❌ 未答 |
| 7 | `UnitPrice` | 🔴 必需 | 「已认证供应商中选**单价最低**者」。🔴 **请注明含税还是未税**——既有 `finallyPriceTC` 的含未税口径我方踩过一次坑（面板曾把两列写反） | ❌ **未答（价目表核心字段）** |
| 8 | `IsApproved` | 🔴 必需 | 该供应商对该料号是否已认证／合格。**当前我方代码里这个值恒为 `True`，那道过滤从来没有生效过**——这一条是合规向的，IATF 体系下尤其经不起审 | ❌ **未答** |
| 9 | `Currency` | 建议 | 外币供应商我方另有规则，不能默认人民币 | ❌ 未答 |
| 10 | `EffectiveDate` ／ `ExpireDate` | 建议 | 同一供应商×料号若有多版本，按「`EffectiveDate ≤ 查询日 < ExpireDate`」选当前生效版（该判法我方在 BOM 上已实现过，照搬即可，**前提是接口给出这两个日期**） | ❌ 未答 |
| 11 | `IsPrimary` | 可选 | 是否首选／主供应商（若 U9C 侧有此概念）。有则直接用，无则退回「认证中选单价最低」 | ❌ 未答 |

🔴 **2.2 这七项是本次的「无着落项」，也是本节的结论所在**：**它们决定了这个需求能不能只靠 `ItemMaster/Query` 解决。答案是不能——第 7、8 两项是必需的，而料品主档里没有它们。**

### 2.3 过滤参数

`itemCode`（🔴 **必须支持多值**，逗号分隔或重复传参——我方常一次查几百个料号，见 2.4）／`supplierCode`／`effectiveOn`（按该日期取当时生效版本，缺省＝今天）／`page`／`pageSize`

### 2.4 关于你说的「一次查一个料号、几百个要自己封装」

你指出 `/webapi/ItemMaster/Query` 一次只能查一个料号、需 token、批量须自行封装。**这一点我方确认是真实成本，且它落在我方每一次调用上，不是一次性的**：

- 我方典型批量 ＝ **一次几百个料号**（库存优化每轮全量重算、交期承诺按订单展 BOM）；
- 单料号 × 几百次往返 ＝ 每轮重算多出几百次 HTTP 与一次 token 生命周期管理，**且每新增一个下游场景就再承担一次**；
- 自建 `/zp/api/*` 端点则是**你这边一次性做完、之后所有场景共享**——这正是既有六个端点（`ForecastOrder`／`Stock`／`Purchase`／`GR`／`AP`／`ZpViewPurOrder`）已经走通六次的形态。

⇒ **这不是「哪条路更对」的问题，是「这份封装成本记在谁头上、算一次还是算每次」的问题。**

### 2.5 明确**不要**的字段（免得你多做）

| 字段 | 为何不要 |
|---|---|
| `PurProcessLT`（采购提前期） | **你已两次答复 ERP 该字段不准确、不要取**（2026-07-15、2026-08-24）。**我方接受，不再索要**，另寻来源。⚠️ 但请知悉：我方提前期目前是写死的 30 天，这个洞并没被补上，只是确认了不从 ERP 补 |
| `MinRcvQty`（最小收货量） | 按你 08-24 口径 MOQ 改取 `purchaseBatchQty`，**此字段不再取** |

---

## 三、🔴 端点 B（`/zp/api/MO/Query`）我方撤回 —— 如果你已经在做，请停下

**这是本信最该先看的一件，因为它能直接省掉你的工时。**

**撤回理由（来自你自己 08-24 那句话）**：你转述 PMC——「**跟 PMC 沟通目前 ERP 没有生产工单，无车间／产线，经外协供应商生产**」。

⇒ 我方据此核实后确认：**`UFIDA.U9.MO.MO.MO` 里没有数据。端点即便建成，返回的也是恒空表。**

🔑 **所以这不是"降优先级"，是"这件事做出来没有用"**——我方不该让你去建一个必然为空的接口。**我方内部已把依赖它的那条解锁条件改判掉了**，不再等这个端点。

**替代方案我方自己解决，不需要你做任何事**：全程委外采购订单（`erpTypeCode ∈ {PO14, PO16, PO17}`）已经在我方手上、且已在用，**无须新建任何端点**。

⚠️ **一句如实说明，免得你以为问题全解决了**：委外 PO 能告诉我方「哪些成品在外协在产」，**但它没有开工日／完工日／工时** ⇒ 我方原本要的「工时是多少」仍然无解。**这是我方自己的范围问题，不是你的欠账，也不需要你回复。**

---

## 四、几句知会（都不用回）

1. **完整的需求规格是我方内部文件，本信已把与你相关的内容全部摘出** —— **你不需要另找任何附件**。（上一次我方给质量部的信里写了「见附件」而实际没有附件，让人白找了一趟；这次先说清楚。）
2. 上一版（v1）里请你自建两个端点，**现在只剩端点 A 一个**。
3. 端点 A 的验收由我方承担，不占你的时间。

---

## 五、汇总：请你回的只有一件

🔴 **第一节那句「是 ⑴ 还是 ⑵」。** 其余都是清单与知会。

- **若你答 ⑵**（关系存在、只是多供无主供）⇒ 我方按第二节清单请你自建 `PurPriceList/Query`，字段以 2.1＋2.2 为准。
- **若你答 ⑴**（ERP 根本不维护供应商×料号关系）⇒ **端点 A 也不用做了**，我方改口径，并把「已认证供应商筛选」这条规则显式作废。**那样你这边两个端点都不必做。**

**不卡时间，方便时回即可。**

---

**IT部 · 陈承　收**
Cowork 业务总线 · 2026-09-02
