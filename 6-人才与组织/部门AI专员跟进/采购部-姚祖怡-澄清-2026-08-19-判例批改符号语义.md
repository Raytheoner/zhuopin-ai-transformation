---
status: 已执行归档
title: "采购部 · 姚祖怡 澄清答复 —— 判例批改表 ✅／❌／✏️ 的确切含义（对采购部#16 的澄清，非新起跟进信）"
created: 2026-08-19
收信人: 采购部 · 姚祖怡
性质: 🔴 **对在途信 采购部#16 的澄清答复，不是新起跟进信** —— 不占部门编号、不进 README 清单新行、不占串行闸
批准: Shao Peishen 2026-08-19 会话内批准（定夺 1 选 (a)）
发送方式: aibot 私信（`邵总数字人🤖` → `YaoZuYi`）**＋ 抄送采购部群**（Shao Peishen 2026-08-19 追加指示）。🔴 **两条通道都必须走 aibot chatid，不得走任何 webhook** —— #282 拍板：webhook 受众已收敛为「Shao Peishen ＋ 陈承」，**业务部门此后不从任何 webhook 收消息**（他 2026-08-06 原话：「免得他们直接给他回信」，依据＝webhook 单向、群成员回复进不到任何地方）；群抄送的正解是 #270／#281 迁移后的 **aibot chatid 通道**（`push_followup_letter.py --department 采购部` 即走该通道；`WECOM_WEBHOOK_URL_PROCUREMENT` 按 #282 属应退役键，**禁用**）
已发送: 2026-08-19 09:49 本地（01:49Z）——aibot 私信 `YaoZuYi` ＋ 抄送采购部群 `wrvDL_DAAAH7IL6I7hsgszeyQvmyjw-Q`，两次 ack 均 `errcode=0`；审计 `clarification_sent` 两条落 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`。执行方 CC-material-board-view-df54d1。**未用 `push_followup_letter.py`**（绑 README 台账行、跟进信专用）；**两条通道均未走任何 webhook**（#282）。
触发: 姚祖怡 2026-08-19 回件（队列 §一 #349／#350）——「中请我批改的选项 √ 和 × 分别代表什么含义，请你说明清楚，否则我无法改判」
---

# 澄清答复正文（发送时只发下方代码块内内容）

```
姚工，抱歉——那张表的 ✅ / ❌ 我没写清楚是在评哪一列，是我的问题。

三个符号评的都是最右边那列「拟改判定」，也就是我们打算改成的判法：
· ✅ ＝ 这样判是对的，引擎以后就该这么算
· ❌ ＝ 这样判不对，但你不用写正确答案
· ✏️ ＝ 不对，而且我知道该怎么判 —— 勾它并写一句你认为的正确判法

每行只勾一个。「现状判定」那一列不用评，它只是给你对照现在是怎么算的。

另外你确认的第三条（有答交记录但数量为 0，等同于没有答交、走规则 2）我已经收到，
谢谢——那条不用再答了。
```

---

## 抄送采购部群的理由（Shao Peishen 2026-08-19 追加）

**不只是让姚祖怡不必转述。** 判例批改表此后还会发给采购团队其他人（一线标注层：解植雅／汤易水，见判例批改法 §人力分层），**符号语义在群里公开一次，等于给所有后续判例表建立了共同读法**——比每封信各自解释一遍便宜，也避免同一套符号在不同人那里再次分叉。

🔑 **顺带一层**：这条抄送的内容是「**我表格没写清楚，是我的问题**」。公开认错比私下更正更能维护 #67 试点反馈 loop 的信任——他 2026-08-19 提这个问题时用的是「否则我无法改判」，那是**被工具挡住而不是不配合**，群里说清楚对他也是个交代。

## 为什么单独落一份档（不是随手回一句）

**因为这不是他理解力的问题，是判例批改法的一个结构性缺口，而它今天第一次真实阻塞了工作。**

判例批改表的表头一直写作「✅ 对 ／ ❌ 错 ／ ✏️ 改判」，**没有主语——对什么、错什么**：

- **采购部#13** 那种布局（`现状判定` 有值、`拟改判定` 为「待定」）里，❌ ＝「**现状判定**是错的、要改」——见队列 #266 对其 6 个勾选控件的逐格核对记录；
- **采购部#16** 这种布局（`拟改判定` 已填好我们建议的新判法）里，✅ 既可读作「同意拟改」，也可读作「现状本来就对」——**两种读法方向正好相反**。

**判例批改法规范 §2.3（L27／L55 表模板）与 `zhuopin-followup-letter` skill §2.2／§2.3 同样只写「三选一：✅对／❌错／✏️改判」，均未定义主语。** ⇒ **缺口在规范层，不在这一封信、更不在他。**

## 连带处置

| 对象 | 处置 |
|---|---|
| **质量部#8**（`⏳ 待你审`，未发出） | ✅ **已当场修**：Q1 表前加三行符号语义图例、表头三列改为带主语的「✅ 这样判对／❌ 这样判不对／✏️ 应改判为」，docx 已重转（真复选框 56 枚、裸方框 0、预勾选 0）。批次 `B-0819_03`／`B-0819_04` |
| **采购部#16**（已发出） | 按「历史记录不追改」**不动信件本身**，只发本澄清 |
| **规范层 §2.3 ＋ skill 两处 ＋ 历史信解读声明** | Shao Peishen 2026-08-19 已答定夺 2 选 (a) 改，**下一批执行**（走 2026-07-25 判例批改法既有先例「规范文件＋拍板」，不新建 openspec 变更包） |

## ⚠️ 同批查出的第二处违规（本澄清刻意未提，以免分散他注意力）

**采购部#16 的判例 3 用的是虚构数据**：「假设某物料需要 15000，答交是 2026-10-20 给 8000、2027-01-20 给 9000」。

判例批改法硬禁令明写 **「❌ 判例包使用虚构/样例数据（必须真实脱敏）」**，理由是**虚构样例会让专员脱离直觉、退回抽象思考**（依据＝2026-07 命令中心 `:8092` 静态原型的虚构占位数据直接导致 11 条误判反馈）。

🔑 **这很可能是他卡住的第二重原因**——前两条判例是真实料号 `R01I.0622`，第三条突然变成假设题，**符号歧义 ＋ 题型切换叠加**。
⇒ 已登记，随规范层修法一并处置；**是否需要为判例 3 补一条真实案例后重问，留 Shao Peishen 定**（本件不代定）。

## 发送后

- 队列 §一 **#349／#350** 销行（已回灌本件）。
- README **采购部#16** 行追记：他已答第三条规则确认（「对」），另两项待符号澄清后补答；**该行仍非闭环态，姚祖怡串行闸仍锁着，SC2 判例包（拟采购部#17）仍不能起草**。
