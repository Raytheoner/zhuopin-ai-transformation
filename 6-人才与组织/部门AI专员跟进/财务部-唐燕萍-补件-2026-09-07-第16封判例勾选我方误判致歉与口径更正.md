---
title: "补件 · 第 16 封判例表：你确实全勾了，是我方没读出来（致歉与口径更正）"
关联: 财务部#16（2026-08-28 发出）／财务部#17（2026-09-07 07:37 发出）
created: 2026-09-07
status: ⏳ 待你审
收信人: 财务部 · 唐燕萍（财务总监 / 财务域 AI 专员）
决策点: 0 项（我方认错、取证结论与口径更正通报，不需要你做任何决定）
配套: 取证件 `6-人才与组织/部门AI专员跟进/财务部16回件勾选误判-取证与更正-2026-09-07.md`／队列 §一 #423（面板 KPI 口径）／§一 #424（摄取层认领口径）／§四 #164（机制缺陷）
---

# 唐燕萍，你确实每一行都勾了——是我方没读出来，向你道歉

（补件，承第 16 封；**不用回，也不需要你做任何决定**）

## 一、先说结论：错在我方，不在你

你今天提出「每次都勾了」之后，我方把你 9 月 4 日回的那份 Word 重新拆开逐格核了一遍。**核出来的结果是你对的**：

- 三张判例表**共 8 行判例，你逐行勾选，一行都没漏**；
- 机器计数：已勾的格子 **8 个**，未勾的 **16 个**，两种读法（Word 的勾选控件、以及方框字符本身）**结果完全一致**。

而我方 9 月 4 日的拆件结论写的是「勾选格实测均为空」。**这句话是错的。**

**错的原因说清楚，免得你觉得又是一句搪塞**：我方的程序去数了"这里有几个复选框"，数出 24 个；**却没有再往里读一层"每个框到底勾没勾"**。于是 24 个框全被当成空的。**是我方的检查只做了一半，不是你没勾。**

⚠️ 你反映的「最后一列『应改判为』无法输入文字」**确有其事**，我方已在修。**但那个毛病只挡住第三列写字，挡不住勾选**——我方却由它推出「你这次没用勾选作答」，**这一步是我方想当然，也是错的**。

## 二、你实际勾的是什么（原样列出，请核对我方有没有再读错）

### 判例表 A · 面板那个「3390」

| 判例 | 你勾的 |
|---|---|
| 1 | ❌ 这样判不对 |
| 2 | ❌ 这样判不对 |
| **3** | **✅ 这样判对** |
| 4 | ❌ 这样判不对 |

并且你在表下写了：希望是**判例 3 的展现形式**，同时保留判例 4 的功能但**不在面板上展现孤立发票**，也不要把它们过滤掉，而是**留在未匹配成功的发票池**——因为那些发票可能只是业务还没立账，等立账时要用。

⇒ **我方完全读懂了，而且这四个勾与你写的那段话是一致的、不矛盾。** 之前我方把它记成「你没按四选一勾、只给了一个自定义方案」，**也是错的**：你是勾了，再把勾选表达不了的那一层用文字补上。

### 判例表 B · 那 1,813 行被丢掉的发票

| 判例 | 你勾的 |
|---|---|
| 5（密封胶，20 支 vs 6200 的单位换算） | ✅ 这样判对 |
| 6（该 AP 单通篇只有一个料号） | ✅ 这样判对 |
| 7（气泡袋，同料号多行按合计开票） | ✅ 这样判对 |

### 判例表 C · 一张发票行拆两条

| 判例 | 你勾的 |
|---|---|
| 8（跨两个料号合计开票） | ✅ 这样判对 |

并附你的一条算法约束：**拆行后两条的未税金额与税额分别相加，必须和发票行一致；分摊产生的尾差并入已有的最后一条记录，不单起一条。**

## 三、这个错让你白等了，一并道歉

**判例表 B 那三条，你 9 月 4 日就裁定完了**，而我方一直记成「未获裁定」。那意味着：**被系统丢掉的 1,813 行发票、421 张 AP 单（其中 221 张一行发票都没有）的处理口径，你三天前就给了答案，我方却还在等你。** 这三天是我方自己耽误的。

🔴 **还有一件更要紧的，必须让你知道**：判例表 A 那一档，我方 8 月 28 日先按**判例 1** 止血上线了。**而你对判例 1 勾的是 ❌、对判例 3 勾的是 ✅** —— 也就是说，**你现在打开面板看到的那一档，正是你判为"不对"的那一档**。

## 四、我方接下来做什么（都是我方的活，不用你回）

1. **面板改判到你勾的判例 3** ——「装载前先按本次 AP 范围过滤」那一档，已列为最优先。改好后我方单独告诉你，请你到面板上看一眼是不是你要的。**在改好之前不敢给你一个具体日子，免得又是一句空话。**
2. **你那两条界面需求已收下**：⑴ 不输 AP 单号、不选期间时，默认展示所有未立账的 AP 单；⑵ 查询具体 AP 单号的功能移到关键汇总面板下方，人员与期间改成下拉框。
3. **判例表 B 的三条按你的裁定开工**，那 1,813 行的认领逻辑不再等任何人。
4. **判例表 C 的尾差算法**按你写的做（并入最后一条，不单起一行）——这一条我方此前的实现与你一致，不用改。
5. **修我方自己的毛病**：⑴ 「最后一列输不了字」的表格格式；⑵ 更要紧的是那个只数框、不读勾选状态的程序——已经定了改法：**今后必须两种读法都读、两边对不上就直接报错停下**，不许再出现"悄悄读成全空"这回事。

## 五、上午那封信不受影响

今天上午发你的第 17 封（借近 2–3 年的跌价准备计提底稿）**内容照旧、不用因为这封补件改什么**。

⚠️ 那封信里有一句写错了——「判例表 A 你没有按四选一勾」——**就是本补件第二节更正的那句，以本补件为准。**

## 六、最后一句

这已经是第三次**你先开口、我方才发现自己的毛病**（第 13 封只发了不能编辑的 md、第 16 封最后一列输不了字、这一次的勾选没被读出来）。**每次都是你在替我方兜底。** 我方已经把「回件到达后必须用另一种方法验一遍」写成一条硬规矩，让它不再依赖你开口。

谢谢你一直照直说。

**OPVP　Shao Peishen**
2026-09-07
