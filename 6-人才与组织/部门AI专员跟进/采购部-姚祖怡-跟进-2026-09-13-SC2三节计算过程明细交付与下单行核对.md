---
title: "采购部#23 · SC2 周报三节「计算过程明细」交付 —— 下单行逐行清单与核对锚点"
created: 2026-09-13
status: ⏳ 待你审
决策点: 1 项（a 核对反馈类：附件 187 行明细与你线下核算是否对得上；若对不上，差额落在哪一类）
---

# 采购部#23 · SC2 周报三节「计算过程明细」交付

姚祖怡：

**本信要你做 1 件事** —— 把附件里的 187 行明细与你线下算的那一遍对一次，回一句「对得上」，或者「对不上，我的数是 N」。其余内容都是我方交代，不用你动手。

## 一、先说你那次核算

9 月 10 日你回 `采购部#22` 判例批改表时，七行全部勾了「这样判对」；同一封回件里，你自己线下把周报的数重算了一遍，并写明：「你的周报数据和我线下计算的数据无法对起来……例如你周报中下单行数是 187 行，你把这 187 行的明细列出来反馈给我，然后我们线下人工一一核对后进行运算逻辑及取数差异的纠正。」

这一问直接改了我方的做法。**此前周报只出汇总数，没有任何办法看到这个数是怎么来的**——你要核对也无从下手。现在下单／收货／在途三节都能导出逐行明细，并且明细与周报指标**由同一套代码算出**：只要明细行数与周报上的数字对不上，系统直接报错、拒绝出文件，不会给你一份「看起来对、数却不一致」的清单。

**这个能力是因你这一问才建的。**

## 二、请你核对的事（做什么／怎么做／什么时候交）

**做什么**：核对附件 `sc2_2026-W36_下单_本周.csv` 的 187 行。

**怎么做**：Excel 直接双击打开即可（文件带 BOM，中文不会乱码）。共 14 列：采购单号、行号、制单日期、单据类型、料号、供应商编码、供应商名称、采购员、确认数量、含税单价、下单金额、累计收货、行状态、未清数量。

**什么时候交**：2026-09-19（周五）前回一句话即可，不需要写任何文档。

> 💡 187 行逐行看是个体力活。采购域的一线标注层已经在位（解植雅、汤易水两位），**可以请这两位先批量初核，你只裁分歧项**。

### 一句话核对锚点

这 187 行 ＝ **ERP 里 `ZPCG20260907001` ~ `ZPCG20260907033` 这 33 张单的全部订单行**（已去掉其中 1 行全程委外 PO17）。逐项特征如下，你可以先用这几条粗核一遍：

| 维度 | 本次取到的结果 |
|---|---|
| 制单日期 | **187 行全部是 2026-09-07（周一）**；09-08 至 09-13 这五个工作日一行都没有 |
| 采购单号 | 33 张，`ZPCG20260907001` ~ `ZPCG20260907033` 连号 |
| 单据类型 | PO22 ×185、PO23 ×1、PO01 ×1 |
| 涉及面 | 101 个料号、25 家供应商、5 位采购员 |
| 行状态 | `0 开立` 4 行／`2 已审核未交清` 171 行／`3 自然关闭` 2 行／`4 短缺关闭` 10 行 |

🔴 **请你顺便确认第一条**：本周（09-07 ~ 09-13）是不是真的只有周一那一批 33 张单？这是机器从 ERP 取到的结果，数据本身没有问题；但「整周只有一天下单」到底是不是正常的作业形态，得由你来说。

### 如果你数出来不是 187

差额只可能落在三处，请在回信里指一下是哪一类，我方据此纠正取数与运算逻辑：

1. **是否也剔掉了全程委外（PO17）那一行**；
2. **数的是订单行、还是交货计划行**；
3. **取数时点不同**（本次取数时刻是 2026-09-13 20:10，此后 ERP 若有新增行或作废，本清单看不到）。

### 另外两个附件

- `sc2_2026-W36_收货_本周.csv` —— 58 行，等于周报上的「收货行数」。
- `sc2_2026-W36_在途_本周.csv` —— 187 行，**比「未清行数」多**。这是有意的：它把本周全部下单行都列出来，用「计入未清行数」一列标出哪 175 行被数了进去，其余 12 行在「剔除原因」列写明为什么被剔（2 行自然关闭、10 行短缺关闭）。**你要核的恰恰是这些剔除理由**——只给「数进去的行」，等于把判据藏起来了。

上周与上月同期两个窗口的另外六个文件也已生成，需要的话回一句，我方一并发你。

## 三、顺带交代两件（不用你动手）

1. **你 9 月 9 日看页面时那个「期次陈旧」的毛病已经修好。** 你当时看到的页面停在 8 月 27 日那一期，成因是服务启动时把「今天是几号」冻住了一次、此后再没更新过。修复已于 9 月 9 日上线，今天（9 月 13 日）又复核了一遍：页面基准日已经跟着当天走。
2. **另一项「计算层口径调整」按原计划顺延**，先把明细核对这件事做完，再动那边——避免两件事同时改，出了差异说不清是哪一处引起的。

## 四、你名下的资产

本次新增的「计算过程明细导出」能力——三节 × 三个时间窗口、逐行 CSV、含剔除原因列——**源自你 2026-09-09 的这一问**，现已作为 SC2 周报的常设能力入库，后续每一期都能导。

你在 `采购部#16`、`#17`、`#22` 三封判例批改表上的结论，也已全部进入采购域口径台账。这些判定是**署名的、可追溯到你本人的**——将来引擎按这些口径算出来的每一个数，依据都写着你的名字。

随附《部门 AI 专员协同一页纸》采购域节。有问题随时找我。

OPVP Shao Peishen
2026-09-13
