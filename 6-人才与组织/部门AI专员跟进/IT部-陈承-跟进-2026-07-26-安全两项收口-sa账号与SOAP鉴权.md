---
title: "IT 部 · 陈承 跟进（2026-07-26）——安全两项收口：U9C 生产库 sa 账号 + /Services SOAP 免鉴权"
created: 2026-07-26
status: 待发（待 Paul 审）
收件人: 陈承（IT，userid 2023458）
抄送: Paul（机器人自动抄送）
关联: 队列 #91 ｜ 依据 `7-外部文档/U9C库存取数-侦察结果与推荐-2026-07-05.md`（本地）
---

# 陈承，两件安全项想请 IT 收口

## 背景一句话

AI 项目取数路线已在 7 月全面切到 **apiKey 接口**（`Stock/Query`、`Purchase/GR/AP Query` 等），**不再需要任何数据库直连**。因此下面第一项对我们这边零影响，纯粹是把不再使用的高权限入口关掉。

---

## 第一项：U9C 生产库 `sa` 超管账号（建议轮换或禁用远程登录）

**做什么**
请轮换 `192.168.6.2`（U9C 生产库）的 `sa` 口令，或按 IT 惯例**禁用 sa 远程登录**——两者取其一即可，以 IT 的规范为准。

**为什么**
今年 6 月早期验证阶段（supplychain 试验项目），我方曾用 `sa` 账号做过只读取数验证，口令以明文形式存在本地配置文件中。该项目现已封存退役、**当前所有在建场景均无 DB 直连**。虽然配置文件从未进入代码仓库、不存在外泄记录，但只要该口令仍然有效，这个超管入口就一直敞着——**关掉它是最干净的处置**，也免去我方再维护一套凭据。

**怎么做（供参考，以 IT 判断为准）**
- 方案 A（推荐）：轮换 `sa` 口令，新口令不必给我方；
- 方案 B：保留口令但禁用 sa 的远程/网络登录，仅限本机；
- 我方无需最小权限只读账号——apiKey 接口已完全覆盖需求，**不用为我们新建任何数据库账号**。

**什么时候**
建议 **8 月 1 日前**完成。完成后回一句"已轮换/已禁用"即可，我方同步清理本地残留配置。

---

## 第二项：U9C `/Services` SOAP 接口免鉴权（建议加网络 ACL）

**做什么**
请评估并处置 U9C `/Services` 路径下 SOAP 接口的**无鉴权访问**问题，建议三个动作：
1. **网络 ACL**：限定仅允许必要的服务器/网段访问该路径；
2. **核实写端点**：确认是否**写操作端点同样免鉴权**（这决定风险等级——只读是信息泄露，可写则是数据完整性风险）；
3. **关闭详细异常回显**：目前异常信息会返回内部实现细节，建议生产环境改为通用错误信息。

**为什么**
7 月 5 日我方在做库存取数方案侦察时实测发现：调用 `/Services` 下的 SOAP 接口**无需任何身份凭据**，仅凭请求体中自填的 OrgID 即可读取**任意组织**的库存数据。这一项与我方是否使用无关（我方最终选择了 apiKey 路线、未采用 SOAP），属于**服务端本身的暴露面**，因此转给 IT 评估。

**什么时候**
建议 **8 月中旬前**给出处置结论或风险接受说明（若 IT 评估认为内网环境下可接受，也请书面回一句，我方留档即可）。

---

## 需要你回什么

一条消息回两句就行：
1. sa 账号：已轮换 / 已禁用远程 / 其他处置（哪种都行）；
2. SOAP：处置计划或风险接受结论。

回复直接发本机器人即可自动归档，无需邮件。

## 附带一项知会（无需动作）

我方办公室新增一台 **Mac Studio**，将 7×24 运行 AI 项目的内部消息监听服务（仅企业微信消息接收与文件归档，不对外提供服务）。如需走 IT 资产登记或网络备案流程，请告知所需信息，我方配合办理。

---

**Paul Shao**
OPVP · 卓品智能 AI 转型项目
2026-07-26
