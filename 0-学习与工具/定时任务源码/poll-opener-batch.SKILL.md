---
name: poll-opener-batch
description: 只读探测卓品仓库 reports/opener-batch/ 有无新收工或新停滞的无头 CC 泳道批；[NO-SIGNAL] 立即空跑结束，[SIGNAL] 由脚本自行推运维群。把「棒跑完了没人知道」从人守转机器守。
---

卓品智能 AI 转型项目 · 无头棒收工探针（承接队列 §一 `#551`／根 `CLAUDE.md` §5 `UPS5:7`）。

🔴 **本任务要极省**：绝大多数次都应该是空跑。**只做下面第 1 步，按结果决定要不要做第 2 步，其余一律不做**——不读 CLAUDE.md、不读队列、不读接力卡、不跑 git、不开子任务。

**第 1 步（唯一必做）**：在本机跑

```
cd C:\Dev\zhuopin-ai
python "0-学习与工具\工具-无头棒收工探针.py" --via-scheduled-task
```

🔴 **`--via-scheduled-task` 这个旗标不能漏**：它只决定企微推送的落款写「定时任务」还是「手工运行」。漏了会把机器守报成手工，**让人以为没在跑**（2026-09-10 实撞过反方向：落款写死成「定时任务」，而那条其实是手工推的）。

**第 2 步（按输出分支）**：

- 输出以 `[NO-SIGNAL]` 开头 ⇒ 回一句「本轮无新事」，**立即结束，不做任何其它动作**。
- 输出以 `[BASELINE]` 开头 ⇒ 回一句「已基线化」，**立即结束**。
- 输出以 `[SIGNAL]` 开头 ⇒ 脚本**已经自己推过运维群企微了，不要重推**。你只需把脚本打印的正文原样复述一遍作为本次运行记录，并额外做**一件**只读核查：对每个报出的批，跑
  ```
  Get-Content "C:\Dev\zhuopin-ai\reports\opener-batch\<批目录名>\summary.txt"
  ```
  把 Status 列里**非 OK** 的泳道单独列出来（`NO-SENTINEL`／`PARTIAL`／`FAIL(n)`）。**不要试图去修、去续跑、去合分支、去改队列**——那些都要 Shao Peishen 在 Cowork 里发话才做。
- 脚本报 `[SIGNAL] 探针自身异常` ⇒ 原样贴出 traceback，说明「探针坏了，需人工核 reports/opener-batch/」，不要自行修脚本。

**🔴 本任务永不做的事**：不发任何对外消息（企微由脚本自己发，且只发运维群）；不碰 `.51`；不 commit、不 push、不 ff master；不写两份队列真身；不起任何 CC 泳道或子任务。

**为什么有这个任务**：2026-09-10，两批无头棒 14:47 收工，直到 16:18 Shao Peishen 主动来问才被发现，空转 90 分钟——因为 Cowork 会话不会自己醒。本任务就是 `UPS5:7` 判据里那条「能当场指名的机器规则」。**它的职责只有『让人知道』，不包括『替人接着做』。**