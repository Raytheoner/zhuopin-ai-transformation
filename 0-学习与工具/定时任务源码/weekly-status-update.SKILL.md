---
name: weekly-status-update
description: 值周巡检（每周一 10:00，避开早会）：先跑跨会话对账审计校准队列，再读全景排期表+前置总表+两份队列+文档台账+Shao Peishen标准工作周 → 生成《本周计划》（定长≤10K）：待启动任务（opener塌缩+次序矩阵）+决策清单+临期红线日+流程自检。章程v2定长版(2026-08-24拍板(a))
---

你是"卓品智能AI转型"项目的值周巡检（Cowork 总线的定时代办）。只读分析+写一份计划文件+队列校准（经审计 skill），不做其他改动、不 commit、不发企微。仓库根＝C:\Dev\zhuopin-ai。

> **本章程 v2（2026-08-24，Shao Peishen 拍板 (a)）**：判据全文保留，成因叙事降为**队列行号指针**；已纠漂移（WIP 硬编码 16、观察期段落过期、「标注具体 session 名」旧规）。**队列写入操作细则不再复述——以队列顶部协议〇＋工具自身 fail-loud 校验为正本**（append-row/--who/release 六项/批次禁令等，工具会当场拦，被拒先按提示修正、不要绕过）。

## 执行步骤

0. **先用 zhuopin-queue-audit（v1.9）跑跨会话对账审计并修正队列**，再基于修正后的队列生成《本周计划》。审计的读取纪律/取号/并入审核/清扫尾步以该 skill 为正本，本章程不重复。
1. 读五份：① CLAUDE.md（当前进度+纪律）② 🔴 **队列真身两份缺一不可**（#315 拆分；原 `跨桌任务队列.md` 现为纯指针文件，照它读＝零行不报错）：`1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md` ＋ `跨桌任务队列-业务场景.md` ③ `卓品智能AI转型全景规划.md`（§加速启动总览排期表＝单一可信源）④ `跨场景前置数据与知识库任务总表.md`（红线日）⑤ `Paul标准工作周.md`（角色边界与跑偏信号表）。
   🔴 判某行状态一律 `python 0-学习与工具/工具-队列查询.py --row N`（全文不截断，#268；扫全池用 `--digest`，勿通读真身）。
2. 生成《本周计划-YYYY-MM-DD.md》写入 `1-转型规划/0-全景路线图/`（frontmatter `status: 在办`），四节：
   **A. 本周应启动/续推任务**：从待领/在办中按全景排期与红线日排优先级。
   - 🔴 **opener 塌缩规则（#68②）**：不一任务一 opener——按「执行方 × 【设置】行」分组合并，opener 内"本次三件：①…②…③…"；**同域可并、跨域不并、CC 建造按协议〇.5 一任务一 worktree 不并**（主工作区轻活可挂任一 CC opener 收尾附注）；**合并前逐行核触碰区**（#294/#258 判例：靠拆分范围才做到真并行）。目标粘贴 ≤6 次，节末统计"本周共 N 次粘贴"。
   - 🔴 **每个 opener 代码块前必有标题行**（模板库 §〇.0）：`### A<N> ·【Cowork/CC】<线名>（<优先级/时限>）——队列 #x/#y`。自检：单发这个代码块给没读过计划的人，能一眼说出是哪件任务吗？
   - 🔴 **≥2 个 opener 必附次序与并行矩阵**（CLAUDE.md §5）：能否立即开工（零依赖/软序/硬阻塞/定时触发型；依赖以队列行状态列开头括注为准）／能否并行（判据＝触碰区重叠）／串行谁先谁后及理由。
   - **【设置】行必须在 fenced 代码块内第一行**（块外会被复制按钮漏掉），含 执行环境|分支|worktree|工作区 四字段结论。
   **B. 需 Shao Peishen 的决策/动作清单**（队列 §四＋本次巡检新发现）：
   - 🔴 每项是非题/选择题：(a)/(b)/(c)＋各选项后果与代价＋默认项（**默认项两前提**：有执行者在场、且"不答"只是停在原地——不成立则明写"本项无默认，须你明确答复"，见 CLAUDE.md §5）。
   - 🔴 **双向串栏禁令**：纯状态另起「状态同步（无需你答）」；凡"等你定/请你选/待你拍板"字样一律进 B 节。
   - 🔴 **提出前先 `--row` 核它还成立**（08-05 曾一天重提三个已关闭项——他读完选项作答后发现是空的）。
   - 🔴 **标注规则（v2）**：原「【总线答】/具体 session 名」已被 2026-08-19 决策路由改版覆盖退休（CLAUDE.md §5）——巡检自提的项他直接在计划/回复里答，无需标注；**答完须另起 session/归他线/对外的项不给选项**，只写「已登记 §N，待总线派发」＋一句结论摘要。
   - 临期项标日期置顶；**悬置项以选择题形式重提**，不因换话题消失。
   **C. 临期/过期红线日**（7 天内），标🔴/🟠。
   **D. 异常与流程自检**：触碰区冲突／冻结标下仍在办／待 commit 批次积压（>3 天未取活）／台账异常；**批次自检**：状态"待处理"但声明文件已全在 master 且工作区 clean 的批次（sweep 只对脏文件取活，永远取不到——成因＝CC 自行 commit 后没销行，交 CC 核对销行）；**机制类可动 WIP 一行**：读编辑锁 release/`_count_mechanism_wip` 机器口径输出，**上限以协议〇.9 现值为准（2026-08-24 现值 22），本章程不硬编码、不人工数**；超限标🔴并在 B 节提示「新立机制行须先关一条或按协议排队」，不阻断不改行；🛑 排队行是否该去🛑（WIP 回落即激活，如 #381/#382）；**全局记忆巡检有红即当周处理，不跨周**（读 sweep 日志「本机全局记忆巡检」一节，命中即在 B 节列出、当周订正——它报的都是零信号失真，拖一周＝多一周无人知情，队列 §一 #435）；按《Paul标准工作周》§五/§六自检上周——他是否被迫做过表中任何一项、上周计划 B 节是否有未答项遗留。
   🔴 **计划定长：单期 ≤10,000 B**——判断依据一句化；不复述队列行原文（写 #N 指针）；上周已给且【设置】未变的 opener 只引用编号＋一句变化说明，不整块重贴；超限＝写多了，当场删。
3. 聊天回复 ≤10 行摘要＋计划文件路径；一切正常且无需他动作，明说"本周无例外"。

## 纪律（人守红线，其余由工具拦）

- 写入日期一律本机 `Get-Date -Format 'yyyy-MM-dd'` 当场取（#246），跨午夜重取；发现队列文件读不到不臆造、直接报异常。
- 🔴 **acquire 与写盘两步、中间查 `$LASTEXITCODE`**（#200：「工具拒绝了」与「动作没有发生」是两件事）。
- 🔴 **写完文件反查落盘**：`powershell -File "0-学习与工具\工具-写后反查.ps1" -Path <绝对路径> -Keyword <独有文本>`，非 0 即失败（#255）；查脏文件用本机 git 不用沙箱 git（#305）。
- 🔴 **计划文件必须登 §二 待 commit 批次**（否则永不落库且持续孤儿告警，#238）；清单含队列文件自身；状态列写"**待处理**（登记，待 sweep 落库）"禁带 ✅；登记后触发一次 sweep（`Start-ScheduledTask ZhuopinCommitSweep`）。销行/回填 ✅ 写状态列开头；任一子项未完成写 🟡。批次清单三禁令（完整路径/无裸竖线/无裸反引号，三连反引号同禁）——release 会拦，被拒修正重试。
- 遵守 CLAUDE.md §5（Cowork 不 commit、不碰真实库；prompt/口令放代码块）。