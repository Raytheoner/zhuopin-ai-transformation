---
status: 生效（2026-08-29 Shao Peishen design 审通过，原话「审核通过」，D2 串行纪律未放宽）
name: zhuopin-lan-closeout
description: 卓品智能AI转型项目·回 LAN 一句话收口。当 Shao Peishen 说"我已回Lan""回LAN了""已回内网"时使用：先探针实证在网，再汇集三源 LAN 留步清单，白名单选件、.51 项全串行逐项冒烟回滚，无头 CC 泳道收口，心跳+企微推送，收工汇总。对外发送与人工门禁永不代办。
---

# 卓品 · 回 LAN 一句话收口（规则正本 v1.0）

> **权威判据正本＝`openspec/changes/lan-closeout-skill/`（归档后＝对应 specs）**：D1 白名单、D2 串行与冒烟回滚纪律、D3 护栏在彼；本文件是执行编排，与 design/spec 冲突时以后者为准。骨架整体继承 clearpool（含 #138 裁定：写活走无头链、Task 子代理限只读）。

## 触发与前置

- 触发：Shao Peishen 说「我已回Lan」（可带参：`上限N`／`只做<行号>`）。
- 🔴 前置探针（不可跳过）：ping `.51` 关键端点与源系统（erp/srm 公网口除外）——**触发词声称≠在网事实**；探针不过即如实报告并停。
- 常规三查：git 对齐、锁无长占、上一收口批汇总已出。

## 执行步骤

1. **汇集三源留步清单**：未闭合产出扫描器形态 1 ＋ 两份队列真身 grep「LAN 留步」 ＋ 近期看护件 LAN 留步节；去重后按 D1 判定，落选者分入「登记不合格／对外待人／他线在办」清单。
2. **排波**：`.51` 触碰项**全串行**成一条泳道链（一次只动一项）；非 `.51` 项可并行侧道；单批上限默认 6（🔴 首跑灰度强制 2、Shao Peishen 在场，三判据过后放开）。
3. **落档**：看护件（格式沿 clearpool 样板＋每项标注「快照点／冒烟判据／回滚 SOP 路径」三件）；走锁登记 §二 批次；落档失败即全停。
4. **起无头泳道**：opener 正文含授权链（他触发本 skill 即授权）、心跳段、以及 🔴 每项固定四步＝快照→执行→冒烟→回写证据；冒烟不过即回滚停项，失败项降回留步态并注明原因。
5. **看护**：心跳＋企微四类事件推送；`.51` 链上任一项停即整链暂停待人。
6. **收工**：汇总（每项一行：判据→证据→行内状态处置）推企微＋落档；收口≠销号，行销与否按行内原纪律。

## 🔴 红线提醒行

对外发送与请人动作永不代办（专员复核＝做到可复核态即停）；L2 门禁不代签；合规红线不动；`.51` 一次一项、带伤不推进；「已部署」必须有冒烟三件套证据，无证据的完成声明视同未做。

## 版本

- v1.0（2026-08-29 出件，随 openspec 变更包 `lan-closeout-skill` 待审）。沿革此后写同目录 CHANGELOG.md。
