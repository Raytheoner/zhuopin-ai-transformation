# zhuopin-lan-closeout · 沿革

> `SKILL.md` §版本约定：正文只留当前版，历史沿革写本文件。

## 2026-09-02（`OP-0902-C`，边界指向补充）

- **背景**：`lane-watch-mode` 架构收敛——`zhuopin-lane-clearpool` 退休，其职能被
  `zhuopin-lane-watch` 吸收；D1 新增 ⏭️「不做，转出」档，`.51` 部署及任何触碰生产
  服务的动作，`zhuopin-lane-watch` 命中即转出到本 skill，本身不执行。
- **改动**：仅 `SKILL.md` frontmatter 的 `description`／`status` 补一句边界指向
  （"本 skill 专管 `.51` 部署与 LAN 留步，其余走 zhuopin-lane-watch"）。
- **不改动**：D1 白名单／D2 串行纪律／D3 护栏正本红线段一字未动（5.5 约束）；
  触发词、探针机制、四步纪律（快照→部署→冒烟→回滚）均未变。

## 2026-09-01（`OP-0901-B`）

- **背景**：2026-09-01 08:16 sweep 探到 off→on，向运维群推「8 条 LAN 留步现在可以补做了」
  （`#312`／`#334`／`#340`／`#354`／`#394`／`#401`／`#418`／`#422`）。逐行核对状态字段与
  行内后续改判记载，**8 条里零条有效**——形态 1 当时只按「LAN 留步」字样 grep，既不读
  状态字段（`#401` 早已 `[S:done]`）、也不读行内后续的改判记载（`#354`「不要再把它当
  LAN 留步捞出来派单」、`#340`「早已闭合，本轮属扫描器形态 1 误报」等）。详见队列
  `openspec/changes/lan-closeout-skill/tasks.md` 2.1「缺口的第三面」（commit `87a8df4`）。
- **修法**：`0-学习与工具/工具-未闭合产出扫描.py::scan_form1` 新增两道过滤——
  ⑴ 状态字段已 `[S:done]` 的行直接排除（落 `done_excluded`）；⑵ 命中格内含否认表述
  （`DENIAL_MARKER_RE`：如「不要再当 LAN 留步捞出来」「已自行解除」「早已闭合」
  「扫描器形态 1 误报」「不再成立」「已收口」「已解除」「无需任何补做」）的行降级为
  `needs_review`（需人读一遍），不当作已核实闭合、也不混入「现在可以补做了」推送。
  两桶均可见可数（`format_report` 新增对应小节、`工具-落库sweep.py` 日志同行回显），
  且仍可走既有 `--ack-form1` 指纹确认收口——不是另立一套白名单。
- **回归结果**（对当日 8 条真实回归）：过滤前 8 条，过滤后可补做清单仅剩 `#312` 一条
  （元讨论行，LAN marker 落在描述本 skill 自身设计范围的「任务」格，非真实登记，属
  两道过滤能力范围之外的第三种假阳性，已知残留、建议走 `--ack-form1` 人读一遍处置）；
  `#401` 按 `[S:done]` 排除；其余 6 条（`#334`／`#340`／`#354`／`#394`／`#418`／`#422`）
  降级为 `needs_review`。
- **不改动**：D1 白名单／D2 串行纪律／D3 护栏三份判据正文一字未动；本次只影响「三源
  汇集」里源 1（扫描器形态 1）自身的准确度，不放宽/收紧 skill 的选件判据。
