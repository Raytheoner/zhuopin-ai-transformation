---
title: "【CC】开场 prompt B — #214 FI2 三单匹配接真实数据（勾 worktree）"
created: 2026-08-03
status: 待发
执行环境: CC
worktree: 勾（fi2-real-data-round1）
来源: Cowork 财务专线 2026-08-03 session（§四#43 已拍板 (b)，§一#214 已解锁）
---

# 【CC · 会话 B】#214 FI2 三单匹配接真实数据（第一轮）

**【设置】执行环境：CC ｜ 分支：feat/fi2-real-data-round1 ｜ worktree：☑ 勾（fi2-real-data-round1，收工自删）｜ 工作区：`.51:8094`**

> **`工作区` 字段的含义＝"写完在哪生效"**：本任务改完**必须真部署 `.51:8094`** 才算收口，"测试全绿 + push master" 不等于生效（根 CLAUDE.md §5「发布即收口纪律」；成因见 2026-07-30 #168 那次"重启验证通过、验证的却是未改动的旧代码"）。
>
> **🔴 进 worktree 后第一件事＝跑 `git branch --show-current` 当刻实测确认身份**，不要靠目录名判断（队列 #166 口径；2026-08-02 曾有 session 因路径习惯而全程改在主工作区）。

## 开工必读

根 `CLAUDE.md` 当前进度 → 队列 `#214`／`§四#43` → `1-转型规划/FI2真实数据接入时点-拍板素材一页-2026-08-03.md` → `1-转型规划/session接力-财务域场景落地.md` 第二十二轮 → 场景 `4-数字员工/财务部/FI2-三单匹配自动对账/CLAUDE.md`

**触碰区**：`4-数字员工/财务部/FI2-三单匹配自动对账/`、`.51:8094`

---

## 步骤 1 · openspec：**走**（Shao Peishen 2026-08-03 已拍板，不必再问）

- **为什么走**：2026-07-31 v8 面板改造经他拍板**跳过** design 审，理由是唐燕萍原话"信息全保留，只换看法"——**纯展示层**；**本次换的是数据源**（mock → 真实 ERP），性质不同。判定口径虽不动，但引擎在真实输入下会走到 mock 从未触及的路径，命中「改变既有模块对外语义」的边缘；IATF 可追溯下无 spec 站不住。
- **怎么走**：`/opsx:propose` → **停下等 Shao Peishen 审 design.md**（§5 固定流程第 3 步，不得跳过）→ `/opsx:apply`（先写测试再实现）→ 真实数据验证 → **`/opsx:archive`（tasks 全 [x] 后当场归档）**。
- `skip_specs` 视是否有 capability 变更自行判断并在 proposal 写明理由。
- design.md 须**显式写清"本轮不动判定口径"是设计前提、非遗漏**。

---

## 步骤 2 · 建造：五件事（范围由 §四#43 拍板 (b) 写死，勿自行扩缩）

1. **面板 `u9c` 模式接真实 PO ＋ 真实 AP**
   - PO 走 `Purchase/Query`、AP 走 `AP/Query`（后者 SQL 列名 bug 陈承 07-22 已修，批量查询可用）
   - 两者**今天就能接**，不依赖任何未决事项
2. **发票段从"如实报错"改为读人工誊录小样**
   - 现状：`u9c` 真实模式在发票段如实报错（OCR 未就绪，属设计非缺陷）
   - **8 组样本已在手**：AP-2026070036／AP-2026070035／AP-2026060004／AP-2026040083／AP-2026060073／AP-2025120181／AP-2026070071／AP-2026050057
   - 🔴 **复用 round-1（07-23）已誊录的成果，不要重誊一遍**——先去 `1-转型规划/FI2-round1真实验证报告-2026-07-23.md` 与 FI2 场景目录找；确实找不到再誊录，并在收工报告如实说明"未找到、已重誊"
3. **面板显式标注"发票为人工誊录小样，OCR 未接入"**
   - FI2 一贯的**不伪装已判定**红线。唐燕萍 07-31 原话："二期四项标灰徽标没问题，**比做假按钮强**"——同一逻辑
4. 🔴 **判定口径本轮一律不动**
   - BLOCK／微差分界、AP-PO 单价容差、五类判定优先级，`match_engine.py`／`result_classify.py`／`price_check.py`／`config.py` 的判据**一个字都不改**
   - **本轮目的是让唐燕萍在真实偏差上表态，不是我们先改口径**；她表态后才走口径变更（那时另立变更包）
   - 收工须证明**判据零漂移**：全量测试绿 + 零回归 + mock 模式判定结果与改造前完全一致
5. **交付后回请唐燕萍复核，并明确告知"这是第一轮"**
   - 必须讲明：**发票侧待 OCR 到位后还有第二轮**
   - 她可能复核两轮，是本方案**已知代价**（Shao Peishen 拍板时已接受），但**必须提前对她讲明**，不能让她以为三单已全自动

### 与 #82 round-2 的关系（勿重复劳动）

**不冲突、不重复**。OCR 到位后（#82）只是把"人工誊录小样"换成"自动直读"，**本轮已收敛的 PO／AP 侧口径不用重做**。#82 目前仍锁着（等 OCR 首轮报告到件且达标）。

### 一条对工作量估计很关键的事实

**round-1（2026-07-23）其实已用真实数据跑通过一轮**——8 组真实 AP、10 个料品全部完全匹配零假阳性，还真实抓出 3 笔 AP-PO 单价偏差（＝队列 #79，唐燕萍 07-24／07-28 两次回件已全部闭环）。

**但那是 CC 侧一份验证报告，唐燕萍在 `.51:8094` 上点不到。** 她 07-31 说的"逻辑对不对看不出来"指的是**面板上看不出来**，不是"我们从没跑过真实数据"。

**所以本次实质不是"从零接真实数据"，是"把已跑通的真实链路接到她能点的那个面板上"**——开工前先核实 round-1 那条链路的代码现在在哪、能复用多少，**再给工作量估计**（Cowork 不写生产码，未估，由你开工时给）。

---

## 步骤 3 · 发布收口 + 跟进信

### 3.1 发布收口四关（根 CLAUDE.md §5「发布即收口纪律」）

- 全量测试绿 + 零回归 + **判据零漂移** + **openspec 归档** + 场景 CLAUDE.md 更新
- 真部署 `.51:8094` + 冒烟（`/api/ping`／首页 200／一次真实 POST `/run` 跑通全链路）+ 回滚 SOP 在位
- **四服务共享口令门禁已上线**（队列 #160）：`/api/ping` 已豁免；程序化访问用 `X-Auth-Token` 请求头，别把 302 当故障
- 两道合规门禁照旧（只归档/通知不自动执行、L2 人工确认）

### 3.2 跟进信 —— 🔴 先过串行原则再起草

根 `CLAUDE.md` §5 与 README 抬头的 **「跟进信串行原则」**（Shao Peishen 2026-08-03 定）：**同一收信人同时只能有一封在途跟进信**，下一封的前提是**前一封已收到回件且回灌消化完毕**。**它优先于并节制第 8 步「发布收口即起草跟进信」**——触发条件满足 ≠ 可以发。

**本次的判断（供你复核，勿直接采信）**：唐燕萍名下最近一封是 **财务部#9**（07-31，FI2 v8 面板请复核），她**已于 07-31 回件**（`7-外部文档/财务部/…-afcc2fba3b1266b10c1a7f0a1b5edac2.md`）且**已回灌**（接力第二十一轮 + #191 销行 + 派生 #214／§四#43）。**故她手上无在途未闭环信，财务部#10 可发。** 但请按 README 表格**当刻实测复核一遍**——中间可能有别的线又发了信。

🔴 **发送硬前置（根 CLAUDE.md §5，2026-08-03 定）**：**代码已 ff 合入 master ＋ `.51:8094` 已部署且冒烟通过 ＋ 用真实案例端到端复现**，三条全过才可发送。起草与送审可先做，**发送必须等**。

**起草后提交 Shao Peishen 审核，通过后再发**。**当前财务部下一个可用号＝财务部#10**。

---

## 收工

1. 队列 **#214** 回填（产出路径、测试数、部署验证证据、判据零漂移的证明方式）；**§二 登记待 commit 批次**，**文件清单必须含队列文件自身**（#68 教训：未声明的脏文件会让 sweep 整轮跳过）
2. 进展写入 `1-转型规划/session接力-财务域场景落地.md`（新起一轮，注明执行方＝CC）
3. commit + push；**收工重跑文档台账**（`0-学习与工具/工具-文档台账生成.py`）
4. **worktree 收工自删**（协议〇.5）；删不掉（句柄占用是已知模式，见 #125／#207）就 `git worktree prune` 并**如实登记留待下期体检**
5. 新任务**当场追加队列待领行**（编号用 `acquire --reserve N --section 一` 取，**不要自己做加法**）

## 如实边界（不许粉饰）

- 找不到 round-1 誊录成果就说找不到，别假装复用了
- 判据零漂移证明不出来就别写"零漂移"
- 部署冒烟没做就别写发布收口完成
