---
title: "Session 接力 —— 采购域场景落地专线"
status: 滚动更新（每次收工覆盖，标日期）
专线定位: 采购域场景（SC1-SC9 + 共用 kit_engine 的 O2）的缺料预警口径校准、待部署场景业务对接、价值验收设计、治理/汇报——全 .md
边界: 不写生产码/不连真实库/不发企微（CC 在 LAN 干）；不改全景规划/实施计划/前置数据总表/Phase1 接力文件；docx 默认不转
与 session接力-Phase1收口.md / session接力-质量域场景落地.md 分开，互不串
---

# 采购域场景落地 · 接力文件

## 新会话起始词（Paul 每次开新会话可直接说）

> 「读 `1-转型规划/开场prompt-采购域场景落地专线.md` + 本接力文件 + CLAUDE.md 当前进度恢复上下文，按【下一会话主攻】继续；开干前先问我 2-3 个澄清。」

本专线只管采购域，独立接力，不碰全景路线图那条 Task。

---

## 【最近一次会话：2026-06-29 · 专线首次建立】

### 主攻：P0 缺料预警误判口径校准（业务侧）

**做了什么**
1. 按开场 prompt 读全上下文（CLAUDE.md §4/§5/§7、采购域工作任务书、就绪清单 §P0、采购专员首月工作信、supplychain 收割策略）。
2. **对真实代码确认了根因**（只读、未改）：`kit_engine.calc_shortage()` 的 B6 特例——子件不在库存快照、又无在途 PO 时，可用量被算成 0，毛需求一减报假缺口；且该子件**同时进了 `missing_snapshot` 告警清单和 `shortages` 缺料集合**，下游当确定缺料追了。源码注释明确 `get_inventory` 真实切换后该路径"必踩"，佐证这是**库存快照覆盖/料号口径数据问题**，非算法错。
3. 产出 **《缺料预警校准需求.md》**（1-转型规划/，v0.1，交 CC）：现象 → 根因（带代码定位）→ "真缺料"业务口径定稿 → 改判定要求（数据补全 + fail-loud 双手）→ 排查步骤(谁干什么) → 验收标准 → 红线 → 待确认决策点。
4. 建本专线接力文件（首次）。

**Paul 3 项裁定（2026-06-29）**
- **本次范围**＝**仅 P0 缺料校准需求** + 专线接力文件；SC3/SC5/O2 部署规则、SC1/SC8 价值验收下次做。
- **快照缺失处理**＝**数据补全 + fail-loud**：补快照覆盖/料号映射，且子件快照缺失时引擎**显式告警"覆盖不全"、不静默判缺料**（移出确定缺料集合、单列"待核实"清单）。
- **可用库存口径**＝**现货 + 在途 − 安全库存**（在途=已下 PO 未到货；已批请购暂不计入；安全库存扣减）。净需求 = 毛需求 − 可用库存 > 0 才算真缺料、才追料。

**核心发现（供下次会话快速恢复）**
- 误判**大概率纯数据问题**：毛需求由 `explode_bom` 按生产计划(FO/SO)展开，无订单驱动的子件毛需求本就为 0、不会进判定——所以"有货却被追"的料是**真有 BOM 需求 + 真有库存，但库存没喂进引擎**（快照漏仓 / 料号 SRM·BOM↔ERP 编码对不上 / 状态过滤误排）。
- 修法分两手并行：**A 逻辑护栏**（缺快照→待核实+告警，不进追料，先止血）+ **B 数据补全**（全仓覆盖+料号映射，治本让缺快照清零）。
- 验收硬指标：误判清零 + 缺快照子件不混入追料清单 + 黄金基准不漂移（SC5 35850/640000/675850）+ 回归全绿。

### 【下一会话主攻 · 候选】
1. **跟踪 P0 校准落实**：CC 在 LAN 排查（步1 重合率、步2 根因归类、步3 护栏、步5 真实回归）后，回收结果、更新验收勾销；专员敲定 §3 口径 + §9 待确认决策点（安全库存例外/在途时间窗/已批请购/追料提前量）。
2. **SC3/SC5/O2 部署业务规则确认材料**（9 月部署等）：SC3 绩效维度权重(交付/质量/价格/响应)+在途阈值；SC5 遴选口径 + L2 门禁(>50万/新供应商人工审)复核；O2 齐套阈值(与 P0 缺料口径同源)。
3. **SC1/SC8 价值验收指标设计**（D-4 两级验收）：SC1 风险评估提效/预警提前量；SC8 真延期识别准确率/催货提前量/误判率（误判率须等 P0 修完才可信）。
4. **SC2 采购周报业务需求**（8 月开发）：周报呈现什么、口径如何。

### 状态快照
| 场景 | 状态 | 本专线待办 |
|------|------|-----------|
| 缺料预警校准（SC8/O2 共用 kit_engine） | 🔴 P0 进行中 | 需求已交 CC（v0.1）；待 CC 排查修复 + 真实回归误判清零；专员敲定口径细节 |
| SC1 供应商风险初筛 | ✅ 上线 | 价值验收指标设计（候选2） |
| SC8 保供看板 | ✅ 上线 | 价值验收（误判率待 P0 修后才可信） |
| SC3 在途/绩效 ｜ SC5 采购建议/遴选 ｜ O2 齐套 | 🔵 引擎合 master，9 月部署 | 业务规则确认材料（候选2） |
| SC2 周报 | 🟡 8 月开发 | 业务需求定义（候选4） |

### 相关文件
- 本次产出：`1-转型规划/缺料预警校准需求.md`（v0.1，交 CC）
- 专线开场：`1-转型规划/开场prompt-采购域场景落地专线.md`
- 任务书 / 清单 / 工作信：`1-转型规划/采购域工作任务书.md`、`1-转型规划/采购域场景就绪工作清单.md`、`6-人才与组织/采购部AI专员-首月工作信.md`
- 引擎代码（CC 改，Cowork 只读参考）：`5-平台底座/zhuopin_platform/zhuopin_platform/agents/kit_engine.py`（calc_shortage）

---
*维护：本接力文件每次收工滚动更新（覆盖旧版、标日期）。Paul 开新会话只需说"读采购域场景落地接力文件 + CLAUDE.md 继续"。Last Updated 2026-06-29。*
