---
title: "开场 prompt · 业务推进 —— 业务总线交接"
created: 2026-09-01
编号: OP-0901-E
执行方: Cowork 业务总线
来源: 环境保障线 OP-0901-D 转场（R1 design 审收口后收工）
status: 待执行
---

# 开场 prompt · 业务推进（业务总线）

> 🔴 **引用版**：本件只给路径与章节指针，**不复制任何权威文件的正文**（防双份漂移）。
> 所有实测数字标注取值时刻，**新线开工须自行复核**——本件不是第二份权威。

---

### A1 ·【Cowork】业务总线 · 业务推进（当日可开工 5 行）——队列 #445／#418／#379／#403／#390

**用法**：把下面代码块**整块**复制进新 Cowork 会话（🔴 `【设置】` 行在块内第一行之后，分块复制会漏掉它）。**本次属 B 类跨线交接，开干前先问 2-3 个澄清。**

```
业务推进-OP0901E
【设置】执行环境：Cowork ｜ CC session：不适用（本线是 Cowork 线，不新建 CC 会话）｜ worktree：不适用（Cowork 线不使用 worktree）｜ 分支：master ｜ 工作区：无（本线只产改 .md，不触碰 .51 四服务、不碰企微机器人载体）｜ 派出线：环境保障线 OP-0901-D

读 `1-转型规划/开场prompt-业务推进-业务总线交接-2026-09-01.md` ＋ CLAUDE.md 当前进度恢复上下文，按文件执行；开干前问我 2-3 个澄清。
```

---

## 一、背景一分钟

- 环境保障线（`OP-0901-D`）今日收工，**R1 口径点台账 design 审已过**，但 apply 卡在机制类可动 WIP 23/22，`#439` 仍 🛑 排队。
- 该线今日轨迹（R1 → `#447` → `#448` → `#324`）**一路往元层漂**，四件均不能马上开工 ⇒ Shao Peishen 决定**转场，聚焦业务推进**。
- 业务队列 §一 在办 **23 行**（09-01 实测）：约 5 行不依赖任何专员回件、**现在就能推**；约 8 行硬卡在等三位专员回件。
- **三把主力专员闸全锁**（09-01 实测，逐封比对 `7-外部文档` 到件时间，确认是真锁、非转态滞后）。
- 🔴 **本线是业务总线，不是建造线**：出结论／方案／队列行，**代码与 `.51` 交 CC**。

## 二、权威依据清单（只给指针）

| # | 路径 | 是什么 |
|---|---|---|
| ① | `CLAUDE.md` §5／§7 | 工作流纪律与合规红线（**本件不复述**） |
| ② | `1-转型规划/session接力-业务总线.md` | 本线接力卡。⚠️ **实测 8,279 字节、已超 R5 硬上限 8,192**，且最后更新 08-29 ⇒ **收工时须覆盖重写并瘦身**（R5：定长、六块、零日期节、覆盖不追加） |
| ③ | `1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md` | 业务任务真身。查行：`python 0-学习与工具/工具-队列查询.py --row N --section 一` |
| ④ | `6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` | **信状态唯一权威**（「发送状态」列）。查闸：`python 0-学习与工具/工具-跟进闸查询.py --to <收信人>` |
| ⑤ | `1-转型规划/0-全景路线图/卓品智能AI转型全景规划.md` §加速启动总览 ＋ §0.2 | 权威排期与场景口径 |
| ⑥ | `6-人才与组织/人员名录-称谓与性别-正本.md` | 🔴 提到任何具名人物前必读，**禁从名字推断性别／职务** |

## 三、任务分段

### A（首要）· 五行不依赖专员回件，现在就能推

`#445` SC8 param_version 展示层未跟随开关 ／ `#418` FI2 面板「发票缺失」假报错（举证方已自带四组对照，**无需再问**）／ `#379` 法定节假日日历每年更新提醒（规则已定死，执行方是我方）／ `#403` 消化 IT部#10 回件（该闸已开、回件已到）／ `#390` FI2 `parse_ap_lines` fail-loud 哑雷（**修复本身不涉口径**）

**做什么**：逐行核实当前状态与阻塞情况 → 定级排序 → 出**派单件或队列行**交 CC 建造。
**验收**：每行给出「能否立即开工／触碰区／谁先谁后」，并按 §1.2 附**次序与并行矩阵**（≥2 项时必附）。
⚠️ **`#418` 另有一条硬前置**：其行内记有「他的批准已登记，但硬前置未满足仍不执行」——**开工前先读该行确认前置是否已解除**。

### B · 八行等回件 —— 是等人，不是等我们

财务 `#224`／`#391`／`#423`／`#424`／`#372` 等唐燕萍；采购 `#118`／`#334` 等姚祖怡；质量 `#340` 等陈忱。

🔴 **串行原则优先于任何触发点规则**：同一收信人同时只能有一封在途信，**前一封未闭环即不得起草下一封**。判闸唯一入口＝ `工具-跟进闸查询.py --to <收信人>`，**队列行里的闸状态复述一律是过时快照、不得作判据**。
**做什么**：**不催、不起草**。仅确认每行的等待对象已登记清楚。

### C（可选）· 闸的结构性问题 —— 知其然即可，不在本线解

B 类八行卡住不是因为专员慢（三封在途最长 4 天，均在正常区间），而是**闸的单位是「信」不是「点」**：一封信 ≤10 条判例、各点不同时销，于是**某一个点答完了也要等整封信闭环**。R2（闸改按点计）解这个，而 R2 硬阻塞于 R1（`#439`，design 审已过、只差 WIP 位子）。
⇒ **本线不推 R1/R2**（属机制线），知道 B 类为何堵着即可。

## 四、开工前置步（认领先于动手）

**开工第一件事**：按本任务触碰区关键词（文件路径／场景号／专员姓名）**同时 grep 两份队列真身**（`跨桌任务队列-机制环境.md` 与 `跨桌任务队列-业务场景.md`）**与 `跨桌任务队列-归档-YYYYMM.md`**，确认无他人在办行重叠、且本任务未被别的行覆盖；**有重叠不得抢领，报总线裁决**。
⚠️ 归档件必须一起 grep（每周清扫后历史行已不在正文）；⚠️ `跨桌任务队列.md` 现为纯指针文件，**照它读会解析到零行且不报错**。

🔑 **本线开工前另加一条（`OP-0901-D` 当日两次踩中）**：**提一个缺口／开一项新工作前，先 grep 它是否已有承接行。** 当日两次差点重复劳动——差点重做 08-31 已完成的分诊、差点重登 `#324` 已记的缺陷；**都是从「该有什么」出发，没先查「已经有什么」。**

## 五、纪律段（引用条目名，正文见 CLAUDE.md §5／§7）

- **编辑锁**：改队列前 `acquire`、改完立刻 `release`；占用中把待登记内容写进接力文件顶部「⏳ 队列更新待补」等回补，**不硬写**。🔴 `acquire` 与写盘必须两步、中间查 `$LASTEXITCODE`——**「工具拒绝了」与「动作没有发生」是两件事**。
- **取号**：一律 `--reserve` 预留、不自己做加法。🔴 **取号前必先做并入审核（协议〇.10）**——逐条扫 §一 待领行，能并入的一律并入，并写一句结论。🔴 **业务场景行不受机制类 WIP 上限约束**（22 只约束 `[D:机]`）。
- **三条静默失败写法禁令**：批次文件清单写完整路径（不用「同上」）／队列文本内不得出现裸竖线／脚本改 md 时串内不得嵌直双引号或裸反引号。**三者失败形态都是「外观同成功」。**
- **状态头纪律**：销行/回填时 **✅ 必须写在状态列开头**；任一子项未完成一律写 🟡。
- **合规红线**（§7）：先 mock／脱敏再真实；AI 决策全写审计；**OEM 数据按客户隔离**；**L2 人工门禁**（采购 >50 万／新供应商／交付预测推客户）；**ASIL C/D ＝ AI 绝对禁区**；对客闸全程关。
- **称呼**一律 `Shao Peishen`；🔴 **人的属性禁推断**，查名录正本；**起草对外件后须扫一遍第三人称代词逐个回名录核对**。
- 🔴 **收工务必确保零孤儿脏文件**——`#416 ⑶` 的触发条件正是「工作区有脏文件不属于任何待处理 §二 批次」，一旦命中，下一封专员回件到达时机器人 release 被拒、**队列锁死 30 分钟**。

## 六、收工段（必含 git 处置）

1. 滚动更新 `1-转型规划/session接力-业务总线.md`（🔴 **覆盖重写、瘦身到 8,192 字节内**，六块、零日期节）。
2. 列出本次全部新产出／修改文件清单 ＋ 建议 commit message ＋ **登记队列 §二 待 commit 批次**。
   🔴 **message 格写法**：**整条 commit message 用一对反引号包起来，内部不得再出现反引号** —— `工具-落库sweep.py::_extract_commit_message` 取第一段反引号内容，写成裸文本会让 commit 主题只剩一个碎片（`#324` ④ 已记七例，`OP-0901-D` 当日贡献三例）。
3. 提示 Shao Peishen 一句话交 CC：**commit + push + 收工重跑台账**。（Cowork 不擅自 commit，但不留未提交悬置。）

## 七、预置澄清问题（B 类必问，供本线开工时向 Shao Peishen 提；一律给带推荐项的选择题）

1. **A 段五行的推进方式**：(a) 本线逐行定级后一次性出派单件交 CC ／ (b) 先只做优先级最高的一两行、验证节奏后再批量 ／ (c) 只出队列行，派发交值周巡检。
2. **`#418` 的硬前置**：其行内记有「批准已登记但硬前置未满足」——(a) 本线先核实该前置具体是什么再决定是否纳入本轮 ／ (b) 本轮直接跳过它、只推其余四行。
3. **B 段八行是否需要任何动作**：(a) 完全不动，等回件（推荐——串行原则）／ (b) 仅核实每行「等谁」登记无误 ／ (c) 其它。
