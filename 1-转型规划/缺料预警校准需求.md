---
title: "缺料预警校准需求（P0 · 交 CC 排查修复）"
created: 2026-06-29
status: 需求草案 v0.1（Cowork off-LAN 出 → 交 CC 在 LAN 改 kit_engine + 跑真实回归；Cowork 不改代码、不连真实库）
主笔: 采购 AI 专员 姚祖怡（业务口径）｜ Paul 拍板（3 项决策见 §6）｜ Cowork 整理成文
执行: CC（Claude Code，LAN）排查 + 修判定 + 真实数据回归
关联: 采购域工作任务书 ｜ 采购域场景就绪工作清单 §P0 ｜ 采购部AI专员-首月工作信 ｜ kit_engine.calc_shortage
代码落点: 5-平台底座/zhuopin_platform/zhuopin_platform/agents/kit_engine.py（calc_shortage）｜ tests/test_kit_engine.py
---

# 缺料预警校准需求（P0）

> **一句话**：缺料预警把"卓品有货、引擎看不见"的子件误判成缺料、推成追料对象。根因是**喂给引擎的库存快照覆盖不全 / 料号口径对不上**（不是算法错）。本需求定义"什么才算真缺料"的业务口径，并要求 CC 在引擎里加 **fail-loud 护栏**（快照缺失→不静默判缺料、显式告警），再补数据，真实回归到误判清零。
>
> **分工**：本文 Cowork 出口径 + 需求；CC 在 LAN 改代码、连真实库、跑回归。Cowork 不碰代码、不连库。

---

## 1. 现象（采购专员手工核对发现）

跑出的物料瓶颈子件里，**部分子件卓品库存充足、既无在途 PO 也无请购**，却被判缺料、列入追料清单。不需要追的料变成追料目标，预警失真——这直接动摇采购 AI 的可信度，是采购域 P0，优先于一切新部署。

## 2. 根因（已对真实代码确认，待 CC 在真实数据上落实）

引擎 `calc_shortage()` 现有逻辑：

```
可用量 = 当前库存 − 安全库存 + 在途未到货（qty_ordered − qty_received）   # 物料在库存快照中
缺口   = max(毛需求 − 可用量, 0)
B6 特例：物料【不在库存快照】时，可用量 = 在途（只算 PO，缺快照不再忽略）
```

问题出在 **B6 特例**：一个子件若**没进引擎读到的库存快照、又没有在途 PO**，可用量被算成 `0`，毛需求一减就报假缺口——哪怕仓里实物一大堆。代码现状：这类子件**既被记进 `missing_snapshot` 告警清单、又同时落进 `shortages` 缺料集合**，于是下游把它当确定缺料追了。

**关键确认**：源码注释明确 `ZpConnector.get_inventory` 真实切换后该路径"必踩"——即真实库存接口尚未把全仓真实库存喂进快照前，**大量子件都会落进"缺快照"分支**。所以这**大概率是数据覆盖问题**（库存快照没覆盖全仓 / SRM·BOM 料号 ↔ ERP 库存料号编码对不上 / 快照状态过滤把可用库存排掉了），引擎逻辑只需补一道"缺快照不当缺料"的护栏。

> **📌 2026-07-05 补充（Unknowns 登记册 U2，Paul 批准）**：数据侧根因新增一个已确认来源——**U9C 实时库存 OpenAPI 存在 bug（原厂未回复，当前接口返回的库存数据是错的）**，已列入实施计划 §七.2 外部依赖表。
> **📌 2026-07-06 更新（U2 闭环）**：**已采纳 `Stock/Query`（/zp webapi 族）为库存实时源**，绕开原 OpenAPI bug——§4-B 数据侧治本的落点由"修旧接口"改为"切新端点"：CC 把 `ZpConnector.get_inventory` 切到新源（real 模式 fail-loud），验证**全仓覆盖 + 状态过滤口径 + 三方料号映射**后跑 §7 真实回归；**10 料"接口数据 vs 实物抽盘"抽验保留（姚祖怡，8 月初，改为验新源）**。CSV 导出降为备份路径。§4-A 逻辑护栏（缺快照不当缺料）不受影响、照做。

## 3. "真缺料"业务口径（定稿 v0.3 —— 姚祖怡 2026-07-10 缺料批改会 §9 四决策点全圈定）

> **净需求 = 毛需求 − 现有可用库存；净需求 > 0 才算缺料、才进追料集合。**

| 要素 | 口径 | 说明 |
|------|------|------|
| 毛需求 | BOM 展开 × 生产计划量 ×(1+损耗率) | 须由**真实订单驱动**（FO/SO 经 BOM 展开）。无真实订单驱动的子件，毛需求不应 > 0（见 §5 步4 副检） |
| 现有可用库存 | **现货 + 在途 − 安全库存** | Paul 2026-06-29 拍板口径（§6 决策三） |
| └ 现货 | 全仓现有库存 | **必须覆盖全部仓库**，状态过滤不得把可用库存误排 |
| └ 在途 | 已下 PO 未到货 = qty_ordered − qty_received，**且限"到货日 ≤ 需求日"才计入**（姚 07-10 定 A1；超期未到的在途不算可用） | **仅计已下 PO**；**已批准未下单的请购不计入**（姚 07-10 定 A4） |
| └ 安全库存 | **全部按系统设定值扣减，无例外料**（姚 07-10 定 A3） | ⚠ **数据缺口**：Stock/Query 安全库存字段恒 0，过渡期按 0（等同暂不扣、不阻塞上线），交接标 CC/IT 补数据源后生效 |
| 判定 | 净需求 > 0 **且 需求日 − 今天 < 采购提前期 L/T** → 进追料集合（姚 07-10 定 A2，临近才追、减噪音）；否则不追 | 库存够、或无真实订单驱动的子件，**一律不进追料集合**；净缺口但未临近 L/T 的暂不追、纳入观察 |

## 4. 改判定要求（交 CC，Paul 决策二＝数据补全 + fail-loud）

**A. 逻辑侧 —— 缺快照 fail-loud，不静默判缺料**

引擎须区分两种"可用量低"的情形，不再混为一谈：

1. **快照内·真零库存**（料号在快照、current_stock 据实为 0/不足）→ 按 §3 口径正常参与缺料判定，净需求 > 0 即真缺料。
2. **快照缺失·无法判定**（料号不在库存快照 = 现有 `inv is None` 分支）→ **不得**默认可用量=0 后落进 `shortages` 当确定缺料；改为：
   - 移出"确定缺料/追料"集合；
   - 单列"待核实——库存快照缺失"清单（沿用并强化现有 `missing_snapshot`）；
   - **fail-loud 显式告警**"库存快照覆盖不全，N 个子件无法判定缺料"，提示数据侧补全后重判；
   - 该清单**不混入**对采购员展示的追料清单。

> 即：把当前"缺快照→可用量0→进 shortages"的旁路，改成"缺快照→进待核实清单 + 告警，不进 shortages"。消费方（O2 齐套清单 / SC8 保供看板）相应分区展示：**确定缺料** vs **待核实(快照缺失)**。

**B. 数据侧 —— 补全快照覆盖 + 料号映射对齐**（治本，与逻辑护栏并行）

1. **库存快照全仓覆盖**：`get_inventory` 真实取数须覆盖全部相关仓库，状态过滤口径正确（在用/可用库存不被排除）。
2. **三方料号映射对齐**：SRM 物料需求/到位反馈 ↔ BOM 子件料号 ↔ ERP 库存料号编码，统一映射主键；消除"实物有货但料号对不上→落进缺快照分支"。
3. 数据补全后，原误判子件应落回"快照内"分支，净需求按 §3 算出 ≤ 0、退出追料集合。

> A 是护栏（先止血、立即让误判不外泄），B 是治本（让缺快照清单逐步清零）。两者都要。

## 5. 排查步骤（谁干什么）

| 步 | 查什么 | 谁 | 产出 |
|:-:|--------|----|------|
| 1 | 把误判的瓶颈子件清单 vs 引擎 `missing_snapshot`（不在快照的料）对一遍——验证是否全落在里面 | CC | 重合率（预期≈100%）|
| 2 | 这些子件库存口径定位：是**仓库范围漏**（某仓未纳入快照）/ **料号映射不一致**（SRM·BOM vs ERP 编码）/ **状态过滤误排**？ | 专员 + CC + IT | 根因归类 + 修复项 |
| 3 | 实施 §4-A 逻辑护栏（缺快照→待核实+告警，不进 shortages）| CC | 改后引擎 + 测试 |
| 4 | **副检·毛需求来源**：误判子件的毛需求是否挂真实 FO/SO？若某子件根本无真实订单驱动却出现毛需求 > 0，属需求侧幻影（陈旧/错挂生产计划），单独排查 | 专员牵头 + CC | 幻影需求清单（如有）|
| 5 | §4-B 数据补全（全仓覆盖 + 料号映射）→ 真实数据重跑 | CC + IT | 误判清零回归 |

## 6. 决策记录（Paul 2026-06-29）

1. **本次范围**：仅 P0 缺料预警校准需求（本文）+ 采购域专线接力文件；SC3/SC5/O2 部署规则、SC1/SC8 价值验收下次。
2. **快照缺失处理**＝**数据补全 + fail-loud**：补快照覆盖/料号映射，且子件快照缺失时引擎显式告警"覆盖不全"、不静默判缺料（与项目 fail-loud 家风一致）。
3. **可用库存口径**＝**现货 + 在途 − 安全库存**（在途=已下 PO 未到货；已批请购暂不计入；安全库存扣减）。

## 7. 验收标准（CC 真实数据回归）

- 之前被误判的子件（卓品有货/无真实净缺口）**全部退出**确定缺料·追料集合，误判清零。
- 库存快照缺失的子件**不再混入**追料清单，而是落"待核实(快照缺失)"清单并触发 fail-loud 告警（数量可见、可追）。
- "确定缺料"集合内每一项，**净需求(现货+在途−安全) > 0** 经手工抽验成立。
- 既有黄金基准不漂移（SC5 35850 / 640000 / 675850 等），平台 / O2 / SC5 / SC8 回归全绿。
- 所有判定结果写平台 `audit`（可追溯）。

## 8. 红线与边界

- 本文是**业务口径 + 需求**，不含代码改动；CC 在 LAN 改 `kit_engine.calc_shortage` 及其消费方、跑真实回归。
- 先 mock/脱敏跑通逻辑，再切真实库（CLAUDE.md §7）。
- 缺料判定结果是"预警/建议"，**结案在采购经理**；SC8 对客外发闸 `CUSTOMER_OUTBOUND_ENABLED` 全程关。
- 采购 SRM/ERP 供应商数据不适用 OEM 隔离（CLAUDE.md §4）。

## 9. 决策点（✅ 姚祖怡 2026-07-10 缺料批改会全圈定）

- ✅ **安全库存（A3）**：**全部按系统设定扣减，无例外料**。⚠ 数据缺口：Stock/Query 字段恒 0，过渡期按 0，交接 CC/IT 补数据源后生效。
- ✅ **在途时间窗（A1）**：在途 PO **限"到货日 ≤ 需求日"才计入可用**（超期未到的不算）——更严、更保供。
- ✅ **已批请购（A4）**：**不计入**可用供给（保守）。
- ✅ **追料触发提前量（A2）**：**临近才追**——净需求 > 0 且 需求日 − 今天 < 采购提前期 L/T 才进追料集合（减噪音）。

> 截止：口径草案专员到位首周敲定 §3 + §9 → CC 排查修复 → 真实回归误判清零。这是采购 AI 信任的根，优先于一切新部署。
>
> **📌 2026-07-04 敲定方式升级（"AI 起草·专家批改"三步法，Paul 批准）**：§9 四个决策点**不要求专员写文档**——CC 先把误判子件案例清单（含各料安全库存设定/在途到货日/请购状态）整理成**对照表 + 每个决策点的建议选项**，专员参加一次 **45 分钟批改会**逐项圈选/改判即定稿；此后每次采购经理在追料清单上的人工改判记录原因，自动累积进判例、口径按月复核升版。本口径（§3+§9 定稿）同步登记进《跨场景前置数据与知识库任务总表》§一.2 知识资产台账（"真缺料口径"行）。

## 10. SC8 保供净额口径（姚祖怡 9 问暴露 → 2026-07-10 缺料批改会全圈定，交 CC）

> 姚祖怡为 SC8 净额开关黄金重核提的 9 问，经核对 SC8 保供真实引擎（`baoguan.py`/`forecast.py`，只读）暴露以下"现货抵扣需求判定"口径事项，与本需求 §3/§4 同源。详见《采购-SC8净额开关-姚祖怡9问答复》。
>
> **✅ 2026-07-10 缺料批改会全定稿**：G7（替代料）此前已随保供 v2 定稿；**G5/G6 多层递归+逐层抵现货、G4 SRM 答交数量、G9 BOM 版本、G2 共用件分摊 现由姚祖怡 07-10 圈定**（下表逐行标结论）。G1/G8 为展示/排查项。全部转《缺料保供引擎改造交接-2026-07-10》交 CC。

| # | 口径 | 引擎现状 | ✅ 定稿结论（姚祖怡 07-10 圈定） |
|:-:|---------|---------|------|
| G2 | **共用子件跨项目分摊** | 各成品各算各的，同批现货被多项目重复算可用 | **B4：按 PMC 每月手工输入的优先级顺序占用扣减**（先高优先级项目占用现货，剩余给低；避免共用料紧时少报警） |
| G4 | **SRM 答交数量核对** | 只看答交"交期(日期)" | **B2：答交"数量 ≥ 缺口量"也纳入满足判定**（日期 + 数量双判，数量不够也算没齐） |
| G5/G6 | **多层递归 + 逐层抵现货** | 保供看板只展一层直接子件、不逐层抵现货 | **B1：要做多层递归展开（到叶子件）+ 逐层先抵成品/半成品现货**。⚠ 工作量大，**排期"照单全收 vs 分批"留 7/17 会定**（预拟：关键成品先多层、其余暂单层） |
| ~~G7~~ | 主料+替代料合并判定 | 逐个料号比 | ✅ 已定稿 C-1（07-08），主料+替代料现货合计判齐，批2 实现中 |
| G9 | **多版本 BOM 版本取值** | 引擎不挑版本，用喂进来的 BOM | **B3：取"当前生效 / 最新已发布"版本**（取数环节落实） |
| G1 | 现货覆盖明细表（展示层） | 只给汇总进度条 | 🟢 先做：逐子件列"毛需求/现货量/是否覆盖/缺口"导出，供专员核对 |
| G8 | 某需求未展示定位 | — | 🟢 先做：CC 拉真实数据一次性定位（成品/子件、是否在订单集、落哪色组） |

> **落地**：口径已全定稿，转《缺料保供引擎改造交接-2026-07-10》交 CC。B1 多层递归排期（照单全收 vs 分批）留 7/17 会上与姚定；净额开关翻 ON 前提 = 黄金重核签字（7/17 同场，见会材）。G1/G8 先做（让专员看得全查得到）。

## 11. 三处取值口径确认（姚祖怡 2026-07-23 V2 反馈稿，队列 #66/#67/#84-86，原拟#80-82撞QD-B同日号续排）

> 承接 CC #66《取数取证结论与现状取值对照表-2026-07-22》问题 4/8/9a 的三处取值口径。姚祖怡 07-23 反馈稿 V2 逐项作答（复选框为死字符，人读提取真值）——**三处全选"现状即正确"，取值逻辑无需改造**。

| 口径 | 现状代码取值 | 姚 07-23 确认 | 结论 |
|------|-------------|--------------|------|
| ① 需求量 | FO API `Num`，`int(float(Num))`，**不换算**（`sc8/loaders.py::parse_forecast_order_rows`） | ✅ "选 num，不用任何换算" | **维持现状**，无换算 |
| ② 需求日 | FO API `ShipPlanDate` 取 `YYYY-MM-DD`，不做时区 | ✅ "计划出货日期就是需求日字段" | **维持现状** = 计划出货日期 |
| ③ 多客户显示 | FO API `CustomerName` 直接透传 | ✅ "多个真实客户，并列出全部客户" | **并列列出全部客户**（现状透传单值，多客户场景需 CC 核 FO 是否已返回多客户、如何并列） |

**结论**：①②取值逻辑**无需改动**（现状即姚确认口径）；③"并列全部客户"——现状透传 FO 单值，若一成品真实对应多客户，需 CC 核 FO `CustomerName` 是否本就多值/如何并列展示（取值口径无换算，属展示实现）。→ 交接见《缺料保供取值口径回灌-2026-07-23》。

> **⚠ 同批姚 07-23 另提（非本节取值口径，另行处置，见交接/队列 #67）**：判据答复 2（无答复提前期 **30→90 天** + 展示估算子件，⚠ 原 30 为 Paul 2026-06-22 定，待 Paul 确认）｜ 答复 1（`.51:8092` 现"需备料"状态、要求保留 `:8091` 老看板功能与口径，⚠ 看板分歧需 CC 核实）｜ 答复 3/4（委外仓/部分齐套认可）｜ 新功能诉求 **#12-#15**（子件状态全展示 / 需求关闭列 / 需求日可齐套列 / **#15 优先级导入窗口 = B4 PMC 优先级的落地实现**）。

---
*维护：本需求与采购域工作任务书、就绪清单 §P0、专员工作信任务1 一致。校准进展滚动同步 `session接力-采购域场景落地.md`。**v0.4（2026-07-23 §11 三处取值口径确认=现状，姚 V2 回灌）** / v0.3（07-10 §3/§9 A1-A4 + §10 B1-B4 定稿）/ v0.2（07-06 §10）/ v0.1（06-29 初版）。*
