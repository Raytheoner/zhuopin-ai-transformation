---
title: "【CC】开场 prompt A — 发送 IT部#6 跟催信（不勾 worktree）"
created: 2026-08-03
status: 待发
执行环境: CC
worktree: 不勾（主工作区跑）
来源: Cowork 财务专线 2026-08-03 session（队列 #127，Shao Peishen 已放行）
---

# 【CC · 会话 A】发送 IT部#6 跟催信

**【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐ 不勾 ｜ 工作区：本机主工作区**

> **为什么不勾**：本会话只发一封信、不写任何代码。发信脚本按惯例在**主工作区**跑，且发完要回填主工作区的 README——在 worktree 里跑会把回填写进 worktree 自己的 README 副本（同队列 #126 缺陷形态）。

---

## 任务

发送已获放行的 IT部#6 跟催信。

- **信件**：`6-人才与组织/部门AI专员跟进/IT部-陈承-跟进-2026-08-03-OCR首轮实测报告已逾期跟催.md`
- **格式**：**纯文字 md 正文，不附 docx**
- **收信人**：陈承 `2023458`（已在白名单）
- **抄送**：唐燕萍 `tangyanping`（同 IT部#4 先例——信里第四节是给她的提醒：OCR 人工基准确认由李姣龙做，别漏）
- **背景**：队列 #127 已逾期（陈承 07-28 承诺"本周内"出 OCR 首轮实测报告，7/28–8/2 已过、`7-外部文档/IT/` 零新件）。Shao Peishen 2026-08-03 已放行（选 (a) 照发，未提修改意见），README 状态已为 `🆕 待发`。

## 一处需你自己判断的坑

`scripts/push_followup_letter.py` **没有内建抄送参数**，IT部#4 当初是分两次推送。但**第二次推送时 README 状态列已被第一次回填改掉**，`--match-topic` 定位与状态门禁可能不再匹配——**先看清 `delivery.py::push_followup` 的门禁与回填逻辑再动手**，不要盲跑第二次导致回填被写坏。若第二次不便走该脚本，用其它方式送达抄送即可，**但必须在 README 与 #127 如实记录抄送是怎么送的**。

## 完成后

1. README 状态列回填 `✅ 已推送 YYYY-MM-DD HH:MM UTC`（**改 README 前先取协议〇.7 编辑锁**——A2 质量专线也可能在改它）
2. 队列 **#127** 回填发送时间与抄送情况，状态由"在办——已获放行待 CC 发送"改为"在办——已发送，等陈承回三个决策点"
3. ⏱ audit 存真 UTC，文件 mtime／计划任务是本地（UTC+8）——**引用任何时刻前先答一句"这是 UTC 还是本地"并显式标基准**（根 CLAUDE.md §5）
4. **§二 登记待 commit 批次，文件清单必须含队列文件自身**
5. 🔴 **读 `README-跟进机制与命名约定.md` 时不要只读表格，抬头规则段必读**——2026-08-03 新增了「跟进信串行原则」

## 如实边界

抄送若没送达，就在 README 与 #127 写"未抄送"及原因，不粉饰。
