---
title: "开场 prompt——QD-B 立项门禁·极简版发布收口（CC 建造交接）"
created: 2026-07-23
status: 已执行归档
关联: CLAUDE.md §5《发布即收口纪律》｜ QD-B极简版先上线-最小任务集-2026-07-09.md ｜ QD-B立项门禁-上线收口清单-2026-07-09.md ｜ 4-数字员工/质量部/QD-B-立项审核门禁/CLAUDE.md ｜ 跨桌任务队列 #79
---

# 开场 prompt——QD-B 极简版发布收口（发布到 .51 + 门户接入 + 质量部灰度）

> ✅ **已执行（2026-07-23，CC，队列 #79）**：本文件 §3 任务清单五步全部完成——`.51` 真实部署
> （`QdBWebServer`）+ 真实黄金三样本上传冒烟通过 + 门户质量域卡真实入口 + 灰度陈忱/朱映桦
> SOP 已出。详见队列 #79 行 + QD-B 场景 CLAUDE.md §7「部署状态」。

## 0. 开场词（复制即用）

```
【设置】分支：master ｜ worktree：☑（全新独立建造）
读 CLAUDE.md + 1-转型规划/开场prompt-QD-B极简版发布收口-CC建造交接-2026-07-23.md，认领跨桌任务队列 #79，按该文件执行 QD-B 极简版发布收口（黄金入库→归档→最小Web服务→.51部署→门户接入→灰度），收工按文件收工段回写。
```

## 1. 目标

按《发布即收口纪律》（Paul 2026-07-19 拍板，四关）把 **QD-B 立项审核门禁·极简版**发布到 `.51` 并接入 AI 运营指挥中心门户，供质量部（陈忱 + backup 朱映桦）灰度试用。质量部明确想尽快用起来（Paul 2026-07-23 转达）；**不等陈忱/朱映桦任何回灌**——口径已全清（见 §2）。

## 2. 现状快照（质量专线 2026-07-23 核实，以场景 CLAUDE.md 状态时间线为准）

- **引擎已够极简版口径**：A 类 55 条 + C 类转人工 4 条（42/80/81/82）+ B 类 10 条 MVP 占位（统一转人工）+ 两档判定（阻断否决→不合格 / 无阻断→合格+待改进清单）+ `report.py::build_report` 六段式报告 + `run_all` 全链审计；**54 tests 全绿**。
- **黄金三真实样本零漂移**：EQ17 合格 / 邦奇不合格（driving rule 74 累计现金流阻断）/ 华丰不合格，引擎档位==评审报告档位。
- **权威表 = 20260717（80 条，F 定论）**；`registry.json` 仍 2026-07-10 82 条内部编号（队列 #45 结论：行为一致、内部可不改）——**对外展示规则编号处（Web 报告页/SOP）须按映射换算或直接展示规则文案不展示编号**，防陈忱对不上号。
- 陈忱侧仅剩扩容件（模块七采购计划版本确认），不阻塞发布；旧 63/64 已被 20260717 删除，PENDING 自然消解。
- 真实样本（EQ17/邦奇/华丰）在**主工作区 gitignore 目录**，worktree 不共享——按 07-20 既例拷入本 worktree 验证、不入库。

## 3. 任务清单（按序）

1. **组7+组8 收尾**：黄金基准正式入库（`data/golden` 结构化清单）；openspec `qd-b-project-gate-review` 阶段性归档（完工即归档纪律；极简版范围内 tasks 勾清，扩容项移 future-work 不留悬置）。
2. **最小 Web 服务**（参照 SC8 保供 8091 范式 + `5-平台底座/deploy-tools/ZhuopinDeploy.psm1`）：上传立项书 xlsx → `run_all` → 页面呈现六段式报告（**"B 类转人工 / C01-C10 未实现"如实标注**，不伪装已判定）；建议端口 **8093**；audit 照写平台 `audit`。
3. **`.51` 部署**：sync 薄封装 + 计划任务常驻 + 防火墙入站规则——吸取 #65 两教训（**脚本 UTF-8 BOM** + **入站规则勿漏**）；冒烟=`/api/ping`+关键页 200+一次真实样本全量跑；回滚 SOP（`schtasks /End`/`/Delete`）在位。
4. **门户接入**：AI 运营指挥中心质量域卡替换为真实入口 `http://192.168.100.51:8093/`（样例卡退役）。
5. **灰度开闸**：首批用户=陈忱+朱映桦；反馈走企微机器人（白名单已含陈忱；朱映桦**不在白名单**，如需其直发反馈须 Paul 拍板加名，否则经陈忱转）；出一页 SOP（md，交质量专线随下封跟进信/触点发，含"试用版·不作为正式立项依据"声明）。

## 4. 红线与门禁（不得放宽）

- 报告=**审核建议**，立项决定由人做；AI 不自动执行任何业务动作（门禁①）。
- ASIL C/D：规则 C06 触发即转功能安全工程师，AI 禁止自动判定安全相关结论（ASIL C/D = AI 绝对禁区）。
- 真实立项书（未脱敏）留 LAN 不入库；Web 上传文件不入 git；全链写 audit（IATF 8.3 可追溯）。
- 灰度期门户/报告页标注"试用版"；先灰度后依赖（四关④），价值兑现后再谈正式依赖。

## 5. 收工段（git 处置，必做）

列出本次全部新产出/修改文件清单；openspec 归档当场做；场景 CLAUDE.md 补「部署状态」段；回写队列 #79（状态+产出路径+冒出的下游任务当场追加待领行）；登记/取 §二 批次 **commit + push**；**收工重跑文档台账**；向 Paul 报告：门户地址 + 冒烟结果 + 给陈忱的灰度通知一句话草稿。
