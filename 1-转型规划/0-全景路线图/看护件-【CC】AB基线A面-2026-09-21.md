---
title: 看护件 · 【CC】A/B 验收 A 面（现状基线）
created: 2026-09-21
派出线: 环境总线 Win-0921-A
批次: B-0921_AB基线
status: 待执行
---

# 看护件 · 【CC】A/B 验收 A 面 · `B-0921_AB基线`

## 〇、口径

本批是「重复调用去重守」A/B 验收的 **A 面＝现状基线**。去重守钩子 `hooks-pretooluse-dedup-guard.ps1` 已建成并逐条实测通过，但**尚未注册进 `.claude/settings.json`**（等 Shao Peishen 按 `0-学习与工具/hooks/README-安装单-0921合并.md` 装），所以本面天然就是「无守」状态，**不需要设逃生阀**。
🔴 **A 面的活不许为省 token 而改做法**——基线要真。B 面跑同一份派单件、同一件活，唯一变量是那道钩子。
🔴 看护件按泳道切片：做什么／不做什么全在 `派单件-【CC】AB基线-sweep告警消费者与夹具核查-2026-09-21.md`（4,427 B），本件不复述。

## 一、硬边界

1. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
2. 🔴 **纯读活**：除产出那一份清单外不改任何既有文件；不碰 `.claude/rules/`、根 `CLAUDE.md`、`.claude/settings.json`。
3. 🔴 **禁起后台子任务／并行 Agent**（`OP-0921-C` 被 600 秒上限掐断过一棒）。
4. 🔴 不整读 `工具-落库sweep.py`（555,367 B）／`test_工具-落库sweep.py`（534,849 B）／`进度编年-CHANGELOG.md`（539,716 B）。
5. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`；`.claude/worktrees/agent-a15b4851f659218aa` 里有 `#455` 真实未提交改动，谁都不许清。
6. 🔴 收工前跑 `git diff --name-only master...HEAD` 贴进报告；只 push 本泳道分支，不 ff master。

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#639` | `AB基线线` | 只读 `0-学习与工具/工具-落库sweep.py`＋其单测；新增 `1-转型规划/0-全景路线图/核查-sweep告警消费者与夹具-<RUNTAG>.md` | 派单件＝`派单件-【CC】AB基线-sweep告警消费者与夹具核查-2026-09-21.md` |

单泳道单棒，无并行；无 ⏭️ 转出项、无「LAN 留步」项。

## 三、泳道 opener

### A1 · A/B 基线 A 面 · 逐类核 sweep 告警消费者与夹具（§一 #639）

粘贴端：CC ｜ 泳道：AB基线线

```
[OP-0921-K]【CC】AB基线A面
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0921k-ab-baseline-run-a`） ｜ worktree：☑（op0921k-ab-baseline-run-a，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】AB基线-sweep告警消费者与夹具核查-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921k-ab-baseline-run-a --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921k-ab-baseline-run-a --done --batch B-0921_AB基线 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头（`工具-opener批处理执行v2.ps1 -Detach`），本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0921-L]【CC】看护AB基线
【设置】执行环境：CC ｜ 分支：0921-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0921L-看护AB基线。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】AB基线A面-2026-09-21.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为该泳道起一个子任务，`isolation: "worktree"`，把【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。
🔴 等待一律用 `python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0921_AB基线 --max-wait 540`，**禁裸 `sleep` 轮询**（队列 §一 `#638`）。
```

## 四、收工汇总与 A/B 取数

```
python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0921_AB基线 --max-wait 540
python 0-学习与工具/工具-Token用量度量.py --since 2026-09-21 --until 2026-09-21
python reports/probe-0921/dup.py <本棒 session id 前 8 位>
```

🔴 **A 面要留下的四个数（B 面逐条对照，缺一不算基线）**：
1. **工具调用数**（transcript 机器核，不取泳道自陈）
2. **完全相同的 (工具,目标) 重复次数与占比**
3. **上下文峰值**
4. **`cache_read`**（🔴 一律按 `requestId` 去重后的口径——未去重会虚高约 1.76 倍，2026-09-21 已实证）

对照基线（2026-09-21 同日四条，同一口径）：峰值 250,882／241,848／223,307／203,666；`cache_read` 21.0M／14.5M／11.2M／5.6M；重复占比 30%／19%／18%／47%（后者为 09-20 `d8a76b23`）。
