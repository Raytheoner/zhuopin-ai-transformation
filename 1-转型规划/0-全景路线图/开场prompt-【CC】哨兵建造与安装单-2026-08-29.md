---
title: "开场prompt-【CC】哨兵建造 apply 与安装单（OP-0829-A）"
created: 2026-08-29
执行方: CC（构建环境方向，队列 #433 建造棒）
来源: openspec 变更包 project-hooks-write-time-sentinels（design 已于 2026-08-29 经 Shao Peishen 审核通过）＋ #433 总线回写
status: 待执行
编号: OP-0829-A
---

### A ·【CC】哨兵建造 apply（H3 乱码＋H4 代词＋公共框架，warn 上线）＋安装单——队列 #433

> 前置已全部解除：design 审通过；裁定 1(a) H1 不回包（editlock-write-side-date-guard 承接）；裁定 2(a) hooks 脚本目录**不**纳入 protected-paths。本棒是 #433 的第三棒（A1 探针、A2 瘦身均已收工）。

**开场词（复制即用，▶ 粘贴端：CC）**：

```
哨兵建造-OP0829A
【设置】执行环境：CC ｜ CC session：☑ 新建 ｜ worktree：☑ 新建 `hooks-sentinels-apply`（⚠️ 开工第一件普通事＝先在主仓 master 取 §二 B-0829_4 批次 commit+push，再建 worktree） ｜ 分支：master 出新分支 ｜ 工作区：无（不触碰 .51 与常驻服务；⚠️ 项目 .claude/settings.json 的 hooks 注册属安装单、由 Shao Peishen 人工一次做完，本棒只产出脚本＋安装单，不得自行改该文件） ｜ 派出线：Cowork 构建环境方案线（OP-0829）
🔴 开工第一件事：把本会话名设为 哨兵建造-OP0829A。工具＝mcp__ccd_session_mgmt__set_session_title，session_id 传字面量 "self"；延迟加载须先 ToolSearch 取 schema；返回值带出旧标题即确认生效。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\开场prompt-【CC】哨兵建造与安装单-2026-08-29.md ＋ CLAUDE.md 当前进度恢复上下文，按文件执行；开干前按文件内预置的 3 个选择题问我澄清。
```

## 背景一分钟

P0 探针（OP-0828-P）已把假设换成实测：Cowork 不吃全局 hooks（收益面＝仅 CC）、`.claude/rules` 可用、H1 原形态判死出包。design 审已过、三项裁定已由总线写进 #433。本棒把变更包 **apply 到可安装状态**：脚本＋测试＋安装单；**注册动作留给 Shao Peishen 人工**（protected-paths 拦 AI 改 `.claude/settings.json`，属设计内）。

## 权威依据（只指针）

① `openspec/changes/project-hooks-write-time-sentinels/`（proposal／design／tasks 0/33／3 份 delta spec）——实现正本；F1（信任门静默忽略→心跳自证在岗＝准入条件）与 F4（按写入目标＋diff 判定，禁对命令串正则）两条硬约束在 design 风险段
② `1-转型规划/0-全景路线图/构建环境P0探针报告-2026-08-28.md` §六/§七
③ 队列 §一 #433 总线回写段（裁定 1a/2a 原文）；`6-人才与组织/人员名录-称谓与性别-正本.md`（H4 判据数据源，A2 棒新建）
④ 全局 `~\.claude\hooks\pretooluse-guard.ps1`——脚本范式参照（stdin JSON→exit 2、fail-open、timeout）

## 开工前置步

1. 主仓 master：取 §二 `B-0829_4_OP0829A_裁定回写与哨兵建造派单` commit+push 销行；随后建 worktree。
2. 按触碰区（`0-学习与工具/hooks/`、openspec 该包、`.claude/settings.json`）grep 两队列真身＋归档件确认无重叠；#433 状态列 edit-row --append 登记「建造棒已认领」。

## 任务分段

**T1 · 实现（tasks.md 33 项逐项勾）**：H3 乱码哨兵＋H4 代词哨兵＋公共框架，脚本落 `0-学习与工具/hooks/`；全部 fail-open（catch{exit 0}）＋timeout 10s；判定一律基于解析出的写入目标路径＋内容 diff（F4）；命中记 `audit-blocks` 同族日志；H4 逐代词对名录正本核对。带单测（含反例）。若 tasks 与 design 冲突，以 design 为准并在收工报告标注差异。
**T2 · 安装单（一页 md，交 Shao Peishen 一次做完）**：① 人工把 hooks 挂进项目 `.claude/settings.json`（给出完整 JSON 片段与粘贴位置）；② `.claude.json` 双信任记录核验/修复（若他已按总线给的命令改完则只核验：两条均须 `hasTrustDialogAccepted=true`）；③ #398 SessionStart 心跳钩子一并注册（F1 对策：哨兵自证在岗）。每步附核验命令与预期输出。
**T3 · 安装后验收（他做完安装单后回本 session 或下一棒执行）**：① 心跳/日志证明哨兵在岗；② Cowork 侧受控 Edit 一次，测**项目级** hooks 对 Cowork 是否生效（P0-1 未答半问，10 分钟）；③ H4 用一封历史跟进信真实跑一次、H3 用含 U+FFFD 的试验文件跑一次，各留证据。
**T4 · 收工**：/opsx 流程走到位（apply 完成、归档按「完工即归档」或写明观察窗口）；#433 回写；warn 两周观察由值周巡检盯（warn→block 的转换判据＝warn 期误报 ≤1 次/周）；自 commit+push＋台账重跑。

## 纪律段（指针）

队列开工必读/收工必写；编辑锁两步查 `$LASTEXITCODE`；状态头 ✅/🟡 写增量开头；三条静默失败写法禁令；日期本机 `Get-Date` 重取；乱码哨兵开工收工各查；🔴 红线＝不自行改 `~\.claude\settings.json`、项目 `.claude/settings.json`、protected-paths（全部留安装单）；不动 `.51`。

## 预置澄清（开干前问，选择题）

1. tasks 33 项与 F1/F4 硬约束若有冲突：(a) 以 design 风险段为准、收工报告列差异（推荐）｜(b) 停下回报。
2. 安装单交付形态：(a) 独立一页 md＋聊天可复制块，不发企微（推荐）｜(b) 另发企微提醒。
3. warn 观察期：(a) 两周（方案默认，推荐）｜(b) 一周（他希望更快转 block 时）。
