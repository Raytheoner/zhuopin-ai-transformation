---
title: "看护件 · 泳道看护批 B-0907_E（六条 B-0903_80 存量 🛑 排队件解禁：全部 propose/design 起草，不 apply）"
created: 2026-09-07
批次: B-0907_E
派出线: Cowork 业务总线 OP-0907-E
LAN: off-LAN（三项未齐，探针实测；本批六条泳道均不依赖内网，无「待回内网」项）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0907_E`

## 〇、本批口径

**立批依据**：Shao Peishen 2026-09-07 现场口令"环境侧构建了不少、优化了机制，现在开可否启新一轮泳道看护？"，答 `1a`（现在出）。

**WIP 现状**（`_count_mechanism_wip` 2026-09-07 现取）：机制类可动 **11/22**，余量充足（早上 `B-0906_G` 起跑时是 20/22）。

**本批候选来源**：扫 §一 全部 `[D:机]` 且非 🛑 行后，发现更大的价值在**已立行、但因 2026-09-02／2026-09-03 两轮 WIP 顶格而被 🛑 排队至今的六行**（`#478`／`#479`／`#481`／`#482`／`#483`／`#462`）——它们的 🛑 段原文均写着"解除条件＝可动 WIP 回落至上限内"，**现在这个条件已经成立**。🔴 **本批范围刻意收窄为 propose/design 起草，不 apply**：六行里 `#483`（本就是纯 openspec 包）、`#462`（自己写"下一步＝起草proposal，不直接开工"）已经这样定性；其余四行（`#478`／`#479`／`#481`／`#482`）的"期望产出"字面上包含真实代码实现，**本批只做到 propose＋design 起草这一步，不碰任何生产文件**——理由：六行一次性全 apply 对应六处不同生产文件的改动，超出一次看护批可控的验证面，且四行里至少两组（`#478`/`#479`/`#482`）历史上都命中过 openspec 门槛①③须走 design 审，**design 都还没审就先动代码等于本末倒置**。apply 阶段留给 design 审过后的下一批。

**同批扫描附带核实一条（不占位）**：`#425`"本地/origin 再度分叉"——现测 `origin/master..master`＝0、`master..origin/master`＝0，**此刻无分叉**；该行本身是只读风险登记（WIP 豁免，不占执行资源），本批未处置，维持登记状态。

**先后关系**：六条泳道互不依赖（各自独立的 openspec 变更包目录），组内无需强制顺序；两波纯因触碰区/并行上限拆分，非依赖关系。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界

1. 🔴 **本批六条全部止步于 propose＋design 起草**：只在 `openspec/changes/<新包>/` 下写 `proposal.md`／`design.md`／`tasks.md`（`tasks.md` 里 apply 段全部留白、标"design 审后另派"）／`specs/<capability>/spec.md`（delta）；**MUST NOT** 修改任何一行生产代码、任何一份生产脚本、任何一处 `.claude/` 载体；`openspec validate --strict` 通过即算收工，不追加实现。
2. 🔴 命中 openspec 三门槛（判据见 `.claude/rules/场景建造与合规.md` §二）即在 `design.md` 里列决策点，**不代拍板**——design 审本身仍是 🟡 档，止步于"起草完成、等他审"，不得因起草顺利就自行推进到 apply。
3. 🔴 推送目标已按 SKILL.md v2.0（2026-09-06 更新）改判：`pause`/`check-heartbeat` 默认发运维逃生群（`WECOM_WEBHOOK_URL_OPS`，Shao Peishen＋IT 陈承二人群，fail-closed 不回落业务群），**不再强制 `--no-notify`**——本批六条均只是"design 审等一句"级别的停点，走默认推送即可，无需刻意抑制。
4. 🔴 本批不修工具（看护会话不修工具，`.claude/rules/队列与落库.md` 同句）：跑批期间发现机制缺陷只登记＋出 CC 派单，唯一例外＝导致本批当场失败且 ≤10 行的就地修，须行内写明"因本批 X 失败就地修，N 行"。
5. 🔴 不碰 `.51`、不碰真实库写操作、不碰他线在办行（`#454`／`#455`／`#480`／`#487`／`#459`／`#436`／`#398` 归各自泳道或已按 B-0906_G/B-0906_P/B-0907_D 处置，本批不复议不重跑）。
6. 🔴 不改队列行历史回写段，只追加；不自造 🛑 豁免理由。

---

## 二、波次与触碰区矩阵

| 波 | 泳道 | 承接 | 触碰区（本批范围＝仅新建 openspec 包，不含下方"生产触碰区"——这里列的是该行未来 apply 阶段会碰、本批不碰的范围，供承接方写 design 时心里有数） |
|---|---|---|---|
| 1 | `483-openspec-backfill` | A1 | `openspec/changes/`（两个新包：sweep 清单覆盖校验＋`#371` 摄取幂等闸） |
| 1 | `478-lanewatch-deploy` | A2 | 未来 apply 会碰 `工具-泳道看护状态机.py`／`zhuopin-lane-watch/SKILL.md`／`lane-watch-mode/design.md`；本批只新建 `openspec/changes/lane-watch-deploy-extension/` |
| 1 | `481-docx-reader` | A3 | 未来 apply 会碰 `md2word.read_checkboxes` 等既有 docx 读取调用点；本批只新建 `openspec/changes/unified-docx-checkbox-reader/` |
| 1 | `479-sweep-manifest` | A4 | 未来 apply 会碰 `工具-落库sweep.py`；本批只新建 `openspec/changes/sweep-manifest-scoped-stage/` |
| 2 | `462-plan-scanner` | A5 | 未来 apply 会碰新扫描器脚本＋`工具-落库sweep.py`（与 A4 未来会撞同一文件，故本批分属不同波，且都只到 propose，不实际冲突）；本批只新建 `openspec/changes/plan-backpressure-scanner/` |
| 2 | `482-serial-gate-guard` | A6 | 未来 apply 会碰 `工具-跟进闸查询.py`／`工具-共享文档编辑锁.py`（闸校验段）；本批只新建 `openspec/changes/followup-serial-gate-hardening/` |

波一 4 条并行（含 A4，虽然它与 A5 未来会碰同一份生产文件，但本批两者都止步于 propose、互不写同一份 openspec 包目录，本波内无实际冲突）；波二 2 条并行（仅因批次总数需要拆波，非依赖关系）。

---

## 三、泳道 opener

### A1 · 483-openspec-backfill——补两个事后 openspec 变更包（sweep 清单覆盖校验＋#371 摄取幂等闸）

粘贴端：CC ｜ 泳道：483-openspec-backfill

```
[OP-0907-F]【CC】483起草2包
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907f-openspec-483`）｜ worktree：☑（`openspec-483`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0907_E
读 ① 队列 §一 `#483`（`python 0-学习与工具/工具-队列查询.py --row 483 --field all`，做什么/不做什么/收工全在该行）→ ② CLAUDE.md 恢复上下文，按该行执行。🔴 本次范围＝只到 propose＋design 起草（两份 openspec 包），不写任何实现代码。本件为 A 类，直接开工。
```

### A2 · 478-lanewatch-deploy——lane-watch ⏭️ 档语义扩展（他在环且 on-LAN 时续做部署）

粘贴端：CC ｜ 泳道：478-lanewatch-deploy

```
[OP-0907-G]【CC】478泳道看护部署语义
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907g-lanewatch-deploy-478`）｜ worktree：☑（`lanewatch-deploy-478`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0907_E
读 ① 队列 §一 `#478`（`python 0-学习与工具/工具-队列查询.py --row 478 --field all`，做什么/不做什么/收工全在该行）→ ② CLAUDE.md 恢复上下文，按该行执行。🔴 本次范围＝只到 propose＋design 起草（新变更包 `lane-watch-deploy-extension`），MUST NOT 改动 `工具-泳道看护状态机.py`／`zhuopin-lane-watch/SKILL.md`／`lane-watch-mode/design.md` 任何一行——三处载体同改是该行"期望产出"里 apply 阶段的事，design 审过后才轮到。核心设计约束"指向而非复制"须体现在 design.md 决策点里。本件为 A 类，直接开工。
```

### A3 · 481-docx-reader——建统一 docx 读取工具

粘贴端：CC ｜ 泳道：481-docx-reader

```
[OP-0907-H]【CC】481统一docx读取
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907h-docx-reader-481`）｜ worktree：☑（`docx-reader-481`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0907_E
读 ① 队列 §一 `#481`（`python 0-学习与工具/工具-队列查询.py --row 481 --field all`，做什么/不做什么/收工全在该行；行内明写"本项刻意排队、不急"，本次是趁 WIP 有余量顺手推进 design，非抢进度）→ ② CLAUDE.md 恢复上下文，按该行执行。🔴 本次范围＝只到 propose＋design 起草（新变更包 `unified-docx-checkbox-reader`），不迁移既有调用点、不改 `md2word.read_checkboxes` 等既有实现。本件为 A 类，直接开工。
```

### A4 · 479-sweep-manifest——sweep 只 stage 清单内路径 ＋ 关键路径宽 except 收窄

粘贴端：CC ｜ 泳道：479-sweep-manifest

```
[OP-0907-I]【CC】479sweep清单收紧
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907i-sweep-manifest-479`）｜ worktree：☑（`sweep-manifest-479`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0907_E
读 ① 队列 §一 `#479`（`python 0-学习与工具/工具-队列查询.py --row 479 --field all`，做什么/不做什么/收工全在该行）→ ② CLAUDE.md 恢复上下文，按该行执行。🔴 本次范围＝只到 propose＋design 起草（新变更包 `sweep-manifest-scoped-stage`），MUST NOT 改动 `工具-落库sweep.py` 一行。本件为 A 类，直接开工。
```

### A5 · 462-plan-scanner——「规划倒逼开工」扫描器

粘贴端：CC ｜ 泳道：462-plan-scanner

```
[OP-0907-J]【CC】462规划倒逼扫描器
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907j-plan-scanner-462`）｜ worktree：☑（`plan-scanner-462`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0907_E
读 ① 队列 §一 `#462`（`python 0-学习与工具/工具-队列查询.py --row 462 --field all`，做什么/不做什么/收工全在该行，含可复用的核心逻辑指针 `取证件-2026-09-02-规划与执行覆盖差集.md` §三）→ ② CLAUDE.md 恢复上下文，按该行执行。🔴 本次范围＝只到 propose＋design 起草（新变更包 `plan-backpressure-scanner`），不建新扫描器脚本、不改 `工具-落库sweep.py`。本件为 A 类，直接开工。
```

### A6 · 482-serial-gate-guard——串行闸两处同触碰修

粘贴端：CC ｜ 泳道：482-serial-gate-guard

```
[OP-0907-K]【CC】482串行闸归一化
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907k-serial-gate-482`）｜ worktree：☑（`serial-gate-482`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0907_E
读 ① 队列 §一 `#482`（`python 0-学习与工具/工具-队列查询.py --row 482 --field all`，做什么/不做什么/收工全在该行）→ ② CLAUDE.md 恢复上下文，按该行执行。🔴 本次范围＝只到 propose＋design 起草（新变更包 `followup-serial-gate-hardening`），MUST NOT 改动 `工具-跟进闸查询.py`／`工具-共享文档编辑锁.py` 任何一行。本件为 A 类，直接开工。
```

---

## 三bis、看护opener（单次粘贴，Task/Agent 工具起六子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0907-E]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0907-E]【CC】看护B-0907_E
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，各子泳道自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0907_E
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0907E-看护六泳道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余六条泳道由你用 Task/Agent 派发，正文里不放这一行），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉；你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0907_E-2026-09-07.md` 全文＋ CLAUDE.md 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，
`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。

扇出与 git（硬口径，逐条照做）：
1. **并行上限 4**，超出的排下一波；同一波内起子任务**错峰 ≥90 s**。波一＝A1～A4（483/478/481/479），波二＝A5～A6（462/482）。
2. 子任务收工**只 push 自己那条分支**——本批六条全部止步于 propose+design 起草，理论上不产生需要 ff master 的实现代码；若子任务的 openspec 包被 `openspec validate --strict` 判过，正常收工即可，**不要求 ff master**（本批性质是"design 审材料"，等 Shao Peishen 审过才轮到 apply）。
3. 🔴 **你自己不 commit**；撞 `.git/index.lock` 不重试、不自造豁免，按「登记待补」写进队列行。
4. 🔴 若任一子任务发现自己被要求的范围超出"propose+design"（比如行内"期望产出"暗示要写实现代码），子任务须在 opener 里已有的范围限制下停手，只做到起草，**不得因为读了队列行就自行扩大范围**。

收工汇总（一次写全）：
5. 每泳道一行 ＋ 心跳全文 ＋ D6 停点列（本批预期为 0——全部止步于起草，design 审本身要等他看，但"起草完成"这个动作本身不是停点，是收工）＋ 转出项（预期 0）＋「待回内网」清单（预期空，本批 off-LAN 但六条均不依赖内网）。
6. 末尾固定一段「**待你一字母**」清单：六份 design.md 各自的决策点逐包列出（不同包不同决策点，不同类，不合并），**design 审类**（本批全部都是）。
7. 🔴 **等待时长只认 `reports/lane-watch-state.json`**（跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_E` 现取），**会话不得自算**。
```

---

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_E` 现取，不手工数；队列回写走锁；推送走默认目标（运维群，fail-closed，无需 `--no-notify`）。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#455`（`edit-row`/`append-row` 写侧校验三缺陷）apply 已完成、未合入 master | 不进本批（不是起草类），已在本次报告里作为「需你定夺」单独列出，答完由业务总线直接合并 |
| `#454`／`#487`／`#480`／`#459`／`#436`／`#398` 各自在办 | 归 `B-0906_G`／`B-0906_P`／`B-0907_D` 各自线，本批不复议不重跑 |
| `#425`（本地/origin 分叉风险登记） | 本批只顺手核实"此刻无分叉"，不处置根因，维持只读登记 |
| `#443`（每周清扫） | 与所有写队列的 session 全重叠，不可并入任何多泳道批次，需单独一条不与其他写队列会话同时跑的 session 处理 |
| `#312`（可 Open 池提醒） | 行内自述已在生产真实生效，只差一次"陈化催办首验"的时间点核实，非新起草工作，不进本批 |
| `#240`／`#358` | 涉外部凭据申请／`.51` 归档策略，可能命中 🔴／⏭️ 档或需人工前置动作，未纳入本次候选评估，留待下一批细看 |
