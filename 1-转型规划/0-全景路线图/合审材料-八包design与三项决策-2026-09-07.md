---
title: "合审材料 · 八包 design ＋ 三项决策（2026-09-07）"
created: 2026-09-07
派出线: Cowork 环境总线 OP-0906-W
status: 待 Shao Peishen 一次作答
---

▶ 本材料 11 项定夺，全部⟨就地答⟩

# 合审材料 · 八包 design ＋ 三项决策（2026-09-07）

> 供 Shao Peishen 一次答完：8 包（#483 拆两包）＋ #455 合并点 ＋ #447 四个开放点 ＋ #487 第三子项。出件：Cowork 环境总线 `OP-0906-W`（起草经只读子代理，逐包读 proposal/design；答 ② 选 (a)「合审材料一次答」）。选 (a) ＝ 全部决策点按起草方推荐，含「无默认」点（答 (a) 即为明确作答）。
> 🔴 默认项（全局）：design 审与合入 master 均属 🟡 档，须你一个字母；不答 ＝ 停在 propose 态不 apply／不合入，**不视为通过**。
> 取证：Read 各包 proposal／design（包 1–7 在 `claude/op0907*` 分支上未合入 master，`git worktree list` 定位；包 8 在 master）；`工具-队列查询.py --row 455/487`；Read `队列行日志/#487.md`。

## 1. sweep-manifest-scoped-stage（§一 #479）
- Why：sweep 的 `git add` 按清单、`git commit` 不带 pathspec ⇒ 提交整个 index，并发 session 暂存内容被套上本批标题（`OP-0827-A`）。
- D① ⒜ pathspec commit；D② ⒝ 五处 commit 全收紧；D③ ⒝ 清单外内容点名告警、照常落库；D④ ⒝ 提交后校验不一致即告警；D⑤ ⒜ 关键路径清单取窄＋反向检查；D⑥ ⒜ 单测 AST 守卫；31 处宽 except 只登记不改。
- 🔴 D① 无默认：⒜ 隐含 `--only` 语义，提交工作树当前值而非 add 时快照（秒级窗口）；不答则夹带继续。
- (a) 通过（①a ②b ③b ④b ⑤a ⑥a）〔荐〕｜(b) 退回：D① 改 ⒞ 临时 index（无窗口风险，代价＝绕开 `git commit` 常规行为）。

## 2. lane-watch-deploy-extension（§一 #478）
- Why：⏭️ 档「碰 .51 一律转出、永不执行」是时序约束非边界；你在环且 on-LAN 时可同 session 授权续做，纪律只指向 `zhuopin-lan-closeout` 正本、不复制。
- D1 (a) 分类仍 ⏭️、转出后新增 `deploy-authorize` 放行；D2 (a) 只打印指针；D3 (a) 状态文件加 append-only 留痕字段；D4 (a) 一次授权＝一项、无时间窗；D5 (a) 新鲜授权即在场唯一证据；D6 (a) 独立 lint 挂 CI、关键词运行时从正本抽取、按序列判违规；D7 (a) 过渡期「不部署」随三载体合入解除、上限 6 不改。
- 🔴 须你拍：决策点 7——放宽后 `lane-watch-mode` D7 首跑上限 6 是否下调（「不改」是判断非事实）。另 `deploy-authorize` 是记录闸、非内核阻断。
- (a) 通过〔荐〕｜(b) 退回：决策点 7 改 (b) 下调首跑上限（须重开 lane-watch-mode D7）。

## 3. unified-docx-checkbox-reader（§一 #481）
- Why：三份 docx 读取实现三种盲区且不出信号，现网 68 份语料 13 份会读成「一条都没勾」，已三次不实指摘专员。
- D① (a) 落 `zhuopin_platform/shared_tools/doc_parser/`；D② (b) 裸 ☑/☒ 给 checked 语义、标来源为字符、与控件分列；D③ (c) 返回三态（读取失败／无载体／有 N 勾 M）＋交叉校验诊断；D④ (b) 覆盖页眉页脚脚尾注；D⑤ (a) 删 `md2word.read_checkboxes()`（无生产调用点）。
- 🔴 D② 无默认：(a) 继续漏读「☑ 认可」（错向＝说人没答，看不见）；(b) 图例 ☒ 可能读成勾了（错向反转、对方可当场反驳；现网三例均支持）。
- (a) 通过（①a ②b ③c ④b ⑤a）〔荐〕｜(b) 退回：D② 改 (a) 维持 #446 不给语义，或 D⑤（不可逆）改 (c) 保留加警示。

## 4. plan-backpressure-scanner（§一 #462）
- Why：35 个场景里「有规划、无队列行、无工程、无包」的对全部机制不可见；现役判据漏报 85%（报 2，实为 13）。
- D1 ⒝ JSONL registry 落全景路线图目录＋一致性 lint；D2 任务列＋标的段＋双侧边界、判定记名；D3 ⒝＋⒞ 别名带上下文＋`excludes` 只降级不剔；D4 (a) 先只读展示、另立行补 `[P:]` 字段；D5 (b) 出队列行草案落 `reports/`、由人 `append-row`；D6 落 `0-学习与工具/`、接 sweep 第 13 类；D7 (a′) 三处查只写一份；D8 退休取证件 §三 脚本、同批上线。
- 🔴 D5 无默认：机器自动写业务场景行撞三条边界（并入审核／机制线越界／你「研发销售域根本不立」明令，R2/S4 首轮即违）。D4 交付时前置闸敞开；D7 (a′) 忠于意图非字面。
- (a) 通过〔荐〕｜(b) 退回：D5 改 (d) 机制线自追「待业务总线立行」行。

## 5. followup-serial-gate-hardening（§一 #482）
- Why：串行闸比的是收信人字符串而非「同一个人」，44 行主表实测误放 4、误拦 1；行长外置工具自动写 `串行豁免：` 已 5 行且单调增长；同一原则四份实现。
- D1 身份键＝(归一化部门, 姓名)；D2 剥姓名段括注、解析失败 None≠None 且出声；D3 (a)「最近一封」改用日期→编号→行序；D4 `❌ 已作废` 按转态前状态分档（已发出须理由＋无开闸措辞＋可核凭据）；D5 (a) 行身份改编号列主键、停产自动豁免；D6 删 5 行机器豁免、保留 `质量部#10` 人写记述、先 D5 后 D6；D7 十条回归用例（T1/T3/T6 改造前先红）；D8 四处消费方改调。
- 🔴 须你拍三点：D3 上线即把陈忱闸由开变锁（`质量部#13` 回填漏洞；处置＝上线前据实改 `✅ 无需回复`＋另立行）；D5 (a) 削弱两态语义检查（十行缓解）；D3／D5 (a) 为起草方扩范围。
- (a) 通过（含追认两处扩范围）〔荐〕｜(b) 退回：D5 改 (b) 专用标记 `行长外置：`（不扩范围，手改其它列仍撞闸）。

## 6. sweep-manifest-coverage-guard（§一 #483⑴，事后补包）
- Why：`6557047` 空壳提交（标题含代码、实际零代码）；修法 2026-08-29 已上生产（`38f3898`），本包零代码、只补 spec。
- 追认 1–4：部分覆盖整批跳过；只拦 `resolved` 非空；形状判据先筛；校验自身异常 fail-open。新问 5 (a) 撤回「跨 worktree 先别登记」口头例外（须总线派单）；6 (a) 跳过只留痕不接企微。
- 🔴 分歧点：6——只留日志无人被通知，误报＝某批静默停在待处理；只可表述「会留痕」。
- (a) 通过〔荐〕｜(b) 退回：6 改 (b) 接企微（须连阈值去重一起做，另开包）。

## 7. fi2-invoice-level-idempotency（§一 #483⑵，事后补包）
- Why：同一发票经两份字节不同的 xlsx 进两次，唐燕萍面板翻倍并误 BLOCK；2026-08-24 已修上 `.51`（#371），本包零代码只补 spec。
- 追认 1–5：按 `inv_no` 只挡跨文件、不按行内容（否则删合法数据）；同文件内不去重；闸状态现读 `invoice.csv`；跳过不进 diagnostics 但计数；存量重建清（3409→3356）。新问 6 (a) `known_invoice_nos=None` 半开状态维持、靠 spec 守；7 (a) 沉淀「未兑现既有意图＝bugfix 可直修，改了下游消费数字则事后补包」判据（须总线派单落 rules）。
- 🔴 分歧点：6——去掉调用点传参无任何测试变红，只会在面板再翻一倍。
- (a) 通过〔荐〕｜(b) 退回：6 改 (c) 加「生产调用点必须传参」源码结构用例（另开包）。

## 8. followup-approval-cooldown-5min（§四 #165，已在 master）
- Why：冷却窗口 10 分钟一天拦住你两次、拦的是你的时间；spec 写死 10，就地改代码会与 spec 漂开且无信号，已回滚走 openspec。
- D1 取 5、否决 3／0／按信件分档；D2 安全性由 `--quote` 必填＋发送前读回承担，削弱二者的变更须重评本阈值；D3 spec 与代码同批改；D4 用例改 3 分钟拒／6 分钟放、写字面量。
- 无分歧，只需通过（Open Questions 为空）。
- (a) 通过〔荐〕｜(b) 退回：维持 10 分钟、撤包。

## 9. #455 写侧校验加固——合并决策点
- 证据（`--row 455`）：2026-09-05 apply 完成（`OP-0905-P`），分支 `claude/op0905n-editrow-guard-455`，commit `7bd70b5` 已 push；design 五点已落地（④ `--repair` 须行内留痕，与原推荐相反）；`WriteGuardHardeningTests` 24 全绿（含 3 反向）；全量 1426 passed／3 failed，该 3 条在未改动主 checkout 同样复现 ⇒ 零回归；`openspec validate --strict` 绿。
- ⚠️ master 已前进（`a9c0c0d`），能否 ff 须先实测 `git merge-base --is-ancestor`；不可 ff 则同 #487 先例 `--no-ff`，先算「工作区脏文件 ∩ 带入文件」为空再动手。
- (a) 现在合入 master〔荐〕｜(b) 暂缓（分支悬置，未合入不归档）。

## 10. #447 跟进信元数据四个开放点（O1–O4，`OP-0902-A` 泳道登记、待你拍；答后 ⑵「事实日／补记日」即可开工）
- O1 README「发送状态」21 种历史取值要不要定枚举：(a) 定＝判据版八态＋闭环三态为闭集，其余由登记 CLI 拒〔荐〕｜(b) 不定，继续自由文本（度量永远要先清洗）。
- O2 「收信人」取值格式：(a) 定为「部门 · 姓名」、姓名以名录正本为准，CLI 校验〔荐〕｜(b) 不定。
- O3 `开放点计数` 与 `开放点` 哪个字段为正：(a) 都不用——以 skill v3.8 已定的 frontmatter `决策点: N 项（…）` 为唯一正本，两旧字段停写〔荐〕｜(b) 保留 `开放点计数`。
- O4 机器守 L1/L2 挂 `release` 咽喉的时机：(a) 与 `#455`／`#448` 同批（锁工具族波，下一批）〔荐〕｜(b) 单独立包先行。
- 一个字母答四点：(a)＝四点全按〔荐〕；(b)＝退回，逐点另答。

## 11. #487 第三子项「取号查重盲区」（取了号未落进载体，算不算占）
- (a) ⑵ 未落盘就不算占〔荐〕：预留但未在载体落成实际行的编号不视为已占；代价＝`_reserve_ids`／`reserved_map` 与 `editlock-queue-number-reservation` spec 均无「预留后从未落盘」的释放逻辑，须真代码＋单测＋spec delta，走 propose→design→apply（同 #480）。
- (b) ⑴ 取了就算占：近于现状、改动最小；代价＝预留失败的号永久空洞累积，查重与载体真身不一致。
- 📎 队列已记你 2026-09-06 裁 ⑵；答 (a) ＝ 确认并放行派工，答 (b) ＝ 改判。

---
答复模板（按推荐全选）：`1a，2a，3a，4a，5a，6a，7a，8a，9a，10a，11a`
