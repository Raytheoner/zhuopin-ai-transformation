---
title: "本周计划 · 2026-09-09（值周加跑）"
created: 2026-09-09
status: 生效
派出线: Cowork 业务总线 OP-0908-V
上游: 值周①对账审计（批次 B-0909总线审计）
---

# 本周计划 · 2026-09-09（加跑）

> **加跑成因**：本周一（09-07）已出过一版；本次系 Shao Peishen 2026-09-09 14:0x 明令「跑值周：对账审计→每周清扫→分诊→出本周计划加跑」。
> **约束（他当日给）**：在跑行 `#462`／`#504`／`#506`／`#419` **只报不改**；单次持锁 ≤30 分钟。
> **上游**：值周①对账审计已跑完，简报见本件 §四；修正 1 处已登 §二 `B-0909总线审计`。

## 一、本周头条（⏰ 09-11，只剩 2 天）

| 序 | 事 | 承接 | 为什么是头条 |
|:--:|---|---|---|
| **1** | **§四 `#181` 三格 K2 外置**：`#482`（6610 B）／`#505`（9974 B）／§四 `#177` | §四 `#181`（🟢 纯执行·可领，**本计划正式派发**） | **09-11 K2 转阻断**，届时任何会话触碰这三行 release 都会被拒。且 `#482`／`#505` 的分诊改判也卡在这上面——改 `[S:xxx]` 前缀须整格重写，而重写后仍超限 ⇒ 不外置就只能滥用行长豁免 |
| 2 | `#506` `.51` 部署 ＋ 全量重算复算 | `OP-0909-K` **在跑** | `采购部#22`／`#499` 双双押后等它；⏰ 09-11 若未完成，§四 `#176` 须重新端给他在 (b)/(c) 间选，**不得默认继续等** |
| 3 | §四 `#174` `#358` P1/P2 两处异常定去向 | 待他 | ⏰ 09-11 |
| 4 | §四 `#177` `#505` design 审四点 | 待他一字母 | ⏰ 09-11；同时是头条 1 的外置对象之一 |

## 二、本周新增（值周本轮判定，需派发）

| 序 | 事 | 状态 |
|:--:|---|---|
| **A** | **核清 §二 38 条「待」批次的真实落库状态** | 🔴 **每周清扫（`#443`）的硬前置**，他 2026-09-09 答 `1a` 定：先核清、清扫挪到核完之后 |
| B | `#160` 三张接力卡 J6 超限：财务 8711 B／质量 8584 B／采购 9466 B（上限 8192） | 业务总线那张已由本线压到 **8060 B 合规**；其余三张属他线触碰区，待各域专线自压或统一派一棒 |
| C | `#519`／`#520`（`OP-0907-AH` 同棒立行，均 `[S:open]`） | 待激活 |

## 三、🔴 本轮查出的一条判据缺口（重复派工第二次，真因与上午那次不同）

**事实**：本轮 12:0x 排 `#462` apply 入泳道批，14:02 收工判为「**重复派工·已回灌**」。而排波前我**做过内容口径实测**——查 `claude/op0907j-plan-scanner-462` 分支，其上只有 propose+design 三笔，无 apply 提交。

**真因（现取）**：`#462` 的 apply **早已在 master** ＝ `1372b3d feat(#462/OP-0907-AH): 规划倒逼扫描器 apply`，产物 `0-学习与工具/工具-规划倒逼扫描器.py` 等三件均已入库。**同名分支有两条**：

| 分支 | 内容 | 队列行是否指名 |
|---|---|---|
| `origin/claude/op0907ah-plan-scanner-462` | **真正做 apply 的那条**（比 master 多 6 笔） | ❌ **行内从未记过** |
| `origin/claude/op0907j-plan-scanner-462` | 只有 propose+design（多 3 笔） | ✅ 行内指名的是这条 |

⇒ **我照队列行的指针查，查到的是起草分支**。`#462` 行内自陈已实测：`--digest --grep "OP-0907-AH"` ＝ **0／174 行命中**，并写下「**不修本格，还会有第三棒**」。

🔑 **判据（本计划立，待并入承接载体）**：**判「某活是否已做」必须查「产物是否在 master」，不得查「行内指名的那条分支上有几笔」。** 分支是过程痕迹、可以有多条且可以没被记全；master 是结果，唯一。

⚠️ **与上午并入 `#397` 的那条判据的关系**：那条防的是「两份看护件排了同一条泳道」（任务维重叠），**防不住本次这种「行内指针指向错分支」**（证据维错源）。两条互补，都不可省。

🔴 **`#462` 本轮只报不改**（他当日明令在跑行只报不改）——本节即「报」；修行内指针的动作待他派发。

## 四、值周①对账审计简报（摘要，全文见本次会话）

- **修正 1 处**：§四 `#170` 类型②字样过时——原写「WIP 23／22 仍超上限」，现取 **22／22 打满未超限**（手段＝`_count_mechanism_wip` 对两份真身现取）。761→1965 B，已回读核实。
- **只报不改 2 处**：`#482` partial→blocked、`#505` open→blocked（分诊器候选，均自陈待他一字母）。**未改理由＝两行超 4096 B**，见头条 1。
- **卫生检查**：高水位线 §一 518／§四 184 **与真实最大号一致** ✅；`ahead=0 behind=0`；worktree 83 个。
- **观察项**：§二 38 条「待」批次积压（最老 08-27，真实落库状态未核清 ⇒ 见 §二 A）；`zhuopin-queue-audit` 装载器内的绝对路径仍是迁移前的 OneDrive 路径（仓库 08-26 已迁 `C:\Dev\zhuopin-ai`）。

## 五、每周清扫（`#443`）· 本轮推迟，理由是判据不是偷懒

`zhuopin-queue-audit` §六 第 1 条写死：「**先完成全部对账修正**——否则会把『其实没完成』的行迁进归档等于永久失踪；批量过滤既误迁也误留（08-03 实测 33/70），唯一可靠是逐行判读，**前提不成立就推迟清扫并如实登记**。」

**前提现不成立**：§二 38 条积压的真实落库状态未核清，而清扫要迁的正是「已完成行」——**拿没核清的行去迁档 ＝ 永久失踪**。

⇒ **Shao Peishen 2026-09-09 答 `1a`**：先派一棒核清积压，清扫挪到核完之后。**本轮不迁行、不回写高水位线**（高水位线本轮实测本就一致，无需动）。

## 六、待你一字母（同类合并，design 审类排 merge 类之前）

**design 审类**
1. §四 `#177` — `#505` design 审四点（⏰ 09-11，本项无默认）
2. `#440` — skill eval 套件 design 审（审读件已由 `B-0909_440审读件` 出完，待你读后答）

**merge 类（🔴 逐条 ff 前必须用内容口径现查，`--no-merged` 假阳性率极高：09-09 实测 67 条里绝大多数是假的）**
3. `#481` ff（apply 已落地 `bdf761f`，分支比 master 多 2 笔）
4. `#504` ff（本轮 apply 已 push，停等）
5. `#162` — `#398`⑺ sweep 自撞锁修复是否 ff

**其他**
6. §四 `#174` `#358` P1/P2 异常去向（⏰ 09-11）
7. §四 `#176` `采购部#22` 是否等 SC2 修复（`OP-0909-K` 在跑，⏰ 09-11）
