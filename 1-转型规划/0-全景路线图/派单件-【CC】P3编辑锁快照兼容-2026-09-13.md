---
title: "派单件 · 【CC】P3 tasks 4.4 编辑锁两处校验的快照兼容性"
created: 2026-09-13
status: 待执行
openspec包: openspec/changes/followup-closure-form-survives-backfill（4.4）
派出线: Cowork 环境总线 OP-0912-E（批 B-0913_收口三泳道）
授权: Shao Peishen 2026-09-13 回「第 5 项选 a」＝放宽编辑锁两处等值比较
---

# 派单件 · P3 `tasks.md` 4.4 收尾

## 〇 · 上一棒停在哪

`OP-0912-AB` 跑完 P3 apply（33/36，已 ff 入 master `5b2ff92`），**4.4 部分完成、如实不勾**：

> 4.4 🔴 **兼容性反例单测**：把「写了快照的状态格」喂给 `classify_status`／`is_closed_status`／`is_reply_arrived_status`／桥一转态／**编辑锁两处校验**，断言结论与「未写快照的同一状态」一致。

已过的：`classify_status`／`normalize_status`／`is_closed_status`／`is_reply_arrived_status`。
**没过的：编辑锁那两处校验**——它们用**等值比较**，喂进带 `━━━ 闭环形态（发出时快照）━━━ …` 后段的状态格就判不出来了。

**Shao Peishen 2026-09-13 答「第 5 项选 a」＝签认放宽这两处等值比较。** 🔴 这是他明确签过的 🟡 口径类改动，**照做即可，但只放宽这两处，不扩散**。

## 一 · 做什么

1. 开工先读：`openspec/changes/followup-closure-form-survives-backfill/tasks.md` 的 4.4 全文与 design 签认段；`0-学习与工具/工具-共享文档编辑锁.py` 现取 `:3989` 与 `:4241` 两处（🔴 **行号会漂，用 `grep -n "FINALIZED_STATUS_MARKER\|FOLLOWUP_FINALIZED_STATUS\|PAUSED_STATUS" | grep "==\|!="` 现取**，不照抄本件行号）。
2. 把这**两处**从「整格等值」改为「**取首段后再等值**」——首段判据**复用 P3 已落地的那一份**（`readme_table` 侧的分段解析），🔴 **不在编辑锁里另写一份切分逻辑**。
3. 补 4.4 要求的兼容性反例单测：带快照的状态格与不带快照的同一状态，在这两处的结论必须一致。
4. 🔴 **只放宽这两处。** `gates.assert_finalized` 是 design D8 明文「不得放宽」的**门禁②红线**，`approval.py` 的整格覆盖语义同样不动——它们不在本次签认范围内。改之前用 `grep` 把七处等值比较全部列出来，在收工报告里逐处标「本棒改了/没改」。
5. 回勾 `tasks.md` 4.4（只有真跑通兼容性单测才勾）。
6. 零回归：跑编辑锁全部单测 ＋ 平台侧跟进信相关单测，与纯 master 出差集。
7. 收工：push 分支 → 登 §二 → 在 §一 `#561` 追一段。

## 二 · 不做什么

- 🔴 **不改 `gates.assert_finalized`**（门禁②红线）、**不改 `approval.py` 两态语义**。
- 🔴 **不碰 P3 的 7.2/7.3**——那两项要真发一封信给真人才算数，属 🔴 对外发送，永不代办。
- 🔴 **不在编辑锁里另写一份状态格切分逻辑**（判据只此一份）。
- 🔴 **不发任何跟进信、不改 README 真身**。

## 三 · 收工报告要有的东西

1. 分支 SHA、两个计数。
2. **七处等值比较逐处现取行号 ＋ 「本棒改了/没改」标注**——没改的要写明为什么不该改。
3. 4.4 兼容性单测名与原文输出（带快照 vs 不带快照，结论一致）。
4. 差集回归结论一句。
5. `tasks.md` 现在几勾几（改前 33/36）、§二 批次号、`#561` 追段原文。
## 硬边界（三条泳道同受，一条都不许省）

- 🔴 **不 ff master**（🟡 档）；收工只 `git push` 本泳道分支。
- 🔴 **不碰 `.51`**、不动常驻执行体运行时（⏭️ 档）；**不发任何跟进信、不触发任何对外发送**（🔴 档）。
- 🔴 **零回归判据＝差集**：分支失败集 ⊆ 纯 master 同命令失败集，**不是「必须全绿」**；纯 master 侧失败原文照抄附上。
- 🔴 **不立新机制行**（机制类可动 WIP 32/22 超限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 队列回写走 `0-学习与工具/工具-共享文档编辑锁.py`；§二「文件清单」格**只放反引号包着的路径**（`#513`）；撞 `.git/index.lock` 不重试。
- 🔴 新文件名形态同批补 `.gitignore` 并 `git check-ignore -v` 实测取证（`#328`）。
- 🔴 **勾 tasks 要诚实**：只勾真做完且有手段可证的。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`；命中 🟡/🔴 决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。