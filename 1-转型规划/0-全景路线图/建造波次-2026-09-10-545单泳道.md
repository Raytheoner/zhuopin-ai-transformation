---
title: "建造波次 · 2026-09-10 · #545 SDK 下载超时透传（单泳道）"
created: 2026-09-10
派出线: Cowork 环境总线 OP-0907-AL
用途: 供 工具-opener批处理执行v2.ps1 -Plan 无头起 OP-0910-J（口令「开启Opener」流程，Shao Peishen 2026-09-10 10:3x「请尽快修复大文件接受机制」）
---

# 建造波次 · #545 单泳道

### A1 · 545 SDK 下载超时透传（OP-0910-J）

▶ 粘贴端：CC
▶ 泳道：sdk-timeout-545
▶ 首次派出：[OP-0910-J]

```
[OP-0910-J]【CC】545下载超时透传
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910j-sdk-request-timeout-545`） ｜ worktree：☑（545-sdk-timeout，会话自起子泳道隔离、收工自删；桌面端勾选框留空） ｜ 工作区：无（纯库内；企微机器人监听重启不在本棒内，由总线在 ff 后按授权另做） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0907-AL
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0910J-545下载超时透传。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/派单件-【CC】545-SDK下载超时透传-2026-09-10.md` → ② `CLAUDE.md` §5 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 前置（机判）：`git fetch origin`；`python 0-学习与工具/工具-队列查询.py --row 545 --field all` 读本行；确认 `5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/notifiers/wecom_aibot.py::AibotConnector.__init__` 的 `factory_kwargs` 仍无 `request_timeout`（有则本棒已被别棒做掉，停手回报）。
2. 修法（三处、不改语义）：⑴ `AibotConnector.__init__` 加关键字参数 `request_timeout_ms: int | None = None`，非 None 时写入 `factory_kwargs["request_timeout"]`（SDK `WSClientOptions.request_timeout` 单位毫秒，默认 10000＝`aibot/api.py` 的 `aiohttp.ClientTimeout(total=10 s)`，即三个 zip 三度失败的真正内层上限）；⑵ `5-平台底座/wecom-aibot-service/aibot_service/connection.py` 构造 `AibotConnector` 处传 `request_timeout_ms=int(media_timeout_seconds * 1000)`，让已存在的环境变量 `WECOM_AIBOT_MEDIA_TIMEOUT_SECONDS` 同时驱动内外两层；⑶ `media_transfer.py` 模块 docstring 补一句「外层 `wait_for` 只在内层 SDK 超时 ≥ 它时才有意义」。
3. 单测：`5-平台底座/wecom-aibot-service/tests/test_connection.py`（或既有 `store["client"].options` 断言所在文件）加 ⑴ `media_timeout_seconds=600` ⇒ `options["request_timeout"] == 600000`；⑵ 未传 `request_timeout_ms` 时 `factory_kwargs` 不含该键（旧调用方零回归）。全量回归按「零回归」闸：失败用例在纯 master 同命令复现即可 ff，写出复跑命令与两侧实测数。
4. 收工：push 本棒分支（不 ff master，🟡 待他一字母）；走锁回写 `#545` 状态列（commit、单测数、回归对照）＋登记 §二；收工汇总末附「待你一字母」与答复模板。

不做什么：
- 不 ff master、不在主仓 commit、不重启 `ZhuopinAibotDevListener`（生产服务，⏭️ 档，ff 后由总线按授权做）；不碰 `.51`、不发企微。
- 不改 SDK 包 `site-packages/aibot/*`（第三方，升级另议）；不改 `media_transfer.py` 的重试语义与默认值；不动 `WECOM_AIBOT_MEDIA_*` 环境变量。
- 不 Read／grep 队列真身；不复述队列协议。
```