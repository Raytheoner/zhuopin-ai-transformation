---
status: 生效
title: "月度环境体检 · 第 0 期基线（2026-07-30）"
执行方: Cowork · CC 环境保障线
队列行: §一 #98（月度环境体检例行，每月最后一个周五；首期 2026-08-28）
取证方式: 本机 PowerShell 只读直读（协议：直读原则）
---

# 月度环境体检 · 第 0 期基线（2026-07-30）

> 本件是 #98「月度环境体检例行」的**基线期**，确立四面取证项与判读口径。首个正式期 = **2026-08-28（8 月最后一个周五）**，届时按本件同结构逐面复测、只记差异。
> 全部结论基于本机 PowerShell 只读直读（sandbox 挂载视图有文件桥滞后，本次已实证：沙箱 `git status` 报数百个"脏文件"，本机直读为 **0**，以本机为准）。

---

## 〇、一句话结论

**四面全绿，无阻塞项**；发现 **5 项待处置**（3 项台面垃圾可直接清、1 项部署惯例缺口、1 项队列字样过时），另有 **2 个临期红线**需 Paul 注意（8/1、8/31）。

---

## 一、面①：worktree 台面与孤儿分支

### 1.1 取证结果

| 项 | 实测 |
|---|---|
| `git worktree list` 登记数 | **8** |
| `.claude/worktrees/` 物理目录数 | **17** |
| `.git/worktrees/` 元数据条目 | **10** |
| 本地分支数 | 15（含 `master`） |
| ahead>0 的分支 | **仅 1 个**（`feat/sc8-baoguan-v2-batch1`，ahead=1） |
| stash | 3 条 |

### 1.2 三档处置清单

**🟢 可直接清（9 个空壳目录 + 2 条陈旧元数据）**

以下 9 个物理目录**已不是 git worktree**（无 `.git` 文件、`git worktree list` 不认），且**内容为空**（0 文件），纯属历次 `worktree remove` 失败或 session 结束后的残壳：

```
affectionate-herschel-c26958      (0 文件)   ← 即 #125 P0 事故那个，文件已被清空
cross-desk-queue-139-143851       (0 文件)
qd-b-release-closure-b1a342       (0 文件)   ← #115 自删遇句柄锁的那个
queue-git-sync-worktree-fbc10f    (0 文件)
r7-price-check-baseline-8b819f    (0 文件)
supply-board-batch1-fixes-93ba23  (0 文件)
supply-board-data-verification-23220c (0 文件)
wecom-listener-macos-migration-ab5815 (0 文件)
fi2-regression-queue-reconcile-07bab3 (1 文件：.claude/settings.local.json，998B)
```

`.git/worktrees/` 里另有 **2 条陈旧元数据**（`affectionate-herschel-c26958`、`qd-b-release-closure-b1a342`）指向已空目录。

> **处置建议（交 CC 执行）**：`git worktree prune` 清元数据 → PowerShell `Remove-Item -Recurse` 清 9 个空壳目录（**不用** `git worktree remove`，#125 已证其非原子失败会先清空目标再报错）。风险≈0：目录空、元数据陈旧、无分支指针依赖。

**🟡 需确认（2 个有未提交改动的 worktree）**

| worktree | 分支 | 未提交内容 | **本次已核实** |
|---|---|---|---|
| `sweep-criteria-sync-fix-7eb8a7` | `claude/sweep-criteria-sync-fix-7eb8a7` | `工具-落库sweep.py` +72 行、`test_工具-落库sweep.py` +137 行 | **✅ 内容已在 master**——文件哈希与主工作区版本**逐字节相同**（均 553 行）；`git diff` 的 72 行只是相对该 worktree 自身陈旧 HEAD（`453fad2`，落后 96 提交）的差，非新内容。**无丢失风险，可清。** |
| `fi2-validation-prep-66ed2c` | `claude/fi2-validation-prep-66ed2c` | `跨桌任务队列.md` 1 处改动（#79 行"分支不一致已收口"） | **✅ 内容已在 master 且更完整**——主库 #79 行不仅含同一收口结论（Paul"不发，按重复处理"+ README 已删除记录），还多一段 **07-28 补记**（#80 真实探测证伪"变更未跟进"假设）。worktree 版是**被超越的陈旧副本**。**可丢弃。** |

> 这两项正是 **#101 那类风险的真实样本**——所幸本次逐一取证证明**均无内容丢失**。判读口径值得固化：**worktree 里的"未提交改动"不等于"未落库内容"**，必须与 master 逐字节/逐语义比对后才能定性（本次若只看 `git diff` 行数，会误判为 209 行成果待抢救）。

**🔴 绝不可动（1 个）**

| `wecom-service-home` | `ops/wecom-service-home` | 企微机器人常驻运行目录，dirty=0，正在服务中 |

**3 条 stash**（均在 master 上下文）：

```
stash@{0}  SAFETY-2026-07-27-主工作区陈旧态修复前快照
stash@{1}  wip before pull check
stash@{2}  cowork-uncommitted-macmini-queue-edit-2026-07-23
```

> 本次**未逐条展开比对**（工作量与本次主攻不匹配）。建议下期或 CC 顺手做：`git stash show -p` 逐条与 master 比对，确认内容均已落库后统一 drop。在此之前**保留不动**——3 条 stash 的存在成本≈0，误删成本很高。

### 1.3 ⚠️ 顺带发现：worktree 目录名与分支名已系统性错位

| 物理目录 | 实际 checkout 的分支 |
|---|---|
| `.claude/worktrees/fi2-web-service-16da2a` | `claude/supply-board-batch1-fixes-93ba23` |
| `.claude/worktrees/dreamy-ramanujan-35e2e8` | `claude/fi2-web-service-16da2a` |
| `.claude/worktrees/qd-b-grayscale-improvements-9dbe6f` | `claude/watermark-audit-sentinel-fix-58d797` |
| `.claude/worktrees/loving-mestorf-98749e` | `claude/qd-b-release-closure-b1a342` |
| `.claude/worktrees/musing-pascal-68d14e` | `claude/four-services-temp-auth-3c6bd5` |

**5/8 个 worktree 的目录名与分支名不对应**，且出现 `dreamy-ramanujan` / `loving-mestorf` / `musing-pascal` 这类**随机代号目录**。后果：**"看目录名即知是什么活"这一惯例已实质失效**——本次盘点必须逐个 `git -C <path> branch --show-current` 才能认出谁是谁，孤儿判定与"绝不可动"识别都因此变慢、变险（`wecom-service-home` 恰好名实相符是运气，不是机制）。

> 建议登为队列新行（P2）：worktree 创建时目录名强制取分支名尾段，或至少在 `.claude/worktrees/<dir>/` 下留一个 `WHOAMI.md` 写明分支与任务号。

---

## 二、面②：常驻服务健康

### 2.1 四条自动化（Windows 计划任务 + Claude 定时任务）

| 服务 | 载体 | 实测状态 |
|---|---|---|
| `ZhuopinCommitSweep` | Windows 计划任务 | **Ready**，上次 07-30 15:41 **Result=0**，下次 07-30 19:36 ✅ |
| `ZhuopinAibotDevListener` | Windows 计划任务 | **Running**（Result=267009 = `SCHED_S_TASK_RUNNING`，非错误码），上次启动 07-30 14:19 ✅ |
| `huijian-chaijian-patrol` | Claude 定时任务 | **enabled**，cron `0 9,13 * * 1-5` **双班已生效**（上次 07-30 13:04 CST 真实跑过），下次 **07-31 09:04** ✅ |
| `weekly-status-update` | Claude 定时任务 | **enabled**，cron `0 10 * * 1`，上次 07-27 10:08，下次 **08-03** ✅ |

> 判读注记（供下期沿用）：`ZhuopinCommitSweep` 与 `ZhuopinAibotDevListener` 是 **Windows** 计划任务（用 `Get-ScheduledTask` 查）；`huijian-chaijian-patrol` 与 `weekly-status-update` 是 **Claude 定时任务**（用 `list_scheduled_tasks` 查）。**两者不在同一个清单里**——本次先用 `Get-ScheduledTask` 查四个，后两个报"找不到"，差点误判为"两条自动化已丢失"。下期直接分两处查。

### 2.2 `.51` 四个 Web 服务（含 #160 门禁上线次日核验）

| 端口 | 服务 | `/api/ping` | 首页 |
|---|---|---|---|
| 8091 | 保供看板 | **200** ✅ | **302** ✅ |
| 8092 | 命令中心 | **200** ✅ | **302** ✅ |
| 8093 | QD-B 立项门禁 | **200** ✅ | **302** ✅ |
| 8094 | FI2 三单匹配 | **200** ✅ | **302** ✅ |

**#160 共享口令门禁（07-30 上线）次日复核通过**：四服务全部存活、`/api/ping` 豁免正常放行（保证 `deploy-server.ps1` 健康检查不被挡）、首页未登录一律 302 跳登录页。**这是门禁"既没挡住机器、也没漏掉人"的直接证据。**

### 2.3 关于"新场景不得新起端口"硬约束

CLAUDE.md §5（2026-07-29 定）已明确新场景一律挂统一门户路由。本次四服务仍是四个独立端口（存量，允许慢慢收编）。**下期体检增设一项：核对本月新上线场景有无新起端口**——这是该硬约束唯一的事后检验点，否则它只是一句写在文档里的话。

---

## 三、面③：安全挂账进展

| 项 | 状态 |
|---|---|
| **#91(a)** `supplychain/.env` sa 超管 + 明文口令（生产库 192.168.6.2）→ 轮换 + 改最小权限只读账号 | 跟进信 **✅ 已发陈承**（2026-07-27 07:49 UTC，CC 机器人，抄 Paul）；**建议截止 8/1**，陈承处置结论待回 |
| **#91(b)** U9C `/Services` SOAP 免鉴权越权 → 网络 ACL + 核验写端点是否亦免鉴权 | 同信已发；**建议截止 8 月中** |
| **#160** 四服务零鉴权 LAN 暴露 | **✅ 已临时止血**（07-30 共享口令门禁上线，本次已复核生效）；正式企微 OAuth SSO 另出架构决策件 |
| **§四#33** git 历史 PII（commit `6ebf962` 客户真实姓名/电话） | **待 Paul 拍板**是否改写历史（破坏性操作）——自 07-20 提出至今未决 |

> **🔴 临期提醒（P1）**：#91(a) 建议截止 **2026-08-01（后天）**，陈承尚无回件。sa 超管 + 明文口令指向**生产库**，是当前挂账里危害最高的一项。距首次登记（07-06）已 24 天。**建议 Paul 8/1 当天若仍无回件，直接线下催一次陈承**——按机制等下一轮巡逻会滑到 8/3。

---

## 四、面④：部署惯例一致性

对全库 5 个 `deploy-server.ps1` 逐项核对四类已踩坑：

| 脚本 | UTF-8 BOM | 防火墙入站规则 | SYSTEM+AtStartup | Get-Command 绝对路径 |
|---|---|---|---|---|
| `1-转型规划/AI运营指挥中心/` | ✅ | ✅ | ✅ | ✅ |
| `4-数字员工/采购部/SC8-客户订单交期智能承诺/` | ✅ | ✅ | ✅ | ✅ |
| `4-数字员工/质量部/QD-B-立项审核门禁/` | ✅ | ✅ | ✅ | ✅ |
| `4-数字员工/财务部/FI2-三单匹配自动对账/` | ✅ | ✅ | ✅ | ✅ |
| `5-平台底座/wecom-aibot-service/` | **❌ 无 BOM** | ❌ 无（**合理**） | ✅ | ✅ |

**四个 Web 服务惯例已完全一致**——这说明 #96《.51 部署标准清单》要固化的四类踩坑，在实践中已被 CC 稳定复用，清单是在**追认既成事实**（好事：清单落地阻力小）。

**唯一缺口 = `wecom-aibot-service/deploy-server.ps1` 缺 UTF-8 BOM**：
- 防火墙规则缺失是**合理**的——机器人是出站 WebSocket 长连接，不监听入站端口，无规则可加。
- 但 **该脚本含中文字符**（已实测），一旦在任何内建 Windows PowerShell 5.1 的机器上执行，会按系统 ANSI 误读中文导致语法错误——这正是 07-21 命令中心部署踩过的坑。目前该服务常驻 Paul 的 Win 笔记本、由 `start-aibot-service-dev.ps1` 拉起，`deploy-server.ps1` **可能并未真正使用**。
- **处置建议**：先确认该脚本是否仍在链路上；若在，补 BOM（一行改动）；若已废弃，删除或标注"未使用"，避免下次有人照着跑踩坑。

---

## 五、待处置汇总（本期新增）

| # | 事项 | 归属 | 优先级 |
|---|---|---|---|
| 1 | 清 9 个空壳 worktree 目录 + `git worktree prune` 清 2 条陈旧元数据 | CC | P3（纯卫生） |
| 2 | 清 `sweep-criteria-sync-fix-7eb8a7` / `fi2-validation-prep-66ed2c` 两个 worktree（**已核实内容均在 master，无丢失风险**） | CC | P3 |
| 3 | worktree 目录名与分支名系统性错位（5/8）→ 建 `WHOAMI.md` 或强制命名 | CC | P2（认活变慢变险） |
| 4 | `wecom-aibot-service/deploy-server.ps1` 缺 BOM（含中文）；先确认是否仍在链路上 | CC | P2 |
| 5 | 3 条 stash 逐条比对后统一 drop（本次未展开） | CC 顺手 | P3 |
| 6 | 队列 §四 #35 **标题字样过时**：写"红线 2026-07-31，4 天后"，正文已正确记为 8/31 顺延——只扫标题会误判明天到期 | 对账审计（第二类失真） | P2 |

## 六、需 Paul 注意的临期红线

| 日期 | 事项 |
|---|---|
| **2026-08-01（后天）** | #91(a) sa 超管账号轮换建议截止，陈承无回件 → 建议当天线下催 |
| **2026-08-31** | R1 工程研发 AI Champion 指定门禁（07-27 由 7/31 顺延）；未达成则 R1 走重组循环顺延入 S3 |
| 待定 | §四#33 git 历史 PII 是否改写——自 07-20 悬置至今，**属破坏性操作、只能 Paul 拍板** |

---

## 七、下期（2026-08-28）执行清单

1. 面①：`git worktree list` / 物理目录 / `.git/worktrees` **三者数量是否一致**（本期 8/17/10，差额即残壳）；ahead>0 分支逐个确认；stash 增量。
   - 🔴 **跨期挂账一项，本期必须清掉（Shao Peishen 2026-08-02 明示"让机制收口"，即不手工强删、交本体检承接）**：`.claude/worktrees/sweep-criteria-sync-fix-7eb8a7` 是一个 **0 文件空壳目录**——#165 执行时内部文件已清空，但**目录外壳本身连续两次 `Remove-Item -Recurse -Force` 均报 `Permission denied`**（排查非 OneDrive、非并发 claude.exe 占用，当时 `SearchIndexer` 在跑，疑为其瞬时扫描所致），故当场跳过、如实登记。**git 已不认它**（`git worktree list` 无此条、`.git/worktrees` 元数据已 prune），**零数据与安全风险，纯待办**。
   - **本期动作**：`Remove-Item` 重试一次即可；仍失败则**先查占用句柄**（`handle.exe` 或 `Get-Process` 交叉）再定性，**不要第三次盲重试**——连续三次同样失败就不再是"瞬时占用"，需换解释。清掉后本条销账。
   - ~~**顺带**：2026-08-02 实测 `git worktree list` = 3 linked、物理目录 = 4（即差额 1 = 上述空壳），**三者一致性判据在本期应回到 3/3/3**。~~ ← **同日 20:00 本地即过时，见下**
   - 🔴 **判据更新（2026-08-02 20:00 本地 / 12:00Z 实测，取代上一条）**：**`git worktree list`=2 ／ 物理目录=4 ／ `.git/worktrees`=2**。三点变化：
     - ⑴ **空壳由 1 个变 2 个**——新增 `qd-b-grayscale-improvements-9dbe6f`（0 文件、git 已不认），成因＝五行同批 CC session 的路径事故补救后按既例删除失败（同 #125／#207 已知的句柄占用模式，CC 已 `git worktree prune` 并如实登记）。**本期两个空壳一并清掉。**
     - ⑵ 🔴 **`musing-pascal-68d14e` 由持有分支 `claude/queue-numbering-alert-criteria-855665` 变为 detached HEAD**（同日 07:45Z 实测尚持有该分支）。**成因未查清，如实留白，两种解释都可能**：(甲) 五行同批 session 的补救动作误落在此 worktree（该 session 已自认发生过路径混淆事故，形态吻合）；(乙) 另有独立动作。**无内容损失**——分支完好存于 `fa9c5b8`、该 worktree 工作区 0 脏文件，**故不作事故处理，只作体检待判项**。
     - ⑶ 该 worktree 现无承载分支、1176 文件、停在 `fa9c5b8`（落后 master 多个提交）。**是否一并清理，按 #166 口径判定**（worktree 身份一律以 `git branch --show-current` 当刻实测为准，不依赖目录名）。
   - **本期新判据**：清完两个空壳后三数应为 **2/2/2**；若 `musing-pascal-68d14e` 一并清理则为 **1/1/1**（只剩 `wecom-service-home` 常驻服务）。
2. 面②：Windows 计划任务（`Get-ScheduledTask`）**与** Claude 定时任务（`list_scheduled_tasks`）**分两处查**；`.51` 四端口 ping+首页；**新增：本月新上线场景有无违反"不得新起端口"硬约束**。
3. 面③：#91(a)(b) 陈承回件是否到位；§四#33 是否仍悬置；#160 是否已被正式 SSO 取代。
4. 面④：全库 `deploy-server.ps1` 四类踩坑逐项核对（本期基线：4 个 Web 服务全绿、机器人脚本缺 BOM）。
5. 只记**与本基线的差异**，不重复抄绿项。
