---
title: "看护件 · B-0916_Token优化P2BP5（两泳道并行）"
created: 2026-09-16
批次: B-0916_Token优化P2BP5
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 1a）
---

# 看护件 · B-0916_Token优化P2BP5

> 路线图正本：`构建Token消耗优化-核验与路线图-2026-09-16.md` §〇。任务全文在队列 §一 `#582`／`#583`，本件不复制。

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#582` | P2B：PostToolUse 上下文计量 hook，越 150k 提醒转场 | 新 hook＋单测、settings.json、opener骨架.md |
| A2 | `#583` | P5：开场底噪拆分实测，再砍 CLAUDE.md／skill 描述／hook 注入 | CLAUDE.md、skills源码 frontmatter、两个注入 hook |

触碰区零交集：`#582` 不碰 CLAUDE.md／SKILL.md，其一行纪律文案由 `#583` 并入。

## 二、硬边界（两条泳道同受）

- 🔴 只 push 本泳道分支，不 ff master——ff 属 🟡。
- 🔴 不碰 `工具-落库sweep.py`、`.51`、企微机器人、定时任务运行时；不改全局 `~/.claude/` 任何文件。
- 🔴 A2 的对照实测只用 `--model haiku` 的最小会话，每次 < 1 分钟，总数 ≤ 12 次；不得改提交态的插件启用配置。

## 三、泳道

### A1 · §一 #582 150k 上下文转场机器提醒

▶ 粘贴端：CC ｜ 泳道：op0916e-context-meter-582

```
[OP-0916-E]【CC】150k转场计量hook
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0916e-context-meter-582`） ｜ worktree：☑（op0916e-context-meter-582，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token优化P2BP5
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916E-150k转场hook。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #582（`python 0-学习与工具/工具-队列查询.py --row 582 --section 一 --field all`，做什么／不做什么／收工全在该行）` → ② `1-转型规划/0-全景路线图/构建Token消耗优化-核验与路线图-2026-09-16.md` §〇 → ③ `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（判据已写死），无需再问澄清，直接开工。
🔴 hook 性能是硬指标：尾读 transcript、p95 < 300ms，单测里量；任何异常静默放行，绝不拦工具。
🔴 测试一律前台跑、`pytest -q --tb=short`，跑完即 commit，严禁放后台等通知（`-p` 模式下会话会在等待时结束，09-16 `OP-0916-B` 实撞）。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916e-context-meter-582 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916e-context-meter-582 --done --batch B-0916_Token优化P2BP5 --text "产出落点：<落点>"`。
🔴 收工只 push 本泳道分支，不 ff master；在 `合入登记/pending-ff.jsonl` 登记待授权合入行；状态列 edit-row --append 回写。worktree 删不掉时如实报、不强删。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾。
```

### A2 · §一 #583 开场底噪拆分实测与瘦身

▶ 粘贴端：CC ｜ 泳道：op0916f-baseline-noise-583

```
[OP-0916-F]【CC】开场底噪拆分瘦身
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0916f-baseline-noise-583`） ｜ worktree：☑（op0916f-baseline-noise-583，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token优化P2BP5
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916F-开场底噪瘦身。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #583（`python 0-学习与工具/工具-队列查询.py --row 583 --section 一 --field all`，做什么／不做什么／收工全在该行）` → ② `1-转型规划/0-全景路线图/构建Token消耗优化-核验与路线图-2026-09-16.md` §〇 → ③ `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（判据已写死），无需再问澄清，直接开工。
🔴 先做第一段拆分实测并回写 #583，再动第二段；CLAUDE.md 改动走编辑锁；`superpowers` 停用只起草、不改默认。
🔴 测试一律前台跑、`pytest -q --tb=short`，跑完即 commit，严禁放后台等通知（`-p` 模式下会话会在等待时结束，09-16 `OP-0916-B` 实撞）。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916f-baseline-noise-583 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916f-baseline-noise-583 --done --batch B-0916_Token优化P2BP5 --text "产出落点：<落点>"`。
🔴 收工只 push 本泳道分支，不 ff master；在 `合入登记/pending-ff.jsonl` 登记待授权合入行；状态列 edit-row --append 回写。worktree 删不掉时如实报、不强删。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾。
```

## 四、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】Token优化P2BP5两泳道-2026-09-16.md" -FullAuto -Yes -Detach
```
