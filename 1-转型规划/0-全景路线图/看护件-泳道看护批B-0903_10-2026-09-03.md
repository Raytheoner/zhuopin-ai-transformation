---
title: "看护件 · 泳道看护批 B-0903_10（第三批：两条续跑段）"
created: 2026-09-03
批次: B-0903_10
派出线: Cowork 业务总线 OP-0903-A
前批: B-0902_60（4 泳道 5 项，停 2 次，2026-09-03 07:30 全部已答已 resume）
status: 执行中
---

# 看护件 · 泳道看护批 `B-0903_10`

## 〇、本批口径

前批 `B-0902_60` 的两条泳道停在 🟡 决策点整夜（等了 7.5／7.3 小时），Shao Peishen 2026-09-03 上午**六项裁决全部取 (a)**，本批即那两段续跑。**裁决原文已内置于各 opener，泳道不必回头猜。**

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不手抄。

## 一、硬边界（继承，逐条适用）

1. 🔴 **⏭️ 档：`.51` 部署及任何触碰生产服务的动作永不执行** —— `transfer-out` 后跳过、继续其余；不进 `OPENER_PARTIAL`。
2. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 永不代办**，只做到「可发送态」。
3. 🔴 **业务*数据*与一切对外发送永不自动入批**。
4. 🔴 **所有 `pause` / `check-heartbeat` 一律带 `--no-notify`，无一例外**。
5. 判据未覆盖按 🟡 停（fail-safe）；⏭️ 转出档不参与。
6. 🔴 **三闸全锁**（唐燕萍／姚祖怡／陈忱）⇒ 本批**不得起草或发送任何跟进信**。
7. 🔴 **规划文档一律不改**（全景规划／实施计划／权威排期表／前置总表）—— 那属全景路线图线，本批只出**移交单**。

## 二、波次与触碰区矩阵

| 波 | 泳道名 | 项 | 触碰区 | 重叠 |
|:--:|---|:--:|---|---|
| 1 | `sc8-kitdate-3` | A1 | `openspec/changes/sc8-atp-batch2-closeout/`、`reports/` 对照表 | 无 |
| 1 | `scenario-rows-2` | A2 | 移交单（新建）、队列 §一 新行 1 条 | 无 |

两条触碰区零重叠，并行。

## 三、泳道 opener

### A1 · ATP 立项 design 重审收口

粘贴端：CC ｜ 泳道：sc8-kitdate-3

```
[OP-0903-A1]【CC】ATP重审收口
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903a1-atp-review`）｜ worktree：☑（sc8-kitdate-3，新 worktree，收工自删）｜ 工作区：无（openspec design/spec 与 reports 对照表，🔴 不进实现、不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_10
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903A1-ATP重审。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_10-sc8-kitdate-3.md`，开工 1 分钟内写首行「HH:mm:ss ｜ 已开工」，每里程碑追加，超 10 分钟的步骤补写「仍在等 X，预计还要 N 分钟」，收工写「DONE ｜ 产出落点」。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_10 --wave 1 --lane sc8-kitdate-3 --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL` 收尾；⏭️ 走 `transfer-out` 后跳过继续。
读 ① `openspec/changes/sc8-atp-batch2-closeout/design.md` → ② 前一段（B3）产出的「改判前后逐行对照表」→ ③ 队列 §一 #118。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 裁决（三项全取 (a)，原文）：
  乙-1 D3-bis =(a) **采纳本班「不重判」判定**——实测前移 11 行（**非裁决书所写 24 行**，
       另 13 行为已答交件、规则3 的 max 令其一天不动、四色一格不动），实际代价小于他已接受的
       代价且方向一致 ⇒ 不重判。🔴 **但该数字更正须写进 design 与对照表**，并在下一封致
       姚祖怡（男）的信中如实说明「我方原估 24、实测 11」。
  乙-2 D4 =(a) **补一条边界口径进判例包，随下一封请姚祖怡签认**——理由：D3 取 (a) 后
       「含边界更保守」的论证由条件句变成既成事实、已失效，现行取值从此既无签认也无保守性背书，
       **必须补签，而不是记录风险接受**。
  乙-3 D5 =(a) **补一条验证用例覆盖不变式③**（它在对照表上空过、未获验证），补完再放行。

做什么：
1. 按乙-1 更正 design 与对照表里的行数（24 → 11），并写明更正来源是逐行核对、不是估算。
2. 按乙-2 在判例包草案中新增一条边界口径条目，标注「待姚祖怡签认」；
   🔴 判据/口径类**永不默认生效**，不得写「48h 默认生效」。
3. 按乙-3 补验证用例覆盖不变式③（可落 tasks，本泳道不进实现）。
4. 重跑 `openspec validate --all --strict`，须绿。
5. 收工：§二 批次登记（走 `0-学习与工具/工具-共享文档编辑锁.py`），§一 #118／#344 回写。

不做什么：
- 🔴 不 `/opsx:apply`、不进实现（design 审此轮只是补齐，实现另立行）
- 🔴 不发任何信（姚祖怡串行闸锁着，`采购部#20` 在途）
```

### A2 · SC4 顺延与前置总表移交单 ＋ FI10 芯片 API 立行

粘贴端：CC ｜ 泳道：scenario-rows-2

```
[OP-0903-A2]【CC】SC4顺延移交单
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903a2-sc4-handover`）｜ worktree：☑（scenario-rows-2，新 worktree，收工自删）｜ 工作区：无（移交单与队列行，🔴 不改任何规划文档、不部署）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_10
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903A2-SC4移交。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_10-scenario-rows-2.md`，规则同 A1。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_10 --wave 1 --lane scenario-rows-2 --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #463 ＋ 前一段立起的 #467-#474 → ② `1-转型规划/0-全景路线图/移交单-O1注销并入-全景路线图线执行重组循环-2026-09-02.md`（**移交单体例参照，照它的结构写**）→ ③ skill `zhuopin-rebaseline` 的固定闭环。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 裁决（三项全取 (a)，原文）：
  甲-1 SC4 排期 =(a) **走全景路线图线重组循环正式顺延** —— 2026-10 已不可行：
       两项知识型前置（法务标准合同条款库、合同风险条款判据）总表状态格**零回填**，
       按「知识型前置须启动前 8 周开工」纪律最早 10 月中下旬；
       账面留着只会每周报一次假逾期（同 #373 那笔）。
  甲-2 前置总表缺 5 行 =(a) **补进《跨场景前置数据与知识库任务总表》** ——
       FI5/FI6/FI8/FI9/FI10 在 §一 与 §一.2 均无条目，**判据源本身缺行**，
       导致这五个场景的前置从未被排过期。🔴 属规划文档改动 ⇒ **出移交单，本泳道不得直接改**。
  甲-3 FI10 芯片价格 API =(a) **单独立行推它** —— 该 API 与 O2 芯片预警共用，
       挂在单个场景名下会随该场景顺延一起无人管；实施计划依赖表至今仍是「待选型」，
       而规划写「8 月完成调研选型、9 月签合同」，**已逾期**。

做什么（三件）：
1. **出一份移交单**（甲-1 ＋ 甲-2 可合并为一份，交全景路线图线），体例照 `移交单-O1注销并入-…-2026-09-02.md`：
   须含 ⑴ 拍板原文与依据 ⑵ 已核定的事实与取证 ⑶ 承接方要做的（按 `zhuopin-rebaseline` 固定闭环）
   ⑷ 🔴 承接方须自行判定的事项 ⑸ 本线已做与未做（边界清楚）⑹ 时点。
   🔴 **SC4 顺延到哪个月由承接方按前置就绪日推算，本线不代定**——只写清「2026-10 不可行」及其证据。
2. **立 FI10 芯片价格 API 独立行**（走编辑锁 `acquire --reserve 1 --section 一 --domain 业`）：
   须写明它与 O2 芯片预警共用、当前状态「待选型」、原定 8 月选型/9 月签合同已逾期、
   以及「不挂单一场景名下」的立行理由。
   ⚠️ 若 WIP 阻断：把新行**状态列**正文以 🛑 起首标「排队中·暂非可动」
   —— 🔴 判据在**状态**格不在任务格（`_count_mechanism_wip` 读状态格，2026-09-02 实撞）。
3. 收工：§二 批次登记，§一 #463 回写（列明移交单落点与 FI10 行号）。

不做什么：
- 🔴 **不改全景规划、实施计划、权威排期表、前置总表任何一个字** —— 一律走移交单
- 🔴 不碰销售域与研发域场景（他 2026-09-02 明令暂缓、等他通知再排序，**不是立成 🛑 排队行，是根本不立**）
```

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0903_10` 现取，须含 D6 停次明细、转出项、「待回内网」清单。
