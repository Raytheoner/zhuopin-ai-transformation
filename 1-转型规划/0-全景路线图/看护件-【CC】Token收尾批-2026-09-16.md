---
title: "看护件 · B-0916_Token收尾批（四泳道，并行上限内分波）"
created: 2026-09-16
批次: B-0916_Token收尾批
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 20:44 明令本线全力收尾 Token 治理）
---

# 看护件 · B-0916_Token收尾批

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#596` | 编辑锁复合命令三处可用性缺口 | 编辑锁工具＋单测 |
| A2 | `#597` | P4 输出截流 | sweep／lint／扫描器输出层、opener骨架、CLAUDE.md |
| A3 | `#598` | P2 看护等待脚本＋长会话计步 | 新看护等待工具、lane-watch SKILL、context-meter 钩子 |
| A4 | `#599` | P6 巡逻无状态化 | patrol_dispatch.py＋单测 |

四条触碰区两两零交集；均不得碰 `.claude/settings.json`。

## 二、泳道

### A1 · §一 #596 编辑锁小修

▶ 粘贴端：CC ｜ 泳道：op0916s-lock-ux-596

```
[OP-0916-S]【CC】编辑锁小修
【设置】执行环境：CC ｜ 分支：`claude/op0916s-lock-ux-596`（新建自 master） ｜ worktree：☑（op0916s-lock-ux-596，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人运行时／计划任务） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token收尾批
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916S-编辑锁小修。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #596（`python 0-学习与工具/工具-队列查询.py --row 596 --section 一 --field all`）` → ② `CLAUDE.md`，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按 #596 期望产出 ⑴→⑷：失败且确未写入才自动放锁；近期持锁提示默认一行、--verbose 展开；commit-append 支持 --reserve；单测覆盖。
🔴 只动本行「触碰区」列所列文件；越界需求只登记不动手。不放宽任何锁／lint／登记／门禁判据。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出（全量回归、大范围 grep）交子代理只收结论。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short --maxfail=5`，跑完即 commit；差集回归与 master 同命令对跑。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916s-lock-ux-596 --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916s-lock-ux-596 --done --batch B-0916_Token收尾批 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用一条命令：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916S --section 一 --number 596 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；报错后锁仍占用则先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

### A2 · §一 #597 输出截流P4

▶ 粘贴端：CC ｜ 泳道：op0916t-out-trim-597

```
[OP-0916-T]【CC】输出截流P4
【设置】执行环境：CC ｜ 分支：`claude/op0916t-out-trim-597`（新建自 master） ｜ worktree：☑（op0916t-out-trim-597，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人运行时／计划任务） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token收尾批
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916T-输出截流P4。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #597（`python 0-学习与工具/工具-队列查询.py --row 597 --section 一 --field all`）` → ② `CLAUDE.md`，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按 #597 期望产出 ⑴→⑸，⑴ 取证先落 reports/ 再动代码；输出精简只动成功路径，失败／警告／豁免信息全量保留。CLAUDE.md 改后 ``(Get-Item CLAUDE.md).Length`` ≤12288，超限撤回只留 opener骨架。
🔴 只动本行「触碰区」列所列文件；越界需求只登记不动手。不放宽任何锁／lint／登记／门禁判据。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出（全量回归、大范围 grep）交子代理只收结论。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short --maxfail=5`，跑完即 commit；差集回归与 master 同命令对跑。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916t-out-trim-597 --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916t-out-trim-597 --done --batch B-0916_Token收尾批 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用一条命令：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916T --section 一 --number 597 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；报错后锁仍占用则先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

### A3 · §一 #598 看护脚本P2

▶ 粘贴端：CC ｜ 泳道：op0916u-watch-wait-598

```
[OP-0916-U]【CC】看护脚本P2
【设置】执行环境：CC ｜ 分支：`claude/op0916u-watch-wait-598`（新建自 master） ｜ worktree：☑（op0916u-watch-wait-598，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人运行时／计划任务） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token收尾批
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916U-看护脚本P2。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #598（`python 0-学习与工具/工具-队列查询.py --row 598 --section 一 --field all`）` → ② `CLAUDE.md`，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按 #598 期望产出 ⑴→⑷；wait 工具只读，不改状态机判据；计步提醒挂在已注册的 context-meter 钩子内部，**不改** .claude/settings.json。若钩子文件被受保护路径拦截，把补丁写 reports/ 并在状态列点名，由 Cowork 应用。
🔴 只动本行「触碰区」列所列文件；越界需求只登记不动手。不放宽任何锁／lint／登记／门禁判据。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出（全量回归、大范围 grep）交子代理只收结论。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short --maxfail=5`，跑完即 commit；差集回归与 master 同命令对跑。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916u-watch-wait-598 --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916u-watch-wait-598 --done --batch B-0916_Token收尾批 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用一条命令：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916U --section 一 --number 598 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；报错后锁仍占用则先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

### A4 · §一 #599 巡逻无状态P6

▶ 粘贴端：CC ｜ 泳道：op0916v-patrol-stateless-599

```
[OP-0916-V]【CC】巡逻无状态P6
【设置】执行环境：CC ｜ 分支：`claude/op0916v-patrol-stateless-599`（新建自 master） ｜ worktree：☑（op0916v-patrol-stateless-599，收工自删） ｜ 工作区：仅 wecom-aibot-service 的 patrol_dispatch.py 与单测，不碰运行时 ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token收尾批
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916V-巡逻无状态P6。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #599（`python 0-学习与工具/工具-队列查询.py --row 599 --section 一 --field all`）` → ② `CLAUDE.md`，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按 #599 期望产出 ⑴→⑷；只改 patrol_dispatch.py 与其单测，**不重启服务、不停启计划任务、不向企微发任何消息**。
🔴 只动本行「触碰区」列所列文件；越界需求只登记不动手。不放宽任何锁／lint／登记／门禁判据。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出（全量回归、大范围 grep）交子代理只收结论。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short --maxfail=5`，跑完即 commit；差集回归与 master 同命令对跑。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916v-patrol-stateless-599 --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916v-patrol-stateless-599 --done --batch B-0916_Token收尾批 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用一条命令：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916V --section 一 --number 599 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；报错后锁仍占用则先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```


## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】Token收尾批-2026-09-16.md" -FullAuto -Yes -Detach
```