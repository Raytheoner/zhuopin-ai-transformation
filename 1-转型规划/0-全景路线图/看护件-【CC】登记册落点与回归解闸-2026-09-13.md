---
title: "看护件 · B-0913_登记册落点与回归解闸（两泳道，可并行）"
created: 2026-09-13
批次: B-0913_登记册落点与回归解闸
派出线: Cowork 环境总线 OP-0913-E
status: 待起
---

# 看护件 · B-0913_登记册落点与回归解闸

> 🔴 **起跑一律 `pwsh`、不传 `-LogDir`**（`#567`：`-Detach` 继承调用方 shell，写 `powershell` 必炸；纯时间戳批目录才进得了探针视野）。

## 一、本批是什么

两条泳道，**触碰面零交集，可并行**。一条把今天刚建成的机制的产物搬到能入库的地方，一条把卡住 WIP 的那条支的回归跑到底。

### A1 · 落点：把 ff 登记册搬出被忽略的目录

今天 `a6d37b5` 关掉了「巡检结论落盘」「登记册销行」两环，但**产物全落在 `reports/`**，而 `.gitignore` L50 `**/reports/` 整棵忽略、`git ls-files reports` 现取为 **0**。即：**🟡 人工 ff 授权原文的唯一载体不入版本控制**。

**这不是理论风险，今天已经真实发生过一次**：`reports/pending-ff.jsonl` 在 09:46:52 之后、09:55:53 之前整个文件消失，无归档、无留痕、七条实测无法归因（见 `1-转型规划/0-全景路线图/取证件-2026-09-13-ff登记册无痕消失-OP-0913-E.md`）。

**落点设计**（Shao Peishen 2026-09-13 答「第一环就按你的设计安排」授权，非本方推定）：迁到 `1-转型规划/0-全景路线图/合入登记/`，并在 `.gitignore` 的 `**/*.jsonl`（L32）**之后**加一条目录级否定 `!1-转型规划/0-全景路线图/合入登记/*.jsonl`。

判据三条，**均为临时仓实测，不是推断**：

1. **留在 `reports/` 点名放行行不通。** L50 是**目录级**忽略，git 根本不下探被排除的目录，否定式对它无效（实测：`**/reports/` ＋ `!reports/pending-ff.jsonl` 仍报忽略）。要让例外生效须把 L50 改成 `**/reports/*`，那等于让 git 下探每一个 `reports/`，爆炸半径远大于搬三类文件。
2. **搬出去 ＋ 目录级 glob 否定有效。** 实测 `**/*.jsonl` ＋ `!1-转型规划/0-全景路线图/合入登记/*.jsonl` 下，`pending-ff.jsonl` 与 `ff-patrol-20260913.jsonl` 两个**带日期的文件名**都可跟踪，而 `reports/noise.jsonl` 仍被忽略。
3. **仓库里已有同形先例，不是新造规矩。** `.gitignore` L38 `!6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl` 就是一条目录级 glob 例外。L37 的 D8 告诫一并适用：**例外不递归，该目录下不得建子目录**。

### A2 · 解闸：把 `op0913a` 的差分回归跑到底

`#570` 关不掉，因为它有一半是**故意押后**的——行内原文写着「留到该分支 ff 之后另派」，那条支是 `claude/op0913a-556-sweep-visibility`（`751495e`）。而它 ff 不了，**唯一原因是 ④ 差分回归从来没跑完**（曾被 600 s 上限掐死）。

链条：跑完回归 → 够格 ff → `#570` 剩半能补 → `#570` 能关 → 可动 WIP **23 → 22**。

**这条泳道是本批真正的价值**：它不产出新功能，它把闸打开。WIP 落到 22 之后，「临时 worktree 清理」和「合入前六条核验」（`#522` ⑧）才谈得上派。

## 二、硬边界

- 🔴 **A1 只改**：`.gitignore`、`0-学习与工具/工具-合入链路留痕.ps1`、`0-学习与工具/工具-待合分支巡检.ps1`、`0-学习与工具/工具-泳道分支合入.ps1` 及其单测；外加迁移 `reports/` 下现有的 ff 类 jsonl（`pending-ff.done-*`、`pending-ff.parked-*`、`ff-patrol-2026*.jsonl`）。
- 🔴 **A1 不得动 `reports/` 下其余任何文件**——`sweep-*.json`、`hooks-audit.jsonl`、`cells*.json` 全是运行时件，必须继续被忽略。判别一句：**只有「正本」搬家，运行时件原地不动。**
- 🔴 **A1 迁移必须保住内容**：迁完逐字回读比对，任何一个文件的字节数或行数对不上即以 `OPENER_PARTIAL` 停手，**绝不「重建一份」**——那些文件里有他的授权原文。
- 🔴 **A2 只跑回归，绝不 ff、绝不改 master、绝不碰 A1 的四个文件。**
- 🔴 **A2 只 rebase 一次**：把 onto 的 master sha 记进报告；跑完不管 master 是否又前进，**都不再 rebase**。理由＝今天上午 `op0913f` 连 rebase 四次未合一次的空转，成因正是回归时长逼近调度周期。
- 🔴 **A1 不碰 `0-学习与工具/工具-落库sweep.py`**（那是 A2 分支上的 +194 行未合内容）；**A2 不碰 A1 的四个文件**。两条泳道触碰面必须零交集。
- 🔴 长回归别丢后台等事件——分步跑、每步落日志，`op0913a` 正是那样被 600 s 上限掐死过一次。
- 🔴 零回归按**差集口径**（分支 vs 纯 master detached worktree 同命令、`comm` 双向），**不是全绿**。
- 🔴 不 ff master；不碰 `.51`／企微／定时任务运行时。

## 三、泳道

### A1 · 登记册落点迁移（承接 `#571`，WIP 不增）

▶ 粘贴端：CC ｜ 泳道：op0913l-ledger-relocate-571

```
[OP-0913-L]【CC】登记册落点迁移
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913l-ledger-relocate-571`） ｜ worktree：☑（op0913l-ledger-relocate-571，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_登记册落点与回归解闸
读 ① `1-转型规划/0-全景路线图/看护件-【CC】登记册落点与回归解闸-2026-09-13.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913l-ledger-relocate-571 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913l-ledger-relocate-571 --done --batch B-0913_登记册落点与回归解闸 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · op0913a 差分回归跑到底（承接 `#556`，WIP 不增）

▶ 粘贴端：CC ｜ 泳道：op0913m-a556-regression-556

```
[OP-0913-M]【CC】op0913a回归跑到底
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913m-a556-regression-556`） ｜ worktree：☑（op0913m-a556-regression-556，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_登记册落点与回归解闸
读 ① `1-转型规划/0-全景路线图/看护件-【CC】登记册落点与回归解闸-2026-09-13.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913m-a556-regression-556 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913m-a556-regression-556 --done --batch B-0913_登记册落点与回归解闸 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词（只在退回「人在环」时才粘；默认无头）

```
[OP-0913-N]【CC】看护看护落点与解闸
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_登记册落点与回归解闸
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0913N-看护看护落点与解闸。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】登记册落点与回归解闸-2026-09-13.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、起跑

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】登记册落点与回归解闸-2026-09-13.md" -FullAuto -Yes -Detach
```
