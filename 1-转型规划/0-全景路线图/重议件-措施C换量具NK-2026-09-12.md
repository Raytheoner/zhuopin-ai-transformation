---
title: "重议件 · 措施 C 换量具 N/K —— 附本方两处断言更正"
created: 2026-09-12
status: 待审（design 审前置件）
承接: `方案-措施C换量具-陈旧度闸-2026-09-11.md` D5；队列 §四 `#58`
授权: Shao Peishen 2026-09-12 回 `1a`（「D5 已触发，现在就回本件重议 N/K」）
执行环境: 【Cowork】起草，不改任何闸
---

# 重议件 · 措施 C 换量具 N/K

## 〇、结论先行

🔴 **N/K 不改**。本件真正的产出是**两处更正**——触发本次重议的那个理由，经复核**不成立**。

## 一、两处更正（本方上一条报告说过头了，当场标正）

### 更正① · D5 **字面未触发**

D5 原文：「若 `WIP豁免：` **在新闸下** 30 天内再破 5 条 ⇒ 回本件重议 N/K」。
**新闸（陈旧度闸）仍在告警期、从未阻断过一次**——手段＝`OP-0912-I`（2026-09-12）实测记录：首日 6 次 `release` 回显 `STALE(7)=2` 恒定，**零次阻断**；今天全部阻断均来自**行数闸**（`release` 拒绝原文写的是「机制类可动 WIP 当前 32／22，已超上限」）。
⇒ 新增的豁免发生在**旧闸**下，**不满足 D5 的「在新闸下」这个条件**。本方 2026-09-12 报告里写的「D5 已触发、首日就破了」**是过头，此处更正**。

### 更正② · `+7` 的性质被说反了

手段＝按结构位取 §一 每行第 6 格（状态列）含 `WIP豁免：` 者，**非全文 grep**（`#58` 记过「全文 grep 得 10、按结构位取得 1」的计数陷阱）。2026-09-11 方案同法实测 15 条，2026-09-12 现取 **22 条**，新增 7 条逐条分类：

| 行 | 性质 | 豁免理由摘句（原文） |
|---|---|---|
| `#556` | **真出血** | 生产中继连续失败 11 天，且失败表象与「无待发消息」不可区分，会静默吞掉 SC2／FI2 周报 |
| `#559` | **真出血** | 审计完整性：人工门禁决策落进无人查的文件 |
| `#563` | **真出血** | 直接伤到与真人的往来：专员答了、我方判「未作答」，于是又去问一遍 |
| `#565` | **真出血** | 看门狗与 D6 收工汇总集体失效 |
| `#566` | **真出血** | 不可撤回的对外事故已真实发生过一次 |
| `#567` | **真出血** | 无头链 `-Detach` 在 PS 5.1 必炸，挡着当天刚生效的「默认后台起」口径 |
| `#381` | **假豁免** | 🛑 起首、本就不计入 WIP（缺陷 ⑸ 那一族的历史残留，D3⑵ 已修判据但行内文本未追改） |

⇒ **6/7 是真例外，1/7 是历史假豁免；「因超限而被逼写」的新增为 0 条。**
本方上一条把 `+7` 当成「闸在乱造豁免、说明阈值定错」，**是把『今天出血多』误读成『闸坏了』，此处更正**。

## 二、那么这一天真正证明了什么

1. **判据①（`#58` 自书：豁免批量出现 ⇒ 上限定错）这次并没有被加强**：扣掉 6 条真出血后，增量为 0。原先 0→15 那次的结论不受影响，但今天不构成新证据。
2. **真正值得记的是另一件事**：一天之内立了 6 条出血行，其中 **4 条（`#563`／`#565`／`#566`／`#567`）属同一族**——「规则活在正本文本里、没有机器在出件时校验」。**这是规则落地机制的问题，不是 WIP 的问题**，不该由措施 C 来背。

## 三、N/K 的裁定

🔴 **维持 `N=7`／`K=3`，不改。** 三条理由，均附手段：
1. N/K 是用**真实 commit 时间**（`git blame --incremental` 取 author-time）定死的，不是推荐值——方案 D2 已自证「不实测就写死这次就会错」；
2. 今天 `STALE(7)=2`，**仍在 `K=3` 之内**，新闸**一次误伤都没有**（`OP-0912-I` 首日 6 次回显全部 `STALE=2` 恒定）；
3. **没有任何一条实测指向 N/K 定错**。改一个没被证伪的参数，等于把方案 §五「不第三次为迁就现状改口径」的错重演一遍，只是换了个参数名。

## 四、该改的是这两处（成本都极低）

- ⑴ **剩余告警期内，新增机制行优先用 🛑 起首或 `blocked`／`timed=`，不用 `WIP豁免：`**——两者都是结构性排除、不计入 WIP，而 `WIP豁免：` 会污染 D5 的计数。2026-09-12 `#568` 已按此做（`release` 回显「不计入机制类可动 WIP…不要求写豁免标记」）。**这是一行口径，不是新机制行。**
- ⑵ `#381` 那条假豁免**随下次触碰该行时顺手删**，不专门开棒、不追改其它历史豁免（同「历史记录不追改」）。

## 五、给 5.1 复评留的话

`OP-0912-I` 已把 D4 的三条前置做完：**4.2** 榜首 3 条人工抽查已做；**4.3** 非主 checkout（worktree）真实取龄 **11.0 s**（< 30 s，**不必回 design D-E 议超时或缓存**）；**4.4** 三个数已记进 `队列行日志/#58.md`。
⇒ **5.1 只剩第 ① 条**「这 7 天里新闸的告警有没有被任何人当回事」。截至今日的答案是**有**：本会话因它两次改写了行的形态（`#568` 改 🛑、本件确立口径 ⑴）。**但这只是 7 天里的 1 天，不得据此提前宣告通过。**

## 六、不做什么

- **不改 N/K**（本件结论）。
- **不第三次抬行数上限**（`#58` 自书此路已封死）。
- **不退休措施 C**（会失去唯一一台强制回头看的机器）。
- **不提前切阻断、也不提前关行数闸**——5.1 的第 ① 条只有 1 天数据，提前切就是 5.1 明禁的「默认续期」的镜像。
- **不追改存量 22 条 `WIP豁免：`**（历史记录不追改）。
