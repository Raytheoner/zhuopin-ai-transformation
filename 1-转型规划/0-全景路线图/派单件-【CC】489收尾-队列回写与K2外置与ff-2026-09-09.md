---
title: "派单件 ·【CC】#489 收尾：队列回写 ＋ K2 外置 ＋ ff master（OP-0909-A）"
created: 2026-09-09
status: 待粘贴
派出线: CC OP-0908-L（自派收尾件）
承接: 队列 §一 #489 ／ §二 批次 B-0908_489判据正本声明式与全量提速
---

# 派单件 ·【CC】#489 收尾（三件，A 类）

## 〇、为什么有这份件（先读，决定你怎么核对现状）

`OP-0908-L` 已把 `#489` 派单件的六步**全部做完并推送**，但收尾的三件事在该会话里
**做不成**——CC 侧 auto mode 的安全检查拦下了「经 Bash 跑 python」这一类动作
（提示原文：不是动作本身不安全，拦的是该会话上下文，且该会话内会持续触发，
并明确要求不要换入口绕）。`git`／`ls`／写文件不受影响，故代码与本件都已落到分支上。

🔴 **因此队列现在与事实不符，这是你开工第一件要修的**：
§一 `#489` 状态列末尾仍写着「🟡 两处骨架正文改动未做、等 Shao Peishen 一字母」
——**已过期**，Shao Peishen 2026-09-09 已答 1a，两处骨架改动已做完并推送（`7cb578a`）。

🔴 **临期**：`#489` 状态列现约 8 KB，超 K2 的 4096 B 上限，**2026-09-11 起由告警转阻断**
（届时任何触碰本行的会话 release 会被拒）。本件第 2 件就是拆这颗雷。

## 一、现状（开工前用只读命令核一遍，不要凭本文件相信）

- 分支 `claude/op0908l-opener-lint-489-ef8652`，两笔提交，均已 push、**未 ff master**：
  - `2613969` 工具三处修（判据正本声明式 ／ 全量提速 ／ 明细漏打三形态）
  - `7cb578a` 骨架两处（§三bis 样例补例外句 ／ 引用版禁手抄纪律行）
- §一 `#461` 已有 `--variant reference` 增量指针，`[S:done]` 未动 —— **不要再动它**。
- §二 批次 `B-0908_489判据正本声明式与全量提速` 已登记、状态「待」，
  文件清单只含 `1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md`。
  🔴 **本件（派单件）与两笔代码提交都在分支上、不经 sweep**，不要往该批次里加它们。
- `1-转型规划/0-全景路线图/队列行日志/#489.md` **不存在**（外置未执行，无半成品）。

核对命令（只读）：

    git log --oneline -3 origin/claude/op0908l-opener-lint-489-ef8652
    python 0-学习与工具/工具-队列查询.py --row 489 --section 一 --field status
    ls "1-转型规划/0-全景路线图/队列行日志/" | grep 489

## 二、做什么（0＋三件，按序；第 1、2 件在同一把锁内完成）

### 0. 先把本件提交掉（否则它随时会随 worktree 一起消失）

    git add "1-转型规划/0-全景路线图/派单件-【CC】489收尾-队列回写与K2外置与ff-2026-09-09.md"
    git commit -m "docs(队列#489): 收尾派单件（OP-0908-L 自派，落盘时 commit 被拦，本棒补提交）"
    git push

🔴 **做完这一步才动别的** —— 在此之前本件是未跟踪文件，清 worktree 即丢。

### 1＋2. 队列回写 ＋ K2 外置（一次锁窗口内做完，不要分两次）

🔴 **不要先 append 再重写**——那会先把行撑到 8 KB 再砍回来，中途留一个超限快照。
正确顺序：**先把外置件写出来，再用 `edit-row --set` 一次性整格重写。**

**步骤 2.1**：取整格原文（只走 K3 认可的读侧入口，不 Read／grep 队列真身）：

    python 0-学习与工具/工具-队列查询.py --row 489 --section 一 --field status

输出第 3 行即整格原文。算它的 md5（`hashlib.md5(cell.encode("utf-8"))`）。

**步骤 2.2**：写 `1-转型规划/0-全景路线图/队列行日志/#489.md`，
frontmatter 与正文格式**照抄同目录 `#439.md`**（该件是 K2 外置的既有范例）：
frontmatter 六项（title／created／status／用途／外置会话／原行 md5），
正文一级标题写「# §一 #489 · 外置前整格原文（2026-09-09 外置，<N> B，md5:<前8位>）」，
其下**原文原样贴整格**，一个字节不改。
🔴 **写主仓权威路径**，不是 worktree 影子副本——行内指针要指向 master 里真实存在的文件。

**步骤 2.3**：`acquire` 后用 `edit-row --changes-json`（🔴 **必须走 `--changes-json`**：
本格数千字且密集使用反引号，`--set` 走 argv 会被反引号奇偶校验拦）把 §一 `#489`
状态列**整格替换**为下列内容。⚠️ 贴之前自检：反引号总数为偶数、格内无裸竖线。

```
[S:open][D:机] 🟢 **可领（2026-09-08 20:2x 去 🛑：Cowork 环境总线 `OP-0907-AL`，Shao Peishen 当日「两处工具缺陷尽快修复」；派单件 `1-转型规划/0-全景路线图/派单件-【CC】489-opener块lint两处同触碰修-2026-09-08.md`，`OP-0908-L`）** ━━━ ✅ **2026-09-08/09 CC `OP-0908-L` 六步全部交付完毕，仅待 ff master。** 分支 `claude/op0908l-opener-lint-489-ef8652`：`2613969` 工具三处修 ＋ `7cb578a` 骨架两处，均已 push。 ⑴ **判据正本识别由「写死路径」改为「件自己 frontmatter 声明角色」**（`opener正本: 骨架／模板库`）——`#493` 只挖了骨架一条路径，模板库不在名单里故恒报 13 处；按路径续加即是在建本行明令禁止的文件名清单。骨架角色沿用 `#493` 口径；模板库角色换掉 F3/F4/F5 三条**版式**判据（依据＝根 `CLAUDE.md` §3「不凭模板库重建（模板库 §〇 是判据、不是格式）」，其块是内容草稿非版式范例），🔴 **F1/F2 刻意不换**（它俩守的正是 2026-08-27 一天 17 次违反的源头）。防外溢＝新判据 `C0`：声明了角色却只有 <2 个 opener 块、或角色值写错 ⇒ **声明不予承认、按普通件全判**并报 `C0`（实测冒充件仍照报 F3/F5）。release 侧只调用不重写（D3），自动覆盖到模板库。**实测骨架 0→0、模板库 13→0，近三份看护件 exit 0 不变。** ⑵ **全量 `--enforce` 实测是两件事**：① 89% 墙钟在逐文件 `git log`——1613 个文件实测 **1057.3s**，纯解析仅 19.4s ⇒ 改一次全历史 `--name-only` 扫描，实测 **2.76s**，且**与逐文件口径 1613/1613 逐个比对完全一致**（非抽样）；② **0 字节 ≠ 挂死**——输出此前全攒到最后一次性打印 ⇒ 进度改写 stderr 并 flush。另加单次 git 60s 超时。**耗时 157s → 4~17s。** ⑶ 🆕 **顺带修出第三处**：`main()` 明细分组循环写死的形态清单**漏了 F3/F4/F5**，致其计入「N 处待修【阻断】」却一行明细不打印（实测声称 49、逐条只数得出 35）⇒ 取消第二处清单、直接遍历 `FORM_TITLE` 并加兜底打印；改后声称数与明细条数逐条相等，且**真实件命中一条未少**、另浮出 1 处此前看不见的 F5。 ⑷ `工具-opener生成.py` 加 `--variant reference`（并入 `#461`，见该行指针）。 ⑸ **骨架两处**（Shao Peishen 2026-09-09 答 1a）：§三bis 样例句改成与生成器 `--variant guardian` 输出逐字同源（**反向对照实测**：补句后照抄不再报 F2，去掉该句 F2 立刻回来）；「状态标记三式」节下加引用版禁手抄纪律行。 **验收：三套单测 603 passed／1 skipped，骨架＋模板库＋近三份看护件 `--file` 全 exit 0。** 🔴 **本行仍 `[S:open]`、不销号**：产出全在未合入分支上，此时标 done 会让后续扫池误判已落地；**销号条件＝上述两笔 ff 进 master**。 ━━━ 📎 **本行历史回写段（🛑 期原文、软序门禁复核实证、两处缺陷立项实证）已按 K2 外置**（原文原样，md5:`<填入>`）见 `1-转型规划/0-全景路线图/队列行日志/#489.md`；本格只留首段＋交付结论＋本指针。
```

**步骤 2.4**：`release` 前回读核实 —— `工具-队列查询.py --row 489 --field status`
确认整格已换、且**不再报「超 4096 B 上限」**。
本次改动落 §二 批次 `B-0908_489判据正本声明式与全量提速`（已存在，文件清单需补上
`1-转型规划/0-全景路线图/队列行日志/#489.md`）。

### 3. ff master

    git fetch origin
    git merge --ff-only origin/claude/op0908l-opener-lint-489-ef8652

ff 后 `#489` 方可销号（`edit-row` 把首段 `[S:open]` 翻成 `[S:done]`，
🔴 翻转 `[S:xxx]` 前缀必须整格重写、只能走 `--changes-json`）。
⚠️ ff 若撞未提交的同文件改动：存补丁 → `checkout` 还原 → ff → `apply --3way`，
**不要用共享的 stash 栈**。

## 三、不做什么

- **不动 §一 `#461`**（`[S:done]`，其增量指针已写好）。
- **不改 lint 的六个失效形态判据本身**，本件只做回写／外置／ff 三件事。
- **不把两笔代码提交或本派单件登进 §二**——它们在分支上、不经 sweep。
- **不手抄 opener**：若需再派下一棒，一律 `工具-opener生成.py --variant reference`
  输出原样贴入（骨架「状态标记三式」节下新纪律，本批刚落地）。

## 四、起棒设置（🔴 复用既有 worktree，不新建）

**Shao Peishen 2026-09-09 答 1a**：新 session **进既有 worktree 执行**，先把本件
`git add` + commit + push，再做 §二 三件事。

    执行环境：CC
    分支：claude/op0908l-opener-lint-489-ef8652（沿用，不新起分支）
    worktree：☑ 复用既有 `.claude/worktrees/op0908l-opener-lint-489-ef8652`，🔴 **不新建、收工不自删**
    工作区：无（纯库内，不触碰 .51／企微机器人／定时任务）
    session：新开
    派出线：CC OP-0908-L（自派收尾件）

🔴 **为什么必须复用**：本件在 `OP-0908-L` 会话里**只落了盘、没提交**（`git add` 当时
已被 auto mode 拦下），是一个**未跟踪文件**，只存在于那个 worktree 里。新建 worktree
从 master 拉，看不到它。⇒ **在它被 commit 之前，那个 worktree 不能清。**

🔴 **三件事各自的执行位置不同，别混**：
- 第 1＋2 件（队列回写／K2 外置）：编辑锁的 `REPO_ROOT` 恒解析到**主仓**，
  在 worktree 里跑命令即可，但 `队列行日志/#489.md` 要**写主仓权威路径**，
  不要写成 worktree 影子副本。
- 第 3 件（ff master）：**必须在主 checkout 里做**，worktree 里 ff 不到 master。

## 五、若还要再派下一棒

🔴 **不手抄 opener**（骨架「状态标记三式」节新纪律，本批刚落地）。跑生成器出引用版：

    python 0-学习与工具/工具-opener生成.py --variant reference --env CC \
      --op-id OP-0909-A --short-name 489收尾 \
      --branch queue-489-closeout \
      --worktree "☑（复用 op0908l-opener-lint-489-ef8652，不新建）" \
      --workspace 无 --session 新开 --line "CC OP-0908-L" --task-class A \
      --ref-file "1-转型规划/0-全景路线图/派单件-【CC】489收尾-队列回写与K2外置与ff-2026-09-09.md"

🔴 `--op-id` 先扫当日已用编号再取（生成器内置撞号查重，撞了会直接报错并给出空号）。
🔴 `--branch` 传的是短横线 slug，生成器会自己拼成 `master（从 master 起 claude/op0909a-…）`
——本棒实际沿用既有分支，故该行仅在「真的另起一棒」时才用。
