---
title: "看护件 · 泳道看护批 B-0905_B（第十一批：WIP 分诊后能开尽开——7 条泳道，含 3 分支合并/2 全建造/3 openspec 起草，#456 design 已现场批准并入建造）"
created: 2026-09-05
批次: B-0905_B
派出线: Cowork 业务总线 OP-0905-C
前批: B-0904_K（3 泳道，2026-09-04 立，2026-09-05 00:35 UTC 确认闭环——见队列 #459/#460/#461 本次回写）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0905_B`

## 〇、本批口径

**立批依据**：Shao Peishen 2026-09-05 问「WIP 现状能否收口占位／能开尽开」——本线先做了 WIP 分诊（§一 `#324`→`blocked`、`#460`→`blocked`、`#461`→`done`，机制类可动 WIP 由 23/22 降至 20/22，详见各行回写），**本批接着答「能开尽开」那半句**：扫存量 20 行可动 WIP，挑出 6 条**今天就能真实推进**的，一次起批。

🔴 **本批口径**：**能建到底的建到底，能起草判据的起草到 design 审为止，能合并的先只读复核+隔离 worktree 合并**——三种深度并存，泳道 opener 里各自写清止步点，不因为「尽量多开」就把该停的地方也推过去。

📌 **§三 A7（`#456`）为本批起草后追加**：Shao Peishen 2026-09-05 现场当场批准 `#456` design 审（proposal 见 `dc24cb3`），故由「随 `rebase3` 只落 proposal 文档」升格为「建到底」，与其余六条一并起。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界

1. 🔴 **触碰区实测重叠、必须串行**：`0-学习与工具/工具-落库sweep.py` 与其测试文件被 `rebase3`（合并 `editable-import-guard-0902`）与 `459-382b-impl` 两条泳道同时碰——**`459-382b-impl` 必须等 `rebase3` 的 A1 段（`editable-import-guard-0902` 合并）完成后再开工**，不得与 `rebase3` 并行起跑（见 §二 波次）。
2. 🔴 **`rebase3` 只做「隔离 worktree 内 rebase + 跑测试 + 只读 `merge-tree` 判冲突」，真正 `ff` 进 master 前必须停下**（命中 `merge_to_master`，🟡）——三条分支一次性列给 Shao Peishen 一并确认，不擅自合。这三条分支已实测**只新增文件、未改任何既有生产逻辑**（`followup-meta-0902` 新增工具脚本+3份文档；`editable-import-guard-0902` 只给既有测试文件加用例；`lint-queue-restate-pkg-0902` 只新增 openspec 目录），风险低，但仍守 D1 判据不降格自行合并。
3. 🔴 **`448-design`／`455-design`／`341-design` 三条泳道止步于「openspec proposal+design 起草」**——命中各自行内自陈的 `openspec design 审` 门槛，**起草完即停**（🟡 `openspec_design_review`），不直接改产线代码。
4. 🔴 **`446-formread` 与 `459-382b-impl` 全建造到底，但落地范围各自的行内「不做什么」为界**——`446` 不改巡逻章程本体（只出改动建议交总线落）；`459` 的实现以 `claude/queue-410-editable-probe`@`330218c` 为蓝本 rebase，不重新实现；`382`(B) 只动 `工具-落库sweep.py` 起跑段，不摘章程步骤（one-in-one-out 摘除动作由总线做）。
5. 🔴 真实库只读起步；不部署 `.51`；不发任何对外消息；不碰 `#374`／oem-chroma 族、不碰 `3-治理与合规/OEM数据隔离规范.md`（另一并发 session 在办中）。
6. 🔴 **不改队列文件里已有的历史回写段**（K2 口径，原文原样）——只在触及的行末追加本次结论。

---

## 二、波次与触碰区矩阵

| 波 | 泳道 | 段 | 承接 | 触碰区 | 依赖 |
|:--:|---|:--:|---|---|---|
| 1 | `rebase3` | A1→A2→A3 | 合并 3 条既有分支：`editable-import-guard-0902`(#410 补测)→`lint-queue-restate-pkg-0902`(#456 proposal)→`followup-meta-0902`(#447 泳道B) | `0-学习与工具/工具-落库sweep.py` 测试文件、新工具脚本、openspec 新目录、3 份新文档 | 无 |
| 1 | `446-formread` | A1 | #446 回件形态识别自动化（建造到底＋8封历史回归） | `5-平台底座/wecom-aibot-service/aibot_service/`（新模块） | 无 |
| 1 | `448-design` | A1 | #448 队列行阻塞源 `[B:]` 字段——openspec 起草到 design 审为止 | `openspec/changes/queue-row-blocker-field/`（新） | 无 |
| 1 | `455-design` | A1 | #455 `edit-row`/`append-row` 写侧三缺陷合并处置——openspec 起草到 design 审为止 | `openspec/changes/editlock-write-guard-hardening/`（新） | 无 |
| 1 | `341-design` | A1 | #341 队列写侧按域路由＋高水位线跨文件同步——openspec 起草到 design 审为止 | `openspec/changes/queue-domain-routing/`（新） | 无 |
| 1 | `456-lint-impl` | A1 | #456 队列结构 lint 扩「禁复述 openspec 包进度」告警——**design 已获现场批准，建到底**（`git show` 只读取设计分支，不必等 `rebase3` 合并） | `0-学习与工具/工具-队列结构lint.py`、其测试文件 | 无 |
| 2 | `459-382b-impl` | A1→A2 | #459 pth 盲区判据实现（以 `330218c` 为蓝本 rebase）→ #382(B) 看门狗下放机器 | `0-学习与工具/工具-落库sweep.py`、其测试文件 | **等 `rebase3` A1（`editable-import-guard-0902` 合并）完成** |

并行判据：波 1 六条泳道触碰区两两不重叠（`rebase3` 的落库sweep测试文件改动是 A1 段独立合并、完成后即释放），可同时起跑；波 2 只有 `459-382b-impl` 一条，等波 1 的 `rebase3`-A1 收到"已合并"信号后再启动（不必等 A2/A3）。

---

## 三、泳道 opener

### A1 · rebase3——三条既有分支只读复核＋隔离 worktree 合并（止步于 merge_to_master）

粘贴端：CC ｜ 泳道：rebase3

```
[OP-0905-D]【CC】三分支合并复核
【设置】执行环境：CC ｜ 分支：master（三条既有分支各自在独立 worktree 内 rebase，不新开分支）｜ worktree：☑（`rebase3-followup-meta`／`rebase3-editable-guard`／`rebase3-lint-restate`，均新 worktree，逐条用完即删）｜ 工作区：无（纯库内只读复核＋隔离合并，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
（本泳道由看护者用 Task/Agent 派发执行，不设 session 标题——源头不放，见 §一 `#487`／(甲)，2026-09-05 Shao Peishen 现场拍板）。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0905_B-2026-09-05.md 的 §〇＋§一（硬边界六条，尤其②）＋ §三 A1 本节 ＋ CLAUDE.md 恢复上下文。本件为 A 类（范围与顺序已写死），无需再问澄清，直接开工。

背景（已核实，直接信任，不必重查）：三条分支均落后 master 165 个提交、均只新增文件未改现有逻辑——`claude/editable-import-guard-0902`@`58db0ad`（只给 `test_工具-落库sweep.py` 加用例）、`claude/lint-queue-restate-pkg-0902`@`dc24cb3`（#456 的 openspec proposal，纯新增目录）、`claude/followup-meta-0902`@`0c3ef14`（#447 泳道B，新增 `工具-跟进信frontmatter校验.py`＋3份文档）。Shao Peishen 已在 §四 `#452` 行内给过合入的原则性授权，本次只需走一遍安全校验并让他一个字母收口。

做什么（三条严格按此顺序，每条独立 worktree，前一条完全结束——含 ff 或明确转出——才起下一条）：
1. **`editable-import-guard-0902`**：`git worktree add .claude/worktrees/rebase3-editable-guard claude/editable-import-guard-0902` → 该 worktree 内 `git rebase origin/master`（预期零冲突，纯新增测试）→ `python -m pytest 0-学习与工具/test_工具-落库sweep.py -q` 全绿 → 回主工作区只读 `git merge-tree $(git merge-base master <rebase后分支>) master <rebase后分支>` 确认无冲突标记 → **停，不 ff**，把结论（rebase 后 commit 哈希、测试结果、merge-tree 结论）写进本轮汇总。
2. **`lint-queue-restate-pkg-0902`**：同样流程（`openspec validate --strict` 替代 pytest，因该分支只有 openspec 文档）。
3. **`followup-meta-0902`**：同样流程；该分支自身提交信息写着"停在 🟡 change_criteria"——**只读一遍该分支新增的 3 份文档，摘出那个未决的 change_criteria 问题是什么，一并写进汇总供 Shao Peishen 参考**，不要求你替他判断。
4. 三条都跑完只读复核后，**在心跳文件写一条清晰的三选项汇总**（哪三条分支、各自 rebase 后哈希、测试/validate 结果、有无冲突），走 `pause --action-key merge_to_master --waiting-for "三条分支只读复核已完成、均无冲突/测试全绿，是否一次性 ff 全部三条进 master"`。

不做什么：
- 🔴 不执行任何一条分支的 `git merge --ff-only` 落地动作——只 rebase＋测试＋只读比对。
- 🔴 不删除、不重命名任何一个 worktree 里的已有分支引用。
- 🔴 不碰 `followup-meta-0902` 文档里那个「change_criteria」判据本身——只转述，不代答。
- 🔴 不碰 oem-chroma 三个批次、不碰 `OEM数据隔离规范.md`。
```

### A2 · 446-formread——回件形态识别自动化（建造到底）

粘贴端：CC ｜ 泳道：446-formread

```
[OP-0905-E]【CC】回件形态识别
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905e-formread-446`）｜ worktree：☑（`formread-446`，新 worktree，收工自删）｜ 工作区：无（纯库内建造＋离线回归，不部署、不触碰 `.51`、不发任何消息）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
（本泳道由看护者用 Task/Agent 派发执行，不设 session 标题——源头不放，见 §一 `#487`／(甲)，2026-09-05 Shao Peishen 现场拍板）。
读 ①〈队列 §一 `#446`〉（`工具-队列查询.py --row 446 --field all`）→ ② `0-学习与工具/md转Word工具/md2word.py` 的 `read_checkboxes()`（约 L85）→ ③ CLAUDE.md 恢复上下文，按 `#446` 行内「做什么（三步，止于此）」执行。本件为 A 类（openspec 判据已过、无需 design 审，行内已写死范围），无需再问澄清，直接开工。

做什么：
1. 形态识别模块：`zipfile` 直读 `.docx` 部件清单，数 `w14:checkbox` 勾了几个哪几个／`w:ins`·`w:del`／`word/comments*`／`w:highlight`，落在 `5-平台底座/wecom-aibot-service/aibot_service/` 下新模块（命名自定，遵项目既有风格）。
2. 按形态提原文片段：逐格 ☒☐ ／高亮段全文／批注内容，结构化输出。
3. 离线回归：对 8 封已归档回件（`财务部#14`/`#15`、`IT部#9`/`#10`、`质量部#9`/`#10`、`采购部#18`/`#19`/`#20`，见 `7-外部文档/<部门>/`）跑一遍，逐封列「机器读出的形态 vs 人当时最终结论」，对不上即视为 bug 修到对上为止。
4. 产出一份「巡逻章程 §二 改动建议」文档（不改章程本体——章程改动交总线做），说明这一步该接在巡逻流程的哪个位置。
5. 补单测覆盖新模块。

不做什么：
- 🔴 不修改巡逻章程 `C:\Users\Paul Shao\Claude\Scheduled\huijian-chaijian-patrol\SKILL.md` 本体——只出建议文档。
- 🔴 不做语义判断与队列回灌自动化——形态识别到「结构化产出」为止，人工判断仍留人。
- 🔴 不碰 `queue_appender.py` 的触发逻辑（那是 `#382`⑴，已完成，不在本泳道范围）。
```

### A3 · 448-design——队列行阻塞源 `[B:]` 字段（起草到 design 审为止）

粘贴端：CC ｜ 泳道：448-design

```
[OP-0905-F]【CC】阻塞源字段设计
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905f-block-source-448`）｜ worktree：☑（`block-source-448`，新 worktree，收工自删）｜ 工作区：无（纯 openspec 起草，不动产线代码）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
（本泳道由看护者用 Task/Agent 派发执行，不设 session 标题——源头不放，见 §一 `#487`／(甲)，2026-09-05 Shao Peishen 现场拍板）。
读 ①〈队列 §一 `#448`〉（`--row 448 --field all`）→ ②`1-转型规划/0-全景路线图/设计-队列行阻塞源结构化字段-2026-09-01.md`（正本，含 §2 失败实验与 §5 两个待 design 审开放点）→ ③ CLAUDE.md 恢复上下文。本件为 B 类——§5 两个开放点需要你在 design.md 里给出推荐方案与理由，不必等我，写清楚推荐项与备选即可，止步于起草不代拍板。

做什么：
1. 起草 openspec 变更包（proposal.md／design.md／tasks.md／spec delta），命中门槛＝「改变既有模块对外语义」（`_suggest_status_reclassification`／`_count_mechanism_wip`／`append-row` 校验行为改变）。
2. design.md 须覆盖行内两个失败实验教训（改读领取方列会连坐打坏 `#96`；判据不该建立在自由文本上）、`[B:none｜person｜env｜external｜split]` 五态定义、`[B:split]` 拆行语义、存量补标范围（约 45 机制行＋23 业务行）。
3. `openspec validate --strict` 跑绿。
4. 收工即停——`pause --action-key openspec_design_review --waiting-for "design 草案已出，等你审"`，不进入实现。

不做什么：
- 🔴 不改 `0-学习与工具/工具-共享文档编辑锁.py` 一行代码——本泳道止于设计文档。
- 🔴 不对存量队列行做任何 `[B:]` 补标——那是 design 审通过后的实现阶段。
```

### A4 · 455-design——`edit-row`/`append-row` 写侧三缺陷合并处置（起草到 design 审为止）

粘贴端：CC ｜ 泳道：455-design

```
[OP-0905-G]【CC】写侧守卫加固设计
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905g-editrow-guard-455`）｜ worktree：☑（`editrow-guard-455`，新 worktree，收工自删）｜ 工作区：无（纯 openspec 起草，不动产线代码）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
（本泳道由看护者用 Task/Agent 派发执行，不设 session 标题——源头不放，见 §一 `#487`／(甲)，2026-09-05 Shao Peishen 现场拍板）。
读 ①〈队列 §一 `#455`〉（`--row 455 --field all`）→ ②实证行 `#324`（未闭合⑵：竖线守卫非反引号感知致状态字段锁死）与 `#454`（反引号奇偶未校验致行塌列）→ ③ CLAUDE.md 恢复上下文。本件为 B 类——三处缺陷的具体校验实现细节需要你判断，止步于起草不代拍板。

做什么：
1. 起草 openspec 变更包，覆盖三处缺陷合并处置：⑴ 反引号奇偶校验；⑵ 写入后列数回读校验（不符即回滚该次写入）；⑶ 已塌列行的 `--repair` 逃生入口。
2. design.md 引用 Shao Peishen 2026-09-02 给出的元判据「如果能做，就不要指派手工——会带入失误」，说明为何这三条都该机守而非人工复查。
3. `openspec validate --strict` 跑绿。
4. 收工即停——`pause --action-key openspec_design_review --waiting-for "design 草案已出，等你审"`。

不做什么：
- 🔴 不改 `0-学习与工具/工具-共享文档编辑锁.py` 一行代码。
- 🔴 不尝试"顺手"修复 `#324`／`#454` 已经手工绕开的具体那两行——本泳道只治根因判据，不追改历史行。
```

### A5 · 341-design——队列写侧按域路由＋高水位线跨文件同步（起草到 design 审为止）

粘贴端：CC ｜ 泳道：341-design

```
[OP-0905-H]【CC】域路由设计
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905h-domain-route-341`）｜ worktree：☑（`domain-route-341`，新 worktree，收工自删）｜ 工作区：无（纯 openspec 起草，不动产线代码）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
（本泳道由看护者用 Task/Agent 派发执行，不设 session 标题——源头不放，见 §一 `#487`／(甲)，2026-09-05 Shao Peishen 现场拍板）。
读 ①〈队列 §一 `#341`〉（`--row 341 --field all`）→ ②`openspec/changes/archive/2026-08-17-queue-dual-file-split/tasks.md` §2.4／§3.7 与同包 design.md 决策点 3／7 → ③ `openspec/specs/queue-dual-file-topology/spec.md`＋`openspec/specs/editlock-dual-queue-routing/spec.md` 里的「实现差异」标注 → ④ CLAUDE.md 恢复上下文。本件为 B 类——「未声明域时该拒绝还是该默认落机制环境」这一取舍需要你在 design.md 里给出推荐方案，止步于起草不代拍板。

做什么：
1. 起草 openspec 变更包，覆盖 `#341` 期望产出五项：①写侧按 `[D:]` 精确路由；②未声明域的取舍（推荐项+备选）；③跨文件高水位线同步语义；④存量错位行处置口径（含 `#336` 真实实例）；⑤两份 spec 的「实现差异」标注随之更新或移除的方案。
2. `openspec validate --strict` 跑绿。
3. 收工即停——`pause --action-key openspec_design_review --waiting-for "design 草案已出，等你审"`。

不做什么：
- 🔴 不改 `0-学习与工具/工具-共享文档编辑锁.py`／`queue_appender.py` 一行代码。
- 🔴 不碰 `#313`（路径常量来源，明确不重叠的邻行）。
```

### A6 · 459-382b-impl——pth 盲区判据实现＋看门狗下放机器（等 rebase3-A1 完成后起跑；本泳道内部再分 A6.1/A6.2 两段）

粘贴端：CC ｜ 泳道：459-382b-impl

```
[OP-0905-I]【CC】pth盲区实现续
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905i-pth-impl-459`）｜ worktree：☑（`pth-impl-459`，新 worktree，收工自删）｜ 工作区：无（纯库内建造，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
🔴 **开工前置**：先向看护者确认 `rebase3` 泳道的 A1 段（`editable-import-guard-0902` 已合并进 master）是否已完成——未完成则先等，避免从旧 master 分叉出的 `工具-落库sweep.py` 与之冲突。
（本泳道由看护者用 Task/Agent 派发执行，不设 session 标题——源头不放，见 §一 `#487`／(甲)，2026-09-05 Shao Peishen 现场拍板）。
读 ①〈队列 §一 `#459`〉（`--row 459 --field all`，含 09-05 后续附记）→ ②〈队列 §一 `#382`〉（`--row 382 --field all`）→ ③ `openspec/changes/editable-pth-blindspot-guard/`（design 草案，分支 `claude/op0904l-pth-blindspot`@`b4a59c7`）→ ④ CLAUDE.md 恢复上下文。本件为 A 类（design 已获 3a 批准，范围已写死），无需再问澄清，直接开工。

段 A1（#459 实现）：
1. `git worktree add .claude/worktrees/pth-impl-459 claude/queue-410-editable-probe`（承接已验活的 `330218c`，含 14 条单测＋4 条真机 `pip install -e` 验活，不重新实现）。
2. `git rebase origin/master`（此时应已含 `rebase3`-A1 的 editable-guard 补测）。
3. 核对 `openspec/changes/editable-pth-blindspot-guard/` 的 design 内容与 `330218c` 现有代码是否一致，不一致处以 design 为准调整实现（design 已 3a 批准，是权威）。
4. 覆盖 setuptools `.pth` 直写目录路径形态（无 finder 模块）的判据，含单测；全量跑 `0-学习与工具/test_工具-落库sweep.py` 确认不回归。
5. 收尾：`pause --action-key merge_to_master --waiting-for "pth 盲区判据实现已完成、单测/真机验活通过，是否 ff 进 master"`（🟡，不擅自合）——**段 A1 到此为止，不因为要接着做 A2 就先斩后奏合并**。

段 A2（#382 看门狗下放机器，A1 收尾后紧接着做，同一 worktree 内继续，不必等 A1 的 merge 答复）：
6. 把巡逻章程 一.3「待发信三态盘点／暂缓-待发红标」与 一.4「起草缺口检测」的判据逻辑移入 `工具-落库sweep.py` 起跑段（每小时已在跑），做成 delta 告警（新增/变化才报，复用第 9 类告警通道既有语义）。
7. 补单测；**不摘章程对应两步**——one-in-one-out 的摘除动作交总线，待生产验活后再做。
8. 收尾：`pause --action-key merge_to_master --waiting-for "sweep delta 告警已建造完成待验活，#382(B) 是否可 ff 进 master；#382 章程摘除两步仍需总线另行处理"`。

不做什么：
- 🔴 不在 `rebase3`-A1 完成信号到达前起跑段 A1 的 rebase。
- 🔴 不擅自 ff 进 master——两段各自停在 `merge_to_master`。
- 🔴 不摘巡逻章程步骤（#382 硬边界，one-in-one-out 由总线做）。
- 🔴 不重新设计 `#459` 的判据——以已批准的 design 与已验活的 `330218c` 为准，不推翻重来。
```

### A7 · 456-lint-impl——队列结构 lint 扩「禁复述 openspec 包进度」告警（design 已获 Shao Peishen 2026-09-05 现场批准，建到底）

粘贴端：CC ｜ 泳道：456-lint-impl

```
[OP-0905-J]【CC】lint扩包进度告警
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905j-lint-restate-456`）｜ worktree：☑（`lint-restate-456`，新 worktree，收工自删）｜ 工作区：无（纯库内建造，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
（本泳道由看护者用 Task/Agent 派发执行，不设 session 标题——源头不放，见 §一 `#487`／(甲)，2026-09-05 Shao Peishen 现场拍板）。
读 ①〈队列 §一 `#456`〉（`--row 456 --field all`）→ ②设计正本 `git show claude/lint-queue-restate-pkg-0902:openspec/changes/queue-lint-restate-package-progress/design.md`（无论该分支此刻是否已合入 master 都可读到，不必等 `rebase3` 泳道）→ ③ 同族先例「队列复述信状态」告警类在 `0-学习与工具/工具-队列结构lint.py` 的既有实现 → ④ CLAUDE.md 恢复上下文。本件为 A 类——design 已获 Shao Peishen 2026-09-05 当场批准，范围已写死，无需再问澄清，直接开工。

做什么：
1. 在 `工具-队列结构lint.py` 新增一类告警「队列复述包进度」，一期只告警、不计退出码（沿信状态告警既有语义）。
2. 识别形态若判不出可靠精确匹配（design 已预判：`tasks X.Y`／`SKILL.md 须重写`／`design 待审`／`未执行` 等表述分散），**按 design 里定的兜底方案降级为低精度零漏报形态**——凡队列行提到某 openspec 包名即提示去读该包 `tasks.md`，宁可多提示不可漏报。
3. 补单测，覆盖 `#439`／`#452` 两个历史致错实例的回归用例（若测试数据难以还原，用等价构造的场景代替，注明来源）。
4. 收尾：`pause --action-key merge_to_master --waiting-for "lint 新告警类已建造完成、单测通过，是否 ff 进 master"`（🟡，不擅自合）。

不做什么：
- 🔴 不改「队列复述信状态」既有告警类的判据一行——只新增一个平行的类。
- 🔴 不擅自把新告警类的严重度提到会阻断 release／退出码非零——design 明写「一期只告警」。
```

---

## 三bis、看护opener（单次粘贴，Task/Agent 工具起七子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0905-C]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0905-C]【CC】看护B-0905_B
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，七条子泳道各自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_B
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0905C-看护七泳道B。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余七条泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0905_B-2026-09-05.md 全文（§〇～§五，含 A7）＋ CLAUDE.md 恢复上下文。

你是本批的**看护者**，不是执行者。

【怎么起活】用 Task/Agent 工具为 §三 七条泳道各起一个子任务，`isolation: "worktree"` 让它各自拿独立工作树；把 §三 里对应那条 opener 的正文**原样**作为子任务的 prompt。🔴 不要改写 opener 正文——里面每条硬要求都是实测教训，改写就等于把护栏拆了。

【并行与错峰】按 §二 波次矩阵：波 1 六条（`rebase3`／`446-formread`／`448-design`／`455-design`／`341-design`／`456-lint-impl`）触碰区两两不重叠，可同时起（错峰 1-2 分钟，只为降编辑锁碰撞，无顺序依赖；`456-lint-impl` 不依赖 `rebase3` 是否已把 proposal 分支合入 master——它直接 `git show` 该分支读设计文档）。波 2 只有 `459-382b-impl` 一条，**必须等 `rebase3` 明确报告其 A1 段（`editable-import-guard-0902` 合并）已完成或已被你转交 Shao Peishen 处理完（收到他的合并指示并执行完）后再起**——这是唯一的强顺序依赖，其余六条互不等待。

【看到这些要停下来问他，不要自行决定】
- 任一子任务命中 🟡（`merge_to_master`／`openspec_design_review`／`change_criteria`）——把它的问题原样转给 Shao Peishen，用一个字母就能答的形式问，他答完你再让对应子任务继续或结束。
- `rebase3` 泳道会在 A1/A2/A3 分别停一次（三条分支各自的 merge_to_master）——可以攒到三条都跑完只读复核后**一次性**问他"三条一起合还是分开答"，不必每条单独打扰。
- `459-382b-impl` 会停两次（A1 的 #459 实现、A2 的 #382(B)），分别转给他。
- `456-lint-impl` 建完会停一次（merge_to_master）——design 虽已批准，落地合并仍照 D1 判据停一下，不因为「design 批过了」就连实现带合并一起自动过。
- 任何一条子任务如果发现自己其实命中 ⏭️（`.51` 部署）或 🔴（对外发送）——直接标注去向，不问、不执行，转出。

【收工汇总】全部七条子泳道到达终态（完成／停在决策点等答／转出）后，产出一份汇总：哪几条已完成可登记、哪几条还停着等他、哪几条转出——不要求你替他关未答的项。

【你自己不做的】不写业务代码、不写队列行、不替子任务的产出做 commit（子任务各自处理）、不碰 `.51`、不发任何对外消息、不碰 oem-chroma 三个批次与 `OEM数据隔离规范.md`。
```

---

## 四、收工

跑
```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0905_B
```
现取，不手工数，推企微 ＋ 落档；队列回写走锁。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `followup-meta-0902`（#447）分支自陈停在的「change_criteria」判据 | 转述给 Shao Peishen，不代答（见 `rebase3` A1 做什么 ③） |
| `#240`（企微凭据申请） | 待 Shao Peishen 本人线下让 IT 介入，非 CC/Cowork 可动项，不入本批 |
| `#22`／`#130`／`#326`／`#358`／`#425`／`#436`／`#443`／`#312`／`#486` | 本批未及细查（`#312` 是纯观测窗口、非可加速项；`#381` 系另一并发 session 在办中，均不入本批），列为下一批候选，不代表判定为不可动 |
| `#382`⑵ 摘章程两步 | one-in-one-out 摘除动作交总线做，不入子泳道范围 |

