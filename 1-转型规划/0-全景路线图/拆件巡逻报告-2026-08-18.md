---
title: "拆件巡逻报告 —— 2026-08-18"
status: 在办
生成方: Cowork · 回件拆件巡逻（Dispatch v0.5，阶段二 live）
执行环境: Cowork（非交互班次，定时任务触发）
班次: 第一班
队列真身: 跨桌任务队列-机制环境.md ＋ 跨桌任务队列-业务场景.md（两份均已直读）
---

# 拆件巡逻报告 · 2026-08-18（第一班）

## 〇、一句话结论

**识别 1 行、已拆 1 行（§一 #346）**：姚祖怡对采购部#14 的回件——① 保供看板 109 行全部完成勾选标注（#110 反馈按钮首次真实使用，且是全量）；② 她把「误判原因」总结为**三条「无答交件预估齐套日期」规则**，其中**规则 1 是全新分支**。**本次未下任何新判断**，三条规则均为她原话逐字转录；两项需 Shao Peishen 定夺已登 §四 **#65／#66**。

---

## 一、扫描范围与结果

| 项 | 结果 |
|---|---|
| 队列真身 | **两份均已直读**：`跨桌任务队列-机制环境.md`（1.55 MB）＋ `跨桌任务队列-业务场景.md`（298 KB）。**未误读已成纯指针的 `跨桌任务队列.md`** |
| 扫描上界 | 顶部编号高水位线 §一 **#345**（开工时）／文内真实最大号 **#346**（机器人 13:07 追行）——**取较大者 #346**，该行即本班唯一待拆件 |
| §一 待领（`[S:open]`）行 | 机制环境 4 条（#98／#240／#337／#338／#341）＋ 业务场景 2 条（#334／#344）＋ 本班命中的 #346 |
| 含「企微反馈自动归档」且待领 | **1 行：§一 #346** |
| 其余「企微反馈自动归档」行 | 全部 `[S:done]`（#271-#278／#280／#316-#319／#329-#332／#336／#260／#291-#293／#320／#323），无遗漏待拆 |

> 🔎 **一处开工时的真实变化，值得记**：本班开始扫描时最新号为 #345；扫描过程中企微机器人于 **13:07:17 本地**追行 #346（commit `67200cd`），**该行是本班唯一待拆件**。若按开工快照作业即会输出「空巡」。⇒ **扫描与拆件之间若隔了几分钟，须重读一次真身，不得用开工快照定「空巡」。**

---

## 二、§一 #346 逐项

| 字段 | 内容 |
|---|---|
| **行号** | §一 #346（物理落在《跨桌任务队列-机制环境.md》） |
| **文件** | `7-外部文档/采购部/采购部-YaoZuYi-回复-2026-08-18-文本反馈-027612bbc002bc44b7f04b3511602fe1.md`（569 字节，mtime 2026-08-18 13:07 本地） |
| **对应信** | **采购部#14**（2026-08-13 06:58 UTC 推送，主题＝看板「正确／误判／原因（可选）」三个按钮是做什么用的），她抬头自陈「采购部跟进信第 14 封答复如下」 |

### 内容摘要

**① 她已在保供看板对 109 行数据全部完成勾选标注。**
原话「我已经在采购看板中针对109行数据全部进行了勾选」。这是 §一 **#110 反馈按钮（2026-08-06 上线）的首次真实使用，且一上来就是全量**。

**② 她把「误判原因」总结为三条规则**——「无答交件预估齐套日期」此前从未完整给出过的口径：

| 规则 | 触发条件 | 起算点 | 结果 |
|------|----------|--------|------|
| 1 | 出货日**不在**三个月内，且该件无答交 | 出货日往前推 3 个月的 **20 日** | 起算点 ＋ 90 天 |
| 2 | 出货日**在**三个月内，且该件无答交 | **此时此刻** | 此刻 ＋ 90 天 |
| 3 | 同一项目**既有答交又有无答交** | —— | 取「无答交预估齐套日期」与「有答交答交日期」中**更晚**者 |

### 判断依据

- **硬约束①（通读整份文档）**：569 字节纯文本，无表格、无附件、无夹带，**正文即全部内容**；同日 `7-外部文档/采购部/` 无新 docx（最新 docx 仍停在 2026-08-12），不存在「文档背后还有夹带」的情形。
- **硬约束②（不看列名看字符）**：本件无勾选控件、无 ✅/❌/✏️ 三列结构，该约束不适用。
- **硬约束③（同批多行合并读取）**：本班只有这一行、只有这一个文件，无同批件。
- **与现状的对照＝事实陈述，非技术判定**：§一 #118 记载 2026-07-29 Paul 拍板的四色判定核心口径为「保守预测优先——未答复子件按 90 天估算，取两者较大值」。**规则 2、3 的方向与之一致；规则 1 是全新分支**（把 90 天起算点由「今天」改为「出货日往前推 3 个月的 20 日」，且只在出货日在三个月之外时适用）。**三条与实现是否逐条对得上、规则 1 是否已被覆盖，须 CC 建造时实测确认，本班不替其定性**（同 #266 对她 08-12 新举证⑵ 的既有处置）。

### 已执行动作

| # | 动作 | 落点 |
|---|---|---|
| 1 | **销行** `[S:open] 待领` → `[S:done][D:业]`，全文结论回写；**顺带补上机器人漏写的 `[D:业]` 域字段** | §一 #346 |
| 2 | **三条规则主回灌**（含与既有口径的逐条对照、规则 1 为全新分支的定性边界） | §一 #118（`[S:partial]` 在办，状态未改，只增内容） |
| 3 | **规则 3 相邻关系追记** ＋ **判例包成文前须与三条规则一并核对的约束** | §一 #344（`[S:open]`，状态未改，只增内容） |
| 4 | **109 行标注＝反馈按钮首次真实全量使用**追记，并标注「未核实真实落盘条数」 | §一 #110（`[S:done]`，状态未改） |
| 5 | **README 采购部#14 追记她仍回件**；原 `✅ 无需回复` 原样保留未改判 | `6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` |
| 6 | **域口径回灌**（新增 2026-08-18 节，含原话全文转录、规则表、与 #344 的关系、落点表） | `1-转型规划/session接力-采购域场景落地.md` |

### 三处刻意未做（如实登记，非遗漏）

1. **未起草采购部#15**——起草属对外动作，按 §〇 红线不进池。串行闸对姚祖怡**本就开着**（采购部#14 为 `✅ 无需回复`＝闭环态，自 2026-08-13 起就不该锁着，见该行 2026-08-18 补标）。
2. **未搬 #346 的物理位置**——本行 `[D:业]` 却落在机制环境件，系机器人 `queue_appender` 统一落机制环境文件的迁移期妥协（#315 归档件 3.7，欠账已由 §一 #341 承接）。**搬家属 #341 范围，巡逻不代办**（同 #332 先例）。
3. **未改 README 采购部#14 的 `✅ 无需回复`**——该态是**起草时**依正文明示字样作出的合法判定；「只能起草时判定、不得事后追认」这条防滥用约束的**对偶是亦不得事后翻掉**。两者同为闭环态，串行闸不受影响。

### 需 Shao Peishen 项：**2 项**

| §四 编号 | 事项 | 选项 | 默认项 |
|---|---|---|---|
| **#65** | 三条规则走哪条确认路径 | (a) 视为已显式签认直接建／**(b) 并进 §一 #344 判例包同批签认**／(c) 单独发确认信 | **(b)** |
| **#66** | 109 行标注怎么取用、要不要回她一句 | **(a) 随采购部#15 一并致谢**／(b) 等 #282 周报机制自然反馈／(c) 现在单独回 | **(a)** |

> 🔴 **#65 的实质不是排期，是一致性**：#344 治「齐料日期只取**最早**答交日期」，而规则 3 要求「取**更晚**者」——**两者落在同一个取值函数上、方向相反**。若分批发判例，她要在两周内为同一个函数签认两套口径，**正是判例批改法要消灭的浪费**。故建议 (b)。
>
> 📌 两项**均不标注回复位置标签**——它们由本次巡逻自己提出，他就在这份报告里答，标 `【原 session 答】` 类标签是零信息量（2026-08-05 定）。

---

## 三、待发信盘点（§一.3，只读只提醒，绝不发送）

**README 数据行 42 行，三态计数：**

| 状态 | 计数 | 明细 |
|---|---|---|
| `⏳ 待你审` | **0** | —— |
| `🆕 待发` | **0** | —— |
| `⏸ 暂缓` | **0** | —— |

**⇒ 无积压，无需催办。**

**🔴 交叉核对（「队列称暂缓而 README 仍待发」）：无命中** —— 全表零条 `🆕 待发`，该类不一致在结构上不可能存在。

**闭环态分布（供参考）**：`📥 已回件并回灌` 4／`✅ 无需回复` 2／`📨 已确认闭环` 1／`❌ 已作废` 1，其余为 2026-08-08 前发出、按旧口径停在 `✅ 已推送` 的历史行（不追改）。

**各部门下一个可用号**：财务部#13／**采购部#15**／质量部#7／IT部#9／销售部#1。

> ⚠️ **README 顶部「当前各部门下一个可用号」段与下表状态列的同族第三次漂移风险**：该段目前写「采购部#15（#14 已于 2026-08-13 06:58 UTC 推送，**姚祖怡处串行闸重新挂起、待其回件**）」——**该括注在 2026-08-18 新闭环三态口径下已不成立**（#14 是 `✅ 无需回复`，从不占串行闸），且她今日**实际也已回件**。**本班未改该段**：它属 README 顶部说明的口径校准、不是本件的事实回灌，且该段已有两次「只改下表漏改本段」的复发记录（#321／2026-08-12 环境总线），**该由一次统一校准处理，不宜由巡逻零星改**。如实登记，供值周巡检下轮对账处理。

---

## 四、「该起草而没起草」缺口检测（§一.4）

**已跑**（沙箱 bash 通道可用，脚本纯只读、零副作用）：

```
$ python 5-平台底座/wecom-aibot-service/scripts/draft_gap_check.py --window-days 14
待发信盘点（扩展版，近 14 天）：发现 1 处疑似「该起草而没起草」
- 陈忱｜场景 4-数字员工/质量部/QD-B-立项审核门禁/｜最早改动 2026-08-04｜commit 119aad38
```

**已复核，判定为真实缺口，但成因与工具报出的理由不同——两点须分开看：**

**⑴ 工具给出的理由是滑动窗口边界假象**（同 2026-08-14 那班已证伪过一次的形态）：`--window-days 20` 与 `--window-days 30` 复跑**均报「无缺口」**；14 天窗口的下界恰好切在 2026-08-04（`119aad38`），使「最早改动」跳到质量部#6 发信日（2026-08-03）之后而触发判据。**同一份事实，窗口挪 6 天结论就反转 ⇒ 该命中不能以工具给的理由采信。**

**⑵ 但换一条独立路径看，缺口是真的**——与窗口无关：
- 质量部#6 **2026-08-17 已回件并回灌**（§一 #336），**串行闸对陈忱已解除**；
- QD-B **2026-08-17／08-18 有真实建造与部署**：`7b3933d`（#340 评估）／`1282470`（C01-C10 档一档二落地）／`6ec316f`（.51 服务入口修复）／`5db68cc`（部署记录）；
- §一 #336 本身已写明「**下一封＝质量部#7 … 本班不起草、只如实登记条件已具备**」。

**⇒ 结论：质量部#7 的起草条件确已具备，且已有承接登记（§一 #336 ＋ §一 #340）。** 起草属建造／对外动作，按 §〇 红线不进池，**本班不起草**；**亦不为此另立行或另登 §四**——#336 与 #340 两行已是有主的承接载体，再登一条只是重复。

> 🔑 **一条方法上的记录（对应 2026-08-14 那班提的「观察一：重复劳动」）**：该命中已是**第二次**被同一形态的窗口假象触发，也是**第二次**被人工复核。**本次不同于上次的是：上次结论是「假阳性，不必处理」，本次是「工具的理由错了，但结论对」。** ⇒ **「工具报错了理由」不能推出「结论也错」——两者要各判一次。** 本条已写进本报告，不再只活在 transcript 里（回应 2026-08-14 那班「结论只活在 transcript、下一班重踩」的观察）。

---

## 五、巡逻自检

| 项 | 结果 |
|---|---|
| **本轮有无预案覆盖不了的情形** | **有 1 处，已按 §五 歧义纪律处理**：Shao Peishen 2026-08-18 口头告知「姚祖怡的批改刚刚回复」，而本件形态是**对采购部#14 的文本答复、不是判例批改件 docx**（该目录最新 docx 仍停在 2026-08-12）。**本班不推断两者是否为同一件**，采购域「取回批改件原件」这一 ⓪ 步前置是否已解除，**留采购域自行核定**，未代其销前置、未登 §四（属采购域内部核实，不需他定夺） |
| **三.6 重读比对结果** | **已做，无冲突**：acquire 后写盘前重读两份真身，`#346` 原行精确命中 1 次、`#118/#344/#110` 各命中 1 行；**未出现自己没写过的新增行**，无撞号，高水位线由工具原子回写（§四 #64 → #66）。全部改动均为**行级定点替换**，未做整文件写回 |
| **本轮实际拿到的编号** | **§四 #65、§四 #66**（`acquire --reserve 2 --section 四` 预留，两个均已用满）。**§一 未取号**——本次无新建 §一 行 |
| **三.2 并入审核结论（协议〇.10）** | **已逐条过 §一 全部 7 条待领行**（机制：#98 月度体检常驻行／#240 企微凭据／#337 场景级 R5／#338 载体健康守卫／#341 队列写侧域路由；业务：#334 物料看板／#344 齐料日期不看答交数量）。**结论：本次不新建任何 §一 行，三项内容全部并入既有行**——三条规则并入 **#118**（其即齐套日期估算口径的承接行，`_resolve_priority_order`／四色判定口径均在此）／规则 3 相邻关系并入 **#344**（同一取值函数）／109 行标注并入 **#110**（反馈按钮本体行）。**升格动作＝0，故 WIP 未增，亦不触发协议〇.9 措施 C 的机制类上限判断** |
| **一.4 缺口检测是否已跑** | **已跑**（14／20／30 三个窗口各一次），结果与复核见 §四 |
| **三.8 写后反查是否已跑** | **已跑，但用的是沙箱等价物、非 opener 规定的 PowerShell `工具-写后反查.ps1`**——本班为非交互班次，无本机 PowerShell 通道。等价做法：每次写盘后重新 `open()` 读回并断言关键词命中数与表格列数（#346／#118／#344／#110 均实测 **8 列**、README 采购部#14 实测 **6 列**、接力新节与规则表关键词各命中 1）。**如实登记为「等价物，非规定形式」**——这正是 2026-08-14 那班「观察二：非交互班次的等价判据没写清」指出的缺口，本班第二次撞上 |
| **锁与结构校验** | 队列锁 `acquire` 退出码 **0** 后才动手（未用 `;` 串联）；README 独立锁单独 acquire／release，退出码均 **0**；`append-row` 用于全部 §四／§二 新增行（未手写整行）。**`release` 六项结构校验一次通过、未被拒**；另跑 `工具-队列结构lint.py` 对两份生产文件 **✓ 通过**（列数／§二 状态列格式／权威模块可 import）。**U+FFFD 乱码哨兵：四个被改文件均 0 命中** |
| **§二 批次登记** | **已登记** `B-0818_拆件巡逻346姚祖怡采购部14回件`，状态列写「**待处理**（登记，待 sweep 落库）」、**不含 ✅**；文件清单 5 个**全部完整路径**，含两份队列文件自身（release 校验②）；清单内无裸竖线、反引号成对 |
| **sweep 触发（§四.5）** | **未触发** —— 本班为**非交互班次，无本机 PowerShell 通道**（既定预案，属执行环境边界、不计入「错写」）。批次已规范登记 §二，**由 `ZhuopinCommitSweep` 周期（1 小时，#189）自然取活** |
| **决策提醒触发（§四.6）** | **未触发** —— 本轮**确实新登了 §四 项（#65／#66）**，按判据本应跑一次 `decision_reminder_check.py`，但该步需本地 shell ＋ 出网，**非交互班次两者皆无**（沙箱侧对 `qyapi.weixin.qq.com` 出网受限，见根 CLAUDE.md）。**兜底为双载体**：`ZhuopinDecisionReminderDaily`（每日 08:30）＋ sweep 起跑段（每小时，#219），两条均会捡起本轮新增项 |

---

## 六、live 首周逐日抽查

**live 首周逐日抽查中，出现一次错写即退回 dry-run。**

> 📌 **回应 2026-08-14 那班的「观察三」**：该班指出「连续两班空巡零写入 ⇒ 观察期积累的是『没写过』的证据、不是『写对了』的证据」。**本班是真实写入班次**——改动落在 5 个文件、7 处（§一 #346 销行／#118／#344／#110 追记／§四 #65／#66 新增／README 追记／接力新节），**可供逐处抽查是否错写**。

---
---

# 拆件巡逻报告 · 2026-08-18（第二班）

> **执行环境**：Cowork（定时任务触发，但**本机 PowerShell 通道实测可用** —— 与第一班不同，本班为**交互能力班次**，故 §一.4 缺口检测／§三.8 写后反查／§四.5 sweep 触发三步**均已真实执行**，不走非交互预案）
> **队列真身**：`跨桌任务队列-机制环境.md` ＋ `跨桌任务队列-业务场景.md`（两份均已直读，**未误读已成纯指针的 `跨桌任务队列.md`**）
> **本机日期取值**：`Get-Date` ⇒ **2026-08-18 17:49:20 本地**（未沿用第一班取值、未估算）

## 〇、一句话结论

**识别 1 行、已拆 1 行（§一 #347）**：姚祖怡对 **采购部#15** 的答复，**两个决策点全部作答、且均为「对」**——① 规则1 里「20 日 ＝ 那个自然月的 20 号」理解确认无误；② F 开头成品穿透改造抽查复核通过。**本信就此闭环，姚祖怡串行闸解除**，#344 判例包（拟采购部#16）的「等 #15 闭环」前置至此满足。**本班未下任何新判断**，两条结论均为她原话逐字转录；**并入审核后未取号、未新建任何行**；**需 Shao Peishen 项：0 项**。

---

## 一、扫描范围与结果

| 项 | 结果 |
|---|---|
| 队列真身 | **两份均已直读**：`跨桌任务队列-机制环境.md`（1.61 MB，17:43 本地最后写入）＋ `跨桌任务队列-业务场景.md`（309 KB） |
| 扫描上界 | 顶部编号高水位线 **§一 #347 ｜ §四 #66**（acquire 回显同值）／文内真实最大号 **#347** —— **两者一致，取 #347** |
| 含「企微反馈自动归档」的全部行 | 机制环境 24 行 ＋ 业务场景 6 行，逐行核状态机器字段：**仅 #347 为 `[S:open]`**，其余全部 `[S:done]` |
| 本班待拆件 | **1 行：§一 #347**（位于**机制环境**那份 —— 机器人默认落该文件，但领取方是「采购专线」、内容属业务域，**两份都扫才不会漏**） |
| 同批合并读取（硬约束③） | #347 与第一班已拆的 #346 **同为姚祖怡 2026-08-18 来件**，已合并对照阅读；另通读采购部#15 信件原文以还原「第一个问题／第二个问题」各指什么 —— **不靠回件里的序号推断** |

> 🔎 **本班的一处判据落点**：#347 正文只有 300 字节、且**通篇不含任何指向性关键词**（原文＝「第一个问题我的答复：对。第二个问题我的答复：对。……」）。**只读回件本身无法知道她在答什么** —— 必须回到 `6-人才与组织/部门AI专员跟进/采购部-姚祖怡-跟进-2026-08-18-三条规则已收到与穿透改造请复核.md` 取两个决策点的原文。⇒ **「答复类回件」的拆件，信件原文是必读件，不是可选件。**

---

## 二、§一 #347 逐项

| 项 | 内容 |
|---|---|
| **行号** | §一 **#347**（`跨桌任务队列-机制环境.md`） |
| **文件** | `7-外部文档/采购部/采购部-YaoZuYi-回复-2026-08-18-文本反馈-bb693306a287783f0f04ed495bc3e7b2.md`（300 字节，2026-08-18 16:28 本地） |
| **一句话摘要** | 姚祖怡答 **采购部#15** 的两个决策点，**均答「对」**，并主动补一句抽查情况 |

### 内容摘要（原文逐字，未改写）

> 「（采购部跟进信第 15 封）第一个问题我的答复：**对**。第二个问题我的答复：**对**。目前我抽查了F开头的成品，第一个已经改为沿主料路径穿透到采购件层重算，不再只看第一层。@邵培申　邵总，上述跟进信我的答复如上，请审阅」

对照采购部#15 正文，两个问题分别是：

1. **（信中「本信第一个要你回的」）** 规则 1 里的「20 日」，我方理解为**那个自然月的 20 号**（举例：出货日 12-05 → 往前推 3 个月为 9 月 → 起算点 9-20 → 再往后 90 天）—— **这样理解对吗？**
2. **（信中「第二个要你回的」）** 麻烦挑几个 **F 开头的成品**看一眼，穿透改造后的**数字对不对**。

### 判断依据

- **两问均为封闭式确认题，她给的是「对」** —— 属**事实与已有裁决的确认**，不含任何新口径、新方向、新排期，**回灌不越 §〇 红线**。
- **规则1 归属**：她 08-18 在 #346 里书面写下三条规则，第一班已如实登记「规则 1 是全新分支、此前任何载体均无此条」并登 §四 #65；本次她确认的正是 #65 那条路径上的**措辞理解**，故回灌落 §一 **#344**（第一班已把规则3 回灌至该行，同一取值函数层）。
- **穿透改造归属**：#266 状态列已记 2026-08-18 建造完工并真部署 `.51:8091`，其中明写「用她原始举证案例端到端复现」；她这次给的是**同一人对同一改造的肯定验收**，故回灌落 §一 **#266**。
- **串行闸判据**：查 README 表格 —— 采购部#15 是姚祖怡最近一封，原状态 `✅ 已推送 2026-08-18 07:18 UTC`；本班回灌完成后转 `📥 已回件并回灌`，**三种闭环形态之一达成 ⇒ 串行闸解除**。

### 已执行动作（4 个文件、5 处）

| # | 落点 | 动作 |
|---|---|---|
| 1 | §一 **#347**（机制环境） | **销行**：`[S:open] 待领` → `[S:done][D:业] ✅ 已拆件（2026-08-18 第二班拆件巡逻）` ＋ 两问结论 ＋ 派生去向 ＋ 并入审核结论。**✅ 写在状态列开头**；**顺带补齐了原行缺失的 `[D:业]` 域字段**（机器人追行不带域字段，缺字段的行不进协议〇.9 措施 C 的 WIP 分母） |
| 2 | §一 **#344**（业务场景） | **追加回灌节**：规则1 措辞理解已确认；**并入一项待办「实测现行程序是否已覆盖规则1，未覆盖就改」**（协议〇.10，未取号）；标注「等 #15 闭环」前置已满足。**机器字段维持 `[S:open][D:业]` 不变** |
| 3 | §一 **#266**（业务场景） | **追加回灌节**：穿透改造经她本人抽查验收通过；如实登记「未说明抽查几个、未列料号」这一边界。**机器字段维持 `[S:done][D:业]` 不变** |
| 4 | **README-跟进机制与命名约定.md** | 采购部#15 发送状态 → `📥 已回件并回灌（…）｜ ✅ 已推送 2026-08-18 07:18 UTC`，**两态并列同 质量部#7 先例**（`📥` 前缀在前是因机器判据只认该前缀；`✅ 已推送` 保留是「历史记录不追改」） |
| 5 | **本报告** | 追加「第二班」小节（**未覆盖第一班任何内容**） |

### 四处刻意未做（如实登记，非遗漏）

1. **未起草采购部#16 判例包** —— 串行闸虽已解除、#344 素材也已备好，但**起草与对外发送属 §〇 红线，不进池**；#344 行内已明写「成文与送审归 Cowork 业务总线」，本班只把「前置已满足」这一事实写进行内供其取用。
2. **未实测规则1 是否已被现有代码覆盖，也未判定其与 #344 是否同根因** —— 那是新判断＋建造动作，同第一班对规则3 的处置。
3. **未据她「抽查了F开头的成品」推断抽查覆盖面** —— 她未给数量与料号，**推断即是替她下判断**。
4. **未改信件 `.md` 的 frontmatter（仍写 `status: 待发` / `发送状态: ⏳ 待你审`）** —— 抽查 采购部#14／财务部#13／IT部#9／质量部#7 四封**已发出**的信，frontmatter **全部同样滞留在「待发」**，即**信件 frontmatter 不随发送更新是现行常态、非本封独有异常**；README 表格是权威载体，本班不单方面改变该惯例，**仅登记为一处观察**（见下 §五）。

### 需 Shao Peishen 项：**0 项**

**本次无需你决策。** 两个决策点都是她答的、不是需你答的；#344 判例包的排期与起草归属**早已在 #344 行内定死**（Shao Peishen 2026-08-18 已拍板 (a)「现在就起草判例包」、成文归 Cowork），**重提即是把已拍板项再问一次**，按 §三.5 的告诫不重复登记。

---

## 三、待发信盘点（§一.3，只读只提醒，绝不发送）

**逐行扫描 README 表格全部 `NF==8` 行的发送状态列**（非全文关键词计数 —— 全文里 `⏳/🆕/⏸` 出现 6／13／10 次，但**那些全部落在三态语义的说明性正文里，不是任何一行的状态**）：

| 三态 | 计数 | 明细 |
|---|---|---|
| `⏳ 待你审`（已起草未送审） | **0** | —— |
| `🆕 待发`（已批准未发） | **0** | —— |
| `⏸ 暂缓`（已批准主动暂缓） | **0** | —— |

**⇒ 无任何积压。** 成因是 Shao Peishen 2026-08-18「所有跟进信都尽快发出」后，当日四封（IT部#9／财务部#13／采购部#15／质量部#7）**全部真实发出**（批次 `B-0818_16_四封跟进信全部发出`）。

🔴 **「队列称暂缓而 README 仍待发」红标交叉核对：0 命中**（`⏸ 暂缓` 与 `🆕 待发` 均为空集，该类不一致**结构上不可能存在**）。

---

## 四、「该起草而没起草」缺口检测（§一.4）

已真实执行 `python 5-平台底座/wecom-aibot-service/scripts/draft_gap_check.py --window-days 14`，stdout 原样附上：

```
待发信盘点（扩展版，近 14 天已部署场景 vs 已起草跟进信）：无缺口
```

**⇒ 无缺口。**

---

## 五、巡逻自检

| 项 | 结果 |
|---|---|
| **预案覆盖不了的情形** | **无。** 本班全程落在既定预案内 |
| **§三.6 重读比对** | **已做。** acquire 回显警告近 120 分钟内另有三个身份持过锁（企微机器人 81 分钟前／全景路线图线 8 分钟前／sweep-commit 7 分钟前），**故写回前逐行重读两份真身**：#347 仍为 `[S:open] 待领`（内容与 acquire 前一致）、#344／#266 尾部与 acquire 前逐字一致、README 采购部#15 仍为 `✅ 已推送`。**未发现自己没写过的新增行，无撞号，高水位线无需推进**（本班零取号） |
| **本轮实际拿到的编号** | **零个。** 未调用 `--reserve`／`--reserve-multi`；高水位线维持 **§一 #347 ｜ §四 #66** 不变 |
| **§三.2 并入审核结论（协议〇.10）** | **已逐条扫过两份队列 §一 全部待领行**（机制环境 `[S:open]`：#98／#240／#337／#338／#341；业务场景 `[S:open]`：#334／#344；另通读 `partial`／`hold` 类共 20 余行）。**结论＝拟升格的唯一事项「实测现行程序是否已覆盖规则1」并入 §一 #344，故本次不取号、不新建行。** 判据：与 #344 同处齐料日期取值链路（触碰区同为 `sc8/{sources.py,forecast.py}`），且 #344 判例包成文本就被要求与规则1／2／3 一并核对 —— 另起一行会把同一个函数的口径确认拆成两处。**排除项**：#334（物料看板新视图，触碰区不重叠）／#118（优先级逐级扣减引擎，不重叠）／#266（可齐套数量字段，已 done 且不同函数） |
| **§一.4 缺口检测** | **已跑**（本机通道可用），结果「无缺口」 |
| **§三.8 写后反查** | **已跑**，4 个被改文件逐一 `工具-写后反查.ps1` 校验，退出码均 **0**（详见下方表格） |
| **锁与结构校验** | `acquire` 退出码 **0** 后才动手（**未用 `;` 串联**，退出码单独检查）；`append-row` 用于 §二 新增行（**未手写整行**）；`工具-队列结构lint.py` 对两份生产文件 **✓ 通过**（列数／§二 状态列格式／权威模块可 import）。🔴 **`release` 首次被拒 1 项、已修正后重试通过，未绕过**（详见下方「两处如实登记」①） |
| **§二 批次登记** | **已登记并已落库** `B-0818_18_拆件巡逻347姚祖怡采购部15回件`：登记时状态列写「**待处理**（登记，待 sweep 落库）」、**不含 ✅**；sweep 于 **2026-08-18 09:56 UTC** 自动销行转 `✅ 已完成`。**未回填进第一班那条已 ✅ 的批次行**（该行已于 05:25 UTC 被 sweep 销行，回填即成孤儿脏文件） |
| **§四.5 sweep 触发** | **已真实触发**（`Start-ScheduledTask -TaskName ZhuopinCommitSweep`，`LastTaskResult=0`，`LastRunTime=2026-08-18 17:56:09 本地`）—— 本班有本机 PowerShell 通道，**不走非交互预案**；已回查批次行确认落库 |
| **§四.6 决策提醒触发** | **按判据不触发**（非环境限制）：本轮**未新登任何 §四 项、也未新增标 P0/P1 的待领行**，`decision_reminder_check.py` 的触发条件（#172：新增或超期）在「新增」这一侧不成立；「超期」一侧由双载体覆盖（`ZhuopinDecisionReminderDaily` 每日 08:30 ＋ sweep 起跑段每小时，#219），且 **sweep 刚于 09:56 UTC 真实跑过一轮**，该侧已自然覆盖 |
| **日期取值** | 每次写入前重取 `Get-Date` ⇒ **2026-08-18**，**未沿用第一班取值**（该纪律 2026-08-15 因跨日会话事故补入） |
| **U+FFFD 乱码哨兵** | 4 个被改文件 **0 命中** |

### 两处如实登记

**① `release` 首次被拒，已修正后重试通过（未绕过，机制按设计工作）。**

被拒原因：`§二 批次「B-0818_18_…」文件清单未声明队列文件自身路径`。**但清单里其实是写了那两个路径的** —— 差别只在**没用反引号包住**。⇒ 该校验读的是**反引号包裹的片段**，裸写路径它看不见。这与 2026-08-08 已记的那条（清单里出现**三连**反引号会打乱配对、使校验读不到路径）是**同一个解析口的两个方向**：一个是配对被打乱，一个是压根没进配对。**处置＝给 5 个路径逐一补上单反引号后重试，`release` 一次通过**，未使用任何逃生阀。

📌 **值得记的一点**：这次是 fail-loud 起了作用 —— 若该校验宽松到接受裸路径，**本班的 5 个改动文件会有 2 个不在任何被识别的批次里**，成为孤儿脏文件并持续告警。**被拒是它该做的。**

**②bis 一个新踩的坑，建议后来者直接抄走：`append-row` 的 `--cell` 里写反引号路径时，若用 bash 双引号传参，反引号会被 shell 当成命令替换执行掉 —— 工具照样回显 `✓ 已追加一行`，但那两个路径在落盘内容里已经消失。**

本班第二批登记时真实发生：清单里的两个反引号路径被 shell 当命令执行（stdout 刷出几十行 `command not found`），落盘后的单元格里 ① ② 后面**直接跟着括号，路径为空**。**工具零报错、退出码 0**。⇒ 若只看 `✓` 回显就走人，这条批次会带着**一份空清单**进入 sweep，两个文件双双成为孤儿。

**这是本班第二次撞上「返回值完全正常、只是答案是反的」这一形态**（第一次是 `release` 把裸路径当没写）。**处置**：读回工具回显里的实际落盘文本 → 发现路径缺失 → 用 `Edit` 定点补回。**给后来者的做法**：`--cell` 参数改用**单引号**传，或干脆避免在 shell 传参里出现反引号。

🔴 **它还有一个更该被记住的副作用 —— 在仓库根目录静默创建了两个文件。** 被反引号执行的那段文本里含 markdown 引用行（`> **规则 1（出货日不在三个月内）**：…`），`>` 被 shell 当成**重定向**，于是在仓库根**新建了两个 0 字节文件**：`规则`（`E8A784E58899`）与 `规则`（`EF80AAEF80AAE8A784E58899`，前缀是 `**` 被字体私用区映射后的残留）。**两者在 `git status` 里显示为同名的两行 `?? 规则`，肉眼分辨不出是两个不同文件** —— 必须 `-c core.quotepath=true` 或直接取文件名字节才看得出差别。

**已处置**：两文件均为本班 17:58:18 的意外产物、0 字节、无内容价值，**已按字节名逐一 `Remove-Item -LiteralPath` 删除并复核 `git status` 干净**。⇒ **教训不止「反引号会被执行」，而是「被执行的那段文本里若含 `>`，它会往仓库里写文件」** —— 这类文件不进任何批次、不报错、且在 `git status` 里长得像同一个东西。

**② 本班未走「非交互预案」，与近期多数班次不同。** 定时任务触发的班次此前普遍无本机 PowerShell 通道，故 sweep 触发／写后反查／缺口检测三步长期按既定预案跳过。**本班实测该通道可用**（`Get-Date` 与 `Start-ScheduledTask` 均正常返回），故三步**全部真实执行**。⇒ **「定时触发」不等价于「无本机通道」，每班应实测一次再决定走不走预案**，不要凭班次类型直接套用。

### 一处观察（不构成动作建议，只登记）

**已发出信件的 `.md` frontmatter 普遍滞留在 `status: 待发` / `发送状态: ⏳ 待你审`。** 抽查 4 封已推送的信（采购部#14 发于 08-13、IT部#9／财务部#13／质量部#7 发于 08-18），**全部如此**。⇒ 这是**现行常态而非本封异常**，README 表格是唯一权威载体、机器判据也只读 README，故**当前无实际危害**；但它意味着**单看信件文件本身会得出与事实相反的结论** —— 属「工具静默回退」同族的「载体各说各话」形态。**本班不改惯例、不提改法**，仅登记供总线在有余力时判断是否值得收口。

---

## 六、live 首周逐日抽查

**live 首周逐日抽查中，出现一次错写即退回 dry-run。**
