---
title: "【Cowork】开场 prompt · 环境保障线接力交接（2026-08-04）"
created: 2026-08-04
执行方: Cowork · 环境保障线（非业务总线、非域专线）
执行环境: Cowork
来源: 2026-08-03~04 环境保障线 session（跨 36 小时，落库 6 个批次），Shao Peishen 确认转场
status: 待执行
取代: 开场prompt-【Cowork】环境保障线-接力交接-2026-08-03.md（其 §四 已过时；§五/§六 已按规则退休制改写，本版继承）
---

# 【Cowork】开场 prompt · 环境保障线接力

## 【开场词（复制即用）】

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ ｜ 工作区：无

读 1-转型规划/0-全景路线图/开场prompt-【Cowork】环境保障线-接力交接-2026-08-04.md ＋ CLAUDE.md 当前进度恢复上下文，按文件执行；先跑 §〇 开工自检四查，通过即直接开工、有差异再问我。
```

**两份要读的文件**：本 opener ＋ `1-转型规划/0-全景路线图/session接力-Phase1收口.md`（最新节＝2026-08-04）

---

## 〇、开工自检四查（A 类同线转场）

1. **接力新鲜度**：`session接力-Phase1收口.md` 最新日期节 **是否 = 2026-08-04（第二轮 · #232 看板）**？不等 → 先读差异。
2. **清单一致性**：从 §四 抽 **#246／#247** 两行，用本机 PowerShell 直读队列**状态列**逐字比对。**⚠️ 只比状态列**（✅ 合法出现在描述文字里）；**且注意 §一 行的编号列不带 `#` 前缀**，正则用 `^\|\s*\d+\s*\|`，状态列取**第 6 格**（`($_.Line -split '\|')[6]`，勿用 `[-2]`——那是登记日期列）。**不一致一律以队列为准。**
3. **降级残留**：grep 四条 session 接力文件顶部「⏳ 队列更新待补」小节；读到"已回补"字样即跳过。
4. **派单件现读**（本 opener 正文一律不写派单件存在性）：
   ```powershell
   Get-ChildItem "1-转型规划\0-全景路线图" -Filter "开场prompt-【CC】*" -File | Select-Object Name,LastWriteTime
   ```

**判定**：四查全通过 → 按 §四 直接开工、不提问。任一不通过 → 只就该差异出**带字母选项**的选择题。**禁止开放式提问。**

---

## 〇bis、开工现读清单（可由工具直读的字段一律不写进本文件）

```powershell
cd "C:\Users\Paul Shao\OneDrive\Projects\企业AI转型"
git fetch origin master 2>&1 | Out-Null
"behind/ahead = " + (git rev-list --left-right --count origin/master...HEAD)
"脏文件 = " + ((git status --porcelain --untracked-files=all)|Measure-Object).Count + " ｜ 分支 = " + (git rev-parse --abbrev-ref HEAD)
$q = ".\1-转型规划\0-全景路线图\跨桌任务队列.md"
"§二 剩余待处理 = " + (((Select-String -Path $q -Pattern "^\| B-")|Where-Object{ (($_.Line -split '\|')[-2]).Trim() -notmatch "✅" })|Measure-Object).Count
(Select-String -Path $q -Pattern "编号高水位线").Line[0].Substring(0,46)
"锁 = " + ((Get-Content "$q.editlock" -Raw|ConvertFrom-Json).released)
foreach($n in @("ZhuopinCommitSweep","ZhuopinAibotDevListener","ZhuopinDecisionReminderDaily")){
  $t=Get-ScheduledTask -TaskName $n; $i=Get-ScheduledTaskInfo -TaskName $n
  "{0,-30} State={1} Missed={2} LastRun={3} LastResult=0x{4:X}" -f $n,$t.State,$i.NumberOfMissedRuns,$i.LastRunTime,$i.LastTaskResult
}
git worktree list
"现在 = " + (Get-Date -Format "yyyy-MM-dd HH:mm") + " 本地 / " + ((Get-Date).ToUniversalTime().ToString("HH:mm")) + "Z"
```

> `NumberOfMissedRuns` 必看——**"该跑而没跑"不产生任何 Task Scheduler 事件**，只能靠这个字段。

---

## 一、本线定位

**环境保障线 ＝ 为构建环境与自动化机制保驾。** 只产改 `.md`、只做只读取证，不写生产代码、不自行 commit、不碰业务决策。

🔴 **默认交付形态 ＝ 机制收口，不直接派活**（Shao Peishen 2026-08-03 定，见 CLAUDE.md §5）：本线交付物是 **① 环境取证与梳理结论 ② 方案定稿 ③ 队列行写入**；**分发交给值周巡检（周计划）与拆件巡逻**。**"备派单件 + 请他粘贴开场词"属直接派活，仅在紧急且经他明示授权时才用。** 实证：2026-08-03 本线备的《五件同车》派单件，被同日周计划 v2 自行吸收并重排 A6 避让——机制本就会分发它。

| 归本线 | 不归本线 |
|--------|---------|
| 构建环境、git/队列台面、worktree/stash 卫生 | 业务口径裁决、场景排期、专员协作内容 |
| 自动化机制（sweep/编辑锁/巡逻/巡检/机器人/决策提醒） | 建造与部署实施（CC）、发信（机器人） |
| 事故拦截与根因取证、机制缺陷登记 | 派活与开 opener（值周巡检/业务总线） |
| 对工具链**只读核实** | 🔴 改本机工具链本身（npm/插件/CLI 升级、`openspec update`）→ **归 CC** |

---

## 二、权威依据（只给指针）

1. `1-转型规划/0-全景路线图/跨桌任务队列.md` — 唯一调度载体，**协议〇 八条必读**（尤其 〇.7 编辑锁、〇.8 每周清扫＋**已复检扫描域含仓库外载体**）。
2. 根 `CLAUDE.md` §5 — 双桌分工、发布即收口、机制优先、执行环境标注、会话末决策项双向规则、时间戳必判 UTC、判重启只认 `ID=27`、**工具静默回退**、**规则退休制**、**机制收口默认形态**、**跟进信串行原则**。
3. `session接力-Phase1收口.md` — 本线滚动接力。
4. `0-学习与工具/` — 机制工具真身（编辑锁／落库sweep／孤儿worktree扫描／文档台账／skills源码／定时任务镜像）。
5. `3-治理与合规/.51部署标准清单-服务侧与笔记本侧-2026-08-01.md` — 「发布即收口」第②关。
6. `环境体检-2026-07-30-第0期基线.md` §七 — 首期体检 2026-08-28 执行清单（**第四版判据：清完三数 `1/1/1`**）。

---

## 三、机制现状（只留不可直读的判断性内容）

**六条自动化**：Windows 计划任务 3（`ZhuopinCommitSweep` `PT1H`+开机／`ZhuopinAibotDevListener` 常驻／`ZhuopinDecisionReminderDaily` 每日 08:30）＋ Cowork 定时任务 3（拆件巡逻 工作日 09:00/13:00／值周巡检 周一 10:00／skill 更新检查 每月 1 日）。后三条只能读镜像。

**已知良性、勿当异常**：
- worktree **目录名与承载分支会漂移**（harness 复用目录重绑分支）——身份以 `git branch --show-current` 当刻实测为准；判空壳看 `.git` 条目＋`.git/worktrees` 注册项，**不看 `git -C` 输出**（它会静默上溯到主工作区）。
- sweep **「非 clean 跳过」占 19%**（288 轮实测：落库 163／空转 84／让路 56／schannel 7）——**让路是常态不是故障**，是安全门在挡别人的未声明脏文件。
- `git fetch` 太早会读到 `ahead=1` 中间态；**判推送是否真失败看 `schannel` 计数是否增加**（基线 7）。

**#219 那个洞已补上**（2026-08-04 CC 落地双载体：sweep 起跑段子进程调用 `decision_reminder_check.py`，每小时一道，与每日 08:30 那道互为备份）。

**⚠️ 但 `ZhuopinDecisionReminderDaily` 的计划任务在 08-04 被 #231 重新注册过**，`LastRunTime` 已重置为 `1999-11-30` 占位、`LastTaskResult=0x41303`（从未运行）——**这是重注册的正常表现，不是故障**；首次真跑在下一个工作日 08:30。**下轮自检看到这个值不要当异常报**，但要顺手核 `NumberOfMissedRuns` 是否仍为 0。

**当前最值得盯的两处（均已登记，非新发现）**：#247 的 4 条模糊批次告警每轮重复（狼来了入口）；#246 的日期基准空白。

---

## 四、本线在办/待跟进

**领取顺序**：本线**当前无在办活**。**本表按队列真身重排于 2026-08-04 23:0x 本地**——上一版（08-04 转场时）列的「待领」里有 8 行已在同日 08-04／08-05 由 CC 五件同车等批次做完，属**表过时**而非漏做；本轮已逐行核对状态列。

**🔴 复核方式**：本表**只是索引**，任何行动手前一律现读队列状态列（判据见 §〇.2），**不以本表为准**。

| 行 | 事项 | 状态（现读于 2026-08-04 夜） |
|---|------|------|
| **#246** | 5 处日期标为尚未到达的 `2026-08-05`；§5 时间戳纪律缺「写入用哪个基准」这半边 | **待领 P3 · 本线可自做**（本轮登记）。两条修法择一：只补口径 ／ 连同订正 5 处；**本线未擅改他人已完工行的日期**（近「历史不追改」边界，需先定性） |
| **#247** | §二 4 条批次「状态列模糊」每轮重复告警，最早一条已 49 轮 | **待领 P3 · 本线可自做**（本轮登记）。修法⑴数据侧（核 commit 在 master 后回填 ✅）／⑵判据侧（sweep 认该措辞 或 #225 `release` 在写入时拦）；**勿只做 ⑵ 不做 ⑴** |
| #235 | 定时任务源码备份脚本挂自动触发器（#169 残项） | 待领 P2 · CC。**行内那句「须等五件同车落地」条件已满足**（#219/#222/#225/#226/#230-1c 均已完成），现在可动 |
| #188 | 仓库外产物「真身↔镜像」无自动核对 | 待领 P2 · CC。**注意**：CC 在 08-04 平台杂项批已核实其核心机制被 #169 实质覆盖并补了回归测试，**剩余半边＝sweep 每小时自动检查**，仍留白 |
| #230 | 并发 session 互不可见：**1c ✅ 已完成**（acquire 回显 120 分钟内在场者，本轮实测生效）；**2d 待核** | 待领 P2 · CC。**2d 疑似已被 #238「sweep 批次隔离安全内核」实质覆盖**（08-05 落地），**本线未核实，勿直接当已完成**——领取前先比对 #238 交付内容与 2d 诉求 |
| #227 | 仓库外活载体游离于复检域外 | **①条文已改／②扫描脚本已落地**（08-05，`工具-仓库外载体扫描.py`，首次真实使用即命中 #242）；**③其余 artifact ＋ `.51` 四页面逐项排查仍未做** |
| #241 | dispatch「README 行→跟进信文件」判据只用「收信人+日期」，同日多封必歧义 | 待领 P2 · CC 平台。**每天真实重跳一次**；修法⑴README 行直接带文件名（推荐）。**不得改成「歧义时猜一个发出去」** |
| #245 | 跟进信「该起草而没起草」检测空白（#124 design D5 的 Non-Goal） | 待领 P3。建造思路 design 里已写清，不需重新设计 |
| #96 | `.51` 部署标准清单 CC 半边：存量端口收编 | 🟡 Cowork 半边已完成；CC 半边**依赖 #162 统一门户网关中间件**，非独立可做 |
| #101①／#165／#195／#196／#203／#205-B／#217 | openspec 治理与台面小件 | P2-P3 待领，见周计划 A6。**#217 是定时触发型**：观察窗 2026-08-09 满、08-10 那轮巡检核算，此前勿提前跑 |
| #208／#220／#221／#223／#224 | 平台测试失败 triage ／ 机器人 Mac 迁移（阻塞于 #170 G0）／ SC8 分支未并 master（**已降 P3，不得 ff-merge 那个分支**）／ SC8 草稿措辞 ／ FI2 R7（需唐燕萍签认） | **业务总线派发，非本线**（#208 已于 08-02 由 Shao Peishen 改判归业务总线） |
| #98 | 月度环境体检 | 常驻，**首期 2026-08-28**；判据＝清完三数 `1/1/1`。**⚠️ 当前 worktree 已达 13 个 linked**（08-04/05 多个并行 CC session 产物），届时工作量远大于第 0 期基线设想 |
| #124／#122 | 跟进信发信班次 ／ Dispatch v0.6 | #124 阶段二**已 apply 并归档**（08-05）；**#122 下次评估时点 = 2026-08-06（周四）** |
| §四 #44／#45／#46／#47／#48 | 队列专项清扫授权 ／ 两份 skill 重打包 ／ QD-B registry 编号错位 ／ IT部#6 抄送 ／ **`.51` 外部心跳探测做不做（发信班次上线硬前置）** | **等 Shao Peishen**；#48 是发信班次的硬前置，临期性最高 |
| §四#35 | R1 Champion | 🔕 **静默至 2026-09-25**，任何机制不得提醒 |

**本轮新完成**：**#232**（sweep 执行结果看板已上线，含顺带修复 artifact §四 数据源漏报 —— 详见该行台账三处静默失效判例）。

**已收口勿重复**（08-04/05 批量落地后更新）：#218／#164（并入#225）／#219／#222／#225／#226／#231／#185／#200／#209／#233／#234(1)／#236／#238／#227①②／#242／#244／#221（已降 P3）／#197／#192-194／#198／#199／#204／#206／#207／#125／#166／#163／#147／#168／#171／#180／#107／#126。

---

## 五、纪律段（引用，勿抄全文）

- **队列纪律**：开工必读、登记触碰区、收工必写；改队列前 `acquire`、改完 `release`；**编号一律用 `--reserve` 取，并使用它的返回值**（勿用预写的旧号——2026-08-04 因此撞号，见 #225 台账⑦）。
- **批次纪律**：Cowork 不 commit；改动登 §二 批次（状态列禁带 ✅、**清单须含队列文件自身**、须声明「本批变更参数 ＋ 已复检」、**复检域含四类仓库外载体**）→ 登记后立即触发 sweep。**清单须含队列文件自身**一项已达退休制阈值，机制化落点＝#225 校验②；**落地前仍须人守。**
- **直读原则**：新鲜度敏感的读取走本机 PowerShell；沙箱 `git status` 与 `prunable` 标记均为假象。
- **memory 写不进**：Cowork `spaces/*/memory/` 写不落盘 → 跨会话纪律一律落 CLAUDE.md。

---

## 六、反面清单（当反面读，已按规则退休制瘦身）

**已退休为机制的，只留指针**：
- 裸竖线致列数偏离 → **已并入 #225 校验①**（2026-08-04 第 4 次仍靠事后拦截，发生在"已判定机制化、CC 未落地"的空窗期）。
- 批次清单漏队列文件 → **已并入 #225 校验②**。

**仍是人守，须记（五条防线）**：
1. **判据本身也要被质疑一次** —— 换一种系统行为（快速启动／休眠／时区／清扫／锁忙／grep 用词／并发）下它还成立吗？
2. **不信"工具说成功了"，也不信"人说跑过了"** —— 写完必反查落盘；引用他人交付前自己复验一次（2026-08-04 复验 patch-id 即此）。 **🔴 2026-08-04 夜三连实证（#232 施工，判例全文见该行）**：⑴ `Edit` 对 `outputs` 目录**返回 success 但零落盘**（大小/mtime/命中数三项未变）；⑵ 沙箱 `bash` 看到的 outputs 文件**滞后两个版本**（本机 36930 vs 沙箱 34106），据它做 node 检查会检查错版本；⑶ PowerShell `$集合 -ne $null` 被重载为元素过滤，**返回值正常而结果全错**。 **共同形态：三者都不报错，只是安静地给出反向答案。** **✅ 已部分机制化（队列 #255，2026-08-06）**：一行可复制的"写后反查三件套"片段（`Test-Path`＋字节数变化＋关键词命中数）已落 `专线opener模板库.md` §〇.6；可选工具化版本 `0-学习与工具/工具-写后反查.ps1` 已建并真实验证"报 success 但零落盘"场景被正确拦下（非 0 退出码）——**该脚本自身建造过程中，`Edit` 工具对本项目 OneDrive 路径文件的写入再次静默失效**（报 success，`git diff` 为空），是本条防线本身在同一批工作里第二次真实兑现。**仍是人守的部分**：本条只提供工具，不强制所有写入都过它——记得用它，仍要靠这条防线本身。
3. **也要质疑自己刚下的判据** —— 本线多次推翻自己 20 分钟前写的东西。
4. **因果断言前先问「有没有一个我没核的变量能同样解释」** —— 排除不了就写"两种解释都可能"。**高危：结论对自己有利时、结果符合预期时。** 🔴 **且「如实标注未核」≠「可以据此下结论」**——标注只是免责不是授权；关键变量未核前应定"待核实"而非按最坏情形定级（#221 教训，已 2/3）。
5. **凡涉数量／位置／"有几处"的陈述，先 grep 再说** —— 说不出确切数字就说"我没数"。

**违反计数台账在队列 #225 行内**（七类，达 3 次即按 CLAUDE.md §5 规则退休制判机制化或删除）。

---

## 七、收工段

1. 滚动 `session接力-Phase1收口.md`；新发现当场登队列行（`--reserve` 取号**并用返回值**）。
2. 登记 §二 批次（含「本批变更参数 ＋ 已复检」）并立即触发 sweep；本线不自行 commit。**触发后必核 `ahead` 与脏文件**；**若被并发线未声明脏文件挡住，属预期，排队即可，勿替他人声明。**
3. session 过长主动提示转场，**并给单一可复制 opener**（有且只有一个 fenced 代码块）。**先压缩、后转场**；转场依据须可核验（他的实际消息数、本 session 落库的 commit 与批次），**禁止用自报工作量**。
4. **会话末固定小节「需你定夺」**：编号 ＋ 带字母选项 ＋ 每项写清选项间实际差异与代价 ＋ **标注默认项** ＋ 临期置顶 ＋ 并列悬置项；无则明写"本次无需你决策"。**两栏各归各位、双向都禁**：不得把纯状态混进决策项，**也不得把需他定夺的事塞进状态同步**。判据＝凡出现"等你定／请你选／待你拍板"字样一律属决策项。
