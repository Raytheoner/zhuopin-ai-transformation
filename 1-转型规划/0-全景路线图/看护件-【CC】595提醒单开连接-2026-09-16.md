---
title: "看护件 · B-0916_595提醒单开连接（单泳道）"
created: 2026-09-16
批次: B-0916_595提醒单开连接
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 2a）
---

# 看护件 · B-0916_595提醒单开连接

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#595` | 企微提醒脚本改为复用主服务连接，消除每小时 :18 后的断连 | wecom-aibot-service 连接与提醒脚本＋单测 |

不得碰 `.claude/settings.json`、hooks、计划任务运行时。

## 二、泳道

### A1 · §一 #595 提醒单开连接

▶ 粘贴端：CC ｜ 泳道：op0916r-aibot-conn-595

```
[OP-0916-R]【CC】提醒复用连接
【设置】执行环境：CC ｜ 分支：`claude/op0916r-aibot-conn-595`（新建自 master） ｜ worktree：☑（op0916r-aibot-conn-595，收工自删） ｜ 工作区：仅代码与单测，**不重启、不停启任何 Zhuopin* 计划任务、不向企微发任何消息** ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_595提醒单开连接
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916R-提醒复用连接。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #595（`python 0-学习与工具/工具-队列查询.py --row 595 --section 一 --field all`）` → ② `CLAUDE.md` → ③ `1-转型规划/0-全景路线图/队列行日志/#586.md` 尾段（grep 定位后分段读），按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按期望产出 ⑴→⑶ 做；⑷ 24 小时观察由 Cowork 合入重启后负责，泳道不做。先取证表再动代码；取证须用审计时间戳实证「单开连接 → 主连接断开」的时序，推翻则如实写回并 PARTIAL 停。
🔴 复用方案优先走主服务已有的发送通道（本机接口或进程内发送队列）；主服务不在线时提醒脚本应失败并记审计，**不得回退为自行建连**。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出交子代理。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short`，跑完即 commit。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916r-aibot-conn-595 --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916r-aibot-conn-595 --done --batch B-0916_595提醒单开连接 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用复合命令一次完成：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916R --section 一 --number 595 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；若报错锁仍占用，先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】595提醒单开连接-2026-09-16.md" -FullAuto -Yes -Detach
```