---
title: "看护件 · B-0916_600泳道隔离根治续三（单泳道）"
created: 2026-09-16
批次: B-0916_600泳道隔离根治续三
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 21:10「确保这个问题根治」；首棒 21:33、续棒 22:1x 越 150k 交接后续三）
---

# 看护件 · B-0916_600泳道隔离根治续三

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#600` | 无头泳道三道隔离：脚本建 worktree、钩子拒写主仓、收工核验主工作区 | v2.ps1、editlock-guard、queue-read-guard、opener骨架 worktree 段 |

## 二、泳道

### A1 · §一 #600 隔离根治续三

▶ 粘贴端：CC ｜ 泳道：op0916zz-lane-isolate-600c

```
[OP-0916-ZZ]【CC】隔离根治续三
【设置】执行环境：CC ｜ 分支：`claude/op0916y-lane-isolate-600`（既有分支续做：⑶ 已完成 6c68fd3；⑴⑵ 已完成 425097b） ｜ worktree：☑（op0916zz-lane-isolate-600c，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人运行时／计划任务） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_600泳道隔离根治续三
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916ZZ-隔离根治续三。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
🔴🔴 **开工第一条命令（先于读任何文件）**：在主仓根执行 `git worktree add .claude/worktrees/op0916zz-lane-isolate-600c claude/op0916y-lane-isolate-600`，随后所有读写与 git 命令都在 `C:\Dev\zhuopin-ai\.claude\worktrees\op0916zz-lane-isolate-600c` 内进行；每次改文件或 commit 前先跑 `git rev-parse --show-toplevel`，结果不是该 worktree 路径即立刻 `OPENER_PARTIAL: 停在🔴决策点——未在 worktree 内`。上一棒在主工作区直接改代码已被 Cowork 中止（09-16 20:55），**主工作区内除 reports/ 与收工回写外一律不许写**。
先读两份实证补丁 `reports/opener-batch/20260916-204736/596-main-leak.patch`、`599-main-leak.patch` 的文件头，理解泄漏形态。
读 ① `队列 §一 #600（`python 0-学习与工具/工具-队列查询.py --row 600 --section 一 --field all`）` → ② `CLAUDE.md`，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 本棒只做 #600 期望产出 ⑷⑸⑹（⑶ 6c68fd3、⑴⑵ 425097b 已完成，勿重做；先 `git show --stat 6c68fd3 425097b`）。顺序 ⑷ → ⑸ → ⑹，每项完成即 commit＋push，越 150k 立即交接。⑸ 必须补齐 v2.ps1 四种情形单测，并给出「无 ZHUOPIN_LANE_WORKTREE 时两钩子行为与 master 一致」的对跑证据写回状态列。
🔴 只动本行「触碰区」列所列文件；越界需求只登记不动手。不放宽任何锁／lint／登记／门禁判据。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出（全量回归、大范围 grep）交子代理只收结论。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short --maxfail=5`，跑完即 commit；差集回归与 master 同命令对跑。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916zz-lane-isolate-600c --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916zz-lane-isolate-600c --done --batch B-0916_600泳道隔离根治续三 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用一条命令：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916ZZ --section 一 --number 600 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；报错后锁仍占用则先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```




## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】600泳道隔离根治-2026-09-16.md" -FullAuto -Yes -Detach
```