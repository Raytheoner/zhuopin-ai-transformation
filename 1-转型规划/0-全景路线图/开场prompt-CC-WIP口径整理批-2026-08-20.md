---
status: 待执行
title: "开场prompt ·【CC】WIP 口径整理批 —— 摘 #282 的 🛑 ＋ 销 A 类四行 ＋ 上限按实测重定"
created: 2026-08-20
执行方: CC
派出线: Cowork 全景路线图线
编号: OP-0820-A
来源: Shao Peishen 2026-08-19 答「摘 🛑 ＋ 销 A 类四行 ＋ 按回收条款降上限」(a)；2026-08-20 答「现在出」(a)
---

# OP-0820-A ·【CC】WIP 口径整理批

## 开场词（复制即用）

```
【OP-0820-A】CC · WIP口径整理批：摘#282的🛑 + 销A类四行 + 上限按实测重定（Shao Peishen 已答 (a)）
【设置】执行环境：CC ｜ CC session：☑ 新建 ｜ worktree：☑ 新建 `wip-caliber-cleanup` ｜ 分支：master ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-CC-WIP口径整理批-2026-08-20.md 全文 ＋ 队列 §一 #282／#68／#234／#270／#279 五行 ＋ 队列文件顶部协议〇.9／〇.10 ＋ CLAUDE.md §5 恢复上下文，按本件三步执行。
```

## 本批要做的三件（一件也不能少，理由见下）

### ① 摘掉 §一 `#282` 的 `🛑` 首标记

2026-08-19 我给它加 `🛑` 的理由是「结构性不可动的 hold —— 他本人押后」。
**现在它已转 `[S:partial]`、⑴ 包 apply 已完成、正在动** ⇒ **`🛑`（结构性不可动）的原义不再成立，留着就是错的。**

🔴 **留着的代价不是「数字难看」，是「WIP 判断每次都偏松」** —— 那正是 2026-08-17 他拍板 `hold` 是否计入时说的「**这个口径本来就是对的**」要防的逃逸口：**改成不计数就能让数字好看**。

⚠️ **摘掉后 WIP 立刻 24 → 25，超上限**。这是预期内的，②③ 就是来解它的。**不要因为怕超限而不摘。**

### ② 销 A 类四行：`#68`／`#234`／`#270`／`#279`

**这四行都有前序 session 写好的销号建议与在案证据**（各行状态列末段可直接读到），且**都曾被指派给「2026-08-17 值周巡检」执行、那次巡检没做**：

| 行 | 前序结论原话（摘） |
|---|---|
| `#68` | 「注册卡点由 07-30 环境体检取证解除」…**销号后可动 WIP −1** |
| `#234` | 「⇒ 建议 2026-08-17 值周巡检据此销号（本线按 opener §二 硬边界 2 不擅自翻 `[S:done]`）。销号后机制类可动 WIP 22→21」 |
| `#270` | 「⇒ 建议 2026-08-17 值周巡检销号（本线不擅自翻）。**销号后可动 WIP −1**」 |
| `#279` | 「两行均已完成…销号 22→20，比合并降得多…两行留痕语义不同，归档价值独立」 |

🔴 **销号前逐行复核，不要照抄建议就翻** —— 那些建议写于 2026-08-12～08-17，**其间可能有新信息**。**若某行复核后发现不该销，如实登记不销，并说明原因。**

🔴 **销 `#270` 有一个必须同批处理的残留，漏了就是重演 `#345` 决策点 5 的反面**：
其剩余范围是「清理 `group_notify.py` 的 webhook 调用路径」（可选卫生项、归 CC）。**直接销号会让这一项失去主人。**
⇒ **同批并入 `#282`**（同治群广播出口下线，且 `#282` 此刻正好在动 ⑴ 包），并在 `#270` 销号说明里写明去向。
🔑 **「不做」与「不管」是两件事** —— 这正是 `#345` 决策点 5 当初拒绝顺手扩范围、同时要求另立一行的那条教训。

### ③ 🔴 上限**不要预设数字**，按销号后的实测值重定

今早 CC 把 `MECHANISM_WIP_CAP_DEFAULT` 由 16 调到 24 时，写了回收条款：
> 「A 类四行销号后本值应回落一档（20 即够）；**若销号做完而上限没跟着降，那就成了一个靠『忘了收回』维持的上限。**」

**但那个「20」和 Cowork 后来说的「降回 20」，都是预设值，不是实测值。**

🔴 **预设值这件事今天已经错过一次**：Cowork 在《机制类可动 WIP 盘点》§五 推荐上限 20，理由是「真在办 15 ＋ 排队 4 ＝ 19，留 1 格余量」——**那个 15 是 A 类四行销号之后的理想态数，而销号当天并未执行** ⇒ 当天实际 19（仍含那四行），再立 4 条 ＝ 23，按 20 会被 `release` 硬阻断。**CC 实测发现并报出，Shao Peishen 据此改定 24。**

**⇒ 本批不重复这个错误。做法：**
1. **先做完 ①②**，然后用 `_count_mechanism_wip` **实测**当前真实值 N。
2. 查 §一 是否还有**已排队待立但尚未落行**的机制类行（读 `session接力-Phase1收口.md` 顶部与近日批次行），记其条数 M。
3. **上限定为 N ＋ M ＋ 1**（1 格余量），**并在 commit message 与队列里写明这三个数各自是多少、怎么来的**。
4. ⚠️ **若 N ＋ M ＋ 1 ＞ 24**（即比现值还高），**不要上调**——那说明该做的是继续清扫而不是抬杠；此时**维持 24 并如实登记「本批未能降下来，原因是 …」**。

**四处载体须同批改**（今早调 16→24 时改的就是这四处，照同一清单）：
- `0-学习与工具/工具-共享文档编辑锁.py` 的 `MECHANISM_WIP_CAP_DEFAULT` 常量（**及其上方注释里的实测值说明**，那句话今天已经过时过一次）
- `1-转型规划/0-全景路线图/专线opener模板库.md` §⑵
- `0-学习与工具/skills源码/zhuopin-kickoff-prompt/SKILL.md`（**改完须 Cowork `save_skill --overwrite` 同步已安装版，CC 做不了 ⇒ 在收工报告里明确交回**）
- `openspec/specs/…/editlock-mechanism-wip-guard` spec

## openspec 判定

- **①②** 是队列内容维护，不触发。
- **③** 改的是常量默认值与配套文档。今早 16→24 那次由 CC 直接改值、未走变更包（先例在），**本批照同一路径**；但**若你在实施中发现它已牵动判据语义（而非仅阈值），停下来走 openspec**。

## 收工

- §一 `#282`（摘 `🛑`）／`#68`／`#234`／`#270`／`#279` 逐行回写；**`#270` 的残留项去向必须写明**。
- §二 登记批次，**文件清单每条必须是仓库根相对完整路径且反引号包裹**。
- **收工报告里给出三个数**：销号前 WIP、销号后 N、新上限及其算式。
- 🔴 **`zhuopin-kickoff-prompt` 已安装版同步是 Cowork 能力，明确交回，不要写成已完成。**
- **若任一行复核后判定不该销，如实登记，不要为了凑数字而销。**
