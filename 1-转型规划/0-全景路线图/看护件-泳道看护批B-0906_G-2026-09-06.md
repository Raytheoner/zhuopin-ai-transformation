---
title: "看护件 · 泳道看护批 B-0906_G（第十三批：机制环境侧五泳道——一条止损核验＋两条WIP余量激活＋两条小件建造）"
created: 2026-09-06
批次: B-0906_G
派出线: Cowork 业务总线 OP-0906-G
LAN: off-LAN（探针三项未齐，本批候选已按此排除依赖内网项）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0906_G`

## 〇、本批口径

**立批依据**：Shao Peishen 2026-09-06 问「这两天环境侧有很大进展，可否有新看护件」——先跑前置四查（LAN 探针／`git fsck`+`fetch` 对齐／编辑锁空闲／上一批 B-0905_C 汇总已出，四项皆过），再用 `工具-队列查询.py --digest` 扫两份队列真身，按 D1 判据（`工具-泳道看护状态机.py criteria` 现取）筛出本批候选。

**WIP 现状**（`_count_mechanism_wip` 现取，非估算）：机制类可动 **20/22**，余量 2。§一 `#479`～`#483`（2026-09-03 批次 `B-0903_80`）五行曾因 WIP 顶格集体标 🛑，现有余量，本批**只激活其中一行**（`#480`，独立触碰区、无需连带其余四行），不占满余量、给下一批留一个位子。

**本批发现一处队列行陈旧**（如实登记，不是新发现的机制缺陷）：§一 `#459`（editable `.pth` 盲区判据）的行内自陈仍停在「待业务总线派发实现泳道」，但 `git log` 实测其实现已随 commit `2635cd5`（2026-09-05，"#459：落库sweep 补 pth 盲区判据实现，配套单测覆盖"）合入 master——**本批 A1 先核实这条 commit 是否真的满足 `#459` 设计正本的验收条件，核实通过即销号，不通过则如实报出缺口**，不假设"合了就等于对了"。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界

1. 🔴 **所有 `pause`／`check-heartbeat` 一律带 `--no-notify`**（2026-09-02 首跑事故硬规则；运维群接线 `#492` 尚未落地，本批一律走 Cowork 会话内「需你定夺」，不推送）。
2. 🔴 A2（`#487` 子项）与 A5（`#480`）都会碰 `opener骨架.md`／`工具-共享文档编辑锁.py` 这类格式或写侧权威文件——**改判据本身须停等你一个字母，不得自行拍板**（同 `#487` 原条 (甲) 先例）。
3. 🔴 看护会话本批不修工具——A1 只读核验，A3/A4 是全新建造非"顺手修"，A5 是已批复交付件的独立实现，均不属于"跑批期间发现缺陷就地修"的例外范围。
4. 🔴 真实库只读起步；不部署 `.51`；不发任何对外消息；不碰 oem-chroma 与 `OEM数据隔离规范.md`；不碰他线在办行（`#443` 值周清扫「须有人看着」、`#358` 待走 design 的 audit 归档、`#454` 若发现需要 design 审即停不代拍板）。
5. 🔴 不改队列文件里已有的历史回写段——只追加新结论。

---

## 二、波次与触碰区矩阵

三条互不重叠的泳道并行起（波一），`#454`／`#480` 因同碰 `工具-共享文档编辑锁.py` 改走串行（波二、波三）：

| 波 | 泳道 | 承接 | 触碰区 |
|---|---|---|---|
| 1 | `459-verify` | A1 | 只读核验 `#459` 是否已被 `2635cd5` 满足（不写代码） |
| 1 | `487-dont-guard` | A2 | `--dont` 在【Cowork】骨架下静默丢弃——起草二选一方案，停等你选 |
| 1 | `486-claude-probe` | A3 | Claude 桌面端包版本错位只读探针（新文件，无既有触碰） |
| 2 | `454-status-triage` | A4 | 状态分诊接常驻轮次 ＋ §一「待拍板」自陈汇总进 §四 |
| 3 | `480-credential-guard` | A5 | 队列写入侧凭据形状即拒闸（等 A4 释放 `工具-共享文档编辑锁.py` 后再动） |

---

## 三、泳道 opener

### A1 · 459-verify——核验 #459 是否已被 2635cd5 满足（只读，不写代码）

粘贴端：CC ｜ 泳道：459-verify

```
[OP-0906-H]【CC】核验459已合
【设置】执行环境：CC ｜ 分支：master（只读，不建分支）｜ worktree：☐（纯只读核验，不建造）｜ 工作区：无（不触碰 `.51`、不发消息）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0906_G
读 ①〈队列 §一 `#459`〉（`工具-队列查询.py --row 459 --field all`）→ ② `openspec/changes/editable-pth-blindspot-guard/`（design 正本，已获 Shao Peishen 3a 批准）→ ③ commit `2635cd5`（`git show 2635cd5 --stat` 与 `git show 2635cd5 -s`）→ ④ CLAUDE.md 恢复上下文。本件为 A 类（纯核验，判据已写死：commit 内容 vs design 验收条件逐条对），无需再问澄清，直接开工。

背景：`#459` 行内自陈仍停在"待业务总线派发实现泳道"，但 `2635cd5` 的提交信息明写"#459：落库sweep 补 pth 盲区判据实现，配套单测覆盖"且改动的正是行内指针文件 `工具-落库sweep.py`。**这看起来是行没跟上实况，但不假设——本泳道去核实。**

做什么：
1. 逐条核对 `openspec/changes/editable-pth-blindspot-guard/design.md`／`tasks.md` 的验收条件，与 `2635cd5` 的 diff（`git show 2635cd5`）逐项对：判据是否真覆盖 `.pth` 直写目录形态（无 finder 模块）、单测是否覆盖该形态、是否有 openspec 要求但代码未做到的缺口。
2. 跑一次相关单测确认真的绿（`pytest 0-学习与工具/test_工具-落库sweep.py -q` 或 design 里点名的具体测试类）。
3. 若逐条对上：在队列 `#459` 行内追加事实更正（`git show` 取证，commit 哈希、验收条件逐条勾对），转 `[S:done]`。
4. 若有缺口：如实列出缺了哪条验收条件，**不代为决定要不要补**，登记后交总线。
5. 收工：`OPENER_DONE` 或 `OPENER_PARTIAL`（若发现缺口需总线判断去留）。

不做什么：
- 🔴 不写任何代码、不建 worktree——本泳道纯只读核验＋队列行回写。
- 🔴 不擅自判定"这就算做完了"——只报事实对得上还是对不上，缺口大小交总线判。
```

### A2 · 487-dont-guard——`--dont` 在【Cowork】骨架下静默丢弃（起草二选一，停等你选）

粘贴端：CC ｜ 泳道：487-dont-guard

```
[OP-0906-I]【CC】dont参数静默丢弃
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906i-dont-guard-487`）｜ worktree：☑（`dont-guard-487`，新 worktree，收工自删）｜ 工作区：无（纯库内起草，不动产线场景代码）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0906_G
读 ①〈队列 §一 `#487`〉子项"【Cowork】骨架缺'不做什么'段，`--dont` 被静默丢弃"（`工具-队列查询.py --row 487 --field all`）→ ② `1-转型规划/0-全景路线图/opener骨架.md`【Cowork】骨架节 → ③ `0-学习与工具/工具-opener生成.py`（`--dont` 参数处理逻辑）→ ④ CLAUDE.md 恢复上下文。本件为 B 类——两个候选修法各有代价，行内已写"由领取方判并写明理由"，但改的是格式权威正本，止步于起草不代拍板。

背景：`--dont` 传给 `--env Cowork` 时不生效也不报错，起草者以为约束传达到了，实际被静默丢弃；已实撞一次（三条约束靠接力卡侥幸兜住）。

做什么：
1. 起草**两版**方案，都做到"可直接照做"的程度（不是空谈利弊）：
   - **(甲)**：给【Cowork】骨架补"不做什么"段（改 `opener骨架.md` 正文 ＋ 同步 `工具-opener块lint.py` 的形态判据，使其认得【Cowork】骨架也该有这一段）。
   - **(乙)**：`工具-opener生成.py` 在 `--env Cowork` 且传了 `--dont` 时直接报错退出（fail-loud），不改格式正本。
2. 各自写清：改动范围（哪些文件）、对已有【Cowork】opener 的兼容性影响、后续新增字段时的维护成本。
3. 两版都跑一次自检（甲：`工具-opener块lint.py --file` 对着补了"不做什么"段的样例块；乙：手动构造 `--env Cowork --dont "x"` 确认真的报错退出）。
4. 收工：`python 0-学习与工具/工具-泳道看护状态机.py pause --batch B-0906_G --wave 1 --lane 487-dont-guard --action-key change_criteria --waiting-for "#487 子项二选一：(甲)补骨架格式 或 (乙)生成器fail-loud，已各自起草+自检" --no-notify`，随后 `OPENER_PARTIAL: 停在🟡决策点——(甲)/(乙) 二选一`。

不做什么：
- 🔴 不擅自选定一版并直接落地——两版都只到"起草+自检"，最终选哪版等你答。
- 🔴 不改 `opener骨架.md`【CC】骨架节——那一节已有"不做什么"段，不受影响。
```

### A3 · 486-claude-probe——Claude 桌面端包版本错位只读探针

粘贴端：CC ｜ 泳道：486-claude-probe

```
[OP-0906-J]【CC】Claude包版本探针
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906j-claude-probe-486`）｜ worktree：☑（`claude-probe-486`，新 worktree，收工自删）｜ 工作区：无（纯只读诊断脚本，不改任何服务配置、不提权）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0906_G
读 ①〈队列 §一 `#486`〉（`工具-队列查询.py --row 486 --field all`）→ ② CLAUDE.md 恢复上下文，按行内「期望产出」执行。本件为 A 类（范围已写死：只读探针+开场横幅接线+单测），无需再问澄清，直接开工。

做什么：
1. 新建 `0-学习与工具/工具-Claude包版本探针.ps1`：核对比三者——`Get-AppxPackage Claude` 已注册版本／`Win32_Service CoworkVMService` 的 `PathName` 里的包版本／`WindowsApps` 下已暂存未注册的 `Claude_*` 目录版本，三者不一致即输出提示（先跑 `Restart-Service CoworkVMService -Force`，勿直接开长会话）。**纯只读，不自动改服务、不提权。**
2. 接入 CC SessionStart 开场横幅，三者不一致时多打一行提示（只读扩展现有横幅逻辑，不改其余行为）。
3. 配套单测（构造三种版本值组合：全一致/其中一个不一致/全不一致，断言脚本输出正确判定）。
4. 队列行日志 `1-转型规划/0-全景路线图/队列行日志/#486.md` 落取证经过（行内已提到"待 CC 落"）。
5. 收工：回写 `#486` 行状态 + 登记 §二 待 commit 批次。

不做什么：
- 🔴 不自动执行 `Restart-Service`——只提示，动手交给他本人。
- 🔴 不碰 CC 开场横幅里 §一待领摘要等其余既有逻辑，只加这一行。
```

### A4 · 454-status-triage——状态分诊接常驻轮次 ＋ §一自陈汇总进 §四

粘贴端：CC ｜ 泳道：454-status-triage

```
[OP-0906-K]【CC】状态分诊常态化
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906k-status-triage-454`）｜ worktree：☑（`status-triage-454`，新 worktree，收工自删）｜ 工作区：无（纯库内建造，不触碰 `.51`、不发消息）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0906_G
读 ①〈队列 §一 `#454`〉（`工具-队列查询.py --row 454 --field all`）→ ② `0-学习与工具/工具-共享文档编辑锁.py::_suggest_status_reclassification`（已建，2026-08-30 `#435` 子项 E）→ ③ CLAUDE.md 恢复上下文。本件为 B 类——「分诊候选接进哪个常驻轮次」这一实现细节需要你判断，若发现命中 openspec 三门槛之一（改变既有模块对外语义等）**止步于 design 起草，不直接 apply**；若判定不命中门槛，可建到底。

背景：分诊器 `_suggest_status_reclassification` 已建、单测齐全，但只在 `wip_count > cap` 时被调用、只对被拦下的那个 session 显示，且被拦 session 无权改他人的行——闭环死锁。行内点名要修两件：⑴ 分诊候选接进常驻轮次并推送；⑵ §一 行内"待 Shao Peishen…"自陈自动汇总进 §四。

做什么：
1. 判定本次改动是否命中 openspec 三门槛（① 改变全项目口径 ② 改变对外可见性 ③ 改变既有模块对外语义）——`_suggest_status_reclassification` 从"被动附在 release 拒绝消息里"改成"主动常驻轮次调用"是否算改变对外语义，自行判断并在收工回写里写明结论。
2. 若不命中门槛：实现⑴⑵，配单测（构造"分诊候选存在但当前 session 未被 WIP 拦"的场景，断言候选仍会被汇总进某处可见载体；构造"§一行内含'待 Shao Peishen'自陈"场景，断言被汇总进 §四）。
3. 若命中门槛：只起草 openspec proposal+design，止步于此，`pause --action-key openspec_design_review`。
4. 接入点选择需你留意：不要新建一套独立的定时轮次——复用现有 sweep 轮次或看护批常驻扫描器（`#462`）的调用点，避免"两套判据各自轮询"重蹈 `#366` 教训。
5. 收工：若建到底，回写 `#454` 行 `[S:done]` + 登记 §二；若停在 design 审，`pause --no-notify` 并 `OPENER_PARTIAL`。

不做什么：
- 🔴 不改 `_count_mechanism_wip` 本身的判据——只改"分诊结果怎么被看见"，不改"WIP 怎么算"。
- 🔴 不替他人队列行做改判决定——分诊只产出"建议候选"，改判仍须行的领取方或总线自己拍板。
```

### A5 · 480-credential-guard——队列写入侧凭据形状即拒闸（WIP 余量激活）

粘贴端：CC ｜ 泳道：480-credential-guard

```
[OP-0906-L]【CC】凭据形状即拒闸
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906l-credential-guard-480`）｜ worktree：☑（`credential-guard-480`，新 worktree，收工自删）｜ 工作区：无（纯库内建造，不触碰 `.51`、不发消息）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0906_G
读 ①〈队列 §一 `#480`〉（`工具-队列查询.py --row 480 --field all`）→ ② `0-学习与工具/工具-密钥扫描lint.py`（既有形状规则，复用不重写）→ ③ CLAUDE.md 恢复上下文，按行内「做什么」执行。本件为 A 类（口径已定：2026-09-03 Shao Peishen 已答"都按你说的"，`B-0903_80` 批次），无需再问澄清，直接开工。⚠️ 本行原因 WIP 顶格标 🛑 排队，本批因机制类可动 WIP 现测 20/22（有 2 空位）而激活，激活依据见本看护件 §〇。🔴 本泳道排在波三——等 A4（`454-status-triage`）释放 `工具-共享文档编辑锁.py` 后再动手，避免同文件并发改动。

做什么：
1. 写入侧（`append-row`／`edit-row`）接 `工具-密钥扫描lint.py` 既有形状规则：内容命中凭据形状（企微 webhook key／token／私钥等）即拒绝写入并点名具体命中了哪条规则。
2. 反例单测：粘一条形状合规的假 key（不是真凭据，纯测试用固定假串）必须被拒；正例单测：正常队列文本不受影响。
3. 拒绝文案给出路："凭据请落 `.env`，队列只写指针"。
4. 判定是否命中 openspec 三门槛——若命中，止步于起草，`pause --action-key openspec_design_review --no-notify`。
5. 收工：视 4 的判定回写 `#480` 行状态 + 登记 §二。

不做什么：
- 🔴 不碰 `工具-密钥扫描lint.py` 本体——只复用其形状规则，不重写第二份判据。
- 🔴 不代 `#479`／`#481`／`#482`／`#483` 去 🛑——本批只激活本行，其余四行仍排队，理由见 §〇（WIP 余量不足以一次全放）。
```

---

## 三bis、看护opener（单次粘贴，Task/Agent 工具起五子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0906-G]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0906-G]【CC】看护B-0906_G
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，五条子泳道各自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0906_G
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0906G-看护五泳道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余五条泳道均由你用 Task/Agent 派发，正文里不放这一行——2026-09-05 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0906_G-2026-09-06.md 全文（§〇～§五）＋ CLAUDE.md 恢复上下文。

你是本批的**看护者**，不是执行者。

【怎么起活】波一（A1/A2/A3）用 Task/Agent 工具各起一个子任务，`isolation: "worktree"`（A1 例外——它是纯只读核验，不建 worktree，直接用 Task/Agent 派发即可，不传 `isolation`），错峰 1-2 分钟；把 §三 里对应那条 opener 的正文**原样**作为子任务 prompt。🔴 不要改写 opener 正文。波二（A4）等波一全部到达终态（`OPENER_DONE` 或 `OPENER_PARTIAL`）后再起，波三（A5）等 A4 到达终态后再起——A4/A5 同碰 `工具-共享文档编辑锁.py`，不得并行。

🔴 子任务返回"已完成"时不要只信它的自述——请你亲自核实分支是否真的 push、文件是否真的写出、单测是否真的跑过。收工汇总每条都要附你亲自核实的证据（分支名、commit 哈希、或文件路径），不要只转述子任务自己说的话。

【看到这些要停下来问他，不要自行决定】
- A2（`487-dont-guard`）预期会命中 🟡（`change_criteria`，二选一方案）——原样转给 Shao Peishen，一个字母能答的形式。
- A4／A5 若发现命中 openspec 三门槛——原样转出，不代判。
- 任何子任务命中 ⏭️／🔴——标注去向、不问、不执行，转出。

【收工汇总】五条子泳道到达终态后，产出一份汇总，**每条都附亲自核实的证据**（不是转述），含 `#459` 核验结论（对上/有缺口）。

【你自己不做的】不写业务代码、不写队列行、不替子任务的产出做 commit、不碰 `.51`、不发任何对外消息、不碰 oem-chroma 三个批次与 `OEM数据隔离规范.md`、不代其余四行（`#479`/`#481`/`#482`/`#483`）去 🛑。
```

---

## 四、收工

跑
```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0906_G
```
现取，不手工数，队列回写走锁（不推企微——`#492` 运维群接线未落地，本批一律 Cowork 会话内呈现）。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#479`／`#481`／`#482`／`#483`（同批 `B-0903_80` 其余四行） | 仍排队 🛑，WIP 余量本批只够放一行，已选 `#480`；余量恢复后可续放 |
| `#443`（每周清扫，280 行待迁） | 他原话「须有人看着、不得无头跑」——不进本批，等他明示开跑 |
| `#358`（audit 留存与归档策略） | 须先走 openspec design 审（命中门槛①），本批未起草，下一批可考虑作为纯 openspec_draft 泳道 |
| `#462`（规划倒逼开工扫描器） | 仍 🛑 排队（2026-09-02 WIP 超限时标注，核心逻辑已在取证件里跑通，可作为下一批候选） |
