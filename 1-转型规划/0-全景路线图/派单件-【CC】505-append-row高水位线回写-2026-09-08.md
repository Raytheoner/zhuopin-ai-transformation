---
title: "派单件 ·【CC】#505 `append-row --number N` 不回写编号高水位线（OP-0908-K）"
created: 2026-09-08
status: 待粘贴
派出线: Cowork 环境总线 OP-0907-AL
承接: 队列 §一 #505（2026-09-08 20:2x 去 🛑，WIP 实测 20/22→21/22）
---

# 派单件 ·【CC】#505

▶ 粘贴端：CC（新开）

▶ 首次派出：[OP-0908-K]

```
[OP-0908-K]【CC】505高水位线回写
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0908k-appendrow-hwm-505`）｜ worktree：☑（505-appendrow-hwm，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0907-AL
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0908K-505高水位线回写。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① 队列 §一 `#505`（`python 0-学习与工具/工具-队列查询.py --row 505 --section 一 --field all`，任务／实证／期望产出全在该行）→ ② `CLAUDE.md` §3／§5 与 `.claude/rules/队列与落库.md` 恢复上下文，按该行执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 前置：`git log origin/master --oneline -20` 核 `#482` 串行闸 apply（分支 `op0907aj-serial-gate-482-apply`，同触碰 `工具-共享文档编辑锁.py`）是否已 ff 入 master；未 ff ⇒ 先 `pause` 报一句、不在旧基线上改。
2. 修 `0-学习与工具/工具-共享文档编辑锁.py`：`append-row --number N` 写入成功后与 `--reserve` 走同一条高水位线回写路径（两个入口对同一计数器行为一致）；回写失败 fail-loud、不静默。先判是否命中 openspec 门槛③（改既有模块对外语义）：命中 ⇒ 出 propose＋design 即停待审；不命中 ⇒ 直接修＋单测，在收工汇总写明判定依据。
3. 单测：`0-学习与工具/test_工具-共享文档编辑锁.py` 加两组——⑴ `--number` 写入后高水位线＝新行号；⑵ 紧接 `--reserve` 取到的号不撞。全量回归（现 340 passed）零回归。
4. 收工：push 自己分支（不 ff master，🟡 待他一字母）；走锁回写 `#505` 状态列（commit、单测数、门槛③判定）＋登记 §二；收工汇总末附「待你一字母」清单与答复模板。

不做什么：
- 不 ff master、不在主仓 commit；不改 `--reserve` 既有语义；不碰 sweep（`#500` 在跑泳道触碰区）。
- 不修 `#489`／`#464`（各有其行）；不复述队列协议。
```
