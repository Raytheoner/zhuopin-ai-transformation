---
status: 待执行
title: "开场prompt ·【CC】#282 ⑴ 包启动：sweep 机制告警改读 _OPS 止血（§四 #73 答 (c)）"
created: 2026-08-19
执行方: CC
派出线: Cowork 全景路线图线
编号: OP-0819-G
来源: §四 #73，Shao Peishen 2026-08-19 答 (c)——并进 #282 的 ⑴ 包作为第一个动作先做掉
---

# OP-0819-G ·【CC】`#282` ⑴ 包启动 —— sweep 机制告警止血

## 开场词（复制即用）

```
【OP-0819-G】CC · #282 ⑴包启动：sweep机制告警改读_OPS止血（§四 #73，Shao Peishen 已答 (c)）
【设置】执行环境：CC ｜ CC session：☑ 新建 ｜ worktree：☑ 新建 `sweep-ops-webhook-cutover` ｜ 分支：master ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-CC-282止血并入包一-队列四73-2026-08-19.md 全文 ＋ 队列 §一 #282 全行 ＋ §四 #73（含 2026-08-19 两段更正）＋ 3-治理与合规\通知通道架构决策件-webhook退役与aibot单一出口-2026-08-06.md ＋ CLAUDE.md §5 恢复上下文。**先做第 0 步前置核查，核不过就停下回报。**
```

## 为什么是现在、为什么走 (c)

**正在发生的事实（2026-08-19 首次量化）**：采购内部工作群**累计已收 58 条**机制告警（宽口径统计 `sweep-commit.log`），当天 2 条——其中 `07:37Z` 那条**正文是一段 Python traceback**（`CalledProcessError: git add … exit 128`）。**一段 git 调用栈落在业务同事的工作群里**，这就是 `#282` 拍板「业务部门此后不从任何 webhook 收消息」要消灭的那件事的具体形态。

**三选一里他选了 (c)**：不新开一条路径（合协议〇.10 并入优先），也不必等 `#282` 的 hold 解除——**把止血作为 `#282` ⑴ 包的第一个动作先做掉**。

🔴 **(c) 的一个关键属性**：它**并进既有行 `#282`、不新取号 ⇒ 不推高机制类可动 WIP**（当前 24/24 已满）。**若你发现自己需要取新号，说明范围理解错了，停下回报。**

## 第 0 步 · 前置核查（🔴 核不过就停手，不要硬做）

`#282` 当前是 `[S:hold]`，2026-08-08 押后的两条理由须逐条复核：

1. **理由①「与 `#308` 撞车」** —— `#308` 触碰区含 `0-学习与工具/工具-落库sweep.py`，与本次同文件。
   **Cowork 的判断（须你验证，不要直接采信）**：`#308` 的 apply 已完成、核心实现已落，**目前没有任何活跃 session 在改 sweep.py**；它只在等 owner 裁定 `tasks.md` 8.3 ＋ 观察窗口（告警到期约 **2026-08-24**，Shao Peishen 2026-08-17 选 (c) 让它按期响）。⇒ **撞车的实质是「两个 session 抢同一文件」，而那个条件已不成立。**
   **验证方式**：查 `#308` 状态字段、查有无在办 worktree/分支在改 sweep.py、查 `openspec/changes/queue-status-machine-field/` 是否还有人在动。
   ⚠️ **若发现确有活跃 session 在改 sweep.py，立即停手回报** —— 别在生产工具上撞车。
2. **理由②「机制类总量压制期」** —— 本次**并入既有行、不取号**，不增 WIP。**核一次当前 WIP 值并记录**（`_count_mechanism_wip`）。

## 任务

### ⑴ 主体：sweep 机制告警的 webhook 通道切到 `_OPS`

🔴 **范围已由 Cowork 复核更正，按这个写，不要按「只改一个常量」**（§四 #73 内有该更正的完整记录）：

- **功能侧 1 处**：`0-学习与工具/工具-落库sweep.py` **L437** 常量 `WECOM_WEBHOOK_ENV_KEY`。
  **10 处调用点全部经 `_load_webhook_url(repo_root)`**（L992／L1010／L1112／L1153／L1179／L1772／L1796／L1842／L2057／L2707，Cowork 逐个数过恰为 10），**一处不用碰**。
- 🔴 **文案侧 12 处 ＋ 1 处 docstring**：`WECOM_WEBHOOK_URL` 被写成**字面量**（L994／L1012／L1042／L1114／L1155／L1181／L1774／L1798／L1844／L2062／L2073／L2709）。**只改 L437 会让日志说「未在 .env 找到 `WECOM_WEBHOOK_URL`」而实际找的是 `_OPS`** ——不影响功能但会误导日后排查，**同批改**。
  🔑 **这是「同一事实的两份副本，改一处漏另一处」的又一例**（README 编号段三次复发、`#345` 的 35 份手抄副本），只是这次副本是**日志里的键名字面量**。

**⚠️ 先自行复核这份行号清单**（文件可能已变动），以你实测为准并在收工报告里说明差异。

### ⑵ 回退与降级语义（须在 design 里明确）

- `_OPS` 未配置时怎么办？**建议 fail-loud 而非静默回退到裸键** —— 静默回退正是把告警发回业务群的路径。
- 但 `_OPS` 未配置时 fail-loud 会不会让 sweep 整轮中止？**参考 `#328`「起跑段阻断整轮」那族教训**，告警发送失败不应掩盖 sweep 本身已完成的事实。

### ⑶ 顺带核一件已并入 `#282` 的事（不一定本批做，但要在 design 里给出处置意见）

`5-平台底座/wecom-aibot-service/scripts/alert_webhook.py:19` 加载 `5-平台底座/.env`、`:42` 读裸 `WECOM_WEBHOOK_URL`，而**该文件里这个键值长为 0、mtime 停在 2026-07-16 建服务当天** ⇒ **这条「第三道防线」告警通道从建成起就没活过**。它与本次同属 `#282` ⑴ 包范围。

## openspec

🔴 **`#282` 行内明写须走 openspec 含 design 审**（命中 CLAUDE.md §5 门槛①改变全项目口径 ＋ ③改变既有模块对外语义）。
**⇒ propose ＋ design 后停下等 Shao Peishen 审，不得直接 apply。** design 里至少要答：⑵ 的降级语义、⑶ 的处置、以及「本次是 ⑴ 包的第一个动作，⑴ 包其余部分（六处调用点迁 aibot 单一出口）是否同批」。

## 验收（apply 阶段用，本次 propose 不做）

1. **真实发一条 sweep 机制告警**，确认它**进的是运维群**（`运维部AI保障群`）。
2. **确认采购内部工作群不再收到** —— 这是止血是否成功的正判据。
3. 全量回归零漂移；`openspec validate --all --strict`。

## 收工

- 回写 **§四 #73**（写明 propose/design 已出、等审）＋ **§一 #282**（状态字段由 `hold` 转 `open`／`partial`，并写明 ⑴ 包已启动、押后理由①经复核已不成立）。
- §二 登记批次，**文件清单每条必须是仓库根相对完整路径且反引号包裹**。
- 🔴 **URL 值一律不落任何文件、报告、commit message**（只用指纹）。
- **若第 0 步核不过，如实登记未开工、不要写 ✅。**
