---
title: "开场 Prompt · 规划端同步 + 默会知识纠正收口（CC 交接）"
created: 2026-07-04
status: 已执行归档
交办人: Paul（分管供应链与质量 VP）
执行方: CC（Claude Code Desktop，LAN，可跑代码/连真实库/git push/发企微）
来源: 《全域健康度体检与松耦合加速评估报告-2026-07-04》+《默会知识瓶颈对照与全方位纠正方案-2026-07-04》（Paul 已批"全部执行"，规划端已由 Cowork 落地）
用途: ① 把 Cowork 在工作区留下的规划端改动审核入库；② 收口移交给 CC 的全部代码/工程侧动作
---

# 开场 Prompt：规划端同步 + 默会知识纠正收口

> 用法：CC 新会话读本文件 + 根 `CLAUDE.md` 当前进度 + `session接力-Phase1收口.md`【2026-07-04 晚】节恢复上下文。开干前问 Paul 2-3 个澄清（建议问：批改会日期是否已约、任务 C/D 是否本会话一并做、P2 杂项是否单独 session）。

## 〇、背景一分钟

2026-07-04 Cowork 完成两件事：全域健康度体检（4 路只读审计）+ 默会知识纠正方案（对照《AI无法大规模落地根本原因》一文）。Paul 批准"全部执行"，Cowork 已在规划端落地七处文档改动（**直接改在工作区，未 commit**——Cowork 云端沙箱推不了 git）。你的任务分两段：**A 段把规划端改动审核入库；B 段收口移交 CC 的工程动作。** 全程无任何场景排期变更，不触发排期同步纪律的跨文档重排。

## 一、A 段 · 规划端改动入库（先做，15 分钟）

1. `git pull` → `git status` 查看工作区未提交改动。预期涉及：
   - `1-转型规划/0-全景路线图/`：全景规划（§0.2 新行+§1.4 成熟度纪律+S2/S3 流程链+四档口径/批次制备注+§6.2 双轨 KPI）、实施计划（§二批次视图+§七.2 第 6 条+版本 2026-07-04）、前置总表（升级知识资产台账）、**甘特图改名** `全景甘特图-40场景排期.html → 全景甘特图-42场景排期.html`、session接力（滚动到 07-04 晚）、全景路线图重组机制与变更日志（甘特引用同步）、两份新报告（体检+纠正方案）、本 prompt 文件
   - `1-转型规划/`：FI1-对接人首周收口单/缺料预警校准需求/质量旗舰开工就绪包（三份就绪单三步法改写）、质量域启动会材料（甘特引用）
   - `6-人才与组织/`：部门AI专员协同一页纸.md（新建）
2. 逐文件 `git diff` 快速过目（重点核对：**无任何场景月份被改动**、场景总数仍 42）。
3. `git add -A`（rename 会自动识别）→ 一次提交，建议 message：
   `docs(governance): 全域体检+默会知识纠正全部执行——成熟度纪律/流程链/四档口径/批次制/知识资产台账/三步法就绪单/专员一页纸（排期零变更，§0.2 已登记 2026-07-04）`
4. `git push`。若与远端冲突（理论上不应有），停下报 Paul。

## 二、B 段 · CC 工程收口（按优先级）

### 任务 1 🔴 QD-B 接入平台 audit（红线，内部试用前提，最优先）

- **现状实证**：QD-B 全场景 `grep -rl zhuopin_platform` = 0 处（体检 2026-07-04 实测）；其场景 CLAUDE.md §3 已声明要接 `zhuopin_platform.audit.AuditLogger`，但代码未落地。
- **要求**：在既有变更包 `qd-b-project-gate-review` 内做（原任务序列里 audit 属"③ 报告聚合+audit+L2"，**现将 audit 接入单独提前**，不等报告聚合）——`pip install -e` 底座后，每次规则判定/评分/试评运行写 `AuditEvent`（含规则版本 2026-07-03、样本标识、逐条判定结果与置信度），append-only。
- **验收**：跑一次华丰试评 → audit JSONL 出现完整判定链 + `verify_chain` 通过；既有 25 tests 全绿不回退；tasks.md 对应项勾销。
- **红线**：华丰样本继续 gitignore（含报价）；OEM 信息附件走 data_isolation_layer 的既有决策不变。

### 任务 2 🟠 L2 门禁加"改判原因"字段（判例采集器，性价比最高项）

- **需求**：凡 L2 人工确认/改判路径（SC8 `approve` 审批、FI1 超阈人工结案、QD-B 未来 L2、保供案例处置中心状态机），在既有 audit 事件上**增加一个可选字段 `override_reason`（人工改判原因，自由文本）**；有 UI 的（保供 Web 处置中心）加一个输入框，无 UI 的走调用参数。
- **边界**：平台底座级小改（audit 事件模型 + 各消费方透传），**不改判定逻辑**。走 openspec 小变更包（如 `l2-override-reason-capture`），proposal+tasks 二件套即可（小场景轻量通道首例，design 以要点清单代替全文——2026-07-04 纠正方案 §四 SDD 轻量通道，Paul 已批）。
- **验收**：平台回归全绿；SC8 approve 一笔带原因的改判 → audit 可查到 `override_reason`；月度可按场景导出判例清单（给知识资产台账 §一.2 升版用）。

### 任务 3 🟠 两场批改会备料（时间敏感，7 月上旬开会）

- **3a FI1 批改会（财务对接人）**：拉 IT 按《FI1-ERP-CSV导出需求》导出**近 3 个月人工对账记录**（含每笔差异的人工处理结论）→ 你从中反推**差异分类规则草案 20-30 条 + L2 阈值量级建议**，输出一页可逐条打勾/改判的批改表（md 或 xlsx）。交 Paul 转对接人开 45-90 分钟批改会。批改结果即收口-1 草案 → 你据此起 FI1 的 openspec propose 更新。
- **3b 缺料口径批改会（采购专员）**：按《缺料预警校准需求》§5 步 1-2 先跑排查（误判子件 vs `missing_snapshot` 重合率、根因归类），产出**误判案例对照表**（每料：现货/在途/安全库存/请购状态/毛需求来源）+ §9 四个决策点的**建议选项**。交采购专员 45 分钟圈选。圈选定稿后按 §4 改 `kit_engine`（fail-loud 护栏 + 数据补全）→ 真实回归误判清零（验收标准 §7 原文不变）。
- **注意**：两份备料都是"AI 起草·专家批改"三步法的首两步，**专家只批改不写作**——这是本次纠正的核心机制，做出样板。

### 任务 4 🟡 8D 预填脚本（给陈忱团队，8/1 归集启动前交付）

- **需求**：按《质量旗舰开工就绪包》1.1 模板 + 1.2 脱敏范式，写预填脚本：输入 8D 原文（docx/pdf）→ 输出录入表字段预填（含脱敏占位建议：`OEM-A-01`/`电容-A`/`某车型平台-A` 等既有范式）→ 陈忱团队只校对/改判。
- **校准**：用陈忱首批 7 份人工样本作黄金基准——脚本预填 vs 人工结果逐字段对比，给出各字段命中率；D4 根因链与"根因验证有效"两列预期最难，明示置信度、低置信标红请人工。
- **顺带评估**：本脚本的解析件是否按底座 `doc_parser` 件设计（纠正方案 §五.2——8D/立项/合同/RFQ 四消费方将现，rule-of-three 在望；10 月 Q2 启动前定）。评估结论写进变更包 design，Paul 审。

### 任务 5 ⚪ P2 杂项包（可单独低峰 session，一次清完）

1. 删 4 个旧分支（体检确认领先 master 均为 0，可安全删）：`feat/o2-kit-shortage-alert` / `feat/platform-hardening-p2` / `feat/sc1-platform-align` / `feat/sc8-delivery-date-commitment`（本地+远端）。
2. `chore/sdd-hygiene-2026-07` 分支：确认 PR #16 状态（内容已被 master 吸收），关闭/删除，防成第 5 个挂着的分支。
3. GitHub 侧核实 PR #2、#5 编号断档（本地 git 无记录）；顺手在治理文档或 CLAUDE.md §5 加一句"本仓库以 ff-only 为主，PR 可能零 diff 关闭"的合并策略注记（外部评审防误读）。
4. fix-a/b/c 三包的 specs delta 文档性合并进根 `openspec/specs/`（**只合 specs、零代码改动**；体检实证：三包均有 delta 但从未合入，根 specs 已过期——如 `audit-hash-chain` 缺 `verify_chain` 要求）。
5. 场景 CLAUDE.md 滚动补齐：SC8（实测 110 tests vs 记录 91，补最新状态）、SC1（v0.0 陈旧版，补收尾/53 tests/交付维度过渡修正）。

## 三、红线与纪律（本次全程）

- **CC 分工**：可改代码/连真实库/git/发企微；**不改全景规划正文**（A 段只是把 Cowork 已落稿的改动 commit 入库，不新增规划内容）。
- `CUSTOMER_OUTBOUND_ENABLED` 全程 False 不动；mock/脱敏先行；所有 AI 决策写 audit；完工即归档（tasks 全 [x] 当场 `/opsx:archive`，任务 2 的轻量包同样适用）。
- 真实数据文件（对账记录/误判清单/华丰样本/8D 原文）一律 gitignore，入库前 `git check-ignore` 自查。
- 排期同步纪律：本批任务**无排期变更**，不触发跨文档重排；若做的过程中发现必须动排期，停下报 Paul 走机制。

## 四、收工

- 每完成一个任务块：勾对应 tasks → commit + push。
- 全部收口后：滚动更新 `session接力-Phase1收口.md`（本次进展 + 下一步）；若 Paul 要求，把"批次1 启动 + 三步法上线"写成企微正文由你发送（Cowork 已留机制：`python 0-学习与工具/发企微.py 正文.md`）。
- 建议完成顺序与预估：A 段（15 分钟）→ 任务 1（半天）→ 任务 3 备料（各半天，赶 7 月上旬会期）→ 任务 2（半天）→ 任务 4（1-2 天）→ 任务 5（低峰半天）。

---
*本 prompt 由 Cowork 应 Paul 要求产出（2026-07-04）。上下文详版：两份同日报告 + session接力【2026-07-04 晚】节。执行中有疑问先问 Paul，不自行重开已拍板决策。*
