---
status: 待执行
title: "开场prompt ·【Cowork】全景路线图线 · 转场接力（2026-08-20）"
created: 2026-08-20
执行方: Cowork 全景路线图线／总线
派出线: Cowork 全景路线图线（本线自转场）
编号: OP-0820-D
来源: Shao Peishen 2026-08-20 11:2x「CC已排出，Session有点长了，我们转场继续」
---

# OP-0820-D ·【Cowork】全景路线图线 —— 转场接力

> **转场类别 ＝ A（同线转场）** ⇒ opener 内置「开工自检三查」，**三查全通过即直接开工、不提问**；任一不通过才就差异发问。

## 开场词（复制即用）

```
【OP-0820-D】Cowork 全景路线图线 · 转场接力（同线转场，A 类）
【设置】执行环境：Cowork ｜ CC session：☐ 不适用 ｜ worktree：☐ 不新建（主工作区）｜ 分支：master ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-【Cowork】全景路线图线-转场接力-2026-08-20.md 全文 ＋ 1-转型规划\0-全景路线图\session接力-Phase1收口.md 的【2026-08-20 · Cowork 全景路线图线】转场交接点一节 ＋ CLAUDE.md 当前进度恢复上下文。

先按本文件 §一 自检三查，三查全通过即直接开工、有差异再问我。
```

## 一、开工自检三查

1. **接力最新日期节 ＝ 2026-08-20「转场交接点」？** 若有更晚的节，以更晚者为准。
2. **抽 2 行核对**：§四 **`#73`**（应已**销号**，止血验收两格全闭）与 §一 **`#334`**（`OP-0820-C` 在跑，视其进度可能已由 `[S:open]` 变动）。**opener 说的与队列实际状态不一致时，以队列为准。**
3. **grep 四条接力顶部「⏳ 队列更新待补」有无残留** —— 本线收工时块 E／F／G 均已收口，**应无活小节**；⚠️ **命中 ≠ 有待补**，读到「已回补」字样即跳过。

## 二、🔴 唯一的等待对象：姚祖怡回件

**采购域整条链只卡在这一处。** 采购部#16（齐料日期判例批改）2026-08-18 发出，至今未闭环 —— **他 08-19 两次回件都是被我方交付缺陷挡住，不是答复**（① 符号无主语 ② docx 勾不动）。两处均已补正、可勾选版已于 08-20 09:24 送达（四帧 `errcode=0`，私信＋抄采购群）。

**他一回件 ⇒ `#16` 闭环 ⇒ 串行闸开 ⇒ SC2 判例包（拟 采购部#17）方可起草。**

⚠️ **判据**：查 README 该收信人最近一封的发送状态列是否已到三种闭环形态之一（`📥 已回件并回灌`／`✅ 无需回复`／`📨 已确认闭环`）。**机器闸仍只认 `📥` 前缀**，其余须走 `串行豁免：`。

## 三、在办与待办

| 项 | 状态 |
|---|---|
| **`OP-0820-C`（`#334` 物料看板 apply）** | 已排出、在跑。**收工时按「CC 复命零粘贴」自取件核实**（transcript ／ 队列行 ／ git 三条信源） |
| **质量部#8** | `⏳ 待你审` —— 批准走 `approve_followup_letter.py --quote`，**10 分钟冷却，首次调用返回 `[COOLDOWN]` 退出码 1 是设计如此** |
| **§四 `#69`** | `--row-md` 是否押后到 `#351` design 审。默认 (a) 已被 CC 事实写入 `#351`，**可按默认** |
| **§四 `#70`** | 判例 3 虚构数据。已在补件中给他选择权，**待回件后按他是否跳过第 3 条收口** |
| **`#282` archive** | **仍不做** —— 6.5 未勾、3.3 待 §四 `#68` 生产载体同步后才可验 |
| **WIP** | **21／22**（22 是实测重定值，退出条件：连续两周实测 ≤18 即回议再降） |
| **skill 版本** | `zhuopin-followup-letter` **v3.3** ／ `zhuopin-kickoff-prompt` **v1.15**，已安装版均已同步 |

## 四、🔴 五条会绊住你的实况

1. **`#334` 的 D5／D6 两列会是空的** —— design 选了「保留列、显式标缺口」。**页面必须有「暂无数据源」标注与原因说明**，否则姚祖怡打开会以为是 bug、**第三次往返**。她这两天已因我方缺陷往返两次，**第三次会真的伤到 `#67` 那个试点反馈 loop 的信任**。
2. **`alert_webhook.py` 那条「第三道防线」从建成起就没活过**（读 `5-平台底座/.env` 裸键，该键**值长 0**、mtime 停在建服务当天）。已并入 `#282`，**它不动这条也不会自己好**。
3. **两套文件系统**：Cowork `Write` 工具写进 outputs 的文件，**本机 PowerShell `Test-Path` 恒 False**；但**仓库连接目录两侧一致**。⇒ 需要脚本中转时走 bash workspace 的 `/sessions/.../mnt/`，或直接用 Edit 工具改仓库文件。
4. **worktree 里的 `.env` 可能是陈旧副本** —— `OP-0819-H` 决策点 7(a) 就是为此：在 worktree 里验收会拿到 `93000` 假故障。**凡涉凭据的动作，在主工作区跑。**
5. **`git -C` 会静默向上找到祖先仓库** —— 跨 worktree 操作用 `cd` 不用 `-C`，并先 `git rev-parse --show-toplevel` 确认。

## 五、方法学（开工前读一遍，本日四条都是当天现报）

- **【设置】行**：规范上午刚落、下午自己就整行漏掉。**根因是「短指令不配【设置】行」这个隐含假设** ⇒ 判据：**长短与大小都不构成省略理由，唯一判据是「要不要粘进一个 CC 会话」。**
- **两向同步一律配机器 diff** —— 转写偏差可以做到「两句话意思一样、肉眼复核不可能发现」。
- **§二 批次行不是跨会话待办载体**（会随 sweep 沉入历史）⇒ **凡产出「待某方执行」的尾巴，先问「有没有既有承接行」，有就追加。**
- **不要预设数字** —— 08-19 推荐 WIP 上限 20 那次，用的是「销号之后的理想态数」而销号并未执行。**凡给数，先问它算的是哪个时点。**

**另**：并入审核应**前置于取号**（已落协议〇.10 正本）—— 同日两 session 处理同两条发现，先取号者撞 WIP 墙、两条只活在收工报告里；先审核者发现本就该并入、墙不必翻。

## 六、纪律段

- **编辑锁**：改队列前 `acquire`、改完立刻 `release`；🔴 **`acquire` 必须在任何写盘之前**（本日两批因次序颠倒触发过协议〇.7 绕锁告警）；`acquire` 与写盘两步、中间验 `$LASTEXITCODE`，**不要接管道**。
- **取号**：`--reserve` 必须在 `acquire` 那一次就带上；**取号前先做协议〇.10 并入审核**。
- **裸竖线**：cell 内严禁 `|`；PowerShell 传参用**单引号**（双引号里反引号会被当转义）。
- **文件清单**：每条必须是**仓库根相对完整路径且反引号包裹** —— 只包扩展名会被 sweep 判孤儿、**批次行转 ✅ 而文件静默漏 commit**。
- **含 Windows 路径的正文用 r-string，落盘后扫控制字符**（heredoc 吃反斜杠已复发三次）。
- **日期每次写入前重取** `Get-Date -Format 'yyyy-MM-dd'`，不得沿用会话早先取值。
- **写后反查**：`powershell -File "0-学习与工具\工具-写后反查.ps1" -Path "<绝对路径>" -Keyword "<该文件独有的一句>"`，非 0 即失败。
- **定夺项路由**：按「答完之后由谁执行」——本 session 接着做 ⇒ ⟨就地答⟩给选项；要另起 session／归他线／对外 ⇒ **不给选项，只写「已登记 §四 #N」**；报告顶部加汇总行。**默认项两前提**：有执行者在场 ＋ 代价是「停在原地」而非「持续发生」，任一不满足则**不设默认项**。

## 七、收工段

- **滚动更新** `session接力-Phase1收口.md`（新增日期节作新转场交接点）。
- **不自行 commit**：列出全部新产出/修改文件清单 ＋ 建议 commit message ＋ **登记队列 §二 待 commit 批次**，提示 Shao Peishen 一句话交 CC。
- **登记后立即** `Start-ScheduledTask -TaskName "ZhuopinCommitSweep"`；**批次转 ✅ 后还要看 `git status` 是否真干净**，两者不一致即有声明未被匹配。
- **压缩手法**（本会话实测有效）：环境治理／大文件读写类小任务**走子代理，主线只收结论**；取证用 `grep -o "关键词[^｜]\{0,200\}"` 只取窗口，不要整行 `--field status`；批量核对写一次性脚本输出摘要。
