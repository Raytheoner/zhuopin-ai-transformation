---
title: "开场 prompt · 环境总线接棒（2026-09-04）"
created: 2026-09-04
执行方: Cowork（环境总线）
来源: OP-0904-A 业务总线转线（他 2026-09-04 判「此线太重了」）
status: 待派出
---

# 开场 prompt · 环境总线接棒（`OP-0904-D`）

> **转场理由（按转场判据五条自查）**：⑤ token 压力真实 —— 本线一日内跑完 `OP-0904-B` 核验、master 合入、worktree ff、服务重启、三轮队列写入，上下文已重。①②③④ 四条**全阴性**（无遗忘、无重复、无未标更正的矛盾、无指令丢失）。⇒ **仅因 ⑤ 转场，不是因为出错。**
> **本件是引用版**：只给指针，不复制任何权威文件正文。

---

## 一、复制即用（粘进新开的 Cowork 会话）

见本文件同名代码块（聊天中已给）。骨架取自 `专线opener模板库.md` §〇.00，未改动任何一行形状。

---

## 二、交接要点（起草依据，**不必粘进 opener**）

### 本线今日已闭合，接棒者不必重做

| 事 | 状态 | 证据 |
|---|---|---|
| `OP-0904-B` 事件驱动起活 | ✅ 已合入 `master` 并 push | merge `bb786b5`，`git merge-base --is-ancestor 875f6f4 HEAD` 退出码 0 |
| 机制真正生效 | ✅ 09-04 16:55 | worktree `ops/wecom-service-home` ff 到 master；服务 pid 18596；同解释器实跑 import 核验（桥一已接线／章程可读 15413 B／`claude` 在 PATH） |
| 注入面登记 | ✅ §四 `#157` | 三候选与代价已写全，**无默认项** |
| 高水位线修复 | ✅ §四 155→156 | 取号被拒后当场修，`#143`⑵ 第三次实证 |

### 接棒者最先要碰的三件（按紧要序）

1. 🔴 **`#382` 行长迁移** —— 状态列 **23261 B**，`行长豁免` 顶着，**09-11 起硬阻断**。这是唯一有硬期限的项。
2. **§四 `#157` 等他答** —— 机制已在生产上跑，**不答＝按「维持现状」事实生效**。接棒者第一次收工报告应把它作为临期项置顶重提。
3. **`#439` R1 台账** —— 包已落地含 `specs/`，apply 前置两件在包内 `tasks.md` §0。

### 三条今日新立、写在 `#382` 末段的判据（接棒者会反复用到）

- **范围结论的迁移，与结论本身同样需要举证**（一日三次同族：放宽／放大／平移）。
- **合入 master ≠ 在跑**，第四形态＝**跑的根本不是 master**；三步齐了才算：主工作区 merge → worktree ff → 服务重启。
- **进程核验以「新 pid 是否出现」为准**，不以「旧 pid 是否消失」为准。

---

## 三、不做什么（写进 opener）

- **不重开**轮巡拆件巡逻（他 09-04 判永久下线）。
- **不动** `.51`、不发企微、不改跟进信 README `发送状态` 列。
- **不替他答** §四 `#157`／`#152`／`#156`。
