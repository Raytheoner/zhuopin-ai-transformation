---
title: "派单件 · 【CC】#556 读失败可见化接进 sweep 值周巡检"
created: 2026-09-13
status: 待执行
队列行: §一 #556（决策点 9 的 9.3 残余，挂本行做，🔴 不立新行）
派出线: Cowork 环境总线 OP-0912-E（批 B-0913_收口三泳道）
授权: Shao Peishen 2026-09-13 回「全按推荐」＝定夺 3a
---

# 派单件 · `#556` 9.3「可见化」接进 sweep 值周巡检

## 〇 · 为什么是这条

`#556` 的决策点 9 已 apply（`19e02c7` 在 master）。design 9.3「可见化」要求**三处对齐同一份状态**：①活人告警（已节流，已接）②体检 CLI `check_outbox_relay.py`（已接，读不到时打印 `first_failed_at`／距今时长）③**值周巡检 sweep（未接）**。`OP-0912-C2` 收工时自己如实登记了这一条未做。

🔴 **本棒挂 `#556` 做，不立新行**——它是那条行 ⑨3 的残余，不是新任务。机制类可动 WIP 现取 32/22 已超限。

## 一 · 已交付、可直接用的纯函数（不要重写）

`5-平台底座/wecom-aibot-service/aibot_service/outbox_relay.py::list_persistently_unreadable(state, now, threshold_seconds=86400)`

- 输入＝`reports/outbox_relay_unreadable_state.json` 反序列化后的 dict（路径解析用 `aibot_service/repo_paths.py::resolve_outbox_relay_unreadable_state_path`）。
- 返回＝`(路径, first_failed_at, 已不可读小时数)` 三元组列表。
- **纯函数、无 I/O**，已有单测 `tests/test_outbox_relay.py::test_list_persistently_unreadable_is_a_pure_function_over_the_state_dict`。

🔴 **不要在 sweep 里另写一份判据**——同族红线：`followup_gate` 模块文档「判据只此一份」。sweep 侧只负责读状态文件、调这个纯函数、把结果排进值周清单。

## 二 · 做什么

1. 开工先读三样：`openspec/changes/aibot-outbox-relay/tasks.md` 的 **9.8 那一行完整留步说明**；`python 0-学习与工具/工具-队列查询.py --row 556 --field all`；`0-学习与工具/工具-落库sweep.py` 里 **队列 `#312` 陈化催办那一类**（范式照它，`grep "STALE"` 能定位到多处同类实现，含 `track_and_alert_standing_state` 一族范式）。
2. 在 `工具-落库sweep.py` 新增一类值周提示：读 `reports/outbox_relay_unreadable_state.json`，凡 `first_failed_at` 距今 **> 24 小时**的路径点名进值周巡检清单。
3. 🔴 **用状态文件里的时间戳判「多久没好」，不用 mtime、不用审计行数**——mtime 会被任何一次写盘刷新，审计行数受重启与休眠影响（本日实测：进程休眠导致小时桶出现 0，用行数判会得出「已恢复」的假阳性）。
4. 类号顺延（第 14 类已被 `#551` 旁生占用、15 类 `#554`、16 类 `#553`）；含单测与**至少一组变异检验**。
5. 零回归：跑 sweep 全部单测并与纯 master 出差集。
6. 收工：push 分支 → 登 §二 → 在 §一 `#556` 状态格追一段（`edit-row --append`），**并同步回勾 `openspec/changes/aibot-outbox-relay/tasks.md` 的 9.3/9.8**（只勾真做完的）。

## 三 · 不做什么

- 🔴 **不立新队列行**；本棒只在 `#556` 上追段。
- 🔴 **不改 `outbox_relay.py` 的告警节流逻辑**（决策点 9 已签认并 apply，动它要重签）。
- 🔴 **不给中继新增任何网络自检**——design 9.1 已否掉；它会复用本项目已被证伪的 `Test-NetConnection` 量具，在真 off-LAN 时得到「在网」假阳性，把该响的真故障静默掉。
- 🔴 **不动 `#394`**（SC2 真实送达销号）——那条仍在 L1/L2 留步。
- 🔴 sweep 的告警出口若会进业务群，**本类一律只进值周清单、不推群**（`#73`／`#282` 教训：采购内部工作群累计收到 58 条机制告警，其中一条正文是 Python traceback）。

## 四 · 收工报告要有的东西

1. 分支 SHA、两个计数、新增的类号。
2. **实弹干跑输出原文**：拿当前真实的 `outbox_relay_unreadable_state.json`（现取存在，186 B，`5-平台底座/wecom-aibot-service/reports/` 下）跑一遍，贴清单原文。
3. 变异检验：改掉判据某一条 ⇒ 对应用例转红、还原复绿，贴原文。
4. 差集回归结论一句。
5. §二 批次号、`#556` 追段原文、`aibot-outbox-relay/tasks.md` 回勾了哪几项。
## 硬边界（三条泳道同受，一条都不许省）

- 🔴 **不 ff master**（🟡 档）；收工只 `git push` 本泳道分支。
- 🔴 **不碰 `.51`**、不动常驻执行体运行时（⏭️ 档）；**不发任何跟进信、不触发任何对外发送**（🔴 档）。
- 🔴 **零回归判据＝差集**：分支失败集 ⊆ 纯 master 同命令失败集，**不是「必须全绿」**；纯 master 侧失败原文照抄附上。
- 🔴 **不立新机制行**（机制类可动 WIP 32/22 超限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 队列回写走 `0-学习与工具/工具-共享文档编辑锁.py`；§二「文件清单」格**只放反引号包着的路径**（`#513`）；撞 `.git/index.lock` 不重试。
- 🔴 新文件名形态同批补 `.gitignore` 并 `git check-ignore -v` 实测取证（`#328`）。
- 🔴 **勾 tasks 要诚实**：只勾真做完且有手段可证的。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`；命中 🟡/🔴 决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。