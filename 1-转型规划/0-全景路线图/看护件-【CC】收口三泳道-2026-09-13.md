---
title: "看护件 · 泳道看护批 B-0913_收口三泳道"
created: 2026-09-13
status: 生效
批次: B-0913_收口三泳道
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0913_收口三泳道`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴。** 授权原文：Shao Peishen 2026-09-13 回「全按推荐」（定夺 `3a` 值周可见化、`4a` 关行三分表）＋ 回「第 5 项选 a」（P3 4.4 放宽编辑锁两处等值比较）。
🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。本批三条泳道，`-MaxParallel 3`，未超上限 4。

## 一、硬边界继承（三条同受）

- 🔴 **不 ff master**（🟡 档）；收工只 push 本泳道分支。
- 🔴 **不碰 `.51`**、不动常驻执行体运行时（⏭️ 档）；**不发任何跟进信、不触发任何对外发送**（🔴 档）。
- 🔴 **零回归判据＝差集**（分支失败集 ⊆ 纯 master 同命令失败集），不是「必须全绿」。
- 🔴 **不立新机制行**（机制类可动 WIP 32/22 超限）；确需时 🛑 起首。
- 🔴 **A2 不关任何一行、不改任何一行状态格**——关行属 🟡，本批只出建议。
- 🔴 队列回写走 `工具-共享文档编辑锁.py`；§二 文件清单**只放反引号包着的路径**（`#513`）。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`，命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与另两条 |
|---|---|---|---|
| `op0913a-556-sweep-visibility` | `#556` 9.3 残余（`3a`），**挂 `#556` 不立新行** | `0-学习与工具/工具-落库sweep.py` 新增一类＋单测；`openspec/changes/aibot-outbox-relay/tasks.md` 回勾 | **零重叠** |
| `op0913b-wip-row-triage` | 关行候选三分表（`4a`），**只出表不关行** | 只读队列（经 `工具-队列查询.py`）＋ `git`；产出落新取证件 | **零重叠**（纯只读） |
| `op0913c-p3-editlock-compat-44` | P3 `tasks.md` 4.4（`5a`） | `0-学习与工具/工具-共享文档编辑锁.py` 两处等值比较＋单测；`openspec/changes/followup-closure-form-survives-backfill/tasks.md` 回勾 | **零重叠** |

**唯一共享资源＝两份队列文件**（三条都要写 §二、A1 追 `#556`、A3 追 `#561`），由编辑锁串行化。

⚠️ **三条各自最大的坑，逐条写明**：
- **A1**：🔴 **用状态文件里的时间戳判「多久没好」，不用 mtime、不用审计行数**——本日实测：进程休眠会让审计小时桶出现 0，按行数判会得出「已恢复」的假阳性。
- **A2**：🔴 **「分支已在 master」≠「这条行可关」**。这正是本件要它避免的那个跳跃；判据只看「期望产出」列，不看状态格自陈（`#462`「未勾≠没做」的反面同样成立）。
- **A3**：🔴 **只放宽编辑锁那两处**。`gates.assert_finalized` 是 design D8「不得放宽」的门禁②红线，不在本次签认范围；收工报告须把七处等值比较逐处标「改了/没改」。

## 三、泳道 opener

### A1 · #556 读失败可见化接进 sweep 值周巡检（OP-0913-A）

粘贴端：CC ｜ 泳道：op0913a-556-sweep-visibility

```
[OP-0913-A]【CC】556值周可见化
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913a-556-sweep-visibility`） ｜ worktree：☑（op0913a-556-sweep-visibility，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0913_收口三泳道）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】556值周可见化-2026-09-13.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913a-556-sweep-visibility --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913a-556-sweep-visibility --done --batch B-0913_收口三泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · 关行候选三分表，只出表不关行（OP-0913-B）

粘贴端：CC ｜ 泳道：op0913b-wip-row-triage

```
[OP-0913-B]【CC】关行三分表
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913b-wip-row-triage`） ｜ worktree：☑（op0913b-wip-row-triage，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0913_收口三泳道）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】关行三分表-2026-09-13.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913b-wip-row-triage --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913b-wip-row-triage --done --batch B-0913_收口三泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · P3 tasks 4.4 编辑锁两处快照兼容（OP-0913-C）

粘贴端：CC ｜ 泳道：op0913c-p3-editlock-compat-44

```
[OP-0913-C]【CC】编辑锁快照兼容
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913c-p3-editlock-compat-44`） ｜ worktree：☑（op0913c-p3-editlock-compat-44，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0913_收口三泳道）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】P3编辑锁快照兼容-2026-09-13.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913c-p3-editlock-compat-44 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913c-p3-editlock-compat-44 --done --batch B-0913_收口三泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0913-D]【CC】看护关行与收口
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0913_收口三泳道）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0913D-看护关行与收口。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】收口三泳道-2026-09-13.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. 每泳道一行：分支／commit／哨兵／tasks 勾选数。
2. 三条各自的差集回归结论（A2 无代码改动，写「不适用」并说明）。
3. **A1 必须贴实弹干跑清单原文 ＋ 变异检验原文**；**A2 必须贴三分表与「全关掉后 WIP 从 32 降到几」那一句**；**A3 必须贴七处等值比较逐处「改了/没改」标注 ＋ 4.4 兼容性单测输出**。
4. 待 ff 清单（**ff 不在本批内做**）。
5. 🔴 等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0913_收口三泳道` 现取。
