---
status: 在办
title: "看护件 · 2026-08-31 · 回 LAN 收口【首跑灰度批】"
编号: OP-0831-L
日期: 2026-08-31
触发: Shao Peishen「我已回Lan」（zhuopin-lan-closeout v1.0 首跑）
首跑灰度: "🔴 tasks 3.1 未打勾 ⇒ 单批强制 2 项、Shao Peishen 全程在场；三判据过后方放开默认上限 6"
---

# 看护件 · 回 LAN 收口 · 首跑灰度批

## 〇 口径

**前置探针（已过，实测 2026-08-31）**

| 项 | 实测 |
|---|---|
| `ping 192.168.100.51` | ✅ True |
| TCP 8091／8092／8093／8096 | ✅ 四端口全通 |
| git | 0 脏文件 ／ `origin/master..master` = 0 |
| 编辑锁 | 无锁 |
| 上一收口批汇总 | 本 skill 首跑，无上一批 |

**三源汇集**：原始命中 28 处（机制环境 17／业务场景 11／看护件 3 份）。

**落选并点名（D1 白名单，宁漏勿滥）**

| 行 | 落选理由 |
|---|---|
| `#312`／`#398` | 是「LAN 留步无承接方」的**元讨论**，不是留步项本身 |
| `#422` | 判据是「要先真的离开内网」⇒ **on-LAN 反而做不了** |
| `#418` | 行内明写「这不是 LAN 留步，是**无人在场时不自行执行需批准的生产写动作**，请 Shao Peishen/业务总线批准后再做」⇒ 人工门禁，不代办 |
| `#401` | **疑似已完成**：`#402` 记载 L1 已由并行泳道 `OP-0826-V` 本日跑出并 ff 入 master（`49dd1b2`）⇒ 须先核实，不入本批 |
| `#334`／`#419`／`#340` | 本批容量已满（首跑限 2），留待下批 |

**入选 2 项**（Shao Peishen 2026-08-31 答 ㉑ 选 (a)）：`#403`（只读）＋ `#394`（写链，满足 tasks 3.1「全链一次」）。

---

## 🔴 一 · 硬边界继承（skill 四条红线，逐条不得越）

1. **先探针后动手** —— 已过，见 §〇。探针不过即停，触发词本身不是在网证据。
2. **`.51` 一次只动一项** —— 每项快照→部署→冒烟三件套；**不过即按回滚 SOP 退回并停链，绝不带伤推进**。
3. **对外发送与请人动作永不代办** —— 本批两项均不涉对外；L2 门禁不代签；合规红线不动。
4. **看护件落档失败即全停**；失败项自动降回留步态并注明原因，不留半收口。

**补充（本批特有）**：**Cowork 侧不执行任何 `.51` 动作**。本件只落档与派发；执行环境标注＝**CC**（触碰生产服务）。

---

## 🔴 二 · 波次矩阵（`.51` 项全串行一条链，先只读后写）

### 预置取证（Cowork 本轮实测，供 CC 直接消费，勿重跑）

- 🔴 **`\\192.168.100.51\C$` 从本机不可列**（管理共享被拒；四个候选 `.env` 路径 `Test-Path` 全 False）。⇒ **两项都需要一条能读 `.51` 文件的通路**，而 `.51部署标准清单-服务侧与笔记本侧-2026-08-01.md` **未记载既有传输通路**（全文只有「共享口令门禁 #160」一处相关）。本机存在 `C:\WINDOWS\System32\OpenSSH\ssh.exe`；`§一 #418` 曾记「`.51` 健康、SSH 通」。
  **⇒ CC 第一步是确定并记录这条通路本身**——它是两项的共同前提，且**至今没有任何文档写着它是什么**。
- `WECOM_AIBOT_OUTBOX_PATHS` 由**笔记本侧** `scripts/run_aibot_service.py` 读取，中继**跑在既有长连接上**（不另起连接——避免同 BotID 多处长连接互相踢线）。⇒ **配置点在笔记本，不在 `.51`**；需要的是「笔记本能读到 `.51` 上 SC2 写的 outbox 文件」。
- 现成冒烟工具：`5-平台底座/wecom-aibot-service/scripts/check_outbox_relay.py`（不传 `--path` 时读同一环境变量）。
- 今日 audit 实证：`outbox_relay_disabled — WECOM_AIBOT_OUTBOX_PATHS 未配置` 已出现 **2 次**。

### 波次 1 · `#403`（只读，风险零）

| 三件套 | 内容 |
|---|---|
| **快照点** | 不适用（纯只读，不写 `.51` 任何文件） |
| **冒烟判据** | 打印 `.51` 上 SC8 服务**实际加载**的 `.env` 全部键名，并明确回答 **`SC8_OUTSOURCE_IDS` 在／不在**。🔴 **须是实读，不得复用 `#403` 行内那份 off-LAN 时期写下的清单** |
| **回滚 SOP** | 不适用 |

### 波次 2 · `#394`（写，须波次 1 通过后才起）

| 三件套 | 内容 |
|---|---|
| **快照点** | 改动前的笔记本侧 `.env` 原文，落**仓库外**双备份（`~/.claude/env-backups/` ＋ 会话 scratchpad，SHA256 一致，沿 `#354` 既例） |
| **冒烟判据** | ⑴ `python scripts/check_outbox_relay.py` 能读到 `.51` 上 SC2 的 outbox 文件；⑵ 重启后 audit 出现 `outbox_relay_started`（**而非** `outbox_relay_disabled`）；⑶ 🔴 **端到端**：让 SC2 写一条测试 outbox 记录，中继把它投出去并在 audit 见 `errcode: 0`。**只见 ⑵ 不见 ⑶ 不算通**（同 `#82` 形态：启动了 ≠ 发出去了） |
| **回滚 SOP** | `3-治理与合规/SC8上线前置门禁-错误回滚SOP与黄金基准校验.md`；动作＝恢复 `.env` 快照 → 重启 `ZhuopinAibotDevListener` → 确认 audit 回到改动前形态 |

🔴 **停链条件**：波次 1 的通路若确定不存在（既无文件共享也无可用 SSH），**波次 2 不得起**——`#394` 的设计前提「笔记本能读 `.51` 文件」就不成立，须先立行解决通路，本批如实降回留步态并注明。

---

## 三 · 泳道 opener（🔴 首跑手动派，不走无头）

> **为何首跑不用无头泳道**：`tasks 3.1` 要求「**须 Shao Peishen 在场**」，而无头泳道的全部意义是「**他不用看着**」——**两者在设计上相互抵消**。首跑用手动派发，他能看见每一步；三判据过后再转无头。
> ⇒ **本条建议并入 `tasks 3.2` 复盘**（与 `3.2.1` 续批缺口同批处置）。

▶ 粘贴端：CC 新会话

```
[OP-0831-L]【CC】回LAN收口首跑批（#403 只读 → #394 写，全串行）
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建一个 ｜ 分支：新建 claude/lan-closeout-firstrun-0831 ｜ 工作区：不动主工作区 ｜ 派出线：Cowork LAN 收口首跑（OP-0831-L）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0831L-回LAN收口首跑。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-08-31-LAN收口首跑批.md 全文（本件即你的作业书，三件套与停链条件都在里面）→ ② 队列 §一 #403 与 #394 全文（用 工具-队列查询.py --row N，勿通读真身）→ ③ CLAUDE.md 当前进度。

🔴 首跑灰度：本批强制只做这 2 项，Shao Peishen 全程在场；全串行，波次 1 不过不得起波次 2。
🔴 第一件实活是确定「本机 ↔ .51 的文件读取通路」并把它写下来——Cowork 已实测 \\192.168.100.51\C$ 不可列，而部署标准清单里没有任何一处记载既有通路是什么。这条通路是两项的共同前提。
🔴 冒烟证据一律要审计条目，不要只看返回码（同 A1 那条 smoke_test_send errcode=0 的形态）。
🔴 任一波次冒烟不过：按回滚 SOP 退回、停链、该项降回留步态并注明原因，不带伤推进。

收工：① 每项一行写「判据→证据→行内状态处置」回写 #403/#394；② 登记 §二 批次；③ 自行 commit+push；④ 🔴 汇总里必须写明「本批 N 项／三源清单剩余 M 项」——skill 步骤 6 不含这一项，是 tasks 3.2.1 已登记的缺口，本批手动补上。
```

---

## 附 · 首跑三判据（复盘时逐条对，tasks 3.2）

1. **误收 0** —— 入选 2 项是否都确属 LAN 留步且判据可执行？
2. **不合格登记全部点名** —— §〇 落选表是否漏点名任何一条？
3. **每项证据链齐全** —— 两项是否都有「快照点／冒烟判据／回滚 SOP」三件 ＋ 实际审计条目？

**全过 ⇒ 摘掉灰度，后续批用默认上限 6。**

---

## 五 · 收工汇总（2026-08-31，CC `OP-0831-L` 执行完毕，Shao Peishen 全程在场）

| 项 | 判据 | 证据 | 行内状态处置 |
|---|---|---|---|
| **#403**（只读） | 打印 `.51` 上 SC8 服务实际加载的 `.env` 全部键名，回答 `SC8_OUTSOURCE_IDS` 在/不在 | 通路＝SSH（`supplychain-server` 别名，本批首次落档，见 `.51部署标准清单` §十一）；在 SC8 自己的 `.51` venv 内直接跑真实 `env_anchor.resolve_env_file()`：`C:\baoguan\.env｜19 键｜SC8_OUTSOURCE_IDS_present=False`，与 08-27 旧结论交叉核验一致 | 状态列已追记，纯只读不改判 [S:open] |
| **#394**（写） | outbox 中继首条真实送达 | 通路＝Windows 凭据管理器 GUI（`.51部署标准清单` §十一）；`.env` 改动前双备份 SHA256 校验一致；重启服务后 `outbox_relay_started`（非 `_disabled`）；2 条真实待发记录（采购部群 + 姚祖怡私发）全部 `errcode:0`，Shao Peishen 本人核实群对；源文件回写 `delivered:true` | 状态列开头文案已改判（见下方 §三 处置），仍不销号 |

**🔴 本批 2 项／三源清单剩余 M 项**（2026-08-31 Shao Peishen 更正：M 须是**去重后的可执行队列行数，不是原始命中处数**——首版直接沿用「28 处」失当，逐行订正如下）：

| 分类 | 数量 | 明细 |
|---|---|---|
| 原始命中（行去重，非命中处计数） | 28 | 机制环境 17 ／ 业务场景 11，逐行 `grep` 已天然去重（单元格不得跨行，一行即一命中） |
| 确认非任务项（元讨论，不计入候选） | 2 | `#312`／`#398` |
| 判据方向错误（须先离网，on-LAN 反做不了） | 1 | `#422` |
| 人工门禁，永不代办 | 1 | `#418` |
| **本批已处置（含今日新纳入的 `#354`）** | 7 | `#403`只读✅／`#394`写✅／`#401`执行完毕✅／`#334`子项⑺部署验证✅（行仍 partial，因其余子项与 LAN 无关）／`#419`确认非 LAN 维持 blocked／`#354`确认非 LAN 改判 `timed=2026-09-03`／`#340`确认非 LAN 维持 partial |
| **⇒ 真正尚未触碰、未经个别核实** | **17** | 28 − 2 − 1 − 1 − 7 |

剩余 17 项处置见本文件「第二批」相关节及队列 §二/§四 后续登记；这 17 项目前只是「关键词命中」，**尚未逐条核实是否真是 LAN 留步**——本批 7 项核实中已有 3 项（`#419`/`#354`/`#340`）证实并非真 LAN 阻塞，命中率不足五成，故 17 这个数**大概率进一步缩水**，不应被读成「17 项待执行」。根治办法即 tasks 2.1 已登记的汇集逻辑缺口。

**首跑三判据自评**：① 误收 0——两项均确属 LAN 留步且判据可执行；② 不合格登记全部点名——§〇 落选表 8 条无遗漏；③ 证据链齐全——两项均有快照点/冒烟判据/回滚 SOP 三件 + 实际审计条目（`.51` 侧 audit JSONL / 源文件回读）。**⇒ 三判据全过，灰度摘除，后续批用默认上限 6**（Shao Peishen 2026-08-31 当场确认进入第二批，上限 6）。

---

## 六 · 第二批（灰度已摘，2026-08-31 当日续跑，全串行，`.51` 一次只动一项）

**⑴ 先改判两行失真状态字段**——按行内最新内容核实后改判，未照抄转述：

| 行 | 转述的失真理由 | 读行核实结论 | 处置 |
|---|---|---|---|
| `#419` | "本轮 off-LAN 无法取证" | **不成立**——该半 08-27 已解除并写入本行；现行 `[S:blocked]` 真实理由是待 Shao Peishen 拍板修法方案（与 LAN 无关） | 维持 `[S:blocked]`，追记澄清，不因回 LAN 解除 |
| `#354` | "`.51` 真机复验 LAN 留步" | **不成立**——LAN 缺口 08-27 已自行解除，行内原话"不要再当 LAN 留步捞出来"；剩余两项均为时点约束 | `[S:blocked]`→`[S:timed=2026-09-03]`；顺带核验 `2.3.5`（`Sc2WeeklyAutoPush` 真实推送）三项全过并完成 |

**⑵ 核实两项，核实结果决定入不入批**：

| 行 | 结论 | 处置 |
|---|---|---|
| `#131` | 正常 §四 事项行（本无 `[S:]` 字段结构），2026-08-27 已查清并销号 | 不入批 |
| `#401` | **不是"疑似已完成"，是"签认已完成、部署动作未做"**——姚祖怡签认／L1-L3 验证材料均已就绪，唯独部署+翻开关+验证四步从未执行 | **并入本批**（Shao Peishen 0831 现场拍板，排 `#419` 之后 `#354` 之前） |

**⑶ 执行（按定序全串行，`#419`/`#354` 因⑴结论已确认无需执行，实际动 `.51` 的只有 `#401`/`#334`）**：

| 行 | 快照点 | 冒烟判据 | 回滚 SOP | 结果 |
|---|---|---|---|---|
| `#401` | 部署前 PID 9800/10012（08-27）；翻开关前 `.env` 本地备份 `C:\baoguan\.env.pre-rule1-2026-08-31.bak`（SHA256 `C5C4AF39…`） | ①部署后 PID/CreationDate 真刷新且基线零漂移（🔴102/🟠0/🟡0/🟢1）②翻开关后 PID 再刷新③`POST /api/refresh` ok=True④四色变化方向吻合签认基线（🔴68/🟠27/🟡7/🟢1，量级贴近 71/27/7/0） | 恢复 `.env` 备份→重启→核对回到翻前基线（未触发） | **✅ 销号**；顺带发现 `param_version` 展示层缺陷，新立 `#445`（Shao Peishen 拍板保持开关 ON、不回滚，另行修复） |
| `#334`⑺ | 无独立快照（复用 `#401` 部署，未单独二次 sync） | `/materials` 页面 200，`colgroup`/`table-layout:fixed` 存在、旧版 `overflow-x:auto`/`nowrap` 不存在，四项逐条核对 | 不适用（纯验证，`#401` 那次部署的回滚同时覆盖本项） | **✅ 子项⑺部署验证完成**；行仍 `[S:partial]`（D1/D2 报修＋⑻⑼未定序＋跟进信未发，均与 LAN 无关，本批未触碰）；🔴 按串行闸**未通知姚祖怡**，只回写队列 |
| `#340` | 不适用（纯读行判断） | 不适用 | 不适用 | 读行复核：LAN 缺口 08-27 已自行解除，本批无新动作 |

**收获之外的副产品**：`#401` 执行时顺带证实 `#334`⑺ 已随同一次 `sync-to-server.ps1` 一并部署到位，未另跑一次部署——两行的"部署"实为同一个物理动作。

---
