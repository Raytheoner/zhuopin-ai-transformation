---
title: "拆件巡逻报告 · 2026-08-03"
status: 在办
created: 2026-08-03
执行: Cowork 拆件巡逻（Dispatch v0.5，阶段二 live）
---

# 拆件巡逻报告 · 2026-08-03

**识别 2 行（#190 采购／#191 财务），已拆 2 行，全部销行。** 派生 §一 待领行 4 条（#211/#212/#213/#214），登记 §四 需 Shao Peishen 项 2 条（#42/#43）。

---

## 〇、待发信积压盘点（只读提醒，未发送任何信件）

`6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` 状态列扫描：**「🆕 待发」1 封**。

| 收件人 | 起草日 | 主题 | 截止 |
|---|---|---|---|
| 采购部 · 姚祖怡（未发，不编号） | 2026-07-29 | **批 2 引擎最后一项口径判例包**（队列 #118 剩余唯一开放点）：S02Y.0120 真实案例，请批「上月未齐套项目的物料，在下月是否继续传导占用」1 项（3 行真实判例三选一） | 不卡时间，方便时回即可 |

**观察（仅提示，不擅自处置）**：该信已起草 **5 天未发**，而它承载的是 #118 批 2 引擎的**最后一项开放口径**。姚祖怡在 07-31 回件里正处于高频互动状态（同一封信里回了三问＋提了两个新问题），此时并信或补发的边际成本很低。是否随下一封采购信一并发出，由采购专线/业务总线定，巡逻不代发。

---

## 一、#190 · 采购部 · 姚祖怡 07-31 回件（八字段交付验收）

- **行号**：§一 #190
- **文件**：`7-外部文档/采购部/采购部-YaoZuYi-回复-2026-07-31-采购部-姚祖怡-跟进-2026-07-30-BOM缺口物料清单八字段整体呈现+答交日期根治（姚祖怡回复）-5d786400f86e387b4d7e8eaa8b5cd21e.docx`（docx，已用 python-docx 逐段＋逐表提取全文，未靠文件名推断）

### 内容一句话摘要

对 #152 交付（八字段整体呈现＋答交日期根治）的三问验收：**①八字段齐全对齐 ✅过、②缺口≤0 不进清单 ✅过、③答交数量与日期 ❌不一致**（举证 S04Y.0112／R01B.0754：看板 500 ／ 2026-08-07，其复核 SRM 正确值 **14000 ／ 2026-12-25**），并直接给全正确取值口径；另附两个新问题（显示不清、替代料假缺口）。

### 已执行动作

1. §一 **#190 销行**，状态列写入三问逐条结论＋他给定的三条取值口径＋两个补充问题原样落字。
2. 新增 §一 待领行 **#211**（🔴 答交数量／日期取值口径根治 v2，CC 采购落地）、**#212**（🟠 两字段显示不清，纯展示层，CC 采购落地）、**#213**（🔴 BOM 替代料假缺口，先取证不改判定，CC 采购落地）。
3. 回灌 `1-转型规划/session接力-采购域场景落地.md` 新增【2026-08-03】节（他给的三条口径逐字落档，供 CC 直接照做）。

### 判断依据

- **③ 为什么判"验收未过"而非"又一次分歧"**：他写的是"**结论是不一致**"并给出可核对的两组具体数字（看板值 vs SRM 正确值），属可证伪的事实陈述，不是口径分歧。
- **为什么 #211 不需要再问一轮**：他把取值口径写全了——取数源（供应计划看板）、筛选条件（**只取「按排程交货」，不取「按订单交货」**）、累计规则（首个答交数量小于缺口则续取下一个，直至累计满足缺口，日期取达成累计的全部日期）。判例批改法下专员即口径权威，**这属"已有裁决的回灌"，在巡逻权限内**，无需转 §四。
- **⚠️ 一处关键交叉核对（这条差点被漏掉）**：#152 的 07-29 根治处理的是"供应计划板查不到时不再静默回退 PO 旧值"，**并未处理「交货方式筛选」这个维度**——两者是不同的失效路径。若把本次偏差当成"上次没修好"，会重复修同一处而错过真正主因。已在 #211 与采购接力中显式标出。
- **为什么 #213 拆出来单列且只做取证**：替代料会改变齐套/缺口计算本身，触碰黄金基准与四色判定（SC8 既有红线：改计算须专员重核＋签字），属**新能力与功能取舍**，按红线不自行落字，转 §四 #42。
- **同批合并读取**：本轮两行分属采购/财务两域，已分别通读全文；#190 docx 的关键内容（三问答复＋两个补充问题）**全部在表格之外的自由文本段落里**，本文档无表格结构可解析——再次印证硬约束 1。

### 需 Shao Peishen 项

**§四 #42** —— 采购三项（#211 答交根治／#212 显示不清／#213 替代料）的优先级 + 替代料是否本轮纳入。选项 (a) #211+#212 本轮做、#213 只取证（建议，**默认项**）／(b) 三项同批做／(c) 只做 #211。

---

## 二、#191 · 财务部 · 唐燕萍 07-31 文本反馈（FI2 v8 面板复核）

- **行号**：§一 #191
- **文件**：`7-外部文档/财务部/财务部-tangyanping-回复-2026-07-31-文本反馈-afcc2fba3b1266b10c1a7f0a1b5edac2.md`

### 内容一句话摘要

对 #182/#183 交付的 FI2 v8 面板复核：**结构、免责声明、二期四项标灰徽标、PO 变更检测不做——四项全部认可，零返工项**；**唯一新增诉求＝把真实数据接进来**（PO/AP 从 ERP 取、发票从已有数据源接），因为"mock 里每条差异都是预设好的"，BLOCK／微差分界与单价容差口径**在 mock 上判不了**。

### 已执行动作

1. §一 **#191 销行**，状态列写入四项认可的原话摘要 + 真实数据诉求 + 已知约束。
2. 新增 §一 待领行 **#214**（🟠 FI2 真实数据接入，CC 财务落地），并在行内写明发票源依赖 #59（OCR 选型）与原 8 月底排期两项既有约束。
3. 回灌 `1-转型规划/session接力-财务域场景落地.md` 新增【第二十一轮 · 2026-08-03】节。

### 判断依据

- **同日同专员共 4 份文本反馈**（`693045…`／`afcc2fb…`（本行）／`bf0fa6e…`／`e8edd3b…`），按硬约束 3 已**全部合并读取**：`693045` 与 `bf0fa6e` 内容几乎相同（同一封回复的两次发送，仅附件名不同）、`e8edd3b` 是"前一封附件发错请忽略"的更正 —— 三者均属 #175 已拆件范围（#175 状态列已记录该更正与三点收敛），**本行 `afcc2fb` 是其后的独立复核回件，内容不重复**，未误并未误销。
- **为什么"真实数据接入"不当成普通功能请求直接派 CC 就完**：它牵动**时点**（原排期 8 月底）与**依赖**（发票源＝U9C 应付附件＋OCR，而 OCR 选型 #59 已悬置约 3 周无回复）——属新排期与范围判断，按红线转 §四。
- **一个不宜掩饰的结构性事实**：她把验收切成了两段——展示层已收，**判定层她拒绝在 mock 上表态**。这意味着 FI2 判定口径要定稿，真实数据接入是唯一路径，不能靠再发一轮判例包绕过。已写入财务接力，供后续排期时不产生"她还没提意见"的错觉。

### 需 Shao Peishen 项

**§四 #43** —— FI2 真实数据接入时点，发票源卡在 OCR 选型未定。选项 (a) 维持 8 月底排期（**默认项**）／(b) 分两步：先接 PO+AP 真实数据、发票以人工誊录小样占位并行跟催 #59（建议）／(c) 先跟催 OCR 落定后一次性接全三单。

---

## 三、巡逻自检

### 三.5 重读比对结果

- **写回前重读**：已用**本机 PowerShell 直读**队列文件真身（非沙箱挂载副本），与 acquire 时读到的内容逐行比对目标行（#190/#191）及 §一 表尾（#210 止）。
- **结果：0 条并入行** —— 未发现自己没写过的新增行（无机器人 `queue_appender` 在窗口内追加），**无撞号**，无需按协议〇.6 续排。
- **写回方式**：全程**行级定点替换**（Edit 精确匹配单行 old_string），未做整文件重排/整文件写回，符合 2026-07-31 首班实证的更稳做法。
- **写回后核验**：#190/#191/#211/#212/#213/#214 六行**列数均为标准 8 列**，§四 #42/#43 均为 4 列（无裸竖线，正文内一律用全角 ／）；`企微反馈自动归档 + 待领` 组合**归零**（0 条残留）。

### 取号

- **§一 用 `--reserve`**：acquire 时 `--reserve 6 --section 一`，工具原子分配并回写，返回字面编号 **#211、#212、#213、#214、#215、#216**。实用 4 个，**#215／#216 预留未用，按规则不复用、留空**。顶部 §一 高水位线已由工具同步回写至 **#216**。
- **§四 手工 +1（工具限制，见队列 #185）**：`--reserve` 单次只能指定一个 `--section`，本轮 §四 编号取自**队列顶部高水位线回显值 `§四 #41`**（acquire 后本机 PowerShell 直读确认仍为 #41），+1 续排得 **#42、#43**；已手工把高水位线改写为 **§四 #43**。留此痕供核对。

### 锁与批次

- 编辑锁：`acquire` 成功（无冲突，非降级路径）→ 四处行级编辑 → **立刻 `release` 成功**（返回"已释放"）。
- §二 批次已登记：**`B-0803拆件巡逻190与191`**，四个文件全路径齐全，状态列为「**待处理**（登记，待 sweep 落库）」，**不含 ✅**。

### 本轮有无预案覆盖不了的情形

- **无**。两条回件均落在既定拆件范围内；两处需新下判断的点（替代料是否纳入范围、FI2 真实数据接入时点）已按红线转 §四，未自行落字。
- **两项执行环境边界（既定预案，不计入错写，不视为未完成项）**：
  1. **sweep 未触发** —— 本班为**非交互班次**（定时任务触发），批次已按规范登记 §二，由 `ZhuopinCommitSweep` 周期任务自然取活落库。
  2. **决策提醒未触发** —— 本轮新登了 §四 #42/#43 两项，本应跑 `decision_reminder_check.py`；非交互班次无本地 shell + 出网通道，**跳过**，由 `ZhuopinDecisionReminderDaily`（每日 08:30）兜底覆盖，代价是首次提醒延迟至次日早上。
- **一处如实说明**：#212 涉及的"两个字段"具体是哪两列，来自回件 docx 内的**截图红圈**，文字段落未点名列名——巡逻未对图片内容作猜测，已在 #212 行内写明"具体是哪两列需开卡对照回件截图确认"，交 CC 开卡核对，不臆测。

---

**live 首周逐日抽查中，出现一次错写即退回 dry-run。**
