---
title: 看护件 · 【CC】环境侧七条
created: 2026-09-20
派出线: 环境总线 OP-0920-B（派单件 `1-转型规划/0-全景路线图/派单件-泳道看护续棒-2026-09-20-B.md`）
批次: B-0920_环境侧七条
交付模式: 无头（他 2026-09-20 派单件 §一⑵ 明示；经 `C:\Tools\pwsh7\pwsh.exe` 起 `工具-opener批处理执行v2.ps1`）
status: 待执行
---

# 看护件 · 【CC】环境侧七条 · `B-0920_环境侧七条`

## 〇、口径

- 本批承接派单件 §一⑴：把接力卡 §二 现取的六条（`#561`／`#563`／`#567`／`#601`／`#416`／`#328`）＋派单件新并入的 `#621`，**一次领完**。
- **前置四查现取**（本机 `Get-Date` ＝ 2026-09-20 10:14:08）：① LAN 探针 `lan-status` ＝ **on-LAN（三项齐备）→ effective=on**，全部可领活入候选；② `git fetch` 后 `master` 与 `origin/master` 一致，主仓脏文件仅 `.claude/settings.json` 与 LAN 线两件在办看护件（已点名豁免至 09-22）；③ 编辑锁 **无锁**；④ 上一批 `OP-0919-K` 汇总已出、**没有泳道在跑**。
- 🔴 **D1 四档判据不在本件复述**——现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。判据未覆盖的动作按 🟡 停（fail-safe）；⏭️ 转出档不参与 fail-safe。
- 🔴 **方案闸（派单件 §〇）本批已过，五条照做**：① 判据只取结论性清单（文件名／表头行／汇总行），`*.patch` 一律走 `工具-main-leak回放.py`、禁 `cat`；② 扫池只用 `工具-队列查询.py --digest --actionable`；③ 凡触碰 git 的命令一律本机（Windows）侧跑，挂载侧已是机器守；④ 长跑段只看哨兵（`exit.txt`／`OPENER_DONE`／`OPENER_PARTIAL`），大间隔探；⑤ 判两处改动重不重叠先 `git diff --stat`／`--unified=0` 看 `@@` 头。

## 一、硬边界继承

1. 🔴 **红线照旧永不代办**：🔴 档（对外发送／L2 门禁签字／合规红线变更／ASIL C-D）只做到可发送态即停；⏭️ 档（`.51` 部署及任何触碰生产服务的动作）本批不自行执行、一律先 `transfer-out` 转出并跳过该项继续其余；🟡 档（ff 入 master／改口径判据阈值／design 审／关他人在办队列行）以 `OPENER_PARTIAL` 停等一字母。
2. 🔴 **别往 `.claude/rules/` 任何文件加字**——现取合计 43,388 B ／ 阈值 30,720 B，已超 12,668 B；`两桌同步与取证.md` 单份 12,339 B 亦超单份阈值 8,192 B。真要处置＝一整件活，须先过方案闸，本批**不开**。
3. 🔴 **别往根 `CLAUDE.md` 加字**——12,110／12,288，余 178 B；sweep 已改为**拒绝**含根 `CLAUDE.md` 的超阈值批次。
4. 🔴 **别碰卡在他一字母的行**：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213` 七个 Owner（永不代填）。
5. 🔴 **不碰 `#539`／`#538`**（同触碰 SC2 周报页面，已判不领）；不碰 LAN 线两件在办看护件。
6. 🔴 **收工只 push 自己那条分支**，不碰主仓、不 ff master、不在主仓 commit；主仓 ff 由看护者收工串行做。
7. 🔴 **回归 narrow 到受影响的测试类**（node-id `<文件>::<类名>`，**不得写 `-k`**），不为一个类跑整份测试文件；纯文档／openspec 改动不传 `-Tests`。
8. 🔴 **看护会话不修工具**：跑批期间发现机制缺陷只登记＋出派单，不就地修；唯一例外＝本批会因该缺陷失败且修补 ≤10 行，须在队列行写明「因本批 X 失败就地修，N 行」。

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区（现取 `--row N --field all`） | 本波边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#563` | `企微回件线` | `5-平台底座/wecom-aibot-service/` 回件归档链路；新建 `0-学习与工具/工具-回件勾选提取.py`；`7-外部文档/`（只读）；**行内新增产出⑤**＝`0-学习与工具/skills源码/zhuopin-followup-letter/` 模板与 docx 生成段＋出件前机器守 | 🔴 口径 2026-09-20 他当场更正：`☑` 与 `☒` **都是已选中**，只有空白 `☐` 才是未选，出现哪个由 Word 控件形式决定。工具侧按**并集**判（`w14:checked val="1"` ∨ `☒` ∨ `☑`）兜在途件；**根治在源头**＝跟进信选择控件统一只用 `☑`，成品 docx 里出现 `☒` 即 fail-loud 拒绝。**存量已发出的件不回改。** |
| 1 | A2 | `#567` | `无头起跑线` | `0-学习与工具/工具-opener批处理执行v2.ps1`（L82 子进程 shell 选取／L240 `Start-Job`／`Exit-WithCode` 覆盖面）＋ `0-学习与工具/test_工具-opener批处理执行v2.py` | 并入两条：`Start-Job -WorkingDirectory` 在 PS 5.1 崩；崩后不落 `exit.txt`，须补 `finally`。🔴 **本批自身正由该脚本起**——改动只在本泳道 worktree 内，**不得回写主工作区**，ff 留给看护者 |
| 1 | A3 | `#621` | `出件顺序线` | `0-学习与工具/工具-opener生成.py`；`0-学习与工具/test_工具-opener生成.py`；`1-转型规划/0-全景路线图/opener骨架.md`；`0-学习与工具/工具-opener块lint.py` | 缺陷形态：派单件先落档、再跑生成器，P7① 查重把「占号者就是本件自己」判成撞号。**实证＝ `OP-0920-A` 号已白烧不回收**；**本件生成 7 条 opener 时是先生成、后落档**，即当下的人守绕法，要根治成机器守 |
| 1 | A4 | `#601` | `落库sweep线` | 新建 `0-学习与工具/工具-承载性一致性扫描.py`；`0-学习与工具/工具-落库sweep.py`（接第 9 类告警）；`reports/`（首轮基线） | 🔴 **派单件正本＝ `1-转型规划/0-全景路线图/派单件-【CC】新场景intent闸机器守-2026-09-17.md`（2026-09-17 整件重写版），照它执行**；初版「intent 闸没建」前提是错的，闸已在 CI 上跑，**不得作派单依据、不碰 intent 闸** |
| 2 | A5 | `#328` | `落库sweep线` | `0-学习与工具/工具-落库sweep.py`（起跑段 `_fetch`／`_push_any_unpushed_commits`／`_check_preconditions`）＋ `0-学习与工具/test_工具-落库sweep.py` ＋ `.gitignore` ＋ `0-学习与工具/工具-共享文档编辑锁.py`（**只读，不改**） | 🔴 **硬序：必须在 A4 之后**——同触碰 `工具-落库sweep.py`。开工前先 `git log --oneline -1 claude/op0920f-carrier-scan-601` 确认 A4 已收工，或以 `OPENER_PARTIAL: 等 A4 落地` 收尾 |
| 2 | A6 | `#416` | `企微回件线` | `5-平台底座/wecom-aibot-service/aibot_service/connection.py`、机器人 release 调用处、`0-学习与工具/工具-共享文档编辑锁.py` 与其单测、`openspec/changes/editlock-aibot-registration-completeness-exemption/` | 🔴 **硬序：必须在 A1 之后**——同触碰 `5-平台底座/wecom-aibot-service/`。只做仍开的 ⑴⑵⑷⑸⑺ 五项；🔴 **锁工具族恒最后一波**，本条即是 |
| 2 | A7 | `#561` | `收口堵塞线` | `openspec/changes/`（**全程只读**）；队列 §一 多行回写（经锁） | 🔴 **本波边界收窄：只做诊断＋分类清单＋队列回写，不动任何 openspec 包文件、不碰 `0-学习与工具/工具-落库sweep.py`**（若判定需要机器判据，**另立一行留下一棒**，不在本波做）——否则与 A4／A5 撞触碰区 |

**硬序小结**：`落库sweep线` ＝ A4 → A5 串行；`企微回件线` ＝ A1 → A6 串行；`无头起跑线`／`出件顺序线`／`收口堵塞线` 各自独立。`-MaxParallel 4`、`-StaggerSec 90`（同波错峰 ≥90 秒）。

**「LAN 留步」清单**：本批 **无**——探针实测 on-LAN，且七条**均不触碰 `.51` 与任何生产服务**，无 ⏭️ 转出项。

## 三、泳道 opener

### A1 · 回件勾选提取根治（并集判兜在途＋源头统一只用 ☑）（§一 #563）

粘贴端：CC ｜ 泳道：企微回件线

```
[OP-0920-C]【CC】勾选提取563
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920c-checkbox-extract-563`） ｜ worktree：☑（op0920c-checkbox-extract-563，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920c-checkbox-extract-563 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920c-checkbox-extract-563 --done --batch B-0920_环境侧七条 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · `-Detach` 在 PS 5.1 下必炸，并入两条（§一 #567）

粘贴端：CC ｜ 泳道：无头起跑线

```
[OP-0920-D]【CC】无头起跑567
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920d-detach-ps51-567`） ｜ worktree：☑（op0920d-detach-ps51-567，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920d-detach-ps51-567 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920d-detach-ps51-567 --done --batch B-0920_环境侧七条 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · opener 出件顺序缺陷：查重把占号者是本件自己判成撞号（§一 #621）

粘贴端：CC ｜ 泳道：出件顺序线

```
[OP-0920-E]【CC】出件顺序621
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920e-opener-order-621`） ｜ worktree：☑（op0920e-opener-order-621，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920e-opener-order-621 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920e-opener-order-621 --done --batch B-0920_环境侧七条 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A4 · 承载性一致性扫描器，接 sweep 第 9 类（§一 #601）

粘贴端：CC ｜ 泳道：落库sweep线

```
[OP-0920-F]【CC】承载性扫描601
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920f-carrier-scan-601`） ｜ worktree：☑（op0920f-carrier-scan-601，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920f-carrier-scan-601 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920f-carrier-scan-601 --done --batch B-0920_环境侧七条 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A5 · sweep 起跑段 `_fetch()` 失败即整轮中止（§一 #328）

粘贴端：CC ｜ 泳道：落库sweep线

```
[OP-0920-G]【CC】起跑段328
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920g-sweep-fetch-328`） ｜ worktree：☑（op0920g-sweep-fetch-328，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920g-sweep-fetch-328 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920g-sweep-fetch-328 --done --batch B-0920_环境侧七条 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A6 · 企微回件链路三缺陷 ⑴⑵⑷⑸⑺（§一 #416）

粘贴端：CC ｜ 泳道：企微回件线

```
[OP-0920-H]【CC】回件链路416
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920h-wecom-reply-416`） ｜ worktree：☑（op0920h-wecom-reply-416，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920h-wecom-reply-416 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920h-wecom-reply-416 --done --batch B-0920_环境侧七条 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A7 · openspec 收口段堵塞：只诊断＋回写，不碰 sweep（§一 #561）

粘贴端：CC ｜ 泳道：收口堵塞线

```
[OP-0920-I]【CC】收口堵塞561
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920i-openspec-closure-561`） ｜ worktree：☑（op0920i-openspec-closure-561，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920i-openspec-closure-561 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920i-openspec-closure-561 --done --batch B-0920_环境侧七条 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为**无头**（他 2026-09-20 派单件明示），实际由 `工具-opener批处理执行v2.ps1` 起 7 条无头 CC，本节**不粘贴**、只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0920-J]【CC】看护七条看护
【设置】执行环境：CC ｜ 分支：env7-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0920J-看护七条看护。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】环境侧七条-2026-09-20.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总

🔴 **现取，不手工数**：

```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0920_环境侧七条
```

必含列：每泳道一行 ＋ 心跳全文 ＋ **D6「本批停 N 次｜逐次：泳道／档位／停在什么动作／他答了什么／等了多久」** ＋ **本批转出 N 项（须走 `zhuopin-lan-closeout`）** ＋ **本批 ⏭️ 档授权放行 N 次｜执行 N 项** ＋ 「待回内网」清单 ＋ 本批终态泳道 N 条。🔴 **等待时长只认 `reports/lane-watch-state.json`，会话不得自算。**

收工三步（派单件 §一⑶）：落 §二 批次 → `python 0-学习与工具/工具-落库sweep.py` → push。🔴 锁走 **一次 `acquire` → 多次 `edit-row --who` → 一次 `append-row --section 二` → 一次 `release`**，**不要串联 `commit-edit`**。🔴 §二 文件清单每个路径**必须用反引号包**。🔴 ff 前先清残留 worktree（`Remove-Item -Recurse -Force` ＋ `git worktree prune --expire now`）。

🔴 **第三道闸**：收工前复查主仓脏文件 → 出 `*-main-leak.patch`，判读一律走 `python 0-学习与工具/工具-main-leak回放.py`，**禁 `cat` 补丁正文**。
