---
title: "看护件 · 泳道看护批 B-0902_46（业务总线首次正式跑）"
created: 2026-09-02
批次: B-0902_46
派出线: Cowork 业务总线 OP-0902-X3
触发: Shao Peishen「开启泳道看护」＋「能领尽领，只看前置阻塞、不看到期」
LAN: on-LAN（三项齐备，实测）
status: 执行中
---

# 看护件 · 泳道看护批 `B-0902_46`

## 〇、本批口径

**候选判据（Shao Peishen 2026-09-02 当场校正，本批据此重扫）**：**只看有没有前置阻塞，不看到期、不看优先级、不看域。** 上一轮扫池误用「到期远／不阻塞在建场景／价值一般」当排除理由——**那是优先级判据**，导致 `#374`／`#373` 两条无阻塞的活被漏掉。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`，**本件不手抄四档表**。

## 一、硬边界（继承，逐条适用于本批每条泳道）

1. 🔴 **⏭️ 档：`.51` 部署及任何触碰生产服务的动作，本包永不执行** —— 跑 `transfer-out` 标注去向后**跳过该项、继续其余不依赖它的任务**，交 `zhuopin-lan-closeout`；**不进 `OPENER_PARTIAL`**。
2. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 相关永不代办**，只做到「可发送态」即停。
3. 🔴 **业务*数据*与一切对外发送永不自动入批**（worktree 内的业务建造可做——git 能回滚；救不回来的是被改写的真实数据）。
4. 🔴 **所有 `pause` / `check-heartbeat` 一律带 `--no-notify`，无一例外** —— 推送目标写死为跨部门大群（17 人含各部门专员），2026-09-02 首跑已误发过一次内部技术细节且无法撤回。决策提示改由 Cowork 会话内以「需你定夺」端给 Shao Peishen。
5. 判据未覆盖的动作**按 🟡 停**（fail-safe）；⏭️ 转出档不参与 fail-safe。
6. 🔴 **不自造豁免理由**；他线在办行不碰；规划文档不碰。
7. 🔴 **串行闸**：本批任何泳道**不得起草或发送任何跟进信**——唐燕萍 🔒／姚祖怡 🔒／陈忱 🔒 三闸全锁。`#344` 只做**判例包成文**（内部件），**不进入发送流程**。

## 二、波次与触碰区矩阵

| 波 | 泳道名 | 项 | 队列行 | 触碰区 | 与谁重叠 |
|:--:|---|:--:|---|---|---|
| 1 | `queue-triage` | A1 | §四 全表 | 队列 §四（只读） | 无 |
| 1 | `sc8-leadtime` | A2 | §一 #403 ⑵ | U9C 只读查询 ＋ 新建统计脚本 | 无 |
| 1 | `oem-chroma` | A3 | §一 #374 | OEM 隔离层归属评估（文档） | 无 |
| 1 | `fi3-prep` | A4 | §一 #373 | FI3 前置交付项 | 无 |
| 2 | `sc8-kitdate` | A5 | §一 #118 | `sc8/` 齐料日期取值链 | 🔴 与 A6 同一函数 |
| 2 | `sc8-kitdate` | A6 | §一 #344 | 同上 | 🔴 与 A5 同一函数 |

🔴 **A5／A6 同泳道串行（按编号升序），不得并行** —— 两者落在同一个齐料日期取值函数上，且**方向相反**（`#344` 取最早、`#118` 三条规则要求取更晚）。分开跑必然互相覆盖。

**波 1 四条泳道并行**（`-MaxParallel 3`、`-StaggerSec 90` 错峰，第四条顺延）。

## 三、泳道 opener

### A1 · §四全表状态分诊

粘贴端：CC ｜ 泳道：queue-triage

```
[OP-0902-A1]【CC】§四全表状态分诊
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902a1-section4-triage`）｜ worktree：☑（section4-triage，新 worktree，收工自删）｜ 工作区：无（纯库内只读＋出报告，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_46
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A1-§四分诊。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
心跳：开工 1 分钟内在 `reports/lane-heartbeat/B-0902_46-queue-triage.md` 写首行「HH:mm:ss ｜ 已开工」，每里程碑追加一行，收工写「DONE ｜ 产出落点」。预计等待超 10 分钟的步骤，等待期内须补写一行「仍在等 X，预计还要 N 分钟」。
分档判据现取：`python 0-学习与工具/工具-泳道看护状态机.py criteria`，不手抄。命中 🟡/🔴 时先跑 `python 0-学习与工具/工具-泳道看护状态机.py pause --batch B-0902_46 --wave 1 --lane queue-triage --action-key <D1键名> --waiting-for "<在等什么>" --no-notify`（🔴 `--no-notify` 必带），随后以 `OPENER_PARTIAL: 停在<档>——<在等什么>` 收尾。命中 ⏭️ 时跑 `transfer-out`（同参数族，不带 waiting-for），跳过该项继续其余。
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0902_46-2026-09-02.md` § 一硬边界 → ② `CLAUDE.md` §5 恢复上下文。本件为 A 类（口径已定），直接开工。

做什么（纯只读取证，出一份报告，🔴 不改队列任何一行）：
1. 逐行读 `1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md` §四 全部行（§四 恒在机制环境文件）。
   🔴 判据：**不得用 `--digest`／行首摘要判状态** —— §四 行首是立行时原文，✅ 闭环追记全在行尾，
   摘要里看不见。2026-09-02 Cowork 侧正是这样把已闭环的 #140 误报成「待他答」（实测已于 08-29 答 (a) 闭环）。
   必须读每行**全文**，以行尾最新追记为准。
2. 每行判三态之一并给出判据出处（引一句原文）：
   ⒜ **真待 Shao Peishen 答**（无 ✅ 闭环追记、无「已拍板」字样）
   ⒝ **已闭环**（有拍板/已执行/已解决追记）
   ⒞ **待他人**（等专员回件／等他线／等外部输入）——须写明等谁
3. 产出 `reports/section4-triage-2026-09-02.md`：三张表 ＋ 每行一句判据出处 ＋ 统计（各态几条）。
   🔴 ⒜ 那张表按「答完能解锁什么」排序，并标注解锁后的活属 D1 哪一档。
4. 收工：§二 批次登记走 `0-学习与工具/工具-共享文档编辑锁.py`（勿裸改）；产出路径写进心跳 DONE 行。

不做什么：
- 🔴 不改任何队列行（本项是只读分诊，改判属另一件事，须先有这份报告）
- 不碰 §一、不碰业务场景队列
```

### A2 · 提前期中位数统计（#403 D-2(b)）

粘贴端：CC ｜ 泳道：sc8-leadtime

```
[OP-0902-A2]【CC】提前期中位数统计
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902a2-leadtime-median`）｜ worktree：☑（leadtime-median，新 worktree，收工自删）｜ 工作区：无（只读查 U9C 端点＋库内出脚本与报告，🔴 不部署、不触碰 `.51` 服务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_46
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A2-提前期统计。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名。
心跳：`reports/lane-heartbeat/B-0902_46-sc8-leadtime.md`，规则同 A1（开工首行、里程碑追加、长等待补写、收工 DONE）。
分档判据现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`；🟡/🔴 走 `pause --batch B-0902_46 --wave 1 --lane sc8-leadtime --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL` 收尾；⏭️ 走 `transfer-out` 后跳过继续。
读 ① 队列 §一 #403（`python 0-学习与工具/工具-队列查询.py --row 403 --section 一`）→ ② `CLAUDE.md` §5／§7。本件为 A 类，直接开工。

背景：§四 #125 已拍板 D-2 ＝ (b)。当前提前期是**写死的 30 天**，陈承已两次答复 ERP 的 `PurProcessLT` 字段不准确、不要取（2026-07-15、2026-08-24），我方接受并另寻来源——本项就是那个来源。

做什么：
1. 用既有 U9C 端点 `Purchase/Query` ＋ `GR/Query`（🔴 只读查询，不写任何数据），按料号统计
   「采购单制单日 → 实际入库日」的天数分布，出**中位数**（另附 P25／P75 与样本量）。
2. 🔴 数据卫生先行，不可跳：① 剔除退货/红字单；② 剔除一单多次分批入库里的非首次到货（或显式说明口径取哪一次）；
   ③ 样本量不足的料号（阈值自定并写明理由）不出中位数、标「样本不足」。
   ⚠️ 财务域已有先例教训：join 字段字面不一致会静默落空且不报错——本项 join 料号前先验字面一致性。
3. 产出：`4-数字员工/` 下相应场景内的统计脚本（可重跑）＋ `reports/` 下一份结果报告（含口径说明与样本量）。
4. 🔴 **不要**把结果直接写进任何生产配置替换那个 30 天 —— 替换属改口径（🟡 档），须先经 Shao Peishen。
   本项只交「这个数是多少、怎么算的」。
5. 收工：§二 批次登记（走编辑锁工具），队列 §一 #403 ⑵ 回写（✅ 写在状态列开头）。

不做什么：
- 🔴 不改任何生产参数、不部署、不碰 `.51`
- 不动 #403 的 ⑴⑶ 子项（已完成）
```

### A3 · OEM 隔离层 Chroma 归属重判（#374）

粘贴端：CC ｜ 泳道：oem-chroma

```
[OP-0902-A3]【CC】Chroma归属重判
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902a3-oem-chroma`）｜ worktree：☑（oem-chroma，新 worktree，收工自删）｜ 工作区：无（评估与文档，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_46
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A3-Chroma归属。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名。
心跳：`reports/lane-heartbeat/B-0902_46-oem-chroma.md`，规则同 A1。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_46 --wave 1 --lane oem-chroma --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #374 全文 ＋ §四 #88 → ② `5-平台底座/CLAUDE.md`（OEM 隔离边界正本）→ ③ 根 `CLAUDE.md` §7 红线 3。本件为 A 类，直接开工。

背景：本行 2026-08-22 立、至今未开工，行内明写「默认沿用比重判更省事、因而更容易一直拖着」——
Shao Peishen 2026-09-02 明确：**不看到期、只看前置阻塞，无阻塞即开工**。本行无前置阻塞。

做什么（评估 ＋ 出结论件，🔴 不改实现）：
1. 读清现状：OEM 隔离层 Chroma 当前归属在哪、由谁写入、检索时如何按客户路由。
2. 逐条对照根 `CLAUDE.md` §7 红线 3（OEM 数据隔离：涉 OEM 技术数据按客户路由、禁跨库）与
   `5-平台底座/CLAUDE.md` 的隔离边界（含质量域扩展：PPAP/FMEA/含 OEM 信息的 8D 客诉）。
3. 判：**沿用现归属** 还是 **重判归属**？两条都要写清代价，尤其
   🔴 IATF／ISO 26262 语境下「同一 Chroma 实例承载多 OEM 数据」是否站得住。
4. 🔴 openspec 门槛自评：按 `CLAUDE.md` §5 三条门槛逐条对照（改跨场景口径／涉鉴权与数据可见性／改对外语义）。
   **本项极可能命中第二条（数据可见性）** ⇒ 若命中，本泳道只出 proposal 与 design 草案，
   **design 审属 🟡 档** ⇒ 跑 `pause --action-key openspec_design_review` 并以 `OPENER_PARTIAL` 收尾，不自行通过。
5. 收工：§二 批次登记，队列 §一 #374 回写。

不做什么：
- 🔴 不改任何隔离层实现代码（本项是评估）
- 🔴 不自行通过 design 审
```

### A4 · FI3 前置交付项（#373）

粘贴端：CC ｜ 泳道：fi3-prep

```
[OP-0902-A4]【CC】FI3前置交付项
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902a4-fi3-prep`）｜ worktree：☑（fi3-prep，新 worktree，收工自删）｜ 工作区：无（库内建造与文档，🔴 不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_46
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A4-FI3前置。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名。
心跳：`reports/lane-heartbeat/B-0902_46-fi3-prep.md`，规则同 A1。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_46 --wave 1 --lane fi3-prep --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #373 全文（含它的任务列，本 opener 不复制其正文）→ ② §一 #339（FI3 排期行，`[S:timed=2026-10-01]`，🔴 **只读参考、不得触碰该行**）→ ③ `CLAUDE.md` §5／§7。本件为 B 类：**读完 #373 任务列后，若其范围不清晰，先在心跳里写明疑点并 `pause --action-key <按 criteria 判>` 等人，不要猜着做**。

背景：本行 9 月上旬到期、状态「待领」、无前置阻塞。FI3 前置按纪律 9 月开工、10 月底就绪。

做什么：
1. 先读 #373 任务列，把「这一项到底要交什么」写成一句话，写进心跳。
2. 按其范围执行（属库内建造/文档/openspec 起草者直接做完；一旦触及 `.51` 或真实数据写入 ⇒ 立即 `transfer-out` 或 `pause`）。
3. 🔴 财务域建造第一步别省（根 `CLAUDE.md` §5）：若涉发票号 join，**先验税务导出发票号与 U9C `InvoiceNo` 字面严格一致**
   （位数／前导零／空格／全半角／代码前缀）——对不上 join 全盘落空**且不报错**。
4. 收工：§二 批次登记，队列 §一 #373 回写。

不做什么：
- 🔴 不碰 §一 #339（那行明写「到期前巡检不得催办」）
- 🔴 不部署、不改真实数据
```

### A5 · ATP 引擎立项（#118）

粘贴端：CC ｜ 泳道：sc8-kitdate

```
[OP-0902-A5]【CC】ATP引擎立项
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902a5-atp-propose`）｜ worktree：☑（atp-propose，新 worktree，收工自删）｜ 工作区：无（openspec 起草，不写实现、不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_46
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A5-ATP立项。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名。
心跳：`reports/lane-heartbeat/B-0902_46-sc8-kitdate.md`，规则同 A1。🔴 本泳道有两段（A5→A6），A5 收工后 A6 接着跑同一心跳文件。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_46 --wave 2 --lane sc8-kitdate --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #118 全文 → ② §四 #65 全文 → ③ `4-数字员工/` 下 SC8 场景 CLAUDE.md → ④ 根 `CLAUDE.md` §5。本件为 A 类，直接开工。

🔴 前置已解除（这是本项能开工的唯一原因，务必看清）：
§四 #65 —— Shao Peishen 2026-09-02 答 (a)：**认姚祖怡（男）2026-08-18 的书面文本即显式签认**，
三条齐套规则可直接用于建造，不必再走判例批改签认。#118 行内那句「在其答复前不据此开工」**至此解除**。
⚠️ 他同时接受了 (a) 的代价并已留痕：IATF 审核问「这条判据谁签的」时，证据是企微文本而非签认件；
且规则1 的「往前推 3 个月的 20 日」**无对照案例可验**。⇒ 你在 design 里必须把这三条规则**逐字引用原文**并标注来源与日期。

做什么（🔴 只到 openspec proposal ＋ design 草案，不写实现）：
1. 逐条核实姚祖怡 08-18 那三条规则与现有实现是否对得上、规则1 是否已被覆盖、
   改它会牵动多少行四色判定 —— 队列行明写「须 CC 建造时实测确认，巡逻不替其定性」。**用实测，不用推断。**
2. 🔴 特别注意方向冲突：`#344` 记的现状是「齐料日期只取最早答交日期」，
   而规则3 要求对远期订单取**更晚**的日期 —— 两者落在同一取值函数上、方向相反。
   **在 design 里显式写清这个冲突怎么解**，不得含糊带过。
3. 走 `/opsx:propose` 出变更包（proposal ＋ design ＋ tasks），
   openspec 门槛按 §5 三条自评——**本项几乎必然命中「改跨场景口径」**，故必走 design 审。
4. 🔴 **design 审属 🟡 档** ⇒ 起草完即跑 `pause --action-key openspec_design_review`，以 `OPENER_PARTIAL` 收尾，
   **不自行通过、不进入实现**。
5. 收工：§二 批次登记，队列 §一 #118 回写。

不做什么：
- 🔴 不写任何实现代码（本项到 design 草案为止）
- 🔴 不发任何信（姚祖怡串行闸锁着，`采购部#20` 在途）
```

### A6 · 齐料日期判例包成文（#344）

粘贴端：CC ｜ 泳道：sc8-kitdate

```
[OP-0902-A6]【CC】齐料日期判例包
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902a6-kitdate-cases`）｜ worktree：☑（kitdate-cases，新 worktree，收工自删）｜ 工作区：无（内部件成文，🔴 不发送、不部署）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_46
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A6-判例包。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名。
心跳：`reports/lane-heartbeat/B-0902_46-sc8-kitdate.md`（与 A5 同一文件，接着追加）。
🔴 本项在同一泳道内排在 A5 之后：开工先跑 `python 0-学习与工具/工具-泳道看护状态机.py show --lane sc8-kitdate`，
**若显示 paused（A5 停在 design 审），立即以同样的 `OPENER_PARTIAL: 依赖 A5 未解除` 收尾，不得盲目往下做** —— 本项与 A5 落在同一取值函数上。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_46 --wave 2 --lane sc8-kitdate --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #344 全文 → ② `6-人才与组织/需求确认方式升级-判例批改法与微会机制-2026-07-25.md`（判例批改法正本）→ ③ `6-人才与组织/CLAUDE.md` §一。本件为 A 类，直接开工。

做什么（🔴 只成文，绝不发送）：
1. 按判例批改法出判例包：**≤10 条真实案例**，每条给「现状判定 vs 拟改判定」对照，
   收信人只需做 ✅对／❌错／✏️改判 ＋ 一句话。🔴 **禁止抽象设问。**
2. 🔴 先与姚祖怡（男）2026-08-18 那三条规则对齐 —— 队列行明写：
   **同一取值函数、方向相反，不对齐＝请他签两套口径**。
   A5 若已产出 design 里的冲突解法，直接沿用；未产出则本项 `pause` 等 A5。
3. 判例包属**判据/口径类** ⇒ 🔴 **永不默认生效**（IATF 显式签认红线），
   件内不得出现「48h 未回复即默认生效」一类措辞。
4. 称呼纪律：正文称呼**一律全名「姚祖怡」，不写「姚工」**（同姓混淆，`6-人才与组织/CLAUDE.md` §5.2）。
   🔴 成文后全文搜「他／她／他的／她的」逐个回 `6-人才与组织/人员名录-称谓与性别-正本.md` 核对——**姚祖怡是男性**。
5. 收工：§二 批次登记，队列 §一 #344 回写「已成文、待闸开后发」。

不做什么：
- 🔴 **不发送、不进入送审发送流程** —— 姚祖怡串行闸锁着（`采购部#20` 在途），
  且发送属 D1 🔴 档，本包永不代办。只做到「可发送态」。
- 🔴 不在 README 写任何终态，起草产物的发送状态列只能是 `⏳ 待你审`
```

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0902_46` 现取（不手工数），须含：每泳道一行 ＋ 心跳全文 ＋「本批停 N 次｜逐次：泳道／档位／停在什么动作／他答了什么／等了多久」＋「本批转出 N 项（须走 `zhuopin-lan-closeout`）」＋「待回内网」清单。
