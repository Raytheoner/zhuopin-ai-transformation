---
title: "看护件 · 泳道看护批 B-0909_三包apply"
created: 2026-09-09
status: 🔴 已作废（Shao Peishen 2026-09-09 12:0x 明令；候选已重扫并入 B-0909_462与504apply。作废理由：两份看护件在同一日排了重叠泳道，见 §一 #397 扩后判据；原文保留供追溯，不得再据此起链）
派出线: 泳道看护 OP-0909-A
批次: B-0909_三包apply
---

# 看护件 · 泳道看护批 `B-0909_三包apply`

## 〇 · 口径

- **触发**：Shao Peishen 2026-09-09 07:31（本机 `Get-Date`）在 Cowork 说「开启泳道看护」。**规则正本**＝该 skill 的 `SKILL.md`（执行编排）＋ `openspec/changes/lane-watch-mode/design.md`（D1-D7，冲突以后者为准）；**D1 四档一律现取**（`工具-泳道看护状态机.py criteria`），本件不复述判据表。
- **前置四查（全过）**：① LAN 探针 ＝ **⛔ off-LAN**（三项未齐）⇒ 排除依赖内网的活；② `git fsck` 仅 dangling、`origin/master` **ahead=0 behind=0**；③ 编辑锁无占用；④ 上一批 `B-0908_四泳道机制根治` summary 已出（停 6 次，5 次已答、1 次未答）。
- **本批规模**：3 条泳道并行（上限 4）、错峰 ≥90 s —— **候选供给受触碰区互斥限制，不凑数**（单批上限 10 未触顶，理由见 §二 未派表）。

## 一 · 硬边界（继承，不复述正本）

- ⏭️ **`.51` 部署及任何触碰生产服务的动作：本批不执行**，命中即 `transfer-out`、跳过该项继续其余，交 `zhuopin-lan-closeout`。off-LAN 下更无例外（`#478` 条件放行的前置之一是 LAN 探针实测 on，本批不成立）。
- 🔴 **对外发送／L2 门禁签字／合规红线变更／ASIL C-D：永不代办**，只做到可发送态即停。
- 🔴 **业务*数据*与一切对外发送永不自动入批**；真实库写动作／规划文档／他线在办行不碰；**不自造 🛑 豁免理由**。
- 🔴 **看护会话与各泳道均不修工具**：发现机制缺陷 ⇒ 只登记（§一／§四）＋出 CC 派单。唯一例外＝本批会因该缺陷失败且修补 ≤10 行，须在队列行写明「因本批 X 失败就地修，N 行」。
- 🔴 **各泳道只 push 自己那条分支**，不碰主仓、不 ff master、不在主仓 commit；主仓 ff 由 sweep 收尾段做。**看护者自己不 commit**，撞 `.git/index.lock` 不重试、按「登记待补」写队列行。

## 二 · 次序与并行矩阵

| 泳道 | 队列行 | 触碰区 | 波次 | 与谁互斥 |
|---|---|---|---|---|
| `docx-481` | `#481` | docx 读取模块与其调用点、其单测 | 1 | 无 |
| `scanner-462` | `#462` | 新建扫描器＋其单测；`aibot_service/open_pool_reminder.py`（只共用判定函数）；`0-学习与工具/工具-落库sweep.py`（新增告警类） | 1 | 🔴 **sweep 族**（`#500`／`#507`／`#459`）—— 本批不派该族，故本波无冲突 |
| `heartbeat-504` | `#504` | `0-学习与工具/工具-泳道看护状态机.py` ＋ 其单测 | 1 | `#478`／`#492`（均未派） |

**三条触碰区两两比对：零重叠。** 均为 🟢 档（worktree 内建造＋单测），可自动做完；收尾 ff 属 🟡，泳道不自合。

### 🔴 本批**未**派出的候选与原因（如实登记，避免下一棒重扫一遍）

| 候选 | 不派的原因 |
|---|---|
| `#500` sweep 载体脏检查 | **停在「合入 master 等一字母」**（🟡）。⚠️ 状态机记 `resumed`、队列行仍在等 —— 两处口径差，已端问 |
| `#507` sweep 覆盖校验自锁／`#459` `.pth` 盲区 | 与 `#500` 同触碰 `工具-落库sweep.py`；未合入前开工会在同一文件上分叉 |
| `#489` opener 块 lint | **paused 等一字母**（🟡），已等 7.8 h（超 D5 4 h）；他此刻在环，**不跑 `check-timeout` 收回**，直接端问 |
| `#503` opener-id-claim／`#487` 例外句 | 触碰含 `opener骨架.md`，与 `#489` 全重叠 ⇒ opener 族三条串行，等 `#489` 先落 |
| `#440` skill eval 套件 | 实查为**待 design 审**、非 apply（首段「可领」是旧文）⇒ 🟡，已端问 |
| `#443` 每周清扫／`#482` 串行闸 | 分别触碰两份队列真身、`工具-共享文档编辑锁.py` 闸校验段 —— 与本看护会话自身的写队列/acquire 同触碰 |

### LAN 留步清单（off-LAN，本批排除）

- `#506` 的 `.51` 部署与页面期次复算（Shao Peishen 2026-09-08 已答「合」，合入 master 后仍须部署 ⇒ 交 `zhuopin-lan-closeout`，**`采购部#22` 解押的必经一步**）。
- `#500` 行内明记的现网 165 个提交清账（⏭️ 档，已在 `B-0907_Q`）。

## 三 · 泳道 opener

### A1 · #481 统一 docx 读取工具 apply

粘贴端：CC ｜ 泳道：docx-481

```
[OP-0909-B]【CC】481docx读取
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909b-docx-481`） ｜ worktree：☑（op0909b-docx-481，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：泳道看护 OP-0909-A，批 B-0909_三包apply
读 ① 队列 §一 `#481`（`python 0-学习与工具/工具-队列查询.py --row 481 --field all`，做什么/不做什么/收工全在该行）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A2 · #462 规划倒逼扫描器 apply

粘贴端：CC ｜ 泳道：scanner-462

```
[OP-0909-C]【CC】462倒逼扫描器
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909c-scanner-462`） ｜ worktree：☑（op0909c-scanner-462，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：泳道看护 OP-0909-A，批 B-0909_三包apply
读 ① 队列 §一 `#462`（`python 0-学习与工具/工具-队列查询.py --row 462 --field all`，做什么/不做什么/收工全在该行）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A3 · #504 心跳件跨 worktree 可见性 apply

粘贴端：CC ｜ 泳道：heartbeat-504

```
[OP-0909-D]【CC】504心跳可见性
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909d-heartbeat-504`） ｜ worktree：☑（op0909d-heartbeat-504，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：泳道看护 OP-0909-A，批 B-0909_三包apply
读 ① 队列 §一 `#504`（`python 0-学习与工具/工具-队列查询.py --row 504 --field all`，做什么/不做什么/收工全在该行）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

> 🔴 **三条 opener 共同内置四句（写在此处，不逐条复制进正文）**：
> ⑴ **D1 判据现取**：`python 0-学习与工具/工具-泳道看护状态机.py criteria` —— **不手抄判据表**；
> ⑵ **命中 🟡/🔴 时**：先跑 `python 0-学习与工具/工具-泳道看护状态机.py pause --batch B-0909_三包apply --wave 1 --lane <泳道名> --action-key <D1键名> --waiting-for "<在等什么>" [--option "<候选答案>"]...`，随后**以 `OPENER_PARTIAL: 停在<🟡|🔴>决策点——<在等什么>` 收尾**；
> ⑶ **命中 ⏭️ 时**：跑 `... transfer-out --batch B-0909_三包apply --wave 1 --lane <泳道名> --action-key <D1键名> --note "<需要走 LAN 收口做什么>"`，**不进 `OPENER_PARTIAL`**，直接跳过该项继续其余；仅当后续项确实依赖它时才对该后续项走 `OPENER_PARTIAL`；
> ⑷ **判据未覆盖的动作按 🟡 停**（工具的 `classify`/`pause` 已 fail-safe），在收尾原因里问「这个动作该进哪一档」，**不必自行判断该不该停**。
> 🔴 **心跳**：落 `reports/lane-heartbeat/<泳道名>.md` —— 开工 1 分钟内写首行「HH:mm:ss ｜ 已开工」，每里程碑追加一行，收工写「DONE ｜ 产出落点」。**预计等待 >10 分钟的步骤**（如全量回归）须在等待期内再写一行「仍在等 X，预计还要 N 分钟」，否则看门狗 30 分钟判读线会误报。
> ⚠️ **`#504` 泳道特别注意**：本行有一项**推定项、非明答** —— claude.ai 侧引用式装载器「已知缺陷」段的回改归属（`design` 决策点 7 拆出的第三个载体）。该项**已端给 Shao Peishen 再确认一次**，未获明答前**不得动它**，其余照做。

## 三bis · 看护者开场词

```
[OP-0909-A]【CC】看护三包apply
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：泳道看护 OP-0909-A，批 B-0909_三包apply
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0909A-看护三包apply。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0909_三包apply-2026-09-09.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四 · 收工汇总

🔴 **不手工数，一律现取**：`python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0909_三包apply`

须含：每泳道一行 ＋ 心跳全文 ＋ **D6 停点列**（`本批停 N 次｜逐次：<泳道>／<档位>／<停在什么动作>／<他答了什么>／<等了多久>`）＋ **转出项**（逐项写去向）＋ **⏭️ 授权放行计数** ＋ §二「LAN 留步」清单（汇总里称「待回内网」）。

🔴 **等待时长只认 `reports/lane-watch-state.json`**（由 `summary` 现取），**会话不得自算** —— 已实撞误报。

🔴 **末尾固定一段「待你一字母」清单**：同类合并（三条 merge 只问一次），每项写清 (a)/(b) 与各自代价、标〔荐〕与默认项；**design 审类排在 merge 类之前**；最后一行给答复模板。

---

## 📌 本批出件时顺带查出的两处生成器缺陷

**已登记进队列 §一 `#487`**（同族承接载体，该行触碰区正是 `opener骨架.md` 与生成器），本件不复述。**本件 §三 三条 opener 系按骨架手写三行、未用生成器成品**，`工具-opener块lint.py --enforce` 零违规。
