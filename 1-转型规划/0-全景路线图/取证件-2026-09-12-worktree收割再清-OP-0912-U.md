---
title: "取证件 · 【CC】12 个 worktree 收割再清（OP-0912-U）"
created: 2026-09-12
status: 生效
用途: 派单件《【CC】12个worktree收割再清》阶段一「收割」的逐个判定与手段、阶段二「清」的实际结果（10 个 remove 均被 git 拒、原文留存）、两个 error-7 的根因与目录取证；清理残局须看护者定夺
派出线: Cowork 环境总线 OP-0912-E（批 B-0912_worktree收割）
配套: 1-转型规划/0-全景路线图/派单件-【CC】12个worktree收割再清-2026-09-12.md
---

# 取证件 · 12 个 worktree 收割再清（OP-0912-U，2026-09-12 20:1x–20:4x 本地）

> 执行环境 CC 无头泳道 `op0912u-wt-harvest`，worktree `.claude/worktrees/wt-harvest`，分支 `claude/op0912u-wt-harvest`（自 master `f2cde15` 起）。
> 本件所有「已比对／已核」均附手段；时刻均为本机本地（UTC+8）。

## 〇、一句话结论

**12 个里没有一个含「只此一份、不在 master 的产出」**——派单件 〇 节担心的三批（#446 三个 .py／两个 openspec 包／两处工具改动），实查结果是：**两处工具改动与 a34c5e 的 15 项是伪脏（内容逐字节已在 master）**，#446 的 .py 与两个 openspec 包是**已被后来同题实现取代的平行草稿**（仍各提一条分支供审、未 ff）。**阶段二 10 个 `git worktree remove`（不加 `--force`）全部被 git 拒**（原文见 §三），按派单件 ⑶ 当场停手；两个 error-7 根因＝admin 目录缺 `commondir`，只取证未动。

## 一、阶段一「收割」：12 个逐个一行（结论＋手段）

| # | worktree | HEAD | 判定 | 手段（现取） | 落点 |
|---|---|---|---|---|---|
| 1 | `agent-a86bf597db6ecff18` | 307d5af（∈master） | **被取代**（实测 6 个未跟踪件，派单件只列 4 个：另有 `tests/docx_fixtures.py`、`tests/test_reply_form_detect.py`）。#446 已由 OP-0905-M 的 `reply_form_detection.py` 承接（`deaef66`→合入 `2d2ffe4`，#446 归档 `[S:done]`，归档件 `跨桌任务队列-归档-202609.md` digest 命中），#481 `5d6f27e` 又把判据迁入 `zhuopin_platform.shared_tools.doc_parser`。**但含两项 master 现无的能力**：`detect_plain_text_form`（.md/.txt 纯文本回件）、`extract_tables` 表格快照；单测 `pytest tests/test_reply_form_detect.py` → **15 passed**（本地实跑） | `git ls-files \| grep -iE 'reply_form\|回件形态'`（master 同名件 4 个）；`git log --all --grep 446`；两版文件头逐字对读 | 分支 `claude/op0912u-harvest-a86bf597` @ `00ba4b0`（6 文件 +1385 行；6 件 `cp` 后 `cmp -s` 全一致），已 push，**不建议原样合并**（与 `reply_form_detection.py` 并存会成第四份实现） |
| 2 | `agent-a286ad92b8989e26f` | 307d5af（∈master） | **被取代**。未跟踪 `openspec/changes/queue-domain-strict-routing/`（5 件 50 KB）＝#341 的平行起草包；同题包 `queue-domain-routing` 已 `f9c6ab7` 起草、`9cd21a4`/`912eda2` apply（2026-09-05），其 proposal 已覆盖本包「共享锚点锁 vs spec 独立锁」分叉发现 | `ls openspec/changes/ \| grep routing`；两包 proposal 首 40 行对读；`git log -- openspec/changes/queue-domain-routing` | 分支 `claude/op0912u-harvest-a286ad92` @ `35ebb56`（`cp -r` 后 `diff -r` 无差异），已 push，供审不 ff |
| 3 | `agent-ae8d2d2789466504a` | 307d5af（∈master） | **被取代**。未跟踪 `openspec/changes/editlock-write-column-integrity/`（7 件 44 KB）＝#455 的 OP-0905-G 平行起草包；#455 已由 OP-0905-N `editlock-write-guard-hardening` 承接并 apply 合入 `9d58d4f`（反引号奇偶＋回读列数＋--repair），`ee0aec4` 记 #534 patch-id 判「#455 三提交内容已在 master」 | `grep -rl '#455' openspec/changes/*/proposal.md`；`git log --grep 455`；proposal 对读 | 分支 `claude/op0912u-harvest-ae8d2d27` @ `88461e0`（`diff -r` 无差异），已 push，供审不 ff |
| 4 | `agent-ad6277e1afc466168` | cb981a8（∈master） | **无价值（伪脏：已在 master）**。已改未提交 `工具-落库sweep.py`（+346/−7）＋ `test_工具-落库sweep.py`（+309）＝ master `0d500e1`（2026-09-04 21:44 `feat(编辑锁/sweep): 批次登记预检＋队列改动即刻落库＋reconcile autostash＋日志周轮转`，stat 恰为 sweep +346／test +309）。补丁新增行逐行查 master 现文件：sweep 302 行中 301 行原样在、1 行（`_run_git(["commit", …])`）被 #479 `22cbd15` 改成带 pathspec 形态；test 258/258 在；3 个新增测试类名均在 | `git -C <wt> diff` → `reports/wt-harvest-0912/ad6277-sweep.patch`；python 逐行 set 比对；`git log -S'_rotate_weekly_logs'`；`git show 0d500e1 --stat` | 不落地 |
| 5 | `agent-ae061d52dee95c68b` | cb981a8（∈master） | **无价值（伪脏：已在 master）**。已改未提交 `工具-共享文档编辑锁.py`（+183/−6）＋ `test_…编辑锁.py`（+223）＝ 同一 `0d500e1`（stat 恰为 +183／+223）。新增行逐行查：test 188/188 在；工具 163 行中 143 在、20 行差异全部属 `_recent_commit_touched_paths`「最近 3 个 commit」窗口——master 2026-09-09 已改为「按登记日时间窗」（现文件 L5483 注释原文）即后续重构，非遗漏 | 同上，`reports/wt-harvest-0912/ae061d-editlock.patch`；`grep -n '_git_known_relative_paths' 工具-共享文档编辑锁.py` | 不落地 |
| 6 | `agent-a34c5e99547e8e4d6` | bc2005d（∈master） | **无价值（伪脏：零新内容）——15 项 staged 全是「删除」（8 插入／2755 删除），不是产出**。实测：`git -C <wt> write-tree` ＝ `2e1e5a89…` ＝ commit `83cd43c^{tree}`（字节一致）；`83cd43c` 是该泳道 2026-09-06 19:53 的原提交（reflog 可见），之后被 rebase 成 `bc2005d` 合入 master（两者 `git patch-id --stable` 同为 `1c44e4b2…`），分支 ref 前移而本 worktree 工作区停在 83cd43c 形态 ⇒ 「删了 CI／.gitignore／测试件」只是 83cd43c 基底（3c091c9）相对新 HEAD 少了后来 master 的提交。**不存在任何需要慎判的 CI／.gitignore 改动。** | `git -C <wt> reflog`；`write-tree` vs `rev-parse 83cd43c^{tree}`；`git diff-tree -p X \| git patch-id --stable` 两侧 | 不落地 |
| 7 | `agent-a5c54b8295bd5236c` | 307d5af（∈master） | **无价值（内容已在 master）——三个「未跟踪目录」其实是三个已注册的嵌套 worktree**（`.git` 文件 gitdir 指向主仓 `.git/worktrees/_check-*`），各自 **干净**（`status --porcelain` 空），HEAD `d1eaf9b`／`5ce17dd`／`8e55462`（#410／#447／#456 三分支的合并复核件，B-0905_B「3 分支合并复核」泳道遗留）。三者 HEAD **非** master 祖先，但 `工具-patchid比对.py --branch <sha>` 三次均 `✓ 内容已在 master`（≡ `d69aab95`／`0a70734b`+`99647be0`／`44af737a`） | `git worktree list`；`cat <dir>/.git`；`git -C <dir> status --porcelain`；patch-id 工具三跑 | 不落地；清法见 §四 |
| 8 | `interesting-rhodes-0da260` | f0b590d（∈master） | **有价值（低，纯文档）**。已改未提交 `openspec/changes/orchestration-guards/tasks.md`（+17/−4）：该包 4.3 全量回归结论（`4 failed, 1459 passed`，4 例均在未触碰文件）与 4.4/4.5 被高水位线滞后阻塞的记录；master 该文件仍停在 f0b590d 形态（4.3–4.6 未勾）；#487 其后由 OP-0909-X 另行完成，本补记不改状态、仅供归档时核对 | `git -C <wt> diff`；`git log -- …/tasks.md`；`--row 487` | 分支 `claude/op0912u-harvest-rhodes` @ `11be075`（`git apply --3way` 干净；去 CR 后 `diff` 无差异），已 push，供审不 ff |
| 9 | `agent-ab93efe1ec3e402d1` | 3cd9bf4（∈master） | **无价值**（派单件 ⑷ 直判）。仅 `.claude/settings.local.json` +5/−1＝权限 allowlist 自动追加（`edit-row --help` 等 5 条）；该文件在当前 master **已不再跟踪** | `git -C <wt> diff`；`git ls-files master -- .claude/settings.local.json` 空 | 不落地 |
| 10 | `morphology-1-criteria-stuck-9519f6` | 1d361f0（∈master） | **无价值**（同上）。仅 `.claude/settings.local.json` +4/−1 | 同上 | 不落地 |
| 11 | `musing-pascal-68d14e` | 697da77（∈master，2026-08-10） | **error-7，只取证**——见 §五。零未跟踪、零修改；仅 5 个 `.claude/commands/opsx/*.md` 在磁盘被删（HEAD 里有） | env 覆盖只读（§五） | 不动 |
| 12 | `recursing-hofstadter-eb7215` | d9dce22（∈master，2026-08-10） | **error-7，只取证**——同上，同 5 个 opsx 删除 | 同上 | 不动 |

## 二、提成的分支清单（`git fetch` 后 `git log origin/<b>` 实查）

| 分支 | commit | 内容 | `rev-list --count master..origin/<b>` |
|---|---|---|---|
| `claude/op0912u-harvest-a86bf597` | `00ba4b0` | #446 平行实现 6 件 | 1 |
| `claude/op0912u-harvest-a286ad92` | `35ebb56` | `queue-domain-strict-routing` 包 5 件 | 1 |
| `claude/op0912u-harvest-ae8d2d27` | `88461e0` | `editlock-write-column-integrity` 包 7 件 | 1 |
| `claude/op0912u-harvest-rhodes` | `11be075` | `orchestration-guards/tasks.md` 补记 | 1 |

四条均自 master `f2cde15` 起、各 1 commit、已 push origin、**未 ff、未登「已授权待合」**（`工具-待合分支巡检.ps1` 动作一只认 `reports/pending-ff.jsonl` 登记处，不会自动合）。前三条 commit message 明写「不建议原样合并」。

## 三、阶段二「清」：实际结果＝0 删、10 拒、2 未动

`git worktree remove <path>`（不加 `--force`）逐个实跑，退出码与原文：

```
[agent-a86bf597db6ecff18] rc=128 :: fatal: '.claude/worktrees/agent-a86bf597db6ecff18' contains modified or untracked files, use --force to delete it
[agent-a286ad92b8989e26f] rc=128 :: fatal: '.claude/worktrees/agent-a286ad92b8989e26f' contains modified or untracked files, use --force to delete it
[agent-ae8d2d2789466504a] rc=128 :: fatal: '.claude/worktrees/agent-ae8d2d2789466504a' contains modified or untracked files, use --force to delete it
[agent-ad6277e1afc466168] rc=128 :: fatal: '.claude/worktrees/agent-ad6277e1afc466168' contains modified or untracked files, use --force to delete it
[agent-ae061d52dee95c68b] rc=128 :: fatal: '.claude/worktrees/agent-ae061d52dee95c68b' contains modified or untracked files, use --force to delete it
[agent-a34c5e99547e8e4d6] rc=128 :: fatal: '.claude/worktrees/agent-a34c5e99547e8e4d6' contains modified or untracked files, use --force to delete it
[agent-a5c54b8295bd5236c] rc=128 :: fatal: '.claude/worktrees/agent-a5c54b8295bd5236c' contains modified or untracked files, use --force to delete it
[interesting-rhodes-0da260] rc=128 :: fatal: '.claude/worktrees/interesting-rhodes-0da260' contains modified or untracked files, use --force to delete it
[agent-ab93efe1ec3e402d1] rc=128 :: fatal: '.claude/worktrees/agent-ab93efe1ec3e402d1' contains modified or untracked files, use --force to delete it
[morphology-1-criteria-stuck-9519f6] rc=128 :: fatal: '.claude/worktrees/morphology-1-criteria-stuck-9519f6' contains modified or untracked files, use --force to delete it
```

🔑 **结构性事实**：`git worktree remove` 只认「脏不脏」，**收割进分支不会让原 worktree 变干净**——派单件 §二 条件 ⑴「已提成分支」与 ⑶「永不 --force、git 拒了即停」在这 10 个上**必然同时成立且互相抵消**。要让它们过 remove，唯一的非 `--force` 路径是**先把工作区复原到 HEAD 形态**（`git checkout -- <file>`／`git clean -fd`／删嵌套 worktree），那一步派单件没授权（⑶ 明写「不得绕过」），故本棒未做，留 §四 决策。

## 四、清理残局——交看护者定夺的项（本棒不代答）

**D1（🟡，产出已有第二份的 8 个）**：
- 伪脏 5 个：`agent-ad6277e1afc466168`／`agent-ae061d52dee95c68b`／`agent-a34c5e99547e8e4d6`（`git -C <wt> checkout -- .` 即复原，内容在 master `0d500e1`／`bc2005d`）、`agent-ab93efe1ec3e402d1`／`morphology-1-criteria-stuck-9519f6`（`git -C <wt> checkout -- .claude/settings.local.json`）；
- 已收割 3 个：`agent-a86bf597db6ecff18`／`agent-a286ad92b8989e26f`／`agent-ae8d2d2789466504a`（`git -C <wt> clean -fd -- <各自未跟踪路径>`，内容在 §二 分支，哈希已核）；
- 然后各自 `git worktree remove <path>`（仍不加 `--force`，git 再拒即再停）。
- （a）授权按上法复原后 remove —— 12 个 `[WT-BLOCKED]` 少 8 个，代价＝丢掉的只是第二份副本；（b）维持原状 —— 巡检每轮继续报。

**D2（🟡，`interesting-rhodes-0da260`）**：同 D1 法（`checkout -- openspec/changes/orchestration-guards/tasks.md`，内容在 `11be075`）；单列是因它是 4 个中唯一判「有价值」的。

**D3（🟡，`agent-a5c54b8295bd5236c` 含 3 个嵌套 worktree）**：先 `git worktree remove .claude/worktrees/agent-a5c54b8295bd5236c/_check-{editable-guard,followup-meta,lint-restate}`（三者干净、不需 `--force`；HEAD 非 master 祖先但 patch-id 判内容已在 master；其中 `5ce17dd`／`8e55462` 仅被这两个 worktree HEAD 引用，remove 后成悬空对象——可接受，内容在 master `99647be0`／`44af737a`），再 remove 父目录。派单件 §二 ⑵ 写的是 `is-ancestor`，三者不满足字面判据，故本棒未动。

**D4（🟡，两个 error-7）**：见 §五 修法；本棒只取证。

## 五、两个 error-7 的根因与目录取证

**根因（与派单件表述不同，以此为准）**：`.git/worktrees/musing-pascal-68d14e/` 与 `.git/worktrees/recursing-hofstadter-eb7215/` **admin 目录存在**（含 `HEAD`／`ORIG_HEAD`／`gitdir`／`index`／`logs`／`refs`／`fsmonitor--daemon`），**缺的是 `commondir` 文件（与 `config.worktree`）**——对照健康的 `.git/worktrees/interesting-rhodes-0da260/` 多出 `commondir`。没有 `commondir`，git 从 worktree 侧找不到公共对象库，于是 `git -C <wt>` 一律 `fatal: not a git repository`（即巡检的 error-7）。两目录 `gitdir` 文件 mtime 均为 2026-08-26 12:14（＝仓库迁出 OneDrive 当日，`gitdir` 被重写而 `commondir` 未带过来，推断、未实证）。

**只读取证手段**（不改任何文件）：`GIT_DIR=<repo>/.git/worktrees/<n> GIT_COMMON_DIR=<repo>/.git GIT_WORK_TREE=<repo>/.claude/worktrees/<n> git status --porcelain --ignored`。

| 项 | `musing-pascal-68d14e` | `recursing-hofstadter-eb7215` |
|---|---|---|
| HEAD | `697da77` 2026-08-10 `fix(editlock): 队列#322…`，detached；**∈ master** | `d9dce22` 2026-08-10 `docs(队列#314③)…`，detached；**∈ master** |
| reflog 末条 | 2026-08-11 `checkout: moving from claude/mutex-deadlock-permission-fix-df5cc5 to HEAD` | 2026-08-10 `checkout: moving from claude/recursing-hofstadter-eb7215 to HEAD` |
| 顶层 | `.claude .env.example .git .github .gitignore 0-学习与工具 1-转型规划 2-试点项目 3-治理与合规 4-数字员工 5-平台底座 6-人才与组织 CLAUDE.md openspec ~$智能AI转型全景规划.md.docx`（最后一项为 HEAD 与 master 均已跟踪的 Word 锁文件，非本棒范围） | 同 |
| 二层目录 | `.claude/{commands,skills}` `.github/workflows` `0-学习与工具/{.pytest_cache,__pycache__,md转Word工具,skills源码,定时任务源码}` `1-转型规划/{0-全景路线图,AI运营指挥中心,z-已执行归档}` `3-治理与合规/外部评审` `4-数字员工/{档案,财务部,质量部,运营部,采购部}` `5-平台底座/{deploy-tools,wecom-aibot-service,zhuopin_platform}` `6-人才与组织/部门AI专员跟进` `openspec/{changes,specs,templates}` | 同 |
| 文件数／体积 | 1515 / 22 MB | 1260 / 18 MB |
| 未跟踪 `??` | **0** | **0** |
| 修改 `M` | **0** | **0** |
| 删除 `D` | 5：`.claude/commands/opsx/{apply,archive,explore,propose,sync}.md`（磁盘上没了，HEAD 里有；master 现也无此目录） | 同 5 个 |
| 忽略 `!!` | `.claude/settings.local.json`、各 `__pycache__`／`.pytest_cache`／`*.egg-info`、`1-转型规划/AI运营指挥中心/data/sales_dashboard_data.json`（与主 checkout 同名件 `cmp` **一致**） | `.claude/settings.local.json`、`__pycache__`／`.pytest_cache` |
| **不在 master 里的文件** | **无** | **无** |

**待定夺（D4）**：(a) 修 `commondir`（向两个 admin 目录各写一行 `../..`）使 git 可读，再 `git -C <wt> checkout -- .claude/commands/opsx` 复原 5 个删除，再 `git worktree remove`（不加 `--force`）；(b) 维持原状。两者 HEAD 都是 2026-08-10 的 master 祖先、无任何独有内容，(a) 的代价＝两行文件写入 `.git/`。

## 六、边界自查（派单件 §三）

- 未 `--force` 任何 worktree；未删任何分支；未 ff master；只 push 本棒 5 条分支（4 收割＋本泳道）。
- a34c5e 的「CI／.gitignore 改动」经实测为伪脏，**未落到任何分支**。
- 未立新机制行；未碰 `.51`／企微机器人／定时任务；未改 `工具-待合分支巡检.ps1`。
- 补丁留痕：`reports/wt-harvest-0912/*.patch`（gitignore 的 `reports/`，本机取证用，不入库）。
