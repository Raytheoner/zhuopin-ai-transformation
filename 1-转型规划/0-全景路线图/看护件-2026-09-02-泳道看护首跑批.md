---
status: 在办
title: "看护件 · 2026-09-02 · 泳道看护模式首跑批（tasks 6.1-6.4 验收）"
编号: OP-0902-A
日期: 2026-09-02
触发: Shao Peishen 2026-09-02 答「都按建议」＝ ①(a) 现在就跑首跑验收（`zhuopin-lane-watch` v2.0，`skill_01GsmGMFZdgiXknSA3FKeqch`，当日安装）
对应队列行: §一 #452｜变更包 openspec/changes/lane-watch-mode（§1-§5 已闭合，本件跑 §6）
---

# 看护件 · 泳道看护模式首跑批 · 2026-09-02

> 🔴 **本批是 `lane-watch-mode` 的首跑验收，不是一次普通清池。** 验收对象是**机制**（误停 0／漏停 0／每次停都有可读的「在等什么」落档），不是产出数量。

## 〇 口径

**前置四查（skill「触发与前置」，任一不过即停）——2026-09-02 13:xx 本机实测**

| 项 | 实测 | 判定 |
|---|---|---|
| ⑴ **LAN 探针**（3.5 新增，本包比清池多的一步） | `lan-status` ⇒ `✅ on-LAN（三项齐备）（原始探测）→ effective=on` | ✅ **全部可领活入候选**（`.51` 项除外，走 D1 ⏭️ 转出档） |
| ⑵ `git fsck` 健康 ＋ 与 origin 对齐 | `fsck` 仅 dangling blob（非错误）；`master..origin/master=0`、`origin/master..master=0` | ✅ |
| ⑶ 编辑锁无他人长占 | 无锁 | ✅ |
| ⑷ 上一看护批汇总已出 | **本包首跑，无上一批** | ✅（不适用） |

**D1 四档判据现取**（`工具-泳道看护状态机.py criteria`，**本件不复述判据表**，只记本次取到的档位键名）：
🟢 `queue_writeback`／`doc_edit`／`readonly_evidence`／`unit_regression_tests`／`worktree_local_build`／`openspec_draft`
🟡 `merge_to_master`／`change_criteria`／`openspec_design_review`／`close_others_queue_row`
⏭️ `deploy_51`
🔴 `external_send`／`l2_gate_signoff`／`compliance_redline_change`／`asil_cd_related`

---

## 🔴 一 · 硬边界继承（skill 红线，逐条不得越）

1. **⏭️ 档：`.51` 部署及任何触碰生产服务的动作本包永不执行** —— 自动 `transfer-out` 标注去向、跳过、继续其余不依赖它的项，交 `zhuopin-lan-closeout`。**不进 `OPENER_PARTIAL`。**
2. **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 永不代办** —— 只做到「可发送态」即停。
3. **业务建造与一切对外发送永不自动入批** —— 本批三项均为机制工具类（`0-学习与工具/` 下 CLI 与巡检判据、跟进信元数据链路），不涉业务场景。
4. **不自造 🛑 豁免理由** —— 本批不去任何 🛑（`#447` 的 🛑 已于本日由 Shao Peishen 答 ①(a) 逐条核后摘除，行内有留痕）。
5. **`.51`／真实库／规划文档／他线在办不碰。**
6. **看护件落档失败即全停** —— 本件即落档产物。
7. **「已部署 `.51`」类报告必假、当场停。**

---

## 🔴 二 · 选件与波次

### 入选（3 项）

| 泳道 | 行 | 触碰区（零重叠已两两核过） | 预期 D1 档位 |
|---|---|---|---|
| **A** | `#456` 队列结构 lint 扩「队列不得复述 openspec 包进度」告警 | `工具-队列结构lint.py` ＋ 其单测 | 🟢 `openspec_draft`（行内明写「下一步＝起草 proposal，不直接开工」） |
| **B** | `#447` 跟进信元数据失血 | `README-跟进机制与命名约定.md`（起草纪律段＋状态列日期语义）／`push_followup_letter.py` 等转态链路 | **大概率 🟡 `change_criteria`**（改的是口径/纪律段） |
| **C** | `#410` editable 安装指向巡检判据 | `工具-落库sweep.py` ＋ 其单测；只读 site-packages、不改任何安装 | 🟢 起草／**可能 🟡 `change_criteria`**（新增巡检判据） |

### 🔴 实选 3 而非 6（上限 6）—— 理由须留痕

**D1 原则是「宁漏勿滥」，上限是允许值不是目标值**（同 `看护件-2026-08-31-offLAN批.md` §二既例原文「本批不为凑满上限而选」）。
其余候选逐条排除理由：`#240`（触碰 `.51` 的 `.env` ＋ 需外部人 ⇒ ⏭️/🔴）／`#358`（触碰 `.51` 现网四服务落盘路径 ⇒ ⏭️）／`#381`（`[A:已派出]` 他线在办）／`#425`（git 引用高风险，且 master 现已对齐）／`#436`（触碰 ops worktree 与部署 SOP）／`#443`（行内自陈「与所有写队列的 session 全重叠，不可并行」）／`#446`（硬依赖 `#382`⑴ 验活）／`#452`（本批自身的验收对象）／`#382`·`#448`·`#455`·`#341`·`#454`（**触碰区与本批或彼此重叠，留待下一波**）。

### 波次

**单波（波 1）**：A／B／C 三泳道并行，触碰区两两零重叠（队列 lint 文件 ／ 跟进信 README 与转态链路 ／ sweep 与其单测）。
**锁工具族恒最后一波**：`#455`／`#341`／`#454`／`#448` 均触碰 `工具-共享文档编辑锁.py`，**本批全部不入**，留待首跑三判据过后的下一批。

---

## 三 · 泳道 opener 正文（原样传给无头 CC）

### A1 · 泳道 A · 队列 #456 起草 proposal

粘贴端：CC ｜ 泳道：A-456-lint

```
[OP-0902-A]【CC】泳道看护首跑批 · 泳道 A · 队列 #456 起草 proposal
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建一个 ｜ 分支：新建 claude/lint-queue-restate-pkg-0902 ｜ 工作区：不动主工作区 ｜ 派出线：Cowork 泳道看护首跑批（OP-0902-A）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A-泳道A-456。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行。
心跳：reports/lane-heartbeat/OP-0902-A-laneA.md —— 开工 1 分钟内写首行「HH:mm:ss ｜ 已开工」，每里程碑追加一行，收工写「DONE ｜ 产出落点」。🔴 预计等待超 10 分钟的步骤（如全量回归），等待期内须再写一行「仍在等 X，预计还要 N 分钟」。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-09-02-泳道看护首跑批.md 全文（本件即作业书，§一 硬边界逐条守）→ ② 队列 §一 #456 全文（python 0-学习与工具/工具-队列查询.py --row 456，🔴 勿通读队列真身）→ ③ CLAUDE.md 当前进度。

本次一件：为 #456 起草 openspec proposal（不开工实现）。
- 变更包名建议 queue-lint-restate-package-progress；产出 proposal.md ＋ tasks.md ＋ specs delta，跑 openspec validate <包名> --strict 至 valid。
- 🔴 行内已写死一条 design 须先定的事：**怎么机械识别「复述了包进度」**。信状态那条能靠「部门#N ＋ 状态词」的形态匹配，包进度的表述形态更散。**判不出可靠形态就不要硬上**，宁可退成「凡队列行提到某 openspec 包名即提示去读该包 tasks.md」这种低精度零漏报形态——把这条如实写进 proposal 的开放点。
- 🔴 不改任何生产代码，不动 工具-队列结构lint.py 一行（本次只起草）。

D1 档位判据现取（不要照抄任何文档里的表）：
  python 0-学习与工具/工具-泳道看护状态机.py criteria

命中 🟡 或 🔴 档动作时（如需要 ff 合入 master、需要改口径/判据/阈值、需要 design 审、需要关他人在办队列行）：先跑
  python 0-学习与工具/工具-泳道看护状态机.py pause --batch OP-0902-A --wave 1 --lane A-456-lint --action-key <D1键名> --waiting-for "<在等什么>" --option "<候选答案>"
随后以  OPENER_PARTIAL: 停在<🟡|🔴>决策点——<在等什么>  收尾。

命中 ⏭️ 转出档（.51 部署及任何触碰生产服务的动作）：跑
  python 0-学习与工具/工具-泳道看护状态机.py transfer-out --batch OP-0902-A --wave 1 --lane A-456-lint --action-key deploy_51 --note "<需要走 LAN 收口做什么>"
🔴 不进 OPENER_PARTIAL——直接跳过该项、继续其余不依赖它的任务。

判据未覆盖的动作按 🟡 停（fail-safe），并在收尾原因里问「这个动作该进哪一档」。不要自行判断该不该停。

收工：产出与结论写进 openspec 包；🔴 本次不 ff 合入 master（那是 🟡 档，须停下问）。队列回写与 §二 批次登记由 Cowork 侧统一做，你不写队列。
全部做完以  OPENER_DONE  收尾。
```

### A2 · 泳道 B · 队列 #447 跟进信元数据失血

粘贴端：CC ｜ 泳道：B-447-followup-meta

```
[OP-0902-A]【CC】泳道看护首跑批 · 泳道 B · 队列 #447 跟进信元数据失血
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建一个 ｜ 分支：新建 claude/followup-meta-0902 ｜ 工作区：不动主工作区 ｜ 派出线：Cowork 泳道看护首跑批（OP-0902-A）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A-泳道B-447。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行。
心跳：reports/lane-heartbeat/OP-0902-A-laneB.md —— 约定同泳道 A。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-09-02-泳道看护首跑批.md 全文（本件即作业书，§一 硬边界逐条守）→ ② 队列 §一 #447 全文（python 0-学习与工具/工具-队列查询.py --row 447）→ ③ 该行指向的上游件。

本次范围：按 #447 行内自陈推进到「下一个需要判断的点」为止。
🔴 三条硬约束：
- **不发任何跟进信、不改任何一封已发出的信** —— 对外发送是 D1 🔴 档，永不代办。
- **README「发送状态」列一字不动** —— 那是 2026-08-21 §四 #85 答 (b) 确立的信级唯一权威源。
- 凡要改 README 的**口径/纪律段**（起草纪律、状态列日期语义等）＝ D1 🟡 档 change_criteria，**先停下问，不要直接改**。

D1 档位判据现取：
  python 0-学习与工具/工具-泳道看护状态机.py criteria

命中 🟡/🔴：先跑
  python 0-学习与工具/工具-泳道看护状态机.py pause --batch OP-0902-A --wave 1 --lane B-447-followup-meta --action-key <D1键名> --waiting-for "<在等什么>" --option "<候选答案>"
随后以  OPENER_PARTIAL: 停在<🟡|🔴>决策点——<在等什么>  收尾。

命中 ⏭️：跑 transfer-out（参数同泳道 A，--lane B-447-followup-meta），不进 OPENER_PARTIAL，跳过该项继续。
判据未覆盖按 🟡 停并问该进哪一档。

收工：不 ff 合入 master；队列回写由 Cowork 侧统一做。全部做完以  OPENER_DONE  收尾。
```

### A3 · 泳道 C · 队列 #410 editable 安装指向巡检判据

粘贴端：CC ｜ 泳道：C-410-editable

```
[OP-0902-A]【CC】泳道看护首跑批 · 泳道 C · 队列 #410 editable 安装指向巡检判据
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建一个 ｜ 分支：新建 claude/editable-import-guard-0902 ｜ 工作区：不动主工作区 ｜ 派出线：Cowork 泳道看护首跑批（OP-0902-A）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A-泳道C-410。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行。
心跳：reports/lane-heartbeat/OP-0902-A-laneC.md —— 约定同泳道 A。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-09-02-泳道看护首跑批.md 全文（本件即作业书，§一 硬边界逐条守）→ ② 队列 §一 #410 全文（python 0-学习与工具/工具-队列查询.py --row 410）→ ③ 该行指向的 #406 相关结论。

本次范围：按 #410 行内自陈推进到「下一个需要判断的点」为止。
🔴 两条硬约束（行内自陈的触碰区边界，逐字守）：
- **只读 site-packages，不改任何安装** —— 任何 pip install / pip uninstall / editable 重装动作一律不做。
- 改 工具-落库sweep.py 新增巡检判据 ＝ D1 🟡 档 change_criteria，**先停下问**；只写单测与只读取证属 🟢，可自动做完。

D1 档位判据现取：
  python 0-学习与工具/工具-泳道看护状态机.py criteria

命中 🟡/🔴：先跑
  python 0-学习与工具/工具-泳道看护状态机.py pause --batch OP-0902-A --wave 1 --lane C-410-editable --action-key <D1键名> --waiting-for "<在等什么>" --option "<候选答案>"
随后以  OPENER_PARTIAL: 停在<🟡|🔴>决策点——<在等什么>  收尾。

命中 ⏭️：跑 transfer-out（参数同泳道 A，--lane C-410-editable），不进 OPENER_PARTIAL，跳过该项继续。
判据未覆盖按 🟡 停并问该进哪一档。

收工：不 ff 合入 master；队列回写由 Cowork 侧统一做。全部做完以  OPENER_DONE  收尾。
```

---

### A4 · 泳道 B 第二段 · #447 续跑（Shao Peishen 已答 (b)(a)(a)）

粘贴端：CC ｜ 泳道：B-447-followup-meta

```
[OP-0902-A]【CC】泳道看护首跑批 · 泳道 B 第二段 · #447 按已答三条执行
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 复用 claude/followup-meta-0902（第一段已建，commit aaaad34）｜ 工作区：不动主工作区 ｜ 派出线：Cowork 泳道看护首跑批（OP-0902-A）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902A-泳道B2-447。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行。
心跳：reports/lane-heartbeat/OP-0902-A-laneB.md 追加。🔴 时间戳一律用本机 Get-Date 当场重取，不得估算——上一段实测自写时刻比真实时间快了 22 分钟（mtime 15:41 vs 自写 16:03），看门狗读的正是这个值。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-09-02-泳道看护首跑批.md 全文（§一 硬边界逐条守）→ ② 你上一段的取证件 1-转型规划/0-全景路线图/取证件-2026-09-02-跟进信元数据失血-泳道B.md → ③ 队列 §一 #447 全文。

🟢 Shao Peishen 2026-09-02 已答，三条均已授权，按此执行（本段不再为这三条停）：

⑴ 选 (b)：定跟进信 frontmatter 最小 schema，并顺带治「收信人/收件人」分裂。
   不是只加「必写 决策点:」一条。理由（写进产出件）：该字段当初消亡的根因就是无机器守，
   只加一条必写而不定 schema，等于再造一个没人守的字段。
   schema 须配可执行校验（lint 或起草期自检），🔴 但校验代码本身若要动 工具-落库sweep.py
   或编辑锁，属锁工具族、本批不入 —— 落成独立校验脚本或写清落点交下一批。

⑵ 选 (a)：回填那 10 封可复原的真实回件日。
   🔴 必须按 R1 包（coverage-point-ledger）已定的 spec 形态，逐条守：
     - 事实日与补记日「并存」，各占独立字段，不得用补记日顶替事实日；
     - 事实日不可考的标 事实日未知，不得猜；
     - 度量取数时 事实日未知 的样本单独列出、不混入中位数。
   🔴 README「发送状态」列一个字节不许动 —— 那是 2026-08-21 §四 #85 答 (b) 确立的信级唯一权威源，
     本次授权改的是日期字段与 frontmatter schema，不含状态列。
     若你发现不动状态列就完不成回填，立即重新 pause 问，不要自行扩权。

⑶ 选 (a)：并跑期 ≥28 天那条结论，本件只登记、不就地改。
   你的实测（右峰 20-27 天全是 2026-08-23 批量补转态的伪影）要写成可直接被 R1 引用的一节，
   结论转 #439 / R1 重算并跑期。🔴 不要在本泳道改 D4 —— D4 是 R1 包的 Decision，
   改它须走 R1 的 design 审；在本泳道就地改＝借本包之机改另一个包的口径，是 R1 proposal 写死的禁忌。

D1 档位判据现取：python 0-学习与工具/工具-泳道看护状态机.py criteria
再命中新的 🟡/🔴（即上述三条授权范围之外的判断）：先跑
  python 0-学习与工具/工具-泳道看护状态机.py pause --batch OP-0902-A --wave 2 --lane B-447-followup-meta --action-key <D1键名> --waiting-for "<在等什么>" --option "<候选答案>" --no-notify
🔴 加 --no-notify：pause 默认推企微群，而企微群属 D1 🔴 external_send，无头批不得对外真实发送（上一段已实测撞上）。
随后以 OPENER_PARTIAL: 停在<🟡|🔴>决策点——<在等什么> 收尾。

收工：不 ff 合入 master（🟡 档）；队列回写由 Cowork 侧统一做。全部做完以 OPENER_DONE 收尾。
```

## 四 · 首跑验收对照（tasks 6.1-6.4，收工时逐条填）

| 判据 | 目标 | 实测（2026-09-02 16:04，B 第二段仍在跑，标「待终判」者收工后回填） |
|---|---|---|
| `6.1` 上限 6 ＋ 他在场 | 实选 ≤6，他在场 | ✅ **实选 3**（宁漏勿滥，12 条候选排除理由见 §二）；**Shao Peishen 全程在场并实时答了两次决策点** |
| `6.2` 🔴 **至少命中一次 🟡 档并成功续跑**（只跑通 🟢 不算通过） | ≥1 次 | ✅ **命中 2 次**：B `change_criteria`（等 13 分钟 → 答 (b)(a)(a) → `resume` → 第二段 A4 已起）／C `change_criteria`（等 9 分钟 → 答 (a) → `resume` → Cowork 侧收口，`#410` 已销号、盲区已立 `#459`）。**两次均成功续跑。** |
| `6.3` 误停 0 ／ 漏停 0 ／ 每次停都有可读的「在等什么」落档 | 三判据全过方摘灰度 | ⚠️ **须 Shao Peishen 终判。** **D1 判据本身：误停 0** ✅（两次停都是真 🟡 `change_criteria`）／**漏停 0** ✅（A 全程只做 🟢 未停，其两个 🟡 如实留步未代办；B 第二段授权范围外的判断登记为开放点 O1-O4 而非乱停）／**落档可读** ✅（`summary` 现取「本批停 2 次」逐次五项俱全）。🔴 **但看门狗误停 1 次**（第三缺口，见下）⇒ **若「误停」计入非 D1 来源则本条不过；若只计 D1 判据则过。** |
| `6.4` 触碰区防撞零越界 | 0 | ✅ **零越界**：A「主工作区 `git status` 与开工前一致、`工具-队列结构lint.py` 一行未动」／B「README 一个字节未改、12 封已发出的信一封未改、`工具-落库sweep.py` 与编辑锁零改动」／C「零 pip 动作」 |

**波次用时**：波 1 三泳道约 32-50 分钟；波 2 单泳道 **17.4 分钟**（`reports/opener-batch/20260902-155449/summary.txt` 记 `Status=OK`）。

### 首跑撞出的两个真缺口（详见队列 `#452`）

1. **看护件格式契约未写进 SKILL.md** —— 首跑第一次调批处理即报 `✗ 未解析到任何 opener` exit 12。**已修**（SKILL.md 步骤 2，附解析器行号与 `-DryRun` 前置）。
2. 🔴 **心跳时间戳是手写的，5.6 看门狗可被静默架空** —— 两泳道均复现（mtime 与自写差 22／28 分钟）。**未修，属 CC 改码，已登记待派发**；修法＝`check-heartbeat` 改读 mtime。
3. 🔴 **看门狗对已 `DONE` 的泳道误判超时** —— 泳道 B 第二段 16:11:34 正常 `OPENER_DONE`，17:17 例行 `check-heartbeat` 报「心跳超时 66 分钟、已暂停等你」并置回 `paused`。**收工后心跳自然静默，被当成失联**：`check_heartbeat` 只排除了「已在 `paused` 态」的泳道，**没排除「已 DONE」的**。已 `resume` 撤销并写明理由；修法＝增加「泳道已收工即跳过」前置，建议与缺口 2 同批处置（同一文件）。
   ✅ **口头约束当场生效的证据**：把该发现写进 A4 opener 后，B 第二段心跳自己写「时间戳改用本机 `Get-Date` 当场重取（上一段自写快 22 分钟已确认）」，时刻 `15:56:28` 与本机真实时间吻合。**但那是人守，机器守仍缺。**

### 一处如实留步（不补推）

泳道 B 第一段 `pause` 时发现：该命令**默认推企微群 webhook**，而企微群属 D1 🔴 `external_send`，无头批不得对外真实发送 ⇒ 它自行改用 `--no-notify` 落状态、**通知未发**。
**本次不补推** —— 那条通知的内容是「等你答三问」，而他已当场答完，补推即发一条已过期的对外消息。**机制侧缺口（`pause` 默认行为与 🔴 档冲突）已登记 `#452`。**

**收工汇总**（D6 必含列，`summary` 子命令现取、不手工数）：每泳道一行 ＋ 心跳全文 ＋「本批停 N 次｜逐次：泳道／档位／停在什么动作／他答了什么／等了多久」＋「本批转出 N 项」＋「待回内网」清单。
