---
status: 在办
---

# 拆件巡逻报告 · 2026-08-26（定时班 · 第一班）

**识别 3 行 · 已拆 3 行**（#411 采购／#415＋#417 财务同批合并读）｜ 新增 §一 #418（业）、§四 #122／#123 ｜ 报告为定长件，回件原文在 `7-外部文档/`，本报告只写指针与结论。

---

## #411 ｜ 姚祖怡对 `采购部#18` 的回件（docx）

- **内容摘要**：物料看板试用回件——两个自由问答题已答、**三条读数口径判例表一格未勾**、并新提三条功能需求。
- **已执行动作**：#411 销行 `[S:done]`；回件事实与三条新需求按协议〇.10 **并入 §一 #334 子项 ⑺⑻⑼**（同视图、同触碰区，不另取号）；跟进信 README `采购部#18` 转 `📥 已回件并回灌`。
- **判断依据**：`python-docx` 逐格实读，判例表 `✅／❌／✏️` 三列 × 3 行共 **9 格全部为空**（守本章程二.2 看字符不看列名）；问题 1 他选 (c) 指名的 SRM「负责采购」字段，**正是 2026-08-19 实测 8 条路径全 404 的那个** ⇒ (c) 实质回到 (a)；问题 2 工时基线他给 **0.5 小时/物料**（另称脚本全量汇总需 1 人天）。
- **需 SP 项**：§四 **#122**（是否起草 `采购部#19` 补件追三条判例＋摆 404 实测；并请为 ⑺⑻⑼ 定序——其中 ⑻「采购员可编辑答交数据反向喂进保供看板判定链」越出该视图「纯派生零判定」原设计边界，须走 openspec）。

## #415 ＋ #417 ｜ 唐燕萍对 `财务部#14 补件⑶` 的回件（文本＋docx，同批合并读）

- **内容摘要**：⑴ **R5 分母签认到手＝读法甲**；⑵ **新提我方缺陷并自带取证**：随机抽 10 张 AP 单，**4 张面板报「发票缺失」而共享目录里发票确实存在**；⑶ 确认重复入库发票已修正。
- **已执行动作**：#415／#417 双双销行 `[S:done]`；签认结论回灌 **§一 #391**（前置①闭环，②可起草）；缺陷立 **§一 #418**（业，附四组 AP 单号／发票号／所在导出文件）；README 补件⑶ 行转 `📥`。
- **判断依据**：docx 表 4「读法甲（我们建议）」行的「你的选择」格实为 `√`、乙格为空；表 1 给全四组对照，其中**两组的发票落在 4 月那份导出文件里而 AP 单是 8 月的** ⇒ 最像根因的线索＝发票摄取可能只覆盖与 AP 同期的导出文件（**是线索、未证实**）。
- **需 SP 项**：§四 **#123**（#418 定级；建议 (a) 插队并与 §一 #390 同车——同触碰 `fi2/feed_source.py`、一次部署）。

---

## 本班两条最该被记住的话

1. **「回件已到」不等于「口径已确认」。** `采购部#18` 三件事回了两件，第三件（判例勾选）整表空着，而机器侧一切正常：信发出了、回件到了、自动归档了、README 自动标了「回件已到」。**只有逐格读 docx 才看得出那 9 格是空的**——目测一眼「他回信了」就转闭环，三条口径会带着「已确认」的外观继续在生产上跑。
2. **她刚被请去「恢复采信面板」，第一轮试用就撞上第二类错数。** 更值得记的是这条缺陷的形状：「无发票支撑（发票还没到）」是账上天天真实发生的结论，所以 883 项里混着假的**不会引起任何人怀疑**——与 §一 #391「门禁静静地什么也不做」同族，**错误不产生任何信号**。⇒ #418 查清前，883 这个底数不得当既定事实去推导容差线。

---

## 待发信盘点（一.3，只读只提醒）

- **主表 54 行 ＋ 补件登记表 3 行，逐行解析发送状态列**：`⏳ 待你审` **0**／`🆕 待发` **0**／`⏸ 暂缓` **0**。
- **交叉红标**：**无**。README 无任何 `🆕 待发` 行 ⇒ 不存在「队列称暂缓、README 仍待发」的误发风险面。
- ⚠️ 直接 `grep -o` 数三态会得到 10／14／12 的假计数——那些全部落在 README 自身的说明正文里，不在表格列内。**必须按表头定位列序解析**。
- **串行闸现状（供下一棒参考，判闸仍以 `工具-跟进闸查询.py` / README 为准）**：姚祖怡与唐燕萍两侧本班转态后均已**开闸**。

---

## 巡逻自检

- **本班为非交互班次**，一.4 起草缺口检测／三.8 写后反查／四.5 触发 sweep／四.6 决策提醒 **按〇bis 跳过**。
- 🔴 **一.0 的 `--digest` 入口在当前工具版本上不存在**（`工具-队列查询.py` 只有 `--row/--section/--field/--file`，传 `--digest` 直接 argparse 报错退 2）。按章程「报错则退回逐条读法」执行：**自建只读 digest**——按 `## 一、`／`## 二、` 切段、逐行取「编号＋`[S:…]`＋任务列前 60 字」，两份真身共 77 行 §一 一次扫完，命中 3 行后才 `--row … --field all` 拉全文，未命中的一行未展开。**建议把这一缺口登记为章程/工具二者对不齐的一处漂移**（本班不自行改章程，守 §五）。
- **三.6 比对结果**：写回前重扫两份真身 —— 机制环境 51 行、业务场景 25 行，**除我自己写的 #418 外零新增、零消失**，无并发写入需并入；高水位线 §一 #418／§四 #123 与实拿编号一致。
- **本轮实拿编号**：§一 **#418**（`--domain 业`）；§四 **#122／#123**。
- **并入审核结论（协议〇.10）**：采购三条新需求 **已并入 #334**（同视图同触碰区，不取号）；财务新缺陷 **过 §一 全部 open 行后无可并入** —— 最近的两行 #390（`feed_source.py` 校验层／无 PO 关联 fail-loud）与 #391（R5 容差线）是**同文件不同缺陷**，并进去会把一条专员自带取证的独立缺陷埋在别人的行里；已在 #418 触碰区显式写明与 #390／#372 重叠、须同车或串行。
- **日期基准**：本班无本机 PowerShell（非交互），日期取会话环境给定的 **2026-08-26**，并与沙箱 `date`（CST, UTC+8）交叉核对一致；未跨午夜。
- **锁**：§一/§四 写入与 README 写入分两次 acquire／release，各自 `--who Cowork-拆件巡逻`，写入均走 `edit-row/append-row --stdin-json`（正文不进 argv，守 #414）。
- 🔴 **release 被拒两次，两次都值得记（都是「写了 ≠ 被认作写了」）**：
  **⑴ §二 文件清单不加反引号 ＝ 等于没写。** 校验⑹ 的覆盖判定是 `re.findall(r"`([^`]+)`", 文件清单)` —— **只认反引号里的路径**。本班首版清单用全角分号分隔、四条路径逐字完整、肉眼完全正确，解析结果却是**零个片段**，于是连我自己刚改的四个文件也被判成「无归属脏文件」。⇒ **章程四.3「路径写仓库根相对完整路径」是必要条件、不是充分条件**，建议下一版章程把「每条路径必须包在反引号里」写进那三条禁令旁边（本班不自行改章程，守 §五）。
  **⑵ 状态格里出现一个用于叙述的 `✅`，被判成头尾不一致。** 我在豁免理由里写「批次 B-0826_10 已 ✅ 落库」——那是描述**别人**的批次，却被 #248 判据读成本行自称完成。⇒ **状态格内不得出现 `✅` 字符，哪怕它在讲另一件事**；已改写为「已落库完成」。
- **登记豁免（一次一用，已写在本班 §二 批次行状态格内）**：`0-学习与工具/工具-迁移到新路径.ps1`、`1-转型规划/0-全景路线图/派单件-【CC】队列写入根治与回件链路六缺陷-414与416-2026-08-26.md` 两件**非本班产出、本班一字未动**，不知其提交意图，**代人登记就是替别人猜** ⇒ 只豁免不代登；两件仍在落库 sweep 第 4 类「未声明脏文件」常驻告警的点名范围内，不会静默掉地上。
