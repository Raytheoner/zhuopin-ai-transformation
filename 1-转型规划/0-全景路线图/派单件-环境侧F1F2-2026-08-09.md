---
title: 派单件 · 环境侧 F1／F2（队列 #285＋#306 同车 ／ #311）
created: 2026-08-09
执行方: F1＝CC（独立 worktree） ／ F2＝Cowork（无 worktree）
派单方: Cowork 环境总线
status: 待执行
---

# 派单件 · 环境侧 F1／F2（2026-08-09 环境总线盘点后按序 Open）

> **用法**：直接复制对应代码块开新 session。**两份可同时开**，判据见文末矩阵。
> **重发时必须连标题行一起发**（`专线opener模板库.md` §〇.0）。
> **本件只是取用副本**——队列行才是权威，开工一律以 `工具-队列查询.py --row N` 现读为准（§〇.4）。

---

## F1 ·【CC】因果断言门槛机制化 ＋ 队列解析断根收尾（#285 ＋ #306 同车）

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑（assertion-gate-and-parser-consolidation，新 worktree，收工自删） ｜ session：新开
读 CLAUDE.md + 跨桌任务队列 #285 与 #306，两行同车认领（两者都碰 工具-共享文档编辑锁.py 与解析器一族，分开做必然撞车）。#285 按已拍板 (a)：收窄适用面（只约束驱动 P0/P1 定级的因果断言）＋ 挂编辑锁 release 校验④，同批把 opener 模板库 §六防线4 正文降为一行指针（依 #206 先例，降指针不等于删、保留可 grep 痕迹）。#306 按缩范围后的口径：#308 状态机器字段已 apply 落地，本行原四件事里 ③开头片段提取已作废、①列语义大幅萎缩，仅剩 ②转义 与 ④列数校验 两件，合并进 5-平台底座/zhuopin_platform/shared_tools/，并把 #307 一并做掉不再单独占行。走 openspec（涉新 design 停下等 Shao Peishen 审），完工即归档、收工登记 §二 批次并触发 sweep。
```

**为什么同车而不是两个 session**：实测两行触碰区**都含 `工具-共享文档编辑锁.py` 与解析器一族**，并行必然冲突；而 #306 缩范围后只剩两件小事，与 #285 合成一个 session 的量刚好。
**#306 的硬阻塞已解除**：其前提是「#308 状态机器字段落地」，#308 apply 已于 2026-08-09 完成并推 master（`efa522f`）。
🔴 **#285 的成因值得开工前读一遍**（他否 (c) 的原话）：「维持现状等于让规则退休制**在它刚立起来的当周就自我失效一次**，此后所有『违反 3 次即机制化』都会跟着松。」

---

## F2 ·【Cowork】CLAUDE.md §5 正文瘦身 A/C/D/E 类 13 条（#311）

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ ｜ 工作区：无（只改 .md，不写生产代码、不自行 commit，收工只登记 §二 批次） ｜ session：新开
读 CLAUDE.md + 跨桌任务队列 #311 + 1-转型规划/0-全景路线图/规则体检-CLAUDE.md-§5-29条判定表-2026-08-05.md §二，认领 #311：按判定表逐条执行 A/C/D/E 四类共 13 条正文瘦身。🔴 三条硬约束：⑴ 严格照判定表逐条做，不得凭"可减 4200 字"这一总量目标反推删哪里；⑵ 改 CLAUDE.md 全程占 python 0-学习与工具/工具-共享文档编辑锁.py --file CLAUDE.md acquire（该文件 2026-08-09 才首次纳入锁保护，acquire 会报一次 #200 绕锁告警属预期）；⑶ 每条改完做写后反查（字节变化＋关键词命中＋被降指针的规则名仍可 grep 到）。C 类须在判定表自带的正反两方案间给出选择并写明理由，D 类须一并决定「取证方法知识库」建在哪、且 D3 必须在 §5 保留一行硬提示。收工把判定表 status 由「待发」改「已执行归档」并回填逐条结果，登记 §二 批次并触发 sweep。
```

**这是「记忆偏差」整治的第二步**，第一步（迁移 −40%／B1／协议〇.8 加一问）已于 2026-08-09 完成。
🔴 **为什么建议专开一个 session、预算给足**：13 条逐条执行 ＋ 逐条反查，而 §5 是全项目最权威的纪律文件——**改错的代价是静默损失约束力，无人会发现**。

---

## 次序与并行矩阵（并行判据＝触碰区是否重叠，2026-08-09 逐行实测队列触碰区列）

| | 能否立即开工 | 可与谁并行 | 判据（实测触碰区） |
|---|---|---|---|
| **F1 #285＋#306** | ✅ 零依赖（#306 的硬阻塞已随 #308 apply 解除） | 与 F2 并行 | 碰 `工具-共享文档编辑锁.py`／`zhuopin_platform/shared_tools/`／`专线opener模板库.md` §六 |
| **F2 #311** | ✅ 零依赖 | 与 F1 并行 | 碰 `CLAUDE.md`／判定表／`进度编年-CHANGELOG.md`／**环境总线 opener**（与 F1 碰的 `专线opener模板库.md` 是**不同文件**，勿混） |

⚠️ **两者都会在收工时写 `跨桌任务队列.md`**——由协议〇.7 编辑锁串行化，**不构成并行冲突判据**。

## 本轮刻意不开的（附理由，免下次重问）

| 行 | 为何不开 |
|---|---|
| **#308 收尾** | 变更包 archive 的剩余条件是**真实生产场景端到端观察**（真实一次 WIP 超限提示／真实一次串行拦截或豁免／真实一类告警完整「出现→解除」周期）——**须自然发生，不可人为构造**，故不派活；另含 F2 首次全量运行命中 13 行的待处理项（已登记在其 `tasks.md` 8.6） |
| **#98 月度环境体检** | **定时触发型**，首期体检 2026-08-28（每月最后一个周五），此前不得提前 |
| **#279／#270** | **依赖外部方**——等 IT 陈承提供采购部真实 chatid，等人不等我们 |
| **#22／#96／#68／#254** | `partial`，剩余范围需先核实边界；作**下一梯队**，等 F1／F2 有收口再放 |
| **#122／#138／#170／#220／#282／#284** | `hold`（挂起／搁置／永久关闭），不在可推进池内 |
