---
title: "看护件 · B-0913_人守打通（构建闭环人守环节，两泳道）"
created: 2026-09-13
批次: B-0913_人守打通
派出线: Cowork 环境总线 OP-0913-E
status: 待他一字母授权起跑（`-FullAuto -Yes` 按批授权、不做常设）
---

# 看护件 · B-0913_人守打通

> 🔴 **默认无头起、不粘给他**（`opener骨架.md`「一次粘贴」节）：Cowork 侧一条命令即可，见 §四；§三bis 只在退回「人在环」时才用。
> 🔴 **起跑不传 `-LogDir`**——批目录必须落成**纯时间戳名**，否则本批自己就掉进 `#571` 的盲区（语义名批次探针一个都看不见，09-08 起 0/3）。**这是零改码的当场规避，不是修复；修复正是 A1 的活。**

## 一、本批是什么

聚焦**构建闭环 workflow 的人守环节**：两条泳道各补一处「机器有能力做、却仍由人做」的断口。**两条触碰区不相交，可并行。**

| 泳道 | 承接行 | 断口一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#571` | 收工探针的目录名正则只认纯数字 ⇒ **语义名批次整批不进视野**；`summary.txt` 里写着的 `PARTIAL` 没有任何消费者 | `工具-无头棒收工探针.py` ＋其单测 |
| A2 | `#570` | 执行体对齐只算 git 落后数、**不看进程起于何时** ⇒ 代码对齐了就报绿，而跑着的还是旧码 | `工具-执行体对齐重启.ps1` ＋其单测 |

**今日两处实证**：⑴ `_batch_started_at('20260913-收口三泳道')` 现取 `None`，该批 `summary.txt` 04:14:33 已写好、首行就是 `op0913a … PARTIAL`，而 04:16–04:18 的定时巡检回显 `[NO-SIGNAL]`；⑵ listener 起于 `13:10:20Z`，`#556` 节流修复落 master 在 `13:11:10Z`，**晚 50 秒**，`工具-执行体对齐重启.ps1` 照报「已对齐、无需处置」。

## 二、硬边界（两条泳道同受）

- 🔴 **只 push 本泳道分支，不 ff master**——ff 属 🟡，由派出线经 `reports/pending-ff.jsonl` 等他一字母、机器合。
- 🔴 **A2 不得碰 `0-学习与工具/工具-落库sweep.py` 一个字节**：`#556`② 的抢救分支 `claude/op0913a-556-sweep-visibility`（`751495e`，未 ff）在该文件上有 **+194 行**未合改动，同批动它必撞。`#570` 里 sweep 侧那半（ff 触碰常驻执行体代码路径即自动 `-RestartOnly`）留到该分支 ff 之后另派。
- 🔴 **不碰 `.51`／企微机器人／定时任务运行时**（⏭️ 档）。A2 只改判据与单测，**本棒不真重启任何常驻服务**；期望产出末句「什么条件下允许自动重启、什么条件下只告警」必须写进交付，不得一刀切。
- 🔴 零回归按**差集口径**报数（分支 vs 纯 master detached worktree 同命令对跑，`comm` 双向），**不跑全量假装零回归**。
- 🔴 长回归**不要丢进后台等事件**：`op0913a` 昨夜正是卡在「等差集回归事件」被 600 s 后台上限掐死、活儿以未提交状态悬在 worktree。跑完即在本泳道分支 commit，**先保命再收工**。

## 三、泳道

### A1 · §一 #571 收工探针批目录盲区 ＋ PARTIAL 升一等状态

▶ 粘贴端：CC ｜ 泳道：op0913f-probe-batchdir-571

```
[OP-0913-F]【CC】探针批目录盲区
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913f-probe-batchdir-571`） ｜ worktree：☑（op0913f-probe-batchdir-571，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_人守打通
读 ① `队列 §一 #571（`python 0-学习与工具/工具-队列查询.py --row 571 --section 一 --field all`，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913f-probe-batchdir-571 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913f-probe-batchdir-571 --done --batch B-0913_人守打通 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · §一 #570 执行体对齐补进程新鲜度判据（本棒只改 .ps1）

▶ 粘贴端：CC ｜ 泳道：op0913g-align-proc-freshness-570

```
[OP-0913-G]【CC】对齐补进程新鲜度
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913g-align-proc-freshness-570`） ｜ worktree：☑（op0913g-align-proc-freshness-570，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_人守打通
读 ① `队列 §一 #570（`python 0-学习与工具/工具-队列查询.py --row 570 --section 一 --field all`，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913g-align-proc-freshness-570 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913g-align-proc-freshness-570 --done --batch B-0913_人守打通 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词（只在退回「人在环」时才粘；默认走 §四 无头）

```
[OP-0913-H]【CC】看护看护人守打通
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_人守打通
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0913H-看护看护人守打通。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】人守打通两泳道-2026-09-13.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 五、ff 预授权（Shao Peishen 2026-09-13 07:5x 当轮口头「预授权合」，本节即其落档）

🔴 **授权的是「满足下列全部条件时替他合」，不是「合任何跑出来的东西」**——预授权的唯一风险是合进没人看过的改动，故条件必须机器可核、逐条留痕。**任一条不满足 ⇒ 不合，退回等他明确一字母。**

| # | 条件 | 核法（手段写死，不凭自陈） |
|---|---|---|
| ⑴ | 收工哨兵 ＝ `OPENER_DONE` | 读 `summary.txt` 的 `Status` 列；`PARTIAL`／`NO-SENTINEL` 一律不合 |
| ⑵ | 零回归按**差集口径**证到 | 收工报告须给出「分支 vs 纯 master detached worktree 同命令」的两侧数字与 `comm` 双向结果；**只给「全量通过」不算** |
| ⑶ | **触碰区未越界** | `git diff --name-only master..<分支>`：A1 只许 `工具-无头棒收工探针.py` ＋其单测；A2 只许 `工具-执行体对齐重启.ps1` ＋其单测 |
| ⑷ | 🔴 **A2 未碰 `0-学习与工具/工具-落库sweep.py` 一个字节** | 同 ⑶ 的 `--name-only` 输出里不得出现该文件——碰了即与 `#556`② 未合分支 `751495e` 的 +194 行撞车 |
| ⑸ | 纯 ff、零冲突 | `git merge-base --is-ancestor` ＋ `rev-list --count master..<分支>` 的 ahead＝0 ＋ `merge-tree --write-tree` 零冲突 |
| ⑹ | 未触碰生产 | 分支内无 `.51`／企微机器人／定时任务运行时改动（⏭️ 档） |

**执行路径**：条件全过 ⇒ 往 `reports/pending-ff.jsonl` 各写一行（`branch` ＋ `authorized_text`，后者写明「Shao Peishen 2026-09-13 预授权合」＋六条核验结果），由 15 分钟待合分支巡检机器合。

🔴 **不得预先写授权行**：写早了，巡检会在回归结论出来之前就把分支合掉——**那等于把预授权变成无条件授权**。授权行只在条件核完之后写。
## 六、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】人守打通两泳道-2026-09-13.md" -FullAuto -Yes -Detach
```

🔴 **必须 `pwsh`、不得 `powershell`**（本机后者＝PS 5.1，`-Detach` 会把它传给子进程而脚本用了 PS7 专有参数 ⇒ 零泳道且父进程照报成功；`#567`，本批 07:40 实撞、07:42 改 `pwsh` 即通）。🔴 **不传 `-LogDir`**（见开篇）。退出码看 `exit.txt`、`summary.txt` 末行 `EXIT=`、`launcher.json` 记 pid——**轮询这三样，不猜**。
