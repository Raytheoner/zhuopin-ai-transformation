---
status: 待发
title: "开场prompt ·【CC】FI2 发票源改道建造：税务导出 Excel 接入（队列 #295）"
created: 2026-08-07
执行环境: CC
来源: 唐燕萍 2026-08-06 回件（财务部#11），2026-08-07 拆件巡逻拆件后派生
---

# 【CC】FI2 发票源改道建造：税务导出 Excel 接入 —— 队列 #295

## 【开场词（复制即用）】

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑（一任务一 worktree） ｜ 工作区：触碰 .51:8094 已部署服务（Fi2WebServer），须 sync-to-server.ps1 推送 + 重启 + 冒烟三件套；另需读 .51 服务器 D:\airead 目录（税务导出原件落盘处）
读 1-转型规划/0-全景路线图/开场prompt-【CC】FI2税务导出Excel接入-2026-08-07.md 全文，＋ 根 CLAUDE.md ＋ 4-数字员工/财务部/FI2-三单匹配自动对账/CLAUDE.md 恢复上下文，按队列 #295 开工。
```

---

## 一、这次的特殊之处：**没有剩余开放口径点**

唐燕萍 08-06 回件把**三个决策点全部定死**，且**真实税务导出原件已放进共享文件夹**。

> **这是 #249 局部定稿点名的那把「钥匙」——一件到手可同时确认三件事**：导出的实际列清单／发票号能否直接与 U9C 应付单 `InvoiceNo` 对上／货物名称列规不规范。

所以本任务**不需要再问她任何口径问题**，直接建造。

---

## 二、她定死的五件（原话为准，不得改写）

| # | 事项 | 她的决定 |
|---|---|---|
| ① | 固定文件夹 | `.51`（`192.168.100.51`）服务器 **`D:\airead`**（决策点 1 选 (a)） |
| ② | 每日导出负责人／时点 | **李姣龙**，**工作日上午 10 点前**导完 ⇒ **取件作业须排其后** |
| ③ | 「新增文件」判据 | **我方自记「已处理清单」**（决策点 2 选 (a)）——**幂等可重跑，不看文件名、也不看 mtime** |
| ④ | 完整性验收口径 | 漏票／跨期／红冲作废三维 **先都不设**，等真实数据跑一轮再定（决策点 3 选 (c)） |
| ⑤ | 真实导出原件 | **已放进共享文件夹** |

### 🔴 ④ 是唯一一处她与我方推荐不同的地方，务必看清

我方推荐的是 (a)（设完整性校验），**她选了 (c)（先都不设）**。

- **按她的选，建造时不得自行加回。**
- 已如实登记：**漏票／跨期／红冲作废三类风险当前无自动校验兜底——这是她知情下的选择，不是遗漏。**
- 你若在建造中认为必须加，**走 §四 报 Shao Peishen，不要"顺手加上"**。

---

## 三、🔴 开工第一步：先验发票号字面是否严格一致

**在写任何摄取代码之前**，先用她做 65.8% 比对的那 8 张发票逐个比一眼：

- 位数 ／ 前导零 ／ 空格 ／ 全半角 ／ 有无发票代码前缀

**为什么这是第一步而不是某一步**：发票号对不上则 join **全盘落空且不报错**——属「返回值正常、结论是反的」故障族（根 CLAUDE.md §5「工具静默回退」同族）。#249 已点名该风险。**先验一眼的成本是十分钟，不验的代价是整条链路建完才发现全空。**

---

## 四、范围红线

1. **三单核对逻辑与面板发票段结构一个字不改**（她原话：「结构和逻辑不变」）。
2. `feed_source.py` 既有 `invoice_sample_dir` 通道**可平滑承接** —— 换目录 ＋ CSV 解析换 Excel 解析即可；`parse_invoice` 边界校验与 `partition_invoices` 孤立发票切分**原样复用**，不重写。
3. **判定口径不得动** —— 全量回归须零漂移。

---

## 五、最小可行拆分（#249 已给，本行不自行顺延排期）

- **第 1 层**：能读固定目录里的 Excel —— **即足以支撑 8 月底评估**。
- **第 2 层**：定时扫描 ＋ 已处理清单 ＋ 失败告警 —— **可后置**。

⚠️ **拆不拆、第 2 层排哪，由业务总线定**（见 §七 Q1）。本行不自行顺延排期。

---

## 六、验收清单（缺一不算完工）

- [ ] **真实导出原件列清单核验结论**（含发票号 join 可行性先验，用那 8 张逐个比）
- [ ] 税务导出接入实现（Excel 解析走既有 `invoice_sample_dir` 通道）
- [ ] **已处理清单幂等机制**（重跑不重复处理；不看文件名、不看 mtime）
- [ ] **全量回归零漂移**（判定口径不得动）
- [ ] `.51:8094` 部署冒烟（`/api/ping` ／ 关键页 200 ／ 一次全量重算）
- [ ] 起草跟进信（README 只写 `⏳ 待你审`）

---

## 六bis、🔴 开工前必须先知道的一件事：**FI2 在 spec 层目前不存在**

**2026-08-07 实测（附证伪命令，别信这段话本身，自己跑一遍）：**

| 事实 | 证伪命令 | 实测 |
|---|---|---|
| FI2 在 `openspec/specs/` **零覆盖** | `ls openspec/specs/ \| grep -i "fi2\|invoice"` | **零命中** |
| 但它的 spec 其实早写好了——**5 个** capability delta | `find openspec/changes/fi2-recon-mvp -name spec.md` | `fi2-feed-source`／`fi2-match-engine`／`fi2-price-check`／`fi2-recon-report`／`fi2-result-classify` |
| 进不去的唯一原因：**`fi2-recon-mvp` 包没归档** | `git log -1 --date=short -- openspec/changes/fi2-recon-mvp` | 最后改动 **2026-08-04**，仍在 `changes/` |
| 而这个包**已完成 116 项、只剩 13 项** | `grep -c '^- \[x\]' tasks.md` | **116 ／ 13** |

**⇒ 你跑 `/opsx:propose` 时，要 delta 的那个基线是空的。**
**⇒ 而你本次要动的 `feed_source.py`，正好就是躺在那个包里的 `fi2-feed-source` capability 的范围。**

这不是让你顺手去归档别人的包（那需要判断剩下 13 项是不是真该完成，不是你本任务的范围）。这是让你**在 propose 之前先做一个决定**，见下方 Q0。

> 机制侧的根治已登记为队列 **#298**（sweep 增 spec 覆盖缺口检测），与本任务并行、不阻塞你。

---

## 七、开工前的澄清问题（B 类跨线交接，**必问**；已预置为选择题）

**Q0 · FI2 无 spec 基线，怎么处理？（🔴 最优先，其余问题都在它之后）**
- (a) **本次 propose 只针对「发票源改道」新建一个独立 capability（推荐）** —— 不碰 `fi2-recon-mvp` 那个包；代价＝FI2 的 spec 层暂时仍是残缺的（只有新的这一个），但你的改动本身可追溯。
- (b) 先把 `fi2-recon-mvp` 归档（要先判断剩余 13 项）再 propose —— 代价＝把一件别人的收尾工作扛进本任务，剩余 13 项若涉真实验证会阻塞你开工。
- (c) 不 propose，直接改代码 —— **不建议**，那正是本次要根治的可追溯断链。
- **默认：(a)**，并在交付里如实写明「FI2 spec 层仍残缺，根因＝`fi2-recon-mvp` 未归档，已登记队列 #298／#196 同族」。

**Q1 · 本次做到第几层？**
- (a) **只做第 1 层（推荐）** —— 能读固定目录里的 Excel，足以支撑 8 月底评估；代价＝仍需人工触发，第 2 层另排。
- (b) 第 1+2 层一次做完 —— 一步到位；代价＝工作量约翻倍，而第 2 层的失败告警口径尚未定，可能返工。
- **默认：(a)**

**Q2 · `D:\airead` 里此刻若还没有文件（李姣龙尚未开始每日导出）怎么办？**
- (a) **用她已放进共享文件夹的那份原件先建（推荐）** —— 她 08-06 已明确"已放入"；先拿它把列清单与 join 先验做掉。
- (b) 等 `D:\airead` 有真实每日文件再开工 —— 代价＝可能空等，而先验本来就不依赖"每日"。
- **默认：(a)**

**Q3 · 第 1 层的触发方式？**
- (a) **手动/按需触发（推荐）** —— 与"第 2 层才做定时扫描"一致，不提前引入调度。
- (b) 先挂个简易定时 —— 会与第 2 层设计冲突，且 ② 的"10 点前导完"约束尚未在调度里体现。
- **默认：(a)**

> 三问若读完 opener 已无歧义，可 0 问直接干。

---

## 八、部署段（工作区字段展开）

- `sync-to-server.ps1` 推送 → 计划任务 **`Fi2WebServer`**（端口 **8094**）
- 冒烟三件套：`/api/ping` ／ 关键页 200 ／ 一次全量重算
- ⚠️ 部署后**核验新进程 `CreationDate` 真的刷新**（SC8 2026-07-14 教训同族：脚本打印"已重启"不等于真重启）
- 回滚 SOP 在位
- **另需 `.51` 服务器 `D:\airead` 的读取权限** —— 若读不到，先确认是权限还是路径，不要绕道用本地拷贝当"真实验证"

---

## 九、跟进信（§5 第 8 步，别漏）

- **串行原则** ✅ **已满足** —— 唐燕萍最近一封（财务部#11）的回件 §一 #291 已于 2026-08-07 由拆件巡逻回灌销行，且**四件全部有明确结论、无一项悬空**。
- **发送硬前置** —— 已 ff 合入 master ＋ `.51:8094` 已部署冒烟通过 ＋ 用真实案例端到端复现，三条全过才可发。
- **README 只写 `⏳ 待你审`**；`🆕 待发` 只能由 `approve_followup_letter.py` 写入。
- 信里建议**如实带上 ④ 的风险留痕**（三类完整性风险当前无自动兜底，是她的选择）——不是提醒她改主意，是让这个选择在书面上有据可查。

---

## 十、收工段

1. 全量回归，逐项贴真实数字
2. `commit + push`；**⚠️ 你的建造 commit 不走 §二 批次，协议〇.8 的「已复检」声明扫不到你** —— 若改了任何判据/参数，请在场景 CLAUDE.md 与队列行内主动写一句「本次变更参数：X（旧→新）；已复检可能受影响：…」（队列 #236 已登记该缺口）
3. 更新场景 CLAUDE.md「部署状态」段
4. 回写队列 #295（**✅ 写在状态列开头**）；#249／#252 若因此可推进，一并回写
5. 起草跟进信 + 提交 Shao Peishen 审
6. **收工重跑文档台账**
