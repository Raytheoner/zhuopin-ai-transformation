---
title: 派单件 · 环境侧 E1／E2／E3（队列 #305／#308／#267）
created: 2026-08-08
执行方: CC（三个独立 worktree，可全并行）
派单方: Cowork 环境总线
status: 待执行
---

# 派单件 ·【CC】环境侧 E1／E2／E3（2026-08-08 环境总线，Shao Peishen 当日选 (a)）

> **用法**：直接复制对应代码块开新 CC session。**三份可同时开**，判据见文末矩阵。
> **重发时必须连标题行一起发**（协议／`专线opener模板库.md` §〇.0：opener 会被多次重发，每次重发都是一次身份丢失的机会）。
> **本件只是取用副本**——队列行才是权威，开工一律以 `工具-队列查询.py --row N` 现读为准（§〇.4）。

---

## E1 ·【CC】队列写侧注入口止血（P1，零依赖）——队列 #305 ＋ §四 #57

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑（queue-appender-guard，新 worktree，收工自删）
读 CLAUDE.md + 跨桌任务队列 #305 + §四 #57，认领 #305：机器人自动追行裸拼 Markdown 行、无门禁且外部可控，按行内取证给写侧加校验（触碰区仅 5-平台底座/wecom-aibot-service/aibot_service/queue_appender.py）。收工前顺带做掉 §四 #57：gh api -X PUT /repos/Raytheoner/zhuopin-ai-transformation/vulnerability-alerts，返回 204 即成，权限不足就回报改由 Shao Peishen 在 GitHub UI 点开。走 openspec（涉新 design 停下等 Shao Peishen 审），完工即归档、部署留痕、收工登记 §二 批次并触发 sweep。
```

**为什么它排第一**：#305 是**外部可控的注入口**（企微来件文本直接拼进队列行），属止血项；且触碰面最小（单文件），最不容易与其它两份撞。
**顺带项 §四 #57**：开启 Dependabot security alerts，是「GitHub 功能正位」plan 的最后一项（第二批八项中唯一取的一项，其余七项不取的理由已在 `构建环境基建方案-CI基线与业务环境双队列-2026-08-08.md` §2.1 逐条写死）。

---

## E2 ·【CC】队列状态机器字段 ＋ 权威解析器（P1，须 propose＋design 审）——队列 #308

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑（queue-machine-field，新 worktree，收工自删）
读 CLAUDE.md + 跨桌任务队列 #308 + 3-治理与合规/构建环境基建方案-CI基线与业务环境双队列-2026-08-08.md §4.4／§4.5（§4.5.1 那段可原样复制进 design.md），认领 #308：状态列加机器可读字段，消掉「用正则猜中文」这一整族判据。🔴 子项 A-G 逐条看全，其中 F（编辑锁 release 补两项校验：§二新增批次行禁以 ✅ 开头／✅ 不在状态列开头即报，均只拦本次新增行、复用现成 _leading_status_segment()）与 G（跟进信串行闸挪进 release，带串行豁免逃生阀）是 2026-08-08 新加，勿漏。propose＋design 写完停下等 Shao Peishen 审，不得直接 apply。
```

**它是双队列 plan 的硬前置**：方案 §5 已判定「双队列拆分（P4）硬阻塞于 #308（P2）」——**#308 不落地，双队列拆不了**；#306（断根：7 份解析实现）同样阻塞于它。
**子项 F／G 的来历**（2026-08-08 承载性核查第三批 → 拍板项 5 与他当场追加）：F 治「登记时写 ✅ 导致批次被 sweep 永久跳过」与「✅ 写在行尾、开头仍待领」两类实证事故；G 把跟进信串行原则从人守挪进咽喉。**两项都只拦本次新增行，不追溯历史行。**

---

## E3 ·【CC】worktree 清理吞掉未入库交付证据的根治（P2，已拍板 (a)）——队列 #267

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑（worktree-evidence-guard，新 worktree，收工自删）
读 CLAUDE.md + 跨桌任务队列 #267，认领 #267：git worktree remove 把未入库的 reports/ 内容一并清空，按行内已拍板的 (a) 实现（触碰区＝CLAUDE.md／跨桌任务队列.md／0-学习与工具/工具-孤儿worktree扫描.py，注意行内已更正为此脚本、非工具-仓库外载体扫描.py）。走 openspec（涉新 design 停下等 Shao Peishen 审），收工登记 §二 批次并触发 sweep。
```

**它治的是真实已发生的损失**：签字材料／交付证据（未入库的 `reports/` 内容）随 worktree 清理被真实清空，属 #227「仓库外活载体」同族。

---

## 次序与并行矩阵（并行判据＝触碰区是否重叠，2026-08-08 逐行实测队列触碰区列，非预判）

| | 能否立即开工 | 可与谁并行 | 判据（实测触碰区） |
|---|---|---|---|
| **E1 #305** | ✅ 零依赖 | E2、E3 全并行 | 只碰 `queue_appender.py` |
| **E2 #308** | ✅ 零依赖（先 propose） | E1、E3 全并行 | 碰 `shared_tools/`／sweep／编辑锁／队列查询／台账生成／`decision_reminder.py`／`draft_gap_detection.py`／`openspec/config.yaml`；**不碰** `queue_appender.py`、**不碰** 孤儿 worktree 脚本 |
| **E3 #267** | ✅ 零依赖 | E1、E2 全并行 | 只碰 `工具-孤儿worktree扫描.py` ＋ 两份协调文档 |
| ~~#306 断根项~~ | ❌ **硬阻塞于 #308** | 不可与 E2 并行 | 同改解析器，必然冲突 |
| ~~双队列拆分本体~~ | ❌ **硬阻塞于 #308** | —— | 方案 §5 P4 |

⚠️ **E2 与 E3 都会在收工时写 `跨桌任务队列.md`**——那是所有 session 共有的协调文件，由协议〇.7 编辑锁串行化，**不构成并行冲突判据**（否则任何两个 session 都不能并行）；真正的代码触碰区零重叠。
📌 **下一梯队（本轮不开，等这三个有收口再放）**：#285（因果断言机制化·出方案型，完工会派生新任务）／#96 CC 半边（`.51` 部署标准清单）／#306（等 #308）。
