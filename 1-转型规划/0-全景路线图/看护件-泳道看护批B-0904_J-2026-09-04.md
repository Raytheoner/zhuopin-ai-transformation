---
title: "看护件 · 泳道看护批 B-0904_J（第十批：存量 13 个被跳过 §二 批次 · 真实状态排查与登记，不 merge、不改代码）"
created: 2026-09-04
批次: B-0904_J
派出线: Cowork 业务总线 OP-0904-I
前批: B-0903_90（FI2 五条同车，2026-09-03，0 停 0 转出，已收工，见同目录 `看护件-泳道看护批B-0903_90-2026-09-03.md`）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0904_J`

## 〇、本批口径

**立批依据**：`#381`⑸ⓘ 行内明写「**存量 13 个被跳过批次**（`B-0902_47/48/49/62/64`、`B-0903_12/13/21/33/51/80/82/99`、`B-0904_H`）**全属业务总线**，由其自行订正清单，本线不代改」——这是本线（Cowork 业务总线）自己的欠账，`B1`（`#381`⑸ⓘ）只堵住了新增、没有回头清存量。今日出泳道看护前的核实中发现：这 13 个批次并非同一种情况，抽查数个即见——`B-0902_48`／`B-0903_13`／`B-0903_21`／`B-0903_33` 均已在各自 worktree 分支里真实 `commit` 并 `push`（如 `a74e7d9`／`1bf6b51`／`e615723`／`9ba60c3`），只是从未 `ff` 进 master；`B-0902_49`／`B-0902_62`／`B-0902_64` 属 `oem-chroma` 泳道产出（与 `#374` 同族，本批**明确排除**，见硬边界 2）；`B-0903_99` 是纯队列回写代补登记，本身已完整。

🔴 **本批口径只做一件事：把这 13 个批次的真实状态（有没有真实 commit／push 到了哪／是否已 ff／是否属 oem-chroma 族）逐个查清楚并写成一份排查表登进队列，不做任何 `ff` 合入 master、不改任何代码**——`merge_to_master` 属 🟡 档，且哪些该合、哪些该弃是需要读代码判断影响面的判断题，不该在无人在场的批次里一次性拍板 13 个。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界

1. 🔴 **只排查、只登记，不 `ff`、不 `merge`、不改任何生产代码一行**——13 个批次逐个查完后，若判断某个批次「内容干净、可安全 ff」，**登记结论并停下**（命中 `merge_to_master`，🟡），不得自行执行 ff。
2. 🔴 **`B-0902_49`／`B-0902_62`／`B-0902_64`（oem-chroma 族）本批不查、不碰**——这三个已知与 `#374`／`OEM数据隔离规范.md` 同族，且该规范文件目前在主工作区处于人工在办的脏改动中（登记豁免，非本泳道所属），碰它有撞车风险。本批清单只剩 10 个：`B-0902_47/48`、`B-0903_12/13/21/33/51/80/82/99`、`B-0904_H`。
3. 🔴 **判据/去向类结论永不默认生效**——每个批次的"该 ff／该弃／该重新登记"三选一，一律只提供排查结果与建议，不擅自选定；`⏭️ deploy_51`／`🔴 external_send` 若排查中带出这类项，直接转出／标记，不代办。
4. 🔴 **不改队列文件里已有的历史回写段**（K2 口径，原文原样）——只在每个批次行末追加本次排查结论，不删改既存内容。
5. 🔴 真实库只读；不部署 `.51`；不发任何消息。

---

## 二、波次与触碰区矩阵

🔴 **本批只有一条泳道 `queue-skip13`，单段**（无需分波，纯排查+登记）。

| 波 | 泳道 | 段 | 承接 | 触碰区 |
|:--:|---|:--:|---|---|
| 1 | `queue-skip13` | A1 | `#381`⑸ⓘ 存量 13 个批次（本批实查 10 个，见硬边界 2） | `1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md`、`跨桌任务队列-业务场景.md`（仅追加排查结论段，不改历史） |

---

## 三、泳道 opener

### A1 · 存量批次真实状态排查与登记

粘贴端：CC ｜ 泳道：queue-skip13

▶ 首次派出：[OP-0904-I]

**开场词（复制即用，▶ 粘贴端：CC 新会话）**：

```
[OP-0904-I]【CC】存量批次排查
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0904i-queue-skip13`）｜ worktree：☑（queue-skip13，新 worktree，收工自删）｜ 工作区：无（纯库内只读排查 ＋ 队列回写，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0904_J
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0904I-存量批次排查。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0904_J-2026-09-04.md 的 §〇（本批只排查不合并）＋ §一（硬边界五条）＋ §三 A1（本节做什么）＋ CLAUDE.md 恢复上下文，按 §三 A1 执行。本件为 A 类（范围已写死），无需再问澄清，直接开工。

做什么：
1. 对 §三 A1 段列出的 10 个批次名，逐个在两份队列真身的 §二 区用 `工具-队列查询.py --grep <批次名>` 定位其登记行，读全文取其"文件清单"与"备注"两列。
2. 逐个核实：⑴ 该批次文件清单里的分支/commit（若有）是否真实存在（`git log --all --oneline | grep <commit前缀>`、`git branch -a | grep <分支名>`）；⑵ 若存在，该分支相对当前 master 是否可安全 `ff`（`git merge-base --is-ancestor master <分支>` 与反向都查，判断有无冲突迹象，只读 `git merge-tree`，不实际合）；⑶ 若无任何真实 commit/分支（纯队列回写型，如 `B-0903_99`），确认其对应的队列行内容是否已完整落在正文里（是则本条视为"已完整、无需处理"）。
3. 每个批次给出结论三选一并写明依据：ⓐ 内容干净可安全 ff（列出分支/commit、只读 merge-tree 结果）；ⓑ 已过时/被后续批次取代（列出证据，如同触碰区后续有更新的 commit）；ⓒ 需要人读代码判断影响面才能决定（列出原因）。
4. 汇总成一份排查表（批次名｜类型｜结论ⓐⓑⓒ｜依据｜建议下一步），登进 §四（机制环境队列）新起一行，标题含"存量批次排查"字样；不追加进原 13 个批次行的历史段（硬边界 4）。

不做什么：
- 🔴 不执行任何 `git merge`／`git rebase`／`ff` —— 只用 `git merge-base --is-ancestor` 与 `git merge-tree` 只读判断。
- 🔴 不选 ⓐⓑⓒ 中任何一个的"下一步" —— 只排查、只登记、不拍板。
- 🔴 不碰 oem-chroma 三个批次（见硬边界 2）。
```

### A1 · 开工前置

**心跳**：`reports/lane-heartbeat/B-0904_J-queue-skip13.md`（🔴 写在主仓、不写在 worktree 内），开工 1 分钟内写首行，每里程碑追加，超 10 分钟补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
**分档**：判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0904_J --wave 1 --lane queue-skip13 --action-key <键> --waiting-for "<...>" --no-notify` ＋ 以 `OPENER_PARTIAL` 收尾；⏭️ 走 `transfer-out --batch B-0904_J --wave 1 --lane queue-skip13 --action-key deploy_51 --note "<...>"`（泳道不停，继续做其余）；判据未覆盖时按 🟡 停、在收尾原因里问「这个动作该进哪一档」，不必自行判断。

### A1 · 收工

1. 排查表已登进机制环境队列 §四（新行）；两份队列文件 `工具-队列结构lint.py` 通过。
2. 🔴 **不合入 master**（本批无代码改动，只有队列文件改动）；**§二 批次登记**（🔴 文件清单每个路径用反引号包住，只列真实脏改动——预期只有 `1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md`）。
3. 心跳收尾写「DONE ｜ 排查表落点＝§四新行 #<N>」。

### A1 · 不做什么

- 🔴 不执行任何 `merge`／`ff`／`rebase` 落地动作。
- 🔴 不碰 oem-chroma 三个批次、不碰 `OEM数据隔离规范.md`。
- 🔴 不部署、不触碰 `.51`（命中即 `transfer-out`，泳道不停）。
- 🔴 不起草、不发送任何跟进信。
- 🔴 真实库只读；不改队列历史回写段（只追加新排查结论行）。

---

## 四、收工

跑
```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0904_J
```
现取，不手工数，推企微 ＋ 落档；队列回写走锁。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `B-0902_49/62/64`（oem-chroma 族） | 排除本批（硬边界 2），归 `#374` 族，待 `openspec/changes/oem-chroma-ownership-rejudge/` 走完 design 审后一并处理 |
| 排查后判定"可安全 ff"的批次，实际执行 ff | 登记 §四 待批，`merge_to_master` 属 🟡，等 Shao Peishen 一个字母 |
| 排查后判定"需人读代码判断影响面"的批次 | 登记 §四 待批，交业务总线或值周巡检下一轮细看 |
| `.51` 部署类（若排查中带出） | ⏭️ 转出，交 `zhuopin-lan-closeout` |
