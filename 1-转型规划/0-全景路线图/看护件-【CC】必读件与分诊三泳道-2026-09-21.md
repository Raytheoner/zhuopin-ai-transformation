---
title: 看护件 · 【CC】必读件缩编与分诊三泳道
created: 2026-09-21
派出线: 环境总线 OP-0921-A
批次: B-0921_必读件与分诊
status: 待执行
---

# 看护件 · 【CC】必读件缩编与分诊三泳道 · `B-0921_必读件与分诊`

## 〇、口径

🔴 **本批同时是 `#627` 的效果验收样本**：三条泳道跑在 2026-09-20 晚 ff 进 master 的新 `工具-opener批处理执行v2.ps1` v2.6 上，跑完由看护者用 `工具-Token用量度量.py` 出上下文峰值／请求数／`cache_read`，与 09-20 基线逐条对照。
🔴 **看护件按泳道切片**：本件只放口径与排波，**三条泳道各自的做什么／不做什么全在各自派单件里**，opener 的 `--input-pointer` 直接指向它自己那一份，**不整读本件**。三份派单件 4,380／6,282／4,383 B——对照 09-20 上午那份 24,865 B 的看护件被七条泳道各整读一份、且读进去就常驻整个会话。
🔴 D1 四档判据现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不复述。判据未覆盖按 🟡 停。

## 一、硬边界继承

1. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
2. 🔴 `.claude/rules/`（43,388 B／阈值 30,720 B，超 12,668 B）与根 `CLAUDE.md`（12,110／12,288，余 178 B）**只准 A2 那一棒动，且只能净减字**；A1／A3 一个字都不许碰。
3. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
4. 🔴 收工只 push 自己那条分支，不碰主仓、不 ff master。
5. 🔴 **回归 narrow 到受影响测试类（node-id，禁 `-k`）；不跑整份回归、不起后台任务轮询等待；测试输出一律重定向到文件只读结论行。**
6. 🔴 **收工前必须自己跑 `git diff --name-only master...HEAD` 并贴进收工报告；派单件「期望产出」点名的文件缺一个就不许写 `OPENER_DONE`。**

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#632` | `sweep线` | `0-学习与工具/test_工具-落库sweep.py`＋按需 `0-学习与工具/工具-落库sweep.py`；`1-转型规划/0-全景路线图/分诊-632-*.md` | 派单件＝`派单件-【CC】632-落库sweep16条分诊-2026-09-21.md`。🔴 不碰 rules／根 CLAUDE.md |
| 1 | A2 | `#629` | `sweep线`（**串在 A1 之后**） | `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`；`.claude/rules/*.md`；`CLAUDE.md`；`进度编年-CHANGELOG.md`；`0-学习与工具/工具-落库sweep.py`＋其单测 | 派单件＝`派单件-【CC】629-必读件缩编与体量棘轮-2026-09-21.md`。🔴 与 A1 在 sweep 两文件上**触碰区重叠**，故同泳道串行 |
| 1 | A3 | `#595` | `企微线` | `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`（只读）＋三个提醒脚本（只读）；`1-转型规划/0-全景路线图/取证-595-*.md` | 派单件＝`派单件-【CC】595-企微断连根因回查-2026-09-21.md`。🔴 不重启服务、不向企微发消息 |

`sweep线` 与 `企微线` **触碰区实测零重叠、可并行**；A1／A2 重叠故同泳道严格串行。
🔴 **ff 前后序（看护者记账，属 🟡 档待他一字母）**：`#632` 先 ff，`#629` 分支随后 rebase 再 ff——两者都改 `test_工具-落库sweep.py`。
无 ⏭️ 转出项、无「LAN 留步」项。

## 三、泳道 opener

### A1 · `#632` 纯 master 16 条 pre-existing 失败逐条分诊（§一 #632）

粘贴端：CC ｜ 泳道：sweep线

```
[OP-0921-B]【CC】632分诊16条
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0921b-632-sweep16-triage`） ｜ worktree：☑（op0921b-632-sweep16-triage，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】632-落库sweep16条分诊-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921b-632-sweep16-triage --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921b-632-sweep16-triage --done --batch B-0921_必读件与分诊 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · `#629` 必读件缩编 ＋ 体量棘轮守（§一 #629）

粘贴端：CC ｜ 泳道：sweep线

```
[OP-0921-C]【CC】629必读件缩编
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0921c-629-readfile-slim`） ｜ worktree：☑（op0921c-629-readfile-slim，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】629-必读件缩编与体量棘轮-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921c-629-readfile-slim --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921c-629-readfile-slim --done --batch B-0921_必读件与分诊 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · `#595` 企微机器人断连根因回查（§一 #595）

粘贴端：CC ｜ 泳道：企微线

```
[OP-0921-D]【CC】595断连根因
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0921d-595-wecom-disconnect-rca`） ｜ worktree：☑（op0921d-595-wecom-disconnect-rca，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】595-企微断连根因回查-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921d-595-wecom-disconnect-rca --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921d-595-wecom-disconnect-rca --done --batch B-0921_必读件与分诊 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头（`工具-opener批处理执行v2.ps1 -Detach`），本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0921-E]【CC】看护0921必读件批
【设置】执行环境：CC ｜ 分支：0921-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0921-A ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0921E-看护必读件批。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】必读件与分诊三泳道-2026-09-21.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。🔴 A1 与 A2 触碰区重叠，必须串行：A1 收工后再起 A2。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master。
```

## 四、收工汇总与 `#627` 效果验收

```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0921_必读件与分诊
python 0-学习与工具/工具-Token用量度量.py --since 2026-09-21 --until 2026-09-21
```

🔴 **`#627` 效果验收三条判据（都要给实测数，不许自陈）**：

1. 有没有泳道真的被 250k 硬线截断并按 `OPENER_PARTIAL: 上下文转场` 记账；
2. `summary.txt` 的「上下文峰值」列与「本批越 150k 的泳道 N 条」汇总行是否真的出现；
3. 峰值分布相对 09-20 基线有没有下移。

**09-20 基线**：上午七条峰值 244,434／228,308／213,218／194,282／163,826／155,660／151,036，`cache_read` 5.02M–15.48M，请求 47–98；下午 `d8a76b23` **257,399／106 请求／17.44M**（已越 250k 硬线），`86ee819b`（4.5 KB 薄派单件）**167,792／46／5.24M**。
