---
title: "【CC】开场 prompt · C 批（#248 独立）· sweep 与编辑锁判据锚定状态列开头"
created: 2026-08-05
执行方: CC 平台
执行环境: CC
来源: 队列 #248（2026-08-05 环境保障线自身踩中并当场取证）；业务总线 2026-08-05 派
status: 待执行
---

# 【CC】开场 prompt · C 批 · #248

## 【开场词（复制即用）】

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑ ｜ 工作区：企业AI转型

读 1-转型规划/0-全景路线图/开场prompt-【CC】C批-248-判据锚定状态列开头-交接.md ＋ CLAUDE.md 当前进度恢复上下文，按文件执行。本行须走 openspec 含 design 审，不得直接改代码。
```

---

## 〇、🔴 为什么本行必须单独一个 session（不许与任何批同车）

1. **触碰的是两个咽喉件**：`工具-落库sweep.py`（提交的咽喉）与 `工具-共享文档编辑锁.py`（队列写入的咽喉）。一次回归失败会让**所有线的落库与队列写入同时受影响**。
2. **先例已写明**：2026-08-04「平台杂项批七行同车」明确记着「本批**刻意**不含任何触碰这两个文件的行」。
3. **须走 openspec 含 design 审**——改 sweep 取活判据属**改变全项目口径**，命中 CLAUDE.md §5 机制类触发门槛第 ① 条。**不得直接改代码。**

同期在跑的 A 批（#241/#245）与 B 批（#208/#223/#101①）**均已刻意避开这两个文件**；但 A 批红线 2 允许它在一种情形下读 `_validate_followup_readme_release`。**若你与 A 批同时在跑，改这两个文件前先看队列在办行的触碰区。**

---

## 一、问题（已取证，2026-08-05 真实发生）

§二 批次 `B-0805_124propose设计交付` 的状态列回填 ✅ 时，说明文字里**引用了 sweep 判据原文**（那句「既不含 ✅ 也不含〔表示尚未处理的单字〕」）——**该单字落在状态列内即被判为可取活** → sweep 取活该批、按其建议 msg 执行一次提交（`a4f6ba3`）、并把状态列覆写为标准回写句，**原核实说明连同 commit hash 一并丢失**（只改了那一行、零业务影响，已复原）。

**⇒ 关键不是这次损失多大，而是：一条状态列只要在任何位置提到判据关键词，就会被判成那个状态——优先级上「可取活」还压过了开头的 ✅。**

**同族前两次**：⑴ **#164** 裸竖线致列数偏离（正文含 `--section 一〔竖线〕四` 等）；⑵ **#225** 校验④ 初版按整行扫 `P0/P1` ＋「未核」字样，**把两条正在讨论这条规则的行自己拦下**，当日已改为只查状态列。**本次说明：只查状态列仍然不够。**

**这是同族第三次，已达规则退休制阈值**（CLAUDE.md §5：任一「人守」条目被违反 3 次即须机制化或删除）。

---

## 二、三条修法（建议 ⑵）

- ⑴ **人守**——写状态列时避开判据关键词。本次即如此处置，**但这正是规则退休制要淘汰的形态**，不可作为最终解。
- **⑵ 机制：判据锚定状态列开头（建议）**——与既有约定「销行 ✅ 须写状态列开头」天然一致：取活判据改为「状态列去掉 `*` 与空白后**以**表示尚未处理的字样**起头**」，✅ 起头即已完成。**改动面小、与现有书写习惯一致、不需要迁移历史数据。**
- ⑶ **更彻底**：批次状态改用受控枚举（`PENDING`／`DONE`／`SKIP`）由工具写入，人不再手写自然语言状态。**更干净，但要迁移全部历史批次行、且改变所有人的书写方式，代价大得多。**

**⑵ 与 ⑶ 的取舍请写进 design.md 交 Shao Peishen 审，不要自行拍板。** 我的倾向是 ⑵，但那是倾向不是结论。

---

## 三、🔴 两处必须一起改（漏一处比不改更糟）

**`#225` 的 `release` 校验同样在扫状态列关键词**（P0/P1 定级 + 「未核」断言门槛）。**两处判据应同批锚定**——否则会出现**一处放行、一处拦截**的分裂状态，比现在更难排查。

改完须验证：**同一段文字**在 sweep 与编辑锁两侧得到**一致的判定**。

---

## 四、验收

1. **单测**：状态列描述文字含判据关键词、但**不在开头**时不被误判（sweep 侧 ＋ 编辑锁侧各一组）。
2. **反向单测**：真正待处理的批次（关键词在开头）仍能被正常取活——**别把判据收得太紧，那会让所有批次永远不落库，而且表现为"一切正常、只是没人提交"**，比误取活更隐蔽。
3. **一轮真实 sweep 验证**：登一个测试批次，确认取活→落库→回写全链路正常。
4. **历史兼容核对**：现存 §二 批次行按新判据重新判一遍，**结果应与人工判断一致**；不一致的逐条列出，**不要静默按新判据处理**。

---

## 五、收工段

1. 队列 **#248** 回填：状态 + openspec 变更包路径 + 采用 ⑵ 还是 ⑶ 及 design 审结论 + 单测清单 + 真实验证记录 + 历史兼容核对结果。
2. **design 审必须由 Shao Peishen 拍板后再 apply**（CLAUDE.md §5 第 3 步"停下"）。
3. 完工即归档（`/opsx:archive`），不留"代码完成但变更包未归档"的中间态。
4. 列出全部新产出/修改文件清单 + commit message；CC 自行 commit + push + 收工重跑台账。
5. 收工自删 worktree（ahead=0 且 clean 时）。
