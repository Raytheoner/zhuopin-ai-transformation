# 派单件 · 泳道看护续棒（OP-0920-B）

> 派出：`OP-0919-K` 收工转场（2026-09-20 上午，Cowork）。本件为 **A 类**。
> 首要输入：本件全文 ＋ `1-转型规划/0-全景路线图/session接力-Phase1收口.md` ＋ `CLAUDE.md`（相关节 §5）。
> 🔴 接棒第一件事按接力卡开头那三行做，**不要**再额外拉全量摘要。

## 〇、开工前必过「方案闸」（本件最高优先级纪律）

见接力卡 §〇。本棒的方案已经替你定好了，照做即可、不要自己重新发明：

1. **判据只取结论性清单**——文件名、表头行、汇总行。🔴 **禁止 `cat` 任何 `*.patch`／日志正文**；要判 main-leak 用 `python 0-学习与工具/工具-main-leak回放.py`。
2. **扫池只用** `python 0-学习与工具/工具-队列查询.py --digest --actionable`（7.6 KB，全量是 22 KB）。
3. **凡触碰 git 的命令一律本机（Windows）侧跑**——挂载侧已是机器守，写类子命令会 fail-loud 拒绝。只读的 `status`／`triage-candidates` 两侧都行。
4. **长跑段只看哨兵**（`exit.txt`／`OPENER_DONE`／`OPENER_PARTIAL`），大间隔探，不逐分钟 tail 日志。ff 回归段已有空闲超时（默认 20 分钟无输出即判挂死，退出码 7＝挂死 ≠ 回归失败）。
5. **判「两处改动重不重叠」先 `git diff --stat`／`--unified=0` 看 `@@` 头**，不拉全文 diff。

**判据**：本棒开工后若因「换个更省的做法」掉头重来一次，即为方案闸没过，须在收工件里如实写明。

## 一、做什么

### ⑴ 把 §二 能领的环境侧任务全领完（本棒主任务）

按接力卡 §二 现取的六条，逐条过方案闸后开工。领取方一栏写「CC 平台（环境总线派）」的**实为待派，可直接派，不必等他**：

- `#561` openspec 收口段堵塞（在办 75／归档 62，25 个包 >14 天无改动）
- `#563` 回件勾选提取（三犯，正在产生对真人的重复追问 —— **本棒优先级最高**）。🔴 口径已由 Shao Peishen 2026-09-20 当场更正：`☑` 与 `☒` **都是「已选中」**，只有空白 `☐` 才是未选；出现哪个符号**由 Word 控件形式决定**，一般三选一。原件模板都是我方给的，AI 专员只做选择与回复、不改控件形式。⇒ 工具侧仍按并集判（`w14:checked val="1"` ∨ `☒` ∨ `☑`）兜住在途件，**根治在源头**＝行内新增产出 ⑤：跟进信选择控件统一只用 `☑`，改 `zhuopin-followup-letter` 技能模板与 docx 生成段并加出件前机器守（成品 docx 里出现 `☒` 即 fail-loud 拒绝）。存量已发出的件不回改。
- `#567` `-Detach` 在 PS 5.1 下必炸 ＋ 并入的两条（`Start-Job -WorkingDirectory` 崩；崩后不落 `exit.txt`，须补 `finally`）
- `#601` 新场景 `propose` 前置 `intent.md` 机器守 —— **派单件与引用版 opener 都已备好，直接起，不要给他粘**
- `#416` 企微回件链路三缺陷 ⑴⑵⑷⑸⑺ 仍开
- `#328` sweep 起跑段 `_fetch()` 失败即整轮中止
- `#621` opener 出件顺序缺陷：派单件先落档、再跑 `工具-opener生成.py`，P7① 查重把「占号者就是本件自己」判成撞号（**本件自身就是实证**，`OP-0920-A` 号已白烧不回收）

**每条改动都要带回归用例**；回归 narrow 到受影响的测试类，不为一个类跑整份测试文件。

### ⑵ 继续无头泳道看护

- 他说「**看看泳道**」＝ 把**无头泳道进展**告诉他（standing instruction，2026-09-19 立）。
- 起批用 `工具-opener批处理执行v2.ps1 -Plan <看护件> -FullAuto -Yes -Detach`；泳道运行期间**看护者不写主仓**。
- 收工走第三道闸（主仓脏文件复查 → `*-main-leak.patch`），判读用回放器。

### ⑶ 收工前

落 §二 批次 → `python 0-学习与工具/工具-落库sweep.py` → push。锁走一次 `acquire` → 多次 `edit-row --who` → `append-row` → 一次 `release`，🔴 **不要串联 `commit-edit`**（release 被拒会把锁扣住，第二条必撞「占用中」）。

## 二、不做什么

1. 🔴 **别往 `.claude/rules/` 任何文件加字**——现取合计 43,388 B ／ 阈值 30,720 B，**已超 12,668 B**；`两桌同步与取证.md` 单份 12,339 B 亦超单份阈值 8,192 B。这道守卫是告警型、没有消费者（与本日已改成拒绝型的根 `CLAUDE.md` 那道同族）。真要处置＝一整件活，**先过方案闸**，不要顺手开。
2. 🔴 **别碰卡在他一字母的行**：`#455`（待合并决策）、`#481`（等他 design 审）、`#440`／`#503`（须 design 审）、`#439`（已 `blocked`，停在 design 审）、`op0910n-529-timeout-semantics`（改口径，他单独裁）、§四 `#213` 七个 Owner（**永不代填**）。
3. 🔴 **CLAUDE.md §5 常设禁区照旧**：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 部署及生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）**永不代办**。ASIL C/D ＝ AI 绝对禁区。
4. **根 `CLAUDE.md` 余量只剩 178 B**（12,110／12,288），sweep 已改为**拒绝**含根 `CLAUDE.md` 的超阈值批次——要往里加字得先腾地方。
5. `#539` 与在办 `#538` 同触碰 SC2 周报页面，**已判不领**。

## 三、接棒时的现场状态（2026-09-20 上午收工时现取）

master `b239f68c`，与 origin 一致；编辑锁已放；**没有泳道在跑**；机制类可动 WIP **14／上限 22**。
主仓脏文件仅 `.claude/settings.json`（本机会话设置，不入库）与他本人 LAN 线的两件在办看护件（`看护件-LAN收口批B-0919_Q2上线-2026-09-19.md`／`…_审计零项-…`，由该线自行登记，已点名豁免至 09-22）。

## 四、仍开的、要他一字母的（本棒只问、不替他答）

`#595` ⑷ 已认失败（他 2026-09-20 答 `1a`），转为回查根因——观察窗 09-16 20:54→09-17 20:54 **17 次** vs 前 24h **15 次，不降反升**，其后逐日 6／4／2；「重启当晚重连震荡」**只是推测、未验证**，三个可查方向已写进 `#595` 行内。
其余：`#614`（落地在他手上）、`#600`／`#477`、`#617` 丙档 `op0910n`。

## 五、答复格式

收工答复末尾必须给**填好推荐项、一次粘走的单个代码块**（不是 `1<a/b>` 空白填空表）；只有明确「本项无默认」的那一项才留空位。告知类／状态表用普通文字，不做成代码块。
