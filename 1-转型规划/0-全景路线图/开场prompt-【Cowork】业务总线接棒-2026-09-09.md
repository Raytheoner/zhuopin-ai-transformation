---
title: "开场 prompt ·【Cowork】业务总线接棒（OP-0908-V）"
created: 2026-09-08
status: 待粘贴
派出线: Cowork 业务总线 OP-0908-A
来源: Shao Peishen 2026-09-08 答「转场」——转场判据第 ⑤ 条阳性（token 压力真实，接力卡侧压缩手法已连用三轮：外置／指针化／教训迁知识库，再压要删有承接价值的内容）
---

# 开场 prompt ·【Cowork】业务总线接棒

▶ 粘贴端：Cowork（新开 session）。本件由 `工具-opener生成.py` 生成，六字段齐全。

> **交棒时的三句话**（其余一律读接力卡与队列，本件不复述）：
> 1. 🔴 **`1-转型规划/0-全景路线图/队列行日志/#502.md` 是 SC2 冒烟八段的唯一副本**（`reports/` 被 gitignore）。它此刻**可能仍不在 git** —— 09-08 查出 sweep 覆盖校验自锁、卡死 6 个批次，已立 `#507` 根治并用一个不含高频件的干净批次抢救。**接棒第一件事就是核它落库没有。**
> 2. 🔴 **关键路径是 `#506`**（SC2 页面基准日冻结在服务启动日、生产页面陈旧 12 天）：`采购部#22` 与 `#499` 的对外承接都压在它后面，⏰ 参照 **09-11**。**修好 ≠ 可发信**——解押判据是复跑全量重算、确认页面期次为当周。
> 3. 🔴 **两条 CC 在跑**（`OP-0908-T` 看护四泳道／`OP-0908-U` 单棒修 `#506`）。它们端出的定夺清单**在 CC 那边答**，本线只转达并标「已答，勿重做」。

▶ 首次派出：[OP-0908-V]

**开场词（复制即用，▶ 粘贴端：Cowork 新会话）**：

```
[OP-0908-V]【Cowork】业务总线接棒
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐（不建，只产改 `.md`） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-A
读 ① `1-转型规划/0-全景路线图/开场prompt-【Cowork】业务总线接棒-2026-09-09.md` → ② `CLAUDE.md` §3／§5 恢复上下文，按下述执行。本件为 A 类。

做什么：
1. 开工自检三查（通过即不问）：⑴ `python 0-学习与工具/工具-队列查询.py --digest --file 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md` 扫池核触碰区，🔴 不得 Read／grep 队列真身；⑵ 四位专员各跑一次 `python 0-学习与工具/工具-跟进闸查询.py --to <人>` 现取串行闸（接力卡与队列里的信状态复述都是过时快照）；⑶ `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0908_四泳道机制根治` 与 `show` 现取在跑批状态。
2. 🔴 **先核三条落地再动手，不重做已完成的活**（09-08 实撞两次，本条是硬前置）：⑴ **`#507` 抢救批是否已落库** —— 判据＝`git ls-files --error-unmatch "1-转型规划/0-全景路线图/队列行日志/#502.md"`，🔴 **该件是 SC2 冒烟八段的唯一副本**（`reports/` 被 gitignore），未落库则先查 `reports/sweep-commit.log` 有无新的跳过原因；⑵ `B-0908_四泳道机制根治` 六任务的 `summary`；⑶ `#506` 有无修法定案（`--row 506 --file 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`）。
3. **两条 CC 会话的回报按新口径处理**：`OP-0908-T`（看护四泳道，含 `#503`／`#504`／`#476` 三条 design 审）与 `OP-0908-U`（单棒修 `#506`）。🔴 **谁端出来的题就在谁那儿答**（`.claude/rules/队列与落库.md` 正本）——它们端的清单**在 CC 那边答**，本线只做转达并标「已答，勿重做」＋写明由谁执行；**同一题不得两端各答一次**（09-08 实撞，差约 30 秒两条线同时 rebase 同四条分支）。
4. **关键路径盯住不放**：`#506`（SC2 页面基准日冻结在服务启动日、生产页面陈旧 12 天）→ 修好并**复跑一次全量重算、确认页面期次为当周** → `采购部#22` 解押（`#152` D-IT11 作废知会 ＋ `#168` 六项指标口径澄清，两件已合并为一封）→ `#499` 对外承接。⏰ **参照截止 2026-09-11**：届时若 `#506` 未修好，§四 `#176` 须重新端给他在 (b)/(c) 间选，🔴 **不得默认继续等**。💡 致陈承那封（请核 SRM 两端点）不受影响、可照常起草。
5. **主攻仍是 `#374` 母行**（部分完成态，接力卡【三】1）：隔离规范新增 §7 运维面／全景规划三处／前置总表三处／质量域重审件 §2.1 追注／两份 docx 重转／**grep 全库零残差**（旧日期实测 213 处、51 件）——全未做。

不做什么：
- 🔴 不 commit、不 push、不碰 `.51`、不发企微；对外发送／L2 门禁／合规红线永不代办——信件只做到「⏳ 待你审」态。
- 🔴 **§二 登记时清单里少写高频共享件**（两份队列真身、接力卡、文档台账）—— 那正是 `#507` 那个自锁的成因：含它们的批次会被 sweep 覆盖校验每轮判「不脏」而整批跳过、永远落不了库。**只列本批独有产出件。**
- 🔴 不修工具（总线会话发现机制缺陷只登记＋出 CC 派单）；不动 `op0908k-work`（`#358`，他答 5b 等 design 审）；不碰在跑两条 CC 的触碰区。
- 🔴 不新立机制行前先重跑 WIP 实测（`_section_bounds` ＋ `_count_mechanism_wip`，09-08 收工值 20/22）；销售域 6 个＋研发域**不立行、不排队**。

收工：产出登记 §二 待 commit 批次（走 `0-学习与工具/工具-共享文档编辑锁.py`，勿裸改、勿自行 commit），由落库 sweep 取活。
```
