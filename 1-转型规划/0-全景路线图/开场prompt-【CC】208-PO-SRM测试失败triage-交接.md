---
title: "【CC】开场 prompt · #208 `test_po_srm_confirmed_date.py` 失败 triage"
created: 2026-08-02
执行方: CC 平台（建造车间）
执行环境: CC
来源: 队列 #208（2026-08-02 环境保障线只读复核后登记）；#197/#207 收工"顺带发现"降格回 triage
派发方: 🔴 业务总线（**非环境保障线**）——见下方「派发归属」段
status: 待派发（派单件已备，待业务总线决定是否派、何时派）
---

# 【CC】开场 prompt · #208 PO-SRM 测试失败 triage

## 🔴 派发归属：本件由业务总线派发，环境保障线只提供材料

**队列 #208 的领取方已于 2026-08-02 改判**：由「CC 平台（环境保障线派）」改为**「业务总线派发」**。

**依据＝CLAUDE.md §5 与本线 opener §一 铁律一细化（Shao Peishen 2026-08-02 原话）**：*"这是环境整治，归本线协同 CC 端治理；具体项目构建任务还是尽量交予业务总线"*。**判别看动作对象**——本件动的是 `zhuopin_platform` 的 `get_purchase_orders`（平台底座功能），且 B 段可能触发 SC8 `.51:8091` 现网部署，**属项目构建，牵动业务口径与现网服务，那是业务总线的责任面**；环境保障线掌握台面与机制证据，**不掌握业务影响判断**。

**故本文件的性质是「交接材料」而非「已派单」**：环境保障线已完成的**只读 triage 取证**（下方 §二/§三）原样保留、可直接复用，**但是否派、何时派、与哪些业务排期串在一起，由业务总线定**。**接手的 CC session 请确认自己是经业务总线派来的**；若你是被环境保障线直接叫来的，那是流程错误，请回报 Shao Peishen。

## 【开场词（复制即用）】

```
【设置】执行环境：CC ｜ 分支：先不建（A 段纯诊断）；确需改码再建 fix/po-srm-test ｜ worktree：☐（A 段）／☑（B 段） ｜ 工作区：⚠️ 见「工作区字段」段——被测函数是 SC8 保供看板取在途 PO 的入口，.51:8091 在跑

读 1-转型规划/0-全景路线图/开场prompt-【CC】208-PO-SRM测试失败triage-交接.md ＋ CLAUDE.md 当前进度恢复上下文，按文件执行。
🔴 本件是 triage 不是修复：A 段结论若为"未复现"或"从未是回归"，即为合格交付、直接销行，不要为了"有产出"去改代码。
开干前先答 §五 的两个澄清问题（均为带推荐项的选择题）。
```

---

## 🔴 先读这一段：本件的定性已被降级，不要按"修复回归"开工

`#197`／`#207` 那个 CC session 收工时"顺带发现"并报告：`test_po_srm_confirmed_date.py` 5 个测试从"通过"变"失败"，定性为**"与本次改动无关、已排除环境混用可能、07-31 后某次改动引入的独立回归"**，并已 spawn 后续任务。

**环境保障线只读复核后认为这个定性支撑不足**，故降格回 triage 并登记为 #208。**下面是已完成的取证，直接用，不要重做。**

---

## 已完成的只读取证（**已排除三个候选成因，收敛到两个**）

| # | 检查 | 结果 | 结论 |
|---|---|---|---|
| 1 | 测试文件自身提交史 | 全部提交史**只有一条** `6427139`（2026-07-13），mtime 同 | **测试侧未变过** |
| 2 | `git log --since=2026-07-13 -S "supplier_confirmed_date"` / `-S "srm_connector"` on `connector.py` | **只命中 `6427139`**（即创建它的那一次） | 🔴 **自测试诞生起，它所测的代码路径再没被任何提交碰过** |
| 3 | `ZpConnector.__init__` 签名 `6427139` vs `HEAD` | **逐字相同**（16 个参数、默认值全一致） | **排除"构造函数新增必填参数导致 5 个用例全挂"** |
| 4 | 跨 checkout 混用假设 | 该 session 跑在 worktree `sweep-criteria-sync-fix-7eb8a7`（当时基线 `453fad2`）；`git diff 453fad2 HEAD` 对 `connector.py` 与该测试文件**均零差异**；整个 `zhuopin_platform` 包在此区间**只新增** `simple_gate.py` + `test_simple_gate.py`（401 行纯新增、零删除） | 🔴 **原本是环境保障线自己提的替代解释，现被自己的取证削弱**——两侧字节相同，混用解释不了本测试的失败 |
| 5 | editable 安装指向 | `import zhuopin_platform` → `C:\...\企业AI转型\5-平台底座\zhuopin_platform\zhuopin_platform`（**主工作区**） | 结构性事实：**从任何 worktree 跑 pytest，导入的都是主工作区的包**。本次虽不构成成因，但**这是全项目性陷阱，值得单独留痕**（见 D 段） |

**➡️ 剩下的候选只有两个（加一个"其实不是回归"）**：

- **(甲) 共用代码/helper 改动** —— `connector.py` 自 07-13 起被 **7 个提交**改过（`eceac23`／`5715dad`／`dcb287d`／`1285e8d`／`471ced2`／`8b6eb98`／`e6bf6a2`，全部是 FI2 AP、BOM 校验、替代料等**其它功能**），虽未碰 SRM 确认日期那段，但**可能改到 `get_purchase_orders` 调用的共用 helper**（鉴权/会话/缓存/mock fallback）。
- **(乙) 测试间污染或运行顺序** —— 注意 #160 新增的 `test_simple_gate.py` 与 `simple_gate.py`（读 `ZP_GATE_PASSWORD` 环境变量）。**若某个测试改了进程级状态（env / 模块级缓存 / 单例）而未还原，就会只在全量跑时挂、单跑时过。**
- **(丙) 它根本不是"回归"** —— 有可能自 07-13 之后就没人单独跑过这个文件。**若如此，本行结论是"历史遗留失败"而非回归，处置方式完全不同。**

---

## 「工作区」字段（协议〇.1 第四字段）

**自检一问：本次改动，有没有任何一份"正在跑的副本"不在我改的这个 checkout 里？**

- **A 段（纯诊断）＝无**，只读不写。
- **B 段（若确需改 `get_purchase_orders`）＝🔴 有**：该函数的现网消费方是 **`4-数字员工/采购部/SC8-客户订单交期智能承诺/sc8/sources.py` L229**（SC8 保供看板取**在途 PO** 的入口，**`.51:8091` 正在跑的服务**）与 `sc8/answer_confidence_engine.py` L144。**改它须按既有部署纪律走**：`sync-to-server.ps1` 推送 → 重启计划任务 → 冒烟三件套（`/api/ping`／关键页 200／一次全量重算）→ 回滚 SOP 在位。
- ⚠️ **但在 A 段出结论前，不得据此提级、不得惊动采购域**——目前**没有任何证据指向线上异常**，只有一个定性存疑的测试失败。

---

## 权威依据清单（只给指针）

1. `1-转型规划/0-全景路线图/跨桌任务队列.md` **#208 行** —— 完整取证记录与验收标准（**顺序不可颠倒**）。协议〇 八条必读。
2. 同上 **#207 行** —— worktree 纪律滞后，本件 D 段的留痕落点之一。
3. 同上 **#98** —— 月度环境体检，D 段另一个留痕落点。
4. `5-平台底座/zhuopin_platform/tests/test_po_srm_confirmed_date.py` —— 被测行为：注入 `srm_connector` 后按 `(po_id, supplier_id)` 查真实确认日期覆盖占位值；查不到/未注入/查询失败时退回现状行为，**全 mock 不触网**。
5. 根 `CLAUDE.md` §5 —— 机制优先、完工即归档、发布即收口、"不信工具说成功了"。

---

## 任务分段

### A 段 · 诊断（**先做完，不要跳到修复**）

**A1 在主工作区干净复现**
- 单独跑该文件一次，再随全量跑一次，**两种方式都记结果**。
- 🔴 **两次结果不同 → 直接指向 (乙) 测试污染**，这是最省力的判别，务必先做。
- **验收**：明确写出"单跑 / 全量"两组结果。**"未复现"是合格且有价值的结论**——若单跑通过，本行即可按 (乙) 或 (丙) 收口，不必进 A2。

**A2 若确实失败，定位引入点**
- 先看失败输出属哪一类（构造失败／断言不符／异常），据此选路：怀疑 (甲) 就 `git bisect` 或对那 **7 个提交**逐个回放；怀疑 (乙) 就用 `-p no:randomly` / 单文件隔离 / 清 env 复跑。
- 🔴 **验收必须给出具体 commit 或具体污染源，不接受"某次改动"这种表述**（这正是本行被降格的原因）。

**A3 判定它是不是"回归"**
- 用 `git stash` 或临时 checkout，在 `6427139` 之后、第一个可疑提交之前的某个点跑一次同一测试，**确认它曾经真的通过过**。
- **验收**：若从未通过过，结论改为"历史遗留失败"，并在 #208 行内明写——**处置方式与回归完全不同，不要混为一谈**。

### B 段 · 修复（**仅当 A 段确证是真回归才做**）
- 按 §5「完工即归档」与「发布即收口」处理；若改动触及 `get_purchase_orders` 行为，**须按上方「工作区字段」走 SC8 部署闭环**。
- **验收**：全量回归零漂移（平台底座 + SC8 + 其余场景），黄金基准不漂移。

### C 段 · 顺带核实现网是否真受影响（**仅当 A 段确证真失败**）
- 只读核对 `.51:8091` 保供看板的在途 PO 数据是否异常（`X-Auth-Token` 请求头访问，见 #160）。
- **验收**：给出"线上正常／异常"的明确结论。**线上正常也要写**——它决定要不要通知采购域。

### D 段 · 留痕（**无论 A 段结论如何都要做**）
- 把「**从 worktree 跑 pytest 会导入主工作区的包**」这条**全项目性陷阱**写进 **#207** 与 **#98**（依据＝上表第 5 行实测）。
- **理由**：本次它虽不是成因，但**它随时可能成为下一次误判的成因**，而现在没有任何文档记着它。

---

## 开工前置步（认领先于动手）

按触碰区关键词（`test_po_srm_confirmed_date`／`get_purchase_orders`／`supplier_confirmed_date`／`srm_connector`／`#208`／`#207`／`#98`／`sc8/sources.py`），**同时 grep 《跨桌任务队列.md》与《跨桌任务队列-归档-YYYYMM.md》**，确认无他人在办行重叠。**归档件必须一起 grep。**

⚠️ **另需确认**：#197/#207 那个 session "已 spawn 的后续任务"是否仍在跑——**若在跑，先与之合并，不要两处并行做同一件事**（这正是 #151/#152 分段交付的成因形态）。

---

## 五、开工前请先答（两个选择题，均带推荐项）

**问题 1 · A1 若"单跑通过、全量失败"，本行怎么收口？**
- **(a) 推荐：定位污染源并修掉那个污染测试，本行按 (乙) 收口。** 代价＝要多花时间找是谁污染的；好处＝**根治**，否则全量跑的结果长期不可信。
- (b) 只记录现象、标注"已知全量跑失败"，不修。代价＝**全量回归从此不再是可信判据**，而本项目每次交付都以"全量回归零漂移"为验收标准——等于把验收基准废掉。
- (c) 给该测试加 `skip`。**不推荐**——把一个可能真实的问题永久隐藏。

**问题 2 · D 段那条"全项目性陷阱"写到哪？**
- **(a) 推荐：写进 #207 与 #98 两处。** #207 是 worktree 纪律本体、#98 是月度体检清单，**两处都会被后续 session 读到**。
- (b) 只写 #98。代价＝#207 的领取方看不到，而**它恰恰是最可能踩到的人**。
- (c) 升格进 `CLAUDE.md` §5。代价＝§5 已很长；**建议先落队列两处，若再次有人踩到再升格**（守"机制优先、从最省起步"）。

---

## 纪律段（只列条目名）

- **跨桌任务队列**：开工必读认领 + 登记触碰区；收工必写状态与产出路径，新冒出的下游任务当场追加待领行。
- **编辑锁（协议〇.7）**：改队列前 `acquire`、改完立刻 `release`；**#197 已把 `acquire` 改为 `O_EXCL` 原子实现**（2026-08-02），行为不变但更可靠。
- **编号取号**：`acquire --reserve N --section 一|四`，不自己做加法。
- **批次须声明变更参数（协议〇.8）**：本批若改了任何判据/阈值/行为，须写明"本批变更参数：X（旧值→新值）；已 grep 复检、可能受影响：#A、#B"。
- **完工即归档、发布即收口、机制优先、不信"工具说成功了"**（CLAUDE.md §5）。

---

## 收工段

1. 回填队列 #208（**A 段结论必须写清是"回归/污染/历史遗留失败/未复现"四者中的哪一个**）；D 段留痕回填 #207 与 #98。
2. **CC 自行 commit + push**；**收工重跑一次文档台账**。
3. 若只做了 A 段，**在行内写清"B/C 段未做及原因"**，不得整体标完成。
4. 会话末按 CLAUDE.md §5 用固定小节「**需你定夺**」罗列决策项：**每项必须是可直接作答的是非题或选择题**（带 `(a)/(b)/(c)` 字母标签 + 每项写清选项间实际差异与代价 + **标注默认项**"若不答我按 (x) 执行"）；🔴 **两栏各归各位、双向都禁（2026-08-03 补齐）**：既**不得把纯状态汇报混进「需你定夺」**，也**不得把需他定夺的事塞进「状态同步（无需你答）」**。判据＝**凡出现"等你定／请你选／待你拍板"字样，一律属「需你定夺」**，不论看起来多像顺带一提。
