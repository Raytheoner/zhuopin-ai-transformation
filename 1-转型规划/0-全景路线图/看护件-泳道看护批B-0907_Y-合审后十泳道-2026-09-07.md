---
title: "看护件 · 泳道看护批 B-0907_Y（合审后十泳道：六包 apply＋#455 ff＋#452 三分支 ff＋#447 元数据校验＋#487 取号查重起草）"
created: 2026-09-07
批次: B-0907_Y
派出线: Cowork 环境总线 OP-0906-W（批 B-0907_Y）
LAN: on（13:5x 探针；本批无 `.51` 项）
status: 待你在 CC 里起（🔴 前提＝`B-0907_R` 看护者已出收工汇总——两批共碰 sweep／编辑锁／状态机三处，见 §〇）
---

# 看护件 · 泳道看护批 `B-0907_Y`

## 〇、本批口径

**立批依据**：Shao Peishen 2026-09-07 合审材料 11 项全 (a)（`合审材料-八包design与三项决策-2026-09-07.md`）：六包 design 审过 → apply；`#455` ff 授权；`#447` O1–O4 定死；`#487` 修法定死。单批上限 10（D2 改判 09-07），本批 10 条。

**🔴 起批前提**：`B-0907_R` 五泳道现全停在 🟡（等你字母）＋波二 `#493` 未起。本批 A3（状态机）、A6/A7（sweep）、A5/A8（编辑锁）与 R 的 A3/A4 同文件——**R 收工汇总出了再粘本批**；若你想先跑，只起波一（`-Only`）。

**分档**：全 🟢 起步；ff 全 🟡；改口径 🟡；无 ⏭️／🔴。现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

## 一、🔴 硬边界

1. 六包 apply 开工第一步＝rebase 到最新 master、在 `design.md` 顶部回填「design 审：✅ 已过 —— Shao Peishen 2026-09-07（合审 §N）」；**按 `tasks.md` 施工，不改 design 已定死的点**。
2. 🟡 即停（`pause`＋`OPENER_PARTIAL`）：ff 合入 master／改口径阈值／关他人行。合入统一由看护者串行 ff，一次一条。
3. 🔴 不碰：`.51`、真实库、对外发送、`3-治理与合规` 正文、FI2 目录（`OP-0907-N`）、`B-0907_R` 五条泳道的分支。
4. 看护会话不修工具；落档失败即全停；不改队列行历史段只追加。

## 二、波次与触碰区矩阵

| 波 | 泳道 | 承接 | 触碰区 |
|---|---|---|---|
| 1 | `483-archive-two` | A1 | `openspec/changes/{sweep-manifest-coverage-guard,fi2-invoice-level-idempotency}`（零代码，validate→archive） |
| 1 | `481-docx-reader-apply` | A2 | `5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/doc_parser/`＋四处调用点＋`md2word.read_checkboxes` 删除 |
| 1 | `452-three-branch-ff` | A4 | 三分支 rebase＋ff（git only；行内已有授权） |
| 1 | `447-metadata-guard` | A9 | `0-学习与工具/工具-跟进信README登记.py`（发送状态闭集／收信人格式校验）＋事实日／补记日两字段＋度量脚本单列 |
| 1 | `487-reserve-draft` | A10 | 新 `openspec/changes/queue-reserve-unfilled-release/`（propose＋design，止步 design 审；不改编辑锁代码） |
| 2 | `478-lanewatch-deploy-apply` | A3 | `0-学习与工具/工具-泳道看护状态机.py`（`deploy-authorize`）＋`zhuopin-lane-watch/SKILL.md`＋新 lint 接 CI |
| 2 | `455-ff` | A5 | `claude/op0905n-editrow-guard-455` → master（编辑锁写侧） |
| 2 | `462-plan-scanner-apply` | A6 | 新扫描器脚本＋JSONL registry＋sweep 第 13 类接入 |
| 3 | `479-sweep-manifest-apply` | A7 | `工具-落库sweep.py`（pathspec commit 五处）＋单测（等 A6 到达终态） |
| 3 | `482-serial-gate-apply` | A8 | `工具-跟进闸查询.py`＋编辑锁闸校验段＋README 五行机器豁免清理（等 A5 到达终态） |

不入本批（下一批）：`#448` apply（等 A8）、`#459` 收口、`#312` 对齐、`#326`（软序 `#448`）。

## 三、泳道 opener

### A1 · 483-archive-two——两个事后补包归档

粘贴端：CC ｜ 泳道：483-archive-two

```
[OP-0907-Z]【CC】483两包归档
【设置】执行环境：CC ｜ 分支：master（检出 `claude/op0907f-openspec-483` rebase 后处理，不新建分支）｜ worktree：☑（`openspec-483`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#483`（`python 0-学习与工具/工具-队列查询.py --row 483 --field all`，做什么/不做什么/收工全在该行；design 审过见末段）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A2 · 481-docx-reader-apply——统一 docx 勾选读取工具 apply

粘贴端：CC ｜ 泳道：481-docx-reader-apply

```
[OP-0907-AB]【CC】docx读取apply
【设置】执行环境：CC ｜ 分支：master（检出 `claude/op0907h-docx-reader-481` rebase 后续建，不新建分支）｜ worktree：☑（`docx-reader-481`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#481`（`python 0-学习与工具/工具-队列查询.py --row 481 --field all`，做什么/不做什么/收工全在该行；正本＝包内 `tasks.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A4 · 452-three-branch-ff——lane-watch 三分支 rebase＋ff

粘贴端：CC ｜ 泳道：452-three-branch-ff

```
[OP-0907-AC]【CC】452三分支合入
【设置】执行环境：CC ｜ 分支：master（三分支逐条 rebase 后 ff，不新建分支）｜ worktree：☐（只做 git 合入，不改代码）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#452`（`python 0-学习与工具/工具-队列查询.py --row 452 --field all`，做什么/不做什么/收工全在该行；末段已有 Shao Peishen (a) 授权合入）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A9 · 447-metadata-guard——跟进信元数据 O1–O3 校验＋事实日／补记日

粘贴端：CC ｜ 泳道：447-metadata-guard

```
[OP-0907-AD]【CC】跟进信元数据校验
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907ad-followup-metadata-447`）｜ worktree：☑（`followup-metadata-447`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#447`（`python 0-学习与工具/工具-队列查询.py --row 447 --field all`，做什么/不做什么/收工全在该行：O1–O3 校验＋⑵ 两字段＋度量单列；O4 不做）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A10 · 487-reserve-draft——「预留未落盘不算占」openspec 起草

粘贴端：CC ｜ 泳道：487-reserve-draft

```
[OP-0907-AE]【CC】取号预留释放起草
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907ae-reserve-draft-487`）｜ worktree：☑（`reserve-draft-487`，新 worktree，收工自删）｜ 工作区：无（纯库内 openspec 起草，不改编辑锁代码）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#487`（`python 0-学习与工具/工具-队列查询.py --row 487 --field all`，做什么/不做什么/收工全在该行：只到 propose＋design，止步 design 审）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A3 · 478-lanewatch-deploy-apply——⏭️ 档「他在环授权续做」apply（波二）

粘贴端：CC ｜ 泳道：478-lanewatch-deploy-apply

```
[OP-0907-AF]【CC】看护部署授权apply
【设置】执行环境：CC ｜ 分支：master（检出 `claude/op0907g-lanewatch-deploy-478` rebase 后续建，不新建分支）｜ worktree：☑（`lanewatch-deploy-478`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#478`（`python 0-学习与工具/工具-队列查询.py --row 478 --field all`，做什么/不做什么/收工全在该行；正本＝包内 `tasks.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A5 · 455-ff——写侧校验加固分支合入（波二）

粘贴端：CC ｜ 泳道：455-ff

```
[OP-0907-AG]【CC】455写侧校验合入
【设置】执行环境：CC ｜ 分支：master（`claude/op0905n-editrow-guard-455` rebase 后 ff，不新建分支）｜ worktree：☐（只做 git 合入＋归档，不改代码）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#455`（`python 0-学习与工具/工具-队列查询.py --row 455 --field all`，做什么/不做什么/收工全在该行；末段已有合入授权）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A6 · 462-plan-scanner-apply——「规划倒逼开工」扫描器 apply（波二）

粘贴端：CC ｜ 泳道：462-plan-scanner-apply

```
[OP-0907-AH]【CC】规划扫描器apply
【设置】执行环境：CC ｜ 分支：master（检出 `claude/op0907j-plan-scanner-462` rebase 后续建，不新建分支）｜ worktree：☑（`plan-scanner-462`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#462`（`python 0-学习与工具/工具-队列查询.py --row 462 --field all`，做什么/不做什么/收工全在该行；正本＝包内 `tasks.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A7 · 479-sweep-manifest-apply——sweep 按清单 pathspec 提交 apply（波三）

粘贴端：CC ｜ 泳道：479-sweep-manifest-apply

```
[OP-0907-AI]【CC】sweep清单apply
【设置】执行环境：CC ｜ 分支：master（检出 `claude/op0907i-sweep-manifest-479` rebase 后续建，不新建分支）｜ worktree：☑（`sweep-manifest-479`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#479`（`python 0-学习与工具/工具-队列查询.py --row 479 --field all`，做什么/不做什么/收工全在该行；正本＝包内 `tasks.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A8 · 482-serial-gate-apply——串行闸归一化 apply（波三）

粘贴端：CC ｜ 泳道：482-serial-gate-apply

```
[OP-0907-AJ]【CC】串行闸归一化apply
【设置】执行环境：CC ｜ 分支：master（检出 `claude/op0907k-serial-gate-482` rebase 后续建，不新建分支）｜ worktree：☑（`serial-gate-482`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务；D3 上线前先据实改 `质量部#13` 状态属 🟡）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
读 ① 队列 §一 `#482`（`python 0-学习与工具/工具-队列查询.py --row 482 --field all`，做什么/不做什么/收工全在该行；正本＝包内 `tasks.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

## 三bis、看护opener（单次粘贴，Task/Agent 工具起十子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0907-Y]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0907-Y]【CC】看护B-0907_Y
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，各子泳道自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Y）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0907Y-看护合审后十泳道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（十条泳道由你用 Task/Agent 派发，正文里不放这一行），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉；你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0907_Y-合审后十泳道-2026-09-07.md` 全文＋ CLAUDE.md 恢复上下文。开工先核 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_R`：R 未出收工汇总则只起波一，波二／三等 R 的 A3（sweep）、A4（编辑锁）到达终态后再起。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，
`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。

扇出与 git（硬口径，逐条照做）：
1. **并行上限 4**，超出的排下一波；同一波内起子任务**错峰 ≥90 s**。波一＝A1／A2／A4／A9／A10（5 条，分两拨 4＋1）；波二＝A3／A5／A6；波三＝A7（等 A6 终态）／A8（等 A5 终态）。
2. 子任务收工**只 push 自己那条分支**——不碰主仓工作区、不 ff master、不在主仓 commit。A4／A5 的任务本身就是 ff，**由你在其报告全绿后串行执行**，一次一条。
3. **主仓 ff 由落库 sweep 收尾段做**；sweep 不可用时由你在**收工时串行**做。**push 不是定夺项**（`#194`）。
4. 🔴 **你自己不 commit**；撞 `.git/index.lock` 不重试、不自造豁免，按「登记待补」写进队列行。

看护：D1 判据现取 `… criteria`；🟡／🔴 即 `pause`＋`OPENER_PARTIAL`；⏭️ 即 `transfer-out` 后继续；波间 `check-heartbeat`（超时先核 mtime）。子任务说「已完成」不要只信自述——亲自核分支是否 push、单测是否跑过、`design.md` 审过行是否已回填。

收工汇总（一次写全）：
5. 每泳道一行 ＋ 心跳全文 ＋ D6 停点列 ＋ 转出项 ＋「待回内网」清单。
6. 末尾固定一段「**待你一字母**」清单：**同类合并**（多条 ff 只问一次），每项写清 (a)/(b) 与各自代价、标〔荐〕与默认项；design 审类排在 merge 类之前；**清单最后一行给答复模板**（如 `1a，2a`）。
7. 🔴 **等待时长只认 `reports/lane-watch-state.json`**（`… summary --batch B-0907_Y` 现取），**会话不得自算**。
```

## 四、收工

`python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_Y` 现取；队列回写走锁；推送按 `#492` 默认运维群。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#448` apply、`#326` 尾项 | 等 A8 终态，下一批（编辑锁写侧同文件） |
| `#459` 收口、`#312` 对齐 | 下一批 |
| `B-0907_R` 五条 🟡 停点 | 在 R 看护者会话里等你字母，本批不代答 |
| `#436`⑵ `.51` 半边、`#440` 起草 | 前者 `B-0907_Q` 收口批；后者本批已满，下一批首位 |
