---
title: "看护件 · 泳道看护批 B-0906_X（需求侧续棒两泳道：A2 决策点校验合入 ＋ R1 台账读写模块 §1.3–1.6）"
created: 2026-09-06
批次: B-0906_X
派出线: Cowork 环境总线 OP-0906-W
LAN: 不依赖内网（纯库内）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0906_X`

## 〇、本批口径

**立批依据**：`B-0906_P` 收工实况（看护件 `看护件-泳道看护批B-0906_P-需求侧五泳道-2026-09-06.md` §六）留下两段续棒：⑴ A2 分支 `claude/op0906r-decision-field-guard`＠`83cd43c` 因 D4(a)「先收尾 `followup-readme-phase2`」暂缓——2026-09-06 21:5x 核实该包 5.4／5.5 实已完成（`#490` 为 `[S:done]`、`5e95bd5` 在 master、`origin/master..master`＝0），tasks 已核勾，**前提已满足**；⑵ `#439` R1 台账 §0（CC）＋ §1.1／§1.2（Cowork）已完成，串行链第三段 §1.3–§1.6 归 CC。

**WIP**：不激活任何行，两泳道全落在已 open 的 `#436`／`#439`。**触碰区互不重叠**（登记 CLI vs 台账模块），一波并行。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

## 一、🔴 硬边界

1. `pause`／`check-heartbeat` 一律 `--no-notify`（`#492` 未落地）。
2. A1 只做 rebase→跑测→ff，**不改分支内容**；rebase 冲突即 `pause --action-key merge_conflict`，不自行解。合入后按「完工即归档」对 `openspec/changes/followup-decision-point-gate/` 走 `/opsx:archive`（tasks 全 [x] 才归；否则写 `暂不归档` 理由）。
3. A2 严格按 `openspec/changes/coverage-point-ledger/tasks.md` §1.3–§1.6 与 `6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`（§三 六条约束＝断言清单）；🔴 不做 §2（回溯拆点归 Cowork）、不写任何真实口径点、五份 `.jsonl` 保持 0 字节（单测用 tmp 目录）。
4. 真实库只读、不部署 `.51`、不发任何对外消息、不碰他线在办行。
5. 不改队列行历史回写段，只追加。

## 二、波次与触碰区矩阵

| 波 | 泳道 | 承接 | 触碰区 |
|---|---|---|---|
| 1 | `436-decision-field-merge` | A1 | 分支 `claude/op0906r-decision-field-guard` → master（`工具-跟进信README登记.py`＋单测）；`openspec/changes/followup-decision-point-gate/` 归档 |
| 1 | `439-ledger-module` | A2 | 新 `5-平台底座/zhuopin_platform/zhuopin_platform/coverage_point_ledger/`（或 tasks 指定位置）＋单测＋lint（D3／D5／D8 三条断言） |

## 三、泳道 opener

### A1 · 436-decision-field-merge——A2 分支合入 master（D4(a) 前提已满足）

粘贴端：CC ｜ 泳道：436-decision-field-merge

```
[OP-0906-Y]【CC】决策点校验合入
【设置】执行环境：CC ｜ 分支：master（检出既有分支 `claude/op0906r-decision-field-guard` rebase 到 master，不新建分支）｜ worktree：☑（`decision-field-merge`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_X
读 ① 队列 §一 `#436`（`python 0-学习与工具/工具-队列查询.py --row 436 --field all`，本泳道＝该行末段「2026-09-06 续棒」之 (i)-合入，做什么/不做什么/收工全在该行）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A2 · 439-ledger-module——R1 口径点台账读写模块与三条断言（tasks §1.3–§1.6）

粘贴端：CC ｜ 泳道：439-ledger-module

```
[OP-0906-Z]【CC】台账读写模块
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906z-ledger-module-439`）｜ worktree：☑（`ledger-module-439`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_X
读 ① 队列 §一 `#439`（`python 0-学习与工具/工具-队列查询.py --row 439 --field all`，做什么/不做什么/收工全在该行；正本＝`openspec/changes/coverage-point-ledger/tasks.md` §1.3–§1.6 与 `6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

## 三bis、看护opener（单次粘贴，Task/Agent 工具起两子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0906-X]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0906-X]【CC】看护B-0906_X
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，各子泳道自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_X
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0906X-看护需求侧续棒。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（两条泳道由你用 Task/Agent 派发，正文里不放这一行），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉；你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0906_X-需求侧续棒两泳道-2026-09-06.md` 全文＋ CLAUDE.md 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，
`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。

扇出与 git（硬口径，逐条照做）：
1. **并行上限 4**，本批两条一波并行，起子任务**错峰 ≥90 s**。
2. 子任务收工**只 push 自己那条分支**——不碰主仓工作区、不 ff master、不在主仓 commit。A1（`436-decision-field-merge`）例外：它的任务本身就是把既有分支 `claude/op0906r-decision-field-guard` rebase 后 ff 进 master——D4(a) 前提已核满足，**由你在 A1 报告全绿后串行执行 ff**（一次一条）；A2（`439-ledger-module`）新分支的 ff 属 🟡 档，等 Shao Peishen 一字母。
3. **主仓 ff 由落库 sweep 收尾段做**；sweep 不可用时由你在**收工时串行**做。**push 不是定夺项**（sweep 起跑段无条件补推，`#194`），不要问他要不要 push。
4. 🔴 **你自己不 commit**；撞 `.git/index.lock` 不重试、不自造豁免，按「登记待补」写进队列行。

收工汇总（一次写全）：
5. 每泳道一行 ＋ 心跳全文 ＋ D6 停点列 ＋ 转出项。
6. 末尾固定一段「**待你一字母**」清单：同类合并，每项写清 (a)/(b) 与各自代价、标〔荐〕与默认项；design 审类排在 merge 类之前；**清单最后一行给答复模板**（如 `1a，2a`＝按推荐全选）。
7. 🔴 **等待时长只认 `reports/lane-watch-state.json`**（`python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0906_X` 现取），会话不得自算。
```

## 四、收工

`python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0906_X` 现取；队列回写走锁；不推企微。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#439` §2 回溯拆点（2.1 先吃 18 封带 `决策点:` 的信） | Cowork 环境总线，A2 建成读写模块后接棒 |
| 8 包补 grill | Cowork 会话逐包，首个 FI10 ＝ `开场prompt-【Cowork】FI10需求grill-首个样本-2026-09-06.md`（`OP-0906-V`），前置已清、可即起 |
| (iii) 真实采样（`.51`） | `zhuopin-lan-closeout`，待回内网 |
| `#447` ⑵／`#440` | 仍 🛑，余量优先需求侧，下一空位候选 |

---

## 六、收工实况（看护者 `OP-0906-X`，2026-09-06）

### 6.1 每泳道一行

| 泳道 | 结果 | 分支／SHA | 测试 | 落点 |
|---|---|---|---|---|
| A1 `436-decision-field-merge` | ✅ 全绿，**已 ff 进 master** | `claude/op0906r-decision-field-guard` `83cd43c`→`bc2005d`（零冲突、内容未改仅换底，已 force-with-lease 推 origin） | 登记 CLI 30 passed ＋ 邻接六件 132 passed（均与基线同数）；`openspec validate followup-decision-point-gate --strict` valid | master `14fa679`→`bc2005d`，7 文件 +550/−2 |
| A2 `439-ledger-module` | ✅ 全绿，**未 ff**（🟡 档待定夺） | `claude/op0906z-ledger-module-439` ＝ `7878c28`（13 files / +2554，已 push origin） | 新增 4 件 60 passed / 1 skipped；`zhuopin_platform` 全量 527 passed / 2 skipped；`openspec validate coverage-point-ledger --strict` valid | `5-平台底座/zhuopin_platform/zhuopin_platform/coverage_point_ledger/` |

### 6.2 看护者独立复核（不只转述泳道自陈）

- **A1 ff 前置**（主仓自证）：`git merge-base --is-ancestor master claude/op0906r-decision-field-guard` exit 0；`git rev-list --left-right --count` ＝ `0/1`；`git diff --name-only master <branch>` 得 7 文件，与主仓 15 处未提交脏改动**零重叠**（故 ff 不撞工作区）；`.git/index.lock` 不存在。ff 后 master ＝ `bc2005d`，脏改动 15 处原样保留。
- **A2 测试复跑**（看护者在 A2 worktree 内亲自跑，非转述）：`python -m pytest tests/test_coverage_point_ledger_{store,aggregate,signoff,lint}.py -q` → **60 passed, 1 skipped**，与泳道自陈同数。
- **A2「写内容入口唯一」独立核**：`grep -rnE "\.open\(|open\(|write_text|write_bytes|\.touch\(|mkdir\("` 扫模块（排除测试），除 `store.py:232` 的 `path.open("a", ...)`（＝ `_append_line`）外仅剩 `touch()` ／ `mkdir()` 两处**结构性**调用（建 0 字节文件、建目录，均不写内容），与泳道的 AST 结论一致。
- **D8 真目录复跑**：lint CLI 打真台账目录 → `✅ 目录布局合规（无子目录、五份域文件平铺）`，exit 0。
- **五份 `.jsonl` 未被碰**：`ls -la` 复核五件仍 **0 字节**、时间戳仍为 `OP-0906-W` 建文件时刻（`Sep 6 21:10`）。
- **A2 分支的真实合入形态**：因 A1 的 ff 后行，该分支现**落后 master 1 提交**（`git rev-list --left-right --count master...<branch>` ＝ `2/1`），**合入须先 rebase，不是直接 ff**；且其 commit 含 `openspec/changes/coverage-point-ledger/tasks.md`，与主仓工作区那份同文件的未提交改动（§1.1 ／ §1.2 勾选）相撞，需人工并一次。

### 6.3 D6 停点列

- **本批停 0 次｜本批转出 0 项｜本批 index.lock 撞击 0 次｜本批企微通知失败 0 次**（`python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0906_X` 现取，逐字照录，会话未自算）。两泳道全程未触 🟡/🔴，未调 `pause`，故状态机内无停点记录。
- 🔑 **状态机「转出 0 项」与 6.5 的清单不矛盾**：状态机的「转出」专指 ⏭️ 档（`.51` 部署及触碰生产服务），本批无此类；6.5 列的是**看护件硬边界之外、需总线接手**的事项，不是 ⏭️ 档，故未调 `transfer-out`（该命令还会推企微 FYI，与本批 §四「不推企微」相抵）。
- 唯一一次**外部阻塞**：看护者收工回写队列时 `acquire` 被 `sweep-commit`（B-0906_CI红取证）持锁 18 分钟拒绝——**未抢锁、未自造豁免**，按工具指引转「队列更新待补」（见 6.4）。

### 6.3bis 心跳全文

**本批无心跳文件** —— `reports/lane-heartbeat/` 下无 `B-0906_X` 相关文件，`reports/lane-watch-state.json` 中 `B-0906_X` 出现 0 次。成因：§三 两条泳道 opener 均未指定 `--heartbeat-file`，两泳道全程未触 🟡/🔴、未调 `pause`，故状态机未为本批建条目。**这是"没有心跳"，不是"心跳全绿"** —— 波间看门狗（5.6）本批实际未生效，两泳道的存活判断依赖 Task 完成通知本身。

### 6.4 队列更新待补（🔴 下次开工首件）

`#436` ／ `#439` 两行的状态列追加**内容已备好、逐字可用**，存于 `1-转型规划/0-全景路线图/队列回写待补/B-0906_X-436.json` ／ `B-0906_X-439.json`（格式即 `edit-row --changes-json` 的入参，反引号奇偶已校验为偶）。复补命令：

```
python 0-学习与工具/工具-共享文档编辑锁.py acquire --who CC-OP0906X --note "补 B-0906_X 回写"
python 0-学习与工具/工具-共享文档编辑锁.py edit-row --who CC-OP0906X --section 一 --number 436 --domain 机 --changes-json "1-转型规划/0-全景路线图/队列回写待补/B-0906_X-436.json"
python 0-学习与工具/工具-共享文档编辑锁.py edit-row --who CC-OP0906X --section 一 --number 439 --domain 机 --changes-json "1-转型规划/0-全景路线图/队列回写待补/B-0906_X-439.json"
python 0-学习与工具/工具-共享文档编辑锁.py release --who CC-OP0906X
```

⚠️ 补完后两行状态列都会超 4 KB（`#436` 4033 B ＋1492 B；`#439` 2741 B ＋3197 B）。今天 `2026-09-06` < 阻断日 `2026-09-11`，**仅告警不阻断**；但 K2 外置须在 09-11 前做完 —— 见 6.5 转出项。

### 6.5 边界外事项与去向（非状态机 ⏭️ 档，见 6.3 说明）

| 事 | 为什么本批不做 | 去向 |
|---|---|---|
| `#436` ／ `#439` 状态列 K2 外置（历史回写段迁 `队列行日志/#N.md`，行内留首段＋末段＋指针） | 看护件 §一.5 硬边界＝「只追加、不改队列行历史回写段」，外置须动历史段 | 🔴 **总线，deadline `2026-09-11`**（该日起 release 由告警转阻断，届时任何人碰这两行都会被拒） |
| 清理旧 worktree `.claude/worktrees/agent-a34c5e99547e8e4d6` | 它占着 `claude/op0906r-decision-field-guard`，工作区停在 rebase 前旧内容；Windows 下 `worktree remove` 有半途失败先例（摘注册但目录残留、静默指回主 checkout） | 总线择机，**非紧急**（分支已 ff 进 master，该目录只读残留无害） |
| `openspec/changes/followup-decision-point-gate/` 归档 | 包内 `tasks.md` 尚 15 项未勾；§4.2 压硬前置「本闸已在真实起草流程里至少拒绝过一次并留痕」、§5.3 要求 3 封新信观察窗口 | 待前置自然闭合后再归，**ff 完成 ≠ 可归档** |
| SCHEMA §二 `carrier` 自相矛盾（字段表要求 ≥1，同节示例两条 `转态` 行均无） | 属口径判据，改它是 🟡 档 | 与 SCHEMA §四「与 `criteria_signoff` 同名」一并进**两包合审** |
| `#439` §2 回溯拆点、8 包补 grill、(iii) 真实采样、`#447`⑵／`#440` | 见 §五原表 | 不变 |

---

## 七、`1a，2a` 拍板后的执行实况（Shao Peishen 2026-09-06 答复，看护者 `OP-0906-X` 执行）

### 7.1 定夺 1 ＝ (a)：`carrier` 口径已定死

`carrier` ≥1 **仅对 `建点` 行**生效；`转态` 行可空、写入期不强制。已改齐**三处**，无遗漏副本：

| 载体 | 改动 |
|---|---|
| `6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md` §二 字段表 | 「≥1（建点或转态行）」→「≥1（**仅建点行**）」，并在说明列写明拍板出处（`Shao Peishen 2026-09-06 经 OP-0906-X 拍板 (a)`）与两条成因 |
| `…/coverage_point_ledger/models.py` 首部 | 标题由《⚠️ 一处 schema 内部不一致（本轮不替它做决定）》→《✅ `carrier` 口径已定死》；末句由「请在两包合审时一并定死」→「本条已闭合，不再进两包合审」；行内注释指针同步 |
| `openspec/changes/coverage-point-ledger/tasks.md` §1.3 | 「如实登记一处不一致／待合审」→「已定死」＋ 三处改齐说明 |

🔑 **模块实现一行未改** —— (a) 选的正是模块既有取舍，本次是把文档追到代码，不是改代码。
🔑 **两包合审剩余项只剩一条**：SCHEMA §四「`id` 与 `criteria_signoff` 同名」。

### 7.2 定夺 2 ＝ (a)：A2 分支已合入 master

1. **rebase**：`claude/op0906z-ledger-module-439` `7878c28` → `1ea9ac8`，零冲突（分支 13 文件与 master 新增的 `bc2005d` ／ `6d6df03` 无交集）。
2. **换底后复跑**：底座全量 `527 passed / 2 skipped`，与 rebase 前**同数** —— 换底没动行为。
3. **push**：`--force-with-lease=…:7878c28` 成功，origin ＝ `1ea9ac8`。
4. **`tasks.md` 撞车处置**（本次唯一有风险的一步，逐字记录以便复核）：主仓工作区那份有未提交的 §1.1 ／ §1.2 勾选，与分支的 §1.3–§1.6 勾选同文件。做法 ＝ `git diff` 存补丁 → `git checkout --` 还原该文件腾出 ff 空间 → `git merge --ff-only` → `git apply --3way` 原样回贴（`Applied patch to … cleanly`）。**复核 §1.1–§1.6 六项全为 `[x]`，两侧改动零丢失。**
5. **ff**：master `6d6df03` → **`1ea9ac8`**（ff 前 `git rev-list --left-right --count` ＝ `0/1` 实证）。
6. **合入后复跑**：`528 passed / 1 skipped`。🔑 **比 worktree 里多跑一条不是新增用例** —— `test_D8_真台账目录当前合规` 在 worktree 中因真台账目录不存在而 skip，回主仓后真跑并通过。`openspec validate coverage-point-ledger --strict` ＝ valid；lint CLI 打真目录 exit 0。

### 7.3 队列回写：已落（6.4 的「待补」已闭合）

`sweep-commit` 的锁在 35 分钟时判为陈旧。**接管前先证伪其存活**：`Get-CimInstance Win32_Process` 查无 `sweep`／`落库` 进程，且其提交 `6d6df03` 已落 master ⇒ 属异常退出遗留，接管合法。随后 `edit-row` 两行、`release` 全绿。

- `#436` 状态列 4033 B → **5523 B**；`#439` 2741 B → **6798 B**。
- release 两条 ⚠ 行长告警如预期（`2026-09-06 < 2026-09-11`，本周仅告警不阻断）。
- ℹ acquire 时工具报「队列文件在上次 release 之后被直接改写（329→329 行，约 2 行不同）」—— **发生在本会话 acquire 之前，非本线所为**，如实登记不追改。

### 7.4 唯一剩余未闭合项（**2026-09-06 已闭合，见 §八**）

🔴 **`#436` ／ `#439` 的 K2 外置，deadline `2026-09-11`**。两行现分别 5523 B ／ 6798 B，09-11 起 release 由告警转阻断，届时**任何人碰这两行都会被拒**。外置须动队列行历史回写段，与本看护件 §一.5「只追加」硬边界相抵，故本批未做。

---

## 八、K2 外置已做（Shao Peishen 2026-09-06 答 `a`，授权越 §一.5 边界一次）

§7.4 的 deadline 项**当场闭合，不再挂到 `2026-09-11`**。

| 行 | 外置前 | 行内新格 | 外置了什么 |
|---|---|---|---|
| `#436` | 5525 B | **3187 B** ✓ | 2 段：本行来源不可追的 `OP-0829-W` 取证段 ＋ `OP-0906-W` 续棒派出段（该派出的 (i)-合入已完成，收工段留行内作末段） |
| `#439` | 6800 B | **4079 B** ✓ | 1 段：`OP-0906-W` 的 §1.1／§1.2 收工段与 §1.3–§1.6 派出段 |

`release` **零行长告警**（此前两条 ⚠ 已消失）；`工具-队列结构lint.py` 退出码 0（列数／§二状态列格式／权威模块可 import／称呼判据全过）。

### 8.1 🔴 `#439` 与前三次外置有一处**不同**，务必知悉

`#439` 的末段（`OP-0906-Z` §1.3–§1.6 收工段）**单段即 4058 B**——只留标准的「首段＋末段＋指针」仍会超 4 KB。⇒ **行内末段是精简版，外置件 `队列行日志/#439.md` 四次外置节那份才是原文全文**。此事已写进行内指针（`🔴 本次与前三次不同…查证以外置件为准`），避免后来者把行内精简版当成完整记录。

精简掉的只有测试函数名与模块文件名枚举；**结论性事实一条未删**，已逐项核过：`1ea9ac8`（合入 SHA）／`528 passed`／`carrier` 定死／`--force-with-lease`／`apply --3way`（撞车处置法）／`§2 回溯拆点`（下一棒）六项在行内末段中全部在位。

### 8.2 无损实证

对两行分别做 `外置前整格原文 in 外置件全文` 的**逐字包含判定**，两条均 `True` —— 不是「看起来搬过去了」，是原文逐字节可在外置件中检出。外置件：`#436.md` 22554 → **28464 B**；`#439.md` 18163 → **25485 B**。

应用的两份 `--changes-json` 入参原样留在 `1-转型规划/0-全景路线图/队列回写待补/`（`-set.json` ＝ 本次外置的整格重写，`.json` ＝ 此前的追加），供复核「行内现在这段字，到底是谁在什么时候按什么内容写进去的」。

### 8.3 `#436` 首段修正（`#268` 冲突警告）＋ 一处**没能清零**的如实登记

`工具-队列查询.py --row 436` 报 `#268` 族冲突警告：开头写「待领」、末尾写「✅ 已合入」，**只读开头会得到相反结论**。

- **该警告先于本次外置就已存在**（实证：外置前整格的 `segs[0]` 含「待领」、`segs[4]` 含「✅」，共现判定 `True`）——不是外置改出来的。但行瘦身后「只读开头」的误判风险更高，且笔在本线手上，故顺手修正首段为：「待领 —— 🔴 **本行分片推进，只读开头会得到相反结论**：**(i)-合入 ＝ ✅ 已完成并已合入 master**（`bc2005d`）；**仍待领的是 ⑵（可即领）与 ⑶（软序于 lan-closeout 灰度与回灌落地后）**」。
- 🔴 **修正后警告仍然触发，没有清零，如实登记**：该检测器是关键词共现式的，首段现在同时含「待领」与「✅ 已完成」，仍命中。**本线刻意不追它到零** —— 要清零只能把「✅ 已完成」从首段拿掉，那恰好会**还原**成「只读开头得到相反结论」的原病灶。⇒ 判定为**检测器在一行真·混合态上的合理触发**，不是缺陷。**判据留给后来者：`#436` 的这条警告可忽略；但它一旦出现在别的行上，仍须逐行核，不得因本条先例批量当噪音。**

（`工具-队列结构lint.py` 退出码 0。）
