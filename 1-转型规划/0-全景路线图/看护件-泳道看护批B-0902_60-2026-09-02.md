---
title: "看护件 · 泳道看护批 B-0902_60（第二批：三条续跑段 ＋ #463 场景立行）"
created: 2026-09-02
批次: B-0902_60
派出线: Cowork 业务总线 OP-0902-X7
触发: Shao Peishen「所有能领任务我们都在业务总线按序泳道看护推进」
前批: B-0902_46（5 泳道 6 项，停 3 次全部已答已 resume）
status: 执行中
---

# 看护件 · 泳道看护批 `B-0902_60`

## 〇、本批口径

**⑴ 三条续跑段**：`B-0902_46` 的 B1/B2/B3 已被 Shao Peishen 答复并 `resume`，但**「已 resume」≠「已续跑」**——续跑段须由 Cowork 起，本批即那三段。**每条 opener 内已内置他的裁决原文**，泳道不必回头猜。

**⑵ `#463` 场景立行**：机制线 `OP-0902-A` 取证后移交业务总线，14 个场景「三处皆无」（队列无承接行／`4-数字员工` 无工程／openspec 无包）。🔴 **立行判据（他 2026-09-02 定，与常规排期不同）＝不管规划时间表、能开尽开，唯一门槛是前置依赖**（判据源＝《跨场景前置数据与知识库任务总表》）。

🔴 **本批范围收窄（Shao Peishen 2026-09-02 当场追加指令，原话「先做质量财务采购域场景，研发和销售域场景依然暂缓等我通知再排序」）**：

| 域 | 本批处置 | 场景 |
|---|---|---|
| **采购** | ✅ 立行 | `SC4`（🔴 最紧，权威排期 2026-10）／`SC10`／`SC11` |
| **财务** | ✅ 立行 | `FI5`／`FI6`／`FI8`／`FI9`／`FI10` |
| **质量** | ✅ 若有则立行 | ⚠️ `#463` 的十四个里**不含质量域**（Q 系列全在那 11 个灰区中，待 `#462` 扫描器收紧判据后重扫）⇒ 本批无质量域可立，如实登记、不硬凑 |
| **销售** | 🛑 **暂缓** | `S1` 客户跟进／`S2` 拜访助手／`S3` RFQ／`S4` 技术文档／`S5` 竞品监控／`S6` 销售预测 —— **等他通知再排序，本批一行不立** |
| **研发** | 🛑 **暂缓** | 同上，等他通知 |

⇒ **本批实立 8 个（采购 3 ＋ 财务 5）。**

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不手抄。

## 一、硬边界（继承 `B-0902_46`，逐条适用）

1. 🔴 **⏭️ 档：`.51` 部署及任何触碰生产服务的动作，本包永不执行** —— `transfer-out` 标注去向后跳过该项、继续其余；**不进 `OPENER_PARTIAL`**。
2. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 相关永不代办**，只做到「可发送态」即停。
3. 🔴 **业务*数据*与一切对外发送永不自动入批**。
4. 🔴 **所有 `pause` / `check-heartbeat` 一律带 `--no-notify`，无一例外**（推送目标写死为跨部门大群，含各部门专员，已误发过一次且撤不回）。
5. 判据未覆盖按 🟡 停（fail-safe）；⏭️ 转出档不参与 fail-safe。
6. 🔴 **三闸全锁**（唐燕萍／姚祖怡／陈忱）⇒ 本批任何泳道**不得起草或发送任何跟进信**。
7. 🔴 **不自造豁免理由**；不碰他线在办行；规划文档（全景规划／实施计划／权威排期表）**一律不改** —— 那属全景路线图线，本批只出移交单或队列行。

## 二、波次与触碰区矩阵

| 波 | 泳道名 | 项 | 触碰区 | 说明 |
|:--:|---|:--:|---|---|
| 1 | `oem-chroma-2` | B1 | `openspec/changes/oem-chroma-ownership-rejudge/`、`5-平台底座/` 文档 | 无重叠 |
| 1 | `fi3-prep-2` | B2 | FI3 前置件、队列 §一 #373 | 无重叠 |
| 1 | `sc8-kitdate-2` | B3→B4 | `sc8/`、队列 §一 #118／#344 | B3→B4 同泳道串行 |
| 2 | `scenario-rows` | B5 | 队列 §一 **大量新行**、`4-数字员工/` 目录骨架 | 🔴 **锁密集 ⇒ 恒最后一波** |

🔴 **`scenario-rows` 必须排在第二波**：它要一次预留并写入十余行，持锁时间长；与波 1 三条的收工回写会正面撞锁（`B-0902_46` 已实证：A6 持锁期间 Cowork 的 `acquire` 被正确拒绝）。

## 三、泳道 opener

### A1 · Chroma 归属重判 · 按五点裁决收口

粘贴端：CC ｜ 泳道：oem-chroma-2

```
[OP-0902-B1]【CC】Chroma重判收口
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902b1-oem-chroma-apply`）｜ worktree：☑（oem-chroma-2，新 worktree，收工自删）｜ 工作区：无（openspec 与文档，🔴 不改隔离层实现代码、不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_60
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902B1-Chroma收口。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行；⚠️ 无头 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0902_60-oem-chroma-2.md`，开工 1 分钟内写首行「HH:mm:ss ｜ 已开工」，每里程碑追加，超 10 分钟的步骤补写「仍在等 X，预计还要 N 分钟」，收工写「DONE ｜ 产出落点」。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_60 --wave 1 --lane oem-chroma-2 --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL` 收尾；⏭️ 走 `transfer-out` 后跳过继续。
读 ① `openspec/changes/oem-chroma-ownership-rejudge/design.md` → ② `1-转型规划/0-全景路线图/OEM隔离层Chroma归属重判-结论件-2026-09-02.md` → ③ `3-治理与合规/OEM数据隔离规范.md`。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-02 design 审五点裁决（原文，逐条落实）：
  D0=(b) 已知客户 NDA 无「物理隔离／独立实例／独立存储介质」字样 ⇒ 不列前置，单实例多 collection 方案成立。
  D1=(b) 不需要检索历史 8D 语料（他原话「先选简单case的实现」）⇒ 新 Q2 采纯规则库逐条判定，
        历史 8D 仅作离线校准语料、不进生产检索路径。
        🔴 连带三条必须写进 spec：① 「归集时即分库」照样适用、不因 (b) 豁免；
        ② 最早消费方退到 R1（2027-07／前置约 2027-05）；
        ③ 「L3 本体这一轮还建不建」升为新的待判项，须另立队列行（本泳道负责立，不代判）。
  D3=(a) 采纳到期日可机械重算规则：L2/L3 到期日 = min(所有需跨客户检索 OEM 数据的已排期场景的前置开工日)，
        另有更早数据落盘门禁时取更早者；重算并入 zhuopin-rebaseline 重组循环固定步骤；
        L1 改述为「不得下线、无到期日」。
  D2=(a) 收紧：不注入 audit 即内建默认 AuditLogger；连默认都不可用则 guard()/resolve() 直接拒绝（fail-closed）。
        🔴 据此须同步修正 spec 与 `3-治理与合规/OEM数据隔离规范.md` §3.2 的冲突表述。
        ⚠️ 本项命中 openspec 门槛③（改对外语义）⇒ 实现属另立行，本泳道只落 spec 与文档，不改 router 代码。
  D5=(a) 采纳「校验入口未建成前通用库只读」：已有内容不受影响，仅禁新写入。

做什么：
1. 按五点更新 `design.md` 与 `specs/platform-oem-isolation/spec.md`，逐条标注裁决人与日期。
2. 跑 `openspec validate --strict`，须绿。
3. 🔴 立两条新队列行（走编辑锁 `acquire --reserve 2 --section 一 --domain 业`）：
   ⑴ D1 连带项「L3 本体这一轮还建不建」；⑵ D2 收紧的实现行（改 OEMRouter，命中门槛③）。
   ⚠️ 若 WIP 阻断，按既有惯例把新行**状态列**正文以 🛑 起首标「排队中·暂非可动」——
   🔴 判据在**状态**格不在任务格（2026-09-02 Cowork 实撞：加在任务格无效，`_count_mechanism_wip` 读状态格）。
4. `/opsx:apply` 后按「完工即归档纪律」判断能否 `/opsx:archive`；不能则按三条入口之一留机器认得的说明。
5. 收工：§二 批次登记，队列 §一 #374 回写。

不做什么：
- 🔴 不改 `router.py` 或任何隔离层实现代码（#374 边界，D2 属另立行）
- 🔴 不改全景规划／实施计划／权威排期表
```

### A2 · FI3 前置 · 到期口径改判后续跑

粘贴端：CC ｜ 泳道：fi3-prep-2

```
[OP-0902-B2]【CC】FI3到期改判收口
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902b2-fi3-duedate`）｜ worktree：☑（fi3-prep-2，新 worktree，收工自删）｜ 工作区：无（队列与文档，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_60
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902B2-FI3改判。🔴 子任务跳过本行；无头 session 里该工具不存在即跳过。
心跳：`reports/lane-heartbeat/B-0902_60-fi3-prep-2.md`，规则同 A1。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_60 --wave 1 --lane fi3-prep-2 --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #373 全文 → ② §一 #339（🔴 **只读参考，不得触碰该行**）。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-02 裁决（原文）：
  #373 到期日改判为 2026-10，与 §一 #339 对齐。
  理由＝其剩余两项物理上均以「FI3 走 openspec propose」为闸，而 propose 属 #339 触碰区
  （[S:timed=2026-10-01]、明写到期前不得催办与提前深读）⇒ 9 月上旬是个交不出来的日期，
  留着只会每周报一次假逾期。🔴 不破例提前触碰 #339。

做什么：
1. 改 §一 #373 的到期列为 2026-10，并在状态列写明本次改判依据、拍板人与日期（走编辑锁 `edit-row`，勿裸改）。
2. 🔴 逐项判：#373 剩余项里**是否存在不依赖 propose、能在 9 月内独立完成的部分**？
   有 ⇒ 照做不等，本泳道内完成；无 ⇒ 在状态列如实写明「全部依赖 propose，10 月起做」。
   ⚠️ 判据要写出证据（引行内原文），不得只下结论。
3. 收工：§二 批次登记，心跳 DONE 行写明产出落点。

不做什么：
- 🔴 不碰 §一 #339（那行明写「到期前巡检不得催办、也不必提前深读」）
- 🔴 不部署、不改真实数据
```

### A3 · ATP 立项按 D3 逐字化更新

粘贴端：CC ｜ 泳道：sc8-kitdate-2

```
[OP-0902-B3]【CC】ATP逐字化更新
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902b3-atp-literal`）｜ worktree：☑（sc8-kitdate-2，新 worktree，收工自删）｜ 工作区：无（openspec design/spec，🔴 不进实现、不部署）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_60
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902B3-ATP逐字化。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0902_60-sc8-kitdate-2.md`，规则同 A1。🔴 本泳道两段（B3→B4），同一心跳文件。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_60 --wave 1 --lane sc8-kitdate-2 --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① `openspec/changes/sc8-atp-batch2-closeout/design.md` → ② 队列 §一 #118 → ③ §四 #65。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-02 裁决 D3 = (a) 逐字化（原文）：
  严格按姚祖怡（男）2026-08-18 书面文本实现规则2。
  🔴 他已知悉并接受代价：24 行齐料日提前 18~79 天、结论整体变乐观，
     方向与 #344／#401 两轮的「变保守」相反。
  ⇒ 三条缓释必须做到：
     ① 反例单测锁死逐字化前后的差异（关掉修复即失败的退化守卫，不是只测 happy path）；
     ② 出一份「改判前后 24 行逐行对照表」落 reports/ 供事后追溯；
     ③ design 中逐字引用那三条规则原文并标注来源与日期。
  另两项须姚祖怡本人答（三个月边界开闭、三级判据是否即优先级表最终形态）：
     其串行闸锁着（采购部#20 在途）⇒ 🔴 不得起草新信，登记为「待闸开后并进下一封」。

做什么：
1. 按 D3 更新 design 与 spec，落实上述三条缓释（① 单测可先写进 tasks，本泳道不进实现）。
2. 跑 `openspec validate --strict`，须绿。
3. 出「改判前后 24 行逐行对照表」落 `reports/`（这是缓释②，不可省）。
4. 收工：§二 批次登记，§一 #118 回写；两项待姚祖怡答的登记为「待闸开后并进下一封」。

不做什么：
- 🔴 不进实现（本项到 design/spec 为止）
- 🔴 不发任何信
```

### A4 · 齐料日期判例包成文（依赖已解除）

粘贴端：CC ｜ 泳道：sc8-kitdate-2

```
[OP-0902-B4]【CC】齐料判例包成文
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902b4-kitdate-cases`）｜ worktree：☑（kitdate-cases-2，新 worktree，收工自删）｜ 工作区：无（内部件成文，🔴 不发送、不部署）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_60
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902B4-判例包。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0902_60-sc8-kitdate-2.md`（与 B3 同文件，接着追加）。
🔴 本项排在 B3 之后：开工先跑 `python 0-学习与工具/工具-泳道看护状态机.py show --lane sc8-kitdate-2`，
若显示 paused，立即以 `OPENER_PARTIAL: 依赖 B3 未解除` 收尾，不得盲目往下做。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_60 --wave 1 --lane sc8-kitdate-2 --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #344 → ② `6-人才与组织/需求确认方式升级-判例批改法与微会机制-2026-07-25.md` → ③ `6-人才与组织/CLAUDE.md` §一/§5.2。本件为 A 类，直接开工。

🔴 前置已解除：D3 = (a) 逐字化已由 Shao Peishen 2026-09-02 拍板（见 B3），
  判例包各条「拟改判定」列的取值来源至此确定，可以成文。
🔑 上一轮 A6 留下的判据（照此执行）：「排在后面」与「依赖前一项」是两件事——
  本项与 B3 落在同一取值函数上，若在 D3 未定时先起草，产出的会是一份需整体重写的判例包，
  而它一旦发出就占掉姚祖怡的串行闸、且判据类永不默认生效 ⇒ 返工成本由他承担而非我方。

做什么（🔴 只成文，绝不发送）：
1. 按判例批改法出判例包：≤10 条**真实**案例，每条「现状判定 vs 拟改判定」对照，
   收信人只做 ✅对／❌错／✏️改判 ＋ 一句话。🔴 禁止抽象设问。
2. 🔴 与 D3 逐字化结论对齐——#344 现状取最早、姚祖怡规则3 要求取更晚，方向相反，
   件内必须显式写清最终口径，不得让收信人替我方调和内部不一致。
3. 判据/口径类 ⇒ 🔴 **永不默认生效**（IATF 显式签认红线），件内不得出现「48h 默认生效」类措辞。
4. 称呼一律全名「姚祖怡」，不写「姚工」（同姓混淆）。
   🔴 成文后全文搜「他／她／他的／她的」逐个回 `6-人才与组织/人员名录-称谓与性别-正本.md` 核对
   —— **姚祖怡是男性**。
5. 收工：§二 批次登记，§一 #344 回写「已成文、待闸开后发」；
   README 登记行发送状态列**只能写 `⏳ 待你审`**，不得写终态。

不做什么：
- 🔴 不发送、不进入送审发送流程（姚祖怡闸锁着，采购部#20 在途；发送属 🔴 档永不代办）
```

### A5 · `#463` 采购与财务域八场景立行与排期（销售·研发域暂缓）

粘贴端：CC ｜ 泳道：scenario-rows

```
[OP-0902-B5]【CC】463场景立行
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0902b5-scenario-rows`）｜ worktree：☑（scenario-rows，新 worktree，收工自删）｜ 工作区：无（队列行与目录骨架，🔴 不写场景实现、不部署、不改规划文档）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0902_60
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0902B5-463立行。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0902_60-scenario-rows.md`，规则同 A1。🔴 本项持锁时间长，务必按里程碑勤写心跳。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0902_60 --wave 2 --lane scenario-rows --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #463 全文 → ② `1-转型规划/0-全景路线图/跨场景前置数据与知识库任务总表`（判据源，文件名以台账为准）→ ③ 全景规划 §加速启动总览排期表（🔴 **只读，不改**）。本件为 A 类，直接开工。

🔴 立行判据（Shao Peishen 2026-09-02 定，与常规排期不同，务必照此执行）：
  **不管规划时间表、能开尽开；唯一门槛是前置依赖**（判据源＝跨场景前置数据与知识库任务总表）。
  ⇒ 销售域 6 个与财务 5 个虽排在 2027，现在就可进候选。
  🔴 SC4 最紧：权威排期表写 2026-10（下个月）而三处皆无 ⇒ **优先立行**。

🔴 本批范围（Shao Peishen 2026-09-02 当场收窄，原话「先做质量财务采购域场景，研发和销售域场景依然暂缓等我通知再排序」）：
  ✅ **本批立行 8 个**：采购域 SC4／SC10／SC11 ＋ 财务域 FI5／FI6／FI8／FI9／FI10。
  🛑 **销售域 6 个（S1 客户跟进／S2 拜访助手／S3 RFQ／S4 技术文档／S5 竞品监控／S6 销售预测）本批一行不立**
     —— 等他通知再排序。🔴 **不要立成 🛑 排队行，是根本不立**：立了就会进 WIP 计数、进巡检视野，
     与「等我通知再排序」不是一回事。在 §一 #463 回写里如实记「销售域 6 个按他 2026-09-02 指令暂缓、未立行」。
  ⚠️ 质量域：#463 的十四个里**不含质量域**（Q 系列全在那 11 个灰区中）⇒ 本批无质量域可立，
     如实登记、不硬凑、不去灰区里翻。

做什么：
1. 🔴 先查前置依赖，逐场景给结论：对每个场景，从前置总表读出它的前置项，判「前置是否已满足」。
   **未满足的照样立行，但状态列写明卡在哪个前置**（他要的是能开尽开，不是只立能开的）。
2. 一次 `acquire --reserve N --section 一 --domain 业` 预留号（N＝本次实际要立的行数），
   逐行 `append-row`。🔴 **不要分多次 acquire**——`--reserve` 在持锁期间用不了（2026-08-27 实证）。
3. 每行必须写清：场景标的一句话／权威排期表上的原排期／前置依赖及其满足情况／触碰区／立行依据（#463 + 他的判据原话）。
4. ⚠️ 若业务类 WIP 有上限且被阻断：把优先级靠后的行**状态列**正文以 🛑 起首标「排队中·暂非可动」
   —— 🔴 判据在**状态**格不在任务格（2026-09-02 实撞，`_count_mechanism_wip` 读状态格）。
   🔴 SC4 无论如何不得标 🛑（下月即到期）。
5. 收工：§二 批次登记，§一 #463 回写（列明立了哪些行、哪些被 🛑、各自原因）。

不做什么：
- 🔴 不建任何场景的工程目录与代码（本行只管「立行与排期」，各场景立行后按 §5 场景固定流程各走各的）
- 🔴 不改全景规划／实施计划／权威排期表（那属全景路线图线；如需改排期，出移交单）
- 🔴 不预判那 11 个灰区场景（O3/O4/Q4/FI3/FI4/FI7/R1-R5）——待 #462 扫描器把判据收紧后重扫
```

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0902_60` 现取，须含：每泳道一行 ＋ 心跳全文 ＋ D6「本批停 N 次｜逐次：泳道／档位／停在什么动作／他答了什么／等了多久」＋「本批转出 N 项」＋「待回内网」清单。
