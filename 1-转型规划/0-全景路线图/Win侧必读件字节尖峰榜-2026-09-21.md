# Win 侧必读件字节尖峰榜 —— 2026-09-21（队列 §一 `#629`／`#630` 吸收项）

> 目的：分清「CC 按 `paths:` frontmatter 自动注入的真常驻」与「skill 被触发词命中时才整份读入的一读一整份尖峰」——两者对"开场底噪"的影响完全不同（真常驻是每轮请求都背的税，尖峰只在触发那一刻背一次），不能用同一把尺子判断"要不要瘦"。
> 现取命令：`wc -c CLAUDE.md .claude/rules/*.md` ＋ `find 0-学习与工具/skills源码 -name SKILL.md -exec wc -c {} \;`（本表数字为该命令 2026-09-21 现取值，非估算）。

## 一、真常驻（CC 每次开工，命中 `paths:` 即自动注入，不必显式触发）

| 载体 | 字节数 | 阈值 | 状态 |
|---|---:|---:|---|
| `CLAUDE.md`（根） | 12,146 B | 12,288 B | ✅ 余 142 B |
| `.claude/rules/两桌同步与取证.md` | 9,988 B | 8,192 B | 🔴 超 1,796 B（瘦身进行中，见下） |
| `.claude/rules/队列与落库.md` | 7,755 B | 8,192 B | ✅ 余 437 B |
| `.claude/rules/环境保障线.md` | 7,885 B | 8,192 B | ✅ 余 307 B |
| `.claude/rules/场景建造与合规.md` | 5,966 B | 8,192 B | ✅ 余 2,226 B |
| `.claude/rules/跟进信与专员.md` | 5,231 B | 8,192 B | ✅ 余 2,961 B |
| `.claude/rules/文档与全景治理.md` | 4,243 B | 8,192 B | ✅ 余 3,949 B |
| **`.claude/rules/` 合计** | **41,068 B** | **30,720 B** | 🔴 超 10,348 B（瘦身进行中，见下） |

🔑 **判别一句**：这一档的字节数直接计入"每轮请求都会看到"的开场底噪——它不是"读不读得起"的问题，是"读多少次"的问题，一次超限就是每轮都超限。

## 二、一读一整份的尖峰（skill 触发词命中时才整份读入，未触发时零开销）

| skill | `SKILL.md` 字节数 | 触发词 | 备注 |
|---|---:|---|---|
| `zhuopin-lane-watch` | 45,119 B | 「开启泳道看护」／「offlan清池」 | 队列 `#629` 瘦身目标对象，见下 |
| `zhuopin-send-followup` | 19,145 B | 「给XX写跟进信」等 | |
| `huijian-chaijian-patrol` | 18,771 B | | |
| `zhuopin-需求grill` | 15,268 B | | |
| `zhuopin-queue-audit` | 14,320 B | 值周巡检开跑前 | |
| `zhuopin-rebaseline` | 9,039 B | 全景路线图重组循环 | |
| `zhuopin-kickoff-prompt` | 6,870 B | 转场判据五条 | |
| `zhuopin-lane-clearpool` | 4,427 B | 已退休、职能并入 lane-watch | |
| `zhuopin-lan-closeout` | 4,114 B | 「我已回Lan」 | `.51` 收口正本 |

🔑 **判别一句**：这一档只在**显式说出触发词**那一次会话里整份进入上下文，不触发就是零——`zhuopin-lane-watch` 虽然单份最大，但它不是"每轮都背的税"，是"每次开泳道看护批才背一次的税"；真正每轮都背的税只有第一档表。**两档不可用同一个阈值治理**：第一档超限用 sweep reject-consumer 拦（见下），第二档超限只影响触发它的那一次会话，用途量惯例判断是否值得瘦（`zhuopin-lane-watch` 因为"泳道看护批"本身高频，才值得同批处理）。

## 三、体量守（同批新增的机器消费者）

`0-学习与工具/工具-落库sweep.py`：
- 第 4 类常驻状态告警（`_check_claude_md_carrier_size`）现已把 `zhuopin-lane-watch/SKILL.md`（阈值 `LANE_WATCH_SKILL_BYTE_CAP = 24,576 B`）并入巡检目标（`_claude_md_targets`），每轮回显、零命中也不省略——与 root/scene/rules 同一套骨架。
- 新增 reject-consumer（`claude_md_rules_over_cap` / `batch_touches_claude_md_rules` ＋ `lane_watch_skill_over_cap` / `batch_touches_lane_watch_skill`），与 `#583` 根 `CLAUDE.md` 那道闸同构地接入批处理主循环：文件清单碰到超限载体 ⇒ 该批次拒绝落库、同轮其它批次不受影响。**这就是"有消费者"的落点**——此前 rules/ 与该 skill 正本只进第 4 类告警（只报不拦），超限十几天无人知道；此后碰到即挡。
- 单测：`test_工具-落库sweep.py::CarrierRejectConsumerTests`（11 条，覆盖零超限返回空 dict 而非 None、单份／合计超限判定、`batch_touches_*` 路径匹配、skill 文件缺失时不误判超限、`_claude_md_targets` 接线）。
