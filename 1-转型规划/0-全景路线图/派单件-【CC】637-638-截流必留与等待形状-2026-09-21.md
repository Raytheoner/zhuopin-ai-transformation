---
title: 派单件 · 【CC】`#637`＋`#638` 同批（输出截流必留 ＋ 看护等待形状机器守）
created: 2026-09-21
派出线: 环境总线 Win-0921-A
批次: B-0921_止血
任务类: A 类
---

# 派单件 · 【CC】`#637`＋`#638`（§一 #637／#638）

🔴 **两行同批、同一 session 做完，判据可复用，不许分两次**（Shao Peishen 2026-09-21 已裁定）。
🔴 **先现取两行原文**：`python 0-学习与工具/工具-队列查询.py --row 637` 与 `--row 638`。**只跑这两条命令读行，禁裸 `Read`／`grep` 队列真身**（一行可达 78 KB）。本件不复述行内容，只给现取事实与边界。

## 〇、方案闸（已过，照做即可，不得中途换做法）

1. **判据粒度**：只读结论行——pytest 只看 `FAILED` 行与末行汇总，体量只看字节数。**禁 `cat` 日志／补丁正文**；要细节就 `Select-String` 定位后只读 ±40 行。
2. **narrow**：回归只跑受影响测试类 node-id（`文件::类`），**禁 `-k`**，禁整份回归。测试输出一律重定向到文件、只读结论行。
3. **禁起后台子任务／并行 Agent**——2026-09-21 `OP-0921-C` 正因此被 claude 自身 **600 秒上限**掐断，一棒白跑。**顺序、inline 做完。**
4. **产出落点**：改动留本泳道分支；不 ff master。

## 一、`#637` — `_输出截流.py` 把「零命中也回显」行裁掉（真生产 bug）

**现取事实**（免得你再找）：`signal_prefixes = ("⚠", "✗", "🔴", "🟡")`，先把这四种前缀起首的行塞进 `body`，上限 `SUMMARY_LINE_CAP = 19`，不足 19 才按原文顺序补普通行、**补满即停**。「孤儿升格 §四 扫描」那行以 **🧭** 起首、不在信号前缀集内，只能走补齐路径且排位靠后，19 个名额轮到它之前就用完 ⇒ **被挤掉**。
**后果**：「没打印」与「没跑」在屏幕上一模一样，而那行零命中回显正是判断「这一类到底跑没跑」的唯一依据。
**关联红**：`test_工具-落库sweep.py::OrphanSectionFourEscalationTests::test_echo_prints_even_when_nothing_to_escalate` —— 这是 `#632` 十六条里唯一判「真失败」、留到本棒修的那一条。

🔴 **修法方向已裁定，不要再选**：**(b) 调用方显式 must-keep 标记**——每一处「零命中也回显」的 `log.append` 显式声明「这行不许被摘要裁」，把约束钉在源头，不靠前缀猜测。
🔴 **必须过一遍同族全部调用点**（第 4／6／7／9／10／11／19 类等「发现即回显、零命中也不省略」的常驻类），它们现在只是**排位靠前才侥幸没犯**，本质同样脆弱——只修 §四 那一条不算修完。

## 二、`#638` — 看护会话侧裸 `sleep` 轮询没有任何机器守

**现取事实**：`0-学习与工具/工具-泳道看护等待.py`（11,498 B）**2026-09-16 已合入 master**，接口 `wait --batch <批次> --max-wait <秒>`，复用状态机、只在事件时退出、退出码区分 DONE/PAUSED/NO-HEARTBEAT/TIMEOUT/MAX-WAIT、输出 ≤20 行。`#598` 期望产出⑵ 也已把 `zhuopin-lane-watch` SKILL 步骤 5 的循环入口改成调它。
**病根**：**走 SKILL 之外直接开的看护续棒线（Cowork 侧）不经过那个入口**，于是整条工具链形同不存在。2026-09-21 实证：某看护线连发两次 `Bash: sleep 585; echo waited`，每个「醒—查—再睡」循环 ＝ 2 个请求 × 当时整份上下文（约 20 万量级）⇒ 每十分钟为「什么都没发生」付约 40 万 token。

🔴 **本棒要造的是「机器守」，不是再写一条纪律**——纪律已经有了、工具已经有了，缺的是有人守着它被用。落点自定（建议形态照 `工具-opener块lint.py` 的形态守，或 sweep 常驻告警类），但**必须满足**：
1. 能判出「看护/泳道语境下出现了裸 `sleep` 等待」这一形状，并指名应改用 `工具-泳道看护等待.py wait --batch … --max-wait …`；
2. **自带消费者**（一条机器动作或一个具名承接行，两样都没有不许上线）；
3. **新增常驻告警类须同批给出沙箱夹具**（`#631` 血泪：第 19 类上线即三条红，正是夹具缺桩）。

🔴 **不要把「Cowork 发不出无头泳道」写进任何口径**——已被同日实证推翻：`工具-opener批处理执行v2.ps1 -Detach` 起的两批分别存活 87 分钟与 18 分钟并写出 `exit.txt`，其中一批的起批工具调用本身还在 60 秒被桥超时报了错。判据是 `launcher.json` 的 pid ＋ `exit.txt`，不是工具调用有没有返回。

## 三、期望产出（缺一个就不许写 `OPENER_DONE`）

1. `_输出截流.py` 的 must-keep 机制 ＋ 同族全部「零命中也回显」调用点已逐点接上（列出改了哪几类）
2. `test_工具-落库sweep.py::OrphanSectionFourEscalationTests` 转绿，narrow 结论行贴出
3. `#638` 的机器守落地 ＋ 消费者 ＋ 沙箱夹具 ＋ 单测绿
4. 收工贴 `git diff --name-only master...HEAD` 原样输出

## 四、不做什么

1. 🔴 禁起后台子任务／并行 Agent（见 §〇.3）。
2. 🔴 **禁止为把测试跑绿而放松一道已经正确拒绝过的闸**（2026-09-20 血教训②）。
3. 🔴 别往 `.claude/rules/` 与根 `CLAUDE.md` 加字——两处 2026-09-21 刚缩编到线内（rules 合计 30,310／30,720，根 12,077／12,288）。
4. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
5. 🔴 收工只 push 本泳道分支，**不碰主仓、不 ff master**；红线三档（🔴／⏭️／🟡）照旧永不代办。
