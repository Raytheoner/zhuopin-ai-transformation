---
title: "派单件 · #504 ff ＋ check-heartbeat 缺 --no-notify 补丁（2026-09-09）"
created: 2026-09-09
status: 待派出
派出线: Cowork 业务总线 OP-0908-V
承接: 队列 §一 `#504`（ff）＋ 本件立的接线补丁（并入同一棒，不新立行）
授权: Shao Peishen 2026-09-09 答 `1a，2a`
---

# 派单件 · `#504` ff ＋ `check-heartbeat` 补 `--no-notify`

## 〇、本棒成因（一句话）

`OP-0909-M` 已跑完收工、3 笔已 push 但**未 ff**；它在跑的过程中发现了一条「纪律写死、机器不支持」的缺口（`check-heartbeat` 没有 `--no-notify`）却没修，只留了一个 suggested task。**本棒把这两件一起收掉**——顺序不可颠倒。

## 一、🔴 三条订正，起棒前必读（【设置】行与任务实质冲突处，一律以本件为准）

### ⑴ 分支字段以本段为准：**不起新分支，直接在 master 上做**

opener【设置】行会写「从 master 起 `claude/op0909p-…`」，那是生成器的固定写法，**本棒不适用**——一旦 `checkout -b`，主 checkout 就离开了 master，随后 ff 会落到那条新分支上、**master 原地不动，等于白做**。本棒三个动作都不需要独立分支：**ff（在 master 上）／改两行接线（在 master 上）／回写队列（走编辑锁，也在 master 上）**。

> ⚠️ **本坑今天第 2 次**（第 1 次＝ `OP-0909-K` LAN 收口棒，看护件 §三 已同款订正）。成因同源：**写进文本的设置不会自己适配当前任务**。

### ⑵ worktree ＝ ☐ 不勾

要 `git merge --ff-only` **进主仓 master**，而 **worktree 里 ff 不到 master**。

### ⑶ 🔴 顺序不可颠倒：**先 ff，再改接线**

`claude/op0909m-heartbeat-vis-apply` 在 `0-学习与工具/工具-泳道看护状态机.py` 上有 **+242/-3** 行改动尚未进 master。**先在 master 上改接线，再 ff ⇒ 撞冲突**；先 ff 再改则天然安全。

> 🔑 这条判据与 `#462` 同源：**判「该在哪个版本上动手」，看的是产物在不在 master，不是当前 checkout 上看到了什么。**（2026-09-09 值周 `OP-0908-V` 差点在 master 上直接改，现取分支才发现）

## 二、现取事实（2026-09-09 15:0x，`git -c core.fsmonitor= --no-optional-locks` 取）

| 项 | 实测值 |
|---|---|
| `master..claude/op0909m-heartbeat-vis-apply` | **3**（`9464fe1` apply ／ `0bd577e` SKILL 缩进 ／ `548741e` tasks 6.3＋队列回写） |
| `分支..master` | 1 ⇒ **可 ff-only** |
| 该分支对本文件的改动 | `0-学习与工具/工具-泳道看护状态机.py` **+242 −3** |
| `--no-notify` 缺口 | 🔴 **master 与 M 分支上都存在**（M 只发现、未修） |
| M 的 worktree | `.claude/worktrees/agent-ab4afe28a7709951c`，**仍 locked**（收工未自删） |

## 三、缺口详情（`--no-notify`）

| 核点 | 行号（master 版） | 实测 |
|---|---|---|
| `check_heartbeat()` 签名 | 908–912 | ✅ **已支持** `notify_fn: Optional[Callable[[str], None]] = None` |
| `_cmd_check_heartbeat` | 1250–1254 | ❌ 调用时**不传** `notify_fn` ⇒ 走 `_notify_best_effort` 真实企微推送 |
| `p_hb` argparse | 约 1362 起 | ❌ **无** `--no-notify` |
| 可照抄的同款模式 | `p_pause` 1356 ／ `_cmd_pause` 1142 ／ `_cmd_transfer` 1207 | `notify_fn = (lambda _msg: None) if args.no_notify else None` |

**⇒ 被调函数早已准备好，缺的只是两处接线。**

**为什么必须修**：`skills源码/zhuopin-lane-watch/SKILL.md`「推送目标」节写死「🔴 所有 `pause` / `check-heartbeat` 一律带 `--no-notify`，无一例外」（成因＝2026-09-02 首跑时一条决策提示误发进 17 人的「跨部门AI建设群」，含内部技术细节、**无法撤回**）。照 SKILL 写 ⇒ `argparse` 直接 `exit 2`；不写 ⇒ 心跳超时时真推那个群。**纪律在机器上无法遵守。**

## 四、🔴 回归验证的红线（本件立，起棒前必读）

**禁止用「真跑一次不带 `--no-notify` 的 `check-heartbeat`」来验证默认行为未变。**

那正是把事故本身当成测试用例——默认路径的行为就是**真推那个 17 人的群，且不可撤回**。

**替代验证（二选一，都不产生真实推送）**：

- **(甲) 桩验证（推荐）**：`monkeypatch` 掉 `_notify_best_effort`（或 `check_heartbeat` 实际调用的那个推送函数），断言 ⑴ 不带 `--no-notify` 时它被调用一次；⑵ 带 `--no-notify` 时零调用。
- (乙) 纯解析级断言：只断言 `parser.parse_args([...])` 解出的 `no_notify` 取值，与 `notify_fn` 是否为 `None` 的分支判断——不进入 `check_heartbeat` 函数体。

> ⚠️ **本条是本件起草期自查逮到的**：Cowork 总线 `OP-0908-V` 起草的第一版指令里写了「再跑一次不带 `--no-notify` 的 `--json` 确认默认行为未变」，**那一跑就会真推群**。收回并改写成本节。**判据：验证一个「会造成不可撤回后果」的默认路径时，不得用真跑该路径的方式验证。**

## 五、做什么（严格按序）

1. 🔴 **先 ff**：`git merge --ff-only claude/op0909m-heartbeat-vis-apply`（在 master 上，3 笔）。ff 后**现查**产物已在 master（`git log --oneline -3`），不看分支。
2. **补接线两处**（照 §三 表里的同款模式，不自创写法）：
   - `p_hb` 加 `--no-notify`，`action="store_true"`，help 文案与 `p_pause` 第 1356 行**逐字一致**
   - `_cmd_check_heartbeat` 加 `notify_fn = (lambda _msg: None) if args.no_notify else None` 并传进 `check_heartbeat(...)`
3. 🔴 **按 §四 验证**（桩或解析级，二选一），并把用了哪一种、断言了什么写进回写正文——只有动作没有手段的验证声明 ＝ 没有验证。
4. **回写 §一 `#504`**：ff 已落地（写 master 上的 SHA）＋ 本接线补丁已随棒完成；回写后**回读核字节数**（K2 上限 4096 B，超出即外置到 `1-转型规划/0-全景路线图/队列行日志/#504.md`）。
5. **登记 §二 批次**；🔴 文件清单每个路径必须包**反引号**，且反引号内**只放仓库根相对路径**——裸路径 sweep 提取不到会静默掉在地上（2026-09-08 实撞），非路径的 OP 号放反引号里会被写侧守卫拒收（2026-09-09 实撞）。
6. **顺手一件卫生**：`git worktree remove --force .claude/worktrees/agent-ab4afe28a7709951c`（M 收工未自删的 locked worktree；`git worktree unlock` 后再 remove）。移除前先确认其中无未提交改动。

## 六、不做什么

- 🔴 **不真跑不带 `--no-notify` 的 `check-heartbeat`**（§四 红线）——那一跑会真推 17 人的跨部门群且不可撤回
- 🔴 **不改 `skills源码/zhuopin-lane-watch/SKILL.md` 那条红线**——纪律是对的、是代码没跟上；反向松纪律等于把 2026-09-02 事故的成因原样留着
- 🔴 **不起新分支、不建 worktree**（§一 ⑴⑵）；不改 `check_heartbeat()` 函数体与 `HEARTBEAT_STALE_MINUTES_DEFAULT`，本棒只接线、不改看门狗语义
- 不碰 `.51`／企微机器人配置／定时任务；不碰在跑行 `#462`／`#506`／`#419`（Shao Peishen 2026-09-09 明令只报不改）
- 不自行 `commit`（由落库 sweep 从 §二 取活）；撞 `.git/index.lock` 不重试、不自造豁免，按「登记待补」写进队列行

## 七、收工

回写 §一 `#504` ＋ 登记 §二 批次（走 `0-学习与工具/工具-共享文档编辑锁.py`，勿裸改）。收工报一行：**ff 的 SHA ／ 接线两处的最终行号 ／ §四 用了哪种验证及断言结果 ／ worktree 是否已清**。
