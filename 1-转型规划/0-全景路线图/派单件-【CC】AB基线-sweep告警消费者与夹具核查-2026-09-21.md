---
title: 派单件 · 【CC】A/B 验收用活 · 逐类核 sweep 常驻告警的消费者与夹具
created: 2026-09-21
派出线: 环境总线 Win-0921-A
批次: B-0921_AB基线
任务类: A 类
---

# 派单件 · 【CC】逐类核 sweep 常驻告警的消费者与夹具（A/B 验收用活）

## 〇、本件的双重身份（先说清，免得你误判范围）

⑴ **它是一件真活**：`0-学习与工具/工具-落库sweep.py` 里的常驻状态告警已建到第 19 类，但「新建告警须自带消费者」「新增常驻告警类须同批给出沙箱夹具」这两条纪律**从未被逐类回头核过**。`#631`（第 19 类上线即三条红＝夹具缺桩）与 `#637`（第 4/6/7/9/10/11 类只是排位靠前才侥幸没被摘要挤掉）都是这个缺口的实例。
⑵ **它同时是 A/B 验收的载荷**：本件会被跑两遍（Run A／Run B），用来量「重复调用去重守」的真实收益。**所以你只管把活做好，不要为省 token 而改变做法**——现状口径就是基线，基线要真。

🔴 **纯读活**：除了产出那一份清单，**不许改任何既有文件**。

## 一、方案闸（已过）

1. **判据粒度**：定位读，不整读。`工具-落库sweep.py` 555,367 B、`test_工具-落库sweep.py` 534,849 B，**两份都不许整读**；一律 `Select-String`／`grep -n` 定位后只读 ±40 行。
2. **narrow**：本棒不跑任何回归（纯读活，无代码改动）。
3. **禁起后台子任务／并行 Agent**（`OP-0921-C` 被 claude 自身 600 秒上限掐断过一棒）。顺序、inline 做完。
4. **产出落点**：`1-转型规划/0-全景路线图/核查-sweep告警消费者与夹具-<RUNTAG>.md`，`<RUNTAG>` 见 §三。

## 二、做什么

逐类核 `工具-落库sweep.py` 的**每一类常驻状态告警**（第 1 类到第 19 类，实际类号以现取为准，不要照抄本件的「19」），每类一行，四列写死：

| 列 | 判据 |
|---|---|
| **类号与名称** | 现取，带该类实现所在行号 |
| **触发条件** | 一句话，什么情况下会出这条告警 |
| **消费者** | 🔴 判据＝「一条机器动作」或「一个具名承接行（队列 §一/§四 #N）」，**两样都没有即判「无消费者」**。不许把「每轮回显」本身当消费者。 |
| **沙箱夹具** | 该类在 `test_工具-落库sweep.py` 里有没有对应的桩／夹具；有就写测试类名，没有就写「缺」 |

再加两节：

- **§二 无消费者清单**：把上表判「无消费者」的逐条列出，每条给一句「该配什么消费者」的建议（只建议，不动手）。
- **§三 夹具缺口清单**：把判「缺」的逐条列出，标明它落地时会不会像 `#631` 那样上线即红。

## 三、`<RUNTAG>` 与自陈（A/B 用，必须照填）

收工报告里**必须**自陈这三行，缺一行即判 `OPENER_PARTIAL`：

1. `RUNTAG=` 本棒的 opener 编号（形如 `op0921x`），产出文件名用它。
2. `工具调用自计数=` 你本棒一共发了多少次工具调用（自己数，看护者会用 transcript 机器核，不一致会被点名）。
3. `重复自陈=` 你有没有出现「同一文件重复 Read」「同一命令重复 Bash」；有就写几次，没有就写 0。

🔴 **不要为了让这三个数字好看而改变做法**——基线要真，虚报会被 transcript 机器核当场打穿。

## 四、期望产出（缺一个不许写 `OPENER_DONE`）

1. `1-转型规划/0-全景路线图/核查-sweep告警消费者与夹具-<RUNTAG>.md`（逐类一行，无空白判定；含 §二／§三 两节）
2. 收工报告含 §三 那三行自陈
3. 收工贴 `git diff --name-only master...HEAD`（**应当只有上面那一份新文件**；多出任何文件即说明违反了「纯读活」）

## 五、不做什么

1. 🔴 **不改任何既有文件**，不补夹具、不加消费者——本棒只出清单，实施另棒。
2. 🔴 不整读 `工具-落库sweep.py`／`test_工具-落库sweep.py`／`进度编年-CHANGELOG.md`。
3. 🔴 不碰 `.claude/rules/`、不碰根 `CLAUDE.md`、不碰 `.claude/settings.json`。
4. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
5. 🔴 收工只 push 本泳道分支，不碰主仓、不 ff master；红线三档（🔴／⏭️／🟡）照旧永不代办。
