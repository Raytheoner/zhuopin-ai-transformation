---
title: "【CC】开场 prompt · 队列 #250 · FI2 面板 6 个显示问题（唐燕萍 2026-08-04 回件）"
created: 2026-08-05
执行方: CC 财务
执行环境: CC
来源: 队列 #250；源＝唐燕萍 2026-08-04 回件（财务部#10 的回复）；本派单件由业务总线 2026-08-05 备
status: 待执行
---

# 【CC】开场 prompt · #250 FI2 面板 6 个显示问题

## 【开场词（复制即用）】

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑ ｜ 工作区：企业AI转型

读 1-转型规划/0-全景路线图/开场prompt-【CC】250-FI2面板6个显示问题-交接.md ＋ CLAUDE.md 当前进度恢复上下文，按文件执行。六项均为展示层问题，判定引擎零改动是本次红线。
```

---

## 一、来龙去脉（30 秒）

FI2 面板 v8（她 07-31 的权威规格书驱动重建）＋ D19 真实数据接入（08-03 交付、08-04 发信）之后，唐燕萍用我们给的 **6 个真实 AP 单号**在 `u9c` 模式跑完第一轮，回件原话：

> **判定逻辑结果与预期一致**，但面板界面有以下 6 个显示问题需要修改（**均为显示问题，不涉及判定逻辑**）。
> 以上 6 个问题修改完成后，**我可以再跑一轮验证**。

**⇒ 引擎判得对，是"看不清/看错列"。她在等这一件。**

口径真身（先自行读一遍，不要只信本文件转述）：
`7-外部文档/财务部/财务部-tangyanping-回复-2026-08-04-财务部-唐燕萍-跟进-2026-08-03-FI2真实数据接入轮1请复核-回复-f922271d2c5ad5bf9e9192f29ed02fbc.docx`

---

## 二、六项（她的原文，逐条已核对，未加工）

| # | 位置 | 现状 | 修改要求 |
|---|---|---|---|
| 1 | 展开详情 → PO 采购订单卡片 | 「不含税金额」与「价税合计」**两列数值写反**。举证 `ZPCG20250902009`：面板显示 不含税金额=160,000.00、价税合计=141,592.92 | 两列对调。U9 ERP 真值＝未税 **141,592.92**／税额 **18,407.08**／价税合计 **160,000.00**（数量 4000 × 未税单价 35.39823000 ＝ 141,592.92；× 1.13 ＝ 160,000.00） |
| 2 | 展开详情 → 三单卡片「单价」字段 | PO、AP 显示 **40.00（含税）**，发票显示 **35.40（未税）**，直接比对对不上 | **PO 与 AP 的单价统一改为未税单价显示**，与发票口径一致 |
| 3 | 三单核对明细表（**收起状态**）第一列 | 「AP单号·行号」里的行号取的是 **PO 行号**（如 `AP-2026040083·行30-50`） | 改为显示 **AP 单的实际行号** |
| 4 | 展开详情 → AP 应付单卡片 → 单号行号 | 显示为 `AP-2026040083·30+`，看不到具体行号 | 显示具体行号；一张 AP 单涉及多行时用「/」隔开（`行30/40/50`） |
| 5 | 展开详情 → 发票卡片 → 发票号 | 末尾多一个「+」，三处命中：`AP-2026060073·行130`／`AP-2026070035·行180-200`／`AP-2026070071·行10-40`（如 `26312000003314325436+`），与真实发票号码不一致 | 去掉末尾「+」 |
| 6 | 展开详情 → ⑥行级映射 | 现状 `PO-ZPCG20250523005行130AP-AP-2026060073发票2631200000314325436发票26312000003314325436`：① 没有 AP 行号 ② 发票号**重复写了 2 次** ③ 各段粘连不易读 | 改**链条格式**：`PO-ZPCG20260625004 行10/行20/行30 → AP-2026070071 行10/行20/行30 → 发票号A/发票号B`（多行、多票用「/」隔开；**发票号只列一次**） |

**⚠️ 第 4、5 项的「+」很可能同源** —— 都像是"多值折叠"的省略号实现（`30+` ＝ 还有更多行；发票号尾「+」＝还有更多票）。**若确证同源，请一处修法覆盖两项，并在队列回填里写明**；若不同源，也请写明各自根因，不要只说"已修"。

---

## 三、🔴 红线（本次最容易出错的地方）

1. **只改展示层。** `match_engine.py` ／ `result_classify.py` ／ `price_check.py` ／ `recon_report.py` ／ `config.py` ／ `models.py` **零改动**——同 07-31 v8 改造先例（那次也是"信息全保留，只换看法"，只动了 `fi2/webapp.py` 一个文件）。
   - **第 2 项是本条最大的陷阱**：「PO/AP 改显示未税单价」听起来像换个字段渲染，但如果 `price_check` 的容差是按含税单价算的，改显示**不得**顺手把比对基准也改了。**先确认比对基准取的是哪一个口径，把结论写进回填**；若发现显示与比对基准本就不一致（即比对用未税、显示用含税，或反之），**如实登记为独立发现、不在本次改判定**。
2. **判定口径零漂移须有显式证明**，不是一句"没动"：mock 模式既有结构断言原样通过 ＋ 用她那 6 个真实 AP 单号跑出的 **5 自动通过／2 金额微差 L2 消化／3 BLOCK** 三个数字与 08-03 交付时完全一致。
3. **3 笔 BLOCK 继续 BLOCK 是预期行为、不是缺陷。** 她已就此选了 (b)「做已知豁免」并给了三条护栏，但明写**「变更包可在 8 月底启用评估通过后立项，不阻塞本轮。本轮判定口径不动，维持现状」** ⇒ 已登为 **#251 挂条件待领**。**本次绝不顺手做豁免**（那会动判定路径，与红线 1 直接冲突）。
4. **金额脱敏红线照旧**：展开卡片内的原始单据金额仅当次会话即时展示，**不落审计/报告持久化**（同 v8 改造边界，`build_report()` 落盘内容不得因本次而变）。

---

## 四、验证（她给的复现路径，照跑）

面板 http://192.168.100.51:8094/ → 数据源选 `u9c` → AP 单号清单粘贴：

```
AP-2026070036,AP-2026070035,AP-2026060004,AP-2026040083,AP-2026060073,AP-2026070071
```

预期：10 个真实料品，**5 自动通过 / 2 金额微差自动 L2 消化 / 3 BLOCK**。
**六项逐项目视确认**（不是"测试绿了就算"）——尤其第 1 项拿 `ZPCG20250902009` 对 U9 真值、第 5 项确认那三处发票号末尾「+」确已消失。

> **门禁提醒**：`.51` 四服务已挂共享口令门禁（队列 #160）。程序化访问用 `X-Auth-Token` 请求头；`/api/ping` 已豁免。

---

## 五、发信（CLAUDE.md §5 场景固定流程第 8 步）

改完部署收口后**当场起草**跟进信（财务部**#11**，README 表格「发送状态」列**只能写 `⏳ 待你审`**，两态语义见 `README-跟进机制与命名约定.md`；起草前须先占该 README 的编辑锁，否则结构性拦截不会触发）。

**🔴 发送硬前置三条件，缺一不得发**：

1. 改动已 ff 合入 `master`（`git rev-list --count master..<分支>` ＝ 0）；
2. `.51:8094` 已部署且冒烟通过（`/api/ping`／首页 200／一次真实 `u9c` 跑通）；
3. **用她举证的那 6 个 AP 单号端到端复现，六项逐项显示正确**——她要看的正是自己举证的那几张单，若生产仍是旧码，她看到的将与上次一模一样（#228 事故形态，同族已复发三次）。

**串行原则已核过**：唐燕萍名下前一封（财务部#10）**已回件且本次即在回灌**，故 #11 不违反串行原则。信里请一并告知：D19 判例她选的 (b) 已按三条护栏登记为 #251、待 8 月底评估通过后立项，**本轮口径未动**（回应她"不阻塞本轮"的原话）；OCR 改道一事**不在本信重复**（那条走 #249 财务域重梳，另行成信，避免一封信塞两个主题）。

---

## 六、收工段

1. 队列 **#250** 回填：状态 + 产出路径 + 六项各自根因（尤其 4/5 是否同源）+ 判定零漂移的三数字证明 + 部署留痕 + 跟进信起草状态。
2. 若第 2 项排查中发现「显示口径 vs 比对基准不一致」，**当场追加待领行，不顺手改**。
3. 列出全部新产出/修改文件清单 + commit message；CC 自行 commit + push + 收工重跑台账。
4. 收工自删 worktree（ahead=0 且 clean 时），删不净留空壳并在队列行注明。
