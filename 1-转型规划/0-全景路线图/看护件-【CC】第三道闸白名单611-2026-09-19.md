---
title: "第三道闸白名单看护件 · #611 销号判据⑶（备件，形态未拍前不起）"
created: 2026-09-19
status: 🚀 **已派出**（Shao Peishen 2026-09-19 答 `1a` ＝ 方案 D 白名单一次补齐三类；由 Cowork 泳道看护 `OP-0919-K` 以 `工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -Detach` 后台起）
产出: Cowork 泳道看护 OP-0919-K
批次: B-0919_三道闸611
源: 派单件 OP-0919-I 第 1 条；回放取证＝1-转型规划/0-全景路线图/取证件-2026-09-19-OP0919K-611回放与戊档判定.md
用途: 单泳道单段；§三 即 `-Plan` 解析格式，默认后台无头起、不给人粘。
---

# 第三道闸白名单看护件（2026-09-19）

## 〇 · 本批要解决的一句话

**第三道闸把「协议〇 要求泳道自己写的东西」判成了泄漏**——13 份有效补丁里 12 份栽在这上面，而白名单只收了 `reports/` 与 `合入登记/pending-ff.jsonl` 两项。

🔴 **本件不是按派单件原话出的**。派单件说根因是白名单漏了 `ff-patrol`，回放证伪：**只补 `ff-patrol`，13 份一条都不转绿**。真正的主因是 `#611` ⑴ 自己写着、至今未拍的那个二选一。**形态未拍 ⇒ 本件只备不起。**

## 一 · 硬边界继承（看护者读，泳道 opener 不复述）

- D1 四档判据**现取**：`python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不抄判据表。
- 本段建造面**是 🟢 档**（worktree 内本地改工具＋单测）；上游的 🟡 形态决策已由他 `1a` 拍定，无待决项。
- LAN 探针：本批全在库内，不依赖内网。
- 机制类可动 WIP 现取 **25／22**（本日 `OP-0919-K` 分诊把 `#479`／`#556` 据实改判 `blocked`，27→25），仍超上限；`#611` 是 `[D:业]` 行，不占机制类在办位。

## 二 · 次序与并行矩阵

| 段 | 泳道 | 队列行 | 触碰区 | 硬序 |
|---|---|---|---|---|
| A1 | `leakgate` | §一 `#611` | `0-学习与工具/工具-opener批处理执行v2.ps1`（L384–L419 第三道闸）｜其回归用例 | 单段，无并行依赖 |

⚠️ **与在跑批次的关系**：本段只碰 `工具-opener批处理执行v2.ps1`。`#565`／`#567` 两行同碰该文件但均未在办；起本批前须现取确认无第二条泳道在改它。

## 三 · 泳道 opener（`工具-opener批处理执行v2.ps1 -Plan` 解析段，人不碰）

🔴 **本节必须排在 `## 三bis` 之前**（`工具-opener块lint.py` 形态⑥ 结构锚）。
🔴 **`### A<N>` 标题必须带 `§一 #<行号>`**（`工具-opener派出前校验.ps1` 靠它现查队列态）。

### A1 · §一 #611 第三道闸白名单一次补齐三类

粘贴端：CC ｜ 泳道：leakgate ｜ 队列行：§一 #611 ｜ OP：OP-0919-L

```
[OP-0919-L]【CC】第三道闸611
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919l-leak-gate-611`） ｜ worktree：☑（op0919l-leak-gate-611，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-K（批 B-0919_三道闸611） ｜ 模型：sonnet
读 ① `队列 §一 #611` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 白名单一次补齐三类（方案 D）：合入登记/ff-patrol-<yyyyMMdd>.jsonl（每次 ff 必写）＋两份队列物理文件 跨桌任务队列-机制环境.md／-业务场景.md（协议〇 要求泳道收工回写）＋1-转型规划/0-全景路线图/队列行日志/#<N>.md（K2 外置件）
2. 单测覆盖该竞态：造一条「泳道回写队列后 sweep 未及提交」的场景，断言不再判 FAIL（#611 ⑵）
3. 自举验收（销号判据⑶）：用 0-学习与工具/工具-main-leak回放.py 的同一套泄漏清单回放 13 份有效 *-main-leak.patch，12 份必须转绿；唯一允许仍 FAIL 的是 20260919-075655 / q2-A1（看护者自身取证件，属换判法那一面，不在本批）

不做什么：
- 🔴 不做「判定前先跑一次 sweep」那个选项（用竞态修竞态）
- 🔴 不做「区分泳道泄漏与看护者自身产出」那一面（须换判法、形态未定，另行请示）
- 🔴 不改 工具-落库sweep.py、不碰 #565／#567 各自的行；不碰 .51／真实库／企微／定时任务
- 🔴 不 ff master、不在主仓 commit、不长持队列编辑锁
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919l-leak-gate-611 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919l-leak-gate-611 --done --batch B-0919_三道闸611 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词（仅在退回「给他一块粘」时使用；默认走 §三 无头）

本件默认后台无头起，不给他粘。若需退回手工，走 `python 0-学习与工具/工具-opener生成.py --variant reference --ref-file 1-转型规划/0-全景路线图/看护件-【CC】第三道闸白名单611-2026-09-19.md` 现出引用版，**不手抄**。

## 四 · 全程红线与收工

- 🔴 **销号判据是「整条不再 FAIL」**，不是「少了一个文件」——本批唯一的验收是 §三 A1 做什么第 3 条那份回放表。
- 🔴 **只碰第三道闸的白名单判据与其单测**；第三道闸的另外两道（⑴⑵）、`工具-落库sweep.py`、`#565`／`#567` 一律不碰。
- 收工登记 §二 批次 `B-0919_三道闸611`，回写 `#611`，顶格 `OPENER_DONE` 收尾。

## 五 · 已知会踩、但不是本批的错

- **`20260919-075655 / q2-A1` 那一份合完本批仍会判 FAIL**，这是预期内的：它的泄漏清单只有看护者自己写的取证件，**文件名任意、不可能进白名单**，要的是换判法（按泳道 worktree 实际触碰过的文件归属）。本批不做，也**不得在收工时表述为「第三道闸已根治」**。
- `20260916-204736/596`／`/599` 两份是老格式补丁（纯 `git diff`、无表头），回放器不解析，**不计入分母**，也不要求转绿。
