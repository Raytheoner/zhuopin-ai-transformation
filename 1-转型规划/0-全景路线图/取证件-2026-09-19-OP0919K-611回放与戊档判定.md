---
title: "取证件 · #611 销号判据⑶ 全量回放 ＋ #617 戊档四条逐条现取判定"
created: 2026-09-19
产出: Cowork 泳道看护 OP-0919-K（派出线 OP-0919-I，前一棒 OP-0919-A）
用途: 只记现取到的事实与判据，不代裁、不动分支、不改工具
---

# 取证件 · 2026-09-19 · OP-0919-K

## 〇 · 一句话

**派单件 `OP-0919-I` 给的根因不完整**：补 `ff-patrol-<yyyyMMdd>.jsonl` 一项，三份补丁**一条都不会转绿**——真正卡住它们的是两份队列物理文件，而那一项正是 `#611` ⑴ 里**还没定的二选一**。

## 一 · `#611` 销号判据⑶ 回放（全量，不止派单件点名的三份）

**手段**：`reports/_op0919k/replay611.py`——把 `工具-opener批处理执行v2.ps1:404-409` 的 `Where-Object` 过滤逻辑逐字复刻成 Python，喂进每份 `*-main-leak.patch` 的泄漏清单重判。原始输出存 `reports/_op0919k/replay611.out.txt`。

⚠️ **分母说明**：`reports/opener-batch/` 下现存 **15** 份 `*-main-leak.patch`，其中 `20260916-204736/596`、`/599` 两份是**老格式**（纯 `git diff`，无 `----- 文件 -----` 表头），本回放器不解析，**不计入分母**。**有效分母 ＝ 13 份。**

| 补丁（批次 / 泳道·段） | 泄漏清单 | A 现行 | B ＋ff-patrol | C ＋两份队列 | D ＋队列行日志 |
|---|---|---|---|---|---|
| `20260917-200505 / fi3-fi6-A1` | `跨桌任务队列-业务场景.md` | FAIL | FAIL | OK | OK |
| `20260917-200505 / fi5-A2` | `跨桌任务队列-业务场景.md` | FAIL | FAIL | OK | OK |
| `20260917-200505 / fi8-A4` | `跨桌任务队列-业务场景.md` | FAIL | FAIL | OK | OK |
| `20260917-221803 / q2-A1` | `队列行日志/#612.md` | FAIL | FAIL | FAIL | OK |
| `20260918-072559 / fi4-baseline-614-A2` | `跨桌任务队列-机制环境.md` | FAIL | FAIL | OK | OK |
| `20260918-072559 / fi56-audit-614-A4` | `跨桌任务队列-机制环境.md` | FAIL | FAIL | OK | OK |
| `20260918-072559 / fi7-baseline-614-A3` | `跨桌任务队列-机制环境.md` | FAIL | FAIL | OK | OK |
| `20260918-081222 / fi3-deploy-A1` | `队列行日志/#615.md` | FAIL | FAIL | FAIL | OK |
| `20260919-075655 / q2-A1` | `取证件-2026-09-19-C段分支逐条判定.md` | FAIL | FAIL | **FAIL** | **FAIL** |
| `20260919-085903 / gitguard-A3` ★ | `ff-patrol-20260919.jsonl`；`跨桌任务队列-业务场景.md` | FAIL | FAIL | OK | OK |
| `20260919-085903 / opener-A1` ★ | `跨桌任务队列-业务场景.md`；`跨桌任务队列-机制环境.md` | FAIL | FAIL | OK | OK |
| `20260919-090057 / q2-A2` ★ | `ff-patrol-20260919.jsonl`；`跨桌任务队列-业务场景.md` | FAIL | FAIL | OK | OK |
| `20260919-095859 / opener-A2` | `ff-patrol-20260919.jsonl`；`跨桌任务队列-机制环境.md` | FAIL | FAIL | OK | OK |

★ ＝ 派单件 `OP-0919-I` 点名的那三份。

**汇总**：A 仍 FAIL **13/13** ｜ B 仍 FAIL **13/13** ｜ C 仍 FAIL **3/13** ｜ D 仍 FAIL **1/13**。

白名单四套方案的定义：
- **A** ＝ 现行（`reports/`＋`合入登记/pending-ff.jsonl`，`v2.ps1:404-409`）。
- **B** ＝ A ＋ `1-转型规划/0-全景路线图/合入登记/ff-patrol-<yyyyMMdd>.jsonl`（**派单件所述的那一项**）。
- **C** ＝ B ＋ `1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md`／`-业务场景.md`（`#611` ⑴ 写着的「加进白名单」选项）。
- **D** ＝ C ＋ `1-转型规划/0-全景路线图/队列行日志/#<N>.md`（K2 外置件，协议要求泳道／看护者写）。

## 二 · 三条据实结论

**⑴ 派单件的根因判断不完整，且是可当场证伪的。** 派单件写「根因已定位到源码一处：白名单没收 `ff-patrol`」。回放显示：**只补 `ff-patrol`（方案 B），13 份里 0 份转绿**——三份点名件里有两份的第二个泄漏项是 `ff-patrol`，但**每一份都还带着一份队列文件**，而队列文件在 B 下照样非白名单。⇒ **判词：泄漏清单里补齐了一项，不等于那一条能转绿；销号判据是「整条不再 FAIL」，不是「少了一个文件」。**

**⑵ 真正的主因是 `#611` ⑴ 自己写着、但还没拍的那个二选一。** 13 份里 **12 份**的泄漏项落在「协议〇 要求泳道收工回写的文件」上——两份队列（9 份）与 `队列行日志/#N.md`（2 份，K2 外置件），加上 `ff-patrol`（4 份，每次 ff 必写）。这三类都不是泄漏，是**机制自己要求写的东西**。

**⑶ 剩下 1 份白名单永远治不了，它是另一面。** `20260919-075655 / q2-A1` 的泄漏清单只有一件 `取证件-2026-09-19-C段分支逐条判定.md`——**看护者本人同时段在主仓写的取证件**，泳道从头到尾没碰过它。取证件的文件名是任意的，**不可能进白名单**。⇒ 这一面要的是**换判法**（按泳道 worktree 实际触碰过的文件判，或按 `git log --author`／时间窗归属），不是加白名单。`#611` 行内 2026-09-19 已记下这条变体，本次回放坐实它**不被任何白名单方案覆盖**。

## 三 · 形态未定 ⇒ 本线不派建造（🟡 档）

`#611` ⑴ 原文给的是**二选一**：「**加进白名单**，或**闸在判定前先跑一次 sweep**」——**至今未拍**。派单件直接说「出看护件转 CC」，但形态没定就出件，等于本方替他拍板。按 `zhuopin-lane-watch` 的 🟡 档（改口径阈值）**停等一字母**，本次**只备件不派出**。

**本方建议（已写进「待你一字母」）**：走**方案 D 的白名单**（三类一次补齐：`ff-patrol-<yyyyMMdd>.jsonl`、两份队列物理文件、`队列行日志/#N.md`），**不走「判定前先跑 sweep」**——理由：跑 sweep 会让第三道闸依赖另一条常驻链路的时序，正是 `#611` 自己判词里那个「竞态」的成因（`fi10` 判 OK 只因 sweep 恰好赶上），**用竞态去修竞态**；白名单是纯静态判据，可被单测钉死。剩下的第 3 类（看护者自身产出）另属换判法，**不并入本批**。

## 四 · `#617` 戊档四条逐条现取判定（不自裁、不自删、一条分支未动）

**判据按 `取证件-2026-09-19-C段分支逐条判定.md` §七 的强度排序用**：① `git log master --grep=<行号>` 找同标题提交；② 取该提交新增的函数／常量名逐个 `git grep master`，**全命中才算已在**；③ `git cherry`（patch-id，rebase 后假阳，需与 ①② 互证）。🔴 未用 `git diff --shortstat`，未用关键词命中数。

| 分支 | 现取 | 判据（逐条写明用什么核的） | 本方判定 |
|---|---|---|---|
| `op0906i-dont-guard-487` | `git cherry` 1 条 `+`（`3c369a16`），内容＝两份 `.patch` 草稿＋自检结论，落 `起草件-队列487子项-dont静默丢弃/` | ① master 有**同标题提交** `241e1785 merge(队列#487子项/OP-0906-M): --dont 静默丢弃落地 (c) 甲＋精简版乙` ＋ `a28bf6fa feat(…)`；② master `opener骨架.md` 现取 **L136「不做什么：」段在**、L142／L162 明写「2026-09-06 补入（起草 `OP-0906-I`、落地 `OP-0906-M`）」「`工具-opener块lint.py` 形态⑦机器守」；③ `起草件-…` 目录在 master `git ls-tree` **零命中**（草稿本身确未合） | **放弃**——子项已由 `OP-0906-M` 以 (c) 甲＋精简版乙落地并有机器守，草稿是被超越的两版方案，合入无增量。删 ref 属 🟡 档，**待他一字母** |
| `queue-315-apply-9f2c1a` | `git cherry` 1 条 `+`（`b2ef1811`），13 文件 **+1130/−243** | ① master 有 `56664125 docs(openspec): 归档 queue-dual-file-split` ＋ `a9c20893` 明写「queue-315 那 1130 行须另行定夺」；② 分支侧新增的**实现符号共 12 个**（`_detect_shadow_copy`／`_is_queue_system_target`／`_iter_queue_paths`／`_lint_one_file`／`_parse_section_two`／`_read_queue`／`_reserve_ids`／`_resolve_append_target`／`_resolve_queue_path_for_domain`／`_write_queue`／`iter_queue_paths`／`resolve_queue_path`）逐个 `git grep master` —— **12/12 全命中**；③ 分支改的 `openspec/changes/queue-dual-file-split/tasks.md` 在 master 已迁 `archive/2026-08-17-queue-dual-file-split/`，**合入会把一份已归档包的未归档副本复活** | **放弃**——功能已由 master 上的 `queue-dual-file-split`（2026-08-17 归档）覆盖，分支是 08-11 的抢救物。🔴 **1130 行盲合风险最高的那条，现在有判据了：不是「不敢合」，是「合了没有增量、还会复活归档件」** |
| `happy-thompson-017679` | `git cherry` 2 条 `+`（`b676aaa1` propose+design+specs、`72479645` fix 本体） | ① master 有 `0c157002 docs(队列#398-6)`，**原文写着**「修法本体在分支 `claude/happy-thompson-017679` 的 commit `7247964`（已 push、**未 ff 进 master**，🟡 档待 Shao Peishen），**design 已全批、tasks 全完、变更包已归档** `2026-09-06-sweep-commit-message-extraction`」；② 该归档目录与 `openspec/specs/sweep-commit-message-extraction/` 在 master `git ls-tree` **零命中** ⇒ 归档件也只在分支上 | 🔴 **改列：不是戊档草稿，是乙档🟢「已审已完成、只差 ff」。** `取证件-…C段…` §五 原写「design 从未审过，合入即带入一个悬空提案包」——**被 master 自己的提交记录证伪**。⇒ 与 `#615` 同族出血（已销号／已全批的活躺在未合分支上）。ff 属 🟡 档，**待他一字母** |
| `op0822c-sweep-alerts-b9424a` | `git cherry` 1 条 `+`（`dcc8396d`），4 文件，全是 `openspec/changes/sweep-resident-hint-deploy-manifest/` 新建 | ① `git log master --grep=sweep-resident-hint` **零命中**；② 路径在 master `git ls-tree` **零命中**（`changes/` 与 `archive/` 都没有）⇒ 内容确实只在分支；③ 该包**未 apply、design 未审**，提交信息自述「抢救…未提交的 openspec 提案包」 | 🟡 **待他一字母**——本方无判据可自裁。**两条出路各自的代价**：(a) 合入 ⇒ 主仓多一个**未审的悬空提案包**，会被 `#561` 的 openspec 收口段计进在办数；(b) 放弃（删 ref）⇒ 丢掉 `OP-0827-D` 那次抢救的成果，但 `origin`／`backup` 恢复路径在。**本方倾向 (b)**：它自 2026-08-22 起 28 天无人认领，且没有对应的开着的队列行 |

⚠️ **与 `#617` 行内「戊 5 条（1 明确放弃／4 待判）」的数字对照**：`取证件-…C段…` §五 表里第 5 条 `op0903f1-fi2-batch-4cc6ad` 已标「续做」，不在本次四条内；派单件点名的四条即上表四条。**本次判定后，真正仍「待他一字母」的是 3 条**（`op0906i` 删／`op0822c` 合或删／`happy-thompson` ff），`queue-315` 判据已足、建议放弃。

## 五 · 本件不做什么

- 🔴 **一条分支未动**——未 ff、未删、未 rebase、未 push。ff 与删分支同属 🟡 档。
- 🔴 **未改 `工具-opener批处理执行v2.ps1` 一个字符**（改工具属 CC，且形态未定）。
- 🔴 未碰 `.51`、未碰真实库、未对外发送、未碰 L2 门禁。
- 🔴 未新建任何机制行（他 2026-09-19 答 `4a`：下一批只做缩编、不立新机制行）。
