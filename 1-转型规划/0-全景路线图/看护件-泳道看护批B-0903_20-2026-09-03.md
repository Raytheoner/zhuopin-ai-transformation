---
title: "看护件 · 泳道看护批 B-0903_20（第四批：ATP apply ＋ 采购/财务八场景开工）"
created: 2026-09-03
批次: B-0903_20
派出线: Cowork 业务总线 OP-0903-B
前批: B-0903_10（2 泳道，A1 停 design 审已放行、A2 已 DONE）
status: 执行中
---

# 看护件 · 泳道看护批 `B-0903_20`

## 〇、本批口径

**⑴ ATP apply 段**：`sc8-atp-batch2-closeout` 的 design 已由 Shao Peishen 2026-09-03 放行（三项补齐 ＋ `validate --all --strict` 127/0），本批做 apply。🔴 **放行 ≠ 开工**。

**⑵ 八场景开工**：`#467`–`#474` 已于 2026-09-03 由 `OP-0902-B5` 立行（采购 3 ＋ 财务 5）。**本批推进它们各自的 §5「每个场景固定流程」第 1-2 步**。

🔴 **开工判据（Shao Peishen 2026-09-02 定，本批一律照此）＝不管规划时间表、能开尽开，唯一门槛是前置依赖。**
⚠️ **但「前置未满足」是真门槛，不是形式** —— 前置没到的场景**不硬推到需要它的那一步**，做到哪一步停在哪一步，并把卡点写进队列行。**不得为了「看起来有产出」而绕过前置去写一个建立在空气上的 proposal。**

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不手抄。

## 一、硬边界（继承，逐条适用）

1. 🔴 **⏭️ 档：`.51` 部署及任何触碰生产服务的动作永不执行** —— `transfer-out` 后跳过、继续其余。
2. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 永不代办**。
3. 🔴 **业务*数据*与一切对外发送永不自动入批**；真实库**只读**、绝不写。
4. 🔴 **所有 `pause` / `check-heartbeat` 一律带 `--no-notify`**。
5. 判据未覆盖按 🟡 停（fail-safe）。
6. 🔴 **三闸全锁**（唐燕萍／姚祖怡／陈忱）⇒ **不得起草或发送任何跟进信**；需要专员确认的口径一律登记「待闸开后并进下一封」。
7. 🔴 **规划文档一律不改**（全景规划／实施计划／权威排期表／前置总表）—— 走移交单。
8. 🔴 **openspec design 审属 🟡** ⇒ 起草完即 `pause`，**不自行通过、不进实现**。

## 二、波次与触碰区矩阵

| 波 | 泳道名 | 项 | 触碰区 | 重叠 |
|:--:|---|:--:|---|---|
| 1 | `sc8-kitdate-4` | A1 | `openspec/changes/sc8-atp-batch2-closeout/`、`openspec/specs/` | 无 |
| 1 | `proc-scenarios` | A2 | `4-数字员工/采购部/SC4·SC10·SC11/`、队列 §一 #467-#469 | 无 |
| 1 | `fin-scenarios` | A3 | `4-数字员工/财务部/FI5·FI6·FI8·FI9·FI10/`、队列 §一 #470-#474 | 无 |

三条触碰区零重叠，并行（`-MaxParallel 3`）。⚠️ 三条收工都要写队列，会有短暂锁竞争——**这是正常的，锁会 fail-loud 拒绝，不要绕**。

## 三、泳道 opener

### A1 · ATP 立项变更包 apply

粘贴端：CC ｜ 泳道：sc8-kitdate-4

```
[OP-0903-B1]【CC】ATP变更包apply
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903b1-atp-apply`）｜ worktree：☑（sc8-kitdate-4，新 worktree，收工自删）｜ 工作区：无（openspec apply 写 spec，🔴 不进实现、不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_20
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903B1-ATPapply。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_20-sc8-kitdate-4.md`，开工 1 分钟内写首行，每里程碑追加，超 10 分钟的步骤补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_20 --wave 1 --lane sc8-kitdate-4 --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① `openspec/changes/sc8-atp-batch2-closeout/` 全包 → ② 队列 §一 #118／#344。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 已放行 design（原文）：
  依据＝三项补齐已完成（D3-bis 24→11 更正落 design 与对照表／D4 边界口径落判例包附录 A、
  状态 ⏳ 待姚祖怡签认／D5 不变式③ 验证用例参数与预期值已算定落 spec + tasks 1.2.13），
  `openspec validate --all --strict` = 127 passed / 0 failed。
  🔴 **放行 ≠ 开工**：⑴ 可 `/opsx:apply` 写 spec；⑵ **实现属另立行、本轮不做**；
  ⑶ D4 附录 A 须姚祖怡（男）签认而其串行闸仍 🔒（采购部#20 在途）
     ⇒ tasks 1.5.4／3.1 仍是硬前置，**不得据此翻开关、不得部署、不得触碰 `.51`**。

做什么：
1. `/opsx:apply sc8-atp-batch2-closeout`，把 spec 落进 `openspec/specs/`。
2. 重跑 `openspec validate --all --strict`，须绿。
3. 按「完工即归档纪律」判能否 `/opsx:archive`：🔴 **tasks 未全 [x] 就不归档**
   （1.5.4／3.1 卡姚祖怡签认）⇒ 按三条入口之一留机器认得的说明
   （`暂不归档` ／ `预期观察窗口：N 天` ／ `--ack-stale-change`），**不要沉默地不归档**。
4. 收工：§二 批次登记，§一 #118 回写。

不做什么：
- 🔴 不进实现、不翻任何开关、不部署、不触碰 `.51`
- 🔴 不发信
```

### A2 · 采购域三场景开工（SC4／SC10／SC11）

粘贴端：CC ｜ 泳道：proc-scenarios

```
[OP-0903-B2]【CC】采购三场景开工
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903b2-proc-scenarios`）｜ worktree：☑（proc-scenarios，新 worktree，收工自删）｜ 工作区：无（场景工程骨架与 openspec 起草，🔴 不部署、不触碰 `.51`、真实库只读）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_20
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903B2-采购三场景。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_20-proc-scenarios.md`，规则同 A1。🔴 三场景逐个做，每个做完追加一行。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_20 --wave 1 --lane proc-scenarios --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #467（SC4）／#468（SC10）／#469（SC11）**全文** → ② 🔴 `4-数字员工/CLAUDE.md`（§5 场景固定流程第 1-7 步正文在此，**动 4-数字员工 之前必读**）→ ③ `5-平台底座/CLAUDE.md`（底座子系统与 OEM 隔离边界）→ ④ 根 `CLAUDE.md` §5／§7。本件为 A 类，直接开工。

🔴 开工判据（Shao Peishen 2026-09-02 定）＝**不管规划时间表、能开尽开，唯一门槛是前置依赖**。
⚠️ **但前置未满足是真门槛**：做到哪一步停在哪一步，把卡点写进队列行；
   **不得为了「看起来有产出」而绕过前置，去写一个建立在空气上的 proposal。**

🔴 SC4 特别说明（先看，免得白做）：其排期已由 Shao Peishen 2026-09-03 裁定**正式顺延**
  （2026-10 不可行——两项知识型前置「法务标准合同条款库」「合同风险条款判据」总表状态格零回填，
   按启动前 8 周纪律算术下界＝ 2026-10-29），移交单已出：
  `1-转型规划/0-全景路线图/移交单-SC4顺延与前置总表补5行-全景路线图线执行重组循环-2026-09-03.md`。
  ⇒ **SC4 的知识型前置未就绪**，本泳道**只做不依赖那两项前置的部分**（如工程骨架、数据源盘点、
     不涉判据的接口草案），**判据/规则相关部分一律停下并登记**，不得自拟法务判据。

做什么（三个场景逐个，按 §5 场景固定流程第 1-2 步）：
1. 逐场景先判**前置依赖是否满足**（判据源＝《跨场景前置数据与知识库任务总表》），
   结论写进心跳与队列行；🔴 **未满足的写明卡在哪一项、卡到什么时候**。
2. 前置允许的范围内，按 `4-数字员工/CLAUDE.md` 的 scaffold 引导建工程骨架
   （目录、`CLAUDE.md`、最小可跑结构），**import 平台底座包、不重复造轮子**。
3. 起草 openspec proposal（🔴 **只到 proposal/tasks 起草，design 审属 🟡 档**）：
   起草完即跑 `pause --action-key openspec_design_review`，以 `OPENER_PARTIAL` 收尾，不自行通过。
4. 收工：§二 批次登记，§一 #467／#468／#469 逐行回写（写清各自做到哪一步、卡在哪）。

不做什么：
- 🔴 不连真实库做写操作（只读可以）、不部署、不触碰 `.51`
- 🔴 不自行通过 design 审、不进实现
- 🔴 不发信（姚祖怡串行闸 🔒，采购部#20 在途）；需他确认的口径登记「待闸开后并进下一封」
- 🔴 不改任何规划文档
```

### A3 · 财务域五场景开工（FI5／FI6／FI8／FI9／FI10）

粘贴端：CC ｜ 泳道：fin-scenarios

```
[OP-0903-B3]【CC】财务五场景开工
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903b3-fin-scenarios`）｜ worktree：☑（fin-scenarios，新 worktree，收工自删）｜ 工作区：无（场景工程骨架与 openspec 起草，🔴 不部署、不触碰 `.51`、真实库只读）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_20
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903B3-财务五场景。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_20-fin-scenarios.md`，规则同 A1。🔴 五场景逐个做，每个做完追加一行。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_20 --wave 1 --lane fin-scenarios --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 队列 §一 #470（FI5）／#471（FI6）／#472（FI8）／#473（FI9）／#474（FI10）＋ #475（芯片价格 API）**全文**
   → ② 🔴 `4-数字员工/CLAUDE.md`（§5 场景固定流程第 1-7 步正文，**动 4-数字员工 之前必读**）
   → ③ `5-平台底座/CLAUDE.md` → ④ 根 `CLAUDE.md` §5／§7。本件为 A 类，直接开工。

🔴 开工判据（Shao Peishen 2026-09-02 定）＝**不管规划时间表、能开尽开，唯一门槛是前置依赖**。
⚠️ **但前置未满足是真门槛**（同 A2 那段，不重复）。

🔴 财务域两条硬提醒（都是本项目踩过的，别再踩）：
  ⑴ **财务建造第一步别省**（根 `CLAUDE.md` §5）：凡涉发票号 join，
     **先验税务导出发票号与 U9C `InvoiceNo` 字面严格一致**（位数／前导零／空格／全半角／代码前缀）
     —— 对不上 join 全盘落空**且不报错**。
  ⑵ **唐燕萍串行闸 🔒**（`财务部#16` 在途）⇒ 任何需要她确认的口径**一律登记「待闸开后并进下一封」**，
     不得起草新信。

🔴 FI10 特别说明：其依赖的**芯片价格 API 尚未选型**（已由 `#475` 独立立行，跨域基础设施，
  与 O2 芯片预警共用）⇒ FI10 中依赖该 API 的部分**前置未满足、停下登记**，
  不依赖它的部分（库存账龄、在途采购、BOM 用量、准则规则引擎）可做。
  ⚠️ 前一班已查出一处待判：前置总表 `O2` 行有「芯片**供货** API 选型」，
  与实施计划的「芯片**市场价格** API」命名/服务对象/时点三项均不同，**是否同一项尚未定** ——
  **不要替它下结论**，如实沿用「未定」并在队列行注明。

做什么（五个场景逐个，按 §5 场景固定流程第 1-2 步）：
1. 逐场景判前置是否满足（判据源＝前置总表；⚠️ **该表财务域现只有 FI1/FI2/FI3，FI5-FI10 缺行**
   —— 缺行本身已由移交单交全景路线图线补，本泳道**据现有信息判、并注明「判据源缺行」**，不代补）。
2. 前置允许范围内建工程骨架（按 `4-数字员工/CLAUDE.md` scaffold 引导，import 平台底座包）。
3. 起草 openspec proposal（🔴 只到 proposal/tasks，design 审属 🟡）；起草完 `pause` 收尾。
4. 收工：§二 批次登记，§一 #470–#474 逐行回写（各自做到哪一步、卡在哪）。

不做什么：
- 🔴 不连真实库做写操作、不部署、不触碰 `.51`
- 🔴 不自行通过 design 审、不进实现
- 🔴 不发信（唐燕萍串行闸 🔒）
- 🔴 不改任何规划文档、不代补前置总表
```

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0903_20` 现取，须含 D6 停次明细、转出项、「待回内网」清单。
