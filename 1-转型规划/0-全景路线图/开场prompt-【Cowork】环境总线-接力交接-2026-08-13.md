---
title: 环境总线接力交接 —— 主攻 partial 分类收口、压 WIP 回线内
created: 2026-08-13
updated: 2026-08-13
执行方: Cowork 环境总线
status: 生效
前一份: 开场prompt-【Cowork】环境总线-接力交接-2026-08-10.md（已完成使命；其 §六bis 边界正本仍指向 08-08 那份）
---

# 开场prompt ·【Cowork】环境总线 · 2026-08-13 转场

## 开场词（复制即用）

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ ｜ 工作区：企业AI转型（只改 .md 与 Cowork artifact，不写生产代码、不自行 commit，收工只登记 §二 批次） ｜ session：新开

读 1-转型规划/0-全景路线图/开场prompt-【Cowork】环境总线-接力交接-2026-08-13.md ＋ 根 CLAUDE.md 当前进度恢复上下文，先按文件 §〇 自检四查，通过即按 §二 开工、有差异再问我。
```

---

## §〇 开工自检四查（A 类硬规则）

1. **接力时效**：队列里若已出现更晚的批次行，**以队列为准**。
2. **在办清单抽核**：抽 §三 任意 2 行核对队列**实际状态列**。🔑 一律用 `python 0-学习与工具/工具-队列查询.py --row N`（不截断、头尾互斥会打冲突警告）。
3. **残留待补**：grep 四条接力顶部「⏳ 队列更新待补」。**2026-08-13 基准：块 A／块 B 均已回补，该节只剩留痕、无待办**——读到"已回补"字样即跳过。
4. **skill 两向漂移扫**：把只读挂载 `.claude/skills/zhuopin-*/SKILL.md` 与 `0-学习与工具/skills源码/<同名>/SKILL.md` 逐份 `diff`（跳过各自 frontmatter 后比正文）。**2026-08-12 基准：四份全部 0 差异行。**
   🔴 **必须先 `tr -d '\r'` 再比**——源码侧是 **CRLF**，直接用 `awk '/^---$/'` 之类会失配、**报出 95 行的假漂移**（2026-08-12 实测，本 session 当日共踩 CRLF 陷阱 4 次）。

5. 🆕 **同线近期班次扫（Shao Peishen 2026-08-14 拍板 (a) 入法）**：`mcp__session_info__list_sessions --limit 10`，挑出与本次任务同域的班次、**尤其两个非交互定时班次**（`回件拆件巡逻 patrol` / `Weekly status update`），`read_transcript --limit 1` 读其结论段；**凡其中出现「机制发现／缺口／未落队列的判断」而队列查不到对应行的，先补登记再开工。**
   🔑 **为什么值得单列一查**：其余四查查的都是「**已写下来的东西还准不准**」，**只有第 5 查能查「有没有东西根本没被写下来」**。2026-08-13 实测当场撞到活例——拆件巡逻在 transcript 里报了「巡逻 prompt 指向的 `跨桌任务队列.md` 已成纯指针文件，照读会解析到零行→静默输出空巡，下一个有待拆件行的班次即会漏拆」，**该发现在队列里查不到独立行**。
   ⚠️ **本查只覆盖 Cowork↔Cowork**（`list_sessions` 只列 Cowork 会话，CC 一个都不列，2026-08-13 实测 102/102）。**CC 那一棒走磁盘直读，见 §五「CC 复命零粘贴」。**

> 🔑 **本节前四查里，查 4 与"查主工作区脏文件"是同一类坑的两面**：**沙箱侧 git 因 `core.autocrlf` 未启用会虚报 30+ 文件为已修改**（2026-08-13 又踩一次，把 `propose.md` 误判为脏）。**凡涉主工作区 git 状态，一律本机直读。**

---

## §一 🔴 本线定位（2026-08-10 Shao Peishen 收窄，仍是首要纪律）

**本线只做构建环境与工作流程的优化，不做具体业务线的任务调度。**

| 做 | 不做 |
|---|---|
| 构建环境与自动化机制本身（worktree/stash 台面、编辑锁、sweep、巡检巡逻、队列纪律、openspec 工作流、本机工具链、机器人链路） | **业务场景的任务分派与排期**——归值周巡检《本周计划》与拆件巡逻 |
| 机制取证、方案定稿、队列行写入 | 替业务线决定"先做哪个场景、派给谁" |
| 工作流本身的缺陷诊断与修法 | 业务口径判定、专员回件处置、场景建造、**对外发信** |

⚠️ **例外**：真实阻塞或安全风险可即时升级，但须在队列行内标明"因何紧急而绕过机制分发"（2026-08-13 A 批即一次，已如实登记）。

---

## §一bis 🔴🔴 先读这段：2026-08-13 全天反复纠缠的**同一个**问题

**一句话**：**同一个事实存在两份副本，改了一处、漏了另一处；而「漏掉的那一处」没有任何机制会发现，只能等人偶然撞见。**

**当日实录 13 例**（全部实测、非印象）：

| # | 权威源 | 过时的那份副本 | 怎么被发现的 |
|---|---|---|---|
| 1 | 队列真身两份 | **两个定时任务 prompt** 里的旧路径 | 拆件巡逻第二班偶然撞见（**带病运行 2 天**） |
| 2 | 主工作区活审计日志 | worktree 里 9 天前的 `reports/` 副本 | 一个后台轮询**空转 4.5 小时**才被问起 |
| 3 | `pip show` 实况 | #313② 与 CLAUDE.md「已卸载、未回滚」 | 本线查 WIP 时顺带 |
| 4 | 实测非原子删除 | #267／#308／CLAUDE.md「零数据风险」（**同一句话多处副本**） | CC 自删 worktree 时真删了 5 个文件 |
| 5 | 扫描工具实际输出 | 6.6「定时任务真身零命中」 | 今日复跑同一命令才发现 |
| 6 | 代码 `_count_mechanism_wip` | 我说的 18／16、CC 沿用的 23／16 | CC 直接跑那个函数 |
| 7 | `commit 221ed46` ＋ tasks.md 原文 | 我写进派单件的「早已定性在等真实场景」 | **CC 拒绝执行并回查** |
| 8 | `.gitignore` 实际五条规则 | 我说的「两条」 | CC 全扫 |
| 9 | README 下表状态 | 第 20 行「下一个可用号」提示段 | **同族第二次复发** |
| 10 | #332 物理位置 | 批次自称「已挪入业务件」（只补了域字段） | 本线核实时 |
| 11 | opener §三 表内数据 | 其表头「2026-08-10 快照」 | 收工读节标题时 |
| 12 | 内容已随 commit 入库 | 2 条批次行仍标「待处理」 | 复查一条收口句时顺带 |
| 13 | §二 实有 2 条待处理 | sweep 日志「§二无待处理批次」 | 同上 |

🔑 **三条结论，比任何单个发现都重要**：

1. **13 例里，没有一例是机制报出来的。** 全是人偶然撞见——**代价分别是：带病 2 天、空转 4.5 小时、一次差点改错生产行为。**
2. **现有对策全是「人守」规则**（「改一处必改另一处」「回原始载体读原文」）。**本线当日把后者写进 #314 勘误段与本文件，随后同一天内自己又犯了三次**（#7／#8／#6）。⇒ **按规则退休制的判据，这一族早已越过「必须机制化」的阈值。**
3. **#328 子项③④⑤ 恰好都是「让一个已知但不可见的状态显形」**——接力未回补块／部署载体落后 master／sweep 日志措辞。**它们是这个问题的机制答案的开端，不是三个孤立的小活。**

⚠️ **给下一 session 的实操要求（不是口号）**：**凡要写进队列行、派单件、或任何会被别人当依据的地方的判断，必须先打开那个真正在执行它的东西**——代码看函数、口径看 commit 与 tasks.md 原文、数字看实测输出、状态看 `工具-队列查询.py`。**摘要只能用来找线索。**

---

## §二 ✅ 已执行完毕（2026-08-13 夜）：12 个 partial 的分类收口

> ✅ **本节主攻已于 2026-08-13 夜由环境总线执行完毕**，12 行三问分类结论均已写进各自队列行（标记 `📋 2026-08-13 环境总线「12 个 partial 三问分类」`，可 grep）。**下一 session 不必重跑**，直接读队列行结论即可；结果摘要见 §七 08-13 夜节。**销号动作按硬边界 2 未做，全部交 2026-08-17 值周巡检。**

<details>
<summary>（原派单正文，保留备查）</summary>

### 原文：12 个 partial 的分类收口（Shao Peishen 2026-08-13 选定方向）

> 🔑 **它与 §一bis 是同一件事的两面**：**已实测出至少 2 个 partial 是「记录与现实脱节」而非真未完成**——#234（三条证据指向已由 #238 完成）、#313②（记录说已卸载、实测已重装）。**所以这次分类不只是压 WIP，是在清同一族的存量。**

### 为什么是这件

**机制类可动 WIP ＝ 18／16，超限。** 而他 2026-08-10 对 §四 #58 已拍板 **(a)：靠收口降 WIP，不再调上限**。
**24 行未完成里有 12 个 partial**，其中相当一部分是「实质已完工、只差归档或等观察项」——**它们是 WIP 的虚高部分**。

🔑 **2026-08-13 已验出一个真实样本**：**#234** 三条证据都指向「(2) 已由 #238 完成」（函数已实现并被 `main()` 调用／注释自陈来源 #234／承接行 #238 已归档），**但当时未销号**——因为没通读 #238 全文确认范围完整覆盖。**这说明虚高是真实存在的，不是猜测。**

### 本次要产出什么

**对 12 个 partial 逐行做三问分类，结论写进各自队列行**：

| 问 | 判据 |
|---|---|
| **① 还差什么？** | 逐条列出未完成项，**必须回原始载体读原文**（承接行、归档件、变更包 `tasks.md`），不得只读本行叙述 |
| **② 归谁？** | Cowork 可做 ／ CC 须做 ／ 等外部方 ／ 等观察窗口 —— 四选一 |
| **③ 能不能现在就收？** | 若 ① 为空 ⇒ **属「已完成未销行」，给出销号证据**；若只差归档 ⇒ 标明卡在哪一项验证 |

**12 个 partial**：#308 ／ #279 ／ #270 ／ #22 ／ #68 ／ #96 ／ #130 ／ #234 ／ #254 ／ #312 ／ #313 ／ #315

### 🔴 三条硬边界

1. **不替它们派单。** 本线出的是「还差什么、归谁」的结论，**分发仍走值周巡检/拆件巡逻**（§六bis C2）。产出派单件属直接派活，**须他明示授权**。
2. **不擅自翻 `[S:done]`。** 判为「已完成未销行」时，**给证据、给建议，销号动作交值周巡检**——除非证据是**逐条对上原文**级别的，而不是「函数存在且被调用」这种推断。
   🔑 **这条是 2026-08-13 花了代价换来的**：当日本线只取一行摘要就写进派单件，被 CC 当场证伪（详见 §七 08-13 节）。
3. **#234／#313 两行已有 2026-08-13 的取证，直接接着做、不要重跑。**

### 建议次序

**先做最可能虚高的**（收益直接）：#234（已有证据，只差读 #238 原文）→ #313（② 已完成，只剩 pip 待查，而那条已交 08-17）→ #270／#279（两行同卡「拿到采购部群 chatid」，实为一件事的两半，**先判它们该不该并行或合并**）。
**再做剩余**：#308／#315（均卡在 openspec 变更包未 archive，判断卡在哪项验证）→ #22／#254（Cowork 侧可自行推进的）→ #68／#96／#130／#312。

</details>

---

## §三 当前状态（2026-08-13 夜快照）

> 🔴 **本表必然会过时**——08-10 那版三处过时、08-12 那版两处过时，**每一版都过时过**。以 `工具-队列查询.py --row N` 实测为准。

**池子实况（2026-08-13 夜实测）**：机制/环境未完成 **25 行**（open 7／partial 12／hold 6，其中 3 行 🛑 首标记不计），**可动 WIP 22／16 超限**；业务场景未完成 10 行（open 3／partial 2／**blocked 5**）。

> 🔴 **上一版这两个数都是错的，记法值得看一眼**：旧版 §三 写「24 行／可动 WIP 18／16」，而同一份文件的 §七 写「实测 22／16」——**同一文件内两处自相矛盾**，正是 §一bis 那族问题的第 14 例。**权威源只有一个**：`工具-共享文档编辑锁.py::_count_mechanism_wip`（cap 见同文件 `MECHANISM_WIP_CAP_DEFAULT`）。**此后写这个数之前先跑一次那个函数，不要抄上一版 opener。**

| 行 | 状态 | 说明 |
|---|---|---|
| #234 | 🔄 partial | ✅ **已判定「已完成未销行」，销号证据四条已逐条对上 #238 原文**（含 sweep 日志生产实证），交 08-17 销号 |
| #270／#279 | 🔄 partial | ✅ **两行均已判定「已完成未销行」**——「卡在采购部 chatid」这个前提本身是错的，chatid 08-06 即已入库。**判不合并、建议双双销号** |
| #68 | 🔄 partial | ✅ **四项全部落地，判定可销号**（③ 的注册卡点 07-30 即解除）；⚠️ 其 ① 落地的内容已被 #267 推翻，另挂 #267、不阻塞销号 |
| #313 | 🔄 partial | 除 pip 疑问外**另有三条未完成**（⑤修法⑶ 无实现无判定／⑦ 看板分母从未核实／② 回写缺一段）⇒ **归 CC** |
| #328 | 🆕 open | sweep 起跑段第三例 ＋ 子项②`.gitignore`（**已由 CC 08-13 做掉，待核**）＋ 子项③接力未回补块检测 |
| #324 | 🆕 open | 编辑锁 release 校验误报家族，现含子项②③④⑤⑥（其中 ④⑴ help 已由 CC 08-13 完工） |
| #267 | 🔄 | **口径已推翻**：`git worktree remove` 权限失败**非原子**，禁再自删活跃 worktree |
| #96 | 🔄 partial | 跨服务排查未做；⚠️ **范围可能已缩小**——`wecom-aibot-channel` 永不上 `.51` |
| #98 | 🆕 open | 月度环境体检，8/28 触点；**10 个 prunable worktree 待处置挂在此** |

---

## §四 已交值周巡检的清单（**本线不重复做**）

> ⚠️ **2026-08-20 校准**：下方 1-4 原交 **2026-08-17** 那班，**该班已于 08-17 10:07 跑过，但本线未核实它是否真的接走了这四件**——如实登记，勿假定已完成。**第 5 件是新加的，交下一班 2026-08-24。**

1. **机制类可动 WIP 上限复评**（8→16 冻结观察期当日到期）
2. **#234 销号核实** —— 🔴 **与第 1 件强相关：含虚高的分子会得出「上限还不够用」这个相反结论**
3. **#313② pip 指针反查** —— 谁/何时/是否有意；三条独立信源与判后二选一动作已写进行内
4. 🆕 **openspec CLI 升版（2026-08-15 加，Shao Peishen 拍板 (a)：不新立行、挂 CC opener 附注）** —— `.github/workflows/ci.yml` **L159** `@fission-ai/openspec@1.7.0` → **`@1.9.0`**，与人力招聘项目对齐。🔴 **硬前置：CC 须先本地跑 1.9.0 验证 `openspec validate --all --strict` 74/74 仍通过再改**；不通过则报回，**不得为过门禁而改 spec**。**出派单件时随车带上即可，体量＝一行版本号＋一次验证。** 来源与完整 triage 见队列 #328「跨项目环境评审 triage」段（同段另记 pre-commit 层「暂不做＋三条前置」的决定）。
5. 🆕 **`工具-队列查询.py` 加 `--digest`（2026-08-20 加，Shao Peishen 拍板 1(a)｜拆件巡逻性能优化 A）** —— 输出每行「编号＋`[S:x][D:y]`＋任务列首 80 字」，覆盖两份队列真身。**实测收益：2173 KB → 25 KB，压缩比约 87:1，判据零损失**（巡逻的判据只有「状态机器字段」＋「任务列是否含企微反馈字样」两项）。🔴 **紧迫性**：拆件巡逻 prompt 已于 2026-08-20 改为**先跑 digest**，并写了「若 `--digest` 未落地则退回逐条读法、自检记一句」的兜底——**兜底能跑，但优化拿不到**，故本项落地前巡逻仍是老开销。**归 CC**（改 `.py`）。

⚠️ **本次主攻（§二）与第 2 件有重叠**：本线做的是**取证与分类**，值周巡检做的是**销号动作**。**别越界替它销号**，但取证做得越扎实，它那天越省事。

---

## §五 纪律段（引用条目名，不复述）

- 根 `CLAUDE.md` §5：完工即归档／§0.2 登记／直读原则／规则退休制／**多任务须指明次序与并行**／决策路由须标**具体 session 名**／**日期取本机本地**
- 🔴 **编辑锁**：改队列前 `acquire`、改完立刻 `release`；**`acquire` 与写盘两步、中间查 `$LASTEXITCODE`**；**收工前确认锁已释放**
- 🔴 **取号**：`--reserve` ／ `--reserve-multi`；**取号前先做并入审核（协议〇.10）**；⚠️ **WIP 已超限，优先并入、不新立行**
- 🔴 **三条静默失败写法禁令**：完整路径／不得裸竖线／不得裸反引号
- 🔴 **§二 批次第 3 个 cell 是「建议 message」不是「说明」** —— `append-row --section 二` 的 help 曾写错（已由 CC 08-13 修），照旧 help 填会让 commit 主题变成清单里第一个反引号词（**已有 4 条受害提交**）
- 🔴 **批次清单必须含队列文件自身**（#225）——2026-08-13 有一例漏写且 release 未拦住，成因未定，见 #324 子项⑥
- 🔴 **写后反查**：`(Select-String -Path $f -Pattern $k -SimpleMatch).Count`（**不加 `-AllMatches`**；`-Pattern` 串内勿含反引号）。**交叉验证优于重复同一验证**，至少配两个互不相关的指标
- 🔴 **查主工作区脏文件须本机直读**（沙箱侧 git 因 `core.autocrlf` 虚报）
- **批次即扫**：登记后 `Start-ScheduledTask -TaskName ZhuopinCommitSweep`；⚠️ **触发 ≠ 落库**，须回看 `reports/sweep-commit.log`
- 🆕 🔴 **CC 复命零粘贴（2026-08-14 拍板 (a) 入法，正本在根 `CLAUDE.md` §5「会话接力」条下）**：他说一句「**CC 跑完了**」即自行取件，**不再要他粘贴 CC 的收工报告**。取件＝三条独立信源：① `C:\Users\Paul Shao\.claude\projects\<路径编码>\*.jsonl` 逐行 `ConvertFrom-Json` 取 `type=assistant` 的 `message.content[].text`（**CC 原话全文**）；② 队列行；③ `git log` ＋ `git rev-list --count origin/master..master`。**不一致时以队列与 git 为准。** 🔴 **取件算法（2026-08-15 修订，替代初版「按 mtime 取最新」——那写法在多 CC 并行时会静默漏读）**：**⑴ 时间窗全取**（自上次取件以来有新写入的全部 jsonl，不取「最新一条」）→ **⑵ 读每条会话首条 `type=user` 消息**（＝他粘的开场词，实测必含【设置】行与派单件路径）→ **⑶ 读该派单件 frontmatter `来源:`（早期件为 `派发方:`）取派出线名，只收匹配本线的** → **⑷ 回报「读到哪几条、各自归属」**，使漏读误收当场可见。**三类兜底禁止静默**：提不出路径／无 `来源` 字段（覆盖 26＋2／35）回落 `git log --diff-filter=A`／匹配 0 条须明说。**归属粒度定在「线」不是 session 实例**——这样转场后新 session 仍能收到前任派出的 CC；代价是同线两个并行实例会互收，已知并接受。 ⚠️ 三条边界：**非公开格式、解析不出立即退回粘贴不得猜**／**只能读不能反向发**（Cowork→CC 仍粘一句开场词）／**目录名编码吃掉中文**（`企业AI转型`→`---AI--`），须 mtime ＋ 内容二次确认。 📌 **`.claude/handoff/` 与 `hoff.ps1` 已明确不做**（2026-08-14 拍板 2(a)）——其收益已被磁盘直读拿走大半，建它等于新增一个 gitignore 里的第二份副本。

**§六bis 本线边界（C1 派单边界／C2 机制收口为默认交付）正本仍在** `开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md` §六bis，**未迁移、以那份为准**；本文件 §一 是其收窄。

---

## §六 收工段

滚动更新本文件（日期节只留 2 个）→ 列文件清单 ＋ 建议 commit message ＋ 登记 §二 批次（**须含队列文件自身、状态列禁带 ✅、第 3 cell 是建议 message**）→ 触发 sweep 并回看日志 → **确认编辑锁已释放**。Cowork 不擅自 commit。

---

## §七 日期节（R5：只留最近 2 个）

### 【2026-08-13】A 批收口 ＋ 一次被 CC 挡下的错误派单 ＋ 两条口径被实测推翻

**收口**：A 批 ⑴⑶ 由 CC 完工（`.gitignore` **五条**规则合并为 `*.editlock*`——本线原判「两条」，CC 全扫发现是五条／`append-row` help 对齐表头）；孤儿 `.stale` 告警止血并解除；`wecom-aibot-channel` 由 CC 补齐 7.2（真杀进程实测，三级退避 60 秒自动拉起、自愈 66 秒零人工）＋9.1-9.4，8.5 声明 7 天窗口**但保持未勾**，sweep 已推「✅ 滞留解除」。**那条告警的正解是把状态补齐到与告警相符，一个字都没改告警。**

🔴 **一次错误派单被 CC 挡下——本线当日最该记住的**：本线在 A 批 ⑵ 指示 CC 改 sweep 措辞，理由是「#314② 早已定性其事实是『在等真实场景』」。**CC 没照做，先核实前提**，发现 commit `221ed46` 提交说明与该包 `tasks.md` 第 1 行都明写「不满足『仅剩观察项』前提、未强套、维持疑似遗忘归档报法」。**⇒ 那个包被明确排除在 (c) 立论之外，告警说的是实话。**
**根因**：只取 opener 一行摘要，**未回原始载体读原文，就写成了给 CC 的执行指令**。**同族第三次，且犯在最不该犯的位置。**
🔑 **已入法**：**摘要可用来找线索，不可用来当依据；凡要写进派单件的判断，必须回原始载体读一遍原文。**

🔴 **两条既有口径被实测推翻**：
1. **`git worktree remove` 报 Permission denied ⇒ 零数据风险」不成立**（#267）——它**在失败前已删除 `commondir` 与 5 个真实文件**。**「工具静默回退」第六形态，方向是「报失败而实际做了一半」——没人会去检查一个已经失败的操作留下了什么。**
2. **「全局 editable 指针已卸载、未回滚」不成立**（#313②）——`pip show` 实测已重装、指向主工作区。两假设指向相反处置，**本线未猜**，已交 08-17 反查。

**其它**：#225 守卫在一次真实场景未拦住（成因未定，决定性证据在 CC 侧，#324⑥）；#234 疑似已完成未销行（三条证据，未擅自销号）。

**当日后半程（A 批交付之后）**：
- **两个定时任务的死指针已修**（拆件巡逻明早、值周巡检 08-17 都能读到两份真身）；根因是 6.6 那句「定时任务真身零命中」的误记，**而扫描工具本身没问题**（今日同命令复跑准确报 3 处命中）。
- **`wecom-aibot-channel` 已收口**（CC 补齐 7.2 真杀进程实测＋9.1-9.4，8.5 声明 7 天窗口但**保持未勾**）；**那条「疑似遗忘归档」告警的正解是把状态补齐到与告警相符，一个字都没改告警。**
- **#335 收口**：`ops/wecom-service-home` 落后 master **62 提交**已 ff 对齐并重启，**#333②③ 的修复到此刻才真正在生产码里生效**——**距 #310 同类事故仅 4 天，是复发不是孤例**；建常驻「落后 master」告警的建议已**并入 #328④**（不新立行）。
- **机制类可动 WIP 口径定案 (b)**：维持含 hold。**实质理由**＝`blocked`／`timed=` 已结构性排除「真的动不了」、线画在 `blocked`；`hold` 是主动挂起、**正是上限要约束的对象**，排除它会让「改成 hold」变成合法逃逸口。**此后不得再以「hold 动不了」这个直觉重开。** 实测 **22／16**。
- **两条纯回写缺口已按 #260 口径人工销行**；sweep 那句「§二无待处理批次」与事实不符已**并入 #328⑤**。

🔑 **当日四次新发现全部走并入、零新立行**（#324②③④⑤⑥／#326／#328③④⑤）——这是 WIP 已 22／16 超限下的唯一可行做法，也是本线此后的默认姿势。

**【同日夜 · 12 个 partial 分类收口执行完毕】**

**做法**：五路并行取证，**每一条判断都回原始载体读原文**（归档队列行／`openspec/changes/*/tasks.md`／真实源码函数／`reports/sweep-commit.log`／artifact 原件），摘要只用来找线索。

**四行判为「已完成未销行」，销号后可动 WIP 22→18**：**#234**（四项期望产出逐条对上 #238，强证据是 sweep 日志 L4809-4821 那轮「孤儿脏路径在场仍成功落库」——旧全局门下必 `return 0`）／**#270**＋**#279**（🔴 **"卡在采购部 chatid" 这个前提本身是错的**——chatid 08-06 即入库、webhook 调用点已下线、#281 已 `[S:done]` 且四部门真实回执成功；**#279 状态列末句本就写着完工结论，只是机器字段没翻**）／**#68**（四项全落地，③ 的「需 Paul 管理员注册」卡点 07-30 环境体检即已解除）。**均未擅自翻 `[S:done]`，证据写进各行、交 08-17 值周巡检。**

**其余八行的归属已判清**：**CC 须做** — #313（三条：⑤修法⑶ 至今无实现无判定／⑦ 看板 WIP 分母从未核实／② 回写缺一段）、#96 的三个服务硬抛、#130(a)、#315 的 6.6 复核；**Cowork 可做** — #22（一次 artifact 编辑：`MILES` 写死日期改直读红线表）、#96 的清单补条、#308 的 7.3／7.5／8.3 三件回填；**等观察窗口** — #312（08-17 到期，且真实发送等部署）、#315 的 6.2-6.5、#254（结构性需 2 周窗口）。

🔴 **两条新发现，均按 WIP 超限并入既有行、零新立行**：
1. **协议〇.5 至今仍在教 CC「收工时 `git worktree remove` 自删」**（并入 **#267**）——而本行 08-13 刚坐实该命令**非原子**、失败前已真删 5 个文件。**修正后的正确写法已存在，却落在 `zhuopin-queue-audit` SKILL 里**，而 CC 每次开工必读的是协议〇.5。**这是 §一bis 那张表的第 14 例，也是其中唯一会真删文件的一例。**
2. **`.51:8090` 跑着不在 master 的代码，已 9 天**（并入 **#96**）——#162 归档行自陈「真实部署验证通过」，实测 commit `648a0e5` **NOT-IN-MASTER**，master 的 `5-平台底座/` 下无 `unified-portal-gateway`。**与 #221／#228／#310／#335 同族但更危险：该行已归档为完成，没有任何机制会再回访它**；而 #221 的先例正是分支被授权删除。

🔑 **本 session 自己犯了一次、值得下一个人看的**（已并入 **#324⑦**）：写后反查时用朴素 `split` 数队列列数，报出「#312 列数从 10 变 11、而 lint 竟然放行」，**一度准备把它登记成守卫缺口**。复核后是**我的验尺短一截**——权威切列器 `editlock._table_data_rows`（#314③ 反引号感知）对两种写法一律得 8 列，**lint 从头判对**。⇒ **写后反查数队列列数一律用 `工具-队列结构lint.py` 或 `_table_data_rows`，不得自行 `split`**。与 §一bis 那 13 例方向相反：那些是「摘要过时致误判」，这次是「**验证工具比被验证对象更弱致误判**」。

**【同日夜 · 五项默认拍板已执行（Shao Peishen 当日答「全部按默认」）】**

1. 🔴 **协议〇.5 已改写，禁用 `git worktree remove`**（原第 2 项默认 (a)）——两处旧指令全部替换：新增**三条硬前提、缺一不删**（禁用该命令改用 `Remove-Item -Recurse -Force` ＋ `git worktree prune`／**不得删除本会话自身正锚定的 worktree**／删前查进程占用并跑孤儿扫描确认不落在第三桶），并明写「删不净只剩空壳、零数据风险」**旧口径已被实测证伪、不得再援引**。**#267 提出的「最权威副本没改」缺口就此闭合。** ⚠️ **未做**：全库另约 20 个 `.md` 仍含该字样，只清活载体、归档件按「历史记录不追改」——**留 08-17 承接**。
2. **`.51:8090` 非 master 代码按 (b) 只标禁删**——分支 `claude/unified-portal-design-8a2ce3` 列为禁删（含 origin），孤儿分支扫描不得列入可删候选。**未派 CC 合入 master、未冒烟**，风险窗口仍在，只是不可逆的那一半被堵住。**08-17 须给出合入排期。**
3. **#308 F1 按 (a) 登记建议**「单测已覆盖，不设为 archive 前置」，**未改 `tasks.md`**（owner 权限）；**08-23 窗口到期前须采纳或否决**，否则告警自动响起。
4. **#315 按 (a) 登记两条建议**：1.2/1.3 标 N/A（观察期已授权跳过 ⇒ 永久不可满足）、6.6 退回 `[ ]`（完成依据已被证伪）；另建议补一条观察窗口声明止噪。**均未改 `tasks.md`。**
5. **§四 #59 按 (c) 维持现状**——不建第 2 层，税务导出继续人工跑 CLI。**代价原样留痕**：每过一个工作日 `D:\airead` 多一份无人消费的文件、唐燕萍多等一天；**(c) 是「暂不投入」不是「判定不需要」，下次复评须重提。**

---

### 【2026-08-12】两个降级块全部回补 ＋ 占锁循环两端收口 ＋ 三个长期「未定位」告警当场定位

**回补**：块 A（挂 2 天）与块 B 均已回补，接力待补节清空。**根因侧** #333 由 CC 同日修完——机器人 `release` 被「新增行须在预留集合内」校验拒绝，而机器人不检查 returncode。🔑 **三方各自都在按设计工作，bug 在组合上。**

**三个长期「未定位」告警当日全部定位**：⑴ **绕锁改写** ＝ 投递/批准链路直接写盘且自行 commit，绕过编辑锁与 §二 批次双通道（git log 侧独立信源交叉验证）；⑵ **串行闸两缺陷** ＝ 以编号列字面值为行标识、按收信人精确匹配漏掉带抄送后缀行；⑶ **`append-row` help 与表头不符** ＝ commit 主题被静默取成第一个反引号词（4 条受害提交）。三者分别并入 #326／#324。
