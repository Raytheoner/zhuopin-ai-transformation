---
status: 待执行
title: "开场prompt ·【CC】生产载体 wecom-service-home 同步 master（§四 #68）"
created: 2026-08-19
执行方: CC
派出线: Cowork 全景路线图线
编号: OP-0819-D
来源: §四 #68，Shao Peishen 2026-08-19 答 (a)
---

# OP-0819-D ·【CC】生产载体同步 master ＋ 重启常驻服务

## 开场词（复制即用）

```
【OP-0819-D】CC · 生产载体 wecom-service-home 同步 master 并重启常驻服务（§四 #68，Shao Peishen 已答 (a)）
【设置】执行环境：CC ｜ CC session：☑ 新建 ｜ 分支：master ｜ worktree：☐ 不新建，直接操作 .claude/worktrees/wecom-service-home ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-CC-生产载体同步-队列四68-2026-08-19.md 全文 ＋ 队列 §四 #68 ＋ CLAUDE.md 当前进度恢复上下文，然后按本件执行。
```

## 为什么现在做（硬时点）

`ZhuopinDecisionReminderDaily` 的**实际执行体**是 worktree `.claude/worktrees/wecom-service-home`，实测 `git rev-list --count HEAD..origin/master` ＝ **246**。

⇒ **2026-08-19 当天修好的 `#312` 可 Open 池双缺口（读侧补业务场景文件 ＋ 陈化催办），明天 08:30 那次运行不会生效**；且这 246 个提交里**所有落在 `wecom-aibot-service` 的修复同样都没生效**。

**实证不是推断**：2026-08-19 09:30 立完四条新行后，**09:36:41 有一次旧码运行改写了状态文件**，写出的 `known_open_ids` 里只有机制环境行。

**⇒ 本任务须在 2026-08-20 08:30 之前完成**，否则又白跑一天。

## 任务

1. **先取证再动手**：确认该 worktree 当前分支、`HEAD..origin/master` 提交数、`git status --porcelain`（**用本机 PowerShell，沙箱 git status 会假报脏文件**）。**若有未提交改动，先固化备份再动**。
2. **ff-merge 对齐 origin/master**（本项目 PR 一律 ff-only）。
3. **重启常驻服务并逐项验活**。
4. **回写 §四 #68**（销号）＋ §二 登记批次。

## 🔴 已知会踩的三个坑（都是本项目实测记录，不是假想）

**⑴ `Stop-ScheduledTask` 会遗留孤儿子进程。** 2026-08-09 那次同类操作（当时只跨 **27** 个提交）即撞上，需**手工补杀**再验心跳。**本次跨 246 个提交，务必先列进程、停后复查、确认无残留再启。**

**⑵ 不要只信脚本打印的「已重启」。** 逐项核对**进程 CreationTime 真的刷新了**（SC8 2026-07-14 教训；2026-08-18 四服务部署即逐个核对过 CreationDate）。

**⑶ 重启后必须验真实建连，不是只看进程在。** 2026-08-09 那次是靠**心跳戳真实建连**才确认成功的。另注意 aibot **同 BotID 单实例**——临时 WebSocket 连接会把常驻监听踢下线，甚至停在「收得到心跳、收不到消息」的**僵尸态**（比直接断开更危险，看起来是活的）。

## 🔴 范围红线

- **本次连带部署的是他人 10 天的改动**（246 个提交横跨多条线）。**若 ff-merge 后任何一项验活不通过，立即停手、如实回报，不要自行 revert 或挑拣提交**——那会在生产载体上造出第三种代码状态。
- **不要顺手改任何业务逻辑**。本任务只做「让生产载体追上 master」这一件事。
- 若发现该 worktree 有本地未推送提交或 gitignore 覆盖的非空内容（`reports/` 等），**先按 #267 的教训固化备份**，不得直接覆盖——那次真实丢过两份签字材料。

## 验收（缺一不算完工）

1. `git rev-list --count HEAD..origin/master` ＝ **0**。
2. 常驻服务进程 **CreationTime 已刷新**（记下新旧值）。
3. **心跳戳真实建连**（不是只看进程存在）。
4. 🔴 **端到端验本次修复真的生效**：手工触发一次 `decision_reminder_check.py`（或等一次真实运行），确认 `reports/open_pool_reminder_state.json` 的 `known_open_ids` **包含业务场景队列的行**（`#334` / `#344` 应在内）——**修复前它只有 `240/337/338/341/98` 五个机制环境行，这是本次最直接的成功判据**。
5. 全量回归零漂移（`wecom-aibot-service`）。

## 收工

- 回写 **§四 #68** 销号 ＋ §一 相关行（`#312`）追记「生产载体已同步、修复已真实生效」。
- §二 登记批次，**文件清单每条必须是仓库根相对完整路径且反引号包裹**（2026-08-19 已因只包扩展名而静默漏 commit 一次）。
- **若任何一项验收未过，如实登记未完成，不要写 ✅。**
