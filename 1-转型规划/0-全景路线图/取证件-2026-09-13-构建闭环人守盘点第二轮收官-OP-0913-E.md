# 取证件 · 构建闭环人守盘点（第二轮 · 收官）2026-09-13 · OP-0913-E

> 🔴 本件是 `取证件-2026-09-13-构建闭环18环人守盘点-OP-0913-E.md`（第一轮，当日上午）的收官对账。
> 🔴 每一条判定都取自**当日实际运行**，不从文档推。凡写「✅ 已关」都点得出当日证据与 commit。
> 现取基准：master `07d98fc`，2026-09-13 23:3x 本地 Get-Date。

## 一、原 7 处缺口的账：关 2，剩 5，其中 1 处建议不修

| 环 | 上午判定 | 现在 | 证据／去向 |
|---|---|---|---|
| 1 选行／排波 | 人 🔴 | 人 —— **建议不列为缺口** | 与 ff 授权同类：决定「接下来做什么」本就该是人，机器只负责把候选与代价摆清楚。硬做自动化＝把判断权交出去 |
| 7 收工 → 收口 | 人 🔴 | **未动，最大一块** | `#571` 首段仍开。当日三次收口（`op0913f`／`op0913g`／`op0913j`）全由本方手做 |
| 8 合入前六条核验 | 人 🔴 | `#574` 已立行，**未建** | 当日实证：⑴⑶⑷⑸⑹ 全手敲，只有 ⑵ 改成机器。同日本方在这一环犯错两次（`is-ancestor` 移动靶／两点 diff） |
| 11 巡检结论落盘 | 无 🔴🔴 | ✅ **已关** | `a6d37b5`；当日 `合入登记/ff-patrol-20260913.jsonl` 累计 50 行，含 `销行`／`拒绝`／`合入` 三类 |
| 12 登记册销行 | 缺 🔴 | ✅ **已关** | `a6d37b5` 建机制、`b1ebcce` 迁落点入库。当日四次合入的授权原文全部归档进 `pending-ff.done-20260913.jsonl` |
| 13 临时 worktree 清理 | 部分 🔴 | **仍缺，且在恶化** | 现取 `git worktree list` **23 条**（上午 22、下午 24 之间波动），其中 **9 条 HEAD 为 `0000000`（可直接 prune）**、2 条 `_rb-*`。`#560` 早已标 `[S:done]`，但它的范围只覆盖「子任务泳道 opener 自删」这一个产出方 |
| 18 告警消费者机器守 | 无 🔴 | **仍无** | `#568` 仍 🛑 |

## 二、当日新开又关掉的两条

- ✅ **ff 授权正本不入库**：`reports/` 被 `.gitignore` 第 50 行整棵忽略，`git ls-files reports` ＝ 0。**当日 09:46–09:55 真实发生过一次无痕消失**（取证件 `ff登记册无痕消失`）。已由 `op0913l` 迁至 `1-转型规划/0-全景路线图/合入登记/`＋目录级 glob 否定放行（`b1ebcce`）。
- ✅ **登记册入库后的孤儿脏文件**：巡检每写一行留痕即成未登记脏文件、3 h 触发 sweep 孤儿告警。已由 `op0913o` 接进 sweep 自动本地提交（`4ba8a6d`，§四 `#199`）。

## 三、当日新发现、**仍无承接**的五条

1. 🔴 **合入前置的脏文件比对只取分支尖端一个 commit**。`工具-待合分支巡检.ps1` 与 `工具-泳道分支合入.ps1` 都用 `git show --name-only $br`。实证：`op0913m` 合入时回显「触碰 1 文件」，而该分支 `merge-base..branch` 实为 **4 个文件**。多 commit 分支的早期触碰面**根本不进比对**。
2. 🔴 **master 上 `test_工具-落库sweep.py` 有 15 条常驻失败**，分六族（`DeploymentTraceHint`／`OrphanFileAlert`／`OrphanResolvedNotification`／`ResidentServiceDeploymentHint`／`ScenarioSpecGapAnnounce`／`StaleChangeAnnounce`），**全是告警类**。差集口径放行它们是对的（分支没让它更坏），但**一个天天红 15 条的仓库，红与真红分不开**。
3. 🔴 **ff 巡检的调度落点未明**。它确实在按时跑（当日 10:47 扫 43 条分支、12:50、13:18 各一次，均有留痕），但**既不在 `list_triggers`（`--recurring` 现取为空）、也不在 `Get-ScheduledTask` 的 ff／待合／patrol 过滤结果里**。判别一句：**一个在跑却找不到开关的东西，出问题时没人关得掉。**
4. **差集回归不按触碰面收窄**。`op0913q` 为 2 个文件跑了双侧各 **2603 条**、耗时逾一小时。拟并进 `#574`（`工具-ff前置核验.py` 的一部分）。
5. **同一种失败连续 N 轮不降频**。轮询守把「命令非零退出」判为信号（这是对的，fail-open 要出声），但**没有「连续同错 N 轮即降频或停下喊人」**。当日 19:21–23:1x 本机 `claude.exe` 因额度不可用期间，若守卫已生效，它会 96 次/天空撞。已并进 `#575` 期望产出。

## 四、两条结构性判断，比单点缺口更要紧

### 甲 · 「验收 ＝ 第一轮真跑」被证明了三次，而它本身没有机器守

- `op0913q` 交付物过了 11 条单测，**真机 S4U 一跑就秒退**（wscript 1 秒 vs 交互式 94.8 秒）——根因是 `Resolve-Exe pwsh` 解析到 `WindowsApps` 下 **0 字节 ReparsePoint 应用执行别名**。
- 同一泳道在**真 `claude.exe` 集成**里抓到 `--allowedTools` 逗号串被 `Start-Process` 按空格切成两条无效规则——**桩测永远测不出来**。
- `#199` 的自提交**至今没等到验收现场**：每次 `合入登记/` 变脏，本方都抢先手工提交了。

**判别一句：单测证明代码对，第一轮真跑才证明环境对。** 目前「第一轮真跑」全靠人记得去做，没有任何东西守着它。**本件判断：这是第 7 环之外第二大的缺口。**

### 乙 · 两个数字都在涨，都没有机器往下压

现取 **可动 WIP 25／22**、**worktree 23 条（9 条可直接 prune）**。关行要他一字母（这合理，是 🟡 档），但 **worktree 清理本该是纯机械的**，`#560` 却早早标了 `done`。

🔴 **一并记本方当日一次误判，因为它正是这类账的反面教材**：见 `claude.exe` 报 403、又见出口 IP 是 GCP 段（`34.86.80.70`），即断言「数据中心出口导致订阅认证被拒」，并查了 clash 适配器／默认路由／代理进程／系统代理四条当作坐实。**证伪只需一个反例：IP 全程未变，而状态在数分钟内从 403 → 周限额提示 → 正常。** 真因在账号侧。**那四条查的都是真的，但它们证明的是「他在用代理」，不是「代理导致了 403」——相关不是因果。** 这一类错误没有任何机器闸拦得住，只能靠先问一句「有没有一个反例能推翻它」。

## 五、建议的补口顺序（只建议，不代定）

1. **第 13 环 worktree 清理** —— 纯机械、零依赖、当日可做，且 9 条现成可 prune。
2. **第三节第 1 条（尖端 commit 漏比对）** —— 它在合入前置上，风险直接。
3. **第 8 环 `#574`** —— 把三节第 4 条（回归收窄）一并做掉。
4. **甲（第一轮真跑无机器守）** —— 需要设计，不是补丁。
5. **第 7 环收工→收口** —— 最大一块，建议在 1–4 之后单独设计。
6. 第 18 环等 `#568` 解冻；第三节第 2、3 条待他定是否立行。
