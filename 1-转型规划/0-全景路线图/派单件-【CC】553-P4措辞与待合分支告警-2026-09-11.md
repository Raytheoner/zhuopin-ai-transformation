---
title: "派单件 · 【CC】#553 ⑵ P4 措辞更正 ＋ ⑶ 未并入分支超阈值告警（2026-09-11）"
created: 2026-09-11
status: 待派出
派出线: Cowork 业务总线 OP-0911-C
承接: 队列 §一 `#553`（⑴ 分类清单已进 master；本件＝⑵⑶）
授权: Shao Peishen 2026-09-11 答 `2a`（阈值＝B 类 ≥5 条且最老 ≥7 天即告警）
---

# 派单件 · #553 ⑵ P4 措辞更正 ＋ ⑶ 未并入分支超阈值告警

## 〇、这一条是怎么浮出来的

`#553` 立行原因：派单件长期写着「主仓 ff 由 sweep 收尾段或看护者收工时串行做（P4）」，而 sweep 里 grep `claude/op` 零命中——**留给 sweep ＝ 留给没有人**。⑴ 已做：134 条 `claude/*` 按 `#534` patch-id 三分类＝A 64（内容已在 master）／B 18（真未落地）／C 4（该删），清单在 `1-转型规划/0-全景路线图/分支分类清单-未并入master的claude分支patch-id三分类-2026-09-10.md`。

⑵ 本方 2026-09-11 复核：**不是纯 .md 件**。`opener骨架.md` L62 那句与 `0-学习与工具/工具-opener生成.py` L239–241 `SUBTASK_PUSH_NOTE` 逐字相同，且 `0-学习与工具/test_工具-opener生成.py` L808（`骨架与生成器契约`）断言 `canon_lines[-2] == M.SUBTASK_PUSH_NOTE` ⇒ 只改骨架即让该单测转红，**三处必须同一提交改**。

⑶ 阈值口径 Shao Peishen 2026-09-11 答 `2a`：**按 patch-id 只数 B 类，≥5 条且最老 ≥7 天即告警**。🔴 只数分支图会把 64 条 A 类假阴性算进去（清单 §七 已给定量）。

---

## 一、做什么

1. **⑵ 三处同一提交改 P4 句**（骨架 L62／生成器常量／单测若有原文断言），新句：「🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。」改完 `test_工具-opener生成.py` 全绿；`opener块lint.py` 对骨架自身重跑一次不得新增告警。
2. **⑶ sweep 新常驻类「未并入分支超阈值告警」**：对 `claude/*` 未并入分支逐条按 patch-id 判 A/B/C（可复用清单 §六 脚本逻辑，注意 `git cherry`／`patch-id` 对空提交与纯合并提交的处理与清单一致）；**B ≥5 且最老 ≥7 天**（按分支首个未在 master 的提交日期算）⇒ 告警一行：B 条数／最老天数／最老三条分支名；A／C 只附计数。指纹不变即静默（同第 13 类）。
3. **单测 ≥6 例 ＋ 变异 2 组**（见 opener 第 3 条）；🔴 只跑本类单测文件与 `test_工具-opener生成.py`。
4. **实弹**：`工具-落库sweep.py` 对该类 `--dry-run` 跑一次，回显贴进 `#553` 收工段。

## 二、不做什么

- 🔴 不合并、不删任何分支；不改 sweep 其它类；不动 `_reconcile_with_origin_and_push`；不改 5／7 阈值。
- 🔴 不自行 commit；不 ff master；acquire 被拒按「登记待补」写 `队列回写待补/*.json`。
- 不整跑 `test_工具-落库sweep.py`（471 条，超工具单次上限）。

---

## 三、plan 形态 opener

🔴 opener 由 `工具-opener生成.py --variant standard` 产出，未手抄。

### A1 · #553 P4 措辞与待合告警

粘贴端：CC ｜ 泳道：553-p4-unmerged-alert

```
[OP-0911-G]【CC】P4措辞与待合告警
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911g-553-p4-unmerged-alert`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0911-C
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0911G-P4措辞与待合告警。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/派单件-【CC】553-P4措辞与待合分支告警-2026-09-11.md 全文` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. ⑵ 三处同一提交改 P4 那句：opener骨架.md L62／工具-opener生成.py SUBTASK_PUSH_NOTE／若 test_工具-opener生成.py 有断言原文处一并改；新句＝「主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 工具-待合分支巡检.ps1 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，#553 更正）」；跑 test_工具-opener生成.py 全绿（含 骨架与生成器契约）
2. ⑶ 给 工具-落库sweep.py 加一条常驻类「未并入分支超阈值告警」：按 #534 patch-id 口径只数 B 类（真未落地）分支，B 类 ≥5 条且最老 ≥7 天即告警一行（列分支名／首提交日／最老天数）；A 类（内容已在 master）与 C 类（该删）不计、只在同一行附计数；指纹不变即静默（同第 13 类范式）
3. ⑶ 单测 ≥6 例（阈值边界 4/5、天数边界 6/7、A 类不计、静默指纹）＋ 🔴 变异 2 组（把 patch-id 比对换成 merge-base 判、把 7 天判去掉，各确认对应用例转红）；🔴 只跑本类相关单测文件与 test_工具-opener生成.py，不整跑 471 条（环境上限，见 #550）
4. 分类清单 §六 脚本可复用为 B 类判定的实现参考（1-转型规划/0-全景路线图/分支分类清单-未并入master的claude分支patch-id三分类-2026-09-10.md）；实弹跑一次 sweep 该类 --dry-run 并把回显贴进 #553 收工段

不做什么：
- 🔴 不合并、不删除任何分支——本行只让「不知道」变「知道」并告警，合与删是后续决策
- 🔴 不改 sweep 其它类、不动 _reconcile_with_origin_and_push；不改阈值 5/7 天（他 2026-09-11 答 2a 定）
- 🔴 不自行 commit；不 ff master；acquire 被拒按「登记待补」写 队列回写待补/*.json，不重试不绕锁
- 不整跑 test_工具-落库sweep.py 471 条；不改 opener块lint.py（P4 句它不判）
```

---

## 四、收工

回写 §一 `#553`（⑵⑶ 各一段，写清手段与回显）＋ 登记 §二 批次 `B-0911_553P4措辞与待合告警`。🔴 文件清单每个路径包反引号、反引号内只放仓库根相对路径。心跳收工带 `--done --batch B-0911_553P4措辞与待合告警`。**本行 ⑴⑵⑶ 齐后才可销号；⑵⑶ 任一未做即写明卡点，不含糊。**
