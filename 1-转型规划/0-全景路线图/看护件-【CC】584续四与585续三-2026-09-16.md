---
title: "看护件 · B-0916_584续四与585续三（两泳道并行）"
created: 2026-09-16
批次: B-0916_584续四与585续三
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 1a／2a 后续派）
---

# 看护件 · B-0916_584续四与585续三

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#584` | 续四：worktree 报告回收（⑶b）＋插件加载探针（⑷） | v2.ps1、包版本探针＋单测 |
| A2 | `#585` | 续三：followup-letter 迁移（⑵）＋扫描口径（⑶）；⑸ description 压缩已完成 | skills源码、.claude/skills、仓库外载体扫描 |

触碰区零交集；两条都不得碰 `.claude/settings.json` 与读侧守卫。

## 二、泳道

### A1 · §一 #584 续四

▶ 粘贴端：CC ｜ 泳道：op0916n-headless-parity-584e

```
[OP-0916-N]【CC】无头开场注入续四
【设置】执行环境：CC ｜ 分支：`claude/op0916i-headless-parity-584b`（既有分支续做，不新建） ｜ worktree：☑（op0916n-headless-parity-584e，新 worktree 检出该既有分支，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_584续四与585续三
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916N-开场注入续四。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #584（`python 0-学习与工具/工具-队列查询.py --row 584 --section 一`，以状态列末段最新口径为准）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 只做 ⑶b、⑷；v2.ps1 改动保留 `#581` 模型字段逻辑，差集回归与 master 对跑。
🔴 开工先 `git rebase master`（#584 分支的 opener骨架 冲突已由 Cowork 手工消解并 force push `dd01b5b`，应可纯快进；仍冲突即 PARTIAL 停）。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读（守卫会直接拒绝整读，别绕）；重输出交子代理。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short`，跑完即 commit。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916n-headless-parity-584e --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916n-headless-parity-584e --done --batch B-0916_584续四与585续三 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支（rebase 后用 `--force-with-lease`），不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

### A2 · §一 #585 续三

▶ 粘贴端：CC ｜ 泳道：op0916o-skill-home-585c

```
[OP-0916-O]【CC】skill归置续三
【设置】执行环境：CC ｜ 分支：`claude/op0916k-skill-home-585`（既有分支续做，不新建） ｜ worktree：☑（op0916o-skill-home-585c，新 worktree 检出该既有分支，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_584续四与585续三
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916O-skill归置续三。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #585（`python 0-学习与工具/工具-队列查询.py --row 585 --section 一`，以状态列末段最新口径为准）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 description 压缩附压缩前后触发词对照表写回 #585；`git mv` 迁移保留历史。
🔴 开工先 `git rebase master`（#584 分支的 opener骨架 冲突已由 Cowork 手工消解并 force push `dd01b5b`，应可纯快进；仍冲突即 PARTIAL 停）。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读（守卫会直接拒绝整读，别绕）；重输出交子代理。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short`，跑完即 commit。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916o-skill-home-585c --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916o-skill-home-585c --done --batch B-0916_584续四与585续三 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支（rebase 后用 `--force-with-lease`），不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】584续四与585续三-2026-09-16.md" -FullAuto -Yes -Detach
```
