---
status: 待执行
title: "开场prompt ·【CC】#334 保供看板「物料看板」视图 apply（design 已批准）"
created: 2026-08-20
执行方: CC
派出线: Cowork 全景路线图线
编号: OP-0820-C
来源: §四 #71 ／ §一 #334；design 三决策点 Shao Peishen 2026-08-19 09:47 答「按推荐」，2026-08-20 答「现在出派单件」(a)
---

# OP-0820-C ·【CC】`#334` 物料看板 apply

## 开场词（复制即用）

```
【OP-0820-C】CC · #334 保供看板「物料看板」视图 apply（design已批准,D4-a/D5-a/D6-a全按推荐）
【设置】执行环境：CC ｜ CC session：☑ 新建 ｜ worktree：☑ 新建 `sc8-material-board-apply` ｜ 分支：master ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-CC-334物料看板apply-2026-08-20.md 全文 ＋ openspec\changes\sc8-material-board-view\ 全部四件 ＋ 队列 §一 #334 全行（姚祖怡已给出完整列模板与逐列取值口径）＋ §一 #344（齐料日期已知缺陷，不在本范围）＋ 4-数字员工\采购部\SC8-客户订单交期智能承诺\CLAUDE.md ＋ CLAUDE.md §5 恢复上下文，然后 /opsx:apply。
```

## 现状（Cowork 2026-08-20 11:09 实测，可直接采信但欢迎复核）

- 变更包 `openspec/changes/sc8-material-board-view/` **四件齐**（proposal／design／tasks／specs），已在 master（commit `0b21ceb`）。
- **`tasks.md` 37 项全未勾** ⇒ 确实停在 design、未 apply。
- **`§一 #334` ＝ `[S:open][D:业]`**，待领。
- **机制类可动 WIP 现为 21／22**（2026-08-20 口径整理批后），**有余量**——本次若确需新立行不会被硬阻断，但**仍应先做协议〇.10 并入审核**（今日已实证：先审核往往发现本就该并入，墙根本不必翻）。

## design 三决策点（Shao Peishen 2026-08-19 09:47 答「按推荐」，全部 (a)）

| # | 问题 | 已定 |
|---|---|---|
| **D4** | 供应商名称取哪个源 | **(a)** 取未交 PO 的供应商、去重并列 |
| **D5** | 责任人列无数据源怎么办 | **(a)** **保留该列、显式标缺口** |
| **D6** | 品牌列无数据源怎么办 | **(a)** 同 D5 |

🔴 **D5／D6 的连带后果必须在页面上说清楚**：这两列**会是空的**，页面上要有明确的「暂无数据源」标注与一句原因说明。
**否则姚祖怡打开会以为是 bug，又要往返一次** —— 而她这两天已经因为符号无主语、docx 勾不动往返了两次，**第三次会真的伤到 `#67` 那个试点反馈 loop 的信任**。

## 🔴 四条范围红线

1. **不新起端口**（CLAUDE.md §5 硬约束）—— 挂在现有 `.51:8091` 保供看板的路由下，不另开服务。
2. **不改任何既有判定口径** —— 本项是**新视图**，不是改判据。**若建造中发现取数口径有分歧，登记、不顺手改**（同 `#266` 建造时对 `F02N.0224` 的处置：那次正确地单独立了 `#344`，而不是"看着像同一个根因就一起改了"）。
3. **齐料日期那条已知缺陷（`#344`）不在本范围** —— 其判例包已作为 **采购部#16** 发出、**正在等姚祖怡回件**。物料看板若要展示齐料日，**沿用现状口径并在页面标注该口径正在确认中**，不要抢先改。
4. **新场景 scaffold 的路径引导**：唯一被允许的样板见 `5-平台底座/zhuopin_platform/zhuopin_platform/bootstrap.py` 模块 docstring，**不要照抄既有场景**（`#345` 已机制化，CI `bootstrap-stub-lint` 会拦）。

## ⚠️ 两条时间/性能预期

- **6.1 那次全量重算约 15 分钟（SRM 限流）** —— 这一段会比较久，**不要以为卡死了**。
- 页面读取应走**快照优先 ＋ 显式重算入口**（同 SC2 的 D21：重算耗时不能挂在 HTTP 请求上）。

## 收口（按 CLAUDE.md §5 场景固定流程）

走到**第 7 步发布收口**：部署 `.51` ＋ 冒烟（`/api/ping`／关键页 200／一次全量重算）＋ 回滚 SOP 在位。
🔴 **逐项验活别只信脚本打印**：进程 `CreationTime` 真刷新、**从笔记本外部**实测 ping 200（不是只在 `.51` 本机测）、服务器侧文件 SHA256 与 master 逐字节一致。

🔴 **第 8 步跟进信只起草到 `⏳ 待你审`，绝对不要发**：
- **姚祖怡串行闸锁着** —— 采购部#16 于 2026-08-18 推送、**至今未闭环**（她 08-19 两次回件都是被我方交付缺陷挡住，不是答复）。
- **且 SC2 判例包（拟采购部#17）已在她前面排队。**
- ⇒ 本场景的通知信排在更后面，起草即可、登记 README 时状态列写纯字面值 `⏳ 待你审`。

## 收工

- 回写 **§一 #334**（apply 进展／部署结果）＋ **§四 #71 销号**。
- §二 登记批次，**文件清单每条必须是仓库根相对完整路径且反引号包裹**（2026-08-19 已因只包扩展名而静默漏 commit 一次）。
- **若任一验收未过，如实登记未完成、不要写 ✅**，并说明卡在哪一步。
- **收工报告的定夺项按新判据标注**：答完本 session 接着做的 ⇒ ⟨就地答⟩ 给选项；要另起 session／归他线／对外的 ⇒ **不给选项，只写「已登记 §四 #N」**；顶部加一行 `▶ 本报告 N 项定夺：X 项⟨就地答⟩，Y 项已登记队列待总线派发`。
