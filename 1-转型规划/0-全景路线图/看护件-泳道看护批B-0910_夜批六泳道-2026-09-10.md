---
title: "看护件 · 泳道看护批 B-0910_夜批六泳道（2026-09-10）"
created: 2026-09-10
status: 待派出
批次: B-0910_夜批六泳道
派出线: Cowork 业务总线 OP-0910-A
---

# 看护件 · 泳道看护批 `B-0910_夜批六泳道`

## 〇、口径

- **触发**：Shao Peishen 2026-09-10 21:3x 原话「**采购和质量有新回灌，我们的项目目标永远是尽早建设完成，所以如果有队列任务可领，就成能建尽建，如果有前置阻塞，能清尽清。我已离开公司，我们是否再来一次泳道看护？**」规则正本＝`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（v2.2）。
- **D1 四档现取**（`工具-泳道看护状态机.py criteria`，非手抄）。**判据未覆盖按 🟡 停。**
- **前置四查（2026-09-10 21:39–21:5x 本机现取）**：① LAN 探针 ＝ **⛔ off-LAN（三项未齐）** ⇒ 排除依赖内网的活；② `git fsck` 健康，`master`＝`origin/master`＝`4c28a85`，`0/0`；③ 编辑锁**无占用**；④ 上一批 `B-0910_起棒基建` summary 已出（停 0／转出 0／终态 1 条）。
  > 🔴 **起棒时须自己重跑一次**——本日 master 已在 3 小时内走过 `a80e6a7`→`ace44d7`→`cfeecb9`→`4c28a85`（同 §一 `#522` 现取表过期守）。
- **上限**：默认 10；本批实际入批 **6 条泳道**。
- **交付模式**：🔴 **无头**（`工具-opener批处理执行v2.ps1 -Detach`）。**为什么不是默认的「看护者单粘贴」**：他此刻已离开公司、不在 CC 前，单粘贴档要求他亲自粘一段，本批做不到；且他 2026-09-09 已立常驻纪律 `UPS5:6`「凡本方能执行的直接执行、不生成需要他动手的中间件」，当日已有三棒按无头起。**§三bis 的看护者开场词照旧保留**——它是切回单粘贴档的逃生口。

## 一、硬边界

1. 🔴 **本批与在跑的 `OP-0910-S`（`550-sentinel-enforce`，21:15 起）并行**。已逐条比过触碰区：`S` 碰 `工具-opener批处理执行v2.ps1`／`工具-opener生成.py`／`opener骨架.md`／`openspec/changes/opener-id-claim-semantics/` ⇒ **`#522`（碰 opener骨架）与 `#531`／`#487`（碰生成器与骨架）本批一律不入**；`#553` 只做第 ⑴ 项（只读 git），其 ⑵ 改 opener骨架、⑶ 加 sweep 告警类**本批不做**。
2. 🔴 **⏭️ 档（`.51` 部署及触碰生产服务）本包不执行**——命中即 `transfer-out` 标注去向、跳过、继续其余，**不进 `OPENER_PARTIAL`**。本批 `#538`／`#506` 一线尤其注意：**SC2 生产页面属生产服务，只改 worktree 内代码、不部署。**
3. 🔴 **🔴 档（对外发送／L2 门禁签字／合规红线变更／ASIL C-D）永不代办**。`quality-intake-13` 泳道**只拆件回灌，不起草、不发送任何信件**。
4. 🔴 **看护会话不修工具**：跑批期间发现机制缺陷只登记＋出派单，不在本会话修；唯一例外＝本批会因该缺陷失败且修补 ≤10 行。
5. **看护件落档失败即全停。**

## 一bis、LAN 留步清单（off-LAN，本批排除）

| 行 | 为什么留步 |
|---|---|
| §一 `#240` | 企微自建应用凭据申请——需外部门户操作 |
| §一 `#416` | 企微回件链路三缺陷——排查须触碰生产 aibot 服务（⏭️ 档） |
| §一 `#506` | SC2 页面基准日冻结——修复要动生产页面（⏭️ 档），且属他指定的「只报不改」在跑行 |
| §一 `#554` | 拆件覆盖面差集——修法候选之一要动常驻 listener（⏭️ 档），须先定路线 |

## 二、次序与并行矩阵

**波 1（并行 4，触碰区零重叠）**

| 泳道 | 承接 | 触碰区 |
|---|---|---|
| `quality-intake-13` | §一 `#542`／`#543`／`#546`／`#547`／`#548` | `6-人才与组织/部门AI专员跟进/`（README 状态回写）＋ 队列行 ＋ 新建拆件报告 |
| `538-sc2-detail` | §一 `#538` | `4-数字员工/采购部/SC2-采购周报自动生成/`（worktree 内） |
| `552-k2-tool` | §一 `#552` | 新建 `0-学习与工具/工具-队列行K2外置.py` ＋其单测；`队列行日志/`（只追加） |
| `553-branch-triage` | §一 `#553` ⑴ | 只读 `git`；产出清单落 `1-转型规划/0-全景路线图/` |

**波 2（并行 2）**

| 泳道 | 承接 | 触碰区 | 硬序 |
|---|---|---|---|
| `513-writeguard` | §一 `#513` | `0-学习与工具/工具-共享文档编辑锁.py`＋其单测 | 🔴 **开工先核 `claude/op0908l-opener-lint-489-ef8652` 是否已进 master**（按 `#534` patch-id 口径看产物，不看分支图）；未进则只出方案不动码 |
| `534-patchid` | §一 `#534` | `0-学习与工具/工具-泳道看护状态机.py`（`pause` 路径）＋新建比对工具 | 与 `#529` 同碰状态机；`#529` 已由 `OP-0910-R` 做完，无在跑冲突 |

🔴 **`513-writeguard` 与 `552-k2-tool` 分处两波**：前者改编辑锁写侧校验，后者的产出要经 `edit-row` 写队列，**同波会让后者跑在一个正在被改的守卫上**。

## 二bis、每条泳道的「做什么／不做什么」

🔴 **按规则正本步骤 2：这些一律写进件与队列行，不进 opener**（opener 恒三行 ＋ 哨兵行）。各泳道开工先读本节自己那一格。

### `quality-intake-13`（§一 `#542`／`#543`／`#546`／`#547`／`#548`）

**做什么**
1. 先读 `.claude/rules/跟进信与专员.md` ＋ `6-人才与组织/CLAUDE.md`。🔴 **人名性别一律查 `6-人才与组织/人员名录-称谓与性别-正本.md`，禁从名字推断**；正本之外一律「该专员／其／对方」。
2. 拆件回灌五条陈忱入信行：`#542`／`#543` ＝ 2026-09-10 09:21／10:14 两条**文本反馈**；`#546`／`#547`／`#548` ＝ 13:39–13:40 三个 zip（`质量部#13` 回复·合格／不合格1／不合格2）。
3. 🔴 **zip 必须解开实读全部内容**（含内嵌图与 `word/comments.xml` 批注），**不得只看文件名**。docx 里的复选框勾选状态**不能用 `python-docx` 的 `cell.text` 判**——那是假阴性，已因此向专员做出过一次不实指摘（§一 `#133`）；须读 `word/document.xml` 原文找 `☒`。
4. 产出拆件报告落 `1-转型规划/0-全景路线图/`，回灌相应队列行并销号；需 Shao Peishen 的项登记 §四。
5. 🔴 **收工请单独写一行**：这五件里**有几件本可由事件驱动自动吃掉、几件落在差集**——那是 §一 `#554` 修法的定量输入。

**不做什么**
- 🔴 **不起草、不发送任何对外信件**（红线）；跟进信 README 只做状态回写，不新建信件。
- 🔴 **不改 `质量部#13` 的「无需回复·已闭环」判定**——那是既有裁定，改它属新裁决。
- 🔴 不改他人在办行；不自造 🛑 豁免；不自行 commit。

### `538-sc2-detail`（§一 `#538`）

**做什么**
1. 先在 worktree 内**复现「下单行数 187」这个数**，写出计算过程（底数 `sc2/metrics.py:137` 的 `order_line_count`，行级来源 `dataset.order_lines`，见 `metrics.py:215-219`）。
2. 新建明细导出能力——现测 `sc2/webapp.py` 无 `csv`／`export`／`明细`／`detail` 任何命中，**能力为零、须新建**。
3. 单测覆盖；🔴 **变异检验**：把口径改错一处，确认单测真的转红。

**不做什么**
- 🔴 **不碰 §一 `#506`（页面基准日冻结）**——他指定的「只报不改」在跑行；本泳道不改基准日逻辑。
- 🔴 **不部署 `.51`、不碰生产页面**（⏭️ 档）；如判定必须部署才能验，跑 `transfer-out` 标注去向后**跳过该项、继续其余**，不进 `OPENER_PARTIAL`。
- 🔴 不自行 commit；不 ff master。

### `552-k2-tool`（§一 `#552`）

**做什么**
1. 建 `0-学习与工具/工具-队列行K2外置.py`：读单格 → 按段切 → 选段外置 → 追加写 → md5 → 回读两侧 → 产出新格 JSON。
2. 🔴 **硬性三条**：外置件**只追加、绝不覆盖**（写前 `exists` ＋ 体积下限断言）；**写后回读两侧**（新段命中 ＋ 旧内容锚点仍在）；**段结构先断言再动手，不猜**。
3. 幂等：已在外置件中的段不重复追加；新格 <4096 B 才产出，否则报错不写。
4. 单测 ＋ 🔴 **变异检验**：把「只追加」改回覆盖，确认用例真的转红。
5. **参考 2026-09-10 的手写原型两次实撞**（都写进 `#552` 行内）：保留段选错致改后仍超闸；重跑会把同段追加两次。**两条都要进验收单。**

**不做什么**
- 🔴 不碰队列真身写入路径（仍走 `工具-共享文档编辑锁.py edit-row`）；不自行 commit。

### `553-branch-triage`（§一 `#553`，🔴 **只做第 ⑴ 项**）

**做什么**
1. 按 `#534` 的 **patch-id 口径**逐条比对 134 条 `claude/*` 分支，分三类：**内容已在 master／真未落地／该删**。
2. 🔴 **只读 git，一个分支都不合、不删**；产出清单落 `1-转型规划/0-全景路线图/`。

**不做什么**
- 🔴 **不做该行的 ⑵（改 `opener骨架.md` P4 措辞）与 ⑶（新增 sweep 告警类）**——⑵ 的触碰区与在跑的 `OP-0910-S` 重叠。
- 🔴 **收工不得表述为「已清理 134 条分支」**——本泳道只产出「知道」，不产出「处置」。
- 🔴 不合并、不删除任何分支；不自行 commit。

### `513-writeguard`（§一 `#513`）

**做什么**
1. 🔴 **开工第一件事**：核 `claude/op0908l-opener-lint-489-ef8652` 是否已进 master——**按 `#534` patch-id 口径看产物，不看分支图**（`#455`／`#341` 已证分支图会骗人）。**未进则本泳道改为只出方案、不动码，如实登记。**
2. §二 文件清单写侧守卫补齐**反面**：合法路径**未包反引号即拒**（现有守卫只拦「反引号里不是路径」）。
3. 单测 ＋ 🔴 变异检验。

**不做什么**
- 🔴 **不改现有守卫的正面判据**——那条是对的，2026-09-10 当日实测拦下本方 5 次。
- 🔴 不自行 commit；不 ff master。

### `534-patchid`（§一 `#534`）

**做什么**
1. 建 patch-id 比对工具：判「某活是否已做」**看产物在不在 master，不看分支 SHA 谱系**；接进 `工具-泳道看护状态机.py` 的 `pause` 路径。
2. 🔴 **实证靶**：2026-09-10 用三点 diff `master...分支` 误判 `#341`「+1452 行未合入」，实为 **100% 已在 master**；**本工具须在该输入上判对**。
3. 单测 ＋ 🔴 变异检验。

**不做什么**
- 🔴 **不改 `#529` 已落地的 `check-timeout` 语义**（同碰状态机，**只加不改**）。
- 🔴 不自行 commit；不 ff master。

## 三、泳道 opener（全部由 `工具-opener生成.py --variant subtask_lane` 产出，未手抄；哨兵行由生成器自动注入＝`#550` 首次真用）

### A1 · 质量部五行拆件回灌（#542/#543/#546/#547/#548）

粘贴端：CC ｜ 泳道：quality-intake-13

```
[OP-0910-T]【CC】质量五行拆件
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910t-quality-intake-13`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_夜批六泳道）
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_夜批六泳道-2026-09-10.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · SC2 周报数字对不上 ＋ 明细导出（#538）

粘贴端：CC ｜ 泳道：538-sc2-detail

```
[OP-0910-U]【CC】SC2明细导出
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910u-538-sc2-detail`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_夜批六泳道）
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_夜批六泳道-2026-09-10.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · K2 外置工具（#552）

粘贴端：CC ｜ 泳道：552-k2-tool

```
[OP-0910-V]【CC】K2外置工具
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910v-552-k2-tool`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_夜批六泳道）
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_夜批六泳道-2026-09-10.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A4 · 134 条分支 patch-id 分类（#553 仅第 ⑴ 项）

粘贴端：CC ｜ 泳道：553-branch-triage

```
[OP-0910-W]【CC】134分支分类
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910w-553-branch-triage`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_夜批六泳道）
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_夜批六泳道-2026-09-10.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A5 · §二 写侧守卫反面补齐（#513）

粘贴端：CC ｜ 泳道：513-writeguard

```
[OP-0910-X]【CC】513写侧守卫
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910x-513-writeguard`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_夜批六泳道）
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_夜批六泳道-2026-09-10.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A6 · patch-id 比对机器守（#534）

粘贴端：CC ｜ 泳道：534-patchid

```
[OP-0910-Y]【CC】patchid比对守
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910y-534-patchid`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_夜批六泳道）
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_夜批六泳道-2026-09-10.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词（**本批未用**——无头档；保留作切回单粘贴档的逃生口）

```
[OP-0910-Z]【CC】看护看护夜批六泳道
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_夜批六泳道）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0910Z-看护看护夜批六泳道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_夜批六泳道-2026-09-10.md 全文` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0910_夜批六泳道` **现取**，不手工数。

🔴 **等待时长只认 `reports/lane-watch-state.json`（由 summary 现取），会话不得自算。**

🔴 **心跳一律跑命令写**，收工带 `--done --batch B-0910_夜批六泳道`：
```
python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane <泳道标识> --done --batch B-0910_夜批六泳道 --text "产出落点：…"
```
**别传 `--repo-root` / `--heartbeat-file`，它没有这两个参数。**

🔴 **预计等待超过 10 分钟的步骤**（如全量回归）**须在等待期内再写一行**「仍在等 X，预计还要 N 分钟」——否则看门狗 30 分钟判读线在长回归面前会常态失效。

### 本批收工时必须一并注明的三件

1. **`quality-intake-13` 是本批唯一的业务回灌**，它拆的五行正是 §一 `#554` 的实证——收工请写明**这五件里有几件本可由事件驱动自动吃掉、几件落在差集**，那是 `#554` 修法的定量输入。
2. **`553-branch-triage` 的清单只是「知道」，不是「处置」**——收工不得表述为「已清理 134 条分支」。
3. **本批与 `OP-0910-S` 并行**：各泳道收工回写队列会与它抢锁；🔴 **acquire 被拒即按「登记待补」写 `队列回写待补/*.json`，不重试、不绕锁。**
