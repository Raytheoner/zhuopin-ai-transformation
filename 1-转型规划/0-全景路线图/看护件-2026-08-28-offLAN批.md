---
status: 待发
title: "CC Desktop 看护件 · 2026-08-28 off-LAN 批（3 条）"
created: 2026-08-28
派出线: Cowork 环境总线（OP-0828-N）
---

# 看护件 · 2026-08-28 off-LAN 批

## 〇、这一批的选件口径：**off-LAN**

Shao Peishen 当前**不在厂内网**。所以本批**刻意只收「不需要 `.51`、不需要 LAN 内数据」的活**。

**被 off-LAN 排除掉的**（本批不领，别去碰）：`#96` `.51` 部署清单的 CC 半边／`#380` 李姣龙通道真实冒烟／`#387` 归档回执四项需人在场／`#399` 补件端到端真实发送／`#358` `.51` audit 309 MB 轮转／`#419` 运维逃生通道验活／`#410` 验收前提阻于 `§四 #132`⑴。
📌 **公网可达的不算 off-LAN 障碍**：企微（`wss://openws.work.weixin.qq.com`）与源系统（`erp.equalitytec.com:4443`／`openapi.xiekeyun.com`）**走公网、off-LAN 亦通**（`OP-0828-U` 当日实测）。

---

## 一、🔴 硬边界（沿用「落地后批」§二，逐条读完再动手）

> **总原则：看护者有「发现权」，没有「派工权」。**

1. 🔴 **本批子任务数量上限 ＝ §三 里写好正文的条数（3 条）。起第 4 条即违规。** 只需数数，不需判断。
2. 🔴 **不得给任何新发现取 opener 号、写 opener 正文、起子任务去做它。** 新发现只许两个落点：走编辑锁在队列登记一行 ／ 写进收工汇总。
3. 🔴 **发现紧急到「等不到下一批」——停下来问 Shao Peishen**，不得代他把「紧急」判成「所以我现在就派」。
4. 🔴 **CC 自派取号空间是 `OP-MMDD-C1/C2…`**，不得占用字母序。本批不预期出现自派号。
5. 🔴 **不得替子任务改判**：报告与本件所述对不上时，**以子任务的实测为准，停下来报**。
6. **收工即停，不找活。** 三条收工 ＋ 汇总写完 ＝ 结束。
7. **看护者不做**：不改代码、不写子任务的队列行、不 commit 别人的产出、不动任何计划任务。
8. ✅ **`set_session_title` 那一行已自带子任务例外句，你什么都不用删**（2026-08-28 根治，见 `专线opener模板库.md` §〇 补充三之三）。照「原样传」即可。

### 🔴 本批新增两条边界（今天现学的）

9. 🔴 **不得部署 `.51`、不得改 `.51` 上任何配置、不得触发其重算** —— 本批是 off-LAN 批，物理上也做不到；**若某条子任务报告「已部署」「已冒烟」，那一定是假的，当场停下来报。**
10. 🔴 **「他没做／它没答」这个结论，永远只能是「我这个读法没查到」的下位。** 本日该形态犯了四次、每次代价都是对人或对他线做出不实指摘。⇒ **任何子任务在报告里断言某人/某线没做某事之前，必须写出它用什么读法查的、覆盖了哪些位置。** 写不出就不许下这个断言。

---

## 一bis、🔴 进展可见性：心跳文件（Shao Peishen 2026-08-28 要求「别让看护看上去像静止」）

### 先说清一个做不到的部分，不给假的「实时」

**看护者用 Task/Agent 起子任务时那个调用是阻塞的** —— 三条并行起下去，它在三条全返回之前**什么都干不了**，包括打印进展。⇒ **「看护者实时播报」在当前形态下做不到**，说能做就是骗人。

**能做到的是换一个观察者**：**让每条子任务自己往心跳文件写，观察权就从看护者手里挪出来了** —— Shao Peishen 或 Cowork 随时可以看，不必等任何人返回。

### 心跳约定（三条 opener 里都写了，此处是正本）

- 落点：`reports/lane-heartbeat/<泳道编号>.md`（如 `reports/lane-heartbeat/OP-0828-X.md`）。**`reports/` 已被 gitignore，不污染版本库、不进任何批次。**
- 写法：**每完成一个里程碑追加一行**，格式 `HH:mm:ss 本地 ｜ <一句话，做完了什么/正在做什么>`。
- 🔴 **首行必须在开工后 1 分钟内写出**（内容＝「已开工，正在读 xxx」）—— **否则「还没开始」与「卡住了」长得一模一样**。
- 🔴 **停下来问人之前必须先写一行**，注明「等 Shao Peishen 答 xxx」——**这一条最要紧**：子任务停下来等人时，从外面看和死掉没有区别。
- 收工写最后一行 `DONE ｜ <产出落点>` 或 `STOPPED ｜ <停在哪一步、为什么>`。

### ⚠️ 下面这条只读命令**不是首选，下一棒不要照抄**（Shao Peishen 2026-08-28 当场否掉）

**他的原话：「另起一个 Terminal 观察泳道进展不是我想要的解决方案。」** 🔑 **判据：一个要他自己去开终端轮询的方案，是把成本又推回给了他** —— 与「凡方案的代价落在他身上而非我方，先退回重想」是同一条。
**本批的实际办法**：他在 Cowork 说一句「进展？」，由 Cowork 读心跳文件回报，**他零动作**。
**下一批的真解法**：心跳推企微（见 §一ter）。
下面这条命令**只作兜底**（Cowork 不在场、又必须自己看时）：

▶ **执行端：本机 PowerShell** ｜ **无需提权**

```powershell
Get-ChildItem C:\Dev\zhuopin-ai\reports\lane-heartbeat -Filter *.md | ForEach-Object {
  "===== $($_.BaseName)  (最后写入 $($_.LastWriteTime.ToString('HH:mm:ss'))) ====="
  Get-Content $_.FullName -Tail 6
}
```

⚠️ **判读**：某条泳道的心跳**超过 10 分钟没长**，才值得怀疑；**刚起活时没有文件是正常的**（还没写第一行）。🔑 **但「文件一直不出现」与「文件出现后不再长」是两回事** —— 前者多半是它还没跑到写心跳那步，后者才是真卡住。

---

## 一ter、🔴 下一批要做的真解法：心跳推企微（本批不做，已登记）

**本批到此为止**（Shao Peishen 2026-08-28「这次泳道看护就这样了出发吧」）。**下一批必须把观察这件事从他手里彻底拿走**：

- **子任务的关键事件直接推企微**，走**已有**通道（`0-学习与工具/发企微.py` 或 `5-平台底座/wecom-aibot-service`），**不新建通道**。
- 🔴 **只推四类，防刷屏**：`开工` ／ `停下来等人`（**最要紧的一类**——子任务停下来等人时从外面看和死掉没区别）／ `完成` ／ `失败或停手`。**中间里程碑仍只写心跳文件，不推。**
- **推给谁**：私信 `ShaoPeiShen`；是否同时进运维群由他定（默认不进，避免把运维群变成噪音源）。
- 🔑 **判据（这条比实现更要紧）**：**进展的推送方向必须是「找到他」，不是「等他来找」。** 本项目已有两个反例——`LAN留步项捞出清单` 存在一个月无人扫、未闭合产出扫描器建成后没有任何轮次调用它；**它们都不是没写，是写在了一个不会主动找人的地方。**
- ⚠️ **与 `OP-0828-Z` 的关系**：`Z` 正在做「回 LAN 翻转推企微」，**同一条推送通道、同一族设计** ⇒ **下一批做心跳推送时先读 `Z` 的产出，复用它选定的通道与防抖写法，不要另造一套**（两处用不同机制，下次会各坏各的）。

---

## 二、泳道矩阵（三条触碰区两两零重叠 ⇒ 可并行，错峰约 2 分钟，无硬序）

| 泳道 | 成员 | 触碰区 |
|---|---|---|
| `openspec` | `OP-0828-X` | `openspec/` 变更包（**只出 propose＋design，不改任何生产代码**） |
| `lint` | `OP-0828-Y` | `0-学习与工具/` 下 opener-lint 工具族 ＋ 其单测 |
| `sweep` | `OP-0828-Z` | `0-学习与工具/工具-落库sweep.py`、`工具-未闭合产出扫描.py` ＋ 其单测 |

✅ **本批刻意不排编辑锁那一族**（`#341` 队列写侧按域路由 ／ `#324` release 校验⑥ 误报）—— 它们都改 `工具-共享文档编辑锁.py`，**而所有 session 收工都要用它**，会重新引入「某条必须最后收工」的硬序。**留到下一批单独排、独占收工窗口。**

### 看护 opener（复制即用）

▶ **执行端：CC** ｜ **无需提权**

```
[OP-0828-N-W3]【CC】2026-08-28 off-LAN 批 · 泳道看护（调度三条，不亲自动手，不自派）
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☐ 不新建（看护者只读与调度）｜ 分支：master ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828W3-offLAN批三条看护。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 1-转型规划/0-全景路线图/看护件-2026-08-28-offLAN批.md 全文。🔴 §一 硬边界十条先逐条读完再动手。
【前置闸】开工先核 git fetch origin 后 origin/master..master 与 master..origin/master 均为 0；不为 0 就先停下来报，不要自己去 ff。
【你的职责】① 按泳道起活（§三 三条，各起一个 Task/Agent 子任务，isolation: "worktree"，把对应 opener 正文原样作为 prompt，🔴 不要改写、也不必删任何行）；② 盯进展与产出；③ 异常或需拍板处停下来问 Shao Peishen；④ 全部收工后给一份汇总。
🔴【进展可见性，本批新增，见看护件 §一bis】起活前先 New-Item -ItemType Directory -Force reports\lane-heartbeat；**起每一条之前先打印一行「▶ 起 <编号>｜触碰区｜预期产出」**，让他看得见你在推进。
🔴 **一句你必须对他说实话的话**：你用 Task/Agent 起子任务时那个调用是**阻塞**的——三条起下去，在它们全返回之前你什么都干不了，**包括播报**。所以**不要承诺"我会实时汇报"**。真正的进展在心跳文件里：reports\lane-heartbeat\OP-0828-{X,Y,Z}.md，**他随时可以自己看，不必等你**。起活前把这句话连同那条只读命令一起告诉他（命令见看护件 §一bis）。
🔴 **每条子任务返回时立刻播报那一条的结果，不要攒到三条都完再说**；并把该条心跳文件的**全文**贴出来——那是这条泳道中途发生过什么的唯一记录。
【🔴 计数边界】本批子任务上限 ＝ 3 条，起第 4 条即违规。新发现只许「队列登记一行 ＋ 写进汇总」。
【🔴 off-LAN 边界】Shao Peishen 当前不在厂内网。任何子任务若报告「已部署 .51」「已冒烟」「已改 .51 配置」，那一定是假的 —— 当场停下来报，不要采信。
【看到这些要停下来问他】① 任何要动 .51 或生产服务的写动作；② 任何对外发送（专员、真实群）；③ OP-0828-X 若发现 design 必须改动生产代码才写得下去；④ 任何子任务想动 0-学习与工具/工具-共享文档编辑锁.py（本批刻意不排它，留下一批独占窗口）；⑤ 子任务报告与本件所述对不上时——🔴 以实测为准、停下来报，不得替它改判。
【收工汇总】每条一行：编号／状态（完成·部分·失败）／产出落在哪个分支或文件／还留了什么。🔴「部分完成」要写清哪一步停的、为什么。另起一节「本批新发现（已登记队列，未派工）」。**再另起一节把三份心跳文件全文贴出**（那是中途过程的唯一记录，收工汇总只有结论、没有过程）。最后跑一次 python "0-学习与工具/工具-未闭合产出扫描.py"，三形态数字贴出来。
🔴 **汇总里必须点名：哪几条子任务没写心跳、或心跳中断超过 10 分钟** —— 心跳是本批新加的要求，**不检查它就等于没加**。
【收工即停】三条收工 ＋ 汇总写完即结束，不再找活。
开干前问我 2-3 个澄清。
```

---

## 三、三条 opener 正文

### `OP-0828-X`【CC】`#366` 闸的开合系统性根治 —— 走 openspec propose ＋ design

```
[OP-0828-X]【CC】闸的开合系统性根治 —— 走 openspec propose ＋ design
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N ／ 看护者 OP-0828-N-W3
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828X-闸开合根治openspec。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。

🔴 本件只出 openspec propose ＋ design，**不实现、不改任何生产代码**。理由：它改的是全项目口径（跟进信状态语义 ＋ 串行闸判据），命中 CLAUDE.md §5 openspec 三问第 ① 条。

先读 ① 机制队列 §一 #366 末段（2026-08-28 追加的根因与三层修法，本件全部依据）② 1-转型规划/0-全景路线图/拆件-质量部10与采购部19回件-2026-08-28.md §〇（实撞现场）③ 0-学习与工具/工具-跟进闸查询.py ④ 0-学习与工具/工具-共享文档编辑锁.py 的 release 串行闸守卫（§一 #308 子项 G）⑤ 6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md 的状态语义正本。

【根因，一句话】闸判的是「状态字段长什么样」，而它想判的是「上一封的往返真的闭环了没有」——中间那一步（有人真的读了回件并把结论回灌进队列）没有任何机器可验证的痕迹，于是机器用一个自己写的字段冒充了它。⇒ 闸依赖的判据，正是它要守护的那件事的自陈。

【三层修法，逐层写进 design】
① 拆字段：`🤖 已归档待拆件` 机器可写（文件到了是它有资格断言的）；`📥 已回件并回灌` 只能由拆件动作写。
② 🔴 终态自带可核引用（本案核心）：写 `📥` 时必须声明被回灌的队列行号；闸判据改为「状态＝📥 **且** 那些行确实在该回件到达之后被改过」（git 可验）。**这一步是唯一能消掉条件的改法** —— 不是要求人别乱写终态（人守必失守），是让终态自己带证据，机器想伪造就得真去改那些行。
③ 闸增第四态 `🔴 待拆件`：现状只有开/关，「回件到了没人拆」与「信还没发」被压成同一态；该态一旦存在，「回件躺着没人拆」就从隐形变成会被值周巡检抓到的状态。

【design 里必须回答的四个问题，不要绕过】
⑴ 存量那些已写 `📥` 但没有行号引用的历史行怎么办？（🔴 建议不追改，判据对历史行降级为「按旧口径视为已闭环」并显式标注，但要说清这意味着什么）
⑵ 「那些行确实被改过」如何判？git 时间戳 vs 行内锚点，各自的失效场景是什么？（⚠️ 已知坑：rebase 会换 commit 号、worktree 副本不是权威副本、`git cherry` 判「不在」不可信）
⑶ 配套那条 sweep 告警（扫「未经人工逐字确认」超 N 小时未见回灌证据）的判据**怎么被关掉**？🔴 关不掉的告警等于噪音，两侧都要设计。
⑷ 四态迁移期怎么过？S4 桥二改写新态后，旧的读 `📥` 的消费方（闸查询工具、release 守卫、每日派发任务）有几处、各自怎么兼容？

【边界】🔴 只出 propose ＋ design，`openspec validate --strict` 要过；不实现、不改生产代码、不动计划任务、不对外发送、不碰 .51。写队列走编辑锁。🔴 实测与本件所述不符，以你的实测为准，停下来如实报。
🔴【心跳，必做】每完成一个里程碑，往 reports/lane-heartbeat/OP-0828-X.md 追加一行：`HH:mm:ss 本地 ｜ <一句话>`（时刻用本机 Get-Date 当场取）。**开工后 1 分钟内必须写出首行**（内容＝「已开工，正在读 xxx」）——否则「还没开始」与「卡住了」长得一模一样。🔴 **停下来问人之前必须先写一行**，注明「等 Shao Peishen 答 xxx」：子任务停下来等人时，从外面看和死掉没有区别。收工写最后一行 `DONE ｜ <产出落点>` 或 `STOPPED ｜ <停在哪一步、为什么>`。（reports/ 已 gitignore，不进任何批次。）
【收工】回写 §一 #366（变更包名与落点、design 是否已 validate 通过），登记 §二 批次（先查同日已用，前缀不得撞号）。单列一行：「design 里那四个问题各答了没有」。开干前问我 2-3 个澄清。
```

### `OP-0828-Y`【CC】`#284` opener 块 lint —— 一条 lint 收两个失效形态

```
[OP-0828-Y]【CC】opener 块 lint：缺 set_session_title ／ 有它但缺子任务例外句
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N ／ 看护者 OP-0828-N-W3
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828Y-opener块lint双向告警。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。

先读 ① 机制队列 §一 #284（阈值计数台账，2026-08-28 追加段就是本件的需求正本）② 1-转型规划/0-全景路线图/专线opener模板库.md §〇 补充三 与 补充三之三（两个形态的成因与标准写法）。

【要建的：一条 lint，一次收两个失效形态】
扫 .md 里的 opener 代码块：
① 块内含「【设置】」而**无** set_session_title ⇒ 告警。**旧形态**：session 名丢编号，2026-08-27 一天欠 17 次，已远超规则退休制阈值（人守 3 次即须机制化）。
② 块内**有** set_session_title 而**无**子任务例外句 ⇒ 告警。**新形态**：Task/Agent 子任务执行它时 "self" 解析到父 session，把调度它的那条会话改名，**调用成功、无报错**（2026-08-28 实撞，见补充三之三）。

【硬要求】
- 🔴 **两侧都要能关掉**：写对的 opener 块必须不报；把一个报警的块改对之后，告警必须自动消失。**关不掉的告警等于噪音**，本项目已有先例。
- 🔴 **先在全库跑一遍，把现存命中数如实报出来**（预期不少，历史件很多）。⚠️ **历史已执行/已归档件按「历史记录不追改」不得改**，所以 lint 要能区分「当前在用件」与「历史件」——判据你定，但**必须写出你用什么区分的**，不许靠目录名猜。
- 配单测：两个形态各至少一条正例、一条反例。
- 落点你定（新建独立 lint，或并入既有 lint 工具），**但要说明为什么选那个落点**。

【边界】🔴 不改任何 opener 正本内容（只做检测，不做自动修复）；不动计划任务；不碰 .51；不对外发送。🔴 不得碰 0-学习与工具/工具-共享文档编辑锁.py（本批刻意不排它）。写队列走编辑锁。🔴 实测与本件所述不符，以你的实测为准。
🔴【心跳，必做】每完成一个里程碑，往 reports/lane-heartbeat/OP-0828-Y.md 追加一行：`HH:mm:ss 本地 ｜ <一句话>`（时刻用本机 Get-Date 当场取）。**开工后 1 分钟内必须写出首行**。🔴 **停下来问人之前必须先写一行**并注明在等什么。收工写 `DONE ｜ <产出落点>` 或 `STOPPED ｜ <停在哪一步、为什么>`。（reports/ 已 gitignore，不进任何批次。）
【收工】回写 §一 #284（本条从人守转机制守的落点、全库现存命中数、两侧可关掉的实测），登记 §二 批次。单列一行：「全库扫出 N 处，其中当前在用件 M 处、历史件 K 处（区分判据＝什么）」。开干前问我 2-3 个澄清。
```

### `OP-0828-Z`【CC】`#422` 扫描器接入轮次 ＋ `#425`⑶ 第 8 类告警的早退盲区

```
[OP-0828-Z]【CC】未闭合产出扫描器接入轮次 ＋ 第 8 类告警的整轮早退盲区
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N ／ 看护者 OP-0828-N-W3
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828Z-扫描器接线与早退盲区。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。

先读 ① 机制队列 §一 #422（扫描器接入轮次，`OP-0826-U` 收工登记的缺口）② §一 #425 全行（尤其 `OP-0828-Q` 的 ④⑤⑦ 与本班 Cowork 那段自我更正）③ 0-学习与工具/工具-落库sweep.py 与 0-学习与工具/工具-未闭合产出扫描.py。

【一 · #422 扫描器接入轮次】它已建成并全绿，**但目前没有任何轮次会调用它** —— 工具建好了没人跑，等于没建。
行内两个决策点：⑴ 选定轮次载体并接线（sweep 常驻告警族 ／ 独立计划任务，二选一，**须给理由**）；⑵ 🔴 **一次真实验活**：构造一个真的未闭合产出，看它真的报出来一条，再把它补做掉、看告警真的解除 —— **两侧都验，只验一侧等于没验**；⑶ 回 LAN 的 off→on 翻转至少经历一次真实翻转再确认（🔴 **本次 off-LAN，这一条做不到，如实登记为留步，不要假称已验**）。
🔴 **接线时不得新建计划任务**（本项目纪律：不修改/停用/删除任何计划任务；新建须 Shao Peishen 批）。若你选的方案要新建计划任务，**停下来报，别自己建**。

🔴🔴 **本件最要紧的一条硬要求（Shao Peishen 2026-08-28 当日提出：「所有 LAN 留步请记好，周一我回 LAN 自动提醒或触发我补上」）**：
**接线必须做到「LAN 探针由 off 翻到 on 的那一刻，主动把形态 1 的 LAN 留步清单推送给他」** —— 推送通道用**已有的企微机器人**（`5-平台底座/wecom-aibot-service`，私信 `ShaoPeiShen`；`0-学习与工具/发企微.py` 亦可），**不要新建通道**。
⚠️ **只把扫描结果打印进 `reports/unclosed-output-scan.md` 不算数** —— 那正是这条留步清单从 2026-07 挂到今天没人看的原因（`LAN留步项捞出清单-2026-08-26.md` 已存在一个月，无人扫）。🔑 **判据：一个「写下来了」的提醒，如果没有一条会主动找到人的通道，它就等于没写。**
**翻转判定要防抖**：`off→on` 才推，`on→on` 不重复推；探针本身失败（既非 off 也非 on）时**不得当作 off**（否则回到内网前会误报一串）。⚠️ 已知坑：**off-LAN 下 `.51` 的 URL 照样返回 http_code**（扫描器文件头 L39 记着 2026-08-25 实测），**不能只看状态码**。
**推送内容至少含**：形态 1 每条的队列行号 ＋ 一句「要补什么」＋ 本次探到 on 的时刻（标 UTC 还是本地）。
🔴 **验活两侧**：造一次 off→on 翻转看它真的推出去（靶子用 `ShaoPeiShen` 自己，不拿专员做实验）；再造一次 on→on 看它**不重复推**。**本次 off-LAN 做不到真实翻转 ⇒ 用可注入的探针结果做双侧单测，并把「真实翻转验活」如实登记为 LAN 留步**（用扫描器认得的标记词写，否则它自己扫不到自己）。

【二 · #425 ⑶ 第 8 类告警的整轮早退盲区】
`OP-0828-Q` 已实测：第 8 类常驻告警（本地 master 独有提交）**排在 main() 起跑段的早退检查之后** ⇒ sweep 因分支不对／编辑锁被占／fetch 失败而整轮跳过时，这盏灯根本不会跑。
🔑 判据：**这盏灯只在 sweep 跑顺的那些轮才亮，而 sweep 跑不顺恰恰是分叉最容易发生的时候** —— 告警被排在了它要监视的那个失败模式之后。
修法方向你定，但要满足三条：
① 早退路径上也要有机会亮灯，**或至少留下「本轮未巡检」的可区分痕迹——不能与「已巡检且干净」长得一样**；
② 🔴 **不得改成 fail-loud 让 sweep 整轮失败** —— 告警发不出去不应让 sweep 本身挂掉（sweep 内已有这条口径，照它走）；
③ 验收**三侧**：造真分叉看它报 ＋ 对齐后看它解除 ＋ **构造一次整轮早退，确认此时它不会静默地表现成「干净」**。
⚠️ 行号会漂移：`OP-0828-Q` 记的与 Cowork 记的不一致（改动过），**自己现测，不要照抄任何一方的行号**。

【边界】🔴 不动主工作区的 master、不 ff、不 push 别人的产出；🔴 不修改/停用/删除任何计划任务（含 ZhuopinCommitSweep），要观察其行为用只读日志与隔离靶子；不碰 .51；不对外发送。🔴 不得碰 0-学习与工具/工具-共享文档编辑锁.py。写队列走编辑锁、撞锁退避重试。🔴 实测与本件所述不符，以你的实测为准，停下来如实报。
🔴【心跳，必做】每完成一个里程碑，往 reports/lane-heartbeat/OP-0828-Z.md 追加一行：`HH:mm:ss 本地 ｜ <一句话>`（时刻用本机 Get-Date 当场取）。**开工后 1 分钟内必须写出首行**。🔴 **停下来问人之前必须先写一行**并注明在等什么。收工写 `DONE ｜ <产出落点>` 或 `STOPPED ｜ <停在哪一步、为什么>`。（reports/ 已 gitignore，不进任何批次。）⚠️ **本件正好在建心跳/告警这一族的东西，别把自己的心跳文件也当成扫描目标算进去。**
【收工】回写 §一 #422 与 #425（🔴 **#425 不销号** —— 它记的是「分叉会反复发生、需常驻监视」这个结构性问题），登记 §二 批次。单列两行：「#422 接在哪个轮次上、为什么选它、两侧验活结果」「早退盲区：三侧验收各过没过」。开干前问我 2-3 个澄清。
```

---

## 四、本批刻意不领的，以及为什么（免得下一批重复盘）

| 不领 | 原因 |
|---|---|
| `#341` 队列写侧按域路由 ／ `#324` release 校验⑥ 误报 | 都改 `工具-共享文档编辑锁.py`，**所有 session 收工都用它** ⇒ 会重新引入「必须最后收工」的硬序。**下一批单独排、独占收工窗口。** |
| `#96`／`#380`／`#387`／`#399`／`#358`／`#419` | 需 `.51` 或需人在场，**off-LAN 做不了** |
| `#410` | 验收前提阻于 `§四 #132`⑴（门户网关审查未立项） |
| `#423`／`#424`／`#391`／`#372` | 依赖唐燕萍签认，信已发出、等回件 |
| `#334`／`#401`／`#432` 的后续 | `采购部#20` 已发出，等姚祖怡回件 |
| `#431` ⑵⒜ | 等陈忱答「三档存废」；**⒝ 判例 2 回灌不依赖回件、可做**，但属 Cowork 质量专线的文档活，不进 CC 泳道 |
