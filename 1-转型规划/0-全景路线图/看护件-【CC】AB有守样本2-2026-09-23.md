---
title: 看护件 · 【CC】A/B 有守样本 2（加固 n=1）
created: 2026-09-23
派出线: 环境总线 Win-0921-A
批次: B-0923_AB有守2
status: 待执行
---

# 看护件 · 【CC】A/B 有守样本 2 · `B-0923_AB有守2`

## 〇、口径

本批**唯一目的是把 A/B 的 `n=1` 补成 `n=2`**（Shao Peishen 2026-09-23 答 `2a`）。载荷与前三次**同一份派单件、一字未改**；去重守已在 master 上生效（起批前机器核：`git show master:.claude/settings.json` 命中 `dedup-guard` ＝ 1 ✓）。**不设 `ZHUOPIN_DEDUP_GUARD`。**

**已有三样本**：
| | 工具调用 | 峰值 | `cache_read` | 重复 |
|---|---|---|---|---|
| A 面 `dedc9bbf` 无守 | 66 | 163,208 | 6,512,264 | 10（15%） |
| B 首跑 `11d565ad` 守未生效 | 67 | 171,978 | 7,654,457 | 3（4%） |
| **B 重跑 `016d88fe` 守生效** | **36** | **103,745** | **2,959,200** | **1（2%）** |

无守两样本自然方差 17.5%；有守仅 1 个样本 ⇒ 本批补第二个。

🔴 **批次额度记账（`2a` 代理指标首次常态化实践）**：起点快照已取 —— **2026-09-23 09:05，五小时窗 `fh`=6%，七日窗 `sd`=2%**。收工再取一次，差值即本批（含 Cowork 主对话）的真实额度成本。**这是 Cowork 主对话唯一的记账办法**（它的 transcript 不落本机盘）。

## 一、硬边界

1. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
2. 🔴 **纯读活**：除产出那一份清单外不改任何既有文件；不碰 `.claude/rules/`、根 `CLAUDE.md`、`.claude/settings.json`。
3. 🔴 **派单件一字不改**；被守拒绝时不要重试同一次调用，拒绝消息里已写明改法。
4. 🔴 禁起后台子任务／并行 Agent；不整读 `工具-落库sweep.py`（562,889 B）／`test_工具-落库sweep.py`（541,758 B）／`进度编年-CHANGELOG.md`（632,434 B）。
5. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`；`.claude/worktrees/agent-a15b4851f659218aa` 里有 `#455` 真实未提交改动，谁都不许清。
6. 🔴 收工前跑 `git diff --name-only master...HEAD` 贴进报告；只 push 本泳道分支，不 ff master。

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#639` | `有守2线` | 只读 `0-学习与工具/工具-落库sweep.py`＋其单测；新增 `1-转型规划/0-全景路线图/核查-sweep告警消费者与夹具-<RUNTAG>.md` | 派单件＝同一份，一字未改 |

## 三、泳道 opener

### A1 · A/B 有守样本 2（§一 #639）

粘贴端：CC ｜ 泳道：有守2线

```
[OP-0923-E]【CC】AB有守样本2
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0923e-ab-guarded-2`） ｜ worktree：☑（op0923e-ab-guarded-2，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】AB基线-sweep告警消费者与夹具核查-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923e-ab-guarded-2 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923e-ab-guarded-2 --done --batch B-0923_AB有守2 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头（`工具-opener批处理执行v2.ps1 -Detach`），本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0923-F]【CC】看护有守2
【设置】执行环境：CC ｜ 分支：0923-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0923F-看护有守2。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】AB有守样本2-2026-09-23.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为该泳道起一个子任务，`isolation: "worktree"`，把【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。
🔴 等待一律用 `python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0923_AB有守2 --max-wait 540`，**禁裸 `sleep` 轮询**（队列 §一 `#638`）。
```

## 四、收工验收

```
python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0923_AB有守2 --max-wait 540
python reports/probe-0921/auditstat.py
python reports/probe-0921/fleet3.py
python 0-学习与工具/工具-额度与Opus对账.py --snapshot
```

🔴 **第一判据＝守生效**：audit 本棒窗口内必须有 dedup-guard 条目，零条即作废、不许拿数字充验收。
🔴 **误拦一次即判不合格**（目标已变却被拒）。
🔴 **总量三数**与有守样本 1（`016d88fe`：工具 36／峰值 103,745／`cache_read` 2,959,200）对照，算**两个有守样本之间的方差**——这才是 `n=2` 的意义：方差小则结论坐实，方差大则说明单次 A/B 本就不可信。
🔴 **收工快照**与起点（09-23 09:05，`fh`=6%／`sd`=2%）作差，记为本批真实额度成本。
🔴 四数全走 transcript 机器核，**不取泳道自陈**（前三次里有两次自陈与产出面对不上）。
