---
status: 在办
title: "未闭合产出扫描 · 首跑结果（三形态存量 ＋ 回 LAN 事件感知）"
created: 2026-08-27
派出线: CC OP-0826-U（源＝Cowork 环境总线 OP-0826-C）
---

# 未闭合产出扫描 · 首跑结果 —— 2026-08-27 00:2x（本机 on-LAN）

> **这份件是干什么用的**：`OP-0826-U` 建的扫描器（`0-学习与工具/工具-未闭合产出扫描.py`）
> **目前还没有任何轮次会调用它**（接线立 §一 `#422`）。在接上之前，本件是那次首跑
> 结果的**唯一承接载体** —— 否则扫出来的东西只存在于那一次会话里，随会话结束消失，
> 而那正是 `#398` 第 ⑷ 处要治的病本身。
>
> 🔴 **本件是快照，会过时。** 判当下状态一律重跑：
> `python 0-学习与工具/工具-未闭合产出扫描.py`（只读，不做任何处置）。

---

## 一、首跑最要紧的三条（都不是原派单件预料到的）

### 🔴 ⑴ 本地 `master` 与 `origin/master` 已分叉 —— 已立 §一 `#421`

`367b883`（`#394` A3 的 outbox→aibot 中继）**commit 了没 push**，本地独有 1 个、
远端独有 6 个。当日从本地 master 新建的 worktree **全都继承了这个未 push 的提交**
（本班自己的就是，rebase 时把它一起带上了新分支，已 `reset --hard origin/master` 摘掉）。

**危险不在分叉本身，在于它完全静默**：`push` 会被拒、`pull` 会造出违反 ff-only 的
merge，而队列里看不出任何异常；更要命的是任何一条这样的 session 照常 push 到 master，
都会把 `#394` 顺带推上主线，**没有任何一步会提示它正在这么做**。

⚠️ 本班刻意未处置：`#394` 属 A3 触碰区，派单件明写不得动。

### 🔴 ⑵ 一份躺了 16 天、1,130 行的未提交产出

`worktree queue-315-apply-9f2c1a`（分支 `claude/queue-315-apply-9f2c1a`）：
**13 个文件、1,130 行新增 / 243 行删除，全部未 commit**；该 worktree 最后一次提交
是 **2026-08-11 09:17**，改动文件 mtime 停在 **08-11 13:52** ⇒ **躺了 16 天**。
触及 `工具-共享文档编辑锁.py`、`工具-落库sweep.py`、`queue_table.py` 与
`openspec/changes/queue-dual-file-split/tasks.md` 等。

🔑 **它比 A5 那 539 行更能说明问题**：A5 是当晚刚发生、还有人记得；**这一份没有任何人
记得，也没有任何机制会提起它，而一次 `git worktree remove` 就会让它永久消失**。
⚠️ **本班未处置**（属他人触碰区，且 `#315` 早已交付、这份改动是否已被别的路径取代
需由知情方判断）——**建议交值周巡检定夺**：要么救、要么明确作废，**不要继续放着**。

### ⑶ 形态 2 的判据反例：`git cherry`，不是 ancestry

派单件给的活样本 `#398` 的 `fb0d185`，实测 ancestry 判「未合入」，而内容早已在
`origin/master`（`e024ead`）—— 两者 `git patch-id --stable` 逐字相同（`db8ae53a…`）。
⇒ 若按直觉用 `merge-base --is-ancestor`，这个告警会对**每一个 rebase／cherry-pick
落地的分支永久报红**，一周内所有人都会学会忽略它。已改用 `git cherry` 并配反例单测锁死。

---

## 二、首跑存量（快照，2026-08-27 00:2x）

| 形态 | 命中 | 说明 |
|---|---|---|
| 形态 1 · 队列里的「LAN 留步」登记 | **9 处** | 另**剔除** §二 批次行里的历史叙述 **19 处**（已完成批次的记述，不是待办；不剔除会长期虚高）。按登记日分档：≥7 天 3 处（`#334` 15 天／`#340` 10 天／`#354` 8 天），其余 <3 天 |
| 形态 2 · 代码卡在未合入分支上 | **12 条分支** | 其中 `master` 一条是上面 ⑴；`backup/*` 已按约定排除。⚠️ 首跑那一刻是 13 条——第 13 条正是本班自己的分支，push 之后它自己消失了（见 §三 已解除） |
| 形态 3 · 产出只是 worktree 里未 commit 的改动 | **4 处** | 其中 `queue-315-apply-9f2c1a` 是上面 ⑵；主工作区一处单列（救法不同，见下） |

**回 LAN 三项探针（首跑，真答）**：`ping 192.168.100.51` 收到带 TTL 的回复 ✅ ／
`:8091/api/ping` HTTP 200 `Server: waitress` 无 `Proxy-Connection` ✅ ／
`:8093/api/ping` 同 ✅ ⇒ **on-LAN 成立**。状态已初始化在
`reports/unclosed-output-state.json`，此后由 off→on 的**那一次翻转**触发提醒。

🔴 **主工作区与 linked worktree 的救法不同，不可混用**：主工作区的未提交改动本该由
落库 sweep 每轮收走 ⇒ 它**只在 sweep 停用期间**才是风险，救法是人工 commit；
linked worktree 的救法是「先跑回归、绿则 commit 到本分支再合入」，且**一次 worktree
清理就会让它消失**。

---

## 三、护栏已实测两侧都通

「判据要能被『已补做』关掉」这条不是设计意图，是**当场验过的**：本班把自己 worktree 的
产出 commit + push 之后复跑，报告如实打出

```
## ✅ 已解除（上一轮报过、本轮不再命中）：2 项
  · form2:claude/unclosed-output-handoff-scan-2b7b85
  · form3:unclosed-output-handoff-scan-2b7b85
```

且第二轮起静音。**报出来那一侧与关掉那一侧都验过，只验一侧等于没验。**

---

## 四、首跑原文（`--skip-lan-probe` 之外的完整输出，仅供对照，会过时）

见下节代码块；权威判断一律以重跑为准。

```text
# 未闭合产出扫描 · 2026-08-27（只读，不做任何处置）
比较基准：`origin/master`

## 回 LAN 感知：✅ on-LAN（三项齐备）
  ✅ `ping 192.168.100.51` —— 收到带 TTL 的回复
  ✅ `http://192.168.100.51:8091/api/ping` —— 统一门户（SC8 等）：HTTP 200｜Server='waitress'｜Proxy-Connection=None
  ✅ `http://192.168.100.51:8093/api/ping` —— QD-B 立项审核门禁：HTTP 200｜Server='waitress'｜Proxy-Connection=None

## 形态 1 · 队列里的 LAN 留步登记：9 处
（已剔除 §二 批次行里的历史叙述 19 处——那些是已完成批次的记述，不是待办）
  · §一 #334（跨桌任务队列-业务场景.md）｜≥7 天／15 天｜…定序；⑻ 另须走 openspec 单独定级）。 ⚠️ **LAN 留步**：本班 off-LAN，`.51` **未部…
  · §一 #340（跨桌任务队列-业务场景.md）｜≥7 天／10 天｜…b）**：《基准变更说明》§六 四件事**做前三件，第四件 LAN 留步**。 **① manifest 已更新**：`…
  · §一 #354（跨桌任务队列-机制环境.md）｜≥7 天／8 天｜…-collapse` 全部代码侧落地，`.51` 真机复验 LAN 留步**。 ━━━ **⑴ 根 `.env` 第 3…
  · §一 #398（跨桌任务队列-机制环境.md）｜<3 天／2 天｜…rk `OP-0826-C` 应 Shao Peishen「回 LAN 后有没有机制把留步项补上」一问实测）——「LAN…
  · §一 #394（跨桌任务队列-业务场景.md）｜<3 天／2 天｜…未闭合，不得读成「SC2 已通」** ━━━ **⑴ L1 LAN 留步**：`WECOM_AIBOT_OUTBOX_P…
  · §一 #401（跨桌任务队列-业务场景.md）｜<3 天／2 天｜…*，真实覆盖要等 ⑸ 的对照表。 ━━━ 🔴 **⑸ 三步 LAN 留步（本机 off-LAN，一步未做，不得当作已闭合…
  · §一 #402（跨桌任务队列-业务场景.md）｜<3 天／2 天｜…升格根因如实记**：本班起草之初 #401 的 L1 仍是 LAN 留步、对照表不存在，原计划只写一句知会；**取队列锁…
  · §一 #403（跨桌任务队列-业务场景.md）｜<3 天／2 天｜…且属建造侧改动；不替 O1/FI1 改范围（红线）。🔒 **LAN 留步**：`.51` 的 `SC8_OUTSOURC…
  · §一 #419（跨桌任务队列-机制环境.md）｜<3 天／1 天｜…如 2026-07-16 停摆 24h49m）。 ⚠️ **LAN 留步**：`.51` 上实际部署的是哪一版 `ale…
  ⇒ 怎么救：on-LAN 后按行内已写死的补法执行，做完回写该行并销号。

## 形态 2 · 代码卡在未合入分支上：12 条分支
  · 🔴 **本地 master 已分叉** `master`：1 个真·未上游提交，落后 8 个｜worktree `C:/Dev/zhuopin-ai`
      367b883 feat(企微机器人): #394 outbox → aibot 通用中继（SC2 与 FI2 合建一份）
      ⇒ 🔴 **不得 `git pull`（会造出违反 ff-only 的 merge）、也不得 `push -f`**：把本地独有提交 rebase 到 `origin/master` 之上再 ff push，或确认它已由别的分支带上去后 ff 对齐本地。
  · `claude/a22-closure-form-apply`：1 个真·未上游提交，落后 172 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/a22-closure-form-apply`
      93245b2 feat(跟进信): 闭环形态判定在回填后仍然存在（队列 #353 apply）
  · `claude/a23-env-anchor-collapse`：1 个真·未上游提交，落后 156 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/a23-env-anchor-collapse`
      c13a959 feat(底座/#354): env-anchor-collapse apply——12→15 处 .env 手抄收拢进 env_anchor，CI 门禁 AST 判据上线
  · `claude/a27-opener-archive-precheck`：1 个真·未上游提交，落后 167 个｜无 worktree
      fa3f6ce docs(openspec): #397 批处理派出前查归档——propose＋design 出件待审
  · `claude/a30-inbound-whitelist-apply`：1 个真·未上游提交，落后 156 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/a30-inbound-whitelist-apply`
      32feda5 feat(aibot/入站白名单): #380 apply——出站即入站判据落地,李姣龙与解植雅同批放行,whitelist_rejected 补独立告警
  · `claude/op0826k-a8-fi2-418-390`：2 个真·未上游提交，落后 8 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/op0826k-a8-fi2-418-390`
      367b883 feat(企微机器人): #394 outbox → aibot 通用中继（SC2 与 FI2 合建一份）
      918bd84 fix(FI2): #418 未解析发票行可重试 + #390 无PO关联行不再打挂整批（同车，同一文件）
  · `claude/op0826l-a9-q408`：2 个真·未上游提交，落后 8 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/op0826l-a9-q408`
      367b883 feat(企微机器人): #394 outbox → aibot 通用中继（SC2 与 FI2 合建一份）
      59da404 feat(质量域): #408 消化质量部#9 回件——8D评审规则库三档口径落档 + Q2 自动化等级定稿
  · `claude/op0826q-a10-purchase19`：2 个真·未上游提交，落后 3 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/op0826q-a10-purchase19`
      aa84ff1 feat(企微机器人): #394 outbox → aibot 通用中继（SC2 与 FI2 合建一份）
      980df2e docs(采购): 起草 采购部#19——三条判例再送与六件一次问齐（待审未发，OP-0826-Q）
  · `claude/op0826r-a12-mat-onescreen`：2 个真·未上游提交，落后 8 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/op0826r-a12-mat-onescreen`
      367b883 feat(企微机器人): #394 outbox → aibot 通用中继（SC2 与 FI2 合建一份）
      ac62ae5 feat(SC8): #334 子项⑺ 物料看板全部列同屏——去 nowrap/横向滚动条，fixed 布局＋colgroup，多值列逐值一行上下对齐
  · `claude/op0826s-a3-outbox-relay`：1 个真·未上游提交，落后 8 个｜worktree `C:/Dev/zhuopin-ai/.claude/worktrees/op0826s-a3-outbox-relay`
      367b883 feat(企微机器人): #394 outbox → aibot 通用中继（SC2 与 FI2 合建一份）
  · `claude/unified-portal-design-8a2ce3`：2 个真·未上游提交，落后 1404 个｜无 worktree
      13546f4 feat(门户): 统一门户网关 propose+design（队列#162）——反向代理路由/企微OAuth SSO/三层权限/访问日志
      648a0e5 feat(门户): 统一门户网关 apply——反向代理/企微OAuth SSO(mock先行)/三层权限/访问日志（队列#162）
  · `feat/sc8-baoguan-v2-batch1`：1 个真·未上游提交，落后 2092 个｜无 worktree
      2c538a5 feat(保供v2批1): 看板窗口过滤(一.1) + 批1 openspec + 专员口径 triage
  ⇒ 怎么救：先验一眼，再 ff 合入 master（`git rebase origin/master` 后 ff push）；验不过则如实登记，不得直接丢弃。

## 形态 3 · 产出只是 worktree 里未 commit 的改动：4 处
  · `C:/Dev/zhuopin-ai/.claude/worktrees/material-board-view-df54d1`（分支 `claude/op0822c-sweep-alerts-b9424a`）：0 个已跟踪改动 ＋ 1 个未跟踪，共 0 行新增
      openspec/changes/sweep-resident-hint-deploy-manifest/
  · `C:/Dev/zhuopin-ai/.claude/worktrees/queue-315-apply-9f2c1a`（分支 `claude/queue-315-apply-9f2c1a`）：13 个已跟踪改动 ＋ 0 个未跟踪，共 1130 行新增
      0-学习与工具/test_工具-共享文档编辑锁.py
      0-学习与工具/test_工具-落库sweep.py
      0-学习与工具/test_工具-队列查询.py
      0-学习与工具/test_工具-队列结构lint.py
      0-学习与工具/工具-共享文档编辑锁.py
  · `C:/Dev/zhuopin-ai/.claude/worktrees/wecom-service-home`（分支 `ops/wecom-service-home`）：0 个已跟踪改动 ＋ 1 个未跟踪，共 0 行新增
      5-平台底座/wecom-aibot-service/run-followup-dispatch-check.ps1
  · `C:/Dev/zhuopin-ai`（主工作区）（分支 `master`）：13 个已跟踪改动 ＋ 2 个未跟踪，共 201 行新增
      1-转型规划/0-全景路线图/派单件-【CC】CLAUDE进度段lint实现-2026-08-21.md
      1-转型规划/0-全景路线图/派单件-【CC】FI2三单核对起点反转-propose与design-2026-08-21.md
      1-转型规划/0-全景路线图/派单件-【CC】FI2面板翻倍修复与R5分母对照表-2026-08-23.md
      1-转型规划/0-全景路线图/派单件-【CC】SC2归档修可追溯链-2026-08-21.md
      1-转型规划/0-全景路线图/派单件-【CC】环境侧E1E2E3-2026-08-08.md
      ⇒ 主工作区的改动本该由落库 sweep 每轮收走；**sweep 停用期间**它就是一堆没人收的产出，需人工 commit（不适用下面那条救法）。
  ⇒ 怎么救（linked worktree）：先跑该改动所属子项目的回归，绿则 commit 到本分支再合入；🔴 不得丢弃、不得替它改判。任何一次 worktree 清理都会让这些行消失。

## ⚠ 判据不可用（**不据此判为干净**）
  · `C:/Dev/zhuopin-ai/.claude/worktrees/conftest-pytest-regression-c9d07e` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/fi2-webhook-var-check-bc19fa` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/followup-dispatch-apply-25679f` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/gallant-lumiere-b482be` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/musing-pascal-68d14e` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/observation-window-archive-91b68d` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/platform-bootstrap-apply` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/queue-313-prerequisites-7d9565` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/queue-lint-bare-pipe-312efa` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/queue-table-reachability-2` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/recursing-hofstadter-eb7215` 的 `git status` 失败；`C:/Dev/zhuopin-ai/.claude/worktrees/sweep-deadlock-transition-tasks-ba394b` 的 `git status` 失败
```
