---
title: "取证件 · 11 份 `☒` 形态回件「机器重读值 vs 已回灌值」逐条对账"
created: 2026-09-12
status: 生效
编号: OP-0912-K
派出线: Cowork 环境总线 OP-0912-E（批 B-0912_夜批四泳道）
上游: 1-转型规划/0-全景路线图/回件勾选逐格重读对账-13份-2026-09-11.md（队列 §一 #563 ④ 产出）
用途: 只出对账表、不改任何既有件；不一致项写清应改成什么、改在哪几处
---

# 取证件 · 11 份 `☒` 形态回件与已回灌结论对账

> **手段**（每份都重跑，不引用上游计数）：本棒自写脚本 `zipfile` 解开每份 docx → `xml.etree` 遍历 `word/document.xml` 的 `w:tbl/w:tr/w:tc`，逐格输出 `w14:checkbox/w14:checked/@w14:val` 与字符层 `☒/☐` 的并集；**已回灌值**取自 ① `工具-跟进信README查询.py --row <编号> --field 发送状态`（主表＋ `README-归档-202609.md`）、② `工具-队列查询.py --row N`（活行）／归档件按同一 `_table_data_rows` 解析器取行（`--row` 对归档件多段 `## 一、` 只取末段，故改用其模块函数逐段扫）、③ `队列行日志/#N.md` 与 `跟进信行日志/<编号>.md` 外置件。
> **范围**：上游 13 份中 **11 份**为表格式判例（可逐格对账）；另 2 份（`采购部` 07-14 会材反馈稿／07-26 V6 反馈稿）是 7 月**内联式表单**（`☐ 确认 ☒ 改：` 一行内并排），上游标「未解析出带勾选的表行」，**不在本件对账范围**，附录 C 只登其真实勾选值供后人核。
> 🔴 **本件未改任何 README／队列行／跟进信／行日志**，全部改法只写在「应改」列。

## 〇、一页结论

| # | 回件（README 编号） | 行数 | 机器重读 vs 已回灌 | 不一致数 | 应改在哪 |
|---|---|---|---|---|---|
| 1 | `财务部#2` 数据闸核实对照表（07-09） | 10 | **一致**（粗粒度） | 0 | 无；但上游对账件把三选一格写成「第3列」丢了 是/否/部分（附录 A） |
| 2 | `财务部#16`（09-04） | 8 | **值一致**；**行内残留已证伪结论** | 0 值 ／ 2 处载体 | 队列 `#423`／`#424` 行内首段（§二） |
| 3 | `质量部#9`（08-25） | 6 | **一致** | 0 | 无 |
| 4 | `采购部（未发，不编号）` 07-29 跨月占用判例（回件 08-06） | 3 | **一致**（勾 ✅ 与 ✏️ 文字表面冲突，已按文字回灌） | 0 | 无 |
| 5 | `采购部#13`（08-12） | 3 | **一致** | 0 | 无 |
| 6 | `采购部#16`（08-20） | 3 | **一致** | 0 | 无 |
| 7 | `采购部#17`（08-21） | 8 | **一致** | 0 | 无 |
| 8 | `采购部#18`（08-26） | 3 | 🔴 **不一致** | **3** | README `采购部#18` 发送状态格（主表 markdown 行 60）；§四 `#122` 首段前提（只加更正指针） |
| 9 | `采购部#19`（08-28） | 4 | **一致** | 0 | 无（README 行只有机器代写转态、无内容，内容在 `#428.md`） |
| 10 | `采购部#20`（08-31） | 1 | **一致** | 0 | 无 |
| 11 | `采购部#22`（09-10） | 7 | **一致**（09-12 已更正） | 0 | 无（README 与 `#499` 均已带更正段；`#499` 机器字段仍 open 属另一事） |

**合计：11 份 ／ 39 个判例行 ／ 值不一致 3 行（全在 `采购部#18`）／ 载体残留错误结论 2 处（`#423`／`#424` 行内首段）。**
另：上游 `#563` 状态列写「`财务部#16` 的逐行答复从未回灌」——**与本件取证不符**（§二.3），本件如实列出、不改 `#563`。

---

## 一、逐份逐行对账表

列义：**机器重读值**＝本棒重跑；**已回灌值**＝台账/队列里现在写着的；**判**＝一致／不一致；**应改**＝只在不一致时写。

### 1. `财务部/FI2-FI3-数据闸推进-核实对照表与转发正文唐燕萍回复.docx`（README `财务部#2`，2026-07-09）

实测：`checked=1` 10 ／ `☒` 10 ／ sdt 30（＝10 行 × 是/否/部分 三格）。表头第 3 列＝「【IT】已通?」，格内三个复选框并排。

| 行 | 数据对象 | 机器重读值 | 已回灌值（`财务部#2` 主要事项＋07-09 信正文） | 判 |
|---|---|---|---|---|
| 1 | 采购订单 PO 明细行 | **否**，何时通 26.7.10 | 「IT 给齐的端点时间表也让 9 月启动基本落定」（整体采纳，未逐行登） | 一致（粗） |
| 2 | 入库单 GR 明细行 | 否，26.7.10 | 同上 | 一致（粗） |
| 3 | 应付单＋配票状态 | 否，26.7.10 | 同上 | 一致（粗） |
| 4 | 已付款凭证/付款记录 | 否，26.7.13 | 同上 | 一致（粗） |
| 5 | 采购合同 | 否，26.7.15 | 同上 | 一致（粗） |
| 6 | 供应商主数据·收款账户 | **部分**，26.7.10 | 同上 | 一致（粗） |
| 7 | 供应商主数据·税率/资质 | **部分**，26.7.10 | 同上 | 一致（粗） |
| 8 | 预付款记录/台账 | 否，26.7.15 | 同上 | 一致（粗） |
| 9 | PO 变更记录 | 否，26.7.13 | 同上 | 一致（粗） |
| 10 | 退单记录 | 否，无则备选＝**不需要** | 「① 退单记录暂不接」 | **一致** |

结论：**一致**。⚠️ 上游对账件把 10 行全写成「第3列」——这是**对账件自身分辨率不足**（一格三框），不是回灌错；真实分布＝否 ×8／部分 ×2，本件附录 A 已补。

### 2. `财务部#16`（09-04 回件，面板 KPI 与发票认领判例批改）

实测：`checked=1` 8 ／ `☒` 8 ／ sdt 24；A 表 4 行、B 表 3 行、C 表 1 行，每行恰勾一格。

| 行 | 判例 | 机器重读值 | 已回灌值 | 载体 | 判 |
|---|---|---|---|---|---|
| 1 | A-1 「本次共 1 项料品／另列孤立发票 3,389 行」 | **❌ 判不对** | ❌ | README `财务部#16` 09-07 更正段；`跟进信行日志/财务部#16.md` 更正横幅；`队列行日志/#423.md` 09-07 重大更正段；取证件 `财务部16回件勾选误判-取证与更正-2026-09-07.md` §二 | 一致 |
| 2 | A-2 「共 3390 行（含孤立发票）」 | **❌ 判不对** | ❌ | 同上 | 一致 |
| 3 | A-3 「装载前先按本次 AP 范围过滤 ⇒ 装载 0 行」 | **✅ 判对** | ✅ | 同上；实现＝`#423` 09-07 段（`_INVOICE_SCOPE` 切 `ap_range`，已部署 `.51`） | 一致 |
| 4 | A-4 「真·孤立发票必须在面板上继续看得见」 | **❌ 判不对** | ❌（她文字：不上面板、留在未匹配发票池） | 同上；实现＝`#423` 09-07 段「未匹配成功的发票池」 | 一致 |
| 5 | B-5 计量单位换算→未税金额＋税额再试 | ✅ | ✅ | `队列行日志/#424.md` 09-07 重大更正段；实现＝`#424` 09-07 段 | 一致 |
| 6 | B-6 单料号单唯一挂 | ✅ | ✅ | 同上 | 一致 |
| 7 | B-7 同料号多行合计；跨两料号一律不认领 | ✅ | ✅ | 同上 | 一致（与 #8 互斥，见 §三） |
| 8 | C-8 跨两料号拆两条按比例分摊 | ✅ ＋ 文字「尾差并入最后一条」 | ✅ ＋ 尾差并入 | `#424` 09-07 段（`_ALLOW_INVOICE_ROW_SPLIT`，尾差减法并入最后一条） | 一致（与 #7 互斥，见 §三） |

结论：**8 行值全部一致**（09-07 `OP-0907-L` 更正已把逐行值写进 README／行日志／取证件）。🔴 **但两条活行的行内首段仍是被证伪的 09-04 结论，且行内无更正标记**——更正段被 K2 外置到了 `#423.md`／`#424.md`，`--row 423` 只读行内会得到「判例表 A 四选一未勾选、9 行勾选格均为空」；`--row 424` 首段仍写「依赖唐燕萍签认」。应改见 §二。

### 3. `质量部#9`（08-25 回件，Q2 改写技术回应＋8D 评审判例批改表）

实测：`checked=1` 6 ／ `☒` 6 ／ sdt 14（6 行×2 格＋2 枚正文控件）。

| 行 | 判例 | 机器重读值 | 已回灌值（README `质量部#9` ＋ 归档 `#405`） | 判 |
|---|---|---|---|---|
| 1 | 7 份无 FMEA 号→建议项 | ✅ | ✅ | 一致 |
| 2 | 无独立 D8 结案日期→扣分项 | **两格空**，✏️「D0和D8不做判断」 | 「判例2 ✏️ 改判『D0和D8不做判断』」 | 一致 |
| 3 | D6 错贴→红线项 | ✅ | ✅ | 一致 |
| 4 | D3 子字段缺失→扣分项 | ✅ | ✅ | 一致 |
| 5 | 不良分类不参与百分制 | ✅ | ✅ | 一致 |
| 6 | 安全相关件一律转人工 | ✅ | ✅ | 一致 |
| 3.1 | 措辞「是笔误」 | ☒ | 「3.1 勾『是笔误』⇒ L2」 | 一致 |

结论：**一致**。上游对账件表里没有第 2 行是正常的（该行无 `☒`，只有 ✏️ 文字），不是漏读。

### 4. `采购部（未发，不编号）` 2026-07-29 批 2 跨月占用判例（回件 08-06）

实测：`checked=1` 3 ／ `☒` 3 ／ sdt 6。表头：`✅ 对 ｜ ❌ 错 ｜ ✏️ 改判（写一句）`（**无主语**，08-19 澄清函的由来）。

| 行 | 判例 | 机器重读值 | 已回灌值（归档 README 该行＋ `#293`／`#150`） | 判 |
|---|---|---|---|---|
| 1 | S02Y.0120 逾期欠账是否继续传导 | ✅ 对 ＋ ✏️「**按照现状判定**，我要求的规则就是没关闭需求就会一直保留在看板中」 | 「3 行判例逐行签认『都按照现状判定』⇒ 上月未齐套占用继续按原始欠账传导、直到 PMC 在 ERP 关闭需求为止」 | 一致 |
| 2 | S02Y.0210 最近一行 | 同上 | 同上 | 一致 |
| 3 | S02Y.0210 最远一行 | 同上 | 同上 | 一致 |

结论：**一致**。⚠️ 留痕：三行都勾在「✅ 对」列、而 ✏️ 写「按照现状判定」——若只看勾选会读成「拟改判定对」，与文字**相反**；回灌走的是文字（现状），且他 08-19 明说过「√ 和 × 分别代表什么含义…否则我无法改判」，本件不重开。

### 5. `采购部#13`（08-12 回件，可齐套套数两字段是否依赖判例批改）

实测：`checked=1` 3 ／ `☒` 3 ／ sdt 6。表头：`✅ 对 ｜ ❌ 错 ｜ ✏️ 改判`。

| 行 | 判例 | 机器重读值 | 已回灌值（README `采购部#13`：「1❌／2❌／3✅」） | 判 |
|---|---|---|---|---|
| 1 | F02N.0233 可齐套＝0 维持现状 | **❌ 错** ＋ ✏️「穿透到半成品下面的采购件层再算」 | ❌ | 一致 |
| 2 | 另 10 个 F 成品同上 | **❌ 错** ＋ ✏️ 同上 | ❌ | 一致 |
| 3 | 对照组 S02Y.0210 现状对 | **✅ 对** ＋ ✏️「第一层子件已是 R 开头采购件，这类现状是对的」 | ✅ | 一致 |

结论：**一致**（上游「第5列／第4列」＝❌／✅，本件已还原成表头文字）。

### 6. `采购部#16`（08-20 回件，齐料日期判例批改可勾选版）

实测：`checked=1` 3 ／ `☒` 3 ／ sdt 9。

| 行 | 判例 | 机器重读值 | 已回灌值（README `采购部#16`：「3 条『✅ 这样判对』全部勾上，❌/✏️ 六格全空」；`#344`） | 判 |
|---|---|---|---|---|
| 1 | R01I.0622 齐料日＝累计够数那笔 | ✅ | ✅ | 一致 |
| 2 | 需 6000、一笔 10000 ⇒ 2027-05-20 | ✅ | ✅ | 一致 |
| 3 | 需 15000 ⇒ 2027-01-20 | ✅ | ✅ | 一致 |

结论：**一致**。上游行末「写了一句：☐ ____________」只是空占位符，不是他写了字。

### 7. `采购部#17`（08-21 回件，SC2 采购周报口径判例批改）

实测：`checked=1` 8 ／ `☒` 8 ／ sdt 24；8 行全部勾第 3 格「✏️ 应改判为」并写字。

| 行 | 机器重读值 | 已回灌值（归档 `#362` ＋ README `采购部#17`） | 判 |
|---|---|---|---|
| A-1／A-2／A-3 | ✏️「采购订单量只取『确认数量』栏…」；A-3 另更正「该单真实订 200 不是 3000」 | 「判例表 A 三条全部改判…只取确认数量…A-3 真实订 200」 | 一致 |
| B-1～B-5 | ✏️「你的下单行数环比计算逻辑是什么？…」各附他自算行数 | 「判例表 B 五条全部改判，五条都先反问同一句…±400% 仅作工作量参考」 | 一致 |

结论：**一致**。

### 8. `采购部#18`（08-26 回件，物料看板三条读数口径）🔴

实测：`checked=1` 3 ／ `☒` 3 ／ sdt 9；表 1 三行**第一格「✅ 这样对」全勾**。

| 行 | 判例 | 机器重读值 | 已回灌值 | 判 |
|---|---|---|---|---|
| 1 | R01A.0090 三个月窗口、窗口外 1,516,800 不计入合计 | **✅ 这样对** | README `采购部#18` 发送状态格：「**三条读数口径判例表 9 个勾选格全空，须另行追问**」；归档 `#411`：「9 个勾选格一格未勾（python-docx 逐格实读）」；`队列行日志/#334.md` 08-26 段：「⑴ 三条读数口径判例表零勾选…9 格全部为空」 | 🔴 **不一致** |
| 2 | R01B.0039 两家供应商全部并列 | **✅ 这样对** | 同上 | 🔴 **不一致** |
| 3 | 供应商列取 ERP 实际下单供应商、空＝「无未交订单」 | **✅ 这样对** | 同上 | 🔴 **不一致** |

**已有的更正（本件核实存在，不重复）**：归档 `#428`（08-28）行内＋`队列行日志/#428.md`：「`采购部#19` 的发信前提是一次读取假阴性、不是他漏勾…三行第一格全部为 `☒`」；`队列行日志/#334.md` 08-28 段：「先更正本行上游的一处结论：§一 #411 记的『9 个勾选格一格未勾』是读取假阴性…三条读数口径自 2026-08-26 起即已确认」；§四 `#133` 立行（修法）。
**未更正的载体（＝本件不一致所指）**：
- ① **README 主表 `采购部#18` 发送状态格**——现文仍是「⚠️ 闭环≠口径已确认：三条读数口径判例表 9 个勾选格全空，须另行追问」，**无任何更正段**；`--digest` 与闸查询都从这一格取首段。
- ② **§四 `#122` 首段前提**——「三条判例零勾选与问题 1 答案冲突的处置」；该项 09-03 已裁决（定序 ⑺→⑼→⑻），裁决本身不受影响，但**首段前提未标「零勾选＝假阴性」**。
- ③ 归档 `#411`＝历史快照，**不追改**（根 CLAUDE.md「历史记录不追改」）。

**出血实证**：因这次假阴性，`采购部#19`（08-26 起草、08-27 发出）把同三条判例「上封原样重送」——他 08-28 又勾了一遍 ✅（本件第 9 份）。**与 `采购部#22`（#563）同型，且早 13 天。**

### 9. `采购部#19`（08-28 回件，三条判例再送与六件一次问齐）

实测：`checked=1` 6 ／ `☒` 6 ／ sdt 16（表 1 三行＋表 2 一行＋正文 2 枚「☒ 认可」）。

| 行 | 判例 | 机器重读值 | 已回灌值（`队列行日志/#428.md` 六项逐条回灌；`#334.md` 08-28 段） | 判 |
|---|---|---|---|---|
| 1～3 | 同 `采购部#18` 三条 | ✅ ×3 | 「判例表 A 三条读数口径 ⇒ 三行全 `☒ ✅ 这样对`（08-26 即已勾，本次复勾）」 | 一致 |
| 4 | 答交累计不足 ⇒ 判无答交，到货日取更晚者 | ✅ | 「判例表 B ⇒ `☒ ✅ 这样对`，同时自由文本写『要例子』⇒ 转 §四 #134」 | 一致 |

结论：**一致**。⚠️ README `采购部#19` 发送状态格只有「S4 桥二自动转态…机器代写、未经人工逐字确认」一句，**内容零字**——不算不一致（内容在 `#428.md`），但它是「只读 README 得不到结论」的又一例，留痕。

### 10. `采购部#20`（08-31 回件，读取缺陷致歉与判例 4 真实案例）

实测：`checked=1` 4 ／ `☒` 5 ／ sdt 10（表内 1 枚＋正文「☒ 其它」「☒ 保留现状」等）。

| 行 | 判例 | 机器重读值 | 已回灌值（归档 `#444`，Shao Peishen 当场确认） | 判 |
|---|---|---|---|---|
| 4 | 答交累计不足判无答交、到货日取更晚者 | ✅ 这样对 | 「⑴ 判例 4 口径＝`☒ ✅ 这样对` ⇒ 终局确认」 | 一致 |

结论：**一致**（`#444` 用的就是 `<w:t>` 直读 `☒`，与本件同路）。

### 11. `采购部#22`（09-10 回件，SC2 六项指标口径澄清）

实测：`checked=1` 7 ／ `☒` 7 ／ sdt 21；三张表 3＋2＋2 行全勾「✅ 这样判对」。

| 行 | 判例 | 机器重读值 | 已回灌值 | 判 |
|---|---|---|---|---|
| 1～7 | 第 5 工作日算法／法定节假日不算／调休计入／达成率自然月累计／跨月周归周日所在月／「5 号以后」以周报所属月／W36 窗口 | ✅ ×7 | README `采购部#22`：09-12 更正段「七行全选『判对』、三处口径已定，下句『未批改』作废」；`#499`：09-11 23:2x 重大更正段（逐条列出七条口径） | 一致 |

结论：**一致**（09-11/09-12 已更正）。留痕：`#499` 机器字段仍 `[S:open]`、README 该格 09-10 原句仍在——两处都带了更正标记，符合「历史原文保留＋当下指针改」，不列不一致。

---

## 二、🔴 优先项：`财务部#16` 三条「判不对」的改判意见

> 她三条 ❌ 的 ✏️ 格**均为空**（她书面反映「最后一列无法输入文字」，§四 `#156`）；改判意见在正文高亮段，原文照录自回件 docx（取证件 09-07 §三已转录，本件复核一致）。

| 判例 | 我方拟改原文（回件表内） | 她勾 | 她的改判意见（正文高亮原文） | 应回灌的结论 | 现状 |
|---|---|---|---|---|---|
| A-1 | 「本次共 1 项料品」／BLOCK 1；另在页面单列一条「孤立发票 3,389 行（不在本次查询的 AP 范围内）」 | ❌ | 「我们希望是判例 3 的展现形式，而又保留判例 4 的功能但不用展现孤立发票在面板上，不要把孤立发票过滤掉，而是留在未匹配成功的发票池」 | **A-1 档（止血档）作废**：面板不得出现「孤立发票 N 行」单列条目 | ✅ 已按此实现（`#423` 09-07：`_INVOICE_SCOPE` 切 `ap_range`，孤立发票移出明细表；`.51` 冒烟 共 1 项／BLOCK 1／孤立发票 0） |
| A-2 | 「本次共 3390 行（含孤立发票）」／BLOCK 3390，注明「其中料品项 1、孤立发票 3,389」 | ❌ | 同上 | **A-2 档作废**：不得把孤立发票计入「本次共 N 行」与 BLOCK 数 | ✅ 同上（`_KPI_ORPHAN_MODE` 三档常量保留但默认不走此档） |
| A-4 | 不论选哪档，「真·孤立发票」都必须在面板上继续看得见 | ❌ | 「即如下图红框的孤立发票不需要体现在三单核对明细表中，但这些孤立发票有可能只是目前业务尚未立账，待立账时才需要用到」 | **A-4 改判为**：真·孤立发票**不上三单核对明细表**，进「未匹配成功的发票池」保留、可检索、不丢弃（排除≠丢掉） | ✅ 已按此实现（`#423` 09-07：新「未匹配成功的发票池」承接，两段并集恒等于入参） |

**三条改判的共同结论一句**：A 表她选的是 **判例 3**（装载前按本次 AP 范围过滤），并把判例 4 的「完整性检查」改成「进发票池不上面板」。三条的实现已于 09-07 上线，**本件核对＝实现与她的改判一致，不需要再改代码**；需要改的是下面两处**载体**：

| 应改处 | 现文（错） | 应改成 | 改法 |
|---|---|---|---|
| 队列 §一 `#423` 行内首段（`跨桌任务队列-业务场景.md`） | 「判例表 A（判例1-4）四选一**未勾选**，她给了一个自定义方案…全部 9 行勾选格实测（w14:checkbox 直读）均为空」 | 在该段前加一句「🔴 **本段 09-04 结论已于 09-07 证伪（`OP-0907-L`），勿引用——她的真实勾选＝A 表 1❌／2❌／**3✅**／4❌，更正段见 `队列行日志/#423.md`**」 | `工具-队列编辑锁.py` acquire → `edit-row 423` 整格改（≤4096 B，须先把 09-07 值周审计段外置） |
| 队列 §一 `#424` 行内首段 | 「待领（P2，**依赖唐燕萍签认**）」 | 「待领（P2，签认已于 09-04 到达＝判例 5/6/7/8 全 ✅，见 `#424.md` 09-07 重大更正段）」 | 同上 |

**附：上游 `#563` 状态列一句须留痕**——「`财务部#16` 的逐行答复从未回灌…2026-09-07 那次只更正了『8 行逐行全部作答』、没写她选了哪一档」。本件核实：09-07 的 README 更正段、`跟进信行日志/财务部#16.md` 横幅、`队列行日志/#423.md`／`#424.md` 更正段、取证件 §二 **四处都逐行写了她选哪一档**。`#563` 这句不成立；本件不改 `#563`，由承接方在收工回写时自行更正。

---

## 三、🔴 档停点：须向唐燕萍确认的矛盾项（本件不替他选）

**B 表 #7 与 C 表 #8 互斥、她两条都勾了「✅ 这样判对」**：

| 判例 | 她判对的原文（回件表内「拟改后系统这样处理」列） |
|---|---|
| #7 | 允许「一张发票行 ↔ 同一个料号的若干 AP 行合计」；凑得出唯一一组就认领。🔴 **凑法不唯一、或要跨两个料号才凑得出来时，一律不认领、不挑一个** |
| #8 | **跨两个料号合计开票**（J02E.0024 1,000 ＋ R02E.0024 5,000 ＝ 6,000）：**把这一行发票拆成两条写进系统**，未税金额与税额按数量比例分摊 |

同一形态（跨两料号才凑得出）在 #7 是「一律不认领」、在 #8 是「拆两条认领」。`#424` 09-07 的实现＝「⒜金额→⒝单料号→⒞组合求和依次回落＋`_ALLOW_INVOICE_ROW_SPLIT` 拆行」，**等于按 #8 办、把 #7 那句红字当作被 #8 覆盖**——这是我方在没有她裁定的情况下替她选了。

**待向唐燕萍确认的问句（可发送态，本件不发）**：

> 判例 7 里我们写「跨两个料号才凑得出来时，一律不认领」，判例 8 里又写「跨两个料号合计开票时拆成两条按比例分摊」，你两条都勾了「判对」。请你定一句：跨两个料号才能凑齐的发票行，**(a) 按判例 8 拆成两条认领**（现在线上就是这样跑的，505 行同形态会被认领）；还是 **(b) 按判例 7 一律不认领**、留在未匹配发票池（那 505 行不进核对）；或 **(c) 拆行但另加条件**（请写一句，例如「只在两料号同属一张 AP 单时才拆」）。

🔴 **本项无默认，须她明确答复**（口径判据类，且线上已按 (a) 在跑——不答的代价是「错误继续发生」而非「停在原地」）。落点建议：随下一封 `财务部#18`（🆕 待发）带上，**不新起信**；登记 §四 由业务总线 `OP-0911-H` 认领（本棒不写 `6-人才与组织/`）。

---

## 四、应改清单（汇总，本件不执行）

| # | 载体 | 现文（错／缺） | 应改成 | 归属 |
|---|---|---|---|---|
| 1 | README 主表 `采购部#18` 发送状态格 | 「⚠️ 闭环≠口径已确认：三条读数口径判例表 9 个勾选格全空，须另行追问」 | 前置一段「🔴 **08-28 更正（`#428`／§四 `#133`）：『9 格全空』是 `python-docx` 读取假阴性，三行『✅ 这样对』08-26 即已勾；下句作废、勿再追问**」，原句保留 | 业务总线 `OP-0911-H`（触碰区）；用 `工具-跟进信README登记.py set-status --number 采购部#18` 整格替换 |
| 2 | 队列 §一 `#423` 行内首段 | 「判例表 A 四选一未勾选…9 行勾选格均为空」 | 见 §二 表 | CC 收工线（`#423` 值周审计 ⑴ 已点名「交 CC 收工线整格重写」，同车） |
| 3 | 队列 §一 `#424` 行内首段 | 「依赖唐燕萍签认」 | 见 §二 表 | 同上 |
| 4 | 队列 §四 `#122` 首段 | 「三条判例零勾选与问题 1 答案冲突的处置」 | 加指针「（零勾选＝假阴性，见 `#428`；裁决不受影响）」 | 同上 |
| 5 | 队列 §一 `#563` 状态列 | 「`财务部#16` 的逐行答复从未回灌」 | 改为「逐行值 09-07 已入 README／行日志／取证件；**残留的是 `#423`/`#424` 行内首段未标证伪**」 | `#563` 承接方（本棒收工回写时以追段形式写明，不改原句） |
| 6 | 新增 §四 行 | — | 唐燕萍 #7/#8 矛盾问句（§三） | 本棒登记（§四 新行），业务总线认领随 `财务部#18` 发 |

---

## 附录 A · 上游对账件三处分辨率不足（不是回灌错，是对账件自身）

| 份 | 上游写法 | 真值 |
|---|---|---|
| 数据闸对照表 | 10 行全「第3列」 | 一格三框：否 ×8（1-5,8,9,10）／部分 ×2（6,7）；各行「何时通」26.7.10／13／15 |
| `采购部#13` | 「第5列」×2、「第4列」×1 | ❌ 错 ×2（行 1,2）、✅ 对 ×1（行 3），✏️ 三行都写了字 |
| 07-29 跨月占用 | 「第4列」×3 | ✅ 对 ×3 ＋ ✏️ 三行同句「按照现状判定…」（勾与字表面相反） |

## 附录 B · 各份取证计数（本棒重跑）

| 份 | checked=1 | ☒ | ☑ | sdt | 与上游计数 |
|---|---|---|---|---|---|
| 数据闸 | 10 | 10 | 0 | 30 | 同 |
| 财务部#16 | 8 | 8 | 0 | 24 | 同 |
| 质量部#9 | 6 | 6 | 0 | 14 | 同 |
| 07-29 跨月 | 3 | 3 | 0 | 6 | 同 |
| 采购部#13 | 3 | 3 | 0 | 6 | 同 |
| 采购部#16 | 3 | 3 | 0 | 9 | 同 |
| 采购部#17 | 8 | 8 | 0 | 24 | 同 |
| 采购部#18 | 3 | 3 | 0 | 9 | 同 |
| 采购部#19 | 6 | 6 | 0 | 16 | 同 |
| 采购部#20 | 4 | 5 | 0 | 10 | 同 |
| 采购部#22 | 7 | 7 | 0 | 21 | 同 |

## 附录 C · 范围外 2 份（7 月内联式表单，只登真值、未对账）

- `采购部-YaoZuYi-回复-2026-07-14-7-17缺料批改会+净额签字-会材姚祖怡反馈稿7-14`：`checked=1` 17／`☒` 17。A1 改（SRM 答交日期≤需求日才计入可用）／A2 改（L/T 改可手工修改日期）／A3 确认／A4 确认／B2 确认／B3 改（BOM 取当前生效版本）／B4 确认／C-1 确认／B1 排期＝分批／PMC 月度优先级表＝汤易水每月月初／21 行新判级整体认可＋3 张转 🟢＋18 张转 🟠＋黄金基准更新＋登记翻 ON 原因（5 项全勾）／净额签字＝7/17 同场、完成。
- `采购部-YaoZuYi-回复-2026-07-26-…保供批1抽验与批2三口径+7.23姚祖怡反馈稿V6`：`checked=1` 9／`☒` 9。子件供给状态＝有问题／#14 需求日可齐套＝有问题／分页＝没问题／导出 Excel＝有问题／品名＝没问题／图例＝没问题／关闭状态存 ERP／仅采购经理·PMC 能关／每次关闭记录谁·何时·关多少·原因。
- 两份均为判例批改法（07-27）之前的表单，7 月已回灌（`采购部#2`／`#5`），本件不对账。
