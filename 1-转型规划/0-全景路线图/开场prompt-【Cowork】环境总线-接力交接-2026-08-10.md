---
title: 环境总线接力交接 —— 两天机制整治收官，新线聚焦构建环境与工作流
created: 2026-08-10
updated: 2026-08-10
执行方: Cowork 环境总线
status: 生效
前一份: 开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md（已完成使命，§六bis 边界正本仍在其中）
---

# 开场prompt ·【Cowork】环境总线 · 2026-08-10 转场

## 开场词（复制即用）

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ ｜ 工作区：企业AI转型（只改 .md 与 Cowork artifact，不写生产代码、不自行 commit，收工只登记 §二 批次） ｜ session：新开

读 1-转型规划/0-全景路线图/开场prompt-【Cowork】环境总线-接力交接-2026-08-10.md ＋ 根 CLAUDE.md 当前进度恢复上下文，先按文件 §〇 自检四查，通过即直接开工、有差异再问我。
```

---

## §〇 开工自检四查（A 类硬规则）

1. **接力时效**：队列里若已出现更晚的批次行，**以队列为准**。
2. **在办清单抽核**：抽 §三 任意 2 行核对队列**实际状态列**。🔑 **读状态列开头**，一律用 `python 0-学习与工具/工具-队列查询.py --row N`（不截断、头尾互斥会打冲突警告）。
3. **残留待补**：grep 四条接力顶部「⏳ 队列更新待补」。⚠️ **命中 ≠ 有待补**——可能是"已回补完毕"的留痕节或正文里的引用叙述，**须读内容再判**。
4. **skill 两向漂移扫**：把沙箱只读挂载 `.claude/skills/zhuopin-*/SKILL.md` 与 `0-学习与工具/skills源码/<同名>/SKILL.md` 逐份 `diff`（跳过各自 frontmatter 后比正文）。
   **2026-08-09 基准：四份全部 0 差异行**——非 0 即是这之后产生的新欠账。

---

## §一 🔴 本线定位（2026-08-10 Shao Peishen 明确收窄，**新线首要纪律**）

**本线只做构建环境与工作流程的优化，不做具体业务线的任务调度。**

**判别（与 §六bis C1 的既有边界一致，本条是它的进一步收窄）**：

| 做 | 不做 |
|---|---|
| 构建环境与自动化机制本身（worktree/stash 台面、编辑锁、sweep、巡检巡逻、队列纪律、openspec 工作流、本机工具链、机器人链路） | **业务场景的任务分派与排期**——那是值周巡检《本周计划》与拆件巡逻的职责 |
| 机制取证、方案定稿、队列行写入 | 替业务线决定"先做哪个场景、派给谁" |
| 工作流本身的缺陷诊断与修法 | 业务口径判定、专员回件处置、场景建造 |

🔑 **为什么要收窄**：2026-08-09～10 两天里，本线多次因"顺手把活也派了"而越过分发环节（值周巡检与拆件巡逻才是派活载体，见 §六bis C2）。**收窄不是缩小价值，是让本线回到它唯一不可替代的位置——没有别的线在看"我们是怎么干活的"这件事本身。**

⚠️ **例外**：真实阻塞或安全风险可即时升级，但须在队列行内标明"因何紧急而绕过机制分发"。

---

## §二 两天成果总结（2026-08-09 ～ 08-10）

### 2.1 断根类（整类问题消失，不再需要人记住）

| # | 成果 | 为什么算"断根" |
|---|---|---|
| **#314③** | **裸竖线断根** —— `queue_table` 反引号感知切列（游程配对算法，非单反引号正则） | 此前靠 #164「表格正文禁写裸竖线」这条**人守规则**，而它要求人在写 `grep "A\|B"` 这类命令片段时记得改写——**违反直觉，故违反率不随提醒下降**（2026-08-09 一个 session 内犯 3 次，其中一次是在**描述这条规则本身**时）。现在解析器认得它，**整类消失** |
| **#314①** | **行结构损坏不再隐身** —— `_table_data_rows` 行首尾双检根因已修 | #313 那行被撑破后**扛了 5 个提交无人知**，因为它压根没进解析器的返回列表——不是"放行"，是"看不见" |
| **#313②** | **全局 editable 指针已卸载** —— 四轮追完（11 处 CLI 引导 ＋ 3 处 conftest） | 此前任一 worktree 跑一次 `pip install -e` 会静默顶替其余 worktree 的 import；现在只能走路径引导 |
| **#312⑹** | **看板 JS 侧计数改读机器字段** | #308「消灭用正则猜中文」在 JS 侧**最后一个存活实例**已清 |

### 2.2 止血类（正在发生的失效）

- **#313③ 决策提醒第二载体**：自 #306 落地起每轮崩溃、#219 双载体实际退回单载体，而失败被 sweep 降级成一行 ⚠️。已止血并验证。
- **#96 并入部署前置检查**：2026-08-10 FI2 部署**真实下线**（`:8094` 启动即崩），根因是 #300 引导代码在 `.51` 扁平布局下硬抛异常。⚠️ **跨服务排查尚未做**——SC8／QD-B／命令中心／企微机器人若 #300 后未重新部署过均可能潜伏同款，**只在下次部署时炸**。

### 2.3 口径类（把说错话的规则改对）

- **#314②**：三个滞留 openspec 包的归档口径 ＝ **(c)**——**问题不是"包没关"，是告警在说谎**。它报「疑似**遗忘**归档」，而事实是「在等真实场景」；措辞与事实不符，所以没人动它。**一个说错话的告警比不报警更糟，它会训练所有人忽略这一整类告警。**
- **协议〇.9 措施 C 上限 8 → 16**：不是 WIP 暴涨，是 #308 机器字段落地后**计数口径第一次变准**（旧口径只数 `open` 一档，漏掉 partial 与 hold）。**上限 8 是在"以为只有 3"的前提下定的，从落地起就一直在超限而无人知道。**

### 2.4 新立与承接

**#315 业务·环境双队列拆分** —— 方案 08-08 已 `status: 生效` 且三项全批，硬前置今日解除，**却零行承接**，直到收工盘点被问"剩余任务是否都已登记"才发现。**这是「结论做完了却无载体承接」的又一实例，且发生在一份状态已是"生效"的正式方案件上——比规则未入法更隐蔽。**

### 2.5 🔑 可复用的方法学（本线两天最值钱的产出）

1. **「枚举法会反复给出『已完备』的错觉」** —— 同一教训两天内出现**四次**：#300 入口清单／G6 CLI 穷举维度／#314③ 消费者清单（第 5 个是 suggested task 才捡回来的）／#321 README 全表比对。**遇到"清单类"收口，先问它是枚举出来的还是穷举出来的。**
2. **穷举是维度相关的** —— CLI 入口一条路径、pytest collection 另一条，**各自独立验证**。"这次全绿了"不是答案，"还有没有第三条路径、依据是什么"才是。
3. **两个可复用的证明方式**：⑴ **拿我方无法伪造的独立信源反驳自己**（用 `origin/master` 证伪"本地状态可能是假的"）；⑵ **全量逐行 byte-identical 对比**（台账修复对真实队列 114 行逐行 diff，证明"只有目标行变、其余 113 行字节相同"）。**测试只能覆盖想到的场景，全量对比才能证明没有意外改动别处。**
4. **「工具静默回退」已累计五个形态**：`git -C` 向上找祖先仓库／PowerShell 变量名大小写不敏感／`Select-String -AllMatches` 返回空集／`_table_data_rows` 看不见破损行／🆕 **`List.InsertRange` 收到 PowerShell 原生数组时静默插入 0 项**（2026-08-10 A4 实测，靠行数差 8 抓出，全程零报错）。
5. **验证域漏掉一种环境 = 测过了、全绿、然后炸** —— #300 让本地更健壮，代价是给生产布局加了一个**从未在 `.51` 上验证过**的硬前提。**本地永远找得到那个目录，所以本地怎么测都是绿的。**
6. ✅ **一处正面证据**：2026-08-10 A1／A2／A3 三个 CC 并行，**各自都撞了一次并发，三次全部被机制自行吸收**（rebase/pull 干净同步、批次机制兜住被带走的编辑、幽灵副本自查自撤），**无一需要人介入**——对照 08-08 同等密度下"分叉后 sweep 整轮跳过、批次积压、需手工解卡"。**这是 #288／#309 子项 F 收尾段统一对齐的首个真实并发验证。**

---

## §三 当前状态（以队列状态列为准，本表为 2026-08-10 快照）

| 行 | 状态 | 说明 |
|---|---|---|
| #322 | 🔄 propose+design 已产出，**等 Shao Peishen 答决策点 1 后 apply** | 编辑锁 mutex 死循环；**本线建议 (a) rename-away**（互斥保证来自 `O_CREAT\|O_EXCL` 原子创建，rename 只清空路径、不引入双持有；(b) 会退回 #197 修复前的两步式竞态） |
| #314 | ✅ ①②③ 全完工 | |
| #313 | ✅ ② 已真实执行 | |
| #312 | 🔄 ⑹ 看板半已完成（2026-08-10 本线），**提醒侧代码半（CC）未动** | |
| #315 | 🆕 待领（P2，须走 openspec 含 design 审）；**本周明确不发** | 触碰区同时撞编辑锁与 sweep；本周"零串行"正是靠押后它换来的。已含子项 ⑥（幽灵副本陷阱） |
| #96 | 🔄 部署前置检查已并入，**跨服务实测未做** | ⚠️ **下次任何服务部署前必须先核这一项** |
| #98 | 🆕 待领 | 月度环境体检，8/28 触点 |
| #82 | 🔄 已开工 | 首份真实税务导出已入库；字段矛盾随之自解 |

---

## §四bis 🔴🔴 新线第一件事：降级—占锁自我强化循环（2026-08-12 实测，**优先于下方 §四 全部条目**）

> **本节是 2026-08-12 追加，写在 §四 之前，因为它已不是"建议方向"而是正在恶化的机制故障。**

### 现象（三次实证，两天内）

| # | 时间 | 谁降级 | 占锁方 | 结果 |
|---|---|---|---|---|
| 1 | 2026-08-10 | 环境总线转场收工 | 企微机器人（归档追加） | 块 A 存接力，**挂 2 天无人回补** |
| 2 | 2026-08-12 14:4x | 拆件巡逻第一班 | 企微机器人（归档追加） | 块 B 存接力，至今未补 |
| 3 | 2026-08-12 15:3x | 环境总线代补块 B | 企微机器人（独立触发脚本兜底补录） | 🔴 **在本线刚 `release` 完块 A 的 7 分钟内被抢走**，块 B 仍补不进 |

### 机理（两半，合起来自我强化）

**第一半 · 机器人侧**：`acquire` 之后**从不写 `released`**（实测锁文件 `released` 字段恒为空）。

🔴 **频率须以实测为准，勿沿用"每 1-3 分钟"那个说法**——2026-08-12 拆件巡逻简报里写的是「每隔约 1-3 分钟重新 acquire」，**本线当日读锁 history 实测证伪**：

```
05:36:42Z  企微机器人  独立触发脚本兜底补录
06:39:43Z  企微机器人  归档追加                    ← 距上次 63 分钟
07:29:12Z  Cowork-环境总线-块A回补                 ← 人类抢到（因上一次已陈旧）
07:30:40Z  企微机器人  独立触发脚本兜底补录        ← 🔴 距人类 release 仅 88 秒
```

**⇒ 真实节奏是约每小时一次 acquire，而非 1-3 分钟。** 但**因为它不释放，每次占用都会锁死整整 30 分钟**（直到陈旧可接管）。

🔑 **为什么这个数字差别很要紧**：若真是 1-3 分钟，人类几乎永远进不去，唯一修法是改机器人；**实测每小时一次意味着每小时约有 20-30 分钟的可用窗口**——只是窗口位置不可预测，且**人类一释放它就在 88 秒内补位**（说明它在等锁、有重试逻辑）。**修法方向因此不同：不是"抢不到"，是"窗口不可预测且无人知道何时可用"。**

⚠️ **本条更正本身也是一个判例**：巡逻那个数字来自观感而非读 history，**本线引用时未复核就写进了 opener，随后才实测证伪**。**引用他人观察时先问一句"这个数字是测出来的还是看出来的"。**

**第二半 · 降级路径侧**：协议〇.7 规定了"待登记内容"降级去哪（接力「⏳ 队列更新待补」），**但没有任何机制会去回补它**。
`zhuopin-queue-audit` 证据源⑨ 只「列出并催回补」，**不代补**；于是回补完全取决于是否恰好有人类 session 开工并读到那一节。

**⇒ 合起来**：占锁越频繁 → 降级越多 → 待补块越积 → 而回补又需要抢到同一把锁。**这是个会自我强化的循环，且已在两天内从 1 块涨到 2 块。**

### 🔑 附带的第二层危害（比卡住更值得注意）

**降级会产生孤儿脏文件**：块 A 那次留下 `开场prompt-…2026-08-10.md` 未提交且未被任何批次声明，**挂 2 天**；2026-08-09 已实证过同类孤儿脏文件会**挡住 sweep 收尾段的 rebase**。
⇒ 这不只是"登记慢了"，是**会向下游传染的**。

### 建议的处置方向（**只是方向，须实测定因后再定修法，勿照抄**）

1. **机器人侧**：查清它为何不写 `released`——是异常退出、还是根本没调 `release`？若是后者，**这是纯 bug，修它即解一半**。
2. **回补侧**：让"接力里有未回补块"变成**有载体的检测**而非只在巡逻简报里列一行——候选落点：sweep 起跑段（它每轮都跑）或 `工具-队列结构lint.py`（CI 侧）。🔴 **须先答协议〇.9 措施 B 的 one-in-one-out。**
3. **⚠️ 别急着调 30 分钟陈旧阈值** —— 那只是让人类更容易抢，**不解决"机器人不释放"这个根因**，且会削弱陈旧判定本身的安全性。

### 未竟事项（新线开工即可接手）

- 🔴 **块 B 仍未回补**（内容在 `session接力-Phase1收口.md` 顶部，**逐字可抄、是转录不是判断**）：销 §一 #329／#330／#331 ＋ 回灌 #240／#81／#266／#118 ＋ 新增 1 行（物料看板，`--domain 业`）＋ §四 2 项 ＋ README 两行 ＋ 登 §二 批次 `B-0812_拆件巡逻三行回件与物料看板立行`。**取号用 `--reserve-multi 一:1 四:2 --domain 业`。**
- **块 A 已于 2026-08-12 回补完毕**（批次 `B-0810_环境总线转场与两天成果总结`），接力内只留一行留痕。

---

## §四 下一 session 建议方向（**本线只做机制，不派业务活**；⚠️ **均排在 §四bis 之后**）

> **次序与并行**：以下均为**建议**而非派单——真正的分发走值周巡检与拆件巡逻（§六bis C2）。

**优先级建议**：

1. 🔴 **#96 跨服务排查的落地方式** —— 这是唯一带"下次部署就炸"性质的挂账。**本线可做的是把检查项写成可机器执行的形态**（而非清单上的一行人守文字），使它挂在部署动作上、不依赖执行者记得。
2. **两次「绕锁改写」告警未定位**（2026-08-09 两次，326→326 行约 2 行不同）——`acquire` 检出但无人溯源，属机制自身的可观测性缺口。
3. **机制类可动 WIP 20／16 连续走高** —— 当日已调过一次上限（8→16）仍超。**建议判"该压一批"而非"再调上限"**；再调就该问是不是别处出了问题。
4. **收工段模板缺「确认编辑锁已释放」** —— 已挂进协议〇.7 第 3 条收工步骤（#314② 同车做掉），**下一 session 可核一次它是否真的生效**。

---

## §五 决策项现状

🔴 **待答 1 项**：#322 design 决策点 1（mutex 清理失败的退路机制）——**本线建议 (a) rename-away**，理由见 §三。

📌 **本周计划 B 段另有 4 项决策 + 4 项悬置**（不属本线，见 `本周计划-2026-08-10.md`）。其中 **IT部#7 已压 5 天**，巡检自陈「(c) 维持是三个选项里唯一没有收益的」。**悬置最久：泓钦线下知会，36 天。**

---

## §六 纪律段（引用条目名，不复述）

- 根 `CLAUDE.md` §5：完工即归档／§0.2 登记／直读原则／**工具静默回退（已五个形态）**／规则退休制／**多任务须指明次序与并行**／决策路由须标具体 session 名／**日期取本机本地**
- 🔴 **编辑锁**：改队列前 `acquire`、改完立刻 `release`；**`acquire` 与写盘两步、中间查 `$LASTEXITCODE`**；🆕 **收工前确认锁已释放**（2026-08-09 两次"已报收工而锁未释放"）
- 🔴 **取号**：`--reserve` ／ `--reserve-multi`；**取号前先做并入审核（协议〇.10）**
- 🔴 **三条静默失败写法禁令**：完整路径／不得裸竖线（**#314③ 后反引号内已安全，但表格正文裸竖线仍会撑列**）／不得裸反引号
- 🔴 **写后反查**：`(Select-String -Path $f -Pattern $k -SimpleMatch).Count`（**不加 `-AllMatches`**；**`-Pattern` 串内勿含反引号**——PS 转义符会吃掉一截并静默返回 0）。**交叉验证优于重复同一验证**，至少配两个互不相关的指标
- 🔴 **查主工作区脏文件须本机直读**（沙箱侧 git 因 `core.autocrlf` 差异虚报 30+ 文件）
- 🆕 🔴 **worktree 内勿用通用 Edit 工具直接改队列文件** —— 锁 CLI 经 `--git-common-dir` 恒解析主工作区，Edit 按 worktree 本地路径，**是两个物理文件**，会产生谁也看不到的幽灵副本（2026-08-10 实测，已并入 #315 ⑥）
- **批次即扫**：登记后立即 `Start-ScheduledTask -TaskName ZhuopinCommitSweep`；⚠️ **触发 ≠ 落库**——锁被占用时整轮零动作跳过且只写日志，**须回看 `reports/sweep-commit.log`**

**§六bis 本线边界（C1 派单边界／C2 机制收口为默认交付）正本仍在** `开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md` §六bis，**未迁移、以那份为准**；本文件 §一 是其 2026-08-10 的进一步收窄。

---

## §七 收工段

滚动更新本文件（日期节只留 2 个）→ 列文件清单 ＋ 建议 commit message ＋ 登记 §二 批次（**须含队列文件自身、状态列禁带 ✅**）→ 触发 sweep → **确认编辑锁已释放**。Cowork 不擅自 commit。

---

## §八 日期节（R5：只留最近 2 个）

### 【2026-08-10】本周计划 A 段四项走完 ＋ 一次真实生产下线 ＋ 本线定位收窄

**A 段**：A1 #322 propose+design 已出（等决策点 1）／A2 #314② 收尾复核（发现②早已完工、只是领取方列未同步，顺带修 `design.md` 自引用导致的 defer 误触发）／A3 #321 四项完工（**采购部#11 整行曾被并发 Cowork session 从陈旧副本静默删除——真实编辑锁竞态**）／A4 #312⑹ 看板两处同批改完（本线执行）。

🔴 **一次真实生产下线**：FI2 部署时 `:8094` 启动即崩，根因 #300 引导代码在 `.51` 扁平布局硬抛异常；已修复重新部署，**但跨服务排查未做**，已并入 #96 作为部署前置检查。

**本线定位收窄**（Shao Peishen 当日定）：**只做构建环境与工作流优化，不做具体业务线任务调度**——见 §一。

### 【2026-08-09】机制整治主日：裸竖线断根 ＋ #313 四轮追完 ＋ #314 三项

见 §二 成果总结。当日跑完 G1–G9 ＋ 1 个 suggested task，**新立 #313／#314／#315 三行、清掉 #221／#252／#267／#305／#308／#310**。
