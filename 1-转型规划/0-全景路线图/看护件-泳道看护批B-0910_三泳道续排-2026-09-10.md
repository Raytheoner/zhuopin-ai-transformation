---
title: "看护件 · 泳道看护批 B-0910_三泳道续排（2026-09-10）"
created: 2026-09-10
status: 待派出
批次: B-0910_三泳道续排
派出线: Cowork 业务总线 OP-0910-A
---

# 看护件 · 泳道看护批 `B-0910_三泳道续排`

## 〇、口径

- **触发**：Shao Peishen 2026-09-10 10:2x 答 `1a，2a`（`#507` apply 并进下一批、三条泳道照常并行排）。规则正本＝`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（v2.2）。
- **D1 四档现取**（`工具-泳道看护状态机.py criteria`，非手抄）。**判据未覆盖按 🟡 停。**
- **前置四查（2026-09-10 10:1x 本机现取）**：① LAN 探针 ＝ **✅ on-LAN**；② 分支 master ＝ `c31b264`、`ahead=0 behind=0`；③ 编辑锁**无占用**；④ 上一批 `B-0910_五泳道全启动` summary 已出（停 1 次／转出 0／五条分支内容全在 master）。
  > 🔴 **起棒时须自己重跑一次** —— 本日 07:23→07:5x 之间 LAN 状态就翻过一次，10:0x→10:1x 之间 master 从 `6e04f00` 走到 `c31b264`（同 §一 `#522` 现取表过期守）。
- **上限**：默认 10；本批实际入批 **3 条泳道 / 3 个队列行**。
- **交付模式**：**CC 看护者单粘贴 ＋ 看护者用 Task/Agent 扇出**。他只粘 §三bis 一段。

## 一、硬边界

1. 🔴 **本批与今日另派的两棒串行件并行，但优先级在后**：`OP-0910-K`（`#443` 清扫）与 `OP-0910-I`（K2 外置 21 行）**独占两份队列真身与编辑锁**。本批三条泳道触碰区与队列真身**零重叠**，可并行；🔴 **但各泳道收工回写队列时会抢锁** ⇒ **若 acquire 被拒，按「登记待补」写 `队列回写待补/*.json`，不重试、不绕锁**。
2. 🔴 **⏭️ 档（`.51` 部署及触碰生产服务）本包不执行**；本批 3 条全为纯库内活，零 `.51` 触碰。
3. 🔴 **🔴 档（对外发送／L2 门禁签字／合规红线变更／ASIL C-D）永不代办**。`采购部#22` 已于 2026-09-09 15:07 发出，**任何泳道不得重复发送或补发**。
4. 🔴 **看护会话不修工具**：跑批期间发现机制缺陷只登记＋出派单，不在本会话修；唯一例外＝本批会因该缺陷失败且修补 ≤10 行。
5. **看护件落档失败即全停。**

## 二、次序与并行矩阵

**波 1（并行 2，触碰区零重叠）**

| 泳道 | 承接 | 触碰区 | 硬序 |
|---|---|---|---|
| `507-sweep-apply` | §一 `#507` | `0-学习与工具/工具-落库sweep.py`（覆盖校验段）＋其单测；`openspec/changes/sweep-manifest-coverage-guard/` | ✅ **design 审已过**（Shao Peishen 2026-09-10 08:5x 答 `1a，2a，3a，4a，5a`，**D4 阈值 K=3**；D6 为 `#136` 承继约束不列拍板项）。本棒＝apply |
| `544-heartbeat-batch` | §一 `#544` | `0-学习与工具/工具-泳道看护状态机.py`（`heartbeat` 段）＋其单测＋`skills源码/zhuopin-lane-watch/SKILL.md` 步骤 4 样例 | 无前置。🔴 **与 `#529` 同碰本文件与本 SKILL**，故 `#529` 排波 2 |

**波 2（1 条，独占）**

| 泳道 | 承接 | 触碰区 | 硬序 |
|---|---|---|---|
| `529-timeout-semantics` | §一 `#529` | `0-学习与工具/工具-泳道看护状态机.py`＋其单测＋`skills源码/zhuopin-lane-watch/SKILL.md`＋`openspec/changes/lane-watch-mode/design.md` | 🔴 **必须等 `544` 落地后再起**（同文件同 SKILL）。⚠️ **本条改口径判据阈值 ⇒ D1 🟡 档，泳道跑到那步必然 `pause` 停等他一字母**，这是预期行为、不是故障 |

### 本批派出前，总线已核实的四件

1. **`#507` design 审已过**，本棒只做 apply；🔴 **开工须先现取 master 版本**——`#500`（sweep 第 5 类脏检查放宽）已在 master（`git grep` 2 处命中），本棒修复要**建在那之上**，不得照 `#507` 2026-09-08 立行时的描述直接动手。
2. **`#544` 是 `#536` 的直接残余**：`#536` 已 ff 入 master（`c31b264`）并把 summary 读侧口径改对；**归属从哪来没变**——`lane_state` 的 `batch` 仍只由 `heartbeat --done --batch`／`pause`／看门狗写入，本棒补的是**写侧**。
3. **`#529` 的口径他早已答过**（2026-09-09 答 `2a`：超时未答按推荐/默认执行），但**实现时若碰到新的阈值/语义分叉，仍按 🟡 停**——已答的是方向，不是每一处取值。
4. **`#419` 已转 `[S:blocked]`**（代码侧无待办、已 ff；剩余 D1 凭据落位与 5.2 人眼半边**均为他本人动作**）⇒ **本批不入，任何泳道不得代做那两项**。

## 三、泳道 opener（三行结构；全部由 `工具-opener生成.py --variant subtask_lane` 产出，未手抄）

### A1 · #507 sweep 覆盖校验自锁 · apply

粘贴端：CC ｜ 泳道：507-sweep-apply

```
[OP-0910-L]【CC】507apply
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910l-507-sweep-apply`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_三泳道续排）
读 ① `队列 §一 #507` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A2 · #544 heartbeat --done 补 --batch

粘贴端：CC ｜ 泳道：544-heartbeat-batch

```
[OP-0910-M]【CC】544心跳带批次
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910m-544-heartbeat-batch`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_三泳道续排）
读 ① `队列 §一 #544` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A3 · #529 check-timeout 语义（🟡 会停等）

粘贴端：CC ｜ 泳道：529-timeout-semantics

```
[OP-0910-N]【CC】529超时语义
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910n-529-timeout-semantics`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_三泳道续排）
读 ① `队列 §一 #529` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

## 三bis · 看护者开场词

```
[OP-0910-J]【CC】看护三泳道续排
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_三泳道续排）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0910J-看护三泳道续排。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_三泳道续排-2026-09-10.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0910_三泳道续排` **现取**，不手工数。

🔴 **本批的 summary 有两处特殊，收工时必须注明**：

1. **`544` 修的正是 `heartbeat --done` 的批次归属** —— 若该泳道已合入，本批 summary 的「归属未知」计数会**因它自己的修复而变化**；**须注明用的是修前还是修后版本**。
2. **`#536` 已于本日 ff 入 master**，故本批 summary **已按批次收敛**（上一批实测报出「另有终态泳道 8 条批次归属未知」正是它生效的证据）；但 `#536` 行内明写「**只实证了终态泳道数一列，停点数／转出数／放行数未逐一验**」⇒ **收工时不得因为看到数字就认为其余列已正确**。

🔴 **等待时长只认 `reports/lane-watch-state.json`（由 summary 现取），会话不得自算。**

🔴 **心跳一律跑命令写**：`python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane <泳道标识> --text "<一句话>"`，收工带 `--done`；**别传 `--repo-root` / `--heartbeat-file`，它没有这两个参数**。
🆕 **本批起，收工 `--done` 请一并带 `--batch B-0910_三泳道续排`**——这正是 `#544` 要修的那件事，**在它落地前先靠人守带上**，免得本批自己又落进「归属未知」。

### 本批与今日两棒串行件的关系（收工时一并看）

今日 `OP-0910-K`（`#443` 清扫）与 `OP-0910-I`（K2 外置 21 行）在同时推进，**它们会改动队列真身的行内容与行数**。本批各泳道收工回写时若发现**目标行已被迁档**（清扫迁走）或**整格已被外置**（只剩首段＋末段＋指针），**这是预期的，不是失真** —— 按当前真身现状回写，**不要试图恢复被外置的原文**。
