---
title: "拆件巡逻报告 · 2026-08-10"
created: 2026-08-10
status: 在办
班次: 第一班（定时任务触发，非交互）
执行: Cowork · 回件拆件巡逻 Dispatch v0.5（阶段二 live）
---

# 拆件巡逻报告 · 2026-08-10（第一班）

**识别 5 行 ／ 已拆 5 行 ／ 销行 #316 #317 #318 #319 #320 ／ 新增 §一 #321 #322 ＋ §四 #58**

---

## 顶部提示（三条，先看这里）

1. **🟢 待发信盘点无积压**：三态计数 —— `⏳ 待你审` **1**（IT部#7，2026-08-05，IT部·陈承，企微自建应用凭据申请＋AP 附件下载异常）／`🆕 待发` **0**／`⏸ 暂缓` **0**。**无「队列称暂缓而 README 仍待发」的红标情形**（`🆕 待发` 为 0，该类不一致在结构上不可能存在）。
2. **🟢 「该起草而没起草」缺口检测：无缺口**（`draft_gap_check.py --window-days 14` 原样输出：`待发信盘点（扩展版，近 14 天已部署场景 vs 已起草跟进信）：无缺口`）。
3. **🔴 两条新发现的机制缺口已立行**：#321（一封已发出的信在 README 无登记行）与 #322（编辑锁 mutex 在 Cowork 沙箱下必然遗留、下一次 acquire 无限卡死）。**#322 是 P1**——它卡的是 Cowork 侧全部共享文件的写入通道，今天实测已卡死三份接力文件达 3 天而无人知。

---

## 一、#316 ／ #317 ／ #318 ／ #319 —— 四条企微测试短信，销行

- **行号**：§一 #316、#317、#318、#319（均 2026-08-06 登记，领取方 Paul）
- **文件**：`7-外部文档/待分拣/待分拣-ShaoPeiShen-回复-2026-08-06-文本反馈-{7bb79f26…, 0ee7b1cb…, 69d75cb0…, 6a502beb…}.md`
- **内容一句话**：四份文件各 33 字节，正文逐字相同 —— 「@邵总数字人🤖 测试短信」。
- **已执行动作**：四行状态列改写为 `[S:done][D:机] ✅ 已拆件（2026-08-10 拆件巡逻）`，判为**非业务反馈、无需回灌**。
- **判断依据**：与已确认的 #271–#278／#280 **同族同批**（Shao Peishen 为采集部门群 chatid 在各群发的标注测试短信，队列 #270／#279／#281）。此处**沿用既有裁决、未新下判断**——#274–#278 的处置结论原文即「非真实业务反馈，无需回灌处理」。
- **需 Shao Peishen 项**：无。

---

## 二、#320 —— 姚祖怡 08-10 回件：两项复核全部通过，销行

- **行号**：§一 #320（2026-08-10 登记，领取方 采购专线）
- **文件**：`7-外部文档/采购部/采购部-YaoZuYi-回复-2026-08-10-文本反馈-68ff72e0fac0a4215f9831b9f7ceabac.md`（474 字节，纯文本，已通读全文；无表格、无附件，正文即全部内容）
- **内容一句话**：她对 **采购部#12**（2026-08-07 发出）第四节请她抽空看的两件事逐一作答，**两项都是「对的」**——这是答交口径拉锯四轮以来第一次「都对」。

### 已执行动作（回灌五处 ＋ 销行一处）

| 落点 | 动作 |
|---|---|
| §一 #320 | 销行，状态列写全结论与边界 |
| §一 #296（答交口径 v4 三态判据） | 追记「2026-08-10 姚祖怡回件验收通过」＋她的原话 |
| §一 #297（替代料并列展示） | 追记同上，并记明 CC 是以 `F02N.0254` 复现（她举证的 `F02N.0242` 已不在活跃订单），她未提异议 |
| §一 #266（可齐套两字段她是否依赖） | 追记：它等待的那封合并信已闭环，但本行当时未被纳入，等待前提转为「下一封信是否起草、是否纳入」，见 §四 #52 |
| §四 #52（下一封给姚祖怡的信怎么组织） | 追记「前提第三次变化，且这次是**条件已满足**而非选项失效」，并把它重出为可直接作答的三选一（见下） |
| README 跟进机制表 采购部#12 行 | 状态列回填为 `📥 已回件并回灌（2026-08-10 …）`，**原状态原样保留在后** |
| `session接力-采购域场景落地.md` | 新增【2026-08-10】节，含原话全文转录、两项对照表、两条边界、串行闸解除说明 |

### 判断依据

- **两项验收**直接引她原话，不作推断：①「我找了一个最近有 SRM 答复的料号，答交数量/日期跟供应计划板上的是一致的」→ #296；②「有替代料的成品卡片，主料/替代料两行信息我确认下来都是对的」→ #297。
- **第三节（黄金基准索签）她未置一词**——采购部#12 该节原文明写「**如果你没意见，这条不用回复**」，故其沉默按**信中自设约定**即「无意见」，与 §四 #55 已拍板的 (a) 默示接受一致。**此处沿用既有裁决，未新下判断**；#55 的性质仍是**行为证据、非书面签认**（IATF 下两者不等价），本次不改变该定性。

### ⚠️ 两条如实标注的边界（勿在任何对外材料中放大）

1. **她的核对是抽样**（一个料号 ＋ 一张成品卡片），**不是全量复核**。#296／#297 期望产出里她三次要求的「整体复核一遍」由 CC 交付材料（107 张真实订单／5819 行缺料清单）承担，**她本次并未逐项核对该材料** ⇒ **不得表述为「她已确认全量口径一致」**。
2. `F02N.0242` 当时已不在活跃订单，复现改用 `F02N.0254` 同组物料（替代料现货 254 与她截图一致），信中已如实说明，**她未就此单独作答**。

### 需 Shao Peishen 项 → **并入既有 §四 #52，未新建**

**为什么不新建**：§四 #52 就是「下一封给姚祖怡的信怎么组织」这一项本身，且仍处开放态；再开一行会造成同题两处。已直接把它重出为选择题（原文已写入该行）：

> **下一封给姚祖怡的信要不要为 #266 单独发？**
> **(a)** 现在就起草一封只问 #266 的短信 —— 她刚回件、上下文热；代价＝为一个 P3 非阻塞问题单独占用她一次注意力。
> **(b)** 不单发，#266 继续挂起、攒到下次有实质交付时随信问 —— 代价＝#266 继续等（已知可接受）。
> **(c)** 由业务总线直接判她不依赖并销 #266 —— **该行已明确否掉**（无依据，此处仅列全）。
> **默认项：不答按 (b) 执行。** 【总线答】

**关键变化**：#296／#297 已验收、§四 #55 已闭合 ⇒ **决策点候选由 4 项收敛为 1 项**，一信 ≤3 上限不再构成取舍压力。**串行闸已对姚祖怡解除**（采购部#12 已闭环）。

---

## 三、派生登记（三条，均非拆件本身，而是拆件过程中撞见的真实缺口）

### #321 —— README 跟进信登记表缺一整行：已发出的「采购部#11」没有登记行（P2，CC 跟进信机制）

**怎么发现的**：回填 采购部#12 状态时被编辑锁的**跟进信串行闸真实拦下一次** —— 它报「前一封（采购部#10）尚未闭环」。查证后发现**问题不在 #10，而在 #11 根本不在表里**：README 主表 39 行内 grep 其主题「答交口径v3已复现」**零命中**，而该信真实存在（md/docx 均在同目录）且已发出（§一 #292 明写「姚祖怡对采购部#11 两项均回件」）。

**两项后果**：⑴ 串行闸回查「前一封」时锚到 采购部#10（2026-08-03）而非 #11，**判据输入本身就是错的**；⑵ README 是「发一封追加一行」的唯一登记载体，**缺行即该信在机制视野里从未发生**。

**为什么不由本巡逻代填**：真实发送时刻须取服务侧 `wecom_aibot_audit.jsonl` 真身，Cowork 读不到；**不猜、不估**。

### #322 —— 编辑锁 mutex 在「删不掉」时无限循环，而 Cowork 沙箱必然删不掉（P1，CC，须走 openspec）

**这是本次巡逻最重要的发现，且它此前一直是静默的。** 三段实测：

1. **工具缺陷**：`工具-共享文档编辑锁.py::_acquire_mutex` 判定 mutex 陈旧（>10s）后调 `unlink()`，若抛 `OSError` 是 `pass` 掉再 `continue` —— **该 `continue` 跳过了 deadline 检查**，于是 `MUTEX_WAIT_TIMEOUT_SECONDS=5` 形同虚设。**表现不是超时报错，是无限循环、零输出。**
2. **环境事实**：Cowork 沙箱对挂载目录**没有删除权限** —— 实测连自己刚创建的临时文件都删不掉（`PermissionError [Errno 1] Operation not permitted`），但 `rename` 可用。故 `finally: unlink` 每次静默失败。
3. **⇒ 组合后果**：**Cowork 每成功占一次锁，就给该文件埋一枚永久 mutex；下一个 Cowork session 对同一文件的 acquire 直接挂死。**

**实证（开工时的真实现场）**：5 枚遗留 mutex —— `session接力-财务域场景落地`／`session接力-采购域场景落地`（均 2026-08-07，**已卡 3 天**）、`session接力-Phase1收口`（2026-08-09）、`跨桌任务队列`／README（本次巡逻自己留下）。本次已用 `os.rename` 逐一挪走恢复可用，**这是绕过工具的人工急救、不是修复**。本轮内该现象**又复现两次**（每次 acquire 之后必留一枚，第二次 acquire 直接 124 超时），已即时挪走。

**Windows 侧不受影响**（能删，10s 后自动被下一次调用接管），**这正是它此前无人发现的原因** —— 同属根 CLAUDE.md §5 点名的「工具静默回退」家族：外观是卡住不动，不报任何错。

**已按 #285 在行内附证伪命令两条**（任一条给出相反结果即本行定级不成立）。

### §四 #58 —— 机制类可动 WIP 超上限（20／16），交 Shao Peishen

**本巡逻不自行处置**（关行取舍属定夺、不属拆件，且 §〇 红线明确）。**不是本次新增造成的** —— 新登 #321／#322 之前已是 18／16。三选一（(a) 关 4 条／(b) 上调阈值／(c) 容忍并保留提示，**默认 (c)**，建议 (a)）原文已写入 §四 #58。

---

## 四、一处未动、只报告的观察

**README 主表 采购部#12 行的「编号」列仍写着「采购部#12（待发，暂不占号）」，而同一行状态列已是「已推送 2026-08-07 03:24 UTC」** —— 该括注在信发出之后即已过期。

**本次刻意未改**，原因值得记：改动「编号」列会改变编辑锁 `_followup_row_identity` 计算出的行身份，使这一行被判为「本次新增行」，从而触发跟进信串行闸（实测已发生一次，release 被拒）。**要么绕（写「串行豁免」——但这不是新信，那样写是假的），要么不改** —— 选择不改，把它连同 #321 一并交给补齐登记行的那次作业顺带校准。

---

## 五、巡逻自检

- **预案覆盖不了的情形**：有一处，即上文 #322 —— 巡逻 prompt 三.1 给的降级预案是「acquire 返回非 0 即降级」，**但本次失败形态是 acquire 既不返回也不报错、直接挂死**，预案的判据（查退出码）在这种形态下不成立。处置：先用 `os.rename` 恢复通道，再按正常协议全程占锁作业，全部动作均在锁内完成，**未发生任何绕锁裸改**。
- **三.6 重读比对结果**：每次写回前后均重读队列真身比对 —— **本轮持锁窗口内未出现他人新增行**（§一 行数 119→121 全部为本次新增的 #321／#322，§二 新增 1 行为本次批次行，§四 新增 1 行为 #58）；无撞号。顶部高水位线由工具同步回写为 **§一 #322 ｜ §四 #58**，与文内真实最大号逐一核对一致。
- **本轮实际拿到的编号**：`--reserve 2 --section 一 --domain 机` → **§一 #321、#322**；`--reserve 1 --section 四` → **§四 #58**。均由工具原子分配，未自行做加法。
- **三.2 并入审核结论**：**已逐条过完当时 §一 全部 4 条待领行**（#82 FI2 round-2 OCR ／ #98 月度环境体检 ／ #312 可 Open 池事件驱动提醒 ／ #315 业务·环境双队列拆分），**无一条可承接**：#321 是跟进信登记数据缺失、#322 是编辑锁工具缺陷，与四行主题均不同源；#98 虽覆盖结构性环境面，但它是每月触点、不是修复载体，把一条正在卡人的 P1 塞进月度巡检等于延后一个月。另核 **#258**（README↔队列一致性校验，在办待批）—— 它是**守卫机制**，#321 是**数据缺行**，不同层，故未并入，仅在 #321 期望产出里点名「可并入其范围评估」。**需 Shao Peishen 项则相反：并入了既有 §四 #52，未新建**（同题不两处）。
- **一.4 缺口检测**：**已跑**（`draft_gap_check.py --window-days 14`，输出「无缺口」，已原样并入本报告顶部）。
- **三.8 写后反查**：**已做，但用的是沙箱等价手段、不是 `工具-写后反查.ps1`**（本班为定时任务触发的非交互班次，无本机 PowerShell 通道）。实际执行：每次写入后重读文件真身并 grep 关键字校验 —— 队列五行销行与四处回灌逐行复读确认、`工具-队列结构lint.py` 三次全绿、README 行改写后重读末尾确认、采购域接力新节 grep 命中 1 处且 `## ` 标题序确认插在最前。
- **sweep 触发**：**未触发**（非交互班次无本机 PowerShell，属既定执行环境边界，不计入错写）；批次已规范登记 §二 `B-0810_拆件巡逻五行回灌`，由 `ZhuopinCommitSweep` 周期（1 小时，#189）自然取活。
- **决策提醒触发**：**未触发**。本轮确有新登 §四 项（#58）与新增 P1 待领行（#322），按 #172 本应跑一次 `decision_reminder_check.py`，但该脚本需出网（Cowork 沙箱实测 qyapi 出网受限），**且失败的半程调用有可能写入去重状态、反而抑制后续提醒**，故**刻意不试**。兜底为 #219 的双载体（`ZhuopinDecisionReminderDaily` 每日 08:30 ＋ sweep 起跑段每小时）。
- **⚠️ 本次留下一个必须由 Windows 侧清理的文件**：`1-转型规划/0-全景路线图/__cowork沙箱遗留-待CC删除.tmp` —— 它是 6 枚遗留 mutex 与 1 个探测临时文件被 `rename` 逐次覆盖后收敛成的**单个 0 字节垃圾文件**（沙箱只能挪、不能删）。**请勿提交入库**，已写进 #322 触碰区。
- **另一处环境观察（未立行，仅记）**：本次两轮 acquire 的近场者回显里，`企微机器人` 在同一分钟内以「补录 git 同步失败追加」身份连续占锁 **4 次** ＋「归档追加」1 次。未见其它异常，队列内容也无损，故不据此下判断；若后续 `sweep-commit.log` 出现同期失败，可回看这一时点。

---

## 六、live 首周逐日抽查

**live 首周逐日抽查中，出现一次错写即退回 dry-run。**
