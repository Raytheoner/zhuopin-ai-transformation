---
title: "看护件 · B-0916_584续二与585（两泳道并行）"
created: 2026-09-16
批次: B-0916_584续二与585
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 4a／5a）
---

# 看护件 · B-0916_584续二与585

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#584` | 续派二：SessionStart timeout 10→30＋worktree 报告回收＋插件加载探针 | sessionstart hook、settings.json（仅 timeout）、v2.ps1、包版本探针 |
| A2 | `#585` | skill 载体归置＋description 压缩 | skills源码、.claude/skills、仓库外载体扫描 |

触碰区零交集。

## 二、泳道

### A1 · §一 #584 续派二

▶ 粘贴端：CC ｜ 泳道：op0916j-headless-parity-584c

```
[OP-0916-J]【CC】无头开场注入续派二
【设置】执行环境：CC ｜ 分支：既有分支 `claude/op0916i-headless-parity-584b` 续做，不新建 ｜ worktree：☑（op0916j-headless-parity-584c，新 worktree 检出该既有分支，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_584续二与585
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916J-开场注入续二。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #584（`python 0-学习与工具/工具-队列查询.py --row 584 --section 一 --field all`，以状态列末段最新口径为准）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 先改 timeout 并实测冷启动（清掉 fsck 缓存后）一次送达，再做 ⑶b、⑷；v2.ps1 改动须保留 `#581` 模型字段逻辑，差集回归与 master 对跑。
🔴 一切 `reports/` 产出写**主工作区**（`git rev-parse --git-common-dir` 的上级目录）。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后的 git 写操作先断言 `git rev-parse --show-toplevel`。
🔴 收到上下文计量提醒即按提醒收尾交接；重输出步骤优先交子代理。测试一律前台跑、`pytest -q --tb=short`，跑完即 commit，严禁放后台等通知。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916j-headless-parity-584c --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916j-headless-parity-584c --done --batch B-0916_584续二与585 --text "产出落点：<落点>"`。
🔴 收工只 push 本泳道分支，不 ff master；在 `合入登记/pending-ff.jsonl` 登记待授权合入行；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

### A2 · §一 #585 skill 载体归置与描述压缩

▶ 粘贴端：CC ｜ 泳道：op0916k-skill-home-585

```
[OP-0916-K]【CC】skill归置描述压缩
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0916k-skill-home-585`） ｜ worktree：☑（op0916k-skill-home-585，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_584续二与585
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916K-skill归置。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #585（`python 0-学习与工具/工具-队列查询.py --row 585 --section 一 --field all`，以状态列末段最新口径为准）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 description 压缩必须附「压缩前后触发词对照表」写回 #585，一个触发词都不得丢；`git mv` 迁移保留历史。
🔴 一切 `reports/` 产出写**主工作区**（`git rev-parse --git-common-dir` 的上级目录）。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后的 git 写操作先断言 `git rev-parse --show-toplevel`。
🔴 收到上下文计量提醒即按提醒收尾交接；重输出步骤优先交子代理。测试一律前台跑、`pytest -q --tb=short`，跑完即 commit，严禁放后台等通知。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916k-skill-home-585 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916k-skill-home-585 --done --batch B-0916_584续二与585 --text "产出落点：<落点>"`。
🔴 收工只 push 本泳道分支，不 ff master；在 `合入登记/pending-ff.jsonl` 登记待授权合入行；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】584续二与585两泳道-2026-09-16.md" -FullAuto -Yes -Detach
```
