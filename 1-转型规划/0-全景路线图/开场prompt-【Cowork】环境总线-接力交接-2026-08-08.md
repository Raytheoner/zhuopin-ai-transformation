---
title: 环境总线接力交接 —— 双队列设计细化落定 + GitHub 原生能力二批收口
created: 2026-08-08
updated: 2026-08-08
执行方: Cowork 环境总线
来源: 2026-08-08 环境总线两轮成果（协议〇.9／〇.10 入法、#305／#308／#309 三行、构建环境基建方案定稿并完成 A/B/C 三段细化）
status: 待执行
---

# 开场prompt ·【Cowork】环境总线 · 接力交接（2026-08-08 第 2 轮收工版）

## 用法

**A 类同线转场**（环境总线接环境总线）。下方开场词复制即用。
本 session **先按 §〇 自检三查，三查全通过即直接开工、不必提问**；任一不通过才就差异发问。

### 开场词（复制即用）

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ ｜ 工作区：无（只改 .md 与 Cowork artifact，不写生产代码、不自行 commit，收工只登记 §二 批次）

读 1-转型规划/0-全景路线图/开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md ＋ 根 CLAUDE.md 当前进度恢复上下文，按文件执行；先按文件 §〇 自检三查，通过即直接开工、有差异再问我。
```

---

## §〇 开工自检三查（A 类硬规则，先做这一步）

1. **接力时效**：本文件日期节是否＝上次收工日？若队列里已出现更晚的批次行，说明本文件已过时，**以队列为准**。
2. **在办清单抽核**：抽 §三 任意 2 行，核对与队列**实际状态列**是否一致；**不一致一律以队列为准**（2026-07-31 实证：接力在办清单 24 小时内即全面过时，本项目推进速度下这是常态）。
   🔑 **读状态列必须读开头**（B-0805 教训：取行尾片段会读到已作废的历史原文且不报错）。
3. **残留待补**：grep 四条 session 接力文件顶部有无「⏳ 队列更新待补」残留（`1-转型规划/session接力-{财务,质量,采购}域场景落地.md` ＋ `1-转型规划/0-全景路线图/session接力-Phase1收口.md`）。
   ⚠️ **已知非残留、可直接跳过**：采购域 3 处命中全为**引用叙述**（非本文件小节）；Phase1收口 1 处命中已标「✅ 已回补完毕，读到本行即可跳过」。**下次仍会命中 4 处，属正常。**

---

## §一 背景一分钟

- 2026-08-08 环境总线两轮完成：**协议〇.9（守卫 one-in-one-out ＋ 机制类可动 WIP 上限 8）** 与 **协议〇.10（建行前必须先做并入审核）** 入法；立 **#305／#308／#309** 三行并放行并行；**#220 改永久关闭·仅手动唤醒**；措施 C 分母由「未收口总数」修订为「**可动 WIP**」；**构建环境基建方案完成 A/B/C 三段细化**（见 §二 依据 2）。
- 根因判断已收敛为一条：**状态语义靠自然语言承载，判据在用正则猜中文**——#248／#302／#304／#306 与 commit scope 第四误报源全是其衍生物，**#308 是对它的源头收敛**。
- 实测底数：§一 机制/环境类 : 业务场景类 ＝ **86 : 20（81%）**；守护队列文件的工具代码 **4822 行**且此前只增不减。
- **`.github` 目录此前根本不存在**——本项目在 GitHub 仓库里自研了 GitHub 原生就有的东西。#309 CI 基线由此立行，**步骤 1 已由 CC 实跑完毕并回写**（1704 通过／40 跳过／0 失败，13 子项目 exit code 全 0）。

---

## §二 权威依据清单（只给指针，不复制正文）

1. `1-转型规划/0-全景路线图/跨桌任务队列.md`
   - **协议〇.9**（措施 B one-in-one-out ／措施 C 可动 WIP 上限 8 及其三类排除）
   - **协议〇.10**（建行前并入审核，门禁挂 `acquire --reserve`）
   - **§一 #308**（状态机器字段；子项 A–E）／**#309**（CI 基线三步 ＋ 子项 F 已完工）／**#305**（写侧注入口止血）
   - **#306 已缩范围降 P3**／**#307 已并入 #306**／**#220 永久关闭·仅手动唤醒**
2. 🔑 `3-治理与合规/构建环境基建方案-CI基线与业务环境双队列-2026-08-08.md`（**本线主产出，status 已改「生效」**）
   - **§2 ＋ §2.1** GitHub 原生能力对照（第一批 8 项 ＋ **第二批 8 项，2026-08-08 新增**）
   - **§3.2 步骤 2 追加设计输入**（CI 白名单三种目录形态 ＋ 三道计数护栏，**2026-08-08 新增**）
   - **§4.4 双队列设计细化四问**（**2026-08-08 新增，本轮主体**）＋ **§4.4.5 两处待确认修正**
   - **§5 次序与并行矩阵**、**§6 未验证项（现 6 项）**、**§6.5／§6.6 外部评审 triage**
3. 根 `CLAUDE.md` §5（工作流、决策路由、规则退休制、多任务须指明次序与并行）

---

## §三 当前在办（以队列实际状态列为准，本清单仅供抽核）

| 行 | 状态 | 归属 |
|---|---|---|
| #309 | 待领（P1）；**子项 F 已完工**；**步骤 1 已实跑完毕并回写**；步骤 2-3 未开工 | CC |
| #308 | 待领（P1），须 propose＋design 审 | CC |
| #305 | 待领（P1） | CC |
| #306 | 🔽 P3 缩范围，硬阻塞于 #308 | CC |
| #220 | 🛑 永久关闭·仅手动唤醒 | —— |

**可动 WIP 当前 3／8**（排除定时触发型／依赖外部方／永久关闭三类）。

---

## §四 本 session 任务分段

> **次序与并行矩阵**（根 CLAUDE.md §5 硬规则）
>
> | 段 | 能否立即开工 | 可与谁并行 | 判据 |
> |---|---|---|---|
> | ~~**A** 待确认修正回填~~ | ⏸ **已出本线范围**（2026-08-08 拍板缓、并进 #308 design 裁定） | —— | 本线不再持有该项 |
> | ~~**B** #308 design 输入打包~~ | ✅ **已完成（0808 第 3 轮）** | —— | 产出＝方案件新增 **§4.5** 成稿段，CC 可原样复制 |
> | ~~**C** 值周巡检口径同步~~ | ✅ **已完成（0808 第 3 轮）** | —— | 见下方 C 段 |
> | ~~**D** 协议↔载体承载性专项核查~~ | ✅ **第一批已完成（0808 第 3 轮）** | —— | 产出＝`3-治理与合规/协议与载体承载性核查-2026-08-08.md`；三个拍板项已登 **§四 #56** |
> | ~~**E** 承载性核查第二批~~ | ⚠️ **本段已改称 F 段，见下** | —— | **E 撞名，见下方说明** |
> | **F** 🆕 承载性核查第二批 | ✅ **零依赖可立即开工**（§四 #56 拍板项 3 已答 (a)） | 无并行对象（本 session 唯一段） | 只读协议与载体，产出新一批核查表 |
>
> **⇒ A–D 四段全部收口，§四 #56 三项拍板亦已全部执行完毕。下一 session 唯一主攻＝F 段，零阻塞。**
>
> 🔴 **一处命名冲突已修正（2026-08-08，Shao Peishen 追问「E1 已全部完成？」时暴露）**：交付给他的 opener 编号是 **E1**，而本文件此前把「承载性核查第二批」也编成 **E 段**——**同一个字母承载两个含义**，正是《专线opener模板库》§〇.2「已有专属含义的词不得表达第二种含义」禁止的形态。**本段此后一律称 F 段**；opener 编号 E1 保持不变（已交付、不追改）。

### ~~A · 回填 §4.4.5 两处待确认修正~~ ⏸ **已出本线范围（2026-08-08 拍板）**

Shao Peishen 2026-08-08 按默认项拍板：**两项修正均缓，并进 #308 的 design 审一起裁定**，不单独占一次决策。
**⇒ 本线不再持有该项**——§4.1／§4.3 原文保持不动是**正确状态、非遗漏**；裁定与回填由 #308 的 design 承接。
**本段保留为删除线仅作留痕**，下一轮接力瘦身时可整段删。
🔑 **但 B 段因此多一项交付**：§4.4.5 那两项必须**显式写进 B 段产出的 design 输入里作为待裁定项**——否则「缓到 design 审」会变成「没人再提起」（同 #202／#250 那类「暂停静默变搁置」的形态）。

### ~~B · 把 §4.4 打包成可直接粘进 #308 design 的一节~~ ✅ **已完成（2026-08-08 第 3 轮）**

产出＝方案件**新增 §4.5**（含「复制起点／复制终点」标记，CC 可原样复制）。**不是机械平移**——把四问**收敛为 #308 真正要拍的那一个决策**：「状态字段是否同批增设『域』字段与『受外部阻塞』态」，因为**双队列只是该字段的下游消费者、不是 #308 的主题**。
🔑 **顺带定死一条体例判断（值得记）**：两项待裁定项**必须写进 design 的 `## Decisions` 带推荐与默认项，不得放 `## Open Questions`** ——对照既有体例（`openspec/changes/archive/2026-08-06-sweep-ff-sync-batch-reorder/design.md`），其 `Open Questions` 是「（无——所有需要现在拍板的问题均已在 Decisions 中给出结论）」。**本项目惯例：Decisions 承载待审结论，Open Questions 只留「需真实数据才能定」的。放错位置＝没人会拍板＝静默作废。**

### ~~B（原文）· 把 §4.4 打包成可直接粘进 #308 design 的一节~~（P2）

定稿件 §4.4 当前是**方案件的一节**，措辞面向「本件读者」。CC 拿去写 design 时须再翻译一次。
**本段做**：产出一份**措辞已面向 design 文档**的版本（明确「本 design 决定什么／不决定什么」、四问各自的决策点与默认项），落定稿件 **§4.5** 或独立小文件（**建议前者，不新建文件**）。
**验收**：CC 可原样复制、无需改写主语与指代。

### ~~C · 值周巡检《本周计划》口径同步~~ ✅ **已完成（2026-08-08 第 3 轮）**

Shao Peishen 当日拍板默认 (a)「直接做掉」，已 `update_scheduled_task` 全量替换 `weekly-status-update` 的 prompt（52 → 60 行，frontmatter 完好、原有 8 处锚点一处未丢，已实测核验）。
**⇒ 协议〇.9 措施 C 三处落点至此落两处**（⑴看板 ✅／⑶本项 ✅／⑵编辑锁 `release` 提示随 #308）。

🔴 **本项真正的产出不是那个计数，是撞出的一个形态**：**「协议已入法、但承载它的 prompt 从未同步」——同一批查出三处**：
① 措施 C 的 WIP 计数（本项要补的）；
② **「≥2 个 opener 须附次序与并行矩阵」**（2026-08-07 定、根 CLAUDE.md §5 已载）**prompt 里完全没有**，而值周巡检正是该场景最典型、每周都在发生；
③ **协议〇.10 并入审核**（协议正文明写「值周巡检适用且比人工 session 更容易」）**同样没有**。
**⇒ 下一轮值得做一次专项：把协议〇 与 CLAUDE.md §5 的条目，逐条核一遍「哪些定时任务／opener 模板应当承载它、是否真的承载了」。** 这与 #227① 的仓库外载体复检是同族，但那条查的是「参数是否过时」，本项查的是「规则是否根本没落到载体上」——**后者更隐蔽，因为它不表现为错误，只表现为什么都没发生。**

**留位未做（须等 #308）**：域字段落地后按域分列统计——已在新 prompt 内写明「#308 若同时落地域字段，本项一并改为按域分列」，无需下一轮再记。

### ~~D · 协议↔载体承载性专项核查~~ ✅ **第一批已完成（2026-08-08 第 3 轮）**

产出＝`3-治理与合规/协议与载体承载性核查-2026-08-08.md`；三个拍板项已登 **§四 #56**。
**已核 3 个载体、命中 6 处缺口**（值周巡检 prompt 3 处已补／拆件巡逻 prompt 1 处／opener 模板库 2 处），**均未擅自改载体**（守「只判定不改」纪律，唯一例外是 C 段那三处已获当日授权）。
**并入审核已做（协议〇.10）**：#284／#285 **均不可并入**，理由已写进 §四 #56；故只取 §四 一号，**未在 §一 新建任务行**。

🔴 **本段最重要的产出是一个形态，不是那 6 处缺口**：**规则写进协议／§5，却没有任何载体执行它——它不表现为错误，只表现为什么都没发生**，故任何告警、sweep 检查、外部评审都捕捉不到。
**最严重的实例**：opener 模板库四组关键词 **grep 全部 0 命中、最新条目停在 2026-08-07**，而它是「opener 该怎么写」的**规范本身**——「≥2 opener 须附次序矩阵」正是关于 opener 怎么写的规则，**规则最该在的地方没有它**。
**两条方法论已写进核查件，下批直接复用**：⑴ 必须区分「不适用」与「缺口」（拆件巡逻不产出 opener，次序矩阵对它不适用，写成缺口是机械套用）；⑵ 怀疑要实测，**实测推翻怀疑同样是有效产出**（本批推翻了「`append-row` 不存在」这一疑，并留痕免下次重查）。

### §四 #56 三项拍板 —— ✅ **全部已执行（2026-08-08 第 3 轮，Shao Peishen 全按默认项 (a)）**

| 项 | 结论 | 执行结果 |
|---|---|---|
| 1 · opener 模板库补三条规则 | (a) 补 | ✅ **新增 §〇.8／§〇.9**（212→254 行）。六组关键词由**全部 0 命中**变为 2／2／4／3／…／1；原 §〇.0–〇.7 八节标题完好 |
| 2 · 拆件巡逻 prompt 补协议〇.10 并入审核 | (a) 补 | ✅ **新增三.2**（91→103 行）。frontmatter 完好、新增 8 处锚点全中、**原有 12 处锚点一处未丢** |
| 3 · 第二批先核什么 | (a) `CLAUDE.md` §5 的 29 条 | ✅ 已落档为下方 **F 段** |

**两节都写明了自己被补入的成因**（＝承载性核查实测 0 命中的直接产物），**留痕以免日后被当作凭空新增的规则**。
**拆件巡逻 prompt 同批顺带补入三项 08-08 实测口径**：#268 冲突警告在「✅ 开头＋保留历史原文」上常态触发属可接受噪声、勿放宽／沙箱 `git status` 因 `core.autocrlf` 虚报须本机直读／回填已 ✅ 批次行会使改动成孤儿。

🔴 **一处描述性瑕疵已登记、待下次同车修**：拆件巡逻新三.2 内写「在下方**三.3 追加新待领行**之前执行」，而插入新节后**三.3 实为「取号」、三.4 才是「追加新待领行」**（其余四处交叉引用经逐一核对均正确）。**判定为描述性瑕疵而非行为缺陷**——按它执行仍会在取号前做并入审核，而协议〇.10 原文正是「取号前」，结果正确；**为一句话再全量替换 103 行，风险与成本高于收益**，故待 F 段碰该 prompt 时一并修。

### F · 承载性核查第二批 —— 🔴 **开工第一步即撞勘误，原计划作废（2026-08-08）**

**F 段原定「做 `CLAUDE.md` §5 的 29 条逐条判定」，实测该工作已于 2026-08-05 完整做过且已拍板** ——
产出＝`1-转型规划/0-全景路线图/规则体检-CLAUDE.md-§5-29条判定表-2026-08-05.md`（160 行，A–F 六类、量化到「可减约 4,200 字且不损失任何约束力」；第 1-5 组拍 (b) 另排／第 6 组拍 (b) 不立行／第 7 组拍 (a) 已立 #259）。

🔴 **根因，必须记**：核查件对三个载体逐一 grep 实测，**却对「这件事是否已做」只凭队列 #253 行内描述下了断言**——**而 #253 状态列开头写着「✅ 全部完成（C1-C4 ＋ 方向 4 规则体检，2026-08-05）」**。**同一 session 当天正把「判一行状态一律读状态列开头」写进值周巡检 prompt 与本文件 §〇 自检段，自己却没读。规则在传播，自己没执行。**
**代价**：§四 #56 拍板项 3 白耗他一次决策（已作废，由核查件 §5 新增的拍板项 4 承接）。
**⇒ 方法已扩写**：不只对「载体有没有」实测，**对「这件事是否已做」同样要实测，且读状态列开头、不读行内叙述**。

### F' · 勘误挖出的两个真实动作（触发条件现已满足，详见核查件 §4.1）

**判定表自己写明了两条唤醒条款，现在正是唤醒时刻**：

1. **§5 增量判定（不重做 29 条）** —— 判定表基于 2026-08-05 的 §5 快照（**47 行**），**实测 §5 现为 49 行、顶层条目 21→22**，新增的正是 2026-08-07 的「≥2 任务须指明次序与并行」。**只判这 1 条**（三问 ＋ 承载性两问）。不做则判定表从此与 §5 不同步。
2. **重新提请 `VP_APPROVERS` 加孙涛** —— 判定表第 6 组自订「下次唤醒本表或做月度环境体检（#98）时应重新提请」。**实测全库仅 `4-数字员工/采购部/SC8-客户订单交期智能承诺/sc8/config.py` 一处代码引用点、孙涛不在其中**；待办自 2026-07-05 挂起**已逾一个月零三天**，判定表当时预言「大概率仍不会被任何巡检／审计／决策提醒扫到」——**已应验**。

🔑 **判定表末尾还有一句预言同样应验，原文照录**：

> 「**若长期未拍板**：本表本身就会变成「发现 2」那种东西——**内容在，但没有机制会再看它一眼**。」

**它已经拍板了，但「没有机制会再看它一眼」依然成立**——本次核查全程不知其存在，**直到一次 `git grep` 偶然撞上**。
⇒ **这是承载性核查要找的形态的又一实例，只是对象不是规则、是产出物**：不是「规则没落到载体」，而是「**结论做完了、也拍板了，却没有任何载体承接它继续往下走**」。**建议后续核查一并覆盖「已拍板产出物的承接」这一维度**（已写进核查件 §4.1）。

### G · 承载性核查后续批（视核查件 §5 拍板项 4 而定）

**未核清单见核查件 §4（现存 5 类）**，逐批销掉。若拍 (a) 则先做上面 F' 两项、再转接力文件与三份 `zhuopin-*` skill（后者须**源码与已安装版两份都核**，#45 已实证「源码改了、已安装版未重打包＝对实际运行的 skill 完全无效」）。
⚠️ **顺带修掉那处描述性瑕疵**（拆件巡逻 prompt 三.2 的「三.3」应为「三.3 取号与三.4 追加新待领行」）——**同一次全量替换里做掉，不为它单开一次**。

---

## §五 决策项现状 —— ✅ **本轮三项已全部拍板（2026-08-08，全部按默认项）**

| # | 决策项 | 结论 | 落点 |
|---|---|---|---|
| 1 | §4.4.5 两处修正何时定 | **(a) 缓，并进 #308 design 审一起裁定** | 方案件 §4.4.5 表首注 ＋ §6 第 5 项；本线不再持有，见 §四 A 段 |
| 2 | QD-A `pyproject.toml` build-backend 配置错误（CC 任务卡片） | **(a) 并进 #309 步骤 3 顺带项，不新立行** | 队列 #309 行 |
| 3 | sweep `git add` 越界缺口（本轮真实事故） | **(a) 并进 #305** | 队列 #305 行 |

**⇒ 本线当前无悬置决策项。** 下一 session 若产生新决策项，按根 CLAUDE.md §5 写成带默认项的选择题、标具体 session 名。

🔑 **第 1 项须警惕的形态**：「缓到 #308 design」若没人在 design 里提起，就等于静默作废——**故 §四 B 段已把「显式列为待裁定项」写成硬交付**（同 #202／#250 的防范）。

---

## §六 纪律段（引用条目名，不复制正文）

- 根 CLAUDE.md §5：排期同步 / 完工即归档 / §0.2 登记 / 直读原则 / 工具静默回退 / 规则退休制 / **多任务须指明次序与并行** / 决策路由须标具体 session 名 / **日期一律取本机本地日期，不得写尚未到达的日期**
- **跨桌任务队列**：开工必读（认领 + 登记触碰区）、收工必写（状态 + 产出路径 + 新任务当场追加）
- 🔴 **编辑锁**：改队列前 `acquire`、改完立刻 `release`；占用中把内容写进自己域接力文件顶部「⏳ 队列更新待补」等回补，**不硬写**。**`acquire` 与写盘必须两步、中间查 `$LASTEXITCODE`**——「工具拒绝了」与「动作没有发生」是两件事。
- 🔴 **取号**：新行一律 `--reserve` 预留，不自己做加法；跨分区用 `--reserve-multi 一:N 四:M`。**协议〇.10：取号前必须先做并入审核**，追加新号已由默认动作降为例外、须给出理由。
- 🔴 **三条静默失败写法禁令**：⑴ 批次文件清单写完整路径，不用「同上」；⑵ 队列文本内**不得出现裸竖线**（改全角 ／）；⑶ 脚本改 md 时串内不得嵌直双引号或裸反引号。**三者失败形态都是「外观同成功」，只能靠写后反查发现。**
  💡 **2026-08-08 补一条实操**：连**转义**竖线（反斜杠加竖线）也不要写进表格单元格——本项目解析器是朴素按分隔符切列、**不认转义**；本轮已据此改写定稿件 §4.4.1 一行。
- 🔴 **写后反查三件套**（队列 #255 在建，本线自己先守）：`Test-Path` ／ 字节数变化 ／ 关键词命中数——**不信工具说「成功」**。
  🔴 **但反查工具本身也会静默失效——2026-08-08 一轮内撞两次**：⑴ 沙箱 grep 把转义竖线当交替符（BRE 语义）；⑵ PowerShell `Select-String -SimpleMatch -AllMatches` 并用时 `$_.Matches` 为空集，**六组关键词全返回 0**，而文件明明已增 42 行。
  🔑 **抓住它们靠的不是"怀疑工具"，是"这个结果与另一个已知事实矛盾"**（行数增加了却零命中）。**⇒ 交叉验证比重复同一种验证更有效**：反查时至少配两个互不相关的指标（如「行数变化」＋「关键词命中」），单一指标失效时另一个会顶出矛盾。
  **已验证可用的写法**：`(Select-String -Path $f -Pattern $k -SimpleMatch).Count`（**不要加 `-AllMatches`**）。
- 🔴 **状态头纪律**：✅ 必须写在**状态列开头**；**尚有任一子项未完成一律写 🟡、不得写 ✅**。
- 🔴 **协议〇.9 措施 C**：机制类**可动 WIP** 上限 8，超限期间新立机制行须先关一条。
- **仓库外载体复检（协议〇.8 / #227①）**：批次声明「已复检」时须过四类仓库外载体清单，可直接跑 `python 0-学习与工具/工具-仓库外载体扫描.py "<变更参数>"`（keyword 是位置参数）。

---

## §七 收工段（必含 git 处置）

1. 滚动更新本文件（**日期节只留最近 2 个**，R5 接力瘦身）。
2. 更新文档台账状态头。
3. **列出本次全部新产出／修改文件清单 ＋ 建议 commit message ＋ 登记队列 §二 待 commit 批次**。
4. 提示 Shao Peishen 一句话交 CC：**commit + push + 收工重跑台账**。
   （Cowork 不擅自 commit，但必须交代去向，不留未提交悬置。）

---

## §八 日期节（R5：只留最近 2 个）

### 【2026-08-08 · 第 2 轮】A/B/C 三段完成 —— 双队列设计细化 ＋ 原生能力二批 ＋ CI 白名单护栏

**改动集中在一个文件**：`3-治理与合规/构建环境基建方案-CI基线与业务环境双队列-2026-08-08.md`（status 待发 → **生效**）。

- **C 段（承接 CC 步骤 1）**：独立复核**无分歧**（157 个测试文件、10 组同名文件与 collection error 成因吻合）；**盲区不存在、不需独立处置**。
  🔑 **但撞出一条 CI 真实约束**：「逐子项目跑」的那份清单是**隐性手写白名单**，实测**三种目录形态并存**（`X/tests/`／`X/test_*.py` 散在根下 7 个／`X/子目录/test_*.py`），**无单一 glob 可覆盖**。⇒ `ci.yml` 若写死 13 条路径，**新场景忘加则 CI 全绿而该场景零覆盖**——CI 要消灭的「靠人记得」在 CI 自己身上复活。已给两道护栏（自动发现 ＋ **三道计数下界 1704／13／40**），落 §3.2。
- **A 段（双队列四问）**：落 **§4.4**。核心结论——
  ⑴ **不需要发明任何跨队列引用语法**：§4.2 自举三例**全是机制行指向业务出处、无一反向**，真实需求是溯源而非跨命名空间寻址 ⇒ **两个队列 ≠ 两套编号**，维持单一编号只分文件，历史引用零回填、`--reserve` 竞态逻辑不必重来。
  ⑵ **归属判据并入 #308 机器字段**——「域」在建行那刻就已知，**从来不需要猜**；落点复用协议〇.10 已挂在 `acquire --reserve` 的那个咽喉。
  ⑶ **三阶段渐进，前两阶段不分文件**；**明确给出「阶段 3 可以不做」的出口**。
  ⑷ 🔴 **发现 §4.3 漏掉一个硬前置**：实测队列文件路径**5 处 Python 硬编码 ＋ 1 处 JS**，而 #306 降 P3 后**该事项无人认领** ⇒ 建议并入 #306 剩余范围（零新建行）。
- **B 段（原生能力第二批）**：落 **§2.1**，8 项逐条给结论，**只取 Dependabot security alerts**（零 PR、零企微推送、私有仓免费）；`Dependabot version updates` 与 security alerts **拆开判**；CODEOWNERS 不取但**写死重评触发条件**；原生 secret scanning **不改变 #309 步骤 2 第④项必做**。
- **两处修正未静默执行**，列 §4.4.5 待 Shao Peishen 定（见 §五）。

### 【2026-08-08 · 第 1 轮】协议〇.9／〇.10 入法 ＋ 三行立行放行并行

协议〇.9（守卫 one-in-one-out ＋ 可动 WIP 上限 8，分母当日修订）／协议〇.10（建行前并入审核，挂 `acquire --reserve`）入法；立 #305／#308／#309 并经 Shao Peishen 拍板三条并行（触碰区实测零重叠）；#220 改永久关闭·仅手动唤醒；#306 缩范围降 P3、#307 并入 #306、#304 并入 #308（**4 行 → 2 行，净减**）；构建环境基建方案立件并完成两份外部评审 triage。
