---
title: 环境总线接力交接 —— 承载性核查两批收口 + 三份 skill 补齐
created: 2026-08-08
updated: 2026-08-08
执行方: Cowork 环境总线
status: 待执行
---

# 开场prompt ·【Cowork】环境总线 · 接力交接（2026-08-08 收工版）

## 开场词（复制即用）

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ ｜ 工作区：无（只改 .md 与 Cowork artifact，不写生产代码、不自行 commit，收工只登记 §二 批次）

读 1-转型规划/0-全景路线图/开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md ＋ 根 CLAUDE.md 当前进度恢复上下文，先按文件 §〇 自检三查，通过即直接开工、有差异再问我。
```

---

## §〇 开工自检三查（A 类硬规则）

1. **接力时效**：队列里若已出现更晚的批次行，**以队列为准**。
2. **在办清单抽核**：抽 §三 任意 2 行核对队列**实际状态列**，不一致以队列为准。🔑 **读状态列开头**，一律用 `python 0-学习与工具/工具-队列查询.py --row N`（不截断、头尾互斥会打冲突警告）。
3. **残留待补**：grep 四条接力顶部「⏳ 队列更新待补」。⚠️ **已知非残留、直接跳过**：采购域 3 处均为引用叙述、Phase1收口 1 处已标"已回补完毕"。**下次仍会命中 4 处，属正常。**

---

## §一 背景一分钟

- 2026-08-08 环境总线三轮完成：协议〇.9／〇.10 入法；#305／#308／#309 三行并行放行；**构建环境基建方案定稿并完成 A/B/C 三段细化**；**协议↔载体承载性核查两批**（新立件 `3-治理与合规/协议与载体承载性核查-2026-08-08.md`）；**三份 `zhuopin-*` skill 补齐并覆盖安装**。
- 🔑 **本轮最有价值的发现是一个形态**：**规则写进协议／CLAUDE.md §5，却没有任何载体执行它——它不表现为错误，只表现为什么都没发生**，故任何告警、sweep 检查、外部评审都捕捉不到。已实测出 3 处（措施 C 的 WIP 计数／2026-08-07 立的「≥2 opener 须附次序与并行矩阵」／协议〇.10 并入审核），**全部已补入相应载体**。
- **同族第二形态**：**结论做完了、也拍板了，却没有任何载体承接它继续往下走**（08-05 的 §5 判定表已拍板，本轮核查全程不知其存在，直到 `git grep` 偶然撞上）。

---

## §二 权威依据（只给指针）

1. `1-转型规划/0-全景路线图/跨桌任务队列.md` —— 协议〇.9（守卫 one-in-one-out ／可动 WIP 上限 8）／协议〇.10（建行前并入审核，挂 `acquire --reserve`）／§一 #308／#309／#305／#306／§四 #56
2. 🔑 `3-治理与合规/协议与载体承载性核查-2026-08-08.md`（**本线主产出**）—— §1 方法两句问法／§2 第一批 3 载体／§3.5 第二批（skill ＋ 接力）／§4 未核清单（剩 3 类）／§4.2 孙涛项永久关闭／§5 拍板项
3. `3-治理与合规/构建环境基建方案-CI基线与业务环境双队列-2026-08-08.md` —— §2.1 原生能力二批／**§4.5 交 #308 design 的成稿段（CC 可原样复制）**／§4.4.5 两项待 #308 design 裁定
4. `1-转型规划/0-全景路线图/规则体检-CLAUDE.md-§5-29条判定表-2026-08-05.md` —— **已拍板**；末尾「增补」节为本轮 §5 增量判定
5. 根 `CLAUDE.md` §5

---

## §三 当前在办（以队列状态列为准）

| 行 | 状态 | 归属 |
|---|---|---|
| #309 | ✅ 已完成（CI 基线三步骤全部收口） | CC |
| #308 | 待领（P1），须 propose＋design 审 | CC |
| #305 | 待领（P1） | CC |
| #306 | 🔽 P3 缩范围，硬阻塞于 #308 | CC |
| #220 | 🛑 永久关闭·仅手动唤醒 | —— |

---

## §四 下一 session 任务

> **次序与并行**：A 段零依赖、建议先做（消除现存漂移）；B 段零依赖；**两段触碰区零重叠，可并行**。

### A · 回写三份 skill 仓库源码（P1，**消除现存反向漂移**）

🔴 **当前状态：已安装版新、源码旧**（本轮补齐了已安装版，源码因上下文预算不足未回写——**非遗漏**）。
**要做**：把三份已安装版正文回写进 `0-学习与工具/skills源码/<name>/SKILL.md`，并同步 frontmatter 的 `description`（**统一为「卓品智能AI转型项目」**，源码此前少「智能」二字）。
- `zhuopin-kickoff-prompt` **v1.7** —— 新增 1.2 次序与并行矩阵硬规则；取号条补协议〇.10 并入审核与〇.9 可动 WIP；禁止条加一项
- `zhuopin-queue-audit` **v1.7** —— §四 取号条同补两项；§三 新增 🟡 复合态判据；读取纪律新增「判是否已做须实测且读状态列开头」与「反查工具自身会静默失效」
- `zhuopin-rebaseline` **v1.1** —— 新增 §2.0 写后反查；收工段补批次三禁令与「已 ✅ 批次行不可回填」

⚠️ **回写后须核对**：源码 description 与系统提示注入的 description **逐字一致**（这是目前唯一可用的源码↔已安装版比对通道；正文层比不了）。
💡 **顺带评估**：「两向同步」纪律**没有为「单 session 预算不足」设降级路径**——本轮临时办法是「只改一边时当场登记反向漂移并指定回补批次」，评估是否正式补入。

### B · 承载性核查第三批（P2）

**未核清单见核查件 §4，剩 3 类**：① 编辑锁校验清单（协议里还有哪些结构约束本该挂在这个咽喉上而没挂）；② `工具-落库sweep.py` 8 类告警的**反向核**（哪些守卫是代码里有、协议正文没写的——与本核查方向相反，同样造成认知错位）；③ 看板 artifact `zhuopin-project-status`。
**方法直接复用核查件 §1 两句问法**。🔴 **只判定、不改载体**，产出表交 Shao Peishen 拍板。
⚠️ **顺带修一处描述性瑕疵**：拆件巡逻 prompt 三.2 内写「在下方**三.3 追加新待领行**之前执行」，而三.3 实为「取号」、三.4 才是「追加新待领行」（**行为正确、仅措辞不符**，不值得单开一次全量替换，**碰到该 prompt 时同车修**）。

---

## §五 决策项现状

✅ **本轮全部已拍板，无悬置项**。§四 #56 拍板项 1／2 已执行、拍板项 3 已作废（其 (a) 早于 08-05 完成）、拍板项 4 已答 (b)。
🛑 **孙涛 `VP_APPROVERS` 项永久关闭·仅手动唤醒** —— 不提／不删／仅他手动唤醒；三处会使其复活的触发点已全部摘除（核查件 §4.2）。

---

## §六 纪律段（引用条目名）

- 根 CLAUDE.md §5：完工即归档／§0.2 登记／直读原则／**工具静默回退**／规则退休制／**多任务须指明次序与并行**／决策路由须标具体 session 名／**日期取本机本地、不得写未到日期**
- 🔴 **编辑锁**：改队列前 `acquire`、改完立刻 `release`；**`acquire` 与写盘两步、中间查 `$LASTEXITCODE`**。
- 🔴 **取号**：一律 `--reserve`／`--reserve-multi 一:N 四:M`；**取号前先做并入审核（协议〇.10）**。
- 🔴 **三条静默失败写法禁令**：完整路径／不得裸竖线／不得裸反引号——**连三连反引号也会让 `release` 拒绝释放**（fail-loud，修正后重试、不要绕过）。
- 🔴 **写后反查**：`Test-Path`／字节数或行数变化／关键词命中数。**反查工具自身也会静默失效**——2026-08-08 撞两次（沙箱 grep 把转义竖线当交替符；`Select-String -SimpleMatch -AllMatches` 并用时命中恒 0）。**已验证写法**：`(Select-String -Path $f -Pattern $k -SimpleMatch).Count`（**不加 `-AllMatches`**）。🔑 **交叉验证比重复同一验证更有效**——至少配两个互不相关指标，单一失效时另一个会顶出矛盾。
- 🔴 **查主工作区脏文件须本机直读**：沙箱侧 git 因 `core.autocrlf` 差异会**虚报 30+ 个未修改文件为已修改**。
- 🔴 **判"某事是否已做"必须实测、且读状态列开头**（本轮实证：只凭行内叙述断言"未做"，而状态列开头写着"✅ 全部完成"，白耗他一次决策）。
- 🔴 **「回填进已有批次行」前先核那行是否仍为待处理**——已 ✅ 的行不能再承载新改动，会当场成为孤儿脏文件，须新登记一条。
- **批次即扫**：登记后立即 `Start-ScheduledTask -TaskName ZhuopinCommitSweep`（本轮曾漏此步致回填成孤儿）。

---

## §七 收工段

滚动更新本文件（日期节只留 2 个）→ 更新台账状态头 → 列文件清单＋建议 commit message＋登记 §二 批次（**须含队列文件自身、状态列禁带 ✅**）→ 触发 sweep。Cowork 不擅自 commit。

---

## §八 日期节（R5：只留最近 2 个）

### 【2026-08-08 · 第 3 轮】承载性核查两批 ＋ 三份 skill 补齐 ＋ 一次实质性勘误

**新增两份件**：承载性核查件（新建）／构建环境基建方案 §4.5 成稿段。
**已补载体**：值周巡检 prompt（3 处）／拆件巡逻 prompt（协议〇.10 升格审核）／opener 模板库（§〇.8 次序矩阵、§〇.9 并入审核与可动 WIP）／三份 skill（见 §四 A）。
🔴 **一次实质性误判，已勘误**：断言「§5 的 29 条判定＝#253 方向 4 至今未做」，实测**08-05 已完整做过且已拍板**——**根因是对载体做了 grep 实测、却对「这件事是否已做」只凭行内叙述下断言，而 #253 状态列开头就写着「✅ 全部完成」**；同一 session 当天正把「读状态列开头」写进两个载体，自己却没读。代价＝§四 #56 拍板项 3 白耗他一次决策。
🔑 **勘误反而挖出两个真实动作**（判定表自订的唤醒条款）：§5 增量判定（已做，新增第 30 条判为 F 类）／孙涛项（已按他拍板永久关闭）。
**已知未闭合**：三份 skill 源码未回写（§四 A）。

### 【2026-08-08 · 第 1-2 轮】协议〇.9／〇.10 入法 ＋ 三行并行 ＋ 双队列设计细化

协议〇.9（守卫 one-in-one-out ＋ 可动 WIP 上限 8，分母当日修订为「可动」）／协议〇.10（建行前并入审核）入法；#305／#308／#309 三行并行放行；#306 缩范围降 P3、#307 并入 #306、#304 并入 #308（**4 行 → 2 行，净减**）；构建环境基建方案立件、两份外部评审 triage、原生能力二批评估（8 项只取 Dependabot security alerts）、**双队列设计细化四问收敛为 #308 的一个决策点**（不需发明跨队列引用语法／域字段并入机器字段／三阶段渐进且阶段 3 可不做／发现路径解析这一漏掉的硬前置）。
