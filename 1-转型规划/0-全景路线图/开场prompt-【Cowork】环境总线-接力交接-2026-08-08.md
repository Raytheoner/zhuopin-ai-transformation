---
title: 环境总线接力交接 —— 承载性核查第四批 + #313 立行 + 措施C口径失真
created: 2026-08-08
updated: 2026-08-09
执行方: Cowork 环境总线
status: 待执行
---

# 开场prompt ·【Cowork】环境总线 · 接力交接（2026-08-09 收工版）

## 开场词（复制即用）

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ ｜ 工作区：无（只改 .md 与 Cowork artifact，不写生产代码、不自行 commit，收工只登记 §二 批次）

读 1-转型规划/0-全景路线图/开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md ＋ 根 CLAUDE.md 当前进度恢复上下文，先按文件 §〇 自检四查，通过即直接开工、有差异再问我。
```

---

## §〇 开工自检四查（A 类硬规则）

1. **接力时效**：队列里若已出现更晚的批次行，**以队列为准**。
2. **在办清单抽核**：抽 §三 任意 2 行核对队列**实际状态列**，不一致以队列为准。🔑 **读状态列开头**，一律用 `python 0-学习与工具/工具-队列查询.py --row N`（不截断、头尾互斥会打冲突警告）。
3. **残留待补**：grep 四条接力顶部「⏳ 队列更新待补」。⚠️ **已知非残留、直接跳过**：采购域 3 处均为引用叙述、Phase1收口 1 处已标"已回补完毕"。**下次仍会命中 4 处，属正常**（2026-08-08 第 4 轮复测仍为 4，口径稳定）。
4. 🆕 **skill 两向漂移扫（2026-08-08 新增，规则真身见 `专线opener模板库.md` §〇.10）**：把沙箱只读挂载 `.claude/skills/zhuopin-*/SKILL.md` 与仓库源码 `0-学习与工具/skills源码/<同名>/SKILL.md` 逐份 `diff`（跳过各自 frontmatter 后比正文），**有差即说明上一轮只改了一边**。
   **2026-08-08 第 5 轮实测基准：四份全部为 0 差异行**（两向同步首次全部清零）——**下次开工若非 0，即是这之后产生的新欠账**。
   ✅ 该挂载实测 `save_skill --overwrite` 后**即时刷新**，故**也可用作写后反查**（样本 1 次，重要场合叠加「系统提示注入的 description ↔ 源码 frontmatter 逐字比对」这条独立通道交叉验证）。

---

## §一 背景一分钟

- **协议↔载体承载性核查已做三批、累计 9 个载体**，主产出 `3-治理与合规/协议与载体承载性核查-2026-08-08.md`（第一批 §2 ／ 第二批 §3.5 ／ **第三批 §3.6**）。
- 🔑 **贯穿三批的是同一个形态、目前已见三个变种**——共同点是**都不报错，只表现为"什么都没发生"**：
  1. **规则写进协议／§5，却没有任何载体执行它**（第一批 3 处）；
  2. **结论做完了、也拍板了，却没有载体承接它往下走**（08-05 判定表，靠 `git grep` 偶然撞上）；
  3. 🆕 **规则自带的「可机械化判据」指向一个载体里不存在的字段**（跟进信串行原则要查「已回件并回灌」，而 README 状态列没有这个态）。
- 🆕 **第三批还做了一次反向核**（代码里有、协议正文没写）：sweep **11 项守卫只有 1 项写进了协议**；最要紧的是**「编辑锁被占用 ⇒ 整轮零动作跳过、只写日志不告警」**，它与协议〇.8「登记后立即触发 sweep」直接冲突。

---

## §二 权威依据（只给指针）

1. `1-转型规划/0-全景路线图/跨桌任务队列.md` —— 协议〇.9（守卫 one-in-one-out ／可动 WIP 上限 8）／协议〇.10（建行前并入审核）／§一 #308／#309／#305／#306／§四 #56
2. 🔑 `3-治理与合规/协议与载体承载性核查-2026-08-08.md`（**本线主产出**）—— §1 方法两句问法／**§3.6 第三批**／§4 未核清单（销 3 项、新增 2 项）／§4.2 孙涛项永久关闭／**§5.1 拍板项 5-9（全部待答）**
3. `3-治理与合规/构建环境基建方案-CI基线与业务环境双队列-2026-08-08.md` —— §4.5 交 #308 design 的成稿段（CC 可原样复制）／§4.4.5 两项待 #308 design 裁定
4. `1-转型规划/0-全景路线图/规则体检-CLAUDE.md-§5-29条判定表-2026-08-05.md` —— **已拍板**；末尾「增补」节为 §5 增量判定
5. 根 `CLAUDE.md` §5

---

## §三 当前在办（以队列状态列为准）

| 行 | 状态 | 归属 |
|---|---|---|
| #309 | ✅ 已完成（CI 基线三步骤全部收口） | CC |
| #305 | ✅ **已完成 2026-08-09**（写侧注入止血，`ce8b784`） | CC |
| #267 | ✅ **已完成 2026-08-09**（孤儿 worktree 第三桶，f1c9e29） | CC |
| #310 | ✅ **已完成 2026-08-09**（常驻部署债：ff-merge 至 8021034、357 passed+1 skip、服务已重启、两空壳已处理） | CC |
| §四 #57 | ✅ **已完成 2026-08-09**（Dependabot alerts，gh api 返回 204） | CC |
| #308 | ✅ **apply 已完成并推 master（fa522f）**；🔴 **openspec 变更包按纪律暂不 archive**（`tasks.md` 部分验证项如实未勾，含 F2 首次全量运行命中 13 行的待处理项） | CC |
| #311 | ✅ **已完成 2026-08-09**（§5 正文 23,886→20,028 字符 −16.2%，单条最长 3,568→1,299 字 −63.6%，顶层条目 22→30；`CLAUDE.md` 92,885→86,240 字节） | Cowork |
| #285＋#306＋#307 | ✅ **已完成 2026-08-09**（F1 完工，`4311420`／归档 `0f96dcb`；本次开工第 2 查已实测确认 #306 为 `[S:done]`，本表上一版「P3 缩范围待确认」已过时） | CC |
| **#312** | 🆕 **待领（P2）**——「可 Open 池」事件驱动提醒机制（双落点：看板拉取式为主 ＋ 企微 D2 指纹抑制推送为辅） | Cowork＋CC |
| **#313** | 🆕 **待领（P2，2026-08-09 本线立）**——`queue_table` 权威化收尾两项：兜底桩静默降级可见化 ＋ 队列文件路径解析收拢（**A3 两项判定后合为一行**） | CC |
| #220 | 🛑 永久关闭·仅手动唤醒 | —— |

📤 **三份 opener 原文见** `1-转型规划/0-全景路线图/派单件-【CC】环境侧E1E2E3-2026-08-08.md`（含次序与并行矩阵、下一梯队）。**重发时连标题行一起发**（§〇.0）。

---

## §四 下一 session 任务

> **次序与并行**：**A 段已全部执行完毕（2026-08-08 第 5 轮）**，本节仅留 B；**B 零依赖，可立即开工**。
> 🔴 **唯一需与他人错开的点**：拍板项 5 已并入 **#308 子项 F**（改 `工具-共享文档编辑锁.py`），**CC 侧做 #308 时一并做**；Cowork 勿再碰该文件。

### ✅ A · 拍板项 5-9 已全部落实（2026-08-08 第 5 轮，他答「全部按默认执行」）

回填明细见核查件 §5.1 顶部表。**本段已收口，下一 session 无须再做**——只在 #308 派单时提醒 CC 带上子项 F。

### ✅ A2 · 队列 #311 已完成（2026-08-09，F2 session）

**「记忆偏差」整治两步全部收口**。F2 另报两处**判定表自身的勘误**，值得知道：⑴ 判定表把 A1 的机制写作 `_verify_fast_forward`，**该函数已于 #288 重构中整体删除**——照字面写就会亲手制造一条 B1 型失效指针，已改指现役 `_reconcile_with_origin_and_push`；⑵ 判定表把队列纪律段记在 E2 条内，实测在 E1 条内，**按意图执行、未按行号**。
🆕 **接棒的是 #312**（「可 Open 池」提醒机制）——本行的下一环：#311 治「规则被稀释」，#312 治「有活可开却没人提醒」。

### ✅ A3 · 已判定完毕并立行 #313（2026-08-09 第四批 session）

**两项均已实测确认、合为 #313 一行**（同一处代码、触碰区完全重叠，按协议〇.10 并入优先不取两号）：

- **A3①（兜底桩）** —— 4 处 `class queue_table:  # type: ignore[no-redef]` 实测确认（编辑锁 L217／台账 L30／sweep L356／队列查询 L53）。**本行不主张删桩**（其存在有正当理由，是 #306 apply 时被两个真实隔离测试用例逼出的），修法方向＝**让降级可见**（stderr 警告 或 CI lint 断言）。
- **A3②（路径解析收拢）** —— 🔴 **实测确认：只是被提及，零行承接**。队列内 3 处提及（#308 状态列／#306 状态列／`B-0808` 批次行），而 **#306 与 #308 现均为 `[S:done]`，两行各自把它推给对方或推给「另立行」，那一行从未建立**——**就是又一次「另排」**。且实测规模比 #308 design 说的「5 处」更大：**10 处独立仓库根解析 ／ 3 处独立队列路径字面量**，`queue_table.py` 权威模块当前**零承载**路径解析。

### 🆕 A4 · 本次 release 首次撞出：措施 C 的 WIP 实测基准从 3／8 变成 **16／8**（**新线开工第一件事：看 §五 决策项 4，他答了再动**）

**现象**：本次 `release` 打出 `⚠ 机制类可动 WIP 当前 16／8，已超上限`。而承载性核查 §2.1 与队列 #309 行内记的都是「**实测基准 3／8**」。

🔑 **不是 WIP 暴涨，是计数口径第一次变准**。逐项核过：
- **旧口径（#308 机器字段落地前）** 靠关键词启发式猜「机制类未收口」，**把 `partial` 与 `hold` 大量漏掉**；
- **新口径（`_count_mechanism_wip`，编辑锁 L404）** ＝ 域为「机」且状态 ∈ {`open`, `partial`, `hold`}、正文不以 🛑 开头；`blocked`／`timed=`／`done` 结构性排除。
- **实测分布**：机·open **3**／机·partial **9**／机·hold **6**（合 18，减去 🛑 类得 16）。**⇒ 16 是对的，3 是旧口径的低估。**

🔴 **两个后果**：⑴ **上限 8 是在「以为只有 3」的前提下定的** —— 真值 16 意味着**它从落地那天起就一直在超限，而无人知道**；⑵ 值周巡检 prompt D 节固定列出的「实测基准 3／8」**已过时**，下一次周计划会照旧数写。
**⇒ 已提请拍板（§五 决策项 4），本次刻意未立行**——理由：上限值 8 是他定的，**调不调、怎么调是他的决策，不是执行细节**；先立行会把一个待定的口径写成任务。

### 〜 A3 原文（保留，供追溯）

**F1（#285＋#306）已完工并推 master，验收三项全过**（详见 §八 日期节）。**但验收时看到一处新风险，尚未立行，请新线判定后处置**：

🔴 **#306 的权威模块 `zhuopin_platform.shared_tools.queue_table` 在四处消费者里配了「兜底桩」**（`工具-队列查询.py`／`工具-共享文档编辑锁.py`／`工具-落库sweep.py`／`工具-文档台账生成.py`，均以 `class queue_table:  # type: ignore[no-redef]` 形式），用于隔离测试环境下 import 失败时不崩。
**风险**：`try import / except 用桩` 这一结构意味着——**哪天路径变了或包装坏了，四处会各自静默降级回本地实现、零报错**，即**回到 #306 要根治的「7 份独立实现」老路，而且不可见**。这正是根 `CLAUDE.md` §5「工具静默回退」的教科书形态。
**建议处置（交新线判定）**：桩被真正启用时**至少往 stderr 打一行警告**（不阻断），使降级可见；或由 `工具-队列结构lint.py` 在 CI 里断言"权威模块可 import"。**判定后要么立行、要么写明不立的理由**——不得只留在这段文字里。
⚠️ **F1 已如实登记的未做范围**：#308 design 提到的「队列文件路径解析收拢」（5 处 Python 各自硬编码仓库根解析）本次未纳入，**队列内实测有 3 处提及，新线须确认它是"有行承接"还是只是"被提及"**——若只是提及，就是又一次"另排"。
### ✅ B · 承载性核查第四批已完成（2026-08-09），未核清单第 7、8 项已销

产出＝核查件新增 **§3.7**（两项判定）＋ **§5.2**（拍板项 10-12）。三条结论摘要：

1. 🔴 **项①「机器人取号豁免的失效条款」缺口坐实，且是该形态的第四个变种——协议自己指名了载体（值周巡检对账审计），载体却没接**（9 个载体 grep，失效检测 0 命中）。**更要紧的是原判据不可机械化**（「为绕开…而**故意**」＝动机不可观测），已提出形态型代理判据。
2. 🟡 **项② `zhuopin-followup-letter`：1 真分歧 ＋ 2 不完整 ＋ 6 组一致**。分歧＝SKILL 把 `❌ 已作废` 判为「可起草」，而 #308 子项 G 闸只认 `📥 已回件并回灌` 前缀、**会拒绝 release**——照 SKILL 做会撞墙（fail-loud，非静默）。
3. 🔑 **得一条可复用的成因通则**：两次 skill 缺规则**成因完全相同——规则落地时间晚于该 skill 末次更新，必然错过**。⇒ 核 skill 先比两个日期，不必逐条 grep（拍板项 12）。

**未核清单销 2 项后并未清空——新增第 9 项，且它指向本核查自己**：反向核「四批结论是否都有拍板项或队列行承接」。理由＝**那正是本核查记录的第二个变种，它没有理由假设自己豁免**。

### 〜 B 原文（保留，供追溯）

① **机器人侧取号路径**（`queue_appender.py::_next_task_id`）—— 协议〇.10 豁免它，**但同时写了「若发现为绕开并入审核而走机器人通道建任务，本豁免立即失效」，而这个"失效检测"目前没有任何载体**（协议自带的未承载条款，同族第四例）。
② **`zhuopin-followup-letter` 正文其余部分**（判例批改法／三要素／编号规则）是否与现行机制一致——第三批只核了称呼、状态值、三态三项。
**方法直接复用核查件 §1 两句问法。🔴 只判定、不改载体**，产出表交 Shao Peishen 拍板。
⚠️ **顺带修一处描述性瑕疵（仍未做）**：拆件巡逻 prompt 三.2 内写「在下方**三.3 追加新待领行**之前执行」，而三.3 实为「取号」、三.4 才是「追加新待领行」（**行为正确、仅措辞不符**，**碰到该 prompt 时同车修**；第三批为守"只判定不改"未碰该 prompt）。

---

## §五 决策项现状

✅ **本轮 4 项已全部答复并执行完毕（2026-08-09，他答：1→(a)／2→(b)／3→全默认）**：

| # | 事项 | 结论 | 执行状态 |
|---|---|---|---|
| 1 | 决策 1 · 子项 ③ 生产失效怎么修 | **(a) 派 CC 尽快修** | ✅ 派单件 `派单件-【CC】G1-313权威模块可达性-2026-08-09.md` 已备，含开工先复现段 |
| 2 | A4 · 措施 C 上限调不调 | **(b) 提到 16 并冻结口径一周（至 2026-08-16 复评）** | ✅ 协议正文 ＋ 值周巡检 prompt **两份**（含仓库外真身）已改；🔴 **代码常量仍是 8，并入 #313 ⑥ 交 CC**；看板分母待核（#313 ⑦） |
| 3 | 拍板项 10 · 失效检测补在哪 | **(b) CI lint** | ✅ 落档 #313 子项 ①，附 one-in-one-out 提议（退休 queue-audit 类型⑧）**及其未复核边界** |
| 4 | 拍板项 11 · `followup-letter` 三处修 | **(a) 三处一起** | ✅ v3 → **v3.1**，两向同步后四份 skill 差异行全部为 0 |
| 5 | 拍板项 12 · 日期初筛入模板库 | **(a) 写进去** | ✅ `专线opener模板库.md` §〇.10 新增 ⑷ |

🔴 **下一 session 须知的两条遗留**：⑴ **`MECHANISM_WIP_CAP_DEFAULT` 仍是 8** —— 在 #313 ⑥ 落地前，`release` 会继续报「超上限」，**属已知且预期，不要当异常处理**（可临时 `--mechanism-wip-cap 16`）；⑵ **观察期至 2026-08-16** —— **期间不得为让数字好看去改任何行的状态**。

✅ **更早轮次无悬置项**——拍板项 5-9 已于 2026-08-08 全部按默认项执行完毕（他答「全部按默认执行」），回填见核查件 §5.1。
📌 **两条落到人身上的新常规（下次起效，写在这里以免被忘）**：⑴ **回件消化完后须手工把 README 该行改为 `📥 已回件并回灌 <日期>`**，否则串行原则会把下一封无限期挡住；⑵ **只改一边 skill 时须在 §二 批次行登记反向漂移**（`专线opener模板库.md` §〇.10）。
🛑 **孙涛 `VP_APPROVERS` 项永久关闭·仅手动唤醒** —— 不提／不删／仅他手动唤醒（核查件 §4.2）。

---

## §六 纪律段（引用条目名）

- 根 CLAUDE.md §5：完工即归档／§0.2 登记／直读原则／**工具静默回退**／规则退休制／**多任务须指明次序与并行**／决策路由须标具体 session 名／**日期取本机本地、不得写未到日期**
- 🔴 **编辑锁**：改队列前 `acquire`、改完立刻 `release`；**`acquire` 与写盘两步、中间查 `$LASTEXITCODE`**。
- 🔴 **取号**：一律 `--reserve`／`--reserve-multi 一:N 四:M`；**取号前先做并入审核（协议〇.10）**。
- 🔴 **三条静默失败写法禁令**：完整路径／不得裸竖线／不得裸反引号——**连三连反引号也会让 `release` 拒绝释放**（fail-loud，修正后重试、不要绕过）。
- 🔴 **写后反查**：`Test-Path`／字节数或行数变化／关键词命中数。**反查工具自身也会静默失效**。**已验证写法**：`(Select-String -Path $f -Pattern $k -SimpleMatch).Count`（**不加 `-AllMatches`**）。🔑 **交叉验证比重复同一验证更有效**（第 4 轮实例：skill description 同时用「沙箱 diff」与「系统提示注入值逐字比对」两个互不相关通道互证）。
- 🔴 **查主工作区脏文件须本机直读**：沙箱侧 git 因 `core.autocrlf` 差异会**虚报 30+ 个未修改文件为已修改**。
- 🔴 **判"某事是否已做"必须实测、且读状态列开头**。
- 🆕 🔴 **每个 opener 的【设置】行必须写第五字段 `session：新开 / 续用〈名〉`**（Shao Peishen 2026-08-09 定，规则真身见 `专线opener模板库.md` §〇.11）——`worktree` 管**文件空间**、`session` 管**对话上下文**，两者不同维度，`worktree：☐` 不蕴含"续用某个 session"。
- 🔴 **「回填进已有批次行」前先核那行是否仍为待处理**——已 ✅ 的行不能再承载新改动。
- **批次即扫**：登记后立即 `Start-ScheduledTask -TaskName ZhuopinCommitSweep`。🆕 **但须知**：若此刻编辑锁被他人有效占用，**本轮 sweep 零动作跳过且只写日志、不告警**（第三批实测，见核查件 §3.6.2）——触发后顺手看一眼 `reports/sweep-commit.log`，或等下一个整点轮。

---

## §六bis 本线边界（C 类，2026-08-09 由根 `CLAUDE.md` §5 迁入，**此处为全文正本**）

> **为何迁**：这两条只约束环境保障线一条线，却写在两桌全局、每会话必载入的 `CLAUDE.md` 里——其余所有 session 每次开场都要为它付 token（依据：08-05 判定表 C 类）。
> **为何 §5 仍各留一句、而非只留指针**：**采纳判定表自带的反方意见**——别的线需要知道本线边界在哪，才不会把活派错，**C1 的消费者恰恰不是本线**。故 §5 保留「判别两分支」，细则·边界个案·成因落此处。

### C1 · 派单边界（Shao Peishen 2026-08-02 定，原文）

**环境保障线的派单边界（Shao Peishen 2026-08-02 定，细化铁律一）**：其原话——「**这是环境整治，归本线协同 CC 端治理；具体项目构建任务还是尽量交予业务总线**」。**判别**：动作对象是**构建环境与自动化机制本身**（worktree/stash 台面、编辑锁、sweep、巡检巡逻、队列纪律、openspec 工作流、本机工具链、机器人链路）＝**环境整治，环境保障线可备派单件并协同 CC 执行**；动作对象是**业务场景代码、平台底座功能、真实数据与 `.51` 现网服务**（即使入口是一个"测试失败"）＝**项目构建，交业务总线派发**。**边界个案（2026-08-02 实例）**：#165／#166／#197／#207／#125／#101＝环境整治 ✅ 本线；#206（openspec 工作流机制）／#205-B（工具链升级）＝机制/环境 ✅ 本线；**#208（平台底座 `connector.py` 测试失败，且可能改 `get_purchase_orders` 并触发 SC8 `.51:8091` 部署）＝项目构建 → 交业务总线**，本线只保留"已完成的只读 triage 取证"作为交接材料。**理由**：项目构建牵动业务口径、现网服务与部门专员，**那是业务总线的责任面**；环境保障线掌握的是台面与机制证据，不掌握业务影响判断。

### C2 · 以「机制收口」为默认交付形态（Shao Peishen 2026-08-03 定，原文）

**🔴 环境保障线以「机制收口」为默认交付形态，不直接派活（Shao Peishen 2026-08-03 定，本线工作方式）**：其原话——「**尽量使用机制流转，除非非常紧急，我们最好聚焦环境梳理、方案定稿、任务写入，最好不直接派活，用机制收口**」。 **判据**：本线的默认交付物是 **① 环境取证与梳理结论 ② 方案定稿 ③ 队列行写入**；**分发由机制完成**——值周巡检读队列生成《本周计划》、拆件巡逻按池分流，**这两者才是派活载体**。**"备一份派单件 + 请他粘贴开场词"属直接派活，仅在紧急且经他明示授权时才用。** **实证成因（2026-08-03）**：本线当日备了《机制加固五件同车》派单件并请他粘贴；**而同日值周巡检的《本周计划》v2 早已把该派单件吸收进 A 段、并围绕它把 A6 的 #200／#185 移出避让**——即**机制本就会分发它，"请他粘贴"是多余的一步**，且本线对此毫不知情（见 #230 并发不可见）。**同日另一实证**：本线手工做掉的 #218 清扫，正是周计划 A4① 已排的活。 **⇒ 写进队列即已完成交付；至于何时被谁领走，交给巡检与巡逻，不必也不应由本线推动。** **例外**：真实阻塞或安全风险可即时升级，但须在队列行内标明"因何紧急而绕过机制分发"。

---

## §七 收工段

滚动更新本文件（日期节只留 2 个）→ 更新台账状态头 → 列文件清单＋建议 commit message＋登记 §二 批次（**须含队列文件自身、状态列禁带 ✅**）→ 触发 sweep。Cowork 不擅自 commit。

---

## §八 日期节（R5：只留最近 2 个）

### 【2026-08-09 · 第四批 session】承载性核查第四批 ＋ A3 判定立 #313 ＋ 撞出措施 C 口径失真

**开工自检四查全通过**：查1 队列 §二 末批次即 A3 那条、高水位线 §一 #312／§四 #57 与本件一致；查2 抽 #306／#312 —— **#306 已 `[S:done]`（本表上一版写「P3 待确认」已过时，队列为准）**、#312 一致；查3 「⏳ 队列更新待补」命中 4 处全为叙述性（与基准一致）；查4 四份 skill 差异行**全部为 0**（与第 5 轮基准一致，**这之后无新欠账**）。

**做完三件**：⑴ **承载性核查第四批**（核查件 §3.7／§5.2，未核清单销 2 增 1）；⑵ **A3 两项判定完毕并立 #313**（合为一行，不取两号）；⑶ **A4 撞出措施 C 的 WIP 口径失真**（3／8 → **16／8**，旧口径低估）。

🔑 **本轮三个发现指向同一件事，值得一起看**：
1. **协议指名了载体、载体没接**（项①）——比「没指定载体」更难发现，因为**协议正文读起来像已经安排好了**；
2. **两个载体各自实现同一条规则、判据不等价，且文档侧更宽松**（项②）——人会按文档做、然后撞墙；
3. **一个上限值在错误的基数上定了，然后一直被引用**（A4）——**3／8 这个数被写进核查件 §2.1 与队列 #309 行，两处都把它当实测基准**。

**⇒ 三者共同点：都不报错，而且都有一份看起来很可信的书面记录在支持错误结论。** 这与前三批的「什么都没发生」是同一族的**下一层**——不是缺记录，是**记录本身在替错误背书**。

**⇒ 四项拍板当场执行完毕**（他答 1→(a)／2→(b)／3→全默认，详见 §五 表）：`followup-letter` v3.1 三处修 ＋ 模板库 §〇.10 ⑷ 日期初筛 ＋ 措施 C 上限 8→16（三处落点已改两处）＋ #313 落档拍板项 10 ＋ 备 CC 派单件 G1。

🔑 **执行过程中撞见一件值得记的事**：`save_skill` 后第 4 查复扫**报 3 行差异**——源码漏了 `§3.7.2` 指针、安装版多一行首空行，**正是 §〇.10 ⑴ 要防的「只改了一边」，而且是我自己刚犯的**。两边修平后复查四份全为 0。**⇒ 第 4 查这个机制真的会抓到东西，不是走形式；且它抓到的第一个人就是设计它的那条线。**

⚠️ **三处本线未做、如实登记**：⑴ 拆件巡逻 prompt 三.2 措辞瑕疵**仍未修**（本批未触碰该 prompt，「同车修」条件未满足，继续挂账）；⑵ **`MECHANISM_WIP_CAP_DEFAULT` 仍是 8**（Cowork 不写生产代码，已并入 #313 ⑥）；⑶ **拍板项 10 的 one-in-one-out 提议未经完整验证**（「类型⑧零命中」只查了当前 §二、未回溯历史巡检简报，已两处标明须领取方先复核）。

---

### 【2026-08-09】E1-E4 全部收口 ＋ 一次真实分叉恢复 ＋「记忆偏差」根因整治第一步

**E1-E4 全完成**：#305 写侧注入止血＋§四#57 Dependabot（`ce8b784`／`6080acc`）／#308 propose→design→apply 全推 master（`efa522f`，变更包按纪律暂不 archive）／#267 孤儿 worktree 第三桶（`f1c9e29`）／#310 常驻部署债（worktree ff-merge 至 `8021034`、357 passed+1 skip、服务已重启、两个空壳已处理）。
🔴 **一次真实分叉恢复**：主工作区与 origin 分叉、sweep 连发 5 轮告警且每轮静默跳过（**当天刚写进协议〇.8 的那句话当天就兑现**）；由 E2 在主工作区手工 rebase 解开，#310 行与其批次逐行核对后保留。**教训**：E2 报的 `0/0` 主语是它自己的 worktree，**主工作区从未被它同步过**——读 `0/0` 要先问"哪个工作区"。
🔑 **「记忆偏差」整治第一步（他 2026-08-09 拍板 (a)）**：四项取证坐实**三段根因链**——①不入法的规则只活当次会话（「opener 须标 session 明细」全库零历史痕迹，同族已入法三条至今在用）②入法但落非必读载体仍缺席③落进必读载体也被稀释（迁移前流水账占 49%）。**已做**：`CLAUDE.md` **154 KB → 92 KB（−40%）**、执行判定表 B1、协议〇.8 加「立法请求须写明落点」一问（不新增条目）、**新增 #311 承接剩余 13 条——不再"另排"**。
⚠️ **新增两条实测教训**：`worktree` 与 `session` 是两个维度（已入法 §〇.11）；**MCP 超时 ≠ 命令没跑**（一次 `acquire --reserve-multi` 超时未返回但已成功预留 #311，重试前须先查 `.editlock`）。

---

### 【2026-08-08 · 第 4-5 轮】三份 skill 源码回写 ＋ 承载性核查第三批 ＋ 拍板项 5-9 全部执行

**第 5 轮（他答「全部按默认执行」）已落实五项**：⑴ **#308 加子项 F**——编辑锁 `release` 补 F1「§二 新增批次行不得以 ✅ 开头」（复用 ③ 的"新增行"判别、不误拦既有行转 ✅）与 F2「✅ 不在状态列开头即报」（直接调 `_leading_status_segment()`）；⑵ **协议〇.8 加一句**——触发 sweep 时若锁被占用则整轮零动作跳过且不告警，须回看 `sweep-commit.log`；⑶ **`zhuopin-followup-letter` v2→v3 双向修**（称呼／状态值改 `⏳ 待你审`／补四态＋批准脚本＋冷却窗口＋`🔒人工发送`＋串行原则闸），**已安装版与源码正文逐行全等**；⑷ **README 扩四态**，新增 `📥 已回件并回灌 <日期>`，串行原则判据改写为可执行口径（**实测无需改代码**）；⑸ **opener 模板库新增 §〇.10**——两向同步允许降级但须登记反向漂移，检测挂开工第 4 查。
🔑 **收尾实测：四份 `zhuopin-*` skill 正文差异行全部为 0**，两向同步首次全部清零，已作为下次开工的比对基准。
🔴 **他当场驳回拍板项 8 的人守尾巴**（原话「这个靠人记恐怕不行」）——「回件消化完记得改 `📥`」正是本核查一直在批评的形态，**已改为挂咽喉**：新增 **#308 子项 G**（`release` 回查该收信人上一封是否仍非闭环态，是则拒绝并提示先据实改标；带 `串行豁免：〈理由〉` 逃生阀）。**刻意不加 sweep 侧告警**——本轮刚坐实 sweep 已 11 项守卫而协议只承载 1 项，再加第 12 项与〇.9 措施 B 冲突；本项是**挪位**不是新增。
📤 **环境侧按序 Open（他选 (a)）**：E1 #305（＋§四 #57 Dependabot）／E2 #308／E3 #267，**三者代码触碰区实测两两零重叠、可全并行**；#306 与双队列拆分本体硬阻塞于 #308，本轮不开。派单件已落档。

**第 4 轮**：
**A 段（已完成）**：三份 `zhuopin-*` skill 源码已按已安装版回写，`description` 统一为「卓品智能AI转型项目」，**与系统提示注入值逐字比对全等**（132/220/138 字符）；源码 frontmatter 的 `status: 在办` 予以保留（R1 要求，已安装版无此字段）。
**B 段（已完成）**：承载性核查第三批，核 3 类载体 —— ①**编辑锁 3 处真缺口**（登记禁带✅／本批变更参数+已复检／✅须在状态列开头，**后两条判据函数现成、成本近零**）；②**sweep 11 项守卫反向核，仅 1 项已承载**；③**看板 artifact 无缺口**（曾疑其未标"依赖外部方未排除"，**实测证伪已留痕**）。
🔑 **一条旧结论被证伪**：§3.5.3 原判「已安装 skill 正文层无法比对」——**实测在 Cowork 沙箱侧有只读挂载**，正文可逐行 `diff`；据此当场比出**第四份 skill `zhuopin-followup-letter` 双向都错**（称呼层反向漂移＋两版状态值都与机器真值不符）。
**已知未闭合（第 5 轮后剩这一条）**：拆件巡逻 prompt 三.2 措辞仍未修（守"只判定不改"，**碰到该 prompt 时同车修**）。
