---
status: 在办
title: "看护件 · 2026-08-31 · 清池批（值周读取面瘦身）"
编号: OP-0831-N
日期: 2026-08-31
触发: Shao Peishen「清池 上限3，优先选 #441 与 #442」（zhuopin-lane-clearpool v1.0，灰度已解除，默认上限 6）
---

# 看护件 · 清池批 · 2026-08-31

## 〇 口径

**前置自检三条（skill「触发与前置」，任一不过即停）**

| 项 | 实测 2026-08-31 | 判定 |
|---|---|---|
| ⑴ `git fsck` 健康 ＋ 与 origin 对齐 | `fsck` 仅 dangling blob（非错误）；`ahead=0 behind=0` | ✅ |
| ⑵ 编辑锁无他人长占 | 无锁 | ✅ |
| ⑶ 上一清池批汇总已出 | `reports/opener-batch/20260829-160020/summary.txt`（08-29 16:45）＋ `reports/lane-heartbeat/OP-0829-T.md`／`OP-0829-U.md` 双泳道心跳 | ✅ |

> 🔴 **本件留一条 Cowork 自身的更正记录（不隐去）**：本轮曾据「`看护件-2026-08-29-offLAN批2灰度.md` 里没有收工汇总节」判定前置⑶ 不过、当场停批。**该判定是错的**——`tasks 3.1／3.2` 均已 `[x]`，汇总以**独立 summary 产物**形式存在，不在看护件内。
> 🔑 **教训**：**「某载体里没有 X」不等于「X 不存在」，除非先确认 X 应当落在该载体。** 与本日 `#419`／`#354` 那两次（据转述的阻塞理由下结论）同族——**都是先假定了一个载体位置，再据其空缺下判断**。

**灰度状态**：`tasks 3.1`（首跑限 2、他在场，全链走通）与 `3.2`（三判据全过）均已 `[x]` ⇒ **灰度已解除，默认上限 6**。本批他指定上限 3。

---

## 🔴 一 · 硬边界继承（skill 红线，逐条不得越）

1. **业务建造与一切对外发送永不自动入批** —— 本批两项均为机制工具类（`0-学习与工具/` 下 CLI 与读取面），不涉业务场景、不涉对外。
2. **不自造 🛑 豁免理由** —— 本批未去任何 🛑。
3. **`.51`／真实库／规划文档／他线在办不碰** —— 本批不触碰 `.51`、不触碰真实库、不改规划文档。
4. **`0828` 看护件 §一 十条硬边界整体继承。**
5. **「已部署 `.51`」类报告必假、当场停** —— 本批无 `.51` 动作，若泳道报出此类内容即为越界。

---

## 🔴 二 · 选件与波次

### 入选（2 项）

| 行 | 内容 | D1 判定 |
|---|---|---|
| `#441` | 给 `0-学习与工具/工具-队列查询.py` 加 `--digest`（值周读取面瘦身 ①） | ✅ 纯机制工具、off-LAN 可做、判据可执行、行内自陈「可立即开工」 |
| `#442` | 把 2.9 MB 的 `跨桌任务队列-归档-202608.md` 移出值周读取面（②） | ✅ 同上；与 `#441` 同族，行内自陈「若与 `#441` 同车做」 |

### 🔴 实选 2 而非 3（上限 3）——理由须留痕

**D1 原则是「宁漏勿滥」。** 第三条候选（`#410`／`#422`／`#324`／`#328` 等）**未逐条核触碰区与他线在办状态**；未核即选＝误派风险，而误派正是首跑三判据的第一条。**本批不为凑满上限而选。**

### 波次

**单波（波 1）**：`#441` ＋ `#442` 同车。
**判据**：两项触碰区高度重叠（同为 `0-学习与工具/工具-队列查询.py` 及其消费方章程），分波反而制造两次改同一文件的冲突。
**锁工具族**：本批不涉，无需独占收工窗口。

---

## 三 · 泳道 opener 正文（原样传给无头 CC）

```
[OP-0831-N]【CC】清池批 · 值周读取面瘦身 #441 ＋ #442（同车）
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建一个 ｜ 分支：新建 claude/clearpool-0831-digest ｜ 工作区：不动主工作区 ｜ 派出线：Cowork 清池批（OP-0831-N）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0831N-清池441442。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-08-31-offLAN批.md 全文（本件即作业书）→ ② 队列 §一 #441 与 #442 全文（用 工具-队列查询.py --row N，🔴 勿通读真身——那正是本批要修的病）→ ③ CLAUDE.md 当前进度。

本次两件（同车，先 #441 后 #442）：

① #441 给 0-学习与工具/工具-队列查询.py 加 --digest
   输出格式：每行「编号 ｜ [S:x][D:y] ｜ 任务列首 N 字」，两份真身合并输出。
   --row 由必填改为与 --digest 互斥可选（二者至少给一个）。
   🔴 实测现状（Cowork 已核，勿重跑）：该文件内 digest 命中 0；而三份章程都在指挥调用它——
      weekly-status-update.SKILL.md:14 ／ zhuopin-queue-audit/SKILL.md:61 ／ huijian-chaijian-patrol.SKILL.md:20。
      即「处方写了、药不存在」，拆件巡逻还写了退路，值周章程没写退路，只剩通读一条路。

② #442 把 2.9 MB 的 1-转型规划/0-全景路线图/跨桌任务队列-归档-202608.md 移出值周读取面
   改哪一处以 #442 行内判据为准，不自行扩范围。

🔴 验收（须报实数，不报「跑通了」）：
   跑一次 --digest，报出输出字节数与行数；与通读真身的实测体量对比并把倍数写进队列行。
   参考实测：机制环境 1,596,765 B ＋ 业务场景 353,062 B ＋ 当月归档 2,891,167 B ≈ 4.8 MB。
🔴 边界：本批不碰 .51、不碰真实库、不改规划文档、不做任何对外发送；不去任何 🛑。
   若发现须改章程正文（如值周 SKILL.md），只登记不改——章程改动须另走门槛。

收工：① 每项一行写「判据→证据→行内状态处置」回写 #441/#442；② 登记 §二 批次；③ 自行 commit+push；
④ 🔴 汇总须写明「本批 2 项／清池候选池剩余 N 项」，N 用去重后的可执行队列行数，不用命中处数。
```

---

## 四 · 三清单（落选去向）

| 清单 | 内容 |
|---|---|
| **待人裁** | 第三条候选（`#410`／`#422`／`#324`／`#328` 等）——未核触碰区，按宁漏勿滥不入本批，待下批逐条核 |
| **待 Cowork 班** | 无 |
| **LAN 留步** | 无（本批两项均 off-LAN 可做；今日 LAN 留步另见 `看护件-2026-08-31-LAN收口首跑批.md`） |

---

## 🔴 四bis · 本批未派出（2026-08-31 18:1x，落档后、起泳道前撤回）

**事实**：看护件已落档并登记 §二 批次 `B-0831_17`、写后反查通过、锁已释放；**但泳道未起**。

**原因**：就在核对 `工具-opener批处理执行v2.ps1` 调用签名的那一刻，Shao Peishen 已并行发出 `[OP-0831-D2]`（Cowork 早先备的直派 opener），**其内容与本批泳道是同两件 `#441`／`#442`**。再起清池泳道将造成**两个 CC 会话同时改 `0-学习与工具/工具-队列查询.py`**。

**处置**：本批**不派出**，`#441`／`#442` 的执行归 `OP-0831-D2`。本件保留为清池流程的完整留痕（前置自检、D1 选件、波次判据、opener 正文），供下批复用。

### 🔑 由此暴露的机制空白（本件的真正产出）

**clearpool 的「选件落档」与「直派 opener」之间没有互斥。**

- 队列行的**触碰区**纪律能防「同队列内两行撞同一文件」，**防不了**「一条线正在落档、另一条线已经派出去」——因为**落档不等于占用**，而派出是瞬时的。
- 本次是良性撞车（同两件、且人在场当场发现）。**换成两条不同的线选中同一行，就会是两个泳道同时改同一文件而互不知情。**
- ⇒ **建议**：clearpool 步骤 3「落档」之后、步骤 4「起泳道」之前，**增加一次「入选行是否已被他线派出」的复查**（判据可用队列行状态列是否已出现新的 `OP-xxxx` 派出记号，或直接在落档时把入选行状态改为「已入清池批 `OP-xxxx`，待派」）。**后者更硬**：它让「已被选中」这件事对其他线可见。
- **本条建议并入** `openspec/changes/lane-clearpool-skill/tasks 2.x`，与 `2.1`（调用签名核对）同批处置。

---

## 六 · 重选件并派出（2026-08-31 18:2x，Shao Peishen「最好今天还是能跑清池」＋「可以串联」）

**原入选 `#441`／`#442` 已归 `OP-0831-D2`，本批重选。** 触碰区逐行实扫（`--` 全部可动机制类行的触碰区列），结论如下。

### 落选并点名（D1 宁漏勿滥，逐条给理由）

| 行 | 触碰区 | 落选理由 |
|---|---|---|
| `#22` | Artifact `zhuopin-project-status` | 🔴 **他另一条会话正在修该 artifact 数据层**，明确撞车 |
| `#358` | `zhuopin_platform/audit/` ＋ **`.51` 现网四服务日志落盘路径** | 🔴 触碰 `.51` —— clearpool 红线「`.51` 不碰」 |
| `#436` | `ops/wecom-service-home` worktree | 🔴 与 `#438` 执行体对齐同一对象 |
| `#434` | 跟进信 README ＋ 四份 session 接力 ＋ opener 模板库 | 🔴 README 今日已被 S4 桥二改过；接力件属权威载体，边界过大 |
| `#324`／`#341`／`#381`／`#416` | 均含 `0-学习与工具/工具-共享文档编辑锁.py` | 四行挤同一文件；且属**锁工具族**（skill：恒最后一波、独占收工窗口）。🔴 **另有时序风险**：`OP-0831-D2` 收工要经编辑锁 release，若此刻正在改锁代码，D2 会跑到半改的码 |
| `#328` | `工具-落库sweep.py` ＋ 单测 ＋ `.gitignore` | 与入选的 `#410` 同文件 ⇒ 本批二选一，取边界更小的 `#410` |
| `#96`／`#130`／`#240`／`#326`／`#366`／`#419`／`#425`／`#433`／`#443` | 各见行内 | 分别涉：治理文档／对外通知（红线）／外部依赖（IT 陈承）／aibot 服务族／同上／今日刚拍板待派单／git 引用只读取证／根 `CLAUDE.md` 权威载体／「与所有写队列 session 全重叠」 |

### 入选（2 项，🔴 上限 3 但实选 2）

| 行 | 内容 | 触碰区 | D1 判定 |
|---|---|---|---|
| `#410` | editable 安装指向巡检判据——让「import 解析到 worktree」下次能被自动发现 | `0-学习与工具/工具-落库sweep.py` ＋ 其单测；**只读 site-packages，不改任何安装** | ✅ 纯工具、边界自陈清晰、零重叠 |
| `#337` | 场景级 R5——SC8／FI2 各建同目录 `CHANGELOG.md`，场景 `CLAUDE.md` 正文只留指针 | `4-数字员工/采购部/SC8-*/CLAUDE.md`、`4-数字员工/财务部/FI2-*/CLAUDE.md` 及新建 `CHANGELOG.md`；**不碰场景代码、不碰测试** | ✅ 纯文档整理、零重叠 |

🔴 **实选 2 不凑满 3 的理由（与本件 §二 同一条口径，不因他给了上限就放宽）**：第三条候选逐条核过，**没有一条能同时满足「触碰区零重叠 ＋ 不碰 `.51` ＋ 不碰权威载体 ＋ 不与在跑线撞」**。D1 原则是宁漏勿滥。

### 波次

**单波并行**：`#410` 与 `#337` 触碰区零重叠（`0-学习与工具/` vs `4-数字员工/`），**可同波并行**；两者也与 `OP-0831-D2`（`工具-队列查询.py`）零重叠，**不必串联**。

### 泳道 opener

### A1 · 【CC】#410 editable 安装指向巡检判据

▶ 粘贴端：CC　▶ 泳道：sweep工具

```
[OP-0831-N-A1]【CC】#410 editable 安装指向巡检判据
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建一个 ｜ 分支：新建 claude/queue-410-editable-probe ｜ 工作区：不动主工作区 ｜ 派出线：Cowork 清池批（OP-0831-N）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0831N-A1-410editable。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-08-31-offLAN批.md 全文（本件即作业书，硬边界在 §一）→ ② 队列 §一 #410 全文（工具-队列查询.py --row 410，🔴 勿通读真身）→ ③ CLAUDE.md 当前进度。

按 #410 行内判据实现：让「import 解析到 worktree」这一形态下次能被自动发现。
🔴 触碰区严格限于：0-学习与工具/工具-落库sweep.py ＋ 其单测；site-packages 只读、不改任何安装。
🔴 边界：不碰 .51、不碰真实库、不改规划文档、不做任何对外发送、不去任何 🛑。
🔴 验收须报实数：造一个「import 解析到 worktree」的真实情形，确认判据能抓到；再造一个正常情形，确认不误报。两个结果都写进队列行。
收工：回写 #410（判据→证据→行内状态处置）＋ 登记 §二 批次 ＋ 自行 commit+push。
```

### A2 · 【CC】#337 场景级 R5：SC8／FI2 建 CHANGELOG

▶ 粘贴端：CC　▶ 泳道：场景文档

```
[OP-0831-N-A2]【CC】#337 场景级 R5——SC8／FI2 各建 CHANGELOG.md
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建一个 ｜ 分支：新建 claude/queue-337-scenario-changelog ｜ 工作区：不动主工作区 ｜ 派出线：Cowork 清池批（OP-0831-N）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0831N-A2-337场景R5。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。

读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-2026-08-31-offLAN批.md 全文（本件即作业书，硬边界在 §一）→ ② 队列 §一 #337 全文（工具-队列查询.py --row 337）→ ③ 两个场景各自的 CLAUDE.md。

按 #337 行内判据：SC8／FI2 各建同目录 CHANGELOG.md，场景 CLAUDE.md 正文只留指针。
🔴 触碰区严格限于：4-数字员工/采购部/SC8-*/CLAUDE.md、4-数字员工/财务部/FI2-*/CLAUDE.md 及该两目录新建的 CHANGELOG.md。
🔴 不碰场景代码、不碰测试（行内自陈的边界，逐字照守）。
🔴 边界：不碰 .51、不碰真实库、不改根 CLAUDE.md 与任何规划文档、不做任何对外发送、不去任何 🛑。
🔴 迁移守 J1：搬进 CHANGELOG 的每条内容，须在场景 CLAUDE.md 留下可 grep 的指针；零残差，改完 grep 自检。
收工：回写 #337（判据→证据→行内状态处置）＋ 登记 §二 批次 ＋ 自行 commit+push。
```

---

## 五 · 收工汇总（泳道回报后补，🔴 缺此节即视为本批未收口）

> 待泳道收工后填：每项一行「判据→证据→行内状态处置」＋ 心跳全文指针 ＋ 未闭合产出扫描器三形态数字 ＋ **本批 2 项／候选池剩余 N 项**。
> 🔴 **本节的存在本身是本日教训的产物**：`lan-closeout` 与 `lane-clearpool` 共用骨架，**收工汇总没有强制承接方**——lan-closeout 首跑漏写、本 skill 08-29 首跑写在了看护件之外，两次都造成"跑完了 ≠ 收口了"的判读困难。**本批把汇总节预先留在看护件里，使缺失可见。**
