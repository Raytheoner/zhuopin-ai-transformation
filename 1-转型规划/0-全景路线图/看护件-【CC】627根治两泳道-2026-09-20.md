---
title: 看护件 · 【CC】`#627` 根治两泳道
created: 2026-09-20
派出线: 环境总线 OP-0920-B
批次: B-0920_627根治
status: 待执行
---

# 看护件 · 【CC】`#627` 根治两泳道 · `B-0920_627根治`

## 〇、口径

🔴 **本批是把 `#627` 真正修掉，不是再登记一遍。** 2026-09-20 一整天环境侧只登记未落地，业务侧为此让路——**没修好不报，彻底修好为止，验收后转场**（Shao Peishen 当日直令）。
🔴 **看护件按泳道切片**（本批自身即实践 `#627` 的一条）：本件只放口径与排波，**两条泳道各自的做什么／不做什么全在各自派单件里**，泳道 opener 的 `--input-pointer` 直接指向它自己那一份，**不整读本件**。A 件 4,549 B、B 件 3,528 B——对照上午那份 24,865 B 的看护件被七条泳道各整读一份、且读进去就常驻整个会话。
🔴 D1 四档判据现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不复述。判据未覆盖按 🟡 停。

## 一、硬边界继承

1. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
2. 🔴 不碰 `.claude/rules/`（合计 43,388 B／阈值 30,720 B，已超 12,668 B）与根 `CLAUDE.md`（12,110／12,288，余 178 B）。
3. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`。
4. 🔴 收工只 push 自己那条分支，不碰主仓、不 ff master。
5. 🔴 **回归 narrow 到受影响测试类（node-id，禁 `-k`）；不跑整份回归、不起后台任务轮询等待；测试输出一律重定向到文件只读结论行。**
6. 🔴 **收工前必须自己跑 `git diff --name-only master...HEAD` 并贴进收工报告；派单件「期望产出」点名的文件缺一个就不许写 `OPENER_DONE`。**

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#627` | `批处理侧线` | `0-学习与工具/工具-opener批处理执行v2.ps1`＋其单测；`0-学习与工具/工具-opener派出前校验.ps1` | 派单件＝`派单件-【CC】627根治A-批处理侧-2026-09-20.md`。🔴 不碰生成器／lint／骨架 |
| 1 | A2 | `#627` | `生成器侧线` | `0-学习与工具/工具-opener生成.py`＋其单测；`0-学习与工具/工具-opener块lint.py`；`1-转型规划/0-全景路线图/opener骨架.md` | 派单件＝`派单件-【CC】627根治B-生成器侧-2026-09-20.md`。🔴 不碰批处理 ps1／派出前校验 |

两条**触碰区实测零重叠、可并行**；无硬序、无 ⏭️ 转出项、无「LAN 留步」项。

## 三、泳道 opener

### A1 · `#627` 根治 A · 批处理侧（§一 #627）

粘贴端：CC ｜ 泳道：批处理侧线

```
[OP-0920-M]【CC】627根治A
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920m-627-fix-a-batchrunner`） ｜ worktree：☑（op0920m-627-fix-a-batchrunner，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】627根治A-批处理侧-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920m-627-fix-a-batchrunner --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920m-627-fix-a-batchrunner --done --batch B-0920_627根治 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · `#627` 根治 B · 生成器侧（§一 #627）

粘贴端：CC ｜ 泳道：生成器侧线

```
[OP-0920-N]【CC】627根治B
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920n-627-fix-b-generator`） ｜ worktree：☑（op0920n-627-fix-b-generator，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】627根治B-生成器侧-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920n-627-fix-b-generator --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920n-627-fix-b-generator --done --batch B-0920_627根治 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头，本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0920-O]【CC】看护627看护
【设置】执行环境：CC ｜ 分支：627-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0920O-看护627看护。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】627根治两泳道-2026-09-20.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总

```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0920_627根治
```

🔴 **本批验收判据（机器可判，缺一不算修好）**：
1. A 面：`reports/context-meter/<session_id>.json` 的 `lastContext` 被 `工具-opener批处理执行v2.ps1` 真正消费——单测覆盖三档（<150k 不动／≥150k 记峰值／≥250k 终止并记 `OPENER_PARTIAL: 上下文转场`）；`summary.txt` 新增「上下文峰值」列与「本批越 150k 的泳道 N 条」汇总行。
2. B 面：四条固定尾句由生成器**强制注入**且 lint 有形态守；`CHECKBOX_RE` 收紧为「`☑` 必须带非空括号名」；`--batch` 真值进尾句。
3. 两条分支 ff 六关全过（ff 属 🟡 档，由看护者拿到 Shao Peishen 一字母后串行做）。
4. 🔴 **最终验收＝实测，不是自陈**：ff 后用 `python 0-学习与工具/工具-Token用量度量.py` 出本批两条泳道的上下文峰值与 `cache_read`，与 2026-09-20 上午七条（峰值 244,434／228,308／213,218／194,282／163,826／155,660／151,036，`cache_read` 5.02M–15.48M）逐条对照，写进收工件。
