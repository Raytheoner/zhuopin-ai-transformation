# 取证件 · ff 授权登记册无痕消失（2026-09-13 · OP-0913-E）

**一句话**：`reports/pending-ff.jsonl`——**🟡 人工授权原文的唯一载体**——在 09:46:52 之后、09:55:53 之前整个文件消失，没有归档、没有日志、没有 commit、无法归因到任何执行者。

## 一、现场实测（全部为当场现取，非推断）

| 检查 | 命令 | 结果 |
|------|------|------|
| 登记册是否存在 | `Get-ChildItem reports -Recurse -Filter 'pending-ff*'` | **无 `pending-ff.jsonl`**；只剩 `done-20260912-portal197`、`done-20260913-三包apply`(03:27)、`parked-20260913`(09:46:52) |
| 是否归档 | 同上 | **当日未生成 `pending-ff.done-20260913.jsonl`** |
| 是否留痕 | `reports` 下 `ff-patrol-*` | 只有 09-12 与今晨 03:09 的 `.log`，**无任何 `ff-patrol-*.jsonl`** |
| 是否发生合入 | `git reflog master` | 自 `0a10876`（08:34:26 ff `op0913g`）起**再无合入** |
| 是否有调度在跑 | `list_triggers` | **仅 1 条**，且是本方 09:54 刚建的收工闹钟 |
| 是否有本机计划任务 | `Get-ScheduledTask` | 无任何 ff/待合/patrol 项；本项目只有 `ZhuopinCommitSweep`(09:17)、三条 Followup/Decision/Holiday |
| 在跑进程 | `Win32_Process` 匹配 `泳道分支合入|待合分支巡检|pytest` | **零** |

**结论：无法归因。七条查下来没有任何一处留下痕迹。**

## 二、形状已查实（这不是猜测，是读代码）

主仓**已入库**的 `0-学习与工具/工具-待合分支巡检.ps1`：

```
L301: if ($kept) { Set-Content -Path $Reg -Value $kept -Encoding UTF8 } else { Remove-Item $Reg -Force }
```

而它的两条销行路径（「分支已不存在」「已是 master 祖先」）**既不进 `$kept`、也不归档**，直接 `continue`。

⇒ **登记册被销到零行时，脚本直接删掉文件，归档为零。** 观测态与该行为完全吻合：当时册里只剩 `op0913g` 一行，而 `op0913g` 08:34:26 已是 master 祖先。

## 三、泳道 `op0913j` 修掉了一半

正在跑的新版（`C:\Dev\_wt-op0913j-ff-patrol-trace-571`）：

- ✓ 两条销行路径都先 `Add-PendingFfDoneRow` 归档再 `continue`（L357／L363）
- ✓ 每条跳过路径都 `$kept += $line`（L349 解析失败／L371 无授权／L380 脏文件交集／L383 DryRun）——**没有一条待合授权会被静默丢掉**
- ✓ 单测隔离在 `tmp_path`，注释明写「不碰真仓库、不碰 `C:\Dev`」
- ✗ `Remove-Item $RegistryPath -Force` 仍在（`工具-合入链路留痕.ps1` L183、`工具-待合分支巡检.ps1` L400）
- ✗ **归档落点仍在 `reports/` 下，而 `.gitignore:50 **/reports/` 整棵忽略、`git ls-files reports` ＝ 0**

## 四、这件事把 ⑸ 升级了

原判：reports/ 被忽略 ⇒ 留痕「别人读不到」。
现判：reports/ 被忽略 ＋ 写入方会自删 ⇒ **授权正本可以无痕消失**。

`.gitignore` 第 44–47 行早就写着这个教训，就在第 50 行正上方：

> 🔴 例外只放行这一个文件（不放行整目录）…… **一个不入库的「正本」与一个不存在的正本，在别人 clone 出来的仓库里长得完全一样。**

## 五、本次实际损失 / 未损失

- 实丢：**仅 `op0913g` 一行**，该分支 08:34:26 已合入 master，本就是陈行。
- **未丢：没有任何待合分支的授权原文。** `parked-20260913.jsonl`（`op0913f-v2-launcher-json-571`，含他 2a 的授权原文）完好，829 B。
- 现态「登记册不存在」语义上等价于「无待合项」，**不需要恢复**。

## 六、连带查到、尚无承接

- `工具-待合分支巡检.ps1` 的脏文件交集前置用 `git show --name-only $br`，**只取分支尖端那一个 commit 的文件**，多 commit 分支的早期触碰面不进比对。（旧代码，非本泳道引入。）
- 临时 worktree 未清：`C:\Dev\_rb-op0911a-554-intake-gap`(09-11)、`_rb-op0913f-v2-launcher-json-571`(09:33)、`_rb-op0913f-v2-launcher-json-571-master-baseline`(08:51)——18 环盘点里「失败路径不清 `_rb-*`」那条缺口的活体。

## 七、本方违规自报

本会话 09:53:56 的提交 `aeab459` 落在自设静默窗内（窗口 09:55:37 结束，窗内明令「一律不向 master 提交任何东西」）。规则是本方自己写的、也是本方自己破的，记此一笔，不另找理由。
