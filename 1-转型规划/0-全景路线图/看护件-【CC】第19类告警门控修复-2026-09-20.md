---
title: 看护件 · 【CC】第 19 类承载性告警门控修复
created: 2026-09-20
派出线: 环境总线 OP-0920-B
批次: B-0920_告警门控修复
status: 待执行
---

# 看护件 · 【CC】第 19 类承载性告警门控修复 · `B-0920_告警门控修复`

## 〇、口径

🔴 **本件是修 master 上的红**，不是新功能。Shao Peishen 2026-09-20 答 `1a`（前向修复，不回滚）。

事故经过，一句不省：`#601` 泳道给 `0-学习与工具/工具-落库sweep.py` 接了**第 19 类常驻状态告警——承载性一致性扫描**。第一次 ff 跑整份 `test_工具-落库sweep.py` 时六关④判出**分支独有的三条新增失败**，按纪律拒绝合入（`exit=4`，分支停 `28b487a4`，备份 `backup/op0920f-carrier-scan-601-pre-rebase`）。随后看护者用**更窄的 narrow 口径**（`test_工具-承载性一致性扫描.py` ＋ `test_工具-落库sweep.py::StandingStateAlertLifecycleUnitTests`）重跑，全绿、合进了 master。**那是看护者的判断错误——用更松的口径覆盖了一道已经正确拒绝过的闸，且把这个错误自动化了。** 现取复核（master `637b70b8`）三条确实全红。

🔴 **本批的 narrow 纪律因此单独写死**：本泳道的回归**必须跑整份 `0-学习与工具/test_工具-落库sweep.py`**，不得再收窄。理由＝**常驻告警类往共享 webhook 通道里发东西，影响面是整份测试里所有断言「零噪音」的用例，不是某一个机制类**。

## 一、硬边界继承

1. 🔴 只改告警**门控**，不改扫描器本体 `0-学习与工具/工具-承载性一致性扫描.py`，不动 `#601` 已落地的其余产出。
2. 🔴 不碰 `.claude/rules/`（合计 43,388 B／阈值 30,720 B，已超 12,668 B）与根 `CLAUDE.md`（余 178 B）。
3. 🔴 收工只 push 自己那条分支，**不 ff master**——ff 属 🟡 档，由看护者拿到 Shao Peishen 一字母后串行做。
4. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`。
5. 🔴 **不要顺手修纯 master 上那 16 条 pre-existing 失败**（`DeploymentTraceHintTests` 3／`OrphanFileAlertTests` 4／`OrphanResolvedNotificationTests` 2／`ResidentServiceDeploymentHintTests` 3／`ExactMatchEndToEndTests` 1／`OrphanSectionFourEscalationTests` 1／`ScenarioSpecGapAnnounceIntegrationTests` 1／`StaleChangeAnnounceIntegrationTests` 1）——那是另一件活，Shao Peishen 已答 `2a` 单独立行给下一棒。**本批只要求：这 16 条不增不减。**

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 本波边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#631` | `告警门控线` | `0-学习与工具/工具-落库sweep.py`（第 19 类 `_check_carrier_consistency`／`_render_carrier_alert` 的告警门控段）＋ `0-学习与工具/test_工具-落库sweep.py`（补接线单测） | 现象：`ScheduledTaskMirrorSyncTests` 三条断言「零 webhook 噪音」，实测收到 1 条（`AssertionError: 1 != 0`，`test_工具-落库sweep.py:2002`）。疑似成因＝沙箱临时仓里**扫描器脚本不存在 ⇒ 走「⚠ 扫描器不可用——不据此判为干净」分支时仍然 `_alert` 推 webhook**。🔴 **先实证再改**，不要照抄本段推测 |

本批单条泳道，无并行、无硬序、无 ⏭️ 转出项、无「LAN 留步」项。

## 三、泳道 opener

### A1 · 第 19 类承载性告警门控修复（§一 #631）

粘贴端：CC ｜ 泳道：告警门控线

```
[OP-0920-K]【CC】告警门控601
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0920k-carrier-alert-gate-601`） ｜ worktree：☑（op0920k-carrier-alert-gate-601，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】第19类告警门控修复-2026-09-20.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920k-carrier-alert-gate-601 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0920k-carrier-alert-gate-601 --done --batch B-0920_告警门控修复 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头，本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0920-L]【CC】看护门控看护
【设置】执行环境：CC ｜ 分支：gate-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0920-B ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0920L-看护门控看护。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】第19类告警门控修复-2026-09-20.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总

```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0920_告警门控修复
```

🔴 **验收判据只有一条，机器可判**：在本泳道 worktree 里跑 `python -m pytest 0-学习与工具/test_工具-落库sweep.py -q -rf`，`ScheduledTaskMirrorSyncTests` 三条**转绿**，且失败集合相对纯 master **不增不减**（纯 master 现为 16 条，清单见 §一 第 5 条）。🔴 **另须补一条接线单测**覆盖第 19 类本身——`#601` 加了告警类却零单测，正是本次事故能溜过去的底层原因。
