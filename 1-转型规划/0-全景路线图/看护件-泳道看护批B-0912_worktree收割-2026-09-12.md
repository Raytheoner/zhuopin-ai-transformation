---
title: "看护件 · 泳道看护批 B-0912_worktree收割"
created: 2026-09-12
status: 生效
批次: B-0912_worktree收割
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_worktree收割`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴。** 授权原文：Shao Peishen 2026-09-12 回 `1a`——12 个 `[WT-BLOCKED]` worktree 派一棒「**收割再清**」。
🔴 **定性先于动作**：那 12 个 `remove` 被拒**不是故障，是保险丝**——`OP-0912-E` 逐个取证发现里面至少三批从没进过 master 的产出（`#446` 的三个 `.py`、两个 openspec 包、两处工具改动）。**一次 `--force` 就永久消失。**
🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。本批 1 条泳道，与在跑的 `env-anchor`／`ledger-conditional`／`ff-whitelist` 合计 4 条，**正好到并行上限 4，不得再加**。

## 一、硬边界继承

- 🔴 **永不 `--force`**；git 拒了就停手记一行，不绕过。
- 🔴 **两个 error-7 的只取证、不删**（git 读不出状态，删了等于闭眼删）。
- 🔴 **先落地、后删**：内容没提成分支并 push 成功、或没写清「无价值」判据之前，**一个都不许删**。
- 🔴 **不 ff master**；**不删任何分支**；**不改 `工具-待合分支巡检.ps1`**。
- 🔴 不碰 `.51`／企微机器人／定时任务；不做任何对外发送。
- 🔴 **不立新机制行**（WIP 超上限）；确需时 🛑 起首。
- 🔴 队列回写走 `工具-共享文档编辑锁.py`，撞 `.git/index.lock` 不重试。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`，命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与其他 |
|---|---|---|---|
| `wt-harvest` | 12 个 `[WT-BLOCKED]` worktree 的收割与清理 | `.claude/worktrees/**`（读＋受限删）；新建 `1-转型规划/0-全景路线图/取证件-2026-09-12-12个worktree收割.md`；新建 `claude/op0912u-harvest-*` 分支若干 | ⚠️ **与在跑三条零重叠**，但它触碰 `.claude/worktrees/` 根——**三条在跑泳道的 worktree 也在该目录下，绝不可误删** |

⚠️ **最高优先的一条**：`.claude/worktrees/` 下同时有**本批自己与另外三条在跑泳道**的 worktree（`wt-harvest`／`env-anchor-ripen`／`ledger-conditional`／`ff-whitelist`）。🔴 **只处置派单件表格里逐名列出的那 12 个，名字不在表里的一律不碰**——按名单白名单操作，不按「扫目录」操作。

## 三、泳道 opener

### A1 · 12 个 worktree 收割再清（OP-0912-U）

粘贴端：CC ｜ 泳道：wt-harvest

```
[OP-0912-U]【CC】wt收割再清
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912u-wt-harvest`） ｜ worktree：☑（wt-harvest，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_worktree收割）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】12个worktree收割再清-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912u-wt-harvest --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912u-wt-harvest --done --batch B-0912_worktree收割 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-V]【CC】看护看护wt收割
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_worktree收割）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912V-看护看护wt收割。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 ``1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_worktree收割-2026-09-12.md`` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. 12 个逐个一行：有价值／无价值／判不准，**结论＋手段**；2. 提成的分支清单（分支名＋commit＋`git log` 实查）；3. 实际删掉哪几个、哪几个仍被拒（原文）；4. 两个 error-7 的目录取证与待定夺项；5. 顶格哨兵。
