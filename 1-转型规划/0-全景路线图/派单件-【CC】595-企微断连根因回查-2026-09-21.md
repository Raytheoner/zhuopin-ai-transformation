---
title: 派单件 · 【CC】`#595` 企微机器人断连根因回查（⑷ 已认失败）
created: 2026-09-21
派出线: 环境总线 OP-0921-A
批次: B-0921_必读件与分诊
任务类: A 类
---

# 派单件 · 【CC】`#595` 断连根因回查（§一 #595）

## 〇、现状（不必再复核，直接开工）

`#595` 的修复（`LocalOutboxConnector` 改走 outbox 中继，`decision_reminder_check.py` 不再自建连接）已于 2026-09-16 合入 master `ac9b5cd` 并重启。**⑷ 24 小时观察窗按本行自己定的判据没过**：`disconnected` **17 次 vs 前 24h 的 15 次，不降反升 2 次**。Shao Peishen 2026-09-20 答 `1a`：**认失败，不销行，回去查根因；不改判据、不据「其后逐日 6/4/2 下降」倒推销号**。

🔑 **本方先前的解释「重启当晚重连震荡」只是推测、未经验证，不得当结论用。** 本棒的活就是把它证实或证伪。

**证据源**：`5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`，筛 `"action": "disconnected"`。

## 一、方案闸（已过，照做即可）

1. **判据粒度**：只出**计数表与时刻分桶**，禁把 jsonl 正文整段贴进报告；举例最多各方向 ≤3 条原始记录。
2. **本机侧**：凡触碰 git 一律本机侧跑。读 jsonl 用 `Select-String`／python 逐行流式统计，**禁一次读进内存再打印**。
3. **引用时刻先判 UTC／本地并标基准**（jsonl 时间戳基准要当场核实并在报告里写死）。
4. **产出落点**：`1-转型规划/0-全景路线图/取证-595断连根因-2026-09-21.md`。

## 二、做什么——三个方向逐个查，每个都要给「证实／证伪／证据不足」的明确裁定

### ⑴ 重启那一下本身贡献了多少次 `disconnected`

把 **09-16 20:53 重启时刻前后各 10 分钟**的记录单独摘出来分钟级分桶。若这一窗就吃掉了大部分增量，「重启震荡」即被**证实**；若没有，即被**证伪**——必须明说是哪一种，不许含糊。

### ⑵ 未改的三个脚本是不是还在自建连接

`annual_holiday_reminder.py`／`dispatch_followup_letters.py`／`push_followup_letter.py` **本次明确未改**（行内已如实登记）。查：

- 这三个脚本各自是否仍自建长连接（读代码，定位到具体建连行号）；
- 观察窗内（09-16 20:54 → 09-17 20:54）是否恰有跟进信投递／节假日提醒触发，时刻是否与 `disconnected` 记录对齐。

若对齐 ⇒ **根因只修掉了一部分**，写清还剩哪几条路径没修、各自的改法建议（**只出建议，不动它们**）。

### ⑶ 换同时段对照重新算一次

原观察窗跨了 09-17 白天，与 09-16 夜间基线的作业量不可比。改用**同时段对照**（例：09-16 20:54–24:00 vs 09-17 20:54–24:00，以及逐日同一时段序列），重新给出「降了没有」的结论。🔴 **这是补一个可比口径，不是改原判据**——原判据按字面没过的结论**保持不变**，报告里必须把两者并列写清。

## 三、期望产出（缺一个就不许写 `OPENER_DONE`）

1. `1-转型规划/0-全景路线图/取证-595断连根因-2026-09-21.md`：三个方向各一节，每节结尾一行明确裁定（证实／证伪／证据不足＋还缺什么证据）
2. 一节「根因结论」：能定就写死根因；**不能定就明说不能定**，并列出下一步要取的证据——不许用推测充结论
3. 若得出需要改代码的结论：**只在本泳道分支上改并 push**，并在报告里写清改了什么；合入与重启须他一字母
4. 收工贴 `git diff --name-only master...HEAD` 的输出

## 四、不做什么

1. 🔴 **泳道内不重启任何服务、不向企微发任何消息**（行内硬约束）。
2. 🔴 **不动 `.51` 与生产服务**（⏭️ 档，永不代办）。
3. 🔴 **不改 ⑷ 的原判据、不据后续下降趋势倒推销号**——`#595` 本行本棒不销。
4. 🔴 不碰 `.claude/rules/`、不碰根 `CLAUDE.md`（同批另一条泳道在做减字）。
5. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
6. 🔴 收工只 push 本泳道分支，**不碰主仓、不 ff master**；合入与重启属 🟡 档，命中即以 `OPENER_PARTIAL` 停。
