---
title: "派单件 · 【CC】#443 每周清扫 ＋ K2 外置 21 行（2026-09-10）"
created: 2026-09-10
status: 待派出
派出线: Cowork 业务总线 OP-0910-A
承接: 队列 §一 `#443`（清扫正体）＋ §四 `#181`（K2 外置头条，⏰ 09-11 转阻断）
授权: Shao Peishen 2026-09-10 答 `1a，2a`
---

# 派单件 · `#443` 每周清扫 ＋ K2 外置 21 行

## 〇、本件含两棒，🔴 必须串行，顺序不可颠倒

| 棒 | OP 号 | 做什么 | 为什么是这个顺序 |
|---|---|---|---|
| **棒 1** | `OP-0910-K` | `#443` 每周清扫 | **先清扫**：37 条 `[S:done]` 行里有 **7 条正在超 4096 B**（`#483`／`#536`／`#500`／`#523`／`#537`／`#449`／§四 `#188`），**迁进归档后它们就不在真身里了，超限自动消解** |
| **棒 2** | `OP-0910-P` | K2 外置剩余 21 行 | **后外置**：先做会给这 7 条白做一次整格搬运。⏰ **09-11 起行长闸转阻断**，届时任何会话触碰超限行的 `release` 都会被拒（**连销号、补豁免都做不到**，同 §四 `#188` 实撞的死锁形态） |

🔴 **两棒都独占两份队列真身与编辑锁，不得与任何写队列的会话并行**；本日同时派出的泳道批 `B-0910_三泳道续排` 触碰区与队列真身**零重叠**，可并行，但**其收工回写会抢锁** ⇒ 本件两棒**优先**。

🔴 **他在场**（`#443` 行内硬纪律：逐行判读、他在场、判不准就停下来问）。

---

## 棒 1 · `OP-0910-K` · `#443` 每周清扫

### 一、口径（现取，2026-09-10 10:3x）

- §一 总行数 **155**；状态分布：`open` 51／**`done` 37**／`blocked` 32／`partial` 20／`hold` 6／`timed` 2。
- **清扫候选 ＝ 37 条 `[S:done]`**，但 🔴 **不是 37 条都迁**——见 §二 判据。
- 规则正本＝`0-学习与工具/skills源码/zhuopin-queue-audit/SKILL.md` §六「每周清扫尾步」，**开工前必读全文**。

### 二、🔴 三条硬纪律（`#443` 行内原文，一字不改）

1. **逐行判读、禁批量过滤** —— 2026-08-03 实测批量过滤 33/70 **既误迁也误留**。
2. **判不准就停下来问，不要猜。**
3. **前提不成立即推迟并如实登记，不降质执行。**

### 三、判据（2026-09-09 那轮清扫验证过，照用）

**迁移前逐行核这三项，任一不过即不迁：**

- **⑴ 状态列**是否**头尾一致**：头部写「🆕 立行」「待领」「🟢 可领」而状态是 `done` ⇒ **先订正头部再迁**（2026-09-09 实测 10 行属此类）。
- **⑵ 是否假 done**：状态列自陈「本行未做／留给后续／真实阻塞＝…」⇒ **改判 `partial`／`blocked`，不迁**（2026-09-09 逐行判读逮到 **3 条**：`#402`／`#408`／`#455`）。
- **⑶ 产出批次是否仍在 §二「待」**：若是 ⇒ **本轮不迁**（迁走＝产物可能从没落库）。**判据用产物在不在 master，不用 commit message grep**——sweep 的 message 取自「建议 message」列、不含批次号。

🔴 **刻意保留不迁**（规则 §六 第 2 条）：明确「无限期搁置／保留不销行」的风险行、🛑 永久关闭行、🔒 常驻台账行（`#284` 形态）。

### 四、做什么

1. 读 `zhuopin-queue-audit/SKILL.md` §六 全文 ＋ 队列 §一 `#443` 全行。
2. **acquire 编辑锁**（`--who OP-0910-K`）。🔴 **单次持锁 ≤30 分钟**（Shao Peishen 既有约束）；超时先 release、下一窗口续。
3. 🔴 **备份**：迁移前把两份真身 ＋ `跨桌任务队列-归档-202609.md` 复制到 scratchpad（2026-09-09 实撞：脚本中途崩溃致 269 行处于「已删除未归档」态，靠备份原样恢复）。
4. **逐行判读 37 条**，按 §三 分桶：可迁／订正头部后迁／改判不迁／刻意保留。
5. **迁移**：§一/§二/§三/§四 已完成行**整行**迁 `跨桌任务队列-归档-202609.md`（按月一档，编号永不复用）。
   🔴 **全内存构建 ＋ 断言校验通过才写盘；先写归档件、再写真身** —— 让失败态是「冗余」而不是「丢失」（2026-09-09 实证）。
6. **回写高水位线** ＝ 迁移**前**各分区真实最大号；**迁移与回写同一个锁窗口**。
7. **校验**：迁走行数、真身剩余行数、每条迁走的行在归档件中可找到、字节数守恒。
8. **登记 §二 批次** ＋ release。

### 五、不做什么

- 🔴 **不批量过滤**、**不猜**、**不迁 §二 未落库对应的行**。
- 🔴 **不碰 §四 `#181` 的 K2 外置**——那是棒 2 的活，本棒只管迁档。
- 🔴 **不自行 commit**（由 sweep 从 §二 取活）；撞 `.git/index.lock` 不重试、按「登记待补」写队列行。

---

## 棒 2 · `OP-0910-P` · K2 外置剩余超限行

### 一、现取清单（2026-09-10 10:3x 实测 **28 行超限**，其中 7 行 `[S:done]` 由棒 1 迁档消解 ⇒ **本棒做剩余 21 行**）

🔴 **开工前必须重跑一次全量扫描**（棒 1 迁档后清单会变；`UPS5:6` 执行前实测条款）。扫描口径：§一 状态列（第 6 格）、§四 事项列（第 2 格），UTF-8 字节数 > 4096。

**按字节降序（供排优先级，非最终清单）**：

| 行 | 字节 | 状态 |
|---|---:|---|
| §一 `#374` | 10,809 | partial |
| §一 `#505` | 9,974 | open |
| §一 `#416` | 8,835 | open |
| §一 `#507` | 8,457 | open |
| §一 `#341` | 8,276 | blocked |
| §四 `#106` | 7,194 | — |
| §一 `#482` | 6,610 | partial |
| §四 `#136` | 6,530 | — |
| §一 `#499` | 6,470 | open |
| §一 `#504` | 6,436 | partial |
| §一 `#487` | 6,408 | open |
| §一 `#466` | 5,949 | partial |
| §一 `#398` | 5,881 | blocked |
| §四 `#176` | 5,451 | — |
| §一 `#455` | 5,450 | partial |
| §四 `#177` | 5,024 | — |
| §一 `#506` | 5,012 | open |
| §一 `#419` | 4,969 | blocked |
| §一 `#394` | 4,776 | partial |
| §一 `#521` | 4,465 | partial |
| §一 `#22` | 4,243 | timed |

### 二、外置口径（K2，既有惯例，照抄不新造）

**整格原文迁 `1-转型规划/0-全景路线图/队列行日志/#N.md`，行内留「首段 ＋ 末段 ＋ 指针」**，指针写法沿用既有行（如 `#489`／`#533`）：

> 📎 **历史回写 N 段已于 2026-09-10 外置**（K2 口径，原文原样、可 grep，md5:xxxxxxxx）见 `1-转型规划/0-全景路线图/队列行日志/#N.md`；本格只留首 1 段＋末 1 段。

🔴 **外置件须记 md5 并回读核对**；🔴 **原文原样，不压缩、不改判、不修辞**——外置是搬运不是重写（2026-09-09 `#352` 那次是人工重写摘要，行内已注明「只压缩未改判」，属例外且已声明）。

### 三、🔴 两个已实撞的坑，必须避开

1. **先 append 再砍会把行推回超限**：2026-09-09 `#489` 从 3302 B 被推到 5404 B。⇒ **先外置、再在行内写指针**，不要反过来。
2. **字符数 ≠ 字节数**：中文 UTF-8 每字 3 字节。2026-09-09 曾据字符数误判「`#489` 只有 3846 B、没超限」，实测 7627 B。⇒ **一律 `len(text.encode("utf-8"))`**。

### 四、做什么

1. 🔴 **重跑全量超限扫描**（棒 1 之后清单已变），出本棒真实清单。
2. acquire 编辑锁；🔴 **单次持锁 ≤30 分钟**，按行分批、每批 release 一次。
3. 逐行外置：读整格 → 写 `队列行日志/#N.md`（记 md5）→ 回读核对 → 行内改为首段＋末段＋指针 → 回读核字节数 < 4096。
4. 每批 **登记 §二 批次**（🔴 文件清单每个路径包反引号、反引号内只放仓库根相对路径——§一 `#513` 写侧守卫）。
5. 全部完成后**重跑扫描**，确认超限行 ＝ 0，写进收工汇总。

### 五、不做什么

- 🔴 **不改任何行的判定与结论**——只搬运。
- 🔴 **不碰在跑行的语义**：`#374`／`#505`／`#507`／`#506` 等仍是活行，**外置只动存放位置，不动内容**。
- 🔴 **不自行 commit**。

---

## 〇bis、两棒共同的收工要求

**各自收工报一行**：迁走／外置了多少、剩余多少、手段是什么、有没有推迟项及理由。

🔴 **「已复核／已回读」类自陈必须同时写出用什么核的**（工具名／命令／md5）——只有动作没有手段的验证声明 ＝ 没有验证（根 `CLAUDE.md` 开篇纪律）。

🔴 **棒 2 收工时若超限行未清零，如实登记剩余清单与原因，不得为了「清零」而降质外置**（例如把不该压缩的段压掉）。
