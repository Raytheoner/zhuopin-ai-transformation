---
title: "LAN 收口批 B-0918_FI3上线 · FI3 门户页部署 .51:8097"
created: 2026-09-18
status: 🟢 无头批待起（`工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -Detach`）
产出: Cowork 业务总线 OP-0911-H
批次: B-0918_FI3上线
源: Shao Peishen 2026-09-18 上午 on-LAN 授权，答 `a`；原话「现在都是内网，先上功能，认证完善以后慢慢上」
判据正本: 3-治理与合规/.51部署标准清单-服务侧与笔记本侧-2026-08-01.md（七类坑 ＋ §五 冒烟三件套 ＋ §六 回滚 SOP ＋ §七 常驻自愈）
---

# LAN 收口批 B-0918_FI3上线（2026-09-18）

## 〇 · 前置探针（Cowork 现跑，非转述）

| 项 | 实测 |
|---|---|
| `ping 192.168.100.51` | ✅ 通 |
| 八端口 | `8090/8091/8092/8093/8094/8096` 全 **200**；**`8097` 无响应 ＝ 空闲** |
| git 对齐 | `origin/master..master` ＝ **0** |
| FI3 门户页 | 已在 master（`2f0ea72`），`pytest` 单跑 **32 passed** |

## 一 · 本批范围

**`.51` 触碰项 ＝ 1 项**（FI3 门户页部署 8097）⇒ **单泳道串行**，无并行项。

🔴 **本批不碰 `.51:8090` 网关、不动其它五个服务、不改任何既有端口。**

## 二 · 他的裁决与已知代价（逐字，不得省略）

答 `a` ＝ **走现有五服务的老形态**：独立端口 8097 ＋ 防火墙入站规则 ＋ 共享口令门禁（`#160`）。

⚠️ **与 design D7「不新起端口、走 `.51:8090` 网关反代」相反，属过渡形态**。先例＝SC2 的 8096 过渡期新端口豁免。**网关收编（决策件线③）时本端口一并回收**，届时 `/finance/fi3` 的 `required_tier` 与权限映射另判。

⚠️ **这一版是 mock 数据**——页面首屏已显著标注。真实数据要等唐燕萍回 `财务部#19`（FI3 tasks 3.3／3.5）。**本批不含发信**，对外发送永不代办。

## 三 · 三件（部署标准清单要求，逐项写死）

### 快照点（部署前必须先取，落 `reports/`）

1. `schtasks /Query /TN Fi3WebServer` —— **预期不存在**（首次部署）；存在则先停下报告，不覆盖。
2. `.51` 上 `C:\fi3\` 是否已存在、内容为何。
3. `netstat` 确认 8097 未被占用；防火墙是否已有同名规则。
4. 记录本次部署的 master sha 与 `sync-to-server.ps1` 推送产物的文件哈希（§五补充核验：用**文件哈希或进程启动时间**确认新代码确已部署，不靠"运气型未改动假象"）。

### 冒烟三件套（三项全绿才算部署完成，缺证据视同未做）

1. **`/api/ping` 200** —— 🔴 **`.51` 本机 `127.0.0.1` 与笔记本各一次**（只测本机会漏掉防火墙问题，标准清单 §三②）。
2. **关键页门禁生效** —— 未带口令访问 `/finance/fi3` 应 **302 到登录页**；带共享口令后 **200**。
3. **主流程跑通** —— `/finance/fi3` 返回内容须**含本次新增结构的关键字**（mock 标注文案 ＋ `dashboard.summarize` 的汇总字段），🔴 **不能只看 HTTP 200**。

### 回滚 SOP（部署前即在位，写下具体 TaskName）

```
schtasks /End    /TN Fi3WebServer
schtasks /Delete /TN Fi3WebServer /F
```
**门禁回滚**：清空 `.51` 上 `.env` 里对应键的值 ＋ `schtasks /End` ＋ `/Run`（恢复零鉴权原状，无需回滚代码）。
**代码回滚**：`sync-to-server.ps1` 重推上一个已验证 commit 的产物 ＋ 重启任务。

🔴 **冒烟任一项不过 ⇒ 立即按上述回滚、停链、降回留步态并注明原因；绝不带伤推进。**

## 四 · 七类坑逐条（标准清单 §三，泳道须逐项打勾回写）

① 脚本 **UTF-8 带 BOM**｜② 防火墙入站规则**显式创建、且不得限 `LocalSubnet`**｜③ 触发器 `AtStartup` ＋ SYSTEM，**不用 `AtLogOn`**｜④ **绝对路径烘焙**，不依赖运行时 PATH｜⑤ 失败自愈参数｜⑥ 共享口令门禁（`#160`）｜⑦ **凭据不入库**。

🔑 **门禁用共享口令，不新设**：`install_flask_gate` 支持 `env_var=`；泳道昨晚填的是 `FI3_GATE_PASSWORD`，**改为共享 `ZP_GATE_PASSWORD`**，与 SC8／QD-B／FI2／SC2 同一个——否则成员要记第二个口令。🔴 **口令值不入库、不打印、不进日志**；`.51` 的 `.env` 已有该键，**复用、不新设、不改值**。

📌 **绑定无需改码**：`run_fi3_web.py` 已读 `FI3_WEB_HOST`／`FI3_WEB_PORT`，部署时置 `FI3_WEB_HOST=0.0.0.0` 即可，**不改默认值**（本机跑仍只绑 127.0.0.1）。

## 五 · 常驻自愈核验（§七，"跑一次成功"不是发布条件）

服务须从**计划任务**拉起（非临时 worktree 手工跑）；触发器 `AtStartup`；有失败重启参数；🔴 **重启验证前先清理孤儿进程**——`Stop-ScheduledTask` 不级联杀子进程，残留 `python.exe` 会造成"双实例"假象。

## 六 · 泳道 opener（`-Plan` 解析段，人不碰）

🔴 本节须排在 `## 三bis` 之前（lint 形态⑥）；`### A<N>` 标题须带 `§一 #<行号>`（派出前校验靠它现查队列态）。

### A1 · §一 #615 FI3 门户页部署 .51:8097


粘贴端：CC ｜ 泳道：fi3-deploy ｜ 队列行：§一 #615 ｜ OP：OP-0918-A

```
[OP-0918-A]【CC】FI3上线8097
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0918a-fi3-deploy`） ｜ worktree：☑（op0918a-fi3-deploy，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：🔴 含 .51 生产面（部署 8097／计划任务 Fi3WebServer／防火墙入站规则／.env 只读键） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0911-H ｜ 模型：opus
读 ① `队列 §一 #615` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0918a-fi3-deploy --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0918a-fi3-deploy --done --batch B-0918_FI3上线 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

---

## 三bis · 看护者开场词（仅在退回「给他一块粘」时使用；默认走 §六 无头）

```
[OP-0918-B]【CC】看护FI3上线
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建；子泳道声明 ☑ 后由 v2.ps1 建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0911-H ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0918B-看护FI3上线。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-LAN收口批B-0918_FI3门户页上线-2026-09-18.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

---

## 七 · 红线

1. 🔴 **`.51` 一次只动一项**——本批只此一项，不顺手动别的服务、不动 8090 网关。
2. 🔴 **「已部署」必须有冒烟三件套证据**，无证据的完成声明**视同未做**（标准清单 §五 原文）。
3. 🔴 **冒烟不过即回滚停链**，失败项降回留步态并注明原因，不留半收口。
4. 🔴 **对外发送与请人动作永不代办**——不发跟进信、不联络唐燕萍。
5. 🔴 **凭据不入库**：口令值不进 git、不进日志、不进回写正文。
6. 🔴 不 ff master（🟡 档），只 push 本泳道分支；不手工 commit，走 §二 批次 ＋ `ZhuopinCommitSweep`。

## 八 · 已知会踩、不是本批的错

收工核验第三道闸会把「按协议〇 回写主仓队列行」判成 `main-leak` ⇒ 误判 FAIL（已立行 §一 `#611`）。**若泄漏文件只有队列文件或 `队列行日志/*.md`，按误判处理**——读泳道日志尾部的 `OPENER_DONE`／`OPENER_PARTIAL` 为准，不读 summary 的 Status 列。
