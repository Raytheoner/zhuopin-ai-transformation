---
title: "LAN 收口批 B-0919_Q2上线 · Q2 8D 判定门户页部署 .51:8098"
created: 2026-09-19
status: 🟢 无头批待起（`工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -Detach`）
产出: Cowork
批次: B-0919_Q2上线
源: Shao Peishen 2026-09-19 晚 on-LAN 授权，答 `1a`——比照 FI3(`#615`) 走独立端口过渡形态先上功能
判据正本: 3-治理与合规/.51部署标准清单-服务侧与笔记本侧-2026-08-01.md（七类坑 ＋ §五 冒烟三件套 ＋ §六 回滚 SOP ＋ §七 常驻自愈）＋ 精确参照先例 `4-数字员工/财务部/FI3-付款申请自动校验/{deploy,sync-to,smoke}-server.ps1`
---

# LAN 收口批 B-0919_Q2上线（2026-09-19）

## 〇 · 前置探针（Cowork 现跑，非转述）

| 项 | 实测 |
|---|---|
| `ping 192.168.100.51` | ✅ 通，6-8ms |
| 端口普查 8090-8100 | `8090-8094/8096/8097` 全 200（既有六服务）；`8095` 404（有服务但非本形态）；**`8098` 无响应＝空闲** |
| git 对齐 | `master`==`origin/master`（当前 `1054716f`） |
| `C:\q2\`（服务器侧） | 未探（笔记本无法直连 `.51` 文件系统；由泳道 `ssh supplychain-server "Test-Path C:\q2"` 现取，预期不存在） |
| `schtasks /Query /TN Q2WebServer`（笔记本侧，仅供参考） | 本机不存在（部署目标是 `.51`，笔记本侧本就不该有） |
| Q2 场景本地回归 | `pytest tests/` **19 passed**（1.55s），commit `1054716f`（`#612` §5.1 已 ff） |
| 门禁代码 | `webapp.py` 已接 `install_flask_gate(service_name=..., exempt_paths=(ROUTE_PREFIX+"/api/ping",))`，走默认 `ZP_GATE_PASSWORD`，**代码侧无需改动** |

## 一 · 本批范围

**`.51` 触碰项 ＝ 1 项**（Q2 8D 判定门户页部署 8098）⇒ **单泳道串行**，无并行项。

🔴 **本批不碰 `.51:8090` 网关、不动其它六个既有服务、不改任何既有端口。**

## 二 · 他的裁决与已知代价（逐字，不得省略）

答 `1a` ＝ **比照 FI3 `#615` 走独立端口过渡形态**：独立端口 8098 ＋ 防火墙入站规则 ＋ 共享口令门禁（`#160`）。

⚠️ **`scripts/run_q2_web.py` 与 `design D7` 原文写的是"唯一走 `.51:8090` 网关反代，本页未获 FI3 式改判"——本次裁决即补上这个改判，需在部署时把该处注释/design 记录同步更新为"已获改判"，口径与 FI3 `#615` 完全对称（先例注释见 `run_fi3_web.py`）**，不得留着"未获改判"的过期字样。网关收编（决策件线③）时本端口一并回收。

⚠️ **这一版是档 1 mock 数据**——页面首屏已显著标注，规则版本 `q2-v3.2-chenchen-2026-08-28+signoff-2026-09-14`。真实数据要等 §4 语义层签认（`PENDING.SEMANTIC_LAYER_ACCEPTANCE`，另案，本批不做）。**本批不含发信、不联络陈忱**，对外发送永不代办。

## 三 · 三件（部署标准清单要求，逐项写死；精确抄 FI3 三脚本改参数，勿另起结构）

### 快照点（部署前必须先取，落 `reports/`）

1. `ssh supplychain-server "schtasks /Query /TN Q2WebServer"` —— **预期不存在**（首次部署）；存在则先停下报告，不覆盖。
2. `ssh supplychain-server "Test-Path C:\q2"` —— 预期 `False`。
3. `.51` 本机 `netstat` 确认 8098 未被占用；防火墙是否已有 `Q2-WebServer-8098` 同名规则（预期无）。
4. 记录本次部署的 master sha（`1054716f` 或 sync 时的最新值）与 `sync-to-server.ps1` 推送产物的 `webapp.py` SHA256（§五补充核验：用文件哈希或进程启动时间确认新代码确已部署）。

### 冒烟三件套（三项全绿才算部署完成，缺证据视同未做；精确抄 `smoke-server.ps1`，替换：`$Port=8098`／`$Prefix="/quality/q2"`／`$EnvFile="C:\q2\.env"`／task 名 `Q2WebServer`／webapp 路径 `C:\q2\app\q2_8d_verdict\webapp.py`）

1. **`/quality/q2/api/ping` 200** —— `.51` 本机 `127.0.0.1` 与笔记本各一次。
2. **关键页门禁生效** —— 未带口令访问 `/quality/q2/` 应 **302**；带共享口令后 **200**。
3. **主流程跑通** —— 页面须含：`（档 1 · mock）`、`本页数据为 <b>mock</b>`、`并非真实 8D 评审结论`、规则版本 `q2-v3.2-chenchen-2026-08-28+signoff-2026-09-14`、`<th>维度</th>`／`<th>分级</th>` 两张表的表头，**不能只看 HTTP 200**。

### 回滚 SOP（部署前即在位）

```
ssh supplychain-server "schtasks /End /TN Q2WebServer ; schtasks /Delete /TN Q2WebServer /F ; Remove-NetFirewallRule -DisplayName Q2-WebServer-8098"
```

🔴 **门禁回滚＝不适用**：本页复用 SC8／QD-B／FI2／FI3／SC2 共享的 `ZP_GATE_PASSWORD` 键，**不新设、不清空**（清空会连带打掉另五个已在线服务，FI3 旧版回滚 SOP 那句"清空 `.env` 对应键"仅适用于服务专属键，本批禁止照抄那一条）。回滚只需停任务＋删任务＋撤防火墙规则，`C:\q2\` 目录可留作下次部署基底不必删。

🔴 **冒烟任一项不过 ⇒ 立即按上述回滚、停链、降回留步态并注明原因；绝不带伤推进。**

## 四 · 七类坑逐条（标准清单 §三，泳道须逐项打勾回写）

① 三份新脚本 **UTF-8 带 BOM**｜② 防火墙入站规则**显式创建、且不得限 `LocalSubnet`**（`RemoteAddress Any`，同六服务惯例）｜③ 触发器 `AtStartup` ＋ SYSTEM，**不用 `AtLogOn`**｜④ **绝对路径烘焙**进 `start-q2.ps1`（venv python 绝对路径，不依赖运行时 PATH）｜⑤ 失败自愈参数（`RestartCount 3 / 1 分钟`，同 FI3）｜⑥ 共享口令门禁——**`ZP_GATE_PASSWORD` 从同机既有服务（建议 `C:\fi3\.env`，今日刚部署、值必新鲜）借值写入 `C:\q2\.env`，值不出机、不打印、不进日志、不进回写正文**｜⑦ **凭据不入库**（`.env`／`data/mock`／`data/rules` 走部署，`tests/`／`reports/`／真实件不上服务器，同 FI3 mock-only 红线）。

📌 **绑定无需改码**：`run_q2_web.py` 已读 `Q2_WEB_HOST`／`Q2_WEB_PORT`，部署时在 `start-q2.ps1` 里置 `Q2_WEB_HOST=0.0.0.0`／`Q2_WEB_PORT=8098` 即可，**不改默认值**（本机跑仍只绑 127.0.0.1）。

📌 **需同步推送的数据**：`data/rules/`（V3.2 判据）＋ `data/mock/samples.json`（11 份合成样本）——均为合成/公开数据，`.gitignore` 的 `reports/`／`results/`／`data/golden/` 不推。

## 五 · 常驻自愈核验（§七，"跑一次成功"不是发布条件）

服务须从**计划任务**拉起（非临时手工跑）；触发器 `AtStartup`；有失败重启参数；🔴 **重启验证前先清理孤儿进程**——`Stop-ScheduledTask` 不级联杀子进程，残留 `python.exe` 会造成"双实例"假象（同 FI3 教训）。

## 六 · 泳道 opener（`-Plan` 解析段，人不碰）

🔴 本节须排在 `## 三bis` 之前（lint 形态⑥）；`### A<N>` 标题须带 `§一 #<行号>`。

### A1 · §一 #612 Q2 8D 判定门户页部署 .51:8098

粘贴端：CC ｜ 泳道：q2-deploy ｜ 队列行：§一 #612 ｜ OP：OP-0919-O

```
[OP-0919-O]【CC】Q2上线8098
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919o-q2-deploy`） ｜ worktree：☑（op0919o-q2-deploy，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：含 .51 生产面（部署 8098／计划任务 Q2WebServer／防火墙入站规则／.env 借值门禁键） ｜ session：新开 ｜ 派出线：Cowork LAN 收口 B-0919_Q2上线 ｜ 模型：opus
读 ① `1-转型规划/0-全景路线图/看护件-LAN收口批B-0919_Q2上线-2026-09-19.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 精确参照 4-数字员工/财务部/FI3-付款申请自动校验/{deploy,sync-to,smoke}-server.ps1 三份，逐字复制后只改参数（端口8098/前缀quality-q2/任务名Q2WebServer/Base C:/q2/SIBLING_ENV C:\fi3\.env），新建到 4-数字员工/质量部/Q2-8D报告AI判定/
2. 同步把 run_q2_web.py 与 openspec/changes/q2-8d-review-verdict-mvp/design.md D7 段里未获改判字样改为已获1a改判，tasks.md 5.2 打勾
3. 按看护件§三顺序执行：快照点→本地pytest 19 passed回归→部署→冒烟三件套→回写队列#612证据
4. 冒烟三项全绿才算完成，缺证据视同未做

不做什么：
- 冒烟任一项不过时带伤推进，须立即按§三回滚SOP执行并把行状态降回留步态
- 清空共享ZP_GATE_PASSWORD键（只借值写入C:\q2\.env，不改其余五服务的.env）
- ff master、手工commit（只push本泳道分支，走§二批次+ZhuopinCommitSweep）
- 发送跟进信或联络陈忱（对外发送永不代办）
- 翻动#612既有的§4语义层与§5.3跟进信销号判据
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919o-q2-deploy --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919o-q2-deploy --done --batch B-0919_Q2上线 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```



---

## 七 · 红线

1. 🔴 **`.51` 一次只动一项**——本批只此一项，不顺手动别的服务、不动 8090 网关。
2. 🔴 **"已部署"必须有冒烟三件套证据**，无证据的完成声明**视同未做**。
3. 🔴 **冒烟不过即回滚停链**，失败项降回留步态并注明原因，不留半收口。
4. 🔴 **对外发送与请人动作永不代办**——不发跟进信、不联络陈忱。
5. 🔴 **凭据不入库**：口令值不进 git、不进日志、不进回写正文；仅借值不新建键。
6. 🔴 不 ff master（🟡 档），只 push 本泳道分支；不手工 commit，走 §二 批次 ＋ `ZhuopinCommitSweep`。
