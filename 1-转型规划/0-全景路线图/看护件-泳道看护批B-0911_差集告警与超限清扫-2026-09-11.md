---
title: "看护件 · 泳道看护批 B-0911_差集告警与超限清扫（2026-09-11）"
created: 2026-09-11
status: 待派出
批次: B-0911_差集告警与超限清扫
派出线: Cowork 业务总线 OP-0910-A
---

# 看护件 · 泳道看护批 `B-0911_差集告警与超限清扫`

## 〇、口径

- **触发**：Shao Peishen 2026-09-11 05:5x 答 `1a，2a`（`#554` 走 sweep 路线立即派；10 行超限行派 K2 清扫）。规则正本＝`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（v2.2）。
- **D1 四档现取**（`工具-泳道看护状态机.py criteria`，非手抄）。**判据未覆盖按 🟡 停。**
- **前置四查（2026-09-11 05:5x 本机现取）**：① LAN 探针 ＝ **⛔ off-LAN**；② `git fsck` 健康、master ＝ origin/master；③ 编辑锁**无占用**；④ 上一批 `B-0910_夜批六泳道` 波 1 汇总已出（3 OK／1 PARTIAL／转出 1），波 2 `513` 已收工、`534` 在跑。
  > 🔴 **起棒时须自己重跑一次**——本日 master 已在数小时内走过 `ed92ed3`→`4a5c113`→`5b72a34`→`945435b`→`0aef9e2`（同 §一 `#522` 现取表过期守）。
- **上限**：默认 10；本批实际入批 **2 条泳道**。
- **交付模式**：🔴 **无头**（`-Detach`）——他不在 CC 前；同前一批口径。

## 一、硬边界

1. 🔴 **本批与在跑的 `534-patchid`（批 `B-0910_夜批六泳道` 波 2）并行**。触碰区比对：`534` 碰 `工具-泳道看护状态机.py`＋新建比对工具；本批 `554` 碰 `工具-落库sweep.py`、`k2-sweep` 碰两份队列真身与 `队列行日志/` ⇒ **代码面零重叠**。🔴 **但 `534` 收工回写会抢编辑锁** ⇒ `k2-sweep` 泳道 acquire 被拒时**按「登记待补」写 `队列回写待补/*.json`，不重试、不绕锁**。
2. 🔴 **⏭️ 档（`.51` 部署及触碰生产服务）本包不执行**——`#554` 的 listener 路线属此档，**本批明令不走**（见 §二bis）。
3. 🔴 **🔴 档（对外发送／L2 门禁／合规红线／ASIL C-D）永不代办**。
4. 🔴 **看护会话不修工具**：跑批期间发现机制缺陷只登记＋出派单。
5. **看护件落档失败即全停。**

## 二、次序与并行矩阵

**波 1（并行 2，代码面零重叠）**

| 泳道 | 承接 | 触碰区 |
|---|---|---|
| `554-intake-gap` | §一 `#554` 期望产出 ⑴ 的 **sweep 路线** | `0-学习与工具/工具-落库sweep.py`（新增一类常驻告警）＋其单测 |
| `k2-sweep-overlimit` | ⏰ **行长闸 2026-09-11 起转阻断**（今日生效）；§一 10 行超限 | 两份队列真身的 §一 相应行 ＋ `1-转型规划/0-全景路线图/队列行日志/`（只追加）；用 `0-学习与工具/工具-队列行K2外置.py` |

🔴 **两条不得互换先后、也不必串行**：`554` 只改 sweep 代码不写队列；`k2-sweep` 只写队列不改代码。

## 二bis、每条泳道的「做什么／不做什么」

### `554-intake-gap`（§一 `#554`，🔴 **只走 sweep 路线**）

**背景（开工前必须读懂，否则会修错地方）**：拆件巡逻已**正式下线**，接替它的事件驱动机制触发点在 `5-平台底座/wecom-aibot-service/aibot_service/followup_readme_bridge.py::_raise_patrol_signal`——**只在「在途信件收到回件」被标第九态时落信号**。⇒ **差集 ＝ 专员主动发言 ＋ 对已闭环信件的补发**，这部分**没有任何消费者**。

**定量输入（2026-09-10 夜批 `quality-intake-13` 泳道实测）**：陈忱当日五件（两条无编号文本反馈 ＋ 三个针对已闭环 `质量部#13` 的补发 zip）**5/5 全部落在差集**。**不是边缘个例。**

**做什么**
1. 在 `工具-落库sweep.py` 新增**一类常驻状态告警**：**§一 里「待领的企微反馈自动归档行」存在且超 N 小时无人拆 ⇒ 告警**。N 由建造方定初值并在行内写明依据（🔴 阈值类，**不设默认生效**，须 Shao Peishen 明确答复；建造不因此停）。
2. 🔴 **类号顺延**：第 14 类已被 `#551` 旁生占用（定时任务目录未分类），**开工先现取当前最大类号**，勿撞。
3. 🔴 **零命中也回显**（同第 4/6/7/9/14 类惯例）——一个从来不出声的机制，没人能判断它是「没问题」还是「没跑」。
4. 走 `_track_and_alert_standing_state` 的「出现→告警／消失→解除」骨架，**不新造通知路径**；企微目标复用既有 OPS 常量，**不出现 webhook 键名字面量**（`#492`）。
5. 单测 ＋ 🔴 **变异检验**：把判读段注释掉，确认用例真的转红。
6. 🔴 **实弹**：造一条假的「待领归档行」跑一次，确认**告警真发出来**、删掉后**解除通知也真发出来**（`#398`：只跑单测证明不了 webhook 那一段；2026-09-10 第 14 类就是这么验的）。

**不做什么**
- 🔴 **不碰 `5-平台底座/wecom-aibot-service/`** ——listener 路线属 ⏭️ 档（触碰生产常驻服务），**本批明令不走**；若判定非改它不可，跑 `transfer-out` 标注去向后**跳过该项**，不进 `OPENER_PARTIAL`。
- 🔴 **不重开 `huijian-chaijian-patrol` 定时任务**——它**已正式下线，本就不该开**（Shao Peishen 2026-09-10 明确更正过本方的误判）。
- 🔴 **不改事件驱动的既有触发条件**（那是改口径，🟡）。
- 🔴 不自行 commit；不 ff master。

### `k2-sweep-overlimit`（⏰ 今日转阻断）

**背景**：行长闸的阻断日期 **2026-09-11 今日到期**（`test_工具-共享文档编辑锁.py` 两条 `test_row_length_*` 用例今日起转红即其证据）。⇒ **超限行从今天起谁也动不了**：销号、改状态、甚至补豁免，release 都会被拒（`#188` 实撞过的死锁形态）。

**现取清单（2026-09-11 05:5x，🔴 开工须自己重扫，勿照抄）**

| 文件 | 行与字节 |
|---|---|
| `跨桌任务队列-机制环境.md` §一 | `#282` 8053／`#284` **13990**／`#312` 8598／`#324` 4897／`#366` 8389／`#381` 7879／`#416` 5276／`#537` 4411 |
| `跨桌任务队列-业务场景.md` §一 | `#372` 9966／`#373` 8183 |

**做什么**
1. 🔴 **重扫全库出真实清单**（本表可能已变），按字节降序排优先级。
2. 逐行用 **`0-学习与工具/工具-队列行K2外置.py`**（2026-09-10 刚进 master，`513` 泳道已首次真用）：先不带 `--apply` 看段表 → 确认无误后 `--apply --expect-segments N --who OP-0911-B` → 持锁 `edit-row` 落格 → `--verify-json` 反查。
3. 🔴 **单次持锁 ≤30 分钟**，按行分批、每批 release 一次并登记 §二。
4. 🔴 **保留段的选法**：默认 `--keep 1,-1`（首段带 `[S:]/[D:]` 机器字段，**不能整段外置**）；若首段自身过长导致改后仍超闸，**如实登记该行做不了并说明原因**——⚠️ **这正是该工具已知的一处形态缺口**（`#538` 昨夜实撞：唯一首段 3942 B，`--keep` 给不出「保留首段但拆开它」这个动作），**不要硬凑，不要自己写一次性脚本绕过**。
5. 全部完成后**重扫**，把「超限行 = N」写进收工汇总。

**不做什么**
- 🔴 **不改任何行的判定与结论**——K2 是**搬运**，不压缩、不改判、不修辞。
- 🔴 **不碰他人在办行的语义**；`#416`／`#537` 等仍是活行，外置只动存放位置。
- 🔴 **不给任何行写「行长豁免」来绕过**——除非该行确实做不了（见上第 4 条），且须写明理由。
- 🔴 不自行 commit。

## 三、泳道 opener（由 `工具-opener生成.py --variant subtask_lane` 产出，未手抄）

### A1 · #554 差集告警（sweep 路线）

粘贴端：CC ｜ 泳道：554-intake-gap

```
[OP-0911-A]【CC】554差集告警
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911a-554-intake-gap`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0911_差集告警与超限清扫）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0911A-554差集告警。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0911_差集告警与超限清扫-2026-09-11.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. …

不做什么：
- …
```

### A2 · 10 行超限行 K2 清扫（⏰ 今日转阻断）

粘贴端：CC ｜ 泳道：k2-sweep-overlimit

```
[OP-0911-B]【CC】K2清10超限行
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911b-k2-sweep-overlimit`） ｜ worktree：☐（不建；独占队列真身，须在主 checkout 上做） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0911_差集告警与超限清扫）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0911B-K2清10超限行。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0911_差集告警与超限清扫-2026-09-11.md 全文（尤其 §二bis 本泳道那一格）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. …

不做什么：
- …
```

## 四、收工汇总

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0911_差集告警与超限清扫` **现取**，不手工数。

🔴 **心跳跑命令写**，收工带 `--done --batch B-0911_差集告警与超限清扫`。**别传 `--repo-root` / `--heartbeat-file`，它没有这两个参数。**

🔴 **等待时长只认 `reports/lane-watch-state.json`，会话不得自算。**

### 本批收工必须一并注明的两件

1. `554-intake-gap`：🔴 **本泳道只补了「让人知道」，没有补「自动拆」**——差集里的件仍然要人（或另一棒）去拆。**收工不得表述为「差集已修复」。**
2. `k2-sweep-overlimit`：收工写明**重扫后超限行 = N**；若有行因工具形态缺口做不了，**逐行点名并写明原因**，不得含糊成「已尽量处理」。
