---
title: "Q2 建造续棒看护件 · 红线转人工与 PPT 勾选解析 ＋ 门户页 5.1"
created: 2026-09-19
status: 🟢 无头批待起（`工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -Detach`，待 Shao Peishen 一字母授权）
产出: Cowork 泳道看护 OP-0919-A
批次: B-0919_Q2建造
源: 派单件 `1-转型规划/0-全景路线图/派单件-泳道看护续棒-2026-09-19.md`（OP-0919-A）＋ 他 2026-09-19 追认「Q2 8D design 审 D3/D4，通过」
用途: 一条泳道内两段串行；§三 即 `-Plan` 解析格式，默认后台无头起、不给人粘。
---

# Q2 建造续棒看护件（2026-09-19）

## 〇 · 本批要解决的一句话

**Q2 的 design 审今天过了，闸解除了——但引擎侧「红线②③ 转人工」和「PPT 场景勾选」这两条已签认口径还停在纸面，专员也还看不见任何页面。**

- `#612` design 审 D1–D8 整表通过（2026-09-18 答 `1a`／`2a`，他 2026-09-19 追认 D3/D4），tasks §3 收口闸解除；档 1 mock 已在 master（`4-数字员工/质量部/Q2-8D报告AI判定/`，19 个文件，泳道自陈 `pytest 12 passed`）。
- `#592` 的口径侧已于 2026-09-17 全部回灌（台账 `Q2-Q14-01`～`09` 转 `已回灌`），但 ⑵⑷ 两项**建造侧一行代码都没有**，行内明写「待派 CC」。
- FI3 走的是同一条路：档 1 → 门户页 5.1 → `.51` 上线（`#615` 已 `[S:done]`，8097）。**Q2 照搬这条路，样板在库内。**

⇒ **A1 兑现 `#592` ⑵⑷ 两条已签认口径｜A2 出 Q2 门户页 5.1，让质量域本周有东西可看。**

---

## 一 · 硬边界继承（看护者读，泳道 opener 不复述）

- D1 四档判据**现取**：`python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不抄判据表。
- 本批**全部为 🟢 档**（worktree 内本地建造）。命中 🟡/🔴 即按 skill 正本跑 `pause` 并以 `OPENER_PARTIAL` 收尾；命中 ⏭️（`.51`）跑 `transfer-out`、跳过该项继续其余。
- 机制类可动 WIP 现取 **29／22（超限）**——本批两条均为 `[D:业]`，不受该上限约束（协议〇.9 措施 C）。
- LAN 探针现取 **off-LAN**（`lan-status` ⛔ 三项未齐）⇒ 依赖内网的活一律不入本批。

## 二 · 次序与并行矩阵

| 段 | 泳道 | 队列行 | 触碰区 | 硬序 |
|---|---|---|---|---|
| A1 | `q2` | §一 `#592` | `4-数字员工/质量部/Q2-8D报告AI判定/q2_8d_verdict/`（`redlines.py`／`config.py`／`engine.py`）＋`tests/`＋`pyproject.toml`；`4-数字员工/质量部/QD-A-8D不良分析/`（PPT 勾选解析） | 先 |
| A2 | `q2` | §一 `#612` | `4-数字员工/质量部/Q2-8D报告AI判定/`（新增 `webapp.py`／`scripts/run_q2_web.py`／`tests/test_webapp.py`）＋`pyproject.toml`＋`openspec/changes/q2-8d-review-verdict-mvp/tasks.md` §5.1 | 后 |

🔴 **两段同属泳道 `q2`、按 `A<N>` 编号升序串行**——同触碰目录、同 `pyproject.toml`，并行必撞。并行上限 4，本批只用 1 条泳道。

**与他人在办行的重叠核查（`--digest` ＋ 触碰区关键词，2026-09-19 现取）**：`#538`／`#539` 同触碰 SC2 周报页面且 `#538` 在办 ⇒ `#539` **本批不领**，报总线裁决；`#603` 领取方 `CC-OP-0917-W1` 标「已认领，开工」⇒ **不代关**，只回写取证。

**「待回内网」清单（off-LAN 留步，本批不做）**：
1. `#612` §5.2 `.51` 部署＋冒烟三件套＋回滚 SOP（⏭️ 档，转 `zhuopin-lan-closeout`）。
2. `#612` §4.2 验收集 10 份校准、§4.4 接 QD-A 真实解析＋PPT D2 页勾选（须陈忱 10 份原件，真实件不入库）。
3. `#538` `.51` 部署与线上 187 行比对。

---

## 三 · 泳道 opener（`工具-opener批处理执行v2.ps1 -Plan` 解析段，人不碰）

🔴 **本节必须排在 `## 三bis` 之前**（`工具-opener块lint.py` 形态⑥ 的结构锚）。
🔴 **`### A<N>` 标题必须带 `§一 #<行号>`**（`工具-opener派出前校验.ps1` 靠它现查队列态）。

### A1 · §一 #592 Q2 红线②③ 转人工与 PPT 勾选解析

粘贴端：CC ｜ 泳道：q2 ｜ 队列行：§一 #592 ｜ OP：OP-0919-B

```
[OP-0919-B]【CC】Q2红线与PPT
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919b-q2-redline-ppt`） ｜ worktree：☑（op0919b-q2-redline-ppt，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A（批 B-0919_Q2建造） ｜ 模型：sonnet
读 ① `队列 §一 #592` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919b-q2-redline-ppt --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919b-q2-redline-ppt --done --batch B-0919_Q2建造 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · §一 #612 Q2 门户页 5.1

粘贴端：CC ｜ 泳道：q2 ｜ 队列行：§一 #612 ｜ OP：OP-0919-C

```
[OP-0919-C]【CC】Q2门户页
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919c-q2-portal`） ｜ worktree：☑（op0919c-q2-portal，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A（批 B-0919_Q2建造） ｜ 模型：sonnet
读 ① `队列 §一 #612` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919c-q2-portal --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919c-q2-portal --done --batch B-0919_Q2建造 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

---

## 三bis · 看护者开场词（仅在退回「给他一块粘」时使用；默认走 §三 无头）

```
[OP-0919-E]【CC】看护Q2建造
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建；各子泳道声明 ☑ 后由 v2.ps1 脚本在起 claude 前建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A（批 B-0919_Q2建造） ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0919E-看护Q2建造。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】Q2建造续棒-2026-09-19.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

---

## 四 · 全程红线与收工

1. 🔴 不碰 `.51`、不部署、不做 `#612` §5.2（⏭️ 档，须走 `zhuopin-lan-closeout` 的快照→部署→冒烟→不过即回滚串行纪律）。
2. 🔴 不代填判据默认值——`PENDING` 两条（措施可验证性标准／语义层上线签认）读即抛，缺签认必须**显式失败**。
3. 🔴 红线②③ 本批**不验收**，只做「P1 全转人工」——不得因为实现了检查器就顺手打开自动判定（判例 11／J8 甲）。
4. 🔴 判例 6 安全相关件一律转人工；判例 9 结构性退回先于红线语义判定，顺序不得颠倒。
5. 🔴 真实 8D 原文一律不入 git（QD-A CLAUDE.md 红线 1：`data/golden/*`、`results/` 全 gitignore）。
6. 🔴 不代指派持有人／backup；不代发信、不代联络陈忱；`质量部#15` 本批不起草。
7. 🔴 不 ff master（🟡 档），只 push 本泳道分支；不在主仓 commit，走 §二 批次 ＋ `ZhuopinCommitSweep`。
8. 🔴 门户页首屏须**显式标注数据为 mock**——2026-07 `:8092` 静态原型用虚构占位数据误导过专员，同一个坑不踩第二次。

**收工汇总**：每泳道一行 ＋ 心跳全文 ＋ D6 停点列 ＋ 转出项 ＋「待回内网」清单，跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0919_Q2建造` 现取，**不手工数**；等待时长只认 `reports/lane-watch-state.json`。

## 五 · 已知会踩、但不是本批的错

**收工核验第三道闸会把「按协议〇 回写主仓队列行」判成 `main-leak` ⇒ 误判 FAIL**（白名单只有 `reports/` 与 `合入登记/pending-ff.jsonl`）。已立行 §一 `#611`。⚠️ 本批若报 `FAIL(main-leak)` 且泄漏文件只有队列文件，**按误判处理**——读泳道日志尾部的 `OPENER_DONE`／`OPENER_PARTIAL` 为准，不读 summary 的 Status 列。

**`-Detach` 继承调用方 shell**（§一 `#567`）：起跑必须用 `pwsh`（本机唯一一份 `C:\Tools\pwsh7\pwsh.exe`，7.6.6 实测），**不得用 `powershell`（PS 5.1）**，否则子进程当场 `NamedParameterNotFound`、零泳道日志、`exit.txt` 不落，而父进程照报 `✓ 已后台起`。不传 `-LogDir`（语义名批目录会掉进 `#571` 探针盲区）。
