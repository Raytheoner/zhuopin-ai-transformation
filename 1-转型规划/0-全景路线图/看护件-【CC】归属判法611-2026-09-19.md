---
title: "第三道闸泄漏归属判法看护件 · #611 最后一条销号判据"
created: 2026-09-19
status: 🟢 待起（Shao Peishen 2026-09-19 答 `1a` ＝ 按泳道 worktree 实际触碰过的文件归属）；**在跑的 B-0919_解冻三条推进 收工后起**，避免四条并行挤满上限
产出: Cowork 泳道看护 OP-0919-K
批次: B-0919_归属判法611
源: 他 2026-09-19 答 `1a`（本轮第二问）
用途: 单泳道单段；§三 即 `-Plan` 解析格式，默认后台无头起、不给人粘。
---

# 第三道闸泄漏归属判法看护件（2026-09-19）

## 〇 · 本批要解决的一句话

**白名单治不了「取证件名是任意的」**——方案 D 已把 13 份补丁做到 12 份转绿，剩下那 1 份（`20260919-075655/q2-A1`）泄漏清单只有看护者自己写的取证件。他 `1a` 定：**改按「该泳道 worktree 实际触碰过的文件」归属**，不按 git author／时间窗（看护者与泳道同身份同时段，切不干净）。

## 一 · 硬边界继承（看护者读，泳道 opener 不复述）

- D1 四档判据**现取**：`python 0-学习与工具/工具-泳道看护状态机.py criteria`。
- 本段 🟢 档（worktree 内改工具＋单测）；形态已由他 `1a` 拍定，无待决项。
- 机制类可动 WIP 现取 **16／22**，在限内。`#611` 是 `[D:业]` 行，不占机制配额。

## 二 · 次序与并行矩阵

| 段 | 泳道 | 队列行 | 触碰区 | 硬序 |
|---|---|---|---|---|
| A1 | `attribution` | §一 `#611` | `0-学习与工具/工具-opener批处理执行v2.ps1`（L384–L419 第三道闸）｜`0-学习与工具/test_工具-opener批处理执行v2.py` | 单段 |

⚠️ **起跑前置**：`B-0919_解冻三条推进`（三条泳道）收工后再起——并行上限 4，且本段与 `#620`／`#565`／`#567` 同碰 v2.ps1，起前须现取确认无第二条泳道在改它。

## 三 · 泳道 opener（`工具-opener批处理执行v2.ps1 -Plan` 解析段，人不碰）

🔴 本节必须排在 `## 三bis` 之前（形态⑥ 结构锚）；`### A<N>` 标题须带 `§一 #<行号>`。

### A1 · §一 #611 第三道闸泄漏归属改按 worktree 触碰面

粘贴端：CC ｜ 泳道：attribution ｜ 队列行：§一 #611 ｜ OP：OP-0919-Q

```
[OP-0919-Q]【CC】泄漏归属判法
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919q-leak-attribution-611`） ｜ worktree：☑（op0919q-leak-attribution-611，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-K（批 B-0919_归属判法611） ｜ 模型：sonnet
读 ① `队列 §一 #611` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 第三道闸改按「该泳道 worktree 实际触碰过的文件」归属泄漏：收工核验时只把泳道 worktree 里动过的路径算进它的泄漏清单，看护者同时段在主仓写的文件天然不计（Shao Peishen 2026-09-19 答 1a，否决了按 git author／时间窗切的方案 b——看护者与泳道同身份同时段，切不干净）
2. 与已合入的方案 D 白名单叠加、不互斥：先按归属过滤，再过白名单
3. 单测：造「看护者在泳道运行期间写了一个任意命名的取证件」的场景，断言不判 FAIL；并造「泳道真的写了主仓非白名单文件」的场景，断言仍判 FAIL（防判据过宽）
4. 自举验收：python 0-学习与工具/工具-main-leak回放.py 之外，另跑一次针对 20260919-075655/q2-A1 的归属判定，必须由 FAIL 转绿——这是 #611 最后一条销号判据

不做什么：
- 🔴 不动方案 D 那三类白名单（已 ff 入 master 09b153d0）
- 🔴 不做按 git author／时间窗归属（他已否）
- 🔴 不碰 .51／真实库／企微／定时任务
- 🔴 不 ff master、不在主仓 commit、不长持队列编辑锁
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919q-leak-attribution-611 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919q-leak-attribution-611 --done --batch B-0919_归属判法611 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词（仅在退回「给他一块粘」时使用；默认走 §三 无头）

默认后台无头起。退回手工走 `python 0-学习与工具/工具-opener生成.py --variant reference --ref-file <本件路径>` 现出，**不手抄**。

## 四 · 全程红线与收工

- 🔴 **销号判据＝`20260919-075655/q2-A1` 由 FAIL 转绿，且「泳道真写了主仓非白名单文件」的场景仍判 FAIL**（防判据过宽）。两条缺一不算。
- 🔴 不动已合入的方案 D 三类白名单；不 ff master、不在主仓 commit。
- 收工登记 §二 `B-0919_归属判法611`，回写 `#611`，顶格 `OPENER_DONE` 收尾。**本条过了 `#611` 才谈销号。**

## 五 · 已知会踩、但不是本批的错

- 泳道收工常不自删 worktree（`#576`）⇒ 看护者 ff 前先按协议〇.5 清（`Remove-Item -Recurse -Force`＋`prune`；若 prune 报 `Permission denied`，先删 `.git/worktrees/<名>/fsmonitor--daemon`）。
