---
status: 在办
---

# 拆件巡逻报告 · 2026-09-10（第一班 · 事件驱动）

**〇ter 探测**：`[SIGNAL]`，待处理 1 条 —— `采购部#22`，`CHECKPOINT=2026-09-10T00:22:18Z`。按 §一~§四 全套执行；信号在本报告登记 §二 之后才消费。

---

## 拆件 1 · `采购部#22` 回件（姚祖怡 · 男，2026-09-10 08:22 落档）

- **行号**：§一 `#541`（bot 追行 `00:22:17Z` 遇锁忙滞留 8.5 小时，本班手动 flush 后才成行——成因见自检末段），已当场销号。
- **内容摘要**：三件——① 六项新增指标的三处口径判例批改（唯一要他动笔的）；② D-IT11 正式作废（知会）；③ 周报页面期次已修复，请他试用。
- **已执行动作**：⑴ 全文实读（python-docx 按 `w:body` 原序遍历 `w:p`＋`w:tbl`，43 段正文＋4 张表全取；`word/comments.xml` **不存在** ⇒ 本次无批注夹带）；⑵ 内嵌图 `word/media/image1.png`（46,761 B，`r:embed="rId8"` 在 `document.xml` 正文、非页眉）解出并实看；⑶ 回灌 §一 `#499`（口径侧仍未解）与 §一 `#506`（试用已实际发生）；⑷ 新立 §一 `#538`／`#539`；⑸ 跟进信 README 转第七态。
- **判断依据**：**判例批改表七行的 ✅／❌／✏️ 三列逐格实读全空** ⇒ 三处口径**一处未答**（守 §二 硬约束②：看字符不看列名）。他建议的 09-16 未到，**不算逾期，不得写成「他没回」**。他改在第三节自由文本里写了两条**新问题**（守 §二 硬约束①：推翻性内容在表格之外）：
  - **问题2（🔴）**：「你的周报数据和我线下计算的数据无法对起来，存在差异较大……把你的计算的过程明细列出来，例如你周报中下单行数是187行，你把这187行的明细列出来反馈给我」⇒ **这是对已上线三节的正确性质疑，不是新指标需求**。实测 `sc2/webapp.py` 无 `csv`／`export`／`明细`／`detail` 任何命中 ⇒ 明细导出能力为零、须新建；底数已有（`sc2/metrics.py:137` 的 `order_line_count`，行级来源 `dataset.order_lines`，`metrics.py:215-219`）。→ 立 §一 `#538`。
  - **问题1（P2）**：页面排版不友好，「可以参考以下：」后接图。图内表头＝料号／品名／状态／可用现货数量／本项目需求数量／缺口数量／答交数量／答交日期；`grep -rln "本项目需求数量" 4-数字员工/` 唯一命中 `4-数字员工/采购部/SC8-客户订单交期智能承诺/sc8/baoguan.py` ⇒ **他要的参考样式就是我方自己的 SC8 保供看板**，无须反问他。→ 立 §一 `#539`。
  - **D-IT11 作废**：知会件，他未提异议，视为已知悉，不派生行。
- **需 SP 项**：§四 `#194`（`#538` 要不要插到 `#499` 前面，默认项 (a)＝插队在前）。🔴 **§四 `#186`（取数层先开工，他已答默认 (a)）本次不重提**，两者不是同一件事。
- **台账回写**：`points-by-letter --letter "采购部#22"` 返回 `[NONE]`（退出码 0）⇒ **本信非台账投影，零回写**。

## 拆件 2 · §一 `#527`／`#528`（陈忱 · 女，2026-09-09 两条文本反馈）

- **行号**：§一 `#527`、§一 `#528`（两条均 `[S:open][D:业] 待领`，本班**同时销号**）。
- **内容摘要**：`#527`＝「以下为提交的10份历史D样本，其中3份合格报告，7份不合格报告：」（冒号后无内容）；`#528`＝「视频/文件超过100M，无法下载」。
- **已执行动作**：两条**合并读**（§二 硬约束③）后销号，新立 §一 `#540` 作收件承接行，登 §四 `#193`。
- **判断依据**：`#528` 是**企微侧失败回执、不是她的话**；`#527` 的附件位就是 `#528`。对照 `质量部#13`（2026-09-04 发出）正文「按你 9/1 定的 2026-09-11 前交即可，我们不催」＋构成「坏报告 7 ＋ 合格 3」，与她发来的份数构成完全吻合 ⇒ **认定即为该批；她已按约在交期前提交，失败在传输通道，我方没收到**。🔴 单独读 `#528` 极易读成她在推脱——**任何催办口径都不得写成「陈忱未按期提交」**。
- **需 SP 项**：§四 `#193`（换哪条通道，默认项 (a)＝我方起草 `质量部#14` 只到「待你审」为止；**截止今日**，晚于今天 (a) 也赶不上 09-11）。串行闸实测 ✅ 开（`工具-跟进闸查询.py --to 陈忱`，依据 `质量部#13` 已闭环；下一个可用号 `质量部#14`）。
- **台账回写**：本批无 `部门#N`（专员主动来件），不适用台账投影。

---

## 待发信盘点（#124）

`工具-跟进信README查询.py --digest`：**⏳ 待你审 ×0 ／ 🆕 待发 ×0 ／ ⏸ 暂缓 ×0**（合计 47 行，未识别已知前缀 0 行）。**无积压，无「队列称暂缓／README 仍 🆕 待发」的交叉红标**。

## 起草缺口检测（#245）

`draft_gap_check.py --window-days 14` stdout 原样：`待发信盘点（扩展版，近 14 天已部署场景 vs 已起草跟进信）：无缺口`

---

## 巡逻自检

- **〇ter 探测结果**：`[SIGNAL]`（`采购部#22`，`CHECKPOINT=2026-09-10T00:22:18Z`）；**信号已在本报告登记 §二 之后消费**，消费回显见 §二 批次行下游追记。
- **〇bis 跳过项**：**本班一项未跳**。〇bis 的前提是「无本机 PowerShell、无出网 shell」，本班实测两者皆有（`Get-Date` 取到 `2026-09-10 08:33:28 +08:00`），前提不成立 ⇒ 一.4／三.8／四.5／四.6 **照做不跳**，并在此留痕。
- **三.1 锁**：首次 `acquire` 被拒（`CC-OP-0910-D-537锚点数` 持锁 7 分钟、有效）。**未走「锁冲突降级」**——本班可等，且两件都有时限（`#193` 截止今日、`#540` 交期明日）；改为后台轮询、释放后再 `acquire`，全程未在无锁状态下写过队列一个字。
- **本轮实拿编号**：§一 `#538`／`#539`／`#540`，§四 `#193`／`#194`（`acquire --reserve-multi 一:3 四:2 --domain 业` 一次原子分配，实拿即全部用满）。
- **并入审核结论（协议〇.10）**：`#538`／`#539` 已用 `--digest --grep` 扫 `周报`／`SC2`／`页面`／`明细`／`对不上` 五组关键词过全池，`#540` 扫 `样本`／`8D`／`验收` 三组；逐条比对 `#361`／`#394`／`#499`／`#502`／`#506`／`#340`／`#408`／`#431` 后**确无可并入**，理由已逐行写进各自状态列。`#538` 与 `#539` **刻意分立**：一为 🔴 正确性、一为 P2 展示，合行会让 🔴 被 P2 拖着不能销号。业务行不受机制类可动 WIP 上限约束。
- **三.6 写回前比对**：本班全程用 `append-row`／`edit-row` 行级定点写入，未整文件覆盖；期间未出现自己没写过的新增行，未撞号（编号由 `--reserve-multi` 原子分配，写入后高水位线由工具同步）。
- **预案覆盖不了的情形**：一处——**收件登记行没落地**（bot 追行遇锁忙，推迟进 `pending_queue_lock_appends.jsonl`），章程默认的「digest 命中待领行」入口本班不成立，实际入口是 README 第九态＋信号文件。**追查后是一枚 fail-silent**：`decision_reminder_check.py` 兜底 flush 静默返回 0，独立脚本回显「无待补录记录」，而同一时刻 `read_deferred_appends()` 实测返回 1 条 ⇒ **「锁忙一条没补」与「确实没有记录」印的是同一句话**。等锁空闲重跑，回显「已补录 1 条」、暂存文件删除，补录成 §一 `#541`（已当场销号，内容即本报告拆件 1）。缺陷**并入 `#398` 作 ⑺**，不另立机制行（避开可动 WIP 闸）。
- **刻意未做**（合并一行）：不修 `#398` ⑺ 那枚缺陷（只并入留痕）；不改 `#506` 状态（🟡 不关他人在办行）；不起草任何对外信件（🔴 红线）；不改 `#499` 的口径结论（他未批改判例，无新事实可落）；不重提已答的 §四 `#186`。
