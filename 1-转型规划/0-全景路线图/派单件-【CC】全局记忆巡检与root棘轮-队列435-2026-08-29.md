---
title: "派单件-【CC】全局记忆巡检与 root 棘轮阈值（队列 §一 #435）"
created: 2026-08-29
执行方: CC（写生产码、自行 commit+push、一任务一 worktree）
status: ✅ 已派出（2026-08-30）
编号: OP-0830-C
---

### C ·【CC】全局记忆巡检 ＋ root `CLAUDE.md` 棘轮阈值 ＋ 状态字段一致性（队列 §一 `#435`）

> ✅ **可派出。** 2026-08-30 分诊改判 8 行后机制类可动 WIP 27 → 19，`#435` 已摘 🛑 转待领。design D1-D7 已过，**不重开决策**。
> 🔴 **六件同车**：A1 路径存在性／A2 尺寸／A3 版本快照／A4 备份堆积／B root 棘轮／E 状态字段一致性。

**开场词（复制即用，▶ 粘贴端：CC 新会话）**：

```
[OP-0830-C]【CC】全局记忆巡检与root棘轮
【设置】执行环境：CC ｜ CC session：☑ 有 ｜ worktree：☑ 新建（一任务一 worktree） ｜ 分支：claude/op0830c-global-memory-inspect ｜ 工作区：worktree ｜ 派出线：CC 机制建造（OP-0830-C，承接队列 §一 #435）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0830C-全局记忆巡检与root棘轮。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\派单件-【CC】全局记忆巡检与root棘轮-队列435-2026-08-29.md 全文（内含范围·顺序·三条易做反处·验收判据）→ ② 其指向的 openspec 变更包四件 → ③ 队列 §一 #435 行 → ④ CLAUDE.md 当前进度恢复上下文；design D1-D7 已审过不重开决策，按 tasks.md 顺序执行。
```

⚠️ **本 opener 2026-08-30 首版漏了第 3 行 `set_session_title`**（补充三形态①，本方第 18 次违反），已补。已在跑的 session 补救句见模板库 §〇 补充三。

## 【范围】

`openspec/changes/global-memory-inspection-and-root-ratchet/tasks.md` **第 1-5 组**（0.1／0.2 已 [x]）。**design D1-D6 Shao Peishen 2026-08-29 已整体审过、D7 系 2026-08-30 并入子项 E 的决策，均不重开。**

🔴 **触碰两个文件，注意先后**：第 1-4 组动 `工具-落库sweep.py`，第 5 组动 `工具-共享文档编辑锁.py`。**第 5 组放最后做**——它改的是你自己收工时要调的那把锁，改坏了会把自己锁在外面。

## 【🆕 子项 E（2026-08-30 他明示并入，第 5 组任务）】

**状态字段与行内自陈一致性检查**——把 2026-08-30 那次手工分诊变成闸自动给出的候选清单。

**为什么必须做**：当天实测机制类可动 WIP 27 行里 **17 行（63%）是 `partial`**，而 `partial` 的实际用法是「主体已做完、尾巴挂在外部条件上」⇒ **活干得越多、WIP 越高**，反向激励。闸早就留了 `blocked`／`timed=` 出口（队列里分别已用 19／5 次），**缺的不是机制，是没人做分诊**。

🔴 **两条不许做反**：⑴ **不改 `_count_mechanism_wip` 的计入口径**——`partial` 里确有真可动者（`#96` Cowork 半完成、**CC 半待领**），改口径＝用静默漏报换掉吵闹超限；⑵ **只建议、不自动改**——状态字段是行所有者对自己工作的判断。
**验收判据只有一条**：对 2026-08-30 改判**前**的队列快照跑，须列全当天人工分诊出的那 8 行（`#282`／`#413`／`#419`／`#398`／`#380`／`#387`／`#399`／`#98`），**列不全就是漏报**。

## 【🆕 A4 备份堆积（2026-08-30 并入，任务 1.6b／2.3b）】

数受检文件同目录 `CLAUDE.md*.bak*` 的个数与最旧日期，超 3 即告警。🔴 **只报不删**——备份件可能是有意保留的还原点。
**来由如实记**：2026-08-30 他问「那个 .bak 能不能删」，本线去核才发现 `~/.claude` 下已攒 **5 个**（最早 2026-06-24）、**其中 3 个含同一个明文 Bearer token**。⇒ **没有人会主动去看备份目录**，与 A1/A3 同属零信号族；与它们共用同一次目录读取，扩得最省。

## 【三条最容易做反的地方】

1. **D2 的取舍方向与既有覆盖校验相反** —— `_looks_like_declared_path` 宁可漏认（误认会把整批跳过＝把 sweep 关掉）；**本包宁可误报**，因为漏报是零信号、正是本包要消灭的东西。照抄前者的取舍会把本包做成摆设。
2. **A3 的噪声排除是硬要求** —— 必须**不**命中 `Python314`、`nodejs`、`#NNN`；单测 2.2 里这三个反例一个都不能少。
3. **3.x 是真实验活，不是 mock** —— 对真实 `~/.claude/CLAUDE.md`（2026-08-29 已订正版）跑，应零红；再造一次真阳性复验（临时副本里写一条已失效路径，确认能报出行号与原文），**核完删副本**。只跑 mock 不算过。

## 【触碰区（认领前核）】

`0-学习与工具/工具-落库sweep.py`（常量段 ＋ `_check_claude_md_carrier_size` 函数族）；**`0-学习与工具/工具-共享文档编辑锁.py`（子项 E：新增措辞集常量与 `_suggest_status_reclassification()`，接入 release 阻断消息）**；`reports/sweep-claude-md-size-state.json`（新增键）；值周巡检清单一行。
🔴 **不得触碰**：根 `CLAUDE.md` §5（`#381` 在办触碰区）、`.51`、队列写入路径、真实业务数据。

## 【收工】

tasks 4.1-4.4：值周清单加一行 → §二 批次登记 → commit+push（ff-only）→ 队列 `#435` 状态列开头写 ✅ 并附实测数字 → **当场** `/opsx:archive global-memory-inspection-and-root-ratchet -y`。
