---
title: "派单件 ·【CC】#358 design 审前置 P1／P2 只读取证（2026-09-08）"
created: 2026-09-08
派出线: Cowork 业务总线 OP-0908-A（批 B-0908_B）
队列行: §一 #358（机制环境队列）
依据: Shao Peishen 2026-09-08 在 Cowork 会话答定夺 2＝(a)（授权取 P1/P2 纯只读取证）＋ 答定夺 1＝(a)（Cowork 通道跑不了 ssh，改派 CC 执行）
status: 待你在 CC 里起
---

# 派单件 ·【CC】`#358` 审前置 P1／P2 只读取证

> **为什么会有这一件**：`#358` 的 design 审一直挂着，表面看是「等 Shao Peishen 一个字母」，实则 `design.md` 自己写死了两条**审前置**——P1（`.51` 审计类文件真实大小与行数）与 P2（该盘剩余空间）。原文：**「在 P1/P2 有真实输出之前，D5／D6 的数值一律视为占位，审时不得当已定值照准。」** 不取回这两条，请他审 D1–D9 等于请他对占位数字拍板。
>
> **为什么改派 CC**：Cowork 侧已按 `#478` 条件放行路径走完 `transfer-out`→`deploy-authorize`（授权记录 #0，LAN 探针 on），但执行第一步就撞上 —— **`ssh.exe` 在 Cowork 那条 PowerShell 通道里根本起不来**：`ssh -V`（纯本地、不连网、只打版本号）也是 exit 255、零输出，而同批次 `git --version`／`ping 192.168.100.51`／TCP 22 全正常。已 `deploy-record --outcome stopped` 降回留步态。五行实测表与定性见 `1-转型规划/0-全景路线图/取证件-2026-09-08-Cowork侧ssh通道不可用.md`。
>
> 🔴 **Cowork 侧那次授权不继承到本泳道**（`#478` 逐项粒度、无批次级授权、无时间窗）——本件第 2 步要求重走一次 `transfer-out`＋`deploy-authorize`，不是冗余。

▶ 首次派出：[OP-0908-I]

**开场词（复制即用，▶ 粘贴端：CC 新会话）**：

```
[OP-0908-I]【CC】358审前置P1P2
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0908i-audit-retention-p1p2`） ｜ worktree：☑（358-p1p2-evidence，从 `claude/op0907x-audit-retention-358` 起新 worktree，收工自删） ｜ 工作区：有：经 SSH 只读访问 `.51`（192.168.100.51，别名 `supplychain-server`）——🔴 **纯只读，不部署、不重启、不改配置、不写任何文件**；不触碰企微机器人与定时任务 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-A（批 B-0908_B）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0908I-358审前置P1P2。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/派单件-【CC】358审前置P1P2只读取证-2026-09-08.md` → ② `CLAUDE.md` §4 路由表 → `.claude/rules/两桌同步与取证.md`（本机 PowerShell 与取证纪律） 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 🔴 **开工第一步＝通道自测，不做这一步不许往下走**：跑 `ssh -V`（纯本地、不连网、不读密钥，只打版本号）。**打不出版本号就当场停**——把退出码与 stdout/stderr 原样写进回报，**不换参数重试、不改 `~/.ssh/config`、不猜原因**。成因见 `1-转型规划/0-全景路线图/取证件-2026-09-08-Cowork侧ssh通道不可用.md`：Cowork 侧同一命令 exit 255、零输出，而 `git --version`／`ping 192.168.100.51`／TCP 22 全正常 ⇒ **是通道问题，不是网络或密钥问题**。自测通过再继续。
2. 🔴 **重走一次条件放行，授权不继承**（`#478` 逐项粒度，无批次级授权、无时间窗）：先 `python 0-学习与工具/工具-泳道看护状态机.py transfer-out --batch B-0908_B --wave 2 --lane 358-p1p2-cc --action-key deploy_51 --note "…"`，再 `deploy-authorize --batch B-0908_B --wave 2 --lane 358-p1p2-cc --action-key deploy_51 --item "#358-P1P2-readonly-evidence-cc" --authorized-text "<逐字：Shao Peishen 2026-09-08 在 Cowork 业务总线 OP-0908-A 答定夺 2＝(a) 授权取 #358 的 P1/P2 纯只读取证；同日答定夺 1＝(a) 指派 CC 侧执行>"`。三项前置由该命令现查，任一不过即拒绝并非零退出——**被拒就停，不绕**。
3. 🔴 **现读 `0-学习与工具/skills源码/zhuopin-lan-closeout/SKILL.md`「执行步骤」第 4 项，照那份正本办**（本件只给指针、一步都不复述）。**每项固定四步＝快照→执行→冒烟→回写证据**。⑴ **快照/定位**：`ssh supplychain-server` 上只读列出审计类文件（起点 `C:\baoguan`，实测该机 `C:\baoguan\.env` 存在，见 `看护件-2026-08-31-LAN收口首跑批.md` `#403` 行），记下 FullName／Length／LastWriteTime。⑵ **执行**：**P1** ＝ 审计类 `*.jsonl` 的真实**大小与行数**（`Get-ChildItem` ＋ 行数统计；309 MB 那个文件行数统计可能耗时，>10 分钟须在心跳里写「仍在等 X，预计还要 N 分钟」）；**P2** ＝ 该盘剩余空间（`Get-PSDrive`）。
4. 🔴 **冒烟判据（只读项的冒烟就是这一条）**：输出必须能明确回答「那 309 MB 到底落在 `baoguan_access_trace.jsonl`（TRACE 类）还是 `baoguan_audit.jsonl`（COMPLIANCE 类）」。**结果「太干净」先怀疑没读到对象**——空清单／零文件／0 字节一律**先当作没读到**，换一个已知存在的路径（如 `C:\baoguan\.env`）做对照证明通路是通的，再下结论。冒烟不过即停项、降回留步态并注明原因，**不得以「查无此文件」结案**。
5. ⑷ **回写证据（三处，一处都不能少）**：① 把 P1／P2 的**真实输出原样**补进 `openspec/changes/audit-retention-archive/design.md` 的「🔴 审前置」节（🔴 **该包只在分支 `claude/op0907x-audit-retention-358` 上，master 没有**——本泳道须从该分支起 worktree）；② `python 0-学习与工具/工具-泳道看护状态机.py deploy-record --lane 358-p1p2-cc --authorization-index <上一步返回的序号> --outcome done|rolled_back|stopped --evidence-ref <落点路径>`（**只记指针，不内联证据内容**）；③ 队列 §一 `#358` 状态列追记结论（走 `0-学习与工具/工具-共享文档编辑锁.py` 的 `edit-row --append`，域 机）。🔴 **`#358` 行内已于 2026-09-08 按 K2 外置**，行长余量有限，追记控制在 600 B 内、超了就写进外置件 `1-转型规划/0-全景路线图/队列行日志/#358.md`。
6. ⑸ **回报一句**：P1／P2 拿到之后，**明写「D5 的阈值默认值与 D7 的迁移优先级是否需要按实测重排」**——这正是这两条取证被定为 design 审前置的全部理由（`design.md` 原文：在 P1/P2 有真实输出之前，D5／D6 的数值一律视为占位）。

不做什么：
- 🔴 **`.51` 上纯只读**：不部署、不重启服务、不改配置、不写任何文件、不删任何文件。本次授权只覆盖 `Get-ChildItem`／`Get-PSDrive` 这一类读取动作，**授权范围之外的任何 `.51` 动作一律另走 `transfer-out`＋`deploy-authorize`**。
- 🔴 **不代答 design 审**：D1–D9 与 O1–O3 一个都不裁。O1 属合规红线**须 Shao Peishen 本人答、孙涛不可代**；三条 Open Question **均不设默认项**。本泳道只把前置补齐，审是他的事。
- 🔴 **不合入 master**（🟡 档，停等他一个字母）；收工只 push 本泳道分支。**不修 ssh 通道本身**——看护/派单会话不修工具，发现机制缺陷只登记＋出派单。
- 🔴 **不碰在跑会话的触碰区**：`B-0908_B` 五泳道（`#440`／`#491`／`#501`／`#500`／`#439`）与【Cowork】FI6 需求 grill（`4-数字员工/财务部/FI6-异常交易实时检测/`、`openspec/changes/fi6-anomaly-detect-mvp/`、队列 `#471`／`#436`）。
```
