---
status: 待执行
title: "开场prompt ·【CC】QD-B 路径引导修复（队列 #345 ①）—— 赶在其下次部署前"
created: 2026-08-18
执行方: CC
派出线: Cowork 全景路线图线
来源: Shao Peishen 2026-08-18 拍板 (a)（「尽快单派一个 CC，先只修 QD-B 入口脚本，赶在它下次部署前」）
---

# 【CC】QD-B 路径引导修复（队列 #345 ①）

## 为什么急

2026-08-18 SC8 发布收口时**真实把 `.51:8091` 打挂过一次**：`sync-to-server.ps1` 把 master 的
`scripts/` 推上去后服务起不来——队列 #300 的 worktree 隔离引导在找不到 monorepo 标记时
**fail-loud 抛错**，而 `.51` 是**扁平布局** `C:\baoguan\{zhuopin_platform,app}`，本就没有该标记。

**QD-B（`.51:8093`）是同一套扁平布局部署，`scripts/run_qd_b_web.py` 带着一模一样的引导段
——它下次 sync 必然同样起不来。** 本任务就是赶在那之前修掉。

**症状长这样（记住它，省得下次重新排查）**：`sync` 报 `OLD_CLEAR` + `RESTART_FAILED_NO_INSTANCE`；
计划任务 `LastResult=0`（**看起来成功**）但进程秒退、端口无监听、`/api/ping` 502；
前台跑 `start-*.ps1` 才会显出真正的 `RuntimeError: 未找到仓库根标记 5-平台底座/zhuopin_platform`。

## 开场词（复制即用）

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `qdb-bootstrap-layout-fix` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-CC-QDB路径引导修复-队列345-2026-08-18.md 全文 ＋ 队列 §一 #345 行全文 ＋ 4-数字员工\质量部\QD-B-立项审核门禁\CLAUDE.md，按文件执行。

开工前先跑 git fsck --connectivity-only 与 git pull。

有差异再问我，没有就直接开工。
```

## 范围（**只做 ①，不做 ②**）

**做**：`4-数字员工/质量部/QD-B-立项审核门禁/scripts/run_qd_b_web.py` 的引导段，照抄 SC8 已验证的范本
`4-数字员工/采购部/SC8-客户订单交期智能承诺/scripts/run_baoguan_web.py`（commit `a858769`）：
找不到 monorepo 标记时**不抛错**，回退到扁平布局的兄弟目录（存在才加），并始终把场景自身目录
加进 `sys.path`。

**不做**：#345 ② 那条「收拢为平台底座共享函数、消灭 20 份副本」——那是独立后续，**本次不碰**
（Shao Peishen 2026-08-18 明确选的是「先只修 QD-B 入口脚本」）。也**不要**顺手改其余 18 处
（各处是否都该改需逐个判：**monorepo 内的 `tests/conftest.py` fail-loud 是有价值的**，
只有扁平布局部署的入口脚本才需要回退）。

## 验收（缺一不算完工）

1. QD-B 全量回归零漂移（含黄金基准，若有）。
2. **真部署 `.51:8093` + 冒烟**：`/api/ping` 200 ＋ 关键页 200；
   并**核对服务器上 `run_qd_b_web.py` 的 SHA256 与 master 逐字节一致**（SC8 本次即这么验的）。
3. commit + push（ff 入 master，`git rev-list --count origin/master..HEAD` ＝ 0）。
4. 队列 #345 回写：① 已完成、② 仍未做（**不要把 ② 一并销掉**）。
5. 收工重跑文档台账。

## 次序与并行

**零依赖，可立即开工**；与当前在办的任何任务**触碰区不重叠**（只碰 QD-B 场景 + `.51:8093`）。
唯一注意：QD-B 2026-08-18 刚由 #340 C01-C10 落地（commit `1282470`），**开工先 `git pull` 取到它**，
别在旧基线上改。
