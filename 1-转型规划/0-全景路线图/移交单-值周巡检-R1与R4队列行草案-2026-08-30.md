---
status: 待发
title: "移交单 · 值周巡检（2026-08-31 周一）· R1／R4 队列行草案（已过并入审核）"
编号: OP-0830-J
日期: 2026-08-30
交付方: Cowork（本会话，OP-0830-A 线）
接收方: 值周巡检（周一 10:00）
本件不写队列: "Shao Peishen 2026-08-30 答 ② (a)／⑨ (a)——交值周巡检统一分发与排序，本会话不占编辑锁、不取号"
---

# 移交单 · 值周巡检 · R1／R4 队列行草案

> **一句话**：三条候选行经并入审核后**只剩两条**（R3 全部并入 `#366`）；两条恰好各占一个 WIP 空位，**但是否该优先于已排队两周的 `#381`／`#382`，请你按全局判**——本件不预定。

---

## §1 开跑前必做的两件（🔴 按序，不可并行）

1. **当场重跑 WIP，不要用本件里的数。** 本件写的 `20／22` 是 2026-08-30 23:16 +08:00 的实测；同一天早些时候源码注释里是 `27／22`，**两个数都对，差别只是时点**（其间另一条 session 做了状态分诊，把 `partial` 改判为 `blocked`／`timed=`）。⇒ **凡引用 WIP 一律当场重跑，含本行。**
2. **跑对账审计**（skill `zhuopin-queue-audit`），按协议〇.8 顺序执行，再读本件。

---

## §2 三条候选的审核结论

| 候选 | 并入审核结论 | 处置 |
|---|---|---|
| **R3 转态检测与分级响应** | 🔴 **整体并入 `§一 #366`，零新增行** | 不取号 |
| **R1 口径点台账** | 全库未见可并入行；`#366` 管「信」的闭环证据，本件管「点」的身份，不同粒度 | **建议新建 1 行** |
| **R4 纪律 eval 套件** | 与 `#284`（规则退休制·阈值计数台账）同族但**不可并入**——`#284` 是 `[S:hold]` 🛑 常驻计数载体、不设完工态，承载不了建造任务；eval 是它的**供数方**，关系写进行内即可 | **建议新建 1 行** |

**R3 并入的依据（两条，均取自 `#366` 状态列原文）**：

- **存量回扫已于 2026-08-23 人工做完**——12 封（采购部 #6/#7/#8/#10、质量部#5、财务部 #7/#8/#9/#10、IT部 #4/#5/#6）已逐封转 `📥`，10 封有归档回件硬证据、2 封经 Shao Peishen 当面确认；转态后 32 封闭环 29、未闭环仅 3 封且全为真在途。
- **超期看守已于 2026-08-28 登记**——三层修法 ③「闸增加第四态 `🔴 待拆件`」＋ 配套「落库 sweep 扫 README 里带『未经人工逐字确认』字样的行」；openspec 包 `followup-closure-evidence-gate` 已出件（proposal＋design＋三份 delta，`validate --all --strict` 119 passed），**只差实现派单**。

---

## §3 两条行草案（可直接取号写入，触碰区已填）

### 候选 A · R1 口径点台账（需求管理 intent 层）

> **需求管理的单位由「信」改为「点」，跟进信改为台账的投影。** 每个口径点一条记录：稳定 ID（如 `FI2-D19-03`，不随队列行号漂移）／场景／提出人／提出日／判例原文／拟改判定／状态（待问→在途→已签认→已回灌→已落码／已作废）／承接载体（openspec change ／队列行 ／commit）／载体信（`部门#N`）／到期。**信是视图、点是真身**——不是在信之外再加一张表。
> **依据**：`度量复盘-判例批改法回退闸到期-2026-08-23.md` §二 自陈「闸原文要的是**口径点**提出到销点中位天数，本复盘算的是**信**发出到回件闭环天数……**没有逐点台账，就没有精确值**」。
> **不改**：判例批改法本身（格式／≤10 条／判据类不默认生效／不加并行专员）；已闭环 17 封历史信不追改，回溯拆点只做未闭环的。
> **proposal 草案已备**：`1-转型规划/0-全景路线图/openspec提案草案-口径点台账-2026-08-30.md`（四条 MANDATORY 全节已成文；🔴 落包时须补 `git check-ignore -v` 真实输出）。
> **是 R2（串行闸改按点计）的硬前置**——R2 已由 Shao Peishen 2026-08-30 判**本轮不立项**。
> **触碰区**：`6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md`（编号段降为派生视图）／两份队列／新建台账文件／`0-学习与工具/工具-跟进闸查询.py`（晋档 3 时改取数源）。
> **域**：机　**建议状态字段**：`open`

### 候选 B · R4 纪律 eval 套件（一期 3 条）

> **给纪律建回归网，让「规则退休制」第一次能真的退休规则。** 一期**只做今天无任何机器守**的三条：`%ERRORLEVEL%` 假 0 ／ 写侧日期未当场重取 ／ 提方案前未先查环境已有能力。其余 4 条（代词／编号／串行闸／队列拼接）已有 H4 哨兵、称呼判据或反例单测覆盖，**作回归网二期做**。
> 🔴 **不自建 harness**——复用 `skill-creator` 既有 `scripts/run_eval.py`／`agents/grader.md`／`evals/evals.json` schema／`eval-viewer/`。
> 🔴 **晋档硬条件**：3 条 eval 各须**捕获一次人为注入的回归**（故意把对应规则从 `CLAUDE.md` 摘掉，eval 必须变红）。**没有这一步，「每天在跑、全绿」不构成通过**——同 `#82` 形状。
> **与 `#284` 的关系**：eval 是 `#284`「人守规则违反计数」的**供数方**；`#284` 是 `[S:hold]` 常驻载体，不并入。
> **退休义务（协议〇.9 措施 B）**：本包上线时不退休任何守卫（eval 是元守卫，未验证前不得替换现役守卫），但 tasks 内写死可检验承诺——**上线后 4 周内以 eval 覆盖为依据对 ≥3 条人守规则执行退休判定，判「不退」须写明理由**。
> **proposal 草案已备**：`1-转型规划/0-全景路线图/openspec提案草案-纪律eval套件-2026-08-30.md`（含三道题全文＋每题的「诱饵」与「注入回归」一行）。
> 🔴 **须 design 审拍板的安全边界**：本仓库 CI **目前零 secret**（实测 `ci.yml` 中 `secrets.` 0 命中）；跑 eval 需 `ANTHROPIC_API_KEY`，**是本仓库 CI 的第一个凭据**，而现行凭据纪律「只进 `.env`」未覆盖 CI secret 这一形态。
> **触碰区**：新建 `evals/`／`.github/workflows/`（新 job 或加进 `ci.yml`）／`reports/`。**不改任何既有 hook／lint／业务代码。**
> **域**：机　**建议状态字段**：`open`

---

## §4 排序判断交给你（本件不预定）

- 实测 `20／22`，两条各占一位 ⇒ **取完即 22／22 满**。
- `#381`（2026-08-23 立行时排队）／`#382`（2026-08-24 排队）两行 🛑 **已排两周**，位一旦被占就继续排。
- **⇒ 是否让新行优先，是你的全局调度判断。** 若判定让位，两条按 `#381`／`#382` 同例立为 🛑 排队行（不占 WIP，WIP 回落时转可动）即可，proposal 草案不受影响。

---

## §5 本会话留下的三条方法教训（与本件同批产生，供 `#284` 计数与 eval 取材）

🔴 **同族三次，全部是「没先查现状就动手」**：

1. **R3 初稿未查 `followup_readme_bridge.py` 已存在** —— 按手册"该有什么"写方案，L1／L2 全是重复建设；
2. **取号未查全库已用 OP 号** —— 撞了 `OP-0830-B`（构建环境接力）与 `OP-0830-C`（派单件-队列435），已改判为 `-H`／`-I`；
3. **未查 `#366` 当前状态，沿用 7 天前快照** —— 把 2026-08-23 度量复盘里"12 封停在 `✅ 已推送`"当成今天的现状，而 `#366` 的清理正发生在同一天。

⇒ **这三次是 eval-7 那道题的真实用例，不是假想题。** 建议 `#284` 记违反计数 +3（形态：开工前未查现状），由值周巡检按规则退休制阈值判定处置。
