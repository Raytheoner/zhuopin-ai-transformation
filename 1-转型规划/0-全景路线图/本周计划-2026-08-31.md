---
title: "本周计划 · 2026-08-31（周一）"
status: 在办
生成: 值周巡检（章程 v2）｜ 前置＝对账审计已跑、修正 9 处
---

# 本周计划 · 2026-08-31

> ▶ **4 项定夺**，全部⟨就地答⟩，0 项待派发。**WIP 当场重跑＝20/22**（未采信移交单 08-30 的时点值）；处置后 **21/22**。
> 🔴 **本周唯一硬期在明天（09-01），而那条机制现在根本不存在——见 B-1。**

---

## A. 本周应启动/续推任务（共 3 次粘贴）

**次序与并行矩阵**：**A2／A3 零依赖、可立即开工**；**A1 硬阻塞于 B-1**。触碰区两两零重叠 —— A1＝`wecom-aibot-service/`；A2＝`0-学习与工具/`＋定时任务真身；A3＝`8D报告AI评审规则库.md` ⇒ **可全并行**。

### A1 ·【CC】aibot 入站通道收口三件（P0，硬期 09-01）——队列 #380/#379/#416

```
【设置】执行环境：CC ｜ 分支：claude/a30-inbound-whitelist-apply（需 rebase） ｜ worktree：☑ 新建一个 ｜ 工作区：不动主工作区
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0831A-aibot入站收口。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① C:\Dev\zhuopin-ai\CLAUDE.md → ② 队列-机制环境 §一 #380、#379、#416 全文。本次三件：
① rebase 并合入 #380 的 apply 产出（commit 32feda5，落后 master 194+，与 connection.py、run_aibot_service.py 冲突）——🔴 解冲突涉入站准入语义，须先拿到 Shao Peishen 对 B-1 的答复，未拿到不得自行改判；
② 合入后真机验：须在 wecom-aibot-service/reports/wecom_aibot_audit.jsonl 见到 errcode=0 才算通，只看返回码不算数；
③ 落地 #379 年度提醒（🔴 实测本机计划任务库没这条任务——不是坏了，是从没建过）。
收工：队列回写＋登记 §二 批次，自行 commit+push，重跑台账。
```

### A2 ·【CC】编辑锁超时 fail-closed（新激活，P2）——队列 #382

```
【设置】执行环境：CC ｜ 分支：新建 claude/queue-382-editlock-timeout ｜ worktree：☑ 新建一个 ｜ 工作区：不动主工作区
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0831B-382编辑锁超时。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
先跑 python 0-学习与工具/工具-队列查询.py --row 382 --section 一 读全文，勿只读开头。本行 08-24 因 WIP 满挂 🛑，08-31 实测回落至 20/22 已去 🛑；修法候选见行内。
最实的一条：release 校验⑹ 在定时/沙箱班次结构性 fail-closed（git status 实测 40s > 工具 20s 上限），逃生阀已走成常态。
⚠️ 触碰区与 §一 #305 同文件（queue_appender.py），领取前先核其是否在办。
收工：队列回写＋登记 §二 批次，自行 commit+push。
```

### A3 ·【Cowork】质量专线 #431 ⑵⒝（P2，不依赖回件）——队列 #431

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐ 不需要 ｜ 工作区：主工作区，只改 .md
读队列-业务场景 §一 #431（已由 08-31 对账审计 open→partial）。只做 ⒝ 这一半：
陈忱 08-28 09:22 已签认「D0／D8 两章整体豁免」，而 1-转型规划/8D报告AI评审规则库.md §2 仍标「⏳ 待确认」——该文件维护说明写着回件到达后须把 §2 条文移入 §1 并改档，至今未做。做掉它。⒜（两份规则库谁是正本）不做，须等她答完 质量部#11 第 1 件。
收工：队列回写＋登记 §二 批次（Cowork 不 commit）。
```

---

## B. 需 Shao Peishen 的决策/动作清单

### 🔴 B-1【09-01，明天】#379 首触发怎么办 —— **本项无默认，须你明确答复**

两条今天实测：⑴ **本机计划任务库里没有这条提醒任务**，`#379` 至今「待领」⇒ **明天什么都不会发生**；⑵ 硬前置 `#380` 的 apply 早写完了，**但那分支从未合入 master**（落后 194 提交、有冲突）⇒ 即使发得出去，她回话仍会被静默挡回。

- **(a) 今天合入 #380 并真机验，明天人工执行首触发** —— 需你先拍板入站准入语义的冲突解法（CC 已停手不代判）；收益＝这条机制拿到唯一一次存活信号。
- **(b) 明天只人工发提醒，#380 留后** —— 她回话被静默挡回，且**挡回不产生任何信号**（已有 36 天无人知情的先例）。
- **(c) 跳过 09-01，改定 2027-09-01** —— **12 个月零存活信号**；你 08-23 定 (a) 的理由正是「一年后才第一次跑的定时任务，到时多半是坏的、没人会知道」。

> **为何不设默认**：提出方 session 收工即止（第①问答不出执行者）；且不答的代价不是「停在原地」——`aibot-inbound-whitelist` 包那句「观察窗口 7 天，取到 09-01」会随之失效，属错误继续发生。

### 🔴 B-2 本轮每周清扫已推迟 —— 默认 (a)

实测待迁 **近 280 行**（§二 216／§一 约 32／§四 约 30）。协议〇.8 定死唯一可靠办法是逐行判读、禁批量过滤（08-03 实测 33/70 误判），本会话无法保质完成 ⇒ **推迟而非降质**，已登记批次行。

- **(a)【推荐】本周单独派一棒无头 CC 专做清扫** —— 代价＝一棒时间；收益＝队列不再滚雪球。
- **(b) 并入下周一值周巡检** —— 届时约 560 行，同一个"做不完"放大一倍。
- **(c) 只清 §二** —— 清掉的是最不占注意力的那部分，§一/§四 噪音没减。
> 若不答，我按 (a) 备派单件等你发动（只搬 .md 行、不碰代码）。

### 🟠 B-3 两条临期悬置项重提 —— **均无默认，须你明确答复**

- **`§四 #133`（09-01）**：`python-docx` 读不到 Word 复选框的勾，已致一封信向姚祖怡做出不实指摘。三问一次答齐：⑴ 修法归属与是否豁免 WIP 立行；⑵ 是否更正（**越晚说越像补台阶**）；⑶ `#411` 怎么办。
- **`§四 #125` D-5（本周内）**：IT 部 **B 线存废**——IT 可能正在做。D-1/D-3 随 `IT部#11` 起草前答。

### B-4 其余悬置项（已逐条核过仍成立，不展开）

`§四 #126`（sweep 解码兜底）／`#127`（豁免挡不住 sweep 连带提交）／`#128`（12 个 worktree 的 git status 全失败）／`#132`（陈旧分支进不进主线）／`#134`（姚祖怡并存信号怎么读）／`#137`＋`#140`（root `CLAUDE.md` 瘦身口径）。
🔴 前三条均标注「**本项无默认执行者**」——不会自己消失，也不会自己被执行。

---

## C. 临期/过期红线日（7 天内）

| 日期 | 事项 | 标 |
|---|---|---|
| **09-01（明天）** | `#379` 年度提醒**首触发**（机制不存在＋前置未合入，见 B-1）；同日 `§四 #133` | 🔴 |
| **09-05** | `§四 #136` 建议定夺日（余 1 项） | 🟠 |
| **09 月** | 权威排期表本月三条：**FI2 三单匹配启动**／**SC7①**／**O2、O1** | 🟠 |
| ~~08-31~~/~~09-30~~ | ✅ **两条已解除**：`§四 #91` 8 月底承诺（R5 分母 08-26 已签认读法甲，载体 §一 #417）／R1 Champion 门禁（08-18 v6 挂起，新门禁日约 2027-05） | ✅ |

---

## D. 异常与流程自检

**① WIP**：当场重跑 **20/22**（机器口径）。处置＝`#382` 排队 7 天、按协议〇.9 **已去 🛑**（+1）；`#439`／`#440` 两条新行**立为 🛑 排队、不占位**——理由：R1 的下游 R2 本轮不立项、R4 的 CI 凭据边界待 design 审，**都不具备立即开工条件**。⇒ 现值 **21/22**，留 1 位缓冲给可能解除的 `#380`／`#397`／`#353`／`#354`。
🔴 **一处更正移交单**：它写「`#381`／`#382` 已排两周」——**实测 `#381` 已在办**（08-24 开工），只有 `#382` 在排队且是 7 天不是两周。

**② 对账审计修正 9 处**（批次 `B-0831_03`）：`§一 #202`／`#382`／`#418`／`#431`／`#432`，`§四 #35`／`#65`／`#91`／`#110`／`#138`。**最该记住的两条**：⑴ `#431`／`#432` **行内早已自陈转态，而机器字段与行首没跟着改** —— 只读开头得到"待领"、只读行内得到"已销号"，**两边都不报错**；⑵ `§四 #35` 是同族更深一层——**前两次漂的是日期，这次漂的是"该日期还成不成立"**。

**③ 批次积压 0**：§二 216 行**全 ✅**。git 台面：`master` 与 `origin/master` 实测 `0`。

**④ §三 冻结标**（观察项）：质量域 Q2 8D ＋ Q4 PPAP 仍挂，解除条件是"等 `质量部#9` 回件签认"，而陈忱**已于 08-25 回件**（§一 #405）⇒ **可能已可解除，无人核过**。未擅自摘标，列为质量专线下次开工首件。

**⑤《Paul 标准工作周》自检 —— 上周一次真实跑偏**：§二 批次 `B-0828_W` 原文写「push 被 auto mode 分类器拦下……**待他在本机 PowerShell 跑一次**」，撞上你 08-30 才立的 `§四 #141` ⇒ 跑偏。**建议**：拦截应转为"换一条已授权路径重试"（如交 sweep），而非转嫁给你；并入 `§四 #138`。

**⑥ 三条漂移（只登记不改）**：⒜ 审计 skill 要求用的 `工具-队列查询.py --digest` **实测不存在**。⒝ 四份活接力件 **8,279–8,487 B**，略超 R5 的 8 KB 上限而 J6 未报。⒞ 🔴 **两个 CC opener 的 `set_session_title` 样板占本计划约 1,000 B（10%）** —— 08-30 的 opener 守卫与 08-24 定的 10,000 B 上限互相挤压，**判据无一可删、只能挤内容**；建议调上限或让样板行不计入。

*Last Updated: 2026-08-31*
