---
title: "看护件 · 泳道看护批 B-0912_三包apply"
created: 2026-09-12
status: 生效
批次: B-0912_三包apply
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_三包apply`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴。** 授权原文：design 审 `4:P1a,P2a,P3a,P6ab另立`／`P2：4a`／`P3：1a，2a，3a，4a，5c，6a`，apply 起批 `1a`＝三条全起。
🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。本批三条泳道，`-MaxParallel 3`，未超上限 4。
🔴 **三条泳道各自的任务正本在各自派单件里**，看护件只负责编排；三条触碰区两两零重叠，可并行。

## 一、硬边界继承（三条同受）

- 🔴 **不 ff master**（🟡 档）；收工只 push 本泳道分支。ff 由派出线 `OP-0912-E` 登 `reports/pending-ff.jsonl` 后由六道守卫做。
- 🔴 **不碰 `.51`**、不动常驻执行体运行时（⏭️ 档）；**不发任何跟进信、不触发任何对外发送**（🔴 档）。
- 🔴 **零回归判据＝差集**（分支失败集 ⊆ 纯 master 同命令失败集），不是「必须全绿」。
- 🔴 **不改签认段一个字**——签认段是他的答复留痕，只对照执行。
- 🔴 **勾 tasks 要诚实**：只勾这一棒真做完并有手段可证的。
- 🔴 新文件名形态同批补 `.gitignore` 并 `git check-ignore -v` 实测取证（`#328`）。
- 🔴 **不立新机制行**（机制类可动 WIP 32/22 超限）；确需时 🛑 起首。
- 🔴 队列回写走 `工具-共享文档编辑锁.py`；§二 文件清单**只放反引号包着的路径**（`#513`）。撞 `.git/index.lock` 不重试。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`，命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与另两条 |
|---|---|---|---|
| `op0912z-p1-archive-precheck-apply` | P1 `opener-batch-archive-precheck`（46 项，0 勾） | `0-学习与工具/工具-队列查询.py`（加两个可选开关）；`0-学习与工具/工具-opener派出前校验.ps1`（新建）；`工具-opener批处理执行v2.ps1` 与 v1 的挂点 | **零重叠** |
| `op0912aa-p2-zombie-lint-apply` | P2 `tasks-zombie-item-detect`（24 项，11 勾） | `0-学习与工具/工具-僵尸未勾项lint.py`（新建）＋单测；`.github/workflows/ci.yml` 加一个 job | **零重叠** |
| `op0912ab-p3-closure-form-apply` | P3 `followup-closure-form-survives-backfill`（36 项，9 勾） | 平台侧 `readme_table.py`／`delivery.py`／`0-学习与工具/工具-跟进闸查询.py`；跟进信 README 的 `质量部#7` **一行**（走编辑锁） | **零重叠** |

**唯一共享资源＝两份队列文件**（三条都要写 §二 与 §一 `#561` 追段），由编辑锁串行化。

⚠️ **三条各自的最大风险，逐条写明，不合并**：
- **P1**：它改的正是起无头批用的那条链。🔴 收工必须用 `-DryRun` 在一份真看护件上验证解析仍正常，否则下一个人起不了批。
- **P2**：决策点 4(a) 的引用解析**必须支持 `前置 1.5/1.6`（斜杠）与 `前置 2.3、0.3`（顿号）**，三形态各一条夹具；不做即门禁上线第一天就红 2 条。另：**他未答陈化门槛 ⇒ 不预设，不要自作主张加阈值。**
- **P3**：碰的是在跑的跟进信链与串行闸。判错的后果是同一收信人手上两封在途信——不可逆、对外可见。🔴 决策点 2 的三条护栏**缺一即退回 (b)**；决策点 5 的必配缓解**不是可选项**。

## 三、泳道 opener

### A1 · P1 归档前置校验 apply（OP-0912-Z）

粘贴端：CC ｜ 泳道：op0912z-p1-archive-precheck-apply

```
[OP-0912-Z]【CC】P1前置校验apply
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912z-p1-archive-precheck-apply`） ｜ worktree：☑（op0912z-p1-archive-precheck-apply，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_三包apply）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】P1归档前置校验apply-2026-09-12.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912z-p1-archive-precheck-apply --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912z-p1-archive-precheck-apply --done --batch B-0912_三包apply --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · P2 僵尸未勾项 lint apply（OP-0912-AA）

粘贴端：CC ｜ 泳道：op0912aa-p2-zombie-lint-apply

```
[OP-0912-AA]【CC】P2僵尸未勾项apply
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912aa-p2-zombie-lint-apply`） ｜ worktree：☑（op0912aa-p2-zombie-lint-apply，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_三包apply）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】P2僵尸未勾项apply-2026-09-12.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912aa-p2-zombie-lint-apply --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912aa-p2-zombie-lint-apply --done --batch B-0912_三包apply --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · P3 闭环形态穿越回填 apply（OP-0912-AB）

粘贴端：CC ｜ 泳道：op0912ab-p3-closure-form-apply

```
[OP-0912-AB]【CC】P3闭环形态apply
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912ab-p3-closure-form-apply`） ｜ worktree：☑（op0912ab-p3-closure-form-apply，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_三包apply）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】P3闭环形态apply-2026-09-12.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912ab-p3-closure-form-apply --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912ab-p3-closure-form-apply --done --batch B-0912_三包apply --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-AC]【CC】看护三包apply
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_三包apply）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912AC-看护三包apply。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】三包apply-2026-09-12.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. 每泳道一行：分支／commit／哨兵／tasks 勾选数（做了几项／共几项）。
2. 三条各自的差集回归结论：分支失败集 ⊆ 纯 master 失败集？是／否；不成立就点名多出来的是哪几条。
3. **P1 必须贴 `-DryRun` 在真看护件上的解析输出原文**；**P2 必须贴三种 `前置` 分隔形态夹具的原文输出**；**P3 必须贴三条护栏各自的单测名与必配缓解的原文输出，外加 `质量部#7` 回改前后两版该格原文与字节数**。
4. 待 ff 清单（**ff 不在本批内做**）。
5. 🔴 等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0912_三包apply` 现取。
