---
title: "【CC】开场 prompt · D 批（#262 → #263）· SC8 答交口径 v3 ＋ 替代料假缺口"
created: 2026-08-05
执行方: CC 采购
执行环境: CC
来源: 队列 #262／#263；源＝姚祖怡 2026-08-05 回件（拆件巡逻 #260）；§四 #51 已拍板 (a)
status: 待执行
---

# 【CC】开场 prompt · D 批 · #262 → #263

## 【开场词（复制即用）】

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑ ｜ 工作区：企业AI转型

读 1-转型规划/0-全景路线图/开场prompt-【CC】D批-262-263-SC8答交v3与替代料-交接.md ＋ CLAUDE.md 当前进度恢复上下文，按文件执行。顺序不可颠倒：先 #262 修完并部署冒烟，再动 #263。
```

---

## 〇、🔴 为什么必须同一 session、且顺序写死

两行都改 SC8 看板的数字链路：**分两个 worktree 必撞**（同一场景同一批文件）。

但更要紧的是**顺序**：#263 会改变缺口／齐套计算结果，而姚祖怡需要**重核黄金基准并签字**。她本轮正是**拿单料号逐个核**的（`F02N.0224`／`R01A.1028`）——**若答交取值与替代料判定同时变，"数字为什么变了"就说不清，她无法归因、也就无法签字。**

**故：#262 修完 → 部署 → 冒烟通过 → 数字稳定下来，再动 #263。**

---

## 一、#262 —— 答交数量／日期口径根治 v3（P1）

### 背景：v2 修完了，她验收仍不通过，且**错误形态变了**

- **#211（v2）那次**：显示**错值**（500 ／ 2026-08-07）→ 已修（改从 SRM 供应计划逐期明细累计、只取"按排程交货"、累计目标改为缺口数量）。
- **本次（v3）**：显示**「无」**——即"查不到就如实显示无"的兜底被命中。

**她的新举证**：`F02N.0224` 的 BOM 缺口物料 `R01A.1028`，看板"答交数量／答交日期"**均显示「无」**，SRM 复核正确值 ＝ **10000 ／ 2026-11-25**（截图两张在回件 docx 内）。

### 🔑 拆件已交叉核实的线索（**是线索不是结论，勿据此直接改码**）

**高度疑似 SRM 供应计划看板的 60 天取数窗口，而非 `receiveType` 筛选**：

1. **08-03 交付自述里其实已经写着这件事**——原文：「`S04Y.0112`／`R01B.0754` 现网 `cb=[]`，该料真实确认批次 **2026-12-25 超出 SRM 60 天查询窗口**，如实显示「无」而非编造」。
2. **她本轮给的 2026-11-25 距今约 112 天，同样落在 60 天窗口外**——**两次举证是同一形态**。
3. **代码侧**：`sc8/sources.py:322` 处 `e = end or (date.today() + timedelta(days=60)).isoformat()`，即 `get_receive_board(s, e)` 窗口硬为 `[今天, 今天+60天]`；`sources.py:219` 另有一处同款默认。**#139 当初修的是 PO 那个 60 天窗口（改走 `po_transit_lookback_days` 默认 365），供应计划看板这一处没被同步覆盖。**

**⇒ 真正的结论是：「查不到就如实显示无」这个当初被我们当作可接受兜底的行为，业务侧从未认可过。** 那不是"如实"，在她看来就是错的。

**🔴 但根因仍须你用真实凭据实测坐实**——上面三条是事实交叉，不是实测。**先证实或证伪，再动手。**

### ⚠️ 放大窗口不是免费的

携客云 SRM **限流 1 req/30s**。是否放大／放多大／怎么与 `srm_answer_cache` 配合，**属你的实测与设计范围，拆件与总线均不代判**。若判断需要走 openspec，自行按 CLAUDE.md §5 触发门槛判断并在回填里说明。

### 她的口径原文（第三次重申，一字未变）

> 取 SRM 系统-供应计划协同-供应计划看板、交付方式为"按排程交货"的答交数量，以及该答交数量对应的日期。

### 范围含"整体复核一遍"

她原话要求「检查取值逻辑并**再整体复核一遍**」——**不止修 `R01A.1028` 单例**，须给出**全量料号口径一致性核验结论**（建议抽样对照 SRM 原始记录，产出可复核的对照表）。

---

## 二、#263 —— 替代料假缺口建造（P1，排在 #262 之后）

### 根因已由 #213 实测坐实，不需要重新排查

替代关系 `R01A.0707` ↔ `R01A.0012` 定义在**半成品子件 `S02Y.0207` 自己的 BOM 里**，而 `_substitute_groups` **只看成品当层**，故误判为缺口（举证 `F02N.0233`）。

### 🔴 授权的来龙去脉（这段必须读完再动手）

- 她 08-05 回件原话：「**这个问题如果没有需要我们的决策项**，替代料问题请你按照现有的理解，直接建造完成修改」。
- 但我方 08-03 信里明确告诉过她：「修好它会改变缺口／齐套计算结果，属于**需要你重新核对黄金基准并签字确认**的那类改动」（与净额开关翻 ON 同性质）。
- **⇒ 她的授权带前提，而前提（无需她参与）恰好不成立。**

**Shao Peishen 2026-08-05 拍板 (a)：开建，签字改到改完之后做。**

**签字没有被取消**，只是顺序变了。所以——

### 交付物硬性包含两样（缺一不可）

1. **修复前后的缺口／齐套数字对照**：哪些料号、哪些项目的数字变了、变了多少。
2. **黄金基准的重跑结果与差异说明**。

**这两样是她重核签字的依据。缺了她无法签，等于把一次口径变更的验收责任静默转移**——而那正是拍板时讨论的风险点（净额开关翻 ON 当初就是靠她签字才敢翻）。

---

## 三、发信（本批不发，但要知道下一步）

**§四 #52 已拍板 (b)**：等 #262 修完，把「答交已修复请复核」＋「替代料已按你授权开建、请重核黄金基准并签字」**合并成一封**发；07-29 那封判例包继续压着、**不并入本封**（否则触 3 决策点上限）。

**🔴 发信硬前置三条，缺一不得发（#228 教训）**：

1. 改动已 ff 合入 `master`；
2. `.51:8091` 已部署且冒烟通过（`/api/ping`／关键页 200／一次全量重算）；
3. **用她本次举证的 `R01A.1028` 端到端复现**——看板须显示 `10000 / 2026-11-25`。

**起草时 README「发送状态」列只能写 `⏳ 待你审`**（两态语义），起草前先占该 README 的编辑锁。

---

## 四、收工段

1. **#262／#263 各自回填**（不要合并成一段）：状态 ＋ 产出路径 ＋ **#262 的根因实测结论（是不是 60 天窗口，证实还是证伪）** ＋ 是否放大窗口及限流权衡 ＋ **#263 的两份对照材料路径**。
2. **部署留痕**必须写（#229 的 sweep 检查会看）。
3. 若排查中发现别的真问题，**当场追加待领行**，不在本批顺手扩范围。
4. 列出全部新产出/修改文件清单 + commit message；CC 自行 commit + push + **收工回来销 §二 批次行**（08-05 已连续两次出现"CC 自行 commit 后没销行、sweep 永远取不到"，见 #236）。
5. 收工自删 worktree（ahead=0 且 clean 时）；**删不净留空壳即可，不必第三次重试、也不必请 Shao Peishen 手动删**——既定处置是留 08-28 首期体检一并清（#98）。
