---
status: 待执行
title: "开场prompt ·【CC】运维群 webhook URL 验通 ＋ FI2 断供告警 .51 部署补配 ＋ #73 重评（§四 #67／#72／#73）"
created: 2026-08-19
执行方: CC
派出线: Cowork 全景路线图线
编号: OP-0819-E
来源: §四 #67 已由 Shao Peishen 2026-08-19 15:11 提供新 URL；#72 他答 (a)；#73 待重评
---

# OP-0819-E ·【CC】运维群 URL 验通 → FI2 部署补配 → #73 重评

## 开场词（复制即用）

```
【OP-0819-E】CC · 运维群webhook URL验通 + FI2断供告警.51部署补配 + #73重评（§四 #67/#72/#73）
【设置】执行环境：CC ｜ CC session：☑ 新建 ｜ worktree：☐ 不新建，复用当前所在 worktree（工作区干净即可）｜ 分支：master ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-CC-运维群URL验通与FI2部署-队列四67-72-73-2026-08-19.md 全文 ＋ 队列 §四 #67／#72／#73 ＋ §一 #82／#282 ＋ CLAUDE.md 当前进度恢复上下文，然后按本件三步依次执行。
```

## 前提（已满足）

Shao Peishen **2026-08-19 15:11 本地**已把新的运维群 webhook URL 写进根 `.env` 的 `WECOM_WEBHOOK_URL_OPS=`（走文件、未发聊天）。
Cowork 侧已只读核过：**键存在、值长 89、`key=` 格式正确**。
⚠️ **但长度与旧值相同（企微 webhook URL 定长），这只证明格式对、不证明它是新的** —— **验通是你的第一步，不是可跳过的形式**。

## 第一步 · 验通 URL（§四 #67）

用**残缺 payload 探针**验证：故意发一个字段不全的请求，看返回码。
- **验通判据**：返回 **非 `93000`**（`93000` ＝ webhook 地址无效／已失效，即上一轮实测到的那个）。参数错类返回码（如 `40008` 等）**说明地址本身是通的**，正是我们要的。
- 🔴 **必须确认不会有任何消息真正进群** —— 残缺 payload 的意义就在这里；**不要为了图省事发一条真实测试消息进运维群**。
- **失败怎么办**：仍返回 `93000` ⇒ **停下、回写 §四 #67 说明仍无效、不要继续第二步**（第二、三步都建立在它通的前提上）。

验通后回写 **§四 #67 销号**，并写明返回码与探测时刻（**不要回显 URL 本身**）。

## 第二步 · `.51` 部署与补配（§四 #72，他已答 (a)：一趟四关走完）

1. **部署代码**：`sync-to-server.ps1` 把 FI2 告警变量名修正（OP-0819-C 的产出，已在 master）推到 `.51`。
2. **补配 `.env`**：`.51` 上补 `WECOM_WEBHOOK_URL_OPS=`（值从本机根 `.env` 取，**走文件不走聊天、不落任何报告**）。
   🔴 **同时核 `.51` 上是否存在裸 `WECOM_WEBHOOK_URL`** —— 若存在且指向业务群，**这是一个正在生效的错发风险，优先处置**（`#82` 的告警读的就是裸键，OP-0819-C 已把读取口径改为优先 `_OPS`，但旧键留着仍是隐患）。
3. **真冒烟**：构造一次真实断供条件，确认告警**真的发到运维群**。
   🔴 **不接受只跑单测** —— `#82` 行内自陈至今「未做任何真实企微 webhook 调用」，**这个缺口就是本步要补的**。
4. 重启相关计划任务后**逐项验活**：进程 `CreationTime` 真的刷新（不要只信脚本打印的「已重启」）、`Fi2TaxExportDailyScan` `LastTaskResult=0`。

完成后回写 **§四 #72 销号** ＋ **§一 #82**（可评估是否具备销号条件）。

## 第三步 · `#73` 重评（🔴 只出结论、不动代码）

`#73` ＝ `#282` 那个**正在发生**的后果：本机 sweep 每轮的分叉告警／孤儿脏文件／部署留痕提示读的是**裸 `WECOM_WEBHOOK_URL`**，而根 `.env` 该键注释写明是「采购内部工作群」——**业务部门群此刻仍在收机制类告警**，正是 `#282` 要消灭的那件事。

**上一轮不能做 (b)（把 sweep 也改成只读 `_OPS`）的唯一理由是 `_OPS` 无效** ⇒ 第一步验通后，**那个理由消失了**。

🔴 **但本步只做重评、不改 sweep 代码**，理由两条：
1. 改 sweep 的告警通道命中 CLAUDE.md §5 门槛③（**改变既有模块对外语义**），**须走 openspec**；
2. `#282` 已有完整变更包设计在等（拆两包、含 design 审），**不该在它外面另开一条路**。

**本步产出** ＝ 在 §四 #73 行内写清三件事，**并把「现在就止血还是等 `#282`」作为定夺项交回 Shao Peishen**：
- ⑴ 验通后 (b) 的技术障碍是否确已消除（用第一步的实测返回码说话）；
- ⑵ 若走 (b)，**最小改动面**是什么（具体到哪个文件哪几处调用点），以及它是否真能只改读取键而不动其它语义；
- ⑶ `#282` 整体变更包目前的开工条件是否已随本次 URL 到位而满足（其前提①正是「运维群 webhook URL」）。

🔴 **不设默认项** —— 按根 CLAUDE.md §5「默认项两前提」，本项代价是「**正在发生**」而非「停在原地」，不得靠默认长期挂着。

## 收工

- 回写 §四 **#67 销号**／**#72 销号**／**#73 重评结论 ＋ 定夺项**；§一 **#82**／**#282** 相应追记。
- §二 登记批次，**文件清单每条必须是仓库根相对完整路径且反引号包裹**。
- 🔴 **URL 值一律不落任何文件、报告、commit message**。
- 若任一步未通过，**如实登记未完成、不要写 ✅**，并说明卡在哪一步。
