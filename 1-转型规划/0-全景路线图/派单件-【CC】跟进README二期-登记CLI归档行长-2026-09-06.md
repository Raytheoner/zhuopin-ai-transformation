---
title: "派单件 ·【CC】跟进 README 二期：登记 CLI ＋ 已闭环归档 ＋ 行长口径 ＋ 门禁"
created: 2026-09-06
执行方: CC（OP-0906-A）
来源: 构建环境瘦身第二轮-方案-2026-09-04.md §一 A3（b 案）；Shao Peishen 2026-09-06 答 (a)「现在派 CC」
status: 待执行
---

# 目标

`6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md`（177.7 KB，主表 102 行；`主要事项` 列 75 KB、`发送状态` 列 34 KB；37 行已闭环）——机器读它不费，**人读它费**：跟进信 skill 登记 `⏳ 待你审` 是 Edit 直改，Edit 前必 Read ⇒ 每封信吞 178 KB。二期把「人碰表」全部换成 CLI，再把表本身减到 ≈60 KB。

# 实测（2026-09-06 只读）

| 项 | 值 |
|---|---|
| 表头 | `编号 ｜ 日期 ｜ 收信人 ｜ 主要事项 ｜ 交期要点 ｜ 发送状态（2026-07-06）` |
| 行数／字节 | 102 行／159 KB；最长行 `财务部#16` 11.9 KB，>5 KB 共 12 行 |
| 已闭环（`📥 已回件并回灌`／状态首段 ✅） | 37 行 |
| 读取方（12） | `工具-跟进闸查询.py`、`工具-跟进信README查询.py`、`工具-共享文档编辑锁.py`（G 闸）、`工具-落库sweep.py`（待发信盘点）、`aibot_service/followup_readme_bridge.py`、`scripts/approve_followup_letter.py`／`dispatch_followup_letters.py`／`draft_gap_check.py`／`push_followup_letter.py` 及各自单测 |

# 四件（一个 openspec 变更包 `followup-readme-phase2`，先 propose）

1. **登记 CLI** `0-学习与工具/工具-跟进信README登记.py`：子命令 `append`（编号／日期／收信人／主要事项／交期要点／发送状态，编号按部门计数器自动取、未发出不占号）与 `set-status`（按编号整格改发送状态，状态枚举校验：`⏳ 待你审`／`🆕 待发`／`✅ 已推送…`／`📥 已回件并回灌`／`❌ 已作废`／`⏸ 暂缓`）；走 `工具-共享文档编辑锁.py --file <README>` 单文件锁；写前列数校验、写后回读；🔴 `主要事项` ≤600 B、`交期要点` ≤400 B，超限即拒并提示外置。附单测。
2. **已闭环归档**：`📥 已回件并回灌`／`❌ 已作废` 且发送日 >30 天的行整行迁 `6-人才与组织/部门AI专员跟进/README-归档-YYYYMM.md`（表头同、原文原样）。🔴 迁前逐个核 12 个读取方：只要活行的（闸查询＝每收信人最新一封；待发信盘点＝非终态行）照旧；需全量的（如按编号回填历史）改为「主表未命中再查归档件」。判据写进 design.md，单测覆盖「最新一封已归档 ⇒ 闸开」「归档编号不复用」。
3. **行长口径与外置**：现存 12 行 >5 KB 的 `发送状态` 历史段外置 `6-人才与组织/部门AI专员跟进/跟进信行日志/<部门#N>.md`（原文原样，行内留首段＋末段＋指针，同队列 K2）；`主要事项` 超 600 B 的行压成摘要、原文进同一日志件。编辑锁 release 校验族对 README 加同款行长判据（首周告警、一周后阻断，逃生阀 `行长豁免：`）。
4. **门禁与规则**：`hooks-pretooluse-queue-read-guard.ps1` 目标清单加 README 主表与归档件（Read 全文即拦，提示 `--digest`／闸查询／登记 CLI）；交付 rules/跟进信与专员 与 `zhuopin-followup-letter`／`zhuopin-send-followup` SKILL 的**改句建议**（登记只用 CLI、判状态只用查询），由 Cowork 在 CLI 验活后落字（先机制后降指针）。

# 验收
- 表 ≤60 KB；12 个读取方单测全绿；`工具-跟进闸查询.py --to 唐燕萍`／`README查询 --digest` 输出与改前一致（改前先留基线）。
- 用 CLI 真实追加一行测试信并 `set-status` 后删除（或用 `--dry-run`），审计可查；hook 拦截一次 Read README 留审计行。
- 不改表头、不改列序；不动 `.51`；不发企微。

# 触碰区
`0-学习与工具/工具-跟进信README登记.py`（新）＋单测、`工具-共享文档编辑锁.py`（README 行长判据）、`hooks-pretooluse-queue-read-guard.ps1`、README 主表（迁行／外置）、新归档件与行日志目录、openspec 变更包。**不碰**两份队列正文、业务场景看护件、`OP-0904-G` 已落地的预检逻辑。

---

# 二期续棒（2026-09-06 收工，OP-0906-A → 待续 OP-0906-B）

> 本节由 CC（`OP-0906-A`）收工时写入，供 Cowork 据此出 `OP-0906-B` 派单件。件①②已完成并合入 `master`；件③④未开工，另开新 session 承接（两者都要动 `工具-共享文档编辑锁.py`，一份 5184 行的既有文件，值得单独一个干净的 session）。

## 件①②：已完成

- **openspec 变更包** `followup-readme-phase2`（propose+specs+design+tasks，`validate --strict` 通过）已落库，四能力 spec 对应件①②③④，design.md 含完整决策记录（D1-D4）与「须 Shao Peishen 书面确认」段。
- **件①登记 CLI**：`0-学习与工具/工具-跟进信README登记.py`（`append`/`set-status`，18 单测）。commit `5e95bd5`。
- **件②已闭环归档**：`0-学习与工具/工具-跟进信README归档.py`（15 单测）；**已对真实 README 执行一次真实迁移**——19 行 → `README-归档-202609.md`，主表 177.7 KB → 144.1 KB。`工具-跟进信README查询.py` 已加 `--file` 支持核对归档件。commit `dc3e57a`／`e71dd54`。
- **Shao Peishen 2026-09-06 对 D2 分类的唯一补充**（已实现）：`followup_readme_bridge.py` stem 匹配须先查活表、未命中再查归档件，否则归档会让老信 stem 匹配从命中变未命中、误配给该部门当前最新一封活信。
- **迁移中现场发现并修复的两处真实缺口**（D2 走查未覆盖到，非分类判错）：
  1. `工具-跟进闸查询.py::_next_available_number` 此前只扫主表——部门历史高编号信已归档、主表仅剩低编号在途信时取号会往回算、与刚归档编号相撞，直接违反「归档编号不复用」。已改为同时扫归档件取两者最大值。
  2. `build_report`/`all_recipients` 此前把「主表已无该收信人任何行」一律当「这个人不存在」报错——真实归档批次里「销售部 · 泓钦」唯一信被归档后当场复现（`--to 泓钦` 从闸开退化为报错）。已改为先查归档件，命中则报「无在途、闸开」。
  - **给续棒 session 的提醒**：件③④落地时如果也涉及"某个既有读取方在归档/行长外置后需要感知归档件/日志件"的场景，先假设它需要、走查一遍代码确认，不要照搬件②"9 个读取方全活行"的现成结论直接套用——那个结论是对件②这批 9 个读取方成立，不是普遍真理。

## 件③行长口径与外置：未开工，决策点

- **现状已随件②归档变化，须以此为准、不要沿用派单件开头"现存 12 行 >5KB"的旧数字**：2026-09-06 归档后实测——`发送状态` 列 >5KB 的行**已归零**（原先的长历史行大多是"回件并回灌+多段原状态回溯"的老信，已随>30天终态一起被归档带走）；但 `主要事项` 列 >600B 的行现有 **25 行**（`采购部#19` 6843 B／`财务部#16` 7020 B 最长）。
- **决策点 D3-1**：行长阈值判据该挂在哪：沿用 design.md 原方案——复用队列 `⑪` 同款代码路径（`_row_length_warnings_and_violations`/`_has_genuine_row_length_waiver`/`ROW_LENGTH_CAP_BYTES` 等），把 `_ROW_LENGTH_CHECK_INDEX`（现只认 `"一"`/`"四"` 两个 label）扩展出一个 README 专属 label，映射到「发送状态」列索引；`主要事项` 600B 阈值与队列 4KB 统一阈值不同，需要单独一套判据常量，不能直接复用 `ROW_LENGTH_CAP_BYTES`。
- **决策点 D3-2**：`主要事项`超 600B 压缩为摘要——摘要生成规则未定（派单件原文只说"压成摘要"，未给算法）：是否比照 `message_length.py`（跟进信长度守卫，`aibot_service`）已有的"标题+小节标题+两处告知"摘要生成套路，还是另定一套更简单的规则（如取前 N 字 + 指针）？建议后者更简单可靠。
- **决策点 D3-3**：外置目录 `跟进信行日志/<部门#N>.md` 尚未创建，命名与内容格式（原文原样，首段+末段+指针）需要在实现前用 1-2 个真实行走一遍手工示例，确认格式对人可读、对未来"归档"这套已有代码（`iter_rows`）不构成新的解析负担（行日志文件不需要被 `iter_rows` 解析，纯人读，风险较低）。
- 单测覆盖：告警模式（首周）与阻断模式（一周后）两个模式的行为均需覆盖，阻断模式的启用时间点需要写死一个具体日期（同队列⑪先例，当时是 `2026-09-11`）。

## 件④门禁与规则：未开工，决策点

- **决策点 D4-1**：`hooks-pretooluse-queue-read-guard.ps1` 的 `$script:ProtectedExactPaths`／`$script:ProtectedArchiveNameRegex` 扩展方案已在 design.md D4 写清楚（复用同一正则形式），可直接照做，唯一需要现场核对的是**机制工具白名单**要把这次新增的三个工具都加进去：`工具-跟进信README登记.py`、`工具-跟进信README归档.py`（件①②新建，尚未加入白名单）、`工具-跟进信README查询.py`（原已存在但同样需要确认在白名单内，否则它自己读 README 会被自己新加的门禁拦下）。
- **决策点 D4-2**：rules/SKILL 改句建议文本——本包（D1/D2）目前**尚未产出**这部分建议文本（`.claude/rules/跟进信与专员.md` 与 `zhuopin-followup-letter`／`zhuopin-send-followup` SKILL 的"登记只用 CLI、判状态只用查询"字样），需要件④落地时一并补上，改字仍归 Cowork（本包/续棒包均只交付建议文本，不自改）。
- 验收沿用派单件原文：hook 拦截一次真实 Read README 留审计行；`test_hooks-pretooluse-queue-read-guard.py` 补两类用例。

## 收尾时一并核对（件③④完工后）

- 主表≤60KB 这条验收——件②后已到 144.1 KB，件③外置 25 行 `主要事项`（累计约 60KB 超限部分）后估算可落到目标区间内，实现时请实测确认，不要只凭估算判完工。
- 队列 §一 `#490` 到时回写销号；rules/SKILL 改句建议文本移交 Cowork 落字后，本轮"跟进 README 二期"方可视为整体完工。
