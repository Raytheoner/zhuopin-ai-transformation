---
title: "看护件 · 泳道看护批 B-0909_两包apply（2026-09-09）"
created: 2026-09-09
status: 待派出
批次: B-0909_两包apply
派出线: Cowork 业务总线 OP-0908-V
---

# 看护件 · 泳道看护批 `B-0909_两包apply`

## 〇、口径

- **触发**：Shao Peishen 2026-09-09「开启泳道看护」。规则正本＝`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`，判据正本＝`openspec/changes/lane-watch-mode/design.md`。
- **D1 四档现取**（`工具-泳道看护状态机.py criteria`，非手抄）：🟢 六键（含 `worktree_local_build`）／🟡 四键／⏭️ `deploy_51`／🔴 四键。**判据未覆盖按 🟡 停**。
- **前置四查（2026-09-09 07:31–07:35 本机现取）**：① LAN 探针 ＝ **⛔ off-LAN（三项未齐）** ⇒ 排除依赖内网的活；② `git fsck` 仅 dangling、`ahead=0 behind=0` 与 origin 对齐；③ 编辑锁无占用；④ 上一批 `B-0908_四泳道机制根治` summary 已出（停 6 次，5 次已答）。
- **交付模式**：CC 看护者单粘贴 → 看护者用 Task/Agent 按 §三 扇出。**并行上限 4、错峰 ≥90 s**；本批 **3 条**，一波跑完。
- ⚠️ **批次名沿用 `B-0909_两包apply`**（§二 登记在先、不改名），实含三条泳道 —— 第三条 `440-eval-review` 系 Shao Peishen 2026-09-09 答 `1a` 后加派，**只读出审读件、不改该变更包一个字节**。

## 一、硬边界（继承，不重述细则）

- 🔴 **三条泳道收工都停在「可 ff 态」，泳道一律不自合**——ff 是 🟡 档 `merge_to_master`。
- ✅ **本批 ff 已获放行，由看护者收工时串行做**（Shao Peishen 2026-09-09 答 `3a`；依据＝SKILL 步骤 4「主仓 ff 由 sweep 收尾段做，sweep 不可用时看护者收工串行做」）。**放行范围恰好三条、不外扩**：⑴ `#500` sweep 常驻执行体脏检查放宽；⑵ `#505` `append-row` 高水位线回写（`claude/op0908x-505-hwm-apply-a1e02f` @ `d890808`）；⑶ `#489` 两笔（`2613969`＋`7cb578a`）。🔴 **`#489` 那条 CC 会话本身归他自行处置**（他 2026-09-09 明示），看护者只做 ff 这一个动作、不碰该 worktree、不接手其收尾件。
- ⚠️ **ff 判据用内容口径、不用 `--no-merged`**：本项目走 ff-only ＋ rebase，**老分支 SHA 恒非祖先**，`git branch -r --no-merged master` 会把 60+ 条早已落地的分支报成「未 ff」（2026-09-09 实测 67 条，绝大多数是假阳性；同族实证见 `71730ad`，该 commit 已把 `#489` 的前置判据从 merge-base 改为 `cat-file` 内容口径）。**逐条 ff 前先用内容口径确认它确实没落地。**
- 🔴 **⏭️ 档（`.51` 部署及任何触碰生产服务的动作）本包不执行**，自动 `transfer-out` 标注去向，交 `zhuopin-lan-closeout`。本批 off-LAN，`.51` 项本就不在候选内。
- 🔴 **🔴 档（对外发送／L2 门禁签字／合规红线变更／ASIL C-D）永不代办**，只做到可发送态即停。
- 🔴 **业务数据与一切对外发送永不自动入批**；真实库写动作／规划文档／他线在办行不碰。
- 🔴 **看护者自己不 commit、不 ff master**；撞 `.git/index.lock` 不重试，按「登记待补」写队列行。
- 🔴 **`#489` 及其 CC 端会话不在本批范围**（Shao Peishen 2026-09-09 明示「CC端会话，你别管了」）——其收尾件 `派单件-【CC】489收尾-…-2026-09-09.md` 由他自行处置，本批**不碰 `opener骨架.md`／`工具-opener块lint.py`／该 worktree**。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 硬序 |
|---|---|---|---|
| `481-docx-reader` | §一 `#481` apply | docx 读取相关模块与其调用点、其单测；`openspec/changes/unified-docx-checkbox-reader/` | 无前置，可立即开工 |
| `504-heartbeat-vis` | §一 `#504` apply | `0-学习与工具/工具-泳道看护状态机.py` ＋ `test_工具-泳道看护状态机.py`；`openspec/changes/lane-watch-heartbeat-visibility/` | 无前置；🔴 §五 装载器子项**本泳道不做**，见下 |
| `440-eval-review` | §一 `#440` design 审读件 | **只读** `openspec/changes/discipline-eval-suite/`；新建 `1-转型规划/0-全景路线图/design审读件-discipline-eval-suite-2026-09-09.md` | 无前置；🔴 只出审读件，**不替他审、不取值、不 apply** |

**两两比对结论：三者触碰区零重叠**（docx 模块族 ／ 状态机族 ／ 只读＋新建件），可并行。`#481`／`#504` 的 design 审均已过（分别为 Shao Peishen 2026-09-07 合审 §3、2026-09-08 答 1(a)），本批只做 apply、**不重开 design 审**；`#440` 的 design **尚未审**，故本批只出审读件、不 apply。

✅ **`#504` §五 装载器子项的归属已定**（Shao Peishen 2026-09-09 答 `2a` ＝ 按 (x)）：该项须 claude.ai 侧 `save_skill`，**CC 无此能力**（`#504` 行内已实证），故**不进 CC 泳道，由 Cowork 业务总线 `OP-0908-V` 本会话执行**。泳道回写时标「已答 2a，归 Cowork，勿重做」——🔴 **同一题不得两端各答一次**。

⚠️ **`504-heartbeat-vis` 改的正是本批看护在用的状态机** —— worktree 内改动不影响主 checkout 的 master 版本，冲突只在 ff 那一刻，而 ff 本就停等；**故可并行，非隐患**。

### LAN 留步（off-LAN 排除项，收工汇总列「待回内网」）

- `#506` 的 `.51` 部署与全量重算复跑（Shao Peishen 已答「合」放行合入 master，但**部署仍须另起 LAN 收口批**）——`采购部#22` 解押的必经一步，⏰ 参照 09-11。
- `#500` 现网 165 个提交的清账（⏭️ 档，已交 `B-0907_Q`）。

## 三、泳道 opener（每条三行结构，`工具-opener批处理执行v2.ps1` 解析锚点）

### A1 · #481 统一 docx 读取工具 apply

粘贴端：CC ｜ 泳道：481-docx-reader

```
[OP-0909-F]【CC】481docx
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909f-docx-reader-apply`） ｜ worktree：☑（复用 op0907h-docx-reader-481 分支，rebase 到 master 后施工） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-V（泳道看护批 B-0909_两包apply）
读 ① `openspec/changes/unified-docx-checkbox-reader/tasks.md` → ② `CLAUDE.md` §§4／§5 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 🔴【设置】行分支字段以本条为准：**沿用既有分支 claude/op0907h-docx-reader-481**，开工第一步＝rebase 到 master，再在该包 design.md 顶部回填一行「design 审：✅ 已过 —— Shao Peishen 2026-09-07（合审 §3）」
2. 按该包 tasks.md 逐条施工：建统一 docx 读取工具、收编分散实现、迁移 tasks §3 列出的调用点
3. 🔴 单测与回归：新工具单测 ＋ 全量回归；自陈零回归须附手段（同一组失败在纯 master 零改动上同命令逐条复现）
4. 收工回写 §一 #481 并登记 §二 批次；🔴 文件清单每个路径必须包反引号——裸路径 sweep 提取不到、会静默掉在地上（2026-09-08 实撞）

不做什么：
- 🔴 ff 合入 master 不做——🟡 档，收工只 push 本分支，停等 Shao Peishen 一字母
- 不改 lint／编辑锁／sweep 工具族；不碰 opener骨架.md 与 #489 那个 worktree（不在本批范围）
- 不碰 .51／企微／真实库／规划文档
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A2 · #504 心跳件跨 worktree 可见性 apply

粘贴端：CC ｜ 泳道：504-heartbeat-vis

```
[OP-0909-E]【CC】504心跳
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909e-heartbeat-vis-apply`） ｜ worktree：☑（新建，从 master 起） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-V（泳道看护批 B-0909_两包apply）
读 ① `openspec/changes/lane-watch-heartbeat-visibility/tasks.md` → ② `CLAUDE.md` §§4／§5 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 按该包 tasks.md 施工：心跳写侧复用与读侧同一个 REPO_ROOT 常量 ＋ 两侧同锚的断言单测（🔴 不改 _resolve_repo_root()，那会同时打断编辑锁）
2. 🔴 单测与回归：pytest test_工具-泳道看护状态机.py 全绿 ＋ 全量零回归，自陈零回归须附手段
3. 收工回写 §一 #504 并登记 §二 批次；🔴 文件清单每个路径必须包反引号——裸路径 sweep 提取不到、会静默掉在地上（2026-09-08 实撞）

不做什么：
- 🔴 该包 §五「claude.ai 侧引用式装载器『已知缺陷』回改归属」子项本泳道不做——Shao Peishen 2026-09-09 已答 2a 确认按 (x)，但该项须 save_skill 能力、CC 侧没有，由 Cowork 业务总线执行；本泳道跳过并在回写里标注「已答2a，归 Cowork，勿重做」
- 🔴 ff 合入 master 不做——🟡 档，收工只 push 本分支；本批 ff 由看护者收工时串行做（他已答 3a 放行）
- 不碰 .51／企微／真实库；不改 lint／编辑锁／sweep 工具族
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A3 · #440 skill eval 套件 · design 审读件

粘贴端：CC ｜ 泳道：440-eval-review

```
[OP-0909-I]【CC】440审读件
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909i-eval-suite-review`） ｜ worktree：☑（新建，从 master 起） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-V（泳道看护批 B-0909_两包apply）
读 ① `openspec/changes/discipline-eval-suite/design.md` → ② `CLAUDE.md` §§4／§5 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 读 openspec/changes/discipline-eval-suite/ 四件（proposal／design／tasks／specs），出一份 design 审读件到 1-转型规划/0-全景路线图/design审读件-discipline-eval-suite-2026-09-09.md
2. 🔴 审读件的唯一目的是让 Shao Peishen 不必读整包就能答字母：逐个决策点写成 (a)/(b)/(c) ＋ 每项写清「选它会发生什么与代价」＋ 标默认项；🔴 无默认项的要明写「本项无默认，须你明确答复」并说明不答的代价是「停在原地」还是「错误继续发生」
3. 🔴 特别核清 CI 里 ANTHROPIC_API_KEY 的凭据边界候选与各自代价（该包 design 已列，审读件须原样转达不得自行取舍）
4. 收工回写 §一 #440 并登记 §二 批次；🔴 文件清单每个路径必须包反引号——裸路径 sweep 提取不到、会静默掉在地上（2026-09-08 实撞）

不做什么：
- 🔴 不替他做 design 审、不自行取值、不改该变更包任何一件——本棒只出审读件，审是他的动作
- 🔴 不 apply 该包任何 tasks；不建 evals/ 目录、不改 CI
- 不碰 .51／企微／真实库；不改 lint／编辑锁／sweep 工具族
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

## 三bis · 看护者开场词

```
[OP-0909-H]【CC】看护两包apply
【设置】执行环境：CC ｜ 分支：lane-watch-0909 ｜ worktree：☐（看护者不建，泳道各自处理） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-V
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0909H-看护两包apply。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0909_两包apply-2026-09-09.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0909_两包apply` **现取**，不手工数。必含：每泳道一行 ＋ 心跳全文 ＋ D6「本批停 N 次｜逐次：泳道／档位／停在什么动作／他答了什么／等了多久」＋「本批转出 N 项」＋「⏭️ 档授权放行 N 次」＋「待回内网」清单 ＋ 末尾「待你一字母」清单（同类合并，design 审类排 merge 类之前）。

🔴 **等待时长只认 `reports/lane-watch-state.json`（由 summary 现取），会话不得自算**——自算已实撞误报。
