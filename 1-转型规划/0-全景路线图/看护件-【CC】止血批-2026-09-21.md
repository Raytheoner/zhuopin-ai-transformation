---
title: 看护件 · 【CC】token 止血批（`#637`＋`#638`／`#639`）
created: 2026-09-21
派出线: 环境总线 Win-0921-A
批次: B-0921_止血
status: 待执行
---

# 看护件 · 【CC】token 止血批 · `B-0921_止血`

## 〇、口径

🔴 **本批只做止血，业务 task 一律不动**（Shao Peishen 2026-09-21 直令：先聚焦 token 止血，业务 task 等止血修复再动）。
🔴 **看护件按泳道切片**：做什么／不做什么全在两份派单件里（5,714 B／4,721 B），opener 的 `--input-pointer` 直指自己那一份，**不整读本件**。
🔴 **两行同批同 session**：`#637`＋`#638` 由 A1 一条 session 做完（Shao Peishen 2026-09-21 裁定，判据可复用，勿分两次）。
🔴 D1 四档判据现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不复述；判据未覆盖按 🟡 停。

## 一、硬边界继承

1. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
2. 🔴 **别往 `.claude/rules/` 与根 `CLAUDE.md` 加字**——2026-09-21 刚缩到线内（rules 合计 30,310／30,720，根 `CLAUDE.md` 12,077／12,288）。
3. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。🔴 **`.claude/worktrees/agent-a15b4851f659218aa` 里有 `#455` 的真实未提交改动，谁都不许清、不许动。**
4. 🔴 **禁起后台子任务／并行 Agent**——`OP-0921-C` 正因 claude 自身 600 秒上限被掐断，一棒白跑。顺序、inline 做完。
5. 🔴 回归 narrow 到受影响测试类（node-id，禁 `-k`）；不跑整份回归；测试输出重定向到文件只读结论行。
6. 🔴 收工前必跑 `git diff --name-only master...HEAD` 并贴进收工报告；期望产出点名的文件缺一个不许写 `OPENER_DONE`。
7. 🔴 收工只 push 自己那条分支，不碰主仓、不 ff master。
8. 🔴 **量体量一律 `git cat-file -s <rev>:<路径>` 看 blob**，不用磁盘 `.Length`（CRLF 已骗过一次：根 `CLAUDE.md` 磁盘 12,146 vs blob 12,077）。

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#637`＋`#638` | `止血线` | `0-学习与工具/_输出截流.py`；`0-学习与工具/工具-落库sweep.py`＋其单测；`0-学习与工具/工具-泳道看护等待.py`；告警/lint 守落点 | 派单件＝`派单件-【CC】637-638-截流必留与等待形状-2026-09-21.md` |
| 1 | A2 | `#639` | `止血线`（**串在 A1 之后**） | 定位读取器新文件；`0-学习与工具/工具-opener块lint.py` 或 sweep 常驻类；`hooks-posttooluse-context-meter.ps1`／`工具-opener批处理执行v2.ps1` | 派单件＝`派单件-【CC】639-泳道读取面切分-2026-09-21.md`。🔴 与 A1 同触碰 sweep 家族，故同泳道严格串行 |

单泳道两棒串行，无跨泳道并行；无 ⏭️ 转出项、无「LAN 留步」项。
🔴 **ff 前后序（看护者记账，🟡 档待他一字母）**：A1 先 ff，A2 分支随后 rebase 再 ff。

## 三、泳道 opener

### A1 · `#637`＋`#638` 输出截流必留 ＋ 看护等待形状机器守（§一 #637／#638）

粘贴端：CC ｜ 泳道：止血线

```
[OP-0921-H]【CC】637-638截流等待
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0921h-637-638-truncate-wait`） ｜ worktree：☑（op0921h-637-638-truncate-wait，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】637-638-截流必留与等待形状-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921h-637-638-truncate-wait --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921h-637-638-truncate-wait --done --batch B-0921_止血 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · `#639` 泳道读取面切分 ＋ 上下文闸轮询低估（§一 #639）

粘贴端：CC ｜ 泳道：止血线

```
[OP-0921-I]【CC】639读取面切分
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0921i-639-lane-readface`） ｜ worktree：☑（op0921i-639-lane-readface，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】639-泳道读取面切分-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921i-639-lane-readface --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921i-639-lane-readface --done --batch B-0921_止血 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头（`工具-opener批处理执行v2.ps1 -Detach`），本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0921-J]【CC】看护止血批
【设置】执行环境：CC ｜ 分支：0921-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0921J-看护止血批。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】止血批-2026-09-21.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为该泳道起子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。🔴 A1 与 A2 同触碰区，必须串行：A1 收工后再起 A2。
🔴 等待一律用 `python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0921_止血 --max-wait 540`，**禁裸 `sleep` 轮询**（队列 §一 `#638`）。
```

## 四、收工汇总

```
python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0921_止血 --max-wait 540
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0921_止血
```

🔴 **本批验收判据（机器可判，缺一不算修好）**：
1. `test_工具-落库sweep.py::OrphanSectionFourEscalationTests` 转绿，且同族「发现即回显、无命中亦不省略」的调用点逐点接上 must-keep（列出改了哪几类；口径原文见派单件 §一）。
2. `#638` 的机器守能判出「看护/泳道语境下的裸 `sleep` 等待」并指名 `工具-泳道看护等待.py`，**自带消费者**，沙箱夹具同批交付。
3. `#639` 的定位读取器可用、已接进泳道默认路径的机器守；上下文闸低估**给出改后误差实测数**（同一条泳道闸读 vs 度量工具算出，现基线差 7,565）。
4. 🔴 **最终验收＝实测**：跑完用 `python 0-学习与工具/工具-Token用量度量.py --since <当日> --until <当日>` 出本批峰值／请求数／`cache_read`，与 2026-09-21 四条基线（峰值 242,594／230,874／212,522／205,114，中位 221,698）逐条对照写进收工件。
