---
title: "构建环境全局扫描 · 待评估报告（只读取证，未动任何文件、未占锁）"
created: 2026-09-04
来源: Shao Peishen 2026-09-04「再把构建环境全局扫描一遍，是否还有类似需要优化的问题」；Cowork 环境总线瘦身线
status: 待评估
关联: 根CLAUDE.md彻底瘦身-方案-2026-09-03.md；跨桌任务队列瘦身-方案-2026-09-04.md（同族方法：去 provenance／外置／读侧禁通读／机器守）
---

# 构建环境全局扫描 · 待评估报告

> 扫描面＝「什么会进 session 上下文」＋「哪些机制在静默失效」。两天已治的根 CLAUDE.md／rules／kickoff skill／机制环境队列不再列。下表按**每次开场或每次动作吃多少 × 频率**排序，估算按 1 KB 中文 ≈ 0.5–0.7k tokens。

## 一、体积类（同一种病：状态列当日志、成因跟着规则走）

| # | 载体 | 实测 | 谁在吃 | 同法处方 | 省多少 |
|---|---|---|---|---|---|
| A1 | **场景级 `CLAUDE.md`** 16 份 240 KB：`wecom-aibot-service` 48.6 KB、FI2 39.4、QD-B 32.7、SC8 25.0、SC2 21.5 | CC 触碰场景目录即整份注入（P0 实证）；建造 session 开场＝根 10＋rules 9.4＋场景 25–49 KB | 「§5 状态时间线」外置 `docs/状态时间线.md`（行内留最近 5 条）；成因迁场景 CHANGELOG；目标单份 ≤8 KB（sweep 现阈值 50 KB 形同虚设，应降到 10 KB） | 建造 session −20~40 KB/次 |
| A2 | **`专线opener模板库.md` 94.5 KB**，§〇 占 77 KB，§〇.0 一节 17.5 KB（全是会话名沿革） | 每份 opener 必 Read §〇.00；session 一旦整读＝≈60k tokens | §〇.00 骨架独立成 `opener骨架.md`（≤3 KB，唯一可照抄物）；§〇.1–.19 去 provenance 迁 CHANGELOG 附录 J；模板库正文目标 ≤15 KB | 每份 opener −40~90 KB |
| A3 | **跟进 README 214 KB**：102 行表（12 行 >5 KB）＋55 KB 机制正文 | 跟进信线每封信必读（信状态唯一权威）；6-人才/CLAUDE.md 另 10.5 KB | 拆两件：`跟进信状态清单.md`（纯表、机器读，已闭环行迁归档）＋`跟进机制.md`（正文去 provenance ≤10 KB）；`工具-跟进闸查询.py` 已是精准读入口，规则改为「判信状态只用闸查询」 | 每封信 −150~200 KB |
| A4 | **业务场景队列 721 KB**：§一 44 行 584 KB，26 行 >10 KB，最长 43 KB | 与机制环境队列同病；行长校验⑪ 9-11 起会对它阻断 | K2 同法外置（脚本已有，反引号感知版）；K1 §二 61 行仅 1 行 ✅（登记格式问题，见 B1） | −60% |
| A5 | **取证方法知识库 37 KB**（34 处日期） | 路由表「取证／时间戳」指向它 | 判据版（同 P1 手法）≤12 KB，原文存附录 | −25 KB/次 |
| A6 | **文档台账-自动生成 109 KB** | R1「找文档先查台账、不翻目录」＝每次 109 KB | 台账生成器加 `--grep`／按目录分片，规则改为只用查询 | −100 KB/次 |
| A7 | 全景规划 165 KB／CHANGELOG 376 KB／归档件 2.9 MB＋0.3 MB | 按需读，但无「禁通读」规则；CHANGELOG 附录 A–I 只该 grep | 三者加入 ⓗ2 门禁清单（Read 全文即拦，提示 grep/`--digest --file`） | 防爆而非减量 |
| A8 | `.claude/settings.local.json` 49 KB、**425 条 allow 规则**，含 OneDrive 旧路径 | CC 每 session 加载；旧路径规则永不命中 | 清理 OneDrive 旧路径与一次性 awk/grep 规则，目标 ≤80 条 | 小，但去噪 |

## 二、机制健康类（比体积要紧——静默失效正在发生）

| # | 现象（实测） | 后果 | 处方 |
|---|---|---|---|
| B1 | **sweep 每轮 13 个批次被跳过**（清单无反引号／路径写错／覆盖校验未过），最早 08-30；登记方零反馈（只写日志） | 改动裸露在主仓 → 今天真分叉的根因；批次登记格式错误一天三次（A7／A13／A11） | ① `append-row --section 二` 预检：每个反引号路径须存在于 git 脏集或为新文件，否则拒登（机器守，CC 半天）；② 值周一次性清理存量 13 批次；③ 释放时若主仓有未被任何批次覆盖的队列改动，sweep 对队列文件即刻 commit（不等批次） |
| B2 | **27 个 worktree，12 个 `git status` 失败**（08-10 起），85 个远端分支 | 未闭合产出扫描「判据不可用」，每轮告警变噪音 | 月度体检 #98 触点提前；孤儿 worktree 三桶扫描后按协议〇.5 手法清理（CC，不用 `git worktree remove`） |
| B3 | sweep 收尾 rebase 被别线脏文件卡住（#425 族第 N 例） | 每逢 CC 直推 master 即分叉 | `_reconcile_with_origin_and_push` 支持 `--autostash`（仅对不在任何批次清单的脏文件） |
| B4 | `reports/sweep-commit.log` 3.2 MB／24,307 行，无轮转；`hooks-audit.jsonl` 持续增长 | 「看末几行」越来越慢；取证 grep 命中历史噪音 | 按周轮转＋保留 4 周 |
| B5 | 每轮「决策提醒第二载体调用异常」；全局 `~/.claude/CLAUDE.md` L57 死路径；`wecom-service-home` 有未提交改动挡住常驻执行体同步 | 三个「用来发现问题的东西自己坏了」（#398 族） | 归 #398 一并收口，值周定级 |
| B6 | CI 长期红（#398⑶） | lint／覆盖率门禁失去信号 | 同上 |
| B7 | Cowork 侧队列写入无 hook 守（ⓐ–ⓓ、ⓗ2 仅 CC） | 两桌规则不对称，Cowork 仍纯人守 | 已知边界；可选：sweep 对队列文件加「结构 lint 不过即拒 commit」作 Cowork 侧事后闸 |

## 三、载体漂移残余（同一规则多份副本）

| # | 位置 | 处方 |
|---|---|---|
| C1 | 协议〇（判据版）↔ `rules/队列与落库.md`：同题两载体，rules 已声明「协议〇为正本」，但 rules 仍复述取号/锁细节 | rules 只留路由句＋Cowork 特有句，其余指向协议〇条号 |
| C2 | 场景 CLAUDE.md 内复述合规红线／L2 门禁／固定流程（未逐份核，按 wecom 48 KB 推断） | A1 处理时一并去重，改为指向 `rules/场景建造与合规.md` |
| C3 | 3 份 skill >14 KB（followup-letter 35.5、send-followup 18.7、lane-watch 18.4）含大量成因 | 同 kickoff v1.25 手法：判据版＋CHANGELOG |

## 四、建议顺序（都是 Cowork 可做的 .md 活，机器守部分交 CC）

1. **B1**（先止血：批次预检＋清 13 批次）→ 2. **A2**（模板库拆骨架，每份 opener 立省）→ 3. **A1**（五份 >20 KB 场景 CLAUDE.md 外置时间线）→ 4. **A3**（跟进 README 拆表）→ 5. **A4**（业务场景队列 K2）→ 6. A5/A6/A7/A8 → 7. B2–B6 归值周巡检与 #98/#398。

## 五、预期

| 场景 | 现状开场/动作读入 | 全部落地后 |
|---|---|---|
| CC 建造 session 开场 | 根 10＋rules 9＋场景 25–49 ≈ 45–70 KB | ≈ 25–30 KB |
| 出一份 opener | 模板库 94 KB（整读）| ≤5 KB（骨架文件） |
| 写一封跟进信 | README 214＋6-人才 10.5 ≈ 225 KB | ≈ 20 KB |
| sweep 跳过批次 | 13/轮 | 0（登记即拒错） |
