---
title: "看护件 · 泳道看护批 B-0903_30（第五批：八包 apply ＋ criteria_signoff 收底座）"
created: 2026-09-03
批次: B-0903_30
派出线: Cowork 业务总线 OP-0903-C
前批: B-0903_20（3 泳道，八包 proposal 已由 Shao Peishen 2026-09-03 全部放行）
status: 执行中
---

# 看护件 · 泳道看护批 `B-0903_30`

## 〇、本批口径

前批八个变更包（采购 3 ＋ 财务 5）的 proposal **已由 Shao Peishen 2026-09-03 全部放行**，本批做 apply 与三项连带裁决的落地。

🔴 **放行依据必须原样传下去，免得后人误读**：八包**没有做任何判据决定** —— 每一项未签认的判据都落成 `config` 的 `None` 并由 CI 用例守住（`test_unsigned_criteria_stay_none`）⇒ **放行不会让任何错误口径进入系统，因为口径根本还没被填**。
🔴 **放行的只是「可以往下建骨架」，不是「可以填口径」。**

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。
📌 **新判例**：`spec_sync_to_main_specs` 已由 Shao Peishen 2026-09-03 裁为 **🟢**（sync 只写 `openspec/specs/`、不碰代码、git 可回滚）。⚠️ 该裁决**尚未回灌进 D1 表**（属机制线改码，业务总线不代改）⇒ 本批泳道若跑 `classify` 仍会得到 fail-safe 的 🟡，**以本节这句为准，不必为它停**。

## 一、硬边界（继承，逐条适用）

1. 🔴 **⏭️ 档：`.51` 部署及任何触碰生产服务的动作永不执行**。
2. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 永不代办**。
3. 🔴 **业务*数据*与一切对外发送永不自动入批**；真实库**只读**。
4. 🔴 **所有 `pause` / `check-heartbeat` 一律带 `--no-notify`**。
5. 🔴 **判据 `None` ＋ CI 守不得松动** —— 任何人不得在本批填任何一个未签认的判据值。
6. 🔴 **三闸全锁**（唐燕萍／姚祖怡／陈忱）⇒ 不得起草或发送任何跟进信。
7. 🔴 **规划文档一律不改**（全景规划／实施计划／权威排期表／前置总表）—— 走移交单。

## 二、波次与触碰区矩阵

| 波 | 泳道名 | 项 | 触碰区 | 说明 |
|:--:|---|:--:|---|---|
| 1 | `proc-apply` | A1 | 采购三包、SC4 移交单 | 无重叠 |
| 1 | `sc8-spec-note` | A2 | `openspec/specs/delivery-date-forecast/` | 无重叠 |
| 1 | `fin-criteria` | A3→A4 | `5-平台底座/`、财务五包 | 🔴 A3→A4 **必须串行** |

🔴 **A3（底座建 `criteria_signoff`）必须排在 A4（五包 apply 并引用它）之前** —— 否则五包会各自 apply 掉本地实现，再收编就是二次返工，正是 rule-of-three 要避免的那件事。

## 三、泳道 opener

### A1 · 采购三包 apply ＋ 移交单补充

粘贴端：CC ｜ 泳道：proc-apply

```
[OP-0903-C1]【CC】采购三包apply
【设置】执行环境：CC ｜ 分支：master（从 `claude/op0903b2-proc-scenarios` 起 `claude/op0903c1-proc-apply`，🔴 **不是从 master 起**——三包在前者上）｜ worktree：☑（proc-apply，新 worktree，收工自删）｜ 工作区：无（openspec 与移交单，🔴 不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_30
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903C1-采购apply。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_30-proc-apply.md`，开工 1 分钟内写首行，每里程碑追加，超 10 分钟补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_30 --wave 1 --lane proc-apply --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① 分支 `claude/op0903b2-proc-scenarios` 上的三个包 → ② `1-转型规划/0-全景路线图/移交单-SC4顺延与前置总表补5行-全景路线图线执行重组循环-2026-09-03.md` → ③ 队列 §一 #467/#468/#469。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 裁决（原文）：
  FF-1=(a) 三包（含财务五包）**全部放行、允许进 apply**；
  EE-5=(a) SC10「物料优先选用级别口径」与 SC11「物流距离矩阵及其口径」**补进前置总表**
        ⇒ 🔴 属规划文档改动，**并进已有那份移交单**（同一份一次做完），本泳道**不得直接改总表**；
  EE-6=(a) **SC4 顺延目标月定 2026-11**（移交单已给算术下界 2026-10-29，取其后第一个整月留缓冲）。

做什么：
1. 三包各跑 `/opsx:sync`（🔴 **不是 `apply`** —— 本仓库 `/opsx:apply` ＝ 实现 tasks 待办，与「不进实现」冲突；
   能写 `openspec/specs/` 的是 `sync`，判法同队列 `#299`。这是 2026-09-03 `OP-0903-B1` 实撞并已确认的结论）。
2. 重跑 `openspec validate --all --strict`，须绿。
3. **改那份移交单**（不是新建）：① 补进 SC10／SC11 两项口径条目 ② 把 SC4 顺延目标月写为 **2026-11**
   并注明拍板人与日期、保留算术下界 2026-10-29 的推导。
4. 按「完工即归档纪律」判能否归档：🔴 **tasks 未全 [x] 就不归档**，按三条入口之一留机器认得的说明。
5. 收工：§二 批次登记，§一 #467/#468/#469 回写。

不做什么：
- 🔴 不进实现、不填任何未签认判据（`pending.py` 三闸与 `None` 一律保持）
- 🔴 不改前置总表本体、不改全景规划/实施计划/权威排期表
- 🔴 不发信（姚祖怡闸 🔒）
```

### A2 · 主 spec 补一句开关未实现标注

粘贴端：CC ｜ 泳道：sc8-spec-note

```
[OP-0903-C2]【CC】spec开关标注
【设置】执行环境：CC ｜ 分支：master（从 `claude/op0903b1-atp-apply` 起 `claude/op0903c2-spec-note`）｜ worktree：☑（sc8-spec-note，新 worktree，收工自删）｜ 工作区：无（只改一处 spec 文字，🔴 不进实现、不部署）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_30
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903C2-spec标注。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_30-sc8-spec-note.md`，规则同 A1。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_30 --wave 1 --lane sc8-spec-note --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`。
读 ① `openspec/specs/delivery-date-forecast/spec.md` → ② `openspec/changes/sc8-atp-batch2-closeout/tasks.md` §4.3。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 答 DD-2 = (a)（原文）：
  在主 spec 里**加一句显式标注**「该开关尚未实现，见 tasks §4.3」。
  成因（上一班主动报上来的）：主 spec 现写着「规则2逐字化由独立开关控制且默认关闭」，
  **而那个开关此刻并不存在**（`sc8/config.py` 未加键）——
  **「默认关闭」与「压根没有」行为同为零漂移，文字上却长得像已建成**。
  `#299` 那批的 spec 至少对应已建成的代码，本包是先写 spec 后建造，连「曾经一致过」都不成立。

做什么（一处改动，别扩大）：
1. 在主 spec 提到该开关处加一句显式标注，指向 `tasks §4.3`。措辞须让读者一眼看出**开关尚不存在**，
   不要写成「已实现但默认关闭」。
2. 跑 `openspec validate --all --strict`，须绿。
3. 收工：§二 批次登记。

不做什么：
- 🔴 不实现那个开关（属另立行）
- 🔴 不改任何其他 spec 段落
```

### A3 · 平台底座新增 `criteria_signoff` 公共模块

粘贴端：CC ｜ 泳道：fin-criteria

```
[OP-0903-C3]【CC】底座criteria_signoff
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903c3-criteria-signoff`）｜ worktree：☑（fin-criteria，新 worktree，收工自删）｜ 工作区：无（平台底座包内建造，🔴 不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_30
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903C3-底座签认模块。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_30-fin-criteria.md`，规则同 A1。🔴 本泳道两段（A3→A4），同一心跳文件。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_30 --wave 1 --lane fin-criteria --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`。
读 ① 🔴 `5-平台底座/CLAUDE.md`（**动 5-平台底座 之前必读**：子系统表／OEM 隔离边界）→ ② 分支 `claude/op0903b3-fin-scenarios` 上五个包里各自的本地判据签认实现 → ③ 根 `CLAUDE.md` §5 openspec 三条门槛。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 答 EE-1 = (a)（原文）：
  **把 `criteria_signoff` 收进平台底座做成公共模块。**
  成因：rule-of-three **已触发（5 > 3）** —— 五个财务场景各需「判据签认」这套逻辑，
  现为**五场景各留一份本地实现**，不收进底座**会各自漂移**
  （同 `#345`「35 份手抄副本」的形状，本项目已吃过一次）。

做什么：
1. 先读五份本地实现，抽出**公共契约**（哪些是真共通、哪些是场景特有）。
   🔴 **不要把场景特有的东西也收进底座** —— 那会让底座反过来绑死场景。
2. 在 `5-平台底座/zhuopin_platform/` 下建 `criteria_signoff` 模块：
   核心语义＝**未签认的判据一律为 `None`，任何读取未签认判据的调用必须 fail-loud**，
   不得提供「给个默认值」的旁路。
3. 🔴 **openspec 门槛自评**（根 `CLAUDE.md` §5 三条）：本项**改变全项目口径**（判据签认是跨场景约定）
   ⇒ **必走 openspec 含 design 审**。起草完 design 即 `pause --action-key openspec_design_review`
   以 `OPENER_PARTIAL` 收尾，**不自行通过**。
4. 单测：反例用例——读取未签认判据必须抛，**关掉守卫即失败**。
5. 收工：§二 批次登记。

不做什么：
- 🔴 不改五个场景包（那是 A4 段的事，且须等本段 design 审过）
- 🔴 不自行通过 design 审、不部署
```

### A4 · 财务五包 apply 并引用底座模块

粘贴端：CC ｜ 泳道：fin-criteria

```
[OP-0903-C4]【CC】财务五包apply
【设置】执行环境：CC ｜ 分支：master（从 `claude/op0903b3-fin-scenarios` 起 `claude/op0903c4-fin-apply`）｜ worktree：☑（fin-apply，新 worktree，收工自删）｜ 工作区：无（openspec sync 与场景包引用改造，🔴 不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_30
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903C4-财务apply。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_30-fin-criteria.md`（与 A3 同文件，接着追加）。
🔴 本项排在 A3 之后：开工先跑 `python 0-学习与工具/工具-泳道看护状态机.py show --lane fin-criteria`，
若显示 paused（A3 停在 design 审），立即以 `OPENER_PARTIAL: 依赖 A3 未解除` 收尾，**不得盲目往下做**
—— 五包若先 apply 掉本地实现，再收编就是二次返工。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_30 --wave 1 --lane fin-criteria --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`。
读 ① 分支上五个包 → ② A3 产出的底座模块 → ③ 队列 §一 #470–#474。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 三项连带裁决（原文，逐条落实）：
  EE-2 【FI8 银行余额取数授权】＝**他本人去推该授权**（涉资金安全、须财务侧＋CFO 办公室，属权限非判据）；
       ⇒ 本泳道**做 FI8 不依赖余额的部分**，`BANK_BALANCE_ACCESS` 保持空值＋独立用例守，**不得绕过**。
  EE-3 【FI9 是否带出 OEM 项目标识】＝(a) **会，按接 `data_isolation_layer` 设计**。
       他的理由：研发费用归集按项目走，OEM 项目几乎必然出现；**这一条错了是合规问题，宁可多接**。
       🔴 接法须守根 `CLAUDE.md` §7 红线 3（OEM 数据按客户路由、禁跨库）与 `5-平台底座/CLAUDE.md` 隔离边界。
  EE-4 【FI10 L9 呆滞口径】＝(a) **FI10 先定，SC7 后对齐**（等 SC7 落地要到 2027-01，等于 FI10 停摆一年多）。
       ⚠️ 但「FI10 先定」不等于「现在就填那个口径」—— 判据仍未签认，**照旧落 `None` ＋ CI 守**，
       只是把「口径归属」定为 FI10 侧先出、SC7 后对齐，**这一点须写进 spec 与队列行**。

做什么：
1. 五包各跑 `/opsx:sync`（🔴 不是 `apply`，同 A1 那条说明）。
2. 把五份本地判据签认实现改为引用 A3 的底座模块；🔴 **`None` ＋ CI 守的行为一处都不许变**。
3. 落实 EE-2／EE-3／EE-4 三项（见上）；FI9 接隔离层的设计若命中 openspec 门槛②（数据可见性）
   ⇒ 起草 design 后 `pause`，不自行通过。
4. 重跑 `openspec validate --all --strict`，须绿。
5. 收工：§二 批次登记，§一 #470–#474 逐行回写。

不做什么：
- 🔴 不填任何未签认判据、不给任何默认值
- 🔴 不碰真实银行数据、不部署、不发信（唐燕萍闸 🔒）
```

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0903_30` 现取。
