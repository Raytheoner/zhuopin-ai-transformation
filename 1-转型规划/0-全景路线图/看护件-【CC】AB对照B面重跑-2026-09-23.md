---
title: 看护件 · 【CC】A/B 验收 B 面 · 重跑（首次作废）
created: 2026-09-23
派出线: 环境总线 Win-0921-A
批次: B-0923_AB对照重跑
status: 待执行
---

# 看护件 · 【CC】A/B 验收 B 面重跑 · `B-0923_AB对照重跑`

## 〇、首次 B 面为什么作废（硬证据，写在这里免得重犯）

首跑 `OP-0923-A`／`11d565ad`（08:03:34 起，17.5 分钟，`OPENER_DONE`）**整程没有受去重守约束**：

1. `.claude/settings.json` 是 **git 跟踪文件**，泳道 worktree 一律「**新建自 master**」。
2. 去重守注册进 master 的时刻是 **08:17:53**（`ab9aaeed`，由 sweep 自动提交），而泳道 worktree 建于 **08:03:34** ⇒ 它拿到的是**没有 dedup 条目的旧 settings**。
3. `reports/hooks-audit.jsonl` 里 dedup-guard 的最后一条是 **2026-09-21 17:43:56**（全是建守时的 smoke 测试），B 面窗口内**零条**。
4. `reports/_from-worktree/` 下**没有** `AB对照线` 目录（worktree 自删成功），排除「留痕随 worktree 走了」这一解释。

🔴 **由此立下的机制口径（本件之外还要登记）**：**任何 `.claude/settings.json` 的钩子注册，必须先进 master，泳道才带得上**；主工作区装好但尚未提交的那段时间窗内，**所有泳道一律不受新钩子约束**。起批前必须机器核 `git show master:.claude/settings.json` 含该钩子。
本次已核：**master `16b6d0ca` 命中 dedup-guard = 1** ✓。

## 〇bis、首跑白跑，但量出了一个更要紧的东西

同一份派单件、无任何变量，两次运行：

| 指标 | A 面 `dedc9bbf` | 首次 B 面 `11d565ad`（实为第二次 A 面） |
|---|---|---|
| 工具调用数 | 66 | **67**（+1.5%） |
| 上下文峰值 | 163,208 | **164,865**（+1.0%） |
| Read | 16 | **1** |
| Grep | 20 | **3** |
| Bash | 23 | **60** |
| 完全相同 (工具,目标) 重复 | 10（15%） | 3（4%） |

⇒ **总量极稳（±1.5%），分布剧烈漂移。** 判据必须用**总量**（工具调用数／峰值／`cache_read`），**按工具分类的重复率不可作为单次 A/B 的判据**——它的自然方差比守的效果还大。

## 一、硬边界

1. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
2. 🔴 **纯读活**：除产出那一份清单外不改任何既有文件；不碰 `.claude/rules/`、根 `CLAUDE.md`、`.claude/settings.json`。
3. 🔴 **派单件一字不改**（与 A 面、首次 B 面同一份），**不设 `ZHUOPIN_DEDUP_GUARD`**。被守拒绝时**不要重试同一次调用**，拒绝消息里已写明改法。
4. 🔴 禁起后台子任务／并行 Agent；不整读 `工具-落库sweep.py`（555,367 B）／`test_工具-落库sweep.py`（534,849 B）／`进度编年-CHANGELOG.md`（539,716 B）。
5. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`；`.claude/worktrees/agent-a15b4851f659218aa` 里有 `#455` 真实未提交改动，谁都不许清。
6. 🔴 收工前跑 `git diff --name-only master...HEAD` 贴进报告；只 push 本泳道分支，不 ff master。

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#639` | `AB重跑线` | 只读 `0-学习与工具/工具-落库sweep.py`＋其单测；新增 `1-转型规划/0-全景路线图/核查-sweep告警消费者与夹具-<RUNTAG>.md` | 派单件＝同一份，一字未改 |

## 三、泳道 opener

### A1 · A/B 对照 B 面重跑 · 去重守已在 master 上生效（§一 #639）

粘贴端：CC ｜ 泳道：AB重跑线

```
[OP-0923-C]【CC】AB对照B面重跑
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0923c-ab-run-b-retry`） ｜ worktree：☑（op0923c-ab-run-b-retry，收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/派单件-【CC】AB基线-sweep告警消费者与夹具核查-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923c-ab-run-b-retry --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923c-ab-run-b-retry --done --batch B-0923_AB对照重跑 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头（`工具-opener批处理执行v2.ps1 -Detach`），本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0923-D]【CC】看护AB重跑
【设置】执行环境：CC ｜ 分支：0923-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 Win-0921-A ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0923D-看护AB重跑。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】AB对照B面重跑-2026-09-23.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为该泳道起一个子任务，`isolation: "worktree"`，把【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。
🔴 等待一律用 `python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0923_AB对照重跑 --max-wait 540`，**禁裸 `sleep` 轮询**（队列 §一 `#638`）。
```

## 四、收工验收

```
python 0-学习与工具/工具-泳道看护等待.py wait --batch B-0923_AB对照重跑 --max-wait 540
python reports/probe-0921/fleet3.py
python reports/probe-0921/auditstat.py
```

🔴 **第一判据＝守到底有没有生效**：`reports/probe-0921/auditstat.py` 必须出现本棒窗口内的 dedup-guard 条目（`pass`／`violation` 计数）。**零条即本次仍然作废**，不许拿数字充验收。
🔴 **总量三数对 A 面**：工具调用数 66／峰值 163,208／`cache_read` 6,512,264（`requestId` 去重口径，未去重虚高约 1.76 倍）。
🔴 **误拦一次即判本版不合格**：从 audit 取有没有「目标已变却被拒」。
🔴 四个数全走 transcript 机器核，**不取泳道自陈**（A 面那条就漏填了派单件 §三 的三行自陈却报 `OPENER_DONE`）。
