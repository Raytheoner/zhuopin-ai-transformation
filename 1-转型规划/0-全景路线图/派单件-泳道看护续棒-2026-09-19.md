---
title: "派单件-泳道看护续棒-2026-09-19"
created: 2026-09-19
执行方: Cowork
来源: OP-0919-A
status: 待执行
---

[OP-0919-A]【Cowork】泳道看护续棒
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐（不建，只产改 `.md`／出看护件转交 CC 建造）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 泳道看护续棒（OP-0919-A）

读 ① 本件全文 → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（同线转场，口径已定、直接开工，无需再问澄清）。

做什么：
1. 开工第一件事：用 `python 0-学习与工具/工具-队列查询.py --digest`（业务场景加 `--file 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`）扫两份队列与归档件的摘要，按触碰区关键词核无他人在办行重叠，命中再 `--row N --field all` 展开；🔴 不得 `Read`／`grep` 队列真身（K3 读侧禁通读）；有重叠不得抢领，报总线裁决。
2. 读 `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` 全文（D1 判据表正本，本件不复述）；读 `1-转型规划/0-全景路线图/Peishen标准工作周.md` §7.2 恢复口令口径。
3. 跑 `python 0-学习与工具/工具-可Open池.py` 现取「可 Open 池」（本件写作时 79 行，机 29／业 50——现取为准，本数会变），逐行按 D1 四档分类：🟢「做错了能靠 git 回滚救回来」的直接做（窄范围、无需 worktree 隔离的可当场做完；需 worktree 的业务场景建造，出看护件转 CC 无头批，走 `工具-opener批处理执行v2.ps1 -Plan ... -FullAuto -Yes -Detach`，按「一次粘贴」纪律只给他一条授权请求）；🟡 停等一字母（ff 合入 master／改口径判据阈值／design 审／关闭他人在办队列行）——同类合并，收进收工清单待他一字母；⏭️ `.51` 部署及任何触碰生产服务的动作转 `zhuopin-lan-closeout`；🔴 对外发送／L2 人工门禁／合规红线变更／ASIL C-D 永不代办。
4. 机制类（`[D:机]`）受可动 WIP 上限约束（协议〇.9 措施 C，跑 `--mechanism-wip-cap` 查默认值现值），业务类（`[D:业]`）不受此约束；超限时新机制类 🟢 候选不应新开，除非上限被正式重议。
5. 今日（2026-09-19）已收口三项，**勿重复扫、勿重判**：`#612`（Q2 8D design 审 D3/D4 通过，queue 行已回写、design.md 决策记录见 `openspec/changes/q2-8d-review-verdict-mvp/design.md`，待 tasks §3 建造——真正代码建造仍是空的，可作为 🟢 worktree 候选之一纳入本轮分类）；`#22`（倒计时卡改造完成，`[S:done]`，artifact 已改，仓库外不需 commit）；`#354`（archive 矛盾结案，`[S:done]`，commit `6fd49deb` 已 push；附带查实分支 `claude/op0912p-env-anchor-ripen` 早已是当前 master 的祖先，**无需 ff、无需 rebase**，该结论已回写队列行，不要沿用更早「STALE 需 rebase」的旧记录）。

不做什么：
- 不重跑今日已收口的 `#612`／`#22`／`#354` 判定；不对外发信、不碰 L2 人工门禁、不改合规红线、不碰 ASIL C/D；不做 `.51` 部署类动作（一律转 `zhuopin-lan-closeout`）；机制类超 WIP 上限时不强开新机制行（无 `--force-mechanism-wip` 授权）。

收工：产出登记 §二 待 commit 批次（走 `0-学习与工具/工具-共享文档编辑锁.py` 的 `commit-edit`／`commit-append`，勿裸改、勿自行 commit 队列文件），由落库 sweep 取活；若产出看护件转交 CC 无头批，按 `opener骨架.md`「一次粘贴」纪律一次出件，不裸出多份 opener；收工汇总末尾给「待他一字母」清单，同类合并、标〔荐〕与默认项、末行给答复模板。