---
title: "拆件巡逻报告 · 2026-07-31"
created: 2026-07-31
status: 在办
说明: Dispatch v0.5 阶段二 live（2026-07-30 转正）。工作日 9:00 / 13:00 双班；同日第二班以「第二班」小节追加，不覆盖本节。
---

# 拆件巡逻报告 · 2026-07-31

## 第一班（09:00 班）

**识别 2 行 / 已拆 2 行**（均为 IT 陈承 2026-07-30 文本反馈，队列 §一 #160、#161）。
扫描范围＝「文内 §一 真实可见最大号 #172」与「顶部编号高水位线 #172」的较大者，两者一致。
编辑锁 `acquire` 成功，持锁瞬间回显高水位线 **§一 #172 ｜ §四 #37**，新行按此 +1 续排；改完立即 `release` 成功。

---

### 行 #160 — IT 陈承 · 安全两项收口回复

- **文件**：`7-外部文档/IT/IT-2023458-回复-2026-07-30-文本反馈-27f38b1da2c99c170cbf112b5a3f6223.md`
- **内容一句话**：陈承对 07-27 发出的《安全两项收口——U9C 生产库 sa 账号 + /Services SOAP 免鉴权》（队列 #91）的正式书面处置回复：**sa 不轮换不禁用、改补偿控制；SOAP 已加网络层 ACL 并确认写端点亦免鉴权**。
- **要点（原件事实，不加解读）**：
  - **(a) sa 超管账号**：口令因被**多处 ERP 客开文件硬编码引用**，牵涉面广，**无法轮换或禁用远程登录**。替代处置＝生产库 192.168.6.2 配 IP 白名单（仅指定 IP 可远程连接）+ 仅授权账号经堡垒机登录；暴露面由「任意网络可达」收敛至「仅限授权路径可达」。
  - **(b) SOAP 免鉴权**：U9C Portal IIS 层对 `/Services` 启用 `ipSecurity` IP 白名单，仅放行 127.0.0.1 / ::1、服务器自身 192.168.6.2、防火墙出口 10.11.0.253（供第三方系统集成），其余全拒。**写端点（应收单创建/删除、销售订单创建/删除等）经核实同样免鉴权**，属数据完整性风险，由同一 ACL 一并收敛。**SOAP Fault 异常回显未在代码层修复**（仍带调用堆栈；修复点在 U9C 官方共享的服务行为配置，涉数百服务节点、有集体不可用风险）；外网实测畸形 SOAP 请求返回 403、未触及应用层。措施已在测试库验证后部署生产并完成生产实测确认。
- **已执行动作**：#160 销行（写明结论 + 派生去向）；**回灌 #91**（补记 IT 实际处置与"本行不自行销行"的理由）；两处结案裁定登记 §四 **#38**、**#39**。
- **判断依据**：#91 原验收口径写的是「轮换口令 + 改最小权限只读账号 / 停用 DB 直连」与「关详细异常」，IT 给的是**补偿控制**而非我方要求的处置 —— 属**处置方式变更 + 风险接受**，是否接受属新判断，按红线不自行落字、不代为销行。
- **需 Paul 项**：**§四 #38**（sa 残余风险是否接受 + 本地 `supplychain/.env` 明文口令如何处置）、**§四 #39**（SOAP 两处残余是否接受 + 两项整体结案与根 CLAUDE.md 隐患段状态改写由谁落字）。

> 附一处巡逻主动提示（事实层，不构成处置建议）：#91 原写"轮换回执到后本地删 `supplychain/.env` 三行 DB 配置"。口令既未轮换，**本地那串 11 位明文目前仍是有效凭据**，而非已作废的历史值 —— 该前提变化已写入 §四 #38，未擅自删改任何文件。

---

### 行 #161 — IT 陈承 · 两个接口行级状态字段已补齐

- **文件**：`7-外部文档/IT/IT-2023458-回复-2026-07-30-文本反馈-5b3c9ba058493ec779d263789d713261.md`
- **内容一句话**：陈承对 07-28《两个接口缺行级状态字段，请评估补齐》跟催信（#144 已发）的回复 —— **已完成，两接口均已上线行级状态字段**。
- **要点**：
  - **根因**：并非数据库缺行级状态，而是**接口 SQL 只取了表头 Status、未取行表本已存在的独立 Status 列**。
  - **① `ForecastOrder/Query` 新增 `LineStatus`**（取自 `SM_ForecastOrderLine.Status`；**2=核准 / 3=关闭**）。真实验证：`FO2026070001` 第 60 行（`S02Y.0120`）、第 230 行（`S02Y.0166`）`LineStatus=3`，与 ERP 界面"关闭"一致，其余行为 2。
  - **② `Purchase/Query` 新增 `LineStatus`**（取自 `PM_POLine.Status`；0=开立 1=审核中 2=已审核未交清 **3=自然关闭 4=短缺关闭 5=超额关闭**），已抽样验证。
- **已执行动作**：#161 销行；**回灌 #139**（两处"待 IT"前置解除）；派生建造行 **§一 #173**（CC 采购落地，待领）。
- **判断依据**：#139 行内白纸黑字记着两处"真实探测证实现有接口做不到"——① #19「只取核准状态 FO」（原因：`Status` 实为单据头审核状态，`FO2026070001` 全部 90 行恒为 2）；④ PO 在途只能靠 365 天回溯窗口折衷（原因：`ZpViewPurOrder/Query` 无客户端可读关闭状态）。本回件**恰好逐一补上这两个字段**，且 IT 验证用的正是 #139 当初证伪时的同两个案例（`S02Y.0120` / `S02Y.0166`）—— 前置解除属**事实认定**，不是新口径；建造本身属 CC，按红线只登记待领行、不派单不建造。
- **需 Paul 项**：**无**。（#19 的口径早由姚祖怡 07-28 判例回件逐字说死并两次书面明示，#139 已判定"C 类限制解除、可直接建造"；本次只是技术前置从"做不到"变为"做得到"。）

---

## 待发信盘点（#124 阶段一 · 只读只提醒，未发送任何信）

`README-跟进机制与命名约定.md` 状态列现有 **1 封「🆕 待发」**，无积压：

| 收件人 | 主题 | 截止日 |
|---|---|---|
| 采购部 · 姚祖怡 | 批 2 引擎最后一项口径判例包（队列 #118 剩余唯一开放点）：真实案例 S02Y.0120 因出货日最早排最前，导致 S02Y.0210 后续 8 个月可齐套数归零 —— 请批"上月未齐套项目的物料，在下月是否继续传导占用"1 项（3 行真实判例三选一） | 不卡时间，方便时回即可 |

---

## 巡逻自检

- **三.5 重读比对结果**：写回前重读队列真身，§一 编号序列与 `企微反馈自动归档` 行数（16 条）**与 acquire 时完全一致，无机器人新增行、无撞号、零并入**。本轮编辑全程采用**行级定点替换**（非整文件重排），即便窗口内有并发追加也不会覆盖。
- **预案覆盖情况**：本轮两行均落在既有预案内，无预案覆盖不了的情形。
  - 三条硬约束执行情况：① 两份回件均为纯文本、已**通读全文**（(a)(b) 两节、根因段与三条改动说明均在自由文本里，无表格）；② 本轮无 ✅/❌/✏️ 判例表形态，第 2 条不适用；③ 两行**同批合并读取**（同一发件人、同一日、同为 IT 回件），确认二者是**两封独立信的回复**、非"一文一档"伴随提示 —— 故**分别销行**，未合并处理。
  - 一处主动核对：#160 的 (a) 与 #91 原验收口径不一致，属"看似已完成、实为处置方式变更"，未按"IT 已回复即销 #91"处理 —— 这是本轮最容易误销的一处。
- **未做（红线内，如实留白）**：未改根 CLAUDE.md「两条独立安全隐患」段（权威记忆，需 Paul 指定落字方）；未删本地 `supplychain/.env`；未起草/发送任何对外信；未派 CC 建造口令，只登记 #173 待领行。
- **编号**：全部取 acquire 回显值 +1 续排（§一 #173、§四 #38/#39），顶部高水位线已同步更新为 **§一 #173 ｜ §四 #39**。
- **锁**：`acquire` / `release` 均成功，未走降级路径。
- **⚠️ sweep 未能主动触发（本轮唯一未按 prompt 完成项，如实记）**：`Start-ScheduledTask -TaskName ZhuopinCommitSweep` 需本机 PowerShell，而本轮为**定时任务非交互 session**，无可用的本机 PowerShell 通道（computer-use 需 Paul 在场逐次授权）。批次 `B-0731拆件巡逻` **已按规范登记 §二**（状态列不含 "✅"，含全部改动文件的完整路径），会被 `ZhuopinCommitSweep` 的 4 小时周期自然取活落库；若 Paul 想立刻落库，本机跑一句 `Start-ScheduledTask -TaskName ZhuopinCommitSweep` 即可。**这不是 prompt 缺陷，是执行环境边界** —— 建议在下次改 live prompt 时把"非交互 session 无法触发 sweep、改为登记后依赖周期"写成显式预案，免得每班都当例外记一次。

---

**live 首周逐日抽查中，出现一次错写即退回 dry-run。**
