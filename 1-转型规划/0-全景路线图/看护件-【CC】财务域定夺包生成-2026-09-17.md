# 财务域定夺包生成 · 看护件（2026-09-17）

> 承接队列 §一 `#614`。他当轮提「学 mac 做法，把所有还需确认的问题成批列出，组织各部门线下集中定夺，快速提升需求初始版质量，然后按新 workflow 滚动前进」，答 `庚a` ＋ `辛b`（只做财务域）。

## 〇 · 先读这一段：本波的闸不在建造侧

🔴 **本波一行业务代码都不写。** 产出只有「问题」。判据是：**每一条问题都要能回答「为什么只有专员能答」**——能从 U9C 数据、现有 spec／design、已签认口径点里查到的，**自己查掉，不许上桌**。这条是 `zhuopin-requirement-grill` 的核心纪律，本波全盘沿用。

🔴 **为什么做这件事**：全项目至今累计口径点 **112 个**，而八个已建三到六成的 openspec 包加起来只提出过 **5 个决策点**。需求初始版质量低不是问题难，是**没人系统地问过**。

🔴 **8 个场景不是一类活，工具不同**：

- **FI3／FI4／FI7**（零工程目录、真·新场景）⇒ 跑 `zhuopin-requirement-grill` **前半段**：只生成问题树，**不与专员交互、不等回复**。
- **FI5／FI6／FI8／FI10**（已有骨架＋openspec 包）⇒ grill 按其自身口径**不适用**（它只对新场景）。改做「**包内未问点盘查**」：通读 design.md／spec.md／tasks.md，找出**已经被假设、却从未问过专员**的判据，逐条落为问题。
- **FI9** 是 D 档（卡工时系统），其未决多半是**前置**而非口径 ⇒ **不在本波**，收口时单独标注。

🔴 **只锁判据，不锁排期不锁范围。** 排期交新四档模型与波去滚——锁排期只会产生第 12 次重排。

🔴 **只生成、不发送。** 对外发送（企微／跟进信）永不代办。线下定夺的答复仍走口径点台账 ＋ 判例批改回灌，不开体外循环。

## 一 · 泳道编排（5 条，全并行，无串行点）

五条互不依赖，并行上限 4、错峰 ≥90 秒，第 5 条排下一波。

🔴 **五条泳道共用 §一 `#614` 一个行号**（不为每条泳道单开行）——现取机制类可动 WIP 已 **29–30／22 超限**（§四 `#208` 待处置），本波刻意不再加五行。派出前校验会对 `#614` 现查队列态，行为不变。

## 二 · 逐泳道产出（统一落点与统一格式）

每条泳道产出一个文件：`1-转型规划/0-全景路线图/定夺素材-财务域-<场景>.md`，格式固定四段：

1. **已自查掉的**（列出查了什么、结论是什么、凭据在哪）——**这一段不许空**，空了说明没做自查；
2. **须专员定夺的问题**，每条写：问题一句话 ／ 为什么只有专员能答 ／ **≤10 条真实案例三选一**（判例批改法）／ 本方倾向与理由；
3. **材料索取**（要文件、要样本、要账号）——**单列，不进定夺会**；
4. **本场景的不确定度自评**：B 档还是 C 档、预计几轮收敛。

🔴 **没有真实案例时，明写「无案例，须专员口述」，不要编案例。**

## 三 · 泳道 opener（`工具-opener批处理执行v2.ps1 -Plan` 解析段，人不碰）

🔴 `### A<N>` 标题里必须带 `§一 #<行号>`——`工具-opener派出前校验.ps1` 靠它现查队列态。

🔴 本节全部由 `0-学习与工具/工具-opener生成.py` 生成（骨架正本 `opener骨架.md`），**未手写**。首版手写被 `工具-opener块lint.py --enforce` 判 F1（缺 `set_session_title`）与 F4（`【设置】` 六字段缺 session／派出线）各 5 处，已废弃重出。

🔴 五条全并行、无串行点；并行上限 4，第 5 条排下一波，错峰 ≥90 秒。

### A1 · §一 #614 FI3 付款申请自动校验

粘贴端：CC ｜ 泳道：fi3-baseline-614 ｜ 队列行：§一 #614 ｜ OP：OP-0917-A

```
[OP-0917-A]【CC】FI3定夺素材
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0917a-fi3-baseline-614`） ｜ worktree：☑（op0917a-fi3-baseline-614，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：批 B-0917_财务域定夺包生成 ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】财务域定夺包生成-2026-09-17.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 跑 skill zhuopin-requirement-grill 前半段，只生成问题树，不与专员交互不等回复不发信；先把能查的查掉（U9C 现有数据／openspec specs／已签认口径点／全景规划描述）
2. 产出 1-转型规划/0-全景路线图/定夺素材-财务域-FI3.md，严格按看护件 §二 四段格式；第 1 段「已自查掉的」不许空

不做什么：
- 不写业务代码、不改 openspec 包、不改 spec、不动排期
- 不发任何对外消息（企微／跟进信／docx 外发一律不代办）
- 不编案例；无真实案例即明写「无案例，须专员口述」
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917a-fi3-baseline-614 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917a-fi3-baseline-614 --done --batch B-0917_财务域定夺包生成 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · §一 #614 FI4 月结其余

粘贴端：CC ｜ 泳道：fi4-baseline-614 ｜ 队列行：§一 #614 ｜ OP：OP-0917-B

```
[OP-0917-B]【CC】FI4定夺素材
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0917b-fi4-baseline-614`） ｜ worktree：☑（op0917b-fi4-baseline-614，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：批 B-0917_财务域定夺包生成 ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】财务域定夺包生成-2026-09-17.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 跑 skill zhuopin-requirement-grill 前半段，只生成问题树，不与专员交互不等回复不发信；先把能查的查掉（U9C 现有数据／openspec specs／已签认口径点／全景规划描述）
2. 产出 1-转型规划/0-全景路线图/定夺素材-财务域-FI4.md，严格按看护件 §二 四段格式；第 1 段「已自查掉的」不许空

不做什么：
- 不写业务代码、不改 openspec 包、不改 spec、不动排期
- 不发任何对外消息（企微／跟进信／docx 外发一律不代办）
- 不编案例；无真实案例即明写「无案例，须专员口述」
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917b-fi4-baseline-614 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917b-fi4-baseline-614 --done --batch B-0917_财务域定夺包生成 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · §一 #614 FI7 预算监控

粘贴端：CC ｜ 泳道：fi7-baseline-614 ｜ 队列行：§一 #614 ｜ OP：OP-0917-C

```
[OP-0917-C]【CC】FI7定夺素材
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0917c-fi7-baseline-614`） ｜ worktree：☑（op0917c-fi7-baseline-614，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：批 B-0917_财务域定夺包生成 ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】财务域定夺包生成-2026-09-17.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 跑 skill zhuopin-requirement-grill 前半段，只生成问题树，不与专员交互不等回复不发信；先把能查的查掉（U9C 现有数据／openspec specs／已签认口径点／全景规划描述）
2. 产出 1-转型规划/0-全景路线图/定夺素材-财务域-FI7.md，严格按看护件 §二 四段格式；第 1 段「已自查掉的」不许空

不做什么：
- 不写业务代码、不改 openspec 包、不改 spec、不动排期
- 不发任何对外消息（企微／跟进信／docx 外发一律不代办）
- 不编案例；无真实案例即明写「无案例，须专员口述」
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917c-fi7-baseline-614 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917c-fi7-baseline-614 --done --batch B-0917_财务域定夺包生成 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A4 · §一 #614 FI5 费用报销 / FI6 异常交易（包内未问点盘查）

粘贴端：CC ｜ 泳道：fi56-audit-614 ｜ 队列行：§一 #614 ｜ OP：OP-0917-D

```
[OP-0917-D]【CC】FI5FI6盘查
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0917d-fi56-audit-614`） ｜ worktree：☑（op0917d-fi56-audit-614，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：批 B-0917_财务域定夺包生成 ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】财务域定夺包生成-2026-09-17.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 不跑 grill（它只对新场景）；通读本场景 openspec/changes 下 design.md/spec.md/tasks.md，盘出「已被假设却从没问过专员」的判据，逐条落为问题，并核台账已签认点避免重问
2. 产出 1-转型规划/0-全景路线图/定夺素材-财务域-FI5+FI6.md，严格按看护件 §二 四段格式；第 1 段「已自查掉的」不许空

不做什么：
- 不写业务代码、不改 openspec 包、不改 spec、不动排期
- 不发任何对外消息（企微／跟进信／docx 外发一律不代办）
- 不编案例；无真实案例即明写「无案例，须专员口述」
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917d-fi56-audit-614 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917d-fi56-audit-614 --done --batch B-0917_财务域定夺包生成 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A5 · §一 #614 FI8 现金流预测 / FI10 存货跌价（包内未问点盘查）

粘贴端：CC ｜ 泳道：fi810-audit-614 ｜ 队列行：§一 #614 ｜ OP：OP-0917-F

```
[OP-0917-F]【CC】FI8FI10盘查
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0917f-fi810-audit-614`） ｜ worktree：☑（op0917f-fi810-audit-614，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：批 B-0917_财务域定夺包生成 ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/看护件-【CC】财务域定夺包生成-2026-09-17.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 不跑 grill（它只对新场景）；通读本场景 openspec/changes 下 design.md/spec.md/tasks.md，盘出「已被假设却从没问过专员」的判据，逐条落为问题，并核台账已签认点避免重问
2. 产出 1-转型规划/0-全景路线图/定夺素材-财务域-FI8+FI10.md，严格按看护件 §二 四段格式；第 1 段「已自查掉的」不许空

不做什么：
- 不写业务代码、不改 openspec 包、不改 spec、不动排期
- 不发任何对外消息（企微／跟进信／docx 外发一律不代办）
- 不编案例；无真实案例即明写「无案例，须专员口述」
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917f-fi810-audit-614 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917f-fi810-audit-614 --done --batch B-0917_财务域定夺包生成 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```


## 三bis · 标准 opener（本波无）

🔴 **本节必须存在、且必须排在 §三 之后**，不是排版偏好：本波五条全是子任务泳道块（`工具-opener生成.py --variant subtask_lane`），按 `工具-opener块lint.py` 形态⑥ 豁免 `set_session_title`；而那个豁免的判据是「块出现在 `## 三bis` 标题**之前**」。没有本节，边界不存在，最后一块即被当成标准 opener 判 F1（本波首次拼完就撞了这一下）。

本波没有标准 opener，**本节留空即是内容**。

## 四 · 全程红线（一条都不例外）

1. **只生成、不发送**——任何对外发送（企微、跟进信、docx 外发）永不代办，须 Shao Peishen 亲自走 `zhuopin-send-followup`。
2. **不编案例**——判例批改法的力量全在案例是真的。没有就写「无案例，须专员口述」。
3. **不动排期、不动范围**——本波只锁判据。
4. **不改已有 openspec 包**——盘查泳道只读不写包。
5. **FI9 不在本波**（D 档，卡工时系统，问题属前置不属口径）。

## 五 · 收口（看护者做，不派泳道）

五条泳道收齐后，由看护者串行做三件：① FI9 的前置盘查单独成节、标注「非口径问题」；② 并入本域台账未闭环条目与包内决策点现状；③ 汇总成一份《财务域线下定夺包》交 Shao Peishen 过目——**过目之后才谈怎么开会、由谁发出**。

🔴 **波 2（采购／质量／IT／运营）的闸现在不要开**——先让财务这一波真跑一轮，形态验过再复制。这是 `#575` 那条线换来的纪律：单测证明代码对，第一轮真跑才证明形态对。
