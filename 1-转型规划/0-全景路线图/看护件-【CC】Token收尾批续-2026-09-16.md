---
title: "看护件 · B-0916_Token收尾批续（两泳道续棒）"
created: 2026-09-16
批次: B-0916_Token收尾批续
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 20:44 明令本线全力收尾；本件为 #596／#599 上一棒未建 worktree、误写主工作区被中止后的续棒）
---

# 看护件 · B-0916_Token收尾批续

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#596` | 编辑锁三处可用性缺口（续棒，基于救回的 WIP） | 编辑锁工具＋单测 |
| A2 | `#599` | P6 巡逻无状态化（重派） | patrol_dispatch.py＋单测 |

## 二、泳道

### A1 · §一 #596 编辑锁小修续

▶ 粘贴端：CC ｜ 泳道：op0916w-lock-ux-596b

```
[OP-0916-W]【CC】编辑锁小修续
【设置】执行环境：CC ｜ 分支：`claude/op0916s-lock-ux-596`（既有分支续做：Cowork 已把上一棒半成品救回为 WIP 提交 b629df8，未测） ｜ worktree：☑（op0916w-lock-ux-596b，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人运行时／计划任务） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token收尾批续
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916W-编辑锁小修续。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
🔴🔴 **开工第一条命令（先于读任何文件）**：在主仓根执行 `git worktree add .claude/worktrees/op0916w-lock-ux-596b claude/op0916s-lock-ux-596`，随后所有读写与 git 命令都在 `C:\Dev\zhuopin-ai\.claude\worktrees\op0916w-lock-ux-596b` 内进行；每次改文件或 commit 前先跑 `git rev-parse --show-toplevel`，结果不是该 worktree 路径即立刻 `OPENER_PARTIAL: 停在🔴决策点——未在 worktree 内`。上一棒在主工作区直接改代码已被 Cowork 中止（09-16 20:55），**主工作区内除 reports/ 与收工回写外一律不许写**。
先 `git show b629df8 --stat` 看清已做部分再续做，WIP 提交须补测后另提交修正。
读 ① `队列 §一 #596（`python 0-学习与工具/工具-队列查询.py --row 596 --section 一 --field all`）` → ② `CLAUDE.md`，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按 #596 期望产出 ⑴→⑷：失败且确未写入才自动放锁；近期持锁提示默认一行、--verbose 展开；commit-append 支持 --reserve；单测覆盖。
🔴 只动本行「触碰区」列所列文件；越界需求只登记不动手。不放宽任何锁／lint／登记／门禁判据。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出（全量回归、大范围 grep）交子代理只收结论。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short --maxfail=5`，跑完即 commit；差集回归与 master 同命令对跑。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916w-lock-ux-596b --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916w-lock-ux-596b --done --batch B-0916_Token收尾批续 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用一条命令：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916W --section 一 --number 596 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；报错后锁仍占用则先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```


### A2 · §一 #599 巡逻无状态续

▶ 粘贴端：CC ｜ 泳道：op0916x-patrol-599b

```
[OP-0916-X]【CC】巡逻无状态续
【设置】执行环境：CC ｜ 分支：`claude/op0916x-patrol-599b`（新建自 master） ｜ worktree：☑（op0916x-patrol-599b，收工自删） ｜ 工作区：仅 wecom-aibot-service 的 patrol_dispatch.py 与单测，不碰运行时 ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token收尾批续
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916X-巡逻无状态续。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
🔴🔴 **开工第一条命令（先于读任何文件）**：在主仓根执行 `git worktree add -b claude/op0916x-patrol-599b .claude/worktrees/op0916x-patrol-599b master`，随后所有读写与 git 命令都在 `C:\Dev\zhuopin-ai\.claude\worktrees\op0916x-patrol-599b` 内进行；每次改文件或 commit 前先跑 `git rev-parse --show-toplevel`，结果不是该 worktree 路径即立刻 `OPENER_PARTIAL: 停在🔴决策点——未在 worktree 内`。上一棒在主工作区直接改代码已被 Cowork 中止（09-16 20:55），**主工作区内除 reports/ 与收工回写外一律不许写**。
上一棒在主工作区留下的 4 分钟半成品已由 Cowork 存为补丁 `reports/opener-batch/20260916-204736/599-main-leak.patch` 并从主工作区撤回；可在 worktree 内 `git apply` 该补丁作起点，或弃用重做。
读 ① `队列 §一 #599（`python 0-学习与工具/工具-队列查询.py --row 599 --section 一 --field all`）` → ② `CLAUDE.md`，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按 #599 期望产出 ⑴→⑷；只改 patrol_dispatch.py 与其单测，**不重启服务、不停启计划任务、不向企微发任何消息**。
🔴 只动本行「触碰区」列所列文件；越界需求只登记不动手。不放宽任何锁／lint／登记／门禁判据。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读；重输出（全量回归、大范围 grep）交子代理只收结论。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short --maxfail=5`，跑完即 commit；差集回归与 master 同命令对跑。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916x-patrol-599b --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916x-patrol-599b --done --batch B-0916_Token收尾批续 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列回写用一条命令：`python 0-学习与工具/工具-共享文档编辑锁.py commit-edit --who CC-OP0916X --section 一 --number 599 --domain 机 --append "状态=<回写内容>"`（acquire 前已脏的他线文件用 `--waiver` 逐个点名；报错后锁仍占用则先 `release` 再重试）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```



## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】Token收尾批续-2026-09-16.md" -FullAuto -Yes -Detach
```