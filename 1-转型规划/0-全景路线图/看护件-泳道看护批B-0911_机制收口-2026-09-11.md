---
title: "看护件 · 泳道看护批 B-0911_机制收口"
created: 2026-09-11
status: 生效
批次: B-0911_机制收口
派出线: Cowork 环境总线 OP-0910-H
看护者: OP-0911-P
---

# 看护件 · 泳道看护批 `B-0911_机制收口`

## 〇、本批口径

- **触发**：Shao Peishen 2026-09-11 答 `1a`（现在就开）。主攻＝机制侧收口，**不开新业务场景**。
- **前置四查实测**：① LAN 探针 **off-LAN（三项未齐）** —— 判据是 ping ＋ 两个服务端口的真实 HTTP 响应，**不是**今日实证会骗人的 `Test-NetConnection`，可信；② `git fsck` 无 error/missing，`master = origin/master = face9b1`（0/0）；③ 编辑锁无占用；④ 上一批状态件 52 条泳道。
- ⚠️ **④ 不是干净通过**：上一批残留 `436-worktree-drift-alert-clear` 仍标 `running`，实为**陈旧态**（接力卡已判该项 ⑵ 从未执行、具备入 `zhuopin-lan-closeout` 条件，属 ⏭️）。**本批不代其改状态**，收工汇总里点名。
- **上限**：本批 5 条泳道（默认 10 内）；**并行上限 4、同波错峰 ≥90 s**。
- **WIP**：现 27／22（行数闸），`STALE(7)=2 ≤ K=3`（陈旧度闸 warn 模式，今日已 ff）。本批新立 `#561`／`#562` 两行后 29 —— 按 Shao Peishen 2026-09-11 答 `1a`：**不改默认 22，本次 release 单次 `--mechanism-wip-cap 30`**，保住 §4.4 告警期观察数据的真实性。

## 一、硬边界继承

规则正本＝`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`，**本件不复述**。四条只提醒行：
- 🔴 D1 判据现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`；未覆盖按 🟡 停。⏭️ 档 `transfer-out` 标注后跳过、继续其余，交 `zhuopin-lan-closeout`；🔴 档永不代办。
- 🔴 业务*数据*与一切对外发送永不自动入批；真实库写动作／`.51`／规划文档／他线在办行不碰；不自造 🛑 豁免。
- 🔴 看护会话不修工具：发现机制缺陷只「登记 ＋ 出派单」（例外：本批会因它失败且修补 ≤10 行，须写进队列行）。
- 🔴 心跳跑命令写 `... heartbeat --lane <泳道> --text "..."`（收工带 `--done`）；**别给 `--repo-root`／`--heartbeat-file`，没有这两个参数**；等待 >10 分钟须补写一行「仍在等 X」。

## 二、次序与并行矩阵

| 波 | 泳道 | 触碰区 | 档位预判 |
|---|---|---|---|
| 1 | `561-openspec-closeout` | `openspec/changes/`（只读）＋队列 §一（经锁） | 🟢；**归档动作本身 🟡**，只出建议不执行 |
| 1 | `560-worktree-autoremove` | `工具-待合分支巡检.ps1` 或 `工具-落库sweep.py` ＋单测 | 🟢（worktree 内建造） |
| 1 | `562-ffbranch-guard` | `工具-泳道分支合入.ps1` ＋单测 | 🟢（worktree 内建造） |
| 1 | `559-audit-anchor` | `wecom-aibot-service/scripts/approve_followup_letter.py`＋`aibot_service/` | 🟢（worktree 内建造） |
| 2 | `556-relay-design` | `openspec/changes/<新包>`（新建）；服务目录只读 | 🟢（openspec 起草）；**apply 与冒烟 ⏭️** |

**硬序**：波 1 四条触碰区两两不重叠，可并行（错峰 ≥90 s）。`556-relay-design` 排波 2，避开 `559-audit-anchor` 同在 `wecom-aibot-service/` 树内。
🔴 **四条都不碰 `工具-共享文档编辑锁.py`**（锁工具族恒最后一波的既有纪律因此不触发）。

### LAN 留步清单（off-LAN，本批做不了，交 `zhuopin-lan-closeout`）

- `#556` 的 **apply 与冒烟**（真因是 off-LAN 够不到 `\\192.168.100.51`；本批只做到 propose）
- `#436` ⑵（前提已满足，具备入收口批条件）
- 上一批残留泳道 `436-worktree-drift-alert-clear`
- 一切碰 `.51` 的动作

### 🔴 永不代办（不在本批，也不在收口批）

- `#387` ⑶ 告警真实群冒烟（对外发送）

## 三、泳道 opener

### A1 · openspec 收口段逐包实证分诊（#561）

粘贴端：CC ｜ 泳道：561-openspec-closeout

```
[OP-0911-Q]【CC】561收口分诊
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911q-openspec-closeout-561`） ｜ worktree：☑（op0911q-openspec-closeout-561，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0911_机制收口）
读 ① `队列 §一 #561（python 0-学习与工具/工具-队列查询.py --row 561 --field all，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · 子泳道「收工自删」补机器守（#560）

粘贴端：CC ｜ 泳道：560-worktree-autoremove

```
[OP-0911-R]【CC】560泳道自删守
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911r-worktree-autoremove-560`） ｜ worktree：☑（op0911r-worktree-autoremove-560，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0911_机制收口）
读 ① `队列 §一 #560（python 0-学习与工具/工具-队列查询.py --row 560 --field all，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · 合入工具 ④ 守卫改基线对比（#562）

粘贴端：CC ｜ 泳道：562-ffbranch-guard

```
[OP-0911-S]【CC】562合入守卫修
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911s-ffbranch-guard-562`） ｜ worktree：☑（op0911s-ffbranch-guard-562，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0911_机制收口）
读 ① `队列 §一 #562（python 0-学习与工具/工具-队列查询.py --row 562 --field all，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A4 · 审计落点与 listener 同源（#559）

粘贴端：CC ｜ 泳道：559-audit-anchor

```
[OP-0911-T]【CC】559审计落点同源
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911t-audit-anchor-559`） ｜ worktree：☑（op0911t-audit-anchor-559，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0911_机制收口）
读 ① `队列 §一 #559（python 0-学习与工具/工具-队列查询.py --row 559 --field all，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A5 · outbox 中继修法设计 → openspec propose（#556）

粘贴端：CC ｜ 泳道：556-relay-design

```
[OP-0911-U]【CC】556中继修法设计
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911u-relay-design-556`） ｜ worktree：☑（op0911u-relay-design-556，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0911_机制收口）
读 ① `队列 §一 #556（python 0-学习与工具/工具-队列查询.py --row 556 --field all，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

```
[OP-0911-P]【CC】看护机制收口
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0911_机制收口）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0911P-看护机制收口。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0911_机制收口-2026-09-11.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（看护者按此产出）

1. 每泳道一行 ＋ 心跳全文。
2. 🔴 **D6 列**：「本批停 N 次｜逐次：<泳道>／<档位>／<停在什么动作>／<他答了什么>／<等了多久>」。
3. 🔴 **转出列**：「本批转出 N 项（须走 `zhuopin-lan-closeout`）｜逐项：<泳道>／<动作>／<说明>」＋「待回内网」清单。
4. 一律现取 `... summary --batch B-0911_机制收口`；🔴 **等待时长只认 `reports/lane-watch-state.json`，会话不得自算**。
5. 末尾固定「待你一字母」清单：同类合并、每项写 (a)/(b) 与代价、design 审类排在 merge 类之前、**最后一行给答复模板**。
6. 主仓 ff 由看护者收工串行做；🔴 **sweep 不做 ff**；看护者自己不 commit。
7. 🔴 **ff 走 `工具-泳道分支合入.ps1` 会被 ④ 假阳拦下**（`#562` 即此事）——按零回归口径核「同一组失败在纯 master 复现」后手工完成 ⑤⑥，读数写进队列行。
