---
title: "看护件 · 泳道看护批 B-0903_40（第六批：A1/A2 两条链合入 master）"
created: 2026-09-03
批次: B-0903_40
派出线: Cowork 业务总线 OP-0903-D
前批: B-0903_30（3 泳道 4 项：A1 DONE／A2 DONE／A3 停 design 审／A4 依赖未解除留步）
status: 已作废（并入 B-0903_50）
---

# 看护件 · 泳道看护批 `B-0903_40`

> 🔴 **本件已作废、不再派出（2026-09-03 10:38，同日同线）** —— Shao Peishen 当日答「全部推荐」，G-6 ＝ (a) 确认合入范围「照原意合、含连带」，同时 `criteria_signoff` design 五条一并裁定。
> ⇒ **本件唯一的 opener（A1+A2 合 master）已原样并入 `看护件-泳道看护批B-0903_50-2026-09-03.md` §四 D1**，与 criteria 收口、财务五包 apply 一起串行派出。
> **保留本件不删的理由**：§二「合入真实范围大于放行表述」那段取证（A1 连带 1 个、A2 连带 6 个，三条分支全部 ff-NO）是 D1 的判据来源，`B-0903_50` §四 D1 直接引用它。
> 🔴 **任何人不得据本件 §五 单独派出 opener** —— 同一动作两处派单必然撞车。

## 〇、本批口径

Shao Peishen 2026-09-03 放行：**A1（`proc-apply`）与 A2（`sc8-spec-note`）合入 `master`；A3（`criteria-signoff`）等 design 审过后再合。**

🔴 **但本批派出前须先解一件事 —— 实测查出合入的真实范围比放行时以为的大，见 §二。本件在他确认范围前不派出。**

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。
📌 `merge_to_master` 属 **🟡** ⇒ 常规须停等；**本批已有前置放行**（他 2026-09-03 就 A1/A2 明确放行），故泳道**不为"合入"这个动作本身再停**，但**范围一旦超出 §二 所列即须停**。

---

## 一、前批收口实测（三信源核过：心跳 ＋ git ＋ 状态机）

| 泳道 | 状态 | commit | 分支 | 合 master |
|---|---|---|---|:--:|
| `proc-apply`（A1） | ✅ OPENER_DONE | `9ba60c3` | `claude/op0903c1-proc-apply` | ❌ 未合 |
| `sc8-spec-note`（A2） | ✅ 完成 | `bab88c7` | `claude/op0903c2-spec-note` | ❌ 未合 |
| `fin-criteria`（A3） | ⏸ paused（design 审五问） | `c700696` | `claude/op0903c3-criteria-signoff` | ❌ 未合（**本批不动**） |
| A4 财务五包 | 🛑 留步、零改动 | — | — | — |

`summary --batch B-0903_30` ＝ **本批停 1 次｜fin-criteria／🟡／openspec design 审｜本批转出 0 项**。

⚠️ **状态机 `show` 里那一片 `resumed` 全是前批遗留泳道**（`proc-scenarios`／`sc8-kitdate*`／`fin-scenarios` 等），**不属 B-0903_30**。本批只有 `fin-criteria` 一条进过状态机 ⇒ **接力卡那条「`resumed` ≠ 已续跑」的坑本批没踩上**，本批真正的出口是 A3 的五条 design 定夺项。

---

## 二、🔴 派出前必须先确认的一件事：合入的真实范围大于放行时的表述

**实测（2026-09-03 10:2x，`git log --oneline master..<分支>`）**：

| 放行项 | 他以为在合什么 | 实际会带进 master 的提交 |
|---|---|---|
| **A1** | 采购三包 sync 主 spec ＋ 移交单 | **2 个**：`9ba60c3`（A1 本身）＋ `04611c3`（`OP-0903-B2` 采购三场景骨架 ＋ proposal 起草） |
| **A2** | SC8 主 spec 加一句开关标注 | **7 个**：`bab88c7`（A2 本身）＋ `35674a6`／`e615723`／`1bf6b51`／`3e8e768`／`16d20b2`／`282853d`（`OP-0903-B1` SC8 `#118` ATP 批 2 收口全套） |

**成因**：A1 的起点分支是 `claude/op0903b2-proc-scenarios`、A2 的起点分支是 `claude/op0903b1-atp-apply`（两条 opener 均写明"🔴 不是从 master 起"）。**合子分支即连带合入其基分支的全部提交**，这是分支拓扑的必然，不是谁做错了。

✅ **连带那些提交的内容本身都已获他放行**（`04611c3` ← FF-1=(a) 三包放行；`OP-0903-B1` 六个提交 ← 2026-09-03 ATP design 放行 ＋ D3/D3-bis/D4/D5 四项裁决），**所以这不是"混进了没批的东西"**。
🔴 **但"批准了内容"与"批准现在把它落进主干"是两件事** ⇒ 如实回报，请他确认后再派。

### 🔴 第二件实测：三条分支目前**全部 ff-NO**

`master` HEAD ＝ `9b78281`（`docs(队列): 收工重跑文档台账（sweep 自动）`）。三条分支分叉后 **master 又被 sweep 推进过** ⇒ `git merge-base --is-ancestor master <分支>` 三条**全部为假**，直接 `merge --ff-only` 会失败。

⇒ **合入前须先把分支 rebase 到 `master` 新头再 ff**（本项目合并策略 ff-only，根 `CLAUDE.md`）。🔴 **这也意味着 commit hash 必变** —— 事后核合入**不认 hash、认 message**（接力卡 §四 既有纪律）。

---

## 三、硬边界（继承，逐条适用）

1. 🔴 **⏭️ 档：`.51` 部署及任何触碰生产服务的动作永不执行**。
2. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 永不代办**。
3. 🔴 **业务*数据*与一切对外发送永不自动入批**；真实库**只读**。
4. 🔴 **所有 `pause` / `check-heartbeat` 一律带 `--no-notify`**。
5. 🔴 **不得合入 `claude/op0903c3-criteria-signoff`** —— design 审未过，代码不应先于 design 落主干。
6. 🔴 **不填任何未签认判据**（`None` ＋ CI 守一处不动）。
7. 🔴 **三闸全锁**（唐燕萍／姚祖怡／陈忱）⇒ 不得起草或发送任何跟进信。
8. 🔴 **规划文档一律不改** —— 走移交单。
9. 🔴 **范围一旦超出 §二 两张表所列的 2＋7 个提交，立即 `pause`**，不自行扩大。

---

## 四、波次与触碰区矩阵

| 波 | 泳道名 | 项 | 触碰区 | 说明 |
|:--:|---|:--:|---|---|
| 1 | `merge-master` | A1 → A2 | `master`、`openspec/specs/`、移交单 | 🔴 **两条链串行，同一泳道** —— 都动 `master` 与 `openspec/specs/`，触碰区完全重叠，**不可并行** |

🔴 **本批只有一条泳道**：合 `master` 这个动作天然串行，拆成两条泳道并行只会互相 ff 失败。

---

## 五、泳道 opener

### A1+A2 ·【CC】两条链合入 master —— 队列 #467／#468／#469／#118

▶ **首次派出**（🔴 待 Shao Peishen 确认 §二 合入范围后再粘）

```
[OP-0903-D1]【CC】两链合master
【设置】执行环境：CC ｜ 分支：master（🔴 直接在主工作区操作 master，不新建特性分支）｜ worktree：☐（不建 —— 合入 master 须在主工作区，worktree 内 master 被 checkout 锁定）｜ 工作区：无（纯库内 git 与 openspec 校验，🔴 不部署、不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_40
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903D1-两链合master。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_40-merge-master.md`，开工 1 分钟内写首行，每里程碑追加，超 10 分钟补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_40 --wave 1 --lane merge-master --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0903_40-2026-09-03.md` §二（合入范围实测，**逐条比对，多一个提交都要停**）→ ② 根 `CLAUDE.md` §5「合并策略 ff-only」。本件为 A 类（范围已由 Shao Peishen 确认、判据已写死），无需再问澄清，直接开工。

🔴 Shao Peishen 2026-09-03 放行（原文）：A1／A2 现在合入 master；A3（criteria-signoff）等 design 审过后再合。

做什么（🔴 严格串行，每步过了才做下一步）：
1. 开工先跑 `git fsck --connectivity-only`（对象库健康哨兵），**报错即停手、不 pull 不 push、报 Shao Peishen**；通过后 `git pull`。
2. **范围复核（🔴 第一道闸）**：对两条分支各跑 `git log --oneline master..<分支>`，与本看护件 §二 两张表**逐个 commit 比对**。
   - A1 应为 2 个：`9ba60c3` ＋ `04611c3`（message 比对，🔴 **不认 hash** —— rebase 后必变）
   - A2 应为 7 个：`bab88c7` ＋ `35674a6`／`e615723`／`1bf6b51`／`3e8e768`／`16d20b2`／`282853d`
   - 🔴 **对不上（多、少、或出现表外 message）即 `pause --action-key merge_to_master`，不自行判断"应该没问题"。**
3. **合 A1 链**：`git rebase master claude/op0903c1-proc-apply` → 回 master `git merge --ff-only claude/op0903c1-proc-apply`。
   - 🔴 rebase 若有冲突：**停**，`pause` 并把冲突文件与两侧内容如实写进心跳，**不自行择一**。
4. **合 A2 链**：同法处理 `claude/op0903c2-spec-note`（🔴 **必须在 A1 合完之后**，否则第二条的 rebase 基点是错的）。
5. **合后校验（🔴 第二道闸，三项全过才算合成）**：
   a. `openspec validate --all --strict` —— 🔴 **须绿**。⚠️ 两条链各自的计数（A1 侧 132、A2 侧 127）是在**各自分支上**测的，合并后总数会变，**不要拿旧数当预期值**；只认「0 failed」，并把实际数字如实写进心跳。
   b. `git rev-list --count master..claude/op0903c1-proc-apply` 与 `..claude/op0903c2-spec-note` **均须为 0**。
   c. 🔴 `git rev-list --count master..claude/op0903c3-criteria-signoff` **须 > 0**（＝确认 A3 没被顺手带进来）。
6. `git push origin master`。
7. 收工：§二 批次登记（🔴 路径写仓库根相对完整路径，**不用裸文件名速记** —— 会让 sweep 退化到后缀匹配，`#234(1)`）；§一 `#467`／`#468`／`#469`／`#118` 四行回写「已合入 master」。

不做什么：
- 🔴 **不合 `claude/op0903c3-criteria-signoff`**（design 审未过）
- 🔴 不删任何分支（合完保留，便于追溯）
- 🔴 不进任何实现、不填任何未签认判据、不改规划文档
- 🔴 不部署、不触碰 `.51`
- 🔴 不发信（三闸全锁）
```

---

## 六、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0903_40` 现取。

---

## 七、本批不做、已明确去向的事

| 事 | 去向 |
|---|---|
| A3 底座 `criteria_signoff` 合入 | 等五条 design 定夺项答复后另起一批 |
| A4 财务五包 apply | 同上，且须先答定夺项 ⑤ |
| `§四` 分诊 22 条（含 4 条逾期） | `reports/section4-triage-2026-09-02.md`，下一轮摆 |
| FI9「工时系统存在性核实」无主 | 见 `design审读件-criteria_signoff底座-五条定夺项-2026-09-03.md` §七，待他判是否立行 |
