---
title: "看护件 · B-0916_P3机制税与586断连（两泳道并行）"
created: 2026-09-16
批次: B-0916_P3机制税与586断连
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 1c）
---

# 看护件 · B-0916_P3机制税与586断连

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#594` | Token 优化 P3：高频机制调用序列合成复合命令、成功输出一行 | 编辑锁工具、队列查询/K2 输出、opener骨架 |
| A2 | `#586` | 企微机器人断连丢信补漏＋延后写入 flush 幂等 | wecom-aibot-service 代码与单测 |

触碰区零交集；两条都不得碰 `.claude/settings.json` 与 hooks。

## 二、泳道

### A1 · §一 #594 机制税削减

▶ 粘贴端：CC ｜ 泳道：op0916p-mech-tax-594

```
[OP-0916-P]【CC】机制税削减P3
【设置】执行环境：CC ｜ 分支：`claude/op0916p-mech-tax-594`（新建自 master） ｜ worktree：☑（op0916p-mech-tax-594，收工自删） ｜ 工作区：无（纯库内） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_P3机制税与586断连
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916P-机制税削减P3。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #594（`python 0-学习与工具/工具-队列查询.py --row 594 --section 一 --field all`）` → ② `CLAUDE.md` → ③ 路线图 Phase 3 段（grep 定位后分段读），按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 按期望产出 ⑴→⑸ 顺序做；⑴ 取证表先落再动代码。复合命令内部仍逐项调用原有校验，**不放宽任何锁／lint／登记判据**；失败时原样透出原工具报错。
🔴 输出精简只改成功路径；失败、警告、豁免放行信息一律保留。
🔴 开工先确认分支基于最新 master（`git rebase master`；冲突即 PARTIAL 停）。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读（守卫会直接拒绝整读，别绕）；重输出交子代理。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short`，跑完即 commit。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916p-mech-tax-594 --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916p-mech-tax-594 --done --batch B-0916_P3机制税与586断连 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。```

### A2 · §一 #586 企微断连丢信

▶ 粘贴端：CC ｜ 泳道：op0916q-aibot-drop-586

```
[OP-0916-Q]【CC】aibot断连补漏
【设置】执行环境：CC ｜ 分支：`claude/op0916q-aibot-drop-586`（新建自 master） ｜ worktree：☑（op0916q-aibot-drop-586，收工自删） ｜ 工作区：仅代码与单测，**不重启、不停启计划任务 ZhuopinAibotDevListener、不向企微发任何消息** ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_P3机制税与586断连
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916Q-aibot断连补漏。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #586（`python 0-学习与工具/工具-队列查询.py --row 586 --section 一 --field all`）` → ② `CLAUDE.md` → ③ `5-平台底座/wecom-aibot-service/` 场景说明，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 先取证再修：审计 jsonl 只 grep／tail，不整读。核实断连是否与每小时 :18 提醒发送同一连接／同一事件循环阻塞相关，结论写回状态列。
🔴 修复面：⑴ 断连后重连补拉或至少「断连窗口告警」让丢信可被发现；⑵ `queue_append_deferred_lock_busy` flush 幂等（按消息 id 去重、成功即出队原子化）＋单测「延后写入只执行一次」。
🔴 重启与上线是 🟡：代码与单测完成即收工，由 Cowork 合入后用 `工具-执行体对齐重启.ps1` 重启。
🔴 开工先确认分支基于最新 master（`git rebase master`；冲突即 PARTIAL 停）。
🔴 **大文件不整读**：>24KB 文件先 `grep -n` 定位再 Read 的 offset＋limit 分段读（守卫会直接拒绝整读，别绕）；重输出交子代理。越 150k 软提醒先 commit 一个可验证增量再交接，越 250k 立即收尾。
🔴 一切 `reports/` 产出写**主工作区**。主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后 git 写操作先断言 `git rev-parse --show-toplevel`。测试前台跑、`pytest -q --tb=short`，跑完即 commit。
🔴 心跳：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916q-aibot-drop-586 --text "已开工"`；每里程碑追加；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916q-aibot-drop-586 --done --batch B-0916_P3机制税与586断连 --text "产出落点：<落点>"`。
🔴 收工只 push 本分支，不 ff master；登记 `合入登记/pending-ff.jsonl`；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。```

## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】P3机制税与586断连-2026-09-16.md" -FullAuto -Yes -Detach
```