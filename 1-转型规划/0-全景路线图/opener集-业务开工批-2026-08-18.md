---
status: 待执行
title: "opener 集 · 业务开工批（2026-08-18）—— 五件可立即开工 ＋ 次序与并行矩阵"
created: 2026-08-18
执行方: 各 CC session ／ Cowork 业务总线
派出线: Cowork 全景路线图线（受 Cowork 值周巡检派出）
来源: Shao Peishen 2026-08-18「现在能开业务 opener 的 task 请都开出」；N3 已拍 (a)（SC2／O1 8 月底前建目录＋出 propose）
---

# opener 集 · 业务开工批（2026-08-18）

## 〇、先看这张矩阵，再复制开场词

| # | 任务 | 环境 | 能否立即开工 | 可与谁并行 | 触碰区 |
|---|---|---|---|---|---|
| ~~**A1**~~ | ~~SC2 需求 grill~~ | ~~Cowork~~ | ✅ **已完成（2026-08-18，本 session 直接跑，未转场）** | — | 产出＝`4-数字员工/采购部/SC2-采购周报自动生成/需求grill产出-2026-08-18.md` |
| **A2** | **SC2 建造**（propose→design→apply） | **CC** | 🟢 **A1 已完成，硬序已解除，可立即开工** | B／C／D 全可 | `4-数字员工/采购部/SC2-*`、openspec |
| **B** | **O1 生产排程优化 · 收割建造** | **CC** | 🟢 **零依赖，立即** | A／C／D 全可 | `4-数字员工/运营部/O1-*`、`5-平台底座`（只读复用） |
| **C** | **#266 穿透改造 ＋ #118 优先级补全**（同车） | **CC** | 🟢 **零依赖，立即** | A／B／D 全可 | `4-数字员工/采购部/SC8-*`、`.51:8091` |
| **D** | **QD-B C01–C10（#340 实现）＋ 档一档二** | **CC** | 🟢 **零依赖，立即** | A／B／C 全可 | `4-数字员工/质量部/QD-B-*`、`.51:8093` |
| **E** | **三封跟进信起草**（陈忱／姚祖怡／唐燕萍） | **Cowork** | 🟡 **软序：等 C／D 有可复核成果更好，但可先起草** | 全可 | `6-人才与组织/部门AI专员跟进/`、README |

**四条 CC 任务（A2／B／C／D）触碰区两两不重叠，可四线并行**，各自独立 worktree。

🔴 **唯一的硬序是 A1→A2**；**C 内部 #266 与 #118 必须同一 session、不可拆两个 worktree**（同场景同批文件，且姚祖怡是按单料号逐个核的，两处若分批变会让她无法归因——同 2026-08-05 #262/#263 的先例）。

⚠️ **不在本批**：#334 物料看板（Shao Peishen 2026-08-17 已定级 P2、明确排在 #266/#118 之后）；SC4（前置标准合同条款库已滑、无 owner）；O3／客诉分流／Q5（排期在 12 月，前置未到开工窗口）。

---

## A1 ·【Cowork】SC2 采购周报 —— 需求 grill（第一道把关首个样本）

> **为什么它排第一**：N3 已拍 (a)，SC2 与 O1 须在 **8/31 前**各建目录并出 openspec propose；而 SC2 又被选为 grill 机制的**首个样本**（唯一「本月到期＋零外部阻塞＋全新场景」的一个，无历史包袱、指标第一次有基线意义）。
> 🔴 **不等 `zhuopin-需求grill` skill 落地**——grill 的三条机制（M1 设计树/前沿、M2 找事实是 agent 的活、M3 前沿为空才算完）**不需要 skill 就能执行**；skill 只是把它固化。先跑通再固化，符合本项目「先跑通最小验证再规模化」。

```
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：无（主工作区）｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 A1 节 ＋ 1-转型规划\0-全景路线图\需求收敛第一道把关-grill机制融合方案-2026-08-18.md 全文 ＋ CLAUDE.md 当前进度，按文件执行 SC2 需求 grill。

开工前先读 1-转型规划\0-全景路线图\session接力-Phase1收口.md 的【2026-08-18】节。

有差异再问我，没有就直接开工。
```

**任务**：对 **SC2 采购周报自动生成** 跑一次完整 grill，产出可直接喂 openspec propose 的需求树。

**必须守的四条接缝**（方案 §四）：
1. **分流判据**：每问出一个问题先过三选一——**能从环境查到 → 自己查，不问任何人**（SC2 的 U9C 端点能力、现有 supplychain 是否有可收割件、采购周报现状是谁在做/怎么做、`5-平台底座` 有哪些现成连接器，**这些全部是可查事实，一条都不许写成待确认**）；**只有 Shao Peishen 能定** → 本轮问他；**只有姚祖怡知道** → 落「判例包待办」，不在 grill 里问。
2. **一轮问完前沿**，逐条编号 ＋ 各给推荐答案 ＋ 各给默认项；**一个问题若取决于本轮另一个尚未答的问题，放下一轮**。
3. **前沿为空才算完**，且他确认达成共识前不得开工。
4. **产出两样**：① 需求树（已 settled 分支 → 将来直接充当 `design.md` 的 Decisions；仍 open 的 → Open Questions）；② **「进入判例包的开放点数量」基线值**——这是 grill 机制自身的可证伪判据，必须记下来。

**边界**：本任务**只做需求收敛，不写任何代码、不建 openspec 变更包**（那是 A2）。**建目录**（`4-数字员工/采购部/SC2-采购周报自动生成/`）可以在本任务内做，因为 N3 的验收判据就是 8/31 看目录在不在。

---

## A2 ·【CC】SC2 采购周报自动生成 —— 建造（propose → design → apply）

> ✅ **A1 已于 2026-08-18 完成，硬序已解除。** 需求已收敛：**5 轮 14 问、前沿已空**，产出件 `4-数字员工/采购部/SC2-采购周报自动生成/需求grill产出-2026-08-18.md`。
> 🔴 **本任务的第一条纪律：不要重想一遍需求。** 产出件 **§二 的 14 项 settled 直接抄进 `design.md` 的 Decisions，§三 的 5 项直接抄进 Open Questions**——这正是 grill 机制接缝 2 的设计目的（消除一次重复劳动）。**若你觉得某一项该改，回报，不要自行改。**
> **N3 已达成一半**：SC2 目录已建（判据是 8/31 看目录在不在）；**剩下的一半是 8/31 前出 propose**。

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `sc2-weekly-report-build` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 A2 节 ＋ 4-数字员工\采购部\SC2-采购周报自动生成\需求grill产出-2026-08-18.md 全文 ＋ 根 CLAUDE.md（§5 场景固定流程与硬约束、§7 合规红线），按文件执行 SC2 建造。

开工前先跑 git fsck --connectivity-only 与 git pull；并按触碰区 grep 两份队列真身与归档确认无在办行重叠。

有差异再问我，没有就直接开工。
```

### 范围（全部来自 A1 已拍板，不再重议）

- **数据源**：ERP + SRM 双源（`ZpViewPurOrder/Query`／`GR/Query`／`Purchase/Query` ＋ `XkySrmConnector.get_confirmed_dates`／`get_receive_board`）；**砍掉质检与物流**（MES/LAN 与 O3 短期取不到）。
- **档位**：mock 跑通后**同一变更包内即切真实，首版直达档 2**。红线「先 mock 再真实」照守，只是不拆包。**不承诺档 3。**
- **交付形态**：Web 页，**路由前缀从第一天就写 `/procurement/sc2`**；过渡期自有进程 + 复用 `install_flask_gate`（共享口令）+ `install_flask_access_log`；**网关落地后只改映射、不改场景代码**。
- **不做**：自然语言查询（二期）、推送管理层（二期，首版只推采购部群 + 姚祖怡）。
- **L3 异常确认**：周报页上一个「确认发布」按钮（与 SC8 看板「✅正确/❌误判」同一套交互）。
- **回溯窗口**：本周 + 上周对比 + 上月环比（全景原文已定）。
- **指标清单**：**先把算得出的全部放上，不预先砍**——留给姚祖怡批改删减（这是 D5「先建再问」的直接推论）。

### 🔴 四条必须守住的

1. **F14 那条坑必做**：首碰 `ZpViewPurOrder/Query` 等新端点，**先做一次「故意传错参数名」对照测试**——`POChange/Query` 传错参数名会**静默返回全表**（FI2 场景 CLAUDE.md L214-222 的实测教训）。
2. **过渡期第 5 个端口须显式留痕**：这是对「新场景不新起端口对外」硬约束的**显式豁免**，已获 Shao Peishen 认可（产出件 D9）；**须在场景 CLAUDE.md 里再留一次痕，不得默默占用**。
3. **`tests/conftest.py` 照抄既有场景（SC7/O2）的路径引导代码**（队列 #300），放在任何 import 之前。
4. **网关不在本任务范围内**——它是独立机制类变更包（9 月上旬、归 CC 环境侧、须走 openspec + design 审）。**本任务只写 `/procurement/sc2` 前缀并预留 auth 接入点，不碰网关本体。**

### 收口

按 §5 场景固定流程：`/opsx:propose` → **停下等 Shao Peishen 审 design** → apply（先测后实现）→ 真实数据验证 → archive → 场景 CLAUDE.md 六段式 → 发布收口四关 → 起草跟进信（**发送三条硬前置：合入 master／`.51` 已部署冒烟／用真实案例端到端复现**）。

⚠️ **判例包（4 项：指标与口径／3 人天基线核实／给谁看与频率／异常阈值）由 Cowork 起草，不在本任务内**；按 D5，**等最小周报建出来之后再发**——拿实物去问她，不拿抽象设问去问。

---

## B ·【CC】O1 生产排程智能优化 —— 收割建造

> **为什么它零依赖**：O1 属**收割型**（supplychain `smt_scheduling`：到货+工时→完工日，已 MVP）。前置总表**无 O1 行 ＝ 无外部知识/数据前置**。N3 已拍 (a)，8/31 前须建目录并出 propose。
>
> 🔴 **2026-08-18 更正：本节原写「路径与 O2 完全同构」，那句不准确，且掩盖了一个会在开工第一天撞上的事实。** 两条已实测：
> ⑴ **`ZpConnector.get_production_plan()` 没有真实端点**——`erp_connector/connector.py:927` 明写「zp 无对应端点。mock→CSV 回退；**real→fail-loud**」，TODO 指向 `UFIDA.U9.MO.MO.MO`（`CommonEntity/Query`，**外网 404 至今未开放**）。⇒ **O1 的核心输入「生产计划/工单」在 real 模式下取不到。**
> ⑵ **收割策略 L131 明写「〔O1 生产排程未在本轮收割，待后续〕」**——O2/SC3/SC5 当时都收割了，**O1 是被跳过的那个**，原因未记载。
> **⇒ O1 到不了 SC2 那个「首版直接档 2」的目标**（SC2 的 ERP 采购段是通的，O1 的生产计划段不通）。**三段输入现状**：到货 ✅ 可取（`ZpViewPurOrder/Query`／`GR/Query`／SRM `get_receive_board`）／工时 ❓ 无端点记载／生产计划 🔴 不可达。
> **本节的「零依赖·立即」仍然成立**（指无外部知识前置、可立即建目录出 propose），**但目标档位须按下方 Q11 的拍板结果执行，不得默认照抄 SC2 的档 2。**

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `o1-smt-scheduling-harvest` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 B 节 ＋ 根 CLAUDE.md（§5 场景固定流程、§6 收割策略、§7 合规红线）＋ 4-数字员工\运营部\O2-物料齐套预警\CLAUDE.md（同构先例），按文件执行 O1 收割建造。

开工前先跑 git fsck --connectivity-only 与 git pull；并按触碰区 grep 两份队列真身与归档确认无在办行重叠。

有差异再问我，没有就直接开工。
```

🔴 **⓪ 步硬前置（Shao Peishen 2026-08-18 拍 Q12(c)）**：**先把 `C:\Users\Paul Shao\OneDrive\Projects\supplychain` 的 `smt_scheduling` 取出来读一遍（约半小时），再定「收割」还是「从零重写」——不预设结论。** 判断依据＝该实现是否真的覆盖「到货+工时→完工日」、以及它依赖哪些数据。**读不到该仓库就先回报，不要猜着建。**

⚠️ **一处已澄清、勿再重复核查**：Shao Peishen 2026-08-18 说明「supplychain 已都收割进本仓库」——**这句对连接器/notifiers/crm_notifier/kit_engine 属实**（均在 `5-平台底座/zhuopin_platform/`，六个场景在真实复用）；**但 `smt_scheduling` 是例外，它不在**（底座 `agents/` 下只有 `kit_engine.py`，全底座 63 个 `.py` 无排程模块，`4-数字员工/` 五个场景包也没有），**且收割策略 L131 自己写着「〔O1 生产排程未在本轮收割，待后续〕」——文档与代码两条互相印证。**

**目标档位（Shao Peishen 2026-08-18 拍 Q11(a)）**：🔴 **首版只到档 1**——mock/CSV 夹具跑通引擎，`real` 模式**显式 fail-loud**，等 `CommonEntity/Query` 开放再切档 2。**不得照抄 SC2 的「首版直达档 2」**。**已知代价并接受**：O1 年内很可能停在档 1，对「使用线 ≥5 场景档 3」零贡献。**未选 CSV 桥接到准真实**，理由＝那会新增一个「人工定期投放」依赖，而我们 08-10~08-17 刚被同一形态咬过（唐燕萍 5 个工作日零投放、无人察觉）；**若日后改走 CSV 桥接，必须同批建源头断供检测**（FI2 08-17 那条补丁是现成范式）。

**范围**：按 §5 场景固定流程走 —— 建 `4-数字员工/运营部/O1-生产排程智能优化/` → `/opsx:propose` → **停下等 Shao Peishen 审 design** → apply（先测后实现）→ archive → 写场景 CLAUDE.md 六段式。**N3 的 8/31 判据只要「目录已建 ＋ propose 已出」，档 1 完全满足。**

**若 ⓪ 步判定为收割**，则守收割纪律（§6）：复用件进底座、**必补两块**——① 审计统一进平台 `audit`（`scenario="O1"`）；② OEM 隔离接口预留（O1 读内部生产计划，**不强加 OEM 路由**，同 O2）。

**红线**：先 mock/脱敏跑通再切真实；L1 内部看板、无 L2 门禁；**不新起端口**——若要 Web 呈现，注册到统一门户 `/operations/o1` 路由下并预留网关 auth 接入点（§5 硬约束）。

---

## C ·【CC】#266 替代料多层穿透改造 ＋ #118 优先级补全（同车，不可拆）

> **前提已定死，无待确认口径**：姚祖怡 2026-08-12 回件（采购部#13）已把 #266 判例 3 行批改完毕（1❌／2❌／3✅），**判定＝改造为多层穿透**；#118 的 ①+④ 首版已建造并真实部署 `.51:8091` 验证通过，本次补齐其余部分。

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `sc8-substitute-penetration-priority` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 C 节 ＋ 队列 §一 #266／#118 两行全文 ＋ 4-数字员工\采购部\SC8-客户订单交期智能承诺\CLAUDE.md，按文件执行。

开工前先跑 git fsck --connectivity-only 与 git pull。

有差异再问我，没有就直接开工。
```

🔴 **两条硬约束**：
1. **#266 与 #118 必须同一 session、顺序不可颠倒**（先 #266 穿透、后 #118 优先级）。分两个 worktree 必撞（同场景同批文件）；**顺序颠倒会让姚祖怡无法归因**——她是按单料号逐个核的，两处同时变则「数字为什么变了」说不清（同 2026-08-05 #262/#263 先例）。
2. **交付物硬性含「修复前后数字对照」＋「黄金基准重跑与差异说明」**（同 §四 #51 (a) 先建后签的既有要求）——缺了她无法签，等于把验收责任静默转移。

**发布收口四关照旧**（§5「发布即收口纪律」）：功能门槛（全量绿＋零回归＋黄金基准不漂移＋openspec 归档＋场景 CLAUDE.md 更新）／部署段（真部署 `.51:8091`＋冒烟＋可常驻）／合规不放松／先灰度后依赖。

🔴 **发送硬前置**：完工后起草的跟进信**必须**满足三条才发——① 改动已 ff 合入 master（`git rev-list --count master..<分支>` ＝ 0）；② `.51` 已部署且冒烟通过；③ **用她原始举证的那个真实案例端到端复现**（#266 用 `F02N.0233`／`S02Y.0207`；#118 用 `S02Y.0210`）。

---

## D ·【CC】QD-B C01–C10 跨模块校验（#340 实现）＋ 档一档二

> **评估已完成，本任务是实现**：#340 评估（2026-08-17，commit `7b3933d`）已产出 `4-数字员工/质量部/QD-B-立项审核门禁/C01-C10跨模块校验-可行性与工作量评估-2026-08-17.md`，结论＝**做（分三档）**。
> 🔴 **最要紧的一条**：陈忱点名的那组（模块一人月数 ↔ 模块八至十）**引擎今天已经在核**（规则 14＋68，三份真实黄金样本偏差全 0.0%）；**缺陷在呈现**——`report.py` 恒定输出「C01–C10 尚未实现」，**把已核过的说成没核**，他正是看到这句才把它当待补能力点名。**⇒ 第一优先是修这句假话，不是补功能。** 另 C09≡规则59、C10≡规则60 公式逐字相同，**真新增只有 C02/C03/C05/C06/C07 五条**。

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `qdb-cross-module-c01-c10` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 D 节 ＋ 4-数字员工\质量部\QD-B-立项审核门禁\C01-C10跨模块校验-可行性与工作量评估-2026-08-17.md 全文 ＋ 同目录 CLAUDE.md ＋ 队列 §一 #340 行，按文件执行。

开工前先跑 git fsck --connectivity-only 与 git pull。

有差异再问我，没有就直接开工。
```

🔴 **⓪ 步硬前置（§四 #64，不得跳过）**：**先核 8D 案例 4／5 的 ASIL 等级**——`CLAUDE.md` §7.5 红线分档：**ASIL C/D ＝ AI 绝对禁区＋FSE 双签；A/B 可 AI 辅助＋资质人员签字**。**命中 C/D 即停下回报、不得径直往下做**（该前置由值周巡检 2026-08-17 主动加上，因其未核该等级、不替它推定）。

**边界**：档一（修呈现层假话＋把已实现的规则如实标出）与档二（新增 C02/C03/C05/C06/C07）；**C09/C10 不重复实现**（等价于规则 59/60）。§四 #63 已拍 (c)——接受陈忱的裁定为当前态、把合规缺口显性留痕，**该留痕不得在本次实现中被抹掉**。

---

## E ·【Cowork】三封跟进信起草（🔴 四位专员的串行闸现已全部解除）

> 🔴 **本节的前提在 2026-08-18 刚被更正过，务必按新口径读**：README「发送状态」的闭环形态已由 1 种扩为 3 种（`📥 已回件并回灌` ／ **`✅ 无需回复`** ／ **`📨 已确认闭环`**）。据此复核后：

| 专员 | 闸 | 依据 |
|---|---|---|
| **陈忱** | 🟢 开 | 质量部#6 `📥 已回件并回灌`（08-17） |
| **姚祖怡** | 🟢 开 | 采购部#14 经复核属 **`✅ 无需回复`**（正文明写「不用截图、不用写信、不用等回复」「没有期限」）⇒ **§四 #61⑵ 那句「串行闸已重新锁上」在新口径下不成立** |
| **唐燕萍** | 🟢 开 | 财务部#12 经复核属 **`📨 已确认闭环 2026-08-10`**（她 08-10 已投文件并在群知会，我方机器人当日回「已收到并入库……不用再单独知会我」） |
| **陈承** | 🟢 开 | IT部#8 属 **`✅ 无需回复`**（正文明写「这件到此收口，你不用再回了」） |

**三封的要点**（各自 ≤3 决策点；判据/口径类**永不默认生效**）：

- **质量部#7 · 陈忱** —— C01–C10 的真实结论回话：**坦承 `report.py` 那句「尚未实现」是错的**、他点名那组其实一直在核（规则 14＋68，黄金样本偏差 0.0%）；真新增只有 5 条；§四 #63 那条合规缺口按 (c) 显性留痕。**触发点：D 完工后发**（发送硬前置三条同 C）。
- **采购部#15 · 姚祖怡** —— ① 先取她「刚刚回复」的那份批改件（⚠️ **归档链路目前看不到 08-12 之后的采购部件，第 ⓪ 步是把原件取回落档**，R6：只认文件不认转述）；② #266/#118 进展；③ **#334 物料看板的次序与理由须一并告知，不得默默押后**（她用的是「我要求」的措辞，压后有被读作不重视的风险——这一条 2026-08-17 已写在 #334 行内）。**触发点：C 完工后发。**
- **财务部#13 · 唐燕萍** —— **不是新需求信，是一句问停投**：`D:\airead` 08-10 之后 **5 个工作日零投放**。🔴 **措辞必须先认我方的错**：群里那句「取不到会有告警」**说的时候告警还不存在**（源头断供检测 08-17 才补齐），她按「有问题你们会告警」放手是完全合理的。**按 §四 #59 已拍板 (a) 走「企微一句话」，不写正式信。**

---

## F ·【CC】队列 #345 —— #300 引导在扁平布局下 fail-loud，27 处未修（Shao Peishen 2026-08-18 拍板插队）

> **它是怎么被发现的**：2026-08-18 C（#266/#118）发布收口时**真实触发**——`sync-to-server.ps1` 把 master 推到 `.51` 后 `BaoguanWebServer` 重启失败，**计划任务 `LastResult=0` 但进程秒退**、8091 无监听、`/api/ping` 502。前台跑才看到 `RuntimeError: 未找到仓库根标记 5-平台底座/zhuopin_platform`。
> **根因**：#300 引导（2026-08-08）找不到 monorepo 标记时**无条件 `raise`**，而长开服务器是**扁平布局** `C:\baoguan\{zhuopin_platform,app}`——平台底座是 `app` 的**兄弟目录**、已由 `deploy-server.ps1` `pip install -e` 进 venv，**本就没有那个标记**。
> 🔑 **最讽刺的一处**：同一文件的 `_find_env()` docstring **早就写明了这套扁平布局**，引导却没照顾到它——**同一文件里两处对同一部署形态的认知不一致**。
> ⚠️ **「计划任务 LastResult=0 但进程秒退」值得单记**：这是「外观同成功」在部署面的形态，**只看计划任务返回码会判它healthy**。

### 🔴 开工前必读：范围已经比 #345 立行时小了，别照旧描述做

**两处已修，不要重复劳动**：
- **SC8（8091）** ✅ 已修并验证（`a858769` → ff 入 master `68430db`；重新部署后 `/api/ping` 200、`/` 与 `/cases` 200、真实全量重算成功，**服务器文件 SHA256 与 master 逐字节一致**）
- **QD-B（8093）** ✅ **已被 D 的 session 顺手修掉**（`6ec316f fix(QD-B): 修复 .51 服务入口被 #300 引导硬失败钉死`）——#345 立行时写的「QD-B 下次 sync 必然起不来」**已不成立**

⚠️ **一条判据陷阱，务必照抄别自创**：**不能用 grep「未找到仓库根标记」来判修没修**——已修的文件里这句**仍然留着**（作为真正失败时的兜底报错）。**正确判据 ＝ 是否仍含无条件的 `raise RuntimeError(f"未找到仓库根标记`**。按该判据实测（2026-08-18）：**仍有 27 个文件未修**，清单见下。

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `bootstrap-flat-layout-fallback` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 F 节 ＋ 队列 §一 #345 行全文 ＋ 已修样板 4-数字员工\质量部\QD-B-立项审核门禁\scripts\run_qd_b_web.py 顶部引导段（第 1-40 行），按文件执行。

开工前先跑 git fsck --connectivity-only 与 git pull。

有差异再问我，没有就直接开工。
```

### 分两步，第一步就把火灭掉

**第一步（本次做，纯 bugfix，不走 openspec）** —— 只修**会在 `.51` 扁平布局下真实起不来**的那些，照抄 QD-B/SC8 已验证的改法：

- 🔴 **`4-数字员工/财务部/FI2-三单匹配自动对账/` 的 `fi2/run.py`／`fi2/confirm.py`／`fi2/dump_u9c_snapshot.py`** —— **FI2 是 `.51:8094` 长开服务，是四个服务里唯一还没修的那个，下次 sync 必然起不来**
- `4-数字员工/财务部/FI1-.../fi1/run.py`／`confirm.py`（FI1 虽暂缓，入口脚本同族）
- `5-平台底座/wecom-aibot-service/scripts/*.py`（**9 个**：`run_aibot_service`／`alert_webhook`／`approve_followup_letter`／`check_connection`／`decision_reminder_check`／`dispatch_followup_letters`／`echo_test`／`flush_pending_lock_appends`／`push_followup_letter`）—— **企微机器人是常驻服务 ＋ 每日批处理定时任务，它起不来会静默吃掉跟进信投递与告警**
- `4-数字员工/采购部/SC1-.../main.py`／`SC7-.../sc7_inventory/agent.py`／`SC8-.../sc8/answer_confidence.py`（CLI 入口）

🔴 **`tests/conftest.py` 一律不动**（清单里那 8 个）——**在 monorepo 内 fail-loud 是有价值的**：测试就该跑在仓库里，找不到标记说明环境真错了，此时静默回退才是隐患。**这一条是 #345 行内已写明的判断，不要顺手"统一"掉。**

**第二步（本次只出提案，不 apply）** —— 把这段引导收拢成平台底座的**一个共享函数**（如 `zhuopin_platform.bootstrap.ensure_paths()`），monorepo 与扁平布局各一条分支，**一处修全体受益，而不是 27 份各自维护、彼此漂移**。
⚠️ **它改变既有模块在相同输入下的行为 ⇒ 命中 §5 机制类门槛③，须走 openspec + design 审**——本次**只做到 propose，停下等 Shao Peishen 审**。

### 验收

- 每个改动文件：**在 monorepo 内 import 正常**（跑该场景全量测试零漂移）＋ **模拟扁平布局能回退**（临时构造兄弟目录，或直接在 `.51` 上验）。
- 🔴 **FI2 必须真部署 `.51:8094` 并冒烟**（`/api/ping`＋关键页 200），不能只在本地判"应该好了"——**这个 bug 的全部特征就是本地看不出来**。
- **不引入静默失败**：平台底座真不可用时，下方 import 仍须 `ModuleNotFoundError` 明确报错（SC8 那版已守住这条，照抄）。

---

## G ·【CC】SC2 部署 `.51` ＋ 收掉 A2 留下的尾巴（Shao Peishen 2026-08-18 答 (a)）

> **为什么它现在最要紧**：SC2 判例包（O-1~O-4 四项）**已经准备好但发不出去**——D5 定的是「先建最小周报再让姚祖怡批改」，而 **SC2 至今未部署 `.51`**（场景 CLAUDE.md 明载 tasks 9.5 未完成），**他打不开就没法批**。
> 🔴 **卡住它的不是决策点数量上限**（那条已于 2026-08-18 正式取消），**而是发送硬前置第②条「`.51` 已部署且冒烟通过」**——#228 事故正是绕过它所致，不得放宽。

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `sc2-deploy-and-archive` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 G 节 ＋ 4-数字员工\采购部\SC2-采购周报自动生成\CLAUDE.md 全文 ＋ openspec\changes\sc2-weekly-report-mvp\tasks.md，按文件执行。

开工前先跑 git fsck --connectivity-only 与 git pull。

有差异再问我，没有就直接开工。

```

### 三件事，按序

**① 部署 `.51` ＋ 冒烟（tasks 9.5）** —— 这是本任务的主目标。
- 🔴 **必须先看 F 节那颗雷**：`#300` 引导在扁平布局下 fail-loud。SC8／QD-B 已修，**SC2 是新场景、其入口脚本大概率带同一颗雷**——**部署前先照 QD-B `scripts/run_qd_b_web.py` 顶部引导段改好，别等它在服务器上炸给你看**。
- **路由前缀 `/procurement/sc2`**（D2/D9 已定），复用 `install_flask_gate` ＋ `install_flask_access_log`。
- ⚠️ **过渡期第 5 个端口须在场景 CLAUDE.md 显式留痕**——这是对「不新起端口对外」硬约束的显式豁免，已获 Shao Peishen 认可，**不得默默占用**。
- **冒烟三件套**：`/api/ping` ／ 关键页 200 ／ **一次全量重算**（实测约 2m19s，走 `POST /api/refresh`，别挂在 HTTP 请求上）。
- 🔴 **验证进程 CreationDate 是否真的刷新**——不能只看脚本打印「服务已重启」（SC8 2026-07-14 那次的教训：脚本报重启成功而旧进程仍占端口）。

**② 收掉 tasks 剩余 7 项并 `/opsx:archive`** —— A2 那个 session 完工时如实留了 7 项未勾（42 已勾）。**逐项判：能做的做掉，做不了的写明理由**；**全 [x] 才 archive，不假装完工**。

**③ 回写场景 CLAUDE.md 部署状态段** —— 含地址、冒烟结果、过渡期端口豁免留痕。

### 完工后

**不要自行起草跟进信** —— SC2 判例包由 Cowork 起草（O-1 指标与口径／O-2「3 人天/月」真实基线核实／O-3 给谁看与频率／O-4 异常波动阈值，**基线数 ＝ 4**）。**你完工并冒烟通过后回报一句即可，Cowork 据此发信。**

---

## H ·【CC】导出 QD-B 真实立项书案例，供 C05/C06/C07 判例表使用（Shao Peishen 2026-08-18 答 (a)）

> **为什么要先导出**：C05/C06/C07 的判定口径要问陈忱，而**判例批改法硬规则第 1 条禁止抽象设问**——必须拿真实立项书案例做成三选一对照表。**现有三份黄金样本（华丰／EQ17／邦奇）覆盖面太窄**，不足以支撑三条规则各自的判例。

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `qdb-c05-c07-case-export` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 H 节 ＋ 4-数字员工\质量部\QD-B-立项审核门禁\CLAUDE.md ＋ 同目录 C01-C10跨模块校验-可行性与工作量评估-2026-08-17.md，按文件执行。

有差异再问我，没有就直接开工。

```

**任务**：从 QD-B 可用的历史立项书里，**为 C05／C06／C07 各导出 ≤10 条真实案例**，每条给出「真实场景数据 ｜ 现状判定 ｜ 拟改判定」三列所需的原始字段。

**三条各自要什么**：
- **C05（匹配口径）** —— 需要能体现「按什么匹配、匹配到什么程度算对上」的真实差异样本。
- **C06／C07** —— **先确认模块四是否已能解析**；解析不了就先回报，**不要为了凑案例去猜字段**。
- **C07（ASIL 同义词表）** —— 导出立项书里 ASIL 字段的**真实写法全集**（大小写、中英文、简写、空值），这正是同义词表要覆盖的对象。

🔴 **红线**：真实立项书含 OEM 技术方案时走 `data_isolation_layer`；**导出件脱敏后落 `data/golden/`（`.gitignore` 覆盖）**，不入库。**ASIL C/D 相关内容不得进入任何 AI 预填/判定链路**（§7.5 绝对禁区）。

**产出**：一份可直接做成判例表的素材件（放场景目录），**不写信、不做判例表本体**——那是 Cowork 的活。

---

## I ·【CC】`platform-bootstrap-ensure-paths` apply（design 已获 Shao Peishen 2026-08-18 批准）

> **背景**：#345 第一步（17 处入口不再硬失败）已由 `0c67355` 完成；**第二步是把这段引导收拢成平台底座的一个共享函数**，一处修全体受益，而不是 35 份各自维护、彼此漂移。**propose 与 design 已产出，design 已批准，本任务是 apply。**
> 🔴 **原 propose 的那个 CC session 已收工**——回合制 session 无法自我唤醒，故另起 session 领。

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `platform-bootstrap-apply` ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\opener集-业务开工批-2026-08-18.md 的 I 节 ＋ openspec\changes\platform-bootstrap-ensure-paths\ 全部四件（proposal/design/tasks/specs）＋ 队列 §一 #345 行，按文件执行 apply。

开工前先跑 git fsck --connectivity-only 与 git pull。

有差异再问我，没有就直接开工。
```

### 五个决策点已拍板，**按此执行，不再重议**

| # | 结论 |
|---|---|
| 1 | **(a) 极小 stub**（每入口 3-5 行）。**不走 `.pth`/`sitecustomize`**——那是用不可见的环境魔法替换可见的显式引导 |
| 2 | **(a) `tests/conftest.py` 保留 fail-loud**，用 `ensure_paths(strict=True)` 显式声明。🔴 **不得顺手统一成非 strict** |
| 3 | **(a) 分两批**：批一 18 个非测试入口 → **验证** → 批二 12 个 `conftest.py` |
| 4 | **(a) CI lint 硬门禁 ＋ 过渡期先按 (c) 跑一轮**——⚠️ **复合默认项，两半都要做** |
| 5 | **(a) `_find_env()` 不同批收拢**，但**须在队列另立一行登记**（见下） |

### 三条执行要求

1. **`tasks.md` 22 项全部未勾** —— 逐项做、逐项勾；**做不了的写明理由，不假装完工**；**全 [x] 才 `/opsx:archive`**。
2. **批一做完必须先验证再动批二**（决策点 3 的核心）：批一 18 个入口改完，**跑全量回归零漂移 ＋ 至少一个 `.51` 服务真实重启验证**，通过后才做批二。
3. 🔴 **决策点 5 的另一半不要漏**：`_find_env()` 同族收拢**须在队列另立一行**。⚠️ **但机制类可动 WIP 当前 25/16 超限**——**若立行被 `release` 阻断，不要走 `WIP豁免：` 逃生阀**，改为在收工报告里明确写「该行待 WIP 清扫后补立」并回报，由值周巡检接。**理由：本项目 2026-08-18 已用过一次豁免，不宜连用；且这一行不紧急。**

### 已知边界

- **`SC2/run_sc2.py` 是第一步唯一的漏网**，已交由 opener **G 节**在部署前顺手修掉。**若 G 尚未跑，本任务批一可把它一并纳入**（形态相同、零额外风险）；**若 G 已跑，跳过它，不要重复改**。
- 本包**改变既有模块在相同输入下的行为**（fail-loud → 可回退），命中 §5 门槛③，**openspec 全流程必走完，不得简化**。

---

## 六、收工段（各 opener 通用）

- **CC**：commit + push + 收工重跑台账；一任务一 worktree；场景 CLAUDE.md 当场更新（六段式）。
- **Cowork**：不自行 commit——列出全部新产出/修改文件清单 ＋ 建议 commit message ＋ **登记队列 §二 待 commit 批次**（🔴 **文件清单里每一条路径必须用反引号包裹**，否则 sweep 判为孤儿、静默漏 commit，见 2026-08-18 `B-0818_2` 行），并提示 Shao Peishen 一句话交 CC。
- **两桌通用**：登记后立即 `Start-ScheduledTask -TaskName "ZhuopinCommitSweep"`；**批次转 ✅ 后还要看 `git status` 是否真干净**，两者不一致即说明有声明未被匹配。
- **编辑锁**：改队列前 `acquire`（🔴 **要取号就必须在这一次 `acquire` 上带 `--reserve`**，事后补取会被自己的锁挡住、只能删行重来）、改完立刻 `release`。
