---
status: 在办
---

# 拆件巡逻报告 · 2026-08-28（定时班 · 非交互）

**识别 2 行（#427 质量／#428 采购），已拆 2 行，销号 2 行，新增 §一 行 0 条、§四 行 2 条（#133／#134）。**

---

## 🔴 本班头条（不属任一拆件行，但由拆件当场撞出，且它推翻了一条既有结论）

**`python-docx` 读不到 Word 复选框内容控件里的勾 —— `采购部#19` 整封信的发信前提是一次读取假阴性，不是专员漏勾。**

- **实测**：`采购部#18` 回件 docx（08-26）的三条判例，`docx.Document(...).tables[1]` 逐格 `cell.text` 全为 `''`；同一文件 `word/document.xml` 原文里 **`☒` 3 个、`☐` 6 个**，三行的第一格（✅ 这样对）**全部为 `☒`**。
- **XML 形态**（决定修法）：`<w:p><w:sdt><w:sdtPr><w14:checkbox><w14:checked w14:val="1"/>…</w:sdtPr><w:sdtContent><w:r><w:t>☒</w:t></w:r></w:sdtContent></w:sdt></w:p>` —— 勾是**内联内容控件 `w:sdt`** 里的 run，而 `python-docx` 的 `Paragraph.text` 只拼 `w:p` 的**直接** `w:r` 子节点 ⇒ 嵌在 `w:sdt/w:sdtContent` 里的 run 被整段跳过，返回空串。**权威判据 ＝ `w14:checked w14:val="1"`，不是 `cell.text`。**
- **后果（已发生、不可撤回）**：§一 #411 据此定性「9 个勾选格一格未勾（`python-docx` 逐格实读，非目测）」→ §四 #122 → `采购部#19` 第一节原样重送三条判例，并写「我们把 Word 文件逐格读过，9 个格子确实都没有内容」。**这句话把责任指向了他**，而他 08-26 就已勾全。他 08-28 回件原话：「我确实已经勾选了……**你是没有看到吗？**」
- **本班若不改读法会二次误判**：#428 这份新回件用同一读法读，四个勾选格同样返回空。
- 🔑 **判据**：**「逐格实读」这四个字本身不构成证据，要问的是「读的是哪一层」** —— 与 CLAUDE.md §5「工具静默回退」同族（工具不报错、给一个看起来合理的空值），也与 §一 #416 那条「答案 60 毫秒就到了、只是打在没人看的 WARN 里」同族：**错误不产生任何信号**。
- **处置**：机制类修法已登 §四 #133（机制可动 WIP 已超上限 22，按本章程三.2 不自行升格 §一 行）。

---

## 拆件行

### §一 #427 ｜ 质量 · 陈忱 文本回件（2026-08-28）

- **内容摘要**：确认 8D 完整性判定的 D0／D8 口径 —— 勾 `☒ 对，就是这个意思（D0／D8 两章整体豁免）`，对应条文＝「D0 与 D8 两个章节整体，不参与完整性判定、不扣分、不提示（缺失或内容不全，引擎一律视同不适用）」。
- **已执行动作**：销 #427（✅ 写在状态列开头）；结论与条文原文写入该行；**口径回灌落点＝质量专线**（本行为唯一载体，未另起行）。
- **判断依据**：他直接勾选我方反推的条文一行确认，属**已有裁决的确认**、非新判断 ⇒ 可直接回灌。
- **需 SP 项**：无。

### §一 #428 ｜ 采购 · 姚祖怡 回件 `采购部#19`（2026-08-28，docx）

**六件事全部有答复**（4 个 `☒` 勾 ＋ 2 个 `☒ 认可` ＋ 3 段自由文本；勾选格一律以 `w14:checked` 为准）：

| 事项 | 他的答复 | 回灌落点 |
|---|---|---|
| ⒈ 判例表 A 三条读数口径 | **三行全 `☒ ✅ 这样对`**（08-26 即已勾，本次复勾并附截图） ⇒ 规则反推条文获书面确认 | §一 #334 |
| ⒉ 判例表 B「答了但仍不够」 | **`☒ ✅ 这样对`，同时文字写「要例子」** ⇒ 两个信号并存 | 🔴 §四 #134 |
| ⒊ 责任人列 | 「那就等 IT，**按（a）**」⇒ 维持「—（取数缺口）」，推陈承开端点 | §一 #334 |
| ⒋ 保供看板 105 行全红 | 选 **(a) 重定分级阈值**，新阈值＝**🔴 晚 2 周以上／🟡 晚 2 周以内**；🔴 **🟠 待催与 🟢 按期两档他未提及**（四色只覆盖了两色） | §四 #110 |
| ⒌ 替代料修复黄金基准 | **`☒ 认可`** ⇒ IATF 可追溯所缺的那条书面签认到手（#51「先建后签」的「后签」已闭合） | §四 #51 |
| ⒍ 规则 1 修复前后对照表 | **`☒ 认可`** ⇒ §一 #401 的硬前置（姚祖怡签认）解除，余下全是我方动作 | §一 #401 |

- **已执行动作**：销 #428；六项逐条回灌至上表落点；#401 硬前置解除已写入其状态列；未新建任何 §一 行（六项全部并入既有行，守协议〇.10）。
- **判断依据**：⒈⒊⒋⒌⒍ 均为**专员对我方已成文选项的选择或签认**，属事实与已有裁决 ⇒ 直接回灌；⒉ 两个信号并存，属需新下判断 ⇒ 不落字，转 §四。
- **需 SP 项**：§四 #134（⒉ 的歧义）；另 ⒋ 的两档缺口随 #110 一并留给总线，本班不替他补。

---

## 本班新登 §四

- **#133** —— docx 复选框读取假阴性的修法与是否向姚祖怡更正一句（含 #411 结论更正）。
- **#134** —— 判例 4「`☒ ✅` 与『要例子』并存」怎么取用。

**同时回灌（未新增行）**：§四 #51（签认到手）、§四 #110（阈值答案 (a) ＋ 两档缺口）、§四 #122（其立项前提「9 格全空」已被证伪；(a) 已执行且已回件，但 ⑺⑻⑼ 定序仍未答 ⇒ 🟡 不销号）。

---

## 待发信盘点（只读，不发送）

- `⏳ 待你审` **12** ／ `🆕 待发` **14** ／ `⏸ 暂缓` **12**。
- **交叉红标扫描：本班未发现「队列称暂缓、README 仍 `🆕 待发`」的存量冲突。**
- 一句提醒：`⏸ 暂缓` 共 12 封 —— **促成暂缓的前提是否已变化，值周巡检宜逐封过一眼**（暂缓不是积压，但它也不会自己解除）。
- 📌 **姚祖怡串行闸本班已由锁转开**（`采购部#19` 今日回件），下一封可起草；🔴 起草前仍须现跑闸查询，不得引用本报告这句快照。

---

## 收工时另撞出两件（均非本次范围，如实登记）

1. **`release` 时 S4 桥二自动把两封信转为「📥 已回件并回灌」并开闸** —— 「质量部#10」← §一 #427（通道 `reply_arrived`）、「采购部#19」← §一 #428（通道 `stem`）。**机制按设计工作，本班未手改 README 一个字**；⇒ 陈忱与姚祖怡两侧串行闸现均为开。
2. 🔴 **本定时班的 `release` 校验⑹ 结构性跑不动，每一班都要走一次逃生阀** —— 沙箱挂载工作区上 `git status --porcelain` 实测 **约 40 秒**（两次一致），而编辑锁取脏文件那处 `timeout=20` ⇒ `TimeoutExpired` ⇒ 走「无法取得工作区脏文件状态」fail-closed 拒绝。**不是脏文件没登记**（本班改动全在 §二 `B-0828_T` 里）。已按提示走行内 `登记豁免：`，**并把实测、判据与三条可选修法登记到 §一 #382**（该行本就是拆件巡逻结构优化的载体）。🔑 判据：**fail-closed 若因环境常态触发，就从安全阀退化成每次都拦人的门；逃生阀走成常态即等于没有阀。**

## 巡逻自检

- **`--digest` 不可用**：本机 `工具-队列查询.py` 当前版本无 `--digest` 参数（`--help` 实证），按本章程一.0 退回逐条读法 —— 用 grep 在**两份真身**上筛「企微反馈自动归档」＋ `[S:...]` 机器字段，命中行再 `--row N` 全文读，未展开任何未命中行。
- **扫描范围**：机制环境 ＋ 业务场景两份；业务场景份内 0 条企微归档行。
- **并入审核结论**：本班本可新建 §一 行 6 条（六项答复），**实际新建 0 条** —— 全部并入 #334／#401／§四 #51／#110。机制缺陷（头条）本应立 §一 机制行，**因机制类可动 WIP 实测已超上限 22，按三.2 不自行处置、转 §四 #133**；未并入 §一 #416（该行触碰区＝`5-平台底座/wecom-aibot-service/`，本缺陷在拆件读取侧，并入会misfile）。
- **本轮实拿编号**：§四 #133、#134（`acquire --reserve 2 --section 四`）；§一 0 个。**另回灌未取号的既有行 6 条**：§一 #334／#401／#382，§四 #51／#110／#122。
- **一.4 起草缺口检测（改为实跑，见下）**：`draft_gap_check.py --window-days 14` ⇒ **无缺口**。（需 `PYTHONPATH=5-平台底座/zhuopin_platform` 才能 import，默认路径下 `ModuleNotFoundError`——如实登记，属沙箱无 `pip install -e` 所致，非脚本缺陷。）
- **三.6 比对结果**：写回前重读两份真身与 acquire 快照逐行比对，**未出现自己没写过的新增行**，无撞号，高水位线一致。
- **〇bis 跳过**：本班为定时非交互班次 —— **三.8 写后反查**（无本机 PowerShell）／**四.5 sweep 触发**（无本机 `Start-ScheduledTask`，sweep 每小时自然取活）／**四.6 决策提醒**（同上）按〇bis 跳过。**一.4 起草缺口检测本班改为实跑**（沙箱有只读 python 环境，属只读、零对外动作），结果见下。
- **日期基准**：本机无 PowerShell 可用，取沙箱 `date` ＝ `Fri Aug 28 13:04 CST 2026`（CST ＝ UTC+8，与会话环境日期一致）⇒ 写入日期 `2026-08-28`。**如实登记这是本章程「禁用沙箱 date」的一次让步**，两处独立信源一致故采信。
- **预案覆盖不了的情形**：⑴ 勾选格读取假阴性（本章程二.2 只说「✏️ 列非空 ≠ 改判，要看字符」，**没说「读到空也可能是假的」** —— 建议二.2 补一句，随 §四 #133 一并定）；⑵ 同一题上「勾选」与「自由文本」给出两个方向的信号（→ §四 #134）。
