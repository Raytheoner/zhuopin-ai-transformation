---
title: "Dispatch v0.5 拆件巡逻报告 —— 2026-07-30（13:00 班）"
created: 2026-07-30
status: 在办
阶段: 阶段一 dry-run（纯只读：未改队列、未改任何 .md、未拿编辑锁、未发企微、未 commit）
本轮识别: §一 待领「企微反馈自动归档」行 2 条（#160、#161，均为 IT 陈承 2026-07-30 文本反馈）
需 Paul 项: 3 项（见文末汇总）
配套: Dispatch试点-回件拆件巡逻-设计-2026-07-24.md ｜ 队列 #106/#143 ｜ 专线opener模板库.md §一
---

# 拆件巡逻报告 · 2026-07-30（13:00 班）

> 本轮为 07-30 首份报告（09:00 班未落报告文件，两条回件均标注日期 2026-07-30、推断在 09:00 班后到件）。
> 筛选口径：§一 状态＝「待领」且任务描述含「企微反馈自动归档／企微反馈归档」。命中 **2 条**：#160、#161。
> 其余含该字样的行（#102/#103/#104/#105/#113/#131/#132/#133/#134/#145/#148/#149）均已标「✅ 已拆件」，不在本轮范围。

---

## 一、#160 ｜ 陈承 → 安全两项收口回复（sa 账号 + /Services SOAP）

**队列行**：`| 160 | 企微反馈自动归档：2023458 发来文本反馈 | Paul | …27f38b1da2c99c170cbf112b5a3f6223.md | 待领 | | 2026-07-30 |`

**归档真身**：`7-外部文档/IT/IT-2023458-回复-2026-07-30-文本反馈-27f38b1da2c99c170cbf112b5a3f6223.md`（已读全文）

**内容一句话摘要**：这是对 `IT部-陈承-跟进-2026-07-26-安全两项收口-sa账号与SOAP鉴权.md`（队列 #91，07-27 07:49 UTC 已发）的正式回复——**(a) sa 账号未按请求轮换/禁用**，改用补偿性控制（生产库 192.168.6.2 已配 IP 白名单 + 仅授权账号经堡垒机登录），理由是 sa 口令在多处 ERP 客开文件中硬编码、牵涉面广无法轮换；**(b) SOAP 免鉴权已实质处置**——IIS 层对 `/Services` 启用 ipSecurity 白名单（仅 127.0.0.1/::1、192.168.6.2、防火墙出口 10.11.0.253），并**确认写端点（应收单/销售订单创建删除等）同样免鉴权**、已由同一 ACL 一并收敛；异常回显（SOAP Fault 带调用堆栈）**未做代码层修复**（修复点在 U9C 官方共享服务行为配置，涉数百服务节点、有集体不可用风险），但已用外网实测验证畸形请求返回 403、未触达应用层。措施已在测试库验证后部署生产并实测确认。

**若实拆会做的动作（拟动作清单）**

1. **落档确认**：回件真身已在 `7-外部文档/IT/`，满足 R6「只认文件不认转述」——无需再补落档动作（#91 期望产出「陈承处置结论落 `7-外部文档/IT/`」**已达成**）。
2. **队列 #91 行内回写**：把 (a)/(b) 两项处置结论逐条落字——(b) **可判已闭环**（三动作里"网络 ACL"完成、"核实写端点"完成且有明确结论、"关闭详细异常回显"以书面理由 + 外网 403 实测替代，属信中已明说可接受的"风险接受说明"形态）；(a) **不可判已处置**——我方请求是"轮换口令或禁用远程登录"，实际交付是"IP 白名单 + 堡垒机"，性质是**补偿性控制而非消除**，残余风险＝sa 口令仍有效且仍散布在多处 ERP 客开文件中。
3. **改派领取方**：本行现填「Paul」（机器人对 IT 无部门映射的落值），拆件时应改派 **Cowork 总线（治理/安全挂账）**，(a) 的后续动作若涉本地 `supplychain/.env` 清理则派 **CC**。
4. **CLAUDE.md 隐患段更新（有条件）**：根 CLAUDE.md「库存实时源落地」段的「🔴 两条独立安全隐患待 IT/安全收口」——(b) 可改「✅ 已由 IT 网络 ACL 收口（2026-07-30，含写端点；异常回显以 403 拦截替代修复，书面留痕）」；**(a) 不改「已处置」**，改为「⚠️ 补偿性控制（IP 白名单+堡垒机），口令未轮换，残余风险待 Paul 定是否接受」。
5. **本地残留清理（建议，非预案自动触发）**：#91 原预案写的是「**轮换回执到后**本地删 `supplychain/.env` 三行 DB 配置」——轮换未发生，触发条件未满足。但我方当前**零 DB 直连**（连接器全走 apiKey），删掉这三行对我方零影响、且能消除"我方本地仍留有一份仍然有效的生产库超管口令"这一独立敞口。**建议仍执行**，但因涉安全口径故不在 dry-run 期自行推进，列入需 Paul 项②。
6. **销行**：#160 状态改「✅ 已拆件（2026-07-30）」+ 摘要注记；#91 视 Paul 对 (a) 的裁决决定是「(b) 闭环 / (a) 转风险接受留痕行后销号」还是「(a) 继续跟催、#91 保持在办」。
7. **可能派生的新行（编号从高水位线 §一 #162 之后续排，dry-run 期不写）**：
   - 「(a) sa 补偿性控制 · 风险接受留痕行」（体裁同 #138：把决策连同已知残留风险一起留痕，供 IATF 追溯）——待 Paul 裁决后建；
   - 「本地 `supplychain/.env` 三行 DB 配置清理 + CLAUDE.md 隐患(a) 段改写」（CC，小件）——待同一裁决后建。

**判断依据**

- 预案出处＝队列 #91 行内「**待办**：等陈承回复 sa 处置/SOAP ACL 结论后落 `7-外部文档/IT/`」+「轮换回执到后本地删 `supplychain/.env` 三行 DB 配置 + CLAUDE.md 隐患(a)改'已处置'」。(b) 的三动作与信中"若 IT 评估认为内网环境下可接受，也请书面回一句，我方留档即可"完全对应 → 预案覆盖，可直接落字。
- (a) **预案未覆盖**：预案只写了"轮换回执到"这一条路径，未写"IT 拒绝轮换、改给补偿性控制"时如何判定。且这属安全**风险接受**性质——按 Dispatch 设计 §一自动池边界，「口径**新**裁决（预案未覆盖的）」永不进池 → 不猜、登需 Paul、跳过该项。

**需 Paul 项**：✅ 有（见文末①②）

---

## 二、#161 ｜ 陈承 → 两个接口行级状态字段已上线（🟢 解锁 #139①，但有一处端点错配须先核）

**队列行**：`| 161 | 企微反馈自动归档：2023458 发来文本反馈 | Paul | …5b3c9ba058493ec779d263789d713261.md | 待领 | | 2026-07-30 |`

**归档真身**：`7-外部文档/IT/IT-2023458-回复-2026-07-30-文本反馈-5b3c9ba058493ec779d263789d713261.md`（已读全文）

**内容一句话摘要**：这是对 `IT部-陈承-跟进-2026-07-28-两个接口缺行级状态字段请评估补齐.md`（队列 #144 记 07-28 14:21 UTC 已发）的回复——**结论：已完成，两接口均已上线行级状态字段**。根因说明：并非数据库缺行级信息，而是接口 SQL 只取了表头 `Status`、未取行表本身已存在的独立 `Status` 列。① `ForecastOrder/Query` 新增 **`LineStatus`**（取自 `SM_ForecastOrderLine.Status`，2=核准 / 3=关闭），并用真实单号 `FO2026070001` 验证第 60 行（`S02Y.0120`）、第 230 行（`S02Y.0166`）`LineStatus=3`、与 ERP 界面"关闭"一致，其余行为 2；② **`Purchase/Query`** 新增 `LineStatus`（取自 `PM_POLine.Status`，0=开立 / 1=审核中 / 2=已审核未交清 / 3=自然关闭已交清 / 4=短缺关闭手动 / 5=超额关闭），已抽样验证通过。

**若实拆会做的动作（拟动作清单）**

1. **销行**：#161 状态改「✅ 已拆件（2026-07-30）」+ 摘要注记；**改派领取方**由「Paul」改 **采购专线 / CC 采购落地**。
2. **回填 #139①**：#139 行内「🔴 ①（#19）真实探测证实现有接口无法实现，本项未完成」及其"非代码可修复范围"判定，**前提已被 IT 侧变更推翻**——需追注「2026-07-30 IT 已补 `ForecastOrder/Query.LineStatus`，本项由'无法实现'转为'可实现、待建'」。同时 SC8 侧 `sc8/sources.py`、`sc8/config.py`、`baoguan_service.py`、`run_baoguan_dashboard.py` 里 07-28 写下的"接口无行级状态字段"注释（`sources.py:42`、`config.py:90` 已定位）需随建造一并更正——**否则下一个读代码的人会按已过时的结论继续绕路**。
3. **新增 CC 待领行（预计 2 条，编号从 §一 #162 之后续排，dry-run 期不写）**
   - **【CC】SC8 保供看板 · FO 行级状态过滤接入**：`ForecastOrder/Query` 取 `LineStatus`，只保留 =2（核准）、剔除 =3（关闭）。**这是 #139① 的原目标，直接消解姚祖怡两次举证的"ERP 已关闭却仍在看板占位"，并预期一举清掉 07-27 现网快照里 44/116（38%）逾期噪声的绝大部分**。验收建议：以 `FO2026070001` 行60 `S02Y.0120` / 行230 `S02Y.0166` 作黄金判例（IT 已给出真值），全量回归零漂移 + 真部署 `.51:8091` 冒烟 + 重算后复核逾期行数变化。
   - **【CC】SC8 PO 在途判定去掉"回溯窗口"折衷**：#139④ 现用 `SC8_PO_TRANSIT_DAYS`（默认 365 天，按 PO 创建日过滤）是明确写了"非精确解"的折衷；有了 PO 行级 `LineStatus` 后，可改为按状态判定在途（2=已审核未交清视为在途；3/4/5 三种关闭一律排除），根治全库 1.2 万条"收货量不足订购量"里的呆滞记录误计。
   - **🔴 开工第一步必须先核实的端点错配（务必写进 opener，否则会白做一轮）**：我方 07-28 信里点名的是 **`ZpViewPurOrder/Query`**（SC8 实际调用处：`shared_tools/erp_connector/connector.py:408` `_zp_post("/api/ZpViewPurOrder/Query")`），而陈承回复里补字段的是 **`Purchase/Query`**（我方 `get_purchase_lines`，`connector.py:613`，目前只被 **FI2** 使用）。两者是不同端点 —— **不能假定 SC8 用的那个也有了 `LineStatus`**。第一步＝只读探测两端点真实响应体确认字段落点，据结果三选一：(i) 两端点都有 → 直接接；(ii) 仅 `Purchase/Query` 有 → 评估 SC8 PO 在途改走该端点（涉字段映射/分页/缓存差异，改动量非零）；(iii) 仅 `Purchase/Query` 有且不宜切换 → 回一句请 IT 在 `ZpViewPurOrder` 视图上一并补。**注**：FO 侧无此问题（我方与 IT 说的都是 `ForecastOrder/Query`，`sc8/loaders.py:119` 一致）。
4. **顺带回填（小件）**：`IT部-陈承-跟进-2026-07-28-两个接口缺行级状态字段请评估补齐.md` 的 frontmatter `status:` 仍为「待发」，而 README 第 62 行已记「✅ 已推送 2026-07-28 14:21 UTC」——R1 状态头与事实不一致，拆件时顺手回填（另 `IT部-陈承-跟进-2026-07-26-安全两项收口…md` 的 status 亦仍为「待发（待 Paul 审）」，同批修）。

**判断依据**

- 预案出处＝队列 #139①（"只取核准状态 FO"目标与验收口径已由姚祖怡 07-26 V6 + 07-28 回件**两次逐字书面明示**、行内已判定"C 类限制解除、可直接建造"）+ #144（要求对她如实交代"①暂未解决、待 IT 回复"）。本次 IT 回复即那个"待"的答复 → **属预案覆盖的既定路径**：口径无新增、目标无变化，只是技术阻塞解除，可直接派 CC 建造，**不需再走判例包**。
- PO 侧同理：#139④ 行内已写明"需 IT 补 PO 行级关闭状态字段"作为根治条件，现条件达成 → 预案覆盖。
- 端点错配属**技术核实**（非口径裁决），按模板库〇「边界个案」应由 CC 在建造首步只读探测解决，**不升 Paul**。

**需 Paul 项**：✅ 有 1 项（见文末③，与 #150 判例包冲突）—— 但**建造本身不必等该项**（两件互不阻塞）。

---

## 三、本轮跨行发现（拆件时须一并处理，否则会各自出错）

**#150 判例包的核心真实案例，正是 IT 确认已关闭的那一行。**

- #150（状态：**待领**，「SC8 批 2 引擎最后一项开放口径判例包待发」，`采购部-姚祖怡-跟进-2026-07-29-批2上月未齐套跨月占用判例批改.md/.docx`，尚未发出）问的唯一决策点是"上月未齐套项目的物料，下月如何继续计入"，**真实案例＝`S02Y.0120`**（07-10 出货、瓶颈物料 `R01D.0418` 欠 4,153，因出货日最早排最前，致 `S02Y.0210` 后续 8 个月可齐套数量全部归零）。
- 而 #161 里 IT 明确给出：`FO2026070001` 第 60 行 **`S02Y.0120` 的 `LineStatus=3`＝关闭**（与姚祖怡在 #139① 举证的"`S02Y.0120` 在 ERP 里是「关闭」却仍在看板占位"完全对上，两侧互证）。
- **后果**：一旦 FO 行级过滤上线，`S02Y.0120` 会被剔出看板，则该判例"上月未齐套项目继续占用下月物料"的**前提（这一行本该在看板上）不再成立**。若判例包按原样发出，等于请她对一个即将消失的案例做判定——既浪费她一次批改往返，也与她 07-28 的统一答复（"实时取到 ERP 预测订单数据就好，不需要做需求端任何自动变更"）撞在一处。
- **拟动作（不在 dry-run 期执行）**：暂缓 #150 发送 → 待 FO 行级过滤上线并重算后，用**过滤后仍然存在**的真实跨月占用案例重做 3 行判例（若过滤后该现象不再复现，则该开放口径点可能直接自然消解、连判例包都不必发）。**是否暂缓、以及是否换样本重做，超出预案 → 列需 Paul 项③。**
- 附注：#150 的 docx 真复选框已验证可解析，重做只需换案例数据、不必重建材料形态。

---

## 四、需 Paul 项汇总

| # | 事项 | 关联行 | 为何需 Paul（预案缺口） |
|---|------|--------|----------------------|
| ① | **sa 账号 (a) 项是否以"IP 白名单 + 堡垒机"结案**：IT 明确无法轮换（口令硬编码在多处 ERP 客开文件），交付的是补偿性控制而非消除，残余风险＝口令仍有效。二选一：**接受**（按 §四#33 范式登记风险接受留痕行，体裁同 #138）／**不接受**（继续跟催轮换，或要求 IT 给出硬编码清理计划与时间表） | #160、#91 | 安全**风险接受**属"口径新裁决"，Dispatch §一 自动池边界明列永不进池 |
| ② | **是否立即清理我方本地 `supplychain/.env` 三行 DB 配置**（含仍然有效的生产库 sa 明文口令）：我方零 DB 直连，删除零影响、可消除一处独立敞口；但 #91 预案把这一步**挂在"轮换回执到后"**，触发条件未满足 | #160、#91 | 预案触发条件未达；且属安全处置动作，dry-run 期不自行推进 |
| ③ | **#150 判例包是否暂缓/换样本**：其唯一真实案例 `S02Y.0120` 已被 IT 证实为 ERP 已关闭行，FO 行级过滤上线后该案例将从看板消失、判例前提失效 | #150、#161、#139① | 涉已定稿待发材料的撤回/改写与专员往返节奏，超出"照预案落档回灌"范围 |

> 三项均**不阻塞** #161 派生的两条 CC 建造行（FO 行级过滤 / PO 在途去折衷）—— 那两条口径完备、可即时开工。

---

## 五、巡逻自检

**本轮遇到预案覆盖不了的情形：有，3 处，均已如实列入"需 Paul 项"、未猜测、未推进。**

- **#160 (a) sa 账号**：预案只写了"轮换回执到 → 判已处置"这一条路径，**未预设"IT 拒绝轮换、以补偿性控制替代"时的判定规则**。这是本试点第一次遇到"回件是**部分/替代**满足请求"的形态（此前回件均为"已完成"或"提出新诉求"两类）。建议 live 版增一条判定规则：**回件所交付的措施与信中请求不一致时，一律不得自行判"已闭环"，落"部分满足＋残余风险"并升 Paul**。
- **#161 端点错配**：预案里没有"IT 回复所改的对象与我方所指对象不是同一个端点"这类核实要求。本次靠交叉比对代码调用处（`connector.py:408` vs `:613`）才发现。建议 live 版在"技术类回件"拆件动作里固化一步：**回件提到的接口/字段/单号，逐个与我方代码真实调用处对照一次，不一致即写进 opener 作为开工首步核实项**（本次若漏，CC 会按"字段已有"直接建造并在真实探测时才失败，白跑一轮）。
- **#150 交叉冲突**：预案是逐行拆件，**没有"本次回件是否推翻其他待领行前提"的横向检查步**。本次是因两行共用同一个真实料号 `S02Y.0120` 才被发现。建议 live 版增一步（成本很低）：**拆件时以回件中出现的真实标识（料号/单号/单据行号）在队列全文 grep 一次**，命中其他待领/待发行即评估前提是否失效——这与协议〇.1"认领先于动手"要求的触碰区关键词检索是同一手法，只是检索对象换成回件里的业务标识。
- **一处机制小瑕（非阻塞）**：两条行的「领取方」列机器人均落「Paul」而非「待分拣」（IT 不在 `department_mapping.yaml` 四部门口径内）。Dispatch 设计 §一 写的进池范围是"采购/财务/质量/**待分拣-IT**"——字面不含「Paul」值。本轮按实质（内容确为 IT 回件）纳入巡逻范围，未因字面不符跳过；建议 live 版把判定改为"按归档路径 `7-外部文档/<部门>/` 判域"，不依赖领取方列的落值。

---

*本报告为阶段一 dry-run 产物，仅供 Paul 抽查与业务总线据以拆件；本轮未对队列、任何 .md、企微、git 做任何写操作（本文件除外）。*
