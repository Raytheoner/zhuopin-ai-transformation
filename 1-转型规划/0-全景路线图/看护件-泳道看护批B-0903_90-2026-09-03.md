---
title: "看护件 · 泳道看护批 B-0903_90（第九批：FI2 五条同车 —— 能做的全做，做不了的产出判例材料）"
created: 2026-09-03
批次: B-0903_90
派出线: Cowork 业务总线 OP-0903-D
前批: B-0903_70（FI9 接 OEM 隔离层，已完成，commit d70ed28 在 master）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0903_90`

## 〇、本批口径

**立批依据** ＝ Shao Peishen 2026-09-03 §四 `#123` 裁决：**唐燕萍试用撞上的第二类错数定为插队优先，与 §一 `#390`／`#372` 同车派 CC 修**（`OP-0903-D`，批次 `B-0903_80`）。

🔴 **本批的核心设计：把「能做的」与「要她签认的」切开，两边都不空转。**

**取证结论（本批立批前逐条读过五行的期望产出）**：

| 行 | 现在能做完吗 | 卡在哪 |
|:--:|---|---|
| `#418` | ✅ **能整条做完** | 无签认依赖 —— 是明确 bug（发票落在跨期文件里没被取到） |
| `#390` | ⚠️ 实现能做，**口径 ⒜⒝⒞ 要问她** | 期望产出 ① 明写「口径部分问唐燕萍」 |
| `#424` | ⚠️ **只读量化能做**，修法要她签认 | 期望产出 ② 明写「判据/口径类，**永不默认生效**」 |
| `#423` | ⚠️ 只读复现能做，口径要她签认 | 期望产出 ① 明写「走判例批改法请唐燕萍签认」 |
| `#372` | ❌ **本批不做** | 要 §四 `#92` 拍板 ＋ design 审，且涉新增对外推送面 |

🔴 **唐燕萍闸 2026-09-03 实测 🔒 锁**（`财务部#16` 在途）⇒ **本批一律不起草、不发送任何跟进信**。
🔑 **但「不能问她」不等于「什么都做不了」** —— `#424` ① 与 `#423` ① 都是**只读量化**，它们的产出**正好就是她闸开时那封信的判例材料**。⇒ 本批把这些材料备齐，闸一开即可起草，**不必再等一轮取证**。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界

1. 🔴 **三闸全锁口径照旧**：唐燕萍 🔒／姚祖怡 🔒／陈忱 ✅ 开 —— **本批不起草、不发送任何跟进信**（`#418` ④「结论回她一句」明写「另走跟进信流程、不在本行范围」）。
2. 🔴 **判据／口径类永不默认生效** —— `#390` ⒜⒝⒞、`#423` 口径、`#424` 修法**一律不得自行选定**；只做取证与非口径实现，选项与数据备齐后**停下登记**。
3. 🔴 **⏭️ `deploy_51`：本批不部署**。⚠️ §四 `#478`（他在环可续做部署）**已立但正本未改** ⇒ **过渡期照旧转出**；命中即 `transfer-out` 记录去向、**泳道不停、继续做其余**。
4. 🔴 **`#372` 不做**（要 `#92` 拍板 ＋ design 审）。
5. 🔴 **不预估、不外推** —— `#424` ④ 明写「不得预估」；`#390` ③ 要求真实全量跑通一次。
6. 🔴 **真实库只读**；不碰真实银行数据；不改规范文档。

---

## 二、波次与触碰区矩阵

🔴 **本批只有一条泳道 `fi2-batch`，四段严格串行**（`A1→A2→A3→A4`）。

| 波 | 泳道 | 段 | 承接 | 触碰区 |
|:--:|---|:--:|---|---|
| 1 | `fi2-batch` | A1 | `#418` 发票缺失假报错 | `fi2/feed_source.py` |
| 1 | `fi2-batch` | A2 | `#390` 无 PO 关联行打挂整批 | `fi2/feed_source.py`（校验层） |
| 1 | `fi2-batch` | A3 | `#424` 只读量化 | `fi2/tax_export_ingest.py`（只读） |
| 1 | `fi2-batch` | A4 | `#423` 只读复现 | `fi2/webapp.py`（只读） |

🔴 **为什么必须同一泳道**：五条队列行**自己就写着**「触碰区重叠，须同车或严格串行」（`#418`／`#423`／`#424` 行内均有此句）。分成多泳道并行必然互相踩。

---

## 三、泳道 opener

### A1 · 【CC】FI2 五条同车 —— 队列 #418／#390／#424／#423

粘贴端：CC ｜ 泳道：fi2-batch

**开场词（复制即用，▶ 粘贴端：CC 新会话）**：

```
[OP-0903-F1]【CC】FI2五条同车
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903f1-fi2-batch`）｜ worktree：☑（fi2-batch，新 worktree，收工自删；🔴 合入 master 那一步回主工作区做）｜ 工作区：无（场景包内建造 ＋ 只读全量取证，🔴 不部署、不触碰 `.51`；真实库只读）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_90
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903F1-FI2五条同车。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0903_90-2026-09-03.md 的 §〇（能做/不能做的切分）＋ §一（硬边界六条）＋ §三 A1（本节「做什么／不做什么」）＋ CLAUDE.md 恢复上下文，按 §三 A1 执行。本件为 A 类（范围已由 Shao Peishen 裁定、判据已写死），无需再问澄清，直接开工。
```

### A1 · 开工前置

**心跳**：`reports/lane-heartbeat/B-0903_90-fi2-batch.md`（🔴 **写在主仓、不写在 worktree 内** —— worktree 内的 `reports/` 被 gitignore 且随 worktree 消失，2026-09-03 已两次实撞），开工 1 分钟内写首行，每里程碑追加，超 10 分钟补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
**分档**：判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_90 --wave 1 --lane fi2-batch --action-key <键> --waiting-for "<...>" --no-notify` ＋ 以 `OPENER_PARTIAL` 收尾；⏭️ 走 `transfer-out`（**泳道不停，继续做其余**）。
🔴 **`merge_to_master` 属 🟡，本批已获 Shao Peishen 2026-09-03 放行**（他在场、本件由他亲手起）⇒ 合入本包改动时不 pause；**范围超出下述四段即照停**。
**另读**：队列 §一 `#418`／`#390`／`#424`／`#423` 四行全文（**四行各自写着触碰区重叠须同车**）／`4-数字员工/财务部/FI2-三单匹配自动对账/CLAUDE.md`。

### A1 段 · `#418` 发票缺失假报错（🔴 本批唯一能整条做完的，先做）

唐燕萍随机抽 10 张 AP 单、**4 张报「发票缺失」而发票实际存在**，她自带四组对照取证。

1. 用她给的 **4 组对照复现**（`AP-2026080041`／发票 `26322000006465433531`／《全量发票查询导出结果（20260801-20260817）》等，全文见 `#418` 行）。
2. 🔴 **先查发票摄取的文件覆盖范围是否只取同期导出文件**（`#418` 行内已指方向）。
3. 修复 ＋ 单测：**构造「发票落在跨期文件里」的输入**。
4. 全量重算后给出「无发票支撑」项数的**修正前后对比**（实测数，不预估）。
5. 🔴 **不回信** —— ④「结论回她一句」明写另走跟进信流程、不在本行范围；且她闸 🔒 锁。

### A2 段 · `#390` 一行无 PO 关联打挂整批

实撞样本 ＝ `AP-2026010125`（全单 1 行、`SrcPONo`／`SrcPOLineNo` 双空）⇒ `_APLineRow` 两个必填校验同时失败 ⇒ **整个 640 单批量加载当场中止、零产出**。

1. 🔴 **⒜⒝⒞ 口径不得自选** —— 行内明写「口径部分问唐燕萍」，她闸 🔒 锁 ⇒ **把三个选项与各自影响面（各会放过/拦下多少行，用真实数据量出来）整理成判例材料，停下登记**。
2. **实现「不因单行失败而打挂整批」这一层**（这一层不含口径判断：无论最终选哪个口径，"一行坏不该让 640 单全灭"都成立）＋ 单测（构造含空 `SrcPONo` 行的批量输入）。
3. 用 `ap_supplier_codes` 在**真实数据上跑通一次全量** —— 🔴 **批量模式至今零次全量真实验证，本行销号须含这一次**。

### A3 段 · `#424` 只读量化（不实现、不选修法）

`resolve_item_code` 的逐行匹配闸把 **421 张有 AP 单可挂的发票整行丢掉**，其中 221 张 AP 单今天在 `invoice.csv` 里一行都没有。

1. 🔴 **只做期望产出 ①**：在全量数据上量各候选修法的**命中率与歧义率**（只读，复用影子重摄取的跑法）。
2. 把两个真实案例逐字整理成夹具素材（**310ML 单位换算**、**跨料号合计开票**）。
3. 🔴 **不实现、不选 ⒜⒝⒞** —— 判据/口径类，永不默认生效，须她签认。产出即判例材料。

### A4 段 · `#423` 只读复现（不实现、不选口径）

`webapp._report_page` 749-750 行把「孤立发票」计进 `n_block` 与 `total_rows` ⇒ 按单号筛选时数值差三个数量级。

1. **只读复现一次**：构造「筛 1 张单 ＋ 全公司发票池」的输入，量出 KPI 爆表的实际倍数。
2. 整理 ⒜⒝⒞ 三个口径各自的显示效果对照（判例材料）。
3. 🔴 **不实现、不选口径**。

### A1 · 收工

1. `openspec validate --all --strict` 须绿；FI2 单测全绿；底座与其余财务场景零回归。
2. **合入 master**：`git fsck --connectivity-only` → `git pull` → rebase → `merge --ff-only` → push。🔴 合前看 `merge-base --is-ancestor`。
3. **§二 批次登记**（🔴 **文件清单每个路径用反引号包住** —— 守卫⑹ 走 ``re.findall(r"`([^`]+)`")``，裸路径提取为空即判孤儿文件拒绝 release）。
4. **§一 `#418`／`#390`／`#424`／`#423` 四行逐行回写**；🔴 **`#424`／`#423` 不得销号**（只做了只读段）；`#390` 是否销号取决于口径是否已定 ⇒ 未定即不销。
5. 🔴 **把 A2/A3/A4 产出的判例材料汇总成一节写进心跳与队列行** —— 标题写「**待唐燕萍闸开后并入下一封财务信**」，使闸一开即可起草、不必重跑取证。

### A1 · 不做什么

- 🔴 **不选任何口径**（`#390` ⒜⒝⒞／`#423` 口径／`#424` 修法）—— 判据类，永不默认生效
- 🔴 **不做 `#372`**（要 `#92` 拍板 ＋ design 审）
- 🔴 **不起草、不发送任何跟进信**（唐燕萍闸 🔒）
- 🔴 **不部署、不触碰 `.51`**（命中即 `transfer-out`，泳道不停）
- 🔴 **不预估、不外推** —— `#424` ④ 明写「不得预估」
- 🔴 真实库只读；不改规范文档；不填任何未签认判据

---

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0903_90` 现取。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#372` design 审 ＋ 回唐燕萍一句 | 待 §四 `#92` 拍板；涉新增对外推送面 |
| `#390`／`#423`／`#424` 的口径签认 | **判例材料本批备齐**，待唐燕萍闸开（`财务部#16` 回件）后并入下一封财务信 |
| `.51` 部署（修完让她看到修正值） | ⏭️ 转出；§四 `#478` 正本改成后可在环续做 |
| `#403` 消化 IT部#10 回件 | 触碰区与本批零重叠，**可另起一条并行泳道**，但剩余范围未核，本批不含 |
| `#466` OEMRouter audit fail-closed | 🔴 **本轮不跑** —— 触碰 `OEM数据隔离规范.md` §5 映射表，与 FI9 三处待批回写、`oem-chroma` 线三方重叠 |
