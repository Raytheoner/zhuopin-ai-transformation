---
title: "波 1bis 建造看护件 · Q2 判定引擎 ＋ FI3 门户页"
created: 2026-09-17
status: 🟢 无头批待起（`工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -Detach`，他 2026-09-17 答 `a` 授权）
产出: Cowork 业务总线 OP-0911-H
批次: B-0917_波1bis
源: Shao Peishen 2026-09-17 问「质量和财务本周都有新回灌，是否有新功能构建已排期？是否有新模块可以发布使用？新场景发布能提高团队的工作积极性」→ 答 `a`（两条都起）
用途: 两条互不触碰的泳道并行；§三 即 `-Plan` 解析格式，默认后台无头起、不给人粘。
---

# 波 1bis 建造看护件（2026-09-17）

## 〇 · 本批要解决的一句话

**本周两个部门都回了件，但成果都还没变成专员能看见的东西。**

现取 `.51`（本机在 LAN）：`8090` 统一门户网关｜`8091` SC8｜`8092` AI 运营指挥中心｜`8093` QD-B｜`8094` FI2｜`8096` SC2，**六个全 200**。本周**零新增可发布模块**。

- **质量域**：陈忱四轮回件攒出的 51 条规则／6 条红线／十一条签认判例／10 份验收样本（7 反例＋3 合格）／7 条真实退回理由，**现取全仓零个 `.py` 引用**。Q2 排 2026-10、**零工程实体零 openspec 包** ⇒ 不是被卡，是没排上。
- **财务域**：FI2 早已上线 `8094`；FI3 今日到档 1，但档 1 不是发布；其 §3 收口里两条都要唐燕萍，而串行闸 🔒 锁。

⇒ **A1 建 Q2 引擎（兑现质量域回灌）｜A2 出 FI3 门户页（财务域最短可见路径，不依赖唐燕萍）。**

---

## 一 · 泳道编排

两条**互不触碰、各占一条泳道、全程并行**（`q2` ／ `fi3-portal`），无串行点。并行上限 4，本批只用 2。

---

## 二 · Q2 为什么不跑 grill（判据写在这里，泳道不必重判）

需求树已成文**且已签认**：

| 项 | 状态 | 凭据 |
|---|---|---|
| 档位口径 | ✅ 两档，以 V3.2 为准 | 陈忱 2026-09-01 逐字 `☑ (b) 改为两档。以V3.2为准。` |
| 五条语义验收口径 V1–V5 | ✅ 全部认可 | 2026-09-02 `质量部#12` 回件 |
| 判例 1–11 | ✅ 全闭环 | 判例签认台账 §1 |
| 10 份验收样本 | ✅ 已交（7 反例／3 合格） | `7-外部文档/质量部/8D验收集-2026-09-10/` |
| 7 条真实退回理由 | ✅ 已落档（脱敏） | `Q2-8D验收集-标签与退回理由-2026-09-17.md` |
| **开放点** | ⏳ **只剩 1 项**：「措施可验证性的判定标准」——什么算「包含可量化的验证标准」，**未单独发问** | 台账 §3 |

⇒ 那 1 项按 FI3 同法落 `PENDING` 读即抛，**不代填**。

🔴 **四条建造纪律**（队列 `#612` 逐条写明，此处只列标题）：① 档位不得自定，只实现 V3.2，台账是签认追溯不是档位来源；② 按 `judge_method` 分流，语义类挂 `PENDING`，判例 11 明定红线②③ 本批不验收；③ 判例 6 安全相关件一律转人工，判例 9 结构性退回先于红线语义判定，顺序不得颠倒；④ **真实 8D 原文一律不入 git**。

⚠️ **一处不得当成已解决**：QD-A 前置抽取层**总体命中率实测 37.9%、低于 60% MVP 门槛**，其 CLAUDE.md 明写「不得称已达门槛」⇒ 接它时须 fail-loud 标注置信度。

---

## 三 · 泳道 opener（`工具-opener批处理执行v2.ps1 -Plan` 解析段，人不碰）

🔴 **本节必须排在 `## 三bis` 之前**——`工具-opener块lint.py` 形态⑥ 的判据是「块出现在 `## 三bis` 标题之前」才算子任务泳道块、才豁免 `set_session_title`（2026-09-17 波 1 首版实撞，五块全误报 F1）。

🔴 **`### A<N>` 标题必须带 `§一 #<行号>`**——`工具-opener派出前校验.ps1` 靠它现查队列态，不带即 `no-row-ref` 告警、这道闸对本批失效。

### A1 · §一 #612 Q2 8D 判定引擎从零到档 1

粘贴端：CC ｜ 泳道：q2 ｜ 队列行：§一 #612 ｜ OP：OP-0917-Q

```
[OP-0917-Q]【CC】Q2判定引擎
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0917q-q2-verdict`） ｜ worktree：☑（op0917q-q2-verdict，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0911-H ｜ 模型：opus
读 ① `队列 §一 #612` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917q-q2-verdict --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917q-q2-verdict --done --batch B-0917_波1bis --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · §一 #613 FI3 门户页 5.1

粘贴端：CC ｜ 泳道：fi3-portal ｜ 队列行：§一 #613 ｜ OP：OP-0917-R

```
[OP-0917-R]【CC】FI3门户页
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0917r-fi3-portal`） ｜ worktree：☑（op0917r-fi3-portal，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0911-H ｜ 模型：sonnet
读 ① `队列 §一 #613` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917r-fi3-portal --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0917r-fi3-portal --done --batch B-0917_波1bis --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

---

## 三bis · 看护者开场词（仅在退回「给他一块粘」时使用；默认走 §三 无头）

```
[OP-0917-S]【CC】看护波1bis
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建；各子泳道声明 ☑ 后由 v2.ps1 脚本在起 claude 前建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0911-H ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0917S-看护波1bis。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】波1bis-Q2判定引擎与FI3门户页-2026-09-17.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

---

## 四 · 全程红线

1. 🔴 不碰 `.51`、不部署、不做 FI3 tasks 5.2（⏭️ 档，须走 `zhuopin-lan-closeout` 串行纪律）。
2. 🔴 不代填判据默认值——缺签认一律 `PENDING` 读即抛；`criteria_signoff` 底座的存在是为了让缺签认**显式失败**，不是为了填占位值。
3. 🔴 不代指派持有人／backup；不代发信、不代联络陈忱／唐燕萍／IT。
4. 🔴 真实 8D 原文与真实付款申请一律不入 git。
5. 🔴 不 ff master（🟡 档），只 push 本泳道分支；不手工 commit，走 §二 批次 ＋ `ZhuopinCommitSweep`。
6. 🔴 FI3 门户页须在页面上**显式标注数据为 mock**——2026-07 `:8092` 静态原型用虚构占位数据误导过专员，同一个坑不踩第二次。

## 五 · 已知会踩、但不是本批的错

**收工核验第三道闸会把「按协议〇 回写主仓队列行」判成 `main-leak` ⇒ 误判 FAIL。** 白名单只有 `reports/` 与 `合入登记/pending-ff.jsonl`，而协议〇 要求泳道回写主仓队列。已立行 §一 `#611`（销号判据＝用波 1 的三份 `*-main-leak.patch` 回放、必须全部不再判 FAIL）。

⚠️ **本批若再报 `FAIL(main-leak)` 且泄漏文件只有队列文件，按误判处理**——读泳道日志尾部的 `OPENER_DONE`／`OPENER_PARTIAL` 为准，不读 summary 的 Status 列。
