---
status: 在办
---

# 拆件巡逻报告 · 2026-09-16（第一班 · 事件驱动）

**〇ter 探测**：`[SIGNAL]`，待处理 3 条——`IT部#13`（09-14）、`质量部#14`（09-14）、`财务部#18`（09-16），`CHECKPOINT=2026-09-16T06:52:28Z`。信号在本报告登记 §二 之后才消费。⚠️ 09-14 两条为何拖到今天：两次无头起活均只留一行「You've hit your monthly spend limit… resets Sep 16, 8am」即退出（`patrol-headless-logs/20260914T*.log`），信号 fail-safe 未丢、但夭折无告警（追记 §一 `#416` ⑸）。

---

## 拆件 1 · `IT部#13` 回件（陈承 · 男，09-14 落档）

- **行号**：§一 `#577`，已销号。
- **内容摘要**：两件全答——三行已写 `C:\portal-gateway\.env` 并重启 8090；招聘 agent `.env` 那套凭据即门户应用的。
- **已执行动作**：实测 `curl -k …/portal/_sso/oauth/start` 由 404 变 302 → 企微 authorize（agentid=1000047）；`#240` 改判 open（原状态列全文原样保留于后）；README 转 📥。
- **判断依据**：探针 302 即 S1 通；**302 里 `redirect_uri` 为 http 且无 `:8443`**＝`#240` 预判的 `ProxyFix` 卡点已现形，🔴 建造不进池，归 `#162`／§四 `#197`。
- **需 SP 项**：无。**台账回写**：`[NONE]`，本信非台账投影，零回写。

## 拆件 2 · `质量部#14` 回件（陈忱 · 女，09-14 落档，§一 `#578`＋`#579` 合并读）

- **行号**：§一 `#578`（导语）／`#579`（docx），同时销号。
- **内容摘要**：她亲答 J1–J9（未经朱映桦初标）：J1 ✏️、J2–J7 ✅、J8 (甲)、J9 材料到齐。
- **已执行动作**：python-docx 按 `w:body` 原序全读（2 张表＋全部段落，勾选看 ☑/☐ 实字符）；台账 `Q2-Q14-01`～`09` 全部转 `已签认`（fact-date 09-14，evidence＝docx）；新立 §一 `#592`（质量专线消化）。
- **判断依据**：J1 只改「处置」不改「判据」——认红线⑥字面执行、样本 A 判 D 级，不移出合格组改标「边界样本」，属专员对验收集的处置权，非新口径；J7 的 PPT 模板 D2 勾选行是实施方式、原则「编制人填、AI 不猜」未变；样本 3「制造 ※」未解释 ⇒ 随 `质量部#15` 带问、不单起信。
- **需 SP 项**：无。**台账回写：已签认 9 点／仍在途 0 点。**

## 拆件 3 · `财务部#18` 回件（唐燕萍 · 女，09-16 落档，§一 `#588`＋`#589` 合并读）

- **行号**：§一 `#588`／`#589` 销号；`#590`／`#591` 为同一暂存记录被 flush 两次的重复行，标 ✅ 重复并入。
- **内容摘要**：J1 ❌＋✏️「清单应交**计划**逐项确认」；J2–J10 全 ✅；J11 财务侧＝财务总监；J12 三项均答；另在导语里问「FI2 还有没有需要财务确认的地方」。
- **已执行动作**：台账 8 点转 `已签认`（fact-date 09-16），`FI10-TERMINATED_PROJECT_ALERT_CRITERIA` 信中未设 J、**未答、保持 待问**；回灌 `#474`（FI10 口径事实）、`#557`（前置②满足＋J1 改判＋J12⑴ 拟并入采购部#24）；新立 §一 `#593`（`财务部#19`：答 FI2 之问，携 §四 `#196` 判例 #7/#8 互斥＋`#224` R7 再签认＋J6 子问）；登 §四 `#205`／`#206`。
- **判断依据**：❌ 列 ☒ 且 ✏️ 有字 ⇒ J1 是改判不是同意（硬约束②）；「谁确认」改为计划涉及她无权代接的部门 ⇒ 不自行落字，登 §四；J6「销售费用率是否含出口费用」她未单独作答 ⇒ 不算已答；FI2 之问在表格之外（硬约束①）且实答＝有（`#196`／`#224`）。
- **需 SP 项**：§四 `#205`（J1 确认权归计划？默认 (a)）、§四 `#206`（L2 供应链侧岗位，**无默认**）。**台账回写：已签认 8 点／仍待问 1 点。**

---

## 待发信盘点（#124）

README 56 行逐行按状态格前缀计数：**⏳ 待你审 ×0 ／ 🆕 待发 ×0 ／ ⏸ 暂缓 ×0**。无积压、无「队列称暂缓／README 仍 🆕 待发」交叉红标。三封 📨 第九态本班全部转 📥。

## 起草缺口检测（#245）

`draft_gap_check.py --window-days 14` stdout 原样：`待发信盘点（扩展版，近 14 天已部署场景 vs 已起草跟进信）：无缺口`

---

## 巡逻自检

- **〇ter**：`[SIGNAL]` ×3；消费命令在 §二 登记后执行，回显附于 §二 批次行。
- **〇bis**：本班有本机 PowerShell（`Get-Date` 取 `2026-09-16 15:05:12 +08:00`）与出网 curl，前提不成立 ⇒ 一.4／三.8／四.5／四.6 照做，同 09-10 班留痕。
- **三.1 锁**：`acquire --who Cowork-拆件巡逻 --reserve-multi 一:2 四:2 --domain 业` 一次成功，退出码 0；全程 `edit-row`／`append-row` 行级写入，未整文件覆盖。
- **本轮实拿编号**：§一 `#592`／`#593`，§四 `#205`／`#206`，全部用满。
- **并入审核**：`#592` 扫 `Q2`／`8D`／`质量部#14`——`#408`／`#431` 各承接前两封、`#555` 已 done ⇒ 新立；`#593` 扫 `FI2`／`唐燕萍`——`#224`/`#251`/`#418`/`#494` 为单项在办、§四 `#196` 为待她答 ⇒ 新立并收拢为一封。业务行不受机制 WIP 上限约束；本班未新立机制行（两枚机制证据追记进 `#416` ⑷⑸）。
- **三.6 比对**：写入期间未出现他人新增行、未撞号；`#588`–`#591` 由 bot flush 在本班开工后 5 分钟内成行，属预案外情形（下一条）。
- **预案覆盖不了的情形**：⑴ `财务部#18` 两条收件行在本班开工时**尚未成行**（06:52 遇锁忙推迟进暂存），本班跑 `flush_pending_lock_appends.py` 补录，但 18 s 内另一次 flush 已先补一遍 ⇒ 成 4 行、2 行重复（审计 `queue_append_pending_flushed` ×4）；已标重复并入，成因＝flush 循环结束才整体重写暂存文件、无幂等键，追记 `#416` ⑷，不修。⑵ 台账 `FI10-*`／`Q2-Q14-*` 在 `待问` 而非章程假定的 `在途`（§四 `#203`：09-13 未提交改动被误删），本班直接 `待问→已签认`（工具放行），并在 `#203` 追记「重建 `在途` 行不得直接 append 到文件末尾」（当前态＝最后一行）。
- **刻意未做**（一行）：不修 redirect_uri／ProxyFix（建造）；不修 flush 幂等（建造）；不起草任何信（红线）；不改 `#474` 状态（§4/§5 解除由建造线按 tasks 2.3 自判）；不替唐燕萍答 J6、不替陈忱解释 ※。
- **四.5／四.6**：sweep 与决策提醒在 §二 登记后触发，回显附批次行。
