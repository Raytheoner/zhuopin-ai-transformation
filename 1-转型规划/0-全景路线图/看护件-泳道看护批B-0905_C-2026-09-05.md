---
title: "看护件 · 泳道看护批 B-0905_C（第十二批：B-0905_B 收尾补跑——3 条未真正起跑的泳道重发＋1 条待合并）"
created: 2026-09-05
批次: B-0905_C
派出线: Cowork 业务总线 OP-0905-K
前批: B-0905_B（7 条泳道，2026-09-05；核实结果：`rebase3`／`459-382b-impl`／`456-lint-impl` 已合入 master，`448-design` 已获批准但未合，`446-formread`／`341-design` 未见任何分支或产出、`455-design` 建了 worktree 但零提交——本批只补这 4 件，不重做已完成的）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0905_C`

## 〇、本批口径

**立批依据**：Shao Peishen 2026-09-05 问「泳道跑完，可否开新批，能开尽开」——核实（git log／branch／队列行三方交叉）后，B-0905_B 七条泳道**不是全部真正跑完**：

| 泳道 | 真实状态（证据） |
|---|---|
| `rebase3` | ✅ 已完成——`editable-import-guard-0902`（`d69aab9`）、`followup-meta-0902`（`0a70734`/`99647be`）均已合入 master |
| `459-382b-impl` | ✅ 已完成——合入 master `2635cd5`（413 passed） |
| `456-lint-impl` | ✅ 已完成——合入 master `5910f81` |
| `448-design` | 🟡 design 已获 Shao Peishen 现场批准（`lane-watch-state.json` 实测），但分支 `worktree-agent-aba0d5214aae82b49`（含 `f2487cd`/`b725ea1`）**未合入 master**；本线尝试合并两次均撞上仓库 git 锁竞争（七泳道并行占用），未成功 |
| `446-formread` | ❌ 未见任何分支、任何队列行回写——判定未真正起跑 |
| `455-design` | ❌ 分支 `claude/op0905g-editrow-guard-455` 存在，但 HEAD 与 master 起点相同（零提交）——判定起了 worktree 就没了下文 |
| `341-design` | ❌ 未见任何分支、任何队列行回写——判定未真正起跑 |

🔴 **本批只补这 4 件**（448 合并 ＋ 446/455/341 三条重发），**不重做已确认完成的三条**——重发用全新 OP 编号与分支名，不复用旧分支（`455` 那条旧分支 `op0905g-editrow-guard-455` 零提交，留作证据不清）。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界

1. 🔴 **`448-merge` 只做 rebase＋只读验证，真正 ff 进 master 前必须停下**（`merge_to_master`，🟡）——design 已批准不等于可以自动合，仍照 D1 走一次确认。
2. 🔴 `446-formread-retry`／`455-design-retry`／`341-design-retry` 范围与止步点与 B-0905_B 原文完全一致（内容未改，只换编号/分支名）——不重新讨论范围。
3. 🔴 若本批任一泳道再次出现「有 worktree 无提交」「有分支无回写」的静默无进展情形，**看护者须在收工汇总里明确点名**，不得笼统写「已处理」。
4. 🔴 真实库只读起步；不部署 `.51`；不发任何对外消息；不碰 oem-chroma 族与 `OEM数据隔离规范.md`。
5. 🔴 不改队列文件里已有的历史回写段——只追加新结论。

---

## 二、波次与触碰区矩阵

四条泳道触碰区互不重叠，**全部可并行起**，无顺序依赖：

| 泳道 | 段 | 承接 | 触碰区 |
|---|:--:|---|---|
| `448-merge` | A1 | 合并已批准的 `#448` design 变更包（隔离 worktree rebase＋只读验证，停在 merge_to_master） | `openspec/changes/queue-row-blocker-field/`（既有分支内容，不新起草） |
| `446-formread-retry` | A2 | 同 B-0905_B A2 原文（回件形态识别，建到底） | `5-平台底座/wecom-aibot-service/aibot_service/`（新模块） |
| `455-design-retry` | A3 | 同 B-0905_B A4 原文（写侧守卫加固，起草到 design 审为止） | `openspec/changes/editlock-write-guard-hardening/`（新） |
| `341-design-retry` | A4 | 同 B-0905_B A5 原文（域路由，起草到 design 审为止） | `openspec/changes/queue-domain-routing/`（新） |

---

## 三、泳道 opener

### A1 · 448-merge——合并已批准的 #448 design 变更包

粘贴端：CC ｜ 泳道：448-merge

```
[OP-0905-L]【CC】合448设计包
【设置】执行环境：CC ｜ 分支：master（隔离 worktree 内检出既有分支 `worktree-agent-aba0d5214aae82b49`，不新开分支）｜ worktree：☑（`merge-448-retry`，新 worktree，收工自删）｜ 工作区：无（纯库内只读复核＋合并，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_C
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0905_C-2026-09-05.md 的 §〇（本批口径表）＋ CLAUDE.md 恢复上下文。本件为 A 类（design 已批准，范围已写死），无需再问澄清，直接开工。

背景（已核实，直接信任）：`#448`（队列行阻塞源 `[B:]` 字段设计）已由分支 `worktree-agent-aba0d5214aae82b49` 产出 `openspec/changes/queue-row-blocker-field/`（`proposal.md`／`design.md`／`tasks.md`／`spec.md`，共 406 行，纯新增），Shao Peishen 已于 2026-09-05 现场批准（`lane-watch-state.json` 448-design 记录：`(a) 批准，按推荐方案定稿`）。本线两次尝试合并均因仓库 git 锁竞争失败，worktree 残留但未损坏 master。

做什么：
1. `git worktree add .claude/worktrees/merge-448-retry worktree-agent-aba0d5214aae82b49`（若该分支已被其他 worktree 占用检出，改用 `git worktree add -b temp-merge-448-v2 .claude/worktrees/merge-448-retry b725ea1`，基于同一提交内容起新分支名，不影响原分支）。
2. `git rebase origin/master`（预期零冲突，纯新增 4 个文件）。
3. `openspec change validate queue-row-blocker-field --strict`（若命令不存在，改读 `tasks.md`／`spec.md` 确认结构完整、无占位符残留）。
4. 只读 `git merge-tree $(git merge-base master <rebase后分支>) master <rebase后分支>` 确认无冲突标记。
5. 收尾：`pause --action-key merge_to_master --waiting-for "#448 design 变更包（已获你批准）rebase 后零冲突，是否 ff 进 master"`。

不做什么：
- 🔴 不修改 `openspec/changes/queue-row-blocker-field/` 任何文件内容——只合并已批准的既有内容。
- 🔴 不擅自 ff——只到只读验证为止。
- 🔴 不删除原分支 `worktree-agent-aba0d5214aae82b49` 或其 worktree。
```

### A2 · 446-formread-retry——回件形态识别自动化（建造到底，与 B-0905_B A2 同范围）

粘贴端：CC ｜ 泳道：446-formread-retry

```
[OP-0905-M]【CC】回件形态识别重发
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905m-formread-446`）｜ worktree：☑（`formread-446-retry`，新 worktree，收工自删）｜ 工作区：无（纯库内建造＋离线回归，不部署、不触碰 `.51`、不发任何消息）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_C
读 ①〈队列 §一 `#446`〉（`工具-队列查询.py --row 446 --field all`）→ ② `0-学习与工具/md转Word工具/md2word.py` 的 `read_checkboxes()`（约 L85）→ ③ CLAUDE.md 恢复上下文，按 `#446` 行内「做什么（三步，止于此）」执行。本件为 A 类（openspec 判据已过、无需 design 审，行内已写死范围），无需再问澄清，直接开工。⚠️ 本泳道是 B-0905_B `446-formread` 的重发——上一批同名泳道未见任何分支或产出，视为未真正起跑，本次请务必在收尾前确认已 `git push` 分支、已回写队列行，不要只在本地 worktree 完成就算数。

做什么：
1. 形态识别模块：`zipfile` 直读 `.docx` 部件清单，数 `w14:checkbox` 勾了几个哪几个／`w:ins`·`w:del`／`word/comments*`／`w:highlight`，落在 `5-平台底座/wecom-aibot-service/aibot_service/` 下新模块（命名自定，遵项目既有风格）。
2. 按形态提原文片段：逐格 ☒☐ ／高亮段全文／批注内容，结构化输出。
3. 离线回归：对 8 封已归档回件（`财务部#14`/`#15`、`IT部#9`/`#10`、`质量部#9`/`#10`、`采购部#18`/`#19`/`#20`，见 `7-外部文档/<部门>/`）跑一遍，逐封列「机器读出的形态 vs 人当时最终结论」，对不上即视为 bug 修到对上为止。
4. 产出一份「巡逻章程 §二 改动建议」文档（不改章程本体——章程改动交总线做），说明这一步该接在巡逻流程的哪个位置。
5. 补单测覆盖新模块。
6. 收尾：`git push` 分支 ＋ 回写队列 `#446` 行（登记 §二 待 commit 批次）＋ 心跳文件写 DONE。

不做什么：
- 🔴 不修改巡逻章程 `C:\Users\Paul Shao\Claude\Scheduled\huijian-chaijian-patrol\SKILL.md` 本体——只出建议文档。
- 🔴 不做语义判断与队列回灌自动化——形态识别到「结构化产出」为止，人工判断仍留人。
- 🔴 不碰 `queue_appender.py` 的触发逻辑（那是 `#382`⑴，已完成，不在本泳道范围）。
```

### A3 · 455-design-retry——`edit-row`/`append-row` 写侧三缺陷合并处置（起草到 design 审为止，与 B-0905_B A4 同范围）

粘贴端：CC ｜ 泳道：455-design-retry

```
[OP-0905-N]【CC】写侧守卫加固设计重发
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905n-editrow-guard-455`）｜ worktree：☑（`editrow-guard-455-retry`，新 worktree，收工自删）｜ 工作区：无（纯 openspec 起草，不动产线代码）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_C
读 ①〈队列 §一 `#455`〉（`--row 455 --field all`）→ ②实证行 `#324`（未闭合⑵：竖线守卫非反引号感知致状态字段锁死）与 `#454`（反引号奇偶未校验致行塌列）→ ③ CLAUDE.md 恢复上下文。本件为 B 类——三处缺陷的具体校验实现细节需要你判断，止步于起草不代拍板。⚠️ 本泳道是 B-0905_B `455-design` 的重发——上一批同名泳道建了 worktree（`op0905g-editrow-guard-455`）但零提交，本次请务必确认真的写出了 `openspec/changes/` 下的文件并 `git add`/commit，不要只创建 worktree 就结束。

做什么：
1. 起草 openspec 变更包，覆盖三处缺陷合并处置：⑴ 反引号奇偶校验；⑵ 写入后列数回读校验（不符即回滚该次写入）；⑶ 已塌列行的 `--repair` 逃生入口。
2. design.md 引用 Shao Peishen 2026-09-02 给出的元判据「如果能做，就不要指派手工——会带入失误」，说明为何这三条都该机守而非人工复查。
3. `openspec validate --strict` 跑绿。
4. 收尾：`pause --action-key openspec_design_review --waiting-for "design 草案已出，等你审"`。

不做什么：
- 🔴 不改 `0-学习与工具/工具-共享文档编辑锁.py` 一行代码。
- 🔴 不尝试"顺手"修复 `#324`／`#454` 已经手工绕开的具体那两行——本泳道只治根因判据，不追改历史行。
```

### A4 · 341-design-retry——队列写侧按域路由＋高水位线跨文件同步（起草到 design 审为止，与 B-0905_B A5 同范围）

粘贴端：CC ｜ 泳道：341-design-retry

```
[OP-0905-O]【CC】域路由设计重发
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0905o-domain-route-341`）｜ worktree：☑（`domain-route-341-retry`，新 worktree，收工自删）｜ 工作区：无（纯 openspec 起草，不动产线代码）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_C
读 ①〈队列 §一 `#341`〉（`--row 341 --field all`）→ ②`openspec/changes/archive/2026-08-17-queue-dual-file-split/tasks.md` §2.4／§3.7 与同包 design.md 决策点 3／7 → ③ `openspec/specs/queue-dual-file-topology/spec.md`＋`openspec/specs/editlock-dual-queue-routing/spec.md` 里的「实现差异」标注 → ④ CLAUDE.md 恢复上下文。本件为 B 类——「未声明域时该拒绝还是该默认落机制环境」这一取舍需要你在 design.md 里给出推荐方案，止步于起草不代拍板。⚠️ 本泳道是 B-0905_B `341-design` 的重发——上一批同名泳道未见任何分支或产出，视为未真正起跑。

做什么：
1. 起草 openspec 变更包，覆盖 `#341` 期望产出五项：①写侧按 `[D:]` 精确路由；②未声明域的取舍（推荐项+备选）；③跨文件高水位线同步语义；④存量错位行处置口径（含 `#336` 真实实例）；⑤两份 spec 的「实现差异」标注随之更新或移除的方案。
2. `openspec validate --strict` 跑绿。
3. 收尾：`pause --action-key openspec_design_review --waiting-for "design 草案已出，等你审"`。

不做什么：
- 🔴 不改 `0-学习与工具/工具-共享文档编辑锁.py`／`queue_appender.py` 一行代码。
- 🔴 不碰 `#313`（路径常量来源，明确不重叠的邻行）。
```

---

## 三bis、看护opener（单次粘贴，Task/Agent 工具起四子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0905-K]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0905-K]【CC】看护B-0905_C
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，四条子泳道各自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0905_C
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0905K-看护四泳道C。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余四条泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0905_C-2026-09-05.md 全文（§〇～§五）＋ CLAUDE.md 恢复上下文。

你是本批的**看护者**，不是执行者。

【怎么起活】用 Task/Agent 工具为 §三 四条泳道各起一个子任务，`isolation: "worktree"` 让它各自拿独立工作树；把 §三 里对应那条 opener 的正文**原样**作为子任务的 prompt。🔴 不要改写 opener 正文。

【并行与错峰】四条触碰区互不重叠，可同时起（错峰 1-2 分钟，降编辑锁碰撞，无顺序依赖）。

🔴 **本批的特殊关注点（上一批 B-0905_B 三条泳道疑似"起了没做"，如实告知你）**：子任务返回「已完成」时，**不要只信它的自我陈述**——请你至少扫一眼它是否真的 `git push` 了分支、是否真的写出了 openspec 变更包文件、心跳文件是否真的更新过。上一批 `446`／`341` 没有任何分支或产出痕迹、`455` 建了 worktree 但零提交，均是子任务"返回成功"但实际没做事的情形。收工汇总里请对每条泳道给出**你亲自核实过的证据**（分支名、commit 哈希、或文件路径），不要只转述子任务自己说的话。

【看到这些要停下来问他，不要自行决定】
- 任一子任务命中 🟡（`merge_to_master`／`openspec_design_review`）——原样转给 Shao Peishen，一个字母能答的形式。
- 任何子任务命中 ⏭️／🔴——标注去向、不问、不执行，转出。

【收工汇总】四条子泳道到达终态后，产出一份汇总，**每条都附亲自核实的证据**（不是转述）。

【你自己不做的】不写业务代码、不写队列行、不替子任务的产出做 commit、不碰 `.51`、不发任何对外消息、不碰 oem-chroma 三个批次与 `OEM数据隔离规范.md`。
```

---

## 四、收工

跑
```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0905_C
```
现取，不手工数，推企微 ＋ 落档；队列回写走锁。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `rebase3`／`459-382b-impl`／`456-lint-impl` | 已确认完成，不重做 |
| `followup-meta-0902`（#447）分支自陈停在的「change_criteria」判据 | 仍未答，转述给 Shao Peishen，不代答（沿用 B-0905_B 遗留项） |
| B-0905_B 遗留的三个孤儿 worktree（`op0905g-editrow-guard-455`、`temp-merge-448` 等） | 本线在沙箱环境因文件权限无法删除，留待 Shao Peishen 本机或下次 CC session 清理 |
