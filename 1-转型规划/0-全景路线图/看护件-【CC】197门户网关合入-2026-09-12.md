---
title: "看护件 · 泳道看护批 B-0912_门户网关合入"
created: 2026-09-12
status: 生效
批次: B-0912_门户网关合入
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_门户网关合入`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴。** 授权原文：Shao Peishen 2026-09-12 回「`2a`」——§四 `#197` 选项 (a)＝派一条 CC 泳道把 `claude/unified-portal-design-8a2ce3` rebase 到 master、过单测与 `openspec validate --all --strict`，再登『已授权待合』由机器 ff。
🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。本批单泳道。

## 一、硬边界继承

- 🔴 **不 ff master**（🟡 档）；收工只 push 本泳道分支。ff 由派出线 `OP-0912-E` 登 `reports/pending-ff.jsonl` 后由六道守卫做。
- 🔴 **不碰 `.51`**、不跑 `sync-to-server.ps1`、不动任何生产服务（⏭️ 档）。
- 🔴 **rebase 撞冲突就 `--abort` 并以 `OPENER_PARTIAL` 收尾**；不硬解、不 `-X ours/theirs`、不删文件凑绿。
- 🔴 **不改网关源码去让测试变绿**——本棒只搬运，跑出来的红原样报。
- 🔴 **零回归判据＝差集**：分支失败集 ⊆ 纯 master 同命令失败集；不是「必须全绿」。
- 🔴 **不立新机制行**（机制类可动 WIP 32/22 超上限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 队列回写走 `工具-共享文档编辑锁.py`；§二 文件清单**只用反引号包路径**，清单格里不写带斜杠的散文（`#513`）。撞 `.git/index.lock` 不重试。
- 🔴 不写任何凭据值进报告、日志、commit 或队列。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`，命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与在跑批次 |
|---|---|---|---|
| `op0912w-portal-gateway-197` | §四 `#197`（他答 `2a`） | 新分支 `claude/op0912w-portal-gateway-197`；只读 `claude/unified-portal-design-8a2ce3`；队列 §二 新批次 ＋ §四 `#197` 状态格追段 | **零重叠**——差集 31 个文件全在 master 尚不存在的新路径下 |

⚠️ **派出前现取，省重复劳动（2026-09-12，master `891a1b1`）**：`git merge-tree --write-tree master claude/unified-portal-design-8a2ce3` **退出码 0、零冲突**；差集三处路径 `git ls-tree -r master` 均为空。
🔴 **据此更正 §四 `#197` 行内的风险陈述**——原写「2540 笔差距下 rebase 很可能撞 openspec 目录结构变更」，**merge-tree 实测未兑现**。仍须自己跑一遍 rebase 确认（merge-tree 与 rebase 不是同一套算法），但**不要**按「预计会撞」预设冲突解法。

## 三、泳道 opener

### A1 · #162 门户网关 rebase 到 master（OP-0912-W）

粘贴端：CC ｜ 泳道：op0912w-portal-gateway-197
```
[OP-0912-W]【CC】门户网关合入
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912w-portal-gateway-197`） ｜ worktree：☑（op0912w-portal-gateway-197，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_门户网关合入）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】197门户网关合入-2026-09-12.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912w-portal-gateway-197 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912w-portal-gateway-197 --done --batch B-0912_门户网关合入 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。
```
[OP-0912-Y]【CC】看护门户网关
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_门户网关合入）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912Y-看护门户网关。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】197门户网关合入-2026-09-12.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。```

## 四、收工汇总（按此产出）

1. 泳道一行：分支／commit／哨兵／`master..分支` 与 `分支..master` 两个计数。
2. 三组命令的**原文退出行**：网关单测（`cd 5-平台底座/unified-portal-gateway && python -m pytest tests -q`）、`openspec validate --all --strict`、纯 master 对照跑。
3. 🔴 **如实报单测条数**——派单件写的「84 例」是 2026-08-04 的记载，不是今天的实测；与 84 不符就直说不符。
4. 差集回归结论一句：分支失败集 ⊆ 纯 master 失败集？是／否；不成立就点名多出来的是哪几条。
5. §二 批次号与 §四 `#197` 追段原文。
6. 🔴 等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0912_门户网关合入` 现取。