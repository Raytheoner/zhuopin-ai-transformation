---
status: 生效
title: "memory 体系 · 全面审核与优化（2026-08-16）"
created: 2026-08-16
执行方: Cowork（Shao Peishen 2026-08-16 直派：不改业务构建流程及体系，全面审核 CC 开发环境 memory 体系，能改则改）
依据: 队列 #254 四阶段（阶段0 盘点 2026-08-07 已完成）＋《Context体系诊断与精简方案-2026-08-05》＋《memory体系-阶段0只读盘点-2026-08-07》
边界: 未改任何仓库文件、未动队列/编辑锁/sweep/openspec 任何机制；改动全部落在 Cowork 云端 memory 层（本层归 Cowork 管辖）
---

# memory 体系 · 全面审核与优化（2026-08-16）

## 〇、一句话结论

**"容易记住的规则要反复讲 N 遍"的最大单一根因已找到并当日修复：Cowork memory 层实际存有 42 件记忆（含 2026-07-30 就存过的称呼纪律），但注入 session 的索引只列 6 件——其余 36 件对所有 session 等于不存在。** 写入通道本身是健康的（#254"已断"的结论只对本机旧存储成立，对现役云端存储不成立），断的是**可见性层（索引）**。索引已全量重建并回读验证；另有三处仓库侧载体问题（CLAUDE.md 双重记账回涨、场景 CLAUDE.md 膨胀病灶转移、#254 过时结论仍挂在 CLAUDE.md 顶部）不在本层权限内，列 §四 待拍板。

## 一、记忆载体全景与实测（2026-08-16 本机＋云端实测，非估算）

| 载体 | 实测规模 | 载入方式 | 健康度 |
|---|---|---|---|
| 根 CLAUDE.md | 106,353 B（08-09 瘦身后 84,399 → 7 天回涨 +26%） | 每 session 必载 | 🔴 双重记账（见 F3） |
| Cowork memory 云端层 | 42 件（修复后 44 件） | 索引注入＋按需读 | ✅ 已修复（原索引 6/42，见 F1/F2） |
| 本机 local-agent memory | 2 件（06-11／07-27 后零增长） | 已不被云端 session 使用 | ⚰️ 半死（#254 实测对象即它） |
| 场景 CLAUDE.md ×9 | SC8 86.9KB／FI2 64.7KB／wecom 49.9KB… | 进场景必载 | 🟡 病灶转移（见 F4） |
| worktree 内 CLAUDE.md 副本 ×13 | 89,908–132,100 B 各版本并存 | 该 worktree 内 session 必载 | 🟡 陈旧规则风险（见 F5） |
| 队列（机制环境＋业务场景） | 1,318,093 B ＋ 247,951 B | 开工必读（协议〇在顶部） | 🟡 清扫节奏落后增长（见 F6） |
| session接力-Phase1收口.md | 52,713 B，最新日期节 08-05 | 开场必读 | 🟡 R5 边缘（3 个日期节） |
| 进度编年-CHANGELOG／队列归档件 | 145KB／约 1MB | 按需 grep | ✅ 归档模式健康 |
| 全局 ~/.claude/CLAUDE.md | 9,280 B | CC 每 session | ✅ 精简样本 |
| skill／opener 模板库／取证知识库 | — | 按需（非必读） | 🟡 已知"非必读载体缺席"问题（#311 根因链②，不重复展开） |

## 二、发现（按危害排序）

### F1 🔴 索引只列 6/42：36 件"孤儿记忆"，含称呼纪律本尊

`user_address_shao_peishen.md`（2026-07-30 建，内容完整：正向规则＋路径反禁令＋历史不追改＋负向断言 `Paul(?! Shao)` 批量替换法）**早已存在，但从未进索引**。同样不可见的还有：会话末决策清单、时间戳 UTC 判别、取证先于定性、机制优先、粘贴端标注、跟进信落款等——**几乎全部是"说过 N 遍才奏效"的那批规则**。证据链闭合：规则被存 → 索引不列 → session 看不见 → 违反 → 他再讲一遍 → 另一 session 再存一份（本次审计当天上午我就重复存了一份称呼纪律进 feedback_working_preferences，事后才发现权威件早已存在）→ 仍不入索引……循环至今。**这不是"记不住"，是写入动作只做了一半（写文件、没写索引），而索引才是唯一可见层。**

### F2 🔴 双存储分裂：#254 的结论对现役存储已过时

本机实测：`AppData\Roaming\Claude\...\spaces\...\memory\` 至今只有 2 个旧文件（与 #254 时逐字相同）；而云端层 42 件全部可读、当日写入回读逐字一致。**⇒ #254"写入通道已断、索引不可信、勿依赖"的实测对象是本机旧存储；现役云端存储读写健康。** 该过时结论仍以 🔴 大段挂在根 CLAUDE.md 顶部，每 session 必载——**它现在本身就是误导源**：既让所有 session 放弃使用一个健康的层，又规定了一个对云端层恒失败的验证法（PowerShell 反查恒 False）。另录一条佐证：队列 #255 行内提到的 `feedback_edit_tool_onedrive_silent_failure` 记忆在 42 件中不存在——CC 侧自称"已补充记忆"写去了第三处或已丢失，跨桌"记忆"从未同层。

### F3 🔴 根 CLAUDE.md 双重记账：同一批进度写两遍，瘦身 7 天即回涨 26%

实测分段：顶部进度段 21,786 B（9 条）＋ **文末 Last Updated 链 22,971 B（内容与顶部同一批事件的复述）** ＝ 44.8KB 流水账，占全文 42%；§5 规则 51,272 B。#311（08-09）瘦到 84,399 B，今 106,353 B——**回涨速率 ≈3KB/天，全部来自双份记账**。另实测正文含裸 `Paul` 30 处（历史署名，按纪律不追改）——这解释了为什么模型输出容易被带偏：必载文件里"Paul"出现频次远高于规则提醒本身。

### F4 🟡 膨胀病灶已转移到场景 CLAUDE.md

08-05 诊断判定场景层"健康、不必动"时：SC8 37.8KB／FI2 25.9KB。今实测：**SC8 86,862 B、FI2 64,713 B——11 天翻倍以上**，成因与根 CLAUDE.md 同构（进度流水账逐 session 追加、无迁出机制）。场景层没有 R5。CC 进 SC8 建造时开场即载 106KB＋87KB≈193KB，规则信噪比问题在场景内复现。

### F5 🟡 13 份 worktree 内 CLAUDE.md 副本版本并存（89KB–132KB）

git worktree 的固有行为，但后果是：在陈旧 worktree 里开的 CC session 载入的是**旧版规则**（最旧副本停在 08-09 之前）。已有家族（#267 孤儿 worktree 扫描）可廉价承接：扫描报告加一列"CLAUDE.md 落后 master N 提交"。

### F6 🟡 队列机制环境文件 1.3MB，清扫节奏落后于增长

协议〇.8 每周清扫机制本身健康（202607/202608 归档件在），但机制环境队列自 08-05 清扫后已回涨至 1.3MB。属既有机制的执行节奏问题，交值周巡检即可，不需新机制。

## 三、已执行修复（全部在 Cowork memory 层，当日完成并回读验证）

1. **MEMORY.md 索引全量重建**：6 行 → 34 行（纪律与偏好 23／项目状态 8／环境 2＋新增 1），全部 42 件逐一读过后分类；过时件明确降级——3 件标"历史快照（2026-06 月态）"、6 件标"已废弃"（各有权威后继或为探针），不再作现状引用。
2. **新增 `reference_memory_system.md`（memory 体系地图）**：双存储结构、#254 结论更新、写入验证法（回读替代 PowerShell 反查）、索引维护三步纪律（写文件→写索引→回读）、"只放事实/教训/指针、不放规则正文"边界（守阶段0 §3.2，不造第二 rules 层）。
3. **`project_zhuopin_ai_transformation.md` 快照刷新**：2026-07-09 → 2026-08-16（FI2/SC8 运行态、队列拆分、机器字段、销售 v5、#300/#313 机制大事）。
4. **重复项归位**：称呼纪律以 `user_address_shao_peishen.md` 为权威件置顶；今晨我在 feedback_working_preferences 重复写入的一段保留为强化引用（内容一致，无两处不同步风险）；`cc_session_handoff_read_file`（06-29）与 `cc_prompt_by_reference`（07-03）同规则两存，索引只列后者并注明并入。
5. **验证**：全部写入经 `project_memory_read` 回读逐字核对；本机 PowerShell 恒 False 已实测确认并写入地图，防止后续 session 复现 08-05 误判。

**明确没做**：未删任何文件（工具无删除能力，以索引降级代替）；未批量改写存量记忆里的历史"Paul"署名（历史不追改）；未新建仓库内任何载体（守 #254"不新建"边界——本次修的是可见性，不是换载体）。

## 四、待拍板：仓库侧三件（本层无权限，均为载体卫生，不改任何流程/机制语义）

**P1 · 根 CLAUDE.md 顶部"memory 层现状"段改写**（对应 F2，最急——它每天都在误导每个 session）。建议替换为（供 CC 原样使用）：

> **🔴 memory 层现状（2026-08-16 复测更新，替代 2026-08-05 #254 结论）**：Cowork memory 分两份——**云端层（现役，健康）**：`project_memory_*` 工具读写，42+ 件，索引已 2026-08-16 全量重建可信；**验证一律用回读，本机 PowerShell 反查对它恒 False，不得作为判据**。**本机 local-agent 旧层（半死）**：`spaces\*\memory\` 仅 2 个旧文件，#254 当时实测的是它，结论对它仍成立。CC 侧看不到 Cowork memory，跨桌纪律的权威载体仍是本 CLAUDE.md 与队列；Cowork 侧体系细节见 memory 索引之《memory 体系地图》。#254 分阶段推进定调不变，阶段1 验收窗口见队列该行。

**P2 · 根 CLAUDE.md 记账去重**（对应 F3）：Last Updated 链只保留最近 1 条＋一行指针（更早的与顶部进度段/CHANGELOG 完全重复，迁出零信息损失）；顶部进度段维持 R5"只留最近一批"。预计一次性减约 20KB 且根治 3KB/天的双份回涨。纯迁移，与 #253 C4 同性质，零风险。

**P3 · 场景 CLAUDE.md 建立场景级 R5**（对应 F4）：SC8/FI2 各建同目录 `CHANGELOG.md`，六段式正文只留"当前态＋最近一批"，历史批次迁出——完全复制根文件已验证的模式，不发明新机制。可随下次进入该场景的 CC session 顺带执行。

（F5 worktree 副本一列提示、F6 队列清扫，分别交 #267 家族与值周巡检既有机制，不单列拍板项。）

## 五、验收判据（阶段1 试验体，两周窗口）

本次索引重建＝#254 阶段1 的天然试验，验收照阶段0 定的判据：**至 2026-08-30，是否有 session 因读到索引条目而改变了行为**。最易测的信号：⑴ 称呼纪律零违反（含新 session 首答）；⑵ 新写记忆是否都带索引行（抽查 MEMORY.md 与文件清单差集＝0）；⑶ 有无 session 再次引用"memory 不可信勿依赖"旧结论。三者任一持续失败 → 阶段1 判失败，回到"只靠 CLAUDE.md"，不硬推阶段2。

## 六、去向

- 本文落 `1-转型规划/0-全景路线图/`，未提交（Cowork 不擅自 commit），git 处置见会话收工段。
- 队列侧：P1-P3 若获拍板，建议登 §一 一行（机制环境队列）由 CC 领取；#254 行可补一句"阶段1 试验体已就位（2026-08-16 索引重建），验收窗口至 08-30"。
