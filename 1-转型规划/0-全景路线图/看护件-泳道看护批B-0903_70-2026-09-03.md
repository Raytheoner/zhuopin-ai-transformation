---
title: "看护件 · 泳道看护批 B-0903_70（第八批：FI9 接 OEM 隔离层，五条裁决落地）"
created: 2026-09-03
批次: B-0903_70
派出线: Cowork 业务总线 OP-0903-D
前批: B-0903_50（A1/A2 OK、A3 PARTIAL 停 FI9 design 审，五条已于本日裁定）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0903_70`

## 〇、本批口径与起法变更

**Shao Peishen 2026-09-03 答「按你推荐执行」** ⇒ FI9 `fi9-rd-cost-mvp` design 审五条全部裁定，A3 段解除暂停。

🔴 **本批起法变更（他 2026-09-03 定，此后按档分流）**：
- **含 🟡 档（合入 master／改口径判据阈值／design 审／关他人队列行）或首次跑通一条链路** ⇒ **他在 CC 里粘 opener 起**（正常权限模式、人在环），**不走无头全自动**。
- 🟢 档为主、不写 `master` 的批 ⇒ 才走 `工具-opener批处理执行v2.ps1` 无头全自动。
📌 **成因**：无头模式跑的是 `claude --dangerously-skip-permissions`。B-0903_50 的 A1 段在**主工作区直接操作 master**，而当时工作区有 10 个未提交文件（含他线在办）；A1 自行做了「两链改动 vs 工作区文件零交集」核对所以没出事，**但那是它自己判的、没有第二个人看过**。判错即 `git rebase` 会把别人的在办改动搅进去。⇒ 本批**含合 master**，按新规矩走人在环。

⚠️ **本批不生成批处理可解析的三行结构**（不经 `工具-opener批处理执行v2.ps1`），opener 直接粘。

---

## 一、五条裁决原文（🔴 逐条落实，勿凭记忆复述）

| 条 | 裁决 | 要点 |
|:--:|---|---|
| **①** 研发费用金额与工时 | **(c) 分层** | **明细**（带项目标识）按硬隔离；**脱敏聚合量**（不带项目号的期间/部门合计）走规范 **§2.3 通用层**既有机制（无 OEM 信息校验 ＋ 脱敏 ＋ 签字确认 ＋ audit）。🔑 **不是新造一层，是复用 §2.3。** |
| **②** 高新辅助账跨全部 OEM | **(c) 立法定申报豁免款 ＋ 🔴 三道锁** | 见下方专节，**三道锁一条都不许省** |
| **③** 未判归属能否先归集 | **(b) 允许归集、禁汇总与对外** | 🔴 **被排除项目清单须在报告显要位置，不得塞附录**——否则 (b) 会退化成"悄悄少算" |
| **④** 谁认定 OEM 归属 | **(a) 研发侧认定、财务侧使用** | FI9 **只读不判**；主数据缺失即走 ③(b)。🔴 **禁止从项目号命名规则推导**（推错不报错，一错就是把 A 客户费用记到 B 客户名下） |
| **⑤** 规范 §3.3 唯一入口卡不卡 | **(a) 不卡** | 规范原文三次点名 Chroma／检索／RAG，射程＝向量库检索路径；FI9 走 U9C 关系型取数 |

### 🔴 ② 的三道锁（豁免款条文必须写死这三条，否则会被下一个场景引用成通用后门）

1. **限用途** —— 仅限**法定对外申报**（高新认定／研发费用加计扣除）。🔴 **不得用于任何内部分析、管理报表、绩效或成本核算。**
2. **限流向** —— 跨 OEM 汇总产物**只能进申报材料包**；🔴 **不得落任何可被其他场景检索的库（含通用库）、不得进 RAG**。
3. **限留痕与人工闸** —— 每次跨 OEM 汇总写 audit（**申报期间／经办人／审批人／覆盖项目清单**）；产物**须人工签认后方可对外**。⚠️ 这一条 FI9 `config.py` 的 `EXTERNAL_FILING_GATE` 已经写了（「AI 归集建议，须财务/研发总监审核确认」「MUST NOT 具备一键生成即可提交的路径」）⇒ **指认它，不要重写一份**。

🔑 **② 立得住的法理依据（写进条文，供 IATF 审核时出示）**：规范 §1 写明依据是**客户保密协议**＋IATF 16949，保护对象是「防 A 客户数据被 B 客户看到」；而高新辅助账的接收方是**税务机关**，**数据没有流向任何竞争客户**。规范此刻对此是**空白**，不是"已禁止"。

---

## 二、🔴 硬边界（本批特有，逐条适用）

1. 🔴 **不改《OEM 数据隔离规范》本体** —— 三处连带回写（① 补 §2 三分法表／② 立豁免款／⑤ 补 §3.3 射程澄清）属 **IATF 正式规范修订**，① 须 Shao Peishen 批准（该文卷首「维护」条），② 触碰区**与 `oem-chroma-ownership-rejudge` 在办改动重叠**（工作区 `M`，派生行 §一 `#466` 未开工）。⇒ **本批只产出「规范修订建议稿」作为待批文本放进变更包，一个字都不写进规范本体。**
2. 🔴 **跨 OEM 汇总能力必须由开关控制、默认 `OFF`** —— 翻开条件 ＝ **② 的豁免款已正式立进规范**。⚠️ **裁决已下 ≠ 规范已立**：代码先于规范落地，在 IATF「单一可信源」下站不住。开关模式照抄既有 `SC8_KIT_DATE_RULE1`（同族先例）。
3. 🔴 **不填任何未签认判据、不给任何默认值** —— `HIGH_TECH_POLICY_LIBRARY` 等一律保持 `None` ＋ CI 守。
4. 🔴 **`TIMESHEET_SYSTEM_EXISTS` 不动** —— 属 §一 `#477`（问人的事，未开工）；人工费用归集在其闭环前一律 fail-loud。
5. 🔴 **不做 `criteria-signoff-platform` §5.1**（同改根 `CLAUDE.md` §4 ＋ `5-平台底座/CLAUDE.md`）—— 超出本 opener 触碰区，已在 B-0903_50 A3 段留步，另派。
6. 🔴 **⏭️：`.51` 部署及任何触碰生产服务的动作本批不做**。
7. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线**本体**变更／ASIL C-D 永不代办**；**三闸全锁**，不起草不发送任何跟进信。
8. 🔴 **范围收窄**：本批只做 design 局部件对应的 **OEM 隔离接法这一段**，**不做本包 `tasks.md` §2 那七条收口项**。

---

## 三、泳道 opener（▶ 首次派出 · 🔴 由 Shao Peishen 在 CC 里粘，不走无头）

### A1 · 【CC】FI9 接 OEM 隔离层 —— 变更包 fi9-rd-cost-mvp

粘贴端：CC ｜ 泳道：fi9-oem

**开场词（复制即用，▶ 粘贴端：CC 新会话）**：

```
[OP-0903-E1]【CC】FI9接隔离层
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0903e1-fi9-oem`）｜ worktree：☑（fi9-oem，新 worktree，收工自删；🔴 合入 master 那一步回主工作区做）｜ 工作区：无（场景包内建造 ＋ openspec，🔴 不部署、不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_70
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903E1-FI9接隔离层。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\看护件-泳道看护批B-0903_70-2026-09-03.md 的 §一（五条裁决原文）＋ §二（硬边界八条）＋ §三 A1（本节「做什么／不做什么」）＋ CLAUDE.md 恢复上下文，按 §三 A1 执行。本件为 A 类（五条已逐条拍板、判据已写死），无需再问澄清，直接开工。
```

### A1 · 开工前置

**心跳**：`reports/lane-heartbeat/B-0903_70-fi9-oem.md`，开工 1 分钟内写首行，每里程碑追加，超 10 分钟补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
**分档**：判据现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`；🟡/🔴 走 `pause --batch B-0903_70 --wave 1 --lane fi9-oem --action-key <键> --waiting-for "<...>" --no-notify` ＋ 以 `OPENER_PARTIAL` 收尾；⏭️ 走 `transfer-out`。
🔴 **`merge_to_master` 属 🟡，本批已获 Shao Peishen 2026-09-03 放行**（他在场、本件由他亲手起）⇒ 合入本包改动时不 pause；**范围超出下述「做什么」即照停**。
**另读**：① 🔴 `3-治理与合规/OEM数据隔离规范.md` §1／§2 三分法／§3 行为规则（**动隔离层之前必读**）② `openspec/changes/fi9-rd-cost-mvp/design.md`（E1 边界表／E2 三处实现选择）③ `1-转型规划/0-全景路线图/design审读件-FI9-OEM隔离接法-五条定夺项-2026-09-03.md`（裁决依据与论证）④ `5-平台底座/CLAUDE.md` 隔离边界。

### A1 · 做什么（🔴 严格按序）

1. **design.md 落裁决**：把 §定夺项五条改写为**已裁决**形态，逐条记裁决原文 ＋ 拍板人 ＋ 日期（2026-09-03 Shao Peishen）＋ 依据指针（审读件）。🔴 ② 须把**三道锁**与**法理依据**（§一 表下方那段）一并写进 design。
2. **E2.1 三态 `oem_customer`**：`None`（未判，默认）／注册 OEM 名（须在 `REGISTERED_OEMS` 内）／`NON_OEM_PROJECT`（显式哨兵常量）。
   🔴 **不许把「明确非 OEM」写成 `None`** —— 那会让「不涉客户数据」和「压根没人看过」共用一个表示。单测须含反例。
3. **E2.2 guard 调在取数入口**（进 `feed_source` 那一层）**调一次**，不在每一行调。⚠️ 入口层 guard 挡不住汇总层 ⇒ 汇总层另有一道，见第 5 步。
4. **③(b)＋④(a)**：未判归属的项目**允许归集明细、禁止进任何汇总与对外产物**；产出「因归属未判被排除的项目」清单，🔴 **放报告显要位置、不得塞附录**。归属**只读主数据**，🔴 **禁止从项目号命名规则推导**（写一条单测钉死：给一个"看起来像比亚迪项目"的项目号但主数据为空 ⇒ 必须判未判、不得推导）。
5. **②(c) 跨 OEM 汇总 —— 🔴 开关默认 OFF**：
   - a. 实现跨 OEM 汇总能力 ＋ 三道锁（人工闸**指认既有 `EXTERNAL_FILING_GATE`，不重写**）；
   - b. 🔴 **由开关控制、默认 `OFF`**，开关命名与形态照抄 `SC8_KIT_DATE_RULE1` 既有模式；**翻开条件写进 config docstring ＝「② 的豁免款已正式立进《OEM 数据隔离规范》」**；
   - c. 🔴 **理由写进注释，别只写行为**：**裁决已下 ≠ 规范已立**；代码先于规范落地在 IATF「单一可信源」下站不住；
   - d. audit 事件按 E2.3 填 `oem_context`；跨 OEM 汇总事件的 `oem_context` 填法在 design 里写清楚（②(c) 已裁 ⇒ 可定，不再留空）。
6. **起草规范修订建议稿（🔴 待批文本，不改规范本体）**：在变更包内新建一份建议稿（`specs/` 之外，或 design 附录），含三处待批条文 —— ① 补 §2 三分法表「研发费用金额／工时」行（按 (c) 分层）／② 豁免款全文（三道锁 ＋ 法理依据）／⑤ §3.3 射程澄清一句（「本条射程为向量库检索路径；关系型取数经 `OEMRouter.guard` 判定，不受本条约束」）。
   🔴 **一个字都不写进 `3-治理与合规/OEM数据隔离规范.md`** —— 触碰区与 `oem-chroma-ownership-rejudge` 在办重叠（§一 `#466` 未开工），且规范修订须 Shao Peishen 批准。**建议稿末尾注明：须与 `oem-chroma` 线收口后并成一次修订派单。**
7. **单测**：三态形态／未判不进汇总／禁推导／开关 OFF 时汇总不可用且 **fail-loud（不是静默返回空）**。🔴 **「关掉守卫即失败」照 D2.4 做两层**：一次性变异实测 ＋ 常驻元测试。
8. `openspec validate --all --strict` 须绿；FI9 单测全绿；底座与其余四场景零回归。
9. 按「完工即归档纪律」判能否归档：🔴 **tasks 未全 [x] 就不归档**（§2 七条收口项本批不做），按三条机器入口之一留说明。⚠️ **解释文字里不要写出 defer 标记的字面值** —— 会被 `工具-落库sweep.py` 的 `STALE_CHANGE_DEFER_MARKER` 当成真声明、把整个包**永久静音**（2026-09-03 实撞）；只引用变量名。
10. **合入 master**：`git fsck --connectivity-only` → `git pull` → rebase 到 master 新头 → `merge --ff-only` → push。🔴 **合前先看 `merge-base --is-ancestor`**（sweep 每轮推进 master，分叉即 ff-NO）。
11. **收工**：§二 批次登记（🔴 **文件清单里每个路径都要用反引号包住** —— 守卫⑹ 走 ``re.findall(r"`([^`]+)`")``，裸路径提取为空会判「孤儿文件」拒绝 release）；§一 `#473` 回写（FI9 场景行）。

### A1 · 不做什么

- 🔴 **不改 `3-治理与合规/OEM数据隔离规范.md` 本体**（三处回写只出建议稿）
- 🔴 **跨 OEM 汇总开关不得默认 ON、不得省掉开关直接实现**
- 🔴 不动 `TIMESHEET_SYSTEM_EXISTS`（属 §一 `#477`，问人的事，未开工）
- 🔴 不做 `criteria-signoff-platform` §5.1 两处 `CLAUDE.md` 同改（超触碰区，另派）
- 🔴 不做本包 `tasks.md` §2 那七条收口项
- 🔴 不填任何未签认判据、不给任何默认值
- 🔴 不部署、不触碰 `.51`、不连真实库、不发信（三闸全锁）

---

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0903_70` 现取。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| 三处规范回写（①/②/⑤） | 只出建议稿；**等 `oem-chroma-ownership-rejudge`（`#466`）收口后并成一次修订派单**，须他批准 |
| `criteria-signoff-platform` §5.1 两处 `CLAUDE.md` 同改 | 可与上一行并成同一批派单 |
| FI9 工时系统存在性核实 | §一 `#477`（问人的事） |
| L2 模型层纪律收编 | §一 `#476`（🛑 触发条件＝两个财务引擎落地） |
| LAN 收口 | K-1=(a)：**2026-08-31 `OP-0831-L` 那次算跑通**，本轮应收口清单取证为空，**不起收口批** |
| §四 分诊 22 条（含 4 条逾期） | `reports/section4-triage-2026-09-02.md`，下一轮摆 |
