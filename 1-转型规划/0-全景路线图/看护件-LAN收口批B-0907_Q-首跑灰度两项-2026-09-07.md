---
title: "看护件 · LAN 收口批 B-0907_Q（首跑灰度：两项、全只读——使用度量真实采样 ＋ SC2 生产代码对齐核验）"
created: 2026-09-07
批次: B-0907_Q
派出线: Cowork 环境总线 OP-0906-W（skill `zhuopin-lan-closeout` 首次真实触发）
LAN: ✅ 在网（11:3x 探针：`192.168.100.51` 8091／8092／8093／8094 TCP 全通；8091／8093／8094 `/api/ping` 200；`~/.ssh/config` `supplychain-server`→192.168.100.51）
status: 待你在 CC 里起
---

# 看护件 · LAN 收口批 `B-0907_Q`

## 〇、本批口径

**触发**：Shao Peishen 2026-09-07 回公司，问「还没做过一次生产 LAN 收口，是否现在跑一次」——本件＝skill `zhuopin-lan-closeout` **第一次真实触发**，按正本 tasks 3.1 首跑灰度：**单批上限 2 项、他在场**；三判据（误收 0／不合格登记全部点名／每项证据链齐全）过后，后续批才放开到 6。

**探针**（不可跳过，已做，见 frontmatter）：在网。**三查**：master↔origin 对齐（0/0，值周 11:xx 实测）；队列锁无长占（值周 A1 清扫 `OP-0907-P` 可能短占，撞上即等）；上一收口批汇总——**无（本批即首批）**。

**三源留步清单汇集结果**（D1 白名单：只收有可执行判据的留步登记）：

| 来源 | 命中 | 判定 |
|---|---|---|
| 未闭合产出扫描器 形态 1（`--skip-lan-probe --json`，11:36） | `#312`／`#398`／`#452`（正文提及「LAN 留步」概念，非留步登记）；`needs_review`：`#354`（行内明写「不要再当 LAN 留步捞出来」）／`#419`（逃生通道第三道防线，须先修代码非收口项） | 全部**落选**：无一条带「快照点／冒烟判据／回滚 SOP」三件 |
| 两份队列 `--digest --grep`「LAN 留步」「待回内网」 | 同上 4 行＋`#452` | 同上 |
| 近期看护件 LAN 留步节 | `B-0906_P` §6.5「待回内网」：**(iii) 使用度量真实采样**（Shao Peishen 09-06 答 5(a)：脚本推上 `.51` 就地跑、只取回报告） | ✅ **入批（项 1）** |
| 接力卡／场景 CLAUDE.md 交叉 | 采购域接力卡 08-22 自陈「SC2 生产仍跑旧码」，而 SC2 `CLAUDE.md` 部署状态停在 08-18 首次部署、`#361` 收口 08-25 又合入两个 commit——**生产是否落后无人核过** | ✅ **入批（项 2，只读核验；落后则登记留步、不在本批部署）** |
| 他线在办（不入批，点名） | `#424` FI2 tax-export 重试 pass（CC `OP-0907-N`，你答 1a、给 Bash 权限后它自跑；`.51` 备份已打好）；FI2 面板改判 `.51:8094` 冒烟（同一 N 会话「服务器本机驱动面板管线」） | 🔴 **本批不碰 `C:\fi2\`**，与 N 零重叠 |

**首跑刻意全只读**：两项都不改 `.51` 上任何服务、不重启任何进程——灰度要验的是「清单汇集→白名单→证据链→回写」这条编排本身，不是部署动作。

## 一、🔴 硬边界

1. `.51` **一次只动一项、串行**：项 1 收工回写后才起项 2。N 会话若正在 `.51` 跑 `ingest_tax_export.py`，等它结束再起本批（问 Shao Peishen 一句即可，不自判）。
2. 只读：不 `Restart-Service`、不改 `C:\sc2\`／`C:\fi2\` 任何文件、不部署；上传的采样脚本落 `C:\tmp\usage_sampler\`，跑完**删目录**（回滚＝删）。
3. 冒烟判据不过即停项、降回留步态并注明原因；不带伤推进。
4. 对外发送与请人动作永不代办；采样报告只落仓库，不推企微、不起草信。
5. 看护件落档失败即全停；`pause`／`check-heartbeat` 一律 `--no-notify`（`#492` 未落地）。

## 二、波次（全串行）

| 序 | 项 | 承接 | `.51` 触碰 | 快照点 | 冒烟判据 | 回滚 SOP |
|---|---|---|---|---|---|---|
| 1 | 使用度量真实采样（只读） | `#436` ⑶③（B-0906_P §6.5） | 读 `C:\fi2\`／`C:\sc8\`（或实际部署目录）下 audit／访问日志；写只在 `C:\tmp\usage_sampler\` | 无需（只读）；记录所读日志文件名＋大小＋mtime | 报告生成且含 FI2＋SC8 两场景近 30 天「场景×用户×日」表＋零触发功能清单＋日志缺口段；`--rotate-warn-mb` 对 `#358` 309 MB 日志给出建议 | 删 `C:\tmp\usage_sampler\` |
| 2 | SC2 生产代码对齐核验（只读） | `#361`（done，只作证据挂靠）／采购域接力卡 | 读 `C:\sc2\app\` 文件哈希 | 无需（只读）；记录 `C:\sc2\app` 顶层文件清单＋哈希 | master `4-数字员工/采购部/SC2-采购周报自动生成/sc2/**` 与 `C:\sc2\app\**` 逐文件 SHA256 对比出结论：**一致**⇒销疑；**落后**⇒登记 LAN 留步「SC2 重部署」并附三件（快照＝`Copy-Item C:\sc2\app C:\sc2\app.bak-<ts>`／冒烟＝`smoke-server.ps1` 七项／回滚＝SC2 CLAUDE.md 部署状态表「回滚」行）交下一批 | 无（未改动） |

## 三、逐项步骤（看护者本人按序执行，不扇出——首跑 ＋ #138「Task 子代理限只读、写活不进子代理」）

### 项 1 · 使用度量真实采样

1. 快照点：`ssh supplychain-server "dir C:\fi2\app\reports C:\sc8 /s /b | findstr /i jsonl"`（或按 `5-平台底座/CLAUDE.md` 子系统表定位 audit 路径），记下文件名／大小／mtime 进回写。
2. 执行：`scp 0-学习与工具/工具-使用度量采样.py supplychain-server:C:/tmp/usage_sampler/`；`ssh supplychain-server "C:\fi2\.venv\Scripts\python.exe C:\tmp\usage_sampler\工具-使用度量采样.py --audit <FI2 audit>=FI2 --audit <SC8 audit>=SC8 --window 30 --asof 2026-09-07 --out C:\tmp\usage_sampler\report.md --json C:\tmp\usage_sampler\report.json"`（真实路径以第 1 步为准；脚本自带 `--help`）。
3. 冒烟：`scp` 取回 `report.md`／`report.json` 到 `1-转型规划/0-全景路线图/使用度量采样报告-FI2SC8-2026-09-07.md`（json 落 `reports/`，不入库）；核 §二 冒烟判据三条；报告顶部写「首轮采样，不做功能全集声明件（Shao Peishen 09-06 答 5(a)）」。
4. 回写证据：`#436` 状态列追加一段（读了哪些日志、行数、报告路径、三档命中数）；`ssh supplychain-server "rmdir /s /q C:\tmp\usage_sampler"`（回滚＝清场）；登记 §二 批次（报告 md）。

### 项 2 · SC2 生产代码对齐核验

1. 快照点：`ssh supplychain-server "powershell -c Get-ChildItem C:\sc2\app -Recurse -File | Get-FileHash -Algorithm SHA256 | Select Hash,Path"` 存 `reports/sc2-server-hash-2026-09-07.txt`。
2. 执行：本机对 `4-数字员工/采购部/SC2-采购周报自动生成/sc2/**`＋`run_sc2.py` 等部署清单文件（以 `sync-to-server.ps1` 的同步范围为准）算 SHA256，与第 1 步逐文件对比；差异文件列表＋所属 commit（`git log -1 --format=%h -- <file>`）。
3. 冒烟判据：结论二选一——**一致**（差异 0）⇒ 在采购域接力卡「SC2 生产仍跑旧码」句旁登记「09-07 实测已对齐」（走该卡锁）；**落后**⇒ 不部署，登记 §一 新行或并入 `#361` 追加「LAN 留步：SC2 重部署」并写全三件（快照／冒烟＝`smoke-server.ps1` 七项／回滚＝CLAUDE.md 回滚行），交下一收口批。
4. 回写证据：`#361`（或新行）状态列一段（差异文件数＋落后 commit 清单＋哈希文件路径）；登记 §二。

## 三bis、看护opener（单次粘贴；本批不起子任务，看护者按 §三 顺序自己做）

粘贴端：CC ｜ 泳道：（看护者本身）

▶ 首次派出：[OP-0907-Q]

```
[OP-0907-Q]【CC】看护LAN收口首跑
【设置】执行环境：CC ｜ 分支：master（不建分支，不改代码；产出只有报告 md 与队列/接力卡回写）｜ worktree：☐（不建，本批不写仓库代码）｜ 工作区：`.51`（192.168.100.51，ssh 别名 `supplychain-server`；本批全只读，不重启服务、不改 `C:\sc2\`／`C:\fi2\`）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_Q）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0907Q-看护LAN收口首跑。🔴 你是本批唯一会话，本条对你适用；本批不起任何 Task/Agent 子任务（首跑灰度＋#138：写活不进子代理），所以不存在「跳过本行」的例外情形。
读 `1-转型规划/0-全景路线图/看护件-LAN收口批B-0907_Q-首跑灰度两项-2026-09-07.md` 全文＋ CLAUDE.md＋`.claude/rules/两桌同步与取证.md` 恢复上下文。授权链：Shao Peishen 2026-09-07 触发 skill `zhuopin-lan-closeout`（回公司首跑），本件即授权；他在场。

做什么：
1. 先问他一句：CC `OP-0907-N` 是否正在 `.51` 跑 `ingest_tax_export.py`——在跑就等，不并行动 `.51`（D2 一次一项）。
2. 探针复核（不信 frontmatter）：`Test-NetConnection 192.168.100.51 -Port 8094`＋`Invoke-WebRequest http://192.168.100.51:8094/api/ping`，不通即停并报。
3. 按 §三 项 1 → 项 2 **串行**执行，每项固定四步＝快照→执行→冒烟→回写证据；冒烟不过即停项、降回留步态并注明原因，继续下一项前先报他一句。
4. 首跑三判据自评写进收工汇总：误收 0？落选项全部点名？每项证据链（快照点／冒烟结果／回写位置）齐全？——任一不过，下一批仍限 2 项。
5. 收工汇总一次写全：每项一行（判据→证据→行内状态处置）；「待你一字母」清单标〔荐〕与默认项；**最后一行给答复模板**（如 `1a，2a`）。收口≠销号：行销与否按行内原纪律，本批不销任何行。

不做什么：
- 🔴 不部署、不重启、不改 `.51` 任何服务文件；不碰 `C:\fi2\`（N 在办）；上传物只落 `C:\tmp\usage_sampler\` 且跑完删。
- 🔴 不推企微（`#492` 未落地）、不发任何对外消息、不起草信；采样报告不做功能全集声明。
- 🔴 不 commit：产出登记 §二 批次由 sweep 取活（文件清单须含本看护件本身——它出件时队列锁被 `OP-0907-N` 占用、尚未登记）；撞锁不重试不豁免，写「登记待补」。
```

## 四、收工

看护者收工汇总落本件 §六（追加，不改上文）；`B-0907_Q` 三判据结论一并写入，作为下一批是否放开上限的依据。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#424` FI2 tax-export 重试 pass（`.51` 写动作） | CC `OP-0907-N`（你答 1a），本批不碰 |
| FI2 面板改判 `.51:8094` 冒烟 | 同 N 会话（3a「服务器本机驱动面板管线」） |
| SC2 若核出落后 ⇒ 重部署 | 下一收口批（三件已在 §二 写死） |
| `#419` 逃生通道第三道防线 | 先修代码（CC），修好后才有可收口的部署项 |
| 值周 A3 `#452` 三分支 rebase＋ff | 值周已定「LAN 收口收工后一并派 CC」——本批收工即其触发条件 |


---

# 六、收工汇总（看护者 `OP-0907-Q`，2026-09-07，本节追加、上文一字未改）

## 6.1 逐项一行（判据 → 证据 → 行内状态处置）

| 序 | 项 | 冒烟判据 | 结果 | 证据 | 行内状态处置 |
|---|---|---|---|---|---|
| 1 | 使用度量真实采样（只读） | 报告含 FI2＋SC8 近 30 天「场景×用户×日」表 ＋ 零触发功能清单 ＋ 日志缺口段 ＋ 对 `#358` 日志出轮转建议 | ✅ **四条全过** | 快照点＝7 个日志的名/字节/mtime（报告 §〇 逐行列出）；执行＝`.51` 就地跑、退出码 0，读入 581,533,683 B／2,641,329 行；报告＝`1-转型规划/0-全景路线图/使用度量采样报告-FI2SC8-2026-09-07.md`（21,281 B，取回后字节与服务器侧一致，中文回读抽验无 U+FFFD）；清场＝`Remove-Item -Recurse` 实测 `REMOVED_OK` | `#436` 状态格追加 (iii) 收工段并**顺手做 K2 五次外置**（把已全闭的 (i)-合入段外置进行日志，整格 3705 → **3198 B**，回到 4096 内）；`#436` 维持 `[S:open]`，(iii) 的「待回内网」已闭 |
| 2 | SC2 生产代码对齐核验（只读） | 逐文件 SHA256 比对出结论：一致 ⇒ 销疑；落后 ⇒ 登记留步并附三件 | ✅ **一致，差异 0** | 快照点＝`.51` `C:\sc2\app` 全 22 文件 SHA256（`reports/sc2-server-hash-2026-09-07.txt`，因 `reports/` 被 gitignore、已把 22 行原文抄进 `队列行日志/#361.md` 作可追溯载体）；比对＝`sync-to-server.ps1` `AppFiles` 白名单 14 文件与本机 master HEAD 全等，本机该目录 `git status --porcelain` 0 行；补证＝文件最后落盘 `2026-08-27 10:48:00` 本地 vs 进程 PID 2608／9168 创建 `2026-08-27 10:48:28` 本地（**晚 28 秒**⇒ 跑的就是这份码） | 采购域接力卡「SC2 生产仍跑旧码」句**已划删并就地登记实测结论**（走该卡锁）；`#361` 状态格追加证据段、**维持 `[S:done]` 不销不改判**；**不产生「SC2 重部署」留步项** |

## 6.2 首跑三判据自评（决定下一批是否放开上限）

| 判据 | 自评 | 依据 |
|---|---|---|
| **误收 0？** | ✅ 过 | 入批 2 项都是有可执行判据的真留步；`#424` FI2 tax-export 与 FI2 面板冒烟（`OP-0907-N` 在办）全程未碰，`C:\fi2\` 只做只读日志读取、未写未重启 |
| **落选项全部点名？** | ✅ 过 | §〇 表已逐条点名 `#312`／`#398`／`#452`／`#354`／`#419` 的落选理由；本节再补两条**首跑当场新出现**的落选：⑴ `#358` 日志轮转（本次实测已 544.9 MB）——报告只出建议、**未实施**，因它压着 openspec design 审；⑵ SC2 `smoke-server.ps1` 七项功能冒烟——**本批只读、刻意未跑**，功能面结论仍以 `#361` 为准 |
| **每项证据链齐全？** | ✅ 过 | 两项的快照点／冒烟结果／回写位置见 6.1 三列，均为实测命令产物、无一处「只有动作没有手段」的自陈 |

⇒ **三判据全过。按 §〇 口径，下一批可由 2 项放开到 6 项**（是否放开由 Shao Peishen 定，本节只给依据）。

## 6.3 首跑暴露的三件事（只报不改）

1. 🔴 **看护件 §三 项 1 的 SC8 路径写错**：`C:\sc8` 在 `.51` 上**不存在**；SC8 实际部署在 `C:\baoguan`（`sync-to-server.ps1` 的 `-ServerBase` 写死此值，8091 端口→PID 3616→`run_baoguan_web.py`）。队列 `#436` 里「SC8（`8091` 连接器审计形态）」这句端口对、路径此前从未落纸。**下一批出件时须按实测路径写。**
2. 🔑 **采样器三档判据有一个首跑即暴露的缺口**：分档只看「闲置天数」，于是 `部署冒烟验证`／`冒烟验证`／`CC-D19-smoketest`／`CC-队列250冒烟`／`CC-队列250冒烟复测` 这类**部署冒烟留下的假用户**原样进了 ≥14 天「需你定夺」档——**首轮 7 项候选里 5 项是误报**。建议加「排除冒烟/测试标识用户」的过滤或标注；属改口径判据，待他拍板。已写进 `#436` 状态格。
3. ⚠️ **`--no-diagnose` 与硬边界 ⑶ 的补做**：`.51` 上没有仓库，「只读诊断」（拿外部证据日期比日志日期，判「已在用但未留痕」）无法在服务器侧跑，故走 `--no-diagnose`，由看护者在本机补做。**结论：7/14 两档共 12 项无一为真人**（6 项冒烟假用户、6 项 IP 粒度），无可诊断对象；真人只有 FI2 `李姣龙`（154 次 AI 决策）与 `财务专员(Web-试用版)`（44 次），均落 3–6 天「只记日志」档 ⇒ **本批不产生任何真人「需你定夺」候选**。

4. ⚠️ **顺带核出一处历史转义损坏（只报不改，非本会话引入）**：`1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md` 里有 1 个 BEL 控制字符（`U+0007`），原文应为 `D:\airead`、现被写成 `D:<BEL>iread`——**`git show HEAD:` 版本里就有**，是某条更早会话经 Python 字符串写入队列时被 `\a` 转义吃掉一个字符，与本会话自己两次踩到的是同一个坑（本会话写入的三处已当场回读发现并修好，产出件控制字符现全为 0）。🔑 **通用判据**：**乱码哨兵只查 U+FFFD，查不出这一类**——`\a`／`\b`／`\f`／`\v` 被吃掉后留下的是合法但不可见的控制字符，肉眼与 U+FFFD 哨兵都看不见，只有按码位计数才查得出。建议把「控制字符 0x07/0x08/0x0B/0x0C 计数」并进 `sentinel-mojibake.ps1` 或队列结构 lint；本行属他人触碰区，**未改、未立行**，交总线派发。

## 6.4 §二 登记与转出

- **§二 批次**：`B-0907_Q1_LAN收口首跑项1使用度量真实采样`（含本看护件本身）／`B-0907_Q2_LAN收口首跑项2_SC2生产代码对齐核验`，均 `**待处理**`，待 `ZhuopinCommitSweep` 取活。**本会话未 commit、未 push。**
- **§四 #166 新立**：他 2026-09-07 当场定的「能自查 session 状态就自决、少占他一轮往返」。🔴 **原按 `--reserve` 取 §一 #496 立行，release 时撞机制类可动 WIP 27／22 硬阻断**（协议〇.9 措施 C；**超限在本行之前就存在**，给自己标 🛑 后仍 27＞22 仍拦），本会话无权关他人的行（`#422` 先例）、亦不满足「紧急」故未动 `WIP豁免：`＋`--force-mechanism-wip` 逃生阀 ⇒ **#496 撤回、编号留空不复用**，改立不受 WIP 限的 §四 #166。原行全文见 6.5。
- **本批不销任何行**（收口≠销号）：`#436` `[S:open]` 不变、`#361` `[S:done]` 不变。
- **下一批触发条件已达成**：值周 A3 `#452` 三分支 rebase＋ff 的触发条件是「LAN 收口收工」，本节即其触发点。

## 6.5 §一 #496 原行全文（撤回前备份，供腾出 WIP 余量后原样立回）

```
| 496 |   🔴 **`.51` 串行闸改「机器自查优先、问人兜底」** —— skill `zhuopin-lan-closeout` 硬边界 1 现写死「N 会话若正在 `.51` 跑，问 Shao Peishen 一句即可，不自判」；实测 `mcp__ccd_session_mgmt__list_sessions` 直接给出每条会话的 `isRunning` 与 `lastActivityAt`，本方看得见，无须占用他一轮往返。**改法**：硬边界 1 与 opener 模板第 1 步改为「先跑 `list_sessions` 判目标会话 `isRunning`：为 `false` ⇒ 自决开跑并在收工汇总留一行机器证据（会话标题＋`isRunning`＋`lastActivityAt` UTC）；为 `true`（或工具不可用／目标会话查不到）⇒ 才问他一句」。同族的「问他一句」前置在 `zhuopin-lane-watch` 里如有同形态，一并核一遍。  | CC（改 skill 源码 ＋ opener 模板；非看护会话） | Shao Peishen 2026-09-07 原话「如果你自己能看到相关Session的转态，以后自决最靠谱，较少人工干预」；本次实证＝`OP-0907-Q` 首跑时 `list_sessions` 返回 `[Win]0907N-FI2改判到判例3` `isRunning:false`、`lastActivityAt 2026-09-07T03:49:05Z`，与他答的 1a 一致；看护件＝`1-转型规划/0-全景路线图/看护件-LAN收口批B-0907_Q-首跑灰度两项-2026-09-07.md` §一.1 | skill `zhuopin-lan-closeout` 硬边界 1 与其 opener 模板第 1 步改毕；`zhuopin-lane-watch` 同形态前置核过；改后至少一次真实收口批验活（收工汇总里出现机器证据行、未再问他这一句） |  [S:open][D:机] 🛑 **立行即挂冻结标，不占可动余量**：立行瞬间机制类可动 WIP ＝ 28／22 已超上限（协议〇.9 措施 C 阻断），本行不紧急、不满足 WIP 豁免条件；按「可动余量优先给需求侧」（Shao Peishen 2026-09-06 定）先冻结登记、**待值周巡检／总线去 🛑 后即可领**。 待领（可即领）。🔑 **成因**：该闸的设计前提是「本方看不见其它会话的运行态」，而这个前提已不成立——`list_sessions` 给的是机器事实，比问人更快也更准。**边界**：改的是「怎么判 `.51` 是否被占」，**不改「一次只动一项、串行」这条硬边界本身**；也不改 🔴 对外发送／L2 门禁／`.51` 部署这三类永不代办的口径。⚠️ 本行由看护会话 `OP-0907-Q` 登记而非就地改——按 rules/队列与落库「总线／看护会话不修工具：只登记＋出派单」。  登记豁免：本次 release 点名的其余脏文件（队列行日志 #142/#159/#324/#456/#472/#474/#480、5-平台底座 coverage_point_ledger/models.py、openspec 的 editlock-credential-shape-guard 两件、本周计划-2026-09-07.md、看护件-泳道看护批B-0907_E、队列回写待补/B-0906_X-*.json 五件、6-人才与组织 口径点台账/财务域.jsonl、1-转型规划/session接力-业务总线.md）均在本次 acquire 之前即已脏、属并发 session 在办产出，本会话一字未动、不由本会话认领；本会话自己的产出全部已登记于 §二 B-0907_Q1 与 B-0907_Q2。  | `0-学习与工具/skills源码/`（`zhuopin-lan-closeout`、`zhuopin-lane-watch`）＋ `~/.claude/skills/` 同名件；`1-转型规划/0-全景路线图/opener骨架.md` 如含该步则同改 | 2026-09-07 CC `OP-0907-Q`（LAN 收口首跑批 `B-0907_Q` 收工登记；文件随批次 B-0907_Q1 落库） |
```
