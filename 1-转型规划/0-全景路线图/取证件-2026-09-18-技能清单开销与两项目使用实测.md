# 取证件 · 技能清单开销与两项目使用实测（2026-09-18）

> 立件：`OP-0918-C`（Cowork 环境保障线，他 2026-09-18 答 `2a` 要求落常驻判据）。规则条目在 `.claude/rules/环境保障线.md`，本件是它的实测基线，规则不复述数字。
> 成因：Cowork 每轮注入的技能清单实测 **67.7 万字符**，且**技能集一变就整份重发**。他问「哪些是本项目从来不用的」。

## 〇 · 判据（规则正文只留这一句的指针）

🔑 **账号级 skill ＝ Cowork 会话能用；项目级 `.claude/skills/` ＝ 只有在那个目录里开的 Claude Code 会话能用。把 skill「降到项目级」＝ 放弃它在 Cowork 侧的使用。**
⇒ 所以清理只有三种落点，不是一种：① 两个项目都不用 ⇒ **直接关账号级**；② 只有某项目的 CC 侧用 ⇒ **可降级到该项目 `.claude/skills/`**；③ Cowork 侧要用 ⇒ **必须留账号级**。

🔑 **装插件前先问一句：这条线的仓库里，将来会出现它的名字吗？** 答不上来就别装——装上的每一条，每一轮都在为一份没人读的描述付字符。

## 一 · 手段（可复跑）

- AI 赋能仓（`C:\Dev\zhuopin-ai`）：`git grep` 单次正则一遍过全部跟踪文件，脚本 `reports/_op0918c/skillscan.py`。
- 行业研报（`C:\Users\Paul Shao\OneDrive\Projects\行业研报`）：非 git 仓，按文本扩展名递归扫 **385 个文件**，脚本 `reports/_op0918c/scan_yanbao.py`。
- 账号级清单真身：`ListPlugins`（5 个插件）＋ `ListSkills`（21 条 skill）。
- 本机 CC 侧插件：`C:\Users\Paul Shao\.claude\plugins\installed_plugins.json`（**另一套，与账号级无交集**）。
- ⚠ **假命中须逐条回查**：`analyze` 在研报侧 14 次全在 `_datasources\last30days-skill\...\twitter-client` 第三方 vendor 代码里（普通英文词）；`memory-management` 在 AI 赋能侧唯一 1 次是在一份审计文档里**提到它存在**。两者实为零使用。

## 二 · 实测结果（2026-09-18）

### 2.1 两个项目都零命中 —— 直接关，无搬迁问题（27 条）

| 来源 | 条目 |
|---|---|
| 插件 `design` | accessibility-review／design-critique／design-handoff／design-system／research-synthesis／user-research／ux-copy（7） |
| 插件 `data` | analyze（假命中）／build-dashboard／create-viz／data-context-extractor／data-visualization／explore-data／sql-queries／statistical-analysis／validate-data／write-query（10） |
| 插件 `productivity` | memory-management（假命中）／start／task-management／update（4） |
| 插件 `cowork-plugin-management` | cowork-plugin-customizer／create-cowork-plugin（2） |
| 账号级 skill | canvas-design／theme-factory／web-artifacts-builder／import-memory／morning（5） |

### 2.2 只有 AI 赋能项目用 —— 必须留账号级（Cowork 侧主力）

`zhuopin-lan-closeout` 196｜`zhuopin-lane-watch` 190｜`zhuopin-followup-letter` 182｜`zhuopin-kickoff-prompt` 178｜`zhuopin-queue-audit` 174｜`zhuopin-send-followup` 73｜`zhuopin-rebaseline` 54｜`zhuopin-requirement-grill` 53｜`skill-creator` 18｜`md-to-word` 15（数字＝本仓命中次数，研报侧全部为 0）

🔴 **这 8 条 zhuopin-* 不可降级**：`C:\Dev\zhuopin-ai\.claude\skills\` 现只有 `zhuopin-followup-letter` 一份，其余 7 条一旦降级，Cowork 这条线立刻用不了——而 lane-watch／lan-closeout／kickoff-prompt／queue-audit 正是 Cowork 侧天天在跑的。`md-to-word` 同理（跟进信 docx 通道）。

### 2.3 只有行业研报项目用

| 条目 | 研报侧命中 | 本仓命中 | 处置 |
|---|---|---|---|
| `industry-market-analyst` | 61 次／15 文件 | 0（唯一 1 次是 memory 文件名，假命中） | 🔑 **账号级是重复件**——`行业研报\.claude\skills\industry-market-analyst\`（10 个文件）已有完整副本，关账号级即可 |
| `equity-value-cycle-review` | 3 次／2 文件（`三环集团300408_价值周期审查_2026-07-12.md`、`数据源接通指南.md`） | 0 | **无项目级副本**，要降级须先复制到 `行业研报\.claude\skills\` |

⚠ **核过不会误伤**：账号下 8 个定时任务逐条看过，**没有任何一个跑行业研报**，且全部 `enabled_plugins` 为空（定时任务不锁插件）；研报 CLAUDE.md 提到的 `chokepoint-weekly-scan` **当前触发器清单里不存在**。

### 2.4 判不出来、不建议动

`docx`／`xlsx`／`pptx`／`pdf` —— 名字本身就是文件扩展名，grep 分不清「用了 skill」与「提到格式」。已知：本仓跟进信 docx 走的是 `md-to-word` 不是 `docx` skill；研报侧确实产 `和林微纳_路演稿.pptx`／`_三情景模型.xlsx`／`_深度研报.docx` 实物。**留。**

### 2.5 不在本次范围（另一套）

`C:\Users\Paul Shao\.claude\plugins\` 是 **Claude Code 侧**的 10 个插件（frontend-design／context7／document-skills／superpowers／agent-skills／firecrawl／claude-code-setup／last30days／last30days-cn／outlook），与账号级清单无交集，不影响 Cowork 每轮注入。其中 `superpowers` 于 2026-09-16 以 **project 级装在 `C:\Dev\zhuopin-ai`**、全仓零引用——他 2026-09-18 答 `1b` **保留**。
研报侧 `last30days` 1134 次命中属这一套，不是账号级 skill。

---

## 三 · 🔴 更正注（同日晚，本方自纠；原文不追改）

**§2.3 关于「降级」的处置作废。** 上文把研报线默认成「在 Code 标签跑」，那是**推断**，被该项目自己的文档实证推翻：

- `行业研报\数据源接通指南.md:3`：「把五源数据……**在 Cowork 会话里**稳定接通，供 `industry-market-analyst`、`equity-value-cycle-review`、板块周巡检等任务调用」。
- `行业研报\CLAUDE.md:22`：周扫描定时任务「**Cowork 侧**」；`:54` 出 PDF 分「Code/宿主端 Chrome headless」与「**Cowork/VM 端 weasyprint**」两条路；`:61-65` 明写 hooks「只覆盖本机 Claude Code 桌面端，**不覆盖 Cowork 云端会话**」。

⇒ **研报线是 Code 与 Cowork 两边都在跑的。** 按 §〇 的判据，降级即等于在 Cowork 侧弄丢工具。**Shao Peishen 2026-09-18 答 `1a`：`industry-market-analyst` 与 `equity-value-cycle-review` 两条都留在账号级，不降级、不关闭。**

**另一条硬事实**：全盘递归搜 `C:\Users\Paul Shao` 下所有 `*equity*` 目录，**零命中**——`equity-value-cycle-review` 的源文件本地根本不存在，只活在账号侧。**即便要降级，本方也复制不出来。**

🔑 **本次真正学到的判词**：**「这个项目的 skill 在哪跑」不能从目录形态推断，要去读它自己的文档。** 有 `.claude/skills/` 不等于只在 CC 跑——研报项目两样都有，而决定权在它 CLAUDE.md 的那几行，不在目录结构。

⇒ **修正后的可清理面**：只剩 §2.1 那 27 条两边都不用的（其中 `canvas-design` 他已于当日关闭）。§2.2、§2.3、§2.4 全部保留不动。
