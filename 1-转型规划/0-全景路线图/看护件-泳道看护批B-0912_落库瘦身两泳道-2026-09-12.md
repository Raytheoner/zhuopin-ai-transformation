---
title: "看护件 · 泳道看护批 B-0912_落库瘦身两泳道"
created: 2026-09-12
status: 生效
批次: B-0912_落库瘦身
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_落库瘦身两泳道`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴。** 授权原文：Shao Peishen 2026-09-12 回「`1a，2a，3b`」——`2a` ＝ 台账重跑改条件触发；`3b` ＝ ff 批量审＋低风险白名单。`1a`（Cowork 改自提交）已由 `OP-0912-E` 直接改纪律载体，不在本批。
🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。**`B-0912_env-anchor` 单泳道 19:38 起跑中**，本批两条 ＋ 它 ＝ 3 条，未超并行上限 4。

## 一、硬边界继承

- 🔴 **不放松任何 🟡／🔴 档**：`3b` 只消灭「纯文档分支也要问一次」，触碰代码／队列／纪律载体／`.51`／对外发送的分支**照旧停等他一字母**。
- 🔴 **不 ff master**；收工只 push 本泳道分支。
- 🔴 **不改 `工具-泳道分支合入.ps1` 的六道守卫**；**不动 sweep 的其它 14 类**。
- 🔴 **新文件名形态必须同批补 `.gitignore` 并 `git check-ignore -v` 实测取证**（`#328` 教训）。
- 🔴 不碰 `.51`／企微机器人／定时任务；不做任何对外发送。
- 🔴 **不立新机制行**（机制类可动 WIP 超上限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 队列回写走 `工具-共享文档编辑锁.py`，撞 `.git/index.lock` 不重试。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`，命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与另一条 |
|---|---|---|---|
| `ledger-conditional` | 台账重跑改条件触发（`2a`） | `0-学习与工具/工具-落库sweep.py` 台账重跑那一段 ＋其单测 ＋ `.gitignore` | **零重叠** |
| `ff-whitelist` | ff 低风险白名单（`3b`） | `0-学习与工具/工具-待合分支巡检.ps1` ＋其单测 | **零重叠** |

**结论：两条可并行**。唯一共享资源＝两份队列文件与 `.gitignore`，由编辑锁串行化；两条都碰 `.gitignore` 的概率低（只有 `ledger-conditional` 需要），若真撞由锁排队。

⚠️ **`ff-whitelist` 的额外注意**：判据一律 **fail-closed**（取不到就判非白名单），与本项目其它地方的 fail-open 底线**方向相反**——因为这里「判错了」的后果是**自动合入了不该自动合入的东西**，不可逆；而 fail-open 的场合后果只是白跑一次。**不要照搬 `opener-batch-archive-precheck` 的 fail-open 口径。**

## 三、泳道 opener

### A1 · 台账重跑改条件触发（OP-0912-R）

粘贴端：CC ｜ 泳道：ledger-conditional

```
[OP-0912-R]【CC】台账条件触发
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912r-ledger-conditional`） ｜ worktree：☑（ledger-conditional，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_落库瘦身）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】台账重跑改条件触发-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912r-ledger-conditional --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912r-ledger-conditional --done --batch B-0912_落库瘦身 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · ff 低风险白名单（OP-0912-S）

粘贴端：CC ｜ 泳道：ff-whitelist

```
[OP-0912-S]【CC】ff低风险白名单
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912s-ff-whitelist`） ｜ worktree：☑（ff-whitelist，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_落库瘦身）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】ff低风险白名单-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912s-ff-whitelist --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912s-ff-whitelist --done --batch B-0912_落库瘦身 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-T]【CC】看护看护落库瘦身
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_落库瘦身）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912T-看护看护落库瘦身。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 ``1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_落库瘦身两泳道-2026-09-12.md`` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. 每泳道一行：泳道／分支／commit／哨兵／单测读数（现跑）。
2. `A2` 必须贴**今天真实分支的干跑判定**：`op0912j`／`op0912k` 应命中白名单，`op0912h` 应不命中。
3. 零回归写成「分支侧失败集合 ⊆ 纯 master 同命令失败集合」；已知常红两条不算本批引入。
4. 待 ff 清单（**ff 不在本批内做**）。
5. 🔴 等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0912_落库瘦身` 现取。
