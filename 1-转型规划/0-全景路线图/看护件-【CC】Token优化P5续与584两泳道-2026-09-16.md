---
title: "看护件 · B-0916_Token优化P5续与584（两泳道并行）"
created: 2026-09-16
批次: B-0916_Token优化P5续与584
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 2a）
---

# 看护件 · B-0916_Token优化P5续与584

> 路线图正本：`构建Token消耗优化-核验与路线图-2026-09-16.md` §〇。任务全文在队列 §一 `#583`（状态列末段「第二段续派口径」）／`#584`。

## 一、本批是什么

| 泳道 | 承接行 | 一句话 | 触碰区 |
|---|---|---|---|
| A1 | `#583` | P5 第二段：带 superpowers 重测底噪后瘦身 CLAUDE.md／skill 描述／UPS 注入 | CLAUDE.md、skills源码 frontmatter、UPS hook |
| A2 | `#584` | 无头 SessionStart 不触发取证修复＋worktree 产出兜底＋插件加载探针 | sessionstart hook、v2.ps1、包版本探针、opener骨架.md |

触碰区零交集。

## 二、硬边界（两条泳道同受）

- 🔴 只 push 本泳道分支，不 ff master——ff 属 🟡。
- 🔴 不碰 `工具-落库sweep.py`、`.51`、企微机器人、定时任务运行时；不改全局 `~/.claude/`；不停用任何插件（`superpowers` 他已明示必须保留）。
- 🔴 对照实测只用 `--model haiku` 最小会话，每条泳道 ≤ 6 次。
- 🔴 主仓内禁用 `git reset --hard`／`checkout --`／`clean`；任何切目录后的 git 写操作先断言 `git rev-parse --show-toplevel` 是目标 worktree（09-16 事故，项目记忆 `feedback_git_cd_guard`）。

## 三、泳道

### A1 · §一 #583 开场底噪第二段

▶ 粘贴端：CC ｜ 泳道：op0916g-baseline-slim-583

```
[OP-0916-G]【CC】开场底噪第二段瘦身
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0916g-baseline-slim-583`） ｜ worktree：☑（op0916g-baseline-slim-583，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token优化P5续与584
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916G-底噪瘦身二段。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #583（`python 0-学习与工具/工具-队列查询.py --row 583 --section 一 --field all`，做什么／不做什么／收工全在该行，以状态列末段最新口径为准）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（判据已写死），无需再问澄清，直接开工。
🔴 先重测基线（superpowers 已加载）并回写，再动 CLAUDE.md（走编辑锁，常驻纪律与路由表一条不删）与 skill description（触发词迁正文不丢）。
🔴 一切 `reports/` 产出写**主工作区**（`git rev-parse --git-common-dir` 的上级目录），不写 worktree 内 `reports/`——09-16 已两次随 worktree 删除丢失。
🔴 测试一律前台跑、`pytest -q --tb=short`，跑完即 commit，严禁放后台等通知。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916g-baseline-slim-583 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916g-baseline-slim-583 --done --batch B-0916_Token优化P5续与584 --text "产出落点：<落点>"`。
🔴 收工只 push 本泳道分支，不 ff master；在 `合入登记/pending-ff.jsonl` 登记待授权合入行；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；**非本行口径内的新发现只登记、不作为停工理由**。
```

### A2 · §一 #584 无头开场注入／产出兜底／插件探针

▶ 粘贴端：CC ｜ 泳道：op0916h-headless-parity-584

```
[OP-0916-H]【CC】无头开场注入与产出兜底
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0916h-headless-parity-584`） ｜ worktree：☑（op0916h-headless-parity-584，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_Token优化P5续与584
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916H-无头环境对齐。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #584（`python 0-学习与工具/工具-队列查询.py --row 584 --section 一 --field all`，做什么／不做什么／收工全在该行，以状态列末段最新口径为准）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（判据已写死），无需再问澄清，直接开工。
🔴 先取证定因并回写，再按因修；v2.ps1 改动须保留 `#581` 的模型字段逻辑，差集回归与 master 对跑。
🔴 一切 `reports/` 产出写**主工作区**（`git rev-parse --git-common-dir` 的上级目录），不写 worktree 内 `reports/`——09-16 已两次随 worktree 删除丢失。
🔴 测试一律前台跑、`pytest -q --tb=short`，跑完即 commit，严禁放后台等通知。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916h-headless-parity-584 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916h-headless-parity-584 --done --batch B-0916_Token优化P5续与584 --text "产出落点：<落点>"`。
🔴 收工只 push 本泳道分支，不 ff master；在 `合入登记/pending-ff.jsonl` 登记待授权合入行；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；**非本行口径内的新发现只登记、不作为停工理由**。
```

## 四、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】Token优化P5续与584两泳道-2026-09-16.md" -FullAuto -Yes -Detach
```
