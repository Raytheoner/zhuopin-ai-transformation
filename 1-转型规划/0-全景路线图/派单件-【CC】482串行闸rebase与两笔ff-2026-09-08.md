---
title: "派单件 ·【CC】#482 串行闸 apply rebase 到 master ＋ 两笔 ff（OP-0908-W）"
created: 2026-09-08
status: 待粘贴
派出线: CC OP-0908-K（#505 高水位线回写，propose+design 已出件）
承接: 队列 §四 #177（#505 apply 的唯一剩余前置）＋ §一 #482 ＋ §一 #505
---

# 派单件 ·【CC】#482 rebase ＋ 两笔 ff

## 〇、为什么要单独派这一棒（读派单件的人先看这段）

`#505`（`append-row` 高水位线回写）的 design 审已于 2026-09-08 通过（Shao Peishen 答 `1a／2a／3a／4a／5a`），**apply 只剩一个前置：`#482` 串行闸 apply 合入 master**（他答 `5a` 定的顺序）。

**但 `#482` 不能直接 ff** —— `OP-0908-K` 实测：

```
$ git merge-base --is-ancestor master origin/claude/op0907aj-serial-gate-482-apply
exit=1                     ⇒ master 不是它的祖先

$ git rev-list --left-right --count master...origin/claude/op0907aj-serial-gate-482-apply
75      2                  （左＝master 独有，右＝482 分支独有）
```

该分支**落后 master 75 个提交**。仓库是 **ff-only 策略**（`.claude/rules/两桌同步与取证.md` §二），所以「ff `#482`」实际是**「先 rebase 到 master，再 ff」**两步 —— 而 75 个提交的漂移意味着 rebase 可能有冲突、且它自己的单测必须在新基线上重跑，这是真活，不是一条命令。

**为什么不是 `OP-0908-K` 自己做**：该分支属 `#482` 线，rebase＋force-push 属他线触碰区；且 `OP-0908-K` 会话内 `git push`／ff 被 auto mode classifier 拦（Bash 与 PowerShell 两入口同拦），做不了。

## 一、🟡 授权状态（**执行方不要再问他一次**）

- **`#482` 合入 master 的 🟡 字母：已给** —— Shao Peishen 2026-09-08 答 **`5a`**＝「先 ff `#482`、`#505` apply 排其后」。`#482` 行内原写的「ff 仍 🟡」由此解除。
- **`#505` propose+design 那一笔合入 master 的 🟡 字母：已给** —— 他同日答 **`1b`**＝「现在就 ff」。
- ⚠️ **仅此两笔**。本棒不得顺手 ff 任何第三条分支。

## 二、【设置】与开场

▶ 粘贴端：**CC（新开）**

▶ 首次派出：[OP-0908-W]

```
[OP-0908-W]【CC】482rebase与ff
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0908w-482-rebase-ff`）｜ worktree：☑（482-rebase-ff，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：CC OP-0908-K
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0908W-482rebase与ff。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/派单件-【CC】482串行闸rebase与两笔ff-2026-09-08.md` 全文（授权状态、做什么、不做什么、验收全在其中）→ ② `CLAUDE.md` §3／§5 与 `.claude/rules/两桌同步与取证.md` §二 恢复上下文，按该件执行。本件为 A 类（两笔 ff 的 🟡 字母均已给，顺序已定），无需再问澄清，直接开工。

做什么：
1. 前置只读核对：`git merge-base --is-ancestor master origin/claude/op0907aj-serial-gate-482-apply`（应为假）＋ `git rev-list --left-right --count master...origin/claude/op0907aj-serial-gate-482-apply`（应约 75/2）。若已变成可直接 ff ⇒ 跳过 rebase 直接做第 3 步，并在收工汇总写明是谁先合的。
2. rebase：把 `claude/op0907aj-serial-gate-482-apply` rebase 到当前 master，解冲突。🔴 冲突面预判＝`0-学习与工具/工具-共享文档编辑锁.py`（其改动集中在跟进信函数 `_followup_status_is_closed` 附近／`_followup_readme_rows_advisory`／`_validate_followup_readme_release`）与 `0-学习与工具/test_工具-共享文档编辑锁.py`（+378 行，尾部追加）。解冲突以**保留双方语义**为准，任何一处判据被删都要在收工汇总逐条写出来。
3. 验收后才 ff：rebase 完成后先跑 `python -m pytest 0-学习与工具/ 5-平台底座/`（或该分支 tasks.md 指定的范围），**全绿且零回归才 ff**；不绿即停，把失败项写进 §一 `#482` 状态列并报，**不要为了 ff 而改测试**。
4. 两笔 ff（严格按此序，一次一条，每条 ff 后 push master）：⑴ `claude/op0907aj-serial-gate-482-apply` → master；⑵ `claude/op0908k-505-hwm-835a34` → master（后者只含 4 份全新 openspec 文件，与前者零交集，正常无冲突）。
5. 收工回写：走锁改 §一 `#482` 状态列（rebase 后 commit、单测结果、已 ff）＋ §一 `#505` 状态列（前置已解除、apply 可开工）＋ §四 `#177` 标结案；登记 §二 批次。另：`1-转型规划/0-全景路线图/队列回写待补/B-0908_K-sec2-待ff合入master后再登.json` 挂的就是 `#505` 那笔的 §二 批次，第 4 步⑵ 做完后照该 json 内容补登 §二 并删该 json。
6. 收工汇总末附「待你一字母」清单与答复模板；若 rebase 有任何语义取舍，该取舍必须作为定夺项列出，不得自行拍板。

不做什么：
- 不 ff 第三条分支；不 force-push master；不改 `#482` 与 `#505` 两个包的 design 决策（均已审过）。
- 不实现 `#505` 的 apply（那是另一棒，本棒只解前置）；不碰 `工具-落库sweep.py`（`#500` 在跑的泳道触碰区）。
- 不为了让测试通过而修改断言或跳过用例。
```

## 三、执行方可能撞上的两件事（先写在这，省一轮）

1. **`git push`／`merge` 被 auto mode classifier 拦** —— `OP-0908-K` 实撞，Bash 与 PowerShell 两入口同拦。你若也撞上：**停下报 Shao Peishen**，不要换第三个入口。解法是他在交互式 `claude` 终端里跑 `/permissions` 加一条 Bash 放行规则（本项目的桌面版 Code 标签页里 `/permissions` 不可用）。
2. **主 checkout 工作区当前为脏** —— `1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md` 等待 `ZhuopinCommitSweep` 落库，`队列回写待补/` 下有 14 件他线挂起文件。ff 前先确认这些脏文件与你要带入的文件**无交集**（`#482` 与 `#505` 两笔均不触碰队列文件），有交集即停。

## 四、验收（缺一不算完）

- [ ] `git merge-base --is-ancestor origin/claude/op0907aj-serial-gate-482-apply master` ＝ 真（`#482` 已在 master 上）
- [ ] `git merge-base --is-ancestor origin/claude/op0908k-505-hwm-835a34 master` ＝ 真（`#505` propose+design 已在 master 上）
- [ ] rebase 后全量单测绿、零回归，结果数写进 `#482` 状态列（**实测数，不引用未复跑的旧数**）
- [ ] `openspec validate --all --strict` 仍绿
- [ ] §一 `#482`／`#505`、§四 `#177` 三处已走锁回写；§二 批次已补登、`B-0908_K-sec2-待ff合入master后再登.json` 已删
- [ ] `#505` 的 apply 前置已解除，可另派一棒
