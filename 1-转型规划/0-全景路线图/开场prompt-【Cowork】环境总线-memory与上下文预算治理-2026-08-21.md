---
status: 待执行
title: "开场prompt ·【Cowork】环境总线 —— memory 机制与开场上下文预算治理"
created: 2026-08-21
执行方: Cowork 环境总线
派出线: Cowork 全景路线图线
编号: OP-0821-B
来源: Shao Peishen 2026-08-21 ——「我大概一两个星期内在 project_memory_* 的会话里做一轮本机 Claude memory 机制审核…它的安排我认为不一定最佳」＋「另一个症状是新会话没互动几轮，context 的 token 值就很大了，可能无效信息载入过多」
---

# OP-0821-B ·【Cowork】环境总线 —— memory 机制与开场上下文预算治理

> **转场类别 ＝ B（跨线交接：全景路线图线 → 环境总线）** ⇒ 开工前允许并鼓励提 2-3 个澄清问题，**不必自检三查后直接开工**。

## 开场词（复制即用）

```
【OP-0821-B】环境总线·memory与上下文预算治理
【会话名】OP-0821-B 环境总线·memory与上下文预算治理
【设置】执行环境：Cowork ｜ CC session：☐ 不适用 ｜ worktree：☐ 不新建（主工作区）｜ 分支：master ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 全景路线图线

读 1-转型规划\0-全景路线图\开场prompt-【Cowork】环境总线-memory与上下文预算治理-2026-08-21.md 全文 ＋ 1-转型规划\0-全景路线图\memory与CLAUDE配置审核输入件-2026-08-21.md 全文 ＋ 1-转型规划\0-全景路线图\memory体系-全面审核与优化-2026-08-16.md（上一轮审核的产出件，本次要复核它）。

本任务是 B 类跨线交接，开工前先提 2-3 个澄清问题再动手。
```

---

## 一、这次要治的是两个症状，但它们很可能是同一条因果链

**症状甲 · memory 写不进**：本地 Cowork 会话往 memory 写入**报成功、实际没落盘**，且**无 `project_memory_*` 工具**、`Read` 被拒 ⇒ 连回读验证都做不了。
**症状乙 · 开场 token 就很大**：Shao Peishen 原话——「**新会话没互动几轮，context 的 token 值就很大了，可能无效信息载入过多**」。

🔴 **本线的判断（供你验证或推翻，不要当结论直接采信）**：两者是一条链，不是两件事——

> **memory 那条分流通道对本地会话是坏的（且坏得没有信号）**
> **⇒ 一切要跨会话活下去的东西，只能往「唯一确定会被读到」的载体挤，也就是根 CLAUDE.md**
> **⇒ 根 CLAUDE.md 膨胀到 61,159 字符（粗估 ~55k tokens），其中顶部流水账占 43.1%**
> **⇒ 真正的规则被挤到注意力边缘**
> **⇒ 规则失效**

**最后一环有活证据**：2026-08-20/21 查「专员性别纠正 ≥5 次仍复发」，根因正是**全库没有任何载体记录过性别**——而本该承接这类事实的 memory 写不进去。**同一天，我按系统提示往 memory 写九人名录，工具报 success，本机 `Test-Path` 是 `False`。**

⇒ **若只治乙不治甲，流水账迁走之后还会长回来**，因为没有别的地方可去。

---

## 二、已经查清的（不必重查，直接用）

### 2.1 上一轮审核的定位（本线 2026-08-21 已取证）

- **审计本体在 Cowork 一端**：产出件 `1-转型规划/0-全景路线图/memory体系-全面审核与优化-2026-08-16.md`，其 frontmatter 明写 `执行方: Cowork（Shao Peishen 2026-08-16 直派）`、「改动全部落在 Cowork 云端 memory 层」。
- **CC 一端只做仓库侧 P1/P2 与队列登记**，session 可精确定位：编辑锁 `进度编年-CHANGELOG.md.editlock` 的 `who` ＝ `CC-memory体系P1P2-0816`；transcript ＝ `C:\Users\Paul Shao\.claude\projects\C--Users-Paul-Shao-OneDrive-Projects---AI--\c3c4d00e-a9c9-4c32-82f2-21f143f156bd.jsonl`（首条 user 2026-08-16 11:09:17Z）。
- 🔴 **Cowork 那个 session 本身查不到**：`list_sessions` 全部 105 个本地 session 按末次活动排序做日期夹逼，**08-15 与 08-16 之间没有任何 session**；本机 `AppData\Local\Claude-Data` 为空，**Cowork transcript 不落本机盘**。⇒ **只能说「指向不是本地 session」，不能坐实。** 这正是本次第一个要验证的点。

### 2.2 那次到底做了什么（五问已答）

| 问 | 答 |
|---|---|
| 启用了机制，还是只重建索引？ | **只修可见性层**——索引 6 行 → 34 行（覆盖 42 件）、新增《memory 体系地图》、刷新项目快照。自陈「**未删任何文件（工具无删除能力，以索引降级代替）／未新建仓库内任何载体**」。它认为写入通道本来就健康，不需要"启用"。 |
| 「42 件」怎么来的？ | 报告只写「全部 42 件逐一读过后分类」，**没记录列举命令或其输出**。可交叉验证：今天注入本会话的索引逐条点名的文件数**正好 42**。 |
| 有没有说「本地/云端 session 同一本」？ | **报告正文没有，且方向相反**——它把本机层写作「已不被云端 session 使用」，明确是两份。那句话**只存在于 `reference_memory_system.md` 的索引描述里**，该件在云端、本地会话读不到正文，**其依据查不到**。 |
| 本机那 2 个旧文件怎么处置？ | **保留不动**，仅标 ⚰️ 半死，未删、未登记清理待办。今天实测仍在且逐字一致。 |
| `#254` 阶段 1 验收窗口？ | **至 2026-08-30**，三信号（称呼纪律零违反／新写记忆都带索引行、差集＝0／无 session 再引用「memory 不可信」旧结论）。**今天 08-21 未到期，但未见任何一方在跑这三项抽查。** |

🔴 **它当时没有考虑「本项目几乎全是本地 session」这个前提** —— 报告全文未讨论"哪种会话类型能写"，§三.5 只把 PowerShell 反查恒 False 归因于"存储在服务端"，**没有由此推到"若会话根本没有那套工具会怎样"**。这一整段被跳过了。**这正是 Shao Peishen 说「它的安排不一定最佳」的具体所指。**

### 2.3 今天新测出的硬数（`memory与CLAUDE配置审核输入件-2026-08-21.md` 有完整版）

| 项 | 实测 |
|---|---|
| 本机 memory 目录 | 唯一 1 个，**仅 2 个文件**，最新 **2026-07-27**，近一月零新增 |
| `MEMORY.md` 本机文件 | **全盘零命中**（而每会话被注入一份 40+ 条索引 ⇒ 来自服务端） |
| 往 memory `Write` | 报 `File created successfully`，**`Test-Path` ＝ `False`** |
| 同路径 `Read`/`Edit` | 报 `outside connected folders`（**读写不对称**） |
| `project_memory_*` 工具 | **本会话不存在**（ToolSearch 只返回 console／剪贴板／widget／transcript／Drive） |
| 项目 CLAUDE.md | **61,159 字符 ／ 122,702 字节 ／ 粗估 ~55k tokens** |
| └ 顶部「当前进度」段 | **26,279 字符 ／ 43.1% ／ 粗估 ~24k tokens ／ 15 条条目** |
| └ §1-§7 规则正文 | 34,685 字符 ／ 56.9% |
| 全局 `~\.claude\CLAUDE.md` | 5,390 字符 ／ 粗估 ~5k tokens |
| 场景级 `CLAUDE.md` | 14 份（按需读，本次未审） |

⚠️ **粗估 token 用「字符数 × 0.9」，不是精确值**，量纲对了即可，**不要拿它当承诺数字**。

---

## 三、开工先做这三件验证（做完再谈治理方案）

**① 一句话把「矛盾」还是「并存」判死。**
在一个**带 `project_memory_*` 的会话**里（claude.ai 网页版 Cowork，或你能找到的那种）跑一次 `ToolSearch` 检索 memory。
- 有 → 结论是「**并存**，各自成立于不同会话类型」，08-16 那份报告没错、只是漏了前提；
- 没有 → 结论是「**能力随时间变了**」，08-16 之后某个时点写权限消失了（旁证：本机旧层最后写入恰好停在 07-27）。

**② 在本地会话里跑一次完整的「写入 → 回读」闭环，验证静默失败是否 100% 复现。**
`Write` 一个探针文件到 `spaces\...\memory\` → 立刻本机 PowerShell `Test-Path` → 再开一个**新的本地会话**看注入的索引里出不出现。**今天已中一次，需要确认是必然还是偶发。**

**③ 数一次「注入索引」与「本机真实文件」的差集。**
注入索引点名 **42 件**，本机只有 **2 件**且**都不在索引里** ⇒ 直接坐实：**「索引维护三步（写文件 → 写索引 → 回读）」在无写工具的会话里无法执行**。
⇒ 由此推出一个**必须在 08-30 前定的事**：**`#254` 阶段 1 的验收信号 ⑵「新写记忆都带索引行、差集＝0」目前没有任何会话能验** —— 这条判据还算不算数？

---

## 四、治理范围与红线

**在范围内**：memory 载体的选型与分工（哪些事实该进 memory、哪些该进 CLAUDE.md、哪些该进队列）；开场上下文预算（CLAUDE.md 瘦身、进度段迁移机制化）；`#254` 阶段 1 判据的存废；本机 2 个陈旧文件的处置。

🔴 **红线（四条，不得越过）**：
1. **不得删除任何 memory 件**——上一轮已明记「工具无删除能力，以索引降级代替」；本轮同理，**降级可以，删除要 Shao Peishen 拍板**。
2. **不得改根 CLAUDE.md §1-§7 规则正文的实质内容**——那是两桌全局纪律，改它须走登记与拍板。**瘦身只动顶部进度段**。
3. **不得凭 08-16 那份报告的结论直接行动**——它是本次的**复核对象**，不是依据。
4. **凡「载体里没有的事实」，问一次并当场落档，不得推断**（根 CLAUDE.md §1，2026-08-21 立）。

---

## 五、已知的坑（会绊住你的）

1. 🔴 **bash 沙箱 grep 本仓库极慢会超时** —— 仓库在 OneDrive 上，用本机 PowerShell `Select-String`，快几十倍。
2. 🔴 **`git status` 从 bash 侧跑会把整库报成 `M`**（Linux 读 Windows checkout，CRLF 全行重写）。**工作区脏净一律以本机 PowerShell 为准**；沙箱 git 只能用于 `log`／`rev-list` 这类不看工作树的操作。
3. **Cowork transcript 不落本机盘**，`list_sessions` 只能看到本地 session；想追溯网页版会话得换别的路。
4. **`Write` 到 memory 目录会静默成功** —— **任何"我已经写进 memory 了"的自陈，一律用本机 `Test-Path` 复核后才算数。**
5. **CLAUDE.md 顶部进度段那条「只留最近一批」的规则已经失效**（现 15 条、43.1%）—— **它本身就是"人守规则无机制即失效"的活例**，治理方案要正面回答"这次怎么不重蹈覆辙"。

---

## 六、可预期的定夺项（先给 Shao Peishen 打个底，不要你现在答）

1. `#254` 阶段 1 的三条验收信号，其中 ⑵ 若无会话能验，**是改判据、还是判阶段 1 失败**（08-30 到期）。
2. 本机那 2 个陈旧文件：**清理／保留并标记／迁进仓库**。
3. CLAUDE.md 顶部进度段的最终归宿：**继续留在文件里靠人守迁移／整体移出改为按需读的指针／做成 lint 判据强制**（第三条与 §一 `#351` 子项 ⑷⑸ 同族，都是"人守该机制化"）。
4. **若结论是"本地会话本就该只读 memory"**，那么系统提示里那段「直接用 Write 写 memory 目录」的指引就是错的 —— **要不要作为产品反馈提出去**。

---

## 七、纪律段（照做，不必重读模板库）

- **编辑锁**：改队列前 `acquire`、改完立刻 `release`；🔴 **`acquire` 必须在任何写盘之前**；`acquire` 与写盘两步中间验 `$LASTEXITCODE`，**不要接管道**。
- **取号**：`--reserve` 必须在 `acquire` 那一次就带上；**取号前先做协议〇.10 并入审核**——本线今天两次靠它省掉翻墙（`[D:机]` WIP 现 **22/22 满格**，新立 `[D:机]` 行会被 `release` 硬阻断）。
- **裸竖线**：cell 内严禁 `|`；PowerShell 传参用**单引号**。
- **文件清单**：每条必须是**仓库根相对完整路径且反引号包裹**。
- **含 Windows 路径的正文用 r-string，落盘后扫一次控制字符**。
- **日期每次写入前重取** `Get-Date -Format 'yyyy-MM-dd'`，不得沿用会话早先取值。
- **定夺项路由**：本 session 接着就做 ⇒ ⟨就地答⟩给选项；要另起 session／归他线／对外 ⇒ **不给选项，只写「已登记 §四 #N」**；报告顶部加一行汇总。**默认项两前提**：有执行者在场 ＋ 代价是「停在原地」而非「持续发生」，任一不满足则**不设默认项**。

## 八、收工段

- **滚动更新** `1-转型规划/0-全景路线图/session接力-Phase1收口.md`（新增日期节）。
- **不自行 commit**：列出本次全部新产出/修改文件清单 ＋ 建议 commit message ＋ **登记队列 §二 待 commit 批次**，提示 Shao Peishen 一句话交 CC。
- **登记后立即** `Start-ScheduledTask -TaskName "ZhuopinCommitSweep"`；**批次转 ✅ 后还要看 `git status` 是否真干净**（本机 PowerShell），两者不一致即有声明未被匹配。
- ⚠️ **§二 批次号目前是裸奔的**（`append-row --section 二` 不预留也不校验重名，同日已重名两次，已并入 §一 `#351` 子项 ⑸）——**取批次号前先扫一眼 §二 有没有同名**。
