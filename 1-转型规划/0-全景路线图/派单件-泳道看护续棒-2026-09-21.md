---
title: 派单件 · 泳道看护续棒（承接 `OP-0920-B` 收工）
created: 2026-09-20
派出线: 环境总线 OP-0920-B 收工转场
任务类: A 类
---

# 派单件 · 泳道看护续棒（承接 `OP-0920-B`）

> 首要输入：本件 ＋ `1-转型规划/0-全景路线图/session接力-Phase1收口.md` ＋ `CLAUDE.md`（§3／§5）。
> 🔴 接棒第一件事按接力卡开头三行做，**不要再额外拉全量摘要**。

## 〇、开工前必过「方案闸」（接力卡 §〇）

本棒方案已定好，照做即可：① 判据只取结论性清单，禁 `cat` 补丁／日志正文；② 扫池只用 `--digest --actionable`；③ 凡触碰 git 一律本机侧跑；④ 长跑段只看哨兵、**起跑前先删旧 `.done`**、判完成比 mtime；⑤ 判两处改动重不重叠先 `git diff --stat`／`--unified=0`。

## 一、做什么（按优先级）

### ⑴ 🔴 `#627` 的**效果验收**——本棒第一件，不做完不开别的

`#627` 的三道闸 2026-09-20 晚已 ff 进 master 并逐项 `git show master:<file>` 实证（详见接力卡 §二），**但效果没验**：那一批跑的仍是旧 ps1。本棒起的**第一批泳道就是验收样本**：

- 起批后用 `python 0-学习与工具/工具-Token用量度量.py --since <当日> --until <当日>` 出各泳道**上下文峰值／请求数／`cache_read`**，与 2026-09-20 基线逐条对照并写进收工件。基线：上午七条峰值 244,434／228,308／213,218／194,282／163,826／155,660／151,036，`cache_read` 5.02M–15.48M，请求 47–98；下午 `d8a76b23` **257,399／106／17.44M**（已越 250k 硬线），`86ee819b`（4.5 KB 薄派单件）**167,792／46／5.24M**。
- **判据三条**：① 有没有泳道真的被 250k 硬线截断并按 `OPENER_PARTIAL: 上下文转场` 记账；② `summary.txt` 的「上下文峰值」列与「本批越 150k 的泳道 N 条」汇总行是否真的出现；③ 峰值分布相对基线有没有下移。**三条都要给实测数，不许自陈。**
- 🔴 **看护件按泳道切片**（实测 4.5 KB vs 24.9 KB），opener 的 `--input-pointer` 指向薄件；**续尾一律新开 session ＋ 薄续棒件，禁 `--resume`**。

### ⑵ `#629` 必读件缩编（已吸收 `#630`）

`zhuopin-lane-watch` 正本 44,445 B ＋ `.claude/rules/` 合计 43,388 B（超 12,668 B）＋ 根 `CLAUDE.md` §3 转场自检改「只在阳性时提醒」（属减字）。🔴 **结构性迁出、不是删**——照根 `CLAUDE.md` 13,148→12,110 B 那次手法，成因／沿革迁 CHANGELOG，正文只留「触发→动作→判据／指针」。🔴 **同批必须加一道有消费者的体量守**（落点建议 sweep 常驻告警类，形状照第 19 类），否则必再胀回来。同批出 Win 侧尖峰榜，分清「CC 按 paths 自动注入的真常驻」与「一读一整份的尖峰」。

### ⑶ `#632` 纯 master 16 条 pre-existing 失败逐条分诊

`test_工具-落库sweep.py` 在纯 master 上 16 failed（`DeploymentTraceHintTests` 3／`OrphanFileAlertTests` 4／`OrphanResolvedNotificationTests` 2／`ResidentServiceDeploymentHintTests` 3／`ExactMatchEndToEndTests` 1／`OrphanSectionFourEscalationTests` 1／`ScenarioSpecGapAnnounceIntegrationTests` 1／`StaleChangeAnnounceIntegrationTests` 1）。后果是**六关④ 对这 16 条覆盖的行为形同虚设**。逐条判真失败／夹具腐坏／已废弃，修或删并写明理由。属大任务，先过方案闸。

### ⑷ `#595` 企微机器人断连根因回查

⑷ 已认失败（他 2026-09-20 答 `1a`），观察窗 17 次 vs 前 24h 15 次不降反升，「重启当晚重连震荡」仍只是推测。三个可查方向已写在行内。

## 二、不做什么

1. 🔴 **别往 `.claude/rules/` 加字**（超 12,668 B）；**别往根 `CLAUDE.md` 加字**（余 178 B）——`#629` 那一棒除外，且那是**减字**。
2. 🔴 **别碰卡在他一字母的行**：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
3. 🔴 **CLAUDE.md §5 常设禁区照旧**：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 部署及生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）**永不代办**。
4. 🔴 **不得用更松口径覆盖一道已经正确拒绝过的闸**，不得起接管脚本自动绕闸。
5. **没修好不报中间态**（他 2026-09-20 直令）：彻底修好并验收通过后再出声转场；只有真卡住需他一字母时才中途打断。

## 三、答复格式

收工答复末尾给**填好推荐项、一次粘走的单个代码块**，块内只放他要回复的字符本身。告知类用普通文字。
