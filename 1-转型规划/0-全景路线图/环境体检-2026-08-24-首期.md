---
title: 环境体检-2026-08-24-首期
status: 已执行
owner: Cowork 环境总线
date: 2026-08-24
---

# 月度环境体检 · 首期（队列 §一 #98）

> **触点与提前量**：本行原定触点 **2026-08-28（8 月最后一个周五）**，本轮按《本周计划-2026-08-24》A6 派单**提前 4 天执行**。提前不影响四面取证的有效性（全部为当刻实测），但**下列两项在 08-28 前若发生变化须复测**：⑴ worktree 台面（会随 CC 开工新增）；⑵ `.51` 四服务可达性（本轮在 LAN 内，离网时结果不同）。
> **第 0 期基线**＝`环境体检-2026-07-30-第0期基线.md`。本件按基线 §七 逐面复测，**只记差异与新发现**。
> 🔴 **本轮为巡检，不做任何删除／清理动作**（守 A6 派单「巡检不删除，处置建议出行交 CC」）。所有处置建议在文末汇总，已并入队列 §一 #98 行内。
> **时间基准**：本件所有时刻若标 `Z` 为 UTC，否则为本机本地时间（UTC+8）。执行时刻＝2026-08-24 19:12–20:0x 本地。

---

## 〇、一句话结论

**四面全部取到证，无数据风险，但抓出 5 项此前无人知晓的结构性问题**——其中 **3 项属于「一个用来判断环境状态的读数不可信」**（与本行已并入的两项同族），**1 项是一个每月例行的检查任务本身已静默停摆近两个月**，**1 项是部署惯例的存量违例**。**没有任何一项会丢数据；但它们共同的形态是「不产生信号」。**

---

## 一、worktree 台面与孤儿分支扫描

### 1.1 三方计数（当刻实测）

| 口径 | 数量 |
|---|---|
| `git worktree list` 注册的 linked worktree | **19** |
| `.git/worktrees/` 元数据目录 | **20** |
| `.claude/worktrees/` 物理目录 | **44** |

🔴 **本行历史上写死过 `3/3/3`→`2/2/2`→`1/1/1`→`8/8/8` 四组数字，全部过时**。本轮按本行自己 2026-08-05 给出的建议改口径：**不写死数字，只写三者应满足的关系**——

> **判据（此后照此用，不再写具体数）**：`注册数 == 元数据数`，且 `物理目录数 - 注册数 == 空壳数`，且**空壳全部为 0 文件**。三条同时成立即台面健康。

本轮实测：`19 ≠ 20`（差 1，见 1.2）；`44 - 19 = 25` 个未注册目录，其中 **24 个是 0 文件空壳**、**1 个有 1780 个文件**（见 1.2）。**⇒ 三条判据中两条不成立。**

### 1.2 新发现 ①：10 个 worktree 元数据缺 `commondir`，其中 9 个仍在注册表内 —— 「注册着、但根本进不去」

**症状**：对 9 个已注册 worktree 跑 `git -C <目录> status`，全部报

```
fatal: not a git repository: .../.git/worktrees/<名字>
```

且它们在 `git worktree list` 中的提交哈希显示为 **`0000000`**（HEAD 解析不出）。

**根因（已定位到文件级，非推测）**：这些元数据目录**缺少 `commondir` 文件**。健康样本 `fi2-webhook-var-check-bc19fa` 的 `commondir` 内容为 `../..`（6 字节），指回主 `.git`；缺了它，git 无法从 worktree 反向找到主仓库。

| 名字 | HEAD 记录 | 物理文件数 | 最后写入 |
|---|---|---|---|
| `conftest-pytest-regression-c9d07e` | `claude/sc8-batch1-merge-review-3f63f5` | 1603 | 2026-08-11 |
| `gallant-lumiere-b482be` | `claude/b6-parallel-isolation-cure-f96a05` | 1278 | 2026-08-14 |
| `musing-pascal-68d14e` | detached `697da77` | 1515 | 2026-08-10 |
| `observation-window-archive-91b68d` | `claude/observation-window-archive-91b68d` | 1236 | 2026-08-09 |
| `queue-313-prerequisites-7d9565` | `claude/open-pool-kanban-push-40f578` | 1327 | 2026-08-10 |
| `queue-lint-bare-pipe-312efa` | `claude/qdb-quality-gate-review-14d556` | 1469 | 2026-08-18 |
| `queue-table-reachability-2` | `claude/queue-table-reachability-2` | 1302 | 2026-08-09 |
| `recursing-hofstadter-eb7215` | detached `d9dce22` | 1260 | 2026-08-10 |
| `sweep-deadlock-transition-tasks-ba394b` | `claude/queue-315-design-e63c85` | 1639 | 2026-08-10 |
| `platform-bootstrap-apply`（**未注册**，仅剩元数据＋物理目录） | 元数据内连 `HEAD`／`gitdir` 都没有 | 1780 | 2026-08-18 |

**内容风险 ＝ 0（已逐条核实，非推断）**：7 条持有分支的，`git rev-list --count master..<分支>` **全部为 0**（即分支尖端已全部并入 master）；2 条 detached 的，`git merge-base --is-ancestor <HEAD> master` **均为真**（`697da77`＝#322 编辑锁死循环修复，`d9dce22`＝#314③ 回写，均已在 master 内）。

⚠️ **一处如实留白**：**这 9 个目录里有没有未提交的脏改动，本轮查不出来**——查脏必须跑 `git status`，而它们恰恰跑不了 `git status`。**所以清理顺序不能反**（见 1.3）。

🔑 **本项最该记住的一句**：`git worktree list` **照常把它们列出来了**，还带着分支名，看起来一切正常；**只有真去用它才会发现是坏的**。这是「工具静默回退」族的又一个入口——**列表命令读的是元数据的一部分，而可用性取决于另一部分**。

### 1.3 处置建议（交 CC，本轮不动手）

🔴 **顺序不可颠倒**：

1. **先补 `commondir` 恢复可读性**——向每个缺失的元数据目录写入内容为 `../..` 的 `commondir` 文件（**先在 1 个上验证 `git -C <目录> status` 恢复正常，再批量**）；
2. **再逐个 `git status` 查脏**——把有未提交改动的挑出来单独处置（这是本轮查不到的那部分）；
3. **最后才 `git worktree remove` ＋ 删已并入的分支**；
4. `platform-bootstrap-apply` 走同一流程，但它已不在注册表内，**须先判断它的 1780 个文件里有无独有内容**再决定；
5. 24 个 0 文件空壳目录可直接 `Remove-Item`；**连续第三次 `Permission denied` 的，不做第四次盲重试**，改查占用句柄（沿用基线件 §七 第 1 条口径）。

### 1.4 存量注册 worktree 的落后与脏文件（可读的 10 个）

| 名字 | 分支 | 脏文件 | 落后 master |
|---|---|---|---|
| `wecom-service-home` | `ops/wecom-service-home` | 1 | **0** ✅ |
| `op0823e-fi2-dup-and-r5` | 同名 | 0 | 22 |
| `fi2-webhook-var-check-bc19fa` | `claude/op0823g-carrier-health-c533fc` | 0 | 60 |
| `followup-dispatch-apply-25679f` | `claude/op0823f-auto-archive-43fe6b` | 0 | 72 |
| `fi2-zombie-task-f36c31` | `claude/op0823d-reply-match-latest-7cc457` | 0 | 84 |
| `sc2-caseback-fix-6eadad` | detached | 0 | 119 |
| `wip-limit-open-pool-fix-c9d2ca` | detached | 0 | 127 |
| `sc2-caseback-fix-8ede5a` | detached | 0 | 156 |
| `material-board-view-df54d1` | `claude/op0822c-sweep-alerts-b9424a` | 1 | 178 |
| `queue-315-apply-9f2c1a` | 同名 | **13** | **729** |

✅ **一条好消息，且它推翻了一条挂账**：**`wecom-service-home`（常驻服务真实执行体）本轮实测落后 master ＝ 0、领先 ＝ 0**。§四 #68 登记的「`ZhuopinDecisionReminderDaily` 的执行体 worktree 落后 master 246 个提交」**已不再成立**——该挂账可据此复核销号（**本轮只报，不代销**）。

⚠️ `queue-315-apply-9f2c1a` 落后 **729** 提交且有 **13** 个脏文件，是全场最陈旧的活体 worktree；**任何人从它跑 pytest 或读文件，读到的都是 7 月的世界**（同本行已记的 `pip install -e` 漂移陷阱）。建议一并交 CC 判定去留。

---

## 二、常驻服务健康

### 2.1 计划任务真实存活（本机 `Get-ScheduledTask` 直读）

| 任务 | 状态 | 上次运行（本地） | 上次结果 | 下次运行 |
|---|---|---|---|---|
| `ZhuopinAibotDevListener` | **Running** | 2026-08-24 00:18:04 | `267009`（＝任务正在运行中，非错误） | — |
| `ZhuopinCommitSweep` | Ready | 2026-08-24 19:08:38 | `0` | 2026-08-24 19:36:34 |
| `ZhuopinDecisionReminderDaily` | Ready | 2026-08-24 08:30:01 | `0` | 2026-08-25 08:30:00 |
| `ZhuopinFollowupDispatchDaily` | Ready | 2026-08-24 09:30:00 | `0` | 2026-08-25 09:30:00 |

✅ **四个 Zhuopin 任务全绿。**

### 2.2 OP-0822-C 指定的常量对齐核对（本轮必做项）

`工具-落库sweep.py` 的两个常量：

```python
RESIDENT_SERVICE_ENTRYPOINT_GLOBS = ("run-*.vbs",)
RESIDENT_SERVICE_REGISTRAR_GLOBS  = ("register-*.ps1",)
```

实测三个任务的 Action：`wscript.exe` ＋ worktree `wecom-service-home` 下 `5-平台底座/wecom-aibot-service/run-hidden.vbs`／`run-decision-reminder-hidden.vbs`／`run-followup-dispatch-hidden.vbs`。

✅ **对齐，无失配。**

⚠️ **顺带一条观察（只记，不改）**：第四个任务 `ZhuopinCommitSweep` 的 Action 是 `powershell.exe -File "0-学习与工具/run-commit-sweep.ps1"`，**位于服务目录之外，不在上述两个 glob 的覆盖范围内**。这不是失配（这两个常量按定义只管 wecom-aibot-service 这一个常驻服务），但意味着：**改动 `0-学习与工具/run-commit-sweep.ps1` 不会触发任何「需同步/需重启」类告警**。是否要把 sweep 自身的入口纳入同族告警，留作下期议题。

### 2.3 新发现 ②：`Claude-Env-HealthCheck` 已静默停摆近两个月

| 项 | 实测值 |
|---|---|
| 上次运行（本地） | **2026-08-01 17:02:20** |
| 上次结果 | **`2147946720` ＝ `0x800710E0`**（操作员或管理员拒绝了请求） |
| 下次运行 | 2026-09-01 09:00:00 |
| Principal / LogonType | `Paul Shao` / **Interactive** |
| `~/.claude/health-check.log` mtime | **2026-07-02 11:08:19** |

🔴 **两个证据合起来才说明问题**：任务**看上去**每月都被调度（NextRun 正常排到 9/1），但**日志文件自 2026-07-02 起再没被写过**——即 **08-01 那次触发失败、没有产生任何输出，而失败本身没有通知任何人**。

**这个任务负责的三件事因此已两个月无人在做**：① 技能重名检查；② 插件缓存非激活版提示；③ CLAUDE.md 叠加/重复检查。

**成因假说（未证实，如实标注）**：`LogonType = Interactive` 意味着**必须在交互式登录会话中才能运行**；`0x800710E0` 正是这一类条件不满足时的典型返回。08-01 是周六 17:02（并非计划的 09:00），形态像是「登录后补跑、但补跑时条件已不满足」。**验证方法**：手动 `Start-ScheduledTask -TaskName Claude-Env-HealthCheck` 一次，看日志是否被写。**本轮不动手**（属本机工具链，按根 CLAUDE.md §5「中间地带：改本机工具链＝CC」移交）。

🔑 **与 §一 #82 那条教训同形，且更彻底**：#82 是「机制建成 9 天、每天在跑，却一次都没真正发出过消息」；本项是「机制建成后连跑都没跑，而它看起来一直排着班」。**共同点：失败不产生信号，成功与失败在外部观察上长得一样。**

---

## 三、安全挂账进展跟催

### 3.1 §四 #33（git 历史 PII）

**状态无变化，无需动作**：2026-07-24 已拍板「①登记风险接受」——private 仓库、访问面仅本人 session；**复评触发条件是「仓库开放协作或对外」**，本轮实测该条件未发生。

### 3.2 `.51` 四服务鉴权 —— ✅ **实质进展，且是本轮意外撞见的**

本轮为核定「仓库外载体」第 ② 类而真联网拉取四个服务首页，结果：

| 端口 | HTTP | 长度 | 内容 |
|---|---|---|---|
| 8091 | 200 | 1616 B | **访问口令页**（`<title>访问口令 · 成品保供预警看板</title>`，表单 POST `/_gate/login`） |
| 8092 | 200 | 1610 B | 同形态 |
| 8093 | 200 | 1614 B | 同形态 |
| 8094 | 200 | 1624 B | 同形态 |

⇒ **「四个平级服务零鉴权绑 `0.0.0.0`」这个原始安全挂账，在四个端口上均已由访问口令门（登录后 30 天本机免登录）覆盖。** 这与 `openspec/specs/platform-service-auth-gate` 的存在互相印证。

⚠️ **但它同时制造了本轮的新发现 ③**（见 4.2）——**一个安全改进，让一个巡检工具变成了永远返回零的空转工具，而没有任何人被告知。**

---

## 四、并入项 A：仓库外载体旧指针核查（63/67 处按载体分组逐类核定）

**执行命令**：`python 0-学习与工具/工具-仓库外载体扫描.py "跨桌任务队列.md"`（本轮 **去掉了 `--skip-http`**，四服务在 LAN 内可达）。

**本行的硬要求是「每一类的零命中都要独立证伪」。下面逐类给出证伪结果。**

### 4.1 ① Cowork artifacts —— 命中 **67** 处（此前记录为 63，已增长）

按载体形态分组：

| 分组 | 处数 | 性质 |
|---|---|---|
| `*/index.html`（**活载体**，真正会被执行的那一份） | **2** | ✅ **均为注释，且内容正是「别读那个文件」的告警** |
| `*/versions/*.html`（历史版本快照） | **65** | 不可变历史，**按定义不该改** |

**两处活载体明细**（原文）：

- `zhuopin-project-status/index.html:136`：`// 原实现读的 跨桌任务队列.md 自 2026-08-11 11:37 起只剩 1237 字节的**指针文件**…`
- `zhuopin-todo-center/index.html:78`：`// 跨桌任务队列.md 自 2026-08-11 11:37 起只剩 1237 字节的指针文件，^## 一 等标题一个都不在…`

⇒ **① 类核定结论：活载体旧指针 ＝ 0 处。67 这个数字的 97% 来自 `versions/` 历史快照，是噪音，不是欠账。**

🔑 **本项本身就是一个「数字会吓人但没有信息量」的例子**：本行挂账时记的是「63/67 处待核」，读起来像 63 处待修；**真实待修是 0 处**。**建议此后该工具按「活载体／历史快照」分列输出**（已列入处置建议）。

### 4.2 ② `.51` 四服务页面 —— 命中 **0** 处，🔴 **但这个零是假零，且假得比 08-11 那次更隐蔽**

**证伪过程**：本轮对四个端口各做一次真实 HTTP GET，**全部 200**——单看这个，工具"确实读到了页面"。**但页面只有 1.6 KB，且四个页面长度几乎相同**；取回原文一看，**全部是访问口令登录页**（见 3.2）。

⇒ **工具扫的是登录页，不是看板正文。它此后永远命中 0，与页面里写了什么无关。**

🔑 **这一条比 08-11 那次「误记为零命中」严重**：那次是**人**没逐类核对；**这次是工具真的跑了、真的返回 200、真的报了零，而结论仍然是错的**。**判据没坏、目标变了，而判据不知道目标变了。** 这正是本行两个已并入项共同的命题（「一个用来判断环境状态的读数不可信」）的第三个实例，**也直接回答了本行那个悬着的问题「剩余命中里是否还有第 5、第 6 个受害载体从未核过」——有，而且② 与 ③ 两整类都从未被真正核过。**

### 4.3 ③ 已安装版 skill —— 命中 **0** 处，🔴 **同样是结构性假零**

**证伪过程（阳性对照法）**：换用一个必然存在于项目 skill 里的关键词 `跨桌任务队列-机制环境.md` 重跑同一工具、同一路径 —— **③ 类仍然 0 命中**，而同轮 ① 类命中 12 处、④ 类命中 2 处。⇒ **不是关键词的问题，是这一类路径下根本没有本项目的东西。**

**直接清点**：工具扫描路径 `C:\Users\Paul Shao\.claude\skills\` 下**只有 2 个子目录**——`download-images-skill` 与 `setup-sound-notifications-windows`，**与本项目无关**。

**本项目的 6 个 `zhuopin-*` skill 在哪**：全盘定位只找到**源码**，位于仓库内 `0-学习与工具/skills源码/`（`zhuopin-followup-letter`／`zhuopin-kickoff-prompt`／`zhuopin-queue-audit`／`zhuopin-rebaseline`／`zhuopin-send-followup`／`zhuopin-需求grill`）。**本机文件系统上不存在任何一份「已安装版」**——它们安装在 Cowork（claude.ai）侧，不落本机磁盘。

⇒ **③ 类按当前实现永远核不了本项目真正在用的 skill。** 而这一类恰恰是**高危载体**：`zhuopin-queue-audit`／`zhuopin-kickoff-prompt` 里若留了旧路径，后果与 ④ 类定时任务真身完全一样（照旧指针读 → 解析到零行 → 静默空巡）。

### 4.4 ④ 定时任务真身 —— 命中 **2** 处，**均为良性**

- `Claude\Scheduled\huijian-chaijian-patrol\SKILL.md:21`
- `Claude\Scheduled\weekly-status-update\SKILL.md:12`

两处原文都**明确写着**「原 `跨桌任务队列.md` 现为纯指针文件，照它读会解析到零行、静默输出空巡」——即 2026-08-13 那次修复留下的**告警文本**，不是残留的旧指针。✅ **该类已真实修复且修复留痕可查。**

### 4.5 并入项 A 的核定总表

| 类 | 命中 | 核定结论 | 零命中是否已独立证伪 |
|---|---|---|---|
| ① Cowork artifacts | 67 | 活载体 0 处欠账，65 处为历史快照 | 不适用（非零） |
| ② `.51` 四服务页面 | 0 | 🔴 **假零**——四服务已加口令门，工具只读到登录页 | ✅ 已证伪（真实 GET ＋ 原文比对） |
| ③ 已安装版 skill | 0 | 🔴 **假零**——扫描路径下无本项目 skill，已安装版不落本机 | ✅ 已证伪（阳性对照 ＋ 目录清点 ＋ 全盘定位） |
| ④ 定时任务真身 | 2 | ✅ 良性（是修复留下的告警文本） | 不适用（非零） |

**⇒ 本并入项的原始欠账（63/67 逐类核定）就此完成，可销。但它的产出不是「修了几处旧指针」，而是「查出这个工具的四类里有两类结构上无效」。**

---

## 五、并入项 B：文档台账份数漂移（08-17 已修，本轮核残余部分）

**原残余任务**：「在主工作区与各存活 worktree 各跑一次台账，确认份数已真正一致」。

### 5.1 本轮实际做法与理由（**判据已修正，如实说明**）

**原判据在本轮实测下不成立**：本轮所有存活 worktree 落后 master **22 ～ 729 个提交**（见 1.4）。**落后 700 个提交的 checkout 与主工作区文档份数不同，是正常的、正确的**——它本来就少了几百个提交里新增的文档。**拿「份数相等」当判据，会把一个正确结果判成故障。**

**⇒ 改用一个与 checkout 新旧无关的等价判据**（这正是 08-17 那次修复真正要保证的性质）：

> **缓存目录物理存在，而台账零收录。**

### 5.2 实测（主工作区，非破坏性，未重跑生成器）

| 项 | 实测值 |
|---|---|
| 主工作区现存 `.pytest_cache` 目录 | **16** 个 |
| 主工作区现存 `__pycache__` 目录 | **39** 个 |
| 台账（mtime 2026-08-24 19:10:07）内 `.pytest_cache` 命中 | **0** 行 |
| 台账内 `__pycache__` 命中 | **1** 行 —— 已逐行核实为**表头说明文字**（L4「已排除构建/缓存产物目录…」），**非数据行** |
| 台账头部读数 | 共 **418** 份 md，**27** 份待补状态头 |

✅ **在缓存产物大量物理存在（合计 55 个目录）的前提下，台账收录 0 条 ⇒ `EXCLUDED_DIR_NAMES` 排除逻辑在生产环境真实生效。**

**⇒ 并入项 B 的残余部分完成，可销。建议同时把本行内记的判据由「各 worktree 份数相等」改为上面这条**——理由与本行 2026-08-05 自己写下的那句一致：**会随环境漂移的量不该被写成判据。**

---

## 六、部署惯例一致性（#96 清单执行情况）

### 6.1 新发现 ④：5 个部署类 `.ps1` 无 UTF-8 BOM 且含中文

全仓（排除 worktree）扫描 `deploy*.ps1`／`sync-to-server.ps1`／`register-*.ps1`／`run-*.ps1`，共 16 份：

| 文件 | BOM | 判定 |
|---|---|---|
| `5-平台底座/wecom-aibot-service/register-decision-reminder-task.ps1` | ❌ | 🔴 **高**——注册脚本按设计在 `.51`／目标机执行 |
| `5-平台底座/wecom-aibot-service/register-followup-dispatch-task.ps1` | ❌ | 🔴 **高**——同上 |
| `5-平台底座/wecom-aibot-service/sync-to-server.ps1` | ❌ | 🟠 中（**#98 行内 OP-0822-C 已登记过这一份**） |
| `1-转型规划/AI运营指挥中心/sync-to-server.ps1` | ❌ | 🟠 中（**新发现**） |
| `4-数字员工/采购部/SC8-客户订单交期智能承诺/sync-to-server.ps1` | ❌ | 🟠 中（**新发现**） |
| 其余 11 份（含 4 份 `deploy-server.ps1`、FI2 的 `register-tax-export-scan-task.ps1`、`run-commit-sweep.ps1`） | ✅ | 合规 |

**分级依据（不搞一刀切）**：根 CLAUDE.md 坑 5 的原文约束是「**部署 ps1 必须存成带 UTF-8 BOM**，因为 `.51` 上是内建 Windows PowerShell 5.1、按 ANSI 读中文会语法错」。据此：

- **`register-*.ps1` 的用途就是在目标机上注册计划任务** ⇒ 一旦真在 `.51` 上跑，中文注释会按 ANSI 解码、**可能直接语法错**。这两份是真风险。
- `sync-to-server.ps1` 在**本机 pwsh 7**（默认 UTF-8）执行，当前不触发该缺陷；**但它是「哪天有人在 .51 上跑一次」就会中招的定时炸弹**，且与惯例不一致。

⚠️ **已知边界**：本轮**没有**在 `.51` 上实际执行这两份 `register-*.ps1` 验证是否真会报错（那属改动生产、超出巡检范围）。上表的「高」是**基于坑 5 已实证机理的推断**，不是本轮实测。**如实标注，不冒充实测。**

### 6.2 处置建议

补 BOM 是**零语义改动**（仅文件头 3 字节），建议交 CC 一批处理五份，并在 `工具-落库sweep.py` 或队列 lint 上加一条「部署类 ps1 缺 BOM 即告警」的机器守卫——**否则这条惯例永远是人守，而它已经漏了 5 份。**

---

## 七、行内 ⚠️ 遗留升格复扫

**本轮做的是普查，不是逐条判定**（逐条判定的工作量远超一次巡检，**如实说明范围，不假装扫完了**）。

**实测**：§一 现有 **33** 行，其中 **28 行含 ⚠️ 标记，合计 98 处**。

密度最高的 5 行：`#328`（13）、`#282`（8）、`#324`(8)、`#387`（8）、`#98`（7，即本行自己）。

🔑 **这个数字本身就是一个结论**：**§一 有 85% 的行带着「⚠️」在跑**。当一个标记覆盖了绝大多数行，**它就不再有区分度**——扫到它不再意味着「这里要特别注意」。这与 §四 #58 判定「非阻断提示连续 6 次被越过后信息量为零」是同一形态，**只不过发生在标记体系上，而不是提示体系上**。

**建议（不在本轮拍板）**：⚠️ 要么分级（如 ⚠️＝待判／🔴＝阻塞），要么设定「⚠️ 满 N 天未处置即强制升格为独立行或删除」的机器规则。已随处置建议一并出行。

---

## 八、本轮处置建议汇总（全部交 CC，本轮零动手）

| # | 事项 | 交付方 | 紧急度 |
|---|---|---|---|
| 1 | 10 个 worktree 元数据补 `commondir` → 查脏 → 再清理（**顺序不可反**）；24 个 0 文件空壳可直接删 | CC | 中 |
| 2 | `queue-315-apply-9f2c1a`（落后 729／脏 13）单独判定去留 | CC | 中 |
| 3 | 手动跑一次 `Claude-Env-HealthCheck` 验证成因，改 `LogonType`（建议 `S4U`，同本行已记的笔记本侧口径）并加失败告警 | CC | **高**（已停摆近两月） |
| 4 | `工具-仓库外载体扫描.py` 修两类结构性假零：② 需带口令登录后再取页（或改为对 `.51` 上的**页面源文件**扫描）；③ 需覆盖 Cowork 侧已安装 skill，做不到则**明确输出「本类无法核验」而非「命中 0 处」** | CC | **高**（该工具是「已复检」声明的依据） |
| 5 | ① 类输出按「活载体／历史快照」分列，避免 67 这种吓人但无信息的数字 | CC | 低 |
| 6 | 5 份 `.ps1` 补 UTF-8 BOM ＋ 加一条机器守卫 | CC | 中 |
| 7 | §四 #68（`wecom-service-home` 落后 246 提交）**本轮实测已归零**，可据此复核销号 | 值周巡检 | 低 |
| 8 | ⚠️ 标记分级或设升格时限（98 处 / 28 行，已失去区分度） | 待议 | 低 |

---

## 九、下期（2026-09-25，9 月最后一个周五）执行清单

1. 复测 1.1 三方计数**关系式**（不再核对具体数字）；
2. 复测 2.1 四个 Zhuopin 任务 ＋ **`Claude-Env-HealthCheck` 是否已真正跑通**（本期新增）；
3. 复跑 `工具-仓库外载体扫描.py`，**核对第 4 项处置是否已落地**——若 ②③ 仍返回「命中 0 处」而未改为「无法核验」，**视为该工具仍不可信，不得据其零命中做任何「已复检」声明**；
4. 复测 6.1 BOM 名单是否清零；
5. 本期未做：⚠️ 遗留逐条判定（仅完成普查）。

---

**Last Updated**: 2026-08-24（首期执行，Cowork 环境总线，A6 派单 ②）
