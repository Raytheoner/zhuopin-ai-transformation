---
title: "看护件 · 泳道看护批 B-0903_50（第七批：两链合 master ＋ criteria 收口 ＋ 财务五包 apply）"
created: 2026-09-03
批次: B-0903_50
派出线: Cowork 业务总线 OP-0903-D
前批: B-0903_30（A1/A2 DONE、A3 停 design 审、A4 留步）；B-0903_40（第六批看护件，其 opener 已并入本批 D1、**该件作废不另派**）
status: 执行中
---

# 看护件 · 泳道看护批 `B-0903_50`

## 〇、本批口径

**Shao Peishen 2026-09-03 一次答完七项（原话「全部推荐」）**，`criteria-signoff-platform` **design 审通过**，A3 可续跑、A4 依赖解除。本批把三件串行做完。

🔴 **B-0903_40 那件看护件已被本批吸收**（其唯一 opener ＝ 本批 D1，合入范围已确认为「照原意合、含连带」）⇒ **B-0903_40 不再单独派出**，避免同一动作两处派单。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。
📌 `merge_to_master` 属 **🟡**，**但本批已有前置放行**（他 2026-09-03 就 A1/A2/A3 三条明确放行、并确认连带范围）⇒ 泳道**不为"合入"这个动作本身停**；**范围一旦超出各 opener 所列即须停**。

---

## 一、七项裁决原文（🔴 逐条落实，勿凭记忆复述）

| 编号 | 事项 | 裁决 |
|:--:|---|---|
| **G-1** | 占位词黑名单 | **(a) 留**；🔴 **另补 `YYYY-MM-DD` 日期格式校验**（现在 `2026-13-45` 能过） |
| **G-2** | 三类「1 次」缺口（权限／存在性／前置） | **(a) 不收进底座**，维持五个场景包各自分别立牌 |
| **G-3** | FI10 `SLOW_MOVING_CRITERIA` | **(a) 归进注册表**；owner ＝ **财务侧**；`question` 须写明「FI10 先出、SC7 后对齐（EE-4），签认前须知会 SC7 口径确认人」；🔴 **连带：`tasks.md` 4.3 验收数 17 → 18** |
| **G-4** | L2 模型层纪律（5/5） | **(b) 另立队列行、不排期、不并进本包** ⇒ 已立 **§一 `#476`**（🛑 排队中，触发条件＝至少两个财务引擎落地） |
| **G-5** | 接 `AuditLogger` | **(a) 不接**（保底座零依赖）；🔴 **改反向依赖** ＝ 引擎 `record(AuditEvent)` 时把当时的 `RULE_VERSION` 写进 `decision`，**并进 A4** |
| **G-6** | A4 同批批准 ＋ 合入范围 | **A4 (a) 同批**；**合入 (a) 照原意合、含连带** |
| **G-7** | FI9 工时系统存在性核实 | **(a) 立行** ⇒ 已立 **§一 `#477`** |

📌 **G-3 的成因值得传下去**：`SLOW_MOVING_CRITERIA` 本来是「无源可取」（等 SC7），**是 EE-4「FI10 先定、SC7 后对齐」把它变成了一条标准的待签认判据** —— 起草方写 design 时尚未意识到这层联动。⇒ **裁决之间会互相改变彼此的性质，答完一条要回头看它是否解掉了另一条的前提。**

---

## 二、硬边界（继承，逐条适用）

1. 🔴 **⏭️ 档：`.51` 部署及任何触碰生产服务的动作永不执行**。
2. 🔴 **🔴 档：对外发送／L2 门禁签字／合规红线变更／ASIL C-D 永不代办**。
3. 🔴 **业务*数据*与一切对外发送永不自动入批**；真实库**只读**。
4. 🔴 **所有 `pause` / `check-heartbeat` 一律带 `--no-notify`**。
5. 🔴 **不填任何未签认判据、不给任何默认值** —— `None` ＋ CI 守一处不许动。**G-3 是把一条判据「登记进注册表」，不是「给它填值」**，两者勿混。
6. 🔴 **三闸全锁**（唐燕萍／姚祖怡／陈忱）⇒ 不得起草或发送任何跟进信。
7. 🔴 **规划文档一律不改**（全景规划／实施计划／权威排期表／前置总表）—— 走移交单。
8. 🔴 **不得开工 §一 `#476`**（触发条件未满足）。

---

## 三、波次与触碰区矩阵

🔴 **本批只有一条泳道 `criteria-chain`，三段严格串行**（`A1 → A2 → A3`）。

| 波 | 泳道名 | 段 | 件号 | 触碰区 |
|:--:|---|:--:|---|---|
| 1 | `criteria-chain` | A1 | `OP-0903-D1` 两链合 master | `master`、`openspec/specs/` |
| 1 | `criteria-chain` | A2 | `OP-0903-D2` criteria 收口并合入 | `5-平台底座/.../criteria_signoff/`、`openspec/changes/criteria-signoff-platform/`、`master` |
| 1 | `criteria-chain` | A3 | `OP-0903-D3` 财务五包 apply | `4-数字员工/财务部/FI5·FI6·FI8·FI9·FI10/`、`openspec/`、`master`、§一 #470-#474 |

🔴 **为什么不拆成三条并行泳道**（批处理的默认倾向是「不同泳道名 ⇒ 并行」，此处刻意不用）：
**三段都要合 `master`** —— 触碰区在最关键那一步完全重叠，**同一时刻只能有一条在合**；且 A2 的合入依赖 A1 已落 master、A3 依赖 A2 已落 master。并行起跑（`-StaggerSec` 默认 90）只会让 A3 在依赖未满足时 `OPENER_PARTIAL` 退出、白跑一趟。⇒ **同一泳道名下按 `A<N>` 升序串行，是本批唯一正确的排法。**
✅ **自查闸仍逐段保留**（A2 查 A1、A3 查 A2）—— 串行已保证顺序，闸是**第二道**防线，防的是「上一段自己 `OPENER_PARTIAL` 了但下一段照跑」。

### 🔴 本批 `merge_to_master` 的 🟡 豁免依据（非自造，可逐字引用）

`merge_to_master` 在 D1 判据表里属 **🟡（停等一个字母）**。本批**不为这个动作停**，依据是 Shao Peishen 2026-09-03 的直令：

> **G-6 ＝ (a)**：「合入照原意做，**含连带**。」（他在被明确告知 A1 会连带 1 个基分支提交、A2 会连带 6 个、且三条分支全部 `ff-NO` 之后作出的答复）
> **G-5 ／ ⑤ A4 ＝ (a)**：「A4 与本包同批批准。」⇒ A2／A3 的合入同在放行范围内。

🔴 **豁免只覆盖「把本批这三条链合进 master」这一件事。** 其余任何 🟡/🔴 动作（改口径判据阈值、新的 design 审、关他人在办队列行、对外发送、L2 门禁）**照停不误**；**范围一旦超出各 opener 列明的 commit 清单，即按 🟡 `pause`**。

### LAN 留步清单

**本批为空。** LAN 探针实测 ＝ **off-LAN**（三项未齐），但本批三段**均不依赖内网**（纯 git／openspec／单测／底座包内建造），故无一项被排除。🔴 三段全部**不触碰 `.51`**，⏭️ 档零项。

---

## 四、泳道 opener

> 🔴 **格式说明（勿改）**：下面三节的 `### A<N> ·` 标题与 `粘贴端／泳道` 行是 `工具-opener批处理执行v2.ps1` 第 33-56 行解析器的**硬要求** —— 标题必须 `###` 三级、编号必须 `A` 加数字、泳道名不得含空格，缺任一条会报 `✗ 未解析到任何 opener` 并 exit 12。**本件 2026-09-03 首版曾用 `### D1 ·` 写法，全批解析不到，已按解析器改正。**
>
> 🔴 **实测勘误 ①（2026-09-03 11:03，A2 段自查闸时撞出，出 opener 方记下）**：A2／A3 的自查闸原写 `git log --oneline origin/master -3`（A3 为 `-5`）去找依赖段的 commit message。**实测取不到** —— A1 一次合入 9 个提交，`采购三包` 那条排在**第 9 位**，`-3` 的窗口根本够不着。A2 自行改用 `--grep` 逐条核 message 后闸过。
> 🔑 **判据没错，取证命令的作用域不够** —— 这是接力卡 §五「命令返回了一个漂亮的数字，而它数的是另一个东西」的**镜像形态**：这次是**命令什么都没返回，而原因不是那个东西不在**。⚠️ **`-N` 这种「取最近几条」的写法，在「一次合入多个提交」面前必然失效**；判「某条 message 在不在某分支上」**一律用 `--grep`／`git branch --contains`，不要用 `-N` 截窗口**。⇒ 此后出 opener 不再写 `-N` 形式的存在性判据。

### A1 · 【CC】两链合 master —— 队列 #467／#468／#469／#118

粘贴端：CC ｜ 泳道：criteria-chain

▶ **首次派出**

```
[OP-0903-D1]【CC】两链合master
【设置】执行环境：CC ｜ 分支：master（🔴 直接在主工作区操作 master，不新建特性分支）｜ worktree：☐（不建 —— 合入 master 须在主工作区，worktree 内 master 被 checkout 锁定）｜ 工作区：无（纯库内 git 与 openspec 校验，🔴 不部署、不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_50
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903D1-两链合master。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_50-criteria-chain.md`（🔴 **本泳道三段 A1/A2/A3 共用同一心跳文件，接着追加**），开工 1 分钟内写首行，每里程碑追加，超 10 分钟补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_50 --wave 1 --lane criteria-chain --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`；⏭️ 走 `transfer-out`。
🔴 **唯一豁免（本批已有直令，不为它停）**：`merge_to_master` 属 🟡 档，但 Shao Peishen 2026-09-03 答 **G-6=(a)「合入照原意做，含连带」**（他已被明确告知连带范围与三条分支全部 ff-NO 后作答）⇒ **本段合入本批列明的两条链时不 pause**。🔴 **豁免只覆盖这一件事**：范围一旦超出下面第 2 步列明的 commit 清单，或命中任何其它 🟡/🔴 动作，**照停不误**。
读 ① `1-转型规划/0-全景路线图/看护件-泳道看护批B-0903_50-2026-09-03.md` §四 D1（合入范围，**逐条比对，多一个提交都要停**）→ ② 根 `CLAUDE.md` §5「合并策略 ff-only」。本件为 A 类（范围已由 Shao Peishen 确认、判据已写死），无需再问澄清，直接开工。

🔴 Shao Peishen 2026-09-03 答 G-6 = (a)（原文）：合入照原意做，**含连带**。他已知晓 A1 会连带 1 个基分支提交、A2 会连带 6 个，内容均已获他放行。

做什么（🔴 严格串行，每步过了才做下一步）：
1. `git fsck --connectivity-only`（对象库健康哨兵）——**报错即停手、不 pull 不 push、报 Shao Peishen**；通过后 `git pull`。
2. **范围复核（第一道闸）**：`git log --oneline master..<分支>` 两条各跑一次，逐个 commit **message** 比对（🔴 **不认 hash** —— rebase 后必变）：
   - A1 `claude/op0903c1-proc-apply` 应为 2 个：`feat(采购三包): SC4/SC10/SC11 跑 opsx:sync…` ＋ `feat(采购三场景): SC4/SC10/SC11 工程骨架 + openspec proposal 起草…`
   - A2 `claude/op0903c2-spec-note` 应为 7 个：`docs(SC8): 主 spec 规则2开关处加建成状态标注…` ＋ 六条 `docs(SC8): #118 …`（ATP 批2 收口链）
   - 🔴 多、少、或出现表外 message ⇒ `pause --action-key merge_to_master`，**不自行判断「应该没问题」**。
3. **合 A1 链**：`git rebase master claude/op0903c1-proc-apply` → 回 master `git merge --ff-only claude/op0903c1-proc-apply`。
   🔴 rebase 冲突即**停**：`pause` 并把冲突文件与两侧内容如实写进心跳，**不自行择一**。
4. **合 A2 链**：同法处理 `claude/op0903c2-spec-note`（🔴 **必须在 A1 合完之后**，否则第二条的 rebase 基点是错的）。
5. **合后校验（第二道闸，三项全过才算合成）**：
   a. `openspec validate --all --strict` 须绿。⚠️ 两条链各自的计数（A1 侧 132、A2 侧 127）是在**各自分支上**测的，合并后总数会变 —— **不要拿旧数当预期值**，只认「0 failed」，实际数字如实写进心跳。
   b. `git rev-list --count master..claude/op0903c1-proc-apply` 与 `..claude/op0903c2-spec-note` **均须为 0**。
   c. 🔴 `git rev-list --count master..claude/op0903c3-criteria-signoff` **须 > 0** ＝ 确认 A3 没被顺手带进来（**A3 由 D2 泳道自己合**）。
6. `git push origin master`。
7. 收工：§二 批次登记（🔴 **文件清单里每个路径都要用反引号包住** —— 守卫⑹ 走 ``re.findall(r"`([^`]+)`")`` 取片段，裸路径提取为空会判「孤儿文件」拒绝 release，2026-09-03 Cowork 实撞两次）；§一 #467／#468／#469／#118 四行回写「已合入 master」。

不做什么：
- 🔴 **不合 `claude/op0903c3-criteria-signoff`**（归 D2 泳道）
- 🔴 不删任何分支（合完保留，便于追溯）
- 🔴 不进任何实现、不填任何未签认判据、不改规划文档
- 🔴 不部署、不触碰 `.51`、不发信（三闸全锁）
```

---

### A2 · 【CC】criteria_signoff 收口并合入 —— 变更包 criteria-signoff-platform

粘贴端：CC ｜ 泳道：criteria-chain

▶ **首次派出**

```
[OP-0903-D2]【CC】criteria收口合入
【设置】执行环境：CC ｜ 分支：master（从 `claude/op0903c3-criteria-signoff` 起 `claude/op0903d2-criteria-finalize`，🔴 **不是从 master 起**——底座模块在前者上）｜ worktree：☑（criteria-finalize，新 worktree，收工自删；🔴 **合入 master 那一步须回主工作区做**）｜ 工作区：无（平台底座包内建造 ＋ openspec，🔴 不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_50
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903D2-criteria收口。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_50-criteria-chain.md`（🔴 与 A1 同一文件，接着追加），规则同 A1。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_50 --wave 1 --lane criteria-chain --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`。
🔴 **`merge_to_master` 豁免同 A1**（Shao Peishen 2026-09-03 答 G-6=(a) ＋ ⑤ A4=(a) 同批批准，本包合入在放行范围内）；🔴 **豁免不含新的 design 审** —— 本段若因故需要重开任何一条判据，照 🟡 停。
读 ① 🔴 `5-平台底座/CLAUDE.md`（**动 5-平台底座 之前必读**）→ ② `openspec/changes/criteria-signoff-platform/design.md` §定夺项五条 → ③ `1-转型规划/0-全景路线图/design审读件-criteria_signoff底座-五条定夺项-2026-09-03.md`（**逐条推荐与取证，裁决依据**）→ ④ `1-转型规划/0-全景路线图/看护件-泳道看护批B-0903_50-2026-09-03.md` §一。本件为 A 类（五条已逐条拍板、判据已写死），无需再问澄清，直接开工。

🔴 Shao Peishen 2026-09-03 拍板：**design 审通过**，五条见本批看护件 §一 表（G-1…G-5）。**本泳道照裁决落，不再重开任何一条。**

做什么（🔴 严格串行）：
1. **G-1**：给 `Signoff.signed_on` 补 `YYYY-MM-DD` **格式校验**（占位词黑名单原样保留、不动）。
   🔴 单测须含**反例**：非法日期（如 `2026-13-45`、`2026/09/03`、`20260903`）必须抛；且**关掉该校验即有用例转红**（照 D2.4 既有做法，把证明常驻进套件）。
2. **G-3**：把 FI10 `SLOW_MOVING_CRITERIA` **登记进注册表**——
   - `owner` ＝ **财务侧**；
   - `question` 须写明「FI10 先出、SC7 后对齐（EE-4 拍板 2026-09-03），签认前须知会 SC7 口径确认人」；
   - 🔴 **它仍是未签认判据，值恒 `None`** —— 本步只是把它「登记进注册表」，**不是给它填值**；
   - 🔴 **连带改 `tasks.md` 4.3：验收数 17 → 18**（不改则 A4 会按「迁移失败」退回）。
3. **G-2／G-5 落档（只改文字、不改代码）**：把 design.md §定夺项五条改写为**已裁决**形态，逐条记裁决原文＋拍板人＋日期（2026-09-03 Shao Peishen）。
   🔴 **G-5 须把「反向依赖」方案写进 design 并在 `tasks.md` §4 加一条给 A4**：*不接 `AuditLogger`（保底座零依赖）；改由引擎在 `record(AuditEvent)` 时把当时的 `RULE_VERSION` 写进 `decision`* —— 依赖方向是「审计日志指向判据版本」，**不是判据模块去写日志**。
4. `openspec validate --all --strict` 须绿；底座全量回归须零回归（基线 452 passed / 1 skipped）。
5. **合入 master（🔴 合入前先自查闸）**：跑 `git log --oneline origin/master -3`，确认 **A1 段**的两条链**已在 master 上**（找得到 `feat(采购三包)…` 与 `docs(SC8): 主 spec 规则2开关…` 两条 message）。
   - 🔴 **找不到 ⇒ 以 `OPENER_PARTIAL: 依赖 A1 未完成` 收尾，不得抢先合** —— 同泳道串行已保证 A1 先跑，但**A1 可能自己 `OPENER_PARTIAL` 退出了**（批处理对 `OPENER_PARTIAL` 退出码为 0、不中断本泳道后续项），本闸防的正是这一种。
   - 找得到 ⇒ 回主工作区 `git rebase master claude/op0903d2-criteria-finalize` → `git merge --ff-only` → `git push origin master`。
6. 按「完工即归档纪律」判能否归档：🔴 **tasks 未全 [x] 就不归档**（§4 A4 段本泳道不做）；按三条入口之一留**机器认得**的说明（`暂不归档`／`预期观察窗口：N 天`／`--ack-stale-change`，写在 tasks 行内不算）。
7. 收工：§二 批次登记（🔴 文件清单路径**用反引号包住**，见 D1 第 7 步那条实撞）；§一 无对应行则如实写明、**不代立行**。

不做什么：
- 🔴 **不做 A4 段**（改五个场景包 ＝ D3 泳道的事）
- 🔴 **不收三类「1 次」缺口进底座**（G-2=(a)，`BANK_BALANCE_ACCESS`／`TIMESHEET_SYSTEM_EXISTS`／`CHIP_PRICE_API` 原样留在各场景包）
- 🔴 **不接 `AuditLogger`**（G-5=(a)，只写方案不接线）
- 🔴 **不做 L2 模型层纪律收编**（G-4=(b) 已另立 §一 `#476`，🛑 排队中，触发条件未满足，**认领即违规**）
- 🔴 不填任何判据值、不给任何默认值、不部署、不发信
```

---

### A3 · 【CC】财务五包 apply 并引用底座 —— 队列 #470–#474

粘贴端：CC ｜ 泳道：criteria-chain

▶ **首次派出**（🔴 同泳道串行，自动排在 A2 之后；opener 内另有自查闸）

```
[OP-0903-D3]【CC】财务五包apply
【设置】执行环境：CC ｜ 分支：master（从 `claude/op0903b3-fin-scenarios` 起 `claude/op0903d3-fin-apply`）｜ worktree：☑（fin-apply，新 worktree，收工自删；🔴 合入 master 那一步回主工作区做）｜ 工作区：无（openspec sync 与场景包引用改造，🔴 不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0903_50
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0903D3-财务五包apply。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。⚠️ 另：无头泳道 session 里该工具可能不存在，不存在即跳过、不报错（2026-09-02 实测）。
心跳：`reports/lane-heartbeat/B-0903_50-criteria-chain.md`（🔴 与 A1／A2 同一文件，接着追加），规则同 A1。
🔴 **开工第一条硬前置**：跑 `git log --oneline origin/master -5`，确认 **A2 段**的收口提交**已在 master 上**（`criteria_signoff` 的 `signed_on` 格式校验 ＋ `SLOW_MOVING_CRITERIA` 入注册表）。**找不到即以 `OPENER_PARTIAL: 依赖 A2 未完成` 收尾，不得盲目往下做** —— 五包若先 apply 掉本地实现，再收编就是二次返工。
分档判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0903_50 --wave 1 --lane criteria-chain --action-key <键> --waiting-for "<...>" --no-notify` ＋ `OPENER_PARTIAL`。
🔴 **`merge_to_master` 豁免同 A1**；🔴 **但 EE-3（FI9 接 `data_isolation_layer`）若命中 openspec 门槛②（数据可见性）⇒ 起草 design 后照常 `pause`，豁免不覆盖它。**
读 ① 分支 `claude/op0903b3-fin-scenarios` 上五个包 → ② master 上 D2 产出的底座模块 → ③ 队列 §一 #470–#474 → ④ 本批看护件 §一 裁决表。本件为 A 类，直接开工。

🔴 Shao Peishen 2026-09-03 裁决（原文，逐条落实）：
  EE-2 【FI8 银行余额取数授权】＝**他本人去推该授权**（涉资金安全、须财务侧＋CFO 办公室，**属权限非判据**）
       ⇒ 本泳道**做 FI8 不依赖余额的部分**，`BANK_BALANCE_ACCESS` 保持空值 ＋ 独立用例守，🔴 **不得绕过**。
  EE-3 【FI9 是否带出 OEM 项目标识】＝(a) **会，按接 `data_isolation_layer` 设计**。
       理由：研发费用归集按项目走，OEM 项目几乎必然出现；**这一条错了是合规问题，宁可多接**。
       🔴 接法须守根 `CLAUDE.md` §7 红线 3（OEM 数据按客户路由、禁跨库）与 `5-平台底座/CLAUDE.md` 隔离边界。
  EE-4 【FI10 L9 呆滞口径】＝(a) **FI10 先定、SC7 后对齐**。⚠️ 「先定」**不等于现在就填那个口径**——
       判据仍未签认、照旧 `None` ＋ CI 守；被定下的只是**口径归属**，🔴 **这一点须写进 spec 与队列行**。
  G-5  【AuditLogger】＝(a) 不接底座；🔴 **改反向依赖，本泳道落**：引擎 `record(AuditEvent)` 时把当时的
       `RULE_VERSION` 写进 `decision`（约一行）。**方向是审计日志指向判据版本，不是判据模块写日志。**

做什么：
1. 五包各跑 `/opsx:sync`（🔴 **不是 `apply`** —— 本仓库 `/opsx:apply` ＝ 实现 tasks 待办；能写 `openspec/specs/` 的是 `sync`，判法同队列 `#299`，2026-09-03 `OP-0903-B1` 实撞已确认）。
2. 五份本地判据签认实现改为**引用底座 `criteria_signoff`**；🔴 **`None` ＋ CI 守的行为一处都不许变**。
3. 落实 EE-2／EE-3／EE-4／G-5 四项（见上）。EE-3 接隔离层的设计若命中 openspec 门槛②（数据可见性）⇒ **起草 design 后 `pause`，不自行通过**。
4. 🔴 **两条验收硬条件（`tasks.md` 4.3／4.4）**：
   a. 迁移前后**未签认判据数量必须相等 ＝ 18**（🔴 **不是 17** —— G-3 把 FI10 `SLOW_MOVING_CRITERIA` 归进了注册表，D2 已把 4.3 由 17 改 18）。任一条在迁移中被填上值即视为迁移失败、退回。
   b. 迁移后五个场景包内**不得残留任何本地判据 `None` 常量**（底座与手抄并存比现状更差）。
   ⚠️ **三类「1 次」缺口不在此列**（G-2=(a) 不收）：`BANK_BALANCE_ACCESS`／`TIMESHEET_SYSTEM_EXISTS`／`CHIP_PRICE_API` **原样留在各场景包内、分别立牌，不迁、不合并**。
5. `openspec validate --all --strict` 须绿；五场景单测全绿（迁移前基线 53 passed）；底座全量零回归。
6. **合入 master**：确认 D2 已在 master 上 → rebase → `merge --ff-only` → push。
7. 收工：§二 批次登记（🔴 文件清单路径**用反引号包住**）；§一 #470–#474 **逐行**回写。

不做什么：
- 🔴 不填任何未签认判据、不给任何默认值
- 🔴 **不把三类「1 次」缺口收进注册表**（G-2=(a)）
- 🔴 不做 L2 模型层纪律收编（§一 `#476` 🛑 排队中）
- 🔴 不碰真实银行数据、不连真实库、不部署、不触碰 `.51`
- 🔴 不发信（唐燕萍闸 🔒）
```

---

## 五、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0903_50` 现取。

---

## 六、本批不做、已明确去向的事

| 事 | 去向 |
|---|---|
| L2 模型层纪律收编 | §一 `#476`，🛑 排队中，**触发条件＝至少两个财务引擎落地**，本批不得开工 |
| FI9 工时系统存在性核实 | §一 `#477`，待领（**问人的事，不是代码任务**） |
| FI8 银行余额取数授权 | **Shao Peishen 本人推**（EE-2），属权限非判据 |
| `§四` 分诊 22 条（含 4 条逾期） | `reports/section4-triage-2026-09-02.md`，下一轮摆 |
| 销售域 6 个＋研发域 | 他 2026-09-02 明令**暂缓、等他通知**；🔴 **不是立成 🛑 排队行，是根本不立** |
