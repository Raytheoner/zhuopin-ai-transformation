---
title: "开场prompt ·【Cowork】环境总线接棒（OP-0906-W → OP-0907-AL）"
created: 2026-09-07
执行方: Cowork 环境总线（新会话）
来源: session接力-Phase1收口.md（09-07 16:1x 覆盖版）；端到端构建workflow优化-方案-2026-09-06.md §六
status: 待你在 Cowork 新会话粘贴
---

# 开场prompt ·【Cowork】环境总线接棒

## 〇、转场判据自检（`zhuopin-kickoff-prompt` 五条）

① 忘记早先事实：阴（本会话已被系统压缩一次，压缩前事实只剩摘要——属 ⑤ 的证据，不是 ①）；② 重复已做工作：阴；③ 前后矛盾未标更正：阴；④ 丢失指令：阴；⑤ token 压力真实且压缩手法用尽：**阳**（会话已自动压缩、后续仍以只读脚本出摘要，仍触顶）。⇒ 一条阳性，转场成立；Shao Peishen 同日主动要求。

## 一、并行矩阵

| 会话 | 关系 |
|---|---|
| `B-0907_R`／`B-0907_Y`／`OP-0907-AK`（CC，他已粘） | 新会话只收复命、答字母路由，不碰其分支 |
| `OP-0907-M`（FI8 grill，Cowork） | 无关；intent 确认后新会话接 §三 建点 |

## 二、opener

粘贴端：Cowork 新会话

▶ 首次派出：[OP-0907-AL]

```
[OP-0907-AL]【Cowork】环境总线接棒
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐（不建，只产改 `.md`，台账写入经 `LedgerStore` 脚本、不改 `.py`）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W
读 ① `1-转型规划/0-全景路线图/session接力-Phase1收口.md` 全文（【二】快照＋【三】主攻 0–6＋【四】等他项）→ ② `CLAUDE.md` §3／§5 恢复上下文，按该卡【三】执行。本件为 A 类（同线转场，口径已定）。

做什么：
1. 开工自检三查（通过即不问）：⑴ `python 0-学习与工具/工具-队列查询.py --digest` 扫两份队列，按触碰区关键词「台账／回写／grill／看护」核无他人在办行重叠，命中再 `--row N --field all` 展开，🔴 不得 Read／grep 队列真身；⑵ `--digest --section 二` 核 `B-0907_W17` 是否已登（未登按接力卡【三】0 补登）；⑶ `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_R` 与 `--batch B-0907_Y` 现取三批状态。
2. 按接力卡【三】1 起 `#439` §2 回溯拆点（Cowork 活，先 18 封带 `决策点:` 的信）；其余按【三】2–5 顺序收 CC 复命、派下一批。
3. 收工：把进展＋下一步滚动覆盖进接力卡（≤8 KB，本机 `Get-Date` 取日期）、未闭合项进队列行；逐条过 `zhuopin-kickoff-prompt` 转场判据五条并出声。

不做什么：
- 不 commit、不 push、不碰 `.51`、不发企微；对外发送／L2 门禁／合规红线永不代办。
- 不新立机制行（除非出血）；不改 R／Y／AK 三批在途分支与其触碰文件。
- 不复述队列协议与骨架格式：opener 一律 Read `opener骨架.md` 后套用并过 `工具-opener块lint.py --enforce`。

收工：产出登记 §二 待 commit 批次（走 `0-学习与工具/工具-共享文档编辑锁.py`，勿裸改、勿自行 commit），由落库 sweep 取活。
```
