---
title: "看护件 · B-0916_584续棒（单泳道）"
created: 2026-09-16
批次: B-0916_584续棒
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 3a）
---

# 看护件 · B-0916_584续棒

> 任务全文＝队列 §一 `#584` 状态列末段「上一棒根因被 Cowork 反证推翻」及其续派口径。上一棒（`OP-0916-H`）结论已作废，不得沿用。

## 一、硬边界

- 🔴 只 push 本泳道分支，不 ff master。
- 🔴 主仓内禁用 `git reset --hard`／`checkout --`／`clean`；切目录后的 git 写操作先断言 `git rev-parse --show-toplevel`。
- 🔴 不改全局 `~/.claude/`；不停用任何插件；对照实测 `--model haiku` ≤ 8 次。
- 🔴 结论必须同时解释「当日触发过 5 次」与「OP-0916-H 两次未触发」，只解释一边的不算定因。

## 二、泳道

▶ 粘贴端：CC ｜ 泳道：op0916i-headless-parity-584b

```
[OP-0916-I]【CC】无头开场注入续棒
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0916i-headless-parity-584b`） ｜ worktree：☑（op0916i-headless-parity-584b，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 模型：sonnet ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_584续棒
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916I-无头开场注入续棒。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #584（`python 0-学习与工具/工具-队列查询.py --row 584 --section 一 --field all`，以状态列末段「续派口径」为准，上一棒根因已作废）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 取证先行：先测 `hooks-sessionstart-context.ps1` 单跑耗时（主仓与 worktree 各 3 次）与 `git fsck` 耗时，再用 `--debug-file` 对照定因；定因写回 #584 后再动手修。
🔴 一切 `reports/` 产出写**主工作区**（`git rev-parse --git-common-dir` 的上级目录）。
🔴 上下文越 150k 收到计量提醒即按提醒收尾交接，不硬撑；优先把重输出步骤（全量回归、大 debug 日志 grep）交子代理，主会话只收结论。
🔴 测试一律前台跑、`pytest -q --tb=short`，跑完即 commit，严禁放后台等通知。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916i-headless-parity-584b --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916i-headless-parity-584b --done --batch B-0916_584续棒 --text "产出落点：<落点>"`。
🔴 收工只 push 本泳道分支，不 ff master；在 `合入登记/pending-ff.jsonl` 登记待授权合入行；状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾；非本行口径内的新发现只登记、不作为停工理由。
```

## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】584续棒-2026-09-16.md" -FullAuto -Yes -Detach
```
