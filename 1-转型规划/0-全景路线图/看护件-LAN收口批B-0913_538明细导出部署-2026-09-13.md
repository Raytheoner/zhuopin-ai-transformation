---
title: "看护件 · LAN 收口批 B-0913_538明细导出部署（2026-09-13）"
created: 2026-09-13
status: 已收口（项 1 过；#538 销号待 Shao Peishen 定夺）
批次: B-0913_538明细导出部署
派出线: Cowork 业务总线 OP-0911-H（`我已回Lan` 触发 zhuopin-lan-closeout）
---

# 看护件 · LAN 收口批 `B-0913_538明细导出部署`

## 〇、本批口径

**触发**：Shao Peishen 2026-09-13 18:5x「我已回Lan」。规则正本＝`0-学习与工具/skills源码/zhuopin-lan-closeout/SKILL.md`，判据正本＝`openspec/changes/lan-closeout-skill/`。

🔴 **前置探针（触发词声称≠在网事实，已实证；本次绕了一大圈才拿到真信号）**：

- **权威探针** `工具-泳道看护状态机.py lan-status` ＝ **✅ on-LAN（三项齐备）（原始探测）**，本机时间 2026-09-13 19:1x +08:00。
- **服务级响应**（唯一算数的一项）：`http://192.168.100.51:8092/api/ping` ＝ **200**（len 70）；`:8092/` ＝ 200（len 1472）。
- 🔴 **19:0x 第一次探针不过、`.51` 一项未动**：当时 Mihomo/Clash TUN 在跑，**TCP connect 被劫、零鉴别力**——`.51` 的 445/80/8092/22 全「连上」，而本应不存在的 `192.168.100.253:445`／`192.168.77.77:445` **也全「连上」**；`Find-NetRoute 192.168.100.51` 出口接口＝`Mihomo`、NextHop `198.18.0.2`。他 19:14 手动关掉 TUN 后，`Mihomo` 网卡消失、出口改为 `WLAN`→`10.11.43.254`，对照实验的两个伪地址**双双超时** ⇒ 量具恢复鉴别力，探针才算过。
- 🔴 **本次纠正一条我方自己的量具错误**：19:0x 那轮我把关键端点写成 `:80/api/ping`（超时），**实际服务在 `:8092`**。当时判「不过」靠的是对照实验、不是那个超时——**但端口错这件事要记住，下次别再用 `:80` 当判据。**
- ⚠️ `工具-未闭合产出扫描.py` 在 TUN 未关时其自带 LAN 探针**挂死**（三次 >60s 无返回，含 `--skip-lan-probe` 那次）⇒ 未关 TUN 前加 `--skip-lan-probe`；本批因此**缺形态 1 这一源**，如实标注、不假装扫过。

**常规三查**：① `master` ＝ `origin/master` ＝ `13148a9`，两向均 0；② 队列锁空闲（起链前需复查）；③ 上一收口批 `B-0909_506部署与419取证` 看护件已出。

**上限**：首跑灰度已过（`B-0907_Q` 三判据全过），默认 6；🔴 **本批实际入批 1 项**——Shao Peishen 2026-09-13 明示「先挑高价值的 `.51` 项，别把上限用满」。

### 三源留步清单汇集结果（D1 白名单：只收有可执行判据的留步登记）

| 来源 | 命中 | 判定 |
|---|---|---|
| 未闭合产出扫描器 形态 1 | 🔴 **未取到**（TUN 未关时探针挂死，三次 >60s；关 TUN 后未再跑，budget 考虑） | **本源缺失，如实标注**；下批补 |
| 两份队列真身 grep「LAN 留步」 | `#312`／`#398`／`#419` | 三项**全部落选**：`#312` 是概念提及（可 Open 池提醒机制）非可执行留步；`#398`／`#419` 均 `[S:blocked]`，`#419` 真读一遍后确认剩余两项＝**他本人动作**（D1 凭据落位／5.2 人眼半边）且生产载体是笔记本 worktree、**不碰 `.51`** ⇒ 落「对外待人」桶 |
| 两份队列真身 grep「`.51`」／「部署」 | 27 行／14 行，逐行判 | 唯一**真部署项＝`#538`** ⇒ **入批（项 1）**。`#506` **经复核已无需再部署**（见下）；`#96`／`#98`／`#358`／`#282`／`#436`／`#478` 等为机制活或 `[S:blocked]`，非本批面 |

🔴 **`#506` 从候选中撤下，理由写明**：它是上一收口批 `B-0909` 的项 1，**实质已完成**——姚祖怡 2026-09-10 回件正面确认页面试用（原话「你让我看的采购周报页面数据我都仔细看了」），全文未再提期次陈旧、未再提「本周窗口尚未走完」（手段＝python-docx 全文实读 43 段正文＋全部表格）。⇒ **它需要的不是再部署一次，是一次销号判断**（🟡 档，状态翻转）。本批不动它，另以定夺项端给 Shao Peishen。

## 一、硬边界

1. 🔴 **`.51` 一次只动一项、全串行**。本批只 1 项，无并发。
2. 🔴 **每项四步固定**：快照 → 执行 → 冒烟 → 回写证据。**冒烟任一项不过 ⇒ 按回滚 SOP 退回并停链**，失败项降回留步态并注明原因，**不带伤推进**。
3. 🔴 **「已部署」必须有冒烟三件套证据，无证据的完成声明视同未做。**
4. 🔴 **对外发送与请人动作永不代办**：本项产出是给姚祖怡的明细交付物，但**发不发是另一封信的事**，本批只做到「可交付态」。L2 门禁不代签；合规红线不动。
5. 🔴 **收口 ≠ 销号**：`#538` 的 `partial` 是否翻 `done` 按行内原纪律，本批不替判。
6. `pause`／`check-heartbeat` 一律带 `--no-notify`；**看护件落档失败即全停**。
7. ⚠️ **通道风险（本批特有，须写明）**：本会话到 `.51` 的桥今天出现 **3 次 60s 超时**（扫描器两次、`Start-Process` 一次）。⇒ 起链前复查桥可用；链上任一步骤超时**按「结果未知」处理，先用只读核验查真实状态，绝不盲目重跑写动作**（同 `zhuopin-send-followup` §6.1 的教训）。

## 二、波次（1 项，全串行）

| 序 | 项 | 承接 | `.51` 触碰 | 快照点 | 冒烟判据 | 回滚 SOP |
|---|---|---|---|---|---|---|
| 1 | **SC2 「187 行明细导出」部署 ＋ 与线上逐行比对** | §一 `#538`（🔴 采购域建造首位，§四 `#194` 裁 (a) 插队排在 `#499` 之前；代码侧已在 master `0aef9e2`，190 passed） | `C:\sc2\{app,.venv}` 写；计划任务 `Sc2WebServer` 重启；调明细导出端点 | ⑴ `C:\sc2\app` 顶层文件 SHA256 清单；⑵ 现 `GET :8092/api/ping` 返回体；⑶ `Sc2WebServer` 进程 CreationDate；⑷ 现网 `sc2_weekly_2026-W36.json` 原件备份 | 沿用 `4-数字员工/采购部/SC2-采购周报自动生成/smoke-server.ps1` 既有七项 ＋ 🔴 **本批新增第八项＝明细导出端点返回 187 行且与线上 `sc2_weekly_2026-W36.json` 逐行比对一致**（判据正本＝`4-数字员工/采购部/SC2-采购周报自动生成/取证-538-下单行数187复现与明细导出-2026-09-10.md`）；🔴 进程 CreationDate 逐次核对真实刷新，**非只信脚本打印「已重启」**（根 `CLAUDE.md` 坑 5） | 还原快照 ⑴ 的 `C:\sc2\app` 副本 → 重启 `Sc2WebServer` → 复跑 `smoke-server.ps1` 七项 → 仍不过即停链报人 |

**执行链**：`sync-to-server.ps1` → `deploy-server.ps1` → `smoke-server.ps1`（三件均在 `4-数字员工/采购部/SC2-采购周报自动生成/`）。

## 三、起链（待 Shao Peishen 一个字母）

🔴 **本件落档即止，链未起。** 起链前须：① 复查队列锁空闲；② 复查桥可用（`lan-status` 再跑一次，#522「起草时的现取表不作数」）；③ opener 一律走 `工具-opener生成.py` ＋ `opener块lint.py`，禁手抄；④ 批处理只能用 `pwsh` 起。

## 四、落选清单（如实点名，不做泛打回）

- **登记不合格／非 LAN 留步**：`#312`（可 Open 池提醒机制，概念提及）。
- **对外待人**：`#419`（剩 D1 凭据落位 ＋ 5.2 人眼半边，均他本人动作）、`#398`（`[S:blocked]` 机制自失效批）。
- **已完成、只待销号判断**：`#506`（见 §〇）。
- **本源缺失**：未闭合产出扫描器 形态 1（探针挂死，下批补）。

## 五、🔴 本件三处更正（2026-09-13 19:3x，读执行脚本源码后自查出来的，起链前必须按更正版走）

**更正一：端口与路径全错，SC2 不在 `:80` 也不在 `:8092`。**

- SC2 的真实端点＝**`http://192.168.100.51:8096/procurement/sc2`**（`smoke-server.ps1` 顶部写死 `$Port = 8096`／`$Prefix = "/procurement/sc2"`；`deploy-server.ps1` 注释记明 8095 被 `ZhuopinRecruitAgent` 占用，Shao Peishen 2026-08-18 改判 8096）。现取 `…/api/ping` ＝ **200**（len 67）⇒ SC2 服务活着、可达。
- 🔴 **§〇 里那条「服务级响应 `:8092/api/ping` ＝ 200」不是 SC2**——`:8092` 是**「AI 运营指挥中心」**（实读其 `<title>访问口令 · AI 运营指挥中心</title>`）。它只能证明 `.51` 上有服务在应答（on-LAN 结论仍成立），**不能证明 SC2 可达**。两者不可混用。
- ⇒ **判 SC2 在网只认 `:8096/procurement/sc2/api/ping`。** 本批第八项冒烟判据据此执行。

**更正二：执行链写错了，本次不跑 `deploy-server.ps1`。**

- `sync-to-server.ps1` —— **在【笔记本】跑**（脚本注释原文），走 **SSH/scp** 推送 ＋ 重启 ＋ 轮询验证，通用逻辑在 `5-平台底座/deploy-tools/ZhuopinDeploy.psm1`。⚠️ `AppFiles` 是**白名单**、刻意不整目录同步（`reports/` 下有真实供应商名与采购金额，`tests/` 与 `.venv` 不上服务器）。
- `deploy-server.ps1` —— **在 `.51` 上以管理员跑，且是「首次部署」用**（建 `.venv`、建计划任务、开防火墙口）。**本次是增量更新，不跑它。**
- `smoke-server.ps1` —— **在 `.51` 上跑**（自己读 `C:\sc2\.env` 取 `ZP_GATE_PASSWORD`，从不打印明文）。
- ⇒ **正确执行链 ＝ 笔记本跑 `sync-to-server.ps1`（含推送＋重启＋轮询）→ 在 `.51` 上跑 `smoke-server.ps1` → 回写证据**。§二 那格写的「三件串跑」作废。

**更正三：SSH 身份未验通，这是起链前最后一个未知。**

- `ssh -o BatchMode=yes Administrator@192.168.100.51 hostname` **空返回**（既非 hostname 也非报错）⇒ 用户名或密钥未确认。`sync-to-server.ps1` 依赖的正是这条 SSH 通道。
- ⇒ 🔴 **未验通之前不起链**：盲跑 sync 若在 scp 中途断，`.51` 上会留下半套文件，而本会话的桥今天已 3 次 60s 超时 ⇒ 正落进硬边界 ⑦ 那个「结果未知」的坑。

## 六、🔴 起链后第二轮更正（2026-09-13 19:4x，冒烟阶段实测出来的，两条都推翻本件早先的判据）

**更正四：§五「更正三：SSH 身份未验通」是误判——量具坏了，不是身份不通。**

- 复测坐实：在 Cowork 桥的 PowerShell 里，连 `ssh -V`（只打版本、必退 0）都 **EXIT=255、stdout+stderr 共 0 字节**；`ssh -vv` 同样 0 字节，落盘文件 `bytes=0`。`ssh.exe`（`C:\WINDOWS\System32\OpenSSH\ssh.exe`，存在）在这个上下文里**根本起不来**，与 `192.168.100.51`、与用户名、与密钥全无关系。
- ⇒ 本件 §五「更正三」那句「`ssh -o BatchMode=yes Administrator@192.168.100.51 hostname` 空返回 ⇒ 用户名或密钥未确认」**作废**。空返回的成因是 `ssh.exe` 启动即死，**不是认证失败**。这与 `#556` 是同一形状：**把量具失效读成了被测对象的属性**。🔑 **空返回先证量具，再证对象。**
- 旁证：他 2026-09-13 19:32 在自己的交互 PowerShell 里跑 `sync-to-server.ps1` **成功**（`NEW_PID=10224`／`CREATED=2026-09-13 19:32:06`），而该脚本全程走 SSH/scp ⇒ **SSH 通道本身是好的，坏的只是本方这条桥。**
- 另一条远程通道也查了：`.51` 的 **WinRM 在听**（`Test-WSMan` OK；对照实验 `192.168.77.77`／`192.168.100.253` 双双失败 ⇒ 量具有鉴别力），但 `Invoke-Command` 报「非域环境须配 TrustedHosts 或走 HTTPS」⇒ 要么改本机安全设置、要么送凭据，**两者都在红线内，本方不走**。
- ⇒ **冒烟这一步只能由他在自己的 shell 里代跑**（合 `代执行优先` 的例外：可粘贴命令行／特权 shell）。本方出脚本、出判据、收原始输出并回写。

**更正五：第八项判据里的「187 行」不是今天的恒等式，照原文判会误判。**

- 187 的来历是 **2026-09-08 14:23 那一刻**的订单快照：187 行全部制单于 09-07（周一），09-08 当天到缓存写入时刻为止 0 行（`4-数字员工/采购部/SC2-采购周报自动生成/取证-538-下单行数187复现与明细导出-2026-09-10.md` §一）。而今天 **2026-09-13 是 W36 窗口的最后一天**（采购口径本周 09-07~09-13 已走完）⇒ 09-08~09-13 新制单的行会进来，**下单行数大于 187 才是正常**。该取证件 §六 第 2 条本来就写明「`.51` 上那份 187 未比对」，我在 §二 把它写成了判据，是我加码。
- ⇒ 第八项判据改为**代码自带的同源恒等**（`sc2/detail.py::reconcile()`：三节 × 三窗口九格，「明细计入行数」必须等于周报同名指标，不等即抛 `DetailMismatch` 并拒绝出文件）：
  - **⑧a** `run_sc2.py detail --period <现网期次>` **退 0**，九格逐格回显「计入 N 行 ＝ 周报『…』N 行」，**逐数抄录，不写「一致」了事**；
  - **⑧b** 导出的 CSV 数据行数与 ⑧a 同格数字相等；
  - **⑧c** 记录现网 `sc2_weekly_<期次>.json` 的 `period`／`base_date`／`fetched_at`，并与 187 **对照说明**：≠187 属预期（窗口走完、新增制单），=187 也要写明（说明 09-08 后本周无新单）。
- 🔴 **判「过」只看 ⑧a／⑧b 的同源恒等，不看是否等于 187。** 187 只作历史锚点，用途是给姚祖怡解释差额来源。
- 🔑 一句话记住：**判据不能钉在某一次取数的数值上，要钉在代码保证的恒等关系上**——数值会随窗口走完而变，恒等关系不会。

**本轮对 `.51` 的写动作，如实声明（三条，仅此三条）**：⑴ 冒烟第 5 项 `POST /api/refresh` 是真实全量重算 ⇒ **会覆盖现网 `sc2_weekly_*.json` 与 `sc2_dataset_*.json`**，故 PART 0 先把两类快照整份备份到 `C:\sc2\snap.bak-<ts>`（＝快照点 ⑷，同 `B-0908` 的 `snap.bak-202609081418` 做法）；⑵ 第八项 `detail --out C:\sc2\smoke8-<ts>` 把 CSV 写进**一次性目录**，不落 `reports/`；⑶ **无部署、不跑 `deploy-server.ps1`、无计划任务改动、无防火墙改动、无配置改动**。

## 七、收工汇总（2026-09-13 20:2x，链已走完，项 1 ＝ **过**）

| 判据 | 证据 | 行内处置 |
|---|---|---|
| 冒烟七项（`smoke-server.ps1` 既有） | **0–7 八段全过**；逐段原文见 `1-转型规划/0-全景路线图/队列行日志/#538.md` 的 2026-09-13 段 | — |
| 本批新增第八项（已按 §六「更正五」改判为**同源恒等**） | `run_sc2.py detail` 退 0，三节 × 三窗口**九格全等**；交叉核「在途节 CSV 行数 ＝ 同窗口下单行数」三处自洽 | — |
| 进程真实刷新（非只信脚本打印「已重启」） | `PID=10224`／`CreationDate=2026-09-13 19:32:06`，与 sync 回显逐字相同；冒烟第 7 段 PID 未变 ⇒ 90 s 重算全程未崩 | — |
| 生产码 ＝ master | 16 件逐文件 SHA256 **DIFF=0**（master `14bbbb8`，本机工作区干净、两向 0/0） | — |
| §一 `#538` | 证据已回写（状态格 2455 → 4055 B，未触 4096 上限、无需 K2 外置） | 🔴 **状态仍 `partial`，本批未替翻**（硬边界 ⑤）；销号交 Shao Peishen 一个字母 |
| §二 批次 | `B-0913_538部署后验证回写` 已登，`⏳ 待 sweep 落库` | — |

**回滚未触发**（无失败项，链未中断）。
**刻意未清**：`C:\sc2\snap.bak-202609132008`（快照点 ⑷，3 件）与 `C:\sc2\smoke8-202609132008`（9 个 CSV）**保留到明细交付信发出后再清**。

### 留给下一批的三件

1. **未闭合产出扫描器 形态 1 这一源仍缺**（§〇 已如实标注），下批补。
2. `autopush-sc2.ps1`／`start-sc2.ps1` 在 `.51` 上存在但**仓库内无对应源件、无版本追溯**（`#361` 2026-09-07 已报，本次复现）——**只报不改**。
3. 🔴 **两条通道事实，已同步写进接力卡，别让下一会话再踩**：⑴ Cowork 桥**不能执行 `C:\Windows\System32\OpenSSH\` 下任何二进制**（`ssh`／`scp`／`ssh-keygen`／`ssh-add` 全部 exit 255、0 字节），绕法 ＝ Git 自带的 `C:\Program Files\Git\usr\bin\ssh.exe`；⑵ 桥对单次调用有 **60 秒硬顶**（tool 侧 timeout 参数无效），长步骤须 `Invoke-CimMethod Win32_Process Create` **脱钩启动 ＋ 只读轮询**，否则必然落进硬边界 ⑦ 的「结果未知」。

### 本会话自身的两处纪律失误，如实记

- **§五「更正三」是误判**：把 `ssh.exe` 启动失败读成了「SSH 身份未验通」——**量具失效当成了被测对象的属性**。这与 `#556` 同型，**本会话是第二次犯**（第一次是 19:0x 的 TCP 探针）。已在 §六「更正四」订正。
- **回写 `#538` 时顺序错了**：`acquire` 参数写错（传了并不存在的 `--why`）导致占锁失败，而随后的 `edit-row` 按协议〇.7 走了**无锁写入**并落盘；补占锁时工具按 `队列 #200` 报出「目标在上次 release 之后被直接改写」告警。同批 `append-row` 又被 §二 守卫拦下——我在 `文件清单` 里写了 `` `.51` `` 这种非路径反引号串，**这条规矩本会话第二次踩**（前一次是把状态值加了反引号）。

## 八、🔴 收工后补取形态 1 源，暴露本批三源汇集不全（2026-09-13 21:5x，`OP-0911-H` 自查）

**补取成功，扫描器根本没挂死——是我一直用同步等待去接一个跑 161 秒的活。**
`工具-未闭合产出扫描.py --no-write-state` 脱钩启动＋只读轮询，**161 秒正常跑完**（21:56:19 → 21:59:0x）。§〇 里「TUN 未关时探针挂死、三次 >60s 无返回」那句只有前半句成立：TUN 确实会让它卡住，**但本次 TUN 已关，三次超时的真因是桥的 60 秒硬顶**。🔑 **「工具挂死」与「我接它的方式不对」是两回事，此前混为一谈了。**

**补取结果（现取）**：回 LAN 感知 ✅ on-LAN 三项齐备（`ping .51` 有 TTL 回复；`:8091/api/ping` 200 统一门户；`:8093/api/ping` 200 QD-B 门禁）。**形态 1 命中 8 处 ＋ 需人读一遍 3 处。**

### 🔴 与本批 §〇「三源汇集」逐条对照：源② 也漏了

§〇 记的源② 「两份队列真身 grep『LAN 留步』」只命中 `#312`／`#398`／`#419` 三行。**形态 1 现取 8 行，其中 5 行本批从未出现、更未点名**——`#240`／`#358`／`#447`／`#454`／`#478`（全在机制环境域）。⇒ **本批 D1 判据「落选项全部点名」当时并未真正满足，我却在 §〇 写成了汇集完成。** 成因：我的 grep 关键词落在「LAN 留步」字面上，而这 5 行的留步语义写法不同（「等人／等 LAN」「记账失真」等），**字面 grep 天然漏召**——形态 1 扫描器存在的理由正是这个，而我恰恰跳过了它。

### 五行补判（如实点名，不做泛打回）

| 行 | 域/状态 | 判定 |
|---|---|---|
| **`#240`** | 机·`[S:blocked]` | 🔴 **真 `.51` LAN 留步，本批应点名而未点名**。缺口＝统一门户网关 `C:\portal-gateway\.env` 三行（`WECOM_GATEWAY_CORP_ID`／`AGENT_ID`／`SECRET`）＋重启 8090；行内「去向」原文就写 `zhuopin-lan-closeout`。**落「对外待人」桶**：Secret 只存在于 `.51` 的 `C:\apps\zhuopin-recruit-agent\.env`，属凭据落位，**须 Shao Peishen 在 LAN 上执行或经跟进信交陈承**，🔴 本方不碰凭据、不代办 |
| `#358` | 机·`[S:blocked]` | **非 `.51` 项**：审计留存分支 `origin/claude/op0908i-358-audit-retention-b52b16` 未合入 master，待定级。落「他线在办」 |
| `#447`／`#454`／`#478` | 机·`blocked`/`partial` | 三行均为 2026-09-08 「`triage-candidates` 强档命中、自陈在等人 ⇒ 记账失真」的改判行，**本质是等 Shao Peishen 拍板**，非可执行 LAN 留步。落「对外待人」 |

**需人读一遍（`needs_review`）3 处**：`#340`／`#402`／`#419`。其中 `#419` 本批已真读并点名落选；**`#340`／`#402` 本批未读**，🔴 按 skill 步骤 1「`needs_review` 批必须真读一遍再定，不得直接当登记不合格批量打回、也不得未读就放行」⇒ **本批不替判，交下一收口批真读**。

### 本批结论的修订

- **项 1（`#538`）的执行与证据不受影响**：冒烟八段、九格对账、生产码比对都是独立取证，与汇集完整性无关。
- 🔴 **但本批不能自称「三源齐、落选全点名」**——§〇 那句已由本节更正。**正确表述：源①（形态 1）收工后补取，源② 字面 grep 有漏召，两者合并后的完整清单与判定以本节为准。**
- 🔑 **留给机制的一条**：字面 grep 不能替代形态 1 扫描器；下次 D1 汇集，**扫描器跑不动就把它脱钩跑，不许用「挂死」把这一源略过去**。

## 九、needs_review 桶清空 ＋ `#240` 转跟进信（2026-09-13 22:0x，Shao Peishen 当日指示「把 needs_review 这桶也清干净；走跟进信交陈承」）

### needs_review 三处，逐条真读后定案（按 skill 步骤 1，读完才判）

| 行 | 真读结论 | 处置 |
|---|---|---|
| `#340` | 本行 2026-08-27 `OP-0827-E` 已裁「LAN 留步早已闭合，本轮属扫描器形态 1 误报」；剩余三项（两份 C05 表待陈忱确认／档三阻断效力未裁定／`8.4` 未完成）**均与 LAN 无关、均待人动作**。本次复核该结论依旧成立，不产生新动作或新证据，`[S:partial]` 维持 | ✅ **已 `--ack-form1`**（指纹 `daa083a4016799e5`，覆盖 1 个留步登记段） |
| `#402` | `OP-0827-E` 明写「本行根本没有 LAN 留步项，扫描器命中的是一段历史叙述」——那处字样出自 ② 的教训记述、说的是 `#401` 当时的状态，不是本行待办。真实阻塞＝§四 `#122` 待 Shao Peishen 答 ＋ `采购部#19` 待批准，与 LAN 无关；误报根因已登记 §一 `#422` | ✅ **已 `--ack-form1`**（指纹 `132c91c7e7d6c6b7`，覆盖 2 个留步登记段） |
| `#419` | 本批 §四 已真读并点名落选：剩余两项＝D1 凭据落位与 5.2 人眼半边，**均 Shao Peishen 本人动作**，生产载体是笔记本 worktree、不碰 `.51` | 🔴 **不 ack**——它有真实待办，只是等人，不是误报。留在「对外待人」桶 |

⚠️ **ack 的落点是 `reports/unclosed-output-form1-ack.json`，本机状态、不入库 ⇒ 换机即失**。好在两行的核实结论本来就写在各自队列行的状态格里（`OP-0827-E` 原文），**知识不丢，丢的只是机器静默**。已知不改，登记备查。

### `#240` 的处置：转跟进信 `IT部#13`，不由本批执行

Shao Peishen 当日定：**走跟进信交陈承**（而非他本人在 LAN 上落位）。已起草 `6-人才与组织/部门AI专员跟进/IT部-陈承-跟进-2026-09-13-统一门户网关env三行未落位请补写并重启.md`（＋docx），README 登记 `IT部#13（待你审，暂不占号）`，`⏳ 待你审`。

**读行读出来的实情比扫描器摘要更具体**：陈承 2026-08-12 **已经交齐四项凭据**，缺的只是「它们从未进网关」——写进了 `.51` 的 `C:\apps\zhuopin-recruit-agent\.env`，而网关只读 `C:\portal-gateway\.env`（`deploy-server.ps1` L60）且键名须为 `WECOM_GATEWAY_CORP_ID`／`AGENT_ID`／`SECRET`。⇒ 信的内容因此不是「请申请凭据」，而是「请把三行改键名落到网关那个文件并重启 8090」，外加一项事实确认（那套凭据是否就是门户应用的，若不是则须另建）。

🔴 **凭据红线照行内原文执行**：信里**不含任何凭据值**，并明写三个值不回给我方、不发聊天、不入库——请陈承直接在 `.51` 上落位。这也正是本批把 `#240` 判「对外待人」而非自行执行的理由。

🔴 **发送侧这一条我判错了，更正（2026-09-14，Shao Peishen 当面指出「是运维部AI保障群」后读配置真身自查）**：我写的「不在四个键内、不得传 `--department`」有误——`department_group_chatid_mapping.yaml` **实有五个键，第五个是 `IT`，对应「运维部AI保障群」**（2026-08-24 新增，队列 `#387` ⑵；chatid 经 intake 归档审计真实采集，2026-09-11 `OP-0910-H` 企微客户端只读反查三条独立证据坐实，取证见 `1-转型规划/0-全景路线图/队列行日志/#387.md`）。⇒ **`IT部#13` 本应带 `--department IT` 抄该群，我漏了**，实发未抄群；危害有限（信内无凭据值），但抄丢的恰是陈承最常回话的那个群——YAML 注释原话即「陈承恰恰是最常在群里回话的那一位」，`IT` 键当初就是为此而加。🔑 **根源＝我照抄了 skill §5 那张「只有 4 个键」的过时表，没读配置真身**；而该 YAML 第 30–33 行早已明写「键名是 `IT`、不是 `IT部`；`IT部` 会命中『不在映射表』分支、静默跳过」。**这不是未知缺陷、是已登记的已知坑，我当成新发现报了上去，属二次失真。** ⚠️ 派发侧从收信人格拆出的部门串是 `IT部`，与键 `IT` 不等 ⇒ 每日 09:30 自动派发同样抄不到群，这一条为真、与上述已知坑同形，须另立行修（本批只报不改）。chatid `2023458` 查名录正本逐字取得（`| 陈承 | 2023458 | IT工具部 | 男 |`），与 7 条历史 `followup_delivered` 审计记录互证。

### 本批至此的收口边界

- 三源已齐：源①（形态 1）补取完成、源② 漏召已由 §八 更正、源③ 上一批看护件已核。
- needs_review 桶已清空（两条 ack、一条留「对外待人」并说明理由）。
- 形态 1 现存 8 处中：`#538` 本批已做；`#240` 转 `IT部#13`；`#312`／`#398`／`#358`／`#447`／`#454`／`#478` 六行落「登记不合格／对外待人／他线在办」并已逐条点名。
- 🔴 **仍未销任何行**——收口≠销号，各行状态按行内原纪律，本批不替判。
