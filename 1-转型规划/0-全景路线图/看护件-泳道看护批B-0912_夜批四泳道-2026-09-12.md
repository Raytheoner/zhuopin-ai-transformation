---
title: "看护件 · 泳道看护批 B-0912_夜批四泳道"
created: 2026-09-12
status: 生效
批次: B-0912_夜批四泳道
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_夜批四泳道`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴**（`opener骨架.md`「一次粘贴」节 ⑶「默认后台起」）。**授权原文**：Shao Peishen 2026-09-12 「那就请直接启动这里的 1/2/3/4 任务；『`#312`／`#240` 会直接卡住新行』这个咬人问题也请提供解决方案」。

🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。启动走绕行：

```
Start-Process pwsh -ArgumentList '-NoProfile -ExecutionPolicy Bypass -File 0-学习与工具\工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -LogDir <目录>'
```

🔴 **硬前置（未满足不得起跑）**：本件与两份派单件（`派单件-【CC】openspec三包17决策点-2026-09-12.md`／`派单件-【CC】☒回件对账-2026-09-12.md`）**必须先落进 master**——泳道从 master 起 worktree，未落库即读不到任务正本（`OP-0912-F` 那次已实证过这条前置）。

## 一、硬边界继承

- 🔴 **不 ff master**：收工只 push 本泳道分支；主仓 ff 属 🟡 档，经『已授权待合』登记处交 `工具-待合分支巡检.ps1` 机器做，仍须他一字母。
- 🔴 **不碰 `.51`、不碰企微机器人、不碰定时任务**；**不做任何对外发送**（🔴 档永不代办，只做到可发送态即停）。
- 🔴 **不改 `工具-opener批处理执行v2.ps1`、`工具-泳道看护状态机.py`**。
- 🔴 **不追改历史**：已 ff 的提交、带日期的历史快照件一律不动。
- 🔴 **队列回写一律走 `工具-共享文档编辑锁.py`**，不裸改、不自行 commit；撞 `.git/index.lock` **不重试**，按「登记待补」写进行内。
- 🔴 **WIP 闸现状**：机制类可动 WIP 现取 **32／22 已超上限**，`release` 会拦新增 `[D:机]` §一 行。**本批四条泳道都不需要立新机制行**；确需时一律 🛑 起首（不计入 WIP），**不得写名不副实的 `WIP豁免：`**（方案 §三 判据①：豁免批量出现＝上限定错，已 0→15）。
- 🔴 **人的属性查 `6-人才与组织/人员名录-称谓与性别-正本.md`，禁从名字推断**；名录外一律「该专员／其」。
- 🔴 **收工哨兵必写**：顶格一行 `OPENER_DONE`；命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与其他 |
|---|---|---|---|
| `312-openpool-align` | `#312` 开放池推送器对齐看板判据（关一条 8.3 天老行） | `5-平台底座/wecom-aibot-service/aibot_service/open_pool_reminder.py` ＋其单测 | 零重叠 |
| `wip-gate-observe` | 措施 C 换量具告警期四条前置（4.1–4.4） | `openspec/changes/mechanism-wip-staleness-gate/tasks.md`；`1-转型规划/0-全景路线图/队列行日志/#58.md`；协议〇.9 正文（队列顶部，走锁） | 零重叠 |
| `openspec-17-decisions` | openspec 三包 17 个决策点起草 | `openspec/changes/{opener-batch-archive-precheck,tasks-zombie-item-detect,followup-closure-form-survives-backfill}/design.md` | 零重叠 |
| `reply-checkbox-recon` | 11 份 `☒` 形态回件与已回灌结论对账 | 新建 `1-转型规划/0-全景路线图/取证件-2026-09-12-☒回件与已回灌结论对账.md`（对 `6-人才与组织/` **只读不写**） | 零重叠 |

**结论：四条可并行**（并行上限 4，错峰 ≥90 s）。唯一共享资源＝两份队列文件，由编辑锁串行化；`A2` 另需锁协议〇.9 正文，同一把锁，不需额外编排。

⚠️ **与并发在跑会话的触碰区**：业务总线 `OP-0911-H`（现读其接力卡 ⏳ 表确认在办，触碰 `6-人才与组织/部门AI专员跟进/`）——`A4` 因此**只读不写**该目录，这是本批唯一一处真实相邻。拆件巡逻班（报告件）零重叠。

⚠️ **`A1` 的额外注意**：`#312` 的判据已于 2026-09-02 由 Shao Peishen 裁定 (a)，**不要重开判据讨论**；🔴 行内写死「对齐不是抄一遍判据，是让两侧共用同一个判定函数」——两侧各写一份判据即视为未完成。行内另有「完整 7 天周期轨迹未验」一项，**本棒不负责验它**，但收工须如实写明它仍未验。

⚠️ **`A2` 的额外注意**：4.3 要求**在非主 checkout 环境**（worktree 即可）真实跑一次取龄并记耗时；🔴 **>30 s 即回 design D-E 议默认超时或缓存，不得静默**。4.2 的抽查要逐条写「末次改动是实质推进／机械触碰」——这是 D-A 那条「blame 会被机械改动刷新、STALE 系统性偏低」的缓解，抽查结论要能支撑或推翻它。

## 三、泳道 opener

### A1 · `#312` 开放池推送器对齐看板判据（关一条 8.3 天老行）（OP-0912-H）

粘贴端：CC ｜ 泳道：312-openpool-align

```
[OP-0912-H]【CC】312开放池对齐
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912h-openpool-align-312`） ｜ worktree：☑（312-openpool-align，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_夜批四泳道）
读 ① `队列 §一 `#312`（`python 0-学习与工具/工具-队列查询.py --row 312 --field all`，做什么／不做什么／收工全在该行；剩余动作＝对齐 `open_pool_reminder.py::build_pool_items_from_repo()` 与看板判据、两侧共用同一判定函数、补断言测试）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912h-openpool-align-312 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912h-openpool-align-312 --done --batch B-0912_夜批四泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · 措施 C 换量具告警期四条前置（4.1–4.4）（OP-0912-I）

粘贴端：CC ｜ 泳道：wip-gate-observe

```
[OP-0912-I]【CC】WIP闸告警期
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912i-wip-gate-observe-58`） ｜ worktree：☑（wip-gate-observe，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_夜批四泳道）
读 ① ``openspec/changes/mechanism-wip-staleness-gate/tasks.md` §4（4.1–4.4 四条告警期前置，逐条做完并勾）＋ `1-转型规划/0-全景路线图/方案-措施C换量具-陈旧度闸-2026-09-11.md`（D4 迁移期与 D-A 量具偏差）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912i-wip-gate-observe-58 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912i-wip-gate-observe-58 --done --batch B-0912_夜批四泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · openspec 三包 17 个决策点起草（OP-0912-J）

粘贴端：CC ｜ 泳道：openspec-17-decisions

```
[OP-0912-J]【CC】三包17决策点
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912j-openspec-17-decisions`） ｜ worktree：☑（openspec-17-decisions，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_夜批四泳道）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】openspec三包17决策点-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912j-openspec-17-decisions --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912j-openspec-17-decisions --done --batch B-0912_夜批四泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A4 · 11 份 `☒` 形态回件与已回灌结论对账（OP-0912-K）

粘贴端：CC ｜ 泳道：reply-checkbox-recon

```
[OP-0912-K]【CC】☒回件对账
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912k-reply-checkbox-recon`） ｜ worktree：☑（reply-checkbox-recon，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_夜批四泳道）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】☒回件对账-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912k-reply-checkbox-recon --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912k-reply-checkbox-recon --done --batch B-0912_夜批四泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-L]【CC】看护看护夜批四道
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_夜批四泳道）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912L-看护看护夜批四道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_夜批四泳道-2026-09-12.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. **每泳道一行**：泳道／分支／commit／`OPENER_DONE` 还是 `PARTIAL`／单测读数（现跑，不引用）。
2. **零回归手段随句写明**：本分支跑一遍 ＋ 纯 master 起点复跑同一条命令比**失败集合差集**。🔴 已知常红两条（`ReleaseStructuralValidationTests::test_row_length_over_cap_warns_before_cutoff_does_not_block`／`test_row_length_section_four_topic_column_checked`）**不得算作本批引入**。
3. **🟡／🔴 停点列**：每条写清 (a)/(b)＋各自代价＋〔荐〕＋默认项（🔴 档与 design 审标「本项无默认」）；同类合并；末行给答复模板。
4. **待 ff 清单**：各分支 commit 与 `merge-base --is-ancestor` 现取结果；**ff 不在本批内做**。
5. **`A1`／`A2` 额外报一个数**：收工时 `STALE(7)` 与机制类可动 WIP 各现取一次（`release` 回显即可），与开工时对比。
6. 🔴 **等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0912_夜批四泳道` 现取**，不得自算。
