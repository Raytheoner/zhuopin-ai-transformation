---
title: "跨桌任务队列瘦身 · 方案（实测＋四刀）"
created: 2026-09-04
来源: Shao Peishen 2026-09-04 提问「机制环境队列 2 MB，泳道看护与调度都要读写，是否需要优化」；Cowork 环境总线瘦身线只读取证
status: 待执行
关联: 根CLAUDE.md彻底瘦身-方案-2026-09-03.md（同族：载体瘦身）；协议〇.8 每周清扫；工具-队列查询.py；#284 规则退休制
---

# 跨桌任务队列瘦身 · 方案

> 结论一句：**需要，但病根不在「行多」，在「行胖」**——§一 97 行平均 11.8 KB／行，18 行超 20 KB，最长一行 78 KB（≈3 万 tokens）。每周清扫只能去掉 ~35%，剩下的要靠「行内日志外置」和「读侧禁通读」两刀。

## 一、实测（2026-09-04 09:10 本地，沙箱只读脚本）

| 分区 | 字节 | 行数 | 已完成 | 已完成占字节 | 均值 | 备注 |
|---|---|---|---|---|---|---|
| 顶部协议〇 | 45.8 KB | — | — | — | — | 每个「读协议〇」的 session 都吞 ≈3 万 tokens |
| §一 任务看板 | **1,140 KB** | 97 | 38 | 18%（208 KB） | 11.8 KB/行 | >20 KB 18 行、>10 KB 34 行、中位 6.4 KB；**重量在活行** |
| §二 待 commit | 336 KB | 300 | **297** | 98%（329 KB） | 1.1 KB/行 | 几乎全是尸体，清扫即消 |
| §四 需定夺 | 487 KB | 87 | 23 | 36%（177 KB） | 5.6 KB/行 | 64 条 open 行均 4.8 KB |
| 合计 | **2,013 KB** | | | | | 业务场景队列另 717 KB；归档 202608 已 2.9 MB |

最胖 8 行：#328（78 KB）／#425（53）／#416（52）／#312（49）／#422（49）／#98（46）／#452（41）／#366（37）——全部是**状态列当日志用**：每次回写追加一段、从不删旧段。

**谁在吃它**（消费者盘点）：
- 精准读：`工具-队列查询.py --row N`／`--digest`（已有，队列 #441/#442）、编辑锁 `append-row/edit-row`、sweep、hook ⓐ 待领摘要、对账审计——**这些不吃 token 或只吃摘要**。
- 危险读：任何 session 对队列文件 `grep`／`Read`——一次命中就把整行 50–78 KB 灌进上下文；`Read` 全文 ≈ 70 万 tokens，直接撑爆。泳道看护、值周巡检、开工「grep 两份队列核触碰区」这三个高频动作**正是这类**。
- 协议〇：凡按规则「读队列顶部协议〇」的 session，固定 46 KB。

## 二、四刀（按省多少 × 风险排序）

| 刀 | 做什么 | 省多少 | 执行方 |
|---|---|---|---|
| **K1 立即清扫** | 跑 `zhuopin-queue-audit` §六 每周清扫尾步：§二 297 ✅ 行、§四 23 ✅ 行、§一 38 done 行迁 `跨桌任务队列-归档-202609.md`，回写高水位线 | **−714 KB（−35%）** → 1.3 MB | Cowork（本线可做，走锁） |
| **K2 行内日志外置（真正的杠杆）** | 立口径：§一/§四 单行 ≤ 4 KB；超限行把「历史回写段」搬到 `1-转型规划/0-全景路线图/队列行日志/#N.md`（原文原样、可 grep），行内只留「状态列开头＋最近一次回写＋指针」。首批 34 行 >10 KB 手工外置 | −560 KB → **~0.55 MB** | 口径＝Shao Peishen 拍；外置首批＝Cowork；机器守＝CC（release 校验族加「行长告警→阻断」，同 J3 形状） |
| **K3 读侧禁通读** | rules/队列与落库 加 🔴：判行状态只用 `工具-队列查询.py --row N`，扫池只用 `--digest`，触碰区核查改用 `--digest --grep <关键词>`（需给查询工具加 `--grep`）；CC 侧 PreToolUse 拦 `Read/Grep` 直击两份队列真身（同 hook ⓒ 形状，提示改用查询工具） | 不减文件，减**每次读入**：一次触碰区核查从 ≥50 KB 降到 <2 KB | 规则＝Cowork；`--grep`＋hook＝CC |
| **K4 协议〇去 provenance** | 与根 CLAUDE.md 同法：协议〇正文只留判据句，成因/沿革迁 CHANGELOG 附录 I；正本仍在队列顶部（口径不变） | 46 KB → ~12 KB | Cowork |

**顺序**：K1 今天（零口径变更）→ K3 规则句今天、hook 并入 #381 ⑸ 追加 ⓗ → K2 口径由他一句话拍板后首批外置 → K4 与 K2 同批。

## 三、效果估计

| 阶段 | 文件 | 典型「核触碰区」读入 | 典型「读协议〇」读入 |
|---|---|---|---|
| 现状 | 2.0 MB | 50–150 KB（grep 命中 1–3 胖行） | 46 KB |
| K1 | 1.3 MB | 同上 | 46 KB |
| K1+K3 | 1.3 MB | **<2 KB** | 46 KB |
| K1+K2+K3+K4 | **~0.5 MB** | <2 KB | ~12 KB |

## 四、其它待优化记录（他 2026-09-04 口头，暂不排期）
- 非开发项目（`C:\Users\Paul Shao\OneDrive\Projects` 下各文件夹）建成 Cowork「项目」，用项目 instructions／memory／context 隔离；插件仍全局，靠项目指令限制「用不用」（官方文档：Cowork 项目无插件级开关，仅 CC 有 `enabledPlugins`）。

## 五、执行记录
- **2026-09-04 10:35–10:55 本地（Cowork 环境总线瘦身线，一个锁窗口）**：K1 §二 301 → 7 行（294 行 ✅ 迁 `跨桌任务队列-归档-202609.md`，325 KB）；K2 首批 44 行（§一 32／§四 12）历史段外置 `队列行日志/#N.md`（原文原样，行内留首段＋末段＋md5 指针）。**机制环境队列 2,015,952 → 876,532 B（−57%）**。`工具-队列结构lint.py` 通过；`--row`／`--digest` 正常。
- ⚠️ 过程教训（已修正）：三行（#324／#326／#353）状态格内含反引号包裹的竖线或跨段代码串，朴素 `split('|')` 与按 ━━━ 切段会破坏列数／反引号配对——lint 当场抓住（列数 7／11）。修法＝按反引号感知拆列（同 lint 判据）、首尾段按反引号奇偶扩展；#324/#326 从 HEAD 原文重建。**判据：改队列格前必先跑结构 lint 作基线，改后再跑；拆列一律用反引号感知，不用朴素 split。**
- 待办：K2 机器守（release 校验加单格 >4 KB 告警→阻断）、K3 `--grep`＋PreToolUse 拦 Read/Grep、K4 协议〇去 provenance ⇒ 并入 #381 ⑸ 追加 ⓗ（CC）。
