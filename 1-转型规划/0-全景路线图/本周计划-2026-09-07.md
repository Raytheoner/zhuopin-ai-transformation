---
title: "本周计划 · 2026-09-07（周一）"
status: 在办
生成: 值周巡检（章程 v2）｜前置＝对账审计已跑
---

# 本周计划 · 2026-09-07

> ▶ **4 项定夺全部⟨就地答⟩，你 09-07 已答 `1a，2a，3a，5a`，四项均已回填队列（见 B 节表）。** 前置对账审计已跑，**队列 7 处当场修正**（D-⑨／⑪）。
> ✅ **今天两封信（`财务部#17` ＋ 第 16 封致歉更正补件）你已亲手发出，全线无第三封待发**——两张表逐行实测：⏳ 待你审／🆕 待发／⏸ 暂缓 在**数据行里各 0 条**（命中的 5 处全在图例与历史说明段），四位专员串行闸：唐燕萍 🔒／姚祖怡 🔒／陈忱 ✅ 开／陈承 ✅ 开。

## A. 本周应启动/续推（**共 1 次粘贴** ＋ 1 项他亲自做 ＋ 2 项状态同步）

**次序与并行矩阵**：**A1 触碰两份队列真身＋归档件，与所有写队列的 session 全重叠 ⇒ 独占窗口、零依赖、可立即开工**；A2 已完成不派；A3 定时触发型（等 LAN 收口）；A4 硬阻塞于他逐包给结论。**本周仅 1 次粘贴**，因原拟的另两棒一棒已做掉、一棒按队列既有约定并进 LAN 收口批。

### A1 ·【Cowork】队列每周清扫单体一棒（P0，本周内）——队列 #443

```
[OP-0907-P]【Cowork】队列每周清扫
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐（不建，只产改 .md）｜ 工作区：无 ｜ session：新开 ｜ 派出线：全景路线图线
读 ① `python 0-学习与工具/工具-队列查询.py --row 443 --section 一` → ② `CLAUDE.md` 恢复上下文，按协议〇.8 六步执行。本件为 A 类，直接开工。

做什么：
1. 逐行判读四分区已完成行，整行迁 `跨桌任务队列-归档-202609.md`（§二 实测 84 行中 65 行已 ✅）；🛑 永久关闭行、🔒 常驻台账行、明写「无限期搁置」的风险行**刻意保留不迁**。
2. 同一锁窗口内回写高水位线＝迁移**前**真实最大号（实测 §一 #493／§四 #165；顶部现写 #492，滞后 1）。

不做什么：
- 不批量过滤（2026-08-03 实测 33/70 误判）；判不动就推迟并如实登记，不降质。
- 不 commit、不碰真实库。

收工：产出登记 §二 待 commit 批次（走 `0-学习与工具/工具-共享文档编辑锁.py`，勿裸改、勿自行 commit），由落库 sweep 取活。
```

### A2 · FI2 面板改判 —— **本周不派，已由 CC 做掉**（状态同步，无需你答）

原拟派一棒 CC 落地 #423／#424。**对账当场拦下：`CC-OP-0907-N` 今日已建成并 ff 合入 master（`b0265e4`）** ⇒ 照原计划派出即重复劳动。该线收工后已再拆两行：`#494`（两条新 UI 需求）／`#495`（FI2 全量重建，三个前置问题未答不得动生产数据）。残项见 D-⑬。

### A3 ·【CC】合入批 #452 三分支 rebase+ff —— **本周不单独出 opener**

`#452` 行末段已定：三分支 rebase＋ff **留到值周巡检 ＋ LAN 收口收工后一并派 CC**（09-07 与他当面确认），若届时 `#459` D4／D5 有答复则同批派出。顺带核 `#480` 的 ff 事实（`B-0907_M` 自陈已合入，本轮未独立复核）。

### A4 ·【他亲自做】七包 design 审（按序、可缓）

六包清单在 §四 #163（已答 1a：一包一审）＋第七包 `followup-approval-cooldown-5min`（§四 #165，D1–D4）。每包按 opener 骨架【CC】式各起一 session。

## B. 需 Shao Peishen 的决策/动作清单

### ✅ 你已于 2026-09-07 答 `1a，2a，3a，5a`，四项全部回填队列

| 项 | 答复 | 落地状态 |
|---|---|---|
| **B-1** 致歉更正补件 | (a) 今天发 | ✅ **你已亲手发出**（企微「财务部AI保障组」08:55 抄送留痕，`财务部#17` 07:38 同批）。回填 `§四 #164`。🔴 **残一格**：README《补件登记》该行发送状态仍写「✅ 无需回复」（列错位假绿、且未反映已推送），待跟进线回填——审计不动权威文档 |
| **B-2** WIP 27/22 | (a) 追认、不回滚 | ✅ 回填 `§四 #142`。超限期间新立机制行须先关一条或排队；「只读 WIP 复算命令」修法仍待承接 |
| **B-3** 每周清扫 | (a) 由 A1 做完 | ✅ 回填 `§一 #443`，不再另备派单件 |
| **B-5** `#424` 重试 pass | (a) 放行 Bash 权限 | ✅ 回填 `§一 #424`。你加 `/permissions` 允许 `ssh supplychain-server` 跑 `ingest_tax_export.py` 后，CC 下次开工即跑 |

> B-2 原据：`#462`／`#478`／`#479`／`#482`／`#483` 今日激活时写「WIP 现测 **11/22**」，而用 release 自己的代码路径复算＝ **27/22**——「11」来自把 §一 按首个 `二、` 标题字样切分，提前截断（111 行 → 74 行）。

### B-4 悬置项重提（均已 `--row` 核过仍成立）

`§四 #152`（D-IT11 改口径，🔴 无默认）／`#158`（opener 守卫误拦模板与骨架）／`#160`（接力卡 J6 超限，`session接力-Phase1收口.md` 已由 8,415 涨到 **9,251 B**）／`#132`⑵／`#459` D4／D5。

> **本周剩下唯一等你的**：B-4 那五条（`#152` 无默认）。其余四项已答已落。

## C. 临期/过期红线日（7 天内）

- 🟠 **09-11（周五）**：10 份历史 8D 样本到期（`§四 #153`，交付日由该专员自定）——🔴 **不得早催**，09-11 后再核。
- ✅ **09-11 K2 行长闸（告警转阻断）已提前清仓**：开跑时全库 9 行超 4,096 B，收工 **2 行**且均已带 `行长豁免：` ⇒ **09-11 不会卡住任何在办行**。见 D-⑫。
- 🟠 **本月内**：权威排期表 2026-09 三条＝**FI2 三单匹配启动**／**SC7①**／**O2、O1 收割**。

## D. 异常与流程自检

**① WIP 27/22（超 5）**，实证见 B-2。🔴 本周新立机制行须先关一条或按协议〇.9 排队。

**② §二 积压 19 条，6 条 sweep 永远取不到**（声明文件无一在脏文件清单里）：`B-0902_47`（产出落被 gitignore 的 `reports/`，刻意）／`B-0903_80`／`B-0903_82`（**清单格里没有任何路径**）／`B-0904_10`／`B-0905_A`（已合入 `4a97d1a`）／`B-0905_F`（已 push 未 ff）。**交 CC 核对销行**。

**③ 队列回写待补未回补**：`队列回写待补/` 下 `B-0906_X` 五份 JSON（09-06 晚）**且该目录未入 git** ⇒ 降级落点自己不落库。

**④ 跟进信 README 补件登记发送状态格＝「✅ 无需回复」**（列错位的假绿），见 B-1。🔴 审计不动权威文档，只登记不改。

**⑤ §三 冻结标未摘**：质量域 Q2 8D ＋ Q4 PPAP 的解除条件「等 `质量部#9` 回件签认」——该件 08-25 已回、`质量部#12` 09-02 已回灌且 V1–V5 全部认可 ⇒ **条件已满足，上周即列观察项、本周仍无人摘标**。归质量专线。

**⑥ 高水位线滞后 1**：顶部 §一 #492，实测最大 #493，已并入 A1。

**⑦ 决策台账缺口 5 行**：`#337`／`#341`／`#436`／`#454`／`#455` 自陈「待他一次动作」而 §四 无覆盖行——**每轮都响、没人接，等于噪音**。

**⑧ 标准工作周自检**：上周 B-1 已答并执行；**B-2 答了 (a) 却没发生**（本周 B-3 重提）；B-3 两条已由 `§一 #481` 与 09-01 五项拍板承接。§五 跑偏信号无新增直接证据，但 B-5 是一次未遂的转嫁。

**⑨ 队列修正 6 处已全部落盘**（锁一度被占、后段取得，未降级）：高水位线 **492→493 当场回写**（`#423` 取号被拒即此根因）／`§四 #142`（WIP 实证）／`#156`（结论作废）／`#164`（补件假绿）／`§一 #423`／`#424`。批次 `B-0907_X1_值周巡检对账审计` 已登记。

**⑪ 🔴 本轮自撞一次并已自查修净（如实登记）**：首次写入的五处追记里，占位符 `QQ`／`BTBT` 未被替换成反引号与分隔符、**字面落进了队列**——成因是当时**只采信 `edit-row` 的「已改」回显、没有反查内容**（#255 同族）。已在同一锁窗口用 `--changes-json` 整格重写六行修净，反查实测字面残留合计 **0**、各格反引号数全偶、无裸竖线。🔑 **判据：工具回显「已改」只证明写发生了，不证明写对了。**

**⑫ K2 行长清仓（09-11 阻断闸前，值周巡检代做）**：外置 8 刀——`§一 #324`／`#456`／`#480`／`#472`／`#474`／`#424`（两刀）、`§四 #142`／`#159`，口径统一为**行内只留首段＋末段＋指针、中间段原文原样迁 `队列行日志/#N.md` 并记 md5**。`§一 #454`／`#487` 首段＋末段本身即超限、机械外置到头 ⇒ 代加 `行长豁免：` 并写明**环境线可随时撤销并做真正的段内瘦身**。另在 `#324` 留痕一处**外置前即存在的未转义竖线**（本次原样保留、未新增）。

**⑬ 同一句答复被两条会话各解读一遍**：`#424` 里同时躺着本线的「答 5a 放行权限」与 CC `OP-0907-N` 的「答 1c 不单独跑」——成因＝他一句 `1a，2a，3a，5a` 撞上两张不同的编号表（`#151` 同族）。**已收敛且被 CC 收工表独立印证**：权限照放行（但等要动 `#495` 时再加即可），重试 pass 不单独跑、并入 `#495`。全文外置在 `队列行日志/#424.md`。

**⑭ README 补件登记那格已回填**：`✅ 无需回复` → `✅ 已推送 2026-09-07（他本人手动发出）`＋更正说明。⚠️ acquire 时工具报「该文件在上次 release 之后被直接改写（约 2 行不同）」——属他手动发信期间的写入，已知情放行。

**⑩ 本周最值得记的一条**：原拟派出的 FI2 那一棒，**在派出前 12 分钟已被另一条 CC 线做完**——拦下它的不是记忆、是对账。同一行还暴露第二次「行内自陈已完成、机器字段仍 `[S:open]`」（同族＝08-31 的 `#431`／`#432`）：**只读开头得「待领」、只读行内得「已完成」，两边都不报错。**

*Last Updated: 2026-09-07*
