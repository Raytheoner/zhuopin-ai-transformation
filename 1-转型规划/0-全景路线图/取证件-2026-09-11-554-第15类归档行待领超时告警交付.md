---
title: "取证件 · #554 第 15 类归档行待领超时告警交付（2026-09-11）"
created: 2026-09-11
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #554 的建造交付报告（行内只留收口结论＋验证手段＋本指针；本件走分支随 ff 进 master。🔴 刻意不叫 `队列行日志/#554.md`——那个名字由主 checkout 的 K2 外置件占用，本会话被隔离在 worktree 写不到主 checkout，同名会在 ff 时撞 add/add）。
配套: 0-学习与工具/工具-落库sweep.py（第 15 类）；0-学习与工具/test_工具-落库sweep.py（IntakeGapRowsTests）；1-转型规划/0-全景路线图/看护件-泳道看护批B-0911_差集告警与超限清扫-2026-09-11.md
---

# §一 #554 · 交付报告（2026-09-11，CC 泳道 `554-intake-gap`／`OP-0911-A`，批 `B-0911_差集告警与超限清扫`，无头 `acceptEdits`）

> 行内只留收口结论；本件为完整原文。🔴 **本泳道只补了「让人知道」，没有补「自动拆」**——差集里的件仍然要人（或另一棒）去拆。**不得表述为「差集已修复」。**

## 一、产出（分支 `claude/op0911a-554-intake-gap` @ `ee21aea`，起自 master `0aef9e2`，已 push、未 ff）

**第 15 类常驻状态告警**（`工具-落库sweep.py::_check_intake_gap_rows`；类号现取：第 14 类已被 `#551` 旁生在主 checkout 未落库的改动占用，顺延 15）：

- **判据**：两份队列 §一 里任务列以 `企微反馈自动归档：` 开头（写手＝`aibot_service/intake.py`，本类只认前缀、不 import 它）**且**状态列 `[S:open]`、正文以「待领」起头（写手＝`queue_appender._build_status_cell`）⇒「存在且无人拆」；存在时长 ≥ `INTAKE_GAP_THRESHOLD_HOURS` ⇒ 告警。状态列缺机器字段的行**不静默跳过**：正文「待领」照算并在日志点名（宁多报不漏报）。
- **存在时长**＝now − min(⑴ 本类首见时刻 `reports/sweep-intake-gap-first-seen.json`（消失即清，同第 13 类）, ⑵ 登记列日期当天 23:59:59 本地 UTC+8)。⑵ 是可证下界：sweep 停跑几天再恢复，不会把三天前的件算成零小时。刻意不用 git 历史（`-S` 扫大文件太慢）与归档文件 mtime（listener 在 `ops/wecom-service-home` worktree 落盘、经 git 同步，mtime＝拉取时刻）。
- **骨架**：`_track_and_alert_standing_state` 出现→告警／消失→解除；行 key＝`#N`（不含会变数值）；再提醒间隔 24 h。**判据自身异常单独状态文件** `reports/sweep-intake-gap-unavailable.json`：异常轮次不判为零待领、**不动行状态**（合成一个状态文件会在崩溃轮把所有行判成「已解除」并推假解除通知，第 8 类同形）。
- **零命中也回显**：`📬 第 15 类：企微反馈自动归档行——待领 N 条，其中超 4 h 无人拆 M 条（阈值为初值，待 Shao Peishen 确认）。`
- **只读、只告警、不向队列写一个字节、不 acquire 锁、不影响退出码**；企微目标复用 `WECOM_WEBHOOK_ENV_KEY`（`#492`，单测 `test_不出现webhook键名字面量` 钉住）。
- **main() 调用位置刻意排在第 13 类之前**：第 14 类（`#551`）与本类同日分别在主 checkout／本泳道加进 main()，都贴在第 13 类之后会撞同一个 hunk；族内各类彼此独立，顺序不构成判据。函数体放在第 13 类块之后、`main()` 之前，与第 14 类函数体（第 9／10 类之间）相距 800+ 行。

## 二、🔴 阈值初值 4 h——判据阈值类，须 Shao Peishen 明确答复，不默认生效为定稿

依据：① 事件驱动路径对在途回件的拆件在一轮 sweep 内（≤1 h）完成并把行改 `[S:done]`，4 h ≈ 4 轮，足以排除「本来就要被自动吃掉」的行、不对它们误报；② 半个工作日——09:21 到件 13:2x 就响，当天仍来得及处理；③ 更短（1~2 h）会对人手拆件班正常节奏内的行也响，把本类做回噪音（`#387` 那一课：告警太多与太少同样致盲）。改值只动 `INTAKE_GAP_THRESHOLD_HOURS` 一个常量。

## 三、验证手段（逐条带命令与回显）

1. **单测** `python -m pytest 0-学习与工具/test_工具-落库sweep.py -k IntakeGapRowsTests -q` ⇒ **14 passed, 471 deselected in 0.58s**（接线／零命中回显／前缀·待领·open 三判据／两份队列都扫／缺机器字段点名／登记日下界／首见下界／两下界取更早／消失即解除并清台账／key 只含行号／24 h 不重推／判据异常不判零且不动行状态／无 webhook 字面量）。
2. **变异检验**（`reports/scratch_554_mutate.py`，逐个注释判读段后跑同一命令、跑完逐字复原并核 SHA256 一致）：M1 前缀判读 ⇒ **1 failed**；M2 阈值判读 ⇒ **6 failed**；M3 登记日下界 ⇒ **6 failed**；M4 待领判读 ⇒ **1 failed**。四组全部转红，复原哈希一致 `51241765aeb0…`。
3. **全量零回归**（`ff` 验收闸口径）：本分支 `pytest 0-学习与工具/test_工具-落库sweep.py -q` ⇒ **1 failed, 484 passed, 66 subtests passed in 824.14s**；唯一红＝`ExactMatchEndToEndTests::test_correctly_declared_file_not_flagged_ambiguous_by_duplicate_basename`，在**纯 master 导出树**（`git archive master`，`9aa5d55`；两份文件与 `0aef9e2` 逐字相同，`git diff --stat 0aef9e2 master -- 两文件` ＝ 空）同命令复跑 ⇒ **1 failed, 470 deselected in 10.42s，同名**（`#506`／`#530` 已登的长期红）⇒ 零回归成立。
4. **实弹（`#398` 口径：只跑单测证明不了 webhook 那一段）**：`reports/scratch_554_live.py` 在临时 repo_root（两份真实队列的**副本** ＋ 一条假行 `#99554 企微反馈自动归档：【第15类实弹·OP-0911-A·勿处理】…`、登记日＝前天）上跑本类，webhook 走主 checkout 真实 `.env` 的 `WECOM_WEBHOOK_URL_OPS`：
   - 06:21:15 本地（`2026-09-10T22:21:15Z`）：`待领 1 条，其中超 4 h 无人拆 1 条` → `✓ 归档行待领超时告警已推送。`（`_send_wecom_markdown` 收到 `errcode=0` 才不抛）；
   - 删掉假行再跑：`待领 0 条` → `✅ 归档行待领超时解除通知：1 项` → `✓ 归档行待领超时解除通知已推送。`
   - 🔴 **生产队列未写一个字节、生产 `reports/` 两份状态文件未被创建**（脚本末行实测 `False False`）。运维群应见两条带「第15类实弹·OP-0911-A·勿处理」字样的消息。
   - 顺带现取：**生产两份队列此刻待领归档行 ＝ 0**（09-10 五条已全部 `[S:done]`）。

## 四、边界与未尽（如实登记）

- **未 ff**（🟡，本批明令不 ff master）；`.51`／aibot 服务一字未碰（⏭️ 档，listener 路线本批明令不走）。
- **不改事件驱动既有触发条件**（🟡 改口径）——本类是它旁边的第二个消费者，不是替代。
- **`#554` 期望产出 ⑵「下线任一机制前先证接替者覆盖面是旧机制超集」那条通用判据未落载体**——它是纪律条目，落 `CLAUDE.md`／`.claude/rules/` 属 Cowork 规划桌的活，本 CC 泳道触碰区只有 sweep 及其单测；留给总线派。
- **worktree 收工自删做不到**：本棒以 `acceptEdits` 起，`git worktree add/remove` 被拒，改走 `EnterWorktree`（分支名由 `worktree-…` 手动 `git branch -m` 为 opener 指定名）；worktree `.claude/worktrees/op0911a-554-intake-gap` 留看护者删。
- **本件走分支、随 ff 进 master**（会话被隔离在 worktree，写不到主 checkout）；ff 前在 `claude/op0911a-554-intake-gap`。`队列行日志/#554.md` 是主 checkout 上由 `工具-队列行K2外置.py` 生成的立行原文外置件，与本件不是同一个文件。
- **§一 `#554` 状态待 ff 后由总线改 `[S:done]`**；阈值答复落行内。
