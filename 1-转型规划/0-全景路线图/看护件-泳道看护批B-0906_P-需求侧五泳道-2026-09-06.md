---
title: "看护件 · 泳道看护批 B-0906_P（需求侧五泳道：R1 台账 apply 前置＋登记 CLI 决策点校验＋intent 闸＋使用采样脚本＋未起草信扫描）"
created: 2026-09-06
批次: B-0906_P
派出线: Cowork 环境总线 OP-0906-W
LAN: 未探针（本批不依赖内网：A4 只写脚本＋mock 单测，实跑列「待回内网」）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0906_P`

## 〇、本批口径

**立批依据**：Shao Peishen 2026-09-06 对《端到端构建workflow优化-方案-2026-09-06》五条决议（`1-转型规划/0-全景路线图/端到端构建workflow优化-方案-2026-09-06.md` §六）。承接行两条、**零新行**（协议〇.10 并入优先）：`#439`（R1 口径点台账，今日去 🛑）＋ `#436` ⑶（第二波蓝图三向的执行件，行内末段「2026-09-06 派出」写明四件）。

**WIP 现状**（`_count_mechanism_wip` 2026-09-06 17:0x 现取）：机制类可动 **20/22**。`#439` 去 🛑 → 21/22；`B-0906_G` 激活 `#480` 后 → 22/22 顶格。**本批不再激活任何行**，五条泳道全部落在已 open 的 `#436`／`#439` 上。

🔴 **更正（2026-09-06 17:40，落档后 20 分钟自查撤回一条泳道）**：原 A5「未起草信扫描」**机制已存在**——`5-平台底座/wecom-aibot-service/scripts/draft_gap_check.py`（队列 `#245`）已并入 `工具-落库sweep.py` 第 11 类常驻告警（`#382`⑵，每轮回显「跟进信起草缺口检测」）。**A5 撤回，本批四泳道（A1～A4，全在波一）**；文件名中的「五泳道」不追改。成因＝提方案前未先查仓库已有能力（与 08-30 R3 同族），已计入 `#284`。

**先后关系**：A1 走既有派单件 `派单件-【CC】R1口径点台账-apply前置-2026-09-04.md`（只做 `tasks.md` §0 两件，串行链 CC §0 → Cowork §1.1/1.2 → CC §1.3–1.6，本批只到 §0）。A2～A5 互不重叠，与 A1 也不重叠。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界

1. 🔴 所有 `pause`／`check-heartbeat` 一律带 `--no-notify`（`#492` 运维群接线未落地）。
2. 🔴 A2／A3 改的是写侧咽喉与 openspec 校验面——**命中 openspec 三门槛即止步于 design 起草**（`pause --action-key openspec_design_review`），不代拍板。A3 若发现 validate 自定义规则做不到硬拦，改走 CI lint（`ci.yml` 新 job），不得退化成「只写进 config.yaml 提示」。
3. 🔴 A4 **不触碰 `.51`**：只写脚本＋mock 日志单测，真实采样列「待回内网」交 `zhuopin-lan-closeout`。**三档 3／7／14 天、只读、不写 README、不自动起草**——他 2026-09-06 拍死。
4. 🔴 本批不修工具、不碰他线在办行（`#443`／`#358` 只读引用／`#454`／`#480` 归 `B-0906_G`）。真实库只读、不发任何对外消息。
5. 🔴 不改队列行历史回写段，只追加。

---

## 二、波次与触碰区矩阵

| 波 | 泳道 | 承接 | 触碰区 |
|---|---|---|---|
| 1 | `439-r1-precheck` | A1 | `.gitignore`（第 32 行之后加 D7 例外）＋真仓复跑 11 条 `git check-ignore -v`（派单件 §0） |
| 1 | `436-decision-field-guard` | A2 | `0-学习与工具/工具-跟进信README登记.py` ＋ 其单测 |
| 1 | `436-intent-gate` | A3 | `openspec/config.yaml`（提示句）＋ 新 lint `0-学习与工具/工具-场景包intent闸lint.py` ＋ `.github/workflows/ci.yml` |
| 1 | `436-usage-sampler` | A4 | 新 `0-学习与工具/工具-使用度量采样.py` ＋ mock 日志单测（不读 `.51`） |
| ~~2~~ | ~~`436-unsent-letter-scan`~~ | ~~A5~~ | 🔴 撤回——机制已存在（`draft_gap_check.py`／sweep 第 11 类告警），见 §〇 更正 |

---

## 三、泳道 opener

### A1 · 439-r1-precheck——R1 口径点台账 apply 前置两件（派单件 §0）

粘贴端：CC ｜ 泳道：439-r1-precheck

```
[OP-0906-Q]【CC】R1台账apply前置
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906q-r1-precheck-439`）｜ worktree：☑（`r1-precheck-439`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_P
读 ① 队列 §一 `#439`（`python 0-学习与工具/工具-队列查询.py --row 439 --field all`，做什么/不做什么/收工全在该行；其末段指向 `1-转型规划/0-全景路线图/派单件-【CC】R1口径点台账-apply前置-2026-09-04.md`，本泳道只做该件 §0 两件）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A2 · 436-decision-field-guard——登记 CLI 校验 `决策点:` 字段

粘贴端：CC ｜ 泳道：436-decision-field-guard

```
[OP-0906-R]【CC】登记CLI决策点校验
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906r-decision-field-guard`）｜ worktree：☑（`decision-field-guard`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_P
读 ① 队列 §一 `#436`（`python 0-学习与工具/工具-队列查询.py --row 436 --field all`，本泳道＝该行末段「2026-09-06 派出」之 (i)，做什么/不做什么/收工全在该行）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A3 · 436-intent-gate——新场景 `intent.md` 前置闸（openspec validate／CI lint）

粘贴端：CC ｜ 泳道：436-intent-gate

```
[OP-0906-S]【CC】intent前置闸
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906s-intent-gate`）｜ worktree：☑（`intent-gate`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_P
读 ① 队列 §一 `#436`（`python 0-学习与工具/工具-队列查询.py --row 436 --field all`，本泳道＝该行末段「2026-09-06 派出」之 (ii)，做什么/不做什么/收工全在该行）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A4 · 436-usage-sampler——使用度量只读采样脚本（mock 先行，不碰 `.51`）

粘贴端：CC ｜ 泳道：436-usage-sampler

```
[OP-0906-T]【CC】使用采样脚本
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0906t-usage-sampler`）｜ worktree：☑（`usage-sampler`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务；真实采样列「待回内网」）｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_P
读 ① 队列 §一 `#436`（`python 0-学习与工具/工具-队列查询.py --row 436 --field all`，本泳道＝该行末段「2026-09-06 派出」之 (iii)，做什么/不做什么/收工全在该行）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### ~~A5 · 436-unsent-letter-scan~~ —— 🔴 已撤回（机制已存在，见 §〇 更正；`OP-0906-U` 号作废不复用）

---

## 三bis、看护opener（单次粘贴，Task/Agent 工具起四子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0906-P]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0906-P]【CC】看护B-0906_P
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，各子泳道自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 B-0906_P
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0906P-看护需求侧四泳道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余四条泳道由你用 Task/Agent 派发，正文里不放这一行），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉；你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0906_P-需求侧五泳道-2026-09-06.md` 全文＋ CLAUDE.md 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，
`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。

扇出与 git（硬口径，逐条照做）：
1. **并行上限 4**，超出的排下一波；同一波内起子任务**错峰 ≥90 s**。本批只有波一＝A1～A4（A5 已撤回，见看护件 §〇 更正，不要起它）。
2. 子任务收工**只 push 自己那条分支**——不碰主仓工作区、不 ff master、不在主仓 commit。
3. **主仓 ff 由落库 sweep 收尾段做**；sweep 不可用时由你在**收工时串行**做，一次一条。
4. 🔴 **你自己不 commit**；撞 `.git/index.lock` 不重试、不自造豁免，按「登记待补」写进队列行。

收工汇总（一次写全）：
5. 每泳道一行 ＋ 心跳全文 ＋ D6 停点列 ＋ 转出项 ＋「待回内网」清单（A4 的真实采样在此列）。
6. 末尾固定一段「**待你一字母**」清单：**同类合并**（三条 merge 只问一次），每项写清 (a)/(b) 与各自代价；design 审类排在 merge 类之前（A2／A3 若命中 openspec 门槛会停在 design 审）。
7. 🔴 **等待时长只认 `reports/lane-watch-state.json`**（跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0906_P` 现取），**会话不得自算**。
```

---

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0906_P` 现取，不手工数；队列回写走锁；不推企微（`#492` 未落地）。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#439` 派单件 §1.1／§1.2（五份域文件骨架，标【Cowork】） | A1 完成 §0 后由 Cowork 环境总线做，再派 CC §1.3–1.6 |
| `#447` ⑴ 口径（`决策点:` 必写） | ✅ 已落 skill `zhuopin-followup-letter` v3.8（Cowork 2026-09-06）；机器守＝本批 A2；`#447` 行仍 🛑，只剩 ⑵（事实日／补记日） |
| 8 个新场景包补 grill（`#436` ⑶① 人的那半） | Cowork 会话逐包做，首个 FI10 ＝ `开场prompt-【Cowork】FI10需求grill-首个样本-2026-09-06.md`（OP-0906-V） |
| W4「可点的判例」 | 待 `#394` outbox→aibot 中继落地后另立行 |
| `#440` R4 纪律 eval | 仍 🛑；余量优先需求侧，下一空位候选 |
