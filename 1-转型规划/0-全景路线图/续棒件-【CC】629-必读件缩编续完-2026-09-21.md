---
title: 续棒件 · 【CC】`#629` 必读件缩编 · 第二棒（承接 OP-0921-C 未完）
created: 2026-09-21
派出线: 环境总线 OP-0921-A
批次: B-0921_629续完
任务类: A 类
---

# 续棒件 · 【CC】`#629` 缩编续完（§一 #629）

## 〇、🔴 起点（先读本节，与 opener【设置】行冲突时以本节为准）

**不新建分支、不新建 worktree。** opener【设置】行的分支／worktree 字段只是格式占位。

- 干活地点：`C:\Dev\zhuopin-ai\.claude\worktrees\op0921c-629-readfile-slim`（**已存在**）
- 分支：`claude/op0921c-629-readfile-slim`（**已存在**，续用，不另起）
- 已有两笔提交：`9061536d`（sweep 体量守＋根 CLAUDE.md §3 减字＋尖峰榜）、`5a4d49cb`（缩编检查点，工作区已干净）
- 第一棒是被 600 秒后台任务上限掐断的，**进度没丢**，都在上面两笔里。**不要从头重做。**

## 一、现取字节（2026-09-21 实测，直接用）

| 对象 | master | 本分支现取 | 必达 |
|---|---|---|---|
| `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` | 45,119 | **32,247** | 继续减（棘轮基线＝完工实测值） |
| `.claude/rules/` 合计 | 43,388 | **37,241** | 🔴 **≤30,720**（还需再减 ≥6,521 B） |
| 根 `CLAUDE.md` | 12,110 | **12,077** | 🔴 **≤12,077，只准更小**（已达标，别让它涨回去） |

`.claude/rules/` 分项现取：`两桌同步与取证.md` 8,564 ｜ `环境保障线.md` 7,106 ｜ `队列与落库.md` 7,263 ｜ `跟进信与专员.md` 5,159 ｜ `场景建造与合规.md` 4,906 ｜ `文档与全景治理.md` 4,243（**本份第一棒没动过**，是下一刀的首选）。

CHANGELOG 附录编号已分配好、**无撞车**：附录 Q＝rules 五份瘦身前存档，附录 R＝SKILL.md 瘦身前存档。新增迁出内容并进这两个附录，不要另起字母。

## 二、方案闸（已过，照做即可）

1. 🔴 **禁起后台子任务／并行 Agent**——第一棒正是因此被 600s 上限掐断。本棒**顺序、inline 做完**：先 rules，再 SKILL.md，再改闸，最后提交。
2. **判据粒度**：体量一律 `(Get-Item <路径>).Length` 现取，禁 `cat` 正文比对；每减完一份只回显新旧字节数。
3. **narrow**：回归只跑受影响测试类 node-id，禁 `-k`，禁跑整份回归；输出重定向到文件只读结论行。
4. **产出落点**：改动全部提交到 `claude/op0921c-629-readfile-slim` 并 push。

## 三、做什么

### ⑴ `.claude/rules/` 合计减到 ≤30,720 B

继续**结构性迁出，不是删**：成因／沿革／实证故事迁 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md` **附录 Q**，原处留一句指针（写明迁到附录 Q 哪一段），保证原文仍可 grep。正文只留「触发 → 动作 → 判据／指针」。
优先顺序：`文档与全景治理.md`（4,243，第一棒未动）→ `两桌同步与取证.md`（8,564）→ `队列与落库.md`（7,263）→ `环境保障线.md`（7,106）。

### ⑵ `SKILL.md` 继续减

同一手法，迁 **附录 R**。正文只留：触发口令 → 各档动作 → 判据（判据能指针化的就写「现取 `工具-泳道看护状态机.py criteria`」）。无硬性目标字节，减到正文里再无「为什么这么定／哪天撞过什么」为止。

### ⑶ 🔴 撤掉第一棒新立的两个字节阈值口径，改成棘轮

第一棒在 `0-学习与工具/工具-落库sweep.py` 里新立了两个**新阈值口径**，越权（「改口径阈值」属 🟡 档，永不代办）：

- 第 837 行 `CLAUDE_MD_RULES_BYTE_CAP = 8 * 1024`（rules **单份** 8,192 B）
- 第 849 行 `LANE_WATCH_SKILL_BYTE_CAP = 24 * 1024`（SKILL.md 24,576 B）

**改法**：两者都换成**棘轮语义＝只降不升**，与既有 `ROOT_RATCHET_*`（同文件 824–852 行）同构——基线取**本棒完工后的现取字节数**（机械取值，不是拍板定数），代码注释里写死「基线＝2026-09-21 `OP-0921-F` 完工实测 N B」。

- rules **合计** 阈值 `CLAUDE_MD_RULES_TOTAL_BYTE_CAP = 30,720` 是**既有口径，保留不动**。
- rules **单份**这一层：若认为仍需守，一并改成棘轮（每份各自的完工实测值），**不得留任何新的固定字节常量**。
- 第一棒新加的两道 reject-consumer 与 11 条单测**保留**，只把它们判定所依据的常量换成棘轮基线，单测同步改并跑绿。

### ⑷ 收尾

- 根 `CLAUDE.md` 提交前再量一次，**必须 ≤12,077 B**（第一棒工作区里一度涨到 12,146，查清那 69 B 是什么、该减的减掉）。
- narrow 回归受影响测试类，贴结论行。
- 提交并 push 到 `claude/op0921c-629-readfile-slim`。

## 四、期望产出（缺一个就不许写 `OPENER_DONE`）

1. `.claude/rules/` 合计 **≤30,720 B**（贴逐份新旧字节数）
2. `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` 进一步变小（贴新旧字节数）
3. 根 `CLAUDE.md` **≤12,077 B**（贴字节数）
4. `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md` 附录 Q／R 承接全部迁出内容
5. `0-学习与工具/工具-落库sweep.py`：两个新固定阈值常量已**不存在**，改为棘轮；`0-学习与工具/test_工具-落库sweep.py` 单测同步并绿
6. 收工贴 `git diff --name-only master...HEAD` 的输出

## 五、不做什么

1. 🔴 **禁起后台子任务／并行 Agent**（见 §二.1）。
2. 🔴 **不得新立任何固定字节阈值口径**；既有 30,720／12,288 两个保留不动。
3. 🔴 **不得把内容直接删掉充作缩编**——迁出必须在 CHANGELOG 附录里逐句可 grep，原处留指针。
4. 🔴 不碰 `0-学习与工具/test_工具-落库sweep.py` 里 `#632` 那一棒（分支 `claude/op0921b-632-sweep16-triage`）已修的夹具以外的东西；两棒都改这份文件，**只加不改他人区块**。
5. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
6. 🔴 收工只 push 本分支，**不碰主仓、不 ff master**；红线照旧永不代办（🔴／⏭️／🟡 三档）。
