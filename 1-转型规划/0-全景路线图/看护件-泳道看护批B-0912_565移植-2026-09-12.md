---
title: "看护件 · 泳道看护批 B-0912_565移植"
created: 2026-09-12
status: 生效
批次: B-0912_565移植
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_565移植`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴**（`opener骨架.md`「一次粘贴」节 ⑶「默认后台起」）。**单泳道批**，无并发编排需求。

🔴 **不得用 `-Detach`**：它在 PS 5.1 子进程下必炸（`#567` 已立行取证）。本批启动走绕行：

```
Start-Process pwsh -ArgumentList '-NoProfile -ExecutionPolicy Bypass -File 0-学习与工具\工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -LogDir <目录>'
```

派出会话轮询日志目录的 `summary.txt` / `exit.txt`，**一次返回结果与停点**，不中途拿日志碎片打扰他。

🔴 **授权原文**：Shao Peishen 2026-09-12 11:4x 回「1a，2a，3a」——`2a` ＝ `OP-0912-E` 报告定夺 2(a)「全量移植」，本批即其执行件。

## 一、硬边界继承

- 🔴 **不 ff master**：收工只 push 本泳道分支；主仓 ff 属 🟡 档、由派出会话经『已授权待合』登记处交 `工具-待合分支巡检.ps1` 机器做，仍需他一字母。
- 🔴 **不改 `工具-opener批处理执行v2.ps1`**；**不改 `工具-泳道看护状态机.py` 任何函数**（本包 design 决策点 0 钉死）。
- 🔴 **不实现方案 B**（决策点 5 已于 2026-09-12 裁定不落地，`OP-0912-E` 定夺 3；design.md 决策点 5 段已落字结案，**不要再动那一段**）。
- 🔴 **不碰 `.51`、不碰企微机器人、不碰定时任务**；不做任何对外发送。
- 🔴 **不追改历史**：已 ff 的 `6733cb4`／`b64e442` 原文不动；带日期的历史看护件不动。
- 🔴 **队列回写一律走 `工具-共享文档编辑锁.py`**，不裸改、不自行 commit；撞 `.git/index.lock` 不重试、按「登记待补」写进行内。
- 🔴 **收工哨兵必写**：顶格一行 `OPENER_DONE`；命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 行 | 触碰区 | 与其他 |
|---|---|---|---|
| `565-realvalue-port` | `#565` | `0-学习与工具/工具-opener生成.py`／`工具-opener块lint.py` ＋两份单测；`0-学习与工具/skills源码/zhuopin-lane-watch/`；`1-转型规划/0-全景路线图/opener骨架.md`；`openspec/changes/lane-watch-heartbeat-wiring/design.md` | **本批单泳道，无并发对手** |

⚠️ **与并发在跑的会话的触碰区**：业务总线 `OP-0911-H`（跟进信侧，零重叠）、拆件巡逻班（报告件，零重叠）。唯一共享资源＝两份队列文件，由编辑锁串行化。

⚠️ **开工前置（泳道自己先核）**：`git log --oneline -1 master` 须为 `07456e5` 或更新（`#566` 称谓守卫已于 2026-09-12 11:43 ff 入 master）；`design.md` 决策点 5 段须已含「design 审已裁」段——两者都不含，说明起点不对，**立刻 `OPENER_PARTIAL` 停住报出**。

## 三、泳道 opener

### A1 · #565 第二棒全量移植＋openspec 决策点 1 修订（OP-0912-F）

粘贴端：CC ｜ 泳道：565-realvalue-port

```
[OP-0912-F]【CC】565真值移植
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912f-heartbeat-realvalue-565`） ｜ worktree：☑（op0912f-heartbeat-realvalue-565，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_565移植）
读 ① `1-转型规划/0-全景路线图/派单件-【CC】565第二棒全量移植-2026-09-12.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳跑命令写，不自己拼路径：开工 1 分钟内跑 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane <泳道标识，无更明确约定时用本任务队列行号，如 561> --text "<一句话：在做什么>"`，每里程碑追加一行；收工带 `--done --batch <本批次名>`（🔴 `--batch` 不能漏——`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；不接受 `--repo-root`／`--heartbeat-file`，工具没有这两个参数；预计等待超过 10 分钟须补写一行「仍在等 X，预计还要 N 分钟」（`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` 步骤 4／5.6；队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-G]【CC】看护看护565移植
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_565移植）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912G-看护看护565移植。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_565移植-2026-09-12.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. 分支名＋commit＋`merge-base --is-ancestor` 现取结果；**ff 不在本批内做**。
2. 派单件 §一.3 的三条「必须成立」逐条写结论**与手段**。
3. 零回归写成「分支侧失败集合 ⊆ 纯 master 同命令失败集合，逐条复现」的形态，读数现跑不引用；已知常红三条不得算作本棒引入。
4. openspec 修订段的落点（文件＋节）与原文摘句。
5. 🟡 停点（如有）：(a)/(b)＋各自代价＋〔荐〕＋默认项；末行给答复模板。
6. 🔴 等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0912_565移植` 现取，不得自算。
