---
title: "审读件 · env-anchor-collapse 剩余两项（2.3.5 最后一跳／3.5.1 切 --enforce）"
created: 2026-09-12
status: 待签认
来源: openspec/changes/env-anchor-collapse/tasks.md 2.3.5 与 3.5.1（2026-08-27 扣住；队列 §一 #354）
用途: 把两项 🟡 的选项与代价做熟成可一字母作答的问句；本件只做现取与可读提取，不落地、不切、不勾、不改 design/proposal 一个字
派出线: Cowork 环境总线 OP-0912-E（批 B-0912_env-anchor，派单件 `派单件-【CC】env-anchor两项选项做熟-2026-09-12.md`）；执行 OP-0912-P【CC】
---

# 审读件 · `env-anchor-collapse` 剩余两项

> ▶ 本件 **2 项定夺，均 🔴 无默认**，均⟨就地答⟩（答完由下一棒 CC 勾 tasks／归档，本棒不代勾）。
> 🔴 **一句总判（现取后才看出来的）**：两项都不是「没做完」，是 **tasks.md 落后于事实**——
> `2.3.5` 的三项核验 **2026-08-31 已在队列行 `#354` 里写「就此完成」**，勾没勾回 tasks；
> `3.5.1` 要「切」的那个开关 **CI 里从 2026-08-18 起就一直是 `--enforce`**（`ci.yml:193`），判据二自 08-27 落 master 起就在 CI 阻断，**无可切之物**。
> 两次「仍不切」判断（08-27、09-06）**都没有去看 `ci.yml`**——本件每条事实都附手段，可当场证伪。

---

## 1 · `2.3.5` 的「真实推送」那一半，用什么证据判完成？　🔴 无默认

### 2026-08-27 的理由 vs 今天现取

| | 2026-08-27（OP-0827-B 扣住时） | 今天现取（2026-09-12，本机 off-LAN） |
|---|---|---|
| 为什么扣 | SC2 入口按期号幂等（`ReviewStore.mark_pushed`），`2026-W34` 票未用；当天强推 ⇒ 次日 08-28 20:00 自然触发必被跳过、姚祖怡收到周四数据 | —（理由已随 08-28 20:00 自然触发消失） |
| 解锁条件 | 等 08-28 20:00 自然触发后核三项：① `LastTaskResult=0` ② `pushed` 转 true ③ `outbox.pending()` 回落 0 | **三项 2026-08-31 已核过**（OP-0831-L on-LAN，SSH 只读）：`Sc2WeeklyAutoPush LastRunTime=2026-08-28 20:00:00｜LastTaskResult=0`；`sc2_publish_state.json` `2026-W34.pushed=true`；outbox 两条均 `delivered:true`、`pending()=0` ⇒ 队列行原文「**三项验证全过，`2.3.5` 就此完成**」 |
| tasks.md | `- [ ] 2.3.5 🟡 做了一半，最后一跳刻意扣住` | **仍是 `[ ]`**——队列行与 tasks 分叉 12 天，5.3 顶部仍写「解锁条件剩两项」 |
| 最后一跳有没有真到群里 | 未知（当天不发） | **本机 aibot 审计**有 4 条 `outbox_relay_delivered`，`ack.errcode=0`：`2026-W34` 群＋私信（2026-08-31 14:20:31／:32 +0800）、`2026-W35` 群＋私信（2026-09-07 09:05:45／:47 +0800）。**两期、四条、企微服务端受理**——不是「写进 outbox 了」，是「群里收到了」 |
| 不能现看的 | — | `.51` 侧 `sc2_publish_state.json`／outbox 文件本轮 off-LAN 不可读（近 24h `outbox_relay_scan_failed` 255 条，SMB 路径不可达，末条 2026-09-12 19:52 +0800）；`2026-W36`（09-11 20:00 应写）**本机无投递记录**——是没写还是没送到，本机不可知 |

### 选项

- **(a) 采信 08-31 三项核验 ＋ 本机审计 W34/W35 四条 `errcode=0` 投递记录，判 `2.3.5` 完成，勾上〔荐〕**　会发生：下一棒 CC 勾 `2.3.5`，`5.3` 归档条件少一项；tasks 与队列行重新对齐。代价：① 三项核验是 **12 天前**的 on-LAN 结果，今天 off-LAN 无法再看 `.51` 一眼；② 其中「`pending()=0`」一条是 08-31 由同批 `#394` 的 SSH 只读核对**间接引用**、非 `2.3.5` 自跑——但本机审计的 `delivered` 记录是同一件事的**另一端物证**（中继侧只在 `errcode=0` 后才写这条），两端互证。
- (b) 不采信，等下次 on-LAN 对 `2026-W36` 再跑一遍三项核验后再勾　会发生：又一个无日期的时点约束，`5.3` 继续挂。代价：**增量为零**——三项核验 08-31 已做过一次、结论已进队列行，再做一遍只是重复；且 W36 能不能送到取决于笔记本何时回 LAN（中继在笔记本上），**与本包改的「凭据从哪来」无关**。
- (c) 改判「真发」不属本包验收范围（本包只改凭据锚定；`autopush --mode real --no-push` 真实取数已证明凭据链路；发送与中继是 SC2／`#394` 的功能），勾上并注明范围收窄　会发生：同 (a) 勾上。代价：**事后收窄 08-27 写死的验收口径**（「四关」），须留痕；而事实上证据已经齐了，收窄没有必要——(c) 只在证据不齐时才值得选。

### 手段（本件现取，非引用）

- 队列行：`python 0-学习与工具/工具-队列查询.py --row 354 --field all` → 状态格末段 2026-08-31 `OP-0831-L` 原文（上表逐字）。
- 本机审计：`python` 逐行 `json.loads` `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`（mtime 2026-09-12 19:52），过滤 `action` 以 `outbox_relay` 起首：`delivered` 4 条（上表）／`started` 23 条／`scan_failed` 1554 条（首条 2026-08-31 22:10 +0800，近 24h 255 条）／`disabled` 7 条。首条 `outbox_relay_started`（带 `.51` 路径）＝2026-08-31 14:20:30 +0800，**1 秒后 W34 两条即投递** ⇒ W34 从 08-28 20:00 写 outbox 到 08-31 送达的 2 天 18 小时，是**中继 `#394` 当时还没建**，不是本包的锅。
- 中继语义：`5-平台底座/wecom-aibot-service/aibot_service/outbox_relay.py` 模块 docstring ②「投递成功才置 `delivered:true`」、判据复用 `delivery.py::_assert_ack_accepted`（`errcode≠0` 即失败）。

---

## 2 · `3.5.1`「切 `--enforce`」——现取发现无可切之物，这一条怎么处置？　🔴 无默认

### 2026-08-27 的理由 vs 今天现取

| | 2026-08-27（OP-0827-B） | 2026-09-06（`happy-thompson`，commit `ae3b428`→master `3c091c9`） | 今天现取（2026-09-12 19:56 +0800） |
|---|---|---|---|
| 前置一「`.51` 冒烟全过」 | 已满足 | 已满足 | 已满足（不变） |
| 前置二「连续一周告警为 0」 | **当天被重置**：cherry-pick 落地后门禁扫出第 16 处 `probe_418_invoice_gap.py`（`91940f8` 08-27 10:03 修）⇒ 最早 09-03 | 门禁又扫出**第 17 处** `SC7/scripts/leadtime_median.py:107`（出生 `da2cb38` 09-05 00:32，修 `3c091c9` 09-06 19:26）；该会话称「两个前置今日同时成立」——**按 08-27 自己定的规则，计时应从 09-06 19:26 重新起算** | 自 `3c091c9` 起 master 全链**连续 0**，至今 **6 天 0 小时 30 分**；按 08-27 规则期满 ＝ **2026-09-13 19:26**，差 23.5 小时 |
| 「切」的对象 | 默认认为 CI 在告警模式 | 同上（「切换 CI 本身＝改口径 🟡 档，本次未执行」） | 🔴 **`ci.yml:193` 自 2026-08-18 `f1f1c52` 起就是 `python 0-学习与工具/工具-引导样板lint.py --enforce`**；apply 提交 `4df8248` 未动 `ci.yml`（23 个文件里没有它）⇒ 判据二一进 master 就在 CI 阻断。**全库只有 CI 这一处调用该脚本**，不存在任何告警模式的调用方 ⇒ 「切」没有对象 |
| `--enforce` 现跑 | 461 个 `.py` 全过 | 581 个 `.py`，0 违规 | **647 个已跟踪 `.py`／70 个含引导块／判据一 0／判据二 0，退出码 0**；`test_工具-引导样板lint.py` 27 passed（含 `test_env锚定存量已清零`）；`test_env_anchor.py` 27 passed |
| CI 真的拦了吗 | — | — | ⚠️ **拦不住**：第 17 处在 master 上活了 **42 小时 54 分**（`da2cb38`→`3c091c9`），期间 CI job `平台底座引导样板 lint` 持续红、日志逐字「✗ … 违规【阻断】… exit code 1」，**但 master 无分支保护**（`gh api …/branches/master/protection` → 404，`rulesets` → `[]`），红灯不挡 push；且 09-04 23:54→09-09 01:18 同一 job 因**判据一** `_coverage_point_ledger_helpers.py` 也红着，两族混在同一盏红灯里 |

### 选项

- **(a) 认定 `3.5.1` 已被 CI 既有 `--enforce` 事实闭合：勾上，条目下注明「无可切之物——`ci.yml:193` 自 08-18 即 `--enforce`，判据二自 08-27 落 master 起在 CI 阻断」；不改 `ci.yml`、不改脚本；前置二「连续一周为 0」作废〔荐〕**　会发生：下一棒 CC 勾 `3.5.1`，与 `2.3.5` 一起解锁 `5.3` 归档。代价：① 推翻 08-27／09-06 两次「仍不切」——两次都没查 `ci.yml`，须在 tasks 留痕；② 前置二存在的理由（「门禁上线第一天就红只会被习惯性忽略」）**已无从谈起**——门禁早就上线、也早就红过（43 小时），作废它不是放宽，是承认它从没起过作用；③ 「CI 红灯拦不住 push」是**分支保护／推送闸**的问题，不属本包，只作观察项登记进 `#354` 末段、**不立新行**（WIP 上限）。
- (b) 保留原意，把「切」落到唯一还存在的告警形态——改脚本默认为阻断（去掉 `--enforce` 开关或默认 True），等 2026-09-13 19:26 一周期满后再勾　会发生：改 `工具-引导样板lint.py`＋单测＋docstring＋`门禁判据族清单.json` 引用，CI 命令行同步去掉开关。代价：**改的是零调用方的默认值，纯形式**；多等 1 天；比 (a) 多动 4 个文件、换来的行为差异为 0。
- (c) 改判 `3.5.1` 为「推送前闸」（pre-push hook 或分支保护 required check），依据＝43 小时实证 CI 阻断拦不住 ff push　会发生：新机制 ⇒ 新 §一 行，机制类 WIP 超上限须 🛑 起首；分支保护会同时卡住**今天仍红着的另外 4 个 job**（CLAUDE 进度段 lint／凭据扫描／取证件回显 lint／aibot 测试）⇒ 等于全仓停推。代价：**超出本包范围**，挂在 `3.5.1` 上会让一个 08-25 就该归档的包再等一个新机制；若真要做应另立、不该压在这里。

### 手段（本件现取，非引用）

- CI 接线：`git log -L193,193:.github/workflows/ci.yml` → 该行出生于 `f1f1c52 2026-08-18`；`git show 4df8248 --stat` → 23 文件，无 `ci.yml`；`git log -- .github/workflows/ci.yml` 08-18 之后 6 笔均与本包无关。
- 调用方：`Grep 引导样板lint\.py --glob *.{py,ps1,yml,json}` → 唯一执行调用 ＝ `ci.yml:193`（其余全是 docstring／清单引用）。
- 现跑：`python 0-学习与工具/工具-引导样板lint.py --enforce` → 「✓ … 647 个已跟踪 .py，其中 70 个含引导块 … 零『向上逐级找 .env』手抄形态」，`exit=0`；`python -m pytest 0-学习与工具/test_工具-引导样板lint.py -q` → 27 passed。
- 全链扫描：临时脚本对 `git rev-list --first-parent 4df8248^..master`（**1097 笔**）逐笔 `ls-tree` ＋ `cat-file --batch` 取全部 `.py` blob，调 `check_env_anchor`／`check_file` 纯函数（按 blob 缓存）：判据二红 **71 笔**＝`probe_418_invoice_gap.py` 1 笔（`4df8248` 自身）＋ `leadtime_median.py` 70 笔（`da2cb38`…`9ffddfd`）；`3c091c9` 起 0。判据一红 242 笔（按作者日期：09-02 1／09-06 12／09-07 91／09-08 70／09-09 68），全部 `_coverage_point_ledger_helpers.py`。脚本已删，不入库。
- CI job 逐日结论：`gh run list --workflow ci.yml --branch master --created ">=2026-08-26"`（495 次）每日首末各一次 `gh run view <id> --json jobs` 取 `平台底座引导样板 lint` 结论：08-26→09-04 00:18 success；09-04 23:54→09-09 01:18 failure；09-09 17:18→09-12 11:38 success。失败样本 run `34290449197` job 日志：「判据一 · sys.path 引导（#345），1 处 —— `_coverage_point_ledger_helpers.py`」。
- 分支保护：`gh api repos/Raytheoner/zhuopin-ai-transformation/branches/master/protection` → HTTP 404 "Branch not protected"；`…/rulesets` → `[]`。
- 时刻：本机 `date` → `2026-09-12 19:56:43 +0800`；`git log -1 --format='%ai / %ci' 3c091c9` → author `19:02:51`／committer `19:26:30 +0800`——本件一律取 **committer 时刻**（＝落 master 的时刻；`da2cb38` 同理 author 09-02 22:21、committer 09-05 00:32），与 `.claude/rules/两桌同步与取证.md` §三 引用的 `19:02:51` 是同一笔、不同基准。

---

## 3 · `--enforce` 现跑的违规条数与分布（现取，供两问共用）

| 范围 | 判据一 `sys.path` | 判据二 `.env` | 手段 |
|---|---|---|---|
| 工作区 HEAD `f9875b6`（647 个已跟踪 `.py`） | **0** | **0** | `--enforce` 退出码 0 |
| master 全链 `4df8248`→`f9875b6`（1097 笔） | 242 笔红（单一文件，09-02→09-09） | 71 笔红（2 个文件：`probe_418` 1 笔 08-25／`leadtime_median` 70 笔 09-05 00:32→09-06 19:18） | 全链扫描脚本 |
| 判据二最近一次红→今 | — | `3c091c9` 2026-09-06 19:26 起连续 0，6 天 0.5 小时 | `git log -1 --format=%ci` |

🔑 **两次「存量重新长出来」都被门禁自己抓到（第 16 处 08-27、第 17 处 09-05）**——「人工清单会过期、门禁不会」在同一个包里应验三次；但**抓到 ≠ 拦住**，第 17 处红着活了 43 小时，因为 CI 红灯不是推送闸。这一条是观察项，不是本包的决策项。

---

## 扣住期间世界变了什么（与两项相关的三条）

1. **`#394` outbox 中继 08-31 建成**——`2.3.5` 那「写进 outbox ≠ 群里看到了」的最后一跳，此后有了本机审计这条独立物证；08-27 时它还不存在。
2. **`#553`：主仓 ff 不再由 sweep 做，改由『已授权待合』登记处＋`工具-待合分支巡检.ps1` 机器做**——09-06 那笔第 17 处修复因此在分支上等了他一个字母才进 master（`ae3b428`→`3c091c9`），「存量清零」与「master 上清零」之间从此有一个等授权的窗口，**前置二的计时应以 master 上的提交时刻为准**（本件即按 `3c091c9` 算）。
3. **`#565`：泳道心跳／`--done --batch` 纪律**——与两项内容无关，只约束本棒收工方式，已照办。

---

## 怎么答

- 两项 🔴 **均无默认**（改口径判据 🟡 档；且本棒收工即结束，不答＝一直挂）。
- 两项都答 **a** ⇒ `5.3` 归档解锁，由下一棒 CC 勾 `2.3.5`／`3.5.1`（各附本件指针）后跑 `/opsx:archive env-anchor-collapse -y`；本棒不代勾、不归档。
- 第 2 项若取 (c)，请一并说是**另立一行**还是**压在 `3.5.1`**（起草方建议另立、且不在本包内）。

```
1a，2a
```
