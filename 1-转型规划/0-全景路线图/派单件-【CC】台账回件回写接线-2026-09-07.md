---
title: "派单件 ·【CC】台账回件回写接线（coverage-point-ledger tasks §2bis，队列 §一 #439 子项）"
created: 2026-09-07
派出线: Cowork 环境总线 OP-0906-W
status: 待你在 CC 里起（可与 `B-0907_R`／`B-0907_Y` 并行，触碰区零重叠）
---

# 派单件 ·【CC】台账回件回写接线

## 〇、为什么派

Shao Peishen 2026-09-07「上图流程缺口那段确保修复」：`采购部#21` 回件 30 分钟自动拆完、台账零回写。成因＝本包 §6.2③ 要求「回件解析 → 回写台账」，但 §1–§7 无一条建造它；拆件 skill 无台账步骤、台账无 CLI。已补登 `openspec/changes/coverage-point-ledger/tasks.md` §2bis（2b.1–2b.5），本件只做 2b.1–2b.4。

## 一、并行矩阵

| 会话 | 关系 | 依据 |
|---|---|---|
| `B-0907_R`（看护五泳道） | 可并行 | R 触碰 sweep／编辑锁／状态机代码；本件不碰 |
| `B-0907_Y`（合审后十泳道） | 可并行 | Y 的 A8 碰 `工具-跟进闸查询.py`、A9 碰 `工具-跟进信README登记.py`；本件 2b.3 明令避开这两文件 |
| `OP-0907-M`（FI8 grill） | 无关 | 只写 `intent.md` |

## 二、opener

粘贴端：CC ｜ 泳道：ledger-writeback-439

▶ 首次派出：[OP-0907-AK]

```
[OP-0907-AK]【CC】台账回件回写接线
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907ak-ledger-writeback-439`）｜ worktree：☑（`ledger-writeback-439`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0907AK-台账回件回写接线。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `openspec/changes/coverage-point-ledger/tasks.md` §2bis（缺口实证＋2b.1–2b.5 逐条判据）→ ② `CLAUDE.md` §4 路由表命中 `.claude/rules/场景建造与合规.md`＋`5-平台底座/CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 按序做 2b.1 → 2b.4（2b.5 归 Cowork＋他，不做）：CLI `cli.py` → 拆件 skill §三 4bis（源码件＋定时任务副本两处同步，安装件位置由 `0-学习与工具/工具-定时任务源码备份.py` 取）→ 发送侧 `在途` 接线 → 两组断言测试各配反向对照。
2. 每完成一条即在 `tasks.md` 该条打 `[x]` 并写一行「验证手段」（pytest 文件名＋passed 数、`openspec validate --strict`、CLI 实跑 exit 码）；只写有手段的验证。
3. 收工：push 本分支、不 ff master（ff 属 🟡，由 Shao Peishen 一字母）；队列 §一 `#439` 状态列**追加**一段收工记录（走 `0-学习与工具/工具-共享文档编辑锁.py`，行长 K2 ≤4 KB，超即按先例外置到 `1-转型规划/0-全景路线图/队列行日志/#439.md` 并留 md5 指针）；改动文件登 §二 待 commit 批次。

不做什么：
- 不碰 `.51`、不注册／改定时任务、不真实发送任何跟进信（发送侧接线只改代码＋测试，用 mock 验）。
- 不改 `0-学习与工具/工具-跟进闸查询.py`（`#482` 在途）与 `0-学习与工具/工具-跟进信README登记.py`（`#447` 在途）；若 2b.3 非改不可，停下来写进 `#439` 状态列「待总线定」，不自行决定。
- 不改 SCHEMA 字段、不改 `models.py`／`store.py` 的写盘路径；`已签认` 不得有任何无 `evidence` 的通路（D5）。
- 不动 §2 回溯拆点、不给采购域建点（那是 Cowork 的活）。
```
