---
title: "看护件 · B-0916_581模型字段接线（单泳道）"
created: 2026-09-16
批次: B-0916_581模型字段接线
派出线: Cowork Token 优化总线 OP-0916-T
status: 已获授权起跑（Shao Peishen 2026-09-16 拍 1a）
---

# 看护件 · B-0916_581模型字段接线

> 任务全文＝队列 §一 `#581` 状态列末段「🔧 合入前补缺」。

## 一、硬边界

- 🔴 在**既有分支** `claude/op0916b-model-route-581` 上续做，不新建分支；只 push 该分支，不 ff master。
- 🔴 **所有测试前台跑，严禁放后台等通知**——上一棒正是在 `-p` 模式下后台等回归时会话结束、活悬在 worktree。
- 🔴 `test_工具-opener批处理执行v2.py::Detach后台起` 在 `%TEMP%` 下的 worktree 里 master 与本支同样失败（环境相关，非回归）；本棒在仓内 `.claude/worktrees/` 跑，照常应通过，若失败按差集口径与 master 对跑判定。
- 🔴 不碰 `#580` 触碰区（`工具-Token用量度量.py`）。

## 二、泳道

### A1 · §一 #581 opener「模型」字段接到批处理执行器

▶ 粘贴端：CC ｜ 泳道：op0916d-model-field-wire-581

```
[OP-0916-D]【CC】opener模型字段接线
【设置】执行环境：CC ｜ 分支：`claude/op0916b-model-route-581`（既有分支续做，不新建） ｜ worktree：☑（op0916d-model-field-wire-581，新 worktree 检出该既有分支，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务运行时） ｜ session：新开 ｜ 派出线：Cowork Token 优化总线 OP-0916-T，批 B-0916_581模型字段接线
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0916D-opener模型字段接线。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `队列 §一 #581（`python 0-学习与工具/工具-队列查询.py --row 581 --section 一`，状态列末段「🔧 合入前补缺」即本棒全部任务）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类，无需再问澄清，直接开工。
🔴 心跳一律跑命令写：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916d-model-field-wire-581 --text "已开工"`；每里程碑追加一行；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0916d-model-field-wire-581 --done --batch B-0916_581模型字段接线 --text "产出落点：<落点>"`。
🔴 测试一律前台跑、跑完即 commit，严禁后台等通知。收工只 push `claude/op0916b-model-route-581`，不 ff master；#581 状态列 edit-row --append 回写。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾。
```

## 三、起跑（Cowork 侧一条命令）

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】581模型字段接线-2026-09-16.md" -FullAuto -Yes -Detach -Model sonnet
```
