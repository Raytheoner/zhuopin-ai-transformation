---
status: 在办
title: "LAN 留步项捞出清单（回内网补做）＋ 与在跑波次的资源争夺分诊"
created: 2026-08-26
派出线: Cowork 环境总线（OP-0826-C）
---

# LAN 留步项捞出清单 —— 2026-08-26 22:36 回内网

## 〇、先证 on-LAN 真的成立（实测，不采信「我在公司」这句话）

🔴 **本项目已记过一条会骗人的判据**（§一 #340 行内）：off-LAN 下 `.51` 的 URL **照样返回 http_code**，「拿到了状态码」≠「服务答了」—— 08-25 那次 `curl :8093/api/ping` 返回 **502，但响应头带 `Proxy-Connection: keep-alive`**，说明 502 是代理给的。故本次判据取三项，缺一不算通：

| 探针 | 2026-08-25（off-LAN） | **2026-08-26 22:36（本次）** |
|---|---|---|
| `ping 192.168.100.51` | 2 发 0 收（100% loss） | ✅ **True** |
| `:8091/api/ping` | —— | ✅ **200**，`Server: waitress`，**无 `Proxy-Connection`**，body `{"status":"ok","time":"2026-08-26T22:36:01"}` |
| `:8093/api/ping` | 502（代理给的） | ✅ **200**，`Server: waitress`，body `{"service":"QD-B 立项审核门禁","status":"ok"}` |

🔑 **判准是 `Server: waitress` ＋ 真实 JSON body ＋ 没有 `Proxy-Connection`** —— 这三样一起出现才说明**是服务自己答的**。**⇒ on-LAN 成立，且 8091／8093 两个服务都活着。**

---

## 一、四组留步项（队列两份真身实扫，剔除批次行等历史叙述后的**活项**）

> 扫出 14 处「LAN 留步」，其中 5 处落在已完成的 §二 批次行（历史叙述，不可动作）。**真正待补的是下面 4 组。**

### 🟢 组 1 · #340 QD-B 基准落地 —— **建议现在就做，零争夺**

**留步的是**：`.51:8093` 部署与三样本真实冒烟（《基准变更说明》§六 第四件）。
**当前态**：**代码已按新基准（EQ17 98.80→100.00）、`.51` 仍跑旧版** —— 版本差状态；变更包因 `tasks.md` 8.3 未完成而**仍不归档**。
**补做（行内已写死补法，照做即可）**：

1. `sync-to-server.ps1` 同步
2. `Restart-ZhuopinTask`
3. `/api/ping` 200 ／首页 200
4. **三份真实样本重传比对 manifest 真值 —— 🔴 EQ17 须为 100.00·合格**
5. 回写 #340 ⑤、`tasks.md` 8.3 打勾，变更包可归档

**与波次争夺**：**无**。8093 是 QD-B，本波次 12 条无一触碰。**A12（sc8）也不碰 8093。**

### 🟡 组 2 · #401 SC8 规则1 —— **拆两半：L1 现在做，L3 必须等 A12**

**留步的是三步**：L1 SRM 真实核验＋真实前后对照表／L2 黄金基准真实重跑／L3 `.51:8091` 部署。
**当前态**：`[S:partial]`，**代码已交付、判据未生效**。行内明写销号判据 ＝「**开关在 `.51` 上翻 ON 且姚祖怡已签认**」，**不是「代码合入」**（同 #82：机制建成 9 天、天天在跑、一条没发出去、没人察觉）。

- **L1 现在可做，零争夺**：脚本已备好并离线冒烟跑通 —— `scripts/compare_kit_date_rule1.py`，同一份输入跑两遍 off/on，出四色计数＋逐行变动＋分支适用面，**自带三条不变式自检**（只有规则 1 适用行会变／齐料日只前移不后移／🔴 只减不增）。**回网段一条命令即可出件。**
- 🔴 **L3 必须等 A12**：本波次 **A12（sc8 泳道，#334⑺ 物料看板）也要部署 `.51:8091`**，两边同时部署会互相覆盖。**A12 至 22:36 尚未启动**（还在等槽位）。
- ⚠️ 另有第四步**不属 LAN 留步**：翻开关的硬前置是**姚祖怡签认**（随 #402／`采购部#19`），LAN 通了也翻不了。

**⇒ 今晚做 L1 出对照表即可**，那正是给他签认用的材料。

### 🟡 组 3 · #354 env-anchor-collapse —— **要动 `.51` 四个常驻服务，🔴 与 A3 有撞车面**

**留步的是**：`tasks` 2.2.1 与 2.3.1–2.3.6 的 `.51` 真机复验**一项未做** —— 4 个常驻服务逐个部署冒烟、定时任务告警真实发出、4 个常驻服务 `REQUIRED_ENV_KEYS` 按实测填实（当前**刻意留空＝保持今天行为不变**）。
**为什么它比别的更值得做一次真机**：行内原话 —— **「改的正是『凭据从哪来』，而 `.51` 是唯一没有 git、走第三段扁平锚点的环境，开发机全绿与它毫无关系。」**
**当前态**：变更包 `暂不归档`（已用机器认得的入口写了 `预期观察窗口：7 天`），门禁**刻意不切 `--enforce`**。

🔴 **与波次争夺（本组唯一需要判断的地方）**：**A3 正在建 outbox→aibot 中继**（22:31 启动，触碰 `wecom-aibot-service`），而 **A2 刚在 22:26 前后重启过 Listener**。若此刻去逐个部署 `.51` 四个常驻服务，**可能与 A3 的部署动作互相覆盖**。
**⇒ 建议等 A3 收工。** 若一定要今晚做，**先跳过与 aibot 相关的那一个服务**，其余三个先做。

### ⬜ 组 4 · #398 A28 的一处 —— **只需复跑一条命令，零争夺**

**留步的是**：skill 核验器的「**200＋口令门**」那条分支 —— 当时**用本地 HTTP 桩按实测形态复现验证的，未在真 `.51` 上跑过**。
**补做**：按 `环境体检-2026-08-24-首期.md` §九 第 3 项，**回内网复跑一次 `--strict` 复核**。
**与波次争夺**：**无**。⚠️ 但注意 **A5（#410）此刻在跑**，它也会碰 `工具-落库sweep.py`；本组不碰 sweep，互不影响。

---

## 二、今晚的建议次序（都不与在跑波次争夺）

| 序 | 做什么 | 争夺 | 为什么现在 |
|---|---|---|---|
| **1** | **组 1**：QD-B 部署 8093 ＋ 三样本冒烟 | 无 | 唯一一步就能把一个「代码新、线上旧」的版本差消掉 |
| **2** | **组 4**：`--strict` 复核一条命令 | 无 | 成本最低，且它是**唯一没在真环境验证过**的那条分支 |
| **3** | **组 2 的 L1**：出 SC8 规则1 前后对照表 | 无 | 这是姚祖怡签认要用的材料，早出早进 `采购部#19` |
| — | 组 2 的 L3（8091 部署） | 🔴 **等 A12** | 与 A12 同一端口，会互相覆盖 |
| — | 组 3（`.51` 四服务） | 🟡 **等 A3** | 与 aibot 服务部署有撞车面 |

---

## 二bis、🔴 更正：这三组不该由 Shao Peishen 自己敲，且组 4 的前提不成立

**⑴ 形态错了（本件初版的错）**：§二 写「你现在就可以动手」是**违反 2026-08-26 刚落档的 §〇.18** 的 —— 这三组本身就是 CC 活（部署、跑脚本、比对取数），**不需要提权**，因此**不该占用他的终端**，也不该让他失去 remote-control 接管能力。**正解＝出一条 CC opener（`OP-0826-V`，见下），他粘一次即可。**

**⑵ 组 4 的前提被实测推翻，不能照原样跑**：
- `#398` A28 的改动在 **`claude/queue-398-mech-signal`（`fb0d185`）**，实测 **`git merge-base --is-ancestor fb0d185 master` ＝ False ⇒ 从未合入 master**，分支领先 master 1 个提交。
- 主工作区那份 `0-学习与工具/工具-仓库外载体扫描.py` **最后一次 master 提交是 `3794d81`（08-04 版）**，`strict` 字样 **0 命中**。
- ⇒ **「复跑 `--strict`」这条命令在主工作区根本不存在。** 组 4 的正确动作是**两步**：先 ff 合入那 1 个提交（`#398` 现为 `✅ 待验收`，合入前该有人验一眼），再复跑。

🔑 **这一条本身就是「LAN 留步无承接」的连带证明**：那个留步项挂在一个**从未合入的分支上，一天多没人动过** —— 而队列行看起来是 `[S:partial]` 正常在办。**⇒ `OP-0826-U` 的存量扫描器必须一并覆盖「留步项所依赖的代码是否还在未合入分支上」这一形态**，否则扫出清单也修不了。

### opener（复制即用）—— 三组 LAN 补做

▶ **执行端：CC**（不提权；CC 起的 session 可 remote-control 接管）

```
[OP-0826-V]【CC】LAN 补做三组 —— #340 QD-B 部署冒烟 ／ #398 扫描器合入后复跑 ／ #401 L1 对照表
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0826-C
读 ① 1-转型规划/0-全景路线图/LAN留步项捞出清单-2026-08-26.md 全文（含 on-LAN 三项探针判据与本次两处更正）→ ② 队列 §一 #340／#398／#401。
🔴 开工第一件事＝重跑 §〇 那三项 on-LAN 探针，三项齐备才继续：ping 192.168.100.51 通 ＋ :8091 与 :8093 的 /api/ping 返回 200 且 Server 头为 waitress ＋ 响应头不含 Proxy-Connection。⚠️ off-LAN 下这些 URL 照样返回 http_code（08-25 实测 502 且带 Proxy-Connection，那 502 是代理给的），只判「有没有状态码」会误报可达。
🔴 波次仍在跑，三条硬边界不得越过：⑴ 不得部署 .51:8091（A12/sc8 泳道也要部署它，会互相覆盖）；⑵ 不得动 wecom-aibot-service 与 .51 上与 aibot 相关的服务（A3 在跑）；⑶ 不得改 0-学习与工具/工具-落库sweep.py（A5 在跑）。撞到即留步登记，不要绕。
【组 1 · #340】4-数字员工/质量部/QD-B-立项审核门禁/sync-to-server.ps1 同步 → Restart-ZhuopinTask → /api/ping 200 ＋ 首页 200 → 三份真实样本重传比对 manifest 真值，🔴 EQ17 须为 100.00·合格。通过后回写 #340 ⑤、把 tasks.md 8.3 打勾，变更包即可归档。
【组 2 · #401 只做 L1】跑 4-数字员工/采购部/SC8-客户订单交期智能承诺/scripts/compare_kit_date_rule1.py 出真实前后对照表（同一输入跑两遍 off/on，四色计数＋逐行变动＋分支适用面）。🔴 它自带三条不变式自检——只有规则1适用行会变／齐料日只前移不后移／只减不增——任一条不成立即停下登记，不要自行解释。L2 黄金基准重跑与 L3 的 8091 部署本次不做（L3 等 A12）。出件落盘，它是姚祖怡签认要用的材料（随 #402／采购部#19）。
【组 4 · #398 两步，先合后跑】① 先验再 ff 合入 claude/queue-398-mech-signal（fb0d185，领先 master 1 个提交，#398 现为「✅ 待验收」）——实测主工作区那份 工具-仓库外载体扫描.py 仍是 08-04 版、strict 零命中，不合入就没有 --strict 可跑；② 合入后在真 .51 上复跑一次 --strict，重点验「200＋口令门」那条分支（此前只用本地 HTTP 桩按实测形态复现过，从没在真 .51 上跑过）。
收工：逐条回写 #340／#401／#398，明写哪几步做了、哪几步仍留步及原因；登记 §二 批次（🔴 批次号先查同日已用，本日 B-0826_22 已被占用过一次）。ZhuopinCommitSweep 当前 Disabled，队列行请自行 commit 并在状态格注明待 sweep 恢复后补销。
开干前问我 2-3 个澄清。
```

## 三、机制修补：让「回 LAN」这件事自己会被发现

**问题不在于留步项写得不好，在于它只有产生方、没有承接方**（详见 §一 #398 第 ⑷ 处）。要补的机制有两半，**两半都不能少**：

- **半一 · 存量能被扫出来** —— 扫两份队列真身与 `reports/opener-batch/*/` 收工日志里的「LAN 留步」，按登记时长分档。
- **半二 · 「回 LAN」这个事件本身要能被感知** —— 否则扫出来也只是一张永远存在的清单，没人知道**什么时候**该动它。判据＝上面 §〇 那三项探针（`ping` ＋ `Server: waitress` ＋ 无 `Proxy-Connection`），**由 off→on 的那一次翻转即触发提醒**。

🔴 **一条必须内建的护栏**：**判据要能被「已补做」关掉**。否则做完了它还在报，一周之内所有人都会学会忽略它 —— 那就又造出一个永远红着的告警（本项目已有三个先例，见 #398 前三处）。

opener 见下节，**标注 `▶ 执行端：CC`**（守 §〇.18：不提权的事不占用他的终端，且 CC 起的 session 他可以 remote-control 接管）。

### opener（复制即用）

🔴 **本 opener 已于 2026-08-26 23:2x 更新一版**（原版前置已过时、且范围少了两个形态）。更新点见下方 §四。

```
[OP-0826-U]【CC】未闭合产出承接机制 —— 三形态存量扫描 ＋ 回 LAN 事件感知 ＋ 先救 A5 遗留（#398 第 ⑷ 处）
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0826-C
读 ① 队列 §一 #398 第 ⑷ 处 → ② 1-转型规划/0-全景路线图/LAN留步项捞出清单-2026-08-26.md 全文（含 on-LAN 三项探针判据、§二bis 更正、§四 三形态）→ ③ 0-学习与工具/工具-落库sweep.py 的常驻告警族写法。
🔴 第 0 步（先做，救火）：救回 A5 的遗留产出。实测——波次 A5（#410 editable 巡检判据）以 NO-SENTINEL 于 22:46:09 结束，日志末行停在「待办：全量回归绿 → commit/push → 回写 #410、#121」；其产出 539 行新增、2 个文件（0-学习与工具/工具-落库sweep.py 与 test_工具-落库sweep.py）只以 modified 状态躺在 worktree .claude/worktrees/queue-410-editable-guard 里，分支 head b510f77 = master、领先 0 个提交，未 commit、未合入、队列未回写。⚠️ 它触碰的正是你本次要改的同一个文件 ⇒ 不先救就必然冲突，且任何一次 worktree 清理都会让这 539 行消失。做法：先跑全量回归确认这批改动是绿的，绿则 commit + ff 合入 master + 回写 #410／#121；若不绿，如实登记并把改动保到分支上再说，🔴 不得丢弃、不得替它改判。另注：A5 自陈拟登记 §二 B-0826_27，而该批次号疑似已被 A6 于 22:55:57 占用（31be62a），登记前先查重。
本次三块：
【一·存量扫描三形态】🔴 不止「LAN 留步」四个字——实测同一族有三种外形，只扫第一种会漏掉后两种：形态1 队列/日志里写着「LAN 留步」的留步项（存量 14 处，🔴 须剔除已完成 §二 批次行里的历史叙述，本次 14 处里有 5 处属此类，不剔除会长期虚高）；形态2 留步项所依赖的代码卡在未合入分支上（活样本＝#398 的 fb0d185 在 claude/queue-398-mech-signal，实测 merge-base --is-ancestor fb0d185 master = False，主工作区那份工具仍是 08-04 版）；形态3 产出连分支都没有、只是某个 worktree 里未 commit 的改动（活样本＝上面第 0 步的 A5）。⇒ 扫描器要能同时报这三种，并对每一种给出「它现在在哪、怎么救」。
【二·回 LAN 事件感知】判据三项齐备缺一不算通：① ping 192.168.100.51 通；② :8091 与 :8093 的 /api/ping 返回 200 且 Server 头为 waitress；③ 响应头不含 Proxy-Connection。🔴 理由：off-LAN 下这些 URL 照样返回 http_code（08-25 实测 502 且带 Proxy-Connection，那 502 是代理给的），只判「有没有状态码」会误报可达——同「工具静默回退」族。由 off→on 的那一次翻转触发提醒，不是每轮都报。
【三·护栏】🔴 判据必须能被「已补做／已合入／已 commit」关掉，否则做完了还在报，一周内所有人都学会忽略它——本项目已有三个这样的先例，见 #398 前三处。
先过 openspec 触发门槛三问并把判定写进收工报告（初判：新建扫描器不改既有对外语义，倾向不走 openspec；若做进 sweep 的告警族则须复核第 ③ 问）。收工回写 #398 第 ⑷ 处、#410、#121＋登记 §二 批次（批次号先查同日已用，B-0826_22 与 _27 都已被占用过）。
开干前问我 2-3 个澄清。
```

---

## 四、`OP-0826-U` 的两次更新（为什么原版不能直接用）

**更新 1（22:5x）· 形态 2** —— 留步项所依赖的代码卡在未合入分支上。实证＝`#398` 的 `fb0d185`：队列行看起来是 `[S:partial]` 正常在办，实际上主工作区那份工具仍是 08-04 版，**「复跑 `--strict`」这条命令在主工作区根本不存在**。

**更新 2（23:2x）· 形态 3，由波次 A5 当场演示** —— **产出连分支都没有**。A5 以 `NO-SENTINEL` 结束，539 行新增只以 `modified` 躺在 worktree 里，分支领先 master **0** 个提交。

🔑 **三种形态的共同点，也是这条机制真正要防的东西**：**每一种在队列里看起来都正常**——形态 1 有一行负责任的「LAN 留步」登记，形态 2 行状态是 `[S:partial]` 在办，形态 3 连行都还没来得及回写。**没有任何一种会自己发出信号。**

⚠️ **原版前置「确认 A4／A5 均已收工合入」现已不适用**：A4 已合入 master（`8f51787`）；**A5 的进程已退出 ⇒ 文件冲突风险消失**，但它留下 539 行未提交改动在同一个文件上 ⇒ **风险从「并发写同一文件」变成了「将来合并必冲突 ＋ 随时可能被清理掉」**。故新版把「救 A5」提为第 0 步，**由同一条 session 一次做掉，不分给两条去抢同一个文件**。
