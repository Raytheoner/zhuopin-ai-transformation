---
status: 生效
title: "Anti 报告 #7（记忆体系全景诊断）· 逐条核实与采纳结论（2026-08-21）"
created: 2026-08-21
执行方: Cowork 环境总线（OP-0821-B）
被核对象: C:\Users\Paul Shao\OneDrive\Projects\项目分析报告Anti\7_Claude_Code_Desktop环境配置与Session记忆体系全景诊断报告.md（mtime 2026-08-21 13:19:14）
原则: 所有建议先实测再判采纳；凡「不在／已完成／落后 N」这类断言一律附验证手段
---

# Anti 报告 #7 · 逐条核实与采纳结论

## 〇、一句话

**方向全对，但快照晚了两小时**——它的头号建议（SC8/FI2 场景级 CHANGELOG 剥离）**在报告写成前 2 小时就已执行并 push**；另有两处落点与数值需更正。**4 条隐患里 2 条已消、2 条成立并已推进；3 条建议里 1 条已完成、1 条采纳并已执行、1 条采纳但落点需改。**

🔑 **一条方法观察**：报告的每个判断都写了依据，但**依据取自它开始审计那一刻的文件快照**，中途没有重取。这与本项目今天刚记下的两次错误同族——**「查了，但查的是过期的那一份」**。⇒ **凡跨越较长时间的审计，结论落笔前应对关键数值重取一次。**

---

## 一、事实核对（先核它引用的数，再谈建议）

| 报告的说法 | 实测 | 判定 |
|---|---|---|
| 全局层 9.28 KB / 154 行 | 9,280 B / **153 行** | ✅ 基本准（行数差 1） |
| 项目根 CLAUDE.md「~101 KB / 178 行」 | **90,652 B**（本日多轮治理后） | 🔶 **过期**——101,463 是当日上午第四批迁移后的中间值 |
| 场景级共 13 份 | 13 份（含根共 14） | ✅ 准 |
| **SC8 100.1 KB／FI2 75.6 KB，「尚未做 CHANGELOG 剥离」** | **SC8 33,350 B／FI2 40,867 B，两份 `CHANGELOG.md` 均已建** | 🔴 **已过期**——剥离于 2026-08-21 11:20 前完成并 push（commit `2ea7d34`） |
| CHANGELOG.md 192 KB | 192,360 B（第四批后），第五批后更大 | ✅ 准 |
| memory 42 条、已判只读、`#254` 销号 | 与实测一致 | ✅ 准 |
| **生产 worktree 落后 master 246 提交** | **实测 106**（`git rev-list --count HEAD..master` 于 `.claude/worktrees/wecom-service-home`） | 🔶 **数值过期**——246 是 2026-08-19 的值；现 106，**仍落后，隐患成立** |
| 「Sweep 的自动检查项」可作为 lint 落点 | `工具-落库sweep.py` 全文 **「lint」命中 0** | 🔴 **落点错**——见 §三.2 |

---

## 二、四条隐患的核实结论

| # | 隐患 | 结论 |
|---|---|---|
| 1 | 场景级 CLAUDE.md「大头娃娃」 | ✅ **已消除**（SC8 -66.7%、FI2 -45.9%）。⚠️ 未达它预估的「20 KB 以内」，**原因是 J1 判据当场拦下了 SC8 的 8 行**（其中三行挂着无载体的未闭合项：`2026-06-24` 未结的 LAN 联调验收、`2026-07-06` 未接线的置信度流水线、`2026-07-15 A2` 一个到 2027-03 才到期的跟踪项）。**这不是没做干净，是判据在起作用。** 要再降须先为那三行立队列行。 |
| 2 | J1~J4 仍是人守 | ✅ **成立，且是当前第一优先**。已按其建议推进，见 §三.2。 |
| 3 | 全局层称呼映射差异 | ✅ **成立，且比报告描述的更实**——实测全局 `~/.claude/CLAUDE.md`「Shao Peishen」命中 **0**、「称呼」命中 **0**，且 **L98／L102／L149 三处是把 `Paul` 当人名的行为指令**（如「若有需要 Paul 手动做的事，单独用带编号的勾选清单列出」）。⇒ 不是「第一眼被带偏的微弱概率」，而是**每个 CC session 必载的文件里有三条正在教模型用 Paul 的指令**。**已执行**，见 §三.3。 |
| 4 | 生产 worktree 滞后 | ✅ **成立**，数值更正为 **106**。归 CC（须 ff-merge ＋ 重启常驻服务；上次同类操作只跨 27 个提交就撞出孤儿子进程、需手工补杀并验心跳）。**已登记待派，本线不就地展开。** |

---

## 三、三条建议的采纳与执行

### 3.1 建议 1（SC8/FI2 场景级 CHANGELOG 剥离）—— ✅ **已完成，无需采纳**

执行于 2026-08-21 上午，手法：PowerShell 按行索引搬运（不重打字）＋ J1 承接载体判据分流 ＋ 三重验证（正文段 SHA256 逐字节比对／迁出行逐行比对 24-24、117-117 全中／控制字符 0）。

**顺带修掉一个报告没看出来的问题**：FI2 存在**与根 CLAUDE.md 同型的双重记账**——顶部叙述块与 §状态 对同一批事件各记一遍（v8 面板／轮1／税务导出三件在两处都有）。已一并消除。

### 3.2 建议 2（J 判据纳入机器检查）—— ✅ **采纳，但落点须改**

**报告说「CI 基线或 Sweep」。实测：`工具-落库sweep.py` 全文「lint」命中 0，lint 一律挂 CI。** 现有 CI（`.github/workflows/ci.yml`，199 行）已有 4 个同构 lint job：`queue-structure-lint`／`bootstrap-stub-lint`／`secret-scan`／`coverage-guardrail`，每个都是一行 `- run: python 0-学习与工具/工具-XXXlint.py`。

**⇒ 修正后的落点（三处，各按其咽喉）**：

| 判据 | 落点 | 理由 |
|---|---|---|
| **J2**（顶部进度条目数 ≤6）、**J3**（单条 ≤1,200 字符）、**J5**（场景级同款） | **新增 `0-学习与工具/工具-CLAUDE进度段lint.py` ＋ ci.yml 新 job `claude-progress-lint`**，与 `bootstrap-stub-lint` 完全同构 | 这三条是**静态可判**的，CI 是自然咽喉 |
| **J1**（承接载体安全阀） | **同一脚本，但只在 J2 触发时作为「拒绝理由」输出**，不单独报错 | J1 不是"违规检测"，是"迁移前置"；单独报会天天误报 |
| **J4**（写入侧未闭合项拦截） | **编辑锁 `release` 校验器**，与既有 `_validate_release_structure`／`_validate_followup_readme_release` 同族 | 队列写入的咽喉在 release，不在 CI |

**派单件已备**：`1-转型规划/0-全景路线图/派单件-【CC】CLAUDE进度段lint实现-2026-08-21.md`。

### 3.3 建议 3（全局层补称呼映射）—— ✅ **采纳并已执行**

在 `C:\Users\Paul Shao\.claude\CLAUDE.md` 的「姓名」行下新增一条映射说明（9,280 → 9,909 B）。

**执行手法值得记**：该路径**在 connected folder 之外**，用 `Write` 工具会**报成功但静默丢弃**（正是本日判死的那个缺陷）⇒ **只能走本机 PowerShell**。改前已备份至同目录 `CLAUDE.md.bak-20260821-OP0821B`（9,280 B）；改后校验：「Shao Peishen」命中 0 → **1**，控制字符 0，无 BOM、CRLF 未漂移。

🔴 **刻意没做的一件**：**未改 L98／L102／L149 三处行为指令里的 `Paul`**。理由——那是**全局文件、对其它项目仍成立**；称呼纪律是本项目的规则，用一条显式映射覆盖它，比在全局层做全局替换更正确。**若日后发现映射一条压不住，再考虑改那三处，届时须意识到那是在改全局行为。**

---

## 四、未采纳／需回给报告方的三点

1. **头号建议已过期两小时** —— 建议报告方在结论落笔前对关键数值重取一次（本项目同日两次同族错误的教训：**查了，但查的是过期的那一份**）。
2. **落点「Sweep」不成立** —— 实证 sweep 内「lint」0 命中，lint 全在 CI。
3. **worktree 落后 246 → 实测 106** —— 引用的是两天前的值。

## 五、去向

- 队列：隐患 4 归 CC 待派；建议 2 的实现归 CC（派单件已备）。
- 本件与派单件均未提交，git 处置见会话收工段。
