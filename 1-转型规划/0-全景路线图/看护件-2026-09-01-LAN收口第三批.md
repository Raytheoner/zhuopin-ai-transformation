---
status: 已收口
title: "看护件 · 2026-09-01 · 回 LAN 收口【第三批 · 零项入批】"
编号: OP-0901-A
日期: 2026-09-01
触发: Shao Peishen「我已回Lan」（灰度已于 2026-08-31 摘除，本批用默认上限 6）
结论: 零项入批——不是漏收，是真的没有了
---

# 看护件 · 回 LAN 收口 · 第三批

> **本批的实际价值不在"做了几项"，在于它是 `OP-0901-B` 两道假阳性过滤的第一次真实流程验证。**
> 今早 08:16 sweep 推送「8 条 LAN 留步现在可以补做了」，逐行核对零条有效；本批走完汇集后 **items 从 8 降到 1，ack 后降到 0**。

## 〇 前置探针与常规三查（章程要求，不可跳过）

| 项 | 实测 | 判定 |
|---|---|---|
| `ping 192.168.100.51` | True（2 包） | ✅ 在网 |
| git | 脏 0 ／ `origin/master..master` = 0 | ✅ 对齐 |
| 编辑锁 | 空闲（释放标记） | ✅ 无长占 |
| 上一收口批汇总 | `OP-0831-L` §五 已出 | ✅ |

🔑 **触发词声称 ≠ 在网事实** —— 探针独立跑过，不因他说"我已回Lan"就默认在网。

## 一 · 汇集结果（三源，含 `OP-0901-B` 两道过滤）

```
形态 1 · 队列里的 LAN 留步登记：      1 处   （#312）
形态 1 · 需人读一遍（否认表述降级）： 6 处   （#334 #340 #354 #394 #418 #422）
形态 1 · 已按 [S:done] 排除：         2 处   （#401 #402）
（另剔除 §二 批次行里的历史叙述 29 处 —— 已完成批次的记述，不是待办）
```

**对照今早 08:16 的推送**：那次列出 8 条（`#312`/`#334`/`#340`/`#354`/`#394`/`#401`/`#418`/`#422`），**零条有效**；本次两道过滤把其中 2 条直接排除、6 条降级，只剩 1 条进 `items`。

## 二 · 逐行读判（章程 §1：`needs_review` 必须真读，不得批量打回、也不得未读放行）

| 行 | 状态字段（本轮实测） | 读判结论 | 处置 |
|---|---|---|---|
| `#312` | `[S:blocked][D:机]` | 本行是**可 Open 池提醒机制**（`open-pool-reminder-dual-file-and-staleness`）的收口行——陈化催办、7 天周期轨迹、读侧双文件解析。**LAN marker 落在「任务」格的描述性文字里，非真实留步登记**；属 `OP-0901-B` 已登记的第三种残留形态（元讨论/描述性提及） | ✅ `--ack-form1` |
| `#334` | `[S:partial][D:业]` | 子项⑺物料看板部署验证 08-31 `OP-0831-L` 已完成；行仍 partial 系其余子项（D1/D2 报修、⑻⑼未定序、跟进信）所致，**与 LAN 无关** | ✅ `--ack-form1` |
| `#340` | `[S:partial][D:业]` | LAN 缺口 08-27 已自行解除，`OP-0831-L` 第二批读行复核确认 | ✅ `--ack-form1` |
| `#354` | `[S:timed=2026-09-03][D:机]` | 08-31 已改判为时点约束，**行内明写「不要再当 LAN 留步捞出来」**；剩余两项均非 LAN 约束 | ✅ `--ack-form1` |
| `#394` | `[S:partial][D:业]` | 08-31 `OP-0831-L` 已完整执行（`.env` 双备份 → 部署 → `outbox_relay_started` → 2 条真实送达 `errcode:0`），LAN 部分已闭合 | ✅ `--ack-form1` |
| `#418` | `[S:open][D:业]` | 行内明写「**这不是 LAN 留步，是无人在场时不自行执行需批准的生产写动作**」⇒ **人工门禁，永不代办** | 🔴 **不 ack**（见下） |
| `#422` | `[S:blocked][D:机]` | 判据是「**要先真的离开内网**」⇒ **on-LAN 恰恰做不了**；本行真未闭合，只是此刻做不了 | 🔴 **不 ack**（见下） |

🔴 **为什么 `#418`／`#422` 不 ack** —— `--ack-form1` 的语义是「读一遍确认**已闭合**」。这两条都**未闭合**：`#418` 等的是一次人工批准（红线：请人动作永不代办）、`#422` 等的是一次真正的离网。**把「做不了」记成「已闭合」是把状态写脏**，宁可让它们每次都被扫出来。⇒ 它们会持续留在 `needs_review` 桶里，这是正确行为，不是缺陷。

## 三 · 处置与复扫验证

**ack 5 条**（各带 `--note` 写明核的是什么、凭什么核），指纹已记录 —— 行一旦被改动，ack 自动失效、重新报出。

**复扫（不采信 ack 回显，重跑一次扫描器）**：

| 桶 | ack 前 | ack 后 |
|---|---|---|
| LAN 留步登记（`items`，真会进批） | 1 | **0** |
| 需人读一遍（`needs_review`） | 6 | **2**（`#418`／`#422`） |
| 已按 `[S:done]` 排除 | 2 | 2 |

## 四 · 本批结论

**零项入批。** 🔴 **这不是漏收，是真的没有可补做的 LAN 留步了** —— `OP-0831-L` 两批已把清单清干净，今早那 8 条全是汇集逻辑的假阳性。

**⇒ 三判据自评**：① 误收 0（唯一进批的 `#312` 经读行判定为元讨论，未误做）；② 不合格登记全部点名（本件 §二 表逐行）；③ 证据链齐全（探针/状态字段/ack 指纹/复扫三桶数各有实测）。

**未解决（如实登记，非本批范围）**：
- `tasks 2.1` 的**假阴性半边**（阻塞理由含 off-LAN 的 `[S:blocked]` 行在 on-LAN 时视为候选）仍未做 —— `OP-0901-B` 按任务书「先做排除、后做补捞」只做了排除。
- 第三种假阳性形态（元讨论/描述性提及）机制侧未识别，本批靠人读 + `--ack-form1` 兜住。`OP-0901-B` 建议不新增第三道过滤（避免重蹈"猜中文"覆辙），本批同意该判断。

## 五 · 与今早那条推送的关系（留给下一位读者）

今早 08:16 那条企微推送**看起来完全正常**：有条数、有格式、有队列链接、有 UTC/本地双时刻。**只有把 8 行逐条拿去比对状态字段，才会发现一条有效的都没有。**

🔑 **而一份"看起来正常"的清单，人的正常反应是照着做，不是去核它。** 这就是本项目反复吃亏的那个形态——与 `#82`（告警建成 9 天每天在跑却一条没真发出）同族：**都表现为拿到一个看起来很正常的结果**。

本批之后，那条推送只会推 `items` 桶，`needs_review` 仅附一句 FYI、绝不进"现在可以补做了"清单。
