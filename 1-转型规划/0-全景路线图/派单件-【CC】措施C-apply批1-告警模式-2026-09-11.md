---
title: "派单件 · 【CC】措施 C 换量具 apply 批 1（告警模式）"
created: 2026-09-11
status: 生效
派出线: Cowork 环境总线 OP-0910-H
OP号: OP-0911-N
---

# 派单件 · 措施 C 换量具 apply 批 1（告警模式）

## 〇、这一棒从哪来

`OP-0911-D` 已把变更包 `mechanism-wip-staleness-gate` 的 propose＋design 做完并 ff 进 master（`b9495e1` → `aad821a` → `01ac018`，`openspec validate --strict` 通过）。design 六决策点 Shao Peishen 2026-09-11 答 `2a` 全按推荐签认，`tasks.md` 1.x 全勾。
`#58` ⑺ 段明写：**下一棒＝apply 批 1（告警模式）另派，派单件待环境总线出**。本件即那份派单件。

**任务正本在 `openspec/changes/mechanism-wip-staleness-gate/tasks.md` 的 §2 与 §3**，本件不复制它，只写边界与硬前置。

## 一、开工硬前置（做不到就停手回报，不要绕）

1. 🔴 **第一件事＝重跑 `tasks.md` 1.1 的探针**（现取表过期守，同 `#522`）。手段已全文写进 `proposal.md`「前置实测复跑」表，按表复算即可。
   - 与 1.1 记录的 **09:55 基数（计入 27／22，🛑 认两列后 23／22；`STALE(7)=0`／`STALE(5)=4`／`STALE(3)=7`／`STALE(2)=9`；最老 6.96–6.97 天＝`#240` `#312` `#381` `#382`）** 对照。
   - **计入行偏差 > 3 条 ⇒ 立即停手回报，不得拿过期基数写进实现。**
   - ⚠️ 本派单件出件后 `#387`／`#58` 等行有改动，队列文件已动过，blame 取龄会随之变——这正是要现取的原因。
2. 探针只读、不 acquire、不落盘。
3. 开工先看 `git status --porcelain` 的未跟踪件——本仓同时有多条会话在跑（今日实撞四次并发抢先）。

## 二、做什么（照 `tasks.md` 勾）

- **§2 单测先写（2.1–2.10），红→绿，实现前先写红。** 用例落 `0-学习与工具/test_工具-共享文档编辑锁.py`，沿用既有 `cmd_release` 端到端范式；取龄的 git 依赖 monkeypatch 注入假 blame 输出，**单测里不真跑 git**。
- **§3 实现（3.1–3.9），只做告警模式** `MECHANISM_WIP_GATE_MODE = "warn"`。
- 3.9 的零回归判据按 `.claude/rules/两桌同步与取证.md` §二：同一组失败用例须在纯 master 同命令复跑逐条复现才算零回归。

## 三、不做什么

- 🔴 **不切阻断模式**——`tasks.md` §5 是 apply 批 2，须先过 D-D 三条前置并等 Shao Peishen 一字母。本棒一个字都不碰 §5。
- 🔴 **不删 `--mechanism-wip-cap`／`MECHANISM_WIP_CAP_DEFAULT`**（一进一出留给批 2）。
- 🔴 **不做 `tasks.md` §4（告警期）**——那是 7 天观察，归值周巡检承接。
- **不 archive 变更包**（5.5 是批 2 的尾步）。
- 不触碰 `.51`／企微机器人代码／定时任务。
- 不改协议〇.9 措施 C 正文（4.1 归值周，不是本棒）。

## 四、收工

1. 只 push 本泳道分支，**不 ff master**——ff 属 🟡 档，等 Shao Peishen 一字母，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做。
2. 队列 `#58` 追一段（走 `0-学习与工具/工具-共享文档编辑锁.py`，勿裸改）：写明探针复跑读数、偏差、2.x／3.x 勾到哪、分支与 commit、以及「批 2 待他一字母」。
3. 产出登记 §二 待 commit 批次，由落库 sweep 取活。
4. 收工汇总末尾给「待你一字母」清单＋答复模板（同类合并，标〔荐〕与默认项）。
5. 🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 五、opener（引用版，生成器产出，禁手抄）

```
[OP-0911-N]【CC】措施C-apply批1
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0911n-wip-staleness-apply1`） ｜ worktree：☑（op0911n-wip-staleness-apply1，会话自起子泳道隔离、收工自删；桌面端勾选框留空） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0911N-措施C-apply批1。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 `1-转型规划/0-全景路线图/派单件-【CC】措施C-apply批1-告警模式-2026-09-11.md` 全文＋ `CLAUDE.md` 恢复上下文，按该件执行。本件为 A 类。
```
