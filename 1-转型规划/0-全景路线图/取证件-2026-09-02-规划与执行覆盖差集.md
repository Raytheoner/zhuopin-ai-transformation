---
status: 取证已完成，待落成常驻扫描器
title: "取证件 · 规划与执行的覆盖差集（「计划倒逼开工」的第一份真数）"
编号: OP-0902-A
日期: 2026-09-02
触发: Shao Peishen 2026-09-02「我好像一直没有看到由计划倒逼的开工任务？」
承接: §一 机制行（扫描器，本件即其核心逻辑）＋ 业务场景队列（14 个未承接场景的立行，含 SC4 优先）
---

# 取证件 · 规划与执行的覆盖差集

> 🔴 **本件存在的意义是「不让这段逻辑消亡」** —— 脚本已跑通、35 个场景已对齐排期表，若只停在一次对话里，就是本项目反复吃亏的那个病（只落文档、无承接载体、于是消亡）的又一次。**落地实现直接取用本件 §三 的代码，不必从零建。**

## 一 · 结论：35 个场景，执行侧只覆盖了 10 个

| 分档 | 数量 | 场景 |
|---|---|---|
| ✅ **有工程实体** | **10** | SC1／SC2／SC7／SC8 ＋ O1／O2 ＋ FI1／FI2 ＋ QD-A(8D)／QD-B(立项门禁) |
| 🟡 队列提及、无工程无包 | 11 | O3／O4／Q4 PPAP／FI3／FI4／FI7／R1／R2／R3／R4／R5 |
| 🔴 **三处皆无（对所有机制不可见）** | **14** | SC4／SC10／SC11／FI5／FI6／FI8／FI9／FI10／销售 S1-S6（**整域 6 个全部**） |

**10 ＋ 11 ＋ 14 ＝ 35** ✅（SC7 两期计 1 个场景）

🔴 **最紧的一个：`SC4`，权威排期表写 2026-10（下个月），三处皆无。**

⚠️ **🟡 那一档是宽松估计，须落地时收紧**：本次「队列行」判据是 §一 全文 grep，**不区分「有承接行」与「被别的行顺带提到」**（R1-R5 极可能只是被某条讲「整域顺延」的行提到）。⇒ **真实不可见数 ≥ 14**，落地实现应改判「首列编号所在行的任务列是否点名该场景」。

## 二 · 根因：「规划 → 队列」之间没有任何机制

现有全部机制 —— 可 Open 池、拆件巡逻、值周巡检、泳道看护 —— **只扫队列 §一**；而队列只装「已经有人立过行」的任务。

⇒ **规划里有、但没人立行的场景，对所有机制完全不可见，且不产生任何信号。**

🔑 **这是本项目那个老病的第三个、也是最大的一个实例**：

| 实例 | 表现 | 处置 |
|---|---|---|
| `决策点:` frontmatter 字段 | 无机器守 ⇒ 覆盖率 70% → 0% | 已治（`#447` schema，2026-09-02） |
| 口径点 | 无台账 ⇒ 承接载体缺失 | 已治（`#439` R1 台账） |
| **规划场景** | **无队列行 ⇒ 永远不会被派工** | **本件** |

**前两个今天都已立行；第三个一直没人看见它 —— 因为看不见正是它的症状。**

## 三 · 扫描器核心逻辑（已跑通，直接取用）

**判据三条**：① 队列 §一 有无承接行；② `4-数字员工/` 有无工程目录；③ `openspec/changes`（含 `archive`）有无包。**三处皆无 ⇒ 未承接。**

🔴 **别名表是必需的，不能只匹配场景码** —— 实测 `Q2`→工程名 `QD-A`、`Q6`→`QD-B`、销售域六场景在排期表里只有中文名。**第一版脚本因缺别名把 QD-B 误判为"三处皆无"（它其实已交付），也把 SC7 两期算成两个场景致总数 36 而非 35。**

```python
import importlib.util, pathlib, re
ROOT = pathlib.Path(r"C:\Dev\zhuopin-ai")
spec = importlib.util.spec_from_file_location("lockmod", ROOT/"0-学习与工具/工具-共享文档编辑锁.py")
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
qd = ROOT/"1-转型规划/0-全景路线图"

# 35 个场景（SC7 两期计 1）＋ 别名（工程实体的真实目录名／中文场景名）
PLAN = [
 ("采购","SC1","2026-07",["SC1"]), ("采购","SC8","2026-07",["SC8"]), ("采购","SC2","2026-08",["SC2"]),
 ("采购","SC7①②","2026-09/2027-01",["SC7"]), ("采购","SC4","2026-10",["SC4"]),
 ("采购","SC10","2027-03",["SC10"]), ("采购","SC11","2027-04",["SC11"]),
 ("运营","O1","2026-09",["O1"]), ("运营","O2","2026-09",["O2"]),
 ("运营","O3","2026-12",["O3"]), ("运营","O4","2027-01",["O4"]),
 ("质量","Q2 8D","2026-10",["Q2","QD-A","8D"]), ("质量","Q6 立项门禁","2026-10",["Q6","QD-B"]),
 ("质量","Q4 PPAP","2027-04",["Q4","PPAP"]),
 ("财务","FI1","暂缓",["FI1"]), ("财务","FI2","2026-09",["FI2"]), ("财务","FI3","2026-11",["FI3"]),
 ("财务","FI4","2027-Q1",["FI4"]), ("财务","FI5","2027-02",["FI5"]), ("财务","FI6","2027-04",["FI6"]),
 ("财务","FI7","2027-05",["FI7"]), ("财务","FI8","2027-05",["FI8"]),
 ("财务","FI9","2027-06",["FI9"]), ("财务","FI10","2027-06",["FI10"]),
 ("工程研发","R1","2027-07",["R1"]), ("工程研发","R2","2027-08",["R2"]),
 ("工程研发","R3","2027-09",["R3"]), ("工程研发","R4","2027-10",["R4"]), ("工程研发","R5","2027-11",["R5"]),
 ("销售","S1 客户跟进","2027-07",["客户跟进","销售日报"]), ("销售","S2 拜访助手","2027-08",["拜访助手"]),
 ("销售","S3 RFQ","2027-09",["RFQ"]), ("销售","S4 技术文档","2027-10",["技术文档生成"]),
 ("销售","S5 竞品监控","2027-11",["竞品监控"]), ("销售","S6 销售预测","2027-12",["销售预测"]),
]

qtext = ""
for name in ["跨桌任务队列-机制环境.md", "跨桌任务队列-业务场景.md"]:
    qtext += m._split_live_sections((qd/name).read_text(encoding="utf-8")).get("一", "")
built = " ".join(d.name for d in (ROOT/"4-数字员工").rglob("*") if d.is_dir())
osp = " ".join([d.name for d in (ROOT/"openspec/changes").iterdir()]
             + [d.name for d in (ROOT/"openspec/changes/archive").iterdir()])

def hit(aliases, hay):
    # 前置断言避免 O1 命中 O10、S1 命中 SC1 一类误匹配
    return any(re.search(r"(?<![A-Za-z0-9])" + re.escape(a), hay) for a in aliases)

for dom, code, mon, al in PLAN:
    q, b, o = hit(al, qtext), hit(al, built), hit(al, osp)
    if not (q or b or o):
        print(f"🔴 未承接：{dom} {code}（规划 {mon}）")
```

## 四 · 落地时必须写死的两条（否则重蹈今天的覆辙）

1. 🔴 **它必须产出队列行，不能只产出报告** —— 否则又是「只落文档、无承接载体、于是消亡」（`#447` 行内自陈那个「第三次」）。
2. 🔴 **排期月份不作为门槛** —— Shao Peishen 2026-09-02 原话「**新场景不要管规划时间表，能开尽开**」「**没有前置任务阻塞的任务都可视为可开**」。唯一门槛是**前置依赖**（判据源＝《跨场景前置数据与知识库任务总表》，已存在）。
   ⇒ **销售域 6 个、财务 5 个现在就能进候选，不必等 2027。**

## 五 · 与「可 Open 池」的关系（Shao Peishen 2026-09-02 定）

**两者不冲突、互补，相加才是真正的「现在能开什么」**：

| | 管什么 | 现值 |
|---|---|---|
| 可 Open 池（`#312`） | **队列内**可开 | N ＝ 35（2026-09-02 判据裁定：以看板判据为准，`partial` 入池、🛑 排除） |
| 本件扫描器 | **队列外**未承接 | 14 个三处皆无 |

⇒ 建议**独立扫描器、与 `open_pool_reminder` 共用「三处查」判定函数**（Shao Peishen 答 (a)）：不把两个语义混进同一个池，但共用判定避免第二套逻辑。
