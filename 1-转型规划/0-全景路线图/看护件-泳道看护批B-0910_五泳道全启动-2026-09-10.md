---
title: "看护件 · 泳道看护批 B-0910_五泳道全启动（2026-09-10）"
created: 2026-09-10
status: 已收口（五泳道全终态 2026-09-10 09:05，OP-0910-G；ff 入 master 待 Shao Peishen 一字母）
批次: B-0910_五泳道全启动
派出线: Cowork 业务总线 OP-0910-A
---

# 看护件 · 泳道看护批 `B-0910_五泳道全启动`

## 〇、口径

- **触发**：Shao Peishen 2026-09-10 07:2x「**全部启动**」＋ 07:5x「我已回 lan」。规则正本＝`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（v2.2）。
- **D1 四档现取**（`工具-泳道看护状态机.py criteria`，非手抄）。**判据未覆盖按 🟡 停。**
- **前置四查（2026-09-10 07:5x 本机现取）**：① LAN 探针 ＝ **✅ on-LAN（三项齐备）**；② 分支 master、`ahead=0 behind=0`；③ 编辑锁**无占用**；④ 上一批 `B-0909_三泳道夜批` summary 已出（**停 0／转出 0／通知失败 0**）。
  > ⚠️ **同日 07:23 首查时为 ⛔ off-LAN，07:5x 重取已 ✅ on-LAN**（他回内网）。**起棒时距本行时间已久，看护者应自行再跑一次**（同 §一 `#522` 现取表过期守）。
- **上限**：默认 10；本批实际入批 **5 条泳道 / 5 个队列行**。
- **交付模式**：**CC 看护者单粘贴 ＋ 看护者用 Task/Agent 扇出**。他只粘 §三bis 一段。

## 〇bis、🔴 「我已回 lan」的处置结论：**本轮无 `.51` 待办，不起 LAN 收口批**

按 `zhuopin-lan-closeout` 汇集三源，逐条现取判定：

| 源 | 命中 | 判定 |
|---|---|---|
| 两份队列 `--digest --grep`「LAN 留步」 | `#312`／`#398`／`#419`／`#502` | 见下逐条 |
| 近期看护件 LAN 留步节 | `#312`／`#354`／`#418` | `#354` 行内明写「不要再当 LAN 留步捞出」；`#418` 行内明写「这不是 LAN 留步，是无人在场时不自行执行的生产写动作」⇒ 均不入 |
| 昨日收口批 `B-0909_506部署与419取证` | `#506`／`#419` | **已起链并跑完**，见下 |

- **`#506`（SC2 部署，⏰ 09-11 关键路径）＝ ✅ 部署已完成**：行内有完整记录——合入走 `cherry-pick f716db2→b226075`（`--ff-only` 因分叉 59 笔不可行，他订正① 授权等价路径，**树 diff 空**）、订正③ 实测 `iso_period` 确为 W37、**回滚未触发**、证据在 `队列行日志/#506.md` 2026-09-09 节。**本次独立复验 `.51` 实况**：`:8096` 可达、`GET /api/ping` ＝ **200**。⇒ **不需再部署**。行内「仍不闭」的唯一原因是 `采购部#22` 走「⏳ 待你审」，**L2 不代办**。
- **`#419` ＝ 已不是 LAN 留步**：昨日 `OP-0909-K` 已完成只读取证（**零新证据**——因留步文本写于 08-26、08-27 取证后未同步更新，扫描器据旧文本误判入批）；**两问已全部答毕**（09-08 答 ①(a)、09-09 答 ②(a)）。⇒ **剩余＝纯库内实现（期望产出⑵）**，故本批以泳道 `419-ops-webhook` 承接，**不走 LAN 收口**。
- **`#312`／`#398` 不入**：`#312` 剩「完整 7 天周期轨迹」一项未验＝**时间等待**，非 LAN 留步；`#398` `[S:blocked]`，两处留步为「守卫拦截／待拍板」，非 `.51` 面。`#502` 已 `[S:done]`。

🔑 **结论：三源汇集后 `.51` 面待办为空。**「我已回 lan」本轮不产生收口批——**这本身是好消息**：昨日那批把 `.51` 面清干净了。

## 一、硬边界

1. 🔴 **⏭️ 档（`.51` 部署及任何触碰生产服务的动作）本包不执行**，自动 `transfer-out` 交 `zhuopin-lan-closeout`。**本批 5 条泳道全为纯库内活，零 `.51` 触碰**——`419-ops-webhook` 改的是 `scripts/alert_webhook.py` 源码与其单测，**不部署、不重启 `.51` 上任何服务**。
2. 🔴 **🔴 档（对外发送／L2 门禁签字／合规红线变更／ASIL C-D）永不代办**。
   > ⚠️ **本条 07:5x 初稿曾写「`采购部#22` 仍走 ⏳ 待你审，任何泳道不得代发」——该表述已过时，08:0x 现取订正**：
   > `工具-跟进信README查询.py --row 采购部#22` 实测发送状态列 ＝ **`✅ 已推送 2026-09-09 07:07 UTC`**（＝北京时间 09-09 15:07），**信已发出**。
   > 初稿那句抄自 §四 `#176`，而 `#176` 至今仍写「仍走 ⏳ 待你审」⇒ **是 §四 未同步，不是信没发**。
   > 🔴 **对泳道的约束不变且更强**：`采购部#22` **已发出，任何泳道更不得重复发送或补发**；其回件到达后由拆件巡逻承接，不在本批范围。
3. 🔴 **不碰在跑行 `#462`／`#506`**（`#506` 部署已完，但其 §四 `#176` 解押证据与 `采购部#22` 属 🔴 档）。
4. 🔴 **看护会话不修工具**：跑批期间发现机制缺陷只登记＋出派单，不在本会话修；唯一例外＝本批会因该缺陷失败且修补 ≤10 行。
5. **看护件落档失败即全停**。

## 二、次序与并行矩阵

**波 1（并行 4，触碰区两两零重叠）**

| 泳道 | 承接 | 触碰区 | 硬序 |
|---|---|---|---|
| `535-fixture-date` | §一 `#535` | `0-学习与工具/test_工具-共享文档编辑锁.py`（**仅该测试类的夹具段**） | 无前置。🔴 **现在正红**（实测 6 failed / 5 passed） |
| `536-summary-scope` | §一 `#536` | `0-学习与工具/工具-泳道看护状态机.py`（`summary` 段）＋其单测 | 无前置。🔴 **与 `#529` 同碰本文件，`#529` 本批不入**（见下） |
| `537-anchor-count` | §一 `#537` | `0-学习与工具/hooks/hooks-userpromptsubmit-standing-five.ps1`＋`hooks-stop-decision-check.ps1`（仅 L12 文案）＋对应单测 | 无前置。🔴 **每轮交互都在打告警**，且在全腿回归里表现为一条常红 |
| `507-sweep-selflock` | §一 `#507` | `0-学习与工具/工具-落库sweep.py`（覆盖校验段）＋其单测；`openspec/changes/`（新包） | ✅ **阻塞已于本日 07:4x 解除**，见下 |

**波 2（1 条，独占）**

| 泳道 | 承接 | 触碰区 | 硬序 |
|---|---|---|---|
| `419-ops-webhook` | §一 `#419` | `5-平台底座/wecom-aibot-service/scripts/alert_webhook.py`、`deploy-server.ps1`、`tests/test_alert_webhook.py`、`openspec/changes/sweep-ops-webhook-cutover/` | 🔴 **排波 2 的理由**：它碰 `deploy-server.ps1`（部署脚本本体），虽**本棒只改不跑**，仍与波 1 的 `507`（sweep 覆盖校验，会读部署相关路径）保守错开 |

🔴 **`#529` 本批刻意不入**：它改 `check-timeout` 的**语义与阈值** ⇒ D1 🟡 档（改口径判据阈值），泳道跑到那步必然 `pause`；且它与 `536` 同碰 `工具-泳道看护状态机.py`。**待 `536` 落地后另批排**。

### 本批派出前，总线已代做／已核实的五件

1. **`#535`／`#536` 已由 §四 `#191`／`#189` 升格**，升格前均**重跑现取实测**（`#535` ⇒ 6 failed/5 passed；`#536` ⇒ 夜批 3 条泳道报「终态 5 条」）。
2. **`#537` 是本线自己造成的**：2026-09-09 23:0x 本方按他立法请求往根 `CLAUDE.md` §5 加了第 6 条常驻纪律 `UPS5:6`，而 hook 判据写死「命中 5 条」⇒ 此后每轮告警。**修法方向已写死：不是把 5 改成 6**，而是改为按实际锚点集判连续性／重复／缺号；**`UPS5:6` 不回退**。
3. 🔴 **`#507` 阻塞解除，成因值得看**：它原写「与在跑的 `B-0908_四泳道机制根治` 泳道 `500-sweep-carrier` 同触碰同两文件」——**两个前提都不成立**：该看护件 frontmatter `status` 至今是**「待你在 CC 里起」**（那批**从未派出**），且 `500-sweep-carrier` 的活**已做完并进 master**（§一 `#500` 本日据 `patch-id` 三重验证销号）。⇒ **开工须先现取 master 版本**，`#500` 的「第5类脏检查放宽」已在 master（`git grep` 2 处命中），本行修复要**建在那之上**。
4. **`#419` 的两问已答毕**，剩余是实现；行内自陈「修法实现＝本行期望产出⑵，不另立行（协议〇.10）」。
5. **全库缺 `[D:]` 行 ＝ 0 条**（本日 07:2x 补齐 9 条存量后回读校验）。

## 三、泳道 opener（三行结构；全部由 `工具-opener生成.py --variant subtask_lane` 产出，未手抄）

### A1 · #535 单测夹具日期硬编码，跨零点自动转红

粘贴端：CC ｜ 泳道：535-fixture-date

```
[OP-0910-B]【CC】535夹具日期
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910b-535-fixture-date`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51 生产服务／企微机器人常驻／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_五泳道全启动）
读 ① `队列 §一 #535` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A2 · #536 summary 终态数未按批次收敛

粘贴端：CC ｜ 泳道：536-summary-scope

```
[OP-0910-C]【CC】536summary收敛
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910c-536-summary-scope`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51 生产服务／企微机器人常驻／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_五泳道全启动）
读 ① `队列 §一 #536` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A3 · #537 hook 锚点数硬编码致每轮告警

粘贴端：CC ｜ 泳道：537-anchor-count

```
[OP-0910-D]【CC】537锚点数
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910d-537-anchor-count`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51 生产服务／企微机器人常驻／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_五泳道全启动）
读 ① `队列 §一 #537` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A4 · #507 sweep 文件清单覆盖校验自锁

粘贴端：CC ｜ 泳道：507-sweep-selflock

```
[OP-0910-E]【CC】507sweep自锁
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910e-507-sweep-selflock`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51 生产服务／企微机器人常驻／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_五泳道全启动）
读 ① `队列 §一 #507` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

### A5 · #419 运维逃生通道第三道防线 · 实现（期望产出⑵）

粘贴端：CC ｜ 泳道：419-ops-webhook

```
[OP-0910-F]【CC】419逃生通道
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0910f-419-ops-webhook`） ｜ worktree：☑（新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51 生产服务／企微机器人常驻／定时任务） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_五泳道全启动）
读 ① `队列 §一 #419` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由 sweep 收尾段或看护者收工时串行做（构建环境瘦身第三轮方案 P4）。
```

## 三bis · 看护者开场词

```
[OP-0910-G]【CC】看护五泳道全启动
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0910-A（批 B-0910_五泳道全启动）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0910G-看护五泳道全启动。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0910_五泳道全启动-2026-09-10.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0910_五泳道全启动` **现取**，不手工数。

🔴 **本批有一个特殊约束**：`536-summary-scope` 修的正是 `summary` 自己。**收工汇总若在该泳道合入之后跑，须注明用的是修前还是修后版本**；且 §一 `#536` 已写明「本次只实证了『终态泳道数』一列，其余列未逐一验」——**收工时不得因为看到数字就认为其余列已正确**。

🔴 **等待时长只认 `reports/lane-watch-state.json`（由 summary 现取），会话不得自算。**

🔴 **心跳一律跑命令写**：`python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane <泳道标识> --text "<一句话>"`，收工带 `--done`；**别传 `--repo-root` / `--heartbeat-file`，它没有这两个参数**。

### 本批四条泳道同源，收工时值得一并看

`#535`（夹具硬编码日期，跨零点转红）与 `#537`（hook 硬编码锚点数，加一条转红）**是同一族**：**任何「写死的数量／日期」都会在下一次变化时自动转红**。`#536`（summary 计数不按批次收敛）与 `#507`（覆盖校验自锁）则同属「**用来发现问题的东西自己坏了，而坏了不产生信号**」（§一 `#398` 立的那一族）。⇒ **收工汇总建议各写一句：这两族在本批之后还剩哪些同族未修。**

## 四bis、实测汇总（看护者 OP-0910-G，2026-09-10 09:05 本地现取）

**手段**：`工具-泳道看护状态机.py summary --batch B-0910_五泳道全启动` 跑两版——master 版（`#536` 修前）与分支 `dbf56a4` 版（修后，临时根目录镜像 `reports/lane-watch-state.json` 只读副本跑，未改真状态文件）；分支核验＝`git ls-remote origin 'refs/heads/claude/op0910*'` ＋ `git diff --name-only 4f9b422..<分支>` 两两取交集。

| 项 | master 版（修前） | `#536` 修后版 | 看护者按看护件手工确认 |
|---|---|---|---|
| 停 | 1 次（`507` 🟡 design 审，等 26 分钟，状态文件现取） | 1 次 | ✅ 一致 |
| 转出 | 0 | 0 | ✅ |
| 终态泳道 | **9 条**（把昨夜 `B-0909` 批 5 条也算进来＝`#536` 缺陷当场复现） | **1 条**＋「归属未知 8 条」 | **本批实际 5 条**：`535`／`536`／`537`／`419` done，`507` resumed（design 审已过，剩 apply 棒） |
| `index.lock` 撞击 | 0 | 0 | ✅（编辑锁撞锁另计：`507` 三次、看护者两次，均等自然释放） |
| 企微通知失败 | 0 | 0（修后标签改为「全库累计」） | ✅ |
| ⏭️ 放行 | 0 | 0 | ✅ |

🔴 **两版数字都不是「对的数」**：修前往多报（跨批串），修后报 1 条是因为只有 `536` 自己的 `--done` 带了 `--batch`——`535`／`537`／`419` 收工时未带（看护者派单附注模板同 SKILL 样例一样不带，§四 `#195` 已登）。上表「手工确认」列是唯一可信列。

### 五泳道终态

| 泳道 | 分支 @ commit | 终态 | 零回归实测（泳道自报、看护者核 ref） |
|---|---|---|---|
| `535-fixture-date` | `claude/op0910b-535-fixture-date` @ `369fe1b` | done | 纯 master `6 failed/522 passed` → 本分支 `531 passed`；新增源码级闸 `FixtureDateTimeBombGuardTests` |
| `536-summary-scope` | `claude/op0910c-536-summary-scope` @ `dbf56a4` | done | `107 passed`（+7），变异验证 3 条转红后恢复 |
| `537-anchor-count` | `claude/op0910d-537-anchor-count` @ `e072d3b` | done | `test_hooks-p3.py` 43 passed；**全量 `pytest 0-学习与工具` 未跑成**（并发窗口 50 分钟未结束被 kill），ff 前须无并发串行复跑 |
| `507-sweep-selflock` | `claude/op0910e-507-sweep-selflock` @ `5f95d36` | resumed | 零代码；`openspec validate --all --strict` 188/0；design 审 D1–D5 已过（D6 承继约束） |
| `419-ops-webhook` | `claude/op0910f-419-ops-webhook` @ `bce5253` | done | `test_alert_webhook.py` 14 passed；`wecom-aibot-service` 全量 824 passed/1 skipped；未触碰 `.51` |

五分支改动文件两两零重叠、与 master `4f9b422..00ea9c8` 前进的 10 笔零交集（`comm -12` 空）。**均未 ff master**（P4，🟡 档，待 Shao Peishen 一字母）。

### 两族在本批之后还剩什么（看护件 §四 要求各写一句）

- **「写死的数量／日期」族**（`#535`＋`#537`）：本批把夹具日期（含 `WaiverCli` 的 10 处 `12-31` 与元旦必红一例）和 hook 锚点数都改成自适应；**已知残余＝§四 `#192`**（`TotalByteCap = 300` 未随条目数走，每加一条常驻纪律就静默砍短一次，🟡 无默认）。
- **「发现问题的东西自己坏了且不出信号」族**（`#536`＋`#507`，`#398` 立）：本批让 summary 不再跨批串、给 sweep 覆盖校验换了锚（design 已过、apply 未做）；**已知残余＝§四 `#195`**（`--done` 不带 `--batch` ⇒ 终态数从「错的」变「没有」还不是「对的」）、`#507` apply 棒、`#529`（`check-timeout` 语义，本批刻意不入，待 `536` ff 后另批）。

### 本批新登记

§一 `#507` 状态回灌（`[S:blocked]`→`[S:open]`，design 审结论合入）；§二 `B-0910_G507状态回灌与design审记录`（sweep 已落 `77c2754`）、`B-0910_535夹具日期`、`B-0910_536summary批次收敛`（sweep 已落 `3e069cd`）、`B-0910_537锚点数判据`、`B-0910_419逃生通道`；§四 `#192`（`537` 登，🟡 无默认）、`#195`（`536` 登）。
