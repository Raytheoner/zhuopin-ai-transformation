---
title: "【CC】开场 prompt · #192／#193／#194／#198／#199 sweep 与机器人机制五行同批"
created: 2026-08-02
执行方: CC 平台（建造车间）
执行环境: CC
来源: 队列 #192／#193／#194／#198／#199（Shao Peishen 2026-07-31／08-01 两次拍板\"五行同车\"）；本件由 Cowork 环境保障线备件
派发方: 环境保障线（属**环境整治·自动化机制**，按 CLAUDE.md §5 铁律一细化，本线可备派单件并协同 CC 执行）
status: 待执行
---

# 【CC】开场 prompt · sweep 与机器人机制五行同批

## 【开场词（复制即用）】

```
【设置】执行环境：CC ｜ 分支：新建 fix/sweep-aibot-followup-192-199 ｜ worktree：☑ ｜ 工作区：🔴 有常驻副本与在跑任务——见「工作区字段」段，动手前必读

读 1-转型规划/0-全景路线图/开场prompt-【CC】192-199-sweep与机器人机制五行同批-交接.md ＋ CLAUDE.md 当前进度恢复上下文，按文件执行。
🔴 五行必须同一 session 一并做，勿拆：#192/#194/#198 撞同一个 `工具-落库sweep.py`，#192/#193/#199 撞同一次机器人部署与重启。
🔴 #192-A 有硬前置（#192-C 路径对齐），顺序错了会静默空转；sweep 起跑段的最终顺序已在 §三 写死，照做。
开干前先答 §五 的两个澄清问题（均为带推荐项的选择题）。
```

---

## 一、🔴 先读这一段：本件为什么必须整批做

五行分属两个物理落点，**各自内部撞同一个文件、跨落点共用同一次部署验证**：

| 落点 | 涉及行 | 撞车理由 |
|---|---|---|
| `0-学习与工具/工具-落库sweep.py` | **#194**（起跑补推）／**#198**(a)(b)(c)／**#192-A**（flush 改挂 sweep） | 三者改**同一个起跑段**。分三次改必然反复冲突/覆盖，且每次都要重跑一遍 sweep 的真实验证 |
| `5-平台底座/wecom-aibot-service/` ＋ 计划任务 | **#192**-B/C／**#193**／**#199** | 同动机器人服务、同需 `ops/wecom-service-home` 同步 + 重启 `ZhuopinAibotDevListener` 验证；#199 那个计划任务正是 #192-A 第二道兜底要挂的载体 |

**共同病理（五行同族）**：**失败/推迟留了痕，但没有任何机制去收敛它**——与已完成的 #171／#172／#180 是同一族病的下一批实例，**判据与去重逻辑可直接复用，勿重新发明**。

**优先级实况**：#198(a)=P0／#192-A=P1／#194=P1（08-01 由 P2 提级）／#199=P1／#192-B=P2／#193=P2／#192-C=P3／#198(c)=P3。**按 P0→P1 排序落地，但同一 session 内一并交付。**

---

## 二、「工作区」字段（协议〇.1 第四字段）

**自检一问：本次改动，有没有任何一份"正在跑的副本"不在我改的这个 checkout 里？**

🔴 **有，三处，且其中一处会与本 session 并发运行**：

1. **`ops/wecom-service-home` worktree ＝ 企微机器人的生产副本**，`ZhuopinAibotDevListener` 常驻在跑。改 `wecom-aibot-service/` 的代码**只在你的 worktree 里生效**，必须 `sync-to-server.ps1` 同步 + 重启该任务才在生产生效——**且重启后须用文件哈希/进程启动时间交叉确认新代码确已加载**（#168／#147 的既定做法，防"运气型未改动假象"）。
2. 🔴 **`ZhuopinCommitSweep` 每小时在跑，而本件正在改的就是 sweep 自己**。你的 worktree 改动不影响主工作区的在跑版本；**但合入 master 之后的第一轮，就是新代码的真实首跑**——请把"合入后手动 `Start-ScheduledTask -TaskName ZhuopinCommitSweep` 触发一轮并核日志"当作交付的一部分，不要合完就走。
3. **三个 Windows 计划任务**（`ZhuopinCommitSweep`／`ZhuopinAibotDevListener`／`ZhuopinDecisionReminderDaily`）的**实际设置不在仓库里**——改注册脚本源码 ≠ 改了在跑的任务（#199 的核心教训之一）。

**长驻 worktree 现状（2026-08-02 15:45 本地 / 07:45Z 本机实测）**：3 个（`musing-pascal-68d14e`／`qd-b-grayscale-improvements-9dbe6f`／`wecom-service-home`），均停在 `fa9c5b8`、**落后 master 17 提交**。另有一个 0 文件空壳目录 `sweep-criteria-sync-fix-7eb8a7`（git 已不认，待 #98 体检清）。

**不涉 `.51` 四服务。**

---

## 三、执行顺序（🔴 sweep 起跑段的顺序已写死，照做）

### A 段 · 先修硬前置：#192-C 路径对齐（否则 A 项静默空转）

- 两个 pending 文件的落点现**硬编码 `SERVICE_DIR / "reports"`**（`run_aibot_service.py` L130/L134），即只存在于 `ops/wecom-service-home` 侧；而 audit 已按 #126 走 `resolve_repo_root()` 解析到主工作区——**两者分处两地**。
- **改法**：两个 pending 路径统一改走 `resolve_repo_root()`，与 #126 对齐。
- 🔴 **为什么必须排第一**：#192-A 要让跑在**主工作区**的 sweep 去 flush 这个文件；**不先对齐，sweep 会去错的 checkout 找、flush 永远空转且不报错**——又一个静默失效。
- ⚠️ **两个暂存文件物理分开，勿混淆**：`pending_queue_lock_appends.jsonl`＝#168 锁忙推迟（**有**消费方 `flush_pending_queue_appends`）；`pending_queue_appends.jsonl`＝#90 D1 的 git 推送失败暂存（`queue_git_sync._append_pending_record` L292 **只写不读、全库无读取方**，实测现存 6 条 error 从未处理）。
- **顺带**：给后者一个消费方（并入 A 的 flush 一起，或至少让哨兵/每日提醒发现"有未消解 error"报一条）；清掉 4 条已修 bug 的历史残留——**2 条推送失败须先确认内容是否已被后续成功推送覆盖，不确认不删**。

### B 段 · sweep 起跑段（#198(b) → #194 → #192-A → 原有流程）

**最终顺序（队列 #192 行内已写死，勿自行调整）**：

1. **`#198(b)` 起跑探编辑锁** —— 提到 `_check_preconditions` 之后、**任何 git 写动作之前**；占用中直接跳过本轮、**一个 git 动作都不做**。现状是 `_process_normal_batch` 先 `git add` 再由 `_strike_off_rows` 去 acquire，锁占用时暂存区里已经是半成品。
2. **`#194` 补推未推送提交** —— 无条件查 `git rev-list --count origin/master..HEAD`，>0 且可快进即先补推（推成功再往下走批次）；非快进则复用 #171 的 webhook 告警通道 + 非 0 退出码收尾。🔴 **判据不得绑在"§二 有无待处理批次"上**——正是这个前置条件让 07-31 那轮空转掉了。🔴 **必须覆盖"任何一处 push 失败后的未推送提交"**——已实证至少两个独立出口（批次主流程 与 `_rerun_ledger` 台账重跑），**只在批次分支加补推不够**。
3. **`#192-A` flush 一次** `pending_queue_lock_appends.jsonl` —— 🔴 **必须在 sweep 取锁窗口之外调用**（flush 内部要取编辑锁，`_strike_off_rows` 也取，会重入）。三个实现约束：⑴ 锁重入见上；⑵ `flush_pending_queue_appends` 是 `async def`、sweep 是同步脚本，需 `asyncio.run()` 包一层；⑶ **flush 异常必须捕获并记日志后继续跑批次**，不可让兜底把主干带崩。
4. 原有前置检查与批次处理。

**另两项**：

- **`#198(a)` 通用异常兜底包住整个 `main()`** —— `except Exception as exc` → 日志写一行 `✗ 未预期异常：<类型>: <消息>` + traceback 末几行 → 复用 #171 webhook 告警 → **退出码用一个新的独立值（如 3）**，与"健康跳过（0）"「分叉（`FORK_EXIT_CODE`）」区分。⚠️ **新增的异常日志行必须显式标注 `UTC`**，与既有 `_now_utc_str()` 一致。
- **`#198(c)` 常驻服务部署提示（P3）** —— 本轮 commit 若命中有常驻副本的路径（如 `5-平台底座/wecom-aibot-service/`），在日志与 webhook 附一句「⚠ 本批改动涉及常驻服务，需 `sync-to-server.ps1` 同步 `ops/wecom-service-home` 并重启 `ZhuopinAibotDevListener` 后才在生产生效」。**纯提示、不阻断、不改退出码**——sweep 无权也不该去重启服务。

> **#198(a) 的核心验收物是判据恢复单义**：修完后 `sweep-commit.log` 零新增行的含义正式收敛为唯一一个——**"任务根本没启动"**（触发器没生效／机器未运行／Principal 权限被拒），不再包含"跑了但崩了"。落点＝`3-治理与合规/.51部署标准清单-服务侧与笔记本侧-2026-08-01.md` §四⑦（两态说明已同日落字）。**⚠️ 生效严格绑定 (a) 落地**——未修完前 ⑦ 判据仍是二义的，排查时必须并查 `ID=27／42／107` 电源事件。

### C 段 · 机器人侧（#192-B → #199 → #193）

- **`#192-B`（P2）配对不变式恢复**：`queue_lock_pending.py` L107 写的是 `queue_append_pending_flushed`，而 #107 的 `find_unreconciled_archives`（L61-62／L168-169）只认 `archived`/`queue_appended` 两类 → 当日 9 个 `archived` vs 8 个 `queue_appended`。**二者择一**：补录成功时补记一条 `queue_appended`（保持"每条 archived 必有配对"），**或**让哨兵把 `queue_append_pending_flushed` 也视为配对事件；**加单测覆盖"推迟→补录→哨兵零误报"**。
- **`#199`（P1）——🔴 先做 (1) 再做 (2)，勿颠倒**：
  - **(1) 查清 `LastTaskResult=0x800710E0` 的触发条件**。**欠账已由环境保障线 2026-08-02 查明、主命题已证实**：audit 真身＝`5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`（**不是仓库根 `reports/`**）；全量 `decision_reminder` 事件只有两条（`07-31 05:56:56Z / 13:56:56 本地` 手动触发、`08-02 00:30:09Z / 08:30:09 本地` 正常触发），**08-01 那天零记录 → 那次运行确实没干活、提醒未送达**。⚠️ **但同时证实是间歇性、非持续故障**（08-02 已正常，本 session 开工现读亦为 `LastResult=0x0`）——**间歇性意味着有触发条件、不是随机**，这才是要查的东西。查证起点＝上述 audit 真身路径 + `Get-ScheduledTaskInfo` 历史。
  - **(2) 再做设置对齐**：① `Set-ScheduledTask` 补 `-StartWhenAvailable`；② `register-decision-reminder-task.ps1` 源码同步补该参数（否则重注册又丢）；③ 顺带补 `-AllowStartIfOnBatteries -DontStopIfGoingOnBatteries` 并核对另两个任务是否齐全。④ 已由本线落字，勿重做。
  - ⚠️ **勿据 #199 初稿的"必然漏跑"立论**——那句已于同日被实测部分推翻并撤回（`NumberOfMissedRuns=0`、唤醒 6 分钟后确实跑了）。**`StartWhenAvailable` 的语义边界至今未查清，队列里如实留白，不要在本件里编一个解释补上。**
  - ⚠️ **改在跑的计划任务设置实测需管理员权限**（同 #189 处置方式）→ 见 §五 问题 2。
- **`#193`（P2）断连"进行中"零信号**：断连持续超阈值（建议 60~90 秒）即私信一条**进行中提示**（含已重试次数与下次退避秒数），恢复后仍发既有 `gap_alert` 作收尾。**判据复用 #147 已建的 `liveness.py` 存活戳**（刻意不进审计链），**去重按 #172 的"新增/超期才提醒、绝不存在即提醒"口径**——避免把一条有用告警变成噪音。
  - **次要项（顺带、不单独设计）**：加 `AtStartup` 触发器与任务级 `RestartCount/RestartInterval` 作二层兜底，覆盖"整条链被杀但机器不重启"这个未实证发生过的窄窗口。
  - ⚠️ **本行原命题"守护者被会话终止杀掉"已被推翻**（实为手动关机 + hiberboot，`ID=27 引导类型 0x1`），**勿据原命题设计**。
  - ⚠️ **一条天花板约束，勿当配置问题绕**：SYSTEM 在 OneDrive 个人版路径下静默权限被拒（#96 素材⑤）、`LogonType S4U` 注册需管理员权限（07-31 实测 `Access denied`）→ 新增任何健康检查任务同样受"用户已登录"限制。

### D 段 · 部署与验证

1. **全量回归零漂移**（wecom-aibot-service + 平台底座 + 受影响场景）。
2. **真实部署 `ops/wecom-service-home` + 重启 `ZhuopinAibotDevListener`** —— 重启前先清理 `Stop-ScheduledTask` 可能未级联杀死的孤儿进程（#171 的既有教训），重启后**用文件哈希/进程启动时间交叉确认新代码已加载**。
3. **sweep 合入 master 后手动触发一轮**（`Start-ScheduledTask -TaskName ZhuopinCommitSweep`）并核 `reports/sweep-commit.log` 新增行——**这是新起跑段的真实首跑**。
4. **三任务设置并列输出**（`Get-ScheduledTask` + `Get-ScheduledTaskInfo`）作 #199 的验收证据。

---

## 四、验收清单（逐条留可核验证据）

| 行 | 验收物 |
|---|---|
| #192-A | 双载体 flush 生效（sweep 每小时为主 + 每日 reminder 第二道），**真实构造一次"锁忙推迟 → 下一轮 sweep 自动补录"** |
| #192-B | 配对不变式恢复 + 单测"推迟→补录→哨兵零误报" |
| #192-C | 两 pending 路径对齐 #126 + 失败暂存有消费方 + 历史残留清理（2 条推送失败须先确认再删） |
| #193 | 断连进行中提示（阈值触发 + 含重试次数 + 去重）；次要项 `AtStartup`+`RestartCount` |
| #194 | 起跑补推 + 单测两种（"提交成功推送失败→下一轮自动补推"／"非快进不强推"）+ 真实验证一轮 |
| #198 | (a) 异常兜底 + 日志留痕（**标 UTC**）+ webhook 告警 + 独立退出码；(b) 起跑前置探锁；单测含"注入异常必写日志"与"锁占用时零 git 动作"；(c) 部署提示 |
| #199 | (1) `0x800710E0` 触发条件结论（**查不出也要如实写"未查清"，不要编解释**）；(2) 三任务设置对齐 + 注册脚本源码同步 + 并列输出 |

🔴 **一项不得随销号消失的观察项（#194 行内明写）**：修复后需**观察一周频次**以分清 (甲) 网络本身变差／(乙) 1h 周期放大既有窗口。**回写时必须明确写出由谁承接**（环境保障线 或 值周巡检），**不得让它随 #194 销号一起消失**。

---

## 五、开工前先答这两个澄清问题（均带推荐项）

**问题 1 · 本批是否走 openspec 变更包 + design 审？**
- **(a)【推荐】整批走一个变更包 + design 审。** 代价＝约半天；好处＝五行同一份 design 覆盖，且本批**命中 CLAUDE.md §5「机制/工具类 openspec 触发门槛」至少两条**：#198(a) 新增独立退出码 + 把"日志零行"判据收敛为单义 → **改变全项目排查口径（判据①）且改变 sweep 对外语义（判据③）**；#192-B 改哨兵配对判据 → 判据③。总纲原话「**判不准就走**」。
- **(b) 只给 #198(a) 与 #192-B 走，其余直接改。** 代价＝一批改动拆成两种流程、design 里描述不完整；好处＝P0 修复更快落地。
- **(c) 全不走。** 代价＝IATF「单一可信源」下站不住，且与 #206 正在做的"把门禁迁回 openspec"背道而驰。

**问题 2 · ~~#199 的在跑计划任务设置对齐，本次就请 Shao Peishen 提权跑一行，还是只改注册脚本源码？~~ ✅ 已由 Shao Peishen 于 2026-08-02 拍板选 (a)，无须再问**
- **(a)【✅ 已拍板】本次就请他提权跑（同 #189 先例）。** 好处＝**#192-A 的第二道兜底才真正装在一个设置完整的任务上**，否则那道兜底在它唯一被需要的场景（周末/清晨未开机）下仍可能失效。
- ~~(b) 只改源码，实际设置留待下次重注册。~~ 代价＝源码与在跑设置继续分叉，正是 #199 本身的成因形态（"改脚本 ≠ 改在跑的任务"）。**未选。**

> **🔴 执行方式（据此拍板，领取方照做）**：**先把 `register-decision-reminder-task.ps1` 源码改好**（补 `-StartWhenAvailable -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries`），**再把需要提权执行的那一行/那一段整理成可直接复制的单个代码块交给 Shao Peishen 跑**（勿分散成多条、勿要求他自己拼参数）；他跑完后**你自己用 `Get-ScheduledTask` 三任务并列输出复核实际生效**，**不以"他说跑过了"为准**（CLAUDE.md §5「不信工具说成功了」同理适用于人工步骤）。**若三任务并列输出显示仍未生效，如实回报，不要改写验收口径。**

---

## 六、收工段

1. 回填 #192／#193／#194／#198／#199 五行（**逐行销号，勿只写一行"整批完成"**）；#194 的一周观察项**写明承接方**。
2. 若 `0x800710E0` 查明成因，**在 #199 行内写清触发条件**；查不出就写"未查清"并说明已排除什么。
3. 全部新产出/修改文件清单 + 建议 commit message；CC 自行 commit + push + 收工重跑台账。
4. 部署与验证证据（哈希/进程启动时间/日志新增行/三任务并列输出）随回填一并留痕。
5. 会话末「需你定夺」小节：编号 + 带字母标签选项 + 每项写清选它会发生什么与代价 + 标注默认项；纯状态汇报另起「状态同步（无需你答）」。
