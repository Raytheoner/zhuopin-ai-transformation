---
title: 派单件 · 【CC】`#629` 必读件缩编（结构性迁出）＋ 体量棘轮守
created: 2026-09-21
派出线: 环境总线 OP-0921-A
批次: B-0921_必读件与分诊
任务类: A 类
---

# 派单件 · 【CC】`#629` 必读件缩编 ＋ 体量棘轮（§一 #629）

## 〇、现取体量（2026-09-21 实测，不必再量一遍）

| 对象 | 现取 | 阈值／状态 |
|---|---|---|
| `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` | **45,119 B** | 🔴 **无任何机器守**——本棒要补的正是这一条 |
| `.claude/rules/` 合计（6 份） | **43,388 B** | `CLAUDE_MD_RULES_TOTAL_BYTE_CAP` 30,720 B，**超 12,668 B** |
| 根 `CLAUDE.md` | **12,110 B** | `CLAUDE_MD_ROOT_BYTE_CAP` 12,288 B，余 178 B |

`.claude/rules/` 分项：`两桌同步与取证.md` 12,339 ｜ `环境保障线.md` 7,864 ｜ `队列与落库.md` 7,745 ｜ `场景建造与合规.md` 5,966 ｜ `跟进信与专员.md` 5,231 ｜ `文档与全景治理.md` 4,243。

## 一、方案闸（已过，照做即可）

1. **判据粒度**：体量一律 `(Get-Item <路径>).Length` 现取，**禁 `cat` 正文比对**；改完只回显新旧字节数。
2. **narrow**：新增/改动的 sweep 告警类，回归只跑该类 node-id（`文件::类`），**禁 `-k`**，禁跑整份回归。
3. **长跑段**：跑测 >3 分钟走后台＋哨兵，起跑前先删旧 `.done`，判完成比 mtime。
4. **产出落点**：迁出的成因／沿革一律进 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md` 附录；尖峰榜落 `1-转型规划/0-全景路线图/必读件尖峰榜-Win侧-2026-09-21.md`。

## 二、做什么

### ⑴ 🔴 结构性迁出，**不是删**

照根 `CLAUDE.md` 13,148 → 12,110 B 那次的手法：**成因／沿革／实证故事迁 CHANGELOG 附录，正文只留「触发 → 动作 → 判据／指针」**。迁出处必须留一句指针（写明迁到 CHANGELOG 哪个附录），使原文仍可 grep 得到。

- **`zhuopin-lane-watch/SKILL.md` 45,119 B**：这是本棒的主目标。正文只留：触发口令 → 各档动作 → 判据（或「现取 `工具-泳道看护状态机.py criteria`」这样的指针）。所有「为什么这么定／哪天撞过什么」全部迁 CHANGELOG。
- **`.claude/rules/` 合计压到 ≤30,720 B**（即至少减 12,668 B）。优先从最大的 `两桌同步与取证.md`（12,339 B）与 `环境保障线.md`（7,864 B）下手，同样只迁成因／沿革。

### ⑵ 根 `CLAUDE.md` §3 转场自检改「只在阳性时提醒」（**减字**）

现行 §3 末条要求「全阴性 ⇒ 明说『不必转场』」。他 2026-09-20 已直令：**转场自检放后台做，不要每次在答复里打印结论**（「五条判据全阴性，不必转场」属污染）。把该条改成：**任一阳性 ⇒ 当场提醒并附引用版接力 opener；全阴性 ⇒ 不出声**。🔴 **只准净减字**，改完根 `CLAUDE.md` 必须 < 12,110 B。

### ⑶ 🔴 同批补一道**有消费者的体量守**——用**棘轮**形态，不新立阈值口径

落点：`0-学习与工具/工具-落库sweep.py` 新增**第 20 类常驻状态告警**，形状照**第 19 类**（承载性一致性扫描，见该文件 **6940–7060 行**的实现与 **8830 行**附近的接线；两处都要照抄形状）。

- 守的对象：`0-学习与工具/skills源码/**/SKILL.md`（逐份）。
- 口径：**棘轮＝只降不升**，与既有 `ROOT_RATCHET_*`（该文件 824–852 行）同一语义——基线取**本棒缩编后的现取字节数**（机械取值，不是拍脑袋定阈值），此后只允许更小；变大即 fail-loud 告警。
- 🔴 **不得另立新的字节阈值口径**（「改口径阈值」属 🟡 档，永不代办）。棘轮基线由实测值写死，并在代码注释里写清「基线＝2026-09-21 `OP-0921-A` 缩编后实测 N B」。
- 🔴 **新增常驻告警类必须同批给出沙箱夹具**（先例血泪：`#631` 第 19 类上线即三条红，正是夹具缺桩）——夹具＋接线单测与告警类同批交付，缺夹具不许写 `OPENER_DONE`。
- 🔴 告警须有消费者：接进 sweep 每轮回显（零命中时亦不省略），形状照第 19 类。

### ⑷ Win 侧必读件尖峰榜

产出一份榜单，把必读件分成两类并各自标体量：

- **真常驻**：CC 按 `paths`／`CLAUDE.md` 自动注入、每次开场必载的；
- **尖峰**：一读就是一整份、读进去即常驻整个会话的（如 `opener骨架.md` 42,266 B、skill 正本、看护件）。

榜单要点名「哪一件超了」，并给出下一刀的建议顺序。不在本棒里动这些文件（除上面 ⑴⑵ 点名的）。

## 三、期望产出（缺一个就不许写 `OPENER_DONE`）

1. `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`（显著变小，正文只剩触发→动作→判据／指针）
2. `.claude/rules/` 合计 **≤30,720 B**（贴出改前改后逐份字节数）
3. 根 `CLAUDE.md` **< 12,110 B**（§3 转场自检改只在阳性时提醒）
4. `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md`（新增附录承接所有迁出内容）
5. `0-学习与工具/工具-落库sweep.py` 第 20 类告警 ＋ 沙箱夹具 ＋ 接线单测（`0-学习与工具/test_工具-落库sweep.py`）
6. `1-转型规划/0-全景路线图/必读件尖峰榜-Win侧-2026-09-21.md`
7. 收工贴 `git diff --name-only master...HEAD` 的输出

## 四、不做什么

1. 🔴 **只准减字，不准往 `.claude/rules/` 与根 `CLAUDE.md` 加字**——本棒是唯一获准动这两处的一棒，且只能是净减。
2. 🔴 **不新立字节阈值口径**（见 ⑶），棘轮基线用实测值。
3. 🔴 **不得把内容直接删掉充作缩编**——迁出必须在 CHANGELOG 里逐句可 grep 得到，原处留指针。
4. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
5. 🔴 收工只 push 本泳道分支，**不碰主仓、不 ff master**。
6. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
