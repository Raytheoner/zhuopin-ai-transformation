---
status: 生效
title: "OneDrive 迁出 · 回滚演练结果（队列 §一 #407）2026-08-25"
created: 2026-08-25
编号: OP-0825-DRILL
执行环境: CC
派出线: 环境总线（Cowork）
承接队列行: §一 #407（母行 #170）、§一 #406（T2 对象）
来源派单件: `1-转型规划/0-全景路线图/派单件-【CC】OneDrive迁出回滚演练-队列170前置项2-2026-08-25.md`
---

# OneDrive 迁出 · 回滚演练结果

> 🔴 **本件不解除 #170 的 🔒 方案阶段红线。** T1 是演练、T3 是影子演练、T2 是独立缺陷修复，三者都不是「开始迁移」。迁移窗口仍未排期。
>
> 🔴 **生产完整性**：全程复核三次，**生产仓库 27 条 worktree 指针、4 个计划任务全部逐字未变**，取证见 §五。

## 〇、一句话结论

**演练的最大产出不是三段耗时，而是发现「T1 那条被派单件认证为『✅ 完全隔离、零生产影响』的步骤，按字面执行会劫持 26 个生产 worktree，且失败是静默的」。**
该步骤**没有按字面执行**——在实验室复现确认后改用了安全形式。若当时照做，随后「收工前删除副本」一步会把 26 个生产 worktree 依赖的 git 目录一并删掉。

| 件 | 结果 | 耗时 |
|---|---|---|
| **T1** worktree repair 演练 | ✅ **完成**（含 1 处安全改判 D2） | 复制 **165.3 s** ／ repair **0.33 s** ／ 校验 **1.14 s** |
| **T3** 计划任务影子演练 | ⛔ **阻塞：需管理员权限**（步骤 2–6 未做） | 注册尝试 5.02 s → `Access is denied` |
| **T2** editable 幽灵 import 修复 | ⛔ **未开跑：独占自检未通过**（硬约束 ④） | — |

---

## 一、T1 · worktree repair 演练 —— 完成，但派单件写法有致命缺陷

### 1.1 步骤 1 复制：`/COPYALL` 直接跑不起来（**坑 D1**）

派单件与迁移方案 §八 指定的 `robocopy … /COPYALL`，在**非管理员** shell 下 **0.1 秒即退出、退出码 16、一个字节没复制**：

```
ERROR : You do not have the Manage Auditing user right.
*****  You need this to copy auditing information (/COPY:U or /COPYALL).
```

`/COPYALL` ＝ `/COPY:DATSOU`，其中 `U`（审计信息）需要 `SeSecurityPrivilege`。
**改用 `/COPY:DAT /DCOPY:DAT` 后成功**（这是**改判 D1**，见 §四）：

| 项 | 值 |
|---|---|
| 退出码 | 1（＝有文件被复制，`≤7` 为成功） |
| Dirs / Files / Bytes | 14,817 ／ **57,210** ／ **1.393 GB** |
| **FAILED** | **0** |
| 墙钟耗时 | **165.3 s（2 分 45 秒）**，均速 655 MB/min |

⚠️ **派单件预期的「副本文件数应 ≈ 47,578」这条核对判据已失效**：实测 **57,210**，差 20%。原因是本会话开工时新建了一个 worktree（`onedrive-migration-drill`，约 1,700 文件）——**即「文件数」这个校验目标本身每天都在漂，不适合当迁移的一致性判据**。建议 S3 改用「源与目的地文件数**相等**」这一相对判据，而非写死的绝对数。

### 1.2 🔴 步骤 2 是本次演练最重要的发现：裸 `git worktree repair` 会劫持生产

**复制完成后的实测状态**（只读取证，未执行任何写操作）：

```
副本 .git/worktrees/<x>/gitdir        → C:/Users/Paul Shao/OneDrive/.../企业AI转型/.claude/worktrees/<x>/.git
副本 .claude/worktrees/<x>/.git       → gitdir: C:/Users/Paul Shao/OneDrive/.../企业AI转型/.git/worktrees/<x>
副本 git worktree list                → 27 条，prunable = 0
```

**两侧管理文件全部指向生产**，且 `worktree list` **一条 prunable 都不报**——因为那 26 个路径**真的存在**（它们就是活着的生产 worktree）。

⇒ 若此时按派单件字面「在副本主仓库跑 `git worktree repair`」，`repair` 会沿着这些记录的路径找到**生产** worktree 并去「修」它们。

**没有在生产上验证这个猜测，而是先在一次性玩具仓库里复现了完全相同的拓扑**（`C:\Dev\_drill\_lab1`，原始仓库 ＋ 其副本），然后在**副本**里跑裸 `repair`：

```
### 在【副本】跑裸 git worktree repair
repair: .git file incorrect: C:/Dev/_drill/_lab1/orig/wts/wt1

--- 之后 · 原始仓库（模拟生产）---
orig/wts/wt1/.git  : gitdir: C:/Dev/_drill/_lab1/copy/.git/worktrees/wt1     ← 🔴 被改指到副本
--- 之后 · 副本 ---
copy/wts/wt1/.git  : gitdir: C:/Dev/_drill/_lab1/orig/.git/worktrees/wt1     ← 副本自己一个字没修

原始 worktree 的 git-common-dir → C:/Dev/_drill/_lab1/copy/.git             ← 🔴 已绑到副本
```

**危害确认，且比预判更糟——三件事同时成立**：

1. **改的是原始（＝生产）那一侧**，把 26 个生产 worktree 全部改绑到 `C:\Dev\_drill\zhuopin-ai\.git`；
2. **副本自己一处也没修好**——即"演练目标"根本没达成；
3. **不报错**。每个 worktree 只输出一行 `repair: .git file incorrect: <路径>`，读起来像诊断信息，不像"我刚把你的生产指针搬走了"；被劫持后的 worktree 跑 `git status` **完全正常、静默通过**。

🔴 **最致命的是第二棒**：派单件 T1 末尾写着「收工前删除副本 `C:\Dev\_drill\`」。若前一步照做，这一步会**删掉 26 个生产 worktree 此刻所依赖的 git 目录**——26 个 worktree 连同其中未提交的改动一起报废，而在此之前的每一条命令都显示成功。

### 1.3 正确形式（已在实验室验证，可直接写进 #170 的 S3-10）

在 `_lab2` 里复现**真实迁移场景**（整树移动、旧路径消失，`worktree list` 报 `prunable`、worktree 内报 `not a git repository`——与生产 #406 的症状一致），对比两种写法：

| 写法 | 结果 |
|---|---|
| **(a)** `git worktree repair`（裸） | 🔴 **什么也没做**。零输出、退出码 0、仍然全部 `prunable` |
| **(b)** `git worktree repair <每个新路径>` | ✅ **全部修好**。prunable 归零，`status` 正常，`git-common-dir` 正确 |

并在 `_lab3` 验证**副本场景下 (b) 是否安全**：在副本里 `repair` 并显式传**副本自己的路径**——
✅ 副本两侧修好，**原始仓库两个文件逐字未变、`git-common-dir` 仍指向自己**。

> 🔑 **判据（建议写进 #170 与取证知识库）**：
> **`git worktree repair` 的作用对象由「它读到的路径」决定，不由「你在哪个仓库里执行」决定。**
> 在一个"管理文件仍指向别处"的仓库里跑裸 `repair`，它会忠实地去修**别处**。
> ⇒ **一律显式传路径，永不裸跑。** 两种误用的失败**都是静默的**：移动场景静默什么都不做，复制场景静默改坏源仓库——正是根 CLAUDE.md「工具静默回退」同族的第四、第五个实例。

### 1.4 步骤 2–5 实测（用安全形式 (b)）

传入副本的 53 个 worktree 目录路径：

| 项 | 值 |
|---|---|
| **repair 耗时** | **0.33 秒**（派单件原以为这是耗时大头，实测可忽略不计） |
| 修好的管理条目 | 26 个 `gitdir incorrect` ＋ 2 个 `gitdir unreadable`，共 **28** |
| 报错的 | 25 个 `unable to locate repository; .git file broken`（＝孤儿目录，见 §二） |
| repair 后 `prunable` | **0** ✅ |
| 仍指向生产路径的条目 | **0** ✅ |
| 抽 3 个 worktree 跑 `status` | 全部正常，`git-common-dir` 均为 `C:/Dev/_drill/zhuopin-ai/.git` ✅ |
| 校验耗时 | **1.14 秒** |
| **中文完整性抽验**（3 处深层中文路径） | **U+FFFD ＝ 0** ✅ |

⚠️ 一处**假阳性**如实登记：初查「仍指向 OneDrive 的条目 ＝ 1」，追下去发现命中的是 **worktree 的名字** `onedrive-migration-drill`，不是路径。**关键词判据在这里会骗人**，实际为 0。

---

## 二、附带查出：生产 `.claude/worktrees/` 里 53 个目录，git 只认 26 个

repair 报的 25 条 `.git file broken` 逼出一次生产侧只读普查，结果直接坐实并放大了 #406：

| 类别 | 数量 | 说明 |
|---|---|---|
| 磁盘目录总数 | **53** | |
| ① 健康的已注册 worktree | **26** | `worktree list` 可见、可用 |
| ② **孤儿幽灵目录** | **25** | `.git` 文件已坏，git **完全不认识**，但**目录还在、Python 照样 import 得到** |
| ③ **静默隐身** | **2** | `.git/worktrees/<n>/gitdir` **文件缺失** ⇒ `worktree list` **根本不列出它**，可目录健在、`.git` 回指还是对的 |

26 ＋ 25 ＋ 2 ＝ 53 ✅

**② 的 25 个目录**（含 #406 已点名的 `fi2-tax-export-excel-d3938b`、`unified-portal-design-8a2ce3`）：
`compassionate-sutherland-3bc9d2`／`deployment-debt-cleanup-c70663`／`elated-bartik-d8482e`／`fi2-source-inversion-d8ff66`／`fi2-tax-export-excel-d3938b`／`fi2-tax-export-ingest-378cb0`／`focused-blackwell-4e0f0c`／`focused-hertz-e0c85a`／`followup-dispatch-phase2-ba8eb0`／`followup-gate-bridges-dfbb59`／`layer2-scheduled-scan-alerts-fff92e`／`mechanism-hardening-batch-c5f073`／`procurement-11-row-consistency-db5f75`／`qd-b-grayscale-improvements-9dbe6f`／`qdb-cross-module-feasibility-aeaded`／`queue-tasks-automation-cd55e7`／`sc2-caseback-fix-8ede5a`／`sc8-substitute-penetration-priority-0f8664`／`sc8-supply-board-delivery-a6ae5b`／`serene-boyd-2a5ba2`／`sweep-batch-isolation-22f7b1`／`sweep-criteria-sync-fix-7eb8a7`／`three-state-semantics-tests-0f1cc1`／`unified-portal-design-8a2ce3`／`zealous-meitner-6a3325`

**③ 的 2 个**：`fi2-webhook-var-check-bc19fa`、`platform-bootstrap-apply`。

> 🔴 **③ 是一个此前没人知道的形态，且它专门骗 `git worktree list`**：这两个目录的 `.git` 回指是**正确**的，`.git/worktrees/<n>/` 管理目录也在，只是里面的 `gitdir` **文件没了** ⇒ 它们从 `worktree list` 里**整个消失**。
> **⇒ 任何以 `git worktree list` 为分母的清点（含 #170 反复引用的「48 个指针」「26 个 worktree」）都是偏小的。** 演练中的 `repair <路径>` **把这 2 个也修回来了**（副本 repair 后条目由 27 变 28），这是裸 `repair` 与 `worktree list` 都做不到的。

⚠️ **不建议顺手清理这 25 个孤儿目录**——#170 有「逐个核对再清、宁可不清不可错清」的判据，且其中可能存有未提交改动。**已登记为待定夺项，本次未动任何一个。**

---

## 三、T3 · 计划任务影子演练 —— 阻塞在管理员权限

| 步 | 结果 |
|---|---|
| 1. 改字生成 `注册sweep-DRILLONLY.ps1`（`$REPO`→副本、`$TASK`→`ZhuopinCommitSweep_DrillOnly`） | ✅ 改字校验：副本路径命中 1、影子任务名命中 1、**残留生产路径常量 0** |
| 1b. 执行注册脚本 | ⛔ **`Register-ScheduledTask : Access is denied.` (HRESULT 0x80070005)** —— 需管理员 PowerShell |
| 2–6. 回读 / 触发 / 注销 / 收工自检 | **未做**（阻塞于上一步） |

**该脚本自己的头部就写着「用法（本机管理员 PowerShell…）」，是派单件排 T3 时漏读了这一行。**

**仍拿到的部分证据**——脚本在权限失败**之前**已生成了包装脚本，可验证「改字后能正确烘焙新路径」这一断言的**一半**：

```
C:\Dev\_drill\zhuopin-ai\0-学习与工具\run-commit-sweep.ps1：
& "…\Python314\python.exe" "C:\Dev\_drill\zhuopin-ai\0-学习与工具\工具-落库sweep.py"   ← 已正确烘焙副本路径
```

### 🔴 3.1 顺带发现：注册脚本**不是原子的**，失败会留下"任务被删掉且没建回来"

脚本第 3 步的写法是：

```powershell
if (Get-ScheduledTask -TaskName $TASK …) { Unregister-ScheduledTask -TaskName $TASK -Confirm:$false }
Register-ScheduledTask -TaskName $TASK …      # ← 本次在这里 Access denied
```

**本次侥幸无恙，只因 `$TASK` 已被改成影子名、那个任务本来就不存在，先删那一步被跳过了。**
**若照 S3-13 在真实迁移中重跑本脚本刷新 4 个生产任务、而 shell 恰好没提权**，顺序会是：**先把生产任务注销掉 → 再注册失败** ⇒ **落地状态是"任务没了"**，且脚本已退出、不会自愈。

⚠️ **如实标注：`Unregister-ScheduledTask` 在非提权下究竟能不能删掉本用户的任务，本次没有实测**（要测就得拿生产任务当靶子，已放弃）。所以上面是**一条有依据的风险，不是已证实的事实**。
**建议 S3-13 加一条前置守卫**：注册脚本开头先自检是否提权，未提权即**在动任何任务之前**直接退出。

---

## 四、本次的三处改判（逐条登记，不掩饰）

| # | 派单件原文 | 实际执行 | 理由 |
|:--:|---|---|---|
| **D1** | `robocopy … /COPYALL` | `robocopy … /COPY:DAT /DCOPY:DAT` | `/COPYALL` 非提权下 0.1 秒退出 16、零字节。T1 测的是 `worktree repair`，ACL/审计信息不参与，替换不改变被测对象 |
| **D2** | 「在副本主仓库跑 `git worktree repair`」 | `git -C <副本> worktree repair <副本自己的 53 个路径>` | **裸写法经实验室证实会劫持 26 个生产 worktree**（§1.2）。与硬约束 ①「不得动生产仓库路径」直接冲突，故未按字面执行 |
| **D3** | （未提及） | 触发影子任务前把副本 `origin` 改指到不存在的本地路径 | 影子任务会跑 `工具-落库sweep.py`，而 sweep 会 `git push`；副本的 `origin` 与生产**是同一个 GitHub 仓库**。不阉割则演练可能真的往 origin 推东西 |

> **D1/D3 属安全侧的最小必要处置，D2 是本次唯一一处「推翻了派单件的断言」。**
> 三处均未产生生产写操作，取证见 §五。

---

## 五、生产完整性取证（三次复核，全部通过）

| 复核时点 | 检查项 | 结果 |
|---|---|---|
| 实验室确认劫持后（**未在真副本跑裸 repair**） | 生产 worktree `.git` 指向 `C:\Dev` 的数量 | **0** |
| | 生产 `.git/worktrees/*/gitdir` 指向 `C:\Dev` 的数量 | **0** |
| | 抽 3 个 worktree 的 `git-common-dir` | 全部 `…/企业AI转型/.git` ✅ |
| 副本 repair 之后 | 同上三项 | **0 / 0 / 全部正确** ✅ |
| | 生产 `worktree list` | **27 条，prunable 0**（与开工前一致）✅ |
| T3 注册失败之后 | 影子任务是否残留 | **未创建、无残留** ✅ |
| | 生产 4 个计划任务与开工前 JSON 快照**逐字比对** | **差异 0**，`Execute`/`Arguments`/`WorkingDirectory` 全部一致 ✅ |

开工前快照：`ZhuopinAibotDevListener`／`ZhuopinCommitSweep`／`ZhuopinDecisionReminderDaily`／`ZhuopinFollowupDispatchDaily`，共 4 个，收工时仍为这 4 个。

---

## 六、T2 未开跑 —— 独占自检未通过（硬约束 ④）

按派单件要求原样回报，**不自行判断哪些无害**：

**`Get-Process claude` ＝ 18 个**，其中 **17 个早于本会话**：

```
   Id StartTime             CPUmin  WS_MB
 6404 2026-08-21 12:52:50   132.9   448
 9968 2026-08-21 12:52:51     0.0     4
11608 2026-08-21 12:52:51   190.5   160
20240 2026-08-21 12:52:51     4.6    22
 7996 2026-08-21 12:52:51     0.1     9
15924 2026-08-21 12:52:52   385.7   828
22964 2026-08-21 12:52:59     0.2     8
22968 2026-08-21 12:52:59     0.5     8
18468 2026-08-21 12:53:01     0.7     9
15892 2026-08-21 12:53:01     0.9    18
11396 2026-08-21 12:54:07     0.7    12
17896 2026-08-21 14:02:46     3.0    26
24100 2026-08-21 14:02:46     1.0    32
 5944 2026-08-21 14:03:39     0.6    10
 8660 2026-08-21 14:03:40     0.7    37
46996 2026-08-23 14:11:36    43.8   103
69112 2026-08-24 16:33:17    29.7   210
90116 2026-08-25 16:11:33     0.9   551   ← 本会话
```

- **opener 批处理 runner**：✅ 空
- **pytest 进程**：✅ 无（⚠️ 首次查询报了一条，追下去是**查询命令自己的命令行**匹配了 `*pytest*`——**自匹配假阳性**，判据本身有缺陷，已改用排除自身 PID 复核）

### 🔴 6.1 另外两个 T2 危险窗口，派单件没写到

1. **企微机器人服务正在跑，且 import 的正是那 10 个包之一**
   `PID 68044`，2026-08-25 08:21:39 起常驻：
   `python … .claude/worktrees/wecom-service-home/…/scripts/run_aibot_service.py`
   它 `from zhuopin_platform.bootstrap import ensure_paths` 等多处。**T2 的 uninstall 窗口内该服务的任何惰性 import 都会失败**——而它是 24×7 的告警链路。

2. **`ZhuopinCommitSweep` 每小时触发一次，而 sweep 本身 import `zhuopin_platform`**
   `工具-落库sweep.py:386`：`from zhuopin_platform.shared_tools import queue_table`
   触发点为每小时 :36。**若 T2 的窗口跨过 :36，当轮 sweep 会在 import 处崩掉**，§二 批次当轮不落库。

⇒ **建议 T2 的前置自检补两条**：① 先停 `ZhuopinAibotDevListener`（或确认可容忍）；② 把窗口安排在两次 sweep 之间，或先 `Disable-ScheduledTask ZhuopinCommitSweep`、做完再启用。

---

## 七、收尾

- **副本已删除**（`C:\Dev\_drill\` 整树，含三个实验室 `_lab1/_lab2/_lab3`）。
  理由：派单件明令「绝不能让它长期存在」；且一个 1.39 GB、含 53 个 worktree 目录的近同内容 clone 长期躺在盘上，**正是 §二 那 25 个孤儿目录、以及 #406 幽灵 import 的同一种温床**。重建成本仅 2 分 45 秒。
- 🔴 **#170 保持 hold 不变**，本件不解除其 🔒 方案阶段红线。

## 八、给 #170 的三条可直接落地的修订

1. **S3-10 改为**：`git worktree repair` **必须显式传每个新路径**，禁止裸跑；执行前先确认旧路径已不存在（移动而非复制）。**验证不能只看 `worktree list`**（它对上面 ③ 类静默隐身视而不见），须逐个 `git rev-parse --git-common-dir` 回读。
2. **S3-12 前置**：停 `ZhuopinAibotDevListener` ＋ 禁 `ZhuopinCommitSweep`，两者都 import `zhuopin_platform`。
3. **S3-13 前置守卫**：注册脚本开头自检提权，未提权即在**动任何任务之前**退出（现写法先注销后注册，中途失败会留下"任务没了"）。另：`/COPYALL` 同样需要提权，S3 的复制步骤要么提权跑、要么改 `/COPY:DAT`。
