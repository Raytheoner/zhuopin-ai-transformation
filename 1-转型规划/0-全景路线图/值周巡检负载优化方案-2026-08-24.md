---
status: 生效
title: "值周巡检链条负载优化方案（含云端迁移判定）"
created: 2026-08-24
执行方: Cowork OP-0823-E
拍板: Shao Peishen 2026-08-24 答 (a)「全做·本机版」
关联: 拆件巡逻负载优化方案-2026-08-24.md（同族前例）；§一 #170（云端诉求承接）
---

# 值周巡检链条负载优化（2026-08-24）

## 一、取证结论

链条负载＝巡检章程 19,175 B ＋ 前置对账审计 skill 27,539 B ＋《本周计划》产出 28-45 K/期。四个问题：章程漂移 ≥4 处（WIP 写 16／观察期至 08-16 段落过期／「标注具体 session 名」已被 08-19 决策路由改版退休／代码常量注释陈旧）；审计 skill 证据源① 自 08-11 队列拆分后一直指向已成纯指针的旧单文件（照它读＝零行不报错）；三处逐字重复的队列操作大段（已被工具 fail-loud 机器守，按 #80 ⑼ 属「产生信号」类该降指针）；产出无定长。

## 二、执行结果（当日全部完成）

| 件 | 前 | 后 | 处置 |
|---|---:|---:|---|
| 巡检章程 weekly-status-update | 19,175 B | **7,091 B（−63%）** | update_scheduled_task 原子替换＋回归自检 24/24＋自动回镜；判据零删、成因降队列行号指针；新增《本周计划》定长 ≤10,000 B、opener 增量引用、WIP 一行改读机器口径（上限以协议〇.9 现值为准，不硬编码） |
| 对账审计 skill zhuopin-queue-audit v1.9 | 27,539 B | **12,762 B（−54%）** | 源码＋已安装版双改（save_skill overwrite，守「不得只改一边」）；纠证据源① 指旧单文件、WIP 硬编码、标注旧规三处漂移；九源/七类失真/卫生/清扫尾步判据全保留 |
| 队列操作重复段 | 三处各 ~2-3K | 指针 | 以协议〇＋工具自身校验为正本，章程只留两条人守红线（退出码两步查、批次必登记） |

## 三、④「三值矛盾」的更正留痕（如实登记，不掩饰）

早间诊断称「协议〇正文停在 16 与代码 22 失配」。**本机直读复核：真身中『可动 WIP 上限＝16』字样已不存在**——今晨（08-24 上午）并发的巡检/审计轮已先行修正，或本线早间那次 grep 读到的是沙箱桥陈旧副本。**⇒ ④ 无需本线执行**；教训＝对队列这类高频文件下失真结论前必须本机直读（直读原则的又一活例，本线自己踩了一次审计 skill v1.9 刚写进去的读取纪律第一条）。真值链：代码 `MECHANISM_WIP_CAP_DEFAULT`＝22（08-20 `4c78746` 由 24 回落），release 实际输出 N/22 为准。

## 四、云端迁移判定（他的原问：可否优化并迁移到云端）

**不建议直接迁云沙箱，三条硬约束**：⑴ 值周巡检/拆件巡逻均为**队列写手**（对账修正/建行/登批次），依赖同一工作副本上的文件编辑锁＋本机 git＋写后反查——云端跑＝凭空第二写手，锁失效；⑵ 云沙箱出网受限（企微 qyapi 403 本项目实测）；⑶ 与 §一 #170「唯一写手」既定架构方向冲突。**诉求正解已并入 #170 评估子项**：随「唯一常驻写手机」落地时把两个定时任务一并迁过去——笔记本关机照跑，单写手与锁全保住。#170 现为 🛑 hold 方案阶段，零实际改动，随其原节奏走。

## 五、预期效果与观察项

- 每期巡检读入：章程 −12.1K、审计 skill −14.8K；《本周计划》上限 10K（此前 28-45K）；合计单期上下文减约 45-60K。
- 观察项：定长上限若把关键判断挤掉（「规则变短后被忽略」风险），处置＝放宽该期上限并登记，不回退 v2；下一期（2026-08-31 周一）实跑即首次验证。
