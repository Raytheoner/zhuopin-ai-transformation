---
title: "看护件 · LAN 收口批 B-0908_AL4（一项：SC2 `.51` 冒烟七项＋周序／订单数量对数 —— `采购部#22` 发信硬前置②）"
created: 2026-09-08
批次: B-0908_AL4
派出线: Cowork 环境总线 OP-0907-AL（skill `zhuopin-lan-closeout` 第五批；Shao Peishen 2026-09-08 12:2x 触发「我已回lan」，参数 `上限2`，答 1a）
LAN: ✅ 在网（12:34 本机探针：`192.168.100.51` TCP 22／8091／8093／8094／8096 全通；8091／8093／8094／8096 `/api/ping` 全 200；`工具-泳道看护状态机.py lan-status` ＝ on）
status: 待你在 CC 里起
---

# 看护件 · LAN 收口批 `B-0908_AL4`

## 〇、本批口径

**触发**：Shao Peishen 2026-09-08 答 1a（现在收、`上限2`）并说「我已回lan」。**三查**：`origin/master..master`＝0；队列锁空闲（12:3x `status`）；上一收口批 `B-0907_Q` 收工汇总已出（其 §六）。**上限**：首跑三判据 09-07 已全过，默认 6；本批按他给的参数 2，实际入批 **1 项**（成因见下表）。

**三源留步清单汇集结果**（D1 白名单：只收有可执行判据的留步登记）：

| 来源 | 命中 | 判定 |
|---|---|---|
| 状态机转出记录（`summary --batch B-0908_B`／`B-0907_R`） | `502-sc2-smoke`／`358-audit-retention-p1p2`＋`358-p1p2-cc`／`436-worktree-drift-alert-clear` | `#502` ✅ **入批（项 1）**；`#358` P1/P2 **落选＝已完成**：`origin/claude/op0908i-358-audit-retention-b52b16`（`56f6aad`）的 `openspec/changes/audit-retention-archive/design.md` 已含「✅ P1／P2 已取回（`OP-0908-I`，CC 侧只读实测，2026-09-08）」全节，授权记录 #0；状态机两条泳道仍 `running` 属**记账滞后**，本批不重做、登记待补（见 §五）；`#436`⑵ **落选＝软序未到**：其转出备注明写「不改脏检查判据必复发」，而该判据正是 `B-0908_B` 在跑泳道 `#500`，等 `#500` ff 入 master 后下一批 |
| 两份队列 `--digest --grep`「LAN 留步」「待回内网」 | `#502`／`#312`／`#398`／`#401`／`#419`／`#452` | `#502` 同上；其余五行为概念提及或需先修代码（`#419`），无三件，落选 |
| 近期看护件 LAN 留步节（`B-0908_B` §五、`B-0907_R` 转出） | 同状态机 | 同上 |
| 场景 `CLAUDE.md` 部署状态 × master 触碰（「发布积攒」核实） | SC8 `#445`（`4477a28`，09-01 已部署并认证态复验 ✅）；FI2 `#423/#424`（`fafaa60` 09-07 已部署冒烟）；`#354` 四服务 env 收拢（`46490d9` 08-27，早于 09-07 SC2 对齐核验＝已在生产）；QD-B 无新触碰 | **功能型积攒＝0 项**——「攒多点再上」目前没有可攒的对象，Shao Peishen 的直觉（先聚焦建设）与实测一致 |
| 未闭合产出扫描器 形态 1 | 🔴 **未采信**：`--json --no-write-state [--skip-lan-probe]` 两次均 >170 s 超时未返回（12:1x），本次未拿到其输出 | 以上三源已覆盖其形态 1 的来源面；扫描器超时本身登记 §五 交环境线核 |

## 一、🔴 硬边界

1. `.51` **一次只动一项**；本批只 1 项，不与在跑看护批 `B-0908_B`（纯库内 worktree）撞 `.51`。
2. **只跑冒烟与只读对数，不部署新码、不改配置、不重启服务**（`#502` 触碰区原文）。`POST /api/refresh` 是脚本第 5 项「一次全量重算」，属发送硬前置②要求的动作，允许；重算前先做 §二 快照点。
3. 冒烟任一项不过 ⇒ 停项、降回留步态并注明原因；不带伤推进；**不部署**（部署项另立、附三件、交下一批）。
4. 对外发送永不代办：本批产出只是 `采购部#22` 的前置②证据，信仍走「⏳ 待你审 → 你回『发』」。
5. `pause`／`check-heartbeat` 一律 `--no-notify`；看护件落档失败即全停。

## 二、波次（单项）

| 序 | 项 | 承接 | `.51` 触碰 | 快照点 | 冒烟判据 | 回滚 SOP |
|---|---|---|---|---|---|---|
| 1 | SC2 `.51` 冒烟七项＋周序／订单数量功能正确性对数 | `#502`（`#361` 边界句同批更新） | `C:\sc2\app` 只读；`POST /api/refresh` 触发一次全量重算（写 SC2 自己的快照／报告输出） | ⑴ `C:\sc2\app` 顶层文件 SHA256（与 `队列行日志/#361.md` 22 行比对，确认仍是 09-07 那份码）；⑵ 重算前把 SC2 输出目录（`smoke-server.ps1` 第 6 项「snapshot path」所指目录，实际路径以脚本为准）`Copy-Item` 到 `C:\sc2\snap.bak-<yyyyMMddHHmm>` | `smoke-server.ps1` 0–7 共八段**逐段原始输出留痕**（0 进程身份／1 ping／2 匿名 302／3 登录 302／4 关键页 200 且含确认表单与 ISO 周标／5 refresh 200／6 快照路径快速返回／7 进程存活）＋ 周序与订单数量对数：取最近一个完整自然周，周报里的「本周／上周／上月同期」订单数量与 SRM／ERP 侧同口径各查一次，**逐数记录，不只写「一致」** | 冒烟不过或对数不一致：不改服务、不部署；若重算把输出写坏 ⇒ 用 `C:\sc2\snap.bak-<ts>` 覆盖回去；结论如实写「前置②未过＋原因」，`#502` 降回留步态；服务级回滚（仅在进程崩溃时）＝ SC2 `CLAUDE.md` 部署状态表「回滚」行 `schtasks /End /TN Sc2WebServer` 再按同表启动行拉起 |

## 三、逐项步骤（看护者本人按序执行，不扇出——#138：写活不进子代理）

### 项 1 · SC2 冒烟与对数

1. 探针复核（不信 frontmatter）：`Test-NetConnection 192.168.100.51 -Port 8096`＋`Invoke-WebRequest http://192.168.100.51:8096/api/ping`；再 `mcp__ccd_session_mgmt__list_sessions` 核无其它会话 `isRunning:true` 且标题含 `.51`／`sc2`（机器自查优先，§四 `#166`）。
2. 快照点：`ssh supplychain-server "powershell -c Get-ChildItem C:\sc2\app -File | Get-FileHash -Algorithm SHA256 | Select Hash,Path"` 与 `1-转型规划/0-全景路线图/队列行日志/#361.md` 22 行比对（差异须为 0，否则停项报「生产码已变」）；读 `smoke-server.ps1` 定位输出目录后 `Copy-Item` 到 `C:\sc2\snap.bak-<ts>`，记下目录名与文件数。
3. 执行：`ssh supplychain-server "powershell -ExecutionPolicy Bypass -File C:\sc2\app\smoke-server.ps1"`（脚本自读 `C:\sc2\.env` 取闸密码、不打印）；**全部原始输出**存 `reports/sc2-smoke-2026-09-08.txt`，并把八段各自的判定行**原文抄进** `队列行日志/#502.md`（`reports/` 被 gitignore）。
4. 对数：登录态下取关键页最近完整周的订单数量三格；本机用 SC2 取数层（`4-数字员工/采购部/SC2-采购周报自动生成/` 的 SRM／ERP feed，`--help` 看入口）对同一周同口径各查一次；三格逐一并列记录。
5. 冒烟判据：八段全过 **且** 三格对数一致 ⇒ 前置② **取到证据**；任一不过 ⇒ 停项、`#502` 降回留步态＋原因，不部署、不修。
6. 回写证据：走锁 `edit-row` —— `#502` 状态列一段（八段结果、对数三格、快照目录、留痕路径）；`#361` 状态列**追加**一句「09-08 `B-0908_AL4` 已跑冒烟七项＋对数，边界句自本段起作废／仍成立（二选一）」，不改其 `[S:done]`；登记 §二 批次（`队列行日志/#502.md`＋本看护件 §六 收工段）。收口≠销号：`#502` 是否 ✅ 由发送硬前置②的证据决定，本批**可以**销它（它就是为取这份证据立的），`#361` 不动。
7. 清场：`snap.bak-<ts>` **保留到 `采购部#22` 发出后再删**（回滚 SOP 依赖它），删除动作交下一收口批；本批不删。

## 三bis、看护opener（单次粘贴；本批不起子任务，看护者按 §三 自己做）

粘贴端：CC ｜ 泳道：（看护者本身）

▶ 首次派出：[OP-0908-J]

```
[OP-0908-J]【CC】看护LAN收口AL4
【设置】执行环境：CC ｜ 分支：master（不建分支，不改代码；产出只有留痕件与队列回写）｜ worktree：☐（不建，本批不写仓库代码）｜ 工作区：`.51`（192.168.100.51，ssh 别名 `supplychain-server`；本批只跑 SC2 冒烟与只读对数，不部署、不改配置、不重启）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0907-AL（批 B-0908_AL4）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0908J-看护LAN收口AL4。🔴 你是本批唯一会话，本条对你适用；本批不起任何 Task/Agent 子任务（#138：写活不进子代理），不存在「跳过本行」的例外情形。
读 `1-转型规划/0-全景路线图/看护件-LAN收口批B-0908_AL4-SC2冒烟一项-2026-09-08.md` 全文＋ CLAUDE.md＋`.claude/rules/两桌同步与取证.md`＋`.claude/rules/场景建造与合规.md` 恢复上下文。授权链：Shao Peishen 2026-09-08 触发 skill `zhuopin-lan-closeout`（答 1a、「我已回lan」），本件即授权；他在场。本件为 A 类，直接开工。

做什么：
1. 机器自查 `.51` 是否被占：`mcp__ccd_session_mgmt__list_sessions` 无其它 `isRunning:true` 且标题含 `.51`／`sc2`／`fi2` 的会话 ⇒ 自决开跑并在收工汇总留一行机器证据；有 ⇒ 才问他一句。
2. 探针复核（不信 frontmatter）：`Test-NetConnection 192.168.100.51 -Port 8096`＋`Invoke-WebRequest http://192.168.100.51:8096/api/ping`，不通即停并报。
3. 按 §三 项 1 步骤 2→7 执行，固定四步＝快照→执行→冒烟→回写证据；八段原始输出逐段留痕、对数三格逐数记录；任一不过即停项、降回留步态并注明原因，不部署、不修。
4. 收工汇总一次写全，追加进本看护件 §六（上文一字不改）：判据→证据→行内状态处置一行；「待你一字母」清单标〔荐〕与默认项；最后一行给答复模板。
5. 心跳与暂停一律 `--no-notify`；等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0908_AL4` 现取。

不做什么：
- 🔴 不部署、不重启、不改 `C:\sc2\` 任何服务文件与配置；不碰 `C:\fi2\`／`C:\baoguan\`；`snap.bak-<ts>` 保留不删。
- 🔴 不发企微、不发任何对外消息、不起草 `采购部#22`（起草归业务总线 `OP-0908-A`，本批只供前置②证据）。
- 🔴 不 commit：产出登记 §二 批次由 sweep 取活；撞锁不重试不豁免，写「登记待补」。
- 不修工具：`smoke-server.ps1`／状态机／扫描器有缺陷只登记＋报，不在本会话改。
```

## 四、收工

看护者收工汇总落本件 §六（追加，不改上文）；`#502` 处置结论作为 `采购部#22` 发送硬前置②的证据源，业务总线 `OP-0908-A` 起草时引用本件 §六。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#358` P1/P2 | **已由 `OP-0908-I` 取回**（分支 `56f6aad` design.md 全节）；状态机 `358-audit-retention-p1p2`／`358-p1p2-cc` 两条 `running` 为记账滞后 ⇒ 登记待补：由 `B-0908_B` 看护者收工时 `transfer-out` 记录标闭合，或环境线下一棒用状态机现有子命令关闭 |
| `#436`⑵ 清 BOM＋ff 常驻执行体＋重启 listener | 等 `B-0908_B` 泳道 `#500` ff 入 master 后的下一收口批（否则「必复发」） |
| 功能型「发布积攒」 | 实测 0 项（§〇 表末行）；后续由定时任务「LAN 收口提醒」每工作日 09:00 只读核（Shao Peishen 2026-09-08 答 2a 建） |
| 未闭合产出扫描器 >170 s 超时 | 环境线核：是 LAN 探针等待还是扫描面膨胀；只登记不修 |
| `snap.bak-<ts>` 清场 | `采购部#22` 发出后的下一收口批 |
