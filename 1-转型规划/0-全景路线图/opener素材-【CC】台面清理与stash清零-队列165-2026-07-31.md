---
title: "opener 素材 · 【CC】台面清理与 stash 清零（队列 #165／#101②③／#166／#125）"
created: 2026-07-31
执行方: Cowork · 环境保障线（**起草素材**）→ 交业务总线定稿派发
执行环境: Cowork（本件）／CC（本件所描述的任务）
status: 待业务总线定稿
---

# opener 素材 · 【CC】台面清理与 stash 清零

> 🔴 **本件是「素材」，不是可直接派发的成品 opener。**
> 环境保障线铁律一：**只出分析与建议，不派活、不开 opener**（记忆 `feedback_decisions_go_to_business_bus`）。故本件把 opener 该有的东西都备齐了——【设置】四字段、开场词、任务分段与验收标准、前置步、纪律指针、收工段——**但由业务总线过一遍并决定派发时机后，才成为正式 opener**。
> **业务总线定稿前需拍的三点见 §五**，其中「派发时机」一项直接关系任务能否顺利执行，请勿跳过。

---

## 一、拟稿：【设置】行 + 开场词（业务总线确认后可整块复制）

> **格式硬规则（Shao Peishen 2026-07-31 定）**：【设置】行**必须在代码块内、作为第一行**——放在块外时渲染出的复制按钮**只会复制开场词、必然漏掉设置行**，这不是"会不会忘"的问题，是渲染机制保证它被漏掉。

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐ ｜ 工作区：无（本任务只删除其他 worktree，不改任何运行中副本；🔴 但 `wecom-service-home` 是企微机器人常驻运行目录，本任务对它只读不动）

读 1-转型规划/0-全景路线图/台面清理执行清单-worktree与stash-2026-07-31.md ＋ CLAUDE.md 当前进度恢复上下文，按清单执行队列 #165／#101②③／#166／#125／#207 剩余。
🔴 清单是 07-31 快照，务必先跑「执行前重跑核验三连」；B 类表已加 08-02 差异提示，B3 有必读补注（它是 #207 唯一未闭合的那个，清单原记"0 未提交改动"已过时）。
#166 修法默认按「只落 ③ 判定口径」执行（改动最小、不动协议〇.5）；若你认为该同时落 ②，开工时告诉我。
```

> **⚠️ 2026-08-02 修正**：原开场词末句为"开干前问我 2-3 个澄清"，**违反 v1.5 格式硬规则「禁开放式提问，一律给带推荐项的选择题」**；已改为直接给出 #166 的默认执行口径 + 一句可推翻的邀请。**"问我 2-3 个"这种写法会让 CC 自己去想问题，而它并不掌握判断依据。**

**为什么 `worktree：☐`（不勾）**：本任务的动作是**在主工作区删除其他 worktree 目录与 stash**。若为它新开 worktree，`git worktree remove`/`Remove-Item` 的目标路径与 stash 都在主工作区上下文里，新 checkout 反而看不到、也删不动——同 CLAUDE.md §5「落库/提交类口令切勿为落库单开新 worktree」的同一道理。

---

## 二、任务分段与验收标准（拟）

权威依据＝`1-转型规划/0-全景路线图/台面清理执行清单-worktree与stash-2026-07-31.md`（下称「清单」）。**opener 只给指针，不复制清单正文**（引用版铁律）。

### A. 执行前重跑核验三连（**不可跳过**，清单 §〇）

- **为什么**：2026-07-31 取证的十几分钟内实测到 master 前进 5 个提交、一个 worktree 被注销、一个 worktree 换了分支——**台面在动是常态，静态快照会过期**。
- **动作**：`git worktree list` ／ 物理目录列举 ／ `.git/worktrees` 列举 ／ `git stash list`，四项当刻真值与清单比对。
- **验收**：差异逐条列出；**清单里列为"可清"的项，若重跑时发现已被重新注册或已有内容 → 立即跳过并回写队列，不按清单硬删**。

### B. A 类空壳目录清理（清单 §二，共 **10 个** + 1 个准空壳）

- 🔴 **绝对不用 `git worktree remove`**——队列 #125 已实证该命令**非原子失败**：遇句柄锁会先把目标目录清空、最后一步才报 `Permission denied`，报错文案让人误以为"什么都没发生"。一律 PowerShell `Remove-Item -Recurse -Force`。
- **含 A10 `affectionate-herschel-c26958`**（Shao Peishen 2026-07-31 拍板选项 A 并入，前提已取证：`Get-Process -Id 49720` 该进程已不存在）——**须一并 `git branch -D claude/md2word-checkbox-control-089198`**，只删目录不删分支台面数字仍对不齐。
- **验收**：10 + 1 个目录消失；#125 销行。

### C. B 类在册 worktree 清理（清单 §三，6 个候选）

- **四步不可跳**：`status --porcelain` → 确认 ahead=0 → `Remove-Item` → `git worktree prune`。
- 🔴 **B1 `wecom-service-home` 绝不可动**（企微机器人常驻运行目录，服务中）。
- ⚠️ **B5 `loving-mestorf-98749e` 取证期间分支刚变过、疑为活动中**，执行前二次确认；有内容即跳过。
- **验收**：`status --porcelain` 有输出而跳过的，**逐个在 #165 行内写明目录名与跳过原因**——这条比"清干净"更重要，是下期体检不重复劳动的唯一依据。

> ### 🔴 C-补 · 本段新增一个明确目标：`fi2-validation-prep-66ed2c`（2026-08-02 加，**即 #207 唯一未处置的那 1/5**）
>
> **来源**：#207（五个长驻 worktree 纪律滞后）已由 CC 于 2026-08-02 处置 4/5，**唯独此目录刻意搁置**，理由是"取证发现 #165 已对这个具体 worktree 给出结论（可安全删除）并备好执行清单，还在待派发；本行若动手会和 #165 重复/冲突，处置权已移交"。**——即它现在就在本件的范围内，请勿再跳过。**
>
> **状态实测（2026-08-02 Cowork 只读取证）**：分支 `claude/fi2-validation-prep-66ed2c`，**落后 master 338 提交**（五个 worktree 里最深的一个），`CLAUDE.md` 仅 30761B（master 43632B，**缺约 13KB 纪律**）。
>
> **⚠️ 它有一份 07-23 的未提交队列文件改动，但已核实可安全丢弃**：内容是 #79「分支不一致已收口」，**早已通过另一路径进入 master**——现见 `跨桌任务队列.md` L37、`6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` L96、`1-转型规划/0-全景路线图/环境体检-2026-07-30-第0期基线.md` L62 三处；那封姚祖怡的信也确实已删且 README「已删除记录」有留痕。**故不是丢失内容，是过时残留。**
>
> **但仍请自行复核一次再丢**（守 #101① 的"脏文件命中 §二 即禁 `checkout --`"精神）——本条结论由 Cowork 只读取证得出，**你有条件做得更硬**。
>
> **验收**：处置结论（删除／保留）+ 那份脏文件的复核留痕；**处置完须回填 #207 行，把它从"1 个刻意搁置"改为已闭合**，否则 #207 会永远停在 4/5。

### D. C 类元数据 + D 类 stash（清单 §四／§六）

- `git worktree prune` 清 2 条陈旧元数据。
- 3 条 stash **先各导一份 patch 存仓库外**（成本≈0）**再倒序 drop**（索引会重排，务必 `{2}→{1}→{0}`）。三条的逐条比对结论已在清单 §六 给出，**CC 不需要重新判断该不该 drop，只需执行 + 核验清零**。
- **验收**：`git stash list` 无输出；#101②③ 销行。

### E. #166 修法落地（清单 §七）

- 07-31 取证把 #166 从"目录名错位"升级为"**同一物理目录被不同任务反复 checkout 到不同分支**"——目录名在时间维度上根本不承载稳定语义。
- **建议采纳 ②+③**：② `WHOAMI.md` 但须**每次 checkout 到新分支时刷新**（只写一次等于把陈旧从目录名搬进文件里）；③ 判定 worktree 身份**一律以 `git -C <path> branch --show-current` 当刻实测为准、不看目录名**，并把该口径写进协议〇.5。
- **验收**：修法落地 + 协议〇.5 补一句；存量 worktree 按选定修法补齐。

### F. 收尾核验（清单 §八）

- **三者数量一致**（物理目录 = `.git/worktrees` = `git worktree list`），stash 清零，本地分支少一条。
- **若因并发/未提交改动实际跳过了某些项**：不算失败，**但必须回写**（同 C 段验收）。

---

## 三、开工前置步（认领先于动手）

按本任务触碰区关键词（`.claude/worktrees`／`git stash`／`worktree`／`#165`／`#101`／`#166`／`#125`）**同时 grep**《跨桌任务队列.md》**与**《跨桌任务队列-归档-202607.md》，确认无他人在办行重叠；有重叠不得抢领，报 §四 裁决。
**归档件必须一起 grep**——每周清扫后历史行已不在队列正文，只搜正文会误判为"没人做过"。

---

## 四、纪律段（指针，不抄全文）

- **协议〇.5**：一任务一分支一 worktree／开工三查／完工即推送（本任务为主工作区清理类，见 §一 说明为何不勾 worktree）。
- **协议〇.7 编辑锁**：改队列前 `acquire`、改完立刻 `release`；**新行编号一律用 `--reserve N --section 一|四` 预留取号**（#163 落地，2026-07-31 起为正式口径；⚠️ 每次只能指定一个 `--section`，两套号时另一套按回显值 +1 并写明用的哪个值，见 #185）。
- **协议〇.8 批次即扫**：改动登 §二 批次（**状态列禁带 "✅"**），登记后立即触发一次 sweep。
- **CLAUDE.md §5**：直读原则（对新鲜度敏感的读取走本机 PowerShell 直读，不信沙箱挂载视图）／乱码文件夹哨兵（开工收工各查一次上级目录 `*AI转*` 应且仅应 1 个）。
- **本任务特有红线**：**只删、不改任何代码**；`wecom-service-home` 只读不动；**遇任何 `status --porcelain` 有输出的 worktree 一律停手不删**，改为回写队列交 Shao Peishen 判。

---

## 五、🔴 业务总线定稿前需拍的三点

1. **派发时机（最要紧）**——清单 §〇 明写"建议在无并发 CC session 的时段执行"。**取证期间实测台面正在被并发改动**（master 前进 5 提交、worktree 被注销/换分支）。若在有并发 CC session 时派发，B 段大概率大面积跳过，任务价值打折、还要再来一轮。**建议**：确认无其他 CC 会话在跑时再发，或在 opener 里显式要求 CC 开工时先自查并发情况、有并发即只做 A/D 段（stash 与元数据不受并发影响）、B/C 段留待下轮。
2. **是否与其他 CC 活同车**——本任务是纯卫生、零口径影响、无测试依赖，理论上可挂在任意 CC 批的收尾。但它**要求主工作区 clean 且无并发**，与"同车"天然冲突。**建议独立一次**，不塞进建造批。
3. **#166 修法是否就此定案**——§二 E 段给的是"建议采纳 ②+③"。若业务总线认为该修法要动协议〇.5（属治理文档），可能需要单独确认；也可让 CC 先只落 ③（判定口径，改动最小）、② 留待下次。

---

## 六、收工段（拟）

1. 回写队列 **#165**（逐类实际删了几个、跳过哪些及原因、收尾核验三数是否一致、stash drop 结果）、**#101②③ 销行**、**#125 销行**、**#166 回写**（采纳哪种修法 + 协议〇.5 是否已补）。
2. 列出本次全部改动文件清单 + commit message；**CC 类本含 commit + push**，另加收工重跑文档台账。
3. 改队列走协议〇.7 锁；改动登 §二 批次并立即触发 sweep。
4. 本任务若未开 worktree 则无自删动作；若开了，按协议〇.5 收工自删。

---

## 七、本件与既有文件的关系（避免重复劳动）

- **执行细节的唯一真身＝清单**（`台面清理执行清单-worktree与stash-2026-07-31.md`），含逐条命令、取证依据、判据。本件**只给指针不复制**，清单更新后本件无需同步。
- **队列 #165 行**已回写了相对 07-30 体检的四处更新与执行清单指针，是任务状态的唯一记录处。
- 本件产出后，环境保障线对 #165 的工作即告完成（取证 + 清单 + 素材），**后续状态流转归业务总线与 CC**。
