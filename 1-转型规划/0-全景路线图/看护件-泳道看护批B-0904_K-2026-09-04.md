---
title: "看护件 · 泳道看护批 B-0904_K（第十一批：扩容三条独立泳道，他本人在环、手动粘 CC 端 opener 跑，不走无头派发）"
created: 2026-09-04
批次: B-0904_K
派出线: Cowork 业务总线 OP-0904-J
前批: B-0904_J（存量批次排查，进行中，无头派发）
status: 待你在 CC 里逐条起（本批不走 `工具-opener批处理执行v2.ps1` 无头派发，三条 opener 直接由 Shao Peishen 手动粘贴进三个 CC 窗口）
---

# 看护件 · 泳道看护批 `B-0904_K`

## 〇、本批口径

**立批依据**：Shao Peishen 2026-09-04 问「是否能开尽开」——答：能，`B-0904_J` 只开 1 条是 Cowork 第一次亲眼验这条无头派发链、刻意压小，链已验通。本批按「🟢 全自动做完、能开尽开」的既有判据（`#467` 立行时 Shao Peishen 2026-09-02 原话）扩容，从机制队列里挑三条**触碰区互不重叠、均为 `[S:open]` 非 🛑 排队、非 oem-chroma 族**的独立候选。

🔴 **本批的派发方式与 `B-0904_J` 不同**：Shao Peishen 本次要求**自己开 CC 窗口手动粘贴、实时在环**，不走 `工具-opener批处理执行v2.ps1` 无头后台派发——三条 opener 逐条复制即用，各开一个新 CC 窗口。等他对这条链熟悉后再切回无头派发。

**分档判据**：现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

---

## 一、🔴 硬边界（三条泳道通用）

1. 🔴 **`merge_to_master`（🟡）一律不自行执行**——三条泳道各自 commit＋push 到自己分支即止，合入 master 登记等他一个字母。
2. 🔴 **`openspec_design_review`（🟡）一律不自行判过**——`A1`（`#459`）命中此档，起草到 design 草案即停，不视为已过审。
3. 🔴 **`A3`（`#460`）不改任何仓库外文件**——诊断与修法建议一律落在仓库内新建文档，不直接改「Migration observation daily check」计划任务的脚本/prompt，也不重启/重新注册该计划任务；他在环，是否现场手动应用由他自己决定。
4. 🔴 三条泳道触碰区互不重叠（见下矩阵），可安全同时开三个 CC 窗口并行跑，无需互相等待。
5. 🔴 判据未覆盖的动作按 🟡 停，在收尾原因里问「这个动作该进哪一档」，不自行判断。

---

## 二、波次与触碰区矩阵

🔴 三条泳道并行、各自单段，互不依赖。

| 泳道 | 段 | 承接 | 触碰区 |
|---|:--:|---|---|
| `pth-blindspot` | A1 | `#459` editable 巡检 `.pth` 直写目录盲区 | `openspec/changes/editable-pth-blindspot-guard/`（新建） |
| `opener-gen` | A2 | `#461` opener 生成器 | `0-学习与工具/工具-opener生成.py`（新建）、`1-转型规划/0-全景路线图/专线opener模板库.md` §〇.00 |
| `daycheck-fp` | A3 | `#460` 迁移观察期日核假阳性 | 仓库外只读定位（计划任务本体）＋ 仓库内新建修法建议文档，不改 `工具-落库sweep.py`／`工具-泳道看护状态机.py` |

---

## 三、泳道 opener（三条，逐条复制进各自新 CC 窗口）

### A1 · `#459` editable `.pth` 盲区——只起 propose+design 草案，不实现代码

粘贴端：CC ｜ 泳道：pth-blindspot

▶ 首次派出：[OP-0904-L]

```
[OP-0904-L]【CC】pth盲区design
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0904l-pth-blindspot`）｜ worktree：☑（pth-blindspot，新 worktree，收工自删）｜ 工作区：无（纯 openspec 草案，不改 `工具-落库sweep.py` 一行代码，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0904_K
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0904L-pth盲区design。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① 队列 §一 `#459` 全文（`python 0-学习与工具/工具-队列查询.py --row 459`）→ ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（范围已写死：只到 design 草案），无需再问澄清，直接开工。

做什么：
1. 起草 openspec 变更包 `openspec/changes/editable-pth-blindspot-guard/`：proposal.md（命题＝editable 巡检判据现只认 import-finder 形态、对 setuptools 的 `.pth` 直写目录路径形态静默零命中）＋ design.md 草案（判据覆盖方案、含计划中的单测覆盖点清单，不必先写测试代码）＋ tasks.md ＋ specs delta。
2. `openspec validate --strict` 须通过。
3. 🔴 **命中 `openspec_design_review`（🟡）—— 起草完即停，不实现任何代码**：跑 `python 0-学习与工具/工具-泳道看护状态机.py pause --batch B-0904_K --wave 1 --lane pth-blindspot --action-key openspec_design_review --waiting-for "design 草案已出，等你审" --no-notify`，以 `OPENER_PARTIAL: 停在🟡决策点——design 待你审` 收尾。
4. §二 批次登记本次产出（走编辑锁 `acquire`/`append-row`/`release`）。

不做什么：
- 🔴 不改 `0-学习与工具/工具-落库sweep.py` 任何一行代码，不写单测代码文件（测试覆盖点只写进 design.md 的计划）。
- 🔴 不合入 master。
- 🔴 不碰 `#454`（同触碰 sweep.py 的另一条，本泳道不代管）。
```

### A2 · `#461` opener 生成器——完整建造到可合入前

粘贴端：CC ｜ 泳道：opener-gen

▶ 首次派出：[OP-0904-M]

```
[OP-0904-M]【CC】opener生成器
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0904m-opener-gen`）｜ worktree：☑（opener-gen，新 worktree，收工自删；合入 master 那一步回主工作区做）｜ 工作区：无（纯库内新建工具＋单测，不部署、不触碰 `.51`）｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0904_K
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0904M-opener生成器。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① 队列 §一 `#461` 全文（`python 0-学习与工具/工具-队列查询.py --row 461`）→ ② `1-转型规划/0-全景路线图/专线opener模板库.md` §〇.00（格式单一可信源）＋ §〇.1 → ③ `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（范围已写死），无需再问澄清，直接开工。

做什么：
1. 新建 `0-学习与工具/工具-opener生成.py`：接受参数（编号／执行环境 CC或Cowork／短名／分支／worktree 情形／工作区情形／session／派出线／输入指针／A或B类），按 §〇.00 逐字骨架拼出成品 opener 文本；任一必填字段缺失即报错退出、不出件。
2. 反例单测三种，均须失败：缺任一字段；`worktree` 写成裸名字（非 ☑／☐ 勾选符号开头）；`set_session_title` 写成伪代码而非真实工具名+"self"字面量。
3. 正例单测：按一组完整参数生成 opener，逐字比对 §〇.00 骨架结构（六字段顺序、`session：新开` 字面出现、`set_session_title` 整行含子任务例外句）。
4. `专线opener模板库.md` §〇.00 增一行「本节已由 `工具-opener生成.py` 承接，手写 opener 仅在生成器不可用时兜底」。
5. 全量跑一次现有测试目录，确认零回归。
6. §二 批次登记（走编辑锁），commit＋push 到本分支，🔴 不合入 master（`merge_to_master` 属 🟡，登记等他一个字母）。

不做什么：
- 🔴 不合入 master、不删除或改写模板库 §〇.00 骨架本身的文字（只加一行指针）。
- 🔴 不碰 §四 `#151`（OP 重号根治，本行完成后由业务总线判断是否一并销号，不自行销）。
```

### A3 · `#460` 迁移观察期日核假阳性——只诊断+写修法建议，不改外部文件

粘贴端：CC ｜ 泳道：daycheck-fp

▶ 首次派出：[OP-0904-N]

```
[OP-0904-N]【CC】日核假阳性
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0904n-daycheck-fp`）｜ worktree：☑（daycheck-fp，新 worktree，收工自删）｜ 工作区：只读定位「Migration observation daily check」计划任务的实际脚本/prompt 路径（本机 `Get-ScheduledTask`/`Get-ScheduledTaskInfo`，仓库外），🔴 定位到之后只读、不改、不重启、不重新注册该计划任务 ｜ session：新开 ｜ 派出线：Cowork 业务总线 B-0904_K
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0904N-日核假阳性。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读队列 §一 `#460` 全文（`python 0-学习与工具/工具-队列查询.py --row 460`）→ `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（范围已写死：只到修法建议文档，不动外部文件），无需再问澄清，直接开工。

做什么：
1. 用 `Get-ScheduledTask -TaskName "*Migration*observation*"`（或队列行给的线索）定位该计划任务的实际脚本/prompt 文件真实路径，回填进产出文档。
2. 只读复核队列 `#460` 已给出的判据（`267009`=`0x41301`=`SCHED_S_TASK_RUNNING`「运行中」、非失败码；`267011` 同族），用当前实机状态（`State`／`LastTaskResult`／`NextRunTime`／对应审计日志活动时间戳）再核一次，确认结论仍成立或有新发现。
3. 在仓库内新建 `1-转型规划/0-全景路线图/修法建议-迁移观察期日核假阳性-2026-09-04.md`：写清楚现判据、新判据（State ＋ 审计日志活动双判，把 `267009`／`267011` 等「运行中」码从失败集合剔除的具体改法，含拟改的代码片段/diff 示意）、单测设计要点。
4. 🔴 **不直接改那个外部脚本/prompt 文件**——诊断与建议全部落在上一步的新建文档里；他本人在环，若他看完想现场应用，由他自己决定下一步（登记为 🟡，等他一个字母）。
5. §二 批次登记本次产出（走编辑锁）。

不做什么：
- 🔴 不改、不重启、不重新注册「Migration observation daily check」计划任务本体。
- 🔴 不改 `0-学习与工具/工具-落库sweep.py`／`工具-泳道看护状态机.py`。
- 🔴 不合入 master（本批仅新建一份文档，登记即可，但仍需他确认是否要把建议转成正式修法立项）。
```

---

## 四、开工前置（三条泳道通用）

**心跳**：各泳道写自己的 `reports/lane-heartbeat/B-0904_K-<泳道名>.md`（🔴 写在主仓、不写在 worktree 内），开工 1 分钟内写首行，每里程碑追加，超 10 分钟补写「仍在等 X」，收工写「DONE ｜ 产出落点」。
**分档**：判据现取 `criteria`；🟡/🔴 走 `pause --batch B-0904_K --wave 1 --lane <泳道名> --action-key <键> --waiting-for "<...>" --no-notify` ＋ 以 `OPENER_PARTIAL` 收尾；⏭️ 走 `transfer-out --batch B-0904_K --wave 1 --lane <泳道名> --action-key deploy_51 --note "<...>"`（泳道不停，继续做其余）。

## 五、收工（他手动跑完三个 CC 窗口后）

三条各自收工后，跑
```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0904_K
```
现取，不手工数；队列回写走锁。

## 六、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `A1`（`#459`）design 草案获批后实现代码 | 待他审完 design、答一个字母后另开一条泳道做实现 |
| `A2`（`#461`）合入 master | 登记 §四 待批，`merge_to_master` 属 🟡 |
| `A3`（`#460`）真的改外部脚本 | 他看完修法建议文档后自己决定，本泳道只到建议文档为止 |
| `#454`（与 `#459` 同碰 sweep.py） | 本批不动，避免撞车 |
