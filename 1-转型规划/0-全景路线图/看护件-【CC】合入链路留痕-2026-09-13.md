---
title: "看护件 · B-0913_合入链路留痕（#571 ⑹⑺⑻，单泳道）"
created: 2026-09-13
批次: B-0913_合入链路留痕
派出线: Cowork 环境总线 OP-0913-E
status: 已起（无头）
---

# 看护件 · B-0913_合入链路留痕

> 🔴 **起跑一律 `pwsh`、不传 `-LogDir`**（`#567`：`-Detach` 继承调用方 shell，写 `powershell` 必炸；纯时间戳批目录才进得了探针视野）。

## 一、本批是什么

承接 `#571` ⑹⑺⑻ —— 盘点件 §二 的 ① ②：**合入链路的机器把活干了，却没把结论留给下一个人**。

**当日实证（本会话亲历三次）**：配套支 `claude/op0913f-v2-launcher-json-571` 08:34、09:18 各被 rebase 一次、至今未合，而 `reports/` 下本轮**零日志**；`claude/op0913g-align-proc-freshness-570` 08:34:26 已 ff 进 master，它那行 09:3x 现取**仍在 `pending-ff.jsonl` 里**。⇒ 本方三次都只能靠 `git reflog` ＋进程表反推——**反推一次就是一次人守**。

## 二、硬边界

- 🔴 **只改 `0-学习与工具/工具-待合分支巡检.ps1`、`0-学习与工具/工具-泳道分支合入.ps1` 及其单测**。
- 🔴 **不得碰 `0-学习与工具/工具-落库sweep.py`**：`#556`② 抢救分支 `claude/op0913a-556-sweep-visibility`（`751495e`，未 ff）在该文件上有 +194 行未合改动。
- 🔴 **不得碰 `0-学习与工具/工具-opener批处理执行v2.ps1`**：配套支 `claude/op0913f-v2-launcher-json-571`（`e20a1c5`，未 ff）在该文件上有未合改动，同批动它必撞。
- 🔴 **不 ff master**；不碰 `.51`／企微／定时任务运行时。
- 🔴 **改的是正在跑合入的那两个脚本本身**：改完**先跑单测再收工**，严禁在主工作区直接试跑真实合入（会与在跑的巡检抢同一把 `.git/index.lock` 与同名临时 worktree）。
- 🔴 零回归按**差集口径**（分支 vs 纯 master detached worktree 同命令、`comm` 双向）。长回归别丢后台等事件——`op0913a` 正是那样被 600 s 上限掐死。

## 三、泳道

### A1 · §一 #571 ⑹⑺⑻ 合入链路留痕与登记册销行

▶ 粘贴端：CC ｜ 泳道：op0913j-ff-patrol-trace-571

```
[OP-0913-J]【CC】合入链路留痕
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0913j-ff-patrol-trace-571`） ｜ worktree：☑（op0913j-ff-patrol-trace-571，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_合入链路留痕
读 ① `队列 §一 #571 的 ⑹⑺⑻（`python 0-学习与工具/工具-队列查询.py --row 571 --section 一 --field all`，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913j-ff-patrol-trace-571 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0913j-ff-patrol-trace-571 --done --batch B-0913_合入链路留痕 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词（只在退回「人在环」时才粘；默认无头）

```
[OP-0913-K]【CC】看护看护合入留痕
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0913-E，批 B-0913_合入链路留痕
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0913K-看护看护合入留痕。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】合入链路留痕-2026-09-13.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、起跑

```
pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划\0-全景路线图\看护件-【CC】合入链路留痕-2026-09-13.md" -FullAuto -Yes -Detach
```
