---
status: 待派出
执行环境: CC
派出线: 环境总线
队列行: §一 #439
created: 2026-09-04
---

# 派单件 ·【CC】R1 口径点台账 · apply 前置两件（队列 §一 #439）

> 出件方：Cowork 环境总线 `OP-0904-E`（2026-09-04）。**本件只出派单，未动包一个字节。**
> 变更包已在 `openspec/changes/coverage-point-ledger/`（proposal＋design＋specs＋tasks 齐备），**design 已由 Shao Peishen 2026-09-01 当场审定**（`OP-0901-D`，九条 Decisions）。

## 一、为什么先派 §0 两件、且只派这两件

`tasks.md` §0 的两条前置**都标【CC】**，而 §1.1／§1.2（定 schema、建五份域文件骨架）标【Cowork】。三者之间是**串行**关系，且顺序不能反：

1. **§0.2 必须最先做** —— D7 例外 `!6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl` **必须加在 `.gitignore` 第 32 行 `**/*.jsonl` 之后**（git 后序优先）。🔴 **加在之前完全无效且不报错**。
2. **§0.1 紧随其后** —— 在真仓库原样复跑第二轮那 11 条 `git check-ignore -v` 并贴输出。`/tmp` 独立 repo 上验的是**规则逻辑**，不是真仓库状态；两者之间隔着一份**可能已被别人改过的 `.gitignore`**。
3. **§1.2 的五份骨架文件在 §0.2 落地之前建，会被 `.gitignore:32` 静默吞掉** —— 空文件加不进版本控制、`git add` 不报错、`git status` 也看不见它们。⇒ **顺序反了等于白做，且不产生任何信号。**

⇒ 本次只派 §0，做完即停、回报输出；§1.1／§1.2 由 Cowork 接棒；§1.3–§1.6（读写模块＋三条断言测试）再回 CC。

## 二、opener（复制即用）

▶ **首次派出：**

```
[OP-0904-F]【CC】R1台账apply前置
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0904f-coverage-ledger-gitignore`）｜ worktree：☑（`coverage-ledger-prereq`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：环境总线
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0904F-R1台账apply前置。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名。
读 ① `1-转型规划/0-全景路线图/派单件-【CC】R1口径点台账-apply前置-2026-09-04.md` → ② `openspec/changes/coverage-point-ledger/tasks.md` §0 与 `design.md` D7／D8 → ③ `CLAUDE.md` §4 路由表命中的 `.claude/rules/队列与落库.md` 恢复上下文。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 【tasks §0.2】在真 `.gitignore` 加 D7 例外 `!6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl`。🔴 **必须加在第 32 行 `**/*.jsonl` 之后**——加在之前完全无效且不报错。加完先 `sed -n '30,36p' .gitignore` 贴出上下文，自证位置对。
2. 【tasks §0.1】在**真仓库**原样复跑第二轮那 11 条 `git check-ignore -v`（清单与预期在 `1-转型规划/0-全景路线图/design审前置-口径点台账三开放点收敛-2026-09-01.md` §6.3），**逐条贴出真实输出**。🔴 其中 4 条是**反向对照组**（`reports/wecom_aibot_audit.jsonl`／`4-数字员工/…/reports/audit.jsonl`／`5-平台底座/logs/runtime.jsonl`／台账子目录内 `.jsonl`）——它们**必须仍被忽略**；任何一条被例外误放行即判前置未过，回滚 `.gitignore` 改动并报总线，不得自行放宽判据。
3. 勾掉 `tasks.md` §0.1／§0.2，并在队列 §一 `#439` 状态列**追加**一段（`工具-共享文档编辑锁.py edit-row --append`），写明：改了 `.gitignore` 哪一行、11 条输出是否全部符合预期、4 条反向对照组的实际结果。
4. 收工把改动登记 §二 待 commit 批次（走 `0-学习与工具/工具-共享文档编辑锁.py`），由落库 sweep 取活。

不做什么：
- **不做 tasks §1 及以后任何一条** —— §1.1／§1.2 标【Cowork】，须由 Cowork 接棒；§1.3–§1.6 依赖 §1.1 的 schema 与 §1.2 的骨架文件，现在做等于凭空猜 schema。
- **不建那五份域文件骨架** —— 顺序若反，文件会被 `.gitignore:32` 静默吞掉，且不产生任何信号。
- **不自行 commit**（§二 登记完即完事，sweep 取活）；**不改 `6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` 的「发送状态」列**（§四 #85 信级唯一权威源，本包管点级，两个粒度不得同批动）。
- **不碰 `.51`、不发企微、不动定时任务**。
```

## 三、验收判据（总线取件时按此核，不采信自述）

| # | 判据 | 怎么核 |
|---|---|---|
| ⑴ | D7 例外确在 `.gitignore` 第 32 行**之后** | 看它贴的 `sed -n '30,36p'` 上下文，不看它的结论句 |
| ⑵ | 11 条 `git check-ignore -v` 为**真仓库**输出 | 输出须带真实规则行号（`.gitignore:NN`）；`/tmp` repo 的行号与真仓库不同 |
| ⑶ | 4 条反向对照组**仍被忽略** | 逐条对，任何一条变成「未忽略」即前置不过 |
| ⑷ | 未越界做 §1 | `git show --stat` 里不得出现 `口径点台账/` 下的新文件 |

## 四、下一棒（本件不执行，只写明去向）

**§0 过 → Cowork 做 §1.1（定 8 字段 schema）＋ §1.2（建五份域文件骨架，🔴 目录下不得建子目录，`!` 例外不递归、子目录内 `.jsonl` 会被静默忽略）→ 再回 CC 做 §1.3–§1.6（读写模块 ＋ 三条断言测试：跨域聚合只读不落盘／`已签认` 只能由真实回件驱动／`提出日` 写入后不得被补记覆盖）。**

⚠️ **编号说明**：本件取 `OP-0904-F`，**取号无机制保障**（队列 §一 `#151`「OP 编号无分配机制」至今未落，2026-09-02 一天内已实撞两条同号）。派出前请总线扫一眼当日是否已有 `-F`。
