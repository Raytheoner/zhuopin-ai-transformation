---
status: 在办
title: "拆件巡逻报告 · 2026-09-09（事件驱动班）"
created: 2026-09-09
用途: Dispatch v0.5 回件拆件巡逻当日报告；本班由 followup_readme_bridge.mark_reply_arrived 无头起活
---

# 拆件巡逻报告 · 2026-09-09（第一班 · 事件驱动无头起活）

▶ 本报告 1 项定夺：0 项⟨就地答⟩，1 项已登记队列待总线派发（§四 `#182`）

## 〇ter 探测

`[SIGNAL]` —— 待处理 1 条，`财务部#17`／`财务部-tangyanping-回复-2026-09-09-文本反馈-3e3e2cbbb582b4c401f0b07e9f0402dc.md`，`CHECKPOINT=2026-09-09T02:47:34Z`。走 §一~§四 全套。

## 一、扫描

`--digest` 覆盖两份真身（机制环境 115 行 ＋ 业务场景 58 行 ＝ 173 行）。**待领且含「企微反馈自动归档」＝ 3 行：`#514`／`#515`／`#516`**，全部 `tangyanping`、全部 2026-09-09 登记、全部指向 `财务部#17` ⇒ **同批合并读取**（§二 硬约束 3）。

## 二、拆件（三行合一件）

### `#514` · 文本反馈（导语）
- **内容摘要**：一句话导语——「这是你发的财务部跟进信第 17 封中需要的存货跌价计提底稿，详见附件《存货跌价底稿2024》《存货跌价底稿2025》」。
- **已执行动作**：销行 ✅（写在状态列开头）；实质内容指向 `#515`／`#516`。
- **判断依据**：本行零实质，附件才是回件本体。
- **需 SP 项**：无。

### `#515` · 2025 底稿 xlsx ／ `#516` · 2024 底稿 xlsx
- **内容摘要**：两份 WPE 格式年度审计工作底稿，各 2 个 sheet（`5300E-4 原材料&委托加工物资跌价测试`／`5300G-4 库存商品跌价测试`）。2025＝75＋131 行，2024＝327＋100 行。
- **已执行动作**：两行各销行 ✅；逐字实测结论落 §一 `#518` 状态列（本报告不复述原文，原件在 `7-外部文档/财务部/`）；台账 `FI10-G-07` 转 `已签认`；派生 §一 `#518` ＋ §四 `#182`。
- **判断依据**：`openpyxl data_only=True` **逐 sheet 全量读**、非只解析首表（§二 硬约束 1 —— 本次实证该约束仍在生效：计提规则原文写在 E-4 第 5 行的自由文本合并单元格里，只读表头下的数据区一个字都取不到）。信里要的三项（近 2–3 年／按年度与物料类别各一份样例／可脱敏）**全部覆盖**。
- **需 SP 项**：§四 `#182`（底稿备注列客户名未打码，处置三选一，默认 (a)）。

### 本班对台账与 README 的回写
- **台账回写：已签认 1 点／仍在途 0 点** —— `FI10-G-07`（材料索取类）`在途 → 已签认`，`--fact-date 2026-09-09`（回件落档日）、`--evidence` ＝ 2025 底稿落档件；退出码 0。FI10 其余 6 个判据类点**未被本信投影**（`points-by-letter` 只返回 `FI10-G-07`），状态一律不动、仍 `待问`。
- **README**：`财务部#17` `📨 回件已到，待拆件 → 📥 已回件并回灌 2026-09-09`，串行闸已解，可起草 `财务部#18`。

## 三、待发信盘点（§一.3）

`⏳ 待你审 × 0 ／ 🆕 待发 × 0 ／ ⏸ 暂缓 × 0`（主表合计 45 行，未识别前缀 0 行）。**无积压，无「队列称暂缓 / README 仍 🆕 待发」交叉红标。**

## 四、起草缺口检测（§一.4）

`draft_gap_check.py --window-days 14`：**无缺口**（原样并入）。

## 五、需 Shao Peishen 的动作

**§四 `#182`** —— 唐燕萍交来的 FI10 底稿脱敏不彻底：备注列留着真实客户名（实测「云内」「南岳」「万开」「海外新客户」；料号原样保留、品名已替换为代号、金额已按比例缩放）。**(a)** 接受现状、由 `#518` 起草时机器剥离该列（**推荐，默认项**）／**(b)** 回她一句请求重发脱敏版／**(c)** 推给 `#518` 起草时一并处置。截止 2026-09-12。**不构成 OEM 隔离跨库**（三家非比亚迪／上汽／理想，件在自有仓库内），风险在下游外发与摄取。

## 六、刻意未做

起草 `财务部#18` 与判例批改表（对外起草＝§〇 红线，已登 §一 `#518` 待总线派发）｜ 未动 `#474` 排期与状态｜ 未动 FI10 六个判据类台账点｜ 未建 FI10 工程/引擎代码｜ 未改高水位线声明行。

## 巡逻自检

- **〇ter 探测**：`[SIGNAL]`，非 fail-open；**信号消费在本报告登记 §二 批次之后执行**（`--clear --before 2026-09-09T02:47:34Z`）。
- **〇bis 跳过项**：**本班不按非交互处理** —— 本机 PowerShell 实测可用（`Get-Date` 返回 `2026-09-09 10:48:32`），故一.4／三.8／四.5／四.6 **四步全部照做、无一跳过**。〇bis 的判据是「无本机 PowerShell、无出网 shell」这一能力事实，不是「由定时/事件触发」这一来源事实；本班由 `mark_reply_arrived` 无头起活但持有完整工具链。
- **本轮实拿编号**：§一 `#518`（走 `acquire --reserve 1 --section 一`，工具返回）；§四 `#182`（**预留豁免**，见下）。§一 `#517` 系首次 acquire 回滚后留下的空洞，编号不复用。
- **🔴 §四 取号被拒 —— 第四次撞高水位线滞后**：首次 `acquire --reserve-multi 一:1 四:1` 被工具拒绝，原文「即将分配的编号 [178] 已存在于当前文件 §四 可见行中（高水位线=177，落后于文件实际内容）」，该次 acquire 一并回滚（§一 高水位线已推、不回退）。改走**逐号只读探测**（`--row 178/179/180/181/182 --section 四`：178–181 命中、182 未找到）后显式指定 `182`，行内已写「预留豁免：」。**高水位线声明行本班未动**（属 `#505` 触碰区）。⇒ 继 09-07／09-08（两次）之后第四次，`#505` apply 的必要性再获一条独立实证。
- **并入审核（协议〇.10）**：`--digest --grep FI10` 扫全池，命中 `#474`／`#475`／`#476` 三行。**无可并入** —— `#474` 期望产出明写「本行只管『立行与排期』、不管怎么建」且已两度 K2 外置、行长吃紧；`#475` 是芯片价格 API 选型；`#476` 是 L2 纪律收编且状态为 🛑 不得开工。派生活（对外起草 `财务部#18`）三者皆非其承接载体 ⇒ 取号新建 `#518`。业务行不受 WIP 上限约束。
- **三.6 写回前比对**：`edit-row`／`append-row` 全程走工具行级定点替换，未整文件写回；三笔 `edit-row` ＋ 两笔 `append-row` 退出码均 0；**未出现自己没写过的新增行**。acquire 时工具报「机制环境.md 在上次 release 之后被直接改写（443→443 行，约 2 行不同）」——**成因已定位为本班自己**：首次 `--reserve-multi` 在 §四 失败前已把 §一 高水位线推进并落盘，而该次 acquire 整体回滚、没有配对的 release，故留下一处无锁写入痕迹。非他线绕锁。
- **本班自造的一处噪声（如实记）**：读 xlsx 用的临时脚本 `dump_xlsx_tmp.py` 曾落在仓库根约 7 分钟，被同期两个 CC 会话（`OP-0909-I`／`OP-0909-I-行长`）在 release 时当作「来源不明的孤儿临时件」写进豁免说明。**该件已删**。教训：巡逻的临时脚本应写在仓库外（本班后续的 JSON 载荷已改写到 `%TEMP%\chaijian0909\`）。
- **预案覆盖不到的情形**：无（三行同批、形态标准）。

---

# 第二班 · 2026-09-09（事件驱动无头起活）

▶ 本报告 2 项定夺：0 项⟨就地答⟩，2 项已登记队列待总线派发（§四 `#186`／`#187`）

## 〇ter 探测

`[SIGNAL]` —— 待处理 1 条，`IT部#12`／`IT-2023458-回复-2026-09-09-文本反馈-1947b911f9fc3e788052770c2eb05faa.md`，`CHECKPOINT=2026-09-09T07:53:52Z`。非 fail-open。走 §一~§四 全套。

## 一、扫描 —— 🔴 digest 零命中，但**不是**空巡

`--digest` 覆盖两份真身（机制环境 83 行 ＋ 业务场景 51 行 ＝ 134 行）。**待领且含「企微反馈自动归档」＝ 0 行**。按 §一.5 字面判据本班应回「空巡」并结束 —— **本班判定不适用，理由是可核的事实而非推测**：

- README 里 `IT部#12` 状态格是 `📨 回件已到，待拆件 2026-09-09T07:53:52Z`（桥一第九态已标）；
- 归档件确实在 `7-外部文档/IT/` 里落盘（实读 1,155 字正文）；
- `5-平台底座/wecom-aibot-service/reports/pending_queue_lock_appends.jsonl` 里正好躺着这一条追加参数（`recorded_at=2026-09-09T07:53:51.502658+00:00`，`sender=2023458`，`input_pointer` 即该归档件）。

⇒ 队列行不是没生成，是**因编辑锁占用被推迟**（`#168` 设计路径），补录要等「下一条消息到达」。**本班照常拆件，源头取归档件本体**；机制缺口已并入 §一 `#416`（详见下）。

## 二、拆件 —— `IT部#12`（陈承 2026-09-09 回件，一件）

- **行号**：无 §一 行可销（见上）；README 侧已由 `📨` 转 `📥`。
- **内容摘要**：三件查证类**全部作答且全部为「可行」**。⑴ 请购需求池已开放（`/requisition/getList.json`＋`/requisition/getDetail.json`，正式库实测调通，取回单头＋2 行明细）；⑵ 供应计划排程已开放（`/schedule/getList.json`＋`/schedule/getDetail.json`）；⑶「负责采购」字段**实测有值**（行级 `buyerList[]` 的 `employeeName`／`erpEmployeeAccount`）。另交来调用凭证已具备、以及两条硬约束：**请购接口不能查 30 天前数据**＋单次窗口与调用间隔受限；**单据行级状态与单头状态可能不一致**，统计须取行级。
- **已执行动作**：回灌 §一 `#499`（端点侧阻塞解除＋六项指标逐项对应到接口与字段＋两条硬约束）、§一 `#334`（D5-a 挂账解锁，08-20「取不到」结论标注作废）、§一 `#416`（并入第 4 条同族缺陷）、§四 `#168`（(a) 的「先核端点」前置达成）；派生 §四 `#186`／`#187`。**未销任何行、未改任何 `[S:]` 状态字段**。
- **判断依据**：通读整份回件全部段落（§二 硬约束 1）——三条端点结论确在表格之外的自由文本里，五节全读；无 ✅/❌ 列形态，不适用硬约束 2；单件到达，不适用硬约束 3。
- **需 SP 项**：§四 `#186`（取数层是否不等口径先开工，默认 (a)）、§四 `#187`（物料看板要不要为「负责采购」列重开，默认 (a)）。
- **台账回写**：`points-by-letter --letter "IT部#12"` 返回 `[NONE]`，退出码 0 ⇒ **本信非台账投影，零回写**。

## 三、并入 §一 `#416` 的第 4 条缺陷（本班实撞，只登记不修）

⑴ **去重不对称**：`flush_pending_git_sync_appends` 有 `input_pointer_already_in_queue` 去重（`#387` ⑹ 固化），`queue_lock_pending.flush_pending_queue_appends` **没有** —— 谁在补录前手工补了这一行，下次消息到达必补出第二条。**本班因此刻意没有手工补行**。
⑵ 🔴 **本班自订正（写完上段后当场推翻）**：上段曾写「无兜底触发、补录唯一入口是 `on_message`」，**这是错的，已撤回** —— `scripts/decision_reminder_check.py` 就有一道兜底 flush（`#219` 双载体设计），本班 §四.6 跑它时它输出了 `[OK] 兜底 flush：已补录 1 条`。**错因**：上段只 grep 了 `aibot_service/` 包内的调用点，而第二道载体在 `scripts/` 下。
⑶ 🔴 **⑴ 当场被实证兑现，且比预想更糟**：兜底 flush 一跑，同一条暂存记录补出了**两行完全相同的队列行 `#524`／`#525`**。审计坐实的不是「跑了两遍」而是「**两个进程并发**」：`queue_append_pending_flushed` 于 16:13:37 与 16:13:41 各一条、`recorded_at` 逐字相同，同时刻 `decision_reminder_sent`／`open_pool_reminder_sent` 也各两条。⇒ **缺陷表述改写**：不只是「缺去重网」，而是 `flush_pending_queue_appends` 的「读暂存 → append → 重写暂存」三步**未落在同一把跨进程锁内**（只有 append 那一步各自 acquire 一次队列锁），去重网在这个竞态里**只能减小窗口、关不掉它**。**修法改为三条**：① 接同源去重网（必要不充分）；② 把整个 flush 包进一把跨进程锁；③ digest 零命中但暂存非空时 fail-loud。
⑷ **已处置**：`#524` 记实质结论并 ✅，`#525` 标重复并 ✅，两行顺带补齐机器人写入时缺失的 `[D:业]`（§一 `#523` 同一形态的新实证）；订正与完整修法已写进 §一 `#416`。🔑 **本班自己的教训**：「在实现目录里找不到第二个调用方」不等于「不存在第二个调用方」。

## 四、待发信盘点（§一.3）·增量

`⏳ 待你审 × 0 ／ 🆕 待发 × 0 ／ ⏸ 暂缓 × 0`（主表 47 行）。较第一班的增量：`采购部#22`（✅ 已推送 2026-09-09 07:07 UTC）与 `IT部#12`（本班转 `📥`）两行落定，**积压仍为零**。**无「队列称暂缓 / README 仍 🆕 待发」交叉红标** —— 交叉核过 `采购部#22`：`#499`／`#506` 行内的「押后待 `#506` 修好再发」判据已于 09-09 12:51 部署冒烟八段全过而解除，发送与押后口径一致，非违规发出。

## 五、起草缺口检测（§一.4）

`draft_gap_check.py --window-days 14`：`待发信盘点（扩展版，近 14 天已部署场景 vs 已起草跟进信）：无缺口`（原样并入）。

## 六、刻意未做

未手工补那条待领行（理由见 §三 ⑴）｜ 未修 `#416` 第 4 条（建造＝红线）｜ 未销任何 §一 行、未改任何 `[S:]` 状态｜ 未起草 `IT部#13`（对外＝红线；本次三件全答、暂无追问必要）｜ 未动 `#95`／`#403` 等其它 SRM 相关行（本回件未触及）｜ 未把凭证类值（携客号／ERP 账号）写进任何队列行，只留落档件指针。

## 巡逻自检（第二班）

- **〇ter 探测**：`[SIGNAL]`，非 fail-open；**信号消费在本报告登记 §二 批次之后执行**（`--clear --before 2026-09-09T07:53:52Z`）。
- **〇bis 跳过项**：**本班不按非交互处理** —— 本机 PowerShell 实测可用（`Get-Date` 返回 `2026-09-09 16:02:50`），一.4／三.8／四.5／四.6 四步全部照做、无一跳过。
- **本轮实拿编号**：§四 `#186`／`#187`，走 `acquire --reserve-multi 四:2` 由工具分配并同步回写高水位线。**§一 零新行**。⚠️ 首次 acquire 时未带 `--reserve`（拿锁后才确认要取号），故 release 后立即重新 acquire 一次带 reserve —— 中间窗口零写入，已在 waiver 里写明。**本班未撞高水位线滞后**（acquire 报 §四 `#185`，与 acquire 前逐号只读探测 183/184/185 命中、186 未找到一致）。
- **并入审核（协议〇.10）**：`--digest --grep 携客云` 命中 1 行（`#499`）、`--grep 端点` 命中 7 行（`#138`／`#282`／`#388`／`#403`／`#467`／`#472`／`#499`）。**端点事实并入 `#499`＋`#334`，机制缺陷并入 `#416`，§一 零取号** —— `#416` 本就是「企微回件链路三缺陷」的容器行、`[S:open]` 且带 `WIP豁免`，本次为其第 4 条同族缺陷，故不新建机制行（也因此不触发机制类可动 WIP 上限判定）。
- **三.6 写回前比对**：四笔 `edit-row` ＋ 两笔 `append-row` 全程走工具行级定点替换，未整文件写回，退出码均 0；**未出现自己没写过的新增行**。
- **锁**：跟进信 README 与队列真身**分两把锁**（`--file` 单文件单锁 ／ 默认双文件共用锁），README 那把改完即释放，未跨窗口持有。
- **预案覆盖不到的情形**：**有一处，已如实登记** —— 章程 §一.5「digest 零命中即空巡」与 §〇ter「有信号即走全套」在本班**直接冲突**（信号真、归档件真、README 第九态真，唯独队列行因 `#168` 锁忙暂存而不存在）。本班按「信号 + README + 暂存文件三者互证」判定不空巡，**未自行改章程**；建议总线考虑把「暂存非空 ⇒ 不得判空巡」写进 §一.5，已并入 §一 `#416` 的建议修法 ⑵。
