---
status: 待执行
title: "派单件 ·【Cowork】质量域场景清单重审 —— 质量部整体改写后的全面审核与推进"
created: 2026-08-21
执行环境: Cowork
派出线: Cowork 环境总线（OP-0821-B）
编号: OP-0821-H
来源: Shao Peishen 2026-08-21 「另起业务新 Session 按质量部业务决策全面审核推进执行」
关联队列行: §一 #368（P1，材料落档与回话）／§四 #88（范围裁决，待拍板）／§一 #363・#365（源材料）
---

# 派单件 ·【Cowork】质量域场景清单重审

## 开场词（复制即用）

```
【OP-0821-H】质量域场景重审
【设置】执行环境：Cowork ｜ CC session：☐ 不适用 ｜ worktree：☐ 不新建（主工作区）｜ 分支：master ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 环境总线

读 1-转型规划\0-全景路线图\派单件-【Cowork】质量域场景清单重审-2026-08-21.md 全文。
```

▶ **粘贴端：Cowork**

---

## 一、发生了什么（一句话）

**质量部经内部集体讨论，把质量域 8 个场景判掉 5 个、重写 2 个、确认 1 个已完成。**

材料＝陈忱 2026-08-21 13:15 经企微发回的 `质量部AI赋能场景_20260820.docx` ＋ 正文说明信。**已拆件落档，见 `§一 #368`**（那一行已把逐场景摘要写全，本件不重复）。

---

## 二、事实基线（现行权威 vs 业务方判定，一张表对齐）

现行权威＝`1-转型规划/0-全景路线图/卓品智能AI转型全景规划.md` §2.1.3（L374-L536）。

| 场景 | 现行排期 | 序位 | 陈忱判定 | 🔴 值得注意 |
|---|---|---|---|---|
| **Q2** 8D 报告智能**起草** | **2026-10** | 旗舰① | **保留，整份重写为「8D 报告 AI 自动判定」** | 🔴 **性质变了，见 §三.1** |
| **Q6** 项目立项审核门禁 | **2026-10** | 旗舰② | **已完成** ✅ | 与我方认知一致（＝已建成的 QD-B） |
| **Q1** 客诉智能分类与自动分流 | **2026-12** | 第③ | ❌ 不适用 | **S1 期内** |
| **Q5** IATF 审核准备助手 | **2026-12** | 第④ | ❌ 不适用 | 🔴 **v6 于 2026-08-18 刚三度提前到 2026-12——三天后业务方说不适用** |
| **Q4** PPAP 文档自动审核 | 2027-04 | 第⑦ | **保留，整份重写并收敛范围** | 收敛为三项校验，见 `#368` |
| **Q3** FMEA 智能维护助手 | 2027-05 | 末位 | ❌ 不适用 | 「最重最敏感、涉 OEM 设计数据」，判掉省的是最大一块协同成本 |
| **Q7** IQC 来料检验智能判定 | 2027-05 | 新增 | ❌ 不适用 | 🔴 **2026-06-11 由我方补入**，全景原文写「原序列遗漏」 |
| **Q8** SPC 制程异常预警 | 2027-06 | 新增 | ❌ 不适用 | 同上 |

---

## 三、🔴 三条 `#368` 与 `#88` 都没写出来的发现（本件的主要增量，请优先核这三条）

### 3.1 Q2 不是「描述改写」，是**场景性质改变** —— 名义保留、实质换了一个场景

| | 输入 | 输出 | 性质 |
|---|---|---|---|
| **全景规划现行 Q2** | 客诉信息 | 8D 报告 D1-D8 **初稿** ＋ 历史根因检索 ＋ 中英双语 | **生成** |
| **陈忱重写的 Q2** | **已有的** 8D 报告 | 按《8D报告AI评审规则库》给出**评审结论**（红线项/扣分项/建议项） | **评审** |

**现行价值指标是「8D 报告输出时间从 2 天缩短至 4 小时」——那是给写报告的人省时间；新版是给审报告的人省时间。受益人都换了。**

⇒ **即便 Q2「保留」，它也已经是一次范围变更，须走登记**；不能因为编号没变就当作原地修订。**这一条决定了 `§四 #88` 的选项要不要多一个分支。**

### 3.2 QD-A 已经把新 Q2 需要的前半段做完了 —— 可直接复用，不必从零

已建成的 `4-数字员工/质量部/QD-A-8D不良分析/`（自述「8D不良分析**预填脚本**」）：**输入陈忱团队提供的 8D 报告（docx/pdf），输出 12 字段预填录入表 ＋ 脱敏令牌建议表**，纯规则提取、不调 LLM。

⇒ **新 Q2（评审）与 QD-A（抽取）输入完全相同、输出不同** —— **QD-A 的「读 8D 报告 → 结构化 12 字段」这一段正是新 Q2 的前置**。**评审规则库可以直接架在 QD-A 的输出上。**

⚠️ **请核实这一点再写进回话**：QD-A 的 12 字段是否覆盖评审规则库需要的维度（读 `qda_prefill/field_extractor.py` 与其字段清单）。**覆盖不足的差集，正是要问陈忱的具体问题。**

### 3.3 「8D 多为 PPT 格式」这条硬事实**直接打穿 QD-A 现有解析器**

陈忱给出「我司 8D 报告多为 PPT 格式，须在脚本编写时明确」——而 **QD-A 的 `doc_reader.py` 处理的是 docx/pdf**，其 CLAUDE.md 明写输入为「8D 报告（docx/pdf）」。

⇒ ⑴ **QD-A 现有校准（>60% 命中率那条）是在什么格式的样本上做的？** 若样本是 docx 而实际生产多为 PPT，**那个命中率对真实场景无效**。
⇒ ⑵ 这直接影响 `doc_parser` 的 rule-of-three 评估（QD-A CLAUDE.md 记「当前 2 个消费方，未触发三法则」）——**加 PPT 就是第三个形态**。

**⇒ 这条要写进给陈忱的回话，并请她给 10-30 份历史 8D 时明确 PPT/docx 各占多少。**

---

## 四、要做什么（四步，第 1 步是硬前置）

### 第 1 步 · 数字对账（先做，因为后面所有排期讨论都依赖它）

`§四 #88` 写「质量域 8→3、全景 42→37」，而**实测全景规划正文里「42 个」命中 0、「40 个」命中 1 处**（KPI 目标段），根 `CLAUDE.md` §1 写 **40**（2026-07-06 采购 v2.3 重排后）。

**⇒ 先把「现行场景总数到底是多少」核准并写下依据**（数 §加速启动总览排期表的行数，不要数正文里的字样）。**这个数错了，重排就会错。**

### 第 2 步 · Q2／Q4 差异比对与落档（**不受 `#88` 阻塞，可立即做**）

逐条比对陈忱重写版 vs 全景规划现行 §2.1.3，产出**差异清单**（痛点／AI 做什么／数据依赖／自动化等级／预期价值／实施步骤，六项逐项对照）。**含 §三.1 那条性质改变。**

### 第 3 步 · 就「请参考是否可行」给陈忱一次回话（**受串行闸约束**）

🔴 **她那句「请参考是否可行」要的是技术回应，不是一句收到。** 回话至少要覆盖：

1. **Q2 性质改变已被识别**（起草 → 评审），我方接受与否、以及它对排期与旗舰定位的影响；
2. **QD-A 可复用**（§三.2）——告诉她 8D 的读取与 12 字段结构化已经建好了，评审规则库可以架在上面，**并列出 12 字段与评审维度的差集，请她补**；
3. **PPT 格式**（§三.3）——请她在给 10-30 份历史 8D 时标明格式分布；
4. **Q4 收敛后的三项校验**我方是否认可，以及「CC 特性严重度须 9-10」这类规则能否结构化。

**信件形态**：按判例批改法（≤10 条真实案例三选一）还是三要素跟进信，由执行方按内容判；**若含口径判定则必须用判例批改法**（根 `CLAUDE.md` §5 硬规则）。

⚠️ **串行闸**：陈忱当前闸是**开**的（`质量部#8` 已于 2026-08-21 转 `📥`）⇒ **可占 `质量部#9`**。
🔴 **发前跑一次判闸**：`工具-跟进闸查询.py` 尚在实现中，**在它上线前一律直读 `6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` 该收信人最近一行**，不读队列里的任何转述（今日新立的 S1）。

### 第 4 步 · 范围处置（**必须等 `§四 #88` 拍板，不得抢跑**）

---

## 五、🔴 红线四条

1. **`§四 #88` 未拍板前，不得改任何权威件** —— 不动 `全景规划 §2.1.3`、不动加速启动总览排期表、不动甘特、不动质量域路线图。**第 2 步只产出差异清单，不落权威件。**
2. **不得把「当前阶段不适用」当成「永久注销」执行** —— 陈忱原话是「部分场景在**我司当前阶段**不适用」，`§四 #88` 已指出这四个字指向暂缓；且 **Q7/Q8 的阻塞在我方文档里明写是「MES/QMS 接口未通 ＋ 检验标准未结构化」，那是条件不具备，不是业务不需要**。**(a) 不可逆，(b) 可逆。**
3. **本件不含范围裁决权** —— 全面审核的产出是**给 `#88` 一个更锐的推荐**，不是替他拍板。审完若认为 `#88` 三个选项不够（例如 §三.1 那条性质改变可能需要第四个分支），**提出来，不要硬塞进现有三选一**。
4. **回话不得抢跑** —— 第 3 步的信须经 Shao Peishen 审核后方可发（`⏳ 待你审` 是唯一合法起草产物，不得直接写终态）。

## 六、收工段

- 回写 `§一 #368`（做到哪一步）与 `§四 #88`（审核后的推荐，含是否需要第四个分支）。
- 若起草了信，README 登记行「发送状态」列**只写 `⏳ 待你审`**。
- **不自行 commit**：文件清单 ＋ 建议 commit message ＋ 登记 §二 待 commit 批次，登记后立即 `Start-ScheduledTask -TaskName "ZhuopinCommitSweep"`。
- 🔴 **取号一律 `--reserve`**；顶部「编号高水位线」是**下一个可用号**，不是「已预留给你」。
- ⚠️ 读队列文件用 `-split "\r?\n"` —— **本日实测队列换行符已被某次写入改成 LF**，按 `\r\n` 切会把整份文件读成一行。
