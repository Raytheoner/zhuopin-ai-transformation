---
status: 在办
date: 2026-08-06
owner: Cowork-拆件巡逻（Dispatch v0.5，阶段二 live）
---

# 拆件巡逻报告 · 2026-08-06（第一班）

**扫描范围**：§一 文内真实可见最大号 **#278** 与顶部编号高水位线 **#279**（acquire 前值）取较大者 → **#279**。
**识别**：状态为「待领」且含「企微反馈自动归档」的行 **8 行（#271-#278）**。
**已拆**：8 行全部销行。**新增**：§一 #280、#281；§四 #53。

---

## 〇、待发信盘点（只读只提醒，本班未发送任何信件）

两态分别计数（README 表格实读）：

| 态 | 条数 | 明细 |
|----|------|------|
| `🆕 待发` | **2** | ① **采购部（未发，不编号）2026-07-29 · 姚祖怡**「批2上月未齐套跨月占用判例批改」——已积压 **8 天**；② **财务部#11 · 2026-08-05 · 唐燕萍**「FI2 面板 6 项显示问题已修复请复核」 |
| `⏳ 待你审` | **1** | **IT部#7 · 2026-08-05 · 陈承**「企微自建应用凭据申请 ＋ 一个 AP 附件下载异常」 |

**两条说明，均不新起决策项**：

- **07-29 那封的积压已有归属**——它正是 **§四 #52**（「下一封给姚祖怡的信怎么组织，三选一」）的第 ② 条事实，且 #52 已写明串行原则前提现已解除。本班不重复提问，只提示它今日仍在表内且已 8 天。
- **财务部#11 今日会自动发出**——#264 行记载 Shao Peishen 2026-08-05 已放行并经 `approve_followup_letter.py` 转入 `🆕 待发`；今日为**周四（工作日）**，`ZhuopinFollowupDispatchDaily` 09:30 本地触发。本报告生成时刻为 **09:18 本地（01:18Z）**，即约 12 分钟后自动推送，无需人工动作。

---

## 一、#271-#278（8 行同批合并读取）

| 项 | 内容 |
|----|------|
| **行号** | §一 #271／#272／#273／#274／#275／#276／#277／#278 |
| **文件** | `7-外部文档/待分拣/待分拣-ShaoPeiShen-回复-2026-08-06-文本反馈-{865f67e0,f8421cf2,08189516,89c412af,6a502beb,42e0432b,29dfec06,6c32746b}.md`（8 件，均已通读全文） |
| **内容一句话** | 全部为单行文本：`@邵总数字人🤖` 后接 **测试短信／财务测试短信／质量测试短信／跨部门测试短信** 四种措辞之一。无业务内容、无口径诉求、无需回灌。 |
| **已执行动作** | 8 行全部销行（状态列以 `✅ 已拆件（2026-08-06 拆件巡逻）` 开头），并在每行写明本班判定与两条派生去向（§一 #280、§四 #53 ②）。 |

### 判断依据（这一段是本班的实际产出，不是形式记录）

**⑴ 它不是闲聊，是一次真实探测——而且是失败的探测。**

单看内容，8 条是纯连通性测试，本可一句「无需处理」销掉。但**同批合并读取 + 交叉核对队列**后指向另一个结论：

- §一 **#270**（2026-08-06 由 CC `agent-a40ffa8425fd046c9` 建，同日）记载：跟进信群 cc 改走智能机器人 chatid 通道的**机制已建造完成并合入 master**（commit `83411f9`），**唯一卡点** ＝ 三部门真实群 chatid 仍是占位空字符串，原文写「**需要有人在每个群发一条消息触发入站帧、从中读出 chatid**」。
- 今晨 **08:08-08:35** 连发 11 条，措辞恰好覆盖 **财务／质量／跨部门**（＝机器人所在的三个部门群）。时间、条数、措辞三者与 #270 那句话吻合。

**判定：这 11 条就是 Shao Peishen 本人在执行 #270 要求的那个动作。**

**⑵ 探测零采集，且与他怎么发无关——接收侧根本没有落点。**（→ 已立 §一 #280）

三条取证：

1. `5-平台底座/wecom-aibot-service/aibot_service/frame_parsing.py` **第 9-10 行注释**已明确写出 `chatid` 在 `body.chatid`、群聊消息另含 `chattype`；**但同文件 `InboundMessage`（24-31 行）只有 7 个字段** —— `sender`／`msgtype`／`text_content`／`file_url`／`file_aes_key`／`file_name_hint`／`msgid`，**没有 `chatid`，也没有 `chattype`**。解析时被直接丢弃。
2. 今日 11 条消息的 `archived` 审计记录，`data_sources` 一律只有 `sender` 与 `msgtype`；**今日全库 77 条审计记录零 chatid／chattype 命中**。
3. 故 #270 行内那个动作**已被真实执行过 11 次，产出为零** —— 不是发法不对，是没地方接。

**这条更正的价值在于**：#270 现在的定性会让人继续在「找人去群里发消息」上打转，而真正的前置是一处约半天的解析改动。**#280 与 #270 同触碰区，已在行内标明须串行或同 session，勿并发。**

**⑶ 一条副作用：他本人不在部门映射表内。**（→ 已立 §四 #53 ②）

`department_mapping.yaml` 现有 5 个键（`YaoZuYi`／`tangyanping`／`ChenChen`／`Hongqin.Wang`／`2023458`），**不含 `ShaoPeiShen`**，故 fail-closed 归 `待分拣` 并**逐条建一行队列行**：11 条消息 ＝ 11 行待拆件。
另一个可复用的事实：**部门归属只认发送人 userid，不看正文关键词** —— 消息里写「财务」不会让它归档进财务部；`inbound_forward_skipped` 的 `reason` 又是 `sender_is_paul`，即**转发路径认得他、归档路径不认得他**，两处身份判定不一致。

**⑷ 顺带发现：11 条消息只生成了 8 行队列行。**（→ 已立 §一 #281）

差的 3 条（`7bb79f26`／`0ee7b1cb`／`69d75cb0`）**没有丢**，在 `reports/pending_queue_appends.jsonl` 里：编辑锁当时被**机器人自己**占用（持锁方记为「企微机器人（归档追加）」与「企微机器人（git冲突重算追加）」），按 #192-#199 设计推迟。第 4 条 pending（`6a502beb`，队列行已建＝#275）的推迟原因不同，是 `git push` 被 remote 拒（`cannot lock ref 'refs/heads/master'`，并发 push 竞态）。
**机制按设计工作，非缺陷**；#281 只做一件事——下一轮 sweep 后核实这 4 条是否已落库，未落则人工 flush 并补齐那 3 行。

### 需 Shao Peishen 项

**有，1 条，两个子决策，均已登记 §四 #53（标注【总线答】）**：

- **①** 三部门群 chatid 你手上是否已有 —— (a) 已有／(b) 没有，派 CC 先做解析改动／(c) 先搁置。**默认 (b)**。截止建议今日内（挡着 #270 收口与 webhook 下线）。
- **②** 是否把你本人补进 `department_mapping.yaml` —— (a) 补入并映射新目的地／(b) 只归档不建队列行／(c) 维持现状。**默认 (c)**。不卡时间。

> 标注说明：本项由本次巡逻自己提出，故不标任何「回本 session 答」类标签；因牵动 #270／#280／#281 三个队列编号并需派 CC，按判据标 **【总线答】**。

---

## 二、巡逻自检

**⑴ 本轮有无预案覆盖不了的情形**：有一处，已按红线处理。

- 8 条消息本身属「无需回灌」，按 §五 本可一句销行了事。但交叉核对后浮出的 **#270 卡点定性有误**这一事实，超出「回件拆件」的常规形态。**处理方式**：只登记**已取证的事实**（解析层无字段、审计零命中、动作已执行过 11 次），**不自行下任何修法判断**（改不改 `InboundMessage`、怎么改、要不要现在派，全部作为选项交 §四 #53），亦未触碰 #270 行一个字。

**⑵ 三.5 重读比对结果**：

- acquire 时读到队列 **187 行**，高水位线 §一 #281／§四 #53（＝本次预留后回写值）。
- 写盘前**再次重读**：仍 187 行，#271-#278 八行状态列均仍为「待领」，**无自己没写过的新增行**，无撞号，故**无需并入、无需按协议〇.6 续排**。
- 写盘后与写前快照 `diff`：**恰好 8 行修改 ＋ 4 行新增**，无其它差异。
- **acquire 回显提示近 120 分钟内另有身份持锁**（企微机器人 13 次、sweep-commit 2 次、`CC-aibot-chatid-fix` 17 分钟前 1 次）。`CC-aibot-chatid-fix` 即 #270 那条 CC session，其新增的 #270 行在我 acquire 前已落盘、已被读到，未构成冲突。

**⑶ 本轮实际拿到的编号**：`--reserve-multi 一:2 四:1` → **§一 #280、§一 #281；§四 #53**，三个全部用满，无留空。高水位线已由工具同步回写。

**⑷ 一次未生效的操作，如实记录**：写盘前曾尝试**二次 acquire** 追加预留一个 §四 号（想把两个子决策拆成两行），命令 **120 秒超时被中断**。事后核验：锁仍为第一次 acquire 的记录（`held_since` 未变、note 未变），高水位线仍为 §一 #281／§四 #53，**未产生任何副作用**。遂改为把两个子决策合并进 §四 #53 一行、以 ①② 分列，各自带 (a)/(b)/(c) 与默认项，仍可分别作答。

**⑸ 本班为非交互班次**（定时任务触发，无本机 PowerShell 通道）：

- **sweep** 未主动触发，由 `ZhuopinCommitSweep` 周期（1 小时，#189）自然取活。批次 `B-0806_拆件巡逻271-278与chatid取证` 已按规范登记 §二（状态列「待处理（登记，待 sweep 落库）」，不含 ✅，文件清单为完整路径，无裸竖线、反引号成对）。
- **决策提醒检查**（`decision_reminder_check.py`）同因无本地 shell 与出网未执行。本轮新登 §四 #53 与一条 P2 待领行，将由 #219 双载体兜底（`ZhuopinDecisionReminderDaily` 08:30 ＋ sweep 起跑段每小时）。
- **release 已成功**（`✓ 已释放`），#225 四项结构校验未报任何拒绝。

---

## 三、live 首周逐日抽查

**live 首周逐日抽查中，出现一次错写即退回 dry-run。**

---
---

# 拆件巡逻报告 · 2026-08-06（**第二班**）

**生成时刻**：2026-08-06 13:2x 本地（05:2x Z）。**非交互班次**（定时任务触发）。

**扫描范围**：§一 文内真实可见最大号 **#285** 与顶部编号高水位线 **#285**（acquire 前值）取较大者 → **#285**。
**识别**：状态为「待领」且含「企微反馈自动归档」的行 **4 行（#271／#272／#273／#280）**。
**已拆**：4 行全部销行。**新增**：§一 #286、#287；§四 #53（重登）。

> **🔴 本班最重要的产出不是这 4 行，是发现第一班（09:19）的全部队列改动已被整体抹掉。** 详见下方 §二。

---

## 〇、待发信盘点（只读只提醒，本班未发送任何信件）

两态分别计数（README 表格实读，只取表格行、不计正文中描述机制的字样）：

| 态 | 条数 | 明细 |
|----|------|------|
| `🆕 待发` | **0** | 第一班盘到的 2 封均已消化：**财务部#11** 已于 **2026-08-06 01:30 UTC（09:30 本地）** 由 `ZhuopinFollowupDispatchDaily` 自动推送（README 已回填「✅ 已推送」）；07-29 那封采购信在 README 表中已非 `🆕 待发` 态 |
| `⏳ 待你审` | **1** | **IT部#7 · 2026-08-05 · 陈承**「企微自建应用凭据申请 ＋ 一个 AP 附件下载异常」——已候审 **1 天** |

无积压告警级情形；`⏳ 待你审` 那封等你审批后才会进入自动发送通道。

---

## 一、#271／#272／#273／#280（四行同批合并读取）

| 项 | 内容 |
|----|------|
| **行号** | §一 #271／#272／#273／#280 |
| **文件** | `7-外部文档/待分拣/待分拣-ShaoPeiShen-回复-2026-08-06-文本反馈-{865f67e0…,f8421cf2…,08189516…,dc23c0ee…}.md`（4 件，均已通读全文） |
| **内容一句话** | 单行文本：`@邵总数字人🤖` 后接 **财务测试短信／质量测试短信／跨部门测试短信／采购测试短信**。无业务内容、无口径诉求。 |
| **已执行动作** | 4 行销行（状态列以 `✅ 已拆件（2026-08-06 拆件巡逻）` **开头**）；**#279 事实回灌**（采购部 chatid）；新增 §一 #286、#287；重登 §四 #53。 |
| **需 Shao Peishen 项** | **有 1 条 → §四 #53**（详见下方）。 |

### 判断依据

**⑴ 与已确认的 #274-#278 同族同批，处置一致。** 四条与那五条是同一批采集动作（08:08-09:25 之间），措辞体例相同，`sender` 同为 `ShaoPeiShen`，非业务反馈，无需回灌。

**⑵ 其中 #280 那条有额外价值——它解掉了 #279／#270 的唯一卡点。**

- #279 行原文写明：**采购部 chatid 仍未采集**，是 #270 完全收口（含 webhook 下线）前最后一个缺口。
- #280 对应的消息发于 **2026-08-06 09:25:40 本地（01:25:40Z）**，**晚于** #279 那次 chatid 捕获补丁部署到常驻服务，故 `reports/wecom_aibot_audit.jsonl` 的该条 `archived` 事件**已带 `chatid`／`chattype=group`**。
- **采购部群 chatid ＝ `wrvDL_DAAAH7IL6I7hsgszeyQvmyjw-Q`**。
- **两条独立证据互证，不是单点推断**：① 该条正文自带「采购」部门标签，与财务／质量／跨部门三条的标注方式完全一致；② 08:32 那轮四条无标签测试短信恰好落在**四个互不相同的 chatid** 上，其中三个已由 08:35 的带标签消息确认为财务／质量／跨部门（与 `department_group_chatid_mapping.yaml` 现有值**逐字符相符**），**剩下的唯一一个正是本值**——排除法与标签法结论一致。
- **本班只回灌事实、不代填 yaml、不代下线 webhook**（那是 #279 行内 CC 的动作），并已在 #279 行内注明「真实发送验证不因此免除——发送验证仍是坐实归属的最终判据」。

**⑶ 顺带取证：今日 12 条归档件只生成了 9 行队列行，3 行真实丢失。**（→ 已立 §一 #286）

- 审计 `archived` **12** 条、`queue_appended` 也是 **12** 条，队列 §一 却只有 **9 行**（#271-#278、#280）。
- 缺的 3 条：`7bb79f269cad…`（08:08:12）／`0ee7b1cbe5f5…`（08:32:16）／`69d75cb0c1ac…`（08:32:18）。**内容经通读均为「@邵总数字人🤖 测试短信」，故本次零业务损失。**
- 三条的暂存记录躺在 `reports/pending_queue_appends.jsonl`（**git 同步层**的暂存文件），`error` 文本是「队列文件编辑锁占用中…」，持锁方标注「企微机器人（git冲突重算追加）」。
- **缺口在这里**：#168 建的锁忙补录机制（`queue_lock_pending.flush_pending_queue_appends`，每条新消息触发）读的是**另一个文件** `pending_queue_lock_appends.jsonl`；**git 同步层那个暂存文件没有任何自动 flush 通道**。⇒ 同一种失败（锁忙）从两层抛出，只有一层会被补录。
- **实证**：09:25 确有新消息到达并成功追行 #280，若这三条落在锁暂存文件里本该被顺带补录；它们至今仍在 git 暂存文件里、队列里也仍无对应行。
- **⚠️ 更该记的一点**：当时确实发过 **4 次** `queue_sync_degraded` ＋ `queue_sync_alert_sent` 私信告警。**告警发了、账仍没人补**——因为告警只说「一行待人工核对合并」，没说去哪个文件捞哪一行。**属「有告警 ≠ 有闭环」，是 #147『狼来了』的反面：不是没喊，是喊了之后没有承接动作。**
- **本班只登记事实与建议，未下任何修法判断**（改哪一层、异常怎么分类，全部作为选项交 CC，并已注明若改 `sync_after_archive_with_retries` 的异常分类口径须走 openspec）。

---

## 二、🔴 第一班（09:19）的全部队列改动被整体抹掉（→ 已立 §一 #287，P1）

**这不是推测，是逐项实测的结果。**

**第一班报告仍在磁盘**（本文件上半部分，mtime 09:19 本地），其「巡逻自检」节自述：`--reserve-multi 一:2 四:1` 取到 §一 #280／#281、§四 #53；写盘前重读比对无冲突；写盘后 diff 为「**恰好 8 行修改 ＋ 4 行新增**」；**`release` 成功且 #225 四项结构校验未拒**。

**但这些改动现在一处都不在**：

| 第一班自述的产出 | 第二班实测 |
|---|---|
| #271-#278 八行销行 | **#271／#272／#273 仍是「待领」**（本班重新销行）；#274-#278 那五行的 ✅ 措辞出自 CC 的 `c852883`（09:01），**不是第一班的成果** |
| 新增 §一 #280 | **该号已被 09:25 的机器人自动追行占用**（commit `4907dd7`）——即**同一个号被二次分配** |
| 新增 §一 #281 | **不存在** |
| 新增 §四 #53 | **不存在**（§四 高水位线至今仍停在 **#52**，即其 §四 取号回写也被回滚） |
| §二 批次 `B-0806_拆件巡逻271-278与chatid取证` | **不存在** |
| 报告文件本身 | **`git status` 里是 `??` 未跟踪** —— 批次行随队列一起被抹掉，**09:36 那轮 sweep 只补销了 B-0805 的遗留尾巴、根本看不到这批**，报告至今未入库，属 #236(2) 定义的孤儿 |

**时间窗已收敛到 6 分钟**：09:19（第一班写盘＋release）→ **09:25:55 `bot(队列): 自动追行 #280`（`4907dd7`）** → 09:36 sweep。**该窗口内唯一触碰队列文件的执行体就是机器人那次追行**，且它正是 §一.⑶ 里那次「冲突重算遇锁忙」的**同一次执行**。

**⇒ 高度怀疑**：git 同步层的冲突重算路径为拿到「最新内容」而对队列文件做了 reset／checkout 类操作，把工作区里第一班尚未提交的改动一并丢弃——**这正是协议〇.8 当初为 `git checkout --` 立规矩要防的那类事，只是这次执行方不是人、是机器人，因而没有任何一条「先核对 §二」的人守规则能拦住它。**

**🔴 但根因未坐实，本班不断言**：同窗口另两个理论候选（sweep 的 pull 路径、并发 session 的整文件写回）本班无法排除，已在 #287 行内写明须 CC 用真实代码路径复现后再改码。

**为什么定 P1、为什么比 #286 严重**：#286 丢的是机器人自己的行，且发过 4 次告警；**本次丢的是人类 session 已 `release` 的成果，零告警、零留痕**——第一班的 release 成功回执与 diff 校验全部通过，**它到收工那一刻都认为写入生效了**。这是 CLAUDE.md §5「工具静默回退」在协作层的形态：**锁与结构校验都正常工作，只是它们保护的那份文件在其之外被另一个进程整体替换。** 本次靠第一班报告侥幸留在磁盘才被发现；**若那份报告也一并没了，这次事故将完全无痕。**

**本班的补救（不代替修复）**：四行重新销行、#279 回灌、§四 #53 重登。**第一班 §四 #53 的第 ① 项（三部门 chatid 你手上是否已有）已因本班取到采购部 chatid 而实质失效，不再重提**；第 ② 项为尚未答复的真实决策项，已重新取号登记。

---

## 三、需 Shao Peishen 项（本班 1 条，已登 §四 #53）

**§四 #53 · 是否把你本人（`ShaoPeiShen`）补进 `department_mapping.yaml`** —— **【总线答】**

- **(a)** 补入映射表并指定归档目的地（此后你发的消息归到指定部门目录、**仍会建队列行**）
- **(b)** 补入映射表但**只归档、不建队列行**（需一处小改动；今日这类噪音归零，但你若真有事要走队列得另说一句）
- **(c)** 维持现状（零改动；代价＝你每发一条消息就多一行待拆件，下次再采集 chatid 或做连通性测试会重演今日的 9 行）

**默认项：若不答，按 (c) 执行。** 不卡时间。

> 标注说明：本项由本次巡逻自己提出，故不标任何「回本 session 答」类标签；因牵动 #270／#286／#287 三个编号并需派 CC，按判据标 **【总线答】**。

---

## 四、巡逻自检（第二班）

**⑴ 本轮有无预案覆盖不了的情形**：**有，一处，且是本 prompt 的预案空白。**

- 本 prompt 的预案覆盖「锁冲突降级」「非交互班次无 shell」等，**但没有覆盖「上一班的改动已被抹掉」这一形态**——因为它此前没发生过。**处理方式**：只登记**已实测的事实**（哪些产出不在了、时间窗多长、窗口内有谁动过文件），**不自行下根因判断**（是谁抹的、怎么改，全部作为待验证项交 CC），亦未触碰机器人代码一个字。
- **另一处边界说明**：把采购部 chatid 值写进 #279 属「回灌**事实**」——该值来自审计留痕与归档件正文两处真实证据，不是新口径判断；且已明确注明真实发送验证不免除。

**⑵ 三.5 重读比对结果**：

- 两次持锁窗口，每次编辑前均从磁盘重读目标行、按**行级定点替换**写回（未做整文件覆盖）。
- 第一次 acquire 时文内可见行号已含 **#282／#283／#284／#285**（`Cowork-财务专线-0806` 于约 120 分钟前新增，**我初次扫描时尚不存在**）——**属"自己没写过的新增行"，已原样保留、未覆盖**；本班取号 #286／#287 与之不撞，无需按协议〇.6 续排。
- 写盘后全表列数复核：§一 全部 8 列、§二 批次行 4 列、§四 全部 4 列，**无异常**。
- **acquire 回显近场者**：第一次为 `Cowork-财务专线-0806`（120 分钟前）；第二次为本班自己（7 分钟前）。

**⑶ 本轮实际拿到的编号**：两次 acquire ——
- 第一次 `--reserve 2 --section 一` → **§一 #286、#287**（高水位线 #285→#287），**两个全部用满**；
- 第二次 `--reserve 1 --section 四` → **§四 #53**（高水位线 #52→#53），**已用满**。
- 分两次取号的原因：第一次取号时尚未发现第一班被抹（那是写 #287 过程中查证出来的），因而没有预留 §四 号；**未做手工 +1**，而是 release 后重新 acquire 取号，符合协议〇.7。

**⑷ 本班为非交互班次**（定时任务触发，无本机 PowerShell 通道）：

- **sweep** 未主动触发，由 `ZhuopinCommitSweep` 周期（1 小时，#189）自然取活。批次 `B-0806_拆件巡逻回灌与队列行丢失取证` 已按规范登记 §二（状态列「**待处理**（登记，待 sweep 落库）」，不含 ✅；文件清单为完整路径；无裸竖线、反引号成对）。
- **⚠️ 本批次的文件清单里特意带上了第一班那份 `??` 未跟踪的报告**——否则它会继续当孤儿躺着。
- **决策提醒检查**（`decision_reminder_check.py`）同因无本地 shell 与出网未执行；本轮新登 §四 #53 与两条待领行（含一条 P1），由 #219 双载体兜底（`ZhuopinDecisionReminderDaily` 08:30 ＋ sweep 起跑段每小时）。
- **release 已成功**，#225 四项结构校验未报拒绝。

---

## 五、live 首周逐日抽查

**live 首周逐日抽查中，出现一次错写即退回 dry-run。**
