---
status: 已执行归档
title: "派单件【CC】FI2 面板发票段翻倍修复（§一 #371）＋ R5 分母判例对照表（§一 #375）"
编号: OP-0823-E
派出线: Cowork 业务总线（OP-0823-D）
日期: 2026-08-23
---

> 🔴 **已执行完毕，勿再据此开工（2026-08-26 `OP-0826-J`，队列 §一 #396 九份普查改对）** —— **类别⑴：关联队列行已迁归档（#397 能拦）**。判定依据：§一 `#371`／`#375` **两行均**已在 `跨桌任务队列-归档-202608.md` §一 任务看板·业务场景节（L584／L585），状态列均为 `[S:done][D:业]`——`#371` 面板翻倍已修复并在生产验证（2026-08-24，CC worktree `op0823e-fi2-dup-and-r5`，commit `2b81c94`＋`6035683`，`git rev-list --count master..分支` ＝ 0）；`#375` R5 全量实测完成、结论为「不做对照表」（派单件预写死的那一支）。
> 🔴 **口径提醒（给 §四 #117 决策点 6(b)）**：`#396` 立行当时（2026-08-25）把本件判为**类别⑵**（「本轮 A1 刚执行完、队列行状态未必同步，#397 无效」）；而 2026-08-24 值周清扫已把这两行迁入归档 ⇒ **今日实测本件属类别⑴**。**类别归属会随值周清扫窗口（最长 7 天）漂移，不是文件的固有属性** —— 告警判据若按类别分流，必须能容忍同一份件在两类之间移动。

# 派单件【CC】#371 面板翻倍修复 ＋ #375 R5 分母对照表

> **承接**：`财务部#14` 回件已于 2026-08-22 到达并拆件回灌完毕（`OP-0823-D`），**串行闸已开**。
> 本件把财务域两条最紧的活打成**同一个 CC 轮次**：一条是她眼前正挂着错数字（`#371`，P1），一条是我方对她的书面承诺（`#375`，本周内）。
> 🔴 **两条都必须在 LAN 内做**（真实 U9C 取数 ＋ `.51` 部署），故同车。

---

## 〇、开场词（复制即用）

▶ **粘贴端：CC（Claude Code Desktop）**

```
FI2翻倍与R5分母-OP0823E
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建 ｜ 分支：claude/op0823e-fi2-dup-and-r5 ｜ 工作区：C:\Users\Paul Shao\OneDrive\Projects\企业AI转型 ｜ 派出线：Cowork 业务总线 OP-0823-D
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"）把会话名设为「FI2翻倍与R5分母-OP0823E」；该工具延迟加载，须先 ToolSearch 取 schema。
读 ① 根 CLAUDE.md 当前进度段 → ② 本派单件全文 `1-转型规划/0-全景路线图/派单件-【CC】FI2面板翻倍修复与R5分母对照表-2026-08-23.md` → ③ 队列 §一 #371 与 §一 #375 全文。按 §二 先取证再动手，按 §五 验收，收工按 §六 回写。开干前问我 2-3 个澄清。
```

---

## 一、次序与并行

| | 任务 | 依赖 | 能否并行 |
|---|---|---|---|
| **A** | `#371` 面板发票段翻倍修复（P1） | 零依赖，**但须先取证再动手**（§二.1） | 与 B 可并行 |
| **B** | `#375` R5 分母判例对照表（补件） | 零依赖 | 与 A 可并行 |

**触碰区实测不重叠**：A 在 `4-数字员工/财务部/FI2-三单匹配自动对账/`（摄取与数据重建）＋ `.51` 部署；B 只产 `6-人才与组织/部门AI专员跟进/` 下的 md＋docx，不改任何代码。

🔴 **但如果只能做一件，先做 A** —— A 是「错数字正挂在她眼前、且她在等我方通知」，B 是「她在等一份我方承诺的材料」；两者都欠她，前者还在持续产生错误结论。

⚠️ **B 不被 A 阻塞，理由须写清免得误判**：`#371` 坏的是**发票段**（行级重复导致数量/金额翻倍），而 R5 分母算的是 **PO 侧未税金额**，两者不共用数据。**若你在取证中发现 PO/AP 侧也有重复行，立即停 B 并回报** —— 那样 B 的对照表本身就是错的。

---

## 二、A（`#371`）：先取证，不要直接采信队列行的推断

### 2.1 队列行自己写明了「本行是静态读码推断、真实数据未取证」

`#371` 的行内已备好**证伪命令**（在 `.51` 本机跑）：

```
python -c "import csv;rs=list(csv.DictReader(open('C:/fi2/app/data/tax_export/invoice.csv',encoding='utf-8-sig')));k=[(r['inv_no'],r['ap_no'],r['item_code']) for r in rs];print(len(k),len(set(k)))"
```

- **总行数 > 去重行数** ⇒ 坐实行级重复，按 §二.2 修。
- **两数相等** ⇒ **本行推断不成立**，🔴 **请改判该行、另找翻倍成因，不要勉强按原方案修补**（她的举证 `AP-2026070036·行10` 数量 42,000／不含税 145,320 恰为真实发票两倍，是硬事实；错的可能是我方对成因的判断）。

### 2.2 三件事，缺一件都不算修完

1. **按发票号重建已入库数据** —— 🔴 **她已经把重复文件删了，而重跑修不好**：重复行早已写进库，重跑只会 skip 那个文件。
2. **给摄取加行级去重** —— 防再次发生。
3. **重跑后复核，再通知她** —— `财务部#14` 已明确请她「在我方通知前不要采信发票段数字」，所以**必须有一次明确的解除通知**（走 §六 的信件流程，不要口头）。

### 2.3 用她的原始举证做端到端复现

按发送硬前置的判据，修完须**用 `AP-2026070036·行10` 这个真实案例复现**，看板须显示修正后的值（不是「测试通过」，是那一行的数字对了）。

---

## 三、B（`#375`）：R5 分母判例对照表 —— 形态与内容都有硬约束

### 3.1 要问的是什么（她已经用文字答过一次，失败了）

design D14 Open Question 问的是：**R5 比例的分母到底取哪一个？**

- **读法甲**：这张 AP **引用到的那几行 PO** 的合计未税金额（现行实现，`_po_untaxed_by_ap` 按 `ap_no` 关联 `(po_no, line_no)` 精确求和）
- **读法乙**：**整张 PO 单全部行**的合计未税金额

她两次给的都是同一句「比例分母 ＝ 整单（PO 行）合计未税金额」——**这句话本身同时容得下甲乙两读**。`财务部#14` 已向她说明「这件事本来就不该用文字问」，所以**不得再用文字问第三遍**。

### 3.2 形态硬约束

- **补件形态**，先例 ＝ `6-人才与组织/部门AI专员跟进/财务部-唐燕萍-补件-2026-08-20-第13封改发Word版.md` —— **不占新信编号、不触发串行闸**。
- **md ＋ docx 双件**（`0-学习与工具/md转Word工具/md2word.py`，`--org 卓品智能科技`）。🔴 **docx 必发**：`财务部#13` 只发 md，她反馈「AI 不好编辑回复」；纯文本她无法勾。
- **判例批改法**：她只勾一列、不写字。
- 🔴 **判据类永不默认生效**（IATF 显式签认红线）—— 表内**不得**出现「不回复即按 X 执行」。

### 3.3 🔴 内容上最容易做废的一步：样本必须刻意挑「两种读法会打架」的单子

**默认做法会产出一张零信息量的表。** 若随手抽 10 张真实 AP，多数单子甲乙两种分母算出的比率虽不同，但**落在同一个分流档位**（L2 自行消化 / L3 转人工），于是表里每一行「分流结果」列都写「一样」——**她看完不知道该勾什么，因为看上去两种都行**。这正是本项目记过的「判据恒真、零信息量」那一族。

**⇒ 要求**：

1. 先在真实数据上**全量算一遍**甲乙两种分母，统计**共 N 张 AP、其中 M 张两种读法的分流结果不同**。
2. 对照表里 **≤10 行**，**优先放那 M 张**（分歧样本）；若 M 为 0，🔴 **不要硬凑表** —— 那意味着这个歧义在真实数据上不产生任何差异，应当如实告诉她并直接按现行实现落档，同时把 N/M 的实测数字写进队列 `#375`。
3. 表内如实写出 N 与 M（她需要知道这 10 行代表多大范围）。

**每行至少五列**：AP 单号 ／ 甲分母与比率 ／ 乙分母与比率 ／ **甲的分流结果** ／ **乙的分流结果** ／ 她勾选的那一列。

### 3.4 顺带说清楚的一句（写进补件，避免她再答一次同样的话）

`财务部#14` 已向她致歉过一次「催了不该催的东西」。**本补件不要重复索取任何她已交付的东西** —— R1–R6 规则草案（2026-07-10 已定稿）与暂估价阈值 30%/50%（她 2026-07-14 已确认）**都不要再提**。

---

## 四、发送前的两道闸（两件都适用）

1. 🔴 **未经 Shao Peishen 审核不得发送** —— 起草、转 docx、README 登记都可以先做，**发送必须等他点头**。
2. **A 的通知信另有硬前置**：改动须已 ff 合入 `master` ＋ `.51` 已部署且冒烟通过（`/api/ping`／关键页 200／一次全量重算）＋ 用她的举证案例端到端复现 —— **三条全过才发**。B 是补件、不涉及部署，不受此条约束。
3. **串行闸**：`财务部#14` 已转 `📥`，闸开；**A 的通知信若要占新编号，即 `财务部#15`** —— 但 `财务部#15` 已被规划承载 `fi2-source-inversion` 的两三个决策点（Shao Peishen 2026-08-23 定：**等 design 审完再起草**）。⇒ 🔴 **A 的通知信优先做成补件形态**（不占号、不触发闸），把 `财务部#15` 的号留给 design 审后那一封；确需占号请先回报。

---

## 五、验收

**A**：① 证伪命令已实跑并记录两个数字；② 数据已按发票号重建、摄取已加行级去重（带回归测试）；③ 用 `AP-2026070036·行10` 端到端复现、看板显示正确值；④ 已 ff 合入 master ＋ `.51` 部署冒烟三件套全绿；⑤ 全量回归零漂移。

**B**：① 全量统计出 N（总 AP 数）与 M（甲乙分流不同的张数）并写进队列；② 对照表 ≤10 行且优先取分歧样本（或 M＝0 时如实回报不硬凑）；③ md ＋ docx 双件已产出；④ README 已登记补件行；⑤ **未发送**（等审）。

---

## 六、收工回写

1. **队列**：`§一 #371`（A 的结论，含证伪命令两个数字）、`§一 #375`（B 的 N/M 实测与产出路径）；`git rev-list --count master..<分支>` ＝ 0 的取证一并写入。
2. **README**：`6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` 补件行追加；**发送状态列只写 `⏳ 待你审`**（唯一合法起草产物，不得直接写终态）。
3. **§二 批次**：一批一行，状态列**不得含 ✅**；须写「本批变更参数：…」与「本批新入法：…」。
4. **收工重跑台账** ＋ `git commit` ＋ `push`。
5. 🔴 **给 Shao Peishen 的收工报告须按判据分栏**：⟨就地答⟩ 的给字母选项，需另起 session／对外发送的只写「已登记 §N，待总线派发」。

---

## 七、本件未做、也不要顺手做的事

- **不碰 `fi2-source-inversion` 变更包**（停在 design 审，四个 Open Question 归 Shao Peishen）。
- **不建群、不改推送、不改 `.env`**。
- **不直接与唐燕萍往返**（对外发送一律经 Shao Peishen 审后走机器人）。
- **不动 `§四 #59`**（税务导出投放恢复）—— 但你在 LAN 内，🔴 **顺手看一眼 `.51` 的 `D:\airead` 最新文件 mtime 是否已越过 2026-08-10 13:11，或 `Fi2TaxExportDailyScan` 的 `LastTaskResult` 是否已由 2 回落到 0，把结果回填 §四 #59** —— 这是一条命令的事，而这条取证已经卡了两次（两次都因为人不在网段）。
