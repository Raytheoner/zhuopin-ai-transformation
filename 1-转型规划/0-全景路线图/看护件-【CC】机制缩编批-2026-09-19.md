---
title: "机制缩编批看护件 · opener 双点机守 ＋ 零命中断言守 ＋ 陈旧锁哨兵"
created: 2026-09-19
status: 🟢 无头批待起（`工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -Detach`，Shao Peishen 2026-09-19 答 `6a` 解冻 ＋「周末一般 off-LAN，优先机制类 task 快速缩编」）
产出: Cowork 泳道看护 OP-0919-A
批次: B-0919_机制缩编
源: 他 2026-09-19 答 `3b`（单开机制清池批理清前置）→ `4a`／`5a` 定形态 → `6a` 解冻 `#620` ＋ 周末口径「优先机制类快速缩编」
用途: 两条泳道并行（`opener` 内 A1→A2 串行、`gitguard` 单条）；§三 即 `-Plan` 解析格式，默认后台无头起、不给人粘。
---

# 机制缩编批看护件（2026-09-19）

## 〇 · 本批要解决的一句话

**这三条的前置今天全部清完了——形态已由他拍定、判据已写死，剩下的纯粹是建造。**

- `#620` opener 生成链双点机守：落地形态他 2026-09-18 已写死，无待决项；今日 `6a` 解冻 WIP 约束。
- `#616` worktree 对 gitignore 失明：他 2026-09-19 答 `4a` ⇒ **③ lint 硬关卡 ＋ ① 生成器软提示**，② 只读绑定不做。
- `#618` 陈旧锁哨兵：他 2026-09-19 答 `5a` ⇒ **② 并进 `工具-worktree体检.ps1`，阈值 30 分钟**。

🔑 **三条同属一句判词的不同出口**：**一个被接受却不生效的参数、一把没人看的锁、一次没人复核的「零命中」——都是「报了成功、其实什么都没发生」。**

## 一 · 硬边界继承（看护者读，泳道 opener 不复述）

- D1 四档判据**现取**：`python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不抄判据表。
- 本批**全部 🟢 档**（worktree 内本地建造）。命中 🟡/🔴 跑 `pause` 并以 `OPENER_PARTIAL` 收尾；命中 ⏭️ 跑 `transfer-out` 后跳过继续。
- LAN 探针现取 **off-LAN**；本批三条全在库内，不依赖内网。
- 机制类可动 WIP 现取 29／22 超限，**本批三条经他 `6a` 与「优先机制类快速缩编」口径显式解冻**，不援引为常设放宽。

## 二 · 次序与并行矩阵

| 段 | 泳道 | 队列行 | 触碰区 | 硬序 |
|---|---|---|---|---|
| A1 | `opener` | §一 `#620` | `0-学习与工具/工具-opener生成.py`｜`0-学习与工具/工具-opener块lint.py`｜`1-转型规划/0-全景路线图/opener骨架.md`｜两者的 `test_*.py` | 先 |
| A2 | `opener` | §一 `#616` | 同上三件（同文件，必须串行） | 后 |
| A3 | `gitguard` | §一 `#618` | `0-学习与工具/工具-worktree体检.ps1`｜`0-学习与工具/工具-泳道分支合入.ps1`｜对应 `test_*.py` | 与 `opener` 泳道**并行** |

🔴 **A1／A2 同触碰 `工具-opener生成.py` 与 `工具-opener块lint.py` 两件，必须同泳道串行**；`gitguard` 与它们零交集，可并行。并行上限 4，本批用 2。

⚠️ **与在跑批次的关系**：`B-0919_Q2建造` 的 A2（Q2 门户页）另行 `-Only` 重起，触碰 `4-数字员工/质量部/`，与本批零交集。

## 三 · 泳道 opener（`工具-opener批处理执行v2.ps1 -Plan` 解析段，人不碰）

🔴 **本节必须排在 `## 三bis` 之前**（`工具-opener块lint.py` 形态⑥ 结构锚）。
🔴 **`### A<N>` 标题必须带 `§一 #<行号>`**（`工具-opener派出前校验.ps1` 靠它现查队列态）。

### A1 · §一 #620 opener 生成链双点机守

粘贴端：CC ｜ 泳道：opener ｜ 队列行：§一 #620 ｜ OP：OP-0919-F

```
[OP-0919-F]【CC】opener双点机守
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919f-opener-guard-620`） ｜ worktree：☑（op0919f-opener-guard-620，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A（批 B-0919_机制缩编） ｜ 模型：sonnet
读 ① `队列 §一 #620` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919f-opener-guard-620 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919f-opener-guard-620 --done --batch B-0919_机制缩编 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · §一 #616 零命中断言守

粘贴端：CC ｜ 泳道：opener ｜ 队列行：§一 #616 ｜ OP：OP-0919-G

```
[OP-0919-G]【CC】零命中断言守
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919g-ignore-blind-616`） ｜ worktree：☑（op0919g-ignore-blind-616，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A（批 B-0919_机制缩编） ｜ 模型：sonnet
读 ① `队列 §一 #616` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919g-ignore-blind-616 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919g-ignore-blind-616 --done --batch B-0919_机制缩编 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · §一 #618 陈旧锁哨兵

粘贴端：CC ｜ 泳道：gitguard ｜ 队列行：§一 #618 ｜ OP：OP-0919-H

```
[OP-0919-H]【CC】陈旧锁哨兵
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0919h-stale-lock-618`） ｜ worktree：☑（op0919h-stale-lock-618，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A（批 B-0919_机制缩编） ｜ 模型：sonnet
读 ① `队列 §一 #618` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919h-stale-lock-618 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0919h-stale-lock-618 --done --batch B-0919_机制缩编 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

---

## 三bis · 看护者开场词（仅在退回「给他一块粘」时使用；默认走 §三 无头）

```
[OP-0919-E]【CC】看护Q2建造
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建；各子泳道声明 ☑ 后由 v2.ps1 脚本在起 claude 前建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A（批 B-0919_机制缩编） ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0919E-看护Q2建造。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】机制缩编批-2026-09-19.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

---

## 四 · 全程红线与收工

1. 🔴 不碰 `.51`、不部署、不碰真实库、不碰企微机器人与定时任务。
2. 🔴 **不得把守卫做成「只会报成功」的绿灯**——三条行的共同成因就是这个；每条守必须有一个能让它当场变红的回归用例。
3. 🔴 `#620` 的两点守**缺一即逃逸**：手写 opener 不经生成器、批处理不经 lint，只守一点必漏。
4. 🔴 `#618` 清锁三条判据不变：`Get-Process git` 为零 ＋ 锁文件 0 字节 ＋ 陈旧超 **30 分钟**，三条齐备才动手；**有 git 进程在跑时误清会真的损坏仓库**。
5. 🔴 不 ff master（🟡 档），只 push 本泳道分支；不在主仓 commit，走 §二 批次 ＋ `ZhuopinCommitSweep`。
6. 🔴 **不长持队列编辑锁**：取锁→改→立刻放，撞锁不重试，按「登记待补」写队列行（本日 `B-0919_Q2建造` 已实撞一次，见 `#611`）。

**收工汇总**：跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0919_机制缩编` 现取，不手工数；等待时长只认 `reports/lane-watch-state.json`。

## 五 · 已知会踩、但不是本批的错

**第三道闸的 `main-leak` 误判有两个来源**（`#611`）：① 泳道按协议〇 回写主仓队列行；② 🆕 **看护者本人同时段在主仓写的文件**也会被算进泄漏清单（2026-09-19 `B-0919_Q2建造` 实撞，A1 因此被判 FAIL、排在其后的 A2 根本没起）。⚠️ 本批若报 `FAIL(main-leak)` 且泄漏清单只有队列文件或看护者自己的产出，**按误判处理**——读泳道日志尾部的哨兵，不读 summary 的 Status 列。

**`git worktree add` 撞「branch already used」**：泳道收工不自删 worktree 是常态（`#576`），`工具-泳道分支合入.ps1` 撞上时只报 `exit=9 脚本异常`、**不报成因**（2026-09-19 实撞一次，手动 `git worktree remove` 后即通过）——这与 `#618` 要修的「ff 失败不报锁成因」是同一处的两个面，A3 泳道可顺手一并补。
