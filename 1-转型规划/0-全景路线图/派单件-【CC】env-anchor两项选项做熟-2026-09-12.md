---
title: "派单件 · 【CC】env-anchor-collapse 剩余两项·把选项做熟"
created: 2026-09-12
status: 生效
用途: 只把两项 🟡 的选项与代价做熟成可直接作答的问句，不落地、不切 enforce
派出线: Cowork 环境总线 OP-0912-E（批 B-0912_env-anchor）
---

# 派单件 · 【CC】`env-anchor-collapse` 剩余两项·把选项做熟

## 〇、由来

该包 tasks 只剩三条未勾，其中 `5.3`（暂不归档）是结论、不是活；真正卡着的是两项 🟡：`2.3.5`（2026-08-27 **刻意扣住的最后一跳**）与 `3.5.1`（切 `--enforce`，2026-08-27 判过「**仍不切，且理由变了**」）。**它们久挂的原因是没人把选项做熟到能一字母作答**——Shao Peishen 2026-09-12 拍：派一棒做熟再问。

## 一、做什么

1. 读 `openspec/changes/env-anchor-collapse/` 的 `proposal.md`／`design.md`／`tasks.md` 全文，**把 `2.3.5` 与 `3.5.1` 两条的上下文吃透**：当初为什么扣住、2026-08-27 那次「理由变了」变成了什么、扣住期间世界变了哪些（尤其 `#549`／`#553`／`#565` 之后 opener 链的变化）。
2. 对**每一项**产出一个可直接作答的问句，格式照 `1-转型规划/0-全景路线图/审读件-openspec-P1-归档前置校验6点-2026-09-12.md`（同日样板）：一句话问题／`(a)(b)(c)` 选项／**每个选项写清「选它会发生什么」与代价**／〔荐〕／🔴 **标「本项无默认」**（两项均属改口径判据 🟡 档）／**推荐理由的手段**（读了哪个文件哪一段、跑了哪条命令、现取到什么数）。
3. 🔴 **必须现取，不得引用历史结论**：`2.3.5` 的「最后一跳」现在还成不成立、`3.5.1` 切 `--enforce` 现在会红几处——**都要现跑一遍拿数**（如 `--enforce` 全库现跑的违规条数与分布），把「2026-08-27 的理由」和「今天的事实」并列写。
4. 产出 `1-转型规划/0-全景路线图/审读件-openspec-env-anchor两项-2026-09-12.md`，末尾给一个可一键复制的答复模板。
5. 队列回写：对应承接行追一段；§二 登记批次。

## 二、不做什么（硬边界）

- 🔴 **不落地、不切 `--enforce`、不动那「最后一跳」**——本棒只做熟选项。
- 🔴 **不改 `design.md`／`proposal.md` 一个字**；`tasks.md` 只允许在两条目下追一行指向本审读件，**不代勾**。
- 🔴 **不 ff master**；收工只 push 本泳道分支。
- 🔴 **不立新机制行**（WIP 超上限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 不碰 `.51`／企微机器人／定时任务；不做任何对外发送。

## 三、收工报告按此产出

1. 两项各自的问句落点；2. 每项「2026-08-27 的理由 vs 今天现取的事实」并列；3. `--enforce` 现跑的违规条数与分布（现取，不引用）；4. 队列回写与 §二 登记；5. 顶格 `OPENER_DONE` 或 `OPENER_PARTIAL: …`。
