---
status: 在办
---

# 拆件巡逻报告 · 2026-09-20

## 事件驱动开班（〇ter）

`[SIGNAL]`：`check_patrol_signal.py` 报 1 条待处理，`财务部#19`（唐燕萍回件，2026-09-20T03:34:47Z）。执行完毕并登记 §二 批次后消费（`--clear --before 2026-09-20T03:34:47Z`）。

## 拆件行 · §一 #622／#623（同批到件，合并读取）

- **行号**：#622（文本反馈）、#623（docx 正文）
- **内容摘要**：唐燕萍对 `财务部#19`《FI2 三单核对：口径覆盖答复 ＋ 四条待你确认的口径》的回件。四条判例（J1-J4）全部作答，无遗留。
- **已执行动作**：
  1. 三条硬约束下通读全文（含表格外的说明段落）；docx 勾选栏用 python-docx 底层 XML（`w:sdt` 内容控件）逐格提取实际选中态——`cell.text` 表面读取会把选中态读成空白（勾选框嵌套在内容控件里），已交叉核验避免假阴性。实测：J1 ✅=☐ ❌=☐ **✏️=☒**；J2/J3/J4 均 **✅=☒**。
  2. J1（判例 7/8 互斥，原 §四 `#196` 待她定）：她选 (c)——拆但另加条件「只在两个料号同属一张 AP 单时才拆」，与信中预置措辞逐字一致，属既定选项确认、非新判断。§四 `#196` 已闭环，实现范围（`_ALLOW_INVOICE_ROW_SPLIT` 需收窄条件）并入 `#424`。
  3. J2/J3（`#224` FI2 R7 单价偏差三方向中的 (a)/(c)）：均确认 ✅。连同 (b) 已知豁免机制（2026-08-04 D19 拍板、承接行 `#251`），三方向签认完毕，`#224` 状态转交建造。
  4. J4（FI10 NRV 可变现净值——现行销售费用率是否已含出口费用）：确认 ✅，现状计算正确、无需代码改动；`FI10-NRV_ESTIMATION_BASIS` 台账点已由 `财务部#18` 签认在案，本条为确认性重申。
  5. 结论回写：§四 `#196`（闭环）、§一 `#424`（新增待建范围）、§一 `#224`（状态转交建造）、§一 `#593`（`财务部#19` 全流程收口摘要）；销行 `#622`／`#623`。README 转 `📥 已回件并回灌 2026-09-20`。追加登记 `#604`：串行闸复查已 ✅ 开（不代起草）。
- **判断依据一句**：四条全落在她已被授权拍板的判定口径范围内（其中 J1 为信中预置三选项之一的确认，非临场新判断），依 §〇 全部回灌落字，无需转 §四。
- **需 SP 项**：无。

## 台账回写

`points-by-letter --letter 财务部#19` ⇒ `[NONE]`，本信非台账投影，零回写（章程 §三.4bis⑵；`FI10-NRV_ESTIMATION_BASIS` 已挂在 `财务部#18` 名下，不因本信重复登记）。

## 待发信盘点（三态）

`⏳ 待你审` ×1（`采购部#24`，非本班触碰）／`🆕 待发` ×0／`⏸ 暂缓` ×0。交叉核未发现队列称"暂缓"而 README 仍"待发"的冲突项。

## 起草缺口检测

`python 5-平台底座/wecom-aibot-service/scripts/draft_gap_check.py --window-days 14` ⇒ 无缺口。

## 巡逻自检

- 预案覆盖不了的情形：无——J1 虽走 ✏️ 路径，但答案与信中预置选项 (c) 逐字一致，按"既定选项确认"处理，未升级为新判断。
- 三.6 比对结果：写回前后用 `--row --field all` 重读 `#196`/`#424`/`#224`/`#593`/`#622`/`#623`/`#604`，未发现他人并发新增行；acquire 时高水位线 §一 #623／§四 #217，本轮未取新号，无撞号。
- 本轮实拿编号：无新取号（全部回写既有行）；§二 追加 1 行 `B-0920_622-623拆件财务部19四点签认`。
- 并入审核结论：未新建 §一/§四 行，无需并入审核。
- 〇bis 跳过项：本班持有交互工具（Bash/PowerShell），未按 〇bis 跳过任何步骤；起草缺口检测已跑（见上）。
- 〇ter 探测结果：`[SIGNAL]` 1 条（财务部#19），执行完毕、§二 批次登记后已消费（fail-open 未触发）。
