---
title: "看护件 · 泳道看护批 B-0912_env-anchor"
created: 2026-09-12
status: 生效
批次: B-0912_env-anchor
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_env-anchor`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴。** 授权原文：Shao Peishen 2026-09-12 回 `2a`（`OP-0912-E` 报告定夺 2(a)：派一棒把 `env-anchor-collapse` 两项的选项与代价做熟）。
🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。单泳道，前序批次均已收工，不叠跑。

## 一、硬边界继承

- 🔴 **不落地、不切 `--enforce`**；只做熟选项。
- 🔴 **不 ff master**；收工只 push 本泳道分支，主仓 ff 属 🟡 档、经登记处交 `工具-待合分支巡检.ps1` 机器做。
- 🔴 不碰 `.51`／企微机器人／定时任务；不做任何对外发送。
- 🔴 **不立新机制行**（机制类可动 WIP 超上限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 队列回写走 `工具-共享文档编辑锁.py`，撞 `.git/index.lock` 不重试。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`，命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与其他 |
|---|---|---|---|
| `env-anchor-ripen` | `env-anchor-collapse` 剩余两项（`2.3.5`／`3.5.1`） | 新建 `1-转型规划/0-全景路线图/审读件-openspec-env-anchor两项-2026-09-12.md`；`openspec/changes/env-anchor-collapse/tasks.md`（只追指针、不代勾） | 本批单泳道 |

⚠️ **额外注意**：🔴 **必须现取**——`2.3.5` 的「最后一跳」是否仍成立、`3.5.1` 切 `--enforce` 今天会红几处，都要现跑拿数，把「2026-08-27 的理由」与「今天的事实」并列写，**不得只引用历史结论**。

## 三、泳道 opener

### A1 · env-anchor-collapse 两项选项做熟（OP-0912-P）

粘贴端：CC ｜ 泳道：env-anchor-ripen

```
[OP-0912-P]【CC】env锚两项做熟
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912p-env-anchor-ripen`） ｜ worktree：☑（env-anchor-ripen，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_env-anchor）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】env-anchor两项选项做熟-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912p-env-anchor-ripen --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912p-env-anchor-ripen --done --batch B-0912_env-anchor --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-Q]【CC】看护看护env锚
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_env-anchor）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912Q-看护看护env锚。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 ``1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_env-anchor-2026-09-12.md`` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. 两项问句的落点；2. 「2026-08-27 的理由 vs 今天现取的事实」并列；3. `--enforce` 现跑违规条数与分布（现取）；4. 分支 commit 与 `merge-base --is-ancestor` 现取（**ff 不在本批内做**）；5. 顶格哨兵。
