---
status: 待发
title: "开场 prompt【Cowork】环境总线 · 接力交接（定长交接卡）"
created: 2026-08-23
更新: 2026-08-28（OP-0826-C 收工覆盖重写）
上一棒: OP-0826-C（跨 08-26 夜 → 08-28 晨）
派出线: Cowork 环境总线
---

# 开场 prompt【Cowork】环境总线 · 接力交接

## 一、开场词（复制即用，一次粘贴）

▶ **粘贴端：Cowork**

```
环境总线-接力OP-0828-N
【设置】执行环境：Cowork ｜ CC session：☐ 不适用 ｜ worktree：☐ 不新建（主工作区）｜ 分支：master ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线
读 ① 根 CLAUDE.md 当前进度段 → ② 本件全文 → ③ 1-转型规划/0-全景路线图/看护件-2026-08-28-可开工批.md（§一ter 取号劈分／§一quater OP-0828-I 正文／§一quinquies OP-0828-M）→ ④ 队列 §一 #423／#424／#425／#426，§四 #132。开干前问我 2-3 个澄清。
```

🔴 **仓库在 `C:\Dev\zhuopin-ai`**（2026-08-26 迁毕），**迁移观察期至 09-02**。

---

## 二、上一棒做完了什么

**三批波次跑完**：08-26 泳道波次 12 条（9 PARTIAL／3 NO-SENTINEL）→ 08-27 `A`–`H` → 08-28 `A`–`L`。

**机制侧**：未闭合产出扫描器**建成并接线**（三形态 ＋ 回 LAN 事件感知），形态 1 由 11 处降到 3 处；`#420` 编辑锁共锁修复（根因＝相对路径按 CWD 解析）；`#421` 分叉已解并销号；**12 个孤儿 worktree 补 commondir 恢复可见后判零产出**（不是删了了事）；形态 3 **抢救 1,413 行**未提交产出。

**业务侧**：`#131` 3390 全 BLOCK **查清，是两个 bug**——`#423`（面板 KPI 把孤立发票计进项料品总数与 BLOCK，按单号筛选差三个数量级）＋ `#424`（摄取层 `resolve_item_code` 逐行匹配闸丢掉 421 张有 AP 单可挂的发票，其中 221 张 AP 单在 `invoice.csv` 里一行都没有）。**两封信已发出**（`质量部#10`／`采购部#19`，四条链路全绿；采购部#19 因正文 25 KB 超限发不出，改用「精简正文 2,754 B ＋ 全文 docx 附件」发成）。

**入法三条**：`§〇.17` 多任务默认出 CC Desktop 看护版；`§〇.18` 执行端默认 CC、Terminal 仅限提权；session 名改定 `[Win]MMDDX-<主题短名>`。

---

## 三、🔴 下一棒最该先看的三件

1. **CC 主看护已跑 460 分钟且在自派新号，已请其收尾停手** —— 它把 `OP-0828-W` 的「调度」用成了「自主发现→自主取号→自主派工」。**质量不低**（`#425`／`#426`／`settings.local.json` 都是它抓的），**但因此失去可预期性**。⇒ **下一版看护件必须加硬边界：看护者可「发现并登记」，不得「自派并起活」——发现权与派工权分开。**
2. **6 个 worktree 带未提交改动**（`agent-a3f2c2e`／`agent-ab93efe`／`agent-ad219bf`／`morphology-1-criteria-stuck`／`wecom-service-home`／主工作区），`op0828h` 领先 4、`rescue/local-master-20260828` 领先 9。**停手指令已发，开工先核这批有没有落盘。**
3. **两条 opener 已备未派**：`OP-0828-I`（`#423` 切 ⒜ ＋ 按发布四关部署 `.51`；正文已从 CC transcript 捞回落档，此前只活在会话里）／`OP-0828-M`（停跟踪 `.claude/settings.local.json`，占 sweep reconcile 失败 13/26；前置＝`#425` 分叉已解）。

**等 Shao Peishen 的两件**：`#424` 容差属**会计口径**待唐燕萍签认（候选与影响面已量清、默认值一字未改）；`§四 #132`⑴ 已拍板 **(b)** 派门户网关变更包审查（连带确认 `§一 #240` 凭据；`#410` 在此前不具备验收前提）。

---

## 四、🔑 最该被继承的判据

1. **「太干净」与「太异常」同样可疑，先怀疑自己的读法** —— 本棒栽 4 次：列位数错（`split '|'` 状态列是 `[6]` 不是 `[7]`）／日期筛法（`ConvertFrom-Json` 转 DateTime 致匹配失效）／退出码被管道换掉（`git ls-files` 判反）／拿「日志提及次数」当「失败次数」的分母。
2. 🔴 **未经验证的因果，本棒犯七次、五次由他当场实测推翻**。最该记的一次：**明明写了「无法证明这个机制」，却在同一段里写「证据与它一致且无反例」并据此立三条规则** ⇒ **标注了不确定性 ≠ 按不确定性行事。判据：一个假说写成规则前，先问「有没有一个 5 分钟就能做的实验能推翻它」。**
3. **一条需要「先判断再选择」的规则，就是一条会被忘的规则** —— 能消掉条件的改法优先于把条件写清楚的改法。
4. **判据依赖的载体不完整时，它恒返回「可用/干净」** —— 取号 grep 看不到 CC 自派的号／形态 1 关不掉／决策提醒关不掉，同一族。
5. **停一条 session 的正确方式是让它自己收工，不是掐掉它** —— 掐了未提交产出永久消失。
6. **产生方有、承接方没有** —— 「LAN 留步」写了一个月没人扫；本棒自己也犯（并进 `OP-0827-G` 时它已收工）。**写下「需要动作」而没有承接方，等于没写。**

---

## 五、本线现行纪律（照旧，不重复正本）

- **执行端默认 `▶ 执行端：CC`**（§〇.18）；`管理员 PowerShell` 只留给真提权并附理由；**「普通 PowerShell」基本不该出现**。
- **多任务派发默认出 CC Desktop 看护版**（§〇.17），不出 headless 波次；**看护版不必停 sweep**。
- **每个 opener 第 3 行必须有 `set_session_title`**，格式 `[Win]MMDDX-<主题短名>`；🔴 **Cowork 侧没有这个工具**，靠开场词首行（语义在前、编号在后）。
- 🔴 **取号空间劈开**：Cowork 用 `OP-MMDD-A/B/C…`，**CC 自派用 `OP-MMDD-C1/C2…`** —— 双方看不见对方也不会撞。
- 沙箱 git 不可用于写；commit/push 走本机 PowerShell。

## 六、收工纪律

- 队列写入走编辑锁 `acquire`／`release`，**写完必回读**；⚠️ 文件清单**路径必须写仓库根相对完整路径**，裸文件名会被守卫拒（本棒被拦两次）。
- §二 批次行必写「本批变更参数」「已复检」「本批新入法」。
- **收工覆盖重写本件**（六块、≤8 KB、零日期节）；进度叙事写 CHANGELOG，方法教训写取证知识库，未闭合项写队列行。
- ⚠️ **本件是唯一一份、会被并发抢写** —— 收工前先 `Read` 回读一次再覆盖。
