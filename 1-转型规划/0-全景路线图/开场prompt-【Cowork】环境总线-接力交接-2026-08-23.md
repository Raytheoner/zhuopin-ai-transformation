---
status: 待发
title: "开场 prompt【Cowork】环境总线 · 接力交接（定长交接卡）"
created: 2026-08-23
更新: 2026-08-28（OP-0828-N 二次收工覆盖重写）
上一棒: OP-0828-N
派出线: Cowork 环境总线
---

# 开场 prompt【Cowork】环境总线 · 接力交接

## 一、开场词（复制即用，一次粘贴）

▶ **粘贴端：Cowork**

```
环境总线-接力OP-0829-A
【设置】执行环境：Cowork ｜ CC session：☐ 不适用 ｜ worktree：☐ 不新建（主工作区）｜ 分支：master ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线
读 ① 根 CLAUDE.md 当前进度段 → ② 本件全文 → ③ 1-转型规划/0-全景路线图/拆件-质量部10与采购部19回件-2026-08-28.md（S4 桥二把未做的回灌写成终态的现场）→ ④ 队列 §一 #366（闸开合根治设计）／#425／#426／#284，§四 #132／#133。开干前问我 2-3 个澄清。
```

🔴 **仓库在 `C:\Dev\zhuopin-ai`**（2026-08-26 迁毕），**迁移观察期至 09-02**。
⚠️ **跨日改开场词首行编号**；2026-08-28 已用完 `A`–`W`＋`W2`，同日再起从 `X`。

---

## 二、上一棒做完了什么

**三封跟进信当日发出，四条链路各自全绿**：`财务部#16`（唐燕萍，面板 KPI 与发票认领两处口径）06:21 UTC ／ `质量部#11`（陈忱，8D 规则库 V3.2 可行性答复：**有条件可行**，34 无异议／17 需改／3 前提）09:24 UTC ／ `采购部#20`（姚祖怡，**读取缺陷致歉** ＋ 判例 4 真实案例）09:29 UTC。

**四条泳道跑完**：`O` 切 ⒜ 并部署 `.51`（18 天／21 提交的追平部署，⒜ 只是搭车）／`P` 高水位线 lint ＋ `#426` 定 ⒜＋③ ＋ 停跟踪 `settings.local.json` ／`Q` `#425` ⑴⑶ ／`R` `#416` ⑵。另 `U`（采购取数）／`V`（8D 规则库评估，62 KB）。

**入法四条**：只有需要提权才找他／他要粘的段落必须在聊天里原样打印／`set_session_title` 的前提焊进那一行（根治子任务改父 session 名）／`.gitignore` 补 `settings.local.json`（原先只靠本机全局 ignore，换台机器就失效）。

---

## 三、🔴 下一棒最该先看的三件

1. **`§四 #132`⑴ 门户网关变更包审查 —— 已拍板 (b) 两天，仍无承接方。** 它是 `§一 #410` 验收前提的唯一解锁项，连带要确认 `§一 #240`（企微自建应用凭据，走 IT）。**这是本卡里最老的一条「写下需要动作却没有承接方」，请第一件处理。**
2. **`§一 #366` 闸开合根治已出设计、未派单** —— 三层修法（机器写 `🤖 已归档待拆件`／终态 `📥` 必须携带可机器核验的被回灌行号／闸增第四态 `🔴 待拆件`）。🔴 **改全项目口径，须走 openspec**；opener 正文已备（`OP-0828-S`，见本卡 §五）。
3. **`Fi2TaxExportDailyScan` 仍处停用** —— `OP-0828-T` 部署 `.51` 时经显式豁免停用，**恢复＝批准那次重摄取（`#418 ⑸②`），审批权在 Shao Peishen**；恢复条件＝**等唐燕萍 `财务部#16` 回件**。`#82` 断供告警会持续报，**属预期噪音、不是真故障**，读到先看 `§一 #418`。

**等回件的三封**：`财务部#16`／`质量部#11`／`采购部#20` 均在途，三个闸都锁着。

---

## 四、🔑 最该被继承的判据

1. 🔴 **「他没答／它没做」这个结论，永远只能是「我这个读法没查到」的下位。** —— **本日兑现四次，每次代价都是对人或对他线做出不实指摘**：`采购部#19` 断言姚祖怡没勾（读取假阴性，已书面致歉）／拆件时漏读**段落内**的勾（误判第 5、6 件未答）／`§四 #133` 错怪 `python-docx`（对照实验推翻：库是好的，是流程层没调）／断言 `OP-0828-U` 没做队列回写（它做了，我只看了一个 commit 的文件列表）。
   ⚠️ **最该记的是第四次**：本班上午刚为同一形态书面致歉、刚把这条判据写进信里，**下午就又犯了一次**。⇒ **写下判据与执行判据之间没有任何自动连接** —— 判据要挂到咽喉上（守卫／lint／机器判据），不能只写在文档里。
2. 🔴 **一个「代表某件事已完成」的状态，若机器能在那件事没发生时写上，它就不再是那件事的证据。** 比「该报的没报」危害更大——**它让所有人都不再去看**。（S4 桥二把没做过的回灌写成 `📥 已回件并回灌`，两封信的实质答复因此差点没人读。）
3. 🔴 **被复制的单位是「那一行」，不是「那条规则」** —— 凡指令带隐含前提，把前提写进指令本身。（`set_session_title` 根治即此。）
4. 🔴 **判一条 ignore／配置是否生效，要连「它写在哪个载体上」一起判** —— `check-ignore` 的退出码只说「被忽略了」，是它输出的那个**文件路径**才说明这条规则跟不跟着仓库走。
5. **先 ff、后取证**；凡在已知落后的副本上取到的「没有／零命中」，一律不得作为结论。
6. **输出没了 ≠ 动作没发生** —— 本日三次批准脚本调用因传输超时看似失败，实测全都成功了。**按状态判，不按输出判。**
7. **专员同日既有附件回件又跟一条文本回复时，文本是附件的说明，必须配对读** —— 归档形态是投递通道的产物，不是重要性分级（281 B 那条才是 `质量部#10` 的正式答复）。

---

## 五、本线现行纪律（照旧，不重复正本）

- 🔴 **只有需要提权才找他**（2026-08-28 定）：不需要提权的本机命令自己跑，不再递给他。凡给命令块必须显式标 `无需提权` 或 `须管理员（理由：…）`。
- 🔴 **凡他本人下一步要复制粘贴的那一段，一律在聊天里原样打印 fenced 代码块**；落文件管留档、不替代呈现。**只打印他要粘的**，给别的 agent 读的正文仍只给文件引用。
- 🔴 **不要给他决策树**：一次一个最优建议 ＋ 默认项；他要的是「一个粘贴解决」。
- **多任务派发默认出 CC Desktop 看护版**（§〇.17），硬边界见 `看护件-2026-08-28-落地后批.md` §二（发现权与派工权分开、上限＝正文条数）。
- **每个 opener 的 `set_session_title` 行必须带子任务例外句**（正本＝`专线opener模板库.md` §〇 补充三／补充三之三）。
- 🔴 **取号空间劈开**：Cowork 用 `OP-MMDD-A/B/C…`，CC 自派用 `OP-MMDD-C1/C2…`。
- **已备未派的 opener**：`OP-0828-S`（闸开合根治 openspec，正文在本卡产出的会话里，须重出一次或从 `#366` 行内重建）。

## 六、收工纪律

- 队列写入走编辑锁 `acquire`／`release`，**写完必回读**；**长单元格一律走 `--changes-json <文件>`**（正文不进 argv），JSON 须写在**本机可达路径**（Cowork 的 outputs 目录本机看不到）。
- ⚠️ **release 守卫今日拦了本班五次，五次都拦对了**：裸文件名（`#234(1)`）三次／半角竖线撑列一次／脏文件未登记多次。**被拦时先看它说什么，不要绕。**
- §二 批次号**前缀不得撞号**（工具会建议 `B-MMDD_<同日最大纯数字+1>`）。
- **收工覆盖重写本件**（六块、≤8 KB、零日期节）；进度叙事写 CHANGELOG，方法教训写取证知识库，未闭合项写队列行。
- ⚠️ **本件是唯一一份、会被并发抢写** —— 收工前先 `Read` 回读一次再覆盖。
