---
status: 待发
title: "开场 prompt【Cowork】环境总线 · 接力交接（定长交接卡）"
created: 2026-08-23
更新: 2026-08-28（OP-0828-N 收工覆盖重写）
上一棒: OP-0828-N
派出线: Cowork 环境总线
---

# 开场 prompt【Cowork】环境总线 · 接力交接

## 一、开场词（复制即用，一次粘贴）

▶ **粘贴端：Cowork**

```
环境总线-接力OP-0828-S
【设置】执行环境：Cowork ｜ CC session：☐ 不适用 ｜ worktree：☐ 不新建（主工作区）｜ 分支：master ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线
读 ① 根 CLAUDE.md 当前进度段 → ② 本件全文 → ③ 1-转型规划/0-全景路线图/看护件-2026-08-28-落地后批.md 全文（硬边界七条／四条 opener 正文／本次实测底稿）→ ④ 队列 §一 #425／#426／#416／#423，§四 #132。开干前问我 2-3 个澄清。
```

🔴 **仓库在 `C:\Dev\zhuopin-ai`**（2026-08-26 迁毕），**迁移观察期至 09-02**。
⚠️ **跨日则把开场词首行编号改成当日**（`OP-MMDD-A`）；`O/P/Q/R` 已被本批四条泳道占用，`S` 起才是空的。

---

## 二、上一棒做完了什么

**出了一份带硬边界的看护件**：`看护件-2026-08-28-落地后批.md` —— 硬边界七条（核心＝**发现权与派工权分开**，且写成「只需数数、不需判断」的形式：**子任务上限 ＝ 本件正文条数**）＋ 前置第 0 步 ＋ 泳道矩阵 ＋ `OP-0828-O/P/Q/R` 四条 opener 正文。**四条已备未派**，看护 opener `[OP-0828-N-W2]` 已在件内、复制即用。

**落地了那一摞悬着的提交**：`[Win]0828M` 把 `op0828m-h-ready` 合进本地 `master` 却只推了一条；核出后由他一条 ff push 补上 —— `8b16e64..bfeb3e6`，现 `origin/master` ＝ `master` ＝ `bfeb3e6`，双向 0。

**推翻了两条上一棒写下的事实**：⑴「6 个 worktree 带未提交产出」——逐个核后**脏的 5 处全部且仅是 `.claude/settings.local.json`**，无真产出未落盘；⑵「高水位线滞后一格」——09:0x 属实，10:0x 已被机器人 `append-row` 顺手补平（详见 §四.2）。

**队列**：`#425`／`#426`／`#416`／`#423` 四行回写，`#426` 处置定 **⒜＋③**，§二 `B-0828_O`。

---

## 三、🔴 下一棒最该先看的三件

1. **本批四条尚未派出，前置已全部满足** —— 前置第 0 步已完成（三条判据实测全过）。**开工第一件就是把 `[OP-0828-N-W2]` 交给他粘**，不要重新盘点、不要重出件。四条＝`O` #423 切⒜＋部署 `.51`／`P` 高水位线不对称＋`#426` ⒜＋③＋停跟踪 `settings.local.json`／`Q` `#425` ⑴⑶／`R` `#416` 六子项。
2. **硬边界是新立的，它有没有真的拦住，只有这一批能验** —— 上一位看护者跑了 460 分钟并自派了 `OP-0828-I/K/L`。**收工时务必检验：它起了几条子任务？有没有取新号？** 若仍越界，说明「数数式边界」也不够，须再降一档（如由 Cowork 逐条派、取消看护形态）。
3. **`§四 #132`⑴ 已拍板 (b) 却至今未立项** —— 门户网关变更包审查是 `§一 #410` 验收前提的唯一解锁项，连带要确认 `§一 #240`（企微自建应用凭据，走 IT）。**它已经悬了一天，且没有承接方** —— 正是 §四.6 那条病。

**等 Shao Peishen／他人的**：`#424` 容差属**会计口径**待唐燕萍签认（候选与影响面已量清、默认值一字未改）；`#427`（陈忱 2026-08-28 文本反馈已自动归档）待质量专线领。

---

## 四、🔑 最该被继承的判据

1. **「太干净」与「太异常」同样可疑，先怀疑自己的读法**（列位数错／退出码被管道换掉／拿「提及次数」当分母，均已实证）。
2. 🔴 **一个缺陷可以被另一条机制偶然掩盖，于是它既没被发现也没被修，只是暂时看不见** —— 本棒实证：`#426` 手工立行没更新高水位线（差一格、会撞号），一小时后被企微机器人 `append-row` 自动追行 `#427` **顺手推平**。当天若没有专员回件，它会一直挂到有人撞号。**根因（手写不更新、机器写才更新）还在。** 比「载体不完整恒返回可用」更进一层。
3. 🔴 **「已合进本地 `master`」≠「已在 `origin`」** —— 中间那一步没有任何一方负责，也没有任何一步会提醒。本棒当日第三次撞见，从 09:0x 到 10:0x 领先 9、**全程无信号**。
4. 🔴 **`git cherry` 判「已在」（`-`）可信，判「不在」（`+`）不可直接采信** —— rebase 换了上下文，patch-id 就不同。**判「不在」必须回目标文件核一次结果**（本棒对 `bea1012` 差点误判）。
5. 🔴 **依据只活在一条会话历史里的拍板，派出前必须回他本人核一次** —— `#423` 切 ⒜ 那句原只在 CC 会话里，而据它派出的件会真部署 `.51`、改唐燕萍每天看的数。核了，属实。
6. **一条需要「先判断再选择」的规则，就是一条会被忘的规则** —— 能消掉条件的改法优先。本棒两处兑现：把 `R` 限定在 `wecom-aibot-service/` 内以消掉与 `P` 的触碰区重叠；把 `P` 限定为「不改判定逻辑」以消掉「必须最后收工」的硬序。
7. **产生方有、承接方没有 ＝ 等于没写**（`§四 #132`⑴ 正在犯）。

---

## 五、本线现行纪律（照旧，不重复正本）

- 🔴 **凡他本人下一步要复制粘贴的那一段，一律在聊天里原样打印一份 fenced 代码块**（Shao Peishen 2026-08-28 定）。落文件是为了留档与承接，**不替代呈现**；「>500 字落文件」管的是留档、不是"所以聊天里不用给"。**只打印他要粘的**——给别的 agent 读的正文（如看护件里的子任务 opener）仍只给文件引用，否则聊天被淹。
- 🔴 **凡给出本机命令块，必须显式标权限层级**：`无需提权` 或 `须管理员（理由：…）`。本棒漏标，他为一条 `git push` 开了 Administrator PowerShell（**并不需要**）。不标 ＝ 让他自己猜，他会选最高那档。
- **执行端默认 `▶ 执行端：CC`**（§〇.18）；`管理员 PowerShell` 只留给真提权并附理由。
- **多任务派发默认出 CC Desktop 看护版**（§〇.17），不出 headless 波次；看护版不必停 sweep。
- **每个 opener 第 3 行必须有 `set_session_title`**，格式 `[Win]MMDDX-<主题短名>`；🔴 Cowork 侧没有这个工具，靠开场词首行。
- 🔴 **取号空间劈开**：Cowork 用 `OP-MMDD-A/B/C…`，**CC 自派用 `OP-MMDD-C1/C2…`**。
- 沙箱 git 不可用于写；commit/push 走本机 PowerShell，**且不由 Cowork 执行**。

## ⏳ 队列更新待补（开工第一件事先清掉，清完删本节）

**两笔写入已备好 JSON、因撞锁未落**（`CC-OP-0828-Q` 持锁回写 `#425`，守卫按 §一 `#351`⑴ 正确拒绝；**这顺带证明 `#420` 的修复在主工作区确已生效**）。JSON 在 `.queue-edit-tmp/`（`q284.json`／`batch4.json`），**开工先看它还在不在**：

1. **§一 `#284`**（阈值计数台账，常驻不占 WIP）追记 —— `set_session_title` 在 Task/Agent 子任务里把父 session 改名这一新形态 ＋ 根治已落 ＋ **opener 块 lint 双向告警的建造需求（待领）**。
2. **§二 `B-0828_O`** 文件清单三追加 —— 模板库／看护件／队列三件。

**做法**：`python 0-学习与工具/工具-共享文档编辑锁.py acquire --who <你的身份> --note '…；登记豁免：.claude/settings.local.json 为本机客户端本地件、非本会话产出'` → 两次 `edit-row --changes-json` → `release`。
⚠️ **若 `.queue-edit-tmp/` 已被清掉**，内容可从本轮 Cowork 会话或模板库 §〇 补充三之三「根治」节重建——**判据别丢**：*被复制的单位是「那一行」，不是「那条规则」；凡一条指令带隐含前提，把前提写进指令本身。*

---

## 六、收工纪律

- 队列写入走编辑锁 `acquire`／`release`，**写完必回读**；⚠️ 文件清单**路径必须写仓库根相对完整路径**，裸文件名会被守卫拒。
- 🔴 **长单元格一律走 `edit-row --changes-json <文件>`，正文不进 argv**（守 `#414`）。⚠️ **JSON 文件必须写在本机可达路径**（如仓库内临时目录，用完删）——Cowork 的 outputs 目录本机 PowerShell **看不到**，本棒在此栽过一次。
- §二 批次行必写「本批变更参数」「已复检」「本批新入法」。
- **收工覆盖重写本件**（六块、≤8 KB、零日期节）；进度叙事写 CHANGELOG，方法教训写取证知识库，未闭合项写队列行。
- ⚠️ **本件是唯一一份、会被并发抢写** —— 收工前先 `Read` 回读一次再覆盖。
