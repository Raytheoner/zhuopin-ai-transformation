---
title: "派单件 · 【CC】#565 第二棒全量移植（OP-0912-F）"
created: 2026-09-12
status: 生效
用途: 把 claude/op0912b-lane-heartbeat-565 @ 8a1021f 的真值填充全量移植到当前 master，并在 openspec 包内补记决策点 1 的修订
配套: 看护件-泳道看护批B-0912_565移植-2026-09-12.md
派出线: Cowork 环境总线 OP-0912-E
---

# 派单件 · 【CC】`#565` 第二棒全量移植（`OP-0912-F`）

## 〇、这件事的由来（一句话）

`#565` 同题两棒：对照棒 `claude/op0912b-heartbeat-wiring-565`（`6733cb4`＋`b64e442`）**已在 master**（手段＝`git merge-base --is-ancestor … master` exit 0），第二棒 `claude/op0912b-lane-heartbeat-565` @ `8a1021f` 未 ff、与 master **6 个文件冲突**（手段＝`git merge-tree --write-tree` exit 1，逐文件 CONFLICT）。

🟩 **Shao Peishen 2026-09-12 回 `2a`（`OP-0912-E` 报告定夺 2(a)）：全量移植。**

## 一、做什么

1. **起棒**：从**当前 master**（不是 `71a4a44`）起分支 `claude/op0912f-heartbeat-realvalue-565` ＋ 同名 worktree，收工自删。
2. **移植 `8a1021f` 的全部改动**，逐文件解冲突。🔴 **叠加，不是替换**——master 上对照棒的成果必须保留：`SUBTASK_HEARTBEAT_NOTE` 的注入位置（`[并行, 心跳, push, 哨兵]`）、lint 形态⑩的存在、SKILL.md v2.3 收工句补 `--batch`、以及 `b64e442` 那条「明细条数正则漏计双位数形态码 F10」的修正。冲突 6 文件：
   - `0-学习与工具/工具-opener生成.py`、`0-学习与工具/工具-opener块lint.py`
   - `0-学习与工具/test_工具-opener生成.py`、`0-学习与工具/test_工具-opener块lint.py`
   - `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md`、`1-转型规划/0-全景路线图/opener骨架.md`
3. **移植后必须成立的三条（逐条自测，写出手段）**：
   - ⑴ 心跳尾句由 `subtask_heartbeat_note(lane, batch)` 填**真值**：`--lane` ＝ worktree 名（分支名去 `claude/` 前缀后相同），`--batch` 由新增的 `--batch` 旗标显式给、或从看护件 `派出线` 字段现取 `B-MMDD_…`；**两者皆无 ⇒ fail-loud 不出件**（不得静默留占位符）。
   - ⑵ 生成器新增 `--batch` 旗标（可选；缺失时走⑴的现取与 fail-loud）。
   - ⑶ lint 形态⑩由「同一行同现 `heartbeat` 与 `--lane`」**收紧**为「同一行同现 `heartbeat`、`--done`、`--batch`」，规则生效日 `2026-09-12` 不变。
4. 🔴 **openspec 决策点 1 修订补记**（本棒的第二件事，不可省）：在 `openspec/changes/lane-watch-heartbeat-wiring/design.md` **决策点 1 段末**追加一段修订，写清：
   - Shao Peishen 2026-09-12 回 `2a`（`OP-0912-E` 定夺 2(a)），决策点 1 的取值由 **(a) 占位符** 改为 **「第三条路」＝真值填充但不经看护者手传**；
   - 改判理由＝design 当初否 (b) 的核心代价「新增了一处两处必须人工保持一致」**对本实现不成立**：取值由 worktree 名与 `派出线` 字段**现取**，看护者不多传一个参数（手段＝`工具-opener生成.py::subtask_heartbeat_note` 源码）；
   - 🔴 **不追改已 ff 的提交**（`6733cb4`／`b64e442` 原文不动），只在 design.md 追加修订段——本包 design.md 自述「视为回改需求、按正常 openspec 变更流程另开一版」，本条即那一版的落字。
   - ⚠️ **design.md 已于本棒开工前被 `OP-0912-E` 改过**（决策点 5 结案段，2026-09-12 11:4x）。从最新 master 起棒，**勿覆盖该段**。
5. **零回归（手段随句写明，只有动作没有手段 ＝ 没有验证）**：
   - `python -m pytest 0-学习与工具/test_工具-opener生成.py 0-学习与工具/test_工具-opener块lint.py -q`；
   - `python 0-学习与工具/工具-opener块lint.py --enforce` 全库跑，与**纯 master 同命令**比**失败集合差集**，不是二值全绿；
   - 🔴 已知常红三条（在纯 master 上同样红，手段＝2026-09-12 `OP-0912-E` 两侧各跑一次）：`OpenerGuardReleaseTests::test_watcher_pattern_lane_without_title_passes`、`ReleaseStructuralValidationTests::test_row_length_over_cap_warns_before_cutoff_does_not_block`、`ReleaseStructuralValidationTests::test_row_length_section_four_topic_column_checked`——**不得算成本棒引入**，也**不得顺手去修**（不在本棒范围）。
6. **队列回写**：`#565` 状态列追 ✅ 建成段（走 `工具-共享文档编辑锁.py`，不裸改）；§二 登记本批次；行内超限则按 K2 外置到 `队列行日志/#565.md`。
7. **收工**：只 `push` 本分支，**不 ff master**；worktree 自删；分支留给 `工具-待合分支巡检.ps1`。

## 二、不做什么（硬边界）

- 🔴 **不 ff master**（🟡 档，等 Shao Peishen 一字母；ff 走『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做）。
- 🔴 **不改 `工具-opener批处理执行v2.ps1`**（`#565` 行内与本包 design Non-Goals 双写死）。
- 🔴 **不改 `工具-泳道看护状态机.py` 任何函数**（本包 design 决策点 0 钉死）。
- 🔴 **不实现方案 B**（决策点 5 已于 2026-09-12 裁定不落地，`OP-0912-E` 定夺 3；design.md 已落字结案，**不要再动那一段**）。
- 🔴 **不追改历史**：已 ff 的提交、带日期的历史看护件一律不动。
- 🔴 **不碰 `.51`／企微机器人／定时任务；不做任何对外发送。**
- 🔴 **不修上面点名的三条已知常红。**

## 三、收工报告按此产出

1. 分支名＋commit＋`merge-base --is-ancestor` 现取结果；
2. 三条「必须成立」逐条写结论**与手段**；
3. 零回归写成「分支侧失败集合 ⊆ 纯 master 同命令失败集合，逐条复现」的形态，读数现跑不引用；
4. openspec 修订段的落点（文件＋节）与原文摘句；
5. 🟡 停点（如有）写清 (a)/(b)＋代价＋〔荐〕＋默认项；
6. 顶格一行 `OPENER_DONE`；命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。
