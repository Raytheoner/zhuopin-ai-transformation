# 看护件 · LAN 收口批 B-0917_419ops-webhook凭据落位

**批次**：B-0917_419ops-webhook凭据落位  
**时间**：2026-09-17 12:10–12:40 +08:00（北京时间）  
**会话**：Cowork LAN 收口批（Shao Peishen 在场）  
**触发**：Shao Peishen 回「我已回lan」

---

## §一 前置

| 项 | 结果 |
|---|---|
| LAN 探针 | ✅ on-LAN（三项齐备，`工具-泳道看护状态机.py lan-status`） |
| git 对齐 | ✅ ahead=0 / behind=0（master） |
| 编辑锁 | ✅ 无长占 |
| 上批收口件 | B-0913_538明细导出部署（2026-09-13） |

---

## §二 LAN 留步清单（三源汇集）

grep「LAN 留步」命中 3 行；D1 筛后：

| 行号 | D1 判定 | 原因 |
|---|---|---|
| #312 | ❌ 假阳性 | 任务列含「LAN 留步无承接方」——历史叙述句，非留步标记 |
| #398 | ❌ 假阳性 | 任务列含「LAN 留步根本没有承接方」——原始取证陈述，非留步标记 |
| #419 | ✅ 真实留步 | 任务列明写「⚠️ LAN 留步：.51 上实际部署的版本、.env 有无 _OPS」 |

grep「待回内网」命中 0 行。  
近三批批次 summary（B-0913_538明细导出部署、B-0912_worktree收割、B-0912_落库瘦身两泳道）均「转出 0 项」。

---

## §三 执行项（#419 ops-webhook 第三道防线）

### 3.1 快照点（执行前）

- `ops/wecom-service-home` 分支 HEAD：`47ad4b1`（与 master 同步，含 OP-0910-F 修法）
- worktree 根 `.env`：**缺失**（D1 未落位）
- `resolve_alert_webhook()` 在 worktree 上下文：**返回 None**（D1 未落位，验证方法：Python import 直调）

### 3.2 执行步骤（全由 Shao Peishen 本人动手，Cowork 辅助）

**D1 凭据落位**（Shao Peishen 本人，2026-09-17 ~12:30）：  
在 `C:\Dev\zhuopin-ai\.claude\worktrees\wecom-service-home\.env` 写入 `WECOM_WEBHOOK_URL_OPS=<值>`，PowerShell 7 脚本写入并自验证。

### 3.3 冒烟判据（D1 后实测）

```
cd C:\Dev\zhuopin-ai\.claude\worktrees\wecom-service-home
python -c "
import sys, os
os.chdir(r'C:\Dev\zhuopin-ai\.claude\worktrees\wecom-service-home')
sys.path.insert(0, r'5-平台底座\wecom-aibot-service')
sys.path.insert(0, r'5-平台底座')
from scripts.alert_webhook import resolve_alert_webhook
val = resolve_alert_webhook()
print('non-None OK' if val else 'None FAIL')
"
```

**实测结果**：`resolve_alert_webhook() = non-None OK`  
**指纹（SHA256 前8位）**：`f26371a5`（与 D1 写入值一致）  
⚠️ 指纹与 tasks.md 2026-08-23 记录的 `4bf10217` 不吻合，Shao Peishen 当日目视确认为运维群 webhook，确认后继续。

### 3.4 回滚 SOP（若冒烟失败）

删除 `C:\Dev\zhuopin-ai\.claude\worktrees\wecom-service-home\.env` 即完全回滚（仅该文件，不影响代码或主工作区）。

### 3.5 6.5 Archive

**执行**：将 `openspec/changes/sweep-ops-webhook-cutover/` 迁至 `openspec/changes/archive/2026-09-17-sweep-ops-webhook-cutover/`  
**前置验证**：tasks.md 所有项全 `[x]`（含 6.5 本身，已追记）  
**手段**：`Move-Item` PowerShell cmdlet，执行后 `Test-Path tasks.md` 验证文件在目标路径 = True ✅

---

## §四 收工汇总

| 项目 | 判据 | 证据 | 行内状态处置 |
|---|---|---|---|
| D1 凭据落位（#419） | `resolve_alert_webhook()` non-None | 实测指纹 `f26371a5` ✅ | 行内追记完工；状态转 `[S:done]` |
| 5.2 人眼半边 | Shao Peishen 本日复确认 | 答「1a」 ✅ | 已记录 |
| 6.5 openspec archive | 目录迁移成功 + `Test-Path` ✅ | `archive/2026-09-17-sweep-ops-webhook-cutover/tasks.md` 存在 ✅ | 变更包已归档 |

**收口≠销号**：行 #419 状态按行内原纪律处置（见 §四 追记）。

---

## §五 §二 批次登记

**批次号**：B-0917_419ops-webhook凭据落位  
**产出文件**：
- `1-转型规划/0-全景路线图/看护件-LAN收口批B-0917_419ops-webhook凭据落位-2026-09-17.md`（本件）
- `openspec/changes/archive/2026-09-17-sweep-ops-webhook-cutover/`（变更包归档，目录整体）
- `C:\Dev\zhuopin-ai\.claude\worktrees\wecom-service-home\.env`（D1 凭据，gitignore 件，不入 commit）
