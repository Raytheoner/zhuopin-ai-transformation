---
status: 待发
title: "CC Desktop 看护件 · 2026-08-28 落地后批（4 条 ＋ 一步前置）"
created: 2026-08-28
派出线: Cowork 环境总线（OP-0828-N）
---

# 看护件 · 2026-08-28 落地后批

> **本件与 `看护件-2026-08-28-可开工批.md` 的唯一实质差别 ＝ §二 硬边界。**
> 上一份的看护者 `OP-0828-W` 把「调度」用成了「自主发现 → 自主取号 → 自主派工」，跑了 460 分钟。**它抓到的东西质量不低**（`#425`／`#426`／`settings.local.json` 都是它抓的），**但因此失去了可预期性**。⇒ 本件把**发现权与派工权分开**，并写成**不需要判断、只需要数数**的形式。

---

## 〇、本件依据的实测底稿（2026-08-28 09:04 本地，Cowork 环境总线本机 PowerShell 只读）

🔴 **看护者与各子任务：凡本节数字与你的实测不符，以你的实测为准，停下来报，不得替本件改判。**

| 量 | 实测值 |
|---|---|
| `origin/master` | `1826529`（2026-08-28 07:41 本地）—— **全程未动** |
| 本地 `master` | `8b16e64`，领先 `origin/master` **1**、落后 **0**，未推 |
| 主工作区 HEAD | **不在 `master` 上** ＝ `claude/op0828m-h-ready` @ `282eb13` |
| `op0828m-h-ready` vs `origin/master` | 领先 **8**、落后 **0**；`merge-base --is-ancestor` ＝ True ⇒ **纯 ff，可直接推** |
| 本地 `master`(`8b16e64`) 是否在 `h-ready` 里 | **是**（`--is-ancestor` True）⇒ ff 后它自动进主线，**不会丢** |
| 主工作区脏文件 | **只有** `.claude/settings.local.json` |

**「6 个 worktree 带未提交改动」这句话已被实测推翻一半** —— 逐个核过 `agent-a3f2c2e`／`agent-ab93efe`／`agent-ad219bf`／`morphology-1-criteria-stuck`／`wecom-service-home`／主工作区：**脏的那 5 处，脏文件全部且仅是 `.claude/settings.local.json`**，`wecom-service-home` 干净。🔑 **没有任何真产出停在未提交状态** —— 那是**同一个被跟踪的本机私有件在 5 处各显示一次**，不是 5 份产出。**这正是 `OP-0828-M` 要停跟踪的那个文件本身。**

**已推 origin 且内容已在 `h-ready` 里的分支**（`git cherry` 判，`-` ＝已在）：`op0828a`(`21639fa`)／`op0828b`(`0e51c9c`)／`op0828g`(`6ebdc14`→ 副本 `f629cb0`)／`op0828h-fi216-letter-r5`(`7a3060a`) 三条。
⚠️ **一处必须看懂的假阳性**：`git cherry` 对 `origin/claude/op0828h-rebased-on-1826529` 的 `bea1012` 打 **`+`**（判为「不在上游」）。**那是 rebase 换了上下文导致 patch-id 不同，不是真的没落地** —— `bea1012` 与 `h-ready` 里的 `4aab5b0` 是同一件事（`#426` 立行），补丁文本 30,222 B vs 37,660 B。**判据应换成看结果**：`git show claude/op0828m-h-ready:<队列文件>` 里 **`#426` 行在**，`origin/master` 里**不在** ⇒ 内容确已在 `h-ready`。🔑 **`git cherry` 判「已在」可信（`-`），判「不在」（`+`）不可直接采信，须回目标文件核一次。**

### 🔴 本次顺带撞出的一处真缺陷（未修，已进本批泳道 P）

**`h-ready` 与主工作区的机制队列里 `#426` 行已经存在，而顶部「编号高水位线」仍写 `§一 #425`。**
⇒ **下一个按协议〇.8 取号的人会取到 `#426`，撞上一条已存在的行。** 同族＝`§一 #341`（跨文件高水位线同步的残留欠账）与 `一ter` 那次撞号：**取号判据依赖的载体没跟上，于是它照常返回一个「可用」的号。**

---

## 一、🔴 前置第 0 步 —— 不是泳道，看护者不做，也不派人做

**本批三条泳道全部阻塞于这一步；这一步没做完，看护者不得起任何子任务。**

把 `claude/op0828m-h-ready` @ `282eb13` ff 进 `master`。它是 `origin/master` 的直系后代（落后 0），**一次 ff 就把这 8 条全部落地**：

```
282eb13 docs(队列/#425): OP-0828-M 停手前的队列回写与接力件更新
6a81996 docs(看护件): OP-0828-W 批次执行记录回填（+95 行）
4aab5b0 docs(队列/#426): 立行——worktree agent 够不着队列权威副本
ea5a38b docs(队列): OP-0828-H 批次行如实登记
a580a1a docs(台账): OP-0828-H 收工重跑文档台账
487d8c4 docs(跟进信/#423/#424): OP-0828-H 备齐财务部#16 判例包（未发送）＋ FI2 场景 CLAUDE.md R5 瘦身
f629cb0 feat(sweep/#425): 第 8 类常驻告警——本地 master 独有提交 + 查清 ⑴ 根因
8b16e64 docs(队列): OP-0828-M 分支整备——L 抢修落库＋ #425 追记远端复验判据
```

▶ **执行端：`[Win]0828M` 那条正暂停的 CC session**（它的上下文全在，且这四件本来就是它列给 Shao Peishen 的）。收口词见本件末尾 §五。
⚠️ **若 `master` ff 再次被分类器拦下**（`ea5a38b` 的提交信息记着上一次就是这么被拦的），**不要绕、不要 `push -f`**，改由 Shao Peishen 在本机 PowerShell 跑同一条命令，并把这一次的拦截如实登记进 `§一 #425`。

**第 0 步完成的判据（三条全过才算）**：① `git rev-list --count origin/master..master` ＝ 0 且 `master..origin/master` ＝ 0；② `git rev-parse origin/master` ＝ `282eb13`；③ 主工作区 HEAD 已切回 `master`。

---

## 二、🔴 硬边界 —— 看护者开工前必须逐条读完

> **总原则：看护者有「发现权」，没有「派工权」。**
> 上一棒的问题不是它发现得太多，是它**发现完就自己派了**。发现是好的，派工不是它的。

1. 🔴 **本批子任务数量上限 ＝ 本件 §四 里写好正文的条数（4 条）。起第 5 条子任务即违规。**
   **这是本件唯一需要你遵守的计数**——不问「这个发现该不该派」，只数「本件里有几条正文」。**判据不需要判断，因此不会被忘。**
2. 🔴 **看护者不得给任何新发现取 opener 号，不得写 opener 正文，不得起子任务去做它。**
   新发现的正确落点只有两个：**① 走编辑锁在队列登记一行；② 写进收工汇总的「本批新发现」小节。** 到此为止。
3. 🔴 **若确有一个发现紧急到「等不到下一批」——停下来问 Shao Peishen，由他决定。** 你可以陈述紧急理由，**不得代他把「紧急」判成「所以我现在就派」**。
4. 🔴 **CC 侧若日后确需自派，取号空间是 `OP-MMDD-C1/C2…`，不得占用字母序**（字母序属 Cowork，见 `专线opener模板库.md` §〇.0 补充一）。**本批不预期出现任何自派号。**
5. 🔴 **不得替子任务改判**：子任务的报告与本件所述事实对不上时，**以子任务的实测为准，停下来报**。
6. 🔴 **收工即停，不找活。** 三条子任务全部收工 ＋ 汇总写完 ＝ 本 session 结束。**「顺手再看看还有什么」不属于本次授权。**
7. **看护者自己不做的**：不改代码、不写子任务的队列行、不 commit 别人的产出、不动任何计划任务、不做前置第 0 步。

---

## 三、泳道矩阵（四条触碰区两两零重叠 ⇒ 可并行；错峰启动约 2 分钟）

| 泳道 | 成员 | 触碰区 |
|---|---|---|
| `fi2` | `OP-0828-O` | `4-数字员工/财务部/FI2-三单匹配自动对账/` ＋ `.51` 部署 |
| `numbering` | `OP-0828-P` | `0-学习与工具/工具-队列结构lint.py`、`工具-共享文档编辑锁.py`（**仅提示文案**）、机制队列顶部高水位线 |
| `sweep` | `OP-0828-Q` | `0-学习与工具/工具-落库sweep.py` ＋ 其单测 |
| `aibot` | `OP-0828-R` | `5-平台底座/wecom-aibot-service/` **全部改动限定在此目录内** |

🔴 **唯一共享资源是编辑锁**（四条收工都写队列）—— 泳道并行、收工写队列天然串行化，错峰启动即可。

⚠️ **`P` 与 `R` 之间有一处曾经重叠、已被切开**：`#416 ⑶`（机器人 release 被拒 ⇒ 队列锁死 30 分钟）原本会摸到 `工具-共享文档编辑锁.py` 的 release 拒绝分支，而那正是 `P` 的触碰区。⇒ **`R` 被限定为「全部改动落在 `wecom-aibot-service/` 之内」**，真因（行内 2026-08-26 更正已基本确定＝机器人 release 时工作区有脏文件不属任何待处理 §二 批次、触发登记完整性校验被拒）本就该在机器人侧修。**重叠因此消失，不是靠谁记得避让。**
✅ **本批刻意不再有「某条必须最后收工」的硬序**：上一批那条硬序来自「`OP-0828-D` 改的正是编辑锁本身」。本批 `OP-0828-P` **被限定为只改提示文案与新增一行 print、不动任何判定逻辑** ⇒ **条件被消掉了，不需要看护者去判断谁先谁后。**（守判据：*能消掉条件的改法，优先于把条件写清楚的改法。*）

### 看护 opener（复制即用）

▶ **执行端：CC**

```
[OP-0828-N-W2]【CC】2026-08-28 落地后批 · 泳道看护（调度四条，不亲自动手，不自派）
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☐ 不新建（看护者只读与调度）｜ 分支：master ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828W2-落地后批四条看护
读 1-转型规划/0-全景路线图/看护件-2026-08-28-落地后批.md 全文。🔴 §二 硬边界七条先逐条读完再动手——上一位看护者跑了 460 分钟并自派了 opener 号，本件就是为此写的。
【前置闸，先查再动】§一 前置第 0 步（把 claude/op0828m-h-ready ff 进 master）必须已完成，三条判据：git rev-list --count origin/master..master ＝ 0 且 master..origin/master ＝ 0；git rev-parse origin/master ＝ 282eb13；主工作区 HEAD 已在 master。🔴 未完成即停手回报，不得自己去做这一步、也不得派人做。
【你的职责】① 按泳道起活（§四 四条，各起一个 Task/Agent 子任务，isolation: "worktree"，把对应 opener 正文原样作为 prompt，🔴 不要改写）；② 盯进展与产出；③ 异常或需拍板处停下来问 Shao Peishen；④ 全部收工后给一份汇总。
【🔴 计数边界】本批子任务上限 ＝ 4 条，起第 5 条即违规。新发现只许「队列登记一行 ＋ 写进汇总」，不许取号、不许写 opener、不许起子任务去做。
【并行】四条触碰区两两零重叠，可并行；错峰启动约 2 分钟（唯一共享资源是编辑锁）。本批无硬序。
【看到这些要停下来问他，不要自行决定】① OP-0828-O 里任何要改 #424 的 strategy 或三个容差的动作（属会计口径、要唐燕萍签认）；② 任何会改写唐燕萍生产 invoice.csv 的重新摄取；③ .51 之外的任何生产写动作；④ 任何对外发送（专员、真实群）；⑤ OP-0828-P 里若发现必须改编辑锁的判定逻辑（而非提示文案）；⑥ OP-0828-R 里若发现必须改 wecom-aibot-service 之外的文件（尤其 工具-共享文档编辑锁.py，那是 P 的触碰区）；⑦ 子任务报告与本件所述事实对不上时——🔴 以实测为准、停下来报，不得替它改判。
【收工汇总】每条一行：编号／状态（完成·部分·失败）／产出落在哪个分支或文件／还留了什么。🔴「部分完成」要写清是哪一步停的、为什么。另起一节「本批新发现（已登记队列，未派工）」逐条列。最后跑一次 python "0-学习与工具/工具-未闭合产出扫描.py"，三形态数字贴出来作为收尾体检。
【收工即停】四条收工 ＋ 汇总写完即结束，不再找活。
开干前问我 2-3 个澄清。
```

---

## 四、四条 opener 正文

### `OP-0828-O`【CC】#423 切 ⒜ 止血 ＋ 按发布四关部署 `.51`

> **来源**：原 `OP-0828-I` 正文（从 CC transcript 捞回、落档在 `看护件-2026-08-28-可开工批.md` §一quater）。**除编号与前置闸外一字未改** —— 里面每条硬要求都是实测教训，改写等于拆护栏。
> ✅ **其依据的那句「Shao Peishen 2026-08-28 拍板 `#423` 先切 ⒜ 止血」已由他本人于 2026-08-28 09:0x 当面复核确认属实，并明确「照派，含部署」。** 该 opener 原文里那句「若与你的记忆不符，以你为准」至此已消解。

```
[OP-0828-O]【CC】#423 切 ⒜ 止血 ＋ 按发布四关部署 .51
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N ／ 看护者 OP-0828-N-W2
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828O-423切a档与部署

Shao Peishen 2026-08-28 拍板：#423 先切 ⒜ 止血，并于当日 09:0x 当面复核确认「照派，含部署」。他答的是这个选项：「⒜ 不改任何判定结论、只改怎么显示，而现状那个数是确凿错的」。

🔴 前置闸：本件全部改动的基点必须是已含 OP-0828-A 三个 commit 的 master。开工先核 git rev-list --count origin/master..master 与 master..origin/master 均为 0；不为 0 立即停手回报（说明前置第 0 步没做完）。

先读：① 业务队列 §一 #423／#424／#418 全行 ② 4-数字员工/财务部/FI2-三单匹配自动对账/ ③ OP-0828-A 的三个 commit（bbf03a6 实现／526d1c3 口径底稿／21639fa 场景 CLAUDE.md）——三档口径与 #424 的四个 strategy 都已实现好，本件不重写，只切换 ＋ 部署。

【🔴 范围：只切 #423，不动 #424】
- 切：面板 KPI 口径 → ⒜（孤立发票移出 KPI 三档、单列一个「孤立发票 N 张」的独立提示条）。入口是 OP-0828-A 建的模块常量 _KPI_ORPHAN_MODE（自己定位确认真实名称，不要照抄）。
- 🔴 不切：#424 摄取层 resolve_item_code 的 strategy 与三个容差一个字节都不动，仍是现状默认。⒜⒝⒞ 必须等唐燕萍签认——它改的是进不进库的数据，不是显示。OP-0828-A 为两个默认值各配了退化守卫单测，#424 那条要保持红线状态、不许放开。
- 判据一句：⒜ 只改「怎么显示」，#424 改的是「装载多少」——他批的是前者。

【为什么必须连部署一起做】
.51 是整包同步部署 ⇒ 不部署，唐燕萍今天在面板上看到的还是 3390 项 / BLOCK 3390（而引擎实际只判了 1 项）。本件的全部意义就是让她看到对的数。

【发布四关，缺一即停（根 CLAUDE.md §5「发布即收口纪律」）】
1. 功能门槛：全量测试绿（OP-0828-A 今晨基线 230 passed / 9 skipped，切了 ⒜ 后重跑）＋零回归＋黄金基准不漂移。
2. 部署段：真部署 .51 ＋ 冒烟——/api/ping、关键页 200、一次全量重算；🔴 回滚 SOP 必须在位并写进报告（怎么退回现状档、退回后如何验证）。
3. 合规不放松：两道门禁照旧（只归档/通知不自动执行、L2 人工确认）。本次是显示口径变更，不放开任何 AI 自动执行。
4. 端到端复现：🔴 用唐燕萍 #418 原始举证的真实案例（AP-2026080041 等）在面板上跑一次，确认按单号筛选显示的是修正后的值。面板有登录口令闸 ⇒ 能自动验的自动验，确需人登录截图的如实登记为待 Shao Peishen 一次点击，不要假称已验。

【🔴 任何一关不过就停，不要硬推】
- 部署失败／冒烟不过／全量重算与 OP-0828-A 今晨那组三方对照数（640 单／2497·883·204·1／影子重摄取）对不上 ⇒ 立即按回滚 SOP 退回现状档并如实报，不得为了让它通过而调整判据或数据。
- LAN 可达性请现在重探一次（OP-0828-A 今晨探过 SSH/ping 均通，但那是几小时前）；不可达就如实登记「未部署」并说明。

【其余边界】
- 🔴 不对外发送任何消息（专员、真实群）——唐燕萍那边由 Shao Peishen 线下落实签字，本次不发信。⚠️ 财务部#16 判例包已由 OP-0828-H 备齐但未发送，本件不碰它。
- ff push 前先 fetch（origin 顶端今晨多次前进）；🔴 绝不 git pull、绝不 push -f；写队列走编辑锁、撞锁退避重试。
- 🔴 不修改、停用、删除任何计划任务。
- 🔴 实测与本件所述不符，以你的实测为准，停下来如实报，不得替它改判。

【收工】
回写 §一 #423（写明 ⒜ 已切、部署与否、依据＝Shao Peishen 2026-08-28 拍板并当日复核；#424 仍待她签认、未动）与 #418；登记 §二 批次（今日已用到 B-0828_M，先查同日已用）。
结构化摘要 ＋ 单列三行：「发布四关：逐关过/不过」「唐燕萍现在在面板上看到的数：筛 AP-2026080041 时显示 N 项 / BLOCK N」「回滚 SOP：一句话怎么退」。
开干前问我 2-3 个澄清。
```

### `OP-0828-P`【CC】取号高水位线补 `#426` ＋ `#426` 处置定案

```
[OP-0828-P]【CC】取号高水位线滞后一格（真缺陷，未修）＋ #426 处置定 ⒜ 并落地
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N ／ 看护者 OP-0828-N-W2
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828P-高水位线补426与426处置

【一 · 先修那个正在出血的：高水位线滞后一格】
🔴 已实测（2026-08-28 09:0x 本地，Cowork 环境总线）：机制队列里 §一 #426 行**已存在**，而顶部那行「编号高水位线：§一 #425 ｜ §四 #132」**仍写 #425**。⇒ 下一个按协议〇.8 取号的人会取到 #426，撞上一条已存在的行。
先自己复核一次再改（不得凭本 opener 描述直接改）：grep 顶部高水位线那行 ＋ grep '^| 426 |'，两侧都看到才动手。
改法：把 §一 高水位线推到实测的最大已用号（Cowork 侧实测 §一 最大已用 ＝ 426、高水位线写 425 ⇒ 差一格；§四 最大已用 ＝ 132、高水位线写 132 ⇒ §四 是对的，别顺手改坏）。⚠️ **业务场景那份文件里没有高水位线行**（Cowork 侧实测，`Select-String '编号高水位线'` 空）——高水位线只在机制环境那一份、两份共用它，**这是设计不是缺陷，不要去给业务场景那份补一行**（补了就成了两个会各自漂移的真值）。
🔑 判据（比这次修复更值得留）：这与 §一 #341（跨文件高水位线同步的残留欠账）、与 08-28 那次 L 撞号是同一族——**取号判据依赖的载体没跟上，于是它照常返回一个「可用」的号，不产生任何信号**。⇒ 顺带评估：能不能让取号这一步**从队列正文里现算最大已用号**，而不是读一个需要有人记得去更新的常量？若能，写成建议登记 §一，🔴 **本件不实现**（那属改变全项目口径，须另走 openspec）。

【二 · #426 处置】
读 §一 #426 全行（OP-0828-H 立，取证在 工具-队列结构lint.py:81-94）。它给了 ⒜⒝⒞ 三个方向，行内明写「本行不自行选」。
✅ **Shao Peishen 2026-08-28 已拍板 ⒜ ＋ 行内第 ③ 项**（理由＝只改文案与加一行 print、不动判定逻辑，当天可完；⒝⒞ 要动所有 session 共用的编辑锁，另排一批）。
🔴 **本件严格限定在「不改任何判定逻辑」之内**：
- ⒜：把「worktree agent 改自己那份 → ff 进 master → sweep 同步」这条**事实上已经在走**的路写进协议〇.7；并把 acquire／status 里那两条**走不通**的建议文案（「直接编辑绝对路径指向的文件」「改用 append-row」）删掉或改对。
- ③：给 工具-队列结构lint.py 加一行**运行时自报** —— 打印它这次实际校验的是哪个绝对路径。（本行成因就是：它从不说自己在看哪份文件，于是「lint 通过」这句话在 worktree 里说出来时含义与所有人以为的不同。）
🔴 **硬边界：本件只改提示文案与新增 print，不动 _resolve_repo_root／acquire／release／status 的任何判定逻辑，不动 :1063 的 G 闸。** 一旦发现要达成目的必须改判定逻辑（即 ⒝ 或 ⒞），**立即停手回报，不在本批做** —— 那要动所有 session 共用的编辑锁，须单独排一批并独占收工窗口。
⚠️ 触碰区提醒：工具-共享文档编辑锁.py 上今晨刚合入 #420 的修复（commit c6d6274 已在 origin/master）。改之前先确认你的基点含它，别覆盖掉。

【收工】
回写 §一 #426（写明选了 ⒜＋③、限定在文案与 print、⒝⒞ 未做及理由）与高水位线修正的落点；若开了「取号改为现算」的建议，另立一行。登记 §二 批次（先查同日已用）。
🔴 单列一行：「本次修改前后，§一／§四／业务场景三处高水位线各是多少」。
开干前问我 2-3 个澄清。
```

### `OP-0828-Q`【CC】`#425` ⑴⑶ —— 第 8 类告警两侧真实验活 ＋ 查清 reconcile 为何没兜住

```
[OP-0828-Q]【CC】#425 ⑴ 查清 reconcile 没兜住的原因 ＋ ⑶ 第 8 类告警两侧真实验活
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N ／ 看护者 OP-0828-N-W2
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828Q-425告警验活与reconcile根因

🔴 前置闸：OP-0828-G 建的第 8 类常驻告警（commit f629cb0，本地 master 独有提交告警）必须已随前置第 0 步落进 master。开工先核它在不在你的基点里；不在即停手回报。

读 ① 机制队列 §一 #425 全行（含 OP-0828-E／OP-0828-G 两次追记）→ ② #421（同族的单个实例，已销号）→ ③ 0-学习与工具/工具-落库sweep.py::_reconcile_with_origin_and_push ＋ 其单测。

【一 · ⑴ 查清 reconcile 为何没兜住】行内已由 OP-0828-G 查到一层：rebase 被孤儿脏文件（.claude/settings.local.json）拒跑，26 次里占 13 次。🔴 那是**一个**根因、不必然是**全部**根因——另外 13 次是什么，本件要答。判据：是没跑、跑了失败不报、还是判据不覆盖并行波次？三选一并给证据，不得只给推断。
⚠️ 已知陷阱（本项目实测栽过）：只信 stdout 会被骗；退出码只认被执行进程自己那一层（cmd /c 里的 %ERRORLEVEL% 在解析期就被展开）；「日志提及次数」不是「失败次数」，别拿它当分母。

【二 · ⑶ 两侧真实验活】🔴 **只验一侧等于没验**（本项目已因此栽过：一个告警建成 9 天、每天在跑，从来没真正发出过一条）。
① 构造一个真的「本地 master 独有提交」状态，看第 8 类告警**真的报出来一条**；
② 再把它对齐掉，看告警**真的自动解除、回落为 0**。
两侧都要有实跑输出贴进报告。🔴 靶子用隔离沙箱或一次性分支，**不得拿主工作区的真 master 做实验**（主工作区正被 sweep 每小时写）。
🔑 判据（写进报告）：一个「关不掉的告警」等于一个永远红着的灯，它比没有告警更坏——所以「能被关掉」这一侧才是本次验收的重点。

【边界】
- 🔴 不修改、停用、删除任何计划任务（含 ZhuopinCommitSweep）。要观察它的行为，用只读日志与隔离靶子。
- 🔴 不动主工作区的 master 与 .claude/settings.local.json —— 后者的停跟踪归 OP-0828-M 那条 session，别抢。
- ff push 前先 fetch；绝不 git pull、绝不 push -f。
- 🔴 实测与本件所述不符，以你的实测为准，停下来如实报。

【收工】回写 §一 #425（⑴ 的答案与证据、⑶ 两侧验活结果；🔴 本行**不销号**——它记的是「分叉会反复发生、需常驻监视」这个结构性问题，验活通过只说明监视装上了）。登记 §二 批次（先查同日已用）。
开干前问我 2-3 个澄清。
```

---

### `OP-0828-R`【CC】`#416` 企微回件链路 —— 先修元缺陷 ⑵，再查 ⑴⑶，顺带 ⑷⑸⑺

> **为什么加进本批**：`#416` 行内自陈「⑶ 正在出血——每一次专员回件都把队列锁死 30 分钟」，2026-08-26 当天两次咬到在办会话。Shao Peishen 2026-08-28 拍板加成第 4 条。
> ✅ **触碰区与 `OP-0828-P` 的重叠已被切开**（见 §三 末段）：本件**全部改动限定在 `5-平台底座/wecom-aibot-service/` 之内**。

```
[OP-0828-R]【CC】#416 企微回件链路——先修元缺陷 ⑵，再查 ⑴⑶，顺带 ⑷⑸⑺
【设置】执行环境：CC ｜ CC session：☑ 新开 ｜ worktree：☑ 新建独立 ｜ 分支：由你新建 ｜ 工作区：C:\Dev\zhuopin-ai ｜ 派出线：Cowork 环境总线 OP-0828-N ／ 看护者 OP-0828-N-W2
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0828R-416回件链路三缺陷

读 ① 机制队列 §一 #416 全行（含 2026-08-26 14:0x 对 ⑶ 真因的更正、Shao Peishen 新增的 ⑷⑸、以及并入的 ⑺）→ ② 5-平台底座/wecom-aibot-service/aibot_service/connection.py（:355-385 归档兜底／:515-540 转发兜底）→ ③ reports/wecom_aibot_audit.jsonl（13:18–13:21 窗口）与 reports/queue_edit_lock_bypass.jsonl → ④ 已备 opener：1-转型规划/0-全景路线图/opener集-迁移后机制审核批-2026-08-26.md 的 T2（OP-0826-E，⑺ 的修法）。

🔴【触碰区硬边界】本件**全部改动必须落在 5-平台底座/wecom-aibot-service/ 之内**。
- 特别地：⑶ 的真因在机器人侧，不在编辑锁侧 —— 行内 2026-08-26 更正已基本确定：机器人 release 时工作区有脏文件、不属任何待处理 §二 批次 ⇒ 触发登记完整性校验被拒，而机器人不会写「登记豁免」，于是锁挂满 30 分钟自动陈旧。修在机器人侧（补捕 stderr ＋ 让它能正确表达豁免或正确失败），**不要去改 0-学习与工具/工具-共享文档编辑锁.py —— 那是本批 OP-0828-P 的触碰区**。
- 若你判断非改编辑锁不可：**停下来回报，不在本批做**。

【顺序：先 ⑵，这是元缺陷】
⑵ connection.py:375/530 的 error=str(exc) 全部改为 f"{type(exc).__name__}: {exc}"，并在机器人调 release 处补捕 stderr。🔑 理由写进提交信息：asyncio.TimeoutError() 的 str() 就是空串 ⇒ 三次失败的 error 全空，它同时掩盖了 ⑴ 和 ⑶ 的真因。**先修它，否则下次仍是空 error。**
🔴 验收不能只看代码：造一个真的会抛无参异常的路径，看审计里那条 error 真的不再是空串（一侧）；再看正常路径没被改坏（另一侧）。**只验一侧等于没验。**

【然后 ⑶】用 ⑵ 修好后的 stderr 定位 release returncode=1 的真实分支并修复，使机器人能正常释放锁。
⚠️ 已知的推理陷阱（行内写着，咬的正是判断者自己）：**用「事后观察到的状态」去否定「事发时的可能原因」是无效推理** —— 排除一条原因前，须确认所用证据与事发同一时刻（原判断「当刻脏文件为 0 故排除」就是拿 14:00 的状态否定 13:19 的事）。

【然后 ⑴】media 下载/上传加重试与超时可配（现 5.0s ack 超时疑过短）；🔴 失败时**明确告知发件人重发**，不得静默。⚠️ 调大超时不是修复，要能重试并能报出来。

【顺带 ⑷⑸⑺，判不准就登记不做】
⑷ 转发给他的回件内容描述缺失——复原 08-24 的老格式（能分出是文档还是文字回复、带内容摘要）。行内有老格式原文，照它复原。
⑸ 文件入档时文件名带上跟进信编号——审计同一时刻已拿到 letter_number（如 财务部#15），信息就在手边、只是没写进文件名。
⑺ 归档↔队列对账哨兵对「Shao Peishen 本人入站」恒真误报——哨兵须复用 forwarding.should_forward **同源**判据（2026-08-24 拍板原文的明确要求），本人入站一律不计入漏行；并复核 queue_append_skipped 是否已在哨兵侧被读取。源码＝aibot_service/queue_reconcile_sentinel.py、scripts/run_aibot_service.py、tests/test_queue_reconcile_sentinel.py。🔑 它属「恒真判据、零信息量」族：一个永远红着的告警等于把哨兵训练成噪音。

【其余边界】
- 🔴 不对外发送任何消息（专员、真实群）。要验发送链路，靶子一律 ShaoPeiShen 自己的 chatid。
- 🔴 不修改、停用、删除任何计划任务；机器人服务如需重启，先确认它当前不在处理回件，并在报告里如实登记重启动作与时刻（标 UTC 还是本地）。
- ff push 前先 fetch；绝不 git pull、绝不 push -f；写队列走编辑锁、撞锁退避重试。
- 🔴 实测与本件所述不符，以你的实测为准，停下来如实报，不得替它改判。

【收工】回写 §一 #416（逐子项 ⑴⑵⑶⑷⑸⑺ 各写做没做、证据、留步理由）。🔴 **⑶ 修好前本行不得销号** —— 它是唯一正在出血的那条。登记 §二 批次（先查同日已用）。
单列一行：「⑵ 的两侧验活结果」「⑶ 的真实拒绝分支是哪一条、证据是什么」。
开干前问我 2-3 个澄清。
```

---

## 五、给 `[Win]0828M` 那条暂停中 session 的收口词（复制即用）

▶ **粘贴端：CC（`[Win]0828M-分支整备与落地前核`）**

```
四件我答了，按下面做完即收工，不要起新活：

① 主工作区 HEAD：切回 master。
② 6a81996 / 282eb13 怎么回 master：不需要挑，claude/op0828m-h-ready @282eb13 是 origin/master(1826529) 的直系后代、落后 0 ⇒ 一次 ff 就把 8 条全落地。做法：git fetch origin，再核 git merge-base --is-ancestor origin/master claude/op0828m-h-ready 为真、git rev-list --count claude/op0828m-h-ready..origin/master 为 0，然后 git push origin claude/op0828m-h-ready:master，最后把本地 master ff 到同一点。🔴 绝不 pull、绝不 push -f。
③ 本地 master 上的 8b16e64：留。已实测它是 282eb13 的祖先（git merge-base --is-ancestor 为真）⇒ ff 之后它自动进主线，不必单独处置、也不会丢。
④ 那 5 处 .claude/settings.local.json：按 G 的建议**停止跟踪**，不要各 commit 一次。就是你 OP-0828-M 原本那三件——git rm --cached ＋ 补 .gitignore ＋ 清全库那句「登记豁免：…为本机客户端本地件」样板文（队列历史行按「历史记录不追改」保留，只清 skill／模板／opener 正本）。前置（分叉双向 0）在 ② 做完后即满足。
🔑 一条实测给你：我逐个核过那 5 处 worktree，脏文件全部且仅是这一个文件，**没有任何真产出停在未提交状态** —— 所以这不是 5 份要抢救的产出，是同一个被跟踪的本机私有件显示了 5 次。

若 ② 的 master ff 又被分类器拦下：不要绕、不要 push -f，如实登记进 §一 #425 并停手，交 Shao Peishen 在本机 PowerShell 跑同一条命令。

收工：回写 §一 #425（对齐结果 ＋ settings.local.json 停跟踪落点），登记 §二 批次（先查同日已用），然后停。不要起新活、不要取新号。
```
