---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度→`进度编年-CHANGELOG.md`；教训→`取证方法知识库.md`；未闭合→队列行。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| — | — | — | — |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0911-H`、拆件巡逻班。09-13 05:0x 划掉 `OP-0913-E`。
> **无头批 `B-0913_收口三泳道` 已收**：`op0913b` ✅ 收工＋白名单自动 ff（`28f93ae`）；`op0913c` ✅ 收工＋授权 ff（`a4487bf`）；🔴 `op0913a` **被 600 s 后台上限杀死**，见 §二。

## 二、当前状态快照（2026-09-13 05:0x，master `26fc409`）

- **机制可动 WIP 32 → 24／22**（手段＝复用 `_mechanism_wip_counted_rows` 现取；逐号清单跑 `--digest` 取）。
- 🟩 **关八行**（答 `1a-6a`）：`459 534 552`／`521 545 566`（J1 载体先落 rules 两件 `90a1c60`）／`492`／`565`；`#416` 收编 `#545` 残余为 ⑻。
- 🔴 **`#554` 已可关、只差一字母**：现取 `merge-base --is-ancestor` 退出码 0、第 15 类实核在 master，⑴⑵与阈值 4 h 三项全到位，**行内「待 ff」是过时字样、已就地更正**。`#462` 等 09-14 观察窗满即关 ⇒ **两天内进 22**。
- 🔴🔴 **`op0913a` 被杀、活儿差点丢**：04:14:28 无头批 600 s 后台上限终止（卡在等差集回归），回 `OPENER_PARTIAL`，五件收工动作全未做。已代为分支提交 `751495e` 并 push（第 18 类 +194 行＋新单测件）。🔴 **绝不可 ff——差集回归从未跑完，零回归无手段**；下一棒须 rebase→重跑→再议 ff。
- 🔴 **收工探针漏判第三态**：04:16–04:18 回显 `[NO-SIGNAL]`，而 PARTIAL 04:14:28 已写盘（已追进 `#571`）。**`[NO-SIGNAL]` 只证明探针没看见，不证明没出事。**
- 🟩 **`.claude/rules/队列与落库.md` 11,367 → 8,150 B**（瘦身前全文存 CHANGELOG【附录 M】，83 条判据逐条核、缺 0）。⚠️ **余量只剩 42 B**。该 sweep 告警此前全库零承接行。
- 🟩 **`#568` 追 ⑵**：sweep 抽 commit message 只取「建议 message」格首处反引号跨度（实例 `bf4df2b` 落成「→ 闭环形态：」而实改 4 文件 63 行）。同触碰区并入，WIP 不增。
- 🟩 **代登四份孤儿行日志**（`#563.md` 曾是全库唯一副本）。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行。本会话新立行 ＝ 0。**

1. **`#554` 关行**（🟡 一字母）＋ **`#462` 09-14 关** ⇒ WIP 进 22。再往下 `#507`（一次 ff ＋ 在该分支重跑全量回归填掉 `{REGRESSION}` 占位符）、`#312`（观察窗＋D1/D2；🔴 4.6 的 7 天轨迹**量具没埋点、不可追溯**，须改判据或补 `row_ids`）。
2. **`op0913a` 续棒**：rebase → 重跑差集回归如实报数 → 再议 🟡 ff。
3. **机守替代人守三条最短路径**（可并入既有行、不增 WIP；全文见 `取证件-2026-09-13-WIP与机守替代人守-OP-0913-E.md`）：⑴ 收工探针补第三态并**先 commit 保命**（`#571`）；⑵ `工具-执行体对齐重启.ps1` 补进程新鲜度（`#570`，现只查 git、过了就报绿）；⑶ sweep 每条告警加必填 `consumer` 字段（`#568`）。
4. 🔴 **建议立为常驻纪律**：**新建任何告警／巡检必须同时指定消费者**（一条能执行的机器动作，或一个具名承接行／载体），两样都没有的不许上线；与 `#554`② 互为正反（那条管下线）。**待他一字母。**
5. ☒ 回件对账剩余两件（`财务部#16` 三条改判意见；B 表 `#7`／`#8` 矛盾须问唐燕萍）。🔴 对外发送，只做到可发送态。

## 四、在途与等他动作（只写指针）

- **等他动作**：① `#554` 关行一字母；② 第 4 条纪律立不立；③ 🔴 **09-14 09:30 前手动发 `质量部#14`／`财务部#18`，否则 `ZhuopinFollowupDispatchDaily` 自动发出去**；④ `#387`⑶ 群冒烟；⑤ §四 `#157`（无默认）／`#152`／`#156`／`#170`；⑥ P3 的 7.2/7.3 **要真发一封信才算数**（🔴 永不代办）。
- 🔴 **Cowork 工作区挂载通道 `device_bash` 已死**（09-08 Windows 更新所致）——**本机 PowerShell 通道实测全程可用**，本会话所有动作都走它。**不是「机器无法代」，是换了条通道。**
- **量具坑**：`--row B-…` 不前缀匹配；`--field` 只认 `status|all`；§四 列名是「事项」；🔴 PS 反引号是转义符 ⇒ 正文走 `--cells-json`／`--changes-json`＋单引号 here-string；🔴 **写进队列格的文本反引号必须偶数**（守卫拒奇数游程，本会话撞两次）；🔴 python 读写 `.md` 必须 `newline=""`。
- 🔴 **状态格 4096 B 硬闸**：追段后极易超，**一律走 `工具-队列行K2外置.py`**（plan→`--apply --expect-segments N`→`edit-row`→`--verify-json`），不裸手改；本会话连跑七行。
- 🔴 `Test-NetConnection` 本机恒真（TUN fake-ip）。🔴 审计行数不能判「已恢复」。**读数「太好看」先怀疑量具。**
- 🔴 **opener 守卫按文件粒度扫全文、不按本次 diff 粒度** ⇒ 只在尾部追加也会被中段历史块拦下（实撞 `CHANGELOG.md:629`），需写 `opener豁免：` 放行。
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash。并发会话可能已把你要报的发现做掉。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不写生产码、不发企微；产出登 §二 后**自行 commit+push**。配置类代执行须他明示授权＋回读实证。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 门禁；ASIL C/D 绝对禁区。**称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；🔴 **人的属性查 `人员名录-称谓与性别-正本.md`，禁从名字推断**（`姚祖怡` 男／`陈忱` 女／`唐燕萍` 女／`泓钦` 男／`陈承` 男）。
- 🔴 **代执行优先 ＋ 他不做人肉搬运**（细则在根 `CLAUDE.md` §5）：本方能做完的直接做、只报结果；🟡（ff／口径阈值／design 审／关他人行）停等一字母，🔴（对外发送／L2／合规／ASIL C-D）永不代办。
- 🔑 提缺口前先查承接行（`--grep`）；**没验证就下结论是本线最常犯的错**。**新告警必须自带消费者**（§三.4）。
- 🔴 opener 一律 Read `opener骨架.md`＋经生成器＋`lint --enforce`，禁手抄；**别用生成器空跑探号——它会真占号**。
- 🔴 **答复模板块里只放答案字面**，选项正文在块外，块正上方写去向会话；决策清单**回给出它的那条会话**。push 不是定夺项。
- 🔴 §二「文件清单」格**只放反引号包着的路径**；「建议 message」格**别写任何反引号**（`#568`⑵）。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史／进度 | `进度编年-CHANGELOG.md`（rules 瘦身前全文＝附录 M） |
| 任务真身 | `工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 看护批状态 | `工具-泳道看护状态机.py summary --batch <批次>` |
| 无头棒进展 | `reports/opener-batch/<戳>/*.log`；接管 `claude --resume <id>` |
| ff 授权 | 写 `reports/pending-ff.jsonl`（`branch`＋`authorized_text`），15 分钟巡检自动合 |