---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度叙事进 `进度编年-CHANGELOG.md`；教训进 `取证方法知识库.md` 或队列行；未闭合项写队列行。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| — | — | — | — |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0911-H`、拆件巡逻班。09-13 划掉 `OP-0912-E`（本卡由它重写）。
> **无头批在跑**：`B-0913_收口三泳道`（03:35 起，日志 `reports/opener-batch/20260913-收口三泳道/`）——`op0913a`／`op0913b`／`op0913c`，看护件同名在 `0-全景路线图/`。

## 二、当前状态快照（2026-09-13 03:4x，master `ec8965c`）

- **全景 35 场景**。机制可动 WIP **32/22**——**一整天没动过一格**；`STALE(7)=0/3`（09-12 21:3x 回显），告警期。
- 🔴🔴 **WIP 降不下来的直接证据**：32 行里 **10 行引用的分支已全在 master**（`#312 #492 #521 #545 #552 #554 #556 #563 #565 #566`），另 4 行部分在（`#459 #462 #507 #534`）。**不是活没干完，是行没关。** 🔴 但「分支在 master」**≠**「可关」——三分表由 `B-0913` A2 出，只出表不关行（关行属 🟡）。
- 🟩 **openspec 三包 17 决策点当日全签、全 apply、全 ff**：P1 `a6f75dc`／P2 `4d14028`／P3 `5b2ff92`（六道守卫全过、四 ref 每次一致）。四个包已归档（三包＋`env-anchor-collapse`）。P1 的派出前校验已实弹生效（`B-0913` 干跑现场 fail-open 照常派出）。
- 🟩 **§四 `#197` 了结**：`#162` 网关代码进 master（`21a21a4`），实跑 **84 passed**。🔴 `.51` 跑的仍是 `sync-to-server.ps1` 直推副本——**「master 有源码」≠「`.51` 跑的是这一版」**，后者未核。
- 🔴🔴 **`#570` 当日重演，本方造成**：listener 起于 `13:10:20Z`，`#556` 节流修复 `19e02c7` 落 master 在 `13:11:10Z`——**晚 50 秒**；`工具-执行体对齐重启.ps1` 只查 git，报「已对齐、无需处置」**并不重启**。已手工 `Stop/Start-ScheduledTask ZhuopinAibotDevListener` 复位（新进程 03:27:11），实证新码已活（审计出 `outbox_relay_scan_unreadable_started`、状态文件已生成）。🔴 **工具未修、`#570` 仍开着；本轮三条 ff 后该 worktree 又落后，下次收口须自己核进程新鲜度，别信工具的绿。**
- 🟩 **ff 全机器化 ＋ 低风险白名单已启用**：往 `reports/pending-ff.jsonl` 写含 `branch`／`authorized_text` 的授权行即可。未合 `origin/claude/*` **134 → 67**。**授权仍须他一字母。**
- 🔴 **新发现未立行**：sweep 把 §二「建议 message」格里**反引号包着的一段**当成整条 commit message——`bf4df2b` 的 message 只有「→ 闭环形态：」，落的却是 `质量部#7` 真身回改。与 `#513` 同族，**第二次**。WIP 超限未立行，下一会话定夺。
- **三处真红**：`#556`（9.3 由 `B-0913` A1 做；`#394` SC2 真实送达仍 L1/L2 留步）／决策台账缺口 9 行／`#561` 收口段分诊本体未做。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行。**

1. **关行降 WIP**——等 `B-0913` A2 的三分表，逐条判、关可关的（🟡 须他一字母）。**唯一能让 WIP 真动的动作。**
2. **`#570`／`#571` 两个事件驱动缺口**——「全自动事件驱动替代人守」的正体。`#570` 今日实证；`#571` 的证据是 09-12 整晚：泳道跑完之后**轮询、写授权、ff、关口、回写全是人在做**。**补上之前「全自动」不成立。**
3. **☒ 回件对账剩余两件**：`财务部#16` 三条「判不对」改判意见；B 表 `#7` 与 `#8` 互相矛盾（跨两料号「一律不认领」vs「拆成两条」都勾判对），须问唐燕萍。🔴 对外发送，只做到可发送态。
4. **sweep commit message 抽取缺陷**（见 §二）立不立行。

## 四、在途与等他动作（只写指针）

- **等他动作**：① `#387`⑶ 告警真实群冒烟；② 🔴 **09-14 09:30 前手动发 `质量部#14`／`财务部#18`，否则 `ZhuopinFollowupDispatchDaily` 自动发出去**；③ §四 `#157`（无默认）／`#152`／`#156`／`#170`；④ `B-0913` 三条收工后的 ff（🟡）；⑤ P3 的 7.2/7.3 **要真发一封信才算数**（🔴 永不代办）。
- **量具坑**：`--row B-…` 不前缀匹配；`--field` 只认 `status|all`；§四 列名是「事项」；🔴 PS 反引号是转义符 ⇒ 正文走 `--cells-json`＋单引号 here-string；🔴 **python 读写 `.md` 必须 `newline=""`**；生成器输出已含 CRLF，勿再整体转换。
- 🔴 `Test-NetConnection` 本机恒真（TUN fake-ip）。🔴 **审计行数不能判「已恢复」**——进程休眠会让小时桶出现 0（09-13 实撞）。🔴 **`工具-执行体对齐重启.ps1` 只查 git、不查进程新鲜度**，绿 50 秒后就可能过期。**读数「太好看」先怀疑量具。**
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash。并发会话可能已把你要报的发现做掉。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不写生产码、不发企微；产出登 §二 后**自行 commit+push**（编辑锁三道守卫照跑，sweep 兜底）。配置类代执行须他明示授权＋回读实证。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。**称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；🔴 **人的属性查 `6-人才与组织/人员名录-称谓与性别-正本.md`，禁从名字推断**（`姚祖怡` 男／`陈忱` 女／`唐燕萍` 女／`泓钦` 男／`陈承` 男）。
- 🔴 **他不做人肉搬运**：唯二可交给他的＝可复制粘贴的命令行、必须提权的 shell。
- 🔑 提缺口前先查承接行（`--grep`）；**没验证就下结论是本线最常犯的错**。
- 🔴 opener 一律 Read `opener骨架.md`＋经生成器＋`lint --enforce`，禁手抄。🔴 **别用生成器空跑探号——它会真占号**（09-12 一次占掉 6 个）；用它撞号时回的「下一个空号」，或读 `reports/op-id-claims.jsonl`。
- 🔴 **答复模板块里只放答案字面**（`1a`），**选项正文一律在块外**，块正上方写去向会话。**判据：两三个字符的块没人会误以为要粘；塞满选项的块加多少说明也救不回来**（09-13 两撞，第二撞是「加一行不用粘贴」这个错修法）。全文见 `opener骨架.md`「一次粘贴」节。
- 🔴 决策清单**回给出它的那条会话**（他常同时开两条 Cowork）。push 不是定夺项。
- 🔴 §二「文件清单」格**只放反引号包着的路径**；`openspec archive` 整包改名在 `git status` 里是 `R old -> new`，**守卫只认新路径**。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史／进度 | `进度编年-CHANGELOG.md`；`…-归档-202608.md` |
| 本线盘面 | `reports/在跑批次册.md`（不进 git） |
| 任务真身 | `工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 看护批状态 | `工具-泳道看护状态机.py summary --batch <批次>` |
| 无头 CC 进展 | `reports/opener-batch/<戳>/*.log`＋`summary.txt`；接管 `claude --resume <id>` |