---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度→`进度编年-CHANGELOG.md`；教训→`取证方法知识库.md`；未闭合→队列行。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0913-E` | [Win]OP-0913-E 环境总线接棒 | 队列／rules／合入链路工具链／轮询守 | 09-13 05:0x—23:5x |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0911-H`、拆件巡逻班。
> 当日无头批全收：`B-0913_人守打通`／`B-0913_合入链路留痕`／`B-0913_登记册落点与回归解闸`／`B-0913_合入登记自提交`／`B-0913_轮询守`／`B-0913_轮询守实机三修`。

## 二、当前状态快照（2026-09-13 23:5x，master `b740ec6`）

**主线＝构建闭环 18 环人守盘点。第一轮 7 处缺口 → 收官对账关 2 剩 5，其中 1 处建议不列为缺口。**
**收官件：`取证件-2026-09-13-构建闭环人守盘点第二轮收官-OP-0913-E.md`（全貌看这份）。**

- ✅ 当日关闭：**第 6 环收工检测**（`#571` ⑶⑷⑸，真批次 `20260913-094209` 验证）、**第 11 环巡检结论落盘**、**第 12 环登记册销行**（`a6d37b5` 建机制、`b1ebcce` 迁落点入库）、**第 14 环 ff 后执行体重启判据**（`#570`）。
- ✅ 当日新建：§四 `#199` 合入登记自提交（`4ba8a6d`）、`#575` 甲轮询守（`99bd05b`）＋实机三修（`c3b1b13`）。
- ✅ **ff 授权正本已入版本控制**：`1-转型规划/0-全景路线图/合入登记/`，当日 `ff-patrol-20260913.jsonl` 累计 50+ 行。**当日 09:46–09:55 曾真实无痕丢失过一次**（取证件 `ff登记册无痕消失`），该洞已闭。
- 现取：可动 WIP **25／22**；`git worktree list` **23 条**（9 条 HEAD 全零可直接 prune）。

🔴 **待他手动两步，`#575` 甲才真生效**（命令见【四】）：管理员重跑注册脚本 ＋ 触发一次验收。**验收通过前 Cowork 桌面端 15 分钟任务不停。**

## 三、下一会话主攻（环境总线）

**仍缺四处**（第 1 环选行排波建议不列为缺口——与 ff 授权同类，判断权本就在他）：

1. **第 13 环 worktree 清理** —— 纯机械、零依赖、9 条现成可 prune。`#560` 已 `done` 但只覆盖「子任务泳道 opener 自删」一个产出方，与本环不同源。**建议先做这个。**
2. **第 8 环 `#574`** —— 产出 `工具-ff前置核验.py`，退出码非 0 即不得写授权行；顺手把「差集回归按触碰面收窄」并进去。
3. **第 7 环收工→收口**（`#571` 首段）—— 最大一块，建议单独设计。
4. **第 18 环告警消费者** —— 等 `#568` 解冻。

**新发现、仍无承接三条**（详见收官件 §三）：① 合入前置的脏文件比对只取分支尖端一个 commit（`git show --name-only $br`，实证 `op0913m` 回显「触碰 1 文件」实为 4）；② master 上 `test_工具-落库sweep.py` **15 条常驻失败**、全是告警类；③ **ff 巡检调度落点未明**——按时在跑却既不在 `list_triggers` 也不在 `Get-ScheduledTask`。

🔴 **收官件判定的第二大缺口**：**「验收＝第一轮真跑」当日被证明三次，而它本身无机器守。单测证明代码对，第一轮真跑才证明环境对。**

## 四、在途与等他动作（只写指针）

- 🔴 **时限**：`质量部#14`／`财务部#18` 手动发窗口到 **09-14 09:30**，过点 `ZhuopinFollowupDispatchDaily` 自动发。
- 🔴 **`#575` 验收两步**（管理员 PowerShell）：
  `pwsh -NoProfile -ExecutionPolicy Bypass -File "C:\Dev\zhuopin-ai\0-学习与工具\工具-注册轮询守计划任务.ps1"`
  然后 `Start-ScheduledTask -TaskName ZhuopinPollGuard`。
  **判据两条缺一不可**：`reports\poll-guard\poll-guard-<日期>.jsonl` 新增一行；任务计划程序 Operational 日志 `id=201` 耗时**不能是 1 秒**（对照交互式 94.8 秒）。
- **`#575` 乙（事件触发根治）未起**，硬前置＝先把 parked 的 `claude/op0913f-v2-launcher-json-571` 放回登记册合掉，否则动 `工具-opener批处理执行v2.ps1` 必撞。
- **09-18 换量具 5.1 复评**：定时任务 `trig_01QTeGjzuoNtJgmqvaRR4i6J` 已绑定本机。🔴 取龄子进程当日实测 **32.9 s 超 30 s 上限降级放行**（`#58` §4.3bis），新闸现为 fail-open。
- `claude/op0913a-556-sweep-visibility` 内容已被 `op0913m` 全部承接，**勿单独 ff**，留每周清扫。
- `#522` 等 `#570` 关行、WIP 落位后解冻（他答乙）。`#462` 观察窗 09-14 满。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **`device_bash` 挂载已死**，一律走 `mcp__remote-devices__Windows-MCP__PowerShell`；`Filesystem` MCP 不可用。
- 🔴 **汇报体裁三条**（`.claude/rules/环境保障线.md`）：代码块只给要他粘贴执行的；**定夺项三件套**（是什么／逐项选项与代价与推荐／复制即用答复模板）；**定夺不等复核**——复核挡的是手，不是他的字母。
- 🔴 **预授权是对具名分支的**，换一份看护件就得重新要（当日已因此拦下本方一次自授权）。
- 🔴 **换机制前先证覆盖面是超集**；**新建告警必须自带消费者**。
- 🔴 **相关不是因果**（当日实证）：查了四条证据不等于建立因果链，**先问「有没有一个反例能推翻它」**——本方曾据 403 ＋ GCP 出口 IP 断言代理致拒，而 IP 全程未变、状态却从 403→周限额→正常，真因在账号侧。
- 🔴 **K3 读队列**：绝不 `Read`／`grep` 队列真身，只用 `工具-队列查询.py --row/--digest`；状态格 >4096 B 走 K2 外置。
- 🔴 **起跑一律 `pwsh -NoProfile`、不传 `-LogDir`**；**差分回归口径＝分支失败集 ⊆ 纯 master 失败集**，不是全绿；长回归 `Start-Process` 脱开超时。
- 🔴 **WindowsApps 下的 exe 可能是 0 字节 ReparsePoint 应用执行别名**，S4U 解析不了——解析可执行文件一律校验 `Length`／`ReparsePoint`。
- 🔴 定时任务一律用 `mcp__claude-code-remote__*`，**绝不用本地 Cron 工具**；绑定不能事后补，**传 `folders` 反而附不上，只传 `requires_local_device: true` 且他在本机时才绑得上**。

## 六、往哪查

- **收官全貌**：`取证件-2026-09-13-构建闭环人守盘点第二轮收官-OP-0913-E.md`。
- 当日三份取证：`18环人守盘点`（第一轮逐环）／`ff登记册无痕消失`（七条实测）／`WIP与机守替代人守`。
- 换量具读数与追因：`队列行日志/#58.md` §4.4、§4.3bis。
- 六条 ff 核验原文：`看护件-【CC】人守打通两泳道-2026-09-13.md` §五（⑵⑸ 已更正）。
