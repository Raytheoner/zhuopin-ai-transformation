---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度→`进度编年-CHANGELOG.md`；教训→`取证方法知识库.md`；未闭合→队列行。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0913-E` | [Win]OP-0913-E 环境总线接棒 | 队列／rules／合入链路工具链 | 09-13 05:0x— |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0911-H`、拆件巡逻班。
> 无头批全收：`B-0913_人守打通`（op0913f/g）、`B-0913_合入链路留痕`（op0913j）、`B-0913_登记册落点与回归解闸`（op0913l PARTIAL／op0913m OK）。

## 二、当前状态快照（2026-09-13 13:1x，master `f4d775f`）

**今日主线＝18 环构建闭环人守盘点，真缺口 7 处 → 剩 5 处。**

- ✅ **⑪ 巡检结论落盘 ＋ ⑫ 登记册销行**已关（`a6d37b5`），且当场 dogfood 在它自己的合入上：`合入登记/pending-ff.done-20260913.jsonl` 保住授权原文、`ff-patrol-20260913.jsonl` 记下销行，逐字回读核对。巡检当日真跑两轮（10:47／12:50）。
- ✅ **⑥ 收工检测**已关（`#571` ⑶⑷⑸，探针批目录正则），并在真批次 `20260913-094209` 验证（`done_notified` 10:17:04）。
- ✅ **⑭ ff 后执行体重启判据**已关（`#570`，进程新鲜度）。
- 🔴 **ff 授权登记册曾无痕消失**（09:46–09:55，七条实测无法归因）——根因＝已入库的 `工具-待合分支巡检.ps1` L301 销到零行即 `Remove-Item`、两条销行路径不归档。取证件已落库。
- 可动 WIP **24／22**（`#574` 走逃生阀立行）；换量具包 `mechanism-wip-staleness-gate` **29/34**，告警期满 **09-18**。

**待合三支，全部 🟡 待他一字母**（绝不沿用既往预授权——预授权是对具名分支的）：

| 分支 | 内容 | 核验状态 |
|---|---|---|
| `claude/op0913l-ledger-relocate-571` | 登记册迁 `1-转型规划/0-全景路线图/合入登记/`＋`.gitignore` L56 目录级否定 | 本方独立四查全过（入库 5 份／blob 等同／运行时件仍忽略／否定行在 L32 后） |
| `claude/op0913m-a556-regression-556` | `op0913a` 差集回归跑到底＋`#556`② 接进 sweep 第 18 类 | 两侧失败集各 19 条 md5 同、comm 双向空 ⇒ 零回归（**只对 onto `e204df2` 成立**） |
| `claude/op0913a-556-sweep-visibility` | `751495e`，内容已被 `op0913m` 全部承接 | 留每周清扫处置，勿单独 ff |

## 三、下一会话主攻（环境总线）

1. **收三支 ff**（等他字母）→ `op0913m` 合入后补 `#570` 剩半 → 关 `#570` → WIP 降。
2. **§四 `#199`**：登记册入库后巡检每写一行即成未登记脏文件、3 h 触发 sweep 孤儿告警。承接二选一 (a) sweep 自动本地 commit（须等 `op0913m` 合入后才能动 `sweep.py`）／(b) 巡检脚本自 commit。**无默认、未代建。**
3. **剩 5 处缺口**：① 选行排波（**不建议机器化**，与 ff 授权同类，本就该是人）／⑦ 收工→收口（`#571` 首段，最大一块）／⑧ 合入前六条核验（`#574` 已立，产出＝`工具-ff前置核验.py`，退出码非 0 即不得写授权行）／⑬ 临时 worktree 清理（`finally` 只管留痕不管 worktree，`git worktree list` 现有 21 条陈迹、8 条 HEAD 全零可 prune）／⑱ 告警消费者机器守（`#568` 🛑）。
4. **09-18 换量具复评**（`#58` 5.1）：🔴 定时任务已建但**未绑定他的电脑**（`no_signed_approval`，只会云端跑、够不着仓库），需他批一次重建。

## 四、在途与等他动作（只写指针）

- 🔴 **时限**：`质量部#14`／`财务部#18` 手动发窗口到 **09-14 09:30**，过点 `ZhuopinFollowupDispatchDaily` 自动发。
- 三支 ff 一字母；§四 `#199` (a)/(b)；09-18 定时任务绑定。
- `#522`（派单件 ff 断言机器守）：他答**乙**——等 `#570` 关行、WIP 落位后再解冻派件。**`#522` 补不上第 8 环**，两者对象与触发时机不同。
- `#462` 观察窗 09-14 满。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **`device_bash` 挂载已死**（Windows 更新 2026-09-08），一律走 `mcp__remote-devices__Windows-MCP__PowerShell`；`Filesystem` MCP 不可用。
- 🔴 **汇报体裁**：代码块只给「要他手工粘贴执行」的东西（shell／Opener／答复模板）；告知汇报状态表一律普通文字——看护者见拷贝按钮的第一反应是「这是要我执行的」。
- 🔴 **预授权是对具名分支的**，换一份看护件就得重新要一个字母（今日已因此拦下本方一次自授权）。
- 🔴 **换机制前先证覆盖面是超集**；**新建告警必须自带消费者**（`.claude/rules/环境保障线.md`）。
- 🔴 **K3 读队列**：绝不 `Read`／`grep` 队列真身（单行可达 78 KB），只用 `工具-队列查询.py --row/--digest`；状态格 >4096 B 走 `工具-队列行K2外置.py`。
- 🔴 **起跑一律 `pwsh -NoProfile`、不传 `-LogDir`**（`#567`：`-Detach` 继承调用方 shell）。
- 🔴 **差分回归口径**＝分支失败集 ⊆ 纯 master 失败集，**不是全绿**；长回归分步跑、别丢后台（600 s 上限杀过一次）。
- 🔴 **回归时长逼近调度周期即空转**：`op0913f` 曾连 rebase 四次未合一次。派件写死「只 rebase 一次、记 onto sha」。
- 🔴 定时任务一律用 `mcp__claude-code-remote__*`，**绝不用本地 Cron 工具**（随 session 结束丢失）。

## 六、往哪查

- 取证件：`取证件-2026-09-13-构建闭环18环人守盘点-OP-0913-E.md`（18 环逐环）／`取证件-2026-09-13-ff登记册无痕消失-OP-0913-E.md`（七条实测）／`取证件-2026-09-13-WIP与机守替代人守-OP-0913-E.md`。
- 换量具读数与追因：`队列行日志/#58.md` §4.4（含 09-13 段：`STALE` 七次读数从未 > K、D-A 缺「混合」第三档）。
- 看护件：`看护件-【CC】合入链路留痕-2026-09-13.md`／`看护件-【CC】登记册落点与回归解闸-2026-09-13.md`（§五六条原文在 `看护件-【CC】人守打通两泳道-2026-09-13.md`，⑵⑸ 已更正）。
