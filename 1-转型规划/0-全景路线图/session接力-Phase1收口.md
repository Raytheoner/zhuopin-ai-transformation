---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `取证方法知识库.md` 或队列行；未闭合项写队列行。迁出守 J1。收工自检 `python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 超限当场删。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0910-H` | Cowork 环境总线接棒（09-11 续用） | 本卡＋两份队列（经锁，§一 387/436/454/505/542／§二）＋ `.gitignore` ＋ `register-*.ps1` ＋ 映射表注释 ＋ 派单件 | 09-10 10:1x – |
| `OP-0911-N` | 【CC】措施C-apply批1（本线派出） | `工具-共享文档编辑锁.py` ＋ 其单测（子泳道 worktree 内） | 09-11 19:5x – |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0911-H`、拆件巡逻班。

## 二、当前状态快照（2026-09-11 19:5x）

- **全景 35 场景**（五个编号空置不得重分配）。机制类可动 WIP 09-11 09:55 实测 **27/22**，🛑 认两列后 **23/22 仍超 1**。
- **措施 C 换量具已 propose＋design 并 ff**（`OP-0911-D`：`01ac018`，`validate --strict` 过，分支 ahead 0）。六决策点 09-11 答 `2a` 全按推荐签认；**N=7／K=3** 实测定死（原推荐 N=10/K=5 永不触发＝悄悄废闸）。apply 批 1 已派 `OP-0911-N`；批 2（切阻断）须过 D-D 三前置＋他一字母。
- 🔴 **WIP 分诊首跑即出错、已全部回滚（09-11）**：只凭 `triage-candidates` 强档就改判，**5 条里 4 条是误报**，命中的全是行内**历史引文**。硬纪律进 `#454`：候选永远只是候选，改判前逐条读正文、写出「声明还是引文」的判据。
- 🔴 **🛑 排除判据只认状态列**，而 `#381`／`#382`／`#447`／`#448`／`#505` 把 🛑 写在**任务列** ⇒ 误计入。修法并入换量具包 D-B。
- **`#387` ⑵⑷ 已闭**（09-11）：⑵ `chatid` 反查完成＝确为**运维部AI保障群**（企微只读三证：全局搜索正文只命中该会话／群内时间戳与审计 `archived` 同分钟吻合／群成员含机器人与陈承）；⑷ 实测 0 B、已自然消解。**剩 ⑶ 告警真实群冒烟＝对外发送，永不代办。**
- **启动包装脏源已根治**（09-11）：`Set-Content -Encoding UTF8` 产物**随 host 分叉**（实测 PS5.1 带 BOM 49 B／PS7 46 B），三份 `register-*.ps1` 统一改 `WriteAllText`＋`UTF8Encoding($false)`；annual 那份补进 `.gitignore`。🔴 **未重跑注册脚本验产物**。
- 🔴 **`git rm --cached` 对常驻执行体是陷阱**（沙箱实证）：移出跟踪后 ff 会**把文件从 worktree 磁盘删掉**（`.gitignore` 救不了）；该份本地有改动则 ff **直接被阻断**。两条路都断服务。
- **三处真红**：⑴ `#556` outbox 中继红 11 天（下一主攻）；⑵ 决策台账缺口 9 行；⑶ openspec 收口段堵塞（在办 75／归档 62，25 包 >14 天无改动；`#462` 已证 tasks 勾选不可信）。
- **机制端体检（09-11）全绿**：五个计划任务健康（`ZhuopinAibotDevListener` 的 `267009`＝`SCHED_S_TASK_RUNNING`，**不是失败**）；sweep 15 类全跑、状态件新鲜；规划倒逼机制正常（已承接 24 场景）。
- ⚠️ **Session 0 有 40 个孤儿 `git.exe`**（09-11 08:06–08:11 起，0% CPU 11 h，父进程 38/39 已退出，合计 215 MB）。**不是 Claude 桌面端起的**（claude.exe 全在 Session 1）；命令行跨 session 读不到 ⇒ 起因未实证，须提权复查。仓库无 `index.lock`、`git status` 0.13 s 正常。
- **`队列回写待补/`** 余 5 份。🔑 K2 外置会把回写段搬进 `队列行日志/`，**真身里找不到≠没落**，须二轮比对行日志。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行。**

1. **`#556` outbox 中继红 11 天**——最出血的一条；定性 09-11 已更正（真因是 off-LAN，不是 SMB 认证）。待领取方与修法（off-LAN 常态下中继该不该自知并静默）。
2. **跟踪 `OP-0911-N`**：收工后核三信源（transcript／队列行／git）；ff 属 🟡 档，等 Shao Peishen 一字母。
3. **`#439` §2 续**：2.2 P1 判别式补拆闭环信；2.3 交他批改时一并给两处矛盾。
4. **`#436`⑵ 入收口批**：前提已满足，交 `zhuopin-lan-closeout`，本线只跟踪。

## 四、在途与等他动作（只写指针）

- **等他动作**：① `#387`⑶ 告警真实群冒烟（对外发送）；② `OP-0911-N` 收工后的 ff；③ §四 `#194`／`#157`（无默认）／`#152`／`#156`／`#170`；④ Session 0 孤儿 git 是否清理。
- **登记豁免**逐路径点名＋作者＋到期（`#416`⑶），`release --waiver` 可中途补；人名后 25 字内出现「他」会被称呼 lint 拒。
- **量具坑**：`--row B-…` 不前缀匹配；`--field` 只认 `status|all`；🔴 PowerShell 反引号是转义符 ⇒ `--cells-json` 正文用单引号 here-string 写脚本再跑；🔴 §二 文件清单里任何反引号串都按路径校验，`#N`／`[D:业]` 这类记号不能加反引号；🔴 圈码 ⑴⑵⑶ 是 U+2474/5/6，别写成 ❶❷❸。
- 🔴 **`Test-NetConnection` 在本机恒为真**（Mihomo/Meta TUN，`198.18.0.1` fake-ip），不得当可达性判据（三个地址全 True）。**读数「太好看」也要怀疑量具。**
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17）。配置类代执行须他明示授权＋回读实证。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。**称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；人的属性查名录正本、不推断。
- 🔴 **他不做人肉搬运**：「去某处点一下／下载一个文件／复制到某目录」不得作为建议；**唯二可交给他的＝可复制粘贴的命令行、必须提权的 shell**。先穷举通道（PowerShell／计划任务／`computer_*`／浏览器）、**实测不通再说不通**。
- 🔑 提缺口前先查承接行（`--grep`）；读数太干净先怀疑量具。🔑 **开工先看 `git status --porcelain` 的未跟踪件**——并发会话常已把你要报的发现做掉。🔴 **跨日会话每天重读根 `CLAUDE.md`**。
- 🔴 opener 一律 Read `opener骨架.md`＋`工具-opener生成.py`（>500 字有文件 ⇒ `--variant reference`）＋`lint --enforce`，禁手抄；短名 ≤12 字。🔴 **桌面端 worktree 勾选框一律留空**，`☑／☐` 由会话自己执行。
- 🔴 决策清单末附答复模板（`1a，2a`）且**模板必须单独成一个代码块**；push 不是定夺项；会话不自醒，说「自动」必须当场指名机器规则。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md`；进度 `进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch <批次>` |
| 无头 CC 进展 | `reports/opener-batch/<时间戳>/*.log`＋`summary.txt`；接管 `claude --resume <session-id>` |
