---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **本文件是一张定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工时「覆盖」六块，不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `0-学习与工具/取证方法知识库.md` 或队列行；未闭合项写队列行。迁出前守 J1（先指承接载体）。收工自检：`python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 超限当场删。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

约定：每次新开 session 都用「读上下文文件交接」，不粘长 prompt。

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0910-H` | Cowork 环境总线接棒（转场件 `开场prompt-【Cowork】环境总线接棒-2026-09-10.md`） | 本卡＋两份队列（经锁，§一 542／§二）＋ `队列回写待补/` ＋ `reports/lane-watch-state.json` | 09-10 10:1x – |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0910-A`、看护者 `OP-0910-G`。

## 二、当前状态快照（2026-09-10 10:4x）

- **全景 35 场景**（五个编号空置不得重分配），S1 目标 9-11。**机制类可动 WIP 26/22 超限**（10:3x `_count_mechanism_wip` 实测；`#545` 出血行带 `WIP豁免`）——🛑 排队 `#381`／`#382`／`#448`／`#464`／`#505`／`#526`。
- **WIP 分诊已出候选（`triage-candidates` 只读跑，未改任何行）**：`#455`／`#482`／`#487`／`#505`／`#531` 五条现 `open|partial` 但正文均自陈「待 Shao Peishen 一字母」⇒ 建议改判 `blocked`（`blocked` 不计 WIP）；全改则 26→21，闸自解。**待他一次答复，未答不得裸改。**
- **陈忱 8D 样本（`#540` 业／§四 `#193`）**：三个 zip 09-10 09:21 与 10:14 两轮重发**全败**；审计 `media_transfer_retry` 实测 `timeout_seconds=600`（环境变量已生效）但每 attempt 仍 ~10-12 s 抛裸 `TimeoutError` ⇒ 真因＝SDK 内层 `WSClientOptions.request_timeout` 默认 10000 ms 未透传（`aibot/api.py` 的 `aiohttp.ClientTimeout(total=10 s)`），外层 600 s 永远等不到。**已立 §一 `#545` ＋ 派单件 `OP-0910-J`**；取件改走微盘链接（需企微登录态，机器无法代）。
- **K2 行长（09-11 转阻断）**：全池实测 **28 格超 4096 B**（§一 23／§四 5；其中 2 格带 `行长豁免`）——与 `#443` 派单件「清扫吸收 7 条 ＋ 外置 21 行」一致，已由 **棒 1 `OP-0910-K`（清扫）→ 棒 2 `OP-0910-P`（K2 外置）** 串行承接（他 09-10 答 `1a，2a`）。🔴 **订正**：`#439` 状态列现 **1994 B**、早已不超限，接力卡与转场件旧述「`#439` >4096 B」作废。
- **泳道记账**：`358-audit-retention-p1p2`／`358-p1p2-cc` 已补终态（`heartbeat --done --batch B-0908_B`）。**另发现 3 条仍 `running` 的滞后泳道**：`436-worktree-drift-alert-clear`（已 ⏭️ 转出 `B-0907_Q`）、`502-sc2-smoke`（已 ⏭️ 转出、`#502` 已 done）、`A`（2026-09-07 dry-run 测试残留，`evidence_ref=reports/xxx-证据.md`）——状态机无 delete 子命令，待定。
- **`队列回写待补/`**：`B-0908_AL4-*` 五份已逐件核毕后删（四份载荷已落队列；第五份 §二 批次行从未登记，但其三件产物经 `git cat-file -e HEAD:` 实证已在 HEAD）。**余 23 份未核**（`B-0906_X`／`B-0907_R|S|Y|Z`／`B-0908_B`／`B-0910_E|五泳道全启动`）。
- **定时任务**：`zhuopin-lan-closeout-reminder` 工作日 09:10 只读核四判据。`.51` 项一律 CC（Cowork 无 `ssh.exe`）。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行（26/22）。**

1. **WIP 分诊落地**：他答复后按 §二 快照那五条一次性改判（`edit-row --changes-json` 整格重写，🔴 09-11 起 `#455`／`#482`／`#487`／`#505` 均超 4096 B，须先等 `OP-0910-P` 外置完再动，否则 release 被行长闸拒）。
2. **滞后泳道三条处置**：两条 ⏭️ 转出项核实去向后补终态；`A` 测试残留需状态机加 delete 或人工改 `reports/lane-watch-state.json`（该文件 gitignore，不入 §二）。
3. **`队列回写待补/` 余 23 份逐件核销**：同 AL4 手法（载荷首 70 字探针比对真身 ＋ `git cat-file -e HEAD:`）。
4. **`#439` §2 续**：2.2 P1 判别式补拆闭环信；2.3 交他批改时一并给两处矛盾。
5. **不归本线**：`#545`（`OP-0910-J`）／`#443` 清扫与 K2 外置（`OP-0910-K`/`P`）／`#538`／`#539`／`#499`／`#542`／FI5 grill 由各自承接线。

## 四、在途与等他动作（只写指针）

- **等他动作**：① WIP 分诊五条改判（本卡 §二，⟨就地答⟩）；② 微盘下载陈忱三包放入 `7-外部文档/质量部/`（§四 `#193` 二次改判，交期 09-11）；③ §四 `#194`／`#157`（无默认）／`#152`／`#156`／`#170`。
- **登记豁免**须逐路径点名＋作者＋到期（`#416`⑶），`release --waiver` 可中途补；一行内「陈忱」后 25 字内出现「他」会被称呼 lint 拒，写 `Shao Peishen`。
- **量具坑**：`--row B-…` 不前缀匹配；`--digest --section 二` 不支持；`--field` 只认 `status|all`；🔴 **PowerShell 里反引号是转义符**——`--cells-json` 的正文要用单引号 here-string 写脚本再跑，别用双引号内插；🔴 §二 文件清单里**任何反引号串都会被当成路径校验**，`#542`／`[D:业]` 这类记号不能加反引号。
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash；ff-only＋rebase 下旧 SHA 永非 master 祖先，内容闸用 `git cat-file -e origin/master:<path>`。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不碰真实库、不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17）。配置类代执行须他明示授权＋回读实证＋登 §二。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。
- **称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；人的属性查名录正本、不推断。
- 🔑 提缺口前先查承接行（`--digest` 截 40 字，并入审核用 `--grep`）；🔑 读数太干净先怀疑量具；管道末端退出码不是命令的退出码。🔑 **开工先看 `git status --porcelain` 的未跟踪件**——并发会话的派单件/看护件常已把你要报的发现做掉了（09-10 实撞：`#545` 与 K2 两棒均已在场）。
- 🔴 opener 一律 Read `opener骨架.md`＋`工具-opener生成.py`（>500 字有文件 ⇒ `--variant reference`）＋`lint --enforce`，禁手抄；派出线＝线名＋OP 号；短名 ≤12 字。
- 🔴 决策清单末附答复模板（`1a，2a`）；push 不是定夺项；转场五条自检出声、「上下文长」不是判据。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md`；进度 `进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch <批次>` |
| 台账 | `6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`；信状态＝跟进信 README「发送状态」列 |
| 无头 CC 进展 | `reports/opener-batch/<时间戳>/*.log`＋`summary.txt`；接管 `claude --resume <session-id>` |