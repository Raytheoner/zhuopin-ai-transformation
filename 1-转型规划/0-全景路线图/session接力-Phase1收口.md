# session 接力 · Phase1 收口卡

> 覆盖不追加，8 KB 上限。最近改写：2026-09-19 18:0x 本地 `Get-Date`（Cowork 泳道看护 `OP-0919-K` 收工）。
> 🔴 **新接棒的第一件事**：读本卡全文 ＋ `CLAUDE.md`，然后跑 `python 0-学习与工具/工具-队列查询.py --digest` 扫两份队列，核触碰区无重叠再动手。

## 一、正在跑的

**没有在跑的会话或泳道。** 编辑锁本棒收工时已放。可 Open 池 **73 行（机 27／业 46）**；**机制类可动 WIP 由 27 降到 25**（本棒分诊改判两条），**仍超上限 22**。

🔴 **接棒入口**：没有待执行的派单件了——`派单件-泳道看护续棒-2026-09-19-B.md`（`OP-0919-I`）本棒已执行完，**且它的第 1 条根因被现取证伪**（见 §三）。下一棒的活全在 §四「待他一字母」里，**每一条都停在他一个字母上，没有可自动推进的 🟢 档机制活**。

🔴 **任何派单件要求起草跟进信之前，必须先跑** `python 0-学习与工具/工具-跟进闸查询.py --to <收信人>`；闸锁即停。

## 二、本棒（`OP-0919-K`）做了什么

**一条分支没动、一个工具字符没改、一次 ff 没做**（ff／删分支／改口径全是 🟡 档）。产出两份新件 ＋ 六处队列回写：

| 产出 | 内容 |
|---|---|
| `取证件-2026-09-19-OP0919K-611回放与戊档判定.md` | §一～§三 `#611` 销号判据⑶ 全量回放（15 份补丁，有效 13）；§四 `#617` 戊档四条逐条现取 |
| `看护件-【CC】第三道闸白名单611-2026-09-19.md` | 🟡 **备件·未派出**，opener 块由生成器出、`--enforce` 零违规；他答形态后一条命令即起 |
| 队列回写 | `#479`／`#556` 改判 `blocked`；`#433`／`#454` 追加留痕；`#611`／`#617` 回写结论 |

## 三、🔴 本棒最重要的一条：派单件第 1 条的根因被现取证伪

派单件写「根因已定位到源码一处：白名单没收 `ff-patrol-<yyyyMMdd>.jsonl`」。**回放证伪**：手段＝`reports/_op0919k/replay611.py`（把 `工具-opener批处理执行v2.ps1:404-409` 的 `Where-Object` 逐字复刻成 Python）重判 13 份有效补丁——

| 白名单方案 | 仍判 FAIL |
|---|---|
| A 现行（`reports/`＋`pending-ff.jsonl`） | **13/13** |
| B ＝A＋`ff-patrol-<yyyyMMdd>.jsonl`（**派单件所述修法**） | **13/13**（一条都没转绿） |
| C ＝B＋两份队列物理文件 | 3/13 |
| D ＝C＋`队列行日志/#N.md` | **1/13** |

🔑 **判词：泄漏清单里补齐了一项，不等于那一条能转绿——销号判据是「整条不再 FAIL」。** 主因是 `#611` ⑴ 自己写着、**至今未拍**的那个二选一（加白名单 vs 判定前先跑 sweep）：13 份里 12 份的泄漏项全落在「协议〇 要求泳道／看护者写」的三类文件上。**形态未拍属 🟡 档 ⇒ 只备件不派出。**

剩下那 1 份（`20260919-075655/q2-A1`）泄漏清单只有看护者自己写的取证件，**文件名任意、白名单永远治不了**，须换判法，不并入本批。

## 四、仍开的与待你一字母

**仍开**：`#614`（四条已定，落地在他手上）、`#600`／`#595`／`#477`；`#617` 丙档 `op0910n` 待裁。**触碰区撞车**：`#539` 与在办 `#538` 同触碰 SC2 周报页面，已判不领。

🔴 **待他一字母（全部在收工汇总的「待你一字母」段，含默认项与代价）**：
1. `#611` 第三道闸形态二选一 —— 本方建议**方案 D**（白名单一次补齐三类），不走「判定前先跑 sweep」（用竞态修竞态）。答了即起备好的看护件。
2. `#617` 戊档三条分支处置 —— `op0906i` 删／`queue-315` 删（判据已足）／`op0822c` 合或删（本方倾向删）。
3. `happy-thompson-017679` **ff 入 master** —— 🔴 它不是草稿：master 提交 `0c157002` 原文写着「design 已全批、tasks 全完、变更包已归档」，与 `#615` 同族出血。
4. §四 `#213` 七个 Owner 指派（不代填，人的属性以 `6-人才与组织/人员名录-称谓与性别-正本.md` 为准）。
5. `op0910n-529-timeout-semantics` 口径（`#529` 的裁定 09-09 已定、不再重提；卡的是这条分支的 ff）。
6. 机制类 WIP **25／22** 长期处置（§四 `#208`）。

## 五、常备纪律（血换的）

- 🔴 **分诊闸在扫自己的排泄物**（本棒实测，已记 `#454`）：`triage-candidates` 报强档 9 条，读全文复核后**只有 2 条成立，7 条假阳**——命中的正是上一轮分诊自己写下的留痕段。⇒ **不得凭强档直接改判，必须读状态格全文逐句定位命中片段。**
- 🔴 **「关键词在 master 有 N 处命中」不是「同一功能已在 master」的证据。** 判据按强度：① `git log master --grep=<行号>` 找同标题提交；② 取该提交新增的函数／常量名逐个 `git grep master`，**全命中才算**；③ `git cherry`（rebase 后假阳，需与 ①② 互证）。**禁用 `git diff --shortstat master <分支>`。**
- 🔴 **Cowork 挂载侧 `rm` 被沙箱拒** ⇒ 任何「用文件当锁」的工具从那边跑只能跑一次。**队列编辑锁工具必须在 Windows 侧跑**（`Windows-MCP PowerShell` ＋ `C:\Dev\zhuopin-ai`）——本棒实证：从挂载侧跑 `commit-edit`，release 被 ⑹ 登记完整性拒后锁会留在那儿。
- 🔴 **`commit-edit` 的 release 会因「脏文件不属于任何 §二 批次」被拒、且锁保持占用** ⇒ 连跑多行时第 2 行起必撞「占用中」。**多行回写的正确形态＝一次 `acquire` → 多次 `edit-row --who` → `append-row --section 二` 登记 → 一次 `release`**，不要连发 `commit-edit`。
- 🔴 **`pwsh` 是单点**：全机只有 `C:\Tools\pwsh7\pwsh.exe`（7.6.6），起无头批必须用绝对路径（`#567`）。
- 🔴 **回归闸 narrow 到受影响的测试类**：`-Tests "<测试文件>::<类名>"`（node-id）；**不得写 `-k`**（脚本把整串当一个 argv，`exit=9`）。
- 🔴 **长任务写哨兵、只看哨兵**；**ff 排序一次只在飞一条**；**看护者不在泳道运行期间往主仓写文件、也不长持队列锁**。
- 队列真身禁裸 `Read`／`grep`，只走 `工具-队列查询.py`；`--field` 只接受 `status`／`all`；`--row N` 撞多分区时要加 `--section`。
- opener 一律用 `工具-opener生成.py` 出（`--input-pointer` 必填）；看护件须有 `## 三bis` 标题；`### A<N>` 标题须带 `§一 #<行号>`。
