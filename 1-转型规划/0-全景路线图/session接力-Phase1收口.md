---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **本文件是一张定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工时「覆盖」六块，不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `0-学习与工具/取证方法知识库.md` 或队列行；未闭合项写队列行。迁出前守 J1（先指承接载体）。收工自检：`python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 超限当场删。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

约定：每次新开 session 都用「读上下文文件交接」，不粘长 prompt。

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| ~~`OP-0907-AL`~~ | ~~Cowork 环境总线接棒（09-08～09-10 续用）~~ | 收工 09-10 10:1x；产出登 `B-0907_AL1`／`B-0908_AL2`～`AL8`／`B-0909_AL9`～`AL13`／`B-0910_AL14`～`AL17`，无待补 | 09-07 16:1x – 09-10 10:1x |
| `OP-0910-H` | Cowork 环境总线接棒（转场件 `开场prompt-【Cowork】环境总线接棒-2026-09-10.md`） | 本卡＋两份队列（经锁）＋ `队列行日志/` | 09-10 10:1x – |

## 二、当前状态快照（2026-09-10 10:1x）

- **全景 35 场景**（五个编号空置不得重分配），S1 目标 9-11。**机制类可动 WIP 26/22 超限**（10:11 `_count_mechanism_wip` 实测；`#532` 给 9 条缺域行补 `[D:机]` 后上涨）——🛑 排队 `#381`／`#382`／`#448`／`#464`／`#505`／`#526`；批量分诊按 `#454`，报他定、不裸改。
- **需求侧**：台账 30 点；`#439` 2.1 已做，2.2／2.3 待 Cowork。
- **SC2 链**：`#506` 修复已部署（CC `OP-0909-K`），`采购部#22`／`IT部#12` 09-09 发出，姚祖怡 09-10 已回（拆件立 `#538` 明细核对 P1、`#539` 排版 P2，归业务总线）。
- **陈忱 8D 样本**：三 zip 入档失败根因＝机器人 media 下载超时 20 s×3（非企微 100 M），已配 600 s×3 并重启监听（PID 48100，`psutil` 回读实证）；§一 `#540`（业）待她重发落盘，§四 `#193`（机）改判「通道不换」；他答 (a)＝本人企微请她重发。`#542`（她 09-10 09:21 文本）待拆件班。
- **口径新落**：`rules/两桌同步与取证.md` §一 口令「开启Opener」；`opener骨架.md`【CC】节 worktree 勾选框留空；`rules/文档与全景治理.md` §二 排期＝不晚于、业务行能开尽开；机器守子项并入 `#522` ⑴–⑹；`#526` 远程可读性（fetch ≳36 KB 失败）🛑 排队。
- **定时任务**：`zhuopin-lan-closeout-reminder` 工作日 09:10 只读核四判据。`.51` 项一律 CC（Cowork 无 `ssh.exe`）。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行（26/22）。**

1. **陈忱三 zip 重发后核入档**：审计日志 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`（UTC）查 `sender=ChenChen, msgtype=file` 是否归档；成功由拆件班接 `#540`；81.7 MB 若仍超时 ⇒ 登 §四 兜底共享盘，不改码。
2. **WIP 26/22 分诊**：按 `#454` 出候选清单（done 未销／可并入／可 hold）交他一次答完。
3. **K2 行长 09-11 起阻断**：`#439` 状态列 >4096 B ⇒ `edit-row --changes-json` 整格重写＋外置 `队列行日志/#439.md`；同法扫其余超长行。
4. **`#439` §2 续**：2.2 P1 判别式补拆闭环信；2.3 交他批改时一并给两处矛盾。
5. **残留清理**：状态机 `358-audit-retention-p1p2`／`358-p1p2-cc` 两泳道 `running` 实为已完成，用状态机子命令关闭；`队列回写待补/B-0908_AL4-*.json` 核后删。
6. **业务端不归本线**：`#538`／`#539`／`#499`／`#542`／FI5 grill（`OP-0909-Y`）由业务总线。

## 四、在途与等他动作（只写指针）

- **等他动作**：企微请陈忱重发三 zip（§四 `#193`，截止今日）；§四 `#194`（`#538` 是否插队 `#499` 前）；§四 `#157`（无默认）；`#152`／`#156`／`#170`。
- **登记豁免**须逐路径点名＋作者＋到期（`#416`⑶），`release --waiver` 可中途补；一行内「陈忱」后 25 字内出现「他」会被称呼 lint 拒，写 `Shao Peishen`。
- **量具坑**：`--row B-…` 不前缀匹配；`--digest --section 二` 不支持；`--field` 只认 `status|all`；`Select-Object -First` 截管道会让 `$LASTEXITCODE` 非零。
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash；ff-only＋rebase 下旧 SHA 永非 master 祖先，内容闸用 `git cat-file -e origin/master:<path>`。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不碰真实库、不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17，起跑段无条件补推）。配置类代执行（环境变量／重启监听）须他明示授权＋回读实证＋登 §二。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。
- **称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；人的属性查名录正本、不推断。
- 🔑 提缺口前先查承接行（`--digest` 截 40 字，并入审核用 `--grep`）；🔑 读数太干净先怀疑量具；管道末端退出码不是命令的退出码。
- 🔴 opener 一律 Read `opener骨架.md`＋`工具-opener生成.py`（>500 字有文件 ⇒ `--variant reference`）＋`lint --enforce`，禁手抄；派出线＝线名＋OP 号；短名 ≤12 字。
- 🔴 决策清单末附答复模板（`1a，2a`）；push 不是定夺项；转场五条自检出声、「上下文长」不是判据。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md`；进度 `进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 需求侧方案与决议 | `端到端构建workflow优化-方案-2026-09-06.md` §四／§六 |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch <批次>` |
| 台账 | `6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`；信状态＝跟进信 README「发送状态」列 |
| 无头 CC 进展 | `reports/opener-batch/<时间戳>/*.log`＋`summary.txt`；接管 `claude --resume <session-id>` |
