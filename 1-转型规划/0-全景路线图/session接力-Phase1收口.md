# session 接力 · Phase1 收口卡

> 覆盖不追加，6 块，8 KB 上限。最近一次改写：2026-09-16 08:1x（Cowork 环境总线 `OP-0913-E`）。
> 🔴 他当轮明确要求：上下文过长，先停复杂任务、记录，等另开 session 优化 token 消耗后再续。

## 一、主仓现状

- master `1a1217d`（sweep 自动提交合入链路留痕 —— §四 `#199` 的机器自提交**已经在跑**，这条当日新开的缺口已闭）。
- 工作区脏件：队列机制环境文件（已登 §二 `B-0916_575三天实机与576字段修正`，等 sweep 落库）＋ 三个跟进件（他线在跑，已点名登记豁免至 09-18）。
- 队列锁：已释放。高水位线 §一 `#579`。

## 二、🟡 待他一个字母（两件，都停在这里三天了）

1. **`claude/op0913t-worktree-guard-576` @ `16f2efd` 是否 ff 合入 master。**
   内容：新增 `工具-worktree体检.ps1`（体检/修复/安全清理，默认 dry-run）＋ `工具-泳道分支合入.ps1`
   的临时 worktree 清理从成功路径移进 `finally` ＋ `test_工具-worktree体检.py` 7 条。
   测试：连同 `test_工具-合入链路留痕.py`（含合入成功／被拒／被打断三条路径）与回归判定单测，**30 passed in 64.67s**。
   🔴 无预授权 —— 旧看护件 §五 只覆盖当时具名的分支，不覆盖「这类活」。
2. **那 10 条「仅缺文件」worktree 是否现在就删**（`工具-worktree体检.ps1 -Apply -IncludeMissingOnly`）。
   它们 ahead=0、脏行全是 `D .claude/commands/opsx/*.md`（文件在 HEAD 里一字不少），删了不丢内容。

## 三、第 13 环（`#576`）：已做到哪一步

- 🔴 **上一轮那句「9 条 HEAD 为 `0000000` 可直接 prune」是错的，而且危险。** 那 11 条（不是 9 条）的工作目录都还在，
  `git worktree prune` 根本不碰它们；真因是管理目录缺 `commondir` 一个文件。其中 `queue-315-apply-9f2c1a`
  修好后带着一个未合入的抢救 commit（1130 行）—— 照那句话 prune 下去正好把它端掉。
  **判别一句：`0000000` 不等于空壳，它等于「读不出来」。**
- 已补 11 条 `commondir`（只动 `.git/worktrees/*/commondir`，不进版本库）。现取 22 条全部可解析、`0000000` 归零。
- 分类：仅缺文件 10、干净已并 1（`wecom-service-home`，在保留名单）、有未合 commit 10、有真脏 1、锁定 1。
- **未做**：期望产出③「挂进周期跑批」故意没做 —— `#575` 的实机验收还没过，此时改轮询守会把那条验收搞浑。

## 四、🔴 `#575` 轮询守：重注册做了，症状照旧

- 他已于 **09-14 07:27** 提权重跑注册脚本，包装脚本 `run-poll-guard.ps1` 已烘 pwsh 真身路径并带路径缺失即 exit 9 的响闸。
- 但现取留痕：09-14 只有 2 行（08:06、08:12）、09-15 只有 2 行（14:06、14:12），其余全无；
  Operational 日志里每 15 分钟照跑，**轮轮 200→201 相差 1 秒、`LastTaskResult=0`、零留痕**。一天 ~96 轮只有 2 轮真跑，
  且那 2 轮都落在开机／登录后头 ~6 分钟内。
- 原行 ⑶ 写的「若仍 1 秒返回，`LastTaskResult` 会非零、不再假报成功」—— **被证伪**。
- 新定位（无承接行）：包装脚本末行 `exit $LASTEXITCODE`，原生命令**压根没启动**时该变量为 `$null`，
  `exit $null` 就是 `exit 0`，把 VBS 那层刚补上的透传又吞掉了。
  修法一行，在生成器 `工具-注册轮询守计划任务.ps1` 的模板里：取不到退出码即当失败、写一行痕并 exit 非零。
  **先把它吵起来，再猜根因。**
- 🔴 **Cowork 桌面端 15 分钟任务仍不得停。**

## 五、下个 session 开头三件事

1. `#576` 状态格现带「行长豁免」4686 B > 4096 B：先在 ⑷ 与 ⑺ 前各插一道 `━━━` 分段线，
   再 `python 0-学习与工具/工具-队列行K2外置.py --row 576 --section 一 --keep 1,-1 --apply --expect-segments 3 --who <OP号>`。
   不要用工具的自动切点 —— 它落在定夺句中间。到期 09-18。
2. 把他对第二块两件事的答复落地（合入走 `工具-泳道分支合入.ps1`，永不手工 merge）。
3. `#575` 的一行修法落分支。

## 六、常备纪律（别再踩）

- **相关不是因果。** 断言前先问一句「有没有一个反例能推翻它」。09-13 那次：见 403 ＋ 出口 IP 是 GCP 段就断言「数据中心出口导致认证被拒」，
  IP 全程未变而状态数分钟内 403 → 周限额 → 正常。四条查证都是真的，它们证明的是「他在用代理」。
- **单测证明代码对，第一轮真跑才证明环境对。**
- **预授权是对具名分支的，不是对「这类活」的。**
- **队列真身禁裸 `Read`/`grep`**，只走 `工具-队列查询.py --row/--digest`。
- **状态前缀只能写合法机器字段**（`[S:open]`/`[S:done]`…）。写成 `[S:待]` 这一行就从所有按状态扫池的工具里消失了 —— `#576` 刚犯过。
- **代码块只留给要他手工粘贴执行的东西**；告知类一律普通文字。
- **凡需他定夺，写全三件套**：这是什么/为什么要他定、逐项选项＋代价（含本方推荐）、一个复制即用的答复模板。
- WindowsApps 下 0 字节 ReparsePoint 是应用执行别名，S4U 解析不了；计划任务绑机需 `requires_local_device: true` 且不传 `folders`。
