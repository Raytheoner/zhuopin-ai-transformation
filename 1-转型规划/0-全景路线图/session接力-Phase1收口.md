---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **本文件是一张定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工时「覆盖」六块，不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `0-学习与工具/取证方法知识库.md` 或队列行；未闭合项写队列行。迁出前守 J1（先指承接载体）。收工自检：`python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 超限当场删。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

约定：每次新开 session 都用「读上下文文件交接」，不粘长 prompt。

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| ~~`OP-0907-AL`~~ | ~~Cowork 环境总线接棒（09-08 续用：标准工作周 v2、LAN 收口第五批、提醒任务）~~ | 收工 09-08 20:1x；产出登 `B-0907_AL1`／`B-0908_AL2`～`AL4`；⏳ **队列更新待补**：本卡本次覆盖未登 §二（锁被 `OP-0908-V3` 占） | 09-07 16:1x – 09-08 20:1x |

## 二、当前状态快照（2026-09-08 20:1x）

- **全景 35 场景**（五个编号空置不得重分配），S1 目标 9-11。**可动 WIP 22/22 满格**（20:08 按 `_count_mechanism_wip` 同判据从 `--digest` 复算；`triage-candidates` 0 候选；🛑 排队 6 行 `#381`／`#382`／`#448`／`#464`／`#489`／`#505`；`[D:机]` blocked 25）。占位的 5 条 `hold` 常驻／搁置行（`#122`／`#138`／`#170`／`#220`／`#284`）是否改判交 §四 `#170`＝值周按 `#454` 批量处理。
- **需求侧链全通**：回件→台账回写 `OP-0907-AK` 2b.1–2b.4 已合入 `51d8ac7`；台账 30 点。`#439` 2.1 已做（`OP-0907-AL`），2.2／2.3 待 Cowork。
- **LAN 收口第五批 `B-0908_AL4` 已收工**（CC `OP-0908-J`）：SC2 冒烟八段全过、对数 63 格全等，`#502` 销号；**附带坐实 `#506` SC2 页面基准日冻结在进程启动日（08-27）**——已升关键路径（§四 `#176` 他答 (a)＝`采购部#22` 等修好再发，截止 09-11）；库内修复 `OP-0908-U` `f716db2` 已 push **未合 master**（🟡）⇒ 下一收口批＝部署＋复跑全量重算确认当周期次，才解押。`C:\sc2\snap.bak-202609081418` 保留待信发出后删。
- **业务总线**已按 `OP-0908-A` 接棒并转场至 `OP-0908-V`（其接力卡 `session接力-业务总线.md` 为准）；FI6 grill 已开。
- **标准工作周 v2**：`Peishen标准工作周.md` §七（7.1 两线配合／7.2 泳道看护随时可开／7.3 七段循环）；**LAN 收口提醒**定时任务 `zhuopin-lan-closeout-reminder` 工作日 09:10 只读核四判据。
- **在跑服务**（`.51`）：8091／8093／8094／8096 `/api/ping` 200（09-08 12:34）；Cowork 本机 `ssh.exe` 不可用，`.51` 项一律 CC。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行（22/22，立即 🛑）。**

1. **收口第六批候选**（他说「我已回Lan」即起）：`#506` 修复 ff 后**部署 SC2＋复跑全量重算＋确认页面期次为当周**（解押 `采购部#22`，截止 09-11）；`#436`⑵（等 `#500` ff）；删 `snap.bak`（等信发出）。三件按 `B-0908_AL4` §二 表式各附快照／冒烟／回滚。
2. **状态机记账滞后**：`358-audit-retention-p1p2`／`358-p1p2-cc` 两泳道 `running` 实为已完成（`OP-0908-I`），`B-0908_AL4` 转出项已闭——用状态机现有子命令关闭；`队列回写待补/B-0908_AL4-*.json` 五件已被 `OP-0908-A` 消化（`#502` done、`#361` 已追、`#506`／`#176` 已立），属残留，删前核一次再删。
3. **`#439` §2 续**（Cowork）：2.2 P1 判别式补拆闭环信；2.3 交他批改时一并给两处矛盾；AK 已合入 ⇒ 可补勾 `tasks.md` 2.1。
4. **未闭合产出扫描器 >170 s 超时**（09-08 两次）：核是 LAN 探针等待还是扫描面膨胀，只登记不修。
5. **K2 行长**：09-11 起阻断；`#439` 状态列 4354 B 需外置一次。
6. **业务端**（不归本线）：`#506`／`#499`／`采购部#22` 由业务总线；FI6→FI5→FI9→SC10→SC11→SC4 grill 次序。

## 四、在途与等他动作（只写指针）

- **等他答**：§四 `#176` (a) 已答、截止 09-11 复核；`#506` ff 一字母；§四 `#167`/`#168`（并入 `采购部#22` 押后）；FI8 intent；§四 `#157`（无默认）；`#152`／`#156`／`#170`。
- **`#416` ⑶ 已 ff（`8e8eb0c`）**：登记豁免须**逐路径点名＋作者＋到期**，泛豁免拒；`release --waiver` 可在持锁中途补。
- **opener 纪律（本会话两次犯）**：>500 字有文件 ⇒ 引用版＝`[OP]【CC】`标题＋`【设置】`＋`set_session_title` 行＋「读 <文件> 全文按其执行」四行，**不得只给一行读文件、也不得整块倒出**。
- **量具坑**：`--row B-…` 对带后缀批次名不前缀匹配；`--digest --section 二` 不支持。`Get-Date` 引用一律标 `+08:00`。
- ⚠️ 「CC 跑完了」核三信源再采信（transcript／队列行／git）；核合入认 message 不认 hash。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不碰真实库、不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17，起跑段无条件补推）。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。
- **称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；人的属性查名录正本、不推断。
- 🔑 提缺口前先查承接行（`--digest` 截 40 字，并入审核用 `--grep` 搜真身）；🔑 读数太干净先怀疑量具；管道末端退出码不是命令的退出码。
- 🔴 opener 一律 Read `opener骨架.md` 逐字套用＋`工具-opener块lint.py --enforce`；派出线＝线名＋OP 号；F5 短名 ≤12 字；OP 号只用字母。
- 🔴 决策清单末附答复模板（`1a，2a`）；push 不是定夺项；「上下文长」不是转场判据，五条自检出声。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md`；进度 `进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 需求侧方案与决议 | `端到端构建workflow优化-方案-2026-09-06.md` §四／§六 |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_R／Y` |
| 台账 | `6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`；信状态＝跟进信 README「发送状态」列 |
| `OP-0907-AL` | Cowork 环境总线（09-09 续用：Mac 远程可读性修复；09-08 收工产出已登 `B-0907_AL1`／`B-0908_AL2`～`AL8`／`B-0909_AL9`～`AL11`） | ⏳ **队列更新待补（15:5x 锁被 `OP-0908-V-每周清扫` 占）**：§二 `B-0909_AL12`（followup 正本瘦身＋CHANGELOG）与 §一 新行「远程可读性守卫」🛑，载荷在 `队列回写待补/B-0909_AL12-*.json`，清扫释放后 `append-row` 回补 | 09-07 16:1x – |