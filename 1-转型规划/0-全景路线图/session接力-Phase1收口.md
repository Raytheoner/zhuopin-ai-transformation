# session 接力 · Phase1 收口卡

> 覆盖不追加，8 KB 上限。最近改写：2026-09-20 上午（Cowork 泳道看护 `OP-0919-K` 收工转场）。
> 🔴 **接棒第一件事**：读本卡全文 ＋ `CLAUDE.md`，再 `python 0-学习与工具/工具-队列查询.py --digest --actionable` 扫池，核触碰区无重叠再动手。
> 🔴 **凡触碰 git 的命令一律 Windows 侧跑（射程＝任何 git 子命令，不只队列／锁工具）**——2026-09-20 实撞：挂载侧 `git grep`＋`git branch` 撞 120 秒超时零产出，本机侧 0.6 秒拿到。⚠️ **这一条只到文字级、不是机器守**：既有 fail-loud 长在工具代码里，拦不住会话直接敲 `git`。队列／锁工具部分——2026-09-20 起已是**机器守**：挂载侧跑 `acquire`／`release`／`edit-row`／`append-row`／`commit-*` 会 fail-loud 拒绝（只读的 `status`／`triage-candidates` 放行）；确属唯一通道时传 `--allow-mounted-side`。

## 〇、🔴 大任务开工前必过「方案闸」（Shao Peishen 2026-09-20 立，所有项目遵守）

**触发**：任何一件要起无头泳道、跑全量回归、连开多棒、或预计跨多次工具调用的大任务。
**动作**：**先花一步把效率与 token 方案定下来，再全程马力**——① 这件事的判据只需要什么粒度的证据（结论清单／表头行／汇总行，还是真要正文）；② 哪些步骤必须在本机侧跑（凡触碰 git）；③ 长跑段怎么落哨兵、探针间隔多大；④ 产出落点与回写时机（泳道运行期间看护者不写主仓）。
**判据**：**开工后不得因「换个更省的做法」而掉头重来**——掉头一次就说明方案闸没过。反面实证（本日）：`cat` 一份补丁 69.7 KB 才想起只需文件名；全池 digest 22 KB 才想起只需可动行；分诊 9 条强档读了 27 KB 才发现 7 条是闸扫到自己的留痕。**三次都是先干后想。**

## 一、正在跑的

**没有泳道在跑，编辑锁已放，`claude/*` 无未合分支阻塞。** master `fe4a769c` 与 origin 一致。机制类可动 WIP **14／上限 22**。

## 二、本棒可直接领的环境侧任务（已现取领取方，均不卡在他一字母）

`#561` openspec 收口段堵塞（在办 75／归档 62，25 个包 >14 天无改动）｜`#563` 回件勾选只认 `☑` 不认 `☒`（三犯、正在产生错判）｜`#567` `-Detach` 在 PS 5.1 下必炸 ＋ 本日并入的两条（`Start-Job -WorkingDirectory`；炸后不落 `exit.txt` 须补 `finally`）｜`#601` 新场景 `propose` 前置 `intent.md` 机器守（派单件与引用版 opener 都已备好，**直接起，不要给他粘**）｜`#416` 企微回件链路三缺陷 ⑴⑵⑷⑸⑺ 仍开｜`#328` sweep 起跑段 `_fetch()` 失败即整轮中止。
🔴 **每条开工前先过 §〇 方案闸**；`#561`／`#563`／`#567` 三条领取方现写「CC 平台（环境总线派）」＝实际待派，可直接派。

**卡在他一字母、别碰**：`#455`（待合并决策）、`#481`（等他 design 审）、`#440`／`#503`（须 design 审）、`#439`（已 `blocked`，停在 design 审）、`op0910n-529-timeout-semantics`（改口径，他单独裁）、§四 `#213` 七个 Owner。

## 三、本日（09-19→09-20）已落地

master 走了十步到 `fe4a769c`。**`#611` 与 `#559` 已销号**（前者两条判据分别由独立复核与差集回归证实；后者最硬的一条是运行事实——两份审计文件里 worktree 那份最后写入停在 09-11，分叉已止）。三条 ff 入库（`op0909i`／`op0919m`／`op0919n`）。

**三条机制化**：⑴ 根 `CLAUDE.md` 13,148→**12,110 B**（阈值 12,288，余 **178 B**），「当前进度」段迁 CHANGELOG **附录 P**，并把 sweep 的超阈值守卫**由告警改拒绝**（只拒清单含根 `CLAUDE.md` 的批次，不连坐同轮其它批次；12 passed 含接线用例）；⑵ 编辑锁入口**挂载侧 fail-loud**（18 passed 含覆盖面用例，带逃生阀）；⑶ ff 回归段加**空闲超时**（`Invoke-PytestWithIdleTimeout`，退出码 7＝挂死≠回归失败；14 passed）。

## 四、🔴 新发现，未处置：`.claude/rules/` 合计严重超阈值

现取 **43,388 B ／ 阈值 `CLAUDE_MD_RULES_TOTAL_BYTE_CAP` 30,720 B ⇒ 超 12,668 B**；其中 `两桌同步与取证.md` 单份 **12,339 B** 也超单份阈值 8,192 B。**这道守卫和根 `CLAUDE.md` 那道是同一族——告警型、没有消费者，所以超了很久没人知道。** 根那道本日已改拒绝型，**rules 这道没动**：真要处置得先瘦 12.7 KB，属一整件活，**先过 §〇 方案闸再开工**。⚠️ 在此之前**别往任何 rules 文件加字**，本日这条新纪律因此写在本卡而非 rules。

## 五、仍开的与待他一字母

🔴 **`#595` ⑷ 已认失败（他 2026-09-20 答 `1a`），改为回查根因**：观察窗 09-16 20:54→09-17 20:54 **17 次** vs 前 24h **15 次，不降反升**；其后逐日 6／4／2。**「重启当晚重连震荡」只是推测、未验证**。三个可查方向已写进行内。
其余：`#614`（落地在他手上）、`#600`／`#477`、`#617` 丙档 `op0910n`。**触碰区撞车**：`#539` 与在办 `#538` 同触碰 SC2 周报页面，已判不领。

## 六、常备纪律（血换的）

- 🔴 **多行回写不要连发 `commit-edit`**（release 被拒时锁保持占用 ⇒ 第 2 行起必撞占用中）。形态：一次 `acquire` → 多次 `edit-row --who` → `append-row --section 二` → 一次 `release`。状态格超 4096 B 先 `工具-队列行K2外置.py --apply`（段数不猜，先跑 plan 读「共 N 段」；`--keep 1,-1` 后仍超就改 `--keep 1`）。
- 🔴 **§二 文件清单每个路径必须用反引号包**（sweep 只提取反引号内的串，裸写＝整批静默不入库，`#513`）。⚠️ 在 PowerShell 里拼这段**必须用单引号字符串**，双引号里反引号是转义符。
- 🔴 **泳道收工常不自删 worktree**（`#576`）⇒ **ff 前先清**，否则 `git worktree add` 撞 `already used by worktree` 报 exit=9。清法按协议〇.5：`Remove-Item -Recurse -Force` ＋ `git worktree prune --expire now`（**必须带 `--expire now`**，默认过期 3 个月）；prune 报 `Permission denied` 就先删 `.git/worktrees/<名>/fsmonitor--daemon`。删前三查：注册状态、tracked 改动、非缓存的被忽略内容。
- 🔴 **`git -C <目录>` 只在该目录确实是注册 worktree 时才代表它自己**：泳道自删可能「已摘注册、目录残留」，此时它会静默返回**主工作区**状态（本日实撞）。判身份看「目录内 `.git` 条目 ＋ `.git/worktrees` 注册项」。
- 🔴 **长任务只探哨兵，但先核进程**：「进程在、日志长时间不动」＝挂死；「进程没了、哨兵没落」＝炸后不落哨兵。两者都不是「还在跑」。
- 🔴 **起无头批一律 `C:\Tools\pwsh7\pwsh.exe` 绝对路径**（PS 5.1 下 `-Detach` 与 `Start-Job -WorkingDirectory` 两处都必炸）。
- 🔴 **回归 narrow 到受影响测试类**（node-id，不得写 `-k`）；**ff 一次只在飞一条**；纯文档／openspec 改动不传 `-Tests`。
- 🔴 **引用时刻先判 UTC／本地并标基准**（本日按 UTC 日切窗差点把 `#595` 判反）。
- 🔴 **分诊不得凭强档直接改判**（闸会扫到自己的留痕，已加两级降档 9→3；残余 3 条属「命中点在引文里」，必须读全文复核）。
- 队列真身禁裸 `Read`／`grep`，只走 `工具-队列查询.py`；`*-main-leak.patch` 禁 `cat`，走 `工具-main-leak回放.py`；opener 一律 `工具-opener生成.py` 出（`--input-pointer` 必填）；看护件须有 `## 三bis`，`### A<N>` 须带 `§一 #<行号>`。
