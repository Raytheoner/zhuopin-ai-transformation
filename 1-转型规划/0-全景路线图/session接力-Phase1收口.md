---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `取证方法知识库.md` 或队列行；未闭合项写队列行。收工自检 `python 0-学习与工具/工具-CLAUDE进度段lint.py`。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| — | — | — | — |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0911-H`、拆件巡逻班。
> 09-12 09:2x 收工划掉三条：`OP-0910-H`（环境总线）／`OP-0911-N`（措施C-apply批1，已 ff）／`OP-0912-A`（`#564`，已 ff `59b54f7`）。

## 二、当前状态快照（2026-09-12 09:2x）

- **全景 35 场景**。机制可动 WIP **31/33**；陈旧度闸 `STALE(7)=2/3`（最老 `#312` 7.9 天），**7 天后转阻断**。
- 🔴🔴 **本日主线＝「规则活在正本文本里、没有机器在出件时校验」同族三连**：`#563` 回件勾选只认 `☑` 不认 `☒`（三犯）／`#565` 心跳写在 SKILL.md、生成器不注入（看护批 5 条泳道 0 心跳）／`#566` 称谓守卫**守在队列行与 README 台账、没守在跟进信正文**（`git grep -ln PERSON_GENDER_ROSTER` 仅两处；`zhuopin-followup-letter` skill 目录零脚本）。
- 🟩 **`#564` 已完成并已 ff**（`OP-0912-A` 无头泳道 → `59b54f7`）：跨进程文件锁＋锁内现读末行重算 `prev_hash`；`verify_chain()` 报全部断点；新增 `find_prev_hash_forks()` 旁证判据。
- 🔴 **`采购部#22` 回灌结论曾全库流通错误**：姚祖怡（**男**）七行全选「判对」，我方判成「未批改」。已更正 3 处（§一 `#541`／§四 `#194`／README 台账）。**更正必须插在 `📥` 之后**——`工具-跟进闸查询.py` 从 `📥` 起截，写行尾看不见（实测）。历史流水 2 处未动。
- 🟩 **「一次粘贴」四条口径已落 `opener骨架.md`**（lint 零违规）：⑴ 要他粘的一律单块；⑵ ≥2 个 opener 且有时序/并发约束 ⇒ 改出看护件；⑶ **默认后台起**——看护件照出照落库但不贴他，只给授权请求，跑完一次返回结果与停点；⑷ **只有 opener 才是要粘的块，答复模板不是**。
- 🟩 **无头链实证可用**：`工具-opener批处理执行v2.ps1 -Plan` 解析的正是看护件 §三 的 `### A<N>`＋`粘贴端／泳道`＋代码块，`-Detach` 注释原文「供 Cowork 调用而不占 PowerShell 通道」；`-DryRun` 已解出 2 泳道。
- **三处真红**：`#556` outbox 中继／决策台账缺口 9 行／openspec 收口段（`#561` 分诊：可归档 0／真没完 22／待复核 3）。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行。**

1. **轮询 `B-0912_心跳与称谓` 无头批收口**——**已于 09-12 09:23:58 起跑**（日志 `reports/opener-batch/20260912-092357/`，A1／A2 两条泳道均已启动）。🔴 **不要用 `-Detach`**：它在 PS 5.1 子进程下必炸（`#567` 已立行取证），本批走绕行＝`Start-Process pwsh -ArgumentList '-NoProfile -ExecutionPolicy Bypass -File <脚本> -Plan <看护件> -FullAuto -Yes -LogDir <目录>'`。轮询 `summary.txt` 与各泳道 `.log`，**一次返回结果与停点**；两条分支的 ff 属 🟡 档、等他一字母。
2. **11 份 `☒` 形态回件对账**（机器重读已落 `回件勾选逐格重读对账-13份-2026-09-11.md`，**与已回灌结论的对账未做**）。🔴 `财务部#16` 三条「判不对」的改判意见优先；**B 表 `#7` 与 `#8` 互相矛盾**（跨两料号「一律不认领」vs「拆成两条」两条都勾判对），须向唐燕萍确认。
3. **openspec 其余三包 17 个决策点**：`opener-batch-archive-precheck`(6)／`tasks-zombie-item-detect`(5)／`followup-closure-form-survives-backfill`(6)。
4. **3 个待复核包立行**：`wecom-aibot-channel` 42/45／`env-anchor-collapse` 29/32／`queue-status-machine-field` 47/48。
5. **姚祖怡补件起草**（他答 `3a`）：误判致歉＋七条口径回执＋`#538`/`#539` 进度＋`#499` 计算层顺延。**走补件不占号不占闸**（README L17/L75/L80），会签信仍占 `采购部#23`。**派业务总线 `OP-0911-H` 利旧**，本线只跟踪。

## 四、在途与等他动作（只写指针）

- **等他动作**：① `#387`⑶ 告警真实群冒烟（对外发送）；② 明早手动发 `质量部#14`／`财务部#18`；③ §四 `#157`（无默认）／`#152`／`#156`／`#170`；④ `B-0912` 两条泳道收工后的 ff（🟡）。
- **量具坑**：`--row B-…` 不前缀匹配；`--field` 只认 `status|all`；§四 列名是「事项」不是「状态」；🔴 PowerShell 反引号是转义符 ⇒ 正文走 `--cells-json`＋单引号 here-string 写脚本再跑；🔴 **python 读写 `.md` 必须 `newline=""`**，否则整份 CRLF→LF、diff 炸开；**生成器输出已含 CRLF，拼装时勿再整体转换**。
- 🔴 `Test-NetConnection` 本机恒为真（TUN fake-ip），不可作可达性判据；**读数「太好看」先怀疑量具**。
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash。**并发会话可能已把你要报的发现做掉**（`#564` 即在 acquire 回显里才发现已完成）。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17）。配置类代执行须他明示授权＋回读实证。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。**称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；🔴 **人的属性查 `6-人才与组织/人员名录-称谓与性别-正本.md`，禁从名字推断**——`姚祖怡（男）`／`陈忱（女）`／`唐燕萍（女）`／`泓钦（男）`／`陈承（男）`；7 个易错名 `祖怡`/`燕萍`/`映桦`/`植雅`/`易水`/`姣龙`/`国庆` 字面观感接近随机。
- 🔴 **他不做人肉搬运**：唯二可交给他的＝可复制粘贴的命令行、必须提权的 shell。
- 🔑 提缺口前先查承接行（`--grep`）；**没验证就下结论是本线最常犯的错**（09-12 三次：fsmonitor 误判／`#566` 误写「零机器守」被 release 当场拦下／`采购部#23` 号未核 `#557`）。
- 🔴 opener 一律 Read `opener骨架.md`＋经生成器＋`lint --enforce`，禁手抄。🔴 **要他粘的一律单块＋随块写去向三要素（端／会话／理由）**，去向写块外不写块内。
- 🔴 决策清单末附答复模板且**模板必须单独成一个代码块**；🔴 **答复模板回给「出它的那条会话」，不是「当前窗口」**——他常同时开着环境总线与业务总线两条 Cowork，说「回到本对话」会歧义，**必须写清回哪一条**（09-12 撞一次）。push 不是定夺项。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史／进度 | `session接力-Phase1收口-归档-202608.md`；`进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch <批次>` |
| 无头 CC 进展 | `reports/opener-batch/<时间戳>/*.log`＋`summary.txt`；接管 `claude --resume <session-id>` |
