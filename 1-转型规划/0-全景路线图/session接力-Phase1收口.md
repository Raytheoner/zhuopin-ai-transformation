# session 接力 · Phase1 收口卡

> 覆盖不追加，8 KB 上限。最近改写：2026-09-19（Cowork 泳道看护 `OP-0919-A` 收工）。
> 🔴 **新接棒的第一件事**：读本卡全文 ＋ `CLAUDE.md`，然后跑 `python 0-学习与工具/工具-队列查询.py --digest` 扫两份队列，核触碰区无重叠再动手。

## 一、正在跑的（接棒后第一件要盯的）

**在跑的**：`CC 无头批 B-0919_Q2建造`（看护件 `看护件-【CC】Q2建造续棒-2026-09-19.md`，泳道 `q2` 内 A1→A2 串行，触碰 `4-数字员工/质量部/Q2-8D报告AI判定/` 与 `QD-A-8D不良分析/`）。`Cowork-财务域专线-OP-0918-D` 已收工放锁（现取 2026-09-19 `status` ＝ 无锁）；它按串行闸判定**本次不起草 `财务部#20`**、拟发要点已落 §一 `#614`。

`#614` 财务域定夺包已收口并经他四条全定：产出 `财务域线下定夺包-2026-09-18.md`（174 行，20 条议程／12 项材料索取／FI9 前置单列／台账未闭环 2 条）；**两场定夺会定 2026-09-22（下周二）**。
🔴 **只生成不发送，对外发送永不代办。** 🔴 **波 2（采购／质量／IT／运营）的闸仍不要开**——财务这波形态还没验完。

## 二、这件事的来龙去脉（别按老数字行事）

他 2026-09-17 提「学 mac 做法，把待确认问题成批列出，组织各部门线下集中定夺」，答 `庚a ＋ 辛b`（只做财务域）。**真缺口不在台账**——八个已建三到六成的 openspec 包累计只提过 **5 个决策点**，这一波一次产出 **20 条**。

🔴 **口径点台账是事件溯源表**：财务域 83 行事件 → 29 个唯一 id → **未闭环只有 2 条**。**判别一句：事件溯源的表，行数不是实体数。**

**细节全文在 `1-转型规划/0-全景路线图/队列行日志/#614.md`**（该行状态格已两次 K2 外置）。

## 三、本机环境有三处必须知道

🔴 **`pwsh` 是单点**：2026-09-17 的 `winget install` 在 92% 失败，**但它先把 Store 版卸掉了才失败**。现全机只有 `C:\Tools\pwsh7\pwsh.exe` 一份（301,368 B，09-18 复测未变），已进用户 PATH 与 Terminal 默认 profile。**误删即轮询守与整条泳道派发链一起停。**

🔴 **轮询守 `#575` 已收**，桌面端「每 15 分钟」任务已由他删除，探针＋巡检只剩本机 `ZhuopinPollGuard` 一条腿。残留风险：它失败会叫（exit 8/9＋留痕），但**没人在看 `LastTaskResult`**。

🔴 **合入前必查的三件（2026-09-18 全踩过）**：① **泳道收工不自删 worktree** ⇒ `git worktree add` 撞「branch already used」，先核干净再 `worktree remove`；② **`.git` 会攒陈旧锁**（当天三枚：`index.lock` 挡 ff、`packed-refs.lock` 挡删分支、`maintenance.lock` 从 08-20 起挡住 maintenance 致 `.git` 涨到 1.2 GB）——清前须证无主：`Get-Process git` 为零 ＋ 0 字节 ＋ 陈旧超 N 分钟（承接行 `#618`）；③ 🔴 **`git worktree list` 从 Cowork 挂载侧跑会把全部条目标 `prunable`——路径命名空间造成的假信号**，Windows 侧实测零条可剪，别据此去剪。

## 四、已收的与仍开的

**已收**：`#575`／`#576`／探针正则；`#614` 五条泳道分支全数 ff 入 master（`c97ccb7 → 13448de`）。**仓库瘦身**：`claude/*` 379→27、`.git` 1,236→116 MB、锁清零。🆕 **2026-09-19 `OP-0919-A` 销号三行**：`#605`（FI3 到档 1）／`#607`（FI6 自查收口，同批 K2 外置 6 段、行长豁免解除）／`#613`（FI3 门户页 5.1，5.2 已由 `#615` 闭合）——判据均＝产出已在 master，手段写在各自行内。

**仍开**：`#603`（波 1 母行，领取方 `CC-OP-0917-W1` 仍标在办，🟡 待他一字母才可关）、`#614`（四条已定，落地在他手上）、`#616`／`#617`／`#618`／`#620` 四条 🛑 排队中、`#600`／`#595`／`#477`；机制类可动 WIP 现取 **29／22 超限**（`MECHANISM_WIP_GATE_MODE=warn`）⇒ 新机制类 🟢 候选一律不开。**触碰区撞车**：`#539`（SC2 排版）与在办的 `#538` 同触碰 SC2 周报页面，已判**不领**，待总线裁决。

🔴 **别再说「`财务部#18` 未发出」**：实测它 `📥 已回件并回灌 2026-09-16`，`#19` 亦已推送，README 零封待发／待审。真实缺口是 **FI5／FI6／FI8 的材料信从未存在**（README 检索三者零命中）——那正是 `OP-0918-D` 在起草的 `财务部#20`。

## 五、常备纪律（血换的）

- 🔴 **worktree 是 git 的视图，不是仓库的视图**：里面的「全库零命中」对 `.gitignore` 路径一律假阴性（`7-外部文档/` 即是），须回主仓跑。（`#616`）
- **相关不是因果**（断言前先找反例）；**只会报成功的守卫等于没有守卫**，会报成功却什么都没检到的绿灯同理；**单测证明代码对，第一轮真跑才证明环境对**。
- **只读命令「太干净」先怀疑没读到对象**——`prune --dry-run` 的空输出与 `worktree list` 满屏 `prunable`，是同一陷阱的两面。
- **旧记忆不能冒充实测**，**转述不是真身**：台账「66 行」真身是 83；「`财务部#18` 未发出」真身是已回件回灌。凡「已复核」类自陈须写出用什么核的。
- opener **一律用 `工具-opener生成.py` 出**（🔴 但生成器与 lint 今日双双放行了两处错，见 `#619`）；看护件须有 `## 三bis` 标题。
- 队列真身禁裸 `Read`/`grep`，只走 `工具-队列查询.py`；状态格超 4096 B 走 `工具-队列行K2外置.py`（plan → `--apply` → `--verify-json`）。
- 回写优先 `commit-edit`/`commit-append`；**挂载路径跑很慢、易超时，改走本机 pwsh**（超时后先 `status` 查锁、再核写没写进去，别盲目重试）。🔴 **一个参数被接受却不生效，比它被拒绝更危险**（`--reserve`／`--dont`／Cowork 的 `--model`，今日三例）。

## MCP 精简（2026-09-18 定夺 · 本线收口）

仓库依赖实查：**无任何脚本／skill／看护件依赖非 remote-devices 的 MCP**。实测调用：remote-devices 367 次（97%），其余近零。

他拍板 **丙（c）**，已断 **Gmail（29 工具）／Google Calendar（9）／Google Drive（11）**，共卸 **49 个工具定义**（账号级连接，网页端邮件／日历／云盘一并失效，要用需重走 OAuth）。🔴 `visualize` 不是连接器，原计划「四项」实为三项。**留**：Browser／Filesystem／Windows-MCP（remote-devices 三条腿，机器守命脉）、Claude in Chrome、Claude Docs、GitHub。

后续若发现某能力缺失，先核是不是这三项之一——它们在本项目内零依赖，缺失应只表现为「用不上」，不应表现为「跑挂」。

---

## ✅ 上一棒「队列更新待补」两条已闭环（2026-09-19 `OP-0919-A` 复核）

- **待补一（新立机制行）** ✅ 已落 §一 **`#620`**（不是 `#619`——`#619` 当时已被企微归档行占，工具给的号是 620）：opener 生成链两处「被接受却不生效／被接受却是错的」合并处置，生成器＋lint 双点机守。手段＝`--digest --section 一` 现取命中 `#620`。
- **待补二（`#614` 追加 ⒂）** ✅ 已落：`--row 614 --field all` 现取命中 ⒂ 段（派出线自指更正＋「模型」字段对 Cowork 无效力，他答 `2a` 已发出件不重发）。
- ⇒ `reports/_op0918c/row619.json`／`row614g.json` 两份快照**已无用**，下一棒不要再照它们回补一次。

## 五bis · 本棒新踩的一个坑（2026-09-19）

🔴 **`工具-opener生成.py` 从 Cowork 挂载侧跑会留下陈旧锁 `reports/op-id-claims.jsonl.lock`**——该侧 `rm` 被沙箱拒（`Operation not permitted`），生成器自己清不掉自己的锁 ⇒ **第二次跑必然 fail-closed「等待超过 10 s」**。绕行＝改在 Windows 侧跑（`Windows-MCP PowerShell` ＋ `C:\Dev\zhuopin-ai`），那边 `Remove-Item` 正常。判据一句：**能写不能删的文件系统上，任何「用文件当锁」的机制都只能跑一次。**
