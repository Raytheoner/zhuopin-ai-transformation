# session 接力 · Phase1 收口卡

> 覆盖不追加，8 KB 上限。最近改写：2026-09-20 上午（Cowork 泳道看护 `OP-0919-K` 四次收工）。
> 🔴 **新接棒第一件事**：读本卡全文 ＋ `CLAUDE.md`，然后 `python 0-学习与工具/工具-队列查询.py --digest --actionable` 扫池（只出能动的行；要全池去掉该参数），核触碰区无重叠再动手。
> 🔴 **凡触碰 git 的队列／锁工具必须在 Windows 侧跑**——**这条 2026-09-20 起已是机器守**：从 Cowork 挂载侧跑 `acquire`／`release`／`edit-row`／`append-row`／`commit-*` 会 fail-loud 拒绝（只读的 `status`／`triage-candidates` 照旧放行）。确属挂载侧唯一通道时传 `--allow-mounted-side`。

## 一、正在跑的

**没有泳道在跑，编辑锁已放，`claude/*` 无未合分支阻塞。** master `7c5bf8fb`。机制类可动 WIP **14／上限 22**。

## 二、本日（09-19→09-20）master 走了九步

`9cc4dab3 → cbae2199`（开场 token 三处瘦身）`→ 9cb533e9`（`happy-thompson`，一处冲突零丢弃解开）`→ 09b153d0`（`#611` 方案 D 白名单）`→ 06039d36`（`#611` 归属判法）`→ 89482e11`（`op0909i`，解 `#440` 前置）`→ 26a68383`（`op0919m`／`#439` 产出）`→ 7c5bf8fb`（`op0919n`／`#440` 产出）。

**`#611` 已销号**（两条判据分别由独立复核与差集回归证实）。**`#559` 已销号**——最硬的一条是运行事实：全库两份 `wecom_aibot_audit.jsonl`，worktree 那份最后写入停在 2026-09-11 19:53（立行当日），主仓那份仍在写 ⇒ 分叉已止。

## 三、两条机制化已落地（各带回归用例）

**⑴ 根 `CLAUDE.md` 从 13,148 B 降到 12,110 B**（阈值 12,288，余量 178 B）——「当前进度」段迁 `进度编年-CHANGELOG.md` **附录 P**，行内只留指针。**配套把 sweep 的超阈值守卫由告警改拒绝**：文件清单含根 `CLAUDE.md` 的批次在超阈值时拒绝落库、行留待处理，**同轮其它批次不连坐**。12 passed，含一条「判据真的接在落库循环上」的接线用例（防「建成没接线」）。🔴 **余量只有 178 B，下次往根文件加任何一条前先核字节数。**

**⑵ 挂载侧 fail-loud 已是机器守**（见卡首）。18 passed，含一条覆盖面用例：CLI 新增写类子命令若忘了挂进守的名单，用例直接转红。带 `--allow-mounted-side` 逃生阀，免得挂载侧成唯一通道时把自己锁死。

**⑶ ff 回归段加了空闲超时**（`#567` 同族第三例）：`Invoke-PytestWithIdleTimeout`——**空闲超时不是总时长超时**，只要还在产出就不打断；连续无输出才杀进程树，新增退出码 **7＝挂死**（映射「被打断」，🔴 不是「回归新增失败」）。14 passed，两个方向都守。成因是 09-19 23:54 那条 ff 机器休眠后**卡了 8 小时 47 分、进程在但日志零输出、哨兵永不落**。

## 四、仍开的与待他一字母

🔴 **`#595` ⑷ 已认失败（他 2026-09-20 答 `1a`），不销行，改为回查根因**：观察窗 09-16 20:54→09-17 20:54 **17 次**，前 24h 对照 **15 次，不降反升**；其后逐日 6／4／2。**「重启当晚重连震荡」只是推测、未验证，不得当结论**。三个可查方向已写进行内（重启本身产生的断连要单独摘出／`annual_holiday_reminder.py` 等三个脚本本次明确未改、若当晚有投递则根因只修掉一部分／窗口跨白天与夜间基线不可比）。

其余：`#614`（四条已定，落地在他手上）、`#600`／`#477`；`#617` 丙档 `op0910n-529-timeout-semantics`（2 个提交未入 master，改口径那条，**他单独裁**，本日三条 ff 刻意没带它）；§四 `#213` 七个 Owner 不代填；`#539` 与在办 `#538` 同触碰 SC2 周报页面，已判不领。
`#439` 已改判 `blocked`（停在 design 审）；`#440`／`#503` 未改判——行内自陈还有执行者能做的活，不为压数字改判。

## 五、常备纪律（血换的）

- 🔴 **多行回写不要连发 `commit-edit`**（release 被拒时锁保持占用 ⇒ 第 2 行起必撞占用中）。正确形态：一次 `acquire` → 多次 `edit-row --who` → `append-row --section 二` → 一次 `release`。状态格超 4096 B 先 `工具-队列行K2外置.py --apply`（段数不猜，先跑 plan 读「共 N 段」；若 `--keep 1,-1` 后仍超，改 `--keep 1`）。
- 🔴 **§二 文件清单里每个路径必须用反引号包**——sweep 只提取反引号内的串，裸写等于整批静默不入库（`#513`）。⚠️ 在 PowerShell 里拼这段**必须用单引号字符串**，双引号里反引号是转义符，会被吃掉（本日实撞）。
- 🔴 **泳道收工常不自删 worktree**（`#576`）⇒ **ff 前先清**，否则 `git worktree add` 撞 `already used by worktree` 报 exit=9（本日三条 ff 撞了一次）。清法按协议〇.5：`Remove-Item -Recurse -Force` ＋ `git worktree prune --expire now`（**要带 `--expire now`**，默认过期是 3 个月）；prune 报 `Permission denied` 就先删 `.git/worktrees/<名>/fsmonitor--daemon`。删前三查：注册状态、tracked 改动、非缓存的被忽略内容。
- 🔴 **`git -C <目录>` 只在该目录确实是注册 worktree 时才代表它自己**：泳道自删可能「已摘注册、目录残留」，此时对它跑 `git -C` 会静默返回**主工作区**状态（本日实撞，误把主仓 7 个脏文件当成它的未提交改动）。判身份看「目录内 `.git` 条目 ＋ `.git/worktrees` 注册项」，不看 `git -C` 输出（同 `#98` 2026-08-03 那条）。
- 🔴 **长任务只探哨兵，但要先核进程**：「进程在、日志长时间不动」＝挂死，不是在跑；「进程没了、哨兵没落」＝炸后不落哨兵。两者处置不同，都不能当成「还在跑」。
- 🔴 **起无头批一律用 `C:\Tools\pwsh7\pwsh.exe` 绝对路径**：PS 5.1 下 `-Detach` 与 `Start-Job -WorkingDirectory` 两处都必炸（`#567`），且炸后不落 `exit.txt`。
- 🔴 **回归 narrow 到受影响测试类**（node-id，不得写 `-k`）；**ff 一次只在飞一条**；纯文档／openspec 改动不传 `-Tests`。
- 🔴 **引用时刻先判 UTC／本地并标基准**：本日按 UTC 日切 `#595` 的观察窗，差点得出与事实相反的「下降」结论。
- 🔴 **分诊不得凭强档直接改判**（闸会扫到自己上一轮的留痕，已加两级降档 9→3；残余 3 条属「命中点在引文里」，必须读全文复核）。
- 队列真身禁裸 `Read`／`grep`，只走 `工具-队列查询.py`；`*-main-leak.patch` 禁 `cat`，走 `工具-main-leak回放.py`；opener 一律 `工具-opener生成.py` 出（`--input-pointer` 必填）；看护件须有 `## 三bis`，`### A<N>` 须带 `§一 #<行号>`。
