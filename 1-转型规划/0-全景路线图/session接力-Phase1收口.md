# session 接力 · Phase1 收口卡

> 覆盖不追加，8 KB 上限。最近一次改写：2026-09-18 08:0x（Cowork 环境保障线 `OP-0913-E` 转场前）。
> 🔴 **新接棒的第一件事**：读本卡全文 ＋ `CLAUDE.md`，然后跑 `python 0-学习与工具/工具-队列查询.py --digest` 扫两份队列，核触碰区无重叠再动手。

## 一、正在跑的（接棒后第一件要盯的）

**波 `20260918-072559`** —— 财务域定夺包生成，承接队列 §一 `#614`，看护件 `1-转型规划/0-全景路线图/看护件-【CC】财务域定夺包生成-2026-09-17.md`。

5 条泳道并行：A1 `fi3-baseline-614`／A2 `fi4-baseline-614`／A3 `fi7-baseline-614`（跑 `zhuopin-requirement-grill` 前半段，只生成问题不与专员交互）；A4 `fi56-audit-614`／A5 `fi810-audit-614`（包内未问点盘查，**不跑 grill**——它只对新场景）。日志在 `reports/opener-batch/20260918-072559/`。

**收工后由看护者串行做收口**（不派泳道）：① FI9 前置盘查单独成节、标「非口径问题」；② 并入本域台账未闭环条目与包内决策点现状；③ 汇总成《财务域线下定夺包》**交 Shao Peishen 过目——过目之后才谈怎么开会、由谁发出**。🔴 只生成不发送；对外发送永不代办。

🔴 **波 2（采购／质量／IT／运营）的闸不要开**——先让财务这波真跑一轮，形态验过再复制。

## 二、这件事的来龙去脉（别按老数字行事）

他 2026-09-17 提「学 mac 做法，把待确认问题成批列出，组织各部门线下集中定夺」，答 `庚a` ＋ `辛b`（只做财务域）。

🔴 **两个错数已更正，写在 `#614` 行里**：口径点台账是**事件溯源**表（306 行＝建点 112＋转态 187＋补记 7，落 112 个 id），按 id 归并**未闭环只有 28 个**，不是先报的 213；「在途 95 条需去重」同样作废。**判别一句：事件溯源的表，行数不是实体数。**

真缺口不在台账——全项目累计口径点 112，而八个已建三到六成的 openspec 包只提过 **5 个决策点**。**需求初始版质量低不是问题难，是没人系统地问过。**

## 三、本机环境有两处必须知道

🔴 **`pwsh` 是单点**：2026-09-17 那次 `winget install Microsoft.PowerShell` 在 92% 报 `0x80070005` 失败，**但它先把 Store 版卸掉了才失败**。现全机只有 `C:\Tools\pwsh7\pwsh.exe` 一份（zip 解压，301,368 B）。已加进用户 PATH；Windows Terminal 已加静态 profile 并设为默认。**它若被误删，轮询守与整条泳道派发链一起停。**

🔴 **轮询守 `#575` 已收**（2026-09-17 11:03 三条判据全过：`LastTaskResult=0`／Operational 63 秒／留痕出真行且 `woke:false`）。Cowork 桌面端「全天每 15 分钟」任务**已由他删除**，探针＋巡检现在**只由本机 `ZhuopinPollGuard` 一条腿承担**。残留风险无承接行：它失败会叫（exit 8/9＋留痕），但**没人在看 `LastTaskResult`**。

## 四、已收的与仍开的

已收：`#575` 轮询守、`#576` 第 13 环 worktree（体检工具＋合入脚本 finally 收 worktree＋git 删不动时回退 Remove-Item）、探针 `FAIL(-1)` 正则。worktree 23→10；待合登记册已清空。

仍开：`#614`（本波）、`#600` 无头泳道隔离（别的线在推，别插手）、`#595` 企微断连、§四 `#208` 机制类 WIP 29–30／22 超限待处置。

## 五、常备纪律（今天用血换的）

- **相关不是因果。** 断言前先问「有没有一个反例能推翻它」。09-17 一天我给出的三个因果（WindowsApps ACL／受控文件夹访问／火绒）**全部被实测证伪**，扛住的是那些与原因无关的修法。
- **只会报成功的守卫等于没有守卫**；**会报成功却什么都没检到的绿灯是同一回事**——lint 对「零个块」照样报零违规。看绿字前先确认它检到了东西。
- **单测证明代码对，第一轮真跑才证明环境对。**
- **事件溯源的表，行数不是实体数。**
- opener **一律用 `工具-opener生成.py` 出**，手写只在生成器不可用时兜底（手写版被 lint 判 F1/F4 各 5 处）。看护件须有 `## 三bis` 标题，否则子任务泳道块的 `set_session_title` 豁免边界不存在。
- 队列真身禁裸 `Read`/`grep`，只走 `工具-队列查询.py --row/--digest`；状态前缀只能写 `[S:open]`/`[S:done]` 这类合法机器字段。
- 回写优先 `工具-共享文档编辑锁.py commit-edit/commit-append`；等泳道用 `工具-泳道看护等待.py wait`，别逐轮自查。
- 代码块只给要他手工粘贴执行的东西；**凡能自己发起的命令不要贴给他粘**，直接发起、只要一句授权。
- 凡需他定夺，写全「是什么／逐项选项＋代价（含本方推荐）／复制即用答复模板」三件套。
