---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `取证方法知识库.md` 或队列行；未闭合项写队列行。迁出守 J1。收工自检 `python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 超限当场删。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0910-H` | Cowork 环境总线接棒（09-11 续用，锁身份 `OP-0911-A`） | 本卡＋两份队列（经锁，§一 436/454/455/482/487/505/531/542／§二）＋ `队列回写待补/` ＋ 方案件 | 09-10 10:1x – |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0910-A`、看护者 `OP-0910-G`。

## 二、当前状态快照（2026-09-10 10:4x）

- **全景 35 场景**（五个编号空置不得重分配），S1 目标 9-11。**机制类可动 WIP 26/22 超限**（10:3x `_count_mechanism_wip` 实测；`#545` 出血行带 `WIP豁免`）——🛑 排队 `#381`／`#382`／`#448`／`#464`／`#505`／`#526`。
- 🔴 **WIP 分诊首跑即出错、已全部回滚（09-11）**：本方只凭 `triage-candidates` 的「强档 5 条」就改判 `blocked`、未逐条读正文 ⇒ 事后取证 **5 条里 4 条是误报**，命中的全是行内**历史引文**：`#455` 09-10 已被 `OP-0910-A` 判定「不改判」／`#482` 首段即写「全部闭合」／`#487` 分支对 master **领先 0 落后 0**、无 ff 可做／`#505` 在等**时间**（窗口至 09-23，真实态候选 `[S:timed=2026-09-23]`）／`#531` 在等**总线激活**。**五条没有一条真的在等你的字母。** 已逐行复原并留痕，WIP 回 27/22；硬纪律写进 `#454`：`triage-candidates` 永远只是候选，改判前必须逐条读正文并写出「声明还是引文」的判据。
- **措施 C 阈值已实测定死**（`git blame --incremental` 取行末次 commit，23 条全解析）：最老 7 天，`STALE(7)=0`／`(5)=4`／`(3)=7` ⇒ **`N=7`／`K=3`**；原推荐 `N=10/K=5` 经实测**永不触发＝悄悄废闸**（推荐值不实测就写死会错，已自证）。
- 🔴 **新发现（比换量具更快止血）**：🛑 排除判据**只认状态列**，而 `#381`／`#382`／`#447`／`#448`／`#505` 五行把 🛑 写在**任务列** ⇒ 其中三行正文自陈「排队中·暂非可动」却被计入。**认两列后 WIP 23→20/22 立即合规**——今天判超限有一半是闸自己算错。属改判据（🟡），待他一字母。
- **`#537` 超限**：两棒（`OP-0910-P`／`OP-0911-B`）均刻意留步并写明——done 单段格无历史段可迁、K2 切不动，属 `#552` 形态缺口。**本线停手不外置**；下次清扫迁档即消解。
- **陈忱 8D 样本（`#540`／§四 `#193`）**：三 zip 两轮重发全败，真因＝SDK 内层 `request_timeout` 默认 10 s 未透传（`media_transfer_retry` 实测）。已立 `#545` ＋派单件 `OP-0910-J`；取件走微盘，🔴 **「机器无法代」系本方未实测的臆断，须先试 `computer_*` 通道**。
- **K2 行长闸 09-11 已生效**：两棒（`OP-0910-K` 清扫 →`OP-0910-P` 外置）跑完，全池超限 28→**1**（仅 `#537`，见下）。
- **泳道记账**：三条补终态、测试残留 `A` 已删。🔴 `436-worktree-drift-alert-clear` 核实＝⑵ 从未执行（`B-0907_Q` 做的是 ⑶③）⇒ 不补终态；前提「等 `#500` ff」已满足 ⇒ **具备入 `zhuopin-lan-closeout` 条件**（⏭️ 档）。
- **`队列回写待补/` 已清池**：28 份核毕删 22，余 5 份（2 份待 ff／3 份在跑会话）。🔑 判据：K2 外置会把回写段搬进 `队列行日志/`，**真身里找不到≠没落**，须二轮比对行日志。🔑 **清池判据**：K2 外置会把历史回写段从状态格搬进 `队列行日志/#N.md`——**「真身里找不到」不等于「没落」，必须二轮比对行日志**（本次 11 份因此从「未落」翻案）。
- **定时任务**：`zhuopin-lan-closeout-reminder` 工作日 09:10 只读核四判据。`.51` 项一律 CC（Cowork 无 `ssh.exe`）。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行（26/22）。**

1. **措施 C 换量具**：`OP-0911-D` 已由 Shao Peishen 发出（09-11）；只 propose＋design，不 apply、不改闸。收工后核三信源（transcript／队列行／git）。
2. **`#436`⑵ 入收口批**：前提已满足，交 `zhuopin-lan-closeout`（清 BOM＋ff 常驻执行体＋重启 `ZhuopinAibotDevListener`）；本线只跟踪、不代办。
3. **`#439` §2 续**：2.2 P1 判别式补拆闭环信；2.3 交他批改时一并给两处矛盾。
4. **不归本线**：`#545`（`OP-0910-J`）／`#443` 清扫与 K2 外置（`OP-0910-K`/`P`）／`#538`／`#539`／`#499`／`#542`／FI5 grill 由各自承接线。

## 四、在途与等他动作（只写指针）

- **等他动作**：① 微盘下载陈忱三包放入 `7-外部文档/质量部/`（§四 `#193` 二次改判，交期 09-11 ＝今日）；② 审措施 C 换量具方案件的 N/K；③ §四 `#194`／`#157`（无默认）／`#152`／`#156`／`#170`。
- **登记豁免**逐路径点名＋作者＋到期（`#416`⑶），`release --waiver` 可中途补；一行内人名后 25 字内出现「他」会被称呼 lint 拒，写 `Shao Peishen`。
- **量具坑**：`--row B-…` 不前缀匹配；`--digest --section 二` 不支持；`--field` 只认 `status|all`；🔴 PowerShell 反引号是转义符 ⇒ `--cells-json` 正文用单引号 here-string 写脚本再跑；🔴 §二 文件清单里任何反引号串都按路径校验，`#N`／`[D:业]` 这类记号不能加反引号。
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash；内容闸用 `git cat-file -e origin/master:<path>`。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不碰真实库、不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17）。配置类代执行须他明示授权＋回读实证。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。**称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；人的属性查名录正本、不推断。
- 🔑 提缺口前先查承接行（并入审核用 `--grep`）；读数太干净先怀疑量具。🔑 **开工先看 `git status --porcelain` 的未跟踪件**——并发会话常已把你要报的发现做掉（09-10 实撞 `#545` 与 K2 两棒）。🔴 **跨日会话每天重读根 `CLAUDE.md`**：09-10 18:19 新增「答复模板须单独成代码块」与「会话不自醒」两条纪律，本会话持 10:2x 旧快照 ⇒ 当日违规两次。
- 🔴 opener 一律 Read `opener骨架.md`＋`工具-opener生成.py`（>500 字有文件 ⇒ `--variant reference`）＋`lint --enforce`，禁手抄；短名 ≤12 字。
- 🔴 决策清单末附答复模板（`1a，2a`）；push 不是定夺项；转场五条自检出声、「上下文长」不是判据。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md`；进度 `进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch <批次>` |
| 无头 CC 进展 | `reports/opener-batch/<时间戳>/*.log`＋`summary.txt`；接管 `claude --resume <session-id>` |