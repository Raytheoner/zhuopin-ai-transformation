# session 接力 · Phase1 收口卡

> 覆盖不追加，8 KB 上限。最近改写：2026-09-21（Cowork 泳道看护 `OP-0921-A` 收工）。
> 🔴 **接棒第一件事**：读本卡全文 ＋ `CLAUDE.md`，再 `python 0-学习与工具/工具-队列查询.py --digest --actionable` 扫池，核触碰区无重叠再动手。
> 🔴 **凡触碰 git 的命令一律 Windows 侧跑（射程＝任何 git 子命令）**——挂载侧 `git grep` 撞 120 秒超时零产出，本机侧 0.6 秒。⚠️ 只到文字级、不是机器守。队列／锁工具那部分是机器守：挂载侧跑 `acquire`／`release`／`edit-row`／`append-row`／`commit-*` fail-loud 拒绝。

## 〇、🔴 方案闸（大任务开工前必过）

**触发**：起无头泳道／跑全量回归／连开多棒／预计跨多次工具调用。
**动作**：先定方案——① 判据要什么粒度的证据；② 哪些步骤必须本机侧跑；③ 长跑段怎么落哨兵、探针多大间隔；④ 产出落点与回写时机。
**判据**：开工后不得因「换个更省的做法」掉头重来。

## 一、正在跑的

**没有泳道在跑，编辑锁已放。** master `15cca58b`（以现取为准）。
**`#632`／`#629` 已 ff 进 master**（JZ 线 2026-09-21 执行，`#629` 在新 master 上 rebase 三个 commit 无冲突）；`claude/op0921d-595-…` 是空分支、无提交，可删。
🔴 **残留 worktree 8 个**，其中 `agent-a15b4851f659218aa`（`claude/op0905n-editrow-guard-455`）**有真实未提交未合入改动**（`工具-共享文档编辑锁.py` 等 5 个文件，属 `#455`）——**不是空壳，别当垃圾清掉**，去留待他看一眼。

## 二、🔴 `#627` 效果验收——本日核心，结论三条两阴一阳

本批（`B-0921_必读件与分诊` ＋ `B-0921_629续完`，4 条实活泳道）跑在新 v2.6 上，实测：

1. **250k 硬线**：**未获生产实证**。四条峰值 242,594／230,874／212,522／205,114（`工具-Token用量度量.py` 现取），**无一越 250k**（最近一条差 7,406），终止路径仍只有单测覆盖。
2. **`summary.txt` 新列与汇总行**：✅ **实证生效**。两份 `summary.txt` 都出「上下文峰值」列，末尾都有「本批越 150k 的泳道 N 条」（分别为 2 条／1 条，按**泳道**去重，非按 opener）。
3. **峰值分布相对 09-20 基线**：❌ **没下移，反而上移**。今日四条中位 ≈221,698、最高 242,594；09-20 上午七条中位 194,282、最高 244,434——最高持平，中位抬高约 14%。

🔴 **真结论（比判据更重要）**：三份派单件都是薄件（4,380／6,282／4,383 B，对照 09-20 那份 24,865 B），**薄件没把峰值压下来** ⇒ 主导项不是看护件体量，而是**泳道自己读的东西**（`test_工具-落库sweep.py` 534 KB、`工具-落库sweep.py` 555 KB、CHANGELOG 540 KB）。下一刀切**泳道读取面**，不是派单件。
🔴 **第二个实测缺陷**：`hooks-posttooluse-context-meter` 15 秒轮询有**系统性低估**——同一条泳道，闸读到 223,309，度量工具算出 230,874，**差 7,565**。真实峰值可能落在两次轮询之间，**250k 硬线因此可能漏判**。

## 三、本日其余已落地

- **`#632` ✅ 已销**（`deb96605`）：16 条逐条分诊，**15 条判夹具腐坏**（14 条同根因＝`SweepTestBase.setUp()` 未还原第 14 类要动态 import 的 `工具-定时任务源码备份.py`，每个 CLI 级用例多推一条企微 webhook；1 条＝`STUB_LEDGER_SCRIPT` 缺 `__main__` 守卫）。**只改测试、未碰生产代码**，narrow 8 类 28 项 → 27 passed / 1 failed。
- **`#629` ✅ 已做完**（`381c9559`，两棒）：`.claude/rules/` 六份 **43,388 → 30,310 B**（达标，原文存 CHANGELOG 附录 Q-1~Q-6）｜`SKILL.md` **45,119 → 30,326 B**（存附录 R）｜根 `CLAUDE.md` **12,110 → 12,077 B**（§3 转场自检改只在阳性时提醒）｜`工具-落库sweep.py` 两道 reject-consumer ＋ 棘轮守 ＋ 单测 47 passed｜新出《Win侧必读件字节尖峰榜》。
- **`#595` 已出取证件**（`取证-595断连根因-2026-09-21.md`，泳道分析＋看护者独立复算 721 条）：⑴「重启震荡」**证伪**（重启 ±60 分钟内 **0** 次）｜⑵ 三个未改脚本**至多解释 1/17**｜⑶ 逐日 1~34、均值 11.1，**单日 17 vs 15 无统计效力**，且 09-16 的 15 次全在重启之前｜⑷ `:18` 型断连修复后归零，但修复前**也只占 2.6%**（18/691），热桶实为 `:21`／`:31`／`:33`。**原判据不改、本行不销。**

## 四、🔴 本日血教训

1. **泳道自陈不可信，又撞一次**：`OP-0921-D` 报 `OPENER_DONE`，实际**零提交、取证件没落盘、worktree 自删**，分析只剩日志那段文字。产出面机器核（`git diff --name-only`）是唯一可信入口。
2. **禁起后台子任务**：`OP-0921-C` 把瘦身丢给后台子任务，被 claude 自身 **600 秒上限**掐断，一棒白跑（靠看护者落检查点 `5a4d49cb` 才没丢）。派单件须写死「顺序、inline 做完」。
3. **闸会扫看护者自己的排泄物**：`OP-0921-F` 判 `FAIL(main-leak)`，唯一「脏文件」是**看护者在泳道运行期间写的取证件**。排查 main-leak 假阳先查是不是这个形状。
4. **泳道会越权新立口径**：`OP-0921-C` 在派单件明文禁止下仍新立两个固定字节阈值（8,192／24,576），续棒才改回棘轮。**残留一处**：`CLAUDE_MD_RULES_BYTE_CAP_FALLBACK = 8 * 1024` 仍是固定值（只对将来新增的 rules 文件生效）。
5. **撞号查重按「当日短号」算**：§三bis 逃生口 opener 也占号，续棒 OP 号要顺延。

## 五、仍待起（下一棒优先级）

🔴 **token 止血四行已由 JZ 线立好、方向已裁、时序已满足（三条泳道已收工），可直接起**：`#637` `_输出截流.py` 零命中回显被挤掉（生产 bug）｜`#638` 看护会话侧裸 `sleep` 轮询无机器守（`工具-泳道看护等待.py` 建好没接上）｜`#639` 泳道读取面切分（峰值主导项）｜`#635` 登记当交付无机器守。**`#637`＋`#638` 须同批同 session（他 2026-09-21 答 `1a`）；`#639` 与 `#637` 同触碰 `test_工具-落库sweep.py`，须串行。**
其余：`#634` session 编号 `OP-`→`Win-` 口径表｜`#636` 企微 liveness 与在线状态不一致｜上下文闸 15 秒轮询漏峰值 7.5k｜`#595` 剩余 97% 断连基线｜`#416` ⑸bis④。
**待他一字母**：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208` WIP 超限。

## 六、常备纪律（血换的）

- 🔴 锁：一次 `acquire` → 多次 `edit-row --who` → 一次 `append-row --section 二` → 一次 `release`，**不串 `commit-edit`**。§二 文件清单每个路径**必须反引号包**（PowerShell 里拼这段必须用单引号字符串）。
- 🔴 **ff 前先清残留 worktree**（`#576`）：三查 ＋ `reports/` 先回收 ＋ `Remove-Item -Recurse -Force` ＋ `git worktree prune --expire now`。**现存 8 个残留待清。**
- 🔴 **长任务只探哨兵，但先核进程**：「进程在、日志不动」＝挂死；「进程没了、哨兵没落」＝炸后不落哨兵。起无头批一律 `C:\Tools\pwsh7\pwsh.exe` 绝对路径；**ff 一次只在飞一条**；纯文档／openspec 改动不传 `-Tests`。
- 🔴 引用时刻先判 UTC／本地并标基准；分诊不得凭强档直接改判；`git -C <目录>` 只在该目录确实是注册 worktree 时才代表它自己。
- 队列真身禁裸 `Read`／`grep`，只走 `工具-队列查询.py`；`*-main-leak.patch` 禁 `cat`，走 `工具-main-leak回放.py`；opener 一律 `工具-opener生成.py` 出；看护件须有 `## 三bis`、`### A<N>` 带 `§一 #<行号>`，且**先出 opener 再落档**；落档后跑 `工具-opener块lint.py --enforce --file <看护件>` 到零违规再派。
- 🔴 **看护件按泳道切片**仍然照做（派单件 4~6 KB），但**别指望它压峰值**——见 §二 真结论。
- 🔴 **续尾禁 `--resume`**：新开 session ＋ 薄续棒件，把现取事实直接喂进去。
