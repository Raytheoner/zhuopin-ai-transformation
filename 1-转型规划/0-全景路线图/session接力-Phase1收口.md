---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，收工「覆盖」不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `取证方法知识库.md` 或队列行；未闭合项写队列行。迁出守 J1。收工自检 `python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 超限当场删。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0910-H` | Cowork 环境总线接棒（转场件 `开场prompt-【Cowork】环境总线接棒-2026-09-10.md`） | 本卡＋两份队列（经锁，§一 436/454/542／§二）＋ `队列回写待补/` ＋ `reports/lane-watch-state.json` | 09-10 10:1x – |

> 并发在跑（勿代登其脏件）：业务总线 `OP-0910-A`、看护者 `OP-0910-G`。

## 二、当前状态快照（2026-09-10 10:4x）

- **全景 35 场景**（五个编号空置不得重分配），S1 目标 9-11。**机制类可动 WIP 26/22 超限**（10:3x `_count_mechanism_wip` 实测；`#545` 出血行带 `WIP豁免`）——🛑 排队 `#381`／`#382`／`#448`／`#464`／`#505`／`#526`。
- **WIP 分诊已拍板（09-10 答 `1a`，结论已落 `#454` 状态列）**：`#455`／`#482`／`#487`／`#505`／`#531` 五条认定为 `blocked`（全改后 26→21、闸自解），🔴 **执行时点排在 K2 外置棒 `OP-0910-P` 之后**——四条现 4.4–10 KB，外置后整格重写成本降一个数量级；09-11 起先改判会撞行长闸。
- **陈忱 8D 样本（`#540` 业／§四 `#193`）**：三个 zip 09-10 09:21 与 10:14 两轮重发**全败**；审计 `media_transfer_retry` 实测 `timeout_seconds=600`（环境变量已生效）但每 attempt 仍 ~10-12 s 抛裸 `TimeoutError` ⇒ 真因＝SDK 内层 `WSClientOptions.request_timeout` 默认 10000 ms 未透传（`aibot/api.py` 的 `aiohttp.ClientTimeout(total=10 s)`），外层 600 s 永远等不到。**已立 §一 `#545` ＋ 派单件 `OP-0910-J`**；取件改走微盘链接（需企微登录态，机器无法代）。
- **K2 行长（09-11 转阻断）**：全池实测 **28 格超 4096 B**（§一 23／§四 5；其中 2 格带 `行长豁免`）——与 `#443` 派单件「清扫吸收 7 条 ＋ 外置 21 行」一致，已由 **棒 1 `OP-0910-K`（清扫）→ 棒 2 `OP-0910-P`（K2 外置）** 串行承接（他 09-10 答 `1a，2a`）。🔴 **订正**：`#439` 状态列现 **1994 B**、早已不超限，接力卡与转场件旧述「`#439` >4096 B」作废。
- **泳道记账（09-10 答 `2a` 后已办）**：`358-audit-retention-p1p2`／`358-p1p2-cc`／`502-sc2-smoke` 三条补终态；测试残留泳道 `A` 已删（备份 `reports/lane-watch-state.json.bak-op0910h`）。🔴 **`436-worktree-drift-alert-clear` 去向核实＝⑵ 从未执行**（`B-0907_Q` 做的是 ⑶③ 使用度量采样）⇒ **不补终态**、已重记转出；其留步前提「等 `#500` ff」现已满足 ⇒ **具备入 `zhuopin-lan-closeout` 收口批条件**（⏭️ 档，Cowork 不代办）。详见 `#436` 状态列新段。
- **`队列回写待补/` 已清池（09-10 答 `3a`）**：28 份逐件核 ⇒ **21 份已证已落后删**（含 AL4 五份），**余 7 份**：`B-0907_Z-371.json`（🔴 目标行 `#371` 已迁 `归档-202608`、活件无此行 ⇒ 该 append 无处可落，且历史不追改）／`B-0908_B-501-质量部7状态格逐字对照.md`（非载荷，是 `#501`⑵ 对照留档，需迁位）／2 份 `待ff合入master后再登`／3 份 `B-0910_*`（在跑会话，不代核）。🔑 **清池判据**：K2 外置会把历史回写段从状态格搬进 `队列行日志/#N.md`——**「真身里找不到」不等于「没落」，必须二轮比对行日志**（本次 11 份因此从「未落」翻案）。
- **定时任务**：`zhuopin-lan-closeout-reminder` 工作日 09:10 只读核四判据。`.51` 项一律 CC（Cowork 无 `ssh.exe`）。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行（26/22）。**

1. **WIP 分诊落地（已拍板，等前置）**：`OP-0910-P` 外置跑完后，把 `#455`／`#482`／`#487`／`#505`／`#531` 一次性改判 `blocked`（`edit-row --changes-json` 整格重写），复测 `_count_mechanism_wip` 应为 21。
2. **`#436`⑵ 入收口批**：前提已满足，交 `zhuopin-lan-closeout`（清 BOM＋ff 常驻执行体＋重启 `ZhuopinAibotDevListener`）；本线只跟踪、不代办。
3. **两份留存载荷处置**：`B-0907_Z-371.json` 作废与否需你一句（目标行已归档）；`B-0908_B-501-质量部7状态格逐字对照.md` 迁去 `队列行日志/` 或场景目录。
4. **`#439` §2 续**：2.2 P1 判别式补拆闭环信；2.3 交他批改时一并给两处矛盾。
5. **不归本线**：`#545`（`OP-0910-J`）／`#443` 清扫与 K2 外置（`OP-0910-K`/`P`）／`#538`／`#539`／`#499`／`#542`／FI5 grill 由各自承接线。

## 四、在途与等他动作（只写指针）

- **等他动作**：① 微盘下载陈忱三包放入 `7-外部文档/质量部/`（§四 `#193` 二次改判，交期 09-11）；② `B-0907_Z-371.json` 是否作废（目标行已归档）；③ §四 `#194`／`#157`（无默认）／`#152`／`#156`／`#170`。
- **登记豁免**逐路径点名＋作者＋到期（`#416`⑶），`release --waiver` 可中途补；一行内人名后 25 字内出现「他」会被称呼 lint 拒，写 `Shao Peishen`。
- **量具坑**：`--row B-…` 不前缀匹配；`--digest --section 二` 不支持；`--field` 只认 `status|all`；🔴 PowerShell 反引号是转义符 ⇒ `--cells-json` 正文用单引号 here-string 写脚本再跑；🔴 §二 文件清单里任何反引号串都按路径校验，`#N`／`[D:业]` 这类记号不能加反引号。
- ⚠️ 「CC 跑完了」核三信源（transcript／队列行／git）；核合入认 message 不认 hash；内容闸用 `git cat-file -e origin/master:<path>`。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不碰真实库、不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17）。配置类代执行须他明示授权＋回读实证。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。**称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；人的属性查名录正本、不推断。
- 🔑 提缺口前先查承接行（并入审核用 `--grep`）；读数太干净先怀疑量具。🔑 **开工先看 `git status --porcelain` 的未跟踪件**——并发会话的派单件/看护件常已把你要报的发现做掉了（09-10 实撞：`#545` 与 K2 两棒均在场）。
- 🔴 opener 一律 Read `opener骨架.md`＋`工具-opener生成.py`（>500 字有文件 ⇒ `--variant reference`）＋`lint --enforce`，禁手抄；短名 ≤12 字。
- 🔴 决策清单末附答复模板（`1a，2a`）；push 不是定夺项；转场五条自检出声、「上下文长」不是判据。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md`；进度 `进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch <批次>` |
| 无头 CC 进展 | `reports/opener-batch/<时间戳>/*.log`＋`summary.txt`；接管 `claude --resume <session-id>` |