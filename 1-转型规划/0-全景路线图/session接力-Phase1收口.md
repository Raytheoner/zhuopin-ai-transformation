---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **本文件是一张定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。**
> **收工时「覆盖」这六块，不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；方法教训写 `0-学习与工具/取证方法知识库.md` 或队列行；未闭合项写队列行。🔴 **任何「本次成果／教训」式的日期节一律不得写回本文件**——它膨胀到 167 KB 就是这么来的：**每一棒只追加、没有一棒负责删。**
> **迁出前守 J1**：要移走一段，先指出承接载体（队列行号或具名文件＋章节）；指不出就先立队列行。
> 🔴 **收工自检（机器可查）**：跑 `python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 报超限就是写多了，**当场删回去**。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 当前进度恢复上下文，按【三、下一会话主攻】继续；开干前先问我 2-3 个澄清。」

约定（2026-06-25 定）：**每次新开 session 都用「读上下文文件交接」**，不靠粘贴长 prompt——读 ① 根 `CLAUDE.md` ② 本卡 ③ 全景规划／实施计划。同步记于 `CLAUDE.md` §5。

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| — | — | — | — |

## 二、当前状态快照（2026-09-04）

- **全景 35 场景**（**五个编号空置不得重分配**）。**S1 目标 9-11**。权威＝全景规划 §加速启动总览 ＋ §0.2。
- **仓库**：主工作区 `master`，与 `origin/master` 同步。**高水位线以 `acquire` 当场回显为准**（🔴 它是「下一个可用」，不是「已预留给我」）。
- **机制类可动 WIP 09-02 分诊后实测 15/22**，🔴 **立新行前仍须当场重跑，不采信本行数字**。新立两条合法路径：⑴ 并入既有行（协议〇.10，优先）；⑵ 立 🛑 行（不计入 WIP，须写 `WIP豁免：` ＋ release 加 `--force-mechanism-wip`）。
- **Cowork 产出一律登记 §二 批次后由 sweep 落库**，不擅自 commit；🔴 **批次行与队列行是两次独立写入**。
- **在跑服务**（`.51`）：8091 保供看板／8092 命令中心／8093 QD-B／8094 FI2。
- 🆕 **事件驱动拆件已上线（09-04 16:55）**：回件落盘 → 桥一打标落信号 → **直接起无头 CC 拆件**，零轮询。服务跑 worktree `ops/wecom-service-home`（已 ff 到 master）。🔴 **轮巡拆件巡逻已由他判永久下线，不得重开**（急件他手动拆）。首次真实端到端验证＝下一条真实回件。
- **专员闸**：`质量部#13` 09-04 已发出（陈忱）；`财务部#16`（唐燕萍）已由他手工拆；`采购部#21`（姚祖怡）仍锁。陈承、泓钦闸开。

## 三、下一会话主攻（环境总线）

🔴 **首要提示：本线缺的不是新任务，是把已有 open 行做完。** ⇒ **除非有出血项，否则不再新立机制行。**

1. 🔴 **`#382` 行长迁移（09-11 起硬阻断，最紧）** —— 状态列已 **23261 B**，超上限 5 倍，靠 `行长豁免` 顶着。按 K2 口径把历史回写段迁 `队列行日志/#382.md`，行内留首段＋末段＋指针。**不是卫生问题，是 09-11 之后写不进队列。**
2. **`#439` R1 口径点台账** —— design 审已过（09-01），包已落 `openspec/changes/coverage-point-ledger/`（含 `specs/`）。apply 前置两件在该包 `tasks.md` §0（真仓库复跑 11 条 `git check-ignore -v`；例外须加在 `.gitignore` 第 32 行**之后**，加前面无效且不报错）。
3. **`#324`（并入 ④⑤）** —— commit message 碎片化 ＋ 高水位线滞后，同源＝**有约定无机制强制 ⇒ 违反不报错 ⇒ 成本由下一个人付**。🔴 后者 09-04 第三次复发（§四 155→156 已当场修）。须走 openspec。
4. **两处 09-04 新登未派**：⑴ `.claude\settings.local.json` 有**通配符位于命令中段**的 Bash 放行规则，会连带批准该位置的任意选项；⑵ 编辑锁 release 把「行长豁免」误当「登记豁免」放行脏文件。
5. **业务端**（不归本线）：`#445`／`#418`／`#379`／`#403`／`#390`。

## 四、在途与等他动作（只写指针，不复述）

- 🔴 **§四 `#157` 等他定夺，无默认项** —— 无头 CC 起活的注入面：**触发权已从他手里转到写回件的人手里**，回件正文（外部不可信内容）经零人工路径进入对全仓库有写权限的 agent 循环。三候选与代价在该行。**不答＝按「维持现状」事实生效。**
- **§四 `#152`**（D-IT11 改口径方向）／**`#156`**（docx 判例表勾选缺陷，唐燕萍反映）待他答；`#153` 已由 09-04 发出质量部#13 实际消化。
- **`#382`⑵** 已交付未销号 —— **原验收基线数错了对象**（数的是说明节的定义示例，真值 0/0/0），须重定判据；**⑶** 已登记未派。**`#151`**（OP 编号无分配机制）咽喉在 `edit-row`／`append-row`，机器守未派。
- **`#416` ⑶ 正在出血**：触发条件＝**工作区有脏文件不属于任何待处理 §二 批次** ⇒ 队列锁死 30 分钟。🔴 **收工务必零孤儿脏文件**。
- **`#447`**／**`#448`**／**`#443`**（每周清扫，须有人看着、不得无头跑）；**SC2 `.51` 未部署** ⇒ `采购部#18` 类发信硬前置未满足。
- ⚠️ **「CC 跑完了」须核三信源再采信**（transcript ／队列行 ／git）；🔴 **核合入不认 commit hash**（rebase 后必变），认 message 或代码内容。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，默认只出 `.md`；**不碰真实库、不写生产码、不发企微**（企微＝Cowork 写 md → CC 发）。
- **合规红线**（全文见根 `CLAUDE.md` §7）：先 mock／脱敏；AI 决策全写审计；OEM 数据隔离；L2 人工门禁；**ASIL C/D ＝ AI 绝对禁区**。
- **排期同步纪律（强制）**：场景实现时间一变 → 同步四份规划 ＋ 重转 docx ＋ grep 零残差。**单一可信源 ＝ §加速启动总览排期表。**
- **称呼**一律 `Shao Peishen`（🔴 路径里的 `Paul Shao` 绝不改）；🔴 **人的属性禁推断**——查 `6-人才与组织/人员名录-称谓与性别-正本.md`，没有就问。
- 🔑 **提一个缺口／开一项新工作前，先查它是否已有承接行。** 🔴 **并入审核必须搜真身**——`--digest` 把任务列截到 40 字，实测同一关键词 digest 0 命中／真身 7 命中。
- 🔑 **一个「看起来正常」或「看起来异常」的读数，都要先怀疑量具。**
- 🔴 **范围结论的迁移，与结论本身同样需要举证**（09-04 一天三次：放宽／放大／平移）；**合入 master ≠ 在跑**，第四形态＝**跑的根本不是 master**（服务跑在 worktree 上，三步齐了才算）；**进程核验以「新 pid 出现」为准，不以「旧 pid 消失」为准**。三条全文与实证见 `§一 #382` 末段。
- **memory 层 Desktop/Cowork 侧判死**（不读不写不引用；权威载体只有根 `CLAUDE.md` 与两份队列）；取证类硬提示见根 §5，**本卡不复述**。

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 历史日期节／会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md` |
| 进度编年 | `进度编年-CHANGELOG.md` |
| 任务真身与状态 | `跨桌任务队列-机制环境.md`／`-业务场景.md`；🔴 只用 `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`，**不得 Read／grep 真身**（K3） |
| 信状态与串行闸 | 跟进信 README「发送状态」列（唯一权威）；查闸 `工具-跟进闸查询.py --to <收信人>` |
| 取证方法与坑 | `0-学习与工具/取证方法知识库.md` |
| 权威排期／场景口径 | `卓品智能AI转型全景规划.md` §加速启动总览 ＋ §0.2 |
| 某份文档在哪 | `文档台账-自动生成.md`（**不要翻目录**） |
