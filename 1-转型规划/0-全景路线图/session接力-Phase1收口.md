---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **本文件是一张定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。**
> **收工时「覆盖」这六块，不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；方法教训写 `0-学习与工具/取证方法知识库.md` 或队列行；未闭合项写队列行。🔴 **任何「本次成果／教训」式的日期节一律不得写回本文件**——它膨胀到 167 KB 就是这么来的：**每一棒只追加、没有一棒负责删。**
> **迁出前守 J1**：要移走一段，先指出承接载体（队列行号或具名文件＋章节）；指不出就先立队列行。
> 🔴 **收工自检（机器可查）**：跑 `python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 报超限就是写多了，**当场删回去**。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 当前进度恢复上下文，按【三、下一会话主攻】继续；开干前先问我 2-3 个澄清。」

约定（2026-06-25 定）：**每次新开 session 都用「读上下文文件交接」**，不靠粘贴长 prompt——读 ① 根 `CLAUDE.md` ② 本卡 ③ 全景规划／实施计划。同步记于 `CLAUDE.md` §5。

## 二、当前状态快照（2026-09-01）

- **全景 35 场景**（v7 质量域五场景下架 40→35，**五个编号空置不得重分配**）。**S1 目标 9-11**。权威＝全景规划 §加速启动总览 ＋ §0.2。
- **仓库**：主工作区 `master`。**高水位线以 `acquire` 当场回显为准**（🔴 它是「下一个可用」，不是「已预留给我」）。
- 🔴 **机制类可动 WIP 实测 23/22，已超限**。**新立 §一 行有两条合法路径**：⑴ **并入既有行**（协议〇.10，优先）；⑵ **立 🛑 行**——以 🛑 起首即不计入 WIP，须行内写 `WIP豁免：` ＋ release 加 `--force-mechanism-wip`（09-01 `#447`／`#448` 即此路，实测加行前后 WIP 同为 23）。
- **Cowork 产出一律登记 §二 批次后由 sweep 落库**，不擅自 commit；🔴 **批次行与队列行是两次独立写入**。
- **在跑服务**（`.51`）：8091 保供看板／8092 命令中心／8093 QD-B／8094 FI2。🔴 **SC2 `.51` 部署仍未做** ⇒ 生产跑旧码。
- **三把专员闸全锁**（09-01 实测，均真锁非转态滞后）：唐燕萍 `财务部#16`／陈忱 `质量部#11` 各在途 4 天，姚祖怡 `采购部#21` 1 天；陈承、泓钦闸开。**业务队列 23 行在办，其中 8 行硬卡在等这三人回件。**

## 三、下一会话主攻（环境总线）

🔴 **首要提示：本线现在缺的不是新任务，是把已有 open 行做完。** 09-01 实测轨迹＝R1 台账 → `#447` 跟进信元数据 → `#448` 队列行元数据 → `#324` 提交函数护栏，**每一步都更往元层漂，且新立的四件没有一件能马上开工**。⇒ **除非有出血项，否则不再新立机制行。**

1. **`#439` R1 口径点台账 —— design 审已过（09-01），只差 WIP 位子。** 全套包内容已预写在 `design审前置-口径点台账三开放点收敛-2026-09-01.md` §5，CC 去 🛑 后原样搬入 `openspec/changes/coverage-point-ledger/`，**不需重读草案**。🔴 apply 前两件硬前置见该件 §9.3（`.gitignore` 例外须在真仓库复跑 11 条；例外须加在第 32 行 `**/*.jsonl` **之后**，加前面无效且不报错）。
2. **WIP 怎么降**：08-31 值周巡检已按 `§四 #142` 判据做过一轮分诊，9 行候选**只 2 行可改**，收益已榨干；09-01 复核确认。⇒ **别再重做分诊**，唯一出路是把在办行做完（或 `#443` 每周清扫，但它迁的是 done 行、不减 WIP）。
3. **`#324`（并入 ④⑤）** —— commit message 碎片化 ＋ 编号高水位线滞后，两条同源（**有约定但无机制强制 ⇒ 违反不报错 ⇒ 成本由下一个人付**）。修法以「给 `_extract_commit_message` 加护栏」为主，**改 help 文案不充分**（09-01 实证：本线从未读 help 也中招）。属改对外语义，须走 openspec。
4. **业务端**（不归本线，交业务总线）：现在就能推、不依赖任何专员回件的有 `#445`／`#418`／`#379`／`#403`／`#390`。

## 四、在途与等他动作（只写指针，不复述）

- **§二 `B-0901_12`** 待 commit（09-01 全部产出），已实测 sweep 会处理。
- **§四 `#143`** 已结案（并入 `#324`）；**`#142`** ＝ WIP 判据与 08-31 分诊结论正本，**答「WIP 上限调不调」前必读**（结论：不调，22 不低）。
- **`#416` ⑶ 正在出血**：机器人 release 被拒 ⇒ 队列锁死 30 分钟，**触发条件＝工作区有脏文件不属于任何待处理 §二 批次**。⇒ 🔴 **收工务必确保零孤儿脏文件**，否则下一封专员回件到达即咬住队列。
- **`#447`**（跟进信 `决策点:` 字段失传 ＋ 转态覆盖事实日）／**`#448`**（队列行 `[B:]` 阻塞源字段）—— 均 🛑 排队，待 WIP 回落。
- **SC2 `.51` 未部署** ⇒ `采购部#18` 类发信硬前置未满足。
- ⚠️ **「CC 跑完了」须核三信源再采信**（transcript ／队列行 ／git）。🔴 **核合入不认 commit hash**（rebase 后必变），认提交 message 或代码内容。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／招聘／汇报，默认只出 `.md`；**不碰真实库、不写生产码、不发企微**（企微＝Cowork 写 md → CC 发）。
- **合规红线**（全文见根 `CLAUDE.md` §7）：先 mock／脱敏再真实；AI 决策全写审计；OEM 数据按客户隔离；L2 人工门禁；**ASIL C/D ＝ AI 绝对禁区**；对客闸全程关。
- **排期同步纪律（强制）**：场景实现时间一变 → 同步四份规划 ＋ 重转 docx ＋ grep 零残差。**单一可信源 ＝ §加速启动总览排期表。**
- **称呼**一律 `Shao Peishen`（🔴 路径里的 `Paul Shao` 绝不改，历史不追改）；🔴 **人的属性禁推断**——查 `6-人才与组织/人员名录-称谓与性别-正本.md`，没有就问；**起草对外件后须扫第三人称代词逐个回名录核对**。
- 🔑 **提一个缺口／开一项新工作前，先 grep 它是否已有承接行。** 09-01 一天内两次差点重复劳动（差点重做 08-31 已做的分诊；差点重登 `#324` ④ 已记的缺陷）——**都是从「该有什么」出发，没先查「已经有什么」**。
- 🔑 **一个「看起来正常」或「看起来异常」的读数，都要先怀疑量具。** 09-01 三例：PowerShell 逐行比对给出假「相同」（真因是行尾符被规范化）／自造路径给出假缺口（真身命名不同）／朴素 grep 报「待 commit 批次＝0」（sweep 实际按状态列**开头片段**判定）。
- **memory 层 Desktop/Cowork 侧判死**（不读不写不引用；权威载体只有根 `CLAUDE.md` 与两份队列）；取证类硬提示（沙箱 git ≠ 本机 git／退出码只认本进程／时间戳 UTC vs 本地）见根 §5，**本卡不复述**。

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 历史日期节／各次会话细节 | `session接力-Phase1收口-归档-202608.md`／`-归档-202607.md` |
| 进度编年（根 `CLAUDE.md` 迁出的条目） | `进度编年-CHANGELOG.md` |
| 任务真身与状态 | `跨桌任务队列-机制环境.md`／`-业务场景.md`；查行用 `python 0-学习与工具/工具-队列查询.py --row N --section 一` |
| 信状态与串行闸 | 跟进信 README「发送状态」列（唯一权威）；查闸 `工具-跟进闸查询.py --to <收信人>` |
| 取证方法与踩过的坑 | `0-学习与工具/取证方法知识库.md` |
| 权威排期／场景口径 | `卓品智能AI转型全景规划.md` §加速启动总览 ＋ §0.2 |
| 某份文档在哪 | `文档台账-自动生成.md`（**不要翻目录**） |
