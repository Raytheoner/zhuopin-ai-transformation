---
status: 在办
title: "Session 接力（滚动·最新）—— 全景规划实现"
---

# Session 接力（滚动·最新）—— 全景规划实现

> 🔴 **本文件是一张定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工时「覆盖」六块，不「追加」。** 进度叙事写 `进度编年-CHANGELOG.md`；教训写 `0-学习与工具/取证方法知识库.md` 或队列行；未闭合项写队列行。迁出前守 J1（先指承接载体）。收工自检：`python 0-学习与工具/工具-CLAUDE进度段lint.py`，J6 超限当场删。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/0-全景路线图/session接力-Phase1收口.md` + CLAUDE.md 恢复上下文，按【三、下一会话主攻】继续；A 类同线转场先做开工自检三查、不问。」

约定：每次新开 session 都用「读上下文文件交接」，不粘长 prompt。

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| ~~`OP-0907-AL`~~ | ~~Cowork 环境总线接棒~~ | 收工 09-07 16:4x；产出登 `B-0907_AL1`；无「队列更新待补」 | 09-07 16:1x – 16:4x |

## 二、当前状态快照（2026-09-07 16:4x）

- **全景 35 场景**（五个编号空置不得重分配），S1 目标 9-11。**可动 WIP 22/22**（09-07 纠偏后；🔴 立新行前当场重跑 `_count_mechanism_wip`，不采信本行）；**余量优先需求侧**（rules/队列与落库）。
- **需求侧链已通到「回件」**：grill → `intent.md` → CI intent 闸 → design 审 → 口径点台账（**30 点**：FI10 7＋黄金集 23；已签认 14／在途 10／待问 6）→ 投影信 `财务部#17`（09-06 已发，FI10-G-07 `在途`）→ 事件驱动拆件（`#499`）。**没接上的一段＝回件→台账回写**：CC `OP-0907-AK` 在建（worktree 已有 `cli.py`／`letters.py`／测试，未 commit）。
- **`#439` tasks 2.1 已做**（`OP-0907-AL`，登 `#439` 末段）：拆点原料与幂等脚本在 `6-人才与组织/部门AI专员跟进/口径点台账-回溯原料/`；`tasks.md` 2.1 勾选**未动**（AK 触碰区）。
- **CC 在跑**：`B-0907_R` 已收工（六停点他已答，回写入 master `818cf99`／`2e2aa2f`）；`B-0907_Y` 波一在跑（0 停点，`op0907x-358` 3 commits、`447`／`487` 尚 0）；`OP-0907-AK` 在建。Cowork：`OP-0907-M`（FI8 grill，intent 待确认 8 点）。
- **合审 11 项全 (a)** 已回写；cooldown 包 design 审过、`tasks.md` 可开工（未入 Y，下一批）。
- **在跑服务**（`.51`）：8091／8092／8093 QD-B／8094 FI2；`B-0907_R` 转出 1 项（`#436`⑵ `.51` 半边）交 `zhuopin-lan-closeout`。Cowork 本机 `ssh.exe` 不可用。
- **看护批上限 10**，并行 4、错峰 90 s。

## 三、下一会话主攻（环境总线）

🔴 **本线缺的不是新任务，是把已有 open 行做完；除非出血，不新立机制行。**

1. **`#439` §2 续**（Cowork）：2.2 用 P1 判别式补拆其余已闭环信（训练集 38 封；先跑 `--dry-run`，复制回溯脚本改 META）；2.3 未闭环 21 封拆完交他批改时，**一并给两处矛盾**（07-29 跨月占用信 README「未发」vs 08-06 回件件；`采购部#7` 编号推定）；AK 合入后补勾 `tasks.md` 2.1。
2. **收 CC 复命**：Y 三波（每波 ff 属 🟡，他一字母；等待时长只认 `summary --batch B-0907_Y`）→ AK（2b.1–2b.4 核三信源；之后 Cowork 做 2b.5＝真实 `财务部#17` 回件走通才勾 §6.2③）。
3. **FI8 intent 已确认后**：§三 八点建点入台账、design 审；余 6 包 grill 次序 FI6→FI5→FI9→SC10→SC11→SC4（复制 `开场prompt-【Cowork】FI8需求grill-第二个样本-2026-09-07.md` 改场景名）。
4. **下一看护批候选**（Y 收工后）：`#448` apply（等 A8）／`#459`／`#312`／`#326`（软序 `#448`）／cooldown tasks／`#440` R4 propose＋design／`#22`（timed 09-12）。
5. **K2 行长**：09-11 起阻断；`#398` 4.2 KB、`#474` 待外置、`#439` 本次又追一段。**WIP 计数 `**🛑` 前缀漏计**待并入 `#455` 族。
6. **业务端**（不归本线）：`#445`／`#418`／`#379`／`#403`／`#390`；`OP-0907-N` FI2。

## 四、在途与等他动作（只写指针）

- **等他答**：§四 `#167`/`#168`（周报六项指标，推荐 1a，2a）；FI8 intent 8 点确认；Y／AK 收工 ff 字母；§四 `#157`（无头 CC 注入面，无默认项，不答＝维持现状）；`#152`／`#156`／`#170`。
- **`#416` ⑶ 出血**：脏文件不属任何待处理 §二 批次 ⇒ 锁死 30 分钟；🔴 收工零孤儿脏文件；**acquire 的 note 必须一开始就带「登记豁免：…」**（本会话漏写一次，只能改锁文件 note 后 release）。
- **量具坑**：`工具-队列查询.py --row B-0907_W17 --section 二` 对带后缀的批次名**不前缀匹配**、报「未找到」——判 §二 批次是否已登，用 `append-row` 撞号拒绝或传全名。
- **`#151`** OP 编号无分配机制（取号 grep `OP-0907-*`，本线已用到 AL）；**SC2 `.51` 未部署** ⇒ `采购部#18` 类发信前置未满足。
- ⚠️ 「CC 跑完了」核三信源再采信（transcript／队列行／git）；核合入认 message 不认 hash。

## 五、常驻纪律（分工·红线·踩过的坑）

- **Cowork** ＝ 规划／设计／治理／汇报，只出 `.md`；不碰真实库、不写生产码、不发企微；产出登 §二 由 sweep 落库（每小时 :17，起跑段无条件补推）。
- **合规红线**：先 mock／脱敏；AI 决策全写审计；OEM 隔离；L2 人工门禁；ASIL C/D 绝对禁区。
- **称呼** `Shao Peishen`（路径 `Paul Shao` 绝不改）；人的属性查名录正本、不推断。
- 🔑 提缺口前先查承接行（`--digest` 截 40 字，并入审核用 `--grep` 搜真身）；🔑 读数太干净先怀疑量具；管道末端退出码不是命令的退出码。
- 🔴 opener 一律 Read `opener骨架.md` 逐字套用＋`工具-opener块lint.py --enforce`；派出线＝线名＋OP 号；F5 短名 ≤12 字；OP 号只用字母。
- 🔴 决策清单末附答复模板（`1a，2a`）；push 不是定夺项；「上下文长」不是转场判据，五条自检出声。
- memory 层 Cowork 侧不读不写；权威载体只有根 `CLAUDE.md`、rules 与两份队列。

## 六、往哪查

| 要找什么 | 去哪 |
|---|---|
| 历史会话细节 | `session接力-Phase1收口-归档-202608.md`／`-202607.md`；进度 `进度编年-CHANGELOG.md` |
| 任务真身 | `python 0-学习与工具/工具-队列查询.py --digest`／`--row N`（不得 Read／grep 真身） |
| 需求侧方案与决议 | `端到端构建workflow优化-方案-2026-09-06.md` §四／§六 |
| 看护批状态 | `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_R／Y` |
| 台账 | `6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`；信状态＝跟进信 README「发送状态」列 |
| 某份文档在哪 | `文档台账-自动生成.md` |
