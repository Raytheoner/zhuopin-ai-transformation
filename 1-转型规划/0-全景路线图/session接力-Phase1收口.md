# session 接力 · Phase1 收口卡

> 覆盖不追加，8 KB 上限。最近改写：2026-09-19（Cowork 泳道看护 `OP-0919-A` 收工）。
> 🔴 **新接棒的第一件事**：读本卡全文 ＋ `CLAUDE.md`，然后跑 `python 0-学习与工具/工具-队列查询.py --digest` 扫两份队列，核触碰区无重叠再动手。

## 一、正在跑的

**没有在跑的会话或泳道。** 本日 `B-0919` 九批全部收工，编辑锁已放，临时 worktree 与 `.git` 锁清零，master `e3942e83` 与 origin 同步。

🔴 **接棒入口＝`1-转型规划/0-全景路线图/派单件-泳道看护续棒-2026-09-19-B.md`（`OP-0919-J`，A 类）**——`OP-0919-A` 交棒，理由＝转场判据 ①②阳性（把 `#602` 当成「待拍两问」而那两问 09-17 已答；据此险些重跑九文档重排。**两条同源：读的是自己写过的东西，不是现取的行文**）。

**唯一未跑完的活**：乙档最后两条 ff——`claude/op0910l-507-sweep-apply` 与 `claude/op0907ai-sweep-manifest-479-apply`。两条都要 `test_工具-落库sweep.py`，**单跑 19 分 44 秒、六关双侧 ≈40 分钟**，故本轮未排。⚠️ `op0910l` 只跑过分支侧一次得 **19 failed／492 passed**，**没跑 master 基线，不可据此下任何结论**（六关④ 判的是「与纯 master 比零新增」，不是「全绿」）。

## 二、本日成果（master `9cc4dab3 → e3942e83`，7 条分支 ff 入库 ＋ 重组循环第 12 例）

| 批次 | 做了什么 |
|---|---|
| `B-0919_Q2建造` | `#592`⑵ 复核确认红线②③ 转人工已于 09-17 建成；⑷ PPT D2 场景勾选行解析器新建（`QD-A doc_reader.extract_scene_checkbox`，读 PPT 勾选态非 Word `w14:checkbox`）；`#612` §5.1 Q2 门户页 `/quality/q2`（19 passed） |
| `B-0919_机制清池` | 销号 `#605`／`#607`／`#613`／`#603`；C 段 28 条分支逐条判定落 `取证件-2026-09-19-C段分支逐条判定.md`；查出 `#615` 出血 |
| `B-0919_机制缩编` | `#620` opener 双点机守（16 例回归）、`#618` 陈旧锁哨兵（并进 `工具-worktree体检.ps1`／30 分钟阈值，19 例）、`#616` 零命中断言守（F13 硬关卡＋生成器软提示） |
| `B-0919_缩编收口`／`ff回写`／`ff收尾` | 代泳道回写、六行补 ff 结论、`#617` 乙档逐条结论 |

**重组循环第 12 例（v11，纯字句级，`#602` 三处遗留待裁收口，他答 `1a，2a，3a` ＋ 重裁 `1a，2a`）**：① 覆盖轨「9-11」保持不动、另记独立读数「S1 期内启动 18 个场景」，两数并列不合并；② 6/8 周纪律维持 v10「只标不改、不套用」，**八个前拉场景一律不按 6/8 周判 D 档**，改按「口径包发出后 1–2 轮（3–6 天）收敛」实测回填——**前置总表七行的倒排日期只作参照、不作判档依据**；③ 前置总表补建七行（`FI5`／`FI6`／`O3`／`FI4`／`FI8`／`FI10`／`FI7`），Owner 全部留空、登 §四 `#213` 待他指派。§0.2 登 v11、变更日志追第 12 例、两份 docx 重转并核过 mtime。**无场景增删、无排期变动，全景仍 35。**

🔴 **`#615` 出血已止**：它早已 `[S:done]`，但 `.51:8097` 的部署三脚本、冒烟、回滚 SOP **一行都不在 master**，全压在 `claude/op0918a-fi3-deploy` 上。已 ff 入库（`45260da6`）。**这是 `#568`／`#617` 那条缺陷第一次踩在已销号的行上。**

## 三、本机环境三处必须知道

🔴 **`pwsh` 是单点**：全机只有 `C:\Tools\pwsh7\pwsh.exe`（7.6.6）。**起无头批必须用这条绝对路径**——`-Detach` 用 `(Get-Process -Id $PID).Path` 继承调用方 shell，用 `powershell`（5.1）起会当场 `NamedParameterNotFound`、零泳道日志而父进程照报成功（`#567`）。本日四次起批全部 `shell=C:\Tools\pwsh7\pwsh.exe`、`launcher-stderr.log` 0 B。**误删即整条派发链停。**

🔴 **Cowork 挂载侧 `rm` 被沙箱拒**（`Operation not permitted`）⇒ 任何「用文件当锁」的工具从那边跑都只能跑一次：`工具-opener生成.py` 第二次调用必 fail-closed。**绕行＝改在 Windows 侧跑**（`Windows-MCP PowerShell` ＋ `C:\Dev\zhuopin-ai`）。同理，队列锁工具走挂载侧动辄 120 秒超时，也改走 Windows 侧。

🔴 **`.git` 一天长一枚陈旧锁**：09-18 清过一次，09-19 又冒出 `.git/objects/maintenance.lock`（0 字节、陈旧 10 小时、`Get-Process git` 为零）。清前三条判据必须齐备。`#618` 的哨兵已合入 master，下次由机器管。

## 四、仍开的与待你一字母

**仍开**：`#603`（已销号）除外——`#614`（四条已定，落地在他手上）、`#617`（乙档余 2 条 ff＋4 条待裁）、`#600`／`#595`／`#477`；机制类可动 WIP 仍超 §四 `#208` 上限。**触碰区撞车**：`#539`（SC2 排版）与在办的 `#538` 同触碰 SC2 周报页面，已判不领。

✅ **2026-09-19 已裁（他答 `1a，2a，3a`）**：
1. **四条老 apply 分支判放弃、本地 ref 已删**（`op0910m` `904a83e7`／`op0907aj` `261f747a`／`a30` `32feda54`／`a22` `93245b2a`）。删前逐条复核 branch＝backup＝origin 同 sha，删后再核一次：**`backup/<短名>-pre-rebase` 与 `origin/claude/<短名>` 全部完好，取回路径两条**。🔴 **远端一条未动**（照 09-18 先例）。
2. **余两条 ff 延后**：`op0910l-507-sweep-apply`／`op0907ai-sweep-manifest-479-apply`，下次开工后台跑。
3. `op0910n-529-timeout-semantics` **维持挂起**，等他单独裁口径。

🔴 **下次开工第一件事（他 2026-09-19 答 `1a` 定）**：把 `#602` 的「待拍两问」整理成**一页现取事实＋选项**再交他拍——⑴ S1 覆盖轨目标（文档 9-11，前拉后实为 18）；⑵ 前置总表 6 周·8 周纪律（与 v10 新工期模型同源不自洽）；⑶ 七个前拉场景缺失的前置行（卡 EE-5 待定项 3/4/6）。🔴 **现取、不转述；Owner 不代填。**

🔴 **另一条仍待他一字母**：机制类可动 WIP 超限的长期处置（§四 `#208`，现取 29／22）。

## 五、常备纪律（血换的）

- 🔴 **看护者不在泳道运行期间往主仓写文件、也不长持队列锁**——本日各撞一次：写文件致第三道闸把看护者自己的产出判成泳道 `main-leak`；持锁致泳道收工写不进队列、只能以 `OPENER_PARTIAL` 收尾等人代补。
- 🔴 **`main-leak` 误判查实一处可当场收口的白名单缺口**：白名单收了 `合入登记/pending-ff.jsonl`，**没收同目录、每次 ff 必写的 `合入登记/ff-patrol-<yyyyMMdd>.jsonl`**。本日三条泳道干完活全被判 FAIL，并两次冲掉排在其后的串行泳道（`#611` 已记，销号判据＝用那三份 patch 回放必须不再判 FAIL）。
- 🔴 **ff 慢不是六关的问题，是测试目标选宽了**：传整个 `0-学习与工具` 目录，单条 26 分钟；换成分支实际触碰的 `test_*.py`，单条 **45 秒**。
- 🔴 **泳道收工不自删 worktree 是常态**（`#576`）：ff 前先 `git worktree remove`，否则 `git worktree add` 撞「branch already used」，而脚本只报 `exit=9 脚本异常`、不报是哪条占着（`#618` 已修并合入）。
- 🔴 **rebase 冲突先看两侧是不是都在追加**：`op0919b` 与 `op0919c` 同时往 `Q2-8D报告AI判定/CLAUDE.md` 时间线追加，按时序合成五行、一字未弃；而四条老分支的冲突在核心码上，那就不是合并问题，是「该不该合」的问题。
- **worktree 是 git 的视图不是仓库的视图**：`.gitignore` 路径在泳道里「零命中」一律假阴性（`#616` F13 硬关卡已上线机器守）。
- **相关不是因果**；**只会报成功的守卫等于没有守卫**；**旧记忆不能冒充实测，转述不是真身**（本日两撞，见 §一 交棒理由）。
- 队列真身禁裸 `Read`/`grep`，只走 `工具-队列查询.py`；状态格超 4096 B 走 `工具-队列行K2外置.py`；回写优先 `commit-edit`／`commit-append`。
- opener 一律用 `工具-opener生成.py` 出；看护件须有 `## 三bis` 标题；`### A<N>` 标题须带 `§一 #<行号>`。
