---
title: "移交单 · O1 生产排程智能优化 → 注销／并入（重组循环）"
created: 2026-09-02
移交方: Cowork 业务总线 OP-0902
承接方: 🔴 全景路线图线（重组循环执行方）
拍板依据: Shao Peishen 2026-09-02 答 §四 #150 ＝ (c) 注销／并入
status: 待执行
---

# 移交单 · O1 注销／并入（重组循环第 N 例）

> 🔴 **本件是移交单，不是执行件**。按根 `CLAUDE.md` §5 分工红线——**域专线出局部定稿＋移交单、不直接改全景；全景路线图线执行重排、不重开域业务口径**。
> 业务总线（本线）已完成事实核定与拍板承接，**全景规划正文与权威排期表一字未动**，由全景路线图线按 skill `zhuopin-rebaseline` 的固定闭环执行。

---

## 一、拍板原文与范围

**Shao Peishen 2026-09-02 答 §四 `#150` ＝ (c)**：

> **(c) 注销／并入** —— 若「外协交付节奏」与 SC2 周报、SC8 交期承诺重叠度高，考虑并入而非独立成场景。

⚠️ **拍板选的是方向，不是终态**：(c) 内部还含「注销」与「并入」两种落法，**并入到哪个载体须由承接方按重叠度实测判定**（见 §四 待定项 1）。

---

## 二、为什么注销 —— 三条已核定的事实（承接方可直接引用，无需重查）

| # | 事实 | 取证 |
|:--:|---|---|
| 1 | **我司无自有车间／产线，成品全部经外协供应商生产** | 陈承 2026-08-24 `IT部#10` 回件转述 PMC 原话：「跟 PMC 沟通目前 ERP 没有生产工单——无车间/产线，经外协供应商生产」 |
| 2 | **`UFIDA.U9.MO.MO.MO`（MO 工单实体）里没有数据** —— 端点即便建成、授权即便买了，返回的也是恒空表 | 同上；且已连带证伪 `IT部#9` 那 ¥72,000 授权诉求（其头号理由正是「O1 取不到 MO」） |
| 3 | **委外 PO 替代得了「有没有在生产」，替代不了「工时是多少」** | `ZpViewPurOrder.erpTypeCode ∈ {PO14,PO16,PO17}`（`models.py:61-67`、`sc2/sources.py:48`）无开工日／完工日／工时；其「制单日→入库日」是**供应商交付周期、是另一个量** |

🔑 **一句话**：**O1 的立项标的是「SMT 产线排程智能优化」，而我司没有产线 ⇒ 这件事没有对象。** 不是做不动，是标的不存在。

⚠️ **连带失效**：O1 晋档 2 的四条解锁条件里，第 1 条（MO 可取数）**永不满足**、第 2 条（PMC 签认 SMT 工时）**标的存疑**——「我司的 SMT 工时」这个量是否存在都要先回答。两条已于 2026-09-02 在 `4-数字员工/运营部/O1-生产排程智能优化/CLAUDE.md` §6 改判并留痕（原文保留作沿革）。

---

## 三、承接方要做的（重组循环固定闭环）

按 skill `zhuopin-rebaseline` 与《全景路线图重组机制与变更日志》执行：

1. **挂冻结标**（§三），停 O1 在途建造——⚠️ 实测 O1 当前无在途建造（档 1 已完成、未部署 `.51`、无跟进信），冻结标主要作用是防止本次重排期间有人重新开工。
2. **九文档重排**：全景规划 §2.1.3 运营段 O1 块／§加速启动总览权威排期表 O1 行／四阶段／甘特指针／实施计划总清单＋时间线＋Phase 2／相关前置数据总表 —— **逐处清扫，grep 全库零残差**。
3. **§0.2 登记修订记录**（🔴 **不登记视为无效修改**）——须写明：拍板人／日期／依据件（本移交单 ＋ §四 #150）／场景数变动／编号是否空置。
4. **场景数与编号**：当前全景 **35** 场景。O1 若真注销 ⇒ **34**；🔴 **编号 `O1` 就此空置、不得重新分配**（同 v7 五场景下架惯例，避免同一编号在 git 历史中指向两个不同场景）。
5. **回填销号**：§四 `#150` 与 §一 `#403` 相关子项。

---

## 四、🔴 承接方须自行判定的三件（本线不代定）

1. **「注销」还是「并入」**：须实测「外协交付节奏跟踪」与 **SC2 周报**、**SC8 交期承诺**的重叠度。
   - 若重叠高 ⇒ 并入既有场景做子功能，**不新增编号**；
   - 若确有独立价值 ⇒ 那已是一个**新场景**（标的与原 O1 不同），须走新增流程、**不得沿用 O1 编号**。
2. **O1 已完成的档 1 产出怎么处理**：`4-数字员工/运营部/O1-生产排程智能优化/` 已有可跑代码（32 tests 全绿、O2 与底座回归零漂移）。**注销 ≠ 删代码**——是留作可复用资产、还是随场景一并归档，须判。
3. **链路与联动点连带**：O1 在四链联动蓝图里的位置（D-A 主干链 `…→O2/O1→SC8→…`）**下架后是否断链**、联动点计数是否变化 —— 同 v7 那次「链 C 由 3 节点降为 2 节点、联动点 12 → 11」的形态，**这一类连带最容易被漏**。

---

## 五、本线已做与未做（如实登记，边界清楚）

**已做**：
- 事实核定（§二三条，全部有取证）
- `O1/CLAUDE.md` §6 解锁条件改判 ＋ 新增 §6.1 三方向 ＋ §5 时间线留痕
- §四 `#150` 立行并取得拍板
- 本移交单

**未做（刻意，非遗漏）**：
- 🔴 **全景规划、权威排期表、实施计划一字未动** —— 属全景路线图线职责
- 🔴 **未删除任何 O1 代码或工程目录**
- 🔴 **未判定「注销」还是「并入」** —— 见 §四 待定项 1

---

## 六、时点

**无硬期**。但 O1 原排期锚 **2026-09**，**拖着不处置等于让一个已知没有对象的场景继续占排期位**；建议随下一次重组循环一并做，不必单独起一轮。
