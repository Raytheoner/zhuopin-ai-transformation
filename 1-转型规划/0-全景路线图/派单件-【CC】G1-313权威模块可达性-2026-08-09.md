---
title: 派单件·【CC】G1 —— #313 queue_table 权威模块可达性收尾（含一处正在发生的生产失效）
created: 2026-08-09
执行方: CC 平台
status: 待发
---

# 派单件 ·【CC】G1 · 队列 #313（P1）

> **本件由 Cowork 环境总线备，Shao Peishen 2026-08-09 明示选 (a)「派 CC 尽快修」，故属经授权的直接派活**（§六bis C2 例外条款）。
> **次序与并行**：本件**只含 1 个任务**，故无需次序矩阵。**与当前待领池零重叠**——#252／#82／#221 为业务域，#98 是月度例行触点，#312 触碰看板 artifact 与 `decision_reminder.py` 的**提醒逻辑**（本件只碰其 **import 路径**，⚠️ **同文件不同处，若 #312 同期开工须错开或同车**）。
> ⚠️ **唯一需错开的点**：若 CC 侧尚有 **#308 未 archive 的收尾工作**触碰 `工具-共享文档编辑锁.py`，与本件顺带项 ⑥ 同文件，**须错开或同车**。

---

## 开场词（复制即用）

```
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☑ 新建 `queue-table-reachability` ｜ 工作区：本项目 ｜ session：新开

领取队列 #313（P1）。读 1-转型规划/0-全景路线图/派单件-【CC】G1-313权威模块可达性-2026-08-09.md ＋ 队列 §一 #313 行 ＋ 根 CLAUDE.md 当前进度恢复上下文。

🔴 子项 ③ 是正在发生的生产失效（每轮 sweep 都在崩），请先止血再做 ①②。开工先按派单件 §三 复现一次，确认根因与派单件描述一致再动手；不一致以你实测为准并回写本行。
```

---

## §一 为什么是 P1：一处正在发生的生产失效

**现象**（`reports/sweep-commit.log`，每轮复现）：

```
⚠ 决策提醒第二载体退出码 1（…UTC，不影响本轮批次处理）：Traceback (most recent call last):
  File ".../scripts/decision_reminder_check.py", line 50, in <module>
    from aibot_service.decision_reminder import (...)
  File ".../aibot_service/decision_reminder.py", line 48, in <module>
    from zhuopin_platform.shared_tools.queue_table import SECTION_COLUMN_COUNTS
ModuleNotFoundError:
```

**根因已用三条只读命令坐实（非推断，但请自行复现一次）**：

| 取证 | 结果 |
|---|---|
| `python -c "import zhuopin_platform,os;print(os.path.dirname(zhuopin_platform.__file__))"` | `…/.claude/worktrees/followup-dispatch-apply-25679f/5-平台底座/zhuopin_platform/zhuopin_platform` —— **陈旧 worktree 的全局 editable 指针** |
| 该目录下 `shared_tools/queue_table.py` 是否存在 | **False** |
| 本 checkout 内同一文件 | **True** |

🔑 **为什么此前一直没事、偏偏今天崩**：同一文件 **L47 的 `from zhuopin_platform.audit import AuditEvent` 照常成功**——旧指针里有 `audit`，**只是没有 #306 今天才建的 `queue_table.py`**。**⇒ 这个错误的指针早就存在，是新增一个模块把它照出来的。**

**⇒ 这是 #300 的失败模式在生产环境的真实兑现**：#300 的结论是「`conftest.py` 与服务入口脚本已内置路径引导、`pip install -e` 降为可选」，**但 `scripts/decision_reminder_check.py` 的引导只有 `sys.path.insert(0, SERVICE_DIR)`（L41），没有插入 `5-平台底座/zhuopin_platform`**——**#300 的覆盖面有一处遗漏，本件即证据。**

🔴 **后果比日志看起来严重**：#219 的立意就是「决策提醒由单载体改为双载体」，防 `ZhuopinDecisionReminderDaily` 错过一轮就没人提醒。**现在第二载体每轮崩、双载体实际退回单载体**，而 sweep 把它降级成一行 ⚠ 并明写「不影响本轮批次处理」——**对人而言同样是「什么都没发生」**。

⚠️ **必须分别归因，勿一并归给 #306**：全日志 **19 次**「第二载体退出码 1」中**只有 2 次**是 `queue_table` 变体；其余 **17 次起于 2026-08-05 14:58 UTC，根因未查**。请顺带判一下那 17 次是什么，**若属另一根因且仍在发生，如实登记为独立项**（可回写本行或另立，由你判）。

---

## §二 交付清单（六项，① 为止血、优先）

| # | 内容 | 备注 |
|---|---|---|
| ① 🔴 **止血** | 给 `scripts/decision_reminder_check.py` 补 #300 同款 `zhuopin_platform` 路径引导，**并逐一核一遍还有哪些入口漏了**（#300 当时覆盖的是 6 份 `conftest.py` ＋ 5 个 `run_*.py`，本脚本不在其中——**说明当时的清单是枚举出来的，不是穷举出来的**） | 修完须**真实跑一次** `decision_reminder_check.py` 确认退出码 0，不接受只改不验 |
| ② **除根** | 卸掉指向陈旧 worktree 的全局 editable install（`pip uninstall zhuopin_platform`），使解析只能走引导 | ⚠️ **会影响所有 worktree**，先确认无其它进程依赖该指针；卸完须跑一轮全量回归确认没有别处在偷偷依赖它 |
| ③ **不再静默** | sweep 对「第二载体退出码非 0」是否由「只写日志」升为告警 | 🔴 **须先答协议〇.9 措施 B 的 one-in-one-out**——退休哪一项。**若判定无可退且不值得为它破例，如实说明并不做，同样是合格结论** |
| ④ **兜底桩可见化（拍板项 10 选 (b)）** | 由 `工具-队列结构lint.py` 在 CI 里**断言权威模块 `zhuopin_platform.shared_tools.queue_table` 可 import** | 🔴 **one-in-one-out**：本线建议同批退休 `zhuopin-queue-audit` 类型⑧「预登记批次陈旧」；⚠️ **该「至今零命中」断言本线只查了当前 §二、未回溯历史巡检简报，请先复核**。四处兜底桩（编辑锁 L217／台账 L30／sweep L356／队列查询 L53）**不主张删**——它们是 #306 apply 时被两个真实隔离测试用例逼出的，有正当理由；本项只求**降级可见** |
| ⑤ **路径解析收拢** | 队列路径 **3 处**字面量（编辑锁 `DEFAULT_TARGET`／队列查询 `DEFAULT_TARGET`／台账 `QUEUE_PATH`）收进 `queue_table.py` | **仓库根解析 10 处**（`发企微.py`／`工具-CI矩阵发现.py`／`工具-共享文档编辑锁.py`／`工具-孤儿worktree扫描.py`／`工具-密钥扫描lint.py`／`工具-文档台账生成.py`／`工具-落库sweep.py`／`工具-队列查询.py`／`工具-队列结构lint.py`／`补R1状态头.py`）**是否一并收由你评估**，**实际范围如实回写本行**——**不要因为顺手做了小的就默认大的也该顺手做**（这正是 #306 当初的原话） |
| ⑥ **顺带项** | `工具-共享文档编辑锁.py` 的 `MECHANISM_WIP_CAP_DEFAULT` **8 → 16** | Shao Peishen 2026-08-09 拍板 (b)。**协议正文与值周巡检 prompt 两处已由环境总线改完，只剩这一处代码**。按 #112／#208「顺带修复」范式，**批次行内单列一句即可，不另立行**。⚠️ **顺带核**：看板 artifact `zhuopin-project-status` 的 WIP 分母是否仍是 8 |

---

## §三 开工第一步：先复现，再动手

```powershell
cd "C:\Users\Paul Shao\OneDrive\Projects\企业AI转型"
python -c "import zhuopin_platform,os;d=os.path.dirname(zhuopin_platform.__file__);print(d);print('queue_table:',os.path.exists(os.path.join(d,'shared_tools','queue_table.py')))"
python "5-平台底座\wecom-aibot-service\scripts\decision_reminder_check.py"; "EXIT=$LASTEXITCODE"
```

**若与 §一 描述不一致，以你的实测为准并回写 #313 行**——本件的取证是 Cowork 侧只读做的，**没有跑过修复路径**。

---

## §四 openspec 判定（须在 #313 行写明结论）

按根 `CLAUDE.md` §5 机制类三条门槛逐条判：

- **①④⑥ 大概率不触发**：纯可观测性增强／CI 断言／一个常量值，**不改既有函数在相同输入下的行为**。
- **②⑤ 需要判**：②改变模块解析来源（虽然预期行为不变，但**"预期不变"正是需要被验证的那件事**）；⑤ 若使既有函数在相同输入下行为改变，**即命中第③条门槛**。
- **判不准就走**——多一道 design 审约半天，而机制类无 spec 在 IATF「单一可信源」下站不住。

---

## §五 收工

全量回归零漂移（编辑锁／sweep／队列查询／台账／队列结构lint／`wecom-aibot-service`）→ 回写 #313 行（**六项各自的实际范围与未做部分如实登记，勿冒领**）→ commit + push → 收工重跑台账。
