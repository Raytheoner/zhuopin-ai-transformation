---
title: "看护件 · 泳道看护批 B-0912_心跳与称谓"
created: 2026-09-12
status: 生效
批次: B-0912_心跳与称谓
派出线: Cowork 环境总线 OP-0910-H
执行形态: 无头（`工具-opener批处理执行v2.ps1 -Detach`）——本批默认不粘贴，三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_心跳与称谓`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴**（`opener骨架.md`「一次粘贴」节 2026-09-12 新口径）。启动命令：

```
powershell -ExecutionPolicy Bypass -File "0-学习与工具\工具-opener批处理执行v2.ps1" -Plan "1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_心跳与称谓-2026-09-12.md" -FullAuto -Yes -Detach
```

它立即打出日志目录并退出 0；派出会话轮询该目录的 `summary.txt` / `exit.txt` / `launcher.json`，**不猜、不中途拿日志碎片打扰他**。

🔴 **硬前置（未满足不得起跑）**：§二 的 `B-0912_一次粘贴口径` 与 `B-0912_称谓发件侧缺口立行` **必须先由 sweep 落进 master**。理由见下方矩阵——A1 触碰 `opener骨架.md`，而该文件的「一次粘贴」节改动此刻仍脏在主 checkout、未 commit；泳道从 master 起 worktree 看不到它，先跑必冲突。

## 一、硬边界继承

- 🔴 **不 ff master**：两条泳道收工**只 push 本泳道分支**，主仓 ff 由派出会话串行做，且属 🟡 档、等他一字母。
- 🔴 **不碰 `.51`、不碰企微机器人、不碰定时任务**；不做任何对外发送。
- 🔴 **不改 `工具-opener批处理执行v2.ps1`**（`#565` 行内已写死该边界，看护正本亦写明只读引用）。
- 🔴 **不追改历史**：`#566` 的 244 处历史写错**只标注、不批量改写**。
- 🔴 **队列回写一律走 `工具-共享文档编辑锁.py`**，不裸改、不自行 commit；撞 `.git/index.lock` 不重试、按「登记待补」写进行内。
- 🔴 **收工哨兵必写**：顶格一行 `OPENER_DONE`；命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 行 | 触碰区 | 与另一条 |
|---|---|---|---|
| `565-heartbeat-tail` | `#565` | `1-转型规划/0-全景路线图/opener骨架.md`；`0-学习与工具/工具-opener生成.py` ＋其单测；`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` | **零重叠** |
| `566-gender-sendside` | `#566` | `0-学习与工具/工具-称谓核对.py`（新建）＋单测；`0-学习与工具/工具-共享文档编辑锁.py`；`0-学习与工具/skills源码/zhuopin-followup-letter/`；`6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md` | **零重叠** |

**结论：两条可并行**（`-MaxParallel` 默认 3、错峰 90 s 足够）。唯一共享资源是**两份队列文件**，由编辑锁串行化，不需要额外编排。

⚠️ **A1 的额外注意**：`opener骨架.md` 在 2026-09-12 当天被派出会话连改三次（「一次粘贴」节 ＋ 去向三要素 ＋ 无头口径）。A1 开工第一步**先确认 master 上该文件已含「## 一次粘贴」节**；不含即说明硬前置未满足，**立刻 `OPENER_PARTIAL` 停住报出**，不要在旧基线上改。

## 三、泳道 opener

### A1 · 子泳道心跳调用做成生成器强制注入（#565）

粘贴端：CC ｜ 泳道：565-heartbeat-tail

```
[OP-0912-B]【CC】565心跳尾句
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912b-lane-heartbeat-565`） ｜ worktree：☑（op0912b-lane-heartbeat-565，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0912_心跳与称谓）
读 ① `队列 §一 #565（python 0-学习与工具/工具-队列查询.py --row 565 --field all，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · 称谓核对补到发件侧（#566）

粘贴端：CC ｜ 泳道：566-gender-sendside

```
[OP-0912-C]【CC】566称谓发件守
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912c-gender-lint-sendside-566`） ｜ worktree：☑（op0912c-gender-lint-sendside-566，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0912_心跳与称谓）
读 ① `队列 §一 #566（python 0-学习与工具/工具-队列查询.py --row 566 --field all，做什么／不做什么／收工全在该行）` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或两条泳道均判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-D]【CC】看护心跳与称谓
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0910-H（批 B-0912_心跳与称谓）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912D-看护心跳与称谓。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_心跳与称谓-2026-09-12.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. **每泳道一行**：行号／分支／commit／`OPENER_DONE` 还是 `PARTIAL`／单测读数（`N passed` 现跑，不引用）。
2. **零回归手段随句写明**：本分支跑一遍 ＋ 在纯 master 起点复跑同一条命令比对，失败项逐条确认非本次引入。
3. **🟡 停点列**：每条写清 (a)/(b) 与各自代价、标〔荐〕与默认项；**同类合并**，末行给答复模板。
4. **待 ff 清单**：两条分支各自的 commit 与 `merge-base --is-ancestor` 现取结果；**ff 不在本批内做**。
5. **转出项**：需 LAN／需 `.51`／需对外发送的一律转出，不在本批做。
6. 🔴 **等待时长只认 `reports/lane-watch-state.json`**（跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0912_心跳与称谓` 现取），**不得自算**。
