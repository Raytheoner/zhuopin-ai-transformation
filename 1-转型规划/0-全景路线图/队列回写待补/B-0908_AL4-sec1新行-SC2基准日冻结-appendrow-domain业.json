{
 "任务": "🔴 **SC2 页面基准日被冻结在服务进程启动日 —— 生产页面已陈旧 12 天，且每一项健康判据都恒真**（2026-09-08 `OP-0908-J`／批 `B-0908_AL4` 跑 `#502` 冒烟时坐实）。**现象**：09-08 14:20 触发的一次真实全量重算返回 `period=2026-W34`（08-24~08-30），写出的快照 `base_date=2026-08-27`、`fetched_at=2026-09-08T14:20:15+08:00`；采购口径本周应为 `2026-W36`。**根因**：`run_sc2.py::cmd_serve` 调 `create_app(base_date=_base_date(args.base))`，而 `_base_date(None)` ＝ `date.today()` —— **在进程启动那一刻求值一次并冻进 app 对象**；`sc2/webapp.py` 两处 `base = base_date or date.today()` 因 `base_date` 恒非 None 而**永远走不到 `date.today()` 这条活路**。`.51` 上 `Sc2WebServer` 是 AtStartup 常驻、PID 9168 自 2026-08-27 10:48:28 起未重启（冒烟段 0／段 7 同 PID 实测）。**影响面只在页面与 `/api/refresh` 这条路**：周五 `autopush` 每次新起进程、基准日现取，故 09-04 那期正确出了 `2026-W35` —— **自动推群的那份是对的、只有人打开页面看的那份是旧的**，这就是它潜伏 12 天无人察觉的原因。🔑 **它不产生任何信号**：页面 200、重算 200、快照 0 秒、进程不崩、采购口径与 ISO 两套周号仍各自自洽、O-7「本周窗口尚未走完」声明还照常挂着——**每一项判据都恒真，因为它们问的都不是「这是哪一周」**。与 `#361` 记的「两套周号各自自洽、都不报错、只是指的不是同一周」是**同一族的第二例**，这次错的不是编号规则而是基准日的生命周期。**做什么**：⑴ 定修法（候选：每请求求值＋`--base` 仅作显式覆盖／app 内存基准日按日滚动／服务侧定时自刷）并判是否命中 openspec 门槛（改变既有模块对外语义？）；⑵ 加单测钉死「同一 app 对象跨日请求须给出不同期次」，防回归；⑶ 顺带查同族：`5-平台底座` 与其它长开服务（FI2／SC8／QD-B）有无同一形态的「启动时求值一次的今天」；⑷ 修好后走发布四关部署 `.51`（**须新起收口批、不并进本批**）。",
 "领取方": "待领 —— **业务场景域**；🔴 **不并入 `#499`**（那行是 SC2 新增六项指标，主题不同）：并入审核（协议〇.10）已用 `工具-队列查询.py --digest --grep` 扫 `基准日`（0 命中）／`冻结`／`serve`／`周序`／`长开`／`重启`／`SC2` 七个词，命中 `#361`（已 `[S:done]`，其边界句同批已更新）、`#499`（新增指标）、`#502`（本次冒烟，已销号）、`#394`（outbox 中继），**确无可并入，故取新号**。",
 "输入（指针）": "`1-转型规划/0-全景路线图/队列行日志/#502.md` §六（四条独立证据与根因全文）／§一 `#502` 状态列／`4-数字员工/采购部/SC2-采购周报自动生成/run_sc2.py`（`_base_date`／`cmd_serve`）＋`sc2/webapp.py` 第 46／79／89 行＋`sc2/windows.py`（`procurement_week` 与 D22/D33 口径）／场景 `CLAUDE.md`「部署状态」段与 D21／D33／`autopush` 缺省口径／`.claude/rules/场景建造与合规.md` §二 发布四关",
 "期望产出": "① 修法定案（含是否走 openspec 的判定与理由）；② 回归单测：同一 app 对象在跨日两个时刻须给出不同期次，且 `--base` 显式传入时仍固定；③ 同族排查结论（其它长开服务有无「启动时求值一次的今天」），有则各自立行；④ 部署收口另起 LAN 收口批，**不得并进 `B-0908_AL4`**（该批硬边界＝只冒烟只读、不部署）",
 "状态": "[S:open][D:业] 🆕 **2026-09-08 立行**（CC `OP-0908-J`，批 `B-0908_AL4` 项 1 附带发现，非派单范围）。🔴 **本批只登记不修**——看护件「不做什么」硬约束：不部署、不重启、不改 `C:\\sc2\\` 任何服务文件、不在本会话改代码。⚠️ **本行直接影响 `采购部#22` 能否发**：发送硬前置② 的字面判据（`.51` 部署且冒烟通过）已由 `#502` 取到证据判过，但「请姚祖怡打开页面试用」这个前置②真正要保的目的未达成——她此刻打开页面看到的是 8 月下旬那一期，顶上还挂着「本周窗口尚未走完（仅含 4/7 天）」。**是否等本行修完再发已登 §四（编号待补），待总线派发**，本行不替总线定。📌 **未取到的一项证据（如实标注）**：拟登录生产页面把渲染出的周标从 HTML 抓出来作第五条证据，该动作被 auto mode classifier 拦下（需读 `.51` 的 `.env` 取门禁口令），按既有纪律**未换第三个入口重试**；已有四条证据中第 1 条来自活着的应用自己返回的 `period`、第 4 条来自它自己写的盘，定性已足。",
 "触碰区": "`4-数字员工/采购部/SC2-采购周报自动生成/`（`run_sc2.py`／`sc2/webapp.py`／`tests/`）；🔴 `.51` 上 `C:\\sc2\\app` 与 `Sc2WebServer`（**修好后另起收口批才可碰，本行立行时不碰**）；场景 `CLAUDE.md`「部署状态」段",
 "登记": "2026-09-08 CC OP-0908-J（批 B-0908_AL4）"
}