---
title: "质量部#7 发送状态格 改写前后逐字对照（队列 §一 #501⑵）"
created: 2026-09-08
status: 生效
批次: B-0908_B（泳道 501-readme-rawcell-mode，分支 claude/op0908f-readme-rawcell）
用途: 队列 §一 #501 期望产出⑵「改写前后逐字对照留档」。原文原样、可 grep。
---

# 质量部#7 · 发送状态列 改写前后逐字对照

## 〇 为什么会有这份留档

`#501` 的成因是**读侧取证链断了**：跟进信 README 有读侧禁通读钩子
（`hooks-pretooluse-queue-read-guard.ps1`）挡住 `Read`/`Grep`/`cat`，唯一写入口
`工具-跟进信README登记.py set-status` 是**整格替换**，而唯一只读入口
`工具-跟进信README查询.py --digest` 把这一格截断（`--digest-width` 对状态列无效
——状态前缀之后的延续文本走的是独立常量 `STATUS_CONTINUATION_WIDTH`）。
⇒ **看不见全文就改 ＝ 用重建文本覆盖一段从未看过的叙述**，上一棒看护者据此当场停手。

本次先补上 `工具-跟进信README查询.py --row <编号>`（单行整格原文、逐字零截断、
每格附字符数／字节数／sha256），**再**据它取到的全文做改写。本文件即那次取证的留档。

## 一 取证手段（可复核）

```
python 0-学习与工具/工具-跟进信README查询.py --row 质量部#7 --field 发送状态
python 0-学习与工具/工具-跟进信README查询.py --row 质量部#7 --json
python 0-学习与工具/工具-跟进信README登记.py set-status --who CC-OP0908F-501 \
    --number 质量部#7 --status "<改写后全文>"      # 唯一写入口，整格替换
```

| 项 | 改写前 | 改写后 |
|---|---|---|
| 字符数 | 382 | 865 |
| UTF-8 字节数 | 919 | 1876 |
| sha256 | `ed5059b844d3c5bd0e03a730ccc528a77096bfdf76a37cab0adb63a9e667cffa` | `20a8429deff9037fedf45dc6d9c6792289ce2b42247cfec41dd0f90d3101aeb8` |

行长口径：1876 B 仍远低于 `工具-跟进信README行长外置.py::ROW_LENGTH_CAP_BYTES`（4 KB），
本次改写不制造新的外置候选。

## 二 改写前原文（逐字）

✅ **无需回复**（起草时即判定：正文三要素表明写「做什么＝不用做任何事」「什么时候交＝不用回」，**发出即闭环、不占串行闸**；符合防滥用约束——起草时判定、非事后追认）｜ ✅ 已推送 2026-08-18 06:53 UTC　🔴 **两态并列是有意的，勿删任一**：发送脚本自动回填的 `✅ 已推送 <UTC>` 会覆盖起草时的闭环形态判定，**这是 2026-08-18 闭环三态设计的一个真实漏洞——起草时判定了、发送时被机器抹掉了**；在代码侧修好之前（发送脚本回填时须保留闭环形态判定，属 §5 门槛③须走 openspec），本行以人工两态并列留痕。⚠️ **机器判据仍只认 `📥 已回件并回灌` 前缀**，故起草质量部#8 时 `release` 仍会被拦，**须走 `串行豁免:前信质量部#7 为 ✅ 无需回复 形态、发出即闭环` 逃生阀**。

> ⚠️ **本节引用件的一处刻意改动**：上一行末尾那个标记，原文写的是全角冒号，
> 本留档件里写作**半角冒号**。理由与本次改写同源——若在这里逐字复刻全角形态，
> 这份留档件自己就成了第二处「机器眼里的有效开闸口令」（`serial_waiver_reason`
> 只搜字面量、不看载体是 README 还是留档件）。除这一个冒号外，本节与改写前原文逐字相同。

## 三 改写后原文（逐字）

✅ **无需回复**（起草时即判定：正文三要素表明写「做什么＝不用做任何事」「什么时候交＝不用回」，**发出即闭环、不占串行闸**；符合防滥用约束——起草时判定、非事后追认）｜ ✅ 已推送 2026-08-18 06:53 UTC　🔴 **两态并列是有意的，勿删任一**：发送脚本自动回填的 `✅ 已推送 <UTC>` 会覆盖起草时的闭环形态判定，**这是 2026-08-18 闭环三态设计的一个真实漏洞——起草时判定了、发送时被机器抹掉了**；在代码侧修好之前（发送脚本回填时须保留闭环形态判定，属 §5 门槛③须走 openspec），本行以人工两态并列留痕。⚠️ **机器判据仍只认 `📥 已回件并回灌` 前缀**，故起草质量部#8 时 `release` 仍会被拦，**须走串行闸逃生阀 `followup_gate.SERIAL_WAIVER_MARKER`（闸侧唯一认的那个标记，取值正本＝`5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/followup_gate.py`）＋非空理由「前信质量部#7 为 ✅ 无需回复 形态、发出即闭环」**。　📌 **本格 2026-09-08 由队列 §一 #501⑵ 改写，仅动上一句的引用形态**：原文在该处**直引**了逃生阀标记的字面取值，而闸侧 `followup_gate.serial_waiver_reason` 是**逐格搜该字面量**、不区分「真豁免」与「叙述里的引用」⇒ 本行在机器眼里一直携带一个有效开闸口令（`#482` D5(a) 编号主键改造后，只有改编号才会把本行判成新增行、残余风险已压低但非零）。改写把**直引换成符号名引用**，理由文本逐字保留；闸判据、状态前缀、事实日尾标一律未动。改写前原文 919 B ／ sha256:ed5059b844d3c5bd，逐字对照留档＝`1-转型规划/0-全景路线图/队列回写待补/B-0908_B-501-质量部7状态格逐字对照.md`。

## 四 字符级差异（`difflib.SequenceMatcher` 全序列比对，非人工誊抄）

**被删除的旧字符合计只有两处**——这就是「原有信息零丢失」的机器证据：

| # | 旧串（被删/被替） | 新串 | 性质 |
|---|---|---|---|
| 1 | （空，位置 342） | `串行闸逃生阀` | 纯插入：把「逃生阀」提到标记前面，句子仍读得通 |
| 2 | `串行豁免`＋全角冒号（旧 344–349） | `followup_gate.SERIAL_WAIVER_MARKER`＋（闸侧唯一认的那个标记，取值正本＝…followup_gate.py）＋非空理由「 | **本次唯一的实质改动**：直引 → 符号名引用 |
| 3 | 一个反引号（旧 374–375） | `」**。　📌` | 引号收口形态随之调整 |
| 4–7 | （空） | 📌 改写留痕全段 | 纯插入 |

⇒ 旧文本里**唯一消失的语义单元**就是那 5 个字的字面标记；「逃生阀」「理由文本」
「机器判据仍只认 `📥 已回件并回灌` 前缀」「质量部#8 起草会被拦」等全部原样保留，
且新增了「为什么改」的留痕。

## 五 边界（本次刻意没做的事）

- **不动闸判据**：`followup_gate.SERIAL_WAIVER_MARKER` 的取值、
  `serial_waiver_reason`「标记后 MUST 跟非空理由」的收窄判据、
  `工具-共享文档编辑锁.py` 串行闸校验，一律未改。本次改的是**数据**，不是判据
  （若改判据即属 🟡 档，须停等 Shao Peishen）。
- **不动 README 其它行**：本行之外一格未碰；改写全程走 `set-status`，未裸手 Edit。
- **不绕读侧钩子**：`--row` 一次只出一行、且必须给确切编号，
  「一次 `Read` 把 178 KB 主表灌进上下文」这条立法目的不受影响。
- **残余风险已清零的部分与仍在的部分**：本行不再携带任何豁免标记
  （`串行豁免`／`转态豁免`／`作废豁免` 三个标记对本行 6 格全部为 False，机器扫过）。
  README 其它行是否还有同类**引用式**口令，本次未扫——不在 `#501` 触碰区，
  如实登记，不假装全覆盖。
