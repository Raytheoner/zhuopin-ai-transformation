{
 "_说明（非队列字段，回灌时删）": "泳道 500-sweep-carrier 出件，CC OP-0908-N。本文件因 worktree 隔离无法直接落到主 checkout 的 队列回写待补/，请看护者取走。状态文本为「追加到现有状态格末尾的一段」，不是整格替换。 🔴 回灌提醒：本段是追加段；若追加后该行状态格超 4 KB（K2 口径，2026-09-11 起 release 阻断），请按 K2 把历史段外置到 1-转型规划/0-全景路线图/队列行日志/#N.md 后再追加，勿截断本段。",
 "append": {
  "状态": " ━━━ ✅ **2026-09-08 判据改完、单测已补、全量绿**（CC `OP-0908-N`，泳道 `500-sweep-carrier`，批 `B-0908_四泳道机制根治`）：产出＝分支 `claude/op0908n-sweep-carrier` @ `05d4ec7`（已 push origin）。**⑴** `_carrier_tracked_dirty` 拆为 `_carrier_dirty_paths`（脏路径集合＋摘要；`status` 跑不通返回 `None`，与「干净」的 `set()` 分开）＋新增 `_carrier_pending_paths`（`head..base` 待入路径，`--no-renames` 使重命名两侧都进清单）；`_ff_carrier` 改判＝**脏∩待入≠∅ 仍停手并点名相交文件／零相交照常 ff 且脏转 `dirty_note` 告警／脏检查不可用或待入清单取不到一律保守停手**；`_sync_resident_carriers` 让 `dirty_note` 与「需人工重启」并列不互相顶掉（**不阻断 ≠ 不吭声**）。🔑 顺带堵掉一个会让本判据**静默失效**的坑：改判为路径逐字符比对后两侧必须都用 `-z`，非 `-z` 形态给含空格路径加引号（`core.quotepath=false` 不关这一层）会使交集恒为空 ⇒ 永远放行，已加用例锁死。**⑵** `ResidentCarrier` 组 20→**29 例**，三态全覆盖，另加含空格路径／重命名两侧／干净 vs 判据不可用可分／sync 层两例；**三条旧用例按新判据改为「相交」形态**（它们断言的正是被本行推翻的旧判据）。**⑶** 全量 `pytest test_工具-落库sweep.py` ⇒ **469 passed, 66 subtests, 1781.71s, exit 0**（该轮收集于最后两例新增前，那两例由 `-k ResidentCarrier` ⇒ 29 passed 覆盖）。**⑷** **未关检查、未加 `--force`**：`--ff-only` 那道防线一字未动，本改动只是不再抢在 git 前面替它拒绝一次它自己并不拒绝的合并；并加 `test_放宽判据不得靠关掉检查或加force实现` 做源码级机器守。📌 顺带删死代码 `_carrier_lag_counts`（`#338` 遗留，实测全仓零调用者），原处留一行注释说明去向。🟡 **停在合入 master 等一个字母，本泳道不自合**；合入后本行即可销号。⚠️ 本行只改判据不做清账——现网 165 个提交的清账仍在 ⏭️ 档、已交 `zhuopin-lan-closeout`（`B-0907_Q`）。",
  "领取方": "CC `OP-0908-N`（泳道 `500-sweep-carrier`）—— 已完成，🟡 待合入 master 后销号"
 }
}
