# 根 CLAUDE.md 瘦身 · P2 映射表（2026-09-03，P1 版 → 根路由表 ＋ `.claude/rules/` 五份）

> P2 是纯剪贴：P1 版每一条要么留在根（常驻），要么整条搬进一份规则文件。两处新增文字只有 ⑴ 根 §4 路由表 ⑵ 各规则文件的两行页眉；一条新判据「锁 release 走本机 PowerShell／管道末端退出码不算」（今日实证，标 ★ 供你确认）。
> 尺寸：P1 根 24,872 B → **P2 根 9,638 B / 65 行**（较原始 48,599 B **−80%**）；五份规则合计 21,033 B，按触碰路径加载。

## 一、加载矩阵（P0 实测）

| 文件 | `paths` | CC | Cowork |
|---|---|---|---|
| 根 `CLAUDE.md` | — | 开场 | 开场 |
| `rules/跟进信与专员.md` 3.9 KB | `6-人才与组织/**` `7-外部文档/**` | 触碰即注入 | 按 §4 路由表 Read |
| `rules/场景建造与合规.md` 3.9 KB | `4-数字员工/**` `5-平台底座/**` `openspec/**` `3-治理与合规/**` `2-试点项目/**` | 触碰即注入 | 按路由表 Read |
| `rules/队列与落库.md` 4.3 KB | 两份队列、`session接力*.md`、`0-学习与工具/工具-*.py`、`hooks/**` | 触碰即注入 | 按路由表 Read |
| `rules/文档与全景治理.md` 3.5 KB | `1-转型规划/**` `3-治理与合规/**` `z-已执行归档/**` | 触碰即注入 | 按路由表 Read |
| `rules/两桌同步与取证.md` 5.5 KB | `0-学习与工具/**` `4-数字员工/**` `5-平台底座/**` `openspec/**` `.claude/**` `reports/**` | 触碰即注入（CC 开工必碰） | 按路由表 Read |

**典型 session 开场纪律读入**：CC 建造 ≈ 根 9.6 ＋ 场景建造 3.9 ＋ 两桌同步 5.5 ＋ 子目录 CLAUDE.md ≈ **22 KB**（原 48.6＋子目录）；Cowork 规划 ≈ 根 9.6 ＋ 文档治理 3.5 ＋ 队列 4.3 ≈ **17 KB**；Cowork 跟进信 ≈ 根 9.6 ＋ 跟进信 3.9 ＋ 6-人才 CLAUDE.md 10.5 ≈ 24 KB。

## 二、逐条去向

| P1 条目 | P2 去向 |
|---|---|
| 顶部：intro／当前进度／📦 指针／验证声明 | 根（不变） |
| 顶部：memory 层全文 | 根留 2 行判别；全文 → 两桌同步与取证 §四 |
| §1 公司／项目／决策人／称呼／节奏 | 根 §1（阶段框架压成一句） |
| §1 人的属性三条 | 根留 1 条硬指针＋禁推断；三条全文 → 跟进信与专员 §一 |
| §2 全景目标五条 | 根留权威总纲＋U9C 一句；其余 → 文档与全景治理 §一 |
| §3 目录树 | 根 §2（压缩注释、加 `.claude/rules/` 与 `7-外部文档/`） |
| §4 平台底座指针 | 场景建造与合规 §一（根路由表保留 `5-平台底座/CLAUDE.md` 哨兵） |
| §5.1 会话接力／开场词与 prompt | 根 §3（压缩） |
| §5.1 CC 复命零粘贴 | 队列与落库 |
| §5.1 执行环境标注 | 根 §5 一行 ＋ 两桌同步与取证 §一 |
| §5.2 需你定夺／默认项两前提／决策路由／输出三条／建议未答复重提 | **根 §5 常驻（全文）**；决策路由的总线配套两动作 → 队列与落库 |
| §5.2 次序并行矩阵 | 根 §5 输出三条末一句 ＋ kickoff skill 1.2（正本） |
| §5.3 队列纪律／机制优先／环境保障线／规则退休制 | 队列与落库（根 §3／§5 各留一句） |
| §5.4 Cowork/CC 分工／同步／ff-only／乱码哨兵／时间戳／静默回退／直读 | 两桌同步与取证（根 §5 留时间戳＋静默回退各一句） |
| §5.5 排期同步／重组循环／六规则／企微推送 | 文档与全景治理（企微推送 → 两桌同步与取证 §三） |
| §5.6 跟进信三条 | 跟进信与专员 §二 |
| §5.7 端口／openspec 门槛／固定流程第 8 步／完工归档／发布收口／评审／代理 | 场景建造与合规 §二（第 8 步＋发送前置 → 跟进信与专员 §三，两处互引） |
| §6 supplychain | 场景建造与合规 §一 |
| §7 合规红线五条 | 根 §5 一句话版 ＋ 场景建造与合规 §三（全文） |
| Last Updated | 根（不变） |
| ★ 新增：锁 `release` 走本机 PowerShell、管道末端退出码不算 | 队列与落库 第 2 条；两桌同步 §三 静默回退实例清单加一项 |

## 三、换版后校验
1. `工具-CLAUDE进度段lint.py --root-only`：已在假根上通过（4 处哨兵全命中）。
2. P1 版 46 个反引号路径在「根＋五份规则」中全部命中（脚本核，缺 0）。
3. 下一个 CC session 触碰 `4-数字员工/` 应看到 `场景建造与合规.md` 被注入——用它作 P2 的真实回归。
4. 待办（CC 一行）：`工具-落库sweep.py::_claude_md_targets` 把 `.claude/rules/*.md` 纳入尺寸巡检（单份 ≤8 KB、合计 ≤30 KB），根阈值降为 12 KB。
