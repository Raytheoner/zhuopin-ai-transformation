---
title: "业务三道看护件 · Q2 验收集＋第一批对照表／FI2 拆行口径／SC2 三节对数"
created: 2026-09-23
status: 🟢 无头批待起（`工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -Detach`，Shao Peishen 2026-09-23 答 `1a` 授权）
产出: Cowork 泳道看护 OP-0923-A
批次: B-0923_业务三道
源: Shao Peishen 2026-09-23「可否启动业务，开启泳道看护」→ 答 `1a`；同轮问「采购部#23 是否需要尽快按要求建造」
用途: 三条泳道互不重叠、并行；§三 即 `-Plan` 解析格式，默认后台无头起、不给人粘。
---

# 业务三道看护件（2026-09-23）

## 〇 · 本批要解决的一句话

**环境侧去重守已进 master（`ab9aaeed`，08:17），业务泳道恢复开工；首批三条各对一位专员的在途承诺。**

- `#592`：陈忱 09-14 答复已签、`质量部#15` 已承诺 10-16 前交第一批验收对照表 ⇒ 验收集落库＋规则库定稿＋10 份样本真跑。
- `#424`：唐燕萍 `财务部#19` J1 已签（同一 AP 单才拆行）⇒ 实现＋单测；221 张匹配键只读实测供下一封判例表。
- `#538`：姚祖怡 `采购部#23` 给出 311／90／2501 三个数与在途口径 ⇒ 复现、定位差异、修正取数。
- 🔴 原拟首批的 `#418` 剩余项＝重新摄取（改写生产明细）＋ `.51` 部署，属 ⏭️／业务数据，**不入本批**；`#423` 行内已自陈 ff（`b0265e4`），状态字段失真交审计，不入本批。

## 一 · 硬边界继承（看护者读，泳道 opener 不复述）

- D1 四档判据**现取**：`python 0-学习与工具/工具-泳道看护状态机.py criteria`，本件不抄判据表。
- 本批三条均为 🟢 档（worktree 内本地建造／只读取证）。命中 🟡/🔴 即 `pause` 并以 `OPENER_PARTIAL` 收尾；命中 ⏭️（`.51`）跑 `transfer-out`、跳过继续。
- LAN 探针现取 **on-LAN**（2026-09-23 08:10 三项齐备）；ERP／`.51` 只读取证可做，任何写动作与部署不做。
- 额度基线快照（`工具-额度与Opus对账.py --snapshot`，09:05）：五小时窗 6%／七日窗 2%；收工再取一次，差值即本批成本。
- 全部 `模型：sonnet`。

## 二 · 次序与并行矩阵

| 段 | 泳道 | 队列行 | 触碰区 | 硬序 |
|---|---|---|---|---|
| A1 | `q2` | §一 `#592` | `4-数字员工/质量部/Q2-8D报告AI判定/`＋`4-数字员工/质量部/QD-A-8D不良分析/`＋`6-人才与组织/部门AI专员跟进/口径点台账/质量域.jsonl` | 并行 |
| A2 | `fi2` | §一 `#424` | `4-数字员工/财务部/FI2-三单匹配自动对账/fi2/tax_export_ingest.py`＋`tests/` | 并行 |
| A3 | `sc2` | §一 `#538` | `4-数字员工/采购部/SC2-采购周报自动生成/`（`sc2/metrics.py`／`sc2/detail.py`／`tests/`） | 并行 |

三条触碰区两两不交；与环境侧 `Win-0921-A`（`0-学习与工具/`、hooks、skills）不交，只在队列锁上排队。

**「待回内网」清单（本批不做）**：`#418` 重新摄取＋`.51` 部署；`#538` 修正后 `.51` 部署；`#592` 无。

---

## 三 · 泳道 opener（`工具-opener批处理执行v2.ps1 -Plan` 解析段，人不碰）

🔴 **本节必须排在 `## 三bis` 之前**；`### A<N>` 标题带 `§一 #<行号>`。

### A1 · §一 #592 Q2 验收集落库与第一批对照表

粘贴端：CC ｜ 泳道：q2 ｜ 队列行：§一 #592 ｜ OP：OP-0923-F

```
[OP-0923-F]【CC】Q2验收集落库
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0923f-q2-accept-set`） ｜ worktree：☑（op0923f-q2-accept-set，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0923-A（批 B-0923_业务三道） ｜ 模型：sonnet
读 ① `队列 §一 #592` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 做 #592 ⑴ 验收集标签与退回理由落库、⑶ 规则库 J2–J5 定稿；真实 8D 原件在主仓 C:/Dev/zhuopin-ai/7-外部文档/质量部/8D验收集-2026-09-10/（gitignore，worktree 内看不见，按绝对路径只读），原文不入 git
2. 在上面基础上，用 Q2 引擎对 10 份样本逐份跑评审，产出《第一批验收对照表》草稿（AI 结论／当时判定／一致否／不一致原因），落 gitignore 目录，供 10-16 前交陈忱

不做什么：
- 不部署 .51、不发信、不联络陈忱；红线②③ 本批不验收只转人工；不代填任何判据默认值
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923f-q2-accept-set --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923f-q2-accept-set --done --batch B-0923_业务三道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · §一 #424 FI2 同 AP 单拆行口径与匹配键实测

粘贴端：CC ｜ 泳道：fi2 ｜ 队列行：§一 #424 ｜ OP：OP-0923-G

```
[OP-0923-G]【CC】FI2拆行口径
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0923g-fi2-424-split`） ｜ worktree：☑（op0923g-fi2-424-split，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0923-A（批 B-0923_业务三道） ｜ 模型：sonnet
读 ① `队列 §一 #424` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 按唐燕萍 财务部#19 J1 签认实现：跨两料号合计开票只在两个料号同属一张 AP 单时才拆行认领，不同 AP 单一律不认领；两个真实案例逐字入夹具并加单测
2. 对 421/221 张被行匹配闸丢掉的发票，只读实测各候选匹配键的命中率与歧义率，结果写进队列行供下一封信出判例表

不做什么：
- 不重新摄取、不改写生产明细文件与 .51 上任何数据；不自定匹配键口径（须她签认）；不发信
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923g-fi2-424-split --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923g-fi2-424-split --done --batch B-0923_业务三道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A3 · §一 #538 SC2 三节数字按姚祖怡口径复现与修正

粘贴端：CC ｜ 泳道：sc2 ｜ 队列行：§一 #538 ｜ OP：OP-0923-H

```
[OP-0923-H]【CC】SC2三节对数
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0923h-sc2-538-recon`） ｜ worktree：☑（op0923h-sc2-538-recon，v2.ps1 起 claude 前脚本建、收工自删） ｜ 工作区：无（纯库内，不触碰 .51／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0923-A（批 B-0923_业务三道） ｜ 模型：sonnet
读 ① `队列 §一 #538` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 按姚祖怡 采购部#23 回件的三个数与口径（下单 311／收货 90／在途 2501，在途＝截至 09-13 行.状态=审核且未交数量>0）只读复现，逐项定位与系统 187／58／175 的差异根因
2. 修正已确认的取数错误（如下单窗口只取到 09-07 单日），加单测；在途口径若与现行定义冲突，实现为她给的定义并在队列行写明依据原文

不做什么：
- 不建三项新指标、不改周报格式（采购部#24 待其回复）；不部署 .51；不发信；ERP 只读
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923h-sc2-538-recon --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0923h-sc2-538-recon --done --batch B-0923_业务三道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

---

## 三bis · 看护者开场词（仅在退回「给他一块粘」时使用；默认走 §三 无头）

```
[OP-0923-I]【CC】看护看护业务三道
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建；各子泳道声明 ☑ 后由 v2.ps1 脚本在起 claude 前建） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0923-A（批 B-0923_业务三道） ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0923I-看护看护业务三道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】业务三道-2026-09-23.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

---

## 四 · 全程红线与收工

1. 🔴 不碰 `.51`、不部署、不改写任何生产数据文件（⏭️／业务数据档）。
2. 🔴 不发信、不联络任何专员；`质量部#15`／`财务部#20` 已发出，`采购部#24` 待 Shao Peishen 审，本批不动。
3. 🔴 判据类不代填默认值；在途口径按姚祖怡原文实现，原文引用写进队列行。
4. 🔴 真实 8D 原件、ERP 明细一律不入 git。
5. 🔴 不 ff master（🟡），只 push 本泳道分支；看护者收工串行 ff 前须他一字母。

**收工汇总**：`python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0923_业务三道` 现取，不手工数；产出面一律 `git diff --name-only master...<分支>` 机器核，不信自陈。

## 五 · 已知会踩、但不是本批的错

- 🔴 **本批首起（09:14）A1 的 worktree 名被取成了括注全文**（`【设置】` 行 worktree 括注首项必须是 worktree 名，生成器把 `--worktree` 原样塞入、v2.ps1 按括注首段取名）；A2 起跑前看护者 09:15:28 停掉启动器、A1 照跑不动，A2/A3 改正括注后以 `-Only` 重起。

- 收工核验第三道闸会把回写主仓队列行判成 `main-leak`（`#611`），泄漏文件只有队列文件时按误判处理，读日志尾哨兵为准。
- `-Detach` 起跑必须用 `C:\Tools\pwsh7\pwsh.exe`，不得用 PS 5.1（`#567`）；不传 `-LogDir`。
