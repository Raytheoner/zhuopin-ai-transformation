---
title: "看护件 · 泳道看护批 B-0912_第二波两泳道"
created: 2026-09-12
status: 生效
批次: B-0912_第二波两泳道
派出线: Cowork 环境总线 OP-0912-E
执行形态: 无头（Start-Process pwsh 绕行，不用 -Detach）——三bis 仅作回退
---

# 看护件 · 泳道看护批 `B-0912_第二波两泳道`

## 〇、本批口径

🔴 **本批走无头，不给 Shao Peishen 粘贴**。**授权原文**：Shao Peishen 2026-09-12 回「`1a，2a，3a`」——`2a` ＝ 派棒出 `wecom-aibot-channel` 8.5 验收小结；`3a` ＝ 派棒核 `#240` 的 OAuth 验证能否 off-LAN 做。

🔴 **本批是第二波**：`B-0912_夜批四泳道`（17:46:46 起跑）占满并行上限 4，**须待其 `EXIT` 写出后再起本批**，不得叠跑。

🔴 **不得用 `-Detach`**（PS 5.1 子进程下必炸，`#567`）。启动走绕行：

```
Start-Process pwsh -ArgumentList '-NoProfile -ExecutionPolicy Bypass -File 0-学习与工具\工具-opener批处理执行v2.ps1 -Plan <本件> -FullAuto -Yes -LogDir <目录>'
```

## 一、硬边界继承

- 🔴 **不 ff master**；收工只 push 本泳道分支，主仓 ff 属 🟡 档、经登记处交 `工具-待合分支巡检.ps1` 机器做，仍须他一字母。
- 🔴 **不碰 `.51`、不部署、不重启任何服务、不改企微机器人配置**；**不做任何对外发送**（🔴 档永不代办）。
- 🔴 **凭据只读不外写**：不得把凭据值写进报告／日志／commit／队列（`B2` 泳道尤其）。
- 🔴 **不代签字、不代勾 owner 裁定项**（同 8.3 的先例：验收是否满足属 owner 裁定，只提供证据）。
- 🔴 **不立新机制行**：机制类可动 WIP 现取超上限，确需时 🛑 起首，**不写名不副实的 `WIP豁免：`**。
- 🔴 **人的属性查 `6-人才与组织/人员名录-称谓与性别-正本.md`，禁从名字推断**（陈承**男**）。
- 🔴 **队列回写走 `工具-共享文档编辑锁.py`**，撞 `.git/index.lock` 不重试。
- 🔴 **收工哨兵必写**：顶格 `OPENER_DONE`，命中决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。

## 二、次序与并行矩阵

| 泳道 | 承接 | 触碰区 | 与另一条 |
|---|---|---|---|
| `wecom-85-acceptance` | `wecom-aibot-channel` 8.5 验收小结（观察窗口早满 23 天无人写） | `openspec/changes/wecom-aibot-channel/tasks.md`（只追指针、不代勾）；新建 `1-转型规划/0-全景路线图/验收小结-wecom-aibot-channel-2026-09-12.md`；只读 audit 产物 | 零重叠 |
| `240-oauth-triage` | `#240` 档位核实（陈旧榜首第二条） | 队列 §一 `#240`／`#162` 状态列（走编辑锁）；凭据 config **只读不外写** | 零重叠 |

**结论：两条可并行**。唯一共享资源＝两份队列文件，由编辑锁串行化。

⚠️ **`B2` 的额外注意**：🔴 `Test-NetConnection` 本机恒为真（TUN fake-ip），**不可作可达性判据**；必须用真实 HTTP 请求的返回码/错误类型判，并把命令与原始返回写进报告。「机器无法代」是一个**须附手段的结论**，不得凭印象下。

## 三、泳道 opener

### A1 · `wecom-aibot-channel` 8.5 验收小结（观察窗口早满 23 天无人写）（OP-0912-M）

粘贴端：CC ｜ 泳道：wecom-85-acceptance

```
[OP-0912-M]【CC】wecom8.5验收
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912m-wecom-85-acceptance`） ｜ worktree：☑（wecom-85-acceptance，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_第二波两泳道）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】wecom8.5验收小结-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912m-wecom-85-acceptance --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912m-wecom-85-acceptance --done --batch B-0912_第二波两泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

### A2 · `#240` 档位核实（陈旧榜首第二条）（OP-0912-N）

粘贴端：CC ｜ 泳道：240-oauth-triage

```
[OP-0912-N]【CC】240档位核实
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0912n-oauth-triage-240`） ｜ worktree：☑（240-oauth-triage，新 worktree，收工自删） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_第二波两泳道）
读 ① ``1-转型规划/0-全景路线图/派单件-【CC】240档位核实-2026-09-12.md`` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912n-oauth-triage-240 --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0912n-oauth-triage-240 --done --batch B-0912_第二波两泳道 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis、看护opener（本批走无头，仅作回退）

⚠️ **本批默认不使用本节。** 只有当无头批 `EXIT` 非 0、或泳道判 `NO-SENTINEL` 需要人在环时，才把下块粘进一个新开的 CC 会话。

```
[OP-0912-O]【CC】看护看护第二波
【设置】执行环境：CC ｜ 分支：master ｜ worktree：☐（看护者不建，各子泳道自建） ｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务） ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0912-E（批 B-0912_第二波两泳道）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0912O-看护看护第二波。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0912_第二波两泳道-2026-09-12.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。

🔴 用 Task/Agent 起子任务时并行上限 4，超出排下一波，错峰 ≥90 秒；各子任务收工只 push 自己分支，不碰主仓、不 ff master（构建环境瘦身第三轮方案 P4）。
```

## 四、收工汇总（按此产出）

1. 每泳道一行：泳道／分支／commit／`OPENER_DONE` 还是 `PARTIAL`。
2. `B1`：核验项实测值与手段、实际观察跨度、签字区待裁问题清单。
3. `B2`：走 (甲)/(乙)/(丙) 哪一支、可达性判定的原始命令与返回、`#240` 状态列改动原文。
4. **两条泳道都要现取一次 `STALE(7)` 与机制类可动 WIP**，与开工时对比。
5. 待 ff 清单（**ff 不在本批内做**）。
6. 🔴 等待时长只认 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0912_第二波两泳道` 现取。
