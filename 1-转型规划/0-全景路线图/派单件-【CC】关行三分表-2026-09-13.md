---
title: "派单件 · 【CC】10 行「分支已在 master」候选逐条核期望产出，只出三分表不关行"
created: 2026-09-13
status: 待执行
队列行: 只读全部 §一 计入 WIP 的 32 行；产出落新取证件
派出线: Cowork 环境总线 OP-0912-E（批 B-0913_收口三泳道）
授权: Shao Peishen 2026-09-13 回「全按推荐」＝定夺 4a
---

# 派单件 · 关行候选三分表（只出表，不关行）

## 〇 · 为什么是这条

机制类可动 WIP 现取 **32/22**，**一整天没动过一格**。今天关掉的都是 §二 批次、§四 行、openspec 包——**这些都不减 §一 的行**。

派出前现取（2026-09-13 03:0x，master `2cafc6e`，手段＝复用 `工具-共享文档编辑锁.py::_mechanism_wip_counted_rows` 拿计入清单，再逐行抽状态格里的 `claude/*` 分支名跑 `git merge-base --is-ancestor <分支> master`）：

计入 WIP 的 **32 行**：`96 312 328 337 416 433 439 440 455 459 462 479 481 492 503 504 507 521 534 544 545 552 554 556 559 561 563 565 566 567 570 571`

其中 **10 行，它引用的分支已全部在 master 上**：**`#312` `#492` `#521` `#545` `#552` `#554` `#556` `#563` `#565` `#566`**
另 4 行**部分在**（多分支、只合了一部分）：`#459` `#462` `#507` `#534`

🔴 **「分支在 master」≠「这条行的期望产出全做完」**——一条行可能有好几样交付物，分支只是其中之一（`#556` 行内自己就写着「出血未止」）。**这 10 行是候选，不是结论。**

## 一 · 做什么

1. 🔴 **先重跑一遍上面的现取**（master 在动，名单会漂），把重跑结果与本件的数字并列，**不覆盖本件数字**。
2. 对 **14 行**（10 全合 ＋ 4 部分合）逐行出三分表，每行一条判词 ＋ 手段：
   - **可关**：期望产出列每一条都能指出落点（master 上的文件／commit／spec／实测输出），逐条点名。
   - **不可关**：还差什么，差的那件在哪（另一条行／对外发送／留步项）。
   - **判不准**：为什么判不准，缺哪一份证据。
3. 🔴 **判据只看「期望产出」列，不看状态格的自陈**——`#462` 已实证过「未勾≠没做」，反过来「行内写了已完成≠真完成」同样成立（`#553` 当日就撞过）。
4. 产出落 `1-转型规划/0-全景路线图/取证件-2026-09-13-关行候选三分表-OP-0913-B.md`（随分支，不进主仓工作区）。
5. 收工：push 分支 → 登 §二。

## 二 · 不做什么

- 🔴 **不关任何一行、不改任何一行的状态格**——关行属 🟡（关他人队列行），本棒只出建议。
- 🔴 **不立新行**。
- 🔴 **不 ff、不碰主仓工作区、不在主仓 commit**。
- 🔴 **不用 `Read`／`grep` 直读队列真身**——读侧门禁：一律经 `python 0-学习与工具/工具-队列查询.py --row N --field all`。
- ⚠️ **不要把「分支已在 master」直接写成「可关」**——那正是本件要你避免的那个跳跃。

## 三 · 收工报告要有的东西

1. 重跑后的 32 行清单与 14 行候选（与本件数字并列，差异点名）。
2. 三分表：可关 N 条／不可关 N 条／判不准 N 条，各自逐行一句判词。
3. **可关那几条，每条至少给一条可复核的落点**（文件路径＋commit 或实测命令）。
4. 一句结论：**若他把「可关」那几条全关掉，WIP 从 32 降到几**。
5. 取证件路径 ＋ §二 批次号。
## 硬边界（三条泳道同受，一条都不许省）

- 🔴 **不 ff master**（🟡 档）；收工只 `git push` 本泳道分支。
- 🔴 **不碰 `.51`**、不动常驻执行体运行时（⏭️ 档）；**不发任何跟进信、不触发任何对外发送**（🔴 档）。
- 🔴 **零回归判据＝差集**：分支失败集 ⊆ 纯 master 同命令失败集，**不是「必须全绿」**；纯 master 侧失败原文照抄附上。
- 🔴 **不立新机制行**（机制类可动 WIP 32/22 超限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 队列回写走 `0-学习与工具/工具-共享文档编辑锁.py`；§二「文件清单」格**只放反引号包着的路径**（`#513`）；撞 `.git/index.lock` 不重试。
- 🔴 新文件名形态同批补 `.gitignore` 并 `git check-ignore -v` 实测取证（`#328`）。
- 🔴 **勾 tasks 要诚实**：只勾真做完且有手段可证的。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`；命中 🟡/🔴 决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。