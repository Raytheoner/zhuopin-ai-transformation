---
title: "派单件 · 【CC】P2 僵尸未勾项检出 apply"
created: 2026-09-12
status: 待执行
openspec包: openspec/changes/tasks-zombie-item-detect
派出线: Cowork 环境总线 OP-0912-E（批 B-0912_三包apply）
授权: Shao Peishen 2026-09-12 回 `P2：4a`（design 审）＋ 回 `1a`（三条 apply 泳道全起）
---

# 派单件 · P2 `tasks-zombie-item-detect` apply

## 〇 · 这个包要治的病

`tasks.md` 里的未勾项，**做完了没人回来勾**。实例＝`fi2-recon-mvp` 的 `1.5`——唐燕萍 7 月 10 日就交了东西，那一项挂着未勾 45 天，而依赖它的 `7.1`／`7.2` 早在 7 月 23 日就被勾上了。**不是没有信号，是没有人在听。**
🔑 **本包只做检出、不做收口**——检出之后仍要人去核、去勾。这正是 2026-08-09 那次失败的形状（发现被写成散文、无承接），所以 J-A／J-C 走 `--enforce`：**CI 红是不能被写成散文的**。

## 一 · 已签认的口径（逐条照做，不改写）

签认段在 `openspec/changes/tasks-zombie-item-detect/design.md` 的 `## 决策点` 节首，🔴 **开工先读它全文**。摘要：

| 点 | 裁决 | 你要做成什么 |
|---|---|---|
| 1 | 默认 (a) | 挂 **CI job `zombie-task-lint`**；🔴 **不改 `0-学习与工具/工具-落库sweep.py` 一行** |
| 2 | 默认 (a) | **强弱两档**：J-A／J-C 判违规（CI 红）；**J-B 只出「请复核」清单、永不返回非零退出码**；J-D 纯回显 |
| 3 | 默认 (a) | 分判据分档：J-A／J-C 直接 `--enforce`；J-B／J-D 恒为提示（脚本对 B/D 根本不返回非零码，CI job **不需要** `continue-on-error`） |
| 4 | **(a)**（本包唯一无默认点） | J-C **强判据、直接 `--enforce`**：写「前置登记」项时必须同时在某个下游项或节标题写下 `前置 N.M` |
| 5 | 默认 (a) | **不扫 `openspec/changes/archive/`**；只扫活跃包 |

🔴 **决策点 4(a) 带一条实现义务，不做即「门禁上线第一天就是红的」**：J-C 的引用解析**必须支持多值写法** —— `前置 1.5/1.6`（斜杠）、`前置 0.1/0.4`（斜杠）、`前置 2.3、0.3`（顿号）。**三种分隔形态各写一条夹具进 tasks 2.1。** 不支持的话 `fi2-recon-mvp` 1.6 与 `status-triage-resident-round` 0.3 会被误判为「无下游引用」，上线即报 2 条假违规。

⚠️ **他没答 Open Question 2（J-B 清单要不要设陈化门槛）**，按起草方原意「无数据支持、不预设」处理＝**不设**。🔴 **不要自作主张加门槛**，也不要在代码里留一个默认开着的阈值。

## 二 · 做什么

1. 开工先读签认段全文 ＋ `python 0-学习与工具/工具-队列查询.py --row 561 --field all`。
2. 🔴 **先重跑实测基数再动手**：design §判据实测表的数字是 2026-08-22 那 **14 个包**的实测，现取活跃包已是 **75 个**。**对 75 包重跑一遍**，tasks 2.5 的「逐格一致」按重跑值判，不按表里的旧值。
3. 新建 `0-学习与工具/工具-僵尸未勾项lint.py`（现取不存在）＋其单测；四条判据 J-A／J-B／J-C／J-D 按 design 定义实现。
4. `.github/workflows/ci.yml` 加一个 `zombie-task-lint` job；🔴 job 注释里写明**为何与 `claude-progress-lint` 取法不同**（tasks 2.6 要求）。
5. 反例单测必须有：`archive/**` 不进扫描面（tasks 2.3）；J-B 永不返回非零退出码（tasks 3.3 同族）；三种 `前置` 分隔形态各一条。
6. 🔴 **R2 自检要做**：脚本须打印「本次扫描共发现 N 个前置声明」——**N 掉到 0 本身就是一个该被看见的信号**（判据恒真那一族）。
7. 零回归：跑本包新单测 ＋ 既有 CI 相关单测，与纯 master 同命令对照出差集。
8. 收工：push 分支 → 登 §二 → §一 `#561` 追段。

## 三 · 不做什么（本包专属，另见文末硬边界）

- 🔴 **不碰 `0-学习与工具/工具-落库sweep.py`**（决策点 1 明确否掉 (b)：sweep 的告警出口是企微业务群，`#73` 实测采购内部工作群已累计收到 58 条机制告警、其中一条正文是 Python traceback）。
- 🔴 **不让 J-B 判违规**——它精度 4/5，唯一那条误报正是 `fi2` 的 `10.11b`，而那一项**是真未决**（design D14 的 Open Question，正在等唐燕萍团队批改）。判违规后最省事的过关方式是把它勾掉，**误勾比漏勾更危险**：漏勾只会催错人一次，误勾会让一条真开放口径从此消失。
- 🔴 **J-B 的输出措辞 MUST 是「请复核」，MUST NOT 是「疑似已完成」**——措辞本身就是防误勾的一道设计。
- 🔴 **不扫 `archive/`**，不追改任何归档包的 `[ ]`。
- 🔴 **不给 J-B 加陈化门槛**（他未答＝不预设）。
- ⚠️ **`#91` 的退休声明只落本包 proposal ＋ CHANGELOG**——`#91` 已清扫进 `跨桌任务队列-归档-202609.md`，tasks 2.8 的「正文降级为一行指针」**不得改归档行**（历史记录不追改）。

## 四 · 收工报告要有的东西

1. 分支 SHA、两个计数。
2. **75 包重跑的实测数字**（四条判据各命中几条），与 design 表里 14 包的旧值并列，不覆盖旧值。
3. tasks 勾选数（做了几项／共 24，现取已勾 11），未勾项逐条写为什么。
4. 三种 `前置` 分隔形态夹具的**原文测试输出**。
5. 差集回归结论一句。
6. §二 批次号与 §一 `#561` 追段原文。
## 硬边界（三条泳道同受，一条都不许省）

- 🔴 **不 ff master**（🟡 档，泳道无权）；收工只 `git push` 本泳道分支。
- 🔴 **不碰 `.51`**、不跑任何部署脚本、不动常驻执行体运行时（⏭️ 档）。
- 🔴 **不发任何跟进信、不触发任何对外发送**（🔴 档）。
- 🔴 **零回归判据＝差集**：分支失败集 ⊆ 纯 master 同命令失败集；**不是「必须全绿」**。纯 master 侧的失败原文照抄附上。做不到这一步就别声称零回归。
- 🔴 **新文件名形态必须同批补 `.gitignore` 并 `git check-ignore -v` 实测取证**（`#328` 教训）。
- 🔴 **不立新机制行**（机制类可动 WIP 32/22 超限）；确需时 🛑 起首，不写名不副实的 `WIP豁免：`。
- 🔴 队列回写走 `0-学习与工具/工具-共享文档编辑锁.py`；§二「文件清单」格**只放反引号包着的路径**，格里不写带斜杠的散文（`#513`）；撞 `.git/index.lock` 不重试。
- 🔴 **不改签认段一个字**——签认段是他的答复留痕，你只对照执行，不改写、不"优化"。
- 🔴 **勾 tasks 要诚实**：只勾你这一棒真做完并有手段可证的；做不到的留 `[ ]` 并在行内写清为什么。
- 🔴 收工哨兵必写：顶格 `OPENER_DONE`；命中 🟡/🔴 决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。