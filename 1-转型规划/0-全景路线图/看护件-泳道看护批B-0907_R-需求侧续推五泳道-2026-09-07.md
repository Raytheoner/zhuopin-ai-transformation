---
title: "看护件 · 泳道看护批 B-0907_R（on-LAN 第一批：FI10 采集层建造＋OEMRouter 审计注入＋常驻 worktree 落后检测＋audit 留存 openspec 起草＋opener 守卫误判修）"
created: 2026-09-07
批次: B-0907_R
派出线: Cowork 环境总线 OP-0906-W（批 B-0907_R）
LAN: on（`工具-泳道看护状态机.py lan-status` 13:5x：三项齐备，effective=on；`.51` 项仍按 ⏭️ 转出）
status: 待你在 CC 里起
---

# 看护件 · 泳道看护批 `B-0907_R`

## 〇、本批口径

**触发**：Shao Peishen 2026-09-07 说「开启泳道看护」。前置四查：LAN 探针 on（frontmatter）；`git fetch` 后 origin↔master 0/0；编辑锁仅 sweep 短占（轮内）；上一批 `B-0907_E` 收工汇总已出并落库（`559912f`）。

**扫池**（两份真身 `--digest`，候选按「做错了能不能靠 git 回滚救回来」分档，不按域）：本批 5 条全 🟢 起步，各自的 🟡 停点已在行内或本件写明；**不激活任何 🛑 行**（机制类 WIP 27/22 已超，值周 §四 `#142` 追认，本周新立须先关一条）。落选点名：`#491`（硬序等 `#466` 合入）／`#494`／`#495`／`#390`／`#391`（FI2，CC `OP-0907-N` 在办同目录）／`#465`／`#312`（先要他一字母）／`#455`（apply 已停在合并决策点，属「待你一字母」不属泳道）／`#436`⑶(iii) 真实采样（⏭️，`B-0907_Q` 收口批）／`#439` §2 回溯拆点（Cowork 活，本线自做）／`#447`⑵、`#440`（🛑 无余量）。

**上限**：默认 6（灰度已摘），本批 5。**分档判据现取** `python 0-学习与工具/工具-泳道看护状态机.py criteria`。

## 一、🔴 硬边界

1. ⏭️ 档：`.51` 部署及任何触碰生产服务的动作**永不执行**，`transfer-out` 记录后跳过，交 `zhuopin-lan-closeout`（`B-0907_Q`）。A3 只建「检测＋告警」，ff＋重启永远是收口批的事。
2. 🟡 档即停：ff 合入 master／改口径判据阈值／design 审／关他人行／改 `3-治理与合规/OEM数据隔离规范.md` 正文——`pause --action-key …`（推送已默认走运维群 `#492`，`--no-notify` 非必需）后 `OPENER_PARTIAL`。
3. 🔴 档永不代办：对外发送／L2 门禁／合规红线变更／ASIL C-D。A2 涉 OEM 隔离红线，**只写代码与断言、留完整审计轨迹，不放宽任何拒绝路径**。
4. 不碰他线在办：`C:\fi2\`／FI2 场景目录（N）、队列清扫（值周 `OP-0907-P`）、`B-0907_E` 六包目录、`.51`。
5. 看护会话不修工具（发现缺陷只登记＋派单）；看护件落档失败即全停；不改队列行历史段只追加。

## 二、波次与触碰区矩阵

| 波 | 泳道 | 承接 | 触碰区 |
|---|---|---|---|
| 1 | `474-fi10-ingest` | A1 | `4-数字员工/财务部/FI10-存货跌价智能分析/`（§3 采集层，mock）＋ `openspec/changes/fi10-inventory-writedown-mvp/tasks.md` |
| 1 | `466-oemrouter-audit` | A2 | `5-平台底座/zhuopin_platform/zhuopin_platform/data_isolation_layer/router.py`＋其单测＋新 openspec 包；🟡 停于规范 §5 映射表改判与 ff |
| 1 | `436-worktree-drift-alert` | A3 | `0-学习与工具/工具-落库sweep.py` 告警段＋`test_工具-落库sweep.py`（只加「常驻 worktree 落后 master 幅度」检测与告警） |
| 1 | `358-audit-retention-draft` | A5 | 新 `openspec/changes/audit-retention-policy/`（propose＋design 起草，止步 design 审；不改 `audit/` 代码） |
| 2 | `493-opener-guard-fix` | A4 | `0-学习与工具/工具-共享文档编辑锁.py` release 期 opener 守卫＋`工具-opener块lint.py`＋二者单测（锁工具族恒最后一波，等波一全部到达终态） |

## 三、泳道 opener

### A1 · 474-fi10-ingest——FI10 §3 采集层建造（mock 先行，design 已审）

粘贴端：CC ｜ 泳道：474-fi10-ingest

```
[OP-0907-S]【CC】FI10采集层建造
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907s-fi10-ingest-474`）｜ worktree：☑（`fi10-ingest-474`，新 worktree，收工自删）｜ 工作区：无（纯库内 mock，不连真实库、不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_R）
读 ① 队列 §一 `#474`（`python 0-学习与工具/工具-队列查询.py --row 474 --field all --file 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`，做什么/不做什么/收工全在该行：只做 §3、§4/§5 不得；正本＝`openspec/changes/fi10-inventory-writedown-mvp/{design,tasks}.md`＋场景 `intent.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A2 · 466-oemrouter-audit——OEMRouter 审计注入语义收紧（D2＝(a)）

粘贴端：CC ｜ 泳道：466-oemrouter-audit

```
[OP-0907-T]【CC】OEM路由审计注入
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907t-oemrouter-audit-466`）｜ worktree：☑（`oemrouter-audit-466`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_R）
读 ① 队列 §一 `#466`（`python 0-学习与工具/工具-队列查询.py --row 466 --field all --file 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`，做什么/不做什么/收工全在该行；改 `OEM数据隔离规范.md` §5 映射表与 ff 均属 🟡 停点）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A3 · 436-worktree-drift-alert——常驻 worktree 落后 master 幅度检测＋告警（`#436` ⑵，不 ff 不重启）

粘贴端：CC ｜ 泳道：436-worktree-drift-alert

```
[OP-0907-U]【CC】worktree落后告警
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907u-worktree-drift-436`）｜ worktree：☑（`worktree-drift-436`，新 worktree，收工自删）｜ 工作区：无（纯库内；只检测 `ops/wecom-service-home` 落后幅度并走 sweep 既有告警出口，ff＋重启验活为 ⏭️ 转出）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_R）
读 ① 队列 §一 `#436`（`python 0-学习与工具/工具-队列查询.py --row 436 --field all`，本泳道＝该行 ⑵「发布式对齐」的**检测＋告警**半边，做什么/不做什么/收工全在该行与 `队列行日志/#436.md`）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A5 · 358-audit-retention-draft——平台 audit 留存/归档策略 openspec 起草（止步 design 审）

粘贴端：CC ｜ 泳道：358-audit-retention-draft

```
[OP-0907-X]【CC】audit留存策略起草
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907x-audit-retention-358`）｜ worktree：☑（`audit-retention-358`，新 worktree，收工自删）｜ 工作区：无（纯库内 openspec 起草，不改 `audit/` 代码、不触碰 `.51`；309 MB 与磁盘余量核实属 `.51` 只读项，写进 design「待收口批核」不自跑）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_R）
读 ① 队列 §一 `#358`（`python 0-学习与工具/工具-队列查询.py --row 358 --field all`，做什么/不做什么/收工全在该行：design 审通过前不得实现）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

### A4 · 493-opener-guard-fix——opener 守卫把骨架占位块判成违规（波二，锁工具族）

粘贴端：CC ｜ 泳道：493-opener-guard-fix

```
[OP-0907-V]【CC】opener守卫误判修
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0907v-opener-guard-493`）｜ worktree：☑（`opener-guard-493`，新 worktree，收工自删）｜ 工作区：无（纯库内，不触碰 `.51`／企微机器人／定时任务）｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_R）
读 ① 队列 §一 `#493`（`python 0-学习与工具/工具-队列查询.py --row 493 --field all`，做什么/不做什么/收工全在该行：排除格式正本自身或一次一用豁免标记，不得关守卫，须写「修好后何时发信号」）→ ② `CLAUDE.md` 恢复上下文，按该行执行。本件为 A 类，直接开工。
```

## 三bis、看护opener（单次粘贴，Task/Agent 工具起五子任务）

粘贴端：CC ｜ 泳道：（看护者本身，不是子任务）

▶ 首次派出：[OP-0907-R]

**开场词（复制即用，▶ 粘贴端：CC 新会话，Shao Peishen 本人在场实时看）**：

```
[OP-0907-R]【CC】看护B-0907_R
【设置】执行环境：CC ｜ 分支：master（看护者本身不建分支，不改代码）｜ worktree：☐（看护者不建，各子泳道自建）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 环境总线 OP-0906-W（批 B-0907_R）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0907R-看护需求侧五泳道。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（五条泳道由你用 Task/Agent 派发，正文里不放这一行），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉；你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0907_R-需求侧续推五泳道-2026-09-07.md` 全文＋ CLAUDE.md 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为各条泳道各起一个子任务，
`isolation: "worktree"`，把对应【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。

扇出与 git（硬口径，逐条照做）：
1. **并行上限 4**，超出的排下一波；同一波内起子任务**错峰 ≥90 s**。波一＝A1／A2／A3／A5，波二＝A4（锁工具族，等波一全部到达终态后起）。
2. 子任务收工**只 push 自己那条分支**——不碰主仓工作区、不 ff master、不在主仓 commit。
3. **主仓 ff 由落库 sweep 收尾段做**；sweep 不可用时由你在**收工时串行**做，一次一条。**push 不是定夺项**（sweep 起跑段无条件补推，`#194`），不要问他要不要 push。
4. 🔴 **你自己不 commit**；撞 `.git/index.lock` 不重试、不自造豁免，按「登记待补」写进队列行。

看护：D1 判据现取 `python 0-学习与工具/工具-泳道看护状态机.py criteria`；子任务命中 🟡／🔴 即 `pause`＋`OPENER_PARTIAL`，命中 ⏭️ 即 `transfer-out` 后继续；波间跑 `check-heartbeat`（报超时先核 mtime，多半误报）。子任务说「已完成」不要只信自述——亲自核分支是否 push、单测是否跑过。

收工汇总（一次写全）：
5. 每泳道一行 ＋ 心跳全文 ＋ D6 停点列 ＋ 转出项 ＋「待回内网」清单。
6. 末尾固定一段「**待你一字母**」清单：**同类合并**（多条 merge 只问一次），每项写清 (a)/(b) 与各自代价、标〔荐〕与默认项；design 审类排在 merge 类之前；**清单最后一行给答复模板**（如 `1a，2a`）。
7. 🔴 **等待时长只认 `reports/lane-watch-state.json`**（跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_R` 现取），**会话不得自算**。
```

## 四、收工

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0907_R` 现取，不手工数；队列回写走锁；推送按 `#492` 默认运维群。

## 五、本批不做、已明确去向

| 事 | 去向 |
|---|---|
| `#491` 通用库只读闸 | 硬序等 A2（`#466`）合入 master 后，下一批 |
| `#494`／`#495`／`#390`／`#391`（FI2） | CC `OP-0907-N` 在办同目录，不并行 |
| `#455` apply 合入决策 | 「待你一字母」（本批收工汇总并入同类合并） |
| `B-0907_E` 六包 design 审 | 你亲自逐包审（§四 `#163`），审过后各包 apply 进下一批 |
| `#439` §2 回溯拆点（吃 18 封带 `决策点:` 的信） | Cowork 环境总线自做 |
| `.51` 类（`#436`⑶(iii) 真实采样、SC2 对齐核验） | `B-0907_Q` 收口批 |
| `#447`⑵／`#440` | 🛑，WIP 27/22 无余量；关行后余量优先需求侧 |
