# 卓品智能 AI 转型 — 全盘代码审计、架构评估与差距分析报告

> 审计日期：2026-06-13 ｜ 审计范围：本仓库全部规划文档 + 5-平台底座 + 4-数字员工（SC1/SC3/SC5/SC8/O2）+ 3-治理与合规
> 审计方式：只读代码审计（未修改任何源文件）+ 文档交叉核对 + 外部趋势调研
> 审计视角：车规级 AI 软件架构 / IATF 16949 / ISO 26262 合规

---

## 0. 执行摘要

**总体结论：项目顶层目标清晰、治理框架是"有代码证据的真治理"、收割式路线是教科书级的去风险动作——方向正确、可以实现。但当前有 2 个 P0 安全/合规缺陷必须在 SC8 对客上线前修复，且排期可信度取决于 4 个 AI 团队之外的外部方（IT/HR/质量部/PMO），缺乏升级机制。**

| 评估维度 | 评分 | 一句话结论 |
|---|---|---|
| 目标清晰度 | ★★★★★ | 三年愿景量化到部门 KPI，"U9C 已覆盖的不建 AI"是少见的克制原则 |
| 顶层设计 | ★★★★☆ | 底座+收割+rule-of-three 提升纪律均落地为代码事实；隔离层 RAG 执行点缺位是短板 |
| 可实现性 | ★★★☆☆ | 2027 H2 隐性缓冲设计聪明，但 2026-10~12 堆叠对 2 人团队偏乐观，关键路径 owner 全在外部 |
| 路线图合理性 | ★★★★☆ | 收割策略 + 数据就绪门 + Phase 2 不并入是三大正确决策；文档漂移（11 项不一致）是最大软肋 |
| 代码合规成色 | ★★★★☆ | L2 fail-closed 门禁、hash-chain 审计、fail-loud 均真实现且有测试；但存在 1 条休眠的自动外发旁路（P0） |

**必须立刻处理的 4 件事（P0）：**

1. **TLS 证书校验被全局关闭**（`erp_connector/connector.py:71-73`，`CERT_NONE`）——client_secret 经公网明文可被中间人截获。**轮换 secret 前先修这个，否则新 secret 轮换完仍在裸奔。**
2. **`submit_commitment` 存在"低风险自动对客外发"旁路**（`sc8/commitment.py:63-64`），绕过 `CUSTOMER_OUTBOUND_ENABLED` 与人工确认，直接违反已签字的 SOP §4.1/§4.2 红线。当前生产入口未走此路径（休眠炸弹），但被测试正向固化。
3. **审计哈希链存在完整防篡改绕过**（`audit/sinks.py:156-159`）——genesis 兼容逻辑对任意行生效，懂格式者可无痕重写整个审计日志，IATF"防篡改"承诺名存实亡。修复仅需限定豁免只对第 1 行生效。
4. **U9C client_secret 活凭据仍在归档仓 GitHub history 暴露**，轮换无截止日期。建议本周内给 IT 下硬截止。

---

## 1. 顶层评估：目标、设计、可实现性、路线图

### 1.1 目标是否清晰？——是

40 个场景每个都有痛点/数据依赖/自动化等级/合规注意四要素，颗粒度一致；"U9C 已覆盖的标准功能直接用、AI 只做增强"防住了最常见的企业 AI 失败模式（重复造 ERP 已有功能）。Phase 2 候选（APQP 数字网关、ASPICE 追溯链、HIL 日志诊断）"不并入 40、Phase-1 末统一评估"的纪律有效防住了范围蔓延。

### 1.2 顶层设计是否合理？——基本合理，两处结构性短板

**做对的事（有代码证据，非纸面治理）：**

- 平台底座"一份代码处处复用"+ rule-of-three 提升纪律：kit_engine 在 SC5 成为第 2 消费方时才提升到 `agents/`，O2 改 import 零回归——这是反过早抽象的正确实践。
- 五条红线全部有代码落点：hash-chain 审计、`CrossOEMAccessError` fail-closed 路由、`RealEndpointNotReadyError` fail-loud、M015≥50万 触发人工审核、对客生产路径恒入待审批队列。
- 收割式 MVP 把"从零搭 40 个"修正为"6-7 个已验证引擎收割+加固"，是整个规划最大的去风险动作。

**两处结构性短板：**

1. **隔离层是"建议型守卫"，无强制执行点**。`data_isolation_layer/router.py` 路由逻辑本身正确（未注册 OEM、None、别名、大小写变体一律拒绝），但全包没有任何 Chroma/RAG 代码，guard 靠场景"自觉调用"，绕过 = 不调 guard。它是 R1（2026-10）和质量域 Q2/Q4 的硬前提，必须在接 RAG 前把 guard 内嵌进唯一检索入口。
2. **审计是"可选注入"**。所有连接器和 Notifier 的 `audit` 参数默认 `None` 且静默跳过——平台层没有任何机制保证场景接了审计。"所有 AI 决策写 audit"的红线目前靠场景代码自律维持。

### 1.3 目标可否实现？——可以，但要承认三个现实

1. **40 场景"全部启动"≠"全部产生价值"**。权威排期把全部启动压在前 12 个月、2027 H2 留作深化——这 6 个月隐性缓冲是排期里最聪明的设计，建议显式承认它是缓冲，而不是等爆雷时被动消耗。
2. **关键路径全部在 AI 团队之外**：U9C MCP（IT）、AIOps 第 2 人（HR）、8D 知识库归集（质量部）、立项规则边界（PMO+财务）。规划缺少对应的升级机制——建议为每条外部依赖设"红线日 + 自动升级到 VP"的机制，写进实施计划第七节。
3. **2026-10~12 堆叠**：10 月 4 场景 + 11 月 4-5 个 + 12 月 5 个，叠加 SC2 稳定期与 SC8 对客收尾，2 人团队 6 个月净新建约 14 个场景。11 月已有"砍到 4-5 个"的显式缓冲动作，建议 10/12 月预设同样的砍单规则（如：第 2 人未到位 → Q2 自动顺延，不挤压其他场景）。

### 1.4 路线图是否合理？——方向对，文档同步是最大软肋

三轮快速决策（06-07/06-08/06-11）造成 11 项文档漂移（详见 §5），其中 SC8 yaml 仍声明依赖 SC6/SC7、Phase1 架构文档整体过时、Q6 排期同文件自相矛盾——**AIOps 第 2 人 onboarding 第一周读这些文档会得出错误结论**。"权威总纲不改"的纪律与"决策快速迭代"的现实已冲突，建议给全景规划做一次修订快照、给 Phase1 架构文档头部加废弃/更新声明。

---

## 2. 平台底座审计（5-平台底座/zhuopin_platform）

### 2.1 子系统成色总览

| 子系统 | 成色 | 说明 |
|---|---|---|
| audit/ JsonlSink 哈希链 | 真实现（1 个 P0 绕过） | prev_hash 链 + 创世块 + verify_chain 真实现，13 个专项测试 |
| audit/ ClickHouseSink | 占位 | `sinks.py:182-189` 全部 `NotImplementedError`，"同接口切换"仅接口层成立 |
| data_isolation_layer/ | 半实现 | 决策逻辑真、fail-closed 正确；无 Chroma 代码、无强制执行点、违规不留审计 |
| shared_tools/ 连接器 | 真实现（1 个 P0） | 真连真实端点，fail-loud 真实现但有旁路，TLS 校验被全局关闭 |
| shared_tools/ 通知器 | 真实现 | L2 fail-closed 门禁质量高，9 个加固测试 |
| agents/kit_engine | 真实现 | 75 行纯函数 + 18 个专项测试（防循环/损耗累乘/在途抵扣/黄金值） |
| agents/ 其余 | 占位 | 自述"占位骨架" |

### 2.2 审计子系统（audit/）发现

| 级别 | 位置 | 问题 | 修复 |
|---|---|---|---|
| **P0** | `audit/sinks.py:156-159` | verify_chain 的 genesis 兼容对**任意行**生效：把全文件 prev_hash 字段删掉重写，校验照样 `ok=True`。哈希链只防"无知篡改"。测试 `test_audit_hash_chain.py:195-205` 把该路径当正向用例固化 | ① genesis 豁免限定 `idx==1`；② 链尾哈希定期外部锚定（另一主机/ClickHouse/每日企微播报链尾指纹），否则持有写权限者可整链重算，IATF 3 年可信留存不成立 |
| P1 | `audit/sinks.py:43-54,101-111` | 锁与 prev_hash 缓存仅进程内有效。两个进程（定时任务+人工脚本）写同一 JSONL 必产生断链误报，且无法与真篡改区分 | 跨进程文件锁（portalocker），或锁内每次写前重读磁盘末行；9 月 ClickHouse 迁移设计时一并解决最经济 |
| P1 | `connector_audit.py:52-58`、`notifiers/dispatch.py:109-110` | audit 默认 None 静默跳过——通知外发可完全不留审计。正面：`AuditLogger.record()` 本身无吞错，写盘失败会阻断业务（正确的 fail-loud） | 生产构造路径（from_env）把 audit 设为必传；至少 None 时 `warnings.warn` |
| P2 | `audit/sinks.py:107-108` | 无 flush/fsync，崩溃丢尾部"已记录"决策；审计日志躺在 OneDrive 同步目录，同步冲突可产生副本损坏 | 每条写后 fsync（审计量级低，开销可接受）；生产审计日志迁出同步盘 |
| P2 | `audit/sinks.py:122-125` | read_all 静默跳过损坏行，query_by 无声漏数据 | verify_integrity 结果暴露 corrupt_lines 计数 |

### 2.3 隔离层（data_isolation_layer/）发现

- **P1**：`CrossOEMAccessError` 抛出路径（`router.py:47,63-65`）无 audit 挂接——**违规企图不留痕**，与 docstring"必须审计"自相矛盾。
- **P1**：guard 是自觉调用型；质量域 Q2 8D（含 OEM 客诉按客户分库）上马前，必须把 guard 内嵌进未来 `rag.retrieve()` 唯一入口，禁止场景直接拿 Chroma client。
- **P2**：`GENERAL_COLLECTIONS`（`router.py:56-58`）是唯一默认放行面——往通用库灌含 OEM 信息的 8D 案例即构成事实泄漏。需给通用库写入侧加"无 OEM 信息"校验规约。
- 正面确认：路由 fail-closed 正确（None→拒绝、英文别名拒绝、大小写变体拒绝、collection 精确匹配）。

### 2.4 连接器（shared_tools/）发现

| 级别 | 位置 | 问题 | 修复 |
|---|---|---|---|
| **P0** | `erp_connector/connector.py:71-73` | `_CTX.verify_mode = ssl.CERT_NONE` 全局关闭 TLS 校验，用于包括携带 client_secret 的 AuthLogin 在内的**所有** U9C 公网请求（erp.equalitytec.com:4443）。中间人可截获 secret 与全部 ERP 数据。对照：SRM 连接器用默认 context（校验开启）——证明不是环境不可行，是遗留 | 向 IT 索取服务器证书做 pin（`load_verify_locations`）；至少改成默认校验、显式 `U9C_TLS_INSECURE=1` 才关并告警。**与 secret 轮换捆绑执行** |
| P1 | `connector.py:197-205` | client_secret 走 URL query string，会进服务器/代理访问日志（用友接口形态，客户端难改） | 记入风险台账；确认链路无日志型网关 |
| P1 | `connector.py:410-412` | `get_bom_for_products` 单品失败 `except Exception: return` 静默丢弃——返回**残缺 BOM 且无错误信号**，下游齐套算出虚低毛需求 → 漏报缺料 → 直接威胁 SC8 对客承诺交期正确性 | 收集失败清单，部分失败返回 `(rows, failed_ids)` 或抛带明细异常 + audit 留痕。**纳入 SC8 对客 L2 签字前置项** |
| P1 | `connector.py:447-455` | `get_bom()` 的 CSV 回退**不走** `_fallback_or_failloud` 闸门：real + allow_mock_fallback 组合下真实 BOM 为空时会拿到 mock BOM，审计只标 `CSV` 而非 `CSV_mock`，分不清权威回退与 mock 混入 | 回退分支同样走闸门 |
| P1 | `srm_connector/connector.py:287-297` | `get_confirmed_dates` 单 PO 失败 `except Exception: pass`——"查询失败"与"供应商未答交"无法区分，在途三色清单漏报延期风险 | 返回失败 PO 清单 + audit error 痕迹 |
| P2 | `connector.py:242,244` | `_zp_post` 超时 300 秒（重试 3 次最坏阻塞 15 分钟）；code=500 但带 data 的响应被当成功 | 降到 60s 内、区分连接/读超时；收紧错误判定 |
| P2 | 缓存目录 | PO/看板缓存（含供应商价格）明文落 OneDrive 同步目录，gitignore 挡不住云同步 | 生产缓存目录可配置并移出同步盘 |
| P2 | `srm_connector/connector.py:134-138` | 直接构造时空凭据默认 `""` 静默通过 | 构造期校验非空 |

**正面确认**：本仓库工作区无任何硬编码生产凭据；`.env` 已 gitignore；`SecretsProvider` 抽象到位；`from_env()` 缺凭据 fail-loud；U9CConnector 退役属实（仅残留一个 `__pycache__` 编译垃圾可清）；SRM 限流（令牌桶 + 900301 指数退避）真实现有测试。

### 2.5 kit_engine 发现

- **P2**：`agents/kit_engine.py:67-71` 物料不在库存快照时在途量被忽略 → 缺口虚高（保守方向，但与"偏差<1%"目标冲突）。注意 `ZpConnector.get_inventory` 目前恒返回 0（无库存端点），**真实切换后该路径必踩**。

---

## 3. 场景合规核对（对照已签字的治理基线）

治理基线提炼自《SC8上线前置门禁-错误回滚SOP与黄金基准校验.md》（VP 已签字 2026-06-11）、《企业AI治理清单.md》、《ISO26262-AI安全使用规范（草案）》、CLAUDE.md §7。

### 3.1 合规对照表

| 要求 | 判定 | 证据 |
|---|---|---|
| 采购≥50万→人工审核 | ✅ 符合 | `SC5/business_rules.py:21,28-30`；M015 测试验证（64万→待人工审核+R1） |
| 新供应商→人工审核 | ✅ 符合 | `business_rules.py:32-34`（无认证供应商触发 R2） |
| 对客发送必经人工确认 | ⚠️ 部分符合 | 生产入口 `route_forecast` 恒入队✅；**`submit_commitment` 有自动放行旁路❌（见 P0-A）** |
| 晚于目标日强制人工 | ✅ 符合 | `gate.py:50-55`（delay>0 → critical+拦截） |
| CUSTOMER_OUTBOUND_ENABLED 结构性保证 | ⚠️ 部分符合 | 硬编码 False、env 不可翻转✅；但只在 `route_forecast` 被读取，`submit_commitment` 与 `pending_queue.approve` 完全不查 |
| 决策全写 audit | ⚠️ 部分符合 | SC8 预测/入队/外发/拦截/更正全链有✅；O2/SC5/SC3/Notifier audit 默认 None 可静默跳过 |
| 更正不删原记录 | ✅ 符合 | `commitment.py:71-94`（original_so_id 关联、append-only） |
| 黄金基准回归 | ⚠️ 部分符合 | SC8 mock 黄金零偏差每次必跑✅；**真实黄金回归整组 skip**（`real_frozen/expected.json` 夹具不存在）；SC5 黄金值用 `pytest.approx(rel=0.01)` 1% 容差，与"零偏差"口径不一致 |
| real 模式 fail-loud | ✅ 符合 | `_fallback_or_failloud` 实现正确（但 get_bom 旁路见 §2.4） |
| 偏差阈值 3 天告警+重算 | ❌ 未实现 | 仅有常量 `DEVIATION_ALERT_DAYS=3`，全仓无消费代码 |
| ISO 26262 红线 | ✅ 确认不涉及 | 5 场景全为供应链分析/通知类，无嵌入式/ASIL 代码 |
| OEM 隔离 | ✅ 符合既定边界 | 采购数据不强加 OEM 路由符合 §4 边界；队列跨客户隔离有测试 |
| 测试规模声明（114+20+41=175） | ✅ 基本吻合 | 实测底座 117 / O2 20 / SC5 41（差 3 为 PR#8/#9 后新增，建议更新 CLAUDE.md 口径） |

### 3.2 影子调用核查结论

逐一核对全部 `urlopen/requests/send_*` 调用点：**生产代码无绕过 Notifier 直发外部通知的影子调用**；ERP/SRM 连接器均为只读，无写 ERP/下单接口。但有三处**审计留痕缺口**：

1. SC8 真实数据拉取（`sc8/sources.py:42,56`）构造连接器时未注入 ConnectorAudit → 访问层无痕（决策层审计仍在）。
2. FO 订单 API（`sc8/loaders.py:121`）直接 `urlopen`，无任何访问审计。
3. Notifier/AuditLogger 可为 None：调用方漏传即产生"无审计的决策/外发"。

### 3.3 门禁缺口分级

**P0-A：`submit_commitment` 低风险自动外发旁路**。`sc8/commitment.py:63-64`：高置信+非首次+不晚于目标日 → `requires_confirmation=False` → 平台 Notifier 直接外发，**全程不查 CUSTOMER_OUTBOUND_ENABLED**，违反 SOP §4.1"高置信仍须人工确认"与 §4.2"任何对客发送必经人工确认"。且被 `test_commitment_gate.py:70 test_low_risk_auto_sends` 当作正确行为固化。缓解：生产入口 run.py 不走此路径，当前是休眠炸弹——但按"结构性保证"标准必须修：删除自动放行分支，或在 `Notifier.send` 层强制叠加总开关校验，并改写该测试。

**P1-B**：`pending_queue.approve`（`pending_queue.py:107`）人工放行后外发不查总开关——L2 满足但 SOP §4.2 的开关语义未闭环。
**P1-C**：审批授权分级未落地——SOP 规定重点客户/晚于目标日/首次承诺/>50万须升 VP 复核，但 `approve()` 接受任意非空字符串作为确认人，无角色校验。
**P1-D**：偏差监控/重算触发未实现（上线检查表第 4 项未达成）。
**P1-E**：SC8 真实黄金基准回归未生效（检查表第 1 项未达成，需回 LAN/VPN 跑 `build_golden_real.py` 生成冻结夹具）。

**结论与 SOP 门禁文档第 3 节判定一致：在 P0-A、P1-D、P1-E 修复并测试覆盖前，不应开启对客外发开关。**

---

## 4. 依赖与死锁分析

### 4.1 依赖断链/倒挂清单

| # | 问题 | 证据 |
|---|---|---|
| 1 | **SC8↔SC6/SC7 断链已在决策层解开、权威总纲未解**：全景 SC8 yaml（约 L281-282）仍写依赖 SC6/SC7，与同文件 L866 及收割策略 §5"不依赖"矛盾 | 全景规划 |
| 2 | **SC8 深化阶段（2027-02）设计决策未关闭**：届时 SC6/SC7 仅运行 1-2 个月，深化到底用不用其数据，规划未写明 | 全景规划 |
| 3 | **Q1→Q2 依赖倒置**：Q1 客诉分流（8D 喂入口，2027-01）比 Q2 8D（2026-10）晚 3 个月——Q2 头 3 个月人工喂数无过渡 SOP | 实施计划 §一 |
| 4 | **SC9 自相矛盾**：以"需历史数据积累"维持 2027-03，但届时 SC7 仅积累 2 个月数据 | 收割策略表③ |
| 5 | **U9C MCP 单点阻塞面**：7/1 申请 + 6-8 周审批 → 最早 8 月下旬就绪；阻塞 SC2/SC6/SC7 + 财务全部 6 场景。唯一缓解阀（SQL 只读直连/文件导出过渡）**未做可行性预演** | Phase1 架构 §4 |
| 6 | **外网 cutover 死锁**：`CommonEntity/Query` 外网 404 → 库存/PO/MO/价格全拉不到，U9C 全量 cutover 只能 LAN/VPN，与 Cowork off-LAN 工作模式冲突；FO 服务 LAN-only 且无健康监控，**对客上线后 FO 502 会静默断供** | U9C 待办追踪 #3/#6 |
| 7 | **FI1 双重断链**：U9C 财务 MCP + 委外商务谈判（合同无数据共享条款），后者是非技术依赖、9 月启动零缓冲 | Phase1 架构 §4 |
| 8 | **人力依赖闭环**：质量 8 场景 ← AIOps 第 2 人（7 月底到位→60 天才能独立交付→9 月底踩线 Q2 入轨）。**招聘滑 1 个月 = 旗舰①顺延**，且与 8D 归集（6-8 周）并行，任一延误即破 | 质量路线图 §3、Onboarding |

### 4.2 u9c_connector / 7-1 ERP 接入技术风险评估

连接器收敛本身干净（ZpConnector 唯一规范、U9CConnector 已退役、fail-loud 有 6 个测试覆盖）。真正的风险按序：① TLS 不校验（§2.4 P0）；② secret 在归档仓 history + 轮换无截止日；③ 外网仅 OAuth2+BOM/Query 可用，全量数据面依赖 LAN/VPN——**建议把"SQL 只读直连备选"的可行性预演提前到 7 月**，作为 IT 审批延误的保险；④ BOM 拉取静默吞错（§2.4）会让接入"看起来成功"实际残缺——这是 7/1 接入后最容易踩的暗坑。

---

## 5. 文档不一致清单（11+1 项）

| # | 不一致 | 位置 |
|---|---|---|
| 1 | SC8"原排期"三个版本（2026-11 / 2027-02 / 2027-02） | 全景 L270 vs L866 vs 收割策略 §4/§5 |
| 2 | SC8 yaml 仍声明依赖 SC6/SC7 | 全景 L281-282 |
| 3 | Q6 排期同文件自相矛盾（§六"维持 2027-03" vs §一表 2026-11） | 实施计划 §一 vs §六.1 |
| 4 | Phase1 架构文档整体过时（SC8/Q6 旧结论、质量域仍 Q1-Q6） | Phase1-基础设施与智能体架构设计.md §1/§2/§4/§5 |
| 5 | 12 月场景计数：15-18 vs 14-16 | 全景 L960 vs 实施计划 L124 |
| 6 | 2027-06"25-29 个场景全覆盖"疑为 38 场景旧版残留 | 全景 L1061 |
| 7 | O1 状态矛盾：全景"已合 master" vs 收割策略"未在本轮收割" | 全景 L756 vs 收割策略 §7 |
| 8 | SC9 原排期：收割策略写 2026-12，其余均 2027-03 | 收割策略表③ |
| 9 | SRM 状态过时："已接通生产中" vs 实际降级+凭据待迁 | 实施计划 §四 vs CLAUDE.md |
| 10 | 8D 归集窗口同文件矛盾（4 周 vs 6-8 周） | 跨场景前置任务总表 §一 vs §三 |
| 11 | AIOps 第 3 人口径：全景"10 月前 3 人" vs 已统一"Q4 到位" | 全景 L1190 vs 从采购部启动 L336 |
| 12 | 小项：待办追踪 "cutford" 笔误；全景 §9 目录树过期；无独立《OEM 隔离规范》文档（要求散落两处） | — |

---

## 6. 借助最新 AI 技术提升 ECU 产品竞争力（Phase 2 候选输入）

结合 2026 年行业趋势（80% OEM/供应商正迁移 zonal 架构；BYD 璇玑架构已落地；EV 新势力开始绕过传统 Tier 1 自研 AI 栈；OEM-供应商协作正从零部件采购转向软件生态绑定），对卓品的建议按"防守→进攻"排序：

**防守（不做会丢单）：**

1. **SDV/Zonal 架构能力卡位**。直供比亚迪/上汽/理想意味着客户都在做域集中/中央计算迁移——单功能 ECU 的料号会被整合消失。建议销售+研发联合做一次"客户 zonal 路线图 → 卓品料号存续性"盘点（可作为 S5 竞品监控的扩展场景），识别 3 年内被整合风险最高的产品线。
2. **AI 辅助 ASPICE/ISO 26262 证据链工厂**（已在 Phase 2 候选）。OEM 对 Tier 1 的 ASPICE CL2/CL3 审核是拿单门槛，追溯矩阵/评审记录/测试覆盖报告的人工成本极高，LLM 做证据组装+一致性检查（人工签字）可把审核准备周期压缩一半。这与现有红线兼容：AI 产草稿、资质人员签字、ASIL C/D 不碰。

**进攻（建立差异化）：**

3. **HIL/测试智能化是最佳切入点**。AI 驱动的场景生成与 MIL/SIL/HIL 日志诊断已是行业活跃方向，且天然在 ISO 26262 安全边界外（AI 生成测试激励、人审测试结论）——比 AI 写产品代码合规风险低一个量级，却直接缩短交付周期。建议把 Phase 2 候选里的"HIL 日志诊断"升为质量域之后的第三旗舰。
4. **AUTOSAR 配置副驾（R2）值得加注**。生成式 AI + 模型驱动开发结合是当前研究与工业落地热点；卓品的踩坑库是真护城河——同样的 LLM 谁都能买，10 年 AUTOSAR 集成踩坑数据只有你有。R2 的知识库归集（8 月启动）建议按"未来对外能力"的标准做脱敏与结构化，而不只是内部工具。
5. **交付确定性本身就是竞争力**。SC8 做成后，"AI 支撑的承诺交期 + 偏差 3 天内告警"可以写进对 OEM 的商务承诺——在缺芯记忆犹新的行业里，可验证的交付确定性是比降价更好的谈判筹码。SC6 芯片短缺预警若与 SC8 打通（2027-02 深化阶段，正好回答 §4.1-2 的未关闭决策），可形成"芯片风险→交期影响→主动通报客户"的闭环，这是多数同级 Tier 1 没有的能力。
6. **边缘 AI 产品化预研（谨慎）**。2026 年 75% 新车型平台预计集成 <10W 车规 AI 芯片做边缘推理；卓品可评估在现有 ECU 产品线上加 NPU 选配做预测性诊断类功能——但这是产品战略而非运营效能，建议只做一页纸可行性，放 Phase 2 评估，不挤占 Phase 1。

参考来源：[IoT Analytics – SDV adoption](https://iot-analytics.com/software-defined-vehicles-adoption-4-dimensions-leading-oems/)、[SemiEngineering – Automotive Outlook 2026](https://semiengineering.com/automotive-outlook-2026/)、[Promwad – Zonal ECU 2026](https://promwad.com/news/centralized-vs-zonal-ecu-architecture-software-defined-vehicles-2026)、[IDTechEx – SDV & AI in Cars 2026-2036](https://www.idtechex.com/en/research-report/software-defined-vehicles-connected-cars-and-ai-in-cars/1108)、[ACL Digital – Automotive AI Hardware 2026](https://www.acldigital.com/blogs/automotive-ai-hardware-power-performance-2026)、[arXiv – GenAI + Model-Based automotive development](https://arxiv.org/pdf/2505.02500)、[Parasoft – Embedded AI testing](https://www.parasoft.com/blog/embedded-ai-automotive-software-testing/)、[QA Systems – ISO 26262 testing](https://www.qa-systems.com/blog/iso-26262-testing-best-practices/)

---

## 7. 改进任务清单（Todo List）

### P0 — 本周启动，SC8 对客上线硬前置

| # | 任务 | 落点 | 建议执行地 |
|---|---|---|---|
| 1 | 修复 TLS 校验：默认开启证书校验，向 IT 索取证书做 pin；与 client_secret 轮换**捆绑**执行（先修信道再换钥匙） | `erp_connector/connector.py:71-73` | Claude Code（LAN） |
| 2 | 给 IT 下达 client_secret 轮换硬截止日（建议 ≤6/20），轮换后验证旧凭据失效 | U9C 待办追踪 #1 | Paul 催办 |
| 3 | 封死 `submit_commitment` 自动外发旁路：删除低风险自动放行分支或在 `Notifier.send` 强制叠加 `CUSTOMER_OUTBOUND_ENABLED` 校验；改写 `test_low_risk_auto_sends` 为"低风险也入队" | `sc8/commitment.py:63-64`、`notifiers/dispatch.py:94-106` | Claude Code |
| 4 | 修复 verify_chain genesis 绕过：豁免限定第 1 行；修正对应测试语义；设计链尾哈希外部锚定方案（可先用每日企微播报链尾指纹的零成本方案） | `audit/sinks.py:156-159` | Claude Code |

### P1 — 6 月内完成（多数是 SC8 对客 L2 签字前置项）

| # | 任务 | 落点 |
|---|---|---|
| 5 | BOM 拉取静默吞错修复：部分失败返回失败清单 + audit 留痕；`get_bom` 回退分支接入 fail-loud 闸门 | `connector.py:410-412,447-455` |
| 6 | SRM 承诺交期吞错修复：失败 PO 与"未答交"区分 | `srm_connector/connector.py:287-297` |
| 7 | 实现偏差监控：消费 `DEVIATION_ALERT_DAYS=3`，实际 vs 预测偏差告警+触发重算（上线检查表第 4 项） | SC8 新模块 |
| 8 | 回 LAN/VPN 跑 `build_golden_real.py` 生成冻结夹具，激活真实黄金回归（检查表第 1 项） | `SC8/tests/test_golden_real.py` |
| 9 | `pending_queue.approve` 叠加总开关校验；审批人分级（重点客户/>50万/首次承诺→VP 级确认人白名单） | `pending_queue.py:86,107` |
| 10 | 审计强制化：连接器/Notifier 生产构造路径 audit 必传或 fail-loud；SC8 sources.py 注入 ConnectorAudit；FO loader 补访问留痕 | `sources.py:42,56`、`loaders.py:121` |
| 11 | `CrossOEMAccessError` 抛出前写 AuditEvent | `router.py:47,63-65` |
| 12 | SQL 只读直连备选方案 7 月预演（U9C MCP 审批延误保险） | IT 协调 |
| 13 | 哈希链跨进程并发方案定稿（与 9 月 ClickHouse 迁移设计合并） | `audit/sinks.py` |
| 14 | SRM 凭据迁本仓库 `.env` 解 900401 | U9C 待办追踪 #2 |
| 15 | FO 服务健康监控（对客上线前必须，否则 502 静默断供） | U9C 待办追踪 #6 |

### P2 — 7-9 月排入

| # | 任务 |
|---|---|
| 16 | 文档对齐冲刺（半天）：修正 §5 全部 11 项不一致；Phase1 架构文档加更新声明；全景规划出修订快照 |
| 17 | 外部依赖升级机制：U9C MCP/第 2 人/8D 归集/立项边界四条链路各设"红线日+自动升级 VP"，写入实施计划第七节 |
| 18 | 10/12 月预设砍单规则（参照 11 月"砍到 4-5 个"）；第 2 人未到位 → Q2 顺延预案显式化 |
| 19 | 隔离层执行点：rag.retrieve() 唯一入口内嵌 guard（R1/Q2 接 RAG 前完成）；GENERAL_COLLECTIONS 写入侧"无 OEM 信息"规约 |
| 20 | 补独立《OEM 隔离规范》文档（要求目前散落 CLAUDE.md 与全景规划） |
| 21 | kit_engine 在途量盲区修复（真实库存切换前）；SC5 黄金值金额断言改精确相等 |
| 22 | 审计/缓存目录迁出 OneDrive 同步盘；fsync 耐久性补强；`_zp_post` 超时降至 60s |
| 23 | Q2 8D 头 3 个月人工喂客诉的过渡 SOP；SC8 深化阶段是否消费 SC6/SC7 数据的设计决策关闭 |
| 24 | ECU 竞争力一页纸：客户 zonal 路线图 vs 卓品料号存续性盘点（销售+研发，Phase 2 评估输入） |

---

*本报告由 AI 辅助审计生成，所有代码发现均附文件:行号证据，建议合并前人工复核关键 P0 项。规划治理桌（Cowork）只读审计，未修改任何源文件。*
