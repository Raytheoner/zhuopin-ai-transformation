---
title: "派单件 ·【CC】#489 rebase 到 master ＋ 三笔 ff（OP-0909-G）"
created: 2026-09-09
status: 待粘贴
派出线: Cowork 业务总线 OP-0908-V（泳道 489-closeout 波3 转出）
承接: 队列 §一 #489
---

# 派单件 ·【CC】#489 rebase ＋ ff（A 类）

## 〇、为什么有这份件

Shao Peishen 2026-09-09 **已答 a、ff 三笔已获授权**。看护者本体执行时实测 **ff 不可行**，阻塞是技术性的、不是等他再决策：

    git merge --ff-only origin/claude/op0908l-opener-lint-489-ef8652
    fatal: Not possible to fast-forward, aborting.

**真因（现取，非推断）**：分支自 `fb0cb43` 分叉后 **落后 master 16 笔**，且两侧在**同两个文件**上有重叠改动 ⇒ 必须先 rebase：

| 侧 | 提交 | 改了什么 |
|---|---|---|
| 分支 | `2613969` | `#489` 判据正本改声明式，**连带改 `工具-共享文档编辑锁.py` 的 release 调用侧**（该包 D3「release 侧只调用不重写」）＋ 其单测 |
| master | `c388c97` | `#505` `append-row --number N` 写行前同步推进编号高水位线，**改同一文件** ＋ 其单测 |

⇒ 重叠面 ＝ `0-学习与工具/工具-共享文档编辑锁.py`、`0-学习与工具/test_工具-共享文档编辑锁.py`（两侧共 76 增 12 删 vs master 侧 #505 改动）。

🔴 **看护者本体不做这一步的理由，写清免得下一棒以为是遗漏**：解这两处冲突要判编辑锁代码怎么合，属「修工具」——`.claude/rules/队列与落库.md`「看护／总线会话不修工具」明令只登记＋出派单；且**编辑锁正被所有并发会话使用，冲突解错会当场打断全项目的队列写入**。同形态先例＝`797b8ed`（为 `#482` 出的「rebase 到 master ＋ 两笔 ff」派单件）。

## 一、现状（开工前只读核一遍，不要凭本文件相信）

> 🔴 **一条对本件自身的订正，先读**：§六 opener 块内写的「现值 3919 B 余量仅 177 B」**已过时** —— 该 opener 由生成器产出、**本件不手改 opener 正文**（骨架「禁手抄」纪律），故订正写在此处：**波3 回写后实测 4048 B、余量仅 48 B**，波4 请以本行为准。🔑 **这正是本件 §四 那条判据的又一次应验**：写进文本的数字不会自己过期，**凡余量类结论，动手前必须现取**（`[Text.Encoding]::UTF8.GetByteCount()`），不得引用任何文件里的历史数值——包括本件。

- 分支 `claude/op0908l-opener-lint-489-ef8652` **三笔**，均已 push：
  - `2613969` 工具三处修（判据正本声明式 ／ 全量 `--enforce` 157s→4~17s ／ `main()` 明细漏打 F3/F4/F5）
  - `7cb578a` 骨架两处（§三bis 样例补例外句 ／ 引用版禁手抄纪律行）
  - `b56317b` 收尾派单件（**本泳道波1 补提交**；原为未跟踪文件、只存在于该 worktree）
- 三笔经 `git merge-base --is-ancestor <sha> master` 逐笔核实 **均未在 master**。
- ff 前置已查：主仓脏文件中**无** `0-学习与工具/*.py`／`opener骨架.md`／`专线opener模板库.md`。
- `#489` 状态列已由波2 整格重写＋K2 外置，现 **4048 B < 4096 B（余量仅 48 B）**；外置件 `1-转型规划/0-全景路线图/队列行日志/#489.md` 已建（**尚未落库，待 sweep**）。

核对命令（只读）：

    git log --oneline -3 origin/claude/op0908l-opener-lint-489-ef8652
    git log --oneline "origin/claude/op0908l-opener-lint-489-ef8652..master" | measure
    git diff --name-only $(git merge-base master origin/claude/op0908l-opener-lint-489-ef8652)..master

## 二、做什么（按序）

### 1. rebase 分支到 master，解两处冲突

    cd "C:\Dev\zhuopin-ai\.claude\worktrees\op0908l-opener-lint-489-ef8652"
    git fetch origin
    git rebase origin/master

🔴 **解冲突的判据（不是"随便挑一边"）**：两侧改的是**同一文件的不同关注点** —— master 侧 `#505` 动的是 `append-row` 的高水位线回写路径，分支侧 `#489` 动的是 `release` 侧对判据正本的**调用方式**。**正解是两者都保留**，不是二选一。逐块核对 hunk 归属，**任何一侧的功能被丢掉都算解错**。

### 2. 重跑三套单测，验零回归

    pytest 0-学习与工具/test_工具-opener块lint.py 0-学习与工具/test_工具-opener生成.py 0-学习与工具/test_工具-共享文档编辑锁.py

🔴 **验收闸是「零回归」不是「全绿」**（`.claude/rules/两桌同步与取证.md` §二，Shao Peishen 2026-09-08 答 1a）：若有失败，须在**纯 master 零改动**上用同命令逐条复现，证明同一组失败本就存在；**自陈零回归必须附手段**。
🔴 `#489` 交付基线是 **603 passed／1 skipped**（`OP-0908-L` 实测），rebase 后不得低于此。

### 3. force push（仅本分支）

    git push --force-with-lease

🔴 **必须 `--force-with-lease`、不得裸 `-f`**：本分支已被 `OP-0908-L` 与看护者本体先后推过，lease 是唯一能挡住"覆盖掉别人刚推的东西"的闸。

### 4. ff master（🔴 在主 checkout 做，worktree 里 ff 不到 master）

    cd C:\Dev\zhuopin-ai
    git fetch origin
    git merge --ff-only origin/claude/op0908l-opener-lint-489-ef8652
    git push

⚠️ ff 若撞未提交的同文件改动：存补丁 → `checkout` 还原 → ff → `apply --3way`，**不要用共享的 stash 栈**。

### 5. 波4 · 回写销号

ff 成功后走编辑锁，把 §一 `#489` 首段 `[S:open]` 翻 `[S:done]`。
🔴 **翻转 `[S:xxx]` 前缀必须整格重写、只能走 `--changes-json`**（该格密集使用反引号，`--set` 走 argv 会被反引号奇偶校验拦）。
🔴 **重写后回读核字节**：`[Text.Encoding]::UTF8.GetByteCount()` 或 `len(s.encode('utf-8'))` —— **K2 的 4096 上限量的是字节不是字符**，中文件按字符判会得出宽约 2 倍的假上限（波2 已实撞一次）。现值 4048 B，余量仅 48 B，**新增内容超出即须同步追进外置件 `队列行日志/#489.md`，不得把行撑回超限**。

## 三、不做什么

- 🔴 **不 rebase／不 ff 任何其它分支**——本棒只处理 `claude/op0908l-opener-lint-489-ef8652` 一条。
- 🔴 **不动 §一 `#461`**（`[S:done]`）；不改 lint 的六个失效形态判据本身。
- 🔴 **worktree `op0908l-opener-lint-489-ef8652` 收工不自删**（`b56317b` 已落分支，风险已解，但本棒仍在其中作业）。
- **不把代码提交登进 §二**——它们在分支上、不经 sweep。§二 批次 `B-0908_489判据正本声明式与全量提速` 已由波2 补全（含反引号修正）。

## 四、一处认知订正，请一并知悉（避免下一棒重复排查）

看护者本体端定夺时曾把 `#500`／`#505` 与 `#489` 并列为「三条待 ff」，**实测前两条都已在 master**：

- `#505` — `git rev-list --count master..origin/claude/op0908x-505-hwm-apply-a1e02f` ＝ **0**，其内容即 master 的 `c388c97`。
- `#500` — 功能改动已在 master（`ad16695`），分支上是旧 SHA；`git diff master <branch>` 反向显示 **master 比分支还多一段注释**。

🔑 **同 `b752d41` 已记的判据「SHA 谱系 ≠ 内容已合入」，这次由看护者本体自己犯了一遍**：队列行里"未 ff master"那句写于当时、此后被别的批次带进 master，而**行内那句话不会自己过期**。⇒ **凡"待 ff"结论，落地前必须用内容口径现查**（`rev-list --count` ＋ `diff`），不得引用队列行里的历史陈述。

## 五、起棒设置

    执行环境：CC
    分支：claude/op0908l-opener-lint-489-ef8652（沿用，不新起）
    worktree：☑ 复用既有 `.claude/worktrees/op0908l-opener-lint-489-ef8652`，🔴 收工不自删
    工作区：无（纯库内，不触碰 .51／企微／定时任务）
    session：新开
    派出线：Cowork 业务总线 OP-0908-V（泳道 489-closeout 波3 转出）

## 六、开场词（复制即用，▶ 粘贴端：CC 新会话）

> 🔑 **本件取号踩了一次 `#487` 那个盲区的反向变体，记在这里**：先把 `OP-0909-J` 写进本件 frontmatter、再跑生成器 ⇒ **生成器扫已落盘痕迹，把我自己刚写的号判成"已占"**，换 `G` 同样被自己占住，两次循环。⇒ **正确次序是「先取号、后落盘」**；本件最终用占位符 `__OPID__` 临时抹掉全部号、取到 `OP-0909-G` 后再回填。**同 `#487` 子项「取号查重扫的是已落盘的痕迹，而取号与落盘之间有一段真空」——那条记的是"取了没落盘＝没占"，本次是它的反面"落了盘却还没取号＝自己占自己"。**

```
[OP-0909-G]【CC】489rebase
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909g-op0908l-rebase`） ｜ worktree：☑（复用 op0908l-opener-lint-489-ef8652，收工不自删） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-V（泳道 489-closeout 波3 转出）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0909G-489rebase。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/派单件-【CC】489rebase与ff-2026-09-09.md` → ② `CLAUDE.md` §§3／§5 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 🔴【设置】分支字段以本条为准：**沿用既有分支 claude/op0908l-opener-lint-489-ef8652、复用既有 worktree**，不新起分支不新建 worktree（生成器只会写「从 master 起新分支」）
2. 按派单件 §二 五步：rebase 到 origin/master 解两处冲突（工具-共享文档编辑锁.py ＋ 其单测）→ 重跑三套单测验零回归 → push --force-with-lease → 主 checkout ff master → 波4 回写销号
3. 🔴 解冲突判据：两侧改的是同一文件的不同关注点（master 侧 #505 动 append-row 高水位线路径；分支侧 #489 动 release 侧对判据正本的调用方式），**正解是两者都保留、不是二选一**；任一侧功能被丢掉都算解错
4. 🔴 验收闸＝零回归非全绿（rules/两桌同步与取证 §二）；#489 交付基线 603 passed／1 skipped，rebase 后不得低于此；自陈零回归须附手段

不做什么：
- 🔴 不 rebase／不 ff 任何其它分支；#500 与 #505 经内容口径实测**早已在 master**，不要再去 ff 它们
- 🔴 波4 回写后必须回读核**字节数**（非字符数）——K2 上限 4096 B，现值 3919 B 余量仅 177 B，超出即同步追进外置件 队列行日志/#489.md
- 不动 §一 #461；不改 lint 六个失效形态判据本身；worktree 收工不自删；不碰 .51／企微／真实库
```