---
title: 派单件 · 【CC】`#632` 纯 master `test_工具-落库sweep.py` 16 条 pre-existing 失败逐条分诊
created: 2026-09-21
派出线: 环境总线 OP-0921-A
批次: B-0921_必读件与分诊
任务类: A 类
---

# 派单件 · 【CC】`#632` 16 条 pre-existing 失败逐条分诊（§一 #632）

## 〇、为什么做

纯 master 上 `0-学习与工具/test_工具-落库sweep.py` **16 failed**（非本棒引入，pre-existing）。后果：**ff 六关④ 对这 16 条覆盖的行为形同虚设**——它们一直红着，合入时看的是「没变得更红」，等于没有守。本棒把这 16 条逐条判死。

现取分布（2026-09-20 `OP-0920-B` 实测，本棒起跑前已另跑一次取全量 node-id，见下 §二）：
`DeploymentTraceHintTests` 3 ｜ `OrphanFileAlertTests` 4 ｜ `OrphanResolvedNotificationTests` 2 ｜ `ResidentServiceDeploymentHintTests` 3 ｜ `ExactMatchEndToEndTests` 1 ｜ `OrphanSectionFourEscalationTests` 1 ｜ `ScenarioSpecGapAnnounceIntegrationTests` 1 ｜ `StaleChangeAnnounceIntegrationTests` 1。

## 一、方案闸（已过，照做即可，不得中途换做法）

1. **判据粒度**：只看 pytest 结论行（`FAILED …` 与末行汇总），**测试输出一律重定向到文件、只读结论行**，禁通读 traceback 全文；需要某条细节再 `Select-String` 定位后只读 ±40 行。
2. **narrow**：回归只跑**受影响的测试类 node-id**（`文件::类`），**禁 `-k`**，禁跑整份仓库回归。整份 `test_工具-落库sweep.py` 本身就是本棒的对象，跑它一次取基线是允许的，但每条修完只重跑该类。
3. **长跑段**：任何 >3 分钟的跑测用后台起、落哨兵文件，**起跑前先删旧 `.done`**，判完成比 mtime 是否晚于本轮起跑时刻；不逐分钟 tail 日志。
4. **产出落点**：分诊表落 `1-转型规划/0-全景路线图/分诊-632-落库sweep16条-2026-09-21.md`；代码改动留在本泳道分支。

## 二、做什么

⑴ 先取全量 node-id 基线（一次）：

```
python -m pytest "0-学习与工具/test_工具-落库sweep.py" -q --no-header -p no:cacheprovider > reports/632-baseline.log 2>&1
```

读 `reports/632-baseline.log` 里的 `FAILED` 行与末行汇总即可，不读正文。

⑵ **逐条三选一判定**，每条都要写明理由与证据行：

| 判定 | 含义 | 处置 |
|---|---|---|
| **真失败** | 被测行为确实坏了 | 修 `工具-落库sweep.py` 本体，补/改单测 |
| **夹具腐坏** | 本体 fail-loud 正确，是沙箱夹具缺桩／接线没搭 | **本体不改**，只补夹具与接线单测（先例：`#631` 第 19 类三条红，裁定 fail-loud 正确、判为夹具缺桩） |
| **已废弃** | 被测行为已被后续改动取代 | 删该测试，**必须写明被谁取代、哪一行队列／commit 取代的** |

⑶ 按判定实施，全部 16 条做完，最终 `文件::类` 逐类重跑到绿。

⑷ 产出分诊表：一行一条，列＝`类::方法` ｜ 判定 ｜ 一句理由 ｜ 处置 ｜ 重跑结论。

## 三、期望产出（缺一个就不许写 `OPENER_DONE`）

1. `1-转型规划/0-全景路线图/分诊-632-落库sweep16条-2026-09-21.md`（16 行，无空白判定）
2. `0-学习与工具/test_工具-落库sweep.py`（及按需 `0-学习与工具/工具-落库sweep.py`）的改动
3. 收工贴 `git diff --name-only master...HEAD` 的输出

## 四、不做什么

1. 🔴 **禁止为了把测试跑绿而放松一道已经正确拒绝过的闸**（2026-09-20 血教训②，实证：六关④ 正确拒了 `#601`，有人用更窄 narrow 重跑合入把 master 弄红）。判成「夹具腐坏」必须给出「本体 fail-loud 正确」的正面证据，不能只因为改本体更省事。
2. 🔴 不碰 `.claude/rules/`、不碰根 `CLAUDE.md`（本棒同批另一条泳道在做减字，加字会撞）。
3. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
4. 🔴 收工只 push 本泳道分支，**不碰主仓、不 ff master**（ff 属 🟡 档，由他一字母后串行做）。
5. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
