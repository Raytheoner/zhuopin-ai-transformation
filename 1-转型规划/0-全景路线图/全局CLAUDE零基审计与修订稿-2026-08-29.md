---
status: 待他一次动作（修订稿已备，未直改）
title: "全局 ~/.claude/CLAUDE.md 零基审计与修订稿（OP-0829-W）"
created: 2026-08-29
执行方: Cowork 构建环境方案线（OP-0829-W，承接 OP-0829-W 交接卡【下一会话主攻】第 2 项）
---

# 全局 `~/.claude/CLAUDE.md` 零基审计与修订稿（OP-0829-W）

> **判据两条**：**① 层级纯度** —— 用户域只放「换个项目仍然成立」的东西；**② 适用半径** —— 一条写在全局的规则，是否真的每个会话都该踩（`#138` 入档判据）。
> **口径**：本件**只出修订稿、不直改**。改 `~/.claude/CLAUDE.md` 属 Claude 配置层，按本役 R/S 两例：走「他人工一次」或经其明示授权后由本线直改。
> 🔴 **一处纠正**：交接卡记「全局 5.7KB」，**本机实测 9,875 B**（`(Get-Item ...).Length`，2026-08-29 22:31 本地）。差近一倍，按实测走。

---

## 一、分段量测（本机实测，非估算）

| 段 | 行 | 字节 | 层级纯度 | 适用半径 | 判决 |
|---|---|---|---|---|---|
| 身份与角色 | 1-8 | 880 | ✅ 真全局 | 全机 | 留 |
| 技术背景 | 10-14 | 314 | ✅ | 全机 | 留 |
| **当前核心项目** | 16-20 | 361 | ✅ | 全机 | **🔴 改（路径已失真）** |
| 行业特殊约束 | 22-26 | 426 | ⚠️ 半径＝卓品业务 | 多项目 | 留（合规红线，违反不产生信号） |
| 沟通偏好 | 28-32 | 360 | ✅ | 全机 | 留 |
| 工作方式偏好 | 34-38 | 286 | ✅ | 全机 | 留 |
| **last30days 中英双版** | 40-62 | **1,668** | ❌ 单工具 | 单工具 | **压（但不迁 memory，理由见 §三.7）** |
| 路径加引号约定 | 64-66 | 453 | ✅ 本机环境红线 | 全机 | 留 |
| 环境维护任务 | 68-73 | 628 | ⚠️ 台账 | 全机 | 留（可压，收益小） |
| **运行环境基线** | 75-81 | 486 | ⚠️ 含版本快照 | 全机 | **🔴 改（快照行删＋项目根写实）** |
| 凭证约定 | 83-84 | 160 | ✅ 红线 | 全机 | 留 |
| **Obsidian 焦点笔记** | 86-96 | 774 | ❌ 单应用 | 单应用 | **🔴 改（补适用条件）＋ token 建议** |
| 回复格式约定 | 98-103 | 367 | ✅ | 全机 | 留 |
| 提示音 Skill | 105-121 | 679 | ⚠️ 一次性配置台账 | 单机 | 压（可选） |
| **last30days-cn PR #12** | 123-143 | **982** | ❌ 外部 PR 台账 | 单工具 | **迁出／删（可选）** |
| 模型路由省成本 | 145-153 | 760 | ✅ 真全局判据 | 全机 | 留（来源叙事可略压） |

**一句话**：全局文件里**真正每个会话都该踩的**约 4.6 KB；其余 5.3 KB 是**单工具说明、一次性配置台账、外部 PR 状态、会静默变旧的版本快照**。

---

## 二、🔴 必改四处（失真／漂移／半径，与瘦身无关，改了才对）

### ⑴ L20「路径均在 `~/OneDrive/Projects/<项目名>`」——对 zhuopin **已假**

实测（2026-08-29 22:3x 本机）：`C:\Dev` 下只有 `zhuopin-ai`；`~/OneDrive/Projects` 下 `supplychain`、`claude-howto` **仍在**。⇒ 「均在」这个全称量词已被一个反例推翻，改为**逐项写路径**。

🔑 **这一处的危险形态**：它不会报错。任何会话按此行去 `~/OneDrive/Projects/企业AI转型` 找卓品仓库，会**找不到**（那是 2026-08-25/26 的 OneDrive 迁出结果，队列 `#407`／`#413`），而不是拿到错的内容——属「静默失效」而非「静默错误」，但同样零信号。

### ⑵ L81「项目根：`~/OneDrive/Projects`（……后续可能移出 OneDrive）」——同族第二处

「后续可能」**已经发生**。同一份文件里两处写同一件已变的事，是副本漂移的标准形态。⇒ 与 ⑴ 一起改，且**只在一处留路径**（§当前核心项目），基线段不留副本。

### ⑶ L80「Claude Code：v2.1.x · Opus 4.x」——版本快照，判据价值为零

实测 `claude --version` ＝ **2.1.226**；模型代已到 Opus 5 / Fable 5。**整行删**，判据一句：

> **没有任何一条纪律以「CC 版本号是多少」为条件** —— 它写在这里唯一的作用就是**静默变旧**，然后被某个会话当成事实引用。要用时现取（`claude --version`）比留快照更准。

⚠️ **同段其余不删**：Python / Node 的**绝对路径**有判据价值（脚本与计划任务要引用），保留。

### ⑷ L88「**每次对话开始时**（回答问题/执行任务前）执行」——半径写得比实际大

这条要求的对象是 Claudian ＋ Obsidian Local REST API 在线的会话；但它写成「每次对话」，于是**每一个 CC 建造会话、每一个 Cowork 规划会话读到它，都在被要求先跑一条与本职工作无关的 `curl localhost:27123`**。这正是 `#138` 那条「适用半径」判据要拦的形态。⇒ 补一句显式适用条件。

**附带一处，属他的凭证、本线不代改，只报**：L91 的 `Authorization: Bearer <明文 token>` 直接写在正文里。
- **不违反本文件 §凭证约定的字面** —— 该条禁的是「写进**会被同步的** settings」，而实测 `~/.claude` **不在** OneDrive 同步树内（`Test-Path "$env:USERPROFILE\OneDrive\.claude"` ＝ **False**）。
- **但**：这份文件**每个会话都被整份注入上下文**。建议把 token 移进不被注入的载体（如 `~/.claude/.env`），正文只留变量名与取法。**这属于凭证处置，等他一句话，本线不动。**

---

## 三、可选瘦身三项（**默认不做**，等他一句）

### ⑸ last30days-cn PR #12 段（982 B）—— 建议迁 CC auto-memory 技巧层

外部 PR 状态台账，最后更新 2026-06-24，**至今两月未核，状态很可能已变**。
🔑 **用根 `CLAUDE.md` 顶部那把尺判**：不知道这个 PR 的状态，**只会多走弯路（重复打补丁），不会做错事** ⇒ **技巧类，可入 memory**。

### ⑹ 提示音 Skill 段（679 B）—— 建议压到 2 行

已部署完毕的一次性配置。留「激活命令＋三层映射」两行，删成因与特性罗列。同为技巧类。

### ⑺ last30days 中英双版段（1,668 B，全文件最大）—— 🔴 **看起来最该迁，实际不能迁 memory**

它长得最像工具技巧，但它的**显式目的**写在段尾：「**防止后续 session 误判中英 last30days 为重复而卸载**」。
⇒ **违反它会让人做错事（误卸载一个在用的插件）** ⇒ 按同一把尺，**属纪律，不入 memory**。

**可做的只有压缩**：留「两版互补·**勿当重复卸载**＋选择规则一句＋细节指针」约 400 B，正文迁 `0-学习与工具/`。
🔴 **但守 J1** —— 承接载体尚未建立 ⇒ **本稿不含这一项**，要做须先建载体、再迁。

---

## 四、修订稿（保守版：只含 §二 必改四处，净 −约 330 B）

> 只列**改动块**，其余行一字不动。授权后由本线直改，或他自行覆盖。

**① 替换 L16-20（「当前核心项目」整段）为：**

```markdown
## 当前核心项目(详情见各项目自己的 CLAUDE.md)
- **企业AI转型** — 18个月·六部门 AI 转型,SuperPowers + Hermes,2026-07 启动 —— `C:\Dev\zhuopin-ai`（🔴 2026-08 已迁出 OneDrive）
- **supplychain** — 供应链 Agent(采购/库存/物流已完成),数据源 携客云 SRM / U9C ERP —— `~/OneDrive/Projects/supplychain`
- **claude-howto** — 系统学习 Claude Code —— `~/OneDrive/Projects/claude-howto`
```

**② 替换 L80-81 两行为（版本快照行整行删）：**

```markdown
- 项目根：见上「当前核心项目」逐项路径（本段不留副本）
```

**③ 在 L88 段首插入一行适用条件：**

```markdown
**适用条件（先判，不满足直接跳过本节）**：仅当本次会话来自 Claudian 且 Obsidian Local REST API 在线时执行；CC / Cowork 会话不适用，不必尝试。
```

**④ L91 的 Bearer token** —— 待他一句话决定是否移入 `~/.claude/.env`。**本稿不动。**

---

## 五、需他的一次动作

1. **授权本线直改** ⇒ 本线按 §四 ①②③ 三处落地，token 那处不碰；或
2. **他自己覆盖** ⇒ §四 三块可直接粘。

**⑸⑹⑺ 三项默认不做**，等他明确要不要。
