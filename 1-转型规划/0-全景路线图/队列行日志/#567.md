---
title: "队列行日志 #567（§一）"
created: 2026-09-20
status: 生效
用途: 跨桌任务队列 §一 #567 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #567 · 外置段（2026-09-20 外置，工具第 1 批，2 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-K；外置前整格 5685 B、共 3 段；本批外置第 2、3 段；本格保留第 1 段。

🔁 **2026-09-13 07:40 复现（Cowork 环境总线 `OP-0913-E` 起 `B-0913_人守打通` 时当场撞上）**，现象与 09-12 逐字相同：`launcher-stderr.log` 报 `Start-Job : A parameter cannot be found that matches parameter name 'WorkingDirectory'`，`launcher.json` 的 `shell` ＝ PS 5.1，零泳道日志、`exit.txt` 未落，而父进程照报 `✓ 已后台起（pid 44592）` 且 `EXIT=0`。 🔑 **本次补两条 09-12 没写进来的新信息**：⑴ **耦合点在 `opener骨架.md`**——它「默认后台起」那一节给出的起跑命令**字面就是 `powershell …`**，而 `-Detach` 用 `(Get-Process -Id $PID).Path` 把**调用它的那个 shell** 原样传给子进程 ⇒ **照骨架抄的人必然踩雷**，与「本机默认 shell 是什么」无关、与「起跑者是谁」也无关。**已同批订正骨架该行为 `pwsh -NoProfile …` 并补两句判据**（本会话 🟢 文档改动，未动脚本一行）。⑵ 🟩 **更省的规避**：同一条 `-Detach` 路径只要**用 `pwsh` 起**就正常——07:42:03 重起，`launcher.json` 的 `shell` ＝ `…Microsoft.PowerShell_7.6.6.0…/pwsh.exe`、`launcher-stderr.log` **0 B**、A1 泳道 07:42:06 正常启动。⇒ **09-12 那条「不传 `-Detach`、调用方自己包一层」的绕行可以退休**：缺陷是**继承调用方 shell**，不是 `-Detach` 本身；期望产出 ① 的「优先 `pwsh`、回退 `powershell.exe`」方向由此再获一次实证支持。 🔴 **残留一个空批目录 `reports/opener-batch/20260913-074005`**（3 个 launcher 文件、无泳道、无 summary）——它会被探针当「在跑」，90 分钟后报一条**假停滞**；下一棒清理时一并删，或由 ⑵ 的退出码兜底落地后自然消失。

━━━

🆕 **2026-09-19／20 同族第二、三例并入本行（`OP-0919-K` 实撞，按「不立新机制行」并记不新建）**：**⑵ 起批用 PS 5.1 必炸在另一处**：Shao Peishen 2026-09-19 14:20 UTC 那次起批（`reports/opener-batch/20260919-222051`）launcher 报 `Start-Job : A parameter cannot be found that matches parameter name 'WorkingDirectory'`（`工具-opener批处理执行v2.ps1:461`；该参数 PS 7 才有）⇒ 除 `-Detach` 外，`Start-Job -WorkingDirectory` 是第二个 PS 5.1 炸点。 🔴 **更坏的一半：炸完不落 `exit.txt`**，批目录永远停在「在跑」，按「只探哨兵」的纪律会一直等下去——**失败连哨兵都不落＝静默挂死**。修法＝launcher 包一层 `finally`，不论结局都落哨兵（同 `#550` 判词）。 **⑶ 回归段会挂死，且挂死同样不落哨兵**：2026-09-19 23:54 起的一条 ff，`pytest` 跑到一半机器休眠，醒来后进程还在、日志 **8 小时 47 分零输出**；主脚本同步等 `& python`，外层哨兵永远不落。**已修并带回归用例**：`工具-泳道分支合入-回归判定.ps1` 新增 `Invoke-PytestWithIdleTimeout`（**空闲超时**，不是总时长超时——只要还在产出就不打断，全量回归本来就要 29 分钟），`工具-泳道分支合入.ps1` 两处 pytest 调用改走它、新增参数 `-TestIdleTimeoutMinutes`（默认 20）、新增退出码 **7＝回归挂死超时**（映射为「被打断」，🔴 **不是**「回归新增失败」——挂死没判出任何结论）；纯 master 基线侧同样超时即判 7（基线跑不出来就无从比较，不得据此放行或拒绝）。回归 narrow：`test_工具-泳道分支合入-回归判定.py` **14 passed**（原 10 ＋ 新 4，两个方向都守：持续有输出的长命令不被误杀／连续无输出即判挂死并杀进程树，另测挂死前已产出的输出不丢、非零退出码不被吞）。 **⑷ 本行原记的假停滞源已清**：`reports/opener-batch/20260913-074005` 与同形态的 `20260919-222051` 两个空批目录（有 launcher、无泳道、无哨兵）已删；现取全库仍有 7 个同形态旧目录（`20260824-163421`／`-165657`／`20260825-081752`／`-113618`／`-141800`／`20260911-153523`／`20260912-092223`），其中数个 launcher 件数较多、疑似泳道日志命名不同，**未敢一并删**，留值周巡检逐个核。
