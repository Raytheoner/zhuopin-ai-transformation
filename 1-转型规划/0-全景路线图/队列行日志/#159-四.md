---
title: "队列行日志 #159（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #159 的事项列外置件（K3 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #159 · 存量批次排查表（11 行逐条）· 2026-09-04 CC 泳道 `queue-skip13`（批次 `B-0904_J` 波1）

> 立行依据：看护件 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0904_J-2026-09-04.md` §三 A1。
> 🔴 本件只登结论、只提建议：**全程未执行任何 `git merge`／`git rebase`／`ff`**，ⓐⓑⓒ 的「下一步」一律未选（`merge_to_master` 属 🟡 档）。
> 🔴 `B-0902_49`／`B-0902_62`／`B-0902_64`（oem-chroma 族）按看护件硬边界 2 **未查、未碰**。

## 一、数量口径先更正

看护件 §〇 写「存量 **13** 个被跳过批次」、§一 硬边界 2 写「本批清单只剩 **10** 个」，但其自身列举的名字是 **14 个**：`B-0902_47/48/49/62/64`（5）＋ `B-0903_12/13/21/33/51/80/82/99`（8）＋ `B-0904_H`（1）。去掉 oem-chroma 三个（`49/62/64`）后**实为 11 个**——两处数字各差 1。**本次实查 11 个，无遗漏**。

## 二、排查表

| # | 批次（§二 全名） | 域 | 类型 | 结论 | 依据 | 建议下一步 |
|:--:|---|:--:|---|:--:|---|---|
| ⑴ | `B-0902_47_OP0902A1_§四全表状态分诊报告` | 机 | 零可落库文件（产出全在 gitignore 的 `reports/`） | **ⓑ** 已完整、无内容需落库 | §二 行本身已在 master 的机制环境队列（`git show master:<队列> ｜ grep -c` ＝1）；`reports/sweep-commit.log` 该批次末条跳过因＝「文件清单列解析不出任何反引号片段，**既无法落库也不能安全补销**」 | 纯销号。但今日 sweep 对「零可落库文件」批次**没有销号入口**（见 §三 ②） |
| ⑵ | `B-0902_48_OP0902A2_采购提前期中位数统计` | 业 | 真有 commit、未进 master | **ⓐ** 内容干净、可安全合入 | 分支 `claude/op0902a2-leadtime-median` ＝ `a74e7d9`，`local==origin`；`git cherry -v master <分支>` ＝ **唯一一个 `+`**（11 个批次里只有它的补丁不在 master）；ahead 1 ／ behind 138；只读 `git merge-tree --write-tree --name-only master <分支>` 退出码 **0**、**零 CONFLICT**，结果树 `a969a30` 相对 master 只有 `A` 两个新文件（`4-数字员工/采购部/SC7-库存优化建议/scripts/leadtime_median.py`、`…/tests/test_leadtime_median.py`），master 上无同名件（`git ls-tree -r master` 核过） | 待批合入（`merge_to_master` 🟡）。🔴 **不是字面 ff**——`git merge-base --is-ancestor master <分支>` ＝ NO ⇒ 须 `git rebase master <分支>` 后 `git merge --ff-only`，同 `B-0903_51` 手法。⚠️ 另：sweep 对本批「能对上的」5 条**全是 oem-chroma 他线脏件**（来自本行「登记豁免」正文里的反引号路径），直接放行 sweep 会把他线改动夹带进本批 |
| ⑶ | `B-0903_12_OP0903A2_SC4顺延移交单与FI10立行` | 业 | 主工作区未跟踪件 | **ⓒ** 需人读后判断 | 其唯一实产出 `1-转型规划/0-全景路线图/移交单-SC4顺延与前置总表补5行-全景路线图线执行重组循环-2026-09-03.md` **从未进过 git**（`git log --all -- <path>` 零命中），现为 `??` 未跟踪、**未被 gitignore**（`git check-ignore -v` 未命中），42,867 B、mtime 2026-09-03 09:58；其余声明路径（两份队列）已在 master。sweep 跳过因＝覆盖校验未过，被点名 4 条里 3 条是**正文引用**（`移交单-O1注销并入…-2026-09-02.md` 体例参照件、gitignore 的心跳件、`0-学习与工具/工具-仓库外载体扫描.py`） | 须人判：该件**交全景路线图线执行**、且已被 `B-0903_33` 二次改写（EE-5／EE-6 并入）⇒ **入不入库是其归属线的判断**，不该由排查线代定 |
| ⑷ | `B-0903_13_OP0903A1_SC8_118_ATP重审收口` | 业 | 有 commit，已被后续批次带进 master | **ⓑ** 已过时 | `git cherry -v master claude/op0903a1-atp-review` ＝ 4 条**全 `-`**（补丁等价已在 master）；原 hash `1bf6b51` 已非 master 祖先——`B-0903_51` 于 2026-09-03 rebase 后 hash 必变；声明的 5 个非队列文件 `git cat-file -e master:<p>` **全 ✓**（`openspec/changes/sc8-atp-batch2-closeout/{design,tasks}.md`、其 `specs/delivery-date-forecast/spec.md`、SC8 两份 `docs/queue_118…`／`queue_344…`） | 纯销号 |
| ⑸ | `B-0903_21_OP0903B1_SC8_118_ATPapply_spec落主specs` | 业 | 同 ⑷ | **ⓑ** 已过时 | `git cherry -v master claude/op0903b1-atp-apply` ＝ 6 条**全 `-`**（含 `e615723`／`35674a6`）；`openspec/specs/delivery-date-forecast/spec.md`、`openspec/specs/pmc-priority-allocation/spec.md` 均在 master ✓ | 纯销号 |
| ⑹ | `B-0903_33_OP0903C1_采购三包sync主spec_暂不归档声明_移交单并入EE5EE6` | 业 | 半落半未落 | **ⓒ** 需人读后判断 | 声明的 6 个 openspec 文件（`openspec/specs/{sc4-clause-extraction,sc10-bom-review-facts,sc11-transfer-planning}/spec.md` ＋ 三包 `tasks.md`）`git cat-file -e master:<p>` **全 ✓**；分支 `claude/op0903c1-proc-apply` 当前 tip `54f56cb` 已是 master 祖先（`--is-ancestor` YES）。**但**其文件清单明写的第 7 件＝⑶ 那份移交单，仍未跟踪 | 与 ⑶ **是同一个对象、须一并判**。📌 附带：`origin/claude/op0903c1-proc-apply` 仍停在 rebase 前的 `9ba60c3`、`origin/claude/op0903c2-spec-note` 停在 `bab88c7`（`local≠origin`）——远端分支指针过时，**不影响 master**，只影响按远端读的人 |
| ⑺ | `B-0903_51_OP0903D1_两链合master_采购三包链_SC8_ATP批2收口链` | 业 | 纯队列回写 | **ⓑ** 已完整 | 其自陈的 master 推进点 `209fde4` 现为 master 祖先 ✓；§二 行与 §一 四行回写全在 master 的业务场景队列里——该文件当前 `git status` **零脏改动** ⇒ 100% 已提交；sweep 跳过因＝覆盖校验未过，被点名 7 条**全部来自其「登记豁免」正文里的反引号路径**（他线脏件） | 纯销号。本行是「正文反引号误伤」最干净的样本 |
| ⑻ | `B-0903_80_OP0903D_§四分诊两批20条裁决回写` | 机 | 纯队列回写 | **ⓑ** 已完整 | 文件清单明写「本批只改队列文件…无独立产出件」、无反引号片段；§二 行已在 master 机制环境队列 ✓；该文件当前脏 diff 仅 3 处（§一 `#284` 台账、`B-0904_A3`、`B-0904_J` 立档行），**与本批无关** ⇒ 其回写已提交 | 纯销号 |
| ⑼ | `B-0903_82_OP0903D_分诊派生五组机制行479_483` | 机 | 纯队列回写 | **ⓑ** 已完整 | master 版机制环境队列里 §四 `#479`～`#483` **五行齐全**（按行首编号 `grep -c` ＝5）；`工具-队列查询.py --row 479／483 --section 四` 均可取到 | 纯销号 |
| ⑽ | `B-0903_99_OP0903D_代F1补登记_390真实全量取数回写` | 业 | 纯队列回写 | **ⓑ** 已完整（与看护件 §〇 预判一致） | §二 行已在 master 业务场景队列 ✓，且该文件工作区零脏改动 ⇒ 含 §一 `#390` 回写在内**全部已提交** | 纯销号。🔴 行内自带一个未闭合项（`probe390_out.json` 全仓搜索无此件、须重跑探针）——那是 `#390` 的事、**不是本批落库的事**，不因此改判 |
| ⑾ | `B-0904_H_OP0904B_382事件驱动起活建成` | 机 | 有 commit 且已在 master | **ⓑ** 已过时 | 声明的 6 个文件 `git cat-file -e master:<p>` **全 ✓**；落库提交 `875f6f4 feat(aibot): 桥一落信号后直接起无头 CC 拆件，去掉轮询（队列 #382⑴bis）`；sweep 跳过因＝文件清单全裸路径、无反引号 | 纯销号 |

**结论分布**：**ⓐ 1 个**（⑵）｜**ⓑ 8 个**（⑴⑷⑸⑺⑻⑼⑽⑾）｜**ⓒ 2 个**（⑶⑹，同指一份移交单）。

## 三、三条跨条结论（比逐条更重要）

① **「被跳过」≠「内容没落库」**。11 个里 **9 个的内容其实早已在 master**，卡住的只是 §二 那一行本身销不掉。真正还没进 master 的只有两样：`B-0902_48` 的两个 SC7 文件（干净可合），以及 ⑶／⑹ 共指的那份 SC4 移交单（从未进过 git）。

② **跳过只有两因，且都是登记格式问题、不是内容问题**：
- **「文件清单列解析不出任何反引号片段」7 个**（⑴⑷⑸⑻⑼⑽⑾）——sweep 自己写明这种「**既无法落库也不能安全补销**」，即**结构性死锁**：凡「只改队列文件」「产出全在 gitignore 的 `reports/`」的批次，登记完就**永远留在 §二**、无任何机器入口销号。
- **「文件清单覆盖校验未过」4 个**（⑵⑶⑹⑺）——被点名的路径多为**正文引用**（他线脏件、gitignore 的心跳件、体例参照件），即 sweep 提示语自己说的那句「若被点名的片段其实不是待落库路径（登记正文里的引用），把那对反引号去掉即可」＝**反引号误伤**。

📌 **补一条实测——本批登记自身撞出来的，正好是 ② 的一半解药**：`B-0904_I`（commit `0d500e1`，2026-09-04 上午，见 §一 `#381`⑸ⓘ1）新上的「§二 登记预检」**已经堵住了新增**。本泳道自己登 §二 时被它逐条拒绝了 5 项：文件清单里 4 个**不是路径的反引号串**（`acquire`、`acquire --reserve 1 --section 四`、`B-0902_48`、`B-0903_51`——正是正文引用误加反引号的同一形态），以及**批次号前缀撞号**（`B-0904_J` 已被 `B-0904_J_OP0904I_泳道看护批立档` 占用，并给出改名建议）。⇒ **② 的「反引号误伤」因对新批次已机制化关闭**；但**存量这 11 行是它上线之前登的、不会被追溯校验**，仍须人工订正或另开入口。⇒ **② 的另一半——「零可落库文件／纯队列回写批次没有销号入口」——至今无任何机制**，是本次排查最值得升格的一条。

③ **看护件 §〇 有一处过时快照须更正**：其称 `B-0902_48`／`B-0903_13`／`B-0903_21`／`B-0903_33`「均已在各自 worktree 分支里真实 commit 并 push…**只是从未 ff 进 master**」——实测 `13`／`21`／`33` 已由 `B-0903_51` 于 2026-09-03 rebase＋ff 合入并 push（`git cherry` 全 `-`、分支 tip 已是 master 祖先），**只有 `B-0902_48` 真未合**。

## 四、本次用什么核的

`工具-队列查询.py --row <批次全名> --section 二 --field all`（取 11 行全文；🔴 **注意批次号必须给全名**，只给 `B-0902_47` 这样的前缀会「未找到」）／`git rev-parse -q --verify <c>^{commit}`／`git merge-base --is-ancestor`（正反双向各查）／`git cherry -v master <分支>`／**只读** `git merge-tree --write-tree --name-only`（**全程未执行任何 merge／rebase／ff**）／`git cat-file -e master:<path>`／`git show master:<队列> ｜ grep -c`／`git ls-tree -r --name-only master`／`git log --all -- <path>`／`git check-ignore -v`／`reports/sweep-commit.log` 逐批次末条。

📌 **一处实测教训（供后来者省一次试错）**：`git log --all --oneline -1 <hash>` 里的 `--all` 会**吃掉 hash 参数**、恒返回 HEAD ——11 个 hash 挨个查会得到 11 行一模一样的 `6a500a8 docs(队列): 收工重跑文档台账（sweep 自动）`，**看着像「每个 hash 都存在」**。核对象存在性须用 `git rev-parse -q --verify <c>^{commit}`。同「只读命令结果太干净先怀疑没读到对象」族。

## 五、本泳道未做（如实登记、非遗漏）

- 未执行任何 `git merge`／`git rebase`／`ff`／`push`；未改任何生产代码一行；未改队列已有的历史回写段（硬边界 4）。
- 未碰 `B-0902_49`／`62`／`64`（oem-chroma 族）与 `3-治理与合规/OEM数据隔离规范.md`（硬边界 2）。
- 未部署、未触碰 `.51`；未起草、未发送任何跟进信；真实库只读（本批全程未访问 SRM／U9C）。
- ⓐⓑⓒ 三类的「下一步」一律未选（硬边界 3）。
