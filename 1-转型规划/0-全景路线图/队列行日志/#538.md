---
title: "队列行日志 #538（§一）"
created: 2026-09-10
status: 生效
用途: 跨桌任务队列-业务场景.md §一 #538 的建造完成报告外置件（K2 口径：单格 ≤4 KB，本段直接落此、行内只留收口结论＋验证手段＋本指针；原文原样、可 grep）。
配套: 4-数字员工/采购部/SC2-采购周报自动生成/取证-538-下单行数187复现与明细导出-2026-09-10.md；4-数字员工/采购部/SC2-采购周报自动生成/sc2/detail.py；4-数字员工/采购部/SC2-采购周报自动生成/tests/test_detail.py
---

# §一 #538 · 建造完成报告（2026-09-10，CC 泳道 `538-sc2-detail`／`OP-0910-U`，批 `B-0910_夜批六泳道`）

> 行内只留收口结论；本件为完整对账原文。立行原文（2026-09-10 拆件巡逻）仍在行内首段，未外置。

## 2026-09-10 22:34 建造完成段（`OP-0910-U`，外置原文）

> 原文原样，未压缩、未改判、未修辞。行内已替换为压缩版＋指针。

⏩ **建造完成·LAN 留步两项（OP-0910-U，泳道 `538-sc2-detail`，2026-09-10 22:34 本地）**。**⑴ 187 已复现**：本机 `po_cache.json`（ERP `ZpViewPurOrder` 90 天真实快照，写入 2026-09-08 14:23:05 本地，md5 `10d0cc0c…`）以基准日 2026-09-09 过同一管线 ⇒ 制单日落 09-07~09-13 共 188 行 − 全程委外 PO17 ×1 − 去重 0 ＝ **187** ✅；187 行全部制单于 09-07（周一），33 张单 `ZPCG20260907001`~`033` 连号（PO22 ×185／PO23 ×1／PO01 ×1，101 料号／25 供应商／5 采购员）；同缓存同管线交叉验证 W34＝201、W35＝47 与真实快照全等。**⑵ 明细导出能力已建**（此前为零）：新建 `sc2/detail.py`（三节×三窗口逐行 CSV，行集判据复用 `metrics.py` 同一组函数；`reconcile()` 逐格断言明细计入行数＝周报指标、不等即拒绝导出；数据集快照 `sc2_dataset_{period}.json` 与周报快照成对落盘、schema 版本号不符拒读）；`webapp` 新增 `GET /api/detail`（索引＋对账）与 `GET /api/detail/<order|receipt|open>.csv?window=`，页面 9 个链接，缺快照 404 提示先 refresh、**绝不现取 ERP**；`run_sc2.py` 新增 `detail --period` 子命令，`report`/`autopush` 落数据集快照；场景 CLAUDE.md 加 D39。**⑶ 验证手段**：`python -m pytest -q` 163 → **190 passed**（+27 `tests/test_detail.py`）；**变异检验四处均转红**（窗口 `<=`→`<`：9 failed／在途不剔已关闭：7 failed／对账放水：4 failed／可溯源索引改窗口内：1 failed，`md5sum` 核还原）；端到端 `run_sc2.py detail --period 2026-W36` 回显九格全等、`sc2_2026-W36_下单_本周.csv` 187 数据行（md5 `2b344e10…`，落 `reports/538-明细复现-2026-09-10/`，gitignore）。**⑷ 产出**：分支 `claude/op0910u-538-sc2-detail` @ `05f60e6` 已 push（起自 master `4c28a85`；master 现 `9cca471`，4 笔 docs 均未触碰 SC2 目录，rebase 无冲突预期），**未 ff master**（P4，由看护者/sweep 串行做）；取证件 `4-数字员工/采购部/SC2-采购周报自动生成/取证-538-下单行数187复现与明细导出-2026-09-10.md`。**⑸ LAN 留步**：① 187 行的确认数量／行状态／未清数量三列本机全为「未取到」（`Purchase/Query` off-LAN 无缓存；行集本身不受影响），交他的完整明细须 LAN 内对 2026-W36 跑 `report --mode real --max-status-materials 0` 再 `detail --period 2026-W36`；② `.51` 那份 187 未比对、本能力未部署（⏭️ 档，LAN 收口批做）。**⑹ 边界**：未碰 `#506` 基准日逻辑；未发送任何对外消息（明细交专员＝对外交付，须走跟进信审批，另起）；openspec 未起包（判深化类新增接口、不改既有语义与鉴权层），总线若判须补包另起行。⏳ 本行与 `#499` 先后仍在 §四 `#194`。

## 2026-09-10 20:5x 外置（`OP-0910-A`，1 段）

> 原文原样，未压缩、未改判、未修辞。行内已替换为指针。

[S:partial][D:业] 建造完成·LAN 留步（原「待领」立行文如下） —— 2026-09-10 拆件巡逻（事件驱动班）由 `采购部#22` 回件拆出立行。**他的原文（不改一字）**：「我检查下来，你的周报数据和我线下计算的数据无法对起来，存在差异较大；下单、收货、在途周报中的数据需要把你的计算的过程明细列出来，例如你周报中下单行数是187行，你把这187行的明细列出来反馈给我，然后我们线下人工一一核对后进行运算逻辑及取数差异的纠正。」 🔴 **这不是新指标需求，是既有指标的正确性质疑**——`#499` 管的是六项**新增**指标，本行管的是**已上线的下单／收货／在途三节本身对不对**，两者不可混同，也不得互相并入。 **实测（只读，2026-09-10）**：⑴ `4-数字员工/采购部/SC2-采购周报自动生成/sc2/webapp.py` 全文无 `csv`／`export`／`明细`／`detail` 任何一处命中 ⇒ **行级明细导出能力当前为零，须新建**；⑵ 明细底数已有——`sc2/metrics.py:137` 定义 `order_line_count 下单行数`，其行级来源＝`dataset.order_lines`（`metrics.py:215-219` 按 `(po_id, line_no)` 建索引、按 `order_date` 落窗），导出可直接从这里出，不必另起取数。 **并入审核（协议〇.10）**：已用 `工具-队列查询.py --digest --grep` 扫 `周报`／`SC2`／`页面`／`明细`／`对不上` 五组关键词过全池，命中 `#361`（SC2 承接行，已 `[S:done]`、不回填）、`#394`（outbox→aibot 中继，主题不同）、`#499`（六项**新增**指标，见上不可混同）、`#502`（已 `[S:done]` 的冒烟核验）、`#506`（基准日冻结，已修、正在收口，属取值时点不属数据口径），**确无可并入，故取号 #538**；业务行不受机制类可动 WIP 上限约束。 🔴 **本行不含任何对外发送动作**——明细交到他手上属对外交付，须走跟进信审批，本行只管建造与自查。 ⏳ **本行与 `#499` 的先后已登 §四 `#194`** 交 Shao Peishen（默认项 (a)＝本行插队在前），**本行不替总线定**。 ⏩ **建造完成·LAN 留步两项（OP-0910-U，泳道 `538-sc2-detail`，2026-09-10 22:34 本地）**。**187 已复现**：09-08 14:23 `po_cache.json`（ERP 真实订单快照）以基准日 09-09 过同一管线 ⇒ 制单日落 09-07~09-13 共 188 行 − 全程委外 PO17 ×1 − 去重 0 ＝ **187** ✅（全部制单于 09-07，单号 `ZPCG20260907001`~`033` 连号）；交叉验证 W34＝201／W35＝47 与真实快照全等。**明细导出已建**（此前为零）：`sc2/detail.py` 三节×三窗口逐行 CSV，行集判据复用 `metrics.py`，`reconcile()` 明细行数≠周报指标即拒绝导出；数据集快照与周报快照成对落盘；`GET /api/detail`＋`/api/detail/<节>.csv?window=`＋页面 9 链接（缺快照 404、绝不现取）；`run_sc2.py detail --period`；场景 CLAUDE.md D39。**验证**：`pytest -q` 163→**190 passed**；变异四处均转红（9/7/4/1 failed）后 `md5sum` 核还原；端到端 `detail --period 2026-W36` 九格全等、CSV 187 数据行。**产出**：分支 `claude/op0910u-538-sc2-detail` @ `05f60e6` 已 push、**未 ff**（master 已前进 4 笔 docs，未碰 SC2 目录）；取证件 `4-数字员工/采购部/SC2-采购周报自动生成/取证-538-下单行数187复现与明细导出-2026-09-10.md`。**LAN 留步**：① 187 行的确认数量／行状态列本机为「未取到」，须 LAN 内对 W36 跑 `report --mode real` 再 `detail`；② `.51` 未比对、未部署（⏭️）。**边界**：未碰 `#506`；未发送（明细交专员＝对外交付，走跟进信）；openspec 未起包（判深化类）。📎 **完整建造段原文已外置** `1-转型规划/0-全景路线图/队列行日志/#538.md`（K2）。⏳ 与 `#499` 先后仍在 §四 `#194`。

---

## 2026-09-13 20:1x `.51` 部署后验证段（`OP-0911-H`，LAN 收口批 `B-0913_538明细导出部署` 项 1）

> 看护件＝`1-转型规划/0-全景路线图/看护件-LAN收口批B-0913_538明细导出部署-2026-09-13.md`（含 §五／§六 两轮更正，起链前后各一轮）。
> 🔴 `.51` 上的原始件在 `C:\sc2\` 下、且 `reports/` 被 `.gitignore` 排除，**换机即失**，故八段原文与九格逐数抄录于此。

### 执行链与通道（与看护件 §二 原写法有两处不同，均已在 §五／§六 更正）

- **实际链** ＝ 笔记本 `sync-to-server.ps1`（Shao Peishen 2026-09-13 19:32 代跑）→ `.51` `smoke-server.ps1` 0–7 段 → `.51` `run_sc2.py detail`。**未跑 `deploy-server.ps1`**——那是首次部署件（建 venv／注册任务／开防火墙），本次是增量更新。
- **通道**：Cowork 桥的 PowerShell 里 `C:\Windows\System32\OpenSSH\` **整目录不可执行**——`ssh`／`scp`／`ssh-keygen`／`ssh-add` **全部 exit 255、stdout+stderr 共 0 字节**（连 `ssh -V` 也是），而同上下文 `ping`／`git` 正常 ⇒ 不是网络、不是身份，是那个目录被拦。改用 **Git 自带的 `C:\Program Files\Git\usr\bin\ssh.exe`**：`-V` → `OpenSSH_10.2p1`（exit 0），`-o BatchMode=yes … hostname` → `WIN-5CPUJV008C7`（exit 0）⇒ **密钥认证本来就配好**。
- **60 秒硬顶**：桥对单次调用有 60 s 硬上限（tool 侧 timeout 参数无效，实测一次 `Start-Sleep 95` 被截断）。故冒烟不走同步等待，改用 `Invoke-CimMethod -ClassName Win32_Process -MethodName Create` **在 `.51` 上脱钩启动**（`LAUNCH-RET=0`、PID 3940），输出重定向到 `C:\sc2\smoke-A-202609132008.txt`，再只读轮询 ⇒ **任何一次桥超时都不会把结果变成「未知」**（看护件硬边界 ⑦）。

### 快照点（四点齐备）

| 点 | 实测 |
|---|---|
| ⑴ `C:\sc2\app` 两级 SHA256 | 顶层 6 件 ＋ `sc2\` 12 件，全表见下「生产码 SHA256」节；与本机 master `14bbbb8` **逐文件比对 16 件 DIFF=0**（本机该目录 `git status --porcelain` 0 行、`HEAD`＝`origin/master`、两向 0/0） |
| ⑵ 现 `/api/ping` | `200`，`{"mode":"real","ok":true,"service":"SC2 采购周报"}` |
| ⑶ `Sc2WebServer` 进程 | `PID=10224  CreationDate=2026-09-13 19:32:06`；`CmdLine="C:\sc2\.venv\Scripts\python.exe" C:\sc2\app\run_sc2.py serve --mode real --port 8096`；任务 `state=Running  LastResult=267009  LastRun=2026/9/13 19:32:32`。⇒ **进程创建时刻与 sync 脚本回显的 `NEW_PID=10224`／`CREATED=2026-09-13 19:32:06` 逐字相同，真实重启坐实**，非只信脚本打印「已重启」（根 `CLAUDE.md` 坑 5） |
| ⑷ 现网快照备份 | `C:\sc2\snap.bak-202609132008`，**3 件**：`sc2_weekly_2026-W34.json` 17041 B（mtime `2026-09-08 14:20:15`）／`sc2_weekly_2026-W35.json` 17042 B（`2026-09-04 20:01:27`）／`sc2_weekly_2026-W36.json` 17060 B（**`2026-09-09 12:54:47`**，即姚祖怡看页面那天之后就没再动过）。⚠️ **`sc2_dataset_*.json` 一件都没有** ⇒ 明细层此前在 `.51` 上从未落过数据集快照，第八项**必须**排在冒烟第 5 项 refresh 之后，顺序由此坐实 |

### 冒烟 0–7 段（逐段原文判定行，`C:\sc2\smoke-A-202609132008.txt`）

```
=== 0. process identity ===
PID=10224  CreationDate=2026-09-13 19:32:06
CmdLine="C:\sc2\.venv\Scripts\python.exe" C:\sc2\app\run_sc2.py serve --mode real --port 8096
Task state=Running  LastResult=267009  LastRun=2026/9/13 19:32:32
=== 1. /api/ping (gate-exempt) ===
ping HTTP 200  body={"mode":"real","ok":true,"service":"SC2 采购周报"}
=== 2. gate blocks anonymous access to key page ===
anon HTTP 302 (expected 302)
=== 3. login with shared gate password ===
login HTTP 302
=== 4. key page after login ===
page HTTP 200  bytes=7966
page has confirm form: True   has ISO week label: True
=== 5. full real recompute via POST /api/refresh ===
refresh HTTP 200  elapsed=90s  body={"anomalies":["order_qty","avg_qty_per_line","open_line_count","open_qty"],"ok":true,"period":"2026-W36"}
=== 6. key page again (snapshot path, must be fast) ===
page HTTP 200  elapsed=0s  bytes=7588
=== 7. process still alive (no crash during recompute) ===
PID=10224  CreationDate=2026-09-13 19:32:06  (same PID as step 0 = never restarted)
=== SMOKE DONE ===
```

⚠️ 第 2、3 段原始输出里的 `MaximumRedirectExceeded` 红字是脚本 `-MaximumRedirection 0` 的**预期捕获路径**（正是靠它判 302），不是失败——同 `#502` 2026-09-08 的注记。

### 第八项 —— 本批新增判据（已按看护件 §六「更正五」改判）

🔴 **判据不是「等于 187」，是代码保证的同源恒等**：`sc2/detail.py::reconcile()` 三节 × 三窗口九格，「明细计入行数」必须等于周报同名指标，**不等即抛 `DetailMismatch` 并拒绝出文件**。`run_sc2.py detail --period 2026-W36 --out C:\sc2\smoke8-202609132008` **退 0**：

| 节·窗口 | 明细计入 | 周报指标 | CSV 数据行 |
|---|---|---|---|
| 下单·本周 | 187 | 「下单行数」187 | 187 |
| 下单·上周 | 47 | 47 | 47 |
| 下单·上月同期 | 519 | 519 | 519 |
| 收货·本周 | 58 | 「收货行数」58 | 58 |
| 收货·上周 | 344 | 344 | 344 |
| 收货·上月同期 | 171 | 171 | 171 |
| 在途·本周 | 175 | 「未清行数」175 | 187（另列 12 行被剔除、附剔除原因） |
| 在途·上周 | 21 | 21 | 47（另列 26 行剔除） |
| 在途·上月同期 | 412 | 412 | 519（另列 107 行剔除） |

回显尾行：`[对账] 期次 2026-W36｜基准日 2026-09-13｜取数时刻 2026-09-13T20:10:12+08:00｜三节×三窗口 9 格全等`

🔑 **交叉核（不只信脚本自己的 ✅）**：在途节 CSV 行数恒等于同窗口下单行数——187 ＝ 175+12、47 ＝ 21+26、519 ＝ 412+107，**三处全部自洽**。

### 两件当场坐实的实质结论

**⑴ 今天重取，本周下单行数仍然正好 187，而且是同一批 187 —— 我自己的预判被实测推翻。**
我在看护件 §六「更正五」里预判「09-13 是 W36 窗口最后一天，09-08~09-13 新制单会进来，**大于 187 才正常**」。实测（`base_date=2026-09-13`、`fetched_at=2026-09-13T20:10:12+08:00`、`mode=real`）逐项与 `4-数字员工/采购部/SC2-采购周报自动生成/取证-538-下单行数187复现与明细导出-2026-09-10.md` §一 记的形状**完全一致**：

- 制单日期：**187 行全部 `2026-09-07`**（无一行落在 09-08~09-13）
- 采购单号：**33 张、`ZPCG20260907001` ~ `ZPCG20260907033` 连号**
- 单据类型：**PO22 ×185、PO23 ×1、PO01 ×1**
- **101 个料号、25 家供应商、5 位采购员**

⇒ **09-08~09-13 五个工作日，本周窗口内零行新下单。** 这与「周一集中下单」的作业形态自洽，但**属业务事实，须姚祖怡确认，本方不替他判**。🔑 对他的价值：取证件 §一 给的那句核对锚点（「ERP 里 `ZPCG20260907001`–`033` 这 33 张单、去掉 1 行全程委外后的全部订单行」）**在整周走完后依然成立**，他线下点数不必担心时点漂移。

**⑵ `#538` 的另一半（「LAN 留步两项 ＝ Purchase/Query 三列」）也解了。**
取证件 §六 第 1 条记：「187 行的确认数量／行状态／未清数量三列在本机复现件里全为『未取到』……要交给他的完整明细须在 LAN 内对 `2026-W36` 跑一次真实取数再 `detail`」。本次正是在 `.51` 上先 `POST /api/refresh`（`--mode real`、服务入口状态取数缺省不限）再 `detail`，导出 **14 列 × 187 行，逐列缺失计数全 0**：

`采购单号｜行号｜制单日期｜单据类型｜料号｜供应商编码｜供应商名称｜采购员｜确认数量｜含税单价｜下单金额｜累计收货｜行状态｜未清数量`

- 行状态分布：**`0 开立` 4／`2 已审核未交清` 171／`3 自然关闭` 2／`4 短缺关闭` 10**；关闭类 2+10 ＝ **12**，恰为在途节剔除的 12 行 ⇒ 又一处自洽。
- 首行样例：`ZPCG20260907001｜行 10｜2026-09-07｜PO22｜R01B.0057｜ZA0017 上海富达实瑞电子有限公司｜沈潇敏｜确认数量 16000｜含税单价 0.03｜下单金额 552｜累计收货 0｜行状态 2 已审核未交清｜未清数量 16000`

### 生产码 SHA256（`.51` 现网；与本机 master `14bbbb8` 逐文件 DIFF=0）

```
CA3AAB15A4AC6E86073FB8E7FC93B2C084A76F8C36C7435D9E207F09E6A8B89A  autopush-sc2.ps1
3F0020AE4840EFB1D804FE4C67EFBB3D6C30707BDF3791F2DF9548CBF2134683  deploy-server.ps1
657CC9DBFAE0DF7A1A377F0D08985104E3C2E44AEC98B5C96A5EBA0B3BF97FF2  pyproject.toml
82CF4FA5F38CDD8FB0B0AC366E68FC2F7D50CAB0EC598F9FE29ACD2B09FA15F0  run_sc2.py
A25ECD6CC6072634AA7B8AC52458AB9D7D660211FE908B2E58FC747C8E788F8F  smoke-server.ps1
0E78EE5261BABB6FEF233854BDC5EDDC1468EECD4F40EE4E73C71110532ACFBA  start-sc2.ps1
159199D74311D7DDE29876CB76503DF55BB48DF5B3AA1ECE1D584D6CC7238749  sc2\config.py
0908D69C0FD431AC06F2C3D975043AAAC59EE5F8CBF20B71D4CEC14C38707007  sc2\detail.py
8830255D4D14581595B2B491102097FB7E9145B4A72C50F5E8D11B28C6FBDB7D  sc2\metrics.py
D29C00E24ECD1E0041302BD4845D46073D2FCA2354176F5243B3B6E0358B571D  sc2\models.py
D530EDEB5908939D070AEEE98E2A0BCD8949F37399750BE9046FFD0623B91120  sc2\notify.py
5836E2A9A4F81CB8FC72C4A4F44DBE82990A5227D730A93EFCC65CAFF9D00DD3  sc2\outbox.py
84E8B65756C0610273CC3E66D0DA22BCB74601A680B04FBC6FB9B111FAB79BEA  sc2\report.py
74C3E261F53713EBBA13C24E3F4011AEDE1731B8167C8ED483162E871430832B  sc2\review.py
C570FFFA1AA90132690EEB3956241AEE5D0D0904E0C90119A676CD81E275376B  sc2\sources.py
47C4FF679B05B58288CD6A7D677765DA553C42262F465381883AE5680476C710  sc2\webapp.py
274F599BDDA17580F29B22EED61665558FC3B3AE03A93398C5DA616D143D90C2  sc2\windows.py
D7F06B196D244BA0DE54EFF326B91CB9517D8A258C49E77DDD75C0DBE1A91930  sc2\__init__.py
```

⚠️ `autopush-sc2.ps1`／`start-sc2.ps1` 仓库内无对应源件、无版本追溯（`#361` 2026-09-07 已报，本次复现，**只报不改**），故不在 16 件比对集内。

### 本轮对 `.51` 的写动作（仅此三条，如实声明）

1. 冒烟第 5 项 `POST /api/refresh` 是真实全量重算 ⇒ **覆盖了** `C:\sc2\app\reports\sc2_weekly_2026-W36.json`（mtime `2026-09-09 12:54:47` → `2026-09-13 20:10:12`，17060 B → 17063 B）并**新建** `sc2_dataset_2026-W36.json`；改前件已在 `C:\sc2\snap.bak-202609132008`。
2. 第八项 `detail --out C:\sc2\smoke8-202609132008` 写入**一次性目录**（9 个 CSV），**不落 `reports/`**。
3. **无部署、未跑 `deploy-server.ps1`、未改计划任务／防火墙／配置。**

🔴 `C:\sc2\snap.bak-202609132008` 与 `C:\sc2\smoke8-202609132008` **保留到明细交付信（`采购部#23` 一线）发出后再清**。

---

# §一 #538 · 外置段（2026-09-13 外置，工具第 1 批，2 段，原文原样）

> 外置人 Cowork-业务总线-OP-0911-H；外置前整格 4053 B、共 5 段；本批外置第 2、4 段；本格保留第 1、5 段。

🟩 **代码侧已进 master（2026-09-11 05:3x，`OP-0910-A`；Shao Peishen 答 `2a`）**：`05f60e6` → rebase → **`0aef9e2`**，master `945435b`→`0aef9e2`（6 文件：`sc2/detail.py` 新建、`sc2/webapp.py`／`run_sc2.py`／`tests/test_detail.py`／`CLAUDE.md`／取证件）。**回归**：`4-数字员工/采购部/SC2-采购周报自动生成/tests` **190 passed**（4.70 s）在 rebase 后的 worktree 里现跑。⇒ 🔴 **本行仍 `partial`，不得销号**：⏭️ **转出项未做**——明细导出要部署 `.51` 并与线上 `sc2_weekly_2026-W36.json` 的 187 比对，已按 D1 ⏭️ 档转 `zhuopin-lan-closeout`；另有 LAN 留步两项（Purchase/Query 三列）。🔴 **行内此前「已 push、未 ff」已过时。** 备份 ref `backup/op0910u-538-sc2-detail-pre-rebase`＝`05f60e6`。**手段（四条同一套，逐条现取）**：备份 ref → 现取该分支唯一提交触碰面并与主 checkout 11 个脏文件逐条比对（**四条全部零交集**）→ 因 master 已前进、四条**均不能 `--ff-only`**（`master...分支` 各 11/1、11/1、11/1、7/1），故各起临时 worktree `git rebase master`，**四条全部零冲突** → 有单测的先在 worktree 里跑绿再 ff → 主 checkout `merge --ff-only` → `push origin master` ＋ `push --force-with-lease` 分支 → **四 ref 逐条核对一致** → 临时 worktree 清除。串行做，每条 ff 完下一条再 rebase 到新 master。

━━━

🆕 **已升为采购域建造首位（2026-09-11 19:4x，Shao Peishen 答 §四 `#194` 的 `4a`）**：本行插队排在 `#499` 之前，先交「187 行明细透出 ＋ 差异定位」。⚠️ 已实测 `sc2/webapp.py` **无任何导出／明细能力** ⇒ 须先建，不是配置开关。⏭️ `.51` 部署与线上比对仍留 LAN 收口批（本行 `partial` 的那一半不因本次排序改变）。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 3 段）：

📎 **1 段已于 2026-09-10 外置**（K2 口径，原文原样、可 grep，md5:f90fa167）见 `1-转型规划/0-全景路线图/队列行日志/#538.md`；本格只留首段与最新收口段。
