---
title: "队列行日志 #133（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §四 #133 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §四 #133 · 外置前整格原文（2026-09-04 外置，11 段）

🔴 **`python-docx` 读不到 Word 复选框里的勾——一条读取假阴性已经导致一封信向专员做出不实指摘（2026-08-28 拆件巡逻定时班实测坐实，非推断）**：**事实链**：⑴ `采购部#18` 回件（08-26）三条判例，`docx.Document(...).tables[1]` 逐格 `cell.text` **全为空串**；⑵ 同一文件 `word/document.xml` 原文里 `☒` 3 个、`☐` 6 个，**三行第一格（✅ 这样对）全部为 `☒`**；⑶ 形态＝ `w:p` 内的**内联内容控件**：`<w:sdt><w:sdtPr><w14:checkbox><w14:checked w14:val="1"/>…<w:sdtContent><w:r><w:t>☒</w:t>`，而 `python-docx` 的 `Paragraph.text` 只拼 `w:p` 的**直接** `w:r` 子节点 ⇒ 嵌在 `w:sdt/w:sdtContent` 里的 run 被整段跳过。**权威判据＝ `w14:checked w14:val="1"`，不是 `cell.text`。** **已造成的后果**：§一 #411 据此定性「9 个勾选格一格未勾」→ §四 #122 → `采购部#19` 第一节原样重送三条判例并写「我们把 Word 文件逐格读过，9 个格子确实都没有内容」——**这句话把责任指向了他**；他 08-28 回件原话「我确实已经勾选了……**你是没有看到吗？**」。信已发出、撤不回。🔑 **判据（比修法更值得记）：「逐格实读」这四个字本身不构成证据，要问的是「读的是哪一层」** —— 同族＝ CLAUDE.md §5「工具静默回退」与 §一 #416 那条「答案 60 毫秒就到了、只是打在没人看的 WARN 里」，**共同点是错误不产生任何信号**。⚠️ **本班已就地止损**：#428 的四个勾选格改用 XML 直读判定，故本次未二次误判。　━━━　**三件事请你各定一句**　━━━　**⑴ 修法** —— **(a)** 在拆件侧建一个统一的 docx 读取小工具（同时取 `w:t` 与 `w:sdt` 内容，`w14:checked` 作勾选权威判据），此后拆件一律经它读，并把拆件章程二.2 补一句「读到空也可能是假的」；代价＝需立 §一 机制行、约半天。**(b)** 只改拆件章程二.2 写清判据、不建工具，靠每班自觉用 XML 直读；代价＝零成本，但这正是「人守」形态，按规则退休制迟早会第 3 次失效。**(c)** 暂不动；代价＝下一次专员回件仍可能被读成「没勾」，而这类误判**不产生任何报错信号**。**建议 (a)。****⑵ 是否向姚祖怡更正一句** —— **(a)** 下一封信开头用一两句如实更正（「上封说你没勾是我方读取工具的问题，你确实勾了」）；代价＝占他几十秒，收益＝把责任放回我方。**(b)** 不单独更正，只在下次自然提及时带过；代价＝他那句「你是没有看到吗」得不到回应。**建议 (a)**——本项目已有先例（财务部#14 代词事故）证明**不更正比更正更伤**。**⑶ §一 #411 那条已 `[S:done]` 的错误结论怎么处理** —— **(a)** 不追改历史行，只在 #428 与本行留更正指针（本班已按此执行）；**(b)** 回头在 #411 行内追加一句更正标记。**建议 (a)**，守「历史记录不追改」。🔴 **本项无默认**（守 CLAUDE.md §5 默认项两前提）：⑴ 需另起 session 执行、⑵ 属对外发送（拆件章程 §〇 红线），提出方本班收工即不能自我唤醒 ⇒ 不答＝同类误判保持可复发，且他那句质问悬着。📌 **机制行未自行升格的理由如实登记**：机制类可动 WIP 实测已超上限 22，按拆件章程三.2「要升格的是机制行且已达上限 ⇒ 不自行处置、登 §四」；亦未并入 §一 #416（该行触碰区＝ `5-平台底座/wecom-aibot-service/`，本缺陷在拆件读取侧，并入会 misfile）。  　━━━　🔴 **2026-08-28 15:4x 本条定性被实测推翻，修法前必须先读本段（Cowork `OP-0828-N`，对照实验）**：本行原写「`python-docx` 读不到 Word 复选框里的勾」——**不成立**。 **实验**：拿 `0-学习与工具/md转Word工具/md2word.py::read_checkboxes()` 与 `zipfile` 直读 `word/document.xml` 正则计数，对采购部近四封真实回件逐一对跑，**两种读法结果逐字一致**：`采购部#18` 回件（`…回复-2026-08-26-…物料看板已上线请试用与三条读数口径确认…docx`）＝ **9 个控件、3 个 `w14:checked val="1"`**，`read_checkboxes()` 同样返回 9 个、3 个 `checked=True`；另三封（08-20 补件 9/3、08-21 周报 24/8、`采购部#19` 回件 16/6）亦全部一致。 ⇒ **`read_checkboxes()` 用的是 `doc.element.body.iter(qn('w:sdt'))`，它会走整棵 body 树、含表格单元格，不存在「表格里的勾读不到」这个盲区。** ━━━ 🔑 **⇒ 真因不在库层，在流程层：不是「读不到」，是「没人去调这个函数」，或调了而结果没被采信。** **修本条前必须先查清那一步**（`采购部#19` 信里那句「我们把 Word 文件逐格读过，9 个格子确实都没有内容」究竟是用什么读法得出的），🔴 **否则会去修一个没坏的东西，而真正那一步原封不动**。 ━━━ ⚠️ **姚祖怡的申诉完全属实**：他 08-26 那封确实勾了三个，工具也读得到 ⇒ `采购部#19` 整封信的前提是错的，我方据此对他做了不实指摘，下一封须致歉。 ━━━ 📌 **同日第二例、同一形态、犯在我方自己身上**：Cowork 拆 `采购部#19` 时用的提取脚本只遍历含复选框的**表格行**（`<w:tr>`），因而漏掉了**段落内**的两个勾——`☒ 认可 —— 替代料按修复后的算法继续跑`（第 5 件黄金基准）与 `☒ 认可 —— 按规则 1 改后的算法开起来`（第 6 件规则 1），初版拆件据此写成「第 5、6 件全文未见答复」，**错的；他六件全答了**。 🔑 **判据（本条真正该留下的）：「他没答」这个结论，永远只能是「我这个读法没读到」的下位** —— 断言专员未作答之前，须先证明读法覆盖了全部可能的作答位置（表格内／段落内／高亮段／批注／修订）。**本项目已在这一点上连栽三次（采购部#16、#17 勾选找错地方；#18 假阴性；本次 Cowork 漏段落内勾），且每一次代价都是对专员做出不实指摘。** 　━━━　🔒 **Shao Peishen 2026-08-31 答《本周计划-2026-08-31》B-3（只答 ⑵，⑴⑶ 明示仍待他答）** ━━━ **⑵ 答「更正」** —— 他原话「尽快对姚祖怡做出更正——越晚说越像补台阶，**这条是四条里唯一有对外信誉成本的**」。 🔴 **但值周巡检当场核实：该更正已经发出去了，本项就此闭环，不再发第二封。** **证据（读的是信件真身，不是队列自述）**：`6-人才与组织/部门AI专员跟进/采购部-姚祖怡-跟进-2026-08-28-读取缺陷致歉与判例4真实案例.md` —— 标题即「上封那句话是我们错了」，§〇 首节原文「**是我们没看到，而且是我们的问题，不是你的**」，并列出两种读法各跑一遍的对照表（工具函数 9/3、直拆 Word 9/3）；该信＝ `采购部#20`，**2026-08-28 09:29 UTC 已推送**（跟进信 README 发送状态列），四条链路全绿。 ⇒ **与 ⑵(a) 要求逐条对应：开头如实更正、把责任放回我方、不拖到自然提及**。 🔑 **为什么必须在这里停手，而不是照指令再发一封**：他今天刚为「催了一件已经交过的东西」立下判据（§四 `#91` ⑶ 的同族形态），**若此刻再发一封更正，就是在同一天把那条判据原样犯一遍**；且姚祖怡串行闸实测 🔒 锁（`采购部#20` 在途），新信本就发不出去。 ⚠️ **留一个口子**：若他要的是**在 `采购部#20` 之外再单独更正一次**（例如认为首节的份量不够），**须他再说一句**——本行不代判、不默认执行。 ━━━ 🔴 **⑴（修法归属与是否豁免 WIP 立行）与 ⑶（`#411` 怎么办）仍待他答**，他明示「不要当作已解决，请在下次交互重提」。**本项无默认**（原判据不变）。 ━━━ ✅ **2026-09-03 Shao Peishen 裁决（§四 分诊消化，Cowork 业务总线 `OP-0903-D`；他两批均答「都按你说的」）**： **⑴ 建统一 docx 读取工具，但立行标 🛑 排队、不豁免 WIP；⑶ 在 §一 `#411` 行内标注更正、不销号重开。** **⑴ 理由**：已连栽三次（`采购部#16`／`#17` 勾选找错地方、`#18` 假阴性、Cowork 漏段落内勾），rule-of-three 已触发；**但更正信 08-28 已发，不在出血，可以排队**。**⑶ 理由**：照「历史记录不追改」，错误结论当场标注更正即可，重开会制造第二份真相。
