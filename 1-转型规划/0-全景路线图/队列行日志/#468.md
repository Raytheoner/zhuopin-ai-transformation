---
title: "队列行日志 #468（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #468 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #468 · 外置前整格原文（2026-09-04 外置，20 段）

[S:open][D:业] 🆕 **2026-09-03 立行，未开工。** **立行依据**＝队列 §一 `#463`（14 个「三处皆无」场景的立行与排期）＋ Shao Peishen 2026-09-02 判据原话「**不管规划时间表、能开尽开；唯一门槛是前置依赖**」（判据源＝《跨场景前置数据与知识库任务总表》），及其当日当场收窄指令「**先做质量财务采购域场景，研发和销售域场景依然暂缓等我通知再排序**」。 ━━━ 📅 **权威排期表上的原排期＝2027-03**（§加速启动总览采购列）。 ━━━ 🔴 **前置依赖及满足情况＝未满足；性质是「窗口未到 ＋ 依赖外部方」，不是「已滑」**：前置总表 §一 `SC10` 行写明前置＝「原厂/第三方贸易网站 API 选型 ＋ 我司价格库/物料属性数据整备」，Owner＝姚祖怡＋IT，整理启动 **2027-01**（数据型前 6 周＋缓冲）、完成截止 **2027-02**，状态格为 ⚪「新增行」⇒ **未开工，且原定窗口本来就还没到**。两项卡点分属**外部方**（第三方贸易网站 API 选型与签约）与 **IT/内部数据整备**（价格库、物料属性），本线无法自行解除。 ━━━ ✅ **照样立行（他的判据＝能开尽开）**：**不依赖前置即可开工的部分**＝BOM 评审判据与「物料优先选用级别」口径的 mock 建模，以及只用现有 ERP 物料主数据跑通档1；**须等前置的部分**＝第三方价格/参数/封装 API 的真实源接入与物料属性（Active/NRND/…）字段整备。 ━━━ ⚠️ **本行不改 2027-03 排期**——按他的判据，立行与排期时间表是两件事：现在可开工不等于排期提前。  ━━━ 🔨 **2026-09-03 开工（CC `OP-0903-B2`，批次 `B-0903_22`，泳道 `proc-scenarios`）：骨架期完成、建议层留步，本行不销号。** 🔴 **前置复核（逐行实读）**：前置总表 §一:21 `SC10` 行状态格 ⚪「新增行」，整理启动 **2027-01**／完成截止 **2027-02** ⇒ **未开工，且窗口本来就还没到——是「窗口未到」不是「已滑」**，与本行原文一致。⚠️ 这个区别已写进代码（`pending.py` 状态文案明写「窗口未到，非逾期」并加测试断言）：「未开工」和「已逾期」对承接方意味着完全不同的动作，前者等窗口、后者要追责。 ━━━ ✅ **已交付（不依赖外部 API 与选用口径的事实层）**：BOM 展开**走底座 `kit_engine.explode_bom`**（与 O2／SC7／SC8 共用，未重写）；跨机型共用料识别（**逐成品单独展开再合并**——`explode_bom` 返回的是合计毛需求、丢掉了「需求来自哪个成品」，而共用料判定要的正是这条来源信息）；物料主数据对齐；**数据完备度体检**（BOM 内物料数／生命周期未知数／无价数／主数据缺失数）；L2 audit 留痕。**18 tests 全绿。** ━━━ 📌 **体检表是本次真正的价值所在**：SC10 三项产出全是「建议」，而建议需要口径与外部数据；在 2027-01 窗口开启前最该被回答的不是「建议怎么算」，而是**「离能算还差多少数据」**——这个问题现在就能用现有 ERP 主数据回答，且答案直接决定那轮数据整备该整备什么（已列入变更包 tasks 3.3：用真实主数据跑一次体检、产出清单交姚祖怡）。 ━━━ 🔴 **建议层三项全部 fail-loud 留步**：`suggest_bom_review`／`suggest_selection_level`／`suggest_obsolescence` 调用即抛并点名对应前置；外部行情源只留 Protocol ＋ `UnselectedCatalogSource` 占位实现（**先接一家等于替姚祖怡把选型做了**，而选型结果会改字段口径／配额／缓存策略）。 ━━━ 🔴 **本次查出判据源缺口一处（已随 pause 登记待裁决，本泳道不擅改规划文档）**：前置总表 `SC10` 行两项前置**皆为数据型**，而规划要求产出「物料**优先选用级别**建议」与「淘汰建议」——**数据整备产出的是属性值，不是排序规则**：Active／NRND／New Product／Obsolete 四个属性到手之后，「NRND 的物料能不能进新 BOM」「已在产机型上的 Obsolete 料何时必须替换」**仍然没有答案**。⇒ 这是一项**知识型**前置，总表无其行；本场景据实单列为第三项 `selection_ranking_criteria` 并挡住相关能力。⚠️ **它按 8 周倒排须与两项数据型前置并行安排、不可串行**（若维持 2027-03 启动，批改会最迟 2027-01 开，与数据型同窗口）——这是拆出它的实际用途，不是记账。补总表行属规划文档改动，同族处置参照 `#470`–`#474`。 ━━━ 📌 **一处实测**：`LifecycleStatus` 若按本仓库惯用写法混入 `str`，`ACTIVE < NRND` 会**静默按字母序成立**——一个看似能用、实际毫无业务含义的排序，正是「靠枚举顺序偷偷造判据」的入口。已改为纯 `Enum` 并加测试硬拦。 ━━━ ⚠️ **本行不改 2027-03 排期**（立行与排期是两件事，本行原文已写明）；openspec `sc10-bom-review-material-governance` 已起草 proposal＋tasks＋specs，**design 审 🟡 档、已 pause 留步、未自行通过**。 ━━━ 🔴 **本行不销号**：三项建议未交付，SC10 **不构成场景上线**、不进覆盖轨计数。 ━━━ **本行做到哪一步／卡在哪**：第 1 步工程骨架完成、第 2 步只到 proposal＋tasks 起草；第 3 步 design 审 🟡 已留步。**卡在**：外部 API 选型／物料属性数据整备（窗口 2027-01 未到）＋ 选用级别口径（总表无此行）。 ━━━ **触碰区（实际）**：`4-数字员工/采购部/SC10-BOM评审与物料库管控/` ＋ `openspec/changes/sc10-bom-review-material-governance/`（proposal＋tasks＋specs 增量，**design.md 未写**）。分支 `claude/op0903b2-proc-scenarios`（worktree `.claude/worktrees/proc-scenarios`），commit `04611c3` 已推 origin，**刻意未合 master**（`merge_to_master` 🟡 档，且 design 审未过）。批次登记见 §二 `B-0903_22_OP0903B2_采购三场景骨架_SC4_SC10_SC11`。  ━━━ 🔴 **2026-09-03 续（Cowork 业务总线 `OP-0903-C1`，批次 `B-0903_33`，泳道 `proc-apply`）：本包已跑 `/opsx:sync`，增量 spec 已并入主 specs；`FF-1=(a)` 放行进 apply 的那一步到此为止，未进实现。** 落点 `openspec/specs/sc10-bom-review-facts/spec.md`（🆕 新建，6 requirement／14 scenario，Purpose 取 delta 原文逐字）。🔴 跑的是 `sync` 不是 `apply`（判法同机制队列 `#299`）。反查：`openspec validate --all --strict` ＝ **132 passed／0 failed**；主 spec 内 delta 操作头计数 0。 ━━━ 🔴 **归档判定 ＝ 不归档**：tasks **9/29**，第 2 组前置到位闸整组未满足（`design.md` 未写且 design 审未过；三项业务前置窗口在 **2027-01～02**）。已按「完工即归档纪律」三条入口写入文本标记 `暂不归档`（`工具-落库sweep.py::_change_package_has_defer_marker` 实测 `True`）——属「作者明知长期不会归档」，故用永久标记而非观察窗口。 ━━━ ✅ **上批本行登记的「物料优先选用级别口径缺行、待裁决」已有裁决**：Shao Peishen 2026-09-03 **`EE-5=(a)` 补进《跨场景前置数据与知识库任务总表》**（落字件＝看护件 `B-0903_30` §三 A1）。🔴 **属规划文档改动 ⇒ 已并进既有那份移交单**（`移交单-SC4顺延与前置总表补5行-…-2026-09-03.md` 新增 §2.4 取证 ＋ §3.3bis 落法），**本泳道不得也未直接改总表**。移交单里已给承接方可直接取用的三项：**启动列 ＝ 2027-03**（权威排期表 `卓品智能AI转型全景规划.md:953`）；**本线判其为知识型**（理由：数据整备产出的是属性值 `Active/NRND/New Product/Obsolete`，不产出排序规则——四个属性到手后「NRND 能不能进新 BOM」仍无答案）；⇒ 按 8 周倒推**最迟开工日 2027-01-04**，🔴 **须与既有两项数据型前置（窗口 2027-01 起）并行安排、不可串行**，否则纪律当场失守——**这正是把它单独拆出来的实际用途**。⚠️ **打标本身仍须业务侧签认**（判据/口径类不默认生效），Owner 亦未代定，已列为移交单 §四 待定项 6／3。本包 `tasks.md` 2.2 已同步回写、**仍保持未勾**（裁决已下 ≠ 补行已落地）。 ━━━ 📌 **补行时会立刻撞上的一处同族缺口（本次逐行核出，已写进移交单 §3.3bis）**：前置总表类型打标行 `跨场景前置数据与知识库任务总表.md:36` **完全没有 `SC10`**（grep 零命中）⇒ 新补的行没有可依的类型，须同批补上打标。  ━━━ ✅ **2026-09-03 已合入 master**（CC 泳道 `criteria-chain` A1 段，`OP-0903-D1`，批次 `B-0903_50`，§二 登记见 `B-0903_51_OP0903D1_两链合master_采购三包链_SC8_ATP批2收口链`）。本行相关的两个提交经 `git rebase master claude/op0903c1-proc-apply` → `git merge --ff-only`（守根 `CLAUDE.md` §5 ff-only）合入并 `git push origin master`，master 由 `9b78281` 推进到 `209fde4`，反查 `git rev-list --count master..claude/op0903c1-proc-apply` ＝ **0**、`origin/master..master` ＝ **0**。🔴 **合入依据**：`merge_to_master` 本属 🟡 档，本次不 pause 的唯一依据是 Shao Peishen 2026-09-03 答 **G-6=(a)「合入照原意做，含连带」**（他已被告知本链连带 1 个基分支提交 `feat(采购三场景): SC4/SC10/SC11 工程骨架…` 后作答）。合入前按 commit **message** 逐条比对（🔴 不认 hash——rebase 后必变）＝ 2 个、与看护件 `B-0903_50` §四 D1 清单全等，无表外提交；rebase **零冲突**（2/2）。合后 `openspec validate --all --strict` ＝ **134 passed ／ 0 failed**（⚠️ 该链在自己分支上测得的 132 已失效，合并后总数必变，勿再引用旧数）。🔴 **本次只做合入，不改变本行任何既有结论**：design 审仍未过、tasks 未全 `[x]`、变更包按「暂不归档」标记留在 `openspec/changes/` 内，未进实现、未填任何未签认判据。分支 `claude/op0903c1-proc-apply` 按 opener **未删除**，保留追溯。
