---
title: "队列行日志 #473（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #473 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #473 · 外置前整格原文（2026-09-04 外置，21 段）

[S:open][D:业] 🆕 **2026-09-03 立行，未开工。** **立行依据**＝队列 §一 `#463`（14 个「三处皆无」场景的立行与排期）＋ Shao Peishen 2026-09-02 判据原话「**不管规划时间表、能开尽开；唯一门槛是前置依赖**」（判据源＝《跨场景前置数据与知识库任务总表》），及其当日当场收窄指令「**先做质量财务采购域场景，研发和销售域场景依然暂缓等我通知再排序**」。 ━━━ 📅 **权威排期表上的原排期＝2027-06**（§加速启动总览财务列「FI9 研发费用归集、FI10 存货跌价 启动；年度总结」，落 Phase 1 收官月）。 ━━━ 🔴 **前置依赖及满足情况＝判据源本身缺行，无从判「已满足」**：本次逐行实测，《跨场景前置数据与知识库任务总表》§一 与 §一.2 **均无 `FI9` 的对应条目**（该表财务域现存行只有 `FI1`／`FI2`／`FI3` 三条）⇒ 按他指定的判据源，`FI9` 的前置**既非「已满足」也非「未满足」，而是从未被排过期**。**⚠️ 这是判据源的缺口、不是本行的结论**——补前置行属规划文档改动（全景路线图线），本行不擅改、不代判，已一并写进 `#463` 回写与本批报告的待定夺项。 ━━━ 📌 **改从场景块自陈的数据依赖读，四项里有两项在本项目内从未出现过**：U9C 项目成本模块 ／ **工时系统** ／ **高新认定政策库** ／ 企业会计政策文档。**「工时系统」在本仓库全库无任何既有指针**（是否存在、由谁维护、能否取数，未经核实——承接方开工第一件事应是核实它是否存在，而不是假设它存在）；**「高新认定政策库」与「企业会计政策文档」属知识型资产，须财务侧产出并签认**（🔴 资本化/费用化判据直接影响对外申报，判据类永不默认生效）。 ━━━ ⚠️ **合规注**：本场景产出用于**政府申报与高新认定**，属对外材料；按 §7-4 口径，其结论一律须人工确认后方可使用，AI 不得自动出具对外文件。 ━━━ ✅ **照样立行**：可开工部分＝资本化/费用化规则引擎骨架与辅助账模板的 mock 建模。  ━━━ 🔨 **2026-09-03 开工（CC `OP-0903-B3`，批次 `B-0903_20`，泳道 `fin-scenarios`，commit `47d76f2`）：骨架期完成、判据与数据源存在性两处留步，本行不销号。** ✅ **已交付**：工程骨架 `4-数字员工/财务部/FI9-研发费用归集与高新认定/`（`pyproject.toml` ＋ `fi9_rd_cost/{__init__,config,models}.py` ＋ `tests/{conftest,test_scaffold}.py` ＋ `data/mock/` 三张合成 CSV），**12 tests 全绿**；openspec 变更包 `fi9-rd-cost-mvp`（proposal ＋ 3 份 specs delta ＋ tasks，`openspec validate` 通过）。🔴 **未写 `design.md`、未进 apply**（🟡，须 Shao Peishen 拍板）。 ━━━ 🔴 **本行的合规注已被落成代码、而不只是写在文档里**（本场景与其余四个财务场景最大的差别）：产出用于政府申报与高新认定 ⇒ 按 §7-4「结论须人工确认后方可使用，AI 不得自动出具对外文件」，本包落了三道：**⑴** `CapitalizationVerdict.is_external_ready` **恒为 `False` 且系统中不存在把它置真的合法路径**——对外可用与否由人决定，不由数据结构声明；**⑵** `AuxLedgerRow.disclaimer` **无默认值（必填）**——给它默认值就等于允许有人忘了写；**⑶** `config.EXTERNAL_FILING_GATE` 明写「MUST NOT 具备『一键生成即可提交』的路径」。三道各有用例守。 ━━━ 🔴 **本行「工时系统全库无既有指针」的判断，本泳道复核确认成立，并给一处更精确的表述**：`工时` 二字在**4 份规划文档**里只作为**依赖名**出现（全景规划／本队列／财务域两份场景描述）；**代码侧唯一一处**是 `5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/models.py:126` 的注释「用于 SMT 工时查询」——**那是 `product_id` 字段的用途说明，不是工时系统的连接器**。⇒ 措辞宜改为「仅作依赖名出现于规划文档，代码侧无任何连接器」。已落成 `config.TIMESHEET_SYSTEM_EXISTS = None` ＋ **独立用例**（**存在性**未核实，性质不同于「还没接」）；**人工费用归集在此之前一律 fail-loud，不得以任何分摊估算代替**——分摊出来的人工费会原样写进加计扣除备查资料，而它看起来和真的一模一样。 ━━━ 🔴 **判据留步（三项 ＋ 一项）**：资本化/费用化判据／高新认定政策库／研发费用占比口径，另加 `LaborRecord.rate` 工时单价口径，全部落 `None` ＋ CI 守。⚠️ **占比口径特别提醒**：**会计准则口径与高新认定口径的分子分母并不一致，混用即出错且结果看起来完全正常** ⇒ specs 已要求两套口径**分别**计算。另：`RdProject.is_high_tech_scope` 默认 `None`（**未判**）而非 `False`（**判了不纳入**）——这个区别在申报口径里是实打实的。⇒ **本包不宣称档 1**（规则引擎骨架可建，但规则的内容就是那三项未签认判据；辅助账模板的**格式**又恰恰由未签认的高新政策库定）；**mock 夹具刻意不打「标准答案」判定**。 ━━━ 🔴 **两处易漏、已单列为 design 停审点，本泳道不代判**：**⑴ OEM 隔离待判** —— 研发项目往往对应特定 OEM 项目，若辅助账或投入产出分析会带出 OEM 项目标识，则触及 §7-3「按客户路由、禁跨库」，**须接 `data_isolation_layer`**。这一条**极容易被「财务数据不隔离」一句话带过去而漏掉**，故单列。**⑵ 备查资料包的载体与版本策略** —— 后续会生成加计扣除备查资料包（很可能是新目录形态），**入版本库＝申报材料进 git 历史；不入库＝无版本可追溯、与 IATF 可追溯要求冲突**。本包尚未产生该形态、**故无从实测其 `.gitignore` 覆盖**，如实写明、不留空猜。 ━━━ ⏸️ **卡在哪**：工时系统存在性核实（**开工第一件事**）；四项判据须财务 ＋ **研发侧**主笔实名签认（⚠️ 需唐燕萍确认者 ⇒ 唐燕萍串行闸在途 —— 🔴 **状态勿在此复述**（会过时），现取：`python 0-学习与工具/工具-跟进闸查询.py --to 唐燕萍`，已登记「待闸开后并进下一封」，**本泳道未起草任何新信**）。 ━━━ 📌 跨五场景 design 停审点同 `#470` 末段。 ━━━ ⏸ **2026-09-03 A4 段（CC `OP-0903-C4`，批次 `B-0903_36`，泳道 `fin-criteria`）开工即留步，本行零改动、不销号。** A4 原定动作＝把本包的**本地判据签认实现**改为引用平台底座公共模块 `criteria_signoff`（A3 段已建成，见批次 `B-0903_31`／分支 `claude/op0903c3-criteria-signoff`）。🔴 **实际未做**：开工前置检查 `工具-泳道看护状态机.py show --lane fin-criteria` ⇒ 泳道仍 **`paused`**，停在「③ openspec design 审」，等 `criteria-signoff-platform` 的五条定夺项（①占位词黑名单去留 ②三类「1 次」缺口收不收进底座＋②′ `FI10` `SLOW_MOVING_CRITERIA` 归属 ③L2 模型层纪律是否另立一件 ④是否接 `AuditLogger` ⑤**A4 段是否与本包同批批准**）。**第 ⑤ 条即本次动作本身** —— 未拍板前动手改包等于替 Shao Peishen 把 A4 批了；且 ②／②′／④ 任一改判，五包的引用面都要重做（A3 design 原文：只批本包不批 A4 即成「底座＋五份手抄并存」，比现状更差，若不批 A4 请整包否决）。**故：未跑 `/opsx:sync`、未改一行实现、未起草任何 design、未 commit／push、未部署、未触碰 `.51`、未连真实库；🔴 判据落 `None` ＋ CI 守的行为一处未动。** 本行状态相对上一次回写**无实质推进**，`[S:]` 不翻、不销号。指针：`reports/lane-heartbeat/B-0903_30-fin-criteria.md`（§「A4 段」，含未做清单与三项连带裁决原文转录）。 ━━━ 📌 **本行另记一条已拍板、待 A4 落地的裁决（EE-3，Shao Peishen 2026-09-03）**：`FI9` **会**带出 OEM 项目标识 ⇒ 按**接 `data_isolation_layer`** 设计。他的理由：研发费用归集按项目走，OEM 项目几乎必然出现；**这一条错了是合规问题，宁可多接**。🔴 接法须守根 `CLAUDE.md` §7 红线 3（OEM 数据按客户路由、禁跨库）与 `5-平台底座/CLAUDE.md` 隔离边界；该设计若命中 openspec 门槛②（数据可见性）⇒ **起草 design 后 `pause`，不自行通过**。本次未落地、未起草。  ━━━ ✅ **2026-09-03 A4 段已做完（CC `OP-0903-D3`，批次 `B-0903_53`，看护批 `B-0903_50` 泳道 `criteria-chain` A3 段）** —— 上一条留步的 ⑤「A4 段是否与本包同批批准」已由 Shao Peishen 当日答 `G-6 = (a)` 批准，五条定夺项全部裁完，依赖解除。本段实做三件：**⑴ 跑 `/opsx:sync`**（🔴 **不是 `apply`**——本仓库 `/opsx:apply` ＝ 实现 tasks 待办，能写 `openspec/specs/` 的是 `sync`）：本包 delta specs 已并入 `openspec/specs/`，新建主 spec 各带一段真 Purpose（原 delta 缺 `## Purpose`，一并补齐，否则 archive 会留 TBD 占位）。**⑵ 判据签认改为引用平台底座** `zhuopin_platform.criteria_signoff`：`config.py` 的裸 `None` 常量收为 `CriteriaRegistry` 声明，模块级调 `assert_rule_version()`（**导入期**即双向校验，比原 `test_rule_version_marked_unsigned` 更早）。🔴 **行为一处未变**：未签认 ⇒ 值恒 `None`、读取即抛 `CriterionNotSignedOffError`、无 `default` 旁路；断言对象由「常量是不是 `None`」变为「注册表这条签认了没有」——后者更硬（`None` 只是碰巧没值，未签认是一个**读了就炸**的状态）。**⑶ `G-5` 反向依赖**：新增 `config.audit_decision(**fields)`（四行，恒带 `RULE_VERSION`）＋ 一条用例拿真 `AuditEvent` 断言 `decision["rule_version"]`；方向是**审计日志指向判据版本**，不是判据模块去写日志。 本包 **3** 条判据迁入注册表（`CAPITALIZATION_CRITERIA`／`HIGH_TECH_POLICY_LIBRARY`／`RD_RATIO_DEFINITION`），迁移后仍全部未签认；新建 3 份主 spec（`fi9-aux-ledger-and-filing`／`fi9-capitalization-rules`／`fi9-cost-collection`）；本包单测 12 → **17 passed**。`TIMESHEET_SYSTEM_EXISTS` **原样留在本包、未并入注册表**（`G-2 = (a)`：存在性未核实靠**去问一个人**解除，已独立立行 §一 `#477`），新增 `test_timesheet_existence_is_not_a_criterion` 守住。 ⏸ **本行是本批唯一一处留步：`EE-3` 接法待 design 审。** Shao Peishen 2026-09-03 已裁 `EE-3 = (a)`「**会**带出 OEM 项目标识，按接 `data_isolation_layer` 设计」（原话理由：研发费用归集按项目走、OEM 项目几乎必然出现，**这一条错了是合规问题，宁可多接**）；但**接法**命中根 `CLAUDE.md` openspec 门槛②（涉鉴权与数据可见性）⇒ 本泳道**只起草 `openspec/changes/fi9-rd-cost-mvp/design.md`（局部件，只覆盖 `tasks` 2.4，含 5 条定夺项）后 `pause --action-key openspec_design_review`，一条都未默认生效**，🔴 **未实现任何隔离层接线、未新增任何带 OEM 归属的字段**。🔴 **其中 §定夺项 ② 是本场景排期的真卡点**：高新认定辅助账天然跨全部 OEM（政府申报要的是**全公司口径**，按客户拆开就不成其为申报材料），与《OEM 数据隔离规范》§3.1 fail-closed 正面相撞——三个选项（只吃脱敏聚合量／逐 OEM 分别产出人工合并／申报口径开例外）差别不在技术、在**谁向客户解释这件事**，属合规红线口径，起草方不给倾向。**裁定前 MUST NOT 实现任何跨项目汇总**（`tasks` §4.5 研发费用占比与 §5 辅助账／备查资料包全卡在这一条上）。🔴 **连带**：无论 §定夺项 ① 裁成哪条，都须回写《OEM 数据隔离规范》§2 三分法表——它对「研发费用金额」这类数据**现在是空的**，空白会让下一个碰研发数据的场景（`R1`-`R5`）原样再撞一次；该规范修订须经 Shao Peishen 批准。另在 `config.py` 落 `OEM_PROJECT_SCOPE_PENDING_DESIGN` ＋ 用例守——防后来者按其余四个财务场景「财务数据不隔离」的结论把本场景顺手归并进去（那个结论对本场景不成立）。 **闸**：`openspec validate --all --strict` **155 passed / 0 failed**；五场景单测 **71 passed**（迁移前基线 53）；底座全量回归 **466 passed / 1 skipped**（零回归）。**验收硬条件两条均实测通过**：迁移前后未签认判据数 **18 ＝ 18**；五个包**零**残留本地判据 `None` 常量（`ast` 扫模块级 `X = None`，命中仅 `G-2` 三类缺口）。已合入并 push master `a30217a`（分支 `claude/op0903d3-fin-apply` 保留备追溯，worktree 已自删）。🔴 **`[S:open]` 不翻**：本行是**场景立行**行，A4 只是其中一步——design 审仍未过，`tasks` §2 起未开工，引擎一行未写。🔴 **本段未填任何判据值、未给任何默认值**；未进 `/opsx:apply`、未连真实库、未部署、未触碰 `.51`、未发任何信、未改任何规划文档。指针：`reports/lane-heartbeat/B-0903_50-criteria-chain.md`（§「A3 段」）。 ━━━ ✅ **2026-09-03 EE-3 隔离层接法五条定夺项已裁并落地为可复用组件（CC `OP-0903-E1`，看护批 `B-0903_70`，泳道 `fi9-oem`，人在环起法）** —— `design.md` §定夺项五条由 Shao Peishen 当场逐条裁定：①(c)分层（明细硬隔离/脱敏聚合走规范§2.3既有通用层机制，非新造一层）／②(c)立法定申报豁免款＋三道锁（限用途/限流向/限留痕人工闸，法理依据＝规范§1保护对象是「防A客户被B客户看到」而高新辅助账接收方是税务机关不流向竞争客户）／③(b)允许归集禁汇总与对外（排除清单须显要位置不塞附录）／④(a)研发侧认定财务侧使用（🔴禁止从项目号/项目名推导，单测钉死反例）／⑤(a)不卡（规范射程是Chroma检索路径，FI9走U9C关系型取数不受§3.3约束）。落地为新模块 `fi9_rd_cost/oem_isolation.py`：`resolve_project_source`（E2.2入口层判定，改用`OEMRouter.resolve()`而非`guard()`——理由已记入design.md E2.2：FI9无「调用方OEM身份」这第二根轴）／`partition_by_ownership`（③④未判归属排除＋`OwnershipPartition.excluded_project_ids`显要清单）／`cross_oem_filing_gate`（②(c)跨OEM汇总闸，开关`FI9_CROSS_OEM_AGGREGATION`默认OFF、翻开条件=豁免款已正式立进规范，三道锁校验+audit留痕，含D2.4式一次性变异实测+常驻元测试证明「开关OFF即拒绝」不是碰巧通过）。`models.py`新增`RdProject.oem_customer`三态字段（None未判默认/注册OEM名/`NON_OEM_PROJECT`哨兵，两者严格不共用表示）；`config.py`新增`cross_oem_aggregation_enabled()`（同`SC8_KIT_DATE_RULE1`既有模式），原`OEM_PROJECT_SCOPE_PENDING_DESIGN`更名`OEM_ISOLATION_DECISION`并更新为已裁决表述。 ━━━ 🔴 **①②⑤对《OEM数据隔离规范》的连带回写已起草为待批文本**（`openspec/changes/fi9-rd-cost-mvp/oem-isolation-spec-amendment-draft.md`：①补§2三分法表新增行／②豁免款全文/三道锁/法理依据／⑤§3.3射程澄清一句），**一个字未落进规范本体**——触碰区与`oem-chroma-ownership-rejudge`（§一`#466`）在办改动重叠，按队列纪律「触碰区与他人在办重叠不得抢领」，待其收口后并成一次修订派单，须 Shao Peishen 批准。 ━━━ ⚠️ **`tasks.md` §2.4 仍不勾选**：本次交付的是「隔离层怎么接」这一段的可复用组件，尚未实际接入任何真实取数/归集引擎——`tasks.md` §3-§5（cost-collection/capitalization-rules/aux-ledger-and-filing）design审仍未全部通过，`§2`其余六项（2.1/2.2/2.3/2.5/2.7/2.8）未起草，仍不归档（`工具-变更包自动归档.py`实测 21/51，30条真未完项，非archive动作）。 ━━━ 🔴 **本行仍不销号 `[S:open]` 不翻**：本行是场景立行行，本次只是design局部件的接法落地，design审整体仍未过，引擎一行未写，未连真实库、未部署、未触碰`.51`、未发任何信、未改任何规划文档正文（仅出待批文本）。 ━━━ **闸**：FI9本包单测 **38 passed**（迁移前基线17，新增21，含①三态形态②未判不进汇总③禁推导④guard调一次不在每行⑤开关OFF fail-loud+D2.4两层元测试）；平台全量回归 **466 passed+1 skip**（零回归）；FI5/FI6/FI8/FI10 零回归；`openspec validate --all --strict` **155 passed / 0 failed**。已 commit `d70ed28`（分支`claude/op0903e1-fi9-isolation-7777de`，已push）并在主工作区 `git merge --ff-only` 合入并 push master（`e4353c3..d70ed28`）。指针：`reports/lane-heartbeat/B-0903_70-fi9-oem.md`；§二批次登记 `B-0903_73`。
