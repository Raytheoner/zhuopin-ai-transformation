---
title: "队列行日志 #352（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #352 的状态列整格原文外置件（K2 口径：单格 ≤4 KB；本行首段本身即超限或整格无分段，机器切不了，故整格外置、行内由人重写摘要）。
---

---

# §一 #352 · 整格原文（2026-09-04 人工重写前外置，md5:fd817b70）

[S:done][D:机] ✅ **已完成（2026-08-24，CC 无头批处理，独立 worktree `queue-lint-appellation-352`，commit `7be22eb` ＋ `8de0137`，均已 ff 入 master 并 push，`git rev-list --count origin/master..master` ＝ 0，openspec 变更包 `queue-lint-appellation-guard`（**未归档**，见下 ⚠️⑴））**。**称呼豁免：本行是称呼判据的定义行，说清判据要拦什么就必须引用该字面量**（同 #355「防回归判据的初版命中了我写在注释里解释这个反范式的那段话」——自指问题第二次出现）。 **修法**＝`工具-队列结构lint.py` 新增第 ④ 项判据，**计入 `lint()` 与退出码**（非只告警）；baseline 入库 `0-学习与工具/队列称呼判据-baseline.json`，冻结 **21 个活行／74 处命中**（2026-08-24 实测）。 ⚠️ **行内原写的「41 行历史命中」被实测推翻**：按表格数据行口径真值是 **21 个活行（74 处命中）**，另有 7 处路径与账户名、7 处 `[S:done]` 历史行——**三个数字、三种口径**，同 §四 #58 记过的「一条监测指令若不写明在哪个结构位置取数，迟早会被在讲解它自己的散文上执行一次」。 🔴 **三类豁免全部按原因分类计数并打印，不静默**：⑴ 路径与账户名形态（根 CLAUDE.md §1「绝不替换路径里的那个目录名」——**实测 7 处全落在 `C:\Users\...` 路径或计划任务运行身份`.../S4U` 里、无一是称呼用法**；代价是一处已知假阴性，刻意选的方向：漏报只是少拦一次，误报会把一个「绝不可改」的路径推给下一个人去改）；⑵ §一 `[S:done]` 历史行（历史记录不追改）；⑶ 行内 `称呼豁免：〈理由〉`（逃生阀，范式沿用 §四 #58 的 `WIP豁免：`——**理由的唯一真源是行内标记、不是命令行开关**，命令行参数随窗口关闭即消失，而这条阀要治的恰恰是「越过之后没人知道为什么」）。 **baseline 用「计数棘轮」不用「快照哈希」**：后者会让「改动离命中点很远的正文」也报违规，而修它最省事的办法就是重刷 baseline ⇒ 门禁自废；**刻意不提供 `--update-baseline` 一键重刷**，`--emit-baseline` 只打 stdout、落盘须显式重定向 + commit，好让它在 diff 里被人看见。 **行键跨两份队列文件全局唯一且刻意不含文件名** —— #315 式「行从一份挪到另一份」的纯搬家不得报违规（已配反例单测锁死）。 **变异验证（判据不是空转）**：空 baseline 喂真实队列 → **21 条违规／21 活行／74 命中**（与 `--emit-baseline` 逐数对上）；换回入库 baseline → **0 违规、退出码 0**，两个方向都验过。 **回归**：`0-学习与工具` **705 passed／63 subtests**（新增 14 用例），零漂移。 **CI 实测（run `32732006473`，2026-08-24）——精确到 job，不含糊说「CI 绿」**：本行的两条相关 job **均 success**——`队列结构 lint（列数／裸竖线／§二状态列格式／称呼判据）` ✅ ／ `测试 · 0-学习与工具` ✅（另 `openspec validate --all --strict` ✅）。⚠️ **整体 run 仍是 failure**，红的三条是 `CLAUDE.md 进度段 lint`／`凭据扫描`／`CI 覆盖率下界护栏`——**三条在本次改动之前就已经在红**（同一组三条在 `88d097e`(#355) 与其前一个 commit 的 run 上逐字相同），`--log-failed` 对本次新增的三个文件名（`队列称呼判据-baseline.json`／`queue-lint-appellation-guard`）**0 命中** ⇒ 与本行无关，**不由本行承接**。 🔴 **这三条长期红本身是个问题**——一条长期红的 CI 与一条长期红的门禁是同一回事（同 §四 #58「非阻断提示被连续 6 次越过后信息量为零」），**但它不在本行触碰区，不擅自扩范围**，仅在此登记以免下一个人把「整体 run 红」误读成 #352 没做成。  🔑 **本条最该被记住的一句——一次首轮 CI 红，而本地是全绿的**：首版 baseline 是按**本地工作区**冻结的，收工回写刚给本行加了 `称呼豁免：` 标记 ⇒ 本地重跑 `--emit-baseline` 时本行被判已豁免、**掉出 baseline**（21 键→20 键）；而 CI checkout 的是 **master**，那里队列写回还没 commit、本行仍有 2 处命中、baseline 里却已经没有它 ⇒ **判为「新增 2 处」，`队列结构 lint` job 与 `测试 · 0-学习与工具` 腿双双红**。 **⇒ 判据（比这次事故本身更值得记）：凡门禁依赖一份入库的冻结快照，「本地绿」不蕴含「CI 绿」——本地跑的是「工作区 ＋ 快照」，CI 跑的是「master ＋ 快照」，两者自洽性互不蕴含，而本地那一份永远是绿的。收工自检必须补一步：用 `git show master:<file>` 取出仓库里那一份再跑一遍。** 同族＝ `#345` 记的「凡引导／路径解析类改动，本地绿 ≠ 生产可启动」，这里是它的第二个形态。 **修法选了对 commit 顺序免疫的那一个**：把本行放回 baseline（落库前 2≤2 放行、落库后转由行内标记豁免）；另一条路「baseline 与队列写回同批 commit」也成立，但它要求每个改到命中行的人都**记得**同批提交——那正是本判据要消灭的那种「靠人记得」。 ⚠️ **留下一个钝角，如实登记**：一条「已 baseline、如今转为豁免」的键既不报违规、也不计入漂移（豁免行在扫描阶段就 `continue` 了，走不到漂移统计）⇒ 这类死条目**没有任何信号**，只能靠 `--emit-baseline` 重跑后逐键 diff 才看得见。  ⚠️ **三处未闭合，如实登记**：⑴ **变更包未归档**（tasks §5 已写机器认得的 `暂不归档`）——design 五项决策点须 Shao Peishen 签认，本次为无人在场的无头批处理，按批处理硬规则①一律登记后停在该点；⑵ **`5-平台底座/wecom-aibot-service/aibot_service/intake.py:42` 的 `UNMATCHED_QUEUE_OWNER` 常量是目前唯一仍在往队列里写该字面量的机器写入方**（生产 `#392`／`#393` 即此形态，两行已进 baseline），**本次刻意未改**——不在本行触碰区、归机器人链路另一条派发线；现状实测已接近死码（入件白名单 6 人在 `#387`⑷ 补入 `IT` 映射后全部有部门归属），若它再被走到，本门禁会红——**那是按设计工作、不是误报**，修法＝该常量改为 `Shao Peishen` 一行 + 三处测试断言；⑶ **扫描面只有 §一／§二／§四 表格数据行**，队列顶部协议〇 正文与各区说明段仍无覆盖，覆盖率如实写成「表格数据行 100%、其余 0%」，不写成「队列 100%」。
