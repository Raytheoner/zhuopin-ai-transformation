---
title: "队列行日志 · §一 #491 状态列历史回写段"
created: 2026-09-08
status: 生效
外置会话: Cowork 业务总线 OP-0908-A
---

# 队列行日志 · §一 `#491`


---

## 📎 2026-09-08 清账外置（`OP-0908-A`，回灌 `B-0908_B` 回写件同批，md5 `3932bb90bdff134eee55c40693865fde`）

━━━ 🟡 **代码闸已建成并全绿，停在一处 🟡 定夺点（`change_criteria`）**（CC `OP-0908-E`，2026-09-08 本地，批 `B-0908_B` 泳道 `491-general-collections-readonly`，分支 `claude/op0908e-general-readonly`，由看护批 `B-0908_B` 派出；本泳道按看护者硬口径不碰主仓工作区、不 ff master、不自写队列真身，本段为「队列回写待补」挂起稿，须总线回灌）。 ━━━ **硬序前置已现取复核**：worktree 树上 `router.py` 已是 `#466` fail-closed 版（`DEFAULT_AUDIT_LOG_PATH` ＋ `_default_audit_logger()` 在位，`_record_denied` 的 `self._audit is None` 分支为 `raise` 而非 `return`）⇒ `#466` 已合入 master，可开工。 ━━━ **交付四件已出三件**：⑴ 独立 openspec 包 `oem-general-collections-readonly`（proposal／design／tasks／delta 四份，`validate --strict` 绿）；⑵ 只读闸实现＝`OEMRouter.guard_write()` ＋ 新异常 `GeneralCollectionReadOnlyError`（**刻意不继承 `CrossOEMAccessError`**，否则既有 `except` 会顺手吞掉本闸）＋ 独立审计动作 `general_collection_write_denied`，通用库写入一律拒、拒前留痕、审计通道故障 fail-closed，**不留运行期开关/可注入 validator**（避免「注入空校验器即绕过」）；⑶ 单测 `5-平台底座/zhuopin_platform/tests/test_general_collections_readonly.py` 13 用例；⑷ **隔离规范 §5 映射表 §2.3 行改判 —— 未落字，见下**。 ━━━ **核验手段与实测（亲跑，非转抄）**：新单测 `python -m pytest tests/test_general_collections_readonly.py -q` ＝ **13 passed**、退出码 0；平台全量 `pytest tests/ -q` ＝ **589 passed / 1 skipped**、退出码 0；FI9 消费方 `pytest tests/test_oem_isolation.py -q` ＝ **21 passed**；FI10 消费方 `pytest tests/ -q` ＝ **47 passed**；`openspec validate oem-general-collections-readonly --strict` ＝ `Change 'oem-general-collections-readonly' is valid`，`validate --all --strict` ＝ **182 passed / 0 failed**；`git check-ignore -v reports/audit_log.jsonl` ＝ 命中 `.gitignore:50:**/reports/`（无新增未忽略产物）。 ━━━ 🔴 **停点（须 Shao Peishen 一个字母）**：`3-治理与合规/OEM数据隔离规范.md` §5 映射表 §2.3 行改判（`❌ 待设计` → `⚠️ 部分实现`）＋ §6 修订记录新增 V1.3 —— 该文件 `status: 生效`、L10 自定「边界变更须经批准并在本文修订记录登记」，且同表 §3.2 行的同类改判（V1.2, 2026-09-07）也是他答 `2(a)` 后才落字，**本泳道不代签批准人**。**拟改文本已逐字备在** `openspec/changes/oem-general-collections-readonly/tasks.md` §5（含现文对照与 §6 新行草稿，批准栏留空）。已跑 `工具-泳道看护状态机.py pause --action-key change_criteria` 落状态。 ━━━ **另有两项 design 审定夺（🟡，随本包评审一并答）**：① `oem-chroma-ownership-rejudge` spec L55「读取侧无条件放行仅在写入侧校验生效的前提下成立」如何处置——本包**未动读取侧**（推荐维持：D5 明写「已有内容不受影响」；闸生效后无新内容进入；全仓从未有过写入通用库的代码），并加了 `test_read_side_unchanged_*` 回归护栏；② 本包 delta 为 **ADDED 新 Requirement**（与既有条款不同名）⇒ 判定**不进** `oem-chroma-ownership-rejudge` → `oem-audit-fail-closed` 那条 archive 次序链，请审查方复核。 ━━━ **穷举核查（重跑，非沿用旧结论）**：`GENERAL_COLLECTIONS` ＋ 三个 `kb_*` 名全仓 grep ⇒ 改动前**零写入侧引用**、无任何生产代码提及通用库 ⇒ D5「零写入路径」前提在 2026-09-08 仍成立；`OEMRouter(` 全仓 **30 处**／7 文件。⚠️ **`#466` tasks §7.4「全仓无任何生产代码构造 OEMRouter」已过时**：`4-数字员工/财务部/FI10-存货跌价智能分析/fi10_inventory_writedown/intake.py` 现有生产实例化（只走 `resolve()`／`guard()` 读取侧、不碰通用库、无写入，故不受本包影响）—— 本行据现取重述，供 `#466` 收尾时同步。 ━━━ 🔴 **未做且不自行做的四件**：**ff 合入 master（🟡）**、**任何 archive 动作（🔴 次序敏感）**、§二批次登记与 sweep 触发、隔离规范落字（上述停点）。 ━━━ **同域已知失真、本包未触碰**：主 spec `openspec/specs/platform-oem-isolation/spec.md` L9／L15-17 仍写「无 audit 注入时仅抛错（向后兼容）」，与已合入实现相反 —— 归 `#466` 盯／`#374` 收尾；本包 delta 为 ADDED 新条、与该条不同名，**无冲突**。 ━━━ **心跳**：`reports/lane-heartbeat/491-general-collections-readonly.md`。━━━ ✅ **隔离规范落字已完成（CC `OP-0908-J` 续段，2026-09-08，批 `B-0908_B` 波 3）**：上一棒 `OP-0908-E` 停在的 🟡 决策点（tasks §5.2 `change_criteria`）已由 **Shao Peishen 2026-09-08 答 2a** 放行，本段只做落字、零扩范围。分支 `op0908j-work`（自 `origin/claude/op0908e-general-readonly` @ `e8023a7` 起），commit `f9a22ce`，已 push origin，**未合入 master**。 ━━━ **落字三处，全在 `3-治理与合规/OEM数据隔离规范.md`（2 files changed, 7 insertions(+), 6 deletions(-)）**：⑴ **§5 映射表 §2.3 行**（L76）由 `❌ 待设计` 改判为 `⚠️ 部分实现`，逐字采用 tasks §5 已备「拟改为」文本（零改动）——同行内显式挂账两项余项：**§2.3 三项校验入口本身仍 ❌ 待设计**（无 OEM 信息校验／脱敏＋质量 Champion 签字／写入写 audit 三项均未建，只读闸只是入口未建成期间的替代控制、不是该入口的实现），**读取侧语义未变**（`GENERAL_COLLECTIONS` 仍无条件放行，`guard()` `router.py` L142-144，是否收紧属新口径变更、未答）；承接载体改为「只读闸＝变更包 `oem-general-collections-readonly`（§一 #491）；校验入口本身待另立行」。⑵ **§6 修订记录新增 V1.3 行**（L88），**批准栏＝`Shao Peishen`**，照同表 §3.2 行 V1.2（2026-09-07）同口径写法：`**Shao Peishen 2026-09-08 答 2a 放行**（看护批 B-0908_B，泳道 491-general-collections-readonly，OP-0908-J 执行）；证据链见队列 §一 **#491**`。⑶ **§5 表头现状日期同批更新**（L71）：`现状（2026-09-02 重核，§3.2 行 2026-09-07 更新，§2.3 行 2026-09-08 更新）`——照 V1.2「同批更新 §5 表头现状日期」先例，并在 V1.3 行内如实记明。 ━━━ **验证手段与实测数字（非自陈）**：① **落字前逐字防漂移**——Python `hashlib.sha256` 比对规范现文 §2.3 行 与 tasks §5「现文」块，两者 `f6147fe006d846da…` **完全相同**、长度均 269 字符 ⇒ 无他人改动，未触发 `pause --action-key change_criteria` 分支；② **行号引用现取复核**——`grep -n` `router.py`（`5-平台底座/zhuopin_platform/zhuopin_platform/data_isolation_layer/router.py`）实测 `guard()` L136 定义、L142-144 即通用库无条件放行块，草案引用属实（旧文所写 L78-80 已过时，本次随改判一并更正）；③ **落字后回读**——Python 重解析规范文件：§5 表 7 行全部 5 列、结构未破；§6 四行修订记录批准栏逐行打印，V1.3 批准记录在位 ⇒ 满足该文 L10「边界变更须经批准并在本文修订记录登记」与 IATF 16949 可追溯性；④ **`npx openspec validate oem-general-collections-readonly --strict` ⇒ `is valid`，退出码 0**。 ━━━ **未动（硬边界内）**：代码零改动、单测零改动、spec delta 零改动、§5 表内 §2.3 之外各行零改动、规范 §2.3/§3.2 正文零改动、§7 运维面仍缺（原挂账不变）；未碰 FI6 两处、队列真身、跟进信 README、`工具-落库sweep.py`、`evals/`、口径点台账；未触碰 `.51`／企微机器人／定时任务。 ━━━ 🟡 **仍留三项待他一个字母（本段不执行）**：tasks 6.3 `merge_to_master`（本包 ff 合入 master）；tasks 6.4 design 审两项定夺（`design.md` §6：读取侧那句怎么处置 ／ 本包 archive 次序是否真解耦）；tasks 6.5 archive 次序判定复核。⚠️ **合入 master 时须一并 ff 本段 commit `f9a22ce`**（分支 `op0908j-work` 是 `claude/op0908e-general-readonly` 的直系后代，纯 fast-forward，无历史改写）——**只 ff 后者会漏掉本次隔离规范落字**。
