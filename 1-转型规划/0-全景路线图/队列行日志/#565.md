---
title: "队列行日志 #565（§一）"
created: 2026-09-12
status: 生效
用途: 跨桌任务队列 §一 #565 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #565 · 外置段（2026-09-12 外置，工具第 1 批，2 段，原文原样）

> 外置人 CC-OP-0912-B-lane-heartbeat-565；外置前整格 7607 B、共 4 段；本批外置第 2、3 段；本格保留第 1、4 段。

🔴 **诊断＋调用侧最小落地已完成（`OP-0912-B`，2026-09-12，worktree `op0912b-heartbeat-wiring-565`／分支 `claude/op0912b-heartbeat-wiring-565`）**：①真实心跳命令＝`heartbeat --lane LANE --text TEXT [--done] [--batch BATCH] [--json]`（`--help` 现取）；②**根因坐实**——现读看护件 `看护件-泳道看护批B-0911_机制收口-2026-09-11.md` §三 全部 5 条泳道 opener 代码块正文，逐字比对 `工具-opener生成.py::generate_opener(variant="subtask_lane")` 输出一致，**均无心跳提及**；心跳约定只写在该件 §一「硬边界继承」（看护者自读）与 SKILL.md 步骤 4，两处都不进子任务 prompt；③已在隔离 worktree 内现取 `heartbeat --lane test-op0912b-verify-565 ...` 与 `--done --batch <测试批次>`，回显确认写入落主 checkout（`REPO_ROOT` 经 `git-common-dir` 解析，worktree 隔离下心跳工具本身正常，验证后已清理不留痕）；同批复核 `summary` 额外发现 8 条历史泳道因 `--done` 未带 `--batch` 而「批次归属未知」。④**调用侧最小修复已落地**（派单件明示豁免、不越过 openspec apply 边界）：`工具-opener生成.py` 新增 `SUBTASK_HEARTBEAT_NOTE` 常量并强制注入子任务泳道 opener 正文（并行上限之后、push 规则之前，含 `--batch`）；`opener骨架.md`【CC · 子任务泳道】骨架三条机器口径扩为四条；`工具-opener块lint.py` 新增形态⑩（子任务泳道块缺心跳约定行）；`zhuopin-lane-watch/SKILL.md` 步骤 4 示例命令补 `--batch`、新增「已知缺陷」缺陷四并标已修、版本历史加 v2.3；`test_工具-opener生成.py`／`test_工具-opener块lint.py` 新增/更新专项用例。**手段＝**`python -m pytest 0-学习与工具/test_工具-opener块lint.py 0-学习与工具/test_工具-opener生成.py 0-学习与工具/test_工具-opener批处理执行v2.py -q` → `226 passed, 40 subtests passed`；`openspec validate lane-watch-heartbeat-wiring --strict` → `valid`；`openspec validate --all --strict` → `190 passed, 0 failed`。⑤**方案 B（看护状态机改读子任务收工哨兵字段自动闭环）只设计、未落地**——`openspec/changes/lane-watch-heartbeat-wiring/design.md` 决策点 5，因本轮实测心跳工具在 worktree 隔离下已可用、非紧急，apply 留待 Shao Peishen 过 design 审后另开泳道。🟡 **停点**：openspec design 审（决策点 1–4 已按推荐取值落地，仅决策点 5／方案 B 待裁是否推进）＋ 主仓 ff（本泳道只 push 分支，不自行合入）。产出分支：`claude/op0912b-heartbeat-wiring-565`。

━━━

⚠️ **同题重复执行（第二棒，`OP-0912-B` 无头批 `B-0912_心跳与称谓` 09:22 起跑那份看护件；2026-09-12 09:26–10:1x 本地）**：本棒按看护件 A1 opener 起 worktree `op0912b-lane-heartbeat-565`／分支 `claude/op0912b-lane-heartbeat-565`（**已 push，commit `8a1021f`，未 ff**），收工核对队列时才发现上一段（`claude/op0912b-heartbeat-wiring-565`，`6733cb4`）已于 09:38 登记同题产出——两棒各自诊断结论一致（①完整形须带 `--batch`；②子泳道正文确无心跳；③worktree 内心跳工具实测可达、落主工作区；④反向方案不需要），**修法落点相同、实现不同、两分支同文件冲突、只能二选一 ff**。本棒差异：⑴ 心跳尾句**不留占位符**——`--lane` 填真值＝worktree 名（`op{MMDD}{x}-{slug}`，与【设置】行分支名同源，看护者据此直接推算 `check-heartbeat --heartbeat-file`），`--batch` 填真值＝`--batch` 显式或从 `派出线` 现取 `B-MMDD_…`，两者皆无 **fail-loud 不出件**（对照棒＝常量占位「无更明确约定时用本任务队列行号」，仍靠子任务自己填）；⑵ lint 形态⑩判据＝同一行须同现 `工具-泳道看护状态机.py heartbeat`＋`--done`＋`--batch`（对照棒＝`heartbeat`＋`--lane`，不查 `--batch`）；⑶ 尾句位置＝倒数第二行（哨兵仍末行），骨架契约单测比尾四行逐字（心跳行比占位符版）；⑷ 生成器新增 `--batch` 旗标，非 `subtask_lane` 传入即拒（同 `#487` 静默丢弃判据）；⑸ 编辑锁单测夹具同步补心跳行；SKILL.md v2.3＋CHANGELOG 同步；**未做** openspec 变更包（对照棒有 `lane-watch-heartbeat-wiring`）。**手段**：`pytest test_工具-opener生成.py test_工具-opener块lint.py -q` → `218 passed, 40 subtests`；`test_工具-共享文档编辑锁.py -k "opener or subtask or 泳道"` → `25 passed`；`工具-泳道纪律复制lint.py` ✓；`工具-opener块lint.py --file opener骨架.md` 零违规；零回归对照＝master 版 lint `--enforce` 47 处 → 本棒 49 处，差 2 ＝ F10 命中 `看护件-泳道看护批B-0912_心跳与称谓-2026-09-12.md:51／:64`（本批两条泳道 opener 确无心跳行，真实缺口非回归；该件归 `OP-0910-H` 触碰区，本棒未改）。 🟡 **停点（二选一，须裁）**：(a) ff 对照棒 `claude/op0912b-heartbeat-wiring-565`、废弃本棒分支（保留其 openspec 包与设计审线）；(b) ff 对照棒后另起一小棒把本棒 ⑴⑵⑷「真值填充＋fail-loud＋`--batch` 判据」移植上去（推荐：占位符留给子任务填＝又一次「靠子任务读懂」，`#487`／`#550` 已付过两次学费）；(c) ff 本棒、废弃对照棒（丢 openspec 包）。本棒**未**改 `工具-opener批处理执行v2.ps1`／状态机；占号台账留有验证用 `OP-1231-Q` 占位（120 分钟自动作废）。收工心跳已按新尾句形态写（`reports/lane-heartbeat/op0912b-lane-heartbeat-565.md`，`--done --batch B-0912_心跳与称谓`）。

---

# §一 #565 · 外置段（2026-09-12 外置，工具第 2 批，1 段，原文原样）

> 外置人 CC-OP-0912-F；外置前整格 4679 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

🟡 **两处停点（2026-09-12，`OP-0912-B` 两棒）**：⑴ 对照棒 `claude/op0912b-heartbeat-wiring-565`（`6733cb4`）——openspec `lane-watch-heartbeat-wiring` design 审，仅决策点 5／方案 B 待裁；⑵ **同题重复二选一**——第二棒 `claude/op0912b-lane-heartbeat-565`（`8a1021f`，已 push 未 ff）与对照棒同文件冲突：(a) ff 对照棒、废第二棒；(b) ff 对照棒后另起小棒移植第二棒的「`--lane`／`--batch` 真值填充＋缺批次 fail-loud＋lint 判 `--batch`」（**推荐**——占位符留给子任务填＝又一次靠子任务读懂，`#487`／`#550` 已付两次学费）；(c) ff 第二棒、废对照棒（丢 openspec 包）。两棒诊断结论一致、皆未改 `工具-opener批处理执行v2.ps1`；差异逐条与验证手段在行日志。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **2 段已于 2026-09-12 外置**（K2 口径，原文原样、可 grep，文件 md5:3d5372e6，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#565.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 CC-OP-0912-B-lane-heartbeat-565）。
