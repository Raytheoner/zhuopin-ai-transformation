---
title: "队列行日志 #109（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #109 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #109 · 外置前整格原文（2026-09-04 外置，6 段，md5:3df946ac）

✅ 已拍板：design 通过 ＋ 裸 URL 判误粘贴（Shao Peishen 2026-08-25 答晨复盘 D-4/D-5 均选 (a)）——env-anchor-collapse 按包内定案 apply 解锁（含无默认的决策点 1、4 按 design 正文定案执行）；根 .env 第 3 行孤立裸 URL 判误粘贴、删除动作并入 apply 首步执行（先备份后删、删后验键数与既有键值不变），不由 Cowork 直接碰凭据文件。执行承接＝§一 #354（已同批追记）。本行销号。━━━ 🔴 **`.env` 同族收拢（#354）的 design 审：六个决策点，其中两个无默认项须明确答复（2026-08-24 CC 无头批处理出件，commit `baaaf65`，包名 `env-anchor-collapse`）**：本包按 opener 授权**只做 propose＋design 出件，未 apply、未改任何一行生产码**（#354 点名的那个生产缺陷是独立的纯 bugfix，已修完并 `.51` 真机验证，见 §一 #354 ⑴）。 ━━━ 🔴 **无默认项的两项（判据＝CLAUDE.md §5「默认项两前提」）** ━━━ **决策点 1 · `.env` 有没有「作用域」这回事？** 主工作区现存三份真实 `.env`：根（19 键，全项目）／`5-平台底座/.env`（6 键，企微机器人服务专属，7 个脚本**显式指名**要它，是刻意的）／`4-数字员工/采购部/SC1-供应商风险初筛/.env`（6 键，**性质未明**，含根里没有的 `DEEPSEEK_API_KEY`，**另 5 个 `XKY_*` 与根 `.env` 同名**）。选项：(a) 承认作用域、两层合并 (b) 否认作用域、统一到部署根（要动 3 个凭据文件与 7 个在跑脚本，含常驻服务） **(c) 承认但显式化——作用域由调用方在代码里指名、不靠目录距离猜，「最近的那份」这个隐式规则彻底删掉（推荐）**。 🔴 **本项无默认项，理由有二**：⑴ 不答的代价不是「停在原地」——**SC1 那 5 个同名键会继续处于「谁先被找到谁生效」的状态**，属正在出血的口子，不得用默认项背书；⑵ 它依赖一个**只有你能回答的事实**：**SC1 那份 `.env` 是刻意的服务级配置，还是早期遗留的历史副本？** `git log` 查不出意图，而答案决定 (a)/(b)/(c)。 **决策点 4 · 找不到任何 `.env` 时，报错还是静默？** 现存 9 份里三种态度、无一处写明理由（8 份静默 return／1 份 `SystemExit`／1 份打印后继续）。选项：(a) 一律静默（保留今天的失败形态） (b) 一律 fail-loud（**会打挂那些本就靠环境变量注入凭据的入口，而「哪些是这样」目前无人能列全**） **(c) 默认静默 ＋ 调用方声明自己必需的键，读完检查这些键是否真的到位、缺则 fail-loud（推荐）** —— 把判断从「文件在不在」移到「**我要的键到手没有**」，对两种凭据来源都成立。 🔴 **本项无默认项**：不答则无人执行（提出本项的 session 已收工，回合制无法自我唤醒），且其形态与决策点 1 耦合（键清单怎么写取决于作用域结论）。 ━━━ **其余四点已给推荐＋默认项，不答即按默认执行**：决策点 2 worktree 里取谁的凭据（默认＝复用 `--git-common-dir` 规范化 ＋ 保留 `ZP_ENV_FILE` 显式覆盖）／决策点 3 无 git 无 marker 时怎么退化（默认＝第二锚点「含 `zhuopin_platform` 子目录的祖先」＝扁平部署根，🔴 **`.51` 上没有 git，任何依赖 `git` 的方案必须能优雅退化**）／决策点 5 CI 门禁挂哪（默认＝扩 `工具-引导样板lint.py`、不新起守卫、不改文件名，先告警后 `--enforce`）／决策点 6 分几批改（默认＝三批，按改错代价从小到大，**4 个 `.51` 常驻服务入口逐个部署冒烟、不许一批推四个**）。 ━━━ ⚠️ **另有一项与本包无关、但须你处置**：根 `.env` 第 3 行是一条**无键名的孤立裸 URL**（一个没有名字的企微 webhook 地址）。不影响现有功能，但 **CC 未动它** —— 凭据文件删哪一行属你的判断，**删错会静默改变某个群的收信去向**。请确认它是误粘贴还是有意留存。 ━━ 🔨 **裁决回填（Shao Peishen 2026-08-24 夜答，2026-08-25 01:08 登记）**：**决策点 1 ＝ (c) 承认作用域但把它显式化**、**决策点 4 ＝ (c) 默认静默 加 调用方声明 `required` 键清单**，其余四点按行内默认执行。 🔑 **映射依据（他给的是事实、不是选项字母，故在此写明推导过程）**：Shao Peishen 明答「**SC1 那份 `.env` 是早期遗留的历史副本**」 ⇒ (a) 会把遗留物正当化；(b) 要为清理一份遗留物去动 7 个脚本指名依赖的常驻服务（企微机器人 07-16 有 24h49m 停摆前科）；故取 (c) —— SC1 那份除非被指名否则永不被误命中，5 个 `XKY_` 同名键的「谁先被找到谁生效」自动消解，其删除由「出血止损」降级为「清理」。 ━━ **并入·根 `.env` 第 3 行无键名裸 URL 的处置（Shao Peishen 授权由本班定）＝ 不删不改、待认领**。三条理由：① `.env` 是 gitignore 件、**git 里没有它的历史**，删掉即永久失去该 webhook 指向，无处可恢；② 查明它指向哪个群必须真实发一条消息＝对外动作，须有人在场；③ 🔴 **不得断言它惰性** —— 企微 webhook URL 内含 `?key=` 参数，朴素的按首个等号切分的解析器会把它切成一个畸形键值对（登记方未读该凭据文件，按「可能有效」处理）。 **下一步**＝有人在场时发一条测试消息认出所属群，再二选一：给它一个键名，或删除。
