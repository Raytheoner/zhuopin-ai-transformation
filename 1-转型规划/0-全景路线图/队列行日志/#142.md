---
title: "队列行日志 #142（§四）"
created: 2026-09-07
status: 生效
用途: 跨桌任务队列 §四 #142 的历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---


---

# §四 #142 · 2026-09-07 外置（7 段迁入，值周巡检）

> 原整格 md5:5392ab4d；行内此后只留首 1 段＋末 1 段＋指针。以下为**外置前整格原文**，原文原样、未改一字。

🔑 **判据（Shao Peishen 2026-08-31 定，落此留痕；答「WIP 上限调不调」时先读本行）** ━━━ **在分诊执行到位之前，任何关于「WIP 上限该不该调」的讨论，都建立在一个已知失真的读数上。** **他的完整因果链（原话结构）**：`partial`／`open` 被用来表示「主体做完、尾巴挂外部条件」→ 分诊没跟上 → WIP 虚高 → 顶爆 22 → 逼出一批 `WIP豁免：` → **逃生阀变常态**（同 §一 `#382`「逃生阀走成常态就等于没有阀」）→ 有人提议重议上限，**而那是在一个已知失真的读数上做决策**。 **结论：不调上限（22 不低），要提的是分诊执行率。** ━━━ 📊 **2026-08-31 首次按本判据执行的结果（值周巡检，逐行判读、未批量改）** ━━━ `_suggest_status_reclassification` 建议改判 **9 行**（#96／#312／#324／#328／#366／#416／#422／#433／#434，全部 → `blocked`）；**逐行过目后只确认 2 行改判成功**（`#312` 收口动作只在他手上；`#422` 硬卡「回内网」），**1 行判定应改而工具挡住未改**（`#324`，缺陷已并入该行），**6 行判定不该改**。WIP 由 **24/22 → 22/22**（他的估算是「约 15」，实测差距的成因见下）。 🔴 **本次最该被记住的一条：建议器对「一行两半」系统性过触发。** 它的触发词是「待 Shao Peishen 定／审／拍板」，但**「有一项等他拍板」≠「整行非我方可推进」** —— 实测 6 行属混合态，各自都有**不依赖任何外部条件的可动半边**（`#96` 的 CC 半边、`#328` 的 ④⑤⑥ 代码、`#366` 的「实现另派单」、`#433` 的 A2、`#434` 的 ⑵ 生产码；`#416` 因 19.4 KB 未通读而按「判不准留原状」处置）。**照单全改会把真实可领的活藏起来 —— 那是另一个方向的失真，且比 WIP 虚高更难被发现**（虚高会顶爆上限而被看见，藏活不会）。⇒ **分诊执行率低是真的，但它不是 24 与 15 之间那 9 位的全部解释**；**建议器需要一条「整行 vs 子项」的区分判据**，否则每次分诊都得靠人逐行读，成本压不下来。同族＝ §一 `#172` 判例「一行两半」。 ━━━ 🧾 **顺带复核『14 条 WIP豁免』这个数，它本身也是失真读数** ━━━ 按 `WIP豁免：` 逐行取证：**真豁免 11 条**（`#387`／`#416`／`#419`／`#421`／`#422`／`#425`／`#433`／`#435`／`#441`／`#442`／`#443`），**另 3 条是误计**（`#324`／`#352`／`#381` 行内那几处是**描述这套逃生阀机制的正文**，不是在用它）。⇒ **grep 一个标记串来数「用了几次」，会把「讲这个机制的话」一并数进去**——同族＝「读的是哪一层」。 **撤销判定：一条都不建议物理删除。** 理由二条：⑴ 豁免**只在 release 新增 `[D:机]` 行且超限时**被查，对已 `done`／`blocked` 的行（`#387`／`#419`／`#421`／`#422`／`#435`）**天然失效、不需要撤**；⑵ 删掉它等于**删掉当时为什么越限的证据**，而那正是这套阀要留下的东西（`#352` 行内原话：理由的唯一真源是行内标记）。**仍在实际承压的只有 5 条**（`#416`／`#425`／`#433` ＋ 今日 `#441`／`#442`／`#443` 中仍 `open` 者），随其转 `done`／`blocked` 自然失效。  　━━━　**预留豁免：本行由 `Cowork-值周巡检-0831E` 于持锁期间新立，acquire 时未用 `--reserve` 预留 §四 编号**（开工时的任务是「对 9 行做状态分诊」，立 §四 判据行是过程中由 Shao Peishen 追加要求的，未预见 ⇒ 属操作疏漏，非行破损）。**编号 142 取自本文件顶部编号高水位线「§四 #141」+1**，落笔前后均核过 `#142` 未被他人占用，无冲突；本次已一并把高水位线回写为 §四 #142。 ━━━ 🔎 **2026-09-07 值周对账审计实证（Cowork-值周巡检0907）**：本轮用 release 自己的代码路径复算机制类可动 WIP ＝ **27／22、超上限 5**（`_split_live_sections` 取 §一 ＋ `_count_mechanism_wip`；机制环境 27、业务场景 0、degraded 0）。而同日激活的 `#462`／`#478`／`#479`／`#482`／`#483` 五行，行内均写「现测 11／22 有余量」——**11 那个数来自把 §一 按首个「二、」标题字样切分**，该字样出现在 §一 某行正文里，切分提前截断（111 行 → 74 行），少看 36 行。🔑 **判据：一个读数只要能被两种切法算出两个值，它就不该由会话自己算。** 修法建议＝给一条只读的 WIP 复算命令（如 `工具-队列查询.py --wip`），任何会话不得自切 §一。定夺见《本周计划-2026-09-07》B-2。 ━━━ ✅ **2026-09-07 Shao Peishen 答 2a**：**追认、不回滚**——五行的 propose＋design 已交付，压回排队等于让已完成的起草悬空；接受名义 27／22，超限期间新立机制行一律先关一条或按协议〇.9 排队。修法（只读 WIP 复算命令、禁会话自切 §一）仍待承接。
