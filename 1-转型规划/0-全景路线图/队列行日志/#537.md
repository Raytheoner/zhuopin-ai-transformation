---
title: "队列行日志 #537（§一）"
created: 2026-09-10
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #537 的状态列历史段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留收口结论＋验证手段＋本指针。
配套: 0-学习与工具/hooks/hooks-userpromptsubmit-standing-five.ps1；0-学习与工具/test_hooks-p3.py；0-学习与工具/hooks/README-P3安装步骤.md；openspec/changes/cc-hooks-p3/specs/hooks-userpromptsubmit-standing-five/spec.md
---

# §一 #537 · 外置件（2026-09-10 外置，CC `OP-0910-D`，泳道 `537-anchor-count`）

## 一、立行时原文（原文原样，未改写）

🟢 **可领 —— 2026-09-10 07:3x 由 §四 `#190` 升格立行**（Cowork 业务总线 `OP-0910-A`；Shao Peishen 当日「全部启动」）。 🔴 **成因是本方造成的，如实登记**：2026-09-09 23:0x 本方按他「以后你能执行的尽量只告知」的立法请求，往根 `CLAUDE.md` §5 加了第 6 条常驻纪律（`UPS5:6` 代执行优先）；而 hook 按 `UPS5:1`–`UPS5:5` 抓取、**判据写死「命中 5 条」** ⇒ 实得 6 ⇒ **此后每一轮 UserPromptSubmit 都打 `⚠ 常驻五条锚点异常（预期 5、实得 6）`**，同一事实在 `0-学习与工具` 全腿回归里表现为一条常红单测。发现者＝看护者 `OP-0909-Z`（2026-09-10 00:2x 全程每轮收到）。 ✅ **hook 本身是 fail-open 的**（命中数 ≠5 仍放行），故**没有阻断任何交互**，危害是持续告警噪声 ＋ 一条常红掩盖真红。 🔑 **修法方向不是把 5 改成 6** —— 那是把同一颗定时炸弹往后推一格，**与当日 §一 `#535` 立的判据同族**（`#535`＝夹具硬编码日期跨零点转红；本行＝hook 硬编码条目数，加一条即转红）。⇒ **两行同源：任何「写死的数量／日期」都会在下一次变化时自动转红。** ⚠️ **本行不回退 `UPS5:6`**：那条纪律本身是他明令入法的，且确属「每次回复都适用」，该进注入集；**要改的是数它的那段代码。** 🔴 **WIP豁免：立行时机制类可动 WIP 现取超上限 22**。豁免依据＝**本方自己造成的污染，且每轮交互都在付成本**；同时它掩盖真红——常红单测会让下一个人对「回归有失败」脱敏。

## 二、收口时的完整验证回显（行内只留摘要，此处留原样）

🔑 **核的人＝CC `OP-0910-D` 会话（分支 `claude/op0910d-537-anchor-count`，commit `bd86519` → `2a9b825` → `e072d3b` 三笔）；核的手段＝以下三组命令的真实回显**，不是「已复核」四个字。时刻取自本机 `Get-Date`，**本地 UTC+8**，非 UTC。

⑴ **本 hook 单测全绿（改前该类 9 条含 1 条常红，改后 14 条）**

    python -m pytest "0-学习与工具/test_hooks-p3.py" -k "StandingFive" -q
    ⇒ 14 passed, 29 deselected in 13.75s          # bd86519 时
    ⇒ 16 passed, 29 deselected in 22.10s          # 2a9b825（补两道挂死防线）后

    python -m pytest "0-学习与工具/test_hooks-p3.py" -q
    ⇒ 43 passed in 79.94s                         # bd86519 时（改前该类 9 条、含 1 条常红）

同族兄弟件同批复跑（确认未殃及同目录另两枚钩子的端到端契约）：

    python -m pytest "0-学习与工具/test_hooks-哨兵.py"                      "0-学习与工具/test_hooks-pretooluse-queue-read-guard.py" -q
    ⇒ 74 passed in 99.70s

⑵ **真实根 `CLAUDE.md` 前后对照（同一份文件、同一条 stdin、两版脚本）**

master 版脚本经 `git show master:0-学习与工具/hooks/hooks-userpromptsubmit-standing-five.ps1` 取出到临时目录（连同 `hooks-common.ps1`）后运行，**未污染工作区、未改动 master**。

    # BEFORE（master 版）
    {"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":
    "⚠ 常驻五条锚点异常（预期 5、实得 6）📌 常驻五条：**决策人**：**Shao Peishen**（分管… ｜
     🔴 **人的属性＝硬事实**：提到… ｜ 🔴 **会话末「需你定夺」**（CC … ｜
     🔴 **输出三条**：① 可粘贴的 p… ｜ 🔴 **默认项只在两个前提下成…"}}

    # AFTER（本棒）
    {"hookSpecificOutput":{"additionalContext":
    "📌 常驻纪律 6 条：**决策人**：**Shao Peishen**（分管… ｜ 🔴 **人的属性＝硬事实**：提到… ｜
     🔴 **会话末「需你定夺」**（CC … ｜ 🔴 **输出三条**：① 可粘贴的 p… ｜
     🔴 **默认项只在两个前提下成… ｜ 🔴 **代执行优先（Shao Peishen 2026…",
    "hookEventName":"UserPromptSubmit"}}
    EXIT=0

🔴 **这组对照证明的不只是「告警没了」**：BEFORE 那一行末条止于「默认项只在两个前提下成…」，
**第 6 条「代执行优先」根本不在注入内容里**——旧实现 `foreach ($i in 1..$ExpectedAnchorCount)`
把它整条丢掉，而告警只说「实得 6」、从不说「其中 1 条没进去」。这是比告警噪声严重得多、
且**此前无人发现**的一处静默丢失。

⑶ **openspec 判据件校验**

    npx openspec validate cc-hooks-p3 --strict
    ⇒ Change 'cc-hooks-p3' is valid

## 二bis、仓库级门禁复跑（本改动会被这几支扫到，逐支实跑而非推断）

    python 0-学习与工具/工具-引导样板lint.py
    ⇒ ✓ 通过（625 个已跟踪 .py，其中 69 个含引导块，全部 ensure_paths() stub 形态）

    python 0-学习与工具/工具-取证件回显lint.py
    ⇒ ✓ 通过（受守取证件 8 件，多族门禁工具 2 个，存量 baseline 冻结 0 处）

    python 0-学习与工具/工具-CLAUDE进度段lint.py
    ⇒ 命中项全部落在 5-平台底座/wecom-aibot-service/CLAUDE.md（J3 条目超长），**本棒未触碰该文件**，属既有存量

    python 0-学习与工具/工具-opener块lint.py
    ⇒ 当前在用件 36 处告警（不阻断）＋ 历史件 643 处按「历史记录不追改」不修，**均非本棒新增**；
      另：release 时的 opener 守卫已对本次触碰的 7 个 .md 校验通过

⚠️ **未取得的一项，如实登记**：`python -m pytest "0-学习与工具" -q` 全量单文件夹回归本次**未跑出结果**——
起跑于 08:15（本地 UTC+8），50 分钟后仍未结束，且同时段至少有另一条泳道在跑
`test_工具-泳道看护状态机.py`、多条泳道在并发写两份队列真身。**在共享真身被并发改写的窗口里跑出来的
全量数，既不可复现、也不能归因**，写进来只会变成又一个「看着像证据的东西」。故本行的零回归主张
**只覆盖上面逐支列出的范围**（本钩子 16 条 ＋ 同族兄弟件 74 条 ＋ 四支仓库级门禁 ＋ openspec --strict），
ff 前若需全量数，请在无并发窗口由看护者串行复跑一次。

## 三、改法要点（与 `#535` 同族的通用判据）

**任何「写死的数量／日期」都会在下一次变化时自动转红。** 本行是「写死的数量」，`#535` 是
「写死的日期」。修法不是把常量改成新值（那只是把定时炸弹推后一格），而是**换判据**——
改为校验对象**自身的自洽性**：

- `#535`：夹具日期 ⇒ 改相对构造（相对「今天」算），不写绝对日期
- `#537`：锚点条目数 ⇒ 改锚点集自洽（编号连续／不重复／编号合法／非空），不数总数

## 四、未动、已升格待他定夺的一处

`$script:TotalByteCap = 300` 总预算未随条目数上调 ⇒ 6 条时每条硬顶由 ~57 B 降到 45 B。
属口径阈值（🟡 档），泳道无权自决，已立 §四 `#192`（含 (a)/(b)/(c) 三选项与推荐项）。

## 五、`2a9b825` 补的两道挂死防线（自查上一笔时发现，同批钉死）

> `e072d3b` 为纯注释订正：段首注释仍写「三族」且把护栏与判据混为一谈，已分清（零行为改动）。

新判据本身引入了两处**比原缺陷更严重**的失效面：

1. **枚举挂死** —— 缺号检查按 `1..max` 枚举，而正则 `\d+` 无上界 ⇒ 一个笔误 `UPS5:999999999`
   会让钩子枚举 1..十亿。**UserPromptSubmit 挂死 ＝ 每一轮对话都卡住**，比它要防的告警噪声严重得多。
   已加 `$script:AnchorIndexSanityCap = 100`：超限编号报「编号异常偏大：N」并排除出枚举，其余条目照常注入。
   🔴 该常量是**防御性护栏、不是「常驻纪律最多 100 条」这种业务判据**——注释已写明，
   免得下一个人把它当成又一个「写死的数量」去改。
2. **溢出打掉钩子本身** —— `[int]$m.Groups[1].Value` 对 >2^31 抛异常 ⇒ 整枚钩子走进 catch、
   每轮只剩一行报错、常驻纪律一条都注入不了。已改 `[int]::TryParse`，解析不了的编号报
   「编号无法解析：…」，其余条目照常注入。

单测各加一条（后者显式断言输出里**没有**「自身报错」字样、且其余条目仍在），spec delta 同批补两个 Scenario。


---

# §一 #537 · 外置段（2026-09-11 外置，工具第 1 批，单段切分，原文原样）

> 外置人 OP-0910-A；外置前整格 4409 B、单段无 `━━━`；切在「标记前」边界，行内留切点前 3549 B、以下为切点后 859 B。🔴 **以下正文直接接在行内结尾之后**（行内末「…全量数，请由看护者在无并发窗口串行复跑一次。**」→ 本批首「🔴 **根 `CLAUDE.md` 正文一字未改…」），不是另一段。

🔴 **根 `CLAUDE.md` 正文一字未改，`UPS5:6` 不回退、不删**（按本行原文要求；改的只是数它的那段代码）。 ⚠️ **未动、已升格的一处**：`TotalByteCap = 300` 总预算未随条目数上调 ⇒ 6 条时每条硬顶由 ~57 B 降到 45 B；属口径阈值 🟡 档、泳道无权自决，已立 §四 `#192`（含三选项与推荐项）。 📦 **立行时原文与完整回显已按 K2 外置**：`1-转型规划/0-全景路线图/队列行日志/#537.md`。 ✅ **已 ff 入 master**（Shao Peishen 2026-09-10 答 `1a`；看护者 OP-0910-G 10:0x rebase+`--ff-only`，master `6e04f00`→`c31b264` 已 push；前置＝集成树全量 `pytest 0-学习与工具` 4 failed/2136 passed，四红与纯 master 同名同红＝存量，本批引入 0；手段与回显见看护件 B-0910 §四bis「ff 入 master」节）。
