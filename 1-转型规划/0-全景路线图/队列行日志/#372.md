---
title: "队列行日志 #372（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #372 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #372 · 外置前整格原文（2026-09-04 外置，31 段）

[S:partial][D:业] （08-24 值周对账审计刷新：本格「财务部#15 已起草待审」字样已过时——该信已批准并于 2026-08-23 08:26 UTC 推送、现等回件；信状态以跟进信 README「发送状态」列为唯一权威，队列只留指针）━━━ 🔴 **`财务部#15` 回件已到并回灌（2026-08-24 10:26，Cowork 业务总线 `OP-0824-B`）—— 两问都答了，而其中一问把 design 决策点 4 的整段推理前提推翻了** ━━━ **形态**：0 批注／0 修订／**6 个真复选框一个都没勾（`w14:checked val` 全为 0）**、55 处高亮 ⇒ **她第二次用「在正文里插高亮段」作答**（`财务部#14` 亦然）。🔑 **⇒ 真复选框对她无效，两次为证**：v3.4「docx 必发」那条要的是「能编辑、能喂 AI」，**不是勾选控件**；此后给她的判例表**不必再指望她勾**，留出可写字的位置更实际。 ━━━ 🔴 **⑴ Q2 跨月未立账 ⇒ 她选 (b)，并给出一条我方完全不知道的操作事实** —— 原话：「**如果当期未立账的 AP 单在当期关账后仍未立账，李姣龙都会手动在 ERP 系统内将该未立账 AP 单的『立账日期』改为下个月**」⇒ 当系统日期进入下一会计期间时，上期跨期的 AP 单日期也已被改到本期，**故不会漏**。**⇒ design 决策点 4 的整段推理前提就此不成立**：那段假设「一张 8 月未立账的单在 9 月会因期间过滤而落选」，**而现实中那张单的立账日期会被人工改成 9 月，它仍在当期范围内**。⇒ **(a) 的「静默丢单」风险在她的实操下不存在；我方推荐的 (c)「跨月尾单单列表」会是一张恒为空的表** —— 又一个「判据恒真、零信息量」，**幸好没照着实现**。 ⚠️ **但必须记下这条答案的依赖**：它成立的前提是**李姣龙每月手工改立账日期这个人工动作不出错**；**漏改一张，那张单就真的静默消失了**（因为筛选条件是当期）。⇒ **design 须显式记这条人工依赖，并考虑加一道「上期遗留检测」**——不是不信任她，是这条链上现在只有一个人、且失败不产生任何信号。 ━━━ 🔴 **⑵ Q3 表1 准入 ⇒ 她选乙，并且没有停在选项里，而是改进了方案** —— 原话：「选乙，**在表1 的标题列加一列『微差金额』列**，用以提取『金额微差（L2 门禁内）』的具体金额，**这样李姣龙就可以自行判断是否需要立账，还是需要返回采购人员进行修改后再立账**。」 🔑 **她绕开了我在信里指出的那个张力**（「自行消化」到底等不等于「可以直接立账」）：**不选边，给数据，让当班的人当场判。** ⇒ **表1 字段清单须新增「微差金额」一列**（`untaxed_amount_diff`，仅「金额微差」类有值，其余留空），design 决策点 7 的字段表须同步改。 ━━━ **⇒ 三条落进 apply 前置**：① 决策点 4 按 (b) 定稿，并把那条人工依赖与「上期遗留检测」写进 design；② 表1 加「微差金额」列；③ **F2 硬前置就此解除**（决策点 4 已由唐燕萍裁定）。🔴 **F1（`IsAP` 实测）与 F3（`§一 #371` 先修）仍未解除，本包仍不得 apply。** ━━━ ✅✅ **design 审已通过（Shao Peishen 2026-08-23 当日拍板「Design 审核通过」）＋ `财务部#15` 已起草待审（同日，Cowork 业务总线 `OP-0823-F`）** ━━━ **⑴ design 审四问按包内推荐落定**（原文 `openspec/changes/fi2-source-inversion/design.md` §八）：① 面板链接**从群文案删掉、改由 Excel 附件承载**（选 (b)；理由＝`simple_gate.py` Cookie 按 host 隔离**不区分端口**，把 8094 口令发给采购部 ＝ 连带开放 8091 保供看板／8092 命令中心／8093 QD-B）；② FI2 群推送**走 outbox 落盘轮询**（新起 webhook 已按 `#282` 否决）；③ 面板「按 AP 单号查询」**本包不做**（随 ① 选 (b) 后不需要）；④ **等 F1（`IsAP` 探测）解除再 apply，F2／F3 可与 mock 层并行**。 ⚠️ **「审通过」不等于「可以 apply」** —— 三条硬前置 F1（`IsAP` 字段实测，**不成立则本包整体不可实现**）／F2（决策点 4 由唐燕萍裁，已随 `财务部#15` 发问）／F3（`§一 #371` 必须先修，已随 `OP-0823-E` 派单）**一条未解除**。**审的是设计，不是开工许可。** ━━━ **⑵ `财务部#15` 已起草，`⏳ 待你审`** ＝ `6-人才与组织/部门AI专员跟进/财务部-唐燕萍-跟进-2026-08-23-起点反转方案已定稿与两处口径请裁.md` ＋ 同名 docx（6 个 `w14:checkbox` 真复选框、预勾选 0，已实测）。**只问两件**：**Q2 跨月未立账 AP 三选一**（design 决策点 4，推荐 (c)）＋ **新增 Q3「表1 收哪几类单」三选一**（源＝她 08-22 回件把表1 定成人工立账依据；争议只在「金额微差」这一档，**我方刻意不设推荐**——(乙) 逻辑最顺但它把「L2 自行消化」从「不用转人工复核」变成「可以直接立账」，那是不是一回事只有她能说）。**原 Q0（李姣龙角色与账号）已由 `财务部#14` 答齐，不再占额度。** 🔴 **⑶ Q1（N 值）本次刻意不问，并在信里写明了为什么** —— design §六 给的形式是「**首跑后**抽 ≤10 张真实滞留 AP 请她圈」，而**首跑一次都还没发生**，硬发就只能编 10 个假单号，正撞「判例包禁止虚构数据」；且上一封刚为「催了她早就交过的东西」道过歉。⇒ **等首跑后另发**，本场景开放点由 3 收到 1。 🔑 **这一条值得记**：**design 里写着「建议形式：判例批改表」，照着做反而会违规** —— 因为那个形式自带一个前置（真实数据已存在），而 design 写它的时候默认首跑已发生。**⇒ 凡「建议形式」类指示，落信前先问一句：它依赖的材料这一刻真的存在吗。** ━━━ 原状态：✅ **`财务部#15` 已拍板挂起，等 design 审完再起草（Shao Peishen 2026-08-23 答定夺 3 选 (a)）** —— 本行 ⑧ 列的 Q1／Q2，加上唐燕萍 08-22 回件新带出的 **Q3「表1 准入门槛：L2 自行消化算不算核对通过、要不要进表1」**，三问**全部是 design 的产物**；design 未审就起草，问出去的口径可能次日作废，而她刚为「被问了一件早已定稿的事」收到过一封道歉信。⇒ **`财务部#15` 的号留着不动**；期间若 §一 #371 修完需通知她，**走补件形态**（不占号、不触发闸），见 `OP-0823-E` §四.3。 ━━━ 🔴 **design 审的四个 Open Question 归 Shao Peishen，不在本次拆件范围**（原文 ＝ `openspec/changes/fi2-source-inversion/design.md` §八）：⑴ 面板链接是否从群文案删掉、改由 Excel 附件承载（**包内推荐 (b) 删链接**；权限边界＝ 8094 门禁四服务共用，给采购部等于连带开放 8091／8092／8093）；⑵ FI2 群推送走 outbox 落盘轮询 还是 接受笔记本关机即延迟（**推荐前者**；新起 webhook 已按 `#282` 否决）；⑶ 面板「按 AP 单号查询」是否纳入本包（若 ⑴ 选 (b) 可暂不做）；⑷ 是否等 F1／F2／F3 三条硬前置全解除再 apply（**推荐等 F1**，F2／F3 可与 mock 层并行）。**本次追记的㈠㈡两条不是这四问之一，是它们之外新增的输入，审的时候要一起过。** ━━━ 原状态：📤 **propose ＋ design 已出件，停在 design 审前（2026-08-22，CC，worktree `fi2-source-inversion-d8ff66`，commit `853509e` 已 ff push master）** —— 变更包 ＝ `openspec/changes/fi2-source-inversion/`（proposal ＋ design ＋ tasks ＋ 6 份 spec delta：新增 `fi2-ap-driven-intake`／`fi2-daily-workbook`／`fi2-recon-notify`，修改 `fi2-feed-source`／`fi2-result-classify`／`fi2-recon-report`）；`openspec validate --all --strict` 96/96 全绿。**边界守住**：一行生产代码未写、未碰 `.51`、未建群、未改推送、未改 `.env`、未与唐燕萍直接往返。 ━━━ 🔴 **① 最该被记住的一条：她说的「方向反转」，引擎内部早就是这个方向了** ━━━ `fi2/match_engine.py` 模块 docstring 原文写着「**迭代方向以 AP 驱动**（遍历 AP 侧聚合键，核验 INV 侧是否有支撑）」，`build_item_matches()` 本来就是拿 AP 去找发票、`has_invoice=False` 本来就是既有分支。**⇒ 真正在反转的是上游的入选集合，不是匹配数学** —— 落点只在 `feed_source`（哪些 AP 进来）＋ 结果分类与路由，`match_engine.py`／`price_check.py`／`config.py` 容差与判定优先级**零改动**，R1–R7 定稿与黄金基准全部继续有效。🔑 这条结论是本包风险可控的全部理由，也是「FI2 改了很多次、判据一次没漂」得以延续的原因。 ━━━ 🔴 **② 反转后多出一类此前根本不存在的结果，且分界锚点恰是她自己要的那个字段** ━━━ 反转前遍历发票 ⇒「找不到 AP」＝有票无单＝异常；反转后遍历全部未立账 AP ⇒「**找不到发票的，绝大多数是『发票还没到』这个正常状态**」。现行代码把 `has_invoice=False` 判 `无发票支撑` 且列在 `_NEEDS_REVIEW_CLASSES` 里 ⇒ **照旧不动的话，上线第一天就会把一大批正常在途单报成异常并 @ 采购制单人**。故新增中性态 **`待票`**（`l0_pending_invoice`，不进异常清单／不进表2／不 @ 任何人）与 **`长期待票`**（超 N 个工作日才转人工并点名）。**分界用什么判 —— 恰好是她要的「首次匹配日期」**：这个看似只是台账字段的东西，**实际是把「正常待票」与「异常滞留」分开的唯一锚点**；没有它就只能二选一：要么全报（噪音）、要么全不报（漏催）。**她提的两件事在这里合流了。** N 的 strawman ＝ 5 个工作日，待她定。 ━━━ **③ 首跑量级已定量（派单件要求「给一个数」）：约 100 张 AP 单 ／ 500–600 明细行，上界 180 张 ／ 约 1000 行** ━━━ 推导锚点两条且互相校验：⑴ `AP/Query` 全表 **> 26000 明细行**（design D15-a① 2026-07-21 实测）；⑵ 全库 9 个真实 AP 单号（`AP-2025120181`／`AP-2026040083`／`AP-2026060073`／`AP-2026070071` 等）按 `AP-YYYYMM+月内流水` 形态给出各月下界 ⇒ 单月约 100 张（区间 70–180）。两者交叉：26000 ÷ 100 张/月 ÷ 约 5 行/张 ≈ 52 个月，与已知最早 PO（`ZPCG20220815002`，2022-08）到 2026-07 的 48 个月吻合。**⇒ 不会「一夜冒出数千张历史单」——因为她的会计期间过滤把范围钉死在当月。** ⚠️ **但这个结论有前提，见 ④**。 ━━━ 🔴 **④ 她的两条规则互相矛盾，且矛盾的代价是静默丢单 —— 须她裁（`财务部#15`）** ━━━ 筛选条件写「会计期间 ＝ 系统日期所在会计期间」，终止条件写「只有 `IsAP` 变 Y 才移出」。**两条不能同时成立**：一张 8 月未立账的 AP，9 月 1 日会因「会计期间 ≠ 当期」落选，**但它的 `IsAP` 仍是 N、按终止条件不该被移出**。三种读法后果差一个数量级：(a) 期间优先 ⇒ 约 100 张但**跨月即静默丢单，且丢的恰是拖最久那张**；(b) 立账优先 ⇒ 量级不可估、可能数千张，首跑一次炸到几十上百位采购同事；(c) 期间优先＋跨月尾单单列 ⇒ 两个风险都没有。**我方推荐 (c)，但 MUST NOT 自行实现** —— 跨月未立账 AP 怎么处理涉暂估与期末结转，是她的账务口径。 ━━━ 🔴 **⑤ 三个关键字段全库零命中，且本次不在网段、无法实测** ━━━ `IsAP`／单据类型（用于「≠ 预付款」）／`AP 单制单人` —— **代码、测试、design、CHANGELOG、真实探测记录全库 `grep` 均 0 命中**；`Test-NetConnection 192.168.100.49:6666` 与 `192.168.100.51:8094` 本次均 False，本机磁盘无任何真实 AP 快照。**她看到的是 XML 视图（`<IsAP>N</IsAP>`），我方 `AP/Query` 走 JSON 信封，两者未必同字段集。** ⇒ design §二 已写成 **7 项探测清单（P1–P7）**，**P1 不成立则本包整体不可实现**，须转 IT 缺口跟催陈承。⚠️ 其中 **P5 极可能是个陷阱**：`dateFrom`/`dateTo` 过滤的是 `AccrueDate`＝**立账日期**，而未立账单据该字段很可能为空 ⇒ **用它做会计期间过滤会把要找的单全部排除，且返回 0 行不报错**。 ━━━ **⑥ 四条派单件未列、本线新识别的问题** ━━━ **⑴ 时序竞态（已自答，不必问她）**：她投放口径「工作日 10:00 前」／现行摄取任务 `Fi2TaxExportDailyScan` **10:30**／她要的核对时点 **10:00** ⇒ **10:00 核对时当天发票还没被摄取，读到的是昨天的 `invoice.csv`**，会让「未收到发票」这一判定系统性晚一天——**而它正是要 @ 采购同事的那一条**。改法＝步骤二核对前**内联**跑一次 `scan_once()`，不靠另一个定时任务「大概率先跑完」；既有 10:30 任务保留（它另担源头断供检测）。**⑵ 面板链接给采购部 ⇒ 命中 openspec 门槛②**：`simple_gate.py` docstring 自陈「Cookie 按 host 隔离、**不区分端口**……一次登录、四服务全通」，**把 8094 口令发给采购同事 ＝ 连带开放 8091 保供看板／8092 命令中心／8093 QD-B**（该门禁存在的理由正是「LAN 内任何人可看到真实客户名/立项书财务数据/供应商单价」）；且 `webapp.py` 全部路由只有 4 条、**根本没有「按 AP 单号查询」这个功能**，她的文案预设了一个不存在的入口。**推荐删掉链接、靠 Excel 附件承载**（她本就要求把汇总表作附件发群），须 Shao Peishen 在 design 审定（权限边界不归财务口径）。**⑶ `@` 人三层全空**：平台 notifier 与 aibot 连接器 **零 `mentioned_list` 支持**（全库 0 命中）、`KNOWN_RECIPIENT_USERIDS` 仅 6 人（**无李姣龙、无任何采购制单人**）、制单人字段本身待测；**且 MUST NOT 由姓名拼音推 userid** —— `陈承: "2023458"` 就证明本公司会猜错。降级方案＝正文纯文本列姓名，今天就能做且目的基本等效。**⑷ 推送通道三层前置全未到位**：按架构决策件 §4.2 判据这两条是**业务内容** ⇒ 必须走 aibot 群 chatid、**不得新起 webhook**（`#282` 拍板）；但群未建、`#282` ⑴ 包的「单一出口」未建、**跨机可达性未解**（aibot 唯一长连接在笔记本、FI2 在 `.51`，与 §5.2 记的 SC8 完全同构）。**⇒ 前置不解除则她要的每日两条群通知发不出去，交付的是「两份 Excel 落在 `.51` 而没有人知道它们生成了」**，这一点不得含糊。 ━━━ 🔴 **⑦ `#371` 由「该修」升级为本包的硬前置** ━━━ 同一个行级去重缺陷，**现在**只是「唐燕萍一个人在面板上看到翻倍数字」；**反转之后**发票侧金额翻倍 ⇒ 该 AP 超容差 ⇒ 判 `数量金额不符` ⇒ **进表2 ⇒ @ 该单采购制单人**。⇒ **一个我方已知的缺陷会以「你的单子对不上账」的形式每天自动推给采购部同事。** 本包 MUST NOT 被当作 `#371` 的替代方案。⚠️ 开工时已核 `#371` 状态为 `[S:open]` 无人在办，且本包只产 `.md`、与其触碰区零重叠。 ━━━ **⑧ 交回的问题清单（归 `财务部#15`，`财务部#14` 已被 OP-0821-L 占用）** ━━━ 共识别 **8 项**，而「一信 ≤3 决策点」是硬规则 ⇒ 只有 **Q1（N 定几个工作日，建议判例批改法：抽 ≤10 张真实滞留 AP 标出实际天数请她圈）** 与 **Q2（跨月未立账 AP 三选一）** 进信；**原 Q0（李姣龙角色与企微账号）已被 `财务部#14` §五 问过，不重复占额度**。其余 5 项（待核对明细表字段／未通过原因措辞／表1「金额」含税与否／请 IT 加服务端 `isAP` 过滤／删面板链接）由我方按 design 写明的假设先行，并在信里以**告知**形式列出，不隐瞒。 ━━━ **⑨ 四条 apply 硬前置（写进 tasks.md §0）** ━━━ **F1** 探测项 P1（`IsAP` 真实存在）｜**F2** 决策点 4 由唐燕萍裁定｜**F3** `#371` 已修并重跑验证｜**F4** 推送通道三层前置（非阻断，但决定交付价值）。**F1/F2/F3 未解除前 apply 不该开工。** ━━━ 2026-08-21 OP-0821-I 拆件立行；**Cowork 只落字未裁决**。 **并入审核（协议〇.10）**：同 #371 之审 —— FI2 侧 #295／#82 已 done，#251／#224 blocked 且触碰引擎判定侧，#339 为 FI3 定时行；无可承载行，按例外取号新建。
