---
title: "队列行日志 #552（§一）"
created: 2026-09-10
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #552 的建造完成报告外置件（K2 口径：单格 ≤4 KB，本段直接落此、行内只留收口结论＋验证手段＋本指针；原文原样、可 grep）。
配套: 0-学习与工具/工具-队列行K2外置.py；0-学习与工具/test_工具-队列行K2外置.py
---

# §一 #552 · 建造完成报告（2026-09-10 22:3x 本地，CC 泳道 `552-k2-tool`／`OP-0910-V`，批 `B-0910_夜批六泳道`）

> 行内只留收口结论；本件为完整对账原文。

━━━ ✅ **建造完成·待 ff（2026-09-10 22:3x 本地，CC 泳道 `552-k2-tool`／`OP-0910-V`）** —— 分支 `claude/op0910v-552-k2-tool` @ `dde1bac`（起自 master `4c28a85`，已 push；收工时 master 已前进到 `9cca471`，`rev-list --count master..分支`＝1／`分支..master`＝4，均为 docs 提交、与本棒两个新建文件零重叠——只记当时 SHA，不写「可 ff」）。**未 ff master**，🟡 留 sweep／看护者。

**期望产出逐条对账**

⑴✅ **单一入口工具** `0-学习与工具/工具-队列行K2外置.py`（约 480 行）：读单格（§一 状态／§四 事项，两份真身自动定位，两处命中即拒绝不猜）→ 按 `━━━` 切段 → `--keep` 选段（默认 `1,-1`＝首段＋末段）→ 外置件**只追加**（既有文件 `exists` ＋ 64 B 体积下限断言 ＋ 读后未被改动断言，`open(path, "a")`；不存在才走 `open(path, "x")` 创建——`x` 模式已存在即抛，物理上不可能覆盖）→ md5（内存预计算＋写后回读比对）→ **回读两侧**（旧内容锚点＝回读全文 `startswith(写前全文)`；新段＝每个外置段原文逐字命中；长度恰等于两者之和）→ 产出 `{"set": {"状态": "<新格>"}}` JSON（§四 为 `"事项"`），直接喂 `工具-共享文档编辑锁.py edit-row --changes-json`。🔴 **队列真身写入路径未动**——本工具一个字节都不写队列文件。

⑵✅ **段结构先断言再动手**：`--apply` 必带 `--expect-segments N`，实际段数≠N 即拒绝且不写；§一 首段必须以 `[S:` 起首且必须在 `--keep` 内（否则机器字段随外置消失）；plan 阶段打印每段「序号／字节数／分类／首 40 字」，分类＝KEEP／EXTERNALIZE／ALREADY-IN-LOG／POINTER→并入新指针。

⑶✅ **指针文案统一**：`📎 **N 段已于 <日期> 外置**（K2 口径，原文原样、可 grep，文件 md5:xxxxxxxx，工具第 K 批）见 \`<外置件路径>\`；本格只留原第 a、b 段（共 S 段）＋本指针（外置人 <who>）。` 其后按需追两句：「其中 n 段此前已在外置件内、本次未重复追加」／「此前 m 条指针段已并入本指针，先后各批原文均在同一文件」。旧指针段自本格移除时**其原文（含旧 md5）同批留档进外置件**，不留缺口。

⑷✅ **新格 <4096 B 才产出**：先算新格、先卡闸、再碰磁盘——超闸即报错，外置件一个字节不写、JSON 不产出，并提示换 `--keep`。

**幂等**：已在外置件内逐字出现的段判 ALREADY-IN-LOG，不重复追加、只从新格移除并在指针计数；旧指针段已留档的不重录；无新段且无待留档旧指针 ⇒ 不写文件、只重算 JSON。

**单测** `0-学习与工具/test_工具-队列行K2外置.py`：`python -m pytest 0-学习与工具/test_工具-队列行K2外置.py -q` ⇒ **28 passed（1.4–2.9 s）**。含两条原型实撞的验收用例：① `test_实撞1_保留段选错仍超闸_不写不产出`／`test_实撞1_对既有外置件同样一个字节不写`；② `test_实撞2_重跑不把同段追加两次`。另有静态守卫用例：源码 `.open(` 模式集合 ⊆ {a, x}、`write_text` 只许出现在 JSON 产物 `out_path` 上。

**变异检验（手段＝脚本替换源码一处 → 同命令跑单测 → 还原，四组）**：M1 `open("a")`→`open("w")` ⇒ **4 failed**（锚点用例／指针并入用例／追加函数用例／静态守卫）；M2 去幂等判定 ⇒ **2 failed**；M3 去新格超闸拒绝 ⇒ **2 failed**；M4 去段数断言 ⇒ **1 failed**；还原后 **28 passed**。

**真实数据冒烟（临时目录，复制两份队列真身＋既有 `#284.md`，不触碰真身）**：`#284`（5 段 4088 B，含 2026-09-04 手写指针段）⇒ 新格 1252 B，既有外置件 6541→7770 字符、`startswith` 前缀保留 True，第 2 段被正确判为 ALREADY-IN-LOG（当日手写外置已含它）、第 3 段旧指针原文留档；`#440`（7 段 4008 B）⇒ 1215 B；§四 `#118`（7 段 4043 B）⇒ 1786 B、外置件命名 `#118-四.md`；三者重跑外置件字节数不变。

**现网扫描结果（只读，`len(cell.encode("utf-8"))`，2026-09-10 22:2x）**：两份真身 §一/§四 中 >3500 B 共 35 格，**>4096 B 仅 1 格＝§一 `#537`（4409 B，1 段）**；`#529` 4014 B 带真实 `行长豁免：`。🔴 **边界如实写**：`#537`／`#544`／`#552` 本行均为**单段格**（无 `━━━`），本工具按段切分对它们无能为力——单段超闸格的出路是写入侧先分段，或另立句级切分（未做，属新判据，不在本行 ⑴–⑷ 范围）。

**未做／留步**：未对任何真实队列行执行 `--apply`（本泳道触碰区为「新建工具＋其单测＋队列行日志只追加」，真实外置属各行作者线动作）；未 ff master（🟡）；hook 白名单 `hooks-pretooluse-queue-read-guard.ps1::AllowlistedToolScripts` 未加本工具——本工具命令行不含队列文件名与读动词，现状可通过，是否入白名单交总线判。

---

# §一 #552 · 外置段（2026-09-11 外置，工具第 1 批，1 段，原文原样）

> 外置人 OP-0910-A；外置前整格 5088 B、共 4 段；本批外置第 2 段；本格保留第 1、4 段。

【立行原文·18:3x `OP-0910-A`】🟢 **可领 —— 2026-09-10 18:3x 立行（Cowork 业务总线 `OP-0910-A`）**。 🔴 **成因是当天的一次真实覆盖事故，原样记下**：`#550` 追加证伪段后单格 5001 B 超 4096 闸，本方现写了一个 K2 外置脚本，对既有外置件用 `write_text` **整体覆盖**——而 `1-转型规划/0-全景路线图/队列行日志/#550.md` 是 `OP-0910-R` 十几分钟前刚写的建造对账全文（3178 B），**一行代码清成 2079 B**。靠 sweep 已在 18:17 那轮把它 commit 进 `9ea7a40`，`git checkout --` 才救回；**若 sweep 晚跑一轮，这份内容就永久没了**。 🔑 **同一支笔当天写过安全版**：`#455` 那次的脚本里有 `assert log.exists(), "既有外置件不在，停手"` 且走**追加**；十分钟后写 `#550` 那版时没照抄，改成了无条件覆盖。⇒ **问题不是不懂，是每次都从零手写**——K2 外置至今没有工具，全靠一次性脚本，而一次性脚本的安全性取决于当次记得多少。**同族＝「写进文本的纪律不会自己被执行」**（`UPS5:7` 的 agent 版，主语这次是本方的手）。 **要什么**：⑴ 单一入口工具，外置件**只追加、绝不覆盖**，写前 `exists` ＋ 体积下限断言，写后**回读两侧**（新段命中 ＋ 旧内容锚点仍在）；⑵ 段切分不猜结构——段数与首末段形态先断言再动手（`#455` 那次实撞：假设 3 段实为 4 段，断言当场拦下）；⑶ 行内指针文案统一，含 md5 与「本格只留哪几段」；⑷ 新格字节数 < 4096 才产出，否则报错不写。 ⚠️ **⏰ 09-11 起行长闸转阻断**，届时超限行连销号都做不了 ⇒ 本行优先级高于一般机制项。 🔴 **WIP豁免：立行时机制类可动 WIP 现取超上限 22**；豁免依据＝它护的是**队列行原文本身**，失手即不可逆数据丢失（本次已发生一次、仅靠 sweep 侥幸兜住）。 预留豁免：本次 acquire 的 note 写的是「550 根因证伪回写」，取号需求是在持锁**之后**才浮出的——K2 覆盖事故发生在本窗口内，立行是它的直接后果。持锁瞬间高水位线 #551，本行取 #552 为紧邻续排、无跳号、无他人在途占用（同 2026-09-10 早些时候 #549 的既知形态：预留与写入分处两个锁窗口时必然触发，正确处置是写豁免不是换号）。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 3 段）：

📎 **建造完成报告全文**（逐条对账／变异回显／冒烟／扫描，原文原样、可 grep，md5:8d03d0fc）见 `1-转型规划/0-全景路线图/队列行日志/#552.md`；本格只留收口段＋立行原文段＋本指针（外置人 CC `OP-0910-V`）。

---

# §一 #552 · 外置段（2026-09-11 外置，工具第 2 批，2 段，原文原样）

> 外置人 CC OP-0911-F；外置前整格 5443 B、共 5 段；本批外置第 3、4 段；本格保留第 1、5 段。

🟩 **已进 master（2026-09-11 05:3x，Cowork 业务总线 `OP-0910-A`；Shao Peishen 答 `2a`）**：`dde1bac` → rebase → **`945435b`**，master `5b72a34`→`945435b`。**回归**：`test_工具-队列行K2外置.py` **28 passed**（2.64 s）在 rebase 后的 worktree 里现跑。⇒ 🔴 **行内此前「已 push、未 ff」已过时，以本段为准。** 备份 ref `backup/op0910v-552-k2-tool-pre-rebase`＝`dde1bac`。**手段（四条同一套，逐条现取）**：备份 ref → 现取该分支唯一提交触碰面并与主 checkout 11 个脏文件逐条比对（**四条全部零交集**）→ 因 master 已前进、四条**均不能 `--ff-only`**（`master...分支` 各 11/1、11/1、11/1、7/1），故各起临时 worktree `git rebase master`，**四条全部零冲突** → 有单测的先在 worktree 里跑绿再 ff → 主 checkout `merge --ff-only` → `push origin master` ＋ `push --force-with-lease` 分支 → **四 ref 逐条核对一致** → 临时 worktree 清除。串行做，每条 ff 完下一条再 rebase 到新 master。

━━━

🧹 **2026-09-11 06:0x K2 清扫泳道 `k2-sweep-overlimit`（CC `OP-0911-B`，批 `B-0911_差集告警与超限清扫`）重扫结论：超限行 = 1，本泳道零外置**。手段＝按编辑锁 ⑪ 同一判据列（§一 `cells[5]` 状态／§四 `cells[1]` 事项，>4096 B）扫两份真身的工作区与 HEAD：工作区仅 `#537` 4409 B（HEAD 另有 `#553` 4773 B，已由他线 05:3x 收缩为 3775 B）。⑴ `#537` 单段格、无分隔符，本工具 plan 只给 `[1] KEEP`，即本行已登的「单段超闸」形态缺口 ⇒ **做不了、未硬凑、未写豁免、未触碰该行**（⑪ 只判持锁期间 touched 行，不动它即不阻断任何 release）。⑵ 看护件「现取清单」10 行中 9 行（`#282/#284/#312/#324/#366/#381/#416/#372/#373`）**为量错列**：其数值＝§一任务列字节 +2（如 `#284` 13990 ↔ 任务列 13988），状态列实为 1260～4088 B、早于 09-04 `8384db6`／`f28cbb0` 已外置——任务列不在 ⑪ 判据内，无需动作；Cowork 侧扫描请改量状态列。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-11 外置**（K2 口径，原文原样、可 grep，文件 md5:2c633a5a，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#552.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 OP-0910-A）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。
