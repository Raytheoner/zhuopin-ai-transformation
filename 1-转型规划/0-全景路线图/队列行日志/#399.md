---
title: "队列行日志 #399（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #399 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #399 · 外置前整格原文（2026-09-04 外置，25 段）

[S:blocked][D:机] ✅ **apply 已完成、单测已本地实跑取证；仅剩 6.4 端到端真实发送留步（2026-08-25 15:12 本地，CC 无头批处理 A29）** —— 分支 `claude/a29-supplement-channel-apply`，commit `39d61d0`，**已 push 远端**；`openspec validate --all --strict` **114/114 通过**。 ━━━ **改了什么** ━━━ **①(a)** 补件登记由列表项改为 README 内**独立第二张表**（首列刻意叫「承接编号」不叫「编号」），四处消费者零改动零过滤 —— 🔴 **免疫来自「它们读不到那张表」，不来自「它们记得跳过补件」**。**②(b)** 两份独立 README 解析实现（`aibot_service.readme_table` ＋ `工具-共享文档编辑锁.py::_followup_readme_rows`）**同批**改为按 `##` 章节标题显式选表，不传 `section` 即强制匹配主表标题、匹配不到即 raise，MUST NOT 回退到「第一个表」；第三处同源函数 `_followup_header_col_index` 一并显式化。**⑤(a)+(b)** 反例单测两份实现各一条，**各配一条变异验证**（把改判前的原实现喂给同一条判据必须变红——不能变红的测试是空转，同 #355 教训）；编辑锁 `release` 断言两表章节标题均在；(c) sweep 告警按签认不做。**③(b)** `push_followup_letter.py`／`approve_followup_letter.py` 各加 `--table 主表／补件`，补件不进自动班次；🔴 **三案共同强制项已落地**：班次每轮显式打印补件表待发行数，**含 `--dry-run`、含 0 行**。**④(b)** 补件表加显式「需回复」列（留空即拒绝批准且不启动冷却计时器），否→`✅ 无需回复`、是→`✅ 已推送 <日期>`；读不到该列按签认型（两方向代价不对称）。**⑥(b)** 审计并回 `followup_*` ＋ `decision.kind="supplement"`，历史 3 条 `supplement_*` 未动。 ━━━ **#400 已闭环（含存量实例）** ━━━ 批准转终态时**同一次写入**剥括注（`write_cells` 一次写两格 —— 分两次写就多出「状态改了、括注没改」的中间态，**而那正是 #400 本身**）；**变异验证已用 `采购部#18` 真实两格对生产 README 实跑**：`采购部#18（待你审，暂不占号）` → `采购部#18`（只剥括注、编号数值未动），复喂幂等无修改；闸查询 `number_status_mismatch` 告警随之消失，README 顶部段「下一个可用号」由 #18 校准为 **#19**（同族第六次复发，本轮由本包抓获并写明「此后取号一律以 `工具-跟进闸查询.py` 输出为准」）。 ━━━ **真身取证（非单测，直接对生产 README 跑新代码）** ━━━ 主表 **54 行**；**去掉补件表整节后仍 54 行、逐字相等**；**把补件表整节挪到主表之前（一次纯排版编辑）主表读数仍逐字不变** ⇒ 隐式顺序判据确已退休。`dispatch --dry-run` 由此前那句会骗人的「无「🆕 待发」行」变为两行：主表结论 ＋ `[补件表] 共 3 行，其中「🆕 待发」0 行……本通道不自动发送补件`。闸查询 `--all` 五位收信人逐一对照：**无一闸状态改变**，唐燕萍仍 ✅ 开、依据仍 `财务部#15`（对照 design 实测：补件若进主表，她的闸在 `⏳ 待你审` 与 `✅ 已推送` **两态下都被锁死**）。**价值指标 3 → 0**。 ━━━ **回归（CI 长期红故一律本地实跑）** ━━━ 服务 **534 passed**（498 基线＋36 新增）／平台 **380 passed 1 skipped**／工具基线两份 **262 passed** ＋ 新增一份 **11 passed**，**零漂移**；改动后失败的 8 例全在 `FollowupReadmeStructuralValidationTests`，成因＝其 fixture 只写主表章节 ⇒ 按新契约本就不合法，**判为改判、非放宽**，已补齐。 ━━━ 🔴 **三项留步，如实登记（非遗漏）** ━━━ **⑴ tasks 6.4 端到端真实发送复现未做** —— 它须**真的发一条消息给唐燕萍**（那封合并补件已于 08-25 06:56 发出，重跑等于再发一遍），本次为无人在场的无头批处理会话，对外真实消息一律留步不发。**⚠️ 与 LAN／`.51` 无关**（补件通道走公网 aibot，且本服务已由 Shao Peishen 2026-08-13 拍板永不部署 `.51`）；留步的原因只有一个：**它会真的发给一个真人**。**⑵ 变更包暂不归档**（`暂不归档` 标记已写进 tasks.md 抬头，供 `工具-变更包自动归档.py` 读取）—— 6.4 未闭合，tasks 未全 [x]。**⑶ ff 合入 master 未做** —— 主工作区 `.git/index.lock` 已存在 27.7 分钟、0 字节、无 git 命令进程持有（疑似 A30 收工时遗留的陈旧锁），按 §二 `B-0825_A23` 先例「竞争共享资源优先让路」**未强删、未强推**；分支已 push 远端，无成果丢失，待人在场或锁自然释放后 ff。⚠️ **未合期间不构成风险**：补件表排在主表之后，master 上的旧代码取「第一个表」仍取到主表 —— **但这正是本包要退休的那份运气，合入后才真正不靠它**。 ━━━ 📌 **一处交付形态需知**：README 改动落在**共享主工作区**（协议〇.5 明警「切勿为落库单开新 worktree，否则新 worktree 看不到主工作区里未提交的改动」），代码改动落在独立 worktree/分支；两处分别登记在 §二 同一批次行内。 ━━━ 原状态：🔵 **apply 在办（2026-08-25 14:27 本地，CC 无头批处理 A29，worktree `a29-supplement-apply`，分支 `claude/a29-supplement-channel-apply`）** —— §四 #119 六点全批（①a②b③b④b⑤a+b⑥b）后 apply 解锁，本班领活。⚠️ **本行由 `[S:blocked]` 转 `[S:open]`**：协议〇.9 三类排除之②（依赖外部方·等签认）的前提已随拍板消失，故不再结构性排除；本次只改状态字段、未新增 §一 行，故 release 的机制类 WIP 上限门禁（其实现只对本持锁窗口内**新增**机制行生效）不触发 —— **如实记这一句，免得日后被读成一次绕过**。 ━━━ 原状态：🟡 **propose＋design 已出件，停在 design 审（2026-08-25，CC 无头批处理 A24，worktree `a24-supplement-channel`，分支 `claude/a24-supplement-channel`，commit `efe485f` 已 push、未合 master，`openspec validate --all --strict` 114/114 通过）** —— 变更包 `openspec/changes/followup-supplement-channel/`（proposal／design／tasks ＋ `wecom-followup-supplement`（新增）／`wecom-followup-review-state`／`wecom-followup-auto-dispatch`／`followup-gate-query` 四份 spec）。🔴 **未 apply、未改任何一行生产源码**（opener 明令「只 propose＋design 出件、登记 §四 待审即停」）。**六个决策点全部无默认项**（提出方是无人在场的无头批处理会话、收工即结束，按根 CLAUDE.md §5「答完之后由谁动手」判据答不出执行者），**已登记 §四 #119 待签认**。⚠️ **本行按协议〇.9 三类排除之②（依赖外部方——等签认）转 `[S:blocked]`**，不计入可动 WIP（沿用 #353／#397 已开的先例）。 ✅ **本行左侧「修复后立即发出上述两封」已由他途兑现**：两封已并成合并件、2026-08-25 06:56 走一次性直发脚本发出（见 §二 `B-0825_5`）⇒ **本行不再阻塞任何在途件**，只剩机制化治本。 ━━━ 🔑 **design 阶段三条实测结论（取证脚本只读＋内存改写，不落盘）** ━━━ **⑴ 「等它发完就好了」是错的 —— 补件放回主表，两种状态下闸都是锁的。** A1 当时只测了 `⏳ 待你审` 那一态；本轮把**补件已经发出去之后**（`✅ 已推送`）也测了，**闸照样锁**。原因很实在：`followup_gate` 的闭环四态里没有一个能描述「一封不需要回复、已经送到的补件」，而 `✅ 已推送` 明明白白属于 `IN_FLIGHT` ⇒ **补件一旦进主表，这个收信人的闸就从此不再打开，直到有人手工给它编一个闭环态**。 **⑵ 补件转「已推送」后会开始截胡本属正式信的回件，而配对结果看起来完全正常。** 实测同一封新回件：补件行 `⏳ 待你审` 时配给 `财务部#15`（对——「还没发出的信不可能收到回件」这条判据挡住了它）；补件行转 `✅ 已推送` 后**配给了补件行**（`channel=latest`）。**两次都没有报错、没有 warning、没有低置信标记。** ⇒ `NOT_YET_SENT_STATUS_PREFIXES` 只挡得住「还没发出的」，挡不住「发出了但不该参与配对的」。 **⑶ 两份独立实现，同时依赖着一条谁都没写下来的判据：谁排在文件前面。** `aibot_service.readme_table.iter_rows` 与 `工具-共享文档编辑锁.py::_followup_readme_rows` 是刻意独立的两份（编辑锁 docstring 明写「避免额外的跨包耦合」），**两份都取「文件里第一个以竖线开头且含『发送状态』的行」当表头**。它今天正确，**只因为补件表恰好排在主表后面** —— 把补件表挪到主表之前（一次纯排版编辑），**两份会同时把补件表读成主表，且都不报错**。⇒ 这就是本包按协议〇.9 措施 B 退休的那个守卫：**一条无人知道自己在守的人守**。 ━━━ **另两处实测** ━━━ ⑷ 现状 `dispatch_followup_letters.py --dry-run` 输出「无「🆕 待发」行」——**它不是「跳过了 N 行补件」、也不是任何告警，而是一句完全正常的「今天没活」**（同族＝根 CLAUDE.md「工具静默回退」）；⑸ 补件若复用父信编号进主表，`followup_readme_bridge._letter_rows` 的 `locations[number]` **静默覆盖 1 条**（信行数 55 ／ 键数 54）⇒ **「补件不占号」与「放回主表加过滤」在结构上直接冲突**。 ━━━ **定案方向（案 C，待签认）**：补件登记改为 README 内**独立第二张表** ＋ **按章节标题显式选表** —— 四处消费者（闸查询／入信桥／起草缺口／编辑锁串行闸）**零改动零过滤**，实测行数与输出逐字不变；**免疫来自「它们读不到那张表」，不来自「它们记得跳过补件」**。对照案 B（放回主表加排除）需 4 处过滤 ＋ 1 处保编号唯一，**5 个漏了都不报错的必守点**；案 A（留列表项各写一套解析）会把 README 解析实现由 2 份变 4 份。 ━━━ ⚠️ **design §五 已登记 4 项未闭合，其中第 1 项当下就有真实实例在等**：**签认型补件的回件无处配对** —— 唐燕萍对 2026-08-25 合并补件的甲／乙签认一旦回来，**那封回件会被入信桥配给 `财务部#15`（正式信）而不是补件行**。本包无样本、不凭空设计，已请 design 审时一并决定是否另立行。 ━━━ 🔗 **并入的 #400 修法已成案**：批准脚本转终态时**同一次写入**剥除编号列 `UNNUMBERED_MARKERS` 括注（🔴 分两次写就多出一个「状态改了、括注没改」的中间态，**而那正是 #400 本身**）；只剥括注、不改编号数值（占号是读侧派生值，写侧不该动）。`followup_gate.number_status_mismatch` **本已能检出并 warning，缺的一直是「谁去清」那一半**。  —— 原状态：`[S:open][D:机] 待领·阻塞两封已授权补件`（来源＝2026-08-24 夜 opener 批处理 A1-A14 全 14 项跑完，run 20260824-165657；登记时间 2026-08-25 01:0x）　➕ **2026-08-25 §四 #119 六点全批（全按建议：①a②b③b④b⑤a+b⑥b）⇒ followup-supplement-channel apply 解锁**，待 CC 领活；附问（签认回件配对）机制修复并入 §一 #366 作 M6、不在本包。 ━━━ 🔴 **2026-08-25 15:27 本地 · 看护 session 收尾追记（Shao Peishen 当次授权，机械收尾、非代做 opener 内容）** ━━━ **⑴ 上方留步 ⑶「ff 合入 master 未做」已解除**：`.git/index.lock`（14:44:15 创建、0 B、陈旧 40.4 分钟）经三重取证确认无持有者——独占打开 `[System.IO.File]::Open(...,'ReadWrite','None')` 成功、锁创建前后两分钟内无 git.exe 新进程、`ZhuopinCommitSweep` 状态 Ready——后清除，`git merge --ff-only claude/a29-supplement-channel-apply` 成功，master `7665517` → `39d61d0`（顺带带入 A24 的 `efe485f`），`origin/master..master` ＝ 2，推送交 sweep。**留步 ⑴⑵ 不变**（6.4 端到端真实发送、变更包暂不归档）。 ━━━ **⑵ §一 #324／#326 裸竖线撑列处置已裁定 (c)＋(b)（Shao Peishen 2026-08-25）**：**(c)** 队列结构 lint 增设「§一 数据行必须 8 格」机器守 —— **openspec 评估随 #397 包同批**，不另立行；**(b)** #324（12 格）与 #326（9 格）**两行本体交下轮值周对账审计清**，本班未擅动他人行。🔑 判据：只修这两行是治标，同族已第 N 次复发（本包自身收工反查也撞了一次：`--table 主表／补件` 里的裸竖线把 #399 由 8 格撑成 9 格）—— **该由机器守的，不再留作人守**。　━━━　🔧 **2026-08-30 `OP-0829-W` 机制类 WIP 分诊改判：`[S:partial]` → `[S:blocked]`**（Shao Peishen 2026-08-30 答「全批」）。**依据＝本行自陈原话**「仅剩 6.4 端到端真实发送留步」——阻塞方不是「现在能动手的人」，不属可动负荷，故改判为 `blocked`。🔴 **只改状态字段，工作范围·优先级·领取方一字未动**；**刻意用 `blocked`／`timed=` 而不用 🛑** —— 三者都不计入可动 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 行内原话），会让本行从催办面整个消失；`blocked` 只是不占配额，仍可见可催。误判可一条命令改回。**根因（`partial` 被当作可动 ⇒ 主体做完留尾巴反而永久占配额）根治已并入 `#435` 子项 E。**
