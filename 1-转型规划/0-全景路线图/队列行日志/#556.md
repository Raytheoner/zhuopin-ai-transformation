---
title: "队列行日志 #556（§一）"
created: 2026-09-11
status: 生效
用途: 跨桌任务队列 §一 #556 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #556 · 外置段（2026-09-11 外置，工具第 1 批，2 段，原文原样）

> 外置人 CC-OP0911U-556design；外置前整格 5740 B、共 4 段；本批外置第 2、3 段；本格保留第 1、4 段。

🔎 **本次取证（2026-09-11 14:3x 本地，手段随结论）**：① `Test-NetConnection 192.168.100.51 -Port 445` ⇒ `TcpTestSucceeded=True` ⇒ **不是 off-LAN**；② `net use` ⇒ **无任何条目**；`Get-SmbConnection` ⇒ **空**；`cmdkey /list` ⇒ **无 192.168.100.51 的凭据**；③ 任何对该 UNC 的只读探针（`File.Exists`／`Directory.Exists`）**均挂住 >60 s 未返回**。⇒ **定性：端口通、SMB 认证不通**——配的是管理共享 `C$`，而本机无凭据；Windows 将此映成 `EINVAL`，故 Python 看到 `[Errno 22]`。🔴 **未做也不得代做**：未写入任何凭据、未建任何映射、未改机器人一行配置与代码（配置类变更需 Shao Peishen 明示授权）。

━━━

🔴 **重大更正：本行上段的定性错了，撤回（2026-09-11 15:4x，Cowork 环境总线 `OP-0911-A`）**。**被撤回的句子**：「`Test-NetConnection :445` ⇒ True ⇒ 不是 off-LAN」与「定性＝端口通、SMB 认证不通」。🔑 **证伪手段＝对照实验**（Shao Peishen 2026-09-11 指出他当刻 off-LAN 后补做）：对 `192.168.100.51`／`192.168.100.253`（同网段几乎必不存在）／`10.255.255.254`（另一网段保留地址）三个地址各跑一次，**三个全部 `TcpTestSucceeded=True`** ⇒ 该量具在本机**对任意 IP:port 恒为真**、零信息量。**成因实证**：`Mihomo / Meta Tunnel` TUN 网卡在跑（`clash-verge` PID 11752、`verge-mihomo`），系统代理 `127.0.0.1:7897` 开启，本机 IPv4 含 `198.18.0.1`（Clash fake-ip 段）；TCP 握手是和本地 TUN 打的，不是和目标。**真实网段＝ `192.168.3.35`（WLAN），不是 `192.168.100.x`** ⇒ 确实 off-LAN、`.51` 不可达。⇒ 🔴 **修正后的定性**：连续失败的直接原因就是 **off-LAN（含长期不在内网的常态）**；`cmdkey`／`net use` 空也不能当「认证不通」的证据——off-LAN 时本来就不会有连接。🟢 **不受影响、仍成立的部分**：失败已连续 11 天、占今日审计 52%、**「读不到」与「没消息」在下游同形**、且此前无行跟踪 —— **本行的立行理由不变**，但 **期望产出 ① 的问法改为「off-LAN 是常态，这条中继该不该在 off-LAN 时自知并静默」**，而不是去查凭据。

---

# §一 #556 · 外置段（2026-09-12 外置，工具第 2 批，1 段，原文原样）

> 外置人 OP-0910-H；外置前整格 3706 B、共 3 段；本批外置第 3 段；本格保留第 1 段。

🔎 **本次设计（2026-09-11，CC `OP-0911-U`，纯设计会话，未 apply）**：修法与①②③④四项期望产出已写入既有变更包 `openspec/changes/aibot-outbox-relay/`（不新开变更包——同一场景只应有一份答案），落点 `design.md` 决策点 9、`specs/aibot-outbox-relay/spec.md`（新增一条 Requirement）、`tasks.md` §9（10 条任务，全部未勾，留给 apply 泳道）、`proposal.md` 追补段。①**根因定性改判**：本条原问法「off-LAN 时该不该自知并静默」，答案是**否**——中继不得新增任何网络自检（`Test-NetConnection`/IP 网段比对），本行上段自己的取证已证明该类探针在本机 `Mihomo/Meta Tunnel` 接管默认路由时对任意 `IP:port` 恒真、零信息量，中继若复用同一量具会把真故障静默掉，比现在更危险。改判为**不问为什么，只问「是不是同一次没好」**：读失败按路径维护跨重启持久化的状态（`first_failed_at`/`last_alert_at`），转入即报、持续态每 6 小时（`WECOM_AIBOT_OUTBOX_UNREADABLE_REALERT_SECONDS` 可调）复报一次，恢复即报并清状态。②**可见化**：状态文件本身即信号——体检 CLI 读不到时追加打印「已连续不可读 X」；新增 sweep 一类（范式同 `#312` 陈化催办），`first_failed_at` 超 24 小时进值周清单；活人告警／体检／巡检三处对齐同一份状态。③**退噪**：审计通道不受影响（每轮照记，怕漏不怕多）；仅活人告警通道节流，首次转入不受节流影响、立即响。④**这 11 天没有真的漏发**——直读本机（非 `.51`）`5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`：`outbox_relay_delivered` 全文件仅 4 条，两两成对，`2026-08-31T06:20:31Z`（`2026-W34`）与 `2026-09-07T01:05:45Z`（`2026-W35`）各一次群＋姚祖怡私信、均 `errcode: 0`，且均发生在服务重连后数秒内——与决策点 5「宁可重发不可静默丢」＋决策点 2「启动立即跑第一轮」对得上：笔记本哪怕只是短暂碰一下那个网段，积压就会被立刻清空。⚠️ **同时发现一个未闭合的活问题、不得读成已解决**：本周 `2026-W36`（2026-09-11 周五）**截至取证时点仍无 `outbox_relay_delivered` 记录**，本机当前 off-LAN 无法确认是否已生成/已送达，已登进 `design.md` §三 L5，核验动作按触碰区约定转 `zhuopin-lan-closeout`、本行不代办。🟡 **决策点 9 未 apply**：机制/工具类模块改变既有模块对外语义，按根 `CLAUDE.md` §5 门槛须先过 design 审，泳道无权自行通过——`tasks.md` §9 十条任务待 Shao Peishen 审过设计后另开 apply 泳道认领。`openspec validate aibot-outbox-relay --strict` 与 `validate --all --strict`（189 passed / 0 failed）均已实跑通过。本行状态维持 `[S:open][D:机]`，不销号、不转 partial→done，WIP豁免原文不变（出血未止，只是止血方案已设计完成待批）。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **2 段已于 2026-09-11 外置**（K2 口径，原文原样、可 grep，文件 md5:c80c2fa3，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#556.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 CC-OP0911U-556design）。
