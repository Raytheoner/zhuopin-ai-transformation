---
title: "队列行日志 #98（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #98 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #98 · 外置前整格原文（2026-09-04 外置，45 段）

[S:timed=2026-09-25][D:机] ✅ **首期体检已执行（2026-08-24，Cowork 环境总线，A6 派单②；本行常驻不销，只滚动）——简报正本＝`1-转型规划/0-全景路线图/环境体检-2026-08-24-首期.md`**。**触点原定 08-28，提前 4 天执行**；提前不影响取证有效性（全为当刻实测），但 worktree 台面与 `.51` 可达性两项若在 08-28 前变化须复测。 **四面全部取到证，无数据风险，抓出 5 项新发现** ━━━ **⒜ 🔴 10 个 worktree 元数据缺 `commondir`（9 个仍在注册表内）** —— 症状＝`git -C <目录> status` 报 `fatal: not a git repository`，且 `git worktree list` 里显示为 `0000000`。**内容风险 0 已逐条核实**：7 条持有分支的 `git rev-list --count master..<分支>` 全为 0；2 条 detached 的 HEAD（`697da77`／`d9dce22`）经 `merge-base --is-ancestor` 均已在 master 内。⚠️ **但这 9 个目录有没有未提交脏改动本轮查不出**（查脏要跑 `git status`，而它们恰恰跑不了）⇒ 🔴 **清理顺序不可反：先补 `commondir`（内容 `../..`，健康样本实测值）恢复可读性 → 再逐个查脏 → 最后才 remove**。另有 24 个 0 文件空壳目录可直接删。**三方计数实测 19／20／44**，判据按本行 08-05 自己的建议改为**「不写死数字，只写关系式」：注册数＝元数据数，且 物理−注册＝空壳数，且空壳全为 0 文件**。🔑 **本项的教训**：`git worktree list` 照常把它们列出来还带着分支名，**只有真去用才发现是坏的**——列表命令读的是元数据的一部分，可用性取决于另一部分。 **⒝ 🔴 `Claude-Env-HealthCheck` 已静默停摆近两个月**：上次运行 2026-08-01 17:02 本地、结果 `0x800710E0`（操作员或管理员拒绝），而 `~/.claude/health-check.log` mtime 停在 **2026-07-02** ⇒ **08-01 那次失败没产生任何输出、也没通知任何人**；它负责的技能重名／插件缓存／CLAUDE.md 叠加三项检查已两月无人做。成因假说（未证实）＝`LogonType=Interactive`。**属本机工具链 ⇒ 交 CC**。 **⒞ 🔴 部署惯例存量违例 5 份**（无 BOM 且含中文）：`register-decision-reminder-task.ps1`／`register-followup-dispatch-task.ps1`（两份**注册脚本按设计在目标机执行 ⇒ 风险高**）、wecom-aibot-service 与 AI运营指挥中心 与 SC8 三份 `sync-to-server.ps1`（本机 pwsh7 跑、当前不触发，属定时炸弹）。⚠️ **未在 `.51` 实跑验证过会否真报错，「高」是基于坑 5 已实证机理的推断、不是本轮实测，如实标注。** **⒟ ✅ 安全挂账实质进展（意外撞见）**：`.51` 四个端口 8091-8094 实测**全部已加访问口令门**（`/_gate/login`，登录后 30 天本机免登录）⇒ 「四个平级服务零鉴权绑 0.0.0.0」这个原始挂账已被覆盖。 **⒠ ⚠️ 观察项**：§一 33 行中 **28 行带 ⚠️、合计 98 处** ⇒ 该标记已无区分度（同 §四 #58「非阻断提示信息量为零」形态）；本轮只做普查、未逐条判定，**如实说明范围**。 **⒡ ✅ 顺带推翻一条挂账**：`wecom-service-home` 本轮实测**落后 master ＝ 0、领先 ＝ 0** ⇒ §四 #68 登记的「执行体 worktree 落后 246 提交」**已不成立，可据此复核销号**（本轮只报不代销）。 **⒢ ⚠️ `queue-315-apply-9f2c1a` 落后 729 提交、脏 13 个**，全场最陈旧活体，建议交 CC 判定去留。 **⒢bis 🔴 收工写后反查顺带查出：§一 存量列数违例 3 行**——`#98` 自身 **10 列**（两处裸竖线来自 OP-0822-C 2026-08-22 写入的 PowerShell 管道符，**本次已就地全角化修复，复制执行前须换回半角**）、`#324` **12 列**、`#326` **9 列**（后两行本次未触碰，**故 release 校验① 不会拦、也从未拦过**——① 只对本次改动行生效）。🔑 **这说明「列数合规」这个读数同样只在被触碰时才被检验**：一行只要没人再动它，写歪了就一直歪着，**且 §一 的下游解析器按列下标取值，10 列意味着「状态列」被读成了别的东西**。**建议交 CC 顺手修 #324／#326，并考虑给 `工具-队列结构lint.py` 加一条全量列数体检**（lint 是全量跑的，不受「只查改动行」限制）。 **⒣ OP-0822-C 指定的常量对齐核对：✅ 对齐**（`RESIDENT_SERVICE_ENTRYPOINT_GLOBS=("run-*.vbs",)`／`REGISTRAR_GLOBS=("register-*.ps1",)` 与三个任务实测 Action 一致）；顺带观察＝第四个任务 `ZhuopinCommitSweep` 的入口`0-学习与工具/run-commit-sweep.ps1` 在服务目录之外、**不在该告警族覆盖内**，改它不会触发任何提示，留下期议题。 ━━━ ✅ **并入项一（仓库外载体旧指针核查 63／67 逐类核定）＝本轮完成，可销** ━━━ **① Cowork artifacts 命中 67 处：活载体（`index.html`）仅 2 处，且两处都是注释、内容正是「别读那个文件」的告警 ⇒ 活载体旧指针欠账 ＝ 0；其余 65 处全在 `versions/` 历史快照里，按定义不该改。** 🔑 **「63／67 处待核」这个数字的 97% 是噪音——真实待修是 0 处。** **② `.51` 四服务页面命中 0 —— 🔴 假零，已证伪**：本轮 `.51` 在 LAN 内可达，去掉 `--skip-http` 真跑，四个端口全 200；**但取回原文一看，1.6 KB 全是访问口令登录页**（即 ⒟ 那个安全改进的副作用）⇒ **工具扫的是登录页、不是看板正文，此后永远命中 0**。🔑 **这比 08-11 那次严重：那次是人没逐类核对；这次是工具真跑了、真返回 200、真报了零，结论仍是错的——判据没坏，目标变了，而判据不知道目标变了。** **③ 已安装版 skill 命中 0 —— 🔴 假零，已用阳性对照证伪**：换关键词 `跨桌任务队列-机制环境.md` 重跑，① 类命中 12 处、④ 类 2 处，**③ 类仍然 0** ⇒ 不是关键词问题。直接清点：扫描路径 `~/.claude/skills` 下**只有 2 个与本项目无关的 skill**；本项目 6 个 `zhuopin-*` skill 全盘定位**只找到仓库内源码** `0-学习与工具/skills源码/`，**已安装版在 Cowork 侧、不落本机磁盘 ⇒ 这一类按当前实现永远核不了**。 **④ 定时任务真身命中 2 处，均为良性**（两处原文正是 08-13 修复留下的「照旧指针读会静默空巡」告警文本）。 **⇒ 本行悬着的那个问题「剩余命中里是否还有第 5、第 6 个受害载体从未核过」＝有，而且 ② ③ 两整类从未被真正核过。** ⇒ 🔴 **处置（交 CC）：该工具须改为对无法核验的类输出「本类无法核验」而不是「命中 0 处」**——否则任何据其零命中做出的「已复检」声明都是无效声明（协议〇.8 那条「已复检须覆盖仓库外载体」正依赖它）。 ━━━ ✅ **并入项二（台账份数漂移残余核验）＝本轮完成，可销，但判据已修正** ━━━ **原判据「主工作区与各存活 worktree 份数一致」在本轮实测下不成立且不该用**：所有存活 worktree 落后 master 22～729 提交，**份数不同是正确的**，拿相等当判据会把正确结果判成故障。**改用与 checkout 新旧无关的等价判据：缓存目录物理存在，而台账零收录。** **实测（非破坏性，未重跑生成器）**：主工作区现存 `.pytest_cache` 16 个、`__pycache__` 39 个；台账（mtime 2026-08-24 19:10）内 `.pytest_cache` 命中 0 行、`__pycache__` 命中 1 行且**已逐行核实是表头说明文字、非数据行**；台账读数 418 份／27 待补。⇒ **排除逻辑在生产真实生效。** **建议把本行内记的该判据一并改掉**——理由同本行 08-05 自己写下的那句：会随环境漂移的量不该被写成判据。 ━━━ **下期（2026-09-25，9 月最后周五）执行清单已写在简报 §九**，其中第 3 条为硬判据：**若 ②③ 两类仍返回「命中 0 处」而未改为「无法核验」，视为该工具仍不可信，不得据其零命中做任何「已复检」声明。** ━━━ 以下为本行原有内容 ━━━ ➕ **并入一项：仓库外载体旧指针核查的真实欠账（2026-08-17 值周巡检并入，源＝#315 `tasks.md` 6.6 退回 `[ ]`；协议〇.10 并入优先，未取号）** ━━━ **要做什么**：重跑 `python 0-学习与工具/工具-仓库外载体扫描.py "跨桌任务队列.md" --skip-http`，把命中的 **63／67 处按「载体」分组逐类核定**——**每一类的「零命中」都要独立证伪，不得因为某一类非零就认定其余各类的零是真的**。 **为何挂账**：2026-08-11 那次 6.6 标 `[x]`，原文写「②③④ 其余三类载体零命中（…、`Claude\Scheduled` 定时任务真身）」，**而那两份 `Claude\Scheduled\*\SKILL.md` 当时就含旧指针**（拆件巡逻件 mtime 08-08、值周巡检件 mtime 08-09，均早于拆分日），**照旧指针读会解析到零行并静默输出空巡／空计划，已带病运行两天**，2026-08-13 实测撞上才发现并当场修复。**2026-08-13 用同一命令同一关键词复跑，工具准确报出「④ 定时任务真身命中 3 处」并逐行给出路径行号 ⇒ 工具没问题，那次「零命中」是误记。** **成因未定、两候选**：⒜ 只读了输出里最显眼的「① 命中 63 处」，②③④ 未逐类核对即写零；⒝ 当时确有一个此后被修掉的 bug ——**决定性证据＝08-11 那次原始输出，未留存 ⇒ 无法判定，如实登记为未决，不猜。** 🔑 **6.6 自己写下的立论在这里反噬了**：它说「一个只会返回零命中的检查，无法证明自己还活着」，据此判自己真实通过——**但那个非零来自类别 ①，而 ②③④ 的零里至少有一个是假零**。这是「枚举法会反复给出『已完备』的错觉」的**第五个实例**（前四：#300 入口清单／G6 CLI 穷举维度／#314③ 消费者清单／#321 README 全表比对）。 **⚠️ 已知未做**：两份定时任务件之外，**剩余命中里是否还有第 5、第 6 个受害载体从未核过**——2026-08-13 只处理了实测撞见的定时任务这一类。 **▸ 并入审核结论（协议〇.10）**：已逐条过 §一 待领池 —— **#338**（名为「载体健康守卫」但实为 `CLAUDE.md` 尺寸与跨 worktree 漂移，主题不同，且其五项预授权带「五项之外不得扩权」硬条款，塞入即违反）／**#327**（孤儿内容，无关）／**#328**（sweep 起跑段，无关）／**#337**（场景级 CHANGELOG，无关）**均不可并入**；**本行是唯一合适归宿**——它是**核查类、四面取证、首期 2026-08-28**，本项恰为第五面，时点亦吻合（距今 11 天，且死指针已修、剩余核查不紧急）。 ⚠️ **一处先例差异须说明**：#315 自身 2026-08-10 的并入审核曾判「#98 月度例行常驻行，不承接建造」——**那条判断针对的是「双队列拆分」这类建造任务，本项是核查任务，正是体检的本职**，故不冲突。 **▸ 未取号理由**：机制类可动 WIP 本轮实测 **24／16 超限**，§四 #58 执行口径为「超限期间不新立机制类行（除非同时关一条）」；**审出可并入却仍新建，才是违规。** ➕ **并入第二项：文档台账份数会随「有没有跑过 pytest」漂移——✅ 已于 2026-08-17 当日修完（CC，worktree `vigilant-snyder-d7a3b8`／分支 `claude/vigilant-snyder-d7a3b8`，commit `5d592b9`，已 ff 入 master；协议〇.10 并入优先，未取号）** ━━━ **问题**：`0-学习与工具/工具-文档台账生成.py` 扫描时只跳过 `__pycache__`，把 pytest 生成的 `.pytest_cache/README.md` 当项目文档收录——实测同一 commit 下 **主工作区 338 份／20 份待补状态头，worktree 内 335／17**，差的 3 份全部是它（`0-学习与工具/`、`0-学习与工具/md转Word工具/`、`1-转型规划/AI运营指挥中心/` 各一份）。 🔑 **值得修的不是那 3 份文件，是一个信号的可信度**：台账头部「共 N 份 md」因此**不是可比对的稳定量**，任何「份数变了 ⇒ 有人新增／删了文档」的判断都可能是假信号；且这 3 份**永远补不上状态头**（补了下次 pytest 重跑又没了），长期把「待补」计数顶高。 **修法**：新增目录名黑名单 `EXCLUDED_DIR_NAMES`（`.git`／`.pytest_cache`／`__pycache__`／`.mypy_cache`／`.ruff_cache`／`.tox`／`.venv`／`node_modules`）＋ 后缀型 `EXCLUDED_DIR_SUFFIXES`（`.egg-info`），按**路径的目录段**判定、不按文件名后缀猜；判定入参用**相对仓库根**的路径——若用绝对路径，仓库一旦被 clone 在名为 `node_modules` 之类的目录下会全库被静默判成产物、台账整个变空，正是 CLAUDE.md §5「工具静默回退」那一族。 **同址第二处一并修**：`0-学习与工具/补R1状态头.py` 有完全相同的缺陷，且后果更重（台账只是数错，它会**真的往 `.pytest_cache/README.md` 写 frontmatter**）；其判据从台账生成器 `import`，不抄第二份（#306／#307 口径）。 **验证**：份数 **338/20 → 335/17**（主工作区实测，非估算）；**且 worktree（刚跑过 pytest、`.pytest_cache` 真实存在）与主工作区的生成结果去时间戳后逐字一致 ⇒ 漂移消失**——这一条比份数本身更能证明修的是那个信号。 新增 6 个回归用例，含**反向用例**（清空黑名单后同一夹具立刻被收录并落入「待补状态头」，证明断言非恒真）；`0-学习与工具` 全量回归 **418 passed ＋ 26 subtests，零漂移**。 **▸ openspec 三门槛逐条核（CLAUDE.md §5）**：①**不改全项目口径**——全库 grep 确认**无任何其它工具消费该份数**（仅 `工具-落库sweep.py` 引用输出路径以触发重跑）；②**不涉鉴权与数据可见性**；③**既有函数对外语义不变**（`check_queue_row_column_counts`／`parse_frontmatter`／`status_bucket`／`classify_layer` 均未动），`main()` 输出变化即本次要修的 bug 本身，属该条明列的「不走：纯 bugfix」⇒ **三条均不命中，不走 openspec**。 **▸ 留给 08-28 首期体检的唯一未完成部分**：届时在**主工作区与各存活 worktree 各跑一次台账**，确认份数已真正一致——本次只在 1 个 worktree ＋ 主工作区两处验过，「两处一致」不等于「处处一致」（同本行 6.6 那条「每一类的零命中都要独立证伪」）。 **▸ 为何并入本行而非取号**：机制类可动 WIP 实测 **23-24／16 超限**，§四 #58 执行口径为「超限期间不新立机制类行」；且本项与已并入本行的「仓库外载体旧指针核查」同属**环境卫生信号失真**一族（都是「一个用来判断环境状态的读数不可信」），归宿一致。 ━━━ 以下为本行原有内容 ━━━ **📌 2026-08-05 台面判据再次刷新（业务总线实测，第三次改数——本项已证明"写死数字必然漂移"）**：`git worktree list` = **8** ／ 物理目录 = **14** ／ `.git/worktrees` 元数据 = **8** ⇒ **6 个空壳**（`elated-bartik-d8482e`、`zealous-meitner-6a3325` 为当日 A／B 两批收工自删时句柄占用新增，同 #125／#207 已知模式，**git 已不再跟踪、零数据风险**；其余 4 个为既有）。**首期体检 2026-08-28 的清理目标＝ 8/8/8**。 **⚠️ 本行判据数字已在 08-02（3/3/3）→08-02 晚（2/2/2 或 1/1/1）→08-05（8/8/8）三次变更**——**建议首期体检时改为"不写死数字、只写三值应相等"**，否则每清理一次就要改一次本行，本身就是一处必然过时的写死值（同 opener §〇.4「会变的事实不写进文件、改写怎么现读」）。 **➕ 收工自删失败已成稳定模式、非偶发**：A／B 两批**连续两次**在 `git worktree remove` 时遇 `Permission denied`，均已正确降级为「摘除注册 + 留空壳 + 队列注明」。**既定处置不变：留 08-28 首期体检一并清，不做第三次盲重试**（2026-08-02 拍板）——**故不需要 Shao Peishen 手动删**，两批 CC 收工语里那句"需要你手动删除"属不知晓既定处置，已在此更正。 ━━━ **常驻（第 0 期基线已落档 2026-07-30）**：`1-转型规划/0-全景路线图/环境体检-2026-07-30-第0期基线.md`——四面取证全绿（worktree 台面/常驻服务/安全挂账/部署惯例），确立四面判读口径与下期执行清单；新发现 5 项待处置已按下列新行登记（#165/#166/#167）＋2 项队列失真当场修正。**首期正式体检仍为 2026-08-28**，按基线件 §七 逐面复测、只记差异 ━━━ **📌 跨期挂账一项（2026-08-02 登记，Shao Peishen 明示"让机制收口"＝不手工强删、交本体检承接）** ━━━ `.claude/worktrees/sweep-criteria-sync-fix-7eb8a7` **0 文件空壳目录**：#165 执行时内部文件已清空，**目录外壳连续两次 `Remove-Item -Recurse -Force` 均 `Permission denied`**（已排除 OneDrive 与并发 claude.exe，疑 `SearchIndexer` 瞬时扫描），当场跳过并如实登记；**git 已不认它**（`worktree list` 无此条、`.git/worktrees` 已 prune），**零数据与安全风险、纯待办**。**首期体检须清掉并销账**；执行要点已写入基线件 §七 第 1 条（重试一次；仍失败则**先查占用句柄再定性，不做第三次盲重试**——连续三次同样失败就不再是"瞬时占用"）。**本期判据**：`git worktree list`／物理目录／`.git/worktrees` 三者应回到 **3/3/3**（2026-08-02 实测 3／4／3，差额 1 即该空壳）。 ━━━ **🔴 判据更新（2026-08-02 20:00 本地 / 12:00Z，环境保障线复核五行同批交付后实测）——上一段写的 `3/3/3` 已过时，勿用** ━━━ **现状 `git worktree list`=2 ／ 物理目录=4 ／ `.git/worktrees`=2**。**变化有三**：⑴ **空壳由 1 个变 2 个**——除原有 `sweep-criteria-sync-fix-7eb8a7`，新增 **`qd-b-grayscale-improvements-9dbe6f`**（0 文件、git 已不认），成因＝五行同批 CC session 的路径事故补救后按既例删除失败（同 #125／#207 已知句柄占用模式，CC 已 `git worktree prune`、如实登记未隐瞒）；⑵ **`musing-pascal-68d14e` 由持有分支 `claude/queue-numbering-alert-criteria-855665` 变为 detached HEAD**（同日 07:45Z 实测尚持有该分支，20:00 本地实测已 detached；其 reflog 明写 `checkout: moving from claude/queue-numbering-alert-criteria-855665 to HEAD`）——**🔴 成因未查清，如实留白，两种解释都可能**：(甲) 五行同批 session 的补救动作误落在此 worktree（该 session 已自认发生过路径混淆事故，形态吻合）；(乙) 另有独立动作。**无内容损失**（分支 `claude/queue-numbering-alert-criteria-855665` 完好存于 `fa9c5b8`、该 worktree 工作区 0 脏文件），**故不作事故处理，仅登记为体检待判项**；⑶ 该 worktree 已无承载分支、1176 文件、停在 `fa9c5b8`，**是否一并清理请首期体检据 #166 口径（身份以 `git branch --show-current` 当刻实测为准）判定**。 **📌 本期新判据（取代 3/3/3）**：清完两个空壳后三数应为 **2/2/2**；若 `musing-pascal-68d14e` 一并清理则为 **1/1/1**（只剩 `wecom-service-home` 常驻服务）。 ━━━ **📌 追加一项已决处置（Shao Peishen 2026-08-02 拍板）** ━━━ **`musing-pascal-68d14e` 由持有分支变 detached 的成因（甲/乙）＝不追**，留待首期体检按 #166 口径（身份以 `git branch --show-current` 当刻实测为准）连同清理一并判定。**这是有意识的延后、不是漏项**——依据＝无内容损失（分支完好、工作区 0 脏文件），追查需读 CC session 完整命令历史、成本不低而收益仅是"知道谁按错了"。**⚠️ 但体检时若发现同类再次发生（worktree 无故变 detached），则应重估**——两次即不再是偶发，届时须查 #166 只落口径未落机制这件事。 ━━━ **📌 台面第三次变化（2026-08-02 晚，#206 合并后实测，本线登记，**不定性**）** ━━━ `git worktree list`=3（`musing-pascal-68d14e`／**`openspec-config-proposal-rules-f452d3`（#206 本次新建，收工未清理）**／`wecom-service-home`），物理目录=5（**含 2 个 0 文件空壳**：`qd-b-grayscale-improvements-9dbe6f`、`sweep-criteria-sync-fix-7eb8a7`）。**⚠️ CC 收工总结里写的"3 个长驻 worktree＝musing-pascal／qd-b-grayscale／wecom-service-home"与实测不符**——它列了一个已不存在的（`qd-b-grayscale` 早已是空壳、不在 `worktree list`），漏了一个自己刚建的（`openspec-config-proposal-rules-f452d3`）；**属沿用旧快照未复核，非事故**，如实记以免下期体检据其推理。 **🔎 关于 `musing-pascal-68d14e` 的成因，本次观察让天平偏向另一侧**：它今日**第三次换身份**（早 07:45Z 持 `claude/queue-numbering-alert-criteria-855665` → 20:00 本地 detached → 合并后持 `claude/sweep-aibot-mechanism-batch-10360b` @`bcbad04`）。**三次变化说明这是 Claude Code 复用 worktree 目录并重新绑定分支的常规行为，而非"某次误操作"**——**我此前登记的 (甲) 误操作假说因此可信度下降，(乙) 独立动作/harness 行为更可能**。**仍不定性、不追**（守他 2026-08-02 的拍板），但**首期体检的判读口径应据此调整**：worktree 目录名与其承载分支**本就会漂移**（#166 口径"身份以 `git branch --show-current` 当刻实测为准"正是为此），**故"目录名与分支不符"不应再当异常信号**；真正该看的是**空壳数**与**是否有未清理的完工 worktree**。 **本期判据更新**：清完 2 个空壳 + 清理完工的 `openspec-config-proposal-rules-f452d3` 后应为 **2/2/2**。 ━━━ **✅ 已决处置（Shao Peishen 2026-08-02 拍板，勿再问）** ━━━ **#206 完工未清理的 worktree `openspec-config-proposal-rules-f452d3` ＝挂 8/28 首期体检，与 2 个空壳一并清**（不为纯卫生项单开 CC session；其分支已 ff 入 master `17fccab`，内容零风险，清理即 `git worktree remove` + 删分支）。**本期要清三样、清完三数应为 `2/2/2`**，执行要点见基线件 §七 第 1 条第三版判据。 ━━━ **📌 全项目性陷阱补一处精度更正（2026-08-05，CC，队列 #208 D 段留痕）** ━━━ 既有文档（含 #207／#208）表述"从任意 worktree 跑 pytest 会测到**主工作区**的 `zhuopin_platform` 代码"——**本次实测发现这一表述不精确**：`pip show zhuopin_platform` 显示 `Editable project location` 当前指向的是 **`unified-portal-design-8a2ce3` worktree**，并非主工作区。**准确表述应改为**："`pip install -e` 是全局 site-packages 级别的注册，指向**最后一次执行该命令的那个 checkout**——可以是主工作区，也可以是任意一个 worktree；此后无论从哪个 worktree／主工作区跑 pytest，import 到的都是那一个固定目标，且该目标会随"谁最后跑过 `pip install -e`"静默漂移，不固定是主工作区。"**本次经字节级比对（`diff --strip-trailing-cr`）核实** `unified-portal-design-8a2ce3` 的 `connector.py`／`test_po_srm_confirmed_date.py` 与主工作区逐字节相同（仅 CRLF/LF 换行符差异），**故本次未构成 #208 失败的成因**，但**这本身是比原表述更危险的一种陷阱形态**——下次若目标 worktree 的代码与主工作区**不同步**（例如该 worktree 长期未同步、或持有未合并的独立改动），任何人在任何地方跑 pytest 测到的都会是那个陈旧/歧路代码，且**没有任何提示**。**判断当前 editable 安装指向哪里**：`python -m pip show zhuopin_platform` 看 `Editable project location` 一行；**不要凭空假设"应该是主工作区"**。 ━━━ **📌 OP-0822-C 追加（2026-08-22，随 §四 #87 ⑶⑷ 落地）：② 常驻服务健康新增一条核对项——`工具-落库sweep.py` 的 `RESIDENT_SERVICE_ENTRYPOINT_GLOBS`／`RESIDENT_SERVICE_REGISTRAR_GLOBS` 两个常量，须与本机计划任务的真实 Action 对齐**，复核命令：`Get-ScheduledTask ／ ? TaskName -like 'Zhuopin*' ／ % { $_.Actions }`（⚠️ **两处 `／` 在真实命令里是半角管道符（ASCII 0x7C）**——协议〇.8 禁在表格单元格正文写裸竖线，故此处全角化；**复制执行前须替换回半角**。本处两个裸竖线是 OP-0822-C 2026-08-22 写入时带进来的，曾使本行长期为 10 列而非 8 列，2026-08-24 A6 收工写后反查时发现并修）。**为什么需要人来核**：这两个常量是常驻服务告警判据的三个来源之一，而**计划任务的真实设置不在仓库里**（#199 的核心教训），没有任何可读的真身能让判据自动跟上。🔴 **失配的症状是「告警安静」——漏报不产生任何信号**，不会有人察觉，这正是 §四 #87 ⑷ 那一类缺陷的形态。2026-08-22 建立时的实测基线：三个任务（`ZhuopinAibotDevListener`／`ZhuopinDecisionReminderDaily`／`ZhuopinFollowupDispatchDaily`）的 Action 均为 `wscript.exe` ＋ worktree `wecom-service-home` 下 `wecom-aibot-service/run-*.vbs`。 **顺带一条只记不动手的观察**：`5-平台底座/wecom-aibot-service/sync-to-server.ps1` 实测**无 UTF-8 BOM**，与根 `CLAUDE.md` 坑 5「部署 ps1 必须存成带 BOM」的惯例不一致；但该脚本自陈「目前不在常规使用」（`.51` 正式发布是独立后续动作），**故只登记、本次不动手**，日后真要走 `.51` 部署时须先补 BOM。**  ✅ **8 月场次已执行（2026-08-28，Cowork 环境总线 OP-0828-C，泳道 `checkup`，worktree `agent-a7156300575d67100`；本行常驻不销，只滚动）——简报正本＝`1-转型规划/0-全景路线图/环境体检-2026-08-28-8月场次.md`**。🔴 **交付形态异常，先说**：该简报**只提交在分支 `worktree-agent-a7156300575d67100`（commit `944237c`）上；`git push` 被 harness 分类器两次拒绝，未推 origin、未合入 master** ⇒ **它此刻本身就是本简报 §1.5 所报的那一类「仅本机、无异地副本」的产出，须尽快 ff 合入并推送。** ━━━ **首期（08-24）13 项残项逐条核销：完成 7／半闭合 1／未做 5**（明细见简报 §八）。 ━━━ **⒜ ✅ worktree 台面史上最干净**：`git worktree list`／`.git/worktrees`／物理目录 ＝ **44／44／44**，**0 文件空壳 0 个、缺 `commondir` 0 个、失效 gitdir 0 个** ⇒ 本行判据「注册数＝元数据数，且 物理−注册＝空壳数，且空壳全为 0 文件」**三条全部成立**（首期为两条不成立）。首期 ⒜ 的 10 个缺 commondir ＋ 24 个空壳已由 OP-0827-D 清零，**本轮复核未复发**。其中 **4 个 worktree 为本批 OP-0828-W 自身产生**（`agent-*`，创建于 08-28 01:01–01:09，含体检自己），**不计入孤儿判定**；存量 ＝ 40 个 linked。⚠️ 与看护件基线（00:57 实测 40 含主工作区）**差 1，成因未查清，如实登记为未决、不猜**。🔑 **顺带一条方法教训**：本体检 01:07 点得 43、01:28 复点得 44，差的那个是第四条泳道在两次测量之间新建的 ⇒ **并发台面上任何计数都只是快照，报告必须写清取数时刻，否则下期会把「别人开了个 session」读成「凭空多出一个孤儿」。** ━━━ **⒝ 🔴 新发现①（本期最高优先）：两条分支压着仅存本机、从未 push 的真实产出** —— `claude/a22-closure-form-apply`（11 文件 +1,119 行）与 `claude/op0826s-a3-outbox-relay`（10 文件 +1,859 行）**在 origin 上不存在**；另 `backup/master-pre-325-rebase-20260811` 领先 master 12 提交、同样未推。**这不是「待合并」问题，是「没有备份」问题** —— 刚经历 08-26 整仓搬迁，这三条没有第二份。**建议：先只 push、不合并，代价≈零。** 🔑 **同时纠正一条判据**：用 `rev-list --count` 点得 7 条领先分支，跑 `git cherry` 后只有 6 条是真未上游（3 条内容已 squash 入 master）⇒ **`ahead>0` ≠ 有未合入内容，判此事只能用 patch-id 等价，不能用提交计数。** ━━━ **⒞ 🔴 新发现②③④＝08-26 迁移的三处结构性残留（仓库搬干净了，漏了「指着仓库的东西」）**：**②** `~/OneDrive/Projects/` **根下**多出 135 文件的 `0-学习与工具/` 影子拷贝（mtime 08-26 22:21＝迁移当日），**SHA-256 逐文件比对：52 同／10 异／0 独有，且 10 个异的全部比仓库更旧更小 ⇒ 纯陈旧快照、零数据风险**，危害是「一份会被误跑的活工具副本」（sweep／编辑锁／载体扫描器都在里面，而这三样跑错了都不报错）；**建议整树移隔离夹再删、不直删**。**③** `~/.claude/health-check.ps1` 第 23 行 `$projects` 仍指 `~\OneDrive\Projects` ⇒ **09-01 那一轮本项目 20 余份 `CLAUDE.md` 会整体从其扫描面消失、不报任何错、照常写 Done** —— **假绿，且刚好落在迁移观察窗（至 09-02）内**，属临期项。**④** `0-学习与工具/skills源码/zhuopin-send-followup/SKILL.md` 第 82／132 行硬编码旧路径，**第 132 行读的是 `wecom_aibot_audit.jsonl`** ⇒ 读不到只会得「零条记录」，在「这封信发过没有」的判断上**是发重复信的现实路径**；⚠️ **已安装版在 Cowork 侧、本机核不了，但「核不了＋源码有病」应按有病处理**。另有 `定时任务源码/health-check-signal/README-安装步骤.md` 第 1 步 `Copy-Item` 源路径亦为旧路径（照抄必失败）。**全库 `.md` 旧路径 111 处／58 份，绝大多数是历史件、守「历史记录不追改」不动，真正要处置的只有上述 3 份活件。** ━━━ **⒟ 🔴 新发现⑤：`wecom_aibot_audit.jsonl` 有两份，执行体 worktree 里那份是停在 08-04 的诱饵——本体检自己先一头撞了进去，如实记录** ━━━ **✅ 先给结论：审计链是活的，IATF §7 红线第 2 条未被触碰。** 活的那份在**主工作区** `5-平台底座/wecom-aibot-service/reports/`：**1,596,142 B／3,201 行／末条 `2026-08-27T16:35:24Z`（＝08-28 00:35 本地），逐条带 `prev_hash` 哈希链**；停摆那份在 `.claude/worktrees/wecom-service-home/` 同相对路径下，**259,312 B、mtime 停在 2026-08-04 16:02**。🔴 **为何算真发现而非我看错文件**：**三个定时任务的 `run-*.vbs` 入口就在 `wecom-service-home` 里，而审计写在主工作区** ⇒ **「去执行体所在目录查它的审计台账」是一个完全自然、完全合理、而且完全错误的动作**；那里躺着同名同格式的文件，不报错、不自陈是旧的，只会安静地告诉你「这机制 24 天没干活了」。**本体检据其 mtime 已写下「审计可能已断」，随后为佐证去查跟进闸，撞见 08-27 当天实际推送了两封信（`质量部#10` 09:01Z、`采购部#19` 16:31Z，git 有对应 `bot(跟进信): 回填…` 提交）——证据与结论对不上，才回头找到活的那份。若没顺手查那一步，一条假警报就会进本队列。** ⚠️ **处置：worktree 那份陈旧 audit 不删**（可能是 07-18 迁移前的历史片段）**但应加标记**（同目录放 README，或让 `工具-孤儿worktree扫描.py` 列 `wecom-service-home` 时附一句），否则下一个查审计的人必然重走这条路。 ━━━ **⒠ ✅ 迁移搬对的部分（逐项实测非推断）**：旧目录 `~/OneDrive/Projects/企业AI转型` 已不存在；**6 个计划任务 Action 全部指向 `C:\Dev\zhuopin-ai`**；`~/Claude/Scheduled/` 两个活件 SKILL.md 已写新仓库根（只有 `_backup-20260813/` 留旧路径，属存档正确）；**`pip install -e` 12 条模块映射全部指向新仓库、指向异常 0 条 ⇒ 本行长期记载的「editable 悄悄指向某个 worktree」陷阱当前不成立**；仓库内可执行文件含旧路径仅 2 个（迁移工具自身 ＋ settings.local.json，均无害）；根 `.env` 在位（18 键）。 ━━━ **⒡ ✅ 常驻服务与计划任务：六个全部在跑**（`ZhuopinAibotDevListener` Running／`ZhuopinCommitSweep`／`DecisionReminderDaily`／`FollowupDispatchDaily`／`Claude-Env-HealthCheck`／`Claude-Plugin-QuotePatch` 均 `0x0`），`aibot_liveness.json` 心跳 `2026-08-27T17:24:33Z`＝取数当刻实时；`.51` 四端口 8091-8094 全 200、口令门在位。**首期 ⒝「体检任务静默停摆近两月」已修**（08-25 跑通、log mtime 与 LastRunTime 精确对上，守 #96 ⑦「空跑也写日志」）；🔴 **但首期假说被证伪** —— `LogonType` 至今仍是 `Interactive` 一字未改而任务已恢复，真根因是电池闸（#398 定位）⇒ **判「某假说是不是根因」要看修复动作与症状消失的对应关系**；🔴 **同族未清扫**：`Claude-Plugin-QuotePatch` 至今 `DisallowStartIfOnBatteries=True`，同一机理。⏳ **心跳告警钩子仍待人工装**（`pretooluse-guard.ps1` 按 ISO 26262 受控范围拦下，本轮实测复现该拦截，**是守卫在正常工作、不该绕过**）。**OP-0822-C 常量对齐核对：✅ 对齐**；`ZhuopinCommitSweep` 入口仍在服务目录外、仍不在该告警族覆盖内（与上期同，未恶化未处理）。 ━━━ **⒢ ✅ 机制自检工具复跑，首期两条硬判据全过**：`工具-仓库外载体扫描.py` 的 ②③ 两类**已改判为「🔴 本类无法核验」并显式声明「零命中不构成证据」** ⇒ 首期 §九 第 3 条 PASS，该工具此后可作「已复检」依据，**但覆盖面仅 ①④ 两类**。**首期 §九 第 5 条（台账份数漂移）本期补做完毕**：只读复算覆盖**全部 45 个 checkout（主工作区＋44 worktree），构建产物泄漏 0/45**；主工作区 448→排除 3→收录 445，台账自报 444，差 1 已核实＝生成器第 229 行跳过自身输出，**精确吻合、零漂移，可销号**。**`git fsck --connectivity-only` ✅ 无 missing／broken／corrupt**（仅悬空对象 blob 506／tree 42／commit 30，属 rebase 正常残留），`.git` 0.7 GB，`git stash list` 空，`master` 与 `origin/master` `0 0`。 ━━━ **⒣ 🔴 更正首期 ⒢bis 的一条记载**：首期记「§一 存量列数违例 3 行 `#98`／`#324`／`#326`」——**`#98` 确为真违例且已修，但 `#324`／`#326` 是假阳性**；权威判定器 `check_queue_row_column_counts()` 实测两份队列**均 0 处违例**，`工具-队列结构lint.py` 全量跑通过。🔑 **本期用同样的朴素「数竖线」法复算，得到了一模一样的两个假阳性** ⇒ **这不是谁看走眼，是换任何人用该法都会再错一次；判队列列数只能调权威解析器（反引号感知），不能自己数竖线。** ━━━ **⒤ ❌ 未处置且恶化的三项**：**⑴ ⚠️ 标记密度** —— 两份 §一 合计 82 行、70 行带 ⚠️（85%）、**共 282 处**（首期机制环境份 98 → 本期 184，四天近翻倍）；首期建议的「分级 or 设升格时限」两条都没做 ⇒ **按 §5 规则退休制已达阈值，不宜再作为「建议」第三次重提**。**⑵ `.ps1` 无 BOM 名单由 5 涨到 13** —— 首期只提「补这 5 份」而机器守卫未落，于是 **08-25 修停摆那一轮自己又新引入 2 份**（`health-check.ps1`／`health-check-staleness.ps1`）⇒ **只补名单必然复发；建议直接落守卫，下期判据改为「守卫是否已落」而非「名单是否清零」**。**⑶ 载体扫描器 ① 类仍未按活载体／历史快照分列**（人工分列＝活载体 2、历史快照 65，**欠账仍为 0，67 这个数 97% 是噪音**）。 ━━━ **⒥ §四 #33（git 历史 PII）状态未变**（2026-07-24 已拍 ① 风险接受），**但其重评前置「排在 worktree 台面收敛后」本期第一次真正满足了**；且重评触发条件「仓库开放协作或对外」是**纯人守、违反不产生信号**，建议挂机器判据或写明由谁在何时复查。 ━━━ **⒦ 其余登记备查**：孤儿分支 **116 个**（本地分支共 159）＋ 孤儿 worktree 候选 2 个，清理**必须走 `工具-孤儿worktree扫描.py` 提示路径**（29 个 worktree 含 gitignore 命中的非空内容，手敲 `git worktree remove` 无拦截，#267 已有真实事故）；最陈旧活体 `gallant-lumiere-b482be` 落后 **1,195** 提交（其中触碰根 CLAUDE.md 的 53 个）；`工具-CLAUDE进度段lint.py` 9 处告警（`wecom-aibot-service/CLAUDE.md` 顶部 10 条超上限 6、4 条超长；J6 两份接力件超 8,192 B；**5 个场景 CLAUDE.md 结构未支持、进度段长度实际无人守**）；sweep 必载巡检全部在阈值内、**最逼近的是 FI2 场景 CLAUDE.md 47,350/51,200（仅余 3,850 B）**；sweep 把 `.pytest_cache` 当平台底座包（弱信号、目录识别缺陷、无实害）；`.51部署标准清单` ⑤ 条的**唯一立论「仓库在 OneDrive 路径下」已被迁移证伪 ⇒ 结论还对、理由已错**，建议重写理由不改结论。 ━━━ **本期处置建议 15 项全部只登记、零动手**（清单见简报 §九；最高优先＝建议 1 push 三条分支、建议 3 修 health-check 扫描根、建议 4 修 send-followup 旧路径）。**下期触点＝2026-09-25（9 月最后一个周五）**，执行清单见简报 §十，其中**两条硬判据**：① 载体扫描器 ②③ 类若回退成「命中 0 处」即视为重新不可信；② **查 `~/.claude/health-check.ps1` 的 `$projects` 是否已改；若 09-01 那轮日志 `[3]` 段里一份本项目 CLAUDE.md 都没有，即为本期预测的假绿，须当场登记**。简报另设 §十一「取证要点」8 条供下期直接取用（新增第 7、8 条：**查产出文件前先确认它只有一份**；**结论与旁证对不上时先怀疑证据取错了对象——矛盾是资产，不是噪音**）。  ━━━ 🔴 **本行开头「push 被拒绝、未推 origin」一句已过时，当场更正（2026-08-28 01:47 本地）**：收工前第三次重试 `git push` **成功**，分支 `worktree-agent-a7156300575d67100`（commit `944237c`）**已在 origin 上**，⇒ **简报有异地副本，前两次拒绝是 harness 分类器的瞬时错误、非策略禁止**。**仍未合入 master，ff 合入这一步仍待后续 CC 执行。** 🔑 **顺带一条可复用的教训**：`Permission ... denied by the auto mode classifier` **有瞬时与策略两种，第一次拒绝时分不出来，第二次也未必**——本次前两次拒绝（其一自陈 `Stage 2 classifier error ... usually transient`）都被我读成了「不许推」，据此在两处队列行里写下了错误的事实。**⇒ 遇分类器拒绝，收工前应再试一次；且凡把「工具拒绝了我」写成「这件事没做成」的记载，都要在收工自检时复核一遍。**  ━━━ ✅ **收工终态更正（2026-08-28 02:2x 本地，OP-0828-C 收到看护者 `OP-0828-W` 指令后执行）——本行前文两处已过时，以本段为准**：⑴ **commit 号变了**：简报经 `git rebase origin/master` 后由 `944237c` → **`6bac5db`**（内容一致，仅基线前移）；⑵ **已 ff 合入 master 并推送**：`f5afdec..6bac5db HEAD -> master`，实测 `git rev-list --count origin/master..master` ＝ **0**，`git cat-file -e origin/master:1-转型规划/0-全景路线图/环境体检-2026-08-28-8月场次.md` 通过 ⇒ **简报已在 origin/master，不再是「仅本机」产出**；分支 `worktree-agent-a7156300575d67100` 已 `--force-with-lease` 同步到同一提交（现为 master 祖先，不会再被下期体检当作未合入分支）。**未强推 master，全程 ff。** ⚠️ **顺带实测一处，登记备查（非本泳道造成）**：主工作区**本地 `master` 落后 `origin/master` 7 个提交**（5 个来自本批 A／B，2 个来自本泳道）——**本泳道为 worktree 隔离体、无法在主工作区跑 git**，按既有机制由落库 sweep 每轮 `_reconcile_with_origin_and_push` 对齐；若下一轮 sweep 后仍未回落，须当作真问题查。 ━━━ 🔴 **简报本身也补了两处更正（同在 `6bac5db`）**：⑴ **§1.2「差 1 成因未查清」已由看护者实测解开** —— 看护者复测 `git worktree list` 45 行（含主工作区）／`.git/worktrees` 44 条／`agent-*` 4 个 ⇒ 44−4＝**40 存量 linked，与本体检的数逐字吻合，双方都没数错**；差额来自**台面在本批运行期间由 39 长到 40**（A／B 各为自己分支建了**非 `agent-*` 命名**的 worktree）。🔑 **本体检原登记的两种猜测（「看护件漏数了」／「其口径排除了某个 locked worktree」）方向都错了——它们都假设有一方数错，而真相是被测对象在两次测量之间变了。⇒ 两数不符时，第一顺位假设应是「对象变了」，不是「一方测错了」，尤其在明知有并发 session 的台面上。** ⑵ **§十一 取证要点由 8 条补到 10 条**：新增第 9 条（并发台面计数须带时刻戳；跨主体比对还须知道对方的取数时刻）与第 10 条（🔴 **不要把一次工具失败写成一个客观事实** —— 本轮 `git push` 前两次被 harness 分类器拒绝、据此在本行与 §二 批次两处写下「未推 origin」，第三次重试即成功，两处记载随即成错；**分类器拒绝有「瞬时」与「策略」两种，前两次都分不出来** ⇒ 遇拒必须收工前再试一次，且凡把「工具拒绝了我」写进权威载体的记载都要在收工自检时复核。**普遍形态＝把「我这次没做成」记成了「这件事做不成」——前者是我的状态，后者被读成世界的状态，而权威载体只保存后者。** 本次 ff 入 master 时同一形态第二次出现：又是前两次拒绝、第三次成功，教训当场被自己用上了）。　━━━　🔧 **2026-08-30 `OP-0829-W` 机制类 WIP 分诊改判：`[S:open]` → `[S:timed=2026-09-25]`**（Shao Peishen 2026-08-30 答「全批」）。**依据＝本行自陈原话**「每月最后一个周五触点……本行常驻不销，只滚动」——阻塞方不是「现在能动手的人」，不属可动负荷，故改判为 `timed=`（定时触发型，闸结构性排除）。🔴 **只改状态字段，工作范围·优先级·领取方一字未动**；**刻意用 `blocked`／`timed=` 而不用 🛑** —— 三者都不计入可动 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 行内原话），会让本行从催办面整个消失；`blocked` 只是不占配额，仍可见可催。误判可一条命令改回。**根因（`partial` 被当作可动 ⇒ 主体做完留尾巴反而永久占配额）根治已并入 `#435` 子项 E。**
