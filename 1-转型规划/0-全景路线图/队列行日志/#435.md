---
title: "队列行日志 #435（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #435 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #435 · 外置前整格原文（2026-09-04 外置，12 段）

[S:done][D:机] ✅ **已完成（2026-08-30，CC `OP-0830-C`，六件同车：A1 路径存在性／A2 尺寸／A3 版本快照／A4 备份堆积／B root 棘轮／E 状态字段一致性）** ━━━ **⑴ design 审：✅ 已过** —— **⑴ design 审：✅ 已过** —— Shao Peishen 2026-08-29 答「审过」，D1-D6 六个决策点**整体通过、无点名修改**。tasks 0.1／0.2 均已 [x]。**⑵ 🛑 已摘（2026-08-30）** —— 他当日答「全批」后，本线按行内自陈对 8 行做 WIP 分诊改判（`#282`／`#413`／`#419`／`#398`／`#380`／`#387`／`#399` → `[S:blocked]`，`#98` → `[S:timed=2026-09-25]`），**机制类可动 WIP 27 → 19**（用闸自身 `_count_mechanism_wip` 对两份真身实测），本行摘 🛑 后 20／22，**余 2 格，不再需要任何豁免**。**⑶ 派单件已备可派**：`1-转型规划/0-全景路线图/派单件-【CC】全局记忆巡检与root棘轮-队列435-2026-08-29.md`。　━━━　🆕 **⑷ 子项 E（2026-08-30 他明示并入本包，不另立行）：`partial` 被当作可动 WIP 的口径缺陷根治。** **根因**：27 行里 17 行（63%）是 `partial`，而 `partial` 在本项目的实际用法是「**主体已做完、尾巴挂在外部条件上**」（`#380`「apply 已完成、真实冒烟仍未做」／`#387`「代码全部修完、四项需人在场的动作未做」／`#399`「仅剩端到端真实发送留步」）⇒ **活干得越多、WIP 越高**，是反向激励。**修法（E3+E4 合一，不改计数口径以避免误伤）**：新增「**状态字段与行内自陈一致性**」检查——扫 `open`/`partial` 行的状态列自陈，命中外部阻塞措辞集（硬阻塞／已押后／待 Shao Peishen／待拍板／需人在场／留步／常驻不销 等）即产出**改判候选清单**，并在 WIP 阻断消息里附带该清单。🔴 **只建议、不自动改**（同 D5）；**不改 `_count_mechanism_wip` 的计入口径**（`partial` 里确有真可动者，如 `#96` CC 半待领，改口径会误伤）。⇒ 把 2026-08-30 这次**手工分诊**变成闸每次阻断时自动给出的东西。　━━━　**WIP豁免：本行不计入可动 WIP，超限系存量、非本行所致。** 实测（用 `工具-共享文档编辑锁.py::_count_mechanism_wip` 自身对两份队列真身跑）：计入的 27 行里**没有 #435**——本行状态列自然语言以 🛑 起首，按其口径属结构性不可动，已被正确排除。27／22 的超限在本行落地前即存在（`#433` 2026-08-28 已记「release 报 WIP 26/22，本行入池前已 25/22 超限约一周」），按 §四 `#58` 监测条款交值周巡检重议上限或清池。⇒ 2026-08-29 那次 `--force-mechanism-wip` 只为释放锁，**不表示新增了可动负荷**。 　━━━　🆕 **⑸ A4 判据追加（2026-08-30 他答 (a)，并入本包不另立行）：备份堆积告警。** 数受检文件同目录 `CLAUDE.md*.bak*` 个数与最旧日期，超 `GLOBAL_MEMORY_BAK_CAP`（默认 3）即告警；🔴 **只报不删**（备份件可能是有意保留的还原点，同 D5 机器无权处置）。**来由如实记——它是被问出来的、不是被查出来的**：2026-08-30 他问「那个 .bak 能不能删」，本线去核才发现 `~/.claude` 下已攒 **5 个备份**（最早 2026-06-24）、**其中 3 个含同一个明文 Bearer token**。⇒ **没有人会主动去看备份目录**，与 A1／A3 同属零信号族；且与 A1-A3 共用同一次目录读取，是本包里扩得最省的一条。落点＝tasks 1.6b／2.3b。 　━━━　📤 **已派出（2026-08-30，`OP-0830-C`，Shao Peishen 当日说「那请出 Opener」）**：派单件 `1-转型规划/0-全景路线图/派单件-【CC】全局记忆巡检与root棘轮-队列435-2026-08-29.md` 的开场词已交出，分支 `claude/op0830c-global-memory-inspect`，新建 worktree。**六件同车最终形态**＝A1 路径存在性／A2 尺寸／A3 版本快照／A4 备份堆积／B root 棘轮／E 状态字段一致性；design D1-D7 全审过。**派出时实测**：本行仍 `[S:open]` 未被他人认领，机制类可动 WIP 21／22。⏳ **待 CC 认领后自行改写本行为在办**。　━━━　✅ **收工实测（2026-08-30，CC `OP-0830-C`）**：全量回归 `test_工具-落库sweep.py` **337 passed（0 failed，含新增 20 项）**；`test_工具-共享文档编辑锁.py` 294 run，仅 1 个失败**且与本包无关**（根 `CLAUDE.md` §1 人名结构已迁移，既有性别代词 lint 用例待另行修复，本线未触碰、未掩饰）。**真实验活逐条过**：A1 对真实 `~/.claude/CLAUDE.md` 跑出 **1 处真红**——`~/.claude/skills/rare-earth-research` 目录已不存在（L57，本机 `ls ~/.claude/skills/` 实测只有 `download-images-skill`／`setup-sound-notifications-windows` 两个）——**这是本包上线首轮即抓到的一处真实失真，`OP-0829-W` 手工审计当时未覆盖到**，只报未改，是否/如何订正待他定；A3 零版本快照红；A4 实测备份 **2 个**（阈值 3，未超限）——🔴 **如实记一处与本行早前记录的数字差异**：本行 ⑸ 段记「已攒 5 个备份」，本线落地时实测仅 2 个，推断是他问「能不能删」之后、本行落地前已自行清理 3 个（本判据全程只读 `glob`+`stat`，未删除/移动任何文件，超限分支已由隔离环境单测覆盖，未在真实目录另造超限）；root 棘轮现值 47,863 B／阈值 49,152 B，差额 1,289 B，回显正确提示「可将 cap 下调至 48,375」；子项 E 对逐字取自 `git show 178979c^` 的 8 行真实文本跑通验收判据，`#282`／`#413`／`#419`／`#398`／`#380`／`#387`／`#399`／`#98` 全部列出、零漏报。⚠️ **一处安全提醒，供他知悉但本线未处置**：本行 ⑸ 段提到的备份文件「其中 3 个含同一个明文 Bearer token」——本判据设计上从不读取备份内容（只统计目录项与 mtime），该凭证是否已随清理一并处理、是否需要轮换，仍待他确认，本行只报告发现路径不再赘述凭证本身。openspec 四件已同步 tasks.md 全 [x]，随本行归档。 　━━━　✅ **组 1-3/5 已入 master（2026-08-30 08:31:45 `1d3c618` ＋ 08:33:39 `d3e1908`，930 insertions／5 files）**：`工具-落库sweep.py` +309、`工具-共享文档编辑锁.py` +106（＝子项 E）、两个测试 +517、`定时任务源码/weekly-status-update.SKILL.md`（＝D6 值周清单一行）。**余组 4 收口（值周清单核、§二 批次、本行回写 ✅、当场 archive）。** 🔴 **顺带如实登记一处与派单件不符**：派单件【设置】写「worktree：☑ 新建（一任务一 worktree）」，实际**复用了既有 worktree** `.claude/worktrees/lan-redo-three-groups-11e8dc`（目录名仍是上一件活的名字），分支 `claude/op0830c-global-memory-inspect-f68c79`。**后果不是错，是可追性**：将来按目录名找 lan-redo 的活会翻到本件的改动（同 `#128` 族）。建议收工时改目录名或在行内留指针，本线只登记不代改。  　━━━　➕ **2026-08-31 并入一条结构洞察（Cowork `OP-0830-A`，Shao Peishen 当日答 (a) 定并入本行；本行是它的直接语境——分诊器正是「机器扫出候选、人一眼过目改判」这一设计的载体）** ━━━ **形态**：本日值周对分诊器点名的 7 行逐行过目后，**5 行判「不该改」**（`#96`／`#328`／`#366`／`#433`／`#434`——**各有不依赖外部条件的可动半边**）、1 行判「不准、留原状并注明」（`#416`，19.4 KB 未通读全格）。⇒ **分诊器的措辞判据（`STALE_STATUS_PHRASES`）只能回答「这一行里有没有等外部条件的表述」，回答不了「这一行整体还能不能动」** —— 一行可以同时含两个半边，而 WIP 以行为原子单位计数。 ━━━ 🔑 **由此得到的判据（比本次个案通用）**：**当一个计数顶到上限时，先问「这个数是不是本来就该这么大」，再问「要不要调上限」。** 本日两次都是这个形状：上午是分诊没做（数**虚高** 24，改判后 22）、此刻是行不可分（数**如实** 22/22）。**两者的处置完全相反——前者该分诊，后者不该动。** ⇒ **不得为了让计数好看去拆行**（拆行推高行数，与协议〇.10「并入优先」直接冲突）；真正该降的是**已完成却未销号的行**（＝ `#443` 每周清扫那 280 行），不是把在办的行拆开。 ━━━ ⚠️ **同批留一条方法教训**：Cowork 本轮曾建议「7 行全部改判、WIP 可降到 15」——**那是把工具输出当成了结论**。分诊器输出的是**候选**，判定权在人。值周的逐行过目处置正确，Cowork 建议已收回。
