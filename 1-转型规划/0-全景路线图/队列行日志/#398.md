---
title: "队列行日志 #398（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #398 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #398 · 外置前整格原文（2026-09-04 外置，23 段）

[S:blocked][D:机] 🟢 **三处根因全部定位并实测坐实，可修的已修完；两处留步（守卫拦截／待拍板）（CC-A28，2026-08-25，分支 `claude/queue-398-mech-signal`）** ━━━ **⑴ Claude-Env-HealthCheck —— 🔴 A6 的成因假说被证伪，真因是另一条** ━━━ A6 猜 `LogonType=Interactive`；**实测推翻**：2026-08-25 在**完全交互式登录会话内** `Start-ScheduledTask`，任务照样起不来。**真因＝ `DisallowStartIfOnBatteries=true` ＋ 本机是笔记本、常态电池供电** ⇒ 触发后进 `Queued` 无限等交流电（任务计划事件 ID 325「已排队」实测复现），最终以 `0x800710E0`（4320 操作员/管理员拒绝了请求）收场；叠加 `StartWhenAvailable` 未开 ⇒ 错过的月度触发**永不补跑**——这也解释了为何 07-01 那次 23:08 补跑成功、08-01 那次 17:02 补跑被拒。**判别实验（同一电池条件下做两次，把两件事分开）**：① 直接跑脚本本体 ⇒ 秒过、日志被写 ⇒ **脚本没坏**；② 经计划任务跑 ⇒ 卡 Queued、日志不动 ⇒ **坏的是任务设置**。 ━━━ **已修**：任务设置改为 `DisallowStartIfOnBatteries=false`／`StopIfGoingOnBatteries=false`／`StartWhenAvailable=true`／`ExecutionTimeLimit=PT2H`；**同一电池条件下复测 ⇒ `LastTaskResult=0x00000000`，日志与心跳均被写**（这是「修好了」的实测证据，不是推断）。**LogonType 保持 Interactive 未动**——A6 建议改 S4U，但成因既已证伪，S4U 只带来安全上下文变化（不加载用户配置、`USERPROFILE`/OneDrive 路径解析行为改变）而无对应收益，**不做无根据的改动**；日后若要「无人登录也跑」再单独议。 ━━━ **信号（本行重点，且刻意不新造无声守卫）**：`health-check.ps1` 现在每次运行（**含异常路径，写在 finally 里**）落一份机器可读心跳 `health-check-status.json`（时间戳／ok／fatal／发现数／发现清单）；由一个 **SessionStart 钩子**在**每次 Claude Code 会话启动时**读它——过期 > 40 天／`ok=false`／`findingCount>0` 任一即横幅告警，全好时**完全静默**。🔑 **为什么不是再建一个后台任务**：再建一个后台守卫去守月度体检，只是把「失败不产生信号」这个失效模式往外挪一层——那个守卫自己停了照样没人知道。会话横幅是**人每天真的会看到**的界面，且钩子自身报错时 Claude Code 会显式提示，**不存在「静默不响」这一态**。**六场景反例测试全过**：心跳缺失／陈旧 55 天／上次崩了／有未处理发现／**全好必须静默（输出长度＝0）**／**心跳文件是坏 JSON 时钩子自己必须出声**。**并且守卫与信号互查**：`health-check.ps1` 新增 `[4] 自身信号完好性`，反过来核「钩子脚本在不在、是否已注册、电池闸有没有被改回去、补跑开关有没有被关掉」，缺一即报。 ━━━ **⏳ 留步 1（守卫按设计拦截，非技术障碍，需 Shao Peishen 人工执行）**：钩子须装进用户目录 `.claude` 下的 hooks 目录并在 `settings.json` 注册，而这两条路径命中 `protected-paths.json` 的受控规则（ISO 26262 受控、`mode:block`），CC 写入被 `pretooluse-guard.ps1` 拦下（拦截已进当日 `audit-blocks` 日志）。**没有绕过——绕过一次，这条守卫此后就不再是守卫**。钩子本体＋逐步安装步骤＋验收方法已落 `0-学习与工具/定时任务源码/health-check-signal/`（见 `README-安装步骤.md`）。🔑 **在你装好之前，每月体检会稳定报出「[4] 钩子脚本不存在」「[4] 未注册」两条——这是故意的：留步项自己会喊，不靠人记得。** ━━━ **顺带修掉一处会把信号淹掉的缺陷**：`[3] CLAUDE.md 叠加` 原先递归进 `.claude` 下的 worktrees 目录，2 个真实发现被 13 个 worktree 放大成 28 条（实测 findingCount 31 → 排除后 7）。**重复告警等于没有告警**，与本批要修的问题同族。**当前真实发现 5 条**（`claude-howto/vi`／`SalesMarketing`／`企业AI转型/1-转型规划/0-全景路线图`／`企业AI转型/5-平台底座`／`行业研报/_datasources/last30days-skill-cn` 的 CLAUDE.md 叠加超阈），**已两个月无人看，装好钩子后会在会话开头喊**。 ━━━ **⑵ 载体扫描工具两类结构性假零 —— 已修，改法是「阳性对照」而不是「补口令」** ━━━ **判据没坏、目标变了，而判据不知道目标变了**，所以修的不是某一处逻辑，是给每一类装一个「这一类此刻究竟还扫不扫得到东西」的自检。**② `.51` 四服务**：新增 `looks_like_auth_gate()`，按**门禁自身特征**（`type="password"`／`访问口令`／`请输入口令`／`请输入密码`）判定，不用页面大小这类会漂移的启发式；命中即归入新的 `gated` 清单，**与 `unreachable` 分开报**（口令门是内容层问题、网络层完全正常，混在一起就又看不出来了）。`scan_51_services` 返回值由 2 元组扩为 **3 元组** `(hits, unreachable, gated)`——**旧签名结构上无法表达「扫到了，但扫的是门」**，那正是假零的容身处，故必须让调用方无法再靠 `hits` 为空蒙混过关。**③ 已安装版 skill**：阳性对照＝**扫描根下是否真的存在本项目的 skill**（拿库内 `0-学习与工具/skills源码/` 的子目录名作参照物），而不是「扫描根下有没有东西」——用户目录 `.claude` 下的 skills 里一直有 2 个与本项目无关的第三方 skill，正是它们让这一类看上去「扫过了」。**①④ 一并加对照**（目录还在但 0 个可扫文本文件 ⇒ 无法核验），并新增**参照物缺失**这一态（源码目录不在 ⇒ 无法核验，不得退化成「没有本项目 skill，所以零命中正常」）。**输出契约改了**：过不了对照的类别一律打「🔴 本类无法核验：<原因>」＋「本类**没有**零命中结论可用」，**绝不再打「命中 0 处」**；结尾强制打「核验状态总表」逐类标 ✅已核验／🔴无法核验；新增 `--strict`（有类无法核验即退出码 2，供机器消费）。**默认不传 `--strict` 时退出码恒为 0，不改变既有调用方语义**（有专门用例钉住这一点）。**实跑取证（本机，off-LAN）**：① 扫 26 个文件／12 命中 ✅；④ 扫 10 个文件／2 命中 ✅；③ 🔴 无法核验——本项目 6 个 `zhuopin-*` skill 一个都不在扫描根下；② 🔴 无法核验——0/4 取到正文；`--strict` 退出码 2。**单测 27 例全过**，其中 6 条是反例（200＋登录页／扫描根只有无关第三方 skill／参照物缺失／目录空／`--skip-http` 不得算已核验／`--strict` 退出码）。⚠️ **LAN 留步（如实登记，不算失败）**：本机当前 off-LAN，`.51` 实测走的是「不可达」分支；**「200＋口令门」那条分支是用本地 HTTP 桩按实测形态复现验证的，未在真 `.51` 上跑过**——回内网后建议按 A6 简报 §九 第 3 项复跑一次 `--strict` 复核。 ━━━ **⑶ CI 三红 —— 归属核对结论：A9 点名的包与现况确实对不上，「先实测再修」是对的** ━━━ **实测 `openspec validate --all --strict` ＝ 113 passed / 0 failed**：§四 #99 当时点名的 `editlock-mutex-stale-cleanup-resilience`／`queue-dual-file-split`／`sc2-weekly-report-mvp` **已不再报 `No deltas found`**。`gh run view 32796970988`（master 最新）逐 job 核实：**15 条测试腿全绿、`openspec-validate` 绿、队列结构 lint 绿、引导样板 lint 绿；红的恰是 `凭据扫描`／`CLAUDE.md 进度段 lint`／`CI 覆盖率下界护栏` 三条**——与 #398 行内清单一致，**与 §四 #99 的主题不是一回事**。⇒ **#99 的原始成因已自行消解，该行已按归属核对结果改写。** **逐条根因**：**①『凭据扫描』（行内点名「建议优先看」）＝ 真命中，不是误报**——2026-08-24 记录 IT 群答复时，把一条**真实企微 webhook key** 逐字粘进了本队列文件的 §一 行内。**工作副本已抹除**（本批同交），但**原值已在 git 历史里**，轮换与历史处置需你拍板 ⇒ **新立 §四 #118**。**②『CI 覆盖率下界护栏』＝ 判据过期，不是静默失效**——报的是 `跳过用例数 59 > 基线 46`，而同一次 `passed=2868 ≫ 1698`、`腿数 15 ≥ 13`。下载该 run 的 artifact 逐条解析 JUnit XML 核实：**59 条 skip 逐条都有显式理由**（QD-B 43 条＝华丰黄金样本／产品类样本 gitignore 不入库；FI2 9 条＝`FI2_RUN_REAL=1` 环境闸；SC8 4 条；U9C 1 条…），**无一条是「测试悄悄不跑了」** ⇒ 属合理增长。**基线 1698/46/13 立于 2026-08-08（13 腿），今已 15 腿／2868 passed** —— 🔑 **绝对数下界在持续长大的仓库上必然过期，这与 ⑵ 的两类假零是同一种失效**。是否改判据形态（绝对上限 → 占比／按理由分类）属口径变更，**不自行决定 ⇒ 已并入 §四 #99 待你拍板**。**已做的非口径改进**：护栏跌破时现在**一并打印按矩阵腿／按理由聚合的跳过明细＋判读提示**——原先只报数字，看到红的人根本无法就地判断「该红不该红」（我这次是下载 artifact 手工解析 XML 才得出结论的），**一道红了也没人能判断的门禁，与没有门禁只差一步**。新建单测 7 例，含反例「无理由的 skip 必须显形为 `(无理由)`」与「三个基线常量被改动即红」（逼出一次显式说明）。**③『CLAUDE.md 进度段 lint』＝ 真实内容欠账，⏳ 留步 2（需取号 ＋ 与文件内既有禁令冲突，不宜无人在场时自决）**——5 处违规全在 `5-平台底座/wecom-aibot-service/CLAUDE.md`：J2 顶部 8 条超上限 6（须迁最早 2 条 `2026-07-15`／`2026-08-06`，**两条都被 J1 判为「无承接载体且含未闭合措辞」⇒ 按判据必须先立队列行再迁**）；J3 两条超长（2,068／2,216 字符 > 1,200，分别对应队列 #387/#380 与 #355）。🔴 **不自决的理由是硬的**：该文件第 51 行**自带一条禁令**——「⚠️ `2026-07-15` 与 `2026-08-13` 两行不得随后顺手迁走」，而 lint 要迁的恰好含 `2026-07-15`。动它就得同时做三件需要人拍板的事：**为那条未闭合项取号立行、推翻文件内既有禁令、改另一条线（aibot 线）的 CLAUDE.md**。按 lint 作者写在工具头部的告诫——「只做超限报错会把它逼成**为了过 lint 而丢掉未闭合项**，比现状更糟」——**半迁比不迁更坏**，故整条留步、不做半件。精确执行方案见 §四 #99 ⑵。 ━━━ **CI 整体信号缺失怎么处置（不新建行，指向既有定论）**：`.github/workflows/ci.yml` 头部已白纸黑字记着——「失败步骤挂企微 webhook 告警」方向对，但**会新增第 9 类「只报出现不报解除」的告警，须等 #308 子项 D1 的告警解除通用骨架落地后再建，本次不做**。本次**遵此不建、不另立行**；#99 已按 A9 建议补上「值周巡检加一问：本周 CI 各 job 是否全绿、红了多久」。 ━━━ **代码已 commit＋push**：`fb0d185` on `claude/queue-398-mech-signal`（8 文件 +1023/-40）。 ━━━ **收工态**：⑴ 已修（含实测复测）＋ 1 处留步；⑵ 已修（27 例过，含 6 反例）＋ 1 处 LAN 留步；⑶ 归属核对已出结论，1 条已修（抹除），2 条留步待拍板。　➕ **2026-08-25 §四 #99 两项拍板并入本行执行**：⑴ CI 覆盖率护栏改判据形态（有显式理由的 skip 不计入、无理由一条即红——替代绝对数上限）；⑵ aibot 场景 CLAUDE.md lint 5 处欠账一次做完（先为 2026-07-15 条立行承接未闭合项→迁→删第 51 行旧禁令；🔴 不许半迁）。 ➕ **2026-08-25 追加子项（Cowork 巡查，Shao Peishen 答 2(a)）：A28 无哨兵待查**——本行的建造会话（08-25 08:17 批 mech 泳道 A28）跑满 **121.5 分钟**、日志 40 行**确无哨兵**（与同轮 A22/A26 的 `-Tail 40` 误判**不同族**，那两条日志里其实有哨兵），而本行已回写 partial ⇒ **活干完了、信号没发出**。**请下次碰本行时顺查**：日志 `reports/opener-batch/20260825-081752/mech-A28.log` 末尾断在哪一步，判断是「模型没输出哨兵」还是「跑到超时被截断」，并据此判断是否需要给 runner 加超时告警。 ━━━ ✅ **2026-08-26 第 ⑵ 处「200＋口令门」分支已在真 `.51` 上复跑通过（OP-0826-V，CC，cherry-pick `e024ead` 已 ff push master）** ━━━ 🔴 **先更正一处 OP-0826-C 的实测口径**：`LAN留步项捞出清单-2026-08-26.md` §二bis 写「`fb0d185` 领先 master 1 个提交 ⇒ 先 ff 合入」，**ff 实测不成立** —— `git rev-list --count origin/master..fb0d185` ＝ 1 只说明分支尖端那 1 个提交不在 master，而 **`fb0d185^` ＝ `883902d` 本身是 master 的祖先、master 已在该分叉点之上前进 159 个提交** ⇒ `git merge --ff-only` 必被拒。**⇒ 判据：`rev-list --count A..B` ＝ 1 推不出「可 ff」，还须 `git merge-base --is-ancestor origin/master fb0d185`**（本例为 False）——**这条误判与本行前几处同族：一个读数看起来完全正常，而它答的根本不是被问的那个问题。** 实际走法（Shao Peishen 2026-08-26 选 (a)）＝**从最新 master 起分支 `git cherry-pick -x fb0d185`，零冲突**（`fb0d185` 只碰 8 个文件，其中 4 个已有文件自分叉点起在 master 上 0 次提交）；原分支保留不动。先验＝`test_工具-仓库外载体扫描.py` ＋ `test_工具-CI覆盖率护栏.py` 共 **34 passed**。 ━━━ **复跑结果**（按 `环境体检-2026-08-24-首期.md` §九 第 3 项，关键词 `跨桌任务队列-机制环境.md`，`--strict`，**退出码 2**）：**② ③ 两类均已由「命中 0 处」改判为「🔴 本类无法核验」** —— **② `.51` 四服务 0/4 取到正文**：四个端口全部 200 但正文是访问口令登录页，**字节数逐个不同（8091=1469 ／ 8092=1471 ／ 8093=1475 ／ 8094=1477）⇒ 是四份真实响应、不是本地桩**；**③ 已安装版 skill：本项目 6 个 skill 一个都不在扫描根下**。① Cowork artifacts 命中 12 处、④ 定时任务真身命中 4 处，两类判「✅ 已核验」。⇒ **§九 第 3 项那条判据（「若 ②③ 仍返回『命中 0 处』而未改为『无法核验』，视为该工具仍不可信」）本次实测已翻面，工具可信。** **本次只对 `.51` 做只读 HTTP GET，未改其上任何服务，不触碰 A3 的 `wecom-aibot-service` 边界。**  ━━━ ➕ **2026-08-27（CC-OP0826U，`OP-0826-U`）第 ⑷ 处「LAN 留步无承接方」已建成扫描器，但未接线**（commit `3de9352`，已 ff 入 master）：新增 `0-学习与工具/工具-未闭合产出扫描.py`（只读）＋ 单测 30 项全绿，覆盖派单件要求的三形态与回 LAN 事件感知。 🔴 **三条判据是实测得来、各配反例单测**：⑴ **形态 2 绝不能用 ancestry 判「未合入」** —— 实测本行自己的 `fb0d185` ancestry 判「未合入」，而内容早已在 `origin/master`（`e024ead`），两者 `git patch-id --stable` 逐字相同（`db8ae53a…`）⇒ 改用 `git cherry`（`+` 才是真·未上游）；用 ancestry 会对**每一个 rebase 落地的分支**永久报红，那正是本行前三处治的那个病。⑵ on-LAN 三项齐备缺一不算通（`ping` ＋ `Server: waitress` ＋ 无 `Proxy-Connection`），只有 off→on 那一次翻转才是事件。⑶ 判据可被「已补做／已合入／已 commit」关掉，key 里刻意不含任何会变的数字。 ━━━ 🔴 **本班自己两次踩中同族坑，已改并配单测锁死**：① 初版把本地 `master` 排除在形态 2 之外，**结果第一次真实运行就漏掉了当天最要紧的一条**（本地 master 已分叉，见新立 §一 `#421`）⇒ 判据教训：**排除一个分支之前先问「它出问题时谁会发现」**；② 形态 1 初版纯字面匹配，把 `#418`「故这不是 LAN 留步」误报成留步 ⇒ 加否定词前瞻。 ━━━ **首跑实数（本机 on-LAN，三项探针真答）**：形态 1 命中 9 处（另剔除 §二 历史叙述 19 处并回显）、形态 2 十三条分支、形态 3 五处。 ⚠️ **不得读成「机制已生效」——目前没有任何轮次会调用它**，接线与真实验活立 §一 `#422`。 openspec 三问：①②③ 均不命中 ⇒ 不走 openspec（未改 sweep／`queue_table`／任何既有模块对外语义）。 　━━━ 🔎 **2026-08-27 `OP-0827-E` 核实结论：⑵ 的 LAN 留步早已闭合，本轮属扫描器形态 1 误报** —— 派单件把本行列进「真待补做（载体扫描 `--strict` 在真 `.51` 上复跑）」，**实测该项已由 `OP-0826-V` 于 2026-08-26 做完并写进本行**（cherry-pick `e024ead` 已 ff push master；②③ 两类由「命中 0 处」改判为「🔴 本类无法核验」，四个端口正文字节数逐个不同＝四份真实响应而非本地桩）。**⇒ 本行无 LAN 侧待补项**，剩余的是 §四 `#118` 凭据轮换待拍板等非 LAN 项。误报根因已登记 §一 `#422`。　━━━　🔧 **2026-08-30 `OP-0829-W` 机制类 WIP 分诊改判：`[S:partial]` → `[S:blocked]`**（Shao Peishen 2026-08-30 答「全批」）。**依据＝本行自陈原话**「两处留步（守卫拦截／待拍板）」——阻塞方不是「现在能动手的人」，不属可动负荷，故改判为 `blocked`。🔴 **只改状态字段，工作范围·优先级·领取方一字未动**；**刻意用 `blocked`／`timed=` 而不用 🛑** —— 三者都不计入可动 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 行内原话），会让本行从催办面整个消失；`blocked` 只是不占配额，仍可见可催。误判可一条命令改回。**根因（`partial` 被当作可动 ⇒ 主体做完留尾巴反而永久占配额）根治已并入 `#435` 子项 E。**　━━━　➕ **2026-09-04 新增第 ⑸ 处（CC worktree `vibrant-cannon-9f1343`／分支 `claude/vibrant-cannon-9f1343`，回归排查 `test_工具-落库sweep.py` 两条既有失败时顺带坐实）——release 失败不产生信号，同族换了个载体**：起因＝`SyncReorderTests::test_multiple_batches_in_one_round_push_exactly_once` 与 `PendingCriteriaIntegrationTests::test_four_status_forms_processed_correctly_end_to_end` 两条既有测试转红。**先判定根因**：加调试输出实测坐实为**测试夹具过期，非生产代码退化**——`SweepTestBase.setUp()` 未跟着 `#437`「opener 守卫挂 release 咽喉」同步补上 `工具-opener块lint.py` 的夹具还原，导致隔离仓库里 `cmd_release` 因 `_opener_guard_violations`→`_load_opener_lint_module` 抛 `FileNotFoundError` 崩溃退出——**与本行 `#435`／`#382⑵` 两次「编辑锁脚本新增依赖、夹具没跟上」同一形态，第三次撞见**。**已修（只改测试夹具，commit `b63c517`；`工具-落库sweep.py` 对外语义零改动）**：`setUp()` 补 `shutil.copy` 真实 `工具-opener块lint.py`（其判据"取不到就 fail-loud，不回退成本地简化版"，故用真文件而非写桩，同 `工具-共享文档编辑锁.py` 自身既有处理）；顺带补 `.gitignore` 的 `__pycache__/` 规则（真实项目本有、夹具此前缺——该文件首次被真正 import 成功后才会落 `.pyc`，此前该路径一直因文件缺失而在写字节码缓存之前就崩溃，从未暴露这个缺口，回归时又误伤一条无关用例）。**全量回归 370 passed（另 51 subtests passed），两条目标测试转绿，无新增失败。** ⚠️ **顺带用调试输出实测坐实一处相邻但未修的生产侧隐患，同族**：`_strike_off_rows()` 的 `finally: _edit_lock(repo_root, "release")` 从不检查 release 子进程返回码——`cmd_release` 若因结构校验被拒、或因自身内部异常（本次撞到的正是这条：opener 守卫依赖加载失败）而非零退出，锁其实仍被占用，但该函数悄悄当作"已释放"处理，**不产生任何信号**；单批次轮次测不出来（没人再抢那把锁），只在一轮处理 2+ 个待提交批次时才现出下游「编辑锁占用中，跳过本轮」的症状，且症状离真正病灶隔着一层。**按 Shao Peishen 2026-09-04 指示未直接改**——这属于批次取活／push 计数／状态翻转语义，须先 openspec propose 再动，本处按其要求只如实留痕、不直接改，待领取方判断是否值得立项修复。
