---
title: "队列行日志 #407（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #407 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #407 · 外置前整格原文（2026-09-04 外置，9 段）

[S:done][D:机] 🆕 **2026-08-25 立行（Cowork 环境总线备派单件）**。**建议次序 T1 → T3 → T2**（T3 硬阻塞于 T1，需其副本路径；把唯一会改生产的 T2 放最后，前两件若暴露问题可随时叫停、此时生产一个字节没动）。🔴 **本行不解除 #170 的 🔒 方案阶段红线**——T1/T3 是演练、T2 是独立缺陷修复，三者都不是「开始迁移」；迁移窗口仍未排期。⚠️ **绕过常规分发的理由**：Shao Peishen 2026-08-25 会话内明示授权「开始回滚演练」，按环境保障线「紧急且经其明示授权」条款备件，非值周巡检派发。 　━━━　🔴 **2026-08-25 CC 执行回报（OP-0825-DRILL，commit `aa8d352`）——T1 ✅完成／T3 ⛔阻塞提权／T2 ⛔未开跑**：结果件＝`1-转型规划/0-全景路线图/OneDrive迁出回滚演练结果-队列407-2026-08-25.md`。 **⑴ T1 完成**（整树复制 165.3 s／57,210 文件／1.393 GB／FAILED=0；`worktree repair` **0.33 s**；校验 1.14 s；中文抽验 U+FFFD=0）。🔴 **但派单件 T1 步骤 2 的写法被实测推翻**——「在副本主仓库跑裸 `git worktree repair`」经一次性玩具仓库复现确认：**它改的是原始（＝生产）那一侧**，把 26 个生产 worktree 全部改绑到副本 `.git`，**副本自己一处没修**，且**只输出 `repair: .git file incorrect` 诊断式提示、不报错**；其后派单件写的「收工前删除副本」会连带删掉生产 worktree 所依赖的 git 目录。**未按字面执行**，改用已实验室验证为安全的「显式传路径」形式（改判 D2）。 **⑵ T3 阻塞**：`Register-ScheduledTask` 报 `Access is denied`(0x80070005)，需管理员 PowerShell——**注册脚本头部本就写着「用法（本机管理员 PowerShell…）」，派单件排 T3 时漏读了这一行**；步骤 2–6 未做。 **⑶ T2 未开跑**：独占自检未通过——`Get-Process claude` **18 个、其中 17 个早于本会话**，按硬约束 ④ 停下原样回报、不自行判断。 **⑷ 生产完整性三次复核 ＋ 收工终检全部通过**：worktree 27 条 prunable 0、`.git`／gitdir 指向 `C:\Dev` 均为 0、计划任务 4 个与开工前 JSON 快照**逐字比对差异 0**、影子任务无残留、`C:\Dev` 已彻底删除还原。 🔴 **本行保持 open**（T1 已交付，T2/T3 待前置条件具备后续跑）。 　━━━　🔴 **2026-08-25 Shao Peishen 四项定夺（Cowork 就地答后当场落档）**：**⑴ T3 补跑＝(a) 要补**——须 **Shao Peishen 本人开一个管理员 PowerShell**（演练实测非提权 `Register-ScheduledTask` 直接 `Access is denied` 0x80070005）；补跑目的＝坐实或排除「注册脚本先注销后注册、中途失败会留下『任务没了』」这条**目前仅为有依据的推断、未实证**的风险。**本项待管理员窗口，属他的动作 ＋ CC 执行。****⑵ T2 跑法＝(b) 安排明确静默窗口**，不只清进程——须同时满足：停 `ZhuopinAibotDevListener`（机器人 import `zhuopin_platform`）＋ 禁 `ZhuopinCommitSweep`（每小时 :36，sweep 亦 import）＋ 无其它 claude 会话存活。**三条已写入迁移方案件 S3-12 前置。****⑷ #170 三条修订＝(a) 已当场落档**：S3-10 改为「显式传每个新路径、禁裸跑 ＋ 逐个 `git rev-parse --git-common-dir` 回读、不得只看 `worktree list`」；S3-12 加三条前置；S3-13 加提权守卫与 `/COPYALL` 说明；§二 A2 行的「24 对/48 个指针」已按演练实测更正为「53 目录＝26 健康＋25 孤儿＋2 静默隐身」。⇒ **本行余下工作＝T3 补跑（等管理员窗口）＋ T2（等静默窗口）**，T1 已完成。 　━━━　🔴 **2026-08-25 T2 补跑完成（CC OP-0825-T2）——本行仍保持 open（T3 未做）**：T2 结果全文回写在 §一 **#406**（已销号），结果件新增 **§九**。**一句话**：**9/10 修好**，`find_spec` 由 MAIN 3／WORKTREE 5／NOT_FOUND 2 → **MAIN 9／NOT_FOUND 1**，回归 **1,928 passed／0 failed**；唯一未修的 `portal_gateway` **不是操作失败、是前提不存在**——其源码只在未并入 master 的分支上，无法「从主工作树重装」（幽灵 import 的第三种形态，见 #406 与 §四 #121）。✅ **§六 6.1 提出的两条前置建议本轮全部照做**（停 Listener ＋ 禁 sweep），且**两条都被实测证明必要**。🔴 **新增一条 T3 同族取证**：`Enable-ScheduledTask` 非提权同样被拒（S4U 任务改定义需 `SeTcbPrivilege`）⇒ **sweep 目前仍 Disabled、须管理员窗口恢复**（§四 #120）；而 `Start-ScheduledTask` 不需提权（Listener 已成功恢复）。⇒ **「改任务定义」与「触发任务」的提权分界，本次两侧都拿到了实证**，可直接写进 #170 的 S3-13。⇒ **本行余下工作 ＝ 仅 T3 补跑（等管理员窗口）**，T1／T2 均已完成。 　━━━　✅ **2026-08-25 深夜收口（Cowork，T1/T2/T3 三件全部有结论）**：**T1 ✅ 完成**（含安全改判 D2，拦下会劫持 26 个生产 worktree 的裸 repair 写法）；**T3 ✅ 结论已取**（管理员窗口补跑，影子任务 `ZhuopinCommitSweep_DrillOnly`）——**非提权 `Unregister-ScheduledTask` 直接「拒绝访问」、任务原封不动 ⇒「先注销后注册留下任务没了」的原风险不成立**；换出的真风险＝**非提权跑注册脚本时四个任务全部原封不动、仍指旧路径 ⇒ 迁移后静默停摆且无人被通知**，守卫目的改为 fail-loud；**T2 ✅ 9/10**（CC 执行，#406 已销号）——`find_spec` 复核 9 个包已回主工作树，仅 `portal_gateway` 仍 NOT_FOUND（**其源码未并入 master，属独立问题，非 T2 未做完**）。━━━ ⚠️ **【2026-08-25 深夜更正，原文误判 CC】** 本行原写「CC 收工漏了一项」——**该指控不成立，已更正**：opener 写死「恢复常驻是本任务的一部分」，它恢复了机器人却**未 `Enable-ScheduledTask ZhuopinCommitSweep`**，且其 commit message 已写明「sweep 停摆中」——**知道 sweep 停着、并因此改为自行 commit，却没意识到那正是自己该恢复的东西**。已由 Shao Peishen 手工启用并触发（`LastTaskResult` 267009＝运行中，正常）。**这是「收工恢复项须逐条回读、不认自陈」的又一实证。**━━━ **本轮五条判据已全部落档**：⑴ T3 结论改写 S3-13 守卫理由；⑵ `Get-Process claude` 判据作废（数的是 Electron 进程组，恒 14+ 永不归零）；⑶ `claude-code` 进程每轮新起即退 ⇒ 只测「此刻在执行」；⑷ 停机器人须三层处置（`Stop-ScheduledTask` 不够、先杀守护再杀 python、复核等 ≥70 秒）＝ 方案 S1 第 5 步重写；⑸ 前置条件按「自动触发物／人开的窗口」两分。**落点＝迁移方案件 §四／S1-5／S3-13 ＋ `0-学习与工具/取证方法知识库.md` §2.5（第七、第八条）。**━━━ ⏳ **遗留：`portal_gateway`**（源码未并入 master、装不上）——**不并入本行，随统一门户线另议**；本行余下无待办。 　━━━　🔴 **上句更正的全文（写在这里而不是抹掉原句，守「历史记录不追改」）**：CC **没有漏**——它按 opener 要求执行了恢复，`Enable-ScheduledTask` 与 `schtasks /Change /ENABLE` **两条都被拒绝访问**，随即按规矩登记 §四 #120 请 Shao Peishen 在管理员窗口做。**根因＝该任务 `LogonType=S4U`／`RunLevel=Limited`，改其定义需 `SeTcbPrivilege`，与属主是不是本人无关——与 T3 的 `Register-ScheduledTask` 被拒是同一堵墙。** ⇒ **它做了能做的、登记了做不了的，流程完全正确**；缺的只是「登记完成 ≠ 事情办完」这一步由人补。🔑 **本次我方的错**：仅凭「sweep 仍 Disabled ＋ commit message 写着 sweep 停摆中」就断定它「知道却没意识到该恢复」，**没有去读它登记的 §四 行就下了结论**——正是本项目已立的判据「**推翻一条既有记录前，须与原记录同口径复测一次**」的反面用例，且更正比原始错误更难被发现（原文带着「实证」二字）。
