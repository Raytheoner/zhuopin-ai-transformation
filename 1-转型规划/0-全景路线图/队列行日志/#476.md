# 队列 §一 #476 · 行日志（K2 外置件）

> 本件收纳 `#476` 状态格外置出来的历史回写段，原文原样、未改写。行内只留首段＋末段＋指针。


## 2026-09-08 K2 外置（CC 看护者 `OP-0908-T`，批 `B-0908_四泳道机制根治`）

外置原因：本批泳道复核结论与 Shao Peishen 答 3(a) 并入后，该行状态格达 4743 B、超 `ROW_LENGTH_CAP_BYTES` 4096 B。以下为原状态格中间段，原文原样；md5:`1e79571fe5b902e5b4c6ec8f1cb82928`。

**立行依据** ＝ Shao Peishen 2026-09-03 答 **G-4 = (b)**（「立队列行、标触发条件、不排期、不并进本包」），落字件见输入指针。 ━━━ 🔬 **实测：这三条纪律现在已经在漂了，而五份是同一天、同一条泳道写出来的**（2026-09-03 业务总线尽调，`git show claude/op0903b3-fin-scenarios:<五份 models.py>` 逐份核）：**三个不同的名字** —— FI6 `confirmed_by: str`（必填）／FI8 `confirmed_by_cfo: bool = False`／FI10 `confirmed_by: str = ""`（有默认空串）；**两种不同的默认语义** —— 必填 vs 默认空串；`needs_manual_review: bool = True` 在 FI5/FI6/FI9/FI10 四份出现、FI8 无。⇒ **这正是 `#345`「35 份手抄副本」的形状，只是还在第一天。** ━━━ 🔴 **为什么判「不现在收」（判据本身值得记）**：五个包**目前全是骨架、引擎一个都没建**（FI5 U9C 报销端点未核实／FI6 案例库须从零建／FI8 银行授权未取得（EE-2 Shao Peishen 本人推）／FI9 工时系统存在性未知（见 `#477`）／FI10 前置卡 `#475`）。此刻收编 ＝ **凭五份 dataclass 的字段名去猜公共契约**。**收晚了的代价是多几份手抄（可测、可查、可批量改）；收早了的代价是底座绑死一个猜错的契约（要动底座，五个场景陪着改）—— 两者不对称。** ━━━ ⚠️ **本行本身是「已知在漂、刻意不动」的登记**，不是待办遗漏；**开工前须先复核触发条件是否已满足，并把复核结论写进本行**。
