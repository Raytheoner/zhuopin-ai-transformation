---
title: "队列行日志 #151（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §四 #151 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §四 #151 · 外置前整格原文（2026-09-04 外置，8 段）

🔴🔴 **OP 编号没有分配机制 —— 唯一被当作「身份」用的编号，是唯一一个没有分配器的**（2026-09-02 `OP-0902-C` 实撞重号：Cowork 业务总线与「AI赋能项目workflow优化」线同日各派出一个 `[OP-0902-C]`，后者已在跑；Shao Peishen 当日答 (a) 并明示「**确保我们需要根治方案**」） ━━━ **⑴ 现象**：两条 Cowork 线同日给出同一个 OP 编号，两条 CC 带着同一身份在跑；发现时两者正在抢同一把队列锁（`CC-OP0902C-lane-watch` 持锁，本线 acquire 被拒）。 **⑵ 🔴 根因不是「忘了查」，是查不到**：取号方法是 `grep` 已落档的 `.md`，而 **OP 编号派出时只存在于聊天里的 opener 文本中，落档要等 CC 跑完并 commit**——而下一次取号往往就发生在这个窗口内。**实测 `grep -rho 'OP-0902-[A-Z]\+' --include=*.md .` 返回空**（当日已派出 A／B／C 三个，一个都没落档）⇒ **它看起来「所有字母都可用」，于是毫无阻力地给出了 C**。同族＝「只读命令返回太干净的结果时，先问它是不是根本没读到我以为的那个对象」。 **⑶ 三类编号对照，缺口一目了然**：§一/§四 行号＝`--reserve` **原子分配＋高水位线立即回写** ✅；§二 批次号＝**前缀查重** ✅（同日刚拦下本线一次 `B-0902_3` 撞号）；**OP 编号＝零机制** ❌。🔑 **同一天、同一批并发、两个编号池：有机制的那个拦住了，没机制的那个没拦住。** 而 OP 编号恰恰是模板库 §〇 明定的「**跨会话世界里的唯一身份**」。 ━━━ 🔴 **⑷ 关键检验：只加一个 `--reserve-op` 子命令，仍然不是根治** —— 他答 (a) 时特别叮嘱要根治，故此处如实拆开：**§一/§四/§二 那三个号之所以拦得住，不只是因为有分配器，而是因为它们都必须被写进队列文件**——写入那一步是**不可绕过的咽喉**，工具在那里校验（今天 §二 查重就是在写入时拦下的）。**而 OP 编号没有写入动作**：它的第一次出现是在一段**直接输出给 Shao Peishen 的文本**里，**那段文本不经过任何机器**。⇒ **没有咽喉可挂 ⇒ 只加分配器＝仍靠「我记得去调它」＝人守。** 🔑 **判据：一个东西要被机制守住，前提是它必须流经某个机器动作；只在人与人之间传递的东西，只能靠人守。** ━━━ ✅ **⑸ 根治方案（拟）＝把「派出 opener」这个动作本身机器化**，而不是给取号加个命令：给编辑锁工具（或独立 CLI）加 **`new-opener`** 子命令——输入「主题短名／执行环境／worktree／工作区／session／派出线／正文要点」，由它 **① 原子分配 OP 编号并立即回写高水位线 ② 写一条 OP 台账行（号／派出线／主题／时刻／状态） ③ 输出完整 opener 块**。**号的产生与文本的产生成为同一个动作，想跳过分配就拿不到文本。** ⇒ 这一举同时消灭四类已实证的问题：**重号**（本行）／**`set_session_title` 整行缺失**（模板库 §〇 补充三，2026-09-02 实测模板库自身三处 CC 模板全缺）／**`▶ 首次派出` 状态标记漏写**（§〇.19，同日实犯）／**`【设置】` 五字段格式漂移**（同日实犯，把 Cowork opener 的字段序抄成了 CC 的）。🔑 **这四件今天都发生过，而它们有同一个成因：opener 是纯人工文本。** 📌 **同族先例**：`set_session_title` 那条的根治手法是「**把前提焊进那一行**」，因为那一行必然被复制；本条同理——**如果 opener 文本必须由工具产出，号就必然被分配**。 ━━━ ⚠️ **⑹ 覆盖边界须先说清（不说清就会变成第二个「以为已解决」）**：`工具-opener块lint.py` 与 release 的 opener 守卫**只覆盖走了队列锁 acquire/release 流程的 opener**，直接粘出去的会话它看不见（模板库 §〇 已明写）。**因此本方案的价值恰恰在于把入口前移**——不是在事后扫描 opener，而是让 opener 只能从工具里出来。**若只做「release 时校验 OP 号」，会重蹈那条覆盖边界。** ━━━ **⑺ 落地约束**：改动 opener 生成与编号分配语义，按根 `CLAUDE.md` §5 机制类三条门槛命中第①条（**改变全项目口径：取号／编号约定**）与第③条（改既有模块对外语义）⇒ **须走 openspec（含 design 审）**；属 `[D:机]`，2026-09-02 机制类可动 WIP 仍超上限 ⇒ **登 §四 待腾位升格 §一**。 **⑻ 今日的临时缓解（明确不是修）**：本线改用**从字母表末尾倒取**（`OP-0902-Z`）——他人顺取、本线倒取，天然分开、零协调成本。🔴 **这只降低碰撞概率，不消除**；**不得因为它今天有效就当问题已解决**。  ━━━ 🔑 **2026-09-02 补记（本段因锁被 `CC-OP0902Z` 占用曾按协议〇.7 降级暂存，此为回补）：本行与「Cowork 直接派出 CC、免他粘贴」是同一个入口的两半，须合并设计、不得分两次做** —— **⑼ 底层能力已存在且已在生产使用，不需要新建**：`0-学习与工具/工具-opener批处理执行v2.ps1` 起**无头 CC**（`isolation: worktree`，**opener 正文原样传**），心跳落 `reports/lane-heartbeat/`；调用方是 skill `zhuopin-lane-clearpool` 与 `zhuopin-lan-closeout`。⇒ **「Cowork 起 CC」早已被授权并跑通，只是这条通路目前只被那两条 skill 编排调用。** **⑽ 为什么日常派单没用它**：那套编排的白名单／护栏是为「周末批量清 off-LAN 候选」设计的（判据正本＝`openspec/changes/lane-clearpool-skill/` design D1），红线写死「业务建造与一切对外发送永不自动入批；`.51`／真实库／规划文档／他线在办不碰」。⇒ **不是能力缺失，是编排未覆盖日常派单。** 🔑 **⑾ 与本行的关系（本次补记的要点）**：本行 ⑸ 的根治是「**opener 由工具产出**」（号与文本合一）；他要的是「**产出后直接执行**」。**两者共用同一入口** —— 先做 ⑸ 再单独做派发，第二次要重新设计同一条链路；只做派发不做 ⑸，**重号原样留着且自动派发会让它更快发生**。⇒ **合并：`new-opener` 生成（原子分配号＋写台账＋输出正文）→ 可选 `--dispatch` 直接经 `工具-opener批处理执行v2.ps1` 起无头 CC。** ━━━ ✅ **⑿ Shao Peishen 2026-09-02 答 S-1 ＝ (a)，原话「a；以后慢慢扩大」** —— **首期只开「纯落库」一类**，判据＝**「做错了能不能靠 git 回滚救回来」**：**能**（只动 `.md`、只 commit／push、有 §二 批次行为凭）⇒ 可自动派；**不能**（改生产数据、重启常驻服务、对外发送）⇒ 须他在场。🔑 该判据与 §一 `#418` 那条「**代码级回滚救不回被改写的数据**」同源，非新发明。📌 **当日四次派单实测验证该判据可用**：`OP-0901-F`（SC8 修 bug＝业务建造）不可自动／`OP-0901-I`（重启监听器）不可自动、当日确由他答 (a) 后才派／`OP-0901-G`、`OP-0901-H`、`OP-0902-A`、`OP-0902-Z`（落库）**属可自动那一类** ⇒ **四次里三类活、多数落在可自动侧**。 **⒀ 「以后慢慢扩大」的落法（建议沿用既有灰度手法，不新造）**：`zhuopin-lane-clearpool` 首跑即用「**上限强制 2 ＋ 须 Shao Peishen 在场 ＋ 三判据（误派 0／漏派入待人裁节／审计落档齐全）全过后方放开默认上限**」。⇒ **本条的扩范围也应逐档放开、每档有验收判据**，而不是一次开到位；**每次自动派发仍推企微告知他**（他不必动作，但要知道发生了什么）。 ⚠️ **⒁ 并发上限与退避必须同批定，否则「省掉粘贴」会换来「更多撞号与锁冲突」**：2026-09-02 当日实测并发已很密——`B-0902_1~11` 一天用满（本线只占 2 个）、`B-0902_3` 撞号被查重拦下、**`OP-0902-C` 撞号无机制未拦住**、队列锁两度被他线占用致本线 acquire 被拒并走协议〇.7 降级（其中一次占锁者正是重号那条 `CC-OP0902C-lane-watch`，另一次是本线派出的 `CC-OP0902Z`）。**这些都发生在「还没有自动派发」的今天。**
