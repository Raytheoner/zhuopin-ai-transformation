---
title: "队列行日志 #413（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #413 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §一 #413 · 外置前整格原文（2026-09-04 外置，5 段，md5:31f984cd）

[S:blocked][D:机] 🆕 **2026-08-26 立行**。**硬阻塞于两条**：⑴ #412（M1）完成；⑵ Shao Peishen 给出窗口日期。**2026-08-26 08:4x 全局冻结已就位**（机器人三层停并复核两条进程皆空、四个 Windows 任务 Ready/Disabled×3、拆件巡逻手工 pause），Shao Peishen 答「今天一次做完」⇒ **窗口即为 M1 完成之后**。🔴 **停机器人期间专员来件永久丢失、企微无离线补推**（2026-08-25 实测 29 分钟断线）——窗口若拉长须事前群告知。 　━━━　🔴 **2026-08-26 M2 干跑后补丁：冒烟第 7 项判据不足，须人工加严** —— `工具-迁移到新路径.ps1` 的 S4 冒烟清单第 7 项只写了「pytest 全量（至少 QD-A / QD-B / FI2）」，**grep 实测脚本内无 skip 相关判据**。而 #412 追记 ⑷ 已查明：T1 改字后 `QD-B/tests/test_golden_product_class.py` 由 **13 个实际执行变为 `13 skipped`**、`QD-A/tests/test_track_a_calibration.py` 由 `10 passed` 变为 `9 passed, 1 skipped`（两处判据都是「路径不存在就 skip」）。⇒ **迁移成功的判据不是「pytest 没有 red」，而是「这两处的 skip 归零：QD-B 的 13 必须变回实际执行、QD-A 的 1 必须变回 passed」**。**只看全绿会把一次没搬成的迁移读成成功**（同族＝一次返回「干净」的复核）。**⚠️ 本条不改脚本**（窗口进行中，改脚本须重新复制到仓库外并重跑干跑，徒增变量）——**由 M3 收口时人工核这一项**，判据以本行为准。 　━━━　✅ **2026-08-26 M2 完成、M3 进行中** —— 结果件＝`1-转型规划/0-全景路线图/OneDrive迁出M2M3执行结果-队列413-2026-08-26.md`（含七次中止逐条、S4 九项、5 条告警判读、5 条判据收获）。**仓库已迁至 `C:\Dev\zhuopin-ai`**；S0 备份 `C:\zhuopin-migration-backup\tree-20260826-102039`（52,766 文件 / 1.35 GB）**保留至观察期结束**。**S4 冒烟 1–7 全绿**：git 与 S0 一致且 ahead=0 脏文件 0 ／ 队列工具与编辑锁可用 ／ sweep 退出码 0 并补销 5 批次、发企微播报 ／ **Listener 出站 authenticated 仅一条 ＋ 入站真人真消息真回复（12:35）** ／ 两日班任务 Result=0 ／ **pytest skip 归零（QD-B 13 skipped→13 passed、QD-A 9+1skip→10 passed）**。🔴 **七次中止全部是脚本自身缺陷、无一是环境问题**，五处已在窗口内修复（robocopy `/LOG` 括号拆参 ／ common-dir 斜杠比较 ／ pip 顺序与 `--no-deps` ／ `$HOME` 只读变量 ／ `$snapDir` 可重入 ＋ XML 导出语法 `ONE` 与内容校验）。🔑 **最该记住的一条**：S0「导出四个计划任务 XML（回滚唯一依据）」实际导出的是四份 174 字节错误信息，而校验只看「存在且长度非 0」⇒ **回滚依据从上午到中午一直是空的，而所有校验都是绿的**。⏳ **余下两项**：⑴ **Shao Peishen 改 Cowork 项目级 Context 到新路径**（会话级已挂；另 `Check skill plugin updates` 定时任务提示「Has access to 1 folder not in this project」，须一并核）；⑵ 观察期 5 个工作日（每日核 sweep 有落库、机器人在线、拆件巡逻两班有产出）。 　━━━　✅ **2026-08-28 观察窗内「迁移残留三修」已完成（`OP-0828-F`，CC，worktree `agent-ab93efe1ec3e402d1`）——源＝`OP-0828-C` 月度体检简报 §九 建议 3／4／5，三条同源：仓库搬干净了，但「指着仓库的那些东西」没跟着改。按协议〇.10 并入本行、未取新号。** ━━ **⑴ `~/.claude/health-check.ps1` 扫描根（临期，09-01 那轮就会咬人）**：🔴 **没有按建议把 `OneDrive\Projects` 换成 `C:\Dev`** —— 先读全脚本查清 `$projects` 的真实语义是「项目根的**父目录**」（第 38 行拿它的每个子目录拼 `\.claude\skills`、第 86 行递归找 CLAUDE.md、第 95 行拿它的长度截相对路径），且实测 `OneDrive\Projects` 下仍住着 claude-howto／supplychain／行业研报／SalesMarketing 等 13 个项目 ⇒ **简单替换会把本项目找回来、却把其余全部丢掉，且丢掉同样不产生任何信号**。改法＝单值 `$projects` 升为多值 `$projectRoots = @("$base\OneDrive\Projects", "C:\Dev")`，四处用法逐处改为遍历多根。**并新增 `[0] Scan roots` 自检**：任一根不存在或根下零项目即记 finding（而不是静默少列几行），[3] 段尾加一行 `Scanned N CLAUDE.md across M root(s)`。**⚠️ 计划任务 `Claude-Env-HealthCheck` 一个字未动**（只改脚本内容）。**⑵ `zhuopin-send-followup` SKILL.md 两处旧路径**（实测行号与体检一致：第 82 行 `cd`、第 132 行 audit 台账 `$p=`）已改为 `C:\Dev\zhuopin-ai`。**⑶ `~/OneDrive/Projects/0-学习与工具/`（135 文件影子拷贝）已改名为 `0-学习与工具_废弃-勿跑`**，并在其内放 `README-勿跑.txt`（说明来历／权威副本在 `C:\Dev\zhuopin-ai\0-学习与工具\`／为何危险／何时可删）。🔴 **零删除**：改名后文件数 135 ＋ 新增 README ＝ 136。 ━━ 🔑 **本轮三条实测收获**：**⒜ 体检的「20 余份 CLAUDE.md」是估数，真值 ＝ 17** —— 修后手工跑一次实测：全库 27 份跨 2 个根，其中 `\zhuopin-ai\` 前缀 **17 份**，与 08-25 迁移前那一轮日志里 `\企业AI转型\` 的 17 份**逐份对上** ⇒ 09-01 那轮不会再假绿。**⒝ 「只改部署件、不改仓库源码」会让这次修复在下一次安装时原样回退** —— `0-学习与工具/定时任务源码/health-check-signal/health-check.ps1` 是同一脚本的仓库源码版，实测与修前部署件**逐字节同内容**（仅 CRLF/LF 之差）；已同步，否则 git 里留的还是旧逻辑。**⒞ 影子拷贝「0 独有」已自证、且比体检更严** —— 体检只比了顶层 62 个文件，本轮**递归全部 135 个逐文件 SHA-256**：123 同／12 异／**0 独有**，且 12 个异的**无一比仓库版新、无一比仓库版大** ⇒ 改名零数据风险。 ━━ **⚠️ 三处如实留白**：① **Cowork／claude.ai 侧的已安装 skill 副本本机核不了** —— 全用户目录穷举 `SKILL.md` 后，本机除仓库源码外**只有影子拷贝里那一份**（08-22 旧件，已随 ⑶ 改名一并冻结），线上那一份是否同源仍待 Shao Peishen 确认（体检建议 4 的后半段未闭合）。② **体检 §九 建议 6**（`README-安装步骤.md` 第 43 行 `Copy-Item` 源路径仍是旧路径）**本轮未动**——不在本派单件范围内，仍待派。③ `~/.claude/` 下 60 余份历史 `audit-2026*.log` 含旧路径，属历史记录，守「不追改」不动。　━━━　🔧 **2026-08-30 `OP-0829-W` 机制类 WIP 分诊改判：`[S:partial]` → `[S:blocked]`**（Shao Peishen 2026-08-30 答「全批」）。**依据＝本行自陈原话**「硬阻塞于两条：⑴ #412（M1）完成；⑵ Shao Peishen 给出窗口日期」——阻塞方不是「现在能动手的人」，不属可动负荷，故改判为 `blocked`。🔴 **只改状态字段，工作范围·优先级·领取方一字未动**；**刻意用 `blocked`／`timed=` 而不用 🛑** —— 三者都不计入可动 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 行内原话），会让本行从催办面整个消失；`blocked` 只是不占配额，仍可见可催。误判可一条命令改回。**根因（`partial` 被当作可动 ⇒ 主体做完留尾巴反而永久占配额）根治已并入 `#435` 子项 E。**
