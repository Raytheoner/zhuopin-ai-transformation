---
title: "队列行日志 #390（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #390 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #390 · 外置前整格原文（2026-09-04 外置，5 段）

[S:open][D:业] 🔧 **2026-08-26 CC `OP-0826-K` 已实现「批量跑得完 ＋ 这类行留可查诊断」；⒜⒝⒞ 原样未选，仍待唐燕萍** —— 与 §一 #418 同车（同 `fi2/feed_source.py`，一次部署，commit `918bd84`）。**已做**：新增 `parse_ap_lines(no_po_link="divert", diverted=[...])` —— 把「无 PO 关联」行分流留痕、其余行照常解析，批量加载不再一行打挂整批；**没地方留痕就抛错**（无处留痕的分流＝静默漏单，函数层面禁掉）；其余脏数据（缺 `ap_no`／`item_code`、数值非法）在两种模式下**一律照旧 fail-loud**；fail-loud 消息补上**单号与行位置**（原消息只有 pydantic 原文 —— `AP-2026010125` 那次 640 单当场中止，日志里查不出是哪张单打挂的）；`raw_ap_rows()` 同步剔除被分流行，保住它与 `load_ap_lines()` 的「按位置一一对应」既有契约（否则 webapp `_u9c_ap_real_line_no` 会因长度不等**整批回落**、全表 AP 行号显示悄悄退化 —— 又一个不报错的错）。新增 11 条单测。 🔴 **默认值刻意仍是 `"raise"`，本次修改不默认生效** —— 「费用类应付要不要进三单核对」是账务口径，⒜⒝⒞ **本班未选、原样留给唐燕萍**。⚠️ 并请注意：`"divert"` 只解决「批量跑得完」，**不等于已选 ⒜** —— ⒜ 与 ⒝ 的真正分歧在「该单**其余行**还参不参与核对」，只在多行单据上才显形，而实撞样本 `AP-2026010125` 只有 1 行、两者看不出差别。 ⚠️ **本行 ③「用 `ap_supplier_codes` 在真实数据上跑通一次全量」仍未做**（同 #418 ⑸：需 `.51` 部署与生产重跑，属待批准动作，**非 LAN 阻塞**）—— **本行销号仍须含这一次**，不因代码已写而提前销。 ━━━ **原立行记录**：[S:open][D:业] 📥 **待领（2026-08-24 CC `OP-0823-E` 立行）** —— 本轮为绕开它，改用「逐单加载、逐单捕获」完成了 #375 的全量计算（639/640 成功），**属分析脚本的一次性绕行，未改任何生产码**；生产侧 `feed_source` 一字未动，哑雷原样还在。  ━━━ 🔴 **2026-09-03 §四 `#123` 裁决派生（`OP-0903-D`，Shao Peishen 答「都按你说的」）**：唐燕萍试用撞上的第二类错数（10 张 AP 单 4 张假报「发票缺失」，已立 §一 `#418`）**定为插队优先，且与本行、`#372` 同车派 CC 修** —— 三者同触碰 `fi2/feed_source.py`，**一次部署**。⚠️ **在修好之前不宜再请她做任何基于面板数字的判例**（含 §一 `#391` 那 204 项判例包）；`#391` 引用的「无发票支撑 883 项」底数可能整体虚高（抽样 4/10 若可外推）。 　━━━　🔬 **2026-09-03 CC `OP-0903-F1`（`B-0903_90` A2 段）：独立复核代码/测试维持既有结论 ＋ 新增只读探针脚本补齐期望产出①③的执行工具（本轮未产出真实规模数字）** —— 复核方法：重读 `fi2/feed_source.py::parse_ap_lines`／`APNoPOLinkRow`／`FeedSource.load_ap_lines` 全文，逐条核对 `no_po_link="divert"` 模式行为与本行既有记录一致；FI2 全量测试 **230 passed/9 skipped**（含 `test_divert_lets_the_whole_batch_finish_and_keeps_the_row_traceable` 等七条既有 divert 专项用例，零回归）。🔴 **⒜⒝⒞ 本轮仍未选、仍待唐燕萍**（她闸 🔒 锁，`工具-跟进闸查询.py --to 唐燕萍` 本轮现跑核实）。 **本轮新增**：`scripts/probe_390_no_po_link_impact.py`（只读，写法同既有 `probe_424_itemcode_candidates.py`）—— 用 `FeedSource(data_source="u9c", ap_supplier_codes=..., ap_no_po_link="divert")` 真实驱动一次 `ap_supplier_codes` 批量取数（即本行期望产出③“批量模式至今零次全量真实验证”要补的那一次），并从 `fs.ap_no_po_link_rows` 与已解析 `APLine` 联合统计出 **⒜（只排除坏行本身）与 ⒝（该单命中即整单一并排除）两个候选的行数级影响面对比**（⒝ 恒 ≥ ⒜，差值＝⒝ 比 ⒜ 多排除多少本来干净的行），产出即本行期望产出①要的“判例材料”。🔴 **本轮未运行该脚本**——本 worktree 无本地 `.env`、且硬边界禁止本轮触碰 `.51`，无真实 U9C 凭据可用；脚本已通过语法检查与 `工具-引导样板lint.py`，供下一个具备真实凭据/`.51` 访问权限的 session 直接跑出真实数字，不必重新设计取数与统计逻辑。产出归属“待唐燕萍闸开后并入下一封财务信”（同 `财务部#16` 起草时“开放点计数”已写明的 2 项剩余开放点之一——`#390` 无 PO 关联应付行）。**本行仍不销号**：期望产出①（真实数据量化）与③（真实批量全量验证）均待下一轮具备真实数据访问的 session 运行本脚本后完成。 　━━━　✅ **2026-09-03 CC `OP-0903-F1`（主仓续做，`B-0903_95` 业务总线捡出探针脚本后）：期望产出①③首次真实全量数据到手** —— 主仓 `C:\Dev\zhuopin-ai` 本地 `.env` 实含全套 `U9C_*` 凭据（此前 worktree 会话核对时误判为缺失，系 bash `grep -oE '^[A-Z_]+='` 未正确处理该文件的行结构漏读，已用 Python 逐行核实更正，教训：凭据类文件核验一律用 Python 明确 `encoding`/逐行处理，不信任 bash grep 的沉默结果）。用 `ZpConnector.get_suppliers()` 只读枚举出**当前 U9C 里全部 546 个供应商编码**（该端点即“供应商价格”视图，非专为本行设计，但目前是唯一可得的供应商全量枚举源）；以此 546 个编码逐一驱动 `FeedSource(data_source="u9c", ap_supplier_codes=..., ap_no_po_link="divert")` 真实批量取数，全程只读，`ZpConnector` 内建 `ConnectorAudit` 留痕（`reports/fi2_access_trace.jsonl` 639 条哈希链记录，逐条可核）。 **③ 批量真实全量验证——通过**：本次取数覆盖 **6,324 张历史 AP 单**（跨 2021~2026，因 546 个编码含大量历史/非当年供应商，覆盖面远超 `#418`/`#423`/`#424` 常引用的“651/672 张”——那是发票已匹配子集，本次是 U9C 里能查到的全部 AP 单据，🔴 两个数字口径不同，不可混用比较）、共 **28,079 行**（26,251 正常解析 ＋ 1,828 分流）。**批量加载全程未中止**——若无 `divert` 修复，现状默认 `"raise"` 下这 1,828 行里任意一行都会让整批当场清零。**本行销号所差的“用 `ap_supplier_codes` 在真实数据上跑通一次全量”至此已完成。** **① ⒜/⒝ 影响面——首次真实全量数**：命中“至少一行无 PO 关联”的单据 **1,230 / 6,324 张**（19.4%）；⒜（只排除坏行本身）排除 **1,828 行**；⒝（该单命中即整单退出核对）排除 **2,296 行**（多出 **468** 行本来干净的行，放大倍数 ×1.26）。🔑 **本次新增的关键细分**（原探针只输出汇总数，本轮就其 `--dump` 产出的逐单据 JSON 另行分析所得）：1,230 张受影响单据里，**1,187 张（96.5%）整单全部行都无 PO 关联**——这些单据 ⒜ 与 ⒝ 结果完全相同（没有“干净行”可供 ⒜ 保留）；⒜⒝ 的真正分歧**只集中在 43 张部分脏的单据上**（3.5%）——这 43 张单据自身贡献 265 行真脏 ＋ 468 行连带殃及的干净行，**上面那 468 行的差值 100% 来自这 43 张单据**。⇒ **这把“⒝ 会不会误伤”这个问题从“覆盖 1230 张单”收窄成“覆盖 43 张单、可逐张列给她看”，判例材料因此能做得比原先设想的更具体。** **明确的口径边界（不越界）**：⒜⒝⒞ 本轮仍**未选**，仍待唐燕萍签认（她闸 🔒 锁，同既有记录，`工具-跟进闸查询.py --to 唐燕萍` 本轮现跑核实）；本轮**未修改任何默认值**（`ap_no_po_link` 生产调用方仍不传该参数，`webapp.py` 未变）；**全程只读**，主仓 `.env` 与 `ZpConnector` 均未被写入或修改，未部署 `.51`，未发任何对外消息。 **产出即“待唐燕萍闸开后并入下一封财务信”的判例材料，本次已补齐真实全量数**——不必再等下一轮 `.51`/真实数据访问。原始数据（546 供应商编码全量、逐单据分布）已落盘本机 `probe390_out.json`（未入库，同 FI2 审计脱敏口径，不含金额/数量）。 **本行仍不销号**：期望产出②③已完成（②见本行既有 `OP-0826-K` 记录，③见本次）；①（选定 ⒜⒝⒞ 之一）仍是唐燕萍的账务口径，不由本方自选。

---

# §一 #390 · 外置前整格原文（2026-09-04 外置，3 段，md5:c8429bf4）

[S:open][D:业] 🔧 **2026-08-26 CC `OP-0826-K` 已实现「批量跑得完 ＋ 这类行留可查诊断」；⒜⒝⒞ 原样未选，仍待唐燕萍** —— 与 §一 #418 同车（同 `fi2/feed_source.py`，一次部署，commit `918bd84`）。**已做**：新增 `parse_ap_lines(no_po_link="divert", diverted=[...])` —— 把「无 PO 关联」行分流留痕、其余行照常解析，批量加载不再一行打挂整批；**没地方留痕就抛错**（无处留痕的分流＝静默漏单，函数层面禁掉）；其余脏数据（缺 `ap_no`／`item_code`、数值非法）在两种模式下**一律照旧 fail-loud**；fail-loud 消息补上**单号与行位置**（原消息只有 pydantic 原文 —— `AP-2026010125` 那次 640 单当场中止，日志里查不出是哪张单打挂的）；`raw_ap_rows()` 同步剔除被分流行，保住它与 `load_ap_lines()` 的「按位置一一对应」既有契约（否则 webapp `_u9c_ap_real_line_no` 会因长度不等**整批回落**、全表 AP 行号显示悄悄退化 —— 又一个不报错的错）。新增 11 条单测。 🔴 **默认值刻意仍是 `"raise"`，本次修改不默认生效** —— 「费用类应付要不要进三单核对」是账务口径，⒜⒝⒞ **本班未选、原样留给唐燕萍**。⚠️ 并请注意：`"divert"` 只解决「批量跑得完」，**不等于已选 ⒜** —— ⒜ 与 ⒝ 的真正分歧在「该单**其余行**还参不参与核对」，只在多行单据上才显形，而实撞样本 `AP-2026010125` 只有 1 行、两者看不出差别。 ⚠️ **本行 ③「用 `ap_supplier_codes` 在真实数据上跑通一次全量」仍未做**（同 #418 ⑸：需 `.51` 部署与生产重跑，属待批准动作，**非 LAN 阻塞**）—— **本行销号仍须含这一次**，不因代码已写而提前销。 ━━━ ✅ **2026-09-03 CC `OP-0903-F1`（主仓续做，`B-0903_95` 业务总线捡出探针脚本后）：期望产出①③首次真实全量数据到手** —— 主仓 `C:\Dev\zhuopin-ai` 本地 `.env` 实含全套 `U9C_*` 凭据（此前 worktree 会话核对时误判为缺失，系 bash `grep -oE '^[A-Z_]+='` 未正确处理该文件的行结构漏读，已用 Python 逐行核实更正，教训：凭据类文件核验一律用 Python 明确 `encoding`/逐行处理，不信任 bash grep 的沉默结果）。用 `ZpConnector.get_suppliers()` 只读枚举出**当前 U9C 里全部 546 个供应商编码**（该端点即“供应商价格”视图，非专为本行设计，但目前是唯一可得的供应商全量枚举源）；以此 546 个编码逐一驱动 `FeedSource(data_source="u9c", ap_supplier_codes=..., ap_no_po_link="divert")` 真实批量取数，全程只读，`ZpConnector` 内建 `ConnectorAudit` 留痕（`reports/fi2_access_trace.jsonl` 639 条哈希链记录，逐条可核）。 **③ 批量真实全量验证——通过**：本次取数覆盖 **6,324 张历史 AP 单**（跨 2021~2026，因 546 个编码含大量历史/非当年供应商，覆盖面远超 `#418`/`#423`/`#424` 常引用的“651/672 张”——那是发票已匹配子集，本次是 U9C 里能查到的全部 AP 单据，🔴 两个数字口径不同，不可混用比较）、共 **28,079 行**（26,251 正常解析 ＋ 1,828 分流）。**批量加载全程未中止**——若无 `divert` 修复，现状默认 `"raise"` 下这 1,828 行里任意一行都会让整批当场清零。**本行销号所差的“用 `ap_supplier_codes` 在真实数据上跑通一次全量”至此已完成。** **① ⒜/⒝ 影响面——首次真实全量数**：命中“至少一行无 PO 关联”的单据 **1,230 / 6,324 张**（19.4%）；⒜（只排除坏行本身）排除 **1,828 行**；⒝（该单命中即整单退出核对）排除 **2,296 行**（多出 **468** 行本来干净的行，放大倍数 ×1.26）。🔑 **本次新增的关键细分**（原探针只输出汇总数，本轮就其 `--dump` 产出的逐单据 JSON 另行分析所得）：1,230 张受影响单据里，**1,187 张（96.5%）整单全部行都无 PO 关联**——这些单据 ⒜ 与 ⒝ 结果完全相同（没有“干净行”可供 ⒜ 保留）；⒜⒝ 的真正分歧**只集中在 43 张部分脏的单据上**（3.5%）——这 43 张单据自身贡献 265 行真脏 ＋ 468 行连带殃及的干净行，**上面那 468 行的差值 100% 来自这 43 张单据**。⇒ **这把“⒝ 会不会误伤”这个问题从“覆盖 1230 张单”收窄成“覆盖 43 张单、可逐张列给她看”，判例材料因此能做得比原先设想的更具体。** **明确的口径边界（不越界）**：⒜⒝⒞ 本轮仍**未选**，仍待唐燕萍签认（她闸 🔒 锁，同既有记录，`工具-跟进闸查询.py --to 唐燕萍` 本轮现跑核实）；本轮**未修改任何默认值**（`ap_no_po_link` 生产调用方仍不传该参数，`webapp.py` 未变）；**全程只读**，主仓 `.env` 与 `ZpConnector` 均未被写入或修改，未部署 `.51`，未发任何对外消息。 **产出即“待唐燕萍闸开后并入下一封财务信”的判例材料，本次已补齐真实全量数**——不必再等下一轮 `.51`/真实数据访问。原始数据（546 供应商编码全量、逐单据分布）已落盘本机 `probe390_out.json`（未入库，同 FI2 审计脱敏口径，不含金额/数量）。 **本行仍不销号**：期望产出②③已完成（②见本行既有 `OP-0826-K` 记录，③见本次）；①（选定 ⒜⒝⒞ 之一）仍是唐燕萍的账务口径，不由本方自选。 ━━━ 📎 历史回写 3 段已于 2026-09-04 外置（K2 口径，原文原样 md5:90652b3b）见 `1-转型规划/0-全景路线图/队列行日志/#390.md`；本格只留首 1 段＋末 1 段
