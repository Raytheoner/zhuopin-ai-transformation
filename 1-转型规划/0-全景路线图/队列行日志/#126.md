---
title: "队列行日志 #126（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #126 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #126 · 外置前整格原文（2026-09-04 外置，8 段，md5:c1d414f3）

🔴 **落库 sweep 的 `_run_git` 无 `errors=` 兜底 —— git 输出里出现一个非 UTF-8 字节即抛 `UnicodeDecodeError`**（2026-08-27 `OP-0827-A` 波次收口时实撞，非纸面推断）。 ━━━ **实撞形态**：本轮 sweep 首跑（有 6 个待补推提交那一轮）在自身日志的 `=== sweep 运行 ===` 表头**之前**打了一段 stderr traceback，末两行＝`File "<frozen codecs>", line 325, in decode` ／ `UnicodeDecodeError: 'utf-8' codec can't decode byte 0x8f in position 2681: invalid start byte`；**该轮此后一切正常**——七个批次全部落库、补销尾巴六条、台账重跑、统一推送全部成功；紧接着的第二轮（§二 空转、无待推提交）**未复现**。 ━━━ **定位**：`0-学习与工具/工具-落库sweep.py::_run_git`（约 L783）对每次 git 调用用 `text=True, encoding="utf-8"`，而**全文件 `errors=` 出现次数为 0**（grep 实测）⇒ git 任一输出流含非 UTF-8 字节即抛；本次被某个宽 `except Exception` 接住、只打 traceback 不中止。`0x8f` 是 GBK 尾字节，怀疑源＝某份非 UTF-8 编码文件进了 diff/status 输出，或 push 进度流——**未坐实，须复现后再改**。已排除 git hook（`.git/hooks` 下无非 sample 钩子、`core.hooksPath` 未设）。 ━━━ 🔑 **为什么值得留一行**：它属「机制自身失效」族，但形态与 `#398` 那三处**相反**——那三处是**坏了不产生信号**，本条是**产生了信号却被埋在别的输出里**（traceback 打在表头之前，被后续几十行正常日志顶走）。**同样不会有人发现，只是原因换了一个。** ⚠️ 真正的风险不是本次这条，而是**下一次同样的抛落在 `_reconcile_with_origin_and_push` 这类关键路径上而仍被宽 except 接住**——那时表现会是「sweep 说自己跑完了、实际某一步没做」。 ━━━ 🔴 **为什么进 §四 而不是 §一**：本班原已按收工纪律在 §一 立行（取号 #423），但 release 被 WIP 闸拦下——**§一 机制类可动 WIP 实测 27／22 已超上限**。本轮无人在场，**不擅自关他人在办行腾位**，也**不动 `--force-mechanism-wip` 逃生阀**（本条属非阻塞缺陷，不满足「紧急必须此时立行」，用逃生阀开这种口子正是规则退休制要防的侵蚀）。⇒ #423 那一行已连同其所在的队列文件改动一并 `git checkout --` 撤回，改立本行。🔴 **注意：撤回同时把顶部高水位线也退回到 `§一 #422`，故 `#423` 实际是被释放、会被下一个占号者复用**——**不要把日后出现的 `#423` 当成本条**（本条与该编号无关，本条的唯一身份是 §四 `#126`）。⚠️ 这与 acquire 当时打印的「即使本次未写满，编号不复用」**并不矛盾**：那句话的前提是「预留后正常 release」，而本次走的是撤回整份文件改动，高水位线随文件一起回退了。 ━━━ **请你定夺（三选一）** ⒜ **维持现状**（建议·默认）——本条非阻塞，何时腾出 WIP 何时转 §一 立行；⒝ **你指定关闭哪一条既有机制类可动行**腾位，随即转 §一；⒞ **走 WIP 豁免**直接放行——🔴 本班不建议，理由同上。 ━━━ 🔑 **顺带一条比本项本身更要紧的观察**：**WIP 27／22 这个超限不是本项造成的**，是既存状态（2026-08-26 `OP-0826-L` 行内已记过一次 24／22）。**它从 24 涨到 27，中间没有任何人被拦下过**——拦阻只发生在「有人想新立行」的那一刻；**对「已经超了多久、为什么越涨越多」这件事本身，机制是沉默的**。这与根 `CLAUDE.md` 里那条『WIP 上限降档要靠人记得收回』是同一处的一体两面。 ━━━ ✅ **2026-09-03 Shao Peishen 裁决（§四 分诊消化，Cowork 业务总线 `OP-0903-D`；他两批均答「都按你说的」）**： **⒜ 维持现状。** **理由**：不答的代价是「停在原地」而非「错误继续发生」；⒝ 关他人行属 🟡、⒞ 走逃生阀本行自陈不建议。🔴 **但本行点出的真风险另立一行、不占当前 WIP**：「下一次同样的抛落在 `_reconcile_with_origin_and_push` 这类关键路径上而仍被宽 except 接住 ⇒ sweep 说自己跑完了、实际某一步没做」。
