---
title: "队列行日志 #128（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §四 #128 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §四 #128 · 外置前整格原文（2026-09-04 外置，28 段）

🔴 **12 个 worktree 的 `git status` 全部失败——它们的未提交产出对每一个 git 工具都不可见**（2026-08-27 `OP-0827-C` 接线真跑时由第 7 类告警本身撞出，非人工普查）。 ━━━ **根因已坐实、只有一个字**：这 12 个 worktree 的管理目录 `.git/worktrees/<名>/` 里**缺 `commondir` 文件**（正常内容＝ `../..`），健康的 worktree 一律有它。缺它 ⇒ 在该目录下跑任何 git 命令都答 `fatal: not a git repository: C:/Dev/zhuopin-ai/.git/worktrees/<名>`。**实测对照**：`queue-table-reachability-2`（坏）目录内 7 个文件、无 `commondir`；`scanner-wiring-422-unclosed-94e730`（好）目录内 9 个文件、有 `commondir` 与 `config.worktree`。 ━━━ **名单（12 个）**：`conftest-pytest-regression-c9d07e`／`fi2-webhook-var-check-bc19fa`／`followup-dispatch-apply-25679f`／`gallant-lumiere-b482be`／`musing-pascal-68d14e`／`observation-window-archive-91b68d`／`platform-bootstrap-apply`／`queue-313-prerequisites-7d9565`／`queue-lint-bare-pipe-312efa`／`queue-table-reachability-2`／`recursing-hofstadter-eb7215`／`sweep-deadlock-transition-tasks-ba394b`。**它们仍出现在 `git worktree list` 里**（共 38 个），所以看上去一切正常。 ━━━ 🔑 **为什么这条比「12 个目录坏了」要紧得多**：⑴ **它是 `#267` 真实丢件事故的同一个形状**——里面可能躺着未提交产出，而**任何以 git 为入口的工具都看不见它们**（`工具-孤儿worktree扫描.py` 与新接线的第 7 类形态 3 都是），一次 `git worktree prune` 或手工删目录就永久消失；⑵ **它坏得毫无声响**：`git worktree list` 照列、目录照在、文件照在，只有真的进去跑一条 git 命令才会露馅——这正是根 `CLAUDE.md` §5「工具静默回退」那一族；⑶ **是新装的守卫把它照出来的**，这本身就是第 7 类接线当天的第一个真实产出。 ━━━ 🔴 **本班未处置的理由（两条，都是硬的）**：① 这 12 份属他人触碰区，**内容该救还是该弃须由知情方判断**，我连它们里面有什么都还看不见；② 写 `commondir` 是往 `.git` 内部补文件，虽只读性质地恢复可见性、且删掉即可回到现状，但**不属 `#422` 三件范围**，不宜顺手扩。 ━━━ **请你定夺（三选一）** ⒜ **先只恢复可见性，再分头处置**（建议）——往 12 个目录各写一行 `../..` 到 `commondir`（幂等、可逆、不动任何工作区内容），随即由第 7 类形态 3 自动照出哪几份真有未提交产出，**有产出的才逐份找知情方**，其余交 `工具-孤儿worktree扫描.py` 常规清理；⒝ **整体交值周巡检**当作一次专项，本行只作登记；⒞ **明确判为可弃**——12 份一律 `git worktree remove --force` 并接受其中内容永久丢失（🔴 本班不建议：在还看不见内容之前判可弃，与 `#267` 当时的判断路径完全一样）。 ━━━ ⚠️ **不答的代价不是「停在原地」**：`unavail:form3` 这个 key 会每天重报一次直到修好——**一个长期红着的告警会把整条第 7 类判据训练成噪音**（`#398` 前三处已有三个先例）。故本项**宜早不宜迟**。  ━━━ ✅ **2026-08-27 已恢复可见性并判定完毕（CC-OP0827D，`OP-0827-D`，主工作区）——他答本行三选一取 ⒜** ━━━ **⑴ 修法与本行判定完全一致，一行字**：往 12 个 `.git/worktrees/<名>/` 各写一个 `commondir`，内容 `../..`。**10 个当场恢复**；另 2 个（`fi2-webhook-var-check-bc19fa`／`platform-bootstrap-apply`）连 `HEAD`／`index`／`fsmonitor--daemon` 一起没了，补 `commondir` 后仍 `fatal`，再从 `ORIG_HEAD` 与同名分支 tip 重设 `HEAD` ＋ `git read-tree HEAD`（**只写索引、不动工作树**）才恢复。 ━━━ 🔴 **⑵ 判定结论：12 个里一份未闭合产出都没有，一个字节都不用救。** 恢复可见后 `git status` 给出的**每一处改动都是删除、且 10 个完全一模一样**：`.claude/commands/opsx/{apply,archive,explore,propose,sync}.md` 五个；`platform-bootstrap-apply` 另多 6 个（`.claude/settings.json` ＋ 5 个 `openspec-*/SKILL.md`）。**这五个文件就是 `#267` 记的「`git worktree remove` 报 Permission denied 之前已删除 `commondir` 与 5 个真实文件」里的那 5 个** —— ⇒ **本行这 12 个不是「躺着未闭合产出的坟场」，是 `#267` 那次失败删除留下的残骸**，被删的文件在 master 里全都还在。 ━━━ **⑶ 那 2 个重伤的另有收获**：其 `ORIG_HEAD` 各吊着一笔**不被任何分支包含**的游离提交（`f2a9920` 载体健康守卫／`5499e89` `ensure_paths()`），一次 `worktree prune`＋`gc` 即永久消失。**已逐笔比对：两笔都是 rebase 前的副本**，master 侧对应物为 `b6370a6`／`1e0ad57`，代码零差异，差的只有 `0-全景路线图/` 下几行且都是 master 侧更新 ⇒ **无一可丢，不必救**。 ━━━ **⑷ 连带已消一条每日噪音**：`#422` 点名「`unavail:form3` 每天重报一次直到有人修好这 12 个」——修完复跑 `工具-孤儿worktree扫描.py`，12 个已全部落进②桶（可判），**判据不可用一项归零**。 ━━━ 🔴 **⑸ 未做：12 个 worktree 的物理清理，本班刻意不做，理由三条** ——① 它们此刻仍被扫描器判为②「有未提交改动」（即那些删除），CLAUDE.md 删除三前提未满足；② **本班执行期间 `OP-0827-E` 已开跑**（12:12，worktree `lan-cleanup-scanner-fix-6a5653`，正在改 `工具-未闭合产出扫描.py`），台面并发变动中；③ **危害已解除，删除只剩整洁收益**：可见性回来了、内容判清了、噪音消了，而删除不可逆 —— 同 `#170` 判据「宁可不清、不可错清」。**⇒ 建议交常规清理（`工具-孤儿worktree扫描.py` 走 CC），可先 `git checkout -- .claude` 把那些误删文件还原、使 12 个落回①/③桶再走既有流程。** ━━━ 🔑 **本行留给下一个人的一句**：**这 12 个之所以能坏 17 天没人发现，是因为它们坏在「git 自己看不见自己」这一层** —— `git worktree list` 照列、目录照在、文件照在，`工具-孤儿worktree扫描.py` 也照跑不报错。**只有真的 `cd` 进去跑一条 git 命令才露馅**，而没有任何一个例行动作会那么做。  ━━━ 📎 **⑹ 收工前补记：⑸ 里建议的那一步已当场做掉，本行 ⑸ 的「未做」只剩物理删除那一半**——已在 12 个 worktree 各跑 `git checkout -- .claude`（**纯修复：把失败删除误删的已跟踪文件从 HEAD 还原，非改判**），**12 个现已全部 `git status` 干净、剩余改动 0**。⇒ 它们已从「②有未提交改动」落回可清理形态，**下一个人走 `工具-孤儿worktree扫描.py` 常规清理即可，不必再判内容**。 ━━━ **⑺ 终态数字（重跑 `工具-未闭合产出扫描.py` 实测，非推断）**：`判据不可用` **12 → 0**；形态 3 **5 处 → 2 处**（剩的正是不该动的两个：`wecom-service-home` 的生成物、主工作区那份登记豁免的波次件）；本轮报告显式打出「**已解除 12 项**」。**⚠️ 中间态留痕**：还原之前那一轮，12 个是从「判据不可用」变成「形态 3 各 5 个删除」的——**即修好可见性的第一效果是告警变多、不是变少**，因为它们一直是脏的、只是此前看不见。**下次遇到同类修复别把「数字涨了」读成修坏了。**  ━━━ 🔑 **⑻ 收工时自撞的一个取号陷阱，连同它当场纠正我的那一半，一并记在这里（本班原想为它新立 §一 一行，被 WIP 上限 27／22 拦下 —— 拦得对，故改折进本行、不占机制 WIP）** ━━━ **怎么撞上的（可复现三步）**：① `acquire --who X` 占锁；② 中途要新立一行、跑 `acquire --who X --reserve 1 --section 四` 取号 ⇒ **被自己的锁拒绝**（`--reserve` 挂在 `acquire` 上，而 `acquire` 对同一身份不可重入）；③ 改用 `append-row --number 132` 写字面号 ⇒ 行写进去了，**但顶部那行「编号高水位线」文本没被推**（只有 `--reserve` 会推它）。**⇒ 随后另一位（`OP-0827-E`）acquire 读到的仍是 `§四 #131`。** ━━━ 🔴 **但我第一版结论写错了，被机制当场纠正，如实留痕**：我原判「这是留给下一位的一颗雷」。**实际不是** —— `release` 有一道守卫：**新增行的编号若不属于本次持锁期间 `--reserve` 预留的集合，release 直接拒绝**（本班第二轮试图新立一行时正是被它拦下）。⇒ 真实后果是**当场 fail-loud、拦在收工那一步**，不是静默留雷。**机制比我以为的更严**。 ━━━ **仍然成立的那一半**：⑴ **`--reserve` 在持锁期间用不了**，正路走不通、逼人绕；⑵ **绕出来的高水位线滞后是真的**（本班已在持锁期间手工把它由 `§一 #422 ｜ §四 #131` 推到 `§四 #132`，并已核对 `#132` 确为 §四 表内最后一行、无 `#133`；`§一` 因新行已撤而回退为 `#422`）。**建议修法**：让 `--reserve` 对持锁者可重入（`--who` 与持锁人一致时不拒绝）——一条改动即可让人不必绕。**本班不实施**（属他人触碰区且 WIP 已超上限），仅登记。 ━━━ 🔑 **两条判据**：**⑴ 「一个写入入口和一个登记该写入的入口分成两个命令时，只要其中一个能被单独调用，两者就会漂」** —— 与 `#345` 的 35 份手抄副本同族，不同的是这次漂的不是内容，是**计数器与它所计的东西**。**⑵ 更要紧的一条：我在写下「这会留一颗雷」时，并没有去试一次 release 看它拦不拦。** 判一个机制有没有防护，**得真的去碰那道防护**，不能从「我没看见防护」推出「没有防护」——这与本行 ⑵ 的教训是同一个：**看不见，不等于不在。** 　━━━　🔬 **判据→证据→处置（OP-0831-W，2026-09-01）——本行是否与「sweep 跑不完」同源**：**判据＝否，两者是两类不同的失效，不同源**。**证据**：直接对本行列出的 broken worktree 逐一实测 `git -C <路径> status`，**全部立即返回 `fatal: not a git repository`（exit 128），零延迟、非挂起**；sweep 自身的 `_check_unclosed_outputs` 对这类调用用 `check=False`，已正确捕获为「判据不可用」并原样列出、不中止整轮——本行 2026-08-27 判定 ⑵ 的既有结论本次复测依旧成立。sweep 真正「看起来跑不完」的根因另有其二，详见 §一 `#326` 本次回写：其一，`main()` 把全部日志攒进内存列表、只在收尾一次性吐出，健康跑法本就全程零输出，与真卡死在人工观察上不可区分；其二，`_parse_section_two`/`_parse_section_one` 原用裸 `str.find()` 定位标题，被队列文件里讨论「标题该怎么匹配」的元讨论段落逐字命中而误锁定假章节，导致连续多轮 `exit 0` 却零批次落库——这才是逼出 `Shao Peishen` 手工绕过落库（`d063d56`）的真正原因。两个根因均已随 `d063d56`/`5fd18c9` 修复。 🔴 **处置＝如实登记一次回归，不在本次授权触碰区内修**：本行 2026-08-27 `OP-0827-D` 已判定并恢复过一次（12→0，写 `commondir` 内容 `../..`，幂等可逆）；**本次实测又变回 11 个 broken**（`conftest-pytest-regression-c9d07e`／`followup-dispatch-apply-25679f`／`gallant-lumiere-b482be`／`observation-window-archive-91b68d`／`queue-313-prerequisites-7d9565`／`queue-315-apply-9f2c1a`／`queue-lint-bare-pipe-312efa`／`queue-table-reachability-2`／`sweep-deadlock-transition-tasks-ba394b`，与原 12 个高度重叠且新增 1 个未见于原名单，原 `fi2-webhook-var-check-bc19fa`／`platform-bootstrap-apply`／`musing-pascal-68d14e`／`recursing-hofstadter-eb7215` 四个当前不在此态，后两者仍是 `(detached HEAD)` 但 HEAD 有效、非 broken），说明 `commondir` 缺失这一根因**会复发**、尚未根治（可能与某些 worktree 创建/清理流程的副作用有关，本次未深挖成因）。本次授权触碰区仅 `delivery.py`／`push_followup_letter.py`／`test_delivery.py`／`工具-落库sweep.py`，**不含 worktree 管理目录修复**，故不顺手处理，留交下一次值周巡检或专项处置（修法同 2026-08-27 先例）。 　━━━　🔬 **2026-09-02 `OP-0902-X`（Cowork 业务总线）复测：第三种形态 ＋ 一笔从未被任何判定覆盖的游离提交 ＋ 三次判据失效实录** ━━━ **⑴ 形态又变，比前两轮都重**：本轮 4 个 worktree 的管理目录里**只剩 `logs`／`refs`／`ORIG_HEAD` 三项**，`HEAD`／`index`／`gitdir`／`commondir` **全部不存在**（前两轮记的分别是「缺 `commondir`」与「连 HEAD/index 一起没了」）；`git worktree prune --dry-run` 对它们的判词是 `gitdir file does not exist`。名单：`fi2-webhook-var-check-bc19fa`／`material-board-view-df54d1`／`platform-bootstrap-apply`／`wip-limit-open-pool-fix-c9d2ca` —— ⚠️ **后两个从未出现在本行 2026-08-27 那 12 个名单里**。 ━━━ 🔴 **⑵ 最要紧的一条**：`wip-limit-open-pool-fix-c9d2ca` 的 `ORIG_HEAD` 吊着 `510fdaf`（`feat(editlock): 咽喉六修同车（§一#351 ⑴～⑹）`），**实测不在 master 内**；因该 worktree 从未进过 08-27 名单，`OP-0827-D` 那轮「两笔游离提交逐笔比对、无一可丢」的判定**从未覆盖它** —— 前两笔当时比对过，**这一笔没有任何人看过**。 ━━━ ✅ **⑶ 已按 Shao Peishen 2026-09-02 拍板（U-3 ＝ (a) 先救后清）当场钉住**：三笔各打 tag 并 `push origin --tags` 成功 —— `salvage/f2a9920-carrier-health-guard`／`salvage/5499e89-ensure-paths`／`salvage/510fdaf-editlock-six-fixes`，**此后 `gc`／`prune` 删不掉它们**；tag 属纯增量、随时可删，不影响任何分支。第四个 `material-board-view-df54d1` 的 `e003483` 实测已在 master 内，无须救。 ━━━ 🔴 **⑷ 判据失效实录 —— 一次会话内连撞三次，三次全部返回「正常」结果**：① `Test-Path` 查 worktree 管理目录判孤儿 —— 目录在、里面缺文件，**4 个漏报 3 个**，是 `git worktree prune --dry-run` 纠正的；② `git -C <worktree> status` 报 `fatal: not a git repository` —— **文案误导**，管理目录其实在，真因是 ⑴ 那类缺件；③ 🔴 **`git rev-list --count master..claude/<分支>` ＝ 0 曾被本线用来判「可安全 prune」，而游离提交不挂在任何分支上、这条命令根本不会去看它** —— **若据此执行 prune，三笔会永久消失，其中一笔从未被人判过。** ⇒ 与根 `CLAUDE.md` §5「工具静默回退」同族；**三次的共同形状是：命令返回了一个漂亮的数字，而它数的是另一个东西。** ━━━ 📌 **⑸ 本轮仍不做物理清理**：本行 ⑸ 原三条理由依旧成立，且现多一条 —— **根因会复发**（08-27 修到 12→0、09-01 回到 11 个、09-02 又出 4 个新形态），**清理治不了复发，先救后清才是顺序**。
