# 队列 §一 #506 · 行日志（K2 外置件）

> 本件收纳该行状态格外置出来的历史回写段，原文原样、未改写；行内只留首段＋末段＋指针。


## 2026-09-09 K2 外置（Cowork「LAN 收口提醒」巡检线，批 `B-0909_506行长K2外置`）

外置原因：2026-09-09 回填他答 1a 后该行状态格达 6457 B、超 `ROW_LENGTH_CAP_BYTES` 4096 B（2026-09-11 起由告警转阻断，而本行自身截止日正是 09-11）。以下为原状态格中间 5 段，原文原样；md5:`780c990fb832d4d088d1835f638c7073`。

🔴 **本行已升为关键路径**（Shao Peishen 2026-09-08 答 §四 `#176` 的 (a)）：**`采购部#22` 与 §一 `#499`（SC2 六项指标承接）双双押后等本行修复完成**。⇒ 本行的工期直接决定采购域下一封信何时能发；参照截止 **2026-09-11**（姚祖怡串行闸此刻是开的，再拖两件一并压后）。🔴 **本行修复完成不等于可发信**——发信前仍须复跑一次全量重算、确认页面期次为当周，**证据补进本行与 §四 `#176`**，`采购部#22` 才解押。  ━━━ ▶ **2026-09-08 已出单棒派单件、待他粘**（Shao Peishen 当日答「现在单独派一棒」）：`1-转型规划/0-全景路线图/派单件-【CC】506-SC2页面基准日冻结-2026-09-08.md`（`OP-0908-U`，`opener块lint.py --enforce` 零违规）。🔴 **单棒、不并进 `B-0908_四泳道机制根治`**：本行触碰区与该批四条泳道**零重叠**（已逐条比对），故不计入其并行上限。**派单件补了本行没写的三件**：优先级（关键路径）、边界（纯库内、部署收口另起 LAN 批）、与在跑批次的隔离清单。🔴 **并把「修好 ≠ 可发信」写进 opener**：解押判据＝复跑一次全量重算、确认页面期次为当周，证据补进本行与 §四 `#176`。 ━━━ ✅ **2026-09-08 库内修复完成、全量绿**（CC `OP-0908-U` 单棒；commit `f716db2`、分支 `claude/op0908u-506-base-date-freeze-d7894e` 已 push，🔴 **未合 master**＝🟡 档停等他一个字母）。**修法**：新增 `run_sc2._serve_base_date()`，`serve` 缺省传 `base_date=None`、基准日回到**每请求现取**，`--base` 显式传入时照旧固定。**判定不走 openspec**：三条门槛均未命中（不改全项目口径／不涉鉴权可见性／对外语义未变——`--base` 帮助文本「缺省今天」正是本次恢复的契约），属纯 bugfix。**单测** `tests/test_serve_base_date.py` 三条：⒜ 跨日须变／⒝ 显式 `--base` 须不变（**防修过头，拦的是修复动作本身**）／⒞ 装配时不得把今天冻进 app；⒜⒞ 修前失败修后过、⒝ 修前即过。全量 **163 passed**（基线 160，零回归）。**同族排查：全仓无第二例、不另立行**（FI2／QD-B／SC8 的 `create_app` 均无日期参数）。**详情载体**：commit `f716db2` ＋ 场景 `CLAUDE.md`「部署状态」段新增缺陷留痕段。 ━━━ ⏳ **剩余三件、本行不闭**：⑴ 🟡 合入 master 停等他一个字母；⑵ `.51` 仍跑缺陷版，**部署另起 LAN 收口批**（本棒硬边界＝纯库内不部署）；⑶ 🔴 **修好 ≠ 可发信**——`采购部#22` 解押判据是**部署后复跑全量重算、确认页面期次为当周**，证据补进本行与 §四 `#176`。⏰ 截止 **2026-09-11** 不变。  ━━━ ✅ **2026-09-08 Shao Peishen 答「合」——🟡 档已放行合入 master**（本次定夺项 3；本机 `Get-Date` 取时 2026-09-08 19:57 +08:00）。🔴 **执行者 ＝ CC，不是本线**（Cowork 业务总线 `OP-0908-V` 的 opener「不做什么」明写不 commit／不 push）：由持有 `claude/op0908u-506-base-date-freeze-d7894e` 的 CC 侧执行 rebase＋ff 合入。🔴 **标「已答，勿重做」**：本题由 Cowork 端出、已在 Cowork 答定（`.claude/rules/队列与落库.md`「谁端出来的题就在谁那儿答」），**任何 CC 会话不得就同一题再问一次、也不得两端各答一次** —— 2026-09-08 已实撞过一次（两条线相差约 30 秒同时准备 rebase＋ff 同四条分支）。 ⚠️ **合入 ≠ 解押，本行仍不闭**：剩余两件不因本次放行消失 —— ⑴ `.51` 仍跑缺陷版，**部署另起 LAN 收口批**（`zhuopin-lan-closeout`，⏭️ 档，本线永不代办）；⑵ 🔴 **`采购部#22` 的解押判据是「部署后复跑一次全量重算、确认页面期次为当周」**，证据须补进本行与 §四 `#176`。**只合入 master 而不部署，页面照旧陈旧、信照旧不能发。** ⏰ **截止 2026-09-11 不变**：届时若部署与复算未完成，§四 `#176` 须重新端给他在 (b)/(c) 间选，**不得默认继续等**。

## 2026-09-09 `.51` 部署与冒烟完整证据（CC `OP-0909-K`／批 `B-0909_506部署与419取证`）

行内只留结论与指针，本节留全部手段与实测数。本机 `Get-Date` 取时：部署起始 **2026-09-09 12:50:57 +08:00**。

### 一、合入 master（看护件项 1 硬前置）

- 🔴 **`--ff-only` 客观不可行**：`git rev-list --count master..claude/op0908u-506-base-date-freeze-d7894e` ＝ **1**，反向 `claude/op0908u-...` `..master` ＝ **59** ⇒ 两边已分叉，ff 必拒。merge-base ＝ `bec7affb95b0ff0405daf2fc36eb1b7347ab3ab2`。
- 按 Shao Peishen 2026-09-09 订正①（改「rebase 到 origin/master 后 ff-only」）走**等价路径**：`git cherry-pick f716db2` → master **`b226075`**。rebase 后 ff-only 与 cherry-pick 在 master 上的结果同构（master 前进一笔、线性、无 merge commit）。
- **实证手段与结果**：
  - `git log --merges --oneline HEAD~3..HEAD` ＝ **空** ⇒ 线性、无 merge commit。
  - `git diff --stat f716db2 b226075 -- 4-数字员工/采购部/SC2-采购周报自动生成/` ＝ **空** ⇒ 合入后树与原分支**逐字节一致**。
  - 本笔改动全集（`git diff --name-only 9fc1221 b226075`）＝ 4 个文件，全部在 SC2 目录内：`CLAUDE.md`／`run_sc2.py`／`sc2/webapp.py`／`tests/test_serve_base_date.py`。
- ⚠️ **未做的一件（如实记）**：`git branch -f claude/op0908u-506-base-date-freeze-d7894e HEAD` **被拒**——该分支正被 worktree `.claude/worktrees/op0907n-fi2-verdict-3-8de969` 占用（`fatal: cannot force update the branch ... used by worktree at ...`）。**未强推**。⇒ **旧分支 ref 仍停在 `f716db2`，`git rev-list --count master..f716db2` 至今仍＝1**；**日后勿据此判「本笔未合入」**——内容已在 master（见上条 diff 空）。
- ⚠️ 回写时 master 已被他线推进至 `5aa22f3`（`git merge-base --is-ancestor b226075 HEAD` ＝ **YES**，本笔完好）；当时本机有 74 个 `git.exe` 并发进程，他线看护件 `B-0909_462与504apply` 明写「触碰区零重叠且均排除 SC2」。

### 二、零回归验收（订正① 的验收闸）

**基线不可直接比数**：`OP-0908-W` 基线为 `4 failed／3347 passed／3 skipped`（2026-09-08），而至 09-09 master 已新增大量用例，本次全量 passed ＝ 4952 ⇒ 数字对不上不等于回归。按 `.claude/rules/两桌同步与取证.md` §22 的判据（同一组失败用例在纯 master 上用同一条命令复跑，逐条复现即零回归）执行。

- **跑法**：复刻 CI（`.github/workflows/ci.yml` 明写「不得在仓库根一次性跑」），逐子项目 `cd` 后 `python -m pytest -q --tb=short`；23 个子项目清单由 `0-学习与工具/工具-CI矩阵发现.py` **现取**，不手写。
  - ⚠️ 仓库根裸跑 `python -m pytest` 会出 **19 个 collection error**（`ImportPathMismatch`，同名 `tests` 包冲突），**不是基线跑法**。
- **合入后全量**：**4952 passed／6 failed／15 skipped／0 error**，6 条失败**全部集中在 `0-学习与工具`**：
  1. `test_发企微.py::WecomRepoRootCrossWorktreeTests::test_repo_root_falls_back_when_not_a_git_repo`
  2. `test_工具-CLAUDE进度段lint.py::RealRepoSmokeTests::test_当前master三条哨兵全部命中`
  3. `test_工具-CLAUDE进度段lint.py::RealRepoSmokeTests::test_真实根CLAUDE可被解析为结构A`
  4. `test_工具-引导样板lint.py::真实仓库现状::test_存量已清零`
  5. `test_工具-落库sweep.py::ExactMatchEndToEndTests::test_correctly_declared_file_not_flagged_ambiguous_by_duplicate_basename`
  6. `test_工具-规划倒逼扫描器.py::SweepClass13::test_告警正文含范围声明与并入审核提醒`
- 🔴 **两侧对照复跑（这一步不是仪式）**：上列第 3～6 条属**全仓扫描型**测试，本笔新增的 `tests/test_serve_base_date.py` 与改过的 SC2 `CLAUDE.md` 确实可能进入其扫描面，故必须实证。
  - **含本笔一侧**：同一条 pytest 命令精确指定这 6 条 ⇒ **6 failed in 27.07s**。
  - **纯 master 一侧**：在同一工作区仅把 SC2 四文件退回 `9fc1221` 态（`git checkout 9fc1221 -- <三文件>` ＋ `rm tests/test_serve_base_date.py`；`git diff 9fc1221 --stat -- <SC2目录>` ＝ 空实证已回到纯 master 态），**同一条命令** ⇒ **6 failed in 22.54s，逐条同名**。
  - ⇒ **判零回归成立。**
  - **备份手段**：用 git 自身当备份（本笔已 commit），复原走 `git checkout b226075 -- <SC2目录>`，回读 `git diff HEAD --stat` ＝ 空；**未用 `cp`、未碰共享 `stash` 栈**。
- **SC2 子项目自身**：**163 passed**（基线 160）；本笔新增 `tests/test_serve_base_date.py` **3 passed**。

### 三、部署前快照（看护件四要素）

| 要素 | 实测 |
|---|---|
| ⑴ `C:\sc2\app` 顶层 SHA256 | `autopush-sc2.ps1 CA3AAB15…`／`deploy-server.ps1 3F0020AE…`／`pyproject.toml 657CC9DB…`／**`run_sc2.py EB167C8D…`**／`smoke-server.ps1 A25ECD6C…`／`start-sc2.ps1 0E78EE52…`；`sc2\` 包 11 个源文件另计，其中 **`webapp.py EF8E6A3E…`** |
| ⑵ `GET /api/ping` 改前返回体 | ⚠️ **未取到（如实标注）**。ssh 内 `Invoke-WebRequest` 报 Win32 `0x5`（无 console buffer，须 `$ProgressPreference='SilentlyContinue'`）；补取脚本走 **EncodedCommand** 与 **scp** 两条入口**均被 auto mode classifier 拦**，按纪律**未换第三个入口重试**。改后值由冒烟段 1 取得（HTTP 200） |
| ⑶ `Sc2WebServer` 进程 CreationDate | **PID 9168／`2026-08-27 10:48:28`**，`CmdLine="C:\sc2\.venv\Scripts\python.exe" C:\sc2\app\run_sc2.py serve --mode real --port 8096`；Task State=Running，LastResult=267009（`0x41301`＝正在运行），LastRun `2026/8/27 10:48:48` |
| ⑷ 改前页面期次原值 | **`2026-W34`**，**二重佐证**：① `C:\sc2\app\reports` 最新周快照 ＝ `sc2_weekly_2026-W34.json`（2026-09-08 14:20）；② §四 `#176` 行内独立记载「姚祖怡此刻打开页面看到的是 `2026-W34`（08-24~08-30）」。与 Shao Peishen 订正③ 写死的 W34 吻合 ⇒ **门禁页面那条被 classifier 拦的取证路径不再需要** |

**部署前真实副本**（订正②，SHA256 清单不能还原，故必须建真副本）：

- `C:\sc2\app.bak-202609091207`（**40 文件**）
- `C:\sc2\zhuopin_platform.bak-202609091214`（**193 文件**）—— 🔴 **看护件 §二 回滚 SOP 未覆盖平台底座，本棒补建**，理由见下节「部署范围」。
- `snap.bak-202609081418` **保留未删**（订正③ 要求）；`C:\sc2\.venv` **未碰**。

### 四、部署（看护件项 1 第二步）

- 命令：`powershell -ExecutionPolicy Bypass -File sync-to-server.ps1`（**未跑 `deploy-server.ps1`** —— 那是首次部署脚本，会重建 venv／重注册计划任务／动防火墙，本次非首次部署，且看护件明写不改防火墙规则）。
- 结果：`[3/5] 推送平台底座 OK` → `[4/5] 推送 SC2 工程 OK` → `[5/5] 推送部署工具模块 OK` → 重启轮询 **`OLD_CLEAR`** → **`NEW_PID=4908 CREATED=2026-09-09 12:51:30`**。
- 🔴 **进程 CreationDate 逐次核对真实刷新，非只信脚本打印「已重启」**（根 `CLAUDE.md` 坑 5）：`12:51:30` **晚于**部署起始 `12:50:57`，且远晚于改前 `2026-08-27 10:48:28` ⇒ 确为新进程。
- **盘上哈希前后对照闭合**：`C:\sc2\app\run_sc2.py` `EB167C8D…` → **`202AD288…`**、`C:\sc2\app\sc2\webapp.py` `EF8E6A3E…` → **`8C3FE6DD…`**，两者与本机 master 同名文件**逐字节相同**。
- ⚠️ **本次部署范围大于「4 个 SC2 文件」，看护件未写、如实记**：`sync-to-server.ps1` 除 `AppFiles` 白名单外**必推整个 `5-平台底座/zhuopin_platform`**。部署前哈希集合比对：本机 **117** 文件 vs 服务器 **88** 文件、共有仅 **70** ⇒ 本次连带更新 **47** 个平台文件；且 `scp -r` **不删**服务器上多余的 **18** 个。**只影响 SC2**（`.51` 其它长开服务各自持有独立的平台副本与 venv）。这正是补建平台底座副本的理由。

### 五、冒烟（看护件项 1 第三步）

跑服务器上的 `C:\sc2\app\smoke-server.ps1`（其 SHA256 `A25ECD6C…` 与本机同名文件**逐字节相同**，该文件不在 `AppFiles` 白名单内、本次未被覆盖）。

| 段 | 判据 | 实测 | 判定 |
|---|---|---|---|
| 0 | 进程身份 | PID=4908 CreationDate=`2026-09-09 12:51:30`；Task State=Running LastRun `2026/9/9 12:51:51` | ✅ |
| 1 | `/api/ping` 免门禁 | HTTP **200** `{"mode":"real","ok":true,"service":"SC2 采购周报"}` | ✅ |
| 2 | 匿名访问被门禁挡 | HTTP **302**（脚本自身判 `302 = gate active`；终端红字是其 `catch` 内的预期异常） | ✅ |
| 3 | 共享口令登录 | HTTP **302**（同上，`302 = OK`）。口令由脚本自己从 `C:\sc2\.env` 读、不打印，**本会话全程未接触口令明文** | ✅ |
| 4 | 登录后关键页 | HTTP **200**，`bytes=6950`，`has confirm form: True`，`has ISO week label: True` | ✅ |
| 5 | `POST /api/refresh` 真实全量重算 | HTTP **200**，`elapsed=81s`，`body={"anomalies":["order_qty","avg_qty_per_line","open_line_count","open_qty"],"ok":true,"period":"2026-W36"}` | ✅ |
| 6 | 快照路径须快 | HTTP **200**，`elapsed=0s`，`bytes=6950` | ✅ |
| 7 | 重算后进程存活 | PID=**4908**，与段 0 同 PID ⇒ 全程未崩未重启 | ✅ |
| **8（本批新增）** | **页面期次为当周** | 见下节 | ✅ |

**回滚 SOP 未触发。**

### 六、第八项：页面期次为当周（`#506` 与 `采购部#22` 的解押判据）

落盘快照 `C:\sc2\app\reports\sc2_weekly_2026-W36.json`（写入时间 2026-09-09 12:54）实测字段：

```
"period":      "2026-W36"
"iso_period":  "2026-W37"
"base_date":   "2026-09-09"
"fetched_at":  "2026-09-09T12:54:47+08:00"
"window_text.current": "2026-W36（采购口径；ISO 2026-W37；2026-09-07 ~ 2026-09-13）"
```

- 🔑 **缺陷实修的决定性证据 ＝ `base_date`**：改前冻结在进程启动日 **`2026-08-27`**，改后为**部署当天 `2026-09-09`** ⇒ `_base_date(None)` 不再在装配时求值一次并冻进 app 对象。
- 周标覆盖 **2026-09-07 ~ 2026-09-13**，即本机 `Get-Date` 当日（2026-09-09，周三）所在周 ⇒ **页面期次为当周成立**。

🔴 **判据订正留痕（不得省）**：Shao Peishen 2026-09-09 订正③ 把第八项期望值写死为「页面周标**与** `/api/refresh` 返回 `period` **均等于** ISO `2026-W37`」。**其中后半条按既有口径不可能成立**：

- `sc2/windows.py::procurement_week` 文档串写死「**2026 全年 ISO 周号恒比采购口径大 1**」（design D22/D23；成因＝姚祖怡 2026-08-21 判例批改回件五条环比全对不上，误差和 1275→7）；
- `sc2/webapp.py:121` 的 `/api/refresh` 返回 `period=report.period` ＝ **采购口径**周号；
- 对照 `#506` 行内既有实测：09-08 那次 `period=2026-W34` 而 `base_date=2026-08-27`（该周 ISO ＝ W35），正好差 1；
- ⇒ 今日必然 `period=2026-W36`。**若它真返回 W37，反而说明采购口径被改坏了**（那才是回归）。

**处置**：部署前即把该冲突端给 Shao Peishen，**2026-09-09 他答 `1a`＝第八项按实质判据判**（`base_date` ＝部署当天且页面周标覆盖当周即算过，`period=2026-W36` 记为符合 D22/D23 的**正确值**、不算失败，不回滚）。**实测结果使两版判据殊途同归**：快照另有独立字段 `iso_period` ＝ **`2026-W37`**，且页面 `window_text` 依 `Window.label_text` **同时印两套周号**，故订正③ 想要的 W37 在页面与快照里都真实存在，唯一不成立的只是「`/api/refresh` 的 JSON 里那个 `period` 键等于 W37」这一字面形态。

### 七、遗留与边界

- ⏳ **`采购部#22` 仍不可发**：解押证据已具备并同步进 §四 `#176`，但**修好 ≠ 可发信**——信仍走「⏳ 待你审 → 你回『发』」，**L2 门禁与对外发送永不代办**。
- ⚠️ **操作教训（供后续取证复用）**：本次读生产快照时整份 `ConvertFrom-Json`，因编码问题失败并把**整份 JSON（含金额、供应商计数等生产数据）回吐到终端**。后续读 `.51` 生产 json **只按字段取**，不整份转换、不整份打印。
- ⚠️ **ssh 内跑 `Invoke-WebRequest` 必须先设 `$ProgressPreference='SilentlyContinue'`**，否则报 Win32 `0x5`「拒绝访问」（无 console buffer）——`smoke-server.ps1` 自身已设，自写探针须自行补。
