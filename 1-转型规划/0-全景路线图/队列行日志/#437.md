---
title: "队列行日志 #437（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #437 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §一 #437 · 外置前整格原文（2026-09-04 外置，7 段，md5:5ae4dc8d）

[S:done][D:机] ✅ **已完成（2026-08-30，CC `OP-0830-G`）** —— 原阻塞：本行改 `0-学习与工具/工具-共享文档编辑锁.py`，而 `#435` 子项 E 正在改同一文件。**2026-08-30 08:26 git 实测**：分支 `claude/op0830c-global-memory-inspect-f68c79` 相对 master **0 提交**，worktree（`.claude/worktrees/lan-redo-three-groups-11e8dc`）有 5 个脏文件、其中就有 `工具-共享文档编辑锁.py` 与其测试 ⇒ **CC 正改着、一个字未落盘**，此时追加范围必撞车。**⇒ 本行不并入 `#435`**（Shao Peishen 2026-08-30 原本倾向并入，取证后本线改判并说明；他未再指定，按会话末默认项 (a) 执行立行）。`#435` 一归档即摘 🛑 转 `[S:open]`。 　━━━　🔎 **前提已变，如实更新（2026-08-30，本线取证）**：`#435` 子项 E 对 `工具-共享文档编辑锁.py` 的改动**已入 master**（`1d3c618`，+106 行）⇒ 本行等的那件事**主体已完成**，只剩 `#435` 组 4 收口（值周清单核／批次／回写／archive）。🔴 **仍维持 `[S:blocked]` 不摘 🛑**：`#435` 未归档，组 4 仍可能回头动同一文件；且「完工即归档」纪律要求归档才算完工。**`#435` 一 archive 即摘 🛑 转 `[S:open]`。** 　━━━　✅ **前提已满足，🛑 已摘、转待领（2026-08-30）**：`#435` 已于本日完成并**归档**（`openspec/changes/archive/2026-08-30-global-memory-inspection-and-root-ratchet`，commit `bdc446a`），其对 `工具-共享文档编辑锁.py` 的改动已入 master ⇒ **触碰区物理重叠已解除**，本行不再有硬阻塞、**可领**。 　━━━　📦 **openspec 变更包已出件（2026-08-30，`OP-0829-W`，他当日说「现在就做」）—— 🛑 待他 design 审**：`openspec/changes/editlock-release-opener-guard/`（proposal＋design＋specs＋tasks 四件齐，`validate --strict` 已过 EXITCODE=0）。**做什么**：`release` 增一道结构检查——本次持锁期间触碰的 `.md` 里若含 opener 块，按块的 `【设置】执行环境` 分流：**CC 块**须有 `set_session_title` 且含子任务例外句，不过即拒绝（fail-closed）；**Cowork 块不校验该项**；**未声明环境不校验**。逃生阀＝`opener豁免：<理由>`。🔑 **D1 的判据（本包立身之本）**：最省事的修法是给 lint 加 `--include-untracked`，**否掉**——lint 是事后扫描器，**没人主动跑它就拦不住任何东西**；2026-08-30 那 18 次违反**没有一次是被 lint 报出来的**，第 18 次是他在会话列表里肉眼看出 session 名丢编号才发现。而 `release` 是派出前必经（本役当天它 fail-closed 拦下本线两次、两次都拦对）。⇒ **判一个守卫有没有用，只问一句：不知道这条纪律的人，会不会去调用它？**（同 `OP-0829-E` 在 `.claude/rules/` 那件事上立过的判据，本包是第二次应用）🔴 **D2 覆盖边界如实声明，不假装全覆盖**：本守卫**只覆盖走了队列登记流程的 opener**；写完直接粘出去、从不 acquire/release 的会话它看不到。**故回显措辞须是「已校验本次触碰的 N 个 .md」，不得写成「opener 已全部合规」**——理由是 `#381 ⒞`「制造已被机器守住的错觉比没有门更危险」。**⇒ `#284` 因此不能销号**，只能改写为「形态① 已由咽喉守住（限已登记路径）」。**D3 复用 lint 判据不重写**（重写＝两处分叉，正是 `#312` 今天付过学费的形态）；**D4 必须按环境分流**（`set_session_title` 在 Cowork 侧不存在，2026-08-27 实测；不分流会把本线自己每次 release 都拦死）；**D5 不加 `--force` 开关**（opener 漏 title 无正当紧急场景，加开关只会让豁免变廉价）。**3.1 是唯一的硬验收**：用 2026-08-30 那个真实漏写的 opener 历史版本（`c7009f4` 之前那版）跑，**必须被拦下**——拦不下就是本包没解决那 18 次里的任何一次。 　━━━　✅ **design 审已过（Shao Peishen 2026-08-30 答「审过」，D1-D5 整体通过、无点名修改）＋ 已派出（`OP-0830-G`）**：派单件 `1-转型规划/0-全景路线图/派单件-【CC】opener守卫挂release咽喉-队列437-2026-08-30.md`（含 opener，**本 opener 自身已带 `set_session_title` ＋ 子任务例外句**——出一份关于 opener 守卫的派单件却漏写第 3 行会很难看），分支 `claude/op0830g-release-opener-guard`，新建 worktree。tasks 0.1 已 [x]。**派出时实测**：本行仍 `[S:open]` 未被他人认领。⏳ 待 CC 认领后自行改写本行为在办。🔴 **收口时务必守住 D2**：`#284` **不得销号**，只能改为「形态① 已由咽喉守住（**限已登记路径**）」——本守卫看不到「写完直接粘出去、从不 acquire/release」的会话，**未登记路径仍是人守**。 　━━━　⚠️ **并行提示（2026-08-30）**：与 §一 `#438`（`OP-0830-F`，执行体对齐）**并行开工，触碰区实测零重叠**。🔴 **本行 push 会让 `#438` 的「落后 master ＝ 0」变成非零** —— 那是正常的，`#438` 的判据只在其对齐那一刻成立，已在该行内写明，本行无需为此等待或协调。　━━━　✅ **实现＋验活已完成（2026-08-30，CC `OP-0830-G`）**：`工具-共享文档编辑锁.py::release` 新增 `_opener_guard_violations`（旁挂 ⑹，同一处 fail-closed），判据逐字复用 `工具-opener块lint.py::check_block`（不重写，D3）；按 `【设置】执行环境` 分流（D4）；回显「已校验本次触碰的 N 个 `.md`，其中含 opener 块 M 个」（D2，如实声明覆盖边界，不写「已全部合规」）；逃生阀 `opener豁免：<理由>`，不设 `--force`（D5）。🔴 **实现途中补了一处 design 未点名的坑**：首版把「不在 git 工作树内」与「在工作树内但 `git status` 失败」两种取不到数据的情形混为一谈、一律 fail-closed，导致既有 39 条走纯 tempdir（无 `git init`）夹具的既有单测集体误伤（`DualFileRoutingTests`／`FollowupReplyStateSyncTests`／`HoldConsistencyValidationTests` 等）；补 `_is_inside_git_work_tree` 前置判断（与 ⑹ 同一判据方向）后全绿，可复现见 git 历史。**单测**：新增 `OpenerGuardReleaseTests` 13 条（覆盖 tasks 2.1-2.7 逐条 ＋ 非 `.md` 排除 ＋ fail-closed／适用前提两态）；全量回归 306 passed（含既有 293 ＋ 新增 13），唯一未过的 `GenderPronounLintTests::test_roster_stays_in_sync_with_root_claude_md` 与本包无关（CLAUDE.md §1 抽取正则失效，前置状态即已如此，不在本包触碰区）。**3.1 真阳性（真实历史件，非 mock）**：用 `派单件-【CC】全局记忆巡检与root棘轮-队列435-2026-08-29.md` 在 `a837b73`（真正缺 `set_session_title` 的首版，`grep -c set_session_title` ＝0）跑 `_opener_guard_violations`，命中 1 处 F1，**正确拦下**；同文件 `1323862`（补写后的版本）跑同一函数，0 违规。**3.2 真阴性**：对 4 份当前真实合规派单件（本行自身 `#437` 派单件、`#312`、`#438`、`#435` 修复版）跑，opener 块 4 个，**0 误报**。**3.3**：本次收工 `release` 本身即由已生效的本守卫校验通过，不误伤自身。🔴 **D2 覆盖边界照旧**：`#284` **不销号**，回写见该行。**worktree 落地说明**：`工具-共享文档编辑锁.py`／其测试／模板库指针／归档后的 openspec 四件／本行自身派单件 走分支 `claude/op0830g-release-opener-guard-a9e7af`（worktree 隔离，REPO_ROOT 解析恒指主 checkout，代码改动天然落 worktree 自己的 git 历史，故本次 §二 批次只登记队列文件自身；代码侧提交见该分支 push 记录）。
