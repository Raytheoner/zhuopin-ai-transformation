---
title: "队列行日志 · §一 #500 状态列历史回写段"
created: 2026-09-08
status: 生效
用途: K2 行长口径外置件（行内留首段＋末段＋指针）；外置前整格 4,653 B > 4,096 上限
外置会话: Cowork 业务总线 OP-0908-A
中间段 md5: 7fa27b4e69b7ecc6fb6e0edee2c83150
---

# 队列行日志 · §一 `#500` 状态列历史回写段（原文原样）

> 中间 3 段，md5 `7fa27b4e69b7ecc6fb6e0edee2c83150`；行内只留首段＋末段＋指针。

🆕 **2026-09-07 立行（看护批 `B-0907_R` 收工，OP-0907-R 登记；Shao Peishen 当日答 3(a) 放行改判）**。 🔑 **成因＝一条比 git 本身更严的自造判据，把机制自己卡死了**：`#436`⑵ 实测，常驻执行体 `ops/wecom-service-home` 的 ff 自 **2026-08-27 17:17 UTC** 起连续 **249 轮**停手（49 轮 `ahead 1` ＋ 200 轮 `dirty`），落后 master **165** 个提交；唯一卡住它的脏文件是 `5-平台底座/wecom-aibot-service/run-followup-dispatch-check.ps1`，而该文件的差异 **100% 是外观的**（工作副本 1318 B 带 UTF-8 BOM＋15 个 CRLF，committed 1300 B 无 BOM／0 CRLF，规范化后正文逐字节相同；差 18 B＝3 BOM＋15 CR），**且 165 个待入提交里碰它的有 0 个 ⇒ git 自己完全允许这次 ff**。上述三组数字均由 `OP-0907-U` 实测、`OP-0907-R` 看护者独立复核逐条吻合。 ⚠️ **本行只做判据放宽，不做清账**：当下这 165 的清账（清 BOM＋ff＋重启验活）属 ⏭️ 档，已 `transfer-out` 交 `zhuopin-lan-closeout`（`B-0907_Q`）；**单清 BOM 只解这一次，判据不改则下次重跑 `register-followup-dispatch-task.ps1` 必复发**（BOM 正是该脚本用 PowerShell 写出时带的）。 📌 **顺带并入本行射程**：`_carrier_lag_counts`（同文件，`#338` 遗留）**从未被任何地方调用**，是当年阈值方案作废后的死代码，领取方可一并清理。 🔴 **不得关掉守卫或加 `--force`**（与 `#493` 同一纪律：守卫拦错人时改判据，不是关守卫）。  ━━━ 📌 **预留豁免：本行编号 500 ＝ 本次 `acquire` 回显高水位线 §一 #499 +1，整个持锁窗口内无第二方写入（可由锁 history 复核）。未走 `--reserve` 的原因＝本会话 acquire 时的计划只含既有行回写与 §四 登记，本行是 Shao Peishen 当场答 3(a) 后才产生的新条目，而同一把锁内无法再 `acquire --reserve`。** ⚠️ 一次性留痕，非常规做法：正确姿势仍是 acquire 时按需 `--reserve`（同 `#493` 立行时的同类留痕）。  ━━━ 🔴 **WIP豁免：本行挂 🛑 后已不计入机制类可动 WIP（实测 24→23），阻断 release 的 23／22 那一条超额是既存状态、与本行无关**——看护件 `B-0907_R` §〇 已载明「机制类 WIP 已超，值周 §四 `#142` 追认」，即该超额有主、有承接。豁免动用的**不是「立行紧急」**（本行恰恰不紧急、已挂 🛑），而是：一把持有 8 项合法回写（`#436`／`#466`／`#474`／`#493`／`#358` 回写＋§四 `#169`＋§二 两批次）的锁，不应被一条既存且已追认的超额扣住；而消解该超额须判定他线在办行是否已完成，属 🟡「关闭他人在办队列行」，**看护会话不得代拍**。 📌 已做的减法：同批关闭 §一 `#493`（其自身判据已满足），使计数由 25 降至 23。**遗留给值周／总线**：机制类 WIP 仍 23／22，需关 1 条方回上限内。


## 2026-09-08 K2 外置（CC 看护者 `OP-0908-T`，批 `B-0908_四泳道机制根治`）

外置原因：本批泳道回写段并入后该行状态格将超 `ROW_LENGTH_CAP_BYTES` 4096 B。以下为原状态格中间段，原文原样、未改写；md5:`d21310917816c88eb5dcb2c39a296ddf`。

✅ **2026-09-08 解冻**（Cowork 业务总线 `OP-0908-A`）：解冻条件「可动 WIP 回到 22 以内**且摘 🛑 后仍 ≤22**」**已满足** —— Shao Peishen 当日答 3a 授权后，`#358`／`#436`／`#447`／`#454`／`#478` 五条据实由 `open`／`partial` 改判 `blocked`，**机制类可动 WIP 由 22/22 降至 17**（手段＝用 `工具-共享文档编辑锁.py` 自身的 `_section_bounds` ＋ `_count_mechanism_wip` 对两份队列真身实测：机制环境 17／业务场景 0，降级项 0，**非会话自算**）；本行与另一行同批摘 🛑 后为 19 ≤ 22。🔴 **未关闭任何既有机制行、未自授 `--force-mechanism-wip`**——腾余量靠的是把失真的记账改回真身，不是牺牲别的行。⇒ **本行可开工**；看护批 `B-0908_B` 泳道 `500-sweep-carrier-dirty` 的硬序前置至此解除。
