---
title: "队列行日志 #462（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #462 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §一 #462 · 外置前整格原文（2026-09-04 外置，4 段，md5:0bf41b73）

[S:open][D:机]🛑 **排队中·暂非可动（2026-09-02 立行时机制类可动 WIP 实测 23／22 超限，release ⑨ 阻断当场生效）** —— 🔴 **本行未走 `WIP豁免` 逃生阀，因为它本就不该是可动行**：状态即「待领、须先起草 proposal、design 审过前不得开工」，标 🛑 是如实标注而非绕过阻断。**解除条件＝可动 WIP 回落至上限内**（同 `#439`／`#447`／`#448` 既例）。🔑 **同 `#439` 那条洞察：立 🛑 反而保护了本行** —— 占位会在 WIP 压力下被催开工，而本行缺的是 design 前置、不是位子。 ⚠️ **本次 release 阻断顺带暴露分诊器一处假阳性（未处置，如实记）**：4 条改判候选里 `#452`／`#454` 属**误命中** —— 分诊器的措辞匹配把「行内引用他人阻塞状态的说明文字」也当成了本行在等他（`#452` 命中的是我写的两 workflow 对照表文字、`#454` 命中的是它并入审核里那句「`#422` 现卡在待 Shao Peishen 人工安装」）。**同族＝ `#460` 日核假阳性**（把「任务正在运行」当失败码）——**都是判据只看字面、不看这句话在说谁**。建议与 `#454` 同批处置。 　━━━　**待领·待定级**（定级由业务总线／值周巡检定，本线不定级——§〇 红线）。🆕 **2026-09-02 立行（Cowork `OP-0902-A`，Shao Peishen 当日问「我好像一直没有看到由计划倒逼的开工任务？」后答 (a)）。** 🔴 **根因一句：「规划 → 队列」之间没有任何机制。** 现有全部机制（可 Open 池／拆件巡逻／值周巡检／泳道看护）**只扫队列 §一**，而队列只装「已经有人立过行」的任务 ⇒ **规划里有、但没人立行的场景，对所有机制完全不可见，且不产生任何信号。** **当日实测（35 个场景逐个三处查，代码见取证件 §三）**：✅ 有工程实体 **10**（SC1／SC2／SC7／SC8／O1／O2／FI1／FI2／QD-A／QD-B）；🟡 队列提及但无工程无包 **11**（O3／O4／Q4／FI3／FI4／FI7／R1-R5）；🔴 **三处皆无、对所有机制不可见 14**（SC4／SC10／SC11／FI5／FI6／FI8／FI9／FI10／**销售 S1-S6 整域 6 个**）。**10+11+14 ＝ 35。** ⚠️ **🟡 一档是宽松估计**：本次「队列行」判据是 §一 全文 grep、**不区分「有承接行」与「被顺带提到」**（R1-R5 极可能只是被讲「整域顺延」的行提到）⇒ **真实不可见数 ≥ 14**，落地须改判「首列编号所在行的任务列是否点名该场景」。 🔑 **这是本项目那个老病的第三个、也是最大的一个实例**：`决策点` 字段无机器守 ⇒ 覆盖率 70%→0%（已治 `#447`）；口径点无台账 ⇒ 承接载体缺失（已治 `#439`）；**规划场景无队列行 ⇒ 永远不会被派工**（本行）。**前两个今天都已立行，第三个一直没人看见它——因为看不见正是它的症状。** 　━━━　🔴 **落地必须写死两条，否则重蹈今天的覆辙**：**⑴ 它必须产出队列行、不能只产出报告** —— 否则又是「只落文档、无承接载体、于是消亡」（`#447` 行内自陈的「第三次」）；**⑵ 排期月份不作为门槛** —— 他原话「**新场景不要管规划时间表，能开尽开**」「**没有前置任务阻塞的任务都可视为可开**」；唯一门槛是前置依赖 ⇒ **销售域 6 个、财务 5 个现在就能进候选，不必等 2027**。 　━━━　📌 **并入审核（协议〇.10）已于取号前完成**：与 `#312`（可 Open 池）**语义互补不可并** —— 那管「**队列内**可开」、本行管「**队列外**未承接」，**两者相加才是真正的「现在能开什么」**（Shao Peishen 2026-09-02 原话）；他当日答 (a) 明定 **独立扫描器、但与 `open_pool_reminder` 共用「三处查」判定函数**（不把两个语义混进同一个池，又不造第二套逻辑）。与 `#454`／`#455`／`#456`／`#459`／`#460` 对象各异，无一可并，故新建。 ⚠️ **命中 openspec 触发门槛 ①「改变全项目口径」**（新增一条「什么算可开工」的跨场景约定）⇒ **须走 openspec 含 design 审**。**⇒ 下一步＝起草 proposal，不直接开工。**


---

# §一 #462 · 2026-09-09 K2 外置第二次（6 段，md5:8c3a073e）

> 由看护者 `OP-0909-N`（批 `B-0909_462与504apply`）外置。回灌 2026-09-09「apply 已合入 master」实情前，行内状态格实测 4615 B，超 K2 的 4 KB 上限，故把中间 6 段整体移出，行内只留首段＋末段＋指针＋本次回灌段。**以下原文原样，一字未删、未改写。**

🔑 **propose 期在真实数据上复跑取证件 §三，坐实其四个判据缺陷；修好判据后同日重跑 ＝ 三处皆无 **13** 个（旧判据只报 2 个，**漏报率 85%**），13 个逐条人工核对为真。四个缺陷与复现手段在该包 `proposal.md`「Why ②／⑸」，本格不复述。** 最要紧的一条：**销售域五场景之所以不报警，唯一原因是 `#463`（那条写着「这 14 个没人接」的行）本身命中了判据——宣布问题存在的那一行，被当成了问题已解决的证据。** ⚠️ **判据尚未落成代码，勿据本格数字直接立行**；`#463` 明写「待 `#462` 把判据收紧后重扫」那 11 个灰区的结果同在该件。

━━━

🔴 **三条须拍板项（design 审前不得 apply）：D5 ＝「产出队列行」的自动化档位，**无默认项、须他一个字母** —— `#462` 硬要求「产出队列行、不只报告」，而机器自动写业务场景行同时撞协议〇.10 并入审核／环境保障线边界（`#463`「机制线只出取证与扫描器」）／他 2026-09-02 的域级暂缓明令；**13 个未承接里 8 个落在研发与销售两个暂缓域，全自动实现会在第一轮就写入他已明令不立的行**（＝「错误继续发生」而非「停在原地」，故不设默认项）；推荐 (b)。**D4** 前置闸两步走（前置总表 emoji 的语义是「这条重要」不是「未就绪」，按它判会得到相反结果；默认 (a) 先行）。**D7** 「与 `open_pool_reminder` 共用三处查判定函数」的落地形式 —— **实测该模块 806 行内无任何三处查逻辑，他答 (a) 时的前提不成立**（默认 (a′)）。三条全文见该包 `design.md`。

━━━

🔴 **起草期自我更正一次**：design D2 初稿定的「任务列点名 ≥3 个场景码即元任务行、一律排除」判据经逐行核对**不成立、已撤回**（会误杀 `#339` FI3 唯一真承接行与 `#467` SC4 场景立行行，把一个假阴性换成四个）；初稿论述连同错在哪一并留在 D2「起草期自我更正」节。🔑 **它与取证件那版是同一种错——「看到一个数对上了就当判据成立」**，故 tasks 把「每条断言都要有反向对照组」写死。

━━━

**退休（措施 B）**：取证件 §三 那段一次性扫描脚本的「可执行判据」身份随本包上线终止，**与上线同批、不得先上线再退**。 **`.gitignore` 覆盖已实测（proposal ⑤）**；**下游追行 2 项**（前置总表机器字段回填／默会判断进知识资产台账 §一.2）见该包 `tasks.md` §4.3、§7。

━━━

📎 **2026-09-07 `OP-0907-J` 按 K2 口径再外置 2 段**：原 🛑「排队中·暂非可动」段与 📌「并入审核」段本次移出本格，**两段原文原样早已在 `队列行日志/#462.md`**（2026-09-04 外置的整格原文 md5:0bf41b73 即含此二段），一字未删、可 grep。🔴 **连带更正**：下方激活段末句「原 🛑 段全文保留于下」写于本次外置之前，**「于下」现改指该外置件**；不追改原句、在此注明所指。

━━━

🟢 **已激活·可领（2026-09-07，Cowork 业务总线 B-0907_E，机制类可动 WIP 现测 11/22 有余量，激活本行不满 22 上限；本次范围＝只到 propose＋design 起草，不 apply）。****原 🛑 段全文保留于下，未删一字。**
