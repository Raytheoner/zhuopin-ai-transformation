---
title: "队列行日志 #108（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #108 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #108 · 外置前整格原文（2026-09-04 外置，5 段，md5:58c21266）

✅ 已拍板 (a)：六个决策点全部通过（Shao Peishen 2026-08-25 答晨复盘 D-4，自述已读 design.md 全文）——包 followup-closure-form-survives-backfill 按包内定案 apply 解锁，执行承接＝§一 #353（已同批追记）。本行销号。━━━ **#353 闭环形态判定被回填抹掉——openspec `followup-closure-form-survives-backfill` 的 design 审：六个决策点须签认（2026-08-24 CC 无头批处理出件，commit `1f303fb`）**：本包按 opener 授权**只做 propose＋design 出件，未 apply、未改任何一行生产码**。🔴 **六项全部无默认项**——提出方是无人在场的无头批处理会话、收工即结束，按根 CLAUDE.md §5「答完之后由谁动手」判据答不出执行者。 ━━━ **⑴ 前置取证推翻了 #353 行自己给的第一个建议（这条比六个决策点更要紧）**：原建议「回填只写状态列约定首段」**做不成**——闭环形态判定**从来就没能待在状态列里**。一封信从起草到发出，状态格被**七处等值比较**钉成「只能是那一个字串」（`gates.assert_finalized`／`readme_table.assert_draft_pending_review`／`dispatch.py:203,213`／`dispatch_followup_letters.py:71`／`工具-共享文档编辑锁.py:1717,1891`），其中 `assert_finalized` 是 `wecom-aibot-service` design.md D8 明文「拍板不得放宽」的**门禁②红线**（该处原文用的是旧称呼，属历史记录不追改，此处引用时按现行称呼纪律略去人名） ⇒ **问题形状是「无处安放」，不是「被覆盖」**。 **⑵ 而它其实早有落脚点，只是机器读不到**：`质量部#7` 的**「主要事项」列**末尾已写着「🔑 闭环形态＝起草时即判定为 `✅ 无需回复`…批准发出后直接记 `✅ 无需回复`、不占串行闸」——**起草人早就写了，写成了自由散文**（实测全表 54 行仅此 1 处）⇒ 本包是把那句话改写到机器认得的形态上，**不是新造位置**；同构先例＝队列 #241 的 `目标文件：` 标注（同列、同手法、不新增列）。 ━━━ **六个决策点（详见 `openspec/changes/followup-closure-form-survives-backfill/design.md`）**：**1** 标注放哪列——(a) 主要事项列内标注【推荐】／(b) 状态列首段化＋放宽含门禁②在内的七处等值／(c) 新增独立列（须迁 54 行）。🔴 选 (b) 则本包 spec 整段作废须重写、且须其亲自签认放宽门禁②。 **2** 回填时是否按闭环形态**直接写闭环态**＝**让机器自动开串行闸**。🔴 **本包刻意不推荐任何一边**：(a) 判错则同一收信人出现两封在途信（串行原则是明令优先于一切触发点规则的那条）；(b) 判对则起草人仍需写 `串行豁免：` 绕闸（而「拿逃生阀去绕它本来要拦的那件事」本项目已明记为坏味道）——**两边都难看，须其本人权衡**。 **3** 回填改「保留式」＋回填时把标注**快照**进状态格【推荐】。它是决策点 5(c) 成立的**前提**；顺带治好 `⏸ 暂缓` 理由同样被抹掉。 **4** 标注取值限定闭环四态枚举＋强制依据、越界 fail-loud【推荐】。 **5** 防「事后追认」靠**结构**（快照冻结）而非新增门禁【推荐 (c)】——(b) 非阻断提示已被本项目实测证否（协议〇.9 ⑵「连续 6 次被无视后信息量已经是零」）；🔴 **配套缓解须一并批**：闸查询在「标注 ≠ 快照」时须明确报出不一致，不做则 (c) 是用一个静默失效换掉一个静默滥用。 **6** 只回改 `质量部#7` 一行、其余 53 行不追改、历史覆盖率如实登记为**永久 0%**【推荐】。 ━━━ 🔴 **签认前不得 apply**；签认后由总线派单进入 tasks §2-§7。 ━━ 🔨 **裁决回填（Shao Peishen 2026-08-24 夜答，2026-08-25 01:08 登记）＝ 六个决策点全部签认**：决策点 1 认可推荐；**决策点 2 ＝ (a) 回填时直接写闭环态，发出即闭环、串行闸当场开**，并一并签认附加护栏「只有标注在批准那一刻已存在时才生效」；决策点 3、4 认可推荐；**决策点 5 ＝ (c) 结构性防止**（此选择使上述护栏零成本成立：闸只读快照，发出后补写标注对闸零效果）；决策点 6 认可推荐。 ⚠️ **A10 登记的边界仍在**：(c) 只让事后追认对闸无效，不阻止它被写下。 ⇒ 「签认前不得 apply」条件已解除，变更包可进入实施；**§一 #353 的 `[S:blocked]` 自此具备解除条件**（由后续 CC 线执行，登记方未执行）。
