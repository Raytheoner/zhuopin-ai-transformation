---
title: "队列行日志 #106（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #106 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #106 · 外置前整格原文（2026-09-04 外置，3 段，md5:7c58e572）

🟡 **请你判一句：`#371` 的修法算不算「改变既有模块对外语义」——即要不要补一个 openspec 变更包（CC 本轮已按「纯 bugfix」判并直接修，如判反可原样转 spec delta、不需返工）**（2026-08-24 CC 立，`OP-0823-E`）：**本轮做了什么** ＝ 给 FI2 税务导出摄取加了一道**发票级幂等闸**，并按发票号重建了 `.51` 上已入库的 `invoice.csv`（3409→3356 行）。**CC 的判定与依据（三条门槛逐条过）**：⒜ **非全项目口径**——只改 FI2 一个摄取通道的去重层，不动任何跨场景约定；⒝ **不涉鉴权与数据可见性**；⒞ **不构成「改变对外语义」**——`write_invoice_csv` 的 docstring 2026-08-07 即自陈「去重责任在『已处理清单』层」，**其意图本就是「同一份数据不该进两次」**，本轮修的是实现没兑现这个意图（同一发票经两份字节不同的文件绕过了该层），故判为**纯 bugfix**，同 2026-08-19 `_OPS` 键名那次的判法。 🔴 **但如实交代张力，不藏**：门槛 ③ 的字面是「同一接口相同输入行为变了」——**本轮确实改变了「同一个 `D:iread` 目录跑出来的行数」**（只是新行数才是对的）。按 §5「**判不准就走**」的保守取向，这一条是可以判成该走变更包的；CC 之所以没走，是因为 **P1 错数字正挂在唐燕萍面板上，而 design 审需要你在场**，走流程等于让错数字继续挂着。 **⇒ 三选一**：**(a) 认同判为 bugfix，不补变更包**（现状，零额外成本）；**(b) 事后补一个变更包**（把本轮改动转为 spec delta 归档，**代码不动、只补文档**，约半天，IATF 可追溯性更稳）；**(c) 认为判错了、且需回退重走**（不建议——回退意味着把重复行放回生产库）。**CC 建议 (b)**：代码已在生产跑通、风险已过去，补 spec 的成本只有文档，而「摄取幂等性」这条约束值得被写进 spec、不该只活在一个 docstring 里。 🔴 **本项无默认，须你明确答复** —— 按 §5「默认项两个前提」自检：提出本项的 session（本次无头批处理）收工后**无人执行**，「不答」＝停在原地而非错误继续发生，故不设默认项。 　━━━ 🐛 **2026-08-27 `OP-0827-E` 顺手发现（只登记不改，非本班范围）：本行正文里残留一个 BEL（`0x07`）控制字符** —— 位置＝本行那处 `D:\airead`，实际存的是 `D:` ＋ `0x07` ＋ `iread`，即 `\a` 在某个写入环节被当成转义序列解释掉了。**它是本项目记过的同一个坑**（`OP-0819-F` 曾修过两处同类损坏），说明**写入端那条路径至今没有被守住**。 🔑 **本班是怎么撞上的**：本班自己第一版队列 payload **同样中招** —— `C:\baoguan` 变成 `C:` ＋ `0x08` ＋ `aoguan`、`\app` 变成 `0x07` ＋ `pp`、`\reports` 里那截变成 `0x0d`；成因是正文经 heredoc 传入时，双反斜杠被外层折叠成单反斜杠、再由 Python 当转义序列解释。**本班改为「一个字面反斜杠都不写、路径全用 `chr(92)` 拼」并在写盘前断言正文控制字符数为 0**，才躲过去。 🔴 **可机械化的判据（建议纳入队列 lint）**：**队列真身里除换行与制表符外，不应出现任何 `0x00`–`0x1F` 控制字符**；现状实测＝机制环境 1 处（即本行）、业务场景 0 处。**它比「盯着某个具体路径字符串」通用得多** —— 损坏后的字节里已经没有原字符串可供 grep，**正是它一直没被发现的原因**。**本行的这一个字符本班未改**（属他人历史行，且改它应当与写入端一起修）。 ━━━ ✅ **2026-09-03 Shao Peishen 裁决（§四 分诊消化，Cowork 业务总线 `OP-0903-D`；他两批均答「都按你说的」）**： **(b) 事后补 openspec 变更包。** **理由**：同 `#136` —— 把「摄取幂等性」这条约束写进 spec，而不是只活在一个 docstring 里；IATF 可追溯性。🟢 `openspec_draft`，纯文档约半天。
