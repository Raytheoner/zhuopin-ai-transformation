---
title: "队列行日志 #117（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #117 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #117 · 外置前整格原文（2026-09-04 外置，9 段，md5:2634b161）

🔴 **批处理派出前查归档 —— openspec design 审，四处请裁（源＝§一 #397，#395 已并入；2026-08-25 CC 无头批处理 A27 出件）**：**背景一句话**——批处理器把「派出」的边际成本压到近乎零，**却也把「误派」的成本压到零，而误派的代价一分没降**（2026-08-24 run `20260824-165657` 的 A4 对应 §一 `#368`，该行三天前已销号、当日已迁归档，仍被照常派出，烧掉一整个 session）；**这一族缺陷的判别特征是错误不产生任何信号**——退出码 0、日志正常、`OPENER_DONE` 照常打出，从机器视角是一次完美成功。本包命中根 `CLAUDE.md` §5 门槛 ③（改变既有模块对外语义：同一份计划同一批参数，脚本此后会自行跳过某些 opener）⇒ 必走 openspec；已按「不跳步」停在 **propose＋design**，**未 apply、未改一行 runner 源码**，`openspec validate --strict` 通过。全文见 `openspec/changes/opener-batch-archive-precheck/`（commit `d7cf69d`，分支 `claude/a27-opener-archive-precheck`）。 ━━━ **请裁四项（均无默认项）** ━━━ **① 决策点 1 · 四态判定**：live 命中且非 done ⇒ 派出／live 命中且 `[S:done]` ⇒ 跳过／live 未命中而归档命中 ⇒ 跳过／**两侧都未命中 ⇒ 照常派出＋告警**。两处请特别过目：**⑴ 「归档命中即已完成，不解析归档行任何状态内容」**——因为实测 `#368` 归档行的**状态列仍是 `[S:open] 🆕 未开工`**（销号写在任务列），按 spec 规范位置读机器字段会得出 open、**使白跑一字不差重演**；改判依据＝迁归档由值周清扫执行，**迁移动作本身就是经人工复核的完成判定**，而状态列是否同步无任何机制保证。**⑵ fail-open 底线**——**误派会被人发现（有人在读那个 session），误跳过不会有任何人在场**，故一切模糊地带一律照常派出。 **② 决策点 2 · 锚点**：沿用 opener 标题现成的 `——队列 #NNN`（两 runner 的解析正则早已捕获，**不改解析代码、不给计划文件新增必填字段**——新增必填字段＝新增一条人守，与本包立意自相矛盾）；显式排除 `§四 #N`（**§一 与 §四 是两套独立编号**）；多行号取**合取**（全部已完成才跳过）。 **③ 决策点 3 · 共同挂点**：解析完成、`-Only` 过滤之后、**任何派出/分组动作之前**（v1 L76 后；v2 L60 后且**必须早于 L62 泳道分组**）；共用新增 `0-学习与工具/工具-opener派出前校验.ps1`，判据本体走 Python 复用 `queue_table`（**判据只存在一份**）。🔴 **附带一个必须实现的短路**：被跳过的 opener 不产生哨兵 ⇒ 若不在状态计算前短路，会被判 `NO-SENTINEL` ⇒ **v1 整批中断、v2 停整条泳道**。 **④ 决策点 6 · 派单件 `status` 回写义务归属**（#396 的治本半）：建议 **(a) 义务归执行该派单件的 session ＋ (b) sweep 常驻告警兜底**；**明确排除 (c) 由本校验器顺带查**（只能靠文件名模糊匹配 ⇒ 误跳过 ⇒ 触碰 fail-open 底线）。🔴 **并请一并裁范围**：建议 **(b) 另立一行紧跟 #396、不并入本包**——它依赖 #396 普查出的 9 份真实样本标定告警口径，与「#397 排在 #396 之后」同一道理（**判据不该凭空设计**）。 ━━━ **决策点 4／5 不在此列**（缓存与查询工具扩法，属实现细节、已给建议 (a)，不答即按建议执行；二者均为「不改变对外语义」的保守选项，符合默认项两个前提）。 ⚠️ **本项无默认，须你明确答复**：提出方是无人在场的无头批处理会话、收工即结束，按「答完之后由谁动手」判据答不出执行者；且属**判据/口径类，绝不默认生效**。 ━━━ ➕ **追加第五处待裁（2026-08-25 Cowork，Shao Peishen 当场答「要」并入）：`【设置】` 行 worktree 字段 ☑/☐ 格式 lint**——正本模板库 §〇 定 `☑/☐`，而今日波次 11 条 opener **无一合规**（9「新建独立」/1「按需新建独立」/1「不新建」）且**已全部派出跑完、零信号**。**拟加判据＝派出前校验该字段须以 `☑` 或 `☐` 起头，否则拒派并指名编号。** 🔴 **请一并裁**：⑴ 加不加这条 lint；⑵ 若加，是硬拒派还是仅告警；⑶ 存量波次文件是否回改（建议不回改，守「历史记录不追改」）。**注**：本项与本行原四处同一挂点（v1/v2 runner 派出前），故并入本次 design 审，不另开一轮。  ➕ 🔴 **2026-08-26 追加口径依据（源＝§一 #396 普查完成，Cowork `OP-0826-J`；本条只补事实、不改判据、不新增待裁项）—— 决策点 6(b)「sweep 常驻告警」若按现有设想实现，会漏掉三种形态中的两种** ━━━ **9 份 `待执行` 全部核完，实测分三类、不是两类**：**类别一 队列行已归档 5 份**（#397 能拦）／**类别二 队列行 `[S:done]` 但仍在 live 2 份**（#397 拦不住，已知）／🔴 **类别三 队列行 `[S:partial]`、而派单件自身范围已交付 2 份**（`跟进信状态单一可信源S2-S4` 对 §一 #366，`FI2三单核对起点反转` 对 §一 #372＋§四 #92）—— **这一类立行时未预料到**。 🔑 **类别三是 (b) 最难的一段**：按「行 done/归档」判，对它们**永远沉默**、会被一直重派；按「派单件自身范围」判，机器又无从知道范围边界。**「派单件完成与否跟队列行走还是跟自身范围走」是个口径问题，本次未擅自定，两份 `status` 照判据保持 `待执行`、只在件内加了警示段。请在本决策点 6 一并裁。** ━━━ **另三条实测事实，均影响 (b) 的判据形状** ━━━ **⒜ 🔴 第四种失真：3 份派单件 frontmatter 根本没有 `status` 字段**（`M1-OneDrive迁出前准备-2026-08-26`／`OneDrive迁出回滚演练-队列170前置项2-2026-08-25`／`队列写入根治与回件链路六缺陷-414与416-2026-08-26`，全部出自环境总线、全部为 08-25 后新建）⇒ **任何形如「扫 `status: 待执行`」的告警都会静默跳过它们** —— 与本包一贯的「错误不产生信号」同族的第二个入口。**(b) 须把「缺字段」判为告警项，不得判为「没问题」。** 本次未替这 3 份补值（不在 #396 的 9 份范围内，补什么值同属口径）。 **⒝ 🔴 类别归属会漂移，不是文件的固有属性**：#396 立行时（08-25）把 `FI2面板翻倍修复与R5分母对照表` 判为类别二，而 08-24 值周清扫已把 §一 #371/#375 迁归档 ⇒ 今日实测它是类别一。**同一份件在最长 7 天的清扫窗口内会自己从类别二移到类别一** ⇒ **(b) 不能按类别分流成两套逻辑，须一套判据同时覆盖「live 且 done」与「已归档」两侧。** **⒞ §一 与 §四 是两套独立编号这条约束对 (b) 同样成立**：`CLAUDE进度段lint实现` 的锚点在 **§四 #80**、不在 §一 —— 按行号查而不先判分区，会拿 §一 #80 的状态去判它。（与决策点 2 已定的排除规则同源。） **⒟ 样本总量已由 21 增至 24 份**（分布：`已执行归档` 15／`待发` 4／`待执行` 2／无字段 3）。 ━━━ **⑸ 决策点 6 之外无新增待裁项**；上述均为 (b) 的输入事实，全文与逐份判定依据见 §一 #396 状态列。
