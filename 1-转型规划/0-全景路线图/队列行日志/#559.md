---
title: "队列行日志 #559（§一）"
created: 2026-09-11
status: 生效
用途: 跨桌任务队列 §一 #559 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #559 · 外置段（2026-09-11 外置，工具第 1 批，1 段，原文原样）

> 外置人 OP-0911-T；外置前整格 5083 B、共 2 段；本批外置第 2 段；本格保留第 1 段。

✅ **OP-0911-T 收口（2026-09-11，CC worktree `agent-a698a17f2f03bc24b`，分支 `claude/op0911t-audit-anchor-559`）**：①③④ 已实现并验证；②仅设计+建造完成，历史两条记录的实际找回**留步**（理由见下）。**①** 根因确认——`approve_followup_letter.py` 是全部 8 个入口（`push_followup_letter.py`／`dispatch_followup_letters.py`／`decision_reminder_check.py`／`draft_gap_check.py`／`check_patrol_signal.py`／`flush_pending_lock_appends.py`／`run_aibot_service.py`／`annual_holiday_reminder.py`）中唯一未跟进 #269（2026-08-06）锚点修法的一个，仍手写 `NAIVE_REPO_ROOT / DEFAULT_QUEUE_RELATIVE_PATH` 当默认锚点——worktree 里跑时 `git rev-parse --show-toplevel` 答的是这个临时 worktree 自己。已改用 `resolve_default_queue_anchor(NAIVE_REPO_ROOT)`，与全部兄弟入口同源。**②** 决策口径写进 `aibot_service/repo_paths.py` 模块头 `AUDIT_RELATIVE_PATH` 常量上方（单向权威＋不裸拼接、走 backfill 事件找回、孤儿文件 quarantine 不删除）；找回工具 `scripts/backfill_orphan_audit_chain.py` 已建成（dry-run 通过），**但未对生产文件跑 `--apply`**——取证时发现主仓 `wecom_aibot_audit.jsonl` 当日（2026-09-11）**已有两处真实的、与本次找回无关的新断链**（第 6367／6399 行，`outbox_relay_scan_failed`／`disconnected` 附近，与已知的 2026-07-28 历史迁移断点不同源），根因疑似 `JsonlSink._last_hashes`/`_locks` 只在单进程内共享、多进程并发写同一文件时互不可见——这是一个独立于 #559、更底层的审计完整性缺陷，已另立 task（`task_d5f777db`，标题「Fix JsonlSink cross-process hash-chain race」）追查。工具的局部窗口校验会如实拒绝在当前不稳定的链尾上写入（这是设计正确，不是 bug）。**③** `0-学习与工具/工具-落库sweep.py` 新增第 17 类常驻告警 `_check_aibot_audit_orphan_writes`（mtime 判据、零依赖、按 `.claude/worktrees/*/` 扫描），已接进 `main()`；对生产仓库实测已正确命中 `wecom-service-home` 那份孤儿文件并推送过一次真实企微运维群告警（验证工具生效，非误发）。**④** 未对真实 README 跑 approve 脚本端到端（会真的转态一封真实业务记录），改用等效隔离回归测试锁定同一条解析链路，见下。**测试**：新增 `5-平台底座/wecom-aibot-service/tests/test_approve_followup_letter_script.py`（2 用例，行为层+AST 结构层双保险，均对旧版本做过变异验证）＋ `0-学习与工具/test_工具-落库sweep-审计文件分叉写入.py`（7 用例）全绿；`wecom-aibot-service` 全量 828 passed／1 skipped，另 2 个失败在 `test_ps1_orphan_cr_guard.py`（未触碰的文件，pre-existing、与本次改动无关）；`工具-落库sweep.py` 自身全量测试本次收工时仍在后台跑（该文件测试套件体量大，多为真实 git 子进程操作，运行较慢），未及等到完整结果，建议着陆前另行确认一次。**产出文件**：`5-平台底座/wecom-aibot-service/scripts/approve_followup_letter.py`（改）／`aibot_service/repo_paths.py`（改，模块头口径）／`0-学习与工具/工具-落库sweep.py`（改，新增第 17 类）／`scripts/backfill_orphan_audit_chain.py`（新，工具已建未执行）／`tests/test_approve_followup_letter_script.py`（新）／`0-学习与工具/test_工具-落库sweep-审计文件分叉写入.py`（新）。**⚠️ 未闭合、如实登记**：worktree `wecom-service-home` 那份孤儿文件里的 2 条真实人工决策（`followup_approval_rejected` @19:42:56／`followup_approved` @19:53:28）**仍未找回进主仓审计**——待 `task_d5f777db` 那条并发断链问题查清、或确认当前断链不影响安全找回后，跑 `python scripts/backfill_orphan_audit_chain.py --orphan-file <worktree路径> --apply` 即可完成，工具本身已就绪。
