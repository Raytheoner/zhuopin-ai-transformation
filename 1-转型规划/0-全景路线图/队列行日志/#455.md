---
title: "队列行日志 #455（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #455 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §一 #455 · 外置前整格原文（2026-09-04 外置，9 段，md5:2dba13b7）

[S:open][D:机] **待领·待定级**（定级由业务总线／值周巡检定，本线不定级——§〇 红线）。🆕 **2026-09-02 立行（Cowork `OP-0902-A`，Shao Peishen 当日答「合并处置」）** —— **三处缺陷合并为一件处置，它们是同一个根因的三面：写侧校验比读侧解析弱一档。** 　━━━　**⑴ 写侧半角竖线守卫不是反引号感知的**（读侧 `_table_data_rows` 是，见 `#314`③）⇒ 状态列在反引号跨度内合法含竖线的行，**整格重写一律被拒**，而 `--append` 改不了 `[S:]` 前缀 ⇒ 🔴 **这类行的状态字段一旦写定即锁死、永远无法翻转**。**实证 `#324`：本次应改判 `blocked` 而改不动，其状态字段至今是 `partial`、与事实不符**（该行状态格在反引号内含 4 处半角竖线，内容恰好就是朴素切列函数的代码引用）。 　━━━　**⑵ 写入时不校验反引号奇偶、也不回读写入后本行还剩几列** ⇒ 能把一个 8 列行写成 7 列而当场不报错。**实证 `#337`／`#422`：本次改判尾巴嵌的引文按 60 字硬截断，恰好把一个反引号截在中间，未闭合跨度吞掉行尾列分隔符**；🔑 **是 release 结构门禁事后拦下的，不是写入时拦的** —— 同 `#82` 形状（探针通了 ≠ 机制通了）的写侧版本。 　━━━　**⑶ 对已塌列的行拒绝一切操作**（含用来修复它的 `--append`），理由正是「列数为 7，应为 8」⇒ 🔴 **行一旦被写坏，唯一能修它的入口就把自己关上了**，只能在持锁状态下裸改文件（本次即如此，已如实留痕；锁全程未释放）。 　━━━　**建议处置（三条一起做，分开做会各自留半个洞）**：① 写入校验补反引号奇偶；② 写入后回读本行列数、不符即回滚该次写入；③ 为已塌列的行开 `--repair` 逃生入口。 　━━━　📌 **并入审核（协议〇.10）已于取号前完成**：与 `#454` 同文件但**不同对象**（那条是分诊器输出的消费路径，本行是写入守卫），🔴 **两行触碰区重叠于 `工具-共享文档编辑锁.py`，必须串行**，先动哪条交值周定；与 `#414`（根治单元格文本经 bash argv 被命令替换/撑列）**同族但已 `[S:done]`、不可并入** —— **本行正是 `#414` 的未闭合面**：那条解决了 argv 传输层，**没解决写侧校验层**。 　━━━　⚠️ **命中 openspec 触发门槛「改变既有模块对外语义」**（补校验后，同一接口相同输入的行为会变：原来能写进去的将被拒）⇒ **须走 openspec 含 design 审，不得直接改码**。 **⇒ 下一步＝起草 proposal，不直接开工。** 　━━━　➕ **并入第四条：OP 编号重号守卫（2026-09-02，协议〇.10 并入优先；由另一条同号 `OP-0902-A` 线提出、本线登记）** **不新立行的判据**：它要动的是 `edit-row`／`append-row` 的**写入校验**，与本行 ⑴⑵⑶ **同一咽喉、同一函数族、同类改动**；分开立行只会让两行抢同一把锁、且各改一半留洞。 **问题**：2026-09-02 两条会话同时用 `OP-0902-A`（`who` 分别为「Cowork OP-0902-A」与「Cowork-机制线-OP0902A」）；该编号在队列真身出现 **52 次，全库最高频**，而它是两条线共用的。 **后果**：可追溯性断裂——`#382`／`#439`／`#452` 三行的追记、以及 **27 个 git commit message** 全署同一编号，后人无法分辨哪条是谁写的；而这两条线当天对同一批行做过**互相覆盖的判断**（其中一条撤回了另一条的 `.yaml` 改判）。**IATF「单一可信源／可追溯」下属硬伤。** **根因（三类编号里只有它裸奔）**：队列行号 → `acquire --reserve` 原子分配 ✅；批次号 → release 前缀查重＋建议改号 ✅（2026-09-02 实测拦了 4 次）；**OP 编号 → 无分配、无查重 ❌**。 🔴 **咽喉已核实为两个，且都在本行触碰区内**：① `edit-row`（`--who` 必填）② `append-row`（`--who` 必填）。**git commit message 不构成第三个入口** —— sweep 是从 §二 批次行的「建议 message」取值、仍经 `append-row`（近 60 个提交实测，27 个含 OP 编号，**无一绕开**）。 **实现＝拼两块现成积木、非新建守卫**：工具维护「OP编号 → who」绑定；写入时若内容匹配 `OP-\d{4}-[A-Z]` 且该编号已绑给别的 `who` ⇒ 拒绝并建议下一个可用字母；同一 `who` 反复写自己的编号照常放行。可复用 `工具-共享文档编辑锁.py:3091`（批次号前缀查重形态）＋ 同文件 `:1667`／`:4270`（`who` 一致性校验，已有）。 ⚠️ **当日不做的理由（提出方自陈，本线认同）**：该文件当日已被多条线并发触碰；且提出方上下文里的队列状态已过时（本线当日做过状态分诊）。**交值周或下一条机制线。** 　🔑 **附一条元判据（本次由 Shao Peishen 反问逼出，建议总线考虑是否升进 `CLAUDE.md` §5 规则退休制旁）**：**判一件事该人守还是机守，不是看它像不像规则，是看有没有必经的咽喉。** 提出方当日两次未找咽喉就定性为人守——本条，以及早上把「合入 master ≠ 在跑」当人守（其实「重启后确认进程启动时刻晚于合入时刻」同样可机器验）。 　━━━　🔑 **元判据升级（Shao Peishen 2026-09-02 当场给出，建议与本行既有那条并列考虑升进 `CLAUDE.md` §5）**：**「如果能做，就不要指派手工——会带入失误。」** **它比本行既有那条「判人守还是机守，看有没有必经的咽喉」更进一步**：咽喉存在只说明「**可以**机守」，本条说的是「**可以就必须**」。 **当日实证（本班自己违反了一次）**：本班曾把 `#312` 的剩余动作写成「等 Shao Peishen 打开看板读『可 Open 池』的 N 与行号」，他一句反问后实测发现——**看板的数是运行时调 `工具-项目状态卡数据层.ps1` 直读仓库真身算的、判据写在 `index.html` 里可读**，⇒ **本班完全能复算，根本不需要人用眼睛读**；且一复算就查出「两套实现判据已漂开、N ＝ 25 vs 35」这个远比核对一个数重要的结论。 🔑 **两条判据的关系**：先问「有没有咽喉」（能不能机守）⇒ 再问「既然能，为什么还派给人」（该不该机守）。**第二问是本条新增的。**
