---
title: "队列行日志 #80（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §四 #80 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §四 #80 · 外置前整格原文（2026-09-04 外置，64 段）

✅ **⑻ 至此销号 —— J8 哨兵存在性已实现并 ff 入 master（2026-08-23，CC，worktree `sc2-caseback-fix-6eadad`，分支 `claude/op0823c-sentinel-lint-j8-5ceaf6`，commit `48c69ee`，`OP-0823-C`）** ━━━ **落点＝ `0-学习与工具/工具-CLAUDE进度段lint.py` 同一脚本、进 violations**；CI job `claude-progress-lint` 二期已切 `--enforce` 且已删 `continue-on-error` ⇒ **workflow 一行未改，J8 进 violations 即自动获得阻断能力**。续用既有变更包 `claude-progress-section-lint` 追加 §15／§16 tasks ＋ 一条 spec 需求，**未新建包**；该包 `tasks.md §0.1` 仍未获追认故仍不得 archive，**本次不受其阻、也未解决它**。 ━━━ 🔴 **判据按派单件用排除法落地，未挂在「先读」句式上** —— 实测三条哨兵只有两条含「先读」，按 ⑻ 原文字面实现会静默漏掉 L120 那条 A5 外部评审纪律（三条里最久才用一次的一条）。现取根 `CLAUDE.md` 全部反引号包裹、以 `CLAUDE.md` 结尾的路径，**只排除「自指」与「仓库外」两类**。 ━━━ 🔑 **Shao Peishen 2026-08-23 三个澄清全答 (a)，其中两个把覆盖面往外推了一档**：⑴ **裸路径（无反引号）也扫，但只进 warnings** —— 理由是**反引号同样是人写的格式约定**，只认它会漏掉「先读 x/CLAUDE.md」这种写法；不进 violations 是因为裸扫描可能误命中散文，而 §四 `#87` ⑶ 已记「误报的代价不是噪音，是它会训练人忽略这条告警」。⑵ **目标文件字节下限 200，覆盖「被清空／只剩标题」** —— 这不算内容级校验，**零同步负担**（现存三份 1,710／2,598／4,030 B，离下限有一个数量级余量）。⑶ 统计行处数**按出现次数计、不按去重路径数计**：被删掉的是某一处引用，按次数数才能让人发现「上周还是 3 处、今天怎么 2 处了」。 ━━━ 🔴 **残余风险如实登记，不得写成「已覆盖」**：**文件存在且非空 ≠ 被下沉的那一块还在** —— 若文件还在、其它内容也在，而下沉的那块被删掉，J8 仍然通过。内容级校验刻意不做（要么在根里冗余存摘要、要么给每块打标记，两者都在造新的同步负担）。已写成 `tasks.md 15.9` 并标注**永不勾选**——它是一条明账上的残余风险，不是待办。 ━━━ **验收四条全过**：⑴ `--enforce` 退出码 0 且回显 `🛡 哨兵存在性：3 处哨兵，全部命中`；⑵ **真身破坏复现（未用 fixture 代替）**：临时改名 `1-转型规划/0-全景路线图/CLAUDE.md` → 退出码 **1**、违规文案点名该路径与 L120 原文片段「外部对抗性评审纪律（Paul 2026-07-02 定）：⏰ 2026-11 中旬前须主动提醒 Shao…」→ 已改回；⑶ `0-学习与工具` 全量 **614 passed ＋ 33 subtests、零失败**，`openspec validate --all --strict` **99 items／0 failed**；⑷ `git status --porcelain` 无预期外未跟踪件，已 ff 入 master 并 push。 ━━━ ⚠️ **派单件 §五 写的回归基线 544 已过时，实测基线 606（本次 +8 → 614）** —— 🔑 **这正是本行 ⑼ 那条教训在同一份派单件里的第二次现身**：⑻ 的规格凭印象写、基线数字凭上一轮的数写，**两处都是「把一条既有登记直接当真值」**。⇒ 派单件里的任何实测数，开工时都该重跑一次再用。 ━━━ 🔶 **次序提醒仍然有效**：`§5 指针化第二批` 与本件同改根 `CLAUDE.md`，**本件已先行落地** ⇒ 指针化此后若不慎改动那三行哨兵的措辞或路径，**J8 会当场抓住**（排除法不挂句式，改措辞不影响、改路径才报）。 ━━━ 原文：🔴 **新增 ⑼：§5 深度瘦身的判据已改换 —— 频率盘点实测不成立，改用「违反是否产生信号」（2026-08-23，Shao Peishen 答 (a) 后被实测推翻）** ━━━ **背景**：根 `CLAUDE.md` 71,729 B，**§5 占 67%（48,391 B／34 条）**。⚠️ **官方「≤200 行」标准对本项目失效** —— 我们 **144 行装了 71 KB**（超长行：`专员跟进纪律` 单行 3,354 B、`执行环境标注` 单行 2,101 B）；折算官方「100 行 ≈ 2,500 tokens」基准，**我们约是它的 10 倍**。**真正的度量是字节，不是行数。** ━━━ ❌ **频率盘点已实测、结果不可用，勿重做同一版** —— 脚本按标题自动截键，在队列／CHANGELOG／近 30 天 commit message 里 grep。**输出的「零触发 17 条」经逐条核对全是假零**：`时间戳必判 UTC vs 本地`（当天我自己违反 2 次）、`跟进信串行原则`（当天 §一 #375 正因它锁着）、`会话末显式罗列决策项`（每次回复都在用）**全部被判零触发**；而泛词 `Cowork` 命中 686、零信息量。**两个根因**：⑴ 键自动截取不可靠（`🔴时间戳必判UTCv` 这种串不可能命中）；🔴 ⑵ **更根本的是语料错了** —— 队列／CHANGELOG 里没有 session 对话，**而规则被「用到」的地方正是对话**；**被提到 ≠ 被遵守，没被提到 ≠ 没用到**。若要重做，语料须换成 `.claude/projects/*.jsonl`（CC transcript），成本为独立一轮。 ━━━ ✅ **改用的判据（不需要新数据，且直接对应「留在根里的价值」）** ━━━ **🔴 这条规则被违反时，会不会产生信号？** **不产生信号**（写错了没人知道，直到第 N 次被当面纠正）⇒ **判据全文留在根**；**产生信号**（lint 报错／`release` 拒绝／sweep 告警／CI 红）⇒ **根里只留「判据 ＋ 指针」一句**，忘了也会被拦住。**当天两侧各有实证**：**不产生信号**＝代词自检（错了信已发出、撤不回）／时间戳 UTC-本地（读错不报错）／退出码（拿到一个看起来正常的 0）／「已复检」自陈；**产生信号**＝裸竖线（lint 拦）／批次清单漏队列文件（release 拒）／8 KB 上限（J6 拦）／状态列完成态符号串栏（release 拒）。**2026-08-05《规则体检 §5 判定表》的 A 类「已有机制守」4 条可直接并入「产生信号」一侧。** 🔴 **执行时的硬要求**：压缩后每条须保留**可机械执行的那一句**，**不得只留规则名**（`执行环境标注 → 见 X` 这种写法不合格）。 ━━━ 原文：📤 **⑻ 已派单，待 CC 领活（2026-08-23，Cowork 环境总线，`OP-0823-B`；零依赖可立即开工）** ━━━ **派单件**＝`1-转型规划/0-全景路线图/派单件-【CC】哨兵存在性lint-J8-2026-08-23.md`（含 opener、J8 可实现规格、三条反例单测、四条验收、三处收工回写）。**落点＝ `0-学习与工具/工具-CLAUDE进度段lint.py` 同一脚本，进 violations**；**CI job `claude-progress-lint` 二期已切 `--enforce` 且已删 `continue-on-error` ⇒ 不需改 workflow 一行**。**续用既有变更包 `claude-progress-section-lint` 追加 tasks，不新建包**（该包 `tasks.md §0.1` 未获追认故仍不得 archive，本次不受其阻、也不解决它）。 ━━━ 🔴 **出件时取证推翻了 ⑻ 自己写的规格一处，派单件已按更正版下发** —— ⑻ 原文写「每处形如**「先读 \`<目录>/CLAUDE.md\`」**的哨兵」，**而 2026-08-23 实测三条哨兵里只有两条含「先读」**：L62（`5-平台底座/`）✅、L124（`4-数字员工/`）✅、**L120（A5 外部评审纪律）写的是「细则与冷备架构师接手演练<u>见</u> …」❌**。⇒ **按字面实现只能命中 2/3，静默漏掉的恰是那条「2026-11 中旬前须提醒排 S1 收口外部评审」** —— 三条里**最久才用一次、最经不起悄悄消失**的一条。**判据因此改为排除法**：取根 `CLAUDE.md` 全部反引号包裹、以 `CLAUDE.md` 结尾的路径，只排除「自指」与「仓库外」两类，**不挂在任何句式上**（句式是人写的会变，「是不是仓库内子目录 CLAUDE.md 路径」是结构性事实）。 ━━━ ⚠️ **裸判据实测命中 5 个路径，两个必须排除**：`CLAUDE.md`（行 66／101，**自指、判它恒真零信息量**）、`~/.claude/CLAUDE.md`（行 4，**家目录全局件不在仓库，按相对路径 `exists()` 恒假 ⇒ 一条永远红的告警**）。**后者不是「多报一条无害」** —— 同 §四 `#87` ⑶ 已记的：**误报的代价不是噪音，是它会训练人忽略这条告警**，而这条告警存在的全部意义正是在真出事那次把人拦住。 ━━━ 🔑 **一句值得留的**：⑻ 那句规格是**下沉动作完成当天凭印象写的**，写的人以为三条用了同一句式。**⇒ 凡把一条既有登记直接当规格去实现，须先与真身对一次** —— 同 `#87` 已记的「**『已登记在案』本身产生了一种『已经核过』的错觉**，而登记只证明当时看到过，不证明现在还成立」。**这次是同一个错误在下一环的复现：登记者与实现者都以为对方核过。** ━━━ ⚠️ **派单件里显式登记了一条残余风险，收工报告不得写成「已覆盖」**：**文件存在 ≠ 内容还在** —— 子目录 `CLAUDE.md` 被清空、或被下沉的那一块被删掉，J8 仍然通过；内容级校验本次刻意不做（要么在根里冗余存摘要、要么给每块打标记，两者都在造新的同步负担）。 ━━━ 🔶 **次序**：与 `§5 指针化第二批` **须串行、且本件在先** —— 两者同改根 `CLAUDE.md`，**编辑锁能防覆盖、防不了「基线在跑的过程中变」**；且指针化若不慎改动哨兵行措辞，本件的排除法判据正好能当场抓住它，**顺序反了就抓不住**。 ━━━ 原文：🟢 **⑻ A 档 5 条已全部下沉落地（2026-08-22，Cowork 环境总线，`OP0822F`）** ━━━ **根 `CLAUDE.md` 78,071 → 71,729 B（−6,342 B，−8.1%）**；J7 开场预算 **86,184 → 79,864 B ／ 上限 120,000（72% → 67%）**；进度段 lint 与 J2／J3 均通过、结构未支持文件数仍为 5（未新增噪声）。**新建三份子目录 `CLAUDE.md`**：`4-数字员工/`（A1 第 1-7 步，2,598 B）／`5-平台底座/`（A2 §4 ＋ A4 §6.1，4,030 B）／`1-转型规划/0-全景路线图/`（A5 外部评审纪律，1,710 B）；根里各留一行哨兵，**A5 哨兵按清单要求写死日期「2026-11 中旬前须提醒排 S1 收口外部评审」**。**A3 §6 supplychain 按他选 (a) 只留一行指针。** ━━━ 🔴 **他当日拍板的两处边界**：⑴ **第 8 步（发布即刻起草跟进信，含串行闸三分支与发送三条硬前置）不随 A1 下沉、整条留根** —— A1 的下沉收益因此由约 4.8 KB 降到 1.7 KB，**而这正是 C3 要保的那部分**：它可能在任何会话被触发、违反后果落在外部同事身上；⑵ **兜底 lint 走「先下沉、同批派 CC 补」** ⇒ **哨兵存在性校验尚未实现，这是本条唯一未闭合项。** ━━━ 🔴 **未闭合项（待总线派发给 CC）**：在 `0-学习与工具/工具-CLAUDE进度段lint.py` 加一条校验 —— 根 `CLAUDE.md` 里每处形如「先读 \`<目录>/CLAUDE.md\`」的哨兵，逐条判其指向文件确实存在，缺失即告警（`--enforce` 下阻断）。**清单 §五 明写「不加这条，本方案不建议执行」** ⇒ 现状是**方案已执行、兜底未到位**，这条不补就是一笔明账上的欠款。按根 `CLAUDE.md` §5「答完之后由谁动手」判据（动手的是 CC）⇒ **不给选项，待总线派发**；**并入本行、不另立 §一 行**（协议〇.10 并入优先，本行 ⑺ 的同一理由适用）。 ━━━ 🔑 **一句值得留的**：下沉之后**哨兵成了新的单点失效，而它恰恰是全篇最短、最像可有可无的那一行** —— 谁顺手整理格式时删掉它，被下沉的整块规则就此对所有新会话彻底消失，**且没有任何机制会报错**。这与「工具静默回退」「判据恒真、零信息量」同族：**错误不产生任何信号。** ━━━ **验收四条全过**：迁出块与改动前原本逐字命中 4／4（基线＝改动前全文副本，SHA256 `4b2bc19ec33ada85`…）；根文件五个未触碰段（§1-§3 ＋ 顶部进度段／§5 前段／131-133 行／第 8 步 ＋ 发送硬前置／§7 ＋ footer）在新文件中逐字存在；四个文件 CRLF 纯度一致、U+FFFD 与控制字符均为 0。 ━━━ ⚠️ **顺带记一处、非本次所为**：`acquire` 报「CLAUDE.md 在上次 release 之后被直接改写（180→177 行）」—— 是 `OP-0822-D` 18:53 那次 `Last Updated` 瘦身绕过了协议〇.7 锁。 ━━━ 原文：🟡 **本行不销号 —— ⑸ 已补做完，但 J6 存量暴露一处真缺口，追加为 ⑺（2026-08-22 19:0x，OP0822A）** ━━━ **⑸ `Last Updated` 行瘦身已做完**（D 于 18:53 落地后撞车解除）：正文 **8,572 B 原文原样迁 `进度编年-CHANGELOG.md`** 新增 §「附录 · Last Updated 链历史 —— 2026-08-22 迁入批」，根文件该行只留**日期 ＋ 一行指针 ＋ 一句「本行不写进度叙事」的规则**；同批清掉两段自陈「保留一轮、下次删除」的 2026-08-19 HTML 注释残留。**根 `CLAUDE.md` 86,147 → 78,071 B**；已 `--enforce` 的进度段 lint 与队列结构 lint **双双通过**。**J7 实测开场预算：根 78,071 ＋ 交接卡 8,113 ＝ 86,184 B / 上限 120,000（72%）。** ━━━ 🔴 **⑺ 新缺口：R5 说的是「四条 session 接力」，而定长交接卡只治了一条** —— J6 存量实测：`session接力-采购域场景落地.md` **62,257 B（超上限 7.6 倍）＋ 10 个日期节**；`session接力-质量域场景落地.md` **46,263 B（超 5.6 倍）＋ 12 个日期节**；`session接力-财务域场景落地.md` **2 个日期节**。🔑 **值得记的一点**：这三份**不进根注入**，所以它们不占「开场预算」那个数字 —— **但它们占对应域会话的开场读入，而那正是采购／财务／质量三条业务线每天在跑的地方。** ⇒ **「总量没超标」掩盖了「某几条线每次都超标」**，与「分层前的随机是分层后的确定」同族。**处置**：按 Phase1 收口那份的同一手法逐份治（迁归档件 ＋ 覆盖式六块），归各域专线或环境总线顺手做，**不新建 §一 行**。 ━━━ 原文：✅ **二期已收口——⑴⑵⑶⑷ 四件全做完（2026-08-22，CC，worktree `followup-dispatch-apply-25679f`，分支 `claude/op0822d-claude-lint-phase2-7d4698`，commit `2383fa5` 已 ff 入 master）**，但 🔴 **本包仍不得归档**，原因见末尾 ⑦。 ━━━ **⑴ `--enforce` 已切**：CI job 加 `--enforce` ＋ **同批删掉 `continue-on-error: true`**，实跑退出码 0，job 清单仍 8 个。🔑 **一处方法教训**：落盘后的反查断言`"continue-on-error" not in job` **误报了**——命中的是新注释里提到这个词的那句话，YAML 键其实已删干净；改用 `yaml.safe_load` 复核才判对。**⇒ 过严的反查与过松的反查一样会给出错误结论，反查本身也要选对粒度。** ━━━ **⑵ 存量已清零**：J2 **26 条**原文原样迁出（QD-B 14→6／SC8 11→6／wecom-aibot-service 19→6），新建 QD-B 与 wecom 两份同目录 `CHANGELOG.md`；J3 由 8 条降为 **0**（5 条压缩、压缩前原文全文存档可 grep，3 条随迁出消失）。🔴 **本行原记的「31 条」确已过时，重测拿到 26——派单件那句「先重跑一次拿真数」是有效的**。**J1 第 ⑵ 档「整条已闭合」已按 Shao Peishen 2026-08-22 拍板开口**（复用 J4 那份 20 词词表，零新判据、零新词表）。🔑 **词表当场证明不是空转**：它保住了 SC8 自己 2026-08-21 注解点名的两行（`2026-06-24` 未结／`2026-07-06` 未接线），另拦下 3 行（待办／阻塞／暂不）——**5 条 blocked 全部落在 SC8，而 SC8 恰是三个文件里唯一被人逐行读过一遍的那个**。⚠️ **边界已写进三处载体**：本判据在用「词表未命中」推出「已闭合」，而 J4 词表自己写着「未命中不代表安全」；两者能并存**只因代价不对称**——J4 在写入侧、误放行即丢失，本判据在迁出侧、误判只是「搬去了 CHANGELOG 而不是队列行」，原文仍在。 ━━━ **⑶ J6 接力件定长交接卡已实现**（≤8 KB ＋ 零日期节；**按字节不按字符**，中文 UTF-8 下按字符判会得出宽约 3 倍的假上限；**用 glob 不硬编码四个路径**，归档件除外）。🔴 **实测撞出一件派单件没预料到的事：R5 改版实际只改了四份中的一份** —— `session接力-Phase1收口.md` 8,113 B 合规，`财务域` 72,482／`采购域` 62,257／`质量域` 46,529 B，**超限 5.7-8.8 倍**。**存量清理已并入 §一 `#337`**（同一命题、同一做法、同一「顺带执行」时机）。⚠️ **故 J6／J7 本期刻意只告警不阻断**——一并硬拦等于把 `--enforce` 这件事本身再推迟一轮；**但这等于新欠下一条「有实现但不生效」的判据，还款条件写在 design 决策点 9 与 `#337`，别让它变成第二个 R5 旧版。** ━━━ **⑷ J7 开场预算已实现**：实测 **94,260 B ／ 上限 120,000（79%）**（根 `CLAUDE.md` 86,147 ＋ 交接卡 8,113），**每次运行都回显、不只在超限时打印**。🔴 **队列刻意不计入，这正是它的设计成果**：真身数百 KB，但 2026-08-06 起（§一 #268）已改由只读 CLI 按行号查，开场读入 ≈ 0。⚠️ **上限 120,000 是本 session 定的默认值、没有实证依据**，`--budget-cap` 可覆盖，待他确认或另定。 ━━━ **⑸ 回归**：`0-学习与工具` **544 passed ＋ 33 subtests，零失败**（一期基线 487）；新增单测 16 条（40 vs 24），含反例守卫 `test_含未结的条目绝不被判可迁`。**同批改了 4 条既有用例的夹具、没有改它们的断言**——它们测的命题（承接载体识别不得误纳伪行号／不存在的路径／离得太远的章节号）二期依然成立，变的只是「正文零未闭合措辞时的结论」。 ━━━ **⑹ 顺带查出一件不属本次范围、但已经在红着的事**：CI job `openspec-validate`（`--all --strict`，**无 `continue-on-error`**）在 **master 上已经是红的** —— `editlock-mutex-stale-cleanup-resilience`／`queue-dual-file-split`／`sc2-weekly-report-mvp` 三个在途包全报 `No deltas found`。已实测确认**与本次改动无关**（`git ls-tree origin/master` 三者 `specs/` 均 0 文件、`.openspec.yaml` 均不存在）。**已登记 §四 #99。** ━━━ 🔴 **⑺ 未闭合，如实登记：`tasks.md §0.1` 仍未获追认 ⇒ 本包未 `archive`。** 该项＝追认「条目区由形状判据收窄为区间判据」。**二期后它的分量变了**：一期它只影响一行告警文案，二期已切 `--enforce`，**它现在决定 CI 拦不拦人**。按根 `CLAUDE.md` §5「答完之后由谁动手」判据（动手的是另起 session）⇒ **不给选项，待总线派发**。0.2／0.3 已闭合（0.3 结论＝**两问都答「都不」**：二期本体不占 §一 行；`#351` 是错的并入位，它是 `append-row` 咽喉三修、与本包零交集；真正剩下的 J6 存量并入 `#337`。**并入而非另立还有一个硬理由：实测机制类可动 WIP ＝ 22／上限 22 已满格，另立会被 `release` 当场拒绝**——协议〇.10「并入优先」在这里不是风格偏好，是唯一可行路径）。 ━━━ 原文：🟡 **二期新增两件待办 ⑸⑹，均须排在 `OP-0822-D` 收工之后（2026-08-22 追记，OP0822A）** ━━━ 🔴 **⑸ `Last Updated` 行瘦身（Shao Peishen 2026-08-22 答「按推荐」＝现在砍，但撞车已实测，改为待办）** —— 该行现为 **8,674 B 的整段进度叙事、占根文件 9%**，而它按定义只该是一个日期；内容已全部在 `进度编年-CHANGELOG.md`。**8,674 → 约 200 B，是全篇性价比最高的一刀，且不需要任何取舍判断。** ⚠️ **本轮未做的真实原因**：`OP-0822-D` 正在改 **根 `CLAUDE.md`** 与 **`进度编年-CHANGELOG.md`**（实测其 worktree 脏 12 项），而本刀两处都要动 ⇒ **按协议〇.2 触碰区重叠不抢领**。🔑 **顺带记一条**：我给他选项时写的顾虑是「若 D 在写会撞 `CLAUDE.md`」，**实际先撞上的是 CHANGELOG** —— **判断依据变了、结论没变，但这说明写顾虑时只想到了最显眼的那个触碰点。** ⇒ 列触碰区要列全，不能只列主角。 ━━━ 🆕 **⑹ 规则下沉候选清单已出件，待他逐条圈定** —— `1-转型规划/0-全景路线图/CLAUDE.md注入量治理-规则下沉候选清单-2026-08-22.md`。**这是与「指针化」并列的第二条路，且是唯一真正减少注入量的那条**：实测**子目录 `CLAUDE.md` 不被注入**（本仓库 13 份、合计约 20 万 B，一份都不在系统提示里），而根文件是无条件全文注入。**清单分三档**：🟢 A 档 5 条低风险建议下沉 ／ 🟡 B 档 3 条须配强哨兵 ／ 🔴 C 档 8 条不得下沉。🔴 **两条硬设计写在清单里，执行时不得省**：⑴ **不做裸下沉，每条下沉必须在根留一行 80-120 B 的哨兵**（哨兵管「知道有这件事」、子目录管「怎么做」）——**因为下沉的最坏情况不是「懒得点开」，而是「根本不知道有这条」**；⑵ **lint 须加一条校验：根文件里每个哨兵指向的子目录 `CLAUDE.md` 确实存在**，否则哨兵本身成为新的单点失效、被删了也没有任何机制会发现。**清单明写「不加这条，本方案不建议执行」。** ━━━ 原文：🟡 **二期 ⑴「§5 指针化」第一批 P0 五条已做完，本行不销号（2026-08-22，Cowork 环境总线，OP-0822-B，批次 `B-0822_22`）** ━━━ **实测收益：五条 26,355 → 15,506 B（−41%），§5 61,618 → 50,769 B（−18%），根 `CLAUDE.md` 105,290 → 94,441 B（−10%）。** ⚠️ **远低于方案件预估的「§5 −66%／全文 −39%」，原因不是没做完，而是 Shao Peishen 当日把三个澄清全部裁给了「判据全文保护」**——定夺 1 答「判据全文优先，目标字节作废」／定夺 2 答「8 步清单本体留全文」／定夺 3 答「第 3 条纳入保护」。**⇒ 方案件 §四 P0 表那五个目标字节（900／500／600／800／600）已整体作废，不得再作为第二批的基准。** ━━━ 🔴 **三处方案件数据被实测推翻，第二批排优先级前必须重算** ━━━ ⑴ **第 3 条体量算大了 2,620 B**：表标 6,253 B，真身仅 3,633 B——多出的是紧随其后的四段独立条目（取证方法已迁出／工具静默回退／配套勘误／直读原则），**其中「工具静默回退」正文明写「本行是硬提示，不得再降」，照方案边界去压会误伤**。⑵ **第 5 条的可迁出量几乎为零**：方案判「#204／#44 串栏实证已在 CHANGELOG 附录 B、正文那段是重复 ⇒ 删重复段」，**实测正文那处早已只是一行 25 字的指针，并无重复段可删** ⇒ 该条 4,063 B 里除三处实证（约 850 B）外全是判据本体，实际只压掉 392 B（−9%）。⑶ **第 1 条压不到 2,300**：迁走第 8 步长注后仍有 4,808 B，因为第 8 步剩下的（串行闸三种闭环形态、⑴⑵⑶三分支、发送三条硬前置）**全是判据**。 🔑 **共同教训：按「条目总字节」排的优先级会系统性高估收益——字节大的条目往往正是判据密度高的条目，而判据不能压。第二批应改按「手册层字节数」排序，不是按条目总字节排。** ━━━ **五条终态**：条1 6,775→4,808 ／ 条2 6,592→1,745（−73%，唯一接近方案预估的一条，因其手册层占比确实最高）／ 条3 3,633→1,683 ／ 条4 5,292→3,599 ／ 条5 4,063→3,671。 ━━━ **19 个迁出块全部逐字核验为原文原样**（以 `git show HEAD:CLAUDE.md` 为原本、双端锚点切片后比对，19/19 逐字命中）。**落点四处**：`0-学习与工具/取证方法知识库.md` §一.3 ／ `1-转型规划/0-全景路线图/专线opener模板库.md` §〇.15 ／ `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md` 附录 D（D-1／D-2／D-3）／ `6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md`「第 8 步串行闸辨析」节。 **验收四条全过**：J1 逐条指得出承接载体；原文原样 19／19；控制字符与 U+FFFD 五个文件均为 0；**§1-§4／§6／§6.1／§7 ＋ 顶部进度段 ＋ footer 的 SHA256 全部逐字节未变**，各文件 CRLF/LF 纯度均未漂移（含 `取证方法知识库.md` 本就是纯 LF、按其原样写回）。 ━━━ 🔴 **两项留给第二批的发现** ━━━ ⑴ **第 3 条内部有一处判据重复**：08-04 与 08-15 两次补齐各自列了一遍「队列行／批次行／Last Updated／接力节标题」，合并可再省约 200 B 且不丢任何判据——**本批刻意未做**，因方案件 §六明写「不要在同一批里既指针化又改规则内容」。⑵ **footer 有一处规则残留**：`CLAUDE.md` 文末注释自陈「2026-08-19 原 Last Updated 行保留一轮，下次写入时按『只留最近 1 条』删除」，而 2026-08-22 OP-0821-M 那次写入并未删它 ⇒ **约 2,100 B 残留仍在**，且这正是方案件 §一 点名的「Last Updated 行 8,674 B／8%」那一块。**本批未动 footer**（不在验收范围、且属另一条规则的执行）。 ━━━ ⚠️ **迁移中发现一处称呼错误，按「原文原样」未改并已就地标注**：迁入跟进信 README 的「发送硬前置成因」段里「专员被请去复核的正是**她**自己举证的 `S04Y.0112 / R01B.0754`」中的「她」指**姚祖怡（男）**，写于名录建立（2026-08-20）之前；已在该节加注说明，更正另行处理，**不在本批范围**。 ━━━ 原文：📤 **二期已派单，但须排在 `OP-0822-B` 之后（2026-08-22，OP0822A；Shao Peishen 当日指示「#80 可否尽量早排」）** ━━━ **派单件**＝`1-转型规划/0-全景路线图/opener集-CLAUDE进度段lint二期与sweep告警修正-2026-08-22.md` §OP-0822-D。**续用既有包** `openspec/changes/claude-progress-section-lint/`（实测 35/38），⑶⑷ 作为新增 tasks 追加，**不新建包**。 ━━━ 🔶 **软序理由（不是保守，是有具体冲突）**：二期要对 `CLAUDE.md` 跑 enforce 并做**存量条目数**迁出，而 `OP-0822-B`（Cowork，§5 指针化第一批）正在大改同一文件。**J1-J5 判顶部进度段、B 改 §5，看似不同区段——但编辑锁只能防覆盖、防不了「基线在跑的过程中变」**：二期要数「存量违反几条」，B 一边改这个数就一边动。**先 B 后 D。** ⚠️ **另一件可立即开工、且与 B 零重叠**：`OP-0822-C`（§四 #87 ⑴⑶，触碰 `工具-落库sweep.py`）**可与 B 并行**，这就是「尽量早排」的实际落点——**能今天开的是 C，不是 D**。 ━━━ 🔴 **二期开工第一件事是重测基线，不要用本行记的数**：行内「3 个文件违反 J2、合计迁出 31 条（QD-B 14→6、SC8 11→6、wecom-aibot-service 24→6）」**已过时** —— `wecom-aibot-service/CLAUDE.md` 已于 2026-08-22 由 54,231 瘦到 29,138 B（−46.3%），条目数几乎肯定变了。**同本行自己记的那条教训（「这个判断三周内第三次被凭直觉做错」）。** ━━━ 原文：🟡 **二期范围已扩大，且规格被实测捅出一个盲区（2026-08-22 并入，OP0822A；按协议〇.10 并入审核，未新建行）** ━━━ 🔴 **盲区：J1-J5 只管「顶部进度段」，而根 `CLAUDE.md` 的大头根本不在那里。** 2026-08-22 分段实测：**§5 工作流 61,356 B（59%）** ／ 顶部进度段 16,866 B（16%） ／ Last Updated 行 8,674 B（8%），全文 103,782 B。**⇒ 此前两次瘦身（08-09、08-16）与第四批迁移（08-21）动的全是那 16%，所以「瘦了又涨」是必然的，不是执行力问题。** 08-21 瘦到 92,665 B、20 小时后回到 103,095 B（第三次反弹，≈ +12 KB/天），**而这次的增量主要落在 §1 名录与 §5 规则正文，不是进度段** —— **换句话说，前三次我们一直在治一个只占 16% 的部位，并且每次都以为治好了。** ⇒ **二期新增第 ⑴ 项：§5 指针化**，每条规则只留「一句话规则 ＋ 承接指针」，成因·实证·教训整段迁 `进度编年-CHANGELOG.md` 附录或 `0-学习与工具/取证方法知识库.md`；`规则体检-CLAUDE.md-§5-29条判定表-2026-08-05.md` 可直接接着用。🔴 **§5 是规则正文，误删代价高于顶部段 ⇒ 先出方案经 Shao Peishen 过目再动手。** ━━━ **⑵ 新增：接力件也要进 lint 范围。** R5 已于 2026-08-22 改版（OP0822A，Shao Peishen 提出）——四条 session 接力**由「留最近 2 个日期节」改为「定长交接卡」**：硬上限 **8 KB**、固定六块、**零日期节**，收工覆盖而非追加。**实测支撑**：`session接力-Phase1收口.md` 当天由 **167,806 B 两次瘦到 8,033 B**；🔑 **旧版规则不是没被执行，而是执行了也不管用——它给「日期节」留了合法席位，于是每一棒只追加、没有一棒负责删。** ⇒ 请在 lint 里加两条：接力件字节 ≤ 8 KB、正文零 `## 【日期` 形态节。**规则文本已同批落 `文档治理规范与规整执行清单-2026-07-07.md` R5 与根 `CLAUDE.md` §5 两处。** ━━━ **⑶ 开场预算检查**（算三份开场件字节并回显、超限告警）同属本包度量侧，一并做掉，不另立行。 ━━━ 原文：✅ **一期已建成、全量绿、已合入 master（2026-08-21，CC，worktree `fi2-webhook-var-check-bc19fa`，分支 `claude/claude-progress-lint`，openspec 变更包 `claude-progress-section-lint`）**：J1／J2／J3／J5 落新建 `0-学习与工具/工具-CLAUDE进度段lint.py` ＋ CI 新 job `claude-progress-lint`（**一期 `continue-on-error: true`、脚本默认告警模式**；二期切 `--enforce` 时须**同时删掉那行**，已写进 job 注释）；J4 落 `工具-共享文档编辑锁.py::_validate_claude_progress_open_item`，**一期即阻断生效**——它只对本次持锁窗口内**新增**的条目判定，存量零影响，且它是唯一在源头阻止「顶部段兼任未闭合项载体」继续新增的那条。条目切分器 `_claude_progress_entries` 落编辑锁、为唯一权威实现，lint 侧委托它——**两处不各写一套**，否则会出现「lint 说 7 条、release 说 6 条」这种两套判据各自为政的情形。回归：`0-学习与工具` **487 passed／0 failed**（10 分 35 秒），`wecom-aibot-service`（编辑锁下游消费者）单独复跑亦全绿，`openspec validate --all --strict` 88 passed，队列结构 lint／引导样板 lint／凭据扫描三道既有门禁均未回归。三条必须的反例单测齐备（🔴 前缀条目被数到／拼接两份队列文本解析会丢行／含「未结」触发 J4）。 ━━━ 🔴 **实现把规格推翻了一处，须 design 审追认**：规格 §六 J2 的裸判据（两种前缀 ＋ 日期模式）在当前 master 真身上数出 **4 条**，而规格自称的当日真值是 **2 条**——多出的两行是 `📦` 迁移指针行与「memory 层已收割并停用」元说明行，**两者结构上与真进度条目完全无法区分**（都是「粗体标题 ＋（ISO 日期）」）。⇒ 条目区改用**区间判据**（`> **当前进度**` 头行之后 → `📦` 行之前），区间外同形状的行一律列为「元说明行，不计入」并回显、不静默吞掉。🔑 **这是同一个判断三周内第三次被凭直觉做错**——前两次是只匹配 `> **` 前缀（12 条数成 15 条／9 条），这一次是两种前缀都匹配了、仍然多数出两条。**「看起来很确定」在这个判断上已经三次不可信。** ━━━ 📊 **二期工作量基线（派单件 §六硬要求的那两个数）**：**3 个文件违反 J2，合计需迁出 31 条**（QD-B 14→6、SC8 11→6、wecom-aibot-service 24→6）；**15 条违反 J3**（根 CLAUDE.md 两条各 3,131／3,066 字符，场景级 13 条）；**5 个文件结构未支持、未判定**（FI1／FI2／QD-A／SC1／SC2，已列名不静默放过）。⚠️ 顺带更正规格一处数据：§六 J5 写「优先 SC8（100 KB）与 FI2（75 KB）」，**实测 git blob 为 SC8 33,245 B、FI2 40,547 B**，两个数都对不上，二期排优先级时勿沿用。 ━━━ 🔴 **三项待 Shao Peishen 定夺，均已写进变更包 tasks.md §0（本包因此保持未归档），三项动手的都不是本 session ⇒ 按 §5 判据一律不给选项、待总线派发**：**⑴ 追认条目区由「形状判据」收窄为「区间判据」**（成因见上一段：裸判据数出 4 条而真值 2 条）；**⑵ J1 对「整条已闭合」的历史条目是否开口**——按规格字面，那 31 条待迁里绝大多数是早已办完的历史记录、本无未闭合项可丢，J1 仍要求它们点名队列行 ⇒ 二期会变成「31 次立队列行」，与 `[D:机]` WIP 上限直接冲突；**本包不擅自开口**；**⑶ 二期那条 §一 行并入 `#351` 还是另立**——即本行原有的那个待裁定项，一期**未占 §一 行**（走 openspec 变更包完成）故未消化，二期（存量清理 ＋ 切 `--enforce`）仍需一条 §一 行。 ⚠️ **本条曾把这三项写成「两项 ＋ 一个『另』」，抬头还写「本报告 2 项定夺」**，2026-08-21 由 Shao Peishen 当场指出（原话：「有两个『定夺项』加一个『另』好像就是三个」）后更正。🔑 **教训：同一批待定夺项一旦分成两种写法，条数就不再可数**——「需你定夺」清单的编号必须覆盖全部需他答的事，任何挂在清单之外的「另：…」都等于把计数交回给他去做，与该格式规则的设立目的正相反。 ━━━ ✅ **已推进到「派单件已备、待派」（2026-08-21，Shao Peishen 指示「赶紧推进环境侧能优化记忆的相关工作」＋ Anti 报告 #7 建议 2 印证）**：派单件 ＝ `1-转型规划/0-全景路线图/派单件-【CC】CLAUDE进度段lint实现-2026-08-21.md`（含开场词、三处落点、四条判据的可实现规格、分期上线策略、四项验收与三条必须的反例单测）。🔴 **落点已实测更正**：Anti 报告说「CI 基线或 Sweep」，而 `工具-落库sweep.py` 全文「lint」命中 0 ⇒ **J1/J2/J3/J5 挂 CI（新 job `claude-progress-lint`，与 `bootstrap-stub-lint` 同构），J4 挂编辑锁 `release` 校验器**，两者都不挂 sweep。⚠️ **一期必须 `--warn` 不硬拦**（存量 J3 均值 2,190 全部超限，一期就 enforce 会挡住所有人 push ⇒ 这条规则会第四次被绕过，这次是绕过 lint 本身）。**仍须裁定**：并入 `#351` 还是另立 §一 行（`[D:机]` WIP 状况以领取时为准）。 ━━━ 原文：OP-0821-B 已出根 CLAUDE.md 进度段 lint 判据规格五条 J1-J5（J1 承接载体安全阀／J2 条目数上限／J3 单条长度上限／J4 写入侧 release 校验／J5 场景级同款），与 §一 #351 子项 ⑷⑸「人守该机制化」同族。按协议〇.10 并入审核，建议并入 #351 而非新立行（[D:机] WIP 现满格）。须裁定并入还是另立。规格见 `1-转型规划/0-全景路线图/memory与上下文预算治理-审核与方案-2026-08-21.md` §六
