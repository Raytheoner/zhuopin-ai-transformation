---
title: "队列行日志 #387（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #387 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #387 · 外置前整格原文（2026-09-04 外置，16 段）

[S:blocked][D:机] 🟡 **五处缺陷代码全部修完、四项需人在场的动作未做（2026-08-24，CC，worktree `aibot-387-380-receipt-route`，commit `cb69852`，已 ff 入 master 并 push；openspec 变更包 `aibot-receipt-route-by-source-group`，propose＋design＋specs＋tasks 齐全、`validate --all --strict` 107/107、**未归档**）** ━━━ ✅ **⑶ 主修法已落**：`chattype=="group"` 且 `chatid` 非空 ⇒ 回执直接回该 chatid，**不查映射表、不看 `matched`**；私聊判据一字未改、原样保留为回落。🔑 **一句话口径**：**来源群是入站帧给出的事实，「发送人属哪个部门→那个部门的群在哪」是推断链；事实可用时不退回推断链**——本次缺陷正是那条推断链给出了一个语法合法、语义全错、且不报错的目的地。**事故反例已配单测锁死**（`test_group_message_replies_to_source_group_not_sender_department`：**刻意把 IT 群配好**，陈承仍在财务群 @ ⇒ 断言必须回财务群且 MUST NOT 回 IT 群 —— 若实现哪天退回按部门查表，这条当场红）。 ✅ **⑵** `IT: wrvDL_DAAARjP0BlFLup5e1Cv3vcCvMQ` 已入映射表；🔴 **键名是 `IT` 不是 `IT部`**（须与 `department_mapping.yaml` 的**值**逐字一致，写错即静默跳过、与本次要修的缺陷同形），已配断言。**webhook 按本行原议未配**。 ✅ **⑷** `DEPARTMENT_TO_QUEUE_OWNER` 增 `"IT": "业务总线"`（不造 `IT专线`——`whitelist.py` 明文禁止；`业务总线` 是队列既有合法 owner，且 `#385`/`#386` 实际就是它派的）。 ✅ **⑸** 本人入站不建行（归档保留）；判据与 `forwarding.should_forward` **同源**；新增独立字段 `queue_append_skipped`，**刻意不复用 `queue_append_deferred`**（后者语义是「还欠着、等补录」，复用会让补录链路去补一条永不该补的行）。 ✅ **⑹** 补录侧幂等：按 `input_pointer` 判重、**扫全部物理队列文件**（只扫写入侧那份会把「已挪走的行」误判为不存在——「一份拆成两份、下游只跟一份」家族的又一入口）；读不到文件时**刻意 fail-open**（去重网 fail-closed 的代价是丢行，比补重行严重）。 ✅ **`group_not_configured` 已由静默跳过改为「跳过并告警」**——⚠️ **只对配置缺口类告警，`sender_unmatched` 刻意不告警**（它已有三处可见后果；本次另一半正是在治「告警太少」，一起做过头会把它做回噪音）。 ━━━ **回归**：服务 **487 passed**／平台 **380 passed**，零漂移。🔑 **改动前有 2 条既有用例失败，两条都失败得完全正确**：`..._four_departments_...` 把「IT 缺席」钉成了期望值、`..._falls_back_to_paul_owner` 把 ⑷ 的缺陷钉成了期望值 —— **已改判、非放宽**（集合仍精确相等，只是多了一个真实存在的键）。 ━━━ 🔴 **四项未做，全部因「无人在场」，逐条列明**：**⑴ design 决策点 2-7 未签认**（决策点 1 主次已由他 08-24 当场确认，其余六个是实现层取舍）；**⑵ IT 群 chatid 首发反查未做**——本行原文已写明「采集只证明机器人收到了来自这个 chatid 的消息，不证明它就是运维部AI保障群」；**⑶ 告警真实群冒烟未做**——🔴 **同族反面教材就在本项目**：`#82` 两类告警建成 9 天、每天在跑、**一次都没真的响过**，「探针通了 ≠ 机制通了」；**⑷ 生产 `reports/pending_queue_appends.jsonl` 里 15:30:53 那条重复记录未清**——代码侧幂等已能挡住它被 flush，但那条记录仍躺在文件里，且**它是机器人状态文件、服务常驻**，动它前须确认服务当刻未在写。 ━━━ ⚠️ **一处本行范围外、如实划界**：机器人追加的队列行**一律 `[域未标注]`**（`queue_appender` 刻意不写 `[D:...]`，原注释「机器人无法判定机制/业务归属，交人工事后补」）。本行 ⑷ 把「owner 落 Paul」与「域未标注」并列描述，但**后者不是 IT 特有的**，是所有机器人追加行的共同属性，「按什么判机制/业务」是跨场景口径 ⇒ 未动。 ━━━ 以下为立行原文 ━━━ 🔴 **主次已由 Shao Peishen 2026-08-24 当场确认，领活方按此排序、不得颠倒**：他原话「**「回执按来源群回」才是主修法：陈承那条 @ 发在财务部群，就算 IT 群配好了，回执还是会跑到 IT 群去。两件都要做。**」⇒ **⑶ 是主修法、⑵ 是补配置**；**把 IT 群 chatid 填进映射表就收工 ＝ 没修**（那样陈承下次在财务群里 @，回执照样跑去运维群）。 ━━━ ✅ **⑵ 的 chatid 已实采到手（2026-08-24 15:30:50）：`wrvDL_DAAARjP0BlFLup5e1Cv3vcCvMQ`** —— Shao Peishen 依既定办法在运维部AI保障群发了一条带标识的消息，`intake.py` 归档审计记下 `chatid` ＋ `chattype=group`（队列 #279 新增字段），**与 2026-08-06 采那四个群 chatid 是同一套办法**。⚠️ **该值仍须首次真实发送时反查一次**——采集只证明「机器人收到了来自这个 chatid 的消息」，**不证明这个 chatid 就是运维部AI保障群**（理论上他若同期在别的群也发过话就会混），**发第一条回执时对着群里看一眼即可**。**webhook 那条路按本行原议不用**（理由见下）。 🔴 **⑸ 同批照出第五处，与 ⑷ 同源、请一并修** —— **`ShaoPeiShen` 本人不在 `department_mapping.yaml`**（该表实测只有 5 键：`2023458`／`ChenChen`／`Hongqin.Wang`／`YaoZuYi`／`tangyanping`），⇒ 他在任何群里说的话都判 `matched=false`、归档落 `7-外部文档/待分拣/`、队列行 owner 落 `Paul`、回执按 `sender_unmatched` 跳过。✅ **修法已拍板（Shao Peishen 2026-08-24 原话「就按这个办：把对你本人的入站消息直接不建队列行」）** —— **不把 `ShaoPeiShen` 加进 `department_mapping.yaml`**（他不属任何专员部门，加哪个都别扭），而是**在入站侧识别出「发送人 ＝ 本人」时直接不建队列行**：他是任务的发起方，不是「需要被拆件的外部来件」。**归档本身保留**（`7-外部文档/待分拣/` 仍落文件，留痕不丢），**只是不再产生待领行**。⚠️ **实现时注意**：`forwarding.py` 已有一条同语义判据 `inbound_forward_skipped ｜ reason=sender_is_paul`（转发侧已经在识别「这是他本人」了），**队列侧照搬同一判据即可，不要新造第二套「谁是本人」的判定**。 ━━━ 🔴 **一处我方自己的口径更正，如实登记不掩饰** —— 本行原写「全库已积 15 条…**没有一条被认领过**」，**这句是错的**。实测：那 15 条里 **14 条早已是 `[S:done]`**，由 2026-08-06／08-12 两班拆件巡逻逐条关闭并写明「非业务反馈、系采集 chatid 的标注测试短信、无需回灌」；**真正待领的只有 `#389` 一条**（已随本次拍板一并拆件销号）。**错因**：我只 `grep` 了任务列的重复文案就外推出「无人认领」，**没看状态列**——同族＝「只读结果看着整齐时，先问它是不是根本没读到我以为的那一列」。 🔑 **⇒ 但这条修法依然该做，只是理由要换成对的那个**：代价不是「留下 15 条孤儿行」，而是**每一条都要占用一次拆件巡逻的人力去关掉一条注定无意义的行，已经真实发生 14 次**；且每次他在群里说句话就再来一次。**这个理由比原来那个弱，但它是真的。** ⚠️ **⑹ 另有一条待人工核对的遗留，本次已核完、结论写在这里免得下一位重查**：15:30:53 那次 `queue_sync_degraded` 把一行推进了 `5-平台底座/wecom-aibot-service/reports/pending_queue_appends.jsonl`（机器人当时发了「队列同步失败 1 次，一行待人工核对合并」告警）。**实测：该行对应的队列行已经存在，就是 §一 #389**（`grep` 归档件名在两份队列里命中数 ＝ 1）⇒ **那条 pending 是重复，若被自动 flush 会产生第二条同内容行**。**Cowork 未动该文件**（机器人状态文件，属 CC 边界）；**请领活方顺手处置**：清掉该行，或在 flush 侧加一道「同 `input_pointer` 已存在则跳过」的幂等判据（**后者更该做——`queue_sync_degraded` 的根因是 `.git/index.lock` 被并发 git 进程占着，这在本机是常态，还会再发生**）。 ━━━ ✅ **IT 群已答（Shao Peishen 2026-08-24）：IT 对应的群 ＝「运维部AI保障群」**，他给的是 webhook（**真实 key 已按 CI「凭据扫描」门禁抹去，2026-08-25 CC #398⑶；原值仍在 git 历史里，轮换与历史处置见 §四 #118**）：`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=<REDACTED-见§四#118>`。 🔴 **但请领活方注意：直接把这个 webhook 配进去是走回头路，先看完这三句再动手** —— ⑴ **两张映射表不是一回事**：`department_group_mapping.yaml` 存的是 **webhook 环境变量名**（旧通道），`department_group_chatid_mapping.yaml` 存的是 **chatid**（现行通道）；**回执现在走的是后者**。⑵ **队列 `#270`／`#279`／`#281` 已于 2026-08-06 把群 cc 从 webhook 整体迁到 chatid，理由白纸黑字**：「webhook 单向，**群成员的回复进不到任何地方**」。**把 IT 一路配回 webhook，等于在这个群上单独退回迁移前的状态** —— 陈承恰恰是最常在群里回话的那一位，**他的回复会再次掉进黑洞**。⑶ 🔴 **更省事的路已经现成**：截图群成员列表显示 **「邵总数字人」BOT 已经是运维部AI保障群的成员**（成员：邵培申／陈承／范君／邵总数字人BOT）⇒ **该群的 chatid 可以直接采集**，方法与 2026-08-06 采到那四个群 chatid 的办法完全一样——**请 Shao Peishen 在该群发一句带标识的话，`intake.py` 的归档审计会把 `chatid` 记下来**（队列 #279 新增的 `chatid`／`chattype` 字段），取出来填进 `department_group_chatid_mapping.yaml` 即可。**⇒ 建议：采 chatid，不用 webhook；webhook 值先留在本行备查，不进配置。** ⚠️ **但这只解决 ⑵ 那一半** —— **⑶「回执按来源群回」仍是主修法**：陈承那条 @ 发在**财务部群**，即便 IT 群配好了，回执还是会跑到 IT 群去。**两件都要做，别拿配映射当收工。** ━━━ 🔴 **WIP豁免：本行是一条正在生产上持续静默丢消息的既存缺陷，不是新开的建设项**——立行当刻机制类可动 WIP ＝ **23／22**，已超上限。**豁免理由三条**：⑴ **它此刻正在发生**（今日两次 `group_notify_skipped`，且已造成外部同事重复劳动——陈承 13:06 群里 @ 后无回执，15 分钟后改私聊又发一遍）；⑵ **不立行即无承接载体**，本次取证结论会退化成一段散文（正是 §四 #91 ⑵ 反复记的那个失败形态）；⑶ **本行不增加建设面**，其修复本身就是「把一个静默失败改成会报警」，属减法。 ⚠️ **豁免不吸收信号，如实上报**：**机制类可动 WIP 已 23／22，须一次分诊** —— 交接卡早已写明「A 类四行销号后上限应回落一档（20 即够），销号做完而上限没跟着降，就成了靠『忘了收回』维持的上限」；本次是该提醒第一次真的挡住了一条该立的行。**已列入本次会话末「需你定夺」。** ⚠️ **本次尝试过先减后加、未奏效**：已把 §一 `#385`／`#386`（陈承来件的两条 intake 行）如实拆件转 `[S:done]`，**计数仍为 23** ⇒ intake 行不计入机制类可动 WIP，减不动。 ━━━ 2026-08-24 Cowork 业务总线 `OP-0824-A` 取证立行；**Cowork 未改任何代码、未碰服务、未重启机器人**（边界）。 **openspec 判定：须走**（命中根 `CLAUDE.md` §5 机制类门槛③「改变既有模块对外语义」—— 回执路由口径由「发送人部门」改为「来源群」，同一入站消息的对外行为变了）。 **并入审核（协议〇.10）**：逐条过 §一 非 done 行——`#380`（李姣龙白名单）触碰 `dispatch.py`、与本行 `group_notify.py`／`connection.py`／`intake.py` 不重叠且粒度差一个量级；`#281` 已 `[S:done]`；`#282` 是运维群 URL 收敛、触碰区在 sweep 侧。**无可承载行，按例外取号新建。** ⚠️ **与 `#380` 可并行但同属 `wecom-aibot-service`，同车更省一次部署与重启。**　➕ **2026-08-25 晨值周复盘追记（Cowork 总线实测）**：常驻监听器进程 PID 53264 自 **08-24 00:18** 启动后从未重启——**本行与 #380 昨晚合入的改动（回执按来源群回／白名单）不在运行中的代码里**；而落库 sweep 21:47 向运维群播报的「wecom-service-home 例外已消除、同步恢复正常」经核属**判据误清**（它清的是 worktree 同步例外标志、未核对进程启动时刻晚于新代码合入时刻）——与「探针通了不等于机制通了」同族。⇒ **本行全部冒烟验活项的硬前置＝先重启验活**（工具-执行体对齐重启.ps1 -RestartOnly，先 -DryRun）；sweep 恢复播报的判据缺口挂本行一并修或另行并入，勿再新立行。　➕ **2026-08-25 晨 A20 重启验活完成（CC 无头批处理，`-RestartOnly` 实跑，退出码 0，适用各关全过）**：**上一条追记指出的硬前置已解除** —— 停服前进程链 `wscript 49992 → powershell 66632 → python 53264` 均起于 08-24 00:18；重启后 `wscript 70220 → powershell 81144 → python 68044`，起于 08-25 08:21:36～39。 **三项验活判据逐条坐实、不采信脚本自报**：⑴ **新进程 08-25 08:21:39 晚于 `cb69852` 合入时刻 08-24 20:14:55** —— 旧进程比修复早 20 小时 ⇒ 上一条追记的判断成立、非虚惊；⑵ **`authenticated` 审计真的出现**：`2026-08-25T00:21:43.288Z`，紧随启动发起时刻 `00:21:36Z`（取自**主工作区** `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`　⚠️ **worktree 侧同名文件停在 08-04、是死档，别读错那一份** —— 本次差点按 #387 立行时的相对路径去读 worktree 那份，读到的会是一段与今天无关的 08-04 历史）；⑶ **常驻任务清单核对（#338 ⑸ 那个坑未复发）**：3 个关联任务只重启了停服前 `Running` 的 `ZhuopinAibotDevListener`，两个日任务 `ZhuopinDecisionReminderDaily`／`ZhuopinFollowupDispatchDaily` 由脚本显式跳过、`LastRun` 未变（08-24 20:36 ／ 08-25 06:15:58）⇒ **未计划外触发**。 ➕ **第四项交叉验证（判据里没写、但不验就等于没验）——「重启到的是不是新码」**：worktree `HEAD=764642a` 含 `cb69852`（`merge-base --is-ancestor` 退出 0）、`aibot_service` 目录 `status --porcelain` 空、`connection.py:499-500` 的 `source_chatid`／`source_chattype` 在位、`department_group_chatid_mapping.yaml` 的 `IT` 键在位 ⇒ **运行码 ＝ HEAD ＝ 新码**，排除「重启了、但重启回旧码」这一形态。 ━━━ 🔴 **sweep 恢复播报判据缺口：读码后确认机理比上一条追记写的更重，本次按 opener「不产新码」设定登记不修** —— 上一条写的是「未核对进程启动时刻晚于新代码合入时刻」，**更准确的说法是它根本没有「需重启」这个能跨轮存活的状态**：`工具-落库sweep.py::render_resolved`（L3240）的触发条件是「worktree 名掉出 `exceptions` 集合」，而 `exceptions` **每轮从零重算**；「触碰常驻服务路径、需人工重启」这一例外**只在 `_ff_carrier` 返回 `ffed` 的那一轮**才被塞进集合，下一轮该 worktree 已是 `aligned`（落后 0）、走 `continue`、**永不再入集**。 ⇒ **这不是一次偶发误判，是结构性保证：ff 发生在第 N 轮，第 N+1 轮必然播报「例外已消除、同步恢复正常」，与有没有人重启过完全无关**；21:47 那条播报是按设计工作，**「误清」二字甚至抬举了它 —— 它不是清错了，是它从来就没打算记住这件事**。 🔑 **最毒的一点在文案**：「同步恢复正常」**字面上是真的**（ff 确实成功了），假的是它被读成「这件事结了」—— 同族＝ §四 #59「无新文件时退出码 0、零告警」，但更隐蔽：那个是沉默，**这个是一句正确的话出现在错误的时机**，而沉默至少不会主动骗人。 **修法方向**＝把「需重启」改为**每轮独立重算**的状态（比对常驻进程 `CreationTime` 与最近一次触碰常驻服务代码路径的提交时刻），而不是只在 ff 那一轮采样一次 —— **即本次人工跑的这套判据，它已经被验证可行**；按行内要求**不新立行、挂本行**。 ━━━ ⚠️ **两处如实登记，不掩饰** —— ⑴ **脚本可用性钝角**：opener 写的「先跑 `-DryRun` 读动作面」**照字面执行得到零信息** —— 不带 `-RestartOnly` 时第 2 关判「已对齐（落后 0）」即 `Stop-WithCode 0` 短路退出，**根本走不到打印动作面那一步**（干跑的价值恰恰全在那一步）；须 `-DryRun -RestartOnly` 才打印。本次两条都跑了，此处登记形态、**未改码**。 ⑵ **停服 5 分钟触发了服务自身的缺口告警**：`gap_alert_sent ｜ recipient=ShaoPeiShen ｜ last_event_at=2026-08-25T00:16:44Z` 于 `00:21:44Z` **真实发出** ⇒ Shao Peishen 手机上那条告警是本次重启的副作用、**不是故障**，如实登记免得被当成新缺陷追一遍。 ━━━ 🔴 **对外真实动作三项仍全部留步（无人在场，批处理硬规则①）**：⑵ IT 群 chatid 首发反查、⑶ 告警真实群冒烟、`#380` 李姣龙真实发送 —— **本次只拆掉了它们的硬前置（新码已在跑），冒烟本身一步未做**，勿把本条读成「#387 可收口」。 ⚠️ **另 ⑹ 那条待清记录的坐标对不上，只登记观测、不宣称已清**：按行内给的 `reports/pending_queue_appends.jsonl` 去找 —— **主工作区侧该文件不存在**，worktree 侧为 **0 字节、`LastWriteTime` 停在 08-02 16:49**（远早于 08-24 15:30:53）⇒ **那条重复记录当前不在任何一份该文件里**。无证据说明它被谁清掉，故**刻意不写「已清」**（写「已清」＝把「我没找到」冒充成「它不存在了」）；该子项须领活方重新定位承载文件后再定处置。　➕ **2026-08-25 拆件巡逻追记（⑹ 已定位，审计直接证据）**：`queue_sync_pending_flushed`＝2026-08-24T10:00:43.900Z（18:00:43 本地；`input_pointer`＝`26b8fa…` 归档件，`recorded_at`＝07:30:53Z）——**那条 pending 重复已于 08-24 傍晚被 flush 成真实队列行 §一 `#392`**（与已销 `#389` 同指同一归档件；flush 时刻早于 `cb69852` 合入 20:14 本地，幂等判据当时未上线）⇒ A20 在两份 pending 文件里找不到该记录的原因＝**已被消费，不是被清**；`#392` 本班已按重复行销号。⚠️ 顺带一条事实给 ⑹ 领活方：`#389` 已被周清扫迁 `跨桌任务队列-归档-202608.md` L545——若 pending 滞留跨过周清扫，只扫两份现役队列的判重会把已归档原行误判为不存在；判重范围是否含归档份（或改按审计 flush 记录判重）请领活方一并定，承接仍挂本行、未另立行。　━━━　🔧 **2026-08-30 `OP-0829-W` 机制类 WIP 分诊改判：`[S:partial]` → `[S:blocked]`**（Shao Peishen 2026-08-30 答「全批」）。**依据＝本行自陈原话**「五处缺陷代码全部修完、四项需人在场的动作未做」——阻塞方不是「现在能动手的人」，不属可动负荷，故改判为 `blocked`。🔴 **只改状态字段，工作范围·优先级·领取方一字未动**；**刻意用 `blocked`／`timed=` 而不用 🛑** —— 三者都不计入可动 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 行内原话），会让本行从催办面整个消失；`blocked` 只是不占配额，仍可见可催。误判可一条命令改回。**根因（`partial` 被当作可动 ⇒ 主体做完留尾巴反而永久占配额）根治已并入 `#435` 子项 E。**
