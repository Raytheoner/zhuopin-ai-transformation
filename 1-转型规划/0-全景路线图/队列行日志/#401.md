---
title: "队列行日志 #401（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #401 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #401 · 外置前整格原文（2026-09-04 外置，23 段）

[S:done][D:业] 🟡 **代码已交付、判据未生效（2026-08-25 CC）** —— 剩三步 LAN 留步（对照表／黄金基准真实重跑／`.51` 部署）＋ 第四步翻开关（硬前置＝姚祖怡签认，随 #402）。**销号判据 ＝ 开关在 `.51` 上翻 ON 且他已签认**，不是「代码合入」（同 #82：机制建成 9 天、天天在跑、一条没发出去、没人察觉）。原状态：待领（P2，判据已签认、零依赖可开工；发布四关照走，第 8 步跟进信并入 #402 不单独起草） ━━━ 🟢 **2026-08-26 ⑸ 三步中的 L1 已完成（OP-0826-V，CC，commit `49dd1b2` 已 ff push master）；L2／L3 仍留步、判据仍未生效** ━━━ **L1 真实前后对照表已出件** ＝ `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_401_kit_date规则1修复前后对照-2026-08-26.md`（随采购部#19 请姚祖怡签认的材料，聚合行 §一 #402）。**跑法**：生产载荷 `SC8_NET_INVENTORY=on`（脚本自检并硬拦，实测 on）；`--srm-cache` 用 `.51` 上 `C:\baoguan\app\reports\srm_answer_cache.json` 的**只读本地副本**（Shao Peishen 2026-08-26 明示放行）；业务日期 2026-08-26。**输入规模**：预测订单行 105 ／ BOM 行 4353 ／ 叶子件 1606 ／ SRM 承诺记录 650 ／ 逐笔答交明细覆盖 542 个物料 ／ 在途 PO 覆盖 777 个物料。 **四色**：🔴 **105 → 71（-34）** ／ 🟠 0 → 27 ／ 🟡 0 → 7 ／ 🟢 0 → 0（合计恒为 105）；**逐行变动 39／105 行**。**分支适用面**：规则 1 **43 行**（本次改的就是这一支）／规则 2 未来 24 行（本次不动）／规则 2 已过期 38 行（本就一致）。 🔴 **三条不变式自检全部成立**：⑴ 变动行中落在规则 1 之外的 **0 行**；⑵ 齐料日变晚的 **0 行**；⑶ 红只减不增（105→71）。 ⚠️ **一条须与对照表一并端上桌、免得「-34」被读成「问题解决了」的现况**：**修复前 105 行全部是 🔴真延期（其余三色皆 0）** —— 这是基线、不是本次改动造成的，但它意味着**规则 1 修好之后看板仍有 71 行红**。 ━━━ **仍留步两步**：**L2 黄金基准真实重跑**（本次未做：须 `build_golden_real.py` 重新取数后再跑）；**L3 `.51:8091` 部署＋冒烟**（本次**刻意不做** —— A12／sc8 泳道同波次也要部署 8091，同时部署会互相覆盖，属 OP-0826-V 三条硬边界之 ⑴）。 **⑹ 翻开关仍是第四步、硬前置未变**：`SC8_KIT_DATE_RULE1=on` 须 **L1 对照表 ＋ 姚祖怡复核签认** 两者齐备；本次只出材料，**未翻开关，`.51` 上一个数字都没变**。 ━━━ ➕ **2026-08-26 CC/OP-0826-Q（§一 #402 起草班）：L1 对照表已随 `采购部#19` 第六节整表端给姚祖怡签认（起草待审、🔴 未发）** —— 信内含分支适用面（规则1 43 行／规则2 未来 24 行／规则2 已过期 38 行）、四色 🔴105→71（−34）／🟠0→27／🟡0→7／🟢0→0、**逐行变动 39/105 全表**、三条不变式自检。🔴 **两处刻意的反读防护已写进信里**：⑴ 「**−34 不等于问题解决了**——开之前 105 全红是基线，规则 1 修好后仍有 71 行红」；⑵ 「**签认之后我方还要做 L2 黄金基准真实重跑与 L3 `.51` 部署冒烟才翻开关，在那之前看板还是现在的样子**」。 🔴 **本行状态与销号判据一字未改**（仍 `[S:partial]`；销号 ＝ 开关在 `.51` 翻 ON 且他已签认）；**L2／L3 仍留步，不得据「对照表已随信送出」认为 ⑸ 已闭合。** 　━━━ 🟢 **2026-08-27（CC，OP-0827-B，worktree `lan-354-401-c82160`）：⑸ 三步中的 L2 与 L3 已完成 ⇒ 三步全闭；⑹ 第四步翻开关仍未做、判据仍未生效，本行不销号** ━━━ **L3 `.51:8091` 部署＋冒烟已过**（A12 已收工，端口占用解除）：`NEW_PID=9800 CREATED=2026-08-27 10:23:41` —— **进程 `CreationDate` 真刷新**（2026-07-14「脚本说重启了其实没有」那条教训的判据）；`/api/ping` 200；`/`、`/materials`、`/cases` 登录后各 200；`POST /api/refresh` 200 `ok=True rows=105`。 🔴 **部署后的关键证据，正是 ⑹ 要的那个形态**：`/api/baoguan` 读回 `counts = {red:105, yel:0, grn:0, gap:0}`、`param_version = sc8-params-v1`（**无 `+rule1` 后缀**）、`C:/baoguan/.env` 里**没有 `SC8_KIT_DATE_RULE1` 这个键** ⇒ **代码上了、看板一个数字都没变、开关仍 OFF**，与 ⑹「L3 做完 `.51` 上跑的仍是现行口径」逐字吻合。 ━━━ **L2 黄金基准真实重跑已过**：`scripts/build_golden_real.py` 用**今天的实时数据**重新取数（FO 真实订单行 107／BOM 2699 行·直接子件 1180／SRM 承诺 480 条，其中权威 answer=14、看板 board=466／取样 3 张／队列 pending=3 **未外发，对客闸红线守住**），冻结件写入 `data/golden/real_frozen`；`tests/test_golden_real.py` **由 skip 转 1 passed**。⚠️ **一条须如实说明的前提**：`data/golden/real_frozen` 属 gitignore 真实数据，**在本 worktree 里原本不存在**，故此前它一直是 skip 状态——本行 08-25 那句「真实黄金基准均零漂移」是在另一处有冻结件的工作区测的，本次是在本 worktree 里第一次真的跑起来。 🔴 **顺带现场坐实了 #354 的核心修复**：从 **linked worktree 内**跑该脚本，解析结果是 `anchor=monorepo → C:/Dev/zhuopin-ai/.env`（**主工作区那份**，18/18 键）——`--git-common-dir` 规范化按设计生效，正是「worktree 里读到陈旧副本且不报错」那个病灶被治好的直接证据。 另：`tests/test_real_integration.py` 3 例在导出凭据后 **3 passed**（默认 skip 是其既定条件「须 `SC8_RUN_REAL=1` 且凭据就位」，**不是回归**——首次直接跑报 `FO_API_BASE 未配置`，核到是 pytest 进程没载入 `.env`，导出后即过，特此写明免得下一班误判）。 ━━━ **SC8 全量 509 passed / 3 skipped**（较 master 基线 508 增 1 ＝ 黄金基准由 skip 转 passed），零漂移。 ━━━ 🔴 **⑹ 第四步「翻开关」本轮未做，硬前置未变、一条都没松**：`SC8_KIT_DATE_RULE1=on` 仍须 **L1 对照表 ＋ 姚祖怡复核签认**（随 `采购部#19`，聚合行 §一 #402）两者齐备。**销号判据仍是「开关在 `.51` 上翻 ON 且他已签认」，不是「三步 LAN 留步做完」** —— 本次只把技术前置清干净，判据侧一步未动。 　━━━ 🔎 **2026-08-27 `OP-0827-E` 核实结论：⑸ 三步已全闭，本行已无 LAN 侧待补项** —— L2／L3 均已由 `OP-0827-B` 于本日完成并回写本行；剩下的「翻开关」等的是**姚祖怡签认**（随 `采购部#19`），不是回内网。**只读旁证（本班经 SSH 只读探测 `.51`，只取键名不读值）**：`C:\baoguan\.env` 现有 19 个键，**其中没有 `SC8_KIT_DATE_RULE1`** ⇒ 开关确实仍 OFF，与 B 记的 `param_version=sc8-params-v1`（无 `+rule1` 后缀）互为独立佐证。 ━━━ 🔴 **本班一次误判如实登记（未落到任何写入，被机制拦下）**：本班开工时在**自己的 worktree 副本**里读本行与 `#354`，读到的是 `origin/master` 那一版（B 的队列回写当时只落在主工作区、尚未 commit）⇒ 一度判定本行「真没做完、L2/L3 仍留步」。**拦下它的是编辑锁**：`工具-共享文档编辑锁.py` 在 acquire 时打出「⚠ 检测到本地影子副本…与权威文件内容不一致」，才发现读错了对象。🔑 **判据一句：队列的权威副本永远是主工作区那份；在 worktree 里读队列，读到的是它被 commit 的那一刻的样子，而队列恰恰是最常处于「已写未提交」状态的文件。** 这与「工具静默回退」同族 —— 读一个存在的相对路径不会报任何错，只是它不是你以为的那一份。 　━━━　✅ **2026-08-28 硬前置解除：姚祖怡已签认规则 1 修复前后对照表（拆件巡逻定时班回灌，源＝§一 #428／`采购部#19` 第六节 `☒ 认可`）**　━━━　他勾的是「认可 —— 按规则 1 改后的算法开起来」。⇒ **本行销号判据的两半里，「他已签认」这一半已成立**；余下 **L2 黄金基准用重新取的数真实重跑确认不漂移 → L3 新版部署 `.51` 并冒烟 → 第四步翻开关**，**全部是我方动作、不再需要他**。🔴 **销号判据不变**：仍是「开关在 `.51` 上翻 ON」，不是「他签了」，也不是「代码合入」。📌 对照表基线（他签认的就是这一份，重跑后如与之偏离须回头找他）：业务日期 2026-08-26／预测订单行 105／叶子件 1606／逐笔答交覆盖 542 个物料；四色 🔴105→71（−34）、🟠0→27、🟡0→7、🟢0→0；逐行变动 39/105；三条自检全过（越界 0 行／齐料日变晚 0 行／红只减不增）。⚠️ **他同时对四色阈值另有新指示（🔴 晚 2 周以上／🟡 晚 2 周以内，见 §四 #110）**——那是**显示层分级**、与本行的**齐料日起算点**是两件事，**翻开关不必等阈值改档**，但两者先后会影响他看到的板子，排期时一并考虑。 　━━━　🟢 **2026-08-28（CC `OP-0828-U`）：硬前置「姚祖怡签认」已满足 —— 他原话 `☒ 认可 —— 按规则 1 改后的算法开起来。`**（`采购部#19` 第 6 件；🔴 该勾**在段落里、不在表格里**，Cowork 初版提取脚本只遍历含复选框的 `<w:tr>` 故整段漏掉，08-28 已更正，见拆件件 §一⑹）　━━━　🔴 **但本次开不了，原因不是没做：`.51` off-LAN，实测不可达**（`ping` 不通／`:8091/api/ping` 返 502 且是本机 clash-verge TUN 代理给的／`-NoProxy` 直连超时／SMB `c$` 亦不可达）⇒ **既改不了 `C:\baoguan\.env`，也重启不了服务**。 ━━━ 🔴 **回内网后不得只翻开关，必须先核一件事（本条是本次最该被记住的）**：`.51` 上跑的代码**是否已含规则 1**（`forecast.no_feedback_start_date` / `heuristic_base_date` 入参）。**本行代码 08-25 才 ff 入 master，而 `.51:8091` 的上一次部署是 08-24（#344）** ⇒ 若 `.51` 仍是旧版，`SC8_KIT_DATE_RULE1=on` **不会报任何错、也不会有任何效果**（旧代码根本不读这个键），**看板一个数字不变、且没有任何信号告诉你为什么** —— 正是本项目记过的「工具静默回退」那一族。**正确次序 ＝ ① `sync-to-server.ps1` 部署并核进程 `CreationDate` 真刷新 → ② 翻开关 → ③ `POST /api/refresh` → ④ 核四色确实变了**；四步缺一，「已开」这句话都不成立。 ━━━ **开之前／开之后两组数已跑（🔴 变体口径，`srm=[]`，同一份冻结真实输入、零额外 API 请求）**：**规则1 OFF ＝ 🔴102 ／🟠0 ／🟡0 ／🟢1**；**ON ＝ 🔴66 ／🟠29 ／🟡7 ／🟢1**（合计均 103）。**逐行变动 36/103；其中齐料日变晚的 0 行 ✅**（规则 1 只应让齐料日前移，这条不变式过了）。 **与信里给他看的那张表（2026-08-26 `.51` 生产实测 71/27/7/0，合计 105）逐格核对**：**🟡 偏紧 7 逐格相同**；🟠 29 vs 27、🔴 66 vs 71、合计 103 vs 105 —— **接近但不相同，如实报，不照抄**。三条可解释原因且均非实现问题：① 本次 `srm=[]`（未跑 `/purchase/answer`，1 req/30s 会抢生产配额）；② 样本本身小一圈（103 行／1413 叶子件 vs 105 行／1606 叶子件）；③ BOM 35 个料号拉取失败（连接器自带告警）。⇒ **`采购部#20` 若要引用四色数，引用 08-26 那份生产实测的 71/27/7/0，不要用本次这组**；本次这组的用途是量方向与量级、并核那张表没算错。 📌 底稿＝`4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_334_402_姚祖怡0828回件四项落地-2026-08-28.md` §五；复跑入口＝同工程 `scripts/find_insufficient_cumulative_cases.py`。 ⚠️ **本行仍 `[S:partial]`**：签认到手、代码在 master，但**部署与翻开关两步都还没做**（销号判据是「开关在 `.51` 上翻 ON 且姚祖怡已签认」，现在只满足后一半）。 　━━━　🔴 **LAN 留步（2026-08-28 新增，Cowork `OP-0828-N` 登记；本行此前在未闭合产出扫描器的「已核实闭合、指纹未变」静默名单里，本次追加会使其自动重新告警——这是有意的）**：**规则 1「开起来」姚祖怡已在 `采购部#19` 回件里签认**（原话「☒ 认可 —— 按规则 1 改后的算法开起来。」），`采购部#20` 已把「今天没开成」如实告诉他。**回内网补做**，四步缺一不可、**次序不能颠倒**： ⑴ 先跑 `sync-to-server.ps1` 部署并**核 `.51` 上的 `CreationDate` 真的刷新了** —— 🔴 **不能只翻开关**：规则 1 的代码（`forecast.no_feedback_start_date` ／ `heuristic_base_date` 入参）2026-08-25 才 ff 入 master，而 `.51:8091` 上一次部署是 08-24（`#344`）；**若 `.51` 仍是旧版，`SC8_KIT_DATE_RULE1=on` 不报任何错、也不产生任何效果**（旧代码根本不读这个键），**看板一个数字不变而没有任何信号告诉你为什么** —— 属「工具静默回退」族。 ⑵ 再翻 `SC8_KIT_DATE_RULE1=on`。 ⑶ 再 `POST /api/refresh` 触发一次重算。 ⑷ **再核四色确实变了**（预期由「102 红／0 橙／0 黄／1 绿」变为「71 红／27 橙／7 黄／0 绿」量级；🔴 **以 `.51` 生产实测为准**，`OP-0828-U` off-LAN 变体口径那组 66/29/7/1 只作方向参照、不作验收基准）。 ━━━ 📌 **同批另两件也须回内网才做得了**：判例 1 展示层 **D1**（Excel 导出件完全无窗口外缺口提示，**有实质误导风险**）与 **D2**（「另有 N 个物料…」只写个数不写数量）的修复与验证 —— 差异清单已由 `OP-0828-U` 出完，见 §一 `#334`。 ━━━ ⚠️ **本条不设截止**，但它挡着一件已对专员书面承诺的事：`采购部#20` 里写了「这四步我们会走完再告诉你，不会只翻开关就说开好了」。 ━━━ ✅ **2026-08-31 `OP-0831-L` 第二段⑶：四步执行全部完成，本行销号** ━━━ **快照点**：`.51` 部署前进程 PID 9800/10012 CreationDate 08-27 10:23:41；`.env` 翻开关前原文已在 `.51` 本地备份 `C:\baoguan\.env.pre-rule1-2026-08-31.bak`（SHA256 `C5C4AF39…`）。**① 部署**：`sync-to-server.ps1`（笔记本→`.51`，`supplychain-server` 通路）推送最新代码，重启后 `NEW_PID=8232/9508 CREATED=2026-08-31 15:29:53`（真刷新，非脚本假象）；开关翻前先核一次基线**零漂移**：`🔴102/🟠0/🟡0/🟢1`、`param_version=sc8-params-v1`（同 08-26/08-28 记载一致）。**② 翻开关**：`SC8_KIT_DATE_RULE1=on` 幂等追加（追加前键不存在，非覆盖），重启后 `NEW_PID=9680/3812 CREATED=2026-08-31 15:32:49`。**③ 触发重算**：`POST /api/refresh` → `ok=True rows=103`。**④ 核四色**：`🔴68/🟠27/🟡7/🟢1`（合计103）——与 08-26 生产实测签认基线 `71/27/7/0`（合计105）**量级/方向吻合**，差异（今日103行 vs 签认时105行、红68 vs 71）属正常数据漂移（5天间隔、真实订单/答交持续变化），不因此判失败；三条不变式对照：越界0行、齐料日变晚0行、红只减不增——均成立。 🔴 **本次冒烟顺带揪出一处真实缺陷（新立 `#445`，详见该行，不阻塞本次销号）**：`baoguan_service.py` 直接引用 `config.PARAM_VERSION` 常量而非 `active_param_version()`，导致翻开关后仪表盘展示的 `param_version` 仍显示 `sc8-params-v1`（无 `+rule1` 后缀）——**已排查范围＝仅限保供看板自身快照展示层，SC8 对客承诺主链路（`forecast.py`/`commitment.py`/`pipeline.py` 走 `default_params()`→`active_param_version()`）不受影响**，计算结果本身正确、错的只是展示标签。Shao Peishen 2026-08-31 当场拍板：保持开关 ON、新立行跟进修复，不因此回滚。**回滚 SOP 未触发**（无需回滚，功能验证通过）。 📌 **销号判据回顾**：本行原判据＝「开关在 `.51` 上翻 ON 且姚祖怡已签认」——签认 08-28 已满足、翻 ON 本次已完成且验证通过 ⇒ **两条件齐备，本行至此销号**。
