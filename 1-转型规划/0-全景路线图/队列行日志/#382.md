---
title: "队列行日志 #382（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #382 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #382 · 外置前整格原文（2026-09-04 外置，17 段）

[S:open][D:机] 🟢 **已激活·可领（2026-08-31 值周巡检：机制类可动 WIP 当场重跑实测 20/22，按协议〇.9「排队行在 WIP 回落时转可动」去 🛑；本行自 2026-08-24 立行起排队 7 天，先到先得优先于本日新取号的 #439/#440）**；🆕 2026-08-24 立行（Cowork OP-0823-E，Shao Peishen 答 (a)），去🛑后可领 　━━━　🔴 **2026-08-28 拆件巡逻定时班实测新增一条（属本行 ⑵ 的邻居，如实登记）：定时班在沙箱里跑时，`release` 的校验⑹ 结构性地无法执行 —— 每一次都会被 fail-closed 拒绝。**　━━━　**实测**：本班在挂载工作区上跑 `git status --porcelain` 耗时 **约 40 秒**（`real 0m40.4s`／关掉 fsmonitor 后 `0m41.5s`，两次一致），而 `工具-共享文档编辑锁.py` 取脏文件状态那处 `subprocess.run(..., timeout=20)` 是 **20 秒** ⇒ `TimeoutExpired` ⇒ 走「无法取得工作区脏文件状态（非 git 仓库／git 不可用／超时）」这一支 **fail-closed 拒绝 release**。🔴 **注意它不是「脏文件没登记」**：本班改动全部已登记 §二 批次 `B-0828_T`，校验⑹ 若真跑起来会通过；**被卡住的是校验本身跑不动**。🔑 **判据（与 §一 #416 那条『校验无法执行 ≠ 校验不适用』互为正反两面）**：那条说的是**不能把取数失败一并豁免掉**（fail-closed 是对的）；本条说的是**当 fail-closed 因环境常态触发时，它就从安全阀退化成了一道每次都拦人的门** —— 拆件巡逻是定时任务、每天都跑，**这意味着它每一班都要走一次逃生阀**，而逃生阀走成常态就等于没有阀。**可选修法（供总线定，本班未擅自改码）**：⑴ 把该处 timeout 提到 60 秒（与本文件另两处 30／60 秒对齐，最小改动）；⑵ 超时与「非 git 仓库」分开处置——超时改为**重试一次再拒**；⑶ 给 `git status` 加 `--no-optional-locks` 或缩小扫描范围。**建议 ⑴＋⑵。**📌 **本班处置**：按拒绝提示走行内逃生阀。**登记豁免：定时非交互班次，`git status` 40s 超出工具 20s 超时上限，校验⑹ 无法执行；本班全部改动已登记 §二 批次 `B-0828_T_拆件巡逻0828_427428拆件与133134立行`，登记完整性实质已满足。** 　━━━　🔒 **Shao Peishen 2026-08-31 答《本周计划-2026-08-31》**：**去 🛑 处置已确认，不推翻。** 🔑 **他明确了理由不是「先到先得」这条形式规则，而是实质判据**——本行是**正在出血的口子**（`release` 校验⑹ 在定时/沙箱班次结构性 fail-closed、逃生阀已走成常态，**2026-08-31 07:5x 他报又实撞一次**），而 `#439`／`#440` 两条**不具备立即开工条件、占位即浪费**。⇒ **本行为本周唯一新激活的可动位**，见《本周计划-2026-08-31》A2。 　━━━　🔒 **Shao Peishen 2026-08-31 定今日执行序列（明示）：A1 → 回 LAN 收口 → 清池。**　⚠️ **本行（A2）不在今日序列内，他未指定时点** —— 本行**保持可领**，时点不代判；领取前照例先核 §一 `#305` 是否在办。 　━━━　✅ **本行⑴收口（CC OP-0901-C，独立 worktree `queue-382-event-driven-patrol-3ff211`，分支 `claude/queue-382-event-driven-patrol`）**：拆件巡逻开班方式由「工作日定时双班」改为「桥一打标即开班」，章程 §一~§五 正文一字未动，只换触发方式。　━━━　**判据 1（先查后动）· Python 进程能否直接触发 Claude 侧 scheduled task？⇒ 不能，两条证据**：⑴ 本 worktree 唯一可用的同类工具 `mcp__scheduled-tasks__list_scheduled_tasks` 看到的是另一个互不相干的任务库（`.claude\scheduled-tasks\`，仅 2 个一次性任务），压根看不见、也碰不到 `Claude\Scheduled\huijian-chaijian-patrol`（17 个任务的另一个 store）；⑵ `draft_gap_check.py`（#245）文首早已记录同一条边界——巡逻 prompt 在仓库外，仓库内代码无法触达/触发——本次实测坐实，非推测。⇒ 走章程原定的退路：桥一打标后写信号文件，巡逻侧改轻量高频探测。　━━━　**建成什么（真实代码，非方案文档）**：新增 `patrol_signal.py`（信号读写：fail-open＋累积列表＋按 checkpoint 消费，防「扫描期间又来一条被整份清空」）；`followup_readme_bridge.mark_reply_arrived` 的 `ACTION_MARKED` 分支同一把锁内顺带落信号；新增探针 CLI `scripts/check_patrol_signal.py`（`--check`/`--clear --before`，docstring 内含给 Cowork 的接入两步原文与建议 cron `*/10 * * * *`）。新增单测 51 条，服务全量 **734 passed / 1 skipped，零回归**。　━━━　**验活（真实代码，隔离 repo_root，未碰任何生产文件）**：真实 `mark_reply_arrived`（真实归档文件名，源自采购部#17 真实事件）→ 真实标记 `ACTION_MARKED` → 真实落信号 → 真实 `check_patrol_signal.py`（subprocess 调用，非 import mock）识别 `[SIGNAL]` → `--clear` 消费 → 复探测 `[NO-SIGNAL]`，全链路一次通过。　━━━　**处置／未闭合**：章程摘除「工作日双班」那句＋把探针接进 SKILL.md＋cron 调至分钟级，这三步须 Cowork 在其能触达 `Claude\Scheduled\` 的会话里用 `update_scheduled_task` 完成——本 worktree 结构性够不着，同 #245 边界，接入原文已写在 `check_patrol_signal.py` 文首。**章程摘句待总线执行，本行未自摘**；巡逻当前定时频率本次未改动，是否已下线本 worktree 无法确认（同一结构性边界，需总线自证）。commit/push 见 §二 `B-0901_382事件驱动信号机制`。  　━━━　🟢 **⑴ 事件驱动开班：建造与接线均已完成（2026-09-01；`OP-0901-C` 建造 ＋ `OP-0901-A` 接线）——判据→证据→处置** 　**判据**：opener 定「必须真触发一次并完成一次拆件才算通，且须报走了哪条路」。 　**证据·走哪条路**：CC 实测坐实 **Python 进程无法直接触发 Claude 侧 scheduled task** —— 该 prompt 跑在仓库外的 `C:\Users\Paul Shao\Claude\Scheduled\` store，worktree 结构性够不着（与 `draft_gap_check.py` #245 文首已记录的边界同源）；`mcp__scheduled-tasks__list_scheduled_tasks` 从 CC worktree 只能看到另一个互不相干的任务库（`.claude\scheduled-tasks\`，仅 2 个一次性任务）。⇒ **改走章程原定退路：信号文件**。 　**证据·建造**（commit `55855ea`，rebase 后 `d0c157b`，已 ff 合入 master 并 push）：新增 `aibot_service/patrol_signal.py`（fail-open ＋ 累积列表 ＋ 按 checkpoint 消费，**防止扫描期间新到的信号被整份清空**）／`aibot_service/repo_paths.py`／`scripts/check_patrol_signal.py`（轻量探测 CLI）；`followup_readme_bridge.mark_reply_arrived` 的 `ACTION_MARKED` 分支**在标第九态的同一把编辑锁内**落信号。单测新增 51 条，服务全量 **734 passed / 1 skipped**，零回归；已用真实 `mark_reply_arrived` ＋ 真实 `check_patrol_signal.py`（subprocess）在**隔离 repo_root** 下完成一次端到端验活（未触碰任何生产文件）。 　**证据·接线（本轮 Cowork 做，CC 结构性做不到）**：⑴ 章程 `huijian-chaijian-patrol` prompt 已加 **§〇ter「事件驱动开班判据」**（先跑探测；`[NO-SIGNAL]` 即空巡结束、§一~§四 全不跑；`[SIGNAL]` 走全套并在**报告登记 §二 之后**才 `--clear --before <CHECKPOINT>` 消费；🔴 探测脚本本身报错一律当 `[SIGNAL]` 处理＝fail-open）；§四.2 自检项与 §六 聊天回复同步加了 〇ter 结果一行。⑵ **cron 由 `0 9,13 * * 1-5` 改为 `*/10 * * * *`**（分钟级轻量轮询）。🔴 **该 SKILL.md 在 Cowork 会话里是 read-only，只能经 MCP `update_scheduled_task` 全量替换 prompt 完成，不能直接编辑文件**——此边界一并记此。 　**处置**：⑴ 建造与接线均已落地。🔴 **但「真实生产环境触发一次」尚未发生**——`enabled` 当前为 `false`（Shao Peishen 2026-09-01 主动下线），**开启属他的决定，未代办**；且现有三封在途信（`采购部#21`／`财务部#16`／`质量部#11`）均未回件，无真实信号可等。⇒ **本 ⑴ 记「建造+接线完成、待生产验活」，不记 done**，同 `#82`「探针通了 ≠ 机制通了」纪律。 🔴 **章程里「工作日双班」那句描述性文字刻意未摘**（章程第 8 行：摘除由总线在验活无误后执行），已在 §〇ter 末尾写明现状与摘除条件。 　**⑵（一.3/一.4 看门狗下放 sweep）本轮未做**，本行仍 `[S:open]`。 　━━━　🔴 **⑴ 事件驱动链路实为死链 —— 根因＝常驻服务进程未重启，新代码从未被加载（2026-09-02 01:0x Cowork `OP-0901-A` 自查，非他人报出）**　**证据（审计时间线，全部本地时刻）**：① `d0c157b` 合入 master ＝ **09-01 09:58**；② 常驻服务 `ZhuopinAibotDevListener` 的 python 进程实测**启动于 09-01 08:14:07**（早于合入 1h44m）；③ 陈忱 `质量部#11` 回件当日两次真实到达，审计有 **`followup_readme_bridge_marked` 09-01 15:02:00** 与 **16:54:57**（桥一确被调用、确实打了标），**但 `reports/` 下零 patrol signal 文件、`check_patrol_signal.py` 至今恒返回 `[NO-SIGNAL]`**；④ 服务 `LastRunTime` ＝ **09-01 23:38:47**（当日唯一一次重启，在那两次打标之后）。⇒ **15:02／16:54 那两次跑的是 08:14 载入内存的旧代码，里面没有 `raise_signal`。** 🔴 **worktree 侧代码是全的**（`ops/wecom-service-home` 有 `patrol_signal.py`、`followup_readme_bridge.py` 含 `raise_signal` 5 处，仅落后 6 个文档提交）——**所以这次不是代码没到，是代码到了、进程没重启**。 🔑 **判据：这是「合入 master ≠ 在跑」的第三种形态，前两种都不足以覆盖它** —— 形态一＝worktree 落后 master（`tasks 8.2` 原版，08-19 记 246 提交）；形态二＝worktree 已同步（本日上午更正）；**形态三＝代码与 worktree 都对，但常驻进程仍持有旧模块**。⇒ **凡改动常驻服务加载的模块，「合入」之后必须补一步「重启并确认进程启动时刻晚于合入时刻」，否则改动对生产零效力且不产生任何错误信号。** 🔴 **本次验活不完整是本 session 的责任，如实记此**：上线时只跑了 `check_patrol_signal.py` 得 `[NO-SIGNAL]` 就判"空巡验活通过"，**而那条路径根本不经过桥一**——验的是消费端，产生端（`mark_reply_arrived` → `raise_signal`）一次都没验。**⇒ 验活判据修正：事件驱动类改动必须验产生端，且必须用一次真实（或等价重放的）触发，不得以消费端的正常返回代替。** 　**当前状态**：23:38:47 重启后新代码应已加载，**但 23:38 之后无新回件，故仍未被证实**；`#450`／`#451` 已由 `OP-0901-E` 人工拆件（事件驱动未参与）。⇒ **本 ⑴ 由「建造+接线完成、待生产验活」降为「链路已建成但生产从未真实触发过一次」**，下一次真实回件即验活点，届时须核三件：桥一有无 `followup_readme_bridge_marked`／有无 patrol signal 落盘／巡逻那一班是否走全套而非空巡。 　━━━　🔧 **cron 由 `*/10 * * * *` 改为 `0 8-20 * * 1-5`（工作日 8:00–20:00 每整点）—— Shao Peishen 2026-09-02 亲自在巡逻任务里直接改，理由原话「回件没有那么多，拆件太频繁」** 　**判断成立**：真实回件密度约 0–2 封/工作日，每 10 分钟探测（7×24 ＝ 144 次/天）对该密度确属过度；改后 13 次/工作日。**代价＝及时性上限由 10 分钟放宽到 1 小时**，但相对本项改造前的「工作日双班、最长等半个班次」仍是大幅改善，判为合理取舍。 　🔴 **本次同时暴露一个真缺口，并入本行（不新立，机制类可动 WIP 已 24/22 超限）：Claude 侧 scheduled task 的配置改动没有任何留痕机制。** **实证**：该 cron 改动经 MCP `update_scheduled_task` 完成，**git 无提交、队列无登记、仓库内零痕迹**；2026-09-02 08:0x 本班开工三查时靠 `list_scheduled_tasks` 与自己昨日的登记逐字比对才发现「值和我昨天设的不一样」，**若当时没有对照，这个改动会永久静默**。⇒ **同族＝「合入 master ≠ 在跑」的孪生面：那条是「仓库里改了、跑的没变」，本条是「跑的改了、仓库里没有」——两者都表现为 git 与生产行为脱节，且都不产生错误信号。** 　**⇒ 判据（人守，待机制化）**：**凡改动 Claude 侧 scheduled task 的 `cronExpression`／`prompt`／`enabled` 三者之一，必须在对应队列行登记「改了什么、从什么改成什么、为什么」**——它们是生产行为的一部分，却不在版本控制内。⚠️ 本次改动方是 Shao Peishen 本人（无需追责），登记本条是为**下一次由 session 做出的同类改动**立规矩。**本行昨日那次 cron 改动（`0 9,13 * * 1-5` → `*/10`）已登记，可作正例参照。**  　━━━　🟢 **⑴ 产生端首次真实触发成功 —— 死链已解（2026-09-02 11:32，Cowork `OP-0902-A` 实测）** 　**触发源**：陈承 `IT部#11` 回件到达（归档件 `IT-2023458-回复-2026-09-02-文本反馈-dfb7ba66944d439b0c05d0ee687051ad.md`）。 　**三条判据逐条（本行昨日写死的那三条）**：**① 桥一打标 ✅** —— 审计 `followup_readme_bridge_marked` **11:32:28**（完整流水线 `archived 11:32:25 → queue_appended 11:32:28 → followup_readme_bridge_marked 11:32:28 → queue_sync_pushed → group_notified → inbound_forwarded_to_paul`）；**② 信号落盘 ✅** —— `reports/patrol_signal.json` **220 B、11:32:28 落盘，与打标同一秒**；`check_patrol_signal.py` 实测返回 `[SIGNAL] 待处理 1 条，最早/最新 2026-09-02T03:32:28Z`，条目 `IT部#11　IT-2023458-回复-…md`，并给出 `CHECKPOINT=2026-09-02T03:32:28Z`；**③ 巡逻接住 ⏳ 待验** —— 巡逻 `lastRunAt` ＝ `2026-09-02T03:04:51Z`（11:04 本地）**跑在信号落盘之前**，`nextRunAt` ＝ `04:04:16Z`（12:04 本地），**信号正躺着等下一班**。 　🔑 **与昨日两次的决定性对照**：09-01 15:02:00 与 16:54:57 两次同样有 `followup_readme_bridge_marked`，**但零信号落盘** —— 那两次进程跑的是 09-01 08:14 载入的旧代码；服务 09-01 23:38:47 重启后，本次**打标与落信号在同一秒完成**。⇒ **「合入 master ≠ 在跑」第三形态（进程未重启）就此闭合，且闭合方式是「重启后拿真实触发验证」而非推断。** 　⚠️ **本条只证产生端，端到端未完** —— 判据 ③ 须待 12:04 那班真实走完全套（非空巡）、`IT部#11` 被拆件回灌、且信号**在报告登记 §二 之后**才被 `--clear` 消费，三者齐备方可判本 ⑴ 为 done。**⇒ 现阶段记「产生端已验、消费端待 12:04 自然触发」，不提前销号**（同本行既有纪律：只验一端不算验活）。 　📌 **顺带记一条运行特征**：本次信号 11:32 落盘、最早 12:04 才被处理，**最长等待 ≈ 1 小时** —— 这是 2026-09-02 他把 cron 由 `*/10` 改为 `0 8-20 * * 1-5` 的直接代价，**已知且可接受**（回件密度 0–2 封/工作日）。若日后回件密度上升，此处是第一个该回看的参数。  　━━━　🟢 **⑴ 事件驱动开班 —— 端到端验活完成，判 done（2026-09-02 12:04–12:14，`IT部#11` 陈承回件真实触发）** 　**四条判据逐条有证**：① **桥一打标** `followup_readme_bridge_marked` **11:32:28**；② **信号落盘** `reports/patrol_signal.json` 220 B、**同一秒**；③ **巡逻接住** —— 12:04:52 那班输出 `Signal confirmed. Row 453 is the target.`，12:14:10 产出 `拆件巡逻报告-2026-09-02.md`（3395 B），队列 `#453` 由 `[S:open] 待领` 销为 `✅ 已拆件`、README `IT部#11` 由 `📨 回件已到，待拆件` 转 `📥 已回件并回灌`、§四 `#152` 注册；④ **消费时机合规** —— 报告已登记而信号仍在待处理，报告 §二 自检末行明标 `[ ] 信号消费（release 后执行 --clear --before …）`，**即 §〇ter 规定的「报告登记之后才消费」被真实遵守**，非事后追认。 　🔑 **它守住了红线**：回件要求「改口径」，巡逻**没有自行改**，而是登 §四 `#152` 交人裁——原文「改口径的具体实施方向须 SP 明示（涉对外动作：知会姚祖怡），属新业务方向判断，拆件巡逻不得自行定夺」。**这正是 `#452` 泳道看护 D1 判据表要复制的行为样本。** 　⚠️ **本次验活过程中 Cowork（`OP-0902-A`）连续三次误判，如实记，因为三次同族**：⑴ 以 `reply_arrived` 全历史 0 断言「桥一从未触发」——**查错了 action 名**，真名是 `followup_readme_bridge_marked`（已触发 8 次）；⑵ 以 `Select-String` 首个命中位置起打印 12 行、未见 §〇ter 正文，遂断言「章程正文没有该节」——**首个命中在 frontmatter 的 description，真正的节在第 19 行**；⑶ 以「信号仍在 ＋ 报告未产出」断言「12:04 那班没接住」——**而那恰是「正在正确执行」的外观**，因为章程（Cowork 自己写的那条）规定信号必须在报告登记之后才消费。🔑 **三次共同点：把「我没看到」当成「它不存在」。** ⇒ **判据：凡断言某机制「没发生」，必须先列出该载体里实际存在的键名／节名／状态集合，再从中确认缺失，不得以一次定向查找的落空为据。** 　**⇒ 本 ⑴ 判 `done`。⑵（一.3/一.4 看门狗下放 sweep）仍未做，本行整体不销号。** 　━━━　🟢 **⑵ 待发信盘点＋交叉红标已下放 sweep 第 10 类常驻告警（2026-09-02，CC `OP-0902-D`，独立 worktree `unclosed-output-handoff-scan-2b7b85`，分支 `claude/readme-digest-queue-sweep-1f6fa4`）——判据→证据→处置** 　**判据**：opener 定"digest 输出体量与耗时报压缩比；sweep 侧盘点跑一次，三态计数与现行巡逻 §一.3 输出逐字对齐（12/15/12 为今日基线），不对齐不算完"。 　**建造**（两处新增，均已测试覆盖）：⑴ `0-学习与工具/工具-跟进信README查询.py --digest [--json]`——结构性扫描「现有跟进信清单」主表（不读全文其余 200 KB 散文），状态列按 `followup_gate` 权威前缀词表匹配取值（命中即取**完整前缀本身**，不做任意宽度截断，免重演本行 `#439` 追记的同族截断漏检缺陷），实测 62 行、8,995 B、0.22s（压缩比 23.4×——低于队列 digest 的 800× 属预期取舍：README 状态列语义比队列任务列复杂，为保留前缀后的日期/来源上下文特意不做狠截断）；⑵ `工具-落库sweep.py::_check_followup_pending_inventory`（第 10 类常驻告警）子进程调用 ⑴ 取 JSON（sweep 刻意不在自身进程 import `aibot_service`/`zhuopin_platform`，同 `#192-A`/`#219` 既有先例）、按 `not_yet_sent_prefix` 三态计数、扫两份队列 §一 判交叉红标（同一行内共现"暂缓/压着"字样与信编号/裸姓名，按 `followup_gate` 已拆好的 `name` 字段匹配，不只匹配"部门 · 姓名"完整格式——单测曾就此抓到一次真实漏检，已修正），复用既有 `_track_and_alert_standing_state` 出现→告警／消失→解除骨架，每轮回显、零命中不省略，只读不改 README、不发跟进信、不碰 `.51`。新增单测 25 条（digest 工具 11 条 ＋ sweep 接线 14 条）。**两路并跑对照过程中另抓到两处缺陷，均已修正**：① sweep 侧既有护栏测试（锁"第 7 类判据只读、不得起子进程"）AST 扫描切片终点原写到 `main()` 而非到下一检查函数，被本次新增的合法只读子进程调用（转发给同目录另一纯只读脚本，不碰 git/pip）意外命中——已收窄切片终点到 `_check_hooks_heartbeat`，精确复刻该测试原意（只锁第 7 类自己的代码），非放宽判据；② 全量回归实测 19 个既有 CLI 级用例因本类新增而转红（`SweepTestBase` 共享测试夹具的临时仓库缺 `工具-跟进信README查询.py`，判据如实报"不可用"并推一条告警，把"本轮恰好 1 次 webhook"类断言全部染红）——与 `#435`（第 4 类新增全局记忆巡检时 17 个用例同源转红）、本文件头部"第 7 类"注释所记为**同一形态第三次复发**，已按既有解法（夹具补一份恒零行合法 JSON 桩）修正，全量回归复跑仅余 1 项失败（`SyncReorderTests::test_multiple_batches_in_one_round_push_exactly_once`，编辑锁竞用导致本轮 push 0 次）——**已用干净 HEAD 版本单独复现确认该失败与本次改动无关**（改动前既已存在，非本次引入），不在本次处置范围。 　**证据（🔴 三态计数与给定基线不对齐，如实登记，非猜测）**：本机对**当前**主表结构化解析（62 行，`followup_gate` 权威判据，零未识别前缀）得 `⏳ 待你审×0／🆕 待发×0／⏸ 暂缓×0`——**全部 62 行现状态均为 `closed`（41 行）或已推送待回（21 行）**，与 opener 给定基线 `12/15/12` 不对齐。**排查两条独立证据，均指向"基线非今日真值"而非本工具算错**：① `拆件巡逻报告-2026-08-28.md`（该班载体为逐行结构化解析、非全文关键词计数，方法论与本工具同源）实测记录 `⏳12／🆕14／⏸12`——与给定基线仅中间一位之差（14 vs 15），很可能是同一次历史测量的转述/誊写偏差；② 对**当前** README 全文做朴素整篇子串计数（不区分表格行与说明性正文），三短语恰好各得 `12/15/12`——与给定基线逐字相等，但这只是巧合式旁证：本工具已验证当前表格贡献为零，该计数实际全部来自"发送状态四态语义"说明节本身（该节用这些短语做定义示例；`2026-08-18` 报告已明文点破同族现象——"全文里 ⏳/🆕/⏸ 出现 6／13／10 次，但那些全部落在三态语义的说明性正文里，不是任何一行的状态"，巡逻自己的既有方法论一直在防这一类误读，本次并非巡逻方法论破功）。⇒ 更平实的解释：`08-28` 那批约 38 封在途信，经随后数日的批准/暂缓解除/`S4` 桥二自动转态等常规活动，已在 `09-02` 之前被处理完——本工具给出的 `0/0/0` 是当前真值，opener 给定的 `12/15/12` 是一个未经重新核实的旧数。 　**处置**：⑴ 机制已按正确判据（`followup_gate` 权威实现，未另起一套）完成建造、测试与真实仓库根验证（非 mock），不因基线对不上而搁置——它就是留给下次真有积压时替代 §一.3 人工读表的；⑵ 章程 `huijian-chaijian-patrol.SKILL.md` §一.3 正文摘除**未做**（本行明写"章程正文的摘除由总线执行，本 session 不擅自改"），建议改法原文见本次 session 收工报告，供总线核后执行；⑶ 本 ⑵ 暂不销号（machinery 已验活，仅"与给定基线数值对齐"这一项因基线本身非今日真值而无法字面满足，已如实登记，未强行凑数）。commit/push 见 §二 `B-0902_跟进信README-digest与sweep第10类告警`。 　━━━　📥 **转述登记三条（2026-09-02）** —— 🔴 **本段据同日另一条同号 `OP-0902-A` Cowork 线转述登记，非本线亲历**（本线＝R1 包勘误·状态分诊·`lane-watch` 首跑；**2026-09-02 一天内有两条会话同叫 `OP-0902-A`**，Shao Peishen 当日当面提醒过——这是 `#151`「OP 编号没有分配机制」的第三次实证，前两次＝`OP-0902-C` 撞号、§二 批次号两次撞号）。转述内容除 ⑴ 的 commit 外未经本线复核，已逐项标注核过与没核过。 　**⑴ `#382`⑵ 已由 `OP-0902-D` 交付并合入 master，但未销号，且理由正当** ✅ **本线已核实（非采信自述）**：`git merge-base --is-ancestor 54529ab master` 退出码 0（确在 master 上）；`git show --stat` 四文件行数与转述**逐项吻合**——`0-学习与工具/工具-跟进信README查询.py` **295** 行新增、`0-学习与工具/工具-落库sweep.py` **+171** 行（sweep 第 10 类常驻告警接线）、两份单测 229 ＋ 178，合计 871 insertions。 🔴 **未销号的理由是验收基线本身错了，不是它没做完**：原基线「三态 12/15/12」数的是 README「发送状态四态语义」**说明节里的定义示例**，**不是任何一行的实际状态**；当前真值 **0/0/0**（08-28 那批约 38 封已在 09-02 前处理完）。**它没凑数，如实登记了。** ⇒ **须据此重定验收判据，不得按旧基线判它失败。** 🔑 **判据（本条最值钱的一句）**：**验收基线本身也要先验一次「它数的是不是真身」** —— 数到说明节的示例而非数据行，**不产生任何信号**，同族＝本日另两起（手写截 §一 得 47 行／`grep 加 head` 截断得零调用）。 ⚠️ **未闭合**：章程 §一.3 的摘除**没做**，守住了「章程正文由总线改」的边界；**改法原文在其收工报告里**，待总线取用。 　**⑵ 待补 ⑶「定期盘点通用判据」**（转述方因锁冲突未能写入，本线代登，**未复核**）：**判别式一句——这个状态的最后一次写入，是不是我方自己做的？** 是 ⇒ 该留派生值；否（外部事件／他人 worktree／真实偏差）⇒ 扫描合理。 **六处全流程核验结论**：①待发信三态 ②可 Open 池 `#312` ③起草缺口检测 ⑤文档台账 ＝ **同病，该改**；④未闭合产出扫描 ⑥队列对账审计 ＝ **真扫描，不动**。 🔴 **派生值必须带真身内容哈希**：一致直接用、不一致**自动降级全扫重建** —— **它是缓存，不是第二份真身**（本项目已因「第二份真身」吃过多次亏：编号/状态两份副本互相印证着一起错 6 次）。 　**⑶ 待补 ⑴bis「真事件驱动」**（Shao Peishen 2026-09-02 已同意：**先做最小验证，再走 design**；本线**未复核**转述、但下条有旁证）：🔴 **`OP-0902-C` 那条结论被读宽了一档** —— 原话是「Python 无法触发 Claude 侧 scheduled task」，**真结论只是「够不着那个 task」，不等于「无法起 Claude 会话」**。 **反证就在仓库里**：`0-学习与工具/工具-opener批处理执行v2.ps1` 第 99-103 行就是从脚本起 `claude -p`，清池与看护一直在用。 ⇒ **桥落信号后可直接 subprocess 调它起无头 CC 拆件，轮询可彻底消失、而不只是变便宜。** **三个真问题留 design**：章程搬家（仓库外 → 仓库内）／并发守卫（抢同一把编辑锁）／`scheduled task` **保留为兜底、不拆梯子**。 🔴 **他要求先插一步最小验证**：手工调一次批处理脚本，确认无头 CC 真能完成一次**拆件**，再走 design。 🔑 **判据**：**「够不着 X」≠「做不到 X 要达成的那件事」** —— 结论被读宽一档，就会把一条本来可行的路径整个关掉。 　🔎 **本线可提供一条旁证（如实标明不能替代 ⑶ 的最小验证）**：本线 2026-09-02 正在跑 `lane-watch-mode` 首跑批，**手工调的正是同一个 `工具-opener批处理执行v2.ps1` 起无头 CC**；它验的是「该脚本能否起无头 CC 并跑完一段队列任务」，**对象是队列任务、不是拆件**，故**只是该链路可用性的旁证，不构成 ⑶ 所要的最小验证**。
