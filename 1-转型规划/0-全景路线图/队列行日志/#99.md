---
title: "队列行日志 #99（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #99 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #99 · 外置前整格原文（2026-09-04 外置，9 段，md5:b3089799）

✅ 两项已拍板（Shao Peishen 2026-08-25 答「全按建议」）：⑴(b) 覆盖率护栏改判据形态——有显式理由的 skip 不计入、无理由一条即红 ⑵(a) 授权一次做完——先为 2026-07-15 那条立行承接未闭合项、再迁、同步删该文件第 51 行旧禁令；🔴 不许半迁。执行承接＝§一 #398（已同批追记）。本行销号（⑶⑷ 已按行内自陈处置）。━━━ 🔴 **本行原始成因已自行消解，按 2026-08-25 实测全文改写；原判据作废，勿再引用（CC-A28，队列 #398⑶ 归属核对）** ━━━ **原文记的是**：CI 硬门禁 `openspec-validate` 在 master 上长期红，`openspec validate --all --strict` ＝ 96 passed / 3 failed，三个在途包 `editlock-mutex-stale-cleanup-resilience`／`queue-dual-file-split`／`sc2-weekly-report-mvp` 全报 `No deltas found`。 ━━━ **2026-08-25 实测**：`openspec validate --all --strict` ＝ **113 passed / 0 failed**，三个包**均已不报错**；`gh run view 32796970988`（master 最新 run）里 **`openspec-validate` job 是绿的**。⇒ **本行点名的那三个包与现况对不上，原始欠账已在此期间被别的动作顺带消化，不必再按原文去补 `.openspec.yaml` 或 archive。** ━━━ 🔑 **但本行真正的分量从来不在那三个包**——原文自己写着：「一道无逃生阀的硬门禁红了，却没有任何人因此停下来」。**这一点不仅仍然成立，而且更清楚了**：红的 job 换了人（现在是 `凭据扫描`／`CLAUDE.md 进度段 lint`／`CI 覆盖率下界护栏` 三条），**而整体 run 依旧长期 failure、依旧没人因此停下来**——**连「红的是哪几条」都换了一轮，也没有产生任何信号**。故本行不销，改为承接下面两项待拍板。 ━━━ **⑴ 请拍板：`CI 覆盖率下界护栏` 的基线怎么复核（口径变更，CC 不自决）** —— 实测 `跳过 59 > 基线 46`，但同一次 `passed=2868 ≫ 下界 1698`、`腿数 15 ≥ 下界 13`；**59 条 skip 逐条都有显式理由**（真实样本 gitignore／`*_RUN_REAL=1` 环境闸），**无一条是「测试悄悄不跑了」** ⇒ 是合理增长，不是静默失效。**三选一**：**(a)** 只把 `BASELINE_SKIPPED_MAX` 由 46 提到当前实测 59（最小改动，但**下次长大还会再红一次**，等于把同一个决定推给下一个人）；**(b)** 改判据形态——把「skip 绝对上限」换成「skip 占比上限」或「按 skip 理由分类：有显式理由的不计入、无理由的一条即红」（**治本，直接对上「绝对数下界在持续长大的仓库上必然过期」这个根因**，CC 建议此项）；**(c)** 维持 46 不动，逼每次新增 skip 都来一次人工复核（最严，但会让这道门禁长期红着，等于回到本行开头那句话）。 ━━━ **⑵ 请拍板：`CLAUDE.md 进度段 lint` 那 5 处欠账怎么动** —— 全在 `5-平台底座/wecom-aibot-service/CLAUDE.md`。**J3 两条超长**（第 6 条 2,068 字符、第 7 条 2,216 字符 > 1,200）本身好办：它们已点名队列 #387/#380 与 #355，正文细节下沉到那两行、顶部只留结论与指针即可。**卡住的是 J2**：顶部 8 条超上限 6，须迁最早两条 `2026-07-15`／`2026-08-06`，而 **J1 判定这两条「无承接载体且含未闭合措辞」（分别命中「暂不」与「如实登记／未闭合」），按判据必须先立队列行才能迁**；**更要紧的是该文件第 51 行自带禁令**：「⚠️ `2026-07-15` 与 `2026-08-13` 两行不得随后顺手迁走」——**lint 要迁的恰好包含 `2026-07-15`**。⇒ 动它须同时**为那条未闭合项取号立行 ＋ 推翻文件内既有禁令 ＋ 改 aibot 线自己的 CLAUDE.md**，三件都不是 CC 无人在场时该自决的。**二选一**：**(a)** 授权按「先为 `2026-07-15` 立一条 §一 行承接未闭合项，再迁，并同步删除第 51 行禁令」执行（CC 可一次做完，建议此项）；**(b)** 交 aibot 线自己收，本行只登记不派。⚠️ **无论选哪个，都不要「只迁 J3、留着 J2」**——lint 作者在工具头部明确告诫：只做超限报错会把它逼成「为了过 lint 而丢掉未闭合项，比现状更糟」，**半迁比不迁更坏**。 ━━━ **⑶ 已按原文建议落地（无需答复）**：值周巡检加一问 —— **本周 CI 各 job 是否全绿；有红的，红了多久**。🔴 **加这一问的判据要写死成「逐 job」，不能写「整体 run 是否绿」**——本行这次改写恰恰证明：整体 run 一直红，而红的**是哪几条**已经悄悄换了一轮，看整体永远看不出来。 ━━━ **⑷ CI 失败告警不在本行建**：`.github/workflows/ci.yml` 头部已记「失败步骤挂企微 webhook 告警」方向对但会新增第 9 类「只报出现不报解除」的告警，**须等 #308 子项 D1 告警解除通用骨架落地后再建**。遵此不建。
