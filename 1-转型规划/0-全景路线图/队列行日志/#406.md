---
title: "队列行日志 #406（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #406 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #406 · 外置前整格原文（2026-09-04 外置，4 段）

[S:done][D:机] 🆕 **2026-08-25 立行（Cowork 环境总线）**。**本行不阻塞 #170 迁移，反之亦然**；但若先迁移后修此项，重装动作可与迁移 S3-12 合并、只做一次。⚠️ **判定依据只认 `importlib.util.find_spec` 的回读**，不认 `pip list`——`pip list` 对这 10 个包全部显示已安装，正是它让这个缺陷藏了这么久（同族＝「工具静默回退：太正常的结果先怀疑它根本没读到我以为的那个对象」）。 　━━━　🔴 **2026-08-25 T2 未开跑、本行未销号（CC OP-0825-DRILL，#407 派单件 T2）**：独占自检未通过（`Get-Process claude` 18 个、17 个早于本会话），按硬约束 ④ 停下回报。 ⚠️ **但演练顺带把本行的分母查清了，且比原估大得多**：生产 `.claude/worktrees/` **磁盘 53 个目录，git 只认 26 个**——**25 个孤儿幽灵目录**（`.git` 文件已坏、git 完全不认识，但目录还在、Python 照样 import 得到；含本行已点名的 `fi2-tax-export-excel-d3938b`／`unified-portal-design-8a2ce3`） ＋ **2 个「静默隐身」新形态**（`fi2-webhook-var-check-bc19fa`／`platform-bootstrap-apply`：`.git/worktrees/<n>/gitdir` **文件缺失** ⇒ `git worktree list` **根本不列出它们**，可目录健在、`.git` 回指还是对的）。🔴 **⇒ 任何以 `git worktree list` 为分母的清点都偏小**；`git worktree repair <显式路径>` 能把这 2 个修回来，而裸 `repair` 与 `worktree list` 都做不到。 **⇒ 本行 ③「清理断链 worktree 目录」的对象由 3 个扩大为 25＋2 个**，须逐个核对再清（同 #170 判据：宁可不清、不可错清），**本次未动任何一个**，已列为待 Shao Peishen 定夺。 🔴 **另查出两个 T2 危险窗口、派单件未写到**：① 企微机器人服务 `PID 68044` 自 08-25 08:21 常驻，`import zhuopin_platform`；② `ZhuopinCommitSweep` **每小时 :36 触发**，而 `工具-落库sweep.py:386` 亦 `from zhuopin_platform.shared_tools import queue_table` ⇒ **T2 的 uninstall 窗口一旦跨过 :36，当轮 sweep 会在 import 处崩掉、§二 批次当轮不落库**。⇒ **T2 前置自检须补两条**：停 `ZhuopinAibotDevListener` ＋ 禁 `ZhuopinCommitSweep`。详见 `1-转型规划/0-全景路线图/OneDrive迁出回滚演练结果-队列407-2026-08-25.md` §二／§六。 　━━━　🔴 **2026-08-25 Shao Peishen 答 3「按你建议稳妥推进」**：⇒ **那 25 个孤儿幽灵目录 ＋ 2 个静默隐身目录，本阶段一个都不动**。判据沿用 #170「逐个核对再清、宁可不清不可错清」：**每个目录先核有无未提交改动、有无被任何 editable 安装或运行中服务引用，确认无主方可处置；任一判不准即整批留步回报。****次序**：先做 T2 的 editable 重装（把 import 拉回主工作树），**再**议孤儿目录清理——反过来做会在包还指着那些目录时把目录删掉。**清理本身不并入 T2，另行派发。** 　━━━　🔴 **2026-08-25 T2 执行完成、本行销号（CC OP-0825-T2，#407 派单件 T2 段）**：**①②④ 三项已做，③ 按定夺不做**。 **① 重装**：9 个包逐包 `pip uninstall` ＋ `pip install -e <主工作树绝对路径>` 全部成功。🔴 **一处偏离派单件字面次序，如实登记**——派单件写「先卸全部 10 个、再逐个装」，实际执行为**逐包 uninstall 紧跟 install、串行走完 9 个**；理由两条：⑴ 全失败窗口由 ~7 分钟压到单包 ~40 秒，而这个窗口正是整个独占自检存在的唯一理由；⑵ 🔴 **`zhuopin_platform` 是其余 8 个包的依赖，若按字面先卸全部 10 个，随后安装任一场景包时 pip 会发现它缺失并转向 PyPI 拉取——那里没有这个私有包，安装会失败**。**动作集合与派单件完全相同**（9 卸 ＋ 9 装，一个不多一个不少），只是交错执行。一律传绝对路径、禁裸包名；依赖全本地已满足未联网，单包实测 42.1 s；**先拿已 NOT_FOUND 的 `fi2` 试装验证构建链路，再动那 8 个正在工作的包**。 **② 回读**：`find_spec` 由 **MAIN 3／WORKTREE 5／NOT_FOUND 2** 变为 **MAIN 9／NOT_FOUND 1**；交叉核验 `grep -l worktrees` 在 `.pth` **0 命中**、在 `_finder.py` **仅剩 portal 一个**；**全程不看 `pip list`**。**回归 1,928 passed／14 skipped／0 failed**——七处场景（SC1 53／SC7 41／SC8 502／O2 20／QD-B 181／FI1 33／FI2 179）**加跑底座 380 与机器人服务 539**，加跑理由＝底座被全部场景依赖、机器人是 24×7 告警链路，**只测七处等于放过了改动面最大的两个**。 **④ 评估（只出结论未实现）**：可行，**建议做成落库 sweep 第 5 类常驻告警、纯读 `__editable___*_finder.py` 的 MAPPING 文本、零 import**，判据两条——目标路径含 `.claude\worktrees` ／ 目标路径不存在。🔑 **「零 import」是关键设计而非省事：sweep 自己 `import zhuopin_platform`，判据若靠 import 被检包，则恰好在底座包坏掉时检查器自己也起不来 ⇒ 检查器与被检对象共享失败模式，等于没有检查器。**已另立 §一 #410，不并入本行。 🔴 **唯一未修 ＝ `portal_gateway`，且原因不是操作失败、是前提不存在**：其源码只在**未并入 master** 的分支 `claude/unified-portal-design-8a2ce3`（commit `648a0e5`），主工作树无该目录、`git ls-files` 0 命中、`merge-base --is-ancestor` 判未并入 ⇒ **根本无法「从主工作树重装」**。🔑 **这是幽灵 import 的第三种形态，本行原表述未覆盖**——形态一＝指向断链 worktree 里的旧副本（跑旧代码且不报错，本次已修 5 个）；形态二＝指向已消失的路径（NOT_FOUND，会报错反而安全，`fi2` 属此已修）；**形态三＝指向一份从未并入主线的代码——不是路径坏了，是这份代码在主线上根本不存在；重装解决不了它，能修的前提是先决定这个门户网关要不要并进 master**（业务决策，非环境修复）。⇒ **本次连 `pip uninstall` 都没做**（现状 NOT_FOUND 已是「坏得看得见」的安全态，卸不卸载对功能无区别，而卸载会改变生产状态），已登记 §四 #121 待定夺。 **③ 那 25 个孤儿 ＋ 2 个静默隐身目录一个都没动**（按 Shao Peishen「稳妥推进」）；✅ **次序上这也正是对的**——先把 editable 拉回主工作树、再议目录清理，反过来做会在包还指着那些目录时把目录删掉；**本次执行后除 portal 外已无任何包指向它们，清理的前置条件由此具备**。 🔴 **收工遗留（未闭合）**：`Start-ScheduledTask ZhuopinAibotDevListener` ✅ 成功（PID 12700，`authenticated` 审计 `15:09:26.492Z` ＝ **23:09:26 本地**，本次启动后**恰好一条**）；但 **`Enable-ScheduledTask ZhuopinCommitSweep` ❌ 拒绝访问**，`schtasks /Change /ENABLE` 同样被拒 ⇒ **sweep 仍处 Disabled**。取证：该任务 `LogonType=S4U`、`RunLevel=Limited`、属主即 `Paul Shao`，**S4U 任务改定义需 `SeTcbPrivilege`，与属主是不是自己无关**——**与 T3 的 `Register-ScheduledTask` 被拒是同一堵墙的两个面**，而「**触发**已有任务」不需提权（上面 Listener 成功即此分界的证据）。**未尝试任何绕行**（不导出 XML 重注册、不改 principal）——改生产任务定义正是非原子操作，失败会留下「任务被注销掉又没建回来」。🔴 **它停摆是静默的**：§二 批次不落库 ⇒ 表现为「GitHub 上突然不再有新提交」且无告警，**含本次 T2 自己的批次**。已登记 §四 #120，须他在管理员窗口跑一行 `Enable-ScheduledTask -TaskName ZhuopinCommitSweep`。 ⚠️ **另记一个判据自身的缺陷**：派单件那条 `Get-Process claude` 独占自检**已作废**——它数的是 Claude Desktop 的 Electron 进程组（恒 14+、永不归零），真会话须按可执行路径筛 `claude.exe` 且命令行含 `claude-code`；**但该进程每轮新起、答完即退 ⇒ 只能测「此刻有无会话在执行」，测不出「有无窗口可能醒来」，这一类判据没有纯机器解**。本次实际自检收敛为两条机器可判项（aibot python 进程已退出 ✅ ／ sweep 为 Disabled ✅），并如实记下一个**自匹配假阳性**：查「opener 批处理 runner 是否为空」时命中一条，追下去是**查询命令自己的命令行**含该字面量——与首轮 pytest 自匹配是同一个坑的第二次发作，**凡用命令行文本匹配找进程都会匹配到自己**。 结果件 §九＝`1-转型规划/0-全景路线图/OneDrive迁出回滚演练结果-队列407-2026-08-25.md`。

---

# §一 #406 · 外置前整格原文（2026-09-04 外置，3 段，md5:b4c2930f）

[S:done][D:机] 🆕 **2026-08-25 立行（Cowork 环境总线）**。**本行不阻塞 #170 迁移，反之亦然**；但若先迁移后修此项，重装动作可与迁移 S3-12 合并、只做一次。⚠️ **判定依据只认 `importlib.util.find_spec` 的回读**，不认 `pip list`——`pip list` 对这 10 个包全部显示已安装，正是它让这个缺陷藏了这么久（同族＝「工具静默回退：太正常的结果先怀疑它根本没读到我以为的那个对象」）。 ━━━ 🔴 **2026-08-25 T2 执行完成、本行销号（CC OP-0825-T2，#407 派单件 T2 段）**：**①②④ 三项已做，③ 按定夺不做**。 **① 重装**：9 个包逐包 `pip uninstall` ＋ `pip install -e <主工作树绝对路径>` 全部成功。🔴 **一处偏离派单件字面次序，如实登记**——派单件写「先卸全部 10 个、再逐个装」，实际执行为**逐包 uninstall 紧跟 install、串行走完 9 个**；理由两条：⑴ 全失败窗口由 ~7 分钟压到单包 ~40 秒，而这个窗口正是整个独占自检存在的唯一理由；⑵ 🔴 **`zhuopin_platform` 是其余 8 个包的依赖，若按字面先卸全部 10 个，随后安装任一场景包时 pip 会发现它缺失并转向 PyPI 拉取——那里没有这个私有包，安装会失败**。**动作集合与派单件完全相同**（9 卸 ＋ 9 装，一个不多一个不少），只是交错执行。一律传绝对路径、禁裸包名；依赖全本地已满足未联网，单包实测 42.1 s；**先拿已 NOT_FOUND 的 `fi2` 试装验证构建链路，再动那 8 个正在工作的包**。 **② 回读**：`find_spec` 由 **MAIN 3／WORKTREE 5／NOT_FOUND 2** 变为 **MAIN 9／NOT_FOUND 1**；交叉核验 `grep -l worktrees` 在 `.pth` **0 命中**、在 `_finder.py` **仅剩 portal 一个**；**全程不看 `pip list`**。**回归 1,928 passed／14 skipped／0 failed**——七处场景（SC1 53／SC7 41／SC8 502／O2 20／QD-B 181／FI1 33／FI2 179）**加跑底座 380 与机器人服务 539**，加跑理由＝底座被全部场景依赖、机器人是 24×7 告警链路，**只测七处等于放过了改动面最大的两个**。 **④ 评估（只出结论未实现）**：可行，**建议做成落库 sweep 第 5 类常驻告警、纯读 `__editable___*_finder.py` 的 MAPPING 文本、零 import**，判据两条——目标路径含 `.claude\worktrees` ／ 目标路径不存在。🔑 **「零 import」是关键设计而非省事：sweep 自己 `import zhuopin_platform`，判据若靠 import 被检包，则恰好在底座包坏掉时检查器自己也起不来 ⇒ 检查器与被检对象共享失败模式，等于没有检查器。**已另立 §一 #410，不并入本行。 🔴 **唯一未修 ＝ `portal_gateway`，且原因不是操作失败、是前提不存在**：其源码只在**未并入 master** 的分支 `claude/unified-portal-design-8a2ce3`（commit `648a0e5`），主工作树无该目录、`git ls-files` 0 命中、`merge-base --is-ancestor` 判未并入 ⇒ **根本无法「从主工作树重装」**。🔑 **这是幽灵 import 的第三种形态，本行原表述未覆盖**——形态一＝指向断链 worktree 里的旧副本（跑旧代码且不报错，本次已修 5 个）；形态二＝指向已消失的路径（NOT_FOUND，会报错反而安全，`fi2` 属此已修）；**形态三＝指向一份从未并入主线的代码——不是路径坏了，是这份代码在主线上根本不存在；重装解决不了它，能修的前提是先决定这个门户网关要不要并进 master**（业务决策，非环境修复）。⇒ **本次连 `pip uninstall` 都没做**（现状 NOT_FOUND 已是「坏得看得见」的安全态，卸不卸载对功能无区别，而卸载会改变生产状态），已登记 §四 #121 待定夺。 **③ 那 25 个孤儿 ＋ 2 个静默隐身目录一个都没动**（按 Shao Peishen「稳妥推进」）；✅ **次序上这也正是对的**——先把 editable 拉回主工作树、再议目录清理，反过来做会在包还指着那些目录时把目录删掉；**本次执行后除 portal 外已无任何包指向它们，清理的前置条件由此具备**。 🔴 **收工遗留（未闭合）**：`Start-ScheduledTask ZhuopinAibotDevListener` ✅ 成功（PID 12700，`authenticated` 审计 `15:09:26.492Z` ＝ **23:09:26 本地**，本次启动后**恰好一条**）；但 **`Enable-ScheduledTask ZhuopinCommitSweep` ❌ 拒绝访问**，`schtasks /Change /ENABLE` 同样被拒 ⇒ **sweep 仍处 Disabled**。取证：该任务 `LogonType=S4U`、`RunLevel=Limited`、属主即 `Paul Shao`，**S4U 任务改定义需 `SeTcbPrivilege`，与属主是不是自己无关**——**与 T3 的 `Register-ScheduledTask` 被拒是同一堵墙的两个面**，而「**触发**已有任务」不需提权（上面 Listener 成功即此分界的证据）。**未尝试任何绕行**（不导出 XML 重注册、不改 principal）——改生产任务定义正是非原子操作，失败会留下「任务被注销掉又没建回来」。🔴 **它停摆是静默的**：§二 批次不落库 ⇒ 表现为「GitHub 上突然不再有新提交」且无告警，**含本次 T2 自己的批次**。已登记 §四 #120，须他在管理员窗口跑一行 `Enable-ScheduledTask -TaskName ZhuopinCommitSweep`。 ⚠️ **另记一个判据自身的缺陷**：派单件那条 `Get-Process claude` 独占自检**已作废**——它数的是 Claude Desktop 的 Electron 进程组（恒 14+、永不归零），真会话须按可执行路径筛 `claude.exe` 且命令行含 `claude-code`；**但该进程每轮新起、答完即退 ⇒ 只能测「此刻有无会话在执行」，测不出「有无窗口可能醒来」，这一类判据没有纯机器解**。本次实际自检收敛为两条机器可判项（aibot python 进程已退出 ✅ ／ sweep 为 Disabled ✅），并如实记下一个**自匹配假阳性**：查「opener 批处理 runner 是否为空」时命中一条，追下去是**查询命令自己的命令行**含该字面量——与首轮 pytest 自匹配是同一个坑的第二次发作，**凡用命令行文本匹配找进程都会匹配到自己**。 结果件 §九＝`1-转型规划/0-全景路线图/OneDrive迁出回滚演练结果-队列407-2026-08-25.md`。 ━━━ 📎 历史回写 2 段已于 2026-09-04 外置（K2 口径，原文原样 md5:31ad023d）见 `1-转型规划/0-全景路线图/队列行日志/#406.md`；本格只留首段＋末段
