---
title: "队列行日志 #507（§一）"
created: 2026-09-10
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #507 的状态列历史段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留 design 审结论首段＋apply 收口段＋本指针。
配套: 0-学习与工具/工具-落库sweep.py；0-学习与工具/test_工具-落库sweep.py；openspec/changes/archive/2026-09-10-sweep-manifest-coverage-selflock/
---

# §一 #507 · 外置件（2026-09-10 外置，CC `OP-0910-L`，泳道 `507-sweep-apply`）

## 一、外置时刻行内状态列全文（原文原样，未改写）

> 外置前该格 4537 字／8457 B（UTF-8），超 K2 4 KB 阈值；行长豁免由 `OP-0910-E` 立、到期 09-17，本次 apply 收口一并完成外置。

[S:open][D:机] ✅ **design 审已过**——Shao Peishen 2026-09-10 08:55（本地）答 `1a，2a，3a，4a，5a`（D1 换锚＝不脏且不在 HEAD 且未被 .gitignore 覆盖；D2 提交信息自陈装入范围；D3 三条 reject 全补；🔴 D4 阈值＝K=3；D5 靠新判据自动消化＋apply 现取核对残余；D6 为 #136 承继约束、design.md 明写不列为拍板项）。看护者 OP-0910-G 已跑 `resume`。**剩余＝apply 棒**（另起，openspec 包 `sweep-manifest-coverage-selflock`，分支 claude/op0910e-507-sweep-selflock @ 5f95d36），待总线派发；行长豁免沿用下文（到期 09-17）。——以下为泳道 OP-0910-E 收工时原文（前缀原为 [S:blocked]）—— 🟡 **停在 design 审，等 Shao Peishen 拍 D1–D5**（propose 已出件，CC `OP-0910-E` 2026-09-10；`[S:blocked]` 的阻塞源 ＝ **人的一次决策**，不是别的泳道，详见本格末尾几段）。 ━━━ **以下为立行原文，未改一字**： 🆕 **2026-09-08 立行**（Cowork 业务总线 `OP-0908-A` 收工清账时坐实）。**并入审核（协议〇.10）**：已扫 `sweep`／`覆盖校验`／`空壳`／`跳过` 四词，命中 `#136`（本防护的裁定行，是本行要改的对象而非可并入项）、`#500`（sweep 第 5 类脏检查放宽，**同族但不同题**：那条管「常驻执行体脏就停手」，本条管「清单路径不脏就整批跳过」），确无可并入。🔴 **本会话不修**（总线/看护会话不修工具，只登记＋派单）；眼下已用一个**不含高频件的干净批次**止血，十件产出已抢救登记。🔑 **本行的判据一句**：**防护本身是对的，错在它把「共享的高频文件」和「本批独有的产出」一视同仁地要求「必须脏」。**  ━━━ ✅ **2026-09-10 07:4x 阻塞解除（Cowork 业务总线 `OP-0910-A`）** —— 本行原写「🔴 与在跑的 `B-0908_四泳道机制根治` 泳道 `500-sweep-carrier` 同触碰同两个文件」，**该阻塞的两个前提都不成立**：⑴ 现取该看护件 frontmatter `status` ＝ **「待你在 CC 里起」** ⇒ **那批从未派出，不存在「在跑」**；⑵ `500-sweep-carrier` 的活**已做完并进 master**（§一 `#500` 本日据 `patch-id` 三重验证销号）⇒ 该分支不会再改 `工具-落库sweep.py`。**本行可入泳道，触碰区无竞争。** ⚠️ **但开工须先现取 master 版本**：`#500` 的「第5类脏检查放宽」已在 master（`git grep` 2 处命中），本行的覆盖校验自锁修复要**建在那之上**，不得照本行 2026-09-08 立行时的描述直接动手。 ━━━ ⏳ **openspec propose 出件完成，停在 design 审（🟡）**（CC `OP-0910-E`，2026-09-10，泳道 `507-sweep-selflock`，分支 `claude/op0910e-507-sweep-selflock`，commit `94ee4a0` 已 push；由看护批 `B-0910_五泳道全启动` 派出。本泳道按看护件硬口径「只 push 本泳道分支、不碰主仓、不 ff master」，且回写当刻编辑锁被 `CC OP-0910-B (#535 夹具日期)` 占用 ⇒ 本段为「队列回写待补」挂起稿，须总线回灌）。 ━━━ **变更包** ＝ `openspec/changes/sweep-manifest-coverage-selflock/`（`proposal.md`／`design.md`／`tasks.md`／`specs/sweep-manifest-coverage-guard/spec.md`，MODIFIED×2 ＋ ADDED×2；**零代码改动，design 审通过前不得 apply**）。 ━━━ 🔑 **本行立行时那句判据被实测改述了**：立行原文写「错在把共享的高频文件和本批独有的产出一视同仁」，而按该口径拟的候选 ⑴（共享高频件白名单）**实测只放行 760 条跳过记录中的 42 条 ＝ 5.5%**，111 轮的头号卡死批次 `B-0903_51` 压根不在其覆盖范围内（它的缺项是看护件与 `reports/lane-heartbeat/…md`）。**据现网全量实测改述为**：判据把「不脏」当成了「没到」——而「不脏」的绝大多数情形其实是「已经到了」。 ━━━ **现网实测（2026-09-10 现取，手段写明）**：解析 `reports/sweep-commit.log` 全量 ＝ **760 条整批跳过记录 / 48 个批次**，最长 `B-0903_51` **连续 111 轮**（sweep 每 27 分钟一轮 ⇒ ≈50 小时以上）；去重缺项路径 **113 条**，逐条跑 `git ls-tree -r --name-only HEAD` ＋ `git check-ignore -q --no-index --` 归类 ＝ **A 16 条（被 `.gitignore:50` 的 `**/reports/` 覆盖，物理上永远不可能脏）／ B 89 条（已在 HEAD，内容早已落库）／ C 8 条（真缺项形态）**。缺项频次 TOP2 ＝ `跨桌任务队列-业务场景.md`（600 次）与 `跨桌任务队列-机制环境.md`（454 次）。 ━━━ **拟改判据（design D1(a)）**：缺项 ＝ 形状合格 ＋ 不脏 **＋ 不在 HEAD ＋ 未被 `.gitignore` 覆盖**。实测放行 **648/760 ＝ 85.3%**、解锁 **42/48** 个批次；对**当前** §二 待处理 36 个批次直接调生产函数复算 ＝ 由「现行判据跳过 2 个」变「新判据跳过 0 个」。 ━━━ 🔴 **`#136` 原防护未被放松，这是实测不是推断**：用 `git ls-tree -r 6557047^` 复原事故当时的 HEAD，该批 8 条声明路径中 **6 条当时不在 HEAD** ⇒ 新判据下**仍判缺项、仍整批跳过**，`6557047` 型空壳提交仍被堵死。 ━━━ **期望产出对应**：① 修法定案 ＝ design D1–D6（**待他拍**）；② 单测三态 ＝ tasks §2.1／2.2／2.6（含非恒真自证）；③ 连续跳过 ≥K 轮升格 §四 ＝ spec ADDED 第二条 ＋ tasks §1.4（**范式复用既有 `_escalate_long_lived_orphans_to_section_four()`，不另起机制**；K 值 ＝ D4，属阈值项、按根 `CLAUDE.md` §5 **本项无默认**）；④ 回扫 ＝ design D5(a)「靠新判据自动消化 ＋ apply 阶段现取核对残余」，**不写一次性脚本**。 ━━━ **另两条实测发现，一并登记**：⑴ 形状判据现存两条误认已定位（`hooks.PreToolUse` ＝ JSON 键名；`C:\…\5-平台底座\.env` ＝ 仓库外 Windows 绝对路径，末尾 `\.env` 的反斜杠落进 `[^/.\s]` 而被现行正则放行），拟补三条 reject（design D3）；⑵ 🔴 **「无斜杠即拒」不能一刀切**——对现网两份队列 §二 全量跑形状判据（69 行 / 84 个整行反引号片段 / 42 个唯一片段）实测，该条会误拒 **`CLAUDE.md`**（仓库根下真实文件、确实以裸名出现在现存清单里），故第三条须写成「无斜杠 **且** 不是 HEAD 里的仓库根文件 ⇒ 拒」。 ━━━ **验证手段与回显**：`openspec validate sweep-manifest-coverage-selflock --strict` ＝ `is valid`；`openspec validate --all --strict` ＝ **188 passed / 0 failed**（本包新增 1 item，propose 前 187，**零新增失败**）。本包零代码改动，未跑 sweep 全量回归（留 tasks §3.2 apply 期做）。 ━━━ 🔴 **未做且不自行做的四件**：**① design 审（🟡，本泳道已 `pause` 落状态）**；② 任何代码改动（design 审前不得 apply）；③ ff 合入 master（🟡）；④ §二 批次登记（编辑锁被占，且本包文件尚在分支上）。 ━━━ ⚠️ **一条须总线定的次序**：在途包 `sweep-manifest-scoped-stage`（§一 `#479`）与本包 **同碰 `_process_normal_batch`**（前者改 `git add` pathspec、本包 ② 改提交信息正文），语义不冲突但文本同段 ⇒ **谁先 apply、谁后 rebase 须总线定**，本泳道不自行裁定（已写进 tasks §0.2）。  ━━━ **行长豁免：本行正处于 design 审在办态（🟡），apply 收口时状态列必然再改一次；此刻做 K2 外置会外置一份马上就要被覆盖的快照，且外置件须新建于主仓工作区、越出本泳道「不碰主仓」的硬口径。** 处置：**K2 外置并入本行 apply 收口时一并做**（届时由 apply 泳道或总线执行，外置件 `1-转型规划/0-全景路线图/队列行日志/#507.md`）。豁免作者 `OP-0910-E`，到期 **09-17**（超期未外置即视为违规，须补）。 ✅ **openspec 包已 ff 入 master**（零代码，apply 棒从 master 起即可）（Shao Peishen 2026-09-10 答 `1a`；看护者 OP-0910-G 10:0x rebase+`--ff-only`，master `6e04f00`→`c31b264` 已 push；前置＝集成树全量 `pytest 0-学习与工具` 4 failed/2136 passed，四红与纯 master 同名同红＝存量，本批引入 0；手段与回显见看护件 B-0910 §四bis「ff 入 master」节）。

## 二、apply 收口时的完整验证回显（行内只留摘要，此处留原样；`OP-0910-L` 2026-09-10 14:5x 本地）

✅ **apply 已出件，停在 ff 入 master（🟡）**——CC `OP-0910-L` 2026-09-10 14:5x（本地），泳道 `507-sweep-apply`，分支 `claude/op0910l-507-sweep-apply` @ `693028b`（代码）→ `c3dac6d`（archive）已 push；从 master `9be6ce2` 起，`#479` apply `22cbd15` 经 `git merge-base --is-ancestor` 验已在 master，本包建在其上、tasks §0.2 的次序问题自然消解。**期望产出逐项销账**：① 修法定案 ＝ design D1(a) 落地 `_manifest_coverage_gap(resolved, not_dirty, repo_index)`（`git ls-tree -r --name-only -z HEAD` 每份队列文件一趟缓存 ＋ `git check-ignore -q --no-index` 逐片段缓存；HEAD 匹配与脏路径解析共用新抽的 `_match_declared_fragment`，不另起一套）；② 单测三态 ＝ `test_gap_three_states_head_ignored_missing`；事故回归 `test_6557047_incident_input_is_still_caught`用该批真实 8 条路径，`git cat-file -e 6557047^:<path>` 逐条现取＝ **5 条当时不在 HEAD**（propose 原文写 6/8 少算了队列文件本身）⇒ 新判据仍判 5 条缺项、仍整批跳过，结论不变；非恒真自证 `test_bypassing_head_and_ignore_anchors_reinstates_gap`（`repo_index=None` 旁路两锚 ⇒ 同一输入重新判缺项）；③ 连续跳过升格 ＝ `MANIFEST_SKIP_ESCALATION_ROUNDS = 3`（D4 他答 4a）、状态 `reports/sweep-manifest-skip-state.json`、`_escalate_manifest_skips_to_section_four` 复用孤儿升格整套范式（`acquire --reserve 1 --section 四` → 位置式 `--cell` → 当日同批次去重 → 失败只留日志不改退出码），跳过日志降频为首轮一条＋每轮一行汇总；端到端 `ManifestSkipEscalationTests` 8 条（达阈值追行／未达只计数／当日去重／跨天再登／落库归零／降频／追行被拒退出码 0／dry-run 不写）全绿；④ 回扫 ＝ D5(a) 不写脚本，现取核对：主仓实机 `--dry-run` 新旧两码各跑一遍（14:2x）——§二 待处理 4 个批次，**旧码整批跳过 2**（`B-0910_537锚点数判据` 缺 4 件、`B-0910_H3_371载荷作废与501留档迁位` 缺 1 件，5 件 `git cat-file -e HEAD:` 全部已在 HEAD）**→ 新码跳过 0**，且回显自陈段「本批清单 6 条，本次实际装入 2 条」；propose 点名的 4 个历史卡死批次＋`B-0904_A13` 经 `grep -l` 核对全部已在 `跨桌任务队列-归档-202609.md`，不再回扫。D2 ＝ `_process_normal_batch(..., coverage_note)`，N<M 提交信息正文追加自陈、标题一字不改（端到端 `test_shared_file_already_in_head_does_not_block_batch` 断言 `%s` 不变、`%b` 含「已在 HEAD」；N==M 断言 `%b` 为空）。D3 ＝ 三条 reject（反斜杠／盘符／无斜杠且非 HEAD 根文件）＋ `CLAUDE.md` 反例单元与端到端各一条。D6 ＝ `ManifestCoverageFailOpenSourceTests` 一行未改、跑通（新增两次 git 子进程调用在 main() 同一 try 内）。**验证手段与回显**：`pytest -k ManifestCoverage` ＝ 12 单元＋1 源码断言＋5 既有端到端＋4 新端到端全绿；`openspec validate --all --strict` ＝ apply 前 188 / archive 后 187 passed、0 failed；日志重放（apply 期 13:2x，脚本同 propose §0.3）＝ 781 条／52 批次 → 放行 670（85.8%）、解锁 47/52，缺项 117 ＝ A17／B93／C7，定性未翻。全量 `test_工具-落库sweep.py` 本分支 vs 纯 master `7d90c59` 基线对照 ＝ {REGRESSION}。**§四 `#136`**：本日每周清扫 `B-0910_443` 已将其迁 `归档-202609.md`（历史不追改），改锚判据／6557047 回归证据登在本段、包内 `tasks.md` §0.3 与合入后的 `openspec/specs/sweep-manifest-coverage-guard/spec.md`。**已 archive**：`openspec/changes/archive/2026-09-10-sweep-manifest-coverage-selflock/`（archive ≠ 合入）。**未做且不自行做**：ff 入 master（🟡，看护者／sweep 收尾段串行做，本泳道按硬口径只 push 本分支）；ff 前生产 sweep 仍是旧判据，本批 §二 登记按 #136 旧行为只列主仓脏件（tasks §5.3.1）。本段外置件 K2 已随本次收口完成（行长豁免 09-17 前）。

## 三、外置后行内保留的内容

行内 ＝ `[S:partial][D:机]` ＋ design 审结论首段（原第一段） ＋ apply 收口段（`OP-0910-L` 2026-09-10） ＋ 指向本件的指针。
