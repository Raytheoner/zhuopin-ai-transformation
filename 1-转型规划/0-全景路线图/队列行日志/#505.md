---
title: "队列行日志 #505（§一）"
created: 2026-09-09
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #505 的状态列外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。本件先承接 2026-09-09 apply 那一棒的收工详录，行内只留摘要＋本指针；该格外置前的历史各段仍在行内，整格外置由 §四 #181 统一处理。
---

# §一 #505 · 2026-09-09 apply 收工详录（CC `OP-0908-X`）

> 本段为 apply 那一棒的完整回写原文。行内摘要见 §一 `#505` 状态列末段。
> 时间基准：本机本地时间（UTC+8），取自 `Get-Date`。

## 一、交付物与位置

- **分支** `claude/op0908x-505-hwm-apply-a1e02f` ＠ **`d890808`**，已 `git push -u origin HEAD` 成功；**未 ff master ⇒ 🟡 待 Shao Peishen 一字母**。
- 触碰面：`0-学习与工具/工具-共享文档编辑锁.py`（+229/−36）＋ `0-学习与工具/test_工具-共享文档编辑锁.py`（+215/−0）＋ `openspec/changes/editlock-append-row-highwater-writeback/tasks.md`。
- **openspec 门槛判定沿用 propose 阶段的①＋③，本棒复判未改判**（改全项目取号口径 ＋ 改 `append-row` 对外语义）。
- **apply 前置**：他 2026-09-08 答 `1a／2a` 后 `#482` 已按**内容口径**结案（内容早在 master、分支保持原样不 ff）⇒ `5a` 定的「先 `#482`、后本包」已满足，apply 无阻。

## 二、按 D1(a)／D2(a)／D3(a)／D4(a) 的实现

1. 抽出 `_locate_high_water_mark_section()` ＋ `_advance_high_water_mark()`：**单调 `max(当前值, N)`**（不回退、不写盘即幂等）、**先推线后写行**、失败一律 `HighWaterMarkFailedError` **fail-loud**。
2. `_reserve_ids` 改为调用同一条路径；**预留侧判定与错误文案一字不变**（`reject_phrase="拒绝预留"` 即原文案），碰撞前置校验仍在 `_reserve_ids` 内、位于调用之前。既有预留单测零改动通过即为证。
3. `cmd_append_row` 在**域路由改写 `args.file` 之前**算出 `hwm_target`（第三个量，与 `#420` 锁锚点同源的坑）；非队列系统目标 ⇒ `hwm_target = None`，不推线不报错（D4）。
4. release ③ 预留归属校验**一个字未动**，旁加注释写明分工（③守取号纪律、本项守状态一致性，**未退休③**），防后来者误以为重复而退掉一个。

## 三、施工中实测踩中的两处（design 未预见，写在这里备查）

- 🔴 **推线后必须重读重算**：§四 与 §一「机」域下，**写入目标与计数器目标恰是同一份物理文件**。若沿用推线**之前**读到的 `text` 拼出的整份正文写盘，这一次写盘会把刚推上去的线**原样盖回去** —— 行落盘、线没动、照样返回 0，**症状与本包要治的病灶完全同形**，只是成因换成「同一函数内两次全文覆写、后一次基于陈旧快照」。⇒ 插入逻辑抽成 `_compose(current_text)` 以便算两次，推线后重读目标文件再算一次。**这一处是先写测试才暴露出来的**（组⑴当场变红），不是读代码想出来的。
- 🔴 **D4 的判定落在调用方，不落在共用函数内部**（tasks 1.3 起草时写的是落在函数内部）。成因：`_reserve_ids` 的既有单测**恰以非队列系统的临时文件跑 `--reserve`**，并断言「声明行缺失 ⇒ 拒绝预留」（`ReserveIdsTests::test_reserve_fails_loud_when_high_water_mark_line_missing`，子进程 `--file <临时路径>`）—— 把「非队列系统 ⇒ 静默返回 `None`」写进共用函数会让这条既有 fail-loud **当场失效**，等于用本包制造一个同族缺陷。⇒ 两个调用方适用面本就不同，判据分在两处调用点；理由写进 `_advance_high_water_mark` docstring 第四条与 tasks 1.3。**副效果更贴 spec**：spec 原文是「MUST NOT **尝试**推进」，调用方拦截才是字面意义上的「不尝试」。

## 四、单测与反向对照

- 新增 `AppendRowHighWaterMarkTests` **10 例**：组⑴（`--number 174` 后线＝174）／组⑵（紧接 `--reserve` 取到 175 不撞）／D3 两例（低于线不回退、同号重复不漂移）／D2 两例（声明行缺失、分区号格式漂移 ⇒ 非零返回**且表格行逐字未写入**）／D4 一例（`--file` 覆盖到含声明行的第三方文件也不动它）／三个量各归各位一例（`--domain 业` 行落业务文件、线推机制文件、业务文件不产生声明行）／§二 不接线一例／预留侧回归闸一例。
- 🔴 **反向对照（tasks 2.7）**：同 10 例在 **master 版脚本**上跑，**5 条变红** —— `test_append_row_number_advances_high_water_mark`／`test_reserve_right_after_append_row_gets_next_free_number`／`test_missing_high_water_mark_line_rejects_and_writes_nothing`／`test_malformed_section_number_rejects_and_writes_nothing`／`test_domain_ye_row_lands_in_business_file_but_hwm_advances_in_mechanism`。另 5 例在两侧同绿（属「本包不改变该行为」的锚点，非失效用例）。

## 五、实测数与取证手段（apply 当日重跑，不引用未复跑的数）

| 范围 | 命令 | 结果 |
|---|---|---|
| 编辑锁单测（改前基线） | `python -m pytest 0-学习与工具/test_工具-共享文档编辑锁.py -q` | **460 passed／1 skipped／43 subtests** |
| 编辑锁单测（改后） | 同上 | **470 passed／1 skipped／43 subtests**（新增 10 例） |
| 编辑锁两文件（含补件选表） | `python -m pytest 0-学习与工具/test_工具-共享文档编辑锁.py 0-学习与工具/test_工具-共享文档编辑锁-补件选表.py -q` | **481 passed／1 skipped** |
| `0-学习与工具` 全量 | `python -m pytest 0-学习与工具 -q` | **5 failed／1955 passed／1 skipped／240 subtests**（27:43） |
| 机器人服务 | `cd 5-平台底座/wecom-aibot-service && python -m pytest -q` | **812 passed／1 skipped** |
| openspec | `openspec validate --all --strict` | **187 passed／0 failed** |

🔑 **零回归的手段（不是只写结论）**：上表那 5 条失败，用 `git checkout master -- 0-学习与工具/工具-共享文档编辑锁.py 0-学习与工具/test_工具-共享文档编辑锁.py` 把本棒改动的**两个文件**换回纯 master 版后，用**同一条 pytest 命令**逐条复跑 ⇒ **5 条原样复现**（`5 failed in 2.14s`），随后 `git checkout HEAD -- <两文件>` 还原、`git status --porcelain` 为空。⇒ 判**零回归**（判据＝ `.claude/rules/两桌同步与取证.md`「ff 的验收闸 ＝零回归而非全绿」，Shao Peishen 2026-09-08 答 `1a`）。

那 5 条失败逐条（均为「真实仓库现状」类既有失败，与本改动零交集）：`test_发企微.py::WecomRepoRootCrossWorktreeTests::test_repo_root_falls_back_when_not_a_git_repo`、`test_工具-CLAUDE进度段lint.py::RealRepoSmokeTests::test_当前master三条哨兵全部命中`、同类 `::test_真实根CLAUDE可被解析为结构A`、`test_工具-引导样板lint.py::真实仓库现状::test_存量已清零`、`test_工具-规划倒逼扫描器.py::SweepClass13::test_告警正文含范围声明与并入审核提醒`。

## 六、受控复现（真 CLI ＋ 真 git 仓库，验收条件 5）

夹具＝临时目录里 `git init` 一个仓库，按真实相对路径放脚本、平台包、`工具-密钥扫描lint.py` 与三份队列文件（`§一 #300 ｜ §四 #173`），**不带 `--file`**（即队列系统目标）走真 CLI：

- **对照组（master 版脚本）**：`append-row --section 四 --number 174` → `✓ 已追加一行`、**退出码 0**、线仍停 **173**；紧接 `acquire --reserve 1 --section 四` → **`✗ 预留取号失败（§四），本次 acquire 一并回滚` 退出码 1**，错误文案即「即将分配的编号 [174] 已存在于当前文件 §四 可见行中（高水位线=173，落后于文件实际内容）」。**这正是 2026-09-08 那次事故的原样。**
- **实验组（本棒实现）**：同一序列 → `append-row` 退出码 0、线跟到 **174**；`acquire --reserve 1 --section 四` → **`📍 已为你预留：§四 #175`**、退出码 0、线到 175。

## 七、tasks 0.9 取证结论（apply 阶段待办，本棒结清）

四次实证走的是**同一个豁免口——行内 `预留豁免：`**，无第四条通路：

| 行 | 取证原文（`工具-队列查询.py --row N --field all` 逐行读） | 豁免口 |
|---|---|---|
| §一 `#493`（2026-09-07 值周对账） | 高水位线声明行原话「按行内『预留豁免』取了 492+1 却未同步抬线」 | 行内 `预留豁免：` |
| §四 `#169`（`OP-0907-R3`） | 行内「📌 **预留豁免：本行编号 169 ＝ 本次 `acquire` 回显高水位线 §四 #168 +1……同一把锁内无法再 `acquire --reserve`**」 | 行内 `预留豁免：` |
| §四 `#170`（同上） | 行内「📌 预留豁免：本行编号 170 ＝ 本次 acquire 回显高水位线 §四 #168 +2……」 | 行内 `预留豁免：` |
| §四 `#174`（`OP-0908-I`，2026-09-08） | 行内「⚠️ **预留豁免：本行非「破损行修复」，是新立行时漏用 `acquire --reserve`**」 | 行内 `预留豁免：`（且显式排除 `is_repair_of_existing`） |

⇒ **机器人收件登记**（仅 §一、who＝企微机器人）与 **`--file` 覆盖**两条通路四次实证**一次都没走过** ⇒ **D1(a) 覆盖全部已实证路径，无需追加 spec delta。**

⚠️ **本项证到的是「哪个豁免口」，不是「用哪个命令写的行」** —— `预留豁免：` 是 release ③ 的放行标记，与「这一行由 `append-row` 写入还是在编辑器里手写」是两个量。协议〇.7 明文允许手写整行，**手写行不经 `cmd_append_row`、故不在 D1 射程内**；那不是本包漏掉的第四条通路，而是**本包刻意不覆盖的一类**（纪律面仍由 release ③ 守）。

## 八、留给下一棒的两件

1. **ff master 待一字母**（🟡）—— 本棒不 ff、不在主仓 commit，按 CC 侧既有纪律。
2. **生产信号回填（tasks 3.4）** —— apply 落 master 后观察机制环境队列文件第 12 行高水位线声明行是否**停止新增**「当场订正」注释；若自 apply 起不再出现，即本包生效的生产信号，回填 `#505`。价值指标基线＝「取号对全线硬阻断」**2 次／2 天**（2026-09-07、2026-09-08），目标 **0 次**。
