---
title: "队列行日志 #418（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #418 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #418 · 外置前整格原文（2026-09-04 外置，12 段）

[S:open][D:业] 🔬 **2026-08-26 CC `OP-0826-K` 代码已修、根因已用真实数据坐实；余下部署与全量重跑留步待批** —— ⑴ 🔎 **本行原记的那条线索被证伪**：她四组发票所在的 4 份导出文件**全部早已在 `.processed_exports.json` 里、早已被摄取过**（含《…20260401-20260430》，该文件贡献 288 行）⇒ **不是「跨期发票根本没进库」**。**真实根因＝摄取期的一次性判决**：`resolve_ap_no` 零命中 → 只落一条 `IngestDiagnostic` → **该诊断只被 CLI 打印、从不落盘** → 而文件 SHA 已进 ledger ⇒ 此后 `discover_new_files` 永远跳过它，**那几行再也没有第二次机会**。发票开出与 AP 立账天然有时间差（她四组里两组发票在 4 月、AP 单在 8 月），摄取跑在两者之间，零命中是**必然而非偶发**。 ⑵ **三组只读实测（`.51` 与 U9C 真实端点，已留 ConnectorAudit 痕）**：① 她四组发票**今天**在 U9C 里 **4/4** 唯一反查到她所说的那张 AP 单；② 同四组在 `.51` 的 `invoice.csv` 里 **0/4** 命中（按 `ap_no` 查亦 0/4）⇒ 从未进过库，非匹配环节出错；③ 全量口径：`D:\airead` 现存 19 份源文件共 **30,017** 条有发票号的明细行，其中**仅 3,321 行**进了 `invoice.csv` （该 CSV 共 3,374 行，另 53 行来自已不在盘上的早期文件）—— **88.9% 从未落库，而落没落、为什么没落，此前没有任何记录**。 ⑶ ⚠️ **不得按 89% 外推假报规模**：抽样 40 张「不在 CSV 里」的发票逐张反查，《20260801-20260817》**0/40**、《20260401-20260430》**1/40** 今天可解 ⇒ 绝大多数是本就无 AP 单可挂的非采购发票，**修复不会把 2.6 万行灌进库**；可捞回的是**少而准**的一批，她这 4 张正在其中。**具体多少，只有真跑一次重摄取才知道，本行不预估**。 ⑷ **已改**（分支 `claude/op0826k-a8-fi2-418-390`，commit `918bd84`）：未解析行落进 ledger `unresolved`、每次运行先跑重试 pass；只碰「源文件仍在盘且 SHA256 与 ledger 键一致」的条目，否则计入 `unretryable_*` 由 CLI 与**定时扫描**双路大声打印（🔴 定时扫描才是每日真跑的那条路）；零新增载体（重试队列寄在既有 ledger）；连带修 `scripts/rebuild_invoice_csv.py` 归属重建 —— 重试会让同一份文件在 CSV 里贡献**不连续的两段**，旧的「按 row_count 顺序累加」模型会当场失真，故 ledger 条目改记 `segments`（`row_count` 仍为各段之和，闸① 口径不变）。新增 22 条单测（含「关掉重试即复现本缺陷」的退化守卫），全套 **201 passed / 9 skipped**。 ⑸ 🔴 **销号还差三件，本班未做**：① `.51` 部署；② 重新摄取（**会改写唐燕萍每天在用的生产 `invoice.csv`**）；③ 640 单全量重跑 + 四类分布修正前后对比。**LAN 实测可达（`192.168.100.51:8094` 健康、SSH 通）—— 故这不是 LAN 留步，是「无人在场时不自行执行需批准的生产写动作」**，请 Shao Peishen/业务总线批准后再做。另：面板有登录口令闸，本行要求的「看板须显示修正后的值」须先部署、再由人登录核对。 ⑹ 📌 **连带**：`无发票支撑 883` 在 ⑸③ 完成前**不得当既定事实**（已在 §一 #391 状态列写入阻塞说明）。 ⑺ ⚠️ **一条口径待定，本班不自定**：`unretryable_*`（源文件已删/已改 ⇒ 那些发票永远进不来）要不要发 webhook 告警、算不算「文件级失败」—— 现行为＝只如实打印，不改退出码、不新增告警触发。 ⑻ 新增只读取证探针 `scripts/probe_418_invoice_gap.py`（可随时重跑复核，不写任何生产文件）。 ⑼ 🔑 **这条缺陷不是「忘了写重试」，是一个当时看着完全合理的判断**：§一 #82（2026-08-10）建第 2 层定时扫描时明文定过「文件级失败才告警，**行级诊断（`ap_no_zero_match` 等）真实数据约 89% 属预期噪声**、不告警」。那句话**当时是对的**（本班抽样 0/40、1/40 佐证绝大多数确属噪声），错在**它和另外两件事叠在一起**：① 诊断不落盘、② 文件按 SHA 锁进 ledger 后永不重看 ⇒ **「本来会自愈的少数」被混进「永远不会自愈的多数」里一起丢掉，且不留痕**。⇒ 修法也不是「把 89% 都告警」（那等于把告警淹掉），而是**把「以后可能解开」与「永远解不开」分开记、分开出声**。 ━━━ **原立行记录**：[S:open][D:业] 待领（2026-08-26 拆件巡逻定时班立行，源＝§一 #417 唐燕萍回件）。🔴 **定级不由巡逻做**（本章程 §〇 红线），已登 §四 #123 请业务总线/Shao Peishen 定序。 **供定级参考的一句**：我方 2026-08-25 补件刚请她「恢复采信面板」，她照做后**第一轮试用就撞上第二类错数** —— 这类缺陷消耗的是专员对看板的信任，而信任掉下来之后，后面每一封判例包的回件质量都会跟着掉。 　━━━　✅ **2026-08-27 Shao Peishen 拍板 ⑸② 「重新摄取」＝(b) 等 `OP-0827-F` 出结论再批** —— **不是不批，是排序**：⑸② 会改写唐燕萍每天在用的生产 `invoice.csv`（本行已定性为「无人在场时不自行执行的需批准的生产写动作」），而 **§四 `#131` 报的「3390 项料品全 BLOCK」尚未定性**。🔑 **理由**：若 3390 全 BLOCK 另有根因，重新摄取解决不了它，却先多欠一次生产写动作；反过来，`OP-0827-F` 起草时撞见的那条线索恰好指向本行 —— 本行 ⑵ 实测 `invoice.csv` 共 **3,374 行**（19 份源文件 30,017 条有发票号明细行里只进了 3,321 行），而 `#131` 报 **3,390 项** ⇒ **两数同量级且都远小于 30,017**（🔴 **是线索、不是已证实的因果**，`OP-0827-F` 已受命第一个去验它）。**⇒ ⑸② 现为 `blocked`，硬阻塞于 `OP-0827-F` 给出「常态／异常」判定**；F 若证实两者相关，⑸② 的范围与做法都要按那个结论重写，而不是照现在这版执行。**⑸① `.51` 部署** 若为跑通 F 所必需，F 已受命「先回报再做、不自行决定」。**⑸③ 640 单全量重跑＋四类分布对比** 已并入 `OP-0827-F` 同车执行（与查 `#131` 本是同一次跑动，分开做等于把同一个全量重算跑两遍）。  　━━━ 🔬 **2026-08-27 `OP-0827-F` 做掉第③件（全量重跑＋四类分布对比），并撞出两件必须改判的事；②仍未做、①经查已 de facto 发生** —— **⑴ 做法（Shao Peishen 08-27 答 (a)）＝影子重摄取，生产零改动**：`.51` 临时目录 `C:\fi2\_shadow418\`，ledger 从零、20 份源文件全量重解析，用完整树删除；跑前跑后两次直读，生产 `data/tax_export/invoice.csv` 与 `.processed_exports.json` 的 **mtime 全程停在 `2026-08-27 10:30:04`、行数全程 3389**。 **⑵ 三组数**（AP 单／items／完全匹配·无发票支撑·数量金额不符·金额微差）：08-24 基线 **640／3585／2497·883·204·1**；修正前（生产 CSV）**651／3628／2528·895·204·1**；修正后（影子）**672／3792／2614·965·212·1**。（修正前与基线的 +31/+12/0/0 ＝ 三天里又长了 11 张单，非口径漂移。） 🔴 **⑶ 反直觉的那一格必须先说清：「无发票支撑」不降反升，895 → 965。** 影子 CSV ＝ 生产 CSV **＋120 行**且为严格超集（零缺失），这 120 行**全部**属于 **21 张全新 AP 单**；**原有 651 张单一行都没多捞回来**（逐张比对，差集为空）。因为 AP 取数范围本身是从 `invoice.csv` 推出来的 ⇒ 捞回 21 张发票＝拉进 21 张新单，而这些单的其余料品行照样没发票，分子分母一起长。**⇒ 把 AP 范围钉死在原 651 张上，重摄取带来的变化是「零」。本次修复真正的收成，一句话＝ 21 张 AP 单从「面板报无发票支撑」变成「有发票」（涉 21 张发票／120 个发票行）。** 🔴 **⑷ 她四组举证只复现出 2 组，另 2 组卡在与本修复无关的第二道闸** —— `26442000009515939266/AP-2026080111` ✅、`26322000003204768541/AP-2026080110` ✅；`26322000006465433531/AP-2026080041` ❌、`26322000003204358531/AP-2026080137` ❌，两者 reason **不是** `ap_no_zero_match` 而是 **`item_code_zero_match`** ——`resolve_ap_no` 两组都成功（与本行 ⑵① 的 4/4 一致），死在下一步 `resolve_item_code`。**⇒ 本行 ⑸③ 那条硬要求「用她四组端到端复现」在现修复下只能做到 2/4，不得据此宣告销号。** **⑸ 第二道闸卡在什么上（`.51` 本机只读取证，数据未出机器）**：`resolve_item_code` 用 **(数量, 含税单价)** 逐行唯一匹配料号、容差 1e-6/1e-4（近乎逐字相等）。① `AP-2026080041` 密封胶：发票 **20 支** @含税 37.014，AP 行 **6200.0** @0.1194 —— **未税 655.12／税额 85.16 一分不差，数量差 310 倍，因为 `20 支 × 310ML = 6200`**（发票按包装件、AP 按容量，两个计量单位）；② `AP-2026080137` 气泡袋：发票 **6000** @0.20 ＝ AP 的 `J02E.0024` 1000 ＋ `R02E.0024` 5000（**跨两个料号**），发票 **7000** @0.34 ＝ `R02E.0016` 4000 ＋ 3000 —— **加起来分毫不差，但没有任何单独一行等于 6000/7000**。 🔑 **⑹ 本次最该被记住的一条**：`build_item_matches` 的 docstring 明写它按 `(ap_no,item_code)` 双向聚合、「天然吸收 AP↔INV 的多对一／一对多／多对多」，**而摄取层在数据进入引擎之前，用了一条比引擎严格得多的『逐行、数量与单价都要对得上』的规则把这些发票整行丢掉了** ⇒ **引擎为多对多而建，喂给它的那道闸只认一对一，这项能力在真实数据上从未被用到过、且不出任何声音。** **更要紧的一层**：被这道闸丢掉的恰恰是「数量或金额对不上」的那类发票 —— **也就是三单核对最该报出来的那一类**；它们不以「数量金额不符」出现，而变成「无发票支撑」。 **⑺ 量级**（影子跑全量诊断）：`ap_no` 类 24,760＋10 行／14,059 张发票；**`item_code` 类 1,342＋471 行／421 张发票**。对那 421 张逐张只读反查 —— **421/421 全部唯一解出 AP 单，涉 421 张不同 AP 单，其中 221 张今天在生产 `invoice.csv` 里一行都没有** ⇒ **面板正把这 221 张报成「无发票支撑」而其发票在 U9C 里找得到；对照本修复的 21 张，第二道闸后面压着的量级约是它的 10 倍。**（⚠️ 不得据此说 221 张全是假报：多为 2025 年的单、个别可能确有实质差异，可确定的只是「有发票而系统说没有」。）已新立 §一 `#424` 承接。 **⑻ 第①件「`.51` 部署」经查已 de facto 发生、没人决定过**：`fi2/tax_export_ingest.py`／`feed_source.py`／`webapp.py` 三个文件 SHA256 与 master **完全一致**（`a1ab6832670e6886`／`bdf300618c22b835`／`3d8b00980e428300`）——08-27 10:35:20 那次 `env-anchor-collapse` 同步把整个 `fi2` 包从 master 推了上去，而 master 当时已含 `1418a29`。**但生产行为未变**：生产 ledger 里**没有任何 `unresolved` 键**（旧代码不记）⇒ 重试 pass 无活可干；`#390` divert 默认仍 `raise`、webapp 不传该参数。🔑 **判据**：**「代码已合入 master 但留步不部署」这个状态，在本项目『整包同步』的部署方式下不存在、也守不住** —— 任何一次别的变更包部署都会把它一并带上生产，且不出声。**要留步，只能留在 master 之外。** **⑼ 明确没做**：第②件「重新摄取」（需批准的生产写动作）一律留步；**一行代码未改**；未发任何对外消息。 **全部取证与可复跑探针见** `4-数字员工/财务部/FI2-三单匹配自动对账/docs/queue_131_418_全BLOCK查证与全量对照-2026-08-27.md`。 　━━━　🔬 **2026-08-28 CC `OP-0828-A`：⑸③「640 单全量重跑＋四类分布对比」第三次独立跑通并三方对照；⑸② 仍未做、仍待批；⑸① 仍未做**（本班与 `#423`／`#424` 同车，commit `21639fa` 已 ff 合入 master）。 **⑽ 三方对照（`scripts/probe_418_full_rerun.py --invoice-dir C:\fi2\app\data\tax_export`，2026-08-28 `.51` 只读）**：AP 单/items/完全匹配·无发票支撑·数量金额不符·金额微差 —— 08-24 基线 **640／3585／2497·883·204·1**；`OP-0827-F` 修正前 **651／3628／2528·895·204·1**；**本班今天 651／3628／2528·895·204·1**；`OP-0827-F` 影子（重摄取后）**672／3792／2614·965·212·1**。⇒ **今天这一栏与 `OP-0827-F` 的「修正前」逐个数字相同**（生产 `invoice.csv` mtime 全程停在 `2026-08-27 10:30:04`、3389 行未变）⇒ **本班对 `#423`／`#424` 的代码改动没有动过任何一个面板数字**，与「默认口径一字未改」的设计一致 —— 这是实测，不是声称。 **⑾ 本行 ⑷ 那条「421 张／221 张」被独立复核并坐实**：本班走的是完全不同的路径（不做影子重摄取，改为逐张真反查 `ap_no` ＋ 逐行跑各候选口径，14,292 次只读调用），得到 **1,813 行／421 张发票／421 张 AP 单／其中 221 张今天零行** —— **与影子跑逐个数字一致**。⇒ 这组数此后可当既定事实引用，不必每次重跑。 **⑿ 🔴 ⑸③ 那条硬要求「用她四组端到端复现」仍只能做到 2/4，且本班已查明另 2 组的出路**：卡在第二道闸的两组（`AP-2026080041`／`AP-2026080137`）在现状口径下**仍然零命中**；实测各候选口径下 —— `AP-2026080041` 密封胶经 ⒜（金额）或 ⒝（单料号回落）**可唯一解出**；`AP-2026080137` 气泡袋 6000 那行 **⒜ 同样解不开**、⒞ 凑得出但跨两个料号 ⇒ 须先接受「发票行拆分」，7000 那行 ⒞ **可唯一解出**。**⇒ 4/4 的前提是唐燕萍先在 `#424` 上签认换匹配键**，本行在此之前不得宣告销号（与 `#424` 状态列既有约束一致）。 **⒀ ✅ ⑸① 关于「`.51` 部署已 de facto 发生」的判据本班再次得到印证并**未**扩大**：本班**未部署 `.51`**（属需批准的生产写动作），只在临时目录 `C:\fi2\_probe424\` 跑只读探针、收工整树已删，生产 `C:\fi2\app` 一字未动（跑前跑后两次直读核对）。⚠️ **但本班的代码已 ff 进 master** ⇒ 按本行 ⑻ 的判据，**下一次任何变更包的 `.51` 同步都会把它一并带上生产**；本班为此把 `#423`／`#424` 的默认值全部钉死在现状并各配一条退化守卫单测 —— **这是「留步」在整包同步部署下唯一守得住的形态**。 **⒁ 🔴 ⑸②「重新摄取」本班未执行、请批**：Shao Peishen 2026-08-27 已答 (b)「等 `OP-0827-F` 出结论再批」，**F 已出结论**（`#131` 判定为面板 KPI 口径问题、已销号；第二道闸另立 `#424`）⇒ **⑸② 的硬阻塞已解除，现在缺的是一次明确批准**。**但本班建议把它与 `#424` 的口径签认排在一起做，而不是现在单独做** —— 理由：⒜ `OP-0827-F` 已实测「把 AP 范围钉死在原 651 张单上，重摄取带来的变化是零」，其真实收成 ＝ 21 张 AP 单从「无发票支撑」变成「有发票」；⒝ 而 `#424` 一旦签认换匹配键，**同一次重摄取可再多捞回 206/221 张**（约 10 倍）；⒞ 生产 `invoice.csv` 是唐燕萍每天在用的文件，**改写它一次和改写两次，对她是两次口径变动、两次要重新建立信任**。⇒ **建议：等 `#424` 口径签认后一次重摄取到位**（本项属需 Shao Peishen 定夺，本班不自行选，已如实登记）。 **⒂ ⑺ 那条口径待定项（`unretryable_*` 要不要发 webhook 告警）本班未动**，原样待定。　━━━　📨 **2026-08-28 CC `OP-0828-H`：本行第③件「用她四组端到端复现」的口径侧已备件** ——她四组里卡住的第 3、4 组（`AP-2026080041`／`AP-2026080137`）正是 `#423`／`#424` 两处口径，已随 `财务部#16` 判例批改包（判例 5～8）请她签认，产物与硬前置见 §一 `#424` 本日回写段。🔴 **本行仍不销号**：⑸① `.51` 部署、⑸② 重新摄取（改写她生产 `invoice.csv`，需批准）、⑸③ 全量重跑对比三件**本班一件未动**；且 `#424` 未有结论前本行不得宣告销号（本行原有约束不变）。 ⚠️ **本班对本行做了一处如实更正的表述**：`财务部#16` 信中**没有**说「第 1、2 组已在生产上修好」——实测口径是「原因已查明、代码已改好、**影子环境**已捞回，但生产重新摄取未跑」，信中按此写，**未把影子跑说成生产复现**。 　━━━　🛑 **2026-08-28 CC `OP-0828-T`：`Fi2TaxExportDailyScan` 已在 `.51` 停用，待批准重摄取后恢复 —— 恢复权不在 CC**。 **停用时刻**：**2026-08-28 13:51:19 本机本地（UTC+8）＝ 05:51:19Z**（🔴 本行凡写时刻均双标；停用命令 `schtasks /change /tn Fi2TaxExportDailyScan /disable` 由 CC 在 `.51` 执行，前后状态均已取证：停用前 Next Run ＝ `2026/8/29 10:30:00`／状态「就绪」，停用后 Next Run ＝ `N/A`／状态「已禁用」）。 **为什么停**：本班按发布四关部署 `#423` ⒜ 时发现 `.51` 上跑的还是 **2026-08-10（`427ba09`）** 的代码，这次部署实为跨 18 天、21 个提交的追平，**包里含 `tax_export_ingest.py` +806 行／`tax_export_scan.py` +152 行／`scan_tax_export_scheduled.py` +161 行／`#371` 发票级幂等闸 —— 全是这个定时任务写唐燕萍生产 `data/tax_export/invoice.csv` 的那条链路**。若不停用，2026-08-29 10:30 本地那次触发就会用 18 天没上过生产的摄取代码**无人看守地自己跑一次重摄取**，而那正是本行 ⑸② 被刻意留步、待签认的那一件。🔑 **且代码级回滚救不回被改写的数据** —— 这是「留步守不住」的第二形态：⑻ 说的是「你的改动会被别人的部署带上去」，这条是「**别人的改动会被你的部署带到一个定时器手里**」。 🔴 **恢复条件（不由 CC 做，也不由任何 CC 班次自行判断）**：**恢复它 ＝ 批准本行 ⑸② 那次重摄取**（会改写唐燕萍每天在用的生产 `invoice.csv`），**审批权在 Shao Peishen**。批准后的恢复动作＝在 `.51` 跑 `schtasks /change /tn Fi2TaxExportDailyScan /enable`，并**建议首次恢复时有人在场看一轮**，跑完立刻与本行 ⑸③ 的四类分布做一次前后对比。 ⚠️ **`#82` 断供告警会因此报警，属预期噪音、不是真故障 —— 起止如下，免得下一个人当真故障追根因**：**起** ＝ 2026-08-29 10:30 本地（＝02:30Z，本该运行而未运行的第一个时刻）起，`#82` 第 2 层「源头断供检测（文件没来）」预计开始按其节奏告警；**止** ＝ 上述恢复动作执行、任务重新跑通之日。**这段期间该告警一律先对照本行，确认是本次人为停用所致后再判**。 📌 **顺带的事实**：停用期间发票摄取停摆 ⇒ `data/tax_export/invoice.csv` 冻结在 **2026-08-28 10:30 本地**那次写入的状态（334,502 B），本行 ⑸③ 的全量重算此后在同一份数据上可重复，**反而比之前更适合做前后对照**。 　━━━　🔴 **`Fi2TaxExportDailyScan` 已停用，待批准重摄取后恢复（2026-08-28，`OP-0828-T` 部署 `.51` 时经 Shao Peishen 显式豁免停用；Cowork `OP-0828-N` 登记）**。 **为什么停**：`.51` 当时落后 18 天／21 个提交，而次日 10:30 的定时摄取会自动跑新代码 ⇒ **`#418 ⑸②`「重新摄取」会被一个定时器替他执行**，而那件事的审批权在 Shao Peishen 手上；数一旦从 651／3628／2528·895·204·1 漂走，**代码回滚救不回数据**。⇒ 把不可逆的风险换成可逆的代价。 **豁免范围**：仅这一个计划任务、仅本次部署窗口；派单件「不修改／停用／删除任何计划任务」那条红线的立意是防止 session 擅自动它，`OP-0828-T` 把它上交而非自作主张，处理正确。 🔴 **恢复条件（Shao Peishen 2026-08-28 答 (a)）：等唐燕萍 `财务部#16` 回件** —— 她一旦勾了判例表 B，重摄取的口径就定了，那时恢复才是一次有依据的重摄取。**不得由任何 session 自行恢复**（恢复它 ＝ 批准那次重摄取）。 ⚠️ **`#82` 断供告警会因此持续报警，属预期噪音，不是真故障** —— 起于本次停用、止于恢复，读到告警的人先看本行再判。 📌 **连带**：`财务部#16` 已于 2026-08-28 06:21 UTC 发出（四条链路全绿），她的回件即本行恢复条件的触发点。 　━━━　🔴 **LAN 留步（2026-08-28 追标，Cowork `OP-0828-N`）—— 恢复 `Fi2TaxExportDailyScan` 这个动作本身只能在内网做，故按形态 1 标记，使未闭合产出扫描器能在回 LAN 时把它捞出来**。⚠️ **但本条与普通 LAN 留步有一处关键不同：它不是「回内网就补做」，而是「回内网 ＋ Shao Peishen 批准，两个条件都满足才做」** —— 恢复该计划任务 ＝ 批准那次会改写唐燕萍生产 `invoice.csv` 的重摄取（本行 ⑸②），审批权在他手上；**恢复条件已定为「等唐燕萍 `财务部#16` 回件」**（她一旦勾了判例表 B，重摄取的口径就定了）。 🔴 **任何 session 回内网后不得因为「扫描器把它报出来了」就自行恢复** —— 扫描器报的是「这件事还没做」，不是「这件事可以做了」。**两者之间隔着他的一次批准。** 　━━━　🔧 **2026-08-31 值周对账审计 · 队列结构 lint 二期告警修正（队列不得复述信状态）**：本格原文「等唐燕萍 `财务部#16` 回件」是会过时的快照 —— 改为写指针：**串行闸与回件状态一律现跑 `python 0-学习与工具/工具-跟进闸查询.py --to 唐燕萍`，唯一权威＝跟进信 README「发送状态」列**（原文按「历史记录不追改」保留）。本次实测＝🔒 锁，依据 `财务部#16` 2026-08-28 06:21 UTC 已推送、在途。 　━━━　🔒 **Shao Peishen 2026-09-01 表示批准（原话「`#418` 卡人工批准…这两项网马上办一下」），但硬前置未满足，本轮仍不执行——如实记此，使他的批准不丢失（Cowork `OP-0901-A`）** 　**为何仍不做**：本行自身写明恢复条件是**两个而非一个**——「回内网 ＋ 他批准」，且**批准的实质前提是唐燕萍 `财务部#16` 回件**（原文：「她一旦勾了判例表 B，重摄取的口径就定了」）。**2026-09-01 现跑 `工具-跟进闸查询.py --to 唐燕萍` ＝ 🔒 锁，依据 `财务部#16` 2026-08-28 06:21 UTC 已推送、在途未回** ⇒ **口径未定，此刻重摄取不知按哪个口径跑**；⑵ 网络侧当日虽有一次真实 off→on 翻转（见 `#422` 本轮回写），on-LAN 条件已满足，**卡的不是网**。 　🔑 **本条判据值得单独记**：他说「马上办」时，缺的并不是他的那次批准——**缺的是被批准对象本身还没成形**。⇒ **凡「等某人批准」的行，须同时写清「批准的输入是否已齐」**，否则批准会落在一个还不存在的方案上（同族＝「已获授权 ≠ 该照原方案执行」）。 　**⇒ 执行条件（三条齐备即可由 CC 直接做，无需再问他）**：① 唐燕萍 `财务部#16` 回件并勾定判例表 B（判闸唯一入口＝`工具-跟进闸查询.py --to 唐燕萍`，权威＝跟进信 README 发送状态列）；② 本机 on-LAN（现已满足）；③ 恢复 `Fi2TaxExportDailyScan` 并按她勾定的口径跑一次全量重摄取，**须留冒烟证据**。**他的批准以本行为凭，届时不必重新征询。** ⚠️ 顺带：`918bd84` 实测**不在 master 上**（`git merge-base --is-ancestor` 返回否），分支 `claude/op0826k-a8-fi2-418-390` 落后 master 310 提交——执行前须先核该代码是否已由别的路径合入，**不得假定已在线**。 　━━━　🔬 **2026-09-03 CC `OP-0903-F1`（`B-0903_90` A1 段）：独立复核，代码/测试维持既有结论，零新发现，未追加任何生产动作** —— 本轮为唐燕萍试用撞上“第二类错数”插队优先派工（§四 `#123` 裁决）后的独立验证，方法：重读 `fi2/tax_export_ingest.py` 全文 ＋ 核对未解析行重试机制（`_retry_unresolved`／`_RowResolver`／ledger `segments`／`unresolved` 字段）与本行既有记录逐字一致 ＋ 跑 FI2 全量测试（**230 passed / 9 skipped**，与 `OP-0828-T` 记录的基线相同，零漂移）＋ `git log` 核实 `1418a29`（原 `918bd84` 内容的等价合入）与 `bbf03a6` 均已在 master、故均已在本 worktree 基点 `d70ed28` 之内。**测试覆盖面确认**：`test_unresolved_row_is_retried_once_the_ap_doc_finally_exists` 与 `test_retry_disabled_reproduces_the_418_defect`（关掉重试即复现 #418 缺陷的退化守卫）均在位——本行原期望产出③“构造发票落在跨期文件里的输入”这一具体形态，已被“AP 单当时不存在、U9C 状态变化后重试解开”这一更一般的机制覆盖（真实根因是摄取期一次性判决，非单纯跨期覆盖面问题，详见本行既有记录）。 🔴 **本轮维持既有结论、不翻转**：生产重新摄取仍需批准且**仍与 `#424` 签认互锁**（她四组举证中有两组卡在 `#424` 的行匹配闸，须先在 `#424` 签认换匹配键才能四组全部复现，本轮未新增任何信息改变这一结论）；全量重跑对比已由 `OP-0827-F` 影子重摄取给出（无发票支撑 895→965，锁定原 AP 范围后变化为零，详见既有记录）；`.51` 代码侧已 de facto 随其它变更包部署上去（详见既有记录），但生产行为未变——仍需实际重新摄取才能兑现修复。 **本 worktree 无本地 `.env`、且硬边界禁止本轮触碰 `.51`** ⇒ 无法也未尝试重新验证真实规模数字，沿用既有记录，不外推、不预估。**本轮无新代码**（`#390` 的判据材料脚本见 §一 `#390` 本轮回写，触碰区共享但归属 `#390`）。**本行仍不销号**——生产重新摄取与她四组端到端复现均未达成，性质与此前记录一致。

---

# §一 #418 · 整格原文（2026-09-04 人工重写前外置，md5:e6e549ad）

[S:open][D:业] 🔬 **2026-08-26 CC `OP-0826-K` 代码已修、根因已用真实数据坐实；余下部署与全量重跑留步待批** —— ⑴ 🔎 **本行原记的那条线索被证伪**：她四组发票所在的 4 份导出文件**全部早已在 `.processed_exports.json` 里、早已被摄取过**（含《…20260401-20260430》，该文件贡献 288 行）⇒ **不是「跨期发票根本没进库」**。**真实根因＝摄取期的一次性判决**：`resolve_ap_no` 零命中 → 只落一条 `IngestDiagnostic` → **该诊断只被 CLI 打印、从不落盘** → 而文件 SHA 已进 ledger ⇒ 此后 `discover_new_files` 永远跳过它，**那几行再也没有第二次机会**。发票开出与 AP 立账天然有时间差（她四组里两组发票在 4 月、AP 单在 8 月），摄取跑在两者之间，零命中是**必然而非偶发**。 ⑵ **三组只读实测（`.51` 与 U9C 真实端点，已留 ConnectorAudit 痕）**：① 她四组发票**今天**在 U9C 里 **4/4** 唯一反查到她所说的那张 AP 单；② 同四组在 `.51` 的 `invoice.csv` 里 **0/4** 命中（按 `ap_no` 查亦 0/4）⇒ 从未进过库，非匹配环节出错；③ 全量口径：`D:\airead` 现存 19 份源文件共 **30,017** 条有发票号的明细行，其中**仅 3,321 行**进了 `invoice.csv` （该 CSV 共 3,374 行，另 53 行来自已不在盘上的早期文件）—— **88.9% 从未落库，而落没落、为什么没落，此前没有任何记录**。 ⑶ ⚠️ **不得按 89% 外推假报规模**：抽样 40 张「不在 CSV 里」的发票逐张反查，《20260801-20260817》**0/40**、《20260401-20260430》**1/40** 今天可解 ⇒ 绝大多数是本就无 AP 单可挂的非采购发票，**修复不会把 2.6 万行灌进库**；可捞回的是**少而准**的一批，她这 4 张正在其中。**具体多少，只有真跑一次重摄取才知道，本行不预估**。 ⑷ **已改**（分支 `claude/op0826k-a8-fi2-418-390`，commit `918bd84`）：未解析行落进 ledger `unresolved`、每次运行先跑重试 pass；只碰「源文件仍在盘且 SHA256 与 ledger 键一致」的条目，否则计入 `unretryable_*` 由 CLI 与**定时扫描**双路大声打印（🔴 定时扫描才是每日真跑的那条路）；零新增载体（重试队列寄在既有 ledger）；连带修 `scripts/rebuild_invoice_csv.py` 归属重建 —— 重试会让同一份文件在 CSV 里贡献**不连续的两段**，旧的「按 row_count 顺序累加」模型会当场失真，故 ledger 条目改记 `segments`（`row_count` 仍为各段之和，闸① 口径不变）。新增 22 条单测（含「关掉重试即复现本缺陷」的退化守卫），全套 **201 passed / 9 skipped**。 ⑸ 🔴 **销号还差三件，本班未做**：① `.51` 部署；② 重新摄取（**会改写唐燕萍每天在用的生产 `invoice.csv`**）；③ 640 单全量重跑 + 四类分布修正前后对比。**LAN 实测可达（`192.168.100.51:8094` 健康、SSH 通）—— 故这不是 LAN 留步，是「无人在场时不自行执行需批准的生产写动作」**，请 Shao Peishen/业务总线批准后再做。另：面板有登录口令闸，本行要求的「看板须显示修正后的值」须先部署、再由人登录核对。 ⑹ 📌 **连带**：`无发票支撑 883` 在 ⑸③ 完成前**不得当既定事实**（已在 §一 #391 状态列写入阻塞说明）。 ⑺ ⚠️ **一条口径待定，本班不自定**：`unretryable_*`（源文件已删/已改 ⇒ 那些发票永远进不来）要不要发 webhook 告警、算不算「文件级失败」—— 现行为＝只如实打印，不改退出码、不新增告警触发。 ⑻ 新增只读取证探针 `scripts/probe_418_invoice_gap.py`（可随时重跑复核，不写任何生产文件）。 ⑼ 🔑 **这条缺陷不是「忘了写重试」，是一个当时看着完全合理的判断**：§一 #82（2026-08-10）建第 2 层定时扫描时明文定过「文件级失败才告警，**行级诊断（`ap_no_zero_match` 等）真实数据约 89% 属预期噪声**、不告警」。那句话**当时是对的**（本班抽样 0/40、1/40 佐证绝大多数确属噪声），错在**它和另外两件事叠在一起**：① 诊断不落盘、② 文件按 SHA 锁进 ledger 后永不重看 ⇒ **「本来会自愈的少数」被混进「永远不会自愈的多数」里一起丢掉，且不留痕**。⇒ 修法也不是「把 89% 都告警」（那等于把告警淹掉），而是**把「以后可能解开」与「永远解不开」分开记、分开出声**。 ━━━ 🔬 **2026-09-03 CC `OP-0903-F1`（`B-0903_90` A1 段）：独立复核，代码/测试维持既有结论，零新发现，未追加任何生产动作** —— 本轮为唐燕萍试用撞上“第二类错数”插队优先派工（§四 `#123` 裁决）后的独立验证，方法：重读 `fi2/tax_export_ingest.py` 全文 ＋ 核对未解析行重试机制（`_retry_unresolved`／`_RowResolver`／ledger `segments`／`unresolved` 字段）与本行既有记录逐字一致 ＋ 跑 FI2 全量测试（**230 passed / 9 skipped**，与 `OP-0828-T` 记录的基线相同，零漂移）＋ `git log` 核实 `1418a29`（原 `918bd84` 内容的等价合入）与 `bbf03a6` 均已在 master、故均已在本 worktree 基点 `d70ed28` 之内。**测试覆盖面确认**：`test_unresolved_row_is_retried_once_the_ap_doc_finally_exists` 与 `test_retry_disabled_reproduces_the_418_defect`（关掉重试即复现 #418 缺陷的退化守卫）均在位——本行原期望产出③“构造发票落在跨期文件里的输入”这一具体形态，已被“AP 单当时不存在、U9C 状态变化后重试解开”这一更一般的机制覆盖（真实根因是摄取期一次性判决，非单纯跨期覆盖面问题，详见本行既有记录）。 🔴 **本轮维持既有结论、不翻转**：生产重新摄取仍需批准且**仍与 `#424` 签认互锁**（她四组举证中有两组卡在 `#424` 的行匹配闸，须先在 `#424` 签认换匹配键才能四组全部复现，本轮未新增任何信息改变这一结论）；全量重跑对比已由 `OP-0827-F` 影子重摄取给出（无发票支撑 895→965，锁定原 AP 范围后变化为零，详见既有记录）；`.51` 代码侧已 de facto 随其它变更包部署上去（详见既有记录），但生产行为未变——仍需实际重新摄取才能兑现修复。 **本 worktree 无本地 `.env`、且硬边界禁止本轮触碰 `.51`** ⇒ 无法也未尝试重新验证真实规模数字，沿用既有记录，不外推、不预估。**本轮无新代码**（`#390` 的判据材料脚本见 §一 `#390` 本轮回写，触碰区共享但归属 `#390`）。**本行仍不销号**——生产重新摄取与她四组端到端复现均未达成，性质与此前记录一致。 ━━━ 📎 历史回写 10 段已于 2026-09-04 外置（K2 口径，原文原样 md5:b3fb5b87）见 `1-转型规划/0-全景路线图/队列行日志/#418.md`；本格只留首 1 段＋末 1 段
