---
title: "队列行日志 #131（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #131 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #131 · 外置前整格原文（2026-09-04 外置，6 段，md5:6eb49283）

👀 **FI2 真实 u9c 全量重算：3390 项料品「0 自动通过／0 微差消化／3390 BLOCK 退回」（2026-08-27 观察项，CC，OP-0827-B）** —— 🔴 **本行只登记观察，不下结论**：这是 `#354` tasks 2.3.3 的部署冒烟顺带跑出来的（`POST /run`，`data_source=u9c`，真实 AP 单 `AP-2026080041`，HTTP 200、报告 6,075,300 字节），**本班没有建立前后对照，因此既不宣称它是本次 env-anchor 收拢引入的，也不宣称它是既有状态**。 ━━━ **为什么不顺手查下去**：查它要跑「同一输入在收拢前后各跑一遍」，而收拢前的那版代码已不在 `.51` 上；再加上本班范围是 `#354`／`#401` 的 LAN 留步项，顺手改判会让一次收口带两个自变量（同 `#344`／`#401 ⑺` 拒绝顺手改规则 2 的理由）。**登记比查明更要紧的是：别让它就这么过去了。** ━━━ **接手的人第一步该做什么（不必从零摸索）**：先回答「**3390 全 BLOCK 是 FI2 round-1 的已知常态，还是异常**」——判据在 `#80`／`#418`／`#390` 那几行的历史数据里，若历史上本来就是大面积 BLOCK（round-1 人工誊录路径 + AP-PO 单价强制比对 R7），那这就是常态、本行可直接销；若历史上有相当比例的自动通过，才需要往下查。⚠️ **注意 `AP-2026080041` 这一张单是筛选参数，而结果却是 3390 项料品** —— 顺带确认一下 `ap_doc_nos` 过滤是否按预期收窄了范围，这本身可能就是解释。 　━━━ 🔎 **2026-08-27 `OP-0827-E` 只读推进一步，仍不下结论（守本行「只登记观察」）** —— 本行给接手人的第一问是「3390 全 BLOCK 是 FI2 round-1 常态还是异常」，并提示先确认 `ap_doc_nos` 过滤是否按预期收窄。**本班只读核了这一条**：`fi2/feed_source.py` 的 `_fetch_u9c_ap_rows` 在 `ap_doc_nos` 分支是**逐单号调 `u9c_connector.get_ap_lines(ap_no)`**，过滤发生在**连接器调用处**、不是拉全量再客户端筛 ⇒ **「客户端没收窄」这个猜测在代码侧不成立**，要查得往 `get_ap_lines` 的**服务端行为**去。 🔑 **而那正好是一个有前科的地方**：`design D15-a①` 记着 `AP/Query` **服务器端过滤参数曾有 bug**（2026-07-21 由 IT 修过一次，见 §一 `#61`）—— **同一个参数、同一类症状，值得先按「它又回来了」这个假设验一次**，而不是从零摸索。 **另给一个尺度参照**：§一 `#418` 记的 FI2 全量重跑规模是「**640 单**」、`无发票支撑 883` ⇒ **3390 这个数不像单张 AP 单的行数**。 ⚠️ 本班**未连 U9C 取数、未跑任何对照**（本次授权范围＝只读核验＋登记），故「常态还是异常」**仍未答**，本行不销。  　━━━ ✅ **2026-08-27 `OP-0827-F` 已查清并销号 —— 判定：异常，但异常不在判定引擎，在面板 KPI 的计数口径**。🔑 **引擎那次实际只判了 1 项料品**：`AP-2026080041 / R02D.0001 / 无发票支撑`。面板上的 `3390` ＝ **1 项真判定 ＋ 3389 行孤立发票**；「0 自动通过／0 微差消化」不是「一项都没通过」，是「总共就 1 项，且它不是 pass」。 **逐个数字原样复现**（`scripts/probe_131_ap_query_docno_filter.py --repro-kpi`，走 `webapp._report_page` 同一套计数）：共 3390 项 ＝ items 1 ＋ 孤立 3389；BLOCK 3390 ＝ needs_review 1 ＋ 孤立 3389；pass 0；l2 0 —— **四个数全中**。旁证非推断：`.51` 的 `reports/fi2_web_audit.jsonl` 在 `2026-08-27T02:37:39Z`（10:37:39 本地）**只落了 1 条 `item_match`**，access trace 同刻**只有 1 次** `AP/Query?docNo=AP-2026080041`；当时生产 `invoice.csv` ＝ 3389 数据行、`ap_no=AP-2026080041` 命中 **0** 行。 **⑴ 线索 2（`docNo` 服务端过滤失效，D15-a① 复发）已证伪**：实测 `AP-2026080041` 返回 1 行、`AP-2026080137` 返回 4 行，两次 `DocNo` 集合各自唯一且互不相同 ⇒ 服务端过滤今天是好的。**⑵ 线索 1（3374 与 3390 同量级）方向对、机制不对**：`3389` 就是当时 `invoice.csv` 的数据行数本身，但不是「共享被截断的输入」，是**孤立发票行直接进了分子和分母**。 **⑶ 本行原给的判据「历史上是不是本来就大面积 BLOCK」直接答掉：不是** —— 同日同码、AP 范围取全量 651 张单时**完全匹配 2528 项（70%）**，五类口径 needs_review 仅 1100 项（30%）⇒「round-1 已知常态」这条排除；也**与 `env-anchor-collapse` 无关**（收拢只改 `.env` 解析路径，不碰计数）。 **⑷ 真实成因＝两件各自正确的事叠在一起**：2026-07-31 v8（commit `09d2ab0`，#182/#183）把孤立发票并进 KPI 时，`u9c` 模式的发票池 ＝ D19 **人工誊录 8 张小样、且是为被核对的那几张 AP 单誊录的** ⇒ 孤立发票必然≈0（`partition_invoices` docstring 至今写着「理论不应出现」），当时完全合理；2026-08-07 发票源改道税务导出（#295/#82）后池子变成**全公司 3389 行／651 张单**，而 AP 侧范围仍由人在表单里手填。**两者一脱钩，「孤立发票」就从异常信号变成必然的绝大多数，而那条公式没人回头看过。** 🔑 **可举一反三**：**一个计数口径的正确性，靠的往往不是它自己的逻辑，而是它当时那个输入的规模假设**；输入换了源、涨了三个数量级而公式没动，它就会毫无报错地开始输出荒谬的数 —— **而「BLOCK 数很大」看着像坏消息，没人会怀疑这个数本身是假的**（同族：错误不产生任何信号）。 **⑸ 顺带坐实**：那 1 项的 BLOCK 本身就是 §一 `#418` 的实例（发票 `26322000006465433531` 今天在 U9C 查得到、生产 CSV 里 0 命中）。 **⑹ 本次一行代码未改**（Shao Peishen 2026-08-27 答 (a)）；由此新立 §一 `#423`（KPI 孤立发票计数口径，需唐燕萍签认）。 **全部取证与可复跑探针见** `4-数字员工/财务部/FI2-三单匹配自动对账/docs/queue_131_418_全BLOCK查证与全量对照-2026-08-27.md`。**本行至此销号。** 　━━━　📌 **2026-08-28 CC `OP-0828-A` 补一条指针（本行已销号，不重开）**：本行查明的那条 KPI 计数口径已由 §一 `#423` 承接并**实现了三档候选 ＋ 装载侧 ⒞**，同一份真实数据上的显示值均已跑出（现状 3390／⒜ 1／⒝ 3390 行含孤立发票／⒞ 1）；**默认口径一字未改，待唐燕萍签认**。 📌 **顺带补一个本行当时没有的对照，它会影响「这事有多急」的判断**：**AP 范围取全量 651 张单时孤立发票只有 1 行**（而筛 1 张单时是 3389 行）⇒ **本行那次爆表只发生在「AP 侧筛得窄、发票侧全公司」这个组合下，不是天天发生**。取证见 `4-数字员工/财务部/FI2-三单匹配自动对账/docs/queue_423_424_口径候选与影响面-2026-08-28.md` §一.3。
