---
title: "队列行日志 #454（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #454 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §一 #454 · 外置前整格原文（2026-09-04 外置，6 段，md5:069cd1dc）

[S:open][D:机] 🆕 **2026-09-02 立行（Cowork `OP-0902-A`，Shao Peishen 当日答 ②(a)）。** **要修的是一个闭环死锁，不是「没人干活」** —— 分诊器 `_suggest_status_reclassification` 2026-08-30 已建、单测齐全、**且确实接在生产路径上**（`工具-共享文档编辑锁.py` release 校验 ⑨ 内），但它**只在 `wip_count > cap` 时才被调用**，输出附在 release 拒绝消息里 ⇒ ⑴ 候选只对**那个正好被拦下的 session** 显示；⑵ 而被拦的 session **无权改他人的行**（`#422` 先例原文「本班无权替他人关行」）；⑶ 它只能给自己标 🛑 排队（`#439`／`#440`／`#447`／`#448`／`#452` 全是这么来的）；⑷ 那些该改判的行继续占位。🔑 **一句话：分诊器的输出从来没有被一个有权限改判的人看到过。不是没建、不是没跑，是跑给了唯一改不动它的人看。** 　━━━　**⑵ 第二半同样必须做**：本次 12 条改判候选里 11 条的阻塞点是「等 Shao Peishen 一次动作」，而**这 11 项在 §四 台账上一条都没有** —— 它们写在 §一 行的状态列自陈里，「会话末罗列决策项」只管单次会话、§四 只收登记过的，**两处都不覆盖**。⇒ 当日实测：§四 6 项 ＋ 行内 11 项 ＝ **真实待他拍板 17 项，台账只见 6 项**。**WIP 满不是拥堵，是决策队列溢出到了任务队列里。** 　━━━　⚠️ **本次是第二轮，不是首次** —— 2026-08-31 值周巡检已做过一次同类分诊（见 `#422` 行内，他当日定「**不调上限，低的是分诊执行率**」，指名 9 行逐行过目），**但未形成常态 ⇒ 三天后再次超限（24／22）**。⇒ 按规则退休制，「靠人记得去分诊」这条已达机制化门槛。 　━━━　📌 **并入审核（协议〇.10）已于取号前完成**：与 `#422`（未闭合产出扫描器接入现有轮次）**同族但不同对象**（那条是未闭合产出扫描器、本条是状态分诊器），**且 `#422` 现卡在「待 Shao Peishen 人工安装」** ⇒ 并入会把本项一并冻住，故**不并入、独立立行**；🔴 **两行触碰区重叠于 sweep 轮次，必须串行**，先动哪条交值周定。 　━━━　⚠️ **顺带发现一处机制缺口（非本行范围，未处置）**：`edit-row` 写侧的半角竖线守卫**不是反引号感知的**，而读侧 `_table_data_rows`（#314③）是 ⇒ **凡状态列在反引号内合法用过半角竖线的行，其 `[S:]` 前缀一旦写定即锁死、无法再翻转**（`--append` 只能加尾巴）。本次 `#324` 即因此改判失败、状态字段维持 `partial` 不实，已在该行内如实留痕。**建议值周巡检定是否立行。** **⇒ 下一步＝起草 design 提纲（机制类命中「改变全项目口径」，须走 openspec 含 design 审），不直接开工。** 　━━━　⚠️ **本次操作自身撞出的两处机制缺陷，如实登记（第一处是我引入的，后两条是机制的）** **⑴ 起因是我**：改判尾巴里嵌了分诊器给的引文片段，该片段按 60 字硬截断，**在 `#337`／`#422` 两行恰好把一个反引号截在中间** ⇒ 整格反引号变奇数 ⇒ 未闭合跨度**吞掉行尾列分隔符**，两行由 8 列塌为 7 列。**`edit-row` 写入时未拦**（它只校验传入值里的半角竖线，不校验反引号奇偶、也不校验写入后本行还剩几列），**是 release 结构门禁事后拦下的**。🔑 **写侧守卫比读侧解析弱一档：读侧反引号感知，写侧不感知。** **⑵ 修复时撞上鸡生蛋**：`edit-row` 对**已塌列的行**拒绝一切操作（含 `--append`），理由正是「列数为 7，应为 8」⇒ **行一旦被写坏，唯一能修它的入口就把自己关上了**，只能在持锁状态下用脚本改文件（本次即如此，已如实记；锁全程未释放）。 **⑶ 与 `#324` 是同一族的两面**：那条是「写侧竖线守卫非反引号感知 ⇒ 状态字段锁死改不动」，本条是「写侧不校验反引号奇偶 ⇒ 能把行写坏、写坏后又改不回」。**⇒ 建议合并处置：给 `edit-row` 的写入校验补两条——反引号奇偶校验、写入后列数回读校验；并为已塌列的行留一个 `--repair` 逃生入口。** **未立行、未处置，交值周巡检定是否并入本行或另立。**


---

# §一 #454 · 第二批外置（2026-09-06 `OP-0906-N` apply 期，2 段，原文原样 md5:e679710b）

> 外置自行内状态格：⑴「本次操作自身撞出的两处机制缺陷」段（2026-09-02 立行期）、⑵「2026-09-06 `OP-0906-G` design 起草 ＋ 三项裁决」段。**原文未改写**，行内只留首段＋指针＋末段。

⚠️ **本次操作自身撞出的两处机制缺陷，如实登记（第一处是我引入的，后两条是机制的）** **⑴ 起因是我**：改判尾巴里嵌了分诊器给的引文片段，该片段按 60 字硬截断，**在 `#337`／`#422` 两行恰好把一个反引号截在中间** ⇒ 整格反引号变奇数 ⇒ 未闭合跨度**吞掉行尾列分隔符**，两行由 8 列塌为 7 列。**`edit-row` 写入时未拦**（它只校验传入值里的半角竖线，不校验反引号奇偶、也不校验写入后本行还剩几列），**是 release 结构门禁事后拦下的**。🔑 **写侧守卫比读侧解析弱一档：读侧反引号感知，写侧不感知。** **⑵ 修复时撞上鸡生蛋**：`edit-row` 对**已塌列的行**拒绝一切操作（含 `--append`），理由正是「列数为 7，应为 8」⇒ **行一旦被写坏，唯一能修它的入口就把自己关上了**，只能在持锁状态下用脚本改文件（本次即如此，已如实记；锁全程未释放）。 **⑶ 与 `#324` 是同一族的两面**：那条是「写侧竖线守卫非反引号感知 ⇒ 状态字段锁死改不动」，本条是「写侧不校验反引号奇偶 ⇒ 能把行写坏、写坏后又改不回」。**⇒ 建议合并处置：给 `edit-row` 的写入校验补两条——反引号奇偶校验、写入后列数回读校验；并为已塌列的行留一个 `--repair` 逃生入口。** **未立行、未处置，交值周巡检定是否并入本行或另立。**

━━━

【2026-09-06 OP-0906-G 看护批 B-0906_G，A4 泳道 454-status-triage：design 起草完成 ＋ Shao Peishen 当场裁决三项，转 apply】**门槛判定**：⑴「分诊器从 release 拒绝消息改为常驻轮次主动调用」单独看**不**命中三门槛（签名/返回值/`STALE_STATUS_PHRASES` 全不动，只多一个调用方；sweep 第 4/6/7/9/10/11 类常驻告警六个同形先例无一走 openspec，第 10 类 `54529ab`、第 11 类 `2635cd5` 均直接建造）；但 ⑵「§一 自陈行汇总进 §四」命中门槛①（追加 §四 行＝取号，且改变「什么有资格成为一条 §四 台账项」——现有 93 行全是人登记的），⑴⑵ 共用同一调用点故同包一次评审。**openspec 包**：`openspec/changes/status-triage-resident-round/`（proposal 108＋design 158＋spec delta 125＋tasks 60），分支 `claude/op0906k-status-triage-454`，commit `34be71c`；看护者独立复跑 `openspec validate status-triage-resident-round --strict` ⇒ `Change is valid`。**实测发现（design 依据）**：① 死锁实证——机制可动 WIP 现测 21/22 未超限 ⇒ release ⑨ 此刻根本不触发、分诊器一次都不会被调用，而现存 8 条候选对任何人不可见；② 🔴 新发现（行内此前未记）——**分诊器对业务场景队列结构性永久失效**：触发条件是机制 WIP 超限，而业务队列机制 WIP 恒为 0，其 6 条候选永远不可达且失效不产生任何信号（看护者独立复核：`工具-队列查询.py --row 161 --section 四` 对两份队列各探一次，机制队列 §四 读得出、**业务队列根本没有 §四 分区**，故业务域自陈行只能跨文件写进机制队列 §四，直接落在 `editlock-dual-queue-routing` 域路由口径上）；③ 🔴 分诊器精度实测 **3/8**——真阳性 `#455`/`#394`/`#418`，假阳性 5 条分两型：亚型A「不看这句话在说谁」（`#462`，其行内 2026-09-02 已自记该缺陷并写「建议与 #454 同批处置」，本次原样复现）、亚型B「不看时态」（`#340`/`#470`/`#471`/`#472`，命中的都是「该留步已闭合/已解除/已批准」，为新增发现）——此数据否掉「把候选原样推进企微群」的做法；④ §四 缺口：16 条自陈行中 13 条是 `blocked` ⇒ ⑵ 的扫描面必须独立于分诊器；5 条未被 §四 覆盖，扣假阳性后真实缺口 ≤4（非 5）；⑤ sweep 当前对 §四 零解析、对队列零写入。🔑 **Shao Peishen 2026-09-06 于 OP-0906-G 看护会话当场裁决（原话「都按建议」）**：**D4=(a)** 只检测＋告警＋给可粘贴的 `append-row` 草稿命令，**不自动写 §四**（理由：精度 3/8 下 (b) 会把 5 条假阳性写进台账；且 (b) 等于给 sweep 新开一条机器写队列正文的路径，与 `#326`/`#322` 两次事故同族）；**D3=(a)** 同意退休 `release ⑨` 的候选接线，one-in-one-out（不退休即「两套判据各自轮询」，`#366` 教训；且 release ⑨ 实测此刻不触发）；**次序=(a)** `#454` 先 apply、`#462` 随后按 `#454` 落定的第 12 类接线点接入（`#454` design 已定义该接线点且明确排除挂 `#462`，而 `#462` 至今未起草 openspec）。已 `工具-泳道看护状态机.py resume --lane 454-status-triage` 落库解除暂停。**待落地泳道 OP-0906-N 做什么**：① 按 `openspec/changes/status-triage-resident-round/tasks.md` 逐条 apply（走 skill `openspec-apply-change`／`opsx:apply`，逐条勾 tasks）；② 严格按上述三项裁决落地——D4 只出告警＋可粘贴命令、绝不自动写 §四；D3 落地时同步摘掉 release ⑨ 的候选接线（one-in-one-out，不得两处并存）；③ 接入点复用 sweep 现有常驻轮次作为第 12 类，**不新建独立定时轮次**（`#366` 教训）；④ 精度改进按 design D2 强弱两档＋否定词表落地，必须含定稿回测阻断项，且回测须复现亚型A（不看主语）与亚型B（不看时态）两类假阳性均被拦掉；⑤ 扫描面须覆盖**两份**队列 §一（业务队列无 §四，其自陈行按域路由口径写进机制队列 §四 的草稿命令里）；⑥ 注意 design D6 的反引号奇偶坑（`#454` 曾撞塌两行）。**不做什么**：不改 `_count_mechanism_wip` 判据；不替他人队列行做改判决定（分诊只产「建议候选」）；不动 `#462`（次序已定其在后）。**收工**：跑 `openspec validate --strict` 与相关单测，回写本行 ＋ 登记 §二。


---

# §一 #454 · `OP-0906-N` apply 收工回写全文（2026-09-06）

> 行内末段只留结论与留步项；本节是同一次回写的完整原文。

【2026-09-06 `OP-0906-N` 泳道 `status-triage-apply-454`：变更包 apply 完成，止步于「未 ff 进 master」＋「企微首次推送未留痕」】

**产出**：分支 `claude/op0906n-status-triage-apply-454`，commit `a4329c2`，已 push origin，**未 ff 进 master**。`openspec validate status-triage-resident-round --strict` ⇒ `Change is valid`。

**做了什么（按三项裁决逐条落地）**：

⑴ **编辑锁侧新增只读子命令 `triage-candidates --queue X [--json]`** —— 不 acquire 任何锁、不写任何文件、连锁文件都不看一眼。🔴 **候选集合的权威判定仍是 `_suggest_status_reclassification()`**：新函数 `_collect_triage_candidates()` 只对它已认定的行重新定位措辞、取宽窗、算档位；两侧行集不一致时**如实记进 `criteria_drift` 并以权威判定为准，不静默取其一**。`_suggest_status_reclassification()` 与 `STALE_STATUS_PHRASES` **一个字未改**（spec MUST NOT）。刻意**一次只扫一份队列**——调用方需要知道每条候选来自哪份文件，合并输出会把来源从天然的调用边界降格成一个额外字段。

⑵ **否定词表 `TRIAGE_NEGATION_PHRASES`（九条，各附真实来源行号，只降档不剔除）＋ `_balance_backticks` 反引号奇偶守卫**（奇数即整体去掉全部反引号——**宁可丢格式，不可丢列**；告警正文虽不写回文件，但会被人复制粘贴回队列行）。

⑶ **sweep 侧新增第 12 类常驻状态告警 `_check_status_triage_and_ledger`**，接在 `main()` 非 dry-run 分支第 11 类之后，复用 `_track_and_alert_standing_state`（key ＝ `队列文件名|行号|措辞`，节流 24h）。强档进企微运维群、弱档只回显、**降档条数每轮回显**（词表一旦写宽，"被降掉的越来越多"是唯一能看见它失效的信号）。**sweep 侧按 JSON 的 `tier` 字段分桶，不重算档位、不复制第二套判据**（design D5）。

⑷ **决策台账缺口检测**：新增 §四 结构化解析（sweep 此前对 §四 零解析），扫描面**覆盖 `blocked`**、独立于分诊器（实测 16 条自陈行里 13 条是 `blocked`，沿用分诊器的 open/partial 限制会一开始就漏掉 81%）；两份队列行号空间重叠时**报错而非静默匹配**——🔴 **静默匹配的后果是「缺口凭空消失」，而缺口消失长得跟「这件事已经登记好了」一模一样**。**D4=(a) 落地**：只出可直接粘贴的**两步** `append-row` 草稿命令（`acquire --reserve 1 --section 四` → `append-row` → `release`），**绝不自动写 §四**；业务域来源在正文点明「跨文件登记，请先确认归属」。

⑸ **D3=(a) 落地——release 校验 ⑨ 的候选接线已退休**：移除调用点与 `_mechanism_wip_over_cap_violations()` 的 `reclass_candidates` 形参；`_suggest_status_reclassification()`／`_render_reclassification_candidates()`／`STALE_STATUS_PHRASES` 原样保留、既有单测保留（**退的是接线，不是判据**）。one-in-one-out，无两处并存。原用例 `test_..._carries_reclassification_candidates` 翻转为 `..._no_longer_carries_candidates_but_keeps_ways_out`：断言不再含候选清单，但**仍含 WIP 计数与「两条出路」**——退候选接线不等于把拒绝文案退成不可行动的。

**🔑 apply 期三条实测发现**：

① **窗口宽度是否定词表能生效的前提，不是显示参数。** 分诊器现行的 `idx-10 / idx+len+20` 窄窗，在 `#470`/`#471`/`#472` 上恰好在「已由」二字处截断，否定词 `已由 Shao Peishen` 落在窗外 ⇒ **四条亚型 B 会全部漏降**。改取前后各 60 字（`TRIAGE_EXCERPT_CONTEXT_CHARS`）后回测才达标。

② **自己撞出一处、就地修。** 初版在判据不可用时仍打了一行「强档 0 条／弱档 0 条」——那句话与真的零候选在日志里**逐字相同**，而两者后果差一个量级（「没事」vs「本轮什么都没看」）。已改为：全不可用时**根本不打计数行**、改打「未取到任何一份队列的候选」；部分不可用时计数行自带「已扫 1／2 份队列，缺 1 份」的覆盖面。两条各有单测钉住。

③ 🔴 **`#462` 落到弱档是巧合，不是能力。** design D2 明确预言亚型 A（不看这句话在说谁）本包解决不了、`#462` 上线后仍会误报为强档。实测它被降档，**原因不是本包学会了判断主语，而是该行正文里恰好写着「日核假阳性」四个字、被否定词表顺手扫到**——换一行同样"引用他人阻塞状态"却不含否定词的行，仍会误报为强档。单测 `test_亚型A本包不试图机器解决_但仍被如实登记为候选` 刻意只断言"它仍被产出且命中片段被原样附上"，**不断言它被正确判为假阳性**，就是为了不让这次巧合被后人误读成「亚型 A 已解决」。

**回测（tasks 1.2 阻断项，对两份队列真身实跑、非夹具）**：亚型 B 四条 `#340`／`#470`／`#471`／`#472` **全降弱档** ✅；真阳性三条 `#455`／`#394`／`#418` **全保强档** ✅。⇒ 达标。

**真身实测 vs design 起草期逐条比对**：

| 指标 | design 起草期 | apply 期实测 | 差异解释 |
|---|---|---|---|
| 候选总数 | 8（机 2 ＋ 业 6） | **9**（机 3 ＋ 业 6） | 多出 `#454` 自己——`OP-0906-G` 把 design 结论回写进本行状态列时，原样引用了「该留步已闭合/已解除/已批准」⇒ **本行被自己的判据命中**，正确降为弱档 |
| 强档 | 3 | **3** ✅ | 逐条一致 |
| 自陈待他一次动作 | 16 | **16** ✅ | 完全一致 |
| 台账缺口 | 5，扣假阳性后 ≤4 | **4**（`#337`/`#341`/`#455`/`#462`） | `#394` 已于起草与 apply 之间进 §四，按 D7「§四 提及即算覆盖」正确出列 |
| 机制类可动 WIP | 21／22 | **20／22** | 期间有行销号；**不影响本包——本类不以 WIP 为触发条件，这正是本包的要点** |
| 两份队列行号交集 | 空集 | **空集** ✅（机 105／业 45） | — |

**单测**：`test_工具-共享文档编辑锁.py` **391 passed / 8 subtests**（新增 18 例、翻转 1 例）；`test_工具-落库sweep.py` **424 passed / 51 subtests**（新增 14 例）；另跑全部 10 个引用这两模块的测试文件。**零回归。** ⚠️ 其中 4 例失败**与本包无关、为既存**：`test_工具-CLAUDE进度段lint.py::RealRepoSmokeTests` 两例（读根 `CLAUDE.md` 进度段，本包未触碰）、`test_工具-引导样板lint.py::test_env锚定存量已清零`（残留在 `4-数字员工/采购部/SC7-库存优化建议/scripts/leadtime_m*.py`）、`test_发企微.py::test_repo_root_falls_back_when_not_a_git_repo`（TEMP 走 8.3 短名 `PAULSH~1` 与 `Paul Shao` 不等）。

**触碰区外的一处，如实点名**：按 proposal §知识资产三问第 3 条「本包 apply 时执行」，已在 `1-转型规划/0-全景路线图/跨场景前置数据与知识库任务总表.md` §一.2 登记一行「**队列状态分诊三尺度**」（持有人 Shao Peishen／backup 孙涛；提取方法＝历史案例反推 ＋ L2 改判判例累积，**满 5 条负例即回头收紧措辞表**；显性化形态处如实写明「§四 准入尺度至今无成文规则、本包不试图代拟」）。🔴 **该行是本表首条机制/环境类条目**（此前 9 行全是业务场景的专家默会经验），已在行内显式标注，避免后人当成漏填场景号的业务条目。**该文件不在本行原声明触碰区内。**

**⏸ 唯一留步项（故本行不销、按 `partial`）**：tasks **6.3「企微首次真实推送留痕」未做** —— 告警出口是 `WECOM_WEBHOOK_URL_OPS`，本 worktree `.env` 无该键（实跑日志原文：「未在 .env 找到 WECOM_WEBHOOK_URL_OPS，跳过状态分诊候选告警推送（仅留痕日志与状态文件）」）。**首次真实推送将在本包合入 master 后的第一轮 `ZhuopinCommitSweep` 自然发生**（当前真身有 3 条强档候选 ＋ 4 条台账缺口，必然触发）。⇒ 留痕核验交合入后的值周巡检；`tasks 7.4 /opsx:archive` 同样待 6.3 之后再做——**归档一条尚未验证过出口的告警类变更，等于把「它到底响没响」这个问题永久埋进 archive**。

**⛔ 合并决策待 Shao Peishen**：是否 ff 进 master 属 🟡 档，本泳道不自决。

**📎 `#462` 的接线点已就位**（次序裁决 (a) 的下半句）：即 `工具-落库sweep.py::main()` 里 `_check_status_triage_and_ledger(repo_root, log)` 那一处调用。`#462` 接入时应新增自己的 `_check_*` 并在**同一处**调用，**不新建独立轮次**（`#366` 教训）。

**📎 `edit-row` 写侧两处缺陷仍未立行**（design Q4 的可见性提醒）：反引号奇偶校验缺失、已塌列行的鸡生蛋。本包只在**输出侧**加了 `_balance_backticks`（防告警引文被粘回队列行时写塌列），**写侧守卫未动**——那属 `#455`／`#462` 触碰区，本包「不做什么」已排除。此处仅再做一次可见性提醒。

**📎 状态文件三个（非两个）**：proposal §.gitignore 覆盖节只点了 `sweep-status-triage-state.json`／`sweep-decision-ledger-gap-state.json`；实现按第 10/11 类同形另需 `sweep-status-triage-unavailable-state.json`。三者均已用 `git check-ignore -v` 实跑核实，全部命中 `.gitignore:35:**/reports/`，`exit=0`。不重演 `#322` 的孤儿脏文件形态。
