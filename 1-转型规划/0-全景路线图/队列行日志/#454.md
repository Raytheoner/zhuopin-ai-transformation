---
title: "队列行日志 #454（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #454 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §一 #454 · 外置前整格原文（2026-09-04 外置，6 段，md5:069cd1dc）

[S:open][D:机] 🆕 **2026-09-02 立行（Cowork `OP-0902-A`，Shao Peishen 当日答 ②(a)）。** **要修的是一个闭环死锁，不是「没人干活」** —— 分诊器 `_suggest_status_reclassification` 2026-08-30 已建、单测齐全、**且确实接在生产路径上**（`工具-共享文档编辑锁.py` release 校验 ⑨ 内），但它**只在 `wip_count > cap` 时才被调用**，输出附在 release 拒绝消息里 ⇒ ⑴ 候选只对**那个正好被拦下的 session** 显示；⑵ 而被拦的 session **无权改他人的行**（`#422` 先例原文「本班无权替他人关行」）；⑶ 它只能给自己标 🛑 排队（`#439`／`#440`／`#447`／`#448`／`#452` 全是这么来的）；⑷ 那些该改判的行继续占位。🔑 **一句话：分诊器的输出从来没有被一个有权限改判的人看到过。不是没建、不是没跑，是跑给了唯一改不动它的人看。** 　━━━　**⑵ 第二半同样必须做**：本次 12 条改判候选里 11 条的阻塞点是「等 Shao Peishen 一次动作」，而**这 11 项在 §四 台账上一条都没有** —— 它们写在 §一 行的状态列自陈里，「会话末罗列决策项」只管单次会话、§四 只收登记过的，**两处都不覆盖**。⇒ 当日实测：§四 6 项 ＋ 行内 11 项 ＝ **真实待他拍板 17 项，台账只见 6 项**。**WIP 满不是拥堵，是决策队列溢出到了任务队列里。** 　━━━　⚠️ **本次是第二轮，不是首次** —— 2026-08-31 值周巡检已做过一次同类分诊（见 `#422` 行内，他当日定「**不调上限，低的是分诊执行率**」，指名 9 行逐行过目），**但未形成常态 ⇒ 三天后再次超限（24／22）**。⇒ 按规则退休制，「靠人记得去分诊」这条已达机制化门槛。 　━━━　📌 **并入审核（协议〇.10）已于取号前完成**：与 `#422`（未闭合产出扫描器接入现有轮次）**同族但不同对象**（那条是未闭合产出扫描器、本条是状态分诊器），**且 `#422` 现卡在「待 Shao Peishen 人工安装」** ⇒ 并入会把本项一并冻住，故**不并入、独立立行**；🔴 **两行触碰区重叠于 sweep 轮次，必须串行**，先动哪条交值周定。 　━━━　⚠️ **顺带发现一处机制缺口（非本行范围，未处置）**：`edit-row` 写侧的半角竖线守卫**不是反引号感知的**，而读侧 `_table_data_rows`（#314③）是 ⇒ **凡状态列在反引号内合法用过半角竖线的行，其 `[S:]` 前缀一旦写定即锁死、无法再翻转**（`--append` 只能加尾巴）。本次 `#324` 即因此改判失败、状态字段维持 `partial` 不实，已在该行内如实留痕。**建议值周巡检定是否立行。** **⇒ 下一步＝起草 design 提纲（机制类命中「改变全项目口径」，须走 openspec 含 design 审），不直接开工。** 　━━━　⚠️ **本次操作自身撞出的两处机制缺陷，如实登记（第一处是我引入的，后两条是机制的）** **⑴ 起因是我**：改判尾巴里嵌了分诊器给的引文片段，该片段按 60 字硬截断，**在 `#337`／`#422` 两行恰好把一个反引号截在中间** ⇒ 整格反引号变奇数 ⇒ 未闭合跨度**吞掉行尾列分隔符**，两行由 8 列塌为 7 列。**`edit-row` 写入时未拦**（它只校验传入值里的半角竖线，不校验反引号奇偶、也不校验写入后本行还剩几列），**是 release 结构门禁事后拦下的**。🔑 **写侧守卫比读侧解析弱一档：读侧反引号感知，写侧不感知。** **⑵ 修复时撞上鸡生蛋**：`edit-row` 对**已塌列的行**拒绝一切操作（含 `--append`），理由正是「列数为 7，应为 8」⇒ **行一旦被写坏，唯一能修它的入口就把自己关上了**，只能在持锁状态下用脚本改文件（本次即如此，已如实记；锁全程未释放）。 **⑶ 与 `#324` 是同一族的两面**：那条是「写侧竖线守卫非反引号感知 ⇒ 状态字段锁死改不动」，本条是「写侧不校验反引号奇偶 ⇒ 能把行写坏、写坏后又改不回」。**⇒ 建议合并处置：给 `edit-row` 的写入校验补两条——反引号奇偶校验、写入后列数回读校验；并为已塌列的行留一个 `--repair` 逃生入口。** **未立行、未处置，交值周巡检定是否并入本行或另立。**
