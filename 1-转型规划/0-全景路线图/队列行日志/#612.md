---
title: "队列行日志 #612（§一）"
created: 2026-09-17
status: 生效
用途: 跨桌任务队列 §一 #612 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #612 · 外置段（2026-09-17 外置，工具第 1 批，8 段，原文原样）

> 外置人 CC-OP-0917-Q；外置前整格 5776 B、共 13 段；本批外置第 2、3、4、5、6、7、8、10 段；本格保留第 1、9、11、12、13 段。

🔑 **立行的真实理由（他 2026-09-17 原话「新场景发布能提高团队工作积极性」）**：陈忱 2026-08-25／08-28／09-01／09-14 四轮回件攒出的全部判据，**现取全仓零个 `.py` 引用**——十一条判例、51 条规则、10 份样本全部躺在 `.md`／`.json` 里没有任何东西消费。**Q2 排 2026-10、零工程实体、零 openspec 包 ⇒ 不是被卡，是没排上**（与 FI3 2026-09-17 早间的处境逐字相同）。

━━━

✅ **为什么不跑 grill**：需求树已成文且已签认——档位由陈忱 2026-09-01 逐字裁定 `☑ (b) 改为两档、以 V3.2 为准`；五条语义验收口径 V1–V5 已于 2026-09-02 全部认可；判例 1–11 全闭环。**开放点只剩 1 项**（「措施可验证性的判定标准」＝什么算「包含可量化的验证标准」，**未单独发问**）⇒ 按 FI3 同法落 `PENDING` 读即抛，不代填。

━━━

🔴 **四条建造纪律，逐条照办**：⑴ **档位不得自定**——本包只实现 V3.2，`1-转型规划/8D报告AI评审规则库.md` 是**签认台账不是档位来源**（2026-09-02 已降格，`#431` ⒜）；⑵ **`judge_method` 分流**——51 条里标「规则引擎」的本批实现，标语义/LLM 的挂 `PENDING`，且**判例 11 明定红线②③ 本批不验收、P1 全部转人工**；⑶ **判例 6：安全相关件一律转人工、不走自动退回**；判例 9：**D3–D7 任一整段为空 ⇒ 结构性退回，先于红线语义判定**（顺序不得颠倒）；⑷ **真实 8D 原文一律不入 git**（QD-A 红线 1）——档 1 用合成样本，7 条退回理由（已脱敏、已落 `.md`）作判据对照。

━━━

⚠️ **一处须如实标注、不得当成已解决**：QD-A 作为前置抽取层，**总体命中率实测 37.9%、低于 60% MVP 门槛**（其 CLAUDE.md 明写「不得称已达门槛」）⇒ 本包 `feed_source` 接 QD-A 时须 fail-loud 标注置信度，**不得把低置信字段当已确认输入**。

━━━

📌 **本行不含发布**：档 1 ＝ mock 验证，`.51` 部署属档 3、另行排。

━━━

**不做什么**：🔴 不代填任何判据默认值（缺签认一律 `PENDING` 读即抛）｜🔴 不代指派持有人/backup｜🔴 不代发信、不代联络陈忱/唐燕萍/IT｜🔴 不碰 `.51`、不碰真实库、不部署｜🔴 不 ff master（🟡 档，无头不做）｜🔴 不在主仓 commit，只 push 本泳道分支｜🔴 **真实 8D 原文一律不入 git**（QD-A CLAUDE.md 红线 1：`data/golden/*`、`results/` 全 gitignore）。

━━━

**收工**：产出 commit 到本泳道分支并 push；登记 §二 批次；回写本行状态（做到哪、卡在哪、证据链）；顶格一行 `OPENER_DONE` 收尾，命中 🟡/🔴 决策点则 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>`。 [S:partial][D:业] 🟡 **2026-09-17 档 1 已建成、停在 design 审决策点（CC 无头泳道 `OP-0917-Q`，分支 `claude/op0917q-q2-verdict` 已 push，commit `0a52378`＋`b220cf6`；未 ff master）**

━━━

🔑 **四条建造纪律逐条落**：⑴ 档位只从 V3.2 JSON 取，判例台账只作 Signoff 凭据；她 09-01 M1–M18/P1–P3 与 09-14 判例 7–11 共 **24 条以 `Signoff(陈忱)` 登记 `config.CRITERIA`**，引擎零写死数字；⑵ `judge_method` 分流：26 条规则引擎/关键词本批实装（含 M18 降级 3 条），25 条语义挂 `PendingSemantic` 待人工；红线①⑤关键词预筛只出「疑似」、②③恒「本批不验收」（判例 11）、④⑥可自动判 D（P1）；⑶ 判例 6 安全相关一律转人工（评分后、退回建议前）、ASIL C/D 抛 `AsilExcludedError`；判例 9 结构闸先于红线（用例守顺序）；⑷ 真实 8D 未入 git。⚠️ **QD-A 37.9% 如实标**：`feed_source.from_qda` 置信度原样带入，红线④仅 HIGH＋确认为空触发（P2 用例守）。

---

# §一 #612 · 外置段（2026-09-19 外置，工具第 2 批，3 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-A；外置前整格 4191 B、共 7 段；本批外置第 3、4、5 段；本格保留第 1、6、7 段。

✅ **⑴ 工程骨架**：`4-数字员工/质量部/Q2-8D报告AI判定/`（`q2_8d_verdict/` 10 模块＋`tests/`＋`data/rules/` V3.2 JSON 搬运件 sha256 `7907d784…` 用例守与正本一致＋`data/mock/samples.json` 11 份合成样本零真实文字；`.gitignore` `reports/`/`results/`/`data/golden/` `git check-ignore -v` 实测命中）。✅ **⑵ openspec 包** `openspec/changes/q2-8d-review-verdict-mvp/`（proposal 含知识资产三问＋四档口径／design D1–D8 🟡／tasks／spec），`openspec validate q2-8d-review-verdict-mvp --strict` ＝ valid。✅ **⑶ 档 1 mock 全绿**：`pytest -q --tb=short --maxfail=5` ＝ **12 passed**（手段＝同命令本泳道 worktree 内实跑）；输出 七维（D1–D7 逐条）＋百分制（自动得分＋语义待人工上界）＋A/B/C/D（语义未裁决时只出区间）；CLI `python -m q2_8d_verdict.run` 出 Markdown 清单＋审计 JSONL。✅ **⑷ `intent.md` 转写版 `status: 待确认`**（不代填）。

━━━

🟡 **停点＝design 审（Shao Peishen）**，两处须明确拍：**D3** M10 六组重复取 (a)（她答「逐组二选一」未逐字指 a/b）；**D4** D3-03 的 1 分并入新增 `D3-06` 作完整度基础分（总分仍 100）。

━━━

**未尽项**：语义层（25 条＋红线①⑤）档 1 无 LLM 实现、只留 `SemanticSource` 接口；验收集校准未跑（真实件不入库、LAN 无关但须她的 10 份原件）；`PENDING` 两条（措施可验证性标准＝台账 §3 开放点 1／语义层上线签认）未代填；持有人 backup 未代指派；未接 QD-A 真实解析；未 ff、未部署、未发信。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **8 段已于 2026-09-17 外置**（K2 口径，原文原样、可 grep，文件 md5:461856ec，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#612.md`；本格只留原第 1、9、11、12、13 段（共 13 段）＋本指针（外置人 CC-OP-0917-Q）。其中 8 段此前已在外置件内、本次未重复追加。

---

# §一 #612 · 外置段（2026-09-19 外置，工具第 3 批，4 段，原文原样）

> 外置人 OP-0919-O；外置前整格 5442 B、共 7 段；本批外置第 3、4、5、6 段；本格保留第 1、7 段。

📌 本行不销号：待 design 审答复后进 tasks §3。 ✅ Shao Peishen 2026-09-19 追认『Q2 8D design 审 D3/D4，通过』——design.md 已记『审过留痕：裁决人 Shao Peishen，2026-09-18，答复原文 1a 2a 3a 第 1 项；整表通过，未驳回任何一条 D』，D3（M10 六组取 a）／D4（D3-03 并入新增 D3-06）均按已签认口径引用，不再标推断。⇒ tasks §3 收口闸解除，转 🟢 候选，待建造棒续做（本条为看护者队列行同步，design.md 决策记录本身已由前一棒写入）。

━━━

🚀 **2026-09-19 派出 §5.1 门户页（Cowork 泳道看护 `OP-0919-A`，批 `B-0919_Q2建造`，泳道 `q2`·A2）**。

━━━

✅ **2026-09-19 `OP-0919-C` 完工**：tasks 5.1 门户页 `/quality/q2` 已落地——`q2_8d_verdict/webapp.py`（Flask 蓝图，首屏显著标注 mock、`X-Zp-Identity` 接入点只预留不实现、门禁用共享 `ZP_GATE_PASSWORD`）＋`dashboard.py`（七维 D1–D7 得分/满分聚合＋A/B/C/D 分级分布含语义层待人工区间桶＋处置建议分布）＋`scripts/run_q2_web.py`（默认只绑 127.0.0.1:8098）＋`tests/test_webapp.py`；回归 19 passed（原 12＋新增 7）；未动 §5.2（`.51` 部署，⏭️ 档不做）／§4／持有人/backup/发信。分支 `claude/op0919c-q2-portal` commit `521fdfde` 已 push；§二 批次 `B-0919_Q2门户页` 已登记 ✅，待 ZhuopinCommitSweep／待合分支巡检取活 ff master。场景 CLAUDE.md／tasks.md 已同步。

━━━

✅ **2026-09-19 §5.1 已 ff 入 master**（分支 `claude/op0919c-q2-portal`，rebase 后 `06633d4d`）（手段＝`工具-泳道分支合入.ps1` 六关全过、四 ref 一致，留痕 `1-转型规划/0-全景路线图/合入登记/ff-patrol-20260919.jsonl`；🔴 改用**窄测试目标**跑六关——传整个 `0-学习与工具` 目录时单条实测 26 分钟，换成对应 `test_*.py` 后单条 45 秒）。🔴 §5.2 `.51` 部署仍是 ⏭️ 档、未做；§4 语义层与验收集校准仍 LAN 留步。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **3 段已于 2026-09-19 外置**（K2 口径，原文原样、可 grep，文件 md5:4faff361，工具第 2 批）见 `1-转型规划/0-全景路线图/队列行日志/#612.md`；本格只留原第 1、6、7 段（共 7 段）＋本指针（外置人 Cowork-泳道看护-OP-0919-A）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。
