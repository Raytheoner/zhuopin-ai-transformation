---
title: "队列行日志 #419（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §一 #419 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §一 #419 · 外置前整格原文（2026-09-04 外置，9 段，md5:f4073e3e）

[S:blocked][D:机] 🔴 **2026-08-26 立行，无默认项——本项不设默认（守 §5「默认项两前提」）**：提出该项的 session 已收工、回合制无法自我唤醒 ⇒ 第①问答不出执行者；且「不答＝继续挂着」的代价是**一条安全网在生产上持续处于死状态**，属「错误继续发生」而非「停在原地」 ⇒ **两条前提均不成立，须他明确答复**。 ━━━ ✅ **本轮已做完的部分**：三条实证已取全并写入 3.3 追记；`sweep-ops-webhook-cutover` 已按 `--ack-stale-change` 打**机器认得**的标记（指纹 `30/31`，落 `reports/sweep-stale-change-ack.json`），**写后反查确认该包已不在 `_find_stale_in_flight_changes()` 告警名单**。 **刻意不用「暂不归档」**——那是作者对未来的永久声明、会把一个活的生产缺陷永久静音；**也不用「预期观察窗口：N 天」**——本项缺的是一次代码修改与他的一次拍板、**不是时间**。 ❌ **本轮未做**：不跑 `/opsx:archive`（3.3 实测不成立，归档即等于把一条死掉的安全网标成已验收，正是 6.5 原文「不假装完工」要挡的事）；不动 `alert_webhook.py` 代码（修法未定，越权）。  ━━━ 🔴 **WIP豁免：本行是一个「已被我亲手静音的告警」的唯一载体，不立即立行即等于销毁证据。** 本轮已对 `sweep-ops-webhook-cutover` 打 `--ack-stale-change` 标记，该包**从此不再触发 sweep 群内告警**；若因 WIP 满而不立本行，则「第三道防线在生产上是死的」这一实测结论**将同时失去自动提醒与台账两个载体**，退化为只存在于一次已收工 session 的会话记录里——**正是本项目当前正在治的「收到了、没落档」那个形态**（见变更包 tasks 5.2 末段：对他的答复做过同一件事）。**代价不对称**：多挂一条 WIP 的代价是台面拥挤，不立行的代价是一条安全网无声死着且无人再被提醒。 ⚠️ **另需他知悉**：本次拒绝时计数为 **24／22**，**扣除本行后仍为 23／22 —— 机制类 WIP 在本行之前就已超限**，属既存状态、非本行造成；本轮无头批处理无人在场，**不擅自关闭他人在办行**来腾位，该超限本身已一并登记，请总线在派发本行时一并处置。 **豁免开关**：`release --force-mechanism-wip`（已按工具要求两者齐备）。 　━━━ 🟢 **2026-08-27 `OP-0827-E`：本行的 LAN 留步已解除，且取证结果推翻了它的一个隐含前提** —— 原文两问＝「`.51` 上实际部署的是哪一版 `alert_webhook.py`、`C:\wecom-aibot\.env` 有无人工补过 `_OPS`」。**实测（on-LAN，SSH 只读）**：① `alert_webhook.py` 在 `.51` 的 `C:\` 下递归 6 层**零命中**；② `C:\wecom-aibot`／`C:\wecom-aibot-service`／`C:\aibot` **三个目录都不存在**，故 `C:\wecom-aibot\.env` 无从谈起；③ `.51` 的计划任务里**没有任何 aibot／wecom 相关项**（在跑的是 `BaoguanWebServer`／`CommandCenterWeb`／`Fi2WebServer`／`QdBWebServer`／`Sc2WebServer`／`ZhuopinRecruitAgent`，另两个 Ready 的是 `Fi2TaxExportDailyScan`／`Sc2WeeklyAutoPush`）。 🔑 **⇒ 两问的答案是同一个：`.51` 上根本没有部署 `wecom-aibot-service`**（与「本服务永不部署 `.51`」的既有拍板一致，见 §一 `#394`）。 🔴 **这不是一句「查不到」，它改变了 D2 的事实基础**：该服务的生产载体在**笔记本**上，所谓「扁平布局」要验的那台机器**不是 `.51`** ⇒ 他拍板 ① 修法（(a)/(b)/(c)）② 归属 两问时请以这一条为准，尤其 (c)「由 launcher 显式注入环境变量」的可行性取决于**笔记本侧**那个 launcher、而不是 `deploy-server.ps1`。**本班按纪律只登记、不代其改判。**　━━━　🔧 **2026-08-30 `OP-0829-W` 机制类 WIP 分诊改判：`[S:open]` → `[S:blocked]`**（Shao Peishen 2026-08-30 答「全批」）。**依据＝本行自陈原话**「待 Shao Peishen …无默认项——本项不设默认」——阻塞方不是「现在能动手的人」，不属可动负荷，故改判为 `blocked`。🔴 **只改状态字段，工作范围·优先级·领取方一字未动**；**刻意用 `blocked`／`timed=` 而不用 🛑** —— 三者都不计入可动 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 行内原话），会让本行从催办面整个消失；`blocked` 只是不占配额，仍可见可催。误判可一条命令改回。**根因（`partial` 被当作可动 ⇒ 主体做完留尾巴反而永久占配额）根治已并入 `#435` 子项 E。** ━━━ 🔎 **2026-08-31 `OP-0831-L` 第二段⑴复核：本行不因回 LAN 而解除**——现行 `[S:blocked]` 的阻塞理由（2026-08-30 `OP-0829-W` 改判依据自陈原话「须他明确答复」）是等 Shao Peishen 拍板修法方案（(a)/(b)/(c)）与归属两问，与 LAN 访问无关；被回 LAN 解除的是它更早的那一半（2026-08-27 已确认并写入本行：`.51` 根本没有部署 `wecom-aibot-service`，`alert_webhook.py`／`C:\wecom-aibot\.env` 的取证工作当时已完成，不需要也不会因本次回 LAN 产生任何新证据）。⇒ `[S:blocked]` 维持不变，本批不对本行执行任何新动作，仍等他拍板。　━━━　🔴 **2026-08-31 Shao Peishen 当场拍板（Cowork `OP-0831-M`）——本行阻塞解除，`[S:blocked]` → `[S:open]`** ━━━ **① 修法：选 (a)**（`sweep-ops-webhook-cutover/design.md` 决策点 4）——**本批同改 `alert_webhook.py` 代码，并如实标注「未生效直到执行体同步」**。**理由**：它只有两行，且 (a) 改法（改读仓库根 `.env`）**恰好绕开 (c) 需要的那个配置动作**——根 `.env` 里 `_OPS` 已就位且已验通。 🔴 **已知代价（拍板时已告知）**：本行 08-27 取证坐实 `.51` 上**根本没部署** `wecom-aibot-service`，该服务生产载体在**笔记本**；而笔记本执行体 2026-08-31 仍落后 master（`§一 #438` 执行体对齐未做）⇒ **选 (a) 改完，在 `#438` 完成前不会生效**。该代价由 `#438` 覆盖，不另立行。 **② 归属：机制线（读法／告警工具族）** —— 🔴 此条为 Cowork 建议、他未反对但亦未逐字确认，**如有异议当场改**；建议与 `§四 #133`（docx SDT 读法）同族一并派单。 **本次只拍板与改判状态字段，未动任何代码**；实现另行派单（属 CC 建造）。 　━━━　🔁 **状态分诊改判 `[S:open]` → `[S:blocked]`（2026-09-02，Cowork `OP-0902-A`；Shao Peishen 当日答 ①(a)，机制类可动 WIP 实测 24／22 超限）** —— **判据＝本行状态列自陈原文**：「＝本行自陈原话**「待 Shao Peishen …无默认项——本项不设默认」——阻塞方」⇒ 剩余动作**硬卡在「等 Shao Peishen 一次动作」这个外部条件**上，非本行执行者可推进；按闸自身口径（`STALE_STATUS_PHRASES`）应为 `blocked`，而 `blocked` 结构性不计入 WIP。 🔴 **改判不是关行、不降优先级、不改任何排序** —— 只是把「在等他」如实标进机器字段；**他答复后应立即改回原字段**。 ⚠️ **本次是第二轮**：2026-08-31 值周巡检已做过一次同类分诊（见 `#422` 行内，他当日定「**不调上限，低的是分诊执行率**」），但未形成常态 ⇒ 三天后再次超限。**常态化已立 `#454`**，本行不重复登记成因。

## 2026-09-09 项 2 · LAN 留步取证（CC `OP-0909-K`／批 `B-0909_506部署与419取证`）

看护件项 2 ＝「只读取证 `.51` 上 `alert_webhook.py` 与 `C:\wecom-aibot\.env` 有无人工补过 `_OPS`」。项 1 冒烟八段全过后才起，守「`.51` 一次只动一项、全串行」。

### 一、实测（全程只读）

| # | 手段 | 结果 |
|---|---|---|
| ⑴ | `Get-ChildItem C:\wecom-aibot` | **`PathNotFound`** —— 目录不存在 |
| ⑵ | `Get-ChildItem C:\ -Directory` | 顶层业务目录 ＝ `apps`／`baoguan`／`command-center`／`fi2`／`nginx-1.30.4`／`nssm`／`portal-gateway`／`qd-b`／`salesmarketing`／`sc2`／`supplychain`／`temp`／`tmp` —— **无 `wecom-aibot`**。`C:\apps` 下仅 `backups`／`zhuopin-recruit-agent` |
| ⑶ | `Get-ScheduledTask` 全量 | 业务任务 ＝ `BaoguanWebServer`／`CommandCenterWeb`／`ECUSalesDashboard`／`Fi2TaxExportDailyScan`／`Fi2WebServer`／`QdBWebServer`／`Sc2WebServer`／`Sc2WeeklyAutoPush`／`SupplyChainWebServer`／`UnifiedPortalGateway`／`ZhuopinRecruitAgent` —— **零 aibot／wecom 命中** |
| ⑷ | `Get-Service`（过滤 aibot／wecom／zhuopin／nssm） | **零命中** |
| ⑸ | 本机 `5-平台底座/wecom-aibot-service/deploy-server.ps1` 第 38-39 行 | `$APP = $PSScriptRoot`（`C:\wecom-aibot\app`）／`$BASE = Split-Path $APP -Parent`（`C:\wecom-aibot`）⇒ 实证目标基目录确为该路径，非我方猜测 |

**⇒ 两问答案**：

1. 「`.51` 上实际部署的是哪一版 `alert_webhook.py`」＝ **没有部署任何版本**。本机 master 版 SHA256 ＝ `43596895815B11E2B0BCB898EE158CA37EF4E4E6578BEC3B9431C9455A3BF7B5`，`.51` 上无对应件可比。
2. 「`C:\wecom-aibot\.env` 有无人工补过 `_OPS`」＝ **无** —— 该文件不存在。属看护件 §二 判据⑵ 明认的「有／无二者皆为有效取证结果」。

### 二、判据符合性

- **判据⑵（取到确定答案）**：✅ 达成，见上。
- **判据⑴（两文件 SHA256 前后一致，证明未改）**：⚠️ **不适用** —— 两文件均不存在，无从记录前后哈希。**替代证据 ＝ 全程只读**：本次对 `.51` 只用了 `Get-ChildItem`／`Get-FileHash`／`Get-ScheduledTask`／`Get-Service` 四条只读命令，**未执行任何写操作、未误触**，故看护件「若误触写操作 ⇒ 立即停链并如实报告」的条件未触发。
- **回滚**：只读，无需回滚。

### 三、🔴 本项是一次重做——零新证据（如实记，不掩盖）

`#419.md` 既有内容载明：

- **2026-08-27 `OP-0827-E`** 已在 on-LAN 下用 SSH 只读取过**同一组证据**，结论逐条相同：`alert_webhook.py` 在 `C:\` 下递归 6 层零命中；`C:\wecom-aibot`／`C:\wecom-aibot-service`／`C:\aibot` 三个目录都不存在；计划任务无任何 aibot／wecom 项。其结论已定性为「**`.51` 上根本没有部署 `wecom-aibot-service`**，与『本服务永不部署 `.51`』的既有拍板一致，见 §一 `#394`」。
- **2026-08-31 `OP-0831-L`** 第二段⑴复核已明写：本行「被回 LAN 解除的是它更早的那一半（2026-08-27 已确认并写入本行……**不需要也不会因本次回 LAN 产生任何新证据**）」。

⇒ **本次取证确为独立复现（四条命令自行实跑、非抄录既有结论），但产出为零新证据。** 两次结论一致，可视为对 08-27 结论的一次交叉验证。

**根因（本项为何还会被排进本批）**：`#419` 那条 **LAN 留步登记文本写于 2026-08-26**，其后 08-27 取证虽已完成并写入行内，**留步登记文本本身未同步更新**；未闭合产出扫描器与三源汇集据该旧文本判定「最老留步 >5 天、已 14 天」，遂入批。**这是登记文本与事实脱节的一个实例**，非扫描器故障。

### 四、🔴 另发现一处口径冲突（只标注、不代改判）

- **本行状态格首段（2026-08-26 立行原文）** 与 **本批看护件 §一 硬边界 5**、**opener「不做什么」** 三处均称：第①问「(a) 补扁平布局分支／(b) 双布局语义／(c) launcher 注入环境变量」**仍无默认、仍须 Shao Peishen 明确答复**。
- 而 `#419.md` 载明：**2026-08-31 Shao Peishen 当场拍板（Cowork `OP-0831-M`）——本行阻塞解除，`[S:blocked]` → `[S:open]`；① 修法：选 (a)**；② 归属：机制线（该条注明系 Cowork 建议、他未反对但亦未逐字确认）。其时已告知已知代价：本服务生产载体在**笔记本**而非 `.51`，笔记本执行体落后 master，选 (a) 改完在 `#438` 完成前不会生效。
- 其后 **2026-09-02 `OP-0902-A`** 的状态分诊，**判据取的正是首段那句过时自陈**（「待 Shao Peishen …无默认项——本项不设默认」），据以把 `[S:open]` 改回 `[S:blocked]`。

⇒ **行内首段与外置件相互矛盾**：按外置件，第①问 08-31 已答；按首段与本批看护件，它仍在等他。**本棒按纪律不代答、不改判、不动状态字段**，只如实标注，端给他一句话定。

### 五、边界

- 未动 `alert_webhook.py`、未动 `deploy-server.ps1`、未动 `openspec/changes/sweep-ops-webhook-cutover/`。
- 未触碰 `.51` 上任何其它服务，未改防火墙规则。

## 2026-09-09 再外置 2 段（K2 口径，`OP-0909-K`）

外置原因：本行状态格因回写他 2026-09-09 对第①问的答复而将超 `ROW_LENGTH_CAP_BYTES` 4096 B。两段原文原样如下。

### ⑴ 2026-09-02 状态分诊改判段（md5:`8542423e32e911a7c71656db699004ab`）

🔴 **该段判据已被 Shao Peishen 2026-09-09 判定为误改**——它取的是本行首段 2026-08-26 的自陈原话，而第①问 2026-08-31 已拍板。原文：

🔁 **状态分诊改判 `[S:open]` → `[S:blocked]`（2026-09-02，Cowork `OP-0902-A`；Shao Peishen 当日答 ①(a)，机制类可动 WIP 实测 24／22 超限）** —— **判据＝本行状态列自陈原文**：「＝本行自陈原话**「待 Shao Peishen …无默认项——本项不设默认」——阻塞方」⇒ 剩余动作**硬卡在「等 Shao Peishen 一次动作」这个外部条件**上，非本行执行者可推进；按闸自身口径（`STALE_STATUS_PHRASES`）应为 `blocked`，而 `blocked` 结构性不计入 WIP。 🔴 **改判不是关行、不降优先级、不改任何排序** —— 只是把「在等他」如实标进机器字段；**他答复后应立即改回原字段**。 ⚠️ **本次是第二轮**：2026-08-31 值周巡检已做过一次同类分诊（见 `#422` 行内，他当日定「**不调上限，低的是分诊执行率**」），但未形成常态 ⇒ 三天后再次超限。**常态化已立 `#454`**，本行不重复登记成因。

### ⑵ 2026-09-09 09:14 LAN 收口排批段（md5:`02a19df98bc4d0b6d188dc80922f96b9`）

该批 `B-0909_506部署与419取证` 已于当日执行完毕（项 1 部署八段全过、项 2 取证完成），故转为历史。原文：

📌 **2026-09-09 09:14 Shao Peishen 答 1a（由「LAN 收口提醒」定时只读巡检端出，Cowork 侧答定，🔴 标「已答，勿重做」）**：本行的 **LAN 留步取证部分**（`.51` 上实际部署的是哪版 `alert_webhook.py`、`C:\wecom-aibot\.env` 有无人工补过 `_OPS`）排进回内网后的收口批、**位列第 2**（第 1＝§一 `#506`，关键路径），触发词「**我已回Lan**」，**不带灰度上限**（他 09-09 当场指出已非首跑；首跑灰度 2026-09-07 批 `B-0907_Q` 已走完）。🔴 **只收取证、不代答本行第 ① 问修法**（(a) 补扁平布局分支／(b) 双布局语义／(c) launcher 注入环境变量）——该问**仍无默认、仍须他明确答复**，取证结果只是该问的输入。**本次提醒触发判据＝③ 最老留步 >5 天**：本行 LAN 留步登记 2026-08-26，距 2026-09-09 已 **14 天**。**实测留痕**：09-09 两次 `lan-status` 均 off-LAN ⇒ 当日未起批、未触碰 `.51`。


## 2026-09-10 再外置 2 段（K2 口径，`OP-0910-F`）

外置原因：本行状态格因回写 2026-09-10 修法落地而将超 `ROW_LENGTH_CAP_BYTES`（4096 B；
外置前实测 4022 B，`ROW_LENGTH_BLOCK_FROM=2026-09-11`，再不外置明日即由告警转阻断）。
两段原文原样如下，行内改留指针。

### ⑴ 2026-09-09 项 2 · LAN 留步取证结论段（md5:`b30fb471591c4c9156fae8d848d1244f`）

该两问（`.51` 上部署哪版 `alert_webhook.py`／`C:\wecom-aibot\.env` 有无 `_OPS`）已答毕，
答案是同一个：`.51` 上根本没有这套部署。其完整实测表见本文件「2026-09-09 项 2」一节，
本段是它在行内的摘要。原文：

✅ **2026-09-09 项 2 取证完成、全程只读**（CC `OP-0909-K`，项 1 全过后才起）。**结论＝`.51` 上根本没有 `wecom-aibot` 这套部署**（四处齐证：目录 `PathNotFound`／计划任务零命中／`Get-Service` 零命中／本机 `deploy-server.ps1` 38-39 行实证目标基目录）⇒ ① 部署哪版 `alert_webhook.py` ＝ **没有任何版本**；② `.env` 有无人工补过 `_OPS` ＝ **无**（文件不存在）。判据⑴ SHA256 前后一致**不适用**（文件不存在），替代证据＝全程只读四条 Get- 命令、无写操作。 🔴 **本项是一次重做、零新证据**：`队列行日志/#419.md` 载明 2026-08-27 `OP-0827-E` 已取过同一组证据、结论逐条相同，2026-08-31 `OP-0831-L` 更已明写「不会因本次回 LAN 产生任何新证据」。本次系**独立复现非抄录**。**根因＝登记文本与事实脱节**：留步登记写于 08-26、08-27 取证后未同步更新，扫描器据旧文本判「留步 14 天」入批，非扫描器故障。 🔴 **另发现口径冲突、只标注不代改判**：本行首段与本批看护件均称第①问「仍无默认、须他明答」，而 `#419.md` 载明 **2026-08-31 他已拍板 ① 选 (a)**（`OP-0831-M`），09-02 分诊却据首段过时自陈把 `[S:open]` 改回 `[S:blocked]`。**本棒不代答、不动状态字段。** 📎 全文见 `队列行日志/#419.md` 2026-09-09 节。

### ⑵ 2026-09-09 Shao Peishen 重答第①问段（md5:`db9df98b7d9ff6f3b8a56d0d35314982`）

该段是 ①(a) 的拍板留痕，2026-09-10 `OP-0910-F` 已按它落地实现（见下节）。原文：

✅ **2026-09-09 Shao Peishen 重答第①问＝(a)，本行阻塞解除 `[S:blocked]` → `[S:open]`**（CC `OP-0909-K`；他先答 1c＝就地重答、再答 `a`）。**① 修法定 (a)**：`find_repo_root()` 增补扁平布局分支（认 `<base>/zhuopin_platform` 兄弟目录）——与 2026-08-31 `OP-0831-M` 所选**同一条**，其理由（只两行、绕开 (c) 所需的配置动作、根 `.env` 里 `_OPS` 已就位已验通）**未被本日取证推翻**：`.51` 无该部署一事 08-27 即已确认，08-31 拍板时已作为已知代价告知。🔴 **② 归属仍未答**（就地补本包重开 3.3 ／ 另开变更包、本包以「3.3 移出范围」结案后再 archive），如实标注、不代答。🔴 **09-02 那次分诊经他本次判定为误改**：其判据取的是本格首段 2026-08-26 的自陈原话，而该题 08-31 已拍板、自陈早已过时⇒ 已答的题被机器重标成「在等他」，并致本日 `OP-0909-K` 依旧留步文本重做一次取证（零新证据）。**同批分诊是否还有同样被误改的行，须回头核。** ⏭️ **修法实现＝本行期望产出⑵，不另立行**（协议〇.10：另取新号即与本行重复）——改回 `[S:open]` 后待总线派发，本棒不代做。

## 2026-09-10 修法 (a) 落地（CC `OP-0910-F`，泳道分支 `claude/op0910f-419-ops-webhook`）

本节是行内 2026-09-10 段的全文载体；行内只留结论与指针。

### 一、范围与边界

- **范围 ＝ 队列行「期望产出⑵」的纯库内一半**：修法落地 ＋ 扁平布局非 mock 实测 ＋
  `deploy-server.ps1` 模板补 `_OPS` ＋ 生产载体 `.env` 落位**方案**。
- 🔴 **未做、也不该由本棒做**：未部署、未重启 `.51` 上任何服务、未触碰生产（`.51` 上本就
  没有这套部署，2026-08-27／2026-09-09 两次取证齐证）；未放任何凭据文件；未对外发送任何东西；
  未跑 `/opsx:archive`；未 ff 入 master（只 push 泳道分支）。
- **② 归属按他 2026-09-09 所答 (a)**：就地补进 `sweep-ops-webhook-cutover`、重开 3.3，
  **不另开变更包**。

### 二、改了什么

| 文件 | 改动 |
|---|---|
| `5-平台底座/wecom-aibot-service/scripts/alert_webhook.py` | `find_repo_root()` 改**两趟扫描**；新增 `FLAT_MARKER_PART`（从 `bootstrap.MARKER_PARTS` 派生）；模块 docstring 补 2026-09-10 段与 D1 未闭合说明 |
| `5-平台底座/wecom-aibot-service/deploy-server.ps1` | `.env` 模板补 `WECOM_WEBHOOK_URL_OPS`；新增部署期空值体检（红字告警、只告警不中止）；头部布局说明补取值来源 |
| `5-平台底座/wecom-aibot-service/tests/test_alert_webhook.py` | 新增 5 条用例（9 → 14） |
| `openspec/changes/sweep-ops-webhook-cutover/tasks.md` | 3.3／6.5 各补一段 2026-09-10 追记，原文一字不追改 |

🔴 **两趟扫描不得合并成一个循环逐层双判** —— 这是本次改动里唯一一处"写法本身就是正确性"的地方。
合并写法在 monorepo 里会给出**反向错误答案**：从 `<repo>/5-平台底座/wecom-aibot-service/scripts/`
往上走，**先到达 `5-平台底座` 这一层**，其下确有 `zhuopin_platform`，扁平判据在此**为真**⇒ 函数返回
`<repo>/5-平台底座`，正是 `#282` 拍板要弃用、裸键值长 0 且无 `_OPS` 的那份 `.env`。
**这不是"少认一种布局"，是"认回了被退役的那一份"**——已配用例
`test_monorepo优先于扁平不得认回被退役的那份env` 把次序锁死。

### 三、实测（非 mock）与手段

**判据「扁平布局下实测取到 `_OPS`（非 mock）」的做法**：照 `deploy-server.ps1` 第 38-39 行实证布局
在盘上搭真盘 `<base>/{app,zhuopin_platform,.env}` —— 把本仓库的 `wecom-aibot-service` 整份复制成
`<base>/app`、`5-平台底座/zhuopin_platform` 整份复制成 `<base>/zhuopin_platform`（`PYTHONPATH`
指向后者，等价于 `.51` 上的 `pip install -e "$PLATFORM[aibot]"`），`.env` 写进基目录，
**在独立子进程里真实 import 该副本并调 `resolve_alert_webhook()`**。不打桩、不 monkeypatch、
不构造假 `Path`；只调 `resolve_*`，**不调 `main()`**（不发任何真实消息）。

**对照跑（同一副盘、只换脚本）**：

| 组 | 脚本 | `find_repo_root()` | `resolve_alert_webhook()` |
|---|---|---|---|
| before | `git show HEAD:` 改前版 | `None` | `None` |
| after | 本次改后版 | `<base>` | 基目录 `.env` 里那条 `_OPS` |

before 那一半即 **D2 的第三次独立复现**（前两次＝2026-08-26 `OP-0826-H`、行内 ⑵）。

**验证手段清单（只有动作没有手段的自陈 ＝ 没有验证）**：

| # | 手段 | 结果 |
|---|---|---|
| ⑴ | `pytest tests/test_alert_webhook.py -q` | **14 passed**（新增 5 条） |
| ⑵ | `pytest tests -q -p no:randomly`（`wecom-aibot-service` 全量） | **824 passed, 1 skipped**，150.99s |
| ⑶ | `python 0-学习与工具/工具-引导样板lint.py` | 通过（625 个已跟踪 `.py`／69 个含引导块，零手抄形态）⇒ `#345` 样板 5 行确未被动 |
| ⑷ | `[System.Management.Automation.Language.Parser]::ParseFile`（**只解析、不执行**） | `deploy-server.ps1` 0 语法错误 |
| ⑸ | 字节级反查 `deploy-server.ps1` | UTF-8 BOM 保留、孤立 CR ＝ **0**（`#355` 判据）、CRLF 未变 |
| ⑹ | 提取并回显生成的 `.env` 模板正文 | 含 `WECOM_WEBHOOK_URL_OPS=`，且模板内无 `$`／反引号（双引号 here-string 不会转义出控制字符） |
| ⑺ | `openspec validate sweep-ops-webhook-cutover --strict` | `is valid` |
| ⑻ | `python -W error::SyntaxWarning` 编译 `alert_webhook.py` | 无告警（模块 docstring 含 Windows 路径，已改 raw 字符串） |

commit `523d20a`；分支 `claude/op0910f-419-ops-webhook` 已 push origin。

### 四、🔴 仍未闭合的一半（D1）——如实写明，不假装完工

本服务的生产载体是**笔记本**上的 worktree `ops/wecom-service-home`，它是 **monorepo 布局**
⇒ 走第一趟即返回自身 worktree 根，而 `.env` 属 `.gitignore` 件、**不随 worktree 生成**
⇒ 该载体上**仍取不到值**。**这是配置落位问题、不是代码缺陷，代码侧无从修。**

**落位方案（本棒只给方案、不执行 —— 放凭据文件是 Shao Peishen 本人的动作，代执行纪律不覆盖凭据）**：

1. 在 `ops/wecom-service-home` 的 **worktree 根**放一份 `.env`，**只写 `WECOM_WEBHOOK_URL_OPS`
   一个键**，值取主工作区 `.env` 里那条已验通的（去向指纹 `4bf10217`，§四 `#67` 探针 44004／
   `#72` `.51` 真冒烟 `errcode=0`）。
2. **不放整份主 `.env`** —— 另外三个业务群键（`_FINANCE`／`_QUALITY`／`_PROCUREMENT`）正是
   `#282` ⑵ 包点名要退役的，多复制一份就是多一处要退役的载体。
3. ⚠️ **已知代价（`design.md` 决策点 7(b) 早已点名，此处照抄不粉饰）**：这是**第二份凭据副本**，
   他下次轮换值时**这份会陈旧**，把一次性陷阱变成常设陷阱。本次新加的部署期空值体检**只覆盖
   `deploy-server.ps1` 那条路径、管不到笔记本这条**。
4. **根治方向（不在本棒范围，只留指针）**：若日后本服务改按扁平布局部署（新代码已支持），
   `<base>/.env` 就是唯一那一份，该陷阱随之消失。

### 五、6.5 `/opsx:archive` 仍不解锁

`tasks.md` 6.5 的解除条件 ⑷ 两条路径他都已答（②(a) ⇒ 不以"移出范围"结案；①(a) ⇒ 修法落地
且扁平布局实测通过）。**但仍缺两项，两项都不在本棒权限内**：

1. **D1 凭据落位** —— 见上节，须 Shao Peishen 本人放文件。
2. **5.2 的人眼半边** —— 采购群「没收到」须他本人确认，机器侧此前已穷尽。

⇒ **本棒不跑 `/opsx:archive`**（3.3 那类「已改未验证」未闭合即不得 archive，不假装完工）。

---

# §一 #419 · 第 2 批外置（2026-09-10，3 段，外置段md5:49c342af，外置前整格 4969 B／整格md5:b2255862）

> K2 口径，原文原样、未改一字（外置段 md5 全值 49c342af05bd9319fb3205d95c0b1fb7）。外置人＝CC `OP-0910-P`（派单件棒 2）；外置原因＝整格 UTF-8 实测 4969 B 超 4096 B 上限，2026-09-11 起行长闸由告警转阻断。行内保留首段＋末段＋指针，本节为被迁出的中间 3 段，段间分隔符 `━━━` 保持原样；行内改后 3920 B。

🔴 **2026-08-26 立行，无默认项——本项不设默认（守 §5「默认项两前提」）**：提出该项的 session 已收工、回合制无法自我唤醒 ⇒ 第①问答不出执行者；且「不答＝继续挂着」的代价是**一条安全网在生产上持续处于死状态**，属「错误继续发生」而非「停在原地」 ⇒ **两条前提均不成立，须他明确答复**。 ━━━ 📎 **K2 外置（原文原样、可 grep）统一见 `1-转型规划/0-全景路线图/队列行日志/#419.md`**：2026-09-04 历史回写 7 段 md5:`f4073e3e`；2026-09-09 2 段＝09-02 分诊改判段 md5:`8542423e`（其判据已被他 09-09 判定失效）＋ 09-09 09:14 LAN 收口排批段 md5:`02a19df9`（该批已执行完毕）；2026-09-10 2 段＝09-09 项 2 取证结论段 md5:`b30fb471`（两问已答毕：`.51` 上根本没有这套部署）＋ 09-09 ①修法重答段 md5:`db9df98b`（所定 (a) 已由 `OP-0910-F` 落地）。本格只留首段＋末段＋本指针。 ━━━ ✅ **2026-09-09 Shao Peishen 答第②问＝(a)：就地补进本包、重开 3.3**（CC `OP-0909-K`）⇒ **不另开变更包**；修法 (a) 落地后回写本行与 `sweep-ops-webhook-cutover` 任务 3.3／6.5，方可解锁 `/opsx:archive`。**本行两问至此全部答毕**，剩余＝实现（＝期望产出⑵），待总线派发。
