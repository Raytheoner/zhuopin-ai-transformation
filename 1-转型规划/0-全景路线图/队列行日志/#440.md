---
title: "队列行日志 · §一 #440 状态列历史回写段"
created: 2026-09-08
status: 生效
外置会话: Cowork 业务总线 OP-0908-A
---

# 队列行日志 · §一 `#440`


---

## 📎 2026-09-08 清账外置（`OP-0908-A`，回灌 `B-0908_B` 回写件同批，md5 `5632c204c9eacf8f666179324df17e9f`）

**并入审核（协议〇.10）已于取号前完成**（移交单 §2）：与 `#284`（规则退休制·阈值计数台账）同族但**不可并入**——`#284` 是 `[S:hold]` 🛑 常驻计数载体、不设完工态，承载不了建造任务；**eval 是它的供数方**，关系写在此处即可。 ━━━ 🔴 **本轮不给可动位的理由（值周巡检判定）**：本包有一处**须 design 审拍板的安全边界尚未定** —— 本仓库 CI **目前零 secret**（实测 `ci.yml` 中 `secrets.` 0 命中），跑 eval 需 `ANTHROPIC_API_KEY`，**是本仓库 CI 的第一个凭据**，而现行凭据纪律「只进 `.env`」未覆盖 CI secret 这一形态 ⇒ **拍板前立行也开不了工**。 ━━━ 🔴 **晋档判据不得放宽**：「每天在跑、全绿」不构成通过——同 `#82` 形状（探针通了 ≠ 机制通了）。 ━━━ **退休义务（协议〇.9 措施 B）**：本包上线时**不退休任何守卫**（eval 是元守卫，未验证前不得替换现役守卫），但 tasks 内写死可检验承诺——**上线后 4 周内以 eval 覆盖为依据对 ≥3 条人守规则执行退休判定，判「不退」须写明理由**。 ━━━ 📊 **供 `#284` 计数**：移交单 §5 登记同族违反 **+3**（形态＝开工前未查现状：R3 初稿未查 `followup_readme_bridge.py` 已存在／取号未查全库已用 OP 号致撞 `OP-0830-B`,`-C`／未查 `#366` 当前状态沿用 7 天前快照）——**这三条正是本包 eval-7 那道题的真实用例，不是假想题**。 　━━━　🔒 **Shao Peishen 2026-08-31 答《本周计划-2026-08-31》**：**立 🛑 不占位已确认**，理由同 `#439`。🔑 **他补的那条洞察同样适用于本行且更贴切**：**占位会在 WIP 压力下被催开工，而本行缺的是 design 审前置（CI 第一个凭据的安全边界）、不是位子。** ⇒ 去 🛑 前须先有该 design 审结论，**不得因 WIP 空出来就派**。━━━ 📐 **propose＋design 草案已落包，待 design 审（CC `OP-0908-D`，2026-09-08，批 `B-0908_B`，泳道 `440-eval-suite-draft`）**：变更包 `openspec/changes/discipline-eval-suite/`（`proposal.md`／`design.md`／`tasks.md`／delta spec `specs/discipline-eval-suite/spec.md` 9 条 ADDED Requirements）；`openspec validate discipline-eval-suite --strict` ＝ **valid（退出码 0）**，`openspec validate --all --strict` ＝ **182 passed／0 failed（退出码 0）**；分支 `claude/op0908d-eval-suite`，**未合入 master**。🔴 **按本行「第一棒只到 propose＋design」，`evals/` 下零文件、`.github/workflows/` 零改动、既有 hook／lint／业务代码零改动。** ━━━ 🔴 **三处推翻本行原述与草案的实测（2026-09-08 现测，全部附手段）** ━━━ ⑴ **「不自建 harness、复用 `skill-creator` `scripts/run_eval.py`、只写题不写跑法」不成立**：读该文件 docstring 原文＝「Run **trigger** evaluation for a skill **description**. Tests whether a skill's description causes Claude to **trigger (read the skill)**」，其 `main()` 的 `add_argument` 全集为 `--eval-set/--skill-path/--description/--num-workers/--timeout/--runs-per-query/--trigger-threshold/--model/--verbose`，**无任何参数吃 `evals.json`、不产 `grading.json`**；`SKILL.md:225` 写明行为评估是**由 agent 编排**（spawn grader subagent 读 `agents/grader.md`）⇒ **没有任何脚本把一条 eval 端到端跑完**。可复用的实际只有四件（schema／`agents/grader.md`／`aggregate_benchmark.py`／`eval-viewer/`），**编排层必须自己出**。 ⑵ **eval-7 第 4 条断言已成假题**：本行输入指针的草案写死「指出该模块虽已合入 master 但生产执行体未对齐」，而 `#438` 实测 `[S:done]`（2026-08-30 `OP-0830-F` 收口，`rev-list --left-right --count` ＝ `0 0`）⇒ 照抄即造出一条永远判错的断言。**草案自己预警过这一条，9 天后它真的发生了** —— 🔑 **这正是 eval-7 那道题要考的错，犯错的是出题人本人**，已作为该题的**第 4 例真实用例**写进包内。 ⑶ 🔴 **eval-5 的注入回归靶点不存在**：`grep -rn 'ERRORLEVEL'` 于 `CLAUDE.md`／`.claude/rules/`／`0-学习与工具/取证方法知识库.md` → **零命中**；`grep -rn 'OP-0819-F'` 同样零命中；全库 `grep -rln 'ERRORLEVEL' --include=*.md` → 13 个文件**全是叙事件**（CHANGELOG／队列／草案／派单件），**无一是纪律载体**。仅存压缩残影 `CLAUDE.md:60`「管道末端的退出码不是命令的退出码」与 `.claude/rules/队列与落库.md:16`（讲 `$LASTEXITCODE` 与管道，**不含解析期展开这个形态**）。 ━━━ 🔑 **⑶ 引出本包相对草案的实质增值**：**那条纪律没有被退休，是被瘦身瘦掉的** —— 规则退休制管「被违反 3 次 ⇒ 机制化或删除」，**它管不住「被压缩时顺手蒸发」**。故本包新增件 `evals/rules-locus.json`（每题一条「载体路径＋锚点字符串＋最后校验日期」），**runner 启动先校验锚点仍在，不在即 fail-loud 判红、不得跳过后报绿** ⇒ **把「这条纪律今天还在不在库」本身变成一条持续跑的断言**，守的正是此前无人守的那个失效形态。 ━━━ 🆕 **发现第三条路（草案写作时未查到）**：`claude plugin eval` 是本机 CLI 一等命令（`claude --version` ＝ **2.1.260**，`--help` 全文已读）：读 `evals/**/case.yaml`；**`--ablation with-without` 自带 no-plugin 基线臂**（正是草案 §9 开放点 3「对照组怎么定义」要的东西，官方已内置）；`--runs`（默认 3）；**`--threshold` 低于即 exit 1**（CI 门禁天然可用）；**`--max-cost-usd` 触顶 exit 2**（直接回答「额度上限与超支行为」）；`--judge-model` 默认 haiku。**错配须先答**：它评的是一个 plugin，而本包评的是仓库纪律 ⇒ design 决策点 1 给了 A/B/C 三候选与代价，**本泳道建议 A2 起步、A1 二期，但不预定**。 ━━━ 🔴 **一条合规发现，无论选哪条路都成立**：`claude plugin eval` 的 `--publish-report` 自述「**already the default when your account supports it**」，`--no-publish` 才本地留存 ⇒ **默认会把含 prompts、grader 判词与模型输出的 HTML 报告发到 claude.ai**，而本包夹具含队列行片段与内部纪律原文。**建议一律 `--no-publish` 并在 CI 里写死，不得依赖默认值不变**（已写进 spec）。 ━━━ **本行拍板点（CI 第一个凭据）的现状已复测**：`grep -c 'secrets\.' .github/workflows/ci.yml` → **0**（退出码 1）；`ls .github/workflows/` → **只有 `ci.yml`**；11 处 `runs-on` 全 `windows-latest`；`"on":` 仅 `push:`／`pull_request:`（**无 path 过滤、无 schedule**）。🆕 **`.env.example:36` 实测已有 `ANTHROPIC_API_KEY=` 空占位** ⇒ **本机侧「只进 `.env`」已成立，缺的只是 CI 侧口径**。design 决策点 2 拆成四子问（repo vs 组织 secret／额度上限与超支行为／爆炸半径与轮换／**与既有 `secret-scan` job 的关系**）各给候选与代价；第四子问按 `工具-密钥扫描lint.py` 的既有豁免推**应放行，但已写死落包时必须实跑坐实——推断不算实测**。 ━━━ **`.gitignore` 覆盖已逐条实跑 `git check-ignore -v`（6 条探针，退出码全留痕）**：`evals/evals.json`、`evals/files/*` **未被忽略**（退出码 1，符合预期，黄金集必须入库）；`reports/**`、`evals/__pycache__/**` **已被覆盖**（退出码 0，命中 `.gitignore:50` `**/reports/` 与 `.gitignore:18` `__pycache__/`）；配套 `git ls-files reports` → 零输出。🔴 **`.claude/commands/x.md` 未被忽略（退出码 1）**，而 `run_eval.py` 源码 L53-L62 会往该目录写临时命令文件 ⇒ **`#322` 形状的风险**（改名退路凭空造出 `*.editlock.mutex.stale`，企微群连响 17.1 小时）；本包一期不用它、故不产生该形态，但已写死：若选中的跑法会落该文件，`.gitignore` 必须同批补规则并实测坐实，**「我们不会跑它」不得作为不补的理由**。 ━━━ **守卫盘点三项复测**：`0-学习与工具/hooks/sentinel-pronoun.ps1` 在；`followup_readme_bridge.py` 实现＋测试在库；`openspec/changes/editlock-write-side-date-guard/tasks.md` ＝ **`[x]` 0／`[ ]` 29（未开工）** ⇒ eval-6 仍是唯一守卫。**eval-7 靶点可定位性**：`find ~/.claude -iname '*grill*'` → **零结果**，插件市场 `anthropic-agent-skills/skills/` 20 个 skill **无 `zhuopin-*`** ⇒ 该 skill 规则本体不可编辑，**在库唯一可打的靶＝`.claude/rules/场景建造与合规.md:20`**。 ━━━ **退休义务（协议〇.9 措施 B）已按本行要求写进 tasks §6.5**：上线后 4 周内以 eval 覆盖为依据对 ≥3 条人守规则执行退休判定，判「不退」须写明理由；本包上线时不退休任何守卫，理由（eval 是元守卫，未验证前不得替换现役守卫）＋ 反向核实（§1.1 表中 1/2/3/4 四条现役守卫无一能被本期三题覆盖）均已成文。 ━━━ 📊 **供 `#284` 计数：同族违反由 +3 追加为 +4**（新增第 4 例＝草案作者未复测 `#438` 即写死断言，形态同为「开工前未查现状」）—— **已过退休制阈值（3 次）**，请值周巡检判定处置。 ━━━ ⚠️ **另有一项超出本包范围、须另裁**：`%ERRORLEVEL%` 那条纪律的在库载体已消失，**是否补回 `.claude/rules/两桌同步与取证.md` §二「工具静默回退」族** —— 它是一条通用工程纪律、不只是 eval 的靶，且**是 eval-5 注入回归（晋档 2 第 3 条）的硬前置**。 ━━━ ▶ **下一棒 ＝ design 审**（🟡 档，本泳道按纪律止步未进）。**七个决策点全部未签认**（1 跑法／2 CI 凭据边界／3 判分粒度／4 阈值与采样／5 CI 落点／6 三条纪律的注入靶点／7 报告对外传输边界），逐点已给候选与代价、**均未预定**。🔴 **决策点 1 未签认前 `evals/` 下不得落任何 case 文件** —— 候选 A 的 `case.yaml` 与候选 B 的 `evals.json` 是两套不兼容 schema，先写就是赌。审通过前不得开工 tasks §3 起任何一项。

## 2026-09-09 迁入（OP-0909-I，K2 行长收口；原文原样，未改写）

> 迁出成因：Shao Peishen 答 1a/2a/3a 后须在行内落 13 项结论，行内余量不足。

### ⑴ 原「propose＋design 草案已落包」段

📐 **propose＋design 草案已落包，待 design 审（CC `OP-0908-D`，2026-09-08，批 `B-0908_B`，泳道 `440-eval-suite-draft`）**：变更包 `openspec/changes/discipline-eval-suite/`（`proposal.md`／`design.md`／`tasks.md`／delta spec `specs/discipline-eval-suite/spec.md` 9 条 ADDED Requirements）；`openspec validate discipline-eval-suite --strict` ＝ **valid（退出码 0）**，…

### ⑵ 原「2026-09-08 机器字段据实改判」段

🔎 **2026-09-08 机器字段据实改判 → `partial`**（Cowork 业务总线 `OP-0908-A` 清账，Shao Peishen 答 a）：本轮范围＝propose＋design 起草、**止步 design 审**（回写件自陈）⇒ 建造已完成而审未过。🔴 **成果已在 master 而行仍标 `open` ＝ 失真**——不清这笔账，下一批扫池会把已完成的活当候选重派（同本周计划 D-⑩ 形态）。

### ⑶ 原「2026-09-09 答 1a 加派审读件泳道」段

✅ **2026-09-09 Shao Peishen 答 1a：本批加派一条泳道先出 design 审读件**（Cowork 业务总线 `OP-0908-V`，泳道 `440-eval-review`，批 `B-0909_两包apply`，opener `OP-0909-I`）。**范围＝只读该包四件、出一份可直接答字母的审读件**（逐决策点 (a)/(b)/(c) ＋ 各自代价 ＋ 默认项；无默认项的须明写不答的代价是「停在原地」还是「错误继续发生」），🔴 **不替他审、不自行取值、不 apply、不改该变更包一个字节**。**成因**：propose＋design 自 2026-09-08 交付后一直挂着没审，而不审就不能 apply、本行排不上。审读件落点 `1-转型规划/0-全景路线图/design审读件-discipline-eval-suite-2026-09-09.md`。

### ⑷ 原「design 审读件已出」段（行内压缩为摘要，此为原文）

📋 **2026-09-09 design 审读件已出（CC `OP-0909-I`，泳道 `440-eval-review`，批 `B-0909_440审读件`）**：落 `1-转型规划/0-全景路线图/design审读件-discipline-eval-suite-2026-09-09.md`（commit `3df08b0`，已 push 泳道分支 `claude/op0909i-eval-suite-review-1a4f02`，🔴 **未 ff master**，由收尾段串行做）。**七个决策点拆成 13 个可答字母项**（(a)/(b)/(c) ＋「选它会发生什么与代价」＋ 默认项；候选取自 design 原文未作取舍；CI 凭据边界四子问＝第 2/3/4/5 项，原样转达）。🔴 **两项按根 `CLAUDE.md` §5 标「本项无默认」**：第 2 项（repo vs 组织 secret）执行体只能是他本人（apply 泳道写得出 workflow、写不出 GitHub secret），不答＝**停在原地**；第 9 项（`%ERRORLEVEL%` 纪律是否补回 `.claude/rules/两桌同步与取证.md`）不答＝**错误继续发生**（09-09 复测仍零命中，与本包是否落地无关）。🔎 **复测 design 的 12 条实测断言：11 条仍成立，1 条行号已漂**——eval-6 靶点由 `:25` 变 `:26`（锚点字符串未改）⇒ 恰是该包 `evals/rules-locus.json` 那条 spec 的当日实证，**坐标表须按锚点字符串定位、不得按行号**。🔴 **未改该变更包一个字节、未建 `evals/`、未改 CI、未 apply。** ⏭️ **待 Shao Peishen 答 13 项字母（模板在审读件 §四）→ 总线派 apply 泳道**；第 9 项若答 (a) 可由环境线独立执行、不必等 apply。

【机器字段解析】状态＝partial，域 机

## 2026-09-09 迁入（OP-0909-I，第二轮 K2；原文原样，未改写）

> 迁出成因：他答 1a 后凭据形态改判，须在行内落新结论，行内余量仅 51 B。

### ⑸ 原「答 1a，2a，3a 十三项结论」段全文

✅ **2026-09-09 Shao Peishen 答 `1a，2a，3a`——13 项全部定案，design 审通过**（落点＝CC `OP-0909-I` 本 session，按「谁端出来的题就在谁那儿答」）。⑴ harness ＝ **A2**（官方 `claude plugin eval`，`--ablation none`；A1 二期）；⑵ CI secret ＝ **repo secret**（🔴 **须他本人在 GitHub 建**，apply 泳道写得出 workflow、写不出 secret）；⑶ 成本 ＝ `--max-cost-usd` **硬闸**，`exit 2`／`exit 1` 分两种红；⑷ 轮换 ＝ **照抄 `OP-0819-F`**（值不回显、只落 SHA256 前 8 位指纹）；⑸ 不预改凭据扫描器，落包时实跑 `工具-密钥扫描lint.py` 坐实；⑹ 判分 ＝ **断言级＋反例**；⑺ 阈值 ＝ 先跑 5 轮取均值−1σ，一期不设 1.0、**不阻断合并**；⑻ **新建独立 workflow** 并自带 `PYTHONUTF8`；⑼ eval-5 靶点 ＝ **(a) 先补回纪律再打靶**；⑽ eval-6 靶点 ＝ rules 正本「🔴 写侧」句；⑾ eval-7 靶点 ＝ `.claude/rules/场景建造与合规.md:20`；⑿ eval-7 断言 4 改成不绑定易变状态；⒀ **一律 `--no-publish`** 且写死在 workflow 里。 🔧 **⑼ 已当场执行**：`.claude/rules/两桌同步与取证.md` §三 补回「退出码只认被执行进程自己那一层」（commit `1f48413`，紧随同族「工具静默回退」条，写清解析期展开机制＋两种正确写法＋管道那一层）⇒ eval-5 **自此有靶**，晋档 2 第 3 条硬前置解除，属「先有网再有规则」第一条。🔴 **两笔仍在泳道分支 `claude/op0909i-eval-suite-review-1a4f02`，未 ff master。** ⏭️ **下一棒＝总线派 apply 泳道**（tasks §0 七项可全部勾销，§3–§6 解锁）；**他本人只剩一件动作＝建 repo secret `ANTHROPIC_API_KEY`**。

### ⑹ 原「apply 泳道暂缓」段全文（含「以前没建 secret 也在推进」的追问答复）

⏸️ **2026-09-09 Shao Peishen 答 a：apply 泳道暂缓，他先建 repo secret**（`ANTHROPIC_API_KEY`，仓库 Settings → Secrets and variables → Actions）。🔴 **本行「下一棒＝总线派 apply 泳道」暂不生效——扫池／值周巡检见本行勿按前段行文派 apply**；secret 就位后由他一句话解冻，届时 apply 一棒做到 tasks §5 CI 集成完（他答 a 的取舍＝不分两棒、宁可等）。📌 顺带核实并答复了他的追问「以前没建也在推进、这次为何要」：仓库既有模型调用（`5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/crm_notifier/draft.py:120`）**无 key 即降级模板**，`.env.example:36` 自陈「可选；缺省降级模板」，且 CI 现有 11 个 job 全是确定性 python/npm 校验、无一调模型 ⇒ 至今零 secret 成立。**eval 是第一个「不调模型就等于没跑」的作业**，故须真凭据。
