---
title: "队列行日志 #397（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #397 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #397 · 外置前整格原文（2026-09-04 外置，9 段）

[S:blocked][D:机] 🟡 **propose＋design 已出件，停在 design 审（2026-08-25，CC 无头批处理 A27，worktree `a27-opener-archive-precheck`，分支 `claude/a27-opener-archive-precheck`，commit `d7cf69d` 已 push、未合 master，`openspec validate --strict` 通过）** —— 变更包 `openspec/changes/opener-batch-archive-precheck/`（proposal／design／tasks ＋ `opener-batch-archive-precheck`／`queue-query-archive-lookup` 两份 spec）。🔴 **未 apply、未改任何一行 runner 源码**（opener 明令「只 propose＋design 出件、登记 §四 待审即停」）。**四项判据/口径全部无默认项**（提出方是无人在场的无头批处理会话、收工即结束，按根 CLAUDE.md §5「答完之后由谁动手」判据答不出执行者），**已登记 §四 #117 待签认**；决策点 4／5 属实现细节、已给建议 (a)，不答即按建议执行。 ⚠️ **本行按协议〇.9 三类排除之②（依赖外部方——等签认，等人不等我们）转 `[S:blocked]`**，不计入可动 WIP（沿用 #353 已开的先例）。 ━━━ 🔑 **design 阶段的取证推翻了两条最自然的实现路径，三句** ━━━ **⑴ 按规范去读那个字段，会把白跑一字不差地重演一遍。** 归档件里 `#368` 那一行，销号信息写在**任务列**（`cells[1]`＝`✅ **[S:done] 整行销号（2026-08-22…）**`），而**状态列 `cells[5]` 原封不动停留在 `[S:open][D:业] 🆕 2026-08-21 立行，未开工`** —— 按 `queue-row-status-field` spec 的规范位置读机器字段，对 `#368` 得到的答案是 **open** ⇒ **一个「正确实现了规范」的校验器，在真实数据上会完全失效、A4 白跑原样重演。** ⇒ 改判：**归档件里「行在哪」比「行说自己是什么」可信** —— 迁归档由值周清扫执行，**迁移这个动作本身就是经人工复核的完成判定**；而状态列是否被同步更新**无任何机制保证**（本行即反例）。故判据定为「归档命中即已完成，不解析归档行任何单元格的状态内容」。 **⑵ `#368` 在归档件里出现 7 次，其中没有一次是它自己那一行。** `grep '#368'` 命中 `归档-202608.md` 的 533/535/902/904/906/912/1028 行，**全是别的行在正文里提到它**；而它自己在**第 583 行**、首列写作裸 `368`、**不带 `#`** ⇒ 子串检索**同时**产生假阳性（7 条无关行）与假阴性（搜不到目标行本身）。判据必须是「§一 表内**首格数值相等**」＋章节归属判定 —— 🔴 **§一 与 §四 是两套独立编号**（归档件里 `§四 #88` 与 `§一 #368` 同时存在且毫不相干），锚点抽取须先剥掉 `§四 #N`。 **⑶ 现有查询工具「查不到」时退出码是 0。** 实测 `工具-队列查询.py --row 368 --section 一` 输出「✗ 未找到…」而 **`exit=0`**，且它**只查在办两份、不查归档** —— 正是本项目「工具静默回退」族的教科书形态 ⇒ 新挂点一律**只读 JSON 的 `done` 字段，不解析中文文案、不依赖退出码**。 ━━━ **判据要点（详见 design 六个决策点）** ━━━ **四态**：live 命中且非 done ⇒ 派出；live 命中且 `[S:done]` ⇒ 跳过（**这一态不能省** —— 反例 `#395` 已 done 却仍在 live、从未进归档，只查归档会漏掉最长 7 天的清扫窗口）；live 未命中而归档命中 ⇒ 跳过；**两侧都未命中 ⇒ 🔴 照常派出 ＋ 告警**。 **底线取向**：**误派会被人发现（有人在读那个 session），误跳过不会有任何人在场** ⇒ 一切模糊地带一律 **fail-open**，绝不 fail-closed。 **锚点**沿用 opener 标题现成的 `——队列 #NNN`（两个 runner 的解析正则早已把它捕获进 `$title`，**不需要改解析代码、不给计划文件新增必填字段**——新增必填字段等于新增一条人守，与本包立意自相矛盾）；多行号取**合取**（全部已完成才跳过）。 **共同挂点**＝解析完成、`-Only` 过滤之后，**任何派出/分组动作之前**：v1 在 L76 之后、v2 在 L60 之后且**必须早于 L62 泳道分组**（否则泳道清单会显示实际不跑的成员、跳空的泳道仍会 `Start-Job` 并白等一次 90s 错峰）；共用新增 `0-学习与工具/工具-opener派出前校验.ps1`，判据本体走 Python 复用 `queue_table.split_row_cells`（#314 反引号游程教训）与逐份解析后合并（#312「绝不拼接文本再解析一次」，归档-202608.md 单文件内就有 4 个 §一 表）。 🔴 **最易踩的一个坑已写进 spec 与单测**：被跳过的 opener **不产生 `OPENER_DONE` 哨兵** ⇒ 若不在状态计算**之前**短路，会被判 `NO-SENTINEL` ⇒ **v1 整批中断、v2 停掉整条泳道**。 ━━━ **取证附产**：live ∪ archive 共 **392** 个 §一 行（在办 61 ＝ 机制 39 ＋ 业务 22；归档 331 ＝ 202607 的 76 ＋ 202608 的 255），**live∩archive 冲突 0 个、归档内重复 0 个** ⇒ 迁归档是移动而非复制、编号不复用，**按行号查可靠**，不需要 (文件,行号) 复合键。 **#395 并入项已覆盖**：v1 第 130 行提示改 `-Command` 形态（`-File` 下 PowerShell 不拆逗号 ⇒ 照抄必得 `exit 12`），列为 tasks 4.5。 **决策点 6（派单件 status 义务归属）**：建议 **(a) 义务归执行该派单件的 session**（与队列行回写同批同锁）**＋ (b) sweep 常驻告警兜底**（机器只保证「失真会产生信号」，不代替判断）；**明确排除 (c) 由本校验器顺带查** —— opener 标题不含派单件路径，只能靠文件名模糊匹配，**模糊匹配 ⇒ 误跳过 ⇒ 触碰本包 fail-open 底线**。🔴 **建议 (b) 另立一行紧跟 #396、不并入本包**（它依赖 #396 普查产出的 9 份真实样本来标定告警口径，与 #397 依赖 #396 出样本是同一个道理）。 **另附带发现**（不在本包范围，建议登记）：归档 `#368` 的状态列与任务列自相矛盾，属数据质量缺陷，建议提请值周清扫在迁移时同步状态列；本包判据刻意设计成不依赖它，**修不修都不影响正确性**。 ━━━ 以下为原登记 ━━━ [S:open][D:机] 待领 —— 依赖 #396 提供样本（软序，可先出 proposal）—— 来源＝2026-08-24 夜 opener 批处理 A1-A14 全 14 项跑完（run 20260824-165657）；登记时间 2026-08-25 01:0x ➕ **2026-08-25 范围追加（Cowork 环境总线，Shao Peishen 当场答「要」）：`【设置】` 行 worktree 字段格式 lint**——正本 `1-转型规划/0-全景路线图/专线opener模板库.md` §〇 定的是 **`☑＝CC 桌勾 worktree`／`☐＝不勾`**，模板三写法 `worktree：☑（〈任务短名〉，新 worktree，收工自删）`。🔴 **实测已系统性漂移**：`建造波次-2026-08-25-泳道版.md` 11 条 opener 的该字段**无一使用 ☑/☐**——9 条「新建独立」、1 条「按需新建独立」、1 条「不新建」；**这 11 条已被无头批处理全部派出并跑完，全程无人拦、无任何信号**。⇒ **建议判据**：派出前校验每条 opener 的 `【设置】` 行 worktree 字段**必须以 `☑` 或 `☐` 起头**，否则拒绝派出并指名违规编号。🔑 **归因**：这是「能从字面猜出来、猜错了也不会当场报错」那一族——「新建独立」读起来完全合理，CC 也照做了，所以能一路漂到今天（同族＝人名性别、工具静默回退）。**与本行既有范围同一挂点**（都在 v1/v2 runner 的「派出前」这一步），故并入而非另立行；🔴 **但本行 design 已出件待审（§四 #117）⇒ 本项属 design 审时的追加裁决点，不得由 apply 方自行实现。**  ━━━ ✅ **2026-08-27 propose＋design 出件已合入 master（CC-OP0827D，`OP-0827-D` 形态 2 处置，cherry-pick `39324eb`，已 push）**——原卡在分支 `claude/a27-opener-archive-precheck`（落后 205 个提交）。**逐条验过再合，非盲合**：`git merge-tree --write-tree` 干跑**零冲突**；该提交只含 `openspec/changes/opener-batch-archive-precheck/` 5 个新文件、653 行、**纯文档零代码**，master 上原本 0 命中（不是重排副本）；合后 `openspec validate --all --strict` **118 passed / 0 failed**。🔴 **合入 ≠ 已批准**：`tasks.md` 的 **0.1 design 审（阻塞项）仍未过**，合进来只是把它放到能被审的地方，不代表可进 1.x。
