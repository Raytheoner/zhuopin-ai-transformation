---
title: "队列行日志 · §一 #501 状态列历史回写段"
created: 2026-09-08
status: 生效
外置会话: Cowork 业务总线 OP-0908-A
---

# 队列行日志 · §一 `#501`


---

## 📎 2026-09-08 清账外置（`OP-0908-A`，回灌 `B-0908_B` 回写件同批，md5 `9f153c38228212c34f0cd6fc87115b87`）

━━━ ✅ **⑴⑵ 两子项均已完成（泳道收工，本行可整体销号）**（CC `OP-0908-F`，2026-09-08，批 `B-0908_B` wave1，泳道 `501-readme-rawcell-mode`，分支 `claude/op0908f-readme-rawcell`；本泳道按看护者硬口径不碰主仓 git、不 ff master，本段为「队列回写待补」挂起稿，须由看护者/sweep 回灌）。 ━━━ **⑴ 整格原文只读模式已落地**：`0-学习与工具/工具-跟进信README查询.py` 新增 `--row <编号>`（＋`--field <列名|all>`／`--json`）——单行、逐字、零截断输出该行各格原文，每格附字符数／UTF-8 字节数／sha256，供改写长状态格前取证。**刻意不是放宽 `--digest-width`**（该参数对状态列本就无效：状态前缀后的延续文本走独立常量 `STATUS_CONTINUATION_WIDTH`），**也不是通读后门**——一次只出一行且必须给确切编号，读侧禁通读钩子的立法目的不受影响；`--digest` 既有行为与 `--digest --json` 下游契约（sweep 第 4 类／`工具-使用度量采样.py`）逐字不变。**行匹配判据不复刻 `set-status`**：先字面相等、无命中才回落 `parse_letter_number` 解析值相等，且任一趟命中多行即报错（`set-status` 会静默取第一行），输出回印「编号格字面值」供改写者原样喂回。 ━━━ **⑵ `质量部#7` 发送状态格已改写**（走 `set-status`，未裸手 Edit；编辑锁 acquire→release 均已完成，现为无锁）：被**引用**的那句逃生阀口令由**直引字面取值**改为**符号名引用** `followup_gate.SERIAL_WAIVER_MARKER`＋非空理由原文，并追加一段「为什么改」的留痕。改写后 `followup_gate.serial_waiver_reason()` 对该行 6 格返回 `None`（改写前返回非空 ⇒ 机器眼里的有效开闸口令，`#482` D5(a) 后残余风险非零那一处已清零）。 ━━━ **核验手段与实测**：`0-学习与工具/test_工具-跟进信README查询.py` 新增一个测试类 15 例（含 `#501` 要求的正反用例：`--row` 逐字还原超长状态格 ／ 同一格 `--digest` 即便 `--digest-width 3000` 仍截断且与默认宽度输出**逐字相同**），该文件 **27 passed**；四份 README 工具单测合计 **131 passed / 8 subtests**（查询・登记・行长外置・归档）；读侧钩子 `test_hooks-pretooluse-queue-read-guard.py` **36 passed**；sweep 侧 `-k "followup or 跟进 or digest"` **14 passed**。README 侧：`工具-跟进信README查询.py --digest --json` ＝ **45 行 / malformed 0**；`工具-跟进闸查询.py --all` **退出码 0**、质量部闸仍 ✅ 开（依据 `质量部#13`，与 `#7` 无关）；`set-status` 自带写后回读比对通过、release 校验通过，另独立再跑一轮 acquire→release 校验亦通过。改写前 919 B ／ `sha256:ed5059b844d3c5bd…`；改写后 1876 B ／ `sha256:20a8429deff9037f…`（远低于 `ROW_LENGTH_CAP_BYTES` 4 KB，不制造新的行长外置候选）。 ━━━ **逐字对照留档**＝`1-转型规划/0-全景路线图/队列回写待补/B-0908_B-501-质量部7状态格逐字对照.md`（含 `difflib.SequenceMatcher` 字符级 opcode 全序列比对：**旧文本唯一消失的语义单元就是那 5 字字面标记**＋一个反引号，「逃生阀」「理由文本」「机器判据仍只认 `📥 已回件并回灌` 前缀」等全部原样保留）。🔴 **建议看护者/总线把该留档件并入 `6-人才与组织/部门AI专员跟进/跟进信行日志/质量部#7.md`**（该文件的既定用途正是本行的列历史外置件）——本泳道未自行迁，因其不在 `#501` 触碰区。 ━━━ **改写后另跑一次全表只读计数**（只出计数、不出内容）：`followup_gate.serial_waiver_reason()` 对主表 **45 行全部返回 `None`** ⇒ 主表已无任何「机器眼里的有效串行开闸口令」。⚠️ 该计数只覆盖串行豁免这一个标记与主表活行；归档件与 `转态豁免`／`作废豁免`／`事实日更正` 等其它逃生阀是否存在同类引用式写法，本次未扫。 ━━━ 🔴 **未做且不自行做的三件**：① README 改动落在**主仓工作区**（`工具-跟进信README登记.py` 按 `git --git-common-dir` 解析到主 checkout，是权威文件、设计如此），本泳道**未在主仓 commit**，须由 sweep §二 批次或看护者收尾登记；② 合入 master（🟡）不做；③ **README 其它行是否还有同类「引用式」口令的**普查**未做**——串行豁免这一个标记已确认主表 45 行全清，但归档件与其它逃生阀（`转态豁免`／`作废豁免`／`事实日更正`）未扫，不在 `#501` 触碰区，如实登记、不假装全覆盖，如需普查建议另立行。
