---
title: "队列行日志 #549（§一）"
created: 2026-09-10
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #549 的建造完成报告外置件（K2 口径：单格 ≤4 KB，本段直接落此、行内只留收口结论＋验证手段＋本指针；原文原样、可 grep）。
配套: 0-学习与工具/工具-opener批处理执行v2.ps1；0-学习与工具/test_工具-opener批处理执行v2.py；0-学习与工具/工具-opener生成.py；0-学习与工具/test_工具-opener生成.py；openspec/changes/opener-id-claim-semantics/；1-转型规划/0-全景路线图/opener骨架.md
---

# §一 #549 · 建造完成报告（2026-09-10，CC 泳道 `549-550-opener-infra`／`OP-0910-R`，批 `B-0910_起棒基建`）

> 行内只留收口结论；本件为完整对账原文，同批 `#550` 报告见 `#550.md`。

━━━ ✅ **建造完成·待 ff ＋ 一项阈值待答（2026-09-10 17:5x 本地，CC 泳道 `549-550-opener-infra`／`OP-0910-R`，批 `B-0910_起棒基建`）** —— 分支 `claude/op0910r-549-550-opener-infra`（与 `#550` 同分支同棒，已 push、**未 ff master**，🟡 留 sweep／看护者）。**开工第一件事已做**：`openspec/changes/opener-id-claim-semantics/design.md` 文首与三个决策点回填 Shao Peishen 2026-09-08 答 `1(a)`＝①(a)／②(a)／③(a)（手段＝`工具-队列查询.py --row 531` 现读），`tasks.md` 首行闸划掉、0.1–0.3 勾选，与 `single-guarded-copy` 先后已定（实查该包未 apply ⇒ 本包先行，写进两包 tasks；其形态编号 ⑧⑨ 均已被占须另取）；`openspec validate --strict` ＝ valid。**期望产出逐条对账**：⑴✅ **`--resume` 接管**：`工具-opener批处理执行v2.ps1` v2.1 每条 opener 起 `claude` 前先生成 GUID，以 `--session-id` 传入，写进该泳道日志**首行**（`session=<id> | resume: claude --resume <id>`）与 `summary.txt` 的 Session 列（另出机读 `summary.json`）；⑵✅ **`-Detach`**：先建日志目录、`Start-Process` 把脚本自身后台起（子进程带 `-LogDir`），立即把日志目录打到 stdout 退出 0；🔴 子进程退出码不再丢——任何退出点写 `<日志目录>/exit.txt`、`summary.txt` 末行 `EXIT=<code>`，`launcher.json` 记 pid／起跑时刻／计划；`-Only A1,A2` 经 `-File` 传递按逗号拆开；⑶✅ **取号即声明**：`工具-opener生成.py` 出件成功即写 `<主工作区>/reports/op-id-claims.jsonl`（按 `git rev-parse --git-common-dir` 落主工作区，各 worktree 共享，同 `#504` 修法），查重同时扫已落档文件与未过期占位；同日同号被**另一份草稿**（短名不同）声明即拒并给避开两者的下一空号；同短名重生成刷新不自撞；落档后或到期（`CLAIM_TTL_MINUTES`）自动清理；台账读写失败 fail-closed 不出件；写侧最小互斥锁（`O_EXCL`＋30 s 陈旧接管）；`_scan_used_suffixes` 文档字符串补射程自陈、撞号文案指向它（tasks 2.1/2.2）；骨架「取号」节写入占用语义四条＋残余、在跑清单条改「只为可见性不构成占号」（2.3/2.4）、模板库 §〇 补一句判据（2.7）、`editlock-queue-number-reservation` spec Purpose 补交叉说明（2.6）、变更包 spec 追加「取号即声明」Requirement 五个 Scenario；⑷✅ 单测：`test_工具-opener批处理执行v2.py` 新建 4 条（DryRun 解析＋exit.txt／session id 传到 `claude` 桩且落首行与 summary／两条 opener 各自 id／`-Detach` 立即返回＋子进程 exit.txt），`test_工具-opener生成.py::取号即声明` 12 条；🔴 **变异检验实做**：把 `_claim_op_id(spec, used)` 调用注释掉 ⇒ **11 failed**（含 `test_并发取同号_第二份起草件撞红`），恢复复绿 93 passed。**现网验证（4.1/4.2，不落盘）**：当日已落档 `OP-0910-A` 试取 ⇒ 拒、文案带「射程见 `_scan_used_suffixes`」；只在接力卡在跑清单里的 `OP-0910-H` 试取 ⇒ 按 ②(a) 被落档查重拦下；两次均未写占位（台账文件不存在）。`-DryRun` 对 `B-0910_三泳道续排` 看护件解出 3 泳道；`-Detach -DryRun` 对本派单件实跑：launcher 立即返回、子进程 `exit.txt=0`（`reports/opener-batch/op0910r-detach-smoke/`）。🔴 **待 Shao Peishen 明确答复（阈值类，不设默认生效，建造不因此停）**：占位时效 `CLAIM_TTL_MINUTES=120` 由建造方定（依据＝`OP-0909-P` 实证起草到复核 80 分钟 ×1.5），已写进 design 文首。⚠️ **残余如实写明（不得表述为撞号已根治）**：① 同一秒内两线各自取号仍可能撞（design 残余 1 未变）；② 台账不进 git、跨机器不可见（两桌同机故可接受）；③ 生成器的**落档扫描仍按本 worktree** 的 `1-转型规划/`——主 checkout 里未提交的派单件对 worktree 内的生成器不可见（本次 4.1 在 worktree 推荐空号 `O`，而主 checkout 已有未提交的 P/Q/R），占位台账共享后此缺口缩小但未消除，属既有行为、不在本行射程；④ tasks 5.2（`#487主项复核` 两条定夺项去向）未动、交总线。**派工描述经实测收窄**（`#531` ⑴ 要求写明）：`#487` 原派工指向 `_reserve_ids`／队列编号侧，实撞与可改点在 OP 号空间，队列编号侧按 ③(a) 不动、只补交叉说明。

## 2026-09-10 20:5x 外置（`OP-0910-A`，1 段）

> 原文原样，未压缩、未改判、未修辞。行内已替换为指针。

**预留豁免：本号非本次持锁窗口所预留，属跨窗口补写，理由如下。** `#549` 由本线 2026-09-10 14:0x 的 acquire（`OP-0910-A-起棒基建`，`--reserve 1 --section 一 --domain 机`）**正式预留**，高水位线当时即已推过；但**行未写成** —— 因为就在那一刻 Shao Peishen 的棒 1（`OP-0910-K` 每周清扫）起跑，而清扫**开工第一步就要 acquire 编辑锁**，本方持锁会直接把它挡停 ⇒ **当场放弃写行、立即 release 让路**。本次（16:2x，`OP-0910-A-哨兵与盯守`）只 `--reserve 2` 取了 `#550`／`#551`，故写 `#549` 被 release 守卫按「不属本次预留集合」拒绝——**守卫判得对**。🔑 **该号未被任何他人使用**（高水位线单调推进、编号不复用），本次系同一条线补写同一个号，非抢号、非撞号。📌 **顺带记一个形态**：「预留了号但因让锁而没写成行」会在下一个窗口触发本条守卫，**这是预留与写入分处两个锁窗口的必然结果**，不是异常；正确处置就是写本豁免、不是换号（换号会留下一个永久空洞）。

## 2026-09-10 20:5x 外置（`OP-0910-A`，1 段）

> 原文原样，未压缩、未改判、未修辞。行内已替换为指针。

[S:open][D:机] 🟢 **可领 —— 2026-09-10 立行（Cowork 业务总线 `OP-0910-A`；Shao Peishen 当日「好建议，直接执行」＋答 `1a，2a`）**。 🔑 **两半是同一件事**：前者让棒**停了能被接管**，后者让号**不撞**；都动取号与起棒这一片，分两棒必然二次触碰同一处。 **当日实证密集，全部现取**：**撞号 4 次** —— `OP-0910-H` 被生成器拦下（对方**已落档**）、`OP-0910-I` 与 `OP-0910-J` **未被拦**（两边都在**起草期、都没落档**，靠读对方锁 note 与人工核实才发现）、另有一次是本方引用了已被改掉的旧号；**`-Detach` 缺失** —— 本方两次起棒都得自己用 `Start-Process` 包一层，且包出来的版本**拿不到子进程退出码**；**`--resume` 缺失** —— 正因它未落地，`#443` 清扫棒只能人工粘贴互动跑，无法无头。 ⚠️ **`#531` 的主项是「变更包 `opener-id-claim-semantics` 被自己的 0.1 前置闸锁死」**（`design.md:4` 仍写「三个决策点全部待拍板」，而 `tasks.md` 首行写「design 审通过前一条都不得动手」）⇒ **本棒开工第一件事是解那个死锁**（回填已答结论），否则包内一条都动不了。 🔴 **本棒不改批处理脚本的解析器**（`### A<N>` 三件套那段）—— 那是 `#550` 的触碰面，两棒须错开或并棒。 🔴 **WIP豁免：立行时机制类可动 WIP 现取超上限 22**。豁免依据＝「说口令起棒」当日已成常态用法，**每多用一次就多一次撞号与不可接管的风险**，且今日已实测各发生多次。

## 2026-09-10 20:5x 外置（`OP-0910-A`，3 段）

> 原文原样，未压缩、未改判、未修辞。行内已替换为指针。

[S:open][D:机] 🟢 **可领 —— 2026-09-10 立行（Cowork 业务总线 `OP-0910-A`）；建造三件已完成、阈值已答 120、但进 master 这一步卡住（见末段）。立行原文＋预留豁免见本格末尾指针。**

✅ **建造完成·待 ff ＋ 一项阈值待答（2026-09-10 17:5x，CC `OP-0910-R`，批 `B-0910_起棒基建`）**：分支 `claude/op0910r-549-550-opener-infra`（自 `a80e6a7`，已 push、**未 ff**，🟡 留 sweep／看护者）。⑴✅ `--resume`：v2.ps1 先生成 GUID 以 `--session-id` 起 `claude`，落日志首行＋`summary.txt` Session 列（＋`summary.json`）；⑵✅ `-Detach`：自起后台、立即返回日志目录，子进程退出码落 `exit.txt`；⑶✅ 取号即声明：出件即写主工作区 `reports/op-id-claims.jsonl`（`git-common-dir` 定位、跨 worktree 共享），查重扫落档＋未过期占位，fail-closed；design 自锁已解（①②③ 回填 09-08 `1(a)`，`openspec validate --strict` valid，与 `single-guarded-copy` 先后已定）；⑷✅ 单测 ps1 4＋生成器 12 条，**变异**：注释 `_claim_op_id` ⇒ 11 failed，恢复 93 passed；现网 4.1/4.2 试取均拒、未写占位。🔴 **待答阈值** `CLAIM_TTL_MINUTES=120`（建造方初值）。残余与派工描述收窄说明全文见 `1-转型规划/0-全景路线图/队列行日志/#549.md`。

🔵 **2026-09-10 20:5x 阈值已答 ＋ ff 前提已失效（Cowork 业务总线 `OP-0910-A`）**。 ⑴ **`CLAIM_TTL_MINUTES` ＝ Shao Peishen 答 `1a` ＝ 120 分钟**（追认建造方初值，依据 `OP-0909-P` 起草到复核 80 分钟 ×1.5）。**现取核过，分支上已经是 120**（`git grep -n CLAIM_TTL <分支>` ⇒ `工具-opener生成.py:230: CLAIM_TTL_MINUTES = 120`）⇒ **无需改码，只需清掉三处「待他确认／待答」字样**（`工具-opener生成.py` 常量注释、`openspec/changes/opener-id-claim-semantics/design.md:6`、同包 `spec.md:46`、`opener骨架.md:154`），已并入 `#550` 重新定向那一棒的任务单。 ⑵ 🔴 **行内「自 `a80e6a7` 可 ff」已失真，现取推翻**：`git merge-base --is-ancestor master <分支>` **非零** ⇒ **不能 `--ff-only`**。master 已从 `a80e6a7` 走到 `ace44d7`，`git rev-list --left-right --count master...<分支>` ＝ **9 / 1**（master 独有 9 笔，全是本日队列落档与 sweep 自动件；分支独有 1 笔＝真正的代码）。**该分支要进 master 只能 rebase 或 merge，不能 ff。** 🔑 **又一次「写进文本的状态不会自己过期」**：17:5x 写下时「可 ff」是真的，三小时后就不是了——而**它是本行留给下游的唯一执行指引**。 ⑶ 🔴 **顺带证伪一条全局信念**：派单件与看护件里反复写的「主仓 ff 由 sweep 收尾段串行做（P4）」**在 sweep 里没有实现**——`工具-落库sweep.py` 全文 grep `claude/op`／`泳道分支`／`lane.*branch` **零命中**；它的 `--ff-only` 只用于 `master ↔ origin/master` 同步与常驻执行体 worktree 对齐，**从不合并任何泳道分支**。⇒ **现取 `git branch -a --no-merged master` 命中 `claude/*` 共 134 条，其中能 `--ff-only` 的 0 条。** ⚠️ **不得据此断言「134 份产出未落地」**：`#455`／`#341` 已证内容可经别的路径先进 master，判「是否已落地」须按 `#534` 的 patch-id 口径逐条比，不看分支图。
