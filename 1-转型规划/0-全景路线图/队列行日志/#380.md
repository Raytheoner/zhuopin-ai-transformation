---
title: "队列行日志 #380（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #380 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #380 · 外置前整格原文（2026-09-04 外置，17 段）

[S:blocked][D:机]  🟡 **2026-09-01 tasks 4.1／4.3 双双闭合，5.2／5.3 归档待做故不写 ✅（Cowork 业务总线 `OP-0901-E` 回填）** —— **4.1（对她本人真实收发）**：`07:11:14Z` `annual_holiday_reminder_sent`／`errcode=0`／`errmsg=ok` **实发成功**，不再是本行 ⑷ 那次「向 `ShaoPeiShen` 发标记消息」的安全代理。**4.3（她回复不再触发 `whitelist_rejected`）**：她 `07:01:55Z` 的入站消息走完了正常路径 `archived` → `group_notified` → `inbound_forwarded_to_paul`，且**当日 `whitelist_rejected` 零条**（本机直读审计实测）⇒ 白名单确实生效、未被 fail-closed 挡回。 🔑 **本行 ⑷ 当初那句自陈「安全代理不构成对李姣龙本人真实收发的验收」今天被证明是对的、且不是形式主义**：真机验通过之后，对她的真实发送仍然连失败两次（`846607`），直到她先开口说话。**⇒ 判据：「白名单通了」≠「能发出去」——我方两张名单（出站已知／入站白名单）都是我方的，企微那边还要求一次由对方发起的会话。** 详见 §一 #379 本日 ⒀~⒂。 **遗留**：`tasks.md` 5.2／5.3（变更包归档）属流程收尾，须走 `/opsx:archive` 当场归档。 ━━━ 🟢 **apply 已完成、真实冒烟仍未做（2026-08-25，CC 无头批处理 A30，worktree `.claude/worktrees/a30-inbound-whitelist-apply`，分支 `claude/a30-inbound-whitelist-apply`，commit `32feda5`，已 push origin，**未合 master**）** —— §四 #116 三点全批后按 openspec 包 `aibot-inbound-whitelist-li-jiaolong` 落地，`tasks` 15/19。 **落地内容**：**⑴ 判据 (乙)「出站即入站」已入 spec 并配执行体**——`wecom-feedback-intake` spec delta 新增第四条需求「出站已知收件人 SHALL 同时入站可达」，执行体＝单测两表求差 `KNOWN_RECIPIENT_USERIDS.values() − WHITELISTED_SENDER_USERIDS == ∅`；🔑 **方向刻意单向**（出站 ⊆ 入站，反向不要求）——解植雅在白名单里但不在出站表内，双向相等会把"入站放行"与"出站投递"两件不同的授权强行绑死。🔴 **白名单本身仍显式枚举、不从 `dispatch.py` 推导**：推导确实能让不对称在结构上不可能出现，但它把白名单变成一张**没有授权来源可查的派生表**，IATF 要的是「谁在何时批准了这个人可达」逐条可追溯 ⇒ **判据靠测试守、名单靠人写**。 **⑵ (b) 两人同批放行**：`whitelist.py` 增 `"2025672"`（李姣龙，财务部）与 `"2025621"`（解植雅，采购部），`department_mapping.yaml` 同批补 `"2025672": 财务部`／`"2025621": 采购部`；`intake.py` **未改**（`DEPARTMENT_TO_QUEUE_OWNER` 本就含 `财务部: 财务专线`／`采购部: 采购专线`，核验已落单测，非改动）。 **⑶ `whitelist_rejected` 告警落地**：`whitelist.py` 新增 `format_whitelist_rejected_alert()`／`alert_whitelist_rejected()`，`connection.py` 新增 `whitelist_alert_fallback_send` 形参并**在 `whitelist_rejected` 审计之后**调用（次序即判据：审计是事实、告警是旁路，旁路坏了不能把事实一起吞掉），`run_aibot_service.py` 接上 **`#387`／`gap_alert` 那条既有独立 webhook 通道，未新造第三条**。🔑 **"不含正文"不是靠"记得别传"守的，是靠拿不到守的**——格式化函数只有 `sender`/`msgtype`/`occurred_at` 三个形参，正文在这一层根本传不进来；已配签名级断言，今后要泄漏正文只能靠有人**专门加一个形参**（那会在 review 里显形）。 **验证**：新增单测 20 条（新文件 `test_whitelist_inbound_admission.py` 19 ＋ `test_department_mapping.py` 1）；**全量回归零漂移——aibot 498 → 518 passed／1 skipped，zhuopin_platform 380 passed／1 skipped**；`openspec validate --strict` 通过。归档链路已在本机端到端验过（tasks 3.1／3.2，不需真人）：以两个新 userid 构造入站帧，归档落 `7-外部文档/财务部/`／`7-外部文档/采购部/`（`待分拣/` 目录根本没被创建），队列行 owner 取 `财务专线`／`采购专线` 且**不含**「发送人身份待确认」。 ⚠️ **一处如实校正**：`tasks.md` 原写的服务全量基线 **487 已过期**——`#387` 等包合入后 master 实测即 **498**，本次按现场重测校正，未沿用旧数字。 🔴 **本包未归档，且理由已写进机器认得的地方**：`tasks.md` 顶部声明 **`预期观察窗口：7 天`**（三入口之一，取到 `#379` 首触发日 09-01）——§4 三条与 5.3 全部卡在"须有人在场做一次对外动作"，属真未完项，非「遗忘归档」；判定器 `工具-变更包自动归档.py` 对本包的结论会是 **⬜ 未完工**，本声明不是为压掉某条告警。 ━━━ 🔴 **两项留步，均非遗漏**： **①真实发送冒烟仍未做**（本行「预期产出」里唯一带 🔴 的那条，A7 起留至今）：向真人发企微消息属对外动作，无人值守不发。**验收判据原样不变、不得放宽**：只看命令返回码不算数，须在 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl` 见到 `errcode=0` 才算通。 **②🔴 本次改动尚未生效于运行码，且这一条比①更容易被误读为"已经好了"**：实测监听器进程 PID `68044`（启动 **2026-08-25 08:21:39 本地**，即 A20 那次重启）代码路径为 worktree `wecom-service-home`，其 HEAD `7665517`；**实测该 worktree 内 `whitelist.py`／`department_mapping.yaml` 均无 `2025672`／`2025621`**，且 `git merge-base --is-ancestor 32feda5 HEAD` 判否 ⇒ **此刻李姣龙若回话，仍会被原样挡回**。生效路径＝ ff 合入 `master` → `wecom-service-home` 同步 → **重启监听器**，三步缺一不可；属运行态动作，同样留人在场。⚠️ **"A20 已重启"只保证出站那次改动在跑，不保证本次的**——同一个进程、同一个 08:21:39，对两次改动的答案一个是"已生效"、一个是"没生效"，**差别只在提交进没进那个 worktree**。 **③硬期不变**：`#379` 年度提醒 **09-01 首触发**，出站已通故提醒发得出去；本行尚未闭合的仍是**她回话能不能被听见**。 🟡 **代码已落、真实冒烟未做（2026-08-24，CC，worktree `aibot-387-380-receipt-route`，commit `cb69852`，已 ff 入 master 并 push）** ━━━ ✅ **已完成**：`dispatch.py::KNOWN_RECIPIENT_USERIDS` 增 `"李姣龙": "2025672"`，配单测（可达／`2025672` 是纯数字工号非拼音猜测／既有六项一字不动）；服务全量回归 487 passed、平台 380 passed，零漂移。 ━━━ 🔴 **未做，且这一项才是本行「预期产出」里唯一带 🔴 的那条**：**一次真实发送冒烟未做**——向真人发企微消息属对外动作，本 session 是无人值守批处理，按纪律不发。**验收判据不变**：只看命令返回码不算数，须在 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl` 见到 `errcode=0` 才算通。 ━━━ ⚠️ **「财务部门映射核对」已做完，结论是一处本行没预料到的缺口，如实登记**：李姣龙**出站可达、入站不可达** —— 她不在 `whitelist.py::WHITELISTED_SENDER_USERIDS`（6 人）、也不在 `department_mapping.yaml`（5 键）。⇒ **`#379` 年度提醒发得出去，但她若回一句话，机器人会用「本机器人目前仅面向指定专员开通」把她挡回去**，且这条挡回的礼貌回复对我方是静默的（只记 `whitelist_rejected`）。**本次未擅自补入站白名单**——那会扩大入站面（新增一个人的消息将被归档进 `7-外部文档/` 并产生队列行），属口径决定，已升为定夺项。 ━━━ 2026-08-23 Cowork 业务总线 `OP-0823-D` 拆件立行；**Cowork 未改任何代码、未发任何消息**。 **并入审核（协议〇.10）**：最像的承载行是 §一 #372 ⑥⑶（同一缺口原文即写在其中），但 #372 为 `[S:partial]` 的 design 审前行、其 apply 被 F1／F2／F3 三条硬前置锁着——**把一条零依赖、且被 #379 当作前置的小改动塞进去，它会跟着 #372 一起等**；#281 触碰区相同但已 `[S:done]`，往已关闭行追加新工作正是清扫要消灭的形态。故按例外取号新建。 ━━━ **2026-08-25 A21 收尾（CC，主工作区）**：**①出站复核已闭合**：监听器进程 PID `68044` 于 **08-25 08:21:39**（即 A20 重启）启动，其代码路径为 worktree `wecom-service-home`（HEAD `764642a`，`cb69852` 为其祖先），该 worktree 内 `dispatch.py:82` 实含 `"李姣龙": "2025672"` ⇒ **出站配置确已生效于运行码，不是「源码已改但进程还是旧的」**。 **②入站补齐按 §5 门槛判定必走 openspec，已停在 propose**：命中门槛 **②鉴权与数据可见性**（入站白名单**就是**那道鉴权门——`connection.py:329` 在归档之前拦截；放行一人后其消息将归档进 `7-外部文档/财务部/`、转发 Shao Peishen、发群回执、并**自动产生队列行**（owner=`财务专线`））与 **③改变既有模块对外语义**（同一输入行为从「礼貌拒复、不落档」变为「归档＋转发＋回执＋建行」）。⇒ 按 §5「不跳步」**本次未预写任何代码**。propose 已成稿：`openspec/changes/aibot-inbound-whitelist-li-jiaolong/`（`proposal.md`＋`design.md`＋`wecom-feedback-intake` spec delta＋`tasks.md`）。**三处待你拍板已立 §四 #116。** **③🔴 翻审计翻出一条本行没预料到的既成事实**：`5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl:204` —— `2025621`（**解植雅，采购部**，见《全员企微账号》名录）于 **2026-07-20T02:55:21Z** 发来一条 text，被 `NOT_ONBOARDED_REPLY` 挡回，**只留一条 `whitelist_rejected` 审计，无告警、无队列行，36 天无人知情**。⇒ **本行原判「她若回话会被静默挡回」不是预测，是已经真实发生过的形态，只是当事人换了个人。** **本次未把解植雅加进任何表**——他不在你提过的任何名单里，代拍即是我替你扩大入站面（正是 A7 当初拒绝做的那件事），已列为 §四 #116 决策点 2。 **④🔴 真实发送冒烟仍未做**（本行「预期产出」里唯一带 🔴 的那条，A7 起留至今）：向真人发企微消息属对外动作，本 session 为无人值守批处理，按纪律不发。**验收判据原样不变、不得放宽**：**只看命令返回码不算数，须在 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl` 见到 `errcode=0` 才算通**。**技术前置已全清**（出站已生效于运行码），有人在场即可直接执行。 **⑤硬期提示**：`#379` 年度提醒 **09-01 首触发**——出站已通，**提醒发得出去**；本行尚未闭合的是**她回话能不能被听见**，不阻塞 09-01 首触发本身。　➕ **2026-08-25 §四 #116 三点全批（⑴乙 出站即入站 ⑵b 解植雅一并补入 ⑶ 补 whitelist_rejected 告警）⇒ aibot-inbound-whitelist apply 解锁**，待 CC 领活；真实发送冒烟仍留人在场（对外动作）。 ✅ **该「待 CC 领活」已于同日被 A30 领走并 apply 完毕，见本格开头**（此句为指针，上文原话不追改）。  ━━━ 🔴 **2026-08-27 `OP-0827-D` 形态 2 逐条验：本行的 apply 产出从未合入 master，且是本次五条里最实的一条（CC-OP0827D）**。**产出在**：分支 `claude/a30-inbound-whitelist-apply` commit `32feda5`（2026-08-25，11 个文件／+585），落后 master **194** 个提交。**已逐文件核实「master 上到底有没有」**：`aibot_service/whitelist.py` 在 master 上存在但**少那 118 行**；`tests/test_whitelist_inbound_admission.py`（**318 行**）在 master 上**根本不存在**；`department_mapping.yaml` 亦有差异。⇒ **「李姣龙与解植雅同批放行」与 `whitelist_rejected` 独立告警这两件，生产上至今没有生效**——openspec 变更包目录早已在 master（5 个文件），**只有实现没进来**，正是「看起来做完了」那一族。**🔴 合不进的原因（干跑实测）**：`aibot_service/connection.py` 与 `scripts/run_aibot_service.py` 两处内容冲突。**解冲突涉机器人入站准入语义，属替它改判**，本班停手、未动该分支一字。**⇒ 建议按 P1 派一次专门的 rebase 与合入，并在合入后按 `#380` 原定判据真机验一次白名单放行。**　━━━　🔧 **2026-08-30 `OP-0829-W` 机制类 WIP 分诊改判：`[S:partial]` → `[S:blocked]`**（Shao Peishen 2026-08-30 答「全批」）。**依据＝本行自陈原话**「apply 已完成、真实冒烟仍未做」——阻塞方不是「现在能动手的人」，不属可动负荷，故改判为 `blocked`。🔴 **只改状态字段，工作范围·优先级·领取方一字未动**；**刻意用 `blocked`／`timed=` 而不用 🛑** —— 三者都不计入可动 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 行内原话），会让本行从催办面整个消失；`blocked` 只是不占配额，仍可见可催。误判可一条命令改回。**根因（`partial` 被当作可动 ⇒ 主体做完留尾巴反而永久占配额）根治已并入 `#435` 子项 E。** 　━━━　🔒 **Shao Peishen 2026-08-31 答《本周计划-2026-08-31》**：🔴 **B-1 答 (a)：今天合入本行的 apply 产出（`claude/a30-inbound-whitelist-apply`，commit `32feda5`）并按本行原定判据真机验**（须在 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl` 见到 `errcode=0`，只看返回码不算数），随后明天人工执行 `#379` 首触发。 ━━━ 🔴 **入站准入语义的冲突解法由他在 A1 开工时当场拍板** —— 他原话：「**CC 停手等我是对的，不要自行改判**」。⇒ **rebase 遇到 `connection.py`／`run_aibot_service.py` 两处内容冲突时一律停手问他，不得代判**（2026-08-27 `OP-0827-D` 的停手处置由此获追认）。 ━━━ 🔴 **次序**：本行与 `#379` 一并**排在「我已回 Lan」之前**（本件有硬期 09-01，LAN 收口没有）。 　━━━　🔒 **Shao Peishen 2026-08-31 定今日执行序列（明示）：A1 → 回 LAN 收口 → 清池。**　**本行（连同 `#379`）＝ A1，排在最前**；理由不变：本件有硬期 09-01、LAN 收口没有，而两件都要他在场。**A3（`#431`）与 B-2 清扫（`#443`）本周内另派，不占今天。**　━━━　🔒 **2026-08-31 `OP-0831-A`（CC，[Win]0831A-aibot入站收口 session）：rebase 冲突已解、已 ff 合入 master，真机验已过，技术前置全部清空。** **⑴ 冲突解法（Shao Peishen 当场拍板，CC 未自行改判）**：cherry-pick `32feda5` 到 master 时，`connection.py`／`run_aibot_service.py` 各冲突一处——**实测两处均为「两分支各自在 `build_connector` 签名/调用点同一插入位置追加独立新 kwarg」的纯结构性碰撞**（master 侧＝#416⑴ 的三个 `media_*` 参数，本分支侧＝`whitelist_alert_fallback_send`），**真正改变准入行为的代码（import `alert_whitelist_rejected`、以及写入 `whitelist_rejected` 审计之后调用它那一段）均已自动合并成功、零冲突**——入站准入语义本身在合并层面并无分歧。拍板：两边全保留，#380 追加在 #416 三参之后（签名/docstring/调用点三处一致）。 **⑵ 全量回归零漂移**：aibot **679 passed / 1 skipped**（较上次实测已隔 6 天 415 个提交，本次现场重测非引用旧数）；zhuopin_platform **407 passed / 1 skipped**；`openspec validate --strict` 通过。 **⑶ ff push 入 master 并同步部署**：`git push origin HEAD:master`（`4e6142f`→`a26c252`，纯 ff）；主工作区本地 `master` 同步；`0-学习与工具/工具-执行体对齐重启.ps1 -WorktreeName wecom-service-home` 九关全过（ff 25→0、停服 2 进程先父后子零残留、重启 PID 起于 `12:30:43` 本地、心跳刷新验活）。 **⑷ 真机验（安全代理，同 OP-0828-B/R 惯例）**：本次未直接向李姣龙发送——向第三方发测试消息需另行授权，经确认后改走安全代理，经 `build_connector` 同款审计链路向 ShaoPeiShen 本人发一条标记消息，`reports/wecom_aibot_audit.jsonl` 实测 `action=smoke_test_send`／`errcode=0`／`errmsg=ok`（`2026-08-31T04:33:58Z`）——**证明的是「今天部署的代码在运行码里确实生效、审计链路确实通」，不构成对李姣龙本人真实收发的验收**（`aibot-inbound-whitelist-li-jiaolong/tasks.md` §4.1/4.3 仍留步）。 **⑸ 顺手排除一处新发现的风险**：新建的 `#379` 年度提醒计划任务默认**每日**触发，若不处理，明日（09-01）08:35 会用默认泛化文案「请提供 2027 年度节假日日历更新」**自动**误发，与 Shao Peishen 已批准的特例文案（确认收到 2027 表＋预告 2028）冲突、重演财务部#14 的错误；已手工预置 `annual_holiday_reminder_state.json` 的 `last_sent_year=2026`（附字段说明本条不代表已发送），使明日自动路径正确判「今年已发过」而跳过，**不影响** Shao Peishen 随时手动执行（`--force` 显式绕开该字段），详见 §一 #379 本次追记。 **⑹ 状态改判**：`[S:blocked]`→`[S:timed=2026-09-01]`——技术前置（合入/部署/重启/真机验）已全部清空，剩余唯一阻塞是「明天 09-01 由 Shao Peishen 本人执行 #379 首触发、并等她真实回复」，属日期门控而非技术阻塞。**⑺ 待闭合**：`tasks.md` 4.1（她本人）／4.3（她回复不再触发 `whitelist_rejected`）／5.2（归档）／5.3，均等 09-01 之后。 　━━━　🔁 **状态分诊改判 `[S:partial]` → `[S:blocked]`（2026-09-02，Cowork `OP-0902-A`；Shao Peishen 当日答 ①(a)，机制类可动 WIP 实测 24／22 超限）** —— **判据＝本行状态列自陈原文**：「━━━ 🔴 **两项留步，均非遗漏**： **①真实发送冒烟仍未」⇒ 剩余动作**硬卡在「等 Shao Peishen 一次动作」这个外部条件**上，非本行执行者可推进；按闸自身口径（`STALE_STATUS_PHRASES`）应为 `blocked`，而 `blocked` 结构性不计入 WIP。 🔴 **改判不是关行、不降优先级、不改任何排序** —— 只是把「在等他」如实标进机器字段；**他答复后应立即改回原字段**。 ⚠️ **本次是第二轮**：2026-08-31 值周巡检已做过一次同类分诊（见 `#422` 行内，他当日定「**不调上限，低的是分诊执行率**」），但未形成常态 ⇒ 三天后再次超限。**常态化已立 `#454`**，本行不重复登记成因。
