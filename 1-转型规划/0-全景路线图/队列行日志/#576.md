---
title: "队列行日志 #576（§一）"
created: 2026-09-17
status: 生效
用途: 跨桌任务队列 §一 #576 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #576 · 外置段（2026-09-17 外置，工具第 1 批，2 段，原文原样）

> 外置人 OP-0917-E；外置前整格 5665 B、共 4 段；本批外置第 2、3 段；本格保留第 1、4 段。

⑷ **已做的修复（只动 `.git/worktrees/*/commondir`，不进版本库、不碰任何工作区内容）**：`git worktree repair` 对本症无效（只回显 `.git file broken`），改为逐个补写单行 `../..`，先单件验证再批量补 10 条。现取 `git worktree list` **22 条全部可解析、`0000000` 归零**。⑸ **修复后的现取分类（22 条，不含主仓）**：干净且 ahead=0＝**11 条**（10 条为本次修复件＋`wecom-service-home`）；ahead>0＝7 条；locked＝1 条（差集回归基线，**不得动**）。另：11 条修复件的脏行**一律是同一组 5 条 `D .claude/commands/opsx/*.md`**，**删除源未查明**，属本行第二件事。⑹ **本行要建的是机守，不是这一次的清理**：清理口径写死为「干净 ＋ ahead=0 ＋ 未 locked」三条同时成立才删，其余一律只报不动；泄漏口在 `工具-泳道分支合入.ps1` L158（worktree 清理只挂在成功路径上，失败/异常退出即留一条），移进 `finally`。

━━━

⑺ **代码已落分支（2026-09-14 00:2x）**：`claude/op0913t-worktree-guard-576` @ `16f2efd`（已 push）。① 新增 `0-学习与工具/工具-worktree体检.ps1`：默认 dry-run；`-Repair` 补 `commondir`；`-Apply` 只删「未 locked ＋ahead=0 ＋工作区干净」三条同时成立的；「仅缺文件」另立一类、要 `-IncludeMissingOnly` 才删；`-Keep`（默认含 `wecom-service-home`）与 `_rb-*` 前缀一律不删；每次跑落一行 `reports/worktree-guard/*.jsonl`。② `工具-泳道分支合入.ps1` 的临时 worktree 清理从成功路径移进 `finally`，例外两条：rebase 冲突（exit 3）留现场、`-KeepWorktree` 显式要求留。③ 新增 `test_工具-worktree体检.py` 7 条；连同 `test_工具-合入链路留痕.py`（含合入成功／被拒／被打断三条路径）与 `test_工具-泳道分支合入-回归判定.py` 共 **30 passed**。④ 本轮**未**做的：期望产出③「挂进周期跑批」故意没做——`#575` 的实机验收还没过，此时改轮询守会把那条验收搞浑；等 `#575` 验收过了再挂。⑻ 🟡 **两件待他一字母**（选项与代价见当轮汇报）：**甲**＝本分支是否 ff 合入 master；**乙**＝那 10 条「仅缺文件」 worktree 是否现在就删。**他答之前不写授权行、不删任何一条。**⚠ 未识别到 [S:...] 机器字段（缺失/非法，回退关键词判据展示）——见队列 #308「非静默降级」。⑼ **三天后回看（2026-09-16 08:0x）**：分支 `claude/op0913t-worktree-guard-576` @ `16f2efd` 仍领先 master **1 个 commit、未合**——甲那一字母没等到，不是闸坏了。另：本行原先写成 `[S:待]`——**不是合法的机器字段**，`--digest` 一直把它解成 `[S:?]`，本轮改回 `[S:open]`。**写错一个枚举值，这行就从所有按状态扫池的工具里消失了。**
