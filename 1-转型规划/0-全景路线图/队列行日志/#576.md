---
title: "队列行日志 #576（§一）"
created: 2026-09-17
status: 生效
用途: 跨桌任务队列 §一 #576 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #576 · 外置段（2026-09-17 外置，工具第 1 批，2 段，原文原样）

> 外置人 OP-0917-E；外置前整格 5665 B、共 4 段；本批外置第 2、3 段；本格保留第 1、4 段。

⑷ **已做的修复（只动 `.git/worktrees/*/commondir`，不进版本库、不碰任何工作区内容）**：`git worktree repair` 对本症无效（只回显 `.git file broken`），改为逐个补写单行 `../..`，先单件验证再批量补 10 条。现取 `git worktree list` **22 条全部可解析、`0000000` 归零**。⑸ **修复后的现取分类（22 条，不含主仓）**：干净且 ahead=0＝**11 条**（10 条为本次修复件＋`wecom-service-home`）；ahead>0＝7 条；locked＝1 条（差集回归基线，**不得动**）。另：11 条修复件的脏行**一律是同一组 5 条 `D .claude/commands/opsx/*.md`**，**删除源未查明**，属本行第二件事。⑹ **本行要建的是机守，不是这一次的清理**：清理口径写死为「干净 ＋ ahead=0 ＋ 未 locked」三条同时成立才删，其余一律只报不动；泄漏口在 `工具-泳道分支合入.ps1` L158（worktree 清理只挂在成功路径上，失败/异常退出即留一条），移进 `finally`。

━━━

⑺ **代码已落分支（2026-09-14 00:2x）**：`claude/op0913t-worktree-guard-576` @ `16f2efd`（已 push）。① 新增 `0-学习与工具/工具-worktree体检.ps1`：默认 dry-run；`-Repair` 补 `commondir`；`-Apply` 只删「未 locked ＋ahead=0 ＋工作区干净」三条同时成立的；「仅缺文件」另立一类、要 `-IncludeMissingOnly` 才删；`-Keep`（默认含 `wecom-service-home`）与 `_rb-*` 前缀一律不删；每次跑落一行 `reports/worktree-guard/*.jsonl`。② `工具-泳道分支合入.ps1` 的临时 worktree 清理从成功路径移进 `finally`，例外两条：rebase 冲突（exit 3）留现场、`-KeepWorktree` 显式要求留。③ 新增 `test_工具-worktree体检.py` 7 条；连同 `test_工具-合入链路留痕.py`（含合入成功／被拒／被打断三条路径）与 `test_工具-泳道分支合入-回归判定.py` 共 **30 passed**。④ 本轮**未**做的：期望产出③「挂进周期跑批」故意没做——`#575` 的实机验收还没过，此时改轮询守会把那条验收搞浑；等 `#575` 验收过了再挂。⑻ 🟡 **两件待他一字母**（选项与代价见当轮汇报）：**甲**＝本分支是否 ff 合入 master；**乙**＝那 10 条「仅缺文件」 worktree 是否现在就删。**他答之前不写授权行、不删任何一条。**⚠ 未识别到 [S:...] 机器字段（缺失/非法，回退关键词判据展示）——见队列 #308「非静默降级」。⑼ **三天后回看（2026-09-16 08:0x）**：分支 `claude/op0913t-worktree-guard-576` @ `16f2efd` 仍领先 master **1 个 commit、未合**——甲那一字母没等到，不是闸坏了。另：本行原先写成 `[S:待]`——**不是合法的机器字段**，`--digest` 一直把它解成 `[S:?]`，本轮改回 `[S:open]`。**写错一个枚举值，这行就从所有按状态扫池的工具里消失了。**

---

# §一 #576 · 外置段（2026-09-19 外置，工具第 2 批，2 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-K；外置前整格 4525 B、共 5 段；本批外置第 2、4 段；本格保留第 1、5 段。

立行（2026-09-14 00:0x 本地 Get-Date，Cowork 环境总线 `OP-0913-E`，他当轮说「按你理解推进」）：第 13 环在第一轮盘点里就记为缺口、第二轮记为**在恶化**，至今无承接行，本行补上。🔴 **先更正本方上一轮一句错话**：收官件与交接卡都写了「现取 23 条含 **9 条 HEAD 为 `0000000` 可直接 prune**」。本轮逐条落地查证，**这句是错的，而且危险**：⑴ 那 9 条（实为 11 条）的工作目录**都还在**，`git worktree prune` 根本不会碰它们（prune 只清目录已消失的记录）；⑵ 真因是它们的管理目录 `.git/worktrees/<名>/` **缺 `commondir` 这一个文件**——缺了它 git 解析不出公共 git 目录，于是 `git status` 报 `fatal: not a git repository`、符号引用型 HEAD 显示成 `0000000`（分离头指针型则照常显示 sha，所以真实坏件是 **11 条**不是 9 条）；⑶ 其中 `queue-315-apply-9f2c1a` 修好后现取 **ahead=1**，那个 commit 是 `b2ef181 wip(#315)：抢救 2026-08-11 遗留的 1130 行未提交改动`——**照上一轮那句话 prune 下去，正好把这条抢救件连窝端掉**。**判别一句：`0000000` 不等于空壳，它等于「读不出来」；把「读不出来」当成「没东西」，就是把量具坏了当成工件没了。**

━━━

⑽ **2026-09-17 收口（他答甲a乙a丙c）**：① 分支 `op0913t` 已经 `工具-泳道分支合入.ps1` 合入 **`edf7fa3`**（rebase 零冲突、两份测试全绿、四 ref 对齐）。② 已清掉 10 条「仅缺文件」worktree，worktree 总数 **23 条 → 10 条**；待合登记册同轮销并归档 16 行。③ 🔴 **清理过程里抓到真因**：本仓里 `git worktree remove --force` 与 `git worktree prune` **一律报 `Permission denied`**，而同一个 shell 里 PowerShell 的 `Remove-Item -Recurse -Force` 删得掉（两条独立的 git 路径都败、PowerShell 同路径成功）。巡检的 worktree 清理长期空转、`[WT-BLOCKED]` 天天报却一条也没删成，就栖在这里。**原因未查明**（`core.longpaths` 未设、系统 LongPathsEnabled=1，但失败路径并不长，不当因果用）；修法与原因无关：git 删不动即回退 Remove-Item。④ 首轮真跑又当场暴露一处：工作目录已消失而管理记录还在时，`git -C` 抛 fatal 把整份体检搞停。两处一并修在分支 **`claude/op0917a-wt-del-fallback-576` @ `00cf953`**（同包 `#575` 的包装脚本修法），`test_工具-worktree体检.py` **8 passed**，已 push、未 ff。🟡 **待他一字母**。⚠ 回退路径无自动化单测——临时仓里复刻不出那个权限条件，不拿假测装样子。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 3 段）：

📎 **2 段已于 2026-09-17 外置**（K2 口径，原文原样、可 grep，文件 md5:5247cdcf，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#576.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 OP-0917-E）。
