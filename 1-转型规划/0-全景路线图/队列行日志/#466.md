---
title: "队列行日志 #466（§一）"
created: 2026-09-06
status: 生效
用途: 跨桌任务队列-业务场景.md §一 #466 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置）。行内只留首段＋末两段＋指针。
🔴 完整性声明: 本件为**重建版、非原文原样**——外置件首次建成后被本方误删（工序失误，见下），而外置前整格在 git／锁快照／工作区三处均已不存在（实测 md5 分别为 779e3082／c2e00f7d／c2e00f7d，无一命中原值 a5924fe8）。本节由 OP-0906-B 依其原始撰写内容逐字重建，**md5 与外置时登记的 a5924fe8 不再可校验**。
---

# §一 #466 · 外置前整格（2026-09-06 外置，5 段，🔴 重建版）

> 🔴 **误删经过与判据**：外置件建成后，`append-row` 被 B1 新增的 ⑹ 登记完整性挡下——它按 **acquire 那一刻**的脏集分类，而该文件建于 acquire 之前，被判成「acquire 之前就已经脏、不属任何待处理批次」。本方为让工序合规，**删掉该文件后重新 acquire 再建**，却没意识到**它是那两段的唯一副本**（队列文件当时尚未 commit，锁快照又已被新 acquire 覆盖）。
> 🔑 **该记的判据不是工序，是那个取舍**：**守卫拒绝是可逆的，删除不可逆。** 为通过一次校验而删掉唯一副本，是拿不可逆动作去换可逆问题——正确解法是保留文件、换满足守卫的方式（写豁免，或先登记后建）。
> 📌 **工序判据（顺带）**：B1 落地后，正确次序＝**先 acquire → 再建文件 → 再 append-row**。

[S:partial][D:业] 🆕 **2026-09-02 立行，未开工。** 由 `#374` 变更包 `oem-chroma-ownership-rejudge` 的 design 审 **D2=(a)** 裁决派生（裁决人 **Shao Peishen 本人**，2026-09-02；落字泳道 `oem-chroma-2`，批次 `B-0902_60`，OP-0902-B1）。 🔑 **本行要修的是一处「两份权威件直接冲突」的实现面**：`openspec/specs/platform-oem-isolation/spec.md` ＋ `router.py` 写「无 audit 注入时仅抛错（**向后兼容**）」＝ 可选；`3-治理与合规/OEM数据隔离规范.md` §3.2 写「**每次**触发**必须**写平台 audit……违规**企图**本身就是审计事件」＝ 必须。**默认构造 `OEMRouter()` 因此在拒绝时静默不留痕** —— 任何一处忘记注入 audit 的调用点，其违规拦截都不产生证据；而「拦截到了却没有证据」，在 IATF 16949 可追溯性审核面前与「没拦截」是同一个结论。 ✅ **文本侧冲突已于 2026-09-02 由 `oem-chroma-2` 泳道收口**（spec MODIFIED 条已定稿 ＋ 隔离规范 §3.2 已补明 ＋ §6 修订记录已追 V1.1 行）；**代码侧＝本行，尚未动一行**。⚠️ **在本行闭环前，规范与代码处于「文本已收紧、实现未跟上」的中间态** —— 该中间态已在隔离规范 §3.2 内显式登记（含过渡纪律「新增调用点必须显式注入 audit」），**不是被遗漏，是被登记后有承接**。 ━━━ 🟡 **2026-09-06 09:34 建造完成、停在 `merge_to_master` 等他一个字母（`OP-0906-C` 执行，业务总线 `OP-0906-B` 代回写——CC 本次未写队列）** ━━━ **产出**：分支 `claude/op0906c-oem-audit-fail-closed-aa7199`（🔴 **实际分支名带 `-aa7199` 后缀，与派单件写的 `claude/op0906c-oem-audit-fail-closed` 不一致**——worktree 工具自动加的，同族＝2026-09-05 `OP-0904-N` 分支名跑偏那次；**日后找它按真实名，别按派单件名**），单 commit `7211db3`，7 文件 +224/−16，已 push origin。 **业务总线复核（实证、非自陈）**：`git merge-base --is-ancestor master <分支>` ⇒ **可 ff**；`git diff --stat master...<分支>` 逐文件过目 ＝ `router.py`／`test_oem_isolation_audit.py`／`test_smoke.py`／新包 `openspec/changes/oem-audit-fail-closed/` 四件；🔴 **`3-治理与合规/OEM数据隔离规范.md` 不在 diff 里** —— 派单里写死的避让（防与 §二 `B-0906_A` 落库撞车）确实生效。 **以下取自其收工报告、本线未复跑（如实标信源）**：平台单测 467 passed/1 skipped、FI9 消费方 21 passed、`openspec validate --strict` 绿。 **四项交付对照**：⑴ 实现 ✓（`audit=None` 内建默认 `AuditLogger` 写 `reports/audit_log.jsonl`；默认 logger 构造或写入失败时 `_record_denied` 由静默 return 改判 `raise CrossOEMAccessError`，`from exc` 保留异常链）；⑵ 既有回归 ✓；⑶ 调用点**穷举**核查 ✓（结论＝生产代码零受影响调用点，仅测试文件与一处 docstring 示例；FI9 现有调用或在允许路径或已显式注入 audit，QD-B 红线尚未接线）；⑷ 两条新 Scenario 测试 ✓（含重命名 `test_no_audit_still_raises` → `test_no_audit_uses_default_logger_and_still_raises`，新增 `test_default_audit_logger_unavailable_fails_closed`）。 🔴 **三件未完，本行不销号**：① **合入 master 未做**（🟡，等他一个字母）；② **隔离规范 §5 映射表 §3.2 那一行的现状判定未改**（按派单避让，待合入后由本线另批做，勿忘）；③ **D5 只读闸它判了「另包」却没立承接行** —— 判完即产生一个无载体的裁决，已由本线取号立 §一 承接行兑现本行原话「不得两条都无人承接」。 ⚠️ **它自陈里的一处误解，已澄清、不必照做**：报告写「未登记 §二…留给 sweep 拾取」——**其产出已 commit+push 到自己分支、主仓工作区零脏文件**，而 §二 是「**待 commit** 批次」，本就不该有它的行；**sweep 不会也不需要拾取它**。 ━━━ ✅ **2026-09-06 14:1x 已合入 master（`OP-0906-E` 执行，业务总线 `OP-0906-B` 代回写——CC 因锁被占如实登记「待补」，内容它已备好）** ━━━ 🔴 **上方「① 合入 master 未做（🟡，等他一个字母）」一句自此过时**（Shao Peishen 2026-09-06 答 (a) 放行）。**新 commit `4b9c2a0`**（原分支 commit `7211db3`，分支 `claude/op0906c-oem-audit-fail-closed-aa7199` rebase 后 ff），已 push，`ahead=0`。**业务总线独立实证**：`git merge-base --is-ancestor <分支> master` ⇒ **已并入**。**`OP-0906-E` 独立复跑（自陈为亲验非转抄）**：平台单测 **467 passed / 1 skipped**、FI9 消费方 `test_oem_isolation.py` **21 passed**，与 `OP-0906-C` 自陈数字一致。 🔴 **本行仍不销号，剩两件**：② 隔离规范 §5 映射表 §3.2 那一行的现状判定（由 `❌ 待实现` 改判）—— 归业务总线另批；③ §一 `#491`（D5 通用库只读闸）硬序等本次合入，**现已解冻、可开工**。 📌 机器字段同批由 `open` 翻 `partial`（建造与合入均已完成，非未开工）。

---

# §一 #466 · 第 2 批外置（2026-09-10，4 段，外置段md5:9f50bb50，外置前整格 5949 B／整格md5:58bdd07b）

> K2 口径，原文原样、未改一字（外置段 md5 全值 9f50bb5004c6aef17bf6fde7c358bf96）。外置人＝CC `OP-0910-P`（派单件棒 2）；外置原因＝整格 UTF-8 实测 5949 B 超 4096 B 上限，2026-09-11 起行长闸由告警转阻断。行内保留首段＋末段＋指针，本节为被迁出的中间 4 段，段间分隔符 `━━━` 保持原样；行内改后 2501 B。

✅ **2026-09-06 14:1x 已合入 master（`OP-0906-E` 执行，业务总线 `OP-0906-B` 代回写——CC 因锁被占如实登记「待补」，内容它已备好）** ━━━ 🔴 **上方「① 合入 master 未做（🟡，等他一个字母）」一句自此过时**（Shao Peishen 2026-09-06 答 (a) 放行）。**新 commit `4b9c2a0`**（原分支 commit `7211db3`，分支 `claude/op0906c-oem-audit-fail-closed-aa7199` rebase 后 ff），已 push，`ahead=0`。**业务总线独立实证**：`git merge-base --is-ancestor <分支> master` ⇒ **已并入**。**`OP-0906-E` 独立复跑（自陈为亲验非转抄）**：平台单测 **467 passed / 1 skipped**、FI9 消费方 `test_oem_isolation.py` **21 passed**，与 `OP-0906-C` 自陈数字一致。 🔴 **本行仍不销号，剩两件**：② 隔离规范 §5 映射表 §3.2 那一行的现状判定（由 `❌ 待实现` 改判）—— 归业务总线另批；③ §一 `#491`（D5 通用库只读闸）硬序等本次合入，**现已解冻、可开工**。 📌 机器字段同批由 `open` 翻 `partial`（建造与合入均已完成，非未开工）。 ━━━ 📎 **历史回写 2 段（2026-09-06 建造完成段）已于 2026-09-06 外置**（K2 口径，原文原样 md5:`a5924fe8`）见 `1-转型规划/0-全景路线图/队列行日志/#466.md`；**本格只留首段＋末两段＋本指针**。外置前整格 5,224 B > 4,096 上限（release 已告警，2026-09-11 起阻断）。 ━━━ ✅ **2026-09-07 剩余项②收口（看护批 `B-0907_R` 泳道 `466-oemrouter-audit`，OP-0907-T 复验＋OP-0907-R 执行，Shao Peishen 答 1a/2a）**：⑴ **`3-治理与合规/OEM数据隔离规范.md` §5 映射表 §3.2 行已由 `⚠️ 部分实现` 改判为 `✅ 已实现`**（注：`#466` 原文写「由 `❌ 待实现` 改判」，现文实为 `⚠️ 部分实现`，已以现文为准），同批更新 §5 表头现状日期并追 §6 修订记录 **V1.2** 行。⑵ 复验落档分支 `claude/op0907t-oemrouter-audit-466` 已 rebase 后 ff 入 master（**新 commit `93003da`**，原 `f2d9689`；仅 `openspec/changes/oem-audit-fail-closed/tasks.md` 一文件，零代码改动）。**OP-0907-R 独立复跑（亲验非转抄）**：平台 `pytest tests/ -q` **528 passed / 1 skipped**（合入前后各一次，均绿）、FI10 47 passed、opener/编辑锁族 129 passed，退出码均 0。 🔴 **本行仍不销号，剩一项③**：主 spec `openspec/specs/platform-oem-isolation/spec.md` L9／L15-17 **仍写「无 audit 注入时仅抛错（向后兼容）」，与已合入 master 的实现直接相反**——该失真此前**无任何载体登记**，由 `OP-0907-T` 本次发现、`OP-0907-R` 直读原文复核确认。🔴 **归档次序硬约束**：`oem-chroma-ownership-rejudge` 与 `oem-audit-fail-closed` **各持同一 Requirement「跨 OEM 访问拒绝前写审计」的 MODIFIED**，实测后者为超集（多「（构造失败或写入失败）」＋「fail-closed 义务范围仅限拒绝路径」一段），前者多的是已过时过渡注解 ⇒ **须先 archive `oem-chroma-ownership-rejudge`、后 `oem-audit-fail-closed`，反序会用过渡版盖掉定稿版**。主 spec 同步被 `#374` 收尾挡着（其 tasks 未全勾 ⇒ 暂不归档），非本行可单独解。⚠️ **销号问题本次未提交 Shao Peishen 裁决**（看护者收工汇总的四项定夺未含此项），按 `OP-0907-T` 建议保守处理＝**不销号**，理由：本行 `期望产出` 列即含 spec delta，而「delta 写好但从未落到主 spec」正是本次发现的失真面，此时销号则那句与实现相反的 Scenario 再无任何行盯着。 ✅ 剩余项③（`#491` D5 通用库只读闸）此前已解冻，不受本次影响。
