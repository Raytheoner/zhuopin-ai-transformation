---
title: "队列行日志 #416（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #416 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #416 · 外置前整格原文（2026-09-04 外置，41 段）

[S:blocked][D:机] 🔴 **WIP豁免：本行 ⑶ 正在出血——每一次专员回件都把队列锁死 30 分钟，2026-08-26 当天已两次咬到在办会话（08:16 与 13:19 两把锁均等到自动陈旧才接管）；它同时阻碍其它 session 正常写队列，属「不能等」一类，按 CLAUDE.md §5 机制优先大原则用机制的方式立即立行，不排队等 WIP 回落。** 🆕 **2026-08-26 立行**。🔴 **⑶ 的现实影响最大**：每次专员回件都把队列锁死 30 分钟，本会话今天两次撞上（08:16 那把与 13:19 那把），两次都只能等它陈旧后接管。**⑵ 是元缺陷**——它同时掩盖了 ⑴ 和 ⑶ 的真因，**建议先修 ⑵ 再查另两条**，否则下次仍是空 error。⚠️ **⑴ 与迁移无关但被迁移期放大**：迁移窗口造成 227 分钟断线（00:40–04:27 UTC），恢复后连接一段时间不稳。**唐燕萍的文本已完整入档**（`7-外部文档/财务部/财务部-tangyanping-回复-2026-08-26-文本反馈-a7632c39….md`），**仅附件需请她重发**。 　━━━　✅ **2026-08-26 13:55 唐燕萍重发文件成功、全链路走通**（`archived` → `queue_appended` → `followup_readme_bridge letter_number=财务部#15` → `queue_sync_pushed` → `group_notified` → `inbound_forwarded_to_paul msgtype=file`），归档件 `7-外部文档/财务部/财务部-tangyanping-回复-2026-08-26-…-回复-b815d4d1….docx`（43,657 B）⇒ **⑴ 确认为连接不稳期的偶发超时，非机制性损坏；Shao Peishen 亦确认「就是连接问题，迁移机制是好的」。****修复优先级建议：⑵（元缺陷，掩盖其余）→ ⑶（每次回件锁死 30 分钟）→ ⑷⑸（可同批做，信息已在审计里）→ ⑴（加重试与超时可配）。** 　━━━　➕ **2026-08-26 修复本行行头时又撞出第 ⑹ 条（编辑锁自身的缺口）**：acquire 取快照时本行正处于**坏行形态**（追加文本误入编号格、第二个竖线被挤掉），持锁期间把它修好之后，release 的结构校验把恢复出来的正常行判为「本次持锁期间凭空冒出、未经 `--reserve` 预留的新行」而**拒绝释放**；而此时想用 acquire 刷新快照，**又被自己这把锁挡住** ⇒ **死锁，只能等 30 分钟陈旧后接管**。**逃生阀只覆盖 WIP（`--force-mechanism-wip`），不覆盖本情形。**🔑 **判据：校验「行是不是新的」时，若快照里那一行本身是坏的，任何修复都会被判成新增——机制对「自愈」这个动作没有留出口。****建议**：release 增加一个与 WIP 同形态的逃生阀（开关 ＋ 行内理由标记），或对「编号已存在于 HEAD」的行豁免预留检查。 　━━━　⚠️ **2026-08-26 派单后出现同题重复 session，两个均已停（Shao Peishen 手动）**：`OP-0826-N` 的开场词被粘了两次——先粘无编号版（14:25 起，worktree `queue-write-reply-defects-bcd1c1`），后粘带编号版（14:52 起，worktree `queue-write-reply-fixes-e37a2c`）。**发现时两者都未写主工作区、未抢编辑锁，零冲突损失**（纯属发现得早）。🔴 **半成品保留**：14:25 那个已产出 **+1087／-8（5 文件）** —— `工具-共享文档编辑锁.py`(+434)／`queue_table.py`(+255)／`test_工具-共享文档编辑锁.py`(+274)／`工具-队列结构lint.py`(+57)／其测试(+75)，已提交到分支 `claude/queue-write-reply-defects-bcd1c1`（**未合 master、未经审查**）；14:52 那个零产出，worktree 与分支已清理。**⇒ 下次续跑先读该分支已有内容，判断接着改还是推倒重来，不要从零开始。**🔑 **教训**：派单件与开场词里**没有「开工前查一次是否已有同题 session 在跑」这一步**——两个 session 同时改 `工具-共享文档编辑锁.py`，而那正是它们各自回写队列要用的工具。**已补进 `专线opener模板库.md`（见本批次行）。**　━━━　🔴 **2026-08-26 ⑶ 真因由 CC `queue-write-reply-defects-bcd1c1` 受控复现坐实（非相关性推断，亦未依赖 ⑵ 的 stderr）**：`工具-共享文档编辑锁.py::_registration_completeness_violations`（校验⑹ 登记完整性）**只豁免 `SWEEP_LOCK_WHO`、不豁免 `AIBOT_LOCK_WHO`**，而它度量的是**整个仓库的脏文件**、不是持锁者自己碰过的文件 ⇒ **机器人被别的会话制造的脏文件挡住**；它给出的两条出路（登记 §二 批次／写「登记豁免」）机器一条都走不了。机器人自己那一行同样被算作「未覆盖」，因为它**从不登记 §二 批次**——它走 `append_task_and_sync_to_git` 自己 commit 自己 push（审计 `queue_sync_pushed`），失败另有 `queue_sync_degraded` ＋ pending 补录路径。**复现方法**：玩具 git 仓库，`who=企微机器人` acquire → 只追一行以「企微反馈自动归档：」开头的 §一 行 → 工作区另存在一个属于别的会话的脏文件 → release 当场被拒，未覆盖脏文件两个（机器人自己写的队列文件 ＋ **别人正在改的那个文件**，后者被明确归入「acquire 之前就已经脏（可能来自并发 session）」一栏）；把 `who` 换成 `SWEEP_LOCK_WHO` 则校验⑹ 不触发。**旁证**：本次 acquire 时锁历史里即写着「接管陈旧锁（企微机器人 31 分钟未释放，根因即本次立行事项）」与「接管自身陈旧锁（release 自愈死锁）」。🔑 **判据：校验⑹ 的适用对象是「要靠别人来提交自己改动的那种持锁者」**——sweep 已因「它就是来提交的那个人」被豁免，**机器人同样是自己提交自己的，只是这一点当初没被识别**，与 #333② 一模一样的疏漏形态：一条既有的合法路径，校验从未认得它。⚠️ **同族第二形态（比本条更值得记住）：一个把「全局状态」当作「本次动作的责任范围」来度量的校验，必然在并发环境里错怪无辜者**——它想问「你改的东西有没有人管」，实现成的却是「仓库里还有没有没人管的东西」。人类会话同样会被别人的脏文件挡住，只是人看得懂拒绝文案、会去登记或写豁免，所以从没被记成缺陷。　**⇒ 修法命中 CLAUDE.md §5 openspec 门槛第③条（同一接口同一输入，release 由拒绝变放行；先例＝`2026-08-12-editlock-aibot-intake-reservation-exemption` 形状完全相同），按派单件硬约束③**出件即停**：变更包 `openspec/changes/editlock-aibot-registration-completeness-exemption/`（proposal＋design＋delta spec＋tasks，`openspec validate` 通过，**代码一行未改**），design 留两问待 Shao Peishen 拍板：**窄修法**＝身份豁免（止血快、边界清楚，建议选它）／**宽修法**＝把校验⑹ 度量范围收窄为「持锁者自身改动」（更治本，但改变全部持锁者判定口径、属门槛①，且 `dirty_at_acquire` 的差集判据当初已被取证推翻，须先弄清为何推翻）。　━━━　⚠️ **⑴⑵⑷⑸ 本会话未做**：与 CC `queue-write-reply-fixes-e37a2c` 商定由对方承接（触碰区 `5-平台底座/wecom-aibot-service/`），**但该 session 已结束且零产出**（master 上无其任何提交）⇒ **四条仍待领**。其中 **⑷⑸ 改转发与归档文案＝改变对外语义，同样须先出 openspec 件、不得直接改文案**；⑵ 属纯 bugfix／日志措辞，不触发门槛，可直接做。 　━━━　✅ **2026-08-26 Shao Peishen 拍板（⟨就地答⟩）**：**⑴ ⑶ 取「窄修法 · 身份豁免」** —— 给机器人加与 sweep 同形态的豁免，**不动校验⑹ 的度量范围**。理由：止血快、边界清楚，且与既有先例 `2026-08-12-editlock-aibot-intake-reservation-exemption` 形状一致；宽修法（把度量范围收窄为「持锁者自身改动」）属门槛①、会改变全部持锁者判定口径，且 `dirty_at_acquire` 差集判据当初被取证推翻的原因尚未查明 ⇒ **本次不取，但不否决**——**它仍是更治本的方向，待日后单独立项时先回答「当初为何被推翻」再议**。⇒ 变更包 `openspec/changes/editlock-aibot-registration-completeness-exemption/` **可进 apply**。**⑵ ⑴⑵⑷⑸ 四条明日派**（今日已跑迁移 ＋ 两轮 CC，且四条中 ⑷⑸ 尚须先出 openspec 件）——**⑵ 属纯 bugfix 可直接做、不触发门槛；⑷⑸ 改对外语义须先出件；⑴ 加重试与超时可配**。🔑 **本行 ⑶ 最该被继承的一句（CC 受控复现后写下，比结论更值钱）**：**一个把「全局状态」当作「本次动作的责任范围」来度量的校验，必然在并发环境里错怪无辜者**——它想问「你改的东西有没有人管」，实现成的却是「仓库里还有没有没人管的东西」。**人类会话同样会被别人的脏文件挡住，只是人看得懂拒绝文案、会去登记或写豁免，所以从没被记成缺陷。** 　━━━　✅ **2026-08-26 22:2x `OP-0826-E`（CC-A2）完成 ⑵⑷⑸⑴＋⑺ 五条，已合入 master 并部署重启**（commit `04f5e81`；⑶⑹ 不在本棒范围，仍归 A 组 / `op0826d-editlock-aibot-exempt`）。**⑵ 元缺陷（先修）**——新增 `aibot_service/error_text.py::describe_exception`（`f"{类型名}: {消息}"`，消息为空只留类型名），`aibot_service` 全部 24 处 `error=str(exc)` 一并收敛（含 `queue_git_sync` 的 `last_error` 与降级告警文案）。判据：**留痕一个异常时类型名必须出现在文案里——消息可以为空，类型名永远不为空。** **⑷ 转发内容描述**——补「类型（📎文档附件/💬文字回复）＋附件标题《…》＋回的是哪一封＋文字摘要」，原文全文保留在 `———— 原文 ————` 之后。🔴 **一处必须更正的事实：08-24 那句被当作「老格式」的话不是机器人生成的**，是唐燕萍本人随附件发来的文字消息，归档件 `7-外部文档/财务部/财务部-tangyanping-回复-2026-08-24-文本反馈-2f95482c5bfc04cf5cea4975cf9023ab.md` 逐字可查；08-26 她只发文件没带这句，三行头部才露出来 ⇒ **本条是新增能力、不是回归修复**，从此不依赖专员是否顺手写了说明。**⑸ 归档文件名带编号**——`财务部-tangyanping-回复-财务部#15-2026-08-26-…docx`；编号**插在日期之前**，因为 `followup_gate._ARCHIVE_NAME_RE` 的 `<topic>` 是贪婪 `.+`，追在末尾会被它吞掉、stem 逐字比对随即失效（该正则同步放开为可选段 `(?:(?P<letter>[^-]+#\d+)-)?`，**历史文件名零影响**，另加 `extract_letter_number`）。新增 `followup_readme_bridge.resolve_letter_number` 只读预配对（不持锁、不写盘、绝不向上抛）。🔴 **实测踩中一处、已修**：它初版只认 `outcome.matched`，而 08-26 那份真件走的正是 `latest_closed`（补件档，`matched=False` 但 `outcome.letter` 确定定位到 `财务部#15`——本行原文点名的 `followup_readme_bridge_supplement_after_closed` 就是它）⇒ **只认 matched 会让本条修复恰好在它要修的那个案例上失效**，改为认 `outcome.letter is not None`。**⑴ media 重试与可配超时**——新增 `aibot_service/media_transfer.py`（`with_media_retry`，本层超时独立于 SDK 的 5.0s ack；每次失败单独留痕 `media_transfer_retry`；`CancelledError` 不吞不重试），下载与上传两侧接入；🔴 **不静默**：耗尽抛 `MediaTransferError`，`connection.py` 兜底并**按来源会话**请发件人重发（`media_resend_notice_sent`）。**⑺ 哨兵本人入站误报**——两道同源修复：`queue_append_skipped` 进 `PENDING_CLEARING_ACTIONS`（结构性，读 intake 的结论）＋ 判据收敛到**新建的唯一实现** `forwarding.is_self_sender`（语义性），`intake.py` 里那份重复的 `== PAUL_USERID` 一并并入。**答本行的复核问：`queue_append_skipped` 此前在哨兵侧完全未被读取**（配对循环只认 `queue_appended`/`queue_append_pending_flushed`）。 　━━━　**验证**：aibot 566 passed / platform 380 passed；⑺ 的 5 条经**受控回退**证实对旧实现变红（只回退两处行为、保留常量名，避免死在 import 而证不到判据），同时护栏用例「专员真漏行照报」保持绿；⑷⑸ 用 **2026-08-26 真实归档件**（43,657 B 那份补件 docx）＋ **真实 README** 做只读演练，逐字复现 08-24 文案。🔴 **⑺ 已在生产坐实**：重启前 09:50 那次连接 `reconcile_sentinel_report_sent count=1`（即本行 ⑺ 的误报），22:26 重启后同一入口**未再发送**，用新代码对同一份审计只读复跑得 **0 条**。 　━━━　➕ **本次新得一条判据（写在这里因为它咬的是修复本身）：一条「已知可能错」的归属，机器不得用肯定句说出来。** 实测：08-26 那份补件走 `latest_closed`，若照 08-24 文案写成「是对你上次发我的『财务部跟进信第 15 封』的回信」，就与 README 补件行**已经写明**的「她下一封回件若含甲／乙／分母／签认任一字样，归本补件行、**勿配财务部#15**」（入信桥已知误配缺陷，机制修复挂 `#366 M6`）直接冲突——**等于用机器的语气替人做了一个人已经说过「要人工判」的判断**。⇒ 补件档改为只报位置不下断言：「落在「财务部跟进信第 15 封」之后（该信已闭环，**属闭环后的补充说明**；归属请拆件时人工确认，见 #366 M6）」。 　━━━　⏳ **两处留步，均未擅自生效**：**⒜ 真实回件冒烟留步**——本行硬要求「重启后实测一次真实回件」，而**真实回件必须由专员（唐燕萍等）主动发来**，22:2x 深夜无人在场、机器人无法自造入站件；🔴 **已做到的是**：重启完成（22:26:22，State=Running）、连接与鉴权在审计里坐实（`connection_established`／`authenticated` @ `2026-08-26T14:26:35Z`）、⑺ 由生产行为直接验证、⑷⑸ 用真实归档件只读演练闭合。**仅 ⑴⑷⑸ 的「真人发件→真转发」这一跳待补**，请在下一个有专员回件的自然时机观察一次即可，不需要专门制造。**⒝ media 重试三个默认值（3 次／20 秒／1 秒）＝ 保守工程默认，不是判据，未按已裁定口径写死**——真实取值取决于「专员等多久算可接受」，须 Shao Peishen 定；三者均已做成环境变量 `WECOM_AIBOT_MEDIA_MAX_ATTEMPTS`／`_TIMEOUT_SECONDS`／`_BACKOFF_SECONDS`，定了改配置即可、不必改代码（`_TIMEOUT_SECONDS<=0` ＝ 摘掉本层超时只依赖 SDK 5.0s，排查用）。　━━━　✅ **2026-08-26 ⑶ 已 apply 完成（CC `OP-0826-D`，窄修法·身份豁免，openspec 包 `editlock-aibot-registration-completeness-exemption`）**：落点＝`工具-共享文档编辑锁.py` 新增常量 `SELF_COMMITTING_LOCK_HOLDERS`（＝`sweep-commit` 与 `企微机器人`），把判据从「是不是机器人」显性化为**「持锁者是否自行提交自身改动」**，`_registration_completeness_violations` 在未覆盖判定处按该常量放行；下一个自动化持锁者的挂靠点就在该常量旁的长注释。🔑 **本次最该被继承的一条实现细节：判据写在哪一行，本身就是判据的一部分**——身份豁免必须落在 `dirty_now is None`（fail-closed）**之后**；写在函数开头虽然更短，却会把「取数失败」一并豁免掉，而**校验无法执行 ≠ 校验不适用**（正是本项目反复吃亏的「工具静默回退」形态）。⚠️ **`SWEEP_LOCK_WHO` 那一支刻意未动**：它恰恰写在函数开头、抢在 fail-closed 之前返回，把它并过来等于**收紧 sweep 的既有判定**，属本变更包范围之外的对外行为改变（且 sweep release 被拒的后果是全项目停摆），已在常量注释里如实登记为独立候选，**未顺手扩范围**。**宽修法**（把校验⑹ 度量范围收窄为「持锁者自身改动」）已连同不取的三条理由一并写进常量注释，防止「止了血就没人再想根治」。**验证**：新增 5 条单测（含 3 条反例 ＋ 1 条判据锚定）；**反向对照**——把该文件换回 master 版跑这 5 条，「机器人被他人脏文件挡住」与「判据锚定」两条当场变红、三条反例保持绿，证明新用例真咬住了本次改动；`0-学习与工具` **全量 748 passed ＋ 63 subtests，0 失败**（含邻居工具 sweep 测试套）；**另做真 CLI 真 git 仓库场景验证**（非只跑单测）：实验组 `who=企微机器人` release **returncode=0**，对照组同一套动作换人类会话 **returncode=1**，拒绝文案与线上事故逐字一致、含「acquire 之前就已经脏（可能来自并发 session）」那一栏。🔴 **顺手核 ⑹（行头断裂自愈死锁）本次是否被一并覆盖——结论：已覆盖，但不是被本包覆盖**，而是 master 上既有的 **#414 A3-2**（commit `f1ad578`，08-26 15:20）：`_head_row_numbers` ＋「编号已存在于 HEAD 即豁免预留归属校验」＋ 行内 `预留豁免：` 逃生阀——**恰好就是 ⑹ 行内建议的那两条**；已用真 CLI 真 git 仓库复核（HEAD 里是好行、现场是坏行、acquire 取到坏快照、持锁期间修好 → release returncode=0）。⏳ **唯一未闭合项**：下一次真实专员回件事件观察 `queue_edit_lock_release_rejected` 不再出现（需真实回件到达，本次无法自证）——变更包按 `工具-变更包自动归档.py` 判据判为 `incomplete`（18/20），**本次不归档**，待观察窗闭合后再归。代码与变更包已由本 CC 自行 commit 并合入 master 推送（`b18e959`，分支 `claude/op0826d-editlock-aibot-exempt`）⇒ **止血已对生产生效**，下一次回件即可观察。 　━━━　✅ **2026-08-28 `OP-0828-B`：跟进信正文长度守卫与超限降级已建成（CC，独立 worktree `agent-ac207b4dd4c7bb7b5`，分支 `claude/op0828b-length-guard-decision-ack`，commit `b6ab7f7` 已 ff 入 master）—— 🔴 本条是 ⑴ 的邻居、不是同一处，如实分开写**　━━━　**为什么不是 ⑴**：⑴ 修的是 **media 通道**上传/下载的重试与超时（A2 已改，默认 3 次/20 秒）；本次卡的是 **SDK `send_reply` 的 ack 硬超时 `_reply_ack_timeout = 5.0`**，是另一层，**A2 那组重试参数一个字都没覆盖到它**。🔴 **而且调大那个 5.0 不是修复**——企微服务端对超限消息**根本不回 ack**，等多久都一样；本次未动 `ws.py`、未改任何第三方 SDK。　━━━　🔴 **先更正派单件的一处口径（实测，非推演）**：`波次收口-2026-08-26泳道版-2026-08-27.md` §十一 那张实测表里的字节数是 **md 文件大小**，而真正发出去的串是 **`_strip_frontmatter` 剥掉题头之后的正文**，两者差一个 YAML 题头（实测 600~700 B）。**按实际发送串复测**：`质量部#8` C05–C07 判例批改表 ＝ **13,242 B 正文**（文件 13,939）✅ 已发出；`采购部#18` ＝ 11,104 B（文件 11,407）✅；🔴 `采购部#19` ＝ **24,597 B 正文**（文件 25,255）🔴 两次同一处失败。⇒ **真实区间＝已知可发 ≤ 13,254 B**（含 `【抄送】` 前缀那一条）／**已知发不出 ≥ 24,597 B**。　━━━　**修法（新增 `aibot_service/message_length.py`，`delivery.push_followup` 在发出任何一条之前先过一次 `plan_body()`）**：**⑴ 🔴 守卫按「每条实际要发出去的串」算，不是按原文算** —— 私信发 `content`，抄送 ShaoPeiShen 与群抄送都发 `f"【抄送】{content}"`、**比原文长**；只按原文算，一封正文刚好卡线的信会「**私信成功、群里什么都没有**」，外观是发出去了（同族＝`#270` 那条 fail-closed 静默跳过）。判据取自两个布尔量 `cc_paul_active`／`cc_group_active`，**下方两处抄送的 `if` 用的是同两个量、绝不各写一份条件**（守卫算的通道集与真正发出去的通道集一旦漂开，就退回到那个最隐蔽的形态）。**⑵ 三条通道统一发同一份 body** —— 不允许「私信全文、群里提要」这种错位。**⑶ 超限即降级为「提要＋附件」，摘要按确定规则生成、不即兴**：H1 标题 ＋ **全部二级小节标题逐条列出** ＋ **两处显式告知**（顶部一句 ⚠️「这条企微消息只是提要，不是完整正文」、底部一句 📎「完整说明、判例表与对照表都在附件里，勾选与批改请直接在附件的 Word 上做，一切以附件为准」）。🔴 **规则刻意是「取全部二级标题」而不是「挑出要他办的事」**——正文里没有任何机器可读标记能区分「这一节要他回」与「这一节只是知会」，靠中文措辞挑就是 `#308` 那一族（`OP-0827-G` 也是因此否掉同类修法）；**全列出来一条不漏**，代价是提要比人手写的略粗，收益是它不会漏掉任何一件要他办的事。**⑷ 🔴 超限但没有任何附件 ⇒ 直接抛 `OversizedMessageError`，一条都不发** —— 降级的前提是「完整内容在附件里」，没有附件的降级等于**静默丢内容**。**⑸ 提要本身仍超限 ⇒ 逐条砍末尾小节并显式写出砍了几条**（`另有 N 个小节未在此列出`），砍到一条不剩仍超限则拒发，**绝不静默截断**。　━━━　🔴 **「干净失败」这个性质已被保住并加测试钉死**（派单件第【四】条）：守卫抛在**第一条 send 之前**、且落在既有 `followup_delivery_failed` 那条审计分支上 ⇒ `sent:False／acks:[]／media_ids:[]／backfilled:False`，README 一字未改仍是 `🆕 待发`，可安全重试、不会触发次日 09:30 重发。单测逐条断言：一条都没发（`sent_messages == []`）／README 逐字未变／审计里没有 `followup_delivered` 与 `followup_backfilled`。　━━━　⏳ **阈值未实测出确切值（如实登记，属留步不是遗漏）**：二分法测准它需要向企微**真发若干条消息**，属对外发送；本班为无头批处理会话，按开工纪律一律留步（且靶子只能是 ShaoPeiShen 自己的 chatid）。**默认取 `MARKDOWN_MAX_BYTES = 14,000` 字节**，取值理由已写进模块 docstring：必须 **>13,254**（否则 `质量部#8` 那种今天发得出去的信被无谓降级，违反派单件验收②）、必须 **<24,597**（否则守卫形同虚设），区间内**靠近已知可发那一端**——因为**两个方向代价不对称**：阈值偏低只是多降级一封（收信人拿到提要＋完整附件，内容一点没少）；阈值偏高则整封发不出去（信压根没到，要等人发现再重试）。**宁可多降级，不可漏拦。** 环境变量 `WECOM_AIBOT_MARKDOWN_MAX_BYTES` 可覆盖，**实测出确切值后只改这一个常量**。**按字节不按字符**，依据是仓内既有实测——§四 `#47` 记着群 webhook「4096 **字节**上限（中文 UTF-8 每字 3 字节）报 `errcode=40058`」；⚠️ 只借「按字节」这一条口径，**不借那个数**（智能机器人通道实测 13,242 B 能发，远超 4096，两者不是同一个限额）。　━━━　**真实件只读实跑（不连网、不发送）**：`采购部#19` 24,597 B ⇒ 降级为 **1,167 B 提要**，**9 个小节标题一条不漏**（含「📋 本信要你做六件事」与一~七各节）；`质量部#8` 13,242 B ⇒ **不降级，行为逐字不变**。　━━━　**回归**：服务 **639 passed / 1 skipped**（606 基线 ＋ 21 新增本条、另 12 属决策提醒那半，零漂移）。**三次变异验证均实撞成红后复原**：① 守卫忽略 `【抄送】` 前缀（＝只按原文算）⇒ **3 红**；② 超限无附件也照样降级（＝静默丢内容）⇒ **2 红**；③ 抄送两处仍发原文（＝私信提要、群里全文的错位）⇒ **2 红**。**openspec 三问均不命中 ⇒ 不走 openspec**：不改全项目口径、不涉鉴权与可见性、对外语义不变（限内输入逐字节同输出，已配用例；新增的是一个只在超限时才走的分支与一个 opt-in 环境变量）。　━━━　⏳ **未闭合、如实登记**：**一条真实消息都没发过。** ⑴ 二分法测准阈值；⑵ 降级路径的端到端真实发送（用 `采购部#19` 那封在 ShaoPeiShen 自己的 chatid 上跑一次）；⑶ 「私信刚好在限内、加前缀后超限」那个用例的真实发送验证——**三项均属对外发送，待有人在场时做**。在那之前**不得把本条读成「已上线验证」**。 　━━━　✅ **2026-08-28 续派 2（Shao Peishen 答 (a)：留步两项现在做）—— 阈值已实测钉死，降级链路已真发走通（CC，commit `0e51c9c` 已 ff 入 master）**　━━━　🔴 **阈值 ＝ `20,480` 字节（＝20 KiB），按【字节】不按字符 —— 这是量出来的，不是估的、也不是凑的整数。** **二分 12 条探针，靶子全部为 ShaoPeiShen 本人**：`20,431 B／6,833 字符` ✅ ／ `20,475 B／6,845 字符` ✅ ／ **`20,480 B／6,850 字符` ✅ 最后一条发得出的** ／ **`20,481 B／6,851 字符` 🔴 多一个字节就发不出** ／ `20,520 B／6,862 字符` 🔴。 **🔴 服务端自己把限额写在错误帧里**：`{"errcode": 40058, "errmsg": "body.markdown.content exceed max length 20480. "}`。 **「按字节」由数据本身证死、不靠推断**：`20,481 B` 那条只有 **6,851 个字符**——若限额是「20,480 个字符」，它离限还差三分之二、不可能被拒；它恰好在**第 20,481 个字节**上被拒。（旁证：§四 `#47` 记着群 webhook 也是「4096 **字节**」且同报 `errcode=40058` ⇒ 同一族错误码、同一种按字节的算法，**只是两条通道额度不同：群 webhook 4,096 B／智能机器人 20,480 B**。）⇒ `DEFAULT_MARKDOWN_MAX_BYTES` 由本班原先的保守值 `14,000` **改为实测值 `20,480`**，判定用 `<=` 与服务端「> 20480 即拒」正好对齐，**不留人为余量**（余量在这里只会白白多降级一批本来发得出的信，而边界已逐字节钉死）。单测由「落在两个样本之间」升级为**钉死 `== 20480`** ＋ 新增一条「按字节不按字符」的判据用例。　━━━　🔴 **一处既有诊断必须更正（本班实测推翻，非推演）**：本行上一段与波次收口件 §十一 都写着「企微服务端**对超限消息不回 ack**，SDK 5.0 秒硬超时」——**前半句不成立**。**服务端回了，而且约 60 毫秒就回了**，回的就是上面那条写明限额的错误帧。**真因在 SDK 侧**：该错误帧**没有 `headers.req_id`**，而 `aibot/ws.py::_handle_frame`（L235-276）**按 `req_id` 路由** ⇒ 取到空串后，既不在 `_pending_acks` 里、也不以 `SUBSCRIBE`／`HEARTBEAT` 开头 ⇒ 落到最后那句 `Received unknown frame` **只记一行 WARN**，pending ack 的 future 从此无人 resolve，5.0 秒后超时。 ⇒ **「调大那个 5.0 不是修复」这个结论仍然成立，但理由要整个换掉**：不是「服务端永远不答」，而是「**答案 60 毫秒就到了，被丢掉了**」。 🔑 **代价最大的一点，值得单独记住**：**这条写明限额的 WARN 每一次失败都打在屏幕上** —— 包括 2026-08-27 `采购部#19` 连续失败那两次。**我们花了两天推演「服务端为什么不回」，而答案一直在日志里，只是没人读那两行 WARN。** ⚠️ **同族＝「工具静默回退」的镜像形态**：那一族是「工具不报错、静默给个看似合理的默认值」；这一族是「**下游确实报了错、只是报在一个没人看的通道里**」——两者的共同点仍是**错误不产生有效信号**。**建议另立一行**：让 `AibotConnector` 在 ack 超时时把同窗口内的 `unknown frame` 一并抛出来（不改 SDK，在本仓库这一侧兜），使这类错误下次一眼可见；**本班未做，属新范围。**　━━━　🔴 **最隐蔽那一种形态已在真实链路上验到（本班原先只有单测，现在有实测）**：同一份 **`20,480 B` 正文裸发 ✅ 发得出**；**加 `【抄送】` 前缀（12 B）变成 `20,492 B` 后 🔴 发不出**。**生产中这正是「私信成功、群里什么都没有」那一种。** 🔴 **做法比看护件给的两条都更安全**：看护件给的是 ⑴ 临时把群 chatid 指向他本人、或 ⑵ 只在字符串计算层验；**本班两条都没用，而是直接把那条带 `【抄送】` 前缀的串发给 ShaoPeiShen 本人** —— 既拿到了**真实链路**的判定（比 ⑵ 强），又**完全没碰任何真实群、也没改任何 chatid 映射**（比 ⑴ 安全，无「改了忘改回」的风险）。　━━━　✅ **降级链路端到端真发走通**：`采购部#19` 正文 24,597 B ⇒ 触发降级 ⇒ **提要 1,167 B 已送达** ＋ **全文 docx 53,866 B 上传并送达**（`media_id` 已取得、`send_file` 回 `errcode:0/ok`）。**刻意不走 `push_followup`**——那条路会回填 README 并自动 commit/push，本次只验长度与降级，不动信状态。　━━━　**新阈值对全库影响面（只读实测，62 封跟进信＋补件）**：**正文超限会降级的只有 1 封**（`采购部#19` 24,597 B）；**「正文在限内、但加 `【抄送】` 后超限」那条危险带 0 封**；次大的 `质量部#8` 13,242 B 距限尚有 7,226 B。⇒ **本守卫上线对现有信件几乎零打扰，只拦住真正超限的那一封。**　━━━　🔴 **发送自核（发送前后各跑一次，靶子零例外）**：**本次共发 14 条测试消息，收件人 100% 为 `ShaoPeiShen` 本人，专员 0 条、任何真实部门群 0 条**（8 条二分 ＋ 2 条边界精确探针 ＋ 2 条抄送前缀对照 ＋ 2 条端到端）。**活台账**＝主工作区 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl`（**非执行体 worktree 里那份停在 08-04 的死账**）：**3,206 → 3,210 行**，新增 4 行全部是常驻监听被本班探针连接挤下线后的 `disconnected／reconnecting／connection_established／authenticated`（`2026-08-27T23:05:44→23:05:47 UTC`，3 秒自愈），**发送类记录 0 条**。⚠️ **如实说明两点**：⑴ 探针**直调 connector、不经 audit 层**，故台账里没有这 14 条的记录——**这是刻意的**，不往业务审计里灌 14 条假的 `followup_delivered`；测试留痕以本行与收工报告为准。⑵ 本班的连接确实把常驻监听挤下线过一次，**已复核其现状为 `ZhuopinAibotDevListener State=Running`、最后一条审计是 `authenticated`**，未留后遗症。　━━━　**回归**：服务 **640 passed / 1 skipped**（639 ＋ 新增「按字节不按字符」一条）。　━━━　⏳ **本行至此只剩一处未做**：`#47`／`#124` 的生产 ack —— **Shao Peishen 2026-08-28 明确走默认 (c)：不做。** 不是遗漏。 　━━━　📌 **2026-08-28 已排入本批 `OP-0828-R`**（Shao Peishen 当日拍板加成第 4 条，理由＝行内自陈「⑶ 正在出血」）。正文见 `1-转型规划/0-全景路线图/看护件-2026-08-28-落地后批.md` §四。 🔴 **触碰区已被切开，不是靠谁记得避让**：`R` 的全部改动被限定在 `5-平台底座/wecom-aibot-service/` 之内 —— ⑶ 原本会摸到 `0-学习与工具/工具-共享文档编辑锁.py` 的 release 拒绝分支，而那是同批 `OP-0828-P` 的触碰区；而 ⑶ 的真因（行内 2026-08-26 更正已基本确定＝机器人 release 时工作区有脏文件、不属任何待处理 §二 批次 ⇒ 触发登记完整性校验被拒，而机器人不会写「登记豁免」）**本就该在机器人侧修**。执行顺序按行内建议：**先修元缺陷 ⑵**（`error=str(exc)` 对无参异常写空串，它同时掩盖 ⑴ 与 ⑶ 的真因），再查 ⑴⑶，顺带 ⑷⑸⑺；🔴 **⑶ 修好前本行不得销号**。 　━━━　✅ **2026-08-28 `OP-0828-R`（CC，worktree `agent-a310e2deb5bffc047`，分支 `claude/op0828r-416-error-text-residuals`，commit `1037a10` 已 ff 入 master）—— 开工即实测：⑴⑵⑶⑷⑸⑺ 六项在 master 上**均已实现**（`OP-0826-E` ＋ `OP-0826-D`），派单件所述「待修」与实测不符 ⇒ 按硬约束不替它改判，转为「独立复核 ＋ 补漏」，得三条结论。**　━━━　**⒈ ⑵ 补上 5 处漏网（已修，并由人守转机制守）**：此前按 `error=str(exc)` 这个**字面量**收敛了 24 处，另有 5 处写成**位置参数或元组元素**、字面量搜索看不见——`dispatch.py:339/341`（`:341` 捕的正是裸 `Exception`，即 08-26 那次 `asyncio.TimeoutError()` 的形态；而同文件 `:342` 审计侧早已用 `describe_exception`，**两行并排不一致**）、`outbox_relay.py:451/452`（`:452` 是**审计 error 字段**，同文件 `:501`／`:511` 早已用新写法）、`followup_readme_bridge.py:333`（该值经 `:381` 进入对外文案「最后一次：」）。⇒ 全部收敛；新增 `tests/test_error_text_no_bare_str_exc.py` 按 **AST** 判定「`except X as e:` 作用域内对 `e` 调 `str()`」全包扫描。🔴 **刻意不用 grep**：`str(exc)` 同时出现在 `error_text.py` 的实现与多处 docstring 里 ⇒ grep 必然误报；而位置参数／元组元素／嵌套 handler ⇒ 必然漏报——**一个既误报又漏报的判据比没有判据更糟，它会让人以为已经守住了**。已配判据自锚（4 种真实写法全被认出、`str(path)` 一类不误报）＋ **变异验证**（把 `dispatch.py` 一处改回 `str(exc)`，守卫当场变红并精确报出 `dispatch.py:339`，复原回绿）。**回归 643 passed ／ 1 skipped**（640 基线 ＋ 3 新增，零漂移）。　━━━　**⒉ 🔴 本行两处自述与审计原文不符，须更正（逐条有据，非推演）**：**⒜「机器人只记 returncode、未捕获 release 的 stderr」不成立** —— `queue_edit_lock.py::_record_release_rejected` 里 `detail = (result.stdout or "").strip() or (result.stderr or "").strip()` 由 commit `818a6ab`（**2026-08-12**，队列 `#333③`）落地，**比第一次拒绝早 12 天**；**五次拒绝的完整文案（含「⑹ 登记完整性」字样、脏文件清单、两条出路）逐条都在审计的 `data_sources.detail` 里**。⇒ **真因从第一次发生起就写在审计里，只是没人读那一格**——与本行下方 `20480` 那条「答案 60 毫秒就到了、WARN 一直打在屏幕上」**是同一族的第二个实例，而且发生得更早**。**⒝ 次数是五次不是三次** —— 本行记「08-24 18:00／08-26 08:16／08-26 13:19」三次，实测审计为 **08-24 02:26Z／08-24 05:06Z／08-24 10:00Z／08-26 00:16Z／08-26 05:19Z 五次**（漏记 08-24 上午两次）。**⒞ 成因分布是混合的，「被别的会话制造的脏文件挡住」只覆盖其中三次** —— 逐条读拒绝文案：**2 次的唯一脏文件就是机器人自己刚写的那份队列文件**（`08-24 10:00Z` 与 `08-26 05:19Z`，全在【本次持锁期间新出现】栏、无任何他人文件）、1 次只有他人文件、2 次两者兼有。⇒ CC 受控复现给出的那句「机器人被**别的会话制造的**脏文件挡住」在两次真实事故上不成立；**窄修法（身份豁免）对两种形态都覆盖，故修法结论不变、但诊断需更正**。　━━━　**⒊ 🔴 最要紧的一条：`OP-0826-D` 留的那个「下一次真实回件」观察窗已经发生了，但它不是有效证据，⑶ 不得据此销号** —— 2026-08-28 01:22:40Z（09:22 本地）**陈忱真实回件**全链路走通（`archived` → `queue_appended` → `followup_readme_bridge_marked 质量部#10` → `queue_sync_pushed` → `group_notified` → `inbound_forwarded_to_paul`），**且无 `queue_edit_lock_release_rejected`**。⚠️ **但它不构成对修复的验证**：直接调用编辑锁**自己那两个函数**（`_pending_batch_fragments` ＋ `_dirty_path_is_covered`）对**事发当刻**的队列快照复算 —— 机器人那份队列文件当时**被 4 条待处理 §二 批次覆盖**（`fc01961^` 处共 49 个待处理片段）⇒ **它根本不进 `uncovered`，旧代码走到这里同样放行**。**方法本身已两侧验过**：拿 `0ce684f^`（08-26 13:19 真拒绝那一刻）同法复算 ⇒ **0 个待处理片段、covered=False**，与当时的拒绝文案逐条对上。🔑 **判据（与本行 08-26 那条时间错位判据互为镜像，值得并排记住）：那一条说「不能用事后观察到的状态去否定事发时的原因」；这一条说「也不能用一次碰巧没触发的正常运行去证明修复有效」——排除与确认是同一个错误的两个方向，都要求先问一句『这次的条件跟当初一样吗』。** ⇒ **⑶ 的生产观察窗仍未闭合**，须等一次「该队列文件不被任何待处理 §二 批次覆盖」时的真实回件（那正是 08-26 两次真拒绝的条件）；变更包 `editlock-aibot-registration-completeness-exemption` **继续不归档**。　━━━　**⒋ ⑷⑸⑺ 只读复核，均在位**：⑷ `forwarding.py:143-203` 的类型（📎文档附件／💬文字回复）＋标题＋回哪一封＋摘要＋`———— 原文 ————` 齐备；⑸ `resolve_letter_number` 在位，⚠️ **但按实现它只作用于 `msgtype=="file"` 分支**（`intake.py:212`），**文本回件的 `.md` 归档名不带编号** —— 08-28 陈忱那条正是文本件，归档名 `质量部-ChenChen-回复-2026-08-28-文本反馈-….md` 无编号；**与本行原文「文件入档时带上编号」的措辞一致，判为按设计、非缺陷**，是否扩到文本件请另行定夺（本班不擅自扩）；⑺ `queue_reconcile_sentinel.py:63/131` 两道同源修复在位。　━━━　⚠️ **本班为 worktree 隔离 agent，按 §一 `#426` 如实登记**：编辑锁的行身份守卫与 `工具-队列结构lint.py` 校验的都是**主工作区那份**、不是本班改的这份 ⇒ 对本次写入**恒真通过、零信息量**；本行回写走「改本 worktree 副本 → ff 入 master → 由 sweep 同步回主工作区」这条既成路径（同 `OP-0828-A／-B／-D／-E`），**本次写入未被任何结构守卫实际校验过**。**未改 `0-学习与工具/工具-共享文档编辑锁.py`**（触碰区属 `OP-0828-P`），仅**只读调用**其函数复算。 　━━━　🔬 **2026-08-31 值周巡检 · 状态分诊逐行判读（Shao Peishen 当日定「不调上限，低的是分诊执行率」，指名对 9 行逐行过目）**：🟨 **不改判，留 `open`；并如实标注本次判读的边界。** **判据**：⑶ 已 apply（`B-0826_25`）、⑵ 已由 `OP-0828-R` 收敛，行内同时存在已完成、在办与待定三种子项。🔴 **本次未逐字通读全格（19.4 KB），仅据首尾段判定为混合态** —— 按他「判不准的留在原状并注明」的要求留原状。**下一次分诊若要动本行，须先通读全格**。  ━━━ 🔴🔴 **2026-09-01 并入第四条同族缺陷（Cowork 业务总线 `OP-0901-E` 实撞并拦下，按协议〇.10 并入本行而非新立，因机制类可动 WIP 已 24／22）：入站消息按「部门」归属，未按「发件人」匹配** —— **⑴ 实撞**：李姣龙（工号 `2025672`，财务部员工、**非部门 AI 专员**、不在跟进信收信人清单内）于 `2026-09-01T07:01:55Z` 主动发来一条 6 字节的消息（我方为打通 §一 #379 通道而请她发的），机器人将其**自动挂到财务部最近一封在途信 `财务部#16`（唐燕萍）名下**，把 README 的「发送状态」列改写为 `📨 回件已到，待拆件`，并自动立 §一 `#449` 未拆件入信行。**唐燕萍至今未回。** **⑵ 为何这条比前三条更危险**：前三条是「该入档的没入档」（漏），本条是「**不该入档的入错了档**」（串）——**漏会被人发现（东西找不到），串不会**：README 那一格看起来完全正常，`工具-跟进闸查询.py` 也据它给出「回件已到、待拆件」，**没有任何一步会报错**。同族＝「工具静默回退：它没错，只是解析到了另一个对象」。 **⑶ 风险链闭合且无人工闸门**：拆件回灌 → `财务部#16` 闭环 → 串行闸开 → §一 `#418 ⑸②` 执行条件① 被误判满足 → 而 `#418` 明写「他的批准以本行为凭、届时不必重新征询」⇒ **可直接全量重摄取、改写唐燕萍的生产 `invoice.csv`，且代码回滚救不回数据**。 **⑷ 本班已做的止血（只做拦截，未改机器人一行代码）**：① §一 `#449` 状态列写明「不是唐燕萍回件、不得拆解、建议销号」；② README `财务部#16` 该格追加红字更正，使只读 README 的人与机器人不会据此误判闸态。 **⑸ 待修（交机制线）**：入站归属应以**发件人工号**匹配 `dispatch.py::KNOWN_RECIPIENT_USERIDS`（或跟进信收信人清单）；**发件人不在收信人清单内时，不得挂到任何跟进信上**，应单列为「非专员来信」另行处置。🔴 **须走 openspec**（改变既有模块对外语义，命中根 CLAUDE.md §5 第③条门槛）。 **⑹ 一个必须在 design 里回答的问题**：同一部门若有多封在途信，即便发件人正确，挂到哪一封也需判据（现行「挂最近一封」在多封在途时同样会串）。 🔑 **⑺ 判据**：**「按部门归属」在部门只有一位专员时看起来完全正确，直到部门里第二个人开口说话。** 本项目今天正是自己请她开口的——**我方为打通一条通道所做的动作，恰好触发了另一条链路的静默错配**。  ━━━ 🔴 **2026-09-01 同族第五条形态（`OP-0901-I` 提交前发现并撤销，已登 §四 `#147`；Cowork `OP-0901-E` 并入本行）：入信行销号 → 桥二自动把对应跟进信转态为「已回件并回灌」** —— **⑴ 经过**：业务总线为拦截误标行而把 §一 `#449` 转 `[S:done]`，**S4 桥二据此自动将 README `财务部#16` 转态为「已回件并回灌」**；而 `#449` 的销号实为**拦截**、不是回灌，唐燕萍至今未回。`OP-0901-I` 在 commit 前逐份核对真实 diff 时发现，已撤销该误写（**未进入 git 历史**）。 **⑵ 🔴 差一步就是那条最坏路径**：若该误写落库，README 这个「信状态唯一权威」就会显示 `财务部#16` 已回灌 ⇒ 串行闸开 ⇒ §一 `#418 ⑸②` 执行条件① 误判满足 ⇒ **可无人工闸门重摄取、改写唐燕萍生产 `invoice.csv`，且代码回滚救不回数据**。**本次靠的是 CC 逐份核 diff 的习惯，不是任何一道机制拦下的。** 🔑 **⑶ 本条真正要留下的判据**：**在一个高度自动化的系统里，任何状态变更都可能是另一条链路的触发器——「我只是改个状态」这句话不成立。** 业务总线当时的推理是「把 `[S:open]` 改掉，让该行退出拆件巡逻的扫描集」，**这个推理本身是对的**（见 `#449` ⑻），错在**只看了它对下游 A 的影响，没查它对下游 B 是不是也是输入**。⇒ **改任何被机器读的状态字段前，先问一句：还有谁在读这个字段？** 同族＝「工具静默回退」与「白名单通了 ≠ 能发出去」——**三者都是「我以为我只动了一处」。** **⑷ 修法与 `#416` 主线同源**：桥二的转态判据也应看**发件人**与**该入信行是否真属回灌**，而不是只看销号动作本身；具体见 §四 `#147`。 　━━━　🔁 **状态分诊改判 `[S:open]` → `[S:blocked]`（2026-09-02，Cowork `OP-0902-A`；Shao Peishen 当日答 ①(a)，机制类可动 WIP 实测 24／22 超限）** —— **判据＝本行状态列自陈原文**：「design 留两问待 Shao Peishen 拍板：**窄修法**＝身份豁免（止血快」⇒ 剩余动作**硬卡在「等 Shao Peishen 一次动作」这个外部条件**上，非本行执行者可推进；按闸自身口径（`STALE_STATUS_PHRASES`）应为 `blocked`，而 `blocked` 结构性不计入 WIP。 🔴 **改判不是关行、不降优先级、不改任何排序** —— 只是把「在等他」如实标进机器字段；**他答复后应立即改回原字段**。 ⚠️ **本次是第二轮**：2026-08-31 值周巡检已做过一次同类分诊（见 `#422` 行内，他当日定「**不调上限，低的是分诊执行率**」），但未形成常态 ⇒ 三天后再次超限。**常态化已立 `#454`**，本行不重复登记成因。

# §一 #416 · 二次外置（2026-09-07 16:40，`OP-0906-W` 派单 OP-0907-AM 时按 K2 口径外置，2 段，md5:5348f96e）

> 外置前状态列 4009 B。本节＝09-02 分诊改判段＋09-04 `OP-0904-E` 只读核实段原文；行内各留一句指针。

🔁 **状态分诊改判 `[S:open]` → `[S:blocked]`（2026-09-02，Cowork `OP-0902-A`；Shao Peishen 当日答 ①(a)，机制类可动 WIP 实测 24／22 超限）** —— **判据＝本行状态列自陈原文**：「design 留两问待 Shao Peishen 拍板：**窄修法**＝身份豁免（止血快」⇒ 剩余动作**硬卡在「等 Shao Peishen 一次动作」这个外部条件**上，非本行执行者可推进；按闸自身口径（`STALE_STATUS_PHRASES`）应为 `blocked`，而 `blocked` 结构性不计入 WIP。 🔴 **改判不是关行、不降优先级、不改任何排序** —— 只是把「在等他」如实标进机器字段；**他答复后应立即改回原字段**。 ⚠️ **本次是第二轮**：2026-08-31 值周巡检已做过一次同类分诊（见 `#422` 行内，他当日定「**不调上限，低的是分诊执行率**」），但未形成常态 ⇒ 三天后再次超限。**常态化已立 `#454`**，本行不重复登记成因。

🔴 **2026-09-04 `OP-0904-E` 只读核实：校验⑹「登记完整性」的逃生阀在本行族上已退化成常开开关**（接力卡记的「release 把『行长豁免』误当『登记豁免』」**不成立**，真实形态更隐蔽，如实更正）。**实证**：`_registration_completeness_violations` 的 `waiver_sources` ＝ 本次 note ＋ **本次触碰过的队列行整行文本**，判据是裸子串 `REGISTRATION_WAIVER_MARKER in s`；而 §一 `#382` **任务列**里 2026-08-24 为一次性目的写下的 `登记豁免：.claude/settings.local.json…` **至今逐字留在行内** ⇒ **此后任何一次触碰 #382 的 release 都会自动放行全部未登记脏文件，并打印 `✓ 检测到登记豁免声明`，外观与正当豁免完全相同。** 🔑 **判据**：该文件 4449-4452 行红字自己写着「逃生阀必须一次一用」，据此把取材面由「队列全文」收窄到「本次触碰的行」——**但那一行里也可以躺着上个月写的豁免**；收窄只把永久开关从「全库一把」变成「每行一把」，**没消除永久性本身**。⇒ **「一次一用」须用时间维判据（本持锁窗口内新写入的），不能用空间维判据（在哪一行）。** **本次未修**（🟡 档，改全项目咽喉工具判定口径，须走 openspec）。🔴 **本线未依赖它，也未实测到它触发**：外置 #382 时先把新增 2 文件登记 §二 再 release，并只读复算 `_pending_batch_fragments`＋`_dirty_path_is_covered` 逐个核 release 当刻 40 个脏文件，**孤儿 0**。⚠️ 故「它会放行」是**推断、非实测**——按「限制类结论须同等举证」，实测留给下次真撞上的人。

---

## 2026-09-07 `OP-0907-AM`（CC）· design 出件即停（⑶ 根治的 openspec 包）

**产出**：`openspec/changes/editlock-waiver-time-scoped/`（proposal.md／design.md／tasks.md／`.openspec.yaml` ＋ 两份 spec delta）。分支 `claude/op0907am-editlock-waiver-bcbc97`，commit `6fe68ed`，已 push。`openspec validate --all --strict` ＝ **172 passed／0 failed**。**未动一行 `工具-共享文档编辑锁.py`／`工具-落库sweep.py` 代码。**

**写死项落地（派单件 §三 D1–D4）**

- **D1 取材面＝时间维**：⑹ 的豁免只从本持锁窗口的 `acquire` note 取；队列行文本、上次 note、任何落盘文本不再算。技术前提已取证坐实——`release` **没有** `--note` 参数（`工具-共享文档编辑锁.py:5698-5710`），note 只在 `acquire` 写入（`:5340-5365`、`:5440-5470`）⇒ **acquire note 是这把锁上唯一一段随锁而生、随锁而灭的文本**，取材面锁死在它身上，逃生阀自动一次一用，不必另造过期机制。
- 🔴 **D1 实现上最容易做错的一处**：`waiver_sources` 这一个列表同时喂着三家——`转态豁免：`（`_auto_sync_followup_reply_state`）、`登记豁免：`（⑹）、`opener豁免：`（`_opener_guard_violations`，`:5546-5573`）。图省事把列表整体收窄成 `[note]`，会顺手改掉另外两道门禁的对外语义，而它们既没立项也没取证。**只收窄 ⑹ 这一项**，已写进 spec delta 并配反例单测（tasks 2.8）。
- **D2 结构化**：`登记豁免：<路径>[；<路径>…]（作者 <OP>，到期 MM-DD）`；分隔符同时接受 `；`／`;`／`、`，反引号可选；**有效路径 ＝ 工作树内存在 或 出现在本次脏文件集合中**（后半句覆盖「删除」形态——被删的脏文件在磁盘上恰恰不存在，只按存在判会把一次合法的删除豁免拒掉）。只放行点名件，其余照常阻断。
- **D3 泛豁免拒**：只给两条出路，且拒绝文案与「压根没写豁免」**必须可区分**——不可区分的后果是写豁免的人以为工具没读到他那句话，于是写得更宽。
- **D4 孤儿升格**：新常量 `ORPHAN_SECTION_FOUR_HOURS = 6`（独立于 `ORPHAN_ALERT_THRESHOLD_HOURS = 3`，两者受众不同：企微推送打给还在场的人，§四 行打给总线排期）；只加一个独立函数 ＋ 一处调用（循环之后，循环内调会同轮登两次），当日去重复用既有 `reports/sweep-orphan-state.json` 加字段，**不新建文件名形态**（`git check-ignore -v` 实测命中 `.gitignore:41` `**/reports/`）。

**Open Question 起草时确认结果**

- ~~`AIBOT_LOCK_WHO` 是否沿用旧通路~~ ⇒ **已确认不开放**：机器人与 sweep 走的是**身份豁免**（`SELF_COMMITTING_LOCK_HOLDERS`，`:4784`），根本不走 `登记豁免：` 文本通路，D1–D3 一个字不碰它。但 apply 须有反例单测锁住这一点（tasks 2.6／2.7），否则本包可能在收窄文本通路时误伤身份通路。
- **仍开放**：`到期` 缺省（决策 3）、窗口内逃生出路（决策 2）。

**决策 2 是 D1 的直接后果，必须有人拍**：note 在窗口内改不了 ⇒ 持锁中途才发现要豁免（典型：另一并发会话此时弄脏了文件）的会话无处可写。(a) 只留「代登 §二」一条路——但止血口径明列「载荷 `队列回写待补/*.json` 与在跑会话的件不代登」⇒ 存在真的无路可走的组合，最坏就是锁挂到 30 分钟自动陈旧，**正是 #416⑶ 一开始要治的那个形态**；(b) 给 `release` 加 `--waiver`（推荐）——随进程生灭、一个字不落盘，比 note 更严格的时间维；(c) 保留队列行取材面但要求结构化点名——D1 写死的「落盘文本一律不算」被打回一半。

**本次持锁的现场实证（顺带留档，比任何论证都直接）**：`acquire` 回显的「最近 120 分钟内其它身份」里，**「登记豁免：他线脏文件由其作者线自登」这一句原样出现 5 次以上**（`OP-0907-V`、`Cowork 环境总线 OP-0906-W` ×3、`Cowork-环境总线-OP-0907-AL` ×2）。同一窗口内用工具自身判据实测：脏文件 26 个、**未覆盖 19 个**，无一属本线。⇒ 这不是历史问题，是**此刻仍在发生**的问题。

**本次未做／刻意不扩范围**

- 未碰 ⑹ 的**度量范围**（仍是「仓库全部脏文件」而非「本持锁者自己改动的文件」）——那是 `editlock-aibot-registration-completeness-exemption` design 决策点 2 记的「宽修法」，Shao Peishen 2026-08-26「本次不取、但不否决」。⚠️ 要看见两者的关系：**正因为度量范围是全局的，会话才会被别人的脏文件挡住，才会去写泛豁免。** D1–D3 把泄压阀关小之后，宽修法的必要性会**上升**——apply 后若出现「合规会话被别人的脏文件反复挡住」，那是宽修法该立项的信号，不是本包判据写错了。
- 未 ff master、未 commit 主仓、未碰 `.51`、未改 `.claude/rules/` 正文。

---

## 2026-09-07 `OP-0907-AM`（CC）· apply 完成（Shao Peishen 答 `1a／2b／3a`）

**分支** `claude/op0907am-editlock-waiver-bcbc97`＠`e19d58d`（已 push，**未 ff master** 🟡）。**测试**：全量 `test_工具-共享文档编辑锁.py` ＋ `test_工具-落库sweep.py` ＝ **876 passed ＋ 73 subtests passed，0 失败**（39 分 30 秒）；`openspec validate --all --strict` ＝ **174 passed／0 failed**。

### 落地内容

- **D1 取材面＝时间维**：`_registration_completeness_violations` 第四形参由 `waiver_sources: list[str]` 改为 `acquire_note: str`。🔴 **签名是判据的一部分**——传列表进来就总有人往里多塞一个来源，而 `#416` ⑶ 的整个病灶就是「取材面多了一个会落盘的来源」；收成字符串之后，想加来源必须改签名，改签名的人会看见那段注释。`转态豁免：`／`opener豁免：` 的 `waiver_sources` **一个字未动**（配反例单测 2.8）。
- **D2 结构化**：`_parse_registration_waiver_clauses`（纯文本，不碰文件系统）＋ `_valid_waiver_paths`。分隔符 `；`／`;`／`、` 全接受、反引号可选、正文在第一个左括号处截断；**有效路径 ＝ 工作树内存在 或 出现在本次脏文件集合中**——后半句不是冗余，被删除的脏文件在磁盘上恰恰不存在，只按「存在」判会把一次合法的删除豁免拒掉。**只放行点名件**，拒绝文案写出「已点名放行 M 个、下列 N 个未被点名」。
- **D2 到期（决策 3a）**：`_resolve_waiver_due_date`，缺省当日有效；跨年按「早于今日 180 天以上则解为次年」。🔴 **用本机本地日期、刻意不用 `_now()`**（后者是 UTC）：note 里的 `MM-DD` 是人按 `Get-Date` 写的，用 UTC 比会在每天 16:00–24:00 那一段整整差一天。
- **D3 泛豁免拒**：文案与「压根没写豁免」**可区分**（明说「检测到标记但没有点名任何有效路径」）——不可区分的后果是写豁免的人以为工具没读到他那句话，于是写得更宽。只给两条出路，不设第三条。fail-closed 分支同理：取数失败 ＋ 一句谁也没点名的豁免 ＝ 最该停下的组合。
- **决策 2b `release --waiver`**：补上 D1 收窄后留下的缺口——note 在窗口内改不了（`release` 没有 `--note`），持锁中途才发现要豁免的会话（典型：另一并发会话此刻弄脏了文件）无处可写；剩下的唯一出路是替别人的文件登批次，而止血口径明列在跑会话的件与回写载荷不代登 ⇒ 存在真的无路可走的组合。本参数**随进程生灭、一个字不落盘**，比 note 更严格的时间维；理由按 `进度豁免：` 惯例落进锁 `history`。泛豁免与到期对它一视同仁，**它不是绕过 D3 的开关**。
- **D4 孤儿升格**：`ORPHAN_SECTION_FOUR_HOURS = 6`（**独立于** `ORPHAN_ALERT_THRESHOLD_HOURS = 3`——企微推送打给还在场的人、§四 打给总线排期，用同一个阈值等于假设两件事总该同时发生，而 09-07 实测恰恰相反：推送一路在响、**没有一个人在场**）。一个独立函数 ＋ 一处调用（避开 `#479` 触碰面），排在本轮**全部 git 操作之后**（它会 acquire 锁把队列改脏，放在推送前会与 autostash 抢同一批文件）。当日去重复用既有 `reports/sweep-orphan-state.json` 加字段、**不新建文件名形态**；跨天仍孤儿则再登。**不 `git add`／不 commit／不代写 §二 声明／不标默认项**；四处失败各自只留一行日志、不改退出码。
- **D4 用 `--cell` 不用 `--set`**：`--set` 走 `queue_table.SECTION_COLUMN_NAMES`，而编辑锁取不到平台包时回落的内建隔离桩没有该属性 ⇒ `--set` 在隔离环境当场 AttributeError；本函数是**无人值守**路径（每 27 分钟自跑一轮），不能依赖一条在部分环境必崩的入口。

### 🔴 反向对照（本包最该被读的一段）

把 `工具-共享文档编辑锁.py` 换回 master 版（`git show origin/master:…`）跑同一批 ⇒ **19 failed／21 passed**：新增用例悉数变红（泛豁免拒／只放点名件／形参名锚定／`ReleaseWaiverCliTests` 全部…），而三条反例——机器人身份豁免 ×2、`opener豁免：` 写在触碰行仍放行 ×1——**保持绿**。⇒ 证明新增用例咬住的是本次改动，且**未误伤身份通路与另两个逃生阀**。事后用 md5（`5ae83a24…`）核对已还原。

⚠️ **反向对照顺带证伪了一条我本以为有效的用例，如实记在这里**：白盒的 `test_stale_inline_waiver_in_touched_row_no_longer_passes`（用 `#382` 那句真实残留做 fixture）在 **master 版上同样是绿的**——因为「本次触碰过的队列行」是**调用点**拼进 `waiver_sources` 的，白盒直接调校验函数根本走不到那一步。已补 `ReleaseWaiverCliTests::test_stale_waiver_in_touched_queue_row_no_longer_passes_end_to_end`（真 CLI 完整时间线：acquire → 队列行里写下点了名的旧豁免 → 另有一个它没点名的脏文件 → release 必须拒）顶上；白盒那条保留、但文档字符串已写明它证明不了什么。🔑 **一条自我感觉良好的用例不该冒充证据——而唯一能发现这件事的动作，就是反向对照本身。**

### 派单件指令的一处偏离（须知情）

派单件 §一 写「`#455` 未 ff 则 rebase 到其分支之上做」。**实测后未照做**：`claude/op0905n-editrow-guard-455`＠`7bd70b5` 确实未入 master，但它同时**落后 `origin/master` 150 个提交**（merge-base ＝ `860dda2`，09-05）。rebase 到它之上会把本包一并拖回 09-05 基线，且要求本会话先替 `#455` 完成它自己的 ff——那是 🟡 动作、且带「脏文件 ∩ 带入文件为空」前置（`#455` 行内明写）。⇒ 改为 rebase 到 `origin/master`＠`44af737`。**代价如实登记**：两分支同改 `工具-共享文档编辑锁.py`，**后 ff 的一方须解冲突**；实测重叠面小——`#455` 集中在 `_build_append_row_line`／`cmd_edit_row`／`cmd_append_row` 与 `main()` 里那两个子解析器，本包集中在 `_registration_completeness_violations`／`cmd_release`／`p_release`。

### 顺带撞出的既存缺口（登记待派，本包不修）

编辑锁的「隔离环境兜底桩」（`工具-共享文档编辑锁.py` 里的 `class queue_table`）**缺 `has_unbalanced_backtick_run` 与 `SECTION_COLUMN_NAMES`**，而 master 的权威模块两者都有、且 `_build_append_row_line`／`_content_cells_from_named` 直接调用 ⇒ **任何没有 `5-平台底座/zhuopin_platform` 的 checkout 跑 `append-row` 都会 AttributeError 崩溃**。生产不撞只因生产恒有那个包。该桩 docstring 明写「取值须与权威模块保持一致」——这是一次真实漂移。本次以「夹具还原真实布局」绕开（同 `SweepTestBase` 复制 `工具-opener块lint.py` 的既有惯例），**不顺手扩范围去修**。

### 本次持锁的现场实证

acquire 回显的「最近 120 分钟内其它身份」里，「登记豁免：他线脏文件由其作者线自登」原样出现 **5 次以上**；同一窗口用工具自身判据实测：脏文件 26 个、**未覆盖 19 个**，无一属本线。**上午那次持锁（新口径未生效时）工具打印「已放行 16 个」，而我 note 里点名了 19 个——三个数字对不上它也不看。** 收工这次持锁走的已是新口径：点名 13 件、逐条校验后放行。

---

## 2026-09-07 三次外置（K2，原文原样 md5:51f644b4）· 09-04 核实两句 ＋ 09-07 派单段 ＋ 09-07 design 出件段

> 三段均已被行内末段（apply 完成段）取代，故迁出；**原文未改一字**。

 🔁 09-02 曾改判 `blocked`（等他答窄修法两问）；🔴 09-04 `OP-0904-E` 核实：校验⑹ 逃生阀 `登记豁免：` 判据＝裸子串、取材面含触碰行整行文本 ⇒ 行内躺着的旧豁免（`#382` 任务列那句）与泛豁免都把全部未登记脏文件放行、外观同正当豁免；「一次一用」须用时间维判据——原文见外置件二次外置节。 ━━━ ▶ **2026-09-07 转回 `[S:open]`＋派单（Cowork 环境总线 `OP-0906-W`；Shao Peishen 同日令「及时止血、及时根治」）**：⑴ **实证**＝16:21 sweep 报 21 个孤儿、≥5 个会话、最老 18 小时，每次 release 均以「他线脏文件由其作者线自登」放行、无一自登；环境总线代登 15 件（`B-0907_W19`）。⑵ **止血（人守，已生效）**＝`.claude/rules/队列与落库.md` 新增「登记豁免止血口径」：只认逐文件点名豁免（路径＋作者＋到期），禁泛豁免。⑶ **根治（机器守）**＝派单件 `1-转型规划/0-全景路线图/派单件-【CC】登记豁免时间维收窄与孤儿升格-2026-09-07.md`（`OP-0907-AM`：openspec `editlock-waiver-time-scoped`，D1 取材面＝本持锁窗口 note／D2 结构化逐文件／D3 泛豁免拒／D4 孤儿 ≥6 h 自动登 §四；design 审 🟡 一字母后 apply；与 Y-A5 `#455` 同碰编辑锁、apply 前先核 ff）。⑷ 09-02 那两问（窄修法）由本派单 D1–D4 取代，不再等答。WIP 豁免沿用行首（出血项）。 ━━━ ▶ **2026-09-07 `OP-0907-AM`（CC）design 出件即停（派单 `OP-0907-AM` 步骤 1）**：openspec 新包 `editlock-waiver-time-scoped` 已建——proposal／design／tasks ＋ 两份 spec delta（`editlock-registration-completeness` MODIFIED 逃生阀取材面＋ADDED 泛豁免拒绝／新 capability `sweep-orphan-escalation`）；`openspec validate --all --strict` ＝ **172 passed／0 failed**；分支 `claude/op0907am-editlock-waiver-bcbc97` 已 push（commit `6fe68ed`），**未动一行工具代码**。🟡 **design 待审**：三个决策点（① apply 范围 ② 窗口内逃生出路 ③ 到期缺省）在 `design.md` §三，推荐＋默认 **1a／2b／3a**；审过再进 apply。详情外置见 `1-转型规划/0-全景路线图/队列行日志/#416.md` 末节。 

---

## 2026-09-07 四次外置（K2，原文原样 md5:80828e4e）· 09-07 apply 完成段

> 已被行内 ff 段取代，故迁出；**原文未改一字**。本节的完整展开见上方「2026-09-07 `OP-0907-AM`（CC）· apply 完成」节。

 ✅ **2026-09-07 apply 完成（`OP-0907-AM`，Shao Peishen 答 `1a／2b／3a`）**：D1 取材面收窄到本持锁窗口 acquire note（形参由 `waiver_sources: list` 改为 `acquire_note: str`——**签名是判据的一部分**，想加来源必须改签名）／D2 结构化点名、只放行点名件、到期缺省当日／D3 泛豁免拒且文案与「没写豁免」可区分／**2b 新增 `release --waiver`**（随进程生灭、不落盘，补上 D1 收窄后「持锁中途才发现要豁免」的缺口）／D4 sweep 孤儿 ≥6 h 自动登 §四（当日去重、两个选项、**不标默认项**、不代 commit）。🔴 **反向对照实测**：换回 master 版跑同一批 ＝ **19 failed／21 passed**，新增用例悉数变红、三条反例（机器人身份豁免 ×2 ＋ `opener豁免：` 触碰行放行 ×1）保持绿。⚠️ **反向对照顺带证伪了一条自认为有效的用例**（白盒版「行内旧豁免」在 master 上同样是绿的，因取材面是调用点拼的、白盒走不到），已补真 CLI 端到端那条顶上。分支 `claude/op0907am-editlock-waiver-bcbc97`，**未 ff master（🟡）**。⚠️ **两项待派**：⑴ 编辑锁「隔离环境兜底桩」缺 `has_unbalanced_backtick_run`／`SECTION_COLUMN_NAMES`（master 既存漂移，无平台包的 checkout 跑 `append-row` 必崩，生产不撞只因恒有该包）；⑵ 本分支与 `#455` 分支同改 `工具-共享文档编辑锁.py`，**后 ff 的一方须解冲突**。详情外置见 `1-转型规划/0-全景路线图/队列行日志/#416.md` 末节。  

---

# §一 #416 · 第 3 批外置（2026-09-10，3 段，外置段md5:f6f53c1a，外置前整格 8835 B／整格md5:f4496cc6）

> K2 口径，原文原样、未改一字（外置段 md5 全值 f6f53c1a09fd6305236459fe6588e0f3）。外置人＝CC `OP-0910-P`（派单件棒 2）；外置原因＝整格 UTF-8 实测 8835 B 超 4096 B 上限，2026-09-11 起行长闸由告警转阻断。行内保留首段＋末段＋指针，本节为被迁出的中间 3 段，段间分隔符 `━━━` 保持原样；行内改后 3902 B。

📎 历史回写 39 段已于 2026-09-04 外置（K2 口径，原文原样 md5:39c97540）见 `1-转型规划/0-全景路线图/队列行日志/#416.md`；**2026-09-07 二次外置（md5:5348f96e）＝09-02 分诊改判段＋09-04 `OP-0904-E` 核实段**；**2026-09-07 三次外置（md5:51f644b4）＝09-04 核实两句＋09-07 派单段＋09-07 design 出件段**（apply 已完成、三段均被末段取代）。本格只留首段＋指针＋末段。 **2026-09-07 四次外置（md5:80828e4e）＝09-07 apply 完成段**（已被 ff 段取代，原文原样）。 ━━━ ✅ **2026-09-07 ⑶ 已 ff 入 master（Shao Peishen 答 `1a`）**：`22cbd15..8e8eb0c` 真 fast-forward（无 `+` 前缀），ff 前三判全过（可 ff／「脏文件 ∩ 带入文件」＝空／工作区干净）。**合入基线为含 `#479`（sweep 提交范围收紧）的 master**——派单件担心的「同碰 `工具-落库sweep.py`」已真实发生并解决：两次 rebase 各撞一处冲突，性质都是「两边各加各的」而非同一处两种写法（① master 在 `if not args.dry_run:` 内加第 13 类扫描器、本包 D4 调用在块外；② 测试文件末尾两个新测试类），**均两边保留**。**凭据**（他答 `a` 取定向档，理由＝上一基线全量已绿、`#479` 与本包函数级不相交）：`test_工具-落库sweep.py` 全套 **462 passed ＋ 66 subtests**（含 `#479` 的 `ScopedCommitUnitTests` 与本包 D4 十条同跑）；编辑锁本包三组 **60 passed ＋ 3 subtests**；上一基线两文件全量 **904 passed ＋ 94 subtests／1 skipped（既存）**。**生产实证**：用新代码对真实仓库 `--dry-run` ⇒ `孤儿升格 §四 扫描…本轮待升格 10 个`、`[dry-run] 本应升格 §四 一行（10 个孤儿），本次不写`，无异常。⚠️ **一次自我更正**：首次 dry-run 跑的是主 checkout 的**旧代码**（`git push origin HEAD:master` 只动远端，本地 master 仍在 `22cbd15`），当时「无升格行」的结论无效；改用 worktree 新代码 `--repo-root` 指真实仓库重跑才是实证。本地 master 落后一格由 sweep 下一轮 `_reconcile_with_origin_and_push` 自动 ff，不裸手拉。 ⏳ **仍欠一项生产信号**：下一轮 sweep 真实升格后回填「孤儿数是否回落」——**本行 ⑶ 在此之前不销号**。 ⚠️ 顺带修：§四 高水位线 168→170、§一 499→500（实测可见行最大值；`#169`／`#170` 由 `OP-0907-R3` 当日登入未抬线，致本会话取不到号；**同一形态当天第二次**，第一次见该线自身批注 §一 492→493）——根因＝写行与抬线两步可分离、无机器守同步，与 §四 `#171` 同族。 ━━━ ➕ **2026-09-09 拆件巡逻并入第 4 条同族缺陷（`IT部#12` 回件当场实撞，事件驱动班）：锁忙暂存通道缺去重，且补录只由「下一条消息到达」驱动、无兜底 flush**。**实撞经过**：陈承回件 2026-09-09T07:53:52Z 到达，`intake.py::archive_inbound_message` 归档落盘成功、桥一标第九态成功、`patrol_signal` 落信号成功，但追加 §一 归档行时撞上人类会话持锁 ⇒ 走 `#168` 设计的 `QueueLockBusy` 分支，参数原样存进 `5-平台底座/wecom-aibot-service/reports/pending_queue_lock_appends.jsonl`（实测该文件当前正好 1 条，即本次这条）。⇒ **拆件巡逻被信号叫醒时，§一 里根本没有对应的待领行**——按 §一.5 字面判据「digest 零命中即空巡」，本班本该回一句「空巡，无待拆件行」然后结束，**而一封三件全答的真实回件就躺在 `7-外部文档/IT/` 里没人拆**。本班是靠 README 第九态与信号载荷交叉核对才没漏掉。**两条子缺陷**：⑴ **去重不对称**——`queue_git_sync.flush_pending_git_sync_appends` 有 `input_pointer_already_in_queue` 这道去重网（`#387` ⑹ 固化，测试 `test_queue_git_sync_flush_idempotent.py`），而 `queue_lock_pending.flush_pending_queue_appends` **没有**：它只是 FIFO 逐条 `append_pending_task`。⇒ 任何人（含拆件巡逻自己）在补录发生前手工补了这一行，下次消息到达时必然补出第二条同内容、不同编号的行——正是 `#387` ⑹ 花了一次人工核对才拦下的同一形态，只是换了另一个物理暂存文件。**本班因此刻意没有手工补这一行**。⑵ **无兜底触发**——补录的唯一触发点是 `connection.py::on_message` 开头，即「下一条消息到达」。专员回件天然稀疏（IT 域上一条在 2026-09-02），⇒ 一条回件可能在暂存里躺若干天；期间它对队列、对 digest、对每一班拆件巡逻**全都不可见**，而它在 README 里已经是第九态、在信号里已经消费掉。**建议修法**（不自行开工，登记待派）：⑴ 把 `input_pointer_already_in_queue` 接进 `flush_pending_queue_appends`，与 git-sync 侧同源；⑵ 给暂存文件加一个不依赖新消息的 flush 入口（sweep 轮次或拆件巡逻 §一.0 前置各调一次皆可），并在 §一 digest 零命中但暂存非空时**fail-loud**，不得静默判空巡。🔴 **本条只登记不修**（拆件红线：本班不发起 CC 建造）。
