---
title: "队列行日志 #586（§一）"
created: 2026-09-16
status: 生效
用途: 跨桌任务队列 §一 #586 状态列整格原文外置件（K2 口径：单格 ≤4 KB；本格含立行、并入⑸旁证、OP-0916-Q 取证与修法收尾三段叠加已超限，行内仅留首段＋末段＋本指针）。
---

# §一 #586 · 整格原文（2026-09-16 OP-0916-Q 收尾时外置）

[S:open][D:机] **WIP豁免：出血**（专员来信已实际丢失一次、专员已在群里追问）。2026-09-16 `OP-0916-T` 立行（Shao Peishen 拍 3a）。🟡 合入与重启须他一字母。 ━━━ 🔧 **并入（09-16 他拍 2a）**：⑸ 企微来信 `queue_append_deferred_lock_busy` 延后写入被 flush 两次 ⇒ 同一来信写成两行（`#588`＝`#590`、`#589`＝`#591`，拆件巡逻已销重复、成因取证在 §一 `#416`）；修为 flush 幂等（按 recorded_at／消息 id 去重、flush 成功即出队原子化）＋单测「延后写入只执行一次」。⑹ 旁证：09-16 15:19 再断连，与 08:18／09:18／13:19 同为每小时 :18 提醒发送后约 1 分钟。附：15:33 已用 `工具-执行体对齐重启.ps1` 重启（九关全过，进程已加载 `#581` 巡逻 sonnet 默认）。  ━━━ 🔧 **代码与单测已完成，待他一字母 ff（OP-0916-Q，2026-09-16，分支 claude/op0916q-aibot-drop-586，commit 0976d5d）**：⑴ 取证——对 09-16 全天审计 jsonl 逐条核对时间戳，**推翻本行原设想的「阻塞读循环」病因**，实测定位两处真实缺陷：①`gap_alert.build_reconnect_notice` 只在进程启动那一次 `connector.connect()` 前后被调用一次，SDK 内部自愈式重连（`on_disconnected`→`on_reconnecting`→`on_authenticated`）此后每次恢复都不再触发任何通知——09-16 当天该循环发生十余次，无一次触达 gap_alert；②即便触达，`format_alert` 对 180s 以下的断连一律硬编码判「期间无消息丢失风险」，与 `gap_alert.py` 自身 docstring「企微无离线消息补推能力」矛盾，唐燕萍那 3 秒的真实窗口正落在这个假阴性区间。⑵/⑶ 按因修——`DisconnectInProgressMonitor` 新增 `loss_risk_fallback_send`，挂在既有 `on_disconnected`/`on_recovered` 回调链上（零新增连接、零新增接线成本），**不管断连时长，每次恢复都点名窗口起止**发一条独立告警（复用既有 `disconnect_alert_fallback_send` 那条独立 webhook 通道），与既有 75s 阈值「进行中」提示互不影响、互不覆盖；企微协议确认无离线消息补拉能力（`gap_alert.py` 原有取证），故未做「自动补拉」，直接落「至少告警」这一档。并入⑸：`flush_pending_queue_appends` 原无并发保护——常驻监听 `on_message` 与 `decision_reminder_check.py` 第二道载体各自独立进程同时 flush 同一份 `pending_queue_lock_appends.jsonl` 会把同一条来信写成两行（真实事故 `#588`＝`#590`／`#589`＝`#591`）；`pending_jsonl` 新增 `try_claim_flush`/`release_flush_claim`（`O_CREAT|O_EXCL` 原子哨兵文件），声明失败即直接返回 0、不做任何处理。⑷ 单测：新增/改动 `test_disconnect_inprogress_alert.py`（7 条新用例，覆盖不看时长必发／无通道不发／无前序断连不误发／与阈值提示互不干扰／发送失败不上抛／不重复发送）、`test_pending_jsonl_claim.py`（5 条新用例）、`test_queue_lock_pending.py`（新增并发复现用例，先证明声明占用时第二次 flush 返回 0 且不重复写队列行，再证明释放后可正常补录）、`test_connection.py` 既有接线用例按新预期改判（原断言「阈值内不告警」本身就是待修的缺陷现场，已改为断言「阈值内不发进行中提示、但发一条窗口丢信告警」）。回归：服务全量 898 passed／1 skipped；2 处 `test_ps1_orphan_cr_guard.py` 失败在纯 master 同命令复现（`register-decision-reminder-task.ps1`／`register-followup-dispatch-task.ps1`，与本次改动无关），零回归。🔴 **本次未做、如实登记**：连接冲突本身未消除——`decision_reminder_check.py` 仍各自开一条独立 `AibotConnector`（同 BotID），与常驻监听共存的「单实例约束赌博」这一层未动（class 同 2026-08-26 outbox_relay 修复前的病因，`outbox_relay` 已改走「零新增连接」范式，`decision_reminder_check.py` 未跟进迁移）——本次判据取「丢信不被发现」这一半优先修（引用行内「判据一句」），「为何频繁断连」那一半留作后续（建议：比照 outbox_relay 改走 outbox 落盘/复用现有连接，而非新起连接；不在本次触碰区内）。合入与重启走他一字母 ff＋`工具-执行体对齐重启.ps1`（`#570` 同范式）。
