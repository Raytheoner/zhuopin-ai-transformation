---
title: "队列行日志 #341（§一）"
created: 2026-09-05
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #341 的状态列回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #341 · apply 回写全文（2026-09-05 外置，`OP-0905-Q`，CC）

> ⚠️ **本文件与它记录的代码/spec 改动同批**，落在分支 `claude/op0905o-domain-route-341`
> commit 上，**尚未 ff 进 master**（泳道状态机已 pause 在 `merge_to_master` 决策点）。
> 队列行内的指针在合并前指向该分支，合并后即在 master 可达。

**执行体**：worktree `agent-ad89a4ae89df16442`，分支 `claude/op0905o-domain-route-341`，
apply commit `f1a1f92`（父 `4b8a98a` ＝ design 起草），已 push origin。
**变更包**：`openspec/changes/queue-domain-routing/`（proposal / design / tasks / delta specs）。

**design 审 2026-09-05 由 Shao Peishen 拍板，四条决策点全部采纳 design.md 自带默认项、未选任何备选。**

## ① 未声明 `--domain` 时维持默认（只改 spec、零代码）

编辑锁未声明 `--domain` 时**维持默认落机制环境文件**（`_resolve_append_target` 的回显提示
保留），反向改两份主 spec 为目标态描述：

- `openspec/specs/queue-dual-file-topology/spec.md`「§二 域路由」
- `openspec/specs/editlock-dual-queue-routing/spec.md`「acquire/append-row 按域路由」

两处「归档时如实登记的实现差异」临时标注已移除，改为 📌 来源说明（记明由来、改判日期，
以及"若未来改真拒绝需先做调用点普查"的前置）。`工具-共享文档编辑锁.py` **一行未动**。

## ② `queue_appender` 域路由切换 ＋ 取号高水位线解耦（代码）

- `aibot_service/repo_paths.py::DEFAULT_QUEUE_RELATIVE_PATH` 改指
  `1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`（机器人来件恒属业务域，恢复
  `queue-dual-file-split` design 决策点 4 项 6 已采纳、被 2026-08-11 止血覆盖了 25 天的结论；
  当时那个阻塞条件"业务场景文件刚拆分完、表格标题存疑"经 2026-09-05 实测已不成立）。
- `aibot_service/queue_appender.py`：新增 `HIGH_WATER_MARK_SOURCE_FILENAME` 与
  `resolve_high_water_mark_path(queue_path, explicit=None)`；`append_pending_task` 新增
  `high_water_mark_path` 参数（与 `queue_path` 各自独立，未传时解析为同目录的机制环境文件，
  该文件不存在时回落 `queue_path` 自身——单测夹具与历史部署形态零回归）；`_next_task_id`
  新增 keyword-only `high_water_mark_lines`；`_bump_section_one_high_water_mark` 的回写作用
  于**来源文件**的行，不再写进新行落进的那份文件。
- 🔴 **次序不可对调**：两份文件不同时，**先推进来源文件高水位线、再写新行**。前者成功后者
  失败 ＝ 跳一个号（编号仍单调递增，无害）；反过来 ＝ 下一次取号取到同一个号（撞号）。
  代价不对称，故写死这个次序并写进 docstring。

### 起草时未识别到、apply 阶段实测补上的四处

1. **`DEFAULT_QUEUE_RELATIVE_PATH` 身兼三职**——①机器人新行的写入目标；②仓库根解析锚点；
   ③"§四／协议〇／编号高水位线在哪"的读侧答案。**只翻转它**会让
   `scripts/decision_reminder_check.py`（每日 08:30 定时任务 `ZhuopinDecisionReminderDaily`）
   去一份**没有 §四** 的文件里读决策项 ⇒ 稳定输出"决策提醒命中 0 项"，**与"今天真的没有
   待决策"在输出上逐字相同**——本项目反复点名的"只读命令结果太干净"失效形态。
   ⇒ 新增 `QUEUE_MECHANISM_RELATIVE_PATH`，`decision_reminder_check.QUEUE_REL` 钉死机制
   环境文件；其补录路径（`flush_pending_queue_appends`，属**写侧**）另用
   `QUEUE_APPEND_TARGET_REL` 指业务场景文件，两个常量刻意分开、不合并。
2. **`queue_git_sync._commit` 原本只 `git add` 写入目标那一份** ⇒ 高水位线的推进永远滞留
   工作区、推不出去，撞号从"本地已避免"退回"跨 checkout 仍会发生"（下一个 checkout 读到的
   仍是旧值），等于决策点 2 只做了一半。⇒ 改为两份同批进同一个 commit；`#287` 那条"外来
   未提交内容"护栏一并覆盖到高水位线来源文件（对它的预期改动更紧：插入 ≤1／删除 ≤1）；
   冲突重算路径的 `git checkout --` 也一并覆盖两份。
3. **`queue_reconcile_sentinel._collect_reconciliation_text` 只读 `queue_path` 那一份** ⇒
   切换生效前若干天里写在机制环境文件、尚未被清扫搬走的行，会被逐条误判为"归档成功但队列
   里没有对应行"，产生一批假阳性私信（同 `#99` 那次"行搬了家就报漏行"、以及 `#312` 缺口一
   "一份拆成两份、下游只跟一份"的形态）。⇒ 改为按 `iter_queue_paths()` **逐份读后合并**，
   **不拼接后只解析一次**（拼接会让 `find` 只取第一个 `## 一、` 而静默丢掉第二份）。
4. 🔴 **最要紧的一处，前三处补漏时也没看见**——`scripts/run_aibot_service.py` 把
   `WECOM_AIBOT_QUEUE_PATH` **同时**当成"仓库根锚点"和"机器人往哪份文件写"。该变量在
   其余**全部**入口（`decision_reminder_check`／`dispatch_followup_letters`／
   `flush_pending_lock_appends`／`push_followup_letter`／`approve_followup_letter`／
   `annual_holiday_reminder`）的文件头里都写着同一句"**可选，仓库根解析锚点**"；只有本
   入口历史上混用了两者——拆分前只有一份队列文件，两者恰好重合，这个混用一直没有后果。
   **实测（2026-09-05）**：常驻监听 `ZhuopinAibotDevListener`（当前 `Running`）的启动
   脚本 `.claude/worktrees/wecom-service-home/…/start-aibot-service-dev.ps1` 把该变量
   **钉在机制环境文件上**（其目的是 `#126` 的"锚到主工作区、别锚到 worktree 自己的
   副本"，与"写哪份"无关）。⇒ **沿用旧写法，本决策点的写入目标切换在生产上完全不生效、
   且不报任何错**——常量改了、行为一点没变，是"改了个看起来对的地方、真实路径根本不经过
   它"这一形态。**修法**：锚点仍走环境变量（`#126`／`#269` 语义一字不动），写入目标改为
   `resolved_repo_root / DEFAULT_QUEUE_RELATIVE_PATH`；配源码断言单测
   `test_run_aibot_service_derives_write_target_from_constant_not_anchor` 钉死不得回退。
   **`start-aibot-service-dev.ps1` 一字未改**——它现在只提供锚点，语义正确。

## ③ 锁粒度 Requirement 更正（只改 spec、零代码）

`editlock-dual-queue-routing`「锁粒度——每份队列文件各持一把独立锁」更正为
「**锁粒度——两份队列文件共用一把协作锁，锚点恒为机制环境文件**」；Scenario 由"两份锁互不
阻塞"改为"后者被前者阻塞、串行化跨域写入"，另补两条 Scenario（高水位线读写天然串行／显式
`--file` 覆盖时锁粒度不受影响）；来源说明指向队列 `#420`。

**这是订正 2026-08-17 归档 `tasks.md` 6.5 的一处分析遗漏**（当时只核对了 `_lock_path()`
通用派生函数本身，未追踪 `cmd_acquire`/`cmd_release`/`cmd_status`/
`_append_row_ownership_violation` 四处调用点对"队列系统目标"的锚点覆盖逻辑），**不是重新
拍板锁架构**——共享锁早在 2026-08-11 apply 阶段就是既成实现。`工具-共享文档编辑锁.py`
与 `queue_appender.py` 的锁逻辑**一行未动**。

## ④ 存量错位行处置口径

**就地留存，不发起批处理**——依赖 `queue-dual-file-topology`「跨文件一致性交叉校验」
Requirement 的既有告警，下次自然触碰该行时顺带修正。口径记录已写进
`openspec/changes/queue-domain-routing/design.md` 决策点 4 尾段。

`#336` 实测 `python 0-学习与工具/工具-队列查询.py --row 336` **零命中**（已销号/归档），
按"历史记录不追改"**不回填已归档行**——口径的权威载体即 design.md 那一节。

## 新增 capability spec

`openspec/specs/aibot-queue-domain-routing/spec.md`（2 条 Requirement、5 条 Scenario，
含 apply 阶段新增的"两份文件的改动同批入库"Scenario，对应上文第 2 点）。

## 回归与取证（本地实跑，不以 CI 绿为准——§一 `#398` ⑶ CI 整体 run 长期 failure）

| 项 | 基线 | 改后 |
|---|---|---|
| `5-平台底座/wecom-aibot-service` | 758 passed / 1 skipped | **768 passed / 1 skipped**（+10 新增，零漂移） |
| `5-平台底座/zhuopin_platform` | — | **466 passed / 1 skipped**（本次未触碰） |
| `openspec validate --strict --changes` | 49/49（design 起草时） | **49/49**，未减少 |
| `openspec validate --strict --specs` | — | **111/111** |
| `grep -rn "实现差异"`（两份主 spec） | 各 1 处 | **0 命中** |

**新增单测**：`tests/test_queue_appender_domain_routing.py`（9 条）＋
`tests/test_queue_git_sync_domain_routing.py`（1 条）。

**两处变异验证均实撞成红后复原**：

1. 把 `_next_task_id` 的 `high_water_mark_lines` 改回 `None`（＝旧的"读写入目标自身"）⇒
   `test_queue_appender_high_water_mark_decoupled_from_write_target` **当场变红**（取到 301，
   一个 ≤ 机制环境高水位线 500 的重复编号），复原回绿。
2. 把 `_commit` 改回只提交写入目标那一份 ⇒
   `test_sync_commits_both_write_target_and_high_water_mark_source` **当场变红**（origin 上的
   机制环境文件仍是旧高水位线），复原回绿。

**测试替身补全一处**：`tests/test_queue_appender.py::_FlakyPath` 新增 `parent` 属性转发——
`resolve_high_water_mark_path` 要按同目录找机制环境文件，替身不转发就只是"替身没写全"；
该目录下并无那份文件，解析仍回落 `queue_path` 自身，那三条用例的语义一字未变。

## 🔑 定稿时踩到的一次自指陷阱（值得留）

tasks 4.2 的验收判据是 `grep -rn "实现差异" ... 命中 0 处`。改写来源注时写了一句"移除此前
『归档时如实登记的实现差异』临时标注"——**这句话本身被判据命中**。与 `queue_table.py`
里记着的"把『格子里被污染成了报错文本』与『格子里在谈论报错文本』当成同一件事"完全同形：
一个负责记录事故的载体，必然会大量引用事故的原文。已把注文改写为"目标态/现状分叉"，
出处一字未少，判据回到 0 命中。

## 🔴 未闭合、如实登记

- **tasks 1.6 未做**：观察下一条**真实**企微来件的队列行落位（应命中业务场景文件）与编号
  连续性（不与机制环境文件当前高水位线冲突）。本班为**无人在场的无头会话**，等一条真实来件
  不在本班能力内，**不制造假来件**。
- ⇒ 变更包**暂不归档**，`tasks.md` 已按机制写「**预期观察窗口：7 天**」（供
  `工具-变更包自动归档.py` 读取），4.4 未勾。
- ⇒ **本行不转 `[S:done]`**——本行的判据是"真实来件按域落位且编号连续"，不是"代码写完了"。
- ⏸ **停在合并决策点**：泳道状态机已 `pause`（`B-0905_C` / wave 2 / lane `341-apply` /
  `merge_to_master`），**未擅自 ff 进 master**，等 Shao Peishen 答是否合并。
- 🔴 **执行体尚未生效，如实登记**：常驻监听 `ZhuopinAibotDevListener` 当前 `Running`，跑的是
  `.claude/worktrees/wecom-service-home` 那份 checkout 的代码。本次改动落在
  `claude/op0905o-domain-route-341` 分支上，**该 worktree 既未同步、服务也未重启** ⇒
  **生产上此刻机器人仍写机制环境文件**。要真正生效需依次：ff 合入 master → 同步
  `wecom-service-home`（它历史上落后过一两百个提交，见 `#438`）→ 重启该计划任务。
  **本班一律未做**（合并待拍板；重启常驻服务属触碰生产服务的动作，不在无人在场的
  无头会话里做）。这也正是 tasks 1.6"观察下一条真实来件"在本班无法验证的另一层原因。
- **`#313` 本次未触碰**（实测零命中，且其触碰区＝路径常量来源，与本行的"写入哪一份"不重叠）。
