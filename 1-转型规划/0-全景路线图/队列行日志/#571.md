---
title: "队列行日志 #571（§一）"
created: 2026-09-13
status: 生效
用途: 跨桌任务队列 §一 #571 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #571 · 外置段（2026-09-13 外置，工具第 1 批，1 段，原文原样）

> 外置人 CC-OP0913F-571；外置前整格 5664 B、共 3 段；本批外置第 2 段；本格保留第 1、3 段。

🔬 **2026-09-13 实证追段（Cowork 环境总线 `OP-0913-E`）——本缺口当日又咬一次，且是新形态**：⑴ 泳道 `op0913a` 04:14:28 **被 600 s 后台任务上限终止**（不是跑完），补问回 `OPENER_PARTIAL`，活儿以**未提交状态**悬在 worktree 里——`一次 git worktree remove 即永久消失`；发现它的不是任何机器，是本方 04:38 手工 `Get-ChildItem` 看日志大小。⑵ 🔴 **「无头棒收工探针」漏判**：同一批 04:16–04:18 的定时巡检回显 `[NO-SIGNAL] 无新收工、无新停滞（在跑 1 批）`——**而 PARTIAL 早在 04:14:28 已写盘**。⇒ 探针只认「收工」与「停滞」两态，**不认「被外部上限终止并留下 PARTIAL」这第三态**，于是一条已死的泳道在它眼里仍是「在跑」。⑶ 本会话内属于人守的动作逐个点名：轮询三条泳道日志、读收工报告、判 ff 去向、抢救未提交工作、代写本行与 `#556` 回写——**除 ff 本身外，全程无一由机器触发**。🔑 **判据一句：`[NO-SIGNAL]` 只证明「探针没看见」，不证明「没出事」——把「无信号」当成「无异常」，正是本行要消灭的那种人守。** 本段只登记实证，**未改探针一行**（总线不修工具）。 　🔴 **更正上一段（2026-09-13 07:2x 本地 Get-Date，`OP-0913-E` 自查）**：上段写「无头棒收工探针只认『收工／停滞』两态、不认被终止留 `PARTIAL` 的第三态」——**机制判断是错的，撤回**。**真因是探针根本看不见这批**：`0-学习与工具/工具-无头棒收工探针.py` L54 的 `BATCH_DIR_RE` 只认纯数字目录名（八位日期＋六位时分秒），`_batch_started_at` 对 `20260913-收口三泳道` 现取 **None** ⇒ 该批**不进扫描视野**，`done`／`stalled` 两条路都走不到。 **实证**：`--peek --json` 现取 `running` 里只有 `20260912-092223` 一条，本批既不在 `done` 也不在 `stalled`；而 `summary.txt` **04:14:33 就已写好**、正文第一行就是「op0913a … PARTIAL 补问」——**机器有能力读，只是那个文件从来没被这只探针打开过。** **盲区规模现取**：09-08 起 22 批中语义名 3 批（`20260912-portal197`／`20260912-三包apply`／`20260913-收口三泳道`），**进过 notified 名单的 0/3**，三批都已有 `summary.txt`。 🔑 **根因是两条机制互不知情**：「批次命名尾段必须是语义词、不得是单字母」（2026-09-08 立，两桌全局）改了上游产物形态，**没有人核下游消费者**——这正是 `#554`② 的镜像面：那条管**下线**，本例是**改约定**，同样没核覆盖面。 🔴 **同时更正 `B-0913_op0913a抢救落库` 那条 §二 与本会话 07:0x 前的口头结论**：凡写「探针漏判第三态」处，一律以本段为准。

---

# §一 #571 · 外置段（2026-09-13 外置，工具第 2 批，1 段，原文原样）

> 外置人 Cowork-环境总线-OP-0913-E；外置前整格 4387 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

✅ **⑶⑷⑸ 已落地（2026-09-13 08:0x 本地 Get-Date，CC 无头泳道 `op0913f-probe-batchdir-571`，`OP-0913-F`，批 B-0913_人守打通 A1）**：分支 `claude/op0913f-probe-batchdir-571` @ `db9d1f5`（已 push，**未 ff**，ff 走看护件 §五 预授权六条）。**改法**：⑶ `BATCH_DIR_RE` 改 `^\d{8}-.+$`（八位日期前缀＋任意尾段，前缀须是合法日期）；起跑时刻新函数 `_batch_started_at(dir)` → `launcher.json.started_at_utc`（UTC 折本地）→ 退目录 mtime，**不再从目录名推**，结果带 `started_from` 来源字段。⑷ `summary.txt` 非 `OK` 泳道进 `bad_lanes`：逐条点名 ＋ 只读 `git -C .claude/worktrees/<泳道> status --porcelain` 回显未提交改动数（找不到 worktree 亦写明「未找到」，不把不知道打成干净）；推送正文一泳道一行；`[NO-SIGNAL]` 改点名在跑的批而不只报个数。⑸ 单测 +12（纯数字名批／语义名批／含 PARTIAL＋worktree 回显，各带变异守卫），`23→35 passed`。**实测手段**：真实 `reports/opener-batch/` 上 `--peek --json` 对照——旧版（master）语义批 done＝0/3、`running` 只列 3 条纯数字批；新版 3/3 报出（`20260912-portal197`／`20260912-三包apply`／`20260913-收口三泳道`），`op0913a` 点名 PARTIAL 并回显 `.claude/worktrees/op0913a-556-sweep-visibility` 工作区干净（+194 行已由 `751495e` 抢救）。**零回归（差集口径）**：同命令 `pytest 0-学习与工具/test_工具-无头棒收工探针.py -q -rf` 分支 `35 passed／0 failed` vs 纯 master detached worktree `4 failed／19 passed`，`comm -13`＝空（无回归），`comm -23`＝4 条 `main()` 用例（夹具 NOW 固定 09-10、真实 now 出 24 h 窗——本支已改带 `--lookback-hours` 修好）。⚠️ **上线后一次性补报**：状态文件里无这 3 批，`poll-opener-batch` 下一轮会把它们当「新收工」推一次（属应报未报的补账，不是误报）。🟡 **配套支待一字母**：`claude/op0913f-v2-launcher-json-571` @ `94aed8e`（`工具-opener批处理执行v2.ps1` 非 `-Detach` 路径也写 `launcher.json`＋单测 +1，v2 全套 `17 passed` 6 min）——越出看护件 §五 ⑶ 给 A1 的触碰区，故单独成支、不进预授权。**本行主缺口（事件驱动收口，期望产出首段）未动、仍 open**。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-13 外置**（K2 口径，原文原样、可 grep，文件 md5:8e13dbb1，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#571.md`；本格只留原第 1、3 段（共 3 段）＋本指针（外置人 CC-OP0913F-571）。
