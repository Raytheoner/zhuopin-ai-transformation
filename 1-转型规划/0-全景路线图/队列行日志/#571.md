---
title: "队列行日志 #571（§一）"
created: 2026-09-13
status: 生效
用途: 跨桌任务队列 §一 #571 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #571 · 外置段（2026-09-13 外置，工具第 1 批，1 段，原文原样）

> 外置人 CC-OP0913F-571；外置前整格 5664 B、共 3 段；本批外置第 2 段；本格保留第 1、3 段。

🔬 **2026-09-13 实证追段（Cowork 环境总线 `OP-0913-E`）——本缺口当日又咬一次，且是新形态**：⑴ 泳道 `op0913a` 04:14:28 **被 600 s 后台任务上限终止**（不是跑完），补问回 `OPENER_PARTIAL`，活儿以**未提交状态**悬在 worktree 里——`一次 git worktree remove 即永久消失`；发现它的不是任何机器，是本方 04:38 手工 `Get-ChildItem` 看日志大小。⑵ 🔴 **「无头棒收工探针」漏判**：同一批 04:16–04:18 的定时巡检回显 `[NO-SIGNAL] 无新收工、无新停滞（在跑 1 批）`——**而 PARTIAL 早在 04:14:28 已写盘**。⇒ 探针只认「收工」与「停滞」两态，**不认「被外部上限终止并留下 PARTIAL」这第三态**，于是一条已死的泳道在它眼里仍是「在跑」。⑶ 本会话内属于人守的动作逐个点名：轮询三条泳道日志、读收工报告、判 ff 去向、抢救未提交工作、代写本行与 `#556` 回写——**除 ff 本身外，全程无一由机器触发**。🔑 **判据一句：`[NO-SIGNAL]` 只证明「探针没看见」，不证明「没出事」——把「无信号」当成「无异常」，正是本行要消灭的那种人守。** 本段只登记实证，**未改探针一行**（总线不修工具）。 　🔴 **更正上一段（2026-09-13 07:2x 本地 Get-Date，`OP-0913-E` 自查）**：上段写「无头棒收工探针只认『收工／停滞』两态、不认被终止留 `PARTIAL` 的第三态」——**机制判断是错的，撤回**。**真因是探针根本看不见这批**：`0-学习与工具/工具-无头棒收工探针.py` L54 的 `BATCH_DIR_RE` 只认纯数字目录名（八位日期＋六位时分秒），`_batch_started_at` 对 `20260913-收口三泳道` 现取 **None** ⇒ 该批**不进扫描视野**，`done`／`stalled` 两条路都走不到。 **实证**：`--peek --json` 现取 `running` 里只有 `20260912-092223` 一条，本批既不在 `done` 也不在 `stalled`；而 `summary.txt` **04:14:33 就已写好**、正文第一行就是「op0913a … PARTIAL 补问」——**机器有能力读，只是那个文件从来没被这只探针打开过。** **盲区规模现取**：09-08 起 22 批中语义名 3 批（`20260912-portal197`／`20260912-三包apply`／`20260913-收口三泳道`），**进过 notified 名单的 0/3**，三批都已有 `summary.txt`。 🔑 **根因是两条机制互不知情**：「批次命名尾段必须是语义词、不得是单字母」（2026-09-08 立，两桌全局）改了上游产物形态，**没有人核下游消费者**——这正是 `#554`② 的镜像面：那条管**下线**，本例是**改约定**，同样没核覆盖面。 🔴 **同时更正 `B-0913_op0913a抢救落库` 那条 §二 与本会话 07:0x 前的口头结论**：凡写「探针漏判第三态」处，一律以本段为准。
