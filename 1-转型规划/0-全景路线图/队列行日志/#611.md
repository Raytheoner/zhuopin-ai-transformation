---
title: "队列行日志 #611（§一）"
created: 2026-09-19
status: 生效
用途: 跨桌任务队列 §一 #611 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #611 · 外置段（2026-09-19 外置，工具第 1 批，7 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-A；外置前整格 4696 B、共 9 段；本批外置第 2、3、4、5、6、7、8 段；本格保留第 1、9 段。

🔴 **现象**：五条泳道里三条判 `FAIL(main-leak)`（A1 `fi3-fi6`／A2 `fi5`／A4 `fi8`），拦到的**是同一个文件**：`M 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`。而三条泳道**各自都已正常收工**（A1 `OPENER_PARTIAL: 停在🟡 design 审`、A2/A4 `OPENER_DONE`），产出完整、四条分支均已 push 并于同日 ff 入 master、**零工作损失**（第三道闸只出补丁、不自动撤回）。

━━━

🔑 **根因＝两个各自正确的机制在接缝处互相矛盾**：**协议〇 要求**泳道收工回写主仓的队列行（本次五条泳道行的「收工」段就是这么写的）；**第三道闸判**「主仓新增非白名单脏文件即 FAIL」，白名单只有 `reports/` 与 `合入登记/pending-ff.jsonl`（L384–L388，队列 `#600` ⑷）⇒ **队列文件在主仓，照协议办事必然踩闸**。

━━━

🔴🔴 **比冲突更坏的是：它是竞态。** A5 `fi10` 同样回写了队列却判 OK——只因 sweep 恰好在它退出前把队列文件提交掉了。⇒ **同一件正确的事，有时 FAIL 有时 OK**。**一个把成功报成失败、而且时好时坏的判据，比没有判据更糟——它会训练下一个人忽略 summary**（与 `#550` 哨兵那次逐字同族）。

━━━

📌 **本次的连带损失**：A3 `fi6` 与 A1 同泳道串行，A1 的**误判** FAIL 直接让整泳道停链，A3 根本没起 session ⇒ **一个假 FAIL 吃掉了一条真泳道**。

━━━

🔑 **本行与 §一 `#610` 同族、判据同一句**：`#610` 是「聊天 vs 文件系统」两条通道，本行是「协议〇 vs 第三道闸」两个机制；今日还有第三例（`opener骨架` 写「必须写 `pwsh`」而本机 PS7 未进 PATH）。**三例形状相同：各自正确，接缝处矛盾。** ⇒ 他 2026-09-17 原话「我已经人守纠正很多次，总有一天会失灵」——**失灵点全在接缝上，不在任何单个机制内部**。

━━━

🆕 **2026-09-19 新变体实撞（`OP-0919-A` 批 `B-0919_Q2建造`）**：第三道闸把**看护者本人**同时段在主仓写的取证件判成了泳道的 `main-leak`——泄漏清单只有一件 `1-转型规划/0-全景路线图/取证件-2026-09-19-C段分支逐条判定.md`，泳道从头到尾没碰过它，泳道日志尾部是 `OPENER_PARTIAL`、心跳是 `DONE`。**后果是连锁的**：该闸判 `FAIL` ⇒ 批处理按 `FAIL*` 中断本泳道 ⇒ 排在其后的 A2（Q2 门户页）**根本没起**。🔑 **判词补一句：第三道闸量的是「主工作区有没有新脏文件」，而看护者本来就在主工作区干活——它分不清泳道的泄漏和看护者自己的产出。**⇒ 销号判据应补一条：用本批这份 `q2-A1-main-leak.patch` 回放，泄漏清单只含看护者自身产出时必须不判 FAIL。

━━━

⚠️ **同批第二个接缝**：看护者持编辑锁期间泳道收工，队列回写被锁挡 ⇒ 泳道只能以 `OPENER_PARTIAL` 收尾并把回写内容写进心跳等人代补。**看护者不应在泳道运行期间长持队列锁**——本次已实撞，回写已由 `OP-0919-A` 代补（见 `#592`）。

---

# §一 #611 · 外置段（2026-09-19 外置，工具第 2 批，2 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-K；外置前整格 6158 B、共 5 段；本批外置第 3、4 段；本格保留第 1、5 段。

🔴 **2026-09-19 第二次实撞，且这次是三条泳道同时中招（批 `B-0919_机制缩编` ＋ `B-0919_Q2建造`）**：`opener`/A1（`#620`）、`gitguard`/A3（`#618`）、`q2`/A2（`#612` §5.1）**三条全部干完活并 push**（哨兵分别为 `OPENER_DONE`／`OPENER_PARTIAL`／`OPENER_DONE`），**却全部被第三道闸判 `FAIL(main-leak)`**。三份 `*-main-leak.patch` 的泄漏清单合计只有三种文件：⑴ 两份队列真身（协议〇 明文要求泳道回写）；⑵ `1-转型规划/0-全景路线图/合入登记/ff-patrol-20260919.jsonl`。 🔑 **本次查实一处可当场收口的白名单缺口**：白名单收了 `合入登记/pending-ff.jsonl`，**却没收同目录、同性质、由 `工具-泳道分支合入.ps1` 六关每次运行必写的 `合入登记/ff-patrol-<yyyyMMdd>.jsonl`**（`#571`⑹ 立的那条留痕）——**一个每次 ff 都必然产生的文件没进白名单，等于每次 ff 之后起的泳道都会被判泄漏**。 🔴 **后果仍是连锁的**：`opener` 泳道 A1 被判 FAIL ⇒ 批处理按 `FAIL*` 中断本泳道 ⇒ **排在其后的 A2（`#616` 零命中断言守）第二次没能起**（上一次是 `#612` 门户页）。**同一个误判，一天之内冲掉了两条已排好的后续泳道。** ⇒ 销号判据补一条：用本日三份 `*-main-leak.patch` 回放，三条必须全部不再判 FAIL。

━━━

✅ **2026-09-19 销号判据所需取证已齐（`OP-0919-J` 续棒，源码级坐实）**：**白名单真身＝`0-学习与工具/工具-opener批处理执行v2.ps1:404`**，逐字为 `-not ($lp -eq 'reports' -or $lp -like 'reports/*' -or $lp -eq '1-转型规划/0-全景路线图/合入登记/pending-ff.jsonl')`——**只有三项**。而同一个 `合入登记/` 目录下的 **`ff-patrol-<yyyyMMdd>.jsonl` 是每次 ff 六关必写的留痕**（`#571`⑹ 所立），现取已攒 **7 份**（09-13～09-19，19 KB～39 KB 不等），**偏偏不在白名单里**。🔑 **一个每次 ff 都必然产生的文件没进白名单，等于每次 ff 之后起的泳道都会被判泄漏。** **本日 `main-leak` 实例现取＝5 份**（🔴 本方先前口述「三份」系记忆，现取更正）：`20260919-075655/q2-A1`／`085903/gitguard-A3`／`085903/opener-A1`／`090057/q2-A2`／`095859/opener-A2`。**五份的泄漏清单合计只有三种文件**：两份队列真身（协议〇 明文要求泳道回写）＋ `合入登记/ff-patrol-20260919.jsonl`。 ⇒ **销号判据可执行化**：修法后用这 5 份 patch 回放，**五条必须全部不再判 FAIL**；最小修法＝白名单把 `1-转型规划/0-全景路线图/合入登记/*.jsonl` 整目录收进（或至少补 `ff-patrol-*.jsonl`），队列真身那一支仍须按本行原设计另解（协议〇 要求写 vs 第三道闸禁写，是两个各自正确的机制在接缝处打架）。🔴 **本棒不改 `工具-opener批处理执行v2.ps1`**——`#565` 立有「不得直接改本脚本」，`#567` 只授权了 `-Detach` 分支；本行要改白名单须自带授权，属建造侧另派。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **7 段已于 2026-09-19 外置**（K2 口径，原文原样、可 grep，文件 md5:28368c05，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#611.md`；本格只留原第 1、9 段（共 9 段）＋本指针（外置人 Cowork-泳道看护-OP-0919-A）。

---

# §一 #611 · 外置段（2026-09-19 外置，工具第 3 批，1 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-K；外置前整格 4238 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

✅ **2026-09-19 `OP-0919-K` 跑了销号判据⑶ 的全量回放——派单件 `OP-0919-I` 所述根因不完整，且可当场证伪。** **手段**＝`reports/_op0919k/replay611.py`（把 `工具-opener批处理执行v2.ps1:404-409` 的 `Where-Object` 过滤逻辑逐字复刻成 Python）喂进 `reports/opener-batch/` 下现存**全部 15 份** `*-main-leak.patch`；其中 `20260916-204736/596`／`/599` 两份是老格式（纯 `git diff`、无 `----- 文件 -----` 表头），本回放器不解析、**不计入分母** ⇒ **有效分母 13**。 **四套白名单方案重判**：**A** 现行（`reports/`＋`合入登记/pending-ff.jsonl`）FAIL **13/13**；**B** ＝A＋`合入登记/ff-patrol-<yyyyMMdd>.jsonl`（**派单件所述的那一项**）FAIL **13/13**——**一条都没转绿**；**C** ＝B＋两份队列物理文件 FAIL **3/13**；**D** ＝C＋`队列行日志/#N.md`（K2 外置件）FAIL **1/13**。 🔑 **判词一句：泄漏清单里补齐了一项，不等于那一条能转绿——销号判据是「整条不再 FAIL」，不是「少了一个文件」。** 🔴 **主因正是本行 ⑴ 自己写着、至今未拍的那个二选一**（加进白名单 vs 判定前先跑一次 sweep）：13 份里 **12 份**的泄漏项全部落在「协议〇 要求泳道／看护者写」的三类文件上——两份队列（9 份）、`队列行日志/#N.md`（2 份）、`ff-patrol`（4 份，每次 ff 必写）。 🔴 **剩下 1 份白名单永远治不了**：`20260919-075655 / q2-A1` 的泄漏清单只有一件 `取证件-2026-09-19-C段分支逐条判定.md`——看护者本人同时段在主仓写的取证件，泳道从没碰过它，**取证件文件名是任意的、不可能进白名单** ⇒ 即本行 2026-09-19 已记的那条变体，本次回放坐实它**不被任何白名单方案覆盖**，须换判法（按泳道 worktree 实际触碰过的文件归属，或按提交作者／时间窗），**不并入本批**。 ⇒ **形态未拍板属 🟡 档，本线停等一字母、只备件不派出**（派单件写「出看护件转 CC」，但形态没定就出件等于替他拍板）。**本方建议＝走方案 D，三类一次补齐**，不走「判定前先跑 sweep」——后者会让第三道闸依赖另一条常驻链路的时序，而那正是本行判词里那个竞态的成因（`fi10` 判 OK 只因 sweep 恰好赶上），**用竞态去修竞态**；白名单是纯静态判据，可被单测钉死。 逐条回放表与四方案定义见 `1-转型规划/0-全景路线图/取证件-2026-09-19-OP0919K-611回放与戊档判定.md` §一～§三。**本次未改工具一个字符。**

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **2 段已于 2026-09-19 外置**（K2 口径，原文原样、可 grep，文件 md5:e520c248，工具第 2 批）见 `1-转型规划/0-全景路线图/队列行日志/#611.md`；本格只留原第 1、5 段（共 5 段）＋本指针（外置人 Cowork-泳道看护-OP-0919-K）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。

---

# §一 #611 · 外置段（2026-09-19 外置，工具第 4 批，2 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-K；外置前整格 5796 B、共 5 段；本批外置第 3、4 段；本格保留第 1、5 段。

🛠 **2026-09-19 `OP-0919-K` 把本行的回放手段收编成常驻工具**：`0-学习与工具/工具-main-leak回放.py`（＋ `test_工具-main-leak回放.py`，8 passed）。**白名单判据现取自 `工具-opener批处理执行v2.ps1`**（解析 `-not ($lp -eq …)` 块），解析不到即 fail-loud —— 🔴 **不在回放器里留第二份硬编码白名单**：闸改了而回放器没跟上时，回放会报「全过」而生产照旧 FAIL，那正是本行判词里「接缝处各自正确、合起来矛盾」的同一形状。候选修法用 `--extra '<glob>'` 追加白名单试算（今日 A/B/C/D 四方案即由它跑出）。**顺带治掉一次真实的 token 浪费**：判 FAIL 只需要泄漏清单里的**文件名**，不需要 diff 正文——`cat` 单份 `opener-A1-main-leak.patch` 实测 **69.7 KB**（大半是队列行完整 diff），本工具出**全量 15 份**的逐条表只 **1.8 KB**。⇒ 已落纪律：`*-main-leak.patch` 禁 `cat`，走本工具（`.claude/rules/队列与落库.md`）。**本行形态仍未拍，看护件仍未派出。**

━━━

✅ **2026-09-19 形态已拍：Shao Peishen 答 `1a` ＝ 方案 D**——白名单一次补齐三类（`合入登记/ff-patrol-<yyyyMMdd>.jsonl` ＋ 两份队列物理文件 ＋ `队列行日志/#<N>.md`），**不走「判定前先跑一次 sweep」**（那会让第三道闸依赖另一条常驻链路的时序，正是本行判词里那个竞态的成因）。🚀 **已派出建造**：看护件 `1-转型规划/0-全景路线图/看护件-【CC】第三道闸白名单611-2026-09-19.md` 转「已派出」，批 `B-0919_三道闸611`，泳道 `leakgate`·A1，OP `OP-0919-L`，分支 `claude/op0919l-leak-gate-611`，由 `工具-opener批处理执行v2.ps1 -FullAuto -Yes -Detach` 后台起。🔴 **销号判据仍是「整条不再 FAIL」**：验收用 `python 0-学习与工具/工具-main-leak回放.py`，13 份有效补丁中 **12 份必须转绿**；唯一允许仍 FAIL 的是 `20260919-075655 / q2-A1`（泄漏清单只有看护者自己写的取证件，文件名任意、白名单治不了，属换判法那一面，**不在本批**）。⚠️ **收工不得表述为「第三道闸已根治」。** ✅ 2026-09-19 `OP-0919-L`（泳道 `op0919l-leak-gate-611`）建造完成：白名单三类已一次补齐（`0-学习与工具/工具-opener批处理执行v2.ps1` 第三道闸新增 `ff-patrol-*.jsonl`／两份队列物理文件／`队列行日志/#*.md`），新增单测 `test_泳道回写队列三类白名单后不判FAIL`（`test_工具-opener批处理执行v2.py`）覆盖「泳道回写队列后 sweep 未及提交」竞态。narrow 回归：`脚本建隔离worktree四场景_v2_600`（5+1 passed）＋`test_工具-main-leak回放.py`（8 passed）。自举验收（销号判据⑶）：`python 0-学习与工具/工具-main-leak回放.py` 13 份有效补丁 12 转绿，唯一仍 FAIL 的 `20260919-075655/q2-A1` 属允许项（看护者自身取证件，属换判法那一面，本批不做）。⚠️ 未根治：⑴⑵ 与「区分泳道泄漏与看护者自身产出」一面均未动，不得表述为已根治。产出＝分支 `claude/op0919l-leak-gate-611`（commit `2a15a07d`）已 push origin，未 ff master；登记 §二 批次 `B-0919_三道闸611`，待经『已授权待合』登记走 `工具-待合分支巡检.ps1` 或看护者收工时串行 ff。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-19 外置**（K2 口径，原文原样、可 grep，文件 md5:520285bb，工具第 3 批）见 `1-转型规划/0-全景路线图/队列行日志/#611.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 Cowork-泳道看护-OP-0919-K）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。
