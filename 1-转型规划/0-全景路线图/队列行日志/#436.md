---
title: "队列行日志 #436（§一）"
created: 2026-09-06
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #436 的行外承载件——⑶ 第二波蓝图三向的 CC 四件任务书全文（行内 ≤4 KB 口径，行内只留指针）。泳道按 (i)～(iv) 对号读，做什么／不做什么／收工／触碰区以本节为准。
---

# §一 #436 ·「2026-09-06 派出」四件任务书

> 来源：`1-转型规划/0-全景路线图/端到端构建workflow优化-方案-2026-09-06.md` §四 W1／W3／W5／W7 ＋ §六 决议 ②③④（Shao Peishen 2026-09-06）。看护件＝`看护件-泳道看护批B-0906_P-需求侧五泳道-2026-09-06.md`。
> 🔴 通用收工三条：① 回写 `#436` 行状态列**只追加一段**（本件编号＋结论＋commit 哈希）；② 登记 §二 待 commit 批次（路径反引号包裹、只列本批脏改动）；③ 判是否命中 openspec 三门槛（改全项目口径／涉鉴权可见性／改既有模块对外语义），命中即止步 design 起草并 `pause --action-key openspec_design_review --no-notify`。

## (i) 登记 CLI `决策点:` 校验 —— 泳道 `436-decision-field-guard`（OP-0906-R）

做什么：
1. `0-学习与工具/工具-跟进信README登记.py append` 在登记前读信件 `.md` frontmatter：`决策点:` **缺失或为空即拒登记**（exit 非 0），拒绝文案给出路——「按 skill `zhuopin-followup-letter` v3.8 §5 步骤 1bis 写 `决策点: N 项（a / b / c）`；B 类通报信写 `0 项（…无需其决策）`」。
2. 值形态弱校验：须以数字＋「项」开头（`0 项` 放行）；不校验括号内容。
3. 单测三例：缺字段拒／`0 项（…）` 放行／`3 项（a / b / c）` 放行；跑既有 `test_工具-跟进信README登记.py`（若存在）零回归。
4. 口径来源写进 docstring 指针：`design审前置-口径点台账三开放点收敛-2026-09-01.md` §2.3（P1–P4）。

不做什么：不改 README 主表；不动 `set-status`；不回溯已发信；不改 skill 源码（Cowork 已改）。

## (ii) 新场景 `intent.md` 前置闸 —— 泳道 `436-intent-gate`（OP-0906-S）

做什么：
1. 新 lint `0-学习与工具/工具-场景包intent闸lint.py`：扫 `openspec/changes/<pkg>/`（非 archive），凡 `proposal.md`／`tasks.md` 触碰 `4-数字员工/<部门>/<场景>/` 的**场景类**包，要求该场景目录存在 `intent.md` 且 frontmatter `status: 已确认`；缺则失败并点名包名＋期望路径。机制类包（不触碰 `4-数字员工/`）不适用；深化类（场景目录已有「部署状态」段的场景）不适用。
2. 🔴 存量豁免写死在 lint 里并带到期日 **2026-10-31**：`sc4-contract-clause-extraction／sc10-bom-review-material-governance／sc11-inventory-transfer／fi5-expense-audit-mvp／fi6-anomaly-detect-mvp／fi8-cashflow-forecast-mvp／fi9-rd-cost-mvp／fi10-inventory-writedown-mvp` 与 SC2（历史件 `需求grill产出-2026-08-18.md`）；到期后一律硬拦；每包补完 grill（`intent.md` 已确认）即从名单删。
3. 接 `.github/workflows/ci.yml` 新 job（形态同 `bootstrap-stub-lint`）；`openspec/config.yaml` `rules.proposal` 加一条 MANDATORY 提示句（两桌起草时可见）——**提示句不是闸，lint 才是**。
4. 单测：有 intent 且已确认／有 intent 未确认／无 intent／豁免名单内／到期后豁免失效 五例。

不做什么：不改 `/opsx:propose` 命令本身；不给任何包写 `intent.md`（那是 Cowork grill 会话的产出）；不动 archive。

## (iii) 使用度量只读采样脚本 —— 泳道 `436-usage-sampler`（OP-0906-T）

做什么：
1. 新 `0-学习与工具/工具-使用度量采样.py`：输入若干日志路径（平台 `audit` JSONL ＋ 场景 web 访问日志），输出 Markdown 采样报告：**场景×用户×日**的访问次数、AI 决策次数、**零触发功能清单**（按路由／动作枚举）、日志缺口（无法识别用户／无时间戳／格式不一）。
2. 三档判定**全部确定性、不涉模型**：连续 **3** 天零使用→报告内「记日志」行；**7** 天→「只读诊断」行（列出该用户最近一次企微回件／文件落档日期，取自 `7-外部文档/` 文件 mtime 与 README 主表，只读）；**14** 天→「需你定夺」候选行。🔴 **不写 README、不推企微、不自动起草任何信**。
3. mock 先行：`tests/` 内构造 FI2（`8094` audit 形态）与 SC8（`8091` 连接器审计形态）两种 mock 日志，各一例正常／一例 14 天零使用；真实 `.51` 日志**本批不读**，实跑列「待回内网」交 `zhuopin-lan-closeout`（脚本用法一行写进收工回写）。
4. 顺带核 `#358`：脚本对 309 MB 级日志用流式读取；报告末附「轮转建议」一段（只建议、不实施，`#358` 须先走 design 审）。

不做什么：不触碰 `.51`；不改任何场景代码；不新增定时任务（job 化待采样报告过目后另议）。

## (iv) ~~「发布后该起草跟进信而没起草」扫描~~ —— 🔴 撤回（2026-09-06 17:40 自查）：机制已存在

`5-平台底座/wecom-aibot-service/scripts/draft_gap_check.py`（队列 `#245`：近 N 天已部署场景 vs README 已起草信行交叉比对）已并入 `工具-落库sweep.py` 第 11 类常驻告警（`#382`⑵，每轮回显「📝 跟进信起草缺口检测」，零命中不省略；`reports/sweep-commit.log` 2026-09-06 09:17 UTC 一轮实测回显「无缺口」）。本件与之重复，**不派**；`OP-0906-U` 作废。成因＝提方案前未查仓库已有能力（08-30 评估件 R3 同族），已计 `#284`。方案件 §四 W7 同步更正。以下原文保留供追溯：

～～原做什么：
1. `0-学习与工具/工具-落库sweep.py` 告警类新增一项：遍历 `4-数字员工/*/*/CLAUDE.md`，取「部署状态」段最近日期（段内 `YYYY-MM-DD` 最大值）；按场景码（FI2／SC8／QD-B…）在跟进信 README 主表匹配该场景最近一封信的日期（用 `工具-跟进信README查询.py --digest --json`，不 Read 主表）；**部署日期晚于最近信 >3 天 ⇒ 告警**（企微逃生通道口径同现有第 4 类告警）＋ 输出一行「§四 登记候选」文案，**只报不起草、不写 README、不写队列**。
2. 单测两例：该报（部署 09-01、最近信 08-20）／不该报（部署 09-01、最近信 09-02；或无部署段）。
3. 场景码↔README 匹配规则写进 docstring；匹配不到的场景归「无法判定」单列，不算告警。

不做什么：不改 sweep 其余告警；不动跟进信 README；不改场景 CLAUDE.md。承 `#124` 阶段二第三条前置（2026-08-03 挂起）。

---

# (ii) 收工回写全文 —— 泳道 `436-intent-gate`（`OP-0906-S`，2026-09-06）

> 🔴 **本节由看护者 `OP-0906-P` 代写**：该泳道按看护件 B-0906_P「不碰主仓工作区、不在主仓 commit」把 `#436` 回写与 §二 登记转出给看护者，本节即其转出文案的承接载体（行内只留指针段）。分支上的建造本体一字未动。

**建成物**：`0-学习与工具/工具-场景包intent闸lint.py`（382 行）＋ 同目录 `test_` 单测 14 例；`.github/workflows/ci.yml` 新 job `scene-intent-gate-lint`（`--enforce`，形态同 `bootstrap-stub-lint`）；`openspec/config.yaml` `rules.proposal` 加一条 MANDATORY 提示句。分支 `claude/op0906s-intent-gate` commit `1c4de05`，已 push，**未 ff 进 master**。

## 🔴 拍板原文「闸落 openspec validate」实测做不到 —— 已按看护件 §一.2 改走 CI lint

`openspec/config.yaml` 的 `rules.proposal` 只是喂给起草方的**提示文本**，`validate` 不读它。**取证手段与输出**：本仓库 8 份 `proposal.md`（`aibot-inbound-whitelist-li-jiaolong`／`aibot-receipt-route-by-source-group`／`batch-registration-precheck`／`fi1-warehouse-reconcile`／`lan-closeout-skill`／`open-pool-assigned-field-and-opener-env-filter`／`qd-b-cross-module-tier3`／`sc8-kit-date-rule1-start`）**都缺** config.yaml 点名 MANDATORY 的「知识资产三问」节，而 `openspec validate --all --strict` 仍报 `169 passed, 0 failed`；`openspec --help` 与 `openspec validate --help` 全表无任何自定义规则／插件挂载点（CLI v1.7.0）。⇒ 闸落 CI job；`config.yaml` 的提示句里写明「**本条只是提示句、不是闸，闸在 CI job**」，防止后来者以为 validate 会拦。

## 判据

三步：建场景索引（从 `4-数字员工/<部门>/<码>-<名>/` 取场景码）→ 判包属于哪个场景（信号 A ＝包名前缀、信号 B ＝ spec capability 前缀，**且**正文触碰该场景目录，两侧都要命中）→ 查 `intent.md` 存在且 frontmatter `status: 已确认`。豁免三层：深化类永久（闭集）／存量 8 包至 2026-10-31／SC2 历史件至 2026-10-31。违规文案点名包名 ＋ 期望路径 ＋ 出路（指向 skill `zhuopin-requirement-grill` 与 rules 正本）。

## 验证手段与结果（逐条可复跑）

| 手段（命令） | 结果 |
|---|---|
| `python 0-学习与工具/工具-场景包intent闸lint.py --enforce` | 退出码 **0**；58 个在办包，违规 **0**（合规 0／豁免 16／疑似 0／机制类仅引用 4） |
| 同上 `--today 2026-11-01 --enforce` | 退出码 **1**，8 个存量包如期转违规 —— **到期后豁免失效实证** |
| `pytest 0-学习与工具/test_工具-场景包intent闸lint.py` | **14 passed**（含派单件点名五例：有 intent 已确认放行／有 intent 未确认违规／无 intent 违规且点名期望路径／豁免名单内放行／到期后豁免失效） |
| `openspec validate --all --strict`（改 config.yaml 后） | 退出码 0，`169 passed, 0 failed`；`rules.proposal` 另用 `yaml.safe_load` 核过 6→7 条 |
| `pytest`（扫仓族四份 ＋ 本件） | **150 passed**，0 failed |
| `工具-引导样板lint.py --enforce` / `工具-队列结构lint.py` | 均退出码 0 |

## 🟡 D6 停点 ＝ ② 改口径/判据/阈值（`pause --action-key change_criteria --no-notify` 已登记）

派单件写的深化类判据是「场景目录 `CLAUDE.md` 已有『部署状态』段」。**实测该字面标记只命中 FI2／QD-B／SC2 三个**，会让 CI job 上线首日即红 **4 个存量包**：`sc8-atp-batch2-closeout`／`sc8-kit-date-rule1-start`／`sc8-material-board-view`（SC8 已部署 `.51:8091`，但部署信息写在 `## 5. 状态时间线` 而非叫「部署状态」的段里）＋ `fi1-warehouse-reconcile`。泳道把深化类扩为与「2026-08-18 grill 机制生效前已建成的场景名单」**取并集**；名单由 `git log --diff-filter=A --format="COMMIT %ad" --date=short --name-only -- "4-数字员工"` 实测首次入库日期得出（SC1 06-06／SC8 06-10／O2 06-10／SC3 06-10／SC5 06-11／QD-B 06-27／FI1 06-29／QD-A 07-04／SC7 07-06／FI2 07-07 全部早于 08-18；八个新场景均为 09-03），是**闭集、永不增长**。口径依据＝`需求收敛第一道把关-grill机制融合方案-2026-08-18.md` §六边界与 G3(a) 原文「只对新场景开工前强制，深化类不用」。**这一处是泳道的判断，未代拍板**，代码已在分支上，停在此点等 Shao Peishen 一个字母。

## 未做 / 边界

未改 `/opsx:propose` 命令本身；未给任何包写 `intent.md`（那是 Cowork grill 会话的产出）；未动 archive。**「待回内网」项：无**（纯库内，未触碰 `.51`／企微／定时任务）。`index.lock` 撞击 0 次，无「登记待补」。

## ⚠️ 顺带上报（非本件所致，当前 master 本就红）

- `0-学习与工具` 全量 pytest **未跑完**（约 25 分钟到 62% 判不可行后停），改分段跑；实测 3 条 failed **全部与本件无关且为既有失败**：`test_发企微.py::test_repo_root_falls_back_when_not_a_git_repo`（环境性，`TMP` 取到 8.3 短路径 `PAULSH~1` 与长路径 `Paul Shao` 不等）、`test_工具-CLAUDE进度段lint.py::test_当前master三条哨兵全部命中`（`4 != 3`）与 `::test_真实根CLAUDE可被解析为结构A`（读真实根 `CLAUDE.md`，其「当前进度」段现为空）。
- `工具-密钥扫描lint.py` 退出码 1（`工具-落库sweep.py` 三处 `*_KEY=` 常量名被判疑似凭据赋值）。
- `工具-CLAUDE进度段lint.py --enforce` 退出码 1（`5-平台底座/wecom-aibot-service/CLAUDE.md` 第 10 条 4974 字符 > J3 上限 1200）。


---

# §一 #436 · 状态列 K2 外置（2026-09-06 22:0x，`OP-0906-W` 触碰本行时按第 4 项 (a) 顺手做，7 段，md5:e8f8f0fa）

> 外置前整格原文（含 2026-09-06 拍板段／派出段／更正段／(i)(ii)(iii) 收工段／合入段）。原文原样、可 grep；行内留首段＋指针＋末段。

[S:open][D:机] 待领（⑵ 可即领；⑶ 软序于 lan-closeout 灰度与回灌落地后） 　━━━　🔎 **本行来源不可追（2026-08-30 `OP-0829-W` 取证登记，只报不改）**：Shao Peishen 当日问「`OP-0830-B` 我好像没手动发起过」，本线逐一核过**三处可查的地方，全部答不出是哪条会话立的本行**——⑴ **本行「登记」列为空**（该列正是为记来源而设）；⑵ **git author 是统一身份** `Paul Shao <peishenshao@gmail.com>`，每条会话都用它，author 不含信息（commit `324a370` @2026-08-30 08:19:12 本地）；⑶ **锁历史**里没有任何 `*OP0830B*` 身份。**排除项**：不是 `OP-0830-C`（其 transcript 全文 `OP-0830-B` 只命中 1 次，且是 `acquire` 回显里读到的 `sweep 落库 B-0830_2_OP0830B_436登记`，即读到别人的记录）；**也不是裸手改**（`reports/queue_edit_lock_bypass.jsonl` 今日无新记录）。⇒ **它规规矩矩走了锁、却仍然查不出是谁——因为「登记」列没填。** 🔑 **通用判据**：**队列行的可追性不靠 git、不靠锁，只靠「登记」列**——前两者对所有会话都是同一个身份／只记锁的持有者名而非行的作者。**建议**：把「§一 新行『登记』列非空」加进锁的 release 结构检查（与 ⑹ 登记完整性同一处咽喉，形态一致）。本条不另立行，待总线派发。 ━━━ 🔒 **2026-09-06 Shao Peishen 拍板五条（Cowork 环境总线 `OP-0906-W`）**：⑶ 蓝图方案件已出＝`1-转型规划/0-全景路线图/端到端构建workflow优化-方案-2026-09-06.md`（§六 决议记录：① 不改协议〇.9、上限 22 不提，`#439` 去 🛑，「余量优先需求侧」落 rules/队列与落库；② `决策点:` 恢复＝skill v3.8＋登记 CLI 校验；③ `intent.md` 定名＋闸落 openspec validate；④ 使用度量立项但**只读采样、三档 3／7／14、不写 README、不自动起草**；⑤ 8 包逐包补 grill、会话末当场审）。**本行承接其 CC 四件（并入优先、零新行）——「2026-09-06 派出」＝(i) 登记 CLI `决策点:` 校验／(ii) 新场景 `intent.md` 前置闸（validate＋CI lint，存量 8 包豁免到 2026-10-31）／(iii) 使用度量只读采样脚本（mock 先行，真实采样列「待回内网」）／(iv) 「发布后该起草信而没起草」扫描并入 sweep 告警。🔴 四件任务书全文（做什么／不做什么／收工／触碰区）＝`1-转型规划/0-全景路线图/队列行日志/#436.md`「2026-09-06 派出」节，泳道按 (i)～(iv) 对号读。**看护件＝`1-转型规划/0-全景路线图/看护件-泳道看护批B-0906_P-需求侧五泳道-2026-09-06.md`（`B-0906_P`，看护者 `OP-0906-P`，泳道 Q～U）。**人的那半**（⑶① 8 包补 grill）＝Cowork 会话逐包做，首个 FI10 opener＝`1-转型规划/0-全景路线图/开场prompt-【Cowork】FI10需求grill-首个样本-2026-09-06.md`（`OP-0906-V`）。W4「可点的判例」待 `#394` 中继后另立行。 ━━━ 🔴 **更正（2026-09-06 17:40，`OP-0906-W` 落档后自查）**：上段「2026-09-06 派出」之 **(iv) 撤回**——机制已存在：`5-平台底座/wecom-aibot-service/scripts/draft_gap_check.py`（`#245`）已并入 `工具-落库sweep.py` 第 11 类常驻告警（`#382`⑵，每轮回显「跟进信起草缺口检测」）。本批 `B-0906_P` 改为四泳道（Q～T），`OP-0906-U` 作废；看护件 §〇 与 `队列行日志/#436.md` (iv) 节已同步更正。成因＝提方案前未查仓库已有能力，计 `#284`。 ━━━ ✅ **(iii) 使用度量只读采样脚本已建**（`OP-0906-T`，2026-09-06）：`0-学习与工具/工具-使用度量采样.py` ＋ 同名 `test_` 单测（20 passed）；三档 3/7/14 确定性、只读，不写 README／不推企微／不起草信；顺带核 `#358` 只出「轮转建议」一段、未实施。分支 `claude/op0906t-usage-sampler` commit `63fef1f`（已 push，**未 ff 进 master**）。真实 `.51` 采样＝留步「待回内网」。  ━━━ ✅ **`OP-0906-R` 完工（2026-09-06，泳道 `436-decision-field-guard`，批 `B-0906_P`）**：⑶「2026-09-06 派出」**(i) 登记 CLI `决策点:` 校验**已建成——`工具-跟进信README登记.py append` 新增必填 `--letter-path`，登记前读信件 frontmatter，`决策点:` 缺失／为空／形态不合判据即拒登记（exit 1，未取锁、未读主表）；判据复用 `工具-跟进信frontmatter校验.py` 的 `parse_frontmatter`／`RE_DECISION`，不新造第二份正则；`0 项（…）` 放行、括号内容不校验；成功路径 `[PLAN]` 行回显取值。**验证手段**：`python -m pytest 0-学习与工具/test_工具-跟进信README登记.py` **30 passed**（改前 18）、邻接六件零回归 **132 passed**、真实语料 `--dry-run` 冒烟三例过（未取锁未写盘）。分支 `claude/op0906r-decision-field-guard` ＠ commit `83cd43c` 已 push、🔴 **未合入 master**。 🟡 **D6 停点＝③ openspec design 审**（门槛③「改既有模块对外语义」命中：新增必填参数、同输入由放行变拒绝）：出包 `openspec/changes/followup-decision-point-gate`（proposal＋design＋spec delta＋tasks，`openspec validate --strict` 过），已 `pause --action-key openspec_design_review --no-notify`；**D1–D4 四点待 Shao Peishen 拍板，选项与代价全文在该包 `design.md`**（D1 逃生阀留不留·荐 (a) 不留／D2 是否比对信件与命令行一致性·荐 (a) 或 (c)／D3 存量豁免期·荐 (a) 无需／D4 与 `followup-readme-phase2` 排序·荐 (a) 先收尾那包）。 **未做**：未改 README 主表／未动 `set-status`／未回溯已发信／未改 skill 源码；rules 示例句补参数与 skill §5 1bis「建成前人守」半句退休已列该包 tasks §4（后者须先真实验活一次）。**§二 批次刻意未登记**（改动已 commit 在自己分支、主工作区无对应脏文件，登记会让 sweep 找不到清单）。**「待回内网」项：无。** ⚠️ 本行状态格追加前已 4.5 KB、超 K2 口径，本次只追加最短段；**K2 外置待看护者统一做**（本批 (ii)(iii) 正并发写同一行）。 ━━━ ✅ **(ii) 新场景 `intent.md` 前置闸已建**（`OP-0906-S`，2026-09-06，泳道 `436-intent-gate`；🔴 **本段由看护者 `OP-0906-P` 代写**——该泳道按看护件 B-0906_P「不碰主仓工作区／不在主仓 commit」把回写与 §二 登记转出）：新件 `0-学习与工具/工具-场景包intent闸lint.py` ＋ 单测 14 例 ＋ CI job `scene-intent-gate-lint`（`--enforce`）。🔴 **拍板原文「闸落 openspec validate」实测做不到**（`rules.proposal` 是提示文本、validate 不读它：8 份缺 MANDATORY 节的 proposal 仍报 `169 passed, 0 failed`；CLI v1.7.0 无自定义规则挂载点），按看护件 §一.2 硬边界改走 CI lint；`config.yaml` 只加提示句并写明「它不是闸」。**验证手段**：`--enforce` 退出码 0（58 包违规 0）／`--today 2026-11-01 --enforce` 退出码 1（8 存量包如期转违规，到期失效实证）／`pytest` 本件 14 passed、扫仓族合计 150 passed／`openspec validate --all --strict` 169 passed。分支 `claude/op0906s-intent-gate` commit `1c4de05`（已 push，**未 ff 进 master**）。🟡 **D6 停点＝② 改口径/判据/阈值**（已 `pause --action-key change_criteria --no-notify`）：深化类豁免判据由字面「部署状态段」扩为与「2026-08-18 前已建成场景」取并集——字面判据只命中 FI2/QD-B/SC2 三个，CI 上线首日会红 4 个存量包（SC8 三包＋FI1）。待 Shao Peishen 一字母。📎 **本件回写全文（判据／逐条验证表／停点成因／顺带上报三项）＝ `1-转型规划/0-全景路线图/队列行日志/#436.md`「(ii) 收工回写全文」节**，行内不复述。**「待回内网」项：无。** ━━━ ✅ **(ii)(iii) 已合入 master**（`OP-0906-P` 看护者，2026-09-06，Shao Peishen 现场答第 3 项 (a)）：`claude/op0906s-intent-gate` → master `3a68bf0`、`claude/op0906t-usage-sampler` → master `1994b53`，均 rebase 后纯 ff、无冲突。**合入后 master 验证手段与结果**：`pytest test_工具-场景包intent闸lint.py test_工具-使用度量采样.py -q` **34 passed ＋ 7 subtests**；`工具-场景包intent闸lint.py --enforce`／`工具-队列结构lint.py`／`工具-引导样板lint.py --enforce` 退出码全 0；`openspec validate --all --strict` **170 passed／0 failed**。深化类豁免判据按 Shao Peishen 第 2 项 (a) **采纳并集**（字面「部署状态」段 ∪ 2026-08-18 grill 机制生效前已建成场景，闭集），维持已实现形态、无需改码。🔴 **(i) 未合入**——Shao Peishen 答 **D4(a)「先收尾 `followup-readme-phase2` 再 apply 本包」**，实测该包 `tasks.md` 30 项已勾、**5.4（`#490` 回写销号）与 5.5（commit＋push master）两项未勾**，前提未成立；2026-09-06 复问第 1 项他再答 (a) 维持暂缓 ⇒ 分支 `claude/op0906r-decision-field-guard` ＠ `83cd43c` **原地待命、未 rebase**，合入时机＝phase2 那包 5.4／5.5 收尾后。其 D1(a) 不留逃生阀／D2(a) 维持不校验／D3(a) 无需存量豁免期 三点＝维持分支已实现形态、无需改码。K2 外置按第 4 项 (a) **交下一次触碰本行的泳道顺手做**（本行现 >7.9 KB，2026-09-11 起由告警转阻断）。**「待回内网」**＝(iii) 真实采样，按第 5 项 (a) 脚本推上 `.51` 就地跑、只取回报告，首轮不做功能全集声明件，交 `zhuopin-lan-closeout`。


---

# §一 #436 · 四次外置（2026-09-06 22:5x，`OP-0906-X` 看护批 B-0906_X 收工时按 K2 口径外置，md5:05cb20e0）

> 外置 2 段：⑴ 本行来源不可追的 `OP-0829-W` 取证段、⑵ 2026-09-06 `OP-0906-W` 续棒派出段（其派出的 (i)-合入已完成，收工段留在行内）。原文原样、可 grep；行内留首段＋末段＋指针。

[S:open][D:机] 待领（⑵ 可即领；⑶ 软序于 lan-closeout 灰度与回灌落地后） 　━━━　🔎 **本行来源不可追（2026-08-30 `OP-0829-W` 取证登记，只报不改）**：Shao Peishen 当日问「`OP-0830-B` 我好像没手动发起过」，本线逐一核过**三处可查的地方，全部答不出是哪条会话立的本行**——⑴ **本行「登记」列为空**（该列正是为记来源而设）；⑵ **git author 是统一身份** `Paul Shao <peishenshao@gmail.com>`，每条会话都用它，author 不含信息（commit `324a370` @2026-08-30 08:19:12 本地）；⑶ **锁历史**里没有任何 `*OP0830B*` 身份。**排除项**：不是 `OP-0830-C`（其 transcript 全文 `OP-0830-B` 只命中 1 次，且是 `acquire` 回显里读到的 `sweep 落库 B-0830_2_OP0830B_436登记`，即读到别人的记录）；**也不是裸手改**（`reports/queue_edit_lock_bypass.jsonl` 今日无新记录）。⇒ **它规规矩矩走了锁、却仍然查不出是谁——因为「登记」列没填。** 🔑 **通用判据**：**队列行的可追性不靠 git、不靠锁，只靠「登记」列**——前两者对所有会话都是同一个身份／只记锁的持有者名而非行的作者。**建议**：把「§一 新行『登记』列非空」加进锁的 release 结构检查（与 ⑹ 登记完整性同一处咽喉，形态一致）。本条不另立行，待总线派发。 ━━━ 📎 历史回写段（含 2026-09-06 五条拍板、派出 (i)–(iv)、更正、(i)(ii)(iii) 收工与合入段）已外置 `1-转型规划/0-全景路线图/队列行日志/#436.md`（2026-09-06 K2 外置，外置前整格 md5 e8f8f0fa）；四件任务书仍在该日志件「2026-09-06 派出」节 ━━━ 🔁 **2026-09-06 续棒（Cowork 环境总线 `OP-0906-W`）**：**(i)-合入**＝A2 分支 `claude/op0906r-decision-field-guard`＠`83cd43c` 的 D4(a) 前提已核满足（`followup-readme-phase2` tasks 5.4／5.5 实已完成：`#490` `[S:done]`、`5e95bd5` 在 master、`origin/master..master`＝0，已核勾），泳道 `436-decision-field-merge`（`OP-0906-Y`）做：检出该分支 → `git rebase master` → 跑 `test_工具-跟进信README登记.py`＋邻接单测（基线 30／132 passed）→ 全绿即报看护者串行 ff（不 force-push、不改分支内容；冲突即 `pause --action-key merge_conflict`）→ 合入后 `followup-decision-point-gate` 包 tasks 全 [x] 即 `/opsx:archive`，否则写 `暂不归档` 理由 → 回写本行一段（commit 哈希＋测试数）。不做：不改校验判据、不动 README 主表。(ii)(iii) 已合入 master 见日志件；(iii) 真实采样待回内网；(iv) 已撤回。看护件＝`1-转型规划/0-全景路线图/看护件-泳道看护批B-0906_X-需求侧续棒两泳道-2026-09-06.md`（`B-0906_X`，看护者 `OP-0906-X`，泳道 Y／Z）。 ━━━ ✅ **2026-09-06 (i)-合入 收工（`OP-0906-Y`，泳道 `436-decision-field-merge`）**：`rebase master` 零冲突，`83cd43c` → **`bc2005d`**（已 force-with-lease push origin，未改分支内容、未自行 ff）；登记 CLI 单测 **30 passed** ＋ 邻接六件 **132 passed**，与基线 30／132 一致、零失败；`merge-base --is-ancestor` exit 0、`master...branch` ＝ 0／1，**可快进**，ff 待看护者 `OP-0906-X` 串行执行；另核 tasks 3.2 `git status --porcelain` 0 行、3.3 `openspec validate --strict` 通过。**归档判定＝暂不归档**：`followup-decision-point-gate/tasks.md` 仅 §1／§2 全 [x]，§0／§3／§4／§5 共 15 项未勾，且 §4.2 压着「本闸已在真实起草流程里至少拒绝过一次并留痕」的硬前置，非本泳道可闭合。opener豁免：本次触碰的 `opener骨架.md` 是占位骨架块、且属他线脏文件（非本泳道改动）。登记豁免：`openspec/changes/archive/2026-09-06-editlock-credential-shape-guard/tasks.md` 与 `openspec/specs/editlock-credential-shape-guard/spec.md` 系 acquire 前既脏的他线产物，由其作者线自登。 ✅ **(i)-合入已做（看护者 `OP-0906-X` 串行执行，2026-09-06）**：分支 `claude/op0906r-decision-field-guard` rebase `83cd43c` → `bc2005d`（零冲突、commit 内容未改仅换底，`git push --force-with-lease` 已推 origin，本地＝origin）；master `14fa679` → `bc2005d` **ff 已落**（`git merge --ff-only`，7 文件 +550/−2）。**验证手段**：ff 前看护者在主仓自证 —— `git merge-base --is-ancestor master claude/op0906r-decision-field-guard` exit 0、`git rev-list --left-right --count master...claude/op0906r-decision-field-guard` ＝ 0/1、`git diff --name-only` 得 7 文件与主仓 15 处未提交脏改动零重叠（故 ff 不会撞工作区）；泳道侧 `python -m pytest` 登记 CLI 30 passed、邻接六件 132 passed（均与基线同数）、`git status --porcelain` 0 行、`openspec validate followup-decision-point-gate --strict` ＝ valid。**归档判定 ＝ 暂不归档**：包内 `tasks.md` 尚有 15 项未勾（§0 design 审 5 项属 🟡 档待 Shao Peishen 拍板、§3 合入 3 项、§4 文档 4 项、§5 收尾 3 项）；且 §4.2 压硬前置「本闸已在真实起草流程里至少拒绝过一次并留痕」、§5.3 要求 3 封新信的覆盖率观察窗口 —— 两者均非本泳道可闭合，**ff 完成后也不应立刻归档**。⚠️ 遗留：旧 worktree `.claude/worktrees/agent-a34c5e99547e8e4d6` 仍占该分支且工作区停在 rebase 前内容，待清理（见 `#436` 转出项）。


---

# §一 #436 · 五次外置（2026-09-07，`OP-0907-Q` LAN 收口批 B-0907_Q 项 1 回写时按 K2 口径外置，外置前整格 md5:5a8fa6be）

> 外置 1 段：`OP-0906-Y`／`OP-0906-X` 的 **(i)-合入 收工段**（该事已全闭：分支 rebase→ff 落 master `bc2005d`，无未闭合动作）。原文原样、可检索；行内换为指针。

✅ **2026-09-06 (i)-合入 收工（`OP-0906-Y`，泳道 `436-decision-field-merge`）**：`rebase master` 零冲突，`83cd43c` → **`bc2005d`**（已 force-with-lease push origin，未改分支内容、未自行 ff）；登记 CLI 单测 **30 passed** ＋ 邻接六件 **132 passed**，与基线 30／132 一致、零失败；`merge-base --is-ancestor` exit 0、`master...branch` ＝ 0／1，**可快进**，ff 待看护者 `OP-0906-X` 串行执行；另核 tasks 3.2 `git status --porcelain` 0 行、3.3 `openspec validate --strict` 通过。**归档判定＝暂不归档**：`followup-decision-point-gate/tasks.md` 仅 §1／§2 全 [x]，§0／§3／§4／§5 共 15 项未勾，且 §4.2 压着「本闸已在真实起草流程里至少拒绝过一次并留痕」的硬前置，非本泳道可闭合。opener豁免：本次触碰的 `opener骨架.md` 是占位骨架块、且属他线脏文件（非本泳道改动）。登记豁免：`openspec/changes/archive/2026-09-06-editlock-credential-shape-guard/tasks.md` 与 `openspec/specs/editlock-credential-shape-guard/spec.md` 系 acquire 前既脏的他线产物，由其作者线自登。 ✅ **(i)-合入已做（看护者 `OP-0906-X` 串行执行，2026-09-06）**：分支 `claude/op0906r-decision-field-guard` rebase `83cd43c` → `bc2005d`（零冲突、commit 内容未改仅换底，`git push --force-with-lease` 已推 origin，本地＝origin）；master `14fa679` → `bc2005d` **ff 已落**（`git merge --ff-only`，7 文件 +550/−2）。**验证手段**：ff 前看护者在主仓自证 —— `git merge-base --is-ancestor master claude/op0906r-decision-field-guard` exit 0、`git rev-list --left-right --count master...claude/op0906r-decision-field-guard` ＝ 0/1、`git diff --name-only` 得 7 文件与主仓 15 处未提交脏改动零重叠（故 ff 不会撞工作区）；泳道侧 `python -m pytest` 登记 CLI 30 passed、邻接六件 132 passed（均与基线同数）、`git status --porcelain` 0 行、`openspec validate followup-decision-point-gate --strict` ＝ valid。**归档判定 ＝ 暂不归档**：包内 `tasks.md` 尚有 15 项未勾（§0 design 审 5 项属 🟡 档待 Shao Peishen 拍板、§3 合入 3 项、§4 文档 4 项、§5 收尾 3 项）；且 §4.2 压硬前置「本闸已在真实起草流程里至少拒绝过一次并留痕」、§5.3 要求 3 封新信的覆盖率观察窗口 —— 两者均非本泳道可闭合，**ff 完成后也不应立刻归档**。⚠️ 遗留：旧 worktree `.claude/worktrees/agent-a34c5e99547e8e4d6` 仍占该分支且工作区停在 rebase 前内容，待清理（见 `#436` 转出项）。


---

# §一 #436 · 六次外置（2026-09-08，`OP-0908-B` FI6 需求 grill 收工时按 K2 口径外置，Shao Peishen 当日答 `1a` 指定本会话顺手做；外置前整格 md5:5ef772dfa04440335fda74cb2969838b，9416 B）

> 外置 4 段（均为**已闭**的收工段，行内以 `#436` 末段的「三片均已完成」总结＋本指针承接）：⑴ 2026-09-06 FI10 需求 grill 收工段；⑵ `OP-0907-Q` (iii) 使用度量真实采样收工段；⑶ `OP-0907-U`／`OP-0907-R` ⑵ 检测＋告警半边收工段；⑷ 2026-09-07/08 FI8 需求 grill 收工段。原文原样、可 grep。

✅ **2026-09-06 FI10 需求 grill 收工（Cowork `OP-0906-V`）**：存量 8 包补 grill 首个样本跑通，**开放点 7**，**design 审 ✅ 已过**；明细见 §一 `#474`，批次 `B-0906_V_FI10需求grill_intent与design审`。余 7 包次序 FI8→FI6→FI5→FI9→SC10→SC11→SC4。 ━━━ ✅ **(iii) 使用度量真实采样已跑完（`OP-0907-Q`，LAN 收口批 `B-0907_Q` 项 1，2026-09-07）**：按 Shao Peishen 09-06 答 5(a)，脚本推 `.51` 的 `C:\tmp\usage_sampler\` 就地跑、只取回报告，跑完清场（`Remove-Item -Recurse` 实测 `REMOVED_OK`），全程只读、未改 `.51` 任何服务件。**读入 7 个日志／581,533,683 B／2,641,329 行**：FI2 4 件（`fi2_web_audit` 436 行、`fi2_access_trace` 35,173、`fi2_web_access_trace` 197、`fi2_http_requests` 168）；SC8 3 件（`baoguan_audit` 2,262、`baoguan_access_trace` 2,600,048、`baoguan_http_requests` 3,045）。**三档命中：≥14 天 7 项／7–13 天 5 项／3–6 天 7 项。** 报告＝`1-转型规划/0-全景路线图/使用度量采样报告-FI2SC8-2026-09-07.md`（JSON 落 `reports/`，不入库）。🔴 **实测更正看护件两处**：⑴ SC8 部署路径不是 `C:\sc8`（该目录不存在）而是 `C:\baoguan`（`sync-to-server.ps1` 的 `-ServerBase` 即写死此值；`.51` 8091 端口→PID 3616→`run_baoguan_web.py`）；⑵ `#358` 那份日志已由 309 MB 涨到 **544.9 MB**（`baoguan_access_trace.jsonl`，仍无轮转），报告 §六 只出建议、未实施。⚠️ `.51` 上没有仓库 ⇒ 本次跑 `--no-diagnose`，硬边界 ⑶ 的「只读诊断」由看护者在本机补做，**结论：7/14 两档共 12 项无一为真人**（6 项是部署／冒烟测试留下的假用户如 `CC-D19-smoketest`，6 项是 IP 粒度即按设计不采集个人身份），故无可诊断对象、**不产生任何「需你定夺」的真人候选**；真人只有 FI2 的 `李姣龙`（154 次 AI 决策）与 `财务专员(Web-试用版)`（44 次），均落 3–6 天「只记日志」档。🔑 **首跑暴露的判据缺口（只报不改）**：三档只看闲置天数，冒烟／部署类假用户会原样进 ≥14 天「需你定夺」档——首轮即 5 个误报；建议给采样器加「排除冒烟/测试标识用户」的过滤或标注，属改口径判据、待 Shao Peishen 拍板。**「待回内网」项：(iii) 已闭，本行无剩余留步。** ━━━ ✅ **⑵ 检测＋告警半边收工（`OP-0907-U`，看护批 `B-0907_R`，2026-09-07）**：第 5 类常驻执行体告警的三个停手分支（`ahead`／`dirty`／`failed`）在 `detail` 里补落后幅度，新增只读函数 `_carrier_drift_note`，**判据与动作一个字节没改**。commit `b1ced5a`（分支 `claude/op0907u-worktree-drift-436`，已 push，**未 ff 进 master**——本批 Shao Peishen 答 1a 只放行 `#474`/`#493`/`#466` 三条，本条不在其中）。 🔴 **实测推翻派单前提**：对齐机制不是「没建」而是**被一处纯外观差异卡死 249 轮**——`5-平台底座/wecom-aibot-service/run-followup-dispatch-check.ps1` 工作副本比 committed 多一个 UTF-8 BOM ＋ LF→CRLF（1300 B→1318 B，正文逐字节相同），触发 `_carrier_tracked_dirty` 每轮停手；而待入提交里碰该文件的有 **0** 个 ⇒ **git 自己允许这次 ff，挡住它的是 sweep 自己的脏检查**。ff 自 **2026-08-27 17:17 UTC** 后再没成功过（49 轮 `ahead 1` ＋ 200 轮 `dirty`）。**OP-0907-R 看护者独立复核（亲验非转抄）**：`git rev-list --count ops/wecom-service-home..master`＝**165**、ahead＝**0**、碰该文件的待入提交＝**0**，逐数吻合；BOM/CRLF 按字节比对复现（1318 vs 1300，差 18 B＝3 BOM＋15 CR，规范化后正文完全相同）；单测 master 侧全量 **430 passed/51 subtests**（39:39，退出码 0）、分支侧 `OP-0907-U` 自跑 **434 passed**，**差 4 ＝ 其新增 4 例**，交叉一致；定向 `-k ResidentCarrier` **20 passed＋3 subtests**。 ⏭️ **留步（已 `transfer-out` 登记）**：清 BOM＋ff 常驻执行体（现落后 165）＋重启验活 `ZhuopinAibotDevListener` 单实例存活 ⇒ 交 `zhuopin-lan-closeout`（`B-0907_Q`）。⚠️ **单清 BOM 只解这一次**——判据不改则下次重跑 `register-followup-dispatch-task.ps1` 必复发。 ✅ **🟡 待拍板项已闭**：脏检查是否放宽为「与待入提交路径相交才停手」——**Shao Peishen 2026-09-07 答 3(a) 放行**，已另立 §一 行承接（本行不做工具改动）。 📌 **顺带上报（非本件所致）**：`_carrier_lag_counts`（`工具-落库sweep.py`，`#338` 遗留）**从未被任何地方调用**，是当年阈值方案作废后的死代码，未动，供后续清理判断。 📌 看护者另见：常驻执行体 worktree 内另有未跟踪文件 `run-annual-holiday-reminder-check.ps1`（不影响 `_carrier_tracked_dirty`，该检查只看已跟踪文件）。  📌 **承接行状态更正**：上句「已另立 §一 行承接」＝ §一 `#500`，但该行立行当时机制类 WIP 24／22 无余量，**已挂 🛑 暂挂**（决策不受影响，口径已定，解冻＝WIP 回到 22 以内后去掉 🛑 即可开工，无须再问 Shao Peishen）。  ━━━ ✅ **2026-09-07/08 FI8 需求 grill 收工（Cowork `OP-0907-M`）**：存量 8 包补 grill **第二个样本**跑通，三轮 17 问、前沿为空，**开放点 8**（严格口径 7 ＝ 3 既有 ＋ 4 新发现；含素材 8），**design 审 ✅ 已过**（Shao Peishen 2026-09-08 答 `1a`，同批确认 `intent.md` `status: 已确认`）。明细见 §一 `#472`，批次 `B-0907_FI8grill_intent与design审`。⚠️ **开放点 8 相对 FI10 的 7 不降反升** —— §六 可证伪判据的两种读法与「`FI8-G-02` 周分桶留作 FI6／FI5／FI9 三包对照观测项」写在 `intent.md` §三末节，本次不下结论。余 6 包次序 FI6→FI5→FI9→SC10→SC11→SC4。
