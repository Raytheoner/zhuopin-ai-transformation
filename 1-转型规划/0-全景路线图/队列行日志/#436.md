---
title: "队列行日志 #436（§一）"
created: 2026-09-06
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #436 的行外承载件——⑶ 第二波蓝图三向的 CC 四件任务书全文（行内 ≤4 KB 口径，行内只留指针）。泳道按 (i)～(iv) 对号读，做什么／不做什么／收工／触碰区以本节为准。
---

# §一 #436 ·「2026-09-06 派出」四件任务书

> 来源：`1-转型规划/0-全景路线图/端到端构建workflow优化-方案-2026-09-06.md` §四 W1／W3／W5／W7 ＋ §六 决议 ②③④（Shao Peishen 2026-09-06）。看护件＝`看护件-泳道看护批B-0906_P-需求侧五泳道-2026-09-06.md`。
> 🔴 通用收工三条：① 回写 `#436` 行状态列**只追加一段**（本件编号＋结论＋commit 哈希）；② 登记 §二 待 commit 批次（路径反引号包裹、只列本批脏改动）；③ 判是否命中 openspec 三门槛（改全项目口径／涉鉴权可见性／改既有模块对外语义），命中即止步 design 起草并 `pause --action-key openspec_design_review --no-notify`。

## (i) 登记 CLI `决策点:` 校验 —— 泳道 `436-decision-field-guard`（OP-0906-R）

做什么：
1. `0-学习与工具/工具-跟进信README登记.py append` 在登记前读信件 `.md` frontmatter：`决策点:` **缺失或为空即拒登记**（exit 非 0），拒绝文案给出路——「按 skill `zhuopin-followup-letter` v3.8 §5 步骤 1bis 写 `决策点: N 项（a / b / c）`；B 类通报信写 `0 项（…无需其决策）`」。
2. 值形态弱校验：须以数字＋「项」开头（`0 项` 放行）；不校验括号内容。
3. 单测三例：缺字段拒／`0 项（…）` 放行／`3 项（a / b / c）` 放行；跑既有 `test_工具-跟进信README登记.py`（若存在）零回归。
4. 口径来源写进 docstring 指针：`design审前置-口径点台账三开放点收敛-2026-09-01.md` §2.3（P1–P4）。

不做什么：不改 README 主表；不动 `set-status`；不回溯已发信；不改 skill 源码（Cowork 已改）。

## (ii) 新场景 `intent.md` 前置闸 —— 泳道 `436-intent-gate`（OP-0906-S）

做什么：
1. 新 lint `0-学习与工具/工具-场景包intent闸lint.py`：扫 `openspec/changes/<pkg>/`（非 archive），凡 `proposal.md`／`tasks.md` 触碰 `4-数字员工/<部门>/<场景>/` 的**场景类**包，要求该场景目录存在 `intent.md` 且 frontmatter `status: 已确认`；缺则失败并点名包名＋期望路径。机制类包（不触碰 `4-数字员工/`）不适用；深化类（场景目录已有「部署状态」段的场景）不适用。
2. 🔴 存量豁免写死在 lint 里并带到期日 **2026-10-31**：`sc4-contract-clause-extraction／sc10-bom-review-material-governance／sc11-inventory-transfer／fi5-expense-audit-mvp／fi6-anomaly-detect-mvp／fi8-cashflow-forecast-mvp／fi9-rd-cost-mvp／fi10-inventory-writedown-mvp` 与 SC2（历史件 `需求grill产出-2026-08-18.md`）；到期后一律硬拦；每包补完 grill（`intent.md` 已确认）即从名单删。
3. 接 `.github/workflows/ci.yml` 新 job（形态同 `bootstrap-stub-lint`）；`openspec/config.yaml` `rules.proposal` 加一条 MANDATORY 提示句（两桌起草时可见）——**提示句不是闸，lint 才是**。
4. 单测：有 intent 且已确认／有 intent 未确认／无 intent／豁免名单内／到期后豁免失效 五例。

不做什么：不改 `/opsx:propose` 命令本身；不给任何包写 `intent.md`（那是 Cowork grill 会话的产出）；不动 archive。

## (iii) 使用度量只读采样脚本 —— 泳道 `436-usage-sampler`（OP-0906-T）

做什么：
1. 新 `0-学习与工具/工具-使用度量采样.py`：输入若干日志路径（平台 `audit` JSONL ＋ 场景 web 访问日志），输出 Markdown 采样报告：**场景×用户×日**的访问次数、AI 决策次数、**零触发功能清单**（按路由／动作枚举）、日志缺口（无法识别用户／无时间戳／格式不一）。
2. 三档判定**全部确定性、不涉模型**：连续 **3** 天零使用→报告内「记日志」行；**7** 天→「只读诊断」行（列出该用户最近一次企微回件／文件落档日期，取自 `7-外部文档/` 文件 mtime 与 README 主表，只读）；**14** 天→「需你定夺」候选行。🔴 **不写 README、不推企微、不自动起草任何信**。
3. mock 先行：`tests/` 内构造 FI2（`8094` audit 形态）与 SC8（`8091` 连接器审计形态）两种 mock 日志，各一例正常／一例 14 天零使用；真实 `.51` 日志**本批不读**，实跑列「待回内网」交 `zhuopin-lan-closeout`（脚本用法一行写进收工回写）。
4. 顺带核 `#358`：脚本对 309 MB 级日志用流式读取；报告末附「轮转建议」一段（只建议、不实施，`#358` 须先走 design 审）。

不做什么：不触碰 `.51`；不改任何场景代码；不新增定时任务（job 化待采样报告过目后另议）。

## (iv) ~~「发布后该起草跟进信而没起草」扫描~~ —— 🔴 撤回（2026-09-06 17:40 自查）：机制已存在

`5-平台底座/wecom-aibot-service/scripts/draft_gap_check.py`（队列 `#245`：近 N 天已部署场景 vs README 已起草信行交叉比对）已并入 `工具-落库sweep.py` 第 11 类常驻告警（`#382`⑵，每轮回显「📝 跟进信起草缺口检测」，零命中不省略；`reports/sweep-commit.log` 2026-09-06 09:17 UTC 一轮实测回显「无缺口」）。本件与之重复，**不派**；`OP-0906-U` 作废。成因＝提方案前未查仓库已有能力（08-30 评估件 R3 同族），已计 `#284`。方案件 §四 W7 同步更正。以下原文保留供追溯：

～～原做什么：
1. `0-学习与工具/工具-落库sweep.py` 告警类新增一项：遍历 `4-数字员工/*/*/CLAUDE.md`，取「部署状态」段最近日期（段内 `YYYY-MM-DD` 最大值）；按场景码（FI2／SC8／QD-B…）在跟进信 README 主表匹配该场景最近一封信的日期（用 `工具-跟进信README查询.py --digest --json`，不 Read 主表）；**部署日期晚于最近信 >3 天 ⇒ 告警**（企微逃生通道口径同现有第 4 类告警）＋ 输出一行「§四 登记候选」文案，**只报不起草、不写 README、不写队列**。
2. 单测两例：该报（部署 09-01、最近信 08-20）／不该报（部署 09-01、最近信 09-02；或无部署段）。
3. 场景码↔README 匹配规则写进 docstring；匹配不到的场景归「无法判定」单列，不算告警。

不做什么：不改 sweep 其余告警；不动跟进信 README；不改场景 CLAUDE.md。承 `#124` 阶段二第三条前置（2026-08-03 挂起）。
