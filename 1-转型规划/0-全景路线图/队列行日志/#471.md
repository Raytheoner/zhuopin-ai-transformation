---
title: "队列行日志 #471（§一）"
created: 2026-09-04
status: 生效
用途: 🔴 跨桌任务队列-**业务场景**.md §一 #471 的状态列历史回写段外置件（2026-09-08 `OP-0908-B` 更正：原写「机制环境」是笔误——`#471` 是业务场景队列的行，`#436` 才在机制环境；frontmatter `用途` 是当下生效的指针，指错会把人带到另一份队列去找行，故按 Shao Peishen 2026-09-08 答 `2a` 更正）（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #471 · 外置前整格原文（2026-09-04 外置，13 段）

[S:open][D:业] 🆕 **2026-09-03 立行，未开工。** **立行依据**＝队列 §一 `#463`（14 个「三处皆无」场景的立行与排期）＋ Shao Peishen 2026-09-02 判据原话「**不管规划时间表、能开尽开；唯一门槛是前置依赖**」（判据源＝《跨场景前置数据与知识库任务总表》），及其当日当场收窄指令「**先做质量财务采购域场景，研发和销售域场景依然暂缓等我通知再排序**」。 ━━━ 📅 **权威排期表上的原排期＝2027-04**（§加速启动总览财务列「FI6 异常交易其余 启动」）。 ━━━ 🔴 **前置依赖及满足情况＝判据源本身缺行，无从判「已满足」**：本次逐行实测，《跨场景前置数据与知识库任务总表》§一 与 §一.2 **均无 `FI6` 的对应条目**（该表财务域现存行只有 `FI1`／`FI2`／`FI3` 三条）⇒ 按他指定的判据源，`FI6` 的前置**既非「已满足」也非「未满足」，而是从未被排过期**。**⚠️ 这是判据源的缺口、不是本行的结论**——补前置行属规划文档改动（全景路线图线），本行不擅改、不代判，已一并写进 `#463` 回写与本批报告的待定夺项。 ━━━ 📌 **改从场景块自陈的数据依赖读**：U9C 交易流水 ／ 供应商·客户主数据 ／ 历史异常案例库 ／ **（联动）`FI3` 付款校验结果**。⇒ **本场景存在一条场景间软依赖**：`FI3` 排 2026-11 启动，早于 FI6 的 2027-04，**顺序上不构成倒置**；但「历史异常案例库」在本项目内**无既有载体**，属须从零建的知识型资产（且判据/阈值类须财务侧显式签认、永不默认生效）。 ━━━ ✅ **照样立行**：可开工部分＝异常检测口径与三类模式（金额突增／频率异常／关联方）的 mock 建模。  ━━━ 🔨 **2026-09-03 开工（CC `OP-0903-B3`，批次 `B-0903_20`，泳道 `fin-scenarios`，commit `47d76f2`）：骨架期完成、判据部分留步，本行不销号。** ✅ **已交付**：工程骨架 `4-数字员工/财务部/FI6-异常交易实时检测/`（`pyproject.toml` ＋ `fi6_anomaly_detect/{__init__,config,models}.py` ＋ `tests/{conftest,test_scaffold}.py` ＋ `data/mock/` 三张合成 CSV），**10 tests 全绿**；openspec 变更包 `fi6-anomaly-detect-mvp`（proposal ＋ 3 份 specs delta ＋ tasks，`openspec validate` 通过）。🔴 **未写 `design.md`、未进 apply**（🟡 `openspec_design_review`，须 Shao Peishen 拍板）。 ━━━ 🔴 **判据留步（四项，全部落 `None` ＋ CI 用例守）**：金额突增口径／频率异常口径／关联方识别口径／升级推送门限。⇒ **本包不宣称档 1**：`#471` 所说「可开工部分＝三类模式的 mock 建模」，而三类模式的**内容就是那三项未签认判据**。**mock 夹具亦刻意不打「这笔是异常」的标签**——那些标签会顺理成章变成回归基准，把判据缺口盖在绿灯下面。 ━━━ 🔴 **本泳道实测出两处「本项目内不存在」，已落成代码常量并由用例守，防后来者假设它们存在**：**⑴ `FI3` 目前没有工程实体** —— `4-数字员工/财务部/` 下**只有 `FI1` 与 `FI2`**（2026-09-03 逐目录实测）。本行所述「联动 `FI3` 付款校验结果」在建造时**无对象可接**；`FI3` 排 2026-11、早于本场景 2027-04，**顺序上确实不倒置**（与本行原判一致），但依赖须等其落地 ⇒ 落成 `config.FI3_NO_ENTITY`。**⑵「历史异常案例库」无既有载体** —— 复核确认本行原判成立 ⇒ 落成 `config.CASE_LIBRARY_ABSENT`，并明写一条纪律：**无案例库时检测器 MUST NOT 以「无历史即正常」放行**（那是把空数据当阴性结论，与静默回退同族）。 ━━━ 🔴 **另做一处防御，写明以免被当成漏项补上**：`PartyProfile` **刻意不含任何「是否关联方」布尔字段**，并有一条用例守住这个「没有」。关联口径（股权？亲属？同址？主数据比对？）在本项目内**连定义都没有**；留一个 `is_related` 字段会诱使实现方先填上再说——**判据就是这样被默默造出来的**。 另：`CaseRecord.confirmed_by` 无默认值（必填实名）——判例的价值全在「谁认的」，匿名判例不能当回归基准。 ━━━ ⏸️ **卡在哪**：四项判据须财务侧实名签认（⚠️ 需唐燕萍确认者 ⇒ 唐燕萍串行闸在途 —— 🔴 **状态勿在此复述**（会过时），现取：`python 0-学习与工具/工具-跟进闸查询.py --to 唐燕萍`，已登记「待闸开后并进下一封」，**本泳道未起草任何新信**）；**关联方定义建议单独走一次专家工作坊**（它是定义题不是经验题，混在判据表里问会得到一句「就是有关系的那种」）；「实时」的实时程度须与 IT 一并定（**批量导出不等于满足「实时监控」**）。 ━━━ 📌 跨五场景 design 停审点同 `#470` 末段（`criteria_signoff` 是否提升进平台底座，rule-of-three 已触发）。 ━━━ ⏸ **2026-09-03 A4 段（CC `OP-0903-C4`，批次 `B-0903_36`，泳道 `fin-criteria`）开工即留步，本行零改动、不销号。** A4 原定动作＝把本包的**本地判据签认实现**改为引用平台底座公共模块 `criteria_signoff`（A3 段已建成，见批次 `B-0903_31`／分支 `claude/op0903c3-criteria-signoff`）。🔴 **实际未做**：开工前置检查 `工具-泳道看护状态机.py show --lane fin-criteria` ⇒ 泳道仍 **`paused`**，停在「③ openspec design 审」，等 `criteria-signoff-platform` 的五条定夺项（①占位词黑名单去留 ②三类「1 次」缺口收不收进底座＋②′ `FI10` `SLOW_MOVING_CRITERIA` 归属 ③L2 模型层纪律是否另立一件 ④是否接 `AuditLogger` ⑤**A4 段是否与本包同批批准**）。**第 ⑤ 条即本次动作本身** —— 未拍板前动手改包等于替 Shao Peishen 把 A4 批了；且 ②／②′／④ 任一改判，五包的引用面都要重做（A3 design 原文：只批本包不批 A4 即成「底座＋五份手抄并存」，比现状更差，若不批 A4 请整包否决）。**故：未跑 `/opsx:sync`、未改一行实现、未起草任何 design、未 commit／push、未部署、未触碰 `.51`、未连真实库；🔴 判据落 `None` ＋ CI 守的行为一处未动。** 本行状态相对上一次回写**无实质推进**，`[S:]` 不翻、不销号。指针：`reports/lane-heartbeat/B-0903_30-fin-criteria.md`（§「A4 段」，含未做清单与三项连带裁决原文转录）。  ━━━ ✅ **2026-09-03 A4 段已做完（CC `OP-0903-D3`，批次 `B-0903_53`，看护批 `B-0903_50` 泳道 `criteria-chain` A3 段）** —— 上一条留步的 ⑤「A4 段是否与本包同批批准」已由 Shao Peishen 当日答 `G-6 = (a)` 批准，五条定夺项全部裁完，依赖解除。本段实做三件：**⑴ 跑 `/opsx:sync`**（🔴 **不是 `apply`**——本仓库 `/opsx:apply` ＝ 实现 tasks 待办，能写 `openspec/specs/` 的是 `sync`）：本包 delta specs 已并入 `openspec/specs/`，新建主 spec 各带一段真 Purpose（原 delta 缺 `## Purpose`，一并补齐，否则 archive 会留 TBD 占位）。**⑵ 判据签认改为引用平台底座** `zhuopin_platform.criteria_signoff`：`config.py` 的裸 `None` 常量收为 `CriteriaRegistry` 声明，模块级调 `assert_rule_version()`（**导入期**即双向校验，比原 `test_rule_version_marked_unsigned` 更早）。🔴 **行为一处未变**：未签认 ⇒ 值恒 `None`、读取即抛 `CriterionNotSignedOffError`、无 `default` 旁路；断言对象由「常量是不是 `None`」变为「注册表这条签认了没有」——后者更硬（`None` 只是碰巧没值，未签认是一个**读了就炸**的状态）。**⑶ `G-5` 反向依赖**：新增 `config.audit_decision(**fields)`（四行，恒带 `RULE_VERSION`）＋ 一条用例拿真 `AuditEvent` 断言 `decision["rule_version"]`；方向是**审计日志指向判据版本**，不是判据模块去写日志。 本包 **4** 条判据全部迁入注册表（`AMOUNT_SURGE_CRITERIA`／`FREQUENCY_ANOMALY_CRITERIA`／`RELATED_PARTY_CRITERIA`／`L2_ESCALATION_CRITERIA`），迁移后仍全部未签认；新建 3 份主 spec（`fi6-case-library`／`fi6-escalation-and-report`／`fi6-pattern-detectors`）；本包单测 10 → **12 passed**。📌 `PartyProfile` 仍无任何「是否关联方」字段（关联口径未签认，用例照守）。 **闸**：`openspec validate --all --strict` **155 passed / 0 failed**；五场景单测 **71 passed**（迁移前基线 53）；底座全量回归 **466 passed / 1 skipped**（零回归）。**验收硬条件两条均实测通过**：迁移前后未签认判据数 **18 ＝ 18**；五个包**零**残留本地判据 `None` 常量（`ast` 扫模块级 `X = None`，命中仅 `G-2` 三类缺口）。已合入并 push master `a30217a`（分支 `claude/op0903d3-fin-apply` 保留备追溯，worktree 已自删）。🔴 **`[S:open]` 不翻**：本行是**场景立行**行，A4 只是其中一步——design 审仍未过，`tasks` §2 起未开工，引擎一行未写。🔴 **本段未填任何判据值、未给任何默认值**；未进 `/opsx:apply`、未连真实库、未部署、未触碰 `.51`、未发任何信、未改任何规划文档。指针：`reports/lane-heartbeat/B-0903_50-criteria-chain.md`（§「A3 段」）。


---

# §一 #471 · 二次外置（2026-09-08，`OP-0908-B` FI6 需求 grill 收工时按 K2 口径外置，Shao Peishen 当日答 `1a` 指定本会话顺手做；外置前整格 md5:6ce2d24de189289278f97cd48de4d15a，6615 B）

> 外置 1 段：**2026-09-03 A4 段收工段**（`OP-0903-D3`，批次 `B-0903_53` —— 判据签认迁移平台底座，该事已全闭）。原文原样、可 grep。

✅ **2026-09-03 A4 段已做完（CC `OP-0903-D3`，批次 `B-0903_53`，看护批 `B-0903_50` 泳道 `criteria-chain` A3 段）** —— 上一条留步的 ⑤「A4 段是否与本包同批批准」已由 Shao Peishen 当日答 `G-6 = (a)` 批准，五条定夺项全部裁完，依赖解除。本段实做三件：**⑴ 跑 `/opsx:sync`**（🔴 **不是 `apply`**——本仓库 `/opsx:apply` ＝ 实现 tasks 待办，能写 `openspec/specs/` 的是 `sync`）：本包 delta specs 已并入 `openspec/specs/`，新建主 spec 各带一段真 Purpose（原 delta 缺 `## Purpose`，一并补齐，否则 archive 会留 TBD 占位）。**⑵ 判据签认改为引用平台底座** `zhuopin_platform.criteria_signoff`：`config.py` 的裸 `None` 常量收为 `CriteriaRegistry` 声明，模块级调 `assert_rule_version()`（**导入期**即双向校验，比原 `test_rule_version_marked_unsigned` 更早）。🔴 **行为一处未变**：未签认 ⇒ 值恒 `None`、读取即抛 `CriterionNotSignedOffError`、无 `default` 旁路；断言对象由「常量是不是 `None`」变为「注册表这条签认了没有」——后者更硬（`None` 只是碰巧没值，未签认是一个**读了就炸**的状态）。**⑶ `G-5` 反向依赖**：新增 `config.audit_decision(**fields)`（四行，恒带 `RULE_VERSION`）＋ 一条用例拿真 `AuditEvent` 断言 `decision["rule_version"]`；方向是**审计日志指向判据版本**，不是判据模块去写日志。 本包 **4** 条判据全部迁入注册表（`AMOUNT_SURGE_CRITERIA`／`FREQUENCY_ANOMALY_CRITERIA`／`RELATED_PARTY_CRITERIA`／`L2_ESCALATION_CRITERIA`），迁移后仍全部未签认；新建 3 份主 spec（`fi6-case-library`／`fi6-escalation-and-report`／`fi6-pattern-detectors`）；本包单测 10 → **12 passed**。📌 `PartyProfile` 仍无任何「是否关联方」字段（关联口径未签认，用例照守）。 **闸**：`openspec validate --all --strict` **155 passed / 0 failed**；五场景单测 **71 passed**（迁移前基线 53）；底座全量回归 **466 passed / 1 skipped**（零回归）。**验收硬条件两条均实测通过**：迁移前后未签认判据数 **18 ＝ 18**；五个包**零**残留本地判据 `None` 常量（`ast` 扫模块级 `X = None`，命中仅 `G-2` 三类缺口）。已合入并 push master `a30217a`（分支 `claude/op0903d3-fin-apply` 保留备追溯，worktree 已自删）。🔴 **`[S:open]` 不翻**：本行是**场景立行**行，A4 只是其中一步——design 审仍未过，`tasks` §2 起未开工，引擎一行未写。🔴 **本段未填任何判据值、未给任何默认值**；未进 `/opsx:apply`、未连真实库、未部署、未触碰 `.51`、未发任何信、未改任何规划文档。指针：`reports/lane-heartbeat/B-0903_50-criteria-chain.md`（§「A3 段」）。

---

# §一 #471 · FI6 需求 grill 收工全文（2026-09-08，`OP-0908-B` 行外承载件）

> 行内只留结论段＋本指针（K2 口径：单格 ≤4 KB）。判例包待办八点与三项待派发的完整表述在此，权威正本仍是 `4-数字员工/财务部/FI6-异常交易实时检测/intent.md` §三／§四 与 `openspec/changes/fi6-anomaly-detect-mvp/design.md` 的 Open Questions。

✅ **2026-09-08 FI6 需求 grill 收工（Cowork `OP-0908-B`）**：存量 8 包补 grill **第三个样本**跑通，三轮 18 问、前沿为空、**全部按推荐当场答定**，**开放点 8**（严格口径 7 ＝ 4 既有注册表点 ＋ 3 新发现；素材 1 另计，且已并进 `财务部#18` 不新增往返），**design 审 ✅ 已过**（Shao Peishen 2026-09-08 答 `1a`，同批答 `2a` 认可 `D19`；同批确认 `intent.md` `status: 已确认`）。产出两件：`4-数字员工/财务部/FI6-异常交易实时检测/intent.md`（新建）＋ `openspec/changes/fi6-anomaly-detect-mvp/design.md`（**该文件本不存在，本次新建**）。**闸**：`npx openspec validate fi6-anomaly-detect-mvp --strict` ⇒ `Change 'fi6-anomaly-detect-mvp' is valid`（RC=0）；`工具-写后反查.ps1` 两件均 RC=0。🔴 **`[S:open]` 不翻**：design 审过 ≠ 开工——`tasks §2` 起仍未开工、引擎一行未写；本会话**未改 `config.py` 一个字节**（四条判据仍 `None` 占位、未签认）、未 `/opsx:apply`、未连真实库、未部署、未触碰 `.51`、**未发任何信**（唐燕萍闸本会话实测 🔒 锁，`财务部#17` 在途）、未改任何规划文档、**未改排期**（FI6 仍 2027-04）。 📌 **判例包待办（intent §三，收信人唐燕萍，待 `财务部#18`）**：四条注册表点 `FI6-AMOUNT_SURGE_CRITERIA`／`FI6-FREQUENCY_ANOMALY_CRITERIA`／`FI6-RELATED_PARTY_CRITERIA`／`FI6-L2_ESCALATION_CRITERIA`（🔴 问法按 `D18` 重写成「四列度量并排 ＋ ≤10 笔真实交易三选一」，**不得照 `config.py` 原文的抽象设问发**）＋ 三条 grill 新发现 `FI6-G-01` 分桶基准（`txn_date` 与 `period` 不一致时以哪个为准）／`FI6-G-02` 历史基线取样口径（现写死 12 个月、全库无出处；🔴 被两条判据共用故独立成点，不并入金额突增）／`FI6-G-03`「财务主管」是哪个岗位（名录正本无此岗位，**禁推断**，她答不了则回 Shao Peishen 一次）＋ 素材 `FI6-G-04`（近 12–24 个月应收与应付交易流水 ＋ 往来单位主数据；🔴 按 `D14` **并进 FI8 那封 `财务部#18`**，不另起一封）。🔴 **次序**：`FI6-G-04` 回件后才能出 FI6 判例批改表（四列度量要跑在真实数据上）⇒ 比 FI8 多一轮闸。 ⏭️ **三项已登记待派发**（不属本行收口）：⑴ U9C 交易流水**实时**通道核实——并进 FI8 §四 1 已登记未派发的那条（FI10 库存/PLM ＋ FI8 应收应付计划/在手订单），并加独立子问「**是否支持实时推送／订阅，还是只能定时拉**」，同一判词**第八次适用**；⑵ 前置总表补 `FI6` 行（移交单 `status: 待执行`，其 `:183` 启动月 2027-04、`:187` 知识型条目已备好）；⑶ `FI6→FI8` 链上箭头**下游无对手方确认**（四链蓝图 `:87` 是全库唯一表述、FI8 `intent.md` 未列 FI6 为输入、42 场景全覆盖校验清单里 FI6 无归属），只报不改。 余 5 包次序 FI5→FI9→SC10→SC11→SC4。
