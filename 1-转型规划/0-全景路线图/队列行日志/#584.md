---
title: "队列行日志 #584（§一）"
created: 2026-09-16
status: 生效
用途: 跨桌任务队列 §一 #584 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 手工（`工具-队列行K2外置.py` 因首段含 `[S:][D:]` 机器字段拒绝整段外置，改为行内保留短摘要＋指针，本件存原文）
---

# §一 #584 · 外置段（2026-09-16 外置，`OP-0916-I`，1 段，原文原样）

> 外置人 `OP-0916-I`；外置前整格 5184 B、共 2 段；本次外置第 1 段（OP-0916-H 初版取证与派单，其根因判定已被 OP-0916-T 反证推翻）；行内保留短摘要指针到本件 ＋ 第 2 段全文（OP-0916-T 反证＋OP-0916-I 续棒，当前有效口径）。

[S:open][D:机] **WIP豁免：出血**（开场横幅从未送达无头泳道＝机器守失效；泳道产出当日丢两次）。2026-09-16 `OP-0916-T` 立行（Shao Peishen 拍 2a：与 `#583` 第二段并行）。🟡 合入 master 须他一字母。  ｜ 2026-09-16 OP-0916-H 取证（claude --debug-file，同一命令独立重跑 2 次于本泳道 worktree，结果 2/2 一致；命令：`echo 只回复ok不要调用任何工具 管道进 claude -p --output-format text --model haiku --debug-file <path>`）：⑴ 根因不是「-p 单独致 SessionStart 不触发」，而是「同一 hook 事件名在全局 ~/.claude/settings.json 与项目 .claude/settings.json 都注册时，项目侧该事件的整条 hooks 数组被丢，全局侧独占」——SessionStart 恰好两边都注册（项目＝hooks-sessionstart-context.ps1，全局＝health-check-staleness.ps1）：2 次重跑里项目侧那一条一次都没被 spawn（debug log 全文 grep hooks-sessionstart-context 命中 0 次；同一日志里全局 health-check-staleness.ps1 与三个插件的 SessionStart hook 均正常 provided additionalContext，无任何 timeout/kill 记录——是「从未尝试 spawn」不是「spawn 后失败」）。UserPromptSubmit 只在项目侧注册（全局无此键，无冲突），同一次跑里正常触发（hooks-userpromptsubmit-standing-five.ps1 命中）。⑵ 旁证：本行执行棒 OP-0916-H 自身也是 -p 无头起的会话，开场同样没收到 hooks-sessionstart-context.ps1 的横幅（对照本会话最早的 system-reminder，只有 last30days/last30days-cn/superpowers/健康体检四条，没有 fsck/ahead-behind/队列摘要那条）——与两次独立复现吻合，3/3。取证件（未入库，含本机路径与会话细节，留 reports/）：reports/op0916h-headless-parity-584/t1-stdin-piped.debug.log、t2-repeat.debug.log。⑶ 本棒因上下文转场（≈158k）只做完⑴取证，⑵⑶⑷未动手，接手指路：⑵按因修——鉴于根因是引擎级「事件名冲突丢整条数组」而非 -p 专属、且引擎闭源不可控，落回行内已授权的 fallback：在 工具-opener批处理执行v2.ps1 的 $header 前置注入阶段（该文件第 182-190 行，当前是纯字面量数组）直接调一次 hooks-sessionstart-context.ps1（复用同一份逻辑、不复制）拼进 header，让无头泳道旁路掉这个引擎级丢弃；🔴 PreToolUse/PostToolUse/Stop 三个事件名同一份 settings.json 对照也都两边各自注册了，同一机制很可能也在丢，但本棒未验证，下一棒先按同法（两次 --debug-file 重跑＋grep 项目侧命令串）测一遍再动手，别假设只有 SessionStart 受影响。⑶泳道产出不丢、⑷插件加载探针：均未动手，仍按行内原方案执行。状态维持 open，判据不改。

---

# §一 #584 · 外置段（2026-09-16 外置，工具第 2 批，5 段，原文原样）

> 外置人 Cowork-OP0916-T；外置前整格 6761 B、共 6 段；本批外置第 2、3、4、5、6 段；本格保留第 1 段。

🔴 **上一棒根因被反证推翻（09-16 `OP-0916-T`）**：「全局与项目同名事件时项目侧被丢」不成立——全局同样注册 PreToolUse／PostToolUse／Stop，项目侧当日照常触发（hooks-audit：queue-read-guard 630、stop 27、context-meter 197）；项目 SessionStart 当日触发 5 次（含无头 `9d5dabfe`）⇒ 时有时无。**新线索（待取证）**：hook 内 `git fsck` 耗时 vs timeout 10s；全局 SessionStart 并发时序。**续派**：⑴' 计时＋`--debug-file` 对照定因（须同时解释触发与未触发）；⑵' 按因修（优先 fsck 移出／缓存或提 timeout，保横幅必达）；⑶⑷ 按原期望产出。 ｜ 2026-09-16 `OP-0916-I` 续棒（⑴'⑵'⑶a 完成）：取证＝对 `hooks-sessionstart-context.ps1` 单跑 3 次 stdin 直喂（主仓 8.6/9.3/8.7s，worktree 7.6/9.4/9.0s）＋ `git fsck --connectivity-only` 单独耗时（主仓 5.0/5.8/5.5s，worktree 5.5/6.1/6.1s）＋一次裸冷启动实测跑到 10.8s——证实高负载下真会越过 10s 超时，与「时有时无」吻合；同批 3 次 `claude -p --model haiku --debug-file` 对照（worktree）显示本次 3/3 均成功送达 772 字符横幅，说明这是边界概率问题、不是恒定故障，与 `OP-0916-H` 的 0/2 及本轮的 3/3 都不矛盾（不同时刻的负载不同）。按因修＝给 fsck 结果加文件缓存（`reports/hooks-cache/fsck-connectivity.json`，TTL 300s，`ZHUOPIN_FSCK_CACHE_TTL_SEC` 可覆盖），命中缓存后钩子总耗时从 ~9s 降到 ~3s；单测 4 例（写缓存／窗口内复用／过期重测／缓存损坏 fail-open），`test_hooks-p3.py` 49/49 全绿；commit `af2997f`。⑶a＝`opener骨架.md` 无头泳道公共段补第 3 条「reports/ 产出写主工作区」约束，commit `004e52a`。分支 `claude/op0916i-headless-parity-584b` 已 push，已登 `合入登记/pending-ff.jsonl`（未 ff，待授权）。**未完成、续派**：⑶b（`工具-opener批处理执行v2.ps1` 收工阶段／worktree 删除前扫描回收残留 `reports/` 新文件到 `reports/_from-worktree/<泳道>/`＋单测）与 ⑷（`工具-Claude包版本探针.ps1` 扩展 `claude plugin list` 探针、接入既有探针消费链路＋桩单测）均未动手，按原期望产出 ⑶⑷ 执行。上下文越 150k 转场收尾。

━━━

🔧 **续派二（09-16 `OP-0916-T`，他拍 4a，同分支续做后整体合入）**：⑵''' 缓存未命中（空闲 >5 分钟首个会话）仍现跑 fsck≈9s 逼近超时 ⇒ `.claude/settings.json` 中 `hooks-sessionstart-context.ps1` 的 timeout 10→30，并实测冷启动一次送达；⑶b、⑷ 按原期望产出做完。触碰区增 `.claude/settings.json`（仅该 timeout 字段）。 2026-09-16 `OP-0916-J` 续派二实测：⑵''' timeout 10→30 改动被全局机器守 `pretooluse-guard.ps1`（`protected-paths.json` 模式 `*/.claude/settings.json`）拦截——Edit／Bash 两条路径均命中 exit 2，本方未做任何绕过尝试，该项须 Shao Peishen 走人工变更流程改 `.claude/settings.json`。⑶b／⑷ 已读毕 `工具-opener批处理执行v2.ps1` 与 `opener骨架.md` §三bis，定好实现方案（laneBlock 每泳道收工前扫描 `.claude/worktrees/<laneName>/reports/` 回收进 `reports/_from-worktree/<laneName>/`，单测思路＝预置桩文件断言回收落点）但未写代码——上下文越 150k 转场收尾，worktree `op0916j-headless-parity-584c` 无任何 commit（干净，未破坏既有分支状态）。**续派**：新棒直接续 ⑶b／⑷ 编码＋单测；⑵''' 待他人工改 settings.json 后再补冷启动实测。 行长豁免：本棒回写已超4096B上限，K2迁移（搬历史段入#584.md）交下一棒处理，理由——本方上下文已越150k转场线，避免此刻做行内大手术风险（OP-0916-J，到期09-18）

━━━

✅ **⑵''' 已由 Cowork 完成并合入**（09-16 `fe41d3d`，他拍 2a）：SessionStart timeout 10→30（泳道改 `.claude/settings.json` 被全局受保护路径守卫拦截，故由 Cowork 做）。同提交根治大文件整读（他「这个问题需要根治」）：读侧守卫对 >24KB 文本文件未带 offset/limit 的 Read、未截断的 cat/Get-Content 一律拒绝并指路分段读，实测无头会话整读 opener骨架.md 被拒；150k 改软提醒（须先 commit 增量）、250k 硬线。**续三只剩 ⑶b、⑷**，不得再碰 settings.json。

━━━

2026-09-16 `OP-0916-L` 续三实测：worktree `op0916l-headless-parity-584d` 检出既有分支后 `git rebase master`（master 含 `fe41d3d`）在 `opener骨架.md`（读侧大文件守卫 150k/250k 段附近）出现冲突——本分支 `004e52a` 与 master `fe41d3d` 各自新增了相邻但不同的段落。按开场纪律「冲突即 PARTIAL 停」未做任何冲突消解尝试，已 `git rebase --abort` 恢复到冲突前的 `004e52a` 干净态（worktree 内 `git status` 无残留）。⑶b／⑷ 均未动手。**续派**：下一棒先人工/复核 `opener骨架.md` 该处两段应如何合并（是否为同一约束的重复表述），再重跑 rebase，之后再做 ⑶b（`工具-opener批处理执行v2.ps1` 收工阶段回收 worktree 残留 reports/）与 ⑷（`工具-Claude包版本探针.ps1` 扩展 `claude plugin list`）。

━━━

🔧 **续三 rebase 冲突已由 Cowork 消解**（09-16 16:0x）：opener骨架.md 无头泳道公共段两边各加第 3 条，保留 master 的「大文件不整读」为 3、分支的「reports/ 写主工作区」顺延为 4；分支 force push `dd01b5b`。续四只做 ⑶b、⑷。 🔧 **续四已完成（09-16 `OP-0916-N`）**：⑶b（v2.ps1 收工阶段兜底回收 worktree 残留 reports/，commit `a6e099d`，单测 3 例）与 ⑷（Claude 包版本探针扩展 `claude plugin list` 检查，新增 verdict=plugin-disabled，commit `cbf1c86`）均已完成。已 rebase 到 master（含 `fe41d3d`），force-with-lease 已 push（HEAD `0e58969`）。回归：`test_工具-opener批处理执行v2.py` 26/26、`test_工具-Claude包版本探针.py` 30/30 全绿，零回归。已登 `合入登记/pending-ff.jsonl`。至此 #584 原计划 ⑴-⑷ 全部完成，🟡 待 Shao Peishen 一字母 ff master。
