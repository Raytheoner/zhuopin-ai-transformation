---
title: "队列行日志 #374（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #374 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #374 · 外置前整格原文（2026-09-04 外置，4 段）

[S:partial][D:业] ✅ **design 审已通过、五点裁决已全部落字（2026-09-02，OP-0902-B1，业务总线 B-0902_60 泳道 `oem-chroma-2`）；本行两问均已答定，剩载体改字未落 ⇒ 停在 `[S:partial]`、未销号。** **裁决人 Shao Peishen，裁决日 2026-09-02，五点结果**：**D0=(b)**（已知客户 NDA 无「物理隔离／独立实例／独立存储介质」字样 ⇒ 不列前置，**单实例多 collection 方案成立**，结论件 §九.2 那条 NDA 取证缺口由裁决人以既有认知直接消解）／**D1=(b)**（新 Q2 采**纯规则库逐条判定**，他原话「先选简单 case 的实现」；历史 8D **仅作离线校准语料、不进生产检索路径**）／**D2=(a) 收紧**（🔴 `compliance_redline_change`，孙涛不可代，**系 Shao Peishen 本人裁决**，代理条已满足）／**D3=(a)**（到期日改为可机械重算的规则）／**D5=(a)**（校验入口未建成前通用库只读）；**D4 随 D0 成立而维持**。 🔴 **本行第 ② 问（2027-01 硬截止是否维持）的答案：作废，重算为 2027-05。** 依 D3=(a) 规则「到期日 ＝ min(所有**需跨客户检索 OEM 数据**的已排期场景的前置开工日)，另有更早数据落盘门禁时取更早者」，D1=(b) 后 Q2 退出推算集合 ⇒ **最早消费方退到 R1（2027-07，前置开工约 2027-05）**。⚠️ **这是「按已成文规则重算」而非「放宽」** —— 本行原约束「重判出结论前一律不得放宽」的解除条件正是「重判出结论」，现已满足；上一泳道对称追加的「也不得自行收紧」同样随裁决解除。 ⚠️ **三条连带结论（裁决人明列，已逐条落 spec，不得漏读）**：⑴ **隔离规范 §4「Q2 历史 8D 归集时即分库、不允许先混库后拆」照常适用，不因 D1=(b) 豁免** —— 那是**数据落盘纪律**，与是否进检索路径无关，**约束点仍在 2026-09 内**（本月）；「不进检索路径」**不得**被读作「可以混库存放」。⑵ L2/L3 最早消费方退到 R1（2027-07／前置约 2027-05）。⑶ 🔴 **「L3 本体这一轮还建不建」升为新的待判项** —— 裁决人明示由本泳道**立行、不代判**。 🔴 **已立两条新行（走编辑锁，`acquire --reserve 2 --section 一 --domain 业`，一次占用）**：**§一 #465**（待判：L3 本体这一轮还建不建，D1 连带项③）／**§一 #466**（待做：按 D2=(a) 改 `router.py` 审计注入语义 ＋ 单测回归 ＋ 调用点核查，命中 openspec 门槛③ 须独立变更包）。 **本次落字（三件，均已实做）**：① `openspec/changes/oem-chroma-ownership-rejudge/design.md` 由草案改**已审定稿**，五点逐条标注裁决人与日期、开放定夺项清零；② 同包 `specs/platform-oem-isolation/spec.md` 定稿（删去 D2 的「审前不生效」封条 ＋ 新增两条 Scenario：`语料只作离线校准不进生产检索路径`／`推算输入已裁决后重算得出的新日期`；`openspec validate oem-chroma-ownership-rejudge --strict` **实跑 = valid**）；③ 🔴 **`3-治理与合规/OEM数据隔离规范.md` §3.2 已补明「未注入 audit 时怎么办」** ＝ 默认 `AuditLogger` ＋ fail-closed 兜底，**两份权威件的直接冲突在文本侧已消解**，§6 修订记录追 **V1.1** 行（批准列记本人裁决）。 🛑 **本泳道未做、如实登记的三项**：⑴ **tasks §3 载体改字仅落 §3.2 一处**，其余（全景规划 §加速启动总览／§8／L1115／L1151、隔离规范 §4 Q4 行与 Q2 行、§5 映射表、新增 §7 运维面、前置总表三处、Q2Q4 差异清单 §2.1、两份 docx 重转、grep 全库零残差）**一律未落** —— opener「不做什么」明写不改全景规划／实施计划／权威排期表，属另批；⑵ **未改任何隔离层实现代码**（本行边界，D2 实现＝ #466）；⑶ ⚠️ **顺带查出一处残差、未改**：隔离规范 §4 R1 行仍写「R1（2026-10）」，系 v6 整域顺延（R1→2027-07）前的旧值，**已随 §3 载体改字一并挂账**。 ⇒ **本行不可销号**：`期望产出` 列明写「上述四处载体同批改字」，仅落其一 ⇒ 诚实停在 `[S:partial]`。变更包 `oem-chroma-ownership-rejudge` 因 tasks 未全 [x] **不归档**，已按「完工即归档纪律」在 tasks 顶写机器认得的 `暂不归档` 说明（判定器＝`0-学习与工具/工具-变更包自动归档.py`）。 ━━━ **原状态原文如下，保留不删**： （原前缀＝ S:blocked ／ D:业）🟡 **评估已完成、结论已出，停在 design 审（2026-09-02，OP-0902-A3，业务总线 B-0902_46 泳道 `oem-chroma`）** —— 须判三件里第 ③ 问已于 2026-08-22 随 88-B 消解，本次答第 ① ② 两问。 **产出两件**：结论件 `1-转型规划/0-全景路线图/OEM隔离层Chroma归属重判-结论件-2026-09-02.md` ＋ openspec 变更包 `openspec/changes/oem-chroma-ownership-rejudge/`（proposal＋design 草案＋tasks＋spec delta；`openspec validate --strict` 实跑 = valid）。 🔑 **核心结论（本次最实质的一条）**：「Chroma 归属」从来不是一件东西，而是三层——**L1 路由 guard（`OEMRouter`，✅ 已就绪且有测试）／L2 唯一入口（`rag.retrieve()`，❌ 未建）／L3 Chroma 本体（❌ 未接）**。v6 与 v8 两次都把它当一件东西挂靠、当一个日期收紧或放宽，**这正是本行「默认沿用比重判更省事」的结构性根因——一件混成的东西无法被干净地判一次**。 **①挂靠点重判**：⑴ **Q4 由「功能挂靠点」降为「存储侧合规挂靠点」**，收敛后三项校验全是公司内部文件比对、不再需要 L2/L3，但 PPAP 文件包本身仍属 OEM 专属技术数据 ⇒ 硬隔离不变；⑵ 🔴 **QD-B 挂错了层** —— 其红线原文要的是 `data_isolation_layer.OEMRouter`（L1，**已就绪**），**故 v6 判词「若 Chroma 停摆，这条 IATF 红线就变成『写了但底层没有』」在事实上不成立**（判例素材 `C05-C07判例素材-2026-08-18.md` L220 实测：立项书不含 OEM 技术方案附件，该路由至今未触发）；⑶ **L2/L3 的真正主人是新 Q2 8D（2026-10）**，但它是**条件性**的，条件＝ D1「新 Q2 生产判定是否需要检索历史 8D 语料」。 **②硬截止**：🔴 **2027-01 的唯一算法是 v6 写死的「留出 Q4 前置 2027-02 的余量」，而 v8 删掉 Q4 的跨客户检索需求后没有人回头重算** ⇒ 这个数字的输入已失效、输出不再有依据。按同一算法以现行权威件重算：最早消费方是 **Q2（场景 2026-10，前置归集 8 月初开工／9 月底完成）**，且 `3-治理与合规/OEM数据隔离规范.md` §4 门禁写死「**归集时即分库，不允许先混库后拆**」⇒ **约束点落在 2026-09 内**。 ⚠️ **本行下方原文那句「本行不阻塞任何在建场景」在 v8 之后已不成立** —— 它是按「挂 Q4（2027-04、前置 2027-02）」写的，把 Q2/2026-10 漏算了；而**它正是本行一直被判为「不急」的依据**。 🔴 **本泳道未改这个日期**：改截止＝改口径（`criteria` → `change_criteria` = 🟡），继承本行原约束「重判出结论前一律不得放宽」，并**对称追加一句「也不得据本件自行收紧」**。 ➕ **顺带查出 L1 的三个缺口（均可独立收口，本次一行代码未改）**：⑴ `rag.retrieve()` 唯一入口未建 ⇒ 按隔离规范 §3.3「入口未就绪前任何场景不得自行接 Chroma」，**现状下 Chroma 即使部署好也没有任何场景被允许使用它**；⑵ 🔴 `OEMRouter(audit=None)` 默认构造静默不写审计（`router.py` L51-53 ＋ 现行 spec 明写「向后兼容」），而隔离规范 §3.2 写「**每次**触发**必须**写 audit……违规**企图**本身就是审计事件」——**两份权威件直接冲突**，属 `compliance_redline_change`（🔴 档，根 CLAUDE.md §5 决策代理条：**孙涛不可代**）；⑶ 通用库 `GENERAL_COLLECTIONS` 读取侧无条件放行（`guard()` L78-80）＋ 写入侧校验「❌ 待设计」⇒ **含 OEM 信息的 8D 若被写进 `kb_quality_cases`，任何客户上下文都能检索到，全程不报错、不留痕**；与新 Q2 直接相关。 ➕ **一处标准更正**：**ISO 26262 不是本项的管辖标准**（它管功能安全，不管客户数据保密）。隔离规范 §1 自己写的依据是**客户 NDA ＋ IATF 16949 §7.5.3 ＋ §8.3**，通篇未引 26262。建议本行及相关载体今后不再以 26262 作为 OEM 隔离的合规理由。 ⚠️ **openspec §5 门槛自评：三条全中**（①改全项目口径／②涉鉴权与数据可见性／③改既有模块对外语义）—— opener 预判「极可能命中第二条」，**实测三条都中**。 🛑 **停在 🟡 档 `openspec_design_review`，本泳道不自行通过**。五个裁决点见变更包 `design.md`：**D0** 客户 NDA 是否含「物理隔离」字样（建议列为审的**前置**，若含则单实例方案整体作废）／**D1** Q2 是否需检索（决定到期日是 2026-09 还是 2027-05，相差约 8 个月）／**D2** `OEMRouter` 无 audit 注入时的行为（🔴 孙涛不可代）／**D3** 到期日由孤立常量改为可机械重算的规则／**D5** 通用库校验入口建成前是否置为只读。 ⚠️ **两项取证缺口如实登记**：⑴ NDA 是否含「物理隔离／独立实例」字样——本 session off-LAN 且 NDA 不在仓内，**无法核实**；⑵ 历史 8D 归集的实际进度与是否已分库——若已开始混库归集，§4 门禁那句已被突破，须向质量线核实（内部进度核实、非发信，不占陈忱串行闸）。 🔴 **本次未改任何隔离层实现代码、未落任何载体一个字**（拟改文本＝结论件 §八，共 6 处文本块，待 D1/D2/D3 裁决后同批执行；触碰全景规划正文须同批重转两份 docx）。 ━━━ **原状态原文如下，保留不删**： 🆕 **2026-08-22 立行，未开工。** **来源**＝OP-0821-H 差异清单 §五 收尾动作② 与 OP-0821-M 收工自查——**该项此前只写在 §四 #88 的行内文字里，没有独立承接行**，按根 CLAUDE.md J1 判据（迁出／销号前须确认已有承接载体）补立本行，避免随 #88 的 88-A 销号一起被读者当成已处置。⚠️ **本行不阻塞任何在建场景**（Q4 排 2027-04、前置 2027-02 开工），但**它有一个真实的到期日 2027-01**，且**默认沿用比重判更省事、因而更容易一直拖着**——这正是它需要独立成行的原因。 ━━━ 🔴 **同批更正（本泳道 2026-09-02 收工前自纠，上文「载体改字仅落 §3.2 一处」已不准确）**：`3-治理与合规/OEM数据隔离规范.md` 的改字范围**由 §3.2 一处扩到四处**。**扩的理由不是顺手，是矛盾**：该件 §4 Q4 行原文写「**Chroma 归属须重判一次，重判出结论前 2027-01 硬截止不得放宽**」——**重判已出结论后，这句话与裁决直接冲突**；而该件本次已因 D2 被打开并盖 V1.1，**在同一份件里留着一句与裁决相反的话，比不改更坏**。**实改四处**：⑴ §3.2 补明默认 `AuditLogger` ＋ fail-closed（D2=(a)）；⑵ **§4 Q4 行**记入重判结论、🔴 **`2027-01` 明确作废并按 D3=(a) 规则重算为 2027-05**；⑶ **§4 Q2 行**追注 D1=(b)（不计入到期日推算）＋ 申明**分库门禁不豁免、约束点仍在 2026-09 内**，**同批更正 §4 R1 行日期 2026-10 → 2027-07**（v6 整域顺延前的旧值，前置约 2027-05，现为 L2/L3 唯一有日期的消费方）；⑷ **§5 映射表**按 L1/L2/L3 三层重核现状、列头日期由 2026-06-13 改 2026-09-02、逐行补承接载体（§3.2→#466／§2.3→tasks 4.2 可由 #466 同包／L3→#465）。§6 修订记录 V1.1 行已同步改写为四处版。 🛑 **仍未做、继续挂在本行**：隔离规范**新增 §7 运维面隔离要求**（spec 侧已有对应 Requirement，规范侧缺该节）／全景规划三处（§加速启动总览 Chroma 归属条、§8 战略决策清单、L1115+L1151 日期复述改指针）／前置总表三处（Q4 行、§三 行动项 4、Q2 行）／`质量域场景重审-…-2026-08-21.md` §2.1 追注／两份 docx 重转／**grep 全库零残差自检**（实测 `2027-01` 全库 213 处/51 件，其中与本判相关的残差主要落在全景规划与实施计划，属未做面）。⇒ 本行仍 `[S:partial]`、仍不销号。

---

# §一 #374 · 整格原文（2026-09-04 人工重写前外置，md5:79439c24）

[S:partial][D:业] ✅ **design 审已通过、五点裁决已全部落字（2026-09-02，OP-0902-B1，业务总线 B-0902_60 泳道 `oem-chroma-2`）；本行两问均已答定，剩载体改字未落 ⇒ 停在 `[S:partial]`、未销号。** **裁决人 Shao Peishen，裁决日 2026-09-02，五点结果**：**D0=(b)**（已知客户 NDA 无「物理隔离／独立实例／独立存储介质」字样 ⇒ 不列前置，**单实例多 collection 方案成立**，结论件 §九.2 那条 NDA 取证缺口由裁决人以既有认知直接消解）／**D1=(b)**（新 Q2 采**纯规则库逐条判定**，他原话「先选简单 case 的实现」；历史 8D **仅作离线校准语料、不进生产检索路径**）／**D2=(a) 收紧**（🔴 `compliance_redline_change`，孙涛不可代，**系 Shao Peishen 本人裁决**，代理条已满足）／**D3=(a)**（到期日改为可机械重算的规则）／**D5=(a)**（校验入口未建成前通用库只读）；**D4 随 D0 成立而维持**。 🔴 **本行第 ② 问（2027-01 硬截止是否维持）的答案：作废，重算为 2027-05。** 依 D3=(a) 规则「到期日 ＝ min(所有**需跨客户检索 OEM 数据**的已排期场景的前置开工日)，另有更早数据落盘门禁时取更早者」，D1=(b) 后 Q2 退出推算集合 ⇒ **最早消费方退到 R1（2027-07，前置开工约 2027-05）**。⚠️ **这是「按已成文规则重算」而非「放宽」** —— 本行原约束「重判出结论前一律不得放宽」的解除条件正是「重判出结论」，现已满足；上一泳道对称追加的「也不得自行收紧」同样随裁决解除。 ⚠️ **三条连带结论（裁决人明列，已逐条落 spec，不得漏读）**：⑴ **隔离规范 §4「Q2 历史 8D 归集时即分库、不允许先混库后拆」照常适用，不因 D1=(b) 豁免** —— 那是**数据落盘纪律**，与是否进检索路径无关，**约束点仍在 2026-09 内**（本月）；「不进检索路径」**不得**被读作「可以混库存放」。⑵ L2/L3 最早消费方退到 R1（2027-07／前置约 2027-05）。⑶ 🔴 **「L3 本体这一轮还建不建」升为新的待判项** —— 裁决人明示由本泳道**立行、不代判**。 🔴 **已立两条新行（走编辑锁，`acquire --reserve 2 --section 一 --domain 业`，一次占用）**：**§一 #465**（待判：L3 本体这一轮还建不建，D1 连带项③）／**§一 #466**（待做：按 D2=(a) 改 `router.py` 审计注入语义 ＋ 单测回归 ＋ 调用点核查，命中 openspec 门槛③ 须独立变更包）。 **本次落字（三件，均已实做）**：① `openspec/changes/oem-chroma-ownership-rejudge/design.md` 由草案改**已审定稿**，五点逐条标注裁决人与日期、开放定夺项清零；② 同包 `specs/platform-oem-isolation/spec.md` 定稿（删去 D2 的「审前不生效」封条 ＋ 新增两条 Scenario：`语料只作离线校准不进生产检索路径`／`推算输入已裁决后重算得出的新日期`；`openspec validate oem-chroma-ownership-rejudge --strict` **实跑 = valid**）；③ 🔴 **`3-治理与合规/OEM数据隔离规范.md` §3.2 已补明「未注入 audit 时怎么办」** ＝ 默认 `AuditLogger` ＋ fail-closed 兜底，**两份权威件的直接冲突在文本侧已消解**，§6 修订记录追 **V1.1** 行（批准列记本人裁决）。 🛑 **本泳道未做、如实登记的三项**：⑴ **tasks §3 载体改字仅落 §3.2 一处**，其余（全景规划 §加速启动总览／§8／L1115／L1151、隔离规范 §4 Q4 行与 Q2 行、§5 映射表、新增 §7 运维面、前置总表三处、Q2Q4 差异清单 §2.1、两份 docx 重转、grep 全库零残差）**一律未落** —— opener「不做什么」明写不改全景规划／实施计划／权威排期表，属另批；⑵ **未改任何隔离层实现代码**（本行边界，D2 实现＝ #466）；⑶ ⚠️ **顺带查出一处残差、未改**：隔离规范 §4 R1 行仍写「R1（2026-10）」，系 v6 整域顺延（R1→2027-07）前的旧值，**已随 §3 载体改字一并挂账**。 ⇒ **本行不可销号**：`期望产出` 列明写「上述四处载体同批改字」，仅落其一 ⇒ 诚实停在 `[S:partial]`。变更包 `oem-chroma-ownership-rejudge` 因 tasks 未全 [x] **不归档**，已按「完工即归档纪律」在 tasks 顶写机器认得的 `暂不归档` 说明（判定器＝`0-学习与工具/工具-变更包自动归档.py`）。 ━━━ 🔴 **同批更正（本泳道 2026-09-02 收工前自纠，上文「载体改字仅落 §3.2 一处」已不准确）**：`3-治理与合规/OEM数据隔离规范.md` 的改字范围**由 §3.2 一处扩到四处**。**扩的理由不是顺手，是矛盾**：该件 §4 Q4 行原文写「**Chroma 归属须重判一次，重判出结论前 2027-01 硬截止不得放宽**」——**重判已出结论后，这句话与裁决直接冲突**；而该件本次已因 D2 被打开并盖 V1.1，**在同一份件里留着一句与裁决相反的话，比不改更坏**。**实改四处**：⑴ §3.2 补明默认 `AuditLogger` ＋ fail-closed（D2=(a)）；⑵ **§4 Q4 行**记入重判结论、🔴 **`2027-01` 明确作废并按 D3=(a) 规则重算为 2027-05**；⑶ **§4 Q2 行**追注 D1=(b)（不计入到期日推算）＋ 申明**分库门禁不豁免、约束点仍在 2026-09 内**，**同批更正 §4 R1 行日期 2026-10 → 2027-07**（v6 整域顺延前的旧值，前置约 2027-05，现为 L2/L3 唯一有日期的消费方）；⑷ **§5 映射表**按 L1/L2/L3 三层重核现状、列头日期由 2026-06-13 改 2026-09-02、逐行补承接载体（§3.2→#466／§2.3→tasks 4.2 可由 #466 同包／L3→#465）。§6 修订记录 V1.1 行已同步改写为四处版。 🛑 **仍未做、继续挂在本行**：隔离规范**新增 §7 运维面隔离要求**（spec 侧已有对应 Requirement，规范侧缺该节）／全景规划三处（§加速启动总览 Chroma 归属条、§8 战略决策清单、L1115+L1151 日期复述改指针）／前置总表三处（Q4 行、§三 行动项 4、Q2 行）／`质量域场景重审-…-2026-08-21.md` §2.1 追注／两份 docx 重转／**grep 全库零残差自检**（实测 `2027-01` 全库 213 处/51 件，其中与本判相关的残差主要落在全景规划与实施计划，属未做面）。⇒ 本行仍 `[S:partial]`、仍不销号。 ━━━ 📎 历史回写 2 段已于 2026-09-04 外置（K2 口径，原文原样 md5:c50cde88）见 `1-转型规划/0-全景路线图/队列行日志/#374.md`；本格只留首 1 段＋末 1 段
