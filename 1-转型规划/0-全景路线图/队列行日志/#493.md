# 队列 §一 `#493` · 状态列历史回写段（原文原样）

> 2026-09-07 由 CC `OP-0907-V` 按 K2 行长口径迁出（行内状态格 7409 B > 4096 B）。
> **原文未改写**；行内只留首段＋本件指针＋末段。段间分隔符沿用队列行内的 ` ━━━ `。

[S:open][D:机] 🆕 **2026-09-07 立行（CC `OP-0906-AA` 实跑取证，只登不修——`#398` ⑺ 派单件 §三 明确不碰编辑锁本体）**：根因与修法边界均已写死，可直接派单。 

━━━

预留豁免：本行编号 493 ＝ 本次 acquire 回显的高水位线 §一 #492 +1，整个持锁窗口内无第二方写入（撞号风险为零，可由锁 history 复核）；未走 `--reserve` 的原因＝本会话 acquire 时只计划回写 `#398` ⑺，本行是主仓实跑取证**中途**才发现的新条目，而同一把锁内无法再 `acquire --reserve`（那正是本会话刚修完的自锁形态）。⚠️ 本条是**一次性**留痕，不是常规做法：正确姿势仍是 acquire 时按需 `--reserve`。

━━━

登记豁免：release ⑹ 点名的 9 个脏文件全部落在「acquire 之前就已经脏」一组，属并发 session 的在办产出（`队列回写待补/` 5 个 JSON 属 `B-0906_X`、`editlock-credential-shape-guard` 归档与 spec 2 件属 `#480`、两份接力件与 `coverage_point_ledger/models.py` 属他线在办），**不由本会话认领**——本会话只碰 `#398`／本行／§二 新批次与其声明的 2 个产出件；这 9 条由 sweep 每轮 `#236` 孤儿告警持续点名，已有承接方，不会静默掉在地上。 

━━━

➕ **2026-09-07 06:0x 当场又撞一次，且形态更宽（同一次收工实测，`OP-0906-AA`）**：本次被拒的不是骨架件占位符，而是**另一条会话正在写的** `1-转型规划/0-全景路线图/看护件-泳道看护批B-0907_E-2026-09-07.md:61` 的 opener 块缺 `[OP-MMDD-X]` 前缀（`[F5]`）——**这一次守卫判得对**，问题在于**被卡住的是 sweep 和全体会话**。🔑 **由此暴露一条比本行原始范围更宽的缺口**：编辑锁「release 校验不过则保持占用、逼持有者原地修正后重试」这条设计，前提是**持有者有能力修正**；而 sweep 是无人值守持有者，撞上「不是自己造成、也不归自己管」的违规时**原地修正不了**，只能把锁留到 30 分钟陈旧 ⇒ 期间全员写队列被拒。`#398` ⑺ 的修法已保证**本轮不再早退**（本次实测：仍落库 `bf353c4`、统一推送、退出码 0，只是轮末带锁），但**轮末带锁这一半仍在**。⇒ 领取方请连这一条一起解：给无人值守持有者一条出路（例如 release 被拒时按「违规是否落在本次持锁者触碰过的文件里」分流，不是自己碰的就放行并把违规转成告警／派单），**不得靠关掉守卫或加 --force 了事**。本次那把锁在对方修好后由 `release --who sweep-commit` 正常释放（非强行接管，退出码 0）。 

━━━

➕ **2026-09-07 CC `OP-0907-V` 建造完成，待合入**（分支 `claude/op0907v-opener-guard-493`，commit `548c323`，已推 origin；🟡 合入 master 需 Shao Peishen 一个字母，故本行**不销号**）：⑴ **格式正本换判据、不是关掉**——`工具-opener块lint.py` 新增 `SKELETON_CANON_REL`／`is_format_canon()`／`check_canon_file()`，`check_block()` 加 keyword-only 参数 `is_format_canon_file`。正本内形态①②③⑤（**占位符敏感**：问的都是「占位符填对了没有」）换成正本自检 `C1`/`C2`/`C3`/`C5`；形态④⑥⑦（与占位符无关）**照常生效**（实测正本上本就零命中）。**修好后它在什么情况下发信号**＝正本不再教 `set_session_title`（C1）／不再教子任务例外句（C2）／标题占位符不再是 `[Win]MMDDX-…`（C3）／首行占位符不再是 `[OP-MMDD-X]…`（C5），外加 F4/F6/F7 照常。🔴 排除**按路径判、刻意不做 basename 匹配**（归档目录同名历史副本不一并放过），🔴 **不外溢**：同一段占位符文本换个路径照样命中 F3/F5——「照抄骨架却漏填占位符」正是形态③要抓的，一起放掉就白建。**未给骨架件写任何永久豁免标记**（期望产出里「一次一用、不得写成永久开关」那条因此不适用）。⑵ **加宽那一半也解了：release 归属分流**——`_opener_guard_violations()` 新增可选参 `lock_data`/`queue_texts`，据新函数 `_opener_attribution()` 把命中分「归本次持锁者」（照旧拒绝）与「不归本次持锁者」（降级为告警、放行）。归属 ＝ ⑴ 持锁期间新出现（不在 `dirty_at_acquire` 快照里）**或** ⑵ 落在本次待处理 §二 批次清单里（`_dirty_path_is_covered`，与 ⑹ 逐字同口径）。🔴 **⑵ 不是多余的**：只有 ⑴ 会开「先写坏 opener、再 acquire」的规避口，补上 ⑵ 后闭合——脏文件想不被 ⑹ 拦死就必须登进 §二，一登进来就重新归本人管，**两道守卫互锁**。🔴 判据是「违规落在谁碰过的文件里」，**不是「持锁者是谁」**——按身份开豁免就是 `SELF_COMMITTING_LOCK_HOLDERS` 那条路，而人类会话同样修不了别人的文件（本行原文「对人类会话是硬拦截」）。🔴 降级 ≠ 静默：不归本人的每一处**逐条打印**并写明由「谁碰谁修」承接、其所属会话自己 release 时照常被拦；打印点**在 waiver 与 `not mine` 两条早退之前**。🔴 无 acquire 快照时一律判「归我」（判不了归属 ≠ 判定不归我）。**未加 `--force`、未关任何守卫**。⑶ **顺带修一处版本错配**（同一函数的取材面，属本行触碰区）：`_OPENER_LINT_SCRIPT` 改取**本脚本同目录兄弟**，不再取 `REPO_ROOT / …`——`REPO_ROOT` 是 `_resolve_repo_root()` 解出的**主工作区**（那是共享同一把锁的正确落点），于是任一 linked worktree 里跑本脚本加载到的是**主 checkout 那份 lint**、与本文件版本对不上；本次即实撞 `AttributeError`，且在 worktree 里**根本无法测**自己刚改的判据。⚠️ 只改判据取处，**`REPO_ROOT` 本身不动**。

━━━

**验证（手段写全）**：`python -m pytest 0-学习与工具/test_工具-opener块lint.py` → **68 passed**（新增 13 条，含「正本漂了要报 C1/C2/C3/C5」四条与「排除不外溢／不做 basename 匹配」两条反例，另一条直接拿**仓库真身** `opener骨架.md` 断言零违规）；`pytest test_工具-共享文档编辑锁.py + test_工具-opener块lint.py + test_工具-opener生成.py + test_工具-共享文档编辑锁-补件选表.py` → **543 passed**（新增 8 条：正本占位符不阻断／正本漂了仍阻断／他人文件降级为告警／本次新写仍阻断／**先写后锁但已登 §二 仍阻断**／无快照仍阻断／自己被拦时别人那处照样打印／豁免不静默他人组）。全库回归：`python 0-学习与工具/工具-opener块lint.py` 扫 1504 份 `.md`，当前在用件命中 **57 → 49**，与改前基线（`git show HEAD:…` 取旧版同址跑）**逐行 diff**，减少的 8 处**全部且仅**来自 `1-转型规划/0-全景路线图/opener骨架.md`，其余零变化。

━━━

⏭️ **待总线/看护者接手**：① 合入 master（🟡 档，需他一个字母）；② 合入后本行方可销号；③ 本会话为子任务泳道，按「只 push 自己那条分支、不在主仓 commit」执行，**未登记 §二 批次**（代码在分支上、主仓工作区无对应脏文件，登了 sweep 也取不到活）。

【机器字段解析】状态＝open，域 机
