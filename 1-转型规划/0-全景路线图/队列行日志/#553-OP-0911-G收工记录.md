---
title: "#553 ⑵⑶ 收工记录 · P4 措辞更正 ＋ sweep 第 16 类「未并入分支超阈值」（OP-0911-G）"
created: 2026-09-11
承接: 队列 §一 `#553`（本件＝行内收工段的全文版，行内 ≤4 KB 只留结论＋指针）
执行: CC 无头泳道 `OP-0911-G`，worktree `.claude/worktrees/op0911g-553-p4-unmerged-alert`（起自 master `454eaf3`，零提交，收工自删）
---

# #553 ⑵⑶ 收工记录（2026-09-11 16:1x 本地）

## 一、⑵ P4 措辞——三处同批改

| 位置 | 改前 | 改后 |
|---|---|---|
| `1-转型规划/0-全景路线图/opener骨架.md` L62（【CC · 子任务泳道】块末行前一行） | 主仓 ff 由 sweep 收尾段或看护者收工时串行做（P4） | 主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（P4，`#553` 更正） |
| `0-学习与工具/工具-opener生成.py::SUBTASK_PUSH_NOTE` | 同上（逐字相同） | 同上（逐字相同，契约用例 `骨架与生成器契约` 比对） |
| `0-学习与工具/test_工具-opener生成.py` | 无原文断言，四处均比常量 `M.SUBTASK_PUSH_NOTE` | 无需改 |
| **顺手**：骨架 L88（看护者块「扇出与 git」第 3 条） | **主仓 ff 由落库 sweep 收尾段做**；sweep 不可用时由你在收工时串行做 | 主仓 ff 由你在收工时串行做，一次一条，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；🔴 sweep 不做 ff |
| **顺手**：`0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` L81 | **主仓 ff 由 sweep 收尾段做**（sweep 不可用时看护者收工串行做） | 主仓 ff 由看护者收工串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（`#553` 更正） |

- 后两处派单件未列，属**同一事实**的当下生效指针（根 CLAUDE.md「历史记录不追改」判别：活件里的引用改、带日期的历史件不改）。全库 grep `sweep 收尾段做|由 sweep 收尾段` 余下 5 处命中全是带日期的历史派单件／看护件／方案件，未动。
- 手段：`python -m pytest test_工具-opener生成.py -q` ⇒ `93 passed, 25 subtests passed in 1.13s`；`python 0-学习与工具/工具-opener块lint.py --file 1-转型规划/0-全景路线图/opener骨架.md` 改前（主仓 master）与改后（泳道）均 `opener 块自检零违规`，rc=0。

## 二、⑶ sweep 第 16 类「未并入分支超阈值」

### 2.1 判据正本（新建）`0-学习与工具/工具-未并入分支分类.py`

- 候选＝`git for-each-ref --no-merged master refs/heads/claude refs/remotes/origin/claude`（同名同尖端合一条、分叉各一条）。
- 三层 A 判（任一命中即 A）→ C 判两条 → 余下 B，与分类清单 §六 `triage.py` 同判据：① 独有非 merge 提交 patch-id 全在 master 侧找到等价（`git cherry` 内核）；② 无等价提交触碰的文件在 master 尖端逐字节相同；③ 新增每一非空行都在 master 同路径文件中（`#482` 行级口径）；C① 是另一条未并入候选的祖先；C② 未落地行只剩过程状态文件。空 diff 提交不算未合入、merge 提交不比（同 `工具-patchid比对.py`／`git cherry`）。
- patch-id 按 SHA 缓存 `reports/sweep-unmerged-branch-patchid-cache.json`（不入库，只留本轮两侧 rev-list 出现的 SHA）：主仓实测首轮 `1m21s`、第二轮 `25.6s`；缓存只加速不改结果（单测 `test_缓存不改结果且只留本轮SHA`）。
- 只读：不 fetch、不合、不删；`git status` 前后不变（单测 `test_只读_status前后不变`）。

### 2.2 sweep 侧 `_check_unmerged_branch_backlog`（`0-学习与工具/工具-落库sweep.py`）

- 子进程调判据 `--json --repo <root> --cache reports/…`（同第 13 类）；判据不可用 ⇒ 「不据此判为零未落地」＋独立 standing state，恢复即解除。
- 阈值常量 `UNMERGED_BRANCH_B_MIN_COUNT = 5`／`UNMERGED_BRANCH_B_MIN_AGE_DAYS = 7`（Shao Peishen 2026-09-11 答 `2a`，属 🟡 口径，本泳道未动）；「最老」按 B 分支**首个未在 master 的提交**的 committer 时刻算。
- 告警一行：B 条数／最老天数／最老三条（分支｜首个未落地提交日｜天数）；A／C 只附计数；指纹＝B 分支名集合 sha1[:12]，集合不变即静默（24 h 复提醒），集合变 ⇒ 旧 key 解除、新 key 告警；回落阈下即解除。零命中也回显。
- 🔴 调用点放在 `main()` 的 `if not args.dry_run:` 块之外，`--dry-run` 也跑到（只回显、不写状态、不推送）。
- 既有 `test_工具-落库sweep.py` 夹具 `SweepTestBase.setUp` 加桩 `STUB_UNMERGED_BRANCH_SCRIPT`（恒零候选），防每个 CLI 级用例因临时仓库缺判据脚本而多推一条「判据不可用」webhook（第 14 类 2026-09-10 实撞形态）。

### 2.3 单测（新文件 `0-学习与工具/test_工具-落库sweep-未并入分支告警.py`，27 例）

`python -m pytest "test_工具-落库sweep-未并入分支告警.py" -q` ⇒ `27 passed in 12.94s`

- 分类器 11 例（真临时 git 仓库）：零候选／B 真未落地（含 `first_unmerged_date`＝3 天前、`first_unmerged_commit`）／A① patch-id 等价（cherry-pick 后 merge-base 仍判未并入）／A① 精确靶（cherry-pick 后 master 又改掉那行，②③ 都救不回、只有 patch-id 能救）／A② squash 后文件逐字节相同／A③ 行级／C① 后继分支祖先／C② 只剩过程状态文件／空提交不算未落地／非 `claude/` 不入候选／缓存不改结果／只读。
- sweep 侧 15 例：接线（调用行存在）／阈值常量＝5、7／零命中回显／**条数边界 4 不触发、5 触发**／**天数边界 6 不触发、7 触发**／最老取最老一条／**A 64＋C 4 不计入阈值只附计数**／**指纹不变即静默（同集合、天数变仍静默）、集合变即旧 key 解除＋新 key 告警**／回落阈下解除／dry-run 回显但不写状态不推送／判据不可用不判零＋恢复解除／正文列最老三条。
- 端到端 1 例：临时仓库放真判据脚本，sweep 子进程真调，5 条 B 各 10 天＋1 条 cherry-pick 的 A ⇒ `A 1／B 5／C 0`、最老 10 天、触发、状态与缓存文件落地。

### 2.4 🔴 变异 2 组（改源→同命令复跑→还原，`grep -c MUTATION` 两文件均 0）

| 变异 | 改法 | 结果 |
|---|---|---|
| ① patch-id 比对 → merge-base 视角 | `工具-未并入分支分类.py::analyze_branch` 的 `plus = [s for s in unique if branch_pids[s] and branch_pids[s] not in master_pid_set]` 改为 `plus = [s for s in unique if branch_pids[s]]`（凡 `base..tip` 提交一律当未落地） | `FAILED …::TriageClassifierTests::test_A类_patchid等价_即使master后来改掉那行` ⇒ `1 failed, 26 passed` |
| ② 去掉 7 天判 | `工具-落库sweep.py::_evaluate_unmerged_branch_backlog` 的 `triggered = b_count >= … and oldest_days >= …` 改为只判条数 | `FAILED …::UnmergedBranchAlertTests::test_天数边界_6天不触发` ⇒ `1 failed, 26 passed` |

还原后：`pytest "test_工具-落库sweep-未并入分支告警.py" "test_工具-opener生成.py" -q` ⇒ `120 passed, 25 subtests passed in 17.10s`（泳道）；补丁落主仓工作区后同命令再跑 ⇒ `120 passed, 25 subtests passed in 12.96s`。

### 2.5 既有套件零回归（两侧同命令）

命令：`python -m pytest test_工具-落库sweep.py -q -k "HappyPathTests or OrphanFileAlertTests or ScheduledTaskMirrorSyncTests or StartupLogLineTests or UnexpectedExceptionFallbackTests or DeploymentTraceHintTests"`（会整跑 `main()` 且断言 webhook 次数的子集；未整跑 471 条，`#550` 环境上限）

- 本棒：`7 failed, 14 passed, 464 deselected in 111.12s`
- 纯 master `454eaf3`（临时 detached worktree `op0911g-baseline-master`，跑完已删）：`7 failed, 14 passed, 464 deselected in 106.48s`，**同 7 条**（`OrphanFileAlertTests` 4 条＋`DeploymentTraceHintTests` 3 条，均「webhook 次数多 1」）。
- 多出的那条 webhook 取证：直跑失败场景抓 sweep 全回显，来源＝`✓ 定时任务分类告警已推送。`（第 14 类在临时仓库里因 `工具-定时任务源码备份.py` 不在而报「备份工具不在」），第 16 类回显为「B 0 … 未触发」零推送。⇒ 与本棒无关的存量红，判零回归。

### 2.6 实弹 `--dry-run` 回显（主仓 `C:\Dev\zhuopin-ai`，2026-09-11 08:05Z＝16:05 本地，用泳道版 sweep 脚本，rc=0，耗时 1m25s）

```
🌿 第 16 类：未并入 claude/* 分支 81 ＝ A 64（内容已在 master）／B 13（真未落地）／C 4（该删）；B 最老 38 天（阈值 B ≥5 且 ≥7 天）——**触发告警**；指纹 unmerged-B13:e51ca5180535。
    · B `claude/unified-portal-design-8a2ce3`@648a0e5b｜2026-08-04｜38 天
    · B `claude/a22-closure-form-apply`@93245b2a｜2026-08-25｜17 天
    · B `claude/a30-inbound-whitelist-apply`@32feda54｜2026-08-25｜17 天
    （dry-run：不写状态文件、不推送；下为真跑时会推的正文）
    | 🌿 落库sweep：**未并入分支超阈值**（第 16 类常驻告警，队列 §一 `#553` ⑶）——B 类（真未落地）**13 条**，最老 **38 天**（阈值 B ≥5 且最老 ≥7 天）。
    | - 最老三条（分支｜首个未落地提交日｜天数）：
    |     · `claude/unified-portal-design-8a2ce3`@648a0e5b｜2026-08-04｜38 天
    |     · `claude/a22-closure-form-apply`@93245b2a｜2026-08-25｜17 天
    |     · `claude/a30-inbound-whitelist-apply`@32feda54｜2026-08-25｜17 天
    | - 另：A 类（内容已在 master）64 条／C 类（该删）4 条不计入阈值（候选共 81）。
    | ⇒ 本告警只让「不知道」变「知道」：🔴 **不自动合并、不自动删除**。合与删走『已授权待合』登记处（`工具-待合分支巡检.ps1`）或看护者收工串行 ff；逐条证据：`python 0-学习与工具/工具-未并入分支分类.py`。B 类集合不变时 24 h 内不重推。
```

- 判据脚本单独实跑（泳道，`--cache`）全表：B 13 条＝`unified-portal-design-8a2ce3`（08-04）／`a22-closure-form-apply`／`a30-inbound-whitelist-apply`（08-25）／`queue-315-apply-9f2c1a`／`op0822c-sweep-alerts-b9424a`（08-27）／`op0906i-dont-guard-487`／`happy-thompson-017679`（09-06）／`op0908i-358-audit-retention-b52b16`／`op0907ae-reserve-draft-487`（09-07）／`op0909i-eval-suite-review-1a4f02`（09-09）／`op0910m-544-heartbeat-batch`／`op0910n-529-timeout-semantics`／`op0910l-507-sweep-apply`（09-10）。与 ⑴ 清单一致：候选 134→81 因 master 前进吸收；B 18→13 即清单 §七「剔除 B1 后为 13」。
- ⇒ 真跑首轮**必推一条第 16 类告警**（B 13／38 天），属预期；此后 B 集合不变 24 h 内静默。

## 三、落库方式与边界

- 按件「🔴 不自行 commit、不 ff master」：泳道零提交。七个文件的改动导出为补丁（`git add -N` 两个新文件后 `git diff`，63 131 B），在主仓 `git apply --check` 通过后 `git apply` 落到主 checkout 工作区；前置实测＝主仓 master 仍 `454eaf3`、七路径自 `454eaf3` 起 master 未动（`git diff --quiet 454eaf3 master -- …`）、七路径在主仓工作区干净。落格后在主仓复跑两份单测 `120 passed` ＋骨架 lint 零违规。登 §二 `B-0911_553P4措辞与待合告警` 由 sweep 取活。
- 未做：不合并、不删任何分支；`_reconcile_with_origin_and_push` 未动；阈值 5／7 未改；`opener块lint.py` 未改；`test_工具-落库sweep.py` 只加夹具桩、未改任何断言。
- 主仓 dry-run 期间临时复制过一份判据脚本到主仓 `0-学习与工具/`（当时主仓尚无该文件），跑完即删；随后随补丁正式落入。`reports/sweep-unmerged-branch-patchid-cache.json` 已在主仓生成（gitignore 内），真跑首轮直接受益。
