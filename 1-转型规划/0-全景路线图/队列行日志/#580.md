---
title: "队列行日志 #580（§一）"
created: 2026-09-16
status: 生效
用途: 跨桌任务队列 §一 #580 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #580 · 外置段（2026-09-16 外置，工具第 1 批，1 段，原文原样）

> 外置人 CC-OP-0916-C；外置前整格 4856 B、共 3 段；本批外置第 2 段；本格保留第 1、3 段。

🔧 **合入前四项修复（2026-09-16 09:35 `OP-0916-T` Cowork 复核，Shao Peishen 拍 1a：同分支修完再与 #581 一起报合入）**：⑴ **子泳道拆链**：Agent 子泳道 jsonl（`<会话>/subagents/*.jsonl`）与主会话同 sessionId，现报告 §二 同一 session_id 出现多行（例 45649529 共 11 行，实为 OP-0907-Y 看护＋10 条子泳道）⇒ 统计单位改为「链」（按文件），每行标 `父会话 id` 与 `主链/子泳道` 类型；另出按父会话合计的一张表。⑵ **看护识别漏检**：§五 为 0，而标题含「看护」的链 24 条、cache_read 3.9 亿（14.4%）⇒ 判据改为「首条用户消息含 看护 或 Bash 含 泳道看护状态机.py/heartbeat/Get-Content…lane-heartbeat 等轮询形态」，用 OP-0909-Q／OP-0907-Y／OP-0907-R 三条真链做夹具式断言（只断言识别结果，不入库真实日志）。⑶ **输出落点**：`--out` 缺省必须解析到主工作区 `reports/`（复用 `git rev-parse --git-common-dir`，同 `工具-泳道看护状态机.py` 手法）；本次 09:14 泳道内跑出的报告随 worktree 删除丢失，已由 Cowork 在主仓重跑。⑷ **本行状态列更正**：前文「≥300 为 0 系窗口差一天」的归因不成立，真实原因是 ⑴ 子泳道拆链；修完重跑基线，用新口径回写：峰值 >150k／>200k／>300k 的链数与 cache_read 占比、父会话合计 Top10、看护链合计。不做：不改 `#581` 触碰区任何文件。
