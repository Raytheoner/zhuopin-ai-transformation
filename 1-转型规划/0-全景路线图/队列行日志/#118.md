---
title: "队列行日志 #118（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #118 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #118 · 外置前整格原文（2026-09-04 外置，46 段）

[S:partial][D:业] **🟡 部分完成（①+④首版，2026-07-29，CC）**：姚祖怡二次举证真实案例（S02Y.0210 三行 600/400/500 套争用现货 1153，"必须逐级扣料"），Paul **07-29 当面拍板对①+④这一窄片跳过判例包直接建造**（原话"就先用出货日期早晚当临时优先级……现在就动手做逐级扣减"）——**①现货净额−优先级在前项目已占用量 + ④跨期依次扣减，已建造+全量回归零漂移+真实部署`.51:8091`+真实数据验证通过**（`_allocate_sequential_inventory`/`_resolve_priority_order`，临时优先级=出货日期升序，`priority_resolver` 接口已就绪待 #15 真实数据接入即自动切换）。真实验证过程中发现并修复自身引入的 bug（真实 FO 文档下同一 so_id 可对应多个行项，首版按 so_id 去重导致跨行竞争检测失效，已改按下标定位修复，SC8 回归 276 passed+4 skip）。详见 SC8 场景 CLAUDE.md 2026-07-29 行。**②缺口≤0 不进缺料清单、③答交数量累计**（已在 07-28 批2 `_cumulative_confirmed_batches` 实现，非本次）、**#15 真实 PMC 优先级表导入 UI、"上月未齐套继续占用"的显式跨月建模**——仍待判例包/排期，未做。**🔄 2026-07-29 业务总线改判剩余三项归属（收窄判例包范围，省一轮往返）**：**(a) ②缺口≤0 不进缺料清单 → 改判为「口径已显式签认，可直接建，不需判例包」**——姚祖怡 07-26 V6 回件原文已逐字写死：「缺口数量126pcs（取值=本项目需求数量-可用现货数量，**缺口数量如≤0，则该行视为满足齐套需求，不应该体现在缺料子件中**）」。这与 #18-a/b/c 属同一情形（专员本人书面明示 → 构成显式签认 → C 类限制解除），**再发判例包去问一个他已经回答过的问题，正是判例批改法要消灭的浪费**。→ 并入下一个 SC8 批次直接实现（注意实现时确认过滤发生在"展示层"还是"计算层"：他的原话是"不应该体现在缺料子件中"＝展示层过滤，**不得顺手把它改成计算层剔除**，否则会影响齐套数量本身）。**(b) #15 真实 PMC 优先级表导入 UI → 属功能排期，非口径**——他早前已给过导入表结构（数字越小优先级越高），`priority_resolver` 接口已就绪，接上即自动替换当前"出货日期升序"临时口径。**(c) 「上月未齐套项目继续占用」的跨月显式建模 → 这一项才是真正仍需口径确认的唯一残项**，判例包只需覆盖它一项（一信 ≤3 决策点绰绰有余）。**故本行剩余开放口径点：3 项 → 1 项。****回复姚祖怡信已合并 #147 徽标修复一并发送**（`✅ 已推送 2026-07-29 06:14 UTC`，Paul 聊天内"确认"后发出，md+docx 双格式，README 已回填） ━━━ ➕ **2026-08-12 拆件巡逻回灌（源＝§一 #331 docx 新问题 4；由环境总线代回补）**：**姚祖怡把预测订单优先级的判据一次写全，补上了此前缺的第二、三级**——① 计划出货日期在前的优先级高（举例：2026-08-10 高于 2026-08-20）；② **出货日期相同则 ERP 预测订单数量大的优先**（举例：同为 2026-08-10 的 F02N.0224 数量 1100 与 F02N.0226 数量 600，F02N.0224 优先）；③ **数量与出货日期都相同则按取值的自然顺序，先取值的先占用**。**⇒ 本行 2026-07-29 建造时的「临时优先级＝出货日期升序」只实现了第 ① 级**（当时 Paul 当面拍板窄片先建，判据本就只到这一层），②③ 两级为本次首次给出。他同时要求检查取值逻辑并整体复核一遍。**本行 _resolve_priority_order／priority_resolver 接口即承接点**，无需另开行。**⚠️ 边界**：他这三级是否即 #15「真实 PMC 优先级表」的最终形态、还是仍需 PMC 侧表格覆盖，他本次未提，**巡逻不推断**。 ━━━ 📌 机器字段维持 [S:partial][D:业] 不变——本次只增内容、不改状态。 ━━━ ✅ **2026-08-18 ②③ 级判据补全完工（CC，同 worktree／同 commit `c562c2d`，与 #266 同车不可拆）**：`_resolve_priority_order` 兜底排序由 `(出货日, 下标)` 改为 `(出货日, -数量, 下标)`，即 ①出货日期在前 → ②同日则 ERP 预测订单数量大的优先 → ③都相同按取值自然顺序；该三级判据**同时用作 `priority_resolver` 分支的二级判据**（resolver 接口粒度只到 so_id，真实数据里同一 FO 文档下多行项共享同一 so_id、它无从区分；改造前该分支仅按原始下标）。**resolver 优先地位不动**——他未说明三级判据是否即 #15「真实 PMC 优先级表」的最终形态，**不替他推断**（他的边界原样保留）。**用他的真实举证验证通过**：同为 2026-08-10 的 `F02N.0224`(1100) 与 `F02N.0226`(600)，两者共用 132 个叶子件，以 `R01A.0004` 为例占用次序由 `[F02N.0226, F02N.0224]` 变为 `[F02N.0224, F02N.0226]`，数量大的先占。**真实数据影响面**：97/109 行的**有效库存快照**发生变化（大量成品共用叶子件，次序一动即连锁），落到显示字段上是 **9 行**变化（含判例 3 对照组 `S02Y.0210` 出货 2026-08-20 那一行，已在 #266 行与对照件中归因说明）。新增 8 个单测；既有用例 `test_priority_resolver_same_so_id_multiple_lines_falls_back_to_original_order` **未改动亦未失败**（该用例三行出货日递增，新键得出的次序与原断言一致）。**他同批新举证⑴（「可齐套」＝今日、「需求日可齐套」＝出货日当日，且须按各项目优先级逐级扣减）**：逐级扣减本行首版已实现、本次补齐判据；两个字段的取值语义经核对与他的表述一致，**且本次 #266 穿透后才真正产出有意义的数字**。**⚠️ 本行仍 `[S:partial]`，未销号**——剩余未做：**#15 真实 PMC 优先级表导入 UI**（属功能排期）与**「上月未齐套项目继续占用」的跨月显式建模**（唯一仍需口径确认的残项，见 #150）。 📌 机器字段维持 [S:partial][D:业]。 ━━━ ➕ **2026-08-18 拆件巡逻回灌（源＝§一 #346 姚祖怡对采购部#14 的回件）：他首次完整给出「无答交件预估齐套日期」的三条规则** ━━━ 他在说明 109 行勾选中「误判」的原因时，把判据一次写全（逐字转录）：**规则1（出货日不在三个月内）**——无答交按照出货日往前推算 3 个月的 20 日起、再往后推算 90 天，预估齐套日期；**规则2（出货日在三个月内）**——无答交按照此时此刻起往后推算 90 天，计算齐套日期；**规则3（同一项目既有答交又有无答交）**——对比无答交的预估齐套日期与有答交的答交日期，**更晚**的那一个是齐套日期。 🔴 **与本行既有口径的对照＝事实陈述，非巡逻的技术判定**：本行 2026-07-29 记载的「保守预测优先——未答复子件按 90 天估算，取两者较大值」（Paul 当面拍板）**在方向上已覆盖规则2 与规则3**；**规则1 是全新分支**——它把 90 天的起算点由「今天」改为「出货日往前推 3 个月的 20 日」，**且只在出货日落在三个月之外时适用**，此前任何载体（本行、#150、SC8 场景 CLAUDE.md、判例包素材）均无此分支。**它建模的是「提前 3 个月下单 ＋ 90 天交期」这条采购节奏，对远期订单会得出比「从今天起推 90 天」更晚、也更保守的预估日期。** ⚠️ **三条与现有实现是否逐条对得上、规则1 是否已被覆盖、改它会牵动多少行四色判定，均须 CC 建造时实测确认，巡逻不替其定性**（同本行对他新举证⑵ 的既有处置）。 ⚠️ **这三条规则的确认路径尚未定**——他是口径权威且已书面写下（与本行 (a) 那条「专员本人书面明示＝显式签认」先例同形），但规则1 属判据类新分支、改动会连带动四色判定，**是否仍需走判例批改签认已登 §四 #65 交 Shao Peishen**，本行在其答复前不据此开工。 📌 机器字段维持 [S:partial][D:业] 不变——本次只增内容、不改状态。 　━━━　🟡 **2026-09-02 CC/`OP-0902-A5`（worktree `atp-propose`，分支 `claude/op0902a5-atp-propose`，commit `282853d` 已 push origin）：ATP 批 2 收口已立项，变更包 `openspec/changes/sc8-atp-batch2-closeout/` 四件齐备（proposal＋specs 两条 delta＋design＋tasks），`openspec validate --all --strict` **127 passed / 0 failed**。🔴 本次只到 design 草案 —— 未写实现、未部署、未触碰 `.51`、未起草任何信；design 审属 🟡 档，已 `pause --action-key openspec_design_review`，未自行通过。** ━━━ **⑴ 开工前置已解除**：§四 `#65` Shao Peishen **2026-09-02 答 (a)** —— 认姚祖怡（男）2026-08-18 的书面文本即显式签认，三条规则可直接用于建造。本行原文那句「在其答复前不据此开工」**至此解除**。他同时接受并留痕 (a) 的代价：IATF 审核问「这条判据谁签的」时证据是企微文本而非签认件，且规则 1 的「往前推 3 个月的 20 日」**无对照案例可验**。⇒ design D1 已把三条规则**逐字引用原文并标注来源与日期**，这是对该代价的唯一缓解手段。 ━━━ 🔴 **⑵ 三条规则逐条实测（opener 指定「用实测、不用推断」；探针自行逐字实现规则、不复用被测代码以免自证；`today=2026-09-02`，三个月边界含边界＝`2026-12-02`，`NO_FEEDBACK_LEAD_DAYS=90`）** ━━━ **规则 1 ＝ 已完全覆盖**：出货日网格 **309 个适用日、不一致 0 个**（抽样：出货 2026-12-05 ⇒ 起算 2026-09-20＝逐字；2027-02-20 ⇒ 2026-11-20；2027-04-20 ⇒ 2027-01-20），落点 `forecast.no_feedback_start_date()`。**规则 2 ＝ 仅部分覆盖，是三条里唯一仍与其书面判据不符的一支**：已过期支 61 个网格日**不一致 0**；**未来但在窗口内支 91 个网格日、不一致 91 个**（实现取 `max(出货日,今天)`、逐字取「此时此刻」，实现恒晚 1~91 天）；真实语料对应 **24 行**（08-26 生产基线分支适用面：规则1 43／规则2 未来 24／规则2 已过期 38，合计 105）。**规则 3 ＝ 结构性已覆盖，不是碰巧对**：齐料日 ＝ `max(mat.arrivals.values())`（`baoguan.py:800`／`forecast.py:350`），有答交件与无答交件放进同一个 `arrivals` 字典后取 `max`，取更晚是这个数据结构本身的性质；两个方向各实测一次均一致。 ━━━ 🔴 **⑶ 一条比本行更新的事实，必须先端出来：规则 1 早已不止「已覆盖」，而是已在生产生效** —— §一 `#401` 已于 **2026-08-31（`OP-0831-L`）销号**：姚祖怡 08-28 单独签认（原话「☒ 认可 —— 按规则 1 改后的算法开起来。」）＋ `.51` 部署核 `CreationDate` 真刷新 ＋ 翻 `SC8_KIT_DATE_RULE1=on` ＋ `POST /api/refresh` ＋ 核数（四色 `🔴68/🟠27/🟡7/🟢1`，103 行）。⇒ **本行 2026-08-18 那句「规则 1 是全新分支、此前任何载体均无」与 §四 `#65` 同句，都是 08-18 的快照、现已过时**；规则 1 不在新变更包范围内，本次一字未动。 ━━━ 🔴 **⑷ opener 点名要求「在 design 里显式写清怎么解」的方向冲突：实测其前提已不成立，不是没写、是查过了没有可调和的东西** —— §四 `#65` 记的是「`#344` 现状取最早答交日期 vs 规则 3 取更晚，方向相反」，而 `#344` 已于 **2026-08-24** 上线按答交数量累计的取值口径、`_cumulative_confirmed_batches` 并由 `baoguan.py` 下沉到 `forecast.py`。**探针实证**：逐笔答交 `[(2026-10-20, 0.0), (2027-05-20, 10000.0)]`、需求 8000 ⇒ **现行返回 2027-05-20**（「取最早」会得 2026-10-20）⇒ **「只取最早答交日期」在当前代码里已不成立**；现行在两处都在取更晚（`_arrival_by_cumulative_qty` 的 `max(fallback, 最晚正数答交日)` ＋ 齐料日聚合 `max`），与规则 3 同向。已写进 design D2-bis 全文留痕。🔑 **留下的通用判据**：**§四 定夺行记录的是提出那一刻的现状、不是当前现状** —— 本次若照字面去「调和冲突」，会在一个已不存在的问题上改一遍取值函数，而那正是 `#344` 花一整轮判例签认才定下来的那个函数。⇒ **凡定夺行里的「现状是……」，动手前必须现测一次。** ━━━ **⑸ 本行其余残项核实（三项，均实测）**：**②「缺口 ≤0 不进缺料清单」已建**（`baoguan.py:556`，`#151`，**展示层**过滤，与其原话一致，未被顺手改成计算层）；**「上月未齐套项目跨月继续占用」口径已关闭**（归档 §一 `#150`：姚祖怡 2026-08-06 三行判例**全批「按照现状判定」**，且**无需改任何代码**）；**`#15` 真实 PMC 优先级表导入仍未接** —— `priority_resolver` 恒为 `None`，生产跑的是 2026-08-18 建的三级兜底判据（出货日 → 数量降序 → 下标）。 ⇒ **本行剩余开放面收敛为两项**：规则 2 逐字化（口径）＋ `#15` 优先级表导入（功能，本行立项时即写明「须与引擎同期设计、不可后补」）。 ━━━ **⑹ 变更包范围 ＝ 两项独立自变量、两个开关、两次上线**（design D5：`#401` 已实证「一次上线两个自变量，对照表上哪一行被哪一条搬动谁也说不清」）：**P1 规则 2 逐字化**（spec delta 落 `delivery-date-forecast`）；**P2 PMC 优先级表导入接入 `priority_resolver`**（spec delta 落 `pmc-priority-allocation`，MODIFIED 桩实现 ＋ ADDED 导入契约/fail-loud/开关）。 ━━━ 🔴 **⑺ design 里最需要他拍的一点（D3，无默认）：规则 2 逐字化会让结论更乐观，方向与 `#344`／`#401` 两轮相反** —— 那两轮的共同论证是「改完更保守、不会低估」，P1 反过来：起算点由 `max(出货日,今天)` 改为今天 ⇒ 无答交件到货日**提前**（提前量＝出货日−今天）。**实测算术影响**（假定该行瓶颈件为无答交件）：窗口内未来出货日 91 个，现行缺口恒 `+90` 天 ⇒ 四色 `🔴91`；逐字后 ⇒ `🔴86/🟡3/🟢2`。**但真实语料出货日集中在每月 20 日**，本窗口内只有三个：`2026-09-20`（齐料 2026-12-19→**2026-12-01**，提前 18 天，缺口 +90→+72）／`2026-10-20`（2027-01-18→**2026-12-01**，提前 48 天，+90→+42）／`2026-11-20`（2027-02-18→**2026-12-01**，提前 79 天，+90→+11）⇒ **齐料日明显前移 18~79 天，而四色按当前语料一格不动、三者仍全 🔴**。⚠️ **该结论三条边界已写进 design、不得当定论端给专员**：⑴ 是**算术**不是生产实测（`.51` 本机实测不可达：`Test-NetConnection 192.168.1.51:8091` ⇒ `TcpTestSucceeded=False`／`PingSucceeded=False`，真实对照表属 **LAN 留步**）；⑵ 假定瓶颈件是无答交件，若瓶颈是已答交件则规则 3 的 `max` 会让该行完全不动 ⇒ **真实影响面 ≤ 上述**；⑶「出货日集中在 20 日」取自 08-26 那份对照表的观察，`#150` 已实证预测订单会月度整批滚动、单号全变。 **三选一**：**(a) 逐字化（建议）** —— 与 §四 `#65` 答 (a) 一以贯之；代价＝24 行齐料日提前、结论变乐观、专员会看到数字明显变化。**(b) 维持现行保守读法并明确告知他** —— 代价＝存在一处**我方明知与其签认判据不符却不改**的口径，IATF 比 (a) 更难解释；且 `#401` ⑺ 当初不改的理由是**排期**（一次一个自变量）不是**方向**，用排期理由长期不改是偷换。**(c) 逐字化＋提高兜底天数抵消** —— **不推荐**，把已签认判据与未签认参数绑一次上线。 **建议 (a)**，理由：既已确立「他写下的即是签认」，那么只挑其中让结论更保守的两条执行、把更乐观的那条压着不动，**是我方在替他做业务判断**；风险控制手段应是开关形态＋对照表＋他复核，不是替他保守。 ━━━ 🔴 **⑻ 两处遗留已随本包重新承接（原承接行 `#401` 已销号，两项待办随之失去载体，属真实缺口）**：**⒜ 规则 2 的「部分覆盖」** —— `#401` ⑺ 明写「已登记为独立待办（**就在本行**）」，而本行已销号 ⇒ 本包 P1 即为其承接；**⒝「三个月内」边界开闭确认** —— `#401` ⑻ 按偏保守侧（含边界当天）落定并登记「随对照表请他以判例确认」，**实测该确认未发生**（`#402`／`采购部#19` 第六节送的是规则 1 整表，未见边界开闭单独成决策点）⇒ 本包 tasks 3.1 重新挂出，**无默认**。⚠️ **⒝ 不能在 D3 之前定**：现行取「含边界」的论证是「走规则 2 ⇒ 起算更晚 ⇒ 更保守」，**若 D3 取 (a)，该论证随之失效**，边界当天算哪边会重新变成真问题。 ━━━ **⑼ 另两项待姚祖怡答（均无默认，随各自对照表以判例形式问，本次未起草、未发送）**：边界开闭（同 ⒝）；**三级判据是否即 PMC 优先级表的最终形态** —— 他 2026-08-12 未提，本行明写「巡逻不推断」，本包同样不推断。 ━━━ **⑽ 回归与门槛**：SC8 全量基线 **509 passed / 4 skipped**（本 worktree 2026-09-02 实测，零改动状态下先取）；openspec 门槛按根 `CLAUDE.md` §5 三条自评 —— **命中①「改变全项目口径」**（规则 2 是跨场景判据、`_cumulative_confirmed_batches` 下沉后齐料日／缺口清单／物料看板三处共用）⇒ **必走 design 审**，与 opener 判断一致。 ━━━ 🔴 **⑾ 两处如实登记、本包只记不代办**：**其一** `openspec/changes/sc8-kit-date-rule1-start/` 对应的 `#401` 已于 08-31 销号却**至今未归档**（违「完工即归档纪律」）——**本包不代它归档**，归档会改 `openspec/specs/` 主 spec、与本包 spec delta 撞车，交值周巡检或原线收口；**其二** `.gitignore` 实测缺口（`git check-ignore -v`，非推断）：P2 拟落点 `…/SC8-客户订单交期智能承诺/data/priority/*.xlsx` **未被覆盖**（`data/*.json` 与 `reports/` 已覆盖）⇒ 实现阶段必须先补规则再落第一个上传件，否则每份上传件都被 sweep 判为孤儿脏文件（`#328` 子项② 同形态）。 ━━━ **⑿ 本次未做、刻意不做**：未写任何实现代码；未改规则 1／规则 3／「缺口≤0」过滤／SRM·PO 取数口径／四色分级阈值（他 08-28 另给的 🔴 晚 2 周以上／🟡 2 周以内属**显示层分级**，与本包的**起算点与占用次序**是两件事，见 §四 `#110`）；**未起草、未发送任何信** —— 姚祖怡串行闸实测 🔒 锁（`采购部#20` 在途）。 ━━━ 📌 **机器字段维持 `[S:partial][D:业]` 不变** —— 本次只增内容、不改状态；**销号判据不变**，仍是引擎侧口径与前置输入全部收敛，不是「立项了」。 📌 **取证与复跑**：`openspec/changes/sc8-atp-batch2-closeout/`（design D2 含逐条实测表）；心跳 `reports/lane-heartbeat/B-0902_46-sc8-kitdate.md`。  　━━━　➕ **同班补记（2026-09-02，commit `16d20b2` 亦已 push 同分支）**：首个提交的 design D2 把取证探针写成仓库外的 `probe_three_rules.py`，而该探针是一次性件、收工即删 ⇒ **留下一个悬空引用**。按 `#267` 的教训（审计件随 worktree 删除而永久丢失）已补落 **tracked** 取证件 `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_118_三条规则实测对位-2026-09-02.md`（方法／全部参数／逐条结果／复跑路径俱全），design D2 改指该件。🔴 **件内如实写明「探针本身已删、数字来自那次运行而非从代码推断」**，不含糊带过。`openspec validate --all --strict` 复跑仍 **127 passed / 0 failed**。⇒ **本行前文所述「commit `282853d`」应读作「分支 `claude/op0902a5-atp-propose` 上的 `282853d`＋`16d20b2` 两个提交」。**  ━━━ ✅ **2026-09-02 `OP-0902-B3`（泳道 `B-0902_60` / worktree `sc8-kitdate-2` / 分支 `claude/op0902b3-atp-literal`，commit `3e8e768` 已 push origin）：D3 已拍板，design/spec 落字完毕，🔴 本泳道到 design/spec 为止，未进实现、未部署、未触碰 `.51`、未起草任何信。** ━━━ **① D3 ＝ (a) 逐字化**（Shao Peishen 2026-09-02 裁，原话「严格按姚祖怡（男）2026-08-18 书面文本实现规则 2」）。**他已明示知悉并接受代价**：结论整体变乐观，**方向与 `#344`／`#401` 两轮的「变保守」相反**。⇒ `sc8-atp-batch2-closeout` 的 tasks 0.2 已勾、0.3（「若取 (b) 则 P1 全部取消」）作废，P1 范围确立；**§1 起的现行硬闸改为 0.1 design 审（🟡 档，未过不得 `/opsx:apply`）**。 ━━━ **② 三条缓释逐条有落点**（design 新增 **D0** 索引；🔴 缓释是裁决的一部分、不是建议）：**①退化守卫反例单测** ⇒ spec 新增 Requirement「规则 2 逐字化的退化守卫」4 条 Scenario ＋ tasks **1.2.9~1.2.12**（判据＝**把修复关掉／退回旧写法必须立刻红**；只测 happy path 不算数，它在「开关没真正接通」与「调用方漏传起算点」两种情形下同样会绿）——**本泳道不进实现，故未勾**；**②改判前后逐行对照表** ⇒ 已出件 `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_118_规则2逐字化改判前后24行对照-2026-09-02.md`；**③design 逐字引用三条规则原文＋来源日期** ⇒ 已在 design **D1**（原文一字未改，本次补标其为缓释③落点）。 ━━━ 🔴 **③ 缓释② 的结论与裁决书表述对不上，必须更正（已写进 design D3-bis）**：裁决原文写「**24 行**齐料日提前 18~79 天」，**逐行核下来会前移的是 11 行、不是 24 行** —— 另 **13 行的瓶颈件是已答交件**，规则 3 的 `max` 让它们**一天不动**；提前量 **≤18／≤48／≤79 天**（按出货日三组，且均为**上界**、非确定值）；**四色一格不动**（24 行前后全 🔴）。⇒ **实际代价小于他已接受的代价、方向一致（都是变乐观）** ⇒ **不构成需请他重判的新事实、不重判**；但**必须留痕**，否则下一个引用「24 行提前 18~79 天」的人会把一个上界当成实测值。 ⚠️ **该 11 行同样不是生产实测**：由 tracked 的 `docs/queue_344_kit_date修复前后对照-2026-08-24.md`（生产载荷）筛出 24 行 ＋ 本机离线算术推出，「瓶颈件性质」一列是由「齐料日是否**恰等于**出货日+90」**反推**的。**两处独立交叉验证已做**：⑴ 筛出的行数**恰为 24**，与 `#401` 对照表「分支适用面：规则 2（未来）24 行」逐个对上；⑵ `#401` 变动行的出货日集合最早为 `2026-12-20`，**与本 24 行零交集** ⇒ 这 24 行的齐料日自 08-24 起未被任何改动搬动过。**真实对照表仍属 LAN 留步**（tasks 1.4.3，本机 off-LAN、`.51` 不可达），且 tasks 1.4.3 已写明「真实结果与推演不符时以真实为准，并回头改写该件与 design D3-bis」。 ━━━ 🔴 **④ 连带两处论证更新（不是措辞调整，是论据失效）**：⑴ **design D4**：现行「三个月内含边界当天」原本的唯一论据是「走规则 2 ⇒ 起算更晚 ⇒ 更保守」；**D3 取 (a) 后规则 2 那一支不再更保守，该论证已失效**（原文是条件句，现改为既成事实）⇒ 现行取值从此只是「**沿用旧值、无人签认、也不再有保守性背书**」，**MUST NOT 再以「反正更保守」为由沿用**——继续引用它就是拿一个失效的理由充当签认。⑵ **design D5 不变式③「四色只变绿不变红」在缓释② 的对照表上是空过的**（24 行无一行变色）⇒ 生产语料对它**零覆盖力**，已明记「本轮空过、未获验证」，**不得因「没出现反例」记作已验证**（同 `#401` D4 那条负面结论）；tasks **1.2.12** 要求造一份跨阈值输入让它真被触发一次。 ━━━ 🔴 **⑤ 两项须姚祖怡本人答的，统一登记「待闸开后并进下一封」**（tasks 新增 §3.0 段承接）：**D4「三个月内」边界当天算哪一边**（无签认，且论据已失效见④，重要性上升，**MUST NOT 被 P1 顺手带过**）／**D6 三级判据是否即 PMC 优先级表的最终形态**（他 2026-08-12 未提，本行明写不推断）。**串行闸当前 🔒 锁（`采购部#20` 在途）⇒ 本泳道未起草任何信、不为其单独起信、不插队**；闸一开即并进下一封采购信一并问。判闸唯一入口＝`python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`，起草前**现跑**，不得引用任何文件里的闸态快照。 ━━━ **⑥ 校验**：`openspec validate --all --strict` ＝ **127 passed / 0 failed**。 ⚠️ **⑦ 两处如实留步**：⑴ 🔴 **ff 合入 master 做不了** —— 本分支基于 `claude/op0902a5-atp-propose`（B2 产出，未合入 master、且 behind master 3），`git merge-base --is-ancestor master HEAD` 实测为否 ⇒ **须先 rebase 才能入 master，属后续 CC 动作**；分支已 push origin，commit 不会随 worktree 删除而丢。⑵ opener 原文要求缓释② 落 `reports/`，但 `reports/` 被 `.gitignore:35:**/reports/` 覆盖（`git check-ignore -v` 实测），**审计件落进去会随 worktree 删除而永久丢失（`#267` 的教训）** ⇒ **正本落 tracked 的 `docs/`**，另在仓库根 `reports/` 放同名副本满足字面要求。 　━━━　✅ **2026-09-03 `OP-0903-A1`（泳道 `sc8-kitdate-3`，批 `B-0903_10`）：B3 落字后新开的三项重审已由 Shao Peishen 全部裁决 (a)，本班按裁决逐条补齐** ━━━ **① 乙-1 D3-bis ＝ (a) 采纳「不重判」**，但他同时**命更正数字**：⇒ design **D3 三处「24 行」表述就地更正** ＋ **D3-bis 改标 ✅ 已闭合**（「不重判」自此不再是本班自判、而是他的裁决）＋ 对照表新增 **§〇-bis 更正记录**。🔴 **更正来源须写明是逐行核对、不是重新估算**，否则「11」会被当成另一个估计值而与「24」并列成两说 —— 二者地位并不对等：**「24」＝ 裁决时点的估算、取自 `#401` 的分支适用面计数（＝窗口内未来支共有多少行）；「11」＝ 24 行全列逐行核（无省略、无抽样）判「现行齐料日是否恰等于出货日+90」的结果（＝其中有多少行会真的动）**。⇒ **11 是 24 的子集，24 ＝ 11 动 ＋ 13 不动，两者不矛盾、是被误当成了同一个量。** 此后凡「24」一律指**适用面**，「会前移」一律是 **11**。📌 对照表**文件名保留 `…改判前后24行对照…` 不改**：该路径已被 design D0／tasks 0.5.2／本行多处引用并已落 commit，改名会让既有引用全部悬空。 ━━━ **② 乙-2 D4 ＝ (a) 补一条边界口径进判例包、随下一封请姚祖怡签认** —— 🔴 **他明确否掉了「记录风险接受」这条路：要的是签认件，不是一条风险登记**。⇒ 落点 `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_344_判例包素材-齐料日期不看答交数量-2026-08-18.md` **附录 A**（1 条判例、2 行批改表 ＋ 反推规则填空 ＋ 影响面 ＋ 边界如实交代）。**刻意并进已有判例包、不另起一个包**：串行闸一次一封，另起一包等于多占一封信、且会让同一个取值函数的口径在两周内被分两次签。🔑 **本次把该口径量化到了可签的程度**：以 `today=2026-09-02`、边界日 `2026-12-02` 为例 —— **含边界 ⇒ 走规则 2 逐字 ⇒ 起算今天 ⇒ 齐料 2026-12-01、缺口 −1 天、🟢**；**不含边界 ⇒ 走规则 1 ⇒ 起算 2026-09-20 ⇒ 齐料 2026-12-19、缺口 +17 天、🔴**。⇒ **两支相差 18 天且四色 🟢/🔴 直接对立** —— 这就是「含边界更保守」失效的可核算证据：P1 开启后含边界不但不更保守，在边界当天反而是**最乐观的那一支**。🔴 状态 `⏳ 待姚祖怡签认`；**判据/口径类永不默认生效**，已核附录全文 4 处「默认生效」**全为禁止性表述、无一处授予**。 ━━━ **③ 乙-3 D5 ＝ (a) 补一条验证用例覆盖不变式③，补完再放行** —— 🔴 **B3 留下的 tasks 1.2.12 只是一条「要造一份跨阈值输入」的占位要求，而没有人验证过这样的输入到底存不存在**；若阈值与窗口的算术恰好使任何输入都无法跨档，那条不变式就是永远无法被触发的、而 1.2.12 会在实现阶段变成一条写不出来的用例。⇒ **本次的「补一条验证用例」＝ 把参数算定、把预期值写死**：`today=2026-09-02`／`LEAD=90`／瓶颈件为无答交件 ⇒ 后侧缺口 ＝ `90 − (出货日 − today)`、**随出货日单调递减** ⇒ 跨档输入不但存在还可精确定位。四条用例：**③-a 出货 2026-11-30 ⇒ OFF 2027-02-28(+90,🔴) → ON 2026-12-01(+1,🟡)**；**③-b 出货 2026-12-01 ⇒ OFF 2027-03-01(+90,🔴) → ON 2026-12-01(0,🟢)**；**③-c 出货 2026-11-27 反向哨兵 ⇒ ON 仍 +4、🔴**（钉住「变绿」不是无条件发生）；**③-d 人为令其后移一天 ⇒ 测试须失败**。🔑 **交叉验证**：窗口内 91 个未来出货日按此算得 **🔴86 / 🟡3 / 🟢2**，与 design D3「影响面」段**独立写下的同一组数字逐个吻合**（两处不同路径、结果一致）。🔴 **并新记一条判据：该不变式在生产语料下属「结构性测不到」，不是碰巧没测到** —— 能跨档的出货日只有 5 个（`2026-11-28`~`2026-12-02`、全在窗口末端），而真实语料的出货日**集中在每月 20 日**，「每月 20 日」这个习惯本身就把语料挡在跨档区之外 ⇒ **LAN 上重跑一次也仍然测不到**，`tasks 1.4.2` 的 LAN 自检 **MUST NOT** 被当作对它的覆盖，构造性单测（新增 **tasks 1.2.13**）是唯一出路；且下次真实对照表若又空过，**那是意料之中、不构成新情报，也同样不得记作已验证**。 ━━━ **④ 落点**：design（头部裁决落点表／D0／D3／D3-bis／D4 定夺节／D5 定夺节／Open Questions 重排）＋ spec `delivery-date-forecast` 新增 Scenario「四色跨档由构造语料真正触发」＋ tasks（新增 §0.6 三项重审登记、**1.2.13**、**3.1c**，并扩写 3.1）＋ 对照表 §〇-bis／§三-bis ＋ 判例包附录 A。**⑤ 校验**：`openspec validate --all --strict` ＝ **127 passed / 0 failed**。commit `1bf6b51`，分支 `claude/op0903a1-atp-review` 已 push origin。 ━━━ 🔴 **⑥ 两处如实留步（本班未做，也不得做）**：⑴ **「下一封致姚祖怡的信中如实说明『我方原估 24、实测 11』」** —— 这是 D3-bis 裁决明令的**第三项落地义务**（前两项已完成），但姚祖怡**串行闸实测仍 🔒 锁**（`采购部#20` 在途）⇒ 本泳道**不起草、不发送任何信**，登记为 **tasks 3.1c「待闸开后并进下一封」**；⚠️ 它**不是决策点、不占一信 ≤3 决策点的名额**，是一条**更正告知** —— 他手上那个「24 行」的说法来自我方，我方核出 11 行却不告知，等于让他按一个已知不准的数继续判断。⑵ **D4 附录 A 成稿 ≠ 已签认** —— 未签认前，现行「含边界」取值**只是事实上的沿用，不得在任何文件里被记作「已确认」**。 ━━━ 🔴 **⑦ design 审（tasks 0.1）仍未过、本班不自行勾** —— 裁决原文是「**补完再放行**」，**「补完」这一半已完成、「放行」这一半仍属 🟡 档**。⚠️ 且**放行后仍带一项未闭合的对外依赖**：D4 附录 A 须姚祖怡签认 ⇒ **tasks 1.5.4／3.1 仍是硬前置，MUST NOT 因 design 已放行而视作可翻开关**。📌 机器字段维持 `[S:partial][D:业]` 不变——本次只补齐 design 与判例素材，不改状态、不销号、不派单。  ━━━ ✅ **2026-09-03 spec 落主 specs（`OP-0903-B1`，泳道 `sc8-kitdate-4`，批 `B-0903_20`，CC）—— design 放行后的第一步，`/opsx:sync` 不归档**：**① 前一班（`OP-0903-A1`）挂的 `openspec_design_review` 已由 Shao Peishen 答 (a) 放行**，其选项**原文含「勾 tasks 0.1」** ⇒ 本班回勾 `tasks 0.1`／`0.6.4`，属**落裁决、非自审**；进度 **72 项 / 10 已勾 / 62 未勾**。🔴 **放行 ≠ 开工** —— 他同一句写明「实现属另立行、本轮不做」，且 `1.5.4`／`3.1` 两项姚祖怡签认仍是硬前置 ⇒ §1 的硬闸只是**由「design 审」换成「实现另立行的排期」，不是解除**。 ━━━ **② 🔴 opener 步骤 1 的命令名与目的句不一致，已取目的句（本批最该看的一条，且是可复用的通用判据）**：`/opsx:apply` 在本仓库的实际语义是**「实现 tasks 里的待办」**（`.claude/commands/opsx/apply.md`；`openspec` CLI 1.7.0 **无 apply 子命令**；实测 `openspec instructions apply` 返回 `progress 72/8/64` ＋ `instruction: work through pending tasks`）。能写 `openspec/specs/` 的只有两条路：`archive`（本轮 🔴 明禁——tasks 未全 [x]）与 agent 驱动的 **`/opsx:sync`**。⇒ **opener 里「`/opsx:apply` 写 spec」与「🔴 不进实现」是一对直接冲突的指令，按字面跑交付物只能落空**；取 opener 与放行原文里共同出现的目的句「**把 spec 落进 `openspec/specs/`**」为准，走 `sync` 达成。📌 **判法有先例、非本班自创**：队列 `#299`（2026-08-07）实测 FI2／FI1／`wecom-aibot-channel` 三包均有真未完项、「不得假装勾完」⇒ 同样用 `/opsx:sync`（非 archive）合入主 specs、包本身维持在办、不归档。本包同形态。🔑 **留下的通用判据**：**opener 里的命令名是手段、不是目的；手段与红线冲突时，以目的句 ＋ 仓库先例定手段，并把这一步的取舍写在明处。** ━━━ **③ 落字**：`openspec/specs/delivery-date-forecast` **＋3 条 Requirement**（规则 2 逐字化／其独立开关且默认关闭／退化守卫，含 D5 的构造性 Scenario「四色跨档由构造语料真正触发」）；`openspec/specs/pmc-priority-allocation` **改写 1 条**（「PMC 优先级占用框架（桩实现）」，桩形态降为**降级路径**）**＋新增 3 条**（优先级表导入／导入失败必须 fail-loud／接入独立开关且默认关闭）。合入后主 spec 内**零 `## ADDED/MODIFIED` 操作头**（已断言）。`openspec validate --all --strict` ＝ **127 passed / 0 failed**（sync 前后同数）。commit `e615723`，分支 `claude/op0903b1-atp-apply` 已 push origin。🔴 **基点非 master（第三次同一处偏离，非新问题）**，已核 master 自 merge-base 起对 `openspec/` 仅改 1 行（`fi2-source-inversion/design.md`，与本包无关）⇒ 基点安全；**ff 入 master 仍须先 rebase，且 `merge_to_master` 属 🟡 档，本泳道不自行拍**。 ━━━ **④ 归档判定：🔴 `暂不归档`，已用机器认得的入口（不是沉默地不归档）** —— 标记落 `tasks.md` §4.1，命中 `工具-落库sweep.py:688 STALE_CHANGE_DEFER_MARKER`。理由＝**62 项真未完**，其中 `1.5.4`／`3.1` 共用同一条串行闸。**另两条入口刻意不选、且逐条写明理由**：`预期观察窗口：N 天` 要现编一个 `N`，而等待对象是「姚祖怡签认＋闸开」、其时点**无人给过** ⇒ 由无头泳道填即等于**自造阈值**，违「判据／口径／阈值类绝不默认生效」，**不填 ＞ 乱填**；`--ack-stale-change` 语义是「复核者对过去某一刻的确认」且适用于「实质完工只差归档」，本包不是那形态、本泳道也无人可充当复核者。 ━━━ 🔴 **⑤ 一条必须端在前面的如实说明：spec 进主 specs ≠ 已实现** —— 主 spec 现在写着「规则 2 逐字化由独立开关控制且默认关闭」，**而那个开关此刻并不存在**（`sc8/config.py` 未加键、`forecast.no_feedback_start_date()` 未加分支）。**「默认关闭」与「压根没有」在行为上同为零漂移、在文字上却长得像已建成** ⇒ 读主 spec 的人 **MUST NOT** 据此认为 P1／P2 可用；本包 §1／§2 全部未勾即其唯一权威状态。该口径同 `#299` 的「spec 存在只代表 spec 层可追溯」，但**本包更进一步：这里连『曾经一致过』都不成立** —— 是先写 spec、后建造。已落 `tasks.md` §4.3。 ━━━ 🔴 **⑥ 闸态现取＝仍 🔒 锁，但卡它的信不是 `#20`** —— `工具-跟进闸查询.py --to 姚祖怡` 本日现跑：依据 **`采购部#21`**（✅ 已推送 2026-08-31 13:50 UTC，在途），下一个可用号 `采购部#22`。**结论不变**（`3.1` D4 边界签认／`3.1b` D6 最终形态／`3.1c` 24→11 更正告知，三项仍不得起信、仍待闸开后并进下一封），但本行、design、tasks、opener 多处写的「`采购部#20` 在途」**是过时快照**。🔴 **刻意不逐处改写正文里的 `#20`**：那些是各泳道当时的如实记录（历史记录不追改），且 tasks `3.0` 已明写「MUST NOT 引用快照、起草前**现跑**」——**真正的修法是永远现跑，而不是把快照改新一点**（快照改新后照样会再变旧，§四 `#65` 行已因写快照被 lint 判过一次）。已落 `tasks.md` §4.5。 ━━━ **⑦ `tasks 3.3`（本行回写）本次一并勾** —— 其要求的四条实测结论（规则 1 已上线生效／规则 3 结构性已覆盖／规则 2 为唯一残项／方向冲突前提已不成立）＋ D3 拍板 (a) ＋ 缓释② 出件且实际影响面 11 行而非 24 行，**已由 `OP-0902-B3`／`OP-0903-A1` 两批分别写进本行**（本次逐条复核在位，非重复回写）。 ━━━ 🔴 **⑧ 本泳道未做（如实登记、非遗漏）**：未进实现（§1／§2 一条未动）／未合 master／未部署、未触碰 `.51`（且本机 off-LAN，`1.4`／`2.4` 整节 LAN 留步）／未起草、未发送任何信／`tasks 3.6`（SC8 场景 CLAUDE.md 第 93~96 行过时）不在本 opener 的「做什么」四项内故未动、不自行扩范围／`pmc-priority-allocation` 主 spec 的 `## Purpose` 仍是 `TBD - created by archiving change shortage-baoguan-criteria-v3`，**刻意不改**（不属本包 delta，改它等于在口径正本上做范围外改动），登记交后续包顺手补。📌 机器字段维持 `[S:partial][D:业]` 不变——本次只落 spec 与登记，不改状态、不销号、不派单。  ━━━ 📌 **收工更正（同批次内，非新批）**：上文写的进度「72 项 / 10 已勾 / 62 未勾」是**勾 `tasks 3.3` 之前的读数**；`3.3`（§一 `#118` 回写）已于本批末一并勾完（其要求的四条结论经逐条复核**已在位**，由 `OP-0902-B3`／`OP-0903-A1` 两批分别写入），⇒ **最终进度 ＝ 72 项 / 11 已勾 / 61 未勾**，本批新勾三项＝`0.1`／`0.6.4`／`3.3`。commit 追加 `35674a6`（已 push origin）。  ━━━ ✅ **2026-09-03 已合入 master**（CC 泳道 `criteria-chain` A1 段，`OP-0903-D1`，批次 `B-0903_50`，§二 登记见 `B-0903_51_OP0903D1_两链合master_采购三包链_SC8_ATP批2收口链`）。本行的 ATP 批2 收口链共 7 个提交（`docs(SC8): 主 spec 规则2开关处加建成状态标注…` ＋ 六条 `docs(SC8): #118 …`）经 `git rebase master claude/op0903c2-spec-note` → `git merge --ff-only`（守根 `CLAUDE.md` §5 ff-only）合入并 `git push origin master`，master 头 ＝ `209fde4`，反查 `git rev-list --count master..claude/op0903c2-spec-note` ＝ **0**、`origin/master..master` ＝ **0**。🔴 **合入依据**：`merge_to_master` 本属 🟡 档，本次不 pause 的唯一依据是 Shao Peishen 2026-09-03 答 **G-6=(a)「合入照原意做，含连带」**（他已被告知本链连带 6 个提交后作答）。合入前按 commit **message** 逐条比对（🔴 不认 hash）＝ 7 个、与看护件 `B-0903_50` §四 D1 清单全等，无表外提交；🔴 **严格排在采购三包链之后合**（否则本链 rebase 基点是错的）；rebase **零冲突**（7/7）。合后 `openspec validate --all --strict` ＝ **134 passed ／ 0 failed**（⚠️ 该链在自己分支上测得的 127 已失效，勿再引用旧数）。🔴 **本次只做合入，不改变本行任何既有结论**：`openspec/changes/sc8-atp-batch2-closeout/` 按原意**不归档**、留在在途包内，未进实现、未填任何未签认判据。分支 `claude/op0903c2-spec-note` 按 opener **未删除**，保留追溯。

---

# §一 #118 · 外置前整格原文（2026-09-04 外置，3 段，md5:99f48320）

[S:partial][D:业] **🟡 部分完成（①+④首版，2026-07-29，CC）**：姚祖怡二次举证真实案例（S02Y.0210 三行 600/400/500 套争用现货 1153，"必须逐级扣料"），Paul **07-29 当面拍板对①+④这一窄片跳过判例包直接建造**（原话"就先用出货日期早晚当临时优先级……现在就动手做逐级扣减"）——**①现货净额−优先级在前项目已占用量 + ④跨期依次扣减，已建造+全量回归零漂移+真实部署`.51:8091`+真实数据验证通过**（`_allocate_sequential_inventory`/`_resolve_priority_order`，临时优先级=出货日期升序，`priority_resolver` 接口已就绪待 #15 真实数据接入即自动切换）。真实验证过程中发现并修复自身引入的 bug（真实 FO 文档下同一 so_id 可对应多个行项，首版按 so_id 去重导致跨行竞争检测失效，已改按下标定位修复，SC8 回归 276 passed+4 skip）。详见 SC8 场景 CLAUDE.md 2026-07-29 行。**②缺口≤0 不进缺料清单、③答交数量累计**（已在 07-28 批2 `_cumulative_confirmed_batches` 实现，非本次）、**#15 真实 PMC 优先级表导入 UI、"上月未齐套继续占用"的显式跨月建模**——仍待判例包/排期，未做。**🔄 2026-07-29 业务总线改判剩余三项归属（收窄判例包范围，省一轮往返）**：**(a) ②缺口≤0 不进缺料清单 → 改判为「口径已显式签认，可直接建，不需判例包」**——姚祖怡 07-26 V6 回件原文已逐字写死：「缺口数量126pcs（取值=本项目需求数量-可用现货数量，**缺口数量如≤0，则该行视为满足齐套需求，不应该体现在缺料子件中**）」。这与 #18-a/b/c 属同一情形（专员本人书面明示 → 构成显式签认 → C 类限制解除），**再发判例包去问一个他已经回答过的问题，正是判例批改法要消灭的浪费**。→ 并入下一个 SC8 批次直接实现（注意实现时确认过滤发生在"展示层"还是"计算层"：他的原话是"不应该体现在缺料子件中"＝展示层过滤，**不得顺手把它改成计算层剔除**，否则会影响齐套数量本身）。**(b) #15 真实 PMC 优先级表导入 UI → 属功能排期，非口径**——他早前已给过导入表结构（数字越小优先级越高），`priority_resolver` 接口已就绪，接上即自动替换当前"出货日期升序"临时口径。**(c) 「上月未齐套项目继续占用」的跨月显式建模 → 这一项才是真正仍需口径确认的唯一残项**，判例包只需覆盖它一项（一信 ≤3 决策点绰绰有余）。**故本行剩余开放口径点：3 项 → 1 项。****回复姚祖怡信已合并 #147 徽标修复一并发送**（`✅ 已推送 2026-07-29 06:14 UTC`，Paul 聊天内"确认"后发出，md+docx 双格式，README 已回填） ━━━ ✅ **2026-09-03 已合入 master**（CC 泳道 `criteria-chain` A1 段，`OP-0903-D1`，批次 `B-0903_50`，§二 登记见 `B-0903_51_OP0903D1_两链合master_采购三包链_SC8_ATP批2收口链`）。本行的 ATP 批2 收口链共 7 个提交（`docs(SC8): 主 spec 规则2开关处加建成状态标注…` ＋ 六条 `docs(SC8): #118 …`）经 `git rebase master claude/op0903c2-spec-note` → `git merge --ff-only`（守根 `CLAUDE.md` §5 ff-only）合入并 `git push origin master`，master 头 ＝ `209fde4`，反查 `git rev-list --count master..claude/op0903c2-spec-note` ＝ **0**、`origin/master..master` ＝ **0**。🔴 **合入依据**：`merge_to_master` 本属 🟡 档，本次不 pause 的唯一依据是 Shao Peishen 2026-09-03 答 **G-6=(a)「合入照原意做，含连带」**（他已被告知本链连带 6 个提交后作答）。合入前按 commit **message** 逐条比对（🔴 不认 hash）＝ 7 个、与看护件 `B-0903_50` §四 D1 清单全等，无表外提交；🔴 **严格排在采购三包链之后合**（否则本链 rebase 基点是错的）；rebase **零冲突**（7/7）。合后 `openspec validate --all --strict` ＝ **134 passed ／ 0 failed**（⚠️ 该链在自己分支上测得的 127 已失效，勿再引用旧数）。🔴 **本次只做合入，不改变本行任何既有结论**：`openspec/changes/sc8-atp-batch2-closeout/` 按原意**不归档**、留在在途包内，未进实现、未填任何未签认判据。分支 `claude/op0903c2-spec-note` 按 opener **未删除**，保留追溯。 ━━━ 📎 历史回写 44 段已于 2026-09-04 外置（K2 口径，原文原样 md5:18a416c7）见 `1-转型规划/0-全景路线图/队列行日志/#118.md`；本格只留首 1 段＋末 1 段
