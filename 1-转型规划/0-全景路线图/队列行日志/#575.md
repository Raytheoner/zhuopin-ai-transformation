---
title: "队列行日志 #575（§一）"
created: 2026-09-13
status: 生效
用途: 跨桌任务队列 §一 #575 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #575 · 外置段（2026-09-13 外置，工具第 1 批，4 段，原文原样）

> 外置人 CC-OP-0913-S；外置前整格 6028 B、共 6 段；本批外置第 2、3、4、5 段；本格保留第 1、6 段。

🆕 **2026-09-13 立行**。**甲乙合一行、不拆两号**：前两项与后两项同一因果（同一个空耗、同一批消费者），**故 WIP 只增 1**；押后不是拖延，是**硬顺序**——配套支未合之前动批处理工具必撞。

━━━

🔴 **为什么先甲后乙**：甲零依赖、当天可止血，且顺带把「运行不可计数」这个更隐蔽的病一起治了；乙才是根治，但它的前置今天做不完。**判别一句：先让空转不花模型，再让空转根本不发生。**

━━━

🔴 **乙落地时必须先证覆盖面是超集**：事件触发覆盖「正常收工」，覆盖不了「批崩了没写 exit.txt」；轮询虽贵但「傻等也一定会来」。兜底心跳不是可选项，是超集成立的必要条件。

━━━

🟡 **2026-09-13 甲已建、行不销（乙未起）**（`OP-0913-Q` 无头泳道，批 `B-0913_轮询守`，分支 `claude/op0913q-poll-guard-575` @ 37a51d8 已 push、🟡 待他一字母合入）。⓵⓶ 落点：新建 `0-学习与工具/工具-轮询守.ps1`（每轮跑探针 `--via-scheduled-task` ＋巡检，只在有信号时 stdin 喂章程＋两命令输出起一次 `claude -p`，白名单只读；每轮无条件追加一行 JSONL 到 `reports/poll-guard/poll-guard-<日>.jsonl` 并回读末行核对，行内写明不入库只计数；非零退出／超时／无标记一律判信号且 stderr 进 prompt；Global 互斥防叠轮；`-DryRun` 零副作用）＋ `test_工具-轮询守.py` 11 条＋ `工具-注册轮询守计划任务.ps1`／`run-poll-guard-hidden.vbs`（任务 `ZhuopinPollGuard` 每 15 分钟、S4U、隐藏窗口、绝对路径烘焙进 gitignored 的 `run-poll-guard.ps1`；`-WhatIf` 免提权、`-Unregister` 回滚）。🔴 **判据一处如实偏离字面**：期望产出写「巡检非 `[NO-ACTION]`」，巡检真身登记册为空时打 `[NO-PENDING]`（章程把两者并列为空跑）——照字面每轮都会唤模型、正是病一，故静默集合＝`WL-NO-ACTION／WL-OFF／NO-PENDING／NO-ACTION／WT-NO-ACTION` 五个字面，其余（含 `WAITING`／`MERGED`／`WT-BLOCKED`／`DRY`／未知）均判信号；探针静默只认 `NO-SIGNAL`。改集合＝改口径判据（🟡），单测锁字面。**验证手段**：单测 11 passed（5 次重跑）；真仓 `-DryRun` 两次走通；真 `claude.exe` 集成一跑发现 `--allowedTools` 逗号串被按空格拆、已修锁测（01962fe）；端到端 `wscript→VBS→5.1→pwsh→轮询守 -DryRun` 走通。**零回归（差集口径）**：`0-学习与工具` 全量 2603 条，分支 vs master 7a90ef5 detached，基线 29 failed/2562 passed、分支 29 failed/2573 passed，失败集 md5 `0f5c4c5a` 相同、`comm` 双向皆空（+11＝本批新增）。未碰 v2／sweep／合入链路三脚本；未注册任务、未动 Cowork 桌面端任务。**留步**：⑴ 注册须提权 shell（`UPS5:6` 唯二可交之一），先 ff 本支再 `pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-注册轮询守计划任务.ps1"`；⑵ 停 Cowork 桌面端 `poll-opener-batch` 归他手动，并存期安全（探针有「已报」态、巡检幂等）；⑶ 尾巴：`[WT-BLOCKED]`／`[WAITING]` 持续态会每轮唤模型直到清掉——按「不另造判据」未做去重，乙落地时一并看。
