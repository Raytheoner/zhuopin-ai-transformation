---
title: "队列行日志 #575（§一）"
created: 2026-09-13
status: 生效
用途: 跨桌任务队列 §一 #575 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #575 · 外置段（2026-09-13 外置，工具第 1 批，4 段，原文原样）

> 外置人 CC-OP-0913-S；外置前整格 6028 B、共 6 段；本批外置第 2、3、4、5 段；本格保留第 1、6 段。

🆕 **2026-09-13 立行**。**甲乙合一行、不拆两号**：前两项与后两项同一因果（同一个空耗、同一批消费者），**故 WIP 只增 1**；押后不是拖延，是**硬顺序**——配套支未合之前动批处理工具必撞。

━━━

🔴 **为什么先甲后乙**：甲零依赖、当天可止血，且顺带把「运行不可计数」这个更隐蔽的病一起治了；乙才是根治，但它的前置今天做不完。**判别一句：先让空转不花模型，再让空转根本不发生。**

━━━

🔴 **乙落地时必须先证覆盖面是超集**：事件触发覆盖「正常收工」，覆盖不了「批崩了没写 exit.txt」；轮询虽贵但「傻等也一定会来」。兜底心跳不是可选项，是超集成立的必要条件。

━━━

🟡 **2026-09-13 甲已建、行不销（乙未起）**（`OP-0913-Q` 无头泳道，批 `B-0913_轮询守`，分支 `claude/op0913q-poll-guard-575` @ 37a51d8 已 push、🟡 待他一字母合入）。⓵⓶ 落点：新建 `0-学习与工具/工具-轮询守.ps1`（每轮跑探针 `--via-scheduled-task` ＋巡检，只在有信号时 stdin 喂章程＋两命令输出起一次 `claude -p`，白名单只读；每轮无条件追加一行 JSONL 到 `reports/poll-guard/poll-guard-<日>.jsonl` 并回读末行核对，行内写明不入库只计数；非零退出／超时／无标记一律判信号且 stderr 进 prompt；Global 互斥防叠轮；`-DryRun` 零副作用）＋ `test_工具-轮询守.py` 11 条＋ `工具-注册轮询守计划任务.ps1`／`run-poll-guard-hidden.vbs`（任务 `ZhuopinPollGuard` 每 15 分钟、S4U、隐藏窗口、绝对路径烘焙进 gitignored 的 `run-poll-guard.ps1`；`-WhatIf` 免提权、`-Unregister` 回滚）。🔴 **判据一处如实偏离字面**：期望产出写「巡检非 `[NO-ACTION]`」，巡检真身登记册为空时打 `[NO-PENDING]`（章程把两者并列为空跑）——照字面每轮都会唤模型、正是病一，故静默集合＝`WL-NO-ACTION／WL-OFF／NO-PENDING／NO-ACTION／WT-NO-ACTION` 五个字面，其余（含 `WAITING`／`MERGED`／`WT-BLOCKED`／`DRY`／未知）均判信号；探针静默只认 `NO-SIGNAL`。改集合＝改口径判据（🟡），单测锁字面。**验证手段**：单测 11 passed（5 次重跑）；真仓 `-DryRun` 两次走通；真 `claude.exe` 集成一跑发现 `--allowedTools` 逗号串被按空格拆、已修锁测（01962fe）；端到端 `wscript→VBS→5.1→pwsh→轮询守 -DryRun` 走通。**零回归（差集口径）**：`0-学习与工具` 全量 2603 条，分支 vs master 7a90ef5 detached，基线 29 failed/2562 passed、分支 29 failed/2573 passed，失败集 md5 `0f5c4c5a` 相同、`comm` 双向皆空（+11＝本批新增）。未碰 v2／sweep／合入链路三脚本；未注册任务、未动 Cowork 桌面端任务。**留步**：⑴ 注册须提权 shell（`UPS5:6` 唯二可交之一），先 ff 本支再 `pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-注册轮询守计划任务.ps1"`；⑵ 停 Cowork 桌面端 `poll-opener-batch` 归他手动，并存期安全（探针有「已报」态、巡检幂等）；⑶ 尾巴：`[WT-BLOCKED]`／`[WAITING]` 持续态会每轮唤模型直到清掉——按「不另造判据」未做去重，乙落地时一并看。

---

# §一 #575 · 外置段（2026-09-17 外置，工具第 2 批，1 段，原文原样）

> 外置人 OP-0917-E；外置前整格 4644 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

🟡 **2026-09-13 甲实机三修已完成、行仍不销（乙未起）**（`OP-0913-S` 无头泳道，批 `B-0913_轮询守实机三修`，分支 `claude/op0913s-pollguard-fix3-575` @ e0cb2de 已 push、🟡 待他一字母合入）。三处缺陷按看护件「不重排查、直接修」落点：⓵ **缺陷一**：`工具-注册轮询守计划任务.ps1` 撤「优先烘 WindowsApps 别名」（0 字节 ReparsePoint、S4U 解析不了＝根因），pwsh 改取 `(Get-Process -Id $PID).Path` 真身；新增 `Assert-RealExecutable`，pwsh／python／git／claude／node 一律 fail-loud（`Length -eq 0` 或 `ReparsePoint` ⇒ 退 1、不生成包装、不注册）；包装起手校验 pwsh 存在，缺则留 `wrapper_error` 痕＋`exit 9`。⓶ **缺陷二**：VBS 改 `rc = objShell.Run(...)`＋`WScript.Quit rc`，`LastTaskResult` 不再恒 0。⓷ **缺陷三**：`工具-轮询守.ps1` 判据 ④ `$PatrolStickyMarkers=@('WT-BLOCKED')`，照抄巡检去重形状：按「标记＋内容集合」比对，集合落 `reports/poll-guard/sticky-markers.json`（不入库、绝不落 合入登记/），同集合再现 ⇒ `quiet-sticky:WT-BLOCKED` 不唤、留痕照写；多／少／换一个或消失再现 ⇒ 唤；异常判定不碰；`-DryRun` 不落状态；静默五字面未动。**手段**：`pytest 0-学习与工具/test_工具-轮询守.py` ⇒ **29 passed 1 skipped**（11 旧＋18 新，含本机真别名活体样本、`cscript` 起 VBS 退出码 7/0）；差集回归按触碰面收窄（全库仅此测试文件引用四件，master 侧 11 条全绿 ⇒ 分支失败集 ∅），未跑全量。未碰 sweep／合入链路／v2；未注册任务；未清 worktree（活体样本仍在）。**留步**：⑴ 🟡 待 ff；⑵ ff 后须**提权** pwsh 重跑 `pwsh -NoProfile -ExecutionPolicy Bypass -File "0-学习与工具\工具-注册轮询守计划任务.ps1"`（注册归人）；⑶ 重注册后预期：首轮 `total_ms` 回到 ~90 s、次轮起 `patrol.verdict=quiet-sticky:WT-BLOCKED` 且 `woke=false`；若仍 1 秒返回，`LastTaskResult` 会非零、不再假报成功；⑷ `[WAITING]` 仍按信号（事件非常驻，未纳入去重）。――― 🔴 **三天实机：③ 的预期被证伪**（2026-09-16 08:0x 现取，`OP-0913-E`）：他已提权重跑注册（包装 `run-poll-guard.ps1` 生于 **09-14 07:27**、已烘 pwsh 真身路径―**缺陷一已修，症状照旧**）。留痕：09-14 仅 **2 行**（08:06、08:12）、09-15 仅 **2 行**（14:06、14:12），其余全无；且这 2 轮都落在开机／登录后头 ~6 分钟内。Operational 日志：09-15 14:12 之后每 15 分钟照跑，**轮轮 200→201 相差 1 秒、`LastTaskResult=0`、零留痕**。一天 ~96 轮只有 2 轮真跑。🔴 ③ 写的「若仍 1 秒，LastTaskResult 会非零」**没兑现**。同族第三个洞（新、无承接）：包装脚本末行 `exit $LASTEXITCODE`——原生命令**没启动**时 `$LASTEXITCODE` 为 `$null`，`exit $null` 即 **exit 0**，VBS 那层的透传被这一层吃掉。修法在生成器模板里一行：取不到退出码即当失败、写一行痕并 exit 非零。**先把它吵起来，再猜根因。** 🔴 Cowork 桌面端 15 分钟任务仍不得停。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **4 段已于 2026-09-13 外置**（K2 口径，原文原样、可 grep，文件 md5:a144bbb5，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#575.md`；本格只留原第 1、6 段（共 6 段）＋本指针（外置人 CC-OP-0913-S）。

---

# §一 #575 · 外置段（2026-09-17 外置，工具第 3 批，3 段，原文原样）

> 外置人 OP-0917-E；外置前整格 4293 B、共 6 段；本批外置第 3、4、5 段；本格保留第 1、6 段。

✅ **修法已落分支（2026-09-17，他答丙c）**：桌面端 15 分钟任务已由他重新 Active（保底那条腿）；包装脚本的修法在生成器模板里改完，跑 `-WhatIf` 核过生成内容：取不到退出码即写一行痕并 **exit 8**。分支 `claude/op0917a-wt-del-fallback-576` @ `00cf953`（与 `#576` 同包），已 push、未 ff，🟡 待他一字母。🔴 ff 后他需**提权重跑一次注册脚本**才能把新模板烘进包装脚本；验收判据改为：它要么写出真留痕，要么 `LastTaskResult` 变 8——**不允许再出现 1 秒返回 0 且零留痕**。

━━━

✅ **2026-09-17 修法已入 master `edd2957`**（他答丁a）。🔴 **下一步归他：提权重跑一次 `工具-注册轮询守计划任务.ps1`**，新模板才会烘进包装脚本。验收判据：要么当日 `reports/poll-guard/*.jsonl` 出真留痕，要么 `LastTaskResult` 变 **8**——**不允许再出现「1 秒返回 0 且零留痕」**。在这条过之前，桌面端 15 分钟任务保持 Active。

━━━

🔴 **2026-09-17 08:25 首轮真跑验收：闸响了，但本行不销**。三条判据全取：① `LastTaskResult=8`（新闸生效）；② Operational 08:25:36→08:25:37 **仍是 1 秒**；③ `poll-guard-20260917.jsonl` 新增一行 `{"round":"wrapper-nostart","skipped":"pwsh-not-started"}`。⇒ **哑掉的失败现在会叫了，但 pwsh 在 S4U 下依旧起不来**；`#575` 的目标（轮询守真跑）未达成。🔴 **桌面端 15 分钟任务不得停。**➕ **证伪一条本方猜想**：曾疑 WindowsApps 目录 ACL 拦下了执行。现取 `Microsoft.PowerShell_7.6.6.0_x64__8wekyb3d8bbwe\pwsh.exe` 的 ACL：`BUILTIN\Users` 有 **ReadAndExecute**。**文件 ACL 不是拦路者。** 剩下的假说是「Store 封装应用在无交互登录的 S4U 上下文里无法完成包激活」——**未证实，不当结论用**。现取本机**没有**非 Store 版 pwsh（`C:\Program Files\PowerShell\7\pwsh.exe` 不存在）。待他定夺下一步（选项见当轮汇报）。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-17 外置**（K2 口径，原文原样、可 grep，文件 md5:d698bea4，工具第 2 批）见 `1-转型规划/0-全景路线图/队列行日志/#575.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 OP-0917-E）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。
