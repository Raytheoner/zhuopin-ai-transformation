---
title: "队列行日志 #394（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #394 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #394 · 外置前整格原文（2026-09-04 外置，8 段）

[S:partial][D:业] 🆕 **中继已建成；🟢 2026-08-31 首条真实送达已发生（详见本行末尾 `OP-0831-L` 记载）—— 仍不销号，销号判据（姚祖怡本人在群里看到）待其自身确认**（2026-08-26，CC `op0826s-a3-outbox-relay`，commit `367b883` 已 ff 入 master，openspec 变更包 `aibot-outbox-relay`，`OP-0826-S`）。**建法**＝按 `fi2-source-inversion` design 决策点 8.2 ⑵ 已批形态：新增 `aibot_service/outbox_relay.py`（扫描/目标解析/代发/回写/常驻轮询）＋ `scripts/check_outbox_relay.py`（**只读体检，刻意没有发送路径**）＋ `scripts/run_aibot_service.py` 接线；`connection.py`／`delivery.py`／`group_notify.py`／两张映射表／`sc2/` **一行未改**。 🔴 **中继跑在常驻服务已经握着的那条连接上，零新增连接** —— 刻意不照 `dispatch_followup_letters.py` 那个「独立计划任务 ＋ 自建连接」的既有范式：那对**每天一次**的批处理才成立，5 分钟一轮 ＝ 每天 288 次拿单实例约束赌博（同 BotID 多处长连接互相踢线，2026-08-06 已真实复现 3 次）。 **本行三条硬约束的落点**：**①「不得再抄一份 chatid」用对源码本身的断言钉住**（中继源码内不得出现真实 chatid 字面量、且必须 import 那张权威 yaml 的加载函数）；**②「投递成功才置 `delivered:true`」正反两面各配用例**，且「算不算成功」直接复用 `delivery._assert_ack_accepted`（`#326` 那条纵深防御）**不另写第二套** —— 「一次发送算不算被企微接受」全仓只应有一个答案；**③ 首条真实送达的人眼反查代码做不了**，故把实际发出的 chatid 写进审计与记录（`delivered_to`），使那次反查**有据可对**。 🔑 **O-10 有结论，且比原设想强一档，请 Shao Peishen 追认**：本行原文设想「合建一份、按 `scenario` 字段**分流**」—— 实现下来发现**分流根本不需要**：决定「这条怎么发」的四个字段（`channel`／目标／`msgtype`／`text`）全部与场景无关，SC2 与 FI2 走**逐字节相同**的路径，`scenario` 只进留痕。⇒ **新场景接入不改中继一行代码。** 已配源码断言禁止出现 `== "SC2"` 一类分支 —— 有人哪天写了，「合建一份」当天名存实亡**且不会有任何测试变红**。 **三条取舍（都进了单测）**：**⑴ 宁可重发不可静默丢** —— 「发送成功」与「标记 delivered」不可能原子，故**发一条标一条**把窗口压到最小；标记走**乐观并发校验**（记住物理行号 ＋ 原文，逐字相等才替换），校验不过 ⇒ 记 `outbox_relay_mark_failed` ＋ 告警，**绝不强行覆盖**（会抹掉写侧刚落的行）、**绝不假装标成功**（那条会再也不被重发）。**⑵ 🔴 outbox 读不到 MUST NOT 被读成「没有待发」** —— `.51` → 笔记本那条文件通路断掉的表象就是「文件不见了」，回落成空列表就会安静地一条不发、与一切正常长得一模一样（同 §四 `#59` 断供 5 个工作日无人察觉的形态）；故读失败一律上抛并**每轮都告警**（不去重）。**⑶ 结构性投不出去的记录留在 outbox 不丢弃** —— `pending()` 数字下不去**正是要的**，它是这条记录还没被人处理的唯一外部信号；同一「文件/行/原因」本进程内只告警一次（审计每轮照记：审计给事后复核用不怕多，告警给活人看怕多）。 **另一条防线**：未配置 `WECOM_AIBOT_OUTBOX_PATHS` ⇒ 中继关闭，但**「关着」也留痕**（`outbox_relay_disabled` 与 `outbox_relay_started` 两条可区分事件 ＋ 各打印一行）—— 「中继压根没启动」与「启动了但没消息」在日志里本来长得一模一样。 **回归**：服务 **606 passed／1 skipped**（基线 569 collected，新增 38，零漂移）／平台 **380 passed／1 skipped**／openspec **117 passed 0 failed**。**真身取证（非单测）**：对**真实** `department_group_chatid_mapping.yaml` 跑只读体检，采购部解析出真实 chatid、`PMC部` 报「不在映射表」、缺文件报「读不到 ≠ 没有待发」，退出码 1 —— 三档行为逐条对上。⚠️ **CI 整体 run 长期 failure（§一 `#398` ⑶）⇒ 上述红绿信号一律本地实跑取证，不以 CI 绿为准。** ━━━ 🔴 **未闭合，不得读成「SC2 已通」** ━━━ **⑴ L1 LAN 留步**：`WECOM_AIBOT_OUTBOX_PATHS` **未配置** —— `.51` → 笔记本的文件通路本机当前 off-LAN、**从未实测**（`fi2-source-inversion` design 探测项 **P6**）⇒ 常驻服务此刻中继**关着**。**刻意不给路径默认值**：猜一个写进代码只会得到一份「看起来配好了、其实一直读不到」的配置，而读不到的表象恰恰是「没有待发消息」。 **⑵ L2 无人在场留步**：首条真实送达 ＋ 人眼反查收件群未做（本次为无头批处理会话，对外真实消息一律留步不发）。⚠️ **与本服务「永不部署 `.51`」的既有拍板无关** —— aibot 通道走公网、不属 LAN 依赖；留步原因只有一个：**它会真的发一条消息到一个真人在的群里。** **⑶ ⇒ 本行销号判据（＝姚祖怡在群里真的看到了那条周报）未满足，故本次只回写进展、不销号**；openspec 变更包同理**暂不归档**（`暂不归档` 已写进 tasks.md 抬头）。 ━━━ **以下为本行原状态，保留原文** ━━━ [S:open][D:业] 🆕 未开工。**它是 SC2 自动推群这条链路上唯一未通的一环**，也是 `采购部#19` 跟进信发送硬前置之一（§5 第 8 步要求端到端复现，推送这一环不通即无法复现） 　━━━ 🔎 **2026-08-27 `OP-0827-E`：⑴ L1 的 P6 探测项已实测，结论是「通路不通，但不是不可达」** —— 本班 on-LAN，按授权只读核验、**未配置任何环境变量、未改 `.51` 任何东西**。**三步实测**：① `.51` 的 SC2 落盘目录实为 `C:\sc2\app\reports\`（`C:\sc2\reports` **不存在**，指错会得到一个「目录是空的」的假象）；`OP-0827-B` 本日 `--no-push` 跑出的 `sc2_weekly_2026-W34.json` 在其中（17,027 B，2026-08-27 10:52:34 本地）；`sc2_group_outbox.jsonl` 在 `C:`／`D:` 全盘**尚不存在**（`--no-push` 不写 outbox，符合预期）。② `.51` 对外 SMB 共享＝`ADMIN$`／`C$`／`D$`／`IPC$`／`airead`（→`D:\airead`），**没有 SC2 专用共享**。③ 🔴 **笔记本侧读不到**：`Test-NetConnection 192.168.100.51 -Port 445` ＝ `TcpTestSucceeded=True`（**网络是通的**），而 `net view \\192.168.100.51` ＝ **System error 5（拒绝访问）**、`dir \\192.168.100.51\airead` ＝ **「用户名或密码不正确」** ⇒ **真因是笔记本本地账户 `spsthinkpadwin\paul shao` 在 `.51` 上没有凭据**，不是网络不可达。 ⚠️ **顺带记一条会骗人的判据**：`Test-Path` 与 `Get-ChildItem` 对 `\\192.168.100.51\C$\...` 这种情形返回的是**「路径不存在」**（`ItemNotFoundException`）—— **它把「无凭据」显示成了「东西不在那儿」**，照它排查会一路查错方向（同「工具静默回退」族）。**判无凭据要看 `net view`／`net use` 的真实错误码（5／1326），不能用 `Test-Path`。** 🔴 **⇒ 由此派生一个原设计里没有的决策点**：`WECOM_AIBOT_OUTBOX_PATHS` **不能简单填一个 `\\192.168.100.51\C$\...` 了事** —— 三条路各有代价：⒜ 笔记本上存 `.51` 凭据（`cmdkey`，最快，但把一份长期凭据放进笔记本）；⒝ 在 `.51` 上开一个带权限的专用共享、不走管理共享（最干净，须一次 `.51` 写操作）；⒞ 反过来由 `.51` 侧把 outbox 推到笔记本（免凭据，方向反转，但要在 `.51` 上加一个推送动作）。**本班不替它选，登记待定夺。** ⑵ L2（首条真实送达）本班未做 —— 属对外发送，一律留步。 ━━━ 🟢 **`OP-0831-L` 首跑灰度批波次2（写）已收口，首条真实送达已发生（2026-08-31）** ━━━ 通路＝Windows 凭据管理器 GUI 存 `192.168.100.51／Administrator`（cmdkey CLI 因 bash/PowerShell/终端多层转义反复失败——先后踩中占位符尖括号残留、PowerShell 7 原生命令参数拆分疑似换行粘贴污染两类坑，改走图形界面表单输入一次成功，密码全程不经过 CC）；UNC 走既有管理共享 `C$`，**未新开专用共享、未改 `.51` 任何配置**（2026-08-27 悬而未决三选一之外找到的第四条路）。快照点＝`.env` 改动前原文双备份（`~/.claude/env-backups/platform-aibot.env.2026-08-31.pre-op0831l-outbox-paths.bak` ＋ 会话 scratchpad 同名件），SHA256 一致（`29bd2fe6…`）。🔴 **实际生产 `.env` 定位澄清（经 `Get-ScheduledTask` 核实、非猜测）**：不在主工作区，在专属 worktree `C:\Dev\zhuopin-ai\.claude\worktrees\wecom-service-home\5-平台底座\.env`（`ZhuopinAibotDevListener` 计划任务 `WorkingDirectory` 实测所在）；`run_aibot_service.py` 走的是 `load_dotenv(SERVICE_DIR.parent/.env)` 定位，不经 `env_anchor`。写入＝新增一行 `WECOM_AIBOT_OUTBOX_PATHS=\\192.168.100.51\C$\sc2\app\reports\sc2_group_outbox.jsonl`。重启＝`Stop-ScheduledTask`+`Start-ScheduledTask`，🔴 实测复现《.51部署标准清单》§七「孤儿进程」条款——`Stop-ScheduledTask` 未级联杀掉旧 PID 16820，已显式 `Stop-Process -Force` 后再启动，新 PID 30812／CreationDate 2026-08-31 14:20:26 核实为真重启（非「脚本打印重启但进程未换」假象）。冒烟三件套全过：⑴ 通路读通（`Get-Content` 直读 UNC 路径，内容与 SSH 只读交叉核验一致）；⑵ `audit` 见 `outbox_relay_started`（非 `_disabled`），`paths` 字段与配置逐字一致；⑶ 端到端——**未新写测试记录，径直投递了 `.51` 上已存在的 2 条真实待发记录**（2026-08-28 20:01 生成的 2026-W34 周报，Shao Peishen 2026-08-31 明确拍板「让它们真发」，非无人在场留步）：`outbox_relay_delivered` × 2，`ack.errcode=0／errmsg=ok`（群聊 `channel=aibot_group_chatid→wrvDL_DAAAH7IL6I7hsgszeyQvmyjw-Q`；私发 `channel=aibot_direct→YaoZuYi`），回读 `.51` 源文件确认两条均已 `delivered:true`（乐观并发校验通过，不会被重发）。**硬约束③（人眼反查）**＝Shao Peishen 本人核实采购部群收到、chatid 无投错。回滚＝冒烟一次性全过，未触发 SOP。 🔴 **精确澄清、避免读成已 100% 销号**：本行原销号判据文本是「姚祖怡在群里真的看到了那条周报」——本次验证到的是「投递成功且群/chatid 经 Shao Peishen 目视核对无误」，**姚祖怡本人是否已亲眼看到/阅读该条尚待其自身确认**，两者不完全等同，如实标注、不代其确认，留给自然发生或另行核实。O-10（合建一份免分流）待追认项本次未触碰，仍按原文悬置。

---

# §一 #394 · 整格原文（2026-09-04 人工重写前外置，md5:082bc51c）

[S:partial][D:业] 🆕 **中继已建成；🟢 2026-08-31 首条真实送达已发生（详见本行末尾 `OP-0831-L` 记载）—— 仍不销号，销号判据（姚祖怡本人在群里看到）待其自身确认**（2026-08-26，CC `op0826s-a3-outbox-relay`，commit `367b883` 已 ff 入 master，openspec 变更包 `aibot-outbox-relay`，`OP-0826-S`）。**建法**＝按 `fi2-source-inversion` design 决策点 8.2 ⑵ 已批形态：新增 `aibot_service/outbox_relay.py`（扫描/目标解析/代发/回写/常驻轮询）＋ `scripts/check_outbox_relay.py`（**只读体检，刻意没有发送路径**）＋ `scripts/run_aibot_service.py` 接线；`connection.py`／`delivery.py`／`group_notify.py`／两张映射表／`sc2/` **一行未改**。 🔴 **中继跑在常驻服务已经握着的那条连接上，零新增连接** —— 刻意不照 `dispatch_followup_letters.py` 那个「独立计划任务 ＋ 自建连接」的既有范式：那对**每天一次**的批处理才成立，5 分钟一轮 ＝ 每天 288 次拿单实例约束赌博（同 BotID 多处长连接互相踢线，2026-08-06 已真实复现 3 次）。 **本行三条硬约束的落点**：**①「不得再抄一份 chatid」用对源码本身的断言钉住**（中继源码内不得出现真实 chatid 字面量、且必须 import 那张权威 yaml 的加载函数）；**②「投递成功才置 `delivered:true`」正反两面各配用例**，且「算不算成功」直接复用 `delivery._assert_ack_accepted`（`#326` 那条纵深防御）**不另写第二套** —— 「一次发送算不算被企微接受」全仓只应有一个答案；**③ 首条真实送达的人眼反查代码做不了**，故把实际发出的 chatid 写进审计与记录（`delivered_to`），使那次反查**有据可对**。 🔑 **O-10 有结论，且比原设想强一档，请 Shao Peishen 追认**：本行原文设想「合建一份、按 `scenario` 字段**分流**」—— 实现下来发现**分流根本不需要**：决定「这条怎么发」的四个字段（`channel`／目标／`msgtype`／`text`）全部与场景无关，SC2 与 FI2 走**逐字节相同**的路径，`scenario` 只进留痕。⇒ **新场景接入不改中继一行代码。** 已配源码断言禁止出现 `== "SC2"` 一类分支 —— 有人哪天写了，「合建一份」当天名存实亡**且不会有任何测试变红**。 **三条取舍（都进了单测）**：**⑴ 宁可重发不可静默丢** —— 「发送成功」与「标记 delivered」不可能原子，故**发一条标一条**把窗口压到最小；标记走**乐观并发校验**（记住物理行号 ＋ 原文，逐字相等才替换），校验不过 ⇒ 记 `outbox_relay_mark_failed` ＋ 告警，**绝不强行覆盖**（会抹掉写侧刚落的行）、**绝不假装标成功**（那条会再也不被重发）。**⑵ 🔴 outbox 读不到 MUST NOT 被读成「没有待发」** —— `.51` → 笔记本那条文件通路断掉的表象就是「文件不见了」，回落成空列表就会安静地一条不发、与一切正常长得一模一样（同 §四 `#59` 断供 5 个工作日无人察觉的形态）；故读失败一律上抛并**每轮都告警**（不去重）。**⑶ 结构性投不出去的记录留在 outbox 不丢弃** —— `pending()` 数字下不去**正是要的**，它是这条记录还没被人处理的唯一外部信号；同一「文件/行/原因」本进程内只告警一次（审计每轮照记：审计给事后复核用不怕多，告警给活人看怕多）。 **另一条防线**：未配置 `WECOM_AIBOT_OUTBOX_PATHS` ⇒ 中继关闭，但**「关着」也留痕**（`outbox_relay_disabled` 与 `outbox_relay_started` 两条可区分事件 ＋ 各打印一行）—— 「中继压根没启动」与「启动了但没消息」在日志里本来长得一模一样。 **回归**：服务 **606 passed／1 skipped**（基线 569 collected，新增 38，零漂移）／平台 **380 passed／1 skipped**／openspec **117 passed 0 failed**。**真身取证（非单测）**：对**真实** `department_group_chatid_mapping.yaml` 跑只读体检，采购部解析出真实 chatid、`PMC部` 报「不在映射表」、缺文件报「读不到 ≠ 没有待发」，退出码 1 —— 三档行为逐条对上。⚠️ **CI 整体 run 长期 failure（§一 `#398` ⑶）⇒ 上述红绿信号一律本地实跑取证，不以 CI 绿为准。** ━━━ 通路＝Windows 凭据管理器 GUI 存 `192.168.100.51／Administrator`（cmdkey CLI 因 bash/PowerShell/终端多层转义反复失败——先后踩中占位符尖括号残留、PowerShell 7 原生命令参数拆分疑似换行粘贴污染两类坑，改走图形界面表单输入一次成功，密码全程不经过 CC）；UNC 走既有管理共享 `C$`，**未新开专用共享、未改 `.51` 任何配置**（2026-08-27 悬而未决三选一之外找到的第四条路）。快照点＝`.env` 改动前原文双备份（`~/.claude/env-backups/platform-aibot.env.2026-08-31.pre-op0831l-outbox-paths.bak` ＋ 会话 scratchpad 同名件），SHA256 一致（`29bd2fe6…`）。🔴 **实际生产 `.env` 定位澄清（经 `Get-ScheduledTask` 核实、非猜测）**：不在主工作区，在专属 worktree `C:\Dev\zhuopin-ai\.claude\worktrees\wecom-service-home\5-平台底座\.env`（`ZhuopinAibotDevListener` 计划任务 `WorkingDirectory` 实测所在）；`run_aibot_service.py` 走的是 `load_dotenv(SERVICE_DIR.parent/.env)` 定位，不经 `env_anchor`。写入＝新增一行 `WECOM_AIBOT_OUTBOX_PATHS=\\192.168.100.51\C$\sc2\app\reports\sc2_group_outbox.jsonl`。重启＝`Stop-ScheduledTask`+`Start-ScheduledTask`，🔴 实测复现《.51部署标准清单》§七「孤儿进程」条款——`Stop-ScheduledTask` 未级联杀掉旧 PID 16820，已显式 `Stop-Process -Force` 后再启动，新 PID 30812／CreationDate 2026-08-31 14:20:26 核实为真重启（非「脚本打印重启但进程未换」假象）。冒烟三件套全过：⑴ 通路读通（`Get-Content` 直读 UNC 路径，内容与 SSH 只读交叉核验一致）；⑵ `audit` 见 `outbox_relay_started`（非 `_disabled`），`paths` 字段与配置逐字一致；⑶ 端到端——**未新写测试记录，径直投递了 `.51` 上已存在的 2 条真实待发记录**（2026-08-28 20:01 生成的 2026-W34 周报，Shao Peishen 2026-08-31 明确拍板「让它们真发」，非无人在场留步）：`outbox_relay_delivered` × 2，`ack.errcode=0／errmsg=ok`（群聊 `channel=aibot_group_chatid→wrvDL_DAAAH7IL6I7hsgszeyQvmyjw-Q`；私发 `channel=aibot_direct→YaoZuYi`），回读 `.51` 源文件确认两条均已 `delivered:true`（乐观并发校验通过，不会被重发）。**硬约束③（人眼反查）**＝Shao Peishen 本人核实采购部群收到、chatid 无投错。回滚＝冒烟一次性全过，未触发 SOP。 🔴 **精确澄清、避免读成已 100% 销号**：本行原销号判据文本是「姚祖怡在群里真的看到了那条周报」——本次验证到的是「投递成功且群/chatid 经 Shao Peishen 目视核对无误」，**姚祖怡本人是否已亲眼看到/阅读该条尚待其自身确认**，两者不完全等同，如实标注、不代其确认，留给自然发生或另行核实。O-10（合建一份免分流）待追认项本次未触碰，仍按原文悬置。 ━━━ 📎 历史回写 6 段已于 2026-09-04 外置（K2 口径，原文原样 md5:4103137e）见 `1-转型规划/0-全景路线图/队列行日志/#394.md`；本格只留首 1 段＋末 1 段
