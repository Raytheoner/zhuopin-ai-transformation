# 队列 §一 #513 · 行日志（K2 外置件）

> 本件收纳该行状态格外置出来的历史段，原文原样、未改写；行内只留首段＋末段＋指针。


## 2026-09-09 K2 外置（CC 看护者 `OP-0909-Q`，批 `B-0909_六泳道机制批`；Shao Peishen 当日答 `2a`）

外置原因：本批 `OP-0909-W` 回写后该行状态格达 4154 B、超 `ROW_LENGTH_CAP_BYTES` 4096 B（2026-09-11 起由告警转阻断，届时该行任何写入都被硬拒）。以下为原状态格中间段，**原文原样**；md5:`2605191d857494f314aa72f13b0b23a0`。

🔑 **真因判定（三条实证，推翻了「含井号的路径匹配不上」这个流行解释）**：**⒜** `1-转型规划/0-全景路线图/队列行日志/` 磁盘 **141 件**、`git ls-files` 已跟踪 **141 件** ⇒ 含井号的路径一直入库正常；**⒝** 同形态对照 —— `队列行日志/#489.md`（带反引号）今日新建**当日入库**，`队列行日志/#506.md`（裸路径）未跟踪；**⒞** 两侧代码同源：编辑锁 `_pending_batch_fragments` 与 sweep `_resolve_batch_fragments` **都只提取反引号内的串**。⇒ **与井号无关，是反引号**；此前多次「点名豁免放行」实为绕过、非必需。 ⚠️ **后果不是告警是静默**：裸路径写法下 sweep 取不到该批任何文件，**整批静默不入库**，而 release 只在**恰好**跑到 ⑹ 时才拦得住（写批次时不拦、sweep 时不报）。⇒ 属 `#507` 同族（清单与取活口径不一致致整批掉地上）。 📊 **WIP 实测**：立行前机制类可动 **21/22**（手段＝`工具-共享文档编辑锁.py` 的 `_count_mechanism_wip` 对两份真身现取，非引用他处数字），立本行后 **22/22 打满**，无需豁免、无需关闭既有行。 **并入审核（协议〇.10）**：已用真身 grep 扫「反引号」「文件清单」「_resolve_batch_fragments」三词，命中 `#507`（管的是「清单含高频共享件致覆盖校验自锁」，与本行「路径未包反引号致提取不到」**不是同一件事**）、`#234(1)`（管的是「速记路径致后缀匹配歧义」，是反引号**内**的形态问题）⇒ **确无可并入，故取新号。**

---

# §一 #513 · 外置段（2026-09-11 外置，工具第 1 批，1 段，原文原样）

> 外置人 CC-OP-0910-X；外置前整格 2721 B、共 2 段；本批外置第 2 段；本格保留第 1 段。

✅ **2026-09-09 已修（CC `OP-0909-W`，分支 `claude/op0909w-513523-editlock-guard`，commit `416b86b`，仅本地+已 push，未合入 master）**：写侧新增 ⓘ2 `_file_list_bare_path_violations` —— `append-row` 硬拒；`edit-row` **按归属分档**（本次真写了「文件清单」格才硬拒，否则只告警放行，判据同 `_opener_attribution`「一道守卫不得让人为自己没碰过的东西负责」）。判据只认「以已知扩展名收尾」的裸串、**刻意不认目录前缀**——那类串即便包进反引号，sweep 的后缀匹配也永远匹配不到任何脏文件（脏路径是文件、不以 `/` 收尾），拦它纯属噪声，且存量 `B-0902_47` 行「两个产出均落 reports/ 目录」会被当场误伤。 🔬 **改前回显**：裸路径行经 `_file_list_path_violations` 与 `_file_list_git_state_violations` **双双返回 `[]`**、`_pending_batch_fragments` 提取 **0** 个片段（手段＝白盒 probe 直调三函数）。**改后回显**：同一行 `✗ §二 写入被拒绝（1 项，未修改目标文件）… 未用反引号包裹 … exit=1`；同一条命令把清单换成反引号 ⇒ `✓ 已追加一行到 §二 … exit=0`。 📊 **存量误伤面实测（本行「不回改存量」那句的配套答案）**：两份真身 §二 待处理 **34** 行中 **25** 行会被本守卫命中（手段＝`_file_list_bare_path_violations` 现取，非引用他处数字），**全部落在「只告警不阻断」档** ⇒ 给存量批次销状态的动作不被堵死；回改仍归 `#443`。 🧪 单测 +40（`FileListBarePathGuardTests` 含「裸路径⇒拒」正向与「反引号⇒过」反向各一组、`WriteSideGuardCliTests` 端到端四档），全量 `python -m pytest 0-学习与工具/test_工具-共享文档编辑锁.py -q -p no:randomly` ＝ `512 passed, 1 skipped, 51 subtests passed`。
