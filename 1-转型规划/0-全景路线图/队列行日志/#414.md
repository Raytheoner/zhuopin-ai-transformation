---
title: "队列行日志 #414（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #414 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #414 · 外置前整格原文（2026-09-04 外置，9 段）

[S:done][D:机] 🆕 **2026-08-26 立行**。🔴 **硬阻塞于迁移全部完成（#412 M1 ＋ #413 M2/M3 收口）——Shao Peishen 2026-08-26 明确「今天只登记，等迁移全部完成再修理」**。⚠️ **为何不趁现在顺手修**：本行要动的正是 `工具-共享文档编辑锁.py`——**迁移期间每一次队列写入都依赖它**，此刻改它等于在搬家途中拆承重墙。⚠️ **迁移期间的临时纪律（人守，明知其弱）**：`--cell` 一律用**单引号**包裹；写完立刻 `awk` 按竖线分列回读与该格内容。**这条纪律本身就是本行要废掉的东西，不要给它加固。** 　━━━　🔴 **2026-08-26 登记当场再得两条实测，缩小了修复面**：**⑴ 竖线守卫其实已经存在** —— 本行的批次登记首次被 `append-row` **当场拒绝**（「第 2 个 --cell 含竖线」）⇒ 上文建议 ⑶ 中的竖线部分**不必新建**，`append-row` 已守。**⑵ 而本行自己之所以被撑成 12 格，是因为它是用 python 直接插行、绕过了 append-row 这道守卫** ⇒ **真正的缺口是「绕过入口」而非「守卫缺失」**。**⇒ 修复面据此收窄为两条**：**A. 反引号侧**——`append-row` 的 argv 仍经 bash，守卫看不见「bash 已经吃掉的东西」，故治本仍是 ⑴ `--cells-json`／`--stdin-json`（正文不进 argv）；**B. 绕过侧**——凡直接改队列文件的路径（python 插行、整文件重写）**一律在写盘前复用同一套单元格校验**（抽成可被两边调用的函数），否则守卫只守住了走正门的人。🔑 **这一条本身就是本行的最佳论据**：一道**已经存在且有效**的守卫，因为存在一条绕过它的常用路径，而形同虚设——同族＝「机制建成了但从没真正发出过一条消息」（OP-0819-F）。 　━━━　➕ **2026-08-26 第四个实证（同族第三种外形）**：CC 在 #412 收工回写时，把「✅ 已完成…」写进了**期望产出**格、把整段收工叙述写进了**触碰区**格，**而状态列始终停在 `[S:open]`**。⇒ **#413 的阻塞判据之一正是「#412 完成」，机器读状态列 ⇒ 修复前它一直认为 M1 没做完。** 🔑 **三种外形至此齐了：裸竖线撑列（内容变成分隔符）／反引号被执行（内容变成命令）／列位错置（内容落进相邻格）——共同点是「内容落错结构位、且不报错」。****⇒ 修复面 B（绕过侧校验）须一并覆盖第三种**：校验不只查「格数对不对」，还要查「关键格是否为空或明显不该出现在该格」（如状态列不以 `[S:` 开头、期望产出格以 `✅ 已完成` 开头）。 　━━━　➕ **2026-08-26 第五、第六个实证（同日同一会话再犯两次）**：**⑸ 追加文本误入「编号格」**——`split` 按竖线切开后 `c[1]` 是编号格、`c[2]` 才是任务格，数错一位把整段正文灌进编号格，`｜416｜` 行头断裂、grep 与队列查询双双找不到该行；**⑹ 而修复它时触发了编辑锁的死锁**（快照里是坏行 ⇒ release 判修好的行为「未预留新行」⇒ 拒绝；acquire 又被自己的锁挡住），详见 #416 行内。🔴 **本日累计**：裸竖线撑列 ／ 反引号被执行 ／ 列位错置（#412 触碰区格、#416 编号格两次）／ 由此派生的锁死锁 —— **同一族当天出现 4 次，其中 3 次出自同一个会话**。⚠️ **这已经不是「偶尔手滑」**：本行提的治本方案 A（`--cells-json`／`--stdin-json`，正文不进 argv）与 B（绕过侧复用同一套校验）**都无法防住列位数错**——**须补方案 C：提供按列名写入的接口**（如 `--set 状态=... --append 任务=...`），让调用方永远不必自己数下标。 　━━━　⚠️ **2026-08-26 派单后出现同题重复 session，两个均已停（Shao Peishen 手动）**：`OP-0826-N` 的开场词被粘了两次——先粘无编号版（14:25 起，worktree `queue-write-reply-defects-bcd1c1`），后粘带编号版（14:52 起，worktree `queue-write-reply-fixes-e37a2c`）。**发现时两者都未写主工作区、未抢编辑锁，零冲突损失**（纯属发现得早）。🔴 **半成品保留**：14:25 那个已产出 **+1087／-8（5 文件）** —— `工具-共享文档编辑锁.py`(+434)／`queue_table.py`(+255)／`test_工具-共享文档编辑锁.py`(+274)／`工具-队列结构lint.py`(+57)／其测试(+75)，已提交到分支 `claude/queue-write-reply-defects-bcd1c1`（**未合 master、未经审查**）；14:52 那个零产出，worktree 与分支已清理。**⇒ 下次续跑先读该分支已有内容，判断接着改还是推倒重来，不要从零开始。**🔑 **教训**：派单件与开场词里**没有「开工前查一次是否已有同题 session 在跑」这一步**——两个 session 同时改 `工具-共享文档编辑锁.py`，而那正是它们各自回写队列要用的工具。**已补进 `专线opener模板库.md`（见本批次行）。**　━━━　✅ **2026-08-26 A 组全部落地并销号（CC，worktree `queue-write-reply-defects-bcd1c1`，commit `f1ad578`，已 push master）**。迁移已完成 ⇒ 原 blocked 前提消失。**三条修复面**：**A 正文不进 argv** —— `append-row` 新增 `--cells-json`／`--stdin-json`，`edit-row` 新增 `--changes-json`；**B 守卫覆盖所有入口** —— 校验收拢进 `queue_table.validate_row_cells()`，新增关键格哨兵（编号格非数字＝行头断裂／§一 状态格不以 `[S:` 开头／`✅` 落进期望产出格＝#412 形态／格内换行）；**C 永不数下标** —— `--set 列名=值` ＋ 新子命令 `edit-row`。**A3-1 lint 盲区**、**A3-2 release 自愈出口**（HEAD 存在性豁免 ＋ 行内 `预留豁免：`）亦已落地。验收＝19 项玩具队列验收（硬约束②）＋ 全量回归 592 passed／0 failed ＋ 生产队列 206 行读侧误报 0。openspec 变更包 `queue-write-root-fix-414`（含三份 delta spec）。　━━━　🔴 **本行两条判断被实测更正，请勿再按原文重做**：**⑴「`#414` 一度为 13 列而 lint 报通过」复现不出来** —— 构造真 13 列的 §一 行，现有列数校验**当场就报**；原报告成因是 **`awk` 按半角竖线切列时不认反引号保护**（对生产队列 §一 #324／#326 同样数出 12／9 列，而两行实际都是正确的 8 列）。真盲区是另外两种：**编号格被清空**（被当表头/分隔行静默跳过）与**行首竖线丢失**（整条丢弃）——共同点是**行根本进不了列数校验**，已修并配对照单测锁死「13 列本就能报」这一事实。🔑 **元判据：一行「坏到连解析器都不认它是一行」的数据会从所有按行校验里彻底消失，于是 lint 报「通过」——通过的意思是「我检查过的都没问题」，不是「没问题」。**　**⑵ 本行建议 ⑶「含 `Is a directory`／`command not found` 即告警」实测否决** —— 照做后对生产队列 **5 行全部误报**，其中两处正是**本行自己**（`#414` 的任务格与期望产出格里就写着那个字样），另有 #98 状态格合法讨论 `Permission denied`。🔑 **判据：这类判据把「格子被污染成报错文本」与「格子在谈论报错文本」当成同一件事，而它们字面上无法区分——记录事故的载体必然大量引用事故原文，判据越贴近原文，越必然打到记录它的那一行。**⇒ 只保留结构性信号（换行／关键格空或非数字／列数／格位哨兵）。**误报会把调用方推去绕过守卫，而绕过正是修复面 B 要消灭的东西——一道误报的守卫比没有守卫更糟。**　━━━　⚠️ **同题重复 session 的实际处置**：与 CC `queue-write-reply-fixes-e37a2c` 商定分工（本会话＝A 组 ＋ B⑶；对方＝B 组 ⑴⑵⑷⑸，触碰区在 `5-平台底座/wecom-aibot-service/`），**§一 #416 整行由对方一人回写**——两个会话写同一条表格行，正是本行在治的事故类型。 　━━━　✅ **2026-09-02 openspec 变更包已归档**（CC session `[Win]0902W-归档414`，worktree `archive-queue-write-root-fix-414-82fe63`，分支 `claude/archive-queue-write-root-fix-414-82fe63`）：核实 tasks.md 26/27 项全 [x]（仅剩 8.3 archive 自身）后跑 `openspec archive queue-write-root-fix-414 -y`，归档为 `openspec/changes/archive/2026-09-02-queue-write-root-fix-414/`，8 条 requirement 合并入 `editlock-queue-number-reservation`（+1）／`editlock-section-append-guard`（+5）／`queue-table-shared-parser`（+2）三份主 spec（0 修改/0 删除），`openspec validate` 通过。⚠️ **过程记录**：worktree 内原已有一份中断产生的半成品同步（3 份主 spec 已改但变更包目录未搬迁、未提交）——先 `git checkout --` 还原为 HEAD 干净态，再让 `openspec archive` 一次性重做，避免 delta 二次叠加。commit `3ccc542`（ff 入 master 并 push，`9e7060d..3ccc542`）。tasks.md 8.3 已补勾。
