---
title: "队列行日志 #344（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #344 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #344 · 外置前整格原文（2026-09-04 外置，48 段）

[S:partial][D:业] 🆕 **2026-08-18 由 #266/#118 建造 session 实测定性后立行**（派单件明令「同根因就一并修，不同根因就单独登记，不许含糊带过」）。**未修，仅登记＋取证完毕**：根因已用当日真实数据坐实到具体两个函数与具体一笔 `qty=0` 记录（见输入指针）。**下一步＝出判例包**（改 kit_date 会连带动四色判定，判据类须显式签认）；排期由业务总线定，本行不自行派单。 ━━━ ✅ **Shao Peishen 2026-08-18 拍板 (a)：现在就起草判例包**（未选「押后与 #334 同批问」）。**判例包素材已备好**：`4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_344_判例包素材-齐料日期不看答交数量-2026-08-18.md`——由本 CC session 在证据最新鲜时写下，含 3 行判例批改表（拟）＋「反推规则」填空＋逐笔答交取证表。🔴 **受串行闸约束，不得并进采购部#15**：#15（opener 集 E 节：取回批改件原件／#266#118 进展／#334 次序）**已占满一信 ≤3 决策点**，故本包须**等 #15 闭环后**作为 **采购部#16** 单独发；判据＝README 中他最近一封是否已到三种闭环形态之一。**成文与送审归 Cowork**（CC 只出素材）。🔴 **判据类不得标「超时默认生效」**；素材内已写明**改此项会连带使四色变红的行明显变多**，须提前向他交代。 ━━━ ➕ **2026-08-18 拆件巡逻回灌（源＝§一 #346）：他当日另给出的「规则3」与本行同处齐套日期取值层，判例包成文前须先与之对齐** ━━━ 他在 #346 里写的第三条＝「一个项目有答交又有无答交的情况，需要对比无答交的预估齐套日期和有答交的答交日期对比谁更晚到，**更晚的那一个是齐套日期**」。 🔴 **它与本行不是同一件事，但落在同一个取值函数上**：本行治的是「只取**最早答交日期**、完全不看答交数量（含 qty=0 记录）」；规则3 说的是「**无答交估算日** 与 **有答交答交日** 之间取更晚者」——**前者是同类记录内部的选取方向，后者是两类记录之间的合并方向，且两者给出的方向相反（本行现状取最早，规则3 要求取更晚）**。 ⚠️ **本班不判定两者是否同根因、也不判定规则3 是否已被现有代码覆盖**（同 #266 对他新举证⑵ 的处置）。 🔴 **但有一条对本行判例包成文的直接约束**：本行已备好的判例包素材（`docs/queue_344_判例包素材-齐料日期不看答交数量-2026-08-18.md`）成文时**须与规则1／2／3 一并核对**，否则可能出现「判例包问的取值方向」与「他同日已书面写下的取值方向」互相矛盾——**请他在两周内为同一个函数签认两套口径，正是判例批改法要消灭的浪费**。三条规则的确认路径已登 §四 #65。 📌 机器字段维持 [S:open][D:业] 不变——本次只增内容、不改状态。 ━━━ ➕ **2026-08-18 第二班拆件巡逻回灌（源＝§一 #347）：规则1 的理解已由姚祖怡书面确认，本行判例包成文的前置不确定性少一项** ━━━ 他对采购部#15 第一问答「**对**」，即确认 **规则1 里的「20 日」＝ 出货日往前推 3 个月那个自然月的 20 号**（信中所举例：出货日 12-05 → 往前 3 个月为 9 月 → 起算点 9-20 → 再往后 90 天）。⇒ 三条规则中唯一的全新分支，**口径措辞至此无歧义**，判例包成文时可直接引用他这一确认、不必再问一轮。 🔴 **由此并入本行一项待办（协议〇.10 并入优先，本次未取号、未新建行）**：**实测现行程序是否已覆盖规则1，未覆盖就改**——这是采购部#15 正文对他的书面承诺原话（「确认之后，我们会实测现在的程序是不是已经覆盖了规则 1，没覆盖就改」），**本班只登记，不实测、不定级、不派单**。并入判据＝与本行落在同一取值函数层（`sources.py`／`forecast.py` 齐料日链路），且本行判例包成文本就被要求与规则1／2／3 一并核对，另起一行会把同一个函数的口径确认拆成两处。 🔓 **本行「等 #15 闭环」这一前置已满足**：本行原写判例包须「等 #15 闭环后作为采购部#16 单独发」——**采购部#15 已于 2026-08-18 由姚祖怡回件、本班完成回灌，README 状态已转 `📥 已回件并回灌`**，串行闸解除；**起草、成文与排期仍归 Cowork 业务总线**，本班不派单（§〇 红线）。 ⚠️ **本班仍不判定规则1／规则3 是否已被现有代码覆盖，也不判定其与本行是否同根因**（同第一班处置，未越界下新判断）。 📌 机器字段仍维持 [S:open][D:业] 不变。 　━━━　✅ **2026-08-19 回灌：姚祖怡已答判例包第三个决策点（规则确认），他答「对」**（CC-material-board-view-df54d1 由接力块 F 回补，源＝§一 #350 归档件 `7-外部文档/采购部/采购部-YaoZuYi-回复-2026-08-19-文本反馈-254ab79430f98a50b5192c85642ebe91.md`）——**「有答交记录但数量为 0」等同「没有答交」、走规则 2（从此刻起推 90 天），不把那个 0 数量的日期当齐料日**。⇒ 该条口径**可直接进本行的建造口径**，不必再问。 🔴 **但本行仍不可开工**：判例包（采购部#16）的 **3 条判例他一条未答**，原因是表头 ✅／❌ **无主语**、他无法判断在评哪一列（见 §一 #349）；澄清答复已于 2026-08-19 09:49 本地经 aibot 私信＋采购部群抄送发出，**等他回件**。⇒ **采购部#16 整封仍非闭环，姚祖怡串行闸仍锁着**（SC2 判例包拟采购部#17 仍不能起草）。 ⚠️ **同批查出判例 3 用的是虚构数据**（「假设某物料需要 15000…」），违反判例批改法硬禁令「判例包不得使用虚构/样例数据」，**很可能是他卡住的第二重原因**（前两条是真实料号 `R01I.0622`，第三条突然变假设题，符号歧义＋题型切换叠加）；**是否为判例 3 补一条真实案例后重问，留 Shao Peishen 定**。 ━━━ ➕ **2026-08-20 补件：采购部#16 的「可勾选版」已出（我方交付缺陷的补救，非新信）** ━━━ **触发**：姚祖怡 2026-08-19 18:16 本地回件——「我在（采购部跟进信第 16 封 · 判例批改包）中第二条判例中，**那张表内无法实现×和√的勾选，请帮我实现能够勾选这个功能**」（已归档 `7-外部文档/采购部/采购部-YaoZuYi-回复-2026-08-19-文本反馈-55bbd92d4fbea1c0a7ff79334bc3e8d7.md`）。 🔴 **成因＝我方交付缺陷，已坐实**：采购部#16 **只发了 md、未出 docx**（实测该目录下仅 `采购部-姚祖怡-跟进-2026-08-18-齐料日期只看日期不看数量判例批改.md` 一个文件、4568 B，无同名 docx），他在企微收到的是纯文本，其中的方框是**死字符**、点不动——**违反判例批改法 §3.3「含勾选项必须走 md2word 原生真复选框」这条既有硬约束**（该条原文即写「死字符点不动，专员填不了」）。 🔑 **两堵墙叠在同一封信上**：⑴ 符号缺主语 ⇒ 他不知道勾什么（2026-08-19 上午问，已发澄清）；⑵ 未出 docx ⇒ 知道了也勾不动（2026-08-19 傍晚再问）。**同一封信、同一天、连续两次被我方交付缺陷挡住，两次他都问得很客气。** ━━━ **补件**＝`6-人才与组织/部门AI专员跟进/采购部-姚祖怡-补件-2026-08-20-齐料日期判例批改可勾选版.md` ＋ 同名 `.docx`（**实测 9 枚 `w14:checkbox` 真复选框、sdt 外裸方框 0、预勾选 0**，图例与带主语表头均已进正文）。**判例内容与原信一字未改**；性质为**对在途信 #16 的补件、不占部门编号、不进 README 新行、不占串行闸**。 ━━━ 📌 **补件内顺带处置了 §四 #70（判例 3 虚构数据）**：向他明说第 3 条是假设场景、按我方自定规矩不该混进判例包，**可照常勾，也可直接跳过只勾 1、2 两条**，我方另找真实的两笔累计案例再问。**未擅自撤下该条**（撤下会少收一个口径点，且那正是「累计到覆盖需求」的关键场景）。 ━━━ ➕ **2026-08-20 拆件巡逻并入一项待确认（源＝§一 #357 销行；协议〇.10 并入优先，未取号、未新建行）：补件截至 2026-08-20 09:15 本地尚未见发送痕迹** ━━━ **两条独立信号**：① `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl` 末条停在 `2026-08-19T10:39:55Z`（18:39 本地），**全日零 2026-08-20 记录**；② 补件 md frontmatter 仍 `status: 待发`。**日志本身未坏**——该文件只在真实发送时落 `*_sent` 事件，同日 `ZhuopinDecisionReminderDaily` 08:30 `LastTaskResult=0` 亦无对应事件，属静默无候选的正常形态；`ZhuopinAibotDevListener` 实测 `State=Running`。 ⚠️ **不断言它一定没发**：`Cowork-全景路线图线-0820-补件定稿` 在本班 acquire 前 6 分钟仍在活动（末次 commit `464bb1a`，09:08:23 本地），可能正在发送途中。 🔴 **之所以要在本行留一句，是因为它此刻没有任何载体承载**：`B-0820_01`／`B-0820_02` 两条 §二 批次均已落库销行，且两者都明写「未取号、未新建队列行」——**「补件已定稿」被记了，「补件要发出去」没被任何行盯着**（同 §四 #71「默认项无执行者在场」那一族）。**发送属 §〇 红线（对外发送）不进池，拆件巡逻只登记、不执行、不催。** **下游是实的**：#16 一日不闭环，姚祖怡串行闸就一日不开，SC2 判例包（拟 采购部#17）就一日不能起草。 　━━━　✅ **2026-08-20 补件已送达（私信 ＋ 抄采购群）——上一段「尚未见发送痕迹」至此销掉** ━━━ **09:24:51 本地（01:24:51Z）** 经 aibot chatid 双通道发出：① 私信 `YaoZuYi`（正文 ＋ docx 附件）；② 抄送采购部群 `wrvDL_DAAAH7IL6I7hsgszeyQvmyjw-Q`（同一 media_id，正文冠 `【抄送】`，沿用 `delivery.py` 群 cc 既有措辞）。**四帧回执 `errcode` 实测均为 0**——不是「脚本打印了已发送」就算数（同 OP-0819-F「探针通了 ≠ 机制通了」那条）；审计落 `5-平台底座/wecom-aibot-service/reports/wecom_aibot_audit.jsonl` 一条 `followup_supplement_delivered`，`acks` 里四帧回执逐条可复核。**开头「这不是一封新的跟进信、你手上的待办没有增加」那段原样送达**（发送前以断言卡住其存在，不是发完再回看）。 🔴 **刻意未用 `5-平台底座/wecom-aibot-service/scripts/push_followup_letter.py`**：该脚本会走 README 门禁②、回填状态列并自动 commit＋push，**套用即把补件污染成一封新信**；改为一次性脚本直调 `AibotConnector`，但**复用既有原语、不另造第二实现**——`delivery._strip_frontmatter`（#283，防内部记账字段泄漏给专员）＋ `delivery._assert_ack_accepted`（#326，观测 errcode 非零即拒）。**全程零 webhook**（#282）。**三条约束如约未破**：不占部门编号／不进 README 新行／不占串行闸。 ⚠️ **未抄送 `ShaoPeiShen`**：`push_followup` 的 `cc_to_paul` 固定抄送逻辑本次未套用（派单件只授权私信 ＋ 抄采购群两条通道，且他即本次派单人），如实登记而非默认照搬。 　━━━　🐛 **顺带证伪了上一段两条判据中的一条，值得记** ━━━ 上一段（B-0820_03 拆件巡逻并入）以「audit 全日零记录」＋「补件 frontmatter 仍 `status: 待发`」两条独立信号推断未发送。**第一条成立；第二条结构性不成立**——实测本目录**已发出**的四封信（`采购部-姚祖怡-跟进-2026-08-10`／`-08-13`／`-08-18` 两封）frontmatter **全部仍是 `status: 待发`**：跟进信的权威发送态载体是 README「发送状态」列，md frontmatter 从不随发送回填。⇒ **该判据对任何已发信恒为「未发」，所谓两条信号其实只有一条**——同「交叉验证优于重复同一验证」那族的变体：**看起来是两个独立信源，实则其中一个恒真、零信息量**。本次亦**未**改动补件 frontmatter，与既有惯例一致。 ⇒ **下一个等待对象 ＝ 姚祖怡回件**（#16 闭环 → 串行闸开 → SC2 判例包方可作 采购部#17 起草）。 　━━━　✅ **2026-08-20 拆件巡逻回灌（源＝§一 #359 销行）：上一段等的那份回件已到，3 条判例他全部签认「这样判对」** ━━━ 补件 09:24:51 本地发出，**姚祖怡当日 15:08 本地把同一份 docx 原样勾选后发回**（归档 `7-外部文档/采购部/采购部-YaoZuYi-回复-2026-08-20-采购部-姚祖怡-补件-2026-08-20-齐料日期判例批改可勾选版-3c451da886c422c08a9cf7995fd324b4.docx`）。**3 条判例的「✅ 这样判对」列全部勾上，❌ 与 ✏️ 六格全空、正文零添字、无批注无修订。** ⇒ **本行的建造口径至此完整、不必再问一轮**： ⑴ **齐料日 ＝ 按答交数量累计到覆盖需求为止的那一笔的日期**（判例1 `R01I.0622` ⇒ 2027-05-20，而非现状取的 2026-08-20 那笔 qty=0）； ⑵ **一笔即够则不再往后累**（判例2 ⇒ 2027-05-20）； ⑶ **不够则继续累计到够**（判例3 8000＋9000 覆盖 15000 ⇒ 2027-01-20）； 连同 2026-08-19 已签认的 ⑷「有答交但数量为 0 等同无答交、走规则 2」，**四条合起来即本行 `sources.py`／`forecast.py` 齐料日链路的目标行为**。 🔴 **勾选状态的取证方式须记住，否则下一次会读反**：python-docx 对 `w14:checkbox` 真控件返回空串，**三个勾选列在文本提取里全是空的**；须读 `word/document.xml` 的 sdt（本件 ☒×3 ／ ☐×6）**并与我方发出的原件对照**（原件 ☒＝0 ／ ☐＝9）才能证明「勾是他打的、不是我方预勾选」。 ━━━ 🔴 **本行仍不自行开工，三条边界原样保留**：⑴ 建造属 §〇 红线，拆件巡逻只回灌口径、不派单、不改代码；⑵ **判例 3 用虚构数据这一问未随签认消失**——他没有按补件给的台阶跳过、而是照常勾了，**§四 #70 三个选项均未预设这一侧**，该条签认在 IATF 口径下是否有效留 Shao Peishen 定，**在他答复前不宜把判例3 那条单独拿去当签认依据**（判例1／2 是真实料号，不受此影响）；⑶ **规则1／3 是否已被现行代码覆盖，本班仍不判定**（同前三班处置），成文时仍须与规则1／2／3 一并核对。 ━━━ ⚠️ **README 中 采购部#16 的状态列本班未改**（仍 `✅ 已推送`）：转闭环态会一并解除姚祖怡串行闸、放行 SC2 判例包（拟 采购部#17），而「回灌消化完毕」是否成立取决于上条 §四 #70——**该判定交采购专线／业务总线，不由拆件巡逻做**。 📌 机器字段维持 [S:open][D:业] 不变——本次只增内容、不改状态。 ━━━ ✅ **2026-08-20 §四 #70 已由 Shao Peishen 拍板 (c) 收口，本行上一段三条边界中的 ⑵ 随之解除**（Cowork 全景路线图线 OP-0820-D 执行） ━━━ **判例 3 的签认认定有效**，其结论「不够则继续累计到够为止」**可与判例 1／2 同等作为签认依据进本行建造口径**，虚构数据一事作已知瑕疵留痕、不重问。**采购部#16 据此判闭环，README 状态列已转 `📥 已回件并回灌`，姚祖怡串行闸已开** ⇒ 本行不再受「等 #70」阻塞；**SC2 判例包（采购部#17）亦随之解锁可起草**。 ━━━ 🔴 **并入一项待办：为判例 3（连同判例 2）补一条真实的多笔累计案例作事后佐证（协议〇.10 并入优先，本次未取号、未新建行）** ━━━ **⑴ 为什么并进本行**：佐证要用的正是本行同一条链路的数据（缺口量 ＋ 逐笔答交），另起一行会把同一个函数的口径确认再拆一处。 **⑵ 为什么本 session 做不到、必须交 CC**：全库只读普查逐份核过 8 处真实留档，**无一合格**——根因是留档里「**缺口量**」与「**逐笔答交**」**从未在同一快照里配过对**；且真实答交按月滚动，实测 `R01A.0090` 在 `docs/queue_262_material_commitment_window_audit.md` §3.2（2026-08-05）尚有两笔非零，而 `docs/queue_334_material_board_audit-2026-08-20.md` §6.2 的 08-20 快照**已两笔全 0** ⇒ **任何据旧表编的判例今天都复现不出来**（与本行已记的「11-18 vs 12-25」漂移同型）。 **⑶ 省事路径（已核实，无需新写取数逻辑）**：`sc8/material_board.py::build_material_board` 的每一行**已同时携带** `total`（窗口合计缺口）与 `cb`（逐笔答交）；跑一次 `#334` 同款底稿后筛 `len(cb)>=2 且 cb[0].q < total 且 sum(q) >= total`，即可在 596 行里直接拿到真实候选。 ━━━ 🐛 **同批查出一件此前四轮核查都没看出来的事：判例 2 的需求量同样是假设数** ━━━ 判例 2 写的是「**若这个物料需要 6000**」——它挂在真实料号 `R01I.0622` ＋ 真实答交表上，**隐蔽度比判例 3 高得多**：判例 3 一眼可见「假设某物料」，判例 2 只在**需求量那一处**是构造的，其余全真。 🔑 **⇒「判例包不得使用虚构数据」这条禁令此前实际是靠肉眼扫「假设」二字在守，而它只抓得住最显眼的那一种。** 本次按 §四 #70 (c) 的同款处置**一并覆盖判例 2**（补佐证、不再要他签）；**该扩展是我方所加、不是 Shao Peishen 答复的原文**，已在 §四 #70 显式留痕，可随时被推翻。 📌 机器字段维持 [S:open][D:业] 不变——建造仍未开工，本次只增口径与待办。  　━━━　✅ **2026-08-24 建造完成并部署 `.51` 上线（CC，worktree `kit-date-qty-cumulative-344`，commit `ac38bec`，openspec `sc8-kit-date-qty-cumulative`，已归档）——`[S:partial]` 而非 `done`，仅因发布收口第 8 步的跟进信被串行闸卡着，见本段末** ━━━ **按已签认四条口径改**（采购部#16 三判例 ✅×3 ＋ 口径 ⑷ 文本回件）：齐料日 ＝ 按答交数量累计到覆盖需求为止的那一笔的日期；一笔即够不再往后累；不够继续累到够；有答交但数量为 0 等同无答交。 ━━━ 🔑 **结构性收益比修好这一处更值得记**：`_cumulative_confirmed_batches` 由 `baoguan.py` **下沉到 `forecast.py`**，齐料日／BOM 缺口清单／物料看板**三处共用同一个函数对象**（配 `is` 同一性单测）——「下面清单对、上面汇总数不对」这一族在齐料日这个落点上**不可能再复发**，而不是靠「记得同步改三处」。 ━━━ **零漂移是结构性的、不是"测了没发现"**：`material_commitments` 与 `required_qty` **两参齐备才走新分支**，任一缺省逐字节回旧口径 ⇒ `sc8/pipeline.py`（对客承诺主流水线）与 mock 黄金基准**根本走不进新代码**；同时这也是数据源加载失败时的降级路径（不会把全场刷成红）。 ━━━ **真实数据（生产载荷 `SC8_NET_INVENTORY=on`，2026-08-24）**：105 行 **100红5绿 → 105红0绿**，**92 行**齐料日或瓶颈物料变动；**姚祖怡举证那一行原样复现并翻面** —— `F02N.0224`（出货日 2026-09-20）齐料日 2026-09-20 → **2027-05-20**、瓶颈物料 → **`R01I.0622`**（与他 2026-08-12 书面指认**逐字一致**）、🟢按期 → 🔴真延期。**同一个 `R01I.0622` 还是另外 11 行的新瓶颈** —— 一个料的一笔 `qty=0` 记录，在看板上掩护了 12 张成品卡片。⚠️ 他举证的日期（11-18 vs 12-25）与今日实测（09-20 vs 2027-05-20）不同，系月度自然滚动所致，**机制一致且今日更极端**（他说差 37 天，今日这行差 8 个月）——给他的信里须主动交代。 ━━━ **离线预测与生产实跑逐字对上**：部署后 `POST /api/refresh` 全量重算 452s 返回 `{red:105, grn:0, new_reds:5}`，与对照表的预测**完全一致**；`/api/baoguan` 实读该行 `bn=R01I.0622`。 ━━━ **部署四关全过**：ff 入 master（`git rev-list --count origin/master..master` ＝ 0）→ `sync-to-server.ps1` → **进程 `CreationDate` 独立核对真刷新**（08-20 12:19:12 → 08-24 23:25:31，不只信脚本打印）→ **服务器 3 个文件 SHA256 与 master 工作区逐字节一致** → **从笔记本外部**实测 `/api/ping` 200／带令牌 `/` 与 `/materials` 200／匿名 302。**回滚 SOP 已把最危险那环测掉**：用 `git archive origin/master` 抽的**改造前副本**反序列化今天的生产快照（7.5 MB）→ `ok=True／rows=105`，**读得动、不崩、不退化空态**（与 #334 那次不同，本变更 `git diff` 实证未触碰 `baoguan_service.py`／`models.py`）；⚠️ **未做一次真实的线上回滚往返**（两次全量重算约 15 分钟且期间看板显示旧口径），如实标注边界。 ━━━ **回归零漂移**：SC8 **共享主工作区 479 passed+3skip**（同环境纯净 master 基线 **457+3**）／**worktree 478+4skip**（同环境基线 **456+4**）——**两处都恰好 +22 条新用例**；🔴 基线均用 `git archive origin/master` 抽纯净副本**实测重定**，**不是照抄场景 CLAUDE.md 上一行的 448+4**。🔑 **两处差 1 的原因已查清、不是异常**：`test_golden_real.py`（**真实**黄金基准 replay）依赖 gitignore 件 `data/golden/real_frozen/`，**worktree 里没有这份夹具故整组 skip，主工作区里实跑并通过** ⇒ 「黄金基准不漂移」在本变更包里**mock 与真实两侧都有实证**。⚠️ **跨环境比这两个数字会得出「凭空多一条」的假结论**，各自比各自的基线、平台 380+1skip、SC1 53／SC2 146／SC7 41／FI1 33／FI2 179+9skip／QD-A 41／QD-B 114+27skip／O1 32／O2 20 全绿；`openspec validate --all --strict` **111/111 全过**。 ━━━ **同车清掉 #334 行内登记的尾巴**：`/materials`「取数说明」第 6 条那句「齐料日期口径正在与采购部确认中……判例包已发出待回件」已撤换（线上实测新文案在线、旧文案已撤）。 ━━━ 🔴 **底稿** `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_344_kit_date修复前后对照-2026-08-24.md`（落 tracked 的 `docs/`、不落 gitignored 的 `reports/`——#267 事故）；真实输入已冻结为一份仓库外 JSON，**任何人拿它可复跑出同一张表**，数字不靠"我当时看到的就是这样"背书。 ━━━ 🔴 **三处未闭合，如实登记，不假装完工** ━━━ **⑴ 四色饱和：105 行全红、🟢 归零 ⇒ 看板不再具备分级功能**。修复前已是 100/105 红，本次推到 105/105。**这不是本次引入的缺陷，但本次让它再也无法回避**。已登记 **§四 #110**，**无默认**。 ━━━ **⑵ 「答了但累计仍不够」的口径是我方保守外推、非他签认原文**（design D3）：签认四条覆盖了「够」与「全 0」，**唯独没覆盖「答了、但加起来还是不够」**（如需求 15000 只答了 8000）。现按保守项在线上生效——判无答交、到货日取 `max(启发式估算日, 最晚正数答交日)`（取更晚，与他自己写的规则 3 同向）。**须以判例形式补问。** 🔒 **串行闸锁着不得起草**（2026-08-24 实测 `工具-跟进闸查询.py --to 姚祖怡` → `🔒 锁`，依据 `采购部#18（待你审，暂不占号）· ⏳ 待你审 · 在途`，下一个可用号 **采购部#19**）⇒ **📌 拟发要点（待前信闭环后取用）**：① 补一条真实的多笔累计案例请他确认「累计到够为止」（**已找到真实候选**：`R01D.1301`，三月合计缺口 10,782，2026-09-20 给 6,000 不够、累计到 2026-10-20 的 12,000 才够 ⇒ 齐料日 2026-10-20；生产载荷里同型真实案例共 **156** 个，一并把判例 2／3 的假设数瑕疵补上，收口 §四 #70 (c)）；② 请他判「答了但不够」该怎么算；③ 告知看板会变得更红及为什么。 ━━━ **⑶ 规则 1 实测未覆盖，本变更包只测不改**（源＝采购部#15 对他的书面承诺「实测……没覆盖就改」）：**规则 3 ✅ 已覆盖**（`kit_date = max(arrivals)` 天然就是"取更晚"）；**规则 2 ⚠️ 部分覆盖**（出货日已过期时逐字一致，未来但在三个月内时现行比规则 2 晚）；**规则 1 ❌ 未覆盖**——现行仍是出货日+90，比规则 1 **晚 89~92 天**（中位 +91，43 行适用，**全部为正 ⇒ 现行是偏保守那一侧、不会低估**）。**刻意不并进本包**：它把估算日**往前挪**（红的会变少），与 #344 方向相反，同车上线就没人说得清哪行是被哪条改动搬的。已登记 **§四 #111**，**无默认**。 ━━━ 🔑 **本轮最该被记住的一条方法教训**：**「影响面大小」不能用来论证口径该不该改**。本轮一度用「换取数源会让 106 个子件翻面、看板全红」论证不换源，**真实数据把这个论证否掉了**——换源变体与已采纳口径只差 **2 行**、四色计数**完全相同**。106 这个数字唬人却对结论毫无贡献。**判据只有一条：谁授权的**（#211 v2 原文明写其 `receiveType==2` 筛选「范围仅限本函数……未经授权不改判定逻辑」，故本次**只改"有答交记录时怎么按数量取值"，不改"什么算有答交"**）。⚠️ 危险之处在于：**按影响面论证时，两个方向都能编出理由，而且都听着很有道理。** 　━━━　⏸️ **2026-09-02 OP-0902-A6（判例包）未开工，如实登记 —— 依赖 OP-0902-A5 未解除** ━━━ 本行原挂待办「⑵ 『答了但累计仍不够』须以判例形式补问」的判例包，2026-09-02 由业务总线派 `OP-0902-A6`（泳道 `sc8-kitdate`，批 `B-0902_46`）承接，**开工首查即命中 opener 硬闸**：`工具-泳道看护状态机.py show --lane sc8-kitdate` ⇒ **`paused`**（🟡 停在「③ openspec design 审」，等 A5 的 **D3「姚祖怡规则2 改不改」** 三选一，**无默认**）。⇒ **零实质开工**：未建 worktree／未建分支／未起草任何判例／未动 `docs/queue_344_判例包素材-…-2026-08-18.md`。🔴 **依赖是实的、不是形式次序**：A5 与本行落在**同一取值函数**上，D3 决定的正是本包每一条判例「拟改判定」列的取值来源——D3 未拍即起草，产出的是一份 D3 拍板后须整体重写的判例包，而它一经发出即占掉姚祖怡串行闸、且**判据类永不默认生效**，⇒ **返工成本由他承担而非我方**。📌 **A5 已实测消解本行一处旧前提，一并记此**：§四 #65 与本行 08-18 段写的「本行现状取最早、与规则3 方向相反」**已不成立**——本行 08-24 上线累计口径后，A5 探针实证逐笔答交 `[(2026-10-20, 0), (2027-05-20, 10000)]`、需求 8000 ⇒ 现行返回 **2027-05-20**（取更晚），与规则3 **同向、无冲突**；取证件 `4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_118_三条规则实测对位-2026-09-02.md`。⇒ **本包成文时不必再「调和方向冲突」，改为只处理规则2 逐字化的取值差**。🔴 **成文前置共三项、均须姚祖怡答，本 session 一项未代拍**：⑴ D3 规则2 改不改（A5 design 审在等）；⑵ 「三个月内」边界开闭；⑶ 三级判据是否即 PMC 优先级表最终形态。📌 机器字段维持 `[S:partial][D:业]` 不变——本次只登记未开工事实，不改状态、不销号、不派单。⚠️ **姚祖怡串行闸实测仍 🔒 锁**（`采购部#20` 在途），故即便三项前置全解，本包亦须待闸开后方可发。** ━━━ ⏸️ **2026-09-03 OP-0902-B4（判例包成文）未开工，如实登记 —— 依赖 OP-0902-B3 未解除** ━━━ 本行判例包由业务总线派 `OP-0902-B4`（泳道 `sc8-kitdate-2`，批 `B-0902_60`）二次承接，**开工首查即再次命中 opener 硬闸**：`python 0-学习与工具/工具-泳道看护状态机.py show --lane sc8-kitdate-2` ⇒ **`paused`**（🟡 停在「③ openspec design 审」，等 `sc8-atp-batch2-closeout` 的 design 审）。⇒ **零实质开工**：未建 worktree（opener 指定的 `kitdate-cases-2` 未创建）／未建分支 `claude/op0902b4-kitdate-cases`／未起草任何判例／未动 `docs/queue_344_判例包素材-…-2026-08-18.md`／未触碰跟进信 README／未发送任何件。 🔴 **本轮须更正一处 opener 前提（这是本次登记的实质内容，不只是「又停了一次」）**：opener 写「**前置已解除：D3=(a) 逐字化已由 Shao Peishen 2026-09-02 拍板**」——**D3 已拍板属实，但 D3 已经不是当前那道闸**。B3 把 (a) 落进 design 的动作本身**实质改写了自 A5 定稿后的 design**，因而新开三项须 Shao Peishen 重审：**① D3-bis** ——缓释②逐行核出「会前移的是 **11 行**、不是裁决书写的 24 行」（另 13 行瓶颈件为已答交件，规则3 的 `max` 让其一天不动；四色一格不动），实际代价小于他已接受的代价、方向一致，故 B3 判「**不重判**」，**但该判定本身仍须他确认**；**② D4** ——D3 取 (a) 后，原「含边界更保守」的论证由条件句变既成事实而**失效**，现行取值**从此既无签认、亦无保守性背书**；**③ D5** ——不变式③在对照表上**空过、未获验证**。⇒ **前置由「一项(D3)未决」变成「一项已解 ＋ 三项新开」，闸未开、方向未变。** 🔑 **本轮留下的判据（与 A6 那条互补，建议一并记住）**：**「前置已解除」是写 opener 那一刻的快照，不是开工那一刻的事实**——B3 与 B4 由同一批 opener 一次性生成，而**B3 的执行本身就会改变 B4 的前置条件**（它落的字正是 design 被改写的原因）。⇒ **opener 里的「前置已解除」只能证明派单时点的判断，开工首查一律以状态机为准，二者不一致时以状态机为准、并把差异当作实质情报回写**（本条即是）。A6 那条讲的是「排在后面 ≠ 依赖前一项，本项依赖是实的」；本条讲的是「**依赖状态会在 opener 写成之后继续变化，且最常见的改变者就是前一棒自己**」。 🔴 **成文前置现共四项、均须姚祖怡或 Shao Peishen 答，本 session 一项未代拍**：⑴ design 审（含 D3-bis／D4／D5，须 Shao Peishen）；⑵ 「三个月内」边界开闭（须姚祖怡，B3 已登记待并进下一封）；⑶ 三级判据是否即 PMC 优先级表最终形态（须姚祖怡，同上）；⑷ 规则2 逐字化最终口径在件内如何显式写清（依赖 ⑴ 过审后方可定稿）。📌 机器字段维持 `[S:partial][D:业]` 不变——本次只登记未开工事实，不改状态、不销号、不派单。⚠️ **姚祖怡串行闸实测仍 🔒 锁**（`采购部#20` 在途），故即便四项前置全解，本包亦须待闸开后方可发；**判据/口径类永不默认生效**（IATF 显式签认红线），本包成文后 README 发送状态列亦只能写 `⏳ 待你审`。 　━━━　➕ **2026-09-03 `OP-0903-A1`（泳道 `sc8-kitdate-3`，批 `B-0903_10`）：本行判例包新增附录 A —— 依 Shao Peishen 裁决 D4 ＝ (a)** ━━━ 🔴 **本次不是本行原待办（「答了但累计仍不够」的判例）的成文，那一项仍未开工、仍在等 `sc8-atp-batch2-closeout` 的 design 审**；本次只是**往同一个判例包里补挂一条来自 ATP 批2 的边界口径判例**。 **裁决原话**：「补一条边界口径进判例包，随下一封请姚祖怡签认 —— 理由：D3 取 (a) 后『含边界更保守』的论证由条件句变成既成事实、已失效，现行取值从此既无签认也无保守性背书，**必须补签，而不是记录风险接受**。」⇒ 🔴 **明确否掉的另一条路是「记录风险接受」：他要的是签认件，不是一条风险登记。** **落点**：`4-数字员工/采购部/SC8-客户订单交期智能承诺/docs/queue_344_判例包素材-齐料日期不看答交数量-2026-08-18.md` **附录 A「三个月内的边界当天算哪一边」**（A-1 问题一句话／A-2 为什么必须补签／A-3 取证／A-4 判例批改表 2 行 ＋ 反推规则填空／A-5 影响面／A-6 本附录不包含的）。 🔑 **为什么并进本包、不另起一个判例包**：姚祖怡处**串行闸**约束一次一封，本条与本包正文落在**同一条齐料日取值链路**上，另起一个包等于多占一封信、且会让同一个取值函数的口径在两周内被分两次签 —— 正是判例批改法要消灭的那种浪费。⚠️ **但两问是同链路、不同层，成文时须在信里分节写清、不得混成一问**：本包正文问「齐料日**看不看答交数量**」，附录 A 问「**三个月边界当天**走哪条规则」。 📐 **附录 A 已把口径量化到可签的程度**（算术，可手算复核，非生产实测）：以 `today=2026-09-02`、边界日 `2026-12-02` 为例 —— **含边界 ⇒ 走规则 2 逐字 ⇒ 起算今天 ⇒ 齐料 2026-12-01、缺口 −1 天、🟢**；**不含边界 ⇒ 走规则 1 ⇒ 起算 2026-09-20 ⇒ 齐料 2026-12-19、缺口 +17 天、🔴**。⇒ **同一张单两种读法相差 18 天，四色一个 🟢 一个 🔴** —— 这是看板上一行会不会报警的差异。 🔴 **状态 `⏳ 待姚祖怡签认`；判据/口径类永不默认生效（IATF 显式签认红线）** —— 已核该件全文 4 处「默认生效」**全为禁止性表述、无一处授予**；**未签认前，现行「含边界」取值只是事实上的沿用，不得在任何文件里被记作「已确认」**。 🔴 **成稿 ≠ 已发**：姚祖怡串行闸实测仍 🔒 锁（`采购部#20` 在途）⇒ 本泳道**未起草任何信、未发送任何件、不为附录 A 单独起信、不插队**；闸一开即随下一封采购信并进。判闸唯一入口＝`python 0-学习与工具/工具-跟进闸查询.py --to 姚祖怡`，起草前**现跑**，不得引用任何文件里的闸态快照。 📌 机器字段维持 `[S:partial][D:业]` 不变——本次只增判例素材，不改状态、不销号、不派单。
