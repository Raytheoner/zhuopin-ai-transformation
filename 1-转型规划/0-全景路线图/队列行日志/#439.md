---
title: "队列行日志 #439（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #439 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #439 · 外置前整格原文（2026-09-04 外置，8 段）

[S:open][D:机] 🛑 **排队（WIP 20/22，唯一空位已给 `#382`）；🆕 2026-08-31 立行（Cowork 值周巡检，源＝移交单 `OP-0830-J` 候选 A），去 🛑 后可领** ━━━ **并入审核（协议〇.10）已于取号前完成**（移交单 §2）：全库未见可并入行；`#366` 管「信」的闭环证据、本行管「点」的身份，粒度不同，不可并入。 ━━━ 🔴 **本轮不给可动位的理由（值周巡检判定，非保守）**：本行是 **R2（串行闸改按点计）的硬前置**，而 R2 已由 Shao Peishen 2026-08-30 判**本轮不立项** ⇒ **下游拉力本轮不存在，立即开工无收益**；proposal 草案已备，WIP 回落即可零成本激活。 ━━━ ⚠️ **R3（转态检测与分级响应）已整体并入 `#366`、零新增行**，不在本行范围内。 　━━━　🔒 **Shao Peishen 2026-08-31 答《本周计划-2026-08-31》**：**立 🛑 不占位已确认。** 🔑 **他补了一条本行原先没有的洞察，值得留在这里**：**立 🛑 反而保护了本行** —— **占位会在 WIP 压力下被催开工，而本行缺的是 design 审前置、不是位子**。⇒ 去 🛑 的触发条件不变（WIP 回落），但**新增一条判据：即便 WIP 有位，R2 未立项前也不应被催**。 ━━━ ✅ **design 审已过（Shao Peishen 2026-09-01 当场审定，`OP-0901-D`）** —— **三条裁决**：⑴ 台账格式**保留 JSONL ＋ 在 `.gitignore` 加范围严格限定的 `!` 例外**（新 **D7**；起因＝实测发现 `.gitignore:32 **/*.jsonl`「含业务数据绝不入库」会把台账整个吞掉，而仓库至今 `.jsonl` 入库数＝0）；⑵ **质量域入库，与其他域同等**（新 **D9**，依据＝质量部跟进信含 8D 判例原文本就已在 git 里，台账入库不比现状更差；⚠️ 属维持现状风险、非消除）；⑶ 其余五条 Decisions **整体照准**。⇒ **Decisions 由 6 条增至 9 条（新增 D7／D8／D9），Open Questions 已清空。** 📋 **审材料＝** `1-转型规划/0-全景路线图/design审前置-口径点台账三开放点收敛-2026-09-01.md`：§9 审记录／§5 全套包预写（proposal 变更点＋design＋spec delta＋tasks）／§6 **19 条 `git check-ignore` 实测**（8 条首轮＋11 条例外验证，含 **4 条反向对照组**证明例外未误放行任何运行时审计 JSONL）。 🔴 **本行去 🛑 的条件由两条减为一条** —— Shao Peishen 2026-08-31 那条「**本行缺的是 design 审前置、不是位子**」**已消解**（前置今日补齐）⇒ **现在缺的确实只剩位子**；触发条件仍为**可动 WIP 回落**（2026-09-01 实测 23／22，未回落）。 ⚠️ **apply 前 CC 两件硬前置（详见审材料 §9.3，不得跳过）**：① §6.3 那 11 条是在 `/tmp` 独立 repo 上验的**规则逻辑**、**不是真仓库状态**，CC 在真 `.gitignore` 落例外后**必须原样复跑并贴出输出**；② 例外须加在第 32 行 `**/*.jsonl` **之后**（git 后序优先），**加在之前完全无效且不报错**。 🔑 **附一条本次 design 审自身的方法留痕**：草案定的「三行 check-ignore 输出须落包后才能补」是**误以为存在的阻塞** —— 实测 `git check-ignore` **不要求文件存在**（8 条被测路径当时全部不存在），该前置在草案阶段即可满足。 　━━━　🔴 **落包已发生，但初版丢了你 09-01 的裁决；2026-09-02 已修正（Cowork `OP-0902-A`，你当日答 ①(a)）** **丢了什么（三条，逐条实证）**：⑴ `tasks.md` 把三个开放点重列为「未收敛、design 审前不得开工」——而它们 09-01 已全部拍完（⇒ D1／D2／D4）；⑵ proposal 把台账格式**反向改判**为 `.yaml`，并把「保 `.jsonl` ＋ 加例外」判为「在写着绝不入库的规则上凿洞」——**而那正是你 09-01 拍的 (b)（D7），且已实测 11 条通过、含 4 条反向对照组**；⑶ **`design.md` 未建**——实测 34 个在途包里本包是唯一缺 design 的一个，D1–D9 无一进包。 **根因（grep 实证）**：草案 `openspec提案草案-口径点台账-2026-08-30.md` 对 `design审前置`／`OP-0901-D`／`2026-09-01` **全库 0 命中**，没有任何前向指针指向 09-01 收敛件；唯一连接两者的载体就是本行状态列，而落包线读了草案、没读本行。⇒ 同 `CLAUDE.md` OP-0819-A ⑴「一份拆成两份，下游只跟了一份」，本次跟的是**过期那一份**。 🔑 **由此得一条判据**：**凡「落档不落包」的前置件，必须在被它更新的上游件里留一条前向指针**——否则上游件是一枚哑雷：内容自洽、格式完整、谁读都不报错，只是结论已过期。 **本次修法（四件 ＋ 草案）**：新建 `design.md` 落 D1–D9 ＋《落包勘误》节；`proposal.md` 撤回 `.yaml` 改判并整节重写伴生文件节（两轮共 19 条实测搬入）、数字全线刷新（506→576 处、17／15→38／21 封、黄金集 17→18 组）；`tasks.md` 按收敛件 §5.4 重排为八节（新增 §0 apply 前置两条）；spec delta 翻转「jsonl 扩展名被拒」为「入库方式须留痕而非绕开」、OEM 一条改写为「按域物理隔离」、新增「事实日期不得被补记覆盖」；草案 frontmatter ＋ 正文顶部补**前向指针**（该判据的第一个落点）。**本机实测 `openspec validate coverage-point-ledger --strict` ＝ valid。** 🔴 **apply 前置两条不变**（已写进 tasks §0）：⑴ 真 `.gitignore` 落例外后**必须在真仓库原样复跑那 11 条**并贴出输出（`/tmp` 上验的是规则逻辑、不是真仓库状态）；⑵ 例外**必须加在第 32 行 `**/*.jsonl` 之后**，加在之前完全无效且不报错。 🔴 **本行 🛑 不摘、本班不去 🛑**：2026-09-02 状态分诊后机制类可动 WIP 已由 **24／22 降至 14／22**、位子确已空出，但你 2026-08-31 那条判据仍成立——**「即便 WIP 有位，R2 未立项前也不应被催」**，而 R2 至今未立项。**摘不摘 🛑 交值周巡检或你定。**  　━━━　🔴 **2026-09-02 本行发生一次重复劳动，成因是机制缺口不是人疏忽，Shao Peishen 当日明示「如何避免重复劳动最好也要登记进队列作为教训」，如实登记** 　**经过**：`OP-0901-D`（2026-09-01）已对本行做过 design 收敛并产出九条 Decisions；`OP-0902-A`（2026-09-02）在**不知情**的情况下又落了一次包，且把台账格式改判为 `.yaml`——**该改判已被撤回**（正解＝以范围严格限定的 `!` 例外保留 `.jsonl`，理由是**改扩展名是绕开规则、加例外才是留痕**，见本包 D7 与 spec「入库方式 SHALL 留痕而非绕开规则」）。 　🔑 **根因（可复现，非态度问题）**：`OP-0902-A` 落包前**做了**协议〇.10 并入审核，方法是 `工具-队列查询.py --digest` 扫关键词（口径点／台账／SDLC／R1／需求管理／intent），**命中只有 `#284`**，遂判「无归属行、需新建」。**但本行 `#439` 的任务列恰好写着「口径点台账」** —— 2026-09-02 实测：**digest 里搜「口径点」＝ 0 条，真身 grep 搜「口径点台账」＝ 7 处**。⇒ **digest 把任务列截断到 40 字，关键词落在截断之外，用它做关键词并入审核会结构性漏检。** 　⚠️ **同一工具当日第二次致错**：早上它把 `#382` 状态显示为「🛑 排队中」，而 `--row` 全文是「🟢 已激活·可领」（与 `#268`「状态列头尾结论不一致时截断读给反向答案」同族）。**两次都不是工具坏了，是用途错配** —— `--digest` 为「扫状态分布、找命中行号」而设，**不为「查某事是否已有行」而设**。 　**⇒ 判据（写进本行，供后续所有 session）**：**协议〇.10 并入审核 MUST 用真身 grep，MUST NOT 只用 `--digest` 关键词搜**；`--digest` 只可用于取状态分布与行号清单，**任何「查了没找到 ⇒ 判定不存在」的结论，都不得建立在截断视图之上**。⚠️ 与本日 `#382` 那条判据同族并互补：那条说「凡断言某机制没发生，须先列出该载体实际存在的键名集合」，本条说「**凡断言某事没有行，须搜未截断的真身**」——**两条的共同根是：在一个会静默省略内容的视图上做否定判断。** 　📌 **本次损失可控**：`OP-0902-A` 那版 proposal／tasks／specs 已被后续会话按 `OP-0901-D` 九条 Decisions 覆盖修正（tasks 顶部已有《2026-09-02 勘误》两条），**包本身现处正确状态**；净损失＝一次重复起草的工时，与一次已被撤回的错误改判。


---

# §一 #439 · 二次外置（2026-09-06 去 🛑 时按 K2 口径外置，2 段，md5:33bdc630）

> 去 🛑 前状态列整格原文，原文原样、可 grep；行内留新首段＋末段＋指针。

[S:open][D:机] 🛑 **排队（WIP 20/22，唯一空位已给 `#382`）；🆕 2026-08-31 立行（Cowork 值周巡检，源＝移交单 `OP-0830-J` 候选 A），去 🛑 后可领** ━━━ 🔴 **2026-09-02 本行发生一次重复劳动，成因是机制缺口不是人疏忽，Shao Peishen 当日明示「如何避免重复劳动最好也要登记进队列作为教训」，如实登记** 　**经过**：`OP-0901-D`（2026-09-01）已对本行做过 design 收敛并产出九条 Decisions；`OP-0902-A`（2026-09-02）在**不知情**的情况下又落了一次包，且把台账格式改判为 `.yaml`——**该改判已被撤回**（正解＝以范围严格限定的 `!` 例外保留 `.jsonl`，理由是**改扩展名是绕开规则、加例外才是留痕**，见本包 D7 与 spec「入库方式 SHALL 留痕而非绕开规则」）。 　🔑 **根因（可复现，非态度问题）**：`OP-0902-A` 落包前**做了**协议〇.10 并入审核，方法是 `工具-队列查询.py --digest` 扫关键词（口径点／台账／SDLC／R1／需求管理／intent），**命中只有 `#284`**，遂判「无归属行、需新建」。**但本行 `#439` 的任务列恰好写着「口径点台账」** —— 2026-09-02 实测：**digest 里搜「口径点」＝ 0 条，真身 grep 搜「口径点台账」＝ 7 处**。⇒ **digest 把任务列截断到 40 字，关键词落在截断之外，用它做关键词并入审核会结构性漏检。** 　⚠️ **同一工具当日第二次致错**：早上它把 `#382` 状态显示为「🛑 排队中」，而 `--row` 全文是「🟢 已激活·可领」（与 `#268`「状态列头尾结论不一致时截断读给反向答案」同族）。**两次都不是工具坏了，是用途错配** —— `--digest` 为「扫状态分布、找命中行号」而设，**不为「查某事是否已有行」而设**。 　**⇒ 判据（写进本行，供后续所有 session）**：**协议〇.10 并入审核 MUST 用真身 grep，MUST NOT 只用 `--digest` 关键词搜**；`--digest` 只可用于取状态分布与行号清单，**任何「查了没找到 ⇒ 判定不存在」的结论，都不得建立在截断视图之上**。⚠️ 与本日 `#382` 那条判据同族并互补：那条说「凡断言某机制没发生，须先列出该载体实际存在的键名集合」，本条说「**凡断言某事没有行，须搜未截断的真身**」——**两条的共同根是：在一个会静默省略内容的视图上做否定判断。** 　📌 **本次损失可控**：`OP-0902-A` 那版 proposal／tasks／specs 已被后续会话按 `OP-0901-D` 九条 Decisions 覆盖修正（tasks 顶部已有《2026-09-02 勘误》两条），**包本身现处正确状态**；净损失＝一次重复起草的工时，与一次已被撤回的错误改判。 ━━━ 📎 历史回写 6 段已于 2026-09-04 外置（K2 口径，原文原样 md5:0f87f0af）见 `1-转型规划/0-全景路线图/队列行日志/#439.md`；本格只留首段＋末段   　━━━　📤 **2026-09-04 `OP-0904-E`（Cowork 环境总线）已出 CC 派单件，未动包一字节**：`1-转型规划/0-全景路线图/派单件-【CC】R1口径点台账-apply前置-2026-09-04.md`（opener `OP-0904-F`，`工具-opener块lint.py --file` 零违规）。**只派 `tasks.md` §0 两件**（§0.2 在 `.gitignore` 第 32 行 `**/*.jsonl` **之后**加 D7 例外 ＋ §0.1 真仓库复跑 11 条 `git check-ignore -v`），做完即停。🔴 **不派 §1**：§1.1／§1.2 标【Cowork】，且**五份骨架若在 §0.2 之前建会被 `.gitignore:32` 静默吞掉**（`git add`／`git status` 都不报错）⇒ **顺序反了等于白做且无信号**。**串行链**：CC §0 → Cowork §1.1/§1.2 → CC §1.3–§1.6。⚠️ 取号无机制保障（`#151` 未落），派出前扫当日是否已有 `-F`。


---

# §一 #439 · 三次外置（2026-09-06 21:4x，§1.1/§1.2 收工时按 K2 口径外置，3 段，md5:585ba0d5）

> 含 `OP-0906-P` 看护者 2026-09-06 合入段全文。原文原样、可 grep；行内留首段＋末段＋指针。

[S:open][D:机] 🟢 **可领（CC）——2026-09-06 Shao Peishen 拍板去 🛑（源＝`1-转型规划/0-全景路线图/端到端构建workflow优化-方案-2026-09-06.md` §六 决议 ①：不改协议〇.9、上限 22 不提、余量优先需求侧；去 🛑 时 WIP 实测 20→21/22）。本轮 apply 范围＝tasks 组 1＋2.1；组 4／5 顺延。🔴 与代码侧 `criteria_signoff`（包 `criteria-signoff-platform`）同 ID 打通、不建第二本账，两包 design 合审——细则见方案件 §四 W2；后续 W6（`已验收` 态＋黄金用例）软序本行。▶ 首次派出：见 `#436` 状态列末段看护件。｜ 🆕 2026-08-31 立行（Cowork 值周巡检，源＝移交单 `OP-0830-J` 候选 A）** ━━━ 📎 **历史回写段全部外置**（K2 口径，原文原样、可 grep）见 `1-转型规划/0-全景路线图/队列行日志/#439.md`：2026-09-04 外置 6 段（md5:0f87f0af）／2026-09-06 去 🛑 时外置 2 段（md5:33bdc630）／**2026-09-06 `OP-0906-Q` 收工时外置 `OP-0904-E` 派单件出件段**（其原文已完整含于上述二次外置节末段，未另存副本；派单件本体＝`1-转型规划/0-全景路线图/派单件-【CC】R1口径点台账-apply前置-2026-09-04.md`，opener `OP-0904-F`）。本格只留首段＋末段＋本指针。 　━━━　✅ **2026-09-06 `OP-0906-Q`（CC，分支 `claude/op0906q-r1-precheck-439`，commit `f54838e`，已 push、未 ff 进 master）已做完 `tasks.md` §0 两件、做完即停**：⑴ **§0.2** —— D7 例外 `!6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl` 已落进真 `.gitignore` **第 38 行**，位于第 32 行 `**/*.jsonl` **之后**（`grep -n jsonl .gitignore` 实测：32 行 ＝ `**/*.jsonl`，38 行 ＝ 例外行；例外上方 4 行注释写死「必须在其后、移到之前完全无效且不报错」与 D8「例外不递归」）。⑵ **§0.1** —— 在真仓库原样复跑 11 条 `git check-ignore -v --no-index --non-matching`，**11/11 全部符合预期**：台账 5 条主文件（采购／财务／质量／销售／IT 域）均命中 `.gitignore:38` 的 `!` 例外，且 `git check-ignore` 退出码 ＝ 1（无一被忽略）；**4 条反向对照组全部仍被忽略** —— `reports/wecom_aibot_audit.jsonl` 命中 `.gitignore:41 **/reports/`、`4-数字员工/财务部/FI2-三单匹配自动对账/reports/audit.jsonl` 命中该场景内嵌 `.gitignore:2 reports/`、`5-平台底座/logs/runtime.jsonl` 命中 `.gitignore:32 **/*.jsonl`、`口径点台账/子目录/深层.jsonl` 命中 `.gitignore:32 **/*.jsonl`（🔴 **D8「例外不递归」在真仓库复现，五份域文件必须平铺**）；2 条伴生件 `采购域.jsonl.editlock`／`采购域.jsonl.editlock.lastknown` 命中 `.gitignore:75 *.editlock*` 仍被忽略。⑶ **另核基线同源**：主仓工作区 `.gitignore` 与本分支基线各自去掉 CR 后 md5 同为 `ffc6bc020ddab2012ed416e54fb367c4`（手段：`git show HEAD:.gitignore` 的字节流与 `C:/Dev/zhuopin-ai/.gitignore` 的字节流分别过 `tr -d` 去 CR 再 `md5sum` 比对；只读，未触碰主仓），排除「两者之间隔着一份可能已被别人改过的 `.gitignore`」这一风险 —— 派单件 §9.3⑴ 的顾虑当场证伪。⑷ `tasks.md` §0.1／§0.2 已勾。🔴 **未做 §1 及以后任何一条**：`git show --stat` 只有 `.gitignore`（+6）与 `openspec/changes/coverage-point-ledger/tasks.md`（+2／-2）两个文件，**无 `口径点台账/` 目录下任何新文件**（派单件验收判据 ⑷ 过）。🟡 **停点 ＝ 合入 master**：本泳道按看护件只 push 自己那条分支，未 ff、未在主仓 commit，待总线派发。**下一棒 ＝ Cowork 做 §1.1（8 字段 schema）＋ §1.2（五份域文件骨架，🔴 平铺、不得建子目录）**，之后再回 CC 做 §1.3–§1.6。 ━━━ ✅ **已合入 master**（`OP-0906-P` 看护者，2026-09-06，Shao Peishen 现场答第 3 项 (a)）：分支 `claude/op0906q-r1-precheck-439` rebase 到 `bec15d5` 后纯 ff，master 至 `44d73f8`。D7 例外现位于 `.gitignore` **第 38 行**（第 32 行 `**/*.jsonl` 之后）。**验证手段**：合入后在 master 上复跑 `工具-队列结构lint.py`／`工具-引导样板lint.py --enforce` 退出码 0，`openspec validate --all --strict` 170 passed／0 failed。**§1 串行链已解冻**——下一棒＝Cowork 做派单件 §1.1／§1.2 五份域文件骨架，🔴 **必须平铺、不得建子目录**（D8 例外不递归已在真仓库复现：子目录内 `.jsonl` 仍被 `.gitignore:32` 静默吞掉，`git add`／`git status` 都不报错）。


---

# §一 #439 · 四次外置（2026-09-06 22:5x，`OP-0906-X` 看护批 B-0906_X 收工时按 K2 口径外置，md5:e429da8b）

> 外置 1 段：`OP-0906-W` 的 §1.1／§1.2 收工段与 §1.3–§1.6 派出段。🔴 **本次与前三次外置不同的一点，务必注意**：本行末段（`OP-0906-Z` §1.3–§1.6 收工段）单段即 4058 B，只留「首段＋末段＋指针」仍会超 4 KB ⇒ **行内那一段是精简版，本节这份才是原文全文**。查证一律以本节为准。

[S:open][D:机] 🟢 **可领（CC）——2026-09-06 Shao Peishen 拍板去 🛑（源＝`1-转型规划/0-全景路线图/端到端构建workflow优化-方案-2026-09-06.md` §六 决议 ①：不改协议〇.9、上限 22 不提、余量优先需求侧；去 🛑 时 WIP 实测 20→21/22）。本轮 apply 范围＝tasks 组 1＋2.1；组 4／5 顺延。🔴 与代码侧 `criteria_signoff`（包 `criteria-signoff-platform`）同 ID 打通、不建第二本账，两包 design 合审——细则见方案件 §四 W2；后续 W6（`已验收` 态＋黄金用例）软序本行。▶ 首次派出：见 `#436` 状态列末段看护件。｜ 🆕 2026-08-31 立行（Cowork 值周巡检，源＝移交单 `OP-0830-J` 候选 A）** ━━━ 📎 历史回写段（含 09-04／09-06 两次外置指针与 `OP-0906-P` 合入段全文）已外置 `1-转型规划/0-全景路线图/队列行日志/#439.md`（2026-09-06 三次外置，外置前整格 md5 585ba0d5） ━━━ ✅ **§1.1／§1.2 已做（Cowork 环境总线 `OP-0906-W`，2026-09-06）**：`6-人才与组织/部门AI专员跟进/口径点台账/SCHEMA.md`（每行＝一次事件 append-only；`fact_date`／`recorded_on` 并存＝spec 第四条落点；`id` 两形态，其中「与 `criteria_signoff` 同名」一条留 SCHEMA §四 待两包合审）＋ 五个 0 字节 `.jsonl` 平铺；**验证手段**：`git check-ignore`（无 `-v`）五条退出码 1＝全部不被忽略、`git status -uall` 可见、对照组 `口径点台账/子目录/深层.jsonl` 仍命中 `.gitignore:32`。包内 `tasks.md` 1.1／1.2 已勾。**下一棒＝CC §1.3–§1.6**（读写模块＋断言 1.4／1.5／1.6），派单件串行链第三段；行长 §1 后再看 K2。 （opener豁免：本批次触碰 `1-转型规划/0-全景路线图/opener骨架.md`，其代码块为 `[OP-MMDD-X]` 占位骨架、本就不是真实 opener，F5 系对骨架自身的结构性误报，同前次改骨架先例。） （登记豁免：`openspec/changes/archive/2026-09-06-editlock-credential-shape-guard/`、`openspec/specs/editlock-credential-shape-guard/spec.md`、`1-转型规划/0-全景路线图/CI长期红-逐job根因取证-2026-09-06.md`、`1-转型规划/session接力-业务总线.md` 系他线在办产出（`#480` 归档收尾／CI 取证／业务总线），由其作者线自登，本线不代登。） ▶ 派出 §1.3–§1.6：泳道 `439-ledger-module`（`OP-0906-Z`）随看护件 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0906_X-需求侧续棒两泳道-2026-09-06.md`（`B-0906_X`）派出，正本＝包内 `tasks.md` §1.3–§1.6 ＋ `口径点台账/SCHEMA.md` §三 六条约束；不做 §2、不写真实点、五份 `.jsonl` 保持 0 字节。 ✅ **§1.3–§1.6 已做并已合入 master（CC `OP-0906-Z` 建造 ＋ 看护者 `OP-0906-X` 合入，2026-09-06）**：新模块 `5-平台底座/zhuopin_platform/zhuopin_platform/coverage_point_ledger/`（`errors` ／ `models` ／ `store` ／ `aggregate` ／ `overdue` ／ `lint` 六件）＋ 4 个测试文件。append-only 靠**没有那个函数**实现（`LedgerStore` 不提供 `update()` ／ `delete()` ／ `set_status()`）；域由 `id` 前缀**最长匹配**裁决，避免枚举声明顺序变成隐形判据。三条断言：D3 ＝ `test_D3_跨域聚合只读不落盘_含缓存`（堵 `builtins.open` ／ `io.open` ／ `write_text` ／ `write_bytes` ／ `touch` ／ `mkdir`，并对台账目录树与一个空 cwd 各做前后 sha256 指纹比对，同时断言真覆盖 5/5 个域）；D5 三面 ＝ 构造面（无 `evidence` 的 `已签认` 构造期即抛）／行为面（超期 1000 天扫完仍 `在途`、台账零字节增长）／入口面（AST 扫全包，写内容代码有且只有 `store.py::_append_line`）；D8 ＝ `test_D8_台账目录出现子目录即lint失败` ＋ lint CLI 对**真台账目录**复跑 exit 0。三条均配反向对照组，否则「全拒」或「什么都没干」的实现同样全绿。**验证手段（泳道自测 ＋ 看护者独立复跑，同数）**：`python -m pytest` 新增 4 件 ＝ 60 passed / 1 skipped；rebase 换底后底座全量复跑 ＝ 527 passed / 2 skipped（与 rebase 前同数）；**合入主仓后再跑 ＝ 528 passed / 1 skipped** —— 多出的那一条是 `test_D8_真台账目录当前合规`，它在 worktree 里因真台账目录不存在而 skip，回主仓后真跑并通过（**不是新增用例**）。`openspec validate coverage-point-ledger --strict` ＝ valid；看护者另以 `grep -rnE` 独立核「写内容入口唯一」——模块内除 `store.py:232` 的 `path.open` 追加模式（＝ `_append_line`）外，仅剩 `touch()` ／ `mkdir()` 两处结构性调用（不写内容），与 AST 结论一致；`ls -la` 复核五份 `.jsonl` 仍 0 字节、时间戳仍为 `OP-0906-W` 建文件时刻（本线未碰）。**合入实况**：分支 `claude/op0906z-ledger-module-439` rebase `7878c28` → `1ea9ac8`（零冲突，`--force-with-lease` 已推 origin），master `6d6df03` → **`1ea9ac8`**（`git merge --ff-only`，ff 前 `git rev-list --left-right --count` ＝ 0/1 实证）。ff 前主仓工作区那份 `openspec/changes/coverage-point-ledger/tasks.md` 有未提交的 §1.1 ／ §1.2 勾选与分支同文件相撞，处置 ＝ 先 `git diff` 存补丁并 `git checkout --` 还原该文件腾出 ff 空间、ff 后 `git apply --3way` 原样回贴（`Applied patch cleanly`），复核 §1.1–§1.6 六项全为 `[x]`，两侧改动零丢失。✅ **`carrier` 口径已定死（Shao Peishen 2026-09-06 经 `OP-0906-X` 拍板 (a)）**：`carrier` ≥1 **仅对 `建点` 行**生效，`转态` 行可空、写入期不强制。此前 SCHEMA §二 字段表（写「≥1（建点或转态行）」）与同节最小示例（两条 `转态` 行均无 `carrier`）自相矛盾，现已按 (a) 改齐三处：`SCHEMA.md` §二 字段表（改为「≥1（**仅建点行**）」并写明拍板出处与成因）、`models.py` 首部留痕（由「待合审／不替它做决定」改为「已定死」）、`tasks.md` §1.3 留痕。理由 ⑴ 写入期强制会让 tasks §6.3 的质量型指标「承接载体缺失数量」**结构性恒为 0**（自己度量自己永远合格）、⑵ 会逼 §2 回溯拆点为历史转态行**编造**载体（与「历史记录不追改」相抵）。⇒ 载体缺失走度量（`Snapshot.missing_carrier`），不走写入期硬拦。**本条不再进两包合审**；合审剩余项只有 SCHEMA §四「`id` 与 `criteria_signoff` 同名」那条。**§2 回溯拆点未动、§3–§5 一字未动、`design.md` 未碰**；`tasks.md` §1.3–§1.6 四项已勾。▶ **下一棒 ＝ §2 回溯拆点（Cowork）**：模块已在 master、`carrier` 口径已定死，前置已清、可即起。

# §一 #439 · 五次外置（2026-09-07 15:56，`OP-0906-W` 登记 tasks §2bis 缺口时按 K2 口径外置，1 段，md5:eac586c9）

> 外置前状态列共 4077 B；本节为第三段（`OP-0906-Z` 收工精简版＋carrier 口径定死段）原文，行内只留一句指针。

✅ **§1.3–§1.6 已做并已合入 master（CC `OP-0906-Z` 建造 ＋ 看护者 `OP-0906-X` 合入，2026-09-06）—— ⚠️ 本段为精简版，原文全文见外置件四次外置节**：新模块 `5-平台底座/zhuopin_platform/zhuopin_platform/coverage_point_ledger/`（六件）＋ 4 个测试文件。append-only 靠**没有那个函数**实现（`LedgerStore` 无 `update()` ／ `delete()` ／ `set_status()`）。三条断言：D3（堵六个写盘入口 ＋ 目录树与空 cwd 双 sha256 指纹比对 ＋ 断言真覆盖 5/5 域）；D5 三面（构造面无 `evidence` 造不出对象／行为面超期 1000 天不改状态／入口面 AST 扫出写内容代码仅 `store.py::_append_line`）；D8（建子目录即 lint fatal ＋ CLI 打真目录 exit 0）。三条均配反向对照组。**验证手段**：`python -m pytest` 新增 4 件 60 passed/1 skipped；rebase 换底后底座全量 527 passed/2 skipped（与换底前同数）；**合入主仓后 528 passed/1 skipped** —— 多的一条是 `test_D8_真台账目录当前合规`，worktree 里因真目录不存在而 skip、回主仓真跑通过，**不是新增用例**；`openspec validate --strict` valid；看护者另以 `grep -rnE` 独立复核「写内容入口唯一」，与 AST 结论一致；五份 `.jsonl` 经 `wc -c` 复核仍全 0 字节。**合入实况**：分支 `claude/op0906z-ledger-module-439` rebase `7878c28` → `1ea9ac8`（零冲突，已 `--force-with-lease` 推 origin），master `6d6df03` → **`1ea9ac8`**（`--ff-only`，ff 前 `left-right` ＝ 0/1 实证）。ff 撞主仓未提交的同文件改动（`tasks.md` §1.1／§1.2 勾选），处置＝存补丁 → `git checkout --` 还原腾空间 → ff → `git apply --3way` 回贴，复核 §1.1–§1.6 六项全 `[x]`、两侧零丢失。✅ **`carrier` 口径已定死（Shao Peishen 2026-09-06 经 `OP-0906-X` 拍板 (a)）**：≥1 **仅对 `建点` 行**生效，`转态` 行可空、写入期不强制；已改齐三处（`SCHEMA.md` §二 ／ `models.py` 首部 ／ `tasks.md` §1.3），**模块代码一行未改**。理由 ⑴ 写入期强制会让 tasks §6.3 的「承接载体缺失」指标**结构性恒为 0**、⑵ 会逼 §2 为历史转态行**编造**载体。⇒ 载体缺失走度量（`Snapshot.missing_carrier`）。**本条不再进两包合审**；合审剩余项只剩 SCHEMA §四「`id` 与 `criteria_signoff` 同名」。**§2 未动、§3–§5 一字未动、`design.md` 未碰**；`tasks.md` §1.1–§1.6 六项全勾。▶ **下一棒 ＝ §2 回溯拆点（Cowork）**：模块已在 master、`carrier` 口径已定死，前置已清、可即起。

# §一 #439 · 六次外置（2026-09-07，`OP-0907-AK` 做完 §2bis 2b.1–2b.4 收工时按 K2 口径外置，md5:07c02491）

> 外置前状态列共 4505 B。本节两部分：**上半**＝被移出行内的第 2／3／4 段原文（历史外置指针段、§1.3–§1.6 合入段、§2bis 缺口补登段）；**下半**＝`OP-0907-AK` 收工段**原文全文**，行内那份是精简版，查证以本节为准。
> 🔴 第 1 段与第 5 段（`OP-0907-AL` §2.1 黄金集回溯）原样留在行内、未动一字。

## 上半 · 移出行内的三段原文

📎 **历史回写段全部外置**（K2 口径，原文原样、可 grep）见 `1-转型规划/0-全景路线图/队列行日志/#439.md`：2026-09-04 六段（md5:0f87f0af）／2026-09-06 去 🛑 时两段（md5:33bdc630）／2026-09-06 `OP-0906-Q` 三次外置（md5:585ba0d5）／**2026-09-06 `OP-0906-X` 四次外置（md5:e429da8b）＝ `OP-0906-W` 的 §1.1／§1.2 收工段与 §1.3–§1.6 派出段**。🔴 **本次与前三次不同**：末段（`OP-0906-Z` 收工段）单段即 4058 B，只留「首段＋末段＋指针」仍会超 4 KB ⇒ **行内末段是精简版，外置件四次外置节那份才是原文全文，查证以外置件为准**。／**2026-09-07 `OP-0906-W` 五次外置（md5:eac586c9）＝ §1.3–§1.6 合入段与 carrier 口径定死段**（行内只留下一句）。

✅ **§1.3–§1.6 已合入 master（`1ea9ac8`，建造 `OP-0906-Z`＋合入 `OP-0906-X`，2026-09-06）；`carrier` ≥1 仅建点行、转态行可空（Shao Peishen 经 `OP-0906-X` 答 (a)）；两包合审只剩 SCHEMA §四「`id` 与 `criteria_signoff` 同名」一条**——原文全文见外置件四次／五次外置节。

🆕 **§2bis 缺口补登（2026-09-07，Cowork 环境总线 `OP-0906-W`；Shao Peishen 同日「上图流程缺口那段确保修复」）**：`采购部#21` 回件 30 分钟自动拆完、台账零回写——成因＝本包 §6.2③ 要求「回件解析 → 回写台账」但 §1–§7 无一条建造它，拆件 skill `huijian-chaijian-patrol` §三 无台账步骤、台账无 CLI（写盘只有 Python 入口）。已补登 `openspec/changes/coverage-point-ledger/tasks.md` §2bis 五项（2b.1 CLI `transition`／2b.2 拆件 4bis 回写／2b.3 发送侧 `在途` 接线／2b.4 正负样本断言／2b.5 真实 `财务部#17` 回件验收），派单件 `1-转型规划/0-全景路线图/派单件-【CC】台账回件回写接线-2026-09-07.md`（`OP-0907-AK`，与 `B-0907_R`／`B-0907_Y` 触碰区零重叠、可并行）。▶ **下一棒（可并行）**：⑴ CC `OP-0907-AK` 做 2b.1–2b.4；⑵ Cowork §2 回溯拆点。

## 下半 · `OP-0907-AK` 收工段原文全文

✅ **§2bis 2b.1–2b.4 已做（CC `OP-0907-AK`，2026-09-07；分支 `claude/op0907ak-ledger-writeback-c84203`，commit `851a46a`，已 push、🟡 未 ff master）**

⑴ **2b.1 台账写入 CLI** ＝ `5-平台底座/zhuopin_platform/zhuopin_platform/coverage_point_ledger/cli.py`（子命令 `transition` 写／`points-by-letter` 只读）＋ 同目录 `letters.py`（信↔点判据）。唯一写盘仍经 `LedgerStore.append_many`；`已签认` 缺 `--evidence` ⇒ exit 3（**判据不在 CLI 侧重写一份**，靠 `LedgerEvent` 构造期不变式 ⑸，CLI 只把异常转成退出码——两份 D5 不一致时松的那份说了算）；`recorded_on` 当场取本机时刻带 `+08:00`，与 `fact_date` 分开取。退出码即判据：`0` 成功（**查到 0 个点也是 0** —— 非投影是正常形态，非零会让巡逻把正常信当故障重试）／`2` 用法／`3` 台账拒绝／`4` 读失败／`5` 定位不到台账目录。🔑 **多做了只读子命令 `points-by-letter`**（2b.1 原文只写 `transition`）：2b.2 要求 4bis「查五份域文件 `letters` 含该编号的点」，而拆件是无头非交互班次——没有这条命令那一步只能靠会话自己 grep 五份 JSONL 再自行判断，判据就退回模型判断。判据落 `letters.py::points_for_letter`，回件侧与发送侧**共用同一份**（否则又是「两份副本互相印证着一起错」，tasks 6.3 风险型指标）。

⑵ **2b.2 拆件 skill §三 4bis**：源码件 `0-学习与工具/skills源码/huijian-chaijian-patrol/SKILL.md` ＋ 副本 `0-学习与工具/定时任务源码/huijian-chaijian-patrol.SKILL.md` 两份同步改，`cmp` 无输出＝**逐字节一致**，`git diff --numstat` 各 `17 0`（插入式、无重写）。4bis 插在 §三.4 与 §三.5 之间，五小步：⑴ `points-by-letter` 只读查点 ⑵ `[NONE]` ⇒ 报固定一句「本信非台账投影，零回写」、**本步结束不写台账**（并明写「不得因为查不到点就去建点」）⑶ 有点则逐点判 已答／未答／新口径 三分支 ⑷ 非 0 退出码照抄进报告、**不重试不改参数绕过**（改参数让它过 ＝ 拿逃生阀去绕它本来要拦的东西）⑸ 报告加一行「台账回写：已签认 N 点／仍在途 M 点」。🔴 **安装件未改**：位置由 `0-学习与工具/工具-定时任务源码备份.py::DEFAULT_SOURCE_DIR` 取得 ＝ `~/Claude/Scheduled/huijian-chaijian-patrol/SKILL.md`（不猜路径），改动前已核它与镜像逐字节一致。派单件「不做什么」明令**不注册／改定时任务**，而回镜方向单向（真身→镜像，「改镜像不生效」）⇒ **4bis 要真跑起来须把新版拷进安装件**，留给 Shao Peishen／总线。

⑶ **2b.3 发送侧接线** ＝ 新 `5-平台底座/wecom-aibot-service/aibot_service/ledger_writeback.py`，挂 `aibot_service/delivery.py::push_followup` **返回前**（主送→回填→两处抄送全部尘埃落定之后）。`push_followup` 是 `scripts/push_followup_letter.py`（手动单封）与 `aibot_service/dispatch.py::dispatch_followup_letters`（批量）**共同**的发送核心，挂一处两条路都覆盖。**触碰文件**：`ledger_writeback.py`（新）、`delivery.py`（＋import ＋返回前一段 try/except ＋ `timedelta` 补进 datetime import）、`tests/test_ledger_writeback.py`（新）。🔴 **`0-学习与工具/工具-跟进闸查询.py`（`#482`）与 `0-学习与工具/工具-跟进信README登记.py`（`#447`）一字未动**；信编号解析复用 `5-平台底座/zhuopin_platform/zhuopin_platform/shared_tools/followup_gate.py::parse_letter_number`（**只读 import，该文件未改**）——全项目唯一一份「信编号长什么样」的实现，不另写正则。
🔑 **「这封信投影哪些点」两个来源（2b.3 原文未指定，此处如实登记本方裁法）**：⑴ 台账侧已连线 ＝ 点的 `letters` 里已出现同一个 `部门#N`（起草期连的线；真身实例 `财务部#17（待你审，暂不占号）` **括注一律归一化容忍**——字面量比较会把它与干净的 `财务部#17` 判成两封信，且判错后的外观与「不投影」一模一样、不产生任何信号）；⑵ 信侧自陈 ＝ 信 md frontmatter `口径点:`／`配套:` 里反引号引起来的 id（真身 `财务部#17` 的 `配套:` 就写着 `FI10-G-07`）。🔴 **两来源都与 `Snapshot.points` 求交** ⇒ 「自由文本里解析出噪声」最坏只是没命中、**不会凭空建出一个点**（断言 `test_2b3_信侧自陈了一个台账里不存在的点则不写`）。
🔴 **只推一步 `待问`→`在途`**：已 `在途` 跳过（重推不产生第二行）；`已签认` 及以后跳过（拖回 `在途` 是无因倒退，凑一个 `note` 出来的理由是假的）。`fact_date` ＝ 发出日、`recorded_on` ＝ 写入时刻，分开取。
🔴 **失败一律不上抛**：异常由 `push_followup` 捕获记审计 `followup_ledger_writeback_failed`，同 `cc_to_paul`／`cc_group` 隔离模式——台账没写上可事后补（`cli transition` 就是那条路），把一次成功的发送报成失败、让下一班当「待发」重发才是不可挽回的。**「不投影任何点」也写审计**（`non_projection:true`）：否则它与「这一步压根没跑」在审计里长得一样，正是本次要补的那个洞的形状。

⑷ **2b.4 两组断言各配反向对照**：负样本＝`采购部#21` 走完 4bis ⇒ 五份域文件**字节数逐份相等**、exit 0、输出含固定一句；**反向**＝换 `财务部#17` 跑同一段逻辑，财务域必须变大且**其余四份必须一字节不变**（写串域即 OEM 隔离越界）——没有这条，「字节数不变」可能只是因为这段逻辑什么都不做。正样本＝模拟回件 ⇒ 全库 `已签认` **恰一行**（不是「至少一行」）、带 `evidence`、`fact_date` ＝ 回件落档日、`recorded_on` ＝ 今天且**断言两者不相等**（回件日取 `2023-05-18` 这个绝不可能等于运行日的过去日期，使该断言无条件成立）；**反向**＝唯一变量去掉 `--evidence` ⇒ exit 3、字节数不变、全库无 `已签认`、点仍 `在途`。🔑 两组都**逐字调 SKILL.md §三 4bis 写的那两条命令**（经 `cli.main`，与无头班次跑的是同一段代码），不另写「测试专用的 4bis 逻辑」。📌 **幂等边界如实登记**（`test_2b4_正样本_已签认后重复跑一次不产生第二行`，断言的是**会**产生第二行）：台账是事件账，跑两次就是两次事件；写成断言是为了让这件事被看见 —— 4bis 调用方须按信只跑一次。

**验证手段**：`pytest` 新增三件 ＝ `test_coverage_point_ledger_cli.py` **18 passed**／`test_coverage_point_ledger_writeback.py` **7 passed**／`test_ledger_writeback.py` **17 passed**；全量回归 底座 **553 passed／1 skipped**、wecom-aibot-service **812 passed／1 skipped**，零回归；`openspec validate coverage-point-ledger --strict` ＝ `Change 'coverage-point-ledger' is valid`；CLI 实跑 tmp 台账四条：`在途` exit=0 ／ `已签认` 缺 `--evidence` ⇒ `[REJECTED]…D5…` **exit=3** ／ 补 `--evidence` 后同一条 ⇒ `[OK] fact_date=2026-09-12，recorded_on=2026-09-07T16:38:24+08:00` exit=0（事实日 ≠ 写入日，两者并存）。
**真台账只读实跑**（在 `OP-0907-AL` 23 点入账后复跑，四个域全覆盖）：`财务部#11` ⇒ 4 点全 `已签认` 且带真实 evidence 路径；`采购部#6` ⇒ 3 点 `已签认`；`质量部#5` ⇒ 3 点 `在途`；`IT部#4` ⇒ 2 点 `在途`；`财务部#17` ⇒ `FI10-G-07` `在途`；`采购部#21`／`财务部#16` ⇒ `[NONE] 本信非台账投影，零回写`、exit=0。**只读，未往真台账写一个字节**。

⚠️ **两条如实登记（不是缺陷、是输入侧还差一步）**：① 安装件未改（见 ⑵）；② 2b.3 的连线输入——起草期把 `部门#N` 写进点的 `letters`，或信 frontmatter 写点 id——**起草侧尚无机器守**（近 27 封 `决策点:` 覆盖率为 0 是同源问题，见 §5.3）。今日真身 `财务部#17` 走得通来源⑵；但今天新起草的信若两者都不写，本接线会安静判为「非投影」（**有审计留痕、可查**）。补法属起草侧，归 §5.3 与 2b.5，本件不越界处理。

▶ **下一棒**：2b.5（真实 `财务部#17` 回件触发全链验收，Cowork＋他）；ff 属 🟡，待 Shao Peishen 一字母。

# §一 #439 · 七次外置（2026-09-08，`OP-0907-AK` 收口：2b.2 判据改判成因全文）

> 本节不是按 K2 腾行长外置的，是**成因太长、行内只留一句指针**。行内那句＝
> 「`定时任务源码/` 系单向镜像、MUST NOT 手改（成因见七次外置节）」。

## 一、结论（判据改判，Shao Peishen 2026-09-08 答 (a)）

**2b.2 原判据作废**：原文写「源码件 `0-学习与工具/skills源码/huijian-chaijian-patrol/SKILL.md` 与副本
`0-学习与工具/定时任务源码/huijian-chaijian-patrol.SKILL.md` 同步改」。

**改判后**：改 **源码件 ＋ 真身 `~/Claude/Scheduled/huijian-chaijian-patrol/SKILL.md`** 两处
（真身位置由 `0-学习与工具/工具-定时任务源码备份.py::DEFAULT_SOURCE_DIR` 取、不猜路径）；
🔴 **`0-学习与工具/定时任务源码/` 那份 MUST NOT 手改** —— 改完真身后它由回镜作业自动跟随。

## 二、成因（实证，非推断）

回镜脚本 `工具-定时任务源码备份.py` 首部第 9 行自陈：**「本脚本只从真身读、只往镜像写，
结构上不提供任何反向路径」**，方向 ＝ `~/Claude/Scheduled/`（真身）→ `0-学习与工具/定时任务源码/`（镜像）。
同目录 `README.md` 另有一句「改镜像不生效」。

⇒ 往镜像里写，写进去的是一个**下一轮回镜必被覆盖的中间态**。实际发生的三步：

1. `51d8ac7`（2026-09-07，`OP-0907-AK`）给镜像 `+17` 行 4bis，随分支 ff 进 master。
2. **`d7fb6a2`「docs(定时任务镜像): sweep 自动核对并更正真身↔镜像差异」把那 17 行原样删掉**
   —— `git show d7fb6a2 --stat` ＝ `1 file changed, 17 deletions(-)`。回镜作业发现镜像与真身
   不符，按它的设计把镜像「改正」回真身当时的旧内容。
3. Shao Peishen 按当时给出的命令从**镜像**拷到真身 —— 拷过去的已是被回退掉的旧内容。
   净结果：真身与镜像双双为旧，`skills源码/` 是唯一还带 4bis 的那份。

🔑 **这件事最要紧的一点：它不是故障，也不产生任何告警。** 提交信息写着「自动核对并更正差异」，
`git log` 上看完全正常；被删掉的 17 行不会向任何人报告它们曾经存在过。
⇒ **往机器生成物里写 ＝ 写了等于没写，而且没有信号。** 与本包 D3「跨域聚合不得落盘（含缓存）」、
D5「`已签认` 只能由真实回件驱动」同族 —— 三条防的都是「错误不产生信号」。

⚠️ **一处自陈更正**：`OP-0907-AK` 2026-09-07 首次交付时，把「两份 `cmp` 逐字节一致」写成了 2b.2 的
验证手段。**那句话在写下的那一刻是真的**（两份确实一致），但它验的是上述那个中间态 —— 
**验证手段选错了对象，不是验证没做**。真正该验的是真身。

## 三、改判后的验证手段（真身，2026-09-08）

Shao Peishen 手动拷贝（CC 侧两个 shell 入口均被 auto mode classifier 拦下，未绕行），CC 侧独立复核：

  · `~/Claude/Scheduled/huijian-chaijian-patrol/SKILL.md` md5 ＝ `27fd3e1c002394786e1a7e8c98f1cfcb`、
    **17,962 B**，与源码件 `cmp` **无差异**
  · `grep -c 4bis` ＝ **1**；五小步关键串逐条命中：`points-by-letter` 1 处／
    `本信非台账投影，零回写` 2 处／`不得因为…就去建点` 1 处／`台账回写：已签认` 1 处／
    `退出码非 0 一律` 1 处
  · 落位实测：`4. **回灌与销行**`(L63) → `4bis.`(L67) → `5. **需 Shao Peishen 项自动登记 §四**`(L84)
  · 乱码哨兵：无 U+FFFD，中文回读抽验通过

判据改判 commit `dd0266d`（分支 `claude/op0907ak-ledger-writeback-c84203`，已 push，🟡 未 ff）。
