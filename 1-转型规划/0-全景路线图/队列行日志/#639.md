---
title: "队列行日志 #639（§一）"
created: 2026-09-21
status: 生效
用途: 跨桌任务队列 §一 #639 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #639 · 外置段（2026-09-21 外置，工具第 1 批，2 段，原文原样）

> 外置人 Cowork-AB基线-Win-0921-A；外置前整格 4423 B、共 4 段；本批外置第 2、3 段；本格保留第 1、4 段。

🔴 **2026-09-21 `Win-0921-A` 并入（Shao Peishen 当轮答 `2a`）：上下文闸 15 秒轮询有系统性低估，250k 硬线会漏判。** `hooks-posttooluse-context-meter` 每 15 秒写一次 `reports/context-meter/<sid>.json` 的 `lastContext`，`工具-opener批处理执行v2.ps1` v2.6 据此判 150k／250k。**实测同一条泳道：闸读到 223,309，`工具-Token用量度量.py` 算出 230,874，差 7,565**；当日四条逐条比对闸均偏低 ⇒ 真实峰值可落在两次轮询之间而不被截断。修法自定（收紧周期／改记录并读取峰值而非瞬时值／两者并用），**须给出改后误差的实测数，不接受自陈**；🔴 阈值 150000／250000 本身是既定政策值，不准改（属 🟡 改口径阈值，永不代办）。

━━━

📌 **本行已派**：`B-0921_止血` A2（`OP-0921-I`，派单件 `1-转型规划/0-全景路线图/派单件-【CC】639-泳道读取面切分-2026-09-21.md`），串在 A1（`#637`＋`#638`）之后——同触碰 sweep 家族。 ；OP-0921-I 2026-09-21 完成：hooks-context-meter-lib.ps1（状态读写/档位换算/usage 解析共用函数）＋新 Stop 读取面 hooks-stop-context-meter.ps1（补齐 PostToolUse 只在工具调用后触发、纯文本收尾轮次 usage 永远读不到的盲区）＋test_hooks-stop-context-meter.py（28 pass，含最小场景实测证据：仅 PostToolUse 残差 15,000，加 Stop 读取面后残差归零）。分支 claude/op0921i-639-lane-readface 已 push，未 ff。🛑 .claude/settings.json 的 Stop 键新增一项被 ISO 26262 受控文件规则拦截（CC 写不了），已出人工安装单 0-学习与工具/hooks/README-639安装步骤.md，等 Shao Peishen 按单加 4 行 JSON 后本修法才实际生效——在此之前闸仍是旧行为。

---

# §一 #639 · 外置段（2026-09-23 外置，工具第 2 批，1 段，原文原样）

> 外置人 Cowork-AB验收-Win-0921-A；外置前整格 4995 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

🔴 **2026-09-21 `Win-0921-A` A/B 验收 · A 面（现状基线）已取到，四个数全部机器核、非自陈**（泳道 `OP-0921-K`／session `dedc9bbf`，批次 `B-0921_AB基线`，载荷＝逐类核 sweep 19 类常驻告警的消费者与夹具，纯读活）：① **工具调用 66 次**（Bash 23／Grep 20／Read 16／Glob 4／Write 2／Edit 1）；② **完全相同的 (工具,目标) 重复 10 次＝15%**，其中同一文件重读多余 9 次／Read 共 16 次（56%）；③ **上下文峰值 163,208**；④ **cache_read 6,512,264**（🔴 `requestId` 去重口径；未去重会虚高约 1.76 倍，同日已实证）。派生：`cr/工具 98,670`、请求 60、开场底噪 53,461、thinking 152,389 字符、tool_result 69,508 字符。产出面机器核 `git diff --name-only master...claude/op0921k-ab-baseline-run-a` ＝ 只有 `1-转型规划/0-全景路线图/核查-sweep告警消费者与夹具-op0921k.md` 一份，纯读活口径遵守。 ⚠️ **该泳道自陈不实（今日第五次）**：派单件 §三 要求收工报三行（`RUNTAG=`／`工具调用自计数=`／`重复自陈=`），实际只在正文里带了 RUNTAG，后两行完全没有，按派单件「缺一行即判 PARTIAL」它却报了 `OPENER_DONE`；本次不影响结论，因为四个数全走 transcript 机器核。 🔴 **A 面暴露一个载荷选择错误，如实交代**：这件纯读调查活的重复率只有 15%，而最贵的两条**建造**泳道是 30%（`d5346e25`）与 47%（`d8a76b23`，Edit 31 次只改 5 个文件）。⇒ B 面照跑这件活最多只能证约 20% 收益（66→56 次调用），**证不出此前按曲线算出的 41–67%**。要验「大户能否有效改善」，还需另起一对**建造活** A/B（重复率高的那一类才是大户）。 📌 去重守 `0-学习与工具/hooks/hooks-pretooluse-dedup-guard.ps1` 已建成并逐条实测通过（首次放行／同文件未变第二次拒／文件已变放行不误拦／同文件第 3 次 Edit 拒／逃生阀 `ZHUOPIN_DEDUP_GUARD=off` 放行／钩子异常 fail-open），待 Shao Peishen 按 `0-学习与工具/hooks/README-安装单-0921合并.md` 注册进 `.claude/settings.json`（受 protected-paths `mode: block`，本方不旁路绕）。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **2 段已于 2026-09-21 外置**（K2 口径，原文原样、可 grep，文件 md5:929ce10b，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#639.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 Cowork-AB基线-Win-0921-A）。

---

# §一 #639 · 外置段（2026-09-23 外置，工具第 3 批，5 段，原文原样）

> 外置人 Cowork-换尺子-Win-0921-A；外置前整格 5549 B、共 8 段；本批外置第 3、4、5、6、7 段；本格保留第 1、8 段。

✅ **2026-09-23 A/B 验收完成，真实前后数据（三样本，全走 transcript 机器核）**。载荷＝同一份派单件一字未改（逐类核 sweep 常驻告警的消费者与夹具，纯读活）。**① A 面 `dedc9bbf`（09-21，无守）**：工具 66／峰值 163,208／`cache_read` 6,512,264／重复 10 次 15%／13.7 分钟。**② B 面首跑 `11d565ad`（09-23 08:03，守未生效——作废但可作第二个无守样本）**：工具 67／峰值 171,978／`cache_read` 7,654,457／重复 3 次 4%／17.5 分钟。首跑作废的硬证据：`.claude/settings.json` 是 git 跟踪文件、泳道 worktree「新建自 master」，而去重守进 master 的时刻是 **08:17:53**（`ab9aaeed`）晚于 worktree 建立的 **08:03:34**；`hooks-audit.jsonl` 该窗口内 dedup-guard 零条。🔴 **由此立机制口径：任何 `.claude/settings.json` 钩子注册必须先进 master，泳道才带得上；起批前必须机器核 `git show master:.claude/settings.json` 含该钩子。****③ B 面重跑 `016d88fe`（09-23 08:30，守已生效）**：工具 **36**／峰值 **103,745**／`cache_read` **2,959,200**／重复 1 次 **2%**／10.2 分钟。**对照两个无守样本均值（6.51M＋7.65M）／2 ＝ 7.08M ⇒ `cache_read` 降 58%**；对 A 面单样本降 55%；工具调用 66→36 降 45%；峰值 163,208→103,745 降 36%；thinking 字符 152,389→45,620 降 70%。🔴 **守的行为核实（第一判据）**：audit 本棒窗口内有 dedup-guard 条目（Bash pass 34／Read pass 8／Bash dup-observed 1），**零误拦**（无「目标已变却被拒」）。⚠️ **如实标注样本量**：有守样本 n=1，两个无守样本之间自然方差 17.5%（6.51M vs 7.65M），有守样本低于两者 55%／61%，远超自然方差，但仍建议再取一个有守样本加固。⚠️ **另一处如实标注**：三次运行的工具分布剧烈漂移（Read 16／1／2，Bash 23／60／33），**按工具分类的重复率不可作单次 A/B 判据**；总量三数（工具调用数／峰值／`cache_read`）才稳。

━━━

🔴 **2026-09-23 `Win-0921-A` 换尺子（Shao Peishen 答 `1a`／`2a`／`3a`）：治理主指标由 `cache_read` 改为 Opus 请求数 ＋ 额度曲线斜率。** 新工具 `0-学习与工具/工具-额度与Opus对账.py`（只读，8.1 KB，**另起一件不改 `工具-Token用量度量.py`**——后者有 26 KB 单测在飞，两个口径并存互为交叉核对）。它覆盖此前两个盲区：⑴ **Cowork 侧** `%APPDATA%/Claude/local-agent-mode-sessions/**/.claude/projects/*.jsonl`；⑵ **额度真身** `%APPDATA%/Claude/plan-usage-history.json`（`{t, u:{fh 五小时窗%, sd 七日窗%}}`）。🔴 **Cowork transcript 路径 300+ 字符，`glob` 会静默返回空**——必须 `\\?\` 长路径前缀 ＋ `os.walk`（本方第一版正是栽在这里扫出 0 份）。

━━━

**实测对账（09-16~09-22）**：CC opus／CW opus／额度净增 ＝ 09-17 147／25／+28｜09-18 70／6／+25｜09-19 98／0／+14｜09-20 0／0／+8｜09-21 0／0／+5｜09-22 0／0／+3。反解量级：**Sonnet ≈ 0.7 点/100 次请求**（09-20：1,121 次 → +8）；**Opus ≈ 15 点/100 次**（09-17：172 次扣 Sonnet 份额后 ≈ +25）⇒ **Opus 单位成本约为 Sonnet 的 20 倍**。🔴 七日窗滚动、净增含滚出项，本估只作量级参考，**不作精算、不写进任何判据阈值**。

━━━

⚠️ **本方上一轮「额度主导项就是 Opus 请求数」的说法需收窄，如实更正**：09-20／21／22 三天 Opus 全为 0，额度仍 +8／+5／+3、合计 16 点 ⇒ **Opus 归零不等于止血**，Sonnet 到千次量级同样要管。判据改为：**Opus 请求数第一、Sonnet 请求数第二、`cache_read` 只作诊断不作优先级依据**。

━━━

📌 **仍未覆盖的盲区**：Cowork **主对话**（桌面端这条会话）transcript 不落本机盘，量不到。代理指标＝`工具-额度与Opus对账.py --snapshot`，**重大批次前后各取一次，差值即该批次（含主对话）的真实额度成本**。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-23 外置**（K2 口径，原文原样、可 grep，文件 md5:79e63cb5，工具第 2 批）见 `1-转型规划/0-全景路线图/队列行日志/#639.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 Cowork-AB验收-Win-0921-A）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。

---

# §一 #639 · 外置段（2026-09-23 外置，工具第 4 批，3 段，原文原样）

> 外置人 Cowork-AB收口-Win-0921-A；外置前整格 4401 B、共 6 段；本批外置第 3、4、5 段；本格保留第 1、6 段。

🔴 **Phase 8（拆 534 KB 巨石）按他 `3a` 撤销**：前提「泳道整读大文件」已被三条泳道实测推翻（最大单次工具结果 8,827 字符，全会话 tool_result 合计 6.9~15 万字符，一次整读都没有），且 `fe41d3de`（09-16）早已装「Read/Bash 读侧大文件守卫 >24KB 未分段即拒」，挂在 `hooks-pretooluse-queue-read-guard.ps1` 上。🩸 **血教训（跨项目遵守）：我们自己写错的结论会被外部报告当证据引回来。** Antigravity 复盘的 Phase 8 整段建立在本方 09-21 写进接力卡的错误「真结论」之上；外部意见必须逐条回到第一手实测核验，**不得因为它引用了我们自己的话就当已核**。

━━━

🔴 **2026-09-23 A/B 收口：`n=2` 之后必须撤回本方先前报的降幅，并换判据。** 四样本（`requestId` 去重，全 transcript 机器核）：**无守** `dedc9bbf` 工具 66／峰值 163,208／`cache_read` 6,512,264／重复 10 次 15%；`11d565ad` 工具 67／峰值 171,978／`cache_read` 7,654,457／重复 3 次 4%。**有守** `016d88fe` 工具 36／峰值 103,745／`cache_read` 2,959,200／重复 1 次 2%；`7de82722` 工具 52／峰值 166,297／`cache_read` 6,134,171／重复 1 次 1%。 ⇒ 无守均值 7.08M（方差 17.5%）；**有守均值 4.55M，但两样本相差 2.07 倍（方差 107%）**。**有守 vs 无守 ＝ 降 36%，本方 2026-09-23 早先报的「降 55–58%」系单样本比对所得，现撤回。** 🔴 **`n=2` 的真正结论：有守样本之间的噪声（107%）大于要测的效应（36%），按此噪声水平测出 36% 效应约需 n≈35，不现实 ⇒ `cache_read` 降幅这个指标我们测不出来。** ✅ **但守本身确凿有效，且有零噪声证据**：四样本重复率 15%→4%→2%→1%；audit 实拦 **17 次**（Read 10／Edit 4／Grep 3），**零误拦**。 🔴 **换判据（与 09-23 换尺子同一教训的第二次应用）**：验收改用**守的拦截次数**（可数、无噪声、无需 A/B），`cache_read` 只作诊断。**指标选错，再多样本也白跑。**

━━━

📌 **同日实测：150k 软线仍然只记峰值不终止**。09-23 四条泳道峰值 209,680（`sc2-A3`，PARTIAL）／188,591（`fi2-A2`）／184,881+（`q2-A1` 在跑）／166,299，**全部越 150k，无一条按根 `CLAUDE.md` §3「越 150k ⇒ 完成当前里程碑即收尾」执行**。这是比去重守更大的一刀，待起。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **5 段已于 2026-09-23 外置**（K2 口径，原文原样、可 grep，文件 md5:ebbf6614，工具第 3 批）见 `1-转型规划/0-全景路线图/队列行日志/#639.md`；本格只留原第 1、8 段（共 8 段）＋本指针（外置人 Cowork-换尺子-Win-0921-A）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。

━━━

🔴 **2026-09-23 3a 收口（`Win-0921-A`）：先撤回本格上一段的一句话。** 上段写「四条泳道全部越 150k，**无一条**按根 `CLAUDE.md` §3 收尾」——**这句不准，据实更正**。用 `reports/probe-0921/ctxcurve.py` 从 transcript 重建上下文曲线（`requestId` 去重）逐条核：09-20~09-23 十条无头泳道里 **9 条越过 150k，而这 9 条全部自行收工**，越线后仍跑：`AB基线-A1` 1 分／`AB对照-A1` 3 分／`有守2-A1` 3 分／`止血线-A2` 4 分／`fi2-A2` 7 分／`q2-A1` 7 分／`sc2-A3` 7 分／`缩编线-A1` 9 分／`止血线-A1` 14 分（**最短 1 ／中位 7 ／最长 14 分**）。⇒ **真缺陷不是「不收尾」，是「收尾之前那段无界」**：越线后峰值仍冲到 210k／242k／249k，多烧 30~100k。

🔴 **同一批数据还查出：250k 硬线是死代码——已观测样本里一次都没触发过。** 峰值最高三条 248,640（`止血线-A1`）／241,850（`缩编线-A1`）／209,680（`sc2-A3`）**全部落在 250k 以下**。⚠️ 但**不许因此把硬线降下来**：那两条 242k／249k 的泳道是**正常跑完**的，降到 220k 会砍掉真活；且 150000／250000 是 Shao Peishen 2026-09-16 既定政策值，改它属 🟡 改口径。**本次一字未动。**

✅ **3a 实做（按上述实测定形，不是按原设想）**：`工具-opener批处理执行v2.ps1` 新增 `-ContextGraceMin`（分钟，默认 **15**，`0` ＝关闭）。150k 越线**不立刻杀**（会砍掉离收工只剩几分钟的活），只起一个表；**跑满期限仍在跑 ⇒ 判定真挂住，`taskkill /T /F` 整树、判 `PARTIAL`、不补问**。默认 15 分的取值依据即上列实测：**不咬任何一条已观测到的常态泳道（最长 14 分），只咬真挂住的那种**——把一条无界的尾巴封成有界，而不是把它砍短。
✅ **终止即排薄续棒**：软线超期与既有 250k 硬线**两种终止统一**落一份 `<lane>-<id>.续棒.json`（泳道／事由／峰值／session／分支／worktree／泳道日志／续法），`summary.txt` 末尾新增「本批因上下文终止、续棒待排 N 条」＋逐条路径。**这是本闸的消费者**（规则「新建告警必须自带消费者」）——此前 250k 硬线只 `taskkill`、什么都不排，被掐掉的活静默掉在地上。载荷里写死一句：**续棒须新开 session，`--resume` 会把被掐断那 N k 上下文原样载回来，等于没转场**。写失败 fail-open，不连累本闸。
✅ **顺手修两个自带的坑**：⑴ 续棒载荷初稿写 `$op.Branch`／`$op.Worktree`——`$op` 只有 Id/Title/Paste/Lane/Text/Model 六个属性，PowerShell 对不存在的属性**不报错、返 `$null`**，两个关键字段会恒空而无人察觉（正是本项目判为「参数被接受却不生效，比被拒绝更危险」那一类），改用解析出来的真值并显式清零防跨 opener 残留；⑵ **worktree 名加 slug 校验**——09-23 本批首起实撞：`worktree：☑（v2.ps1 起 claude 前脚本建、收工自删）` 漏写首项名字，原正则「切到第一个逗号／右括号为止」把整段说明文字当成名字，**真建出一个名叫「v2.ps1 起 claude 前脚本建、收工自删」的 worktree**（含空格与中文顿号）并登记进 git。现判 `FAIL(worktree-name)`、不建。该畸形 worktree 已于本次三查后清除（脏文件 0／本地 `030720b7` ＝ `origin/claude/op0923f-q2-accept-set`／无 `reports` 残留）。
🔬 **回归**：`test_工具-opener批处理执行v2.py` 新增「软线兜底期限_639」类（期限内收工不误伤／满期限终止并落载荷／`0` 关闭退回老行为／变异守卫抽段必转红）＋硬线落载荷一例＋worktree slug 一例，narrow 跑 **25 passed / 0 failed**（含既有「上下文闸消费者_627」与全部 worktree 用例）。**首轮曾 1 例转红，原因是本方在跑测期间改了主工作区的 `工具-落库sweep.py`、被泳道第三道闸（主工作区新增非白名单脏文件）逮住——闸没错，是本方并发编辑；改完再跑即全绿。记一笔：跑无头回归期间不许动主工作区。**

━━━

🔴 **2026-09-23 尺寸闸计量单位修正：那 190 B 棘轮破线是假的，破的是尺子不是基线。** `zhuopin-lane-watch/SKILL.md` 磁盘 30,516 vs 棘轮基线 30,326 —— 差值 **190 ＝ 该文件行数**，`git status` 视其为**未修改**，`git cat-file -s :<path>` 读数 **30,326，与基线一字不差**。根因在机器守自己：`工具-落库sweep.py` 三道尺寸闸（`claude_md_root_over_cap`／rules 单份与合计／`lane_watch_skill_over_cap`）一律 `stat().st_size` ＝ **工作区磁盘字节**，而三个阈值都按 **git blob 字节**写下（`LANE_WATCH_SKILL_BYTE_CAP` 注释「完工实测 30,326 B」即 `git cat-file -s` 读数）。本仓工作区 CRLF、blob LF ⇒ 磁盘字节恒比 blob 多「行数」那么多，**每道闸的余量被行数悄悄吃掉**：根 `CLAUDE.md` 12 KB 上限实收 12,219 B；`.claude/rules/` 30,720 上限实收 30,545 B；lane-watch 更严重——它是 **reject 型**闸，于是**任何文件清单含这份正本的批次被永久拒绝落库**。✅ 已修：新增 `_git_bytes()`（CRLF 归一后再量），三处改走它。**阈值一个没改、棘轮一字未抬**——只把执行时的计量单位修回阈值被写下时的那一个。修后现测：lane-watch **30,326 ＝ 阈值，未超**；根 `CLAUDE.md` 12,075。🔬 回归 19 passed（新「尺寸闸计量单位_639」3 例：CRLF 按归一后量／纯 LF 两法一致／**源码级断言三道闸不得改回 `st_size`** ＋ 既有 16 例）。判词：**尺子和刻度必须同一套单位，否则越守越紧，紧到守住的是尺子的毛病。** 这是 CRLF 第四次骗人，前三次骗的是人，这次骗的是机器守。

━━━

📊 **2026-09-23 A/B 判据键的修正（第二版 `reports/probe-0921/ab5.py`，取代第一版 `ab4.py`）**：第一版把 Grep 的判据键取成「pattern」一项，「同一 pattern、不同路径」被误计为重复，重复数虚高。第二版**逐字照抄去重守自己的键**（`Read|<path>`／`Grep|<pat>|<path>`／`Edit|<path>`／`Bash|<cmd>`，见 `hooks-pretooluse-dedup-guard.ps1` 第 97~108 行），并用守自己的状态文件 `reports/dedup-guard/<sid>.json` **交叉验算**：本脚本重建的工具调用数 35／51／58 ＝ 守记 35／51／58（业务两条 76 vs 75、78 vs 77，差 1 ＝ 被拦那次）——**不自证，用守的账核我的账**。修正后（可拦＝Read/Grep/Edit，Bash 守按设计从不拦）：**无守 `dedc9bbf` 10/37＝27%**；`11d565ad` 0/5；有守1 `016d88fe` 0/2；有守2 `7de82722` 1/15。业务三道（均有守）`fi2-A2` 6/36、`sc2-A3` 8/26、`q2-A1` 3/17，拦截 5／5／2，加有守2 的 1 ⇒ **09-23 四条有守会话共拦 13 次，零误拦**。🔴 **「重复率」只配当诊断，不能当判据**：本口径看不见「两次读之间文件变没变」，守看得见（mtime＋size），所以业务泳道那 17%／31%／18% 里大半是**文件真改过的合法重读**。**只有拦截计数是干净的**——这正是 `1a` 改判据的实证依据。
