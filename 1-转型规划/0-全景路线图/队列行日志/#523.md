# 队列 §一 #523 · 行日志（K2 外置件）

> 本件收纳该行状态格外置出来的历史段，原文原样、未改写；行内只留首段＋末段＋指针。


## 2026-09-09 K2 外置（CC 看护者 `OP-0909-Q`，批 `B-0909_六泳道机制批`；Shao Peishen 当日答 `2a`）

外置原因：本批 `OP-0909-W` 回写后该行状态格达 5522 B、超 `ROW_LENGTH_CAP_BYTES` 4096 B（2026-09-11 起由告警转阻断，届时该行任何写入都被硬拒）。以下为原状态格中间段，**原文原样**；md5:`dc4e8677b92a4395a8306c06e40be18e`。

🔴 **本次未造成损害的唯一原因是巧合**：9 行**恰好全是 `[S:done]`**，而 WIP 只计 open/partial/hold ⇒ 计数未偏。**但机器人一旦写入非 done 行，WIP 就会算少，而 WIP 超限是阻断性的（release 直接拒）⇒ 算少 ＝ 该拦的没拦、超限行照立**，同族＝ `#143` 记的「用法错误不报错，直到下一个人撞上才炸」。 📌 **发现路径值得记（本行立行的副产品）**：本缺陷是在 2026-09-09 值周清扫中被**一次误报**牵出来的 —— 值周方用自写正则 `\[S:…\]\[D:…\]` 解析 digest 输出，把这 9 行吃掉了，于是先得出「`--digest` 漏 9 行」这个**错误结论**并写进了 §二 批次；复核 digest 原始输出才发现**digest 一行没漏（178 条与真身一致）**，漏的是解析方的正则。🔑 **判据：`[S:x][D:y]` 不是恒定形态，`[D:]` 可缺 ⇒ 任何解析 digest 的正则都必须容忍 `[D:]` 缺失，否则会静默丢行。** ⚠️ **同一天第二次栽在同一处**：上一轮刚立「自写解析不可替代官方工具」，转头就用自写正则去解析官方工具的**输出**——**教训要收窄为：不只是『别自写解析真身』，是『别对任何结构化输出假定它的形态恒定』。**

🔴 **2026-09-09 16:5x 同日订正（Shao Peishen 答 `1a`）：本行降为 P2，并入泳道看护批 `B-0909_六泳道机制批` 的 `513523-editlock-guard` 那一棒顺手做（与 `#513` 同触碰同一份写侧校验段），不单独派棒。** **订正的是后果描述，不是缺陷本身** —— 立行时引用的「SKILL 写死『所有 pause／check-heartbeat 一律带 --no-notify，无一例外』」是 `zhuopin-lane-watch/SKILL.md` 里**折叠在 `<details>` 中、标注「仅供追溯，不再适用」的旧版原文**（2026-09-02 事故后加、2026-09-06 前有效）；**现行口径是 2026-09-06 改判**：推送目标已代码固定为 `WECOM_WEBHOOK_URL_OPS`（Shao Peishen ＋ IT 陈承二人群），未配置或失败一律 fail-closed、绝不回落业务群，`--no-notify` 人守约定已作废、语义收窄为「联调时连运维群也不想发」。⇒ **最坏后果＝多发一条到二人运维群，不是 17 人的跨部门群**；性质由「纪律与机器的死结」降为「三个子命令参数不一致」。🔑 **判据（本次立）：读到带日期、带「旧版」标记或折叠在 `<details>` 里的段落，先确认它是不是已被取代** —— 历史原文与现行口径在同一份文件里并存是本项目常态（「历史记录不追改」的直接后果）。⚠️ **本日第三次同根**：前两次是队列行内的过时状态字样（`#462` 指错分支、10 行头部过时），本次是 SKILL 的折叠块 —— **同一个根的三种载体：队列行、生成器填的字段、文档的历史折叠块。**
