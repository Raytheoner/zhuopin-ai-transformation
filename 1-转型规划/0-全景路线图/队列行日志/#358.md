---
title: "队列行日志 · §一 #358（平台 audit 留存与归档策略）状态列历史回写段"
created: 2026-09-08
status: 生效
用途: K2 行长口径外置件。§一 #358 状态列 2026-09-08 实测 9,333 B > 4,096 上限（2026-09-11 起 release 阻断），按「行内留首段＋末段＋指针、中间段原文原样迁出」处理
外置会话: Cowork 业务总线 OP-0908-A
外置段数: 12（原状态列共 14 段，以 U+2501 三连分隔符计）
中间段 md5: 9568457bc9ccbd168a7fed763d92703a
---

# 队列行日志 · §一 `#358` 状态列历史回写段（原文原样）

> 🔴 **本件是原文原样、一字未改、可 grep**；行内只留首段＋末段＋本件指针。**任何细节以本件为准。**
> 外置前整格 **9,333 B**；外置后行内约 2.2 KB。中间段 md5 `9568457bc9ccbd168a7fed763d92703a`。
> 首段（保留在行内）＝「待领·待定级」；末段（保留在行内）＝ 2026-09-08 条件放行尝试与停项结论。

📌 **并入审核（协议〇.10）已于取号前完成**：对两份队列真身 grep `轮转`／`磁盘`／`留存`／`logrotate` 四词，**无可并入的既有行**，故新立。 ━━━ ⚠️ **立行时点 WIP 21 → 22，恰为上限 22 满格**——下一条 `[D:机]` 行会被 `release` 硬阻断，须先清扫或再议上限。 ━━━ 🔴 **本行刻意未按 suggested task 的原题「评估 .51 保供看板审计日志无轮转」立**：Shao Peishen 2026-08-20 答定夺 (a)，题目由「单机日志轮转」抬为「平台 audit 留存与归档策略」，那 309MB 降为**首个暴露点**而非问题本身。 ━━━ ⚠️ **刻意未在 OP-0820-C 运行中途插队执行**（其部署冒烟 tasks 当时未走完，且它刚因中途改 `sources.py`、后发 lazy import 拿到已加载旧模块吃过一次亏）。 ━━━ 📐 **design 草案已落包，待审（CC `OP-0907-X`，2026-09-07，批 `B-0907_R`）**：变更包 `openspec/changes/audit-retention-archive/`（`proposal.md`／`design.md` status:待审／`tasks.md`／两份 delta spec：新建 `audit-retention` ＋ 改 `audit-hash-chain`）；`openspec validate audit-retention-archive --strict` ＝ valid；commit `e5dfa26`（分支 `claude/op0907x-audit-retention-358`，**未合入 master**）。🔴 **按本行「design 审通过前不得直接实现」，`audit/` 下代码零改动。** ━━━ **核心结论：归档不是「轮转」，是「滚动封存 roll-and-seal」——换本子，不撕页**（热文件整份改名封存为不可变段、另起新热文件，无一行被移动改写删除）。 ━━━ 🔴 **最硬的一条来自本仓库实证，不是推演**：2026-07-28 归并审计文件时**重算过哈希链，且 `verify_chain()` 通过**（`进度编年-CHANGELOG.md` 第 43 行原文「hash 防篡改链按合并后顺序重新计算，`verify_chain()` 核验通过」）⇒ **重算过的链，验证结果永远是 ✅**，哈希链对「由持有写权限者执行的整理动作」**零防御力**。⚠️ **那次重算本身是已批准的正当修法、不是事故**（真实缺陷是审计流因服务与脚本跑在两个 checkout 而分裂成两份、闭环无法在单一文件内还原，见队列归档 #126 行；原件亦另存未删）—— **本包援引它不是追责，是取它作存在性证明**：动机完全正当的一次整理就足以无痕重写整条链而校验照过 ⇒ **靠「别去重算」这种纪律拦不住，必须在机制上堵死**。故 D3 禁止封存重算 `prev_hash`，D9 禁止跨文件合并（2026-07-28 干的恰是「归并两份」，照先例走会在 OEM 隔离红线 3 上开洞）。 ━━━ **十条实测事实**（本次开工现读）：⑴ `audit/` 四个文件对 `rotat`／`retention`／`max_bytes` **零命中**（本行原述属实）；⑵ `ClickHouseSink.write()`／`read_all()` 两方法体**仍全是 `NotImplementedError`** ⇒ 本包不得依赖它；⑶ 走 audit 的构造点 **42 处**（`AuditLogger` 26 ＋ `ConnectorAudit` 16，已去测试），跨 FI1／FI2／QD-B／SC1／SC2／SC7／SC8／wecom-aibot-service ⇒ 本行「四场景同墙」的估计**偏低**；⑷ 同一 `reports/` 下审计／访问痕迹／运行时状态三族**命名不可区分**（`fi2_audit.jsonl`／`fi2_access_trace.jsonl`／`fi2_http_requests.jsonl`／`pending_queue_appends.jsonl`）⇒ **D1 留存类别必须由写方声明，工具 fail-closed、不得从文件名推断**；⑸ 主工作区 `.jsonl` 共 18 份、超 100 KB 仅 3 份，最大两份 ＝ `wecom_aibot_audit.jsonl` 2,704,451 B／5,170 行（静态）、`reports/hooks-audit.jsonl` 1,959,699 B／6,041 行（**仍在写**，2026-09-07 15:23 本地复测）；🔴 **该文件在本 session 期间实测涨了**：14:08 ＝ 1,858,355 B／5,727 行 → 15:23 ＝ 1,959,699 B／6,041 行 ⇒ **75 分钟 ＋314 行 ≈ 251 行·小时⁻¹**（⚠️ 约 40 并发会话下的**峰值上界、非稳态**，定阈值不得直接当常态用）⇒ **D5 的 `OPS` 一档据此已定为纯「跨自然月」触发（单段 ≤18 万行／≤60 MB），不再依赖 `.51` 的 P1 前置**；仍待 P1 的只剩 `COMPLIANCE`／`TRACE` 两档；⑹ 仓库已存在一次手工归档遗迹 `wecom_aibot_audit-split-archive-2026-07-28.jsonl`；⑺ 仓库已存在一处**按 4 周删审计行**的实现（`工具-落库sweep.py::_rotate_hooks_audit_log`，`LOG_ROTATION_KEEP_WEEKS = 4`）——它合法**只因为 `hooks-audit.jsonl` 不是 AI 决策证据**；⑻ 2026-06-13 全盘审计 P0 第二条「链尾哈希外部锚定，否则持写权限者可整链重算、IATF 3 年可信留存不成立」**至今无实现**（≈86 天）；⑼ 外部评审 `supplychain收割与平台底座评审清单.md:55` 点同一处；⑽ 12 条 `git check-ignore -v --no-index` 实测：归档产物全落 `reports/` 已被现有规则覆盖、**本包无需新增忽略行**，但**锚点台账绝不能放 `reports/` 也不能用 `.jsonl`**（两者均被静默忽略、不报错），须落 `3-治理与合规/审计链锚点/` 的 `.md` 或 `.json`；含 1 条反向对照组（口径点台账 `采购域.jsonl` 仍不被忽略，#439 例外未受影响）。 ━━━ ⚠️ **本行转述的 309 MB 与 `.51` 磁盘余量，本包待核实**（本 session 工作区栏＝无，不触碰 `.51`），已落 `tasks.md` §0 两条 ⏭️ 收口批只读项，且**定为 design 审的前置**而非实现的前置——那 309 MB 落在 `*_access_trace.jsonl`（TRACE 类）还是 `baoguan_audit.jsonl`（COMPLIANCE 类），会改 D5 阈值默认值与 D7 优先级，属定 Decision 的输入。**P1/P2 无真实输出前，D5／D6 数值一律视为占位，不得照准。** ━━━ 🔴 **三个 Open Question 均不设默认项，须 Shao Peishen 本人答**：O1「满 3 年后能否删——全项目五处载体一律止于『3 年留存』四字，从未说过是下限还是有效期」（**属合规红线，孙涛不可代**）；O2「封条摘要要不要进 git 做外部锚」（第二问答**「错误在继续发生」**⇒ 按根 CLAUDE.md §5 须标『本项无默认』并**登记 §四**）；O3「WORM／数字签名是否并入本包」。 ━━━ **退休守卫（协议〇.9 措施 B）**：退 `_rotate_hooks_audit_log` 的**自成一体留存策略**（降为调用方、删私有常量 `LOG_ROTATION_KEEP_WEEKS`），**退的是它自己拍板、不是它的行为**；⚠️ 折叠须沿用 sweep 既有的**软依赖兜底桩**形态（`工具-落库sweep.py:401-413`，队列 #306 起），不得让 sweep 硬依赖平台包。不可退三项已逐条论证（`verify_chain` 的 `idx==1` genesis 收紧＝2026-08-04 `fix-a-security-compliance-p0` 的成果，引入续接记录后**极易被顺手放宽成『每段首行都豁免』而把那个绕过原样打开**；`connector_audit.py` 的 D2 物理分离＝TRACE 类的唯一既有依据；`_rotate_sweep_commit_log` 管 `.log` 非审计、不在射程）。 ━━━ ▶ **下一棒 ＝ design 审**。前置两条：先取回 `.51` 的 P1（309 MB 落在哪个文件）与 P2（磁盘余量）真实输出并补进 `design.md` 审前置节（tasks §0.3），再逐条裁 D1–D9 与 O1–O3。**审通过前不得开工 tasks §1 起任何一项。**


---

## 📎 2026-09-08 第二次外置（K2，`OP-0908-A`，改判 `open`→`blocked` 同批）

> 原文原样、一字未改、可 grep；中间 4 段，md5 `282a913585973dca701ea20b04875ff6`。

⏭️ **2026-09-08 条件放行尝试：授权有效、前置全过，但执行通道不可用 ⇒ 该项停项、降回留步态**（Cowork 业务总线 `OP-0908-A`，批 `B-0908_B`，泳道 `358-audit-retention-p1p2`）。Shao Peishen 当日答定夺 2＝(a) 授权取 P1/P2 两条纯只读取证；已按 `#478` 走完 `transfer-out` → `deploy-authorize`（授权记录 #0，绑定转出记录 #0，LAN 探针 status=on），随后照 `zhuopin-lan-closeout` SKILL.md「执行步骤」第 4 项四步执行——**步 1 快照/定位即失败**。🔑 **定性支点＝一个不连网的对照命令**：`ssh -V`（只打印版本号、不建连接、不读密钥）在同一通道里**也是 exit 255、零输出**，而同批次 `git --version`／`ping 192.168.100.51` 均正常、TCP 22 实测可达、私钥存在、`~/.ssh/config` 别名正确。⇒ 不是网络、不是密钥、不是 `.51`，是 **Cowork 这条 PowerShell 通道里 `ssh.exe` 根本起不来**（连 `-vv` 都产不出一个字节 ⇒ 进程在产出任何输出前即被终止）。**无需回滚**：纯只读且快照那步就没跑成，`.51` 上零写入。已跑 `deploy-record --outcome stopped`。🔴 **授权未被消耗、也未被扩大**——仍只覆盖 P1/P2 这一项；换执行体重跑须重走一次 `transfer-out`＋`deploy-authorize`（逐项粒度，无批次级授权、无时间窗）。📎 五行实测表＋对照组＋定性全文见 `1-转型规划/0-全景路线图/取证件-2026-09-08-Cowork侧ssh通道不可用.md`。▶ **本行的真实前置至此明确**：不是「等他答 design 审」，是**等 P1/P2 取回来**；可行执行体＝CC 侧（`本周计划-2026-09-07.md` B-5 载他已加 `/permissions` 允许 `ssh supplychain-server`，⚠️ 该依据系转述、本次未独立复核，CC 首跑前应先用同一个 `ssh -V` 对照自测）。 ━━━ 📎 **历史回写 12 段已于 2026-09-08 外置**（K2 口径，原文原样、可 grep，md5:`9568457bc9ccbd168a7fed763d92703a`）见 `1-转型规划/0-全景路线图/队列行日志/#358.md`；本格只留首段＋末段＋本指针。外置前整格 9,333 B > 4,096 上限（release 已告警，2026-09-11 起阻断）。🔴 **被外置的段里有三处不可只读本格就下结论**：design 草案与十条实测事实（`OP-0907-X` 落包）／三个 Open Question 的原文与「均不设默认项」的理由／退休守卫的三条不可退论证——**审 design 前必须读外置件全文**。  ▶ **2026-09-08 已改派 CC 执行，派单件已出**（Shao Peishen 当日答定夺 1＝(a)）：`1-转型规划/0-全景路线图/派单件-【CC】358审前置P1P2只读取证-2026-09-08.md`（`OP-0908-I`，opener 块 lint enforce 零违规）。🔴 该件第 1 步硬性要求 CC 先跑 `ssh -V` 自测同一个不连网对照命令，打不出版本号即当场停、不猜；第 2 步须**重走**一次 `transfer-out`＋`deploy-authorize`（Cowork 侧授权不继承）。⚠️ **本行真实前置＝P1/P2 取回，不是等他答 design 审**。 ━━━ ✅ **2026-09-08 P1／P2 已取回，本行前置解除**（CC `OP-0908-I`，授权 #0，`.51` 只读零写入）：**那 309 MB 落在 `baoguan_access_trace.jsonl` ＝ `TRACE` 类**，现 557.06 MB／2,657,752 行；`COMPLIANCE` 三份合计仅 1.64 MB（TRACE 占全盘 99.58%）。P2：C: 盘余 70.58 GB／99.40 GB（71%），不构成压力。⇒ **可进 design 审**。📎 实测全文＋三点须审时裁见 `openspec/changes/audit-retention-archive/design.md`，commit `56f6aad`，分支 `claude/op0908i-358-audit-retention-b52b16`（未合 master）。 ━━━ ⚠️ **该 commit 尚未 push**：`git push` 两个入口（Bash／PowerShell）均被 auto mode classifier 拦，已按纪律当场停、未试第三入口。分支 `claude/op0908i-358-audit-retention-b52b16` **目前仅存于本地 ref**（worktree `happy-thompson-017679` 因此未自删）——**接棒者别去 `origin` 上找，找不到不等于工作丢了**。
