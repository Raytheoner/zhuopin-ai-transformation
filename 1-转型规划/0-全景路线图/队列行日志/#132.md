---
title: "队列行日志 #132（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #132 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #132 · 外置前整格原文（2026-09-04 外置，6 段，md5:a4237e63）

🔴 **两条陈旧分支要不要进主线 —— 属业务决策，环境线不得代判（2026-08-27 `OP-0827-D` 形态 2 逐条验后升格，CC-OP0827D）**。本次五条分支已处置三条（`a27` 已合入；`a22`／`a30` 冲突、如实登记回 §一 `#353`／`#380`），**剩两条不是「验不过」，而是「验过了但不该由我合」** ━━━ **⑴ `claude/unified-portal-design-8a2ce3`（统一门户网关，队列 `#162`，3 个提交，落后 1442，2026-08-04）**：干跑 `merge-tree` **零冲突、技术上随时可合**。**刻意不合**——`§四 #121` 你已答 (a) 只卸载，且该行白纸黑字写明「**(b) 先并入 master 再重装……属业务决策、不该由环境修复顺手带过**」，同时又写明「**要不要并入 master 这个业务问题不在本次范围内、也不因卸载而被否决**」⇒ **它至今悬着，而且是唯一挡在 `§一 #410` 巡检判据上线前的那一条**（#410 的验收判据就是「当前环境应恰好报 1 条 `unified_portal_gateway`，把它修掉或并入 master 后自动回落为 0」）。**选项**：**(a) 维持不合**——`#410` 上线后长期报这一条，或用行内豁免标记压住（等于给真问题发免死金牌）；**(b) 派一次专门的门户网关变更包审查，审过再合**——彻底解决 `#410` 的验收前提，代价是要真的把一个 2026-08-04 的未审门户设计重新过一遍（其外部前置企微自建应用凭据＝`§一 #240` 是否已到位也要一并确认）；**(c) 明确判为放弃**——该分支封存不动、`#410` 的判据改写为「已知豁免」。🔴 **本项无默认项**：提出它的 session（本班）收工即无法自我唤醒，且三个选项走向完全不同。**建议 (b)**，因为 (a) 与 (c) 都是在给一条正在建的巡检判据先挖一个永久例外。 ━━━ **⑵ `feat/sc8-baoguan-v2-batch1`（保供 v2 批 1，1 个提交，落后 **2130**，2026-07-07）**：**冲突**（`sc8/baoguan.py`／`sc8/config.py`／`tests/test_baoguan_netting.py` 等 7 个文件）。它是本台面上最老的一条，跨了整整 51 天与两千余个提交，且属 **SC8 业务场景码**——按 CLAUDE.md §5「环境保障线派单边界」，业务场景代码**交业务总线派发**，本班只登记。**选项**：**(a) 交业务总线判「它还适用吗」**（建议）——SC8 保供 v2 后续批次是否已把批 1 的内容做掉，只有业务侧知道；**(b) 直接封存判废**——省事，但在没人看过内容之前判废，与 `#267` 那次丢件同形；**(c) 派 rebase 强合**——不建议，2130 个提交的解冲突等于重写。🔴 **本项亦无默认项。** ━━━ 📎 **两条分支本班一个字节未动，均在本地与 origin 上完好**，随时可继续。 ━━━ 🔑 **顺带一条判据**：这两条之所以能躺 23 天与 51 天，是因为**「分支还在」看起来永远不像一个待办**——它不过期、不报错、不占 WIP，`git branch` 里安安静静。**未合入分支需要一个会催的机制，而那正是 `#422` 第 7 类告警从 2026-08-27 起开始做的事。** 　━━━　✅ **2026-08-27 Shao Peishen 拍板 ⑴ ＝ (b) 派一次专门的门户网关变更包审查，审过再合** —— 采纳 `OP-0827-D` 的建议，理由原文：**(a) 与 (c) 都是在给一条正在建的巡检判据先挖一个永久例外**。⇒ **`claude/unified-portal-design-8a2ce3`（`#162`，3 个提交、落后 1442、干跑 `merge-tree` 零冲突）不就地合、也不封存，改为立项审查。****连带**：⑴ 审查须一并确认其外部前置 **`§一 #240`（企微自建应用凭据）是否已到位**——凭据不到位则门户 OAuth 那部分无从验；⑵ 🔴 **`§一 #410` 的巡检判据在本审查出结论前不具备验收前提**（其验收判据＝「当前环境应恰好报 1 条 `unified_portal_gateway`，把它修掉或并入 master 后自动回落为 0」），#410 若先行上线须显式说明这一条例外的存续期，**不得用行内豁免标记压住了事**。⑵ `feat/sc8-baoguan-v2-batch1`（落后 2130）按本行原议**转业务总线**，环境线不代判。
