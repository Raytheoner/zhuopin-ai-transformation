---
title: "队列行日志 #489（§一）"
created: 2026-09-09
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #489 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋交付结论＋本指针。
外置会话: Cowork 业务总线 OP-0908-V（泳道 489-closeout 波2，承接 CC OP-0908-L 收尾件 §二.1+2）
原行md5: 5db26b6180f70a6601ef892ac406f880
---

# §一 #489 · 外置前整格原文（2026-09-09 外置，7627 B，md5:5db26b61）

> 原文原样、一个字节未改。外置动因：整格 UTF-8 实测 7627 B，超 K2 的 4096 B 上限，**2026-09-11 起由告警转阻断**（届时任何触碰本行的会话 release 会被拒）。
> ⚠️ **量的是字节不是字符**：同一格字符数 3827、字节数 7627，中文件按字符数判会得出一个宽约 2 倍的假上限（同 `工具-CLAUDE进度段lint.py` 12.3 已记的同族坑）。

[S:open][D:机] 🟢 **可领（2026-09-08 20:2x 去 🛑：Cowork 环境总线 `OP-0907-AL`，Shao Peishen 当日「两处工具缺陷尽快修复」；`_count_mechanism_wip` 当场重跑实测 20/22，去 🛑 后 22/22，无豁免；派单件 `1-转型规划/0-全景路线图/派单件-【CC】489-opener块lint两处同触碰修-2026-09-08.md`，`OP-0908-L`。🔴 软序：触碰 `工具-opener块lint.py`／`opener骨架.md` 与在跑批 `B-0908_四泳道机制根治` 泳道 `503-opener-single`（A3 `#503`→A4 `#487`）同文件，须等该泳道 ff 入 master 后再开工）** ━━━ 原 🛑 段原文保留于下： 🛑 **排队中·暂非可动。** 🆕 **2026-09-06 立行（Cowork 环境总线 `OP-0905-P`，Shao Peishen 当日答 ②(a)「登记为 CC 派单」、③「只登记不修」）。** ━━━ **实证**：本班改 §〇.17／骨架／lane-watch SKILL 后 release，opener 守卫报 21 处，用 `工具-opener块lint.py --file` 对 `git show HEAD` 的改前版逐份实测——改前改后同为 8（骨架）／13（模板库），**本批零新增、零形态变化**，21 处全部落在两份判据正本自身。⑵ 的实证＝全量 `--enforce` 起于 22:05、至 07:24 进程消失，输出文件始终 0 字节。 ━━━ 🔑 **本行按 `.claude/rules/队列与落库.md`「总线／看护会话不修工具」只登记不就地修**——该条正是本批 P3 新立的判据，本行是它的第一次实际适用。 ━━━ **WIP豁免：** 本行以 🛑 起首、不计入机制类可动 WIP；立行时机制类可动位无空，按接力卡两条合法路径之二（立 🛑 行）处理，转可动须先有位。 ━━━ **预留豁免：** 本行编号未走 `--reserve`——`--reserve` 只在 `acquire` 时可用，而本班需要新号时锁已在手（为订正高水位线而持），中途无取号入口；编号取自本班刚订正并回读核实的高水位线 §一 488 +1，持锁期间他方无法取号，故 489 可证空闲。🔑 **「持锁中途无法 `--reserve`」本身就是 `#324` 高水位线滞后的一个结构性成因**，已在报告里点名，未在本行展开。 ━━━ **登记豁免：** 脏文件 `1-转型规划/0-全景路线图/派单件-【CC】跟进README二期-登记CLI归档行长-2026-09-06.md` 由并发会话 `OP-0906-B`（业务总线）在本班持锁期间新建，不属本班触碰区、正文可能仍在起草，**代其登记会把半成品推进 commit**，由该会话自行登记 §二。 ━━━ 📎 **2026-09-06 顺带登记一处骨架↔lint 不一致（Cowork 环境总线 `OP-0906-W`，只登不修）**：`opener骨架.md` §三bis 看护者开场词样例句「标题设定后不要再被子任务顶掉」含「子任务」而无「例外／跳过本行」，`工具-opener块lint.py` F2 弱校验（`子任务` 与 `例外|跳过本行` 须同现）对照抄骨架的看护件直接报 F2（本日 `B-0906_P` 实撞，补一句「你自己不属于「跳过本行」的例外范围」后过）。二选一待本行实现方判：骨架样例补该句，或 F2 对 §三bis 块豁免。  ━━━ 🔴 **2026-09-08 22:19 CC `OP-0908-L` 开工即停：软序机器判据永不会转 0，本行按原判据不可开工。** 实证：`git merge-base --is-ancestor origin/claude/op0908p-opener-single origin/master` 退出码 1（`git fetch origin` 退出码 0、两 ref 均 rev-parse 成功，非「没读到对象」）；但该分支相对 master 唯一未合入提交 `a5ad0ce` 的内容与 master 已有的 `f00dbe9` 完全相同（`openspec/changes/single-guarded-copy/` 6 文件 431 行，`git diff a5ad0ce f00dbe9 -- openspec/` 在该目录下为空差）——泳道 `503-opener-single` 的产出是以另一个 SHA 进的 master，故分支祖先判据**永远不会满足**，等待与轮询都无解。且 `a5ad0ce` **只碰 `openspec/`、零触碰本行触碰区**（`工具-opener块lint.py`／`工具-共享文档编辑锁.py` 及两份单测），逐分支扫描 `origin/*` 亦确认 `op0908p` 不在「有未合入改动触碰本行触碰区」名单内 ⇒ 软序要防的同文件冲撞在当前基线上不存在。🔑 本条本身即 `#503`「同一内容两份、只有一份有机器守」的又一实例（同内容两个 SHA，门禁只认 SHA）。⇒ 本会话零改动退出（`git status` 空），泳道 `489-opener-lint` 已落 `pause`（🟡，`reports/lane-watch-state.json`）。待 Shao Peishen 一字母：(a) 放行、直接开工本行；(b) 先把 `op0908p` no-op merge 进 master 再重开 session；(c) 软序判据改判为「触碰区无未合入改动」口径并另立行。  ━━━ ✅ **2026-09-08 CC `OP-0908-L` 两处均已修，另顺带修出第三处。** 分支 `claude/op0908l-opener-lint-489-ef8652`（commit `2613969`，已 push、**未 ff master**）。⑴ **判据正本识别由「写死路径」改为「件自己 frontmatter 声明角色」**（`opener正本: 骨架／模板库`）——`#493` 只挖了骨架一条路径，模板库不在名单里故恒报 13 处；按路径续加即是在建本行明令禁止的文件名清单。骨架角色沿用 `#493` 口径；模板库角色换掉 F3/F4/F5 三条**版式**判据（依据＝根 `CLAUDE.md` §3「不凭模板库重建（模板库 §〇 是判据、不是格式）」，其块是内容草稿非版式范例），🔴 **F1/F2 刻意不换**（它俩守的正是 2026-08-27 一天 17 次违反的源头）。防外溢＝新判据 `C0`：声明了角色却只有 <2 个 opener 块、或角色值写错 ⇒ **声明不予承认、按普通件全判**并报 `C0`（实测冒充件仍照报 F3/F5，声明什么也换不来）。release 侧只调用不重写（D3），自动覆盖到模板库。**实测：骨架 0→0、模板库 13→0，近三份看护件 exit 0 不变。** ⑵ **全量 `--enforce` 实测是两件事、不是一件**：① 89% 墙钟在逐文件 `git log`——1613 个文件实测 **1057.3s**（均值 1.32s），纯解析仅 19.4s ⇒ 改一次全历史 `--name-only` 扫描，实测 **2.76s**，且**与逐文件口径 1613/1613 逐个比对完全一致**（不一致 0、取不到 0，非抽样）；② **0 字节 ≠ 挂死**——此前输出全攒到最后一次性打印，`> out.txt` 期间本就是 0 字节，「跑得慢」与「卡死了」观测上同形 ⇒ 进度改写 stderr 并 flush（不污染结果文件）。另加单次 git 60s 超时，一个卡住的调用不再拖死全量。**耗时 157s → 4~17s（热/冷），远低于 5 分钟验收线。** ⑶ 🆕 **顺带修出第三处**：`main()` 明细分组循环写死的形态清单**漏了 F3/F4/F5** ⇒ 这三个形态计入「N 处待修【阻断】」却**一行明细都不打印**（实测「49 处待修」而逐条只数得出 36 条）——本行 ⑵ 记的「验收命令拿不到结论」有本条一份；根因＝同一份形态清单存在两处、只有一处会被新增形态改到，已取消第二处并加兜底打印。⑷ `工具-opener生成.py` 加 `--variant reference`（本行步骤 5，并入 `#461`，见该行指针）。**验收：三套单测 603 passed／1 skipped。** 🟡 **两处骨架正文改动未做、等 Shao Peishen 一字母**（见收工汇总）：① §三bis 样例句补「你自己不属于「跳过本行」的例外范围」（现样例含「子任务」而无「例外/跳过本行」⇒ 照抄者必撞 F2；且生成器 `--variant guardian` 已含该句，二者现不一致）；② 「状态标记三式」节下加引用版禁手抄纪律行。🔑 本行**未销号**，待该一字母落定后再销。


---

# §一 #489 · 波次执行留痕（2026-09-09，泳道 `489-closeout`，Cowork 业务总线 `OP-0908-V` 看护者本体）

> 承接 CC `OP-0908-L` 收尾件 `1-转型规划/0-全景路线图/派单件-【CC】489收尾-队列回写与K2外置与ff-2026-09-09.md` §二。
> 🔴 **本节写在外置件而非行内**：行内一度被本节内容推回 5404 B（超 4096 B 上限），已当场收回——**这正是收尾件 §二.1+2 警告过的「先 append 再砍」形态，我照样撞了一次**，如实记。

## 波1 · 抢救未跟踪的唯一副本（🟢，看护者本体做）

该收尾派单件此前是**未跟踪文件、只存在于 worktree** `.claude/worktrees/op0908l-opener-lint-489-ef8652`（`OP-0908-L` 会话内 `git add` 被 auto mode 拦下），**worktree 一清即丢**。已 `git add` + commit + push ＝ **`b56317b`**，唯一副本风险解除。🔴 该 worktree 在 ff 前仍禁自删。

📌 **本波的 commit/push 系 Shao Peishen 2026-09-09 直令指派看护者本体执行**（原因＝子任务 `isolation: worktree` 从 master 新拉、看不见未跟踪文件），**覆盖 `OP-0908-V` opener「不 commit、不 push」的约束，如实留痕**。

## 波2 · K2 外置 ＋ 整格重写（🟢，同一把锁内）

外置前整格 **7627 B**，md5 `5db26b6180f70a6601ef892ac406f880`；重写后回读实测 **3302 B < 4096 B**，09-11 那颗雷已拆。

🔑 **顺带修出本批次一处会致命的登记缺陷**：§二 `B-0908_489判据正本声明式与全量提速` 的文件清单原写作**裸路径**，而编辑锁 `_pending_batch_fragments` 与 sweep `_resolve_batch_fragments` **只提取反引号内的串** ⇒ **该批次对 sweep 完全不可见、会静默掉在地上**（同形态 2026-09-08 已实撞一次）。已整格重写加反引号并补入本外置件。

⚠️ **一处量纲订正**：K2 的 4096 上限量的是**字节不是字符**。本格外置前字符 3846、字节 7627 —— 中文件按字符数判会得出一个宽约 2 倍的假上限（同 `工具-CLAUDE进度段lint.py` 12.3 已记的同族坑）。**我最初正是按字符数误判「未超限」，当场用 `Encoding.UTF8.GetByteCount` 复核才纠正。**

## 波3 · 停等 ff 一字母（🟡 `merge_to_master`）

已 `pause --lane 489-closeout --action-key merge_to_master`。**等**：ff 三笔进 master ＝ `2613969`（工具三修）＋ `7cb578a`（骨架两处）＋ `b56317b`（收尾派单件，本泳道波1 补提交）。

**ff 前置已现查**：主仓 26 个脏文件中**无** `0-学习与工具/*.py`／`opener骨架.md`／`专线opener模板库.md`，不撞 ff；三笔经 `git merge-base --is-ancestor` 逐笔核实**均未在 master**。ff 须在**主 checkout** 做，worktree 里 ff 不到 master。

## 波4 · 待办（ff 后）

补一次锁把本行首段 `[S:open]` 翻 `[S:done]`（🔴 翻转 `[S:xxx]` 前缀必须整格重写、只能走 `--changes-json`）。


## 波3 续 · ff 实测不可行 ⇒ 转出为 CC 派单件（2026-09-09）

**Shao Peishen 2026-09-09 答 `a`：三笔 ff 已获授权。** 看护者本体在主 checkout 执行时实测：

    git merge --ff-only origin/claude/op0908l-opener-lint-489-ef8652
    fatal: Not possible to fast-forward, aborting.

**真因（现取）**：分支自 `fb0cb43` 分叉后**落后 master 16 笔**，且两侧在**同两件**上有重叠改动 —— 分支侧 `2613969` 因 `#489` 判据正本改声明式而**连带改了 `工具-共享文档编辑锁.py` 的 release 调用侧**（该包 D3「release 侧只调用不重写」）＋其单测；master 侧 `c388c97`（`#505` 高水位线回写）改同一文件。⇒ **须先 rebase 解冲突，非单条 ff**。

🔴 **看护者本体不做这一步**：解冲突要判编辑锁代码怎么合，属「修工具」（`rules/队列与落库.md` 明令看护会话只登记＋出派单）；且**编辑锁正被所有并发会话使用，解错会当场打断全项目队列写入**。同形态先例＝`797b8ed`（为 `#482` 出的同类派单件）。
⇒ **已出** `1-转型规划/0-全景路线图/派单件-【CC】489rebase与ff-2026-09-09.md`（`OP-0909-G`，`工具-opener块lint.py --file` 零违规），含解冲突判据、零回归验收闸、`--force-with-lease` 硬约束、波4 回写销号与回读核字节。

### 🔑 一处认知订正：我把「待 ff」端错了两条

端定夺时曾把 `#500`／`#505` 与 `#489` 并列为「三条待 ff」，**实测前两条早已在 master**：

| 行 | 内容口径实测 | 结论 |
|---|---|---|
| `#505` | `git rev-list --count master..origin/claude/op0908x-505-hwm-apply-a1e02f` ＝ **0** | 内容即 master 的 `c388c97`，**无需 ff** |
| `#500` | 功能改动已在 master（`ad16695`）；`git diff master <branch>` 反向显示 master 比分支**还多一段注释** | 分支上是旧 SHA，**无需 ff** |

**同 `b752d41` 已记的判据「SHA 谱系 ≠ 内容已合入」，这次由我自己犯了一遍。** 根因不是查得不细，是**引用了队列行里的历史陈述**：那句「未 ff master」写于当时为真，此后被别的批次带进 master，而**行内那句话不会自己过期**。⇒ **凡「待 ff」结论，落地前必须用内容口径现查**（`rev-list --count` ＋ `diff`），不得引用行内历史陈述。

### 🔑 另一处：取号次序踩了 `#487` 盲区的反面

出派单件时先把 `OP-0909-J` 写进 frontmatter、再跑生成器 ⇒ **生成器扫已落盘痕迹，把我自己刚写的号判成「已占」**；换 `G` 同样被自己占住，两次循环。⇒ **正确次序是「先取号、后落盘」**，最终用占位符临时抹掉全部号才取到 `OP-0909-G`。**`#487` 子项记的是「取了没落盘 ＝ 没占」，本次是它的反面「落了盘却还没取号 ＝ 自己占自己」——两者同根：查重是取号那一刻的快照，不是锁。**
