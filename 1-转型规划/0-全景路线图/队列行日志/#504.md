# 队列 §一 #504 · 行日志（K2 外置件）

> 本件收纳该行状态格外置出来的历史回写段，原文原样、未改写；行内只留首段＋末段＋指针。


## 2026-09-09 K2 外置（CC `OP-0909-M`，批 `B-0909_462与504apply`，泳道 `504-heartbeat-vis`）

外置原因：apply 回写前该行状态格已达 5598 B、超 `ROW_LENGTH_CAP_BYTES` 4096 B（2026-09-11 起由告警转阻断），本次回写还要再加约 1.4 KB。以下为原状态格中间两段（2026-09-08 起草段 ＋ 2026-09-09 答 2a 段），原文原样、一字未改；md5:`843d015a47f63542e6aae8c691990884`。

✅ **2026-09-08 openspec 起草完成、止步 design 审**（CC `OP-0908-R`，泳道 `504-heartbeat-vis`，批 `B-0908_四泳道机制根治`）：产出＝`openspec/changes/lane-watch-heartbeat-visibility/`（proposal／design／spec／tasks，4 文件 456 行全新增、未改任何既有文件、未动一行 Python），分支 `claude/op0908r-heartbeat-vis` @ `b784019`，已合入 master（commit `e998ec5`）。`openspec validate --strict` ⇒ valid；`pytest test_工具-泳道看护状态机.py` ⇒ `87 passed in 9.62s`。🔑 **⑴ 读侧没有 `#488` 那种偏移，别修错地方**：`工具-共享文档编辑锁.py` 的 `REPO_ROOT` 解出主 checkout、符合其 docstring；缺陷在**写侧根本没有工具入口** —— 泳道靠 opener 散文里的相对路径 `reports/lane-heartbeat/<泳道>.md`，由泳道自己的 CWD 解析。⇒ 正确落法是写侧复用同一个 `REPO_ROOT` 常量＋两侧同锚的断言单测，**不是**去改 `_resolve_repo_root()`（那会同时打断编辑锁）。**⑵ 这是 v2.0 架构收敛带进来的回归**：SKILL.md v2.0 步骤 4 把默认起法从无头 ps1（CWD＝主 checkout）改成 Task/Agent `isolation: worktree`，心跳路径那句散文一字未动。实证：主 checkout 26 份心跳全是 ps1 时代的 `B-0902/03/04_*` 前缀，本批九条泳道零命中。**⑶ 「读侧去各 worktree 搜一遍」被实测否掉**：同名心跳件同时存在于三个 worktree，取「最新那份」在重试场景下≠「当前这条泳道那份」 —— **误报只是吵，读错副本是把真失联判成健康**。**⑷ 状态机 `status` 只有 `running`/`paused`/`resumed`，没有终态概念**。🔴 **一处口径订正**：本行期望产出 ④ 说「回改 `zhuopin-lane-watch/SKILL.md` 的『已知缺陷』段」，但该文件全文检索该词**零命中、仓库侧无此对象**；那段话实际长在 claude.ai 侧引用式装载器（`save_skill` 装的，**CC 做不了**）与历次看护件（历史快照件、按「历史记录不追改」不动）。design 决策点 7 已把 ④ 拆成三个载体分别处置。✅ **Shao Peishen 2026-09-08 答 2-1(a)：泳道自写的 `STOPPED ｜` 算「有信号」、同等豁免看门狗**（§五 无默认项之一，已明答）。⏳ **§五 另一项（claude.ai 装载器『已知缺陷』回改归属）：他答 `2a` 未覆盖到该子项的 (x)/(y)，看护者按其整体走「全按推荐」记作 (x)** ——随本包 apply 同批派 Cowork 执行，并作为本包完成的必要条件；🔴 **该项属推定、非明答，apply 前须向他确认一次**（不设默认的原因＝不答的代价是装载器继续对下一棒宣称此处有已知缺陷、下一棒继续人守绕路，属「错误继续发生」）。 ✅ **Shao Peishen 2026-09-08 答 1(a)：design 放行、按起草方推荐取值**（CC 看护者 `OP-0908-T` 就地回填，批 `B-0908_四泳道机制根治`）。🔴 **放行的是 design、不是实现** —— 后续 apply 另起 session，本行不因放行而销号。  ━━━ ✅ **2026-09-09 Shao Peishen 答 2a：§五「claude.ai 侧引用式装载器『已知缺陷』回改归属」确认按 (x)＝ design 决策点 7(a)**（Cowork 业务总线 `OP-0908-V` 端出、就地答）。**该项从此不再是推定，是明答。** 🔴 **归属＝Cowork，不进 CC 泳道**（`save_skill` 是 claude.ai 侧能力，CC 工具清单里没有——本行此前已实证）；apply 泳道 `504-heartbeat-vis`（`OP-0909-E`，批 `B-0909_两包apply`）已在 opener「不做什么」里写死跳过并标「已答2a，归 Cowork，勿重做」，🔴 **同一题不得两端各答一次**。 ⏳ **但它有硬前置、本次未执行，如实登记**：决策点 7(a) 的三个载体是**有序**的——载体①（仓库侧 `0-学习与工具/skills源码/zhuopin-lane-watch/SKILL.md` 新增「已知缺陷与其状态」节）由 apply 泳道建，载体②（装载器）**要指向的正是那一节**。⇒ **在①建成前改②只会造出一个悬空指针**，而装载器恰恰是下一棒最先读到的那一份，指错比不指更坏。**另一层风险**：`save_skill` 覆写整份 skill，而本会话手上的是**渲染态**内容、非原始件，直接覆写有失真面。⇒ **本项排在 `504-heartbeat-vis` apply 完成并 ff 之后，由 Cowork 侧执行**（承接＝本行，不另立行）。**判据：不做的代价是装载器继续对下一棒宣称「已知缺陷未修」、下一棒继续人守绕路——该代价在 ① 建成前本就无法消除，故此处的等待不是拖延。**
