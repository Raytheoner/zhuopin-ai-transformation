---
title: "队列行日志 #550（§一）"
created: 2026-09-10
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #550 的建造完成报告外置件（K2 口径：单格 ≤4 KB，本段直接落此、行内只留收口结论＋验证手段＋本指针；原文原样、可 grep）。
配套: 1-转型规划/0-全景路线图/opener骨架.md；0-学习与工具/工具-opener生成.py；0-学习与工具/工具-opener块lint.py；0-学习与工具/test_工具-opener块lint.py；0-学习与工具/test_工具-opener生成.py；0-学习与工具/test_工具-共享文档编辑锁.py
---

# §一 #550 · 建造完成报告（2026-09-10，CC 泳道 `549-550-opener-infra`／`OP-0910-R`，批 `B-0910_起棒基建`）

> 行内只留收口结论；本件为完整对账原文，同批 `#549` 报告见 `#549.md`。

━━━ ✅ **建造完成·待 ff（2026-09-10 17:5x 本地，CC 泳道 `549-550-opener-infra`／`OP-0910-R`，批 `B-0910_起棒基建`）** —— 分支 `claude/op0910r-549-550-opener-infra`（自 master `a80e6a7`，已 push、**未 ff master**，🟡 留 sweep／看护者）。**期望产出逐条对账**：⑴✅ `opener骨架.md`【CC · 子任务泳道】块末行补收工哨兵行，正文写明「第三条是 2026-09-10 有意扩入、不是漏删，下一个人不要按旧纪律删掉」＋成因；⑵✅ **生成器强制注入**：`工具-opener生成.py` 新增 `SUBTASK_SENTINEL_NOTE`，`--variant subtask_lane` 拼装无条件追加（与骨架末行逐字同，单测「骨架与生成器契约」比对）；⑶✅ `工具-opener块lint.py` **形态⑨**（`F9`，生效日 2026-09-10）：子任务泳道块缺「同一行同现 `OPENER_DONE` 与 `OPENER_PARTIAL`」即报，只判 `is_subtask_lane` 块（同 F6／F8 收窄；标准／guardian／Cowork 块不经批处理器，不判）；release 侧 opener 守卫复用 `check_block` 自动带上；⑷✅ 单测：lint +9（`形态九_子任务泳道块缺收工哨兵` 含骨架自带哨兵行用例）、生成器 +4（`收工哨兵强制注入`）、既有干净夹具补哨兵行（lint／编辑锁两处，同 09-04 六字段先例）；🔴 **变异检验实做**：把注入行注释掉 ⇒ `test_工具-opener生成.py` **12 failed＋7 errors**、CLI `--variant subtask_lane` 直接被自检拦下报 `[F9]`；恢复后复绿。**实测数**：`test_工具-opener块lint.py` 103 passed（原 94）／`test_工具-opener生成.py` 93 passed（原 76；含 #549 的 12 条）／编辑锁 `OpenerGuardReleaseTests` 25 passed。**不做什么已守**：`v2.ps1` 解析器（`### A<N>` 三件套）一字未动。⚠️ **如实登记**：全库 lint 当前在用件命中 36→47，新增 11 处全是 `F9`＝三份已执行看护件（`B-0909_三泳道夜批`×3／`B-0910_三泳道续排`×3／`B-0910_五泳道全启动`×5），按「历史记录不追改」不修、告警不阻断；下一份看护件用生成器重出即零命中。🔑 **第一个实证**：本泳道自己就是无头起的（`reports/opener-batch/20260910-170523/`），opener 手工带了哨兵行，收工以 `OPENER_DONE` 收尾——summary 状态见看护者取件。

## 立行原文（2026-09-10 16:2x，`OP-0910-A`）

> 2026-09-10 18:2x 追加外置。**原文原样，未压缩、未改判。**
> 🔴 **本段的根因判断已被证伪**（哨兵要求其实一直由批处理前言 ⑤ 注入），纠正见队列行内末段。保留原文是因为「已发生事实的原文不追改」——**错的推理过程本身是实证**。

[S:open][D:机] 🟢 **可领 —— 2026-09-10 16:2x 立行（Cowork 业务总线 `OP-0910-A`；Shao Peishen 当日答 `2a`）**。 🔴 **成因是本方的，如实登记**：2026-09-10 本方出的两份件（`B-0910_三泳道续排` 看护件、`443清扫与K2外置` 派单件）其 opener 正文**只写了并行上限与 push 纪律，一个字没提收工哨兵**；而批处理脚本把哨兵当**判成败的两个指标之一**。 **实证（四条泳道全中，非个例）**：`507-sweep-apply` **NO-SENTINEL** 43.2 min／`529-timeout-semantics` **NO-SENTINEL** 37.3 min／`544-heartbeat-batch` **NO-SENTINEL** 11.9 min／`k2-externalize` **PARTIAL** 13.2 min ⇒ **四条无一 `OPENER_DONE`**。 🔑 **危害不是活没做完，是收工协议没走完 ⇒ summary 不可信**：同一时段实测**活其实都做了**（超限行 28→1、`#544` 已回写、`#529` 单测 107→120 且两枚变异验证复绿）——**做完的活被报成异常收尾**。**一个把成功报成失败的判据，比没有判据更糟**：它会训练下一个人忽略 summary。同族＝§一 `#398`「用来发现问题的东西自己坏了」。 ⚠️ **修法必须落在生成器强制注入，不能只改骨架文字** —— §一 `#487` 已证明「正文里写一句」拦不住；**何况本次是正文里根本没写**，属于比 `#487` 更前一步的失效（不是没被遵守，是压根没生成）。 🔴 **WIP豁免：立行时机制类可动 WIP 现取超上限 22**。豁免依据＝**它污染的是所有批次的验收判据**，而「说口令起棒」今日刚成为常态用法，不修则每批 summary 都要人工复核一遍，等于把无头起省下的人工又加回去。

## 2026-09-10 20:5x 外置（`OP-0910-A`，3 段）

> 原文原样，未压缩、未改判、未修辞。行内已替换为指针。

[S:open][D:机] 🟢 **可领 —— 2026-09-10 16:2x 立行（Cowork 业务总线 `OP-0910-A`；Shao Peishen 当日答 `2a`）；建造已完成、待 ff，但立行根因已被证伪、须重新定向（见下两段）。立行原文见本格末尾指针。**

✅ **建造完成·待 ff（2026-09-10 17:5x 本地，CC `OP-0910-R`，批 `B-0910_起棒基建`）**：分支 `claude/op0910r-549-550-opener-infra`（与 `#549` 同棒，已 push、**未 ff**，🟡 留 sweep／看护者）。⑴✅ 骨架【CC · 子任务泳道】块末行补哨兵行，正文写明「有意扩入、勿按旧纪律删」；⑵✅ 生成器 `SUBTASK_SENTINEL_NOTE` 无条件注入（与骨架逐字同、契约单测比对）；⑶✅ lint **形态⑨ `F9`**（只判子任务泳道块，release 守卫自动复用）；⑷✅ 单测 lint 103／生成器 93／编辑锁守卫 25 passed；🔴 **变异**：注入注释掉 ⇒ 12 failed＋7 errors、CLI 被自检拦报 `[F9]`，恢复复绿。解析器一字未动。⚠️ 全库 lint 在用件 36→47（+11 全为 F9＝三份已执行看护件，历史不追改、告警不阻断）。🔑 本泳道自身即第一个实证（无头起、手带哨兵、以 `OPENER_DONE` 收尾）。对账全文见 `1-转型规划/0-全景路线图/队列行日志/#550.md`。

🔴 **2026-09-10 18:2x 立行根因被证伪，本行须重新定向（Cowork 业务总线 `OP-0910-A`，Shao Peishen 问「OP-0910-R 就是基建棒？」时顺带查出）**。 **原根因（本方写的）**：「Cowork 侧出件时【CC · 子任务泳道】opener 正文一个字没提收工哨兵……不是没被遵守，是压根没生成」。 **证伪手段＝逐个读四条泳道的 opener 真身**（`reports/opener-batch/20260910-140400/507-sweep-apply-A1.opener.txt`／`529-timeout-semantics-A3`／`544-heartbeat-batch-A2`，`20260910-143435/k2-externalize-A1`）：**四份全部含 ⑤ 前言、全部出现 `OPENER_DONE` 各 1 次**（`[regex]::Matches(...,'OPENER_DONE').Count`＝1，`-match '全部完成输出顶格一行'`＝True）。**来源＝`工具-opener批处理执行v2.ps1` 第 84 行——批处理在每个无头 opener 前自注入五条硬规则，⑤ 即哨兵要求。** ⇒ 🔴 **哨兵要求一直都在；agent 拿到了、没照做。** **纠正后的形态**：本行**不是** `#487` 的「更前一步」，**它就是 `#487` 本身**——正文里写一句拦不住。**而本方提出的修法恰恰是再往正文写一句**，且会是**第三份副本**（批处理前言 ⑤ ／ 骨架 ／ 生成器注入），三份同义指令都不带强制力。🔑 **本方一边引用「写一句拦不住」这条教训，一边把它当成了修法** —— 同族＝「写进文本的状态不会自己过期」的镜像：**写进文本的要求不会自己被执行**（`UPS5:7` 主语从本方换成 agent）。 **重新定向（建议，待 Shao Peishen 定）**：修法应落在**检测/强制**侧而非注入侧——`NO-SENTINEL` 时由批处理用 `claude --resume <sessionId>` 补问一次「只输出一行哨兵」，仍无才判 FAIL。⇒ **它依赖 `#549` 的 `--resume` 接管，故硬序应由「`#550` → `#549`」反转为「`#549` → `#550`」**（原派单件 §〇 写反了）。 🔴 **`OP-0910-R`（17:05:27 起，18:2x 仍在跑）正按被证伪的前提执行本项** —— 不中断（`#529` 收尾与 `#549` 基建仍有效），收工后按本段重判 `#550` 产出，**不得因为它交付了「生成器已注入」就判本行完成**。
