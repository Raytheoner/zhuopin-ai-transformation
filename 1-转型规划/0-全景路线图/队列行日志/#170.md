---
title: "队列行日志 #170（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #170 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #170 · 外置前整格原文（2026-09-04 外置，8 段）

[S:hold][D:机] 🛑 **不占 WIP（2026-08-19 定夺 2(a)：结构性不可动的 hold 行统一改用既有 `🛑` 首标记，`_count_mechanism_wip` 据此结构性排除；本行语义未变、状态字段未动）** ／ 🔒 **方案阶段·禁止任何实际改动（Shao Peishen 2026-07-30 明确）**——「只停留在方案阶段，不影响任何现有开发环境与开发流程，等我完全消化吸收、万无一失批准后才允许试点任何实际改动」。**本行不是待领任务，任何 session 不得据此开工**；范围已定＝选项 1（决策点⓪ 已拍板），实施方案深化见 `3-治理与合规/构建环境迁移-选项1实施方案深化（未批准·方案阶段）-2026-07-30.md`（含 G0-G5 六道批准闸口，G0 未过不得进入任何后续动作）。**✅ G0 已获批准启动（Shao Peishen 2026-07-31，Mac 侧 Claude Code 已装好）**——作业指导书＝`3-治理与合规/G0作业指导书-MacStudio只读试用一周（Shao Peishen 本人执行）-2026-07-31.md`（三条铁律／每日只读三连／七问评估表／三种判定与退出路径）。**G1 及以后仍未批准**。 ➕ **2026-08-24 并入评估子项（Cowork OP-0823-E，Shao Peishen 答「值周巡检优化 (a) 全做」当日）：定时任务常驻化**——他问「值周巡检可否迁移到云端」；实测判定**云沙箱不可行**：值周巡检/拆件巡逻均为队列写手（对账修正/建行/登批次），依赖同一工作副本上的文件编辑锁＋本机 git＋写后反查，云端跑＝凭空第二写手；且云沙箱出网受限（企微 qyapi 403 为本项目实测）。**诉求正解＝随本决策件「唯一常驻写手」落地时，把 huijian-chaijian-patrol 与 weekly-status-update 两个定时任务一并迁至常驻写手机**（笔记本关机照跑，单写手与锁全保住）。属方案范围扩充、零实际改动，评估随本行原节奏走，不改变 hold 态。 ➕ **2026-08-25 唤醒「OneDrive 迁出」切片（Cowork 环境总线，方案会话）**：Shao Peishen 原话「**目前多数情况下我的 OneDrive 是关闭的，但以后不能经常关闭，我需要把企业AI转型项目目录从 OneDrive 路径移出，以免和 github 冲突**」。⇒ 本切片属本行范围内的**同机换路径**（`C:\Users\Paul Shao\OneDrive\Projects\企业AI转型` → 移出 OneDrive 层级；🔴 路径中的 `Paul Shao` 是 Windows 账户名，不可替换），**与 Mac Studio 主写手迁移互不冲突、可先行**。**方案件＝**`3-治理与合规/仓库迁出OneDrive-迁移方案（未批准·方案阶段）-2026-08-25.md`。**本切片同样受本行 🔒 方案阶段红线约束：本会话只出方案与取证，零实际改动；执行属 CC，须 Shao Peishen 对方案整体批准后另开执行窗口。**（**登记豁免：`.claude/settings.local.json`** —— 本机 Claude 客户端本地设置文件，非本会话产出、亦非本会话应决定其入库与否的对象，本次不为其登记 §二 批次。） ━━━ **2026-08-25 当日三项定夺（Shao Peishen 就地答）**：**⑴ 目标路径＝`C:\Dev\zhuopin-ai`**（全英文短路径；实测最深路径 246 字符距 260 仅余 14，迁后降至 219）／**⑵ 方式＝整树移动＋逐项修指针**／**⑶ 不入库件备份＝OneDrive 内另设「仅不入库件」目录 + 每周 robocopy 计划任务**。**前置项 ①（查清现在 import 的是哪份代码）已当场完成**，结论另立 **§一 #406**（5 个场景包 import 的是断链 worktree 副本、2 个 NOT_FOUND）；**前置项 ②（回滚演练）未做、属 CC**。**`.git` 内三个 `-SPSThinkpadWIN` 冲突副本＝OneDrive 冲突副本实物证据**，Shao Peishen 答 2(b)：不单独查、随迁移一并处置。🔴 **派发时点：Shao Peishen 明确「先做完 ①② 看情况再派发」⇒ 迁移窗口暂不排期**，待 ② 有结论后再议。✅ **GitHub 侧不受影响已实测**（remote 由 `.git/config` 记录、与磁盘路径无关，仓库名与历史均不动）；真正风险点是本地 `工具-落库sweep.py::MAIN_WORKSPACE` 不改则 sweep 静默失败、表现为「GitHub 上突然不再有新提交」且无告警。 ━━━ ➕ **2026-08-25 方案四处调整（Shao Peishen 追问「git 本身没在 OneDrive 路径里吧」后取证；本条为接力件「⏳ 队列更新待补」的回补，回补后该小节已删）**：**⑴ 新增 §一「冲突发生在哪一层」**——git 程序（`C:\Program Files\Git`）与全局配置（`C:\Users\Paul Shao\.gitconfig`）**都不在 OneDrive**；但仓库 `.git` **就在 OneDrive 里，8,251 文件 / 632.5 MB、占仓库体积一半** ⇒ 迁移目标可更精确表述为「让 `.git` 不再有第二个进程碰它」。**⑵ S3 新增第 10bis 步 `git fsck --full`**（不是 `--connectivity-only`）——632 MB pack 刚被逐字节搬过，连通性检查只验引用图、验不出 pack 内部字节损伤。**⑶ S2 第 7 步 `/COPYALL` → `/COPY:DAT /DCOPY:DAT`**——`/COPYALL` 含 SACL 审计信息需备份特权，未提权会成片 ERROR 5；`.git` 与源码树不依赖自定义 ACL。**⑷ S2 第 8 步判据改写 ＋ 新增 6bis**——① 文件数**不得写死期望值**（实测同日 09:56 为 47,578、11:30 为 51,670，1.5h 涨 4,092），基线须在冻结窗口内当场取；② 迁移前须确认**云端占位符为 0**（当日实测为 0），非 0 须先 hydrate，否则 robocopy 会搬出「文件数对、内容是空壳」且不报错的副本。━━━ **第七节第 2 项已由「未查」升级为已查清**：现役 `config` 4,915 B（08-25）vs 副本 590 B（07-08）／747 B（07-23），**均为陈旧快照、现役配置未被降级**；但它们证明 **OneDrive 曾两次判定 `.git/config` 冲突**，反向判则 24 个 worktree 注册与 remote 配置会一次性消失——**这次没出事是运气不是机制**。🔴 **另一处已知缺口（待补）**：迁移窗口判据第 5 条「常驻服务已停」**只点名了 Listener，未覆盖存活的 claude 进程与 opener 批处理 runner**（2026-08-25 实测 16 个 claude 进程自 08-21 存活至今），**待 Shao Peishen 定夺后补入判据**。 ━━━ ➕ **2026-08-25 判据 5 扩写（Shao Peishen 答 4(a)）**：迁移窗口判据第 5 条由「常驻服务已停」扩为**三条缺一不可**——① 常驻监听器已停且**进程确已退出** ② **无 claude 进程存活** ③ **无 opener 批处理 runner 在跑**（核验命令见方案件 §四 代码块）。🔴 **成因＝实测 16 个 claude 进程自 08-21 存活至今**（最长累计 CPU 361 分钟，另 08-23／08-24 各 1 个）；批处理不背锅（08-25 那轮 headless claude 全部正常退出），但**它们是活写手，随时可能抢编辑锁／写队列，而原判据里根本没有它们**。**清理动作＝Shao Peishen 2026-08-25 定「派 CC 时带上逐个核对再清的判据」**：对每个 PID 先核 StartTime／CPU／是否有对应终端窗口在前台，**确认非在办会话方可终止；任一判不准即整批留步回报，宁可不清、不可错清**（杀错＝杀掉他正开着的会话）。本项随迁移执行窗口一并派 CC，不单独立行。 　━━━　🔴 **2026-08-25 前置项 ② 回滚演练部分完成（#407，CC OP-0825-DRILL，commit `aa8d352`）——本行保持 hold 不变，🔒 方案阶段红线不解除**：**T1 worktree repair 演练 ✅ 完成**，实测三段耗时——整树复制 **165.3 s**（57,210 文件／1.393 GB／FAILED=0，均速 655 MB/min）、**`worktree repair` 仅 0.33 s**（原以为是耗时大头，实测可忽略不计）、校验 1.14 s、中文完整性抽验 U+FFFD=0。**T3 计划任务影子演练 ⛔ 阻塞于管理员权限**（`Register-ScheduledTask` → `Access is denied` 0x80070005），步骤 2–6 未做。 🔴 **四条可直接落进 S3 的修订**：**⑴ S3-10 改写**——`git worktree repair` **必须显式传每个新路径、禁止裸跑**：裸跑在**移动场景静默什么都不做**（零输出、退出码 0、仍全部 prunable）、在**复制场景静默改坏源仓库**（把源的 26 个指针改绑到副本），**两种误用都不报错**（实验室实证见结果件 §1.2／§1.3）；且验证**不能只看 `git worktree list`**（它对 gitdir 缺失的「静默隐身」worktree 视而不见），须逐个 `git rev-parse --git-common-dir` 回读。**⑵ S3-12 前置**——先停 `ZhuopinAibotDevListener` ＋ 禁 `ZhuopinCommitSweep`，两者都 `import zhuopin_platform`。**⑶ S3-13 前置守卫**——`0-学习与工具/工具-注册落库sweep计划任务.ps1` **不是原子的**（先 `Unregister-ScheduledTask` 后 `Register-ScheduledTask`），非提权下重跑会留下「**任务被注销掉、又没建回来**」；须在脚本开头自检是否提权、未提权即在**动任何任务之前**退出。（⚠️ 如实标注：`Unregister` 在非提权下能否删掉本用户任务**本次未实测**——要测须拿生产任务当靶子，已放弃；故此为**有依据的风险，非已证实事实**。）**⑷ `robocopy /COPYALL` 同样需提权**——非提权下 0.1 秒即退出码 16、零字节复制（`/COPY:U` 审计信息需 `SeSecurityPrivilege`）；S3 复制步骤要么提权跑、要么改 `/COPY:DAT /DCOPY:DAT`。 ⚠️ **另**：原方案「副本文件数应 ≈ 47,578」这条核对判据**已失效**（实测 57,210，仅因本会话新建一个 worktree 即 +1,700）——**改用「源与目的地文件数相等」的相对判据**，勿写死绝对数。结果件＝`1-转型规划/0-全景路线图/OneDrive迁出回滚演练结果-队列407-2026-08-25.md`。 　━━━　✅ **2026-08-26 Shao Peishen 批准迁移方案整体（答定夺 1(a)）** ⇒ 🔴 **本行 🔒「方案阶段·禁止任何实际改动」红线，仅就「OneDrive 迁出」这一切片解除；Mac Studio 主写手迁移部分仍为 hold、G1 及以后仍未批准，红线对其继续有效。****前置项 ①②（幽灵 import 查清 ／ 回滚演练）均已闭环**（#406／#407 皆 done）。**执行拆为两行**：**#412 M1 迁移前准备**（零迁移动作、可进泳道、待领 CC）＋ **#413 M2＋M3 迁移窗口执行与收口**（blocked，硬阻塞于 M1 完成 ＋ 窗口日期）。🔴 **M2 不由 CC 执行的结构性理由**：`robocopy /MOVE` 会把 CC 自己的 worktree 与 `.git` 指针一并抽走——**执行者站在被搬的地板上**；改由 Shao Peishen 在管理员 PowerShell 跑 M1 产出的脚本。**2026-08-26 08:4x 全局冻结已就位**（机器人三层停并复核、四个 Windows 任务 Ready/Disabled×3、拆件巡逻手工 pause），Shao Peishen 答「今天一次做完」。 　━━━　✅ **2026-08-26 「OneDrive 迁出」切片执行完成** —— 仓库已迁至 `C:\Dev\zhuopin-ai`，S4 冒烟 1–7 全绿，详见 #413 与结果件。🔴 **本行整体仍为 hold**：Mac Studio 主写手迁移部分（G1 及以后）未获批准，红线对其继续有效；**本次只兑现了同机换路径这一个切片**。**收益已实测兑现**：最深路径 246→219；路径无空格无中文（编码事故入口关闭一个）；CC transcript 目录名由「含中文被吃掉后形如 `…-Projects---AI--` 的编码名」变为全英文可辨，**§5「必须用 mtime ＋ 内容二次确认、不得只认目录名」那条人守可随之退休**。⚠️ **但 §一 #414 不因此撤销**——触发它的是反引号与竖线等**元字符**，与路径有无空格无关（方案 §三 已逐条界定）。
