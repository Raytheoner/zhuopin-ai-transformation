---
title: "队列行日志 #135（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列 §四 #135 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

---

# §四 #135 · 外置前整格原文（2026-09-04 外置，4 段，md5:c8ba3e01）

🔴 **CLI 侧 auto-memory 是否启用 —— 需裁定（OP-0828-P 探针 P0-3 发现，2026-08-29 立行）**：本机 Claude Code 2.1.226 的 auto-memory **已存在且处于开启态**（本会话系统提示带完整使用说明；写入路径 `C:\Users\Paul Shao\.claude\projects\<cwd 编码>\memory\`，**按项目路径分桶、无全局桶**）。🔑 **它与 CLAUDE.md 顶部判死的那层 memory 不是同一物**——那层在 MSIX 包容器内（写真路径、读容器），这层在真实 `~/.claude` 下、CC 直接可读写。**实测存量**：本项目**当前**路径桶 `C--Dev-zhuopin-ai` **0 件**（建于 2026-08-27）；本项目**旧**路径桶 `C--Users-Paul-Shao-OneDrive-Projects---AI--` **26 件**（建于 2026-06-10）——⇒ **仓库从 OneDrive 迁到 C:\Dev 那天它静默换了桶，26 件留在原地，没有任何东西报错**（又一个「2 汉字＋AI＋2 汉字」编码撞名实例）。 ⚠️ **判据冲突点（这才是要你裁定的核心）**：启用它 ＝ 引入**第四份跨会话载体**，与根 `CLAUDE.md` 顶部「🔴 不得新建第四份载体；跨会话纪律的权威载体只有本 CLAUDE.md 与队列」**直接冲突**；不启用则维持现状（桶为空、无人写、无害）。 **另一面**：P0-1 已证 **Cowork 读不到这一层**（其沙箱 `HOME` 在容器内）⇒ 即便启用，它也只对 CC 一桌成立，天然做不成「两桌权威载体」。 **本棒动作到此为止：全程只读，未写入、未删除任何 memory 文件，也未做任何启停。** 证伪命令：`Get-ChildItem 'C:\Users\Paul Shao\.claude\projects' -Directory ｜ ForEach-Object { $m = Join-Path $_.FullName 'memory'; if (Test-Path $m) { '{0} = {1}' -f $_.Name,(Get-ChildItem $m -Recurse -File).Count } }` —— 若本项目当前桶不是 0、旧桶不是 26，则上述底数被证伪。 **不设默认项**（默认「继续挂着」会让 26 件旧记忆继续无人处置，且本项无执行者——本 session 收工后无人接手）。正本＝`1-转型规划/0-全景路线图/构建环境P0探针报告-2026-08-28.md` P0-3 节。  📌 **上式含全角「｜」（队列表格不收半角竖线），复跑请改回半角；或用这两条无竖线的等价式，更直接**：`(Get-ChildItem 'C:\Users\Paul Shao\.claude\projects\C--Dev-zhuopin-ai\memory' -Recurse -File).Count`（应为 **0**）与 `(Get-ChildItem 'C:\Users\Paul Shao\.claude\projects\C--Users-Paul-Shao-OneDrive-Projects---AI--\memory' -Recurse -File).Count`（应为 **26**）——任一不符即本行底数被证伪。  🔴 **称呼豁免：本行两处 `Paul` 均为 Windows 用户目录路径字面量**（memory 桶路径 `C--Users-Paul-Shao-OneDrive-…` 与取证命令里的 `C:\Users\Paul Shao\.claude\projects`），属根 `CLAUDE.md` §1 称呼纪律明文例外「🔴 绝不替换路径里的 `Paul Shao`（改了路径即失效）」——**改成 `Shao Peishen` 会让这两条命令与桶名指向一个不存在的目录**，故保留字面量。（本豁免由 `OP-0828-P` 于 2026-08-29 补，成因：立行时漏加标记，被队列结构 lint 当场抓到——**这正是该判据该有的行为，不是误报**。） ━━━ ✅ 已裁定（2026-08-29，Shao Peishen 答 (a)）：CLI auto-memory 不启用；旧路径桶 26 件原地封存、不删不迁；根 CLAUDE.md「不得新建第四份载体」禁令维持原文。无后续动作，待值周清扫迁档。 ━━━ 🔄 改判（2026-08-29，Shao Peishen 答「可」，推翻本日早前裁定 (a)、原文保留不删）：启用 CC 侧 auto-memory 作「技巧笔记层」，三护栏＝⑴ 边界：只记工具技巧/环境坑/Shao Peishen 偏好表述，纪律口径判据一律不入（唯一权威仍＝CLAUDE.md＋两份队列）；⑵ MEMORY.md 索引 ≤200 行/25KB；⑶ 旧桶 26 件只读收割对账一次（有效迁承接、过时封存、不删不改）。成因＝「说很多遍也没记住」实测归因：CC 桶三天 0 条是被根 CLAUDE.md「完全忽略 memory 层」措辞误伤禁死，非机制失灵；Cowork 侧维持仓库文件法（Cowork 绑 MSIX、AppData 虚拟化不可避，官方部署文档已查证：.exe 形态无 Cowork）。执行载体＝OP-0829-E 追加段 T5（root memory 段措辞修订＋本行回写）/T6（旧桶对账）。 ━━━ ✅ **T5/T6 已执行（2026-08-29，`OP-0829-E`）**：⑴ 根 `CLAUDE.md` memory 段已按改判改写为「**两桌不同命**」——Desktop/Cowork 判死照旧，CC 侧启用为技巧笔记层，边界三条入正文，并补一句可机械执行的判别：**「违反会让人做错事 ⇒ 纪律口径，不入 memory；不知道只是多走弯路、不会做错 ⇒ 技巧，可入」**；⑵ 旧桶 26 件只读收割对账已出件 `1-转型规划/0-全景路线图/auto-memory旧桶收割对账-2026-08-29.md`（**仍真 12／过时 7／涉纪律 6**；底数 26 复点一致、未被证伪；**旧桶一个未删、未改、未移**）。 🔴 **对账查出两条「过期的正确记录」与现行口径正面冲突，须在 CC 侧首次启用前排除出注入面**：① `project_wecom_aibot_authorization_model` 的「🆕 待发＝预授权」已被场景固定流程第 8 步「⏳ 待你审」两态语义 ＋ 发送硬前置三条取代（按旧标记直发＝跳过串行闸与硬前置，而信发出撤不回）；② `project_sales_dashboard_git_history_pii` 仍写「改天清理」，而本 §四 `#33` 已于 2026-07-24 拍板「①登记风险接受」。**两条都不是记错，是过期的正确记录，且过期得没有任何信号**——这正是边界②「纪律口径判据一律不入 memory」的实证。 另查出 **1 条真判据无常驻载体**：`feedback_historical_check_must_include_tests`（改判据前须同时对照生产数据与既有回归测试），Grep 全库仅命中归档 `openspec/changes/archive/2026-08-05-sweep-editlock-status-keyword-anchoring/design.md`，根 `CLAUDE.md` 与两份队列零命中 ⇒ 已列 `OP-0829-E` 收工报告定夺项待补登记。 ⚠️ **本行状态头仍为 🔴 未翻**：裁定本身已完成，但残留两项**非 Shao Peishen 动作**（CC 侧注入面排除、`historical_check` 补载体）；翻头须整格重写 2,362 字含密集反引号的格，本棒不裸翻，留值周巡检按判据处置。
