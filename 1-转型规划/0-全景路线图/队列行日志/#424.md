---
title: "队列行日志 #424（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #424 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #424 · 外置前整格原文（2026-09-04 外置，5 段）

[S:open][D:业] 待领（P2，**依赖唐燕萍签认**；⚠️ **`#418` 不得在本行有结论前宣告销号** —— 她四组举证里有两组卡在本行，`#418` 第⑸③ 那条「用她四组端到端复现」在现修复下只能做到 2/4） 　━━━　🔬 **2026-08-28 CC `OP-0828-A`：四条候选口径已实现、两条修法轴已在全量真实数据上量清；⒜⒝⒞ 原样未选，仍待唐燕萍签认**（与 `#423` 同车，commit `21639fa` 已 ff 合入 master）。 **⑴ 已做（代码）**：`resolve_item_code` 拆出 `strategy` 参数 —— `qty_price`（现状）／`qty_price_then_amount`（⒜）／`qty_price_then_single_item`（⒝）／`qty_price_then_subset_sum`（⒞），三个容差留显式入口**只供探针、生产调用方不传**。🔴 **默认值＝现状 `qty_price`，一字未改**，并配一条**退化守卫单测**锁死（依据 `#418` ⑻：整包同步部署下「留步不部署」守不住，唯一守得住的是默认值本身）。🔴 **回落只在「零命中」时发生、绝不在「歧义」时发生**——现状口径已找到多个候选说明这行本身就分不清挂哪一行，换把尺子只会换一批候选；**「换到能出一个答案为止」就是静默猜测**，已配反例单测。她两组失败案**逐字入夹具**。 **⑵ 全量实测（`.51` 本机只读，`scripts/probe_424_itemcode_candidates.py --resolve-ap-no`，14,292 次 `ap_no` 真反查 ＋ 883 张单 AP 明细，约 13.5 分钟；数据未出机器，临时目录收工整树已删，生产 `invoice.csv` mtime 全程停在 `2026-08-27 10:30:04`）**：现状口径挡掉 **1,813 行／421 张发票／421 张 AP 单**，其中 **221 张 AP 单今天在生产 `invoice.csv` 里一行都没有** —— ✅ **与 `OP-0827-F` 影子跑经完全不同的路径独立得到同一组数（1,342＋471＝1,813／421／221），这组数可以当既定事实用了。** **⑶ 🔴 轴 A（放宽容差）＝基本无效，这条最省事的路可以当场排除**：现状 0 行；±1e-4/±1e-3 → 38 行/22 张；±1e-3/±1e-2 → 46 行/24 张；±1e-2/±1e-2 → 61 行/33 张；**±5%/±5% → 122 行/63 张（仅 6.7%）**。⇒ **挡住这批行的不是精度，是计量单位换算与「发票按合计开票」，与容差无关**；何况 ±5% 的数量差在账上本身就该报出来而不是被容忍。 **⑷ 轴 B（换匹配键）实测**（末列＝**能让多少「今天零行」的 AP 单重新有发票**，分母 221）：⒜ `then_amount` 168 行/141 张/**114**；⒝ `then_single_item` 266 行/181 张/**175**；⒞ `then_subset_sum` 346 行/164 张/**53**；**⒜＋⒝＋⒞ 依次回落 645 行/336 张/206**。**被挡 1,813 行的形态分布（互斥五档）**：四种候选全都解不开 **663**／组合求和跨多料号（**须先接受「发票行拆分」才解得开**）**505**／组合求和唯一可解 316／金额唯一可解 177／AP 单只有一个料号 152。 **⑸ 🔴 两条实测更正本行原文（以本班实测为准，不替它改判）**：① 本行原写「⒜ …两个真实案例里这两个值都分毫不差」——**那说的是合计**；逐行比时 `AP-2026080137` 气泡袋那一行没有任何单独一行 AP 的金额等于发票行金额 ⇒ **⒜ 在「按合计开票」这一类上同样零命中，不是通解**（单测锁死）。② ⒞ 能凑出 6000＝1000＋5000，**但跨两个料号**，而 `invoice.csv` 每行必须带**一个** `item_code` ⇒ **要真正解开这一类，必须同时接受「把一张发票行拆成多行写进 `invoice.csv`」——那是另一个口径决定，不是换个匹配函数就完事**。 **⑹ 🔑 本次最该被记住的一句（比这些数更要紧）**：**摄取层的职责是「认领」——这张发票属于哪张单、哪个料品；「合不合得上」是判定引擎的职责。** 现在这道闸把两件事混成一件：数量对不上的发票**不是被报出来、而是被丢掉**，于是以「无发票支撑（发票还没到）」的面目上板 ⇒ **对账系统正好在它该失明的反面失明了。** ⒝ 之所以单看最有效（175/221）又最容易被误会成「最松」，正是这个道理：发票已由发票号唯一反查到 AP 单、而该单通篇只有一个料号 ⇒ **认领这一步不含猜测**，它「松」在不再要求数量金额对得上 —— **而那本就该交给引擎去判、落进「数量金额不符」那一档给人看**。⚠️ **但代价必须一并说给她**：接受 ⒝ ＝ 接受面板「数量金额不符」那一档明显变长，**变长多少、她的团队接不接得住，只有她能判**。 **⑺ ✅ 本行第①件「先在全量数据上量一次各候选的命中率与歧义率」已完成**（⑶⑷ 即是）；②③④ 仍待她签认后才动。 **⑻ 🔴 丢行可见并可查已默认生效（本项不属口径）**：未解析记录落盘 `reports/fi2_ingest_diagnostics.jsonl`（**追加写**，可按 reason／发票号／AP 单号回查）＋ 按 `reason` 汇总打印（真实数据 26,000 行起步，**逐条打印等于没人在看**）＋ 每条挂不上料号的行带一段可 grep 的分类身份 `ap=… ap行数=… 料号数=… 换口径可解=[…]`。🔴 **不含金额与数量原始值**（FI2 审计口径＝金额不落盘，该文件长期留存、不得成为金额的第二个出口，单测逐值断言不出现）；**不改变任何摄取结果**。⇒ 这补上的是 `#418` 根因链的第 ⑵ 环（诊断只被 CLI 打印、从不落盘）——`#418` 修的是「还有没有下一次机会」，本行修的是「已经丢掉的这批看不看得见」。 **回归 230 passed / 9 skipped**（原 201+9）。**全部数字与取证见** `4-数字员工/财务部/FI2-三单匹配自动对账/docs/queue_423_424_口径候选与影响面-2026-08-28.md` **§二**。 🔴 **本行不销号**：⒜⒝⒞ 属账务口径，须走判例批改法请唐燕萍显式签认（**串行闸状态以跟进信 README「发送状态」列为唯一权威，本行不复述**）。　━━━　📨 **2026-08-28 CC `OP-0828-H`：`财务部#16` 判例批改包已备齐，状态 `⏳ 待你审`，**🔴 未发送****（Shao Peishen 当日明示「唐燕萍的签字他线下确保落实」⇒ 本件定位＝**把材料备齐到他随时能拿去线下用**，不走自动推送）。**产物**：`6-人才与组织/部门AI专员跟进/财务部-唐燕萍-跟进-2026-08-28-面板KPI与发票认领两处口径判例批改.md` ＋ 同名 `.docx`（**24 枚 `w14:checkbox` 真复选框、预勾选 0，已解压 `document.xml` 逐枚核过 `w14:checked val="0"`**；frontmatter 未渲染进正文）；README 登记行「发送状态」＝ `⏳ 待你审`、编号列带「（待你审，暂不占号）」。**串行闸本班已独立复核过**（不凭派单件转述），🔴 **但本行不复述闸态与信状态**（它会过时）——判据唯一入口＝`python 0-学习与工具/工具-跟进闸查询.py --to 唐燕萍`，权威载体＝跟进信 README「发送状态」列。**起草期代词自检**：全文第三人称代词**命中 0 处**（正则扫 `[他她]`，显式核过而非跳过——skill v3.7 §5 步骤 2 要求命中 0 也须出声）。 🔴 **发送硬前置两条今天未满足，任何人不得据本行认为可以发**：② `.51` 已部署且冒烟通过 —— **未满足**（`OP-0828-A` 刻意留步未部署，理由＝口径未定就部署等于替她定了默认值，且 `.51` 是整包同步部署、「留步不部署」守不住）；③ 用她原始举证做端到端复现 —— **只做到数值层**（面板有登录口令闸，须先部署再由人登录截图，**截图对照未做**）。① 改动已 ff 合入 master 由 `OP-0828-A` commit `21639fa` 满足。 **本行相关的判例表 B（3 条）＋ 判例表 C（1 条）**：B 按实测形态切分且全部用她自己举证的那两张单——判例 5＝计量单位/包装换算（`AP-2026080041` 密封胶 20 支 @37.014 vs 6200.0 @0.1194，`20 支 × 310ML`，未税 655.12／税额 85.16 一分不差）；判例 6＝该 AP 单通篇只有一个料号（仍是 `AP-2026080041`，信中讲清「认领这一步不含猜测」的道理）；判例 7＝同料号多行合计开票（`AP-2026080137` 气泡袋 7000 ＝ `R02E.0016` 4000＋3000）。C 判例 8＝跨两料号合计开票（气泡袋 6000 ＝ `J02E.0024` 1000＋`R02E.0024` 5000，同形态 505 行），拟改＝拆两条入库、金额按数量比例分摊、同带一个数电发票号码，并写明「答 ❌ 会怎样」。 **四条实测事实全部如实入信、一条未略**：⑴ **轴 A（放宽容差）已排除**（±5% 只捞回 122/1,813 ＝ 6.7%），信中明写「这条最省事的路你不用考虑」；⑵ **⒜ 不是通解**（逐行比时气泡袋那行同样零命中，本班实测更正本行原文，以实测为准）；⑶ **⒝ 的代价**（面板「数量金额不符」那一档会明显变长，**变长多少、团队接不接得住只有她能判**），并给出「先小步走＝只勾 5 和 7」的退路；⑷ 回落数 ⒜114／⒝175／⒞53、三者依次 **206/221** 与分母 221 全部写明；**另 663 行四种候选全都解不开，信中明说本次不问、不占她时间**。 🔴 **本行仍不销号**：⒜⒝⒞ 属账务口径，须等她显式签认。 　━━━　🔬 **2026-09-03 CC `OP-0903-F1`（`B-0903_90` A3 段）：独立复核，代码/测试/既有实测数字维持既有结论，未新增任何测量（无真实数据访问）** —— 复核方法：重读 `fi2/tax_export_ingest.py::resolve_item_code`、五个候选策略实现、`_item_match_diagnosis` 全文，逐条核对与 `docs/queue_423_424_口径候选与影响面-2026-08-28.md` §二记录一致；单测覆盖两个真实案例（`AP-2026080041` 密封胶 310ML 单位换算、`AP-2026080137` 气泡袋跨料号合计开票）均在位；FI2 全量测试 **230 passed/9 skipped**，`_ITEM_MATCH_STRATEGY` 默认值仍为 `"qty_price"`（现状，一字未改），退化守卫单测在位。**本轮无法访问真实数据**（同 `#390`／`#418` 本轮回写所述环境限制）⇒ 既有实测数字（挡掉 1,813 行/421 张发票，其中 221 张今日零行；轴 A 放宽容差最多只捞回 6.7%；轴 B 换匹配键三候选依次回落114/175/53、合计 206/221）沿用既有记录，不重新测量、不外推。**本行仍不销号**：判据/口径类须唐燕萍显式签认，本轮串行闸复核仍为 🔒 锁（依据 `财务部#16`，2026-08-28 06:21 UTC 已推送、在途未回；2026-09-01 桥接自动转态两次误标“回件已到”均已被更正撤销，见跟进信 README 该行）。 　📌 **性别豁免（本轮 CC `OP-0903-F1` 补，同 `#423` 已有惯例）**：本行历史文本「唐燕萍的签字他线下确保落实」一句里的「他」指 Shao Peishen 本人、不指唐燕萍（同行出现两人，属守卫说明里的合法情形）。原句为既有文本，按「历史记录不追改」保持原样，仅在此加豁免标记。 　📌 性别豁免：本行历史文本「唐燕萍的签字他线下确保落实」一句里的「他」指 Shao Peishen 本人、不指唐燕萍（同行出现两人，属守卫说明里的合法情形）。原句为既有文本，按「历史记录不追改」保持原样，本注为 CC `OP-0903-F1` 补记，同 `#423` 已有惯例一致。 　━━━　📥 **2026-09-04 唐燕萍回件已拆件（`财务部#16`，Cowork 业务总线 OP-0904-B）**：判例表 B（判例5/6/7，摄取层认领口径）**未见她的文字或勾选作答**，本次未获裁定，本行阻塞未解除。判例表 C（判例8，跨料号拆行）她书面 ✏️ 式作答：**同意拆行，但要求拆行后两条的未税金额与税额分别相加须与原发票行一致，分摊产生的尾差并入后一条记录（不单独成一条）**——原文见归档 docx 第 62 段。🔴 **暴露一处 docx 缺陷**：她反映「表格最后一列『应改判为』无法输入文字」，全部 9 行勾选格（w14:checkbox 直读）实测均为空，**需登记跟踪、不得当『她没答』**。逐句转录见队列 §一 `#485`。
