---
title: "队列行日志 #403（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #403 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #403 · 外置前整格原文（2026-09-04 外置，13 段）

[S:open][D:业] 🟡 **①② 已交付（2026-08-26，OP-0826-P），③④ 未做、本行不销号** ━━━ **①** 需求规格正本已改 **v2**（`IT接口需求-U9C自建端点-生产工单与采购价目表-2026-08-24.md`，新增 〇bis 修订记录五条；§三 重写为他索要的**详细字段清单**，切成 🅰 料品级 2 个字段／🅱 供应商级 **7 项无着落**；新增 §3.4 封装成本归属、§3.5「无强相关是哪种无」二选一请他回一句、§3.6 明确不要的字段）。**②** 消化件＋拍板选项 **D-1~D-5** 落 `1-转型规划/0-全景路线图/IT部10回件消化-A线路线分叉与B线前提-2026-08-26.md`，同步立 §四 #125。 ━━━ 🔑 **三条实质结论**：⑴ **端点 A 价值主张缩水**——原称「让三条算式成立」，实为**只剩 moq/mpq 一条能修**；提前期已被他两次否掉（K2），`is_approved` 来源未答且「料号与供应商无强相关」暗示**可能根本无源** ⇒ v2 已如实改写，**不照 v1 再发一次**。⑵ **B 线拟撤回，非降优先级**——ERP 无生产工单实体，端点建了是恒空表；**替代实体已在我方手上且已在生产中用**：全程委外采购订单 `ZpViewPurOrder.erpTypeCode ∈ {PO14,PO16,PO17}`（`models.py:61-67`、`sc2/sources.py:48`，SC2 周报据此剔除），**无须 IT 新建任何端点**——但它无实际开工／完工日、无工时，**替代得了「有没有在生产」，替代不了 O1 要的「工时是多少」** ⇒ O1 晋档 2 第 1 条解锁条件（MO 可取数）**永不满足、须改写**，第 2 条（PMC 签认 SMT 工时）在「无车间／产线」下**标的存疑**；连带 IT部#9 那 ¥72,000 授权**买了也取不到 MO**（给 08-24 不买的拍板补了个当时不知道的独立理由）。⑶ 🔴 **本次新发现第三处冲击（opener 与拆件均未列）**——**SC8 委外识别可能恒假**：`sc8/config.py:298` `OUTSOURCE_PRODUCT_IDS=set()` 且前缀规则默认关、工艺路线口径未接通 ⇒ `is_outsourced()` 恒 `False`，而它在 `forecast.py:308-318` 同时门控 **+10 天附加工期**与**置信度降级** ⇒ 若「全部成品外协」成立，则对外交期**系统性偏乐观、且被贴上「高置信」标签**。与 `is_approved` 恒真同族第三入口（过滤从未生效且不产生信号）。**处置见 §四 #125 D-4，明确不建议按一句转述翻口径。** ━━━ **未做（如实登记，非遗漏）**：③ 起草 IT部#11 —— opener 红线不起草对外信，且内容依赖 D-1／D-3 拍板；④ K1/K2 落 SC7/SC8 载体 —— K2 替代路线未拍板，此时落下去是半句话，且属建造侧改动；不替 O1/FI1 改范围（红线）。🔒 **LAN 留步**：`.51` 的 `SC8_OUTSOURCE_IDS` 是否已配，本机 off-LAN 查不了。 ━━━ 以下为原登记 ━━━ 待领 　━━━　🔎 **2026-08-27 `OP-0827-A` 波次收口核定（本行未被续跑，特此留痕）**：2026-08-26 泳道波次 runner 退出时打红字建议 `续跑：-Only A11,A12,A5`，**该建议已实测判为不能照做**。🔑 **判据一句：`NO-SENTINEL` 只说明顶格那行 `OPENER_DONE`／`OPENER_PARTIAL` 没打出来，不说明活没干完。**三条里两条是**干完了没打哨兵**（A11／A12，日志末尾均为完整收工报告＋「需要你手动做的事」清单）、一条是**干完了没提交但产出已被救回**（A5，产出在 `OP-0826-U` 的 `35dd6ae` 里、已进 origin/master）。**照红字续跑 ＝ 让三条 session 重做已完成的活**，正是 `#396`「同一件活被反复派出」那一族——⚠️ **而这次的诱因是机器给的建议，比人误判更容易被照单执行**。 **本行（A11／`OP-0826-P`）实测**：其产出未起 worktree、直接写在主工作区，已由本次收口按产出方拆分单独提交＝`5b4baa6`（需求正本改 v2 ＋ `IT部10回件消化-A线路线分叉与B线前提-2026-08-26.md`），已 push。 　━━━ 🟢 **2026-08-27 `OP-0827-E`：本行的 LAN 留步已解除（取证完成，结论＝未配）** —— 原文「`.51` 的 `SC8_OUTSOURCE_IDS` 是否已配，本机 off-LAN 查不了」。**实测（on-LAN，SSH 只读，只取键名不读值）**：`C:\baoguan\.env` 共 **19 个键** —— `WECOM_WEBHOOK_URL`／`XKY_API_BASE`／`XKY_OWNER_COMPANY_CODE`／`XKY_APP_KEY`／`XKY_APP_SECRET`／`XKY_ERP_CODE`／`FO_API_BASE`／`FORECAST_API_KEY`／`U9C_API_BASE`／`U9C_USER_CODE`／`U9C_ENT_CODE`／`U9C_ORG_CODE`／`U9C_CLIENT_ID`／`U9C_CLIENT_SECRET`／`U9C_DATA_SOURCE`／`STOCK_API_KEY`／`STOCK_API_BASE`／`SC8_NET_INVENTORY`／`ZP_GATE_PASSWORD`，**`SC8_OUTSOURCE_IDS` 不在其中 ⇒ 未配**。 ⚠️ **本条只解除「查不了」这一个留步，不解除本行** —— ③ 起草 IT部#11 与 ④ K1/K2 落载体仍依赖 D-1／D-3 拍板。 ━━━ 🟢 **`OP-0831-L` 首跑灰度批波次1（只读）已收口（2026-08-31）**：判据＝打印 `.51` 上 SC8 服务实际加载的 `.env` 全部键名并回答 `SC8_OUTSOURCE_IDS` 在/不在，🔴 须实读不得复用旧清单。通路＝SSH key-based（`~/.ssh/config` 既有别名 `supplychain-server`：`HostName 192.168.100.51／User Administrator／IdentityFile ~/.ssh/supplychain_server`，免密钥，实测可用；`.51部署标准清单-2026-08-01.md` 全文未记载此通路，本次系首次落档）。证据＝在 `.51` 上 SC8 自己的 venv（`C:\baoguan\.venv\Scripts\python.exe`）内直接调用真实 `zhuopin_platform.env_anchor.resolve_env_file()`（非猜路径、非复用旧结论）：`resolved_path=C:\baoguan\.env｜anchor=flat｜19 键｜SC8_OUTSOURCE_IDS_present=False`，与 `OP-0827-E`（2026-08-27）19 键结论逐字交叉核验一致（同一 `.env`，4 天内无漂移）。处置＝纯只读，无需回滚；不改变本行既有③④未做结论与其余记载。 ━━━ ✅ **2026-09-01 Cowork 业务总线 `OP-0901-E`：本行 ③④ 的口径依赖已全部解除** —— §四 #125 五项拍板于当日落定（**D-1(a)／D-2(b)／D-3(a)／D-4(a)／D-5(a)**，全文与各项代价见 §四 #125 本日追记，此处不复述）。 **⇒ 解锁后的下游三件，本班一件未做、如实登记**：⑴ **起草 IT部#11**（合并 D-1 的自建端点请求 ＋ D-3 的「无强相关是哪种无」歧义）—— 2026-09-01 现跑 `python 0-学习与工具/工具-跟进闸查询.py --to 陈承` ＝ ✅ **开**（依据 `IT部#10` 📥 已回件并回灌 2026-08-25、已闭环），**串行闸不挡、可起草**；起草后须经 Shao Peishen 审核方可发出（第 8 步纪律）。⑵ **D-2(b) 提前期中位数统计**（`Purchase/Query` ＋ `GR/Query` 按料号统计「制单日→实际入库日」）—— 属建造侧，交 CC，本线不做。⑶ **D-5(a) 撤回 B 线的连带改写**（O1 晋档 2 第 1 条解锁条件改写）—— 属 `.md`，Cowork 可做，但触碰 O1 场景口径，开工前须先 grep 两份真身＋归档确认无他人在办。 ⚠️ **D-4(a) 的一半已有实测在手、不必重做**：`.51` 的 `SC8_OUTSOURCE_IDS` **未配**已由 `OP-0827-E`（2026-08-27）与 `OP-0831-L`（2026-08-31）两次独立取证坐实（同一份 19 键 `.env`，四天无漂移）；**D-4(a) 剩下的只是「请 PMC／运营部书面确认是否全部成品外协」这一半**。 **本行仍不销号**：③④ 一件未做。  ━━━ ✅ **2026-09-02 子项 ⑶（D-5(a) 连带改写）已执行（Cowork 业务总线 `OP-0902`）；⑴⑵ 仍未做** —— **⑸ 真身定位**：那两条「O1 晋档 2 解锁条件」**不在全景规划、也不在前置总表**，真身是 `4-数字员工/运营部/O1-生产排程智能优化/CLAUDE.md` **§6「关键依赖/前置（解锁条件）」**（晋档 2 须四条全满足）。⚠️ 「晋档 2」也不是 O1 块的字段，而是全景规划 §加速启动总览「**上线四档口径**」里的档 2（真实数据跑通）——**先定位真身再动手，避免改到转述件上**。 **⑹ 已改（§6 第 1、2 条）**：第 1 条由「等 `UFIDA.U9.MO.MO.MO` 可取数」**改判为已被证伪、永不满足**（PMC 原话：ERP 无生产工单、无车间产线、全部外协 ⇒ MO 实体恒空，端点建成也是空表），改用**委外 PO** `ZpViewPurOrder.erpTypeCode ∈ {PO14,PO16,PO17}`（已在 SC2 生产使用、不卡 IT）取「在产与否」；🔴 **同时写死它替代不了什么**——委外 PO 无开工/完工日、无工时，其「制单日→入库日」是**供应商交付周期、是另一个量**。第 2 条补注**标的存疑**（「我司的 SMT 工时」这个量是否存在需先回答）＋ 原提取方法（从历史工单反推）已同步失效。**原文全部保留作沿革，未删。** **⑺ 🔴 D-5(a) 的后半句「重新定义可达目标」本线刻意未做**——那是**场景范围变更**，opener 与本行原文均写明「不替 O1／FI1 改范围（红线）」。已在 O1 CLAUDE.md 新立 **§6.1** 摆出资产盘点与三个方向（改标的／降范围保留／注销并入），并**登 §四 `#150` 请 Shao Peishen 定夺**（不推荐、不设默认）。 🔑 **⑻ 本次留下的判据**：**在他定夺之前，O1 晋档 2 记为「条件未定」而非「条件未满足」** —— 后者会让人继续等一个不会到来的端点，前者会让人去找他要一个决定。⚠️ 同族已实证：`IT部#9` 那 ¥72,000 授权诉求的头号理由就是「O1 取不到 MO 被定为档 1」，而**买了也取不到、因为里面没数据**。 **⑼ 本行仍不销号**：⑴ 起草 `IT部#11`（陈承闸开、可起草，须他审后发）与 ⑵ D-2(b) 提前期中位数统计（交 CC）**均未做**。  ━━━ 📤 **2026-09-02 子项 ⑴ 已起草、待 Shao Peishen 审（Cowork 业务总线 `OP-0902`）；⑵ 仍未做，本行仍不销号** —— **⑽ 产出**：`IT部#11` ＝ `6-人才与组织/部门AI专员跟进/IT部-陈承-跟进-2026-09-02-端点A字段清单与一句澄清并撤回端点B.md` ＋ 同名 docx（md 8,814 B ／ docx 44,009 B），README 已登记、发送状态 `⏳ 待你审`，陈忱……**更正：陈承**闸随起草转 🔒 在途。 **⑾ 一封信办三件（按 §四 #125 拍板合并）**：① **D-3(a)** —— 「料号与供应商无强相关」是哪种「无」写成二选一，**放在字段清单之前问**，因为它直接决定端点 A 在技术上成不成立；② **D-1(a)** —— 端点 A 详细字段清单全文内嵌（🅰 料品级 2 字段＋1 建议／🅱 供应商级 7 项无着落），**结论写死：第 7 `UnitPrice`、第 8 `IsApproved` 两项必需而料品主档没有 ⇒ 只靠 `ItemMaster/Query` 解决不了**；③ **D-5(a)** —— 端点 B 撤回并请他停手，理由用他自己 08-24 那句 PMC 转述（ERP 无生产工单、无车间产线、全部外协 ⇒ MO 恒空），**明写「不是降优先级，是做出来没有用」**，并如实交代替代方案（委外 PO）我方自解、**且它替代不了工时、那是我方自己的范围问题不是他的欠账**。 🔑 **⑿ 本信刻意做的一件事（判据的第一次正面应用）**：主动声明「**完整需求规格是我方内部文件，本信已把与你相关的内容全部摘出，你不需要另找任何附件**」，并点名上一次给质量部的信写了「见附件」而实际没有附件、让人白找一趟。**不是不写附件，是主动消除「是不是还有个附件我没收到」的疑问**——同 §四 #148 ⑻ 那条「我手边有 ≠ 他手上有」。 ⚠️ **⒀ 一处系统性泄漏点，值得下一位起草者知道**：**跟进信 frontmatter 的「源:」字段是代词自检的固定命中点** —— `质量部#12` 与 `IT部#11` **连续两封都在同一位置写了第三人称代词**（分别是「她」「他」，指向均正确、名录已核，但仍逐次改写为实名）。⇒ **起草模板里那一行应直接写实名，不写代词**；两封的最终自检均为「他 0／她 0」。 **⒁ 本行仍不销号**：子项 ⑵ **D-2(b) 提前期中位数统计**（`Purchase/Query` ＋ `GR/Query` 按料号统计「制单日→实际入库日」）属建造侧、交 CC，**本线不做**；且 ⑴ 尚待他审后才发。 ━━━ ✅ **2026-09-02 子项 ⑵（D-2(b) 提前期中位数统计）已执行完毕（CC `OP-0902-A2`，泳道 `sc8-leadtime`）；⑴ 仍待审未发，本行仍不销号** —— **⒂ 🔴 落点更正（如实登记，非扩范围）**：泳道名叫 `sc8-leadtime`，但 `lead_time_days=30` 的**真实消费方是 SC7 不是 SC8**。取证：`connector.py:1133` 的 `Supplier.lead_time_days` 全库只被 `4-数字员工/采购部/SC7-库存优化建议/sc7_inventory/purchase_engine.py::calc_order_date` 消费（最迟下单日 ＝ 计划日 − 提前期 − 3）；**SC8 的 `lead_time_map` 是另一个量**（SMT 生产工时，产品级，齐料日→完工），与采购提前期无关——与 ⑹ 里「委外 PO 的制单日→入库日是供应商交付周期、是另一个量」同族。⇒ 脚本落 SC7。 **⒃ 产出**：`4-数字员工/采购部/SC7-库存优化建议/scripts/leadtime_median.py`（可重跑，整表分页＋4h 磁盘缓存）＋ `4-数字员工/采购部/SC7-库存优化建议/tests/test_leadtime_median.py`（22 条单测，SC7 全量 **63 passed 零回归**）＋ 报告四件落 `4-数字员工/采购部/SC7-库存优化建议/reports/`（`**/reports/` 全库 gitignore，故不入库）。已 commit＋push `claude/op0902a2-leadtime-median`（`a74e7d9`），§二 批次 `B-0902_48`。 **⒄ 结论（首次到货·近 24 个月）**：出数料号 **471** 个、样本不足 3,480 个；料号中位数的中位 **26 天**（P25 17／P75 47.5／min 1／max 192.5）。🔑 **20% 料号中位数 ≤15 天、26% >45 天** ⇒ 一律按 30 天，**对短周期料过度提前、对长周期料严重滞后**，而那正是采购拿来排优先级的那一列。 🔑 **⒅ 本次最该被下一位知道的一条：这个数没有唯一答案，口径带宽比想象的宽。** 同一份数据换口径重算——首次到货·近24月 ＝ **26 天**／末次到货·近24月 ＝ **36 天**／首次到货·全历史 ＝ **20 天**。**20~36 天的差别全部来自口径选择（首次 vs 末次到货、窗口长短），不是来自数据质量。**⇒ **下游若要用这个数，必须连口径一起用，不能只搬数字**；三档已全部出报告备查。 **⒆ 数据卫生（漏斗逐条计数，不静默丢弃）**：JOIN 命中率 **27,186/27,186 ＝ 100.0%**（低于 30% 即 fail-loud 中止出数）；H1 无来源单号剔 983／H2b 零入库剔 567／H5 提前期为负剔 3／窗口外剔 11,140 ⇒ 进入统计 10,167 行。⚠️ **命中率 100% 属「太干净」，已按取证知识库 §二 判据反向验过一次**：`PO*`／`SF*`／`DLJS*`／`ZPCG*W` 等异形前缀**实测确在 `Purchase.DocNo` 中存在**（早先 5 页抽样未覆盖到而已），两侧行号均无空值 ⇒ 不存在空键互撞的假命中；另做端到端抽验：料号 `R02B.0005`（n=38）脱离脚本手工复算，中位数 40.0 与报告逐位一致。 🔴 **⒇ 一处必须如实说、不能含糊的**：opener 要求「剔除退货/红字单」——**实测 `GR.RcvQtyTU<0` 与 `Purchase.ConfirmQty<=0` 两处均为 0 行**（GR 最小值 0、PO 最小值 0.5）。⇒ 正确表述是「**红字行未在数据中出现**」，**不是**「已把退货剔干净」。两端点**均无单据类型/状态字段**可识别独立退货单 ⇒ **若公司退货走独立单据类型而非负数量冲销，本统计既看不到也无从剔除**——**这一点无法从我方现有端点自证，需 IT 或采购确认**（与 `is_approved` 恒真、`is_outsourced()` 恒假同族：**过滤器在位、但从未匹配到任何东西，且不产生任何信号**）。另三条口径边界（分批到货取首次是选择非唯一解／长周期料因幸存者偏差系统性低估／料号×多供应商合并中位数会抹平差异）已全部写进报告 §四。 🔴 **(21) 未替换任何生产参数**：`connector.py:1133` 的 `lead_time_days=30` **原样保留、未动**——替换属改口径（🟡 档），opener 明令须先经 Shao Peishen。本件只交「这个数是多少、怎么算的」，**替不替、按哪个口径替，是他的决定**。 **(22) 本行仍不销号**：子项 ⑴ `IT部#11` 尚在 `⏳ 待你审`，未发出。

---

# §一 #403 · 外置前整格原文（2026-09-04 外置，3 段，md5:4297f608）

[S:open][D:业] 🟡 **①② 已交付（2026-08-26，OP-0826-P），③④ 未做、本行不销号** ━━━ ✅ **2026-09-02 子项 ⑵（D-2(b) 提前期中位数统计）已执行完毕（CC `OP-0902-A2`，泳道 `sc8-leadtime`）；⑴ 仍待审未发，本行仍不销号** —— **⒂ 🔴 落点更正（如实登记，非扩范围）**：泳道名叫 `sc8-leadtime`，但 `lead_time_days=30` 的**真实消费方是 SC7 不是 SC8**。取证：`connector.py:1133` 的 `Supplier.lead_time_days` 全库只被 `4-数字员工/采购部/SC7-库存优化建议/sc7_inventory/purchase_engine.py::calc_order_date` 消费（最迟下单日 ＝ 计划日 − 提前期 − 3）；**SC8 的 `lead_time_map` 是另一个量**（SMT 生产工时，产品级，齐料日→完工），与采购提前期无关——与 ⑹ 里「委外 PO 的制单日→入库日是供应商交付周期、是另一个量」同族。⇒ 脚本落 SC7。 **⒃ 产出**：`4-数字员工/采购部/SC7-库存优化建议/scripts/leadtime_median.py`（可重跑，整表分页＋4h 磁盘缓存）＋ `4-数字员工/采购部/SC7-库存优化建议/tests/test_leadtime_median.py`（22 条单测，SC7 全量 **63 passed 零回归**）＋ 报告四件落 `4-数字员工/采购部/SC7-库存优化建议/reports/`（`**/reports/` 全库 gitignore，故不入库）。已 commit＋push `claude/op0902a2-leadtime-median`（`a74e7d9`），§二 批次 `B-0902_48`。 **⒄ 结论（首次到货·近 24 个月）**：出数料号 **471** 个、样本不足 3,480 个；料号中位数的中位 **26 天**（P25 17／P75 47.5／min 1／max 192.5）。🔑 **20% 料号中位数 ≤15 天、26% >45 天** ⇒ 一律按 30 天，**对短周期料过度提前、对长周期料严重滞后**，而那正是采购拿来排优先级的那一列。 🔑 **⒅ 本次最该被下一位知道的一条：这个数没有唯一答案，口径带宽比想象的宽。** 同一份数据换口径重算——首次到货·近24月 ＝ **26 天**／末次到货·近24月 ＝ **36 天**／首次到货·全历史 ＝ **20 天**。**20~36 天的差别全部来自口径选择（首次 vs 末次到货、窗口长短），不是来自数据质量。**⇒ **下游若要用这个数，必须连口径一起用，不能只搬数字**；三档已全部出报告备查。 **⒆ 数据卫生（漏斗逐条计数，不静默丢弃）**：JOIN 命中率 **27,186/27,186 ＝ 100.0%**（低于 30% 即 fail-loud 中止出数）；H1 无来源单号剔 983／H2b 零入库剔 567／H5 提前期为负剔 3／窗口外剔 11,140 ⇒ 进入统计 10,167 行。⚠️ **命中率 100% 属「太干净」，已按取证知识库 §二 判据反向验过一次**：`PO*`／`SF*`／`DLJS*`／`ZPCG*W` 等异形前缀**实测确在 `Purchase.DocNo` 中存在**（早先 5 页抽样未覆盖到而已），两侧行号均无空值 ⇒ 不存在空键互撞的假命中；另做端到端抽验：料号 `R02B.0005`（n=38）脱离脚本手工复算，中位数 40.0 与报告逐位一致。 🔴 **⒇ 一处必须如实说、不能含糊的**：opener 要求「剔除退货/红字单」——**实测 `GR.RcvQtyTU<0` 与 `Purchase.ConfirmQty<=0` 两处均为 0 行**（GR 最小值 0、PO 最小值 0.5）。⇒ 正确表述是「**红字行未在数据中出现**」，**不是**「已把退货剔干净」。两端点**均无单据类型/状态字段**可识别独立退货单 ⇒ **若公司退货走独立单据类型而非负数量冲销，本统计既看不到也无从剔除**——**这一点无法从我方现有端点自证，需 IT 或采购确认**（与 `is_approved` 恒真、`is_outsourced()` 恒假同族：**过滤器在位、但从未匹配到任何东西，且不产生任何信号**）。另三条口径边界（分批到货取首次是选择非唯一解／长周期料因幸存者偏差系统性低估／料号×多供应商合并中位数会抹平差异）已全部写进报告 §四。 🔴 **(21) 未替换任何生产参数**：`connector.py:1133` 的 `lead_time_days=30` **原样保留、未动**——替换属改口径（🟡 档），opener 明令须先经 Shao Peishen。本件只交「这个数是多少、怎么算的」，**替不替、按哪个口径替，是他的决定**。 **(22) 本行仍不销号**：子项 ⑴ `IT部#11` 尚在 `⏳ 待你审`，未发出。 ━━━ 📎 历史回写 11 段已于 2026-09-04 外置（K2 口径，原文原样 md5:72fc4735）见 `1-转型规划/0-全景路线图/队列行日志/#403.md`；本格只留首 1 段＋末 1 段
