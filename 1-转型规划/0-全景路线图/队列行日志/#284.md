---
title: "队列行日志 #284（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #284 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #284 · 外置前整格原文（2026-09-04 外置，19 段）

[S:hold][D:机] 🛑 **不占 WIP（2026-08-19 定夺 2(a)：结构性不可动的 hold 行统一改用既有 `🛑` 首标记，`_count_mechanism_wip` 据此结构性排除；本行语义未变、状态字段未动）** ／ **🔒 常驻·不清扫**（协议〇.8 每周清扫须跳过本行；本行不设完工态） 　━━━　➕ **2026-08-28 登记一条新形态与它的机制守需求（Cowork `OP-0828-N`；Shao Peishen 当日判「Session 中途改名需要根治」后定案）** ━━━ **形态（新）：Task/Agent 子任务执行 opener 正文里的 `set_session_title（session_id 传字面量 "self"）` 时，`"self"` 解析到的是父 session ⇒ 子任务把调度它的那条会话改名，调用成功、无报错**（同族＝「工具静默回退」）。**实撞**：本日 `看护件-2026-08-28-落地后批.md` 要求看护者「opener 正文原样传给子任务、不要改写」，`OP-0828-R` 一起活就把看护者 `[Win]0828W2-落地后批四条看护` 改成了 `[Win]0828R-416回件链路三缺陷`；四条会互相覆盖，左栏里再也找不到看护者。 ━━━ 🔴 **首版修法（人守）已作废**：原写「派工者传给子任务前必须删掉那一行」—— 它是**另一条规则（「原样传、不要改写」）的例外**，正合接力卡判据「**一条需要先判断再选择的规则，就是一条会被忘的规则**」，Shao Peishen 当场判为需要根治。 ━━━ ✅ **根治已落（选 ①：前提焊进那一行）**：标准写法改为 `…标题：[Win]MMDDX-<主题短名>。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。` ⇒ **一份正文两种用法都对，没有人需要记得删任何东西。** 🔑 **核心判据**：**被复制的单位是「那一行」，不是「那条规则」** —— 写在模板库里的前提只在有人去读那份文件时才存在，写进那一行本身则任何复制它的人都会连前提一起复制。⇒ **凡一条指令带隐含前提，把前提写进指令本身，而不是写进一份可能不被读的规则。** 落点＝`1-转型规划/0-全景路线图/专线opener模板库.md` §〇 补充三之三「根治」节 ＋ 补充三「标准写法」块；当前在用件 `1-转型规划/0-全景路线图/看护件-2026-08-28-落地后批.md` 五处已改（Shao Peishen 答「只改正本＋当前在用件」，**历史已执行件按「历史记录不追改」不动**）。 ━━━ 🔴 **待建的机制守（本条登记，待领）——一条 lint 一次收两个失效形态**：扫 `.md` 里的 opener 代码块，① 块内含 `【设置】` 而**无** `set_session_title` ⇒ 告警（**旧形态**：编号丢失，2026-08-27 一天欠 17 次，已达退休制阈值）；② 块内**有** `set_session_title` 而**无**子任务例外句 ⇒ 告警（**新形态**，即本条）。**在它建成前：① 仍是人守且已知守不住；② 已由「前提焊进那一行」消掉条件。** ⚠️ 本条只登记需求与判据，**未建造**；建造须另行派单（触碰区＝`0-学习与工具/` 下 lint 工具族）。 　━━━　✅ **机制守已落（2026-08-28，CC `OP-0828-Y`；分支 `worktree-agent-ab0c375af1d22b136`，commit `553389e`，已 push origin，🔴 未合入 master、待合入）——本条自此由人守转机制守**：新建 `0-学习与工具/工具-opener块lint.py` ＋ `0-学习与工具/test_工具-opener块lint.py`（19 条单测，两形态各配正例／反例，并各配一条「改对之后告警消失」用例）＋ CI 注册 job `opener-block-lint`（`.github/workflows/ci.yml`，`fetch-depth: 0`，**告警模式不阻断**）。 ━━━ 🔴 **与本行需求原文的一处刻意收窄，依据是实测不是偏好**：原文写「块内含 `【设置】` 而无 `set_session_title` ⇒ 告警」，未分执行环境；**而模板库 §〇 补充一已实测坐实 Cowork 侧根本没有这个工具**，并明写「把 CC 的做法抄给 Cowork 会写出一个不存在的工具调用」⇒ 形态① **只对 `执行环境：CC` 成立**，Cowork 块**结构性排除**（不收窄则本 lint 会对 Cowork 块报警，而按规则去修它们全都是错的——那正是「关不掉的告警等于噪音」）；`执行环境` 未标者**不猜**、单列一桶、不判形态①。 ━━━ **全库首扫（2026-08-28 本机实跑）——扫描面 ＝ `git ls-files "*.md"` 全部 1044 份已跟踪 md，无目录白名单、无文件名过滤，只排除未跟踪件**：候选文件 83 份，opener 块 189 个（CC 141 ／ Cowork 40 ／ 执行环境未标 8），含 `set_session_title` 的块 66 个；**命中合计 131 处 —— 当前在用件 30 处（形态① 16 ／ 形态② 14），历史件 101 处（形态① 60 ／ 形态② 41）**。 ━━━ **区分判据（三层，任一命中即历史件；🔴 不靠目录名猜）**：**H1** 路径含 `z-已执行归档/` **目录段**（R3 生命周期定义的归档物理落点）；**H2** frontmatter `status` 归桶 ∈ 已执行归档／已作废／历史快照，判定**复用** `工具-文档台账生成.py::status_bucket()`（R1 机制守的六枚举 ＋ 同义词表，**不自造第二套判据**）；**H3** 该文件**最后一次 git 提交时刻**早于该形态所属规则的生效日（形态① ＝ 2026-08-26 补充一 ／ 形态② ＝ 2026-08-28 补充三之三），语义 ＝「**规则生效后没有任何人再动过这份件**，追改它就是违反『历史记录不追改』」。🔴 **用 git 提交时刻、不用文件 mtime**（mtime 会被 checkout／同步／台账重跑刷新）；🔴 **不用 `git blame -L`**（块的行号随上方增删漂移，会静默给出另一段的历史）。git 历史取不到（浅克隆）时 **fail-loud 单列一桶并打印文件名**，不静默把整库判成历史件。 ━━━ ⚠️ **三层缺一不可，两个实测反例**：⑴ `本周计划-2026-08-03.md` 状态头至今写 `在办`（R3 回填是季度批量做的，日期型件的状态头天然滞后）——**只有 H3 能判它为历史件**；⑵ `专线opener模板库.md`（`status: 生效`、2026-08-28 仍在改）三层都不命中 ⇒ 当前在用，而**它 §二／§三／§四 那三个 CC 模板确实缺 `set_session_title`，正是 17 次违反的源头**，必须报出来。 ━━━ ✅ **「两侧都能关掉」已实测，且证据是天然 A／B**：同源两份看护件——`看护件-2026-08-28-可开工批.md`（未改）命中 **7 处形态②**，`看护件-2026-08-28-落地后批.md`（当日已改 5 处）命中 **0 处**；单测另逐形态钉死「补齐那一句后告警消失」。🔴 **刻意不设 baseline、不设豁免清单**：`队列结构lint` 称呼判据用 baseline 冻结存量是对的（那些按历史不追改永远不该修），**本件不同——当前在用件里的命中是真该修的**，冻结它们等于把最该修的模板库三处永久隐身。 ━━━ 🔴 **未做的，如实登记**：① **未改任何 opener 正本**（本件边界＝只检测、不自动修复）⇒ **当前在用件 30 处存量待另行派单清零；`--enforce` 的切换前提就是这 30 处清零**，否则门禁上线第一天就是红的、只会被习惯性忽略；② 本件**未走 openspec 变更包**（off-LAN 无法取 design 审），是否须补待定夺；③ CI job 尚未在真实 runner 上跑过（本机已实跑，告警模式退出码 0、`--enforce` 退出码 1，两者均已验）。 ━━━ **one-in-one-out（协议〇.9 措施 B）**：本次退休的是**人守规则**（模板库 §〇 补充三「起草期自检：每写完一个 opener 回头看第 3 行」＋ 补充三之三「机制守待建」），**不退休任何既有守卫代码**——现有五个 `工具-*lint.py` 各守一个互不重叠的域，没有一个覆盖 `.md` 里的 opener 代码块，退掉任何一个都会开新口子。 ━━━ 2026-08-29 +1（Cowork 构建环境方案线 OP-0829 自报）：违反 §5 输出规范③「每题只给 1 个最优建议、不摆等价选项」——信任记录修复同时给出 PowerShell 命令与「可不做、等安装单」两条路，且附带「当时别开 CC 会话」情境判断；Shao Peishen 当场纠正原话「两个方案都可奏效只出一个、手工成本最小的那个；附带条件判断增加人为判断、留下出错机会」。同族延伸：给他的动作项也适用——单路径、零附带条件。 　━━━　➕ **违反计数 +1（2026-08-30，形态①，`OP-0830-C` 派单件首版）**：本方为 `#435` 出的 opener **第 3 行 `set_session_title` 漏写**，CC session 因此按内容自动命名为「全局记忆巡检」、编号 `OP-0830-C` 丢失，**由 Shao Peishen 当场在会话列表里看出来并指出**。已补写并给出在跑 session 的补救句。🔴 **本次的意义不在又欠一次，而在暴露 lint 的一个覆盖缺口**：`工具-opener块lint.py` 只扫 **git 已跟踪** 的 `.md`（`_tracked_md_files`），而 opener 的高危时刻恰恰是**刚写出来、还没 commit、马上就要粘出去**的那几分钟——**派出发生在跟踪之前，lint 结构性地看不到它**。⇒ 形态① 名义上已机制化，实际对「新出的 opener」仍是人守。**建议修法（未做，待派）**：⑴ lint 增 `--include-untracked` 或直接扫工作区 `.md`；⑵ 或在编辑锁 release 的 ⑹ 登记完整性检查旁挂一钩——本次触碰的 `.md` 若含 `【设置】` 而无 `set_session_title` 即拒绝 release（**这个位置最好：派出前必经，且已有 fail-closed 语义**）。 　⇒ **根治已立行 §一 `#437`**（2026-08-30，opener 守卫挂编辑锁 release 咽喉，`[S:blocked]` 等 `#435` 归档——触碰同一文件）。本条形态① 的人守状态在 `#437` 落地前不变。　━━━　✅ **`#437` 已落地（2026-08-30，CC `OP-0830-G`）——形态① 由「仅事后 lint 扫描」延伸挂到编辑锁 `release` 咽喉**：本次持锁期间触碰的 `.md` 若含 CC 环境 opener 块缺 `set_session_title`（或有 title 缺子任务例外句），`release` 直接拒绝（fail-closed，逃生阀 `opener豁免：<理由>`）。判据逐字复用 `工具-opener块lint.py::check_block`，不重写第二份。**用 2026-08-30 那次真实第 18 次违反的历史版本验证：正确拦下**（见 `#437` 行内3.1 实测）。🔴 **不销号——只是「已由咽喉守住（限已登记路径）」**：本守卫只覆盖走了队列锁 acquire/release 流程的 opener；写完直接粘出去、从不经本工具的会话它仍看不到，那条路径**仍是人守**（design D2）。详见 `#437`。 　━━━　📊 **2026-08-31 值周巡检登记 · 违反计数 +3（形态＝开工前未查现状）**：源＝移交单 `OP-0830-J` §5 自陈的同批三次 —— ⑴ R3 初稿未查 `followup_readme_bridge.py` 已存在，按手册「该有什么」写方案，L1／L2 全是重复建设；⑵ 取号未查全库已用 OP 号，撞了 `OP-0830-B`／`OP-0830-C`，已改判为 `-H`／`-I`；⑶ 未查 `#366` 当前状态、沿用 7 天前快照，把 2026-08-23 度量复盘里「12 封停在 ✅ 已推送」当成现状，而 `#366` 的清理正发生在同一天。 **⇒ 规则退休制阈值判定**：本形态（提方案/动手前未先查现状）**一次性 +3 即达『3 次』阈值**，按协议须二选一——机制化或删除。**处置＝机制化，且承接载体已建**：§一 `#440`（R4 纪律 eval 套件）一期三道题中的第三道即「提方案前未先查环境已有能力」，**这三条正是它的真实用例、非假想题**。⚠️ **但 `#440` 本轮 🛑 排队（WIP 20/22 且其 CI 凭据边界待 design 审）** ⇒ **本形态在 `#440` 落地前仍是纯人守、仍会复发**，值周巡检下次复核时若再 +1，须把 `#440` 的排队优先级上提一档。 　━━━　📊 **2026-08-31 值周巡检登记 · 违反计数 +1（形态＝开工前未查现状，本日第五次；由 Shao Peishen 当日指名计入）**：移交单 `OP-0830-J` §4 写「`#381`／`#382` 两行 🛑 已排两周」——**实测 `#381` 为 `[S:open][A:已派出]` 在办**（2026-08-24 开工），只有 `#382` 在排队且是 **7 天**不是两周。**成因＝写移交单时没跑 `--row` 就写下了状态断言**，与本日前三次（未查 `followup_readme_bridge.py` 已存在／未查全库已用 OP 号致撞号／未查 `#366` 当前状态沿用 7 天前快照）同族。 🔑 **本次这一例的形态更窄也更准：「没查现状就写下依赖」** —— 依赖别人的状态而不去读它，比一般的「没查现状」更容易被下游原样继承（本次即由值周巡检在排序判断中当场撞上）。 ⇒ **本形态累计已达 5 次，远超规则退休制 3 次阈值**；机制化承接载体＝§一 `#440`（R4 纪律 eval 套件）一期第三道题，**该行仍 🛑 排队** ⇒ **在它落地前本形态仍是纯人守、仍会复发**。
