---
title: "队列行日志 #615（§一）"
created: 2026-09-18
status: 生效
用途: 跨桌任务队列 §一 #615 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #615 · 外置段（2026-09-18 外置，工具第 1 批，5 段，原文原样）

> 外置人 CC 无头泳道 OP-0918-A；外置前整格 2842 B、共 7 段；本批外置第 2、3、4、5、6 段；本格保留第 1、7 段。

✅ **前置探针已过（Cowork 现跑，非转述）**：`ping 192.168.100.51` 通；八端口探针 `8090/8091/8092/8093/8094/8096` 全 **200**、**`8097` 无响应＝空闲**；`git rev-list --count origin/master..master` ＝ **0**（对齐）；FI3 门户页已在 master（`2f0ea72`），`pytest` 单跑 **32 passed**。

━━━

🔴 **他的裁决与代价，逐字记下**：答 `a` ＝ **走现有五服务的老形态**（独立端口 8097 ＋ 防火墙入站规则 ＋ 共享口令门禁 `#160`），原话「**现在都是内网，先上功能，认证完善以后慢慢上**」。⇒ **与 design D7「不新起端口、走 `.51:8090` 网关反代」相反，属过渡形态**，先例＝SC2 的 8096 过渡期新端口豁免。**网关收编（决策件线③ 收 8091/8093/8094）时本端口一并回收**，届时 `/finance/fi3` 的 `required_tier` 与权限映射另判——**本批不碰网关、不动 8090**。

━━━

🔑 **门禁用共享口令，不新设**：`install_flask_gate` 支持 `env_var=`，泳道昨晚填的是 `FI3_GATE_PASSWORD`；**改为共享 `ZP_GATE_PASSWORD`**，与 SC8／QD-B／FI2／SC2 同一个口令——否则成员要记第二个口令。🔴 **口令值不入库、不打印、不进日志**（标准清单 ⑦ 凭据不入库）；`.51` 上 `.env` 已有该键，**复用、不新设**。

━━━

📌 **绑定无需改码**：`run_fi3_web.py` 已读 `FI3_WEB_HOST`／`FI3_WEB_PORT`，部署时置 `FI3_WEB_HOST=0.0.0.0` 即可，**不改默认值**（本机跑仍只绑 127.0.0.1）。

━━━

⚠️ **这一版是 mock 数据，跟进信里必须写死「看形态、不看数字」**——真实数据要等唐燕萍回 `财务部#19`（FI3 tasks 3.3 账龄天数、3.5 导出字段对表）。2026-07 `:8092` 静态原型用虚构占位数据让专员脱离直觉，同一个坑不踩第二次；页面首屏已由泳道显著标注 mock。🔴 **本行不含发信**——对外发送永不代办。

---

# §一 #615 · 外置段（2026-09-19 外置，工具第 2 批，1 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-A；外置前整格 4113 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

⚠️ **本行落笔时撞过一次号，如实留痕**：`acquire --reserve` 返回的是 **#615**，而本线按上一行 `#613` **顺推硬写了 #614**——`#614` 当时已被他线（财务域定夺包生成，`看护件-【CC】财务域定夺包生成-2026-09-17.md`）写过，`append-row --number` 的高水位线推进是**单调的、但不校验该号是否已被他线占用** ⇒ 同号两行并存。持锁中已改回 **#615**，现取 `#614`／`#615` 各 1 行。🔑 **判据一句：工具已经把号给你了，就不要再自己推一个**——同族于今日另两例（聊天旁路、pwsh 不在 PATH），都是「现成的机器结论没被采用」。⇒ 是否该让 `append-row --number` 对「该号已存在」fail-loud，已并入 §一 `#611` 的同族清单待判。 ✅ **2026-09-18 已完成（CC 无头泳道 `OP-0918-A`，§二 `B-0918A_FI3上线-8097部署`；本行原第 2–6 段已 K2 外置至 `队列行日志/#615.md`）**。⑴ `deploy-server.ps1`／`sync-to-server.ps1`／`smoke-server.ps1` 三件新建（文件头 `EF BB BF`，`xxd` 取证）；⑵ 门禁 env 改共享 `ZP_GATE_PASSWORD`（`install_flask_gate` 默认键），`run_fi3_web.py` 经 `env_anchor.load_env` 读 `C:\fi3\.env`，默认仍绑 127.0.0.1、`.51` 由 `start-fi3.ps1` 置 `0.0.0.0`；口令值由 `deploy-server.ps1` 在 `.51` 本地从 `C:\fi2\.env` 借同名行，值不出机、不回显；⑶ **冒烟三件套三项全绿、证据落档场景 `CLAUDE.md` §5b**：① `/finance/fi3/api/ping` 200（`.51` 本机 smoke §1 ＋ 笔记本外部 `curl` 各一次）；② 未登录 `GET /finance/fi3/` 302→`/_gate/login`（本机＋外部各一次，门禁生效），登录后 200；③ 主流程＝mock 判定表渲染 `bytes=3565`，关键字 `<b>mock</b>`／`并非真实付款申请`／`fi3-v1-tangyanping-2026-07-10`／`<th>结果态</th>` 四项 True，前后 PID 9920 同一；`smoke-server.ps1` 回显 `SMOKE DONE failures=0`、exit 0；webapp.py sha256 `CAE140BF…41F95` 本机 `sha256sum` ＝ `.51` `Get-FileHash`。计划任务 `Fi3WebServer`＝`MSFT_TaskBootTrigger`＋`SYSTEM`＋Restart `3x/PT1M`；防火墙 `Fi3-WebServer-8097` `RemoteAddress=Any`；8090 未动（外部探 302 如旧）。回滚 SOP 已写死 TaskName（场景 `CLAUDE.md` §5b §六）。pytest 32 passed。分支 `claude/op0918a-fi3-deploy`（`ae6a2d8`→`6433d34`）已 push，**待合 master**（本泳道不 ff）。⚠️ 留步：5.3 跟进信未起草（对外发送永不代办，须写死「看形态、不看数字」）；`deploy-server.ps1` 首跑因本地 `iconv` 回退被重复执行一次，幂等无副作用（venv／防火墙／.env 均报已存在）。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **5 段已于 2026-09-18 外置**（K2 口径，原文原样、可 grep，文件 md5:c2965f1b，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#615.md`；本格只留原第 1、7 段（共 7 段）＋本指针（外置人 CC 无头泳道 OP-0918-A）。
