---
title: "队列行日志 #561（§一）"
created: 2026-09-12
status: 生效
用途: 跨桌任务队列 §一 #561 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #561 · 外置段（2026-09-12 外置，工具第 1 批，1 段，原文原样）

> 外置人 Cowork-OP-0912-E；外置前整格 4212 B、共 3 段；本批外置第 2 段；本格保留第 1、3 段。

🟩 **`OP-0912-J`（2026-09-12 18:1x，CC 无头泳道 `op0912j-openspec-17-decisions`，批 `B-0912_夜批四泳道`）：本行 ②③ 类里的三个 openspec 包已补齐 design 决策点五件形态，只起草不 apply**——`opener-batch-archive-precheck` 6 点（design.md「Decisions」节，决策点 1/2/3/6 无默认、4/5 默认 (a)）、`tasks-zombie-item-detect` 5 点（「决策点」节，决策点 4 无默认、1/2/3/5 默认 (a)）、`followup-closure-form-survives-backfill` 6 点（「决策点」节，六项全部无默认，决策点 2 新增推荐 (a)＋三条护栏）；实际条数＝计划条数 6/5/6，无凑数、无缩水。**19 条现取更正**随附各包节首（要点：`#395`/`#396`/§四 `#91` 已清扫入归档 ⇒ 夹具与「紧跟 #396」「降级 #91 正文」三处失效；`工具-队列查询.py` 未找到已 exit=1；J-C 引用有 `1.5/1.6`／`2.3、0.3` 两种多值分隔、解析不支持则上线即红 2 条；README「主要事项」列 600 B 判据、`质量部#7` 闭环散文已迁行日志；`#399` 补件表已放行「回填直写 `✅ 无需回复`」构成决策点 2 先例）。手段＝`openspec validate --strict` 三包 valid ＋ 五件机器核脚本 17/17（问号标题／(a)(b)／代价≥2／推荐／默认或无默认／手段／关系）。落点＝分支 `claude/op0912j-openspec-17-decisions` commit `2b691d3`（已 push、**未 ff master**，§二 `B-0912_openspec三包17决策点` 待合）。🔴 **17 点全部待 Shao Peishen 审，本泳道停在 design 审、不 apply**；本行状态不改、仍 🟢 可领（超期包分诊本身未动）。

---

# §一 #561 · 外置段（2026-09-12 外置，工具第 2 批，1 段，原文原样）

> 外置人 CC-OP-0912-AB；外置前整格 4425 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

🟩 **`OP-0912-J` 补齐的三包 17 决策点，2026-09-12 当日全部签认完毕（Cowork 环境总线 `OP-0912-E` 分三批端，Shao Peishen 逐批回字母）**：P1 `opener-batch-archive-precheck` 6 点（回 `4:P1a,P2a,P3a,P6ab另立`，其中决策点 6 的 (b) 已另立本队列 §一 `#572`）／P2 `tasks-zombie-item-detect` 5 点（回 `P2：4a`，唯一无默认点取 (a)，Open Question 2 未答按「不预设」处理）／P3 `followup-closure-form-survives-backfill` 6 点（回 `P3：1a，2a，3a，4a，5c，6a`，**六点全无默认、六点全签**，含 IATF 显式签认红线决策点 2 的三条护栏）。签认段已逐包写进各自 `design.md`，`openspec validate --strict` 三包均 valid。🔴 **三包均只签认、未 apply**——apply 各由后续 CC 泳道领，须逐条对照签认段；本行 ①②③ 的超期包三分表本身仍未做，状态不变、仍 🟢 可领。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-12 外置**（K2 口径，原文原样、可 grep，文件 md5:75c78b66，工具第 1 批）见 `1-转型规划/0-全景路线图/队列行日志/#561.md`；本格只留原第 1、3 段（共 3 段）＋本指针（外置人 Cowork-OP-0912-E）。

---

# §一 #561 · 外置段（2026-09-12 外置，工具第 3 批，1 段，原文原样）

> 外置人 CC-OP-0912-Z；外置前整格 5115 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

🟩 **P3 `followup-closure-form-survives-backfill` apply 已完成（2026-09-12 23:0x 本地，CC 泳道 `OP-0912-AB`，分支 `claude/op0912ab-p3-closure-form-apply` `7a382aa` 已 push、未 ff）**：tasks **9/36 → 33/36**；未勾三项——**4.4** 编辑锁两态语义一项结论不一致（整格等值判，七处等值比较之一、派单件明令不改；带快照段的「🆕 待发＋快照段」新增行不被拦，风险＝零：门禁②同为等值断言发不出去；用例 `test_新增行终态加快照段_两态语义等值拦截不命中_如实钉住` 钉住现状、spec 已如实更正，闭合须 Shao Peishen 另签放宽）；**7.2／7.3** 首次真实生效／真实开闸须真信真起草，无头留步（fake connector 端到端已通、不算数）⇒ 变更包**暂不归档**。落点：`followup_gate` §八／`readme_table.build_closure_form_annotation`＋`extract_closure_form`／`delivery.resolve_backfill`（保留式＋快照；`✅ 无需回复` ⇒ 首段闭环态、闸当场开；三护栏全在；补件表分支一字未动）／`工具-跟进闸查询.py`（同行输出＋「以快照为准」）。单测 +23+23+5+3。`质量部#7` 真身回改＋README 规范增补＋行日志追加登 §二 `B-0912_P3闭环形态apply`（详数在该批次行）。差集回归：aibot 885p/2f、platform 642p、tools 732p/2f，4 条失败全在纯 master `4fb9eab` 同命令复现 ⇒ 零回归。档 3 弱一档如实登记（永不部署 `.51`，无生产冒烟）。本行 ①②③ 三分表仍未做、状态不变。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-12 外置**（K2 口径，原文原样、可 grep，文件 md5:36f31b50，工具第 2 批）见 `1-转型规划/0-全景路线图/队列行日志/#561.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 CC-OP-0912-AB）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。


---

# §一 #561 · OP-0912-AA 追段（2026-09-12 23:1x 本地，P2 `tasks-zombie-item-detect` apply 收工报告，只追加）

> 泳道 `op0912aa-p2-zombie-lint-apply`，分支 `claude/op0912aa-p2-zombie-lint-apply` `d378206`（已 push，待 ff，本泳道不 ff）。派单件 `派单件-【CC】P2僵尸未勾项apply-2026-09-12.md`。

**1 · 产出**：`0-学习与工具/工具-僵尸未勾项lint.py`（纯只读）＋ `0-学习与工具/test_工具-僵尸未勾项lint.py`（29 例，`pytest -q` 29 passed）＋ `.github/workflows/ci.yml` 新 job `zombie-task-lint`（`--enforce`、无 `continue-on-error`、`fetch-depth: 0`）＋ `0-学习与工具/门禁判据族清单.json` 登记四族 ＋ `进度编年-CHANGELOG.md` 附录 L（`#91` 退休声明）＋ 本包 proposal 状态行／tasks 回写。`git check-ignore -v` 两个新文件退出码 1（均未被忽略、会入库）；本包零生成物、无需补 `.gitignore`。

**2 · 现取实测（79 个活跃包，非 design 表的 14 个；design 写 75 是 `grep -v archive` 把 `audit-retention-archive`／`opener-batch-archive-precheck` 两个活跃包也滤掉了）**，与 design 14 包旧值并列、不覆盖：

| 判据 | design 14 包（2026-08-22） | 本次 79 包（2026-09-12） |
|---|---|---|
| 前置声明（「前置 N.M」引用处） | 3（现取更正 2：11） | **11**；「前置登记」项 **6** |
| J-A 前置矛盾 | 0（勾除前 4） | **首跑 3 → 处置后 0** |
| J-B 节内乱序 | 7 | **47**（本包 tasks 回写后 48：2.8 留 `[ ]` 而 2.9 `[x]`，正是「请复核」该有的样子） |
| J-C 前置登记无引用 | 0 | **0** |
| J-D 未勾项 | — | 76 包 1,058 项 |

🔴 **J-A 首跑 3、非 0——按 tasks 2.5 停手查因**：三条全部是 `status-triage-resident-round: 7.1／7.2／7.3 → 6.3`，来源 §7 标题 `## 7. 收口（前置 6.3）`。6.3＝「企微推送首次真实触发留痕（合入 master 后第一轮 sweep）」，7.1（CI 护栏推演）／7.2（知识资产台账）／7.3（队列回写）已按实做完且不依赖它；只有 7.4 归档依赖 6.3，7.4 正文自己写着「待 6.3 留痕后再归档」。⇒ 是该包 09-06 写入、晚于 08-22 基线的**真命中、非解析错**（解析行为与 spec Scenario「节标题声明的前置被识别」逐字一致）。**处置：未改判据**，把该前置声明从 §7 标题下移到 7.4 正文（`## 7. 收口` ／ `- [ ] 7.4 （前置 6.3）…`，行末附下移说明），**不动任何勾选**；`openspec validate --strict status-triage-resident-round` valid。这是「先清零、再关门」的存量清零动作，同 `claude-progress-lint` 二期；若不认可，`git revert` 该文件即可，代价＝CI `zombie-task-lint` 首跑红 3 条。

**3 · tasks 20/24**：留 `[ ]` 四项——0.2（Open Question 2 J-B 陈化门槛，他未答＝不预设，未答≠已答）；0.3（`#91` 退休是否成立，他未明答；声明已落 proposal＋CHANGELOG 附录 L，不替他答）；2.8（`#91` 正文降级——目标行已在归档件，「历史记录不追改」）；2.10（不 archive）。

**4 · 三种「前置」分隔形态夹具原文输出**（`python -m pytest -v -k TestPrereqRefParsing`）：`test_slash_fi2_form PASSED`（`前置 1.5/1.6`→`['1.5','1.6']`）／`test_slash_status_triage_form PASSED`（`前置 0.1/0.4`）／`test_dunhao_form PASSED`（`前置 2.3、0.3`）／`test_comma_forms_spec_listed PASSED`（`,`／`，`）／`test_three_values_dunhao PASSED`／`test_single_with_trailing_condition PASSED`／`test_two_refs_same_line_count_two PASSED`／`test_multivalue_prevents_false_jc PASSED`——8 passed。

**5 · 差集回归**（`cd 0-学习与工具 && python -m pytest -q --tb=line -p no:cacheprovider .`）：分支侧 **33 failed / 2462 passed / 1 skipped（1860 s）**，失败分布＝`test_工具-落库sweep.py` 15／`test_工具-取证件回显lint.py` 7／`test_工具-无头棒收工探针.py` 4／`test_工具-CLAUDE进度段lint.py` 2／`test_工具-共享文档编辑锁.py` 2／`test_发企微.py` 1／`test_工具-引导样板lint.py` 1／`test_工具-规划倒逼扫描器.py` 1；其中 `取证件回显lint` 7 条的原文＝「opener块lint：docstring 里出现了未登记的判据族 ['形态⑩']——工具长出新族而本清单没跟上（漂移守命中）」（`工具-取证件回显lint.py --enforce` 退出码 2，纯 master 态同样复现），**与本泳道新登记的 `僵尸未勾项lint` 条目无关**。纯 master `4fb9eab` 同命令对照（detached worktree `op0912aa-master-baseline`）**登记时仍在跑，结论追记于本节末尾**。

**5 · 追记（2026-09-12 23:5x 本地）· 纯 master 对照跑完，零回归成立**：纯 master `4fb9eab`（detached worktree `op0912aa-master-baseline`）同命令 **33 failed / 2433 passed / 1 skipped（1637 s）**；`comm -23 分支失败集 master失败集` 为空、`comm -13` 亦为空 ⇒ 两侧失败集**逐条相同**（33＝33），分支侧多出的 29 passed 即本包新单测。纯 master 侧失败原文（33 条 test id）：`test_发企微.py::WecomRepoRootCrossWorktreeTests::test_repo_root_falls_back_when_not_a_git_repo`；`test_工具-CLAUDE进度段lint.py::RealRepoSmokeTests::test_当前master三条哨兵全部命中`；`test_工具-CLAUDE进度段lint.py::RealRepoSmokeTests::test_真实根CLAUDE可被解析为结构A`；`test_工具-共享文档编辑锁.py::ReleaseStructuralValidationTests::test_row_length_over_cap_warns_before_cutoff_does_not_block`；`test_工具-共享文档编辑锁.py::ReleaseStructuralValidationTests::test_row_length_section_four_topic_column_checked`；`test_工具-取证件回显lint.py::清单自洽::test_真实清单自洽`；`test_工具-取证件回显lint.py::退出码::test_enforce无违规退0`；`test_工具-取证件回显lint.py::退出码::test_enforce有违规退1`；`test_工具-取证件回显lint.py::退出码::test_告警模式有违规也退0`；`test_工具-取证件回显lint.py::真实仓库现状::test_emit_baseline输出可解析且不写盘`；`test_工具-取证件回显lint.py::真实仓库现状::test_json输出可解析`；`test_工具-取证件回显lint.py::真实仓库现状::test_全库实跑为绿`；`test_工具-引导样板lint.py::真实仓库现状::test_env锚定存量已清零`；`test_工具-无头棒收工探针.py::test_主流程默认落款为手工`；`test_工具-无头棒收工探针.py::test_主流程带旗标时落款为定时任务`；`test_工具-无头棒收工探针.py::test_no_notify必须喊出自己吃掉了信号`；`test_工具-无头棒收工探针.py::test_主流程写状态且不推送`；`test_工具-落库sweep.py::ExactMatchEndToEndTests::test_correctly_declared_file_not_flagged_ambiguous_by_duplicate_basename`；`test_工具-落库sweep.py::ResidentServiceDeploymentHintTests::test_batch_not_touching_resident_service_path_gets_no_hint`；`test_工具-落库sweep.py::ResidentServiceDeploymentHintTests::test_batch_touching_resident_service_path_gets_hint`；`test_工具-落库sweep.py::ResidentServiceDeploymentHintTests::test_docs_only_batch_in_resident_service_dir_gets_no_hint`；`test_工具-落库sweep.py::OrphanFileAlertTests::test_alert_not_repeated_within_threshold_window`；`test_工具-落库sweep.py::OrphanFileAlertTests::test_alert_repeats_after_another_threshold_window`；`test_工具-落库sweep.py::OrphanFileAlertTests::test_newly_seen_orphan_is_tracked_but_not_alerted`；`test_工具-落库sweep.py::OrphanFileAlertTests::test_orphan_past_threshold_triggers_alert`；`test_工具-落库sweep.py::DeploymentTraceHintTests::test_deployed_scenario_touched_with_same_batch_trace_stays_silent`；`test_工具-落库sweep.py::DeploymentTraceHintTests::test_deployed_scenario_touched_without_trace_gets_hint`；`test_工具-落库sweep.py::DeploymentTraceHintTests::test_non_deployed_scenario_path_gets_no_hint`；`test_工具-落库sweep.py::OrphanResolvedNotificationTests::test_never_alerted_orphan_resolved_sends_no_notice`；`test_工具-落库sweep.py::OrphanResolvedNotificationTests::test_previously_alerted_orphan_resolved_sends_notice`；`test_工具-落库sweep.py::ScenarioSpecGapAnnounceIntegrationTests::test_scenario_gap_triggers_alert_once_then_throttled`；`test_工具-落库sweep.py::StaleChangeAnnounceIntegrationTests::test_high_completion_stale_change_triggers_alert_once`；`test_工具-规划倒逼扫描器.py::SweepClass13::test_类号为13且不与既有类争号`。


---

# §一 #561 · OP-0913-C 追段（2026-09-13 04:0x 本地，P3 tasks 4.4 收尾——编辑锁两处等值比较放宽，只追加）

> 泳道 `op0913c-p3-editlock-compat-44`，分支 `claude/op0913c-p3-editlock-compat-44` `535ee82`（已 push，待 🟡 ff，本泳道不 ff）。派单件 `派单件-【CC】P3编辑锁快照兼容-2026-09-13.md`；授权＝Shao Peishen 2026-09-13 回「第 5 项选 a」＝签认放宽编辑锁两处等值比较。

**1 · 产出**：`followup_gate` §八新增 `leading_status_segment`（按 `━━━` 核心切首段再 `normalize_status`，切分口径只此一份、与写侧 `STATUS_SEGMENT_SEPARATOR` 同源）；`工具-共享文档编辑锁.py` 两态语义＋⑥ 暂缓一致性两处由整格等值改为 `gate.leading_status_segment(...)` 等值，`_FollowupGateFallback` 逐条镜像；单测编辑锁 +4（`test_4_4_*`，其中双跑类各跑权威＋回落）、platform `TestLeadingStatusSegment` +4；spec 场景「快照不影响两态语义拦截」改回成立并留历史；`tasks.md` **33/36 → 34/36**（余 7.2／7.3 须真信，无头留步，暂不归档）；`openspec validate followup-closure-form-survives-backfill --strict` → valid。

**2 · 七处等值比较逐处现取（分支 `535ee82`，`grep -rn "FINALIZED_STATUS_MARKER\|FOLLOWUP_FINALIZED_STATUS\|PAUSED_STATUS\|DRAFT_PENDING_REVIEW_STATUS" --include=*.py 5-平台底座 0-学习与工具 | grep -v tests | grep "==\|!="`）**：
① `工具-共享文档编辑锁.py:4006`（两态语义）——**本棒改了**（签认范围内）；
② `工具-共享文档编辑锁.py:4261`（⑥ 暂缓一致性）——**本棒改了**（签认范围内）；
③ `gates.py:16 assert_finalized`——**没改**：design D8 明文「不得放宽」的门禁②红线，不在本次签认范围；
④ `readme_table.py:75 assert_draft_pending_review`——**没改**：批准脚本入口，草稿态从不带快照段，放宽无对象且不在签认范围；
⑤ `dispatch.py:72`（候选筛选）／⑥ `dispatch.py:240`（`PAUSED_STATUS`）／⑦ `dispatch.py:250`——**没改**：与门禁②同一条发送链，等值是「唯一可发送标记」的结构性保证，放宽即变相放宽门禁②；
⑧ `scripts/dispatch_followup_letters.py:75`——**没改**：同 ⑤。
（另 `delivery.py:153` 为 P3 新增的 `normalize_status(previous) != FINALIZED_STATUS_MARKER` 保留式分支，本就非整格等值、不在七处之列，未动。）`approval.py` 整格覆盖语义未动。

**3 · 4.4 兼容性单测原文输出**（`python -m pytest -q --tb=short -p no:cacheprovider 0-学习与工具/test_工具-共享文档编辑锁.py -k 4_4` → `7 passed, 566 deselected in 2.51s`）：`FollowupReadmeStructuralValidationTests::test_4_4_新增行终态加快照段_两态语义按首段判_与不带快照结论一致`（bare 与 snapshot 两轮 release 均非 0、输出均含「违反两态语义」）／`test_4_4_既有草稿行转终态加快照段_与不带快照同样放行`（两形态 release 均 0）／`HoldConsistencyValidationTests::test_4_4_hold_row_readme_pending_with_snapshot_segment_blocks_release`（两形态均拒、输出含「仍为「🆕 待发」」）／`FollowupSerialGateIdentityTests`＋`…FallbackTests::test_4_4_两态语义首段判_带快照与不带快照结论一致_权威与回落双跑`／`test_4_4_leading_status_segment_回落桩与权威实现同口径`。**反证**：把 `HEAD~1` 的编辑锁临时换回再跑同命令 → `5 failed, 2 passed`（失败的正是两处放宽用例＋回落镜像用例），证明用例确在测本改动而非恒真。

**4 · 差集回归**（同命令分别在分支与纯 master `ec8965c` detached worktree `op0913c-master-baseline` 跑，`python -m pytest -q --tb=line -p no:cacheprovider <目标>`）：编辑锁＋闸查询 分支 **2 failed / 596 passed / 1 skipped** vs master **2 failed / 590 passed / 1 skipped**（`test_row_length_over_cap_warns_before_cutoff_does_not_block`／`test_row_length_section_four_topic_column_checked` 两侧同）；platform 分支 **646 passed** vs master **642 passed**（0 失败）；aibot 分支 **2 failed / 885 passed** vs master **2 failed / 885 passed**（`test_ps1_orphan_cr_guard.py` 两参数化用例两侧同）。`comm -23 分支失败集 master失败集` 与 `comm -13` 均为空、交集 4 ⇒ **分支失败集 ＝ master 失败集，零回归**。

**5 · 留步与边界**：不 ff master、不碰 `.51`、不发信、不改 README 真身；7.2／7.3 仍留步（须真信）；本泳道零新文件名形态、零生成物入库（`git check-ignore -v reports/…` 命中 `.gitignore:50 **/reports/`）；§二 `B-0913_P3编辑锁快照兼容`。

---

# §一 #561 · 外置段（2026-09-13 外置，工具第 4 批，2 段，原文原样）

> 外置人 CC-OP-0913-C；外置前整格 4867 B、共 5 段；本批外置第 3、4 段；本格保留第 1、5 段。

🟩 **P1 `opener-batch-archive-precheck` apply 已完成（CC 泳道 `OP-0912-Z`，2026-09-12 23:2x，分支 `claude/op0912z-p1-archive-precheck-apply` @`36db5bf` 已 push，待 🟡 ff）**：① `工具-队列查询.py` 加 `--include-archive`／`--format json`（默认行为逐字不变：分支 vs 纯 master 脚本 10 组命令 md5 10/10 同字节）；② 新建 `工具-opener派出前校验.ps1`（标题抽 §一 行号、多行号合取、只读 JSON `done`、校验自身失败 fail-open）；③ v1 v1.2／v2 v2.3 解析后、派出/分组前挂点，SKIPPED 在哨兵判定前短路、整泳道跳空不 Start-Job、进 summary、退出码不受影响；④ `test_工具-opener派出前校验.py` 36 例全绿（含 C 态防回归：`#368` `status_field=open` 且 `done=true`）；⑤ 回放：`本周计划-2026-08-24.md` **A4 被跳过**（`#368 archived@归档-202608.md:583`），另 5 项因所引行已迁归档同跳、9 项照派；08-25 泳道计划 v1/v2 一致（仅 A25 跳、A27 合取照派）；真看护件 `-DryRun` 3 泳道正常；⑥ 零回归：7 份相关测试 `2 failed／917 passed／1 skipped`，2 条 `test_工具-共享文档编辑锁.py::…test_row_length_*` 在纯 master `4fb9eab` 同命令复现 ⇒ 分支失败集 ⊆ master 失败集；`openspec validate --strict` valid。tasks 44/46：**7.3 `/opsx:archive` 未做**（🟡 关行类，留待 ff 后）、8.x 范围外（8.2＝`#572`）。§二 `B-0912_P1归档前置校验apply`。本行 ①②③ 三分表仍未做、状态不变。

━━━

🟩 **P2 apply 完成（`OP-0912-AA`，2026-09-12 23:1x 本地）**：`工具-僵尸未勾项lint.py`＋29 单测＋CI `zombie-task-lint`（`--enforce`）＋`门禁判据族清单` 四族，在分支 `claude/op0912aa-p2-zombie-lint-apply` `d378206` 已 push 待 ff；79 包实跑 J-A 3→0／J-C 0／J-B 47／前置声明 11；tasks 20/24（0.2／0.3 他未答、2.8 归档行不改、不 archive）；全文＋差集回归见 `队列行日志/#561.md` §OP-0912-AA。§二 `B-0912_P2僵尸未勾项apply`。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **1 段已于 2026-09-12 外置**（K2 口径，原文原样、可 grep，文件 md5:702885f1，工具第 3 批）见 `1-转型规划/0-全景路线图/队列行日志/#561.md`；本格只留原第 1、4 段（共 4 段）＋本指针（外置人 CC-OP-0912-Z）。此前 1 条指针段已并入本指针，先后各批原文均在同一文件。
