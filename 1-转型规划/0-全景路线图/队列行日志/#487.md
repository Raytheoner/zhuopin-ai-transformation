---
title: "队列行日志 #487（§一）"
created: 2026-09-06
status: 生效
用途: 跨桌任务队列 §一 #487 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋指针＋末段。
---

---

# §一 #487 · 外置前整格原文（2026-09-06 `OP-0906-G` 看护者收工外置，11727 字节，md5:`18261ef8`）

> 外置动机：外置前状态格 **11727 字节**，超 K2 上限（4096 B）。本批 `B-0906_G` 多条泳道向本行追加了取证与裁决，不外置将持续膨胀。**原文一字未改**，行内改留「首段＋本指针＋精简末段」。
> 同批先例：`#454`（`OP-0906-N`，9.4 KB → 3.7 KB）、`#480`（`OP-0906-O`，7.8 KB → 3.5 KB）。

[S:open][D:机] 🆕 **2026-09-05 立行（Cowork 业务总线 OP-0905-C，Shao Peishen 现场实测发现）。** **现象**：B-0905_B 泳道 `456-lint-impl`（`OP-0905-J`）被看护者用 Task/Agent 起为子任务后，其 opener 正文里的 `set_session_title("self")` 那一行**连同紧跟的子任务例外句一起，被子 agent 忽略执行了**——`"self"` 解析到父 session（看护者 `OP-0905-C`），把看护者会话标题改成了 `[Win]0905J-lint扩包进度告警`。 ━━━ 🔑 **这不是 `#437` 的回归，是 `#437` 从未覆盖的另一层**：`#437` 守卫（release 前置钩＋`工具-opener块lint.py`）检查的是**文本格式**——本行涉及的 opener 块本身两次过 lint 零违规（含例外句，格式完全合规）；今天暴露的是**格式合规 ≠ 子 agent 语义上真的遵守**，例外句写没写、写对没写对，跟子 agent 会不会执行它是两件事，前者可机守、后者目前无法机守。 ━━━ **影响面（如实评估，不夸大）**：**只影响会话标题显示，不影响子任务本身的执行内容**——`456-lint-impl` 该做的建造/单测/pause 逻辑不受 titling 影响；代价是看护者会话标题被顶替，人工肉眼从任务列表里找哪个是看护者变难，且若七条泳道陆续都触发同一 bug，标题会被反复覆盖。 ━━━ **候选修法（本行先判据，不预设哪个对）**：`(甲)` §三 lane opener 正文（会被 Task/Agent 当子任务 prompt 使用的那些）**干脆不含** `set_session_title` 指令——只有 §三bis 看护者自己的开场词才需要它，因为只有看护者是真正被粘贴进独立 CC 会话的；子任务从未被单独粘贴过，天生不需要也不该有这一步。`(乙)` 保留现状、只登记不改——赌以后遇到率低。`(丙)` 换一种机制识别「我是否是子任务」（如检查某个环境变量/工具是否存在），让 agent 能真判断而非靠读文本——技术可行性未核，成本可能远高于 (甲)。 ⚠️ **(甲) 直觉上最省事且最贴合 `#455` 元判据**（能拿掉指派就别指望人/agent 记得例外）——但会改 `opener骨架.md` 这个唯一格式来源，须走一次判据确认，本行不擅自改。  ━━━ ⚠️ **预留豁免：本行编号未走 `--reserve` 预留，属操作者用法错误，如实留痕**——编号取自本次 acquire 返回的高水位线 §一 #486 + 1，内容完整、经查无撞号（同 `#446` 先例）。  ━━━ ➕ **2026-09-05 已实现（Cowork 业务总线 OP-0905-C，Shao Peishen 现场拍板 (甲)）**：opener骨架.md 新增【CC · 子任务泳道】变体＋§三bis 看护者开场词范式（判据锚点＝文件含 `## 三bis` 小节）；`工具-opener块lint.py` 新增形态⑥（子任务泳道含 set_session_title 即报）；`工具-共享文档编辑锁.py::_opener_guard_violations` 同步判据（逐字复用，未重写第二份）。单测：`test_工具-opener块lint.py` +7（46/46 全绿）、`test_工具-共享文档编辑锁.py` +2（OpenerGuardReleaseTests 15/15、ReleaseStructuralValidationTests 66/66 全绿；全文件 342 测因单次 git 提交耗时限制未跑全，已跑的两个最相关类零回归）。B-0905_B 看护件本身已同步修正（7 条泳道正文移除该行，看护者自己的一行保留并核实）。⚠️ **代码尚未 commit**——本次持锁期间仓库因 B-0905_B 七泳道并行跑 git 操作、`.git/index.lock` 反复被占，多次 `git commit` 均未落地（已 `git add` 暂存，未丢失），待仓库空闲后补提交。  ━━━ 登记豁免：本次 release 时仓库正被 B-0905_B 七泳道并行 git 操作占用，`git status` 多次超时取不到脏文件状态，非静默绕过——已如实记录、未擅改判据。opener豁免：同一原因，opener 守卫同样因 git 超时无法执行，本次内容（B-0905_B 七条泳道 opener 与 §三bis 看护者 opener）均已用 --file 单文件自检模式跑过、零违规，非未检查。 ━━━ 【2026-09-06 OP-0906-G 看护批 B-0906_G，A2 泳道 487-dont-guard 起草完成 ＋ Shao Peishen 当场裁决】起草件在分支 `claude/op0906i-dont-guard-487`（commit `3c369a1`，已 push origin），落 `1-转型规划/0-全景路线图/起草件-队列487子项-dont静默丢弃/`：两版方案对比件 ＋ `方案甲-骨架补不做什么段.patch` ＋ `方案乙-生成器fail-loud.patch`（看护者独立复跑 `git apply --check` 两版均 OK；patch 内测试函数数实测 甲=12／乙=9，与自述一致；`git diff master <分支> -- opener骨架.md 工具-opener生成.py 工具-opener块lint.py` 输出为空，确认两版都未落地）。**A2 实测追加三条派单件未记的发现**：⑴ `--variant guardian` 是同族第二处静默丢弃（`body_lines` 既不拼 `--do` 也不拼 `--dont`），方案甲堵不了它；⑵ 方案甲实为三件事而非两件——`工具-opener生成.py` 的 Cowork 分支根本没拼 `dont_block`，只改骨架＋lint 则 `--dont` 仍被丢弃且多一条会误报自身产物的门禁（patch 已含第三件）；⑶ `工具-共享文档编辑锁.py` 无需改但会自动变严（release 守卫逐字复用 `check_block` 且不分历史件，是方案甲唯一真实阻断面，会拦 1 份历史件）。🔑 **Shao Peishen 2026-09-06 于 OP-0906-G 看护会话当场裁决：(c) 甲＋精简版乙——Cowork 走补「不做什么」段、guardian 走 fail-loud，两个洞全堵；guardian 洞按 (a) 并入本子项、不另开队列行**（原话「有建议项都按建议执行」）。已 `工具-泳道看护状态机.py resume --lane 487-dont-guard` 落库解除暂停。**待落地泳道 OP-0906-M 做什么**：① 以 `3c369a1` 的 `方案甲-骨架补不做什么段.patch` 为基底落地甲（改 `1-转型规划/0-全景路线图/opener骨架.md`【Cowork】骨架节补「不做什么」段 ＋ 同步 `0-学习与工具/工具-opener块lint.py` 形态判据 ＋ 补 `0-学习与工具/工具-opener生成.py` Cowork 分支拼 `dont_block` 这第三件）；② 从 `方案乙-生成器fail-loud.patch` 中只取 guardian 那一段做精简版乙——`--variant guardian` 传了 `--do`／`--dont` 即报错退出（fail-loud），**不要**把乙对 `--env Cowork` 的拦截一并落地（甲已让它生效，两者同落会互相打架）；③ 判据 F7 已知两坑必须保留其单测：`"做什么："` 是 `"不做什么："` 的子串（裸 `in` 会把已补段的块误判成缺段）、要求行尾无正文会误伤把「做什么：建造到底。」写成整行的散文式块；④ 合跑 `工具-opener块lint.py`／`工具-opener生成.py`／`工具-共享文档编辑锁.py` 三套单测；⑤ 量化回写：全库 lint 命中数与「当前在用件」命中数各一个前后对比（甲单独实测 688→689、在用件 57→57 不变）。**不做什么**：不改【CC】骨架节（已有「不做什么」段）；不重写 `check_block` 第二份判据；不碰他线在办行。**收工**：回写本行 ＋ 登记 §二 待 commit 批次。[S:open] ━━━ 【2026-09-06 OP-0906-M 落地泳道（CC，分支 `claude/op0906m-dont-guard-land-487`，commit `a28bf6f`，已 push origin，**尚未 ff 合入 master**）】**(c) 甲＋精简版乙已建造完成**，以 `3c369a1` 的两份 patch 为基底。⑴ **甲三件全落**：`opener骨架.md`【Cowork】骨架节补「不做什么」段（＋成因说明；「三处最常丢的结构」表 →「四处」并新增判据行）；`工具-opener生成.py` Cowork 分支 `body_lines` 拼 `dont_block`（🔴 派单件原文未列的第三件，缺它 `--dont` 仍被丢弃、且多一条会误报自身产物的门禁）；`工具-opener块lint.py` 形态⑦（`RULE_EFFECTIVE_FORM7=2026-09-06`／`FORM_TITLE`／主报告明细段／历史件分项计数，四处同步）。⑵ **精简版乙只取 guardian**：新增 `BODY_PARAM_SUPPORT` 白名单 ＋ `_reject_silently_dropped_body_params()`，挂在 `_validate_spec` 之后、`_check_op_id_not_reused` 之前；🔴 **刻意未落地乙对 `--env Cowork` 的拦截**——`("Cowork","standard")` 登记为两个都支持，守卫落地后只剩 `("CC","guardian")` 一个组合在守，与甲不打架（单测 `test_cowork_传dont_不再被守卫拦` 把这条交付约束钉死）。⑶ **F7 两坑各配单测**：子串陷阱（`做什么：` 是 `不做什么：` 的子串 ⇒ 行锚）＝ `test_不做什么这一行不得被误判成做什么段`；散文陷阱（整行写「做什么：建造到底。」的裸块 ⇒ 要求行尾无正文）＝ `test_无分段结构的裸块不受约束`。⑷ **三套单测合跑**：`test_工具-opener块lint.py` ＋ `test_工具-opener生成.py` ＝ **104 passed**（基线 84；+8 形态七／+4 CoworkDontItems／+8 SilentlyDroppedBodyParam）；`test_工具-共享文档编辑锁.py` **371 passed** 零回归（release 守卫逐字复用 `check_block`、自动跟着变严，未写第二份判据）。⑸ **量化回写（前后各实跑一次全库 `python 0-学习与工具/工具-opener块lint.py`，非估算）**：全库命中 **688 → 689（+1）**；**当前在用件 57 → 57（0，零阻断）**；历史件 631 → 632。唯一新增命中＝`开场prompt-构建环境瘦身第二轮A2A3-环境总线交接-2026-09-04.md:13`，`git log -1` 取末次提交 2026-09-04 < 生效日 2026-09-06 ⇒ H3 判历史件、按「历史记录不追改」不修；🔴 **但 release 守卫不分历史件**，将来谁编辑该件会被拦一次，出路＝补一行「不做什么：- …」或按 `OPENER_EXEMPT_MARK` 豁免声明（影响面全库 1 份，与起草件 §一.3 预估一致）。⑹ **端到端实测**：`--variant guardian --dont "…"` ⇒ exit 1、报错点名 `--dont` 并给出路「改看护件」；`--env Cowork` 传 2026-09-06 实撞的原三条约束（不动销售域／不催姚祖怡／不代改其余四张接力卡）⇒ exit 0 且三条真的进成品；`工具-opener块lint.py --file opener骨架.md` 自检，Cowork 块（:79）不再命中 F7（「两侧都能关掉」验收条款）。⑺ **未做（派单件明列）**：未改【CC】骨架节、未重写 `check_block` 第二份判据、未碰他线在办行。⑻ **§二 批次 `B-0906_M_OP0906M_dont闸落地回写` 已登记（待落库）**；🔴 **该批次清单刻意只列队列文件、不列 5 件代码**——代码在 worktree 分支上、主仓 git 状态里没有它们，写进清单会让 sweep 套着代码标题做一次空壳提交（#136 实测事故形态）；**代码入 master 走 ff 合入该分支，属 🟡 档、须 Shao Peishen 一个字母**。⚠️ **本行仍 `[S:open]`**：第三个子项「取号查重盲区（取了号但没落进任何载体＝没占）」的修法候选 ⑴ 取了就算占／⑵ 未落盘就不算占，尚未择一，不在本泳道范围内。⚠️ **本行状态格 7.3 KB 已超 4 KB 外置线（K2）**，本次按「回写只追加末段、不复述历史」处理、未擅自外置——外置须整格重写，属另一件事，留待下次触碰本行时一并做。 2026-09-06 10:31 UTC 回填：已合入 master，commit `241e178`（`git merge --no-ff claude/op0906m-dont-guard-land-487`）；`git merge-base --is-ancestor 241e178 HEAD` 实测 exit=0 确认。合并动作由另一活跃会话/sweep 完成（非本线执行），本线只做核验与回填。「代码入master走ff合入…属🟡档、须Shao Peishen一个字母」一句已了结。本行第三子项（取号查重盲区，⑴取了就算占／⑵未落盘就不算占）仍未择一，本行仍不销。


## 2026-09-08 K2 外置（CC 看护者 `OP-0908-T`，批 `B-0908_四泳道机制根治`）

外置原因：本批 openspec 起草结论与 Shao Peishen 答 1(a) 并入后该行状态格达 7427B、超 `ROW_LENGTH_CAP_BYTES` 4096 B。以下为原状态格中间段，原文原样；md5:`83d0b514369efbfca07fefcc1496fd23`。

📦 **历史全文已按 K2 口径外置**（原文一字未改）：`1-转型规划/0-全景路线图/队列行日志/#487.md` —— 含立行段、三个子项原文、`OP-0906-I` 两版起草段与总线回写段（外置前 11727 字节，md5:`18261ef8`）。 ━━━ 【2026-09-06 `OP-0906-G` 看护批 B-0906_G：子项一「【Cowork】骨架缺『不做什么』段、`--dont` 被静默丢弃」已闭合】**起草**（`OP-0906-I`，分支 `claude/op0906i-dont-guard-487`，commit `3c369a1`）出两版可直接照做的方案＋各自自检；看护者复核：两版 `git apply --check` 均 OK、patch 内测试函数 甲 12／乙 9 与自述逐数一致、`git diff master <分支> -- opener骨架.md 工具-opener生成.py 工具-opener块lint.py` 输出为空（确认起草期两版都未落地）。**起草期三条派单件未记的实测发现**：⑴ `--variant guardian` 是同族第二处静默丢弃（`body_lines` 既不拼 `--do` 也不拼 `--dont`），方案甲堵不了它；⑵ 方案甲实为三件事而非两件——`工具-opener生成.py` 的 Cowork 分支根本没拼 `dont_block`，只改骨架＋lint 则 `--dont` 仍被丢弃且多一条会误报自身产物的门禁；⑶ `工具-共享文档编辑锁.py` 无需改但会自动变严（release 守卫逐字复用 `check_block` 且不分历史件）。🔑 **Shao Peishen 2026-09-06 裁 (c)：甲＋精简版乙——Cowork 走补『不做什么』段、guardian 走 fail-loud，两洞全堵；guardian 洞按 (a) 并入本子项、不另开队列行**（原话「有建议项都按建议执行」）。**落地**（`OP-0906-M`，分支 `claude/op0906m-dont-guard-land-487`，commit `a28bf6f`，已 push）：甲三件全落（`opener骨架.md`【Cowork】节补段＋「三处最常丢的结构」表扩为四处／`工具-opener生成.py` Cowork 分支拼 `dont_block`／`工具-opener块lint.py` 形态⑦四处同步）；精简版乙新增 `BODY_PARAM_SUPPORT` 白名单与 `_reject_silently_dropped_body_params()`，**刻意未落地乙对 `--env Cowork` 的拦截**（甲已让其生效，同落会互相打架），由单测 `test_cowork_传dont_不再被守卫拦` 钉死。F7 两坑（子串陷阱：`做什么：` 是 `不做什么：` 的子串；散文陷阱：要求行尾无正文会误伤整行写法）各配单测。**看护者独立复核**：opener 两套单测 `104 passed, 18 subtests`（基线 84）、`test_工具-共享文档编辑锁.py` `371 passed` 零回归；**自跑全库 `工具-opener块lint.py`** ⇒ 历史件 632（F7 占 1）＋当前在用件 57 ＝ 689，**在用件 57→57 零新增阻断**，与起草件预估逐数吻合。**Shao Peishen 2026-09-06 裁 (a) 合入**，看护者代跑 `git merge --no-ff` ⇒ **merge commit `241e178`**（合并前算「工作区脏文件 ∩ 合并带入文件」交集为空才动手，未吞掉他线未提交改动）。⚠️ **遗留一处、影响面全库 1 份**：`开场prompt-构建环境瘦身第二轮A2A3-环境总线交接-2026-09-04.md:13` 判历史件不修，但 release 守卫不分历史件——谁下次编辑该件会被 F7 拦一次，出路是补一行「不做什么」段或写 `OPENER_EXEMPT_MARK` 豁免。**本行仍 open 的原因**：第三个子项「取号查重盲区（取了号但没落进任何载体＝没占）」修法候选尚未择一，不在上述泳道范围。 2026-09-06 回填：第三子项「取号查重盲区」Shao Peishen 现场裁决——**⑵ 未落盘就不算占用**（预留但未在任何载体落成实际行的编号，不视为已占）。🔴 这是设计决策，不是文档说明：现有 `_reserve_ids`／`reserved_map`（`工具-共享文档编辑锁.py`）与 `openspec/specs/editlock-queue-number-reservation/spec.md` 均未覆盖"预留后从未落盘"这一情形的释放逻辑，落地需要真代码改动＋新增单测＋对应 spec delta，不是本次能顺手改完的量级。本行决策部分已了结，**实现仍待派工**（走 propose→design→apply，同 `#480` 先例），未派工前本行不销号。 登记豁免：release 时 git status 反复超时，本 session 今日已多次实测该沙箱在多方并发写入下存在真实延迟（非编造，非回避）。opener豁免：本次未新增/改动任何 opener 块，只为让同一处 fail-closed 校验通过而一并声明。  ━━━ 行长豁免：首段＋末段本身即 4,508 B（唯一可迁的中间段迁走后仍超 4,096 B），机械外置到此为止；由值周巡检 `Cowork-值周巡检0907` 于 2026-09-07 代加，**环境线可随时撤销本豁免并做一次真正的段内瘦身**——保留豁免只是为了不让 09-11 起的 K2 阻断闸把这条在办行卡死。 ━━━ ✅ **第三子项修法定死（2026-09-07，Shao Peishen 答合审材料 §11 (a)＝⑵「未落盘就不算占」，确认 09-06 现场裁定）**：派工 propose→design→apply（`_reserve_ids`／`reserved_map`＋`editlock-queue-number-reservation` spec delta＋单测，同 `#480` 先例）；下一看护批以 openspec 起草泳道承接，design 审再走一次。


## 2026-09-09 apply 回写（CC `OP-0909-X`，泳道 `487-opener-lint`，批 `B-0909_六泳道机制批`）

> K2 口径：本节为 §一 `#487` 状态列 2026-09-09 apply 记录的**完整原文**；行内只留一句
> 结论＋本指针（该行状态格 apply 前实测已 4362 B、超 4096 B 上限，不再往行内堆）。

分支 `claude/op0909x-487-opener-lint` @ `bd881f2`，已 push origin，**未 ff master（🟡 档，待他一字母）**。

**⑴ `--variant subtask_lane` 未传 `--do`／`--dont` 时不再硬塞两段占位。**
骨架【CC · 子任务泳道】明写该变体正文恒为三行、做什么／不做什么一律写进**队列行**。
改前实测出 11 行（含 `做什么：` `1. …` `不做什么：` `- …`），改后 5 行＝三行正文 ＋ P4 两条默认口径。
传了 `--do`／`--dont` 仍照拼（`BODY_PARAM_SUPPORT` 登记本组合两个都支持），既有用例
`test_cc_subtask_lane_两个都传_照常出件` 零回归。
🔑 与本行既有子项**同源而镜像**：既有子项＝「一个参数被接受却不生效」（`--dont` 在 `--env Cowork`
下静默丢弃），本次＝「**一个参数没传却仍产出内容**」。

**⑵ `--branch` 前缀重复拼改 fail-loud。**
传 `--branch op0909b-docx-481` 改前得 `claude/op1231r-op0909b-docx-481`（前缀重复、**不报错**、
成品是个看起来正常的分支名）；改后 exit 1 并点名「应传 `docx-481`」，同时给出会拼坏成什么。
`--help` 的 `--branch` 一条同步写明「只传 OP 短号之后那一截……勿再含 `opNNNN[a-z]` 形态的 OP 短号」。
判据 `BRANCH_SLUG_OP_PREFIX_RE = ^op\d{4}[a-z]?(?=-|$)` 要四个真数字 ⇒ 实测
`opener-gen`／`ops-fix`／`op-0909-x`／`dont-guard-487`／`openspec-apply`／`docx-481` 一个都不误伤；
`guardian` 变体的分支字段是骨架 §三bis 固定字面量，结构性排除在外。

**⑶ 闸（本行判据写死的必建项）＝三件一体。**
- ① `工具-opener块lint.py` 新增**形态⑧**：子任务泳道 opener 块里出现**未替换的正文占位条目**
  （整行只有 `1. …`／`- …`，全角半角、三种列表记号皆认）即报。生效日 2026-09-09，已同步登记
  `RULE_EFFECTIVE_FORM8`／`RULE_EFFECTIVE_BY_FORM`／`FORM_TITLE`／历史件计数行／模块首部形态表五处。
- ② **闸自动接在生成器出件路径上**：`generate_opener` 末尾本就复用 `check_block` 自检，
  形态⑧ 一加，「未填的模板」在出件那一刻就被拦下。单测 `test_半填即fail_loud_不出半成品` 钉死
  ——只传 `--do` 的半填调用现在报错退出，而不是产出一份留着 `- …` 的半成品。
- ③ 单测 `骨架与生成器契约`：期望值全部**现读** `opener骨架.md`【CC · 子任务泳道】那一节
  （行数、尾两行逐字、六字段、无做什么段、硬规则原文仍在），**不在测试里抄第二份判据**
  ——正本改了而生成器没跟、或生成器多塞一行，本类当场红。

**🔴 同批发现并已就地对齐的第三例漂移。**
正本该节写「**恒为三行**」，而生成器实际输出**五行**（尾部固定追加 P4 两条默认口径），
两处口径各自演进、此前无人比对——这正是本行判据担心的「同族第三例」，且已 latent 存在。
处置：`opener骨架.md` 该节把 P4 两行写进块内、并写明「除这两条外一个字都不许多」，
措辞由「本变体恒为三行」改为「本变体**正文**恒为三行」。🔴 **动了格式正本，须复核**；
若判为不该动，回退这一处即可（生成器与 lint 侧不依赖它的措辞，只依赖该节的结构）。

**实测（命令＋真实回显）。**
- `python -m pytest "0-学习与工具/test_工具-opener块lint.py" "0-学习与工具/test_工具-opener生成.py" -q`
  ⇒ 改前 `143 passed, 20 subtests passed in 49.51s`；改后 `170 passed, 40 subtests passed in 23.50s`。
- `python -m pytest "0-学习与工具/test_工具-共享文档编辑锁.py" -q`
  ⇒ `472 passed, 1 skipped, 43 subtests passed in 173.34s`（release 守卫逐字复用 `check_block`，零失败）。
- `python "0-学习与工具/工具-opener块lint.py" --enforce`
  ⇒ 改前 `✗ 当前在用件 36 处待修【阻断】`、`历史件命中 632 处`、`exit=1`；
     改后 `✗ 当前在用件 42 处待修【阻断】`、`历史件命中 643 处（… F8 11）`、`exit=1`。
- `python "0-学习与工具/工具-opener块lint.py" --file "1-转型规划/0-全景路线图/看护件-泳道看护批B-0909_六泳道机制批-2026-09-09.md"`
  ⇒ `opener 块自检命中 6 处`，六条全是 `[F8] … 有 2 条未替换的正文占位条目（如 1. …）`，
     行号 58／76／94／112／130／148 —— 即本批看护件 §三 的六条泳道块。

**🔴 关于「`--enforce` 必须零违规」这条验收要求。**
`--enforce` **改前基线本就非零**：`36 处待修、exit 1`（F1 15／F2 17／F5 1／F6 3，全部先于本棒存在）。
故本棒只能自陈「**新增 6 处、全部同源可解释**」，**不得自陈「零违规」**。
新增的 6 处 100% 集中在 `1-转型规划/0-全景路线图/看护件-泳道看护批B-0909_六泳道机制批-2026-09-09.md`
一件的六条泳道块上——正是本行 2026-09-09 追记里点名的「现场活样本」。该件属 `OP-0908-V`
看护者线触碰区、且本批六条泳道正在跑，**本棒未代改**；出路＝把六个块按骨架改成三行
（各泳道的做什么／不做什么本就写在各自队列行里，删掉占位段不丢任何信息），
或按 `OPENER_EXEMPT_MARK` 写一次豁免声明。
⚠️ 连带影响：本改动合入 master 后，编辑锁 release 侧 opener 守卫（逐字复用 `check_block`）
会对该件命中 F8×6——按 `_opener_attribution`，**对该件作者线是阻断、对他线降级为告警**。

**⚠️ 本棒未做的事（如实声明，不含糊）。**
- `openspec/changes/opener-id-claim-semantics/` **未 apply**。理由：其 `tasks.md` 的 0.1 前置闸
  要求「决策点 ①②③ 均已拍板，答案逐条回填 `design.md`（写明拍板日期与手段）」，而实测
  `design.md:4` 仍写「**三个决策点全部待拍板**」，回填未发生；且 `design.md:61` 已记该包与
  同泳道 A3（变更包 `single-guarded-copy`）同触 `工具-opener生成.py`、须定先后。
  队列行记着 Shao Peishen 2026-09-08 答 `1(a)`＝design 放行，但**放行的记录只在队列行、未落到包内**，
  该包自己的闸因此不成立。⇒ 建议单独派一棒，先补 0.1 回填再动手。
- **未 ff master、未销号**（两者都按派单件要求交他）。
- 未碰 `#522`（真身现取 `🛑 排队中·暂非可动`，且机制类可动 WIP 26/22 仍超限，解冻条件不成立）；
  未碰在跑行 `#462`／`#504`／`#506`／`#419`；未碰 SC2 触碰区。
