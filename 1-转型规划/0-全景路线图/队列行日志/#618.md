---
title: "队列行日志 #618（§一）"
created: 2026-09-19
status: 生效
用途: 跨桌任务队列 §一 #618 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #618 · 外置段（2026-09-19 外置，工具第 1 批，3 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-A；外置前整格 4733 B、共 6 段；本批外置第 2、3、4 段；本格保留第 1、5、6 段。

📌 **2026-09-19 前置理清（`OP-0919-A`，他答 `3b`）**：**本行前置＝「三选一形态」＋「陈旧阈值 N 未定」两项，均属 🟡 档，只能由他拍**。本方判断：① 独立哨兵——多一条要人看的腿，与「新建告警须自带消费者」的现行纪律相抵（它自带清锁动作，勉强够格，但多一个进程）；② 并进 `工具-worktree体检.ps1`——**同属 git 层健康检查、已有留痕通道 `reports/worktree-guard/`、已挂 `poll-opener-batch` 轮询**，增量最小；③ 挂进 `ZhuopinPollGuard`——接力卡现记「它失败会叫但没人在看 `LastTaskResult`」，往一条没人看的腿上再加活，风险最高。**本方建议＝②**，阈值取 **30 分钟**（与泳道看护看门狗同值，少一个要记的数）。🔴 清锁三条判据不变：`Get-Process git` 为零 ＋ 锁文件 0 字节 ＋ 陈旧超阈值，三条齐备才动手。待他一字母。

━━━

✅ **2026-09-19 形态与阈值已定（Shao Peishen 答 `5a`）**：**形态 ② 并进 `0-学习与工具/工具-worktree体检.ps1`**（同属 git 层健康检查、已有留痕通道 `reports/worktree-guard/`、已挂 `poll-opener-batch` 轮询，增量最小）；**陈旧阈值 ＝ 30 分钟**（与泳道看护看门狗同值，少一个要记的数）。🔴 清锁三条判据不变：`Get-Process git` 为零 ＋ 锁文件 0 字节 ＋ 陈旧超 30 分钟，三条齐备才动手；配套仍须给 `工具-泳道分支合入.ps1` 的 ff 失败分支补打印锁状态。⇒ 前置已清，本行可开工。

━━━

🚀 **2026-09-19 派出建造（批 `B-0919_机制缩编`，泳道 `gitguard`·A3，与 `opener` 泳道并行）**。**本批做什么**：按他 `5a` 定的形态 **②** 落地——把陈旧锁扫描并进 `0-学习与工具/工具-worktree体检.ps1`（同属 git 层健康检查、已有留痕通道 `reports/worktree-guard/`、已挂 `poll-opener-batch` 轮询）：扫 `.git/**/*.lock`，**阈值 30 分钟**，🔴 清除三条判据齐备才动手（`Get-Process git` 为零 ＋ 锁文件 0 字节 ＋ 陈旧超 30 分钟）；配套给 `0-学习与工具/工具-泳道分支合入.ps1` 的 **ff 失败分支补打印锁状态**，让「ff 失败」能指出成因；🆕 **顺手补同处另一面**：该脚本 `git worktree add` 撞「branch already used by worktree」时只报 `exit=9 脚本异常`、不报成因（2026-09-19 实撞一次，手动 `git worktree remove` 后即通过）——改为当场点名是哪条 worktree 占着。每条守各带回归用例。 **本批不做什么**：🔴 **不做形态 ①／③**（独立哨兵／挂 `ZhuopinPollGuard`，他已判不做）｜🔴 **不在本泳道真清任何锁**（只建能力，不在建造期对生产仓库动手）｜🔴 不碰 `.51`／真实库｜🔴 不 ff master、不在主仓 commit｜🔴 不长持队列编辑锁。 **收工**：只 push 本泳道分支；登记 §二 批次；回写本行；顶格 `OPENER_DONE` 收尾。
