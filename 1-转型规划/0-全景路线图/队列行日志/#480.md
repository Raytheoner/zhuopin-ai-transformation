---
title: "队列行日志 #480（§一）"
created: 2026-09-06
status: 生效
用途: 跨桌任务队列 §一 #480 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋指针＋末段。
---

---

# §一 #480 · 外置前整格原文（2026-09-06 `OP-0906-O` apply 期外置，7778 字节，md5:1a9252e1）

> 外置动机：外置前状态格 **7778 字节**，已近 K2 上限（4 KB）的两倍；本次 apply 收工还要再追加一段，不外置必然继续膨胀。**原文一字未改**，行内改留「首段＋本指针＋apply 末段」。
> 同批先例：`#454`（`OP-0906-N`，整格 9.4 KB → 3.7 KB）。

[S:open][D:机]🟡 **2026-09-06 CC `OP-0906-L` 已起草 openspec 变更包，停在 design 审等你一个字母**（分支 `claude/op0906l-credential-guard-480`，commit `a778c19`；包＝`openspec/changes/editlock-credential-shape-guard/`，proposal＋design＋tasks＋spec delta）。**门槛判定**：命中 ③「改变既有模块对外语义」（同一输入由放行改为拒绝）＋②「涉鉴权与数据可见性」；先例＝`#455` 在**同两个函数**上新增写侧拒绝判据亦走完整 openspec＋design 审 ⇒ **止步于起草，未写一行实现代码**。🔴 **「加不加」不复议**（你 2026-09-03 已拍板 (a)＝加），design 审只拍**怎么加**五点：①判据范围（只取四条结构化规则／再加通用启发式；**本项无默认，须你明确答**）②有无逃生阀（推荐硬拒）③回显多少命中内容（推荐前 8 字符）④是否含 `--note`（推荐不含）⑤校验对象＝新值还是整行（推荐只校验新值，否则重犯 `#324` 锁死）。**实测取证（非推断）**：全部 **1465 个已跟踪 `.md`** 各跑两遍判据——结构化四条命中 **0** 处、通用启发式亦 **0** 处 ⇒ 决策点① 两个选项现网误伤面均为零；`git check-ignore -v` 实跑 `*.editlock*` 命中 `.gitignore:69` ⇒ `--note` 不进 git。⚠️ **不因起草完成而销号**——实现（tasks 2.x/3.x）须 design 审后另派。 ━━━ 🟢 **已激活·可领（2026-09-06，Cowork 业务总线 B-0906_G，机制类可动 WIP 现测 20/22 有余量，激活本行不满 22 上限）**——`#479`／`#481`／`#482`／`#483`（同批 `B-0903_80`）仍排队，本批只激活本行（触碰区独立，不与其余四行同触碰）。**原 🛑 段全文保留于下，未删一字。**  ━━━ 🛑 **排队中·暂非可动（2026-09-03 立行时机制类可动 WIP 顶格，同 `#381`／`#439`／`#447`／`#478` 既例）。** ━━━ **立行依据** ＝ Shao Peishen 2026-09-03 §四 分诊消化裁决（`OP-0903-D`，他答「都按你说的」），批次 `B-0903_80`。 ━━━ 🔑 **本行要记的成因（比实现更重要）**：**队列文件的本职就是「把对话原样留痕」，而原样留痕与凭据隔离天然冲突** —— 2026-08-24 记录「IT 对应的群＝运维部AI保障群」时，把他给的 webhook **连 key 一起原样粘进了 §一 `#351` 行内**。**这不是谁不小心，是载体属性** ⇒ 只能靠机制拦，不能靠人记。 ━━━ ⚠️ **本行不含轮换动作** —— `#118` ⑴ 轮换 key 须 **Shao Peishen 本人**去企微后台重建群机器人（AI 不碰真实群配置）；⑵ git 历史**已裁定不清洗**（轮换后旧 key 即失效，清洗＝重写全部 commit hash，代价远大于收益）。 ━━━ 登记豁免：release 时 git status 反复超时，本 session 已多次实测该沙箱对本仓库存在真实 git 锁竞争（非编造），不是回避真实登记缺口。 ━━━ opener豁免：本次未新增/改动任何 opener 块，只为让同一处 fail-closed 校验通过而一并声明。 ━━━ 【2026-09-06 OP-0906-G 看护批 B-0906_G，A5 泳道 480-credential-guard：design 起草完成 ＋ Shao Peishen 当场裁决五点，转 apply】**门槛判定**：命中 openspec 三门槛 ⇒ 止步起草、未写一行实现代码。判据（rules/场景建造与合规 §二）：③「改变既有模块对外语义」——同一输入（含凭据形状的单元格值）由 `edit-row`／`append-row` 放行改为拒绝，是对两个子命令对外契约的收紧；②「涉鉴权与数据可见性」——本闸作用面即凭据可见性；**直接先例 `#455`（`editlock-write-guard-hardening`）在同两个函数 `cmd_edit_row`／`cmd_append_row` 上新增写侧拒绝判据，走了完整 openspec ＋ design 审**。**openspec 包**：`openspec/changes/editlock-credential-shape-guard/`（proposal 105＋design 115＋spec delta 76＋tasks 56 ＝ 352 行），分支 `claude/op0906l-credential-guard-480`，commit `a778c19`（从 master `fc4e4ca` 起，未推送、未合入）。**看护者独立复核**：`git show a778c19 --stat` ⇒ 恰 4 个新增 openspec 文件、零删除、无实现代码；`git diff --stat master <分支> -- 工具-密钥扫描lint.py 工具-共享文档编辑锁.py` ⇒ **输出为空**，两份本体确未改动（判据正本唯一，未重写第二份）；`openspec validate editlock-credential-shape-guard --strict` ⇒ `Change is valid`；`git ls-files "*.md" | wc -l` ⇒ **1465**，与 A5 报告的分母一致；`git check-ignore -v` ⇒ `.gitignore:69:*.editlock*` 命中，④ 的实测依据成立。**实测取证（A5 本次实跑，非推断）**：⑴ **误伤面＝零**——对全部 1465 个已跟踪 `.md` 各跑两遍，`工具-密钥扫描lint.py` 的结构化 `CREDENTIAL_PATTERNS` 四条命中 **0 处**，`GENERIC_ASSIGNMENT_RE` 通用启发式（含占位符／纯大写标识符／长度<8 三重过滤）亦命中 **0 处**；⑵ `--note` 不进 git（见上 check-ignore）。🔑 **Shao Peishen 2026-09-06 于 OP-0906-G 看护会话当场裁决（原话「A5按建议走」），五点全按 design 推荐**：**①=(b)** 判据范围取「结构化四条 ＋ 通用启发式 `GENERIC_ASSIGNMENT_RE`」（本项原无默认；裁决依据＝两选项实测均 0 命中，(b) 的理论误伤面在现网为零，而 (a) 的漏网面确定存在）；**②=(a)** 命中后硬拒、不给逃生阀（取严可逆、取松不可逆——凭据进历史只剩轮换）；**③=(b)** 拒绝文案回显前 8 字符＋截断标记，**刻意不与 lint 的 24 字符对齐**（两者输出落点传播面不同，包内已注明防后人顺手对齐）；**④=(a)** 作用面不含 `--note`（note 被再粘进队列行时仍走本闸，覆盖闭合）；**⑤=(a)** 只校验本次新值、不校验整行（(b) 会把含凭据形状的历史行锁死，正是 `#324`／`#454` 已付学费的形态）。🔴 **「加不加」不在审议范围**——Shao Peishen 2026-09-03 已拍板 (a)＝加（§四 `#118` ⑶），变更包三处明写不复议。已 `工具-泳道看护状态机.py resume --lane 480-credential-guard` 落库解除暂停。**待落地泳道 OP-0906-O 做什么**：① 按 `openspec/changes/editlock-credential-shape-guard/tasks.md` 逐条 apply（走 skill `openspec-apply-change`／`opsx:apply`，逐条勾 tasks；1.1–1.4 propose 期已勾，从 2.x 起）；② 严格按上述五点裁决落地，**不得复议、不得偷改成别的选项**；③ 🔴 `工具-密钥扫描lint.py` 本体一行不许改——只复用其 `CREDENTIAL_PATTERNS` 与 `GENERIC_ASSIGNMENT_RE`，不重写第二份判据；④ 单测：反例（形状合规的**固定假串**，非真凭据）必须被拒且点名命中了哪条规则、正例（正常队列文本）不受影响、`--note` 不被拦（④ 的边界）、历史行含凭据形状时 `edit-row` 改**其它列**不被误拒（⑤ 的边界）；⑤ 拒绝文案须给出路「凭据请落 `.env`，队列只写指针」；⑥ 🔴 **`proposal.md` §Impact 的「下游消费者核实」标为未核，apply 期必须重跑、不得引用 `#455` 上一个包的结论**——须实测 `工具-队列结构lint.py`／`工具-落库sweep.py`／`工具-队列查询.py` 是否消费这两个子命令的返回码或 stdout；⑦ 合跑 `test_工具-共享文档编辑锁.py` 全量确认零回归。**不做什么**：不碰 `工具-密钥扫描lint.py` 本体；不代 `#479`／`#481`／`#482`／`#483` 解除排队；不做凭据轮换、不清洗 git 历史、不追改 `#351` 等历史行。**收工**：跑 `openspec validate --strict` ＋ 全量单测，回写本行 ＋ 登记 §二。


---

# §一 #480 · `OP-0906-O` apply 收工回写全文（2026-09-06）

> 行内末段只留结论与留步项；本节是同一次回写的完整原文。

【2026-09-06 `OP-0906-O` 泳道 `credential-guard-apply-480`：变更包 `editlock-credential-shape-guard` apply 完成，止步于「未 ff 进 master」＋「openspec 未归档」】

**产出**：分支 `claude/op0906o-credential-guard-apply-480`（从 master `e472016` 起，cherry-pick 起草期 commit `a778c19` 把变更包带进来），commit `cad0588`，已 push origin，**未 ff 进 master**。`openspec validate editlock-credential-shape-guard --strict` ⇒ `Change is valid`。

## 一、五点裁决逐条落地（未复议、未偷改成别的选项）

| # | 拍板 | 落地形态 |
|---|---|---|
| ① | (b) | `_credential_shape_violations()` 同时跑 lint 的 `CREDENTIAL_PATTERNS`（结构化四条）与 `GENERIC_ASSIGNMENT_RE`（通用启发式），后者复用 lint 的 `_looks_like_real_secret` 三重过滤（占位符右值／纯大写标识符右值／长度<8）。单测 `test_33b_generic_assignment_rule_is_named` 专门钉住这一族——**有人把范围偷改回 (a) 会立刻变红** |
| ② | (a) | 命中即 `print` ＋ `return 1`，全程不提供任何豁免开关或行内豁免标记；拒绝文案里明写「本闸不提供豁免开关」及其理由（取严可逆／取松不可逆） |
| ③ | (b) | `CREDENTIAL_HIT_PREFIX_LEN = 8`，`_credential_hit_excerpt()` **无论命中串多短都只回显前缀且始终带截断标记**。🔴 常量处与模块文件头**两处**都写明「刻意不与 `工具-密钥扫描lint.py` 的 `m.group(0)[:24]` 对齐，后人请勿当作漂移去统一」——理由是两者输出落点的传播面不同（lint 落 CI 日志、本闸落操作者终端 scrollback） |
| ④ | (a) | 只挂 `cmd_edit_row`／`cmd_append_row` 两处；`acquire`／`release --note` 通道**未接线**。单测 `test_boundary_04_note_channel_is_not_guarded` 实测 note 原样落进 `*.editlock` |
| ⑤ | (a) | `cmd_edit_row` 只喂 `changed_values`（本次新值），`cmd_append_row` 只喂 `_resolve_append_cells()` 归一后的内容格；**不校验改动后整行**。单测 `test_boundary_05b_legacy_credential_can_be_redacted` 钉住那口井：含凭据形状的历史行必须仍能被改写成 `<REDACTED-见§四#118>` |

## 二、实现落点

`0-学习与工具/工具-共享文档编辑锁.py`：

- `_CREDENTIAL_LINT_SCRIPT` ＋ `_load_credential_lint_module()` —— 逐字仿照 `_load_opener_lint_module()` 的动态加载与 fail-loud 惯例（文件名含中文/连字符，不能直接 `import`）。
- `_credential_hit_excerpt()`／`_credential_shape_violations()` —— 返回「列名 ＋ 规则名 ＋ 截断前缀」三元文案。
- `_named_append_values(section, cells)` —— 给 `append-row` 的内容格配列名（有编号列的分区跳过列名表首项；格数多于列名时按 `第N格` 兜底，**不静默丢格**）。
- `_print_credential_lint_unavailable()`／`_print_credential_rejection()`／`_credential_guard_rejects()`。
- 挂载点两处：`cmd_edit_row` 在 `changed_values = {**sets, **appends}` 之后、`--repair` 留痕检查之前；`cmd_append_row` 在 `_resolve_append_cells`／`_build_append_row_line` 的 try 块之后、回读列数校验之前（挂在四入口归一之后 ⇒ `--cell`／`--set`／`--cells-json`／`--stdin-json` 一次覆盖，不逐入口打补丁）。
- 模块文件头新增 `#480` 段：成因（载体属性冲突）／判据来源（复用不重写）／五点结论／🔴 覆盖边界如实措辞／回显位数刻意不同的理由。

🔴 **`工具-密钥扫描lint.py` 本体一行未改** —— `git diff --stat -- 0-学习与工具/工具-密钥扫描lint.py` **输出为空**；`git status --porcelain` 全程只有编辑锁本体与其单测两个 `M`，**零新增文件**（proposal §伴生文件 那条"不出现任何新文件名形态"的实跑核实）。

## 三、单测（`CredentialShapeGuardTests`，15 例／11 subtests）

反例：`edit-row`／`append-row` 各一（成对，证明判据装在两个入口）；四条结构化规则各点名到**正确的那条规则名**；通用启发式一条。
正例：正常队列文本（中文叙述／反引号包路径／`[S:done][D:机]` 状态串／`` 见 `.env` 的 `WECOM_WEBHOOK_URL_OPS` `` 指针写法）；占位符叙述写法三例（`` `XKY_APP_KEY=<value>` ``／`API_TOKEN = "TODO"`／`_GATE_ENV_VAR = "ZP_GATE_PASSWORD"`）。
文案：含 `.env` 与「指针」与可照抄替代写法；**不含**完整命中串、不含 key 主体、含「已截断」，并钉死 `CREDENTIAL_HIT_PREFIX_LEN == 8`。
🔴 **非恒真自证**：把 `_load_credential_lint_module` mock 成返回**零条规则**的桩（＝本包实现前的状态）⇒ 同一输入由**拒绝变放行并真的落盘** ⇒ 同时证明拒绝来自本包新增判据、且编辑锁内不存在第二份复制的判据。
fail-loud：加载器抛异常 ⇒ 返回 1、文案含判据正本路径与「修复方向」、目标文件字节不变。
边界：④ `--note` 不被拦；⑤ 历史行改其它列不被误拒；⑤ 历史行那一格可被改写成 `<REDACTED>`；「判据正本唯一」机器守（拿 lint 本体的 `.pattern` 去编辑锁源码反查，一个都不许有）。

回归：`test_工具-共享文档编辑锁.py` 全量 **406 passed, 19 subtests passed**（229 秒），零回归。

## 四、apply 期实跑取证（🔴 未引用起草期数字，也未引用 `#455` 上一个包的结论）

1. **tasks 1.5 重跑全量 `.md`** —— 手段：一次性只读脚本，对 `git -c core.quotepath=false ls-files` 列出的 **1473 个已跟踪 `.md`**（比起草期 1465 多 8 份）各跑两遍判据。结果：结构化四条命中 **0**、通用启发式命中 **0**，与起草期同结论、未漂移。
2. **tasks 1.6 下游消费者重跑** —— `工具-队列结构lint.py` 只 `importlib` 加载编辑锁**模块**取三个常量、从不调这两个子命令；`工具-落库sweep.py` 的 `_edit_lock()` 全部 4 处调用点只传 `status`／`acquire`／`release`（L4868／L4986／L5002），`_run_triage_candidates_json()` 只传 `triage-candidates --json`，L5847 的 `append-row` 只是**打印给人看的建议命令行字符串**；`工具-队列查询.py` 仅两处注释提及。另跑一次全仓补充 grep 无新增消费者。⇒ **无下游解析这两个子命令的返回码或 stdout 格式**。
3. **tasks 4.1 现网只读跑（用已实现的判据）** —— 按读侧 `split_row_cells` 逐格喂：机制队列 **1492 格**、业务队列 **652 格**、跟进信 README **441 格**，合计 **2585 格 ⇒ 命中 0**，与 1.2／1.5 一致。
4. **tasks 0.2 触碰区** —— 遍历 `git for-each-ref refs/heads/` 全部 **279 个本地分支**，3 个分支对本文件有 `master..<branch>` 提交，但 `git diff --stat master <branch> -- <该文件>` 三者全部是「相对 master 大量删除」（-397／-3505／-4655 行）⇒ **均落后于 master，无一领先**。派单件点名的同碰者 `claude/op0906k-status-triage-454` **已合入 master**（head `e472016`），无冲突。
5. **tasks 0.3 `#98` 漂移** —— `pip show zhuopin_platform` 的 `Editable project location` 指向**主 checkout**而非本 worktree；本包不改 `queue_table`，且实测两份文件 SHA-256 前 16 位同为 `82eb1ab361b4c1ae`（逐字节一致）⇒ 本轮无漂移。**不是"因此可以不管"**：改 `queue_table` 的包仍须先处理这条。

## 五、⚠️ 如实报出，不在本包处置

现网跑 `python 0-学习与工具/工具-密钥扫描lint.py` 实测 **exit=1，5 处疑似**：

- `工具-未闭合产出扫描.py` 的 `EXPECTED_SERVER_TOKEN`
- `工具-落库sweep.py` 的 `LOCAL_ONLY_DIVERGED_KEY`／`LOCAL_ONLY_AHEAD_KEY`／`LOCAL_ONLY_UNSCANNED_KEY`／`CLAUDE_MD_RULES_TOTAL_KEY`

五处**全部落在本包未触碰的既有文件**（`git status --porcelain` 全程只有编辑锁本体与其单测两个 `M`）⇒ **既有假阳性，非本包引入**；形态都是「变量名像凭据、右值其实是普通字符串常量」。🔴 **不在本包范围内处置、也不顺手改 lint**（派单件「不做什么」第一条）。**这正是 `#118` 记的「CI 红了两个多月没人看」那条判据的当下实证** —— 建议另立行。

## 六、留步（不由本泳道代做）

1. **ff 进 master**（分支已 push，未合）。
2. **tasks 5.2** —— §四 `#118` 行内追加一句「⑶ 的人守面已由机器接管」指针（只追加、不改历史正文）。`#118` 属**他人触碰区**，按「决策路由」不就地改，随本次回写一并登记、待总线派发。
3. **tasks 5.4** —— `/opsx:archive editlock-credential-shape-guard -y`，**合入 master 之前不归档**。
4. **`#118` ⑴ 凭据轮换**仍须 Shao Peishen 本人去企微后台重建群机器人，本闸不代替；⑵ git 历史已裁定不清洗，本包不复议。

## 七、🔴 覆盖边界与档位（不得读成全覆盖）

本闸**只覆盖走 `edit-row`／`append-row` 正门的写入**。编辑器直接改文件、脚本整文件重写这类绕过正门的写法它看不见 ⇒ 回显与汇报只能说「已校验本次经正门写入的 N 个值」，**不得**说「队列已不可能进凭据」。全仓事后兜底仍归 CI `凭据扫描`，两道闸分工互补（本闸管"别让新的进来"，CI 管"已经在里面的有没有"），**谁也不退休谁**。另：形状判据的本质是形状，拦不住「把 key 拆成四段中文描述」这类改写，也拦不住形状不在规则内的新型凭据。

**交付档位＝档1（mock 验证）** —— design 审通过、判据接线与单测完成、现网全量只读回归跑过，但**尚未在生产真实拦下过一次凭据写入**。🔴 **零命中不得读成「规则已生效」，正确表述是「未被检验」**（同 `#58`／`#122` 既有教训）。晋档2 的条件见 proposal §验收与晋档条件。


---

# §一 #480 · 2026-09-07 外置（6 段迁入，值周巡检）

> 原整格 md5:ad03bc07；行内此后只留首 1 段＋末 1 段＋指针。以下为**外置前整格原文**，原文原样、未改一字。

[S:partial][D:机] 🔴 **未销号：代码已建成并 push，未 ff 进 master、openspec 未归档。** ━━━ 📦 **历史全文已按 K2 口径外置**（原文一字未改）：`1-转型规划/0-全景路线图/队列行日志/#480.md` —— 含起草期／激活期整格原文（外置前 7778 字节，md5:`1a9252e1`）＋ 本次 `OP-0906-O` apply 收工回写全文。 ━━━ 【2026-09-06 `OP-0906-O` 泳道 `credential-guard-apply-480`：apply 完成】分支 `claude/op0906o-credential-guard-apply-480`（从 master `e472016` 起，cherry-pick 起草期 `a778c19` 带入变更包），commit `cad0588`，已 push、**未 ff 进 master**；`openspec validate editlock-credential-shape-guard --strict` ⇒ `Change is valid`。**五点裁决逐条落地、未复议、未偷改**（①=(b) 结构化四条＋通用启发式／②=(a) 硬拒无逃生阀／③=(b) 回显前 8 字符且**刻意不与 lint 的 24 字符对齐**（代码两处注明防后人顺手对齐）／④=(a) 不含 `--note`／⑤=(a) 只校验本次新值）。🔴 `工具-密钥扫描lint.py` **本体零改动**（`git diff --stat` 实测输出为空），编辑锁内**零正则字面量**（单测拿 lint 的 `.pattern` 反查源码机器守）。**单测** `CredentialShapeGuardTests` 15 例／11 subtests（含非恒真自证：零规则桩 ⇒ 同一输入由拒绝变放行并真落盘；含 fail-loud 与 ④⑤ 边界四例）；`test_工具-共享文档编辑锁.py` 全量 **406 passed／19 subtests**，零回归。**apply 期实跑取证（未引用起草期与 `#455` 旧结论）**：1473 个已跟踪 `.md` 两族判据命中均 **0**；下游三消费者重跑（`工具-队列结构lint.py`／`工具-落库sweep.py`／`工具-队列查询.py`）**均不消费这两个子命令的返回码或 stdout**；用**已实现**判据对两份队列＋跟进信 README 只读跑 **2585 格 ⇒ 命中 0**；遍历 **279** 个本地分支，无一领先 master 地改本文件。 ━━━ ⚠️ **如实报出、不在本包处置**：现网跑 `python 0-学习与工具/工具-密钥扫描lint.py` 实测 **exit=1，5 处疑似**，全在本包**未触碰**的既有文件（`工具-未闭合产出扫描.py` 与 `工具-落库sweep.py` 的 `*_TOKEN`／`*_KEY` 常量），系「变量名像凭据、右值其实是普通字符串常量」的**既有假阳性**、非本次引入；**不顺手改 lint**（派单件「不做什么」第一条），建议另立行。**这正是 `#118`「CI 红了两个多月没人看」那条的当下实证。** ━━━ ⏳ **留步（不由本泳道代做）**：⑴ ff 进 master；⑵ tasks 5.2 —— §四 `#118` 行内追加「⑶ 的人守面已由机器接管」指针（`#118` 属他人触碰区，按决策路由不就地改，待总线派发）；⑶ tasks 5.4 `/opsx:archive`（合入 master 之前不归档）；⑷ `#118` ⑴ 凭据轮换仍须 Shao Peishen 本人去企微后台，本闸不代替。 ━━━ 🔴 **覆盖边界（不得读成全覆盖）**：只覆盖走 `edit-row`／`append-row` 正门的写入，编辑器直接改文件／脚本整文件重写它看不见；全仓事后兜底仍归 CI `凭据扫描`，两闸互补、**谁也不退休谁**。**交付档位＝档1（mock 验证）**——尚未在生产真实拦下过一次，**零命中＝未被检验，不得读成「规则已生效」**。 ━━━ 行长豁免：本次已按 K2 口径把 7778 字节历史段整体外置至 `队列行日志/#480.md`（原文未改写），行内只留首段＋指针＋末段。 2026-09-06 回填：tasks 5.2（`#118` 指针）与 5.4（归档）已由本线补齐——本沙箱无 `openspec` CLI，手工复刻 `/opsx:archive` 步骤：`git mv` 至 `openspec/changes/archive/2026-09-06-editlock-credential-shape-guard/`，并按既有主 spec 惯例新写 `openspec/specs/editlock-credential-shape-guard/spec.md`（该 capability 此前无主 spec，delta 即初版，非合并）。Shao Peishen 2026-09-06 现场裁决"(a) 现在归档"。tasks.md 35/35 全勾。🔴 归档不等于验证：档1（mock）→档2（生产真实拦下过一次）的跃迁仍未发生，「零命中不得读成规则已生效」原文保留有效，不因归档而撤销。  ━━━ Cowork业务总线2026-09-07核验：拟修正#478/#479/#480/#482/#483陈旧登记（本行原文仍写「未ff进master」，但`git merge-base --is-ancestor cad0588 master`实测exit=0，实为已经ff＝`bec15d5`），acquire后发现CC-看护B-0907_E已于本次acquire前数分钟完成同批回填（#483/#478/#479/#482四行状态＋六包design审派单到§四），本会话遂不重复改写，仅本条如实补一句：#480的ff完成事实与本行现有历史段不一致，留给下一手回填时一并更正。登记豁免：release因跨session git状态查询超时被拒，本会话对内容仅本条追加、无其余改动，故补此登记豁免放行。opener豁免：本会话未产出任何opener/交接prompt。
