---
title: "队列行日志 #533（§一）"
created: 2026-09-09
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #533 的状态列历史回写段外置件（K2 口径：单格 ≤4096 B，超限外置；原文原样、可 grep）。行内只留首段＋交付结论＋本指针。
外置会话: CC 泳道 `OP-0909-AA`（批 `B-0909_三泳道夜批` 波次 1，泳道 533-editlock-deadlock）
原格md5: b660b0cd2d55d8e64741891249f8d6af
---

# §一 #533 · 外置前整格原文（2026-09-09 外置，5652 B，md5:b660b0cd）

> 原文原样、一个字节未改。外置动因：收口回写后整格 UTF-8 实测 5652 B，超 K2 的 4096 B 上限，**2026-09-11 起由告警转阻断**（届时任何触碰本行的会话 release 会被拒）。
> ⚠️ **量的是字节不是字符**：本格字符 3032、字节 5652，中文件按字符数判会得出一个宽约 2 倍的假上限（同 `#489` 外置时已记的同族坑）。
> 🔑 **本行本身就是「守卫无合法出路」的判例**：若不在本周内外置，09-11 之后这一行会因**行长闸**再次变成「改不动」——与它自己要修的死锁同形态。

[S:done][D:机] ✅ **已完成 —— 2026-09-09 23:4x 本地（CC 泳道 `OP-0909-AA`，批 `B-0909_三泳道夜批` 波次 1，分支 `claude/op0909aa-533-editlock-deadlock`）。** 🔑 **死锁已解，两条 lint 红已转绿**：⑴ ⓘ1 校验窗口由「最近 3 个 commit」改为**按登记日取时间窗** —— 批次号 `B-MMDD_…` 可解出登记日 ⇒ `git log --all --since=<登记日-1天>`（新增 `_resolve_batch_registration_date`，年份往过去推、与 `_resolve_waiver_due_date` 方向相反；-1 天是时区余量），解不出日期 ⇒ 退回全历史 `git log --all`（即本行期望产出里 `-S<路径>` 那一支）；`_recent_commit_touched_paths` 改名 `_commit_touched_paths_since`。🔴 `--all` 不可省：merge 提交自身在 `--name-only` 下不产出任何文件名行，被并进来的**原始 commit** 才产出 —— 这正是「7 次 merge 冲掉窗口」的机理。 ⑵ `cmd_edit_row` 侧 ⓘ1 **按归属分档**（照抄 `OP-0909-W` 对 ⓘ2 的实现）：「文件清单」在本次 `--set`／`--append` 里 ⇒ 硬阻断；不在 ⇒ ⚠ 告警放行。`cmd_append_row` 不分档（新行整行都是本次写的）。**两边缺一不可** —— 只放宽窗口，写坏清单的人仍会在别处撞上；只分档不改窗口，`append-row` 侧照旧误拦。 ⑶ 新增 **16 条单测**（`BatchRegistrationDateTests`／`FileListGitStateWindowTests`／`EditRowFileListAttributionTierTests`）：登记日年份推断四向、merge 潮后仍放行（**含「该文件确已落出最近 3 个 commit」的前提钉子**，前提失效则用例自曝）、窗口外仍拒、全历史退路两向、`--all` 跨分支取证；分档四条 —— 只改状态可过／可补 `称呼豁免：`／写「文件清单」格仍硬阻断／原样重写同一坏值仍拒。 ⑷ 两行 `称呼豁免：` 已补挂（`B-0908_BN_称呼口径收窄与标准工作周改名`、`B-0909_值周章程改名指针补齐`，均追加在「状态」格尾，不动「建议 message」以免污染 sweep 的 commit 文案）。 🔬 **验证手段与真实回显**：① `python -m pytest 0-学习与工具/test_工具-共享文档编辑锁.py -q` ⇒ `528 passed, 1 skipped, 51 subtests passed in 280.22s`（改前同命令 `512 passed, 1 skipped`，零回归、净增 16）；② 补挂前 `python -m pytest 0-学习与工具/test_工具-队列结构lint.py -q` ⇒ `2 failed, 59 passed`（`QueueLintTests::test_real_production_queue_file_passes` 与 `AppellationRealQueueTests::test_real_queue_with_committed_baseline_is_clean`），补挂后同命令 ⇒ `61 passed, 6 subtests passed in 8.61s`；③ 死锁本身的复现与解除 —— 改前直接调 `_file_list_git_state_violations` 对两行现取，三条反引号串全报「未在主仓 git 状态里找到对应实体…也不在最近 3 个 commit 内」；改后 `edit-row --changes-json` 两次实测退出码 **0**（`✓ 已改 §二 #… 的「状态」格`），其中 `B-0908_BN` 那次只打了 ⓘ2 的 ⚠ 存量告警、未阻断 —— 分档生效的实证。 🔴 **本棒不 ff master**（按批次纪律，主仓 ff 由 sweep 收尾段或看护者串行做）；分支已 push（`05d6dab`）。 📎 成因与两条追加实证正本仍在 §四 `188`，本行不复述；`188` 可随本行一并收。　　—— 以下为 2026-09-09 立行时的原文，保留不追改 —— 🟢 **可领 —— 2026-09-09 19:5x 升格立行（CC 看护者 `OP-0909-Q`，Shao Peishen 当日答 `1a`＝下批派一棒治根因、两条 lint 红着等它）。** 🔴 **本行有正在发生的损害，不是预防性建设**：master 上 `test_工具-队列结构lint.py` 的 `test_real_production_queue_file_passes` 与 `AppellationRealQueueTests::test_real_queue_with_committed_baseline_is_clean` **双双红着**，且**无合法出路**。 🔑 **判据（本行的核心，勿简化成「窗口太窄」）**：这不是一条行改不了，是**守卫 A 的逃生阀被守卫 B 锁住** —— 称呼 lint 的逃生阀是行内写 `称呼豁免：`，而**写这个逃生阀恰恰要过 `edit-row`**，`edit-row` 的文件清单 git 状态校验又恒拒该行。两道守卫各自都对，合起来构成死锁。⇒ **修法必须同时看这两道，只放宽一边会再造一个洞**。 📎 **成因与两条追加实证的正本在 §四 `188`**（`--repair` 实测不解此锁，只放宽列数前置；以及「未采取的绕法及不采的理由」），本行不复述。 ⚠️ **锁面在扩大**：校验窗口是 commit **条数**（3）不是时间，一次批收工的 merge 潮就能把窗口冲掉，**越是正常落库的行越先被锁死**；本批 7 次 merge 即锁死 2 行。 🔴 **不采纳的两条替代路**（Shao Peishen 已看过并选了 `1a`）：⑴ 改 `队列称呼判据-baseline.json` 把 5 处冻进 baseline —— baseline 是判据、改它属 🟡，且等于用「冻结存量」掩掉当日新造的违规，不是同一件事；⑵ 回退 `2a` 去反引号 —— 那 5 条批次会重新自锁、`#443` 清扫再次卡死。 🔴 **WIP豁免：立行时机制类可动 WIP 现取超上限 22**（手段＝`0-学习与工具/工具-共享文档编辑锁.py` 的 `_count_mechanism_wip` 对两份队列真身现取）。**本行以 🟢 可领立行而非 🛑 排队**，理由＝它是当日已发生损害且无合法出路的唯一一条（其余四条今日新行均为 🛑），压回排队等于让 master 的两条红无限期红着；出路⑴「先关闭一条既有机制类可动行」涉他线在跑行，本棒无权触碰（看护件硬边界 3 只报不改）。
