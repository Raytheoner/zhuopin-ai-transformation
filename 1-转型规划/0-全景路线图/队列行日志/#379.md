---
title: "队列行日志 #379（§一）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §一 #379 的状态列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §一 #379 · 外置前整格原文（2026-09-04 外置，11 段）

[S:done][D:业]  ✅ **2026-09-01 15:11:06 本机本地（＝07:11:06Z）首触发成功送达，本行销号（Cowork 业务总线 `OP-0901-E`，Shao Peishen 亲手执行）** —— **⒀ 三次尝试的时间线本身就是判据**（全部本机直读 `reports/wecom_aibot_audit.jsonl` 实测，UTC）：`05:53:54Z` ❌ `annual_holiday_reminder_failed` → `06:39:56Z` ❌ 同前 → **`07:01:55Z` 李姣龙发来第一条入站消息**（`archived` → `group_notified` → `inbound_forwarded_to_paul`，路径完整） → `07:11:14Z` ✅ **`annual_holiday_reminder_sent`／`errcode=0`／`errmsg=ok`**，`07:11:20Z` 抄送 `annual_holiday_reminder_cc_paul`。状态文件已写入 `last_sent_at=2026-09-01T07:11:06Z`／`last_target_year=2027`。 🔑 **⒁ 本行 ⑼ 的新主假设由此坐实，不再是假设**：`846607` 的错误文案写的是 `aibot send msg frequency limit exceeded`，**真因却是 aibot 无法主动向从未与其建立会话的用户发起**——她一发消息，同一台机器、同一份代码、同一个收件人、同一个出口 IP，9 分钟后立刻发送成功。**⇒ 不是频控、不是时间窗、不是重连风暴**（三者均已被实测逐一排除，见 ⑺⑻⑼ 与 §四 #144）。 🔑 **⒂ 可复用判据（已同步写入 `0-学习与工具/取证方法知识库.md`）**：**「限流」类错误码的隐含前提是「你以前发得出去」；若对该对象的历史成功次数为 0，先怀疑的是从未获得过权限，而不是暂时被限。** 判别成本极低——数一下这个收件人历史上有没有成功过一次。本次实测：`2025672` 在全审计史上的成功次数是 **0**，而同期对 `ShaoPeiShen` 连发五次全部成功。 🔑 **⒃ 本行选 (a)「09-01 真发一次」的价值最终结算**：它买到的不是一封信，是**提前 12 个月暴露了一个会让整条机制在 2027-09-01 静默失效的真实缺陷**，且当时无人在场、无人会知道。**这条机制第一次运行就抓到了自己的致命伤——这正是当初否掉「直接定 2027」的全部理由。** **⒄ 遗留（不阻塞本行销号）**：`#380` 的 `tasks.md` 5.2／5.3 变更包归档待做，见 §一 #380。 ━━━ 待领（🔴 **硬前置 ＝ §一 #380** —— 白名单不通则本行发不出去，**且是静默发不出去**）━━━ ✅ **首触发口径已拍板：选 (a) —— 2026-09-01 照跑一次，不跳过（Shao Peishen 2026-08-23 答定夺 2）**。**内容改为「确认收到 2027 表 ＋ 预告明年此时提醒你出 2028 表」**，而不是索取一份她已经给过的东西（否则就在重犯 `财务部#14` 刚道过歉的那个错）。🔑 **选它的理由是「用一次真实发送证明通道是通的」** —— 若直接定 2027-09-01，这条机制将有 **12 个月零存活信号**，而本项目已反复证明：**一年后才第一次跑的定时任务，到时候多半是坏的、且没人会知道它坏了**。⇒ 09-01 那次同时是 §一 #380 白名单接入的真实验收。━━━ 2026-08-23 Cowork 业务总线 `OP-0823-D` 拆件立行；**Cowork 未建任何定时任务、未碰 `.51`**。 **并入审核（协议〇.10）**：#373 是「FI3 前置交付项」行、其节假日子项本次已交付并将销该子项——**把一条要一直活到 2027 年的机制挂在一条即将销号的交付行里，正是 §四 #91 ⑵ 刚记下的那个教训**（带日期的任务随行蒸发）；#339 为 FI3 定时行且行内明写到期前不得催办。故按例外取号新建。 　━━━　🔒 **Shao Peishen 2026-08-31 答《本周计划-2026-08-31》**：🔴 **B-1 答 (a)：今天合入 `#380` 并真机验，明天（2026-09-01）人工执行一次首触发。** ━━━ 🔴 **附加执行次序（他明示，硬要求）**：**本件（连同 `#380`）排在「我已回 Lan」之前** —— 理由是**本件有硬期 09-01、LAN 收口没有硬期，而两件都要他在场**；抢在前面的是有期限的那一件。⇒ 承接 opener ＝《本周计划-2026-08-31》A1（`OP-0831-A`）。 ━━━ **他同时复述了当初定 (a) 的理由，作为否掉 (c) 的论据**：「一年后才第一次跑的定时任务，到时多半是坏的、没人会知道」。 　━━━　🔒 **Shao Peishen 2026-08-31 定今日执行序列（明示）：A1 → 回 LAN 收口 → 清池。**　**本行随 `#380` 同为 A1，排在最前。** 　━━━　🔒 **2026-08-31 `OP-0831-A`（CC，同一 session）：#380 硬前置已解除，年度提醒机制（含存活自证）已建成，明日 09-01 等 Shao Peishen 人工执行首触发。** **⑴ #380 前置已清空**：ff 合入 master＋部署重启验活＋真机验（详见 §一 #380 本次追记）——她明天若回话，将走正常归档路径，不再被 fail-closed 静默挡回。 **⑵ 机制已建成**：新增 `aibot_service/annual_holiday_reminder.py`（纯逻辑层，21 条单测）＋`scripts/annual_holiday_reminder.py`（CLI，`--dry-run`/`--force`/`--message` 三开关）＋Windows 计划任务 `ZhuopinAnnualHolidayReminderDaily`（**每日 08:35 触发，非原生年度触发器**——脚本内部按本地日期门控是否真发；理由即本行原文「一年后才第一次跑的定时任务，到时候多半是坏的、且没人会知道它坏了」：每日触发使 `last_checked_at` 心跳每天可验证，不必等到明年 9 月才发现坏没坏）。已实测手动触发一次（今日非 9-1，仅刷心跳未发送，审计 `action=annual_holiday_reminder_heartbeat`，`LastTaskResult=0`）。 **⑶ 🔴 已预置状态防止明日 08:35 自动误发**：`annual_holiday_reminder_state.json` 已手工预置 `last_sent_year=2026`（附字段注明本条非真实发送）——若不预置，明日 08:35 自动路径会用默认泛化文案「请提供 2027 年度节假日日历更新」自动发出，与你已批准的特例文案冲突、重演财务部#14 的错误；预置后自动路径会判「今年已发过」而跳过，**不影响你随时手动执行**（`--force` 显式绕开该字段）。 **⑷ 明日你要跑的命令**（单行，在 `C:\Dev\zhuopin-ai` 下执行；文案按本行已拍板的「确认收到 2027 表＋预告 2028」措辞拟就，发送前会自动核验 #380 前置，未通过会中止不发并告警）：`python "5-平台底座\wecom-aibot-service\scripts\annual_holiday_reminder.py" --force --message "您好，已收到您提前提供的 2027 年度节假日日历，谢谢！本提醒机制自本次起生效，将于明年（2027）9 月 1 日提醒您提供 2028 年度节假日日历，届时请留意企业微信消息。"`。跑完后须在 `reports/wecom_aibot_audit.jsonl` 见 `action=annual_holiday_reminder_sent`／`errcode=0` 才算通（同 `#380` 原定判据，只看命令返回码不算数）。 **⑸ 全量回归**：aibot 服务 701 passed / 1 skipped（698 基线 ＋ 21 新增），零漂移。 ━━━ 🔴 **2026-09-01 首触发已由 Shao Peishen 亲手执行，但发送失败、消息未投递（Cowork 业务总线 `OP-0901-E` 全程陪跑并取证）** —— **⑴ dry-run 四项判据全绿**（2026-09-01 13:52:30 本机本地 ＝ 05:52:30Z）：收件人 `李姣龙（2025672）` 正确、文案与 Shao Peishen 已批准的特例一致、`#380` 前置「出站已知＝True，入站白名单＝True」双向皆通、`今年已发过=True` 与 `--force=True` 说明 08-31 预置的防误发字段正确生效并被显式绕开。 **⑵ 正式发送失败**（13:53:49 本地 ＝ 05:53:49Z）：WebSocket 建连与鉴权均成功，死在 reply ack —— `errcode=846607, errmsg=aibot send msg frequency limit exceeded`。 **⑶ 🔴 已确认未投递、重试安全（三条独立证据，非推断）**：① 审计落 `annual_holiday_reminder_failed` 且 `decision.sent=false`／`recipient=2025672`；② `reports/annual_holiday_reminder_state.json` 一字未改（`last_sent_year` 仍是 08-31 预置的 2026、`last_checked_at` 仍停在当日 08:35 那次自动心跳）；③ 失败发生在 ack 层而非投递后。**⇒ 她没收到，重试不会让她收到两条。** 🔑 **此点必须先坐实再谈重试**——否则重演 `财务部#14` 那类「发出撤不回」；判「失败了没有」和判「发出去没有」是两件事，`errcode≠0` 只回答前一件。 **⑷ 疑似成因（🔴 未证实，不得当结论）**：当日 aibot 链路重连风暴（`reconnecting` 28／`connection_error` 13／`outbox_relay_scan_failed` 16）＋当日已成功发出 3 条，共同吃掉发送配额 —— **目前只是时间相关性**，已另立 §四 #144 交机制线取证。 **⑸ ✅ Shao Peishen 2026-09-01 答 E-1 ＝ (a)：等 30~60 分钟原样重跑一次，不改代码、不动常驻服务。** 重跑判据同原定——须在 `reports/wecom_aibot_audit.jsonl` 见 `action=annual_holiday_reminder_sent`／`errcode=0` 才算通，**只看命令返回码不算数**。 🔴 **硬期风险如实写明**：本行首触发的硬期就是 2026-09-01 当日；**若重试仍失败，须由 Shao Peishen 定夺是否顺延**——顺延须重写文案（`09-01` 这个日期本身就是文案内容：「将于明年 9 月 1 日提醒您」），且会弱化当初选 (a) 的全部理由（用一次真实发送证明通道是通的）。 **本行不销号；`#380` 的 `tasks.md` 4.1／4.3 亦仍未闭合**（等她本人收到并回复）。 ━━━ 🔴 **2026-09-01 第二次重试同样失败，且我方原判据被实测推翻（Cowork 业务总线 `OP-0901-E`）** —— **⑹ 二次失败**：2026-09-01 14:39:56 本机本地（＝06:39:56Z），`errcode=846607` 同前，但 `hint` 不同（首次 `1788242028181470063542186`，二次 `1788244790181471519230413`）⇒ **两次独立的限流判定，不是同一次的回放**。审计再落一条 `annual_holiday_reminder_failed`／`sent: false`，状态文件仍一字未改 ⇒ **两次均未投递**。 **⑺ 🔴 我方「频控窗口已过、通道现在是通的」这条判据是错的，就地更正**：当时的依据是「同一 botid 在 14:22:42／14:29:21 本地各成功发出一条 `decision_reminder_sent`」。**错在核了通道、没核收件人这个变量** —— 实测当日全部 5 条成功发送（`open_pool_reminder_sent` 1 ＋ `decision_reminder_sent` 3 ＋ `open_pool_reminder_sent` 1）**收件人无一例外全是 `ShaoPeiShen`**。同族＝「下因果断言前先问有没有一个没核的变量能同样解释」。 **⑻ 🔴 更硬的一条实测**：全审计历史里 `2025672`（李姣龙）**总共只出现过 2 次，全是本行这两条 `annual_holiday_reminder_failed`；从未有过任何一条针对她的 `*_sent`** ⇒ **这个机器人对她从来没有成功发出过一条消息**，不存在「以前发得通、今天发不通」这回事。 **⑼ 新假设（🔴 未证实，交机制线，不得当结论）**：`846607` 的错误文案写的是 frequency limit，但更可能的成因是**企微 aibot 的 reply 语义要求先有用户侧消息、无法主动向从未与其建立会话的用户发起**。**排除项**：非 IP 级限流（同一出口 IP `61.160.125.186` 同期对 `ShaoPeiShen` 发送成功）；非时间窗（间隔 46 分钟的两次独立尝试同样被拒）。 🔑 **⑽ 由此得到的判据值得单独记：「白名单通了」≠「能发出去」** —— `#380` 前置校验的「出站已知＝True、入站白名单＝True」是**我方两张名单**，不是企微侧的许可；而 `#380` 的真机验当时走的是「向 `ShaoPeiShen` 发一条标记消息」的安全代理，其行内已如实写明「**不构成对李姣龙本人真实收发的验收**」、`tasks.md` 4.1／4.3 留步 ⇒ **那条留步今天被坐实为真实缺口，不是形式主义**。 🔑 **⑾ 本行首触发选 (a) 的价值今天全额兑现**：当初否掉「直接定 2027-09-01」的理由是「一年后才第一次跑的定时任务，到时候多半是坏的、且没人会知道它坏了」。**今天这次「失败」正是它要买的东西** —— 把一个本会到 2027-09-01 才暴露、届时无人在场也无人知晓的真实故障，提前 12 个月暴露出来。**这次失败是这条机制迄今最大的产出，不是这条机制的失败。** **⑿ 不再盲试第三次**：两次已排除「等时间」这一解释，再试只是多吃一次拒绝、且可能延长冷却。**下一步待 Shao Peishen 定夺**（通道改走群 webhook ／ 请她先发一条建立会话 ／ 顺延并先修通道）。 **本行不销号。**

---

# §一 #379 · 外置前整格原文（2026-09-04 外置，3 段，md5:cd1fc9db）

[S:done][D:业]  ✅ **2026-09-01 15:11:06 本机本地（＝07:11:06Z）首触发成功送达，本行销号（Cowork 业务总线 `OP-0901-E`，Shao Peishen 亲手执行）** —— **⒀ 三次尝试的时间线本身就是判据**（全部本机直读 `reports/wecom_aibot_audit.jsonl` 实测，UTC）：`05:53:54Z` ❌ `annual_holiday_reminder_failed` → `06:39:56Z` ❌ 同前 → **`07:01:55Z` 李姣龙发来第一条入站消息**（`archived` → `group_notified` → `inbound_forwarded_to_paul`，路径完整） → `07:11:14Z` ✅ **`annual_holiday_reminder_sent`／`errcode=0`／`errmsg=ok`**，`07:11:20Z` 抄送 `annual_holiday_reminder_cc_paul`。状态文件已写入 `last_sent_at=2026-09-01T07:11:06Z`／`last_target_year=2027`。 🔑 **⒁ 本行 ⑼ 的新主假设由此坐实，不再是假设**：`846607` 的错误文案写的是 `aibot send msg frequency limit exceeded`，**真因却是 aibot 无法主动向从未与其建立会话的用户发起**——她一发消息，同一台机器、同一份代码、同一个收件人、同一个出口 IP，9 分钟后立刻发送成功。**⇒ 不是频控、不是时间窗、不是重连风暴**（三者均已被实测逐一排除，见 ⑺⑻⑼ 与 §四 #144）。 🔑 **⒂ 可复用判据（已同步写入 `0-学习与工具/取证方法知识库.md`）**：**「限流」类错误码的隐含前提是「你以前发得出去」；若对该对象的历史成功次数为 0，先怀疑的是从未获得过权限，而不是暂时被限。** 判别成本极低——数一下这个收件人历史上有没有成功过一次。本次实测：`2025672` 在全审计史上的成功次数是 **0**，而同期对 `ShaoPeiShen` 连发五次全部成功。 🔑 **⒃ 本行选 (a)「09-01 真发一次」的价值最终结算**：它买到的不是一封信，是**提前 12 个月暴露了一个会让整条机制在 2027-09-01 静默失效的真实缺陷**，且当时无人在场、无人会知道。**这条机制第一次运行就抓到了自己的致命伤——这正是当初否掉「直接定 2027」的全部理由。** **⒄ 遗留（不阻塞本行销号）**：`#380` 的 `tasks.md` 5.2／5.3 变更包归档待做，见 §一 #380。 ━━━ 🔴 **2026-09-01 第二次重试同样失败，且我方原判据被实测推翻（Cowork 业务总线 `OP-0901-E`）** —— **⑹ 二次失败**：2026-09-01 14:39:56 本机本地（＝06:39:56Z），`errcode=846607` 同前，但 `hint` 不同（首次 `1788242028181470063542186`，二次 `1788244790181471519230413`）⇒ **两次独立的限流判定，不是同一次的回放**。审计再落一条 `annual_holiday_reminder_failed`／`sent: false`，状态文件仍一字未改 ⇒ **两次均未投递**。 **⑺ 🔴 我方「频控窗口已过、通道现在是通的」这条判据是错的，就地更正**：当时的依据是「同一 botid 在 14:22:42／14:29:21 本地各成功发出一条 `decision_reminder_sent`」。**错在核了通道、没核收件人这个变量** —— 实测当日全部 5 条成功发送（`open_pool_reminder_sent` 1 ＋ `decision_reminder_sent` 3 ＋ `open_pool_reminder_sent` 1）**收件人无一例外全是 `ShaoPeiShen`**。同族＝「下因果断言前先问有没有一个没核的变量能同样解释」。 **⑻ 🔴 更硬的一条实测**：全审计历史里 `2025672`（李姣龙）**总共只出现过 2 次，全是本行这两条 `annual_holiday_reminder_failed`；从未有过任何一条针对她的 `*_sent`** ⇒ **这个机器人对她从来没有成功发出过一条消息**，不存在「以前发得通、今天发不通」这回事。 **⑼ 新假设（🔴 未证实，交机制线，不得当结论）**：`846607` 的错误文案写的是 frequency limit，但更可能的成因是**企微 aibot 的 reply 语义要求先有用户侧消息、无法主动向从未与其建立会话的用户发起**。**排除项**：非 IP 级限流（同一出口 IP `61.160.125.186` 同期对 `ShaoPeiShen` 发送成功）；非时间窗（间隔 46 分钟的两次独立尝试同样被拒）。 🔑 **⑽ 由此得到的判据值得单独记：「白名单通了」≠「能发出去」** —— `#380` 前置校验的「出站已知＝True、入站白名单＝True」是**我方两张名单**，不是企微侧的许可；而 `#380` 的真机验当时走的是「向 `ShaoPeiShen` 发一条标记消息」的安全代理，其行内已如实写明「**不构成对李姣龙本人真实收发的验收**」、`tasks.md` 4.1／4.3 留步 ⇒ **那条留步今天被坐实为真实缺口，不是形式主义**。 🔑 **⑾ 本行首触发选 (a) 的价值今天全额兑现**：当初否掉「直接定 2027-09-01」的理由是「一年后才第一次跑的定时任务，到时候多半是坏的、且没人会知道它坏了」。**今天这次「失败」正是它要买的东西** —— 把一个本会到 2027-09-01 才暴露、届时无人在场也无人知晓的真实故障，提前 12 个月暴露出来。**这次失败是这条机制迄今最大的产出，不是这条机制的失败。** **⑿ 不再盲试第三次**：两次已排除「等时间」这一解释，再试只是多吃一次拒绝、且可能延长冷却。**下一步待 Shao Peishen 定夺**（通道改走群 webhook ／ 请她先发一条建立会话 ／ 顺延并先修通道）。 **本行不销号。** ━━━ 📎 历史回写 9 段已于 2026-09-04 外置（K2 口径，原文原样 md5:2b4bbf57）见 `1-转型规划/0-全景路线图/队列行日志/#379.md`；本格只留首 1 段＋末 1 段
