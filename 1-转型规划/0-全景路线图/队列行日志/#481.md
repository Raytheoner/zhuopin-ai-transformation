---
title: "队列行日志 · §一 #481（K2 外置）"
created: 2026-09-09
---

# §一 #481 · 历史回写段外置

> 外置 20 段（2026-09-09 07:51 `OP-0907-AL`，md5:ef2ebafe）；原文原样、可 grep；行内只留首段＋末段＋指针。

## 一次外置（2026-09-09 07:51）

━━━ 🔑 **propose 期实测三条（手段、命令、全部数字在该包 `tasks.md` §1，可现取重跑，本行只留结论）**：⑴ `7-外部文档/` 全部 68 份 docx 只读对照，19 份带勾选形态、**13 份**在 naive `python-docx` 视角下读成「一条都没勾」而 XML 里确有勾，另 1 份根本不是合法 zip ⇒「读不了」这条路径现网真实存在；⑵ 逐字复现 `#133` ⑴：`采购部#18` 回件 `tables[1]` 24 格 9 空串，同表 XML 恰有 9 个 `w14:checkbox`（勾 3），**空串格与复选框格一格不差**（⚠️ 更准是**勾选列**全空非全表全空——只记在包内，**不回改 `#133` 原文**）；⑶ 三份既有实现各有盲区：`md2word.read_checkboxes` 对 `质量部#11`（实勾 22 处、全为段落内裸 ☑）返回「勾 0」，`qda_prefill._read_docx` 丢 26%–71% 正文，`#446` 那份判据最全却**装在 aibot 服务包里、场景 import 不到**——这才是重复实现并存的**结构性成因**，落点选错则第四份还会长出来。

━━━ ❓ **design 审五点（选项与代价全文在该包 `design.md`，本行不复述）**：① 落点（荐平台底座 `shared_tools/doc_parser/`，实测零新增依赖）；🔴 **② 裸 ☑/☒ 给不给 `checked` 语义——本项无默认、须他明确答**（两选项错误方向**相反**，不答不会停在原地）；③ 「读取失败」判到多严（荐在返回类型上根除「空即没勾」＋漏读诊断）；④ 部件覆盖面（荐加页眉页脚脚注，现网零差异）；⑤ `md2word.read_checkboxes()` 去留（荐删——实测生产调用点为 0，本包**唯一一条真 one-in-one-out**；不可逆，仍请他点头）。

━━━ **下一步**：答完五点 ⇒ 回填 `design.md` 结论表与本行 ⇒ 另起 apply 派单件。

━━━ 登记豁免：release 报的 12 个脏文件全部在 acquire 之前就已脏，属并发 session 在办产出（B-0906_X 回写 JSON／#480 归档件／看护件 B-0907_E／跟进信件），本会话一件未碰、不认领。

━━━ 📎 原「已激活·可领」段与更早的 🛑 段全文保留于下，未删一字。

━━━ 🟢 **已激活·可领（2026-09-07，Cowork 业务总线 B-0907_E，机制类可动 WIP 现测 11/22 有余量，激活本行不满 22 上限；本次范围＝只到 propose＋design 起草，不 apply）。****原 🛑 段全文保留于下，未删一字。**

━━━ 🛑 **排队中·暂非可动（2026-09-03 立行时机制类可动 WIP 顶格，同 `#381`／`#439`／`#447`／`#478` 既例）。**

━━━ **立行依据** ＝ Shao Peishen 2026-09-03 §四 分诊消化裁决（`OP-0903-D`，他答「都按你说的」），批次 `B-0903_80`。

━━━ **rule-of-three 已触发 —— 已连栽三次**：`采购部#16`／`#17`（勾选找错地方）、`#18`（假阴性）、Cowork 侧漏段落内勾。🔴 **代价是对专员做出不实指摘**（说人家没勾，其实勾了）。

━━━ 🛑 **但本行刻意排队、不豁免 WIP**：更正信已于 2026-08-28 随 `采购部#20` 发出，**本项不在出血** ⇒ 可以等。

━━━ 📌 **`#133` ⑶ 的处置已定、不属本行**：§一 `#411` 那条已 `[S:done]` 的错误结论 **在行内标注更正、不销号重开** —— 照「历史记录不追改」，重开会制造第二份真相。

━━━ ✅ **design 审已过（2026-09-07，Shao Peishen 答合审材料 §3 (a)＝全部决策点按起草方推荐，含无默认点；材料＝`1-转型规划/0-全景路线图/合审材料-八包design与三项决策-2026-09-07.md`）**：包 `unified-docx-checkbox-reader` 在分支 `claude/op0907h-docx-reader-481` 上，apply 泳道开工第一步＝rebase 到 master 后在 `design.md` 顶部回填「design 审：✅ 已过 —— Shao Peishen 2026-09-07（合审 §3）」，再按 `tasks.md` 施工；ff 仍 🟡。

━━━ ✅ **apply 已完成（CC `OP-0907-AB`，2026-09-07，批 `B-0907_Y`）**：分支 `claude/op0907h-docx-reader-481`（rebase 到 master `3ff92a6` 后续建，**未新建分支、未 ff 合入 master**），已 push origin。`design.md` 顶部已回填「design 审：✅ 已过 —— Shao Peishen 2026-09-07（合审 §3）」，**design 正文一字未改**（拍板结论只落在审过行与 `tasks.md` 勾选状态）。`openspec validate unified-docx-checkbox-reader --strict` ＝ valid。

━━━ **产出 ＝ 平台底座 `zhuopin_platform/shared_tools/doc_parser/`（新子包，纯 stdlib、`pyproject.toml` 一行未改、零新增第三方依赖）**：`_xml.py`（OOXML 直读原语 ＋ 两种文本视图）／`checkbox.py`（**全项目勾选判据正本**，三态入口 `read_checkboxes()`）／`signals.py`（四类回件形态信号，判据自 `#446` 原样迁入、语义未改）／`text.py`（取文，覆盖表格与内容控件）。

━━━ 🔑 **三态在类型上根除「空即没勾」**：`ReadingStatus.READ_FAILED`／`NO_CARRIER`／`HAS_CARRIERS` 各有其值，且 `CheckboxReading.__bool__` **直接抛 TypeError** —— `if not reading:` 这行代码在本件上**写不出来**，不依赖任何人记得判据。裸 ☑/☒ 按决策点②(b) 给 `checked` 语义、标 `source="char"`、**与控件分列不合并**。部件覆盖面按④(b) 扩到 `header*`／`footer*`／`footnotes`／`endnotes`。

━━━ **迁移三处调用点（期望产出 ③）**：⑴ `aibot_service/reply_form_detection.py` 改**薄壳委托**（只剩 docstring ＋ re-export，零判据）；⑵ `qda_prefill/doc_reader._read_docx` 改走统一件取文；⑶ `md2word.read_checkboxes()` 按⑤(a) **已删除**，`test_md2word.py` 三例改调统一件做写侧验收（`md2word.py` 本身仍零平台依赖）。全仓复查：生产码里的读勾选判据**只剩统一件一处**。

━━━ 🔬 **实测（手段、命令、数字全在包内 `tasks.md`，可现取重跑）**：⑴ **CLI 输出逐字不变已用哈希证死** —— `check_reply_form_signals.py` 对 `7-外部文档/` **68 份**合法 docx，迁移前后落盘各 **129,694 字节、SHA256 同为 `F3FCE3AECE78D00A0471EF66431295A251A765341D3221F6D157EA074A60826F`**，CLI 源码一行未改；⑵ **模拟高层视图已被真 python-docx 逐格证伪**（tasks 2.4 硬前置）——`采购部` 08-26 回件三张表 **116 格逐字相等**，`tables[1]` **真 24 格 9 空／模拟 24 格 9 空**，与 1.3 录得的真值一格不差；统一件对该件读出「控件 9 个（勾 3）」＋ 一条漏读诊断「高层视图只看得见 0 个、XML 实有 9 个」；⑶ **现网 69 份语料回归重跑**（apply 期独立重跑，未引用 propose 期数字，对照脚本自带判据、不 import 统一件任何探测器）：读取失败 1 份（`创建盘点差异单.docx` 非法 zip）、带勾选载体 19 份、**统一件 vs 独立 XML 直读不一致 ＝ 0 份**。

━━━ **回归零漂移（本地实跑，不以 CI 为准）**：平台 **528 → 551 passed / 1 skipped**（＋23 全为本包新测）｜ aibot 服务 **795 passed / 1 skipped**（前后相等）｜ QD-A **41 passed**（前后相等）｜ md2word **16 passed**（前后相等）。`git status --porcelain` ＝ 12 M ＋ 3 ??，三个 `??` 全是本包有意新增（两份单测＋`doc_parser/` 子包），**未出现任何新文件名形态**；`git check-ignore -v` 对新增源码与单测无输出（一条都没被忽略）。引导样板 lint：本包新增四处**零违规**。

━━━ ⚠️ **如实登记三处连带改动与一处行为变化** —— ⑴ **QD-A 原先完全不依赖平台底座**，本次起要 import ⇒ `pyproject.toml` 加 `zhuopin_platform`，并按 `#345` 唯一样板给 `tests/conftest.py`＋`scripts/run_prefill.py`＋`scripts/run_calibration.py` 补引导（不补则 41 例里 15 例 `ModuleNotFoundError`，本次已实撞过一次）；⑵ `test_md2word.py` 同样补引导（**是测试 import 平台包，`md2word.py` 本身不**）；⑶ 🔴 **非法 docx 的 CLI 行为变了、是改善不是退步**：`zipfile.BadZipFile` 继承自 `Exception`、**不是** `ValueError`／`OSError`，CLI 那句 `except (ValueError, OSError)` 从来接不住 ⇒ 迁移前遇到那份文件**整批崩掉**（traceback、退出码 1、其后按字典序排在它之后的文件一份都没被分析），与该 CLI docstring 自己写的「退出码 2 ＝ 至少一个路径不是合法 docx，这类失败不得被静默吞掉」正相反；迁移后 `DocxReadError` **刻意继承 `ValueError`** ⇒ 同一份文件输出 `[读取失败] …` ＋ 退出码 **2**，与 docstring 一致。⑷ 4.5 复算 naive 假阴性 ＝ **11 份**，propose 期 1.2 记 13 份 —— **口径不同（本次只数「naive 读到勾 0 而 XML 有勾」，1.2 含未勾 ☐ 也被漏读的那几份），两个数字并记、不回改 1.2**。

━━━ 🔴 **本会话刻意未做（派单件限定，非遗漏）**：① **未 ff 合入 master**（🟡，交总线）；② **未写任何队列文件**（派单件明写「不碰主仓工作区、不在主仓 commit」，队列真身在主 checkout）；③ **未碰 §一 `#411`**（`#133` ⑶ 已定：行内标注更正、不销号重开）；④ **未回改 `#133` 正文**（1.3 的更准措辞与 4.5／1.2 的口径差只记在包内）；⑤ **变更包未归档**（`暂不归档` —— 待 §四 `#133` 指针与本行回写落地后由看护者执行 `/opsx:archive`）。
