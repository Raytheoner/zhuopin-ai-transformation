# 队列 §一 #503 · 行日志（K2 外置件）

> 本件收纳该行状态格外置出来的历史回写段，原文原样、未改写；行内只留首段＋末段＋指针。


## 2026-09-08 K2 外置（CC 看护者 `OP-0908-T`，批 `B-0908_四泳道机制根治`）

外置原因：本批 openspec 起草结论与 Shao Peishen 答 1(a) 并入后该行状态格达 4292B、超 `ROW_LENGTH_CAP_BYTES` 4096 B。以下为原状态格中间段，原文原样；md5:`0a7186e7bd03029946f634489d92d333`。

✅ **2026-09-08 解冻**（Cowork 业务总线 `OP-0908-A`）：解冻条件「可动 WIP 回到 22 以内**且摘 🛑 后仍 ≤22**」**已满足** —— Shao Peishen 当日答 3a 授权后，`#358`／`#436`／`#447`／`#454`／`#478` 五条据实由 `open`／`partial` 改判 `blocked`，**机制类可动 WIP 由 22/22 降至 17**（手段＝用 `工具-共享文档编辑锁.py` 自身的 `_section_bounds` ＋ `_count_mechanism_wip` 对两份队列真身实测：机制环境 17／业务场景 0，降级项 0，非会话自算）；本行与 `#503` 同批摘 🛑 后为 19 ≤ 22。🔴 **未关闭任何既有机制行、未自授 `--force-mechanism-wip`**——腾余量靠的是把失真的记账改回真身，不是牺牲别的行。

---

# §一 #503 · 外置段（2026-09-19 外置，工具第 1 批，1 段，原文原样）

> 外置人 OP-0919-P；外置前整格 5628 B、共 4 段；本批外置第 3 段；本格保留第 1、4 段。

✅ **2026-09-08 openspec 起草完成、止步 design 审**（CC `OP-0908-P`，泳道 `503-opener-single`，批 `B-0908_四泳道机制根治`）：产出＝`openspec/changes/single-guarded-copy/`（proposal／design／tasks／spec／同族普查清单，6 文件 431 行全新增），分支 `claude/op0908p-opener-single` @ `a5ad0ce`，已合入 master（看护者串行 ff，master `fb0cb43` 链内 commit `f00dbe9`）。`openspec validate single-guarded-copy --strict` ⇒ `Change 'single-guarded-copy' is valid`。🔑 **候选 ⒞ 被收窄为「消灭第二次创作，不是第二份字节」** —— 纯 ⒞（对话里只给路径）与 Shao Peishen 2026-09-08 退回 `OP-0908-I` 削块那次的裁定正面冲突（接力卡 §五：引用版省的是做什么／不做什么，不是六字段）。改法落在 `工具-opener生成.py` 今天只 `print()` 到 stdout、不落任何文件这一点（`:559` 实读）：加 `--out`，一次调用内落盘→回读→出块，块内盖出件指纹，lint 加形态⑧ 守「这份是不是生成器出的」。🔬 **第一人称实证（立行时没有）**：本批看护者给各泳道 prompt 加的「环境适配」七条约束，在落库看护件里出现 0 次；其中一条与落库块正面冲突（落库块写 `worktree：☑ 新建`，实发 prompt 写「不要再建 worktree」）。**落库那份通过了全部七个形态检查，而它是错的那一份** —— 本命题被本批自身现场复现一次。同族普查 10 条（含 2 条对照组证明判据非恒真），发现两个正面先例：哨兵 hook 的指向式安装、`工具-泳道纪律复制lint.py`（指向而非复制，判据词运行时从正本提取）。**三个 design 决策点**：① 未传 `--out` 时 fail-loud 还是警告放行；② 指纹放块内末行／块外／sidecar 台账（实测 `reports/` 被 `.gitignore:50:**/reports/` 整目录忽略，sidecar 会不可见）；③ 形态⑧ 存量处置＋release 射程自陈落哪。 ✅ **Shao Peishen 2026-09-08 答 1(a)：design 放行、按起草方推荐取值**（CC 看护者 `OP-0908-T` 就地回填，批 `B-0908_四泳道机制根治`）。🔴 **放行的是 design、不是实现** —— 后续 apply 另起 session，本行不因放行而销号。

> 以下 1 条为本批并入新指针、自本格移除的旧指针段原文（第 2 段）：

📎 **历史回写段已于 2026-09-08 外置**（K2 口径，原文原样、可检索，md5:`0a7186e7bd03029946f634489d92d333`）见 `1-转型规划/0-全景路线图/队列行日志/#503.md`；本格只留首段＋末段＋指针，任何细节以外置件为准。
