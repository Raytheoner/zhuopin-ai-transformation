---
title: "队列行日志 #570（§一）"
created: 2026-09-13
status: 生效
用途: 跨桌任务队列 §一 #570 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #570 · 外置段（2026-09-13 外置，工具第 1 批，单段切分，原文原样）

> 外置人 CC-OP-0913-G；外置前整格 4446 B、单段无 `━━━`；切在「粗体小标题前」边界，行内留切点前 3203 B、以下为切点后 1243 B。🔴 **以下正文直接接在行内结尾之后**（行内末「… 0（那是「没起来」不是「过期」，不代为启动）。」→ 本批首「**手段**：新单测 `0-学习与工具/test…」），不是另一段。

**手段**：新单测 `0-学习与工具/test_工具-执行体对齐重启.py` 7 例（新鲜／过期干跑 18／OFF 三态只告警不动进程／ON 走停服关退 17／无进程／进程起于提交后 ff 前按 reflog 仍判过期／落后>0 回归），临时 git 仓＋`worktree add --detach`＋命令行含 worktree 路径的 `Start-Sleep` 充当常驻进程，`powershell.exe` 5.1 实跑，`python -m pytest 0-学习与工具/test_工具-执行体对齐重启.py -q` ⇒ `7 passed in 75.61s`；sweep 侧回归 `python -m pytest 0-学习与工具/test_工具-落库sweep.py -q -k "carrier or 执行体 or restart"` ⇒ `47 passed, 438 deselected`（**未改 sweep 一字节**，`git diff --stat` 只 1 文件＋1 新增）。对真实执行体只跑过 `-DryRun`（只读，现取落后 3 ⇒ 走原 ff 路径、退出 0，2b 未触发），**本棒未重启任何常驻服务**。⏭️ **留待下棒**（本行期望产出前半）：sweep／ff 巡检判定 ff 触碰常驻执行体代码路径即自动 `-RestartOnly`——等 `#556`② 分支 `claude/op0913a-556-sweep-visibility` ff 后另派；届时 sweep 侧「aligned」分支同样只看 git 不看进程，可直接复用本脚本 `-DryRun` 的退出码 18 作为判据。

---

# §一 #570 · 外置段（2026-09-19 外置，工具第 2 批，1 段，原文原样）

> 外置人 Cowork-泳道看护-OP-0919-K；外置前整格 4229 B、共 3 段；本批外置第 2 段；本格保留第 1、3 段。

**WIP豁免：出血**（ff 与进程之间无机器，已实咬一口；且官方量具在断口上报绿）。2026-09-12 `OP-0912-E` 立行（Shao Peishen 当日拍：两个缺口现在就立）。**本行只立行取证＋已补跑一次重启，未改任何脚本。** 　🔎 **本棒范围限定（2026-09-13 08:0x 本地 Get-Date，Cowork 环境总线 `OP-0913-E` 派 `OP-0913-G`，批 `B-0913_人守打通`）**：🔴 **只改 `0-学习与工具/工具-执行体对齐重启.ps1` 与其单测，不碰 `0-学习与工具/工具-落库sweep.py` 一个字节**——`#556`② 的抢救分支 `claude/op0913a-556-sweep-visibility`（`751495e`，未 ff）在该文件上有 **+194 行**未合改动，同批动它必撞；期望产出里「sweep／ff 巡检判定触碰常驻执行体代码路径即自动 `-RestartOnly`」那半**留到该分支 ff 之后另派**。 **本棒交付＝期望产出「顺带修量具」那一条的正体**：第 2 关现在只算落后数（`rev-list --count <targetHead>..master`），为 0 即报「已对齐（落后 0），无需处置」并退出，**完全不看进程起于何时**。改为落后数为 0 时**再判一道进程新鲜度**——常驻进程 `StartTime` 对比 `git log -1 --format=%cI <targetHead>` 的提交时刻，进程早于代码落库时刻 ⇒ 判「代码已对齐、执行体过期」，走既有 `-RestartOnly` 路径。⚠️ 自动重启仍受期望产出末句约束（什么条件下允许自动、什么条件下只告警），**不得一刀切**；本棒不真重启任何常驻服务。 **今日实证**：listener 起于 13:10:20Z，`#556` 节流修复落 master 在 13:11:10Z，**晚 50 秒**，工具照报绿。 　🔴 **本棒收工（2026-09-13 07:5x 本地 Get-Date，CC 无头泳道 `OP-0913-G`，批 `B-0913_人守打通`）**：「顺带修量具」那条已建成，落分支 `claude/op0913g-align-proc-freshness-570`（commit `f6c8ddc`，已 push 分支，**未 ff master**——按 P4 由看护者收工串行 ff 或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 做）。**改了什么**：`0-学习与工具/工具-执行体对齐重启.ps1` 第 2 关落后 0 时新增 **2b 进程新鲜度**——常驻进程 `CreationDate` 对比**代码落库时刻＝max(`git log -1 --format=%cI <targetHead>` 提交时刻, 该 worktree HEAD 最近一次 reflog 移动时刻)**（取 reflog 是因提交时刻只是下界：进程若起于「提交之后、ff 之前」只比提交时刻会误判新鲜），任一在跑进程更早 ⇒ 判「代码已对齐、执行体过期」。**分流不一刀切**（本行末句约束）：`-DryRun` ⇒ 只报告、**退出码 18**（不再是 0，量具不得报绿）；实跑且 `.env` `CARRIER_AUTO_RESTART_ENABLED`＝ON ⇒ 走既有 `-RestartOnly` 路径（停服→启动→验重启→验活）；OFF／缺失（缺省）⇒ 只告警、退出码 18、打印处置命令——开关与 sweep `_carrier_auto_restart_enabled` **同一把、同一解析**（读不到即 OFF），不新造第二套。落后 0 且无在跑进程 ⇒ 明写「无在跑进程」退出 0（那是「没起来」不是「过期」，不代为启动）。
