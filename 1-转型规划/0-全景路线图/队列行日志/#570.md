---
title: "队列行日志 #570（§一）"
created: 2026-09-13
status: 生效
用途: 跨桌任务队列 §一 #570 的状态格历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋指针。
生成: 0-学习与工具/工具-队列行K2外置.py
---

---

# §一 #570 · 外置段（2026-09-13 外置，工具第 1 批，单段切分，原文原样）

> 外置人 CC-OP-0913-G；外置前整格 4446 B、单段无 `━━━`；切在「粗体小标题前」边界，行内留切点前 3203 B、以下为切点后 1243 B。🔴 **以下正文直接接在行内结尾之后**（行内末「… 0（那是「没起来」不是「过期」，不代为启动）。」→ 本批首「**手段**：新单测 `0-学习与工具/test…」），不是另一段。

**手段**：新单测 `0-学习与工具/test_工具-执行体对齐重启.py` 7 例（新鲜／过期干跑 18／OFF 三态只告警不动进程／ON 走停服关退 17／无进程／进程起于提交后 ff 前按 reflog 仍判过期／落后>0 回归），临时 git 仓＋`worktree add --detach`＋命令行含 worktree 路径的 `Start-Sleep` 充当常驻进程，`powershell.exe` 5.1 实跑，`python -m pytest 0-学习与工具/test_工具-执行体对齐重启.py -q` ⇒ `7 passed in 75.61s`；sweep 侧回归 `python -m pytest 0-学习与工具/test_工具-落库sweep.py -q -k "carrier or 执行体 or restart"` ⇒ `47 passed, 438 deselected`（**未改 sweep 一字节**，`git diff --stat` 只 1 文件＋1 新增）。对真实执行体只跑过 `-DryRun`（只读，现取落后 3 ⇒ 走原 ff 路径、退出 0，2b 未触发），**本棒未重启任何常驻服务**。⏭️ **留待下棒**（本行期望产出前半）：sweep／ff 巡检判定 ff 触碰常驻执行体代码路径即自动 `-RestartOnly`——等 `#556`② 分支 `claude/op0913a-556-sweep-visibility` ff 后另派；届时 sweep 侧「aligned」分支同样只看 git 不看进程，可直接复用本脚本 `-DryRun` 的退出码 18 作为判据。
