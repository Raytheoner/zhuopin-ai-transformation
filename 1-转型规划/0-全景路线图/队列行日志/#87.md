---
title: "队列行日志 #87（§四）"
created: 2026-09-04
status: 生效
用途: 跨桌任务队列-机制环境.md §四 #87 的事项列历史回写段外置件（K2 口径：单格 ≤4 KB，超限外置；原文原样、可 grep）。行内只留首段＋末段＋本指针。
---

# §四 #87 · 外置前整格原文（2026-09-04 外置，55 段）

✅ **⑻ `OP-0823-F` 已收口并合入 master（2026-08-23，CC，commit `b4580fd`，分支 `claude/op0823f-auto-archive-43fe6b`，变更包 `auto-archive-substantive-complete`）—— 范围由「自动归档」改为「只修告警判据」（Shao Peishen 当日拍板 (b)）** ━━━ ⚠️ **本条与上面 ⑺ 判据三不重复**：⑺ 已完整收录 F 对 §3.1ter 的三处更正与「真身只覆盖当天恰有的形态」那条方法教训，此处不复述，只写 ⑺ 没有的部分。 ━━━ 🔑 **⑴ 派单件 §一 的立论「归档做不了」不成立** —— 它断言「这不是人忘了归档，是判据死锁 ⇒ 做不了」。实测 `openspec/changes/archive/` 下 **50 个已归档包，39 个归档时是 N/N**，做法是**先把那条 archive task 自己勾上、再跑 archive**，最近三例都在 48 小时内。⇒ **归档一直在发生，人做得对，做了 50 次。** 本件的价值因此从「解锁一件做不了的事」降为「省 50 次手工往返」，而代价（不可逆目录移动）没变 —— 这是范围改判 (b) 的第一条理由。 ━━━ 🔴 **⑵ 判据对生产真身首跑，3 命中 3 个都不该动（⑺ 未记，这是范围改判的决定性证据）** —— 判出的三个「实质完工」：`editlock-hold-scope-and-wip-block` 33/34、`sweep-startup-nonblocking` 25/26、以及派单件称为「死锁纯样本」的 `sweep-ops-webhook-cutover` 30/31。**三个包那条未勾的 archive 行上，作者都写着「本次不做／本轮不做，前置条件未满足」** —— 正是本行 ⑹ 已经说穿的形态（「都把理由写了……只是没写在机器认得的地方」），**只是没有人把它代入那个判据去看一眼**。🔴 **若当天按原方案开了自动执行，第一轮就会永久移走两到三个未完工的包，而 archive 不可逆、移完之后没有任何机制会告诉我们移错了。** ⇒ 据此加了一道形态判别 `carries_human_note()`：**不解析那些字是什么意思，只判断「这里有没有人留了话」**（避开本行 ⑹ 已明确否掉的「认自然语言」那条路）；光秃的 archive 行放行，带说明或带缩进子项的不放行。 ━━━ 🔑 **由 ⑴⑵ 得到的两条判断纪律：** **① 「机制比人靠谱」的前提是机制判得比人准。** 本例机器 0/3、人 39/50 且没出过错 ⇒ **前提不成立时，正确动作是把机制降级为「给人分类、让人判」，而不是硬上。** 这不是否定该原则，是它的适用边界。 **② 「不再互斥」不等于有价值**（Shao Peishen 定夺 5 补）—— 判据补 N/N 覆盖后，§四 治本与判据已不互斥，**但障碍消失不产生收益**：一个动作被否决时常同时有「它有害」与「它没用」两类理由，**解除前者不产生后者**。⇒ 治本维持撤销（(b) 下它买不到东西，且净增一条只能靠人记的规则；原定落点 `openspec/templates/` 实测无 tasks 模板）；**若日后范围回到 (a) 值得重评，已登记 design ④，不算永久否决。** ━━━ 🔴 **⑺ 判据二正好指向本次交付自身，如实自陈**：(b) 下**归档仍是一个「人得记得做」的动作** ⇒ 按判据二「剩一个就还没闭环」，**本次交付按该判据不算闭环**。**这是明知的取舍，不是遗漏。** ⇒ **判据二须补一条边界**：**当「那件事」不可逆、且机器判得比人差时，「让它不再需要人做对」会把一个偶发的人为疏忽换成一个系统性的机器错误** —— 此时正确解法是**缩小机器的职责（判类、给出口），而不是扩大它的权限**。 ━━━ 📌 **对 ⑺「操作纪律」一处的更正**：⑺ 说「两条 CC 已按旧规格做完前段工作 ⇒ 由『补进去』变成『返工』」—— **F 侧不成立，没有返工**：判定内核是 (a)(b) 两方案共用件，§3.1ter 是**加法**（一个 `is_substantively_complete()` ＋ 一处 `alert_class` 映射 ＋ 6 条单测），约半小时。⚠️ **但这不削弱判据一** —— 反而值得注意：**判据一能在出件时发现「有洞」，发现不了「洞有多大」。**「治本后会失效」是逻辑推演、出件时就该做；「它今天就在漏、39/50」要去数一遍 archive 目录才知道。**两者都需要。** ━━━ **⇒ 实际交付（不含任何不可逆操作）**：① `0-学习与工具/工具-变更包自动归档.py` —— **只判定、不归档**（文件名保留不改，已被 commit／本行／派单件引用，改名即让指针失效；以文件头注释为准）；② sweep 滞留告警**分四类各自措辞**，「疑似遗忘归档」只留给真遗忘 —— N/N 与「未勾项只剩 archive 且无人留话」＝真遗忘；「有人留话」＝作者已写明理由但未用机器认得的入口（**告警正文连同三条入口一并印出**，因为只说「你没声明」而不给声明方式，等于把缺口原样留着）；「尚有 N 条真未完项」＝它没完工；「判不了完工」＝无 tasks.md。③ **本行 ⑹ 的 ⑶ 已落地**：`专线opener模板库.md` **新增 §〇.16**（三入口对照表＋成因＋「不得靠认自然语言绕过」＋「archive 不写进 tasks」已撤销的记载），根 `CLAUDE.md` §5 一行指针（净增 517 B，进度段 lint 通过）。 **实测复现**：用 08-23 那 4 个被报的包做端到端夹具，**旧措辞对 4 个全部不再成立**（4 报 3 误 ⇒ 0 误用）。判定器加载失败时从低取值：退回原措辞＋记日志，不抛、不中断 sweep（已配单测）。**回归 652 passed ＋ 51 subtests 零失败**（含 sweep 既有 196＋9）；`openspec validate --all --strict` 101 passed／1 failed，唯一失败为 master 既有的 `sc2-weekly-report-mvp`，**零新增**。 ━━━ ⚠️ **本行 ⑴⑵ 仍挂着，未做**：⑴ 对 `sweep-startup-nonblocking`／`editlock-hold-scope-and-wip-block` 执行 `--ack-stale-change`（**现在有了新的执行方式**：它们会被新告警明确标为「未用机器认得的入口」并附上入口清单，可等它自己催，也可现在就 ack）；⑵ 补 `open-pool` 的 5.1／5.2 后归档（实测现为 21/23，未勾＝`4.5 真实推送待确认` 与 `5.3 archive`，新判据判它「尚有 1 条真未完项」、不再叫遗忘归档）。**⑸ index.lock 预检本次未动**（本次只改同文件的告警渲染段，未触碰 `git add` 路径，不硬凑同车）。 ━━━ ⚠️ **一个不在范围内、但会再咬人的实物**：CC 的 worktree 里出现了 **2 个主工作区根本不存在的空「幽灵变更包」目录**（0 文件 0 tracked），系 `git merge` 拉入他人归档提交后**删了文件、留下空目录**所致，`openspec validate --all` 会把它们当真包并判失败。**主工作区 `git pull` 后同样会长出来。** 本次已清 worktree 那两个；**清理主工作区侧不属本变更范围，登记备查。** ━━━ 原文：🆕 **新增 ⑺：两条可机器化的规格自检判据（2026-08-23 追记，`OP-0823-B`；Shao Peishen 追问「既然查出根因，方案自洽了吗、闭环了吗」后由 Cowork 自查得出）** ━━━ **背景**：当日出的两份派单件（`OP-0823-F` 自动归档／`OP-0823-G` 载体健康守卫）**各含一个自洽性缺陷，均非新信息带出，而是出件时就该发现的**；直到他追问才暴露，且暴露时两条 CC 已按旧规格做完前段工作 ⇒ **由「补进去」变成「返工」**。 ━━━ 🔴 **判据一 · 方案是否自我拆台（可做成 design 审检查项）**：**凡一份方案同时包含「改现状的治本动作」与「按现状判定的判据」，必须验证治本生效后判据仍成立。** **实例**：`F` 的治本是「新包 tasks 不再写 archive 条目」，而其判据是「未勾项全部是 archive 类」⇒ **治本一生效，新包完工即 N/N，判据永远不命中，机制对增量彻底失效**；更直接的证据是 §3.1 第 1 条原文写着「未勾数为 0 ⇒ **跳过**」，**与同一份文件里的治本目标正相反，两段隔不到 60 行**。**⇒ 机器化落点建议**：design 审加一条——列出方案中所有「判据依赖的现状特征」，逐条问「本方案自己会不会改掉它」。 ━━━ 🔴 **判据二 · 解法里还剩几个「人得记得做」的动作**：**剩一个，就还没闭环。** **实例**：`G` 原建议的 c 档「检测＋提醒＋人工对齐」，**只是把触发人从『自己想起来』换成『被机器提醒』，对齐本身仍是一次性动作**，而根因是「落后是持续过程，约 70 提交/天」⇒ `#68` 的历史必然重演。**⇒ 该判据同族于当日反复出现的形态：给解法时停在「让人更容易做对」，而不是「让这件事不再需要人做对」，而 Shao Peishen 当日原则原话是「能建机制决不妥协，比人靠谱」。** ━━━ 🔴 **判据三 · 「拿真身跑过一遍」不等于覆盖完整 —— 真身只覆盖它当天恰好有的形态（2026-08-23 由 `OP-0823-F` 复核 §3.1ter 时发现，本条与判据一、二不同类：前两条管「方案写得对不对」，本条管「你怎么知道它对」）** ━━━ **F 的原话**：判据、单测、**以及对生产真身跑的 dry-run，三样全都通过了** —— 因为它们**全都只测了「有未勾项」的那个世界**；当天 17 个在途包里**恰好一个 N/N 都没有**，所以真身实测也照不出这个洞。 ━━━ ⚠️ **它同时纠正了 Cowork 在 §3.1ter 里的一处实质错误**：Cowork 论证该洞是「治本之后的未来形态」，**F 实测 50 个已归档包中 39 个在归档时是 N/N，且任何包从『勾完最后一条』到『跑完 archive』之间必然经过 N/N** ⇒ **它是今天的主流形态，不是未来的**；Cowork 低估了严重性。**另两处纠正**：失效形态不是「新包没人归档」（(b) 方案本无自动归档），而是「**N/N 落进 incomplete 分支、被告警明确告知『它没完工』⇒ 最该喊的那一类被主动排除，比不报更糟**」；`no-tasks` 类同坐该分支、同样印「尚有 0 条真未完项」，已拆独立类。 ━━━ **⇒ 可执行判据**：**验证一个判据时，不要问「我跑过真身了吗」，要问「这个判据的每个分支，我都造过样本吗」** —— 实测能防「我以为」，**防不了「样本里恰好没有」**。**⇒ 机器化落点建议**：单测须按**判据分支**枚举（而非按已知用例），design 审加一条「列出判据的全部输出分支，逐条指出对应单测」。 ━━━ ⚠️ **附带一条操作纪律（非机器化，但代价明确）**：**凡说「还来得及改／补丁进得去」，先核对方进度，不要凭印象** —— 当日 Cowork 说「F 和 G 都还在跑，补丁进得去」时未核，实测两者产物均不在 master（在各自分支），而它们其实已按旧规格完成前段 ⇒ **「还在跑」与「已跑完前面的任务」在报告方看来一样，但它决定的是「补进去」还是「返工」。** ━━━ 原文：🔴 **新增 ⑹「疑似遗忘归档」告警对三个守纪律的包误报 —— 而根因不是判据坏了，是三条现成的降噪入口一条都没人走（2026-08-23 追记，`OP-0823-B`，Shao Peishen 指示「4 个遗忘归档的问题也要查一下彻底解决」）** ━━━ 🔴 **先纠正我自己上一版给他的两个判断，都是从日志反推、没查真身** —— **⑴ 我说「已经报了 3 天、天天在报」是错的**：日志每轮全量记 4 条，**而推送侧只报新增**（`_track_and_alert_standing_state` 状态机，`STALE_CHANGE_ALERT_INTERVAL_HOURS=24`）；他的企微截图（08-23 11:37）实际只推了 **2 个**（`editlock` 97%／`open-pool` 83%），另两个此前已报过即静默。**⑵ 我提的「改判据」修法是多余的** —— 机制早已能区分「留置」与「遗忘」。**⇒ 今天第二次犯同型错误**（第一次是从 sweep 日志两条时间戳反推运行间隔，推出「12:26」，真值是 PT1H／12:36，且那一轮其实已经跑完）。**判据：凡「这个机制多久跑一次／报过几次／报给谁」这类问题，先找它的权威来源（计划任务 `NextRunTime`／状态文件／代码常量），日志是副产品、能反推出一个看起来合理的数，那正是它危险的地方。** ━━━ ✅ **机制侧实测：已有三条降噪路径，互不相同、设计文档写得很清楚** —— ⑴ **`STALE_CHANGE_DEFER_MARKER = "暂不归档"`**（文本标记，**变更作者对未来的永久声明**，扫 proposal／design／tasks 任一处）；⑵ **`预期观察窗口：N 天`**（队列 #314② 2026-08-09 拍板 (c) 所建，命中即报「🔭 观察中（已等 X 天／窗口 N 天）」、**不计异常**，超窗才升级）；⑶ **`--ack-stale-change <包名> --note <依据>`**（队列 #308 子项 D2，**复核者对过去某一刻的确认**，带 `done/total` 指纹，**tasks 一有新勾选即自动失效**，`--note` 空则拒绝）。 ━━━ 🔴 **实测：四个被报的包，`暂不归档` 命中 0／4；而唯一没被报成「遗忘」的 `queue-status-machine-field` 正是因为它写了「预期观察窗口：14 天」并附了估算依据** ⇒ **机制是好使的，现成正反例就摆在同一轮告警里。** ━━━ 🔑 **真正的根因，且它是本项目老熟人的镜像版**：那三个包**都把理由写了**，写在 tasks 未勾项的行内（「🔴 本次不做，前置条件未满足，如实登记不假装完工」「本次不执行，如实留置」「须等 ⑨ 真实拒绝过一次…之后再评估」）——**写得比机制要求的还详细，只是没写在机器认得的地方。** **⇒ 与「规则只活在报告文本里、没有承接行」是同一个病的镜像：那边是有承接没规则，这边是有理由没声明。** ⚠️ **且不该把机制放宽去认自然语言** —— 「本次不做」这种话随手就能写，模糊匹配会让降噪变成默认；**要求一个特定字符串正是它有价值的地方**。缺的是**让写 tasks 的人知道有这三个入口**，而这一条目前**不在任何一份收工纪律里**（实测 `CLAUDE.md` §5／opener 模板库 §〇 对「暂不归档」「预期观察窗口」「ack-stale-change」三个词均 0 命中）。 ━━━ **⇒ 四路处置，逐包分开（不是一刀切降噪）**：**① `sweep-startup-nonblocking` 25/28（等真实外网中断自然发生，实测 2.8% 频次）＋ ② `editlock-hold-scope-and-wip-block` 33/34（等 ⑨ 真实拒绝一次、逃生阀真实用过一次）** ⇒ 两者都在等自然样本、非人力可推，**走 ⑶ `--ack-stale-change`**（我是复核者不是作者，按代码注释这正是该用的那个入口；它自带指纹、tasks 一动即失效，比替作者写永久声明稳妥）；**③ `sweep-ops-webhook-cutover` 29/31 ⇒ 🔴 不得降噪**，它等的是 Shao Peishen 本人去采购内部工作群看一眼「有没有不再收到」（5.2 负判据，机器侧四条证据已穷尽、这半边我读不到）——**它正需要被催**；**④ `open-pool-reminder-dual-file-and-staleness` 19/23 ⇒ 🔴 四个里唯一的真遗忘**：4.5 等他确认收到推送，但 **5.1 队列 #312 回写、5.2 `wecom-aibot-service/CLAUDE.md` 更新是纯粹没做、行内也没写任何理由**，须补做后归档。 ━━━ 📌 **本条的未闭合项（待总线派发）**：⑴ 对 ① ② 执行 `--ack-stale-change` 并附复核依据（**降噪是有代价的决定，待 Shao Peishen 拍板后执行，本轮未擅自跑**）；⑵ 补 ④ 的 5.1／5.2 后归档；⑶ **把三条降噪入口写进收工纪律**（落点建议 `专线opener模板库.md` §〇 收工段与 openspec 变更包 tasks 模板，**不落 `CLAUDE.md` 根**——它正在瘦身）。**⑸ index.lock 预检仍在本行、随下次 sweep 侧改动同车，本次不动代码。** ━━━ 原文：🐛 **新增 ⑸ index.lock 预检缺失，本行因此不销号（2026-08-22 追记，OP0822A；Shao Peishen 答「并回本行随下次 sweep 侧改动一起修」）** ━━━ **现象**：2026-08-22 13:02 UTC 运维群收到「落库 sweep 遇到未预期异常：CalledProcessError … `['git','-c','core.quotepath=false','add','--','0-学习与工具/skills源码/zhuopin-kickoff-prompt/SKILL.md']` returned non-zero exit status 128」。 **根因已实测**：并发 git 进程（`OP-0822-E`／`OP-0822-F` 同时在跑）持有 `.git/index.lock`。**无实际影响、已自愈**：13:03 那轮 sweep **主动检测到 index.lock 并优雅跳过**，13:36 重试成功、21:37 已落库；`git diff HEAD` 对该文件为空。 ━━━ 🔴 **真缺陷：同一个 `index.lock` 检测，13:03 那个入口有、13:02 这个入口没有** —— **守卫只装在一条路上，另一条撞上了才抛异常，并且惊动了运维群。** **同族＝ §一 #351「裸竖线检查只装在最常走的那条路上」。** **修法**：把 index.lock 预检提到 `git add` 之前（复用 13:03 那段现成逻辑），撞上即按「跳过本轮、下轮重试」处理，**不发未预期异常告警**。**排期**：不紧急、不阻塞任何人（仅多 session 并发时触发），**随下次 sweep 侧改动同车做掉**。 ━━━ ⚠️ **顺带记一条我自己的取证失误**：核这次有无影响时，我先用 `git show HEAD:<file> > $tmp` 再算哈希，得出「磁盘 43,827 B ／ HEAD 43,667 B、哈希不同」，**差点报「文件不一致」**；**实际那 160 字节正好是 160 个换行符** —— `core.autocrlf=true` 下 blob 存 LF、磁盘存 CRLF，**这是正常表现**。**权威判据是 `git diff HEAD` 为空，不是经 PowerShell 重定向算出来的哈希。** **⇒ 与 §四 #98「沙箱 git ≠ 本机 git」同族：又一次是取证方法本身把结论带偏，而不是被查对象有问题。** ━━━ 原文：✅ **⑶＋⑷ 已实现并合入 master（2026-08-22，CC，worktree `sc2-caseback-fix-8ede5a`，分支 `claude/op0822c-sweep-alarm-rewrite-b86654`，openspec 变更包 `sweep-resident-service-judge`，`OP-0822-C`）** ━━━ **判据由「单条路径前缀 `startswith`」整体替换为「常驻服务运行体集合」**，三来源合并：① 该服务 `sync-to-server.ps1` 的 `-AppFiles` 显式部署清单（现读现算）② `-LocalPlatformDir` 指向的平台底座（除其 `tests/`）③ 计划任务执行入口与注册脚本。**排除项（文档／`tests/`／sync 脚本自身）不写黑名单**——它们本就不在①②③任一集合内，靠白名单天然排除；黑名单会漏，白名单只会多报。**协议〇.9 措施 B**：`RESIDENT_SERVICE_PATH_PREFIXES` **整体退休、不留兜底分支**（退 1 进 1，守卫净增 0），并配 AST 级护栏断言该符号不得被加回来。 ━━━ 🔴 **开工取证推翻了派单件的一处前提，据此把范围从「只读 `-AppFiles`」扩为三来源合并（Shao Peishen 当日答定夺 1(a)）** —— 本机 `Get-ScheduledTask` 直读实测：`ZhuopinAibotDevListener`(Running)／`ZhuopinDecisionReminderDaily`／`ZhuopinFollowupDispatchDaily` 三个任务的 Action **均为 `wscript.exe` + worktree `wecom-service-home` 下的 `run-*.vbs`** ⇒ **告警说的通道（本机 worktree）与 `-AppFiles` 描述的通道（scp 到 `.51`）不是同一条**，且那份 sync 脚本文件头自陈「本脚本目前不在常规使用」。**`run-*.vbs` 是 Action 真身却不在 `-AppFiles` 内** ⇒ 只读 `-AppFiles` 会修掉 `.md` 误报、**同时新造一类「改 `run-*.vbs` 不报」的漏报**，正是派单件自己警告的「只修一面等于换一种不准」。🔑 **教训：一份「显式清单」看起来像真值，但它只是某一条通道的真值——先确认这条告警守的是哪条通道，再决定读谁的清单。** ━━━ **真实数据双向对照（不是只跑 fixture）**：批次 `B-0822_17`（7 个文件，服务目录下只有一份 `CLAUDE.md`）**旧判据报（即 08-22 14:52 那次误报）／新判据不报**；`zhuopin_platform/.../notifiers/wecom.py` **旧判据不报（即 ⑷ 的漏报）／新判据报（命中：平台底座）**。**6 份 sync 脚本全解析通过**，含 FI2 那份「跨行 ＋ 带 BOM」的组合，以及第 6 份 `AI运营指挥中心` 按预期解析为 `None`（它确实不走 `Sync-ZhuopinPlatformAndApp`、无 `-AppFiles`）。 ━━━ **另两处按 Shao Peishen 当日定夺落地**：**2(a)** ⑷ 只补平台底座、**不扩到 `.51` 六目标**；**3(a)** 先 propose+design 送审、审毕再实现。**⑵ 入口类与注册脚本类处置措辞分开**（定夺 2(a)）——#199 已实证「改注册脚本源码 ≠ 改了在跑的任务」，对它印「同步并重启」等于给出一个做了也没用的处置，**比不发更糟**。 ━━━ **告警正文改为逐条回显命中路径与命中原因**：08-22 那次误报之所以要靠人反查批次内容才发现，正是因为告警只印一句写死的话、不说命中了什么，**判据无法被现场证伪**。 ━━━ **解析失败一律从低取值**（按命中处理并在正文说明），不静默沉默——静默不报正是 ⑷ 那类漏报的成因，**而漏报不产生任何信号**。 ━━━ **单测 +18**（17 条纯函数逐类锁死召回率 ＋ 1 条 e2e 直接复现 08-22 误报）；**既有 e2e 一度失败，未放宽断言**——它的临时仓库没有 sync 脚本、走的是「保守判定」兜底分支，已补 sync 脚本 fixture 并加断言 `命中：部署清单` 且 `不含保守判定`，否则那条 e2e 会变成只验兜底、验不到真判据。**`0-学习与工具` 全量 196 passed ＋ 9 subtests（基线 178 ＋ 新增 18，数字对得上）、零回归**；`openspec validate --all --strict` 97/97；跑完 `git status --porcelain` 无新增未跟踪件（对应 #328② 伴生文件复核，实测不采信推断）。 ━━━ ⚠️ **本次未覆盖 `.51` 四个部署目标（FI2／QD-B／SC2／SC8），是刻意取舍不是遗漏**（定夺 2(a)：本次「常驻服务」仍指本机常驻那一个）——**显式登记在此，免得下一轮误以为已经覆盖了**；那四个的另一面由 #229 部署留痕守卫覆盖。 ━━━ ⚠️ **一处明知而接受的残余风险**：来源③ 只能写成 sweep 侧常量——**计划任务的真实设置不在仓库里**（#199 的核心教训），没有可读的真身。若日后任务 Action 改了而常量没跟上，会重新出现漏报**且没有任何自动信号**。常量处已注释写明取证来源与复核命令，并列入月度环境体检 `#98` 核对项。 ━━━ 🐛 **顺带更正一处 v1 残留**：派单件 §OP-0822-C 的「约束」小节仍要求「⑴ 断言不再出现已销号行号」与「⑶ 断言本批仅含 `.md` 时不发」，与同节 v2 正文自相矛盾——**按后缀写的断言会把一个已被推翻的旧判据重新钉死进单测**，已同批改正。 ━━━ **⑴ 不做**：实测 L2098／L2173 注释自陈 2026-08-21 已改为动态查，现存两处 `#299` 是合法历史注释、不是缺陷。 ━━━ **⇒ ⑴⑶⑷ 三条至此全部销号，本行关闭。** ━━━ **原状态头（保留不删）**：📤 **派单件 v2 已重写并重派（2026-08-22 16:4x，Shao Peishen 当日答「现在重写并重派」）** —— **范围＝⑶＋⑷ 两条同车，⑴ 直接销号**；修法判据由「文件后缀」改为「**读 sync 脚本的 `-AppFiles` 显式清单**」；v2 顶部标注「原 v1 三处被推翻，若读到 v1 以 v2 为准」。**派单件**＝`1-转型规划/0-全景路线图/opener集-CLAUDE进度段lint二期与sweep告警修正-2026-08-22.md` §OP-0822-C（v2）。⚠️ **v2 里加了一条它自己的防复发**：「实现前先确认 `-AppFiles` 里是相对哪个根的路径、多份 sync 脚本如何枚举，**这一步没查清之前不要动代码**」——**v1 就是栽在「没查清就给修法」，v2 不能用同样的方式再给一个修法。** ━━━ 原文：🔴 **派单件三处被 `OP-0822-C` 当场推翻，须重写后再派（2026-08-22 16:2x，OP0822A 据 C 的澄清问题逐条实测核实）** ━━━ **⑴ 我登记的这一条是过时的，它在我登记之前就已经修好了** —— 实测 `工具-落库sweep.py` L2098／L2173 两处注释明写「原文把『存量补齐见队列 #299』写成硬编码字符串，该行已销号」⇒ **已改为动态查，2026-08-21 合入 master；现存 2 处 `#299` 是合法历史注释，不是缺陷**。🔴 **这是同一个错误的第 N 次**：我 2026-08-21 从运维群告警反查登记它，**今天重述并写进派单件时没有核当前代码**。**⇒ 与本项目已记的「同源验证」「未核就断言」同族，但这次的形态更值得警惕——「已登记在案」本身产生了一种「已经核过」的错觉**，而登记只证明当时看到过，不证明现在还成立。**⇒ 凡把一条既有登记写进派单件，须当场重核它是否仍然成立。** **处置：⑴ 直接销号，本次不重复实施。** ━━━ **⑶ 我给的修法是猜测，而现场有硬事实可用** —— 派单件建议「非 `.md` 非 `docs/` 则报」是按文件后缀猜；**而 `sync-to-server.ps1` 自带显式部署清单**（实测 FI2 那份 L30：`-AppFiles @("pyproject.toml", "fi2", "scripts", "deploy-server.ps1")`；aibot 那份同构）。🔑 **「这次改动到底需不需要同步重启」的真值就是这份清单，不需要猜文件后缀。** **且按后缀判会继续误报**：`CLAUDE.md`／`tests/`／`register-*.ps1`／`run-*.vbs`／`sync-to-server.ps1` 自己全都不该报，而按「非 `.md`」判后四类照样误报。**⇒ 修法改为：读 sync 脚本的 `-AppFiles` 清单作判据。** ━━━ 🆕 **⑷ 同一个函数还有反向缺陷：漏报（C 顺带发现，本次一并纳入）** —— 实测 `RESIDENT_SERVICE_PATH_PREFIXES = ("5-平台底座/wecom-aibot-service/",)` **只有一个前缀**，而同一份 sync 脚本还同步 `5-平台底座/zhuopin_platform` ⇒ **改底座代码会真实影响常驻服务，这条告警却一声不吭**。🔑 **⑶ 治的是「多报」、⑷ 是「漏报」，同一个判据的两面 —— 只修一面等于把判据从「不准」改成「另一种不准」。** ━━━ ⚠️ **`OP-0822-C` 现状：分支 `claude/op0822c-sweep-alerts-b9424a` 已建但零提交，未落地任何改动**（实测其 HEAD ＝ 当时的 master）。**派单件须按上述三点重写后再派**：范围由「⑴⑶ 两条同车」改为「**⑶＋⑷ 两条同车，⑴ 直接销号**」，且**修法判据由「文件后缀」改为「读 `-AppFiles` 清单」**。 ━━━ 原文：📤 **⑴⑶ 已派单，等 CC 领活（2026-08-22，OP0822A；`OP-0822-C`，零依赖可立即开工）** ━━━ **处置已拍板 (a)「一起做」（Shao Peishen 2026-08-22）**，但 🔴 **我给他的并入理由有一半是错的，如实更正**：我当时写「并入 §四 #80 二期，**同一个文件、同一批单测**」——**后半句实测不成立**。**#80 一期建的是 `0-学习与工具/工具-CLAUDE进度段lint.py`；⑴⑶ 那两个告警在 `0-学习与工具/工具-落库sweep.py`**（`涉及常驻服务` 字样实测命中该文件与其单测），**两者不是同一个文件**。⇒ **正确的并法：⑴ 与 ⑶ 互相并入（它们才是同文件同族）；与 #80 只是「可同车一次 CC session」，不是并入同一行。** **队列上各自承接、执行上同车**——他要「一起做」的意图不变，只是记法要对，**否则日后按 #80 去找这两个修法，会找到一个根本不含它们的变更包**。🔑 **这条错误值得留着**：我是用「同一个文件」这个具体理由说服他选 (a) 的，**而那个理由我没有核过**——**一个听起来很具体的理由，比一句含糊的推荐更容易被采信，也因此更该先核。** ━━━ **派单件**＝`1-转型规划/0-全景路线图/opener集-CLAUDE进度段lint二期与sweep告警修正-2026-08-22.md` §OP-0822-C。 ━━━ 原文：🐛 **机制缺陷登记（无需答复）——三条**（第 ⑶ 条 2026-08-22 由 Shao Peishen 转来的运维群截图反查而来，OP0822A 并入，按协议〇.10 未新建行） ━━━ 🔴 **⑶ 「涉及常驻服务」的告警判据过宽，改一份 `.md` 文档也会报「需 sync-to-server.ps1 同步并重启 ZhuopinAibotDevListener」** —— **实证**：2026-08-22 14:52 运维群收到该告警，而触发它的批次 `B-0822_17` 在 `wecom-aibot-service` 下**只改了 `CLAUDE.md` 一份文档、零行代码**。⇒ **判据显然只匹配了路径前缀，没有区分「代码/配置」与「文档」。** 🔑 **误报的代价不是噪音，是它会训练人忽略这条告警** —— 而这条告警存在的全部意义，正是为了在真的改了常驻服务代码时把人拦住；**等它被忽略成习惯，真出事那次也不会有人看**。**同族＝本行 ⑴**（告警文案硬编码了一个已销号的承接行 `#299`）：**两条都是「告警还在发，但它说的话已经不对了」。** **修法建议**：判据加一层——命中路径前缀后，再看本批在该目录下是否有**非 `.md`／非 `docs/` 文件**，全是文档则不报。 ━━━ 原文：🐛 **机制缺陷登记（无需答复；2026-08-21 由运维群 sweep 告警反查而来）——两条**：**⑴ sweep 告警文案硬编码了一个已销号的承接行**：`工具-落库sweep.py` 内 `存量补齐见队列 #299` 为硬编码字符串（该文件内 `#299` 命中 2 处），而 §一 `#299` 已于 2026-08-07 `[S:done]` 销号 ⇒ **告警每天指向一个已关闭的行，等于没有承接**。同族＝本项目已记的「指针指向已不存在的东西」。**修法**：改指向长期有效载体，或发出时动态查该行状态、已 done 则改口。 **⑵ SC2 的可追溯链是真断的，不只是流程问题**：实测 `openspec/specs/` 下 74 个 capability 中**没有任何 sc2 开头的**（sc1／sc5／sc7／sc8 均有），而 SC2 **已部署 `.51:8096`、冒烟四关通过、跟进信 采购部#17 已发出且专员已回件** ⇒ **一个已上线并让业务部门在用的场景，spec 层零落地**，这是 IATF 可追溯性会直接挂掉的形态，比「变更包忘了归档」严重一档。**根因**：`sc2-weekly-report-mvp` tasks **46/49** 未全 [x]，按完工即归档纪律不能 archive，delta spec 因而进不了 specs。**已并入 §一 `#361` ⑴。** 💡 **顺带一个可复用的观察**：在途 13 个变更包里有 **6 个处在 33/34、47/48、25/26、29/31、35/37 这类「只差最后一两项」的状态**，而 sweep 只报了 1 个（4.1 天无改动那个）——**「差最后一步」是本项目的系统性形态**，与今日查出的「回灌完不转态」「状态列改了编号列不改」同族。 ⚠️ **本行是一次编号事故的产物**：初登记时把顶部高水位线「§四 #86」误读为「我已预留的号」，实际它是**下一个可用号**；`release` 结构校验以「不属于本次持锁期间 --reserve 预留的编号集合」当场拒绝（守卫有效，当日第五次）。已撤行重取为 87。**⇒ 高水位线是「下一个可用」，不是「已预留给我」。**
