---
title: "派单件 ·【CC】措施 C 换量具 —— openspec propose（OP-0911-D）"
created: 2026-09-11
status: 待派出
派出线: Cowork 环境总线 OP-0910-H
承接: 队列 §四 `#58`（措施 C 正体）＋ §一 `#454`（状态分诊常态化）
授权: Shao Peishen 2026-09-10 答 `1a`（换量具）＋ 2026-09-11 答 `1a`（先实测定 N/K 再派 propose）
首要输入: `1-转型规划/0-全景路线图/方案-措施C换量具-陈旧度闸-2026-09-11.md`
---

# 派单件 ·【CC】措施 C 换量具 · openspec propose

## 〇、本棒只做 propose ＋ design，**不 apply、不改任何闸**

🔴 **闸是全项目唯一写入咽喉（编辑锁 `release`）。改错它，所有会话当场写不进队列。** `#58` design 决策点 4 记过一次真实的自锁坑：若把判据写成「release 时超限即拒」，则连来关行降 WIP 的那个 session 也会被挡在门外。本棒**只产 `openspec/changes/` 下的 `.md`**，apply 另派。

## 一、口径已定死的部分（A 类，不必再问）

| 项 | 值 | 立据 |
|---|---|---|
| `N`（陈旧天数） | **7** | 方案件 §D2 实测：23 条计入行最老 7 天，`STALE(7)=0`；`N=10` 经实测永不触发＝悄悄废闸 |
| `K`（允许陈旧条数） | **3** | 今天 `STALE(7)=0`，留 3 条真实余量，不会上线即阻断 |
| `STALE` 取数手段 | `git blame --incremental` 取行末次 commit `author-time`，按行首 `\|<编号>\|` 映射行号 | 方案件 §D2；增量模式不带正文，避开大文件与 78 KB 长行 |
| 触发时机 | **维持原样**——仅在本次持锁真正新增 `[D:机]` §一 行时判 | `#58` design 决策点 4（防自锁） |
| 逃生阀 | **维持原样**——`--force-mechanism-wip` ＋ 行内 `WIP豁免：<理由>`，缺一即拒 | `#58` design 决策点 5 |
| 上线方式 | **先告警 7 天**，与旧行数闸并行回显两个数；7 天后复评再切阻断 | 方案件 §D4 |

## 二、做什么（逐条）

1. **前置实测复跑**（现取表过期守，同 `#522`）：起棒当时重跑一次 `STALE` 分布（方案件 §D2 的手段），与方案里的 23 条 / 最老 7 天对照；**偏差 >3 条即停手回报**，不要拿过期基数写进变更包。
2. **`openspec propose` 变更包 `mechanism-wip-staleness-gate`**，`design.md` 至少含四个决策点：
   - **D-A**：`STALE` 的「真实活动」定义 —— 本件取 `blame` 末次 commit。🔴 **已知偏差必须写进 design，不得省略**：机械改动（K2 外置／每周清扫／称呼订正）也会刷新时间 ⇒ `STALE` 系统性偏低、是乐观估计。缓解案＝观察期内值周巡检对行龄榜首 3 条人工抽查一次。
   - **D-B**：🛑 排除判据只认状态列的缺陷修法 —— **(甲)** 判据认「状态列或任务列任一以 🛑 起首」／**(乙)** 写侧强制 🛑 落状态列并订正存量 5 行。**本件推荐 (甲)＋写侧告警**（(乙) 要改历史行，与「历史记录不追改」冲突）。🔴 **实测影响必须写进 design**：`#381`／`#382`／`#448` 三行正文自陈「排队中·暂非可动」却被计入，改判后 WIP **23 → 20/22 立即合规**。
   - **D-C**：`#58` ⑸ 缺陷修法 —— 新增行不计入 WIP 时不触发阻断，只回显存量（`#58` 自己给的修法⑴）。
   - **D-D**：迁移期与退休判据 —— 告警 7 天后复评须先答「这 7 天里新闸的告警有没有被任何人当回事」（照抄 `#58` 手法，答否则不得默认续期）；本版退休判据见方案件 §D5。
3. **单测先写**（apply 棒会用，本棒只写 spec 与用例清单，不写实现）：⑴ `STALE` 计数对「有 blame／无 blame／行号漂移」三态的行为；⑵ 🛑 两列写法各自被正确排除；⑶ 存量超限时来关行的 session 照旧放行（`#58` 决策点 4 的关键回归，**这条不许丢**）。
4. **收工**：push 本棒分支（不 ff master，🟡 待他一字母）；走锁把本棒产出回写 §四 `#58`（一段，指向变更包路径）＋登记 §二；收工汇总末附「待你一字母」与答复模板（🔴 模板须单独成一个代码块）。

## 三、不做什么

- **不 apply、不改 `工具-共享文档编辑锁.py` 任何一行**；不 ff master、不在主仓 commit。
- **不第三次抬行数上限** —— `#58` 自书「第三次为迁就现状改口径即等于废掉措施 C」，此路已封。
- **不退休措施 C**、不动业务域（措施 C 只管 `[D:机]`）。
- **不订正存量 5 行 🛑 写法**（D-B 若裁 (乙) 再另派），不追改任何历史行。
- 不 Read／grep 队列真身；读队列只用 `工具-队列查询.py --digest／--row`。

## 四、开场词（引用版，工具-opener生成.py --variant reference 原样输出，禁手抄）

▶ 粘贴端：CC

▶ 首次派出：[OP-0911-D]

✗ 编号 OP-0911-D 当日（0911）已被使用（撞号，队列 #461／#487 P7① 查重，命中全称或 `[Win]0911D-` 短形；射程见 `_scan_used_suffixes` 文档字符串：只认已落档痕迹）；下一个空号：OP-0911-E
