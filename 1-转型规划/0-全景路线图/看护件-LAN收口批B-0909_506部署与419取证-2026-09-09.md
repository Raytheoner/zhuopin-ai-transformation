---
title: "看护件 · LAN 收口批 B-0909_506部署与419取证（2026-09-09）"
created: 2026-09-09
status: 已起链（Shao Peishen 2026-09-09 答 1a）
批次: B-0909_506部署与419取证
派出线: Cowork 业务总线 OP-0908-V（`我已回Lan` 触发 zhuopin-lan-closeout）
---

# 看护件 · LAN 收口批 `B-0909_506部署与419取证`

## 〇、本批口径

**触发**：Shao Peishen 2026-09-09 11:2x「我已回Lan」。规则正本＝`0-学习与工具/skills源码/zhuopin-lan-closeout/SKILL.md`，判据正本＝`openspec/changes/lan-closeout-skill/`。

🔴 **前置探针（触发词声称≠在网事实，已实证）**：`工具-泳道看护状态机.py lan-status` ＝ **✅ on-LAN（三项齐备）**；佐证 `Test-Connection 192.168.100.51` ＝ True、`Test-NetConnection :22` ＝ True。本机时间 2026-09-09 11:21:04 +08:00。

**常规三查**：① `origin/master..master` ＝ 0／`master..origin/master` ＝ 0，分支 master；② 队列锁空闲；③ 上一收口批 `B-0908_AL4` 汇总已出。

**上限**：**首跑灰度 2026-09-07 批 `B-0907_Q` 已走完、三判据全过**（`#419` 行内 09-09 明记「不带灰度上限」），默认 6；本批实际入批 **2 项**。

### 三源留步清单汇集结果（D1 白名单：只收有可执行判据的留步登记）

| 来源 | 命中 | 判定 |
|---|---|---|
| 未闭合产出扫描器 形态1（`--skip-lan-probe`，探针已单独实证） | `items` 8 项：`#312`／`#358`／`#398`／`#436`／`#447`／`#454`／`#478`／**`#506`**；`needs_review` 3 项：`#354`／`#419`／`#340` | 仅 **`#506`** 属 `.51`／LAN 面且三件齐备 ⇒ **入批（项 1）**；`#419` 经**真读一遍**后 ⇒ **入批（项 2，只读取证）**；其余 8 项为库内机制活、**非 LAN 留步**，不入本批 |
| 两份队列真身 grep「LAN 留步」 | `#312`／`#398`／`#401`(done)／`#419`／`#502`(done) | `#419` 同上入批；`#312`／`#398` 属概念提及非可执行留步；两个 done 行排除 |
| 近期看护件 LAN 留步节（`B-0909_两包apply` §二／`B-0909_三包apply`／`B-0908_AL4`） | `#506`／`#500` | `#506` 同上入批；**`#500` 现网 165 提交清账已在他线 `B-0907_Q`**，不重复排 |

🔴 **`needs_review` 已按规则真读、未批量打回**：`#354`（`_find_env()`／`parents[N]` 同族收拢，`[S:timed=2026-09-03]`，库内活非 LAN）、`#340`（QD-B 跨模块校验，业务域库内活）⇒ 两项**均非 LAN 留步**，落「登记不合格」桶；`#419` 读后判定够 D1，入批。

## 一、硬边界

1. 🔴 **`.51` 一次只动一项、全串行**：项 1 未完（冒烟全过并回写证据）之前，项 2 不起。
2. 🔴 **每项四步固定**：快照 → 执行 → 冒烟 → 回写证据。**冒烟任一项不过 ⇒ 按回滚 SOP 退回并停链**，失败项降回留步态并注明原因，**不带伤推进**。
3. 🔴 **「已部署」必须有冒烟三件套证据，无证据的完成声明视同未做。**
4. 🔴 **对外发送与请人动作永不代办**：本批产出只是 `采购部#22` 的解押证据，信仍走「⏳ 待你审 → 你回『发』」。**L2 门禁不代签；合规红线不动。**
5. 🔴 **项 2 只收取证、不代答 `#419` 第①问修法**（(a) 补扁平布局分支／(b) 双布局语义／(c) launcher 注入环境变量）——该问**无默认、须他明确答复**，取证只是它的输入。
6. `pause`／`check-heartbeat` 一律带 `--no-notify`；**看护件落档失败即全停**。
7. 🔴 **收口 ≠ 销号**：行销与否按各行原纪律。

## 二、波次（全串行）

| 序 | 项 | 承接 | `.51` 触碰 | 快照点 | 冒烟判据 | 回滚 SOP |
|---|---|---|---|---|---|---|
| 1 | SC2 页面基准日冻结修复 **部署 ＋ 全量重算复算** | §一 `#506`（关键路径，⏰ 09-11；`采购部#22`／`#499` 双双押后等它） | `C:\sc2\{app,.venv}` 写；计划任务 `Sc2WebServer` 重启；`POST /api/refresh` 全量重算 | ⑴ `C:\sc2\app` 顶层文件 SHA256 清单；⑵ 现 `GET /api/ping` 返回体；⑶ `Sc2WebServer` 进程 CreationDate；⑷ **改前页面期次原值**（用于事后对照「确实变了」） | 沿用 `smoke-server.ps1` 七项 ＋ 🔴 **本批新增第八项＝页面期次为当周**（`#506` 的解押判据，非既有七项之一）；🔴 进程 CreationDate 逐次核对真实刷新，**非只信脚本打印「已重启」**（根 `CLAUDE.md` 坑 5） | 还原快照 ⑴ 的 `C:\sc2\app` 副本 → `schtasks /End /TN Sc2WebServer` → 重新 `schtasks /Run` → 复跑 `smoke-server.ps1` 确认回到改前态；防火墙规则 `Sc2-WebServer-8096` 保留不动 |
| 2 | `#419` 运维逃生通道第三道防线 · **只读取证** | §一 `#419`（LAN 留步登记 2026-08-26，**已 14 天**） | **全程只读**：`.51` 上 `alert_webhook.py` 与 `C:\wecom-aibot\.env` | 取证前记录两文件 SHA256（证明只读未改） | ⑴ 两文件 SHA256 前后一致（未改）；⑵ 取到「有无人工补过 `_OPS`」的确定答案（有／无，二者皆为有效取证结果） | 无需回滚（只读）；若误触写操作 ⇒ 立即停链并如实报告 |

🔴 **项 1 有一个硬前置，必须先做**：修复仍在分支 `claude/op0908u-506-base-date-freeze-d7894e`（现取 `rev-list --count master..分支` ＝ **1 笔，未合 master**）。**Shao Peishen 2026-09-08 已答「合」授权 ff**，但至今无人执行（`OP-0909-G` 那棒只做了 `#489`）。⇒ **先 ff 进 master，再从 master 部署**，不部署未合入的分支版本。

**触碰区两两比对**：项 1 ＝ `C:\sc2\`；项 2 ＝ `C:\wecom-aibot\` ＋ aibot 服务件。**零重叠**，但按 D2 **仍全串行**（`.51` 一次一项是硬纪律，不因触碰区不重叠而放宽）。

## 三、泳道 opener

> 🔴 **对本件 opener【设置】行的两条订正，起棒前先读**（opener 由生成器产出、**本件不手改 opener 正文**，骨架「禁手抄」纪律 ⇒ 订正写在此处，以本段为准）：
>
> **⑴ worktree ＝ ☐ 不勾**（opener 已写对，此处只是强调理由）：本棒要 git merge --ff-only **进主仓 master**，而 **worktree 里 ff 不到 master**；部署脚本 sync-to-server.ps1 也从笔记本主仓跑。
>
> **⑵ 🔴 分支字段以本段为准：不起新分支，直接在 master 上做。** opener 写的「从 master 起 `claude/op0909k-lan-closeout-0909`」是生成器固定写法，**本棒不适用** —— 一旦 `checkout -b`，主 checkout 就离开了 master，随后 ff 会落到那条新分支上、**master 原地不动，等于白做**。本棒三个动作都不需要独立分支：**ff（在 master 上）／部署（不产生 git 改动）／回写队列（走编辑锁，也在 master 上）**。
>
> ⚠️ **这是本会话今天第 6 次撞同一个根**（写进文本的设置不会自己适配当前任务）：生成器按通用模板填了分支字段，而本棒是少数「不该有自己分支」的棒。**凡【设置】行与任务实质冲突处，一律以看护件正文为准。**

本批单链、逐项串行。粘贴端：CC ｜ 泳道：506-deploy-419-eval

```
[OP-0909-K]【CC】506部署419证
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0909k-lan-closeout-0909`） ｜ worktree：☐（主 checkout 执行，需触碰 .51 与主仓 master） ｜ 工作区：☑ .51 生产面（SC2 服务 ＋ aibot 只读取证） ｜ session：新开 ｜ 派出线：Cowork 业务总线 OP-0908-V（我已回Lan → zhuopin-lan-closeout）
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0909K-506部署419证。🔴 例外：你若是被 Task/Agent 起的子任务，跳过本行不要执行——子任务没有自己的 session，"self" 会解析到父 session、把调度你的那条会话改名（2026-08-28 实撞）。
读 ① `1-转型规划/0-全景路线图/看护件-LAN收口批B-0909_506部署与419取证-2026-09-09.md` → ② `CLAUDE.md` §§3／§5／§7 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。

做什么：
1. 🔴 项1 硬前置：先 ff claude/op0908u-506-base-date-freeze-d7894e 进 master（Shao Peishen 2026-09-08 已答「合」授权），再从 master 部署——不部署未合入的分支版本
2. 🔴 项1 四步固定：快照（C:\sc2\app 顶层 SHA256 ＋ /api/ping 返回 ＋ Sc2WebServer 进程 CreationDate ＋ 改前页面期次原值）→ sync-to-server.ps1 / deploy-server.ps1 → smoke-server.ps1 七项 ＋ 本批新增第八项「页面期次为当周」→ 回写证据
3. 🔴 项1 收工回写两处：§一 #506 与 §四 #176（采购部#22 解押证据落点）；回写后回读核字节数（#506 现 2926 B，上限 4096 B）
4. 🔴 项2（项1 全过后才起）：只读取证 .51 上 alert_webhook.py 与 C:\wecom-aibot\.env 有无人工补过 _OPS；取证前后记 SHA256 证明未改

不做什么：
- 🔴 冒烟任一项不过即按看护件 §二 回滚 SOP 退回并停链，失败项降回留步态注明原因，不带伤推进；「已部署」无冒烟证据视同未做
- 🔴 不代答 #419 第①问修法（三选一、无默认、须他明答）；对外发送与 L2 门禁永不代办；采购部#22 仍走「⏳ 待你审」
- 🔴 .51 一次只动一项、全串行，项1 未完不起项2；不碰其它 .51 服务；不改防火墙规则
```

## 四、收工汇总

跑 `python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0909_506部署与419取证` **现取**，不手工数。每项一行：**判据 → 证据 → 行内状态处置**。

🔴 **项 1 收工必须同时回写两处**：§一 `#506` 与 §四 `#176`（`采购部#22` 的解押证据落点）。**回写后回读核字节**——`#506` 现 2926 B，K2 上限 4096 B，余量 1170 B；超出即同步追进 `1-转型规划/0-全景路线图/队列行日志/#506.md`。

🔴 **解押判据写死，不得放宽**：`采购部#22` 解押 ＝ 部署后**复跑一次全量重算、确认页面期次为当周**，证据补进 `#506` 与 §四 `#176`。**修好 ≠ 可发信**；信仍走「⏳ 待你审」。
