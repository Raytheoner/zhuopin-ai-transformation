---
title: 看护件 · 【CC】`#629` 缩编续完单泳道
created: 2026-09-21
派出线: 环境总线 OP-0921-A
批次: B-0921_629续完
status: 待执行
---

# 看护件 · 【CC】`#629` 缩编续完单泳道 · `B-0921_629续完`

## 〇、口径

承接 `B-0921_必读件与分诊` 里 A2（`OP-0921-C`）被 600 秒后台任务上限掐断的那一棒。**进度没丢**，已由看护者在分支 `claude/op0921c-629-readfile-slim` 上落检查点 `5a4d49cb`。本棒**续用同一分支与同一 worktree，不新建**。
🔴 看护件按泳道切片：做什么／不做什么全在 `续棒件-【CC】629-必读件缩编续完-2026-09-21.md`（6,208 B），本件不复述。

## 一、硬边界

1. 🔴 红线照旧永不代办：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 与生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）。
2. 🔴 **禁起后台子任务／并行 Agent**——上一棒正因此被掐断。
3. 🔴 **不得新立固定字节阈值口径**；上一棒新立的两个（`CLAUDE_MD_RULES_BYTE_CAP` 8,192、`LANE_WATCH_SKILL_BYTE_CAP` 24,576）必须改成棘轮。
4. 🔴 收工只 push 本分支，不碰主仓、不 ff master。
5. 🔴 回归 narrow 到受影响测试类（node-id，禁 `-k`）；输出重定向到文件只读结论行。
6. 🔴 收工前跑 `git diff --name-only master...HEAD` 贴进收工报告；期望产出缺一个不许写 `OPENER_DONE`。

## 二、次序与并行矩阵

| 波 | 条 | 队列行 | 泳道名 | 触碰区 | 边界 |
|---|---|---|---|---|---|
| 1 | A1 | `#629` | `缩编线` | `.claude/rules/*.md`；`CLAUDE.md`；`0-学习与工具/skills源码/zhuopin-lane-watch/{SKILL,CHANGELOG}.md`；`进度编年-CHANGELOG.md`；`0-学习与工具/工具-落库sweep.py`＋其单测 | 续棒件＝`续棒件-【CC】629-必读件缩编续完-2026-09-21.md`。单泳道，无并行 |

## 三、泳道 opener

### A1 · `#629` 必读件缩编续完（§一 #629）

粘贴端：CC ｜ 泳道：缩编线

```
[OP-0921-F]【CC】629缩编续完
【设置】执行环境：CC ｜ 分支：master（从 master 起 `claude/op0921f-629-slim-cont`） ｜ worktree：☐ 不建（续用既有 op0921c-629-readfile-slim，见续棒件 §〇） ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0921-A ｜ 模型：sonnet
读 ① `1-转型规划/0-全景路线图/续棒件-【CC】629-必读件缩编续完-2026-09-21.md` → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 A 类（口径已定、判据已写死），无需再问澄清，直接开工。
🔴 并行上限 4，超出排下一波，错峰 ≥90 秒（构建环境瘦身第三轮方案 P4）。
🔴 心跳一律跑命令写、不自己拼路径：开工 1 分钟内 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921f-629-slim-cont --text "已开工"`；每里程碑追加一行（等待 >10 分钟须补写「仍在等 X，预计还要 N 分钟」）；收工 `python 0-学习与工具/工具-泳道看护状态机.py heartbeat --lane op0921f-629-slim-cont --done --batch B-0921_629续完 --text "产出落点：<落点>"`——不带 `--batch` 该泳道不计入任何批（`heartbeat --done` 不带它就不计入任何批次的 `summary`，已实测撞过 8 条历史泳道）；它没有 `--repo-root`／`--heartbeat-file` 两个参数，别给（队列 §一 `#565`）。
🔴 自测只跑受影响测试类（node-id `pytest <文件>::<类名>`，禁 `-k`，禁跑整份回归——那是 ff 六关④的活，泳道再跑一遍＝同一件事做三遍）；测试输出一律重定向到文件、只读结论行；长任务不起后台任务轮询等待收尾，改用哨兵文件、大间隔探测，不逐分钟 tail（队列 §一 `#627`）。
🔴 收工只 push 本泳道分支，不碰主仓、不 ff master——主仓 ff 由看护者收工时串行做，或经『已授权待合』登记处由 `工具-待合分支巡检.ps1` 机器做；sweep 不做 ff（构建环境瘦身第三轮方案 P4，`#553` 更正）。
🔴 收工前自己跑 `git diff --name-only master...HEAD`，把输出原样贴进收工报告；期望产出点名的文件只要有一个不在清单里，就必须改以 `OPENER_PARTIAL` 收尾（不许判定为收工完成）——看护者按同一条命令机器核，不看哨兵自陈。本批若新增常驻状态告警类，须同批自陈「是否影响其它沙箱测试、用什么核的」（队列 §一 `#627`）。
🔴 新增常驻状态告警类须同批给出其它测试的沙箱夹具要求（哪些用例要补桩／改夹具，不能等上线后测试全红才回头查）；收工报告自陈「本次新增告警类是否影响其它沙箱测试、用什么核的」，只有结论没有手段不算（队列 §一 `#627` B 面）。
🔴 收工以顶格一行 `OPENER_DONE` 收尾；命中 🟡/🔴 决策点则以 `OPENER_PARTIAL: 停在<档位>决策点——<在等什么>` 收尾（`工具-opener批处理执行v2.ps1` 判成败双指标之一，缺它做完的活也会被判 NO-SENTINEL；队列 §一 `#550`）。
```

## 三bis · 看护者开场词

> 本批交付模式为无头（`工具-opener批处理执行v2.ps1 -Detach`），本节只作切回「看护者单粘贴」档时的逃生口保留。

```
[OP-0921-G]【CC】看护629续完
【设置】执行环境：CC ｜ 分支：0921-guardian ｜ worktree：☐ 不建 ｜ 工作区：无 ｜ session：新开 ｜ 派出线：环境总线 OP-0921-A ｜ 模型：sonnet
开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Win]0921G-看护629续完。🔴 你是本批唯一真正被粘贴进独立 CC 会话的一份（其余泳道均由你用 Task/Agent 派发，正文里已不再放这一行——2026-09-05 队列 §一 `#487`／(甲)：源头不放，不再指望子任务的文本例外句被真正遵守），本条对你适用，正常执行即可，标题设定后不要再被子任务顶掉，你自己不属于「跳过本行」的例外范围。
读 `1-转型规划/0-全景路线图/看护件-【CC】629缩编续完-2026-09-21.md` 全文＋ `CLAUDE.md` 恢复上下文。

你是本批的**看护者**，不是执行者。用 Task/Agent 工具为该泳道起一个子任务，把【CC · 子任务泳道】opener 的正文原样作为子任务 prompt。🔴 不要改写 opener 正文。🔴 该泳道续用既有分支与 worktree，不新建。
```

## 四、收工汇总

```
python 0-学习与工具/工具-泳道看护状态机.py summary --batch B-0921_629续完
```
