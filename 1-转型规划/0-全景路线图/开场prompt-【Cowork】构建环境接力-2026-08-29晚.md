---
title: "开场prompt-【Cowork】构建环境线接力（OP-0830-A，定长交接卡）"
created: 2026-08-30
执行方: Cowork（构建环境方案线，A 类同线转场）
来源: 2026-08-29/30 OP-0829-W 收工（四问定夺全落地＋WIP 分诊改判 8 行＋#435 可派）
status: 待执行
编号: OP-0830-A
---

### A ·【Cowork】构建环境线接力（A 类同线转场）

**开场词（复制即用，▶ 粘贴端：Cowork 新会话）**：

```
环境接力-OP0830A
【设置】执行环境：Cowork ｜ CC session：☐ 无（此「无」只表本任务默认不进 CC——派 CC 活走 offlan清池 或出 opener） ｜ worktree：无 ｜ 分支：master（只改 md，收工登 §二 批次） ｜ 工作区：无 ｜ 派出线：Cowork 构建环境方案线（OP-0830-A）
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\开场prompt-【Cowork】构建环境接力-2026-08-29晚.md ＋ CLAUDE.md 当前进度恢复上下文；先按本文件 §自检三查，通过即直接开工，有差异再问。
```

## 【下一会话主攻】

**把 `#435` 派出去 —— 一次粘贴的事，别重做前面任何一步。**

`#435` 已 `[S:open]` 待领、design 审过、派单件已转「✅ 可派出」、`openspec validate --strict` 绿、WIP 20／22 有余量。**本线动作＝把派单件里的开场词交出去**（走 `offlan清池` 排波，或直接给他一句粘贴），然后本卡即完。

🔴 **三条不要做**：① 不要重开 design 审（D1-D7 已审过）；② 不要另起 root 瘦身棒（他已答定夺 3(a)，≤40 KB 人设目标撤销，体积改由 `#435` 棘轮持有）；③ 不要再做一次 WIP 分诊（2026-08-30 已做，机制化已并入 `#435` 子项 E）。

## 【状态快照 · 2026-08-30 收工（OP-0829-W）】

- **他答的四问＋两次追加，全部落地**：1(a) 授权直改／2(b) Obsidian 段整段删／3(a) 撤 40KB 目标改棘轮／4(a) 建巡检走 openspec ／「审过」／「全批＋并入 `#435`」。
- **本机 `~/.claude/CLAUDE.md`**：9,875 → 9,382 B（路径写实／版本快照行删＋补判据／Obsidian 段连带明文 token 整段删）；动前备份留哈希，改后回读八查全过。
- **root `CLAUDE.md`**：48,173 → 47,863 B（§2 阶段框架条指针化）。
- **WIP 分诊**：按 8 行**各自的书面自陈**改判状态字段（`#282`/`#413`/`#419`/`#398`/`#380`/`#387`/`#399` → `blocked`，`#98` → `timed=2026-09-25`），**27 → 19**，`#435` 摘 🛑 后 20／22。
- **🔑 本役四条判据**：① **判「单侧覆盖算不算半覆盖」只问一句——*没有这道门的那一侧，能不能做出这道门要拦的那个动作？*** 答「不能」即半径匹配。② **目标若既无单一可信源、又与机器阈值脱钩，就会被每一棒重新发现一次**（`OP-0829-C` 与 `OP-0829-W` 各自独立算出同一个「够不到」）——修法是把它变成机器持有的那个数。③ **`partial` 被当作可动 ⇒ 活干得越多 WIP 越高**，是反向激励；闸早留了 `blocked`／`timed=` 出口，**缺的不是机制，是没人做分诊**。④ **`blocked` ≠ 🛑**——都不计入 WIP，但 🛑 的语义是「勿在周计划／巡逻／审计里再催」（`#138` 原话），会让该被催的行整个消失。

## 【悬置项（跨会话不丢，载体在队列）】

1. **🔴 他的两条凭证尾巴（本线不代办，见 §四 #139）**：备份件 `~/.claude/CLAUDE.md.bak-20260829-2243` 仍含那个 Bearer token，核完请删；该 token 已多次进上下文，建议 Obsidian 侧轮换；
2. lan-closeout tasks 2.1/2.2 接线＋3.1 首跑灰度——待他真实回 LAN 说「我已回Lan」（**改判后 `#380`/`#387`/`#399` 三条 LAN 阻塞行正等这一棒**）；
3. `#422` §一ter 心跳推企微 vs 08-28「不用大动」冲突疑虑——本线欠他一次取证后以选择题重提；
4. `editlock-write-side-date-guard` 变更包 design 审未交他；
5. 防外改哈希基线哨兵实现（`#138` 行内两层方案，归 CC 班/清池）；
6. followup-letter 已安装版 save_skill 同步＋`#434`④；
7. `#136` 行内 4 例既有 lint 红与 openspec 一问（值周）；
8. **`#433` 状态列疑似漏销**（卡记「三棒 ✅」、队列实为 `[S:open]` 且只见 A1）——对账审计第 1 类失真，留值周按机制修，不裸手改。

## 【自检三查（A 类，通过即开工不提问）】

① 本卡日期＝上次收工日（2026-08-30）？② 抽 §一 `#435`，核状态列是否已被人认领（已认领则本卡主攻已完成，直接转悬置项）；③ `grep` 本文件「⏳ 队列更新待补」应零命中（§自检三查这一行本身除外）。

## 【纪律段（指针）】

队列开工必读收工必写；**锁：`acquire --reserve` 取号，别手工 +1；release 必查退出码，且不要用管道吃掉它**（`OP-0829-W` 两处均已实证栽过）；报告顶部定夺汇总行＋4.1 路由；单路径零附带条件；**日期本机 `Get-Date` 当场取**（本役实测跨日：22:42 还是 08-29，06:01 已是 08-30）；历史不追改；机器守判据以代码为准、正文只留指针；**改本机 Claude 配置层须他逐次授权，且动前必备份留哈希、动后必回读抽验中文完整性**。收工段：文件清单＋建议 commit message＋§二 批次＋一句话交 CC（或 sweep 自动收）。
