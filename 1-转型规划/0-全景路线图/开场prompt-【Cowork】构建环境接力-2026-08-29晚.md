---
title: "开场prompt-【Cowork】构建环境线接力（OP-0830-E，定长交接卡）"
created: 2026-08-30
执行方: Cowork（构建环境方案线，A 类同线转场）
来源: 2026-08-30 OP-0829-W 收工（C/D 两件 CC 已跑完，看板半已收口，队列已当场回补）
status: 待执行
编号: OP-0830-E
---

### E ·【Cowork】构建环境线接力（A 类同线转场）

**开场词（复制即用，▶ 粘贴端：Cowork 新会话）**：

```
环境接力-OP0830E
【设置】执行环境：Cowork ｜ CC session：☐ 无（此「无」只表本任务默认不进 CC——派 CC 活走 offlan清池 或出 opener） ｜ worktree：无 ｜ 分支：master（只改 md，收工登 §二 批次） ｜ 工作区：无 ｜ 派出线：Cowork 构建环境方案线（OP-0830-E）
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\开场prompt-【Cowork】构建环境接力-2026-08-29晚.md ＋ CLAUDE.md 当前进度恢复上下文；先按本文件 §自检三查，通过即直接开工，有差异再问。
```

## 【下一会话主攻】

**`#437`（opener 守卫挂 release 咽喉）已可派 —— 出 opener 交出去，本卡即完。**

`#437` 已 `[S:open]` 待领（2026-08-30 摘 🛑，`#435` 已归档、触碰区解除），WIP 21／22。**须先出 openspec 变更包＋design 审**（它改 release 会新增拒绝分支，命中门槛第 ③ 条），再派 CC。

🔴 **不要做**：① 不重开 `#435`／`#312` 的 design；② 不另起 root 瘦身棒；③ 不再做 WIP 分诊；④ `#312` tasks 3.2 那次真跑**要他本人开看板读数**，本线代替不了。

## 【状态快照 · 2026-08-30 收工（OP-0829-W）】

- **C（`#435`）完整闭环**：六件同车已交付、`[S:done]`、**已归档**，未推送数 0。
- **D（`#312`）代码已入 master**（`924c1b9`），CC 半全绿（658 passed／1 skipped，零回归）；**看板半本线已改完**，余 tasks 3.2 一次真跑、4.4 暂不归档（观察窗口 3 天，至 2026-09-02，理由已写进 tasks.md 机器认得的位置）。
- **D 的真实数据验证发现比预想更大**：不止 `#435` 一例，`#410`／`#425`／`#418` 此前也被同一份 Cowork 接力卡误配 opener ⇒ **是一类结构性缺陷，不是孤立事故**。
- 🔑 **本役五条判据**：① 判「单侧覆盖算不算半覆盖」只问——*没有这道门的那一侧，能不能做出这道门要拦的那个动作？* ② 目标若既无单一可信源、又与机器阈值脱钩，就会被每一棒重新发现一次。③ `partial` 被当作可动 ⇒ 活干得越多 WIP 越高，是反向激励。④ `blocked` ≠ 🛑（都不计入 WIP，但 🛑 会让该行从催办面消失）。⑤ 🆕 **给带前缀解析的实现加新前缀字段时，必须找出所有「剥前缀」的地方——判据处会报错，剥前缀处只会静默归错桶。**

## 【悬置项（跨会话不丢，载体在队列）】

1. **`OP-0830-B` 已确认是活的 Cowork 会话**（2026-08-30 13:5x 持队列锁改 `#312` 子项三「定时清池分层」）——**不是幽灵**。但 `#436` 的「登记」列仍为空、来源不可追的机制缺口未修（已登记 `#436`，建议把「新行『登记』列非空」加进 release 结构检查）；
2. 他的凭证尾巴已闭（三个 .bak 已删、Obsidian 侧已轮换）；
3. lan-closeout tasks 2.1/2.2＋3.1 首跑灰度——待他真实回 LAN；
4. `#422` 心跳推企微冲突疑虑——本线欠他一次取证后以选择题重提；
5. `editlock-write-side-date-guard` 变更包 design 审未交他；
6. （已升为本卡主攻，见上）；
7. `#136` 行内 4 例既有 lint 红与 openspec 一问（值周）；
8. `#433` 状态列疑似漏销——留值周对账按机制修。

## 【自检三查（A 类，通过即开工不提问）】

① 本卡日期＝上次收工日（2026-08-30）？② `grep` 本文件「⏳ 队列更新待补」应零命中（本行文字除外）——`OP-0829-W` 收工时那两条**已当场回补进队列**，未留待补；③ 抽 `#312`／`#437` 两行，核 `#437` 是否已被认领（已认领则本卡主攻已完成，直接转悬置项）。

## 【纪律段（指针）】

🔴 **凡不需要提权、你自己能执行的动作，直接执行，不得让他开终端手动跑**（他 2026-08-30 明示，正本＝§四 `#141`）——`git pull/push/fetch`、跑仓库内脚本与 lint、编辑锁 CLI、只读探针，一律自己做完再报。**仍须他动手的只有三类，且每次须写明属于哪一类**：① Claude 硬边界（永久删除／输入凭证／改系统设置，即便他授权也不做）② 本机分类器拦下的 ③ 需他在场判断的生产动作。判别一问：*我做不了，还是我只是没去做？*
🔴 **无用产物一律清掉**（同 `#141` ⑵）：能交机制的交机制，机制收不了的当场给删除命令，**不留在原地当噪音**；判据＝留着会不会被某个机制误报。
队列开工必读收工必写；**锁：`acquire --reserve` 取号别手工 +1；release 必查退出码、不要用管道吃掉它；锁被他人持有时不抢、按提示写待补**（本役三条均已实证）；报告顶部定夺汇总行＋4.1 路由；单路径零附带条件；**日期本机 `Get-Date` 当场取**；历史不追改；机器守判据以代码为准；**改本机仓库外载体（全局 CLAUDE.md／artifact 源码）须动前备份留哈希、动后回读抽验中文完整性**。收工段：文件清单＋建议 commit message＋§二 批次＋一句话交 CC（或 sweep 自动收）。
