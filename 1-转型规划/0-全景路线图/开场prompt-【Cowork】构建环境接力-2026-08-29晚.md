---
title: "开场prompt-【Cowork】构建环境线接力（OP-0829-X，定长交接卡）"
created: 2026-08-29
执行方: Cowork（构建环境方案线，A 类同线转场）
来源: 2026-08-29 OP-0829-W 收工（深瘦身第三批 ＋ 全局 CLAUDE.md 零基审计）
status: 待执行
编号: OP-0829-X
---

### X ·【Cowork】构建环境线接力（A 类同线转场）

**开场词（复制即用，▶ 粘贴端：Cowork 新会话）**：

```
环境接力-OP0829X
【设置】执行环境：Cowork ｜ CC session：☐ 无（此「无」只表本任务默认不进 CC——派 CC 活走 offlan清池 或出 opener） ｜ worktree：无 ｜ 分支：master（只改 md，收工登 §二 批次） ｜ 工作区：无 ｜ 派出线：Cowork 构建环境方案线（OP-0829-X）
读 C:\Dev\zhuopin-ai\1-转型规划\0-全景路线图\开场prompt-【Cowork】构建环境接力-2026-08-29晚.md ＋ CLAUDE.md 当前进度恢复上下文；先按本文件 §自检三查，通过即直接开工，有差异再问。
```

## 【下一会话主攻】

🔴 **本卡没有可自行推进的主攻——两项都卡在他的一次答复上，答复到位前不要另起瘦身棒。**

1. **§四 #139（全局 `~/.claude/CLAUDE.md`）**：修订稿已备，等他二选一（授权本线直改／他自行覆盖）。**得到授权即执行 §四①②③ 三块，token 那处不碰。** 稿件＝`全局CLAUDE零基审计与修订稿-2026-08-29.md`。
2. **§四 #140（root ≤40 KB 目标）**：`OP-0829-W` 已取证该目标在本线约束内不可达，三选一待答（建议 (a) 撤销硬目标、跟随 `#381` 自然到位）。**答 (a) ⇒ 本线不再单独排瘦身棒**；答 (b)/(c) ⇒ 按其点名范围另立行。

## 【状态快照 · 2026-08-29 收工（OP-0829-W）】

- **做成的**：root `CLAUDE.md` §2 阶段框架条压缩指针化，**48,173 → 47,863 B（−310 B）**，J1 承接＝全景规划 §0.1／§加速启动总览排期表／§0.2；全局 `~/.claude/CLAUDE.md` 零基审计出稿（**实测 9,875 B，非交接卡记的 5.7 KB**）。
- **🔑 本役最该记住的一条**：**没有任何机器判据认为 root 现在超限** —— sweep 阈值 92,160 B（用掉 52%）、lint 开场预算 120,000 B（含交接卡 47%）。「≤40 KB」是人设目标、无机器背书；而 §5 ＝ 35,089 B（73%）是 `#381` 在办触碰区，动不得。**⇒ 瘦身的真实瓶颈不在字节，在「留」类判据本身**（`OP-0829-C` §四 早已论证，本役以算术复证）。
- **不动的**：§5 全段（`#381` 圈占）；§2 SC1/SC8 条（改它触发排期同步纪律，非瘦身棒可单方处置）；全局文件里的 Bearer token（属他的凭证处置）。
- 批次 `B-0829_30` 已登记 §二 待 commit，交 sweep 落库；编辑锁已 release（exit 0）。

## 【悬置项（跨会话不丢，载体在队列）】

1. **§四 #139 / #140 —— 本卡两项主攻，等他答**（详见上）；
2. lan-closeout tasks 2.1/2.2 接线＋3.1 首跑灰度——待他真实回 LAN 说「我已回Lan」；
3. `#422` §一ter 心跳推企微 vs 08-28「不用大动」冲突疑虑——**本线欠他一次取证后以选择题重提**（已问未答不得烂尾）；
4. `editlock-write-side-date-guard` 变更包（H1 承接体）design 审未交他——随下批提请；
5. 防外改哈希基线哨兵实现（`#138` 行内两层方案，归 CC 班/清池）；
6. followup-letter 已安装版 save_skill 同步＋`#434`④（Cowork 班件，信件线重启前做）；
7. `#136` 行内 4 例既有 lint 红与 openspec 一问（值周）；
8. **`#433` 状态列疑似漏销**：OP-0829-W 卡记「方案三棒 ✅」，队列状态列开头实为 `[S:open]` 且只见 A1 认领/收工、无 A2/A3 回写 —— 属对账审计七类失真第 1 类，**留给值周对账按机制修，不裸手改**。

## 【自检三查（A 类，通过即开工不提问）】

① 本卡日期＝上次收工日（2026-08-29）？② 抽 §四 #139／#140 两行，核是否已有他的答复（有则按答复开工，无则本卡两项主攻均不可推进）；③ `grep` 本文件「⏳ 队列更新待补」应零命中（§自检三查这一行本身除外）。

## 【纪律段（指针）】

队列开工必读收工必写；**锁：`acquire --reserve` 取号，别手工 +1（OP-0829-W 因此触发预留豁免留痕）；release 必查退出码，且不要用管道吃掉它**；报告顶部定夺汇总行＋4.1 路由；单路径零附带条件；日期本机 `Get-Date`；历史不追改；机器守判据以代码为准、正文只留指针。收工段：文件清单＋建议 commit message＋§二 批次＋一句话交 CC（或 sweep 自动收）。
