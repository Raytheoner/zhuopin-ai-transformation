---
title: 【CC】ops/wecom-service-home worktree 同步与常驻服务重启（队列 §一 #335）
created: 2026-08-13
updated: 2026-08-13
执行环境: CC
status: 待发
来源: Cowork 业务总线 2026-08-13（Shao Peishen 当日会话内明示「开 CC 端同步」；缺口系发信前置取证时实测撞见）
---

# 开场prompt ·【CC】ops/wecom-service-home 同步与常驻重启 · 队列 §一 #335 · 2026-08-13

## 开场词（复制即用）

```
【设置】执行环境：CC ｜ 分支：从 master 切 ｜ worktree：☑ 新建 `wecom-home-sync-335` ｜ 工作区：企业AI转型 ｜ session：新开
读 1-转型规划/0-全景路线图/开场prompt-【CC】ops-wecom-service-home同步与常驻重启-335-2026-08-13.md ＋ 根 CLAUDE.md 当前进度恢复上下文。领队列 §一 #335，按文件 §二 四步执行、§三 逐项自检、§四 收工。范围已在 §一 收窄，§五 列了明确不做的事。
```

---

## §一 🔴 本轮范围（先读这段，它主要说明**你不做什么**）

**本轮只做一件事：把 `ops/wecom-service-home` 这个部署载体同步到 master，并让常驻服务真正跑上新码。**

| 做 | 不做 |
|---|---|
| ff-merge 该 worktree 到 master、worktree 内独立跑全量回归、重启 `ZhuopinAibotDevListener`、核心跳戳确认真实建连 | ❌ **不建常驻告警** —— "是否要让 sweep 尾部比对该 worktree 与 master 的差距并超阈值告警"是**机制取舍**，见 §四 第 3 项：只给结论与建议，**不动手实现** |
| 把 §四 那个取舍问题的结论带回来 | ❌ **不改任何 `wecom-aibot-service` 业务代码** —— 本轮是部署同步，不是建造 |
| | ❌ **不碰主工作区** —— 主工作区今天已有多条提交落库且 `origin/master...master` 为 0/0，别去 rebase/reset 它 |
| | ❌ **不碰今天已发出的两封跟进信与 README** —— IT部#8／采购部#14 已于 2026-08-13 06:57／06:58 UTC 真实送达，README 已回填 `✅ 已推送`；**那两行是已发生的事实，任何情况下不要改** |

---

## §二 四步（照做，每步都有已知坑）

### 步骤 0 · 先重测一遍差距，别用本文件里的数字

本文件写作时实测 `git rev-list --count HEAD..master` ＝ **56**；**你开工时这个数字一定不同**（master 还在前进）。**以你当场测到的为准**，并把新数字写进收工回写。

```powershell
cd "C:\Users\Paul Shao\OneDrive\Projects\企业AI转型\.claude\worktrees\wecom-service-home"
git rev-parse --abbrev-ref HEAD          # 应为 ops/wecom-service-home
git rev-list --count HEAD..master        # 落后数
git status --porcelain                   # 必须先确认工作区干净，有脏改动先查清来源再动
```

### 步骤 1 · ff-merge 对齐

**必须是 ff-only**（CLAUDE.md 合并策略）。若报非快进，**停手**——说明该 worktree 上有本地提交，先查清是什么，不要强推、不要 reset。

### 步骤 2 · 在该 worktree 内独立复跑全量回归

**关键：必须在这个 worktree 里跑，不能拿主工作区的绿当数**——这正是队列 #300 的教训（本机无 venv、多 worktree 共享全局 `site-packages`，`pip install -e` 会把"哪份源码是权威"写成全机唯一指针）。`tests/conftest.py` 已内置路径引导，直接 `pytest` 即可。

基准：`wecom-aibot-service` **357 passed + 1 skipped**（2026-08-09 记录值，**若你对齐后的 master 已改变基准，以 CI 矩阵口径为准，不要为了对上旧数字去改测试**）。

### 步骤 3 · 重启常驻监听并验心跳

🔴 **已知坑（#310 实测记过，别重蹈）**：`Stop-ScheduledTask` **会遗留孤儿子进程**——停完必须手工核对并补杀，否则新进程起来了、旧进程还占着连接，看着"重启成功"其实没有。

**验收不是"进程在"，是"心跳戳时刻是重启之后的"**：核 `reports/wecom_aibot_audit.jsonl` 里 `connection_established` / `authenticated` 事件的时刻晚于你的重启时刻，才算真实建连。⚠️ **时间戳先判 UTC 还是本地**（审计文件里是带 `+08:00` 的本地偏移，`LastRunTime` 是本地）。

### 步骤 4 · 顺带核一件（只读，不修）

本次滞留未生效的是 `818a6ab`（队列 #333②③ 企微机器人 release 豁免预留归属校验 ＋ 被拒绝时补审计与告警）。重启后**只读确认**该逻辑已在生产码里（如 grep 到对应函数即可），**不要为验证它去人为构造一次豁免场景**——那会往真实队列写测试数据。

---

## §三 自检项（逐条对）

1. 该 worktree `git rev-list --count HEAD..master` ＝ **0**。
2. worktree 内 `wecom-aibot-service` 全量回归**零漂移**（passed/skipped 数与基准一致，或差异有明确来源说明）。
3. `ZhuopinAibotDevListener` 重启后**无孤儿子进程残留**（进程列表实查，不是假设）。
4. 心跳/建连事件时刻**晚于重启时刻**，且已显式标注该时刻是 UTC 还是本地。
5. 主工作区 `origin/master...master` 仍为 **0 0**（你没顺手动它）。
6. README 里 IT部#8／采购部#14 两行仍是 `✅ 已推送 2026-08-13 06:57／06:58 UTC`，**一字未变**。

---

## §四 收工段

1. **回写队列 §一 #335**：写明你当场测到的落后数、回归结果、重启与心跳证据、步骤 4 的只读确认结论。达成 §三 六项即可转 `[S:done]`。
2. **登记 §二 批次**（CC 可自行 commit + push，但批次仍要登记）；文件清单逐个写完整路径且**必须含队列文件自身**；状态列**禁带 ✅**。
   🔴 **第 3 个 cell 是「建议 commit message」不是「说明」** —— sweep 取的是该 cell 里**第一个反引号包裹的字符串**当 commit 主题。
   🔴 **`--cell` 值里含反引号时不要走 bash 传参** —— 2026-08-13 本行的上游批次实测踩过：反引号被 shell 当成命令替换执行，工具报"✓ 已追加一行"但**多处内容被静默清空**，退出码 0、列数正确，只有内容不是你写的那份。改用编辑器直接写行。
3. 🔑 **带回一个结论（不动手实现）**：#335 行里留了一个问题——**#310 刚在 2026-08-09 做过同样四步并标 done，四天后又落后几十个提交**，说明这不是一次性债，是"master 每前进就自动产生"的结构性缺口，而目前无任何机制盯它（月度体检 #98 的②只核"存活"不核"新旧"）。**是否该建常驻告警（如落库 sweep 尾部比对差距、超阈值即报）？** 给出你的建议与代价分析即可。
   ⚠️ **若你判断该建**：那属**新增守卫**，须走 openspec（CLAUDE.md §5 机制/工具类触发门槛①"改变全项目口径"），且 `openspec/config.yaml` 的守卫退休问询规则要求同批回答"**本次退休哪个既有守卫**"。**本轮不要顺手把它做了。**
4. 收工重跑台账。

---

## §五 本轮不处理但你会看到的（避免误捡）

- **主工作区今天有 5 个批次已落库**（两封回信起草／docx 拍板落档／skill v3.2 同步／定时任务死指针修复／两封回信送达收口），均已 push、分叉 0/0，**不需要你做任何事**。
- **队列 §一 #334／#266／#118** —— 采购域三行，定级排期归 **2026-08-17 值周巡检**，不属本轮。
- **机制类可动 WIP 当前 23／16 已超上限**（协议〇.9 措施 C，非阻断）——#335 是在明知超限下新增的，理由已写在行内；**本轮收口它正好回落一格**。
