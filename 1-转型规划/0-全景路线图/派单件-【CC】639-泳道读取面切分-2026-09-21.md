---
title: 派单件 · 【CC】`#639` 泳道读取面切分（峰值主导项）＋ 上下文闸轮询低估
created: 2026-09-21
派出线: 环境总线 Win-0921-A
批次: B-0921_止血
任务类: A 类
---

# 派单件 · 【CC】`#639` 泳道读取面切分（§一 #639）

🔴 **先现取行原文**：`python 0-学习与工具/工具-队列查询.py --row 639`。**禁裸 `Read`／`grep` 队列真身。**
🔴 **本棒串在 `#637`＋`#638` 那一棒之后**（同泳道、同触碰 `test_工具-落库sweep.py`）。起跑时 `#637` 那条分支可能尚未 ff——**从 master 起分支，不要去合它**，两条各自 push，ff 先后由看护者串行做。

## 〇、方案闸（已过）

1. **判据粒度**：体量一律 `git cat-file -s <rev>:<路径>` 取 **blob 字节**——**不要用磁盘 `.Length`**，CRLF 会让同一份文件看起来多出几十到上百字节（2026-09-21 实测已骗过一次：根 `CLAUDE.md` 磁盘 12,146 vs blob 12,077）。
2. **narrow**：回归只跑受影响测试类 node-id，禁 `-k`，禁整份回归；输出重定向到文件只读结论行。
3. **禁起后台子任务／并行 Agent**（`OP-0921-C` 被 600 秒上限掐断，一棒白跑）。顺序、inline 做完。
4. **产出落点**：改动留本泳道分支，不 ff master。

## 一、要解决的事（`#627` 效果验收的反向结论）

2026-09-21 实测：四条泳道的派单件全是薄件（4,380／6,282／4,383 B，对照 09-20 那份 24,865 B），峰值 242,594／230,874／212,522／205,114，**中位 221,698**；09-20 上午七条厚件批中位 194,282、最高 244,434 ⇒ **最高持平、中位反而抬高约 14%**。**切薄派单件没有把峰值压下来。**

**真正的主导项＝泳道一次整读的几个大文件**（现取 blob 字节）：

| 文件 | 字节 |
|---|---|
| `0-学习与工具/工具-落库sweep.py` | 555,367 |
| `0-学习与工具/test_工具-落库sweep.py` | 534,849 |
| `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md` | 539,716 |

🔴 **本棒不是去「拆散这三个文件」**（那是大重构，会撞一切在飞分支）。本棒要造的是**让泳道不必整读它们的手段**，至少做到：

1. **一个只出结论面的读取器**——按类名／函数名／告警类号定位，只回吐该段 ±N 行与表头，形状照既有 `工具-main-leak回放.py`（该工具实测把 15 份补丁从 69.7 KB 压到 1.8 KB）。
2. **把它接进派单件模板与 opener 固定尾句**，让泳道**默认走定位读、整读要给理由**——机器守，不是再写一条纪律。守要**自带消费者**，落点建议 `工具-opener块lint.py` 形态守或 sweep 常驻告警类。
3. 🔴 **新增常驻告警类须同批给出沙箱夹具**（`#631` 血泪）。

## 二、同批并入：上下文闸 15 秒轮询有系统性低估（Shao Peishen 2026-09-21 答 `2a` 并入本行）

`hooks-posttooluse-context-meter` 每 15 秒写一次 `reports/context-meter/<sid>.json` 的 `lastContext`，`工具-opener批处理执行v2.ps1` v2.6 据此判 150k／250k。**实测同一条泳道：闸读到 223,309，`工具-Token用量度量.py` 算出 230,874，差 7,565**；四条逐条比对均为闸偏低。
⇒ **真实峰值可能落在两次轮询之间，250k 硬线会漏判。** 修法自定（收紧周期／改记录并读取峰值而非瞬时值／两者并用），但**必须给出改后误差的实测数**，不接受自陈。🔴 **阈值 150000／250000 本身是 Shao Peishen 既定政策值，不准改**（改口径阈值属 🟡 档，永不代办）。

## 三、期望产出（缺一个就不许写 `OPENER_DONE`）

1. 定位读取器（或等价手段）＋ 它的单测
2. 把它接进泳道默认路径的**机器守** ＋ 消费者 ＋ 沙箱夹具
3. 上下文闸低估的修法落地，**并给出改后误差实测数**（同一条泳道闸读 vs 度量工具算出）
4. 一句话写清：本棒预期把泳道峰值压下多少（有数就给数，没数就明说没法预估）
5. 收工贴 `git diff --name-only master...HEAD` 原样输出

## 四、不做什么

1. 🔴 **不拆散那三个大文件本体**（会撞在飞分支），本棒只造「不必整读」的手段。
2. 🔴 **不改 150k／250k 阈值**。
3. 🔴 禁起后台子任务／并行 Agent。
4. 🔴 别往 `.claude/rules/` 与根 `CLAUDE.md` 加字（2026-09-21 刚缩到线内：rules 合计 30,310／30,720，根 12,077／12,288）。
5. 🔴 不碰卡在他一字母的行：`#455`／`#481`／`#440`／`#503`／`#439`／`op0910n-529-timeout-semantics`／§四 `#213`／`#208`。
6. 🔴 收工只 push 本泳道分支，不碰主仓、不 ff master；红线三档照旧永不代办。
