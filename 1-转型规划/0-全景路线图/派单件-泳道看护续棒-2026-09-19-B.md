---
title: "派单件-泳道看护续棒-2026-09-19-B"
created: 2026-09-19
执行方: Cowork
来源: OP-0919-J
前一棒: Cowork 泳道看护 OP-0919-A
status: 待执行
---

[OP-0919-J]【Cowork】泳道看护续棒
【设置】执行环境：Cowork ｜ 分支：master ｜ worktree：☐（不建，只产改 `.md`／出看护件转交 CC 建造）｜ 工作区：无 ｜ session：新开 ｜ 派出线：Cowork 泳道看护 OP-0919-A

读 ① 本件全文 → ② `CLAUDE.md` 恢复上下文，按下述执行。本件为 **A 类**（同线转场，口径已定，直接开工，无需再问澄清）。

开工第一件事：用 `python 0-学习与工具/工具-队列查询.py --digest`（业务场景加 `--file 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`）扫两份队列与归档件的摘要，按触碰区关键词核无他人在办行重叠，命中再 `--row N --field all` 展开；🔴 不得 `Read`／`grep` 队列真身（K3 读侧禁通读）；有重叠不得抢领，报总线裁决。

## 前一棒（`OP-0919-A`）为什么交棒

转场判据五条自查，**两条阳性、可指到具体文字**：① **忘记早先事实**——把 `#602` 描述成「待他拍板两问」，而那两问 2026-09-17 已答 `1a 2a`、九文档当日已重排；② **险些重复已做过的工作**——据此差点再跑一次九文档重排。③④⑤ 全阴性（矛盾均已当场更正留痕、指令未丢失、token 压力未到）。🔑 **两条阳性同源：读的是自己写过的东西，不是现取的行文。**

## 做什么（按序，1–2 可并行，3 起串行）

1. **乙档最后两条 ff**（队列 §一 `#617`）：`claude/op0910l-507-sweep-apply` 与 `claude/op0907ai-sweep-manifest-479-apply`。
   - 🔴 **ff 前先 `git worktree remove` 掉同名残留 worktree**——泳道收工不自删是常态（`#576`），不删则 `git worktree add` 撞「branch already used」、脚本只报 `exit=9 脚本异常`（`#618` 的修法已合入 master，本条应已能报出成因，顺手验一次）。
   - 🔴 **测试目标必须收窄**：两条都要 `0-学习与工具/test_工具-落库sweep.py`，**单跑实测 19 分 44 秒、六关双侧 ≈40 分钟**。先试 `pytest -k` 过滤到分支实际触碰的测试类；做不到就照原样后台跑，日志落 `reports/_op0919a/ff-batch5.log`。
   - ⚠️ `op0910l` 曾跑过一次分支侧得 **19 failed／492 passed**，**没跑纯 master 基线，不是结论**——六关④ 判的是「与纯 master 比零新增」，须两侧同命令对跑。
   - ff 完回写 `#507`／`#479` 各自「apply 已合入 master」，并补 `#617` 的乙档收口结论。
2. **`#611` 销号判据的取证**：本日三份 `*-main-leak.patch`（`reports/opener-batch/20260919-085903/` 两份 ＋ `20260919-090057/` 一份）回放，**三条必须全部不再判 FAIL**。🔑 本日已查实一处可当场收口的白名单缺口：白名单收了 `合入登记/pending-ff.jsonl`，**没收同目录、每次 ff 必写的 `合入登记/ff-patrol-<yyyyMMdd>.jsonl`**。
3. ~~**`#604`／`FI6` 阻塞项**：起草 `财务部#20`~~ 🔴 **本条 2026-09-19 由 `OP-0919-J` 现取作废**：`python 0-学习与工具/工具-跟进闸查询.py --to 唐燕萍` 回报 **闸 🔒 锁**（依据＝`财务部#19` 已推送 2026-09-17、在途、闭环形态无标注）⇒ **闸锁状态下不得起草、不得占号**。等 `财务部#19` 闭环后再起。
   🔴 **本条也是前一棒的流程缺陷留痕**：派单件是凭记忆写的、没跑闸查询，若照单执行就会在闸锁下起草。**判词：凡「起草跟进信」类任务，派单件里不能只写「去起草」，必须写「先跑 `工具-跟进闸查询.py --to <收信人>`，闸锁即停」。** 起草本身仍走 `zhuopin-followup-letter`（md+docx 双件），🔴 **发送永不代办**，走 `zhuopin-send-followup` 并等他一句话。
4. **机制类可动 WIP 现取 29／22 超限**（§四 `#208`）：新机制类 🟢 候选一律不开，除非他正式重议上限。

## 不做什么

- 🔴 不重跑重组循环第 12 例（本日已三问全裁全落，`#602` 状态格与变更日志 §四 均有留痕）；**不因前置总表七行的倒排日期去判任何场景的档位**——他 2026-09-19 答 `1a` 已定「八场景一律不按 6/8 周判 D 档」，那些日期**只作参照**。
- 🔴 不代填 §四 `#213` 的七条前置行 Owner（人的属性＝硬事实，须以 `6-人才与组织/人员名录-称谓与性别-正本.md` 为准）；不代对外发送、不碰 L2 人工门禁与合规红线、不碰 ASIL C/D。
- 🔴 不做 `.51` 部署类动作（一律转 `zhuopin-lan-closeout`）；不领 `#539`（与在办的 `#538` 同触碰 SC2 周报页面，已判等 `#538` 收口后再领）。
- 🔴 **不在泳道运行期间往主仓写文件、也不长持队列锁**——本日各撞一次：写文件致第三道闸把看护者自己的产出判成 `main-leak`；持锁致泳道收工写不进队列、只能以 `OPENER_PARTIAL` 收尾等人代补。

## 本机三条必须知道（血换的）

1. 🔴 **起无头批必须用绝对路径 `C:\Tools\pwsh7\pwsh.exe`**——`-Detach` 用 `(Get-Process -Id $PID).Path` 继承调用方 shell，用 `powershell`（5.1）起会当场 `NamedParameterNotFound`、零泳道日志，而父进程照报成功（`#567`）。不传 `-LogDir`。
2. 🔴 **Cowork 挂载侧 `rm` 被沙箱拒**（`Operation not permitted`）⇒ 任何「用文件当锁」的工具从那边跑只能跑一次（`工具-opener生成.py` 第二次必 fail-closed）。**绕行＝改在 Windows 侧跑**；队列锁工具走挂载侧也动辄 120 秒超时，同样改走 Windows 侧。
3. 🔴 **`.git` 一天长一枚陈旧锁**（09-18、09-19 各清一枚）。清前三条判据齐备：`Get-Process git` 为零 ＋ 锁文件 0 字节 ＋ 陈旧超 30 分钟。`#618` 的哨兵已合入 master，下次应由机器管，顺手验一次。

## 收工

产出登记 §二 待 commit 批次（走 `0-学习与工具/工具-共享文档编辑锁.py` 的 `commit-edit`／`commit-append`，勿裸改、勿自行 commit 队列文件），由落库 sweep 取活；若产出看护件转交 CC 无头批，按 `opener骨架.md`「一次粘贴」纪律一次出件，不裸出多份 opener；收工汇总末尾给「待他一字母」清单，同类合并、标〔荐〕与默认项、末行给答复模板，并按 `CLAUDE.md` §3 把进展滚动覆盖进 `1-转型规划/0-全景路线图/session接力-Phase1收口.md`（8 KB 上限）。
