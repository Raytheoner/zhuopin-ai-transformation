---
title: "开场prompt-C1C2真实数据验证-CC Desktop交接-2026-07-15"
created: 2026-07-15
更新: 2026-07-15（①已由沙箱环境补做完成，本文件收窄为②③交接）
执行方: 采购专线（②③是姚祖怡业务判断，非编码任务，走既有"AI起草·专家批改"三步法即可）
来源: 采购专线 2026-07-15 session 收口交接
status: 待执行（部分完成）
---

# C-1/C-2 真实数据验证 · 交接（2026-07-15，②③版）

> **用法**：本文件原计划要求"换到 CC Desktop 长期环境"，但①已在当天补做完成（详见下）。剩下②③是需要姚祖怡本人参与的业务判断，**不需要专门的编码环境**——按项目既有"AI起草·专家批改"三步法处理即可：把下面的素材整理成一份确认稿，经企微机器人/人工发给姚祖怡，等她回复。

## 开场词（复制即用，供 Cowork 起草确认稿用）

```
读 1-转型规划/0-全景路线图/跨桌任务队列.md #33 ＋ 本文件 ＋ openspec/changes/archive/2026-07-15-sc8-baoguan-substitute-partial-kit/design.md"2026-07-15 补充"段 恢复上下文，起草姚祖怡确认稿（②真实数据抽验素材 + ③kittable_shortfall口径一句话确认），走AI起草·专家批改三步法。
```

## 背景一分钟

- 2026-07-15 当天，采购专线在沙箱 worktree 里完成了 C-1/C-2 的 openspec 批2全流程并 apply 归档、commit+push（已在 master）。
- 最初判断"沙箱无 `.env`、连不上内网"，计划把真实数据验证整体交给 Paul 的 CC Desktop 长期环境。**当天晚些时候发现判断有误**：U9C webapi 走的是公网域名 `erp.equalitytec.com:4443`（非内网 IP），沙箱环境实际可达；且沙箱所在机器的主检出目录（`C:\Users\Paul Shao\OneDrive\Projects\企业AI转型\.env`，非本次 worktree 自己的目录）本来就有凭证——于是**①（替代料 DTO 真实字段结构验证）已经在沙箱环境当场补做完成**，不需要再等 CC Desktop 环境。
- **①的验证结果很关键，发现并修正了一处会导致真实误判的 bug**：design.md D2 原假设"替代料自带用量则优先用自己的"是错的——7 母件/20 组真实替代料样本 100% 显示，替代料 DTO 自带的 `m_usageQty` 恒为占位值 `1.0`，与其所属主件行的真实用量（样本中出现 1/2/3/4/9/10/16 等值）完全无关。若不修正，会在主件行真实用量>1 的场景严重低估替代料应有的展开需求量，从而错误得出"现货够、判齐"的结论。已修正 `get_bom_for_products`（恒继承主件行用量）+ 同步测试与 spec，全量回归零漂移，commit+push 完成。详见 `openspec/changes/archive/2026-07-15-sc8-baoguan-substitute-partial-kit/design.md` "2026-07-15 补充"段。
- `192.168.100.51`（保供看板部署服务器）当天仍 ping 不通，与 U9C webapi 是否可达无关（不同服务器）——这个仍需确认是否关机/离线，关联 `#32`（sync-to-server.ps1 真实部署验证），**这个才是真的需要人到现场或有内网访问的机器去处理的事**。

## 剩下要做的两件事（跨桌任务队列 `#33`）

### ② 姚祖怡真实数据抽验

①补做时拿到的 **7 母件/20 组真实替代料样本**（母件：S02Y.0035/S02Y.0162/S04Y.0112/S07Y.0137/S02Y.0188/F02N.0040/F02N.0226）可以直接作为抽验素材，不必重新取数。整理成一份简短确认稿，内容建议：
- 列出几组代表性的替代料判齐结果（主料+替代料现货合计 vs 毛需求），请姚祖怡确认结果符合她的预期；
- 附一两个部分齐套的示例（"可齐套 X / 总需求"+"卡在子件 Rxx、还差 N 件"），请她确认可读、不引起误解。
- 如需真实线上看板画面（而非文字素材），需等 `192.168.100.51` 服务器状态确认后部署最新代码才能看，不阻塞先用文字素材走一轮确认。

### ③ `kittable_shortfall`（还差 N 件）口径一句话确认

目前"还差 N 件"算的是**"凑够下一整套（`kittable_qty+1`）所需的缺口"**，不是"凑够客户下单总量所需的缺口"（两者数值差很多）。这是实现时自己选的推荐口径，没有正式确认过。随②一起问一句就行，不用单独开会。

## 权威依据清单

1. `openspec/changes/archive/2026-07-15-sc8-baoguan-substitute-partial-kit/design.md`（"2026-07-15 补充"段是①验证过程与结论的完整记录）。
2. `4-数字员工/采购部/SC8-客户订单交期智能承诺/CLAUDE.md`（状态时间线 2026-07-15 两行 + "3. 复用底座资产"更新行）。
3. `1-转型规划/0-全景路线图/跨桌任务队列.md` `#33`。

## 收工纪律提醒

- 姚祖怡回复后：队列 `#33` 状态改"完成"+回填结论，SC8 CLAUDE.md 补一行，若回复涉及口径调整需回 design.md 补记；commit+push+收工重跑台账。
- `192.168.100.51` 服务器状态确认后，回填队列 `#32`。
