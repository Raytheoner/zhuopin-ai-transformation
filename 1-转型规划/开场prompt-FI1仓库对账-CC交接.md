---
title: "开场 Prompt · FI1 供应链自动仓库对账（CC 建造会话用）"
created: 2026-06-28
用途: 新开 Claude Code（CC，LAN 建造车间）会话起 FI1 时，复制下方代码块粘入。
说明: FI1 已决策 2026-08 启动/09 试点（复用 SC8 已验证 ZpConnector+U9C webapi + CSV 备选，内部对账先行、委外二期）。
关联: 全景规划 §场景 FI1 ｜ 实施计划 §一财务表/§二 ｜ 跨场景前置数据总表 FI1 行 ｜ 财务部场景开工就绪清单 ｜ 财务部AI对接人-首月任务单（工作信）
---

# 开场 Prompt —— FI1 供应链自动仓库对账（CC 交接）

> 用法：新开 Claude Code 会话，**让 CC 读本文件**（不必整段复制粘贴）。短开场词：
> 「读 `1-转型规划/开场prompt-FI1仓库对账-CC交接.md` + CLAUDE.md 当前进度恢复上下文，按文件里的任务执行；开干前问我 2-3 个澄清。」
> CC = LAN 建造车间（连真实库、写代码、跑 OpenSpec）。下方代码块是给 CC 读的任务全文。

```
【CC 建造任务·FI1 供应链自动仓库对账 —— LAN 执行】

== 先读上下文恢复（按序）==
① CLAUDE.md 当前进度（尤其 §4 底座架构、§5 场景固定流程、§7 红线）
② 全景规划 §场景 FI1（"供应链自动仓库对账"yaml 块：痛点/AI做什么/数据依赖/L2/预期价值）
③ 实施计划（最新版）§一财务表 FI1 行 + §二 timeline 8-9月块
④ 跨场景前置数据总表 FI1 行（启动 08/09 试点、复用 SC8 连接器、委外二期）
⑤ SC8 工程（4-数字员工/采购部/SC8-客户订单交期智能承诺）的 ZpConnector 用法 —— FI1 要搭它的便车

== 本任务定位与范围 ==
FI1 = 财务/供应链交叉场景，2026-06-28 已决策 09→08 启动/09 试点。
【MVP 范围·内部对账先行】只做 SMT 实际投料 vs BOM 理论用量的差异分析与对账：
  - 复用底座 ZpConnector + U9C webapi 读 BOM 理论用量（SC8 已验证那条路）+ ERP 出入库/库存
  - SMT 投料数据：先确认数据源（见下澄清），不通则走 CSV 备选（已授权）
  - 差异自动分类（损耗溢短/来料短缺/管理差异等）+ 生成对账差异报告 + 标记需人工干预异常项
  - 损耗基线模型可留二期，MVP 先把差异算准
【明确不做·委外二期】委外加工商库存对账卡 8/15 商务数据条款，本期不做，留接口位。

== 工作流（OpenSpec，不跳步，CLAUDE.md §5）==
1. 建场景工程 4-数字员工/财务部/FI1-供应链仓库对账/ → pip install -e ../../../5-平台底座/zhuopin_platform
2. openspec init（首次）→ /opsx:propose "FI1 仓库对账·内部对账MVP（SMT投料 vs BOM理论用量差异对账）"
3. ★停下，我审 design.md（技术决策我拍板）★ —— 别直接往下做
4. /opsx:apply → SuperPowers 先写测试再实现
5. 真实数据验证（复用 ZpConnector 读真实 BOM/库存；SMT 投料按澄清结果接真实或 CSV）
6. /opsx:archive → git commit + push

== 红线（建造时守住）==
- 先 mock/脱敏跑通逻辑，再切真实库。
- 所有对账判定 + 差异分类写平台 audit（append-only，可追溯）。
- L2 人工门禁：差异超阈值（金额/比例）必须人工确认，不自动结案。
- OEM 隔离【不适用】本场景：FI1 是供应商/ERP 内部数据，按 CLAUDE.md §4 不强加 OEM 路由。
- 财务红色数据（具体金额）按需脱敏/仅传聚合。

== 开干前问我 2-3 个澄清（建议先问这些）==
1. SMT 实际投料数据从哪来——MES 直连 / U9C ERP / 还是先 ERP 定期 CSV 导出？（决定 connector 还是 CSV loader）
2. BOM 理论用量口径：直接用 SC8 已读的 U9C BOM，损耗率含不含？以谁为准？
3. 差异分类档（损耗溢短/来料短缺/管理差异）的判定规则 + L2 异常阈值，谁定？
   —— 注：财务 AI 对接人 2026-06-29 到位，**到位首周（约 7 月初）出规则+阈值草案**；
      你据草案即可起 propose（7 月初启动规格，不必等定稿）；规则定稿经你的 design 反馈
      迭代、7/31 收口，作为 8 月开发与上线验收基线。对账规则/差异分类/历史样本均由对接人提供与验收。
4. 黄金基准：能否拿 1-2 份历史人工对账结果做回归对比？

== 边界（CC 桌纪律）==
- 不改规划文档（全景规划/实施计划/前置总表/接力）——那是 Cowork 的活。
- 若 FI1 实现时间再变，只在场景进度里记，并提示我"需通知 Cowork 回填路线图"，CC 自己不动规划。
- 本场景自建 CLAUDE.md / 进度笔记，与采购/质量场景分开。
- 收工 git push；开工 git pull。
```
