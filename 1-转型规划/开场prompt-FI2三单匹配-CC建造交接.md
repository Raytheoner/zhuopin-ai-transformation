---
title: "开场 Prompt · FI2 三单匹配自动对账（CC 建造会话用，mock 先行）"
created: 2026-07-06
用途: 新开 Claude Code（CC，LAN 建造车间）起 FI2 时读本文件。财务域 FI1 暂缓后，FI2/FI3 是 2026 唯一落地场景；mock 先行、不等数据闸，赶 9 月。
说明: FI2 = 原月结采购切片"三单匹配"，2026-09 启动、L3→L4 分期。打法照 FI1（临时口径起 propose、复用底座、真实验证待数据闸）。
关联: 全景规划 §2.1.4 FI2 块（0-全景路线图/）｜ 财务域AI场景描述（标准编排）FI2 ｜ FI2-三单匹配口径-mock备料 ｜ FI4-三单匹配-就绪清单与MVP细化（内容即 FI2）｜ FI2-FI3 数据闸推进-核实对照表
---

# 开场 Prompt —— FI2 三单匹配自动对账（CC 建造交接，mock 先行）

> 用法：新开 Claude Code 会话，**让 CC 读本文件**。短开场词：
> 「读 `1-转型规划/开场prompt-FI2三单匹配-CC建造交接.md` + CLAUDE.md 当前进度恢复上下文，按文件执行；开干前问我 2-3 个澄清。」
> CC = LAN 建造车间（连真实库、写代码、跑 OpenSpec）。下方代码块是给 CC 读的任务全文。

```
【CC 建造任务·FI2 三单匹配自动对账 —— mock 先行，LAN 执行】

== 先读上下文恢复（按序）==
① CLAUDE.md 当前进度（§4 底座架构、§5 场景固定流程/会话接力/同步纪律、§7 红线）
② 1-转型规划/0-全景路线图/卓品智能AI转型全景规划.md §2.1.4 FI2 块（痛点/AI做什么/数据依赖/L3→L4/预期价值）
③ 1-转型规划/财务域AI场景描述（标准编排）.md 的 FI2（子场景 FI2-1..FI2-8 + 最优子序 M1-M4）
④ 口径底稿（Cowork 已出，作 propose 输入）：
   - 1-转型规划/FI2-三单匹配口径-mock备料.md（四维匹配键/容差/匹配状态机/9 类根因/mock 表结构）
   - 1-转型规划/FI4-三单匹配-就绪清单与MVP细化.md（文件名沿用专员号，内容即 FI2；数据/规则/治理三就绪 + FI2-A/B MVP 口径）
⑤ FI1 场景工程 4-数字员工/财务部/FI1-供应链仓库对账/ —— 借它的 feed-source 三源抽象 + audit 接线 + ZpConnector 用法（SC8/SC5 已在 LAN 验证 U9C）
⑥ 1-转型规划/session接力-财务域场景落地.md（FI1 暂缓、FI2/FI3 重心、7/15 数据闸现状）

== 本任务定位与范围 ==
FI2 = 财务/采购交叉场景，2026-09 启动，自动化 L3→L4 分期。**mock 先行、不等数据闸**（就是 FI1 打法）。
【MVP 范围】FI2-1 三单数据准备/完整性校验 + FI2-2 PO+GR+INV 明细行**四维匹配**（物料码+数量+金额+税额）：
  - 复用底座：FI1 的 feed-source 三源抽象（mock/csv/u9c）+ 平台 audit + ZpConnector；不重造。
  - **临时口径起 propose**（仿 FI1 的 fi1-temp）：四维容差/分批 FIFO/结果五分类先占位，等唐燕萍规则口径（R1-R6，约 8 月底）替换、不改引擎。
  - OCR（FI2-1 发票解析）未选型前，用 mock 结构化发票数据占位；数据源三态切换不改逻辑。
  - 核心价值点：结果五分类里的**"总额对明细错位"检出**（防成本失真），务必做准。
【后叠·本期不做】FI2-3 税率 / FI2-4 查重 / FI2-5 容差 / FI2-6 退单 / FI2-7 考核 / FI2-8 学习 —— 二期叠加。
【FI3 另起】付款校验依赖 FI2 结果，单独场景，不在本任务。
【FI1 处置】FI1 暂缓=封存：打 tag / README 指针冻结、代码测试留库不删、不推进；confirm.py 的 L2 判例采集模式保留作平台复用件，供 FI2/FI3 用。

== 工作流（OpenSpec，不跳步，CLAUDE.md §5）==
1. 建 4-数字员工/财务部/FI2-三单匹配自动对账/ → pip install -e ../../../5-平台底座/zhuopin_platform
2. openspec init（首次）→ /opsx:propose "FI2 三单匹配·MVP（数据准备 + 明细行四维匹配，mock 先行）"
3. ★停下，Paul 审 design.md（技术决策 Paul 拍板）★ —— 别直接往下做
4. /opsx:apply → SuperPowers 先写测试再实现
5. 真实验证（待数据闸：U9C 财务侧端点/SRM 发票/OCR + 唐燕萍规则口径）—— 未到位前只跑 mock，
   真实小样本按《数据闸推进-核实对照表》回填结果接（CSV 桥接或直读）
6. mock MVP 可先 commit（不 archive）；真实验证通过 + 唐燕萍口径定稿后再 /opsx:archive → push

== 红线（建造时守住）==
- 先 mock/脱敏跑通逻辑，再切真实库。
- 每笔匹配判定 / 分类 / 退回写平台 audit（append-only，可追溯）。
- **L3→L4 分期**：FI2 先 L3（AI 建议/预警，人工确认）跑查准/查全率，达标才升 L4（自动过账/拦截）。MVP=L3。
- 财务红色金额脱敏/仅聚合；OEM 隔离【不适用】财务（供应商/ERP 内部数据）。

== 开干前问 Paul 2-3 个澄清（建议先问）==
1. 现在就用临时口径起 mock 建造，还是等唐燕萍 R1-R6 规则草案（约 8 月底）？（建议：临时口径先起、仿 FI1，才赶得上 9 月）
2. FI2 场景目录名 / 包名确认（FI2-三单匹配自动对账 / 包 fi2_recon？）
3. 直接搬 FI1 的哪些件复用（feed_source 三源 / models / audit 接线 / ZpConnector）？

== 边界（CC 桌纪律）==
- 不改规划文档（全景规划/实施计划/前置总表/接力）——那是 Cowork 的活。
- FI2 实现时间/范围若变，只在场景进度里记，并提示"需通知 Cowork 回填路线图"，CC 自己不动规划。
- 本场景自建 CLAUDE.md（六段式：定位/决策/底座/红线/时间线/依赖），与采购/质量场景分开。
- 开工先 git fsck + pull，收工 push（CLAUDE.md §5 同步纪律）。
```

---
*本 prompt 由 Cowork 财务专线产出。数据闸/OCR 现状与推进见《FI2-FI3 数据闸推进-核实对照表与转发正文》；唐燕萍规则口径进度见财务接力。created 2026-07-06。*
