---
status: 在办
title: "Session 接力（滚动·最新）—— 业务总线（三域合一）"
---

# Session 接力（滚动·最新）—— 业务总线

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工「覆盖」不「追加」。**
> 进度叙事写 CHANGELOG；方法教训写取证知识库；未闭合项写队列行。收工跑 `工具-CLAUDE进度段lint.py`，J6 报超限即当场删回去。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/session接力-业务总线.md` + 根 CLAUDE.md 恢复上下文，按【三】继续；开干前先问我 2-3 个澄清。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0908-T` | 【CC】看护 `B-0908_四泳道机制根治` | 六任务四泳道：`#500`→`#459`／`#503`→`#487`／`#504`／`#476`，逐条见看护件 §二 | 09-08 起 |
| `OP-0908-U` | 【CC】单棒 `#506` SC2 页面基准日冻结 | `4-数字员工/采购部/SC2-采购周报自动生成/`（纯库内，不碰 `.51`） | 09-08 起 |

> 🔴 两者触碰区零重叠（已逐对比对）。**先登、收工划掉，两头都要做。**

## 二、当前状态快照（2026-09-08 `OP-0908-V` 收工实测，手段随句写明）

- **串行闸现取**（`工具-跟进闸查询.py --to`，非复述快照）：姚祖怡 ✅／陈忱 ✅／陈承 ✅／**唐燕萍 🔒**（`财务部#17` 在途）。四人「下一个可用号」＝采购部#22／财务部#18／质量部#14／IT部#12。
- ✅ **`#507` 抢救批已落库、`队列行日志/#502.md` 安全**。🔑 **判据要换**：opener 写的 `git ls-files --error-unmatch` 在本环境给**假阴性**（它读 index，而 index 会被在跑 sweep 写到半途）；**以 `git ls-tree HEAD -- <path>` 为准**，editlock history 亦独立佐证。
- ✅ **`#506` 库内修复完成**（CC `OP-0908-U`，`f716db2`，163 passed 零回归）。✅ **他 09-08 答「合」＝🟡 档已放行**，🔴 **执行者＝CC（持该分支那侧）rebase＋ff，标「已答，勿重做」，本线不 commit/push**。⚠️ **合入 ≠ 解押**：`.51` 仍跑缺陷版，须另起 LAN 收口批部署，再**复跑全量重算、确认页面期次为当周**，`采购部#22` 才解押。
- **`B-0908_四泳道机制根治`**：三条 paused 等他答（`#476`／`#503`／`#504`），其余 resumed。🔴 **由 CC 端出、就在 CC 那边答**，本线只转达。
- 🔴 **Cowork 侧队列锁一律走本机 PowerShell**（`rules/队列与落库.md` 早有此条；本机 `git status` 0.2 秒 vs 沙箱 200s+ 必撞穿工具内建 `timeout=60`）。**JSON 载荷走 base64 过界**（沙箱 `base64 -w0` → 本机 `[IO.File]::WriteAllBytes`）。⚠️ 09-08 因未先读该规则而误判「环境限制无解」，见取证知识库 §2.14。
- **关键路径未变**：`#506` 部署并复跑全量重算、确认页面期次为当周 → `采购部#22` 解押（`#152`＋`#168` 合并信）→ `#499` 对外承接。⏰ 参照 **09-11**；届时若未修好，§四 `#176` 须重新端给他在 (b)/(c) 间选，**不得默认继续等**。

## 三、下一会话主攻（业务总线）

1. ✅ **`#374` 载体改字已全部落地（`OP-0908-V`）** —— 全景规划四处（§0.2 已登）／隔离规范新增 **§7 运维面** ＋V1.5／前置总表三处／质量域重审件 §2.1 追注／两份 docx 重转并回读核串／零残差。tasks 第 3 节除 3.13 外全 [x]。🛑 **未销号，只等 3.13**。
2. ⏰ **开工第一件（他答 2a）：迁行长超限的两行进 `队列行日志/`** —— `#374`（10809 B，已走逃生阀）＋ `#506`（5413 B，仅告警）；上限 4096 B，**须早于 09-11**，届时 lint 转阻断、谁都写不进这两行。
3. 🔴 **`#374` 唯一剩项 ＝ 3.13，他答 1a：并进 `OP-0908-T` 看护批、由 CC 执行**（标「已答，勿重做」，本线只转达）。活＝本包 spec 的运维面 Requirement 补 D4 反向触发条件一句，**改 Requirement 正文须走 design 审**，与该批既有三条同批答。
4. 🔴 **归档次序硬约束，别搞反**：`oem-chroma-ownership-rejudge`（等 3.13）→ `oem-audit-fail-closed`（`#466` 剩③）。反序会用过渡版盖掉定稿版。
5. **质量域 Q2／Q4 已解冻**；补 grill 次序余下 FI5→FI9→SC10→SC11→SC4，**Q2/Q4 不插队**（他答 2a）。
6. **两条未给推荐**：`#117` design 审／`#101⑶` 质量域上溯 OEM 红线。
7. **销售域 6 个＋研发域**：明令暂缓；🔴 **不立行、不排队。**

## 四、在途与等他动作（只写指针）

- **等他推的**：FI8 银行余额取数授权（EE-2）／`#118`⑴ 轮换企微 webhook key（须本人后台重建）／`#81` 改 `/profile.md`。
- **等他一字母**：`B-0908_四泳道机制根治` 三条 design 审（`#503`／`#504`／`#476`）＋ 3.13 —— 🔴 **由 CC 端出、在 CC 那边答**。
- **LAN 收口**：🔴 **`#506` 合入后的 `.51` 部署须另起收口批**（`采购部#22` 解押的必经一步），**不得并进 `B-0908_AL4`**。
- **W6 候选**：`#499`／`#494` 举证路径已核；`#495` 举证路径尚缺，他答 3b＝留给领行者解析。
- **FI2**：`#390`②③ 已完 ①待唐燕萍；`#418`/`#423`/`#424` 代码侧已完、口径待她签认。⚠️ `probe390_out.json` 丢失，起草那信前需重跑探针。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **opener 一律 Read `opener骨架.md` → `工具-opener生成.py` → 过 `opener块lint.py --enforce`**。🔴 **引用版省的是「做什么／不做什么」，不是六字段**——【设置】行（含 `session：新开`）与 CC 的 `set_session_title` 行受众是粘贴的人，做什么的受众是读文件的会话。**编号脱形**：标题只放 OP 短号，批次名尾段用语义词（`opener骨架.md`「编号脱形」节）。
- 🔴 **探本机环境一律本机 PowerShell、禁沙箱 bash**（沙箱盘 git 不可用，队列锁 `release` 必走本机）。
- 🔴 **队列行每段只在写下那刻为真**；信源：机器字段 > `git log --grep` > 生产实测 > 自陈。**工具回显「已改」只证明写发生、不证明写对** ⇒ 写后必 `--row N` 反查（知识库 §2.11）。
- 🔑 **下「不通／坏了／会丢」这种定性前，先找一个能推翻它的对照** ⇒ 全文与两个实例见 `0-学习与工具/取证方法知识库.md` §2.13。
- 🔴 **§二 文件清单里每个反引号串都必须是合格仓库根相对路径**；⚠️ **批次号会撞**，取名前先 grep 当日已用。🆕 **清单里少写高频共享件**（两份队列／接力卡）——它们是 `#507` 那个自锁的成因。
- 🔴 **闸锁即不起草**；**判据／口径／阈值类永不默认生效**。**人的属性查 `人员名录-称谓与性别-正本.md`、禁从名字推断**（姚祖怡男／陈忱女／唐燕萍女／泓钦男／陈承男）——本日 release 的称呼哨兵实拦一次真错。
- **Cowork** 只出 `.md`；🔴 **收工零孤儿脏文件**（登 §二 或 release `--waiver` 逐个点名，**禁泛豁免**）；🔴 CC 未登 §二 时总线代登。

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 任务真身与状态 | `工具-队列查询.py --row N --section 一/四`（🔴 禁 Read／grep 真身） |
| 信状态与闸 ｜ 待问口径点 | `工具-跟进闸查询.py --to <人>` ｜ `6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl` |
| 泳道看护 ｜ 落库为什么没发生 | skill `zhuopin-lane-watch`；`状态机.py show/summary` ｜ `reports/sweep-commit.log`（逐轮警告原文） |
| 取证方法 ｜ opener 格式 | `0-学习与工具/取证方法知识库.md` ｜ `0-全景路线图/opener骨架.md` |
| 权威排期 ｜ 文档在哪 ｜ 本周 | `全景规划.md` §加速启动总览＋§0.2 ｜ `文档台账-自动生成.md` ｜ `本周计划-2026-09-07.md` |
