---
status: 在办
title: "Session 接力（滚动·最新）—— 业务总线（三域合一）"
---

# Session 接力（滚动·最新）—— 业务总线

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工「覆盖」不「追加」。**
> **进度叙事写 CHANGELOG；方法教训写取证知识库；未闭合项写队列行。** 收工跑 `工具-CLAUDE进度段lint.py`，J6 报超限即当场删回去。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/session接力-业务总线.md` + 根 CLAUDE.md 恢复上下文，按【三】继续；开干前先问我 2-3 个澄清。」

## 二、当前状态快照（2026-09-03 实测）

- **全景 35 场景**（五编号空置勿重分配）。**S1 目标 9-11**。
- **在跑服务**（`.51`）：8091 保供看板／8092 命令中心／8093 QD-B／8094 FI2。🔴 **SC2 未部署** ⇒ 生产跑旧码，`采购部#18` 发不出去。
- **串行闸**：唐燕萍 🔒／姚祖怡 🔒（卡的是 `采购部#21` **不是 #20**）／陈忱 **已开**／陈承 ✅／泓钦 ✅。🔴 **唯一权威＝跟进信 README「发送状态」列**，查闸 `工具-跟进闸查询.py`；**队列里的信状态复述是过时快照，不得作判据。**
- 🔴 **第七批 `B-0903_50` 已跑完（首次全自动无头三段串行）**：A1 OK 8.6min／A2 OK 19.2min／**A3 PARTIAL 36.1min**。`master` ＝ **`a30217a`**（已 push）。
  - **已进主干**：采购三包＋SC8 ATP 批2（9 提交）／`criteria_signoff` 底座（G-1 日期校验，用例 45→59）／财务五包迁移引用底座（五场景单测 53→**71**，16 份新主 spec）。
  - **判据 18 条进 18 条出**（G-3 把 `SLOW_MOVING_CRITERIA` 归入，`tasks` 4.3 已由 17 改 18）；🔴 **一条判据值都没填。**
- ⏸ **泳道 `criteria-chain` ＝ paused**，停在 **FI9 `EE-3` 接 `data_isolation_layer` 的 design 审**（五条定夺项，其中 ② 是合规红线）。

## 三、下一会话主攻（业务总线）

1. 🔴 **收 FI9 五条定夺项答复** —— 审读件＝`0-全景路线图/design审读件-FI9-OEM隔离接法-五条定夺项-2026-09-03.md`（逐条已给推荐）。答完跑 `工具-泳道看护状态机.py resume --lane criteria-chain --answer "<...>"` 再起下一段。
   🔴 **② 属合规红线（AI 永不代办）**：高新辅助账要全公司口径、隔离规范 §3.1 是 fail-closed，两者正面相撞；本线只给了分析与三道锁建议，**不得代裁**。
2. 🔴 **两处留步待派单**（可并成一批）：⒜ ①/②/⑤ 的**规范 §2/§3 回写** —— 🔴 **触碰区与 `oem-chroma-ownership-rejudge`（§一 `#466` 未开工）重叠，本线不代改**，建议等其收口后一次改完；⒝ `criteria-signoff-platform` **§5.1 同改根 `CLAUDE.md` §4 ＋ `5-平台底座/CLAUDE.md`**（原文原样下沉关系，**改一处即漂移**），超出 A3 触碰区已留步。
3. **§四 分诊结果待消化**：`reports/section4-triage-2026-09-02.md`（42.8 KB）——**⒜ 真待他答 22 条，含 4 条已逾期**，已按「答完能解锁什么」排序。**本轮未动，优先级最高的未办事项。**
4. **新立两行待派**：`#476`（L2 模型层纪律收编，🛑 **触发条件＝至少两个财务引擎落地**，未满足前认领即违规）／`#477`（FI9 工时系统存在性核实，**问人的事、不是代码任务**）。
5. **销售域 6 个＋研发域**：2026-09-02 明令**暂缓、等他通知**；🔴 **不是立 🛑 排队行，是根本不立。**

## 四、在途与等他动作（只写指针）

- **等他推的**：FI8 银行余额取数授权（EE-2 **他本人推**，属权限非判据）。
- **待机制线**：§四 `#154`（取消 lane-watch `check-timeout`）／§一 `#461`（opener 生成器）／`#464`（`release` 放行缺口 🛑）／`#466`（`OEMRouter` audit fail-closed 落码）／`spec_sync_to_main_specs` 裁 🟢 待回灌 D1 表。
- **待全景路线图线**：移交单 `SC4顺延与前置总表补5行`（**已并入 EE-5/EE-6，SC4 目标月落 2026-11**）／`O1注销并入`。
- **采购**：`采购部#18` 已拍板但**不发**（等 SC2 部署冒烟）／§一 #118 #266 #334 #344。
- **质量**：陈忱闸已开 ⇒ 是否起 `质量部#13` 见 §四 #153；`#431 ⒞` 10 份样本 **09-11 到期，不得早催**。
- **财务**：`财务部#16` 在途（一回件则 `#418`／`#424`／`#390` 一串解锁）。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **起无头泳道前必过三关**：① opener 格式 —— 标题必须 `### A<N> ·`（**`A` 加数字，写 `D1` 全批解析不到 exit 12**）＋ 独立一行 `粘贴端：CC ｜ 泳道：<无空格名>`；② **同一泳道名下按编号串行、不同泳道名并行** ⇒ 有依赖或都合 master 的**必须同名**；③ **先跑 `-DryRun -Yes`**，起跑前唯一能发现格式错的手段。骨架一律照抄模板库 **§〇.00**。
- 🔴 **判「某条 message 在不在某分支上」用 `--grep`／`--contains`，禁用 `-N` 截窗口** —— A1 一次合 9 个提交，目标那条排第 9 位，`-3` 够不着。**判据没错，取证命令的作用域不够**（"命令什么都没返回，而原因不是那个东西不在"）。
- 🔴 **§二「文件清单」格里每个路径必须用反引号包住** —— 守卫⑹ 走 ``re.findall(r"`([^`]+)`")``，裸路径提取为空 ⇒ 判「孤儿文件」拒绝 release。
- 🔴 **解释一个机制标记时不要写出它的字面值** —— A2 实撞：正文里写出 defer 标记字面值会被 sweep 的 `STALE_CHANGE_DEFER_MARKER` 当成真声明命中、**把整个变更包永久静音**。修法：只引用变量名。**与「PreToolUse 守卫拿受保护通配符匹配整条命令字符串」同族 —— 描述一个标记，被当成了使用它。**
- 🔴 **心跳静默 ≠ 泳道卡住**（A3 静默 30 分钟仍在干活）—— **交叉验证用 worktree 改动数／分支 HEAD／锁占用方**。
- 🔴 **合 master 前看 `merge-base --is-ancestor`**（sweep 每轮推进 master，分叉过夜即 ff-NO）；**合子分支必连带合入其基分支全部提交**，派单前逐条列清交他确认。
- 🔴 **`/opsx:apply` ＝ 实现 tasks 待办**；写 `openspec/specs/` 用 **`sync`**。
- 🔴 **闸锁即不起草**；**触发满足≠可发**。**判据／口径／阈值类永不默认生效** ⇒ 这类定夺**不设默认项**。
- 🔴 **财务建造第一步**：先验税务导出发票号与 U9C `InvoiceNo` **字面严格一致**——对不上 join 全盘落空**且不报错**。
- **Cowork** 只出 `.md`；🔴 **收工零孤儿脏文件**；看护件类批次登记**机制环境**文件。**对外件须扫第三人称代词回名录核对**（**姚祖怡是男性**）。
- 🔑 **rule-of-three 触发的是「该收」不是「现在收」** —— **收晚了是多几份手抄（可批量改），收早了是底座绑死错契约（五场景陪改）。**
- 🔑 **裁决会互相改变彼此的性质**（EE-4 把 `SLOW_MOVING_CRITERIA` 从"前置"变成"判据"）。**答完一条回头看它是否解掉了另一条的前提。**

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 任务真身与状态 | 两份队列；`工具-队列查询.py --row N --section 一`／`--digest` |
| 信状态与闸 | 跟进信 README「发送状态」列（唯一权威）；`工具-跟进闸查询.py --to <人>` |
| 泳道看护 | skill `zhuopin-lane-watch`；`状态机.py show/summary/resume`；起批＝`工具-opener批处理执行v2.ps1`；看护件＝`0-全景路线图/看护件-泳道看护批*.md` |
| 本轮两份审读件 | `0-全景路线图/design审读件-criteria_signoff底座-*.md`（已裁）／`design审读件-FI9-OEM隔离接法-*.md`（**待裁**） |
| 取证方法与坑 | `0-学习与工具/取证方法知识库.md`；opener 格式＝`专线opener模板库.md` **§〇.00** |
| 权威排期／口径 ｜ 文档在哪 | `全景规划.md` §加速启动总览 ＋ §0.2 ｜ `文档台账-自动生成.md`（**别翻目录**） |
