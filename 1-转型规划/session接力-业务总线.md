---
status: 在办
title: "Session 接力（滚动·最新）—— 业务总线（三域合一）"
---

# Session 接力（滚动·最新）—— 业务总线

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工「覆盖」不「追加」。**
> 进度叙事写 CHANGELOG；方法教训写取证知识库；未闭合项写队列行。收工跑 `工具-CLAUDE进度段lint.py`，J6 报超限即当场删回去。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/session接力-业务总线.md` + 根 CLAUDE.md 恢复上下文，按【三】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0911-H` | 【Cowork】业务总线接棒；**窗口标题：`[Win]0911H-业务总线接棒`** | `session接力-业务总线.md`＋`6-人才与组织/部门AI专员跟进/`＋两份队列 §一/§二/§四 | 09-11 18:52 – 在办（三批已登 §二） |

> 🔴 **Cowork 侧 `device_bash` 自 09-08 Windows 更新起挂载失败** ⇒ 一律走 `Windows-MCP PowerShell`；批处理只能用 `pwsh` 起。🔴 **`set_session_title` 在 Cowork 侧不存在**（ToolSearch 关键词两次＋`select:` 精确一次全负；它是 CC 端工具、只在 opener 骨架【CC】变体里）⇒ **窗口标题只能他自己改**。

## 二、当前状态快照（手段随句写明）

- **`master` 现取 `8a87b65`**（较上卡 +7 笔，均 sweep 自动）；🔴 **判落地一律按 `#534` patch-id 口径看产物，不看分支图**。
- **`质量部#14` `🆕 待发`**（`决策点: 9 项`，docx 36 枚 `w14:checkbox`）；回件在第九态桥覆盖面内，不落 `#554` 差集。
- **台账连线机制**：`质量域.jsonl` 9 点；`财务域.jsonl` +9 行（6 点补记 `letters`，新建 `FI10-F18-01/02/03`），`lint_ledger_dir` findings=()。**发出时由 `ledger_writeback.record_letter_sent` 按 `letters` 机器转「在途」**，不需人补。
- **`财务部#18` 已 `🆕 待发`**（`#518` 销号）：FI10 判例批改表 md＋docx，判例 10 条全取自唐燕萍两份底稿真身，`决策点: 9 项`。🔴 **转 `🆕 待发` 只有 `approve_followup_letter.py --quote` 一条路，且有 10 分钟冷却窗（首跑只记时、二跑才放行）。**
- 🆕 **`采购部#22` 补件已起草 `⏳ 待你审`**（09-12）：误判致歉＋七条口径回执＋`#538`/`#539` 进度＋`#499` 顺延。🔴 **补件走《补件登记》表、不占号不占闸**（闸 `--to 姚祖怡` 仍 ✅ 开）；**该表无 CLI 入口**（登记 CLI 只写 `MAIN_TABLE_SECTION`）⇒ 按 `#399` 形态持锁写入；批准／发送用 `--table 补件`，**不进 09:30 班次**。
- 🔴 **行长闸已阻断**：格 >4096 B 先 `工具-队列行K2外置.py --apply` 再把新段并进 JSON `edit-row`（`--verify-json` 报「≠」若差只在首尾空白即属预期）。
- **sweep 第 16 类「未并入分支超阈值」已上线**（判据正本 `工具-未并入分支分类.py`）；🔴 **sweep 不做 ff，主仓 ff 由看护者收工串行或 `工具-待合分支巡检.ps1` 机器做**。
- **他 09-11 的 `2a`／`3a`／`4a` 已回填落库**：§四 `#194` 裁 (a) ⇒ `#538` **插队排在 `#499` 之前**（`#499` 取数层按 §四 `#186` (a) 不变、计算层顺延）；sweep 第 16 类 B 类 13 条**只出清单不合不删**、交 `#521`；`采购部#23` 立 §一 `#557`。`#538` 仍 `partial`——⏭️ `.51` 留 LAN 收口批。
- **`#556` 真因＝off-LAN**（`1ac9fc1`）：🔴 **`Test-NetConnection` 因本机 Mihomo TUN 对任意 IP:port 恒为真、零信息量**，判在网只看真实网段。

## 三、下一会话主攻（业务总线）

1. 🔴 **三件停在他手里**：`质量部#14`（陈忱）与 `财务部#18`（唐燕萍）均 `🆕 待发`，09-14 09:30 同轮派发；🆕 `采购部#22` 补件 `⏳ 待你审`，**批准后走人工触发、不等班次**。发出后分别核闸转 `✅ 已推送`／`✅ 无需回复`、台账对应点转「在途」。
2. **回件到了怎么走**：`财务部#18` 回件 ⇒ 拆件回灌 ⇒ 台账 8 点转 `已签认` ⇒ 才解锁 §一 `#557`（`采购部#23` 会签）；`质量部#14` 回件 ⇒ 第一批验收。**别提前起草。**
3. **业务域 `[S:open]` 待领池**（`--digest` 现取）：`#538`（采购域首位，先交 187 行明细导出＋差异定位；⚠️ `sc2/webapp.py` 无导出能力、须先建）、`#499`（计算层顺延）、`#539` SC2 排版（参考图＝我方 SC8 保供看板样式，已认出、无须再问）、`#494`/`#495`、`#506`、`#463`+14 场景立行（`#467` 最紧）、`#557`（未解锁）。
4. 🔴 **两处回归未整跑不得当已绿**：`test_工具-落库sweep.py` 471 条／`test_工具-opener批处理执行v2.py` 16 条（整跑超单次上限）；**行长⑪ 两条日期炸弹用例**长期红，须改按日期分档断言（`#513` ⑷）。

## 四、在途与等他动作（只写指针）

- **等他一字母**：无（`#553` 已销、两封信已批、`Claude outputs/` 已按他答 `a` 入 `.gitignore`）。
- **等他动作**：§四 `#194`／`#157`（无默认）／`#152`／`#156`／`#170`。**`#193` 已闭合**（三 zip 09-10 自动落盘），勿再引用旧句。
- **⚠️ 权限规则**：`.claude/settings.local.json` 2 条过宽 Bash 允许规则。🔴 **安全设置，本方不代改、只报。**
- **无头档权限 ＝ `acceptEdits`**：`git worktree remove` 被拒 ⇒ 泳道 worktree 需看护者收工手工 `--force` 清。
- **§二 五条旧批次长期被覆盖校验卡死**（每轮 sweep 报「整批跳过」）＝`#507` 形态，环境线在办。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **opener 一律走 `工具-opener生成.py` ＋ `opener块lint.py`，禁手抄**；无头＝`standard`，扇出＝`subtask_lane`。
- 🔴 **§二 文件清单里每个反引号串都必须是合格仓库根相对路径**——OP 号／锚名／批次号放进去即被拒。
- 🔴 **凡「由 X 做」，必须能当场指出 X 的哪一行代码在做；指不出即为空头。** 同族：`UPS5:7`／`#550`／`#553`／`#554`。
- 🔴 **跟进信三查**：闸 `工具-跟进闸查询.py --to`；名录核性别、称呼用全名；代词自检用 Python `re`（bash `grep` 中文按字节匹配出乱码）；docx 用仓库转换器。🆕 **发件侧已挂机器守 `工具-称谓核对.py`**（`#566`，`a63e8a6`）——队列锁 `release` 每次自动对脏信件跑一遍。
- 🔴 **跟进信派发通道与 `.51` 无关**：`dispatch_followup_letters.py` 的 `AibotConnector` 本机直连企微；`#556` 断的是 SC2 周报那条 SMB 中继 ⇒ off-LAN 不挡派发，只需本机开机在线。
- 🔴 **K2 外置只追加、绝不覆盖**；行内留首段＋末段＋指针。
- 🔴 **人的属性查名录正本、禁推断**；称呼 `Shao Peishen`（路径里的 `Paul Shao` 不改）。判据／口径／阈值类**永不默认生效**；决策清单末附**独立代码块**答复模板。
- **Cowork** 只出 `.md`（例外须他明示授权并留痕，如 `B-0911_ClaudeOutputs入表` 改 `.gitignore`）；收工零孤儿脏文件（登 §二 或 `release --waiver` 逐个点名，禁泛豁免）。🆕 **交付／预览通道在仓库根自动生成、库内已有权威件的副本一律入 `.gitignore`**。

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 任务真身 | `工具-队列查询.py --row N --section 一/四`（🔴 禁 Read／grep） |
| 信状态与闸 | `工具-跟进闸查询.py --to <人>` ｜ 台账 `6-人才与组织/部门AI专员跟进/口径点台账/` |
| 泳道／无头棒 | skill `zhuopin-lane-watch`；`reports/opener-batch/<戳>/summary.txt` |
| 落库为什么没发生 | `reports/sweep-commit.log`（sweep 每小时 :17 跑） |
| 取证方法 ｜ opener 格式 | `取证方法知识库.md` ｜ `opener骨架.md` |
| 权威排期 ｜ 文档在哪 | `全景规划.md` §0.2 ｜ `文档台账-自动生成.md` |
