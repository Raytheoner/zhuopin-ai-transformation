---
status: 在办
title: "Session 接力（滚动·最新）—— 业务总线（三域合一）"
---

# Session 接力（滚动·最新）—— 业务总线

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工「覆盖」不「追加」。**
> **进度叙事写 CHANGELOG；方法教训写取证知识库；未闭合项写队列行。** 收工跑 `工具-CLAUDE进度段lint.py`，J6 报超限即当场删回去。
> 三域细节查队列行，本卡只留「谁在等什么」。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/session接力-业务总线.md` + 根 CLAUDE.md 恢复上下文，按【三】继续；开干前先问我 2-3 个澄清。」

## 二、当前状态快照（2026-09-03 实测）

- **全景 35 场景**（五编号空置不得重分配）。**S1 目标 9-11**。
- **在跑服务**（`.51`）：8091 保供看板／8092 命令中心／8093 QD-B／8094 FI2。🔴 **SC2 部署未做** ⇒ 生产跑旧码，`采购部#18` 发不出去。
- **串行闸**：唐燕萍 🔒／姚祖怡 🔒（🔴 卡的是 `采购部#21` **不是 #20**）／陈忱 **已开**（`质量部#12` 已回灌，V1–V5 全 ☑）／陈承 ✅／泓钦 ✅。🔴 **唯一权威＝跟进信 README「发送状态」列**，查闸 `工具-跟进闸查询.py`；**队列里的信状态复述是过时快照，不得作判据。**
- **泳道看护已跑五批**（`B-0902_46`／`_60`／`B-0903_10`／`_20`／`_30`）。🔴 **第五批 `B-0903_30` 在途**（3 泳道 4 项：采购三包 sync＋移交单补充／主 spec 开关标注／底座 `criteria_signoff`→财务五包 sync），下一 session 接管看护。
- **机制类可动 WIP 顶格** ⇒ 新 `[D:机]` 行会被 `release` 拒；按既例把**状态列**正文以 🛑 起首标「排队中」（🔴 判据在**状态**格，不在任务格）。

## 三、下一会话主攻（业务总线）

1. 🔴 **接管第五批泳道看护**：`show` 查状态；🔴 **`resumed` ≠ 已续跑，续跑段须 Cowork 起**（本线已两次栽在这）。预计 `fin-criteria` 的底座 `criteria_signoff` 会停 design 审（命中门槛①改全项目口径）。
2. **§四 分诊结果待消化**：`reports/section4-triage-2026-09-02.md`（42.8 KB）——**⒜ 真待他答 22 条，含 4 条已逾期**。按「答完能解锁什么」排过序，可直接摆定夺。
3. **八场景已立行**（`#467`–`#474` 采购 3＋财务 5，`#475` 芯片价格 API 独立行）。骨架与 openspec 包已建、proposal 已放行；🔴 **判据全落 `None` ＋ CI 用例 `test_unsigned_criteria_stay_none` 守住**——**放行只是可以建骨架，不是可以填口径。**
4. **销售域 6 个＋研发域**：他 2026-09-02 明令**暂缓、等他通知再排序**；🔴 **不是立成 🛑 排队行，是根本不立。**

## 四、在途与等他动作（只写指针）

- **等他推的**：FI8 银行余额取数授权（属**权限非判据**，须财务侧＋CFO 办公室）。
- **待机制线**：§四 `#154`（取消 lane-watch `check-timeout`，判据＝**兜底机制若自己也要人跑，兜的不是底是空气**）／§一 `#461`（opener 生成器，根治六字段漂移＋OP 重号）／`#464`（`release` 放行缺口，🛑 排队）／`spec_sync_to_main_specs` 裁为 🟢 待回灌 D1 表。
- **待全景路线图线**：移交单 `SC4顺延与前置总表补5行`（SC4 定 **2026-11**；前置总表财务域缺 FI5–FI10 五行）／`O1注销并入`。
- **采购**：`采购部#18` 已拍板但**不发**（等 SC2 部署冒烟）／§一 #118 #266 #334 #344。
- **质量**：陈忱闸已开 ⇒ 是否起 `质量部#13` 见 §四 #153；`#431 ⒞` 10 份样本 **09-11 到期，不得早催**。
- **财务**：`财务部#16` 在途（一回件则 `#418`／`#424`／`#390` 一串解锁）。
- ⚠️ **「CC 跑完了」须核三信源**（git＋队列行＋transcript）；🔴 核合入**不认 commit hash**（rebase 必变），认 message。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **opener 一律从模板库 `§〇.00 照抄骨架` 复制，不得从下面各节重建**（2026-09-02 立）。六字段 `执行环境｜分支｜worktree｜工作区｜session｜派出线`；三处最常丢：`worktree：☑/☐` 勾选符、`session：新开` 字面、`set_session_title` 整行含子任务例外句（**缺例外句 `release` 会拒**）。
- 🔴 **`/opsx:apply` 在本仓库＝实现 tasks 待办**；要把 spec 写进 `openspec/specs/` 用 **`sync`**（2026-09-03 实撞）。
- 🔴 **闸锁即不起草**，改登记「待前信闭环后发」。**触发满足≠可发。**
- 🔴 **财务建造第一步别省**：先验税务导出发票号与 U9C `InvoiceNo` **字面严格一致**——对不上 join 全盘落空**且不报错**。
- 🔴 **`.51` 部署是整包同步**，派单件须写死范围。
- **Cowork** ＝ 规划／治理／汇报，只出 `.md`；产出登记 §二 批次（**批次行与队列行是两次独立写入**）；🔴 **收工零孤儿脏文件**。⚠️ 取号会撞（一天撞 8 次），前缀去重会 fail-loud，**改号重试、不要绕**。
- 🔴 **对外件起草自检**：① 扫第三人称代词回名录核对（**姚祖怡是男性**，本线 2026-09-02 又错一次）；② 凡写「见附件」须确认被引对象真在投递清单里。判例批改法＝口径确认唯一格式，**判据／口径／阈值类永不默认生效**。
- 🔑 **今日七次同族失效已落知识库 §2.9**：**命令返回了一个漂亮的数字，而它数的是另一个东西。** 「0／空／全绿」出现时先反问：这条命令的作用域覆盖我要判的那个集合吗？

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 任务真身与状态 | 两份队列；`工具-队列查询.py --row N --section 一`／`--digest` |
| 信状态与闸 | 跟进信 README「发送状态」列（唯一权威）；`工具-跟进闸查询.py --to <人>` |
| 泳道看护 | skill `zhuopin-lane-watch`（规则正本在 `0-学习与工具/skills源码/` 下）；状态 `工具-泳道看护状态机.py show`；看护件＝`0-全景路线图/看护件-泳道看护批*.md` |
| 取证方法与坑 | `0-学习与工具/取证方法知识库.md`；opener 格式＝`0-全景路线图/专线opener模板库.md` **§〇.00** |
| 权威排期／口径 | `卓品智能AI转型全景规划.md` §加速启动总览 ＋ §0.2 |
| 文档在哪 | `文档台账-自动生成.md`（**别翻目录**） |
