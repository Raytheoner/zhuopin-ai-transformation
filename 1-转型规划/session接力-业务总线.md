---
status: 在办
title: "Session 接力（滚动·最新）—— 业务总线（三域合一）"
---

# Session 接力（滚动·最新）—— 业务总线

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工「覆盖」不「追加」。**
> 进度叙事写 CHANGELOG；方法教训写取证知识库；未闭合项写队列行。收工跑 `工具-CLAUDE进度段lint.py`，J6 报超限即当场删回去。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/session接力-业务总线.md` + 根 CLAUDE.md 恢复上下文，按【三】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0911-H` | 【Cowork】业务总线接棒；**窗口标题应为 `[Win]0911H-业务总线接棒`**（须他自己改） | 接力卡＋`6-人才与组织/部门AI专员跟进/`＋两份队列 | 09-11 18:52 – 在办 |

> 🔴 **Cowork 侧 `device_bash` 自 09-08 Windows 更新起挂载失败** ⇒ 走 `Windows-MCP PowerShell`（沙箱 bash 出网 403）；批处理只能 `pwsh` 起。🔴 **`set_session_title` 在 Cowork 侧不存在**（三次精确查全负）⇒ 窗口标题只能他自己改。

> 🔴 **LAN 判据（09-13 实测）**：**TUN 在跑时 TCP connect 被劫、零鉴别力**（伪地址也「连上」）⇒ 判在网只认真实服务响应。**SC2 端点 ＝ `:8096/procurement/sc2`**（`:8092` 是 AI 运营指挥中心，别混用）。⚠️ 扫描器自带 LAN 探针在 TUN 下挂死 ⇒ 加 `--skip-lan-probe`。
> 🔴 **两条 Cowork 通道事实（09-13 实测）**：⑴ `C:\Windows\System32\OpenSSH\` 下二进制**全部起不来**（连 `ssh -V` 也 exit 255／0 字节；`ping`／`git` 同上下文正常）⇒ 用 Git 自带 `C:\Program Files\Git\usr\bin\ssh.exe`（别名 `supplychain-server` 密钥认证本就通）。🔑 **空返回先证量具、再证对象。** ⑵ 桥单次调用 **60 秒硬顶**（timeout 参数无效）⇒ 长步骤用 `Invoke-CimMethod Win32_Process Create` **脱钩启动＋只读轮询**。

## 二、当前状态快照（手段随句写明）

- **`master` 现取 `098dc6b`**（09-14~16 CC 线 +109 笔，主体＝Token 优化 `#594`–`#600`）；🔴 **判落地一律按 `#534` patch-id 口径看产物，不看分支图**。
- 🔴 **`质量部#14` 已回件、卡在我方**：`📨 回件已到，待拆件 2026-09-14T06:25:32Z`。9 项判据类决策点、docx 36 枚真复选框 ⇒ **拆件回灌是本线此刻唯一在手的活**。
- **台账连线**：`财务域.jsonl` 8 点连 `财务部#18`、`质量域.jsonl` 9 点连 `质量部#14`；发出时 `ledger_writeback.record_letter_sent` 按 `letters` 机器转「在途」。
- **`财务部#18` ＝ `✅ 已推送 2026-09-14 01:30 UTC`**（09:30 班次自动发出），在途、无回件，不催。🔴 转 `🆕 待发` 只有 `approve_followup_letter.py --quote`，10 分钟冷却、首跑只记时。
- 🔴 **补件通道**（`采购部#22` 09-12 已发）：**不占号不占闸**，批准／发送带 `--table 补件`、**不进 09:30 班次**，该表**无 CLI 入口**⇒按 `#399` 持锁写入。
- 🔴 **行长闸已阻断**：格 >4096 B 先 `工具-队列行K2外置.py --apply` 再把新段并进 JSON `edit-row`（`--verify-json` 报「≠」若差只在首尾空白即属预期）。
- **sweep 16 类已上线**；🔴 **sweep 不做 ff，主仓 ff 由看护者收工串行或 `工具-待合分支巡检.ps1` 机器做**。
- **09-11 `2a/3a/4a` 已落库**：§四 `#194` 裁 (a) ⇒ `#538` **先于 `#499`**（`#499` 取数层按 `#186` (a) 不变、计算层顺延）；sweep 16 类 B 类 13 条只出清单交 `#521`；🔁 **会签信顺延 ＝ `采购部#24`**（`#557` 已改）。✅ **`#538` `.51` 半边已收口**；他答 `1a` 维持 `partial`，🔑 **销号判据收敛为一条：⑵「差异定位」完成即可销**。

## 三、下一会话主攻（业务总线）

1. 🔴 **拆 `质量部#14` 回件并回灌** —— 四封信 09-13~14 已全部发出（`采购部#23` 明细交付含 3 CSV／`IT部#13` 网关 env 三行／`财务部#18`／`质量部#14`）；陈忱已回，姚祖怡与陈承交期均 09-19、未到期。🔴 **未到「已回件并回灌」前，这四位的下一封都不得起草。**
2. **回件到了怎么走**：`财务部#18` 回件 ⇒ 拆件回灌 ⇒ 台账 8 点转 `已签认` ⇒ 才解锁 §一 `#557`（`采购部#24` 会签）；`质量部#14` 回件 ⇒ 第一批验收。**别提前起草。**
3. **业务域 `[S:open]` 待领池**（`--digest`）：`#538`（余差异定位待姚祖怡回件）、`#499`（计算层顺延）、`#539`（样式＝我方 SC8 保供看板）、`#494`/`#495`、`#463`+14 场景立行（`#467` 最紧）、`#557`（未解锁）。
4. 🔴 **两处回归未整跑不得当已绿**：`test_工具-落库sweep.py` 471 条／`test_工具-opener批处理执行v2.py` 16 条；**行长⑪ 两条日期炸弹用例**长期红（`#513` ⑷）。

## 四、在途与等他动作（只写指针）

- **等他一字母**：无（四封已全部批准并发出）。
- **等他动作**：§四 `#194`／`#157`（无默认）／`#152`／`#156`／`#170`。**`#193` 已闭合**（三 zip 09-10 自动落盘），勿再引用旧句。
- **⚠️ 权限**：`.claude/settings.local.json` 2 条过宽 Bash 规则。🔴 **安全设置，不代改、只报。**
- **无头档权限 ＝ `acceptEdits`**：`git worktree remove` 被拒 ⇒ worktree 待看护者收工 `--force` 清。
- **§二 五条旧批次被覆盖校验卡死** ＝ `#507` 形态，环境线办。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **队列写侧改用复合命令**（`#594`，09-16 CC 线）：编辑锁 `commit-edit`／`commit-append` ＝ acquire→改→release 一条龙，**别再手工三段式**（三段式正是 09-13 那次无锁写入的成因）。
- 🔴 **opener 一律走 `工具-opener生成.py` ＋ `opener块lint.py`，禁手抄**；无头＝`standard`，扇出＝`subtask_lane`。
- 🔴 **§二 文件清单里每个反引号串都必须是合格仓库根相对路径**——OP 号／锚名／批次号放进去即被拒。
- 🔴 **凡「由 X 做」，必须能当场指出 X 的哪一行代码在做；指不出即为空头。** 同族：`UPS5:7`／`#550`／`#553`／`#554`。
- 🔴 **跟进信三查**：闸 `工具-跟进闸查询.py --to`；名录核性别、称呼用全名；代词自检用 Python `re`；docx 用仓库转换器。发件侧已挂机器守 `工具-称谓核对.py`（`#566`），队列锁 `release` 每次自动跑。
- 🔴 **跟进信派发通道与 `.51` 无关**：`AibotConnector` 走公网 `wss://openws…`，off-LAN 可发；`#556` 断的是 SC2 周报那条 SMB 中继。
- 🔴 **K2 外置只追加、绝不覆盖**；行内留首段＋末段＋指针。
- 🔴 **人的属性查名录正本、禁推断**；称呼 `Shao Peishen`（路径里的 `Paul Shao` 不改）。判据／口径／阈值类**永不默认生效**；决策清单末附**独立代码块**答复模板。
- **Cowork** 只出 `.md`（例外须他明示授权并留痕）；收工零孤儿脏文件（登 §二 或 `release --waiver` 逐个点名，禁泛豁免）。**交付通道在仓库根落的副本已入 `.gitignore`**。

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 任务真身 | `工具-队列查询.py --row N --section 一/四`（🔴 禁 Read／grep） |
| 信状态与闸 | `工具-跟进闸查询.py --to <人>` ｜ 台账 `6-人才与组织/部门AI专员跟进/口径点台账/` |
| 泳道／无头棒 | skill `zhuopin-lane-watch`；`reports/opener-batch/<戳>/summary.txt` |
| 落库为什么没发生 | `reports/sweep-commit.log`（sweep 每小时 :17 跑） |
| 取证方法 ｜ opener 格式 | `取证方法知识库.md` ｜ `opener骨架.md` |
| 权威排期 ｜ 文档在哪 | `全景规划.md` §0.2 ｜ `文档台账-自动生成.md` |
