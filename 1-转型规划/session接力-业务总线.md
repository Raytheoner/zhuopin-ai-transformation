---
status: 在办
title: "Session 接力（滚动·最新）—— 业务总线（三域合一）"
---

# Session 接力（滚动·最新）—— 业务总线

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工「覆盖」不「追加」。**
> 进度叙事写 CHANGELOG；方法教训写取证知识库；未闭合项写队列行。收工跑 `工具-CLAUDE进度段lint.py`，J6 报超限即当场删回去。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/session接力-业务总线.md` + 根 CLAUDE.md 恢复上下文，按【三】继续；开干前先问我 2-3 个澄清。」

## 二、当前状态快照（2026-09-03 实测）

- **全景 35 场景**（五编号空置勿重分配）。**S1 目标 9-11**。
- **`.51` 在跑**：8091/8092/8093/8094/8096 **五端点实测全 200**。SC8 规则1 开关 **已 ON**（`param_version` 带 `+rule1`）。
- **串行闸**：唐燕萍 🔒／姚祖怡 🔒（卡 `采购部#21`）／陈忱 已开／陈承 ✅／泓钦 ✅。🔴 **唯一权威＝`工具-跟进闸查询.py`**；🔴 **本卡与队列里的信状态复述都是过时快照，不得作判据**。
- **第七批 `B-0903_50` 已跑完**（首次无头三段串行）：进主干＝采购三包＋SC8 ATP 批2（9 提交）／`criteria_signoff` 底座（用例 45→59）／财务五包迁移引用底座。**判据 18 进 18 出，一条值都没填。**
- ✅ **第八批 `B-0903_70`（FI9 接 OEM 隔离层）已完成**（`OP-0903-E1`，他在 CC 里起）：commit `d70ed28` 已在 master。五条裁决全落（①(c)/②(c)+三道锁/③(b)/④(a)/⑤(a)）。**Cowork 独立实测复核**（非信自陈）：FI9 **38 passed**／底座 466+1skip／FI5·FI6·FI8·FI10 各 10/12/16/16 全绿／`openspec validate` **156 passed 0 failed**。🔴 **两条硬边界均守住**：规范本体一字未改（只出 `oem-isolation-spec-amendment-draft.md` 建议稿）、跨 OEM 汇总开关 `FI9_CROSS_OEM_AGGREGATION` 默认 `off` 且 OFF 时 fail-loud。
- **LAN 收口线本轮收**：`K-1=(a)` —— **2026-08-31 `OP-0831-L` 那次算跑通**（批次 `B-0831_10`–`_14`）；本轮三源汇集**应收口清单为空**，不起收口批。

## 三、下一会话主攻（业务总线）

1. 🔴 **§四 分诊待消化 —— 本线优先级最高的未办事项，已连续三轮未动**：`reports/section4-triage-2026-09-02.md`，**真待他答 22 条、含 4 条已逾期**，已按「答完能解锁什么」排过序，可直接摆。
2. 🔴 **三处留步可并成一次派单**：⒜ FI9 三处**规范回写**（① 补 §2 三分法表／② 立豁免款／⑤ 补 §3.3 射程；建议稿已在 `fi9-rd-cost-mvp/oem-isolation-spec-amendment-draft.md`）—— 触碰区与 `oem-chroma`（`#466` 未开工）重叠，**本线不代改**，等其收口；⒝ `criteria-signoff-platform` **§5.1 同改根 `CLAUDE.md` §4 ＋ `5-平台底座/CLAUDE.md`**（原文原样下沉，改一处即漂移）。**均须他批准。**
3. **三行待派**：`#476`（L2 模型层纪律收编，🛑 触发条件＝两个财务引擎落地）／`#477`（FI9 工时系统核实，**问人的事**）／`#478`（lane-watch ⏭️ 档扩展，🛑 排队，🔴 **指向而非复制**四步纪律，机制线承接）。
4. **销售域 6 个＋研发域**：2026-09-02 明令**暂缓、等他通知**；🔴 **不是立 🛑 排队行，是根本不立。**

## 四、在途与等他动作（只写指针）

- **等他推的**：FI8 银行余额取数授权（EE-2，属权限非判据）。
- **待机制线**：§四 `#154`／§一 `#461`／`#464` 🛑／`#466`／`spec_sync_to_main_specs` 裁 🟢 待回灌 D1 表。
- **待全景路线图线**：移交单 `SC4顺延与前置总表补5行`（已并 EE-5/EE-6，SC4 落 2026-11）／`O1注销并入`。
- **采购**：§一 #118 #266 #334 #344。**质量**：陈忱闸已开 ⇒ 是否起 `质量部#13` 见 §四 #153；`#431 ⒞` 10 份样本 **09-11 到期，不得早催**。**财务**：`财务部#16` 在途（一回件则 `#418`／`#424`／`#390` 一串解锁）。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **起泳道按档分流（他 2026-09-03 定）**：**含 🟡 档（合 master／改口径阈值／design 审／关他人行）或首次跑通一条链路 ⇒ 出 CC Opener 给他、他在 CC 里粘**（人在环、正常权限）；🟢 档为主且不写 `master` ⇒ 才走 `工具-opener批处理执行v2.ps1` 无头（那是 `--dangerously-skip-permissions`）。**成因**：A1 在主工作区操作 master 时工作区有 10 个未提交文件（含他线在办），其零交集核对无人复核。
- 🔴 **走无头时 opener 三关**：① 标题必须 `### A<N> ·`（写 `D1` 全批 exit 12）＋ 独立一行 `粘贴端：CC ｜ 泳道：<无空格名>`；② 同泳道名串行、不同泳道名并行 ⇒ 有依赖或都合 master 的**必须同名**；③ **先跑 `-DryRun -Yes`**。骨架照抄模板库 **§〇.00**。
- 🔴 **探本机环境（LAN／`.51` 可达／时区／进程）一律本机 PowerShell，禁走沙箱 bash** —— 沙箱到不了内网，`lan-status` 会给出 `error=False`、完全笃定的 `off-LAN`。**三态设计防的是「我没测成」，防不住「我在错的地方测成了」。** 判据＝**跑命令前先答「这条命令在哪台机器上执行」**。知识库 §2.10。
- 🔴 **队列行里每一段只在写下的那一刻为真**（「历史记录不追改」的必然副作用）—— 2026-09-03 一天连撞三次。**信源可信度：机器字段 > `git log --grep=<行号>` > 生产实测 > 行内自由文本（最软，只能当线索）。** 知识库 §2.11。
- 🔴 **判「某条 message 在不在某分支上」用 `--grep`／`--contains`，禁用 `-N` 截窗口**（一次合 9 个提交时 `-3` 够不着）。
- 🔴 **§二「文件清单」每个路径必须用反引号包住** —— 守卫⑹ 走 ``re.findall(r"`([^`]+)`")``，裸路径提取为空 ⇒ 判孤儿文件拒绝 release。
- 🔴 **解释一个机制标记时别写出它的字面值** —— 会被 sweep 的 `STALE_CHANGE_DEFER_MARKER` 当成真声明、**把变更包永久静音**；只引用变量名。
- 🔴 **心跳静默 ≠ 泳道卡住** —— 交叉验证用 **worktree 改动数／分支 HEAD／锁占用方**。
- 🔴 **合 master 前看 `merge-base --is-ancestor`**；**合子分支必连带合入其基分支全部提交**，派单前列清交他确认。
- 🔴 **`/opsx:apply` ＝ 实现 tasks 待办**；写 `openspec/specs/` 用 **`sync`**。
- 🔴 **闸锁即不起草**；**触发满足≠可发**。**判据／口径／阈值类永不默认生效** ⇒ 这类定夺**不设默认项**。
- 🔴 **裁决已下 ≠ 规范已立** —— 代码先于规范落地在 IATF「单一可信源」下站不住；用开关默认 OFF 兜住（同 `SC8_KIT_DATE_RULE1`）。
- **Cowork** 只出 `.md`；🔴 **收工零孤儿脏文件**；看护件类批次登记**机制环境**文件。**对外件须扫第三人称代词回名录核对**（**姚祖怡是男性**）。
- 🔑 **rule-of-three 触发的是「该收」不是「现在收」**；**裁决会互相改变彼此的性质**（EE-4 把 `SLOW_MOVING_CRITERIA` 从"前置"变成"判据"）。

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 任务真身与状态 | 两份队列；`工具-队列查询.py --row N --section 一` |
| 信状态与闸 | `工具-跟进闸查询.py --to <人>`（唯一权威） |
| 泳道看护 | skill `zhuopin-lane-watch`；`状态机.py show/summary/resume`；看护件＝`0-全景路线图/看护件-泳道看护批*.md` |
| 本轮两份审读件 | `0-全景路线图/design审读件-criteria_signoff底座-*.md`／`design审读件-FI9-OEM隔离接法-*.md`（均已裁） |
| 取证方法与坑 | `0-学习与工具/取证方法知识库.md`（**§2.10／§2.11 是本日新增**）；opener 格式＝`专线opener模板库.md` **§〇.00** |
| 权威排期 ｜ 文档在哪 | `全景规划.md` §加速启动总览 ＋ §0.2 ｜ `文档台账-自动生成.md`（**别翻目录**） |
