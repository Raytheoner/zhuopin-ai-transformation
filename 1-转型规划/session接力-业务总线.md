---
status: 在办
title: "Session 接力（滚动·最新）—— 业务总线（三域合一）"
---

# Session 接力（滚动·最新）—— 业务总线

> 🔴 **定长交接卡，不是日志。硬上限 8 KB，固定六块，零日期节。收工「覆盖」不「追加」。**
> 进度叙事写 CHANGELOG；方法教训写取证知识库；未闭合项写队列行。收工跑 `工具-CLAUDE进度段lint.py`，J6 报超限即当场删回去。

## 一、新会话起始词（复制即用）

> 「读 `1-转型规划/session接力-业务总线.md` + 根 CLAUDE.md 恢复上下文，按【三】继续；A 类同线转场先做开工自检三查、不问。」

## ⏳ 本线在跑会话（开新会话先登、收工划掉）

| 编号 | 会话名 | 触碰区 | 起止 |
|---|---|---|---|
| `OP-0911-A` | 【CC】`554-intake-gap`（差集告警·sweep 路线） | `0-学习与工具/工具-落库sweep.py`＋其单测 | 09-11 05:57 – |
| `OP-0911-B` | 【CC】`k2-sweep-overlimit`（10 行超限清扫） | 两份队列真身 §一 相应行＋`队列行日志/`（只追加） | 09-11 05:59 – |

> 批次 `B-0911_差集告警与超限清扫`，日志 `reports/opener-batch/20260911-055727/`。收工由定时任务 `poll-opener-batch` 推运维群。

## 二、当前状态快照（手段随句写明）

- **`master` 现取 `9aa5d55`**；🔴 **判落地一律按 `#534` patch-id 口径看产物，不看分支图**——`master...分支` 三点 diff 会把已由别路径进 master 的改动算成未合入（`#341`／`#455` 两次实撞）。
- 🆕 **无头起棒基建已全部在 master**：`--resume` 接管（session id 落日志首行与 `summary.txt`）／`-Detach` 后台起（`launcher.json` 记 pid 与 `exit.txt`）／**取号即声明**（`reports/op-id-claims.jsonl`，`CLAIM_TTL_MINUTES=120`，他答 `1a`）。**已两次真拦撞号。**
- 🆕 **收工哨兵已从「注入」升到「强制」**：`NO-SENTINEL` 时 `claude --resume` 补问一次、仍无才判 FAIL；`summary` 多出 `Sentinel` 列与三数**分计遵守率**。🔴 **立行根因已证伪**：四条泳道 opener 真身**全都含哨兵要求**（批处理前言 ⑤ 自注入）⇒ **是没照做、不是没生成**，实为 `#487` 同形态。
- 🆕 **「棒跑完没人知道」已转机器守**：探针 ＋ 定时任务 **`poll-opener-batch`（全天每 15 分钟）**，新收工／静默 ≥90 分钟即推运维群；**已无人值守真报出一次**（21:55 收工 → 22:02 推送）。🔴 **人工核查一律 `--peek`**；`--no-notify` 会把信号标记为已报、**静默吞掉推送**（已实撞一次）。
- ⏰ 🔴 **行长闸 2026-09-11 起转阻断**（编辑锁两条 `test_row_length_*` 今日转红即其证据）：**超限行碰不得**，销号／改状态／补豁免的 release 一律被拒（`#188` 死锁形态）。工具 `工具-队列行K2外置.py` 已在 master（先看段表 → `--apply --expect-segments N` → `--verify-json` 反查）。
- **`#554` 拆件覆盖面差集（新立）**：巡逻**已正式下线、本就不该开**；接替的事件驱动只在「**在途**信件收到回件标第九态」时落信号 ⇒ **差集 ＝ 专员主动发言 ＋ 对已闭环信件的补发**，无任何消费者。**定量：陈忱当日五件 5/5 全落差集。**
- **`#553` ⑴ 已出**：134 条 `claude/*` 未并入分支 → **86 组合 ＝ A 64 已在 master／B 18 真未落地／C 4 该删，0 合 0 删**。🔑 **P4 那句「主仓 ff 由 sweep 收尾段做」在 sweep 里没有实现**（全文 grep `claude/op` 零命中）⇒ 留给 sweep ＝ 留给没有人。
- **`#538` SC2**：187 复现 ✅、190 passed、代码侧已进 master；🔴 **仍 `partial`** —— ⏭️ 转出项（`.51` 部署＋与线上比对）待 LAN 收口批。

## 三、下一会话主攻（业务总线）

1. **接本批收工**：`554-intake-gap`／`k2-sweep-overlimit` 回写＋逐条 rebase→回归→ff（分支放过夜就会掉出 ff 关系，本日四条全中）。
2. **`#553` ⑵⑶ 未做**：⑵ 改 `opener骨架.md` 的 P4 措辞使其与实现一致；⑶ 加一条「未并入分支数超阈值即告警」的 sweep 常驻类。**⑴ 已交付不等于本行可销。**
3. **`#554` listener 路线**：⏭️ 档，须回 LAN 走 `zhuopin-lan-closeout`；本批只补了「让人知道」，**没补「自动拆」**。
4. 🔴 **两处回归未整跑，不得当已绿**：`test_工具-落库sweep.py` 471 条／`test_工具-opener批处理执行v2.py` 16 条（只单跑过 7 条）。**成因是环境：整跑超工具单次上限，后台 detach 会被会话超时连带杀掉（当日三次）。**
5. **行长⑪ 两条日期炸弹用例**今日起长期红，须改按日期分档断言或注入时钟（已写进 `#513` ⑷）。
6. **`#555`（质量部#14 候选判例）** 新立待办；`#542`–`#548` 已销号。

## 四、在途与等他动作（只写指针）

- **等他动作**：§四 `#193`（陈忱 8D 微盘取件，需登录态）／`#194`／`#157`（无默认）／`#152`／`#156`／`#170`。
- **⚠️ 权限规则**：`.claude/settings.local.json` 有 2 条过宽 Bash 允许规则（通配符在命令主体之前，会连带放行插入的选项），其一还引用已迁走的 OneDrive 旧路径。🔴 **安全设置，本方不代改，只报。**
- **无头档实际权限模式 ＝ `acceptEdits`**（非 `-FullAuto`）：`git worktree remove` 会被拒 ⇒ 泳道 worktree 常需看护者收工手工 `--force` 清。
- **`#552` 工具已知形态缺口**：唯一首段过长时 `--keep` 给不出「保留首段但拆开它」（`#538` 实撞），只能退回合成精简头。

## 五、常驻纪律（分工·红线·踩过的坑）

- 🔴 **opener 一律走 `工具-opener生成.py` ＋ `opener块lint.py`，禁手抄**（手写必漏：漏过 `set_session_title`、漏过哨兵行）。⚠️ **变体随交付模式走**：无头＝每条泳道自成 session ⇒ `standard`；看护者 Task/Agent 扇出 ⇒ `subtask_lane`。**lint 靠文件里有没有 §三bis 看护者段来判类型**，不是靠你声明。
- 🔴 **§二 文件清单里每个反引号串都必须是合格仓库根相对路径**——OP 号／锚名／批次号放进去即被拒（本日实拦 5 次，**一次都没漏过去**）。
- 🔴 **凡「由 X 做」，必须能当场指出 X 的哪一行代码在做；指不出即为空头。** 同族三条：`UPS5:7`（承诺不会自己执行·主语本方）／`#550`（要求不会自己被执行·主语 agent）／`#553`（机制不会自己存在·主语那句话）／`#554`（换机制时覆盖面不会自己被核）。
- 🔴 **看目录时间戳会骗人，要看里面文件的时间戳**（同族＝「写进文本的状态不会自己过期」）。
- 🔴 **K2 外置只追加、绝不覆盖**（写前 `exists`＋体积下限断言，写后回读两侧）——本日实撞一次覆盖事故，靠 sweep 已 commit 才救回。
- 🔴 **人的属性查名录正本、禁从名字推断**；称呼 `Shao Peishen`（路径里的 `Paul Shao` 绝不改）。
- 🔴 **判据／口径／阈值类永不默认生效**；决策清单末尾附**独立代码块**的答复模板。
- **Cowork** 只出 `.md`；收工零孤儿脏文件（登 §二 或 `release --waiver` 逐个点名，禁泛豁免）。

## 六、往哪查（本卡不复述，只指路）

| 要找什么 | 去哪 |
|---|---|
| 任务真身与状态 | `工具-队列查询.py --row N --section 一/四`（🔴 禁 Read／grep 真身） |
| 信状态与闸 ｜ 待问口径点 | `工具-跟进闸查询.py --all` ｜ `6-人才与组织/部门AI专员跟进/口径点台账/*.jsonl` |
| 泳道看护 ｜ 无头棒进展 | skill `zhuopin-lane-watch`；`状态机.py summary --batch` ｜ `reports/opener-batch/<时间戳>/summary.txt`＋`--resume <id>` |
| 落库为什么没发生 | `reports/sweep-commit.log`（逐轮警告原文） |
| 取证方法 ｜ opener 格式 | `0-学习与工具/取证方法知识库.md` ｜ `0-全景路线图/opener骨架.md` |
| 权威排期 ｜ 文档在哪 | `全景规划.md` §0.2 ｜ `文档台账-自动生成.md` |
