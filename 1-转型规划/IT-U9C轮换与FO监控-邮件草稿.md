# 致 IT —— 邮件草稿（两封）

> 用途：Paul off-LAN 期间起草、回公司即发。
> **发送时序（重要）**：邮件①必须在 **PR #10（A 安全包）合入 master 之后**再发——先修信道（连接器 TLS 默认证书校验已在 A1 恢复）再换钥匙，否则新 secret 仍可能走裸信道暴露。邮件②无时序依赖，可随时发。
> 凭据交付红线：新 client_secret **不得用明文邮件 / 即时消息发送**；当面 / 保密渠道交付，由我方填入仓库 `.env`（已 gitignore、不入库）。

---

## 邮件① 【最急 · 请 6/20 前完成】轮换 U9C webApi client_secret

**收件人**：IT / ERP 接口负责人
**抄送**：（视情况）信息安全
**主题**：【最急·6/20前】轮换 U9C webApi client_secret（旧凭据已暴露，需停用）

正文：

> XX，
>
> 有一项安全处置需要你这边在 **6/20 前**配合完成，背景如下：
>
> 我们对接 U9C webApi 用的 **client_secret 曾以明文写在一个调试脚本里并推到了 GitHub**。脚本里的明文行虽可删除，但**代码仓库的历史记录中仍然保留该值**，等同于这把能读取真实 ERP 数据的钥匙已经处于暴露状态。因此删行不够，**必须轮换（停用旧值、重新生成）**。
>
> 请协助：
> 1. 在 U9C webApi 后台**重新生成 client_secret**，并**停用 / 失效旧值**（client_id 可保持不变）；
> 2. 新的 client_secret 请**通过安全渠道**交我（当面或保密通道，**不要走明文邮件 / 微信**）；我方会填入受控配置、不再写入任何代码或仓库；
> 3. 完成后告知确认，便于我方更新配置并验证连通。
>
> 我方侧已先行修复了连接器的 TLS 证书校验（此前默认关闭），确保轮换后的新凭据走的是经过校验的加密信道。所以这件事的正确顺序是：我方信道修复合入 → 你方轮换 → 我方更新验证。
>
> 时间上 **6/20 是我们设的处置红线**，麻烦优先安排。有任何技术细节我随时对接。
>
> 谢谢！
> Paul

---

## 邮件② FO（客户订单服务）健康监控与告警

**收件人**：IT / 运维
**主题**：申请为 FO 服务（192.168.100.51:8800）增加可用性监控与告警

正文：

> XX，
>
> 我们供应链交期承诺场景（SC8）上线后会**实时依赖 FO 客户订单服务**（内网 `192.168.100.51:8800`）取数。该服务一旦 502 / 宕机，对客环节可能出现**静默断供**——系统不报错但数据停更，风险较高。
>
> 申请在内网为 FO 增加基础可用性监控：
> 1. **每 1–5 分钟**探测健康端点 / 8800 端口连通性；
> 2. 连续探测失败时**自动推送告警**（企业微信 / 值班群均可），便于第一时间介入；
> 3. 如已有统一监控平台（Zabbix / Prometheus 等），纳入即可，无需单独搭建。
>
> 这是 SC8 对客上线前希望补齐的运维安全网，优先级中、不紧急，麻烦评估排期。
>
> 谢谢！
> Paul

---

## 附：可一并向 IT 确认的两件（非邮件正文，口头/会上跟进即可）

1. **外网代理是否开放 `CommonEntity/Query`**——决定 U9C 库存 / PO / MO / 价格表能否离网做全量 cutover；不开则这些取数走 LAN/VPN（见 `U9C接入与连接器收敛-待办追踪.md` 待办 #3）。
2. **U9C MCP 接口 7/1 申请进度**——FI1 / SC2 等强依赖；若延误，启用实施计划 §四备选（SQL 只读直连 / 文件导出过渡），避免骨牌阻塞。

---
*关联：`U9C接入与连接器收敛-待办追踪.md` 待办 #1 / #3 / #6、CLAUDE.md §7 红线、`session接力-Phase1收口.md` 第二节。*
