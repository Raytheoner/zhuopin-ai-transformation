# 部门 → 企微群 chatid 映射（队列 #270：群 cc 改走智能机器人 chatid 通道，
# 取代此前的群机器人 webhook 通报——webhook 单向，群成员的回复进不到任何
# 地方；智能机器人已是这些群的成员、群里也确实回复过，只是 chatid 值本身
# 尚未采集）。
#
# 与 `department_group_mapping.yaml`（部门→webhook 环境变量名，服务旧的
# webhook 通道）是姊妹表但服务不同通道，语义不同、不要合并：那张表存的是
# **环境变量名**（真实 webhook URL 落 `.env`）；本表存的是 **chatid 本身**。
#
# chatid 不是秘密——同 `dispatch.py` 里明文放的 KNOWN_RECIPIENT_USERIDS
# （如 "姚祖怡": "YaoZuYi"）一个敏感级别，可安全提交进 git。
#
# 真实 chatid 值由 Shao Peishen 2026-08-06 在各群发送带部门标签的测试消息
# （"财务测试短信"/"质量测试短信"/"采购测试短信"等）、经 intake.py 归档
# 审计事件（队列 #279 新增的 chatid/chattype 字段）真实采集确认——不是
# 猜测/占位，四个部门均已采集完毕。消费方（`resolve_group_cc_chatid`）
# 遇空值一律 fail-closed 跳过群 cc 并记审计，不拿空值当 chatid 发送——
# 该分支现无部门命中，留作未来新增部门时的既有安全网。
#
# 销售部：与 `department_group_mapping.yaml` 同一拍板（Paul 2026-07-15），
# 暂不启用，故意不建映射条目。
#
# 跨部门（2026-08-06 新增，Shao Peishen 明确："采购部还是自己的部门，
# 我现在有4个部门单独使用，跟其他部门的信件无关"——即"跨部门"是与财务/
# 质量/采购平级的独立第4个部门，只收发给它自己的跟进信，不是"额外收全部
# 部门"的广播/cc 对象；此前一度按后者设计过、已撤销，仅登记本次以防
# 未来又走回头路）。
#
# IT（2026-08-24 新增，队列 #387 ⑵）——对应「运维部AI保障群」。
# 🔴 **键名是 `IT`，不是 `IT部`**：本表的键必须与 `department_mapping.yaml`
# 的**值**逐字一致（那里写的是 `"2023458": IT`），消费方拿到的是归档时解析
# 出的那个部门字符串，`IT部` 会命中"不在映射表"分支、静默跳过——与本行要
# 修的缺陷是同一个形态。
#
# 该 chatid 由 Shao Peishen 2026-08-24 15:30:50 在运维部AI保障群发一条带
# 标识的消息、经 intake.py 归档审计（#279 的 chatid/chattype 字段）真实
# 采集，与 2026-08-06 采那四个群是同一套办法。
#
# ✅ **2026-09-11 已反查，确为运维部AI保障群**（`OP-0910-H`，企微客户端
# 只读核，未发任何消息）。三条独立证据：① 全局搜索 2026-08-24 那条入站
# 正文「这是运维AI保供群发的」**只命中这一个会话**；② 群内时间戳
# 「8月24日 下午03:30」与审计 archived 记录 2026-08-24T07:30:50+00:00
# （＝15:30 CST）**同分钟吻合**；③ 群成员 4 人＝邵培申（群主）／陈承／
# 范君／邵总数字人 BOT。取证全过程见
# `1-转型规划/0-全景路线图/队列行日志/#387.md`。
#
# 📌 该群里另有一个**不在群成员列表**的 `运维部机器人` 在推落库 sweep
# 卡片——那是群 webhook 机器人（webhook 机器人不进群成员名单），只出不进，
# 与下文「本表不走 webhook」**不矛盾**：不配的是 aibot 回执通道。看到群里
# 有 webhook 机器人，不要据此认为口径已破（2026-09-11 反查时一并核实）。
#
# ⚠️ **本表不走 webhook**：Shao Peishen 当日另给过一个该群的 webhook URL，
# 刻意**不**配进来——`#270`/`#279`/`#281` 已于 2026-08-06 把群 cc 从
# webhook 整体迁到 chatid，理由是「webhook 单向，群成员的回复进不到任何
# 地方」。陈承恰恰是最常在群里回话的那一位，配 webhook 等于让他的回复
# 再次掉进黑洞。webhook 值留在队列 #387 行内备查，不进配置。
财务部: "wrvDL_DAAAva1MWrKjLmuDWOu1BNxHaA"
质量部: "wrvDL_DAAAiR1HJ_JFx1H-k-tjnriXEg"
采购部: "wrvDL_DAAAH7IL6I7hsgszeyQvmyjw-Q"
跨部门: "wrvDL_DAAAntxO_zDcRCmGHMcuhvBTGw"
IT: "wrvDL_DAAARjP0BlFLup5e1Cv3vcCvMQ"
