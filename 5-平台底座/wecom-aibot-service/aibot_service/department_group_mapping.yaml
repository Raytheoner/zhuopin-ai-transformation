# 部门 → 企微群 chatid 映射（Paul 2026-07-12 认可：归档成功后机器人回部门群
# 发一条通报留痕）。
#
# ⚠️ 以下均为占位符，尚未填入真实群 chatid——填之前，通报环节会被安全跳过
# （不会往错误的群/无效 chatid 发消息），见 group_notify.py 的 fail-closed
# 设计。获取真实 chatid 的方法：对应部门群里随便发一条消息（任何人发都行，
# 不必 @机器人——群消息只要机器人在群里就能收到，见 frame_parsing.py 里
# 已确认的 body.chatid 字段路径），从审计日志/服务日志里的
# `"chattype": "group", "chatid": "..."` 抓真实值填进来即可。
财务部: PLACEHOLDER_FINANCE_GROUP_CHATID
质量部: PLACEHOLDER_QUALITY_GROUP_CHATID
采购部: PLACEHOLDER_PROCUREMENT_GROUP_CHATID
销售部: PLACEHOLDER_SALES_GROUP_CHATID
