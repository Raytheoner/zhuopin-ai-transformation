# 部门 → 群 webhook 环境变量名映射（Paul 2026-07-12 拍板：归档成功后机器人
# 回部门群发一条通报留痕）。
#
# 2026-07-15 Paul 澄清：三部门群各自独立小群，走既有企微群机器人 webhook 机制
# （非智能机器人 chatid 通道），复用 shared_tools/notifiers/wecom.py。本表只存
# **环境变量名**、不存真实 webhook URL——真实 URL 只落 `5-平台底座/.env`
# （gitignore，不入库）。本表本身不含任何秘密，可安全提交。
#
# 销售部：Paul 2026-07-15 拍板暂不启用，故意不建映射——sender 命中销售部时
# group_notify 会因"部门不在本表"走 fail-closed 跳过（不报错、留痕）。
财务部: WECOM_WEBHOOK_URL_FINANCE
质量部: WECOM_WEBHOOK_URL_QUALITY
采购部: WECOM_WEBHOOK_URL_PROCUREMENT
