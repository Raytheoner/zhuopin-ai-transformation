# 企微消息发送人 → 部门映射（design.md D7）
# 持有人：Paul；backup：孙涛（Paul 2026-07-11 确认，随根 CLAUDE.md §5 决策代理纪律）。
#
# 键 = 企微 userid（非中文名——2026-07-13 真实测试群联调实测确认字段路径是
# body.from.userid，取值为 userid 而非中文显示名，如实测抓到 "ShaoPeiShen"）。
# 下方四位专员真实 userid 由 Paul 2026-07-13 提供，已替换原中文名占位。
# 未命中本表的发送人一律 fail-closed 归入 `7-外部文档/待分拣/`（不猜测部门）。
YaoZuYi: 采购部        # 姚祖怡
tangyanping: 财务部     # 唐燕萍
ChenChen: 质量部        # 陈忱
Hongqin.Wang: 销售部    # 王泓钦

# 陈承（IT）2026-07-22 补入（队列 #70）——此前白名单内但不在本表，命中白名单
# 后仍落"待分拣"（见 whitelist.py 说明）；此后归档进 `7-外部文档/IT/`。IT 不是
# Cowork 的四域专线之一，`intake.py::DEPARTMENT_TO_QUEUE_OWNER` 未加对应项，
# 归档产生的队列行按现有 fail-closed 逻辑落 owner=Paul（与未命中部门同一默认
# 值），不臆造一个不存在的"IT专线"角色。
"2023458": IT          # 陈承

# 🔴 2026-08-25 补入两人（队列 #380 ／ §四 #116，变更包 aibot-inbound-whitelist-li-jiaolong）
# ——**与 `whitelist.py::WHITELISTED_SENDER_USERIDS` 同批加，两张表串联、缺一不可**：
# 白名单只回答"这条消息要不要被处理"，本表回答"处理后归哪个部门"。只加白名单
# 会让来件落 `7-外部文档/待分拣/`、队列行标"发送人身份待确认"、owner 落未命中
# 默认值——**与"连身份都没认出来"共用同一个落点，读队列的人分不出二者**；陈承
# 2026-07-16→07-22 之间就是这个形态（队列 #70 修的就是它），本次不重演。
#
# 键按 `"2023458": IT` 的既有写法加引号。**引号是写法一致性、不是正确性**：
# `department_mapping.py:19` 为 `{str(k): str(v) ...}`，YAML 把 `2025672:` 解析成
# int 后仍被 `str()` 归一、查得到；加引号是为了一眼可辨"这是工号不是数字"。
#
# 部门值取的是**本项目四域口径、不是 HR 部门名**——对照：姚祖怡在《全员企微账号》
# 里是"供应链中心"，本表映射为 `采购部`。李姣龙在《企微 chatid 名录》财务部段内，
# 三项职责均为财务对账链路；解植雅在《全员企微账号》名录里即采购部
# （另：组织事实 `PMC部` ⊂ `采购部`，见根 CLAUDE.md §1）。
# `intake.py::DEPARTMENT_TO_QUEUE_OWNER` 无需改动——`财务部: 财务专线` 与
# `采购部` 对应项均已存在（本次为核验，非改动）。
"2025672": 财务部        # 李姣龙
"2025621": 采购部        # 解植雅
