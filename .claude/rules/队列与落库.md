---
paths:
  - "1-转型规划/0-全景路线图/跨桌任务队列*.md"
  - "1-转型规划/**/session接力*.md"
  - "0-学习与工具/工具-*.py"
  - "0-学习与工具/hooks/**"
---
# 规则 · 队列与落库、环境保障线（从根 CLAUDE.md §5.1 复命／§5.3 迁入，2026-09-03 P2）

> CC 触碰上述路径自动加载；**Cowork 不自动加载，按根文件路由表显式 Read**。协议〇细则以队列顶部为正本，本文件不复述。

- **跨桌任务队列纪律**：`1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md` ＋ `…-业务场景.md`（两份真身、共用一把锁；旧 `跨桌任务队列.md` 为纯指针）是两桌任务流转的**唯一载体**——**开工必读**（认领＋登记触碰区）、**收工必写**（状态＋产出路径＋下游任务当场追行）；触碰区与他人在办重叠不得抢领，报总线裁决。**commit 由 `ZhuopinCommitSweep` 自动从 §二「待 commit 批次」取活销行（约 27 分钟一轮）——登记完 §二即完事，不需要「一句话交 CC」**；CC 仅在 sweep 不可用时兜底。§三口径冻结标挂标即停该场景建造。🔑 **编辑锁／每周清扫／高水位线／并入审核／可动 WIP 等细则以队列顶部「协议〇」为正本。** 值周巡检每周一 10:00 出《本周计划》，开跑前先跑 skill `zhuopin-queue-audit`。**gap 收口**：设计↔执行 gap 须驱动到有主·有截止·已派单后计划才往下走。
- 🛡 **机器守（CC，hook ⓒ PreToolUse，2026-09-04 验活）**：未 `acquire` 直接 Edit/Write 两份队列或接力卡即被拦（exit 2，审计 `reports/hooks-audit.jsonl`）；`acquire` 时按触碰区打印「先读 `.claude/rules/X.md`」路由提示（ⓔ）。Cowork 侧不受 hook 约束、仍人守。
- 🔴 **读侧禁通读（K3，2026-09-04 定）**：两份队列真身与归档件**不得 `Read` 全文、不得 `grep`**——一行可达 78 KB，一次命中即灌爆上下文。判行状态只用 `python 0-学习与工具/工具-队列查询.py --row N [--section 一|四] [--field all]`；扫池／核触碰区只用 `--digest`（业务场景加 `--file 1-转型规划/0-全景路线图/跨桌任务队列-业务场景.md`），命中再按需 `--row` 展开；归档件同理 `--digest --file <归档件>`。**行内日志外置口径**：§一 状态格／§四 事项格 ≤4 KB，超限把历史回写段迁 `1-转型规划/0-全景路线图/队列行日志/#N.md`（原文原样），行内留首段＋末段＋指针；回写时只追加末段、不复述历史。🛡 机器守（CC，2026-09-04 验活）：`--grep` 已生产生效；PreToolUse 门禁 `hooks-pretooluse-queue-read-guard.ps1` 拦直击队列的 Read/Grep/Bash（验收 2/2 过，审计 `reports/hooks-audit.jsonl`）；行长校验⑪ 首周告警、2026-09-11 起阻断。Cowork 侧不受 hook 约束、仍人守。
- 🔴 **锁操作只认退出码**：`acquire`／`release` 各自独立一步、中间查 `$LASTEXITCODE`；**沙箱挂载盘上 git 极慢，队列锁 `release` 一律走本机 PowerShell**；管道后的 `$?`／`echo $?` 只是管道末端的退出码。**「工具拒绝了」与「动作没有发生」是两件事**——写后用 `status` 反查。
- **CC 复命零粘贴**：他说一句「CC 跑完了」，Cowork 自行取件，**三源缺一不可**：① CC 会话原文 `C:\Users\Paul Shao\.claude\projects\<路径编码>\*.jsonl`（逐行 `ConvertFrom-Json` 取 `type=assistant` 的 `message.content[].text`）；② 队列行；③ `git log` ＋ `git rev-list --count origin/master..master`。**三者不一致以队列与 git 为准。** 🔴 边界：⑴ jsonl 是内部格式，解析不出即退回粘贴、不得猜；⑵ 只能读、不能反向发；⑶ 目录名编码会吃掉中文，**必须用 mtime＋内容二次确认**。Cowork 侧会话原文＝`session_info.read_transcript`。 ⇒ 取件算法见 `专线opener模板库.md` §〇.15。
- **决策路由的总线侧配套**：总线答复「待派发」项后须 ① 结论写进队列行 ② 附「回原 session 收口词」（收工自检四项：产出已推送／队列行已回写且 ✅ 在状态列开头／§二批次已落库／行内遗留是否需升格）。
- 🔴 **总线／看护会话不修工具**：调度中的会话（总线、看护者）发现机制缺陷**只做两件事——登记（§一／§四）＋出 CC 派单**，不在本会话修。**唯一例外**：正在跑的这一批会因该缺陷失败，且修补 ≤10 行；例外须在队列行写明「因本批 X 失败就地修，N 行」。
- **机制优先**：能等的交给机制（对账审计／sweep／值周巡检）；不能等的用机制的方式立即做（编辑锁、批次登记、留痕，不裸手改）。发现绕开机制的裸改（含自己）应拦下提醒。
- **环境保障线边界**：对象是构建环境与自动化机制本身（worktree／编辑锁／sweep／巡检／队列纪律／openspec／工具链／机器人）＝环境整治，本线可备派单件；对象是业务场景代码／底座功能／真实数据／`.51` 现网（即便入口是一个「测试失败」）＝项目构建，交业务总线。本线默认交付＝取证结论／方案定稿／队列行写入，分发由巡检与巡逻完成。 ⇒ 细则见 `开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md` §六bis。
- **规则退休制**：纪律分「机制守」（有代码／门禁强制）与「人守」。**任一「人守」条目被违反 3 次即必须二选一**：① 机制化（挂到不可绕过的咽喉：队列写入＝编辑锁 `acquire/release`；提交＝sweep），正文降为一行指针；② 删除。违反计数落在队列对应行内。 ⇒ 根问题与首例见 CHANGELOG 附录 E-1。
- **接力卡 R5**：定长交接卡（8 KB、六块、零日期节、收工覆盖；进度→CHANGELOG、方法→取证知识库、未闭合→队列行，迁出守 J1；机器守＝J6）。
