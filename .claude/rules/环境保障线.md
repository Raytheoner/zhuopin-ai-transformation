---
paths:
  - "0-学习与工具/工具-*.py"
  - "0-学习与工具/hooks/**"
---
# 规则 · 环境保障线与机制治理

> CC 触碰上述路径自动加载；**Cowork 不自动加载，按根文件路由表显式 Read**。🔴 **本文件只写「触发 → 动作 → 判据／指针」；成因／实证一律在 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md`【附录 M】。** **2026-09-13 由 `.claude/rules/队列与落库.md` 拆出**（`OP-0913-E`，Shao Peishen 答 `1a`；成因＝该件 8,178／8,192 B 只剩 14 B 余量，再加一句判据就要被压成指针）。队列写入／读侧禁通读／编辑锁／登记豁免／批次命名／接力卡 R5 等**落库侧纪律仍在 `队列与落库.md`**，两件互不复述。

- 🔴 **总线／看护会话不修工具**：调度中的会话发现机制缺陷**只做两件事——登记（§一／§四）＋出 CC 派单**。**唯一例外**：正在跑的这一批会因该缺陷失败且修补 ≤10 行，须在队列行写明「因本批 X 失败就地修，N 行」。
- 🔴 **规划倒逼**（`#462`）：判定正本＝`工具-规划倒逼扫描器.py`＋机读正本 `场景registry-规划倒逼扫描器.jsonl`（**改权威排期表必须同批改它**，CI `--lint-registry` 硬失败）；常驻载体＝sweep **第 13 类**。🔴 **只出清单与草案、一个字节都不写队列**——**落库前必须由人做一次协议〇.10 并入审核**；域级暂缓场景留清单、不推送、不出草案。🔴 `取证件-2026-09-02-…覆盖差集.md` §三 脚本**已退休，勿复制**。
- **机制优先**：能等的交给机制（对账审计／sweep／值周巡检）；不能等的也用机制的方式做（编辑锁、批次登记、留痕，**不裸手改**）。见绕开机制的裸改（含自己）即拦下提醒。
- **环境保障线边界**：构建环境与自动化机制本身（worktree／编辑锁／sweep／巡检／队列纪律／openspec／工具链／机器人）＝本线；业务场景代码／底座功能／真实数据／`.51` 现网（**即便入口是一个「测试失败」**）＝交业务总线。默认交付＝取证结论／方案定稿／队列行写入。⇒ 细则见 `…环境总线-接力交接-2026-08-08.md` §六bis。
- **规则退休制**：分「机制守」与「人守」。**任一「人守」条目被违反 3 次即必须二选一**：① 机制化（挂到不可绕过的咽喉：队列写入＝编辑锁 `acquire/release`；提交＝sweep），正文降为一行指针；② 删除。违反计数落在队列对应行内。 🔴 **换机制前先证覆盖面是超集**（`#554`② 正体，`OP-0913-E` 迁入，为该行关闭前的 J1 承接载体）：**下线或替换任一机制前，必须先证明接替者的覆盖面是旧机制的超集**，证明写进下线那一笔的留痕；**证不出即不下线**。判别一句：**「新机制跑通了」是抽样，「旧机制覆盖的都还在」要的是集合包含关系——前者永远证不出后者。** 🔴 **反向同理：新建告警／巡检必须自带消费者**（2026-09-13 答 `2a` 立）——上线前须指定**一条能执行的机器动作**，或**一个具名承接行／载体**（队列行号，或具名文件＋章节）；**两样都没有的不许上线**，已上线而缺的限期补或退休。**判别一句：只会报不会做的告警不减人守，只是把触发方式从「定期想起」换成「碰巧看见」。** ⇒ 首例见 CHANGELOG 附录 E-1，实证见附录 M。

- 🔴 **汇报体裁：代码块只给「要他手工粘贴执行」的东西**（2026-09-13 他当场纠正立）——shell 命令、Opener／看护件、答复模板，这些才进围栏代码块。**告知类、汇报类、状态表、盘点结果、实测数字一律用普通文字或表格**，不得做成一键拷贝代码块。判别一句：**看护者看见拷贝按钮的第一反应是「这是要我去执行的」——把结论排成代码块，等于在每份汇报里埋一个假指令。**
- 🔴 **定夺项三件套，一个都不能少**（2026-09-13 他当场纠正立，同轮第二条）——凡需他定夺的项，必须写全：**⑴ 这是什么、为什么要他定；⑵ 逐项选项＋每项的代价（含本方推荐与理由）；⑶ 一个「复制即用」的答复模板代码块**。🔴 **答复模板是上一条的正例、不是例外**：它正是「要他手工粘贴」的内容，必须是代码块。**只写「压着 N 个字母」而不列选项和模板，等于把定夺成本转嫁给他去回翻上文。**判别一句：**他应该能只看这一段就回一行字母，不必往上翻。**

