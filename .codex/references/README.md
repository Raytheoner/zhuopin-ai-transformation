# 历史资料检索边界

Claude 已封存；历史只作检索，不用于执行或回退。

26份原历史记忆仍在主工作区 `.codex/references/claude-memory/`。隔离工作树通过 `git rev-parse --path-format=absolute --git-common-dir` 定位共享主仓，再按需读取原位置；不得因为本工作树没有副本就断言历史不存在。

本次自动审批拒绝将历史原文复制入迁移分支，原因是凭据模式扫描不足以排除敏感内容。因此本分支只保留检索指针，不包含历史原文；需要进一步迁移时逐文件审查或另取明确授权，不绕过审批。
