---
title: "平台底座加固待办清单（Antigravity 评审 → 架构师 triage）"
created: 2026-06-08
status: 生效 — 工作 backlog
来源: 3-治理与合规/外部评审/supplychain收割与平台底座评审清单.md（Antigravity 只读评审）
triage: 首席 AI 架构师（Claude）
---

> Antigravity 对平台底座收割版做了只读评审，10 项发现 + D1-D6 批评 + 5 条切库红线。本清单是架构师 triage 后的**分桶待办**：每项标【何时做 / 阻塞什么 / 谁做】。**所有改动由 Claude Code 实施（Antigravity 只评审不改）。**

## 门禁联动（先记住两条线）
- **P0 三项**：SC8 碰真实客户推送**之前**必须已合入。
- **P2 全部**：**切真实库之前**必须完成（不阻塞 mock）。
- 这两条同时并入《SC8上线前置门禁》检查表。

---

## P0 · 立即修 · 分支 `feat/l2-gate-hardening`（合并 PR#1 后紧接着做）

| # | 项（严重度） | 位置 | 处理 |
|---|------------|------|------|
| 1 | L2 门禁 fail-open（**Blocker**） | crm_notifier/contracts.py L16-22；notifiers/dispatch.py L45-50 | 核心是**改成 FAIL-CLOSED**：Protocol 显式声明 `requires_confirmation`；`_is_high_risk` 改为"未显式标 False 即拦截转人工"。加测试：缺字段的通知必须被拦下。 |
| 4 | 多线程并发写日志损坏（**High**） | connector_audit.py L75-86；audit/sinks.py L25-29 | `JsonlSink` 和 `DebugLog` 文件写加 `threading.Lock` 串行化 + 并发写测试。 |
| 5 | 回退路径丢失审计（**High**） | erp_connector/connector.py L373-386 | `ZpConnector.__init__` 把 `audit` 传入 fallback：`CSVConnector(fallback_dir, audit=audit)` + 测试回退路径有留痕。 |

---

## P1 · 随 SC8 做（不在 hardening 分支）

| # | 项 | 处理 |
|---|----|------|
| 2 | L2 门禁"空转"无审批队列（**Blocker**） | 平台 notifier 本次先暴露**持久化钩子**；SC8 接 `pending_approvals` 持久化队列 + `approve(id, confirmed_by)` 审批流。正是《SC8门禁》要求。 |
| 6b | CRM prompt/措辞硬编码在平台（**Medium，收割过界**） | 把 `crm_notifier/draft.py` 的邮件 LLM prompt 与商务 copy **下放到 SC8 场景层**；平台只留通用 LLM 调用 + 派发引擎。 |
| — | 写动作幂等性与补偿（切库红线①） | 推客户/未来写 ERP 的动作引入**幂等键(UUID)** + 失败补偿/反向撤销，并入 SC8 错误/回滚 SOP。 |

---

## P2 · 切真实库前 · 平台加固（不阻塞 mock，先记 TODO）

| # | 项（严重度） | 处理 |
|---|------------|------|
| 3 | 审计可篡改（High） | `JsonlSink` 加 **hash-chaining**（每条含上条哈希，防篡改可检测）；9月 ClickHouseSink 叠加集中化 + 权限。 |
| 7 | 连接器容错漏洞（Medium） | `_post` 捕获 `(URLError, JSONDecodeError, IncompleteRead)`；5xx 也重试（排除 4xx）；给并行 `_u9c_bom_post` 加重试容错。 |
| — | Pydantic 强 Schema 校验（切库红线②） | 连接器输入/输出在边界用 Pydantic 校验，挡住 U9C/SRM 改字段或脏数据。 |
| — | SRM 限流退避（切库红线⑤） | 携客云 SRM 有限流（30s 重复限制、查询跨度≤60天，错误码 `900301`）；底座加令牌桶限流 + 退避，防多实例并发超限。 |
| — | 凭证安全（切库红线④） | 生产对接 Vault / K8s Secrets 动态注入，不在 `.env` 明文存生产密钥。 |
| 6a | OEM 映射硬编码（Medium） | `REGISTERED_OEMS`/`GENERAL_COLLECTIONS` 改从 config/env/db 读，新增 OEM 不改底座代码。（研发/知识库场景用到前完成即可） |
| 8 | csv_loaders 相对路径（Low，最大遗留债） | mock 夹具移进包内 `shared_tools/fixtures/` + pyproject 配 package data；用 `importlib.resources`，避免打包后 `tests/` 丢失崩溃。 |

---

## P3 · 低优 / 卫生（择机）

| # | 项 | 处理 |
|---|----|------|
| 9 | AccessTrace 违反 AuditSink 契约（Low，静态类型） | `AuditSink.write` 放宽为 `AuditEvent | AccessTrace` 联合类型（或共享序列化 Protocol）。 |
| 10 | LLM 失败静默降级无告警（Low） | `generate_draft` 降级分支加 `logger.warning` / 企微告警，让 AIOps 知道 Claude API 坏了。 |

---

## D1-D6 评审采纳

- D1 拆 models ✅；遗留 #8 打包债待修。
- D2 移除 SQLite ✅；但注意 #4 并发写风险 + 连接器访问痕迹可考虑记 context tag 便于追溯。
- D3 Protocol 解耦 ✅；#1 已是其暴露的门禁漏洞，P0 修。
- D4/D5 ✅ 无异议。
- D6 模板降级 → 必须加 #10 告警，否则掩盖真实故障。

---
*维护：Claude Code 按桶推进，完成项在此打勾并附 commit/PR；阈值（重试次数、限流窗口、偏差天数）定稿后回填。*
