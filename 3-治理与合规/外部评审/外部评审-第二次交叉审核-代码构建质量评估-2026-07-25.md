---
title: "外部评审·第二次交叉审核——全面代码构建质量评估（第三方只读，2026-07-25 收档）"
created: 2026-07-25
status: 历史快照
说明: 第三方对全仓库的只读代码质量评估（未改动任何字符），Paul 2026-07-25 转入。triage 结论（采纳三小件/机制已覆盖/缓办留 S1 收口评审/不采纳）见同目录《tech-debt备忘-2026-07外部审核-留S1收口评审.md》与队列 #107；本文件为报告原文存档，不代表项目采纳立场。
---

# 卓品智能 AI 转型项目 · 全面代码构建质量评估报告（第三方原文）

> **评审说明**：本评估遵循**"只看不动"**原则，未修改任何源文件或配置文件。针对 `企业AI转型` 代码库，从项目结构、代码风格、架构设计、安全审计、测试覆盖及技术债务 6 个维度展开全范围代码质量评估。

## 第一步：项目结构分析

双层架构布局（规划治理 + 模块化单体引擎）；工程规范评估：模块化单体（Modular Monolith），`4-数字员工/` 下 7 个场景工程（SC1, SC7, SC8, QD-A, QD-B, FI1, O2）交叉 import 数为 0，仅依赖 `5-平台底座/zhuopin_platform/`；治理桌与建造车间分界清晰。改进空间：① 脚本与文档混杂（`0-学习与工具/` 平铺运维脚本）；② Web 视图资源无独立 templates/static 目录，HTML/CSS 以字符串常量硬编码在 Python 内。

## 第二步：代码风格与一致性（8.5/10）

正面示例：① `connector.py` L49-72 Pydantic 边界校验与数据清洗（`_ZpPurOrderRow.itemCode` 必填校验器）；② `audit/sinks.py` L41-56 线程安全防篡改 Hash-Chain（类级文件锁+哈希游标）；③ `工具-共享文档编辑锁.py` L49-65 跨 worktree 的 `git rev-parse --git-common-dir` 路径解析。

负面示例：① `sync_sales_data.py` L25 硬编码本地 Windows 绝对路径（`C:\Users\Paul Shao\...\SalesMarketing\...`），建议改环境变量+回落；② 巨型 HTML/CSS 字符串内联（`qd_b_gate/webapp.py`、`sc8/baoguan.py` 61KB），建议抽离 templates/；③ `connector.py` L28/L31 同时导入 `Optional` 与 `Optional as _Opt` 交替使用，建议统一。

## 第三步：架构与设计质量

模块化单体+单一平台底座包；SDD（OpenSpec）+ Rule-of-Three 提级纪律执行到位；场景间零耦合。反模式：① God File——`connector.py` 920 行（OAuth/PO/Stock/BOM/SSL/缓存多职责），建议拆 `auth/stock/bom/po_cache`；② 改判数据流断层——底座 `override_reason` 字段有、FI1 场景无人工改判写入入口，建议为 FI1/FI2 补齐改判确认端点。

## 第四步：潜在风险与安全审计（7.5/10）

🔴 高危 1：git 历史 commit `6ebf962` 残存真实 PII（建议 filter-repo 清理）。🟡 中危 2：`supplychain/.env` sa 超管明文口令（建议最小权限只读账号）。Web：QD-B 上传文件名建议 `secure_filename` 级过滤；`.51` 三 Web 服务（8091/8092/8093）无登录鉴权，灰度期符合预期、建议限定子网。

## 第五步：测试覆盖与可测试性（8.8/10）

全仓 300+ 测试全绿（平台 193+1skip / SC8 226+4skip / SC1 53 / SC7 41 黄金基准零漂移 / O2 20 / QD-B 60 含三真实样本黄金回归）；依赖注入设计良好（audit/secrets/data_sources 可 mock）。建议补测：① wecom-aibot-service 断网自愈与并发高压进件；② sync_sales_data 脱敏边界（脏 JSON/缺字段/嵌套）；③ ISO 26262 ASIL C/D 静态 Guardrail。

## 第六步：综合评分与 Top 5

架构 9.0 / 风格 8.5 / 安全 7.5 / 测试 8.8。Top 5：① 清 git 历史 PII；② 消硬编码路径；③ 拆巨型文件+抽内联 HTML；④ ASIL C/D 静态 Lint；⑤ FI1/FI2 L2 改判交互闭环。附 8-9 月渐进路线图（Phase A 安全清理 / Phase B 架构重构 / Phase C 合规自动化+ClickHouse 迁移）。

---

## 项目侧 triage 批注（Cowork 环境保障线，2026-07-25，完整版见 tech-debt 备忘）

- **采纳三小件**（→队列 #107）：硬编码路径改 env、QD-B secure_filename、脱敏边界单测。
- **机制已覆盖不动**：PII=§四#33 已拍①风险接受（其"成本低"判断不成立）；sa=#91 挂主挂期；LAN 鉴权=灰度期设计内；**事实纠错**：断线自愈单测已存在（test_connection/test_gap_alert，#42 起 TDD），报告此条过时。
- **缓办留 S1 收口评审（≈2026-11 中旬）**：connector 拆分、内联 HTML 抽模板、_Opt 统一、FI2 L2 改判交互（挂 round-2 后）、并发进件测试（可并 #99）。
- **不采纳**：FI1 补改判入口（FI1 已封存暂缓）；ASIL C/D 检查器（本仓库现阶段零 C/C++/ASIL 代码，属 Phase 2 域，红线本体是流程管控）。
