---
status: 待发
owner: Cowork 环境总线
created: 2026-08-08
scope: 协议〇 / 根 CLAUDE.md §5 的条目 ↔ 承载它的载体，逐条核查是否真被承载
批次: 第一批 §2 ／ 第二批 §3.5 ／ 第三批 §3.6（累计已核 9 个载体，未核清单见 §4，不假装核全）
updated: 2026-08-08
---

# 协议与载体承载性核查（第一批 §2 ／ 第二批 §3.5 ／ 第三批 §3.6）

> **只判定、不改载体**（同队列 #253 方向 4 处置口径——规则删除比新增更需谨慎）。
> 唯一例外：值周巡检 prompt 的三处缺口已于 2026-08-08 当日经 Shao Peishen 拍板「直接做掉」补入，见 §2 表首行。

---

## §0 结论摘要（先看这段）

1. 🔴 **存在一类此前无人系统检查过的失效形态：规则写进协议／`CLAUDE.md` §5，却没有任何载体执行它。** 它**不表现为错误，只表现为什么都没发生**——所以不会被任何告警、任何 sweep 检查、任何评审捕捉到。
2. **已核 3 个载体，命中 6 处真实缺口**（其中 3 处已补、3 处待拍板）。
3. 🔴 **最严重的一处不是漏了某条规则，而是 opener 模板库整体停在 2026-08-07**——它是「opener 该怎么写」的**规范本身**，而 2026-08-07 立的「≥2 opener 须附次序与并行矩阵」**恰恰是一条关于 opener 怎么写的规则**，却没进去。**规则最该在的地方没有它。**
4. **一次怀疑被实测推翻，如实记录**：曾怀疑拆件巡逻 prompt 声称依赖的两个机制（`append-row` 子命令、`release` 第⑥项校验）并不存在（因队列 #258 行状态列开头曾读到「待领」）——**实测证伪，两者都真在盘**（源码 grep：`append-row` 5 处／`append_row` 8 处／「暂缓」19 处），#258 已于 2026-08-07 apply。**怀疑经实测被推翻同样是有效产出**，此处留痕以免下次重查。
5. **本核查未覆盖 5 类其它载体**——**未核清单见 §4，明确列出，不假装核全**。（🔄 **第三批已销掉其中 3 类**，见第 7-9 条）
7. 🆕 **第三批（§3.6）最要紧的一条来自反向核**：`工具-落库sweep.py` 的 **11 项守卫里，只有 1 项（部署留痕缺失）写进了协议正文**；其中 **「编辑锁被他人占用 ⇒ 整轮零动作跳过、只写日志不告警」直接与协议〇.8「登记后立即触发 sweep」冲突**——照协议做的动作可能什么都没发生，且无任何信号。**同一形态，这次藏在我们自己写的守卫里。**
8. 🆕 **第三批发现该形态的第三个变种**：不是"规则没落到载体"，也不是"结论没有载体承接"，而是**规则自带的「可机械化判据」指向了一个载体里不存在的字段**——跟进信串行原则要求查「发送状态是否已到『已回件并回灌』」，而 README 状态列**根本没有这个态**（信一推送就停在「✅ 已推送 <时刻>」）。**判据写得像可执行，实际只能靠人记。**
9. 🆕 **§3.5.3 的「正文层比不了」已被证伪并收窄**（§3.5.5）：已安装 skill 虽不在 Windows 磁盘上，但**在 Cowork 沙箱侧有只读挂载**，正文可逐行 `diff`——本轮据此实测出**第四份 skill `zhuopin-followup-letter` 双向都错**（称呼层是反向漂移；状态值 `⏳ 待 Paul 审` 两个版本都错，机器真值是 `⏳ 待你审`，会在**他点批准那一步**被脚本拒绝）。
6. 🔴 **本件 2026-08-08 收到一条实质性勘误，原文照录不掩饰**：本节第 5 条与 §4 第 1 项原写「`CLAUDE.md` §5 的 29 条逐条判定（#253 方向 4 已获授权但**至今未做**）」——**该断言是错的**。实测：`1-转型规划/0-全景路线图/规则体检-CLAUDE.md-§5-29条判定表-2026-08-05.md` **已于 2026-08-05 完整做过**（160 行，A–F 六类、29 条逐条判定、量化到「可减约 4,200 字且不损失任何约束力」），**且已送审、三组均已拍板**（第 1-5 组 (b) 另排／第 6 组 (b) 不立行／第 7 组 (a) 已立队列 #259）。 **根因（值得记，因为它与本件的方法论直接冲突）**：本件对三个载体逐一做了 grep 实测，**却对「这件事是否已做」只凭队列 #253 行内描述就下了断言**——**而队列 #253 的状态列开头明明白白写着「✅ 全部完成（C1-C4 ＋ 方向 4 规则体检，2026-08-05）」**。**同一个 session 当天正在把「判一行状态一律读状态列开头」这条规则写进值周巡检 prompt 与接力自检段，自己却没读。** **⇒ 本件 §1 那句「grep 实测，不靠回忆」必须扩展一句：不只对「载体有没有」实测，对「这件事是否已做」同样要实测**，且**读的是状态列开头，不是行内叙述**。 **代价已发生**：据此设计的第二批任务是重复劳动，且 §四 #56 拍板项 3 让 Shao Peishen 在一件已完成的事上做了一次选择（同 B-0805 那条「白耗他三次决策」的方法学教训，形态重演）。

---

## §1 方法（两句问法，可复用）

对协议〇 与 `CLAUDE.md` §5 的**每一条**，问两句：

1. **它应当由哪个载体执行？**（值周巡检 prompt ／ 拆件巡逻 prompt ／ opener 模板库 ／ 编辑锁校验 ／ sweep ／ 看板 artifact ／ session 接力文件 ／ zhuopin-* skill）
2. **那个载体里真的有这句话吗？**（grep 实测，不靠回忆）

🔑 **与队列 #227① 仓库外载体复检的区别（写明以免被当重复）**：#227① 查「载体里的**参数**是否过时」（如 sweep 周期 4h→1h 后还有 3 处旧值）；本核查查「规则**是否根本没落到载体上**」。
**后者更隐蔽**：过时参数会导致**错误行为**（有迹可循）；未承载的规则导致**没有行为**（无迹可循）。

⚠️ **本核查也有一个必须写明的边界**：「不适用」与「缺口」必须分清，**不得机械套用**。实例见 §2 拆件巡逻那两行——它不产出 opener，故「次序与并行矩阵」对它**不适用**，写成缺口是错的。

---

## §2 已核载体（3 个）

### 2.1 值周巡检 prompt（`weekly-status-update`）—— ✅ 三处缺口已补

| 规则 | 立法时间 | 核前状态 | 处置 |
|---|---|---|---|
| 协议〇.9 措施 C：《本周计划》固定列出机制类可动 WIP 计数 | 2026-08-08 | ❌ **无** | ✅ 已补（D 节固定一行，含可动口径三类排除、②「依赖外部方」由巡检人工核定、#308 落地后改读字段、实测基准 3／8） |
| ≥2 个 opener 须附次序与并行矩阵 | 2026-08-07 | ❌ **完全没有** | ✅ 已补（A 节，含并行判据＝触碰区是否重叠、自检一问） |
| 协议〇.10：取号前须先做并入审核 | 2026-08-08 | ❌ **无** | ✅ 已补（含「值周巡检做这件事比人工 session 更容易，因第 0 步对账审计本就通读全队列」这一协议原有判断） |

**⚠️ 第 2 行是三处里最刺眼的**：值周巡检**正是「一次给出多个 opener」最典型、且每周都在发生**的场景，该规则自 2026-08-07 立法起从未进入它的 prompt。
**顺带补入的三项小口径**：🟡 状态头纪律（尚有子项未完成不得写 ✅）／三连反引号会致 `release` 拒绝释放的实测／沙箱 `git status` 虚报须本机直读。
**全量替换安全性已核**：52→60 行，frontmatter 完好，新增 6 处锚点全中，**原有 8 处锚点逐一复查一处未丢**。

### 2.2 拆件巡逻 prompt（`huijian-chaijian-patrol`）—— 🔴 1 处缺口 ／ 2 处不适用

| 规则 | 判定 | 依据 |
|---|---|---|
| 协议〇.10 并入审核 | 🔴 **缺口，待拍板** | **协议正文明确指名它适用**：「⑵ 拆件巡逻——适用于「升格」环节，不适用于「收件」环节……真正受本条约束的是把某条收件**升格为正式任务**的那一步——升格＝新建任务，须先过并入审核」。而 prompt 三.2（取号）／三.3（`append-row` 追加新行）**完全没有这一步**。 |
| 协议〇.9 措施 C（WIP 上限） | 🟡 **部分适用，待拍板** | 它不生成《本周计划》，故「固定列出计数」不适用；但它**会新建 §一 行**（三.3 派生待领行），故「超限期间新立机制行须先关一条」**在升格为机制行时适用**，prompt 无此约束。**多数派生行是业务回件类、不受措施 C 限制**，故本项影响面小于上一行。 |
| ≥2 opener 须附次序与并行矩阵 | ✅ **不适用** | **它不产出 opener**——只做回灌、销行、登 §四，派活不在其范围（§〇 红线：建造与 commit 发起不进池）。**写成缺口是机械套用。** |
| 其余已核条目（只读 CLI #268／写后反查 #255／`append-row` #258／✅ 写开头／本地日期 #246／决策项选择题＋默认项／具体 session 名／三条写法禁令／批次即扫及其非交互班次预案） | ✅ **均已承载** | 逐条 grep 命中，且多处附了成因与实证日期，质量高于值周巡检 prompt |

### 2.3 opener 模板库（`1-转型规划/0-全景路线图/专线opener模板库.md`，212 行）—— 🔴 2 处缺口，且整体停在 08-07

| 规则 | grep 命中 | 判定 |
|---|---|---|
| 「次序与并行」／「并行矩阵」 | **0 ／ 0** | 🔴 **缺口，且是本次核查最严重的一处** |
| 「可动 WIP」／「未收口」 | **0 ／ 0** | 🔴 缺口（协议〇.9） |
| 「并入审核」／「〇.10」 | **0 ／ 0** | 🔴 缺口（协议〇.10） |
| 「〇.9」／「one-in-one-out」 | **0 ／ 0** | 🔴 同上 |

**现有硬规则条目 §〇.0 – §〇.7 共 8 条**（执行环境标注／opener 标题行／【设置】第四字段／命名不得与执行环境冲突／开场澄清四类＋A 类自检三查／在办清单开工现读／只读 CLI #268／写后反查 #255／`append-row` #258）——**最新一条 §〇.7 标注的是 2026-08-07 的 #258。**

🔴 **⇒ 判定：opener 模板库对 2026-08-07 之后立的规则完全没跟上。** 而这一处的严重性高于 §2.1：

> **值周巡检 prompt 是一个「消费者」；opener 模板库是「规范本身」。**
> 「≥2 opener 须附次序与并行矩阵」是一条**关于 opener 该怎么写**的规则——**它不在 opener 模板库里，等于这条规则最该在的地方没有它**。
> 后果：任何按模板库写 opener 的 session（含值周巡检以外的所有专线）都不会知道要附矩阵；而值周巡检那份已补的，反而成了唯一守这条规则的地方。

---

## §3 实测推翻的一个怀疑（留痕，免下次重查）

**怀疑**：拆件巡逻 prompt 第 49-51／66 行声称依赖 `append-row` 子命令与 `release` 第⑥项校验，均标注「队列 #258，2026-08-06 落地」；而扫队列时读到 #258 状态含「待领」「待你审批五个决策点」字样 ⇒ 疑为**反向缺口：载体声称有机制、而机制不存在**（若成立，定时任务会直接跑挂）。

**实测证伪，两者都真在盘**：

| 核法 | 结果 |
|---|---|
| `工具-共享文档编辑锁.py` 源码 grep | `append-row` **5 处**／`append_row` **8 处**／「暂缓」**19 处** |
| `工具-队列查询.py --row 258` 全文 | 「✅ **已 apply（2026-08-07 状态校准，实测坐实）**」，且附活证据——2026-08-07 当日实际调用 `append-row` 写了 5 个 §二 批次行 |

🔑 **两条值得记的**：

1. **我读到的「待领」是行内合规保留的历史原文**（按「历史记录不追改」纪律，✅ 已写在状态列开头、原文附在其后）。**这正是 #268 冲突警告要处理的形态**——查询工具当场打了「开头片段与其余文本出现互斥关键词……只读开头或只读片段会得到相反结论」，**机制正确工作**。
2. **由此派生一个观察**：对「✅ 开头 ＋ 合规保留历史原文」的行，#268 的冲突警告会**常态触发**。这是可接受的噪声（宁可多报），**但若日后有人因噪声过多而想放宽该警告，须先读本条**——它拦住的正是本次这种真实误判。

---

## §3.5 第二批核查（2026-08-08 同日续做）：四份接力文件 ＋ 三份 `zhuopin-*` skill

### 3.5.1 三份 skill 源码 —— 1 处重灾、1 处真缺口、1 处"链上有而 skill 内无"

| skill | 次序并行 | 并入审核 | 可动WIP | 写后反查 | 判定 |
|---|---:|---:|---:|---:|---|
| `zhuopin-kickoff-prompt` | **0** | **0** | 0 | 1 | 🔴 **重灾区** |
| `zhuopin-queue-audit` | 0 | **0** | **0** | 0 | 🟡 链上有、skill 内无 |
| `zhuopin-rebaseline` | 0 | 0 | 0 | **0** | 🔴 缺写后反查 |

🔴 **`zhuopin-kickoff-prompt` 是本批最严重的一处，而且与 §2.3 的 opener 模板库是同一个问题的第二个面**：**它是「生成 opener／开场 prompt」的 skill**，却对「次序与并行矩阵」**0 命中** —— 即**本项目两个生成 opener 的载体（模板库与 skill），在 2026-08-08 之前都不知道这条规则存在**（模板库已于当日补，**本 skill 未补**）。**成因可精确定位**：两份 skill 末次重打包是 **2026-08-04**（队列 #45，v1.5／v1.6），而该规则 **2026-08-07 才立** —— **时间上必然错过，不是疏忽**。
🟡 **`zhuopin-queue-audit` 属"链上有、skill 内无"**：它是值周巡检的第 0 步，而值周巡检 prompt 已于当日补入并入审核与可动 WIP 计数 ⇒ **经 prompt 调用时规则会生效**；**但若被单独调用（如直接说"跑对账审计"），两项都不在**。**判为真缺口但优先级低于 kickoff-prompt。**
🔴 **`zhuopin-rebaseline` 缺写后反查是真缺口**：它的职责是**跨九个文档重排并要求 grep 零残差**，是全项目写入面最大的 skill 之一，却无写后反查要求（#255）。**其余规则对它不适用**（不出 opener、不建行、不判 WIP）。

### 3.5.2 四份 session 接力文件 —— 判定：**多数规则不适用，非缺口**

四份实测：三份域接力对「次序并行／并入审核／状态列开头／写后反查」**全部 0 命中**，`session接力-Phase1收口.md` 有状态列开头 ×3、写后反查 ×1。

**判定「不适用」而非「缺口」，理由**：接力文件的职责是**承载上下文与进度**（下一会话主攻、状态快照、决策留痕），**操作规则的自然载体是 opener 模板库与各 prompt** —— 规则若同时写进四份接力，即造成 §2.3 那种"同一规则存多套"的漂移风险（07-27 `▶ 粘贴端：` 已实证）。
**唯一例外已确认无缺口**：协议〇.7 降级路径要求把内容写进接力顶部「⏳ 队列更新待补」小节，**该用法在采购域接力实测有 3 处引用、Phase1收口有 1 处留痕节，机制在用**。

### 3.5.4 ✅ 三处 skill 缺口已补齐（2026-08-08 同日，Shao Peishen 选 (b) 三个都补）

三份均以 `save_skill --overwrite` **覆盖同名 skill**（他明示"避免跟以前的版本重复安装"），未新建：

| skill | 版本 | 补入内容 |
|---|---|---|
| `zhuopin-kickoff-prompt` | v1.6→**v1.7** | **新增 1.2 次序与并行矩阵硬规则** ＋ 取号条补协议〇.10 并入审核与〇.9 可动 WIP ＋ 禁止条加一项 |
| `zhuopin-queue-audit` | v1.6→**v1.7** | §四 取号条同补两项 ＋ §三 新增 🟡 复合态判据 ＋ 读取纪律新增「判是否已做须实测且读状态列开头」与「反查工具自身会静默失效」两条 |
| `zhuopin-rebaseline` | v1.0→**v1.1** | **新增 §2.0 写后反查** ＋ 收工段补批次三禁令与「已 ✅ 批次行不可回填」 |

🔴 **仓库源码尚未回写，属已知反向漂移（已安装版新、源码旧）** —— 未做原因是**本 session 上下文预算不足**（约需再输出 250 行），**非遗漏、非判断为不必要**。⚠️ **这暴露了「两向同步」纪律的一个真实执行边界：它在单 session 内可能因预算无法完成，而纪律本身没有为此设降级路径**——本核查按临时降级办法处理（**只改一边时当场登记反向漂移并指定回补批次**），建议下批评估是否将其正式补入纪律。

### 3.5.3 🔴 「源码 ↔ 已安装版」漂移核查 —— **勘误后的准确结论**

> 🔴 **本节原写「已安装版无法在本机定位 ⇒ 漂移核查不可执行」，经 Shao Peishen 提供 Skills 面板截图证伪，2026-08-08 同日勘误如下。**
>
> **准确结论：不是「没安装」，是不以可访问的磁盘文件形式存在。** 四份 `zhuopin-*` 均在册且带 Last updated 日期（`kickoff-prompt` 8/4／`queue-audit` 8/4／`followup-letter` 7/30／`rebaseline` 7/10）。`save_skill` 工具描述明写「**This is the only way to change a saved skill — editing skill files on disk does not persist**」——**故这是机制设计，不是环境故障**，与定时任务 prompt 只能经 `update_scheduled_task` 改是同一范式。
>
> 🔑 **且存在一条可用的比对通道，此前被我漏掉**：**系统提示里注入的 skill description ＝ 已安装版的 description**，可与源码 frontmatter 逐字比对。
> **该通道当场抓到了真实漂移**：`queue-audit` 的 description，已安装版写「**七**类失真」、源码写「**八**类失真」（v1.6 新增的类型⑧）——**漂移就落在那一个字上、持续两天**；而其源码 v1.6 末尾**自己写着**「⚠️ 本次改动源码后尚未同步已安装版」，与面板 Last updated ＝ 8/4、源码改于 08-06 完全吻合。**⇒ 队列 #45 那条纪律的一个活判例**（已随 §3.5.4 修复）。
> ⚠️ **边界仍需如实知悉**：**描述一层能比，正文一层比不了**（`save_skill` 只写不读）。故「正文是否漂移」目前仍无法验证，只能靠「改完一边即同步另一边」这一人守动作保证。

**以下为原节保留的实测记录（路径查找过程，结论已由上方勘误取代）**：

**#45 已实证的形态**（源码改了、已安装版未重打包 ＝ 修复对实际运行的 skill 完全无效）**无法在本机验证**：

| 核法 | 结果 |
|---|---|
| 系统提示给出的 skill location（`AppData\Roaming\Claude\local-agent-mode-sessions\skills-plugin\...`） | 🔴 **该路径整条不存在**（连父目录都不存在） |
| 在 `AppData\Roaming\Claude` 下递归搜 `zhuopin-*` 目录 | **零命中** |
| 在同一根下递归搜任意 `SKILL.md` | **零命中** |
| 对照：定时任务 prompt 真身 `C:\Users\Paul Shao\Claude\Scheduled\<task>\SKILL.md` | ✅ **存在、可读、可经 `update_scheduled_task` 改**（当日已两次实操验证） |

**⇒ 准确结论：不是「没漂移」，是「无法验证有没有漂移」。** skill 本身在正常工作（系统提示列出、可调用），但其**已安装内容不在本机文件系统上**，故源码与运行版的一致性**当前无任何手段可核**。
🔑 **与队列 #254 同族**（注入 session 上下文的 memory 索引与磁盘双向不一致、写入通道已断）——**本项目已有两处"上下文里声称的路径在磁盘上不存在"**。**⇒ 通则：凡系统注入的路径，使用前须先 `Test-Path`，不得直接引用。**
⚠️ **实质影响**：#45 那条纪律（源码与已安装版必须两向一致）**目前只能靠"每次改完记得重打包"这一人守动作保证，且无任何手段事后验证** —— 属承载性核查发现的**不可闭环项**，非本核查能解决，登记待议。

### 3.5.5 🔑 §3.5.3「正文层比不了」已被证伪 —— 找到第二条比对通道（2026-08-08 第 4 轮补记）

> **§3.5.3 的结论「已安装内容不在本机文件系统上 ⇒ 正文一致性无任何手段可核」只对了一半，须收窄。**
>
> **准确表述**：已安装内容**不在 Windows 磁盘上**（§3.5.3 的三条 `Test-Path` 实测无误，结论保留），**但它在 Cowork 沙箱侧以只读挂载存在**——路径 `.claude/skills/<name>/SKILL.md`（沙箱内），四份 `zhuopin-*` 均在，mtime 与本轮覆盖安装时刻吻合。
> **⇒ 正文层可以逐行 `diff`，本轮已实做**（三份回写前后各比一次，另比出第四份 `followup-letter` 的双向漂移，见 §3.6.4）。
> ⚠️ **该通道的边界须一并写明**：它是**给本 session 的只读投影**，若某次会话中途重新 `save_skill`，投影是否即时刷新未验证；故它适合做**开工时的一次性比对**，不适合当作"改完立即回读"的写后反查手段。**交叉验证已做**：三份的 description 与系统提示注入的 description **逐字比对全等**（132/220/138 字符），两个互不相关的通道互证，故判定该投影确实反映已安装版、非陈旧副本。

---

## §3.6 第三批核查（2026-08-08 第 4 轮）：编辑锁校验清单 ／ sweep 守卫反向核 ／ 看板 artifact

**方法同 §1 两句问法。本批 🔴 只判定、不改任何载体**（含拆件巡逻 prompt 三.2 那处措辞瑕疵，本轮未碰该 prompt，仍待同车修）。

### 3.6.1 编辑锁 `工具-共享文档编辑锁.py`（未核清单 #4）—— 3 处真缺口、2 处已知未落地

**现状**：`release` 已挂六项结构校验（①列数 ②§二批次清单须含队列文件自身 ③编号唯一且属 `--reserve` 预留集合 ④§一状态列 P0/P1＋未核断言门槛 ⑤§二模糊状态 ⑥跟进信暂缓一致性）。
**反问「协议里还有哪些同类结构约束本该挂这个咽喉而没挂」**，对该文件 1 479 行逐关键词 grep 实测：

| # | 协议条目 | 咽喉实测 | 判定 |
|---|---|---|---|
| a | 〇.8 §二 **登记状态列禁带 ✅** | 文件内 7 处「✅」**全部**属 ⑤ 的"既不含待也不含✅"模糊态判据，**无一处检查"新增批次行以 ✅ 开头"** | 🔴 **真缺口** |
| b | 〇.8 批次行须声明「**本批变更参数 ＋ 已复检**」(#203) | `本批变更参数` **0**／`已复检` **0** | 🔴 真缺口 |
| c | 〇.8「已复检」须覆盖四类**仓库外载体** (#227①) | `仓库外载体` **0** | 🟡 缺口（内容属人判，但"有没有写这句"可机器查） |
| d | §5 **「✅ 必须写在状态列开头」** | `_leading_status_segment()` **已实现**，但只被 ⑤ 用于 §二 模糊态；**未对 §一/§二 做"✅ 存在却不在开头"检查** | 🔴 真缺口 |
| e | 〇.1 **认领即预登记批次** | `预登记` 6 处**全部**是 ⑤ 的白名单放行文案 | 🟡 缺口，可行性中（需 §一在办行 ↔ §二预登记行的关联键） |
| f | 〇.10 **并入审核**（明文挂 `acquire --reserve`） | 0 | ⚪ **已知未落地**（随 #308 同车），不计新缺口 |
| g | 〇.9 措施 C **超限提示**（明文挂 `release`） | 0 | ⚪ 已知未落地（随 #308） |
| h | 〇.8 **禁裸竖线** | 由 ①列数间接抓住（①的 docstring 明写此意图） | ✅ 已承载 |

🔑 **本组的形态**：**咽喉已经建好了，挂上去的却只是"最近三次事故"那几条**（#225／#247②／#258）。协议里**同族、同样可机器判、判据函数还是现成的**那几条（a、d）没有随之挂上——**不是做不到，是"协议新增一条结构约束 → 挂咽喉"从来不是一个固定动作**。
💡 **a 与 d 的实现成本近乎为零**：a 复用 ③ 已有的"identity 不在快照 ⇒ 本次新增"判别法（新增行才拦，既有行转 ✅ 是 〇.3 的合法收工动作，不能拦）；d 直接调现成的 `_leading_status_segment()`。

### 3.6.2 `工具-落库sweep.py` 守卫的**反向核**（未核清单 #5）—— 方向与本核查相反

**测法**：把 §〇 协议正文（15 行／24 971 字节）与根 `CLAUDE.md` §5（49 行／51 639 字节）各抽成一份文本，逐守卫 grep，命中后**逐条读上下文判断是"描述该守卫行为"还是"只是出现了这个词"**。

| sweep 守卫（代码里有） | 协议〇 | §5 | 判定 |
|---|---:|---:|---|
| **编辑锁被占用 → 整轮跳过、零 git 动作** (#198b) | 0 | 0 | 🔴 **本组最要紧一处** |
| 分叉告警 (#171) | 0 | 1（仅作"这些模块 0 spec 命中"的举例，非行为描述） | 🔴 未承载 |
| 孤儿脏文件告警 ＋ 解除通知 (#236(2)) | 概念出现（〇.1 用它论证预登记），**无告警行为** | 0 | 🔴 未承载 |
| 定时任务镜像差异 (#169) | 「镜像」1 处，属仓库外载体清单、非本守卫 | 0 | 🔴 未承载 |
| 凭据扫描拦截 | 0 | 「凭据」1 处指 `.env` 存放位置，无关 | 🔴 未承载 |
| 场景 spec 缺口 (#298 M1/M2) | 0 | 0（§5 的 spec 13 处均属 openspec 触发门槛） | 🔴 未承载 |
| 在途变更包滞留 | 0 | 0 | 🔴 未承载 |
| **部署留痕缺失 (#229)** | 0 | **2（明写"配套机制侧强制见 #229"）** | ✅ **已承载** |
| 未预期异常兜底 (#198a) | 0 | 0 | 🟡 未承载（兜底件，人无须预知，不建议补） |
| 陈旧待领行检测 (#302) | 0 | 0 | 🟡 未承载（纯提示） |
| 批次隔离 (#238) | 0 | 0 | 🟡 未承载 |

🔴 **「编辑锁占用即整轮跳过」为何最要紧——三件事叠起来才看得出**：① 协议〇.8 明令「§二 登记新批次后**立即触发一次 sweep**」；② 协议〇.7 允许多 session 并发、且允许"已 release 但尚未 commit"这一合法态，**触发那一刻别人正持锁是常态而非异常**；③ `_abort_if_edit_lock_held` 抛的 `SweepAbort` **只写日志、不发 webhook**（实测 `main()` 的 `except SweepAbort` 分支仅在 `is_fork` 时调 `_handle_fork_detected`）。
**⇒ 一个完全照协议做的动作，可能什么都没发生，而且没有任何信号**——正是本核查开篇那个形态（**不表现为错误，只表现为什么都没发生**），只是这次它藏在"我们自己写的守卫"里。**批次不会丢**（下一个整点轮会捡起），**但登记方以为已落库、当场就走了**，这正是 #101 那类"批次在敞口期被误弃"事故的土壤。

### 3.6.3 看板 artifact `zhuopin-project-status`（未核清单 #6）—— **判定：无缺口**，并实测排除一处怀疑

实测（50 347 字节／623 行，mtime **2026-08-08 10:45 本地**）：`机制类` 4／`可动` 2／`WIP` 6／`定时触发型` 5／`状态列开头` 2 ⇒ 协议〇.9 三处落点之⑴（看板显示「机制类未收口 N／8」并超限标红）**已落地**，且分桶逻辑确实排除了 ①定时触发型（独立桶）与 ③🛑 永久关闭（`^🛑` 首标记）两类，与 〇.9 口径一致。

🔑 **曾怀疑一处，实测证伪，留痕免下次重查**：担心它把 `N／8` 显示成权威值、而不标"②依赖外部方尚未机器排除"。**实测其展示段原文已写**：「**「依赖外部方」尚无机器判据、当前未排除**……在此之前**本数偏高**，以值周巡检人工核定为准」——口径注完整，**非缺口**。
**判「不适用」而非缺口的三项**（`并入审核` 0／`孤儿` 0／`部署留痕` 0）：看板是**只读展示层**，〇.10 并入审核只挂取号咽喉、无看板落点，孤儿与部署留痕属 sweep 侧告警，**协议均未要求看板承载**——按 §1 边界，写成缺口是错的。

### 3.6.4 顺带发现两处（在本批范围外，但同族，均为实测）

**(1) 🔴 第四份 skill `zhuopin-followup-letter` 此前从未纳入核查，实测双向都错**

| 层 | 已安装版 | 仓库源码 | 机器真值 |
|---|---|---|---|
| 称呼 | **「Paul」**（description ＋ 正文 3 处） | 「Shao Peishen」（已按 2026-07-30 称呼纪律改） | —— |
| 起草状态值 | **`⏳ 待 Paul 审`** | **`⏳ 待 Shao Peishen 审`** | `readme_table.DRAFT_PENDING_REVIEW_STATUS = "⏳ 待你审"` |

- **称呼层是与 A 段三份方向相反的反向漂移**（源码新、已安装旧）——**同一个"两向同步"纪律，同一天在两个方向上各破一次**。
- 🔴 **状态值层更严重：两个版本都错**。`approval.py:128` 调 `assert_draft_pending_review()`，对状态值做**严格等值**断言 ⇒ 照该 skill 起草的信，**在他点批准那一步被脚本拒绝**；而 `release` 侧不拦（它只拦"新行直接写终态"），**所以卡点会出现在他手上、不是起草者手上**。
- 该 skill 亦**完全不含**三态第三态 `⏸ 暂缓`、`approve_followup_letter.py`、10 分钟冷却窗口 ⇒ **落后现行机制两代**（2026-08-05 阶段二 ／ 2026-08-06 #294）。

**(2) 🔴 跟进信串行原则的「可机械化判据」指向一个载体里不存在的字段**

根 `CLAUDE.md` §5 与 README L24 都写着同一句判据：**「查该收信人最近一封的发送状态列**是否已到「已回件并回灌」**；未到即不得起草下一封」**。
**实测 README「发送状态」列的真实取值集合**：`⏳ 待你审`／`🆕 待发`／`⏸ 暂缓`／`✅ 已推送 <时刻>`／`❌ 已作废`。**「已回件并回灌」只在个别行以自由文本出现（全表 2 处）**，`readme_table.py` 的状态词表里**没有这个态**——信一旦推送，该列就停在「✅ 已推送 <时刻>」，此后不再变化。
**⇒ 对绝大多数已推送的信，这条判据要问的问题在载体上根本无法回答**；规则写得像可机械执行，实际只能靠人记。
🔑 **这是本核查那个形态的第三个变种**：①规则没落到载体（第一批）；②结论做完了没有载体承接（§4.1 判定表）；③**规则自带的判据指向了一个不存在的字段**——三者都不报错，都只表现为"什么都没发生"。

---

## §4 🔴 未核清单（明确列出，不假装核全）

**本批只核了 3 个载体。以下未核，下一批处理：**

| # | 未核载体 | 为什么值得核 |
|---|---|---|
| 1 | ~~根 `CLAUDE.md` §5 的 29 条逐条判定~~ | ✅ **已勘误移出未核清单（2026-08-08，见 §0 第 6 条）**——**2026-08-05 已完整做过并已拍板**，产出＝`1-转型规划/0-全景路线图/规则体检-CLAUDE.md-§5-29条判定表-2026-08-05.md`。 🔑 **但它留了两个「唤醒时动作」，触发条件现已满足，见 §4.1** |
| 2 | ~~四份 `session接力-*.md`~~ | ✅ **已核（§3.5.2）**：判定**多数规则不适用、非缺口**（接力承载上下文，规则载体是 opener 模板库与各 prompt） |
| 3 | ~~三份 `zhuopin-*` skill~~ | ✅ **源码已核（§3.5.1）**：1 处重灾（`kickoff-prompt` 生成 opener 却无次序矩阵）＋1 处真缺口（`rebaseline` 无写后反查）＋1 处链上有 skill 内无（`queue-audit`）。🔴 **已安装版无法在本机定位，漂移核查不可执行（§3.5.3），登记为不可闭环项** |
| 4 | ~~编辑锁 `工具-共享文档编辑锁.py` 的校验清单~~ | ✅ **已核（§3.6.1）**：**3 处真缺口**（a 登记禁带✅／b 本批变更参数+已复检／d ✅须在状态列开头）＋2 处已知未落地（〇.10／〇.9 随 #308）。**a、d 判据函数现成，实现成本近乎为零** |
| 5 | ~~`工具-落库sweep.py` 的 8 类告警／守卫~~ | ✅ **已核（§3.6.2）**：11 项守卫中**仅"部署留痕缺失"1 项写进了协议正文**，其余全未承载；🔴 最要紧的是**「编辑锁被占用即整轮跳过、且只写日志不告警」**——它与协议〇.8「登记后立即触发 sweep」直接冲突 |
| 6 | ~~看板 artifact `zhuopin-project-status`~~ | ✅ **已核（§3.6.3）**：**无缺口**。〇.9 落点⑴已落地且口径注完整；曾疑其未标"②依赖外部方未排除"，**实测证伪、已留痕** |
| 7 | 🆕 机器人侧取号路径 `queue_appender.py::_next_task_id` | 协议〇.10 明文豁免它（收件登记≠建任务），但同时写明「若日后发现为绕开并入审核而走机器人通道建任务，本豁免立即失效」——**该"失效检测"目前没有任何载体，属自带的未承载条款** |
| 8 | 🆕 `zhuopin-followup-letter`（第四份 skill） | §3.6.4(1) 已实测出双向漂移与状态值错配，**但只核了这三项**；其正文其余部分（判例批改法、三要素、编号规则）是否与现行机制一致，未核 |

### §4.1 🔑 08-05 判定表留下的两个「唤醒时动作」，触发条件现已满足（2026-08-08 勘误时发现）

该表末尾自订了两条唤醒条款，**现在正是唤醒时刻**：

| # | 判定表原文要求 | 现状实测 | 建议 |
|---|---|---|---|
| 1 | 「⚠️ **唤醒时请连带复核**：本表判定基于 2026-08-05 的 §5 快照（**47 行**）。若期间 §5 有增删，A/B/C/D/E 的归类需重判。」 | 🔴 **§5 现为 49 行**（实测 `CLAUDE.md` L95–L143），**顶层条目由 21 增至 22**——新增的正是 2026-08-07 的「**≥2 任务须指明次序与并行**」 | **做增量判定即可，不重做 29 条**：只对新增那 1 条判五问（三问 ＋ 承载性两问）。**成本极低，且不做就意味着判定表从此与 §5 不同步** |
| 2 | ~~第 6 组「下次唤醒本表或做月度环境体检时应重新提请」~~ | 🛑 **已终结·永久关闭·仅手动唤醒（Shao Peishen 2026-08-08）** | **本项已出本系统跟踪范围，见下方 §4.2。任何后续核查不得再将其列为缺口或提请项。** |

### §4.2 🛑 一项已永久关闭，任何后续核查不得再提（Shao Peishen 2026-08-08）

**`VP_APPROVERS` 加孙涛这一待办已终结。** 其原话：「**孙涛的事就这样收尾，请你放心我们线下有安排保障，系统里不要再提了，除非我以后手动唤醒。**」

**三条处置口径（同队列 #220 范式）**：① **不提**——任何巡检／审计／体检／承载性核查／决策提醒**均不得再把本项列为缺口、待办或提请项**；② **不删**——判定表第 6 组的判定与「不立行 ＝ 不会被做」的实证原文原样保留，它仍是「规则退休制」的有效判例；③ **仅手动唤醒**——只有他本人明示唤醒时才可重启，**任何 session 不得自行判断「时机到了」而重提**。

🔑 **本次处置的关键动作不是「这次不提」，是把触发点本体摘掉**：判定表第 6 组原写着「下次唤醒本表或做月度环境体检（#98）时应重新提请」——**这句话把「重新提请」变成了由该表自身携带的触发点，任何人唤醒它都会被触发一次**。该句已加删除线并替换为永久关闭标记。**⇒ 通则：终结一个事项时，必须同时找出并摘除所有会使它复活的触发点，否则终结只在本次生效。**

**风险已知并被接受**：配置层的代理权缺口客观仍在，**其保障已移至线下**，不再由本系统跟踪。

---

🔴 **判定表末尾还有一句预言，同样应验了，值得原文照录**：

> 「**若长期未拍板**：本表本身就会变成「发现 2」那种东西——**内容在，但没有机制会再看它一眼**。」

**它已经拍板了，但「没有机制会再看它一眼」依然成立**——2026-08-08 的本核查从头到尾不知道它存在，**直到一次 `git grep` 偶然撞上**。⇒ **这恰恰是本核查要找的那个形态的又一个实例，只是对象不是规则、是产出物**：不是「规则没落到载体」，而是「**结论做完了、也拍板了，却没有任何载体承接它继续往下走**」。

---

## §5 交 Shao Peishen 的拍板项

> **本节只提议、不执行**（§0 已声明只判定不改载体）。三项均为选择题、带默认项。

### 拍板项 1 · opener 模板库补三条规则（§2.3 那三处缺口）

- **(a) 补（推荐）** —— 新增 §〇.8（次序与并行矩阵，2026-08-07）与 §〇.9（协议〇.9 措施 C ＋ 〇.10 并入审核，2026-08-08）。**理由**：模板库是 opener 规范本身，缺了「opener 该怎么写」的规则等于规则空转；且它是所有专线 opener 的来源，补一处受益面最大。
- **(b) 不补，只在各专线 opener 里单独写** —— 代价：每条专线各写一遍、易漂移，且新开专线不会知道。
- **默认项：若不答，按 (a) 执行。**

### 拍板项 2 · 拆件巡逻 prompt 补协议〇.10 升格环节的并入审核（§2.2 第 1 行）

- **(a) 补（推荐）** —— 在三.3「派生的新待领行当场追加」之前插一步：升格为正式任务前先扫 §一 待领池、能并入的并入。**理由**：协议〇.10 正文**点名它适用**，是协议自己指定的适用对象。
- **(b) 不补** —— 代价：拆件巡逻是**每工作日两班**的高频建行入口，它不做并入审核，等于协议〇.10 在最高频的那条路径上不生效。
- **(c) 补，但同时把措施 C（§2.2 第 2 行那项）一并补** —— 一次改到位；代价是全量替换的改动面更大（该 prompt 90 行，比值周巡检更长）。
- **默认项：若不答，按 (a) 执行**（措施 C 那项影响面小、留待下批）。

### ~~拍板项 3 · 下一批是否先做 `CLAUDE.md` §5 的 29 条逐条判定~~ 🔴 **本项作废（2026-08-08 勘误）**

**作废原因**：其 (a) 选项指向的工作**已于 2026-08-05 完成并拍板**（见 §0 第 6 条与 §4.1）。**Shao Peishen 已于 2026-08-08 按默认项选了 (a)，即在一件已完成的事上做了一次选择——白耗一次决策，责任在本件的错误断言，已如实登记。**
**该拍板不予执行**（无可执行内容）；其原意「下一批先核什么」由**下方拍板项 4** 承接。

### 拍板项 4 · 下一批实际做什么（替代作废的拍板项 3）

- **(a) 先做 §4.1 那两个「唤醒时动作」（推荐）** —— 成本极低、且是判定表**自己写明**的触发条件已满足：① 对 §5 新增的 1 条做增量判定（不重做 29 条）；② 按其要求重新提请 `VP_APPROVERS` 加孙涛（已挂一个月零三天）。**做完再转 §4 第 2-3 项。**
- **(b) 直接做 §4 第 2-3 项（接力文件 ＋ 三份 `zhuopin-*` skill）** —— 覆盖面更大，但会让判定表继续与 §5 不同步、孙涛待办继续挂着。
- **(c) 两者本轮一起做** —— 工作量约为 (a) 的三倍，须评估单 session 上下文是否够。
- **默认项：若不答，按 (a) 执行。**

---

## §5.1 第三批的拍板项（2026-08-08 第 4 轮，均为选择题、均带默认项）

> ## ✅ 五项已全部拍板并执行完毕（Shao Peishen 2026-08-08：「**全部按默认执行**」）
>
> | 项 | 结论 | 执行情况 |
> |---|---|---|
> | **5** | (a) 只补 a＋d | ✅ **已并入 §一 #308 子项 F**（F1 §二新增行禁以 ✅ 开头／F2 ✅ 不在状态列开头即报），**未另立行**——代码改动交 CC 随 #308 同车 |
> | **6** | (a) 只补最要紧那一条 | ✅ **协议〇.8「批次即扫」后已加一句**：触发那刻锁被占用则 sweep 整轮零动作跳过、只写日志不告警，**触发后须看一眼 `sweep-commit.log`** |
> | **7** | (a) 一次修到位 | ✅ **`zhuopin-followup-letter` v2→v3**：称呼对齐／状态值改机器真值 `⏳ 待你审`／补四态语义＋批准脚本＋10 分钟冷却＋`🔒人工发送`＋串行原则闸；**已安装版与源码同步完成，实测正文逐行全等** |
> | **8** | (a) 增显式状态 | ✅ **README 扩为四态**，新增 `📥 已回件并回灌 <日期>`，串行原则判据改写为可执行口径；**实测无需改代码**（`readme_table.py` 对非三态值一律按终态处理，不会误发） |
> | **9** | (a) 设降级路径＋挂开工自检 | ✅ **入 `专线opener模板库.md` §〇.10**（允许只改一边但须在 §二 批次行登记反向漂移）＋ **环境总线 opener 开工自检增设第 4 查**（四份 skill 源码 ↔ 沙箱挂载全量 diff） |
>
> 🔑 **执行时实测到的一个新事实（已写入 §〇.10）**：`save_skill --overwrite` 后**沙箱挂载即时刷新**（同分钟内读到新内容）——故它**不只能做开工比对，也能做写后反查**；**样本仅 1 次**，重要场合仍叠加 description 通道交叉验证。
> **本轮收尾实测**：四份 `zhuopin-*` skill 源码 ↔ 已安装版**正文差异行全部为 0**，两向同步首次全部清零。
>
> **次序与并行**：拍板项 5／6／7／8 **触碰区两两不重叠**（5＝`工具-共享文档编辑锁.py`／6＝协议正文 `.md`／7＝skill 已安装版＋源码／8＝README 状态词表＋`readme_table.py`），**四项可完全并行**；其中 **5 与 #308 同文件、须错开或同车**（#308 已明文要在该文件加 〇.9／〇.10 两处），**建议 5 直接并入 #308**，不另起行。

### 拍板项 5 · 编辑锁补挂哪几条（§3.6.1 的 a／b／d）

- **(a) 只补 a ＋ d（推荐）** —— 两条判据函数现成、实现近乎零成本，且各自有实证事故背书（a＝B-0728 两批石沉大海；d＝2026-08-03 一次性抓出 6 行）。**并入 #308 同车，不新立行。**
- **(b) a ＋ b ＋ d 三条都补** —— b（批次行须写「本批变更参数／已复检」）覆盖面最大，但它是**内容型**约束，机器只能查"有没有写这句话"、查不了写得对不对，**有把人训练成写套话的风险**。
- **(c) 都不补** —— 维持现状，这三条继续只靠人守；代价是 a 与 d 各自已复发过，按规则退休制迟早要挂。
- **默认项：若不答，按 (a) 执行。**

### 拍板项 6 · sweep 守卫是否写进协议正文（§3.6.2）

- **(a) 只补最要紧那一条（推荐）** —— 在协议〇.8「批次即扫」那句后加一句：**"若此刻编辑锁被他人有效占用，本轮 sweep 会零动作跳过且只写日志、不告警；触发后请顺手看一眼 `reports/sweep-commit.log` 或等下一个整点轮"**。**一句话，不新增条目、不新增载体。**
- **(b) 在协议〇 加一节「sweep 守卫清单」列全 11 项** —— 认知错位一次性清除；代价是协议正文再长一截，且这 11 项本就多数是"人无须预知"的兜底件（§5 规则退休制正是在治这个）。
- **(c) 不补，改为给"编辑锁占用即跳过"加一条 webhook 告警**（改代码而非改文字）—— 更彻底（不依赖人读到），但属机制类新增，须走 #308 或另立行，且与协议〇.9 措施 B「守卫 one-in-one-out」冲突（要新增守卫就得退一个）。
- **默认项：若不答，按 (a) 执行。**

### 拍板项 7 · `zhuopin-followup-letter` 的双向修（§3.6.4(1)）

- **(a) 一次修到位（推荐）** —— 覆盖安装（`save_skill --overwrite`）＋ 源码同步，一并把 ⑴称呼改「Shao Peishen」⑵状态值改 **`⏳ 待你审`**（机器真值）⑶补三态第三态 `⏸ 暂缓`、批准脚本与 10 分钟冷却窗口。**理由：状态值错配会卡在你按批准的那一刻。**
- **(b) 只改状态值那一处，其余留待下批** —— 最小改动、最快；但称呼与"落后两代"仍在。
- **(c) 先不动** —— 该 skill 近期未被调用过；代价是下次真用它起草，卡点落在你手上。
- **默认项：若不答，按 (a) 执行。**

### 拍板项 8 · 串行原则的判据要不要救（§3.6.4(2)）

- **(a) 给 README 增一个显式状态 `📥 已回件并回灌 <日期>`（推荐）** —— 判据立刻可执行、也让"这封信闭环了没有"变成看一眼就知道；代价是**每封信多一次人工改状态**，且需同步 `readme_table.py` 词表（否则新状态会被机器当"终态"处理——**实测现行逻辑对非三态值一律视为已推送类终态，故不改代码也不会误发，风险可控**）。
- **(b) 把 §5 与 README 里那句判据改成"人判"** —— 承认它就是人守规则，不再写成"可机械化判据"；零成本，但串行原则从此完全无痕可查。
- **(c) 维持现状** —— 判据字面留着、实际执行不了。**不推荐**：它正是本核查要找的形态。
- **默认项：若不答，按 (a) 执行。**

### 拍板项 9 · 「两向同步」要不要为"单 session 预算不足"设正式降级路径（§3.5.4 顺带评估）

**背景**：队列 #45 的纪律是「skill 源码与已安装版必须两向一致」，但**它假定一次改动能在一个 session 内做完两边**。2026-08-08 实测两次破例、**方向还相反**：第 3 轮补了已安装版、源码欠着（预算不足）；`followup-letter` 则是源码改了、已安装版欠着（2026-07-30 至今）。**⇒ 破例不是偶发，是这条纪律的常态。**
**本轮实测的一个新事实支持设降级路径**：§3.5.5 找到的沙箱只读挂载**让"欠了哪一边"变得可机器检测**（逐行 `diff` 四份 skill，本轮耗时约 1 分钟）——**降级路径不必靠人记得登记，可以靠开工时扫一遍**。

- **(a) 设降级路径 ＋ 挂到开工自检（推荐）** —— 纪律改为：**允许只改一边，但当场在批次行登记反向漂移并指定回补批次**；同时把「四份 skill 源码 ↔ 沙箱挂载 `diff`」加进**环境总线 opener 的 §〇 开工自检**（现为三查 → 四查）。**理由：只写"要登记"仍是人守（今天已破两次）；配一个开工时的机器扫，欠账最多存活一个 session。**
- **(b) 只设降级路径、不加自检** —— 改动最小；代价是仍靠人记得登记，与今天破例的成因完全相同。
- **(c) 不设，维持"必须两向同步"** —— 纪律保持严格；代价是**它今天已被破两次而无人察觉**，按规则退休制第 3 次即须机制化或删除。
- **默认项：若不答，按 (a) 执行。**

---

## §6 变更记录

| 日期 | 变更 | 执行方 |
|---|---|---|
| 2026-08-08 | 立件。第一批核 3 个载体，命中 6 处缺口（3 处已补 3 处待拍板）；实测推翻 1 个怀疑；明确列出 6 类未核载体 | Cowork 环境总线 |
| 2026-08-08 | 第二批（§3.5）：四份接力 ＋ 三份 skill 源码；同日勘误 §3.5.3、补 §4.1／§4.2 | Cowork 环境总线 |
| 2026-08-08 | **第三批（§3.6）**：编辑锁校验清单（3 真缺口）／sweep 11 项守卫反向核（仅 1 项已承载）／看板 artifact（无缺口，1 处怀疑实测证伪）；顺带实测出第四份 skill 双向漂移＋状态值错配、串行原则判据指向不存在字段；**§3.5.5 证伪「正文层比不了」，找到沙箱只读挂载这条比对通道**；未核清单销 3 项、新增 2 项；新增拍板项 5-8 | Cowork 环境总线 |
| 2026-08-08 | **拍板项 5-9 全部按默认项执行完毕**（他答「全部按默认执行」）：#308 加子项 F ／ 协议〇.8 加 sweep 跳过提示 ／ `followup-letter` v2→v3 双向修 ／ README 扩四态新增 `📥 已回件并回灌` ／ opener 模板库 §〇.10 两向同步降级路径＋开工第 4 查。**收尾实测：四份 skill 正文差异行全部为 0**，两向同步首次全部清零 | Cowork 环境总线 |
