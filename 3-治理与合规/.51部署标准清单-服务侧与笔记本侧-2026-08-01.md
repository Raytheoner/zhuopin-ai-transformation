---
status: 生效
title: "《.51 部署标准清单》——服务侧与笔记本侧（队列 #96 Cowork 起草半边）"
生效日: 2026-08-01（Shao Peishen 拍板）
created: 2026-08-01
执行方: Cowork · 环境保障线（起草）→ CC 平台（对齐 psm1 与各 deploy-server.ps1）
来源: 队列 #96（07-24 登记，07-24 补笔记本侧素材⑤⑥⑦）
挂接: CLAUDE.md §5「发布即收口纪律」第 ② 关（部署段）门禁化
---

# 《.51 部署标准清单》——服务侧与笔记本侧

> **本件是什么**：把四个已上线服务真实踩过的坑，固化为下一次部署前**逐条打勾**的清单，并挂进「发布即收口」四关的第 ② 关（部署段）作门禁。
> **本件不是什么**：不是新架构方案。所有条目都来自**已发生的真实事故与已生效的既有实现**（逐条给了取证位置），不含任何"应该这样做"的推测。
> **起草说明**：Cowork 环境保障线只起草 `.md`；`ZhuopinDeploy.psm1` 对齐与各 `deploy-server.ps1` 改薄封装是 **CC 半边**（见 §八）。

---

## 〇、适用范围与两条前置红线

**适用**：`.51`（192.168.100.51）上的服务部署/重部署，以及**笔记本侧**常驻计划任务（sweep／机器人监听／决策提醒等）的注册。

**🔴 前置红线一：新场景一律不新起端口**（CLAUDE.md §5，Shao Peishen 2026-07-29 定）。本清单默认服务对象是**存量四服务**（8091 保供看板／8092 命令中心／8093 QD-B／8094 FI2）的重部署与运维。**新场景不得照抄本清单去起第五个端口**——一律注册到统一门户路由 `/{域}/{场景}` 下并预留网关 auth 接入点，设计见《统一门户架构决策件-SSO与权限-2026-07-29.md》。本清单的 §三 各条在门户形态下**仍然适用**（BOM／防火墙／触发器／绝对路径的坑不因换形态而消失），只是端口与防火墙规则由门户统一持有。

**🔴 前置红线二：部署 ≠ 放开自动执行**。发布＝部署供试用与反馈，两道门禁（只归档/通知不自动执行、L2 人工确认）照旧（CLAUDE.md §7）。

---

## 一、先分清两侧——**最容易致命的一处混淆**

| | **.51 服务侧** | **笔记本侧计划任务** |
|---|---|---|
| 典型对象 | `BaoguanWebServer`／`CommandCenterWeb`／`QdBWebServer`／`Fi2WebServer` | `ZhuopinCommitSweep`／`ZhuopinAibotDevListener`／`ZhuopinDecisionReminderDaily` |
| 文件根 | `C:\baoguan\`／`C:\qd-b\`／`C:\fi2\`（普通本地盘） | `C:\Users\Paul Shao\OneDrive\Projects\企业AI转型`（**OneDrive 个人版路径**） |
| 运行身份 | **SYSTEM + ServiceAccount + RunLevel Highest**（四服务现状一致） | **当前账户 + `LogonType S4U`**（**不可用 SYSTEM**，见 §四⑤） |
| 触发器 | `AtStartup` | `AtStartup`／`AtLogOn`／按周期，视任务而定 |

> 🔴 **别把 §四⑤⑥⑦ 套到 .51 服务上**。⑤"慎用 SYSTEM"成立的**唯一原因**是笔记本仓库在 OneDrive 个人版路径下、ACL 归当前用户，SYSTEM 无权访问；`.51` 的 `C:\baoguan\` 等是普通本地盘，SYSTEM 完全可读写，四服务用 SYSTEM 是**正确且已验证**的。读到⑤就去改 `.51` 服务的 Principal，会把一个好好的配置改坏。
> **本条本身就是本清单存在的理由之一**——队列 #96 行里⑤⑥⑦ 只标了"笔记本侧计划任务专属"一句，容易被扫读漏掉。

---

## 二、部署前置（进入部署段之前必须已成立）

对应「发布即收口」第 ① 关，逐条打勾：

- [ ] 全量测试绿 + 零回归 + 黄金基准不漂移
- [ ] openspec 归档（业务场景类）；机制/工具类按 CLAUDE.md §5「openspec 触发门槛」三类判定是否需先补 spec
- [ ] 场景 `CLAUDE.md` 已更新
- [ ] 队列 §三 无本场景冻结标（口径重梳期不得部署在途建造物）

---

## 三、`.51` 服务侧标准清单（七类坑，逐条打勾）

### ① UTF-8 BOM —— 脚本必须带 BOM
- [ ] `deploy-server.ps1` 与其生成的启动包装脚本**文件头为 `EF BB BF`**
- **为什么**：`.51` 内建 Windows PowerShell 5.1 对**无 BOM** 的 UTF-8 文件按系统 ANSI 解码，中文注释/字符串直接变成语法错误。
- **实证**：2026-07-21 命令中心首次部署即因此报错（队列 #65）。
- **现状核对（2026-08-01 逐文件 `head -c 3` 取证）**：保供看板 ✅／命令中心 ✅／QD-B ✅／FI2 ✅／**`5-平台底座/wecom-aibot-service/deploy-server.ps1` ❌ 无 BOM（文件头为 `# =`）——即队列 #167 记录的唯一缺口，至今未修**。

### ② 防火墙入站规则 —— 必须显式创建，且**不得限 LocalSubnet**
- [ ] `New-NetFirewallRule -DisplayName "<服务>-WebServer-<端口>" -Direction Inbound -Protocol TCP -RemoteAddress Any`
- **为什么**：本机 `127.0.0.1` 冒烟 200 **不能证明**外部可访问；缺规则时外部一律超时，且这个假象极具迷惑性。组员笔记本常在不同网段（如 WLAN），`LocalSubnet` 会把他们挡在外面。
- **实证**：2026-07-21 命令中心本机 200、外部全超时，补 `CommandCenter-WebServer-8092` 后恢复（队列 #65）。
- **现状命名惯例**：`Baoguan-WebServer-8091`／`CommandCenter-WebServer-8092`／`QdB-WebServer-8093`／`Fi2-WebServer-8094`。

### ③ 触发器与运行身份 —— `AtStartup` + SYSTEM，**不用 `AtLogOn`**
- [ ] `New-ScheduledTaskTrigger -AtStartup`
- [ ] `New-ScheduledTaskPrincipal -UserId "SYSTEM" -LogonType ServiceAccount -RunLevel Highest`
- **为什么**：`AtLogOn` + 交互式登录在"会话已断开"状态下**无法启动新进程**（`0x80070002`）。
- **实证**：2026-07-23 `CommandCenterWeb` 原按 `AtLogOn` 注册，Administrator 会话断开时服务短暂中断（队列 #79 附带发现）。

### ④ 绝对路径烘焙 —— 不依赖运行时 PATH
- [ ] 注册时用 `Get-Command` 解析出**绝对路径**并烘焙进启动包装脚本，不写裸命令 `python`
- **为什么**：`python` 常是当前用户级安装（`AppData\Local\Programs`），SYSTEM 账户的 PATH 里根本没有该目录，裸命令**连"找不到命令"都不会明确报**。
- **实证**：同 #79；`0-学习与工具/工具-注册落库sweep计划任务.ps1` 头部注释有完整复盘。

### ⑤ 失败自愈参数
- [ ] `-RestartCount 3 -RestartInterval (New-TimeSpan -Minutes 1)`
- **现状**：命令中心／QD-B／FI2／保供看板四服务均已带；`WecomAibotService` 的注册脚本头部明确说明"Windows 计划任务的 RestartCount/RestartInterval 只支持单一固定间隔"，另行处理。

### ⑥ 共享口令门禁（#160，2026-07-30 起）
- [ ] 目标机 `.env` 含 `ZP_GATE_PASSWORD=<与其余服务同值>`
- [ ] 部署脚本对"已存在的老 `.env` 缺这一行"场景**幂等补行、不覆盖已填值**（SC8 已实现，可照抄）
- **注意**：`ZP_GATE_PASSWORD` 未配置时门禁**自动 no-op**——即"忘了填"不会报错、只会静默零鉴权，属静默失效形态，必须显式核对而非依赖报错。
- **程序化访问**：`/api/ping` 已豁免；其余程序化读取一律走 `X-Auth-Token` 请求头。

### ⑦ 凭据不入库
- [ ] 真实凭据只写 `.env`（已 gitignore），不进 commit／日志／audit 明文
- [ ] 口令类**不写进任何会被提交的文件**（含队列与接力）——2026-07-06 销售域 PII 入 git 历史即反例，至今未清

---

## 四、笔记本侧计划任务标准清单（⑤⑥⑦，**只适用笔记本侧**）

### ⑤ Principal 慎用 SYSTEM —— 改当前账户 + `LogonType S4U`
- [ ] `New-ScheduledTaskPrincipal -UserId "<当前账户>" -LogonType S4U`（不落密码、不要求保持登录会话）
- [ ] 不加 `-RunLevel Highest`（sweep 类任务只需一般用户权限）
- **为什么**：仓库在 OneDrive 个人版路径下，ACL 归当前用户，**SYSTEM 触发的任务静默权限被拒——零执行、零日志，而 `Get-ScheduledTaskInfo` 可能仍显示"已运行"**。
- **⚠️ 但 S4U 也有门槛（2026-07-31 实测新增）**：注册 `LogonType S4U` 需要"以批处理作业登录"权限，**赋予该权限本身要求管理员权限**——本机注册 `ZhuopinDecisionReminderDaily` 时实测 `Access denied`，已隔离验证 S4U 失败／Interactive 成功。**退路 = `LogonType Interactive`**（同 `ZhuopinAibotDevListener` 既有先例），**代价是任务触发时需当前用户已登录本机**。
- **选型口径**：能注册 S4U 就用 S4U；报 `Access denied` 就退 Interactive **并在任务说明里写明"需用户已登录"这一约束**，不要反复试。
- **两个都真实存在的先例（2026-08-01 本机只读核实）**：`ZhuopinCommitSweep` 的 `Principal = Paul Shao / S4U`——**S4U 注册成功且在跑**；而同一台机上注册 `ZhuopinDecisionReminderDaily` 时 S4U 报 `Access denied`，退 Interactive。**即 S4U 不是普遍被禁**，成败随注册时的权限上下文而变，所以口径是"先试 S4U、失败即退"，而不是"S4U 不可用"。

### ⑥ 笔记本电池策略
- [ ] `New-ScheduledTaskSettingsSet ... -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries`
- **为什么**：该 cmdlet **默认** `DisallowStartIfOnBatteries`，拔电后任务直接不触发。

### ⑥bis `StartWhenAvailable = True` —— **错过必须补跑**（2026-08-01 审计新增）
- [ ] `New-ScheduledTaskSettingsSet ... -StartWhenAvailable`
- **为什么**：笔记本会休眠、会关机、会没登录。该项为 `False` 时，**错过的触发时刻直接作废、不补跑**，且**不报错、不留痕**——是标准的静默失效。
- **实证（本机三任务并列核实）**：`ZhuopinCommitSweep` **True**（#189 已修）／`ZhuopinAibotDevListener` **True**／`ZhuopinDecisionReminderDaily` **False ❌**（= 队列 #199）。
- **🔴 这条为什么单列一节**：`ZhuopinDecisionReminderDaily` 是 **2026-07-31 新建**的任务，而 #189 **同日稍早**刚为 sweep 修过同款设置——**新任务原样复发了刚修完的老坑**，因为注册脚本是照抄的、没人核对设置项。**这正是本清单存在的理由的最好例证**：坑修过一次≠不会再犯，得有清单。
- **连带风险**：#192-A 拍板的"机器人锁忙推迟件的兜底 flush"正挂在这个任务上，其关键收益写的是"保证不会跨周末"——载体不可靠时兜底一并不可靠。
- **⚠️ 但危害程度勿夸大（2026-08-01 同日实测更正）**：`ZhuopinDecisionReminderDaily` 的 `LastRunTime = 08-01 09:31:52 本地`、`NumberOfMissedRuns = 0`，而当日休眠恢复是 09:25:52——**机器唤醒后该任务确实跑了**。故"错过即永久不跑"是**推断而非观察**，`StartWhenAvailable` 的实际语义边界比直觉窄，**本条按"设置项应保持一致"这一卫生理由保留，不按"必然漏跑"立论**。真正需要查的是同次实测发现的 `LastTaskResult = 0x800710E0`（执行被拒绝，非成功）——见队列 #199。
- **📌 方法学**：本条初稿的推断在 20 分钟内被同一批实测数据部分推翻。**清单类文档尤其容易把"配置看起来不对"写成"必然出事"**——凡未实际观察到后果的条目，一律写成"应对齐"而非"会漏"。
- **判排查用字段（队列 #219，2026-08-04 补，成因＝当日真实丢一轮）**：Task Scheduler 操作日志对"该跑而没跑"是盲的——它只在**实际启动/结束**时记事件，未触发不产生任何日志条目；查"是否漏跑"**只能靠 `Get-ScheduledTaskInfo -TaskName <名>` 返回的 `NumberOfMissedRuns` 字段**（配合 `LastRunTime`／`NextRunTime` 交叉核对）。`NumberOfMissedRuns > 0` 即证实确有一轮该触发而未触发（`StartWhenAvailable` 恢复后也未必立即补跑，见 #219 行内三条机制结论）。**⚠️ 已知局限，不虚构：为什么本条已单靠自身载体不够**——决策提醒的自动兜底路径见队列 #219（sweep 每小时复用同一判定作第二载体），但**本清单本身仍是纯静态检查表，不是自动巡检脚本**——`NumberOfMissedRuns` 需要人/脚本主动去查才会被发现，不查就不知道。

### ⑥ter 三任务设置一致性核对（注册任何新计划任务后跑一次）
```
foreach($n in @("ZhuopinCommitSweep","ZhuopinAibotDevListener","ZhuopinDecisionReminderDaily")){
  $t=Get-ScheduledTask -TaskName $n
  "{0,-32} MultipleInstances={1} StartWhenAvailable={2} Principal={3}/{4}" -f `
    $n,$t.Settings.MultipleInstances,$t.Settings.StartWhenAvailable,$t.Principal.UserId,$t.Principal.LogonType
}
```
- [ ] 新任务的 `MultipleInstances`／`StartWhenAvailable`／电池两项与既有任务**逐项对齐**，不一致必须有写明的理由
- **现状基线（2026-08-01）**：三者 `MultipleInstances` 均为 `IgnoreNew` ✅（sweep 因此不会双实例并跑）。

### ⑦ 验证标准改"空跑也写日志"
- [ ] `Start-ScheduledTask` 后，目标日志文件**必须出现新时间戳**——哪怕内容只是"本轮无事可做"
- **为什么**：`LastTaskResult=0` **不等于**任务真的跑了。没有新时间戳＝脚本根本没启动（回查 Principal／权限）。
- **⚠️ 判"任务为何没跑"之前，先确认机器这段时间是否在运行**（CLAUDE.md §5，2026-08-01 实证）：查事件日志 `ID=27／42／107`，并对比两次读时钟的真实间隔——**休眠期间任务排队 `Queued`、唤醒即执行，是 Windows 正常行为，不是缺陷**。2026-08-01 曾因漏查此项把一次正常休眠误判为 P1 缺陷。

#### ⑦bis 「日志零行」的含义 —— **两态判据，切换点绑定队列 #198(a)**

| 时期 | 「日志零行」意味着 | 排查动作 |
|---|---|---|
| **现在（#198(a) 未修完）** | **二义**：① 任务根本没启动 **或** ② 跑了但抛了未预期异常（`main()` 只有 `except SweepAbort`，其余异常直接冒泡，`_flush_log()` 没机会执行） | **必须并查**电源事件 `ID=27／42／107` + 计划任务 `LastTaskResult`，**不得**直接下"任务没执行"的结论 |
| **#198(a) 修完后**（Shao Peishen 2026-08-01 拍板确立） | **单义**：任务根本没启动（触发器没生效／机器未运行／Principal 权限被拒） | 直接查触发器与 Principal，排查路径收敛 |

- **为什么要写成两态而不是直接改判据**：判据的正确性**依赖代码修完**，提前按新判据下结论就会重演 2026-08-01 那次误判（当时正是因为零行无法区分这两个成因，绕了一整轮才靠 `ID=27` 收敛）。**切换点＝#198(a) 落地并验证**，届时请回来把本表上半行划掉。
- **时间基准**：#198(a) 新增的异常日志行须与既有 `_now_utc_str()` 一致、**显式写 `UTC`**；本清单一切时刻引用同样遵 CLAUDE.md §5「时间戳必判 UTC vs Win 本地」——`LastRunTime`／事件日志／文件 mtime 是**本地**，sweep 日志与审计 JSONL 是**真 UTC**，同一段推理里混用必错。

### 笔记本侧路径纪律
- [ ] 计划任务 Action 指**主工作区稳定路径**，绝不指向任何 `.claude\worktrees\<name>`（建造 worktree 会被删，删了任务就废）

---

## 五、冒烟三件套（部署后立即跑，三项全绿才算部署完成）

1. [ ] **`/api/ping` 200**（`.51` 本机 `127.0.0.1` **与** 外部机器各一次——只测本机会漏掉 §三② 那类防火墙问题）
2. [ ] **关键页可访问**：未登录应 **302 到登录页**（门禁生效）；带 `X-Auth-Token` 或登录 Cookie 后 **200**
3. [ ] **一次真实全量重算/主流程跑通**（如 SC8 `/api/refresh`、FI2 一次真实 `POST /run`），并**核对返回内容含本次新增字段/结构的关键字**，不能只看 HTTP 200

> **补充核验（涉进程重启时必做）**：用**文件哈希比对**或**进程启动时间**确认新代码确已部署，而不是"运气型"未改动假象（#147／#168 部署时已固化此做法）。

---

## 六、回滚 SOP（部署前必须已在位，不是出事再想）

**服务侧**：
```
schtasks /End    /TN <TaskName>        # 停止本次运行
schtasks /Delete /TN <TaskName> /F     # 彻底注销（回到未部署态）
```
**门禁回滚（#160）**：清空 `.51` 上对应 `.env` 里 `ZP_GATE_PASSWORD=` 的值 + `schtasks /End` + `/Run`，即恢复零鉴权原状，**无需回滚代码**。

**代码回滚**：`sync-to-server.ps1` 重推上一个已验证 commit 的产物 + 重启任务。

- [ ] 回滚命令已实际写进本次部署的交接记录（不是"照本清单查"，是写下具体 TaskName）

---

## 七、常驻自愈核验（"可常驻"是发布条件，不是"跑一次成功"）

- [ ] 服务从**计划任务**拉起（不是从临时 worktree 手工跑一次）
- [ ] 触发器为 `AtStartup`（服务侧）——重启机器后自动恢复
- [ ] 有失败重启参数（§三⑤）
- [ ] **重启验证时先清理孤儿进程**：`Stop-ScheduledTask` 不级联杀子进程，残留 `python.exe` 会导致"双实例"假象（机器人服务已多次踩到，同 BotID 双连接风险见 #90 D5／#110）
- **反面教材**：2026-07-16 企微机器人停摆 24h49m，即"跑通了但不可常驻"的代价。

---

## 八、现状差距（**CC 半边待办，本清单起草方不执行**）

| 项 | 现状（2026-08-01 取证） | 待办 |
|---|---|---|
| 笔记本→`.51` 推送侧 | ✅ **已统一**：`ZhuopinDeploy.psm1` 导出 `Sync-ZhuopinPlatformAndApp`／`Restart-ZhuopinTask`，**五份 `sync-to-server.ps1` 全部已 import** | 无 |
| `.51` 侧 `deploy-server.ps1` | ❌ **未统一**：五份各自独立、各约 130–175 行，四类坑靠"照抄上一个"传承 | **#96 CC 半边**：抽公共段进 `ZhuopinDeploy.psm1`，各场景脚本改薄封装统一调用 |
| 机器人部署脚本 BOM | ❌ `wecom-aibot-service/deploy-server.ps1` 无 BOM | **队列 #167**，未修 |
| 新场景端口 | ⚠️ 四服务为平级独立端口（历史形态） | 存量慢慢收编；**增量走统一门户**（§〇 红线一） |

---

## 九、挂接方式（怎么变成门禁而不是又一份没人看的文档）

1. **CLAUDE.md §5「发布即收口纪律」第 ② 关**（部署段）指向本件——部署段的"冒烟三件套 + 回滚 SOP 在位 + 可常驻"三项，展开即本件 §五／§六／§七。
2. **场景固定流程第 7 步**（发布收口）执行时，把本件 §二～§七的勾选结果**写进场景 `CLAUDE.md` 的「部署状态」段**，作为可追溯证据。
3. **新增坑就地回灌本件**——凡新踩一类坑，当场在对应节追加一条并写明实证位置，不另起文档。

---

## 十、两点已定（2026-08-01 Shao Peishen 拍板转「生效」）

1. **本件放置位置＝保留 `3-治理与合规/`**（与《SC8上线前置门禁-错误回滚SOP与黄金基准校验.md》同类）——它要挂进「发布即收口」第 ② 关当门禁，落治理件便于 IATF 审核直接引用。
2. **门禁强度＝自证型**（逐条打勾 + 结果写进场景 `CLAUDE.md`）。**不要求**贴冒烟三件套原始输出。**复评时点＝2026-11 S1 收口前的外部对抗性评审**（CLAUDE.md §5）——届时若外部评审认为自述型证据不足，再升级为"原始输出入交接记录"。

> **📌 一处如实留痕**：以上两项他答的是"同意转「生效」"，未逐条点选。两项均**按起草方在会话中给出的建议默认执行**（保留治理件／先自证型），此处显式标注以便审计；**如需改任一项，一句话即可，改动成本极低**（前者＝移动文件并更新台账指针，后者＝在 §五 加一条"输出粘贴"要求）。

---

## 十一、笔记本 ↔ `.51` 文件读写通路（2026-08-31 `OP-0831-L` 首跑灰度批实测补，队列 #403／#394）

> **本节存在的理由**：本清单 08-01 定稿以来只写"部署到 `.51` 之后怎么办"（七类坑），**从未记载"怎么到 `.51`"这件事本身**——`.51部署标准清单` 全文此前 SSH／凭据管理器／通路 三词零命中。2026-08-27／2026-08-31 两轮 LAN 实测踩出**两条互不相同、用途也不同**的通路，一并落档，免得下一个人从零摸索。

**两条通路各管一件不同的事，不能互相替代**：

| | 通路 A：SSH 远程执行 | 通路 B：Windows 凭据管理器 + SMB |
|---|---|---|
| **解决什么** | 在 `.51` **那一端**跑命令、读 `.51` 本机文件（如 `.env` 键名、进程/服务状态） | 让**笔记本本地**代码用 `pathlib.Path` 原生读写 `.51` 上的文件（如 outbox 中继 `mark_delivered` 需要的原地改写） |
| **典型用途** | 查 `.51` 上某服务实际加载的配置、查计划任务/进程状态、执行只读诊断脚本 | `WECOM_AIBOT_OUTBOX_PATHS` 这类"本地代码直接读一个远端 UNC 路径"的场景 |
| **两者不可互相替代** | SSH 能执行命令，但不能让*本地* `Path.read_text()`/`os.replace()` 直接操作 `.51` 上的文件 | SMB 挂载后能直读文件，但不能像 SSH 那样在 `.51` 上执行任意命令 |

### 通路 A · SSH（key-based，免密）

- **怎么建立**：`~/.ssh/config`（笔记本本地）已有别名，直接 `ssh supplychain-server` 即可，无需每次指定 IP/用户/密钥：
  ```
  Host supplychain-server
      HostName 192.168.100.51
      User Administrator
      IdentityFile ~/.ssh/supplychain_server
      StrictHostKeyChecking no
  ```
- **凭据放在哪**：私钥文件 `~/.ssh/supplychain_server`（笔记本本地，不入库）；`.51` 侧对应公钥已预置在目标账户的 `authorized_keys`。**本节只记位置，不记密钥/私钥内容本身。**
- **验证通路是否还通**：`ssh -o BatchMode=yes -o ConnectTimeout=8 supplychain-server "whoami && hostname"`——预期回显 `win-5cpujv008c7\administrator` / `WIN-5CPUJV008C7`。`-o BatchMode=yes` 使其在密钥失效/需要交互时**直接报错退出**而非挂起等密码，适合脚本化探测。
- **失效时怎么排查**：⑴ 先确认网络层——`ping 192.168.100.51`；⑵ 若网络通但 SSH 失败，检查 `.51` 侧 OpenSSH Server 服务（`sshd`）是否在跑、`authorized_keys` 是否被覆盖/机器重装过；⑶ **不要去改密码类配置排查这条**——这条走的是密钥认证，与账户密码是否过期/修改无关（除非账户本身被禁用）。
- **执行远程 PowerShell 时的编码坑**：`ssh ... "powershell -Command \"...\""` 这种多层引号嵌套（bash → ssh → 远端 cmd → PowerShell）极易在含中文/特殊字符时错位或产生乱码（GBK/UTF-8 控制台编码不一致）。**可靠做法**：把要跑的逻辑写成本地 `.ps1` 脚本文件，`scp` 推到 `.51`（如 `C:\Windows\Temp\`），再 `ssh ... "powershell -NoProfile -File C:\Windows\Temp\xxx.ps1"` 执行，跑完随手删掉临时文件；比在命令行里塞多层转义可靠得多。

### 通路 B · Windows 凭据管理器 + SMB 管理共享

- **怎么建立**：**不要用 `cmdkey` 命令行**——实测在这台笔记本上 `cmdkey /add:... /pass:...` 反复因终端/PowerShell 多层转义失败（占位符尖括号残留、`@` 符号疑似被 PowerShell 7 原生命令参数拆分、多行粘贴换行错位，三种失败形态在 2026-08-31 一次尝试中先后全撞上）。**改走图形界面**：`Win+R` → `control /name Microsoft.CredentialManager` →「Windows 凭据」→「添加 Windows 凭据」→ 网络地址填 `192.168.100.51`、用户名 `Administrator`、密码填入表单框——**密码在表单里输入，不经过任何命令行/脚本解析，没有转义问题**。
- **凭据放在哪**：Windows 凭据管理器（笔记本本地，按当前登录用户存储，DPAPI 加密）。**本节只记这条路径，不记密码本身。**
- **通路是什么**：走既有管理共享 `\\192.168.100.51\C$`（未新开专用共享，未改 `.51` 任何配置）。验证：`Get-Content '\\192.168.100.51\C$\<路径>' -TotalCount 1`（**用 `Get-Content`/交互式读文件本身做验证，不要只信 `Test-Path`/`Get-ChildItem`**——见下一条）。
- **🔴 判断这条通路通不通，只能看 `net view`/`net use` 的真实错误码，不能看 `Test-Path`/`Get-ChildItem`**：后两者对 `\\192.168.100.51\C$\...` 这类路径，认证失败时返回的是 `ItemNotFoundException`／"路径不存在"，把"无凭据"显示成了"东西不在那儿"，照它排查会一路查错方向（与队列 `#394` 2026-08-27 记的判据同族）。判无凭据要看 `net view \\192.168.100.51`／`net use \\192.168.100.51\C$` 的真实报错（`System error 5`＝拒绝访问，`System error 1326`＝用户名或密码错误）。
- **🔴 已知的会话隔离陷阱**：Windows 凭据管理器存的凭据是**按登录会话**生效的——在人工交互终端（用户实际登录的桌面会话）里 `cmdkey`/凭据管理器 GUI 建的凭据，**CC/自动化工具跑的进程若不在同一个登录会话里，读不到它**（本次实测：人工终端里显式 `net use ... 'password'` 能连通并读到文件，CC 侧同名命令仍报错）。**因此本通路目前只对"人在场、由人在自己终端里验证"的场景可靠**；若要给一个跑在**服务/计划任务**上下文里的常驻进程用，前提是该计划任务的 `LogonType` 是 `Interactive`（即绑定到人的实际登录会话，如 `ZhuopinAibotDevListener` 的既有配置）——`LogonType S4U`/`ServiceAccount` 类任务大概率读不到这份凭据，需另评估（如改走通路 A 的 `.ps1` 脚本模式，或专用共享+独立账户）。
- **失效时怎么排查**：⑴ 先用人工终端里的 `net use \\192.168.100.51\C$ /user:192.168.100.51\Administrator '密码'` 显式指定凭据测试，排除"密码本身错了"；⑵ 若显式指定能通、隐式（不带 `/user`/`/pass`）不通，大概率是会话隔离问题（见上条），换终端/会话重试；⑶ `net use \\192.168.100.51\IPC$ /delete` 可清一次可能存在的失效缓存会话后重试。

---

*状态：**`生效`**（2026-08-01 Shao Peishen 拍板）。**⚠️ 生效范围＝本件 §〇～§七、§九 的清单与判据即刻对所有 `.51` 部署与笔记本侧计划任务注册生效**；**§八「现状差距」三项仍是 CC 待办**（`deploy-server.ps1` 改薄封装、机器人脚本补 BOM＝队列 #167、存量端口收编）——**生效不等于差距已补，只等于"此后新部署必须按本清单走"**。*
