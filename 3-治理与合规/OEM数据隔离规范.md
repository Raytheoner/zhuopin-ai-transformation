---
status: 生效
title: "OEM 数据隔离规范"
---

# OEM 数据隔离规范

> 版本：V1.0（2026-06-13）｜ 状态：待 Paul 签发
> 性质：IATF 16949 审核可出示的正式规范。整合此前散落于 CLAUDE.md §4 与《全景规划》§4.3 的隔离要求，并纳入 2026-06-11 质量域边界扩展决策。
> 维护：边界变更须经 Paul 批准并在本文修订记录登记；与平台代码（`zhuopin_platform/data_isolation_layer/`）的映射关系见 §5。

---

## 1. 目的与依据

卓品智能作为 Tier 1 直供比亚迪、上汽、理想等多家 OEM，各客户的技术数据存在严格的保密与隔离义务（客户保密协议 + IATF 16949 §7.5.3 成文信息控制 + 8.3 设计开发的客户特殊要求）。本规范定义：哪些数据必须按客户隔离、隔离的技术实现要求、违规的处置与留痕要求。

## 2. 隔离边界（三分法）

### 2.1 硬隔离（必须按客户分库，禁止跨库访问）

| 数据类别 | 示例 | 涉及场景 |
|----------|------|----------|
| OEM 需求与设计技术数据 | OEM 需求文档、ECU 规格、设计变更记录 | R1-R5（研发知识库） |
| OEM 专属质量技术数据 | PPAP 文件包、DFMEA/PFMEA（含客户设计信息）、控制计划 | **Q4 PPAP**〔🔴 **2026-08-21 v7：Q3 FMEA 已下架**，本类现只剩 Q4 一个消费方〕 |
| 含特定 OEM 信息的客诉/8D | 含客户名称、车型、失效场景、客户返回件信息的 8D 报告与客诉记录 | **Q2 8D**（2026-06-11 扩展，Paul 批准）〔🔴 **v7：Q1 客诉分流已下架**；⚠️ **但本类的隔离要求一点没松** —— 8D 报告本身就含 OEM 信息，且陈忱重写后的新 Q2 是**批量评审已有 8D**，处理量只增不减〕 |

**硬隔离要求**：按客户独立 collection/库实例存储；RAG 检索结果不得混入其他客户数据（比亚迪历史 8D 不得出现在上汽相关查询的检索结果中）；查询必须携带客户上下文，由平台路由，不允许场景代码自行选库。

### 2.2 不隔离（公司自有数据）

| 数据类别 | 示例 | 涉及场景 |
|----------|------|----------|
| 公司自有制造数据 | IQC 检验记录、SPC 制程数据、MES 生产数据 | **O1/O2**〔🔴 **v7：Q7 IQC／Q8 SPC 已下架**，本类在质量域已无消费方〕 |
| 供应商与采购数据 | SRM/ERP 供应商主数据、PO、承诺交期、价格 | SC1-SC9 全部采购场景 |
| 公司体系文件 | IATF 体系文件、内部 SOP、立项模板 | **Q6**〔🔴 **v7：Q5 已下架**〕 |

**明确约定**：采购连接器（erp_connector/srm_connector）**不强加 OEM 路由**——供应商数据不是 OEM 技术数据。

### 2.3 通用知识库（GENERAL_COLLECTIONS）— 受控开放

平台允许设立跨客户可读的通用 collection（如通用工艺知识、脱敏后的方法论案例）。**写入侧控制是这一层的全部安全性所在**：

1. 任何内容进入通用库前，须经"无 OEM 信息"校验：不含客户名称/车型/项目代号/客户零件号/可反推客户身份的组合信息。
2. 含 OEM 信息的 8D/客诉案例若需进通用方法论库，必须完成实体脱敏（技术实体屏蔽，按质量旗舰开工就绪包脱敏 SOP）并由质量 Champion 签字确认。
3. 通用库写入操作必须写平台 audit（操作人、内容摘要、脱敏确认人）。

## 3. 行为规则

1. **fail-closed**：未注册客户、空客户上下文、客户别名/变体（如 "BYD" vs "比亚迪"）一律**拒绝访问**并抛 `CrossOEMAccessError`，不做模糊匹配放行。
2. **违规留痕**：每次 `CrossOEMAccessError` 触发必须写平台 audit（含请求方场景、请求客户上下文、目标 collection、时间）——违规**企图**本身就是审计事件。（对应审计报告 #11，代码落实由 CC 变更包 B5 执行。）
   🔴 **2026-09-02 补明「未注入 audit 时怎么办」（Shao Peishen 本人裁决 D2=(a) 收紧，变更包 `oem-chroma-ownership-rejudge`）**：本条原文只写「每次必须写」、**未写未注入 audit 时的行为**，正是这个空档让 `openspec/specs/platform-oem-isolation/spec.md` 与 `router.py` 得以用「向后兼容／仅抛错」把它读成**可选**——两份权威件由此直接冲突。现补明：**审计不是可选项。** 构造 `OEMRouter` 时未显式注入 `audit` 的，**必须内建平台默认 `AuditLogger`**；连默认 logger 都不可用时，**`resolve()`／`guard()` 必须直接拒绝访问（fail-closed，与 §3.1 同向）**，不得在无留痕的情况下放行，也不得无留痕地静默抛错。
   🔑 **理由（一句）**：默认构造 `OEMRouter()` 静默不写审计，意味着任何一处忘记注入 audit 的调用点，其违规拦截都不产生证据；而「拦截到了却没有证据」，在 IATF 16949 可追溯性审核面前与「没拦截」是同一个结论。
   ⚠️ **代码尚未落实本条**：本次仅收口规范与 spec 文本；`router.py` 的构造签名与 `_record_denied` 行为变更命中 openspec 门槛③（改既有模块对外语义），**已另立独立队列行承接 ＝ §一 `#466`**（含单测回归与全部调用点核查）。在该行闭环前，**新增调用点必须显式注入 audit**。
3. **唯一入口**：场景代码不得直接持有向量库（Chroma）client；所有检索必须经平台 `data_isolation_layer` 提供的统一入口（未来 `rag.retrieve()`），guard 内嵌于入口内部、不可绕过。该入口未就绪前，任何场景不得自行接 Chroma。
4. **人员侧同步**：知识库录入人员按客户项目授权；跨客户岗位（如质量 Champion）的录入操作须双人复核含 OEM 信息的条目归属。

## 4. 场景准入门禁

| 场景 | 隔离前提 | 准入判定 |
|------|----------|----------|
| R1 软件需求分析（🔴 **2027-07**） | 硬隔离执行层（含 rag.retrieve 唯一入口）部署完成 | 未完成 → R1 顺延（全景规划已列为 R1 前置）。 🔴 **2026-09-02 更正本行日期**：原写「2026-10」，系 **v6（2026-08-18）R 系列整域顺延入 S3 之前的旧值**，顺延后 R1＝**2027-07**、前置开工约 **2027-05**（依据：《跨场景前置数据与知识库任务总表》R1 行）。⚠️ **顺延成因是公司级优先级调整，不是 9/30 条件未达**，两个成因不可混淆。 ✅ **重判后本行是 L2/L3 唯一有日期的消费方**（因 Q2 已按 D1=(b) 退出推算集合）⇒ **L2/L3 到期日 ＝ 2027-05**（#374 design D3 规则代入值）。 |
| Q2 8D（2026-10） | 历史 8D 库分客户分库 + 脱敏 SOP 执行 | 归集时即分库，不允许"先混库后拆"。 🔴 **2026-09-02 追注（#374 design 审 D1=(b)，Shao Peishen 裁决）**：新 Q2 采**纯规则库逐条判定**，历史 8D **仅作离线校准语料、不进生产检索路径** ⇒ **本行不是 L2/L3 的消费方，不计入其到期日推算**。 ⚠️ **但本行的分库门禁照常适用、不因 D1=(b) 豁免** —— 那是**数据落盘纪律**，与 Chroma 建不建、与是否进检索路径无关；**「不进检索路径」不得被读作「可以混库存放」**。**约束点落在归集期内（2026-09），早于场景上线日（2026-10）**，也早于 L2/L3 的 2027-05。 ⚠️ 离线校准语料同样**不得**以「不进生产检索路径」为由写入通用库绕过写入闸（见 §2.3 与 #374 design D5）。 |
| Q4 PPAP（2027-04） | 硬隔离 + 各 OEM 要求清单分库 | 隔离层就绪是排期前提（已在排期表标注）。🔴 **2026-08-21 v7 须一并知悉**：陈忱已把 Q4 收敛为「三文件跨文档一致性校验」，**不再需要按 OEM 分库检索要求手册** ⇒ v6 定的「Chroma 挂靠 Q4 PPAP 前置」这个理由被业务方拆掉了一半。**但本行的硬隔离要求不变**——PPAP 文件包本身仍是 OEM 专属技术数据。**Chroma 归属须重判一次，重判出结论前 2027-01 硬截止不得放宽。** ━━━ ✅ **2026-09-02 重判已出结论（队列 §一 #374，变更包 `oem-chroma-ownership-rejudge`，design 审裁决人 Shao Peishen）**：**Q4 保留存储侧硬隔离身份，不再是 L2/L3（`rag.retrieve()` 与 Chroma 本体）到期日的来源** —— 收敛后三项校验为公司内部文件比对，不需跨客户检索；PPAP 文件包本身仍属 OEM 专属技术数据，硬隔离要求不变。 🔴 **`2027-01` 硬截止已作废**：其唯一算法是 v6 写死的「留出 Q4 前置 2027-02 的余量」，v8 删掉 Q4 跨客户检索需求后该输入已失效。按 D3=(a) 新规则（到期日 ＝ min(所有**需跨客户检索 OEM 数据**的已排期场景的前置开工日)，另有更早数据落盘门禁时取更早者）重算 ⇒ **L2/L3 到期日 ＝ 2027-05**（消费方＝R1）。**本次是「按已成文规则重算」而非「放宽」**——上句「重判出结论前不得放宽」的解除条件正是「重判出结论」，现已满足。 ⚠️ **L1（`OEMRouter`）无到期日、不得下线**，其理由＝QD-B 现行红线 ＋ 根 `CLAUDE.md` §4 质量域隔离边界，两者皆常设。 |
| ~~Q3 FMEA（2027-05）~~ | — | 🔴 **2026-08-21 v7 已下架，本行撤销** |

## 5. 与平台代码的映射

🔴 **现状列已于 2026-09-02 按 `#374` 重判取证逐行重核（原列头日期 2026-06-13 已过时近三个月）。层次记法**：L1 ＝ 路由 guard（`OEMRouter`）／L2 ＝ 唯一入口（`rag.retrieve()`）／L3 ＝ 向量库本体（Chroma）——三层的就绪度与到期日各自独立，**不得再用「隔离层就绪」这类不指明层次的表述**。

| 规范条款 | 层 | 代码落点 | 现状（2026-09-02 重核） | 承接载体 |
|----------|:--:|----------|---------------------|----------|
| §3.1 fail-closed | L1 | `data_isolation_layer/router.py` OEMRouter.resolve/guard | ✅ 已实现并有测试 | — |
| §3.2 违规留痕 | L1 | CrossOEMAccessError 抛出前写 AuditEvent | ⚠️ **部分实现** —— **注入 audit 时**会写；**默认构造 `OEMRouter()` 静默不写**（`router.py` L51-53）。🔴 该缺口即 §3.2 正文 2026-09-02 补明的那一条（默认 `AuditLogger` ＋ fail-closed 兜底），**文本已收紧、代码未跟上** | 队列 §一 **#466**（原记「CC 变更包 B5」已过时，改指本行） |
| §3.3 唯一入口 | L2 | `rag.retrieve()`（待建），内嵌 guard | ❌ **未建** ⇒ 按 §3.3「入口未就绪前任何场景不得自行接 Chroma」，**现状下 Chroma 即使部署好也没有任何场景被允许使用它**。到期日 ＝ **2027-05**（消费方＝R1 2027-07，前置约 2027-05；审计报告 #19） | 变更包 `oem-chroma-ownership-rejudge` tasks 4.3 |
| §2.3 通用库写入校验 | L1 | 通用库写入侧"无 OEM 信息"校验 | ❌ **待设计**；且读取侧对 `GENERAL_COLLECTIONS` **无条件放行、不校验 OEM 上下文**（`guard()` L78-80）⇒ 含 OEM 信息的内容一旦写进通用库，任何客户上下文都能检索到，全程不报错不留痕。🔴 **D5=(a)（Shao Peishen 2026-09-02）：校验入口建成前通用库置为只读**，已有内容不受影响、禁止新写入 | 变更包 tasks 4.2（可由 §一 **#466** 同包承接） |
| — | L3 | Chroma 本体实例与部署 | ❌ **未接**；🔴 **「这一轮还建不建」本身已成待判项**（D1=(b) 后 2027-05 前无有日期的消费方） | 队列 §一 **#465** |

⚠️ **本表未覆盖运维面**（备份／快照／导出／日志与监控采样）——该要求已写入 `openspec/specs/platform-oem-isolation` 的「运维面 SHALL 同受隔离约束」Requirement，**本规范尚缺对应章节**，挂账于变更包 tasks 3.6。

## 6. 修订记录

| 日期 | 版本 | 变更 | 批准 |
|------|------|------|------|
| 2026-06-13 | V1.0 | 初版：整合 CLAUDE.md §4 + 全景规划 §4.3 + 2026-06-11 质量域边界扩展，新增通用库写入侧控制与场景准入门禁 | 待 Paul 签发 |
| 2026-09-02 | V1.1 | **四处，均为 `#374` 重判（变更包 `oem-chroma-ownership-rejudge`）design 审五点裁决的落字**：⑴ **§3.2** 补明「未注入 audit 时怎么办」＝默认 `AuditLogger` ＋ fail-closed 兜底，消解本规范与 `platform-oem-isolation` spec／`router.py`「向后兼容仅抛错」的直接冲突（D2=(a)）；⑵ **§4 Q4 行** 记入重判结论——Q4 降为存储侧身份，🔴 **`2027-01` 硬截止作废、按 D3=(a) 规则重算为 2027-05**；⑶ **§4 Q2 行** 追注 D1=(b)（纯规则库判定、不进生产检索路径 ⇒ 不计入到期日推算），并申明**分库门禁不因此豁免**；**§4 R1 行日期由旧值 2026-10 更正为 2027-07**（v6 整域顺延，前置约 2027-05）；⑷ **§5 映射表** 按三层（L1/L2/L3）重核现状并逐行补承接载体。 🛑 **未动**：§7 运维面隔离要求（本规范仍缺该节，spec 侧已有对应 Requirement）——挂账于该变更包 tasks 3.6 | **Shao Peishen 本人裁决 D2=(a)（`compliance_redline_change`，孙涛不可代）＋ 同日 D0/D1/D3/D5 裁决，2026-09-02**；落字泳道 `oem-chroma-2`（批次 `B-0902_60`，OP-0902-B1） |
