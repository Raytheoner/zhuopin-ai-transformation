---
status: 待发
owner: Cowork 环境总线
created: 2026-08-08
decided_by: Shao Peishen（2026-08-08 三项默认全部批准）
scope: 构建环境基建（CI 基线 / 业务·环境双队列 / GitHub 原生能力对照）
---

# 构建环境基建方案 —— CI 基线与业务·环境双队列

> 本件是**方案定稿**，非执行记录。执行由队列行驱动，见 §5 次序与并行矩阵。
> 立项依据：Shao Peishen 2026-08-08「我们在环境总线进一步优化我们的构建环境」，三项决策默认全批。

---

## §0 结论摘要（先看这段）

1. **最大缺口不在队列，在仓库根目录**——实测 `.github` 目录**根本不存在**：无 CI、无 issue 模板、无 CODEOWNERS、无 PR 模板。本项目在一个 GitHub 仓库里，用 **4822 行自研工具**重新实现了 GitHub 原生就有的东西。
2. **但不建议整体迁移到 GitHub Issues**——离线可用是硬需求，企微机器人走文件追加，IATF 要求单一可信源在库内。迁移风险高于收益。
3. **只取一件：CI（GitHub Actions）**。它是唯一能一次性消灭多个「靠人记得」的东西，且**可作为退休若干现有守卫的容器**（满足协议〇.9 one-in-one-out，净减代码）。
4. **双队列拆分值得做，但它是「治理与注意力的解耦」，不是「技术解耦」**；且**硬阻塞于队列 #308**，提前做会把改造成本乘以 2。

---

## §1 现状取证（2026-08-08 实测，非估计）

### 1.1 机制层已经变成产品本身

| 指标 | 实测值 |
|---|---|
| §一 现存行 机制/环境类 : 业务场景类 | **86 : 20（81%）** |
| 未收口 机制类 : 业务类 | **约 15 : 11**（随当日推进浮动） |
| 08-07／08-08 两天新建行 | **机制 12 ／ 业务 0** |
| 守护队列文件的工具代码合计 | **4822 行** |

守卫代码明细：`工具-落库sweep.py` 2292 ＋ `工具-共享文档编辑锁.py` 1479 ＋ `工具-队列查询.py` 203 ＋ `工具-文档台账生成.py` 242 ＋ `工具-仓库外载体扫描.py` 169 ＋ `工具-孤儿worktree扫描.py` 169 ＋ `工具-主工作区安全同步.ps1` 130 ＋ `工具-定时任务源码备份.py` 138。

### 1.2 工程基建现状

- `.github/` **不存在**（无 workflows／ISSUE_TEMPLATE／CODEOWNERS／PULL_REQUEST_TEMPLATE）
- remote：`https://github.com/Raytheoner/zhuopin-ai-transformation.git`
- 远程分支：**20 个**
- 合并策略：ff-only 直推为主，PR 多因零 diff 被 CLOSE

### 1.3 测试可离线性分类（CI 方案的关键输入）

| 项 | 实测 |
|---|---|
| 测试文件总数 | **156** |
| 初筛「纯离线」 | **137（88%）** |
| 初筛「疑似需凭据/网络/真实数据」 | 19 |

🔑 **抽验推翻了初筛**——19 个里的代表性样本实测：

- `test_srm_connector.py`：**用 mock/patch，不发真实网络** → CI 可跑
- `test_notifications.py`：同上 → CI 可跑
- `test_golden_real.py`：无 mock，但**带 `pytest.skip` 标记，数据缺失时自跳过** → CI 安全（会 skip，不会失败）
- `test_工具-落库sweep.py`／`test_工具-共享文档编辑锁.py`／`test_工具-孤儿worktree扫描.py`：命中「git 子进程」，但 **GitHub Actions 自带 git**，属误报 → CI 可跑

**⇒ 实际 CI 可跑比例远高于 88%，逼近全量；真正跑不了的只有依赖 `.51` 真实服务与真实凭据的少数集成测试，且它们本就带 skip。**

> ⚠️ **如实登记**：以上是**静态特征扫描 + 3 个样本抽验**，**未真实在无凭据环境跑过一次**。CI 落地第一步必须是「在干净环境实跑一次全量、按真实失败清单定 CI 范围」，不得按本节估计直接配置。

---

## §2 GitHub 原生能力对照表

| 本项目自研 | 代价 | GitHub 原生等价物 | 是否建议迁移 |
|---|---|---|---|
| 编号高水位线 | #146／#163／#185 三次修，仍有竞态 | Issue 自动编号 | ❌ 不迁（离线不可用） |
| 编辑锁（1479 行） | #197／#200／#225／#248 反复修 | Issue 并发写，无需锁 | ❌ 不迁（同上） |
| 状态列自然语言 ＋ #308 机器字段 | #248／#302／#304 全族衍生物 | Issue state ＋ labels，天生机器可读 | ❌ 不迁，但 **#308 是它的本地等价物** |
| 机制/业务分类靠关键词猜 | 措施 C 判据模糊 | labels ／ 两个 Project | ❌ 不迁，**双队列是本地等价物** |
| 协议〇.10 并入审核 | 纯人守＋取号咽喉 | 建 issue 时原生搜索去重 | ❌ 不迁 |
| sweep 内各类检查 | 2292 行，且只在 sweep 跑时生效 | **CI** | ✅ **建议采纳** |
| 无回归保护 | 靠 session 记得跑测试 | **CI** | ✅ **建议采纳** |
| 无密钥扫描 | 靠 `.gitignore` ＋ 人眼 | **CI secret scan** | ✅ **建议采纳** |

**判断依据**：凡「需要离线可用 / 需要机器人文件追加 / 需要库内单一可信源」的能力，一律不迁；凡「只需在 push 后跑一遍、结果回报即可」的能力，迁 CI 收益最大、风险最低。

---

## §3 CI 基线方案（决策 1，已批准 (a)）

### 3.1 交付条件（这是批准时的附加条件，不是可选项）

> **必须以「退休等量 sweep 检查」为交付条件**——CI 不是第 9 个守卫，是用来退休若干现有守卫的容器。满足协议〇.9 one-in-one-out，**净减代码**。

### 3.2 分三步，不得一次性全上

**步骤 1｜真实分类（不可跳过）**
在干净环境（无 `.env`、无 LAN）实跑一次全量测试，产出真实的「通过／失败／skip」三分清单。**CI 范围按真实清单定，不按 §1.3 的静态估计。**

**步骤 2｜CI 基线上线**
`.github/workflows/ci.yml`，在 push 与 PR 触发，跑四件：

1. 可离线测试全量（按步骤 1 清单）
2. `openspec validate --all --strict`
3. **队列结构 lint**（列数／裸竖线／§二 状态列格式）
4. secret scan（`.env` 类文件与疑似凭据字符串）

🔑 **第 3 项是本方案对既有机制的实质补强**：编辑锁的 release 校验**只在有人走锁时才生效**，而 `queue_appender.py` 那条机器人路径**完全绕过**（见队列 #305）。**CI 不论谁写、走哪条路都拦**——它比 #305 的写侧修复覆盖面更大，两者互补而非重复。

**步骤 3｜退休等量守卫**
CI 稳定运行一段时间后，把 sweep 内已被 CI 覆盖的检查删除，并在队列行回写「本次退休了哪几项、代码净减多少行」。

### 3.3 明确不做

- ❌ 不引入 PR 强制评审／branch protection（单操作者场景，收益低于摩擦）
- ❌ 不迁 GitHub Issues
- ❌ 不在 CI 里跑需要真实凭据或 `.51` 的集成测试（那些留在本机建造流程）

---

## §4 双队列方案定稿（决策 2，已批准 (a)：只定稿、不立行）

### 4.1 能真解耦的（4 项，实的）

| 项 | 现状 | 分开后 |
|---|---|---|
| 文件锁 | 一文件一锁，2 小时内 8 次 acquire 全串行 | 两把独立锁 |
| 取号空间 | 单一高水位线，取号必串行 | 两套编号独立推进 |
| 批次流 | §二 混装两类 | 两条独立落库流水 |
| WIP 与注意力 | 措施 C 靠关键词猜分类 | 结构上分开，81:20 变成可独立治理的两条线 |

### 4.2 不能解耦的（3 项，必须写明，否则分完会失望）

🔴 **触碰区照样重叠**——实测机制类未收口 15 行里 **5 行触碰 `4-数字员工/5-平台底座`**（业务代码），业务类 11 行里 3 行触碰同一处。#305 改机器人服务、#308 改 7 个消费者含平台底座——**分队列不改变代码层争用**，worktree 冲突照旧。

🔴 **跨队列引用天然存在**——#22 源于业务可见性需求、#129 源于姚祖怡的业务反馈、#282 里挂着业务专员通道。**机制任务大多是业务问题的残渣**，两边会互相引用编号。

🔴 **读取侧成本翻倍**——值周巡检／看板／#302 判据都得读两边。

### 4.3 结论与硬前置

- **值得做，但卖点不是「文件锁」**（今日实测锁争用仍是烦扰级、未到阻塞级），而是**让 81:20 的失衡结构化、可独立治理**。
- 🔴 **硬阻塞于队列 #308**：现有 **7 份独立解析实现**，先分文件＝**7 × 2 = 14 处**要改；#308 的权威解析器与机器状态字段落地后再分，成本降一个量级。
- **本件定稿后不立行**，待 #308 落地并满足协议〇.9 措施 C（机制类未收口 ≤ 8）后再启动。

---

## §5 次序与并行矩阵（按根 CLAUDE.md §5 要求）

| 编号 | 内容 | 能否立即开工 | 可与谁并行 | 并行判据 |
|---|---|---|---|---|
| **P1** | CI 基线（三步） | ✅ **零依赖，可立即开工** | 与 P2／P3 全并行 | 只新增 `.github/`，**触碰区与两者零重叠** |
| **P2** | 队列 #308（机器字段＋权威解析器） | 待 propose＋design 审 | 与 P1／P3 并行 | 触碰区不含 `.github/`、不含 `queue_appender.py` |
| **P3** | 队列 #305（写侧注入口止血） | ✅ 可立即开工 | 与 P1／P2 并行 | 只改 `queue_appender.py` |
| **P4** | 双队列拆分 | ❌ **硬阻塞于 P2** | 不可与 P2 并行 | 同改解析器，必然冲突 |

**⇒ 现在能立刻开的是 P1 与 P3；P2 待 design 审；P4 等 P2。**

---

## §6 如实登记的未验证项（不假装闭环）

1. **测试可离线性未真实验证**——§1.3 是静态扫描＋3 个样本抽验，**未在干净环境实跑**。这是 P1 步骤 1 的全部内容，不得跳过。
2. **CI 能退休哪几项 sweep 检查，尚未逐项对照**——步骤 3 才能确定，本件不预先承诺数量。
3. **GitHub Actions 在本项目网络环境下的可用性未验证**（企业网络对 Actions runner 的访问、私有仓额度）。P1 步骤 2 前须确认。
4. **双队列的跨队列引用如何表达**（`环境#12` vs `业务#7` 的命名与互引语法）**本件未定**，留 P4 启动时随方案细化。

---

## §7 变更记录

| 日期 | 变更 | 决策人 |
|---|---|---|
| 2026-08-08 | 立件。三项决策全批默认：CI 基线以退休等量守卫为交付条件／双队列只定稿不立行、等 #308／本对照件落 `3-治理与合规/` | Shao Peishen |
