# CLAUDE.md — 卓品智能 AI 转型（项目级记忆）

> 项目级上下文（Hermes L1）。Claude Code / Cowork 进入本仓库先读它。全局身份见 `~/.claude/CLAUDE.md`；本文件只写**本项目**的背景、架构、工作流与红线。代码与注释用中文，术语保留英文，功能用供应链业务语言描述（齐套分析／承诺交期／在途跟踪）。
> **每条规则只写「触发 → 动作 → 判据／指针」；谁定、何时定、成因、实证一律在 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md`（附录 A–H），本文件不复述。** 瘦身前全文存档＝该文件附录 H。

> **当前进度**：本段只留最近一批，当前＝空。🔴 迁出前置判据 J1：**迁出前须确认该条已有承接载体**（队列行号，或具名文件＋章节）。本段的条目数／长度／J1／字节／批次跨度已机器守（`工具-CLAUDE进度段lint.py` ＋ `工具-落库sweep.py` 第 4 类告警），阈值以代码为准。
> **📦 历史批次指针**：全部进度原文在 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md` 各批次节（可 grep、原文未改写）；瘦身前根文件全文＝附录 H。
> **memory 层**：Desktop/Cowork 侧**完全忽略**（不读、不写、不引用，容器内文件不删不并）。CC 侧 auto-memory ＝「技巧笔记层」：**只记**工具技巧／环境坑／Shao Peishen 偏好表述；**纪律口径判据一律不入**——跨会话纪律的权威载体只有本文件与两份队列，memory 不是第四份，chat 个人层 `/areas/zhuopin-*` 同禁；`MEMORY.md` 索引 ≤200 行 / 25 KB。🔑 **判别只问一句「违反了会怎样」**：违反会做错事 ⇒ 纪律，不入；只是多走弯路 ⇒ 技巧，可入。 ⇒ 判死实测见 `0-全景路线图/构建环境自动纠错与上下文治理-方案-2026-08-28.md` §一 ＋ 同目录 `memory索引收割对账-2026-08-21.md`、`auto-memory旧桶收割对账-2026-08-29.md`。
> 🔑 **凡「已复核／已回读／已比对」类自陈，必须同时写出用什么核的（工具名／命令／哈希）；只有动作没有手段的验证声明 ＝ 没有验证。**

---

## 1. 公司与项目背景

- **公司**：卓品智能科技股份 — 汽车 ECU 设计研发制造 Tier 1，直供比亚迪／上汽／理想等 OEM。
- **项目**：18 个月企业 AI 转型（2026-07 启动），六部门并行（采购／财务／质量／销售／运营／工程研发），共 **35 个数字员工场景**（质量域在建只剩 Q2／Q4，Q6 已交付＝QD-B）。🔴 **编号 Q1／Q3／Q5／Q7／Q8 已空置、不得重新分配**；下架场景历史只靠 git 保留，复苏由 Shao Peishen 手动触发。 ⇒ 场景数沿革见全景规划 §0.2。
- **决策人**：**Shao Peishen**（分管供应链与质量的 OPVP）。**称呼一律 `Shao Peishen`，不用 `Paul`**；🔴 **绝不替换路径里的 `Paul Shao`**；历史记录不追改（旧件里的 `Paul` 指同一人）。
- 🔴 **人的属性（姓名／性别／职务／部门）＝硬事实，正本 ＝ `6-人才与组织/人员名录-称谓与性别-正本.md`（21 人）**：提到任何具名人物前先读正本；本文件不留第二份名录。企微 chatid／部门→群映射见 `6-人才与组织/部门AI专员跟进/README-跟进机制与命名约定.md`。
  🔴 **禁从名字推断性别**：正本之外的人物一律用「该专员／其／对方」并当场问一次，不得二选一猜。🔑 **举一反三**：性别、职务、称谓、汇报关系、审批权限——一律以权威载体为准，载体里没有就问一次并当场落档。**判别特征＝「能从字面猜出来、且猜错了不会当场报错」**——错误不产生信号。
  🔴 **读到规则 ≠ 执行规则**：起草完成后扫全文第三人称代词、逐个回正本核对（判据见跟进信 README《起草期自检》）。
- **节奏**：先跑通最小验证，再规模化；先 mock／脱敏，再切真实库。

## 2. 全景目标与时间线（指针）

- 权威总纲：`1-转型规划/0-全景路线图/卓品智能AI转型全景规划.md`——正文可修正，**必须在 §0.2 修订记录登记，否则视为无效修改**。
- 最新时间线＋Phase 1 修正：`1-转型规划/0-全景路线图/卓品智能AI转型实施计划（最新版）.md` 第七节。
- **U9C 已覆盖的标准功能直接用，不建 AI**；AI 只做 U9C 不覆盖或需智能增强的场景。
- Phase 1 能上线的只有 **SC1**（供应商风险初筛）与 **SC8**（客户订单交期承诺，收割式 MVP）；其余被 U9C MCP／外部 API／知识库三类依赖阻塞，先在底座上做 mock 原型。
- **阶段框架（正本＝全景规划 §0.1）**：18 个月（2026-07 → 2027-12）＝ **Phase 1**，内分 S1 筑基（2026-07~12）／S2 扩面（2027-01~06）／S3 深化（2027-07~12），月度 M1/M2/M3；**Phase 2 ＝ 产品工程跃升期（2028 H1）**，候选 Phase-1 末评估立项、不并入 Phase 1，清单见全景规划 "Phase 2" 章 ＋ `AI候选场景增补-backlog（产品工程方向）.md`。**单一可信源 ＝ 全景规划 §加速启动总览排期表 ＋ §0.2。**

## 3. 仓库结构

```
企业AI转型/                    # GitHub: Raytheoner/zhuopin-ai-transformation
├── 0-学习与工具/              # 学习路径、U9C 申请、取证知识库、md 转 Word、hooks、skills 源码
├── 1-转型规划/                # 各域规划、口径、专线接力/prompt
│   └── 0-全景路线图/          # ★全景规划(权威)+实施计划+甘特+CHANGELOG+跨桌任务队列+opener 模板库
├── 2-试点项目/                # 采购部试点
├── 3-治理与合规/              # IATF/ISO26262/OEM 隔离、回滚 SOP、统一门户架构决策
├── 4-数字员工/部门/场景名/     # 各场景独立 Python 工程，import 平台底座
├── 5-平台底座/zhuopin_platform/  # 可安装 Python 包
└── 6-人才与组织/              # 人员名录正本、专员跟进、AIOps 岗位
```

## 4. 平台底座（指针）

🔴 **动 `5-平台底座/` 之前，先读 `5-平台底座/CLAUDE.md`**——子系统表／OEM 隔离边界／context7 文档查询均在那里。

## 5. 工作流（OpenSpec + SuperPowers + Hermes，不跳步）

### 5.1 会话接力与开场
- **接力方式＝读文件，不粘长 prompt**：开场读 ① 本文件 → ② `1-转型规划/0-全景路线图/session接力-Phase1收口.md`（【下一会话主攻】＋快照）→ ③ 全景规划／实施计划第七节；开干前问 2-3 个澄清。**收工**把进展＋下一步滚动覆盖进接力文件（标日期）。
- **CC 复命零粘贴**：他说一句「CC 跑完了」，Cowork 自行取件，**三源缺一不可**：① CC 会话原文 `C:\Users\Paul Shao\.claude\projects\<路径编码>\*.jsonl`（逐行 `ConvertFrom-Json` 取 `type=assistant` 的 `message.content[].text`）；② 队列行；③ `git log` ＋ `git rev-list --count origin/master..master`。**三者不一致以队列与 git 为准。** 🔴 边界：⑴ jsonl 是内部格式，解析不出即退回粘贴、不得猜；⑵ 只能读、不能反向发；⑶ 目录名编码会吃掉中文，**必须用 mtime＋内容二次确认**。 ⇒ 取件算法见 `专线opener模板库.md` §〇.15。
- **开场词与 prompt**：凡产出交接 prompt，必须内置「开场词（复制即用）」——单句、含完整路径；≤500 字直接给原文、>500 字落文件＋单句引用；一律放 fenced 代码块。**收工段必带 git 处置**。 ⇒ 十二条细则见 `专线opener模板库.md` §〇；生成器＝skill `zhuopin-kickoff-prompt`。
- **执行环境标注**：任务／opener 标题必标 `【Cowork】`／`【CC】`，`【设置】` 行同步写。**判别**：只产改 `.md`、不写生产码、不自行 commit ＝ Cowork；写跑代码／连真实库与 `.51`／部署／自行 commit+push／一任务一 worktree ＝ CC；只读取证属 Cowork，一旦改代码或触发服务动作即转 CC；**改本机工具链（npm/插件/CLI 升级、`openspec update` 类）＝ CC**，Cowork 破例须其明示授权＋固化升级前证据＋逐文件过目 diff＋事后登记。**「已获授权」≠「该照原方案执行」**——取证推翻方案即回报改判。

### 5.2 输出与报告规范（两桌全局）
- 🔴 **会话末「需你定夺」**：回复末尾固定小节＋编号清单；**每项必须是可直接作答的是非题或选择题**：① 带字母标签 `(a)/(b)/(c)` 或「是/否」；② 每个选项写清「选它会发生什么」与代价；③ 🔴 **两栏各归各位**——状态另起「**状态同步（无需你答）**」，任一方向串栏都算违规，判据：凡出现「等你定／请你选／待你拍板」即属「需你定夺」；④ 标「默认项」（若不答按 (x) 执行）。**临期项标日期置顶；此前未答复的悬置项以选择题形式重提**；无决策项时明写「本次无需你决策」。
- 🔴 **默认项只在两个前提下成立**：设前自问 ① 他不答，谁去执行？② 「不答」的代价是「停在原地」还是「错误继续发生」？答不出执行者、或是后者 ⇒ **不设默认项**，改标「本项无默认，须你明确答复」并登记 §四；并查有无下游依赖被冻住。 ⇒ 论证见 CHANGELOG 附录 D。
- 🔴 **决策路由（只问一句：答完之后由谁动手？）**：本 session 接着做 ⇒ **⟨就地答⟩**给选项＋默认项；要另起 session／归他线／对外发送／改他人触碰区 ⇒ **不给选项**，只写「已登记 §一 #N（或 §四 #N），待总线派发」＋一句结论。**报告顶部加一行**：`▶ 本报告 N 项定夺：X 项⟨就地答⟩，Y 项已登记队列待总线派发`。判定责任在写报告的一方。总线答复后须①结论写进队列行②附「回原 session 收口词」（含收工自检四项：产出已推送／队列行已回写且 ✅ 在状态列开头／§二批次已落库／行内遗留是否需升格）。 ⇒ 成因与旧判据见 CHANGELOG 附录 D。
- 🔴 **≥2 个 opener／派单件必附次序与并行矩阵**：逐项写 ① 能否立即开工（零依赖／软序／硬阻塞／定时触发型四选一，以队列行状态列开头括注为准）② 能与哪几项并行（判据＝触碰区是否重叠）③ 串行时谁先谁后及理由。自检：*发给没读过本会话的人，能一眼说出先开哪个吗？*
- 🔴 **输出三条**：① 可粘贴的 prompt 必标粘贴端 `▶ 粘贴端：Cowork／CC`，多条各自标；② 他亲自要做的动作 ≥2 个，按序编号并标「按序／可并行／可缓」；③ 路径写仓库根相对路径（唯一例外＝本机取证命令用绝对路径）；每个问题给 1 个最优建议——选择题也必须同时给推荐项。
- **建议未获答复须再次提醒**：建议牵动后续动作而未答 ⇒ 下次交互重提，不得当默认通过。**判据：建议 vs 状态，机制只认后者**——涉归属／优先级／范围／排期的建议，要么当场改状态字段，要么标「待他确认」，不留中间态。高危形态＝A 处写「建议并入 B」而 A 状态未改。收工自检：本次的「建议…」哪些未答却已影响他处？逐条重提。

### 5.3 队列与落库
- **跨桌任务队列纪律**：`1-转型规划/0-全景路线图/跨桌任务队列-机制环境.md` ＋ `…-业务场景.md`（两份真身、共用一把锁；旧 `跨桌任务队列.md` 为纯指针）是两桌任务流转的**唯一载体**——**开工必读**（认领＋登记触碰区）、**收工必写**（状态＋产出路径＋下游任务当场追行）；触碰区与他人在办重叠不得抢领，报总线裁决。**commit 由 `ZhuopinCommitSweep` 自动从 §二「待 commit 批次」取活销行（约 27 分钟一轮）——登记完 §二即完事，不需要「一句话交 CC」**；CC 仅在 sweep 不可用时兜底。§三口径冻结标挂标即停该场景建造。🔑 **编辑锁／每周清扫／高水位线／并入审核／可动 WIP 等细则以队列顶部「协议〇」为正本，此处不复述。** 值周巡检每周一 10:00 出《本周计划》，开跑前先跑 skill `zhuopin-queue-audit`。**gap 收口**：设计↔执行 gap 须驱动到有主·有截止·已派单后计划才往下走。
- **机制优先**：能等的交给机制（对账审计／sweep／值周巡检）；不能等的用机制的方式立即做（编辑锁、批次登记、留痕，不裸手改）。发现绕开机制的裸改（含自己）应拦下提醒。
- **环境保障线边界**：对象是构建环境与自动化机制本身（worktree／编辑锁／sweep／巡检／队列纪律／openspec／工具链／机器人）＝环境整治，本线可备派单件；对象是业务场景代码／底座功能／真实数据／`.51` 现网（即便入口是一个「测试失败」）＝项目构建，交业务总线。本线默认交付＝取证结论／方案定稿／队列行写入，分发由巡检与巡逻完成。 ⇒ 细则见 `开场prompt-【Cowork】环境总线-接力交接-2026-08-08.md` §六bis。
- **规则退休制**：纪律分「机制守」（有代码／门禁强制）与「人守」。**任一「人守」条目被违反 3 次即必须二选一**：① 机制化（挂到不可绕过的咽喉：队列写入＝编辑锁 `acquire/release`；提交＝sweep），正文降为一行指针；② 删除。违反计数落在队列对应行内。 ⇒ 根问题与首例见 CHANGELOG 附录 E-1。

### 5.4 两桌分工与同步
- **Cowork（规划治理桌）＝项目总架构师**：规划／路线图／治理合规／招聘／汇报；默认只改 `.md`，Word 仅在明确要求时用 md-to-word；不碰真实库、不写生产代码。**文件链接纪律**：为他定位或产出文件一律附可达文件卡片（present_files）。
- **Claude Code Desktop（建造车间）**：写并运行场景代码、连真实 SRM/ERP、跑真实数据；不改规划文档。
- **同步**：开工 `git fsck --connectivity-only` 再 `git pull`；收工 `git push`；同一文件别两边同时改。**fsck 报错即停**，报 Shao Peishen 走恢复流程（先把未 commit 改动备份到仓库外，再重 clone，再拷回 `.env`/reports 等 gitignore 件）。**OneDrive**：平时全关、每周手动开一次作离线备份。排查「文件像是变旧了」见 `0-学习与工具/取证方法知识库.md` §三.1。
- **合并策略 ff-only**：PR 一律 ff-only，由 `工具-落库sweep.py::_reconcile_with_origin_and_push` 强制对齐；零 diff 的 PR 验证 `git log master..branch` 为空后带说明 CLOSE，不强 push、不 rebase。
- 🔴 **乱码文件夹哨兵**：CC 开工与收工各查一次上级目录，`*AI转*` 目录应且仅应 1 个（`Get-ChildItem ..\ -Directory | ? Name -like '*AI转*'`）；出现含 U+FFFD 的兄弟文件夹即停手，整树移入 `_乱码重复文件夹隔离-日期\`、逐文件哈希比对、不直删。**根因指纹**：路径与内容同现 U+FFFD；CC 写含中文路径的关键文件后，读回抽验一处中文再继续。
- 🔴 **时间戳必判 UTC vs 本地**（本机＝UTC+8）：引用任何时刻前先答「这是 UTC 还是本地」并显式标基准（`09:04:31Z (17:04:31 本地)`）。`wecom_aibot_audit.jsonl` 与企微告警文案＝真 UTC；文件 mtime／计划任务 `LastRunTime`／事件日志／`LastBootUpTime`＝本地。🔴 **写侧**：向队列行／批次行／`Last Updated`／接力节标题写日期，一律用本机 `Get-Date -Format 'yyyy-MM-dd'` 当场重取——不估算、不用 UTC、不写未到日期、不引用本会话早先取值、禁用沙箱 `date`；文本里出现相对表述（窗口／天数差／到期）顺手换算校验。 ⇒ 陷阱见 `取证方法知识库.md` §一.2／§一.3。
- 🔴 **工具静默回退**：一类工具参数不符预期时不报错而回退到「看起来合理」的默认行为。**判据：只读命令返回「太干净／太正常」的结果，先问它是不是根本没读到我以为的那个对象。** 已知实例（`[System.IO.File]::*` 相对路径按宿主 CWD 解析／`git -C` 向上找到祖先仓库／PowerShell 变量名大小写不敏感／混合换行符）见 `取证方法知识库.md` §二；写后反查三件套见 `专线opener模板库.md` §〇.6。同族：判本机是否重启只认 `ID=27 Kernel-Boot 引导类型`（§一.1）；**判据本身也需被质疑一次**（§四.1）。
- **直读原则**：对新鲜度敏感的读取（状态页数据层／状态核验／完整性抽验）一律本机直读（PowerShell 只读 cmdlet 全名，严禁 `rd` 等可变更别名），不用沙箱挂载副本（有文件桥滞后与尾截断假象）；沙箱 bash 只做 git 取证、批量 grep。状态页 Artifact＝`zhuopin-project-status`。

### 5.5 文档与全景治理
- **排期同步**：任何场景实现时间变更，必须同步全部规划与路线图档（全景规划场景块＋排期表＋四阶段＋甘特指针、实施计划总清单＋时间线＋Phase 2、前置数据总表）并重生成 docx；零残差、grep 自检。**单一可信源＝全景规划 §加速启动总览排期表。** 🔴 凡触碰 `全景规划.md` 或 `实施计划（最新版）.md` 正文，收工前必用 `0-学习与工具/md转Word工具/md2word.py` 重转两份 docx 入同批 commit。 ⇒ 落点清单见 CHANGELOG 附录 G-2。
- **全景路线图重组循环**：全景相关档统一归集 `1-转型规划/0-全景路线图/`。**分工红线**：域专线出局部定稿＋移交单、不直接改全景；全景线执行重排、不重开域口径。闭环（冻结标→九文档重排→§0.2 登记→grep 零残差→回填销号）＝skill `zhuopin-rebaseline` ＋ `全景路线图重组机制与变更日志.md`。
- **文档治理六规则**（正本＝`文档治理规范与规整执行清单-2026-07-07.md`）：R1/R2 机制守——**找文档先查 `0-全景路线图/文档台账-自动生成.md`、不翻目录**（状态头六枚举由 `工具-文档台账生成.py::STATUS_ORDER` 强制；CC 不经 sweep 的直接改档须手动重跑台账）；R3 已执行即回填 status，每季度批量搬 `z-已执行归档/` 并清扫引用；R4 命名四律（主题-对象-日期-事项／不含会变数字／md·docx 同名／prompt 类 `开场prompt-` 前缀）；R5 接力＝定长交接卡（8 KB、六块、零日期节、收工覆盖；进度→CHANGELOG、方法→取证知识库、未闭合→队列行，迁出守 J1；机器守＝J6）；R6 专员正式材料落 `7-外部文档/<部门>/`，专线只认文件不认转述。本文件尺寸由 sweep 第 4 类告警守（`_check_claude_md_carrier_size`）。
- **企微推送**：Cowork 写 `.md` → 对 CC 说「把 X.md 发企微」（Cowork 沙箱出网 403）。工具 `0-学习与工具/发企微.py`（读 `.env` 的 `WECOM_WEBHOOK_URL`）；底座另有 `shared_tools/notifiers/wecom.send_markdown`。

### 5.6 专员跟进
- **跟进信**归集 `6-人才与组织/部门AI专员跟进/`，命名 `部门-姓名-跟进-YYYY-MM-DD-主要事项.md`，每封含**做什么／怎么做／什么时候交**，发一封在 README 清单追加一行；编号 `部门#N`（跨收信人共用计数器、未发出不占号、已发旧信不改名）。**口径／需求确认唯一格式＝判例批改法**：≤10 条真实案例「现状判定 vs 拟改判定」对照，专员只做 ✅/❌/✏️＋一句话；🔴 **判据／口径／阈值类永不默认生效**（纯展示类可标 48h 默认生效）；不加并行专员。生成器＝skill `zhuopin-followup-letter`。 ⇒ 全文见 `6-人才与组织/CLAUDE.md` §一，正本＝该目录 README ＋ `需求确认方式升级-判例批改法与微会机制-2026-07-25.md`。
- 🔴 **串行原则（优先于任何「某动作完成即触发发信」的规则）**：同一收信人同时只能有一封在途；下一封起草前提＝前一封已回件且回灌消化完毕；起草后提交 Shao Peishen 审核后发。**判闸唯一入口＝`python 0-学习与工具/工具-跟进闸查询.py --to <收信人>`**；闸锁即不得起草，改在队列登记「待前信闭环后发」＋要点。
- 🔴 **信状态唯一权威＝跟进信 README「发送状态」列**：队列里「等某某#N 闭环／待回件」类复述是会过时的快照，不得作判据，队列只写指针。 ⇒ 设计正本 `跟进信状态单一可信源-架构设计-2026-08-21.md`。发送器＝skill `zhuopin-send-followup`。

### 5.7 场景建造
- 🔴 **新场景一律不新起端口**：注册到统一门户路由 `/{域}/{场景}`（如 `/procurement/sc7`），预留网关 auth 接入点（可空壳，路由与接入点必须留）；后端保持独立服务不合并进程。 ⇒ `3-治理与合规/统一门户架构决策件-SSO与权限-2026-07-29.md`。
- **机制／工具类模块的 openspec 门槛**：命中任一即必走 openspec（含 design 审）：① 改变全项目口径（取号／编号／判据／状态语义）；② 涉鉴权与数据可见性；③ 改变既有模块对外语义。不走：纯 bugfix／告警文案／日志措辞／单测补充／纯文档清理。**判不准就走。**
- **每个场景固定流程**：🔴 **第 1-7 步：动 `4-数字员工/` 前先读 `4-数字员工/CLAUDE.md`**。**第 8 步 · 发布即刻起草跟进信（不得省略）**：🔴 先查串行闸再决定起不起草：⑴ 闸开 ⇒ 当场起草，README 登记行只写 `⏳ 待你审`；⑵ 闸锁 ⇒ 不起草，队列登记「待前信闭环后发」＋要点；⑶ 已起草者提交审核，批准后方可发。🔴 **发送硬前置三条缺一不得发**：① 改动已 ff 合入 master（`git rev-list --count master..<分支>`＝0）；② `.51` 已部署且冒烟通过（`/api/ping`／关键页 200／一次全量重算）；③ 用专员原始举证的真实案例端到端复现显示修正值。**串行管前一封是否闭环，本条管这一封是否已上线，两条都过才发。** 机制侧强制＝队列 #229；README 单一可信源机制行＝#366。 ⇒ 辨析见 `6-人才与组织/CLAUDE.md` §四。
- **完工即归档**：变更包 tasks 全 [x] 后当场 `/opsx:archive <change-name> -y`；「代码完成但未归档」不得跨 1 个 session。🔴 不归档时理由须写在机器认得的地方（`暂不归档`／`预期观察窗口：N 天`／`--ack-stale-change` 三选一），判定器＝`0-学习与工具/工具-变更包自动归档.py`。 ⇒ 用法见 `专线opener模板库.md` §〇.16。
- **发布即收口**：收口标准＝「最小 MVP 已部署 `.51` 且部署段测试通过」，非「代码跑通」。四关：① 全量测试绿＋零回归＋黄金基准不漂移＋openspec 归档＋场景 CLAUDE.md 更新；② 真部署 `.51`＋冒烟＋回滚 SOP＋可常驻；③ 两道门禁照旧（只归档／通知不自动执行、L2 人工确认），发布≠放开 AI 自动执行业务；④ 先灰度后依赖。统一入口＝`1-转型规划/AI运营指挥中心/`。
- **外部对抗性评审**：⏰ **2026-11 中旬前提醒 Shao Peishen 排 S1 收口外部评审**（S2 ≈2027-05、S3 ≈2027-11）。细则见 `1-转型规划/0-全景路线图/CLAUDE.md`。
- **决策代理**：Shao Peishen 缺席时**孙涛**代理——可代 design 审、批间 Triage、L2 日常审批；不可代对客外发开闸（`CUSTOMER_OUTBOUND_ENABLED`）、预算、合规红线。CC 侧 `VP_APPROVERS` 须加孙涛（待办）。

## 6. supplychain 收割（指针）

收割已完成、源仓库已 tag 只读；模块迁移表见 `1-转型规划/0-全景路线图/supplychain收割与全景推进策略.md`。

## 7. 合规红线（IATF 16949 / ISO 26262）

1. 先 mock／脱敏跑通逻辑，再切真实库。
2. 所有 AI 决策写平台 `audit`（append-only，3 年留存）。
3. **OEM 数据隔离**：涉 OEM 技术数据按客户路由、禁跨库（研发／知识库＋质量域 PPAP/FMEA/含 OEM 的 8D，见 §4）。
4. **L2 人工确认门禁**：采购金额 > 50 万、新供应商、交付预测推客户——不可自动执行。
5. **ISO 26262**：AI 生成不得直接合入，须人工审核。**ASIL C/D ＝ AI 绝对禁区**（不参与自动修改／最终判定、不进安全证据链，FSE 双签）；A/B 可 AI 辅助＋资质人员签字。 ⇒ `3-治理与合规/ISO26262-AI安全使用规范（草案）.md`。

---
**Last Updated**: 2026-09-03　本行只写日期与一行指针；历史见 CHANGELOG §「附录 · Last Updated 链历史」。
