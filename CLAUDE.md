# CLAUDE.md — 卓品智能 AI 转型（项目级记忆）

> 项目级上下文（Hermes L1）。Claude Code / Cowork 进入本仓库先读它。全局身份见 `~/.claude/CLAUDE.md`。代码与注释用中文，术语保留英文，功能用供应链业务语言描述（齐套分析／承诺交期／在途跟踪）。
> **本文件只留常驻纪律＋路由表；场景化纪律在 `.claude/rules/`（§4 路由表）。每条只写「触发 → 动作 → 判据／指针」；谁定、何时定、成因、实证一律在 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md`（附录 A–H），本文件不复述。**

> **当前进度**：本段只留最近一批，当前＝空。🔴 迁出前置判据 J1：**迁出前须确认该条已有承接载体**（队列行号，或具名文件＋章节）。条目数／长度／J1／字节／批次跨度已机器守（`工具-CLAUDE进度段lint.py` ＋ `工具-落库sweep.py` 第 4 类告警）。
> **📦 历史批次指针**：全部进度原文在 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md` 各批次节（可 grep、原文未改写）；瘦身前根文件全文＝附录 H。
> **memory 层**：Cowork 侧**完全忽略**；CC 侧 auto-memory 只记技巧、**纪律口径判据一律不入**（判别一句：违反会做错事 ⇒ 纪律，不入）。纪律的权威载体只有本文件、`.claude/rules/` 与两份队列。细则在 rules/两桌同步与取证 §四。
> 🔑 **凡「已复核／已回读／已比对」类自陈，必须同时写出用什么核的（工具名／命令／哈希）；只有动作没有手段的验证声明 ＝ 没有验证。**

---

## 1. 公司与项目背景

- **公司**：卓品智能科技股份 — 汽车 ECU 设计研发制造 Tier 1，直供比亚迪／上汽／理想等 OEM。
- **项目**：18 个月企业 AI 转型（2026-07 → 2027-12 ＝ Phase 1，S1/S2/S3 三段；Phase 2 ＝ 2028 H1 产品工程跃升，不并入），六部门并行，共 **35 个数字员工场景**（质量域在建只剩 Q2／Q4，Q6 已交付＝QD-B）。🔴 **编号 Q1／Q3／Q5／Q7／Q8 已空置、不得重新分配**；下架场景复苏由 Shao Peishen 手动触发。
- **决策人**：**Shao Peishen**（分管供应链与质量的 OPVP）。**称呼一律 `Shao Peishen`，不用 `Paul`**；🔴 **绝不替换路径里的 `Paul Shao`**；历史记录不追改。缺席时**孙涛**代理（范围见 rules/场景建造与合规）。<!-- UPS5:1 -->
- 🔴 **人的属性＝硬事实**：提到任何具名人物前先读 `6-人才与组织/人员名录-称谓与性别-正本.md`；**禁从名字推断性别**，正本之外一律「该专员／其／对方」并当场问一次。判别特征＝「能从字面猜出来、且猜错了不会当场报错」——此类属性（性别／职务／称谓／汇报关系／审批权限）一律以权威载体为准。<!-- UPS5:2 -->
- **节奏**：先跑通最小验证，再规模化；先 mock／脱敏，再切真实库。
- **权威总纲**：`1-转型规划/0-全景路线图/卓品智能AI转型全景规划.md`（改正文必登 §0.2，否则无效）＋ `卓品智能AI转型实施计划（最新版）.md` 第七节。**U9C 已覆盖的标准功能直接用，不建 AI。**

## 2. 仓库结构

```
企业AI转型/  (GitHub: Raytheoner/zhuopin-ai-transformation)
├── .claude/rules/        ★场景化纪律（CC 按 paths 自动加载；Cowork 按 §4 路由表 Read）
├── 0-学习与工具/         取证知识库、md 转 Word、hooks、skills 源码、队列/锁/sweep 工具
├── 1-转型规划/0-全景路线图/  ★全景规划(权威)+实施计划+CHANGELOG+跨桌任务队列+opener 模板库
├── 2-试点项目/  3-治理与合规/  6-人才与组织/(名录正本·专员跟进)  7-外部文档/<部门>/
├── 4-数字员工/部门/场景名/   各场景独立 Python 工程，import 平台底座
└── 5-平台底座/zhuopin_platform/  可安装 Python 包
```

## 3. 开场与接力（每个 session）

- **接力＝读文件，不粘长 prompt**：开场读 ① 本文件 → ② `1-转型规划/0-全景路线图/session接力-Phase1收口.md`（或本线接力卡）→ ③ 按 §4 路由表读本次任务命中的规则文件；开干前问 2-3 个澄清（A 类同线转场先自检三查、不问）。**收工**把进展＋下一步滚动覆盖进接力卡（标日期，本机 `Get-Date` 取），未闭合项进队列行。
- **开工必读两份队列、收工必写**（认领＋触碰区；状态＋产出路径＋下游追行；CC 开场横幅自动带 §一待领摘要）。🔴 **读队列只用 `工具-队列查询.py --digest／--row N`，不得 Read／grep 真身**（一行可达 78 KB）；commit 由 `ZhuopinCommitSweep` 从 §二批次自动取活——登记完 §二即完事。细则在 rules/队列与落库。
- 🔴 **产出任何 opener／交接 prompt／要粘进 CC 的指令块** ⇒ 先 Read `1-转型规划/0-全景路线图/opener骨架.md` **逐字套用**，复制替换占位符——**格式唯一来源，不凭记忆、不凭 skill、不凭模板库重建**（模板库 §〇 是判据、不是格式）；步骤与自检＝skill `zhuopin-kickoff-prompt`（v1.26 起不含格式）。

## 4. 路由表：做什么之前先读什么

🔴 **Cowork 不会自动加载 `.claude/rules/` 里带 `paths` 的文件，也不自动加载子目录 `CLAUDE.md`——必须按本表显式 Read；CC 触碰对应路径会自动注入，但 Cowork 起草、CC 执行的任务两边都要读。**

| 触发（本次任务会碰到…） | 先读 |
|---|---|
| 具名人物／跟进信／判例包／发信前置／`6-人才与组织` `7-外部文档` | `.claude/rules/跟进信与专员.md` ＋ `6-人才与组织/CLAUDE.md` |
| `4-数字员工`／`5-平台底座`／openspec／部署 `.51`／合规红线（OEM 隔离·L2 门禁·ASIL） | `.claude/rules/场景建造与合规.md` ＋ `4-数字员工/CLAUDE.md`／`5-平台底座/CLAUDE.md` |
| 两份队列／接力卡／§二批次／取号／编辑锁／CC 复命取件／环境保障线派单 | `.claude/rules/队列与落库.md`（协议〇正本在队列顶部） |
| 全景规划／实施计划／排期变更／重组循环／文档命名归档／台账 | `.claude/rules/文档与全景治理.md` ＋ `1-转型规划/0-全景路线图/CLAUDE.md` |
| git 同步／fsck／乱码哨兵／时间戳／取证／直读／企微推送／执行环境判别 | `.claude/rules/两桌同步与取证.md` ＋ `0-学习与工具/取证方法知识库.md` |

## 5. 常驻纪律（两桌全局、每次回复都适用）

- 🔴 **会话末「需你定夺」**（CC 侧 Stop hook ⓓ 机器守；Cowork 人守）：回复末尾固定小节＋编号清单；**每项必须是可直接作答的是非题或选择题**：① 带字母标签 `(a)/(b)/(c)` 或「是/否」；② 每个选项写清「选它会发生什么」与代价；③ **两栏各归各位**——状态另起「**状态同步（无需你答）**」，任一方向串栏都算违规，判据：凡出现「等你定／请你选／待你拍板」即属「需你定夺」；④ 标「默认项」（若不答按 (x) 执行）。临期项标日期置顶；此前未答复的悬置项以选择题形式重提；无决策项时明写「本次无需你决策」。<!-- UPS5:3 -->
- 🔴 **默认项只在两个前提下成立**：设前自问 ① 他不答，谁去执行？② 「不答」的代价是「停在原地」还是「错误继续发生」？答不出执行者、或是后者 ⇒ **不设默认项**，改标「本项无默认，须你明确答复」并登记 §四；并查有无下游依赖被冻住。<!-- UPS5:5 -->
- 🔴 **决策路由（只问一句：答完之后由谁动手？）**：本 session 接着做 ⇒ **⟨就地答⟩**给选项＋默认项；要另起 session／归他线／对外发送／改他人触碰区 ⇒ **不给选项**，只写「已登记 §一 #N（或 §四 #N），待总线派发」＋一句结论。报告顶部加一行 `▶ 本报告 N 项定夺：X 项⟨就地答⟩，Y 项已登记队列待总线派发`。判定责任在写报告的一方（总线答复后的两个配套动作见 rules/队列与落库）。
- 🔴 **输出三条**：① 可粘贴的 prompt 必标粘贴端 `▶ 粘贴端：Cowork／CC`；② 他亲自要做的动作 ≥2 个，按序编号并标「按序／可并行／可缓」；③ 路径写仓库根相对路径（唯一例外＝本机取证命令用绝对路径）；每个问题给 1 个最优建议——选择题也必须同时给推荐项。≥2 个 opener／派单件必附次序与并行矩阵。<!-- UPS5:4 -->
- **建议未获答复须再次提醒**：建议牵动后续动作而未答 ⇒ 下次交互重提，不得当默认通过。**判据：建议 vs 状态，机制只认后者**——涉归属／优先级／范围／排期的建议，要么当场改状态字段，要么标「待他确认」，不留中间态。收工自检：本次的「建议…」哪些未答却已影响他处？逐条重提。
- 🔴 **写日期一律本机 `Get-Date` 当场重取；引用任何时刻先判 UTC 还是本地并标基准。** 🔴 **只读命令结果「太干净」先怀疑没读到对象；管道末端的退出码不是命令的退出码。**（全文在 rules/两桌同步与取证）
- 🔴 **执行环境标注**：任务／opener 标题必标 `【Cowork】`／`【CC】`，`【设置】` 行同步写；判别与工具链例外见 rules/两桌同步与取证 §一。
- 🔴 **合规一句话版**：mock 先行、AI 决策全写 `audit`、OEM 数据禁跨库、L2 门禁（>50 万／新供应商／交付预测推客户）不可自动执行、**ASIL C/D ＝ AI 绝对禁区**。全文在 rules/场景建造与合规 §三。
- **规则退休制**：「人守」条目被违反 3 次即机制化或删除（rules/队列与落库）。**机制优先**：能等的交给机制，不能等的用机制的方式做，不裸手改。

---
**Last Updated**: 2026-09-04　本行只写日期与一行指针；历史见 CHANGELOG §「附录 · Last Updated 链历史」。
