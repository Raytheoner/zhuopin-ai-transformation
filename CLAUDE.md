# CLAUDE.md — 卓品智能 AI 转型（项目级记忆）

> 项目级上下文（Hermes L1），进入本仓库先读。全局身份见 `~/.claude/CLAUDE.md`。代码与注释用中文、术语保留英文、功能用供应链业务语言描述（齐套分析／承诺交期／在途跟踪）。
> **本文件只留常驻纪律＋路由表；场景化纪律在 `.claude/rules/`（§4）。每条只写「触发 → 动作 → 判据／指针」；谁定、何时定、成因、实证一律在 `1-转型规划/0-全景路线图/进度编年-CHANGELOG.md`（附录 A–N），本文件不复述。**

> **当前进度**：本段已迁 `进度编年-CHANGELOG.md` **附录 P**（2026-09-20 `OP-0919-K`，本文件当时超阈值 860 B，该段是最大单块）。🔴 此后本段只留指针，新批次直接写进附录 P，不回填到这里。
> **memory 层**：Cowork **完全忽略**；CC auto-memory 只记技巧、**纪律口径判据一律不入**（判别一句：违反会做错事 ⇒ 纪律，不入）。权威载体只有本文件、`.claude/rules/` 与两份队列（细则在 rules/两桌同步与取证 §四）。
> 🔑 **凡「已复核／已回读」类自陈，须同时写出用什么核的（工具名／命令／哈希）；只有动作没有手段 ＝ 没有验证。**

---

## 1. 公司与项目背景

- **公司**：卓品智能科技股份，汽车 ECU 研发制造 Tier 1，直供比亚迪／上汽／理想等 OEM。
- **项目**：18 个月企业 AI 转型（2026-07→2027-12＝Phase 1，S1/S2/S3 三段；Phase 2＝2028 H1 产品工程跃升不并入），六部门并行，共 **35 个数字员工场景**（质量域在建只剩 Q2／Q4，Q6 已交付＝QD-B）。🔴 **编号 Q1／Q3／Q5／Q7／Q8 已空置、不得重新分配**；下架场景复苏由 Shao Peishen 手动触发。
- **决策人**：**Shao Peishen**（分管供应链与质量的 OPVP）。**称呼一律 `Shao Peishen`，不用 `Paul`**；🔴 `Paul` 只在两处保留：① 真实路径 `C:\Users\Paul Shao\…`；② 计划任务身份字面量 `Paul Shao / S4U`——余皆 `Shao Peishen`／`Peishen`，文件名亦然。🔴 **历史记录不追改**——**已发生事实的原文**不改，**当下生效的指针**改（成因见 CHANGELOG 附录 N）。缺席时**孙涛**代理（范围见 rules/场景建造与合规）。<!-- UPS5:1 -->
- 🔴 **人的属性＝硬事实**：提到具名人物前先读 `6-人才与组织/人员名录-称谓与性别-正本.md`；**禁从名字推断性别**，正本之外一律「该专员／其／对方」并当场问一次。判别特征＝「能从字面猜出来、且猜错了不会当场报错」——性别／职务／称谓／汇报关系／审批权限一律以权威载体为准。<!-- UPS5:2 -->
- **节奏**：先验证再规模化；先 mock／脱敏再切真实库。
- **权威总纲**：`1-转型规划/0-全景路线图/卓品智能AI转型全景规划.md`（改正文必登 §0.2）＋ `卓品智能AI转型实施计划（最新版）.md` 第七节。**U9C 已覆盖的标准功能直接用，不建 AI。**

## 2. 仓库结构

```
企业AI转型/  (GitHub: Raytheoner/zhuopin-ai-transformation)
├── .claude/rules/        场景化纪律（CC 按 paths 自动加载；Cowork 按 §4 路由表 Read）
├── 0-学习与工具/         取证知识库、md 转 Word、hooks、skills 源码、队列/锁/sweep 工具
├── 1-转型规划/0-全景路线图/  全景规划+实施计划+CHANGELOG+跨桌任务队列+opener 模板库
├── 2-试点项目/  3-治理与合规/  6-人才与组织/  7-外部文档/<部门>/
├── 4-数字员工/部门/场景名/   各场景独立 Python 工程，import 平台底座
└── 5-平台底座/zhuopin_platform/  可安装 Python 包
```

## 3. 开场与接力（每个 session）

- **接力＝读文件，不粘长 prompt**：开场读 ① 本文件 → ② `1-转型规划/0-全景路线图/session接力-Phase1收口.md`（或本线接力卡）→ ③ 按 §4 路由表读本次任务命中的规则文件；开干前问 2-3 个澄清（A 类同线转场先自检三查、不问）。**收工**把进展＋下一步滚动覆盖进接力卡（标日期，`Get-Date` 取），未闭合项进队列行。
- **开工必读两份队列、收工必写**（认领＋触碰区；状态＋产出路径＋下游追行）。🔴 **读队列只用 `工具-队列查询.py --digest／--row N`，不得 Read／grep 真身**（一行可达 78 KB）；commit 由 `ZhuopinCommitSweep` 从 §二批次自动取活——登记完 §二即完事。细则在 rules/队列与落库。
- 🔴 **产出任何 opener／交接 prompt** ⇒ 先 Read `1-转型规划/0-全景路线图/opener骨架.md` **逐字套用**——**格式唯一来源，不凭记忆重建**；步骤与自检＝skill `zhuopin-kickoff-prompt`。
- 🔴 **转场由本方主动提醒，不等他问（两桌全局）**：**收工自检逐条过 skill `zhuopin-kickoff-prompt`「转场判据」五条**（忘记早先事实／重复已做过的工作／前后矛盾未标更正／丢失或曲解指令／token 压力真实且压缩手法用尽）——**任一阳性 ⇒ 当场提醒并附引用版接力 opener；全阴性 ⇒ 明说「不必转场」**，不得沉默。**「上下文长」不是判据**（成因见 CHANGELOG 附录 N）。🔴 **无头泳道**：上下文越过 150k（`hooks-posttooluse-context-meter` 提醒）⇒ 完成当前里程碑即收尾，以 `OPENER_PARTIAL: 上下文转场` 收尾。

## 4. 路由表：做什么之前先读什么

🔴 **Cowork 不自动加载 `.claude/rules/` 或子目录 `CLAUDE.md`——必须按本表显式 Read；CC 触碰对应路径自动注入，但两边都要读。**

| 触发 | 先读 |
|---|---|
| 具名人物／跟进信／`6-人才与组织` `7-外部文档` | `.claude/rules/跟进信与专员.md` ＋ `6-人才与组织/CLAUDE.md` |
| `4-数字员工`／`5-平台底座`／openspec／`.51`／合规红线（OEM 隔离·L2 门禁·ASIL） | `.claude/rules/场景建造与合规.md` ＋ `4-数字员工/CLAUDE.md`／`5-平台底座/CLAUDE.md` |
| 两份队列／接力卡／§二批次／取号／编辑锁／CC 复命取件 | `.claude/rules/队列与落库.md`（协议〇正本在队列顶部） |
| 环境保障线派单／规则退休／机制优先／规划倒逼／新建告警 | `.claude/rules/环境保障线.md` |
| 全景规划／实施计划／排期变更／重组循环／文档归档／台账 | `.claude/rules/文档与全景治理.md` ＋ `1-转型规划/0-全景路线图/CLAUDE.md` |
| git 同步／fsck／乱码哨兵／时间戳／取证／直读／企微推送／执行环境判别 | `.claude/rules/两桌同步与取证.md` ＋ `0-学习与工具/取证方法知识库.md` |

## 5. 常驻纪律（两桌全局、每次回复都适用）

- 🔴 **会话末「需你定夺」**（CC 侧 Stop hook 机器守；Cowork 人守）：末尾固定小节＋编号清单，每项须可直接作答（`(a)/(b)/(c)` 或是否）＋写清各选项代价；**状态另起「状态同步（无需你答）」，两栏不得串栏**（判据：出现「等你定／请你选」即属需你定夺）；标默认项；**末尾附答复模板且必须单独成代码块**（用复制按钮一键取走，不得只写正文一行；成因见 CHANGELOG 附录 N）。判别一句：**凡需他原样回传的内容，一律给可一键复制的块。**临期项置顶；无决策项写「本次无需你决策」。<!-- UPS5:3 -->
- 🔴 **默认项只在两个前提下成立**：① 他不答谁去执行？② 不答代价是「停在原地」还是「错误继续发生」？答不出执行者、或是后者 ⇒ **不设默认项**，标「本项无默认，须你明确答复」并登记 §四。🔴 **超时生效仅限 `zhuopin-requirement-grill` 问答轮**：模板发出 **15 分钟无答复 ⇒ 按模板执行**（design 审／「本项无默认」的项／本 skill 以外场合除外），须写「超时按默认项生效（未获明确答复，<OP 号> <时刻>）」，不得写成「他当场答定」。细则见该 skill `SKILL.md` §二。<!-- UPS5:5 -->
- 🔴 **代执行优先（两桌全局；排在决策路由之前）**：**凡本方能执行的，直接执行、只告知结果，不生成需要他动手的中间件。** 判据：**这件事除了我，还需不需要他的手或判断？** 不需要 ⇒ 只报「已做／手段／结果」；需要 ⇒ 才进「需你定夺」。🔴 **红线不因本条放松**：🔴 档（对外发送／L2 签字／合规红线／ASIL C-D）、⏭️ 档（`.51` 部署及生产服务）、🟡 档（ff 入 master／改口径阈值／design 审／关他人在办行）**照旧永不代办**。**执行前须重跑前置实测**（现取表不作数）；**代执行后照旧走锁登记 §二**并写清手段。🔴 **收窄一档：他不做人肉搬运**——手工搬运类不得作为建议出现；**唯二可交给他的动作＝可复制粘贴的命令行、以及必须提权的 shell**；「机器无法代」须先穷举通道（PowerShell／计划任务／计算机使用／浏览器工具）、实测不通再下此结论（成因见 CHANGELOG 附录 N）。<!-- UPS5:6 -->
- 🔴 **会话不自醒；说「自动」必须当场指名机器规则**：**Cowork／CC 会话只在他发消息时被唤醒**，本方的记性不构成触发器。Cowork 有 `mcp__scheduled-tasks__*`（cron／`fireAt`，`notifyOnCompletion` 会回调进本会话）——**缺的从来不是能力，是有能力却不建**。🔴 **禁「我盯着…」「一出来就自动…」这类表述**——说出口就不成立，改为**明确交回**：「**X 完成后请说一句『Y』**」。🔑 **判据：Y 的触发器只能是 ① 他的下一句话，或 ② 能当场指名的机器规则（`taskId`／hook／脚本路径），不能是本方记性**；指不出即空头承诺（成因见 CHANGELOG 附录 N；承接行＝§一 `#551`）。<!-- UPS5:7 -->
- 🔴 **决策路由（只问一句：答完之后由谁动手？）**：本 session 接着做 ⇒ **⟨就地答⟩**给选项＋默认项；另起 session／归他线／对外发送／改他人触碰区 ⇒ **不给选项**，只写「已登记 §一/§四 #N，待总线派发」（配套动作见 rules/队列与落库）。
- 🔴 **输出三条**：① 可粘贴 prompt 必标 `▶ 粘贴端：Cowork／CC`；② 他亲自要做的动作 ≥2 个按序编号标「按序／可并行／可缓」；③ 路径写仓库根相对路径。每题给 1 个最优建议。<!-- UPS5:4 -->
- **建议未获答复须再次提醒**：建议牵动后续动作而未答 ⇒ 下次交互重提，不得当默认通过。**判据：建议 vs 状态，机制只认后者**——涉归属／优先级／范围／排期，要么改状态字段，要么标「待他确认」。
- 🔴 **写日期一律本机 `Get-Date` 当场重取；引用时刻先判 UTC／本地并标基准。** 🔴 **只读命令「太干净」先怀疑没读到对象；管道末端退出码不是命令退出码。**（全文在 rules/两桌同步与取证）
- 🔴 **执行环境标注**：任务／opener 标题必标 `【Cowork】`／`【CC】`，`【设置】` 行同步写（例外见 rules/两桌同步与取证）。
- 🔴 **模型路由默认 sonnet**：无头链路／看护／巡检／批量跑测走 sonnet；design 起草、需求 grill、ASIL／合规建造可在 opener【设置】行写「模型：opus」覆盖。
- 🔴 **合规一句话版**：mock 先行、AI 决策全写 `audit`、OEM 数据禁跨库、L2 门禁不可自动执行、**ASIL C/D ＝ AI 绝对禁区**。全文在 rules/场景建造与合规 §三。
- **规则退休制**：「人守」违反 3 次即机制化或删除（rules/环境保障线）。**机制优先**：能等的交给机制，不能等的用机制方式做，不裸手改。🔴 **新建告警／巡检须自带消费者**——一条机器动作或一个具名承接行，两样都没有不许上线。

---
**Last Updated**: 2026-09-16　历史见 CHANGELOG 附录·Last Updated 链历史。
